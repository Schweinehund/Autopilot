---
phase: 139-governance-carve-fetch-depth-retrofit-shallow-job-repair
plan: 05
type: execute
wave: 5
depends_on: ["139-04"]
files_modified:
  - .github/workflows/audit-harness-integrity.yml
  - .github/workflows/audit-harness-v1.5-integrity.yml
  - .github/workflows/audit-harness-v1.6-integrity.yml
  - .github/workflows/audit-harness-v1.7-integrity.yml
  - .github/workflows/audit-harness-v1.8-integrity.yml
  - .github/workflows/audit-harness-v1.9-integrity.yml
  - .github/workflows/audit-harness-v1.10-integrity.yml
  - .github/workflows/audit-harness-v1.11-integrity.yml
  - .github/workflows/audit-harness-v1.12-integrity.yml
  - .github/workflows/audit-harness-v1.13-integrity.yml
  - .github/workflows/audit-harness-v1.14-integrity.yml
  - .github/workflows/audit-harness-v1.15-integrity.yml
  - .github/workflows/audit-harness-v1.16-integrity.yml
  - .github/workflows/audit-harness-v1.17-integrity.yml
  - .github/workflows/audit-harness-v1.18-integrity.yml
  - .github/workflows/audit-harness-v1.19-integrity.yml
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: true
requirements: [SWEEP-01, SWEEP-02, GOV-02]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every `actions/checkout@v4` step across all 16 `.github/workflows/audit-harness-*.yml` files carries `fetch-depth: 0` — 97 previously-shallow checkouts converted, 182 total, verified repo-wide rather than per-file (D-13/D-15)."
    - "The invariant is stated repo-wide, not per-file: a per-file equality form was false for 13 of the 16 files under any partial scope (D-15)."
    - "Every `fetch-depth: 0` line is the line immediately following its own `uses: actions/checkout@v4` line, so no step's key is attributed to a neighbouring step, proven by an adjacency scan rather than a bare count."
    - "No checkout step gains a duplicate `fetch-depth` key or a second `with:` mapping, and every one of the 16 files still parses as valid YAML."
    - "The repo-wide invariant holds trivially for any workflow with zero checkout steps, and no `fetch-depth` key is added to a non-checkout step."
    - "The grep pattern used to verify carries the space — the no-space form matches 13 header-comment prose lines that are not YAML keys (D-15)."
    - "`audit-harness-integrity.yml`'s `paths:` filter includes `.github/workflows/**`, so it watches its own file (D-17)."
    - "Each of the 16 workflows carries exactly one `frozen-read-probe` job with NO `needs:` key, so it is immune to the `needs: harness-run` skip fan-out (D-24)."
    - "Each probe job performs a raw frozen `git show` read, one real `readAtClose` call, and one real `lsTreeAtClose` call — exercising the library, not just a bare git command."
    - "With any subset of the 97 checkouts edited and uncommitted, `check-phase-69.mjs` and `check-phase-70.mjs` both exit 0 — the frozen-to-frozen pins from Plan 04 are worktree-independent."
    - "The 16 workflow edits plus the probe jobs plus the `paths:` fix land in ONE commit (D-41 atom 5)."
    - "Every additive-only required-substring assertion over these workflows still passes: `check-phase-66`'s v1.6 path-filter list, `check-phase-69`'s v1.7 job/substring set, and `check-phase-70`'s v1.7 reference checks."
  prohibitions:
    - requirement_id: SWEEP-02
      category: transparency
      statement: "MUST NOT make a job appear green by masking — no `continue-on-error`, no `if: always()`, and no error-suppressing shell fallback may be added to any probe job; a probe that cannot read must fail the job."
  artifacts:
    - path: ".github/workflows/audit-harness-integrity.yml"
      provides: "4 deep checkouts + frozen-read-probe job + self-watching paths filter"
      contains: "frozen-read-probe"
    - path: ".github/workflows/audit-harness-v1.5-integrity.yml"
      provides: "18 deep checkouts + frozen-read-probe job"
      contains: "frozen-read-probe"
    - path: ".github/workflows/audit-harness-v1.6-integrity.yml"
      provides: "10 deep checkouts + frozen-read-probe job"
      contains: "frozen-read-probe"
  key_links:
    - from: ".github/workflows/audit-harness-*.yml frozen-read-probe jobs"
      to: "scripts/validation/_lib/frozen-at-close.mjs"
      via: "each probe imports the library and calls readAtV15Close and lsTreeAtV15Close for real"
      pattern: "frozen-at-close"
    - from: "every actions/checkout@v4 step"
      to: "the full git object graph"
      via: "with: { fetch-depth: 0 } on the immediately following line"
      pattern: "fetch-depth: 0"
---

<objective>
Atom 5 of D-41, in one commit: the 97-checkout `fetch-depth: 0` sweep across all 16 workflows
(D-13/D-15/D-16), one `frozen-read-probe` job per workflow with no `needs:` (D-24), and the
`paths:` filter fix on the base workflow (D-17).

This is an owner-ratified success-criterion amendment, recorded by Plan 01 Task 3, not a
clarification: the scope is all 97 shallow checkouts repo-wide, not the 32 in the three named
files and not an intermediate 45. Every one of the 16 workflows carries a shallow `harness-run`,
and Phase 140's SWEEP-05 converts harnesses v1.4 through v1.19 to frozen reads — so the 13
unnamed `harness-run` jobs would hard-crash under a partial scope, and a partial scope pays the
same amendment price while leaving 52 shallow checkouts and an uncheckable invariant.

Uniform coverage is correct because `fetch-depth: 0` is MONOTONIC (D-16): it strictly widens the
available object graph and can change no job's semantics, only runtime and disk. Cost is bounded
by a roughly 23 MiB pack.

Purpose: SWEEP-01 and SWEEP-02's evidence surface. Output: 16 workflows edited in one commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-CONTEXT.md
@.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-PATTERNS.md
@.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-04-SUMMARY.md
</context>

## Artifacts this phase produces

See the table in `139-01-PLAN.md` → "Artifacts this phase produces". This plan creates the
`frozen-read-probe` CI job name, 16 instances.

<tasks>

<task type="auto">
  <name>Task 1: Sweep all 97 shallow checkouts across 16 workflows + the paths filter fix</name>
  <precondition>`node scripts/validation/check-phase-69.mjs` and `node scripts/validation/check-phase-70.mjs` both exit 0 with one of the three named workflow files dirty in the worktree — Plan 04's frozen-to-frozen conversion is in place, so editing these files cannot take the apex red.</precondition>
  <read_first>
    - `.github/workflows/audit-harness-v1.7-integrity.yml` — the already-converted deep-checkout idiom to replicate exactly; read the `linux-chain-ubuntu-latest` job and one bare `check-phase-NN` job to see both shapes, and read the header comment block to see the no-space prose form that the verification grep must not match
    - `.github/workflows/audit-harness-integrity.yml` — the full file; the `paths:` filter at the top is the D-17 target and all 4 of its checkouts are shallow
    - `.github/workflows/audit-harness-v1.5-integrity.yml` and `-v1.6-integrity.yml` — the other two all-shallow files, 18 and 10 checkouts
    - `scripts/validation/check-phase-66.mjs` lines 40-45 and 150-180 — `V-66-05`'s required-substring list over the v1.6 workflow; every entry must survive
    - `scripts/validation/check-phase-69.mjs` lines 45-135 — `V-69-01` through `V-69-07`, all required-substring presence checks over the v1.7 workflow
    - `scripts/validation/check-phase-70.mjs` lines 300-340 — `V-70-16`, which asserts the v1.7 workflow's `parse`/`path-match`/`harness-run` jobs reference v1.7 and carry ZERO references to the earlier milestone token; any text added to that file must not introduce one
  </read_first>
  <files>.github/workflows/audit-harness-integrity.yml, .github/workflows/audit-harness-v1.5-integrity.yml, .github/workflows/audit-harness-v1.6-integrity.yml, .github/workflows/audit-harness-v1.7-integrity.yml, .github/workflows/audit-harness-v1.8-integrity.yml, .github/workflows/audit-harness-v1.9-integrity.yml, .github/workflows/audit-harness-v1.10-integrity.yml, .github/workflows/audit-harness-v1.11-integrity.yml, .github/workflows/audit-harness-v1.12-integrity.yml, .github/workflows/audit-harness-v1.13-integrity.yml, .github/workflows/audit-harness-v1.14-integrity.yml, .github/workflows/audit-harness-v1.15-integrity.yml, .github/workflows/audit-harness-v1.16-integrity.yml, .github/workflows/audit-harness-v1.17-integrity.yml, .github/workflows/audit-harness-v1.18-integrity.yml, .github/workflows/audit-harness-v1.19-integrity.yml, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <action>
**(a) GOV-02 target-scoped grep first (D-12).** Search `scripts/validation/` for each of the 16
workflow PATH literals and for the symbols `CI_WORKFLOW`, `V17_WORKFLOW`, `REQUIRED_JOBS`, and
`fetch-depth`. Record every hit. The known hits to expect and preserve are `check-phase-66.mjs`'s
v1.6 path literal plus `V-66-05`'s five required substrings, `check-phase-69.mjs`'s v1.7 path
literal plus `V-69-02` through `V-69-07`'s required substrings, and `check-phase-70.mjs`'s v1.7
path literal plus `V-70-16`. All are ADDITIVE-safe presence checks — none asserts an exact job
set or an exact line count — but confirm that empirically before editing, not by assumption.

**(b) The sweep — all 97 previously-shallow checkouts across all 16 files.** Measured at HEAD:
182 `actions/checkout@v4` steps repo-wide, 85 already carrying the key, 97 shallow. The 97 split
as 4 in `audit-harness-integrity.yml`, 18 in `-v1.5-`, 10 in `-v1.6-`, and exactly 5 in each of
the other 13 files (their `parse`, `path-match`, `harness-run`, `rotting-external-quarterly` and
`pin-helper-advisory` jobs). Do not treat any of those numbers as a stopping condition — the
stopping condition is the adjacency scan in the acceptance criteria.

For each shallow step, add the key on the line immediately after its `uses:` line, using the
repo's own inline flow-mapping form: `with: { fetch-depth: 0 }` at the step's `with`-indent level.
This is 100% of the existing convention — a grep across all 16 files finds zero multi-line
`fetch-depth` blocks. Where a checkout step already carries a `with:` mapping with other keys,
MERGE the key into that mapping rather than emitting a second `with:` — a duplicate mapping key is
a YAML error and a duplicate `fetch-depth` inside one mapping is silently last-wins. Where a step
already has the key, leave it exactly as it stands.

Do not reformat, reorder or reindent anything else in any of the 16 files. The diff should consist
only of inserted lines plus the `paths:` addition and the probe jobs from Task 2.

**(c) D-17 companion edit.** Add `.github/workflows/**` to `audit-harness-integrity.yml`'s
`pull_request` `paths:` filter list. It does not currently watch its own file, which is the exact
selectively-retrofitted defect class this phase repairs.

**(d) Verify repo-wide, not per-file (D-15).** The invariant is *every `actions/checkout@v4` step
in `.github/workflows/` is followed by the key*. The per-file equality form
count-equals-count is false for 13 of the 16 files under any partial scope and must not be used as
the primary gate. When grepping to confirm, use the pattern WITH the space: the no-space form
matches 13 header-comment prose lines that are not YAML keys at all, and would report more matches
than there are real keys.

**(e) Append a GOV-02 ledger row** covering all 16 workflow paths, carrying the grep commands, the
per-needle hit counts, the pre-edit and post-edit checkout/deep counts, and the standalone exit
codes of `check-phase-66`, `check-phase-69` and `check-phase-70` after the edit.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');let bad=[],tot=0,deep=0;for(const f of fs.readdirSync('.github/workflows').filter(x=>x.endsWith('.yml'))){const L=fs.readFileSync('.github/workflows/'+f,'utf8').split('\n');for(let i=0;i<L.length;i++){if(/uses:\s*actions\/checkout@v4\s*$/.test(L[i])){tot++;if(/^\s*with:\s*\{\s*fetch-depth:\s*0\s*\}\s*$/.test(L[i+1]||'')){deep++}else{bad.push(f+':'+(i+1))}}}}if(bad.length){console.error('shallow or non-adjacent:\n'+bad.join('\n'));process.exit(1)}console.log(tot+' checkouts, '+deep+' deep, 0 shallow')"</automated>
  </verify>
  <acceptance_criteria>
    - The adjacency scan exits 0 and reports `182 checkouts, 182 deep, 0 shallow`
    - `for f in .github/workflows/*.yml; do a=$(grep -c 'actions/checkout@v4' "$f"); b=$(grep -vE '^\s*#' "$f" | grep -c 'fetch-depth: 0'); [ "$a" = "$b" ] || { echo "MISMATCH $f $a/$b"; exit 1; }; done` completes with no mismatch
    - `node -e "const fs=require('fs');for(const f of fs.readdirSync('.github/workflows').filter(x=>x.endsWith('.yml'))){const L=fs.readFileSync('.github/workflows/'+f,'utf8').split('\n');for(let i=0;i<L.length;i++){const m=L[i].match(/fetch-depth/g);if(m&&m.length>1){console.error('duplicate key '+f+':'+(i+1));process.exit(1)}}}console.log('no duplicate keys')"` exits 0
    - Every one of the 16 files still parses: `node -e "const {execFileSync}=require('node:child_process');execFileSync('node',['-e','1'])"` is replaced by an actual YAML parse of each file using the runtime available in the repo, or by `gh workflow list` succeeding against the pushed branch in Plan 06 — record which proof was used
    - `grep -c '.github/workflows/\*\*' .github/workflows/audit-harness-integrity.yml` is at least 1 (D-17)
    - `node scripts/validation/check-phase-66.mjs`, `node scripts/validation/check-phase-69.mjs` and `node scripts/validation/check-phase-70.mjs` each exit 0
    - `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-138.mjs` reports 0 FAIL
    - `git diff --stat .github/workflows/` shows insertions and effectively zero deletions outside the `paths:` list — no file was reformatted
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` gained a row naming all 16 workflow paths with pre/post checkout and deep counts
  </acceptance_criteria>
  <done>All 182 checkouts across all 16 workflows carry the key on the immediately-following line, the base workflow watches its own directory, and the three validators that assert over these files plus the full apex are green.</done>
</task>

<task type="auto">
  <name>Task 2: Add one frozen-read-probe job per workflow and land atom 5 as a single commit</name>
  <read_first>
    - `.github/workflows/audit-harness-v1.7-integrity.yml` — the bare `check-phase-67` job skeleton (checkout, setup-node, one run step, no upstream `needs:`) which the probe job's shape mirrors
    - `.github/workflows/audit-harness-v1.5-integrity.yml` — the `harness-run` job and the `check-phase-48` job, to see the `needs:` chaining this probe deliberately avoids
    - `scripts/validation/_lib/frozen-at-close.mjs` — `readAtV15Close` and `lsTreeAtV15Close` as built in Plan 02; the probe calls both for real
    - `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-CONTEXT.md` — D-24, D-25, D-26 and the Claude's-Discretion note covering the probe job's naming and step layout
  </read_first>
  <files>.github/workflows/audit-harness-integrity.yml, .github/workflows/audit-harness-v1.5-integrity.yml, .github/workflows/audit-harness-v1.6-integrity.yml, .github/workflows/audit-harness-v1.7-integrity.yml, .github/workflows/audit-harness-v1.8-integrity.yml, .github/workflows/audit-harness-v1.9-integrity.yml, .github/workflows/audit-harness-v1.10-integrity.yml, .github/workflows/audit-harness-v1.11-integrity.yml, .github/workflows/audit-harness-v1.12-integrity.yml, .github/workflows/audit-harness-v1.13-integrity.yml, .github/workflows/audit-harness-v1.14-integrity.yml, .github/workflows/audit-harness-v1.15-integrity.yml, .github/workflows/audit-harness-v1.16-integrity.yml, .github/workflows/audit-harness-v1.17-integrity.yml, .github/workflows/audit-harness-v1.18-integrity.yml, .github/workflows/audit-harness-v1.19-integrity.yml</files>
  <action>
Add exactly one job named `frozen-read-probe` to each of the 16 workflows, per D-24. Job shape,
identical in all 16 (the naming and step layout are Claude's Discretion; uniformity is the point,
because it makes the invariant a single greppable fact):

  - `runs-on: ubuntu-latest`, a human-readable `name:`, and **NO `needs:` key at all**. This is
    the load-bearing property: all 14 `check-phase-48..61` jobs in the v1.5 workflow and all 7 in
    the v1.6 workflow are `needs: harness-run`, both harnesses exit 1 at HEAD, so every one of
    them reports a skipped conclusion on any ref and can never produce SWEEP-02's evidence in
    Phase 139 — the greening depends on Phase 141. A job with no upstream dependency is immune to
    that fan-out.
  - `uses: actions/checkout@v4` followed on the next line by the same inline `with:` mapping Task 1
    applied everywhere else, then `uses: actions/setup-node@v4` with node 20, matching the
    existing jobs in the same file.
  - Step 1, raw frozen read: run `git show` for the v1.5 close SHA and the Linux glossary path,
    piping to a small head so the log stays short. This is the exact pair reproduced live during
    research: under a depth-1 checkout it fails with an invalid-object-name error, and under a
    full checkout it succeeds. Use the SAME pair in all 16 probes rather than a per-milestone pair
    — the v1.5 close is the oldest pinned SHA in the table, so a probe that reaches it proves every
    later pin is reachable too, and one uniform pair means one fact to verify instead of sixteen.
  - Step 2, real library read: run a small `node -e` that dynamically imports
    `scripts/validation/_lib/frozen-at-close.mjs`, calls `readAtV15Close` on the same glossary
    path, and exits non-zero if the returned content is empty. This makes the probe an exercise of
    the real reader, not just a bare git command.
  - Step 3, real enumeration: run a small `node -e` that calls `lsTreeAtV15Close` on the
    `docs/l1-runbooks` prefix and exits non-zero unless it returns exactly 34 entries. This proves
    the new SWEEP-04 API works in a previously-shallow CI job, and the exact count is what catches
    a regression to live-HEAD enumeration, where the same prefix holds 42.

Add NO `continue-on-error`, NO `if:` condition, and no shell fallback that swallows a non-zero
exit anywhere in the probe. A probe that cannot read must fail its job — a masked probe would
manufacture exactly the accepted-red class this milestone's bar requires deleting.

Placement: append the job at the end of each file's `jobs:` mapping, after the existing jobs, so
the first `name:` line in each file remains the workflow-level name (`check-phase-66.mjs`'s
`V-66-05` extracts it with a first-match regex). In `audit-harness-v1.7-integrity.yml`
specifically, keep the probe's text free of any reference to the v1.6 milestone token — `V-70-16`
asserts that file carries zero such references.

Then land atom 5 as ONE commit per D-41: Task 1's 97-checkout sweep, Task 1's `paths:` fix, and
these 16 probe jobs together. Do not split into two commits.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('.github/workflows').filter(x=>x.endsWith('.yml'));if(files.length!==16){console.error('expected 16 workflows, got '+files.length);process.exit(1)}for(const f of files){const c=fs.readFileSync('.github/workflows/'+f,'utf8');const n=(c.match(/^  frozen-read-probe:/gm)||[]).length;if(n!==1){console.error(f+' has '+n+' frozen-read-probe jobs');process.exit(1)}const seg=c.slice(c.indexOf('  frozen-read-probe:'));if(/^\s+needs:/m.test(seg.split(/\n(?=  [a-z])/)[0])){console.error(f+' probe has a needs: key');process.exit(1)}}console.log('16/16 probe jobs, none with needs:')"</automated>
  </verify>
  <acceptance_criteria>
    - The probe-job scan exits 0 and reports `16/16 probe jobs, none with needs:`
    - `grep -c 'frozen-read-probe' .github/workflows/*.yml | grep -vc ':0$'` equals 16 — every workflow has at least one occurrence
    - `grep -l 'continue-on-error' .github/workflows/*.yml | xargs -r grep -A20 'frozen-read-probe' | grep -c 'continue-on-error'` equals 0 — no probe job is advisory <!-- planner-discipline-allow: continue-on-error -->
    - `grep -c 'readAtV15Close' .github/workflows/audit-harness-integrity.yml` is at least 1 and the same for all 15 other workflows
    - `grep -c 'lsTreeAtV15Close' .github/workflows/audit-harness-integrity.yml` is at least 1 and the same for all 15 other workflows
    - `grep -c '34' .github/workflows/audit-harness-v1.5-integrity.yml` is at least 1 — the exact-count enumeration assertion is present, not a lower bound
    - The adjacency scan from Task 1 still exits 0 after the probe jobs are added, now reporting 198 checkouts all deep
    - `node scripts/validation/check-phase-66.mjs`, `check-phase-69.mjs` and `check-phase-70.mjs` each exit 0
    - `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-138.mjs` reports 0 FAIL
    - `git log --oneline -1 --name-only` shows all 16 workflow files in a single commit (D-41 atom 5 is one commit)
    - `node scripts/validation/carve-gate.mjs` exits 0 — all 16 workflow paths are on-list by category
  </acceptance_criteria>
  <done>Each of the 16 workflows carries one dependency-free probe job that reads a frozen blob, calls the real reader, and enumerates exactly 34 entries at the v1.5 close — all landed with the sweep in one commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow YAML -> GitHub Actions runner | Every added step becomes executable content on a hosted runner |
| Full git object graph -> CI job | `fetch-depth: 0` widens every job's access from one commit to the whole history |
| Probe job exit code -> SWEEP-01/02 evidence | A masked or suppressed probe would certify a capability that does not exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-16 | Spoofing | Probe job appearing green while inert | high | mitigate | No `continue-on-error`, no `if:`, no shell fallback on any probe step; asserted absent by the acceptance criteria, and evidence is job-level JSON in Plan 06, never the checks-UI colour |
| T-139-05 | Elevation of privilege | Widened object graph in every job | low | accept | `fetch-depth: 0` is monotonic — it only widens the available history and changes no job's semantics; no workflow in this repository references any secret, and the over-covered jobs were independently confirmed to spawn no git subprocess |
| T-139-17 | Tampering | Duplicate or misplaced `fetch-depth` key silently last-wins | medium | mitigate | Adjacency scan plus a duplicate-key scan; merge into an existing `with:` mapping rather than emitting a second one |
| T-139-18 | Denial of service | Full-history fetch cost on every job | low | accept | The pack is roughly 23 MiB over about 3000 commits; the cost is bounded and was weighed against a partial scope that leaves 52 shallow checkouts and an uncheckable invariant |
| T-139-SC | Tampering | npm/pip/cargo installs | n/a | accept | The probe jobs add no `npm install`; the existing quarterly job's install is unchanged |
</threat_model>

<verification>
- The adjacency scan reports every checkout deep, zero shallow, across all 16 workflows
- No duplicate `fetch-depth` key anywhere
- 16 `frozen-read-probe` jobs, exactly one per workflow, none carrying a `needs:` key
- `check-phase-66`, `check-phase-69`, `check-phase-70` each exit 0
- `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-138.mjs` reports 0 FAIL
- `node scripts/validation/carve-gate.mjs` exits 0
- Atom 5 is a single commit

</verification>

<success_criteria>
- 97 previously-shallow checkouts converted; the repo-wide invariant holds with adjacency, not just counts
- `audit-harness-integrity.yml` watches `.github/workflows/**`
- One dependency-free `frozen-read-probe` job per workflow, each exercising raw git, the real reader, and the real enumeration API with an exact count
- Zero masking constructs on any probe job
- One commit for the whole atom
</success_criteria>

<output>
Create `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-05-SUMMARY.md` when done
</output>
