---
phase: 139-governance-carve-fetch-depth-retrofit-shallow-job-repair
plan: 06
type: execute
wave: 6
depends_on: ["139-05"]
files_modified:
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: false
requirements: [SWEEP-01, SWEEP-02]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The dispatch is from a short-lived feature branch, never master — a master push mid-milestone has no precedent and would fire the full 16-workflow cascade while Phases 140-143 debt is still red (D-25)."
    - "The workflows are dispatched with `gh workflow run --ref <branch>`; the files already exist on the default branch, which is all `workflow_dispatch` requires for API availability, and the ref selects which content runs."
    - "Evidence is job-level JSON from `gh run view <id> --json jobs`, never the checks-UI colour — a green run is compatible with a cron-skipped quarterly job and a `continue-on-error: true` advisory job (D-25)."
    - "Every one of the 16 `frozen-read-probe` jobs reports a success conclusion, and each one's log shows the frozen read succeeding, the real reader returning content, and the enumeration returning exactly 34 entries."
    - "The 'previously threw' half of the evidence comes from the local `file://` shallow clone built in Plan 03, not from a pre-retrofit CI dispatch — a pre-retrofit dispatch buys nothing under the skip fan-out, costs a second owner authorization, and would require a probe step to exist before the retrofit (D-26)."
    - "The residual SWEEP-02 question — whether the 11 already-frozen-aware validators complete their frozen reads in CI — is explicitly NOT answered by this dispatch and is recorded as depending on Phase 141's RED-01 (D-23)."
    - "Any `check-phase-*` job reporting a skipped conclusion in this dispatch is recorded as the expected `needs: harness-run` fan-out, not as a probe failure."
  prohibitions:
    - requirement_id: SWEEP-02
      category: transparency
      statement: "MUST NOT present a run-level green as SWEEP-01 or SWEEP-02 evidence — only the individual probe job's own conclusion, read from job-level JSON, counts."
    - requirement_id: SWEEP-01
      category: safety
      statement: "MUST NOT push this phase's commits to the default branch; the dispatch ref is a short-lived feature branch and master stays untouched until the milestone close."
  artifacts:
    - path: ".planning/milestones/v1.20-GOV-02-LEDGER.md"
      provides: "CI dispatch evidence rows — run ids, probe job conclusions, and the local negative-clone contrast"
      contains: "frozen-read-probe"
  key_links:
    - from: "the short-lived feature branch"
      to: "16 dispatched workflow runs"
      via: "gh workflow run --ref <branch>, one invocation per workflow file"
      pattern: "workflow run --ref"
    - from: "gh run view --json jobs"
      to: "SWEEP-01 SC#2 and SWEEP-02 evidence"
      via: "per-job conclusion for the frozen-read-probe job only"
      pattern: "frozen-read-probe"
---

<objective>
The owner-gated close of D-41: push a short-lived feature branch, dispatch all 16 retrofitted
workflows against it, and capture job-level JSON evidence that a frozen `git show` read now
succeeds in CI where it previously threw.

D-26 supplies the "previously threw" half from Plan 03's local `file://` shallow clone rather than
from a pre-retrofit dispatch, so this phase needs exactly one dispatch and one owner authorization.

Purpose: SWEEP-01 SC#2 and the re-scoped SWEEP-02 (D-24). Output: recorded run ids and per-job
conclusions in the GOV-02 ledger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-CONTEXT.md
@.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-03-SUMMARY.md
@.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-05-SUMMARY.md
</context>

## Artifacts this phase produces

See the table in `139-01-PLAN.md` → "Artifacts this phase produces". This plan creates no code
symbol; it produces CI evidence rows in the GOV-02 ledger.

<tasks>

<task type="auto">
  <name>Task 1: Push the feature branch, dispatch 16 workflows, collect job-level JSON</name>
  <precondition>`gh auth status` reports an authenticated account and `git remote -v` shows the `origin` remote reachable — the dispatch requires both and neither can be established by this task.</precondition>
  <read_first>
    - `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-05-SUMMARY.md` — the exact probe job name and the three probe steps that must appear in each run's logs
    - `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-03-SUMMARY.md` — the local shallow-clone negative result that supplies the "previously threw" half of the evidence
    - `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-CONTEXT.md` — D-23, D-24, D-25, D-26
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` — the row schema to append the dispatch evidence to
  </read_first>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <action>
**(a) Create and push the short-lived feature branch.** Name it following this repository's own
recorded convention for dispatch branches, `phase-<N>-atom-<M>`, giving `phase-139-atom-5` — the
remote already carries three prior branches under that convention from earlier milestones. Create
it from the current local head, which carries all five atoms, and push it with upstream tracking.
Do NOT push to master. A master push mid-milestone has no precedent in this repository — every
recorded push is at a milestone close — and it would fire the full 16-workflow cascade while
Phases 140-143 debt is still red.

**(b) Dispatch each of the 16 workflows against that ref.** Run `gh workflow run <workflow-file>
--ref phase-139-atom-5` once per workflow file. These workflows fire on pull_request, schedule and
workflow_dispatch only — a push fires nothing — so an explicit dispatch is required and is the
only mechanism that works here. The workflow files already exist on the default branch, which is
all the dispatch API needs for availability; the `--ref` selects which file content actually runs,
so the probe jobs added on the branch are what execute.

**(c) Collect job-level JSON, never the checks-UI colour.** For each dispatched workflow, resolve
the run id with `gh run list --workflow <file> --branch phase-139-atom-5 --limit 1 --json
databaseId,status,conclusion`, wait for completion, then pull `gh run view <id> --json jobs`.
Extract, for each run: the run id, the run-level conclusion, and specifically the `frozen-read-probe`
job's own conclusion and its step conclusions. A run-level green is NOT evidence — it is compatible
with a cron-skipped quarterly job and with the `continue-on-error: true` advisory job present in
all 16 workflows. The assertion is that all 16 probe jobs individually report success.

**(d) Capture the probe logs.** For at least the three previously-all-shallow workflows, pull the
probe job's log and confirm it shows the frozen read returning content, the real reader returning a
non-empty string, and the enumeration returning exactly 34 entries. Record the observed values.

**(e) Record the contrast and the residual.** Append GOV-02 ledger rows carrying: the branch name;
one row per workflow with its run id and the probe job's conclusion; the local `file://`
shallow-clone result from Plan 03 as the "previously threw" half per D-26; and an explicit note
that any `check-phase-*` job reporting a skipped conclusion in these runs is the expected
`needs: harness-run` fan-out described by D-23, not a probe failure. Also record explicitly that
this dispatch does NOT answer whether the 11 already-frozen-aware validators complete their frozen
reads in CI — that observation is blocked until Phase 141's RED-01 greens the harnesses, and the
probe job is the ratified proxy for it (D-24).

If any probe job fails, stop and report the failing run id and job log rather than re-dispatching
or adjusting the criterion.
  </action>
  <verify>
    <automated>gh run list --branch phase-139-atom-5 --limit 20 --json databaseId,workflowName,conclusion --jq 'length'</automated>
  </verify>
  <acceptance_criteria>
    - `git branch -r --list 'origin/phase-139-atom-5'` returns the branch, and `git rev-parse origin/master` is unchanged from its value before this task
    - `gh run list --branch phase-139-atom-5 --json databaseId --jq 'length'` returns 16
    - For every one of the 16 runs, `gh run view <id> --json jobs --jq '.jobs[] | select(.name | test("frozen-read-probe")) | .conclusion'` returns `success` — 16 successes, zero failures, zero skips
    - The probe job log for `audit-harness-integrity.yml`, `audit-harness-v1.5-integrity.yml` and `audit-harness-v1.6-integrity.yml` each shows the frozen read succeeding and the enumeration reporting exactly 34 entries
    - The evidence recorded in the ledger is job-level conclusions and run ids, and the SUMMARY explicitly states that no run-level colour was used as evidence
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` carries one dispatch row per workflow plus the local shallow-clone contrast row and the D-23 residual note
    - `node scripts/validation/carve-gate.mjs` exits 0 on the tree at dispatch time
  </acceptance_criteria>
  <done>All 16 workflows are dispatched against the feature branch and every `frozen-read-probe` job reports success at the job level, with the pre-retrofit failure evidence supplied locally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner review of the dispatch evidence and branch disposition</name>
  <what-built>
Atoms 1-5 are complete and committed on master locally, and a short-lived branch
`phase-139-atom-5` has been pushed and used to dispatch all 16 `audit-harness-*` workflows. Every
`frozen-read-probe` job reported success at the job level. Master itself was not pushed.
  </what-built>
  <how-to-verify>
1. Confirm master was not pushed: `git rev-parse origin/master` should equal its pre-phase value,
   and `git log --oneline origin/master..master` should list this phase's commits as unpushed.
2. Review the dispatch evidence table in `.planning/milestones/v1.20-GOV-02-LEDGER.md` — 16 rows,
   each with a run id and a `frozen-read-probe` job conclusion of `success`.
3. Spot-check one run yourself: `gh run view <id> --json jobs --jq '.jobs[] | {name, conclusion}'`
   and confirm the probe job's conclusion, and that any `check-phase-*` job showing `skipped` is
   the expected `needs: harness-run` fan-out rather than a probe problem.
4. Open the probe job log for `audit-harness-v1.5-integrity.yml` and confirm the enumeration step
   printed exactly 34 entries.
5. Decide the fate of the branch: it is short-lived by design. Either authorize deleting
   `origin/phase-139-atom-5` now, or state that it should be kept until Phase 144's close audit.
6. Confirm the recorded residual is acceptable: this dispatch proves the frozen read works in a
   previously-shallow job; it does NOT prove the 11 already-frozen-aware validators run, because
   they sit behind the `needs: harness-run` fan-out until Phase 141.
  </how-to-verify>
  <resume-signal>Type "approved" (and say whether to delete `origin/phase-139-atom-5`), or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repository -> `origin` remote | A branch push publishes this phase's content to a hosted remote |
| Feature-branch workflow content -> GitHub Actions runner | `--ref` selects branch content, so a dispatch executes steps that do not exist on the default branch |
| GitHub API JSON -> phase acceptance evidence | Run and job conclusions become the record that a success criterion was met |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-19 | Spoofing | Run-level green mistaken for probe evidence | high | mitigate | Evidence is read from `gh run view --json jobs` and filtered to the probe job's own conclusion; the acceptance criteria assert 16 job-level successes, and the SUMMARY must state no run-level colour was used |
| T-139-05 | Elevation of privilege | Dispatch executes branch content on a hosted runner | medium | mitigate | The branch is short-lived, created from reviewed local commits, and the dispatch is owner-gated by the blocking checkpoint; no workflow in this repository references any secret |
| T-139-20 | Tampering | Accidental push to the default branch | high | mitigate | Only the named feature branch is pushed; an acceptance criterion asserts `origin/master` is byte-identical to its pre-task value |
| T-139-21 | Repudiation | Re-dispatching until a run is green | medium | mitigate | A failing probe job stops the task and reports the run id and log rather than re-dispatching; every dispatched run id is recorded in the ledger |
| T-139-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs zero packages |
</threat_model>

<verification>
- `origin/phase-139-atom-5` exists; `origin/master` is unchanged
- 16 dispatched runs, each with a `frozen-read-probe` job conclusion of `success` read from job-level JSON
- Probe logs for the three previously-all-shallow workflows show the frozen read succeeding and exactly 34 enumerated entries
- The GOV-02 ledger carries the dispatch rows, the local shallow-clone contrast, and the D-23 residual note
- The owner checkpoint is resolved

</verification>

<success_criteria>
- One dispatch, one owner authorization, sixteen job-level successes
- Master untouched
- The "previously threw" half sourced locally per D-26, not from a second dispatch
- The SWEEP-02 residual recorded honestly rather than papered over
</success_criteria>

<flagged_assumptions>
- **SWEEP-02 / unclassified edge remains UNRESOLVED** (first surfaced in Plan 01). This dispatch
  delivers the ratified PROXY evidence (D-24), not the literal criterion (D-23). Phase 144's close
  audit must read the proxy as authorized — which is why Plan 01 Task 3 re-words both ROADMAP SC#3
  and REQUIREMENTS SWEEP-02 to match. Surfaced, not dismissed.
</flagged_assumptions>

<output>
Create `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-06-SUMMARY.md` when done
</output>
