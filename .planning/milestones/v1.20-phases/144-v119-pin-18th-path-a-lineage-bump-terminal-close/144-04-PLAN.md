---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 04
type: execute
wave: 3
depends_on: ["144-01"]
files_modified:
  - scripts/validation/check-phase-139.mjs
  - scripts/validation/check-phase-140.mjs
  - scripts/validation/check-phase-141.mjs
autonomous: true
requirements: [HARN-18]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All three leaves are LIGHTWEIGHT: `const CHAIN_PHASES = [];`, `const CHAIN_SKIP = new Set([]);`, needles plus a SELF dual-invariant, and ZERO AUDIT, AUDIT-HARNESS or nested-invocation-guard references — the template is `check-phase-135/136/137.mjs`, never the apex (D-14)."
    - "Header line 4 of each leaf states it is a LIGHTWEIGHT base with no chain, chain living only in the apex — the wording restated at `check-phase-132.mjs:4`."
    - "Every needle derives from its phase's VERIFICATION.md / SUMMARY.md MEASURED ACTUALS, never from that phase's CONTEXT.md (`check-phase-132.mjs:5`)."
    - "Zero `.planning/phases/` reads in any leaf (D-15): `MILESTONE_CLOSE_SHAS` tops out at V119 and phases 139-143 exist at no pin, so an own-milestone leaf cannot be frozen-aware against its own not-yet-existing close. Needle targets are `scripts/`, `.github/` or `.planning/milestones/v1.20-*` paths only."
    - "No corpus-wide negative assertion appears in any leaf (D-05/D-14) — a live-HEAD `docs/`-wide absence assertion inside a permanent apex member goes red at the first v1.21 content commit and manufactures the accepted-red class this milestone's bar requires deleting."
    - "`check-phase-139.mjs` asserts frozen-to-frozen facts about the governance artifact at a fixed past commit using the `check-phase-63.mjs:208-250` blob-comparison idiom — `git rev-parse <literal SHA>:<path>` against a recorded baseline blob, never a live diff and never a `MILESTONE_CLOSE_SHAS` lookup."
    - "`check-phase-140.mjs` asserts only the POSITIVE, permanently-true half — that all sixteen v1.4 through v1.18 harnesses carry the frozen-corpus reader — and says NOTHING about the v1.19 harness's conversion state, which this same phase's Plan 02 changes."
    - "`check-phase-141.mjs` asserts nothing about `check-phase-67.mjs`'s content — that file's site classes are converted by this same phase's Plan 03 — and uses Phase 141's own durable deliverables instead."
    - "Empty is the correct and asserted state for a leaf's chain: each SELF check fails if its own phase number appears in `CHAIN_PHASES` or if `CHAIN_SKIP` is non-empty, so the empty case is a positive invariant rather than an unhandled input."
    - "Each leaf exits 0 standalone and prints an `N PASS, 0 FAIL, K SKIPPED` summary with N equal to its registered check count."
  artifacts:
    - scripts/validation/check-phase-139.mjs
    - scripts/validation/check-phase-140.mjs
    - scripts/validation/check-phase-141.mjs
  key_links:
    - "Each leaf -> `check-phase-144.mjs`'s 96-entry chain (Plan 07). HAZARD FIX 3 makes an absent chain child a hard FAIL, which is why the leaves are authored BEFORE the apex (D-11)."
    - "`check-phase-139.mjs`'s frozen blob baseline -> `git rev-parse <SHA>:<path>`, which needs no `MILESTONE_CLOSE_SHAS` entry and works against any reachable commit."
  prohibitions:
    - statement: "No leaf asserts a fact that this same phase's own later plans make false — no needle may be phrased as 'X is NOT yet true' where X is something Phase 144 subsequently does."
      status: flagged-unverified
      verification: "Re-run all three leaves after Plans 02, 03, 06, 07 and 08 have landed; each still exits 0 with an unchanged summary triple."
    - statement: "No leaf reads any path under `.planning/phases/`."
      status: flagged-unverified
      verification: "A grep for the phases directory prefix across the three new files returns zero hits."
---

<objective>
Author the first three of the five new lightweight leaf validators — `check-phase-139.mjs`,
`-140.mjs`, `-141.mjs` — on the true leaf template, with needles derived from each phase's own
measured actuals.

Purpose: HAZARD FIX 3 in the apex makes an absent chain child a hard FAIL, so the leaves must exist
before `check-phase-144.mjs` runs — apex-first would yield exactly five FAILs indistinguishable from
a real regression (D-11). Building these on the apex template instead would spawn five extra harness
runs and five extra chain expansions inside the apex's own 98-child sweep (D-14).

Output: three write-once leaf validators, each exiting 0 standalone, each durable against ordinary
future content commits and against this phase's own remaining plans.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-PATTERNS.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: check-phase-139.mjs — governance CARVE, fetch-depth sweep, dependency-free probe</name>
  <files>scripts/validation/check-phase-139.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-135.mjs` — the true LIGHTWEIGHT leaf template in full: header line 4, the empty chain constants at `:44`, the check-registration shape, the SELF dual-invariant, and the runner-loop tail (`LABEL_WIDTH`, `padLabel`, the summary line), which is byte-identical boilerplate across 135/136/137
    - `scripts/validation/check-phase-63.mjs:190-260` — the frozen-to-frozen blob-comparison idiom (`git rev-parse <SHA>:<path>` against a recorded baseline, graceful skip when git is unavailable)
    - `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-VERIFICATION.md` and the `139-0N-SUMMARY.md` files — the measured actuals every needle must derive from (SWEEP-01's 97 retrofitted checkouts, SWEEP-02's dependency-free probe job, GOV-01/GOV-02's governance artifacts)
    - `scripts/validation/carve-gate.mjs` (read with `-a` / plain Read — NUL-classified) and `.planning/milestones/v1.20-CARVE.md` — the assertion targets
  </read_first>
  <action>
Copy the leaf template and rename throughout. Set the empty chain constants, write header line 4 in
the established LIGHTWEIGHT wording, and copy the runner-loop tail verbatim — it is fixed
boilerplate, not a design choice.

Register these needles, all targeting `scripts/`, `.github/` or `.planning/milestones/v1.20-*`:

Needle 1 — frozen-to-frozen governance blob. Follow the `check-phase-63.mjs` idiom exactly: resolve
the blob hash of the CARVE gate script at a fixed literal commit SHA and compare it to a recorded
baseline hash. Use a RAW literal SHA, not a `MILESTONE_CLOSE_SHAS` lookup — `git rev-parse` works
against any reachable commit, and no pin exists for this milestone's own phases. `144-RESEARCH.md`
supplies a live-measured, ready-to-use commit/blob pair; re-measure it at authoring time with
`git log -1 --format=%H -- <path>` and `git rev-parse <sha>:<path>` and pin what you measure. Graceful
skip if git is unavailable, matching the analog.

Needle 2 — the fetch-depth sweep, as a per-file invariant rather than a global count. For each
`.github/workflows/audit-harness-*.yml`, the number of checkout steps must equal the number of
`fetch-depth: 0` settings. Do not pin the milestone-wide 97 or 182 totals: the 17th workflow lands
in Plan 08 and would shift any global count, while the per-file equality strengthens rather than
breaks. Strip comment lines before counting so header prose cannot satisfy or invalidate the count.

Needle 3 — SWEEP-02's dependency-free probe: every `audit-harness-*.yml` declares a frozen-read probe
job and that job carries no dependency key, which is the property that makes it report independently
of the harness job's outcome.

Needle 4 — the governance artifacts exist and are structurally intact: the CARVE document contains
exactly one fenced allowlist block, the three-rule amendment procedure, and a GOV-02 grep procedure
section; the GOV-02 ledger has at least as many rows as its schema header describes. Use a
lower-bound threshold for the row count, never an equality — this phase appends rows.

Needle 5 — the SELF dual-invariant, mirroring the analog: fail if `CHAIN_PHASES` includes 139, fail
if `CHAIN_SKIP` is non-empty, pass with a detail string naming both.

Commit alone. Subject form `feat(144-04): …`.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-139.mjs; echo $?` prints `0` and the summary line reads `N PASS, 0 FAIL, K SKIPPED` with N equal to the registered check count.
    - `grep -c 'const CHAIN_PHASES = \[\];' scripts/validation/check-phase-139.mjs` resolves to `1`.
    - `grep -c 'CHECK_PHASE_NESTED' scripts/validation/check-phase-139.mjs` resolves to `0`.
    - `grep -c 'AUDIT-HARNESS' scripts/validation/check-phase-139.mjs` resolves to `0`.
    - `grep -c '\.planning/phases' scripts/validation/check-phase-139.mjs` resolves to `0`.
    - The blob-comparison needle passes against a re-measured baseline: `git rev-parse <pinned SHA>:<pinned path>` printed at authoring time equals the literal recorded in the file.
    - `node scripts/validation/carve-gate.mjs` exits 0 with the new file counted on-list.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-139.mjs</automated>
  </verify>
  <done>The leaf exists, exits 0 standalone, carries an empty chain and a SELF dual-invariant, and its governance needle is frozen-to-frozen rather than a live diff.</done>
</task>

<task type="auto">
  <name>Task 2: check-phase-140.mjs — the sixteen conversions, the V14 pin, the C17 carve-out</name>
  <files>scripts/validation/check-phase-140.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-136.mjs` — leaf template variant with more needles; confirm the empty chain constant at `:40` and the same runner tail
    - `.planning/phases/140-frozen-aware-harness-conversion/140-VERIFICATION.md` and its `140-0N-SUMMARY.md` files — measured actuals for SWEEP-05 (the conversions), SWEEP-06 (the timing measurement), SWEEP-07 (the template-sentinel remedy) and SWEEP-08 (the V14 pin)
    - `scripts/validation/_lib/frozen-at-close.mjs` — the V14 entry and its recorded rationale comment
    - `scripts/validation/v1.18-milestone-audit.mjs` — the C17 exception comment block that Phase 140 left deliberately live-HEAD
    - `144-RESEARCH.md` Pitfall 1 — why this leaf must never assert anything about the v1.19 harness's conversion state
  </read_first>
  <action>
Same template, same empty chain, same runner tail.

Needle 1 — the conversions, POSITIVE half only. Assert that at least sixteen
`scripts/validation/v1.*-milestone-audit.mjs` files carry the frozen-corpus reader, and that each of
the v1.4 through v1.18 harnesses individually does. Use a lower bound, not an equality: Plan 02
converts the v1.19 harness, making seventeen, and an equality would self-detonate the apex the moment
this phase's own commits land. Assert NOTHING about which harnesses are unconverted — that is the
self-invalidating phrasing Pitfall 1 names.

Needle 2 — SWEEP-08's V14 pin: the frozen-read library's milestone map carries a V14 entry with an
explicitly recorded rationale comment, and V14 is still the last entry in the map (it is an
AUDIT-CLOSE pin, not a milestone-close-gate pin). This stays true after Plan 02's V119 append, which
inserts before V14.

Needle 3 — the C17 carve-out, as a positive presence assertion across the C17-bearing harnesses: each
of the v1.15 through v1.18 harnesses still contains the contract spawn and its exception comment.
Scope this needle to v1.15 through v1.18 only — v1.19's carve-out is recorded as a requirement
amendment in Plan 02 and asserting it here would couple this leaf to a file this phase edits.

Needle 4 — SWEEP-06's measurement discipline: assert the subprocess budget constant that
`check-phase-60.mjs` enforces is still 60 seconds, so the measurement recorded by Phase 140 (and
extended in Plan 02) remains scored against the same bar.

Needle 5 — the SELF dual-invariant for 140.

Commit alone.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-140.mjs; echo $?` prints `0` with an `N PASS, 0 FAIL, K SKIPPED` summary.
    - `grep -c 'const CHAIN_PHASES = \[\];' scripts/validation/check-phase-140.mjs` resolves to `1`.
    - `grep -c 'CHECK_PHASE_NESTED' scripts/validation/check-phase-140.mjs` resolves to `0`.
    - `grep -c '\.planning/phases' scripts/validation/check-phase-140.mjs` resolves to `0`.
    - `grep -c 'v1.19-milestone-audit' scripts/validation/check-phase-140.mjs` resolves to `0` — this leaf never references the harness Plan 02 converts.
    - Re-running the leaf AFTER Plan 02's conversion has landed still exits 0 with the same summary triple.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-140.mjs</automated>
  </verify>
  <done>The leaf asserts only permanently-true positive facts about Phase 140's deliverables and survives this phase's own later commits unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: check-phase-141.mjs — the pin helper self-test and the landed fail-loud sites</name>
  <files>scripts/validation/check-phase-141.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-137.mjs` — the leaf template's smallest variant (empty chain at `:36`)
    - `scripts/validation/check-phase-119.mjs:135-160` — the subprocess-spawn needle idiom: graceful skip when the tool is absent, narrowed missing-interpreter classifier, failure detail from stdout/stderr
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-VERIFICATION.md` and `141-03-SUMMARY.md` — measured actuals for RED-01/02/03 and SWEEP-09's thirteen landed sites in `check-phase-61/68/70.mjs`
    - `scripts/validation/regenerate-supervision-pins.mjs` — the self-test entry point and its exit contract
    - `144-RESEARCH.md` Pitfall 2 and Open Question 1 — why this leaf must not reference `check-phase-67.mjs`, and the resolved needle choice
  </read_first>
  <action>
Same template, same empty chain, same runner tail.

Needle 1 — the pin helper's self-test, as a subprocess spawn following the `check-phase-119.mjs`
idiom: spawn the pin-regeneration helper with its self-test flag and require exit 0. This is Phase
141's own RED-02 deliverable, measured green at HEAD, and it stays true past this phase's end —
Plan 06 appends a new baseline block to that helper without touching its self-test contract. Use the
same graceful-skip and failure-detail shape as the analog; no nested-invocation guard (a leaf has
none).

Needle 2 — SWEEP-09's landed sites. Measure, at authoring time, the current occurrence count of the
deferred-guard phrase in each of `check-phase-61.mjs`, `-68.mjs` and `-70.mjs`, and pin the measured
value per file as an exact equality. These three files are Phase 141's own deliverable and Phase 144
never edits them, so a measured pin is durable. Do NOT reference `check-phase-67.mjs` from this leaf
in any form: Plan 03 of this same phase converts that file's site classes, and any count or presence
assertion about it would self-detonate the apex (Pitfall 2).

Needle 3 — RED-01's outcome as a static property rather than a re-run: assert that the v1.5 and v1.6
harnesses carry the frozen-corpus reader and that the pin helper names the rebased baseline
identifier Phase 141 corrected. Prefer content substrings over line numbers — an inserted line breaks
a line-pinned assertion.

Needle 4 — the SELF dual-invariant for 141.

Commit alone.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-141.mjs; echo $?` prints `0` with an `N PASS, 0 FAIL, K SKIPPED` summary.
    - `grep -c 'const CHAIN_PHASES = \[\];' scripts/validation/check-phase-141.mjs` resolves to `1`.
    - `grep -c 'check-phase-67' scripts/validation/check-phase-141.mjs` resolves to `0`.
    - `grep -c '\.planning/phases' scripts/validation/check-phase-141.mjs` resolves to `0`.
    - The spawned self-test needle passes: `node scripts/validation/regenerate-supervision-pins.mjs --self-test; echo $?` independently prints `0`.
    - Re-running the leaf AFTER Plans 03 and 06 have landed still exits 0 with the same summary triple.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-141.mjs</automated>
  </verify>
  <done>The leaf spawns the pin-helper self-test, pins only measured durable facts, references nothing this phase edits, and exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leaf needle → future commits | Every leaf becomes a permanent apex chain member; a needle that encodes a temporary fact converts ordinary future work into a chain failure. |
| leaf → spawned subprocess | One needle shells out to a repo script; argument arrays only, never a shell string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-12 | Denial of Service | permanent apex chain | high | mitigate | No corpus-wide negative assertion and no self-invalidating needle; each leaf is re-run after this phase's own later plans land, asserted by a must_have |
| T-144-13 | Tampering | subprocess spawn in the 141 leaf | medium | mitigate | `execFileSync` with an argument array copied verbatim from the `check-phase-119.mjs` analog; no concatenated shell string |
| T-144-14 | Spoofing | frozen blob baseline in the 139 leaf | medium | mitigate | Baseline re-measured at authoring time from `git rev-parse` output and pinned literally; the target commit is fixed and past, so the assertion cannot drift with HEAD |
| T-144-15 | Repudiation | needles sourced from prose | low | mitigate | Every needle derives from a VERIFICATION/SUMMARY measured actual, never from a CONTEXT document |
</threat_model>

<verification>
- All three leaves exit 0 standalone with `N PASS, 0 FAIL, K SKIPPED` summaries.
- Each declares an empty chain and an empty skip set and carries a SELF dual-invariant.
- Zero nested-guard, AUDIT, AUDIT-HARNESS or `.planning/phases/` references in any leaf.
- `check-phase-140.mjs` references neither the v1.19 harness nor any unconverted-state claim; `check-phase-141.mjs` references `check-phase-67.mjs` nowhere.
- `node scripts/validation/carve-gate.mjs` exits 0 with the three new files on-list.
</verification>

<success_criteria>
Three write-once lightweight leaves exist and pass, they encode only facts durable past this phase's
own remaining plans, and the apex authored in Plan 07 will find them present rather than absent.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files (this plan): `scripts/validation/check-phase-139.mjs`, `-140.mjs`, `-141.mjs`.
New files (phase-wide): `check-phase-142.mjs`, `-143.mjs`, `check-phase-144.mjs` (apex),
`v1.20-milestone-audit.mjs`, `v1.20-audit-allowlist.json`, `audit-harness-v1.20-integrity.yml`,
`v1.20-MILESTONE-AUDIT.md`, `144-EVIDENCE.md`, `dist/docs-library-v1.20.zip` (gitignored).
New exports: `readAtV119Close`, `lsTreeAtV119Close`.
New constants: `MILESTONE_CLOSE_SHAS.V119`, `BASELINE_24`, and `CHAIN_START` / `CHAIN_END` /
`CHAIN_PHASES` / `CHAIN_EXTRA` / `CHAIN_SKIP` in the new apex.
New CI job keys: `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`,
`check-phase-144`, `check-phase-139` … `check-phase-143`, `rotting-external-quarterly`,
`pin-helper-advisory`, `frozen-read-probe`.
Modified across the phase: `_lib/frozen-at-close.mjs`, `v1.19-milestone-audit.mjs`,
`check-phase-67.mjs`, `regenerate-supervision-pins.mjs`, `audit-harness-v1.7-integrity.yml`,
`audit-harness-v1.8-integrity.yml`, `v1.20-CARVE.md`, `v1.20-GOV-02-LEDGER.md`,
`v1.20-DEFERRED-CLEANUP.md`, `.planning/{PROJECT,ROADMAP,STATE,REQUIREMENTS}.md`.
</artifacts_produced>

<output>
Create `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-04-SUMMARY.md` when done
</output>
