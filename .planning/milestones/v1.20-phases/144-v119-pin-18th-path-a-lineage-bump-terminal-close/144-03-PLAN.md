---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 03
type: execute
wave: 3
depends_on: ["144-01"]
files_modified:
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
  - scripts/validation/check-phase-67.mjs
autonomous: true
requirements: [HARN-18, SWEEP-09]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All TEN remaining fail-loud sites in `check-phase-67.mjs` land — SEVEN chicken-and-egg early returns (`:73, :88, :117, :132, :165, :199, :227`) plus THREE partial-null silent-pass accumulator blocks (V-67-01, V-67-03, V-67-06), which is the obligation `v1.20-CARVE.md:272-274` states as 7 plus 3 (D-03)."
    - "V-67-05 and V-67-07 are NOT in the vulnerable class and are not restructured: their fail thresholds equal their file counts, so any partial-null read already forces a hard FAIL — a naive five-site count is wrong."
    - "The two classes take different fix shapes: the seven early returns are a RETURN-FIELD-ONLY flip (a skipped pass becomes a failing return with file-specific detail, per Phase 141 Plan 03's precedent in `check-phase-61/68/70.mjs`), while the three accumulator blocks require full readability before the aggregate is evaluated so an unread file cannot be silently skipped."
    - "All FOUR literals `check-phase-73.mjs:266-302` pins against this file's source survive the edit verbatim: the frozen-at-close reference, the `V-67-05` / `v1.7-frozen` regex pair, the Apple-artifact callout text, and the SWEEP-02 date-row text (D-03)."
    - "A GOV-02 target-scoped census (path string AND symbol, `grep -a` on any NUL-classified target) is recorded as a ledger row BEFORE the edit, per `v1.20-CARVE.md:93-112` (D-03)."
    - "The shared wrapper reader's own try/catch is untouched — other call sites in the same wrapper still legitimately receive a null."
    - "`check-phase-67.mjs` and `check-phase-73.mjs` both exit 0 after the edit, and `check-phase-138.mjs` still reports 95 PASS, 0 FAIL, 0 SKIPPED — this file is a chain member in every apex and is pinned by 73, so it sits inside the chain twice over."
  artifacts:
    - scripts/validation/check-phase-67.mjs
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
  key_links:
    - "`check-phase-67.mjs` -> `check-phase-73.mjs`'s `V-73-CONVERT-67-05`/`-06` four-literal source pin (a whitespace-level edit to any pinned string trips it)."
    - "`check-phase-67.mjs` -> every apex chain run (48..137 today, 48..143 after Plan 07) — a broken pin takes the whole chain red."
  prohibitions:
    - statement: "No assertion is weakened or deleted to make a site fail-loud — a silent pass becomes a loud failure, never a removed check."
      status: flagged-unverified
      verification: "The registered check count of `check-phase-67.mjs` is identical before and after, and every `V-67-NN` id present before is present after."
    - statement: "The shared frozen-read wrapper's own try/catch is not modified; the fix is applied at the call sites only."
      status: flagged-unverified
      verification: "`git diff HEAD~1 -- scripts/validation/check-phase-67.mjs` shows no change inside the wrapper reader function body."
---

<objective>
Land `check-phase-67.mjs`'s remaining fail-loud sites — TEN sites in TWO classes, not seven. This is
the Phase-144 half of SWEEP-09, formally routed into this phase and owner-ratified 2026-08-12
(D-03/D-04).

Purpose: `v1.20-CARVE.md:272-274` states the obligation as "7 chicken-and-egg guards plus 3 partial-null
silent passes". `[MEASURED]` the seven chicken-and-egg returns sit at `:73, :88, :117, :132, :165,
:199, :227`; of the five `nullCount` accumulator blocks exactly three (V-67-01, V-67-03, V-67-06) can
silently pass on a partial-null read, because each one's fail threshold is strictly below its file
count — the remaining two (V-67-05, V-67-07) have thresholds equal to their file counts and already
hard-FAIL. A five-site count would fix two sites that are not broken and miss the distinction the
CARVE records.

Output: one census ledger row (committed first), one edit commit, one regression proof. Rated costly:
`check-phase-67.mjs` is a chain member in every apex and is pinned by `check-phase-73.mjs`, so a
broken pin takes the whole chain red.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-RESEARCH.md
@.planning/milestones/v1.20-CARVE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GOV-02 target-scoped census row — path string AND symbol, before the edit</name>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `.planning/milestones/v1.20-CARVE.md` — `## GOV-02 grep procedure (D-12)`: the census must search for the FILE PATH STRING as well as the symbol, because a symbol-only grep misses call sites that pin a path literal (the worked example is `check-phase-66.mjs:42`'s `CI_WORKFLOW` path literal)
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` — the row schema and the most recent rows
    - `scripts/validation/check-phase-73.mjs:250-310` — `V-73-CONVERT-67-05` and `-06`, the four literals pinned against this file's source
  </read_first>
  <action>
Census before edit. Grep `scripts/`, `.github/` and `package.json` for BOTH forms — the path string
`check-phase-67.mjs` and every symbol this file exposes to other validators (`V-67-01` through
`V-67-07` and any constant another validator names). Pass `grep -a` on `carve-gate.mjs`, which is
NUL-classified and otherwise reports a bare binary-match line with no line numbers, silently reading
as zero hits.

Record one appended ledger row: both grep forms and their hit counts, every pinning call site found
with file and line, the four literals `check-phase-73.mjs` asserts against this file's source quoted
in the Result column so the next reader can re-check them without re-deriving, and the planned diff
shape (ten sites, two classes, return-field-only for seven of them). Append only — never edit or
reorder an existing row.

Commit alone, before any byte of the validator changes. Per D-33 do not quote the ledger's or the
validator's path in the message body.
  </action>
  <acceptance_criteria>
    - The ledger gains exactly one row; `git diff HEAD~1 --numstat -- .planning/milestones/v1.20-GOV-02-LEDGER.md` shows a deletions count of `0`.
    - The row records a path-scoped grep hit count and a symbol-scoped grep hit count separately.
    - The row names `check-phase-73.mjs` and the four pinned literals explicitly.
    - `git show --name-only --format= HEAD | wc -l` resolves to `1`.
    - `git diff HEAD~1 -- scripts` is empty.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>The census row is committed, names both grep forms and every pinning call site, and no validator byte has changed yet.</done>
</task>

<task type="auto">
  <name>Task 2: Land the ten fail-loud sites — seven return flips, three accumulator restructures</name>
  <reversibility rating="costly">`check-phase-67.mjs` is a chain member in every apex and is pinned verbatim by `check-phase-73.mjs`; undoing a wrong edit means re-greening the whole chain, not reverting one file in isolation.</reversibility>
  <precondition>The GOV-02 census row for this target is committed (Task 1) — `v1.20-CARVE.md:93-112` requires it to land before the edit.</precondition>
  <files>scripts/validation/check-phase-67.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-67.mjs` — in full: the seven early-return sites at `:73, :88, :117, :132, :165, :199, :227`, the five `nullCount` accumulator blocks and each one's fail condition and file count, and the shared wrapper reader
    - `scripts/validation/check-phase-73.mjs:266-302` — the four literals that must survive byte-identical
    - `scripts/validation/check-phase-61.mjs`, `-68.mjs`, `-70.mjs` — Phase 141 Plan 03's landed precedent for the same site class: a RETURN-FIELD-ONLY flip at the call site, never a change to the shared wrapper's try/catch
    - `144-RESEARCH.md` — the code-derived vulnerability analysis of each accumulator block (why V-67-01/03/06 can pass on a partial null and V-67-05/07 cannot)
  </read_first>
  <action>
Two distinct fix shapes, one commit.

Class 1 — the seven early returns. Apply the Phase 141 Plan 03 precedent exactly: flip only the
returned object at the call site, from a skipped pass to a failing return, and give each one a
file-specific detail string naming the unreadable path and the frozen tag it was read at. Do not
touch the shared wrapper reader's own try/catch — other call sites in the same wrapper still
legitimately receive a null and that behaviour is load-bearing for them.

Class 2 — the three vulnerable accumulator blocks (V-67-01, V-67-03, V-67-06). A threshold tweak is
not the fix: each of these can currently reach its pass threshold from the readable files alone while
an unreadable file is silently skipped. Restructure each so full readability is required before the
aggregate is evaluated at all — a non-zero null count is itself a failure naming which files were
unreadable — rather than letting the aggregate absorb the gap. Leave V-67-05 and V-67-07 structurally
alone: their thresholds equal their file counts, so any partial-null read already forces a fail.

Preserve every one of the four literals `check-phase-73.mjs` pins against this file's source
byte-identical, including whitespace: that pin is a plain substring/regex match, so even a
reformatting trips it. Preserve the check inventory: the same `V-67-NN` ids, the same count, the same
names apart from any detail-string change the fix requires.

Commit alone. Subject form `fix(144-03): …`; per D-33 name the artifact class in words rather than
quoting the path.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-67.mjs; echo $?` prints `0`, with the same registered check count and the same `V-67-NN` ids as before the edit.
    - `node scripts/validation/check-phase-73.mjs; echo $?` prints `0` — the four-literal pin survives.
    - Each of the four pinned literals is still present: `grep -c 'frozen-at-close' scripts/validation/check-phase-67.mjs` >= 1; `grep -Ec 'V-67-05.*v1\.7-frozen' scripts/validation/check-phase-67.mjs` >= 1; `grep -c 'Apple calls this artifact' scripts/validation/check-phase-67.mjs` >= 1; `grep -c 'SWEEP-02' scripts/validation/check-phase-67.mjs` >= 1.
    - The seven former early-return sites now return a failing result: a node one-liner or targeted read confirms each of the seven sites returns `pass: false` with a file-specific detail, and none returns `skipped: true`.
    - `git diff HEAD~1 -- scripts/validation/check-phase-67.mjs` shows no hunk inside the shared wrapper reader function body.
    - `node scripts/validation/carve-gate.mjs` exits 0 (this path is on-list under Category 5).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-67.mjs</automated>
  </verify>
  <done>Ten sites fail loud across two classes, V-67-05/07 are untouched, all four pinned literals survive, and both 67 and 73 exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Negative proof and chain regression gate</name>
  <files>.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md</files>
  <read_first>
    - `scripts/validation/check-phase-67.mjs` — the edited file
    - `scripts/validation/frozen-read-negative-test.mjs` — the existing negative-test harness idiom this repo uses to prove a fail-loud site actually fails
  </read_first>
  <action>
A fail-loud site that has never been observed failing is an assertion, not a proof. For at least one
site per class (one early return and one restructured accumulator), drive the read to fail in a
throwaway copy of the validator OUTSIDE the repository working tree — copy it to the session
scratchpad, point its frozen tag at an unreachable SHA or its file list at an absent path, run it,
and confirm the run reports a failure naming the unreadable file rather than a skip or a pass. Never
create or mutate a file under `scripts/` to stage this: an untracked in-scope path is a hard gate
failure.

Then run the chain regression gate: `check-phase-73.mjs` (the direct pin) and `check-phase-138.mjs`
(the predecessor apex, which spawns 67 as chain member). Record in `144-EVIDENCE.md`: the two negative
proofs with their observed failure text, the exit codes of 67 / 73 / 138, and 138's summary triple.
  </action>
  <acceptance_criteria>
    - The scratchpad negative run of the early-return site reports a failure naming the unreadable file; exit code is non-zero.
    - The scratchpad negative run of the restructured accumulator reports a failure naming the unread file; exit code is non-zero.
    - `node scripts/validation/check-phase-138.mjs` exits 0 and prints `95 PASS, 0 FAIL, 0 SKIPPED`.
    - `git status --porcelain --untracked-files=all -- scripts` prints nothing.
    - `144-EVIDENCE.md` records both negative proofs and all three exit codes.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-138.mjs</automated>
  </verify>
  <done>Both fix classes are proven to fail on a genuinely unreadable input, and the predecessor apex is still 95/0/0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen git read → validator verdict | A read that fails silently converts an unverified file into a passing check; this plan closes the last ten such conversions in this validator. |
| this validator's source text → `check-phase-73.mjs`'s pin | Another validator asserts on this file's literal bytes; the edit crosses that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-08 | Tampering | `check-phase-67.mjs` fail-loud sites | high | mitigate | Each class proven to fail on a genuinely unreadable input in a scratchpad copy, never asserted from source reading alone |
| T-144-09 | Tampering | `check-phase-73.mjs`'s four-literal pin | high | mitigate | GOV-02 census records the four literals before the edit; four positive greps plus a 73 run assert them after |
| T-144-10 | Denial of Service | the whole apex chain | medium | mitigate | `check-phase-138.mjs` re-run as a regression gate; 67 sits in the chain twice over (member and pinned) |
| T-144-11 | Repudiation | undocumented frozen-surface edit | medium | mitigate | Census row lands first, in its own commit, naming both grep forms |
</threat_model>

<verification>
- `check-phase-67.mjs`, `check-phase-73.mjs` and `check-phase-138.mjs` all exit 0; 138 prints 95/0/0.
- Ten sites changed, in two classes; V-67-05 and V-67-07 structurally untouched.
- All four pinned literals present; check inventory unchanged.
- Both fix classes proven failing against an unreadable input, outside the working tree.
- The census row landed in its own commit before the edit.
</verification>

<success_criteria>
The last ten silent-pass frozen-read sites in this validator fail loud, proven by negative test, with
the four-literal cross-validator pin and the apex chain both still green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files (phase-wide): `check-phase-139.mjs` … `-143.mjs`, `check-phase-144.mjs`,
`v1.20-milestone-audit.mjs`, `v1.20-audit-allowlist.json`,
`audit-harness-v1.20-integrity.yml`, `v1.20-MILESTONE-AUDIT.md`, `144-EVIDENCE.md`,
`dist/docs-library-v1.20.zip` (gitignored).
New exports: `readAtV119Close`, `lsTreeAtV119Close`.
New constants: `MILESTONE_CLOSE_SHAS.V119`, `BASELINE_24`, and `CHAIN_START` / `CHAIN_END` /
`CHAIN_PHASES` / `CHAIN_EXTRA` / `CHAIN_SKIP` in the new apex.
New CI job keys: `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`,
`check-phase-144`, `check-phase-139` … `check-phase-143`, `rotting-external-quarterly`,
`pin-helper-advisory`, `frozen-read-probe`.
Modified (this plan): `scripts/validation/check-phase-67.mjs`,
`.planning/milestones/v1.20-GOV-02-LEDGER.md`.
</artifacts_produced>

<output>
Create `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-03-SUMMARY.md` when done
</output>
