---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 05
type: execute
wave: 3
depends_on: ["144-01"]
files_modified:
  - scripts/validation/check-phase-142.mjs
  - scripts/validation/check-phase-143.mjs
autonomous: true
requirements: [HARN-18]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Both leaves are LIGHTWEIGHT on the `check-phase-135/136/137.mjs` template: empty `CHAIN_PHASES`, empty `CHAIN_SKIP`, needles plus a SELF dual-invariant, zero AUDIT / AUDIT-HARNESS / nested-guard references (D-14)."
    - "`check-phase-143.mjs` implements `143-NEEDLE-SPEC.md` sections 1 through 5 verbatim and HONOURS section 6's no-harness-fold hand-off boundary (D-05)."
    - "`check-phase-143.mjs` DECLINES the spec's optional corpus-level invariant recommendation (D-05, OWNER-RATIFIED): a live-HEAD, corpus-wide absence assertion inside a permanent apex member would go red at the first v1.21 content commit and stay red forever, manufacturing exactly the accepted-red class this milestone's bar requires deleting — `carve-gate.mjs:8-12` states the doctrine."
    - "The declined invariant's omission cost is bounded by the spec itself as trivial (a stray override renders as visible junk text, caught in ordinary content review), and HARN-19's sub-second spot-check of the nav-hub link checker protects the bar independently (D-26)."
    - "`check-phase-142.mjs` spawns `check-phase-30.mjs` and `check-phase-31.mjs` bare (standalone, non-nested) as its OWN needles, mirroring the apex's sidecar pair but WITHOUT registering them — `CHAIN_PHASES` stays empty."
    - "`check-phase-142.mjs` pins NEST-01's outcome by asserting the predecessor apex declares the two-member sidecar array, which is Phase 142's own deliverable and is not edited by this phase."
    - "No leaf pins a row count on `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` — Plan 11 appends to that file, so any equality would self-detonate; a lower bound is the only durable form."
    - "Zero `.planning/phases/` reads in either leaf (D-15); needle targets are `scripts/`, `.github/` or `.planning/milestones/v1.20-*` paths only."
    - "Each leaf exits 0 standalone and prints an `N PASS, 0 FAIL, K SKIPPED` summary with N equal to its registered check count."
  artifacts:
    - scripts/validation/check-phase-142.mjs
    - scripts/validation/check-phase-143.mjs
  key_links:
    - "Both leaves -> `check-phase-144.mjs`'s 96-entry chain (Plan 07); HAZARD FIX 3 turns an absent child into a hard FAIL (D-11)."
    - "`check-phase-143.mjs` -> `scripts/validation/check-nav-hub-links.mjs`, whose path is also pinned by `check-phase-123.mjs:14,40` — the tool must not be renamed (the spec's rename bar)."
  prohibitions:
    - statement: "No corpus-wide or `docs/`-wide negative assertion appears in either leaf, in any form, including a zero-count grep over a directory tree."
      status: flagged-unverified
      verification: "Neither new file references the docs corpus root as a walk target; a read of both sources confirms every assertion targets a named `scripts/`, `.github/` or milestones path."
    - statement: "`check-nav-hub-links.mjs` is not renamed, moved, or modified by this plan."
      status: flagged-unverified
      verification: "`git diff HEAD~2 -- scripts/validation/check-nav-hub-links.mjs` is empty."
---

<objective>
Author the remaining two lightweight leaves — `check-phase-142.mjs` and `check-phase-143.mjs` —
completing the five chain children the apex requires before it can run.

Purpose: `check-phase-143.mjs`'s content is already fully specified by the upstream hand-off
(`143-NEEDLE-SPEC.md` sections 1-5, with section 6's boundary honoured and the optional corpus-level
invariant declined per D-05). `check-phase-142.mjs` covers Phase 142's RED-04 through RED-07 and
NEST-01 deliverables. Both must exist before Plan 07's apex runs, because an absent chain child is a
hard FAIL (D-11).

Output: two write-once leaf validators, each exiting 0 standalone, neither carrying a live-HEAD
corpus assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-PATTERNS.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-NEEDLE-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: check-phase-142.mjs — the sidecar pair, the chain adoption, the cold-clone threshold</name>
  <files>scripts/validation/check-phase-142.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-135.mjs` — the LIGHTWEIGHT leaf template and the byte-identical runner tail
    - `scripts/validation/check-phase-119.mjs:135-160` — the subprocess-spawn needle idiom (graceful skip, narrowed missing-interpreter classifier, failure detail)
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-VERIFICATION.md`, its `142-0N-SUMMARY.md` files and `142-PATTERNS.md` — the measured actuals for RED-04 through RED-07 and NEST-01
    - `scripts/validation/check-phase-138.mjs:160-175` — the predecessor apex's two-member sidecar array and the concatenated chain loop, the shape this leaf asserts against
    - `scripts/validation/_lib/archive-path.mjs` — Phase 142's archival-path fix, including the live-path-first resolution order and the null-without-throw contract
  </read_first>
  <action>
Copy the leaf template, rename throughout, set the empty chain constants, write header line 4 in the
established LIGHTWEIGHT wording, copy the runner tail verbatim.

Needle 1 — the two standalone validators Phase 142 greened: spawn each of `check-phase-30.mjs` and
`check-phase-31.mjs` bare (no nested-invocation environment variable, since a leaf has no such guard)
and require exit 0 from each. Use the `check-phase-119.mjs` spawn idiom: graceful skip when the
interpreter is missing, real failure detail from stdout/stderr otherwise. Both are measured green at
HEAD with one deterministic environment skip each, driven by an absent optional link/diagram tool —
classify those skips in the detail string rather than treating them as failures.

Needle 2 — NEST-01's outcome: the predecessor apex declares the two-member sidecar array and keeps it
outside all three of its module-load guards. Assert the declaration's presence in that file's source.
That file is frozen for this phase, so the pin is durable.

Needle 3 — the archival-path helper's contract: it resolves the live path first and returns null
without throwing when neither location exists. Assert both properties from source, and record in the
detail string that this ordering is exactly why a null-resolve cannot be used as a wrong-token
detector (the reason Plan 07 asserts its archive-root token literally instead).

Needle 4 — the cold-clone threshold Phase 142 established: assert the recorded threshold constant is
present in the validator that owns it, using a content substring rather than a line coordinate.

Needle 5 — the SELF dual-invariant for 142.

Do not pin a row count on the deferred-cleanup document — Plan 11 appends to it. If a needle needs it
at all, use a lower bound.

Commit alone. Subject form `feat(144-05): …`.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-142.mjs; echo $?` prints `0` with an `N PASS, 0 FAIL, K SKIPPED` summary.
    - `grep -c 'const CHAIN_PHASES = \[\];' scripts/validation/check-phase-142.mjs` resolves to `1`.
    - `grep -c 'CHECK_PHASE_NESTED' scripts/validation/check-phase-142.mjs` resolves to `0`.
    - `grep -c '\.planning/phases' scripts/validation/check-phase-142.mjs` resolves to `0`.
    - The two spawned validators independently exit 0: `node scripts/validation/check-phase-30.mjs; echo $?` and `node scripts/validation/check-phase-31.mjs; echo $?` both print `0`.
    - `node scripts/validation/carve-gate.mjs` exits 0 with the new file on-list.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-142.mjs</automated>
  </verify>
  <done>The leaf exists, spawns both sidecar validators as its own needles without registering them, and exits 0 standalone.</done>
</task>

<task type="auto">
  <name>Task 2: check-phase-143.mjs — the hand-off spec, sections 1-5 verbatim, corpus invariant declined</name>
  <files>scripts/validation/check-phase-143.mjs</files>
  <read_first>
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-NEEDLE-SPEC.md` — in full: section 1 (the tool and its invocation), section 2 (the spawn idiom to copy), section 3 (the literals Phase 144 may pin), the optional corpus-level invariant section, section 4 (what the validator should assert), section 5 (the rename bar), section 6 (what is explicitly NOT handed off)
    - `scripts/validation/check-phase-119.mjs:135-160` — the spawn idiom the spec names
    - `scripts/validation/check-phase-135.mjs` — the LIGHTWEIGHT leaf shell the spec's checks are registered inside
    - `scripts/validation/check-phase-123.mjs:14,40` — the existing path pin on the nav-hub link checker (the rename bar's enforcement point)
    - `144-CONTEXT.md` D-05 — the ratified decline of the optional corpus-level invariant and the correction that the apex's termini assertion is numeric-only
  </read_first>
  <action>
Implement the hand-off specification. Sections 1 through 5 are implemented verbatim — the named tool,
its invocation form, the spawn idiom, the literals the spec authorizes pinning, the assertions it
lists, and the rename bar. Section 6's boundary is honoured: nothing the spec explicitly withholds is
folded in, in particular no harness-side check.

DECLINE the spec's optional corpus-level invariant section. This is an owner-ratified reversal of the
spec's own recommendation: a live-HEAD, corpus-wide absence assertion inside a permanent apex member
goes red at the first v1.21 content commit and stays red forever — the exact class the gate script's
own header doctrine bars, and the exact class this milestone's bar requires deleting rather than
manufacturing. Record the decline in a one-line source comment naming the authorizing decision so a
future reader does not re-add it as an oversight.

Wrap the spec's checks in the LIGHTWEIGHT leaf shell: empty chain constants, header line 4 in the
established wording, the byte-identical runner tail, and a SELF dual-invariant for 143.

Commit alone.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-143.mjs; echo $?` prints `0` with an `N PASS, 0 FAIL, K SKIPPED` summary.
    - Every assertion listed in section 4 of the hand-off spec is registered as a check, one for one, verified by reading the spec and the new file side by side.
    - `grep -c 'const CHAIN_PHASES = \[\];' scripts/validation/check-phase-143.mjs` resolves to `1`.
    - `grep -c '\.planning/phases' scripts/validation/check-phase-143.mjs` resolves to `0`.
    - The spawned tool needle passes independently: `node scripts/validation/check-nav-hub-links.mjs; echo $?` prints `0`.
    - `node scripts/validation/check-phase-123.mjs; echo $?` prints `0` — the tool's path pin still resolves, proving the rename bar was honoured.
    - The new file carries a source comment naming the declined corpus-level invariant and its authorizing decision.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-143.mjs</automated>
  </verify>
  <done>The leaf implements the hand-off spec sections 1-5, honours section 6, declines the optional corpus invariant with a recorded reason, and exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leaf needle → future content commits | Both leaves become permanent apex chain members; a live-HEAD corpus assertion here converts ordinary v1.21 content work into a permanent chain failure. |
| leaf → spawned validator/tool subprocess | Three needles shell out; argument arrays only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-16 | Denial of Service | permanent apex chain | high | mitigate | The optional corpus-level invariant is declined and the decline is recorded in source; no needle walks the docs corpus |
| T-144-17 | Tampering | spawned subprocesses in both leaves | medium | mitigate | `execFileSync` argument arrays copied verbatim from the `check-phase-119.mjs` analog |
| T-144-18 | Denial of Service | the nav-hub link checker's path pin | medium | mitigate | The rename bar is honoured and proven by running the validator that pins the path |
| T-144-19 | Repudiation | a silently dropped spec recommendation | low | mitigate | The decline carries a source comment naming its authorizing decision, so it reads as a ruling rather than an omission |
</threat_model>

<verification>
- Both leaves exit 0 standalone with `N PASS, 0 FAIL, K SKIPPED` summaries.
- Both declare empty chain and skip sets and carry a SELF dual-invariant.
- Section 4 of the hand-off spec maps one-for-one onto registered checks; section 6's boundary is intact; the optional corpus invariant is declined with a recorded reason.
- The nav-hub link checker is unmodified and its existing path pin still resolves.
- `node scripts/validation/carve-gate.mjs` exits 0 with both new files on-list.
</verification>

<success_criteria>
All five chain children now exist, so Plan 07's apex will find a complete `[48..143]` span rather
than five absent-child failures.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files (this plan): `scripts/validation/check-phase-142.mjs`, `check-phase-143.mjs`.
New files (phase-wide): `check-phase-139.mjs`, `-140.mjs`, `-141.mjs`, `check-phase-144.mjs` (apex),
`v1.20-milestone-audit.mjs`, `v1.20-audit-allowlist.json`, `audit-harness-v1.20-integrity.yml`,
`v1.20-MILESTONE-AUDIT.md`, `144-EVIDENCE.md`, `dist/docs-library-v1.20.zip` (gitignored).
New exports: `readAtV119Close`, `lsTreeAtV119Close`.
New constants: `MILESTONE_CLOSE_SHAS.V119`, `BASELINE_24`, and `CHAIN_START` / `CHAIN_END` /
`CHAIN_PHASES` / `CHAIN_EXTRA` / `CHAIN_SKIP` in the new apex.
New CI job keys: `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`,
`check-phase-144`, `check-phase-139` … `check-phase-143`, `rotting-external-quarterly`,
`pin-helper-advisory`, `frozen-read-probe`.
Modified across the phase: `_lib/frozen-at-close.mjs`, `v1.19-milestone-audit.mjs`,
`check-phase-67.mjs`, `regenerate-supervision-pins.mjs`, `audit-harness-v1.7-integrity.yml`,
`audit-harness-v1.8-integrity.yml`, `v1.20-CARVE.md`, `v1.20-GOV-02-LEDGER.md`,
`v1.20-DEFERRED-CLEANUP.md`, `.planning/{PROJECT,ROADMAP,STATE,REQUIREMENTS}.md`.
</artifacts_produced>

<output>
Create `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-05-SUMMARY.md` when done
</output>
