---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 08
type: execute
wave: 6
depends_on: ["144-07"]
files_modified:
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
  - .github/workflows/audit-harness-v1.20-integrity.yml
  - .github/workflows/audit-harness-v1.7-integrity.yml
  - .github/workflows/audit-harness-v1.8-integrity.yml
autonomous: true
requirements: [HARN-18]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.github/workflows/audit-harness-v1.20-integrity.yml` is a Path-A copy of the v1.19 workflow with EVERY hardcoded predecessor literal re-derived: the parse job's sidecar path (which appears twice), the path-match job's grep target, the harness-run job's harness path, and the chain job's span notice (D-16)."
    - "Copying the path-match job's grep string verbatim EXITS 1 — it greps for the predecessor's sidecar path inside the new harness, which does not contain it (D-16)."
    - "The `paths:` filter mirrors all SIX entries: the v1.20 script glob, the check-phase glob, the workflow's own path, the requirements document, and one milestones glob covering the four v1.20 governance documents (D-16)."
    - "The workflow is BORN with `fetch-depth: 0` on every checkout step — the count of checkout steps equals the count of depth settings, per file (HARN-18, SWEEP-01's standing contract)."
    - "The dual-apex contract is carried forward for THIS milestone's apex only: a chain job and a standalone job both run `check-phase-144.mjs`. No job runs `check-phase-138.mjs` — the real Path-A precedent is that the predecessor apex is never duplicated forward (D-16)."
    - "Five leaf jobs run `check-phase-139.mjs` through `-143.mjs`; chain jobs carry a 30-minute job timeout and leaf jobs 15 (D-16)."
    - "The advisory pin-helper job's SHAPE is copied forward but is never treated as evidence: it combines a continue-on-error setting with a trailing true and an echo fallback, so its conclusion is structurally always success and it can never report failure (D-16/D-21d)."
    - "Every job except the frozen-read probe declares a dependency on the harness job, so a harness failure produces six silent skips rather than one visible red — this topology is recorded in the workflow header so a future evidence pass reads those skips as gaps (D-16/D-21c)."
    - "Both stale runtime-estimate sites are corrected — the v1.7 workflow at line 96 AND the v1.8 workflow at line 95 — using the already-measured replacement figure of 664,979 ms, with no fresh measurement. Five routing documents name only v1.7 while citing the line number that is actually v1.8's, so a line-keyed edit would change the wrong file (D-06)."
    - "Both corrections land in the SAME commit as the new workflow's authoring, never alone (D-06)."
    - "A GOV-02 census row lands BEFORE the two frozen-workflow edits; the new workflow is a creation and needs no census of its own."
  artifacts:
    - .github/workflows/audit-harness-v1.20-integrity.yml
  key_links:
    - "The path-match job's grep literal -> `v1.20-milestone-audit.mjs`'s own sidecar path constant (Plan 06) — the two strings must be identical or the job exits 1."
    - "The chain and standalone apex jobs -> `check-phase-144.mjs` (Plan 07); the five leaf jobs -> the five leaves (Plans 04 and 05)."
    - "`audit-harness-v1.7-integrity.yml`'s existing pull-request path filters already match this phase's check-phase authoring and the requirements rewrite, so it fires on the close pull request regardless of whether its own line is touched — the incremental fan-out cost of the correction is zero (D-06)."
  prohibitions:
    - statement: "No job in the new workflow runs the predecessor apex; forward-duplicating a superseded apex is the anti-pattern D-16 names."
      status: flagged-unverified
      verification: "A grep of the new workflow for the predecessor apex filename returns zero hits."
    - statement: "The advisory pin-helper job's success is never recorded as evidence for any requirement."
      status: flagged-unverified
      verification: "`144-EVIDENCE.md` and the milestone audit classify that job as non-evidence wherever it appears."
    - statement: "No workflow file gains a secrets reference; none of the integrity workflows has ever carried one and this phase does not introduce the first."
      status: flagged-unverified
      verification: "A grep for the secrets context across all workflow files returns zero hits."
---

<objective>
Author the 17th CI coexistence workflow and, in the same commit, correct the stale runtime estimate
that lives in TWO predecessor workflows.

Purpose: HARN-18 requires the new workflow to exist and to be born with full-depth checkouts —
frozen reads resolve `git show <sha>:<path>`, and a shallow checkout makes every frozen read fail
with an invalid-object error. The stale-figure correction is routed into this phase and rides here
because both its cost arguments were falsified: the v1.7 workflow already fires on this phase's own
changed paths, so the incremental fan-out cost is zero, and the replacement figure is already
measured.

Output: one new workflow, two one-line corrections, one census row, and a structural proof that the
new workflow's literals resolve.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-PATTERNS.md
@.planning/milestones/v1.20-CARVE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GOV-02 census row for the two predecessor workflows</name>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `.planning/milestones/v1.20-CARVE.md` — the GOV-02 grep procedure, including its worked example of a validator pinning a workflow PATH LITERAL rather than a symbol (`check-phase-66.mjs:42`), which is exactly the class a symbol-only grep misses here
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` — the row schema
    - `.github/workflows/audit-harness-v1.7-integrity.yml` and `-v1.8-integrity.yml` — the two edit targets and their surrounding context
  </read_first>
  <action>
Census both workflow files before touching either. Grep `scripts/`, `.github/` and `package.json` for
each file's PATH STRING as well as for any symbol another validator uses to reach it — several
chain validators pin workflow paths as string constants, which a symbol-only grep would miss
entirely. Use `grep -a` on any NUL-classified target.

Record one appended ledger row covering both targets: the two grep forms and their hit counts, every
pinning call site with file and line, and the planned diff shape (one line changed per file, a
figure correction, no structural change). Note in the row that the new workflow is a creation covered
by the pre-existing Category 1 glob and needs no census of its own.

Commit alone, before either workflow byte changes.
  </action>
  <acceptance_criteria>
    - The ledger gains exactly one row covering both targets; `git diff --numstat` shows a deletions count of `0`.
    - The row records path-scoped and symbol-scoped hit counts separately, for each of the two files.
    - `git show --name-only --format= HEAD | wc -l` resolves to `1`.
    - `git diff HEAD~1 -- .github` is empty.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>The census row is committed and names every validator that pins either workflow path, before any workflow edit.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 17th workflow and correct both stale-figure sites, one commit</name>
  <files>.github/workflows/audit-harness-v1.20-integrity.yml, .github/workflows/audit-harness-v1.7-integrity.yml, .github/workflows/audit-harness-v1.8-integrity.yml</files>
  <precondition>The GOV-02 census row covering both predecessor workflows is committed (Task 1).</precondition>
  <read_first>
    - `.github/workflows/audit-harness-v1.19-integrity.yml` — in full, as the copy template: `:11-14` the dual-apex header contract, `:23-30` the six path entries, `:37` the parse job's sidecar path (twice), `:57-70` the path-match job's grep, `:83` the harness invocation, `:98-102` the chain job's span notice, `:105-145` the three leaf jobs, `:147` the standalone apex job, `:161` the quarterly job's event guard, `:196-213` the advisory pin-helper job, `:214` the frozen-read probe (the only job with no dependency key)
    - `.github/workflows/audit-harness-v1.7-integrity.yml:14-22` and `:96` — the existing pull-request path filters (proving the zero incremental fan-out cost) and the first stale-figure site
    - `.github/workflows/audit-harness-v1.8-integrity.yml:95` — the second stale-figure site
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md:480-481` — the already-measured replacement figure; do not re-measure
    - `scripts/validation/v1.20-milestone-audit.mjs` — its literal sidecar path constant, which the path-match job's grep must match exactly
  </read_first>
  <action>
Copy the v1.19 workflow forward and re-derive every predecessor literal.

Re-derive, at minimum: the parse job's sidecar path in both places it appears; the path-match job's
grep target, which must name the new sidecar path and must match the literal that actually appears
inside the new harness source (copied verbatim this job exits 1); the harness-run job's harness path;
and the chain job's span notice, which currently hardcodes the predecessor's span in prose.

Author the `paths:` filter with all six entries: the v1.20 script glob, the check-phase glob, the
workflow's own path, the requirements document, and one milestones glob covering the four v1.20
governance documents — the carve document, the deferred-cleanup document, the governance ledger and
the milestone audit this phase will author.

Job topology. Keep the dual-apex contract for THIS milestone's apex: a chain job and a standalone job,
both running the new apex. Do not add any job that runs the predecessor apex — the v1.19 workflow is
the only file that ever carried one and that is a defect, not the pattern. Five leaf jobs replace the
predecessor's three. Chain jobs carry a 30-minute timeout, leaf jobs 15. Every checkout step is born
full-depth. Copy the quarterly job's event guard, the advisory pin-helper job's shape, and the
frozen-read probe forward as-is, and restate the dual-apex header contract verbatim.

Record two facts in the workflow header so a future evidence pass reads them correctly: the advisory
pin-helper job is structurally always successful and is therefore not evidence; and every job except
the frozen-read probe depends on the harness job, so a harness failure yields six silent skips rather
than one visible red.

In the SAME commit, correct the stale runtime estimate in the two predecessor workflows — one line
each, at the two sites named in the read-first list. Replace each with the already-measured figure of
664,979 ms; take no fresh measurement. Note that the routing documents which flag this defect name
only the v1.7 file while citing a line number that belongs to the v1.8 file, so edit by CONTENT at
each site rather than by line number.

Commit all three files together. Subject form `feat(144-08): …`; per D-33 do not quote workflow paths
in the message body.
  </action>
  <acceptance_criteria>
    - The new workflow parses as YAML: a node or `yq`-equivalent parse of the file exits 0.
    - `grep -c 'uses: actions/checkout' .github/workflows/audit-harness-v1.20-integrity.yml` equals `grep -c 'fetch-depth: 0' .github/workflows/audit-harness-v1.20-integrity.yml`.
    - `grep -c 'check-phase-138' .github/workflows/audit-harness-v1.20-integrity.yml` resolves to `0`.
    - `grep -c 'check-phase-144' .github/workflows/audit-harness-v1.20-integrity.yml` resolves to `2` or more (chain job plus standalone job).
    - Each of `check-phase-139` through `check-phase-143` appears at least once in the new workflow.
    - `grep -c 'v1.19' .github/workflows/audit-harness-v1.20-integrity.yml` resolves to `0`.
    - The path-match job's grep target string appears verbatim in `scripts/validation/v1.20-milestone-audit.mjs`, verified by running that exact grep locally and observing exit 0.
    - The `paths:` list contains exactly six entries.
    - `git diff HEAD~1 --numstat -- .github/workflows/audit-harness-v1.7-integrity.yml` and the same for the v1.8 file each show one added and one deleted line; both new lines contain `664,979` (or the same figure in an unpunctuated form consistently applied).
    - `git show --name-only --format= HEAD | wc -l` resolves to `3`.
    - `node scripts/validation/carve-gate.mjs` exits 0 (all three paths on-list under Category 1).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-66.mjs</automated>
  </verify>
  <done>The 17th workflow exists with re-derived literals, six path entries, full-depth checkouts, a dual-apex pair for this milestone's apex and five leaf jobs; both stale-figure sites are corrected in the same commit.</done>
</task>

<task type="auto">
  <name>Task 3: Structural proof and predecessor-workflow regression sweep</name>
  <files>.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md</files>
  <read_first>
    - `.github/workflows/audit-harness-v1.20-integrity.yml` — the authored workflow
    - `scripts/validation/check-phase-66.mjs` and any other validator the Task 1 census found pinning a workflow path literal
  </read_first>
  <action>
Prove the new workflow's cross-file literals resolve locally, since a dispatch cannot happen until
the owner checkpoint in Plan 09. For each job that names a script path, confirm the path exists and
the script exits 0 when run with the job's own arguments. For the path-match job, run its grep
verbatim and confirm exit 0. Enumerate every job key and its display name and record both columns —
the display name is what the evidence pass in Plan 10 must match on, and the two differ.

Then run the regression sweep the census identified: every validator that pins either edited workflow
path, plus the new apex. Record exit codes and summary triples in `144-EVIDENCE.md`, along with the
job-key-to-display-name table and the count of checkout steps versus depth settings for the new file.
  </action>
  <acceptance_criteria>
    - Every script path named by a job in the new workflow exists on disk; each named validator exits 0 when run locally.
    - The path-match job's grep run verbatim exits 0.
    - Every validator the census identified as pinning either edited workflow path exits 0.
    - `node scripts/validation/check-phase-144.mjs` exits 0 and still prints `100 PASS, 0 FAIL, 1 SKIPPED`.
    - `144-EVIDENCE.md` contains the job-key-to-display-name table for all jobs in the new workflow.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-144.mjs</automated>
  </verify>
  <done>Every cross-file literal in the new workflow is proven to resolve locally, the job-key/display-name table is recorded for Plan 10's evidence pass, and no predecessor pin regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow YAML → CI runner | The workflow defines what runs on the default branch; a wrong literal degrades a gate into a silently-passing or silently-skipped job. |
| workflow job conclusion → requirement evidence | A structurally-always-green job and a dependency-cascade skip both look like "not red" without being evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-29 | Spoofing | the advisory pin-helper job | high | mitigate | Its structurally-always-success shape is recorded in the workflow header and classified as non-evidence in the evidence file and the milestone audit |
| T-144-30 | Tampering | shallow checkout | high | mitigate | Born full-depth; a per-file equality between checkout steps and depth settings is asserted, since a shallow checkout makes every frozen read fail with an invalid-object error |
| T-144-31 | Denial of Service | the harness-dependency cascade | medium | mitigate | The six-silent-skips topology is recorded in the header, and the evidence rules classify a cascade skip as a gap rather than a legitimate skip |
| T-144-32 | Tampering | copied predecessor literals | medium | mitigate | Every literal re-derived; the path-match grep is run verbatim locally and a zero-count grep for the predecessor version string asserts nothing was carried over |
| T-144-33 | Elevation of Privilege | secrets in a pull-request-triggered workflow | low | accept | No integrity workflow has ever referenced a secrets context and this one does not either; asserted by a zero-count grep |
</threat_model>

<verification>
- The new workflow parses, carries six path entries, full-depth checkouts, a dual-apex pair for this milestone's apex, five leaf jobs, and zero references to the predecessor apex or the predecessor version string.
- The path-match grep resolves against the new harness source.
- Both stale-figure sites corrected, one line each, in the same commit as the new workflow.
- The census row landed first; every validator pinning either edited workflow path still exits 0.
- The apex still prints `100 PASS, 0 FAIL, 1 SKIPPED`.
</verification>

<success_criteria>
The 17th integrity workflow exists on disk, structurally sound and internally consistent with the
harness, sidecar, apex and leaves this phase authored — ready for the owner to push and dispatch.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files (this plan): `.github/workflows/audit-harness-v1.20-integrity.yml`.
New CI job keys (this plan): `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`,
`check-phase-144` (standalone), `check-phase-139` … `check-phase-143`,
`rotting-external-quarterly`, `pin-helper-advisory`, `frozen-read-probe`.
New files (phase-wide): `check-phase-139.mjs` … `-144.mjs`, `v1.20-milestone-audit.mjs`,
`v1.20-audit-allowlist.json`, `v1.20-MILESTONE-AUDIT.md`, `144-EVIDENCE.md`,
`dist/docs-library-v1.20.zip` (gitignored).
New exports: `readAtV119Close`, `lsTreeAtV119Close`.
New constants: `MILESTONE_CLOSE_SHAS.V119`, `BASELINE_24`, and the apex's span and sidecar constants.
Modified across the phase: `_lib/frozen-at-close.mjs`, `v1.19-milestone-audit.mjs`,
`check-phase-67.mjs`, `regenerate-supervision-pins.mjs`, `audit-harness-v1.7-integrity.yml`,
`audit-harness-v1.8-integrity.yml`, `v1.20-CARVE.md`, `v1.20-GOV-02-LEDGER.md`,
`v1.20-DEFERRED-CLEANUP.md`, `.planning/{PROJECT,ROADMAP,STATE,REQUIREMENTS}.md`.
</artifacts_produced>

<output>
Create `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-08-SUMMARY.md` when done
</output>
