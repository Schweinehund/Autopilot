---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 07
subsystem: validation
tags: [chain-apex, validator, ci, HARN-18]
dependency graph:
  requires: ["144-04", "144-05", "144-06"]
  provides: ["check-phase-144.mjs (v1.20 chain-apex)"]
  affects: ["144-08+ (17th workflow authoring, close-gate)"]
tech-stack:
  added: []
  patterns:
    - "Apex chain-loop mechanism inherited from check-phase-138.mjs, every count/span re-derived (D-12)"
    - "Fourth module-load guard (CHAIN_EXTRA disjointness) added on top of the inherited dedup/length/termini triad (D-09)"
    - "Archive-root token asserted as a literal string in source, not detected via resolver null/success behavior (D-15)"
key-files:
  created:
    - scripts/validation/check-phase-144.mjs
  modified:
    - .planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md
decisions:
  - "CHAIN_EXTRA disjointness guard declared immediately after CHAIN_EXTRA itself (matching the predecessor's declaration position), keeping it grouped with — but structurally distinct from — the three CHAIN_PHASES-only guards that precede it"
  - "Archive-root token 'v1.20-phases' asserted as a bare literal-string constant (ARCHIVE_ROOT_TOKEN) passed to the resolver, satisfying both the grep acceptance criterion and the D-15 replacement guardrail"
metrics:
  duration: "~35 minutes"
  completed: 2026-08-13
status: complete
actuals:
  tokens: 5930
  tasks: 3
  commits: 4
---

# Phase 144 Plan 07: Author the v1.20 Chain-Apex (`check-phase-144.mjs`) Summary

Authored `check-phase-144.mjs`, the new chain-apex spanning `[48..143]` (96 arithmetic entries plus
the `[30, 31]` sidecar), proved its four module-load guards fail-first against mutated scratchpad
copies, and ran it twice consecutively to confirm the exact expected pre-`144-VERIFICATION.md`
triple (`100 PASS, 0 FAIL, 1 SKIPPED`) with zero writes.

## What Was Built

**Task 1 — the apex itself.** `scripts/validation/check-phase-144.mjs`:
- `CHAIN_START = 48`, `CHAIN_END = 143`, `CHAIN_PHASES` generated by `Array.from` arithmetic (96
  entries), never a transcribed literal array.
- Three inherited module-load guards (dedup, length `!== 96`, termini `48`/`143`) carried forward
  as mechanism from `check-phase-138.mjs`, with every embedded number re-derived.
- `CHAIN_SKIP = new Set([])`, empty per the Phase 68 invariant.
- `CHAIN_EXTRA = [30, 31]` carried forward verbatim, outside all three CHAIN_PHASES guards and
  outside the `V-144-SELF` dual-invariant — plus a **fourth, new** module-load guard asserting
  every `CHAIN_EXTRA` member is absent from `CHAIN_PHASES`, closing the gap the predecessor
  structurally cannot cover (its own `CHAIN_EXTRA` is declared after all three of its guards).
- All three HAZARD FIXes (20 MiB `maxBuffer`, narrowed `ENOENT`/`127` missing-child classifier,
  absent-chain-child-is-FAIL) inherited unchanged as mechanism.
- `V-144-AUDIT-HARNESS` targets `scripts/validation/v1.20-milestone-audit.mjs` (16 checks, counted
  directly off the live `checks` array at authoring time — never copied from the predecessor's
  stale "17-check" claim); its existence-absent branch is a hard FAIL and precedes the NESTED guard.
- Archive-root token `'v1.20-phases'` asserted as a literal string constant, not inferred from the
  resolver's null/success behavior (which cannot distinguish a wrong token pre-archival — see
  `check-phase-125.mjs:86`'s permanently-green wrong-token counterexample).
- Every runtime-emitted count and span string (entry count, header span, SELF detail, harness check
  count) re-derived at authoring time; no phrase claims a non-nested chain result (D-32).

**Task 2 — guard-proof evidence.** Four mutated copies of the apex (plus its two `_lib/`
dependencies) were built and loaded from the session scratchpad, never from `scripts/`. Each
mutation isolated exactly one invariant:
- Dedup: a duplicated interior entry (length/termini untouched) → throws `unique count 95 !== 96`.
- Length: `CHAIN_END` shifted to `142` → throws `length 95 !== 96`.
- Termini: `CHAIN_START`/`CHAIN_END` shifted to `49`/`144` (length still 96) → throws `must span
  48..143 (got 49..144)`.
- Disjointness (the guard the predecessor cannot have): `CHAIN_EXTRA` mutated to `[48, 31]` → throws
  `CHAIN_EXTRA overlaps CHAIN_PHASES -- must stay disjoint`.

All four throw messages recorded verbatim in `144-EVIDENCE.md`. `scripts/` stayed untouched
throughout (`git status --porcelain --untracked-files=all -- scripts` empty before and after).

**Task 3 — idempotency run.** Two consecutive `node scripts/validation/check-phase-144.mjs` runs at
the same commit both produced `100 PASS, 0 FAIL, 1 SKIPPED (total checks: 101)`, byte-identical
stdout, exit 0, ~24.4-24.5s wall-clock each. `git status --porcelain` identical before and after
both runs — the apex performs no write. Arithmetic reconciled: 1 AUDIT + 96 CHAIN_PHASES + 2
CHAIN_EXTRA + 1 AUDIT-HARNESS + 1 SELF = 101 total checks, matching D-10 exactly. Run separately
from the verifier/`carve-gate.mjs` pass per D-35 (`carve-gate.mjs` was verified independently in
Task 1: `exit 0`, 115 in-scope, 0 off-list).

## Verification Results

- `node scripts/validation/check-phase-144.mjs` (twice): `100 PASS, 0 FAIL, 1 SKIPPED`, exit 0, both
  times, byte-identical.
- `node scripts/validation/carve-gate.mjs`: exit 0, `in-scope=115, on-list=115, off-list=0`.
- All four module-load guards proven fail-first via mutated scratchpad copies (Task 2 above).
- `CHAIN_PHASES.length === 96`, `CHAIN_PHASES[0] === 48`, `CHAIN_PHASES[95] === 143`,
  `CHAIN_SKIP.size === 0`, `CHAIN_EXTRA` deep-equal `[30, 31]` — confirmed via a standalone
  arithmetic re-derivation (not by running the full module, which would execute the 98-child chain).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Self-introduced: overwrote the shared `144-EVIDENCE.md` measurement ledger
instead of appending to it**
- **Found during:** Task 2, immediately after committing.
- **Issue:** `144-EVIDENCE.md` already existed as a cross-plan "Measurement Ledger" opened by
  Plan 01 Task 2, with an explicit "later plans append rather than replace" contract, and Plans
  01/02/03/06 had already appended sections to it. The `Write` tool was used to create the guard-proof
  evidence content without first reading the existing file, which silently replaced the entire prior
  ledger (all Plan 01/02/03/06 content) with only the new Task 2 section. This was caught immediately
  by inspecting the commit's diffstat (`169 deletions` on what should have been a pure append).
- **Fix:** Recovered the full pre-overwrite content via `git show <parent>:<path>`, reconstructed the
  complete file (original Plan 01-06 content + the new Plan 07 Task 2 section appended, not
  replacing), and committed the restoration as a separate fix commit.
- **Files modified:** `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md`
- **Commit:** `27253f61`

## Commits

- `a36d48e1`: feat(144-07): author the apex — arithmetic span, four module-load guards, re-derived literals
- `1a3cc6c1`: docs(144-07): prove the four module-load guards fail-first, outside the working tree
- `27253f61`: fix(144-07): restore Plan 01-06 ledger content in 144-EVIDENCE.md, appended not replaced
- `738217e2`: docs(144-07): run the apex and record the exact triple, twice

## Self-Check: PASSED

- `scripts/validation/check-phase-144.mjs` FOUND on disk.
- `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md` FOUND on
  disk, contains both the restored Plan 01-06 content and the new Plan 07 Task 2/Task 3 sections.
- All four commit hashes (`a36d48e1`, `1a3cc6c1`, `27253f61`, `738217e2`) FOUND in `git log --oneline --all`.
- `node scripts/validation/carve-gate.mjs` exits 0 with `check-phase-144.mjs` on-list.
