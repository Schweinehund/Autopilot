---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 11
type: execute
wave: 9
depends_on: ["144-10"]
files_modified:
  - .planning/milestones/v1.20-MILESTONE-AUDIT.md
  - .planning/milestones/v1.20-DEFERRED-CLEANUP.md
autonomous: true
requirements: [HARN-19]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The deferred-cleanup close artifact is ABSORB-AND-APPEND at the existing v1.20 path, never a wholesale rewrite: all 15 routed rows are preserved verbatim and Part A (new) / Part B (carried) / Part C (dropped-and-Closed) are appended (D-27)."
    - "A wholesale rewrite would destroy evidence pinned as VERIFIED in two phases' verification documents and one plan summary, in the same phase that authors the validators derived from those measured actuals (D-27)."
    - "The predecessor close artifact carries 30 top-level entries; the v1.20 artifact's carried list must account for the eight still-open entries the draft omitted — the non-barred hub-wiring surface, the hygiene item, the Part-A scope correction distinct from the Part-C entry of the same name, the shared-taxonomy document, the past-due anchor review, the rollback-recovery divergence count, the wired-802.1X item, and both correction-of-record sections — plus the fence-axis item covering a live fence tracker inside a frozen chain validator that runs in every apex (D-28)."
    - "The pin-helper deferral moves to DROPPED-and-Closed, not CARRIED: its entry's entire content is the single defect it solely owns, that helper's self-test now exits 0 with an identical diff, and the requirement that owned it is Complete — carrying the owner while dropping the entry it owns is incoherent (D-28)."
    - "The broken hub-wiring regex item is NOT dropped: the validator carrying it is byte-unchanged since the predecessor close, and it is a NEGATIVE assertion, which no link-resolution checker can discharge (D-28)."
    - "The recipe outbound-link-coverage item is NOT dropped: its complaint is durability, and the tool it names has zero execution sites across scripts, workflows and the package manifest — only an existence pin — so the new leaf supplies coverage via the apex chain, not as a standalone job (D-28)."
    - "The carve residue item is either carried re-scoped OR dropped with its residue named as NEW — never both, which is what the draft did (D-28)."
    - "The identifier prefix the draft attributed to the predecessor artifact does not appear in it at all and is REMOVED; the unowned-tooling-item count is 10, not 12 (rows 1-6, 9, 10, 14, 15; one row is recorded-not-routed and four are routed into this phase) (D-28)."
    - "V120-PIN-DEFERRAL is registered as NEW, alongside the C17 frozen-aware residue across the five C17-bearing harnesses (D-02/D-28)."
    - "The root-cause-level double-booking guard is kept — it is what makes the pin-helper correction findable (D-28)."
    - "The milestone audit inherits the predecessor's section shape and records the five corrections of record: no severity ranking of workflows exists anywhere in this repository; the stale runtime estimate was a two-site class whose routing documents cited the wrong file's line number; the predecessor apex mis-states its harness's check count; a prior summary's row-count pin on the deferred-cleanup document was already stale when written; and the post-gate audit check cannot detect a wrong archive-root token because the resolver checks the live path first."
    - "No figure enters the milestone audit that was not measured — every number traces to a command run in this phase, recorded in the evidence file, or to a measured figure carried explicitly from the phase context."
  artifacts:
    - .planning/milestones/v1.20-MILESTONE-AUDIT.md
    - .planning/milestones/v1.20-DEFERRED-CLEANUP.md
  key_links:
    - "`v1.20-MILESTONE-AUDIT.md` -> the 17th workflow's milestones path glob (Plan 08), which is why that glob was written to cover a file that did not yet exist."
    - "The close artifact's Part C -> the two accepted-red dispositions the milestone bar requires deleted rather than carried a seventh milestone."
    - "`144-EVIDENCE.md` -> every mechanical-checks value in the audit's front matter."
  prohibitions:
    - statement: "No figure is written into the milestone audit that was not measured in this phase or carried as an explicitly-measured value — no estimate, no inference, no figure transcribed from a predecessor's prose."
      status: flagged-unverified
      verification: "Every numeric claim in the audit is traceable to a row in `144-EVIDENCE.md` or to a phase-context measurement, checked one by one during authoring."
    - statement: "The apex is never described as producing a result across a non-nested chain — it sets the nested variable on every child, so no such result exists (D-32)."
      status: flagged-unverified
      verification: "A grep of the milestone audit for that phrasing returns zero hits."
    - statement: "No existing row of the deferred-cleanup document is edited, reordered or deleted; the close artifact appends only."
      status: flagged-unverified
      verification: "`git diff HEAD~1 -- .planning/milestones/v1.20-DEFERRED-CLEANUP.md` shows additions only, zero deletions."
---

<objective>
Author the two close artifacts: the v1.20 milestone audit and the v1.20 deferred-cleanup close
artifact, absorbing and appending rather than rewriting.

Purpose: these are the documents the close-gate is read against, and both have a specific known
failure mode. The audit's is fabricated precision — a figure that sounds measured and is not. The
close artifact's is destruction by rewrite: the existing file already carries 15 routed rows,
authored across two prior phases and pinned as VERIFIED in their verification documents, and this is
the same phase that derived its new validators from those measured actuals.

Output: one milestone audit inheriting the predecessor's section shape, and one absorb-and-append
close artifact with Part A / Part B / Part C.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md
@.planning/milestones/v1.20-DEFERRED-CLEANUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: v1.20-MILESTONE-AUDIT.md — the predecessor's shape, this phase's measurements</name>
  <files>.planning/milestones/v1.20-MILESTONE-AUDIT.md</files>
  <read_first>
    - `.planning/milestones/v1.19-MILESTONE-AUDIT.md` — in FULL, all 431 lines: the front-matter key set, the executive summary, the per-phase closure narrative, the three-axis stacking section, the cross-OS exact-match table, the predecessor-cascade scan, the byte-unchanged gate, the traceability table, the mechanical-checks detail, the four-part terminal gate, and the harness-lineage list. The research read only through line 342 and flagged the remainder as unread, so read the tail directly rather than inferring it
    - `.planning/phases/144-*/144-EVIDENCE.md` — every measurement this audit may cite
    - `.planning/REQUIREMENTS.md` — all 28 requirement ids for the traceability table
    - `144-CONTEXT.md` specifics — the five corrections of record and the measured figures marked do-not-re-derive
  </read_first>
  <action>
Author the audit on the predecessor's section shape, with this milestone's own numbers.

Front matter carries the mechanical-checks block: the close SHA, the back-anchor pin SHA, the
authoritative CI SHA and run ids, the cross-OS exact-match flag, the apex counts standalone and at
close, the self-test result, the harness result, the axis dispositions and the notes field. Every
value comes from `144-EVIDENCE.md`. Where the evidence file has no value, the field is absent or
explicitly null — never a plausible-looking placeholder.

Body sections follow the predecessor's ordering, which is Claude's discretion within that inherited
shape: executive summary; per-phase closure narrative with one subsection per phase 139 through 144;
the three-axis stacking section; the cross-OS exact-match table with one row per validator class; the
predecessor-workflow cascade scan over the freshly-enumerated 17; the predecessor byte-unchanged gate;
requirements traceability with one row per requirement id; the mechanical-checks detail; this
milestone's terminal gate in parts; and the harness-lineage list — harness copy, sidecar copy,
baseline refresh, the new leaves and apex, the back-anchor pin, the CI workflow, and the archive-root
token confirmation.

Record the five corrections of record explicitly, in their own section:
  1. No severity ranking of workflows exists anywhere in this repository; the red set of record is the
     base workflow plus nine versioned ones, with no ordering, and most of them were outside the
     sample a prior phase drew from.
  2. The stale runtime estimate was a TWO-site class, and five routing documents named one file while
     citing the other file's line number.
  3. The predecessor apex describes its harness as carrying one more check than it registers.
  4. A prior plan summary's row-count pin on the deferred-cleanup document was already stale when
     written — a passed-at-the-time assertion, not a re-runnable gate.
  5. The post-gate audit check cannot detect a wrong archive-root token, because the resolver checks
     the live path first; the counterexample is a predecessor validator that has been permanently
     green with a factually false detail string.

Never describe the apex as producing a chain result outside nested invocation: it sets the nested
variable on every child, so no such result exists (D-32). The predecessor audit already corrects this
same defect once — do not reintroduce it.

Commit alone. Subject form `docs(144-11): …`.
  </action>
  <acceptance_criteria>
    - The audit's front matter contains a mechanical-checks block whose every populated field traces to a row in `144-EVIDENCE.md`.
    - The requirements traceability table has exactly 28 rows, one per requirement id in the requirements document.
    - The cross-OS exact-match table has one row per validator class and one column per axis.
    - The corrections-of-record section contains all five items.
    - A grep of the audit for the barred non-nested-chain phrasing returns `0`.
    - Every numeric claim in the audit is listed, during authoring, against its source row in the evidence file — with zero unsourced numbers.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-144.mjs</automated>
  </verify>
  <done>The milestone audit exists on the inherited shape, every figure is sourced, and the five corrections of record are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: v1.20-DEFERRED-CLEANUP.md — absorb and append, Part A / B / C</name>
  <files>.planning/milestones/v1.20-DEFERRED-CLEANUP.md</files>
  <read_first>
    - `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` — in full: its header offering the same-path or successor-named option, its supersession clause, and all 15 routed rows (rows 1-12 from one phase, rows 13-15 appended by another)
    - `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` — in full: the Part A / Part B / Part C shape, its 30 top-level entries, and specifically the still-open entries at the non-barred hub-wiring surface, the hygiene item, the Part-A scope correction, the shared-taxonomy document, the past-due anchor review, the rollback-recovery divergence count, the wired-802.1X item, both correction-of-record sections, the carve-residue entry, the Windows deep-nest timeout entry, the two accepted-red dispositions, the latent non-frozen-aware assertion entry, and the pin-helper deferral entry
    - `144-CONTEXT.md` — D-27 and all six of D-28's corrections, plus the deferred-ideas list naming what carries forward
    - `144-EVIDENCE.md` — the discharge evidence for both accepted-red dispositions
  </read_first>
  <action>
Absorb and append at the existing path. Preserve all 15 routed rows byte-identical — they are pinned
as VERIFIED in two verification documents and a plan summary, and this is the phase that derived its
new validators from those same measured actuals. Then append three parts.

Part A, NEW this milestone: the C17 frozen-aware residue across all five C17-bearing harnesses (the
carve residue named as new, if the carve entry itself is dropped rather than carried re-scoped —
choose one, never both), and the deferred back-anchor pin for the next milestone.

Part B, CARRIED: every still-open predecessor entry, including the eight the draft omitted and the
fence-axis item covering a live fence tracker inside a frozen chain validator that runs in every apex.
Also carried, explicitly: the broken hub-wiring regex item — the validator holding it is byte-unchanged
since the predecessor close and its assertion is a NEGATIVE one, which no link-resolution checker can
discharge — and the recipe outbound-link-coverage item, whose named tool still has zero execution
sites outside an existence pin. Carry the ten unowned tooling items, not twelve: one row is
recorded-not-routed and four are routed into this phase.

Part C, DROPPED-and-Closed: the two accepted-red dispositions, each with its discharge evidence cited
from the evidence file — all 17 green at one shared SHA for the CI disposition, and all ten members
re-run standalone at the close SHA for the scoped disposition. Also the pin-helper deferral, moved
here rather than carried: its entry's entire content is the one defect it solely owns, that defect is
fixed, and the requirement owning it is Complete.

Remove the identifier prefix the draft attributed to the predecessor artifact — it does not appear in
that document at all.

Keep the root-cause-level double-booking guard: it is what makes the pin-helper correction findable,
and it is the mechanism that stops an entry being carried and dropped at once.

Commit alone.
  </action>
  <acceptance_criteria>
    - `git diff HEAD~1 --numstat -- .planning/milestones/v1.20-DEFERRED-CLEANUP.md` shows a deletions count of `0`.
    - All 15 pre-existing routed rows are present and byte-identical, verified by diffing the pre-existing region against the previous revision.
    - Parts A, B and C each exist as top-level sections.
    - Part C contains both accepted-red dispositions, each with a cited discharge evidence reference, and the pin-helper deferral.
    - Part B contains every still-open predecessor entry named in the read-first list, including the fence-axis item; the two link-related items are present and NOT dropped.
    - A grep of the file for the removed identifier prefix returns `0`.
    - No entry appears in both Part B and Part C.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-144.mjs</automated>
  </verify>
  <done>The close artifact preserves all 15 routed rows and appends a complete Part A / B / C with no double-booked entry and both accepted-red dispositions in the dropped list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence file → audit claim | The audit is the permanent record; a figure written here outlives the run that produced it and is cited by later milestones. |
| predecessor close artifact → carried backlog | An entry silently dropped here disappears from the project's memory entirely; there is no other list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-43 | Repudiation | unsourced figures in the audit | high | mitigate | Every numeric claim traced to an evidence row during authoring; the do-not-re-derive measurements are carried explicitly rather than recomputed |
| T-144-44 | Tampering | wholesale rewrite of the close artifact | high | mitigate | Absorb-and-append with a zero-deletions assertion and a byte-identical check on the 15 pre-existing rows |
| T-144-45 | Repudiation | silently dropped backlog entries | high | mitigate | Every still-open predecessor entry enumerated in the read-first list and asserted present in Part B; no entry in both B and C |
| T-144-46 | Spoofing | a discharge claimed without evidence | high | mitigate | Both accepted-red dispositions carry a cited discharge reference into the evidence file rather than a narrative assertion |
</threat_model>

<verification>
- The audit exists on the inherited shape with 28 traceability rows, a sourced mechanical-checks block, and the five corrections of record.
- The close artifact preserves 15 rows verbatim and appends Parts A, B and C.
- Both accepted-red dispositions sit in Part C with cited discharge evidence; the pin-helper deferral is there too.
- Every still-open predecessor entry is in Part B; nothing is double-booked.
- The barred non-nested-chain phrasing appears nowhere.
</verification>

<success_criteria>
The two close artifacts exist, are evidence-sourced, preserve everything that had to be preserved, and
drop only what has documented discharge — so the close-gate flips against a record rather than a
narrative.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files (this plan): `.planning/milestones/v1.20-MILESTONE-AUDIT.md`.
New files (phase-wide): `check-phase-139.mjs` … `-144.mjs`, `v1.20-milestone-audit.mjs`,
`v1.20-audit-allowlist.json`, `audit-harness-v1.20-integrity.yml`, `144-EVIDENCE.md`,
`dist/docs-library-v1.20.zip` (gitignored).
New exports: `readAtV119Close`, `lsTreeAtV119Close`.
New constants: `MILESTONE_CLOSE_SHAS.V119`, `BASELINE_24`, and the apex's span and sidecar constants.
New CI job keys: `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`,
`check-phase-144`, `check-phase-139` … `check-phase-143`, `rotting-external-quarterly`,
`pin-helper-advisory`, `frozen-read-probe`.
Modified across the phase: `_lib/frozen-at-close.mjs`, `v1.19-milestone-audit.mjs`,
`check-phase-67.mjs`, `regenerate-supervision-pins.mjs`, `audit-harness-v1.7-integrity.yml`,
`audit-harness-v1.8-integrity.yml`, `v1.20-CARVE.md`, `v1.20-GOV-02-LEDGER.md`,
`v1.20-DEFERRED-CLEANUP.md`, `.planning/{PROJECT,ROADMAP,STATE,REQUIREMENTS}.md`.
</artifacts_produced>

<output>
Create `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-11-SUMMARY.md` when done
</output>
