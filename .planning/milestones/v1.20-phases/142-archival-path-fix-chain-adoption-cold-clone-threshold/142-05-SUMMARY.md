---
phase: 142-archival-path-fix-chain-adoption-cold-clone-threshold
plan: 05
subsystem: chain-apex governance / RED-06 chain adoption
tags: [check-phase-138, CHAIN_EXTRA, RED-06, GOV-02, CARVE]
dependency graph:
  requires: [142-02 (CARVE amendment authorizing check-phase-138.mjs edits), 142-03 (check-phase-30.mjs green), 142-04 (check-phase-31.mjs green)]
  provides: [check-phase-30.mjs and check-phase-31.mjs as executed members of the apex chain]
  affects: [scripts/validation/check-phase-138.mjs, Phase 144's successor apex (must carry CHAIN_EXTRA forward)]
tech-stack:
  added: []
  patterns: [hand-authored non-contiguous CHAIN_PHASES-adjacent sidecar array, precedent: check-phase-60.mjs / check-phase-62.mjs]
key-files:
  created: []
  modified:
    - scripts/validation/check-phase-138.mjs
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
decisions:
  - "CHAIN_EXTRA = [30, 31] declared as a literal sidecar array, concatenated only at the chain loop's iteration source; CHAIN_PHASES stays pure arithmetic (D-11)"
  - "Ledger row split across two commits within the same plan: Task 1 lands grep+precondition evidence, Task 2 finalizes with post-edit results in the same commit as the source edit (matching Plans 03/04's established pattern)"
metrics:
  duration: ~35 min
  completed: 2026-08-10
status: complete
actuals:
  tokens: 4250
  tasks: 2
  commits: 2
---

# Phase 142 Plan 05: Chain Adoption (RED-06) Summary

Adopted `check-phase-30.mjs` and `check-phase-31.mjs` — the last two members of the ten-member
standalone-RED set, both repaired and green in Plans 03/04 — into the apex `check-phase-138.mjs`'s
executed chain via a hand-authored `CHAIN_EXTRA = [30, 31]` sidecar array, concatenated only at the
chain loop's iteration source and kept structurally outside the three arithmetic-span module-load
guards and `V-138-SELF`'s two invariants.

## What Was Built

**Task 1 (precondition proof, grep, ledger row — no source edit):**
- Re-verified the D-12 hard sequencing precondition: `check-phase-30.mjs` exits 0 (`12 passed, 0
  failed, 1 skipped`) and `check-phase-31.mjs` exits 0 (`29 passed, 0 failed, 1 skipped`) — both
  green, splicing authorized to proceed.
- Ran the target-scoped + symbol-scoped GOV-02 grep for `check-phase-138.mjs` (D-18): 13 hits in
  `scripts/validation/`+`.github/workflows/` (all comments/job-invocations, zero `String.includes()`
  pins), 99 hits in `.planning/milestones/` (historical prose), 0 `CHAIN_EXTRA` hits in any `.mjs`
  source (confirming it is genuinely net-new to this edit; the only prior mention was the CARVE
  allowlist's own Plan-02 amendment comment).
- Confirmed `check-phase-138.mjs` is on the CARVE Category 5 allowlist (`grep -c
  '^scripts/validation/check-phase-138\.mjs$' v1.20-CARVE.md` = 1) and `carve-gate.mjs` exits 0
  (46/46 in-scope, pre-edit).
- Appended the GOV-02 ledger row (the third and last of D-18's three mandatory frozen-path rows for
  this phase) with the grep/precondition evidence; the Result/Regression-gate-run columns were left
  documenting the pending state, to be finalized in Task 2's commit alongside the source edit — the
  same two-commit-per-row pattern established by Plans 03/04.
- Committed alone (`c9f014c7`), no source file touched.

**Task 2 (the splice):**
- Declared `const CHAIN_EXTRA = [30, 31];` immediately after the three module-load guards
  (dedup/length/termini), with a rationale comment recording: it is a literal sidecar, not part of
  the arithmetic span and not subject to its guards; the HARN-18 "generated by arithmetic, never
  transcribed" mandate is satisfied because `CHAIN_PHASES` itself stays pure arithmetic; the
  `check-phase-60.mjs`/`check-phase-62.mjs` precedent for a hand-authored non-contiguous
  `CHAIN_PHASES`-adjacent array; the D-12 sequencing rule and the pre-fix 93 PASS/2 FAIL splice
  result it exists to avoid; and the note that the skip-set early exit now also applies to
  `CHAIN_EXTRA` members (untested, not wrong, since `CHAIN_SKIP` is empty and stays asserted empty).
- Changed exactly one line: the loop's iteration source, from `for (const phaseNum of CHAIN_PHASES)`
  to `for (const phaseNum of [...CHAIN_PHASES, ...CHAIN_EXTRA])`. The pre-existing
  `=== V-138-CHAIN-NN ===` comment block above it was left byte-unchanged (an earlier draft of this
  edit accidentally reworded that comment, producing a second removed line in the diff; corrected by
  moving all new prose into the sidecar's own comment block above, restoring the plan's "exactly one
  line removed" constraint).
- The loop body needed no change — its template-string interpolation already produces well-formed
  check ids/names (`V-138-CHAIN-30`, `V-138-CHAIN-31`) for the two new members.
- Finalized the GOV-02 ledger row with post-edit verification results and committed source + ledger
  together (`8019ab83`).

## Verification

- `node scripts/validation/check-phase-138.mjs` — `Result: 95 PASS, 0 FAIL, 0 SKIPPED (total checks:
  95)`, exit 0 (up from the pre-adoption 93/0/0 baseline)
- `--verbose` output: `V-138-CHAIN-30:` and `V-138-CHAIN-31:` each grep-count exactly 1
- `grep -c 'CHAIN_PHASES.length !== 90'` = 1, `grep -c 'CHAIN_END = 137'` = 1, `grep -c 'CHAIN_START
  = 48'` = 1 — all three module-load guards byte-unchanged
- `git diff -- scripts/validation/check-phase-138.mjs | grep -c '^-[^-]'` = 1 — exactly one line
  removed
- `node scripts/validation/check-phase-68.mjs` (RED-07 regression guard) — `Result: 33 PASS, 0 FAIL,
  0 SKIPPED`, exit 0, unchanged
- `node scripts/validation/carve-gate.mjs` — `47 in-scope, 47 on-list, 0 off-list`, exit 0
- `git status --porcelain docs/` — empty

## Assumption Delta Decision (recorded verbatim from the plan)

**Primary noun:** `CHAIN_PHASES` — unchanged. **Decision:** add-alongside — a literal `CHAIN_EXTRA =
[30, 31]` sits beside the arithmetic span rather than promoting a generalized "chain members"
primary. **Rationale:** D-11 / HARN-18 / `PROJECT.md:25` mandate the span array stay generated by
arithmetic and never transcribed; the sidecar keeps `CHAIN_PHASES` pure and confines the
hand-authored element to a named, precedented exception (`check-phase-60.mjs`, `check-phase-62.mjs`).
**Accepted debt (recorded, not guarded):** a third membership source, or an apex whose span start
drops to 31 or below, would force the promote this decision defers — the dedup guard is scoped to
the arithmetic array alone and does not see the union, so a collision at that point would be silent.
Neither condition holds today.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] First splice attempt reworded a pre-existing comment, breaking the "exactly one
line removed" acceptance criterion**
- **Found during:** Task 2, immediately after the initial edit
- **Issue:** The first draft of the splice rewrote the wording of the pre-existing `=== V-138-CHAIN-NN
  ===` comment block (to mention `CHAIN_EXTRA`), which turned that comment line into a second
  removed+added pair in the diff — violating the plan's explicit acceptance criterion that the diff
  removes exactly one line.
- **Fix:** Moved all new prose into the sidecar's own comment block (placed above the pre-existing,
  untouched comment), leaving the `=== V-138-CHAIN-NN ===` block byte-identical to its pre-edit
  wording. Re-verified `git diff | grep -c '^-[^-]'` = 1.
- **Files modified:** `scripts/validation/check-phase-138.mjs`
- **Commit:** `8019ab83` (fixed before commit; no separate commit needed)

No other deviations. Plan executed as written otherwise.

## Self-Check: PASSED

- FOUND: `scripts/validation/check-phase-138.mjs` (modified, `CHAIN_EXTRA` present)
- FOUND: `.planning/milestones/v1.20-GOV-02-LEDGER.md` (modified, row appended)
- FOUND commit `c9f014c7` in `git log --oneline`
- FOUND commit `8019ab83` in `git log --oneline`
- `node scripts/validation/check-phase-138.mjs` re-run: 95 PASS/0 FAIL/0 SKIPPED, exit 0 — confirmed
- `node scripts/validation/carve-gate.mjs` re-run: 47/47/0, exit 0 — confirmed
