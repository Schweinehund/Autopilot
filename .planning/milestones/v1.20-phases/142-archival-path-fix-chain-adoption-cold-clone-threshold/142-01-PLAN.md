---
phase: 142-archival-path-fix-chain-adoption-cold-clone-threshold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/milestones/v1.3-phases/31-ios-l2-investigation/31-VALIDATION.md
  - .planning/milestones/v1.20-DEFERRED-CLEANUP.md
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: true
requirements: [RED-04, RED-05, RED-06, NEST-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node scripts/validation/check-phase-54.mjs` exits 0 with the same tally after every planning-document edit in this plan (it is the sole LIVE reader of REQUIREMENTS.md/ROADMAP.md)"
    - "ROADMAP Phase 142 SC#1..SC#5 and the Discuss-phase-flags line each carry at least one `[DISCHARGED, D-NN]` or `[SUCCESS-CRITERION AMENDMENT, D-NN]` marker, and no pre-existing sentence has been deleted"
    - "`grep -c '48\\.\\.143' .planning/REQUIREMENTS.md` returns 2 and `grep -c '48\\.\\.143' .planning/PROJECT.md` returns at least 1, with every original `[48..138]` occurrence still present alongside its correction (annotate-not-overwrite, D-25)"
    - "The `~17s` / `+0.35s` figures are byte-unchanged in value on every surface that carries them; only markers are added (D-15)"
    - "`.planning/milestones/v1.20-DEFERRED-CLEANUP.md` exists and contains one row per each of the twelve CONTEXT.md `<deferred>` items, each naming a destination"
    - "`.planning/milestones/v1.20-GOV-02-LEDGER.md` gains one appended row for this plan's planning-document edits, following the Phase-139 precedent row that uses `check-phase-54.mjs` as the regression gate"
    - statement: "STATE.md's Phase-142 row still lists all five requirement IDs after this plan's edits (D-34 verified-not-assumed; the probe's unclassified RED-04/RED-05/RED-07 edge rows have no defensible criterion beyond this document-integrity check at Wave 1)"
      verification: backstop
    - statement: "Three of the eight applicable spec-less edge-probe rows carry no requirement anchor in the probe output and cannot be resolved here; they are surfaced as a flagged assumption rather than dismissed"
      verification: backstop
  prohibitions:
    - statement: "No file under `docs/` is created, edited, or deleted by this plan or any plan in this phase (D-01 — every failure resolves in the validators)"
      status: flagged-unverified
    - statement: "The numeric values `+0.35s` and `~17s` are never amended, only annotated — amending them would overturn Phase 141 mandatory statement 2 at 141-EVIDENCE.md:276-281 (D-15)"
      status: flagged-unverified
    - statement: "No original sentence on any amended surface is overwritten or deleted; markers annotate and supersede (D-23/D-33 discipline, Phase 141 D-27 precedent)"
      status: flagged-unverified
    - statement: "This plan's commit touches no path under `scripts/`, `.github/`, or `docs/` — the code edits belong to Plans 03/04/05 and the CARVE amendment to Plan 02 (D-19)"
      status: flagged-unverified
    - statement: "No marker bold span contains a colon; any file:line citation sits outside the `**...**` wrapper (project decision-bullet grammar rule)"
      status: flagged-unverified
  artifacts:
    - .planning/milestones/v1.20-DEFERRED-CLEANUP.md
    - "`.planning/milestones/v1.20-GOV-02-LEDGER.md` — one appended row (Plan 01)"
    - "`.planning/milestones/v1.3-phases/31-ios-l2-investigation/31-VALIDATION.md` — appended `[SUCCESS-CRITERION AMENDMENT, D-24]` annotation"
  key_links:
    - "`.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` -> `scripts/validation/check-phase-54.mjs` (the sole live reader; it fails if these documents drift in a shape it pins)"
    - "ROADMAP Phase 142 SC#1/SC#2 amendments -> Plans 03 and 04 (a code edit unauthorized by an amendment is drift at Phase 144's close-gate)"
    - "`31-VALIDATION.md`'s re-derived band -> `check-phase-31.mjs`'s V-31-29 bounds object in Plan 04 (the numbers must match)"
    - "`.planning/milestones/v1.20-DEFERRED-CLEANUP.md` -> Phase 144 CONTEXT (four of the twelve rows are named Phase-144 inputs)"
---

<objective>
Land **Commit 1 of 3** in D-19's fixed sequence: every success-criterion amendment this phase
needs, before any code is touched.

Purpose: `carve-gate.mjs` cannot see `.planning/`, so D-19's ordering is written-word discipline
enforced by plan sequencing alone. Phase 144's close-gate reads amendment markers and the CARVE's
"Recorded scope amendments" list to distinguish authorized scope from drift — an unmarked edit in
Plans 03-06 reads as drift at close. This plan also routes all twelve deferred items to one named
destination file, because Phase 141 established that an unrouted deferral is itself the defect.

Output: annotated ROADMAP/REQUIREMENTS/PROJECT/STATE surfaces, an annotated archived validation
spec, a new milestone-level deferral file, and one GOV-02 ledger row.

**Tracer note.** TRACER_MODE is on, but a tracer that reaches the deepest layer (the apex splice)
is *barred* by D-12 — adoption strictly after RED-04/RED-05 are green. Task 1 is therefore the
tracer over the layer stack this phase can prove first: one document surface edited end-to-end
through its live reader (`check-phase-54.mjs`), the governance gate (`carve-gate.mjs`), and the
apex, before the other seven surfaces follow. The thinnest *code*-layer slice is Plan 03 Task 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md
@.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-PATTERNS.md
@.planning/milestones/v1.20-CARVE.md
@.planning/milestones/v1.20-GOV-02-LEDGER.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end amendment slice — one surface, full gate stack</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` (D-15, D-11, D-19, D-33)
    - `.planning/ROADMAP.md` lines 167-184 (the Phase 142 block)
    - `.planning/REQUIREMENTS.md` lines 14-24 (the SWEEP-01/03/09 bullets — the live marker-shape precedent)
    - `scripts/validation/check-phase-54.mjs` lines 25-40 (the two live-read path constants)
  </read_first>
  <action>
    Prove the whole amendment pipeline on exactly ONE surface before expanding to the other seven.

    Amend ROADMAP Phase 142 **SC#3** only. It needs two markers, both inserted immediately after
    the `3. ` list marker and before the existing sentence, which stays byte-identical:

    (a) `**[DISCHARGED, D-15]**` — the "roughly +0.35s on a ~17s apex run" figure is CORRECT.
    Record inline, after the marker and outside any bold span, that four independent measurement
    sessions reproduced 15.9-18.7 s at 93/0/0 and the marginal adoption cost measured +220/+283 ms,
    and that `141-EVIDENCE.md:276-281` (Phase 141 mandatory statement 2) explicitly grandfathers
    the figure. Do NOT change the numbers — D-15 forbids it.

    (b) `**[SUCCESS-CRITERION AMENDMENT, D-11]**` — the *mechanism* changes. SC#3 as written says
    30 and 31 are "members of the apex's `CHAIN_PHASES` array". Per D-11 the mechanism is a
    hand-authored `CHAIN_EXTRA` sidecar concatenated into the check loop and deliberately EXCLUDED
    from `CHAIN_PHASES` and its three module-load span invariants, because HARN-18 and
    `PROJECT.md:25` require the span array stay generated by arithmetic and never transcribed.
    Record that chain membership is therefore satisfied by the apex's executed-chain set
    (the arithmetic span plus the sidecar), not by mutating the span; and that extending the span
    to 30 is not impossible but wrong in a specific way — with `CHAIN_START=30, CHAIN_END=137` the
    dedup guard passes and only the length guard throws, and a co-rebased constant set yields 16
    deterministic FAILs for the absent phases 32-47.

    Marker grammar: no colon inside any `**...**` span — put file:line citations in the plain text
    that follows the marker. Annotate only; delete nothing.

    Do not commit yet — Task 3 makes the single Commit 1.
  </action>
  <acceptance_criteria>
    - `grep -c "DISCHARGED, D-15" .planning/ROADMAP.md` returns `1`
    - `grep -c "SUCCESS-CRITERION AMENDMENT, D-11" .planning/ROADMAP.md` returns `1`
    - `grep -c "+0.35s on a ~17s apex run" .planning/ROADMAP.md` returns `1` (the original figure survives verbatim)
    - `node scripts/validation/check-phase-54.mjs` exits 0 and its `Summary:` line reports `0 failed`
    - `node scripts/validation/carve-gate.mjs` exits 0
    - `node scripts/validation/check-phase-138.mjs` exits 0 and prints `Result: 93 PASS, 0 FAIL, 0 SKIPPED (total checks: 93)` — the pre-adoption apex is unchanged by a documentation edit
    - `git status --porcelain scripts/ .github/ docs/` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs && node scripts/validation/carve-gate.mjs && node scripts/validation/check-phase-138.mjs</automated>
  </verify>
  <done>ROADMAP SC#3 carries both markers with its original figure intact; the live reader, the governance gate and the apex all still exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the amendment set across the remaining seven document surfaces</name>
  <reversibility rating="costly">Undoing this task is a one-line doc revert, but its downstream consequence is one-way for Phase 144 — that apex array is generated from these constants and a short chain silently validates nothing it omits; the choice is owner-ratified in CONTEXT.md D-25, so the door is already walked through and no checkpoint re-asks it.</reversibility>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` (D-02, D-03, D-11, D-15, D-20, D-21, D-22, D-25, D-27, D-28, D-29, D-30, D-33, D-34, D-35)
    - `.planning/ROADMAP.md` lines 140-184 (Phase 141's already-landed marker at :163 is the shape precedent, plus the Phase 142 block)
    - `.planning/REQUIREMENTS.md` lines 30-48 and 65-84
    - `.planning/STATE.md` lines 122-143 and line 305
    - `.planning/PROJECT.md` lines 24-25
  </read_first>
  <action>
    Apply the same annotate-never-overwrite treatment to every remaining surface. Re-grep each
    target before editing — line numbers in this repo drift between sessions, so key off text, not
    offsets. Use `grep -n "0\.35s\|~17s\|~\*\*17s\*\*\|17s apex"` across the four documents to
    enumerate the figure surfaces, and `grep -n "48\.\.138"` to enumerate the span surfaces.

    **ROADMAP Phase 142 SC#1** — `**[SUCCESS-CRITERION AMENDMENT, D-35]**`. Extend to name a THIRD
    defect: V-30-02's regular expression is built from a double-quoted JS string, so its intended
    any-character class collapses to a two-letter character class and the captured block text has
    always been empty. The check has never inspected a Mermaid block since Phase 30 and sits in the
    passing column, invisible to any run-and-count method. Record that the amendment authorizes the
    fix rather than replacing it, and that a milestone whose bar is deleting vacuous passes cannot
    knowingly ship one in its own edit target. Also record, per D-33's honesty obligation, that
    SC#1's named `l1-template.md` literal is UNRESTORABLE — the corpus moved under Phase 40-01
    (`600eabd6`, Android added to the enum) and restoring it is barred by D-01.

    **ROADMAP Phase 142 SC#2** — `**[SUCCESS-CRITERION AMENDMENT, D-20]**`, ADDITIVE, not a
    replacement. Record that `resolveArchivedPhasePath(..., ['v1.3-phases'])` alone is insufficient:
    the resolver works, but the D-23 target line moved from 182 to 259 under
    `### iOS-Specific Timing Considerations`, so a path-only fix merely changes the failure message.
    The amended mechanism is the resolver for the FIXTURE plus a presence-only content anchor for
    the TARGET (D-21 — presence, never uniqueness, because `06-compliance-policy.md` is
    EEE-enrolled and a future Summary retrofit would re-break an exactly-once clause). Record the
    D-22 ruling in the same bullet: the prose is byte-identical and unmoved in substance, only
    relocated by the Phase-122 reorganization, so the current location DOES satisfy D-23's intent —
    the same later-ratified-decision supersession class as V-30-01 and V-30-10. Add a second marker
    `**[SUCCESS-CRITERION AMENDMENT, D-24]**` recording V-31-29's instrument: the metric corrected
    to `wc -l` (which `31-VALIDATION.md:69` mandates and the code implements one line high) plus a
    re-derived documented target band for runbook 14; bound-widening alone and per-runbook
    exemption are both rejected.

    **ROADMAP Phase 142 SC#5** — `**[DISCHARGED, D-15]**` for its `~17s` reference figure, plus
    `**[SUCCESS-CRITERION AMENDMENT, D-27]**` restoring all four named method elements (clone
    depth — full/`fetch-depth: 0` is the ONLY admissible value because a depth-1 clone fatals
    `readAtClose()` per SWEEP-01; cache state; Defender state; runner) and adding tree identity and
    an explicit `--verbose`-or-not declaration as declared variables. Add
    `**[SUCCESS-CRITERION AMENDMENT, D-28]**` fixing the threshold as a RATIO at 8x or greater
    against a declared same-tree same-session warm median at n of 3 or more, with the absolute
    "50% of the 30-minute CI cap" leg DROPPED (it binds a Windows measurement to a Linux job cap on
    a runner class that does not exist — all CI jobs are `ubuntu-latest`). Add
    `**[SUCCESS-CRITERION AMENDMENT, D-29]**` recording that mechanism X is two-tier — in-phase
    advisory per-child marginal-cost publication, plus a structural tier attributed to CARVE-1 and
    SWEEP-01 and explicitly NOT double-booked into Phase 142 — and that the timeout-raise leg is
    withdrawn because a timeout raise moves zero wall-clock cost and can never satisfy a cost
    threshold. State per D-30 that the disposition is Advisory and does not breach the milestone
    bar, since the two dispositions the bar requires deleting are named elsewhere and this item is
    neither.

    **ROADMAP Phase 142 Discuss-phase flags** — `**[SUCCESS-CRITERION AMENDMENT, D-02]**`. Both
    forks the roadmap left open are now resolved. The separate v1.3 pin is REJECTED: it is
    mechanically feasible (all three assertions pass at tag v1.3 and the close-SHA map is an
    unordered literal, so the key insertion carries no collision) but a pin against an immutable
    corpus is permanently true by construction — the exact class this milestone exists to delete.
    Record per D-03 that the "barred three times over" argument is WITHDRAWN because two of its
    three legs are false; state the honest vacuity argument only. The NEST-01 threshold is resolved
    at the D-28 ratio. Follow Phase 141's own already-landed flags amendment at ROADMAP `:163` as
    the shape.

    **REQUIREMENTS.md** — annotate RED-04 with the D-35 extension, RED-05 with D-20/D-21/D-22/D-24,
    RED-06 with D-11's membership mechanism, the RED-06 rationale paragraph with
    `**[DISCHARGED, D-15]**`, NEST-01 and its "Scoped cold-clone-only" paragraph with
    `**[DISCHARGED, D-15]**` plus the D-27/D-28/D-29 method and threshold, and correct the span at
    BOTH NEST-01 and HARN-18 per D-25 — leaving each original `[48..138]` in place and appending
    the correction beside it, with the derivation: one validator per phase over an unbroken
    30, 31, 48 through 138 sequence, the `[48..N-1]` invariant explicit in every prior apex's
    source, v1.20 spanning phases 139 through 144, so Phase 144's apex is `check-phase-144` at
    `[48..143]`, 96 entries. Record why this matters: a wrong end constant yields a five-phase-short
    chain that still passes its own self-guards — a silent close-blocker, and this is the last cheap
    place to catch it.

    **PROJECT.md** — same D-25 correction at the Pillar E bullet. Add `**[DISCHARGED, D-15]**` to
    the Pillar D bullet, which carries the `~17s, 93 PASS / 0 FAIL / 0 SKIPPED` figure; note in the
    plan record that this is an EIGHTH figure surface found at plan time, one more than D-33's
    enumeration of seven, and annotate it on D-33's stated intent. In the milestone-start footer,
    which is a historical record, append a single bracketed pointer noting that the span reads
    `[48..143]` as corrected by D-25 — do not rewrite the footer prose.

    **STATE.md** — the Phase 142 block is a third verbatim copy of the criteria. Annotate the
    `~+0.35s cost on a ~17s apex` line and the `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` bullet with
    `[DISCHARGED, D-15]`, and add the D-11 mechanism note to the adoption line. Respect the ASCII
    flow diagram: new lines carry the same leading pipe-and-space prefix as their neighbours.
    Per D-34, VERIFY (do not assume) that the Phase-142 coverage row still names all five
    requirement IDs; Phase 141's silent-drop defect does not recur here and that verification is
    itself the deliverable.
  </action>
  <acceptance_criteria>
    - `grep -c "SUCCESS-CRITERION AMENDMENT" .planning/ROADMAP.md` increases by at least 6 relative to its pre-task value, and `grep -c "DISCHARGED, D-15" .planning/ROADMAP.md` returns `2`
    - `grep -c "48\.\.143" .planning/REQUIREMENTS.md` returns `2`; `grep -c "48\.\.138" .planning/REQUIREMENTS.md` still returns `2` (both originals survive)
    - `grep -c "48\.\.143" .planning/PROJECT.md` returns at least `2`; `grep -n "48\.\.138" .planning/PROJECT.md` still lists its original occurrences
    - `grep -c "DISCHARGED, D-15" .planning/STATE.md` returns `2`
    - `grep -n "^| 142 |" .planning/STATE.md` prints a row containing all of `RED-04`, `RED-05`, `RED-06`, `RED-07`, `NEST-01` and the count `5`
    - `grep -n "0\.35s\|~17s\|~\*\*17s\*\*" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/PROJECT.md` shows every pre-existing occurrence still present with its numeric value unchanged
    - `node scripts/validation/check-phase-54.mjs` exits 0 with `0 failed` in its `Summary:` line
    - `git status --porcelain scripts/ .github/ docs/` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs && node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>All eight figure surfaces and all four span surfaces carry markers or corrections; no original sentence deleted; `check-phase-54.mjs` unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the archived band spec, route the twelve deferrals, add the ledger row, land Commit 1</name>
  <files>.planning/milestones/v1.3-phases/31-ios-l2-investigation/31-VALIDATION.md, .planning/milestones/v1.20-DEFERRED-CLEANUP.md, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` (the `<deferred>` block and its routing rule, plus D-19, D-24, D-31)
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-RESEARCH.md` "The V-31-29 tension" (the CORRECTED BY ORCHESTRATOR block and the four-runbook delta table)
    - `.planning/milestones/v1.3-phases/31-ios-l2-investigation/31-VALIDATION.md` lines 55-84
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` lines 8-26 (Discipline, Row Schema, and the Phase-139 planning-document row that uses `check-phase-54.mjs` as its gate)
    - `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` (the naming and section shape this repo's deferral files use)
  </read_first>
  <action>
    Three artifacts, then one commit.

    **(a) Annotate `31-VALIDATION.md`** at the line mandating the line-count metric. APPEND a
    `**[SUCCESS-CRITERION AMENDMENT, D-24]**` annotation directly beneath it; do not edit the
    original sentence. The annotation records: the metric correction (the spec mandates the
    line-count-utility semantics, the code implements a newline-split which is one higher, measured
    at 215/211/195/200 versus 216/212/196/201); runbook 14's documented target band re-derived from
    the measured v1.3-to-HEAD retrofit delta of +31 lines (commits `114973ba` then `956818a0`,
    Phase 116-07's EEE retrofit), shifting the documented target from roughly 160-180 to roughly
    191-211; the coded bound recomputed by the same plus-or-minus-15-percent-per-endpoint rule the
    other three already satisfy, giving 162 to 242, which contains 215 with 27 lines of headroom;
    and the fact that runbooks 15, 16 and 17 grew +21, +19 and +13 so their documented targets are
    EQUALLY STALE but still passing and are deliberately left unchanged. State explicitly that the
    assertion stays live and still fails if runbook 14 collapses or explodes, and that both
    widening the coded ceiling alone and exempting the runbook were rejected. Note that this file
    is referenced by a source-of-truth comment at the top of `check-phase-31.mjs` but is not read
    by any validator at runtime.

    **(b) Create `.planning/milestones/v1.20-DEFERRED-CLEANUP.md`** — the ONE named destination for
    every deferred item, chosen milestone-level for the same reason the GOV-02 ledger is (Phase 139
    D-11: milestone placement avoids any archived-phase-path resolution call site and means later
    phases never mutate a completed phase's artifact). Verified safe: every validator reference to a
    deferral file names a specific predecessor file by path; none globs or counts the directory.
    Give it a header matching the predecessor files' shape and a table with columns
    `Item | Evidence | Destination | Disposition`. Author exactly twelve rows, one per
    CONTEXT.md `<deferred>` bullet, each with a real destination:
      1. `check-phase-31 --quick` silently loses a check (its active-check filter drops the external
         type with no skip accounting, so the quick run reports 29 of 30) — and the same file's
         provenance comment claiming its summary block was inherited verbatim from Phase 30 is
         provably false. Destination: a future tooling requirement; no v1.20 requirement covers it.
      2. Seven remaining `check-phase-31` checks print a positive detail string on FAIL. The eighth,
         V-31-25's, is fixed opportunistically in Plan 04 because that check is already being
         rewritten. Destination: same future tooling requirement.
      3. `V-31-22` has no zero-file guard, so it passes if all six L1 files vanish. Destination:
         same future tooling requirement.
      4. `check-phase-30.mjs`'s unescaped-dot filename regex, where the sibling file has the correct
         escaped form. Destination: same future tooling requirement; explicitly NOT fixed in Plan 03
         (it is the second regular-expression construction in that file and is out of RED-04's
         amended scope).
      5. `carve-gate.mjs` calls its entry point at module scope with no import guard and that entry
         point exits the process, so importing its exported helpers kills the importing process.
         Destination: a future tooling requirement; this phase avoids it by invoking the gate only
         through its CLI.
      6. The dead `required:` metadata, never read by either runner — zero occurrences in
         `check-phase-30`, two in `check-phase-31`. Destination: same future tooling requirement.
      7. The 13-workflow pull-request path-filter cascade that every edit here fans out on Phase
         144's close PR. Destination: **Phase 144 CONTEXT, as a named verification input**, with
         Phase 141's 41-of-41 fan-out as the precedent for how to read it; it lands on HARN-19's bar.
      8. The consequence of D-25's span correction — Phase 144 must author `check-phase-144` with
         the end constant at 143 and 96 entries. Destination: **Phase 144 CONTEXT, as a named input**.
      9. `check-phase-66` at 386 235 ms is over the 300 000 ms per-subprocess cap as a non-peer child
         of `check-phase-67` (D-31). Record it; do NOT re-measure `check-phase-64`, whose answer is
         already recorded at 91 710 ms, 30.6 percent of cap, and is not a crossing candidate.
         Destination: unowned — needs its own requirement.
     10. The non-nested standalone-chain cost is still unowned after this phase; NEST-01 covers only
         the apex cold-clone curve and the predecessor cleanup file forbids re-collapsing the two.
         Destination: unowned — needs its own requirement.
     11. `audit-harness-v1.7-integrity.yml`'s stale Windows wall-clock reference, carried from Phase
         141 and still unfixed. Destination: **Phase 144's workflow work**, hygiene.
     12. Seven stale agent-worktree copies of `check-phase-31.mjs` under `.claude/worktrees/`
         carrying the pre-fix path — verified present, 7 files. Not a repo defect (untracked), but a
         working-tree hazard that makes any repo-wide post-edit grep report the defect still present.
         Destination: recorded here as a verification hazard; Plans 03/04 scope their greps to
         `scripts/` and exclude `.claude/`.

    **(c) Append one GOV-02 ledger row** for this plan, following the Phase-139 planning-document
    row's shape exactly: a target-scoped path-string grep across `scripts/`, `.github/` and
    `.planning/` for the four edited planning documents plus the archived validation spec, the hit
    count with its interpretation, `node scripts/validation/check-phase-54.mjs` named as the
    regression gate with its tally, a narrative PASS/FAIL Result sentence naming the command re-run,
    and Plan `01`. Append at the end; never edit or reorder an existing row. Note in the row that
    D-18's three MANDATORY rows are for the frozen validator paths in Plans 03/04/05 and that this
    row is precedent-following rather than D-18-required.

    **(d) Commit 1.** Stage ONLY paths under `.planning/`. Verify before committing that
    `git status --porcelain scripts/ .github/ docs/` is empty and that
    `.planning/milestones/v1.20-CARVE.md` is NOT among the staged paths — the CARVE amendment is
    Commit 2 and must stand alone. Commit message subject names the phase and the amendment set.
  </action>
  <acceptance_criteria>
    - `grep -c "SUCCESS-CRITERION AMENDMENT, D-24" .planning/milestones/v1.3-phases/31-ios-l2-investigation/31-VALIDATION.md` returns `1`, and the file's original metric sentence is still present verbatim
    - `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` exists, and `grep -c '^| ' .planning/milestones/v1.20-DEFERRED-CLEANUP.md` returns `14` (12 item rows + header + separator)
    - `grep -c "Phase 144" .planning/milestones/v1.20-DEFERRED-CLEANUP.md` returns at least `3`
    - `grep -c "check-phase-64" .planning/milestones/v1.20-DEFERRED-CLEANUP.md` returns at least `1` and the row states the recorded figure rather than requesting a re-measurement
    - `tail -1 .planning/milestones/v1.20-GOV-02-LEDGER.md` is a table row whose last cell is `01` and which names `check-phase-54.mjs`
    - `git show --stat HEAD --name-only | grep -v '^$' | grep -c -v '^\.planning/'` returns `0` after the commit, and `git show --name-only HEAD | grep -c 'v1.20-CARVE.md'` returns `0`
    - `node scripts/validation/check-phase-54.mjs` exits 0; `node scripts/validation/carve-gate.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs && node scripts/validation/carve-gate.mjs && git show --name-only --format= HEAD</automated>
  </verify>
  <done>Commit 1 exists, touches only `.planning/` paths, excludes the CARVE file, and all twelve deferrals have a destination row.</done>
</task>

</tasks>

## Artifacts this phase produces

Phase-wide roll-up (this plan owns the rows marked **[01]**):

| Symbol / artifact | Kind | Owning plan |
|---|---|---|
| `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` | new file, 12 routed rows | **[01]** |
| `31-VALIDATION.md` `[SUCCESS-CRITERION AMENDMENT, D-24]` annotation | doc annotation | **[01]** |
| GOV-02 ledger row, Plan `01` | ledger row | **[01]** |
| `[DISCHARGED, D-15]` markers on 8 figure surfaces | doc markers | **[01]** |
| `[SUCCESS-CRITERION AMENDMENT, D-NN]` markers on SC#1/2/3/5 + flags + 5 requirement bullets | doc markers | **[01]** |
| `[48..143]` corrections at REQUIREMENTS NEST-01, REQUIREMENTS HARN-18, PROJECT Pillar E (+ footer pointer) | doc corrections | **[01]** |
| `scripts/validation/check-phase-138.mjs` added to CARVE Category 5 | allowlist entry | 02 |
| CARVE "Recorded scope amendments" bullets, incl. Phase-141 D-27/D-28 back-fill | doc bullets | 02 |
| GOV-02 ledger rows, Plans `03`, `04`, `05` | ledger rows | 03/04/05 |
| Rewritten `V-30-01` (Routing-Verification table assertion) | new check body + new `name` string | 03 |
| Repaired `V-30-02` (regex literal) | check body | 03 |
| Rewritten `V-30-10` (line-anchored author-guidance enum) | new check body + new `name` string | 03 |
| New `isMissing` classifier arm, 3 call sites | classifier arm | 03/04 |
| Rewritten `V-31-23` (own resolver call site + presence anchor) | new check body + new `name` string (drops "line 182") | 04 |
| Rewritten `V-31-25` (line-anchored enum + pass-conditional detail) | new check body + new `name` string | 04 |
| Rewritten `V-31-29` (line-count metric + re-derived runbook-14 bound `[162, 242]`) | check body + new `name` string | 04 |
| `CHAIN_EXTRA = [30, 31]` | new constant in `check-phase-138.mjs` | 05 |
| Apex check ids `CHAIN-30`, `CHAIN-31` | new check ids | 05 |
| `142-EVIDENCE.md` (NEST-01 method, ratio, per-child cost table) | new file | 06 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning document -> `check-phase-54.mjs` | This plan's only machine consumer; a live reader that fails if the documents drift in a shape it pins |
| planning document -> Phase 144 close-gate | Amendment markers and the CARVE list are the sole evidence separating authorized scope from drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-142-01-R | Repudiation | ROADMAP/REQUIREMENTS/PROJECT/STATE amendments | medium | mitigate | Every amendment carries an explicit `[DISCHARGED, D-NN]` or `[SUCCESS-CRITERION AMENDMENT, D-NN]` marker naming its ratifying decision; Phase 144's close-gate reads markers plus the CARVE list, so an unmarked edit is indistinguishable from drift |
| T-142-02-T | Tampering | `check-phase-54.mjs`'s live read of REQUIREMENTS.md and ROADMAP.md | medium | mitigate | Run `check-phase-54.mjs` after every document edit in this plan and require an unchanged tally; a document edit is the one way this plan can red an already-green chain member |
| T-142-03-I | Information disclosure | — | low | accept | This plan writes only repository-local planning prose; no secret, credential, token, or network endpoint is read or emitted |
| T-142-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and runs no package manager; RESEARCH.md's Package Legitimacy Audit records "not applicable" for the phase, and the two pre-existing external call sites are untouched here |
</threat_model>

<verification>
- `node scripts/validation/check-phase-54.mjs` exits 0 with an unchanged tally
- `node scripts/validation/carve-gate.mjs` exits 0
- `node scripts/validation/check-phase-138.mjs` prints `Result: 93 PASS, 0 FAIL, 0 SKIPPED (total checks: 93)` and exits 0 (pre-adoption baseline preserved)
- `git show --name-only --format= HEAD` lists only paths beginning `.planning/`, and does not list `v1.20-CARVE.md`
- `git status --porcelain docs/` prints nothing
</verification>

<success_criteria>
- Commit 1 of D-19's three-commit sequence has landed and is scoped to `.planning/` excluding the CARVE file
- Every success criterion this phase deviates from carries a marker naming its ratifying decision
- The `[48..143]` correction is present at all three ratified sites without deleting any original
- All twelve deferred items have a row in one named destination file
- No file under `docs/`, `scripts/`, or `.github/` has been modified
</success_criteria>

<output>
Create `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-01-SUMMARY.md` when done
</output>
