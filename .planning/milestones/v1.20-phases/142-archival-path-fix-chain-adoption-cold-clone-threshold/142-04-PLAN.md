---
phase: 142-archival-path-fix-chain-adoption-cold-clone-threshold
plan: 04
type: execute
wave: 4
depends_on: ["142-03"]
files_modified:
  - scripts/validation/check-phase-31.mjs
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: true
requirements: [RED-05, RED-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node scripts/validation/check-phase-31.mjs` exits 0 and prints `Summary: 29 passed, 0 failed, 1 skipped` (from the recorded pre-fix baseline of 26 passed, 3 failed, 1 skipped)"
    - "`node scripts/validation/check-phase-68.mjs` prints `Result: 33 PASS, 0 FAIL, 0 SKIPPED` and exits 0 after the edit — RED-07's two guards survive"
    - "`grep -c 'archive-path' scripts/validation/check-phase-31.mjs` returns at least `1` — the import the 5-call-site floor guard requires is still present"
    - "`grep -c '_missing' scripts/validation/check-phase-31.mjs` returns at least `5` — the pre-existing 4 discriminator sites plus V-31-23's new resolver-null branch"
    - "V-31-29 is proven LIVE, not exempted: with runbook 14's bound temporarily mutated to an impossible interval the check FAILS and names runbook 14, and the mutation is reverted leaving a clean tree"
    - "`grep -c 'lines\\[181\\]' scripts/validation/check-phase-31.mjs` returns `0` and `grep -c 'line 182' scripts/validation/check-phase-31.mjs` returns `0` — the brittle index and the stale name string are both gone"
    - "`node scripts/validation/carve-gate.mjs` exits 0 with `check-phase-31.mjs` on-list"
    - "`git status --porcelain docs/` prints nothing — zero corpus edits"
    - statement: "The V-31-23 content anchor asserts PRESENCE, never uniqueness — the target file is enrolled in the documentation contract and a future Summary retrofit that quotes the bullet would re-break an exactly-once clause without that clause adding any coverage"
      verification: backstop
    - statement: "The RED-05 and RED-07 edge-probe rows are `unclassified` in the deterministic probe; they resolve here to the standalone exit-0 criterion and the unchanged guard tally respectively, which are the strongest defensible criteria available"
      verification: backstop
  prohibitions:
    - statement: "No file under `docs/` is edited, and no line of `06-compliance-policy.md`, either template, or any L2 runbook is moved to satisfy an assertion (D-01)"
      status: flagged-unverified
    - statement: "V-31-29 is NOT made to pass by exempting runbook 14 from its bound. A per-runbook exemption map, or any early loop exit keyed on a runbook number, permanently exempts that file from every bound — the check would pass at 10 lines or at 900 — which is a vacuous pass of exactly the class this phase exists to delete (D-24, RESEARCH.md Pitfall 5b)"
      status: flagged-unverified
    - statement: "V-31-29 is NOT made to pass by widening only the coded ceiling while leaving the documented target band untouched — that amends the content-size contract rather than the tolerance, with nothing counter-ratcheting (D-24, RESEARCH.md Pitfall 5a)"
      status: flagged-unverified
    - statement: "No nested-execution short-circuit is added to this file. It carries zero such tokens today, the apex already sets that variable unconditionally on every child it spawns, and with no CI job of its own the required external check would then execute nowhere (D-14, withdrawn)"
      status: flagged-unverified
    - statement: "V-31-23 does NOT reuse the archived-path call site inside the inventory parser — that call resolves a different fixture and returns a different discriminator shape; the successor needs its own call site (D-23)"
      status: flagged-unverified
    - statement: "The discriminator's justification comment does not name the sibling guard it happens to satisfy, and does not borrow the predecessor sweep's vocabulary — that guard is not at risk under any rewrite of this check, so citing it would be a non-reason (D-23)"
      status: flagged-unverified
    - statement: "Seven of the eight pass-worded FAIL detail strings are NOT fixed here — only the one belonging to a check already being rewritten. The rest are a routed deferral"
      status: flagged-unverified
  artifacts:
    - "`scripts/validation/check-phase-31.mjs` — rewritten V-31-23, V-31-25 and V-31-29 bodies and `name` strings, one new classifier arm, one opportunistic detail-string fix"
    - "`.planning/milestones/v1.20-GOV-02-LEDGER.md` — one appended row (Plan 04)"
  key_links:
    - "`resolveArchivedPhasePath(..., ['v1.3-phases'])` -> V-31-23's own new call site (the fixture lives under the archived v1.3 phase root, not the live phase root)"
    - "The `_missing` marker anywhere in this file -> the sibling guard's bare whole-file substring check, which is why the marker must survive the rewrite"
    - "The `archive-path` import -> the sibling guard's 5-call-site floor, which is a floor and cannot be broken by adding a use"
    - "V-31-29's runbook-14 bound -> the re-derived documented band annotated onto the archived validation spec in Plan 01 (the numbers must agree)"
    - "V-31-25's line-anchored regex -> the byte-identical regex Plan 03 applied to the L1 template"
---

<objective>
Make `check-phase-31.mjs` exit 0 standalone while leaving the two guards that watch it untouched.

Purpose: three assertions here fail for three different reasons, and only one of them is the
archival-path bug the roadmap named. V-31-23 reads its fixture from a phase directory that was
archived, AND hardcodes a zero-based index for prose that relocated during the Phase-122
reorganization — the resolver fix alone only changes the failure message. V-31-25 tests a frontmatter
line for a pipe-enum that never lived there. V-31-29 measures with a metric one higher than the one
its own archived spec mandates, against a documented target band that every one of its four runbooks
outgrew under the EEE retrofit.

Output: a validator that exits 0, and a regression guard that still reports the same tally it did
before the edit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md
@.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-RESEARCH.md
@.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-PATTERNS.md
@.planning/milestones/v1.20-CARVE.md
@scripts/validation/check-phase-31.mjs
@scripts/validation/_lib/archive-path.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: GOV-02 grep, guard baseline, ledger row — before any edit</name>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `.planning/milestones/v1.20-CARVE.md` GOV-02 grep procedure and the ratified definition of a pin
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` Discipline section and the most recent rows
    - `scripts/validation/check-phase-68.mjs` — the two guard checks that read this file: the 5-call-site floor over the archived-path helper, and the bare whole-file discriminator-marker check
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` D-17, D-18
  </read_first>
  <action>
    Run both grep forms and capture the guard baseline before touching the file.

    Target-scoped: grep recursively for the literal path string `check-phase-31.mjs` across
    `scripts/`, `.github/` and `.planning/milestones/`. EXCLUDE `.claude/` — seven stale
    agent-worktree copies of this exact file live under `.claude/worktrees/` carrying the pre-fix
    path, and they will make any repo-wide post-edit grep report the defect still present (routed
    deferral 12; verified present at plan time, 7 files, untracked, not a repo defect).

    Symbol-scoped: grep across `scripts/validation/` for the four check ids in scope, for the
    archived-path helper's symbol, and for the discriminator marker string.

    The target-scoped grep will surface the sibling guard validator pinning this file's exact path
    string at two call sites — that is the "later frozen validator pins an earlier one's call site"
    hazard GOV-02 exists to catch, and it is already inside RED-07's scope. Classify each hit as
    comment or substring assertion. Record that the floor guard is a FLOOR over five named files
    (adding another use of the helper cannot remove the import), and that the marker guard is a bare
    whole-file substring test already satisfied at four sites this rewrite does not touch — both
    structurally safe, and the row is still mandatory rather than optional.

    Capture pre-edit baselines: `node scripts/validation/check-phase-31.mjs` (expected
    `Summary: 26 passed, 3 failed, 1 skipped`, exit 1) and `node scripts/validation/
    check-phase-68.mjs` (expected `Result: 33 PASS, 0 FAIL, 0 SKIPPED`, exit 0).

    Append ONE row to the ledger following the most recent rows' shape, with Plan `04` in the last
    cell. Append at the end; never edit or reorder an existing row.
  </action>
  <acceptance_criteria>
    - `tail -1 .planning/milestones/v1.20-GOV-02-LEDGER.md` is a table row whose first cell names `check-phase-31.mjs` and whose last cell is `04`
    - That row's Grep command cell contains both a path-string grep and a symbol grep, and explicitly excludes `.claude/`
    - The row's Hit count cell names the sibling guard validator's two pinning call sites and classifies them
    - The row's Regression gate cell records `Result: 33 PASS, 0 FAIL, 0 SKIPPED` as the pre-edit guard baseline
    - `git diff --quiet scripts/` succeeds — the grep precedes the edit
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-68.mjs && tail -1 .planning/milestones/v1.20-GOV-02-LEDGER.md | grep -q 'check-phase-31\.mjs' && git diff --quiet scripts/</automated>
  </verify>
  <done>One ledger row exists for `check-phase-31.mjs` with both grep forms and the guard baseline; no source file modified yet.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite V-31-23 (own resolver call site + presence anchor) and V-31-25 (line-anchored enum)</name>
  <files>scripts/validation/check-phase-31.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-31.mjs` — the V-31-23 body, the V-31-25 body, the inventory parser's existing archived-path call and its discriminator return shape, and the top-of-file import
    - `scripts/validation/_lib/archive-path.mjs` — the resolver's signature and its caller-owns-fail-semantics contract
    - `.planning/milestones/v1.3-phases/31-ios-l2-investigation/expected-d23.txt` — the fixture, in full (READ ONLY)
    - `docs/admin-setup-ios/06-compliance-policy.md` — the heading at the prose's current location and the prose itself (READ ONLY)
    - `docs/_templates/l2-template.md` — the author-guidance comment lines carrying the platform enum (READ ONLY)
    - `scripts/validation/check-phase-30.mjs` — the line-anchored author-guidance regex Plan 03 just landed, to copy byte-for-byte
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` D-07, D-20, D-21, D-22, D-23
  </read_first>
  <action>
    **V-31-23.** Replace the body and the `name` string. Two defects, both fixed:

    Give the check its OWN archived-path resolver call for the fixture, resolving the v1.3 phase
    root. Do not reuse the call inside the inventory parser — it resolves a different fixture, sits
    inside a different function, and returns a different shape. On a null resolution, return a
    failing result carrying the discriminator marker and a detail naming both candidate roots that
    were searched; on a resolved-but-unreadable fixture, return a failing result with the
    discriminator and a distinct detail. Keeping the discriminator is required; do NOT justify it in
    the comment by naming the guard it happens to satisfy, and do not borrow the predecessor sweep's
    vocabulary — that guard is not at risk under any rewrite of this check, so citing it would be a
    non-reason (D-23). The only reason to record is the one that is true: the detail string must
    distinguish a resolver miss from a read failure.

    Replace the hardcoded zero-based line index with a presence-only content anchor: the target file
    contains the trimmed fixture text as a substring. PRESENCE, never uniqueness — the target file is
    enrolled in the documentation contract and one of its own commits is a hand-authored Summary
    addition, so an exactly-once clause would re-break the moment a future retrofit summarised or
    quoted the bullet, adding a failure mode without adding coverage (D-21).

    The check's `name` currently embeds a line number that no longer holds. Replace it. Record in
    the plan summary that this changes the apex's printed output line for this check (D-22).

    Answer D-22's open question IN the comment above the check, as a ruling: the prose is
    byte-identical to the fixture and unmoved in substance — only relocated from under the
    configure-actions step to under the iOS-specific-timing heading during Phase 122's EEE
    reorganization — so the current location DOES satisfy the original decision's intent. This is
    the same later-ratified-decision supersession class as the two assertions repaired in Plan 03.

    **V-31-25.** Replace the body and the `name` string. The current check tests a frontmatter
    platform line for a pipe-enum that never lived there — frontmatter carries a single value. The
    pipe-enum lives in the template's author-guidance comment. Use the IDENTICAL line-anchored
    regular expression Plan 03 landed for the L1 template, applied to the L2 template, so the two
    validators stop diverging: a bare whole-file substring test and a line-anchored test have
    different bypass surfaces, which is how they drifted apart (D-07). Record the supersession cause
    for THIS check — the later decision that replaced the pipe-list placeholder with a single
    frontmatter value — and note that this is a DIFFERENT cause from the L1 sibling's, whose break
    commit is one whole milestone earlier (D-06/D-07).

    While rewriting this check, fix its hardcoded pass-worded detail string opportunistically — it
    currently prints a positive detail on FAIL. Emit a pass-conditional detail instead. This is ONE
    of the eight such strings in this file; the other seven are a routed deferral and are NOT
    touched here.
  </action>
  <acceptance_criteria>
    - `node --check scripts/validation/check-phase-31.mjs` exits 0
    - `grep -c 'lines\[181\]' scripts/validation/check-phase-31.mjs` returns `0`
    - `grep -c 'line 182' scripts/validation/check-phase-31.mjs` returns `0`
    - `grep -c 'resolveArchivedPhasePath' scripts/validation/check-phase-31.mjs` returns `3` (the import, the inventory parser's call, and V-31-23's new call)
    - `grep -c "v1.3-phases" scripts/validation/check-phase-31.mjs` returns at least `2`
    - `grep -c '_missing' scripts/validation/check-phase-31.mjs` returns at least `5`
    - `grep -c 'specific platform' scripts/validation/check-phase-31.mjs` returns `1`, and that regex text is byte-identical to the one in `scripts/validation/check-phase-30.mjs` — verify by diffing the two matched lines
    - `grep -c 'enum present' scripts/validation/check-phase-31.mjs` returns `0` — the hardcoded pass-worded detail is gone
    - `node scripts/validation/check-phase-31.mjs` prints `Summary: 28 passed, 1 failed, 1 skipped` — V-31-29 is still the sole remaining failure at this point
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/validation/check-phase-31.mjs && node scripts/validation/check-phase-31.mjs --verbose; node scripts/validation/check-phase-68.mjs</automated>
  </verify>
  <done>V-31-23 and V-31-25 pass; the failure count is down to one; the guard is still 33/0/0.</done>
</task>

<task type="auto">
  <name>Task 3: V-31-29 metric and band, the classifier arm, commit, then prove the check is live</name>
  <files>scripts/validation/check-phase-31.mjs, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `scripts/validation/check-phase-31.mjs` — the V-31-29 body and its bounds object, and the external-tool check's error classifier
    - `.planning/milestones/v1.3-phases/31-ios-l2-investigation/31-VALIDATION.md` — the metric mandate and the four documented target bands, plus the annotation Plan 01 appended
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-RESEARCH.md` "The V-31-29 tension" — the CORRECTED BY ORCHESTRATOR block and the measured four-runbook delta table
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` D-13, D-14, D-24
    - `scripts/validation/check-phase-30.mjs` — the classifier arm Plan 03 landed, to copy verbatim
  </read_first>
  <action>
    **The metric.** Switch V-31-29's line measurement from the newline-split count to the
    line-count-utility semantics the archived spec mandates — the split count is exactly one higher.
    Update the check's `name` to say which metric it now uses.

    **The band.** Change runbook 14's bound only, to the interval re-derived in Plan 01's annotation:
    the documented target shifted by the measured retrofit delta of +31 lines, then the same
    plus-or-minus-15-percent-per-endpoint rule the other three bounds already satisfy. The resulting
    interval contains the file's real measured count with real headroom. Leave the other three bounds
    byte-unchanged — their documented targets are equally stale (they grew +21, +19 and +13) but they
    still hold, so they are annotated-not-changed. Put the derivation in a comment above the check:
    the measured delta, the two commits that produced it, the retrofit that caused it, and the
    arithmetic. Note that the metric fix and the band re-derivation are BOTH required and neither
    alone suffices — the file's count is above the old ceiling under either metric. Note that
    RESEARCH.md's ORIGINAL recommendation here was reversed by the orchestrator; read the corrected
    block, not the superseded one, and per D-37 verify the re-derivation's arithmetic yourself
    rather than transcribing it — a confident recommendation is not thereby correct.

    The assertion stays live. Do not add a per-runbook exemption structure and do not add any early
    loop exit keyed on a runbook number; the existing early exit for a missing path stays exactly as
    it is.

    **The classifier arm.** Add the same new stderr-text arm Plan 03 added, verbatim, to this file's
    external-tool classifier. This file's classifier already diverges from its sibling's by one
    clause; reconciling that divergence is optional and zero-risk while the line is already being
    touched. Do not remove the auto-confirm or no-install flags from the spawn — dropping the
    no-install flag would turn a skipped check into a live registry resolution on every CI run. Do
    not add a nested-execution short-circuit (D-14, withdrawn).

    **Commit** the source edit and the ledger row together.

    **Then prove the check is live** — the anti-vacuity gate. After the commit, temporarily mutate
    runbook 14's bound in the working tree to an interval that cannot contain its real count, run the
    validator, and confirm it exits non-zero with V-31-29 FAILing and naming runbook 14 with its
    measured count. Then restore the committed state with a checkout of that one path and confirm the
    tree is clean. This proves the bound is evaluated rather than skipped — the exact property a
    per-runbook exemption would silently destroy. Do the probe in the working tree, never by copying
    the validator elsewhere: a stray copy under `scripts/` is an untracked in-scope path and would
    make the governance gate report an off-list hit.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-31.mjs` exits 0 and prints `Summary: 29 passed, 0 failed, 1 skipped`
    - `node scripts/validation/check-phase-31.mjs --verbose` shows V-31-29's detail reporting all runbooks within bounds
    - `grep -c "'15': \[187, 322\]" scripts/validation/check-phase-31.mjs` returns `1` and the same for the 16 and 17 bounds — only runbook 14's interval changed
    - `grep -c 'no YES option' scripts/validation/check-phase-31.mjs` returns `1`
    - `grep -c 'no-install' scripts/validation/check-phase-31.mjs` returns `1`
    - `grep -c 'CHECK_PHASE_NESTED' scripts/validation/check-phase-31.mjs` returns `0`
    - Liveness probe: with runbook 14's bound temporarily mutated, `node scripts/validation/check-phase-31.mjs` exits non-zero and its output contains `V-31-29` on a FAIL line naming `14:`
    - After the probe, `git status --porcelain scripts/` prints nothing
    - `node scripts/validation/check-phase-68.mjs` prints `Result: 33 PASS, 0 FAIL, 0 SKIPPED` and exits 0
    - `node scripts/validation/carve-gate.mjs` exits 0 with zero off-list paths
    - `git status --porcelain docs/` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-31.mjs && node scripts/validation/check-phase-68.mjs && node scripts/validation/carve-gate.mjs && git status --porcelain scripts/ docs/ | wc -l</automated>
  </verify>
  <done>`check-phase-31.mjs` exits 0 at 29/0/1, the liveness probe failed-then-reverted cleanly, the guard is 33/0/0, and no corpus file changed.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol / artifact | Kind |
|---|---|
| V-31-23 successor body + new `name` string (drops the stale line reference) | rewritten check |
| V-31-23's own archived-path resolver call site + resolver-null discriminator branch | new call site |
| V-31-25 successor body + new `name` string + pass-conditional detail | rewritten check |
| V-31-29 line-count metric + runbook-14 bound `[162, 242]` + derivation comment | rewritten check |
| New external-tool classifier arm, 1 call site in this file | classifier arm |
| GOV-02 ledger row, Plan `04` | ledger row |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> npm registry | The pre-existing external-tool spawn crosses this boundary; the no-install flag is what keeps it from resolving a package over the network |
| repository working tree -> frozen-surface allowlist gate | This file is a frozen surface AND is pinned by a sibling guard validator at two call sites |
| archived phase directory -> validator | The fixture is read from a path that moved at milestone close; the resolver owns fail semantics and the caller must discriminate a miss from a read error |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-142-11-T | Tampering | The external-tool spawn in this file | high | mitigate | The classifier repair adds a stderr string arm ONLY; the argv array and both flags are unchanged, asserted by greps for the surviving no-install flag. Removing the no-install flag would convert a skipped check into a live registry resolution on every apex run in CI |
| T-142-12-T | Tampering | Frozen-surface edit to a file pinned by a sibling guard | high | mitigate | Both guards re-run before and after the edit and must report an identical tally; the floor guard is structurally unbreakable by an additive use and the marker guard is satisfied at four untouched sites, but the GOV-02 grep and ledger row are mandatory procedure regardless (D-17) |
| T-142-13-E | Elevation of privilege | V-31-29's bound | high | mitigate | A per-runbook exemption would silently disable the assertion for the one file it is meant to bound. The liveness probe — mutate the bound, observe a real FAIL naming the runbook, revert, prove a clean tree — is the gate that makes exemption detectable |
| T-142-14-I | Information disclosure | Resolver-null detail strings | low | accept | The detail names repository-relative candidate roots only; no absolute path, credential, or environment value is emitted |
| T-142-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; the only external-tool change is a string literal in an error-classification predicate |
</threat_model>

<verification>
- `node scripts/validation/check-phase-31.mjs` exits 0, `Summary: 29 passed, 0 failed, 1 skipped`
- `node scripts/validation/check-phase-68.mjs` exits 0, `Result: 33 PASS, 0 FAIL, 0 SKIPPED`
- `node scripts/validation/check-phase-30.mjs` exits 0, `Summary: 12 passed, 0 failed, 1 skipped` (Plan 03 stays green)
- `node scripts/validation/carve-gate.mjs` exits 0, zero off-list
- `node scripts/validation/check-phase-138.mjs` still prints `Result: 93 PASS, 0 FAIL, 0 SKIPPED (total checks: 93)` — the apex is unchanged until Plan 05
- `git status --porcelain scripts/ docs/` prints nothing
</verification>

<success_criteria>
- RED-05 is satisfied: `check-phase-31` exits 0 standalone, including the archived-path resolution
- RED-07 is satisfied: both guards report an identical tally before and after the edit
- V-31-29 is demonstrably live, not exempted
- Zero corpus edits
</success_criteria>

<output>
Create `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-04-SUMMARY.md` when done
</output>
