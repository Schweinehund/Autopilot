---
phase: 141-standalone-red-validator-set-chain-members-green
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
  - .planning/milestones/v1.20-CARVE.md
autonomous: true
requirements: [RED-01, RED-02, RED-03, SWEEP-09]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ROADMAP.md Phase 141 SC#2 no longer prescribes the withdrawn classifier-context-window mechanism; it carries an in-line [SUCCESS-CRITERION AMENDMENT, D-28] marker naming the stale-BASELINE_9 rebase, with the superseded wording preserved verbatim in the same block (D-27 annotate-and-supersede)."
    - "ROADMAP.md Phase 141 SC#4 carries the corrected SWEEP-09 census on the requirement's own reader-site unit; the ~19-validators wording is superseded, not deleted."
    - "ROADMAP.md Phase 141 SC#1 is annotated as DISCHARGED by Phase 140, never as falsified (D-29)."
    - "ROADMAP.md Phase 141 'Discuss-phase flags: None dominant' is corrected to name the two live owner-ratified forks (RED-02 root cause, SWEEP-09 scope)."
    - "REQUIREMENTS.md SWEEP-09 carries the corrected reader-site unit AND retains its 'of which Phase 139's SWEEP-03 fixes 4' clause and its check-phase-61.mjs note."
    - "REQUIREMENTS.md 'Ordering, corrected' paragraph is annotated only — the original [MEASURED] sentence is byte-unchanged, because REQUIREMENTS.md:8 stamps every [MEASURED] figure to HEAD 347c20a8 and overwriting destroys provenance."
    - "STATE.md's Phase-141 requirement row reads all four IDs (RED-01, RED-02, RED-03, SWEEP-09) with count 4, and every 27/27 / all 27 / 27 total is corrected to 28."
    - "STATE.md's dependency diagram for Phase 141 no longer prescribes the withdrawn classifier-context-window mechanism and lists SWEEP-09."
    - "scripts/validation/check-phase-67.mjs appears in .planning/milestones/v1.20-CARVE.md's carve-allowlist fenced block, landed by a commit whose git show --stat reports exactly 1 file changed (D-09 rule 1)."
    - "The CARVE amendment commit lands BEFORE any commit that edits scripts/validation/check-phase-67.mjs (D-09 rule 3)."
    - "node scripts/validation/check-phase-54.mjs — the sole LIVE reader of ROADMAP.md/REQUIREMENTS.md/STATE.md — still exits 0 after every amendment."
    - "No code path (scripts/, .github/, docs/) is modified by either commit in this plan."
  artifacts:
    - .planning/ROADMAP.md (Phase 141 SC#1/SC#2/SC#4 + Discuss-phase flags amended)
    - .planning/REQUIREMENTS.md (SWEEP-09 amended; 'Ordering, corrected' annotated)
    - .planning/STATE.md (six stale locations corrected)
    - .planning/milestones/v1.20-CARVE.md (Category 5 gains scripts/validation/check-phase-67.mjs)
    - .planning/milestones/v1.20-GOV-02-LEDGER.md (one appended row for the governance-doc edit)
  key_links:
    - "REQUIREMENTS.md / ROADMAP.md / STATE.md live content -> check-phase-54.mjs (the only live reader; every other reference reads a FROZEN milestone-close SHA)"
    - "v1.20-CARVE.md carve-allowlist block -> carve-gate.mjs on-list/off-list decision for scripts/validation/check-phase-67.mjs, consumed by Plan 04's :261 timeout raise"
    - "The [SUCCESS-CRITERION AMENDMENT, D-NN] bracket marker -> Phase 144's close-gate, which must read each amendment as authorized scope rather than drift"
  prohibitions:
    - "Never overwrite an existing [MEASURED] figure, dated observation, or ratified success-criterion sentence — annotate and supersede, leaving the original text readable (D-27/D-23 discipline)."
    - "Never use the D-09 CARVE amendment instrument for a .planning/ document amendment — .planning/ is outside carve-gate.mjs's IN_SCOPE_PREFIXES, so the cited combined-commit hazard cannot fire and D-09 rule 1 would bar the shape anyway (D-27)."
    - "The CARVE amendment commit must touch no path other than .planning/milestones/v1.20-CARVE.md — not the ledger, not a plan file, not a source file."
    - "Do not mark ROADMAP.md Phase 141 SC#1 as false or struck-as-wrong; it is satisfied, and deleting the record erases the evidence that the ratified ordering was correct (D-29)."
    - "Do not flip any requirement to Validated in this phase — Validated is the Phase 144 close-gate state (D-30)."
---

<objective>
Land the phase's governance gate: the six owner-ratified document amendments (D-27/D-28/D-29)
as one commit that touches only `.planning/` governance documents, then the D-09 CARVE
amendment adding `scripts/validation/check-phase-67.mjs` to the allowlist as a second commit
touching only `.planning/milestones/v1.20-CARVE.md`.

Purpose: `ROADMAP.md` Phase 141 SC#2 currently hard-codes a mechanism this phase does not use
(a corrected classifier context window). D-01/D-03 withdrew that mechanism, so the written
criterion **forecloses the correct fix**. Amending it is the phase's first gate — no code edit
may land before it. The CARVE amendment is the one path that hard-blocks `carve-gate.mjs`:
`check-phase-67.mjs` is absent from the allowlist entirely, and Plan 04 raises its `:261`
timeout.

Output: amended ROADMAP/REQUIREMENTS/STATE, an extended CARVE allowlist, one GOV-02 ledger row.

## Artifacts this phase produces

Contributed by this plan:
- New in-line markers `**[SUCCESS-CRITERION AMENDMENT, D-28]**` in `.planning/ROADMAP.md`
  (Phase 141 SC#2 and SC#4) and `.planning/REQUIREMENTS.md` (SWEEP-09).
- New in-line marker `**[DISCHARGED, D-29]**` in `.planning/ROADMAP.md` Phase 141 SC#1.
- New dated annotation line under `.planning/REQUIREMENTS.md`'s "Ordering, corrected" paragraph.
- New allowlist glob line `scripts/validation/check-phase-67.mjs` plus its `#` rationale comment
  in `.planning/milestones/v1.20-CARVE.md`'s `carve-allowlist` fenced block.
- One new row in `.planning/milestones/v1.20-GOV-02-LEDGER.md`.

Produced later in the phase (roll-up, for the phase verifier):
- `BASELINE_9` rebased array + a new `// BASELINE_9 refreshed 2026-08-07 (Phase 141 Plan 02)`
  audit-trail comment line (Plan 02).
- `readAtV15CloseFor61`'s delegating body in `check-phase-61.mjs` (Plan 03).
- Twelve converted call-site returns in `check-phase-68.mjs` / `check-phase-70.mjs` (Plan 03).
- Three new numbered assertions in `scripts/validation/frozen-read-negative-test.mjs` (Plan 03).
- Raised `timeout:` literals at `check-phase-66.mjs:318` and `check-phase-67.mjs:261` (Plan 04).
- 29 new `if: always()` YAML keys and 4 raised `timeout-minutes:` values across the three
  `audit-harness-v1.{5,6,7}-integrity.yml` workflows (Plan 04).
- `141-sweep.ps1` and `141-EVIDENCE.md` in the phase directory (Plan 05).
- Additional GOV-02 ledger rows (Plans 02, 03, 04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md
@.planning/phases/141-standalone-red-validator-set-chain-members-green/141-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Six owner-ratified document amendments (D-27/D-28/D-29), one commit, .planning/ only</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md` — D-27, D-28 (all six statements), D-29, D-30.
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-RESEARCH.md` §"Document Amendments (D-28) — exact current text, quoted" — the verbatim current text of every one of the six targets.
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-PATTERNS.md` §"`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` — D-28 six amendments" — the bracket-marker convention and the two verbatim precedent lines.
    - `.planning/REQUIREMENTS.md` lines 14-46 (the existing markers at :16, :17, :18, :20, :24 and the "Ordering, corrected" paragraph at :40) and lines 119-162 (the traceability table and the coverage counts).
    - `.planning/ROADMAP.md` §"Phase 141" (lines 140-156).
    - `.planning/STATE.md` lines 20-30, 99-112, 190-222.
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` lines 1-24 (discipline rules and the row schema; row 1 is the direct precedent — the same ROADMAP/REQUIREMENTS edit class).
  </read_first>
  <action>
Amend six statements across three governance documents using the established in-line bracket
marker `**[SUCCESS-CRITERION AMENDMENT, D-28]**` (precedent: the SWEEP-02 re-scope marker
already live at `REQUIREMENTS.md:17`). Annotate and supersede in every case — the original
sentence stays readable inside the same bullet, prefixed or suffixed by the marker and the
correction. Never delete an original clause.

1. `ROADMAP.md` Phase 141 SC#2 (currently at `:148`, the bullet beginning with the `--self-test`
   exits-0 claim). Supersede the prescribed mechanism. The amended criterion states that
   `regenerate-supervision-pins.mjs --self-test` exits 0 via a rebase of the stale `BASELINE_9`
   coordinate array at `regenerate-supervision-pins.mjs:533-543` to its live descendants, with
   `classify()` byte-unchanged, `v1.7-audit-allowlist.json` byte-unchanged, and no classifier
   relaxation. Record that the original classifier-context-window mechanism is WITHDRAWN per
   D-03 for two independent reasons (Phase 48 D-14 already accepted the Tier-2 classifier output
   for that line; the classifier scans only `androidDocPaths()` = 32 files / 26 occurrence lines,
   so all tested window variants are behaviourally identical at 26/0), and that the withdrawal
   rests on a reproduction executed four times independently.
2. `ROADMAP.md` Phase 141 SC#4 (currently at `:150`). Supersede the "scoped to the ~19 validators
   already open for RED-03 in this phase" clause with the corrected census on the requirement's
   own unit — reader SITES, not validators: 19 `chicken-and-egg` return sites measured
   (`check-phase-67.mjs` 7, `check-phase-68.mjs` 2, `check-phase-70.mjs` 10) plus
   `check-phase-61.mjs:39-45`'s inline reader. Phase 141 lands 13 of them (61's 1, 68's 2, 70's
   10); `check-phase-67.mjs`'s 7 defer to Phase 144 with its CARVE amendment landing in Phase 141
   (D-11/D-12).
3. `ROADMAP.md` Phase 141 SC#1 (currently at `:147`). Do NOT amend it as false. Append the marker
   `**[DISCHARGED, D-29]**` and one sentence recording that Phase 140's frozen-aware conversion
   satisfied it with zero glossary edits, measured, and that the ROADMAP's own Depends-on line
   already predicted this — the criterion is satisfied, not falsified, and the ordering it
   encodes was correct.
4. `ROADMAP.md` Phase 141 "Discuss-phase flags: None dominant …" (currently at `:154`). Correct
   it: RED-02's root cause and SWEEP-09's scope were both live forks requiring owner
   ratification, resolved 2026-08-07 by `/grill-me` plus a scored `/adversarial-review` that
   reversed three headline recommendations. Keep the original sentence visible as superseded.
5. `REQUIREMENTS.md` SWEEP-09 (currently at `:24`). Add a second bracket marker
   `**[SUCCESS-CRITERION AMENDMENT, D-28]**` alongside the existing `[NEW REQUIREMENT, D-33,
   scoped to Phase 141]` marker and correct the census onto the reader-site unit as in item 2.
   RETAIN the existing "of which Phase 139's SWEEP-03 fixes 4" clause and RETAIN the entire
   existing `check-phase-61.mjs:39-45` explanatory note verbatim — the draft dropped both.
6. `REQUIREMENTS.md` "Ordering, corrected" paragraph (currently at `:40`). ANNOTATE ONLY. Leave
   the paragraph's existing sentences byte-unchanged and append one new dated line beneath it
   recording the Phase 141 re-measurement (`check-phase-48` 6 PASS / 1 FAIL; `check-phase-60`
   23/2; `check-phase-61` 31/3, all at HEAD `5b8afca1` on a quiesced machine) and the reason for
   annotate-only: `REQUIREMENTS.md:8` stamps every `[MEASURED]` figure to HEAD `347c20a8`, so
   these are dated observations and overwriting them destroys provenance.

Then correct `STATE.md`'s six stale locations. These are plain hygiene corrections, not
success-criterion amendments — no bracket marker.
  a. "Current focus:" (currently `:26`) reads Phase 140; set it to Phase 141 — Standalone-RED
     Validator Set — Chain Members Green.
  b. The Phase-141 requirement row in the "v1.20 Requirement Coverage" table (currently `:216`)
     omits SWEEP-09 entirely; make it list all four IDs with count 4. This omission is the single
     most likely cause of a silently dropped requirement.
  c. The table header (currently `:210`) and the Total row (currently `:220`) read 27; both
     become 28 (28/28 mapped, 0 orphaned), matching `REQUIREMENTS.md:156-158`.
  d. The HARN-19 narrative line (currently `:195`, the close-gate sentence about flipping every
     v1.20 requirement to Validated) still carries the old requirement count; correct it to 28.
  e. The Phase-141 block of the dependency diagram (currently `:101`) lists only three
     requirement IDs; add SWEEP-09.
  f. The same diagram block (currently `:106-109`) repeats the withdrawn classifier-context-window
     mechanism; replace those diagram lines with the rebase mechanism from item 1, keeping the
     diagram's existing indentation and line-width style.

Finally append one row to `.planning/milestones/v1.20-GOV-02-LEDGER.md` for this edit, modelled
on the existing row 1 (same edit class). Record: the target-scoped grep actually run, its hit
count, the regression gate `node scripts/validation/check-phase-54.mjs` and its PASS tally, and
the Result sentence. The ledger row goes in THIS commit.

Commit all of the above as one commit, message `docs(141-01): D-28 six document amendments —
SC#2 mechanism superseded, SWEEP-09 census corrected, STATE 27->28`. The commit must touch
`.planning/` paths only.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs</automated>
    <automated>node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SUCCESS-CRITERION AMENDMENT" .planning/ROADMAP.md` returns at least 2 (SC#2 and SC#4).
    - `grep -c "DISCHARGED, D-29" .planning/ROADMAP.md` returns 1.
    - `grep -c "classifier context window" .planning/ROADMAP.md` may remain non-zero (the superseded text is preserved), but `grep -n "SUCCESS-CRITERION AMENDMENT" .planning/ROADMAP.md` shows a marker on the same bullet as every surviving occurrence inside the Phase 141 section.
    - `grep -c "SWEEP-03 fixes 4" .planning/REQUIREMENTS.md` returns 1 — the clause the draft dropped is retained.
    - `git diff HEAD~1 -- .planning/REQUIREMENTS.md | grep "^-" | grep -c "Ordering, corrected"` returns 0 — the paragraph was annotated, not overwritten.
    - `grep -n "| 141 |" .planning/STATE.md` shows a row containing all four of RED-01, RED-02, RED-03, SWEEP-09 and the count 4.
    - `grep -c "27/27" .planning/STATE.md` returns 0 and `grep -c "28/28" .planning/STATE.md` returns at least 1.
    - `grep -c "all 27 v1.20 requirements" .planning/STATE.md` returns 0.
    - `node scripts/validation/check-phase-54.mjs` exits 0; record its PASS/FAIL tally in the SUMMARY (row-1 precedent recorded 32/32).
    - `node scripts/validation/carve-gate.mjs` exits 0.
    - `git show --stat HEAD --name-only | grep -cv "^\.planning/"` returns 0 for the file list — no `scripts/`, `.github/`, or `docs/` path is in the commit.
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` gained exactly one row, appended at the end, with no existing row edited (`git diff HEAD~1 -- .planning/milestones/v1.20-GOV-02-LEDGER.md | grep -c "^-"` returns 0 apart from the no-newline marker).
    - No requirement anywhere reads `Validated` as a result of this task (D-30).
  </acceptance_criteria>
  <done>All six D-28 statements amended with the superseded text preserved, STATE.md's SWEEP-09 omission and 27->28 counts fixed, check-phase-54 still green, one ledger row appended, commit touches .planning/ only.</done>
</task>

<task type="auto">
  <name>Task 2: CARVE D-09 amendment — add check-phase-67.mjs to the allowlist, alone, before Plan 04's edit</name>
  <files>.planning/milestones/v1.20-CARVE.md</files>
  <read_first>
    - `.planning/milestones/v1.20-CARVE.md` lines 64-77 (the three numbered amendment rules) and lines 149-210 (the `carve-allowlist` fenced block, Category 5 at :178-193).
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-PATTERNS.md` §"`.planning/milestones/v1.20-CARVE.md` — D-09 amendment adding `check-phase-67.mjs`" — the verbatim shape of precedent commit `1bf0a65f`.
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md` D-12 and D-13.
    - `scripts/validation/carve-gate.mjs` lines 30-45 (`IN_SCOPE_PREFIXES`, `CARVE_PATH`, `DEFAULT_BASE`).
  </read_first>
  <action>
Confirm the working tree carries no in-scope modification before starting: `carve-gate.mjs`
enforces D-09 rule 1 mechanically by failing any commit OR dirty working tree in which
`v1.20-CARVE.md` and any in-scope path (`scripts/`, `.github/`, `docs/`, `.gitattributes`,
`package.json`) are touched together.

Add `scripts/validation/check-phase-67.mjs` to the `carve-allowlist` fenced block, inside the
Category 5 chain-validator list, in the file's existing sorted position (between
`check-phase-66.mjs` and `check-phase-68.mjs`). Immediately above the added glob, add a `#`
comment line carrying the one-line rationale required by amendment rule 2: `check-phase-67.mjs`
was absent from the allowlist entirely; Phase 141 raises its `:261` chain-spawn timeout per D-17
and Phase 144 lands its 7 chicken-and-egg guards plus 3 partial-null silent passes per D-12/D-13.

The Category 5 header comment states a count of chain validators that is now one short.
<!-- planner-discipline-allow: fifteen chain validators -->
A grep across `scripts/` and `.github/` for that count phrase returns zero hits, so nothing pins
it — update the number word to sixteen so the comment does not become
a false audit-trail line of exactly the class D-05 records. Re-run that grep before editing and
record the hit count; if it is non-zero, leave the header untouched and note the reason.

Change nothing else in the file — do not touch the "Recorded scope amendments" section, the
amendment procedure, or any other category.

Commit alone, message `docs(141-01): CARVE amendment — allowlist check-phase-67.mjs (Category 5,
D-12)`, with a body naming the D-17 `:261` raise (Phase 141) and the deferred SWEEP-09 edits
(Phase 144) as the edits this amendment authorizes. The commit must touch exactly one file.
  </action>
  <verify>
    <automated>node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `git show --stat HEAD --name-only` lists exactly one path, `.planning/milestones/v1.20-CARVE.md` (D-09 rule 1).
    - `git status --porcelain scripts/ .github/ docs/` was empty at commit time — record the captured output in the SUMMARY.
    - `sed -n '/```carve-allowlist/,/```/p' .planning/milestones/v1.20-CARVE.md | grep -c "^scripts/validation/check-phase-67.mjs$"` returns 1.
    - The line immediately preceding the added glob starts with `#` and names the rationale (amendment rule 2).
    - `grep -rn "fifteen chain validators" scripts/ .github/` returned 0 hits — record the command output; the Category 5 header now reads sixteen.
    - `node scripts/validation/carve-gate.mjs` exits 0.
    - No commit editing `scripts/validation/check-phase-67.mjs` exists at or before this commit (`git log --oneline -- scripts/validation/check-phase-67.mjs` shows nothing newer than the phase base) — D-09 rule 3.
    - No GOV-02 ledger row is added for this commit: `.planning/milestones/v1.20-CARVE.md` is a `.planning/` path, outside `carve-gate.mjs`'s `IN_SCOPE_PREFIXES`, and no frozen surface is modified. The ledger's "absence is correct, not missing evidence" rule applies and must be stated in the SUMMARY.
  </acceptance_criteria>
  <done>check-phase-67.mjs is on the CARVE allowlist via a single-file commit landed before any edit to that file, carve-gate exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author working tree -> `carve-gate.mjs` diff scope | Frozen-surface edits cross here; `.planning/` is deliberately excluded, so this plan's edits are outside the gate's reach by design |
| Governance document -> `check-phase-54.mjs` live read | The only validator that reads ROADMAP/REQUIREMENTS/STATE at live HEAD; an amendment that breaks its assertions converts a doc edit into a chain failure |
| CARVE allowlist -> every later frozen-surface edit in the milestone | The allowlist is the authorization artifact; a self-authorizing amendment would void GOV-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-141-01 | Tampering | `.planning/milestones/v1.20-CARVE.md` | high | mitigate | D-09 rules 1 and 3 enforced mechanically: Task 2 commits one file only, with `git status --porcelain scripts/ .github/ docs/` proven empty first, and lands before the edit it authorizes |
| T-141-02 | Repudiation | ratified success criteria in ROADMAP/REQUIREMENTS | medium | mitigate | Annotate-and-supersede only; original sentences preserved verbatim and the acceptance criteria negative-grep for deletion of the "Ordering, corrected" paragraph |
| T-141-03 | Information disclosure | governance documents | low | accept | Documents contain no secrets, credentials, or tenant identifiers; the three workflows this milestone touches carry zero `secrets.` references (RESEARCH.md Security Domain) |
| T-141-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager install tasks in this phase; RESEARCH.md's Package Legitimacy Audit records "Not applicable — this phase installs zero external packages". No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `node scripts/validation/carve-gate.mjs` exits 0 — HARD BLOCK (CARVE D-10, no warn-and-continue path).
- `node scripts/validation/check-phase-54.mjs` exits 0 with its PASS/FAIL tally recorded.
- GOV-02 ledger: exactly one row appended (Task 1); Task 2 adds none, and the SUMMARY states that
  absence is correct per the ledger's own discipline rule, not an evidence gap.
- Two commits exist, in order: the six-document amendment, then the CARVE amendment. `git log
  --oneline -2` confirms the order and `git show --stat` confirms the file counts.
- No `scripts/`, `.github/`, or `docs/` path was modified anywhere in this plan.
</verification>

<success_criteria>
The withdrawn classifier-context-window mechanism no longer foreclosed by ROADMAP SC#2;
SWEEP-09 present in STATE.md's Phase-141 row; requirement counts read 28; `check-phase-67.mjs`
on the CARVE allowlist by a single-file commit; `carve-gate.mjs` and `check-phase-54.mjs` both
exit 0.
</success_criteria>

<output>
Create `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-01-SUMMARY.md` when done
</output>
