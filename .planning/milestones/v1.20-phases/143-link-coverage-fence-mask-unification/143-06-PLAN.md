---
phase: 143-link-coverage-fence-mask-unification
plan: 06
type: execute
wave: 7
depends_on: ["143-02", "143-05"]
files_modified:
  - scripts/validation/check-nav-hub-links.mjs
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
  - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
autonomous: true
requirements: [LINK-02, LINK-04]
estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "BOTH `:259` (`if (hubSet.has(relPath)) continue;` — the hub-source skip) and `:269` (`if (!hubSet.has(resolvedRel)) continue;` — the hub-target filter) are deleted from `checkInboundLinks`, so the surviving corpus pass scans every file as both source and target."
    - "The `hub file not found` hard-fail at `:222-224` is preserved explicitly — it is the ONLY assertion that the four ratified nav-hubs exist, and `checkInboundLinks:261` merely `continue`s on a missing file."
    - "`HUB_PATHS` is retained as a live roster feeding the hub-existence assertion, not left as dead code; D-13's requirement is satisfied by conscious preservation, never by omission."
    - "The corpus pass covers 274 files and 6252 relative links — every `.md` under `docs/` except the 8 under `docs/_templates/`."
    - "`node scripts/validation/check-nav-hub-links.mjs` exits 0 on the repaired corpus with no accepted-violation baseline, allowlist, ratchet file or expected-failure list of any kind."
    - "The literal prefix `check-nav-hub-links summary: ` is byte-unchanged so the machine-readable summary line stays greppable for Phase 144's needle-spec; only the two bucket labels are reworded so a 274-file scan no longer prints 'inbound failure(s)'."
    - "`--self-test` remains a supported flag spelled exactly `--self-test`, and the `Self-test: N passed, M failed` tail and `process.exit(stFailed > 0 ? 1 : 0)` semantics are unchanged — the sibling file's identical flag is pinned by `check-phase-115.mjs:75` as a bare substring and Phase 144 will pin this file's equivalent."
    - "The checker never self-registers into any chain: the identifier `CHAIN_PHASES` does not appear anywhere in `check-nav-hub-links.mjs`, mirroring the required-ABSENT constraint `check-phase-115.mjs:102` places on the c17 sibling."
    - "LINK-02 adjacency: with both filters gone, a hub file is scanned exactly once as a source by the corpus pass, so no failure is double-reported across the two passes."
    - "LINK-02 empty: `walkMd` on a missing `docs/` returns `[]` and the scan reports 0 failures rather than throwing; `resolvableAnchorSet` on an unreadable target returns an empty Set."
    - "LINK-04 exit semantics: exit 0 iff zero failures in normal mode and zero failed assertions in `--self-test` mode; exit 1 otherwise. No third exit code exists."
    - "LINK-01's model change survives the corpus reaching zero `{#id}` tokens. After D-38's conversion the corpus holds none, but the checker must still model GitHub correctly — the ABSENCE of `{#id}` special-casing is the deliverable, and re-adding a recognition branch would silently re-admit the 65-link false-negative class the moment anyone authors a new override. The header comment written here must defend the deletion on semantics (GitHub does not implement Pandoc attribute syntax), never on corpus census, so a future maintainer reading a zero count does not conclude the branch is now harmless."
    - statement: "The widened scan's runtime stays inside any future CI job budget Phase 144 wires it into."
      verification: backstop
  artifacts:
    - scripts/validation/check-nav-hub-links.mjs
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
    - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
  key_links:
    - "`checkInboundLinks` (both filters removed) -> the only corpus-scale scan in the repo; it is what LINK-02 delivers."
    - "`checkOutboundLinks:222-224` `hub file not found` -> the sole existence assertion for `docs/index.md`, `docs/common-issues.md`, `docs/quick-ref-l1.md`, `docs/quick-ref-l2.md`."
    - "`check-nav-hub-links summary: ` literal -> Phase 144's `check-phase-143.mjs` needle-spec (Plan 08)."
  prohibitions:
    - statement: "No accepted-violation baseline, allowlist, ratchet file, expected-failure list or skip list of any kind is created (LINK-04)."
      status: flagged-unverified
      verification: "`grep -icE 'baseline|allowlist|accepted|expected_fail|ratchet|skiplist' scripts/validation/check-nav-hub-links.mjs` returns 0, and `git status --porcelain scripts/validation/` shows no new file."
    - statement: "`check-nav-hub-links.mjs` is not renamed and no net-new checker file is created (D-11 — `check-phase-123.mjs:40` pins its path string)."
      status: flagged-unverified
      verification: "`node scripts/validation/check-phase-123.mjs` exits 0 and `test -f scripts/validation/check-nav-hub-links.mjs` succeeds."
    - statement: "The `hub file not found` hard-fail is not deleted and `HUB_PATHS` is not removed."
      status: flagged-unverified
      verification: "`grep -c 'hub file not found' scripts/validation/check-nav-hub-links.mjs` returns 1 and `grep -c 'HUB_PATHS' scripts/validation/check-nav-hub-links.mjs` returns at least 2."
    - statement: "No `check-phase-143.mjs` is authored and the checker is added to no `CHAIN_PHASES` array (D-23 — validators are the close phase's indivisible atom)."
      status: flagged-unverified
      verification: "`test ! -f scripts/validation/check-phase-143.mjs` and `grep -c 'CHAIN_PHASES' scripts/validation/check-nav-hub-links.mjs` returns 0."
    - statement: "No corpus file is edited in this plan — the flip is a checker-only change."
      status: flagged-unverified
      verification: "`git diff --name-only HEAD~3..HEAD | grep -c '^docs/'` returns 0."
---

<objective>
Flip `checkInboundLinks` from a four-hub filter to the corpus-wide scan LINK-02 requires, preserve
the hub-existence assertion D-13 protects, and land LINK-04's exit-0-with-no-baseline state.

Purpose: D-14 puts this last on purpose. Flipping first produces a 311-finding red interval with no
diagnostic value; flipping after LINK-01, the `_templates` exclusion, the inline mask and all 145
repairs means the very first corpus-wide run in a committed state reports zero. That is what makes
LINK-04's "no accepted-violation baseline of any kind" trivially true: there is never a moment where
a corpus-wide red state exists in a landed commit, so no baseline is ever tempting.

Output: a 274-file / 6252-link corpus checker that exits 0, with the four ratified hubs still
asserted to exist and the summary line still greppable for Phase 144.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
@scripts/validation/check-nav-hub-links.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: GOV-02 pre-edit census for the flip, and the D-13 ruling on `HUB_PATHS`</name>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md, .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md</files>
  <read_first>
    - `scripts/validation/check-nav-hub-links.mjs:26-32` `HUB_PATHS`, `:218-247` `checkOutboundLinks`, `:253-283` `checkInboundLinks`, `:396-423` the main runner and summary line
    - `scripts/validation/check-phase-123.mjs:40,83` — the `presence()` pin on this file's PATH STRING
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md:19-23,55` — schema and the four-part grep shape
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md` — D-11, D-12, D-13
  </read_first>
  <action>
Append the GOV-02 ledger row for this edit BEFORE touching the file, copying row 55's four-part
shape: (1) target-scoped path-literal grep for `check-nav-hub-links.mjs` across `scripts/`,
`.github/` and `.planning/`; (2) symbol-scoped grep for `checkInboundLinks`, `checkOutboundLinks`,
`HUB_PATHS`, `hubSet` and the literal `check-nav-hub-links summary:` across `scripts/`; (3) an
explicit cross-reference check re-confirming `check-phase-123.mjs:40,83` pins only the path string
via `presence()` and carries no content pin on the summary line or on either scan function; (4) a
Result column with the concrete `git diff --numstat` figure.

Record the pre-edit regression baseline in the same row: `node scripts/validation/check-phase-123.mjs`
result, plus the checker's own `0 outbound / 0 inbound / 0 total` exit 0.

Then write the **D-13 ruling** into `143-EVIDENCE.md`, because D-13 requires the roster be preserved
consciously or retired consciously, never by omission:

> **Ruling: preserve both, with roles split.** `checkInboundLinks` becomes the single corpus-wide
> link scan — both `:259` and `:269` are deleted, so it scans every one of the 274 files as a source
> and accepts every file as a target. `checkOutboundLinks` retains its `hub file not found`
> hard-fail at `:222-224`, which is the ONLY assertion anywhere that the four ratified nav-hubs
> exist (`checkInboundLinks:261` merely `continue`s on a missing file, so collapsing to one pass
> would silently delete that assertion). Its per-link scanning role is retired as redundant — with
> `:259` gone, the corpus pass already covers the hubs' 484 relative links as a source, and keeping
> both scans would double-report every hub-link failure. `HUB_PATHS` therefore stays live as the
> hub-existence roster, not as dead code.

Record the consequence explicitly: the first reported bucket now counts hub-presence failures only
and is expected to be 0 in every healthy state, while the second counts every corpus link failure.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-123.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` gains one appended row naming `scripts/validation/check-nav-hub-links.mjs` and citing "Plan 143-06".
    - `git diff -- .planning/milestones/v1.20-GOV-02-LEDGER.md | grep -c '^-[^-]'` returns 0 (append-only).
    - The row's Grep column names all three search roots (`scripts/`, `.github/`, `.planning/`) and at least five symbol targets.
    - `143-EVIDENCE.md` contains the D-13 ruling verbatim, naming `:222-224`, `:261`, `HUB_PATHS` and the double-reporting rationale.
    - `node scripts/validation/check-phase-123.mjs` exits 0 (pre-edit baseline recorded).
  </acceptance_criteria>
  <done>The pre-edit ledger row is appended with a four-part target-scoped grep, and D-13's roster ruling is recorded with its rationale before any code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete `:259` and `:269`, retire the redundant outbound link scan, reword the report</name>
  <files>scripts/validation/check-nav-hub-links.mjs</files>
  <read_first>
    - `scripts/validation/check-nav-hub-links.mjs` — the whole file; specifically `:2-15` the header comment, `:26-32` `HUB_PATHS`, `:82-84` the `buildFenceMask` provenance comment, `:218-247` `checkOutboundLinks`, `:249-283` `checkInboundLinks`, `:396-423` the main runner
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the D-13 ruling from Task 1
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-RESEARCH.md` — Q5 (report format and exit-code conventions) and Pitfall 4
  </read_first>
  <action>
Land the flip as one commit.

**The two deletions (D-12).** In `checkInboundLinks`, delete `:259`
(`if (hubSet.has(relPath)) continue;`) and `:269`
(`if (!hubSet.has(resolvedRel)) continue; // only care about links targeting a hub`), together with
their comments. Remove the now-unused `const hubSet = new Set(HUB_PATHS);` from this function if
nothing else in it references `hubSet`. Rename the function to reflect its new role only if the
rename stays internal — do NOT rename the file, and do not change the `--self-test` flag spelling.

**Preserve the hub-existence assertion (D-13).** In `checkOutboundLinks`, keep the `content === null`
branch at `:222-224` pushing `{ file: hubRel, line: 0, reason: 'hub file not found' }` exactly as
written. Retire only the per-link loop below it (`:226-244`) — with `:259` gone, the corpus pass
covers the four hubs' 484 relative links as sources, so keeping the loop would double-report every
hub-link failure. Keep `HUB_PATHS` and keep the function reading each hub file so the existence
check is real.

**Reword the report, keep the shape (Pitfall 4 / D-11).** The literal prefix
`check-nav-hub-links summary: ` at `:418-420` must stay byte-identical — it is the greppable
anchor Phase 144's needle-spec will pin. Change only the two bucket labels so a 274-file scan stops
printing "N inbound failure(s)": for example
`check-nav-hub-links summary: N hub-presence failure(s), M corpus-link failure(s), T total`.
Keep the labelled-counts-plus-total format and the `process.exit(allFailures.length > 0 ? 1 : 0)`
tail. Update the matching section headers at `:406` and `:410` and the clean-run line at `:414`.

**Update every stale string D-11 measured**, so the file does not describe a four-hub tool while
being a corpus tool: the header comment at `:2-7` and `:9-12`, the `HUB_PATHS` comment at `:26`,
the `buildFenceMask` provenance comment at `:82-84` (which currently claims a scope it no longer
has), the `checkInboundLinks` doc comment at `:249-252`, and the main-runner comment at `:397-398`.
State in the header that this is the corpus-wide relative-link and anchor checker for `docs/`,
excluding `docs/_templates/`, modelling GitHub anchor semantics, and that it exits 0 iff zero
failures with **no accepted-violation baseline of any kind**.

Add one further header paragraph, because the corpus now contains zero `{#id}` tokens and a future
maintainer will notice: the absence of `{#id}` handling is deliberate and permanent, justified by
GitHub not implementing Pandoc attribute syntax — NOT by the corpus happening to contain none.
Re-adding a recognition branch would silently re-admit the 65-link false-negative class the moment
anyone authors a new override. Do not write the corpus count into this comment; a count invites
exactly the "it's harmless now" reasoning the paragraph exists to block.

**Do not add** a `CHAIN_PHASES` array, a baseline file, an allowlist, an expected-failure list, a
skip list, or a `check-phase-143.mjs`. D-23 makes CI wiring Phase 144's work; this file must never
self-register, mirroring the required-ABSENT `CHAIN_PHASES` constraint `check-phase-115.mjs:102`
places on the c17 sibling.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; echo "exit=$?"; node scripts/validation/check-nav-hub-links.mjs --self-test | tail -1; node scripts/validation/check-phase-123.mjs; echo "cp123_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 and prints a summary line whose total is `0`.
    - The summary line begins with the byte-identical literal `check-nav-hub-links summary: ` — asserted by `node scripts/validation/check-nav-hub-links.mjs | grep -c '^check-nav-hub-links summary: '` returning 1.
    - `node scripts/validation/check-nav-hub-links.mjs | grep -c 'inbound failure'` returns 0 — the misleading wording is gone.
    - `grep -c 'hubSet.has' scripts/validation/check-nav-hub-links.mjs` returns 0 — both filters are deleted.
    - `grep -c 'hub file not found' scripts/validation/check-nav-hub-links.mjs` returns 1 — the existence assertion survives.
    - `grep -c 'HUB_PATHS' scripts/validation/check-nav-hub-links.mjs` returns at least 2 — the roster is live, not dead code.
    - `grep -c 'CHAIN_PHASES' scripts/validation/check-nav-hub-links.mjs` returns 0.
    - `grep -icE 'baseline|allowlist|accepted-violation|expected_fail|ratchet' scripts/validation/check-nav-hub-links.mjs | head -1` — any match must be inside the header comment asserting NO baseline exists; a review of each hit confirms none names a data structure.
    - `node scripts/validation/check-nav-hub-links.mjs --self-test` prints `Self-test: 10 passed, 0 failed` and exits 0.
    - `node scripts/validation/check-phase-123.mjs` still exits 0 — the path-string `presence()` pin survives the in-place edit.
    - `test ! -f scripts/validation/check-phase-143.mjs`.
    - `git diff --name-only HEAD~1 | grep -c '^docs/'` returns 0 — the flip is a checker-only commit.
  </acceptance_criteria>
  <done>Both filters are deleted, the hub-existence hard-fail and `HUB_PATHS` are preserved with roles split, every stale four-hub string is corrected, the summary-line prefix is byte-identical, and the corpus scan exits 0 with no baseline.</done>
</task>

<task type="auto">
  <name>Task 3: LINK-04 no-baseline proof and the committed-green record</name>
  <files>.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md</files>
  <read_first>
    - `scripts/validation/check-nav-hub-links.mjs` — the post-flip file
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the dry-run ladder
    - `.planning/ROADMAP.md:223` — the amended SC#2 wording
  </read_first>
  <action>
Discharge LINK-04's "no accepted-violation baseline of any kind" with positive evidence, not with a
zero count alone — a zero count is compatible with a baseline that happens to be empty, and the
disposition class this milestone exists to delete is exactly the invisible one.

Record in `143-EVIDENCE.md`, under a `## LINK-04 no-baseline discharge` heading:

1. **Absence of any baseline artifact.** `git status --porcelain scripts/validation/` shows no new
   file from this phase; the phase creates no `.json`, `.txt` or `.md` sidecar consumed by the
   checker. Record the command and its empty output.
2. **Absence of any in-source baseline structure.** Record the result of a case-insensitive grep
   over `check-nav-hub-links.mjs` for `baseline`, `allowlist`, `accepted`, `expected`, `ratchet`,
   `skip`, `ignore` and `known`, and confirm every hit (if any) is prose in the header comment
   asserting no baseline exists, never an identifier, array or file read.
3. **No committed red interval.** Record `git log --oneline` for the phase and confirm that no
   commit in the sequence leaves a corpus-wide red state: the flip is the last checker commit and
   the corpus was already fully repaired when it landed. This is the structural reason no baseline
   was ever tempting, and it is what D-14's ordering bought.
4. **Exit-code semantics.** Record that exit 0 occurs iff zero failures (normal mode) or zero failed
   assertions (`--self-test`), that exit 1 covers every other case, and that no third exit code
   exists anywhere in the file.
5. **The dry-run ladder, closed.** Append the final committed-state row to the ladder: the same
   run, now from a landed commit rather than a working-tree patch. The full sequence is
   175 -> 173 -> 143 -> 78 (D-38's all-87 conversion) -> 49 -> 13 -> 0 (dry-run) -> 0 (committed).

Also record the measured runtime of the committed corpus scan as a range across at least three runs
on a quiesced machine, and declare tree identity, cache state and node version alongside it — never
quote a single timing value. This figure is what Phase 144 needs when it wires the spawn into
`check-phase-143.mjs` under a job timeout.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; echo "exit=$?"; node scripts/validation/c17-eee-contract.mjs | tail -2; node scripts/validation/carve-gate.mjs | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `143-EVIDENCE.md` contains a `## LINK-04 no-baseline discharge` section with all five numbered items, each carrying the command run and its actual output.
    - The recorded grep for baseline-shaped identifiers has zero hits that are identifiers, arrays or file reads.
    - `git status --porcelain scripts/validation/` is empty.
    - The dry-run ladder has a final committed-state row reading 0.
    - The runtime record states a range across n>=3 runs plus tree identity, cache state and node version — never a single value.
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0; `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations, 0 total violations`; `node scripts/validation/carve-gate.mjs` exits 0 with `off-list=0`.
  </acceptance_criteria>
  <done>LINK-04 is discharged by positive evidence of absence across five independent checks, not by a zero count alone; the ladder is closed; the runtime range is recorded for Phase 144.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus `.md` content -> the widened scan | Scope rises from 4 files / 484 links to 274 files / 6252 links; every path-shaped string in the corpus now reaches `path.resolve` and `existsSync`. |
| checker exit code -> Phase 144's `check-phase-143.mjs` -> the apex chain | This exit code becomes a chain-validated gate next phase; a silently-permissive checker would green the chain while the corpus rots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-22 | Spoofing | an invisible accepted-violation baseline | critical | mitigate | Task 3 discharges LINK-04 with five independent positive checks — no sidecar artifact, no in-source baseline identifier, no committed red interval, explicit exit-code semantics, and a closed measurement ladder. A zero count alone is explicitly rejected as sufficient evidence. |
| T-143-23 | Denial of Service | silent loss of the hub-existence assertion | high | mitigate | D-13's ruling is written into `143-EVIDENCE.md` before the edit and asserted afterwards by `grep -c 'hub file not found'` returning 1 and `grep -c 'HUB_PATHS'` returning at least 2. |
| T-143-24 | Tampering | frozen path-string pin breakage | high | mitigate | GOV-02 row records the target-scoped path-literal grep before the edit; the file is edited in place, never renamed; `node scripts/validation/check-phase-123.mjs` is run as an acceptance criterion after the edit. |
| T-143-25 | Tampering | path traversal via a corpus link target | medium | mitigate | Unchanged code path. `resolveLinkTarget` is pure string math over `path.resolve` and the caller's `existsSync` reports not-found; self-test case G asserts a six-level `../` traversal resolves without throwing, and it now runs against a 274-file corpus rather than 4 files. |
| T-143-26 | Elevation of Privilege | checker self-registering into the chain | high | mitigate | `grep -c 'CHAIN_PHASES'` must return 0 and `check-phase-143.mjs` must not exist — mirroring the required-ABSENT constraint `check-phase-115.mjs:102` places on the c17 sibling, which is the precedent D-23 names as binding here. |
| T-143-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs; `check-nav-hub-links.mjs:17` pins "Node built-ins ONLY". `143-RESEARCH.md` Package Legitimacy Audit: "Not applicable". |
</threat_model>

<verification>
- `node scripts/validation/check-nav-hub-links.mjs` -> exit 0, total 0, summary-line prefix byte-identical.
- `node scripts/validation/check-nav-hub-links.mjs --self-test` -> `Self-test: 10 passed, 0 failed`, exit 0.
- `node scripts/validation/check-phase-123.mjs` -> exit 0.
- `node scripts/validation/c17-eee-contract.mjs` -> `234 files checked, 0 with violations, 0 total violations`.
- `node scripts/validation/carve-gate.mjs` -> exit 0, `off-list=0`.
- `grep -c 'hubSet.has' scripts/validation/check-nav-hub-links.mjs` -> 0; `grep -c 'hub file not found'` -> 1; `grep -c 'CHAIN_PHASES'` -> 0.
</verification>

<success_criteria>
The corpus-wide scan covers 274 files and 6252 links and exits 0; both `:259` and `:269` are gone;
the hub-existence hard-fail and `HUB_PATHS` survive with roles consciously split; the summary-line
prefix is byte-identical; LINK-04's no-baseline claim is discharged by five positive checks; no
`check-phase-143.mjs` exists.
</success_criteria>

## Artifacts this phase produces

Created or modified by **this plan**:

| Artifact | Kind | Note |
|---|---|---|
| `checkInboundLinks` (corpus-wide) | function, filters removed | `:259` + `:269` deleted (D-12) |
| `checkOutboundLinks` (hub-existence only) | function, link loop retired | `hub file not found` hard-fail preserved (D-13) |
| Reworded report buckets + section headers | output strings | `check-nav-hub-links summary: ` prefix byte-unchanged |
| Corrected header / provenance / runner comments | source comments | D-11's measured stale-string set |
| D-13 `HUB_PATHS` ruling | decision record in `143-EVIDENCE.md` | preserve-both, roles split |
| LINK-04 no-baseline discharge (5 checks) | evidence section | absence proven positively |
| Committed-state runtime range (n>=3) | measurement | for Phase 144's job timeout |
| `v1.20-GOV-02-LEDGER.md` row (the flip) | ledger row | row-per-edit, appended |

<output>
Create `.planning/phases/143-link-coverage-fence-mask-unification/143-06-SUMMARY.md` when done.
</output>
