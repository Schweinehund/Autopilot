---
phase: 143-link-coverage-fence-mask-unification
plan: 02
type: execute
wave: 2
depends_on: ["143-01"]
files_modified:
  - scripts/validation/check-nav-hub-links.mjs
  - docs/error-codes/01-mdm-enrollment.md
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
  - .planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md
  - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
autonomous: true
requirements: [LINK-01, LINK-02]
estimate:
  tokens: 96000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`computeAnchorSetFromContent` no longer registers a `{#id}` token as a verbatim anchor and no longer suppresses the auto-slug of a heading carrying one — LINK-01 is a net DELETION at `:137-143`, `:150-156` and `:123`, plus one ADDITION (the `<a id>` loop)."
    - "`computeAnchorSetFromContent` recognises `<a id=\"x\"></a>` on any non-fenced line via a `matchAll` loop, so `docs/admin-setup-android/05-dedicated-devices.md:242`'s two adjacent tags both register."
    - "The checker models the UN-prefixed authored id. GitHub rewrites authored ids to `user-content-<id>` at render time while heading permalink hrefs stay unprefixed and its client-side JS reconciles the two — a maintainer must not 'correct' the checker to expect the `user-content-` prefix."
    - "The GitHub model is the permanent ABSENCE of `{#id}` special-casing, not a temporary state justified by the corpus. After D-38's conversion zero `{#id}` tokens remain in `docs/`, but re-adding a recognition branch would silently re-admit the entire 65-link false-negative class the moment anyone authors a new override — so the deletion must be defended on semantics (GitHub does not implement Pandoc attribute syntax), never on corpus census."
    - "D-01's one-way rating stands and its gate is DISCHARGED, not removed: the owner ruled on 2026-08-11 (D-38) to convert all 87 `{#id}` overrides across 29 files to `<a id>` anchors, which delivers the complete D-01 remedy target-side with a measured ZERO regressions — no link anywhere in the corpus currently resolves *because* a heading's slug is inflated by its own `{#…}` text."
    - "Ordering is a hard gate, not a preference: the D-38 conversion MUST land AFTER this plan's `<a id>` recognition loop. Running the conversion against the pre-LINK-01 checker was measured to turn the hub-scope scan RED with 2 outbound failures (`docs/quick-ref-l2.md:345,347` -> `operations/patch-management/04-android-patch-delivery.md#deadlines-cutover-dates`), because the shipped checker registers `{#id}` verbatim and does not recognise `<a id>` at all."
    - "`node scripts/validation/check-nav-hub-links.mjs --self-test` exits 0 with at least 9 assertions, including a rewritten GitHub-model `{#id}` case asserting `foo-bar-custom-anchor` present and bare `custom-anchor` ABSENT, plus an `<a id>` recognition case covering the two-tags-on-one-line shape."
    - "The end-to-end tracer path — `docs/error-codes/00-index.md:49` -> `../01-mdm-enrollment.md#0x80180014` -> the `<a id=\"0x80180014\"></a>` on row `docs/error-codes/01-mdm-enrollment.md:33` — resolves under a temporary corpus-scope dry-run, and the dry-run total drops from the measured 175 to 173."
    - "After the `docs/_templates/` exclusion and inline-code masking land, the dry-run corpus scan measures 274 files / 6252 relative links / 13 broken file targets / 130 broken anchors / 143 total."
    - "The inline-mask leg is load-bearing: without it broken file targets measure 14, not 13 — the extra being `docs/recipes/03-windows-11-multi-app-kiosk.md:173`'s backtick-spanned PowerShell `[xml](Get-Content …)` cast read as a markdown link."
    - "LINK-01 empty/null input: `computeAnchorSetFromContent('')` returns an empty Set without throwing, and `resolvableAnchorSet` on a missing file returns an empty Set (the existing `:174` null-guard is preserved)."
    - "LINK-01 encoding: slug equality is exact-string `Set.has` over JS UTF-16 strings with an ASCII-only `[^a-z0-9 _-]` filter after `toLowerCase()`; no Unicode normalization, no grapheme segmentation, no case folding at comparison time — which is why the mixed-case fragment `0x801C03F3` must be anchored with its case preserved verbatim."
    - "LINK-02 adjacency: two headings whose slugs collide receive `-1`/`-2` encounter-order suffixes via the existing `Map<slugBase,count>`; an `<a id>` value equal to a heading slug collapses to one Set member rather than colliding, because the anchor set is a `Set`."
    - "LINK-02 empty: a `.md` file with zero links contributes zero failures, `walkMd` on a missing directory returns `[]`, and a bare `#anchor` self-link resolves against the linking file itself via `resolveLinkTarget:212`."
    - statement: "The `<a id>` inside a `<td>` renders as a working anchor on github.com for every one of the repaired rows, not only the row proven via the rendering API."
      verification: backstop
  artifacts:
    - scripts/validation/check-nav-hub-links.mjs
    - docs/error-codes/01-mdm-enrollment.md
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
    - .planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md
    - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
  key_links:
    - "D-38's `<a id>` conversion (Plan 09) -> this plan's `<a id>` recognition loop. The ordering is a measured hard gate: conversion before recognition turns the hub-scope scan red."
    - "`computeAnchorSetFromContent` -> `resolvableAnchorSet` (per-file cache at `:171-177`) -> `checkOutboundLinks`/`checkInboundLinks` anchor test — the single point every anchor decision flows through."
    - "`check-phase-123.mjs:40,83` pins `check-nav-hub-links.mjs`'s PATH STRING via `presence()` — an in-place edit is safe, a rename is not."
    - "`check-nav-hub-links.mjs:418-420`'s summary line -> Phase 144's `check-phase-143.mjs` needle-spec — keep the shape, widen only the wording."
  prohibitions:
    - statement: "`check-nav-hub-links.mjs` is not renamed and no net-new checker file is created (D-11)."
      status: flagged-unverified
      verification: "`test -f scripts/validation/check-nav-hub-links.mjs` succeeds and `git status --porcelain scripts/validation/ | grep -c '^A'` returns 0."
    - statement: "No new documentation content is authored — the only `docs/` change is an `<a id>` tag prepended to an existing table row's first cell."
      status: flagged-unverified
      verification: "`git diff HEAD~1 -- docs/ | grep '^+' | grep -vc '<a id='` returns 0 for the corpus commit."
    - statement: "No `{#id}`-recognition branch is re-added under a different name; the GitHub model is the ABSENCE of special-casing (RESEARCH Pitfall 1)."
      status: flagged-unverified
      verification: "The `computeAnchorSetFromContent` + `stripHeadingText` diff has strictly more removed lines than added lines."
    - statement: "No accepted-violation baseline, allowlist, ratchet file or expected-failure list is created for the link checker."
      status: flagged-unverified
      verification: "`git status --porcelain` shows no new file under `scripts/validation/` and the checker source contains no `BASELINE`, `ACCEPTED`, `EXPECTED_FAIL` or `allowlist` identifier."
    - statement: "The dry-run patch removing `:259`/`:269` is never committed in this plan — the corpus flip is Plan 06's commit."
      status: flagged-unverified
      verification: "After the tracer, `grep -c 'hubSet.has' scripts/validation/check-nav-hub-links.mjs` returns 3 (one construction site plus the two surviving guards) and `git status --porcelain scripts/validation/check-nav-hub-links.mjs` is empty."
---

<objective>
Adopt the GitHub anchor model in `check-nav-hub-links.mjs` and prove it end-to-end on ONE real
corpus path, then narrow the scan's true-positive set with the `docs/_templates/` exclusion and
inline-code masking — D-14 steps 1, 2 and 3, in order. The corpus flip (D-14 step 4) is
deliberately NOT in this plan.

Purpose: 65 links are green in the checker and broken on GitHub because
`computeAnchorSetFromContent` implements Pandoc/kramdown `{#id}` semantics against a corpus
rendered by GitHub. D-01 (OWNER-RATIFIED, rated **one-way**) adopts the GitHub model, which is what
makes those 65 visible and fixable. Landing this before any corpus repair is what stops the phase
from freezing a 201-link invisible false-positive baseline — the exact disposition class this
milestone exists to delete.

This plan is also the hard ordering gate for D-38's all-87 `{#id}` conversion (Plan 09, wave 3):
the conversion run against the pre-LINK-01 checker was MEASURED to turn the hub-scope scan red,
because the shipped checker registers `{#id}` verbatim and cannot see an `<a id>` at all. Model
first, conversion second, always.

Output: the recorded D-38 ruling and its zero-regression evidence, the corrected anchor model, an
expanded `--self-test`, one proven corpus repair, and a measured end-to-end dry-run number
(175 -> 173 -> 143) that Plan 09 then drops to 78 and every later plan drives to zero.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-RESEARCH.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-PATTERNS.md
@scripts/validation/check-nav-hub-links.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record D-38 — the owner's discharge of D-01's one-way gate — and open the evidence artifact</name>
  <files>.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md, .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md` — D-01, D-04, D-07, D-30 (the marker instrument), and the highest existing decision number, which is D-37
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-RESEARCH.md` — Open Question 3's `gh api` transcript, the first-party evidence that the `{#…}` token renders as visible literal text
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-PATTERNS.md` — the `<a id>` placement precedent section
  </read_first>
  <action>
This task replaces the blocking `checkpoint:decision` this plan previously carried. **The owner has
ruled; the gate is discharged, not deleted.** Record the ruling so execution does not stop here and
so the reasoning survives into Phase 144's close-gate.

**Step 1 — append D-38 to `143-CONTEXT.md`**, after D-37, using D-30's ratified marker instrument
and this repo's decision-bullet grammar (bold `**D-38, short label.**` lead-in, at most one colon
and the colon kept OUTSIDE the bold span — a `file:line` inside the bold span hard-blocks the
decision-coverage gate). Record verbatim:

- **The ruling.** Convert ALL 87 `{#id}` overrides to `<a id="…"></a>` anchors, across all 29
  files. Not the 22 that happen to be link targets — all 87.
- **The rationale the owner accepted.** The `{#…}` token renders as visible literal junk text in
  the heading on github.com, confirmed first-party against GitHub's own rendering API
  (`143-RESEARCH.md` Open Question 3). It is therefore a rendering defect in all 87 places, not
  only in the 22 that are link targets. Converting all 87 ends the class rather than pruning it,
  and leaves exactly one anchor convention corpus-wide.
- **What it supersedes.** D-04's source-side Class-B routing for the 26 (target-file, fragment)
  pairs whose target heading carries an override. D-38 does not weaken D-04 — it delivers the same
  links through the target-side branch D-04 already sanctions. The remaining pairs stay on D-04's
  per-class routing unchanged.
- **What it does NOT change.** D-04's bar on "add the missing section", D-11's rename prohibition,
  D-12's two-condition deletion, D-14's checker ordering, D-23's Phase-144 hand-off, D-31's
  three-step commit sequence, D-33's ledger-before-edit rule, and both CARVE Standing bars. D-38
  authors no prose: it removes junk text and adds an anchor.

**Step 2 — create `143-EVIDENCE.md`** and open it with a `## D-38 ruling and its evidence` section
carrying the measured blast radius as a table, every row tagged `[MEASURED]` with the tree
identity, cache state and node version declared (D-36):

| state | brokenFile | brokenAnchor | distinct pairs |
|---|---|---|---|
| BEFORE (HEAD, GitHub model, templates excluded, inline mask on) | 13 | 132 | 77 |
| AFTER (all 87 converted) | 13 | 67 | 51 |

Record these four findings explicitly, because each one is load-bearing downstream:

1. **Zero regressions.** No pair broken in the AFTER set is absent from the BEFORE set. No link
   anywhere in the corpus currently resolves *because* a heading's slug is inflated by its own
   `{#…}` text. This was the one real hazard and it is measured absent.
2. **65 links / 26 pairs fixed by the conversion alone** — exactly the 65 phantom links D-01
   identified. The all-87 conversion IS the complete D-01 remedy, delivered target-side.
3. **Ordering is a hard gate.** Running the conversion against the *pre*-LINK-01 checker turns the
   hub-scope scan RED: 2 outbound failures at `docs/quick-ref-l2.md:345,347` pointing at
   `operations/patch-management/04-android-patch-delivery.md#deadlines-cutover-dates`, because the
   shipped checker registers `{#id}` verbatim and does not recognise `<a id>` at all. The
   conversion therefore lands in Plan 09 at wave 3, strictly after this plan's Task 2.
4. **c17 is unaffected.** A trial application of all 87 conversions, measured and reverted, leaves
   `c17-eee-contract.mjs` at `234 files checked, 0 with violations, 0 total violations` with all
   thirteen counters at 0, and produces a `git diff --numstat` of exactly 174 added / 87 removed
   across 29 files — one removed heading line replaced by two lines, everywhere, with no
   collateral line touched.

**Step 3 — record the `<a id>` placement ruling**, since D-38 says convert but not where the tag
goes. The ruling, with its precedent: **the `<a id>` goes on its own line immediately ABOVE the
heading line**, following `docs/admin-setup-android/02-zero-touch-portal.md:38-39`. Two independent
reasons, both measured:

- **Corpus precedent.** Of the 200 live `<a id>` instances in `docs/`, **182 are alone on their own
  line**; **zero** are inline within a heading line. Own-line placement is overwhelmingly the
  house convention; inline-in-heading has no precedent at all.
- **A correctness hazard inline placement would create.** GitHub strips HTML tags before slugifying
  a heading, but this checker's `stripHeadingText` (`:121-127`) strips only `**` and backticks —
  it does not strip HTML. An inline `<a id>` would therefore be slugified as ordinary text and the
  checker's computed auto-slug would diverge from GitHub's, re-creating the exact model-divergence
  defect class D-01 exists to close. Own-line placement leaves the heading text clean for both.

Commit this record before any code change, subject
`docs(143-02): record D-38 owner ruling and open the phase evidence artifact`.
  </action>
  <verify>
    <automated>test -f .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md; echo "evidence=$?"; grep -c 'D-38' .planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \*\*D-38, ' .planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md` returns 1.
    - `grep -n '^- \*\*D-38,[^*]*:[^*]*\*\*' .planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md` returns no match — no colon inside the bold span.
    - `143-EVIDENCE.md` exists and its `## D-38 ruling and its evidence` section carries the 2-row blast-radius table with both `13` values, `132`, `67`, `77` and `51`.
    - All four numbered findings are present, and the zero-regression finding states the comparison method (AFTER pair set minus BEFORE pair set is empty), not just the conclusion.
    - The placement ruling is present and cites both the 182-of-200 own-line census and `stripHeadingText`'s non-stripping of HTML as independent reasons.
    - `git show --name-only --format= HEAD` lists only paths under `.planning/` — zero paths under `docs/` or `scripts/`.
    - `node scripts/validation/carve-gate.mjs` exits 0 with `off-list=0` (this task edits `.planning/` only, outside `IN_SCOPE_PREFIXES`).
  </acceptance_criteria>
  <done>D-38 is recorded in CONTEXT.md with its rationale, supersession scope and unchanged bars; the evidence artifact exists carrying the measured blast radius, the zero-regression finding, the ordering gate, the c17 null result and the placement ruling with both its precedents.</done>
</task>

<task type="tracer">
  <name>Task 2: TRACER — GitHub anchor model end-to-end on one real corpus path</name>
  <files>scripts/validation/check-nav-hub-links.mjs, docs/error-codes/01-mdm-enrollment.md, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `scripts/validation/check-nav-hub-links.mjs` — the whole file; specifically `:111-116` `githubSlug`, `:121-127` `stripHeadingText`, `:132-166` `computeAnchorSetFromContent`, `:290-394` the self-test block
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-RESEARCH.md` — Q1's four-step change list, Q3's regex census, Open Question 3's `gh api` transcript
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-PATTERNS.md` — the `matchAll` loop shape and the `<a id>` table-row placement rule
    - `docs/error-codes/01-mdm-enrollment.md:28-42` — the error-code table whose row `:33` carries the tracer anchor
    - `docs/error-codes/00-index.md:49` and `docs/error-codes/04-pre-provisioning.md:34` — the two source links of the tracer path
    - `docs/admin-setup-android/13-aosp-meta-quest.md:62-65` — the live table-row `<a id>` placement precedent
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md:19-23,55` — row schema and the four-part grep shape
  </read_first>
  <action>
Wire ONE link through every layer this phase touches — governance ledger, anchor model, self-test,
corpus repair, corpus-wide resolution — and prove it end-to-end before any expansion.

**Step 1 — GOV-02 ledger row, BEFORE the edit (D-33).** Append one row to
`.planning/milestones/v1.20-GOV-02-LEDGER.md` for `scripts/validation/check-nav-hub-links.mjs`,
copying row 55's four-part shape: (1) target-scoped path-literal grep for
`check-nav-hub-links.mjs` across `scripts/`, `.github/` and `.planning/`; (2) symbol-scoped grep
for `computeAnchorSetFromContent`, `stripHeadingText`, `githubSlug` across `scripts/`; (3) an
explicit cross-reference check confirming `check-phase-123.mjs:40,83` pins only the PATH STRING via
`presence()` and carries no content pin; (4) a Result column with the concrete
`git diff --numstat` figure. Record the pre-edit regression baseline in the same row:
`node scripts/validation/check-nav-hub-links.mjs` = `0 outbound / 0 inbound / 0 total`, exit 0;
`--self-test` = 7 passed / 0 failed; `node scripts/validation/c17-eee-contract.mjs` =
`234 files checked, 0 with violations, 0 total violations` with all 13 counters 0.

**Step 2 — LINK-01, a net deletion plus one addition.** In `check-nav-hub-links.mjs`:
- Delete the `{#id}` override-registration loop at `:137-143` in its entirety, together with its
  `// (a) {#id} overrides` comment.
- Delete the auto-slug-suppression branch at `:150-156` — the comment block plus the line
  `if (/\s*\{#[a-zA-Z0-9_-]+\}\s*$/.test(m[1])) continue;`. Every heading must now always produce
  an auto-slug and always consume a dedup count.
- Delete the `{#id}`-stripping line at `:123`, `.replace(/\s*\{#[a-zA-Z0-9_-]+\}\s*$/, '')`, from
  `stripHeadingText`, and update that function's leading comment so it no longer claims to strip a
  trailing override token. The `{`, `#` and `}` characters are then stripped by `githubSlug`'s
  existing `[^a-z0-9 _-]` filter as ordinary punctuation, exactly as GitHub does.
- Add, in the position the deleted loop occupied, an `<a id>` recognition loop with the identical
  `matchAll` shape, using the regex `/<a\s+id\s*=\s*"([a-zA-Z0-9_-]+)"\s*>\s*<\/a>/g` and adding
  `m[1]` to the set. It must be `matchAll`, never `.match()` —
  `docs/admin-setup-android/05-dedicated-devices.md:242` carries two adjacent tags on one line and
  a single-match implementation silently drops the second.
- Update the `computeAnchorSetFromContent` header comment to describe the GitHub model: the union
  of every `<a id>` value and every heading's GitHub slug, with `{#id}` treated as ordinary heading
  text. Add an explicit maintainer note that GitHub rewrites authored ids to `user-content-<id>`
  at render time while heading permalink hrefs stay unprefixed, so this checker deliberately models
  the UN-prefixed id and must not be "corrected" to expect the prefix.

Do not touch `githubSlug` itself. Do not rename the file, any exported name, or the
`check-nav-hub-links summary:` line. Do not add a `CHAIN_PHASES` array or any self-registration.

**Step 3 — self-test.** Rewrite existing case D (`:341-350`) rather than adding beside it: with
content `'### Foo Bar {#custom-anchor}\n\nsome prose'`, assert the set HAS
`foo-bar-custom-anchor` and does NOT have `custom-anchor` or `foo-bar`, and reword the label to
name the GitHub model. Add case H asserting `<a id>` recognition on a synthetic table row
(`'| <a id="0x80180014"></a>`0x80180014` | detail |'` inside a two-row table) yields
`0x80180014` in the set. Add case I asserting the two-adjacent-tags line
`'<a id="exit-kiosk-pin-synchronization"></a><a id="exit-kiosk-pin"></a>'` yields BOTH ids.
Keep `padLabel`, the `[ST] <label> PASS/FAIL -- <detail>` line shape, the
`Self-test: N passed, M failed` tail and `process.exit(stFailed > 0 ? 1 : 0)` byte-identical in
shape — `check-phase-115.mjs:75` pins the bare substring `--self-test` on the sibling file and
Phase 144 will pin this file's equivalent. Commit.

**Step 4 — the one corpus repair.** In `docs/error-codes/01-mdm-enrollment.md`, prepend
`<a id="0x80180014"></a>` to the first cell of the row at `:33` — the row whose first cell is
`0x80180014`. Follow `docs/admin-setup-android/13-aosp-meta-quest.md:62-65`'s placement exactly:
the tag is the very first token after the opening `| `, with **no space between `</a>` and the
cell's existing content**. `0x80180014` occupies two rows (`:33` and `:34`, D-10); the editorial
rule for this phase, applied to every double-row code, is **the numerically first matching row
carries the anchor** — it is the primary definition row. Record that rule in the commit body.
Change nothing else in the file: no prose, no new row, no metadata. Commit separately from Step 3.

**Step 5 — the end-to-end proof, uncommitted.** With Steps 2-4 committed, temporarily widen the
scan in the working tree only: delete `:259` (`if (hubSet.has(relPath)) continue;`) and `:269`
(`if (!hubSet.has(resolvedRel)) continue;`) from `checkInboundLinks`, run
`node scripts/validation/check-nav-hub-links.mjs --verbose`, and capture the output. Then restore
the file with `git checkout -- scripts/validation/check-nav-hub-links.mjs` so the flip is NOT
committed here — it is Plan 06's commit. Expected, measured at plan time under this exact state
(GitHub model on, `<a id>` on, `docs/_templates/` still INCLUDED, inline masking still OFF): the
scan reports 282 files, 6311 relative links, 40 broken file targets and 135 broken anchors before
the Step-4 repair, so after it the total is **173** (40 + 133) and the string
`01-mdm-enrollment.md#0x80180014` appears ZERO times in the failure list. Paste the captured
summary line and the tracer-link grep count into the plan SUMMARY as the tracer evidence.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs --self-test; echo "selftest_exit=$?"; node scripts/validation/check-nav-hub-links.mjs; echo "hub_exit=$?"; node scripts/validation/c17-eee-contract.mjs | tail -2; node scripts/validation/carve-gate.mjs | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-nav-hub-links.mjs --self-test` prints `Self-test: 9 passed, 0 failed` and exits 0.
    - `grep -c '{#' scripts/validation/check-nav-hub-links.mjs` returns 0 outside the self-test block — verified by `sed -n '1,290p' scripts/validation/check-nav-hub-links.mjs | grep -v '^ *//' | grep -c '{#'` returning 0.
    - `grep -c 'a\\s+id' scripts/validation/check-nav-hub-links.mjs` returns 1 (the single new recognition regex).
    - `grep -c 'matchAll' scripts/validation/check-nav-hub-links.mjs` returns 1.
    - `git diff HEAD~2 HEAD~1 -- scripts/validation/check-nav-hub-links.mjs --numstat` shows more removed than added lines inside `computeAnchorSetFromContent` and `stripHeadingText` (RESEARCH Pitfall 1's warning sign).
    - `grep -c '<a id="0x80180014"></a>' docs/error-codes/01-mdm-enrollment.md` returns 1.
    - `grep -c '<a id="0x80180014"></a>|' docs/error-codes/01-mdm-enrollment.md` returns 0 (the tag is not alone in the cell — it prefixes existing content with no intervening space).
    - `node scripts/validation/check-nav-hub-links.mjs` still exits 0 and prints `0 outbound failure(s), 0 inbound failure(s), 0 total` — the hub-scope scan must not regress.
    - `node scripts/validation/c17-eee-contract.mjs` still prints `234 files checked, 0 with violations, 0 total violations` and `#1=0 … #13=0` (D-08 measured this exact insertion class safe on this exact file).
    - `node scripts/validation/carve-gate.mjs` exits 0 with `off-list=0`.
    - The Step-5 dry-run summary line reports a total of **173**, and `grep -c '01-mdm-enrollment.md#0x80180014'` over the dry-run failure list returns 0.
    - After Step 5, `git status --porcelain scripts/validation/check-nav-hub-links.mjs` is empty (the dry-run patch was reverted, not committed).
    - `test -f scripts/validation/check-nav-hub-links.mjs` succeeds and no file named `check-phase-143.mjs` exists anywhere under `scripts/`.
  </acceptance_criteria>
  <done>The single path `docs/error-codes/00-index.md:49 -> ../01-mdm-enrollment.md#0x80180014` resolves end-to-end under a corpus-wide dry-run; the anchor model, the self-test, the ledger row and the corpus repair are all committed; hub-scope scan, c17 and carve-gate are all unregressed.</done>
  <reversibility rating="one-way">Adopting the GitHub anchor model is what makes the 65 checker-green/GitHub-broken links visible; reverting it after the corpus is repaired would re-hide nothing but would silently re-admit the class. The rating STANDS. Its gate is DISCHARGED by the owner ruling recorded in Task 1 (D-38), which chose a wider remedy than either option originally offered and whose zero-regression basis is measured, not asserted — execution therefore proceeds without a blocking checkpoint here.</reversibility>
</task>

<task type="auto">
  <name>Task 3: D-14 steps 2 and 3 — `docs/_templates/` exclusion and inline-code-span masking</name>
  <files>scripts/validation/check-nav-hub-links.mjs</files>
  <read_first>
    - `scripts/validation/check-nav-hub-links.mjs:51-69` `walkMd`, `:85-103` `buildFenceMask`, `:186-198` `extractLinks`
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-RESEARCH.md` — Q2 (no multi-backtick spans exist in this corpus; a single-backtick-run regex is correctly sized)
    - `docs/recipes/03-windows-11-multi-app-kiosk.md:173` — the false positive the inline mask exists to kill
  </read_first>
  <action>
Land D-14's remaining two narrowing steps as two separate commits, in this order. Neither removes
`:259` or `:269` — the corpus flip stays in Plan 06.

**Commit 1 — `docs/_templates/` exclusion.** Exclude any path under `docs/_templates/` from the
corpus walk. Implement it as a single filter on `walkMd`'s results at the call site inside
`checkInboundLinks`, or as an early `continue` on the normalized relative path — whichever keeps
`walkMd` itself a general-purpose walker, since `walkMd` is documented as copied verbatim from
`c17-eee-contract.mjs:68-86`. Use the exact prefix string `docs/_templates/` against the
forward-slash-normalized relative path produced by `relNormalize`, never a platform-native
separator. `docs/` holds 282 `.md` files at HEAD, of which 8 are under `docs/_templates/`, leaving
274. Add a one-line comment naming LINK-02 and the 26 template placeholder targets this excludes.

**Commit 2 — inline-code-span masking.** Mask single-backtick inline code spans on non-fenced lines
BEFORE `extractLinks`'s `/\[([^\]]*)\]\(([^)]+)\)/g` regex runs against them. Apply
`/`[^`\n]*`/g` per line, replacing each matched span with an equal-length run of spaces so that the
1-based line numbers and the intra-line column arithmetic stay exact. Layer it inside
`extractLinks` on lines the fence mask has not already excluded; do not add a second full pass over
the file. Do NOT write a CommonMark backtick-run tokenizer — RESEARCH Q2 measured zero
multi-backtick spans anywhere in `docs/` (the search `` ``[^`\n]*`[^`\n]*`` `` over `docs/` returns
no matches), so a general tokenizer would be unused complexity in a file whose every existing regex
is single-purpose by design.

Add a self-test case J asserting that a line containing
`` `$x = [xml](Get-Content .\kiosk.xml -Raw)` `` inside a single-backtick span yields ZERO extracted
links, and that an ordinary `[text](target.md)` on the same synthetic line outside any span is still
extracted. This is the `docs/recipes/03-windows-11-multi-app-kiosk.md:173` failure mode D-14 names
as the load-bearing evidence for this leg.

Re-run the Step-5 dry-run procedure from Task 2 (temporarily delete `:259`/`:269`, run, restore) to
confirm the measured narrowing.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs --self-test; echo "exit=$?"; node scripts/validation/check-nav-hub-links.mjs; echo "hub_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-nav-hub-links.mjs --self-test` prints `Self-test: 10 passed, 0 failed` and exits 0.
    - `grep -c 'docs/_templates/' scripts/validation/check-nav-hub-links.mjs` returns at least 1 and the literal uses a forward slash.
    - `node scripts/validation/check-nav-hub-links.mjs` still exits 0 with `0 outbound failure(s), 0 inbound failure(s), 0 total`.
    - The dry-run (`:259`/`:269` temporarily removed, then reverted) reports **274** files, **6252** relative links, **13** broken file targets, **130** broken anchors, **143** total.
    - A second dry-run with the inline-mask line reverted reports **14** broken file targets, and the extra failure names `docs/recipes/03-windows-11-multi-app-kiosk.md:173` — proving the inline-mask leg is load-bearing, not decorative.
    - `git status --porcelain scripts/validation/check-nav-hub-links.mjs` is empty after both dry-runs.
    - `git log --oneline -2 -- scripts/validation/check-nav-hub-links.mjs` shows two distinct commits for this task, exclusion before masking.
  </acceptance_criteria>
  <done>The corpus walk excludes `docs/_templates/` (282 -> 274 files) and inline code spans are masked (13 not 14 broken file targets); the dry-run total is 143; the hub-scope scan is unregressed and the flip is still uncommitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus `.md` content -> checker regexes | Untrusted-shaped input: arbitrary Markdown, arbitrary link targets, arbitrary raw HTML crosses into `RegExp` and `path.resolve`. |
| checker anchor model -> corpus remedy decisions | A wrong model silently generates false negatives, which is the defect class D-01 exists to close. |
| `check-nav-hub-links.mjs` -> `check-phase-123.mjs:40,83` | A frozen validator pins this file's path string via `presence()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-05 | Denial of Service | `<a id>` and inline-mask regexes | low | mitigate | Both regexes are linear-time with no nested quantifiers and no ambiguous alternation: `/<a\s+id\s*=\s*"([a-zA-Z0-9_-]+)"\s*>\s*<\/a>/g` and `` /`[^`\n]*`/g `` — the negated character classes cannot backtrack catastrophically. Matches the existing file's regex complexity level. |
| T-143-06 | Tampering | `resolveLinkTarget` path traversal | medium | mitigate | Unchanged in this plan. `path.resolve` is pure string math and the caller's `existsSync` reports not-found; self-test case G (`:376-390`) already asserts `../../../../../../etc/passwd` resolves without throwing and is preserved verbatim. |
| T-143-07 | Spoofing | false-negative anchor resolution | high | mitigate | The GitHub model is a net deletion of special-casing; the rewritten self-test case D asserts the bare `custom-anchor` slug is ABSENT, which is the assertion that fails if a phantom-anchor branch is ever re-added (D-03's false-negative-generator class). |
| T-143-08 | Repudiation | frozen call-site pin | high | mitigate | GOV-02 ledger row records the target-scoped path-literal grep across `scripts/`, `.github/` and `.planning/` before the edit; the file is edited in place and never renamed, so `check-phase-123.mjs:40,83`'s `presence()` pin survives. |
| T-143-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs. `check-nav-hub-links.mjs:17` pins "Node built-ins ONLY -- zero external npm packages"; `143-RESEARCH.md`'s Package Legitimacy Audit records "Not applicable". No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `node scripts/validation/check-nav-hub-links.mjs --self-test` -> `Self-test: 10 passed, 0 failed`, exit 0.
- `node scripts/validation/check-nav-hub-links.mjs` -> `0 outbound failure(s), 0 inbound failure(s), 0 total`, exit 0.
- `node scripts/validation/c17-eee-contract.mjs` -> `234 files checked, 0 with violations, 0 total violations`, all 13 counters 0.
- `node scripts/validation/carve-gate.mjs` -> exit 0, `off-list=0`.
- Dry-run ladder recorded in the SUMMARY: 175 (model only) -> 173 (after the tracer repair) -> 143 (after exclusion + masking).
</verification>

<success_criteria>
The GitHub anchor model is live as a net deletion plus one `matchAll` addition; the self-test grows
to 10 assertions including the model rewrite, `<a id>` recognition, the double-tag line and the
inline-mask case; one real corpus path is proven end-to-end; the dry-run total is 143; hub-scope,
c17 and carve-gate are all unregressed; the corpus flip is not committed.
</success_criteria>

## Artifacts this phase produces

Created or modified by **this plan**:

| Artifact | Kind | Note |
|---|---|---|
| D-38 decision record | governance text in `143-CONTEXT.md` | owner ruling; discharges D-01's one-way gate |
| `143-EVIDENCE.md` (created) | new evidence artifact | blast radius, zero-regression finding, ordering gate, c17 null result, `<a id>` placement ruling |
| `computeAnchorSetFromContent` (GitHub model) | function, rewritten in place | `{#id}` override loop and auto-slug suppression DELETED |
| `stripHeadingText` | function, edited in place | trailing-`{#id}` strip DELETED |
| `<a id>` recognition loop | new code in `computeAnchorSetFromContent` | `/<a\s+id\s*=\s*"([a-zA-Z0-9_-]+)"\s*>\s*<\/a>/g`, `matchAll` |
| `docs/_templates/` walk exclusion | new filter | 282 -> 274 files |
| inline-code-span mask | new pass inside `extractLinks` | `` /`[^`\n]*`/g ``, length-preserving |
| self-test cases D (rewritten), H, I, J | assertions | GitHub model, `<a id>`, double-tag line, inline mask |
| `<a id="0x80180014"></a>` | corpus anchor | `docs/error-codes/01-mdm-enrollment.md:33` |
| `v1.20-GOV-02-LEDGER.md` row (checker) | ledger row | row-55 four-part shape |

<output>
Create `.planning/phases/143-link-coverage-fence-mask-unification/143-02-SUMMARY.md` when done.
</output>
