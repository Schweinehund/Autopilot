---
phase: 143-link-coverage-fence-mask-unification
plan: 09
type: execute
wave: 3
depends_on: ["143-01", "143-02"]
files_modified:
  - docs/l1-runbooks/02-esp-stuck-or-failed.md
  - docs/l1-runbooks/11-macos-setup-assistant-failed.md
  - docs/l1-runbooks/12-macos-profile-not-applied.md
  - docs/l1-runbooks/13-macos-app-not-installed.md
  - docs/l1-runbooks/14-macos-compliance-access-blocked.md
  - docs/l1-runbooks/21-ios-compliance-blocked.md
  - docs/l1-runbooks/25-android-compliance-blocked.md
  - docs/l1-runbooks/27-android-zte-enrollment-failed.md
  - docs/l1-runbooks/28-android-knox-enrollment-failed.md
  - docs/l1-runbooks/29-android-aosp-enrollment-failed.md
  - docs/l1-runbooks/30-linux-enrollment-failed.md
  - docs/l1-runbooks/31-linux-compliance-non-compliant.md
  - docs/l1-runbooks/32-linux-ca-blocking-web-access.md
  - docs/l2-runbooks/00-index.md
  - docs/l2-runbooks/19-android-enrollment-investigation.md
  - docs/l2-runbooks/21-android-compliance-investigation.md
  - docs/l2-runbooks/22-android-knox-investigation.md
  - docs/l2-runbooks/23-android-aosp-investigation.md
  - docs/l2-runbooks/25-linux-agent-investigation.md
  - docs/operations/app-lifecycle/01-windows-win32-msix-scale.md
  - docs/operations/app-lifecycle/04-android-mgp-lifecycle.md
  - docs/operations/co-management/00-overview.md
  - docs/operations/co-management/03-cocmgmt-migration-paths.md
  - docs/operations/drift-migration/01-windows-drift-detection.md
  - docs/operations/patch-management/00-overview.md
  - docs/operations/patch-management/01-windows-wufb-rings.md
  - docs/operations/patch-management/02-macos-update-enforcement.md
  - docs/operations/patch-management/03-ios-update-lifecycle.md
  - docs/operations/patch-management/04-android-patch-delivery.md
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
  - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
autonomous: true
requirements: [LINK-01, LINK-03, LINK-04]
estimate:
  tokens: 84000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 87 `{#id}` overrides across all 29 files are converted to `<a id>` anchors — not the 22 that happen to be link targets, all 87 (D-38, OWNER RULING). `grep -rc '{#' docs/ --include=*.md` sums to 0 afterwards."
    - "The `<a id=\"…\"></a>` goes on its OWN LINE immediately ABOVE the heading, following `docs/admin-setup-android/02-zero-touch-portal.md:38-39`. Measured precedent: 182 of the corpus's 200 live `<a id>` instances are alone on their own line and ZERO are inline within a heading line."
    - "Inline-in-heading placement is barred for a correctness reason, not only a stylistic one: GitHub strips HTML before slugifying a heading but `stripHeadingText` (`check-nav-hub-links.mjs:121-127`) strips only `**` and backticks, so an inline tag would be slugified as ordinary text and the checker's auto-slug would diverge from GitHub's — re-creating the exact model-divergence class D-01 exists to close."
    - "The edit is mechanical and identical everywhere: strip the trailing `{#id}` token from the heading line, emit `<a id=\"id\"></a>` on the line above. All 87 tokens are heading-TRAILING; zero are mid-heading and zero occur outside a heading line, so no second shape exists to handle."
    - "The conversion introduces ZERO regressions. The AFTER set of broken (target-file, fragment) pairs is a strict subset of the BEFORE set — no link anywhere in the corpus resolves *because* a heading's slug is inflated by its own `{#…}` text."
    - "The conversion closes 65 broken anchor links across 26 distinct pairs — exactly the 65 phantom links D-01 identified — so the all-87 conversion IS the complete D-01 remedy, delivered target-side."
    - "After this plan the dry-run corpus total falls from 143 to 78: 13 broken file targets (untouched by this plan) and 65 broken anchors."
    - "`c17-eee-contract.mjs` is unaffected: `234 files checked, 0 with violations, 0 total violations` with all 13 counters at 0, measured by a trial application of all 87 conversions at plan time."
    - "The diff shape is exactly 174 added / 87 removed lines across 29 files — one removed heading line replaced by two lines, everywhere. Any other numstat means a collateral line was touched."
    - "LINK-01 encoding: every `<a id>` value is the `{#id}` token's inner text copied byte-for-byte, including case. Anchor comparison is exact-string `Set.has` with no case folding and no Unicode normalization, so a re-spelled id silently breaks the link the conversion was meant to fix."
    - "LINK-01 adjacency: an `<a id>` value that equals a heading's auto-slug collapses to one member of the anchor `Set` rather than colliding — the anchor set is a `Set`, and the heading now also emits its own clean auto-slug because LINK-01 deleted the suppression branch."
    - "LINK-03 concurrency: the 29 file edits are sequential, single-process writes on the main tree (`use_worktrees:false`), and the checker is a read-only single-process scan — no partial-write or interleaving hazard exists."
    - statement: "Every converted `<a id>` renders as a working anchor on github.com, not only the instances proven via the rendering API."
      verification: backstop
  artifacts:
    - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
  key_links:
    - "`{#id}` heading token -> `<a id>` on the line above -> `computeAnchorSetFromContent`'s `matchAll` loop (added in Plan 02) -> `resolvableAnchorSet` -> the anchor test. This chain only works in that order; the conversion is inert-to-harmful without Plan 02's loop."
    - "Category 10 of `v1.20-CARVE.md` (Plan 01) -> `carve-gate.mjs` globToRegExp -> all 29 files edited here. An off-list path is a hard exit 1 with no warn-and-continue path."
    - "Converted heading lines -> every `file:line` citation in Plans 03/04/05. Line numbers inside the 29 files shift down by one per preceding converted heading in the same file."
  prohibitions:
    - statement: "No new documentation content is authored. Every edit removes a `{#id}` token from an existing heading and adds an anchor line; no sentence, heading, table row or section is added."
      status: flagged-unverified
      verification: "`git diff -- docs/ | grep '^+' | grep -v '^+++' | grep -vcE '^\\+<a id=\"[a-zA-Z0-9_-]+\"></a>$|^\\+#{1,6} '` returns 0 — every added line is either an anchor tag alone or the heading it was stripped from."
    - statement: "No heading's visible TEXT is reworded, re-cased or re-punctuated — only the trailing `{#id}` token and the whitespace before it are removed."
      status: flagged-unverified
      verification: "For each of the 87 pairs, the added heading line equals the removed heading line with `/\\s*\\{#[a-zA-Z0-9_-]+\\}\\s*$/` deleted — asserted by a scripted comparison over `git diff`, not by eye."
    - statement: "No `<a id>` value is normalized, lowercased or otherwise re-spelled relative to the `{#id}` token it replaces."
      status: flagged-unverified
      verification: "The set of 87 `{#id}` inner texts at HEAD equals the set of 87 newly-added `<a id>` values, compared as exact strings."
    - statement: "No file's line endings are changed. `core.autocrlf` is `true` and `.gitattributes` declares only `scripts/pipeline/reference.docx binary`, so a rewrite that normalizes EOLs would show every line of all 29 files as changed."
      status: flagged-unverified
      verification: "`git diff --numstat -- docs/` totals exactly 174 added and 87 removed across exactly 29 files."
    - statement: "Neither `docs/_glossary-macos.md` nor `docs/_glossary-android.md` is touched at all — neither carries a `{#id}` override, and both sit at exactly 90 days against a `>90` freshness test (v1.20-CARVE.md:178-182)."
      status: flagged-unverified
      verification: "`git diff --name-only -- docs/ | grep -c '_glossary-'` returns 0."
    - statement: "No accepted-violation baseline, allowlist, ratchet file or expected-failure list is created for the link checker."
      status: flagged-unverified
      verification: "`git status --porcelain` shows no new file under `scripts/`, and the dry-run residue is reconciled rather than declared acceptable."
---

<objective>
Convert all 87 `{#id}` heading overrides across 29 corpus files to the corpus's own `<a id>`
anchor convention, per D-38 (OWNER RULING). This closes 65 of the 132 broken anchors target-side
in one mechanical edit and leaves exactly one anchor convention corpus-wide.

Purpose: the `{#…}` token renders as visible literal junk text in the heading on github.com —
confirmed first-party against GitHub's own rendering API (`143-RESEARCH.md` Open Question 3) — so
it is a rendering defect in all 87 places, not only in the 22 that happen to be link targets.
Converting all 87 ends the class rather than pruning it. It is also the complete D-01 remedy: the
65 links it fixes are exactly the 65 phantom links D-01 identified, and it delivers them without
baking a doubled slug such as `#cause-a-package-install-failure-cause-a-package-install` into ~57
source links.

Numbering note: this plan is numbered 09 but executes at **wave 3**, between Plan 02 (wave 2) and
Plan 03 (wave 4). It was added after Plans 01-08 were written and committed; renumbering six plan
files and every cross-reference in them would have been far more churn than one out-of-order
number. Execution order is driven by the `wave` field, not by the filename.

Output: 87 converted headings, a measured 143 -> 78 drop on the dry-run ladder, and a zero-`{#id}`
corpus.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-PATTERNS.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
@.planning/milestones/v1.20-CARVE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GOV-02 pre-edit census for the 29-file conversion set, and the pre-flight assertions</name>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md, .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md</files>
  <precondition>Plan 02's `<a id>` recognition loop is committed in `scripts/validation/check-nav-hub-links.mjs`. Assert `grep -c 'matchAll' scripts/validation/check-nav-hub-links.mjs` returns 1 and `grep -c 'a\s+id' scripts/validation/check-nav-hub-links.mjs` returns 1 before proceeding; if either returns 0, HALT — converting against the pre-LINK-01 checker was measured to turn the hub-scope scan red.</precondition>
  <read_first>
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md:19-23,55` — the row schema and the four-part grep shape
    - `.planning/milestones/v1.20-CARVE.md` — the Category 10 block Plan 01 appended, for the 63-path roster this plan's 29 files must all appear in
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the D-38 record, the blast-radius table and the `<a id>` placement ruling written by Plan 02 Task 1
    - `docs/admin-setup-android/02-zero-touch-portal.md:36-40` — the live own-line placement precedent, including the blank line above the anchor
    - `scripts/validation/check-nav-hub-links.mjs:118-166` — post-LINK-01 `stripHeadingText` and `computeAnchorSetFromContent`, to confirm the `<a id>` loop is live and the `{#id}` branches are gone
  </read_first>
  <action>
Append ONE GOV-02 ledger row covering the 29-file conversion set BEFORE any edit (D-33), following
row 55's four-part shape and the row-23 batching precedent for a single mechanical commit set:

1. **Path-literal grep**, target-scoped across `scripts/`, `.github/` and `.planning/`, for the
   token `{#` and for the substring `override` in an anchor context — the question this answers is
   whether any validator or workflow reads a `{#id}` token out of the corpus. Record the hit list
   in full, not a count.
2. **Symbol-scoped grep** for `\{#[a-zA-Z0-9_-]+\}` across `scripts/` — the specific text being
   deleted from the corpus. Note explicitly whether `c17-eee-contract.mjs` or any retrofit script
   matches, since those are the corpus-reading tools most likely to care.
3. **Cross-reference check**, opened and read rather than inferred: confirm no frozen validator
   pins a heading line, a heading slug or a `{#id}` token from any of the 29 files. Name the
   validators checked.
4. **Result** with the concrete `git diff --numstat` totals once the edit lands: expected exactly
   174 added / 87 removed across 29 files.

Record the pre-edit regression baseline in the same row: `c17-eee-contract.mjs` prints
`234 files checked, 0 with violations, 0 total violations` with all thirteen counters at 0;
`check-nav-hub-links.mjs` exits 0 with a total of 0; `carve-gate.mjs` exits 0 with `off-list=0`.

Then run and record three **pre-flight assertions** in `143-EVIDENCE.md` under a
`## D-38 conversion pre-flight` heading. Each is a genuine gate — do not proceed past a failure.

**Pre-flight A — the census still holds.** Re-derive, do not trust: `docs/` (excluding
`docs/_templates/`) holds exactly **87** `{#id}` occurrences across exactly **29** files, all 87
heading-TRAILING, **0** mid-heading and **0** outside a heading line. Record the per-file counts.
If the totals differ from 87/29, stop and reconcile before editing — the roster, the ladder and
Category 10 are all keyed to those two numbers.

**Pre-flight B — Category 10 covers every file.** Every one of the 29 files appears in the
Category 10 block Plan 01 committed. `carve-gate.mjs` hard-exits 1 on an off-list in-scope path
with no warn-and-continue branch, so a single missing path blocks execution mid-conversion.

**Pre-flight C — the ordering gate is satisfied.** `check-nav-hub-links.mjs` carries the `<a id>`
recognition loop and carries no `{#id}` recognition branch. Record the measured consequence of
getting this wrong: with the conversion applied against the *pre*-LINK-01 checker, the hub-scope
scan reports `2 outbound failure(s)` at `docs/quick-ref-l2.md:345,347` pointing at
`operations/patch-management/04-android-patch-delivery.md#deadlines-cutover-dates` and exits 1.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs | tail -2; node scripts/validation/check-nav-hub-links.mjs; echo "cnhl=$?"; node scripts/validation/carve-gate.mjs | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` gains exactly one appended row naming the 29-file conversion set and citing "Plan 143-09, D-38".
    - `git diff -- .planning/milestones/v1.20-GOV-02-LEDGER.md | grep -c '^-[^-]'` returns 0 — append-only, no existing row edited or reordered.
    - The row names all three grep roots and records the hit list for each grep, not a bare count.
    - Pre-flight A is recorded with per-file counts summing to 87 across 29 files, and states `0` mid-heading and `0` non-heading occurrences.
    - Pre-flight B records the set difference between the 29 files and the Category 10 block as empty, in both directions checked.
    - Pre-flight C records `grep -c 'matchAll' scripts/validation/check-nav-hub-links.mjs` returning 1 and the corpus-wide `{#` count in the checker source (outside its self-test block) returning 0.
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 and `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations, 0 total violations` — the pre-edit baseline.
  </acceptance_criteria>
  <done>One append-only ledger row records the target-scoped greps and the pre-edit baseline; all three pre-flight gates pass and are recorded with their measured values, including the red-scan consequence of violating the ordering gate.</done>
</task>

<task type="auto">
  <name>Task 2: Convert all 87 `{#id}` overrides to own-line `&lt;a id&gt;` anchors across 29 files</name>
  <files>docs/l1-runbooks/02-esp-stuck-or-failed.md, docs/l1-runbooks/11-macos-setup-assistant-failed.md, docs/l1-runbooks/12-macos-profile-not-applied.md, docs/l1-runbooks/13-macos-app-not-installed.md, docs/l1-runbooks/14-macos-compliance-access-blocked.md, docs/l1-runbooks/21-ios-compliance-blocked.md, docs/l1-runbooks/25-android-compliance-blocked.md, docs/l1-runbooks/27-android-zte-enrollment-failed.md, docs/l1-runbooks/28-android-knox-enrollment-failed.md, docs/l1-runbooks/29-android-aosp-enrollment-failed.md, docs/l1-runbooks/30-linux-enrollment-failed.md, docs/l1-runbooks/31-linux-compliance-non-compliant.md, docs/l1-runbooks/32-linux-ca-blocking-web-access.md, docs/l2-runbooks/00-index.md, docs/l2-runbooks/19-android-enrollment-investigation.md, docs/l2-runbooks/21-android-compliance-investigation.md, docs/l2-runbooks/22-android-knox-investigation.md, docs/l2-runbooks/23-android-aosp-investigation.md, docs/l2-runbooks/25-linux-agent-investigation.md, docs/operations/app-lifecycle/01-windows-win32-msix-scale.md, docs/operations/app-lifecycle/04-android-mgp-lifecycle.md, docs/operations/co-management/00-overview.md, docs/operations/co-management/03-cocmgmt-migration-paths.md, docs/operations/drift-migration/01-windows-drift-detection.md, docs/operations/patch-management/00-overview.md, docs/operations/patch-management/01-windows-wufb-rings.md, docs/operations/patch-management/02-macos-update-enforcement.md, docs/operations/patch-management/03-ios-update-lifecycle.md, docs/operations/patch-management/04-android-patch-delivery.md</files>
  <read_first>
    - `docs/admin-setup-android/02-zero-touch-portal.md:36-40` — the canonical own-line placement: blank line, then `<a id="prerequisites"></a>`, then `## Prerequisites`
    - `docs/l1-runbooks/30-linux-enrollment-failed.md:60-66` — a representative conversion site carrying three overrides, each preceded by a `---` rule and a blank line
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the placement ruling and the pre-flight census from Task 1
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-PATTERNS.md` — the `<a id>` shape (`<a id="EXACT-FRAGMENT-VERBATIM"></a>`, no attributes beyond `id`, no self-closing form)
  </read_first>
  <action>
Apply the identical mechanical edit at all 87 sites. The shape, everywhere, with no variants:

A heading line matching `^(#{1,6}\s+.*?)\s*\{#([a-zA-Z0-9_-]+)\}\s*$` becomes two lines — first
`<a id="ID"></a>` where `ID` is capture group 2 copied byte-for-byte, then the heading line reduced
to capture group 1. Nothing else on the line changes: the hash run, the space after it, the visible
heading text, its case and its punctuation are all byte-identical to what was there.

**The anchor goes ABOVE the heading, on its own line, with nothing else on that line.** This
follows `docs/admin-setup-android/02-zero-touch-portal.md:38-39`, the dominant corpus shape (182 of
200 live `<a id>` instances are alone on their own line; zero are inline within a heading line).
Do NOT place the tag inline in the heading: `stripHeadingText` (`check-nav-hub-links.mjs:121-127`)
strips only `**` and backticks, so an inline tag would be slugified as ordinary text and the
checker's auto-slug would diverge from GitHub's, re-creating the exact defect class this phase
exists to close.

Do NOT insert a blank line between the anchor and the heading, and do NOT add a blank line above
the anchor. Every one of the 87 heading lines already has a blank line above it; inserting the
anchor directly above the heading inherits that blank line and reproduces the precedent shape
exactly. Adding another blank line would change the document's vertical rhythm across 87 sites for
no gain.

**Preserve line endings byte-for-byte.** `core.autocrlf` is `true` and `.gitattributes` declares
only `scripts/pipeline/reference.docx binary`, so nothing normalizes Markdown EOLs. Detect each
file's existing terminator and re-emit it; a rewrite that normalizes EOLs shows every line of all
29 files as changed and buries the 87 real edits.

**Do not hand-edit 87 sites.** Drive the change with a throwaway script that applies the regex
above per file, and verify the result with the numstat assertion rather than by reading 29 diffs.
Do not commit the script — it is a one-shot transform, not a repo tool, and D-11's rename
prohibition and the no-net-new-file discipline both point away from adding one.

Commit in **four commits grouped by directory family** so a bisect lands on a small diff:
`docs/l1-runbooks/` (13 files, 43 tokens), `docs/l2-runbooks/` (6 files, 24 tokens),
`docs/operations/patch-management/` (5 files, 12 tokens), and the remaining
`docs/operations/` files (5 files, 8 tokens — `app-lifecycle/01` 3, `app-lifecycle/04` 1,
`co-management/00` 1, `co-management/03` 1, `drift-migration/01` 2). The four counts sum to 87
across 29 files; check that sum before committing. Each commit body states the D-38 ruling, the
placement precedent and the per-commit token count.

Change nothing else in any of the 29 files. No prose, no new heading, no table row, no metadata,
no link rewrite — the link repairs are Plans 03, 04 and 05.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs | tail -2; node scripts/validation/check-nav-hub-links.mjs; echo "cnhl=$?"; node scripts/validation/carve-gate.mjs | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc '{#' docs/ --include=*.md | awk -F: '{s+=$2} END {print s}'` returns 0 — no `{#id}` token remains anywhere in the corpus.
    - `git diff --numstat -- docs/ | awk '{a+=$1;r+=$2;f++} END {print f, a, r}'` prints exactly `29 174 87`.
    - `git diff -- docs/ | grep '^+' | grep -v '^+++' | grep -cE '^\+<a id="[a-zA-Z0-9_-]+"></a>$'` returns 87 — every anchor is alone on its line.
    - `git diff -- docs/ | grep '^+' | grep -v '^+++' | grep -vcE '^\+<a id="[a-zA-Z0-9_-]+"></a>$|^\+#{1,6} '` returns 0 — the only other added lines are the stripped headings.
    - `grep -rn '^#\{1,6\} .*<a id=' docs/ --include=*.md | wc -l` returns 0 — no anchor was placed inline in a heading.
    - The set of 87 `{#id}` inner texts at `HEAD~4` equals the set of 87 added `<a id>` values, compared as exact case-sensitive strings — asserted by a scripted set comparison, not by eye.
    - For every one of the 87 pairs, the added heading line equals the removed heading line with `/\s*\{#[a-zA-Z0-9_-]+\}\s*$/` deleted — asserted by a scripted comparison over `git diff`.
    - `git diff --name-only -- docs/ | grep -c '_glossary-'` returns 0 — neither glossary was touched.
    - `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations, 0 total violations` and `#1=0 … #13=0`, byte-identical to Task 1's baseline.
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0 outbound failure(s), 0 inbound failure(s), 0 total` — the hub-scope scan does NOT regress, which it would if the ordering gate had been violated.
    - `node scripts/validation/carve-gate.mjs` exits 0 with `off-list=0`.
    - `git status --porcelain | grep -c '\.mjs$'` returns 0 — the throwaway transform script was not committed.
    - `git log --oneline -4 -- docs/` shows the four family-grouped commits.
  </acceptance_criteria>
  <done>All 87 overrides across 29 files are converted to own-line `&lt;a id&gt;` anchors with byte-identical ids and byte-identical heading text; the corpus holds zero `{#id}` tokens; the diff is exactly 174/87 across 29 files; c17, the hub-scope scan and carve-gate are all unregressed.</done>
  <reversibility rating="one-way">D-38 removes the corpus's Pandoc-attribute anchor convention entirely. Reverting would re-introduce visible junk text in 87 headings and re-break the 65 links this closes. Owner-ruled 2026-08-11; its zero-regression basis is measured, not asserted.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Dry-run checkpoint — corpus total falls 143 -> 78, with the class split re-derived</name>
  <files>.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md</files>
  <read_first>
    - `scripts/validation/check-nav-hub-links.mjs:253-269` — `checkInboundLinks`, specifically `:259` and `:269`
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-02-SUMMARY.md` — the dry-run procedure and the 143 baseline
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the D-38 blast-radius table
  </read_first>
  <action>
Run the temporary-widening dry-run Plan 02 established, to measure this plan's contribution and to
prove the zero-regression claim on the landed state rather than on a plan-time projection. In the
working tree only, delete `:259` (`if (hubSet.has(relPath)) continue;`) and `:269`
(`if (!hubSet.has(resolvedRel)) continue;`) from `checkInboundLinks`, run
`node scripts/validation/check-nav-hub-links.mjs --verbose`, capture the full failure list and the
summary line, then restore with `git checkout -- scripts/validation/check-nav-hub-links.mjs`.

Expected: 274 files, 6252 relative links, **13** broken file targets (untouched by this plan),
**65** broken anchors, **78** total — down from the 143 Plan 02 measured. Measure it; do not
transcribe it. If the figure diverges, reconcile against the pre-flight census before proceeding.

**Prove zero regressions on the landed state.** Compare the captured (target-file, fragment) pair
set against Plan 02's captured 77-pair set: the new set must be a strict SUBSET, with exactly 26
pairs removed and **zero** added. A single added pair means a heading's slug moved in a way the
plan-time measurement missed, and it must be reconciled before Plan 03 builds on this state.

**Append the measured row to the dry-run ladder** in `143-EVIDENCE.md`, and record the re-derived
class split for the surviving 51 pairs / 67 links, which is what Plans 03, 04 and 05 consume:

| class | remedy | pairs | links | plan |
|---|---|---|---|---|
| C — target-side `<a id>` on an existing table row | prepend anchor | 31 | 38 | 04 (2 links already landed by Plan 02's tracer) |
| D — de-anchor, no correct target exists | drop `#fragment` | 12 | 16 | 03 |
| B — source-side rewrite to an existing anchor | rewrite fragment | 8 | 13 | 05 |

State the arithmetic explicitly so a later reader can check it: 31 + 12 + 8 = 51 pairs, and
38 + 16 + 13 = 67 links. Record the full ladder as
**175 -> 173 -> 143 -> 78 -> 49 -> 13 -> 0**, marking 78 as the measured value from this run and
the rest as the projections Plans 03, 04 and 05 must each replace with a measurement.

**Record the line-number shift as a hazard for the downstream plans.** Every `file:line` citation
into one of these 29 files, in any plan or artifact written before this conversion, is now stale by
one line per preceding converted heading in the same file. The two live instances to flag:
`docs/l2-runbooks/25-linux-agent-investigation.md:352` is now `:356`, and
`docs/l2-runbooks/23-android-aosp-investigation.md:362` is now `:367`. State the general rule —
locate every downstream edit by its content, never by a pre-conversion line number.

Do not commit the dry-run patch. Do not delete `:259`/`:269` permanently — that is Plan 06.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; echo "hub_exit=$?"; git status --porcelain scripts/validation/check-nav-hub-links.mjs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The dry-run summary line reports **13** broken file targets, **65** broken anchors and **78** total across 274 files and 6252 relative links.
    - The captured pair set has exactly **51** distinct (target-file, fragment) pairs.
    - The subset proof is recorded: 26 pairs removed versus Plan 02's 77, **0** added. A non-zero added count halts the plan.
    - `git status --porcelain scripts/validation/check-nav-hub-links.mjs | wc -l` returns 0 after the dry-run — the patch was reverted, not committed.
    - `node scripts/validation/check-nav-hub-links.mjs` (unpatched) exits 0 with `0 outbound failure(s), 0 inbound failure(s), 0 total`.
    - `143-EVIDENCE.md`'s dry-run ladder gains a row reading 78, and the class-split table sums to 51 pairs / 67 links with the arithmetic written out.
    - The line-number-shift hazard is recorded with both named instances and the general locate-by-content rule.
  </acceptance_criteria>
  <done>The dry-run measures 78 on the landed state, the pair set is a strict 51-pair subset of Plan 02's 77 with zero additions, the class split for Plans 03/04/05 is re-derived and recorded, and the flip is still uncommitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus heading lines -> GitHub's renderer | 87 heading lines are rewritten; the anchor they carry must resolve in a renderer this repo does not control. |
| corpus heading lines -> `c17-eee-contract.mjs`'s 13 frozen assertions | A heading-line edit crosses a frozen contract that inspects headings directly. |
| corpus edit -> published `.docx` / SharePoint knowledge base | Every corpus edit reaches the Copilot Studio knowledge base via the publish bundle. |
| conversion ordering -> the shipped checker's anchor model | Converting before LINK-01 lands turns a green checker red; the dependency is invisible in the diff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-38 | Tampering | a mechanical 87-site rewrite touching collateral lines | high | mitigate | The diff shape is pinned to exactly `29 174 87` by numstat, every added line must match one of two literal shapes, and a scripted per-pair comparison asserts each added heading equals the removed heading minus the token. Reading 29 diffs by eye is explicitly rejected as the check. |
| T-143-39 | Spoofing | a re-spelled or case-normalized `<a id>` value | high | mitigate | Anchor comparison is exact-string with no case folding; an acceptance criterion asserts the set of 87 pre-edit `{#id}` inner texts equals the set of 87 post-edit `<a id>` values as case-sensitive strings. |
| T-143-40 | Denial of Service | conversion landing before LINK-01's `<a id>` loop | high | mitigate | Task 1 carries a `<precondition>` that HALTS on a missing `matchAll` loop, plus Pre-flight C recording the measured red-scan consequence (2 outbound failures at `docs/quick-ref-l2.md:345,347`). Wave 3 ordering enforces it structurally. |
| T-143-41 | Tampering | EOL normalization burying the 87 real edits | medium | mitigate | `core.autocrlf=true` with no Markdown entry in `.gitattributes` is recorded as the hazard; the numstat assertion fails loudly if any file is rewritten wholesale. |
| T-143-42 | Tampering | c17 frozen-contract regression across 29 files | high | mitigate | A trial application of all 87 conversions was measured at plan time to leave c17 at 234/0/0 with all 13 counters at 0; every task re-runs c17 and asserts byte-identical output against Task 1's recorded baseline. |
| T-143-43 | Repudiation | a stale `file:line` citation silently mis-targeting a downstream edit | medium | mitigate | Task 3 records the line-number-shift hazard with both live instances and a general locate-by-content rule; Plans 03, 04 and 05 each instruct location by first-cell or heading content rather than by line number. |
| T-143-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs; this plan edits Markdown only and its transform script is a throwaway using Node built-ins. `143-RESEARCH.md` Package Legitimacy Audit: "Not applicable". |
</threat_model>

<verification>
- `grep -rc '{#' docs/ --include=*.md` sums to **0**.
- `git diff --numstat -- docs/` totals exactly **29 files / 174 added / 87 removed**.
- Dry-run: 274 files / 6252 links / **13** broken file targets / **65** broken anchors / **78** total / **51** pairs.
- Pair-set subset proof: 26 removed, **0** added versus Plan 02's 77.
- `node scripts/validation/c17-eee-contract.mjs` -> `234 files checked, 0 with violations, 0 total violations`, all 13 counters 0.
- `node scripts/validation/check-nav-hub-links.mjs` -> exit 0, `0 outbound / 0 inbound / 0 total`.
- `node scripts/validation/carve-gate.mjs` -> exit 0, `off-list=0`.
</verification>

<success_criteria>
All 87 `{#id}` overrides across 29 files are converted to own-line `<a id>` anchors with
byte-identical ids and heading text; the corpus holds zero `{#id}` tokens; the diff is exactly
174/87 across 29 files; the dry-run measures 78 with a strict 51-pair subset and zero added pairs;
c17, the hub-scope scan and carve-gate are unregressed; the class split and the line-shift hazard
are recorded for Plans 03, 04 and 05.
</success_criteria>

## Artifacts this phase produces

Created or modified by **this plan**:

| Artifact | Kind | Note |
|---|---|---|
| 87 converted heading anchors across 29 files | corpus edits | `{#id}` stripped, own-line `<a id>` added above |
| Zero-`{#id}` corpus | invariant | one anchor convention corpus-wide |
| Dry-run ladder row: 78 | evidence row | 13 file targets + 65 anchors, measured |
| Post-conversion class split (31 C / 12 D / 8 B) | evidence table | the set Plans 04, 03 and 05 consume |
| Landed-state zero-regression proof | evidence | 26 pairs removed, 0 added, versus Plan 02's 77 |
| Line-number-shift hazard record | evidence | 2 named instances + locate-by-content rule |
| `v1.20-GOV-02-LEDGER.md` row (29-file conversion set) | ledger row | row-per-edit-set, appended |

<output>
Create `.planning/phases/143-link-coverage-fence-mask-unification/143-09-SUMMARY.md` when done.
</output>
