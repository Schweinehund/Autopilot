---
phase: 140-frozen-aware-harness-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: true
requirements: [SWEEP-05, SWEEP-06, SWEEP-07, SWEEP-08]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md SWEEP-05 states that Phase 140 converts v1.4 through v1.18 and that v1.19 converts in Phase 144 alongside the V119 pin (D-13/D-14a)."
    - "REQUIREMENTS.md's traceability row maps SWEEP-05 to both Phase 140 and Phase 144, so SWEEP-05 cannot be flipped to Validated at Phase 140 with v1.19 unconverted (D-14c)."
    - "ROADMAP.md Phase 140 SC#1 carries the same converted range as the amended SWEEP-05 text — the two never diverge (D-14b)."
    - "ROADMAP.md Phase 140 SC#2 states the D-09 evidence path: the apex cannot evidence the budget because check-phase-60.mjs's spawn sites and the seven V-NN-AUDIT-HARNESS blocks are NESTED-guarded, so evidence is a direct wall-clock measurement plus the V-60-23 line."
    - "ROADMAP.md Phase 140 SC#3 records the D-26 reconciliation: the C5 check's overall pass is what 'passes', the per-file suppression of the sentinel record is the mechanism, and the suppression also covers a genuinely malformed review_by value at the pin."
    - "The unconverted C17 live-HEAD leg in v1.15-v1.18 is a named SWEEP-05 limitation inside the requirement text itself, not only a source comment."
    - "A GOV-02 ledger row records the target-scoped pre-edit grep census and the pre-edit regression baseline for every path Plans 02-05 will modify."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
  key_links:
    - "REQUIREMENTS.md SWEEP-05 text <-> ROADMAP.md Phase 140 SC#1 — same converted range, must not diverge."
    - "REQUIREMENTS.md traceability row <-> Phase 144's single close-gate commit that flips requirements to Validated."
    - "Pre-edit baseline in the ledger <-> the post-edit re-run in Plans 02, 03, 04 — the diff is the blast-radius gate."
  prohibitions:
    - ".planning/milestones/v1.20-CARVE.md is not edited in this plan."
    - "No file under scripts/ or .github/ is edited in this plan."
    - "No glossary's last_verified or review_by value is edited anywhere in this phase."
    - "No corpus document under docs/ is edited anywhere in this phase."
    - "check-phase-60.mjs's 60000 ms subprocess timeout is not raised anywhere in this phase."
    - "The amendment commit touches no path outside .planning/REQUIREMENTS.md and .planning/ROADMAP.md."
---

<objective>
Land the recorded scope amendments that authorize this phase's actual conversion range, and
capture the GOV-02 pre-edit grep census and regression baseline that every later plan in this
phase diffs against.

Purpose: D-30 requires the authorizing edit to land as its own commit, alone, before the edit it
authorizes. D-14 names three amendments (not one); the phase direction adds two more
reconciliations (SC#2's file-count claim, SC#3's passing-vs-skipping tension). Without these,
Phase 144's close-gate reads a v1.4-v1.18 delivery against a v1.4-v1.19 criterion and calls it
drift.

Output: amended `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` (one commit, alone), plus
one appended GOV-02 ledger row carrying the pre-edit census and baseline.

**Tracer note:** this plan touches no code layer, so it carries no tracer task. The phase's
tracer is Plan 02 Task 2 (library -> converted harness -> real assertion outcome, end to end).
This plan is the D-30 amendment-first prerequisite that must precede it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/140-frozen-aware-harness-conversion/140-CONTEXT.md
@.planning/phases/140-frozen-aware-harness-conversion/140-RESEARCH.md
@.planning/milestones/v1.20-CARVE.md
@.planning/milestones/v1.20-GOV-02-LEDGER.md
</context>

## Artifacts this phase produces

The complete symbol inventory for Phase 140. Every plan writes against this list; no executor
invents an alternative name.

| Symbol / artifact | Kind | Home | Plan |
|---|---|---|---|
| `readManyAtClose(milestoneTag, relPaths)` | new named export | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `createFrozenCorpusReader(milestoneTag, { extraPaths })` | new named export | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `readAtV14Close` | new convenience export | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `MILESTONE_CLOSE_SHAS.V14` | new registry key (`0b3be9ab`) | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `VUNPINNED` | replacement self-test probe tag (assertion 3) | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `MILESTONE_TAG` | new module constant, one per harness | each `vX.Y-milestone-audit.mjs` | 02, 03, 04 |
| `SIDECAR_PATH` | new module constant, one per harness | each `vX.Y-milestone-audit.mjs` | 02, 03, 04 |
| `FROZEN` | new module constant (the reader instance), one per harness | each `vX.Y-milestone-audit.mjs` | 02, 03, 04 |

No new files, no new CLI flags, no new dependencies. `readFile`, `walkMd` and `parseAllowlist`
keep their existing names and signatures in all 16 harnesses — only their bodies change.

<tasks>

<task type="auto">
  <name>Task 1: Land the five recorded scope amendments as one commit, alone, first</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (SWEEP-05 at line 20; the traceability row `| SWEEP-05 | Phase 140 | Pending |` at line 129; note SWEEP-01/02/03 for the established `**[SUCCESS-CRITERION AMENDMENT, D-NN]**` prefix convention)
    - `.planning/ROADMAP.md` lines 101-117 (Phase 140 section: goal, SC#1-SC#4)
    - `.planning/phases/140-frozen-aware-harness-conversion/140-CONTEXT.md` — D-13, D-14, D-15, D-18, D-26, D-30, and the `<deferred>` entry naming the C17 live-HEAD leg
    - `.planning/phases/140-frozen-aware-harness-conversion/140-RESEARCH.md` — Finding 2's "Family deviation" block (the exact C17 guard being excepted) and Finding 4 (why no CARVE allowlist amendment is required)
    - `.planning/milestones/v1.20-CARVE.md` — the amendment procedure (D-09) and the `.planning/` scope exclusion
  </read_first>
  <action>
Amend five criteria across two files, then commit those two files and nothing else.

`.planning/REQUIREMENTS.md`:
1. SWEEP-05 (line 20) — prefix with `**[SUCCESS-CRITERION AMENDMENT, D-13/D-14]**` matching the
   SWEEP-01/02/03 convention already in this file, and re-word so it states: Phase 140 converts
   the v1.4 through v1.18 harnesses; the v1.19 harness converts in Phase 144, in the same plan
   as HARN-17's `V119` pin, because `MILESTONE_CLOSE_SHAS` carries no `V119` today. Name the
   carve-out explicitly rather than leaving the original range and hoping a reader infers it.
2. In the same amended SWEEP-05 text, record the named limitation: in v1.15 through v1.18 the
   C17 contract-presence `existsSync` guard and the `c17-eee-contract.mjs` subprocess spawn it
   gates stay on live HEAD, so those harnesses are frozen-aware for every check except C17.
   `c17-eee-contract.mjs` is CARVE Category 3 and is owned by Phase 143 (LINK-01..06). This
   limitation lives in the requirement text — a source comment alone does not discharge it.
3. Traceability row (line 129) — change `| SWEEP-05 | Phase 140 | Pending |` to
   `| SWEEP-05 | Phase 140, Phase 144 | Pending |` so SWEEP-05 cannot be flipped to Validated by
   Phase 140 alone (D-14c). Leave the SWEEP-06/07/08 rows unchanged.

`.planning/ROADMAP.md` (Phase 140 section only — do not touch Phases 139, 141, 142, 143, 144):
4. SC#1 (line 108) — carry the same converted range and the same C17 carve-out as the amended
   SWEEP-05 text. The two strings must agree; a reviewer comparing them must find no divergence.
5. SC#2 (line 109) — replace the fabricated live-HEAD scope figure with the measured frozen
   scope (the converted v1.5 harness derives its scope by enumerating at `V15`, and reads fewer
   files than a live-HEAD walk — the honest number is whatever that enumeration yields, recorded
   in Plan 05, not a transcribed constant). Add the D-09 evidence path in the criterion itself:
   both `check-phase-60.mjs` spawn sites and all seven `V-NN-AUDIT-HARNESS` blocks are
   `NESTED`-guarded, so no converted harness ever runs under the apex; the evidence is a direct
   wall-clock measurement of the harness plus the specific `V-60-23` result line from a
   standalone `check-phase-60.mjs --verbose` run, never that validator's overall exit code
   (which stays non-zero until Phase 141 RED-03 for unrelated reasons).
6. SC#3 (line 110) — record the D-26 reconciliation instead of papering over it. State that the
   C5 check's own `pass: true` is what "passing" means here; that the remedy suppresses the
   single sentinel file's violation record via a per-file `continue` and does not mark the check
   skipped; and that the same `continue` additionally suppresses a genuinely malformed
   `review_by` placeholder carried by that template at the pin. Skip-counts-as-pass is the
   masking class D-08 refuses elsewhere, so the mechanism is stated, not implied.

Also update the Phase 140 one-line summary in the `## Phases` list (line 31) so it carries the
amended range too — a stale range there is the same drift in a second place.

Do NOT edit `.planning/milestones/v1.20-CARVE.md`. RESEARCH.md Finding 4 establishes that this
phase's concrete edit list is already covered by CARVE Categories 2 and 4, so no allowlist
amendment applies; and D-09 rule 1 bars combining a CARVE edit with any other path in one commit,
so adding a narrative row there would force a second solo commit for no enforcement gain.

Commit these two files alone, before any code edit in this phase. Suggested subject:
`docs(140-01): record Phase 140 scope amendments (D-13/D-14/D-18/D-26)`.
  </action>
  <acceptance_criteria>
    - `grep -c '| SWEEP-05 | Phase 140, Phase 144 | Pending |' .planning/REQUIREMENTS.md` returns `1`.
    - `grep -v '^#' .planning/ROADMAP.md | grep -c '282'` returns `0`.
    - `grep -v '^#' .planning/REQUIREMENTS.md | grep -c 'SUCCESS-CRITERION AMENDMENT, D-13/D-14'` returns `2` (the pre-existing SWEEP-01 marker plus the new SWEEP-05 marker).
    - `grep -v '^#' .planning/ROADMAP.md | grep -c 'NESTED'` returns at least `1` (SC#2 now carries the D-09 evidence path).
    - `grep -v '^#' .planning/ROADMAP.md | grep -ci 'c17'` returns at least `1` within the Phase 140 section (SC#1 names the unconverted leg) — confirm placement with `sed -n '101,118p' .planning/ROADMAP.md`.
    - `git show --stat --name-only HEAD` for the amendment commit lists exactly two paths: `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
    - `node scripts/validation/check-phase-54.mjs` exits 0 — it is the sole live reader of these two files (per the GOV-02 ledger's first row) and must survive the re-wording.
  </acceptance_criteria>
  <verify>
    <automated>grep -c '| SWEEP-05 | Phase 140, Phase 144 | Pending |' .planning/REQUIREMENTS.md; grep -v '^#' .planning/ROADMAP.md | grep -c '282'; node scripts/validation/check-phase-54.mjs; echo "exit=$?"</automated>
  </verify>
  <done>
    All five amendments are present and mutually consistent; the amendment commit touches exactly
    two paths; `check-phase-54.mjs` still exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: GOV-02 pre-edit grep census and pre-edit regression baseline</name>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <precondition>`git cat-file -e 0b3be9ab^{commit}` exits 0 in this worktree — the V14 candidate commit is reachable, not merely referenced. A shallow or partial clone fails this and invalidates every measurement in Plans 02-05.</precondition>
  <read_first>
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` — the row schema and the append-only discipline; read the Phase 139 rows for the established level of detail
    - `.planning/milestones/v1.20-CARVE.md` — the GOV-02 grep procedure (D-12): target-scoped, path literal AND symbol, never symbol-only
    - `.planning/phases/140-frozen-aware-harness-conversion/140-RESEARCH.md` — Finding 3's grep table (every target, command, and expected hit count) and its 800-char-window analysis
    - `.planning/phases/140-frozen-aware-harness-conversion/140-CONTEXT.md` — D-28 (the hazard class the grep must cover), D-29 (the Category-5 question RESEARCH.md answers negatively)
  </read_first>
  <action>
Run the D-28/Finding-3 census against HEAD, capture the pre-edit regression baseline, and append
exactly one row to the ledger. No source file is edited in this task.

Census (target-scoped per D-12 — path literal AND symbol, both forms, for every target):
- Each of the 15 `.github/workflows/audit-harness-*.yml` `path-match` jobs that grep their
  harness source for the sidecar path literal — confirm each expected literal is the one this
  phase preserves unchanged.
- `check-phase-48.mjs:107` and `check-phase-70.mjs:126` — sidecar path-literal pins.
- `check-phase-66.mjs:76` and `:139` — harness source-text token pins.
- `check-phase-58.mjs:425-441` — the 800-character forward window anchored on the C12 check
  name. Record RESEARCH.md's finding that this phase edits only the Helpers section, strictly
  upstream of the `checks` array, so the anchor and the flag it scans toward shift together and
  the window carries zero risk here.
- `check-phase-73.mjs:96-107` and `check-phase-120.mjs:107-111` — the two literal pins on
  `_lib/frozen-at-close.mjs`'s own text. Record which substrings are pinned required-PRESENT and
  which required-ABSENT, so Plan 02's additive edit is written against them rather than around
  them.
- The negative census that decides the whole risk profile: the "exact array-index line lookup"
  grep across the 13 pin-holding validators. RESEARCH.md reports zero hits; re-run it and record
  the actual number. If it is not zero, stop and escalate — a line-index pin means shifting the
  Helpers section is unsafe and the conversion recipe needs a different shape.

Pre-edit regression baseline (the "before" half of the blast-radius diff every later plan runs):
- `node scripts/validation/check-phase-138.mjs` at top level, never with `CHECK_PHASE_NESTED`
  pre-set — record the PASS/FAIL/SKIPPED tally.
- `CHECK_PHASE_NESTED=1` runs of `check-phase-{48,58,66,70,73,120}.mjs` — record each exit code
  and tally. The nested form is the documented workaround for the pre-existing standalone-RED
  `{48,60-66}` set, which is Phase 141's scope and unrelated to this phase's edits.
- `node scripts/validation/carve-gate.mjs` — record in-scope / on-list / off-list counts.
- `node scripts/validation/_lib/frozen-at-close.mjs --self-test` — record the tally verbatim.
  This is the D-22 evidence Plan 02 must preserve by substitution.
- A bare `node scripts/validation/v1.4-milestone-audit.mjs` and `node
  scripts/validation/v1.5-milestone-audit.mjs`, each run without a pipe so the real exit code is
  observed — these are the two before-states Plans 02 and 03 are measured against.

Write the results into ONE appended ledger row using the existing six-column schema
(File / Grep command / Hit count / Regression gate run / Result / Plan). Append at the end; never
edit or reorder an existing row. Record the File column as the full set of paths Plans 02-05 will
modify, since this row is their shared pre-edit evidence.
  </action>
  <acceptance_criteria>
    - The appended ledger row records a measured hit count for every target named in RESEARCH.md Finding 3's table, and the line-index negative grep is recorded as `0` (or the task escalated instead of proceeding).
    - `node scripts/validation/check-phase-138.mjs` (top level, not pre-nested) reports `93 PASS, 0 FAIL, 0 SKIPPED` and the tally is recorded verbatim in the row.
    - `node scripts/validation/_lib/frozen-at-close.mjs --self-test` prints `6/6 PASS` and exits 0, recorded verbatim as the D-22 pre-edit evidence.
    - `node scripts/validation/carve-gate.mjs` exits 0 with zero off-list paths, recorded.
    - `node scripts/validation/v1.4-milestone-audit.mjs` (no pipe) prints `Summary: 3 passed, 2 failed, 0 skipped` and exits 1, recorded as the v1.4 before-state.
    - `git status --porcelain scripts/` is empty — this task edits no source file.
    - The ledger grows by appended rows only: `git diff HEAD~1 -- .planning/milestones/v1.20-GOV-02-LEDGER.md` shows insertions and zero deletions.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-138.mjs; node scripts/validation/_lib/frozen-at-close.mjs --self-test; node scripts/validation/carve-gate.mjs; git status --porcelain scripts/</automated>
  </verify>
  <done>
    One ledger row carries the full target-scoped census and the pre-edit baseline; the
    line-index negative grep is zero; no source file changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs -> Phase 144 close-gate | The amended criteria are the authorization Phase 144's close audit reads; a wrong or self-contradictory amendment silently authorizes undelivered scope. |
| git object store -> validator process | `check-phase-54.mjs` reads these two files live; every later plan reads git objects at pinned SHAs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-140-01 | Tampering | `.planning/ROADMAP.md` Phase 140 section | medium | mitigate | The amendment commit is restricted to two paths and verified by `git show --name-only`; a wide-file `Write` that clobbers sibling phases is barred — use scoped `Edit` only. |
| T-140-02 | Repudiation | scope amendment provenance | high | mitigate | Every amended criterion carries the `**[SUCCESS-CRITERION AMENDMENT, D-NN]**` marker naming its authorizing decision, matching the SWEEP-01/02/03 convention, so Phase 144 can trace authorization rather than infer it. |
| T-140-03 | Information disclosure | GOV-02 ledger row | low | accept | The ledger records only repo-local paths, grep counts and validator tallies; no secret, token or tenant identifier is in scope. |
| T-140-SC | Tampering | npm/pip/cargo installs | high | accept | This phase runs zero package-manager installs (RESEARCH.md "Standard Stack": no new dependency, everything used is already present or is the system `git`). No supply-chain surface exists to gate. |

ASVS level 1; blocking threshold `high`. T-140-02 is mitigated in-plan; T-140-SC is accepted on a
measured absence of installs, not on convenience.
</threat_model>

<verification>
- The amended SWEEP-05 text and ROADMAP SC#1 state the same converted range; a diff of the two
  strings surfaces no divergence.
- `node scripts/validation/check-phase-54.mjs` exits 0.
- `node scripts/validation/check-phase-138.mjs` reports 93 PASS / 0 FAIL / 0 SKIPPED.
- `node scripts/validation/carve-gate.mjs` exits 0.
- The GOV-02 ledger carries one new row and zero deleted lines.
</verification>

<success_criteria>
- Five amendments landed in one commit touching exactly `.planning/REQUIREMENTS.md` and
  `.planning/ROADMAP.md`.
- The C17 live-HEAD leg is a named limitation in the requirement text.
- One GOV-02 ledger row carries the pre-edit census (including the zero-hit line-index negative
  grep) and the pre-edit regression baseline.
- No file under `scripts/`, `.github/` or `docs/` changed.
</success_criteria>

<output>
Create `.planning/phases/140-frozen-aware-harness-conversion/140-01-SUMMARY.md` when done
</output>
