---
phase: 140-frozen-aware-harness-conversion
plan: 04
type: execute
wave: 4
depends_on: ["140-03"]
files_modified:
  - scripts/validation/v1.11-milestone-audit.mjs
  - scripts/validation/v1.12-milestone-audit.mjs
  - scripts/validation/v1.13-milestone-audit.mjs
  - scripts/validation/v1.14-milestone-audit.mjs
  - scripts/validation/v1.15-milestone-audit.mjs
  - scripts/validation/v1.16-milestone-audit.mjs
  - scripts/validation/v1.17-milestone-audit.mjs
  - scripts/validation/v1.18-milestone-audit.mjs
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: true
requirements: [SWEEP-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All sixteen in-scope harnesses (v1.4 through v1.18) read their corpus and their sidecar at their own close SHA — the phase's conversion range is complete after this plan."
    - "`node scripts/validation/v1.14-milestone-audit.mjs` prints `Summary: 15 passed, 0 failed, 0 skipped`, and the v1.16 and v1.18 harnesses each print `Summary: 16 passed, 0 failed, 0 skipped` — the already-green harnesses stay green after losing their live-HEAD coupling."
    - "In v1.15 through v1.18 exactly one bare membership guard remains per file: the C17 contract-presence guard that gates the live `c17-eee-contract.mjs` subprocess spawn, deliberately left on live HEAD because that validator is owned by Phase 143."
    - "Each of v1.15 through v1.18 still reports its C17 check with the same result as its recorded pre-conversion baseline — the excepted leg was not perturbed by converting the four legs around it."
    - "The four remaining Family C harnesses (v1.11 through v1.14) no longer report the 60-day-vs-90-day mismatch class in their output."
    - "The unconverted C17 leg is marked in the source with a comment naming the phase that owns it, and the same limitation is already recorded in the amended requirement text from Plan 01 — the code comment supplements the requirement record, it does not substitute for it."
  artifacts:
    - scripts/validation/v1.11-milestone-audit.mjs
    - scripts/validation/v1.12-milestone-audit.mjs
    - scripts/validation/v1.13-milestone-audit.mjs
    - scripts/validation/v1.14-milestone-audit.mjs
    - scripts/validation/v1.15-milestone-audit.mjs
    - scripts/validation/v1.16-milestone-audit.mjs
    - scripts/validation/v1.17-milestone-audit.mjs
    - scripts/validation/v1.18-milestone-audit.mjs
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
  key_links:
    - "Family D's fifth guard -> the `c17-eee-contract.mjs` spawn -> Phase 143's LINK-01..06 scope — converting it here would pre-empt another phase's ownership of the live corpus that leg reads."
    - "`createFrozenCorpusReader` (Plan 02) -> each converted harness's `FROZEN` constant."
    - "Each `SIDECAR_PATH` literal -> its workflow `path-match` job -> the `harness-run` job that depends on it."
  prohibitions:
    - "The C17 contract-presence guard in v1.15 through v1.18 is not converted."
    - "The c17-eee-contract.mjs subprocess spawn in v1.15 through v1.18 is not converted or removed."
    - "c17-eee-contract.mjs is not edited."
    - "No corpus document under docs/ is edited."
    - "No glossary's last_verified or review_by value is edited."
    - "No JSON audit-allowlist sidecar file is edited."
    - "No SIDECAR_PATH string literal is renamed, re-cased or re-quoted."
    - "No malformed-JSON degrade branch in any parseAllowlist is converted to a throw."
    - "No freshness comparison operator or threshold constant in any check body is changed."
    - "No check is removed, reordered, renumbered or converted to informational."
    - "scripts/validation/_lib/frozen-at-close.mjs is not edited in this plan."
    - "v1.19-milestone-audit.mjs is not edited."
    - "check-phase-60.mjs is not edited and its 60000 ms subprocess timeout is not raised."
---

<objective>
Complete the conversion range: the four remaining Family C harnesses and the four Family D
harnesses, the latter with their C17 leg deliberately left on live HEAD.

Purpose: v1.11-v1.13 carry the same red freshness pair as the rest of Family C. v1.14-v1.18 are
already green at live HEAD — but only because Phase 133 re-pinned their sidecars to live
coordinates, not because the corpus stopped drifting. Converting them removes the live-HEAD
coupling that made that re-pin necessary in the first place; it does not repair today's red, and
the SUMMARY must state that cause correctly.

Output: eight converted harnesses, sixteen of sixteen for the phase, with the C17 exception
implemented and documented, and one GOV-02 ledger row.

**Tracer note:** the phase's tracer is Plan 02 Task 2. Both conversion tasks here are horizontal
expansion off that proven slice; the one genuinely new element is Family D's documented exception,
which is a subtraction from the recipe, not a new mechanism.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-frozen-aware-harness-conversion/140-CONTEXT.md
@.planning/phases/140-frozen-aware-harness-conversion/140-RESEARCH.md
@.planning/phases/140-frozen-aware-harness-conversion/140-PATTERNS.md
@.planning/phases/140-frozen-aware-harness-conversion/140-03-SUMMARY.md
@.planning/milestones/v1.20-GOV-02-LEDGER.md
</context>

## Artifacts this phase produces

| Symbol / artifact | Kind | Home | Plan |
|---|---|---|---|
| `readManyAtClose(milestoneTag, relPaths)` | new named export | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `createFrozenCorpusReader(milestoneTag, { extraPaths })` | new named export | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `readAtV14Close` | new convenience export | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `MILESTONE_CLOSE_SHAS.V14` | new registry key (`0b3be9ab`) | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `VUNPINNED` | replacement self-test probe tag (assertion 3) | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `MILESTONE_TAG` | new module constant, one per harness | each `vX.Y-milestone-audit.mjs` | 02, 03, 04 |
| `SIDECAR_PATH` | new module constant, one per harness | each `vX.Y-milestone-audit.mjs` | 02, 03, 04 |
| `FROZEN` | new module constant (the reader instance), one per harness | each `vX.Y-milestone-audit.mjs` | 02, 03, 04 |

No new files, no new CLI flags, no new dependencies. `readFile`, `walkMd` and `parseAllowlist`
keep their existing names and signatures in all 16 harnesses — only their bodies change.

<tasks>

<task type="auto">
  <name>Task 1: Convert v1.11 through v1.14 (Family C, remaining four)</name>
  <files>scripts/validation/v1.11-milestone-audit.mjs, scripts/validation/v1.12-milestone-audit.mjs, scripts/validation/v1.13-milestone-audit.mjs, scripts/validation/v1.14-milestone-audit.mjs</files>
  <read_first>
    - `.planning/phases/140-frozen-aware-harness-conversion/140-RESEARCH.md` — Finding 2's Family C row (`readFile` `:50`, `walkMd` `:57`, `parseAllowlist` `:78`, sidecar `:79`, 4 guards) and the tag mapping (`V111` `919b23b`, `V112` `12f2c7b`, `V113` `ba24f1a`, `V114` `7d922a7`)
    - `scripts/validation/v1.10-milestone-audit.mjs` (post-conversion, from Plan 03) — the immediate reference implementation for this family
    - `.planning/phases/140-frozen-aware-harness-conversion/140-03-SUMMARY.md` — the recorded per-file before/after transitions for the first five Family C files
    - `.planning/phases/140-frozen-aware-harness-conversion/140-CONTEXT.md` — D-16 (why v1.14 is green today, stated correctly) and D-11 (per-harness sidecar alignment verification)
  </read_first>
  <action>
Apply the same four-item recipe already landed on five Family C siblings. Four membership guards
per file, all four converted; `MILESTONE_TAG` and `SIDECAR_PATH` are the only per-file values,
with each sidecar literal read out of its own file rather than constructed.

v1.14 is the first already-green harness in the phase. Its conversion is not a repair — it is the
removal of live-HEAD coupling. State the cause correctly in the SUMMARY: v1.14 is green today
because Phase 133's TOOL-04 re-pinned its sidecar to live coordinates, not because the corpus has
stayed still; the corpus has drifted heavily. Its measured post-conversion result is firm and is
an acceptance criterion below — a green harness going red after conversion is a real finding and
must stop the task, not be smoothed over.

Run each harness after converting it and record its baseline and post-conversion summary lines.

Commit the four files as one atom. Suggested subject:
`refactor(140-04): frozen-aware conversion for v1.11-v1.14 harnesses`.
  </action>
  <acceptance_criteria>
    - For each of the four files, `grep -c 'existsSync(join(process.cwd()' <file>` returns `0`.
    - For each of the four files, `grep -c 'createFrozenCorpusReader' <file>` returns `2`.
    - `node scripts/validation/v1.14-milestone-audit.mjs` (no pipe) prints `Summary: 15 passed, 0 failed, 0 skipped` and exits 0.
    - For each of v1.11, v1.12, v1.13, `node <harness> 2>&1 | grep -c '(>60)'` returns `0`.
    - Each of the four harnesses runs to completion without an unhandled throw, with baseline and post-conversion summary lines recorded verbatim in the SUMMARY.
    - `git diff HEAD~1 -- scripts/validation/v1.11-milestone-audit.mjs scripts/validation/v1.12-milestone-audit.mjs scripts/validation/v1.13-milestone-audit.mjs scripts/validation/v1.14-milestone-audit.mjs | grep -c "audit-allowlist.json"` returns `0`.
    - `node scripts/validation/carve-gate.mjs` exits 0 with zero off-list paths.
  </acceptance_criteria>
  <verify>
    <automated>for h in 1.11 1.12 1.13 1.14; do echo "=== v$h ==="; node scripts/validation/v$h-milestone-audit.mjs; echo "exit=$?"; grep -c 'existsSync(join(process.cwd()' scripts/validation/v$h-milestone-audit.mjs; done; node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>
    Four harnesses read corpus and sidecar frozen with zero remaining bare membership guards;
    v1.14 reports 15 passed / 0 failed / 0 skipped; every summary line recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert v1.15 through v1.18 (Family D) with the C17 leg left live</name>
  <files>scripts/validation/v1.15-milestone-audit.mjs, scripts/validation/v1.16-milestone-audit.mjs, scripts/validation/v1.17-milestone-audit.mjs, scripts/validation/v1.18-milestone-audit.mjs</files>
  <read_first>
    - `.planning/phases/140-frozen-aware-harness-conversion/140-RESEARCH.md` — Finding 2's Family D row (`readFile` `:53`, `walkMd` `:60`, `parseAllowlist` `:81`, sidecar `:82`, 5 guards of which 4 convert), the "Family deviation" block with the exact excepted guard and its required comment, the tag mapping (`V115` `29a3599`, `V116` `3dd2512`, `V117` `b56bba5`, `V118` `7af8a147`), and Pitfall 3
    - `scripts/validation/v1.15-milestone-audit.mjs` lines 45-120 (helper section), its four convertible guard sites at `:103`, `:116`, `:164`, `:215`, and the excepted guard plus its subprocess spawn at `:827` and `:831`
    - `.planning/phases/140-frozen-aware-harness-conversion/140-CONTEXT.md` — the `<deferred>` entry naming the C17 live-HEAD leg as Phase 143's scope
    - `.planning/phases/140-frozen-aware-harness-conversion/140-01-SUMMARY.md` — the amended requirement text where this limitation is already recorded
  </read_first>
  <action>
Convert the four Family D harnesses on their first four chokepoints only, and leave the fifth
alone deliberately and visibly.

Convert: `readFile`, `walkMd`, `parseAllowlist`, and the four membership guards that gate
path-set membership. Same recipe, same shape, per-file `MILESTONE_TAG` and `SIDECAR_PATH`.

Do NOT convert the fifth guard — the contract-presence check that gates the
`c17-eee-contract.mjs` subprocess spawn — and do not convert that spawn. Converting it would make
the C17 check report a hard failure on every frozen tree that predates that validator's own
authoring, which is a false failure about a file the check never asserts on, and it would pre-empt
Phase 143's ownership of the live corpus that leg reads. Leave the `node:fs` import in place for
exactly this reason.

Mark the exception in the source directly above the excepted guard: a short comment naming this
phase, naming Phase 143 as the owner of `c17-eee-contract.mjs`, and stating that the leg is
intentionally live-HEAD. The comment supplements the requirement-level record landed in Plan 01
Task 1; it does not replace it — a limitation that lives only in a code comment is invisible to
the close audit.

v1.16 and v1.18 have measured post-conversion results and are acceptance criteria below. v1.15
and v1.17 do not; record their outcomes rather than predicting them, and treat any regression from
their recorded green baselines as a stop-and-investigate, not a thing to patch around.

Commit the four files as one atom. Suggested subject:
`refactor(140-04): frozen-aware conversion for v1.15-v1.18 harnesses, C17 leg excepted`.
  </action>
  <acceptance_criteria>
    - For each of the four files, `grep -c 'existsSync(join(process.cwd()' <file>` returns exactly `1` — the four membership guards converted, the C17 contract-presence guard intact.
    - For each of the four files, `grep -c 'c17-eee-contract' <file>` returns the same count as before the edit (compare against `git show HEAD~1:<file> | grep -c 'c17-eee-contract'`).
    - For each of the four files, `grep -c 'createFrozenCorpusReader' <file>` returns `2`.
    - `node scripts/validation/v1.16-milestone-audit.mjs` and `node scripts/validation/v1.18-milestone-audit.mjs` (no pipe) each print `Summary: 16 passed, 0 failed, 0 skipped` and exit 0.
    - For each of the four harnesses, the C17 check line in `node <harness> --verbose` output shows the same result as the recorded pre-conversion baseline for that harness.
    - `node scripts/validation/v1.15-milestone-audit.mjs` and `node scripts/validation/v1.17-milestone-audit.mjs` each run to completion with their summary lines recorded verbatim and no regression against their recorded green baselines.
    - `git diff HEAD~1 -- scripts/validation/v1.15-milestone-audit.mjs scripts/validation/v1.16-milestone-audit.mjs scripts/validation/v1.17-milestone-audit.mjs scripts/validation/v1.18-milestone-audit.mjs | grep -c "audit-allowlist.json"` returns `0`.
    - `node scripts/validation/carve-gate.mjs` exits 0 with zero off-list paths.
  </acceptance_criteria>
  <verify>
    <automated>for h in 1.15 1.16 1.17 1.18; do echo "=== v$h ==="; node scripts/validation/v$h-milestone-audit.mjs; echo "exit=$?"; grep -c 'existsSync(join(process.cwd()' scripts/validation/v$h-milestone-audit.mjs; done; node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>
    Four Family D harnesses read corpus and sidecar frozen on every leg except C17; exactly one
    bare membership guard remains per file and it is the documented exception; v1.16 and v1.18
    report 16 passed / 0 failed / 0 skipped.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sixteen-harness completeness gate and GOV-02 ledger row</name>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` — the row schema and the Phase 140 rows appended by Plans 01, 02 and 03
    - `.planning/phases/140-frozen-aware-harness-conversion/140-01-SUMMARY.md` — the recorded pre-edit gate figures
    - `.planning/phases/140-frozen-aware-harness-conversion/140-CONTEXT.md` — D-13 (the range is v1.4 through v1.18; v1.19 is Phase 144's)
  </read_first>
  <action>
Assert the conversion range is exactly complete — no harness missed, none converted out of scope —
then run the gate and append one row.

Completeness: enumerate every `scripts/validation/v*.*-milestone-audit.mjs` file and check each
for the reader import. Sixteen must carry it; exactly one, the v1.19 harness, must not. A
seventeenth converted file means v1.19 was converted here and Phase 144's plan has been
pre-empted; fifteen means a harness was skipped.

Gate: top-level `check-phase-138.mjs`; `CHECK_PHASE_NESTED=1` runs of the six pin-holding
validators; `carve-gate.mjs`. Compare against the recorded pre-edit figures.

Append one row naming all eight paths modified in this plan, citing the Plan 01 census, and
recording per-harness before/after summary lines plus the Family D guard counts that prove the
C17 exception was implemented rather than forgotten.
  </action>
  <acceptance_criteria>
    - `grep -lc 'createFrozenCorpusReader' scripts/validation/v*.*-milestone-audit.mjs | wc -l` returns `16`.
    - `grep -c 'createFrozenCorpusReader' scripts/validation/v1.19-milestone-audit.mjs` returns `0`.
    - `node scripts/validation/check-phase-138.mjs` (top level, never pre-nested) reports `93 PASS, 0 FAIL, 0 SKIPPED`.
    - `CHECK_PHASE_NESTED=1` runs of `check-phase-48`, `-58`, `-66`, `-70`, `-73`, `-120` each exit 0 with tallies recorded.
    - `node scripts/validation/carve-gate.mjs` exits 0 with zero off-list paths.
    - `git diff HEAD~1 --numstat -- .planning/milestones/v1.20-GOV-02-LEDGER.md` shows insertions and zero deletions.
  </acceptance_criteria>
  <verify>
    <automated>grep -l 'createFrozenCorpusReader' scripts/validation/v*.*-milestone-audit.mjs | wc -l; grep -c 'createFrozenCorpusReader' scripts/validation/v1.19-milestone-audit.mjs; node scripts/validation/check-phase-138.mjs; for n in 48 58 66 70 73 120; do CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-$n.mjs > /dev/null 2>&1; echo "$n exit=$?"; done; node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>
    Exactly sixteen harnesses carry the frozen reader and the v1.19 harness does not; the apex,
    all six pin-holders and the CARVE gate are unchanged; one ledger row appended.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git object store -> harness process | Eight more harnesses begin reading assertion inputs from pinned git objects. |
| converted harness legs -> the one unconverted C17 leg | A single file now reads from two sources; the boundary between them must be explicit or a future reader will "finish the job" and break Phase 143. |
| harness source text -> CI `path-match` jobs and chain validators | Substring pins on these files gate their CI runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-140-13 | Tampering | the C17 contract-presence guard in Family D | high | mitigate | Post-conversion guard count asserted to be exactly one per file, plus an unchanged `c17-eee-contract` reference count against the parent commit, plus an in-source comment naming the owning phase. Converting it produces a false failure that looks like a corpus regression. |
| T-140-14 | Spoofing | an already-green harness going green for a different reason | high | mitigate | v1.14, v1.16 and v1.18 carry exact measured post-conversion tallies as acceptance criteria; a changed tally stops the task. The SUMMARY must state the correct cause of their pre-existing green rather than the plausible-sounding wrong one. |
| T-140-15 | Tampering | `SIDECAR_PATH` literal in each converted harness | high | mitigate | Literal read out of the file, never reconstructed; negative diff grep asserts no allowlist path string was rewritten in any converted file. |
| T-140-16 | Repudiation | scope creep into the v1.19 harness | medium | mitigate | Completeness gate asserts exactly sixteen converted files and zero reader references in the v1.19 harness. |
| T-140-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs in this plan; no new dependency is added. No supply-chain surface exists to gate. |

ASVS level 1; blocking threshold `high`. All three `high` code threats are mitigated in-plan;
T-140-SC is accepted on a measured absence of installs.
</threat_model>

<verification>
- Sixteen harnesses carry the frozen reader; the v1.19 harness carries none.
- v1.14 reports 15 passed / 0 failed / 0 skipped; v1.16 and v1.18 each report 16 passed / 0 failed / 0 skipped.
- Family D files each retain exactly one bare membership guard, the documented C17 exception.
- Every converted harness returns zero hits for the 60-day-vs-90-day mismatch string.
- Apex reports 93 PASS / 0 FAIL / 0 SKIPPED; all six nested pin-holders exit 0; `carve-gate.mjs` exits 0.
- One GOV-02 ledger row appended, zero lines deleted.
</verification>

<success_criteria>
- The phase's conversion range is complete at sixteen harnesses, v1.4 through v1.18.
- The C17 live-HEAD leg is excepted in code and recorded in the requirement text.
- The three measured already-green harnesses stay green with their exact tallies.
- Zero corpus edits, zero sidecar edits, zero threshold edits, zero v1.19 edits.
</success_criteria>

<output>
Create `.planning/phases/140-frozen-aware-harness-conversion/140-04-SUMMARY.md` when done
</output>
