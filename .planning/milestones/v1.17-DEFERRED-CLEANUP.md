# v1.17 Deferred Cleanup — v1.18+ Backlog

**Authored:** 2026-07-11 (Phase 128 Plan 128-07 close-gate — canonical `.planning/milestones/` artifact)
**Milestone:** v1.17 (Docs-Library .docx Publish-Bundle Pipeline (SharePoint / Copilot Upload Automation))
**Status:** FINALIZED at Phase 128 Plan 128-07 close-gate `{phase_128_close_SHA}` (2026-07-11).

**Authoring rationale:** v1.17 inherits the standalone deferred-cleanup artifact convention (Path-A from `.planning/milestones/v1.16-DEFERRED-CLEANUP.md`). Per the "**do NOT mask via deletion**" doctrine, all carried open items are PRESERVED verbatim and the new v1.17 deferrals are ADDED. Any item resolved during v1.17 is DROPPED here and recorded Closed in `.planning/milestones/v1.17-MILESTONE-AUDIT.md` (the "do NOT mask via deletion" doctrine governs *open* items only).

**Scope:** This document enumerates deferred **requirements** and **open technical items** only. Working-tree cruft (`.agents/`, `.obsidian/`, stray `graphify-out/`, `docs/graphify-out/`, `skills-lock.json`) is out of scope — deferred to `/gsd-complete-milestone`.

**Trigger-to-sweep:** Each section carries explicit criteria for when v1.18+ should pick up the work.

**Not deferred (resolved at close):** the `check-phase-124.mjs` archival-drift fix (`readAtV116Close` conversion, commit `76d147b5`) is NOT a deferred item — it was resolved AT PLAN TIME (pre-push, per STATE's plan-time-remediation discipline; NO value-masking, NO frozen-surface edit, `CHAIN_SKIP` empty). Honestly recorded in `v1.17-MILESTONE-AUDIT.md`.

**Resolved during v1.17 (dropped from the carried v1.16 list, recorded Closed):** `V116-PIN-DEFERRAL` (closed by HARN-08 — the mandatory back-anchor pin now lands, see below), `DEFER-125-06-A` (`docs/_glossary-android.md` stale `phase_46_wave2_retrofit` key → fixed by HYG-02/Phase 126, along with 4 sibling files carrying the identical defect). Both are CLOSED, not carried forward. See `v1.17-MILESTONE-AUDIT.md` for the closing detail.

---

# Part A — v1.17 New Deferrals

## V117-PIN-DEFERRAL: Freeze the v1.17 Corpus (Back-Anchor Invariant — v1.18's Path-A Job)

**Scope:** The `scripts/validation/_lib/frozen-at-close.mjs` `MILESTONE_CLOSE_SHAS` map does NOT contain a `V117` entry after this close, and MUST NOT — a pin referencing the v1.17 close-gate SHA would create ordering circularity (the close-gate cannot reference its own not-yet-existing SHA; the back-anchor invariant requires every pin to reference a PAST close SHA). Freezing the v1.17 corpus (adding `V117` + `readAtV117Close`) is the **successor milestone's** job: v1.18's Atom 2 adds the `V117` pin the same way v1.17's Atom 2a added `V116`.

**Why it is NOT a v1.17 item:** back-anchor invariant (mirrors `T-125-07-CIRCULAR` from the v1.16 precedent). The close-gate SHA is recorded ONLY as a grep-recoverable placeholder in the MILESTONE-AUDIT, never as a pin.

**Where it belongs when resolved:** v1.18 Atom 2 — append `V117: '<v1.17 close-gate SHA>'` (recover via `git log --all --grep="MILESTONE-AUDIT" --grep="MILESTONE CLOSE" --all-match -1 --format="%H %s"`, POSITIVELY CONFIRMING the returned commit's SUBJECT LINE carries both tokens — v1.17's own close-gate authoring surfaced a false-positive risk in this recovery command when an intermediate commit's BODY TEXT quotes the grep string; see `v1.17-MILESTONE-AUDIT.md`'s "Post-close hand-off" section) + `readAtV117Close` export, following the V18..V116 single-entry pattern.

**Resolution trigger (v1.18):** the v1.18 harness lineage bump (16th Path-A milestone). MANDATORY at that close.

**Estimated effort:** included in the v1.18 Atom 2 (additive; < 1 plan on its own).

**Status:** DEFERRED to v1.18 (structural — the successor milestone pins the predecessor). NEW at v1.17 close.

---

## FROZEN-AWARE-ADOPTION-SWEEP-01 (v1.17 update — now with concrete evidence)

**Scope:** Broad-cohort frozen-aware conversion of the REMAINING predecessor content-assertion validators that still read live HEAD instead of a frozen milestone-close snapshot — proactive, not reactive-only.

**v1.17 contribution (new concrete evidence):** the Class-B predecessor cascade RED found at Plan 128-05's 3-axis re-audit is now empirically attributed to exactly this class: `regenerate-supervision-pins.mjs --self-test` AND all v1.4-v1.16 predecessor milestone-audit harnesses (via their frozen `-audit-allowlist.json` sidecars) read `docs/_glossary-android.md` / `docs/reference/android-capability-matrix.md` / the other 3 HYG-02-touched files at LIVE HEAD, and drift `-1` line when HYG-02 deletes one frontmatter line per file. This is proven **NESTED-masked** for the apex-128 close-gate purpose (`CHECK_PHASE_NESTED=1` causes their CHAIN/AUDIT sub-checks to skip, so apex-128 itself stays green) but is **standalone-CI-RED** for every one of the 10 chain-running predecessor workflows (v1.7-v1.16) plus the base workflow's harness-replay job. See `deferred-items.md` DEFER-128-A for the full root-cause chain.

**Why NOT fixed during v1.17:** editing any of the 14 frozen v1.4-v1.16 `-audit-allowlist.json` sidecars (or the `v1.4-v1.16-milestone-audit.mjs` harnesses themselves, or `regenerate-supervision-pins.mjs`'s BASELINE_9 array) is the forbidden frozen-surface-edit anti-pattern (D-00a) — a frozen milestone-audit validates its OWN close-SHA corpus, not the evolved v1.17 corpus. D-128-C's mandate this milestone was scoped strictly to the 8 LIVE (non-frozen) `check-phase-NN.mjs` validators that read a HYG-touched doc directly, not to any frozen predecessor sidecar/harness.

**Where it belongs when resolved (v1.18):** either (a) repoint each pre-v1.17 frozen `-audit-allowlist.json` sidecar's affected `{file,line}` pins `-1` to account for the HYG-02 shift (a targeted, scoped re-pin of ALREADY-frozen surfaces — a genuine exception-worthy edit, since the alternative is permanent standalone-CI-RED on 10+ workflows forever), or (b) convert `v1.4-v1.16-milestone-audit.mjs`'s own reads + `regenerate-supervision-pins.mjs`'s self-test classifier to frozen-aware reads of their OWN milestone's close snapshot rather than live HEAD. Either approach requires an explicit v1.18 discuss-phase decision — this is NOT resolved here.

**Resolution trigger (v1.18):** low-to-medium priority — the condition is NESTED-masked/non-blocking for apex-close purposes, but the standalone predecessor-workflow noise (11 workflows red on every future PR touching `check-phase-*.mjs`) is an operational hygiene cost that compounds with every milestone.

**Estimated effort:** 2-4h (mirrors the prior estimate; the fix shape is now concretely scoped by this milestone's evidence).

**Status:** CARRIED from v1.8→v1.16 (superset of `LATENT-NON-FROZEN-AWARE-CONTENT-ASSERTION-01`). UPDATED at v1.17 close with concrete root-cause evidence (HYG-02 `-1` shift, NESTED-masked, standalone-CI-RED). DEFERRED to v1.18.

---

## Deployment — SharePoint Content-Approval (if Draft must gate retrieval)

**Scope:** v1.15/v1.16 PIPE-02 empirically confirmed `Status: Draft` is a LABEL only — Draft docs ARE indexed and retrieved. v1.17 did not re-exercise this probe (no PIPE-02/grounding leg attaches to this close per D-128-A). If Draft must actually GATE retrieval (not just label), enabling + maintaining SharePoint content approval on the indexed library is required (tenant/ops config, not a doc change).

**Where it belongs when resolved:** a deployment-hardening task (operator responsibility).

**Resolution trigger:** when a genuine retrieval gate on Draft is required.

**Status:** CARRIED from v1.15/v1.16 (unresolved — deployment lever, not a doc/harness fix). Not re-exercised at v1.17 close (harness-only close, no grounding leg).

---

## Infra — Azure AI Search Structured Index

**Scope:** Upgrade from the native SharePoint knowledge source to an Azure AI Search + SharePoint indexer to make `platform`/`doc_type` filterable metadata (the D1 normalized labels are already the right term set).

**Resolution trigger:** when metadata-filtered retrieval is required at scale.

**Status:** CARRIED from v1.14/v1.15/v1.16 (infra escalation). Unresolved at v1.17 close.

---

## Deployment — Auto-Upload to SharePoint (Graph API)

**Scope:** Push the publish bundle (`docs-library-v1.17.zip`) to the SharePoint document library via Graph API (needs app registration + `Sites.ReadWrite.All` + secret handling). v1.17 explicitly built the bundle only — owner performs the upload manually (owner-confirmed scope, 2026-07-10).

**Where it belongs when resolved:** a future milestone adding the Graph/SharePoint auth surface (app registration, secret handling, `Sites.ReadWrite.All` scope).

**Resolution trigger:** if the manual per-milestone upload becomes an operational bottleneck.

**Status:** NEW at v1.17 close (the milestone's own build-only scope boundary, explicitly out of scope per REQUIREMENTS.md Out-of-Scope table). DEFERRED to v1.18+.

---

# Part B — Carried-Forward Open Items from v1.16 (PRESERVED verbatim per "do NOT mask via deletion")

*The following are preserved from `.planning/milestones/v1.16-DEFERRED-CLEANUP.md`. They remain unresolved / indefinite at v1.17 close. Source-of-truth, per-site root-cause detail, and trigger gates are unchanged; see the v1.16 artifact for full detail.*

## WINDOWS-CLONE-DEEPNEST-TIMEOUT-01: Chain-Apex Cold-Clone Deep-Nesting Subprocess Cost on Windows (CARRIED — apex deepened to [48..127] at Phase 128, but completed cleanly this milestone)

Running any chain validator from a cold fresh-clone on Windows can exhibit an O(n²) cascade of cold Node subprocess spawns. **Carried forward from v1.16; the apex chain is now `[48..127]` (+3 deeper than v1.16's `[48..124]`).** **Honest v1.17 note:** unlike v1.16 (where the apex genuinely stalled on Windows and required Linux-GHA-sole-authoritative fallback for the chain apex), **v1.17's apex `check-phase-128.mjs` [48..127] completed cleanly on BOTH Windows axes this milestone** (Axis 1 fresh clone AND Axis 3 independent reproduction, 82/0/1 each, no stall observed). This is recorded as a corroborating result, NOT as resolution of the underlying item — Linux GHA remains sole-authoritative per D-03, and the chain will deepen further at v1.18, where the O(n²) cost may resurface. **Remediation options (v1.18+):** cache-warm clone / raise per-subprocess timeout / cache chain-validator subprocess results within a single apex invocation (the O(n²) durable fix — increasingly attractive as the chain deepens past [48..127]). **Status:** CARRIED FORWARD (unresolved in principle; empirically non-blocking this milestone). DEFERRED to v1.18+.

## ACCEPTED-STANDALONE-CI-RED: Frozen Predecessor Integrity Workflows Go RED Standalone on the Evolved Corpus (CARRIED — NOW SPANS v1.4-v1.16)

The frozen `audit-harness-v1.4..v1.16-integrity.yml` workflows trigger on any PR touching `check-phase-*.mjs` and FAIL standalone against the evolved v1.17 corpus (frozen harnesses pin their own close-SHA corpus). Editing frozen workflows/harnesses is barred by D-00a; a frozen milestone-audit validates its OWN close-SHA corpus, not future evolved corpus. **v1.17 re-confirmation:** every predecessor `Run vX.Y milestone audit harness` job (v1.7…v1.16, plus the base workflow's harness-replay job) failed against the HYG-02-shifted corpus on the close PR (run `29165955062`'s sibling cascade) — architecturally expected, not a regression; v1.17 chain correctness is guaranteed by the NEW `audit-harness-v1.17-integrity.yml` (all-green run `29165955062`). **Status:** ACCEPTED, path-filter-scoped condition. CARRIED from v1.16/v1.15/v1.14, now one milestone deeper. Not a blocker.

## O(n²)-CHAIN-RUNNER-REMEDIATION-01: Subprocess-Caching Rewrite for WINDOWS-CLONE-DEEPNEST

The durable fix for `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01`: cache chain-validator subprocess results within a single apex invocation to avoid redundant sub-spawns at depth ≥ 2. Mitigation (Linux GHA sole-authoritative for the chain apex) makes this non-blocking. **Status:** CARRIED from v1.16/v1.15/v1.14. DEFERRED to v1.18+. Estimated 2–4h (durable O(n²) fix); increasingly attractive as the chain deepens past [48..127].

## LATENT-NON-FROZEN-AWARE-CONTENT-ASSERTION-01: Predecessor Content Assertions Not Yet Frozen-Aware (EXERCISED AGAIN at v1.17 — 9 more converted)

Predecessor `check-phase-NN.mjs` validators carrying DIRECT content assertions reading LIVE HEAD rather than their own milestone close-SHA can drift on a broad-corpus milestone. **v1.17 note:** the D-128-C plan-time-scoped conversion (Plan 128-03, Atom 2a) converted 8 MORE (`check-phase-49/58/59/62/101/109/118/121` → `readAtV116Close`) that would have broken on the HYG-02 corpus edit, PLUS the pre-push adversarial-review fix converted a 9th (`check-phase-124` → `readAtV116Close`, a DIFFERENT class — archival drift, not HYG-02). The D-128-C plan-time scoping run (Plan 128-01) proactively identified 8 of these 9 BEFORE the close-gate; the 9th was outside D-128-C's HYG-doc-scoped mandate by design and was instead caught by a dedicated pre-push adversarial review — a further process refinement beyond v1.16's own "pre-scope at plan time" lesson (v1.16 pre-scoped its 3 Class-A conversions too, but v1.17 additionally caught an out-of-mandate-class blocker before the push, not just the in-mandate class). **Status:** CARRIED from v1.16/v1.15/v1.14 (exercised again at v1.17, both classes; process lesson reaffirmed and extended). DEFERRED to v1.18+ chain-validator retrospective. Estimated 2–4h. See `FROZEN-AWARE-ADOPTION-SWEEP-01` above for the superset broad-sweep item.

## DEFER-119-A: `regenerate-supervision-pins.mjs --self-test` Advisory-RED (Frozen-Fixture vs Live-Corpus Divergence — ACCEPTED, re-confirmed at v1.17 with a NEW concrete root cause)

`node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 1 on the current tree. The `--self-test` classifier scans the LIVE Android corpus and compares its Tier-1 new-pin set against the FROZEN `scripts/validation/v1.7-audit-allowlist.json` fixture (immutable Phase-43/v1.7 baseline). **v1.17 re-confirmation:** HYG-02's `-1` frontmatter-line-deletion shift is the SAME concrete root cause now attributed to this advisory-RED (see `FROZEN-AWARE-ADOPTION-SWEEP-01` above) — the live classifier emits line numbers `-1` from the frozen fixture's expectation on the 4 shifted files. **Why it is NOT a v1.17 failure (accepted condition, unchanged):** the CI job that runs this helper is advisory, not a hard gate; it reported success (informational annotation only) on the authoritative Axis-2 run `29165955062`. Editing the `v1.7-audit-allowlist.json` fixture is the forbidden anti-pattern; relaxing the classifier is forbidden. A frozen fixture validates its OWN v1.7-era corpus, not the evolved v1.17 corpus. **Status:** ACCEPTED advisory-RED condition. CARRIED from v1.16/v1.15/v1.14. Not a blocker. Resolution trigger unchanged: only if the advisory-RED becomes operationally noisy, or folds into the `FROZEN-AWARE-ADOPTION-SWEEP-01` v1.18 remediation.

---

## MTPSSO-01/02/03 + PSSO-FUT-03: Multi-Tenant Platform SSO (registration models, CA/compliance scoping, L2 troubleshooting, admin-setup notation)

Cross-tenant macOS Platform Credential registration, multi-tenant Conditional Access / compliance scoping, multi-tenant PSSO L2 troubleshooting, and the admin-setup counterpart notation. Net-new architectural scope. **Status:** DEFERRED to v1.18+. *(Carried v1.10 → … → v1.17.)*

## KRBFUT-01/02: On-Prem-AD-Only Kerberos Realm Deep-Dive + Azure Files Cloud-Kerberos GA

On-prem-AD-only Kerberos realm configuration; full Azure Files Cloud-Kerberos macOS coverage on GA promotion. **Status:** DEFERRED to v1.18+. *(Carried v1.10 → … → v1.17.)*

## CI-3: Managed Apple ID → Managed Apple Account Corpus Rename (45 occurrences / 16 files)

Corpus-wide rename pending Microsoft Intune adopting the rebrand portal-side. **Status:** DEFERRED to v1.18+. *(Carried v1.6 → … → v1.17; byte-unchanged hazard + motivated scope-padding — reconfirmed DEFERRED.)*

## AOSP-Wired 802.1X + Cloud PKI / Intune Certificate Connector Deep-Dive

Android AOSP-specific wired 802.1X depth; full Cloud PKI / Intune Certificate Connector cert-delivery deep-dive for 802.1X. **Status:** DEFERRED to v1.18+ (carried from v1.14 Out-of-Scope).

---

# Part C — Carried-Forward v1.8 Deferred Items (PRESERVED verbatim per "do NOT mask via deletion")

*Preserved from `.planning/milestones/v1.16-DEFERRED-CLEANUP.md` Part B/C. They remain unresolved / indefinite at v1.17 close.*

- **ARCHIVE-UPSTREAM-01:** Upstream PR to get-shit-done-cc for the `extractOneLinerFromBody` regex fix (vendored fallback canonical until upstream lands). DEFERRED. *(Carried v1.8 → … → v1.17.)*
- **v1.1 line 164 `Edit N --` token-class:** `check-phase-71.mjs` `PLACEHOLDER_TOKENS` extension surface; reactive pickup. Documented for awareness. *(Carried v1.8 → … → v1.17.)*
- **HELPER-SPAWN-STDERR-01:** residual slice-budget tuning at the 3 helper-spawn stderr sites (core resolved by v1.14 TOOL-03). DEFERRED to v1.18+ chain-validator retrospective. *(Carried v1.8 → … → v1.17.)*
- **Multi-tenant Apple Business surfaces:** cross-tenant device transfer / multi-org-account. DEFERRED indefinitely. *(Carried v1.8 → … → v1.17.)*
- **Apple Business Device API documentation:** on Apple publishing the developer landing page. DEFERRED to v1.18+. *(Carried v1.8 → … → v1.17.)*
- **Per-OU Conference Room Display deep-dive:** TRACKER ONLY (no acute demand v1.6–v1.17). *(Carried v1.8 → … → v1.17.)*
- **Account Holder lockout dedicated recovery runbook:** CONDITIONAL (promote on 5+ escalations/quarter). *(Carried v1.8 → … → v1.17.)*
- **Apple School Manager (ASM) education-specific surfaces:** DEFERRED to v1.18+ (outside enterprise scope). *(Carried v1.8 → … → v1.17.)*

*(EXEC-FAIL-DETAIL-EXTRACTION-01 was closed at v1.14 by TOOL-01 — recorded there; not re-listed as open.)*

---

## Cross-References

- **v1.17 milestone audit:** `.planning/milestones/v1.17-MILESTONE-AUDIT.md`
- **v1.16 predecessor (single-source-of-truth for carried items):** `.planning/milestones/v1.16-DEFERRED-CLEANUP.md`
- **Phase 128 3-axis re-audit results (Axis-2 GREEN + byte-gate EMPTY + cascade Class-B scan):** `.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-05-AUDIT-RESULTS.md`
- **Phase 128 emergent remediation slot (confirmed NO-OP):** `.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-06-SUMMARY.md`
- **Phase 128 pre-push adversarial-review finding + fix (`check-phase-124.mjs` archival drift, `76d147b5`):** `.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/deferred-items.md`
- **Phase 128 close-gate verification:** `.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-VERIFICATION.md`
- **v1.17 requirements (10/10 Validated):** `.planning/REQUIREMENTS.md`

---

*Authored 2026-07-11 at Phase 128 Plan 128-07 close-gate (`{phase_128_close_SHA}`). Part A: 4 NEW v1.17 deferrals (V117-pin deferral + FROZEN-AWARE-ADOPTION-SWEEP-01 concrete-evidence update + SharePoint content-approval re-carry + auto-upload-to-SharePoint) — 2 CLOSED (V116-PIN-DEFERRAL by HARN-08, DEFER-125-06-A by HYG-02). Part B: WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 (now [48..127], empirically non-blocking this milestone) + accepted-standalone-CI-RED (now spans v1.4-v1.16) + O(n²) chain-runner + latent-content-assertion class (exercised again at v1.17, both in-mandate and out-of-mandate classes) + DEFER-119-A advisory-RED (new concrete root cause) + MTPSSO/PSSO-FUT-03 + KRBFUT-01/02 + CI-3 + AOSP-wired-802.1X + Cloud-PKI, all carried from v1.16/v1.15/v1.14. Part C: carried v1.8 items PRESERVED. Resolved-at-v1.17 items (V116-PIN-DEFERRAL, DEFER-125-06-A) DROPPED — recorded Closed in v1.17-MILESTONE-AUDIT.md. Working-tree cruft cleanup deferred to /gsd-complete-milestone. v1.18+ routing confirmed for all open items.*
