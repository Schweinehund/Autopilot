# v1.20 Deferred Cleanup — In-Milestone Routing (Phase 142)

**Authored:** 2026-08-10 (Phase 142 Plan 142-01, Commit 1 of D-19's three-commit sequence)
**Milestone:** v1.20 (Frozen-Aware CI Remediation & Chain-Validator Debt Closure)
**Status:** OPEN — routing document, not a milestone-close artifact.

**Authoring rationale.** Unlike the predecessor `vX.Y-DEFERRED-CLEANUP.md` files (each authored at its
milestone's close-gate), this document is authored **mid-milestone**, at Phase 142's Wave 1, because
`142-CONTEXT.md`'s `<deferred>` block names twelve items discovered during Phase 142's own
`/grill-me` + `/adversarial-review` scoping that this phase's requirements (RED-04/05/06/07, NEST-01)
do not cover. Per Phase 141's own precedent, "an unrouted deferral is itself the defect" — this file
gives every one of the twelve items a named destination now, rather than letting them evaporate
between this phase's SUMMARY and the eventual v1.20 close-gate's own deferred-cleanup authoring.

**Placement:** Milestone-level (`.planning/milestones/`), not phase-dir-level — chosen for the same
reason `v1.20-GOV-02-LEDGER.md` is: it avoids any `resolveArchivedPhasePath(..., ['v1.20-phases'])`
call site and means no later phase in this milestone mutates a completed phase's own artifact.
Verified safe at plan time: every validator reference to a deferred-cleanup file names a specific
predecessor file by path (e.g. `v1.19-DEFERRED-CLEANUP.md:252`'s non-collapse rule, cited by
`REQUIREMENTS.md:69`); none globs the `.planning/milestones/` directory or counts files in it, so
adding this file mid-milestone introduces no new validator surface.

**Relationship to the eventual v1.20 close-gate deferred-cleanup document.** This file is NOT a
substitute for the `v1.20-DEFERRED-CLEANUP.md` (or `v1.21-DEFERRED-CLEANUP.md`-named successor) that
Phase 144's close-gate will author following the standard per-milestone convention — that document
will additionally carry whatever new items surface in Phases 143-144 and will supersede this one's
routing where the two overlap (items 7 and 8 below, both routed to Phase 144 CONTEXT, are expected to
be consumed and closed before that document is authored). This file exists so the twelve items are
not silently lost in the interval between Phase 142 and that eventual close-gate document.

## Routed Items

| # | Item | Evidence | Destination | Disposition |
| --- | --- | --- | --- | --- |
| 1 | `check-phase-31 --quick` silently loses a check | `activeChecks` filters `type!=='external'` with no skip-accounting, so `-31 --quick` reports 29 of 30 checks; the same file's `:137` "inherited verbatim from Phase 30, lines 303-337" provenance comment is provably false (968 B/15 lines vs 1,307 B/35 lines) | A future tooling requirement — no v1.20 requirement covers it | DEFERRED, unowned |
| 2 | Seven of `check-phase-31`'s eight pass-worded `detail` strings print a positive string on FAIL (`:47,53,56,59,74,83,95,119`) — live output reads e.g. `V-31-25: … FAIL` / `-> enum present` | Confirmed by direct read this session; the eighth (`V-31-25`) is fixed opportunistically in Plan 04 because that check is already being rewritten for RED-05 | Same future tooling requirement as item 1 — the remaining seven detail strings | DEFERRED, unowned (1 of 8 closes incidentally in Plan 04) |
| 3 | `V-31-22` has no zero-file guard — passes vacuously if all six L1 files vanish | `bad = []` → PASS with zero files checked; `check-phase-30.mjs:123`'s zero-file guard shape (`if (files.length === 0) return { pass: false, … }`) is the fix `check-phase-31.mjs:110` is missing | Same future tooling requirement as items 1-2 | DEFERRED, unowned |
| 4 | `check-phase-30.mjs:45`'s unescaped-dot filename regex (`'-ios-.*\.md$'` compiles with a bare `.` before `md$`, matching more than intended) — the sibling `check-phase-31.mjs:27` has the correct escaped `'\\.md\$'` form | Confirmed by direct read this session | Same future tooling requirement as items 1-3; explicitly NOT fixed in Plan 03 — it is a second, distinct regex-construction defect in the same file and out of RED-04's amended scope (RED-04 covers only V-30-01/V-30-02/V-30-10) | DEFERRED, unowned, explicitly out of Plan 03 scope |
| 5 | `carve-gate.mjs:445` calls `main()` at module scope with no import guard, and `main()` calls `process.exit` — importing the file's exported helpers from another script kills the importing process | Confirmed by direct read this session | A future tooling requirement; this phase avoids the hazard by invoking the gate only through its CLI (`node scripts/validation/carve-gate.mjs`), never by importing its exports | DEFERRED, unowned, avoided not fixed |
| 6 | Dead `required:` metadata on check objects — never read by either runner at runtime | Zero occurrences in `check-phase-30.mjs`, two in `check-phase-31.mjs` (`V-31-29` and `V-31-30`) | Same future tooling requirement as items 1-4 | DEFERRED, unowned |
| 7 | The 13-workflow `pull_request.paths` filter cascade — every edit this phase (and Plans 03/04/05) makes fans out to a 13-run CI cascade on Phase 144's close PR | `[MEASURED]` 13 of 16 workflows carry `check-phase-*.mjs` path filters; Phase 141's own 41-of-41-green fan-out is the precedent for how to read a comparable cascade | **Phase 144 CONTEXT, as a named verification input** — it lands on HARN-19's 3-axis terminal-close bar, with Phase 141's 41/41 result as the precedent for interpretation | ROUTED to Phase 144 |
| 8 | The consequence of D-25's span correction (`[48..138]` → `[48..143]`, landed in this plan on `REQUIREMENTS.md`/`PROJECT.md`) — Phase 144 must author `check-phase-144.mjs` with `CHAIN_END = 143` and 96 chain-array entries, not the pre-correction 90 | This plan's own D-25 edits to `REQUIREMENTS.md` (NEST-01, HARN-18) and `PROJECT.md` (Pillar E, footer) | **Phase 144 CONTEXT, as a named input** — HARN-18's own authoring task | ROUTED to Phase 144 |
| 9 | `check-phase-66` measured at 386,235 ms is over the 300,000 ms per-subprocess timeout cap, as a non-peer child of `check-phase-67` | `141-EVIDENCE.md:288`'s already-recorded 60→66 series: 5,755 / 12,547 / 24,576 / 46,129 / 94,794 / 194,372 / **386,235 ms**; per D-31 this figure is NOT re-measured here — `check-phase-64` (the item Phase 141 originally handed forward) is separately confirmed NOT a crossing candidate at 91,710 ms, 30.6% of the 300,000 ms cap, and is recorded here for completeness, not as an open item | Unowned — needs its own future requirement; not addressed by RED-04/05/06/07 or NEST-01 | DEFERRED, unowned |
| 10 | The non-nested standalone-chain wall-clock cost is still unowned after this phase | NEST-01 (this phase) covers only the apex's own cold-clone curve; `v1.19-DEFERRED-CLEANUP.md:252`'s explicit non-collapse rule forbids re-collapsing the non-nested standalone cost into the same item | Unowned — needs its own future requirement | DEFERRED, unowned |
| 11 | `audit-harness-v1.7-integrity.yml`'s stale `~102s` Windows wall-clock reference — carried unfixed from Phase 141 | Recorded, not re-measured, per this phase's own "do not re-derive" discipline for figures already on record | **Phase 144's workflow work** — hygiene, folded into the terminal-close cluster | ROUTED to Phase 144 |
| 12 | Seven stale `.claude/worktrees/agent-*/` copies of `check-phase-31.mjs` carrying the pre-fix path — a working-tree hazard, not a repo defect (untracked) | `[MEASURED]` 7 files verified present this session under `.claude/worktrees/` | Recorded here as a verification hazard only — Plans 03/04 scope their post-edit greps to `scripts/` and explicitly exclude `.claude/`, so this hazard does not produce a false "defect still present" reading in this phase's own verification | RECORDED, not routed — mitigated by scoped greps in Plans 03/04 |
| 13 | Phase 144 wiring of `check-phase-143.mjs` — the `check-nav-hub-links.mjs` spawn contract handed off by Phase 143 (D-23, OWNER-RATIFIED) rather than authored in-phase | `[MEASURED]` `143-EVIDENCE.md:975-982`: the committed corpus scan runs in **~738-754 ms** (n=3, quiesced worktree, HEAD `db576147`). Phase 144 is terminal and blocked on Phases 139-143 (`.planning/STATE.md`'s dependency chain); if the wiring turns the checker red at that point, no phase remains to absorb it and the milestone bar ("both accepted-red dispositions **deleted**") is what would give. At its measured sub-second runtime the checker can ride HARN-19's 3-axis terminal re-audit (`.planning/ROADMAP.md:281`) as a spot-check **regardless** of whether the `check-phase-143.mjs` wiring lands, decoupling the close from the wiring's success (D-26, `143-CONTEXT.md:389-395`) | `.planning/phases/143-link-coverage-fence-mask-unification/143-NEEDLE-SPEC.md` — the six-section spec Phase 144 implements verbatim; HARN-19's 3-axis terminal re-audit as the independent spot-check path | ROUTED to Phase 144, decoupled from milestone close via the HARN-19 spot-check (D-26) |
| 14 | **`FENCE-AXIS-02`** — two deferred fence-unification axes LINK-05 does not cover | `[MEASURED, 143-CONTEXT.md:302-310 (D-18)]` (a) fence **length**: `scripts/pipeline/convert.ps1:110` closes a fence via `TrimStart().StartsWith($fenceChar)` where `$fenceChar` holds only the 3-character opener prefix, so a 4-backtick opener is closed by a 3-backtick line, while the 14 JS sites track `fenceLen` and require `>=`; (b) mask **scope**: `scripts/pipeline/retrofit-mermaid-structural.mjs:422` and `scripts/pipeline/retrofit-nav-hub.mjs:406` mask the whole file including front matter, while `scripts/validation/c17-eee-contract.mjs:150` and the six first-H1 scanners run over post-front-matter body lines. LINK-05's subject is indentation only, not length or scope | A future tooling requirement — no v1.20 requirement covers either axis | DEFERRED, named carry-forward (D-18) |
| 15 | Out-of-census divergent fence-handling sites (D-20), confirmed present and untouched by LINK-05's unification | `[MEASURED, 143-EVIDENCE.md:1303-1327]` four classes, all confirmed unedited this phase: `scripts/validation/check-phase-66.mjs:274` (a live fence tracker in a **frozen chain validator**, CARVE Category 5, runs in every apex — bare `^\s*` toggle, no `fenceChar`/`fenceLen` tracking); **12** unanchored strip sites (`.replace(/```[\s\S]*?```/g,'')`-shaped) across `scripts/validation/check-phase-54.mjs` through `check-phase-59.mjs`; `scripts/validation/carve-gate.mjs:65` (allowlist-fence parser, tolerates an indented opener but requires a column-0 closer, with a hard throw on parse failure); column-0-only detectors at `scripts/validation/check-phase-135.mjs:128`, `check-phase-136.mjs:216`, `check-phase-55.mjs:345` and `check-phase-122.mjs:44` | Same `FENCE-AXIS-02` token (adjacent scope) — a future tooling requirement | DEFERRED, named carry-forward (D-20) |

## Cross-References

- `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` — the `<deferred>` block (source of all twelve items, owner-ratified 2026-08-10)
- `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-RESEARCH.md` — "The V-31-29 tension" (item 2's eighth-detail-string closure context) and the D-13 classifier-repair detail
- `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` — `PRE-CHAIN-VALIDATOR-RED-30/31` and `ACCEPTED-SCOPED-RED` (the predecessor entries this phase's RED-04/05/06 discharge; items 1-6 and 9-10 above are residue this phase's fixes do not cover)
- `.planning/milestones/v1.20-GOV-02-LEDGER.md` — this plan's own appended row, evidencing the frozen-surface edits that produced items 1-6 as discovered-but-out-of-scope findings
- `.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md` — D-18 (`FENCE-AXIS-02` naming), D-20 (out-of-census residual), D-23/D-26 (the check-phase-143.mjs hand-off and its decoupling mitigation), source of items 13-15
- `.planning/phases/143-link-coverage-fence-mask-unification/143-NEEDLE-SPEC.md` — the six-section spec item 13 routes to
- `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — item 13's runtime measurement (Plan 06) and item 15's out-of-census confirmation (Plan 07), plus the `## Enforcement` section naming the trigger-blindness gap that survives item 13's hand-off

## Provenance Footer

*Authored 2026-08-10 at Phase 142 Plan 142-01, Task 3, as Commit 1 of D-19's fixed three-commit
sequence. All twelve items transcribed from `142-CONTEXT.md`'s `<deferred>` block, each given a named
destination per that block's own owner-ratified routing rule ("every item below gets a row in one
named destination file, cited by the plan"). Two items (7, 8) route forward to Phase 144 CONTEXT as
named verification/authoring inputs; one item (11) routes to Phase 144's workflow hygiene work; one
item (12) is recorded as a verification hazard already mitigated by scoped greps rather than routed
forward; the remaining eight (1-6, 9-10) are recorded as unowned, pending a future tooling
requirement or milestone that explicitly picks them up.*

*Extended 2026-08-11 at Phase 143 Plan 08, Task 2. Items 13-15 route Phase 143's own deliverable
hand-off (D-23/D-26 OWNER-RATIFIED) and two named carry-forward tokens (D-18/D-20) through this
same instrument rather than through SUMMARY prose, per D-26's explicit instruction. Item 13
decouples Phase 144's terminal-close bar from `check-phase-143.mjs`'s wiring outcome via the
sub-second-runtime HARN-19 spot-check path.*

---

# v1.20 Terminal Close — Absorbed and Appended (Phase 144 Plan 144-11)

**Appended:** 2026-08-18 (Phase 144 Plan 144-11 close-gate). This document now carries the standing
milestone-close status the header above described as pending — the fifteen routed rows above are
preserved **byte-identical**, per D-27: they are pinned as VERIFIED evidence in `142-VERIFICATION.md`,
`143-VERIFICATION.md`, and `142-01-SUMMARY.md`, and this is the same phase that derives its own new
validators from those measured actuals. Nothing above this line is edited, reordered, or deleted.

**Consumption of the fifteen pre-existing rows, recorded here rather than edited into their own
cells (preserving byte-identity):** rows 7, 8, 11, and 13 above were ROUTED to Phase 144 as
verification/authoring inputs and are now CONSUMED by this phase's own work — row 7 (the
13-workflow `pull_request.paths` cascade concern) discharged by the full 17-workflow direct
`workflow_dispatch`, all green, a strictly stronger result than the routed concern anticipated (see
`v1.20-MILESTONE-AUDIT.md`'s Cascade Scan section); row 8 (the `[48..138]` → `[48..143]` span
correction) built directly into `check-phase-144.mjs`'s `CHAIN_END = 143` (HARN-18, Plan 144-07); row
11 (the two-site stale `~102s` figure) corrected in both workflows in the same commit as the 17th
workflow's own authoring (Plan 144-08); row 13 (the `check-phase-143.mjs` wiring hand-off) implemented
verbatim against `143-NEEDLE-SPEC.md` (Plan 144-05). Row 12 (the stale `.claude/worktrees/` copies)
stays RECORDED-not-routed, unchanged — no v1.20 phase further routes it. **Rows 1-6, 9, 10, 14 and 15
are the TEN genuinely unowned tooling items** (not twelve — the earlier-circulated "12" figure
double-counted row 12, which was never routed, and omitted that rows 7/8/11/13 are routed and now
consumed). They carry forward unchanged into Part B below.

# Part A — v1.20 New Deferrals (Phase 144 Close)

## C17-FROZEN-AWARE-RESIDUE-V15-V19: The C17 Contract-Presence Leg Stays Live-HEAD Across All Six C17-Bearing Harnesses (V15-V20) [SUCCESS-CRITERION AMENDMENT, D-08]

**Scope:** `SWEEP-05`'s frozen-aware conversion (Phase 140 for v1.4-v1.18, Phase 144 Plan 02 for the
seventeenth and final harness, v1.19) converts every milestone-audit harness's CORPUS reads to its own
close-SHA snapshot, but leaves the C17 contract-presence `existsSync` guard, and the subprocess spawn
of `c17-eee-contract.mjs` it gates, on LIVE HEAD in every harness that carries a C17 check at all —
now confirmed, at this close, to be **5 of the 5** C17-bearing harnesses (v1.15 through v1.19; v1.20's
own new harness carries no C17 check of its own to convert, since it is not yet frozen against
anything). This is a NAMED SUCCESS-CRITERION AMENDMENT on SWEEP-05 (D-02), not a silent gap: extending
a pre-existing limitation (originally scoped "v1.15 through v1.18" at SWEEP-05's own text) to cover
v1.19 required this project's own inline-amendment instrument, because the limitation's scope is
ratified requirement text. `[SUCCESS-CRITERION AMENDMENT, D-08 — recorded at Phase 153]` v1.20's own
harness is no longer excluded: v1.21 folds it into the C17-bearing set (six total, v1.15-v1.20),
converting it in the same simultaneous pass.

**Why NOT fixed at v1.20:** `c17-eee-contract.mjs` is CARVE Category 3, owned by Phase 143 (LINK-01..06)
under this milestone's own scoping — Phase 143 unified fence-mask behaviour across both C17 call
sites but did not convert C17's contract-presence guard to frozen-aware reads, which is a distinct
concern (mask behaviour vs. corpus-read timing) that no v1.20 requirement covers.

**Where it belongs when resolved:** a future milestone's tooling pass that extends `createFrozenCorpusReader`
(or an equivalent) to the C17 contract-presence guard across all six affected harnesses (v1.15-v1.20,
per Phase 153's `[SUCCESS-CRITERION AMENDMENT, D-08]`) simultaneously
— converting only some would leave the same class of drift risk on the unconverted remainder.

**Resolution trigger:** a dedicated tooling milestone, or the same trigger that would fire
`CARVE-1`'s successor work (the frozen-aware-adoption class), since this residue is what remains of
that root cause once the workflow-checkout-level unpinning (SWEEP-01) and the harness-corpus-level
conversion (SWEEP-05) both discharge.

**Status:** NEW at v1.20 close. DEFERRED to v1.21+. This is `CARVE-1`'s genuine surviving residue —
see Part C below for why `CARVE-1` itself closes rather than carrying forward re-scoped to this same
content (D-28: carry re-scoped OR drop-and-name-as-new, never both).

---

## V120-PIN-DEFERRAL: Freeze the v1.20 Corpus (Back-Anchor Invariant — v1.21's Path-A Job)

**Scope:** The `scripts/validation/_lib/frozen-at-close.mjs` `MILESTONE_CLOSE_SHAS` map does NOT
contain a `V120` entry after this close, and MUST NOT — a pin referencing the v1.20 close-gate SHA
would create ordering circularity (the close-gate cannot reference its own not-yet-existing SHA; the
back-anchor invariant requires every pin to reference a PAST close SHA). Freezing the v1.20 corpus
(adding `V120` + `readAtV120Close`) is the **successor milestone's** job: v1.21's harness-close phase
adds the `V120` pin the same way v1.20's Plan 144-02 added `V119`.

**Why it is NOT a v1.20 item:** back-anchor invariant (mirrors `V119-PIN-DEFERRAL` / `V118-PIN-DEFERRAL`
/ `V117-PIN-DEFERRAL`). The close-gate SHA is recorded ONLY as a grep-recoverable subject-line-pair
discriminator target, never as a pin, until the successor milestone freezes it.

**Where it belongs when resolved:** v1.21's harness-close phase — append `V120: '<v1.20 close-gate
SHA>'` (recover via the subject-line pair discriminator `git log --all --format="%H|%s" | awk -F'|'
'$2 ~ /v1\.20/ && $2 ~ /MILESTONE CLOSE/'`, POSITIVELY CONFIRMING the returned commit's SUBJECT LINE
carries both tokens — the same false-positive risk this milestone's own Plan 144-02 ruled out when
recovering V119 via the naive dual-token `--grep --all-match` form) + `readAtV120Close` export,
following the V18..V119 single-entry pattern.

**Resolution trigger (v1.21):** the v1.21 harness lineage bump (19th Path-A milestone). MANDATORY at
that close.

**Estimated effort:** included in v1.21's harness-close Atom 2 (additive; < 1 plan on its own).

**Status:** DEFERRED to v1.21 (structural — the successor milestone pins the predecessor). NEW at
v1.20 close.

---

# Part B — Carried-Forward Open Items (from v1.19, PRESERVED verbatim per "do NOT mask via deletion")

*The following are preserved from `.planning/milestones/v1.19-DEFERRED-CLEANUP.md`. They remain
unresolved / indefinite at v1.20 close — v1.20 carries no content pillar, so none of the content-side
triggers named below could have fired this milestone. Source-of-truth and full prior-milestone detail
live in the v1.19 artifact; this section carries the scope and status forward, not a re-derivation.*

## HUB-WIRING-NON-BARRED-SURFACE: A Future Runbook May Cross-Link the Kiosk/MHS Recipes

**Scope, transcribed verbatim from `v1.19-DEFERRED-CLEANUP.md:119-125`:** if a kiosk-lockout or
MHS-exit-PIN-lockout L1/L2 runbook is ever authored, the hub routes to THAT runbook and the runbook
may cross-link the recipe; the three troubleshooting hubs never link `docs/recipes` directly. Named
for the two non-barred surfaces `docs/l1-runbooks/00-index.md` and `docs/decision-trees/` (neither is
one of the three hubs `V-132-HUBSNOTWIRED`/`check-phase-137.mjs` bar `docs/recipes` from).

**Resolution trigger:** such a runbook is authored. Unmet at v1.20 close (no content pillar).

**Status:** CARRIED from v1.19. DEFERRED to v1.21+.

## HYG-05 Fenced-Content Retrieval Quality — Unfalsified Extrapolation

**Scope, transcribed verbatim from `v1.19-DEFERRED-CLEANUP.md:155-167`:** HYG-05's shipped correction
to `docs/_standards/EEE-SOP-standard.md` states that fenced content "retrieves poorly," but its own
cited evidence base (`PIPE-02-FINDINGS.md`) has zero occurrences of `fence`, `code block`,
`SourceCode`, or `VerbatimChar` — retrieval quality of fenced content specifically was never tested.
The honest formulation, already shipped: fenced content reaches the index as non-prose runs; PIPE-02
did not test how well it retrieves.

**Where it belongs when resolved:** a future PIPE-0N-style grounding pass that specifically probes
fenced-content retrieval ranking/quality against the live index.

**Resolution trigger:** the next PIPE-family grounding pass, or a reported retrieval-quality complaint
traceable to fenced content. Unmet at v1.20 close (v1.20 runs no grounding leg).

**Status:** CARRIED from v1.19. DEFERRED to v1.21+.

## CI-3 Scope Correction (Part A): Managed Apple ID → Managed Apple Account Corpus Rename (Text Correction Only)

**Scope, transcribed verbatim from `v1.19-DEFERRED-CLEANUP.md:171-184`:** distinct from the Part C
`CI-3` substance entry below — this sub-entry pins only the corrected derivation of CI-3's scope
figures (57 occurrences / 17 files, markdown-only filtered:
`grep -rn "Managed Apple ID" docs --include=*.md`), so a successor does not re-derive a stale count
from the untracked `docs/graphify-out/` generated directory.

**Status:** CARRIED from v1.19 (text-correction sub-entry). DEFERRED to v1.21+ alongside the Part C
substance entry below.

## SHARED-TAXONOMY-DOC (Option B): A Shared Kiosk/Dedicated Taxonomy Reference Doc

**Scope, transcribed verbatim from `v1.19-DEFERRED-CLEANUP.md:188-194`:** a shared kiosk/dedicated
taxonomy Reference doc — barred at v1.19 because the publish-bundle canary was locked at 223→225 and
Phase 137 carried only a two-row budget. **Trigger:** a third lockdown recipe lands and the canary
budget is free.

**Status:** CARRIED from v1.19, OPEN (trigger unmet — v1.20 ships no recipe of any kind). DEFERRED to
v1.21+.

## ANCHOR-REVIEW-BY-PAST-DUE: `05-dedicated-devices.md` Freshness Refresh Tracker

**Scope, transcribed verbatim from `v1.19-DEFERRED-CLEANUP.md:198-204`:** the anchor's past-due
`review_by: 2026-06-22` (`docs/admin-setup-android/05-dedicated-devices.md`) was already 42 days past
due at v1.19 close. **Trigger:** the next phase or milestone that touches this anchor doc refreshes its
`last_verified`/`review_by` pair as a named correction. Not re-measured this milestone (v1.20 touches
no corpus document at all, per its own Out-of-Scope table) — the day-count above is the v1.19 figure,
carried, not re-derived, and is understood to be further stale by the elapsed interval.

**Status:** CARRIED from v1.19, OPEN (trigger unmet). DEFERRED to v1.21+.

## ROLLBACK-RECOVERY-DIVERGENCE-COUNT: `## Rollback/Recovery` Template-Divergence Tally

**Scope, transcribed verbatim from `v1.19-DEFERRED-CLEANUP.md:208-214`:** the `## Rollback/Recovery`
template-divergence count sits at **2-of-4** as of v1.19 close. **Trigger:** a third recipe needs the
`## Rollback/Recovery` slot.

**Status:** CARRIED from v1.19, OPEN (trigger unmet — v1.20 ships no recipe). DEFERRED to v1.21+.

## AOSP-Wired 802.1X + Cloud PKI / Intune Certificate Connector Deep-Dive

**Scope, transcribed verbatim from `v1.19-DEFERRED-CLEANUP.md:330-332`:** Android AOSP-specific wired
802.1X depth; full Cloud PKI / Intune Certificate Connector cert-delivery deep-dive for 802.1X.

**Status:** CARRIED from v1.14 Out-of-Scope, through every milestone to v1.19, now to v1.20. DEFERRED
to v1.21+.

## Correction of Record (Carried from v1.19)

*Both sections below are historical corrections against the v1.18 record, not open items requiring
future action — carried here per D-28's explicit instruction so neither drops out of the document
lineage, and per this project's "do NOT mask via deletion" doctrine applied to the corrections-of-record
class as well as to open items.*

**The Predecessor's False "Zero-Failure Full-Chain" Claim (transcribed in substance from
`v1.19-DEFERRED-CLEANUP.md:220-222`):** `v1.18-DEFERRED-CLEANUP.md:123` claimed the "complete
non-nested `[48..133]` chain" found zero failures — FALSE, since `check-phase-134.mjs` unconditionally
sets the nested guard on every child it spawns and therefore cannot produce a genuinely non-nested
chain result, and separately eight of that chain's own members exited 1 standalone on inputs
unchanged since the v1.18 close. Corrected at the v1.19 close; carried here as a permanent record, not
re-litigated at v1.20 (v1.20's own apex is subject to the identical structural property — see D-32 in
`144-CONTEXT.md` and the companion milestone audit's own care not to repeat the banned phrasing).

**The Predecessor Audit's Push-Semantics Claim (transcribed in substance from
`v1.19-DEFERRED-CLEANUP.md:224-226`):** `v1.18-MILESTONE-AUDIT.md` claimed a push fires the integrity
workflow cascade — FALSE, since no workflow in this repository has ever carried a `push:` trigger;
the actual mechanism is `workflow_dispatch`, for which a push is only the precondition (the workflow
file must already be on the default branch). Corrected at the v1.19 close; v1.20's own Axis-2 capture
(`v1.20-MILESTONE-AUDIT.md`) again used `workflow_dispatch` exclusively, consistent with this
correction.

## V-132-HUBSNOTWIRED-REGEX-BROKEN: `check-phase-132.mjs`'s Hub-Coverage Regex Does Not Cover `docs/recipes` Generically

**Scope, transcribed verbatim from `v1.19-DEFERRED-CLEANUP.md:105-115`:** `check-phase-132.mjs:97`'s
`V-132-HUBSNOTWIRED` check tests `/docs\/recipes|01-shared-windows-avd|02-shared-ipad/` against the
three troubleshooting-hub files — the hub files live *inside* `docs/`, so a real link from one of them
never carries the `docs/` prefix, and the `docs\/recipes` arm never fires; the other two arms are
hardcoded to two named recipes.

**NOT dropped at v1.20 (D-28, explicit):** `git log a7bda73e..HEAD -- scripts/validation/check-phase-132.mjs`
is empty — the validator is byte-unchanged since the V119 pin, confirming the regex sits verbatim, and
`V-132` is a NEGATIVE assertion (hubs must NOT link to recipes) that no link-**resolution** checker
(including Phase 143's own `check-nav-hub-links.mjs`) can discharge, since resolving links proves
nothing about an absence assertion.

**Where it belongs when resolved:** a future milestone that legitimately re-opens `scripts/validation/`
predecessors could correct both the regex and any remaining stale claim about its coverage.

**Status:** CARRIED from v1.19, OPEN (the underlying regex stays frozen and unfixed by design).
DEFERRED to v1.21+.

## RECIPE-OUTBOUND-LINK-COVERAGE: Recipes 03/04's `../` Outbound Links Have No Standalone-Job Validator Coverage

**Scope, transcribed verbatim from `v1.19-DEFERRED-CLEANUP.md:93-101`:** `check-nav-hub-links.mjs`
scans only links *from* the 4 troubleshooting hubs and links *targeting* the 4 hubs — the 73 `../`-relative
outbound links recipes 01-04 ship (measured at `144-CONTEXT.md`'s own re-derivation, superseding the
v1.19-era 42-link figure which covered only recipes 03/04) are checked by no dedicated CI job.

**NOT dropped at v1.20 (D-28, explicit):** its complaint is durability, and `check-nav-hub-links.mjs`
has zero execution sites across `scripts/`, `.github/` and `package.json` outside `check-phase-123.mjs`'s
existence pin — Phase 143's own `check-phase-143.mjs` leaf supplies coverage additively via the apex
chain (`CHAIN-143`), never as a standalone job, so the durability complaint this entry names is only
partially answered, not discharged.

**Where it belongs when resolved:** a future tooling milestone extending `check-nav-hub-links.mjs` (or
a sibling checker) with a real standalone CI execution site, beyond the apex-chain coverage Phase 143
supplies.

**Resolution trigger:** a fifth or later recipe lands and the coverage gap becomes an operational cost,
or a broken recipe outbound link is discovered in production.

**Status:** CARRIED from v1.19, OPEN (partially, not fully, answered by Phase 143's apex-chain
coverage). DEFERRED to v1.21+.

## WINDOWS-CLONE-DEEPNEST-TIMEOUT-01: Chain-Apex Cold-Clone Deep-Nesting Subprocess Cost on Windows (CARRIED — apex deepened to `[48..143]` — corroboration, not resolution, now WITH one incomplete data point)

Running any chain validator from a cold fresh-clone on Windows can exhibit an O(n²) cascade of cold
Node subprocess spawns. Carried forward from v1.19; the apex chain is now `[48..143]` (+6 deeper than
v1.19's `[48..137]`). **v1.20 note:** Axis 1 (this milestone's own fresh clone, Plan 144-10) completed
clean in 28.8s, 100 PASS / 0 FAIL / 1 SKIPPED, no deep-nest stall — a further clean data point on the
same corroboration-only basis v1.19 and v1.18 already established (three-then-four consecutive clean
cycles from v1.17 forward). **A genuinely dispatched, context-independent Axis-3 attempt this
milestone did NOT complete the apex leg** — it stalled at `[CHAIN-142/101]` with no further output
growth (`v1.20-MILESTONE-AUDIT.md`'s Auditor-Independence section, OWNER-RULED 2026-08-17). This is the
first time this item has manifested as an observed incompleteness in a genuinely independent
(context-independent) invocation, rather than only as a theoretical hazard corroborated-by-absence
across repeated clean local runs — recorded honestly as a data point that STRENGTHENS this item's
standing carried status, not as a new, separately-tracked defect. **Remediation options (v1.21+):**
unchanged — cache-warm clone / raise per-subprocess timeout / a genuinely frozen-aware `harness-run`
checkout (would also help the residual `CARVE-1` class). **Status:** CARRIED FORWARD (unresolved in
principle; empirically non-blocking on every completed local run, now with one incomplete
genuinely-independent run recorded honestly rather than omitted). DEFERRED to v1.21+.

## LATENT-NON-FROZEN-AWARE-CONTENT-ASSERTION-01: Predecessor Content Assertions Not Yet Frozen-Aware (re-scanned at v1.20)

Predecessor `check-phase-NN.mjs` validators carrying DIRECT content assertions reading LIVE HEAD
rather than their own milestone close-SHA can drift on a broad-corpus milestone. **v1.20 note:** this
milestone's own Class-1 archival-drift census (`144-EVIDENCE.md`, Plan 09 Task 1) re-confirmed ZERO
survivors reading `.planning/phases/139-144` live in code (the three matching lines are header-comment
provenance notes, never `PATH =` constants or read-function arguments) — `check-phase-139.mjs` through
`check-phase-143.mjs` and the apex all pass the same test the four new v1.19 leaves passed at their own
close. The NAMED live-reading exposure `check-phase-54.mjs`'s live `REQUIREMENTS.md`/`ROADMAP.md`
reads with a negative assertion is unchanged and still not remediated (out of scope for a harness-close
phase); Plan 144-12's post-close-gate confirmatory run is what proves this exposure does not manifest a
false result at this close, exactly as it did at v1.19's own close-gate.

**Status:** CARRIED from v1.19/v1.18/v1.17/v1.16/v1.15 (re-scanned at v1.20, zero new frozen-aware
conversions required, the one live-reading exposure re-confirmed non-manifesting via the post-close-gate
confirmatory run). DEFERRED to v1.21+ chain-validator retrospective (superset item, the residual
`CARVE-1`-class frozen-aware-adoption sweep, covers the durable fix).

## Deployment — SharePoint Content-Approval (if Draft must gate retrieval)

**Scope, carried verbatim in substance from `v1.19-DEFERRED-CLEANUP.md:282-290`:** `Status: Draft` is a
LABEL only — Draft docs ARE indexed and retrieved (v1.15/v1.16 PIPE-02). v1.17 through v1.20 have not
re-exercised this probe (v1.20 runs no grounding leg). If Draft must actually GATE retrieval, enabling
+ maintaining SharePoint content approval is required (tenant/ops config, not a doc change).

**Status:** CARRIED from v1.15 through v1.19, now v1.20 (unresolved — deployment lever). Not
re-exercised at v1.20 close.

## Infra — Azure AI Search Structured Index

**Scope, carried verbatim in substance from `v1.19-DEFERRED-CLEANUP.md:294-300`:** upgrade from the
native SharePoint knowledge source to an Azure AI Search + SharePoint indexer to make
`platform`/`doc_type` filterable metadata.

**Status:** CARRIED from v1.14 through v1.19, now v1.20 (infra escalation). Unresolved at v1.20 close.

## Deployment — Auto-Upload to SharePoint (Graph API)

**Scope, carried verbatim in substance from `v1.19-DEFERRED-CLEANUP.md:304-312`:** push the publish
bundle to the SharePoint document library via Graph API — would introduce the first `secrets.`
reference in any workflow in this repository, at tenant-wide `Sites.ReadWrite.All` scope. v1.20's own
requirements document names this explicitly Out of Scope, for the same reason.

**Status:** CARRIED from v1.17 through v1.19, now v1.20. DEFERRED to v1.21+.

## MTPSSO-01/02/03 + PSSO-FUT-03: Multi-Tenant Platform SSO

Cross-tenant macOS Platform Credential registration, multi-tenant Conditional Access / compliance
scoping, multi-tenant PSSO L2 troubleshooting, and the admin-setup counterpart notation. Net-new
architectural scope. **Status:** DEFERRED to v1.21+. *(Carried v1.10 → … → v1.20.)*

## KRBFUT-01/02: On-Prem-AD-Only Kerberos Realm Deep-Dive + Azure Files Cloud-Kerberos GA

On-prem-AD-only Kerberos realm configuration; full Azure Files Cloud-Kerberos macOS coverage on GA
promotion. **Status:** DEFERRED to v1.21+. *(Carried v1.10 → … → v1.20.)*

## CI-3 (Substance): Managed Apple ID → Managed Apple Account Corpus Rename (57 occurrences / 17 files)

Corpus-wide rename pending Microsoft Intune adopting the rebrand portal-side — no in-repo evidence it
has. **Status:** DEFERRED to v1.21+. *(Carried v1.6 → … → v1.20; the 57/17 figure is the corrected
scope per the Part A text-correction sub-entry above, not re-derived this milestone.)*

## Part D — Carried-Forward v1.8 Deferred Items (PRESERVED verbatim per "do NOT mask via deletion")

*Preserved from `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` Part D, itself preserved from
`v1.18-DEFERRED-CLEANUP.md` Part C. Unresolved / indefinite at v1.20 close.*

- **ARCHIVE-UPSTREAM-01:** Upstream PR to get-shit-done-cc for the `extractOneLinerFromBody` regex fix
  (vendored fallback canonical until upstream lands). DEFERRED. *(Carried v1.8 → … → v1.20.)*
- **v1.1 line 164 `Edit N --` token-class:** `check-phase-71.mjs` `PLACEHOLDER_TOKENS` extension
  surface; reactive pickup. Documented for awareness. *(Carried v1.8 → … → v1.20.)*
- **Multi-tenant Apple Business surfaces:** cross-tenant device transfer / multi-org-account. DEFERRED
  indefinitely. *(Carried v1.8 → … → v1.20.)*
- **Apple Business Device API documentation:** on Apple publishing the developer landing page. DEFERRED
  to v1.21+. *(Carried v1.8 → … → v1.20.)*
- **Per-OU Conference Room Display deep-dive:** TRACKER ONLY (no acute demand v1.6–v1.20). *(Carried
  v1.8 → … → v1.20.)*
- **Account Holder lockout dedicated recovery runbook:** CONDITIONAL (promote on 5+ escalations/quarter).
  *(Carried v1.8 → … → v1.20.)*
- **Apple School Manager (ASM) education-specific surfaces:** DEFERRED to v1.21+ (outside enterprise
  scope). *(Carried v1.8 → … → v1.20.)*

## The Ten Unowned Tooling Items (Rows 1-6, 9, 10, 14, 15 of the Preserved Table Above)

Cross-referenced, not re-stated: `check-phase-31 --quick`'s silent check loss (row 1), seven
pass-worded `FAIL` detail strings (row 2), `V-31-22`'s zero-file vacuous-pass guard (row 3),
`check-phase-30.mjs`'s unescaped-dot filename regex (row 4), `carve-gate.mjs`'s import-time
`process.exit` hazard (row 5), dead `required:` check metadata (row 6), `check-phase-66`'s timeout
margin against the 300,000ms per-subprocess cap (row 9), the unowned non-nested standalone-chain
wall-clock cost (row 10), and `FENCE-AXIS-02`'s two axes — fence length and mask scope (rows 14-15,
also restated as this document's own Part A residue item above for the C17-specific instance of the
mask-scope axis). None has an owning requirement in v1.20; all ten carry forward unchanged.

**Status:** CARRIED (recorded-not-owned) from the mid-milestone routing table above. DEFERRED to
v1.21+, pending a future tooling requirement or milestone that explicitly picks any of them up.

---

# Part C — Dropped-and-Closed at v1.20

## ACCEPTED-STANDALONE-CI-RED — DISCHARGED and CLOSED at v1.20

**What it was:** frozen predecessor integrity workflows (v1.4-v1.18) going RED standalone against the
evolved corpus, root-caused to `CARVE-1`'s unresolved ref-less `harness-run` checkout. Discharged with
live evidence at v1.19 (5 PASS / 10 FAIL across the fifteen pre-existing workflows at that close), then
carried forward at its extended v1.4-v1.18 span, per the milestone bar's own "delete rather than carry
a seventh milestone" wording.

**Closed by:** Phase 144 Plan 144-10's Axis-2 capture — all 17 workflows (the sixteen pre-existing plus
the new v1.20 workflow) dispatched at the shared SHA `2858c0b5e6d3c05133fd1bcc4c5c12f97e01c55c`,
job-level JSON quoted for all 211 jobs across all 17 runs: **196 success, 15 classified event-gated
skips, 0 failures.** One remediation round (D-22) was spent on the first dispatch's sole failure
(`check-nav-hub-links.mjs`'s own self-test, a genuine Phase-143 defect unrelated to the `CARVE-1`
pattern this disposition tracks), and it succeeded — the second dispatch at the corrected SHA was 17 of
17 green. Since the pattern this disposition names (a frozen harness validating live HEAD rather than
its own pinned corpus, going red as the corpus evolves) produced ZERO red jobs at this close, the
disposition discharges fully rather than re-extending an eighth milestone.

**Dropped from the carried-forward list above.** Recorded Closed here and in `v1.20-MILESTONE-AUDIT.md`'s
Predecessor-Workflow Cascade Scan section. Full detail: `144-EVIDENCE.md`, Plan 10 Task 1.

## ACCEPTED-SCOPED-RED — DISCHARGED and CLOSED at v1.20 (including its `PRE-CHAIN-VALIDATOR-RED-30/31` cross-reference)

**What it was:** the ten-member standalone-RED chain-validator set (`check-phase-30, 31, 48, 60, 61,
62, 63, 64, 65, 66`), four root-cause classes, named at v1.19's own close with an explicit MANDATORY
STATEMENT that fixing only two of the four classes would not green the set.

**Closed by:** Phase 141 (RED-01/02/03 — closing root-cause classes (a) self-test classifier, (b)
superseded C5 freshness rule, and (c) cascading chain-guards, by fixing 48 and 60-66) and Phase 142
(RED-04/05/06/07 — closing root-cause class (d), the pre-chain content drift in 30/31, and joining them
to the apex's executed-chain set via `CHAIN_EXTRA`). Re-confirmed fresh, all ten exit 0, at Phase 144
Plan 09 (HEAD `7ad104bf`); re-confirmed unchanged at the close SHA per this document's companion
milestone audit's own Cascade Scan section (`git diff --name-only 7ad104bf..HEAD` touches none of the
ten members' own files between Plan 09's measurement and the close). All four of the MANDATORY
STATEMENT's named root-cause classes are now closed, not merely two — the condition the predecessor's
own statement said would NOT be sufficient is exceeded.

**`PRE-CHAIN-VALIDATOR-RED-30/31` (v1.19's own five-assertion detail record for two of the ten members)
closes together with this entry**, per the double-booking guard below — its content (the five named
`check-phase-30`/`check-phase-31` assertion failures) is exactly the class RED-04/05 fixed, and it was
always a cross-referenced detail record for `ACCEPTED-SCOPED-RED`'s class (d), never a separately
owned defect.

**Dropped from the carried-forward list above.** Recorded Closed here and in `v1.20-MILESTONE-AUDIT.md`'s
Predecessor-Workflow Cascade Scan section. Full detail: `144-EVIDENCE.md`, Plan 09 Task 1.

## DEFER-119-A — DROPPED-and-Closed at v1.20 (moved here rather than carried, per D-28)

**What it was:** `regenerate-supervision-pins.mjs --self-test` advisory-RED against
`docs/_glossary-android.md:145` (frozen-fixture vs. live-corpus divergence), CARRIED UNCHANGED across
v1.14 through v1.19, and the SOLE OWNER of `ACCEPTED-SCOPED-RED`'s class-(a) content.

**Closed by:** Phase 141's RED-02 — the classifier's backward-only scan window is repaired (the iOS
token sits two lines *after* the heading it was scanning backward from), with the frozen v1.7 fixture
left unedited and the classifier not relaxed. `regenerate-supervision-pins.mjs --self-test` now exits
**0** (`Diff: identical`, `Self-test: PASS`), and RED-02, the requirement that owned this defect, is
Complete.

**Why moved to DROPPED-and-Closed rather than CARRIED (D-28's explicit correction):** this entry's
entire content IS the one defect RED-02 fixed. Carrying the owning entry forward while dropping the
`ACCEPTED-SCOPED-RED` class-(a) cross-reference that depends on it would be incoherent — the
root-cause-level double-booking guard this document inherits from v1.19 exists precisely to prevent
that kind of split.

**Dropped from the carried-forward list above.** Recorded Closed here. Full detail:
`144-EVIDENCE.md`, Plan 09's `regenerate-supervision-pins.mjs --self-test` re-confirmation; substantive
fix landed at Phase 141 (`REQUIREMENTS.md` RED-02).

## V119-PIN-DEFERRAL — CLOSED at v1.20 via HARN-17

**What it was:** the mandatory back-anchor invariant deferred by v1.19's own close — freezing the
v1.19 corpus (`_lib/frozen-at-close.mjs` gaining a `V119` entry + `readAtV119Close`/`lsTreeAtV119Close`
exports) was structurally barred from v1.19's own close (ordering circularity) and was the successor
milestone's job.

**Closed by:** Phase 144 Plan 02 (HARN-17), which appended `MILESTONE_CLOSE_SHAS.V119 = 'a7bda73e'` +
`readAtV119Close` + `lsTreeAtV119Close` to `scripts/validation/_lib/frozen-at-close.mjs` as a pure
append-only edit, recovered the SHA via the subject-line pair discriminator (count=1), and proved the
pin end-to-end (`readAtV119Close('.planning/REQUIREMENTS.md')` returned 27,286 bytes;
`lsTreeAtV119Close('docs')` returned 296 entries).

**Dropped from the carried-forward list above.** Recorded Closed here and in
`v1.20-MILESTONE-AUDIT.md`'s Requirements Traceability table (HARN-17 → Validated). Full detail:
`144-02-SUMMARY.md`.

## CARVE-1 (FROZEN-AWARE-ADOPTION-SWEEP-01) — CLOSED at v1.20 (root cause discharged; residue redirected, not double-booked)

**What it was:** the durable structural debt behind `ACCEPTED-STANDALONE-CI-RED` — every
`audit-harness-vX.Y-integrity.yml`'s `harness-run` job checks out its branch with no `ref:` pin, so a
frozen milestone-audit harness always validates LIVE HEAD rather than its own close-SHA corpus.

**Closed by:** SWEEP-01 (Phase 139, `fetch-depth: 0` retrofit, the precondition frozen reads need) and
SWEEP-05 (Phase 140 + Phase 144 Plan 02, converting every one of the seventeen harnesses' own corpus
reads to frozen-aware). Per D-28's explicit ruling, this entry's stated root cause (the un-pinned
`ref:`) IS discharged by these two requirements together.

**Why closed rather than re-scoped-and-carried (D-28):** the C17 contract-presence leg is genuine
surviving residue, but carrying `CARVE-1` forward re-scoped to that residue WHILE ALSO opening a NEW
entry for the identical content would double-book the same defect under two identifiers — exactly the
error the root-cause-level double-booking guard exists to catch. The residue is named as NEW instead:
see `C17-FROZEN-AWARE-RESIDUE-V15-V19` in Part A above.

**Dropped from the carried-forward list above.** Recorded Closed here and in
`v1.20-MILESTONE-AUDIT.md`. Full detail: `144-EVIDENCE.md` Plan 02 (SWEEP-05 conversion) and Plan
139-level SWEEP-01 evidence.

## C17-VS-PIPELINE-FENCE-MASK-DIVERGENCE — CLOSED at v1.20 (indentation axis; length/scope residue is `FENCE-AXIS-02`)

**What it was:** `c17-eee-contract.mjs`'s fenced-content mask was column-0-anchored while
`convert.ps1`'s tolerated leading whitespace — latent for most content, live the moment an indented
fence contained a `>` or `|` line.

**Closed by:** LINK-05 (Phase 143) — fence-mask behaviour unified across all 15 call sites in 9 files,
including both C17 sites, using a `^ {0,3}` CommonMark-shaped rule proven to cover 100% of the corpus's
74 live indented-fence instances. LINK-06 proved c17's before/after file and violation counts identical
and the newly-masked lines hide no suppressed violation.

**Why the residue is NOT also closed here:** LINK-05's own subject is INDENTATION only — two adjacent
axes (fence-closer LENGTH matching, and mask SCOPE relative to front matter) are explicitly out of its
scope, per `143-CONTEXT.md` D-18/D-20. That residue is `FENCE-AXIS-02`, already present at rows 14-15
of the preserved table above, carried forward unchanged (Part B's "Ten Unowned Tooling Items" entry).

**Dropped from the carried-forward list above** (the indentation-divergence content only — not
double-booked with `FENCE-AXIS-02`, which covers the distinct length/scope residue). Recorded Closed
here. Full detail: `143-CONTEXT.md` D-18, `143-EVIDENCE.md`.

---

## Double-Booking Guard — Result

Run against the frozen predecessor-deferral exclusion list AND, per the inherited D-27/D-28 ruling,
against this document's own new and carried entries at CONTENT level, not identifier level only.

**One real double-booking avoided by construction, not merely checked after the fact:** `CARVE-1`'s
root-cause content and the new `C17-FROZEN-AWARE-RESIDUE-V15-V19` entry are deliberately kept
SEPARATE — `CARVE-1` closes on its root-cause content (the un-pinned checkout, discharged by
SWEEP-01/05) and the C17 residue is named fresh, never restating `CARVE-1`'s own closed content. This
is the exact split D-28 mandated ("carry re-scoped OR drop-and-name-as-new, never both").

**One cross-reference preserved, not duplicated:** `PRE-CHAIN-VALIDATOR-RED-30/31` and
`ACCEPTED-SCOPED-RED`'s class-(d) remain the same defect they always were (the former is the latter's
own detail record for two of its ten members) — both close together in the same Part C entry, not as
two separate closures of the same content.

**Identifier prefix a prior draft wrongly attributed to the predecessor artifact:** confirmed absent
from this document (a self-referential grep for the prefix's own text would be the wrong test — the
prefix is deliberately not spelled out anywhere in this file, including in this very sentence),
matching the fact that it never appeared in `v1.19-DEFERRED-CLEANUP.md` either (per `144-CONTEXT.md`
D-28's own citation: zero hits for that prefix across the predecessor document).

**No other double-book found:** `V120-PIN-DEFERRAL` is distinct from `V119-PIN-DEFERRAL` (successive
milestones' own pins, not the same defect, exactly as `V119-PIN-DEFERRAL` was distinct from
`V118-PIN-DEFERRAL` at the prior close); `RECIPE-OUTBOUND-LINK-COVERAGE` and
`V-132-HUBSNOTWIRED-REGEX-BROKEN` remain two distinct complaints (durability-of-coverage vs.
correctness-of-a-negative-assertion) despite both concerning link/hub validation, and neither restates
the other's detail.

**Guard result: CLEAN after the two deliberate splits above** (the `CARVE-1`/C17-residue split, and the
`ACCEPTED-SCOPED-RED`/`PRE-CHAIN-VALIDATOR-RED-30/31` co-closure, both already resolved by construction
rather than discovered as errors).

---

## Cross-References (v1.20 Close)

- **v1.20 milestone audit:** `.planning/milestones/v1.20-MILESTONE-AUDIT.md`
- **v1.19 predecessor (single-source-of-truth for carried items' full prior-milestone detail):**
  `.planning/milestones/v1.19-DEFERRED-CLEANUP.md`
- **The 15 pre-existing routed rows' own sources:** Phase 142 `142-CONTEXT.md`'s `<deferred>` block
  (rows 1-12) and Phase 143 `143-CONTEXT.md` D-18/D-20/D-23/D-26 (rows 13-15)
- **Phase 144 close-cluster evidence:** `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md`
- **Phase 144 close-cluster context (D-27, D-28 govern this document in full):**
  `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md`
- **v1.20 requirements (28/28, Validated pending Plan 144-12):** `.planning/REQUIREMENTS.md`

---

## Provenance Footer (v1.20 Close)

*Appended 2026-08-18 at Phase 144 Plan 144-11 close-gate, absorbing the 15 pre-existing routed rows
byte-identical.* **Part A: two new entries** — `C17-FROZEN-AWARE-RESIDUE-V15-V19` (the genuine
surviving residue of `CARVE-1`, named fresh rather than double-booked) and `V120-PIN-DEFERRAL`
(structural, back-anchor invariant, mirrors `V119-PIN-DEFERRAL`). **Part B: carried from v1.19** — the
eight previously-omitted still-open entries (`HUB-WIRING-NON-BARRED-SURFACE`, `HYG-05`, the Part-A
`CI-3` scope correction, `SHARED-TAXONOMY-DOC`, `ANCHOR-REVIEW-BY-PAST-DUE`,
`ROLLBACK-RECOVERY-DIVERGENCE-COUNT`, `AOSP-Wired 802.1X`, both Correction-of-Record sections), plus
`V-132-HUBSNOTWIRED-REGEX-BROKEN` and `RECIPE-OUTBOUND-LINK-COVERAGE` (both explicitly NOT dropped),
`WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` (now with one incomplete genuinely-independent data point recorded
honestly), `LATENT-NON-FROZEN-AWARE-CONTENT-ASSERTION-01` (re-scanned, widened census re-confirmed
zero new survivors), the Deployment/Infra trio, the Part C legacy trio (MTPSSO/KRBFUT/CI-3 substance),
Part D's seven v1.8 items preserved verbatim, and the ten unowned tooling items cross-referenced from
the preserved table. **Part C: six closures** — `ACCEPTED-STANDALONE-CI-RED` and `ACCEPTED-SCOPED-RED`
(both DISCHARGED with live evidence at the shared/close SHA, satisfying the milestone bar's own
"deleted, not carried a seventh milestone" wording), `PRE-CHAIN-VALIDATOR-RED-30/31` (co-closed with
`ACCEPTED-SCOPED-RED`, same content), `DEFER-119-A` (moved here rather than carried, per D-28),
`V119-PIN-DEFERRAL` (closed by HARN-17), and `CARVE-1` (root cause discharged, residue redirected to
Part A rather than double-booked) plus `C17-VS-PIPELINE-FENCE-MASK-DIVERGENCE` (indentation axis closed
by LINK-05/06; length/scope residue remains open as `FENCE-AXIS-02`, already present in the preserved
table).

**Root-cause-level double-booking guard — result:** CLEAN. See the dedicated section above for the two
deliberate splits (never double-books) this document's own authoring made by construction.
