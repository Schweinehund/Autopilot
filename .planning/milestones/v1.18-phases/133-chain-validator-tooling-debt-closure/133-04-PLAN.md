---
phase: 133-chain-validator-tooling-debt-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/check-phase-48.mjs
  - scripts/validation/check-phase-60.mjs
  - scripts/validation/check-phase-61.mjs
autonomous: true
requirements: [TOOL-06]
must_haves:
  truths:
    - "The 3 HELPER-SPAWN-STDERR-01 --self-test call sites (check-phase-48:85, check-phase-60:201, check-phase-61:397) have their execFailDetail slice budget raised from n:200 to the chosen larger value"
    - "Only the 3 --self-test call-site {n:...} literals change — the shared _lib/exec-fail-detail.mjs is untouched, and the n:500 CHAIN/harness sites in check-phase-60/61 are untouched"
    - "The tuning lands as its OWN separate D-00a-exception commit, never bundled with the TOOL-04 re-pin (D-09)"
    - "DEFER-119-A is documented as remaining ACCEPTED-ADVISORY (continue-on-error:true, structurally non-blocking) — no independent action (D-05/GA-2)"
  artifacts:
    - path: "scripts/validation/check-phase-48.mjs"
      provides: "Retuned --self-test stderr slice budget"
      contains: "execFailDetail"
  key_links:
    - from: "scripts/validation/check-phase-48.mjs"
      to: "scripts/validation/_lib/exec-fail-detail.mjs"
      via: "import + call-site {n:...} argument (definition unchanged)"
      pattern: "execFailDetail"
---

<objective>
Close the `HELPER-SPAWN-STDERR-01` residual: raise the stderr slice budget at the 3 `--self-test` catch-block call sites in the frozen validators `check-phase-48.mjs` (line ~85), `check-phase-60.mjs` (line ~201), and `check-phase-61.mjs` (line ~397) from `n: 200` to a larger value, so a `--self-test` failure shows a useful excerpt. The core bug (dropping stdout) was already fixed by v1.14 TOOL-03; only the numeric budget remains (carried since v1.8). These 3 files are FROZEN (inside the `[48..127]` chain-apex range), so this is its OWN D-00a-exception commit, SEPARATE from the TOOL-04 re-pin (D-09, Pitfall 4).

Purpose: Close TOOL-06 SC#3 and, in writing, confirm DEFER-119-A stays ACCEPTED-ADVISORY (D-05) so Phase 134's close-audit does not re-litigate it.
Output: 3 retuned call sites in one separate atomic D-00a-exception commit; a written DEFER-119-A disposition note.

The exact new `n` value is Claude's discretion (per CONTEXT) — pick one budget large enough to show a full self-test failure block and apply it identically to all 3 sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-chain-validator-tooling-debt-closure/133-CONTEXT.md
@.planning/phases/133-chain-validator-tooling-debt-closure/133-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retune the 3 --self-test stderr slice budgets</name>
  <files>scripts/validation/check-phase-48.mjs, scripts/validation/check-phase-60.mjs, scripts/validation/check-phase-61.mjs</files>
  <read_first>
    - scripts/validation/check-phase-48.mjs (line ~85: the `--self-test` catch-block `execFailDetail(stdout, stderr, { n: 200, trim: false, prefix: '--self-test FAIL: ' })` site)
    - scripts/validation/check-phase-60.mjs (line ~201: the `--self-test` site; note the SEPARATE n:500 CHAIN/harness sites that must NOT be touched)
    - scripts/validation/check-phase-61.mjs (line ~397: the `--self-test` site; same n:500 do-not-touch caveat)
    - .planning/phases/133-chain-validator-tooling-debt-closure/133-RESEARCH.md (TOOL-06 section: exact sites table, "execFailDetail is a SHARED lib — do not touch its definition", frozen-surface status)
  </read_first>
  <action>
    In each of the 3 files, at the `--self-test` catch-block call site ONLY (the one currently `{ n: 200, trim: false, prefix: '--self-test FAIL: ' }`), change the `n: 200` literal to the chosen larger budget (same value in all 3). Change nothing else on the line (keep `trim: false` and the `prefix` verbatim) and nothing else in the file. Do NOT touch the shared function definition `scripts/validation/_lib/exec-fail-detail.mjs` (editing it would ripple across ~40 call sites in 20 files — out of scope). Do NOT touch the additional `n: 500` `execFailDetail` sites in check-phase-60.mjs / check-phase-61.mjs (those are the CHAIN/harness checks, not the HELPER-SPAWN-STDERR-01 scope). This is a frozen-surface edit — no other frozen file (sidecars, check-phase-128, regenerate-supervision-pins, workflows) is touched here.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && for F in check-phase-48 check-phase-60 check-phase-61; do node -c scripts/validation/$F.mjs || exit 1; done && test -z "$(git diff --name-only -- scripts/validation/_lib/ scripts/validation/check-phase-128.mjs scripts/validation/regenerate-supervision-pins.mjs)" && test -z "$(git diff --name-only -- '.github/')" && test -z "$(git diff --name-only -- 'scripts/validation/'*audit-allowlist.json)"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files pass `node -c` (syntactically valid).
    - Each of the 3 `--self-test` sites now shows the chosen budget instead of `n: 200`; `git diff` shows exactly one changed line per file with only the `n:` value differing (trim/prefix unchanged).
    - Zero changes to `_lib/exec-fail-detail.mjs`, `check-phase-128.mjs`, `regenerate-supervision-pins.mjs`, any other `check-phase-*.mjs`, any `*-audit-allowlist.json`, or any `.github/` workflow.
    - The two n:500 sites in check-phase-60/61 remain n:500 (unchanged).
  </acceptance_criteria>
  <done>All 3 --self-test stderr budgets raised identically; only those 3 lines changed; shared lib and n:500 sites untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Land the separate D-00a commit and document the DEFER-119-A disposition</name>
  <files>scripts/validation/check-phase-48.mjs</files>
  <read_first>
    - .planning/phases/133-chain-validator-tooling-debt-closure/133-CONTEXT.md (D-09 separate-commit mandate; D-05 DEFER-119-A ACCEPTED-ADVISORY outcome)
    - .planning/phases/133-chain-validator-tooling-debt-closure/133-RESEARCH.md (DEFER-119-A Mechanics: pin-helper-advisory job continue-on-error:true, both steps wrapped non-blocking; Pitfall 4 do-not-bundle)
  </read_first>
  <action>
    Land the 3-site retune as ONE atomic commit SEPARATE from the TOOL-04 re-pin (Pitfall 4 / D-09), with the exact attestation line `D-00a-EXCEPTION: TOOL-06 stderr slice-budget tuning, 3 call sites`. The commit contains ONLY `check-phase-{48,60,61}.mjs` — no sidecar JSON, no TOOL-05 artifact. In the SUMMARY, document the DEFER-119-A disposition in writing (D-05): it auto-falls-out of the TOOL-04 re-pin and REMAINS ACCEPTED-ADVISORY — its `pin-helper-advisory` job is `continue-on-error:true` with both the `--report` (`|| true`) and `--self-test` (`|| echo`) steps structurally non-blocking regardless of outcome, so no independent disposition is needed; state this so Phase 134's close-audit does not re-litigate it. Note (do NOT edit anything to prove it) that the TOOL-04 re-pin of `v1.7-audit-allowlist.json` may incidentally flip `--self-test`'s pass/fail but CANNOT change whether the advisory job blocks the build.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && git log -1 --format='%B' | grep -q 'D-00a-EXCEPTION: TOOL-06 stderr slice-budget tuning, 3 call sites' && test $(git show --stat HEAD --format='' | grep -Ec 'check-phase-(48|60|61)\.mjs') -eq 3 && ! git show --stat HEAD --format='' | grep -qE 'audit-allowlist|check-phase-128|\.github|_lib/'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1` for this commit contains the exact `D-00a-EXCEPTION: TOOL-06 stderr slice-budget tuning, 3 call sites` line.
    - `git show --stat HEAD` lists exactly the 3 `check-phase-{48,60,61}.mjs` files and NOTHING else (no sidecar JSON, no `_lib/`, no `check-phase-128`, no `.github/`) — proving separation from the TOOL-04 commit (D-09).
    - The SUMMARY documents DEFER-119-A remaining ACCEPTED-ADVISORY (continue-on-error:true, both steps non-blocking) with no independent action taken.
  </acceptance_criteria>
  <done>3-site retune landed as its own attested D-00a-exception commit (no bundling); DEFER-119-A disposition written for Phase 134.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (frozen-code numeric-literal edit) | Edits are single numeric-literal changes at 3 call sites in trusted repo validators; the shared function and all other logic are untouched. No network, auth, user input, or new executable path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-10 | Tampering | Editing the shared `_lib/exec-fail-detail.mjs` would ripple behavior across ~40 sites in 20 files | mitigate | Only the 3 CALL-SITE `{n:...}` literals change; the diff gate asserts `_lib/` is untouched |
| T-133-11 | Tampering | Frozen-surface blast radius / accidental bundling with TOOL-04 (D-09) | mitigate | Separate atomic commit with its own exact attestation; `git show --stat HEAD` asserts exactly the 3 files and no sidecar/other frozen file |
| T-133-12 | Repudiation | DEFER-119-A re-litigated at Phase 134 close | accept | Its ACCEPTED-ADVISORY disposition is documented in writing (D-05); the advisory job is continue-on-error:true, structurally non-blocking. Residual severity: low |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no supply-chain surface |
</threat_model>

<verification>
- `node -c` passes on all 3 files; each has exactly one changed line (only the `n:` value).
- No change to `_lib/exec-fail-detail.mjs`, `check-phase-128.mjs`, `regenerate-supervision-pins.mjs`, `*-audit-allowlist.json`, or `.github/`.
- Separate atomic commit carries the exact TOOL-06 D-00a attestation and touches only the 3 `check-phase-{48,60,61}.mjs` files (`git show --stat HEAD`).
- DEFER-119-A documented as remaining ACCEPTED-ADVISORY in the SUMMARY.
</verification>

<success_criteria>
The 3 HELPER-SPAWN-STDERR-01 stderr slice budgets are raised in one separate atomic D-00a-exception commit (TOOL-06 SC#3), with the shared lib and n:500 sites untouched, and DEFER-119-A's ACCEPTED-ADVISORY disposition documented for Phase 134.
</success_criteria>

<output>
Create `.planning/phases/133-chain-validator-tooling-debt-closure/133-04-SUMMARY.md` when done. Include the chosen `n` value, the separate-commit proof, and the DEFER-119-A disposition note.
</output>
