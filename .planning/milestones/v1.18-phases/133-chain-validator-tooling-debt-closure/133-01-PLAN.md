---
phase: 133-chain-validator-tooling-debt-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/133-chain-validator-tooling-debt-closure/133-REPIN-COORDINATES.md
autonomous: true
requirements: [TOOL-04]
must_haves:
  truths:
    - "Ground-truth freshness is re-confirmed: the pinned Android files are byte-unchanged since the v1.17 close, so v1.17-audit-allowlist.json line numbers are today's correct coordinates"
    - "A pin-by-pin old->new coordinate table exists for all 6 reconciliation targets (v1.4, v1.4.1, Group-S, v1.14, v1.15, v1.16)"
    - "C17 #12 fragmentation cases are resolved identity-preserving: one pre-fragmentation semantic occurrence maps to the N current fragment lines, no value-masking (D-01 interpretation)"
    - "The recon produces coordinates only — NO sidecar or code file is edited in this plan"
  artifacts:
    - path: ".planning/phases/133-chain-validator-tooling-debt-closure/133-REPIN-COORDINATES.md"
      provides: "Authoritative old->new {file,line} tables per sidecar for the Plan 02 edit, plus the fragmentation-expansion list"
      contains: "v1.16"
  key_links:
    - from: ".planning/phases/133-chain-validator-tooling-debt-closure/133-REPIN-COORDINATES.md"
      to: "scripts/validation/v1.17-audit-allowlist.json"
      via: "reason-text semantic match against the ground-truth current line map"
      pattern: "v1\\.17"
---

<objective>
Produce the authoritative pin-by-pin coordinate reconciliation tables that the Plan 02 re-pin edit will apply — WITHOUT editing any sidecar or code (D-01, recon-only, mirroring Phase 128's own 128-01 "recon eliminates remaining discovery" pattern). The re-pin is NOT a uniform -1 shift: there are 6 distinct reconciliation targets (v1.4, v1.4.1, the byte-identical Group-S = v1.5..v1.13 computed once, v1.14, v1.15, and the already-solved v1.16). This plan walks each stale pin against `scripts/validation/v1.17-audit-allowlist.json` (the git-confirmed ground truth) and records the correct current line, including the C17 #12 fragmentation expansions.

Purpose: Eliminate all coordinate discovery so the Plan 02 atomic D-00a-exception commit is a mechanical, reviewable, low-risk table application. Guessing coordinates in the edit plan is prohibited.
Output: `.planning/phases/133-chain-validator-tooling-debt-closure/133-REPIN-COORDINATES.md` — the coordinate tables consumed by Plan 02.

This plan touches ZERO frozen surfaces and ZERO docs content. It reads sidecars plus one ground-truth reference; it writes only a planning artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-chain-validator-tooling-debt-closure/133-CONTEXT.md
@.planning/phases/133-chain-validator-tooling-debt-closure/133-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm ground-truth freshness and assemble the solved/near-solved tables (v1.16, v1.15)</name>
  <files>.planning/phases/133-chain-validator-tooling-debt-closure/133-REPIN-COORDINATES.md</files>
  <read_first>
    - .planning/phases/133-chain-validator-tooling-debt-closure/133-RESEARCH.md (TOOL-04 section: root cause, "v1.17-audit-allowlist.json as ground truth", Case 1 v1.16, Case 2 v1.15; Appendix B full 35-pin v1.16 table; Appendix C identity map)
    - scripts/validation/v1.17-audit-allowlist.json (the ground-truth current-line map; note the reason-field shift-history preamble convention used for semantic matching)
    - scripts/validation/v1.16-audit-allowlist.json (source of the -1 case)
    - scripts/validation/v1.15-audit-allowlist.json (partial post-Phase-119 case)
  </read_first>
  <action>
    First run the ground-truth freshness guard (RESEARCH "Valid until" caveat): derive the union of every `file` across the four pin arrays (`supervision_exemptions`, `c7_knox_allowlist`, `c9_exemptions`, `safetynet_exemptions`) of all 14 sidecars, then confirm `git log --oneline a96f3b76..HEAD -- <that file set>` returns ZERO lines. `a96f3b76` is the v1.17-era anchor at/before the v1.17 close; zero output means `v1.17-audit-allowlist.json` line numbers are today's live-tree-correct coordinates. If it returns ANY line, STOP and record that v1.17 can no longer be trusted as ground truth (Plan 02 must recompute against live content) — per the pre-run check this is expected to be empty.

    Create `133-REPIN-COORDINATES.md`. Record the freshness-guard result at the top. Then author the v1.16 table: copy RESEARCH Appendix B's verified 35-pin old->new set exactly (the pure HYG-02 -1 shift across the 4 touched files; the 2 non-HYG-02 files already match v1.17 — mark them "no change"). Then author the v1.15 table per RESEARCH Case 2: the `00-enrollment-overview.md` pins move from the pre-Phase-125 51/53/83 to the current 65/67/97; the `20-android-app-install-investigation.md` pin is already 33 (no change); the glossary/capability-matrix/cobo/version-matrix single-line pins reconcile against v1.17's now-split set (flag any that require fragmentation expansion — Task 2 collects the master list). Record `line` values only; each pin's `reason` text stays verbatim in Plan 02.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && FILES=$(node -e "const fs=require('fs');const s=new Set();for(const v of ['v1.4','v1.4.1','v1.5','v1.6','v1.7','v1.8','v1.9','v1.10','v1.11','v1.12','v1.13','v1.14','v1.15','v1.16']){const j=JSON.parse(fs.readFileSync('scripts/validation/'+v+'-audit-allowlist.json'));for(const k of ['supervision_exemptions','c7_knox_allowlist','c9_exemptions','safetynet_exemptions']){(j[k]||[]).forEach(e=>e.file&&s.add(e.file))}}process.stdout.write([...s].join(' '))") && test -z "$(git log --oneline a96f3b76..HEAD -- $FILES)" && grep -q 'v1.16' .planning/phases/133-chain-validator-tooling-debt-closure/133-REPIN-COORDINATES.md && grep -q 'v1.15' .planning/phases/133-chain-validator-tooling-debt-closure/133-REPIN-COORDINATES.md</automated>
  </verify>
  <acceptance_criteria>
    - The freshness guard command prints no lines (all pinned files unchanged since a96f3b76); the artifact records this result explicitly.
    - `133-REPIN-COORDINATES.md` contains a v1.16 table with 35 `line` transitions matching RESEARCH Appendix B exactly (e.g. `docs/_glossary-android.md` 38->37, 90->89, ... 338->337), plus the 2 no-change files noted.
    - The artifact contains a v1.15 table covering all v1.15 pinned files, with the `00-enrollment-overview.md` 51/53/83 -> 65/67/97 transition and the 33 no-change pin recorded.
    - Every recorded transition changes only the `line` integer; no `reason` text is altered or invented.
  </acceptance_criteria>
  <done>Freshness guard passes and is logged; v1.16 (35-pin) and v1.15 tables are complete and value-accurate in `133-REPIN-COORDINATES.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the heavy targets (Group-S = v1.5..v1.13, v1.14, v1.4, v1.4.1) and finalize the fragmentation-expansion list</name>
  <files>.planning/phases/133-chain-validator-tooling-debt-closure/133-REPIN-COORDINATES.md</files>
  <read_first>
    - .planning/phases/133-chain-validator-tooling-debt-closure/133-RESEARCH.md (TOOL-04 Case 3, the "KNOWN RISK — C17 #12 fragmentation" note, Pitfalls 1 and 2, Appendix A --report output, Appendix C identity map showing Group-S byte-identity)
    - scripts/validation/v1.5-audit-allowlist.json (representative of Group-S; compute once, reuse for v1.6..v1.13)
    - scripts/validation/v1.14-audit-allowlist.json (unique, 1-line bump vs Group-S)
    - scripts/validation/v1.4-audit-allowlist.json and scripts/validation/v1.4.1-audit-allowlist.json (unique; 18 supervision + 4 safetynet each, no Knox/COPE)
    - scripts/validation/v1.17-audit-allowlist.json (ground-truth target lines)
  </read_first>
  <action>
    Confirm Group-S byte-identity first: assert `v1.5`..`v1.13` share byte-identical `supervision_exemptions`/`c7_knox_allowlist`/`c9_exemptions`/`safetynet_exemptions` arrays (per RESEARCH Appendix C), so one computation covers all 9. Then, for each pin in v1.5, v1.14, v1.4, and v1.4.1, match its `reason` semantic tail (the description after the shift-history preamble) to the corresponding pin in `v1.17-audit-allowlist.json` and record that pin's current `line` as the new coordinate (Pitfall 1: do NOT blind-subtract 1 — these sidecars drifted by years of growth, not the HYG-02 event).

    Apply the LOCKED D-01 interpretation for C17 #12 fragmentation (planning_context research_wrinkle 2, RESEARCH Pitfall 2): where a single pre-Phase-125 pin's semantic occurrence was split into N current fragment lines (each independently carrying a supervision/Knox/COPE match), map that ONE pin identity to the N current fragment lines — i.e. the raw pin COUNT for the affected sidecar may increase while semantic IDENTITY is preserved. This is mechanical fragmentation, NOT value-masking. Record every such expansion in a dedicated "Fragmentation expansions (identity-preserving)" section listing sidecar, original pin (file + old line + reason), and the full set of current fragment lines it expands to. This list feeds Plan 02's added-sibling-pin edits and the commit attestation. Finalize the artifact with a per-sidecar summary of expected post-fix pin counts (v1.16 stays 26/10/4/4; older sidecars may show higher supervision counts due to expansions).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && node -e "const fs=require('fs');const a=JSON.stringify(JSON.parse(fs.readFileSync('scripts/validation/v1.5-audit-allowlist.json')).supervision_exemptions);for(const v of ['v1.6','v1.7','v1.8','v1.9','v1.10','v1.11','v1.12','v1.13']){const b=JSON.stringify(JSON.parse(fs.readFileSync('scripts/validation/'+v+'-audit-allowlist.json')).supervision_exemptions);if(a!==b){console.error('Group-S mismatch at '+v);process.exit(1)}}console.log('Group-S byte-identical confirmed')" && grep -qi 'Fragmentation expansions' .planning/phases/133-chain-validator-tooling-debt-closure/133-REPIN-COORDINATES.md && grep -q 'v1.4.1' .planning/phases/133-chain-validator-tooling-debt-closure/133-REPIN-COORDINATES.md && grep -q 'v1.14' .planning/phases/133-chain-validator-tooling-debt-closure/133-REPIN-COORDINATES.md</automated>
  </verify>
  <acceptance_criteria>
    - Group-S byte-identity assertion passes (v1.5 supervision array == v1.6..v1.13).
    - The artifact contains complete old->new `line` tables for Group-S (one, reused across v1.5..v1.13), v1.14, v1.4, and v1.4.1 — every pin accounted for (v1.4/v1.4.1: 18 supervision + 4 safetynet each; Group-S/v1.14: 20 supervision + 10 Knox + 4 COPE + 4 safetynet).
    - A "Fragmentation expansions (identity-preserving)" section lists every 1-occurrence->N-line split with its source pin and all target fragment lines.
    - Each entry cites the matching v1.17 pin (source assertion), not arithmetic; no `reason` text is altered.
  </acceptance_criteria>
  <done>All 6 reconciliation targets have complete coordinate tables plus a finalized fragmentation-expansion list in `133-REPIN-COORDINATES.md`; no repo code/sidecar edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (recon-only) | This plan reads trusted repo JSON + runs read-only `git log`/`node` and writes one planning-doc artifact. No frozen surface, no code, no docs content, no network/auth/user-input path is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Tampering (integrity) | A mis-derived coordinate silently masking a real future regression once applied in Plan 02 | mitigate | Coordinates are derived by reason-text semantic match against git-confirmed-frozen ground truth (v1.17), not arithmetic; the freshness guard proves the ground truth is still valid; Plan 02's per-category harness green is the downstream check that a wrong coordinate cannot pass |
| T-133-02 | Tampering | Stale ground truth (v1.17 files changed since close) | mitigate | Explicit `git log a96f3b76..HEAD` freshness guard; nonzero output halts and forces live-content recompute |
| T-133-03 | Repudiation | Frozen-surface edit doctrine (D-00a) | accept | No frozen surface is edited here; `regenerate-supervision-pins.mjs`/`readAtClose` are NOT introduced (D-02/D-04) — recon is read-only. Residual severity: low |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no supply-chain surface |
</threat_model>

<verification>
- Freshness guard `git log --oneline a96f3b76..HEAD -- <pinned file union>` returns zero lines and is logged in the artifact.
- Group-S byte-identity confirmed programmatically.
- `133-REPIN-COORDINATES.md` holds complete old->new tables for all 6 targets + a fragmentation-expansion section.
- `git status` shows ONLY the planning artifact changed — no sidecar or `.mjs` file modified.
</verification>

<success_criteria>
Authoritative, ground-truth-derived coordinate tables for all 6 reconciliation targets (with identity-preserving fragmentation handling) exist and are ready for mechanical application in Plan 02. Zero frozen surfaces touched.
</success_criteria>

<output>
Create `.planning/phases/133-chain-validator-tooling-debt-closure/133-01-SUMMARY.md` when done. Note the freshness-guard result and the total pin-and-expansion counts per target so Plan 02 can cross-check.
</output>
