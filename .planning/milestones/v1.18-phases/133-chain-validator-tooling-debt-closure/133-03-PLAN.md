---
phase: 133-chain-validator-tooling-debt-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/133-chain-validator-tooling-debt-closure/133-ONE-N-ATTESTATION.md
autonomous: true
requirements: [TOOL-05]
must_haves:
  truths:
    - "The single-apex O(n) property of check-phase-128.mjs under CHECK_PHASE_NESTED=1 is verified and attested with evidence read directly from source"
    - "A Windows cold-clone run of check-phase-128.mjs is captured (PASS/FAIL/SKIP + wall-clock), non-authoritative per D-08"
    - "NO code is authored — no in-process memo, no on-disk cache; check-phase-128.mjs is read-only (D-06/D-07)"
    - "CARVE-2 is recorded as a Phase-134 hand-off note: the 'cache within a single apex' wording is already satisfied by the shipped NESTED guard; TOOL-05 is re-scoped to verify-and-attest"
  artifacts:
    - path: ".planning/phases/133-chain-validator-tooling-debt-closure/133-ONE-N-ATTESTATION.md"
      provides: "The O(n) attestation + captured Windows cold-clone evidence + CARVE-2 hand-off"
      contains: "CHECK_PHASE_NESTED"
  key_links:
    - from: ".planning/phases/133-chain-validator-tooling-debt-closure/133-ONE-N-ATTESTATION.md"
      to: "scripts/validation/check-phase-128.mjs"
      via: "source-cited line references (CHAIN_PHASES, NESTED short-circuit, subEnv spawn)"
      pattern: "CHECK_PHASE_NESTED"
---

<objective>
Ship TOOL-05 as verification + attestation ONLY — no code (D-06/D-07 locked; CARVE-2 re-scopes the requirement). Attest, with source-cited evidence, that the pre-existing `CHECK_PHASE_NESTED=1` guard already gives `check-phase-128.mjs` the required single-apex O(n) subprocess property, and capture a Windows cold-clone run as post-fix verification (non-authoritative per D-08; Linux GHA stays authoritative per D-03, and Phase 134's HARN-13 3-axis re-audit is the final exact-match).

Purpose: Close `O(n^2)-CHAIN-RUNNER-REMEDIATION-01` correctly — by proving the O(n) property already holds — instead of authoring a redundant memo/cache that would either be a frozen-apex edit (D-00a violation) or a false-pass-masking on-disk artifact (breaks HARN-13 cross-OS exact-match). There is NO within-apex redundancy to memoize and NO legal non-frozen apex to edit in Phase 133 (the next new apex `check-phase-129` is a Phase-134/HARN-12 deliverable).
Output: `133-ONE-N-ATTESTATION.md` — the attestation artifact + captured cold-clone evidence + CARVE-2 hand-off. No source file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-chain-validator-tooling-debt-closure/133-CONTEXT.md
@.planning/phases/133-chain-validator-tooling-debt-closure/133-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attest the O(n) mechanism from source (read-only)</name>
  <files>.planning/phases/133-chain-validator-tooling-debt-closure/133-ONE-N-ATTESTATION.md</files>
  <read_first>
    - scripts/validation/check-phase-128.mjs (CHAIN_PHASES array + throw-on-drift assertions; the run() NESTED short-circuit; the subEnv spawn passing CHECK_PHASE_NESTED='1' to every child; the V-128-AUDIT-HARNESS guard; the isPeer timeout split)
    - .planning/phases/133-chain-validator-tooling-debt-closure/133-RESEARCH.md (TOOL-05 section: "The O(n) mechanism, read directly", the DUAL-APEX note, "Next apex boundary")
    - .planning/phases/133-chain-validator-tooling-debt-closure/133-CONTEXT.md (D-06/D-07/D-08; CARVE-2)
  </read_first>
  <action>
    Author `133-ONE-N-ATTESTATION.md`. Attest, citing exact `check-phase-128.mjs` line references, that: (1) `CHAIN_PHASES` is a flat 80-integer array `48..127`, each phase once, enforced by module-load throw-on-drift assertions; (2) each `run()` returns `skipped` immediately when `CHECK_PHASE_NESTED==='1'` (no spawn), else spawns `node check-phase-{n}.mjs` exactly once with `subEnv={...process.env, CHECK_PHASE_NESTED:'1'}` — so every child receives NESTED and short-circuits its OWN chain-loop + audit-harness re-run; (3) net = a top-level non-nested run performs exactly 80 child spawns + 1 audit-harness spawn = 81 total (O(depth), not O(depth^2)); (4) the `isPeer = phaseNum >= 67` split only sets subprocess timeout (600s vs 300s), NOT spawn count — note this so it is not mistaken for a second optimization axis. Also record the DUAL-APEX boundary note (the standalone check-phase-N job AND linux-chain-ubuntu-latest BOTH run the full apex at top-level WITHOUT NESTED, intentionally and audited — the O(n) collapse applies WITHIN a single top-level descent only; this phase does not disturb it). State explicitly that NO code is authored and `check-phase-128.mjs` is read-only.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q 'CHECK_PHASE_NESTED' .planning/phases/133-chain-validator-tooling-debt-closure/133-ONE-N-ATTESTATION.md && grep -qiE 'O\(n\)|81|80' .planning/phases/133-chain-validator-tooling-debt-closure/133-ONE-N-ATTESTATION.md && test -z "$(git diff --name-only -- scripts/validation/)"</automated>
  </verify>
  <acceptance_criteria>
    - `133-ONE-N-ATTESTATION.md` cites specific `check-phase-128.mjs` lines for the CHAIN_PHASES array, the NESTED short-circuit, and the subEnv spawn.
    - It states the 81-spawn (80 children + 1 audit-harness) O(depth) conclusion and the isPeer=timeout-only clarification.
    - It records the DUAL-APEX do-not-disturb boundary note.
    - `git diff --name-only -- scripts/validation/` is EMPTY (no source edited).
  </acceptance_criteria>
  <done>The O(n) mechanism is attested from source with line-cited evidence; zero source files changed.</done>
</task>

<task type="auto">
  <name>Task 2: Capture Windows cold-clone verification + record CARVE-2</name>
  <files>.planning/phases/133-chain-validator-tooling-debt-closure/133-ONE-N-ATTESTATION.md</files>
  <read_first>
    - .planning/phases/133-chain-validator-tooling-debt-closure/133-RESEARCH.md (TOOL-05 "Windows evidence captured this session": the ~15s / 82 PASS baseline + the recommended cold-clone command)
    - .planning/phases/133-chain-validator-tooling-debt-closure/133-CONTEXT.md (D-08 "Windows cold-clone verification is a Phase-133 build step, non-authoritative"; CARVE-2 wording)
  </read_first>
  <action>
    Perform the cold-clone verification: `git clone --no-hardlinks . <scratch>/cold-133` (use the session scratchpad dir), then in that clone run `node scripts/validation/check-phase-128.mjs` (non-nested, no --verbose) and capture the PASS/FAIL/SKIP tuple and wall-clock time. Append the captured tuple to `133-ONE-N-ATTESTATION.md` and compare it against the RESEARCH baseline (~82 PASS, 0 FAIL, 1 SKIP, ~15s — a linear signature; an O(n^2) design would push wall-clock into minutes). Mark this evidence explicitly NON-AUTHORITATIVE (D-08): Linux GHA is authoritative (D-03) and HARN-13's 3-axis re-audit at Phase 134 is the final exact-match. Then record CARVE-2 as a Phase-134 hand-off note: "the 'cache within a single apex stops O(n^2)' wording is already satisfied by the shipped CHECK_PHASE_NESTED guard; the imagined residual within-apex O(n^2) does not exist; TOOL-05's success criterion is re-scoped to 'verify + attest the existing single-apex O(n) property cross-OS' — no cache code was or should be authored." Delete the scratch clone when done.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -qiE 'cold.?clone|--no-hardlinks' .planning/phases/133-chain-validator-tooling-debt-closure/133-ONE-N-ATTESTATION.md && grep -qiE 'PASS|SKIP' .planning/phases/133-chain-validator-tooling-debt-closure/133-ONE-N-ATTESTATION.md && grep -qi 'CARVE-2' .planning/phases/133-chain-validator-tooling-debt-closure/133-ONE-N-ATTESTATION.md && test -z "$(git diff --name-only -- scripts/validation/)"</automated>
  </verify>
  <acceptance_criteria>
    - A cold-clone (`git clone --no-hardlinks`) run of `check-phase-128.mjs` is executed and its PASS/FAIL/SKIP + wall-clock tuple is recorded in the artifact.
    - The captured result is compared to the linear-signature baseline and marked NON-AUTHORITATIVE per D-08 (Linux GHA authoritative; HARN-13 is final).
    - CARVE-2 is recorded verbatim in intent as a Phase-134 hand-off note.
    - `git diff --name-only -- scripts/validation/` remains EMPTY; the scratch clone is removed.
  </acceptance_criteria>
  <done>Windows cold-clone O(n) evidence captured and marked non-authoritative; CARVE-2 handed off; no code authored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (verify-only) | This plan reads one frozen source file, runs a read-only cold-clone of the repo, and writes one attestation artifact. No code, no frozen-surface edit, no network/auth/user-input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-07 | Tampering (integrity) | An on-disk result cache would risk a stale PASS surviving a tree change (false-pass masking -> HARN-12 violation) and diverge across HARN-13's 3 axes | mitigate | Rejected by design (D-07); this plan authors NO cache and NO in-process memo — attestation only, so the failure mode cannot be introduced |
| T-133-08 | Repudiation | Editing the frozen apex check-phase-128.mjs (D-00a violation) to add a memo | mitigate | check-phase-128.mjs is read-only here; the attestation is a planning artifact; no legal non-frozen apex exists in Phase 133 |
| T-133-09 | Information disclosure | Cold-clone into scratch could leak repo copy | accept | Clone lands in the session scratchpad and is deleted after capture; no secrets in repo. Residual severity: low |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no supply-chain surface |
</threat_model>

<verification>
- `133-ONE-N-ATTESTATION.md` cites check-phase-128.mjs lines for the O(n) mechanism (81-spawn conclusion, isPeer clarification, DUAL-APEX note).
- Windows cold-clone PASS/FAIL/SKIP + wall-clock captured and marked non-authoritative (D-08).
- CARVE-2 recorded as a Phase-134 hand-off note.
- `git diff --name-only -- scripts/validation/` EMPTY (no code authored, D-06/D-07 honored).
</verification>

<success_criteria>
The single-apex O(n) property is verified from source and cross-checked by a Windows cold-clone run, attested in a planning artifact with zero code authored; CARVE-2 re-scoping is handed off to Phase 134.
</success_criteria>

<output>
Create `.planning/phases/133-chain-validator-tooling-debt-closure/133-03-SUMMARY.md` when done. Reference the attestation artifact and the CARVE-2 hand-off.
</output>
