---
phase: 131-recipe-2-shared-ipad-full-provisioning
plan: 02
type: execute
wave: 2
depends_on: [131-01]
files_modified:
  - docs/recipes/02-shared-ipad-full-provisioning.md
autonomous: true
requirements: [IPAD-01, IPAD-02, IPAD-03]
must_haves:
  truths:
    - "A per-role layered-configuration worked example shows a device-group baseline (common Wi-Fi + all apps Required) plus a user-group overlay (per-role home-screen layout + show/hide-apps allow-list) for two healthcare roles (Nurse / Clinician)"
    - "ALL apps are assigned to the DEVICE group as Required — the recipe never assigns apps per-role to user groups; per-role differentiation is layout + show/hide allow-lists only (T-2)"
    - "The conflict warning uses the three verbatim first-party phrases 'can't be pre-determined', 'first setting assigned', and 'chosen by the operating system' — never 'last-writer-wins' (T-3)"
    - "Wi-Fi / VPN / Certificate baseline placement is stated as platform-FORCED (Not applicable to user groups), not a best-practice preference (C3)"
    - "The guest decision block records the real inverted 'Block Shared iPad temporary sessions' setting (guest-on = Block No/Not configured), never a fabricated positive 'enabled' key (B3)"
    - "Verification is on-device (sign in as each role, observe layout/app set) — no Company Portal check, no Intune user-status-report check (C5)"
    - "The full corpus C17 run (--verbose) exits 0 including the new recipe file"
  artifacts:
    - path: "docs/recipes/02-shared-ipad-full-provisioning.md"
      provides: "Layered-config steps (applicability table, guest decision, device baseline + user overlay, conflict warning) + Verification + Configuration-Caused Failures + See Also"
      contains: "## Verification"
      min_lines: 180
  key_links:
    - from: "docs/recipes/02-shared-ipad-full-provisioning.md"
      to: "docs/admin-setup-ios/04-configuration-profiles.md"
      via: "full applicability-matrix link (C1)"
      pattern: "04-configuration-profiles"
    - from: "docs/recipes/02-shared-ipad-full-provisioning.md"
      to: "docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md"
      via: "session/lifecycle link (A4)"
      pattern: "09-shared-ipad-lifecycle"
    - from: "docs/recipes/02-shared-ipad-full-provisioning.md"
      to: "scripts/validation/c17-eee-contract.mjs"
      via: "green-gate verification"
      pattern: "c17-eee-contract"
---

<objective>
Complete `docs/recipes/02-shared-ipad-full-provisioning.md`: author the per-role layered-
configuration steps (IPAD-03) — the trimmed applicability reference table, the guest/temporary-
session decision block, the device-group baseline, the user-group overlay, and the conflict-
warning callout — then close with on-device Verification, Configuration-Caused Failures, and See
Also, and pass the C17 green gate on the full corpus (IPAD-01 completion).

This plan continues the single Markdown file started in 131-01; it edits the same file and
therefore runs in wave 2 after 131-01 completes. No application code, no schema.

Purpose: Deliver the device-baseline + user-overlay LAYERING pattern (structurally distinct from
the sibling recipe's non-converging branches) with the never-set-the-same-setting-twice conflict
warning, and prove the finished recipe green against the frozen C17 validator.
Output: The completed recipe file; C17 self-test AND full-corpus `--verbose` both exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-CONTEXT.md
@.planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-RESEARCH.md
@.planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-PATTERNS.md
@docs/recipes/01-shared-windows-avd-client.md
@.planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the layered-config steps — applicability reference table, guest decision block, device-group baseline, user-group overlay, and the conflict-warning callout (IPAD-03, IPAD-01)</name>
  <files>docs/recipes/02-shared-ipad-full-provisioning.md</files>
  <read_first>
    - docs/recipes/02-shared-ipad-full-provisioning.md (the file from 131-01 — append the layered-config steps after the VPP step)
    - docs/recipes/01-shared-windows-avd-client.md (sibling: `### Step N:` pattern, what-breaks split-blockquote callouts)
    - .planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-RESEARCH.md (applicability table lines 258-266; app-type table lines 271-280; guest Pattern 1 lines 156-164; conflict phrases T-3; Pitfall 6 breadcrumb caution lines 248-252)
    - .planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-PATTERNS.md (Pattern Assignments #3 boolean-as-prose, #7 blockquote splitting, #8 RE-110/OU-07 anchors)
    - docs/admin-setup-ios/04-configuration-profiles.md (RE-110 — full applicability/home-screen-layout matrix owner; anchors Wi-Fi/Home Screen Layout/Show or Hide Apps, C1)
    - docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md (OU-07 — session/lifecycle; recipe supersedes its training-sourced Intune values, A4)
  </read_first>
  <action>
    Append the layered-config steps, honoring the device→user spine (device baseline earlier, user
    overlay as a late step before Verification, C5). This recipe LAYERS (baseline + overlay applying
    simultaneously) — it does NOT branch; do not copy the sibling's `### Step 5a` / `### Step 5b`
    fork structure.

    Trimmed applicability reference table (C1): a 4-8 row table scoped ONLY to the setting types the
    worked example touches (from RESEARCH lines 258-266) — Home screen layout (Device / User); Block
    Shared iPad temporary sessions (Device / Not applicable); other device restrictions (Device /
    User); Wi-Fi, VPN, Certificate (Device / Not applicable); Email (Device / User, with an inline ⚠
    pointing to the anti-feature table — unsupported per Known Limitations, T-5). Link RE-110
    (docs/admin-setup-ios/04-configuration-profiles.md) for the full matrix; do NOT grow this table
    toward RE-110's full set (keeps C17 #11 dormant).

    Guest / temporary-session decision block (B3), at the device-restrictions step: STD-05 Case-1
    BOOLEAN rendered as if/then PROSE (D-04 rule 3 — no branch table, no H3 branch headings). One-
    sentence `> **Ask the admin:**` lead-in asking whether this Shared iPad should allow guest
    (temporary) sessions without a Managed Apple Account; then, outside the blockquote (or in a
    second blank-line-separated `>` run), state: temporary sessions are allowed by default; to
    disable them, set "Block Shared iPad temporary sessions" to Yes in an iOS/iPadOS device-
    restrictions profile assigned to the device group; leaving it No or Not configured keeps guest
    sign-in available (the inverted-polarity default). The recorded value MUST be the real inverted
    Block setting — never fabricate a positive "enabled" key. Do NOT assert an exact Settings Catalog
    category/breadcrumb for this setting (Pitfall 6) — "an iOS/iPadOS device restrictions profile" is
    as specific as the sourcing supports; a one-line in-tenant verify note is acceptable. Also state
    the third mode — the enrollment-profile "Require Shared iPad temporary session only" (guest-only,
    no Managed Apple ID sign-in) — as a distinct, out-of-scope-but-named mode (B4), on a different
    surface than this Block toggle; keep them separate.

    Device-group baseline step (C2 / C3): common Wi-Fi assigned to the device group, stated as
    platform-FORCED — Wi-Fi / VPN / Certificate are Not applicable to user groups, so device-group
    placement is a constraint, not a preference (C3). All apps (device-licensed VPP / LOB) assigned
    Required to the device group (T-2 — never per-role app assignment to user groups). Plus common
    device restrictions on the device group.

    User-group overlay step (C2), the late step before Verification: two roles, healthcare Nurse and
    Clinician (Claude's discretion on the concrete app sets / layouts). Per-role home-screen layout
    and per-role show/hide-apps allow-list assigned to the user groups. Home-screen layout is user-
    applicable for per-role differentiation AND device-applicable on device-group assignment — do NOT
    overstate it as "user-only, never device" (C2 trim). Frame layout as per-signed-in-ROLE, not per-
    physical-user persistence. Render the worked example as ONE contiguous table artifact at this
    step. State that temporary/guest sessions receive device-group assignments only — user overlays
    never apply in guest mode (C5).

    Conflict-warning callout (C4) at the layered-config step: a `> **What breaks if misconfigured:**`
    callout (NOT an STD-05 block — this is a hazard, not an admin decision). Its WHY MUST use the
    three VERBATIM first-party phrases (T-3): a conflicting value "can't be pre-determined"; Intune
    applies "the first setting assigned"; when both group types apply, the outcome is "chosen by the
    operating system." Never phrase this as last-writer-wins or any deterministic precedence. This
    content is ~330 chars as one run — word-split it across blank-line-separated `>` paragraphs (one
    short fact per paragraph) so each contiguous run stays ≤200 chars for C17 #12.
  </action>
  <verify>
    <automated>grep -qi "can't be pre-determined" docs/recipes/02-shared-ipad-full-provisioning.md && grep -qi "first setting assigned" docs/recipes/02-shared-ipad-full-provisioning.md && grep -qi "chosen by the operating system" docs/recipes/02-shared-ipad-full-provisioning.md && ! grep -qi "last-writer" docs/recipes/02-shared-ipad-full-provisioning.md && grep -qi "Block Shared iPad temporary sessions" docs/recipes/02-shared-ipad-full-provisioning.md && node scripts/validation/c17-eee-contract.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - The worked example shows a device-group baseline (common Wi-Fi + all apps Required) and a user-group overlay (per-role home-screen layout + show/hide allow-list) for Nurse and Clinician
    - All apps are Required to the DEVICE group; no per-role app assignment to user groups appears (T-2)
    - The conflict warning contains all three verbatim phrases "can't be pre-determined", "first setting assigned", "chosen by the operating system"; the file contains no "last-writer" (T-3)
    - Wi-Fi/VPN/Certificate baseline placement is stated as platform-forced (Not applicable to user groups), not a preference (C3)
    - The guest decision block records the inverted "Block Shared iPad temporary sessions" setting (guest-on = No/Not configured), with no fabricated positive "enabled" key (B3)
    - The trimmed applicability table has 4-8 rows scoped to the worked example; the "Require Shared iPad temporary session only" third mode is named as out-of-scope (B4)
    - No exact Settings Catalog breadcrumb is asserted for the Block setting beyond "an iOS/iPadOS device restrictions profile" (Pitfall 6)
    - node scripts/validation/c17-eee-contract.mjs --self-test exits 0
  </acceptance_criteria>
  <done>The layered-config steps are authored with the applicability table, guest decision, baseline+overlay, and the verbatim conflict warning; every contiguous blockquote run is ≤200 chars; C17 self-test exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Author Verification + Configuration-Caused Failures + See Also, then pass the full-corpus C17 green gate (IPAD-01 completion)</name>
  <files>docs/recipes/02-shared-ipad-full-provisioning.md</files>
  <read_first>
    - docs/recipes/02-shared-ipad-full-provisioning.md (the file — append the closing sections)
    - docs/recipes/01-shared-windows-avd-client.md (sibling: Verification `- [ ]` idiom lines 206-223, Configuration-Caused Failures table lines 225-234, See Also lines 236-243)
    - .planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-CONTEXT.md (C5 on-device verification constraint; the Configuration-Caused-Failures candidate list)
    - .planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-PATTERNS.md (Pattern Assignments #5 verification idiom, #6 failures table, #7 blockquote splitting; C17 gate note)
    - scripts/validation/c17-eee-contract.mjs (the green gate — DO NOT EDIT; run only)
  </read_first>
  <action>
    Append the closing sections.

    `## Verification`: a single FLAT `- [ ]` checklist (no bold pseudo-heading branch groups — this
    recipe has no forks). Per C5, verification MUST be on-device: sign in as each role (Nurse,
    Clinician) and observe that the correct home-screen layout and app set appear; confirm the device
    is Supervised / Shared iPad enrolled with no user affinity; confirm device-licensed apps are
    present before any user signs in. Do NOT include a Company Portal check or an Intune user-status-
    report check — user-assigned policy status does not surface in Intune reports (C5). Include a
    guest-session check reflecting that temporary sessions receive device-group assignments only (the
    user overlays do not apply in guest mode).

    `## Configuration-Caused Failures`: a table (Misconfiguration | Symptom | Runbook) with the
    highest-value rows, each linking back to its step — VPP assigned Available or user-licensed
    instead of Required + device-licensed (app absent at sign-in); an email profile assigned (an
    assignment error occurs, T-5); the guest Block toggle left in the wrong polarity (guest sessions
    unexpectedly on or off); the same setting configured on both the device group and a user group
    (outcome is OS-arbitrated / "chosen by the operating system," not deterministic — links the
    conflict-warning step).

    `## See Also`: link the STD-05 spec (docs/_standards/EEE-SOP-standard.md) plus the five cross-
    link owner docs — RE-109 (03-ade-enrollment-profile), RE-110 (04-configuration-profiles), RE-111
    (05-app-deployment), OU-06 (08-managed-apple-account-provisioning), OU-07 (09-shared-ipad-
    lifecycle).

    Then run the green gate: `node scripts/validation/c17-eee-contract.mjs --self-test` (must exit 0)
    AND the full-corpus `node scripts/validation/c17-eee-contract.mjs --verbose` (must exit 0,
    including the new recipe file). Fix any failures in-file: split any contiguous top-level `>` run
    exceeding 200 chars by inserting a blank line (C17 #12); confirm the body opens NO fenced code
    block (values render as tables, not code fences); confirm no single table exceeds 25 data rows
    (the anti-feature and applicability tables are both well under, #11). Do NOT edit the validator or
    any other file to make the gate pass.
  </action>
  <verify>
    <automated>grep -q "## Verification" docs/recipes/02-shared-ipad-full-provisioning.md && grep -q "## Configuration-Caused Failures" docs/recipes/02-shared-ipad-full-provisioning.md && grep -q "## See Also" docs/recipes/02-shared-ipad-full-provisioning.md && ! grep -qE '^```' docs/recipes/02-shared-ipad-full-provisioning.md && node scripts/validation/c17-eee-contract.mjs --self-test && node scripts/validation/c17-eee-contract.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - The recipe contains '## Verification', '## Configuration-Caused Failures', and '## See Also'
    - Verification is a flat on-device `- [ ]` checklist (sign in as each role, observe layout/app set); it contains no Company Portal item and no Intune user-status-report item (C5)
    - The Configuration-Caused Failures table includes the email-profile-assignment error (T-5) and the device+user same-setting conflict (OS-arbitrated) rows, each linking its step
    - See Also links all five owner docs (RE-109/110/111, OU-06/07) plus the STD-05 spec
    - No line in the recipe body opens a fenced code block (`^```` matches nothing)
    - node scripts/validation/c17-eee-contract.mjs --self-test exits 0
    - node scripts/validation/c17-eee-contract.mjs --verbose exits 0 including docs/recipes/02-shared-ipad-full-provisioning.md
  </acceptance_criteria>
  <done>The recipe is complete end-to-end; C17 self-test and full-corpus --verbose both exit 0 with the new file included.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Docs-only phase: no executable code, no auth flow, no data flow, no user-input handling. The only
output is static Markdown rendered to .docx downstream. No trust boundary is crossed by this work.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-03 | Tampering (Integrity) | Layered-config accuracy | mitigate | Primary integrity risk is a factually-wrong instruction that misconfigures a tenant — regressing T-2 (per-role app assignment), T-3 (deterministic conflict framing), or B3 (guest polarity). Mitigated by the first-party-verified fact base (131-CONTEXT.md / 131-RESEARCH.md, verified 2×) and the trap-specific acceptance criteria on Task 1. |
| T-131-04 | Tampering | C17 corpus gate | accept | The finished recipe must pass the frozen C17 validator; the validator is run, never edited (explicitly out of scope). Accepting the gate as authoritative. |
| T-131-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (docs-only) — supply-chain surface is nil. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `node scripts/validation/c17-eee-contract.mjs --self-test` exits 0.
- `node scripts/validation/c17-eee-contract.mjs --verbose` exits 0 on the full corpus including the new recipe.
- IPAD-03 delivered: device-baseline + user-overlay worked example (Nurse/Clinician) with the verbatim conflict warning; T-2 and T-3 honored.
- IPAD-01 completed: on-device Verification, Configuration-Caused Failures, See Also all present.
- No fenced code block in the recipe body; every contiguous top-level blockquote run ≤200 chars.
</verification>

<success_criteria>
- `docs/recipes/02-shared-ipad-full-provisioning.md` is complete end-to-end.
- IPAD-03 delivered: per-role layered example (device baseline + user overlay), never-set-twice conflict warning with the three verbatim phrases (T-3), apps device-group-only (T-2), forced Wi-Fi placement (C3), inverted guest polarity (B3).
- IPAD-01 completed: on-device verification (C5), failures table, See Also with all five owner docs.
- C17 self-test AND full-corpus --verbose both exit 0.
</success_criteria>

<output>
Create `.planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-02-SUMMARY.md` when done.
</output>
