---
phase: 129-device-recipe-doc-class-foundation
plan: 02
type: execute
wave: 2
depends_on: [129-01]
files_modified:
  - docs/_templates/recipe-template.md
autonomous: true
requirements: [CLASS-02]
must_haves:
  truths:
    - "docs/_templates/recipe-template.md exists with doc_type: Guide, TEMPLATE-SENTINEL last_verified: 1970-01-01, platform: all, applies_to: [FILL-IN] (D-06)"
    - "The template's `## Summary` is the first H2 (nothing precedes it), opens with the D-13 concrete end-state sentence, and totals ≥30 words so C17 #4 and #5 pass on the template"
    - "The template carries a `## Unsupported and Anti-Feature Callouts` section with columns `| Feature | Why it's unsupported / what breaks | Do this instead |` (hard-required container for AVD-03/IPAD-02), placed between `## Prerequisites` and `## Steps` per the D-06 skeleton order (D-06)"
    - "The template carries ONE HTML-comment-wrapped, delete-marked worked-example region with three examples in order branching → enumerable → free-value, using ### / bold labels only and NEVER `## ` inside the comment (D-08)"
    - "The Decision Record ledger section is absent (cut per D-07); the `> **Version gate:**` blockquote is replaced by a `> **Scope:**` blockquote ≤200 chars (D-06)"
    - "C17-green: --self-test 4/4 PASS and --verbose reports 230 files / 0 violations (template auto-enrolls via its doc_id key; up from the 229/0 baseline) — no C17 script edit, no allowlist entry (D-10)"
  artifacts:
    - path: "docs/_templates/recipe-template.md"
      provides: "Canonical C17-green, EEE-conformant, TEMPLATE-SENTINEL'd recipe scaffold with worked decision-point examples"
      contains: "## Unsupported and Anti-Feature Callouts"
  key_links:
    - from: "docs/_templates/recipe-template.md"
      to: "docs/_standards/EEE-SOP-standard.md (STD-05 + D-02 ruling)"
      via: "template comment/See Also cites the STD-05 decision-point spec and the D-02 Guide ruling"
      pattern: "STD-05"
    - from: "docs/_templates/recipe-template.md (doc_id key)"
      to: "C17 enrollment"
      via: "opt-in-by-doc_id enrollment counts the template in the corpus (229 → 230)"
      pattern: "doc_id:"
---

<objective>
Create the canonical `docs/_templates/recipe-template.md` — an EEE-conformant, C17-green, TEMPLATE-SENTINEL'd scaffold that instantiates the STD-05 decision-point block format authored in Plan 01, with three worked decision-point examples. This is the file Phases 130/131 copy to author both recipes without further design decisions (ROADMAP SC3).

Purpose: Give recipe authors a scaffold that mechanically teaches the composite decision-point shape (the ONLY place the shape is taught by example — C17 cannot enforce it), so the two recipes land consistently and C17-green on the first pass.
Output: New `docs/_templates/recipe-template.md`, enrolled and 0-violation under C17 (corpus 230/0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-device-recipe-doc-class-foundation/129-CONTEXT.md
@.planning/phases/129-device-recipe-doc-class-foundation/129-RESEARCH.md
@.planning/phases/129-device-recipe-doc-class-foundation/129-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recipe-template.md from the admin-template.md base with the D-06/D-07/D-08/D-13 diffs</name>
  <files>docs/_templates/recipe-template.md</files>
  <read_first>
    - docs/_templates/admin-template.md — the full 90-line base skeleton (D-06 base): frontmatter block, HTML comment header, EEE visible-header line, `## Summary`, `## Prerequisites`, `## Steps` with `### Step N` + per-step `> **What breaks if misconfigured:**` callouts, `## Verification`, `## Configuration-Caused Failures` table, `## See Also`. Copy this shape verbatim, then apply the diffs.
    - docs/_standards/EEE-SOP-standard.md — the NOW-EDITED file from Plan 01: read the `## Admin Decision-Point Block Format (STD-05)` section (the spec this template instantiates), the D-02 Device Recipe ruling bullet, and the D-05 visible-header house style (lines ~82–84). The template's worked examples must match the STD-05 case shapes and column headers exactly.
    - .planning/phases/129-device-recipe-doc-class-foundation/129-CONTEXT.md — D-06 (skeleton + placement-locked-after-Summary + `> **Scope:**` blockquote + Unsupported/Anti-Feature BEFORE Steps), D-07 (Decision Record ledger CUT), D-08 (THREE worked examples in ONE HTML-comment region; ###/bold only; enumerable example must be synthetic/non-colliding), D-13 (Summary opener sentence + ≥30-word floor).
    - .planning/phases/129-device-recipe-doc-class-foundation/129-PATTERNS.md — the `recipe-template.md` section: the full diff list (items 1–8) including the exact Summary opener text, the `## Unsupported and Anti-Feature Callouts` column headers, and the full three-example HTML-comment region shape.
    - .planning/phases/129-device-recipe-doc-class-foundation/129-RESEARCH.md — C17 Mechanics: which assertions fire on TEMPLATE-SENTINEL files (#4 Summary-first-H2 and #5 ≥30-word Summary FIRE; only #9/#12 are skipped); Pitfalls #2/#3/#4 (blockquote ≤200 chars for copy-safety, nothing before Summary, no `## ` inside the HTML comment).
    - .planning/REQUIREMENTS.md — AVD-01..05 and IPAD-01..04 field names, to confirm the synthetic enumerable-example subject does NOT collide with any of them.
  </read_first>
  <action>
    Create docs/_templates/recipe-template.md by copying admin-template.md's structure verbatim, then applying these diffs — ALL recipe-specific additions placement-locked AFTER `## Summary` (D-06); nothing may precede the Summary H2 or C17 #4 fires (Pitfall #3).

    The D-06 LOCKED skeleton order is authoritative and MUST be reproduced exactly: frontmatter → EEE header → H1 → `## Summary` → `> **Scope:**` blockquote → `## Prerequisites` → `## Unsupported and Anti-Feature Callouts` → `## Steps` → `## Verification` → `## Configuration-Caused Failures` → `## See Also`. Note: `## Unsupported and Anti-Feature Callouts` comes BEFORE `## Steps`, not after.

    1. FRONTMATTER: keep the TEMPLATE-SENTINEL block. `doc_id: RE-[FILL-IN]`, `status: Draft`, `owner: [FILL-IN]`, `doc_type: Guide` (unchanged — already correct for the D-02 ruling), `platform: all`, `last_verified: 1970-01-01 # TEMPLATE-SENTINEL`, `review_by: YYYY-MM-DD`, `applies_to: [FILL-IN]` (generic — REPLACES the APv1 | APv2 | both pipe-list; C17 never inspects this key), `audience: admin`. Update the HTML comment header's usage rules to describe a recipe (not "ADMIN SETUP GUIDE") and to instruct authors to set `platform` to the correct D1-mapped value (Windows | macOS | iOS | Android | Linux | all) and `applies_to` to their recipe's scope.

    2. EEE VISIBLE-HEADER LINE: keep the `**Platform:** ... · **Doc Type:** Guide · **Doc ID:** RE-[NNN] · **Status:** Draft` line using `·` (U+00B7 middle-dot) separators, Platform then Doc Type first (C17 #6/#7).

    3. `## Summary` (must be the FIRST H2): open with the D-13 mandatory end-state sentence: "Following this recipe yields [a concrete, named end-state — e.g., a self-deploying Entra-joined shared Windows AVD-client device], provisioned end-to-end from zero through Intune." Then 1–2 descriptive bracket sentences (target platform, admin role/permissions). Keep the total Summary body ≥30 words — C17 #5 fires on templates.

    4. Immediately AFTER the Summary body, add ONE HTML-comment-wrapped, delete-marked worked-example region (D-08). Open it with a delete instruction (e.g. "WORKED DECISION-POINT EXAMPLES (delete this whole comment block before publishing)"). Inside, use `###` or bold pseudo-labels ONLY — NEVER a `## ` line (HTML comments are NOT fence-masked; a `## ` here would break C17 #4's Summary-first-H2 scan — Pitfall #4). Three examples IN THIS ORDER:
       - Example 1 — Branching decision (Case 1): a ONE-SENTENCE `> **Ask the admin:**` lead-in ≤200 chars, then a blank line (mandatory — D-02), then the 4-column table `| Option | When to choose | Consequence if wrong | Branch |` with Branch cells linking to branch step anchors. Use the Kiosk-vs-Shared-PC subject.
       - Example 2 — Enumerable-value decision (Case 2): a ONE-SENTENCE `> **Ask the admin:**` lead-in ≤200 chars, blank line, then the 3-column table `| Option | When to choose | Recorded as |`. The subject MUST be SYNTHETIC and NON-COLLIDING — do NOT use DeletionPolicy, EnableSharedPCMode, AccountModel, RestrictLocalStorage, ResetAppOnCloseOnly/AfterConnection/OnIdle, InactiveThreshold, MaintenanceStartTime, QuotaSize, AutoSubscription, or any other field named in AVD-01..05 / IPAD-01..04. A synthetic `RetentionDays`-style illustrative field is fine; label it illustrative inside the region.
       - Example 3 — Free-value prompt (Case 3, no table needed): a ONE-SENTENCE `> **Ask the admin:**` lead-in ≤200 chars, alone, with a note that no table is needed (a naming-prefix prompt is a good subject).
       Every lead-in must be ≤200 chars so authors who copy an example into a live (non-sentinel) recipe survive C17 #12 (Pitfall #2).

    5. `> **Scope:**` blockquote: replace admin-template's `> **Version gate:**` blockquote with a `> **Scope:**` blockquote (≤200 chars — keep compliant even though the template is #12-sentinel-exempt, so copies stay safe). Place it after the worked-example region, before `## Prerequisites`.

    6. Carry the admin-template shapes verbatim (D-06 inherits wholesale) in this D-06 skeleton order: `## Prerequisites` → `## Unsupported and Anti-Feature Callouts` (item 7) → `## Steps` with `### Step N: [action]` + per-step `> **What breaks if misconfigured:**` callouts → `## Verification`. The Unsupported/Anti-Feature section is interleaved between Prerequisites and Steps — do NOT place Prerequisites and Steps adjacent.

    7. NEW required section `## Unsupported and Anti-Feature Callouts` (hard-required container for AVD-03/IPAD-02, D-06): a table with columns exactly `| Feature | Why it's unsupported / what breaks | Do this instead |` and one bracketed placeholder data row. Place `## Unsupported and Anti-Feature Callouts` BETWEEN `## Prerequisites` and `## Steps` — it comes BEFORE `## Steps`, per D-06's LOCKED skeleton order (frontmatter → EEE header → H1 → Summary → Scope → Prerequisites → Unsupported/Anti-Feature → Steps → Verification → Configuration-Caused Failures → See Also).

    8. `## Configuration-Caused Failures` table (verbatim admin-template columns `| Misconfiguration | Symptom | Runbook |`) and `## See Also`. Do NOT add a `## Decision Record` ledger section (CUT — D-07). In the HTML comment header or `## See Also`, cite the STD-05 decision-point spec and the D-02 Guide ruling in EEE-SOP-standard.md so authors know where the format is defined.

    DO NOT edit scripts/validation/c17-eee-contract.mjs and DO NOT add any allowlist entry — the template auto-enrolls via its doc_id key (D-10).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs --self-test && node scripts/validation/c17-eee-contract.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/c17-eee-contract.mjs --self-test` exits 0 with 4/4 PASS.
    - `node scripts/validation/c17-eee-contract.mjs --verbose` exits 0 reporting 230 files checked, 0 files with violations, 0 total violations (up from 229 — the new template enrolled via its doc_id key; every per-assertion counter #1..#13 reads 0).
    - `grep -n "^## " docs/_templates/recipe-template.md` shows `## Summary` as the FIRST H2 line, and no `## ` line appears inside the HTML comment region.
    - The D-06 H2 order holds: with `grep -n "^## Prerequisites"`, `grep -n "^## Unsupported and Anti-Feature Callouts"`, and `grep -n "^## Steps"` on docs/_templates/recipe-template.md, the line number of `## Unsupported and Anti-Feature Callouts` is STRICTLY GREATER than `## Prerequisites` and STRICTLY LESS than `## Steps` (i.e. Prerequisites < Unsupported/Anti-Feature < Steps).
    - `grep -c "^last_verified: 1970-01-01" docs/_templates/recipe-template.md` returns 1 (TEMPLATE-SENTINEL present); `grep -c "^doc_type: Guide" docs/_templates/recipe-template.md` returns 1.
    - The Summary opens with `Following this recipe yields` and the Summary body is ≥30 words (C17 #5 passes, confirmed by the 0-violation run).
    - The file contains the exact column headers `| Feature | Why it's unsupported / what breaks | Do this instead |`, `| Option | When to choose | Consequence if wrong | Branch |`, and `| Option | When to choose | Recorded as |`.
    - The HTML-comment worked-example region contains exactly three `> **Ask the admin:**` lead-ins, each ≤200 chars, in order branching → enumerable → free-value; the enumerable example uses no AVD-01..05/IPAD-01..04 field name (grep confirms none of DeletionPolicy|EnableSharedPCMode|AccountModel|RestrictLocalStorage|InactiveThreshold|MaintenanceStartTime|QuotaSize|AutoSubscription|ResetApp appears in the enumerable table).
    - `grep -c "## Decision Record" docs/_templates/recipe-template.md` returns 0 (ledger cut).
    - `grep -c "Version gate" docs/_templates/recipe-template.md` returns 0 and `grep -c "> \*\*Scope:\*\*" docs/_templates/recipe-template.md` returns 1.
    - The template references `STD-05` and the D-02 Guide ruling (grep `STD-05` returns ≥1).
    - `git diff --quiet scripts/validation/c17-eee-contract.mjs` exits 0 (C17 script untouched); no `-audit-allowlist.json` file changed.
  </acceptance_criteria>
  <done>recipe-template.md exists, is C17-green and enrolled (corpus 230/0, self-test 4/4), Summary-first with the D-13 opener, has the Unsupported/Anti-Feature container placed between Prerequisites and Steps (D-06 order), the three-example comment region (###/bold only, non-colliding enumerable), a `> **Scope:**` blockquote, no Decision Record ledger, cites STD-05 — and the C17 script and allowlists are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan authors one Markdown template file. No runtime, network, auth, or untrusted-input surface exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-04 | Denial (regression) | Corpus C17 greenness on template enrollment | mitigate | The template auto-enrolls via doc_id; C17 #4/#5/#10 fire on templates — acceptance criteria assert --verbose 230/0 and Summary-first/≥30-word/D1-mapped-platform conformance, so any non-conformance blocks the plan |
| T-129-05 | Tampering | Copy-forward safety of worked-example blockquotes | mitigate | Every `> **Ask the admin:**` lead-in ≤200 chars so authors copying an example into a live non-sentinel recipe survive C17 #12 (Pitfall #2) |
| T-129-02 | Tampering | scripts/validation/c17-eee-contract.mjs + audit allowlists (shared frozen surfaces) | mitigate | D-10: no C17 edit, no allowlist entry (opt-in-by-doc_id enrollment is automatic); acceptance criterion proves the script is byte-unchanged |
| T-129-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A) — no supply-chain surface |
</threat_model>

<verification>
- `node scripts/validation/c17-eee-contract.mjs --self-test` → 4/4 PASS, exit 0.
- `node scripts/validation/c17-eee-contract.mjs --verbose` → 230 files checked, 0 violations, exit 0 (up from 229 baseline).
- `git diff --quiet scripts/validation/c17-eee-contract.mjs` → exit 0.
- Template is Summary-first, TEMPLATE-SENTINEL'd, has the Unsupported/Anti-Feature container placed between Prerequisites and Steps (D-06 order), the three-example comment region, a `> **Scope:**` blockquote, no Decision Record ledger, and cites STD-05.
</verification>

<success_criteria>
- CLASS-02 satisfied: a canonical, EEE-conformant, C17-green, TEMPLATE-SENTINEL'd `docs/_templates/recipe-template.md` exists with a worked decision-point block example (three cases) and the anti-feature-callouts container.
- Corpus C17 is green with the template enrolled (230/0); C17 script and allowlists untouched.
- The template is precise enough that Phases 130/131 instantiate it without further design decisions (ROADMAP SC3).
</success_criteria>

<output>
Create `.planning/phases/129-device-recipe-doc-class-foundation/129-02-SUMMARY.md` when done.
</output>
</task>
</output>
