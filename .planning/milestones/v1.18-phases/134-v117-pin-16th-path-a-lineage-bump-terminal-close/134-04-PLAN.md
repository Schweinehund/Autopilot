---
phase: 134-v117-pin-16th-path-a-lineage-bump-terminal-close
plan: 04
type: execute
wave: 3
depends_on: [134-01, 134-02, 134-03]
files_modified: []
autonomous: true
requirements: [HARN-13]
must_haves:
  truths:
    - "The FULL predecessor chain [48..133] runs standalone (non-nested) with 0 FAIL BEFORE the close-gate is authored (LATENT-NON-FROZEN-AWARE-CONTENT-ASSERTION-01 ordering constraint)"
    - "The predecessor byte-unchanged HARD gate passes: frozen surfaces diff clean against the Wave-0 anchor EXCEPT the two sanctioned Phase-133 exceptions (TOOL-04 14 sidecars + TOOL-06 check-phase-60/61), with check-phase-48 net-zero"
    - "Axis 1 (Windows fresh clone, ADVISORY) and Axis 3 (independent/zero-context) results are captured; any WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 stall is documented explicitly as an accepted D-03/GA-1 divergence, never silent, never a close-blocker"
  artifacts:
    - path: ".planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-04-SUMMARY.md"
      provides: "Pre-push re-audit evidence: full-chain 0-FAIL, byte-unchanged gate result, Axis 1/3 PASS/FAIL/SKIP counts"
  key_links:
    - from: "full-chain sweep"
      to: "check-phase-134.mjs (non-nested)"
      via: "recursive spawn of 48..133"
      pattern: "check-phase-134"
---

<objective>
HARN-13 (pre-push re-audit) — run the full predecessor chain to 0-FAIL, enforce the predecessor byte-unchanged HARD gate (enumerating BOTH sanctioned Phase-133 exceptions), and capture the two local re-audit axes (Axis 1 Windows advisory + Axis 3 independent). This lands BEFORE the close-gate commit (Plan 134-05), per the LATENT ordering constraint that a pre-push full sweep catches new archival-drift-class failures before the push, not reactively.

Purpose: The close-gate must not be authored on an unproven chain. Axis 2 (GHA, authoritative) fires on the close-PR in Plan 134-05; this plan establishes the local evidence base (full-chain 0-FAIL + byte-unchanged gate + Axis 1/3) that the MILESTONE-AUDIT doc will cite.
Output: pre-push re-audit evidence recorded in this plan's SUMMARY (no source files modified).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md
@.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-CONTEXT.md
@.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-01-SUMMARY.md
@.planning/phases/133-chain-validator-tooling-debt-closure/133-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full predecessor-chain 0-FAIL sweep + predecessor byte-unchanged HARD gate</name>
  <files>(no file edits — evidence recorded in SUMMARY)</files>
  <read_first>
    - .planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-01-SUMMARY.md (the recorded WAVE0_ANCHOR SHA for the byte-unchanged diff base)
    - .planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md (Research Target 6 ordering constraint + Pitfall 3 — the exact sanctioned exception enumeration: 14 TOOL-04 sidecars + check-phase-60/61 TOOL-06, check-phase-48 net-zero via ba6d53f4)
    - .planning/phases/133-chain-validator-tooling-debt-closure/133-CONTEXT.md (D-09 TOOL-04 atomic commit; CARVE-1/CARVE-2)
  </read_first>
  <action>
    Run the FULL predecessor chain standalone and non-nested: `node scripts/validation/check-phase-134.mjs --verbose` (ensure CHECK_PHASE_NESTED is UNSET so it recursively spawns 48..133) and confirm 0 FAIL. Then run the predecessor byte-unchanged HARD gate: `git diff <WAVE0_ANCHOR>..HEAD` restricted to the frozen surface set (predecessor check-phase-48..128, the vN-audit-allowlist.json sidecars, the vN-milestone-audit harnesses) and confirm the ONLY diffs are the sanctioned Phase-133 exceptions — the 14 TOOL-04 re-pin sidecar JSONs (commit aaf0d2ff) AND the TOOL-06 stderr-budget change at check-phase-60.mjs + check-phase-61.mjs (commit 74939dfb), with check-phase-48.mjs showing ZERO net diff (reverted by ba6d53f4). Any diff on a file NOT in this exact enumerated list is a HARD-gate failure — halt and remediate before proceeding. Note this phase's own new files (check-phase-129..134.mjs, v1.18-*, frozen-at-close.mjs V117 append, BASELINE_22 comment) are NEW/appended surfaces, not predecessor-frozen, and are expected in the diff. Record the full-chain result and the enumerated byte-unchanged verdict in SUMMARY.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-134.mjs --verbose` (CHECK_PHASE_NESTED unset) exits 0 with 0 FAIL across the full [48..133] recursion.
    - `git diff --name-only <WAVE0_ANCHOR>..HEAD` over the frozen predecessor surface set yields ONLY: the 14 TOOL-04 sidecar JSONs, check-phase-60.mjs, check-phase-61.mjs (plus this phase's own new/appended files) — zero unexplained predecessor drift.
    - check-phase-48.mjs shows zero net diff against the anchor.
    - SUMMARY records the full-chain PASS/FAIL/SKIP summary line and the enumerated byte-unchanged verdict.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-134.mjs --verbose</automated>
  </verify>
  <done>Full predecessor chain is 0-FAIL and the byte-unchanged HARD gate passes with exactly the two sanctioned Phase-133 exceptions enumerated (check-phase-48 net-zero).</done>
</task>

<task type="auto">
  <name>Task 2: Axis 1 (Windows fresh-clone, advisory) + Axis 3 (independent/zero-context) local re-audit</name>
  <files>(no file edits — evidence recorded in SUMMARY)</files>
  <read_first>
    - .planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md (Research Target 5 — exact per-axis commands, GA-1 D-01 advisory-timeout handling, Pitfall 4 axis-independence)
    - .planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-CONTEXT.md (GA-1 D-01 — Windows axis ADVISORY with documented accepted-timeout; Axis 3 must be independent host/runner or dispositioned honestly)
  </read_first>
  <action>
    Axis 1 (Windows fresh clone, ADVISORY per GA-1 D-01): `git clone --no-hardlinks <local-repo-path> <scratch-clone-dir>`, then in the clone run `node scripts/validation/v1.18-milestone-audit.mjs --verbose`, the five leaf validators, and `node scripts/validation/check-phase-134.mjs --verbose` (full [48..133]). If the deep-nest chain recursion times out (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01, now at depth 86), DO NOT silently omit — record "timed out at depth 86, ADVISORY axis per GA-1 D-01, non-blocking" in SUMMARY (honest-accounting style). Axis 3 (independent/zero-context): dispatch a fresh zero-context sub-agent carrying NO context from Axes 1/2; per the GA-1 mandatory guardrail, run it on a genuinely independent execution context OR restrict its scope to the fast leaf validators (129-133) plus a `CHECK_PHASE_NESTED=1`-gated apex run — and if Axis 3 runs on the same Windows host and also stalls on the deep-nest, disposition it HONESTLY as inheriting Axis 1's ADVISORY status (do NOT claim 3-axis exact-match if two axes are really one; Axis 2/GHA remains sole authoritative per D-03). Capture each completing axis's `Result: N PASS, M FAIL, K SKIPPED` summary line for the cross-axis diff.
  </action>
  <acceptance_criteria>
    - Axis 1 result recorded in SUMMARY: either the apex PASS/FAIL/SKIP summary line, OR an explicit "WINDOWS-CLONE-DEEPNEST-TIMEOUT-01, ADVISORY, non-blocking per GA-1 D-01" disposition (never silent).
    - Axis 3 result recorded with an explicit independence disposition (independent host = corroborating; same-host stall = honestly ADVISORY, Axis 2 sole-authoritative).
    - The leaf-validator PASS results from at least one completing axis match the local run (exact-match where an axis completes).
    - No axis omission is undocumented.
  </acceptance_criteria>
  <verify>
    <automated>test -d "$SCRATCH_CLONE_DIR" && node "$SCRATCH_CLONE_DIR/scripts/validation/check-phase-129.mjs" && echo "Axis1-leaf-OK (apex disposition recorded in SUMMARY)"</automated>
  </verify>
  <done>Axis 1 and Axis 3 captured with explicit dispositions; any Windows deep-nest timeout documented as accepted GA-1 D-01 divergence, never silent; axis independence honestly stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave-0 anchor → byte-unchanged gate | The gate proves no unsanctioned frozen-surface drift crept in; a mis-scoped exception list would wave real drift through |
| axis independence | Presenting two same-host axes as three independent ones would fabricate a false exact-match |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-09 | Tampering | frozen predecessor surfaces | mitigate | Byte-unchanged HARD gate diff vs Wave-0 anchor; ONLY the two enumerated Phase-133 exceptions permitted; halt on any other drift |
| T-134-10 | Repudiation | 3-axis exact-match claim | mitigate | Axis independence dispositioned honestly; Windows timeout documented explicitly (never silent); Axis 2 sole-authoritative if axes collapse |
| T-134-11 | Denial of Service | Windows deep-nest recursion | accept | WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 is a known recurring advisory timeout (GA-1 D-01), non-blocking; Linux GHA authoritative |

No package-manager installs occur (no `T-134-SC`). Read-only validation over the trusted repo and a local scratch clone.
</threat_model>

<verification>
- Full predecessor chain [48..133] non-nested exits 0 with 0 FAIL BEFORE close-gate.
- Byte-unchanged HARD gate: only the 14 TOOL-04 sidecars + check-phase-60/61 (TOOL-06) drift; check-phase-48 net-zero.
- Axis 1 + Axis 3 captured with explicit dispositions; timeouts documented, never silent.
</verification>

<success_criteria>
HARN-13 (pre-push) satisfied: the chain is proven 0-FAIL, the predecessor byte-unchanged gate passes with both sanctioned exceptions enumerated, and the two local re-audit axes are captured with honest independence/timeout dispositions — the evidence base for the close-gate MILESTONE-AUDIT doc.
</success_criteria>

<output>
Create `.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-04-SUMMARY.md` when done. MUST record the full-chain PASS/FAIL/SKIP line, the enumerated byte-unchanged verdict, and Axis 1/Axis 3 dispositions.
</output>
