---
phase: 134-v117-pin-16th-path-a-lineage-bump-terminal-close
plan: 03
type: execute
wave: 2
depends_on: [134-02]
files_modified:
  - scripts/validation/check-phase-129.mjs
  - scripts/validation/check-phase-130.mjs
  - scripts/validation/check-phase-131.mjs
  - scripts/validation/check-phase-132.mjs
  - scripts/validation/check-phase-133.mjs
  - scripts/validation/check-phase-134.mjs
  - .github/workflows/audit-harness-v1.18-integrity.yml
autonomous: true
requirements: [HARN-12]
must_haves:
  truths:
    - "check-phase-129..133 exist as lightweight leaf validators (126/127 template: CHAIN_PHASES=[], no resolver, needle-based + SELF dual-invariant) and each exits 0"
    - "check-phase-134 is the new apex: CHAIN [48..133] (86 entries, module-load fail-loud), AUDIT via ['v1.18-phases'] resolver token, AUDIT-HARNESS spawning v1.18-milestone-audit.mjs, SELF dual-invariant (134 NOT in chain, CHAIN_SKIP empty)"
    - "audit-harness-v1.18-integrity.yml exists as the 15th coexistence workflow with 5 leaf jobs (129-133) + dual-apex (standalone check-phase-134 + linux-chain), harness-run checkout carrying no ref (CARVE-1 root cause untouched)"
    - "check-phase-134 exits 0 standalone (full [48..133] recursion, non-nested) BEFORE any push"
  artifacts:
    - path: "scripts/validation/check-phase-134.mjs"
      provides: "New chain apex for v1.18"
      contains: "v1.18-phases"
    - path: ".github/workflows/audit-harness-v1.18-integrity.yml"
      provides: "15th CI coexistence workflow"
  key_links:
    - from: "scripts/validation/check-phase-134.mjs"
      to: "scripts/validation/v1.18-milestone-audit.mjs"
      via: "AUDIT-HARNESS spawn step"
      pattern: "v1\\.18-milestone-audit"
    - from: "scripts/validation/check-phase-134.mjs"
      to: "resolveArchivedPhasePath"
      via: "AUDIT check milestoneRoots token ['v1.18-phases']"
      pattern: "v1\\.18-phases"
---

<objective>
HARN-12 (Atom 2) — author the 6 new chain validators (5 leaf 129-133 + 1 apex 134) and the 15th CI coexistence workflow, and prove the apex green standalone before any push.

Purpose: The validator-atom deferral means content phases 129-133 shipped no validators; the close phase authors the whole [129..134] block as one indivisible atom. The apex continues the `[48..N-1]` invariant (chain [48..133], 86 entries). CRITICAL: the new apex must use the OBJECTIVELY CORRECT archive-root token `['v1.18-phases']` (own root) — NOT the predecessor-root off-by-one bug that check-phase-119/125/128 carry (frozen, must not be copied a 4th time). CHAIN_SKIP stays empty (adding entries to force green is a self-disqualifier).
Output: 6 `check-phase-*.mjs` files + `audit-harness-v1.18-integrity.yml`; apex-134 exits 0 standalone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md
@.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-CONTEXT.md
@scripts/validation/check-phase-128.mjs
@scripts/validation/check-phase-126.mjs
@scripts/validation/check-phase-127.mjs
@.github/workflows/audit-harness-v1.17-integrity.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 5 leaf validators check-phase-129..133</name>
  <files>scripts/validation/check-phase-129.mjs, scripts/validation/check-phase-130.mjs, scripts/validation/check-phase-131.mjs, scripts/validation/check-phase-132.mjs, scripts/validation/check-phase-133.mjs</files>
  <read_first>
    - scripts/validation/check-phase-126.mjs (leaf template — CHAIN_PHASES=[], needle-based presence/content checks, SELF dual-invariant at lines ~98-112; NO resolveArchivedPhasePath, NO AUDIT-HARNESS)
    - scripts/validation/check-phase-127.mjs (second leaf template, confirms the shape)
    - .planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md (Research Target 2 — per-validator needle table for 129-133; leaf shape confirmation; Open Question 1 recommendation: leaves stay needle-based, only apex reads its own VERIFICATION.md)
    - .planning/phases/129-*/129-VERIFICATION.md, .planning/phases/130-*/130-VERIFICATION.md, .planning/phases/131-*/131-VERIFICATION.md, .planning/phases/132-*/132-VERIFICATION.md, .planning/phases/133-*/133-VERIFICATION.md (derive final needles from each phase's own Required-Artifacts list — do not blindly trust the research table)
  </read_first>
  <action>
    Copy the check-phase-126/127 leaf shape for each of check-phase-129..133: `CHAIN_PHASES = []`, `CHAIN_SKIP = new Set([])`, needle-based presence/content checks against that phase's ACTUAL deliverable files (derive needles from each phase's own -VERIFICATION.md Required Artifacts, not the research table alone), plus a `SELF` check asserting `CHAIN_PHASES` does NOT include the own phase number AND `CHAIN_SKIP.size === 0`. Target needles per Research Target 2: 129 = STD-05 D-02 ruling row in EEE-SOP-standard.md + recipe-template.md + TEMPLATE-SENTINEL; 130 = docs/recipes/01-*.md exists + kiosk/SharedPC decision block + 4 anti-feature callouts + RE-084 HYG-04 edit; 131 = docs/recipes/02-*.md exists + unsupported-feature callouts + temp-session decision block + layered-config example; 132 = RE-index rows at Status: Approved for both recipes + filename-map regenerated + index.md recipes section + nav-last ordering; 133 = TOOL-04 re-pin landed (14 sidecars) + TOOL-05 attestation + TOOL-06 stderr-budget at check-phase-60/61 only. Do NOT add resolveArchivedPhasePath, a populated CHAIN_PHASES, or an AUDIT-HARNESS step to any leaf — leaves are needle-only per precedent.
  </action>
  <acceptance_criteria>
    - `for n in 129 130 131 132 133; do node scripts/validation/check-phase-$n.mjs || exit 1; done` — all five exit 0.
    - `grep -L "resolveArchivedPhasePath" scripts/validation/check-phase-{129,130,131,132,133}.mjs` lists all five (none contain the resolver — leaf shape confirmed).
    - Each leaf's SELF check asserts its own phase number is absent from CHAIN_PHASES and CHAIN_SKIP.size === 0.
    - `grep -c "CHAIN_SKIP = new Set(\[\])" scripts/validation/check-phase-{129,130,131,132,133}.mjs` confirms empty-Set invariant in each.
  </acceptance_criteria>
  <verify>
    <automated>for n in 129 130 131 132 133; do node scripts/validation/check-phase-$n.mjs || exit 1; done; echo OK</automated>
  </verify>
  <done>Five leaf validators exist, needle-based (no resolver, empty CHAIN_PHASES, empty CHAIN_SKIP), each exits 0 against the live corpus.</done>
</task>

<task type="auto">
  <name>Task 2: Author apex check-phase-134 + the 15th CI workflow; prove apex green standalone</name>
  <files>scripts/validation/check-phase-134.mjs, .github/workflows/audit-harness-v1.18-integrity.yml</files>
  <read_first>
    - scripts/validation/check-phase-128.mjs (apex template — module-load fail-loud length/terminus asserts at lines 70-73, CHAIN_SKIP empty invariant at lines 64-66, the AUDIT + CHAIN + AUDIT-HARNESS + SELF structure, the NESTED guard, and the fragile `['v1.16-phases']` milestoneRoots call to be CORRECTED not copied)
    - scripts/validation/_lib/archive-path.mjs (resolveArchivedPhasePath — the fail-loud-vs-silent-wrong guardrail is the CORRECT token, not a throw; resolver-null legitimately SKIP-PASSes pre-close-gate)
    - .github/workflows/audit-harness-v1.17-integrity.yml (CI template — 194 lines; the 2-leaf-job pattern to extend to 5, the dual-apex pattern, the harness-run checkout with NO ref)
    - .planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md (Research Target 2 archive-root fix + Research Target 4 CI diff spec; Open Question 2 — derive the apex check count, do not hardcode 89)
  </read_first>
  <action>
    Copy check-phase-128.mjs verbatim as the apex template for check-phase-134.mjs. Set CHAIN_PHASES to the integers 48..133 inclusive (86 entries); update the module-load fail-loud asserts to `CHAIN_PHASES.length !== 86` and termini `48..133` (so an off-by-one aborts rather than false-greens); de-duplicate the interior [48..133] entries (length+termini asserts alone won't catch a duplicated/dropped interior entry). Keep `CHAIN_SKIP = new Set([])` — never add entries. In the AUDIT check, pass the OBJECTIVELY CORRECT milestoneRoots token `['v1.18-phases']` (v1.18's own root, where 134-VERIFICATION.md lands at v1.19's archival) — do NOT copy check-phase-128's wrong `['v1.16-phases']`. The operative fail-loud-vs-silent-wrong guardrail IS the objectively-correct token (a wrong token silently mis-resolves to a predecessor milestone's phases). Do NOT add a literal throw-on-resolver-null: pre-close-gate the resolver legitimately returns null (134-VERIFICATION.md does not exist until Plan 134-05), and SKIP-PASS is the EXPECTED, correct behavior that lets the apex exit 0 here — a throw would contradict this task's own pre-close-gate exit-0 criterion (RESEARCH Target 2: a file-existence-based fail-loud is not implementable without new logic no precedent validator has). Add a reviewable inline code comment at the AUDIT/resolver call documenting (a) the deliberate correction away from the predecessor-copied wrong token, and (b) that resolver-null → SKIP-PASS is legitimate pre-close-gate, NOT the silent-wrong bug. Point the AUDIT-HARNESS step at `v1.18-milestone-audit.mjs`. SELF asserts 134 NOT in CHAIN_PHASES AND CHAIN_SKIP.size===0. Then copy audit-harness-v1.17-integrity.yml verbatim to audit-harness-v1.18-integrity.yml, relabel v1.17→v1.18 (name, paths globs, parse job, path-match grep, harness-run `node v1.18-milestone-audit.mjs --verbose`), extend the 2 leaf jobs to 5 (check-phase-129..133), point linux-chain-ubuntu-latest + the standalone check-phase-134 job at the new apex (comment [48..133], DUAL-APEX preserved, no CHECK_PHASE_NESTED on either top-level GHA invocation), keep pin-helper-advisory `continue-on-error: true`, and DO NOT add a `ref:` to the harness-run checkout (that CARVE-1 fix is deferred). Finally prove the apex green standalone: `node scripts/validation/check-phase-134.mjs --verbose` exits 0 with the full [48..133] recursion, and record the derived total check count (AUDIT + 86 CHAIN + AUDIT-HARNESS + SELF) from the --verbose summary — do NOT hardcode 89, derive it.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-134.mjs --verbose` exits 0 (full non-nested [48..133] recursion) and its summary reports 0 FAIL.
    - `grep -c "v1.18-phases" scripts/validation/check-phase-134.mjs` >= 1 AND `grep -c "v1.16-phases\|v1.17-phases" scripts/validation/check-phase-134.mjs` == 0 (correct token, drift not copied — this token-correctness IS the operative fail-loud-vs-silent-wrong guardrail).
    - The reviewable design-decision comment exists near the AUDIT/resolver call: `grep -iq "resolver-null\|SKIP-PASS legitimate\|corrected token" scripts/validation/check-phase-134.mjs` succeeds (documents the token correction AND that resolver-null→SKIP-PASS is legitimate pre-close-gate, not a throw).
    - Module-load asserts reference `!== 86` and termini `48`/`133`; tampering CHAIN_PHASES length throws at load (module-load only — the AUDIT resolver-null path does NOT throw; it SKIP-PASSes pre-close-gate).
    - `grep -q "v1.18-milestone-audit" .github/workflows/audit-harness-v1.18-integrity.yml` AND the workflow has 5 leaf jobs (check-phase-129..133) + a standalone check-phase-134 job + linux-chain-ubuntu-latest.
    - The harness-run job checkout in the new workflow has NO `ref:` (CARVE-1 root cause deliberately untouched).
    - Apex derived check count recorded in SUMMARY (from --verbose, not hardcoded).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-134.mjs --verbose && grep -q "v1.18-phases" scripts/validation/check-phase-134.mjs && ! grep -Eq "v1.1[67]-phases" scripts/validation/check-phase-134.mjs && grep -Eiq "resolver-null|SKIP-PASS legitimate|corrected token" scripts/validation/check-phase-134.mjs && echo OK</automated>
  </verify>
  <done>Apex check-phase-134 exists with chain [48..133] (86, fail-loud), correct ['v1.18-phases'] token, empty CHAIN_SKIP, AUDIT-HARNESS on v1.18 harness; the 15th CI workflow exists (5 leaf + dual-apex, no harness-run ref); apex exits 0 standalone; check count derived and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive resolver → milestoneRoots token | A wrong root token makes the apex AUDIT silently SKIP-PASS forever (false-green) — the exact live bug in 119/125/128 |
| CHAIN_SKIP invariant | Adding chain-skip entries to force green is a self-disqualifier (D-119-3/D-125-1/D-128-C) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-06 | Spoofing | apex AUDIT archive-root token | mitigate | Use correct `['v1.18-phases']` (that IS the fail-loud-vs-silent-wrong protection); grep-assert wrong tokens absent; reviewable inline comment documents the correction + legitimate pre-close-gate SKIP-PASS (no throw) |
| T-134-07 | Tampering | CHAIN_PHASES / CHAIN_SKIP | mitigate | Module-load fail-loud length/terminus asserts (`!== 86`, 48..133); de-dup interior; SELF asserts CHAIN_SKIP.size===0 |
| T-134-08 | Tampering | CI harness-run checkout | accept | CARVE-1 root cause (no `ref:` → validates live HEAD) is knowingly deferred to a future dedicated tooling milestone (D-04); do not add ref |

No package-manager installs occur (no `T-134-SC`). GitHub Actions YAML runs `node` over the trusted local repo; no user-input/auth/network surface.
</threat_model>

<verification>
- All five leaf validators exit 0; none contain the resolver.
- Apex check-phase-134 exits 0 standalone with full [48..133] recursion, 0 FAIL, correct token, wrong tokens absent.
- audit-harness-v1.18-integrity.yml exists (5 leaf + dual-apex, harness-run has no ref).
</verification>

<success_criteria>
HARN-12 (Atom 2) satisfied: 6 validators authored (5 needle-based leaves + 1 apex on chain [48..133] with the corrected root token, empty CHAIN_SKIP, fail-loud asserts), 15th CI workflow authored, apex proven green standalone before push.
</success_criteria>

<output>
Create `.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-03-SUMMARY.md` when done. MUST record the apex's derived total check count and confirm the ['v1.18-phases'] token correction.
</output>
