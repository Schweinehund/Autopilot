---
phase: 134-v117-pin-16th-path-a-lineage-bump-terminal-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/_lib/frozen-at-close.mjs
autonomous: true
requirements: [HARN-11]
must_haves:
  truths:
    - "frozen-at-close.mjs carries a V117 entry pinning the subject-line-verified v1.17 close-gate SHA (b56bba5)"
    - "readAtV117Close convenience export exists and resolves through readAtClose('V117', p)"
    - "The phase-start Wave-0 anchor SHA is captured and recorded for the P04 byte-unchanged HARD gate"
  artifacts:
    - path: "scripts/validation/_lib/frozen-at-close.mjs"
      provides: "V117 back-anchor pin + readAtV117Close export"
      contains: "V117:"
  key_links:
    - from: "scripts/validation/_lib/frozen-at-close.mjs"
      to: "MILESTONE_CLOSE_SHAS.V117"
      via: "readAtClose('V117', path) -> git show <SHA>:<path>"
      pattern: "readAtV117Close"
---

<objective>
HARN-11 — add the mandatory back-anchor V117 pin that freezes the v1.17 corpus, and capture the phase-start Wave-0 anchor SHA that the terminal byte-unchanged gate (Plan 134-04) will diff against.

Purpose: The back-anchor invariant requires that at the close of milestone N the PREVIOUS milestone's close SHA is pinned (V117 at the v1.18 close). A wrong SHA silently corrupts every v1.17-frozen `readAtClose` read, so the SHA must be recovered by dual-token positive-confirmation grep with explicit SUBJECT-LINE verification (the v1.17 false-positive caveat).
Output: `scripts/validation/_lib/frozen-at-close.mjs` gains the `V117` entry + `readAtV117Close` export; the Wave-0 anchor SHA is recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md
@.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-CONTEXT.md
@scripts/validation/_lib/frozen-at-close.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recover + subject-verify the V117 SHA and capture the Wave-0 anchor</name>
  <files>(no file edits — recovery + recording only; anchor recorded in SUMMARY)</files>
  <read_first>
    - .planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md (Research Target 1 — exact command, candidate SHA, subject-line verification, archival/safety-SHA cross-check)
    - .planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-CONTEXT.md (GA-2 dual-token guardrail; false-positive caveat)
    - scripts/validation/_lib/frozen-at-close.mjs (lines 60-75 — the V116 recovery-command comment precedent this mirrors)
  </read_first>
  <action>
    Run the dual-token positive-confirmation recovery: `git log --all --grep="MILESTONE-AUDIT" --grep="MILESTONE CLOSE" --all-match --format="%H %s"`. Confirm the newest matching commit is `b56bba5ea19f9b3fea6376a48dcc24f4ea1d3428` and that its SUBJECT LINE literally contains BOTH `MILESTONE-AUDIT` and `MILESTONE CLOSE` (subject: `docs(128-07): v1.17 MILESTONE-AUDIT + DEFERRED-CLEANUP + 10-req traceability + v1.17 MILESTONE CLOSE`). Explicitly REJECT the runner-up `066a9068` (Atom 2a — subject lacks "MILESTONE CLOSE"), the archival git-rm `d0fda4f9`, and the safety `6851b54a` (neither appears in the dual-token output). Do NOT trust `-1` blindly — perform the subject-line check even though it resolves cleanly this cycle. Then capture the phase-start Wave-0 anchor via `git rev-parse HEAD` BEFORE any edit lands, and record that SHA verbatim in the SUMMARY as `WAVE0_ANCHOR` for Plan 134-04's predecessor byte-unchanged HARD gate.
  </action>
  <acceptance_criteria>
    - The recovery command output's newest line is `b56bba5ea19f9b3fea6376a48dcc24f4ea1d3428 docs(128-07): v1.17 MILESTONE-AUDIT ... v1.17 MILESTONE CLOSE`.
    - The chosen SHA's subject line contains both tokens (verified via `git show -s --format=%s b56bba5e` grep for `MILESTONE-AUDIT` AND `MILESTONE CLOSE`).
    - `d0fda4f9` and `6851b54a` are confirmed ABSENT from the dual-token `--all-match` output.
    - SUMMARY records `WAVE0_ANCHOR=<git rev-parse HEAD>` captured at phase start.
  </acceptance_criteria>
  <verify>
    <automated>git show -s --format=%s b56bba5e | grep -q "MILESTONE-AUDIT" && git show -s --format=%s b56bba5e | grep -q "MILESTONE CLOSE" && echo OK</automated>
  </verify>
  <done>V117 SHA (b56bba5) is subject-line-confirmed as the v1.17 close-gate; archival/safety SHAs ruled out; Wave-0 anchor captured and recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Add the V117 entry + readAtV117Close export to frozen-at-close.mjs</name>
  <files>scripts/validation/_lib/frozen-at-close.mjs</files>
  <read_first>
    - scripts/validation/_lib/frozen-at-close.mjs (the V116 entry at lines 68-75 and the `readAtV116Close` convenience export at line 114 — the exact patterns to mirror)
    - .planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md (Research Target 1 — exact diff text for the V117 entry comment block and the export line)
  </read_first>
  <action>
    Insert a `V117: 'b56bba5',` entry into `MILESTONE_CLOSE_SHAS` immediately after the `V116: '3dd2512',` entry, mirroring the V116 comment-block style: annotate it as the Phase-128 Plan 128-07 close-gate, note the dual-token subject-line confirmation and the full recovered SHA `b56bba5ea19f9b3fea6376a48dcc24f4ea1d3428`, and note the single-entry back-anchor invariant (V117 references a PAST close; V118 is deferred to v1.19). Append `export const readAtV117Close = (p) => readAtClose('V117', p);` immediately after the `readAtV116Close` export line. Make NO other edits to this file — this is an append-only sanctioned close edit; do not touch V116 or any earlier entry, do not reformat existing lines.
  </action>
  <acceptance_criteria>
    - `grep -n "V117: 'b56bba5'" scripts/validation/_lib/frozen-at-close.mjs` returns exactly one match.
    - `grep -n "readAtV117Close" scripts/validation/_lib/frozen-at-close.mjs` returns exactly one match, defined as `readAtClose('V117', p)`.
    - `node -e "import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>{if(m.readAtV117Close && m.MILESTONE_CLOSE_SHAS.V117==='b56bba5')process.exit(0);process.exit(1)})"` exits 0.
    - `git diff --stat` shows frozen-at-close.mjs as the ONLY modified file, additions only (V116 and prior lines byte-unchanged).
  </acceptance_criteria>
  <verify>
    <automated>node -e "import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>process.exit(m.readAtV117Close && m.MILESTONE_CLOSE_SHAS.V117==='b56bba5'?0:1))"</automated>
  </verify>
  <done>frozen-at-close.mjs exports readAtV117Close and MILESTONE_CLOSE_SHAS.V117 === 'b56bba5'; module loads clean; diff is append-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → frozen-read pin | The recovered SHA becomes the authoritative anchor for every v1.17-frozen `readAtClose` — an untrusted/wrong value corrupts downstream reads silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-01 | Tampering | V117 pin value | mitigate | Dual-token positive-confirm grep with mandatory SUBJECT-LINE check; reject archival d0fda4f9 / safety 6851b54a explicitly (Task 1 verify command) |
| T-134-02 | Tampering | frozen-at-close.mjs existing entries | mitigate | Append-only edit; `git diff --stat` confirms V116-and-prior byte-unchanged (acceptance criterion) |

No package-manager installs occur in this plan (no `T-134-SC` applicable). This is internal validation tooling with no runtime/auth/network/user-input surface.
</threat_model>

<verification>
- `node -e` import check: `readAtV117Close` exported and `MILESTONE_CLOSE_SHAS.V117 === 'b56bba5'`.
- Subject-line dual-token grep on b56bba5e passes; archival/safety SHAs absent from dual-token output.
- `git diff --stat` shows only frozen-at-close.mjs changed (append-only).
</verification>

<success_criteria>
HARN-11 satisfied: V117 back-anchor pin (subject-verified b56bba5) + readAtV117Close export land as an append-only edit to frozen-at-close.mjs; Wave-0 anchor SHA recorded for the terminal byte-unchanged gate.
</success_criteria>

<output>
Create `.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-01-SUMMARY.md` when done. MUST record `WAVE0_ANCHOR=<sha>` and the confirmed V117 SHA.
</output>
