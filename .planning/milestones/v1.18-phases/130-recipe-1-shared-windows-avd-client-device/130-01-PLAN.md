---
phase: 130-recipe-1-shared-windows-avd-client-device
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-apv1/08-self-deploying.md
autonomous: true
requirements: [HYG-04]
must_haves:
  truths:
    - "RE-084 states Wi-Fi is SUPPORTED for self-deploying (but not zero-touch) at every former stale site (per C-LOCK-1/3)"
    - "The fabricated 'cannot reach the Autopilot service before OOBE' / 'pre-authentication stage' mechanism is removed from RE-084 (per C-LOCK-3)"
    - "The legitimate Ethernet-recommended-for-zero-touch guidance is preserved (per C-LOCK-4)"
    - "RE-084 frontmatter last_verified = 2026-07-17 and review_by advanced to 2026-10-15 (per C-LOCK-5)"
    - "RE-084 carries a dated v1.18 HYG-04 Version-History row (per C-LOCK-5)"
    - "C17 self-test and full-corpus run exit 0 with the edited RE-084 (per C-LOCK-6)"
  artifacts:
    - path: "docs/admin-setup-apv1/08-self-deploying.md"
      provides: "Corrected, non-contradictory Wi-Fi-at-OOBE framing that the Phase-130 recipe links (never restates)"
      contains: "supported"
  key_links:
    - from: "docs/admin-setup-apv1/08-self-deploying.md L61/L63 blockquotes"
      to: "C17 assertion #12 (200-char contiguous-run cap)"
      via: "blank-line-split blockquote runs, each re-measured ≤200 chars"
      pattern: "^> "
---

<objective>
HYG-04: correct the verifiably-STALE "Wi-Fi unsupported for self-deploying" claim in RE-084 (`docs/admin-setup-apv1/08-self-deploying.md`) as a single atomic 6-site edit, plus freshness-stamp and changelog updates. This is the milestone's ONLY edit to an existing doc, and it MUST land before/with the Phase-130 recipe (Plan 130-02), which LINKS the corrected RE-084 for its Prerequisites Ethernet row and anti-feature Wi-Fi row (D-LOCK-5 / H-LOCK-3 / E-LOCK-3) — authoring the recipe first would reintroduce the stale claim in two docs at once (RESEARCH Pitfall 2).

Purpose: Wi-Fi IS supported for self-deploying today (verified 3× vs. `learn.microsoft.com/autopilot/self-deploying#validation`); the claim and its fabricated causal mechanism are stale and self-contradicting once the recipe ships. Correcting once here scopes the frozen-surface/validator reconciliation exactly once (HYG-04-FOLD).

Output: Corrected RE-084 (6 sites + frontmatter + changelog), C17-green, with verification evidence recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-recipe-1-shared-windows-avd-client-device/130-CONTEXT.md
@.planning/phases/130-recipe-1-shared-windows-avd-client-device/130-RESEARCH.md
@.planning/phases/130-recipe-1-shared-windows-avd-client-device/130-PATTERNS.md
@docs/admin-setup-apv1/08-self-deploying.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the 6 stale Wi-Fi-claim sites in RE-084</name>
  <read_first>
    - docs/admin-setup-apv1/08-self-deploying.md — the file being edited (read in full; the 6 sites are L31, L55, L61, L63, L69, L108; L35 is the hybrid-join line and is NOT a Wi-Fi site — do not touch it)
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-RESEARCH.md "HYG-04 Fix Plan" section (lines 415-472) — current text per site, first-party ground-truth quotes, and character-counted candidate replacement wording for L31/L55/L61/L63/L69/L108
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-CONTEXT.md decisions C-LOCK-1..C-LOCK-7 (lines 45-52) — the binding correction contract
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-PATTERNS.md lines 208-243 — per-site edit shape and the blank-line-chunked blockquote idiom
  </read_first>
  <action>
    Edit each of the 6 sites in docs/admin-setup-apv1/08-self-deploying.md to the two-stage framing (Ethernet = recommended/zero-touch; Wi-Fi = supported but not zero-touch, requires manual language/locale/keyboard + network join at OOBE), using the char-counted candidate wording in RESEARCH.md lines 444-461 as the target state (author may copyedit within the locks):
    - L31 (Prerequisites bullet): replace "Wi-Fi is NOT supported" with the supported-but-not-zero-touch bullet (RESEARCH L447). This corrected bullet IS the recipe's H-LOCK-3 Prerequisites link target.
    - L55 (Step 2 "Wired ethernet connection available" bullet): reframe as recommended-for-zero-touch with the Wi-Fi-supported caveat (RESEARCH L450).
    - L61 (Step 2 blockquote): reframe away from "What breaks if misconfigured" (Wi-Fi is no longer a misconfiguration) to a "Wi-Fi at OOBE: supported, not zero-touch" callout (RESEARCH L453).
    - L63 (Step 2 blockquote): delete the fabricated "pre-authentication stage" claim AND the external `../l1-runbooks/04-network-connectivity.md` link; replace with an internal-only Step-2 pointer (RESEARCH L456) — this satisfies C-LOCK-6(ii) (net external-URL count decreases, never increases).
    - L69 (Step 3 numbered flow): add the parenthetical Wi-Fi caveat (RESEARCH L459) or keep Ethernet-first per C-LOCK-4 — author's call; do not leave it asserting Wi-Fi is impossible.
    - L108 (Configuration-Caused Failures row): REMOVE the "Wi-Fi used instead of wired ethernet" row entirely (RESEARCH L461 recommendation — a "not really a failure" row contradicts the table's own semantics; the caveat is fully carried by the corrected L31/L55/L61/L63). Do NOT add a 4th "nuance" column to the frozen 3-column table.
    Preserve the blank line between the L61 and L63 blockquotes so each stays its own C17 #12 run. Do NOT change the H1, doc_id (RE-084), title, or add any new external URL to the body (C-LOCK-6). Do NOT touch L35 (hybrid-join, unrelated to Wi-Fi).
  </action>
  <acceptance_criteria>
    - `grep -nE 'NOT supported.*Wi-Fi|Wi-Fi is NOT supported|pre-authentication stage|cannot reach the Autopilot' docs/admin-setup-apv1/08-self-deploying.md` returns ZERO matches (all stale claims + fabricated mechanism gone).
    - The file contains the word "supported" adjacent to Wi-Fi in the two-stage framing (Prerequisites + Step 2).
    - The former L108 "Wi-Fi used instead of wired ethernet" failures-table row is absent; the Configuration-Caused Failures table header remains the frozen 3-column `| Misconfiguration | Symptom | Runbook |`.
    - No new external `http(s)://` or `../l1-runbooks/04-network-connectivity.md` link exists in the body versus baseline (net external link count is unchanged-or-lower): `grep -c '04-network-connectivity' docs/admin-setup-apv1/08-self-deploying.md` returns 0.
    - H1 (`# Self-Deploying Mode Configuration`), `doc_id: RE-084`, and the EEE header block line are byte-unchanged.
    - Each rewritten L61/L63 blockquote line, measured including the leading `> `, is ≤200 chars (re-measure `.length` after edit per Pitfall 3 / H-LOCK-2).
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && grep -cE 'Wi-Fi is NOT supported|pre-authentication stage|cannot reach the Autopilot|04-network-connectivity' docs/admin-setup-apv1/08-self-deploying.md | grep -qx 0 && echo PASS-no-stale || echo FAIL</automated>
  </verify>
  <done>All 6 sites reflect "Wi-Fi supported, not zero-touch"; the fabricated mechanism and its runbook link are gone; the failures row is removed; H1/doc_id/title unchanged; L61/L63 each ≤200 chars.</done>
</task>

<task type="auto">
  <name>Task 2: Freshness-stamp, changelog row, and C17 green gate</name>
  <read_first>
    - docs/admin-setup-apv1/08-self-deploying.md — frontmatter (L1-11) and Version History table (L128-132), as left by Task 1
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-CONTEXT.md C-LOCK-5 (freshness + changelog rules; note the 129-D-12 distinction — RE-084 is a live Guide, this IS a content correction that DOES bump last_verified)
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-RESEARCH.md lines 463-479 — computed dates (2026-07-17 + 90 = 2026-10-15), the exact Version-History row wording, and the C17 guardrail checklist
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-PATTERNS.md lines 245-258 — append-a-row changelog idiom (keep RE-084's own 3-column `Date | Change | Author` shape, author `—`)
  </read_first>
  <action>
    In docs/admin-setup-apv1/08-self-deploying.md: set frontmatter `last_verified: 2026-07-17` and `review_by: 2026-10-15` (90-day cadence, currently overdue at 2026-07-12). Append (do not replace) one new row to the Version History table (L128-132) describing the v1.18 HYG-04 correction — model the wording on RESEARCH.md L472 / PATTERNS.md L258: dated 2026-07-17, naming HYG-04, the 6 sites, the "supported but not zero-touch" correction, the removed fabricated mechanism, the preserved Ethernet guidance, and the removed failures row; Author column `—`. Add NO in-body dated verification note (C-LOCK-5). Then run the C17 gate. Record the first-party verification evidence (the `autopilot/self-deploying#validation` + `autopilot/pre-provision` URLs and the verbatim quotes from RESEARCH.md L430-442) in this plan's SUMMARY / VERIFICATION artifact — NOT in the RE-084 body (C-LOCK-6(ii)/C-LOCK-7).
  </action>
  <acceptance_criteria>
    - Frontmatter shows `last_verified: 2026-07-17` and `review_by: 2026-10-15`.
    - The Version History table has exactly one NEW row dated 2026-07-17 naming "HYG-04" and the 6-site correction; the pre-existing 2026-07-05 row is retained; table stays 3-column `| Date | Change | Author |`.
    - `node scripts/validation/c17-eee-contract.mjs --self-test` exits 0 (4 passed, 0 failed).
    - `node scripts/validation/c17-eee-contract.mjs --verbose` full-corpus run reports 0 violations (RE-084 still enrolled and green; no retroactive #12 failure introduced).
    - The SUMMARY records the two first-party Learn URLs + quoted ground-truth wording as HYG-04 verification evidence; no such URL is added to the RE-084 body.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && node scripts/validation/c17-eee-contract.mjs --self-test 2>&1 | grep -q "0 failed" && node scripts/validation/c17-eee-contract.mjs --verbose 2>&1 | grep -qiE '0 violation' && echo PASS-c17 || echo CHECK-c17-output</automated>
  </verify>
  <done>Frontmatter dates bumped; one dated HYG-04 changelog row appended; C17 self-test 4/4 and full-corpus 0 violations; verification evidence recorded in SUMMARY (not in RE-084 body).</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only phase. No executable surface, no data flow, no trust boundary, no external package installs. The edited artifact is a static Markdown file in a C17-validated corpus; the correction removes an inaccurate claim and one internal link, adds no runtime behavior. The only "gate" is the frozen C17 validator (never edited, D-10). Package Legitimacy Gate does not fire (no npm/pip/cargo installs).
</threat_model>

<verification>
- `grep` confirms zero residual stale Wi-Fi claims and zero fabricated-mechanism strings in RE-084.
- C17 `--self-test` exits 0 (4/4) and `--verbose` reports 0 corpus violations post-edit.
- Frontmatter dates and the appended changelog row present; H1/doc_id/title byte-unchanged.
- First-party verification evidence recorded in SUMMARY, not in the RE-084 body (no new external URL).
</verification>

<success_criteria>
RE-084's "Wi-Fi unsupported" claim is corrected at all 6 sites to "supported but not zero-touch," the fabricated pre-authentication-stage mechanism is deleted, Ethernet-for-zero-touch guidance is preserved, freshness dates and a HYG-04 changelog row are updated, and C17 stays green — leaving a non-contradicting link target for the Phase-130 recipe (Plan 130-02).
</success_criteria>

<output>
Create `.planning/phases/130-recipe-1-shared-windows-avd-client-device/130-01-SUMMARY.md` when done, including the HYG-04 first-party verification evidence (URLs + quoted wording).
</output>
