---
phase: 130-recipe-1-shared-windows-avd-client-device
plan: 02
type: execute
wave: 2
depends_on: ["130-01"]
files_modified:
  - docs/recipes/01-shared-windows-avd-client.md
autonomous: true
requirements: [AVD-01, AVD-02, AVD-03, AVD-04, AVD-05]
must_haves:
  truths:
    - "docs/recipes/01-shared-windows-avd-client.md exists, carries doc_id RE-222, is C17-enrolled and C17-green (AVD-01)"
    - "The recipe walks the linear happy-path spine: self-deploying profile → device-phase ESP → dynamic device group → Windows App (Store, Required, device-context) → per-user feed subscription → verification (AVD-01)"
    - "Both branches are fully worked and non-converging: ### Step 5a Kiosk (Assigned Access GUI) and ### Step 5b Shared PC (SharedPC Templates GUI), under one ## Steps H2 (AVD-02)"
    - "The Shell-Launcher / Assigned-Access mutual-exclusion constraint is stated in Step 5a (AVD-02)"
    - "The ## Unsupported and Anti-Feature Callouts table carries exactly 4 rows in order hybrid Entra join → APv2/Device Preparation → MSRDC → Wi-Fi-at-OOBE (anti-pattern, qualified cell) (AVD-03)"
    - "AVD-04's three decision points exist at their natural steps: session-reset (Step 4), InactiveThreshold nested under DeletionPolicy=2 (Step 5b), update-ring/MaintenanceStartTime (AVD-04)"
    - "AVD-05 wired-vs-Wi-Fi is a boolean if/then that cross-links docs/admin-setup-8021x/03-windows.md + 00-overview.md, never inlining 802.1X config (AVD-05)"
    - "The Scope banner is specialized to the 173-char client-vs-session-host disambiguation (H-LOCK-1)"
    - "The single ## Verification H2 has a shared 'confirm first' lead-in plus two bold-pseudo-heading branch checklists (G-LOCK-1)"
    - "Configuration-Caused Failures rows route to in-recipe anchors or existing linked refs — no fabricated runbook links (G-LOCK-5)"
    - "Every in-doc `#fragment` link (Step 5 branch cells, anti-feature 'Do this instead', Config-Caused-Failures runbook cells, See Also) resolves against the file's own GitHub-exact slugified headings — verified by an automated anchor-resolution gate (D-LOCK-3 double-hyphen slug trap caught)"
  artifacts:
    - path: "docs/recipes/01-shared-windows-avd-client.md"
      provides: "The Shared Windows AVD-client provisioning recipe (first live instantiation of the Phase-129 recipe doc-class)"
      contains: "## Unsupported and Anti-Feature Callouts"
      min_lines: 150
  key_links:
    - from: "docs/recipes/01-shared-windows-avd-client.md Prerequisites + anti-feature Wi-Fi row"
      to: "docs/admin-setup-apv1/08-self-deploying.md (corrected RE-084)"
      via: "relative markdown link (link-not-copy, D-LOCK-5 / D-LOCK-8)"
      pattern: "08-self-deploying"
    - from: "docs/recipes/01-shared-windows-avd-client.md Step 3"
      to: "docs/admin-setup-apv1/04-dynamic-groups.md (RE-080 ZTDId rule)"
      via: "link, never re-inline the fenced membership string"
      pattern: "04-dynamic-groups"
    - from: "docs/recipes/01-shared-windows-avd-client.md AVD-05 fork"
      to: "docs/admin-setup-8021x/03-windows.md + 00-overview.md"
      via: "boolean if/then cross-link, 802.1X never inlined"
      pattern: "admin-setup-8021x"
    - from: "Step 5 decision-table Branch cells"
      to: "#step-5a-kiosk-configuration / #step-5b-shared-pc-configuration"
      via: "in-doc anchor links synced to the heading style (D-LOCK-3), resolution proven by the anchor-resolution gate"
      pattern: "#step-5a-kiosk-configuration"
---

<objective>
Author `docs/recipes/01-shared-windows-avd-client.md` — the first live instantiation of the Phase-129 recipe doc-class — walking an Intune admin from zero to a verified, self-deploying, Entra-joined shared Windows device running the Windows App as an AVD client. Covers the linear happy-path spine (AVD-01), both fully-worked non-converging forks kiosk-vs-SharedPC (AVD-02), the 4-row anti-feature table (AVD-03), session-hygiene/patch decision points (AVD-04), and the wired-vs-Wi-Fi 802.1X cross-link (AVD-05).

Purpose: Deliver ROADMAP SC1-SC4 as one coherent, C17-green Guide. All design is LOCKED via two adversarial-review rounds (CONTEXT.md decisions A-LOCK-* … H-LOCK-* with amendments E-LOCK-4/F-LOCK-6/G-LOCK-2) — this plan IMPLEMENTS those locks; it does not re-open them. RESEARCH.md Patterns 1-6 supply ready-to-adapt, first-party-verified portal paths, GUI↔CSP mappings, and markdown blocks; authoring is assembly-within-locks, not new research.

Output: One new recipe file, doc_id RE-222 (Draft), C17-enrolled and green. depends_on Plan 130-01 (links the corrected RE-084; must NOT restate the Wi-Fi claim).

doc_id decision (RESEARCH Open Question 3): assign RE-222 NOW in frontmatter (status Draft). Consequence: C17 opts a file in by doc_id presence, so the recipe enrolls in the full-corpus run immediately (which is required — the recipe must be C17-green this phase). The RE-index registry ROW and status→Approved flip remain Phase 132's job (CLASS-03); this phase adds NO registry/nav edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-recipe-1-shared-windows-avd-client-device/130-CONTEXT.md
@.planning/phases/130-recipe-1-shared-windows-avd-client-device/130-RESEARCH.md
@.planning/phases/130-recipe-1-shared-windows-avd-client-device/130-PATTERNS.md
@docs/_templates/recipe-template.md
@docs/_standards/EEE-SOP-standard.md
@docs/admin-setup-apv1/08-self-deploying.md
@scripts/validation/check-nav-hub-links.mjs
</context>

<anchor_resolution_gate>
The recipe carries in-doc `#fragment` links (Step 5 Branch cells → `#step-5a-…`/`#step-5b-…` — the exact D-LOCK-3 double-hyphen slug-trap hazard; anti-feature MSRDC/Wi-Fi "Do this instead" internal anchors; Configuration-Caused-Failures Runbook cells; See Also). `scripts/validation/check-nav-hub-links.mjs` does NOT cover them — its `HUB_PATHS` roster is a D-01-locked, frozen 4-file set (`docs/index.md`, `docs/common-issues.md`, `docs/quick-ref-l1.md`, `docs/quick-ref-l2.md`) that never scans this recipe as source or target, and mutating it would break that standalone validator's locked surface. `c17-eee-contract.mjs` has no link/anchor logic. So an inline `node -e` gate below verifies every in-doc `#fragment` link resolves against the file's OWN headings, computing slugs the SAME way `check-nav-hub-links.mjs` does (lowercase → strip `[^a-z0-9 _-]` in place → spaces→hyphen, NEVER collapsing consecutive hyphens; plus `{#id}` overrides added verbatim and their auto-slug suppressed; plus encounter-order `-1/-2` dedup; fenced-block headings/links masked out). Because the strip-in-place is what produces the double-hyphen artifact, a mis-slugified `#step-5a--kiosk`-class anchor is actually caught. The gate is executed in Task 2 (as soon as the Step 5 branch cells + 5a/5b headings exist — the earliest point the trap can be caught) and again in Task 3 over the completed file. It scans only in-doc `#fragment` links (cross-file links keep their existing link-not-copy targets, verified structurally by the `grep` path checks).
</anchor_resolution_gate>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold + happy-path spine (Steps 1-4) + anti-feature table + scope banner + prerequisites</name>
  <read_first>
    - docs/_templates/recipe-template.md — the literal D-06 skeleton to instantiate wholesale (frontmatter, EEE header, Summary opener, Scope banner placeholder L89-90, anti-feature table header, `### Step N:` idiom, per-step "What breaks" callout, trailing H2 order). DELETE the WORKED DECISION-POINT EXAMPLES HTML comment (L56-87) when copying.
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-RESEARCH.md Pattern 1 (lines 125-146, Steps 1-4 exact portal paths + verbatim device-context "installed before the user signs in" quote + the AutoSubscription inline legacy note) and the recipe-structure tree (lines 97-123)
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-CONTEXT.md decisions D-LOCK-1 (slug/platform), D-LOCK-5 (link-not-copy by ownership), D-LOCK-8 (one Wi-Fi story), E-LOCK-1..E-LOCK-6 (anti-feature table), H-LOCK-1..H-LOCK-5 (scope banner + prerequisites), B-LOCK-1..B-LOCK-3 (feed fact), and D-LOCK-6 (session-reset block placement in Step 4)
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-PATTERNS.md lines 18-119 (skeleton copy shapes) and lines 176-184 (ZTDId link-not-copy)
    - docs/admin-setup-apv1/08-self-deploying.md — the corrected RE-084 (as left by Plan 130-01), the Prerequisites/anti-feature Wi-Fi link target; confirm the corrected wording before linking it (must NOT restate the claim)
  </read_first>
  <action>
    Create docs/recipes/01-shared-windows-avd-client.md by instantiating recipe-template.md:
    - Frontmatter: `doc_id: RE-222`, `status: Draft`, `owner: Intune Admin Lead`, `doc_type: Guide`, `platform: Windows` (D-LOCK-1), real `last_verified: 2026-07-17` + `review_by: 2026-10-15` (replace the TEMPLATE-SENTINEL — this is a live doc), `applies_to` naming the AVD-client device/Intune scope, `audience: admin`. Update the EEE header block line to `**Platform:** Windows · **Doc Type:** Guide · **Doc ID:** RE-222 · **Status:** Draft`. Delete the worked-example HTML comment.
    - H1: a concrete end-state title. Summary: open with the D-13 mandatory end-state sentence ("Following this recipe yields a self-deploying Entra-joined shared Windows AVD-client device, provisioned end-to-end from zero through Intune."), ≥30 words, naming platform + required admin role (C17 #4/#5 fire).
    - Scope banner: ship the H-LOCK-1 verbatim 173-char banner (CONTEXT.md specifics L171) as an ISOLATED blockquote run with blank lines both sides (H-LOCK-2); re-measure ≤200 chars. Never invoke Entra "Shared device mode" (H-LOCK-5).
    - Prerequisites (H-LOCK-3, plain bullets, no verify column): RBAC + licensing inline; Autopilot-registered → link 01-hardware-hash-upload.md; dynamic group → link RE-080 (04-dynamic-groups.md); TPM 2.0 → link RE-084 + one-liner; Ethernet-for-zero-touch → link the CORRECTED RE-084 with the D-LOCK-8 two-stage framing (never restate the old claim); ESP device-phase → link RE-079 (03-esp-policy.md); AVD infra exists → plain assumption, no link; users assigned to AVD app groups → plain assumption. First bullet MAY carry the one-sentence "what this recipe is NOT" absorbing the SDM/RemoteApp traps (H-LOCK-4/5).
    - `## Unsupported and Anti-Feature Callouts` table (frozen header, E-LOCK-1): exactly 4 rows in order hybrid Entra join → APv2/Device Preparation → MSRDC (retired client) → Wi-Fi at OOBE. Wi-Fi Feature cell carries the inline qualifier "Wi-Fi at OOBE (supported — anti-pattern for zero-touch)" (E-LOCK-2), never bare "Wi-Fi." Each "Why" cell one scannable clause; "Do this instead" delegates by link: hybrid→RE-084, APv2→RE-177 (apv1-vs-apv2.md), Wi-Fi→corrected RE-084 (+ optional internal anchor to the AVD-05 step), MSRDC→internal anchor to this recipe's own Step 4 Windows-App step. MSRDC retirement date 2026-03-27 stated ONCE here (E-LOCK-4). No 5th AutoSubscription row, no AVDClientKiosk row.
    - `## Steps` H2, then Steps 1-4 per RESEARCH Pattern 1 (INLINE the minimal actionable toggle, LINK the exhaustive reference): Step 1 self-deploying profile (link RE-084/02-deployment-profile.md); Step 2 device-phase-only ESP (link RE-079, note "no user phase"); Step 3 dynamic device group (link RE-080; do NOT re-inline the fenced ZTDId string, D-LOCK-5); Step 4 Windows App device-context deployment (Microsoft Store app (new), Required, device group) with the verbatim "installed before the user signs in" behavior stated, the AutoSubscription legacy user-scope CSP note stated ONCE inline (B-LOCK-3/E-LOCK-4), and the feed auto-subscribes-per-user fact stated (B-LOCK-1/2, NOT a decision block). Place the AVD-04 session-reset Case-2 STD-05 decision block inside Step 4 (D-LOCK-6); flag the `ResetAppOn*` node names `[ASSUMED]` per RESEARCH Pitfall 5 / Assumption A4 with a "verify against your tenant's Settings Catalog" caveat.
    Use blank-line-chunked blockquotes for every multi-clause callout so each contiguous `>`-run stays ≤200 chars (C17 #12, Pitfall 3). No fenced code blocks for CSP values (D-LOCK-5). Ensure Step 4's heading anchor matches what the anti-feature MSRDC row and the failures table will link to.
    Any in-doc `#fragment` anchor emitted in this task (the MSRDC row → Step 4 anchor; the optional Wi-Fi row → AVD-05-step anchor whose target heading is authored in Task 2) is a forward-safe reference. In-doc anchor RESOLUTION is NOT gated here — the automated anchor-resolution step runs in Tasks 2 and 3, at the earliest point all referenced target headings exist. Running it now would false-fail on the not-yet-authored AVD-05 target.
  </action>
  <acceptance_criteria>
    - File docs/recipes/01-shared-windows-avd-client.md exists with frontmatter `doc_id: RE-222`, `platform: Windows`, `status: Draft`, real `last_verified: 2026-07-17` (no `1970-01-01` sentinel).
    - Summary is the FIRST H2 and is ≥30 words (C17 #4/#5); EEE header block reads Platform→Doc Type→Doc ID→Status with `·` separators matching frontmatter (C17 #7/#9).
    - The Scope banner is present as an isolated blockquote and measures ≤200 chars for its contiguous run; it names "AVD client" vs "session hosts" and the host-pool/FSLogix assumption.
    - `## Unsupported and Anti-Feature Callouts` table has exactly 4 body rows (`grep -c` the row pattern) in the locked order; the Wi-Fi row's Feature cell contains "supported" and "anti-pattern"; no row bare-labels "Wi-Fi" under the unsupported header; MSRDC "2026-03-27" appears exactly once in the file.
    - Prerequisites link (not inline) RE-084, RE-080, RE-079, 01-hardware-hash-upload.md; the ZTDId regex string does NOT appear inline in Step 3 (`grep -c 'devicePhysicalIDs' docs/recipes/01-shared-windows-avd-client.md` returns 0).
    - The word "supported"/"unsupported" appears next to "Wi-Fi" ONLY inside the anti-feature table's qualified cell, nowhere else in the body (Pitfall 2 guard).
    - `node scripts/validation/c17-eee-contract.mjs --verbose` reports 0 violations with the new file enrolled (partial file — trailing sections added in later tasks — but every `>`-run already ≤200 chars).
    - (In-doc `#fragment` anchor resolution is deferred to the Task 2/Task 3 anchor-resolution gate — not asserted here, since forward-referenced targets like the AVD-05 step do not yet exist.)
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && test -f docs/recipes/01-shared-windows-avd-client.md && grep -q 'doc_id: RE-222' docs/recipes/01-shared-windows-avd-client.md && [ "$(grep -c 'devicePhysicalIDs' docs/recipes/01-shared-windows-avd-client.md)" = "0" ] && node scripts/validation/c17-eee-contract.mjs --verbose 2>&1 | grep -qiE '0 violation' && echo PASS || echo CHECK</automated>
  </verify>
  <done>Recipe file created with RE-222 frontmatter, specialized scope banner, linked prerequisites, the 4-row anti-feature table in locked order, and Steps 1-4 (incl. Step 4 session-reset block); C17 corpus green; no inlined ZTDId string; no stray Wi-Fi supported/unsupported claim outside the anti-feature cell. In-doc anchors emitted here are verified by the Task 2/3 anchor-resolution gate.</done>
</task>

<task type="auto">
  <name>Task 2: The dominant fork — Step 5 decision + Step 5a Kiosk + Step 5b Shared PC + AVD-04 remaining blocks + AVD-05</name>
  <read_first>
    - docs/recipes/01-shared-windows-avd-client.md — the spine authored in Task 1 (read to align anchors: Step 4 Windows-App step is a dependency of Step 5a per A-LOCK-5(ii))
    - scripts/validation/check-nav-hub-links.mjs — READ its `githubSlug` (L111-116) + `computeAnchorSetFromContent` (L132-166) logic; the anchor-resolution gate below MIRRORS it exactly (strip-in-place double-hyphen behavior, `{#id}` handling, encounter-order dedup, fence mask). Do NOT edit this script or its `HUB_PATHS` (D-01-locked frozen roster).
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-RESEARCH.md Pattern 2 (Step 5 decision block, lines 148-161), Pattern 3 (Step 5a Kiosk GUI path + mutual-exclusion quote + MSIX/offline-license + AUMID discovery + WindowsAppKiosk pointer, lines 163-182), Pattern 4 (Step 5b SharedPC tables + AccountModel/EnableAccountManager/DeletionPolicy/InactiveThreshold/RestrictLocalStorage + MaintenanceStartTime, lines 184-223)
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-CONTEXT.md decisions A-LOCK-1..A-LOCK-5 (kiosk), F-LOCK-1..F-LOCK-7 + F-CROSS-NOTE (SharedPC internals), D-LOCK-2/D-LOCK-3 (branch mapping + Step 5a/5b heading style + anchor sync), D-LOCK-6/D-LOCK-7 (AVD-04 placement, AVD-05 boolean prose)
    - docs/_standards/EEE-SOP-standard.md STD-05 section (lines 456-548) — the Case-1/Case-2/Case-3 composite block format this fork instantiates live for the first time
    - docs/macos-lifecycle/01-psso-provisioning-walkthrough.md — RECOMMENDED non-converging branch idiom (link-not-copy sentence ~L111; non-reconvergence ~L65)
  </read_first>
  <action>
    Append to docs/recipes/01-shared-windows-avd-client.md:
    - Step 5 STD-05 Case-1 decision block (RESEARCH Pattern 2): the `> **Ask the admin:**` lead-in blockquote (blank line before the table, D-02) + the 4-column `Option | When to choose | Consequence if wrong | Branch` table whose Branch cells link `#step-5a-kiosk-configuration` / `#step-5b-shared-pc-configuration` (D-LOCK-3 anchor sync — single hyphen slugs, avoid the double-hyphen trap). The heading text `### Step 5a: Kiosk configuration` slugifies (strip-in-place) to `step-5a-kiosk-configuration`; author the Branch-cell anchors to match EXACTLY — the anchor-resolution gate below fails the task if they diverge.
    - `### Step 5a: Kiosk configuration` (A-LOCK-2, first-party Intune Kiosk-template GUI path, portal prose only): Templates > Kiosk → Single app, full-screen kiosk → Auto logon → Add Store app (the Windows App from Step 4). Must-carry callouts (A-LOCK-5): (i) Shell-Launcher/Assigned-Access mutual exclusion with the first-party quote; (ii) MSIX machine-wide provisioning tied to Step 4's device-context Required deployment (else kiosk launches to a missing app); (iii) autologon-account offline-Store-license caveat as a `[ASSUMED]` plan-time verification item (Assumption A2); (iv) "configure from console, not over RDP" one-liner (`[ASSUMED]`, A3); (v) optional advanced pointer to `Azure/WindowsAppKiosk` ONLY, never `Azure/AVDClientKiosk`. AUMID: instruct discovery via `Get-StartApps` on a reference device, do NOT hardcode a guessed AUMID (A-LOCK-4 / A5). Keep the AVDClientKiosk warning inline here (not a table row, E-LOCK-1). Raw AssignedAccess XML at most an optional advanced pointer.
    - `### Step 5b: Shared PC configuration` (F-LOCK series, Intune Templates > Shared multi-user device GUI path): the "Recorded-as" table (RESEARCH Pattern 4, GUI Setting | GUI Value | CSP Node | Recorded As) asserting EnableSharedPCMode=Enable (branch premise, F-LOCK-4), AccountModel="Guest account: Domain" = 1 with the guest-empty-feed anti-note (F-LOCK-3), Local Storage: Disabled = RestrictLocalStorage true with the one-line polarity/why-here note — never "Enabled" (F-LOCK-2). EnableAccountManager as an explicit boolean if/then gate ("Account management = Enabled", F-LOCK-5). DeletionPolicy as a STD-05 Case-2 enumerable (3 values) under that gate (F-LOCK-6). InactiveThreshold as a NESTED conditional under the DeletionPolicy="…and inactive threshold" (2) arm only, GUI 0-60-day bound stated (F-LOCK-6). Callouts: account-deletion-clears-token-cache → fresh Entra auth next session (F-LOCK-7(i), cross-ref the G-LOCK-2 SharedPC feed check); local-storage why-here (F-LOCK-7(ii)); note Templates-vs-Settings-Catalog (F-LOCK-1). Use GUI wording "storage space," not "disk."
    - AVD-04 update-ring/`MaintenanceStartTime` block (D-LOCK-6): author's discretion single late-shared block OR per-branch (Claude's Discretion); if single shared, STATE that the underlying CSP differs per branch (SharedPC `MaintenanceStartTime` vs kiosk `ApplicationManagement/ScheduleForceRestartForUpdateFailures`, RESEARCH lines 221-223) — do not imply one shared CSP value.
    - AVD-05 wired-vs-Wi-Fi boolean if/then prose pair (D-LOCK-7), a late post-enrollment step or callout before `## Verification`, cross-linking docs/admin-setup-8021x/03-windows.md + 00-overview.md. NEVER inline any 802.1X config; do NOT over-structure as a full Step Na/Nb branch.
    Blank-line-chunk every multi-clause callout (C17 #12). SharedPC values render as tables, never code fences (D-LOCK-5).
  </action>
  <acceptance_criteria>
    - Headings `### Step 5a: Kiosk configuration` and `### Step 5b: Shared PC configuration` exist; the Step 5 decision-table Branch cells link `#step-5a-kiosk-configuration` and `#step-5b-shared-pc-configuration` exactly.
    - The anchor-resolution gate (see `<verify>`) — mirroring `check-nav-hub-links.mjs`'s GitHub-exact strip-in-place slugify — confirms EVERY in-doc `#fragment` link (Step 5 Branch cells, the Task-1 MSRDC/Wi-Fi anti-feature internal anchors) resolves against the file's own headings; a mis-slugified `#step-5a--kiosk`-class double-hyphen anchor (the D-LOCK-3 trap) fails the task.
    - Step 5a contains the Shell-Launcher/Assigned-Access mutual-exclusion statement, references `Get-StartApps` for AUMID discovery (no hardcoded AUMID), points to `Azure/WindowsAppKiosk` and NOT `Azure/AVDClientKiosk` (`grep -c 'AVDClientKiosk'` returns 0), and carries `[ASSUMED]` flags on the offline-license and console-not-RDP caveats.
    - Step 5b renders SharedPC settings as a markdown table (not a code fence); asserts "Guest account: Domain" (AccountModel 1) with the guest-empty-feed anti-note; asserts "Local Storage: Disabled" mapped to RestrictLocalStorage true (the string "RestrictLocalStorage = Enabled" is ABSENT); has an explicit Account-management Enabled gate; DeletionPolicy is a 3-value enumerable; InactiveThreshold appears only nested under the DeletionPolicy=2 arm with the "0-60 day" bound stated.
    - AVD-05 links `admin-setup-8021x/03-windows.md` and `00-overview.md` and inlines no 802.1X CSP/profile config; no `### Step` heading is created for the AVD-05 fork.
    - No fenced code block contains a SharedPC CSP value; every contiguous `>`-run in the appended content is ≤200 chars.
    - `node scripts/validation/c17-eee-contract.mjs --verbose` reports 0 violations.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && f=docs/recipes/01-shared-windows-avd-client.md && grep -q '### Step 5a: Kiosk configuration' "$f" && grep -q '### Step 5b: Shared PC configuration' "$f" && [ "$(grep -c 'AVDClientKiosk' "$f")" = "0" ] && ! grep -q 'RestrictLocalStorage = Enabled' "$f" && grep -q 'admin-setup-8021x' "$f" && node -e 'const fs=require("fs");const f="docs/recipes/01-shared-windows-avd-client.md";const c=fs.readFileSync(f,"utf8").replace(/\r\n/g,"\n");const L=c.split("\n");const mask=Array(L.length).fill(false);let fenced=false,fc="",fl=0;for(let i=0;i<L.length;i++){const m=L[i].match(/^(`{3,}|~{3,})/);if(!fenced){if(m){fenced=true;fc=m[1][0];fl=m[1].length;}}else if(m&&m[1][0]===fc&&m[1].length>=fl){fenced=false;}else{mask[i]=true;}}const slug=t=>t.toLowerCase().replace(/[^a-z0-9 _-]/g,"").replace(/ /g,"-");const set=new Set(),seen=new Map();for(let i=0;i<L.length;i++){if(mask[i])continue;for(const m of L[i].matchAll(/\{#([a-zA-Z0-9_-]+)\}/g))set.add(m[1]);}for(let i=0;i<L.length;i++){if(mask[i])continue;const m=L[i].match(/^#{1,6}\s+(.*)$/);if(!m)continue;if(/\s*\{#[a-zA-Z0-9_-]+\}\s*$/.test(m[1]))continue;const b=slug(m[1].replace(/\*\*/g,"").replace(/`/g,"").trim());const n=seen.get(b)||0;seen.set(b,n+1);set.add(n===0?b:b+"-"+n);}const bad=[];for(let i=0;i<L.length;i++){if(mask[i])continue;const re=/\[[^\]]*\]\(#([^)]+)\)/g;let m;while((m=re.exec(L[i])))if(!set.has(m[1]))bad.push((i+1)+": #"+m[1]);}if(bad.length){console.error("UNRESOLVED IN-DOC ANCHORS:\n"+bad.join("\n"));process.exit(1);}console.log("in-doc anchors OK: "+set.size+" headings, all #fragment links resolve");' && node scripts/validation/c17-eee-contract.mjs --verbose 2>&1 | grep -qiE '0 violation' && echo PASS || echo CHECK</automated>
  </verify>
  <done>Both non-converging branches fully worked (kiosk GUI path + mutual-exclusion + AUMID discovery + MSIX/license caveats; SharedPC recorded-as table + account-mgmt gate + DeletionPolicy enumerable + nested InactiveThreshold + RestrictLocalStorage correct polarity), AVD-04 maintenance block and AVD-05 802.1X cross-link present; in-doc `#fragment` anchors all resolve via the mirrored-slugify gate (double-hyphen trap caught); C17 corpus green.</done>
</task>

<task type="auto">
  <name>Task 3: Verification + Configuration-Caused Failures + See Also + final C17 green gate</name>
  <read_first>
    - docs/recipes/01-shared-windows-avd-client.md — the full file from Tasks 1-2 (read to reference exact Step 4/5a/5b anchors in the failures table and to word the per-branch verification checks)
    - scripts/validation/check-nav-hub-links.mjs — the anchor-set/slugify logic the Task-3 gate mirrors (do NOT edit it or its D-01-locked `HUB_PATHS`; it does not and must not scan this recipe)
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-RESEARCH.md Pattern 5 (Verification ready-to-copy markdown, lines 225-249) and Pattern 6 (Configuration-Caused Failures rows + runbook-gap routing, lines 251-260)
    - .planning/phases/130-recipe-1-shared-windows-avd-client-device/130-CONTEXT.md decisions G-LOCK-1..G-LOCK-5 + G-PLANNER-NOTE (verification structure, per-branch feed check, failures routing, runbook gap) and B-LOCK-4/G-LOCK-2 (per-branch feed re-population check), H-PLANNER-NOTE (app-group-assigned second user)
    - docs/admin-setup-apv1/08-self-deploying.md lines 94-110 — the RE-084 `- [ ]` verification idiom + failures-table shape to mirror
  </read_first>
  <action>
    Append to docs/recipes/01-shared-windows-avd-client.md:
    - Single `## Verification` H2 (G-LOCK-1, no per-branch H2s, no new H2 beyond the D-06 skeleton): a prose lead-in "Both branches — confirm first:" carrying only genuinely-shared `- [ ]` checks (G-LOCK-3: OOBE-completed-unattended one-liner; Entra-joined-not-hybrid + Intune-enrolled single-line gates; dynamic-group-membership gate WITH the minutes-to-hours latency caveat; Windows App present BEFORE any user signs in). Then two bold pseudo-heading labels `**Kiosk branch:**` and `**Shared PC branch:**` (never H3s), each with its per-branch checks. Per-branch feed check (G-LOCK-2): Shared PC branch = a second distinct, app-group-assigned Entra user signs in → feed auto-re-populates (note the app-group-assignment pre-check inline in the item per H-PLANNER-NOTE, not a separate checkbox); Kiosk branch = after a session reset, autologon relaunches Windows App and the next end user authenticates the feed interactively in-app (the autologon-launch check IS the feed-reach check). Declarative end-state `- [ ]` idiom, no "You should see X" (G-LOCK-4). DeletionPolicy/InactiveThreshold cleanup is NOT a checkbox (unobservable at provisioning time). Keep checklists bare — route failures via the table, no inline "if not, see X."
    - `## Configuration-Caused Failures` table (frozen 3-column header, G-LOCK-5): recipe-OWNED rows only, each branch-specific symptom prefixed `(Kiosk) …` / `(Shared PC) …` (or `(Both) …`) in the Misconfiguration cell (no Branch column). Route every "Runbook" cell to an in-recipe step anchor (#step-4-…, #step-5a-…, #step-5b-…) or an existing linked reference (RE-084/RE-080/RE-079/RE-137) — NEVER a fabricated runbook link (no AVD runbook exists; RESEARCH lines 251-260 give example rows). No cell may link a target whose path contains "runbook". No ESP-timeout row (LINK-owned).
    - `## See Also` — link the STD-05 spec, corrected RE-084, RE-177, and the 802.1X overview as relevant.
    - Run the anchor-resolution gate + the final C17 gate. In the SUMMARY, flag the AVD-runbook gap as a candidate future phase (G-PLANNER-NOTE) and record which `[ASSUMED]` items (Windows App session-reset node names, exact AUMID, offline-license inference) remain author-time-verify flags.
  </action>
  <acceptance_criteria>
    - Exactly one `## Verification` H2 exists; it contains the "Both branches — confirm first:" lead-in and the two bold pseudo-headings `**Kiosk branch:**` / `**Shared PC branch:**` (no `### ` heading inside Verification).
    - The shared checklist includes the dynamic-group-membership check WITH a minutes-to-hours latency caveat and a "Windows App present BEFORE any user signs in" check; the Shared PC branch feed check names a "second distinct" app-group-assigned Entra user; the Kiosk branch feed check describes post-reset autologon + interactive in-app sign-in (no "second distinct user" wording on the kiosk branch).
    - `## Configuration-Caused Failures` uses the frozen `| Misconfiguration | Symptom | Runbook |` header; every Runbook cell resolves to an in-file anchor or an existing docs/ path. The automated anchor-resolution gate (see `<verify>`) — which mirrors `check-nav-hub-links.mjs`'s GitHub-exact slugify/anchor-set logic but scans THIS recipe (which `check-nav-hub-links.mjs` never does — its `HUB_PATHS` is a D-01-locked 4-file frozen roster) — confirms every in-doc `#fragment` Runbook/anti-feature/See-Also/branch-cell anchor resolves; and `grep` confirms no cell links a target path containing "runbook" (G-LOCK-5 fabricated-link guard).
    - Total H2 set equals the D-06 skeleton (Summary, Prerequisites, Unsupported and Anti-Feature Callouts, Steps, Verification, Configuration-Caused Failures, See Also) — no extra H2s.
    - `node scripts/validation/c17-eee-contract.mjs --self-test` exits 0 (4/4) AND `--verbose` full-corpus run reports 0 violations with the completed recipe enrolled.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && f=docs/recipes/01-shared-windows-avd-client.md && [ "$(grep -c '^## Verification' "$f")" = "1" ] && grep -q '\*\*Kiosk branch:\*\*' "$f" && grep -q '\*\*Shared PC branch:\*\*' "$f" && grep -q '^## Configuration-Caused Failures' "$f" && ! grep -qiE '\]\([^)]*runbook[^)]*\)' "$f" && node -e 'const fs=require("fs");const f="docs/recipes/01-shared-windows-avd-client.md";const c=fs.readFileSync(f,"utf8").replace(/\r\n/g,"\n");const L=c.split("\n");const mask=Array(L.length).fill(false);let fenced=false,fc="",fl=0;for(let i=0;i<L.length;i++){const m=L[i].match(/^(`{3,}|~{3,})/);if(!fenced){if(m){fenced=true;fc=m[1][0];fl=m[1].length;}}else if(m&&m[1][0]===fc&&m[1].length>=fl){fenced=false;}else{mask[i]=true;}}const slug=t=>t.toLowerCase().replace(/[^a-z0-9 _-]/g,"").replace(/ /g,"-");const set=new Set(),seen=new Map();for(let i=0;i<L.length;i++){if(mask[i])continue;for(const m of L[i].matchAll(/\{#([a-zA-Z0-9_-]+)\}/g))set.add(m[1]);}for(let i=0;i<L.length;i++){if(mask[i])continue;const m=L[i].match(/^#{1,6}\s+(.*)$/);if(!m)continue;if(/\s*\{#[a-zA-Z0-9_-]+\}\s*$/.test(m[1]))continue;const b=slug(m[1].replace(/\*\*/g,"").replace(/`/g,"").trim());const n=seen.get(b)||0;seen.set(b,n+1);set.add(n===0?b:b+"-"+n);}const bad=[];for(let i=0;i<L.length;i++){if(mask[i])continue;const re=/\[[^\]]*\]\(#([^)]+)\)/g;let m;while((m=re.exec(L[i])))if(!set.has(m[1]))bad.push((i+1)+": #"+m[1]);}if(bad.length){console.error("UNRESOLVED IN-DOC ANCHORS:\n"+bad.join("\n"));process.exit(1);}console.log("in-doc anchors OK: "+set.size+" headings, all #fragment links resolve");' && node scripts/validation/c17-eee-contract.mjs --self-test 2>&1 | grep -q '0 failed' && node scripts/validation/c17-eee-contract.mjs --verbose 2>&1 | grep -qiE '0 violation' && echo PASS || echo CHECK</automated>
  </verify>
  <done>Single Verification H2 with shared lead-in + two bold-pseudo-heading branch checklists (per-branch feed checks per G-LOCK-2); Configuration-Caused Failures routes only to in-recipe anchors/existing refs (no fabricated runbook links — grep + anchor-resolution gate prove it); See Also complete; D-06 H2 set exact; every in-doc `#fragment` link resolves; C17 self-test 4/4 and corpus 0 violations; ASSUMED flags + AVD-runbook-gap recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only phase. No executable surface, no data flow, no trust boundary, no external package installs. The deliverable is one static Markdown Guide in a C17-validated corpus; it documents Microsoft's Intune/Windows platform-security surface (Assigned Access, SharedPC CSP, Entra join) but introduces no runtime code, auth, session, input-validation, or crypto into this repository. The only gates are the frozen C17 validator (never edited, D-10) and a read-only inline `node -e` anchor-resolution check (no writes, no network, no new file/dependency — mirrors the existing `check-nav-hub-links.mjs` slugify without touching it). Package Legitimacy Gate does not fire (no npm/pip/cargo installs).
</threat_model>

<verification>
- File exists with `doc_id: RE-222`, `platform: Windows`, real `last_verified`; C17 `--self-test` 4/4 and `--verbose` 0 corpus violations with the recipe enrolled.
- Anti-feature table = exactly 4 rows in locked order; Wi-Fi cell qualified; MSRDC date once.
- Both Step 5a/5b branches fully worked and non-converging; Step 5 Branch-cell anchors resolve; mutual-exclusion stated; RestrictLocalStorage polarity correct; InactiveThreshold nested.
- Single Verification H2 with shared lead-in + two bold-pseudo-heading branch checklists; per-branch feed checks correct (SharedPC auto-re-populate vs kiosk interactive).
- Configuration-Caused Failures routes only to in-recipe anchors / existing refs (no fabricated runbook links); the automated anchor-resolution gate (mirrors `check-nav-hub-links.mjs` slugify; scans THIS recipe, which the frozen 4-hub script never does) reports every in-doc `#fragment` link resolves — catching the D-LOCK-3 double-hyphen slug trap — and `grep` confirms no Runbook cell links a path containing "runbook".
- Prerequisites + anti-feature Wi-Fi row link the corrected RE-084 (never restate); no stray Wi-Fi supported/unsupported claim outside the anti-feature cell; ZTDId string not inlined; 802.1X linked not inlined.
</verification>

<success_criteria>
`docs/recipes/01-shared-windows-avd-client.md` delivers ROADMAP SC1-SC4: a complete, C17-green Shared Windows AVD-client provisioning recipe with a linear happy-path spine, both fully-worked kiosk/SharedPC branches, the 4-row anti-feature table, session-hygiene/patch decision points, and the 802.1X cross-link — implementing every LOCKED CONTEXT.md decision without re-opening it, linking (never restating) the corrected RE-084, and with every in-doc `#fragment` anchor proven to resolve by an automated slugify-mirrored gate.
</success_criteria>

<output>
Create `.planning/phases/130-recipe-1-shared-windows-avd-client-device/130-02-SUMMARY.md` when done, recording: the assigned doc_id (RE-222) and its Phase-132 registry-row/status-flip handoff; the `[ASSUMED]` author-time-verify items resolved or still flagged (session-reset node names, exact AUMID, offline-license inference); the AVD-runbook-gap future-phase flag (G-PLANNER-NOTE); and the outcome of the in-doc anchor-resolution gate (headings scanned, all `#fragment` links resolved).
</output>
