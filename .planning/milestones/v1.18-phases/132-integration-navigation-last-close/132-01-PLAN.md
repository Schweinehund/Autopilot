---
phase: 132-integration-navigation-last-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/recipes/01-shared-windows-avd-client.md
  - docs/recipes/02-shared-ipad-full-provisioning.md
  - docs/_registry/RE-index.md
  - scripts/pipeline/filename-map.md
autonomous: true
requirements: [CLASS-03]
must_haves:
  truths:
    - "Both recipes read Status: Approved consistently across frontmatter, byline, and registry row"
    - "RE-222 and RE-223 have registry rows in RE-index.md immediately after RE-221"
    - "filename-map.md is regenerated (not hand-edited) and lists both recipes"
    - "Zero pipeline CODE changes — build-filename-map.mjs is run, never edited"
  artifacts:
    - path: "docs/recipes/01-shared-windows-avd-client.md"
      provides: "RE-222 recipe, status flipped to Approved"
      contains: "status: Approved"
    - path: "docs/recipes/02-shared-ipad-full-provisioning.md"
      provides: "RE-223 recipe, status flipped to Approved"
      contains: "status: Approved"
    - path: "docs/_registry/RE-index.md"
      provides: "RE-222 + RE-223 rows at Approved"
      contains: "RE-222"
    - path: "scripts/pipeline/filename-map.md"
      provides: "Regenerated map including both recipes"
      contains: "RE-222"
  key_links:
    - from: "docs/_registry/RE-index.md"
      to: "scripts/pipeline/filename-map.md"
      via: "node scripts/pipeline/build-filename-map.mjs (parseRegistry reads every RE- row)"
      pattern: "RE-22[23]"
---

<objective>
Register both already-authored recipes into the publish pipeline: flip RE-222 and RE-223 from Draft to Approved, append their registry rows after RE-221, and regenerate `filename-map.md` so both recipes enter the publish set. This satisfies CLASS-03.

Purpose: Prove the registry -> filename-map pipeline end-to-end on real recipe content with ZERO pipeline code changes. The recipes flow through the existing generic generator untouched.
Output: Two Approved recipes, two new registry rows, a regenerated map artifact.

<deferral_note priority="MUST-HONOR">
KNOWN LATENT LANDMINE — do NOT act on it in this phase. `build-filename-map.mjs`'s internal `--self-test` assertion (c) hardcodes "yields exactly 221 rows" (line ~279). After registering RE-222/223 the registry becomes 223 rows, so `node scripts/pipeline/build-filename-map.mjs --self-test` would FAIL assertion (c). This self-test is NOT wired into any gate/validator/CI (verified: check-phase-124 asserts only file-exists+non-empty; check-phase-126 asserts only exports parseRegistry; no gate runs --self-test), so it does NOT block Phase 132.

DECISION (pre-made, do NOT reopen): LEAVE `build-filename-map.mjs` byte-unchanged. It was authored in Phase 124 (v1.16, a SHIPPED/frozen milestone); editing it — even the fixture count — violates CLASS-03 "zero pipeline code changes" and the byte-unchanged-frozen-surface invariant. The stale count is a known-issue deferral to Phase 133 (the tooling-debt phase scoped to touch frozen tooling under a D-00a exception). Do NOT run `--self-test` as a verification gate in this plan; run the generator WITHOUT `--self-test`.
</deferral_note>
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-integration-navigation-last-close/132-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip both recipes Draft -> Approved</name>
  <files>docs/recipes/01-shared-windows-avd-client.md, docs/recipes/02-shared-ipad-full-provisioning.md</files>
  <read_first>
    - docs/recipes/01-shared-windows-avd-client.md (lines 1-15: frontmatter `status:` line + the `**Platform:** ... . **Status:** Draft` byline line)
    - docs/recipes/02-shared-ipad-full-provisioning.md (lines 1-15: same two locations)
  </read_first>
  <action>
    In each recipe, change the frontmatter `status: Draft` value to `status: Approved`, and change the trailing `Status:** Draft` token in the rendered byline line to `Status:** Approved`. Change ONLY the two `Draft` occurrences per file (frontmatter value + byline token). Do NOT touch doc_id, doc_type (stays `Guide` per Phase 129 D-02), platform, last_verified, review_by, applies_to, or any body content. `doc_type` MUST remain `Guide` — never a "Recipe" value.
  </action>
  <verify>
    <automated>test $(grep -rn 'Draft' docs/recipes/01-shared-windows-avd-client.md docs/recipes/02-shared-ipad-full-provisioning.md | grep -iE 'status' | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'status: Approved' docs/recipes/01-shared-windows-avd-client.md` returns 1; same for the 02 file
    - `grep -c 'Status:\*\* Approved' docs/recipes/01-shared-windows-avd-client.md` returns 1; same for 02
    - no `Draft` remains in any status/byline context in either recipe (grep for Draft returns no status-line or byline hits)
    - `grep -c 'doc_type: Guide' docs/recipes/01-shared-windows-avd-client.md` still returns 1 (unchanged); same for 02
  </acceptance_criteria>
  <done>Both recipes read `Approved` in frontmatter status AND byline; doc_type unchanged; no body edits.</done>
</task>

<task type="auto">
  <name>Task 2: Append RE-222/223 registry rows and regenerate filename-map</name>
  <files>docs/_registry/RE-index.md, scripts/pipeline/filename-map.md</files>
  <read_first>
    - docs/_registry/RE-index.md (lines ~234-238: 5-column table header context + RE-218..RE-221 rows for exact row format `| RE-NNN | <path> | <title> | <doc_type> | <status> |`; last data row is RE-221 at ~line 237, followed by `## Review Notes`)
    - docs/recipes/01-shared-windows-avd-client.md (line 15 H1 for the Title column text)
    - docs/recipes/02-shared-ipad-full-provisioning.md (line 15 H1 for the Title column text)
    - scripts/pipeline/filename-map.md (lines 1-11: "GENERATED FILE — DO NOT HAND-EDIT" header + the `node scripts/pipeline/build-filename-map.mjs` regen invocation)
  </read_first>
  <action>
    Append exactly two data rows to `docs/_registry/RE-index.md` immediately after the RE-221 row (before the `## Review Notes` section): a RE-222 row with path `docs/recipes/01-shared-windows-avd-client.md`, doc_type `Guide`, status `Approved`; and a RE-223 row with path `docs/recipes/02-shared-ipad-full-provisioning.md`, doc_type `Guide`, status `Approved`. Title column: reuse each recipe's H1 (Claude's discretion per CONTEXT — keep consistent with existing RE-row title style). Match the exact `| Doc ID | Path | Title | Doc Type | Status |` 5-column pipe format of the RE-221 row.

    Then regenerate the map by running `node scripts/pipeline/build-filename-map.mjs` (writes `scripts/pipeline/filename-map.md`). Do NOT pass `--self-test` (see deferral_note). Do NOT hand-edit filename-map.md and do NOT edit build-filename-map.mjs — regeneration only. Map inclusion is purely by registry-row presence, so the regen picks up both recipes automatically.
  </action>
  <verify>
    <automated>test $(grep -cE '^\| RE-[0-9]+ ' docs/_registry/RE-index.md) -eq 223 && node scripts/pipeline/build-filename-map.mjs && grep -q 'RE-222' scripts/pipeline/filename-map.md && grep -q 'RE-223' scripts/pipeline/filename-map.md && git diff --quiet scripts/pipeline/build-filename-map.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\| RE-[0-9]+ ' docs/_registry/RE-index.md` returns 223 (was 221)
    - registry contains a row `| RE-222 | docs/recipes/01-shared-windows-avd-client.md | ... | Guide | Approved |` and a `| RE-223 | docs/recipes/02-shared-ipad-full-provisioning.md | ... | Guide | Approved |` row, positioned after RE-221 and before `## Review Notes`
    - `node scripts/pipeline/build-filename-map.mjs` exits 0
    - `scripts/pipeline/filename-map.md` contains a line with `RE-222` and a line with `RE-223`
    - `git diff --quiet scripts/pipeline/build-filename-map.mjs` succeeds (generator byte-unchanged — zero pipeline code changes)
    - `--self-test` was NOT used as a gate
  </acceptance_criteria>
  <done>Registry has RE-222/223 at Approved; filename-map regenerated listing both; generator unedited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (docs-only) | Change surface is Markdown registry edits + a deterministic regeneration of a committed build artifact. No new executable code, network, auth, or user-input path is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-01 | Tampering | RE-index.md registry integrity (bad/duplicate RE-NNN row or path collision) | mitigate | build-filename-map.mjs is fail-closed on D-08 title collisions; the row-count (223) + RE-222/RE-223 presence + generator-byte-unchanged assertions catch a malformed row; C17 corpus gate (Plan 02) re-validates enrollment |
| T-132-02 | Tampering | build-filename-map.mjs frozen surface | accept | Generator is NOT edited (git diff --quiet asserts byte-unchanged); the stale --self-test fixture is a non-gating dev-tool count deferred to Phase 133, not touched here |
| T-132-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no supply-chain surface |
</threat_model>

<verification>
- `grep -cE '^\| RE-[0-9]+ ' docs/_registry/RE-index.md` == 223
- Both recipes: `status: Approved` in frontmatter AND `Status:** Approved` in byline; no `Draft` in status contexts
- `node scripts/pipeline/build-filename-map.mjs` exits 0; filename-map.md lists RE-222 and RE-223
- `git diff --quiet scripts/pipeline/build-filename-map.mjs` (generator unchanged)
</verification>

<success_criteria>
Both recipes registered at Approved, filename-map regenerated with both recipes, zero pipeline code changes (CLASS-03 mechanical pipeline complete).
</success_criteria>

<output>
Create `.planning/phases/132-integration-navigation-last-close/132-01-SUMMARY.md` when done.
</output>
