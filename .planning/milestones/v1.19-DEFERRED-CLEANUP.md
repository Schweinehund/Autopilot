# v1.19 Deferred Cleanup — v1.20+ Backlog

**Authored:** 2026-08-04 (Phase 138 Plan 138-06 close-gate — canonical `.planning/milestones/` artifact)
**Milestone:** v1.19 (Device Configuration Recipes #3 & #4 (Windows Multi-App Kiosk + Android Dedicated))
**Status:** FINALIZED at Phase 138 Plan 138-06 close-gate (2026-08-04).

**Authoring rationale:** v1.19 inherits the standalone deferred-cleanup artifact convention (Path-A from `.planning/milestones/v1.18-DEFERRED-CLEANUP.md`). Per the "**do NOT mask via deletion**" doctrine, all carried open items are PRESERVED verbatim and the new v1.19 deferrals are ADDED. Any item resolved during v1.19 is DROPPED here and recorded Closed in `.planning/milestones/v1.19-MILESTONE-AUDIT.md` (the "do NOT mask via deletion" doctrine governs *open* items only).

**Scope:** This document enumerates deferred **requirements** and **open technical items** only. Working-tree cruft (`.agents/`, `.obsidian/`, stray `graphify-out/`, `docs/graphify-out/`, `skills-lock.json`) is out of scope — deferred to `/gsd-complete-milestone`.

**Trigger-to-sweep:** Each section carries explicit criteria for when v1.20+ should pick up the work.

**This document is strictly log-only; the phase around it is not.** Zero corpus edits and zero fixes are made by authoring THIS file. The close-gate that lands in the same phase necessarily rewrites `PROJECT.md`/`ROADMAP.md`/`STATE.md`/`REQUIREMENTS.md` to flip 17 requirements — that rewrite is a phase-level action, not a deferred-cleanup-document action; the two are named separately here so they are never conflated.

**Resolved during v1.19 (dropped from the carried v1.18 list, recorded Closed):** `V118-PIN-DEFERRAL` (closed by HARN-14 — the mandatory back-anchor pin now lands, see the Dropped section below). No other carried item closes at this milestone.

---

# Part A — v1.19 New Deferrals

## V119-PIN-DEFERRAL: Freeze the v1.19 Corpus (Back-Anchor Invariant — v1.20's Path-A Job)

**Scope:** The `scripts/validation/_lib/frozen-at-close.mjs` `MILESTONE_CLOSE_SHAS` map does NOT contain a `V119` entry after this close, and MUST NOT — a pin referencing the v1.19 close-gate SHA would create ordering circularity (the close-gate cannot reference its own not-yet-existing SHA; the back-anchor invariant requires every pin to reference a PAST close SHA). Freezing the v1.19 corpus (adding `V119` + `readAtV119Close`) is the **successor milestone's** job: v1.20's harness-close phase adds the `V119` pin the same way v1.19's Plan 138-01 added `V118`.

**Why it is NOT a v1.19 item:** back-anchor invariant (mirrors `V118-PIN-DEFERRAL` / `V117-PIN-DEFERRAL` / `V116-PIN-DEFERRAL`). The close-gate SHA is recorded ONLY as a grep-recoverable subject-line-pair discriminator target, never as a pin, until the successor milestone freezes it.

**Where it belongs when resolved:** v1.20's harness-close phase — append `V119: '<v1.19 close-gate SHA>'` (recover via the subject-line pair discriminator `git log --all --format="%H|%s" | awk -F'|' '$2 ~ /v1\.19/ && $2 ~ /MILESTONE CLOSE/'`, POSITIVELY CONFIRMING the returned commit's SUBJECT LINE carries both tokens — the same false-positive risk this milestone's own Plan 138-01 ruled out when recovering V118 via the naive dual-token `--grep --all-match` form, which returns 2 candidates) + `readAtV119Close` export, following the V18..V118 single-entry pattern.

**Resolution trigger (v1.20):** the v1.20 harness lineage bump (18th Path-A milestone). MANDATORY at that close.

**Estimated effort:** included in the v1.20 harness-close Atom 2 (additive; < 1 plan on its own).

**Status:** DEFERRED to v1.20 (structural — the successor milestone pins the predecessor). NEW at v1.19 close.

---

## PRE-CHAIN-VALIDATOR-RED-30/31: check-phase-30/31 Standalone-RED (RE-SURFACED, not a new defect)

**Scope:** `scripts/validation/check-phase-30.mjs` and `scripts/validation/check-phase-31.mjs` sit OUTSIDE every `CHAIN_PHASES` array (never touched by any apex's cascade) and OUTSIDE the byte-unchanged gate (they are not frozen predecessor-close surfaces), yet both fail standalone against the evolved v1.19 corpus.

**RE-SURFACED, not NEW:** `.planning/milestones/v1.16-MILESTONE-AUDIT.md`'s Phase 125 closure narrative already root-caused `check-phase-30/51/92/99` as Phase-122 Mermaid-conversion-driven drift, found by the flag-#6 full predecessor-chain scoping run at that close. The defect worth naming here is that it was **never logged to any DEFERRED-CLEANUP document** in the four milestones since — this entry closes that gap, retroactively, rather than presenting a fresh discovery.

**All five failing assertions, named** (the Mermaid hypothesis covers only 1 of 5):
1. `check-phase-30.mjs` — "Expected 1-5 decision-diamond nodes, found 0" (the Mermaid-conversion-driven assertion).
2. `check-phase-30.mjs` — the `l1-template.md` `"Windows | macOS | iOS | all"` literal mismatch (NOT Mermaid-related).
3. `check-phase-31.mjs` — `V-31-23` (`docs/l2-runbooks/06-compliance-policy.md` line 182 vs. `expected-d23.txt`).
4. `check-phase-31.mjs` — `V-31-25` (L2 template iOS enum).
5. `check-phase-31.mjs` — `V-31-29` (runbook line counts ±15%).

**PRESERVATION CONSTRAINT (must survive any future fix):** `scripts/validation/check-phase-68.mjs` pins `check-phase-31.mjs` as a frozen call-site — `:100` (`V-68-04`, `check-phase-31.mjs` must remain among the `archive-path` helper's 5 import call-sites) and `:170-174` (`V-68-08`, `check-phase-31.mjs` must retain its `_missing` discriminator marker) — and `check-phase-68.mjs` IS in every apex's chain. Whoever fixes `check-phase-31.mjs` preserves both markers or takes the apex red.

**Why NOT fixed at v1.19:** fixing content drift is a corpus/tooling change inside a harness-close phase, which violates the HARNESS-PHASE discipline (the harness lineage bump never batches with content or drift remediation). Editing either validator's content assertions is out of scope for Phase 138.

**Where it belongs when resolved:** a future content or tooling milestone that explicitly scopes a `check-phase-30/31` remediation, preserving the `check-phase-68.mjs` call-site pins named above.

**Status:** RE-SURFACED (not new). DEFERRED to v1.20+.

---

## ACCEPTED-SCOPED-RED: Ten-Member Standalone-RED Chain-Validator Set, Four Root-Cause Classes

**Scope:** Ten standalone-RED chain validators, observed and enumerated by name at Phase 138 Plan 138-04: `check-phase-30, 31, 48, 60, 61, 62, 63, 64, 65, 66`. None of these are close blockers for `check-phase-138.mjs`'s own standalone-non-nested apex run (92 PASS / 0 FAIL / 1 SKIP), because the apex's own `CHAIN-N` checks assert only that each chain member's file exits 0 when invoked NESTED (`CHECK_PHASE_NESTED=1`), which short-circuits every one of these ten members' own drift-bearing sub-checks.

**Four root-cause classes, all four present in a single validator (`check-phase-61.mjs`), confirming that earlier records naming only two understated the failure by half:**

(a) **Self-test classifier vs. frozen Phase-43 fixture** — `regenerate-supervision-pins.mjs --self-test` fails at `docs/_glossary-android.md:145`. **This IS `DEFER-119-A`, one defect, not a distinct new one — see the double-booking guard below.** Present directly in: 48, 60, 61 (`V-61-34`), and re-manifests inside 62-66's own `AUDIT` sub-check (each generation's own frozen milestone-audit harness carries an equivalent freshness/self-test check against the same unresolved file).

(b) **v1.5-harness C5 freshness `90d > 60d`** on `docs/_glossary-android.md`, formally superseded by Phase 112's 90d lock. Present directly in: 60, 61 (`V-61-33`), and re-manifests inside 62-66's own `AUDIT` sub-check.

(c) **Cascading chain-guards** — 48 red → 60 red → 61 red → 62..66 inherit via their own `CHAIN-*` sub-checks (`check-phase-62`: `CHAIN-48/60/61` FAIL (3); `63`: `CHAIN-48/60/61/62` FAIL (4); `64`: 5; `65`: 6; `66`: 7).

(d) **Pre-chain content drift in 30/31** (outside every `CHAIN_PHASES` array — never touched by the cascade; see `PRE-CHAIN-VALIDATOR-RED-30/31` above for the full five-assertion detail).

**`check-phase-61.mjs`'s four individual failures, named per the standing D-16 ruling:**

| Check ID | Detail | Class |
|---|---|---|
| `V-61-21` | `check-phase-48.mjs` exits 0 (CHAIN regression-guard) FAIL | (c) cascade from 48 |
| `V-61-32` | `check-phase-60.mjs` exits 0 (CHAIN regression-guard) FAIL | (c) cascade from 60 |
| `V-61-33` | `v1.5-milestone-audit.mjs` C5 freshness FAIL — `docs/_glossary-android.md` (review_by-last_verified=90d (>60)) | (b) superseded freshness |
| `V-61-34` | `regenerate-supervision-pins.mjs --self-test` exits 0 (AUDIT-07 closure persistence) FAIL | (a) self-test classifier |

**MANDATORY STATEMENT:** fixing class (a) (the self-test classifier) and class (b) (the superseded freshness rule) would **NOT** green the ten-member set. Class (c) (cascading chain-guards, 62-66's `CHAIN-*` sub-checks) and class (d) (30/31's pre-chain content drift) are structurally independent of (a)/(b) and would survive both fixes untouched. A successor who "fixes" only (a)+(b) will find 30, 31, and the `CHAIN-*` legs of 62-66 still red.

**Why NOT fixed at v1.19:** editing any of the ten frozen validators or their upstream frozen dependencies is out of scope for a harness-close phase (HARNESS-PHASE discipline) and, for the frozen predecessors, is the forbidden D-00a frozen-surface-edit anti-pattern.

**Double-booking cross-reference (see the provenance footer for the guard's own result):** this entry's class-(a) content and `DEFER-119-A` are the SAME defect — `V-61-34` **is** `regenerate-supervision-pins.mjs --self-test`, failing at the identical `docs/_glossary-android.md:145` fixture-vs-classifier condition `DEFER-119-A` already tracks. `DEFER-119-A` (below) is the SOLE OWNER of that defect; this entry cross-references it and does not restate its detail.

**Status:** ACCEPTED, scoped, non-blocking for any apex-close purpose. DEFERRED to v1.20+.

---

## RECIPE-OUTBOUND-LINK-COVERAGE: Recipes 03/04's `../` Outbound Links Have No Validator Coverage

**Scope:** `scripts/validation/check-nav-hub-links.mjs` scans only links *from* the 4 troubleshooting hubs and links *targeting* the 4 hubs. All **42** `../`-relative outbound links recipes 03 and 04 ship (10 unique file targets, all 10 anchored) are checked by **no tool in this repository**. Phase 137 D-17 performed a one-off manual sweep before close (all 42 resolved against the filesystem, relative to `docs/recipes/`) — this is a point-in-time proof, not durable coverage; any future edit to either recipe or a linked target could silently break one of these 42 links with no validator to catch it.

**Where it belongs when resolved:** a future tooling milestone extending `check-nav-hub-links.mjs` (or a sibling checker) beyond the 4 hubs to cover recipe-originating outbound links generically.

**Resolution trigger:** a third or later recipe lands and the manual-sweep-per-close pattern becomes an operational cost, or a broken recipe outbound link is discovered in production.

**Status:** NEW at v1.19 close. DEFERRED to v1.20+ (v1.19 carries NO-TOOLING-PILLAR — no dedicated tooling-debt phase this milestone).

---

## V-132-HUBSNOTWIRED-REGEX-BROKEN: `check-phase-132.mjs`'s Hub-Coverage Regex Does Not Cover `docs/recipes` Generically

**Scope:** `scripts/validation/check-phase-132.mjs:97`'s `V-132-HUBSNOTWIRED` check tests `/docs\/recipes|01-shared-windows-avd|02-shared-ipad/` against the three troubleshooting-hub files. The hub files live *inside* `docs/`, so a real link from one of them reads `recipes/03-windows-11-multi-app-kiosk.md` (relative, no `docs/` prefix) — the `docs\/recipes` arm of the regex never fires, and the other two arms are hardcoded to recipes 01/02 by name. Verified directly: `/docs\/recipes|01-shared-windows-avd|02-shared-ipad/.test('[x](recipes/03-windows-11-multi-app-kiosk.md)')` evaluates `false`.

**Correction of record:** `.planning/milestones/v1.18-DEFERRED-CLEANUP.md`'s inherited assumption (repeated in this project's `REQUIREMENTS.md:31` before Phase 137's correction) that this check "bars `docs/recipes` … generically" is FALSE. Phase 137 (D-02) corrected this on the record; this entry carries the correction forward into the deferred-cleanup lineage.

**Why NOT fixed at v1.19:** `check-phase-132.mjs` is a v1.18 predecessor frozen surface under the D-00a byte-unchanged doctrine. Editing it is an explicit CARVE for zero gain, since Phase 137's new leaf validator (`check-phase-137.mjs`) covers recipes 03/04's hubs-not-wired assertion additively, using the corrected `recipes/03-` / `recipes/04-` literals.

**Where it belongs when resolved:** a future milestone that legitimately re-opens `scripts/validation/` predecessors could correct both the regex and any remaining stale claim about its coverage.

**Status:** NEW at v1.19 close (correction of record; the underlying regex stays frozen and unfixed by design). DEFERRED to v1.20+.

---

## HUB-WIRING-NON-BARRED-SURFACE: A Future Runbook May Cross-Link the Kiosk/MHS Recipes

**Scope, copied verbatim from `137-02-SUMMARY.md:197` (D-06):**

> If a kiosk-lockout or MHS-exit-PIN-lockout L1/L2 runbook is ever authored, the hub routes to THAT runbook and the runbook may cross-link the recipe; the three hubs never link `docs/recipes` directly.

**Scope line — the two surfaces this entry is named for:** `docs/l1-runbooks/00-index.md` and `docs/decision-trees/`. The drafted sentence above is about future runbook routing; the identifier `HUB-WIRING-NON-BARRED-SURFACE` is named for these two non-barred surfaces specifically (neither is one of the three troubleshooting hubs `V-132-HUBSNOTWIRED`/`check-phase-137.mjs` bar `docs/recipes` from) — without this scope line the entry loses the thing it is named for.

**Where it belongs when resolved:** whatever future phase authors a kiosk-lockout or MHS-exit-PIN-lockout L1/L2 runbook.

**Resolution trigger:** such a runbook is authored.

**Status:** NEW at v1.19 close. DEFERRED to v1.20+.

---

## C17-VS-PIPELINE-FENCE-MASK-DIVERGENCE: Column-0 vs. Leading-Whitespace-Tolerant Fence Masking

**Scope:** `scripts/validation/c17-eee-contract.mjs`'s fenced-content mask is anchored at **column 0** (the `^`-anchored fence regex, see the coordinate correction below), while `scripts/pipeline/convert.ps1:108`'s fence mask tolerates **leading whitespace** (`^\s*`). Latent for an XML payload (whose lines begin with `<` or whitespace) — but live the moment an indented fence contains a `>` or `|` line, since C17 would treat that line as unmasked prose (subject to its EEE-SOP structural checks) while the pipeline would still treat it as fenced content.

**Second candidate named at `135-01-SUMMARY.md:113`** (the sibling item to the HYG-05 unfalsified-extrapolation entry below — both are carried from the same source line, per the milestone's carrier convention).

**Coordinate correction (transcribed verbatim from `136-01-SUMMARY.md:203`, D-25.2 item 3 — appended here as this entry's own correction, NOT opened as a new entry):**

> **The `c17-eee-contract.mjs:150` → `:158` coordinate correction at all six sites** (`135-CONTEXT.md:48`, `:204`, `:287`; `135-01-SUMMARY.md:113`; `135-02-SUMMARY.md:196`; `135-PATTERNS.md:312`). The `^`-anchored fence regex is at `:158`; `:150` is the enclosing `inCodeFence` IIFE opener (T-6). Appended to the existing fence-mask entry rather than opening a new one. **Trigger:** the next phase that edits any of the six sites, or a dedicated tooling pass.

The six `.planning` sites carrying the superseded `:150` coordinate are historical planning artifacts and are **NOT edited** by this correction — none of `135-CONTEXT.md`, `135-01-SUMMARY.md`, `135-02-SUMMARY.md`, or `135-PATTERNS.md` is touched by Phase 138. The correction rides here, as this append, exactly as the handing-over summary mandated.

**Where it belongs when resolved:** a dedicated tooling pass unifying the fence-mask behavior between `c17-eee-contract.mjs` and `convert.ps1`.

**Resolution trigger:** an indented fence containing a `>` or `|` line is authored anywhere in the corpus, or a dedicated tooling milestone picks this item up proactively.

**Status:** NEW at v1.19 close (both the divergence entry and its coordinate-correction append). DEFERRED to v1.20+.

---

## HYG-05 Fenced-Content Retrieval Quality — Unfalsified Extrapolation

**First candidate named at `135-01-SUMMARY.md:113`** (transcribed in substance; the sibling second candidate is `C17-VS-PIPELINE-FENCE-MASK-DIVERGENCE` above — both entries are carried from the same source line so neither half of the handover is left on the floor).

**Scope:** HYG-05's shipped correction to `docs/_standards/EEE-SOP-standard.md` states that fenced content "retrieves poorly" (reaches the `.docx` body text and therefore the retrieval index, but as `SourceCode`/`*Tok` non-prose runs rather than prose). This clause is shipped as mandated by the requirement, but its own evidence base does not test it: `PIPE-02-FINDINGS.md` has **zero** occurrences of `fence`, `code block`, `SourceCode`, or `VerbatimChar` — retrieval quality of fenced content specifically was never tested by the standard's own cited PIPE-02 evidence. The honest formulation, shipped: fenced content reaches the index as non-prose runs; PIPE-02 did not test how well it retrieves.

**Why NOT fixed at v1.19:** falsifying (or confirming) the "retrieves poorly" clause requires a fresh retrieval-quality probe against the live Copilot Studio / SharePoint index — an operational grounding pass (mirrors the PIPE-02/PIPE-05 family), not a corpus-text edit, and out of scope for the content phase that surfaced it.

**Where it belongs when resolved:** a future PIPE-0N-style grounding pass that specifically probes fenced-content retrieval ranking/quality against the live index.

**Resolution trigger:** the next PIPE-family grounding pass, or a reported retrieval-quality complaint traceable to fenced content.

**Status:** NEW at v1.19 close. DEFERRED to v1.20+.

---

## CI-3 Scope Correction: Managed Apple ID → Managed Apple Account Corpus Rename (Text Correction Only)

**Scope:** `CI-3` (carried unresolved since v1.6, gated on Microsoft Intune adopting the rebrand portal-side — no in-repo evidence it has) is carried forward unchanged in substance below. This entry is the **text correction to its recorded scope only** — zero corpus edits.

**Corrected figures, derived with a markdown-only include filter (without it the count is inflated by the untracked `docs/graphify-out/` generated directory and a successor reads the corrected figure as stale again):**

```bash
grep -rn "Managed Apple ID" docs --include=*.md | wc -l   # occurrences: 57
grep -rl "Managed Apple ID" docs --include=*.md | wc -l   # files: 17
```

**Live scope is 57 occurrences / 17 files, not the previously recorded 45/16** — the sidecar-derived figure was stale by **+27% (occurrences-only label)**; the file-count increase (16→17) is a much smaller **+6%**. `REQUIREMENTS.md`'s own carried-backlog entry already carries these corrected 57/17 figures — this entry pins the exact derivation command so a successor does not re-derive 58/18 from the untracked directory and mistake the corrected 57/17 for stale.

**Status:** CARRIED from v1.6 → … → v1.19 (see the full CI-3 entry in Part B below for its unresolved-substance detail). This scope-correction sub-entry is NEW at v1.19 close.

---

## SHARED-TAXONOMY-DOC (Option B): A Shared Kiosk/Dedicated Taxonomy Reference Doc

**Transcribed verbatim from `136-01-SUMMARY.md:201` (D4.7's gate), per that summary's binding mandate at `:199` — "Phase 138/HARN-16 creates the file and transcribes these four entries verbatim":**

> **Option B — a shared kiosk/dedicated taxonomy Reference doc.** Barred here: the canary is locked at 223 → 225 and Phase 137 has a two-row budget (`ROADMAP.md:104` SC2, `REQUIREMENTS.md:30`). **Trigger:** a third lockdown recipe lands and the canary budget is free.

**Status:** NEW at v1.19 close, OPEN. Phase 135 ruled the taxonomy sentence for RE-224's own Scope banner only and deliberately did not duplicate it into RE-225.

---

## ANCHOR-REVIEW-BY-PAST-DUE: `05-dedicated-devices.md` Freshness Refresh Tracker

**Transcribed verbatim from `136-01-SUMMARY.md:202` (D4.7's gate), per the same binding mandate:**

> **The anchor's past-due `review_by: 2026-06-22`** (`docs/admin-setup-android/05-dedicated-devices.md`). Now 42 days past due as of this session's date (2026-08-03). This entry applies **because Task 1 took the NO-DRIFT branch.** **Trigger:** the next phase or milestone that touches this anchor doc refreshes its `last_verified`/`review_by` pair as a named correction.

**Status:** NEW at v1.19 close, OPEN. The HYG-06 spot-verification found NO DRIFT in the facts RE-225 depends on, which is precisely why the freshness metadata itself remains stale and needs a named landing spot rather than an unlogged drive-by edit to an Approved doc.

---

## ROLLBACK-RECOVERY-DIVERGENCE-COUNT: `## Rollback/Recovery` Template-Divergence Tally

**Transcribed verbatim from `136-01-SUMMARY.md:204` (D4.7's gate), per the same binding mandate:**

> **The `## Rollback/Recovery` template-divergence count.** `135-02-SUMMARY.md:195` set the trigger at *"when a third recipe needs the slot"*; RE-225 (Plan 2) will be the **second** recipe to carry this section, so the trigger stays unmet. Recorded here so the successor does not re-derive the count: **2-of-4** as of this phase. **Trigger:** a third recipe needs the `## Rollback/Recovery` slot.

**Status:** NEW at v1.19 close, OPEN (trigger unmet at 2-of-4). Recorded so the successor reads the count rather than re-deriving it.

---

# Correction of Record

## The Predecessor's False "Zero-Failure Full-Chain" Claim (D-24)

`v1.18-DEFERRED-CLEANUP.md:123` claims the "complete non-nested `[48..133]` chain" found **zero failures**. This claim is **FALSE** and is corrected here rather than repeated: `check-phase-134.mjs:143` unconditionally sets `CHECK_PHASE_NESTED:'1'` on every child it spawns, so that apex **structurally cannot** produce a genuinely non-nested chain result in the first place — and separately, `check-phase-48/60/61/62/63/64/65/66.mjs` all exit 1 standalone on inputs that have been unchanged since the v1.18 close-gate SHA `7af8a147`. `STATE.md:343` already warned the v1.19 planning artifacts away from inheriting this phrasing; this document records the correction explicitly rather than silently omitting the predecessor's claim. **This corrected accounting does not imply any v1.19 regression** — the ten-member standalone-RED set (`ACCEPTED-SCOPED-RED` above) was present, unremediated, at the v1.18 close too; only the predecessor's own characterization of it was wrong.

## The Predecessor Audit's Push-Semantics Claim (D-31)

`v1.18-MILESTONE-AUDIT.md:258` ("push … fires the `audit-harness-v1.18-integrity.yml` cascade") and `:290` ("the cascade only fires on push") are **both FALSE** — no workflow in `.github/workflows/` carries a `push:` trigger anywhere in this repository's history. The actual firing mechanism for this milestone's Axis-2 cascade was `workflow_dispatch` (`gh workflow run <name> --ref master`), which requires the workflow file to already be present on the default branch — the push is the *precondition* for dispatch, not the trigger itself. Recorded here as a correction of record; the predecessor document is not edited in place (a live option a successor could take per `6acc429b`'s post-close-append precedent, but not exercised by this phase). See `v1.19-MILESTONE-AUDIT.md` for the same correction restated in the audit narrative.

---

# Part B — Carried-Forward Open Items from v1.18 (PRESERVED verbatim per "do NOT mask via deletion")

*The following are preserved from `.planning/milestones/v1.18-DEFERRED-CLEANUP.md`. They remain unresolved / indefinite at v1.19 close. Source-of-truth, per-site root-cause detail, and trigger gates are unchanged except where explicitly updated below; see the v1.18 artifact for full prior-milestone detail.*

## CARVE-1 (FROZEN-AWARE-ADOPTION-SWEEP-01): Root Cause Survives — Re-Pin Does NOT Masquerade as "Sweep Resolved"

**Scope:** Broad-cohort frozen-aware conversion of the REMAINING predecessor content-assertion validators/harnesses that still read live HEAD instead of a frozen milestone-close snapshot — the permanent structural fix. Every `audit-harness-vX.Y-integrity.yml`'s `harness-run` job checks out the branch with NO `ref:` pin, so a frozen milestone-audit harness always runs against LIVE HEAD, not its own close-SHA corpus.

**v1.19 status:** unchanged. No v1.19 phase touched this root cause — v1.19 carries NO-TOOLING-PILLAR (no dedicated tooling-debt phase this milestone), and CARVE-1 is barred verbatim by its own routing from folding into a content/close milestone. The `ACCEPTED-STANDALONE-CI-RED` disposition below (its symptom-level manifestation) was again exercised and again accepted this milestone, now spanning v1.4–v1.18.

**Where it belongs when resolved (future dedicated tooling milestone):** unchanged from the v1.18 record — either (a) continue the targeted, scoped, per-close re-pin pattern indefinitely, or (b) convert the frozen predecessor harnesses' + `regenerate-supervision-pins.mjs`'s self-test classifier + every `audit-harness-vX.Y-integrity.yml`'s `harness-run` checkout to genuinely frozen-aware reads.

**Resolution trigger (v1.20+):** unchanged — escalate priority if the re-pin pattern needs to repeat 2+ more milestones in a row, or the standalone-RED noise starts obscuring genuine chain/apex regressions in the checks UI. Neither trigger fired this milestone (v1.19 needed zero coordinate re-pins — the pin-drift proof at Plan 138-02 found zero pinned-file drift across all 59 sidecar pins).

**Estimated effort:** unchanged from the v1.18 record (4-8h for option (b); < 1h per-milestone for continuing option (a) reactively).

**Status:** CARRIED from v1.8→v1.18. DEFERRED to a future dedicated tooling milestone (NOT necessarily v1.20 — v1.20's scope is not yet determined).

---

## WINDOWS-CLONE-DEEPNEST-TIMEOUT-01: Chain-Apex Cold-Clone Deep-Nesting Subprocess Cost on Windows (CARRIED — apex deepened to `[48..137]`, THIRD consecutive clean cycle — corroboration, not resolution)

Running any chain validator from a cold fresh-clone on Windows can exhibit an O(n²) cascade of cold Node subprocess spawns. **Carried forward from v1.18; the apex chain is now `[48..137]` (+4 deeper than v1.18's `[48..133]`).** **Honest v1.19 note:** `v1.15-MILESTONE-AUDIT.md:221` records "apex NOT run on Windows (cascade per D-119-2)" and v1.16 likewise — **not-run at v1.15/v1.16, clean at v1.17/v1.18, clean again at v1.19** (Plan 138-04's Axis-1 fresh clone: apex `[48..137]` completed in 19s, 92 PASS / 0 FAIL / 1 SKIPPED, no deep-nest stall). This is a **third consecutive clean cycle** and is recorded as corroborating evidence, NOT as resolution of the underlying item — a not-run history at v1.15/v1.16 is not the same as a clean history, and three clean cycles do not retire a known platform timeout; promoting it to a hard close-blocker would make a flaky win32 property gate the settled D-03 OS split. Linux GHA remains sole-authoritative per D-03. TOOL-05's CARVE-2 verify-and-attest (v1.18, Phase 133) confirmed the single-apex O(n) property holds via the `CHECK_PHASE_NESTED=1` guard — this item concerns the COLD-CLONE, NOT the within-apex, cost curve; the two are related but distinct, and this separation (already collapsed once by the v1.14 112-06 NESTED-guard remediation for the within-apex curve) is NOT re-scoped or re-collapsed at v1.19. **Remediation options (v1.20+):** unchanged — cache-warm clone / raise per-subprocess timeout / a genuinely frozen-aware `harness-run` checkout (would also help CARVE-1). **Status:** CARRIED FORWARD (unresolved in principle; empirically non-blocking for a third consecutive milestone). DEFERRED to v1.20+.

## ACCEPTED-STANDALONE-CI-RED: Frozen Predecessor Integrity Workflows Go RED Standalone on the Evolved Corpus (DISCHARGED at v1.19 with live evidence, then EXTENDED — now spans v1.4-v1.18)

The frozen `audit-harness-v1.4..v1.18-integrity.yml` workflows trigger on any PR touching `check-phase-*.mjs` and can FAIL standalone against the evolved v1.19 corpus (frozen harnesses pin their own close-SHA corpus). Editing frozen workflows/harnesses is barred by D-00a; a frozen milestone-audit validates its OWN close-SHA corpus, not future evolved corpus.

**v1.18's status at ITS OWN close was explicitly "disposition PENDING owner push, not pre-assumed"** (`v1.18-DEFERRED-CLEANUP.md:113`) — v1.18's close-gate landed local/unpushed, so whether the predecessor cascade actually went RED against v1.18's evolved corpus was not yet known when that document was authored.

**DISCHARGED at v1.19 with live evidence (Plan 138-05, 2026-08-04):** after the owner's push landed at shared SHA `0fd5589c`, all sixteen integrity workflows (the 15 pre-existing + the new v1.19 workflow) were dispatched and captured. **Corrected baseline: 5 PASS / 10 FAIL across the fifteen PRE-EXISTING workflows** (green = v1.14, v1.15, v1.16, v1.17, v1.18; red = base + v1.5..v1.13; 5+10=15, arithmetic holds against the 15-workflow pre-existing set). This **supersedes** the earlier-recorded figure of "7 PASS / 10 FAIL" (`STATE.md:346`/`:361`), which could not have held arithmetically against a 15-workflow set (7+10=17). With the new v1.19 workflow added (green), the full current sixteen-workflow set is **6 green / 10 red**.

All three `ACCEPTED-STANDALONE-CI-RED` conditions were verified individually from job-level JSON for every one of the ten red runs (not eyeballed via the checks UI): exactly one failed job per red run; that job is always a harness-replay / milestone-audit-harness job, never a chain/apex job; zero chain-job failures anywhere across all ten. Root cause is the unresolved `CARVE-1` (the `harness-run` job's ref-less checkout validates live HEAD rather than each frozen generation's own pinned content), NOT a v1.19 regression. The predecessor workflow (`audit-harness-v1.18-integrity.yml`) is excluded from this disposition by construction — it carries the sole standalone `check-phase-134` job in the entire sixteen-workflow set, so it MUST be green (confirmed: 89 PASS / 0 FAIL / 0 SKIPPED) rather than eligible for the fallback.

**Extended span, now DEFERRED again:** with the acute v1.18-close-time uncertainty now discharged, the SAME `ACCEPTED-STANDALONE-CI-RED` classification applies going forward, now spanning **v1.4-v1.18** (one milestone deeper than the v1.18 record's v1.4-v1.17 span). **Status:** DISCHARGED at v1.19 close (the not-yet-known disposition recorded at the v1.18 close is now resolved with live per-run evidence), then CARRIED forward at its now-extended v1.4-v1.18 span. DEFERRED to v1.20+.

## LATENT-NON-FROZEN-AWARE-CONTENT-ASSERTION-01: Predecessor Content Assertions Not Yet Frozen-Aware (re-scanned at v1.19 — WITH a correction to the predecessor's stated scan result)

Predecessor `check-phase-NN.mjs` validators carrying DIRECT content assertions reading LIVE HEAD rather than their own milestone close-SHA can drift on a broad-corpus milestone. **v1.19 note, WITH the correction of record (see the dedicated Correction-of-Record section above for the full detail):** the predecessor's own claim that its pre-close full-chain sweep was "non-nested" and found zero failures is FALSE — the predecessor apex structurally cannot run non-nested (it force-sets `CHECK_PHASE_NESTED=1` on every child spawn). Corrected accounting: v1.19's own PRE-scan (Plan 138-04) widened this item's scope beyond hardcoded `.planning/phases/` path readers to ALSO cover live REQUIREMENTS.md/ROADMAP.md readers — the class `/gsd-complete-milestone` (ROADMAP-collapse / REQUIREMENTS-overwrite) exercises. Result: `check-phase-31.mjs:3`'s hardcoded phase-path reference is comment-only (safe); `check-phase-70.mjs`/`check-phase-124.mjs` read hardcoded phase paths but frozen-aware (`readAtV17Close`/`readAtV116Close`, safe); the four new v1.19 leaves (135-138) and the apex contain ZERO `.planning/phases/` reads in code. **The named live-reading exposure: `check-phase-54.mjs:30-31,346` reads `REQUIREMENTS.md`/`ROADMAP.md` LIVE via `fs.readFileSync` with a negative assertion (`V-54-21`)** — the close-gate rewrites both documents, so this check's negative assertion reads whatever the two documents say at the moment it runs. It was scanned, not remediated (out of scope for a harness-close phase); the post-close-gate confirmatory apex run (`v1.19-MILESTONE-AUDIT.md`'s HARN-15 gate part 4) is what proves this exposure did not manifest a false result this close. `check-phase-61.mjs`/`check-phase-70.mjs` read the same two docs frozen-aware (safe); `check-phase-66.mjs` was confirmed to NOT read either doc at all (a naive-grep false positive on an error-string line-number citation only). **Status:** CARRIED from v1.18/v1.17/v1.16/v1.15 (re-scanned and WIDENED at v1.19, zero new frozen-aware conversions required, one live-reading exposure named and confirmed non-manifesting via the post-close-gate confirmatory run). DEFERRED to v1.20+ chain-validator retrospective (superset item `FROZEN-AWARE-ADOPTION-SWEEP-01` / CARVE-1 above covers the durable fix).

## DEFER-119-A: `regenerate-supervision-pins.mjs --self-test` Advisory-RED (Frozen-Fixture vs. Live-Corpus Divergence — CARRIED UNCHANGED; NOT re-opened, NOT new; ONE clause corrected; SOLE OWNER of the ACCEPTED-SCOPED-RED class-(a) defect)

`node scripts/validation/regenerate-supervision-pins.mjs --self-test` compares the LIVE Android corpus against the FROZEN `scripts/validation/v1.7-audit-allowlist.json` fixture (immutable Phase-43/v1.7 baseline) and exits 1 at `docs/_glossary-android.md:145`. **This item carries forward UNCHANGED — it is NOT re-opened and NOT new at v1.19.** Its complete classifier inputs (`androidDocPaths()` at `regenerate-supervision-pins.mjs:107-124`, the helper, and the frozen fixture) are byte-identical across `7af8a147..HEAD` — this was exit-1 at the v1.18 close too, so nothing about the v1.19 corpus caused or changed this condition.

**ONE genuinely false clause is corrected here, per the predecessor record's own error:** the v1.18 entry's parenthetical "no acute instance exists at this close" (`v1.18-DEFERRED-CLEANUP.md:129`) was FALSE — an acute instance existed at that close and still exists now, at `docs/_glossary-android.md:145`. This document does not repeat that false clause.

**SOLE OWNERSHIP of the `ACCEPTED-SCOPED-RED` class-(a) defect (the root-cause-level double-booking guard's ruling — see the provenance footer):** `V-61-34` (part of the ten-member `ACCEPTED-SCOPED-RED` set above) IS `regenerate-supervision-pins.mjs --self-test`, failing at this exact same `docs/_glossary-android.md:145` fixture-vs-classifier condition. `DEFER-119-A` is the SOLE OWNER of this defect; `ACCEPTED-SCOPED-RED` cross-references it above and does not restate its detail.

**Why it is NOT a v1.19 failure (accepted condition, unchanged):** the CI job that runs this helper is advisory (`continue-on-error: true`), structurally non-blocking regardless of outcome. Editing the `v1.7-audit-allowlist.json` fixture is the forbidden anti-pattern; relaxing the classifier is forbidden. A frozen fixture validates its OWN v1.7-era corpus, not the evolved v1.19 corpus. **Status:** ACCEPTED advisory-RED-capable condition (currently acute, corrected on the record above). CARRIED UNCHANGED from v1.18/v1.17/v1.16/v1.15/v1.14. Not a blocker. Resolution trigger unchanged: only if the advisory-RED becomes operationally noisy, or folds into the `FROZEN-AWARE-ADOPTION-SWEEP-01` / CARVE-1 v1.20+ remediation.

---

## Deployment — SharePoint Content-Approval (if Draft must gate retrieval)

**Scope:** v1.15/v1.16 PIPE-02 empirically confirmed `Status: Draft` is a LABEL only — Draft docs ARE indexed and retrieved. v1.17, v1.18, and v1.19 did not re-exercise this probe (no PIPE-02/grounding leg attaches to any of the three). If Draft must actually GATE retrieval (not just label), enabling + maintaining SharePoint content approval on the indexed library is required (tenant/ops config, not a doc change).

**Where it belongs when resolved:** a deployment-hardening task (operator responsibility).

**Resolution trigger:** when a genuine retrieval gate on Draft is required.

**Status:** CARRIED from v1.15/v1.16/v1.17/v1.18 (unresolved — deployment lever, not a doc/harness fix). Not re-exercised at v1.19 close.

---

## Infra — Azure AI Search Structured Index

**Scope:** Upgrade from the native SharePoint knowledge source to an Azure AI Search + SharePoint indexer to make `platform`/`doc_type` filterable metadata (the D1 normalized labels are already the right term set).

**Resolution trigger:** when metadata-filtered retrieval is required at scale.

**Status:** CARRIED from v1.14/v1.15/v1.16/v1.17/v1.18 (infra escalation). Unresolved at v1.19 close.

---

## Deployment — Auto-Upload to SharePoint (Graph API)

**Scope:** Push the publish bundle to the SharePoint document library via Graph API (needs app registration + `Sites.ReadWrite.All` + secret handling). v1.17 explicitly built the bundle only — owner performs the upload manually; v1.18 and v1.19 do not touch the publish pipeline auth surface at all (zero pipeline auth-code changes). Would introduce the first `secrets.` reference in any workflow in this repository, at tenant-wide `Sites.ReadWrite.All` scope.

**Where it belongs when resolved:** a future milestone adding the Graph/SharePoint auth surface (app registration, secret handling, `Sites.ReadWrite.All` scope).

**Resolution trigger:** if the manual per-milestone upload becomes an operational bottleneck.

**Status:** CARRIED from v1.17/v1.18. DEFERRED to v1.20+.

---

# Part C — Carried-Forward Open Items from v1.18 (Additional, cross-milestone since v1.10/v1.14)

## MTPSSO-01/02/03 + PSSO-FUT-03: Multi-Tenant Platform SSO (registration models, CA/compliance scoping, L2 troubleshooting, admin-setup notation)

Cross-tenant macOS Platform Credential registration, multi-tenant Conditional Access / compliance scoping, multi-tenant PSSO L2 troubleshooting, and the admin-setup counterpart notation. Net-new architectural scope. **Status:** DEFERRED to v1.20+. *(Carried v1.10 → … → v1.19.)*

## KRBFUT-01/02: On-Prem-AD-Only Kerberos Realm Deep-Dive + Azure Files Cloud-Kerberos GA

On-prem-AD-only Kerberos realm configuration; full Azure Files Cloud-Kerberos macOS coverage on GA promotion. **Status:** DEFERRED to v1.20+. *(Carried v1.10 → … → v1.19.)*

## CI-3: Managed Apple ID → Managed Apple Account Corpus Rename (57 occurrences / 17 files — corrected, see the scope-correction entry in Part A)

Corpus-wide rename pending Microsoft Intune adopting the rebrand portal-side. **Status:** DEFERRED to v1.20+. *(Carried v1.6 → … → v1.19; byte-unchanged hazard + motivated scope-padding — reconfirmed DEFERRED; the 57/17 figure is the corrected scope, see `CI-3 Scope Correction` above.)*

## AOSP-Wired 802.1X + Cloud PKI / Intune Certificate Connector Deep-Dive

Android AOSP-specific wired 802.1X depth; full Cloud PKI / Intune Certificate Connector cert-delivery deep-dive for 802.1X. **Status:** DEFERRED to v1.20+ (carried from v1.14 Out-of-Scope).

---

# Part D — Carried-Forward v1.8 Deferred Items (PRESERVED verbatim per "do NOT mask via deletion")

*Preserved from `.planning/milestones/v1.18-DEFERRED-CLEANUP.md` Part C. They remain unresolved / indefinite at v1.19 close.*

- **ARCHIVE-UPSTREAM-01:** Upstream PR to get-shit-done-cc for the `extractOneLinerFromBody` regex fix (vendored fallback canonical until upstream lands). DEFERRED. *(Carried v1.8 → … → v1.19.)*
- **v1.1 line 164 `Edit N --` token-class:** `check-phase-71.mjs` `PLACEHOLDER_TOKENS` extension surface; reactive pickup. Documented for awareness. *(Carried v1.8 → … → v1.19.)*
- **Multi-tenant Apple Business surfaces:** cross-tenant device transfer / multi-org-account. DEFERRED indefinitely. *(Carried v1.8 → … → v1.19.)*
- **Apple Business Device API documentation:** on Apple publishing the developer landing page. DEFERRED to v1.20+. *(Carried v1.8 → … → v1.19.)*
- **Per-OU Conference Room Display deep-dive:** TRACKER ONLY (no acute demand v1.6–v1.19). *(Carried v1.8 → … → v1.19.)*
- **Account Holder lockout dedicated recovery runbook:** CONDITIONAL (promote on 5+ escalations/quarter). *(Carried v1.8 → … → v1.19.)*
- **Apple School Manager (ASM) education-specific surfaces:** DEFERRED to v1.20+ (outside enterprise scope). *(Carried v1.8 → … → v1.19.)*

*(EXEC-FAIL-DETAIL-EXTRACTION-01 was closed at v1.14 by TOOL-01; HELPER-SPAWN-STDERR-01 was closed at v1.18 by TOOL-06 — both recorded there, not re-listed as open.)*

---

# Dropped — Closed at v1.19 (recorded Closed, per "do NOT mask via deletion" governing OPEN items only)

## V118-PIN-DEFERRAL — CLOSED at v1.19 via HARN-14

**What it was:** the mandatory back-anchor invariant deferred by v1.18's own close (`v1.18-DEFERRED-CLEANUP.md`'s Part A) — freezing the v1.18 corpus (`_lib/frozen-at-close.mjs` gaining a `V118` entry + `readAtV118Close` export) was structurally barred from v1.18's own close (ordering circularity: a close-gate cannot reference its own not-yet-existing SHA) and was the successor milestone's job.

**Closed by:** Phase 138 Plan 138-01 (HARN-14), which appended `MILESTONE_CLOSE_SHAS.V118 = '7af8a147'` + `readAtV118Close` to `scripts/validation/_lib/frozen-at-close.mjs` as a pure append-only edit (0 deletions), recovered the SHA via the subject-line pair discriminator (count=1, avoiding the 2-candidate false positive the naive dual-token `--grep --all-match` form produces), and proved the pin end-to-end (a real frozen-content read from `7af8a147`, plus a loud throw — not an empty-string false success — on an absent path).

**Dropped from the carried-forward list above.** Recorded Closed here and in `v1.19-MILESTONE-AUDIT.md`'s Requirements Traceability table (HARN-14 → Validated). Full detail: `138-01-SUMMARY.md`.

## CARVE-2 (TOOL-05 Re-Scope) — Closed at v1.18, not re-carried

**Status note (documentary hygiene, deliberate, not an oversight):** `CARVE-2` (the TOOL-05 verify-and-attest re-scope) was already CLOSED at the v1.18 close (`v1.18-DEFERRED-CLEANUP.md:57`) and was never carried into that document's own Part B (Carried-Forward Open Items) — it appeared only in v1.18's Part A as a NEW-but-already-CLOSED item, logged for the mandatory `133-CONTEXT.md` dual-carve-out completeness requirement (both CARVE-1 and CARVE-2 had to appear in that document). Because it was never open at the START of v1.19, it has no carried-forward entry in this document's Part B either. This one-line note exists so the departure from GA-3's original Option-B reasoning at v1.18 reads as deliberate, not as an unlogged drop.

---

## Cross-References

- **v1.19 milestone audit:** `.planning/milestones/v1.19-MILESTONE-AUDIT.md`
- **v1.18 predecessor (single-source-of-truth for carried items):** `.planning/milestones/v1.18-DEFERRED-CLEANUP.md`
- **Phase 135 HYG-05 unfalsified-extrapolation + fence-mask-divergence handover:** `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-01-SUMMARY.md`
- **Phase 136 four-entry binding handover (verbatim mandate):** `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-01-SUMMARY.md`
- **Phase 137 hub-wiring + outbound-link-coverage + hub-regex-correction handover:** `.planning/phases/137-integration-navigation-last-close/137-02-SUMMARY.md`, `137-CONTEXT.md`
- **Phase 138 pre-push evidence base (byte-unchanged gate, ten-member RED set, archival-drift scan):** `.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-04-SUMMARY.md`
- **Phase 138 cross-OS axis capture + cascade disposition (5/10 corrected baseline):** `.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-05-SUMMARY.md`
- **Phase 138 close-gate verification:** `.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-VERIFICATION.md`
- **v1.19 requirements (17/17 Validated):** `.planning/REQUIREMENTS.md`

---

## Provenance Footer

*Authored 2026-08-04 at Phase 138 Plan 138-06 close-gate.* **Part A: nine new entries** — `V119-PIN-DEFERRAL` (structural, back-anchor invariant), `PRE-CHAIN-VALIDATOR-RED-30/31` (RE-SURFACED not new, five assertions named, `check-phase-68.mjs` preservation constraint carried), `ACCEPTED-SCOPED-RED` (ten-member set, four root-cause classes, will-not-green-on-partial-fix statement), `RECIPE-OUTBOUND-LINK-COVERAGE`, `V-132-HUBSNOTWIRED-REGEX-BROKEN` (correction of record), `HUB-WIRING-NON-BARRED-SURFACE` (verbatim sentence + two-surface scope line), `C17-VS-PIPELINE-FENCE-MASK-DIVERGENCE` (with the six-site coordinate correction appended, not a new entry), the `HYG-05` unfalsified-extrapolation entry (the first of the two `135-01-SUMMARY.md:113` candidates), and the `CI-3` scope-correction text-only sub-entry (57/17, occurrences-only percentage label, markdown-only-filtered derivation command pinned). **Plus a dedicated Correction-of-Record section** carrying the D-24 false zero-failure claim correction and the D-31 push-semantics correction. **Part B: five items carried from v1.18** — `CARVE-1` (unchanged, no trigger fired this milestone), `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` (now `[48..137]`, third consecutive clean cycle recorded as corroboration not resolution), `ACCEPTED-STANDALONE-CI-RED` (DISCHARGED with live per-run evidence at this close, corrected 5/10 baseline, then re-extended to span v1.4-v1.18), `LATENT-NON-FROZEN-AWARE-CONTENT-ASSERTION-01` (re-scanned and widened, one live-reading exposure named), `DEFER-119-A` (carried UNCHANGED, one false clause corrected, now the explicit sole owner of the `ACCEPTED-SCOPED-RED` class-(a) defect) — plus the Deployment/Infra trio (SharePoint content-approval, Azure AI Search structured index, Graph-API auto-upload) restored to carried, all three grep-confirmed present. **Part C:** MTPSSO/PSSO-FUT-03, KRBFUT-01/02, CI-3, AOSP-wired-802.1X + Cloud-PKI, carried from v1.10/v1.14/v1.6. **Part D:** v1.8 items PRESERVED verbatim. **Dropped-and-Closed:** `V118-PIN-DEFERRAL` CLOSED by HARN-14 (dropped from the carried list, recorded Closed here and in `v1.19-MILESTONE-AUDIT.md`); `CARVE-2` documentary one-line note (already closed at v1.18, never carried, not re-listed as open, note added for deliberateness).

**Root-cause-level double-booking guard — result:** run against the frozen predecessor-deferral exclusion list AND, per D-27's explicit ruling, against this document's own new entries at content level (not identifier level only). Exact-identifier matching alone would have missed the one real double-book: `ACCEPTED-SCOPED-RED`'s class-(a) content and `DEFER-119-A` are the SAME defect (`V-61-34` IS `regenerate-supervision-pins.mjs --self-test`, failing at the identical `docs/_glossary-android.md:145` condition both would otherwise separately describe). **Ruling applied: `DEFER-119-A` is the SOLE OWNER of that defect; `ACCEPTED-SCOPED-RED` cross-references it and does not restate its detail** — both entries above already carry this ruling inline. No other double-book was found: `V119-PIN-DEFERRAL` is distinct from `V118-PIN-DEFERRAL` (successive milestones' own pins, not the same defect); `PRE-CHAIN-VALIDATOR-RED-30/31` is distinct from `ACCEPTED-SCOPED-RED` (the former is the five-assertion detail record for two of the ten members, explicitly cross-referenced by the latter's class-(d), not duplicated); `C17-VS-PIPELINE-FENCE-MASK-DIVERGENCE`'s coordinate-correction append is inside its OWN entry (per the explicit non-new-entry mandate), not a separate booking. **Guard result: CLEAN after the one correction above.**
