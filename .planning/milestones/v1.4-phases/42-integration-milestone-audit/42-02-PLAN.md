---
phase: 42
plan: 02
type: execute
wave: 1
depends_on: [42-01]
files_modified:
  - docs/reference/android-capability-matrix.md
autonomous: true
requirements: [AEAUDIT-01]
tags: [android, capability-matrix, reference-doc, cross-platform-equivalences]

must_haves:
  truths:
    - "An Intune admin can compare Android enrollment modes (COBO / BYOD / Dedicated / ZTE / AOSP) by feature across 5 locked domains (Enrollment / Configuration / App Deployment / Compliance / Software Updates)"
    - "A Cross-Platform Equivalences H2 provides exactly 3 paired rows mapping Apple↔Android capability analogs (iOS Supervision↔Android Fully Managed; Apple ADE↔Google Zero-Touch; iOS User Enrollment↔Android Work Profile)"
    - "The word 'supervision' / 'supervised' appears ONLY in the iOS column of the Cross-Platform Equivalences section (D-12 writing rule), protected by an inline HTML comment guard"
    - "Deferral footers preempt encroachment from Knox ME (v1.4.1), Full AOSP (v1.4.1), and 4-platform comparison (v1.5 AECOMPARE-01)"
    - "Matrix `review_by` is exactly 60 days after `last_verified` per Phase 34 D-14 Android-specific cycle (NOT 90-day iOS/macOS cycle)"
  artifacts:
    - path: "docs/reference/android-capability-matrix.md"
      provides: "Android mode × feature capability matrix with 5 domains + Cross-Platform Equivalences H2 + deferral footers"
      min_lines: 120
      contains_regex: "^## Cross-Platform Equivalences$"
  key_links:
    - from: "docs/reference/android-capability-matrix.md cells"
      to: "docs/android-lifecycle/02-provisioning-methods.md, docs/android-lifecycle/03-android-version-matrix.md, docs/admin-setup-android/06-aosp-stub.md"
      via: "relative-path markdown links from matrix cells (Anti-Pattern 1 guard — do not duplicate grids)"
      pattern: "\\]\\(\\.\\./android-lifecycle/|\\]\\(\\.\\./admin-setup-android/06-aosp-stub"
    - from: "Cross-Platform Equivalences section"
      to: "platform-attributed pair headers (iOS Supervision ↔ Android Fully Managed, Apple ADE ↔ Google Zero-Touch, iOS Account-Driven User Enrollment ↔ Android Work Profile)"
      via: "paired-row table with platform name in EVERY column header"
      pattern: "iOS Supervision|Apple Automated Device Enrollment|iOS Account-Driven User Enrollment"
---

<objective>
Author `docs/reference/android-capability-matrix.md` per AEAUDIT-01 / ROADMAP SC#1 (governs per D-07). This is the fourth platform capability matrix (siblings: `windows-vs-macos.md`, `macos-capability-matrix.md`, `ios-capability-matrix.md`) and introduces a new shape per D-08 (S-C Hybrid): Android-mode-first primary matrix + separate `## Cross-Platform Equivalences` H2 with exactly 3 paired rows.

Purpose: Give Intune admins a scannable Android mode × feature comparison and a bounded Apple↔Android equivalence view that answers "can Android do X" without reading every mode admin guide. Precludes AECOMPARE-01 v1.5 4-platform doc encroachment via explicit footer labeling.

Output: A new markdown file under `docs/reference/` with frontmatter, H1, lead paragraph, 5 locked domain H2s, Cross-Platform Equivalences H2 (with HTML comment guard), Key Gaps Summary H2, See Also H2, 3 Deferral footer sub-sections, and a Version History H2 + table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-integration-milestone-audit/42-CONTEXT.md
@.planning/phases/42-integration-milestone-audit/42-RESEARCH.md
@.planning/phases/42-integration-milestone-audit/42-PATTERNS.md
@docs/reference/ios-capability-matrix.md
@docs/reference/macos-capability-matrix.md
@docs/android-lifecycle/00-enrollment-overview.md
@docs/android-lifecycle/02-provisioning-methods.md
@docs/android-lifecycle/03-android-version-matrix.md
@docs/admin-setup-android/03-fully-managed-cobo.md
@docs/admin-setup-android/04-byod-work-profile.md
@docs/admin-setup-android/05-dedicated-devices.md
@docs/admin-setup-android/06-aosp-stub.md

<interfaces>
<!-- Structural contracts from the sibling iOS/macOS matrices. Do NOT re-explore — use these directly. -->

From docs/reference/ios-capability-matrix.md (PRIMARY structural template, 107 lines, VERIFIED):
- Frontmatter (lines 1-7): `last_verified`, `review_by` (iOS: +90 days; Android MUST use +60 days per D-16), `applies_to: both`, `audience: admin`, `platform: all` (Android uses `platform: Android`)
- H1 title (line 9): `# Intune: iOS/iPadOS Capability Matrix — Windows, macOS, iOS`
- Lead paragraph (line 11): 1-paragraph audience-mixed framing, points to sibling matrices
- H2 spine (lines 13, 30, 44, 57, 70, 80, 93, 103): `## Enrollment`, `## Configuration`, `## App Deployment`, `## Compliance`, `## Software Updates`, `## Key Gaps Summary`, `## See Also`, `## Version History`
- `🔒` inline glyph convention: `🔒 supervised ADE only` / `🔒 supervised ADE only for...` — applied inside cell text
- `## Key Gaps Summary` = 8 numbered bullets with `**Bold title** — explanation sentence` format
- `## Version History` explicit H2 (NOT the bare table form macos-capability-matrix.md uses)

From docs/android-lifecycle/00-enrollment-overview.md (VERIFIED lines 51-53 — Phase 34 D-03 narrative template for Cross-Platform Equivalences Row 1 body):
> Android's Fully Managed mode is the closest analog to iOS Supervision, but the mapping is partial — iOS supervision is a permanent per-device state gating ~60 restriction settings; Android Fully Managed is an ownership-mode designation.
> "Supervision" is not an Android management term — Android does not use "supervised" or "unsupervised" as device states.

From docs/admin-setup-android/03-fully-managed-cobo.md line 35 (VERIFIED — blockquote analog for COBO↔Supervision cross-platform framing):
> **Cross-platform note:** Android's Fully Managed is the closest analog to iOS Supervision on ADE-enrolled devices, but the mapping is partial — iOS supervision is a permanent per-device state that gates approximately 60 restriction settings on top of a normal MDM enrollment, whereas Android Fully Managed is an ownership-mode designation set at provisioning time.

Target column headers (D-10, exact, Android primary matrix):
`| Feature | COBO (Fully Managed) | BYOD (Work Profile) | Dedicated (COSU) | ZTE (Zero-Touch) | AOSP |`

Target section ordering (D-15, locked):
1. Frontmatter
2. `# Intune: Android Capability Matrix — Modes by Feature`
3. Lead paragraph
4. `## Enrollment` (anchor `#enrollment`)
5. `## Configuration` (anchor `#configuration`)
6. `## App Deployment` (anchor `#app-deployment`)
7. `## Compliance` (anchor `#compliance`)
8. `## Software Updates` (anchor `#software-updates`)
9. `## Cross-Platform Equivalences` (anchor `#cross-platform-equivalences`, + HTML comment guard)
10. `## Key Gaps Summary` (anchor `#key-gaps-summary`)
11. `## See Also` (anchor `#see-also`)
12. `---` + 3 Deferral sub-sections (KME / Full AOSP / 4-platform)
13. `---` + `## Version History` + table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/reference/android-capability-matrix.md — frontmatter, H1, lead paragraph, 5 domain H2s (primary matrix)</name>
  <files>docs/reference/android-capability-matrix.md</files>
  <read_first>
    - `docs/reference/ios-capability-matrix.md` (PRIMARY template, 107 lines — frontmatter shape, H1 format, lead-paragraph tone, 5 domain H2 spine, 🔒 glyph usage, Key Gaps/See Also/Version History format)
    - `docs/reference/macos-capability-matrix.md` (101 lines — sibling bilateral precedent, confirms 5-domain spine; note it uses bare-table Version History, NOT what Android uses)
    - `docs/android-lifecycle/00-enrollment-overview.md` (all — narrative template for Cross-Platform Equivalences Row 1; also supplies 5-mode canonical descriptions and tri-portal context)
    - `docs/android-lifecycle/02-provisioning-methods.md` (the provisioning-method canonical source — cells MUST link here, NOT duplicate grids — Anti-Pattern 1 D-13)
    - `docs/android-lifecycle/03-android-version-matrix.md` (canonical version-gate source — cells MUST link here for minimum-OS gates)
    - `docs/admin-setup-android/03-fully-managed-cobo.md` line 35 blockquote (cross-platform note — Row 1 body template)
    - `docs/admin-setup-android/04-byod-work-profile.md` (BYOD mode details — feature rows BYOD column)
    - `docs/admin-setup-android/05-dedicated-devices.md` (Dedicated / COSU / MHS details — feature rows Dedicated column)
    - `docs/admin-setup-android/06-aosp-stub.md` (AOSP stub — every AOSP cell must link here per D-10)
    - `.planning/phases/42-integration-milestone-audit/42-CONTEXT.md` decisions D-08 through D-18
    - `.planning/phases/42-integration-milestone-audit/42-PATTERNS.md` section "`docs/reference/android-capability-matrix.md`" (lines 26-192)
  </read_first>
  <action>
    Create `docs/reference/android-capability-matrix.md` with the following content in order. No other files touched. Use LF line endings; do not insert Byte Order Mark.

    **Frontmatter (lines 1-7)** — values as specified, `review_by` MUST be exactly 60 days after `last_verified`:
    ```yaml
    ---
    last_verified: 2026-04-24
    review_by: 2026-06-23
    applies_to: both
    audience: admin
    platform: Android
    ---
    ```
    (2026-04-24 + 60 days = 2026-06-23 — per D-16, NOT the 90-day cycle iOS/macOS uses. If plan execution date differs from 2026-04-24, use execution-date + 60 days for `review_by` and execution-date for `last_verified`.)

    **H1 (line 9)** — exact:
    ```
    # Intune: Android Capability Matrix — Modes by Feature
    ```

    **Lead paragraph (line 11)** — ~3-4 sentences. Claude's discretion on tone, but MUST:
    - Name all 5 modes once: COBO (Fully Managed), BYOD (Work Profile), Dedicated (COSU), ZTE (Zero-Touch), AOSP stub
    - Explicitly direct readers to `## Cross-Platform Equivalences` for Apple↔Android mappings
    - Reference sibling matrices by relative path: `[iOS Capability Matrix](ios-capability-matrix.md)` and `[macOS Capability Matrix](macos-capability-matrix.md)`
    - Reference `[Android Provisioning Lifecycle](../android-lifecycle/00-enrollment-overview.md)` for enrollment narrative

    **`## Enrollment` domain H2** (anchor `#enrollment` — heading text governs slug; do NOT add explicit `{#...}` for the 5 domain headings):
    - Table with the exact column header: `| Feature | COBO (Fully Managed) | BYOD (Work Profile) | Dedicated (COSU) | ZTE (Zero-Touch) | AOSP |`
    - Feature rows to include (≥8 rows; Claude's discretion on exact wording; MUST cover):
      - Tri-portal requirement (Intune + MGP + Zero-Touch portal — link to `[Zero-Touch portal setup](../admin-setup-android/02-zero-touch-portal.md)` in ZTE cell)
      - Provisioning methods per mode — cells link to `[provisioning methods](../android-lifecycle/02-provisioning-methods.md)` (NOT duplicate the grid)
      - Hardware identity / token model per mode (Google Account vs IMEI/serial upload)
      - User affinity / userless support per mode
      - DPC identity per mode (Android Device Policy vs Managed Google Play vs OEM DPC)
      - Minimum Android version gate — link to `[version matrix](../android-lifecycle/03-android-version-matrix.md)` (NOT duplicate)
      - Entra join / Entra Shared Device Mode availability per mode
      - AMAPI migration note (BYOD row) — footer link or inline note about Microsoft Intune app replacing Company Portal as BYOD DPC post-April 2025
    - AOSP column cells per D-10: all read `AOSP stub — see [06-aosp-stub.md](../admin-setup-android/06-aosp-stub.md)` EXCEPT the provisioning-method row, which may read `QR-only provisioning on select OEMs — see [stub](../admin-setup-android/06-aosp-stub.md)`.

    **`## Configuration` domain H2**:
    - Table header same 5-mode shape.
    - Feature rows (≥7): Settings Catalog applicability, restriction profile breadth (`🔒 COBO-only` glyph where applicable per D-18), Configuration Profiles vs OMA-URI post-AMAPI (BYOD — call out "OMA-URI removed April 2025 per AMAPI migration"), Policy Sets, app-config targeting, per-mode restriction-count summary. DDM is NOT applicable on Android — include a row that explicitly states "DDM: N/A (Android uses Play-integrated policy channel instead)".
    - AOSP cells per D-10 stub-reference rule.
    - Use `🔒 COBO-only` or `🔒 FM/DO-only` inline within cell text for mode-restricted capabilities (D-18).

    **`## App Deployment` domain H2**:
    - Table header same shape.
    - Feature rows (≥7): MGP as mandatory app channel (`Managed Google Play` link-out to glossary: `[Managed Google Play](../_glossary-android.md#managed-google-play)`); Silent install per mode (with `🔒 COBO-only` or `🔒 FM/DO-only` where applicable); LOB APK delivery path per mode; MHS (Managed Home Screen) — row body: "`🔒 Dedicated-only` — see [Dedicated Devices](../admin-setup-android/05-dedicated-devices.md)"; app-config per mode; work-profile app isolation (BYOD); AMAPI migration footnote (post-April 2025 Microsoft Intune app is BYOD DPC).
    - AOSP cells per D-10.

    **`## Compliance` domain H2**:
    - Table header same shape.
    - Feature rows (≥6): Play Integrity verdict levels — Basic / Basic+Device / Strong (link to `[Play Integrity](../_glossary-android.md#play-integrity)`); Conditional Access attestation requirements per mode; device-based restrictions per mode (with `🔒 COBO-only` glyph); password-policy enforcement per mode; compliance-investigation L2 runbook link (`[21-android-compliance-investigation.md](../l2-runbooks/21-android-compliance-investigation.md)`).
    - **CRITICAL (AEAUDIT-04 / C1):** Do NOT use the bare word "SafetyNet" anywhere in this section. If Play Integrity context requires deprecation framing, use phrasing like "Play Integrity (successor to the prior Google attestation API, turned off January 2025)" — both "successor" and "turned off" are allow-list-safe keywords per D-27 regex.
    - AOSP cells per D-10.

    **`## Software Updates` domain H2**:
    - Table header same shape.
    - Feature rows (≥4): System update policy support per mode, deferral window capabilities per mode (`🔒 COBO-only` for forced-deferral), Play Store app-update control, OEM update enforcement (with COBO/Dedicated restrictions noted inline).
    - AOSP cells per D-10.

    Commit message: `docs(42-02): add android-capability-matrix.md primary matrix — 5 domains, 5 modes (part 1/2)`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/reference/android-capability-matrix.md','utf8');const fm=c.match(/^---\n([\s\S]*?)\n---/);if(!fm){console.error('FAIL: no frontmatter');process.exit(1)};const pl=/platform:\s*Android/.test(fm[1]);const lv=fm[1].match(/last_verified:\s*(\d{4}-\d{2}-\d{2})/);const rb=fm[1].match(/review_by:\s*(\d{4}-\d{2}-\d{2})/);if(!pl){console.error('FAIL: platform must be Android');process.exit(1)};if(!lv||!rb){console.error('FAIL: last_verified and review_by required');process.exit(1)};const dd=(new Date(rb[1])-new Date(lv[1]))/86400000;if(dd!==60){console.error('FAIL: review_by - last_verified must be exactly 60 days, got '+dd);process.exit(1)};const h1=/^# Intune: Android Capability Matrix — Modes by Feature$/m.test(c);if(!h1){console.error('FAIL: H1 exact mismatch');process.exit(1)};const domains=['## Enrollment','## Configuration','## App Deployment','## Compliance','## Software Updates'];for(const d of domains){if(!c.includes('\n'+d+'\n')){console.error('FAIL: missing '+d);process.exit(1)}};const mode_header=/\| Feature \| COBO \(Fully Managed\) \| BYOD \(Work Profile\) \| Dedicated \(COSU\) \| ZTE \(Zero-Touch\) \| AOSP \|/.test(c);if(!mode_header){console.error('FAIL: primary matrix column header missing');process.exit(1)};const sn=(c.match(/SafetyNet/g)||[]).length;if(sn!==0){console.error('FAIL: '+sn+' SafetyNet occurrences — must be 0 (use Play Integrity phrasing)');process.exit(1)};console.log('PASS: frontmatter valid, 60-day cycle, 5 domains, mode header, zero SafetyNet')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^platform: Android$' docs/reference/android-capability-matrix.md` = **1**
    - `grep -c '^last_verified: 2026-04-24$' docs/reference/android-capability-matrix.md` = **1** (or current date if executing later)
    - `grep -c '^review_by: 2026-06-23$' docs/reference/android-capability-matrix.md` = **1** (or last_verified + 60 days)
    - `grep -c '^# Intune: Android Capability Matrix — Modes by Feature$' docs/reference/android-capability-matrix.md` = **1**
    - `grep -cE '^## (Enrollment|Configuration|App Deployment|Compliance|Software Updates)$' docs/reference/android-capability-matrix.md` = **5**
    - `grep -c 'COBO (Fully Managed) | BYOD (Work Profile) | Dedicated (COSU) | ZTE (Zero-Touch) | AOSP' docs/reference/android-capability-matrix.md` >= **1**
    - `grep -c '06-aosp-stub.md' docs/reference/android-capability-matrix.md` >= **5** (one per domain's AOSP column)
    - `grep -c 'SafetyNet' docs/reference/android-capability-matrix.md` = **0** (AEAUDIT-04 C1 precondition)
    - `grep -cE '\bsupervis(ion|ed|ory)\b' docs/reference/android-capability-matrix.md` (case-insensitive) = **0** at this point (Cross-Platform Equivalences section added in Task 2 where supervision is iOS-attributed only, covered by section-scoped guard — Task 2 will NOT add any Android-side supervision term)
    - `grep -c '../android-lifecycle/02-provisioning-methods.md' docs/reference/android-capability-matrix.md` >= **1**
    - `grep -c '../android-lifecycle/03-android-version-matrix.md' docs/reference/android-capability-matrix.md` >= **1**
    - File length >= 80 lines at this point (Task 2 will add Cross-Platform Equivalences, Key Gaps, See Also, deferral footers, Version History — growing to 150+ lines)
  </acceptance_criteria>
  <done>Primary matrix (frontmatter + H1 + lead paragraph + 5 domain H2s with 5-column mode tables) authored, zero SafetyNet tokens, zero supervision tokens, AOSP cells link to stub, canonical-source cells link to lifecycle docs (not duplicate grids), 60-day cycle math correct.</done>
</task>

<task type="auto">
  <name>Task 2: Append Cross-Platform Equivalences + Key Gaps + See Also + Deferral footers + Version History</name>
  <files>docs/reference/android-capability-matrix.md</files>
  <read_first>
    - `docs/reference/android-capability-matrix.md` (just-authored file from Task 1 — append to end)
    - `docs/reference/ios-capability-matrix.md` lines 80-107 (Key Gaps Summary shape — 8 numbered `**Bold title** — explanation` bullets; See Also shape; Version History shape)
    - `docs/reference/macos-capability-matrix.md` lines 80-101 (Key Gaps Summary shape — 7 numbered bullets; confirms iOS is 8 / macOS is 7)
    - `docs/android-lifecycle/00-enrollment-overview.md` lines 51-53 (Row 1 body template — EXACT text to paraphrase in the iOS Supervision↔Android Fully Managed pair body)
    - `docs/admin-setup-android/03-fully-managed-cobo.md` line 35 (COBO cross-platform blockquote — Row 1 body text reference)
    - `.planning/phases/42-integration-milestone-audit/42-CONTEXT.md` D-11 (exact 3 pairs), D-12 (supervision iOS-attribution rule + HTML comment guard), D-14 (3 deferral footer blocks — exact wording), D-15 (section ordering)
    - `.planning/phases/42-integration-milestone-audit/42-PATTERNS.md` lines 99-164 (exact target wording for Cross-Platform Equivalences and Deferral footers)
    - `.planning/phases/42-integration-milestone-audit/42-RESEARCH.md` section "Cross-Platform Equivalences section — exact pair framing"
  </read_first>
  <action>
    Append to the end of the file (do NOT mutate the Task 1 content above). Sections in this exact order:

    **1. `## Cross-Platform Equivalences`** (anchor `#cross-platform-equivalences` — implicit from heading)

    Immediately after the heading, insert the HTML comment guard EXACTLY (D-12 locked text):
    ```
    <!-- AEAUDIT-04: "supervision" in this section MUST appear only as an iOS-attributed
         reference. Android management states are "Fully Managed" / "Work Profile" /
         "Dedicated" / "ZTE" — never "supervised". Each paired row MUST attribute the
         platform in the column header (e.g., "iOS Supervision" not "Supervision"). -->
    ```

    Then 1-2 sentences of lead prose ("This section maps three Apple↔Android capability pairs called out in ROADMAP SC#1. It is NOT a 4-platform comparison — see the deferral footer.").

    Then a table with EXACTLY 3 paired rows. Recommended shape (Claude's discretion; per D-11 table form satisfies SC#1 columns-plural):
    ```
    | iOS / Apple | Android |
    |-------------|---------|
    | **iOS Supervision (ADE-enrolled)** | **Android Fully Managed (COBO / DPC owner)** |
    | [Row 1 body — left side: iOS supervision gates ~60 restriction settings, permanent per-device state] | [Row 1 body — right side: Android Fully Managed is an ownership-mode designation set at provisioning time; mapping is partial. "Supervision" is not an Android management term] |
    | **Apple Automated Device Enrollment (ADE) via ABM** | **Google Zero-Touch Enrollment via ZT portal** |
    | [Row 2 body — hardware-vendor-chain enrollment via ABM token; factory pre-provisioning; reseller relationship not required] | [Row 2 body — Zero-Touch portal binding, reseller relationship prerequisite, tri-portal admin surface] |
    | **iOS Account-Driven User Enrollment (BYOD iOS 15+)** | **Android Work Profile (BYOD, Company Portal enrollment)** |
    | [Row 3 body — privacy-preserving BYOD with corporate-identity separation; Managed Apple ID context] | [Row 3 body — privacy-preserving BYOD via work container; selective wipe of work data only; post-AMAPI Microsoft Intune app is DPC] |
    ```

    Claude's discretion on the body cell prose, BUT:
    - Every paired-row header (bold text line) MUST contain the platform name explicitly on BOTH sides ("iOS Supervision" not "Supervision"; "Android Fully Managed" not "Fully Managed").
    - The words `supervision`/`supervised`/`supervisory` appear ONLY in the left (iOS) column (header or body prose). NEVER in the right (Android) column.
    - Row 1 right-side body MUST include the exact phrase "'Supervision' is not an Android management term" or equivalent (from 00-enrollment-overview.md:53 template).
    - EXACTLY 3 paired rows. No 4th pair. No 4-platform variant.

    **2. `## Key Gaps Summary`** (anchor `#key-gaps-summary`)

    - 1 sentence lead: "The most significant Android capability gaps relative to Windows/macOS/iOS are:"
    - Numbered list (Claude's discretion on count: 7 or 8, parity with macOS or iOS; document choice inline). Each item: `N. **Bold title** — explanation sentence.`
    - Recommended 8 items (iOS parity): (1) **No CLI diagnostic hook** — Android has no equivalent of mdmdiagnosticstool; (2) **Mode-locked restriction scope** — deep restriction settings require COBO/DO; (3) **Tri-portal admin surface** — unique to Android (Intune + MGP + ZTE); (4) **Play Integrity replaces device-attestation predecessor** — January 2025 transition; (5) **AMAPI-migrated BYOD DPC** — Microsoft Intune app post-April 2025; (6) **No DDM equivalent** — Android uses Play-integrated policy channel; (7) **AOSP coverage is stub-only in v1.4** — OEM matrix evolving; (8) **Dedicated = COSU + MHS** — kiosk pattern distinct from Apple single-app mode.
    - Do NOT use the word "supervision" in this section (guarded by repo-wide D-28 semantic-zero check — no allow-list entry exists for this file).

    **3. `## See Also`** (anchor `#see-also`)

    5 relative-path bullets (exact anchor count; Claude's discretion on exact phrasing):
    - `[iOS Capability Matrix](ios-capability-matrix.md)` — sibling iOS trilateral matrix
    - `[macOS Capability Matrix](macos-capability-matrix.md)` — sibling macOS bilateral matrix
    - `[Android Provisioning Lifecycle](../android-lifecycle/00-enrollment-overview.md)` — enrollment overview narrative
    - `[Android Provisioning Methods](../android-lifecycle/02-provisioning-methods.md)` — canonical method grid
    - `[Android Version Matrix](../android-lifecycle/03-android-version-matrix.md)` — canonical version gates

    **4. Deferral footers** (per D-14 — each a separate `### ` sub-heading under an H2-separator `---`):

    Insert `---` then the 3 deferral sub-sections EXACTLY (wording from D-14 / 42-CONTEXT.md specifics block):

    ```
    ---

    ### Deferred: Knox Mobile Enrollment row

    Knox Mobile Enrollment (KME) is a Samsung-specific zero-touch enrollment path
    mutually exclusive with Google Zero-Touch on Samsung hardware. KME coverage —
    including a provisioning-method row and capability mapping — is deferred to v1.4.1
    per PROJECT.md Key Decisions. See [Knox Mobile Enrollment deferral note](../android-lifecycle/02-provisioning-methods.md#knox-mobile-enrollment).

    ### Deferred: Full AOSP capability mapping

    AOSP (Android Open Source Project) devices — RealWear, Zebra, Pico, HTC VIVE
    Focus, Meta Quest — appear in this matrix as a single stub-reference row.
    Per-OEM capability mapping and feature-by-feature expansion are deferred to
    v1.4.1. See [AOSP stub](../admin-setup-android/06-aosp-stub.md).

    ### Deferred: 4-platform unified capability comparison

    This matrix is Android-centric with a bounded 3-row Cross-Platform Equivalences
    section. A unified Windows|macOS|iOS|Android 4-platform feature comparison doc
    is deferred to v1.5 (AECOMPARE-01). The paired rows in this matrix are NOT a
    4-platform comparison — they are mode-level feature parity assertions between
    Apple and Android, constrained to the 3 SC#1-named pairs.
    ```

    **5. `## Version History`** (per D-15 step 13 — iOS-matrix-style explicit H2; do NOT use the macOS bare-table form):
    ```
    ---

    ## Version History

    | Date | Change | Author |
    |------|--------|--------|
    | 2026-04-24 | Initial version — Phase 42: Android Enterprise capability matrix (5 domains, 5 mode rows, Cross-Platform Equivalences H2 with 3 paired rows, Key Gaps Summary, deferral footers) | -- |
    ```

    Commit message: `docs(42-02): add android-capability-matrix.md Cross-Platform Equivalences + Key Gaps + deferral footers (part 2/2)`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/reference/android-capability-matrix.md','utf8');const ordered=['## Enrollment','## Configuration','## App Deployment','## Compliance','## Software Updates','## Cross-Platform Equivalences','## Key Gaps Summary','## See Also','## Version History'];let last=-1;for(const h of ordered){const idx=c.indexOf('\n'+h+'\n');if(idx<0){console.error('FAIL: missing heading '+h);process.exit(1)};if(idx<=last){console.error('FAIL: heading out of order: '+h);process.exit(1)};last=idx};const guard=c.includes('AEAUDIT-04: \"supervision\" in this section MUST appear only as an iOS-attributed');if(!guard){console.error('FAIL: HTML comment guard missing or wrong wording');process.exit(1)};const xpe=c.split('## Cross-Platform Equivalences')[1].split('## Key Gaps Summary')[0];const pairs=(xpe.match(/\\*\\*iOS (Supervision|Automated Device Enrollment|Account-Driven User Enrollment)/g)||[]).length;if(pairs!==3){console.error('FAIL: expected 3 iOS-side pair headers, got '+pairs);process.exit(1)};const androidPairs=(xpe.match(/\\*\\*Android (Fully Managed|Work Profile)|\\*\\*Google Zero-Touch/g)||[]).length;if(androidPairs!==3){console.error('FAIL: expected 3 Android-side pair headers, got '+androidPairs);process.exit(1)};const xpePrimary=c.split('## Cross-Platform Equivalences')[1].split('## Key Gaps Summary')[0];const otherSections=c.split('## Cross-Platform Equivalences')[0]+(c.split('## Key Gaps Summary')[1]||'');const outside_supervision=(otherSections.match(/\\bsupervis(ion|ed|ory)\\b/gi)||[]).length;if(outside_supervision!==0){console.error('FAIL: '+outside_supervision+' supervision term(s) outside Cross-Platform Equivalences section — D-12 violation');process.exit(1)};const kme=c.includes('Deferred: Knox Mobile Enrollment row');const aosp=c.includes('Deferred: Full AOSP capability mapping');const fourp=c.includes('Deferred: 4-platform unified capability comparison');if(!kme||!aosp||!fourp){console.error('FAIL: missing deferral footer(s)');process.exit(1)};const sn=(c.match(/SafetyNet/g)||[]).length;if(sn!==0){console.error('FAIL: '+sn+' SafetyNet tokens — must be 0');process.exit(1)};console.log('PASS: section ordering valid, guard present, 3 pairs, zero outside-section supervision, all 3 deferral footers, zero SafetyNet')"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 H2 headings present in locked order (`## Enrollment` → `## Configuration` → `## App Deployment` → `## Compliance` → `## Software Updates` → `## Cross-Platform Equivalences` → `## Key Gaps Summary` → `## See Also` → `## Version History`)
    - `grep -c 'AEAUDIT-04: "supervision" in this section MUST appear only as an iOS-attributed' docs/reference/android-capability-matrix.md` = **1** (HTML comment guard)
    - `grep -c '^## Cross-Platform Equivalences$' docs/reference/android-capability-matrix.md` = **1**
    - Between `## Cross-Platform Equivalences` and `## Key Gaps Summary`, exactly 3 iOS-attributed pair headers: `grep -cE '\*\*iOS (Supervision|Automated Device Enrollment|Account-Driven User Enrollment)' docs/reference/android-capability-matrix.md` = **3**
    - Between `## Cross-Platform Equivalences` and `## Key Gaps Summary`, exactly 3 Android-attributed pair headers (counting Android Fully Managed + Google Zero-Touch + Android Work Profile)
    - Supervision tokens OUTSIDE `## Cross-Platform Equivalences` section: **0** (D-12 section-scoped rule)
    - `grep -c '### Deferred: Knox Mobile Enrollment row' docs/reference/android-capability-matrix.md` = **1**
    - `grep -c '### Deferred: Full AOSP capability mapping' docs/reference/android-capability-matrix.md` = **1**
    - `grep -c '### Deferred: 4-platform unified capability comparison' docs/reference/android-capability-matrix.md` = **1**
    - `grep -c 'SafetyNet' docs/reference/android-capability-matrix.md` = **0** (AEAUDIT-04 C1 precondition — no allow-list entry for this file)
    - `grep -c '^## Version History$' docs/reference/android-capability-matrix.md` = **1** (iOS-matrix-style explicit H2, not bare-table macOS form)
    - Deferral footer block present and is explicitly labeled NOT a 4-platform comparison (preempts AECOMPARE-01 v1.5 encroachment per D-14): `grep -c 'NOT a' docs/reference/android-capability-matrix.md` >= **1** (within the 4-platform deferral footer body)
    - Total file length >= 140 lines (primary matrix ~80 + appended sections ~60)
  </acceptance_criteria>
  <done>Matrix fully authored: 5 domain H2s + Cross-Platform Equivalences (with HTML guard + 3 platform-attributed pairs + supervision-iOS-only) + Key Gaps Summary (no supervision) + See Also (5 siblings) + 3 Deferral footers + Version History H2. Zero SafetyNet. Supervision confined to Cross-Platform Equivalences section only.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `node -e` checks in both tasks PASS
- Manual spot-check: read top-to-bottom; verify Cross-Platform Equivalences table structurally maps Apple→Android with platform name in every header cell
- `grep -n 'supervis' docs/reference/android-capability-matrix.md` — every match line number must be inside the Cross-Platform Equivalences section bounds (between `## Cross-Platform Equivalences` and `## Key Gaps Summary`)
- `grep -c 'SafetyNet' docs/reference/android-capability-matrix.md` = 0 — no allow-list entry for this file; must use successor/Play Integrity phrasing
</verification>

<success_criteria>
- `docs/reference/android-capability-matrix.md` exists with frontmatter, H1, lead paragraph, 5 domain H2s, Cross-Platform Equivalences (with HTML guard + 3 paired rows), Key Gaps Summary, See Also, 3 Deferral footers, Version History H2 + table
- `last_verified` and `review_by` frontmatter obey 60-day Android cycle (D-16, Phase 34 D-14)
- AOSP cells are stub-reference only (D-10); canonical-source cells link out (D-13 Anti-Pattern 1 guard)
- `supervision` appears only in Cross-Platform Equivalences section's iOS side (D-12)
- Zero `SafetyNet` tokens (C1 precondition — no allow-list entry for this file)
- `🔒` glyph used inline for mode-restricted capabilities (D-18)
- File ready for C5 freshness scope inclusion (D-31 — activates after this file lands)
</success_criteria>

<output>
After completion, create `.planning/phases/42-integration-milestone-audit/42-02-SUMMARY.md` recording:
- H2 count + ordering check (9 headings, in D-15 order)
- Cross-Platform Equivalences section supervision audit: `grep -n 'supervis' docs/reference/android-capability-matrix.md` output (every line must be inside XPE section)
- `grep -c 'SafetyNet' docs/reference/android-capability-matrix.md` = 0 confirmed
- File length and final token-count signals
</output>
