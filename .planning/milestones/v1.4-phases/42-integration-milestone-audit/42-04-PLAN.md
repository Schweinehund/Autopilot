---
phase: 42
plan: 04
type: execute
wave: 1
depends_on: [42-01]
files_modified:
  - docs/_glossary-macos.md
autonomous: true
requirements: [AEAUDIT-03]
tags: [glossary, banner-extend, surgical-edit, cross-reference]

must_haves:
  truths:
    - "docs/_glossary-macos.md line 10 contains BOTH the original Windows Autopilot Glossary see-also AND a new Android Enterprise Provisioning Glossary see-also in a single blockquote continuation line"
    - "Line 9 (first banner line beginning `> **Platform coverage:**`) is byte-for-byte unchanged"
    - "Line 11 onward (all term content, category headings, Version History) is byte-for-byte unchanged (except the optional Version History append at the bottom)"
    - "Total blockquote structure remains 2 lines (line 9 + line 10) — no new blockquote line inserted"
  artifacts:
    - path: "docs/_glossary-macos.md"
      provides: "Reciprocal see-also cross-reference to _glossary-android.md on line 10"
      contains_regex: "For Android Enterprise terminology, see the \\[Android Enterprise Provisioning Glossary\\]\\(_glossary-android\\.md\\)\\."
  key_links:
    - from: "docs/_glossary-macos.md line 10 continuation blockquote"
      to: "docs/_glossary-android.md"
      via: "relative-path markdown link — single appended sentence on existing continuation line"
      pattern: "\\[Android Enterprise Provisioning Glossary\\]\\(_glossary-android\\.md\\)"
---

<objective>
Perform a SURGICAL SINGLE-LINE edit to `docs/_glossary-macos.md` per AEAUDIT-03 / ROADMAP SC#3 / CONTEXT D-23. Extend the line-10 continuation blockquote to include a second see-also pointing at `_glossary-android.md`.

Current line 10 (VERIFIED):
```
> For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md).
```

Target line 10:
```
> For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md). For Android Enterprise terminology, see the [Android Enterprise Provisioning Glossary](_glossary-android.md).
```

This matches the multi-sibling continuation-banner pattern at `docs/_glossary-android.md:11` (VERIFIED — one blockquote continuation line points at BOTH Windows and Apple glossaries). D-23 interprets AEAUDIT-03's "1-line" as "one added sentence on the existing continuation line" — preserves 2-line blockquote structure, no new line inserted, total file line count unchanged (except optional Version History append).

Purpose: Close the Android→macOS reciprocal link so admins reading the macOS glossary can reach the Android glossary in a single hop. Previously, only Android→macOS existed (via `_glossary-android.md:11`); the inverse was missing.

Output: Modified `docs/_glossary-macos.md` with line 10 extended and an optional Version History row appended. Every other line byte-for-byte identical to pre-edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-integration-milestone-audit/42-CONTEXT.md
@.planning/phases/42-integration-milestone-audit/42-RESEARCH.md
@.planning/phases/42-integration-milestone-audit/42-PATTERNS.md
@docs/_glossary-macos.md
@docs/_glossary-android.md

<interfaces>
<!-- Exact structure of the target file and the structural analog. -->

From docs/_glossary-macos.md (TARGET — VERIFIED current state of lines 1-11):
```
---
last_verified: 2026-04-17
review_by: 2026-07-16
applies_to: both
audience: all
platform: all
---

> **Platform coverage:** This glossary covers Apple-platform provisioning and management terminology for macOS and iOS/iPadOS.
> For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md).

# Apple Provisioning Glossary
```

From docs/_glossary-android.md:11 (STRUCTURAL ANALOG — VERIFIED — the multi-sibling continuation-banner pattern that D-23 matches):
```
> **Platform coverage:** This glossary covers Android Enterprise provisioning and management terminology for Intune-managed Android devices. For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md). For Apple-platform terminology (macOS, iOS/iPadOS), see the [Apple Provisioning Glossary](_glossary-macos.md).
```
This is a SINGLE-LINE blockquote containing both the platform-coverage declaration AND two sibling see-alsos. The `_glossary-macos.md` shape after this edit will be the 2-line variant of the same pattern: line 9 has the platform declaration, line 10 has both see-alsos.

From docs/_glossary.md:9-11 (Windows glossary — 3-line continuation banner, VERIFIED — confirms multi-sibling see-also is precedent-aligned, not an exception).

Must-not-modify lines per D-24:
- Lines 1-7 frontmatter (exception: `last_verified` may bump only)
- Line 9 (first banner line — `> **Platform coverage:** This glossary covers Apple-platform...`)
- Line 11 onward (ALL term content, category headings, Version History) — exception: Version History row may be appended as provenance
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend line 10 continuation blockquote with Android Enterprise Glossary see-also + optional Version History row</name>
  <files>docs/_glossary-macos.md</files>
  <read_first>
    - `docs/_glossary-macos.md` entire file — confirm current line 10 content (anchor: second `>` line starting with `> For Windows Autopilot terminology`), locate Version History table at bottom (if present)
    - `docs/_glossary-android.md` lines 1-15 (confirm file exists and is the link target; confirm line 11 multi-sibling banner pattern is the analog D-23 mirrors)
    - `docs/_glossary.md` lines 1-15 (3-line continuation banner confirms the pattern scales to multi-sibling)
    - `.planning/phases/42-integration-milestone-audit/42-CONTEXT.md` decisions D-23 (exact target wording for line 10) and D-24 (must-not-modify lines)
    - `.planning/phases/42-integration-milestone-audit/42-PATTERNS.md` section "`docs/_glossary-macos.md`" (lines 260-289 — confirms analog pattern + target line text)
  </read_first>
  <action>
    Single-file surgical edit. Locate line 10 by content anchor (`> For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md).`) — NOT by raw line number. Replace that line EXACTLY with:

    ```
    > For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md). For Android Enterprise terminology, see the [Android Enterprise Provisioning Glossary](_glossary-android.md).
    ```

    Rules:
    - The replacement line BEGINS with `> ` (blockquote marker + space) — the same marker that starts the current line 10.
    - The replacement line has EXACTLY 2 complete sentences on it. The first sentence is byte-for-byte identical to the current line 10 text (preserves the original Windows see-also). The second sentence is the newly appended Android see-also.
    - Do NOT split the replacement across multiple lines. Line count must be unchanged.
    - Do NOT change any other `>` blockquote line (line 9 is UNTOUCHED).

    **Optional edits** (provenance only, D-24 permits):
    1. Bump frontmatter `last_verified: 2026-04-17` → `last_verified: 2026-04-24` (current execution date) if the file has a Version History table. Do NOT touch `review_by` (this file uses 90-day Apple-platform cycle — not in C5 Android-60-day scope).
    2. If the file has a Version History table at the bottom, append a new TOP row (below the `|---|` divider) EXACTLY:
       ```
       | 2026-04-24 | Phase 42: added Android Enterprise Provisioning Glossary see-also to continuation banner (AEAUDIT-03) | -- |
       ```
       (Use current execution date if different from 2026-04-24.)
       If the file has NO Version History table, skip this sub-edit. Do NOT create a Version History table from scratch.

    **Must-NOT modify** (D-24):
    - Lines 1-7 frontmatter except the optional `last_verified` bump above
    - Line 9 first banner line — UNTOUCHED byte-for-byte
    - Line 11 onward — all term content, category headings (`## Alphabetical Index`, `### ...` term entries, etc.) UNTOUCHED byte-for-byte
    - Any existing Version History row — UNTOUCHED (only append one new top row if table exists)

    Commit message: `docs(42-04): add Android glossary see-also to docs/_glossary-macos.md line 10 banner (AEAUDIT-03)`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/_glossary-macos.md','utf8');const lines=c.split('\\n');if(lines[8]!=='> **Platform coverage:** This glossary covers Apple-platform provisioning and management terminology for macOS and iOS/iPadOS.'){console.error('FAIL: line 9 modified — D-24 violation');process.exit(1)};const line10=lines[9];const expected='> For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md). For Android Enterprise terminology, see the [Android Enterprise Provisioning Glossary](_glossary-android.md).';if(line10!==expected){console.error('FAIL: line 10 does not match target');console.error('EXPECTED: '+expected);console.error('GOT:      '+line10);process.exit(1)};if(lines[10]!==''){console.error('FAIL: line 11 must remain blank — D-24 says line 11 onward unchanged; got: '+JSON.stringify(lines[10]));process.exit(1)};const bqCount=lines.slice(0,20).filter(l=>l.startsWith('> ')).length;if(bqCount!==2){console.error('FAIL: blockquote must remain 2-line, got '+bqCount+' blockquote lines in top 20');process.exit(1)};const androidMention=(c.match(/_glossary-android\\.md/g)||[]).length;if(androidMention<1){console.error('FAIL: _glossary-android.md link missing');process.exit(1)};console.log('PASS: line 9 unchanged, line 10 is 2-sentence extended banner with Android link, blockquote is 2-line, line 11 is blank')"</automated>
  </verify>
  <acceptance_criteria>
    - Line 10 of `docs/_glossary-macos.md` equals EXACTLY: `> For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md). For Android Enterprise terminology, see the [Android Enterprise Provisioning Glossary](_glossary-android.md).`
    - Line 9 of `docs/_glossary-macos.md` equals EXACTLY: `> **Platform coverage:** This glossary covers Apple-platform provisioning and management terminology for macOS and iOS/iPadOS.` (unchanged)
    - Line 11 is blank (unchanged)
    - `grep -c '_glossary-android.md' docs/_glossary-macos.md` >= **1** (at least one mention — line 10 link; potentially 2 if Version History row added)
    - `grep -cE '^> ' docs/_glossary-macos.md | head -15` — blockquote lines in top 15 lines = **2** (unchanged from pre-edit)
    - `git diff docs/_glossary-macos.md` shows: (a) exactly 1 line replaced at line 10, (b) optional 1 line changed in frontmatter (`last_verified`), (c) optional 1 line appended in Version History. NO other changes.
    - Total line count delta from pre-edit: **0** (line 10 replacement in-place) OR **+1** (if Version History row appended)
    - All term content (`## Alphabetical Index`, `### ...` term headings, definitions) byte-for-byte identical pre- vs post-edit
  </acceptance_criteria>
  <done>Line 10 extended to 2-sentence banner with Android glossary link. Line 9 and line 11+ byte-for-byte unchanged (exception: optional Version History row + `last_verified` bump). Commit isolated to docs/_glossary-macos.md.</done>
</task>

</tasks>

<verification>
- `node -e` check in `<verify>` PASSES (line 9 exact, line 10 exact, line 11 blank, 2-line blockquote preserved)
- `git diff docs/_glossary-macos.md` shows <= 3 line modifications
- `diff <(sed -n '11,$p' pre-edit-backup) <(sed -n '11,$p' docs/_glossary-macos.md)` shows at most 1-2 line changes (optional Version History top row) or 0 changes
</verification>

<success_criteria>
- AEAUDIT-03 content requirement satisfied: line-10 banner points at `_glossary-android.md`
- D-23 pattern match: line 10 is now a 2-sentence continuation with both Windows and Android see-alsos, mirroring `_glossary-android.md:11`'s multi-sibling banner
- D-24 must-not-modify guarantee: every line except line 10 (+ optional provenance rows) is byte-for-byte identical pre- vs post-edit
- Commit isolated to `docs/_glossary-macos.md`
</success_criteria>

<output>
After completion, create `.planning/phases/42-integration-milestone-audit/42-04-SUMMARY.md` recording:
- `git diff docs/_glossary-macos.md` full diff output
- Confirmation: line 9 and line 11 pre-edit vs post-edit `sha256sum` identical
- Line count delta (expected: 0 or +1)
</output>
