---
phase: 34-android-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/android-lifecycle/00-enrollment-overview.md
autonomous: true
requirements:
  - AEBASE-02
user_setup: []

must_haves:
  truths:
    - "An admin familiar with iOS can open `docs/android-lifecycle/00-enrollment-overview.md` and place any Android scenario on the ownership axis (corporate vs personal) and the management-scope axis (fully managed, work profile, dedicated, AOSP)"
    - "The admin finds a 5-column comparison table listing the 5 Android enrollment modes (ZTE, COBO, BYOD Work Profile, Dedicated, AOSP) and can scan across to identify Ownership Model, Management Scope, Provisioning Surface, and Use Case"
    - "A dedicated 'For Admins Familiar with iOS' narrative subsection explains that Android Fully Managed is the closest analog to iOS Supervision, but the mapping is partial — with the exact D-03 framing"
    - "AOSP is called out as structurally different (out-of-GMS, no MGP binding, QR-only, OEM-gated, forward-ref to Phase 39 stub)"
    - "The document is 800-1200 words (excluding frontmatter) and contains zero raw uses of 'supervision' as an Android management term in comparison-table rows"
  artifacts:
    - path: "docs/android-lifecycle/00-enrollment-overview.md"
      provides: "5-mode comparison table, Two Axes narrative, iOS-supervision-analog bridge subsection, per-mode H3 narrative sections, See Also cross-refs"
      contains:
        - "| Mode | Ownership Model | Management Scope | Provisioning Surface | Appropriate Use Case |"
        - "## Two Axes of Android Enterprise"
        - "## For Admins Familiar with iOS"
        - "### Fully Managed (COBO)"
        - "### BYOD Work Profile"
        - "### Dedicated (COSU)"
        - "### Zero-Touch Enrollment (ZTE)"
        - "### AOSP"
        - "## How to Use This Guide"
        - "## See Also"
  key_links:
    - from: "docs/android-lifecycle/00-enrollment-overview.md"
      to: "docs/_glossary-android.md#fully-managed"
      via: "markdown link inside the For Admins Familiar with iOS subsection"
      pattern: "_glossary-android\\.md#fully-managed"
    - from: "docs/android-lifecycle/00-enrollment-overview.md"
      to: "docs/android-lifecycle/02-provisioning-methods.md"
      via: "Provisioning Surface column cells and See Also section"
      pattern: "02-provisioning-methods\\.md"
    - from: "docs/android-lifecycle/00-enrollment-overview.md"
      to: "docs/android-lifecycle/03-android-version-matrix.md"
      via: "See Also section"
      pattern: "03-android-version-matrix\\.md"
---

<objective>
Create `docs/android-lifecycle/00-enrollment-overview.md` — the foundational document that explains the two orthogonal axes (ownership × management scope) of Android Enterprise enrollment and provides the iOS supervision analog bridge for admins coming from iOS Phase 26 content (AEBASE-02 / ROADMAP Success Criterion 2).

Purpose: This is THE routing document for any admin deciding which Android enrollment mode to use. It also is the iOS-to-Android bridge — admins who understand iOS supervision need a clean analogy without letting "supervision" leak into Android terminology (AEAUDIT-04).

Output: One new markdown file at `docs/android-lifecycle/00-enrollment-overview.md` with frontmatter → version-gate blockquote → "How to Use This Guide" audience-routing → 5-col mode comparison table → Two Axes narrative → For Admins Familiar with iOS subsection → per-mode H3 narrative sections → See Also.

This plan MUST honor 7 locked decisions: D-01 (5-col table columns), D-02 (Two Axes narrative placement), D-03 (exact iOS-analog framing), D-04 (Android-native Management Scope values), D-05 (AOSP out-of-GMS annotation), D-06 (Provisioning Surface column has 1-2 methods + link, NOT the full matrix), D-07 (800-1200 words).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-android-foundation/34-CONTEXT.md
@.planning/phases/34-android-foundation/34-RESEARCH.md
@.planning/phases/34-android-foundation/34-PATTERNS.md
@.planning/phases/34-android-foundation/34-VALIDATION.md
@.planning/research/FEATURES.md
@docs/ios-lifecycle/00-enrollment-overview.md

<interfaces>
<!-- Canonical anchors this file MUST publish (Phase 35-42 cross-reference load-bearing targets). -->
<!-- Source: 34-RESEARCH.md Pitfall 7 lines 479-486, 34-PATTERNS.md Shared Patterns → Anchor stability registry. -->

Anchors produced by H2/H3 headings in this file (GitHub kebab-case):
  #how-to-use-this-guide
  #enrollment-mode-comparison   (from the H2 above the comparison table)
  #two-axes-of-android-enterprise
  #for-admins-familiar-with-ios
  #enrollment-mode-details      (from the H2 above the per-mode H3s)
  #fully-managed-cobo
  #byod-work-profile
  #dedicated-cosu
  #zero-touch-enrollment-zte
  #aosp
  #see-also

Frontmatter:
  last_verified: 2026-04-21
  review_by: 2026-06-20   # = last_verified + 60 days per D-14
  applies_to: all
  audience: all
  platform: Android      # Android-specific doc — not `all` like the glossary

Cross-ref targets this file links to:
  ../_glossary-android.md (for term anchors: #fully-managed, #work-profile, #dedicated, #cobo, #byod, #aosp, #zero-touch-enrollment)
  ../_glossary-macos.md#supervision (iOS supervision analog target)
  02-provisioning-methods.md (full method matrix — NOT yet created at Wave 1 authoring time; file written as Plan 03 in Wave 2 — link target resolves at merge time)
  03-android-version-matrix.md (See Also — also Wave 2)

NOTE: This plan is Wave 1. The link targets `02-provisioning-methods.md` and `03-android-version-matrix.md` will not exist until Wave 2 (Plans 03/04) lands. Broken-link markers are acceptable transient state within Wave 1→Wave 2; verification happens at phase-gate after all Wave 2 files land.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold file with frontmatter, version-gate blockquote, H1 title, and "How to Use This Guide" audience-routing block</name>
  <files>docs/android-lifecycle/00-enrollment-overview.md</files>

  <read_first>
    - docs/ios-lifecycle/00-enrollment-overview.md (lines 1-19 — the exact frontmatter + version gate + "How to Use This Guide" shape to mirror per D-01)
    - .planning/phases/34-android-foundation/34-CONTEXT.md (D-01 through D-07 — enrollment overview decisions)
    - .planning/phases/34-android-foundation/34-PATTERNS.md (section "`docs/android-lifecycle/00-enrollment-overview.md`" lines 110-201 — frontmatter/blockquote/routing patterns with verbatim code excerpts)
    - .planning/phases/34-android-foundation/34-RESEARCH.md (Architectural Responsibility Map lines 104-117 — this file serves Admin primary, L1/L2 secondary via audience routing)
  </read_first>

  <action>
    Create `docs/android-lifecycle/00-enrollment-overview.md` with the following exact structure. Do NOT populate the comparison table, Two Axes, iOS-analog, or per-mode sections in this task — those come in Tasks 2, 3, 4.

    The `docs/android-lifecycle/` directory does not exist yet — creating the file auto-creates the directory. Use `mkdir -p docs/android-lifecycle` first if the filesystem requires the directory in advance.

    1. **Frontmatter block** (YAML between `---` fences):
       ```yaml
       ---
       last_verified: 2026-04-21
       review_by: 2026-06-20
       applies_to: all
       audience: all
       platform: Android
       ---
       ```
       `platform: Android` (literal, not `all`) because this is an Android-specific document per PATTERNS.md line 124. `review_by` = `last_verified + 60` per D-14.

    2. **H1 title**:
       ```markdown
       # Android Enterprise Enrollment Overview
       ```

    3. **Version-gate blockquote** (immediately after H1):
       ```markdown
       > **Platform gate:** This guide covers Android Enterprise enrollment modes in Microsoft Intune. For iOS/iPadOS enrollment, see [iOS/iPadOS Enrollment Overview](../ios-lifecycle/00-enrollment-overview.md). For macOS ADE, see [macOS ADE Lifecycle](../macos-lifecycle/00-ade-lifecycle.md). For Windows Autopilot, see [Autopilot Lifecycle Overview](../lifecycle/00-overview.md). For Android terminology, see the [Android Enterprise Provisioning Glossary](../_glossary-android.md).
       ```
       Critical: Do NOT link to `../common-issues.md`, `../quick-ref-l1.md`, `../quick-ref-l2.md`, or `../index.md` — all deferred per Pitfall 8.

    4. **H2 "How to Use This Guide"** with 3-bullet L1/L2/Admin routing per D-03 iOS precedent (PATTERNS.md lines 132-142):
       ```markdown
       ## How to Use This Guide

       This guide describes all five Android Enterprise enrollment modes and establishes the two orthogonal axes (ownership × management scope) that govern what each mode can manage.

       - **L1 Service Desk:** Use the [Enrollment Mode Comparison](#enrollment-mode-comparison) table to identify the enrollment mode, then read the per-mode H3 section for use-case confirmation.
       - **L2 Desktop Engineering:** Use the [Two Axes of Android Enterprise](#two-axes-of-android-enterprise) section to understand ownership × management-scope gating; follow the [For Admins Familiar with iOS](#for-admins-familiar-with-ios) section if your mental model is iOS supervision.
       - **Intune Admins:** Use the per-mode sections for selection guidance and links to admin setup guides (available from Phase 35 onward).
       ```
       Rationale: Three-bullet routing matches the iOS Phase 26 D-03 precedent lines 13-19. Audience: L1 triage → L2 engineering → Admin decision-making.

    5. **H2 scaffolds (empty bodies — populated in Tasks 2-4)**:
       ```markdown
       ## Enrollment Mode Comparison

       ## Two Axes of Android Enterprise

       ## For Admins Familiar with iOS

       ## Enrollment Mode Details

       ## See Also
       ```
       Order is load-bearing: comparison table FIRST (scannable), Two Axes narrative IMMEDIATELY AFTER per D-02, iOS-analog THEN so iOS-brained admins land correctly, per-mode detail deeper, See Also at bottom.

    At this point the file should have frontmatter + H1 + version-gate blockquote + H2 "How to Use This Guide" with 3-bullet routing + 5 empty H2 scaffolds. Word count will be well under 800 at this stage.
  </action>

  <verify>
    <automated>
      test -f docs/android-lifecycle/00-enrollment-overview.md &&
      grep -c "^platform: Android$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^review_by: 2026-06-20$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^# Android Enterprise Enrollment Overview$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^## How to Use This Guide$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^## Enrollment Mode Comparison$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^## Two Axes of Android Enterprise$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^## For Admins Familiar with iOS$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^## Enrollment Mode Details$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^## See Also$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      test $(grep -cE "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md|^\.\./index\.md" docs/android-lifecycle/00-enrollment-overview.md) -eq 0
    </automated>
  </verify>

  <acceptance_criteria>
    - `docs/android-lifecycle/00-enrollment-overview.md` exists and `docs/android-lifecycle/` directory created
    - Frontmatter has `platform: Android` (not `all`), `last_verified: 2026-04-21`, `review_by: 2026-06-20`, `audience: all`, `applies_to: all`
    - H1 is `# Android Enterprise Enrollment Overview`
    - Platform-gate blockquote immediately after H1 mentions iOS, macOS, Windows, Android glossary — and does NOT link to common-issues / quick-ref / index
    - `## How to Use This Guide` contains exactly 3 bullets with bold role labels (L1 Service Desk / L2 Desktop Engineering / Intune Admins) and embedded anchor links to `#enrollment-mode-comparison`, `#two-axes-of-android-enterprise`, `#for-admins-familiar-with-ios`
    - All 5 remaining H2 scaffolds present in the load-bearing order: Enrollment Mode Comparison → Two Axes of Android Enterprise → For Admins Familiar with iOS → Enrollment Mode Details → See Also
    - Grep check `grep -cE "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md" docs/android-lifecycle/00-enrollment-overview.md` returns 0 (no deferred-file links per Pitfall 8)
  </acceptance_criteria>

  <done>
    File scaffold committed. All 6 H2 sections in load-bearing order. Frontmatter + platform-gate + audience-routing block complete. Ready for Task 2 to fill the comparison table.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate the 5-column mode comparison table under `## Enrollment Mode Comparison` with 5 rows (ZTE, COBO, BYOD Work Profile, Dedicated, AOSP)</name>
  <files>docs/android-lifecycle/00-enrollment-overview.md</files>

  <read_first>
    - docs/ios-lifecycle/00-enrollment-overview.md (lines 29-36 — the iOS 5-col comparison table this mirrors structurally per D-01)
    - docs/android-lifecycle/00-enrollment-overview.md (scaffold from Task 1)
    - .planning/phases/34-android-foundation/34-CONTEXT.md (D-01 column names; D-04 Management Scope values; D-05 AOSP annotation; D-06 Provisioning Surface column rule — 1-2 methods + link)
    - .planning/phases/34-android-foundation/34-PATTERNS.md (lines 144-154 — the exact comparison table shape with Android-specific columns)
    - .planning/phases/34-android-foundation/34-RESEARCH.md (Pattern 2 lines 242-259 — canonical 5-col table with all 5 rows populated)
    - .planning/research/FEATURES.md (for mode-use-case content accuracy)
    - .planning/phases/34-android-foundation/34-VALIDATION.md (check 34-02-01 — exact table header string)
  </read_first>

  <action>
    Under the `## Enrollment Mode Comparison` H2, write a markdown table with this EXACT header row (must match grep check 34-02-01 verbatim, including spacing):

    ```markdown
    | Mode | Ownership Model | Management Scope | Provisioning Surface | Appropriate Use Case |
    |------|-----------------|------------------|----------------------|----------------------|
    ```

    Then add exactly 5 data rows in this order (one per Android mode per D-01). The Management Scope column uses Android-native terminology only per D-04 — do NOT use "Supervised" or "Unsupervised" here. The Provisioning Surface column lists 1-2 representative methods + link to `02-provisioning-methods.md` per D-06 (Anti-Pattern 1 guard — do NOT embed the full 4-method grid here).

    Row 1 — Zero-Touch Enrollment:
    ```markdown
    | Zero-Touch Enrollment (ZTE) | Corporate-owned | Fully managed (or Dedicated via portal) | Zero-Touch portal + Intune (see [02-provisioning-methods.md#zero-touch](02-provisioning-methods.md#zero-touch)) | Corporate-scale automated enrollment via reseller; devices enroll on first boot |
    ```

    Row 2 — Fully Managed (COBO):
    ```markdown
    | Fully Managed (COBO) | Corporate-owned | Fully managed | QR, NFC, afw#setup, Zero-Touch (see [02-provisioning-methods.md#cobo](02-provisioning-methods.md#cobo)) | Corporate single-user device; maximum admin control; no personal profile |
    ```

    Row 3 — BYOD Work Profile:
    ```markdown
    | BYOD Work Profile | Personally-owned | Work profile (containerized) | Company Portal (user-initiated) — see [02-provisioning-methods.md#byod-work-profile](02-provisioning-methods.md#byod-work-profile) | BYOD with data separation; user keeps personal side; admin sees only work-profile telemetry |
    ```

    Row 4 — Dedicated (COSU):
    ```markdown
    | Dedicated (COSU) | Corporate-owned | Dedicated (COSU — locked kiosk) | QR, NFC, afw#setup, Zero-Touch (see [02-provisioning-methods.md#dedicated-cosu](02-provisioning-methods.md#dedicated-cosu)) | Kiosk, digital signage, shared shift devices with Managed Home Screen |
    ```

    Row 5 — AOSP (per D-05 — MUST carry explicit "out-of-GMS" annotation in the Provisioning Surface column):
    ```markdown
    | AOSP | Corporate-owned (specialty) | AOSP (unmanaged — no GMS) | QR only, OEM-gated, out-of-GMS — see [02-provisioning-methods.md#aosp](02-provisioning-methods.md#aosp) | AR/VR headsets, rugged specialty devices without Google Mobile Services (see Phase 39 stub) |
    ```

    **Critical content gates per D-04 + AEAUDIT-04:**
    - Management Scope column values MUST be one of: "Fully managed", "Work profile (containerized)", "Dedicated (COSU — locked kiosk)", "AOSP (unmanaged — no GMS)". NEVER "Supervised" or "Unsupervised".
    - Zero raw uses of "supervision" or "supervised" anywhere in this table.
    - Per D-05, AOSP Provisioning Surface cell explicitly includes both "QR only" AND "out-of-GMS" (or equivalent "no GMS" wording).
  </action>

  <verify>
    <automated>
      grep -c "^| Mode | Ownership Model | Management Scope | Provisioning Surface | Appropriate Use Case |$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "Zero-Touch Enrollment (ZTE)" docs/android-lifecycle/00-enrollment-overview.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "Fully Managed (COBO)" docs/android-lifecycle/00-enrollment-overview.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "BYOD Work Profile" docs/android-lifecycle/00-enrollment-overview.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "Dedicated (COSU" docs/android-lifecycle/00-enrollment-overview.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "| AOSP |" docs/android-lifecycle/00-enrollment-overview.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -ciE "out-of-gms|no gms" docs/android-lifecycle/00-enrollment-overview.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      test $(grep -iE "^\| .* \| .*[Ss]upervised" docs/android-lifecycle/00-enrollment-overview.md | wc -l) -eq 0
    </automated>
  </verify>

  <acceptance_criteria>
    - Table header row `| Mode | Ownership Model | Management Scope | Provisioning Surface | Appropriate Use Case |` is present exactly once (validates check 34-02-01)
    - 5 data rows: one each for ZTE, COBO, BYOD Work Profile, Dedicated (COSU), AOSP
    - AOSP row's Provisioning Surface cell contains both "QR only" AND "out-of-GMS" (or "no GMS") — validates D-05
    - AOSP row's Provisioning Surface cell links to `02-provisioning-methods.md#aosp`
    - NO "Supervised" or "Unsupervised" appears as a value in any Management Scope cell (manual grep — no data-row match containing those words in the Management Scope column position)
    - Every Provisioning Surface cell contains a link to `02-provisioning-methods.md` with an appropriate anchor (`#zero-touch`, `#cobo`, `#byod-work-profile`, `#dedicated-cosu`, `#aosp`) per D-06
    - Management Scope values use only: "Fully managed", "Work profile (containerized)", "Dedicated (COSU — locked kiosk)", "AOSP (unmanaged — no GMS)" — no variations
  </acceptance_criteria>

  <done>
    5-row × 5-col enrollment mode comparison table in place under `## Enrollment Mode Comparison`. AOSP row flagged out-of-GMS per D-05. No supervision terminology in any cell. Provisioning Surface cells link to the yet-to-be-created Plan 03 matrix file (broken link transient-acceptable until Plan 03 lands in Wave 2).
  </done>
</task>

<task type="auto">
  <name>Task 3: Populate `## Two Axes of Android Enterprise` narrative + `## For Admins Familiar with iOS` bridge subsection with exact D-03 framing</name>
  <files>docs/android-lifecycle/00-enrollment-overview.md</files>

  <read_first>
    - docs/android-lifecycle/00-enrollment-overview.md (current state after Task 2)
    - docs/ios-lifecycle/00-enrollment-overview.md (lines 38-48 — iOS Supervision section structural analog)
    - .planning/phases/34-android-foundation/34-CONTEXT.md (D-02 — narrative placement; D-03 — EXACT framing locked)
    - .planning/phases/34-android-foundation/34-PATTERNS.md (lines 166-171 — Two Axes + For Admins Familiar with iOS adaptation guidance)
    - .planning/phases/34-android-foundation/34-VALIDATION.md (checks 34-02-02 and 34-02-03)
    - .planning/research/FEATURES.md (lines 10-19 — two-axis complexity narrative source)
  </read_first>

  <action>
    **Section A: Under `## Two Axes of Android Enterprise` H2**, write a narrative section (NOT a table — narrative-only per D-02) explaining the two orthogonal axes that define every Android Enterprise deployment scenario. Target ~200-300 words for this section.

    Content requirements:
    - Open with a 1-2 sentence framing: Android Enterprise enrollment is defined by two orthogonal axes.
    - **Axis 1: Ownership Model** — a bulleted paragraph defining the two values: "Corporate-owned" (org purchases / provides the device; full-device management is acceptable) and "Personally-owned" (user owns the device; org provisions a work profile container).
    - **Axis 2: Management Scope** — a bulleted paragraph defining the four values (Fully managed, Work profile, Dedicated (COSU), AOSP unmanaged) with 1-sentence operational meaning per value. Reference Android-native terminology only (D-04).
    - Close with a 1-sentence pointer: "The five enrollment modes in the table above are combinations of these two axes. AOSP is structurally outside both — see the AOSP mode detail below for why."

    Do NOT use "supervision" or "supervised" anywhere in this narrative. Do NOT create a sub-table — the narrative format is locked per D-02 (table column was rejected during adversarial review; narrative placement is load-bearing so that readers unfamiliar with the two-axis concept don't bounce off dense tabular content).

    **Section B: Under `## For Admins Familiar with iOS` H2**, write the iOS-supervision-analog bridge. D-03 LOCKS the core framing sentence — reproduce it EXACTLY (verbatim — do not paraphrase the locked sentence):

    Opening paragraph (exact wording required per D-03):
    ```markdown
    Android's Fully Managed mode is the closest analog to iOS Supervision, but the mapping is partial — iOS supervision is a permanent per-device state gating ~60 restriction settings; Android Fully Managed is an ownership-mode designation. See [_glossary-android.md#fully-managed](../_glossary-android.md#fully-managed) for disambiguation.
    ```

    After the locked opening paragraph, add 1-3 sentences of elaboration (author discretion within D-07 word budget):
    - Clarify that "Supervision" is NOT an Android term (link to `../_glossary-android.md#supervision` for the callout-only disambiguation entry).
    - Note the opposite-direction analog: Android BYOD Work Profile is closer in intent to iOS User Enrollment (BYOD privacy container) than to anything involving supervision.
    - Point reader to `../_glossary-macos.md#supervision` for the full iOS supervision definition if deeper context needed.

    **Content gates:**
    - The exact sentence beginning "Android's Fully Managed mode is the closest analog to iOS Supervision" appears VERBATIM.
    - Every use of "supervision" / "supervised" in this entire file MUST be in this subsection OR in explicit `_glossary-macos.md#supervision` cross-reference links — zero uses in the comparison table, zero uses in other narrative sections (per AEAUDIT-04 + D-03 + D-04).
  </action>

  <verify>
    <automated>
      grep -c "^## Two Axes of Android Enterprise$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^## For Admins Familiar with iOS$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "Android's Fully Managed mode is the closest analog to iOS Supervision" docs/android-lifecycle/00-enrollment-overview.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "_glossary-android.md#fully-managed" docs/android-lifecycle/00-enrollment-overview.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -ciE "\\bownership\\b" docs/android-lifecycle/00-enrollment-overview.md | awk '$1 >= 2 { exit 0 } { exit 1 }'
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "## Two Axes" docs/android-lifecycle/00-enrollment-overview.md` returns exactly 1 (validates check 34-02-02)
    - `grep -c "For Admins Familiar with iOS" docs/android-lifecycle/00-enrollment-overview.md` returns 1 (validates check 34-02-03)
    - The exact locked sentence "Android's Fully Managed mode is the closest analog to iOS Supervision, but the mapping is partial — iOS supervision is a permanent per-device state gating ~60 restriction settings; Android Fully Managed is an ownership-mode designation." appears verbatim
    - Link `[_glossary-android.md#fully-managed](../_glossary-android.md#fully-managed)` present in the For Admins Familiar with iOS section
    - Two Axes section introduces both Ownership Model and Management Scope axes with Android-native scope values only (no "Supervised"/"Unsupervised")
    - AEAUDIT-04 gate: every occurrence of "supervis" in the file is either in the `## For Admins Familiar with iOS` subsection OR in an explicit `_glossary-macos.md#supervision` link (validates check 34-02-05 manually)
    - No sub-tables under `## Two Axes` — narrative-only per D-02
  </acceptance_criteria>

  <done>
    Two Axes narrative establishes the ownership × management-scope framework. For Admins Familiar with iOS bridges the iOS supervision mental model to Android's Fully Managed with the exact D-03 locked framing. AEAUDIT-04 cleanliness preserved — all supervision usage contained to this subsection or cross-reference links.
  </done>
</task>

<task type="auto">
  <name>Task 4: Populate `## Enrollment Mode Details` with 5 per-mode H3 sections + `## See Also` cross-refs, then verify 800-1200 word count</name>
  <files>docs/android-lifecycle/00-enrollment-overview.md</files>

  <read_first>
    - docs/android-lifecycle/00-enrollment-overview.md (current state after Task 3)
    - docs/ios-lifecycle/00-enrollment-overview.md (lines 52-84 — per-path H3 sections + See Also shape)
    - .planning/phases/34-android-foundation/34-CONTEXT.md (D-05 — AOSP out-of-GMS narrative treatment; D-07 — 800-1200 word budget)
    - .planning/phases/34-android-foundation/34-PATTERNS.md (lines 172-194 — per-mode H3 adaptation guidance and See Also pattern)
    - .planning/research/FEATURES.md (mode-by-mode deep dives for content accuracy)
    - .planning/phases/34-android-foundation/34-VALIDATION.md (check 34-02-04 — word-count gate)
  </read_first>

  <action>
    **Section A: Under `## Enrollment Mode Details` H2**, create 5 H3 sub-sections — one per mode, in the same order as the comparison table rows (ZTE, COBO, BYOD Work Profile, Dedicated (COSU), AOSP). Each H3 should be ~80-120 words.

    Per-mode structure (each H3 follows this shape):
    - **H3 heading** — with parenthetical mode acronym on first appearance (e.g., `### Fully Managed (COBO)`).
    - **Opening sentence** — define the mode on its own Android merits.
    - **2-3 sentences** — key characteristics (ownership, management scope, provisioning methods available, typical use case, any Android version gating).
    - **Forward-reference** — link to the upcoming admin guide (Phase 35-39 targets are conceptual at Phase 34 time; use filename references like `../admin-setup-android/03-fully-managed-cobo.md` which don't exist yet — this is acceptable; these links resolve as subsequent phases ship).

    Required H3 sections:

    1. `### Zero-Touch Enrollment (ZTE)` — Corporate-scale, reseller-mediated provisioning via Zero-Touch portal. Devices enroll on first boot. Requires reseller relationship (Step 0). Link to `../_glossary-android.md#zero-touch-enrollment` and future `../admin-setup-android/02-zero-touch-portal.md`. Mention Samsung KME mutual-exclusion consideration (deferred to v1.4.1).

    2. `### Fully Managed (COBO)` — Android's highest-control mode. Entire device is managed, no personal profile. Supports all four provisioning methods (QR, NFC, afw#setup, Zero-Touch). Link to `../_glossary-android.md#cobo` and future `../admin-setup-android/03-fully-managed-cobo.md`. Mention the iOS-supervision-analog sentence is in the For Admins Familiar with iOS section above.

    3. `### BYOD Work Profile` — Personally-owned, work-profile-only. User installs Company Portal; work profile container is provisioned. Admin sees only work-profile telemetry. **Key accuracy points**: AMAPI migration April 2025 — custom OMA-URI profiles no longer supported; Microsoft Intune app is the primary management surface post-migration (not Company Portal). Link to `../_glossary-android.md#byod` and future `../admin-setup-android/04-byod-work-profile.md`.

    4. `### Dedicated (COSU)` — Kiosk/COSU. Device runs a locked set of apps via Managed Home Screen. MHS exit-PIN must sync between device-restrictions profile and MHS app config. Scenarios: single-app, multi-app, digital signage, Entra shared device mode. Link to `../_glossary-android.md#dedicated` and future `../admin-setup-android/05-dedicated-devices.md`.

    5. `### AOSP` — **Per D-05 MUST explicitly call out structural difference**: AOSP (Android Open Source Project) devices run Android WITHOUT Google Mobile Services. No Managed Google Play binding, no work profile. Only QR provisioning is supported, and only on OEMs that have implemented the AOSP management enrollment handshake (e.g., RealWear confirmed GA; full OEM matrix in Phase 39 stub). Forward-ref to `../admin-setup-android/06-aosp-stub.md` (Phase 39) and note that v1.4 coverage is a stub; full AOSP is v1.4.1.

    **Section B: Under `## See Also` H2**, create a bulleted list matching the iOS Phase 26 pattern (PATTERNS.md lines 186-193 adapted):

    ```markdown
    ## See Also

    - [Android Enterprise Provisioning Glossary](../_glossary-android.md) — for all 19 Android terms including [Fully Managed](../_glossary-android.md#fully-managed), [Work Profile](../_glossary-android.md#work-profile), and [Zero-Touch Enrollment](../_glossary-android.md#zero-touch-enrollment)
    - [Android Provisioning Methods](02-provisioning-methods.md) — full mode × method matrix (NFC / QR / afw#setup / Zero-Touch) with Android version availability
    - [Android Version Matrix](03-android-version-matrix.md) — minimum Intune-supported Android OS per mode and breakpoint callouts for Android 11, 12, 15
    - [iOS/iPadOS Enrollment Overview](../ios-lifecycle/00-enrollment-overview.md) — cross-platform comparison for admins familiar with iOS supervision axis
    - [Apple Provisioning Glossary](../_glossary-macos.md) — for iOS/macOS terminology including [supervision](../_glossary-macos.md#supervision)
    ```

    Do NOT link to `../common-issues.md`, `../quick-ref-l1.md`, `../quick-ref-l2.md`, or `../index.md` per Pitfall 8 deferred-file prohibition.

    **Section C: Word count verification**. After Sections A and B are written, run `wc -w docs/android-lifecycle/00-enrollment-overview.md` and confirm the count is in the 800-1200 range per D-07. Frontmatter YAML counts toward word count in standard `wc -w` but is minimal (~15 words) so it doesn't meaningfully impact the budget.

    If word count is under 800: expand per-mode H3 sections with 1-2 more sentences of operational context.
    If word count is over 1200: trim the Two Axes narrative (Task 3 Section A) or tighten per-mode H3 openings — DO NOT cut the comparison table (load-bearing per D-01) or the locked D-03 sentence in For Admins Familiar with iOS (locked exact wording).
  </action>

  <verify>
    <automated>
      grep -c "^### Zero-Touch Enrollment (ZTE)$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^### Fully Managed (COBO)$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^### BYOD Work Profile$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^### Dedicated (COSU)$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      grep -c "^### AOSP$" docs/android-lifecycle/00-enrollment-overview.md | grep -q "^1$" &&
      wc -w docs/android-lifecycle/00-enrollment-overview.md | awk '{ if ($1 >= 800 && $1 <= 1200) exit 0; else exit 1 }' &&
      grep -c "02-provisioning-methods.md" docs/android-lifecycle/00-enrollment-overview.md | awk '$1 >= 2 { exit 0 } { exit 1 }' &&
      grep -c "03-android-version-matrix.md" docs/android-lifecycle/00-enrollment-overview.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      test $(grep -cE "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md" docs/android-lifecycle/00-enrollment-overview.md) -eq 0
    </automated>
  </verify>

  <acceptance_criteria>
    - All 5 H3 mode sections exist with the exact names: `### Zero-Touch Enrollment (ZTE)`, `### Fully Managed (COBO)`, `### BYOD Work Profile`, `### Dedicated (COSU)`, `### AOSP` (in this order)
    - AOSP H3 section explicitly states: no GMS, no MGP binding, no work profile, QR-only, OEM-gated, Phase 39 stub forward-reference (per D-05)
    - BYOD Work Profile H3 section mentions AMAPI migration April 2025 and Microsoft Intune app as primary BYOD management surface
    - `wc -w docs/android-lifecycle/00-enrollment-overview.md` returns a count in the 800-1200 range (validates check 34-02-04)
    - `## See Also` section contains links to `../_glossary-android.md`, `02-provisioning-methods.md`, `03-android-version-matrix.md`, `../ios-lifecycle/00-enrollment-overview.md`, and `../_glossary-macos.md`
    - Zero links to deferred files: `grep -cE "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md" docs/android-lifecycle/00-enrollment-overview.md` returns 0
    - No Mermaid diagram unless added at author discretion within the 800-1200 word budget (Claude's Discretion per CONTEXT.md)
  </acceptance_criteria>

  <done>
    All 5 per-mode H3 sections published under `## Enrollment Mode Details`. AOSP explicitly flagged out-of-GMS per D-05. BYOD section records AMAPI migration. Word count verified in 800-1200 range per D-07. See Also section complete with valid cross-refs (no deferred files). Document ready for Phase 35-42 consumers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc author → Phase 35-42 consumers | Heading text of H2/H3 sections in this file produces anchors that Phase 35-42 plans and admin guides will cross-reference. Changing "## Two Axes of Android Enterprise" to "## Two Axes" breaks downstream links silently. |
| iOS-brained admin → Android content | The For Admins Familiar with iOS section is the bridge — if it fails (e.g., conflates Android Fully Managed with iOS Supervision as equivalents rather than "partial analog"), readers carry wrong mental model into Phase 35-39 admin guides. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Tampering (terminology) | Management Scope column, narrative sections outside For Admins Familiar with iOS | mitigate | AEAUDIT-04 grep check after each edit: `grep -i "supervis" docs/android-lifecycle/00-enrollment-overview.md` — every match must be inside the For Admins Familiar with iOS subsection OR a `_glossary-macos.md#supervision` link. Zero matches in comparison table rows. |
| T-34-06 | Denial of service (cross-reference) | Anchor stability for Phase 35-42 | mitigate | Publish canonical H2/H3 anchor list in `<interfaces>` section of this plan; executor validates heading text produces the exact kebab-case anchors Phase 35+ will reference. |
| T-34-07 | Information disclosure | AOSP row OEM specifics | accept | Phase 34 AOSP content is deliberately thin (D-05 — forward-ref to Phase 39 stub); no sensitive OEM details documented at Phase 34. |
| T-34-08 | Tampering (factual) | BYOD AMAPI migration claim | mitigate | Source verified via 2026-04-21 research in RESEARCH.md State of the Art table lines 589-604; phrasing matches "AMAPI migration April 2025 — custom OMA-URI no longer supported". |
</threat_model>

<verification>
All checks from 34-VALIDATION.md that apply to this plan:

**Per-task grep checks (mechanical):**
- `grep -c "| Mode | Ownership Model | Management Scope | Provisioning Surface | Appropriate Use Case |" docs/android-lifecycle/00-enrollment-overview.md` returns 1 (check 34-02-01)
- `grep -c "## Two Axes" docs/android-lifecycle/00-enrollment-overview.md` returns 1 (check 34-02-02)
- `grep -c "For Admins Familiar with iOS" docs/android-lifecycle/00-enrollment-overview.md` returns 1 (check 34-02-03)
- `wc -w docs/android-lifecycle/00-enrollment-overview.md` returns 800-1200 (check 34-02-04)
- Manual AEAUDIT-04: every "supervis" match is in the iOS-analog subsection or a cross-reference link (check 34-02-05)

**Phase-gate checks:**
- `grep "^platform: Android$" docs/android-lifecycle/00-enrollment-overview.md` matches (check 34-all-02)
- `grep -E "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md" docs/android-lifecycle/00-enrollment-overview.md` returns 0 matches (check 34-all-03)
- `review_by` minus `last_verified` ≤ 60 days (check 34-all-01)

**Anchor stability (for Phase 35-42):**
- Anchors `#two-axes-of-android-enterprise`, `#for-admins-familiar-with-ios`, `#fully-managed-cobo`, `#byod-work-profile`, `#dedicated-cosu`, `#zero-touch-enrollment-zte`, `#aosp`, `#enrollment-mode-comparison` all resolve from H2/H3 headings.

**Manual semantic checks:**
- AOSP row Provisioning Surface cell contains "QR only" AND "out-of-GMS" (or equivalent "no GMS") per D-05 (from VALIDATION.md Manual-Only Verifications)
- Management Scope column rows use Android terminology only — no iOS "Supervised"/"Unsupervised" leaks
</verification>

<success_criteria>
An admin familiar with iOS supervision can read `docs/android-lifecycle/00-enrollment-overview.md` and correctly place any Android scenario on both axes — ownership (corporate vs personal) and management scope (fully managed, work profile, dedicated, AOSP) — and understand how Android "fully managed" is the supervision analog (ROADMAP Phase 34 Success Criterion 2 — AEBASE-02).
</success_criteria>

<output>
After completion, create `.planning/phases/34-android-foundation/34-02-SUMMARY.md` documenting:
- File created + byte count + final word count (should be 800-1200)
- Anchors published (the 11-anchor list from interfaces)
- Confirmation that AEAUDIT-04 gate holds (every "supervis" match located)
- Confirmation that no deferred-file links exist
- Broken-link markers (transient): Provisioning Surface cells reference `02-provisioning-methods.md` which doesn't exist yet — resolved when Plan 03 (Wave 2) lands
</output>
