---
phase: 98-guide-07-comprehensive-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-macos/07-platform-sso-setup.md
autonomous: true
requirements: [ACC-03, TS-02]

must_haves:
  truths:
    - "Guide 07 Step 2 no longer offers 'VPP from Apps and Books' as a Company Portal deployment option (ACC-03)"
    - "The Step 2 'Deploy to the device' callout distinguishes install target (device) from assignment target (user group for affinity / device group for userless) (ACC-03)"
    - "The existing ADE Path Prerequisites table consolidates the three net-new A2 delivery facts: Intune-licensed user, Included-apps trimmed to com.microsoft.CompanyPortalMac, enrollment profile device/serial-targeted bridged by user affinity (TS-02)"
    - "Guide 07 body prose carries a lightweight A1/A2 equivalence pointer to guide 01 without renaming any heading (D-03)"
  artifacts:
    - path: "docs/admin-setup-macos/07-platform-sso-setup.md"
      provides: "ACC-03 VPP fix + reworded callout; TS-02 augmented ADE Prerequisites table; D-03 A1/A2 pointer; D-04 lockstep line-311 stamp bump"
      contains: "com.microsoft.CompanyPortalMac"
  key_links:
    - from: "docs/admin-setup-macos/07-platform-sso-setup.md (Advanced section body prose)"
      to: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md#which-path-is-right-for-you"
      via: "A1/A2 equivalence cross-reference"
      pattern: "which-path-is-right-for-you"
---

<objective>
Apply the ACC-03 VPP fix and the TS-02 A2-delivery-requirements augmentation to guide 07, plus the D-03 A1/A2 equivalence pointer — all surgical edits to a single file.

Purpose: Remove the last incorrect "VPP from Apps and Books" Company Portal claim (macOS CP is always LOB/PKG), cleanly separate install target from assignment target, and complete the A2 ADE-during-Setup-Assistant delivery requirements in the one table guide 01 already cross-links — without creating a duplicate A2 table and without breaking any anchor.
Output: Edited `docs/admin-setup-macos/07-platform-sso-setup.md` (ACC-03 + TS-02 + D-03 pointer + D-04 lockstep stamp); no new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/98-guide-07-comprehensive-pass/98-CONTEXT.md
@.planning/phases/98-guide-07-comprehensive-pass/98-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ACC-03 — remove VPP Company Portal option and reword the Step 2 "Deploy to the device" callout</name>
  <files>docs/admin-setup-macos/07-platform-sso-setup.md</files>
  <read_first>
    - docs/admin-setup-macos/07-platform-sso-setup.md (Step 2, ~lines 121–134 — the VPP line and the "Deploy to the device" blockquote; note line 127 already correctly says "(LOB app, NOT VPP)" for the A2 path)
    - .planning/phases/98-guide-07-comprehensive-pass/98-RESEARCH.md (§"ACC-03: VPP Fix Details" — VERIFIED: macOS CP is always PKG/LOB; VPP/Apps-and-Books applies to iOS/iPadOS CP only)
    - .planning/phases/98-guide-07-comprehensive-pass/98-CONTEXT.md (D-01 scope boundary; ACC-03 install-vs-assignment-target requirement)
  </read_first>
  <action>
    In the Step 2 bullet at ~line 126, remove the parenthetical "VPP from Apps and Books or as a" so the deploy instruction describes macOS Company Portal as a LOB/PKG-only managed app (e.g. deploy via Intune as a managed app — DMG/PKG line-of-business app — or verify existing deployment meets the version floor). The literal substring "VPP from Apps and Books" must no longer appear anywhere in Step 2. Per ACC-03 / SC#1.
    In the "Deploy to the device, not for the user to launch (assignment target)" blockquote at ~lines 129–134, keep the existing install-vs-assignment logic but disentangle it from any VPP framing: the callout must clearly state that (a) the INSTALL TARGET is the device — the Enterprise SSO plug-in inside Company Portal must be present on the device — and (b) the ASSIGNMENT TARGET is a user group for user-affinity fleets (assigned Required to the same user groups as the Platform SSO policy) or a device group for shared/userless Macs. Do not mention VPP. Preserve the existing inline cross-references to Step 4 and the Advanced section's three-policy same-group rule. Per ACC-03.
    Do NOT change line 127's existing "(LOB app, NOT VPP)" A2 reference — it is already correct. Do NOT rename any heading (D-03).
  </action>
  <acceptance_criteria>
    - `07-platform-sso-setup.md` contains zero occurrences of the literal substring "VPP from Apps and Books"
    - `07-platform-sso-setup.md` Step 2 deploy bullet (~line 126) describes Company Portal as an LOB/PKG/DMG managed app with no VPP mention
    - The "Deploy to the device" callout still contains both "install target" / "device" wording AND the assignment-target split ("user group" for affinity, "device group" for userless)
    - Heading lines `## Registration Approach: Decision and Alternatives`, `## End-User Sign-In Experience (Secure Enclave)`, `## Configuration-Caused Failures`, and `## Advanced / Optional: ADE-during-Setup-Assistant` are byte-identical to pre-edit (no slug change — D-03)
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && ! grep -q "VPP from Apps and Books" docs/admin-setup-macos/07-platform-sso-setup.md && grep -q "assignment target" docs/admin-setup-macos/07-platform-sso-setup.md && echo PASS</automated>
  </verify>
  <done>"VPP from Apps and Books" is gone from Step 2; the callout cleanly separates install target (device) from assignment target (user group / device group); no heading renamed.</done>
</task>

<task type="auto">
  <name>Task 2: TS-02 — augment the ADE Path Prerequisites table with three net-new A2 facts, add the A1/A2 equivalence pointer (D-03), and bump the line-311 stamp in lockstep (D-04)</name>
  <files>docs/admin-setup-macos/07-platform-sso-setup.md</files>
  <read_first>
    - docs/admin-setup-macos/07-platform-sso-setup.md (Advanced section ~lines 307–352: the `## Advanced / Optional: ADE-during-Setup-Assistant` heading line 307 — DO NOT RENAME; the line-311 provenance stamp; the `### ADE Path Prerequisites` table ~lines 315–324)
    - .planning/phases/98-guide-07-comprehensive-pass/98-RESEARCH.md (§"TS-02: A2 Company Portal Delivery Requirements" — VERIFIED Included-apps trim to com.microsoft.CompanyPortalMac and CP 5.2604.0 floor; [ASSUMED] Intune-licensed user and device-serial-bridged-by-affinity, with Pitfall 5 framing; §"Code Examples → ADE Path Prerequisites Table" three-rows-to-add snippet)
    - .planning/phases/98-guide-07-comprehensive-pass/98-CONTEXT.md (D-01: augment existing table, do NOT create a second A2 table; D-03: A1/A2 pointer in body prose only; D-04 caveat: bump line-311 stamp in lockstep when Advanced content is edited)
    - docs/macos-lifecycle/01-psso-provisioning-walkthrough.md (~lines 11–20: "Which Path Is Right for You?" — confirm the section heading and its `#which-path-is-right-for-you` slug for the equivalence-pointer link target; A1 = standard post-enrollment, A2 = ADE-during-Setup-Assistant)
  </read_first>
  <action>
    Add exactly three net-new rows to the EXISTING `### ADE Path Prerequisites` table (~lines 317–324) — do NOT create a second A2-requirements table (D-01 / Pitfall 1): (1) a "User license" row stating the user must have an active Microsoft Intune license, framed as a standard Intune requirement that is commonly overlooked as an A2 failure point (Pitfall 5 framing — do not present it as A2-exclusive); (2) a "Company Portal Included apps" row stating that in the LOB app's App bundle ID list only `com.microsoft.CompanyPortalMac` is included and any other bundle IDs are removed (VERIFIED via Microsoft Learn); (3) an "Enrollment profile device targeting" row stating the enrollment profile is assigned to devices/serials in Apple Business Manager while in Intune it is assigned to the same Assigned (static) user group as the PSSO policy and CP LOB app, with user affinity bridging the two. The existing rows (macOS version, CP 5.2604.0, enrollment method, group type, three-policy same-group rule, ADE profile settings) stay unchanged. Per TS-02 / SC#3.
    Add a single lightweight A1/A2 equivalence pointer in the Advanced section BODY PROSE (not in any heading — D-03 / Pitfall 2): a one-sentence note that this guide's ADE-during-Setup-Assistant configuration corresponds to the A2 path defined in guide 01, linking to `../macos-lifecycle/01-psso-provisioning-walkthrough.md#which-path-is-right-for-you` (and noting standard post-enrollment is the A1 path). Place it near the top of the Advanced section under the line-307 heading or under the ADE-only callout. Per D-03.
    Because this task edits the Advanced section's content, bump the line-311 per-section provenance stamp in lockstep (D-04 caveat / Pitfall 4): change `last_verified: 2026-06-20` to `2026-06-29` and `review_by: 2026-09-20` to `2026-09-29` (+3-month / same-day-of-month invariant preserved). Do NOT add any new per-section pair-stamp elsewhere.
    Leave the heading `## Advanced / Optional: ADE-during-Setup-Assistant` (line 307) byte-identical — slug `#advanced--optional-ade-during-setup-assistant` carries inbound anchors from guide 01:394/408 and internal 07:25/40/127/253.
  </action>
  <acceptance_criteria>
    - The `### ADE Path Prerequisites` table contains a row whose value names `com.microsoft.CompanyPortalMac` as the only Included app (Included-apps trim)
    - The table contains a "User license" / "Intune license" row and an enrollment-profile device-targeting / user-affinity row (three net-new rows total; no other table added)
    - `07-platform-sso-setup.md` contains exactly one occurrence of the link fragment `01-psso-provisioning-walkthrough.md#which-path-is-right-for-you` (A1/A2 equivalence pointer present, D-03)
    - Heading `## Advanced / Optional: ADE-during-Setup-Assistant` is byte-identical to pre-edit (slug `#advanced--optional-ade-during-setup-assistant` preserved)
    - Line-311 region shows `last_verified: 2026-06-29` and `review_by: 2026-09-29` (lockstep bump, +3-month/same-day)
    - No second A2-requirements table exists (no new heading such as `### ADE Company Portal Requirements`)
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && grep -q "ADE Path Prerequisites" docs/admin-setup-macos/07-platform-sso-setup.md && grep -c "com.microsoft.CompanyPortalMac" docs/admin-setup-macos/07-platform-sso-setup.md | grep -qv '^0$' && grep -q "which-path-is-right-for-you" docs/admin-setup-macos/07-platform-sso-setup.md && grep -q "2026-06-29" docs/admin-setup-macos/07-platform-sso-setup.md && grep -q "## Advanced / Optional: ADE-during-Setup-Assistant" docs/admin-setup-macos/07-platform-sso-setup.md && echo PASS</automated>
  </verify>
  <done>The ADE Path Prerequisites table carries the three net-new A2 facts; the A1/A2 equivalence pointer links to guide 01; the line-311 stamp is bumped in lockstep; the Advanced heading/slug is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This is a documentation-only edit to one static markdown file. No code, no inputs, no auth, no data flow, no executable attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering (content accuracy) | TS-02 [ASSUMED] facts (Intune-licensed user; device-serial bridged by affinity) | mitigate | RESEARCH spot-verified; framed per Pitfall 5 as standard requirement, not A2-exclusive; VERIFIED facts (CP floor, Included-apps trim) drive the table |
| T-98-02 | Tampering (link integrity) | Advanced-section heading slug + A1/A2 pointer link | mitigate | Slug discipline (D-03) — no heading rename; pointer targets a verified guide-01 anchor; Phase-100 link-integrity sweep is the durable guard |
| T-98-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed — pure markdown edit (RESEARCH §Package Legitimacy Audit: Not applicable) |
</threat_model>

<verification>
- `grep "VPP from Apps and Books"` returns zero matches in guide 07
- `grep "com.microsoft.CompanyPortalMac"` returns matches inside the ADE Path Prerequisites table region
- `grep "which-path-is-right-for-you"` returns exactly one match
- `git diff` shows no change to any `## ` heading line (slug discipline)
- Line-311 stamp reads `2026-06-29` / `2026-09-29`
</verification>

<success_criteria>
- ACC-03 satisfied: VPP option removed; callout separates install target (device) from assignment target (user group / device group)
- TS-02 satisfied (table portion): the three net-new A2 facts live in the existing ADE Path Prerequisites table; no duplicate table
- D-03 honored: A1/A2 pointer in body prose; all slugs byte-identical
- D-04 lockstep: line-311 Advanced stamp bumped; no new per-section pair-stamp added
</success_criteria>

<output>
Create `.planning/phases/98-guide-07-comprehensive-pass/98-01-SUMMARY.md` when done
</output>
