---
phase: 98-guide-07-comprehensive-pass
plan: 03
type: execute
wave: 3
depends_on: ["98-02"]
files_modified:
  - docs/admin-setup-macos/07-platform-sso-setup.md
  - .planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md
autonomous: true
requirements: [DEP-03]

must_haves:
  truths:
    - "Guide 07 carries the full PSSO admin-setup depth (Registration-Approach decision record, End-User Sign-In Experience + local-password lifecycle, AccountName token mapping, Non Platform SSO Accounts, Optional & Advanced settings with two account models) bound to DEP-03 and freshness-stamped (DEP-03)"
    - "The file-level frontmatter last_verified/review_by is bumped to the Phase-98 edit date with the +3-month/same-day-of-month invariant preserved (D-04)"
    - "One dated version-history row documents the ACC-03 + TS-01/02/03 + DEP-03 work (D-04)"
    - "A Phase-100 needle-spec hand-off records the stable tokens for the future check-phase-98.mjs without authoring a validator"
  artifacts:
    - path: "docs/admin-setup-macos/07-platform-sso-setup.md"
      provides: "DEP-03 formalization binding + D-04 frontmatter bump + version-history row"
      contains: "Phase 98"
    - path: ".planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md"
      provides: "Phase-100 stable-token needle inventory hand-off (no validator authored this phase)"
      contains: "check-phase-98.mjs"
  key_links:
    - from: "version-history row (2026-06-29)"
      to: "ACC-03 + TS-01/02/03 + DEP-03 work"
      via: "dated change record"
      pattern: "Phase 98"
---

<objective>
Formalize the already-present DEP-03 admin-setup depth, apply the D-04 file-level freshness mechanism, and record the Phase-100 needle-spec hand-off — closing the phase's documentation contract.

Purpose: DEP-03 content (Registration Approach, End-User Sign-In, Optional & Advanced, AccountName token, Non-PSSO-Accounts) is already written and Microsoft-Learn-verified — formalize-only (freeze, do not re-author). Bind it to DEP-03 via the version-history row, bump file-level freshness, and hand Phase 100 a ready-to-use needle inventory so it authors check-phase-98.mjs without re-discovery.
Output: Edited `docs/admin-setup-macos/07-platform-sso-setup.md` (frontmatter + version-history row) and new `.planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/98-guide-07-comprehensive-pass/98-CONTEXT.md
@.planning/phases/98-guide-07-comprehensive-pass/98-RESEARCH.md
@.planning/phases/98-guide-07-comprehensive-pass/98-01-SUMMARY.md
@.planning/phases/98-guide-07-comprehensive-pass/98-02-SUMMARY.md
@.planning/phases/97-enrollment-filevault-depth-formalization/97-NEEDLE-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEP-03 formalize-only — bounded spot-verify, file-level frontmatter bump, and the dated version-history row</name>
  <files>docs/admin-setup-macos/07-platform-sso-setup.md</files>
  <read_first>
    - docs/admin-setup-macos/07-platform-sso-setup.md (frontmatter lines 2–3 — `last_verified: 2026-06-27` / `review_by: 2026-09-27`; the DEP-03 content sections: Registration Approach 17–51, End-User Sign-In 52–79, AccountName token 175–182, Non-PSSO-Accounts 183–184, Optional & Advanced 233–293; the version-history table 355–364; the Recovery wipe+re-enroll claim 341–351)
    - .planning/phases/98-guide-07-comprehensive-pass/98-RESEARCH.md (§"DEP-03: Formalize-Only Audit" — all five DEP-03 areas confirmed PRESENT; the ONLY warranted spot-verify is the "wipe + re-enroll" recovery claim, already VERIFIED; §"Code Examples → Version-History Row Format")
    - .planning/phases/98-guide-07-comprehensive-pass/98-CONTEXT.md (D-04: file-level frontmatter bump + ONE version-history row; NO new per-section pair-stamp; DEP-03 is formalize-only — freeze content)
    - .planning/phases/97-enrollment-filevault-depth-formalization/97-CONTEXT.md? (skip — use the inherited Phase 97 D-04 bounded-spot-verify cap already summarized in 98-CONTEXT.md)
  </read_first>
  <action>
    Bounded spot-verify ONLY (Phase 97 D-04 cap): confirm the single uncited high-drift claim — the Recovery "wipe + re-enroll" procedure (~lines 341–351) — is consistent with Microsoft Learn (RESEARCH marks it VERIFIED). Make NO content change to any DEP-03 section unless a genuine factual error is found; if none, leave the five DEP-03 sections byte-unchanged (formalize-only — freeze, do not re-author). Per DEP-03 / SC#5.
    Bump the file-level frontmatter (lines 2–3): `last_verified: 2026-06-27` → `2026-06-29`, `review_by: 2026-09-27` → `2026-09-29` (+3-month / same-day-of-month invariant — never bump one without the other). Per D-04.
    Add exactly ONE dated version-history row to the table at the top of the history block (~line 356), dated `2026-06-29`, in the existing `| Date | Change | Author |` format, documenting the whole Phase-98 pass: removed the VPP Company Portal option (ACC-03); added the Extension-Identifier-typo Configuration-Caused-Failure + deep-dive (TS-01); augmented the ADE Path Prerequisites table with the three net-new A2 delivery requirements + A1/A2 pointer (TS-02); added the Setup-Assistant SSO-extension diagnostic tree (TS-03); formalized the PSSO admin-setup depth under DEP-03. Per D-04.
    Do NOT add any NEW per-section last_verified/review_by pair-stamp to the troubleshooting content (D-04). (The Advanced-section line-311 stamp was already bumped in lockstep in Plan 98-01 — do not touch it again here.) Do NOT rename any heading (D-03).
  </action>
  <acceptance_criteria>
    - Frontmatter shows `last_verified: 2026-06-29` and `review_by: 2026-09-29` (exactly 3 months apart, same day-of-month 29)
    - The version-history table has exactly one new row dated `2026-06-29` whose text references Phase 98 and names ACC-03, TS-01, TS-02, TS-03, and DEP-03 (or the work each denotes)
    - No DEP-03 content section heading is renamed; the five DEP-03 sections are unchanged except where a genuine error correction was required (expected: none)
    - No NEW per-section `last_verified`/`review_by` pair-stamp was introduced beyond the existing line-311 stamp (which was bumped in 98-01, not here)
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && grep -q "last_verified: 2026-06-29" docs/admin-setup-macos/07-platform-sso-setup.md && grep -q "review_by: 2026-09-29" docs/admin-setup-macos/07-platform-sso-setup.md && grep -qE "^\| 2026-06-29 \|.*Phase 98" docs/admin-setup-macos/07-platform-sso-setup.md && echo PASS</automated>
  </verify>
  <done>Frontmatter bumped with the +3-month/same-day invariant; one Phase-98 version-history row binds ACC-03 + TS-01/02/03 + DEP-03; DEP-03 content frozen; no new per-section stamp.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-100 needle-spec hand-off — record stable tokens for the future check-phase-98.mjs (author NO validator)</name>
  <files>.planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md</files>
  <read_first>
    - .planning/phases/97-enrollment-filevault-depth-formalization/97-NEEDLE-SPEC.md (the established hand-off format: header note that the phase authors NO validator, the stable-token needle inventory table, and the Phase-100 structure-template section)
    - .planning/phases/98-guide-07-comprehensive-pass/98-RESEARCH.md (§"Phase-100 Needle-Spec Hand-Off" — the 10 candidate tokens and their sections; §"Harness coverage note" re check-phase-76 V-76-PRESENCE and check-phase-81 E7)
    - .planning/phases/98-guide-07-comprehensive-pass/98-CONTEXT.md (Integration Points: the candidate stable tokens; Deferred Ideas: check-phase-98.mjs is Phase 100 / HARN-02 indivisible Atom 2 — presence-only on tokens, never needle dates)
    - docs/admin-setup-macos/07-platform-sso-setup.md (confirm each candidate token is present verbatim after the 98-01 and 98-02 edits, so the needle inventory is accurate post-edit)
  </read_first>
  <action>
    Create `.planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md` modeled on 97-NEEDLE-SPEC.md. Open with a note that this file is the Phase-100 executor's input for authoring `check-phase-98.mjs` as part of the indivisible Atom 2 (HARN-02), and that Phase 98 authors NO validator (do not create `scripts/validation/check-phase-98.mjs` until Phase 100).
    Include a stable-token needle inventory table (Needle ID | Literal Token | Present | Notes) covering the post-edit tokens — verify each is present verbatim in guide 07 before listing it: `com.microsoft.CompanyPortalMac.ssoextension` (Extension Identifier / TS-01 typo anchor), `Enable Registration During Setup` (TS-03 bisect + Advanced), `5.2604.0` (A2 CP floor), `com.microsoft.CompanyPortalMac` (Included-apps trim — note this is a substring of the .ssoextension token, so Phase 100 must use a structurally-anchored match such as the table-row context to avoid double-counting), `Non Platform SSO Accounts`, `AccountShortName`, and the stable section labels `Configuration-Caused Failures`, `ADE Path Prerequisites`, `Registration Approach: Decision and Alternatives`, `End-User Sign-In Experience (Secure Enclave)`, plus the new TS-03 diagnostic-tree heading and the TS-01 deep-dive sub-heading slug.
    Record a precision note that all needles are PRESENCE-only and Phase 100 must NEVER needle the freshness dates (Phase 97 D-02). Note the existing harness coverage that must stay green: check-phase-76 V-76-PRESENCE (file exists + non-empty) and check-phase-81 E7 (the inbound link string `](07-platform-sso-setup.md)` from guide 03 — path-level, unaffected by internal edits). Note the anchor-stability set that the Phase-100 link-integrity sweep guards: `#advanced--optional-ade-during-setup-assistant`, `#end-user-sign-in-experience-secure-enclave`, `#registration-approach-decision-and-alternatives`, `#configuration-caused-failures`.
    This is a NEW planning-dir file; it touches no doc content and no validator.
  </action>
  <acceptance_criteria>
    - `.planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md` exists and contains the literal string `check-phase-98.mjs`
    - It states explicitly that Phase 98 authors NO validator and that the validator is Phase-100 HARN-02 Atom 2
    - The needle inventory lists at minimum: `com.microsoft.CompanyPortalMac.ssoextension`, `Enable Registration During Setup`, `5.2604.0`, `com.microsoft.CompanyPortalMac`, `Non Platform SSO Accounts`, `AccountShortName`
    - It records the presence-only / never-needle-dates rule and notes the com.microsoft.CompanyPortalMac vs .ssoextension substring caution
    - It notes the existing check-phase-76 / check-phase-81 coverage and the guarded anchor-stability set
    - No file under `scripts/validation/` is created
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && test -f .planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md && grep -q "check-phase-98.mjs" .planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md && grep -q "com.microsoft.CompanyPortalMac.ssoextension" .planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md && test ! -f scripts/validation/check-phase-98.mjs && echo PASS</automated>
  </verify>
  <done>98-NEEDLE-SPEC.md records the presence-only stable-token inventory and harness-coverage notes for Phase 100; no validator authored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only edits (one markdown doc + one planning-dir hand-off file). No code, inputs, auth, or data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-05 | Tampering (content accuracy) | DEP-03 formalize-only freeze | accept | Content already Microsoft-Learn-verified (RESEARCH §DEP-03 audit); bounded spot-verify of the one uncited claim only — re-authoring would risk regressing verified depth |
| T-98-06 | Tampering (freshness integrity) | Frontmatter date bump | mitigate | +3-month/same-day invariant enforced (D-04); Phase-100 harness forbidden to needle dates, so no date time-bomb |
| T-98-07 | Repudiation (traceability) | DEP-03 ↔ REQ binding | mitigate | One dated version-history row + Phase-100 needle-spec bind the depth to DEP-03 and to the future validator |
| T-98-SC | Tampering | npm/pip/cargo installs | n/a | No packages — pure markdown + planning-doc edit |
</threat_model>

<verification>
- Frontmatter reads `last_verified: 2026-06-29` / `review_by: 2026-09-29`
- Exactly one new `| 2026-06-29 | … Phase 98 …` version-history row
- `98-NEEDLE-SPEC.md` exists with the stable-token inventory and the "no validator this phase" note
- `scripts/validation/check-phase-98.mjs` does NOT exist
- `git diff` shows no rename of any `## ` heading and no new per-section pair-stamp beyond the 98-01 line-311 bump
</verification>

<success_criteria>
- DEP-03 satisfied: the admin-setup depth is bound to DEP-03 and freshness-stamped; content frozen (formalize-only)
- D-04 satisfied: file-level frontmatter bumped with invariant; one version-history row; no new per-section pair-stamp
- Phase-100 hand-off: needle-spec recorded; no validator authored (deferred to Phase 100)
</success_criteria>

<output>
Create `.planning/phases/98-guide-07-comprehensive-pass/98-03-SUMMARY.md` when done
</output>
