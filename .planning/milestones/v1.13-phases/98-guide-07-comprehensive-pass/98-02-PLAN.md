---
phase: 98-guide-07-comprehensive-pass
plan: 02
type: execute
wave: 2
depends_on: ["98-01"]
files_modified:
  - docs/admin-setup-macos/07-platform-sso-setup.md
autonomous: true
requirements: [TS-01, TS-03]

must_haves:
  truths:
    - "The Configuration-Caused Failures table has an Extension-Identifier-typo row with a Runbook pointer to an adjacent prose deep-dive (TS-01)"
    - "The deep-dive documents symptom (looping Setup-Assistant sign-in despite Company Portal Installed + PSSO policy Succeeded), root cause (typo in the free-text Extension Identifier; correct value com.microsoft.CompanyPortalMac.ssoextension; Intune does not validate it), fix, and that it affects both A1 and A2 (TS-01)"
    - "Guide 07 contains a Setup-Assistant SSO-extension diagnostic tree as a numbered bisection ladder, in its own subsection NOT nested under the Configuration-Caused Failures H2 (TS-03)"
  artifacts:
    - path: "docs/admin-setup-macos/07-platform-sso-setup.md"
      provides: "TS-01 failures-table row + deep-dive; TS-03 diagnostic-tree subsection"
      contains: "com.microsoft.CompanyPortalMac.ssoextension"
  key_links:
    - from: "Configuration-Caused Failures table (Extension-Identifier-typo Runbook cell)"
      to: "TS-01 deep-dive sub-heading anchor"
      via: "in-page anchor link"
      pattern: "#extension-identifier-typo"
---

<objective>
Author the two net-new TS troubleshooting deliverables in guide 07: the Extension-Identifier-typo Configuration-Caused-Failure (table row + adjacent prose deep-dive) and the Setup-Assistant SSO-extension diagnostic tree (numbered bisection ladder).

Purpose: Capture the two highest-value real-world failure modes from the 2026-06-27/28 session so an admin can recognize the permanent-typo signature (distinct from the transient "CP still downloading" case) and bisect a Setup-Assistant SSO-extension failure deterministically.
Output: Edited `docs/admin-setup-macos/07-platform-sso-setup.md` (TS-01 row + deep-dive under the existing Configuration-Caused Failures H2; TS-03 as a new sibling H2). No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/98-guide-07-comprehensive-pass/98-CONTEXT.md
@.planning/phases/98-guide-07-comprehensive-pass/98-RESEARCH.md
@.planning/phases/98-guide-07-comprehensive-pass/98-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TS-01 — add the Extension-Identifier-typo row to the Configuration-Caused Failures table and author the adjacent prose deep-dive</name>
  <files>docs/admin-setup-macos/07-platform-sso-setup.md</files>
  <read_first>
    - docs/admin-setup-macos/07-platform-sso-setup.md (the `## Configuration-Caused Failures` table ~lines 221–231 — note the `Misconfiguration | Portal | Symptom | Runbook` schema and pointer-style Runbook cells; the Extension Identifier value already at line 147; the contrast-table idiom at 177–180 / 190–195)
    - .planning/phases/98-guide-07-comprehensive-pass/98-RESEARCH.md (§"TS-01: Extension-Identifier-Typo Failure" — VERIFIED correct value com.microsoft.CompanyPortalMac.ssoextension; [ASSUMED] "Intune does not validate" + symptom string, with the directive to describe the OBSERVABLE signature rather than quote an exact UI string; §"Open Questions" #1)
    - .planning/phases/98-guide-07-comprehensive-pass/98-CONTEXT.md (D-01: TS-01 row + adjacent prose deep-dive immediately below the table; symptom string; affects both A1 and A2)
  </read_first>
  <action>
    Add one schema-conformant row to the EXISTING Configuration-Caused Failures table (`Misconfiguration | Portal | Symptom | Runbook`): Misconfiguration = the Extension Identifier free-text field contains a typo (note the correct value `com.microsoft.CompanyPortalMac.ssoextension` and that Intune does not validate this free-text field); Portal = Intune; Symptom = looping Setup-Assistant sign-in ("Unable to Sign-In … necessary SSO application or extension"-class message) despite Company Portal Installed and the PSSO policy showing Succeeded; Runbook = an in-page pointer to the deep-dive below (anchor link to the deep-dive sub-heading). Per TS-01 / SC#2 / D-01.
    Immediately below the table, author a prose deep-dive under a new `### ` sub-heading whose slug contains `extension-identifier-typo` (e.g. `### Extension Identifier typo — looping Setup-Assistant sign-in`). The deep-dive must cover, in this shape: SYMPTOM — the persistent/looping failure, with the distinguishing tell that Company Portal is Installed and the PSSO policy reads Succeeded yet "Try Again" loops indefinitely (contrast this PERMANENT typo case with the documented TRANSIENT "Company Portal still downloading → Try Again resolves it" case so the admin can tell them apart); ROOT CAUSE — the Extension Identifier in the Settings Catalog policy does not byte-match the actual extension bundle ID, so macOS cannot activate the SSO extension; the field is free-text and (per [ASSUMED], framed observably) Intune does not flag the typo at save time; FIX — set the Extension Identifier to exactly `com.microsoft.CompanyPortalMac.ssoextension` and re-sync; AFFECTS — explicitly state this affects BOTH the A1 (standard post-enrollment) and A2 (ADE-during-Setup-Assistant) paths, since the Extension Identifier is shared configuration. Describe the symptom by observable signature, not by a hard-quoted exact UI string (Open Question #1). If the two near-identical bundle IDs end up adjacent, use the existing contrast-table idiom to disambiguate `com.microsoft.CompanyPortalMac.ssoextension` (Extension Identifier) from `com.microsoft.CompanyPortalMac` (CP app bundle ID).
    Do NOT rename the `## Configuration-Caused Failures` heading (D-03). The deep-dive `###` sub-heading is net-new (no inbound anchors) and safe to add.
  </action>
  <acceptance_criteria>
    - The Configuration-Caused Failures table contains a new row referencing the Extension Identifier typo and the value `com.microsoft.CompanyPortalMac.ssoextension`
    - A `### ` deep-dive sub-heading whose GitHub slug contains `extension-identifier-typo` exists immediately below the table, and the table's Runbook cell links to that anchor
    - The deep-dive prose contains all four elements: the looping symptom WITH the "Installed + Succeeded" distinguishing tell, the root-cause (identifier mismatch / free-text not validated), the fix (exact value), and the explicit "affects both A1 and A2" statement
    - The deep-dive distinguishes the permanent-typo case from the transient CP-still-downloading case
    - Heading `## Configuration-Caused Failures` is byte-identical to pre-edit (slug preserved — D-03)
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && grep -q "com.microsoft.CompanyPortalMac.ssoextension" docs/admin-setup-macos/07-platform-sso-setup.md && grep -iq "affects both" docs/admin-setup-macos/07-platform-sso-setup.md && grep -iqE "extension identifier typo" docs/admin-setup-macos/07-platform-sso-setup.md && grep -q "## Configuration-Caused Failures" docs/admin-setup-macos/07-platform-sso-setup.md && echo PASS</automated>
  </verify>
  <done>The typo failure has a table row pointing to an adjacent deep-dive covering symptom (with the permanent-vs-transient tell), root cause, fix, and A1+A2 applicability; the failures heading/slug is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: TS-03 — author the Setup-Assistant SSO-extension diagnostic tree as a numbered bisection ladder in its own sibling subsection</name>
  <files>docs/admin-setup-macos/07-platform-sso-setup.md</files>
  <read_first>
    - docs/admin-setup-macos/07-platform-sso-setup.md (the end of the Configuration-Caused Failures section and the start of `## Optional and Advanced Platform SSO Settings` at line 233 — the new TS-03 H2 lands between them; note the guide contains ZERO mermaid)
    - .planning/phases/98-guide-07-comprehensive-pass/98-RESEARCH.md (§"TS-03: Diagnostic Tree — Sequence Locked" — the exact 5-step order; format = numbered ordered list with nested sub-bullet for the A1 bisect; NOT mermaid, NOT a decision table)
    - .planning/phases/98-guide-07-comprehensive-pass/98-CONTEXT.md (D-02: bisection-ladder format and rationale; D-01: own subsection, NOT nested under the Configuration-Caused Failures H2 / Pitfall 3)
  </read_first>
  <action>
    Add a NEW sibling `## ` heading (e.g. `## Setup-Assistant SSO-Extension Diagnostic Tree`) placed immediately AFTER the Configuration-Caused Failures section (after the TS-01 deep-dive) and BEFORE `## Optional and Advanced Platform SSO Settings`. It must be an H2 sibling — NOT an H3/H4 nested under `## Configuration-Caused Failures` (D-01 / Pitfall 3). Per TS-03 / SC#4.
    Author the diagnostic content as a numbered/nested ordered list (markdown `1.` `2.` …), an "if X → check Y" bisection ladder in this locked order (D-02): (1) Check the Intune device record — is the device enrolled and is the PSSO policy showing Succeeded? (2) Check the Company Portal version on the device — "Installed ≠ correct version" (below `5.2404.0` for A1, below `5.2604.0` for A2)? (3) Check the Extension Identifier — does the policy value exactly match `com.microsoft.CompanyPortalMac.ssoextension`? (cross-reference the TS-01 deep-dive); (4) Check the user license — does the user have an active Intune license? (5) A1 bisect — as a NESTED sub-bullet branch under the final rung: if the A1 path is suspected, disable `Enable Registration During Setup` and re-test with the standard post-enrollment flow. Use the literal backticked tokens `Enable Registration During Setup`, `5.2604.0`, `5.2404.0`, and `com.microsoft.CompanyPortalMac.ssoextension`. Do NOT use mermaid and do NOT render this as a decision table (D-02).
    Do NOT rename any existing heading (D-03). The new H2 is net-new and carries no inbound anchors.
  </action>
  <acceptance_criteria>
    - A new `## ` heading for the diagnostic tree exists and sits at H2 level between `## Configuration-Caused Failures` content and `## Optional and Advanced Platform SSO Settings` (verified by reading the heading hierarchy — it is NOT an `###`/`####` under the failures H2)
    - The diagnostic content is a numbered ordered list (not a mermaid block, not a table) containing, in order, the five rungs: Intune device record → Company Portal version → Extension Identifier → user license → A1 bisect
    - The list contains the literal tokens `Enable Registration During Setup`, `5.2604.0`, and `com.microsoft.CompanyPortalMac.ssoextension`
    - The A1-bisect rung is a nested sub-bullet branch (indented under the ladder), and references disabling `Enable Registration During Setup`
    - Guide 07 still contains zero ```mermaid fences
    - All pre-existing `## ` headings byte-identical (D-03)
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && grep -qiE "^## .*(diagnostic|sso-extension)" docs/admin-setup-macos/07-platform-sso-setup.md && grep -q "Enable Registration During Setup" docs/admin-setup-macos/07-platform-sso-setup.md && grep -q "5.2604.0" docs/admin-setup-macos/07-platform-sso-setup.md && ! grep -q '```mermaid' docs/admin-setup-macos/07-platform-sso-setup.md && grep -q "## Optional and Advanced Platform SSO Settings" docs/admin-setup-macos/07-platform-sso-setup.md && echo PASS</automated>
  </verify>
  <done>The diagnostic tree exists as an H2-sibling numbered bisection ladder in the locked 5-step order with the nested A1-bisect branch; no mermaid; no heading renamed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only edit to one static markdown file. No code, inputs, auth, or data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-03 | Tampering (content accuracy) | TS-01 [ASSUMED] claims (Intune-does-not-validate; exact symptom UI string) | mitigate | Authored by observable signature (CP Installed + policy Succeeded + looping), not a hard-quoted UI string; correct value VERIFIED via Microsoft Learn; permanent-vs-transient contrast prevents misdiagnosis |
| T-98-04 | Tampering (structure/link integrity) | New deep-dive + diagnostic-tree headings | mitigate | New headings only (no rename); deep-dive anchor is in-page; Phase-100 link-integrity sweep is the durable guard |
| T-98-SC | Tampering | npm/pip/cargo installs | n/a | No packages — pure markdown edit |
</threat_model>

<verification>
- `grep "com.microsoft.CompanyPortalMac.ssoextension"` matches in both the failures section and the diagnostic tree
- The diagnostic-tree heading is an H2 sibling (read the heading hierarchy: failures H2 → deep-dive H3 → diagnostic-tree H2 → Optional/Advanced H2)
- `grep '```mermaid'` returns zero matches
- `git diff` shows no change to any pre-existing `## ` heading line
</verification>

<success_criteria>
- TS-01 satisfied: typo row + deep-dive (symptom/root-cause/fix/A1+A2) under the existing Configuration-Caused Failures section
- TS-03 satisfied: diagnostic tree as a numbered bisection ladder in its own H2 subsection, locked 5-step order, nested A1-bisect branch, no mermaid
- D-01 honored: deep-dive stays under the failures H2; diagnostic tree is a sibling H2 (not nested)
- D-02 honored: ladder format, not mermaid, not a table
- D-03 honored: no heading renamed
</success_criteria>

<output>
Create `.planning/phases/98-guide-07-comprehensive-pass/98-02-SUMMARY.md` when done
</output>
