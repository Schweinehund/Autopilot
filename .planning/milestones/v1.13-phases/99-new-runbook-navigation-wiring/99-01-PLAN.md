---
phase: 99-new-runbook-navigation-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l1-runbooks/37-macos-local-password-reset.md
  - docs/l1-runbooks/36-macos-secure-enclave-key.md
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "Runbook #37 documents all three local-password recovery paths in sequential per-path sections: PRIMARY escrowed FileVault recovery key, then macOS LAPS managed admin, then Apple ID (gated 'where org policy allows') — per D-01/D-03 (RUN-01)"
    - "Runbook #37 states inline at the explanation step that SSPR resets the Entra password but does NOT reset the independent local password under Secure Enclave, with a cross-link to depth (03 Local Password Policy / 07-08 auth) — per D-03"
    - "Every recovery path in #37 ends with a single hand-off cross-link to L1 #36 for the mandatory PSSO re-registration, and states that recovery-key use / any local-password reset destroys the Secure Enclave binding — per D-02 (obj-17)"
    - "All recovery steps are authored as PRE-LOGIN GUI actions; no Terminal/CLI command (app-sso, fdesetup, etc.) appears anywhere in #37's Steps — per obj-7"
    - "L1 #36 receives exactly one reciprocal back-link line to #37 and is otherwise byte-stable (slugs, steps 1-4, re-registration paths at 36:49-53 unchanged) — per D-02"
  artifacts:
    - path: "docs/l1-runbooks/37-macos-local-password-reset.md"
      provides: "New L1 runbook for macOS local-password recovery on Secure-Enclave PSSO devices"
      contains: "audience: L1"
    - path: "docs/l1-runbooks/36-macos-secure-enclave-key.md"
      provides: "Reciprocal back-link line to #37 (only edit)"
      contains: "37-macos-local-password-reset.md"
  key_links:
    - from: "docs/l1-runbooks/37-macos-local-password-reset.md"
      to: "docs/l1-runbooks/36-macos-secure-enclave-key.md"
      via: "hand-off cross-link at end of each recovery path"
      pattern: "36-macos-secure-enclave-key\\.md"
    - from: "docs/l1-runbooks/36-macos-secure-enclave-key.md"
      to: "docs/l1-runbooks/37-macos-local-password-reset.md"
      via: "reciprocal back-link line"
      pattern: "37-macos-local-password-reset\\.md"
---

<objective>
Author the new L1 runbook `docs/l1-runbooks/37-macos-local-password-reset.md` covering the three local-password recovery paths for Secure-Enclave PSSO devices, and add the single reciprocal back-link line to L1 #36. This is Wave 1 of the navigation-last sequence: both edits MUST be committed before any navigation-hub wiring (Plan 99-02) begins.

Purpose: Deliver RUN-01's core artifact — one authoritative L1 source for locked-out macOS users (per D-01), with a clean boundary to #36 for PSSO re-registration (per D-02).
Output: New file `37-macos-local-password-reset.md`; one reciprocal-link line added to `36-macos-secure-enclave-key.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-new-runbook-navigation-wiring/99-CONTEXT.md
@.planning/phases/99-new-runbook-navigation-wiring/99-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author runbook #37 (three pre-login recovery paths, SSPR inline, #36 hand-offs)</name>
  <files>docs/l1-runbooks/37-macos-local-password-reset.md</files>
  <read_first>
    - docs/l1-runbooks/36-macos-secure-enclave-key.md (frontmatter 1-7, step ordering 1-4, trigger 13, SE-destruction note 43-45, escalation/L2-#27 at 86, version history — structural and voice analog; confirm Step 1 immediately requires Terminal so #37 must NOT)
    - docs/l1-runbooks/35-macos-sso-sign-in-failure.md (frontmatter 1-7, "Root Cause N" labeled-section house style, inline-fact-plus-cross-link pattern at 35:32)
    - docs/admin-setup-macos/03-configuration-profiles.md (FileVault facts 158-182: escrow auto-on 160, Intune admin retrieval 180, Company-Portal user self-service "Get recovery key" 182, Corporate-only 161, RBAC "Rotate FileVault key" 181; Local Password Policy heading at 03:98)
    - docs/admin-setup-macos/07-platform-sso-setup.md (LAPS facts 72-77: managed local admin LAPS 74, recovery context 77; "Local password lifecycle and rotation" heading at 07:67)
    - 99-RESEARCH.md §"Runbook Content Map" (skeleton 80-147), §"Recovery Path A/B/C Verified Facts" (151-207), §"SSPR Clarification" (210-225), §"Pre-Login Authoring Constraint" (229-246), §"Common Pitfalls" (426-459)
    - 99-CONTEXT.md D-01, D-02, D-03 + execution cautions (lines 122-126)
  </read_first>
  <action>
    Create docs/l1-runbooks/37-macos-local-password-reset.md mirroring the #35/#36 skeleton exactly. Frontmatter = the 5 keys in #36 order: last_verified: 2026-06-29, review_by: 2026-09-29, applies_to: ADE, audience: L1, platform: macOS. Then the platform-gate blockquote (mirror #36 line 1-2), an H1 title, a trigger paragraph ("Use this runbook when the user cannot log in — local password lost or unknown"), ## Prerequisites, ## Steps, ## Escalation Criteria, a Back-to-triage link, and a ## Version History table with a single 2026-06-29 Initial-version row (per D-01).

    H1 slug discipline (D-01 / Pitfall 4): do NOT use an em-dash-plus-space in the H1 (it collapses to a double-hyphen anchor). Use a colon or a plain title (e.g. "macOS Local Password Recovery: Secure Enclave PSSO Devices"). The file slug 37-macos-local-password-reset.md is fixed.

    Structure ## Steps as three sequential labeled per-path sections in this order (D-03): Recovery Path A — Escrowed FileVault Recovery Key (Primary), Recovery Path B — macOS LAPS Managed Admin Account (Secondary), Recovery Path C — Apple ID Reset (Where Org Policy Allows). Corpus-backed Path A facts: escrow is automatic for corporate-encrypted Macs (03:160); admin retrieves the key in Intune via Devices > device > Monitor > Recovery keys > Show Recovery Key, Corporate-only, RBAC Remote tasks > Rotate FileVault key = Yes (03:180-181); user self-service via Company Portal "Get recovery key" (03:182). RESEARCH flags as [ASSUMED] (not in corpus): the user-side FileVault pre-boot recovery-key entry screen (Path A step 2), the entire macOS LAPS Intune-retrieval + login-window admin sequence (Path B), and the entire Apple-ID "Forgot Password?" login-window flow (Path C). Author these [ASSUMED] steps from verified macOS/Intune knowledge and label each path so it is self-consistent and complete. For Path C add a "confirm the user has an Apple ID associated; ADE/MDM-created accounts may not — use Path A or B instead" pre-check (Claude's discretion on wording); keep FileVault-key + LAPS as always-available fallbacks so the gated path is never a dead-end.

    obj-7 (EXECUTION-CRITICAL): author every recovery step as a PRE-LOGIN GUI action — FileVault pre-boot recovery-key screen, LAPS managed-admin login at the login window, Apple-ID reset at the login window. A locked-out user cannot open Terminal, so NO `app-sso platform -s`, `fdesetup`, or any CLI/Terminal command may appear in #37's Steps. All Terminal verification stays in #36 (the post-recovery re-registration runbook).

    SSPR clarification (D-03): at the explanation step, state inline that SSPR resets the Entra password only and does NOT reset the independent local password under Secure Enclave, then cross-link to depth using the #35:32/#36:34,45 inline-fact-plus-cross-link pattern — link to ../admin-setup-macos/03-configuration-profiles.md (Local Password Policy / Passcode) and ../admin-setup-macos/07-platform-sso-setup.md (Local password lifecycle and rotation) and/or ../admin-setup-macos/08-auth-methods-deep-dive.md. NOT a standalone top callout; NOT cross-link-only.

    #36 hand-off (D-02 / obj-17): end EACH of the three recovery paths with a single cross-link to [L1 #36](36-macos-secure-enclave-key.md) for the mandatory PSSO re-registration, and state explicitly that using the recovery key / resetting the local password destroys the Secure Enclave key binding — which is WHY #36 re-registration is mandatory. Do NOT duplicate #36's macOS-14 Repair / macOS-13 Company-Portal re-registration steps (Pitfall 3) — cross-link only. Escalation Criteria escalates to L2 #27 (../l2-runbooks/27-macos-sso-investigation.md), the same L2 #36 routes to at 36:86.
  </action>
  <acceptance_criteria>
    - File docs/l1-runbooks/37-macos-local-password-reset.md exists with YAML frontmatter containing all five keys: last_verified: 2026-06-29, review_by: 2026-09-29, applies_to: ADE, audience: L1, platform: macOS.
    - File contains a platform-gate blockquote, ## Prerequisites, ## Steps, ## Escalation Criteria, and ## Version History (single 2026-06-29 row).
    - ## Steps contains three labeled per-path sections in order: FileVault recovery key (primary) → macOS LAPS managed admin (secondary) → Apple ID (gated "where org policy allows").
    - The H1 heading produces NO double-hyphen anchor (no em-dash-plus-space in the H1).
    - An inline SSPR clarification fragment is present (e.g. "SSPR resets the Entra password" AND "not ... local password" / "does not reset the ... local password"), accompanied by a cross-link to 03-configuration-profiles.md and/or 07-platform-sso-setup.md / 08-auth-methods-deep-dive.md.
    - The string `](36-macos-secure-enclave-key.md)` appears at the end of each of the three recovery paths (>= 3 occurrences), each framed as the mandatory PSSO re-registration hand-off.
    - The file explicitly states that recovery-key use / local-password reset destroys the Secure Enclave key binding.
    - NO Terminal/CLI command appears in Steps: grep finds zero occurrences of `app-sso`, `fdesetup`, or fenced ```bash / ```sh / ```shell blocks in the file.
    - Escalation Criteria links to L2 #27 via `](../l2-runbooks/27-macos-sso-investigation.md)`.
  </acceptance_criteria>
  <verify>
    <automated>grep -Fq "audience: L1" docs/l1-runbooks/37-macos-local-password-reset.md && [ "$(grep -Fco '](36-macos-secure-enclave-key.md)' docs/l1-runbooks/37-macos-local-password-reset.md)" -ge 3 ] && ! grep -Eq 'app-sso|fdesetup' docs/l1-runbooks/37-macos-local-password-reset.md && grep -Fq "27-macos-sso-investigation.md" docs/l1-runbooks/37-macos-local-password-reset.md && echo PASS</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add the single reciprocal back-link line to L1 #36</name>
  <files>docs/l1-runbooks/36-macos-secure-enclave-key.md</files>
  <read_first>
    - docs/l1-runbooks/36-macos-secure-enclave-key.md (full file; specifically frontmatter 1-7, trigger paragraph at line 13, ## Prerequisites that follows, steps 1-4, re-registration paths 49-53, slugs)
    - 99-RESEARCH.md §"#36 Reciprocal Cross-Link" (407-422) and Freshness Stamp Reference (491-505)
    - 99-CONTEXT.md D-02 (reciprocal link ONLY — no restructuring) and execution caution obj-12 (preserve inbound #36 refs)
  </read_first>
  <action>
    Make ONLY two changes to docs/l1-runbooks/36-macos-secure-enclave-key.md (D-02 — reciprocal cross-link line plus the per-file freshness re-stamp; no restructuring):
    1. Insert one blockquote callout line after the trigger paragraph (after line 13, before ## Prerequisites) pointing a locked-out user to #37 first, e.g. a "> If the user cannot log in: ... use [macOS Local Password Recovery (L1 #37)](37-macos-local-password-reset.md) first to regain access, then return here for PSSO re-registration." (exact wording is Claude's discretion).
    2. Re-stamp this file's frontmatter per the file-level freshness convention: last_verified: 2026-06-29, review_by: 2026-09-29.

    Do NOT touch steps 1-4, the macOS-14 Repair / macOS-13 Company-Portal re-registration paths (36:49-53), the SE-destruction note, the L2-#27 escalation at 36:86, or any heading slug. No additional prose in any existing step (Pitfall 6).
  </action>
  <acceptance_criteria>
    - docs/l1-runbooks/36-macos-secure-enclave-key.md contains exactly one new reference `](37-macos-local-password-reset.md)`.
    - Frontmatter shows last_verified: 2026-06-29 and review_by: 2026-09-29.
    - `git diff` for #36 shows only the inserted reciprocal-link line(s) plus the two frontmatter date changes — no edits to steps 1-4, the re-registration paths (49-53), heading slugs, or the L2-#27 escalation line.
    - The existing string `27-macos-sso-investigation.md` and all existing heading anchors remain byte-identical (no slug churn).
  </acceptance_criteria>
  <verify>
    <automated>grep -Fq "](37-macos-local-password-reset.md)" docs/l1-runbooks/36-macos-secure-enclave-key.md && grep -Fq "review_by: 2026-09-29" docs/l1-runbooks/36-macos-secure-enclave-key.md && echo PASS</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → L1 operator | Runbook prose is the operator's authoritative instruction set; a wrong/unsafe step is followed literally |
| #37 → #36 seam | Cross-link routing carries the operator from access-recovery to mandatory PSSO re-registration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering (unsafe instruction) | #37 recovery steps | mitigate | obj-7 pre-login authoring is an acceptance criterion + automated grep blocks any `app-sso`/`fdesetup`/fenced shell block — a locked-out user can never be told to open Terminal |
| T-99-02 | Information disclosure (state drift) | #37 → #36 boundary | mitigate | SE-binding-destruction warning + mandatory #36 hand-off at end of every path stop the operator leaving the device in a half-recovered, PSSO-broken state |
| T-99-03 | Tampering (collateral edit) | L1 #36 byte-stability | mitigate | Task 2 limited to one reciprocal link + freshness stamp; git-diff acceptance criterion forbids touching steps/slugs/49-53 (preserves inbound refs, obj-12) |
| T-99-SC | Supply chain | npm/pip/cargo installs | accept | N/A — documentation-only phase, zero package installs |
</threat_model>

<verification>
- Both files committed together in a single Wave-1 commit BEFORE Plan 99-02 (navigation-last, D-04 HARD CONSTRAINT).
- `grep -Fc '](36-macos-secure-enclave-key.md)' docs/l1-runbooks/37-macos-local-password-reset.md` returns >= 3 (one hand-off per path).
- `grep -E 'app-sso|fdesetup' docs/l1-runbooks/37-macos-local-password-reset.md` returns nothing (no Terminal in recovery steps).
- `git diff --stat` for #36 shows a minimal change (reciprocal link + frontmatter only).
</verification>

<success_criteria>
- ROADMAP SC1 met: #37 documents all three recovery paths (FileVault key / LAPS / Apple ID where allowed).
- ROADMAP SC2 met: #37 states inline that SSPR resets Entra but not the local password under Secure Enclave.
- ROADMAP SC3 met: #37 documents the mandatory PSSO re-registration follow-up and cross-links to L1 #36 (and #36 back-links to #37).
- obj-7 met: all recovery steps are pre-login GUI; no Terminal in #37 Steps.
- D-02 met: #36 edited only for the reciprocal link + freshness stamp.
</success_criteria>

<output>
Create `.planning/phases/99-new-runbook-navigation-wiring/99-01-SUMMARY.md` when done. Confirm both files are committed before Plan 99-02 starts.
</output>
