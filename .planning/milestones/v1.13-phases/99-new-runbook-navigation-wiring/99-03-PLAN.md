---
phase: 99-new-runbook-navigation-wiring
plan: 03
type: execute
wave: 3
depends_on: ["99-02"]
files_modified:
  - .planning/phases/99-new-runbook-navigation-wiring/99-NEEDLE-SPEC.md
autonomous: true
requirements: [RUN-01]

must_haves:
  truths:
    - "99-NEEDLE-SPEC.md enumerates all 18 presence-only stable tokens/anchors (N1-N18 from RESEARCH) for the future check-phase-99.mjs — per Phase-100 hand-off directive (RUN-01)"
    - "The needle-spec records PRESENCE-only tokens and explicitly forbids needling dates (last_verified/review_by) or line numbers — per 97 D-02 convention"
    - "Phase 99 authors NO validator; the spec states check-phase-99.mjs is owned by Phase 100 (HARN-02 Atom 2)"
    - "The spec mirrors the 98-NEEDLE-SPEC.md format (token inventory table + precision notes + Phase-100 structure template)"
  artifacts:
    - path: ".planning/phases/99-new-runbook-navigation-wiring/99-NEEDLE-SPEC.md"
      provides: "Phase-100 hand-off needle inventory for check-phase-99.mjs"
      contains: "37-macos-local-password-reset.md"
  key_links:
    - from: ".planning/phases/99-new-runbook-navigation-wiring/99-NEEDLE-SPEC.md"
      to: "Phase 100 check-phase-99.mjs (future)"
      via: "presence-only token inventory"
      pattern: "PRESENCE"
---

<objective>
Author `99-NEEDLE-SPEC.md` recording the 18 presence-only stable tokens/anchors that Phase 100 will use to author check-phase-99.mjs (HARN-02 Atom 2). Phase 99 authors NO validator — this is a hand-off artifact only. Runs last (Wave 3) so the recorded tokens reflect the committed state of the runbook (99-01) and all five hubs (99-02).

Purpose: Satisfy the Phase-100 needle-spec hand-off deliverable so Phase 100 does not re-discover tokens.
Output: `.planning/phases/99-new-runbook-navigation-wiring/99-NEEDLE-SPEC.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-new-runbook-navigation-wiring/99-CONTEXT.md
@.planning/phases/99-new-runbook-navigation-wiring/99-RESEARCH.md
@.planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 99-NEEDLE-SPEC.md (18 presence-only tokens, Phase-100 hand-off)</name>
  <files>.planning/phases/99-new-runbook-navigation-wiring/99-NEEDLE-SPEC.md</files>
  <read_first>
    - .planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md (format to mirror: header note, stable-token inventory table, precision notes, existing-coverage section, Phase-100 structure template, presence-only/no-dates rule, CRLF normalization note)
    - 99-RESEARCH.md §"Needle-Spec Hand-Off" (462-488) — the N1-N18 table is the source of truth
    - 99-CONTEXT.md §"Phase-100 needle-spec hand-off" (112-120) and §Deferred (validator owned by Phase 100)
    - The six committed files from 99-01/99-02 to grep-confirm each token is present verbatim: docs/l1-runbooks/37-macos-local-password-reset.md, docs/l1-runbooks/36-macos-secure-enclave-key.md, docs/l1-runbooks/00-index.md, docs/index.md, docs/common-issues.md, docs/quick-ref-l1.md, docs/decision-trees/06-macos-triage.md
  </read_first>
  <action>
    Create .planning/phases/99-new-runbook-navigation-wiring/99-NEEDLE-SPEC.md mirroring 98-NEEDLE-SPEC.md structure. Open with the Phase-100 hand-off note stating Phase 99 authors NO validator (check-phase-99.mjs is built in Phase 100, HARN-02 Atom 2). Then a stable-token inventory table enumerating N1-N18 from 99-RESEARCH.md lines 462-488, with columns: Needle ID, Literal Token, File, Present (grep-confirmed verbatim against the committed files), Notes. Confirm each token actually exists by grep before marking it present (the runbook H1/section-heading slugs are Claude's discretion in 99-01, so record the actual minted anchor strings, not placeholders). Add a precision-notes section flagging substring/false-positive risks (e.g. N15/N5/N9/N10 all share `](37-macos-local-password-reset.md)` or `(l1-runbooks/37-...)` — note which file each belongs to and that a per-file structurally-anchored match is required; note N18 `](36-macos-secure-enclave-key.md)` appears in both #37 body and #36's own internal refs). Add the PRESENCE-only / never-needle-dates-or-line-numbers rule (per 97 D-02; freshness dates are time-bombs) and the CRLF-normalization note. Add a Phase-100 structure template (model: check-phase-97.mjs / check-phase-98.mjs — CHAIN_PHASES=[], CHAIN_SKIP=new Set([]), per-file DELIVERABLE constants, PRESENCE + CONTENT + SELF assertion classes) and a total-check count. Close with the standard footer (Phase / spec-recorded date 2026-06-29 / validator authored: Phase 100).
  </action>
  <acceptance_criteria>
    - .planning/phases/99-new-runbook-navigation-wiring/99-NEEDLE-SPEC.md exists.
    - It contains a token inventory enumerating at least 18 needle IDs (N1-N18) covering: the #37 file path, its three frontmatter keys (audience: L1 / platform: macOS / applies_to: ADE), the #37 inbound links in 00-index / docs/index / common-issues / quick-ref-l1 (x2) / 06-macos-triage (MACR9 + click + Routing row "Runbook 37"), the common-issues L2-#27 escalation link, the #36 reciprocal link, the SSPR clarification fragment, and the 3x #36 hand-off links in #37.
    - It explicitly states the needles are PRESENCE-only and that dates/line-numbers must NEVER be needled.
    - It explicitly states Phase 99 authors no validator and check-phase-99.mjs is a Phase-100 (HARN-02) deliverable.
    - It includes a Phase-100 check-phase-99.mjs structure template with CHAIN_PHASES=[] / CHAIN_SKIP=new Set([]).
    - No file under scripts/validation/ is created in this phase (grep confirms check-phase-99.mjs does not exist).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/99-new-runbook-navigation-wiring/99-NEEDLE-SPEC.md && grep -Fq "37-macos-local-password-reset.md" .planning/phases/99-new-runbook-navigation-wiring/99-NEEDLE-SPEC.md && grep -Fiq "PRESENCE" .planning/phases/99-new-runbook-navigation-wiring/99-NEEDLE-SPEC.md && [ ! -f scripts/validation/check-phase-99.mjs ] && echo PASS</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 99 → Phase 100 | The needle-spec is the sole hand-off contract; a wrong/forged token misdirects the future validator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-08 | Tampering (unverified token) | needle inventory | mitigate | every token grep-confirmed verbatim against the committed files before marked present; runs Wave 3 after 99-01/99-02 are committed |
| T-99-09 | Repudiation (date time-bomb) | needle selection | mitigate | spec hard-states PRESENCE-only, never needle dates/line-numbers (97 D-02) — prevents a self-invalidating future validator |
| T-99-10 | Elevation (scope creep) | validator authoring | mitigate | acceptance criterion asserts no scripts/validation/check-phase-99.mjs is created — validator stays a Phase-100 atom |
| T-99-SC | Supply chain | npm/pip/cargo installs | accept | N/A — documentation/planning-only, zero package installs |
</threat_model>

<verification>
- 99-NEEDLE-SPEC.md mirrors 98-NEEDLE-SPEC.md format and enumerates >= 18 grep-confirmed presence-only tokens.
- No validator file authored (scripts/validation/check-phase-99.mjs absent).
</verification>

<success_criteria>
- Phase-100 hand-off deliverable met: 99-NEEDLE-SPEC.md records all 18 tokens for check-phase-99.mjs.
- Phase 99 authors no validator (deferred to Phase 100 HARN-02 Atom 2).
</success_criteria>

<output>
Create `.planning/phases/99-new-runbook-navigation-wiring/99-03-SUMMARY.md` when done.
</output>
