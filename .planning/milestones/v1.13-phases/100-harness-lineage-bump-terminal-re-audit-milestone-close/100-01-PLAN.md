---
phase: 100-harness-lineage-bump-terminal-re-audit-milestone-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/v1.13-milestone-audit.mjs
  - scripts/validation/v1.13-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
autonomous: true
requirements: [HARN-01]
must_haves:
  truths:
    - "v1.13-milestone-audit.mjs runs default with the SAME C1-C16 PASS/0/0 result as v1.12 (C1-C16 inherited verbatim; NO C17 — Phase 100 has no corpus rename) — per ROADMAP SC#1 / HARN-01"
    - "v1.13-milestone-audit.mjs --self-test exits 0 with 9/9 passed (self-test preserved verbatim, no tests added)"
    - "v1.13-milestone-audit.mjs reads scripts/validation/v1.13-audit-allowlist.json at the line-79 functional sidecar repoint (the CI path-match needle); no stale v1.12-audit-allowlist reference remains"
    - "v1.13-audit-allowlist.json is a Path-A copy of the v1.12 sidecar with phase repointed to 100; all carried arrays byte-identical (c13_broken_link_allowlist exact-count, c16_missing_endpoint_exemptions: [])"
    - "regenerate-supervision-pins.mjs gains a BASELINE_17 comment block after line 459 closing the BASELINE_16 v1.12 carry-over; the BASELINE_9 line-coord array is byte-unchanged"
    - "Atom 1 lands as ONE indivisible commit touching EXACTLY 3 files (v1.13-milestone-audit.mjs + v1.13-audit-allowlist.json + regenerate-supervision-pins.mjs) — V112 does NOT ride Atom 1 (rides Atom 2 per HARN-02)"
  artifacts:
    - path: "scripts/validation/v1.13-milestone-audit.mjs"
      provides: "11th Path-A milestone-audit harness (lineage v1.4→v1.13; C1-C16 verbatim)"
      contains: "v1.13-audit-allowlist.json"
    - path: "scripts/validation/v1.13-audit-allowlist.json"
      provides: "v1.13 sidecar allowlist (Path-A from v1.12; phase repointed to 100)"
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "BASELINE_17 freshness comment (audit-trail only; BASELINE_9 array unchanged)"
      contains: "BASELINE_17"
  key_links:
    - from: "scripts/validation/v1.13-milestone-audit.mjs"
      to: "scripts/validation/v1.13-audit-allowlist.json"
      via: "readFile sidecar repoint at line 79"
      pattern: "v1\\.13-audit-allowlist\\.json"
---

<objective>
Ship Plan 100-01: HARN-01 Atom 1 — the harness-core Path-A bump (v1.12 → v1.13), the 11th milestone-audit in the lineage v1.4 → v1.4.1 → … → v1.12 → **v1.13**.

This plan produces ONE commit (ATOM 1, HARN-01, INDIVISIBLE, exactly 3 files):
- `v1.13-milestone-audit.mjs` — Path-A copy of `v1.12-milestone-audit.mjs`, relabel ONLY lines 2/4/35/79 (per 100-RESEARCH §"Exact 4-Line Relabel"); C1-C16 inherited verbatim; self-test 9/9 preserved; NO C17.
- `v1.13-audit-allowlist.json` — Path-A copy of `v1.12-audit-allowlist.json`, repoint `phase` to 100 + `generated` to authoring date; all arrays carried verbatim.
- BASELINE_17 freshness comment appended in `regenerate-supervision-pins.mjs` after line 459, mirroring the BASELINE_16 format.

Purpose: Ship the v1.13 harness core as a clean revertable atom. **V112 does NOT ride Atom 1** — it rides Atom 2 (Plan 100-02) per ROADMAP SC#2 / HARN-02, mirroring the v1.12 Phase 95 V111-rides-Atom-2 divergence. Atom 1 is exactly 3 files.
Output: `v1.13-milestone-audit.mjs`, `v1.13-audit-allowlist.json`, BASELINE_17 comment in `regenerate-supervision-pins.mjs`; 1 commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-CONTEXT.md
@.planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-Atom-1 anchor + author v1.13-milestone-audit.mjs (Path-A, 4-line relabel) + v1.13-audit-allowlist.json (sidecar repoint)</name>
  <files>scripts/validation/v1.13-milestone-audit.mjs, scripts/validation/v1.13-audit-allowlist.json</files>
  <read_first>
    - scripts/validation/v1.12-milestone-audit.mjs (PATH-A SOURCE for v1.13-milestone-audit.mjs — copy verbatim, relabel ONLY lines 2/4/35/79)
    - scripts/validation/v1.12-audit-allowlist.json (PATH-A SOURCE for v1.13-audit-allowlist.json — copy verbatim, change ONLY the header fields)
    - .planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-RESEARCH.md (§"Atom 1 Delta Map" — the exact 4-line relabel table for lines 2/4/35/79 with literal replacement text; §"v1.13-audit-allowlist.json Structure" — the 3 header fields to change + the carried-array list)
  </read_first>
  <action>
    First capture the pre-Atom-1 anchor SHA: run `git rev-parse --short HEAD` and record it (this is the current/known-PAST HEAD before Atom 1 lands — it is BOTH the BASELINE_17 anchor used in Task 2 AND the base for the predecessor-byte-unchanged gate in Plan 100-04). Store it in the plan SUMMARY.

    **File 1 — `v1.13-milestone-audit.mjs`:** Copy `v1.12-milestone-audit.mjs` verbatim, then apply EXACTLY the 4 relabel sites from 100-RESEARCH §"Exact 4-Line Relabel for v1.13-milestone-audit.mjs":
    - Line 2 (header lineage): replace with the v1.13 header — `// v1.13 Milestone Audit Harness (Path A copy of v1.12; lineage v1.4 → v1.4.1 → v1.5 → v1.6 → v1.7 → v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13; C1-C16 inherited verbatim)`.
    - Line 4 (sidecar comment): repoint to `scripts/validation/v1.13-audit-allowlist.json` … `v1.13 Path-A from v1.12 with c13_rotting_external carried for v1.13 per Phase 100 close-state` (use the exact line-4 replacement text in 100-RESEARCH).
    - Line 35 (Usage): `// Usage: node scripts/validation/v1.13-milestone-audit.mjs [--verbose] [--self-test]`.
    - Line 79 (functional sidecar repoint — the line the CI path-match job greps): `const raw = readFile('scripts/validation/v1.13-audit-allowlist.json');`.
    DO NOT touch anything else: the `checks` array stays exactly C1-C16 (NO C17 — Phase 100 has no corpus rename per the locked Path-A doctrine), the self-test block stays 9/9, all helpers/runner-loop verbatim. Default run MUST produce the same PASS/0/0 result as v1.12 (capture the exact PASS count).

    **File 2 — `v1.13-audit-allowlist.json`:** Copy `v1.12-audit-allowlist.json` verbatim, then change ONLY the header fields per 100-RESEARCH:
    - `generated`: set to the authoring date (today).
    - `phase`: set to `100` (match the v1.12 sidecar's `phase`-field form — if v1.12 used the long slug, use `100-harness-lineage-bump-terminal-re-audit-milestone-close`; if it used the bare number, use `100`; mirror the v1.12 field type exactly).
    - `schema_version`: carry the v1.12 value verbatim.
    Carry forward byte-identical: all arrays — `supervision_exemptions`, `c13_broken_link_allowlist` (exact entry count unchanged), `cope_banned_phrases`, `c7_knox_allowlist`, `c9_exemptions`, `c11_ops_exemptions`, `c13_rotting_external`, `c16_missing_endpoint_exemptions: []`. Changing any carried count breaks the C-checks.
  </action>
  <verify>
    <automated>node scripts/validation/v1.13-milestone-audit.mjs --self-test && echo "---DEFAULT---" && node scripts/validation/v1.13-milestone-audit.mjs | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/v1.13-milestone-audit.mjs --self-test` exits 0 with "9 passed, 0 failed" (9/9 preserved).
    - `node scripts/validation/v1.13-milestone-audit.mjs` (default) exits 0 and reports the SAME PASS / 0 FAIL / 0 SKIP result as v1.12 (C1-C16; capture and record the exact PASS count in SUMMARY).
    - `grep -v '^#' scripts/validation/v1.13-milestone-audit.mjs | grep -c "v1.13-audit-allowlist.json"` >= 1 (line-79 functional repoint present); `grep -c "v1.12-audit-allowlist" scripts/validation/v1.13-milestone-audit.mjs` == 0 (no stale v1.12 sidecar reference).
    - `node -e "const j=require('./scripts/validation/v1.13-audit-allowlist.json'); if(String(j.phase).indexOf('100')===-1)process.exit(1)"` exits 0 (valid JSON, phase repointed to 100).
    - The pre-Atom-1 anchor SHA is captured via `git rev-parse --short HEAD` and recorded (for BASELINE_17 in Task 2 and the byte-unchanged gate base in Plan 100-04).
  </acceptance_criteria>
  <done>v1.13-milestone-audit.mjs authored (4-line relabel only; self-test 9/9; default C1-C16 PASS/0/0); v1.13-audit-allowlist.json repointed to phase 100; sidecar repoint confirmed; pre-Atom-1 anchor SHA recorded. NOT yet committed (commit in Task 2 with BASELINE_17 as one atom).</done>
</task>

<task type="auto">
  <name>Task 2: Insert BASELINE_17 comment in regenerate-supervision-pins.mjs + commit Atom 1 (exactly 3 files)</name>
  <files>scripts/validation/regenerate-supervision-pins.mjs</files>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs (the file being MODIFIED — the BASELINE_16 comment block at lines 453-459 is the mirror template; the `const BASELINE_9 = [` array immediately after line 459 must NOT change)
    - .planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-RESEARCH.md (§"BASELINE_17 Comment Block" — exact 7-line format mirroring BASELINE_16, insertion point after line 459, the `<HEAD-SHA-at-Atom-1>` anchor rule)
  </read_first>
  <action>
    Insert a 7-line BASELINE_17 comment block immediately AFTER line 459 (the existing BASELINE_16 forward-pointer line) and BEFORE `const BASELINE_9 = [`, mirroring the BASELINE_16 block format exactly (per 100-RESEARCH §"BASELINE_17 Comment Block"). The block records:
    - `BASELINE_17 refreshed <today> (Phase 100 Plan 100-01): closes BASELINE_16 v1.12 carry-over per HARN-01 contract (REQUIREMENTS.md + ROADMAP.md Phase 100 SC#1)`;
    - `v1.13 line positions verified against HEAD <pre-Atom-1 short SHA from Task 1>` (REPLACE the `<HEAD-SHA-at-Atom-1>` placeholder with the real anchor SHA captured in Task 1 — do NOT leave a literal placeholder, and do NOT anchor to the future Phase-100 close SHA);
    - `BASELINE_9 entries above remain unchanged -- Phase 100 does NOT alter the line-coord array`;
    - resolution path mentioning BASELINE_18 at the next milestone close (`... -> v1.12 -> BASELINE_16 -> v1.13 -> BASELINE_17`).
    Before writing the SHA, verify BASELINE_9 line coordinates are still valid for the v1.13 corpus (no v1.13 phase touched `_glossary-android.md` — all v1.13 edits are macOS-only; spot-confirm via grep if uncertain). DO NOT alter the BASELINE_9 array (comment-additions only).

    Then stage all 3 Atom-1 files (`v1.13-milestone-audit.mjs` + `v1.13-audit-allowlist.json` from Task 1 + `regenerate-supervision-pins.mjs`) and commit as ONE indivisible atom. V112 is NOT added here (it rides Atom 2 in Plan 100-02). Commit message: `feat(100-01): v1.13 harness-core Path-A — HARN-01 (atomic SC#1 Atom 1)`.
  </action>
  <verify>
    <automated>grep -c "BASELINE_17" scripts/validation/regenerate-supervision-pins.mjs && grep -c "{Wave-1-commit-SHA}\|<HEAD-SHA-at-Atom-1>" scripts/validation/regenerate-supervision-pins.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BASELINE_17" scripts/validation/regenerate-supervision-pins.mjs` >= 1; the block contains a real 7-char SHA anchor and NO literal placeholder (`grep -c "{Wave-1-commit-SHA}\|<HEAD-SHA-at-Atom-1>" scripts/validation/regenerate-supervision-pins.mjs` == 0).
    - `git diff` on `regenerate-supervision-pins.mjs` shows comment-additions only; the BASELINE_9 line-coord array is byte-unchanged.
    - COMMIT message is exactly: `feat(100-01): v1.13 harness-core Path-A — HARN-01 (atomic SC#1 Atom 1)`.
    - The commit touches EXACTLY 3 files: `git show --stat HEAD --format=` lists only `v1.13-milestone-audit.mjs`, `v1.13-audit-allowlist.json`, `regenerate-supervision-pins.mjs`. `_lib/frozen-at-close.mjs` is NOT touched (V112 rides Atom 2).
  </acceptance_criteria>
  <done>BASELINE_17 comment present with a real past-SHA anchor; BASELINE_9 array unchanged; Atom 1 (exactly 3 files) committed; V112 deliberately absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — local CLI tooling) | Atom 1 is tooling-ONLY: a validation `.mjs` harness + sidecar JSON + a comment-only edit to a generator script. No user input, no network endpoints, no auth, no secrets, no runtime data. |

## STRIDE Threat Register

ASVS L1 review found **no high-severity items** — the realistic threat surface for this close/tooling phase is harness-integrity (a false-green harness), not web/app threats.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01-T | Tampering | Atom 1 silently mutating C1-C16 / self-test count (false-green harness) | mitigate | Acceptance gate asserts self-test 9/9 + default C1-C16 PASS/0/0 + carried allowlist counts unchanged; any drift fails the gate. |
| T-100-01-I | Integrity | Stale `v1.12-audit-allowlist` reference left in the v1.13 harness (sidecar points at wrong file) | mitigate | Acceptance gate asserts `grep -c "v1.12-audit-allowlist"` == 0 and the v1.13 sidecar repoint is present (filtered grep, comment-safe). |
| T-100-01-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | NO package installs (pure Path-A copy of existing in-tree scripts; no new deps). RESEARCH confirms no install tasks → no Package Legitimacy Gate required. |
</threat_model>

<verification>
- `node scripts/validation/v1.13-milestone-audit.mjs --self-test` → 9 passed, 0 failed (exit 0).
- `node scripts/validation/v1.13-milestone-audit.mjs` → C1-C16 PASS / 0 FAIL / 0 SKIP (exit 0; same as v1.12).
- v1.13 sidecar parses with `phase` repointed to 100; no stale v1.12-audit-allowlist reference.
- BASELINE_17 comment present with a real past-SHA anchor; BASELINE_9 array byte-unchanged.
- Atom 1 = exactly 3 files, one commit; V112 absent.
</verification>

<success_criteria>
- ROADMAP SC#1 satisfied: Atom 1 ships as ONE indivisible commit — `v1.13-milestone-audit.mjs` (Path-A, C1-C16) + `v1.13-audit-allowlist.json` + BASELINE_17 in `regenerate-supervision-pins.mjs`.
- self-test 9/9 and default C1-C16 PASS/0/0 both green; sidecar repointed; BASELINE_9 array unchanged.
- Atom 1 = exactly 3 files; V112 deliberately absent (rides Atom 2 per locked divergence); pre-Atom-1 anchor SHA recorded for downstream gates.
</success_criteria>

<output>
Create `.planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-01-SUMMARY.md` when done. Record: the Atom-1 COMMIT SHA, the captured pre-Atom-1 anchor SHA (BASELINE_17 anchor AND the Plan 100-04 byte-unchanged gate base), the self-test/default PASS counts observed, and confirmation V112 was deliberately not added.
</output>
</content>
