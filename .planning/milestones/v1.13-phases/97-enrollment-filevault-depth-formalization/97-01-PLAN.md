---
phase: 97-enrollment-filevault-depth-formalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-macos/02-enrollment-profile.md
  - docs/admin-setup-macos/03-configuration-profiles.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/97-enrollment-filevault-depth-formalization/97-NEEDLE-SPEC.md
autonomous: true
requirements: [DEP-01, DEP-02]

must_haves:
  truths:
    - "Guide 02 carries a 2026-06-28 version-history row formalizing the Account Settings section under DEP-01 (newest-at-top, bare table, Author column `--`)"
    - "Guide 03 carries a 2026-06-28 version-history row formalizing the FileVault and Local Password Policy sections under DEP-02 (newest-at-top, bare table, Author column `--`)"
    - "All 13 stable needle tokens (5 DEP-01 in guide 02, 8 DEP-02 in guide 03) remain present verbatim — formalize-only, no content rewrite"
    - "Frontmatter last_verified/review_by stay at 2026-06-27 / 2026-09-27 in BOTH guides — the bounded 4-claim spot-verify found no corrections, so no bump"
    - "REQUIREMENTS.md marks DEP-01 and DEP-02 as [x] and flips both traceability rows from Pending to Complete"
    - "The Phase-100 needle-spec hand-off (16-check spec) is recorded in 97-NEEDLE-SPEC.md so check-phase-97.mjs can be authored without re-discovery"
    - "check-phase-95.mjs (current chain apex) still passes — no regression to the existing guide-03 E7 needle"
  artifacts:
    - path: "docs/admin-setup-macos/02-enrollment-profile.md"
      provides: "DEP-01 Account Settings depth, formalized with a 2026-06-28 version-history row"
      contains: "Formalized Account Settings section under DEP-01"
    - path: "docs/admin-setup-macos/03-configuration-profiles.md"
      provides: "DEP-02 FileVault + Local Password Policy depth, formalized with a 2026-06-28 version-history row"
      contains: "Formalized FileVault (Full Disk Encryption) and Local Password Policy (Passcode) sections under DEP-02"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DEP-01/DEP-02 closure — checkbox [x] + traceability Complete"
      contains: "| DEP-01 | Phase 97 | Complete |"
    - path: ".planning/phases/97-enrollment-filevault-depth-formalization/97-NEEDLE-SPEC.md"
      provides: "Phase-100 hand-off: the 16-check check-phase-97.mjs needle-spec"
      contains: "V-97-CONTENT-DEP02-N8"
  key_links:
    - from: "docs/admin-setup-macos/02-enrollment-profile.md"
      to: "97-NEEDLE-SPEC.md"
      via: "stable DEP-01 needle tokens recorded for the Phase-100 validator"
      pattern: "Non Platform SSO Accounts"
    - from: "docs/admin-setup-macos/03-configuration-profiles.md"
      to: "97-NEEDLE-SPEC.md"
      via: "stable DEP-02 needle tokens recorded for the Phase-100 validator"
      pattern: "dontAllowFDEDisable"
    - from: ".planning/REQUIREMENTS.md"
      to: "DEP-01/DEP-02"
      via: "traceability table flip to Complete"
      pattern: "DEP-0[12] \\| Phase 97 \\| Complete"
---

<objective>
Formalize — do not author. The Account Settings depth in guide 02 (DEP-01) and the FileVault / Local Password Policy depth in guide 03 (DEP-02) were already written and committed during the 2026-06-27 live session. This plan binds that already-present content to requirement IDs, records the one-time formalization in each guide's version-history table, confirms four bounded factual claims (all already verified correct in 97-RESEARCH.md), flips DEP-01/DEP-02 to Complete in REQUIREMENTS.md, and records the Phase-100 needle-spec hand-off.

Purpose: Bring live-session depth additions under requirement IDs + freshness convention + durable assertable anchors so the Phase-100 chain validators CAN assert their presence — without re-authoring content, bumping frontmatter, or pre-authoring any validator.

Output: Two new version-history rows (one per guide), a flipped REQUIREMENTS.md, and a new 97-NEEDLE-SPEC.md hand-off file.

LOCKED scope (do NOT violate — from 97-CONTEXT.md D-01..D-04):
- D-01: Freshness = file-level frontmatter (UNCHANGED) + one dated version-history row per guide. NO per-section stamp pairs. Do NOT bump frontmatter (spot-verify found no corrections).
- D-02: Do NOT author scripts/validation/check-phase-97.mjs or touch any chain-apex / CHAIN_PHASES. Only record the needle-spec for Phase 100.
- D-03: Needle-spec is recorded now, implemented in Phase 100. Ensure the 13 tokens remain present.
- D-04: Formalize-only + bounded spot-verify of EXACTLY 4 claims (all VERIFIED in research — confirmation only, no content edit). No broader re-audit, no re-authoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/97-enrollment-filevault-depth-formalization/97-CONTEXT.md
@.planning/phases/97-enrollment-filevault-depth-formalization/97-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guide 02 — confirm spot-verify claim 2 + add DEP-01 formalization version-history row</name>
  <files>docs/admin-setup-macos/02-enrollment-profile.md</files>
  <read_first>
    - docs/admin-setup-macos/02-enrollment-profile.md (full file — see current state: frontmatter lines 2-3, Account Settings section ~lines 61-102, version-history table lines 186-193)
    - .planning/phases/97-enrollment-filevault-depth-formalization/97-CONTEXT.md (D-01 row wording/placement; D-04 spot-verify scope)
    - .planning/phases/97-enrollment-filevault-depth-formalization/97-RESEARCH.md (§ "Current File State → Guide 02" for exact row text; § "Bounded Spot-Verify Results → Claim 2")
  </read_first>
  <action>
    Bounded spot-verify (D-04, confirmation only): confirm the existing text at guide 02 ~line 71 still contains the literal phrase `rotated every 6 months by default` (macOS LAPS recovery-key rotation). Research already verified this claim CORRECT against Microsoft Learn (learn.microsoft.com/en-us/intune/device-security/laps/setup-macos). This is a no-op confirmation — make NO content edit. If (and only if) the phrase is absent or contradicts the verified value, STOP and surface the discrepancy rather than silently editing.

    Then insert ONE new version-history row at the TOP of the existing table (immediately after the separator line `|------|--------|--------|` at line 187, becoming the new line 188, pushing the existing 2026-06-27 rows down). Use the exact row from 97-RESEARCH.md § "Current File State → Guide 02": `| 2026-06-28 | Formalized Account Settings section under DEP-01; bounded spot-verify of 4 factual claims confirmed correct against Microsoft Learn (2026-06-28) | -- |`. Per D-01 the wording is "formalized" (not "rewrote"/"added"); the Author column is `--`; there is NO "Version History" heading (bare table after `---`).

    Do NOT touch frontmatter lines 2-3 — `last_verified: 2026-06-27` / `review_by: 2026-09-27` stay UNCHANGED (D-01: bump only on a substantive content correction; the spot-verify found none). Do NOT edit any Account Settings body content (D-04 formalize-only).
  </action>
  <acceptance_criteria>
    - `grep -F "rotated every 6 months by default" docs/admin-setup-macos/02-enrollment-profile.md` returns a match (spot-verify claim 2 still present, unchanged).
    - `grep -c "Formalized Account Settings section under DEP-01" docs/admin-setup-macos/02-enrollment-profile.md` returns exactly 1 (new row added, once).
    - The `2026-06-28` formalization row appears ABOVE the first `2026-06-27` row in the version-history table (newest-at-top; verify by reading lines 186-194).
    - Frontmatter unchanged: `grep -n "last_verified: 2026-06-27" docs/admin-setup-macos/02-enrollment-profile.md` and `grep -n "review_by: 2026-09-27" docs/admin-setup-macos/02-enrollment-profile.md` both still match on lines 2 and 3.
    - All 5 DEP-01 needle tokens still present: each of `grep -F "Non Platform SSO Accounts"`, `grep -F "Restrict editing"`, `grep -F "Prefill account info"`, `grep -F "{{partialUPN}}"`, `grep -F "{{username}}"` on the file returns a match.
    - No `### Version History` (or any heading) was added before the table: `grep -ci "version history" docs/admin-setup-macos/02-enrollment-profile.md` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Formalized Account Settings section under DEP-01" docs/admin-setup-macos/02-enrollment-profile.md | grep -qx 1 && grep -qF "rotated every 6 months by default" docs/admin-setup-macos/02-enrollment-profile.md && grep -qF "{{partialUPN}}" docs/admin-setup-macos/02-enrollment-profile.md && grep -q "last_verified: 2026-06-27" docs/admin-setup-macos/02-enrollment-profile.md && echo PASS</automated>
  </verify>
  <done>Guide 02 has exactly one new 2026-06-28 DEP-01 formalization row at the top of the version-history table; all 5 DEP-01 needle tokens and the LAPS spot-verify phrase remain present; frontmatter is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Guide 03 — confirm spot-verify claims 1/3/4 + add DEP-02 formalization version-history row</name>
  <files>docs/admin-setup-macos/03-configuration-profiles.md</files>
  <read_first>
    - docs/admin-setup-macos/03-configuration-profiles.md (full file — see current state: frontmatter lines 2-3, Local Password Policy section ~lines 98-121, FileVault section ~lines 123-196, version-history table lines 288-297)
    - .planning/phases/97-enrollment-filevault-depth-formalization/97-CONTEXT.md (D-01 row wording/placement; D-04 spot-verify scope)
    - .planning/phases/97-enrollment-filevault-depth-formalization/97-RESEARCH.md (§ "Current File State → Guide 03" for exact row text; § "Bounded Spot-Verify Results → Claims 1, 3, 4")
  </read_first>
  <action>
    Bounded spot-verify (D-04, confirmation only): confirm the following three existing claims are still present unchanged — claim 1 `XTS-AES 128` (FileVault cipher, ~line 137); claim 3 `-2016341107` (escrow error code, ~line 189); claim 4 the macOS 14.4 Setup-Assistant admin-role caveat (~line 174, look for `macOS 14.4`). Research already verified all three CORRECT against Microsoft Learn. Make NO content edit. If any phrase is absent or contradicts the verified value, STOP and surface the discrepancy rather than silently editing.

    Then insert ONE new version-history row at the TOP of the existing table (immediately after the separator line `|------|--------|--------|` at line 289, becoming the new line 290, pushing the existing 2026-06-27 rows down). Use the exact row from 97-RESEARCH.md § "Current File State → Guide 03": `| 2026-06-28 | Formalized FileVault (Full Disk Encryption) and Local Password Policy (Passcode) sections under DEP-02; bounded spot-verify of 4 factual claims confirmed correct against Microsoft Learn (2026-06-28) | -- |`. Per D-01 the wording is "formalized"; Author column `--`; NO "Version History" heading.

    Do NOT touch frontmatter lines 2-3 — stay at `last_verified: 2026-06-27` / `review_by: 2026-09-27` (D-01). Do NOT edit any FileVault, Recovery Key Escrow, or Local Password Policy body content (D-04 formalize-only). Do NOT add, remove, or alter the existing `](07-platform-sso-setup.md)` link asserted by check-phase-81 E7.
  </action>
  <acceptance_criteria>
    - All three spot-verify claims still present: `grep -F "XTS-AES 128"`, `grep -F "-2016341107"`, and `grep -F "macOS 14.4"` on the file each return a match.
    - `grep -c "Formalized FileVault (Full Disk Encryption) and Local Password Policy (Passcode) sections under DEP-02" docs/admin-setup-macos/03-configuration-profiles.md` returns exactly 1.
    - The `2026-06-28` formalization row appears ABOVE the first `2026-06-27` row (verify by reading lines 288-298).
    - Frontmatter unchanged: `grep -n "last_verified: 2026-06-27"` matches line 2 and `grep -n "review_by: 2026-09-27"` matches line 3.
    - All 8 DEP-02 needle tokens still present: each of `grep -F "FileVault Options"`, `grep -F "Recovery Key Escrow"`, `grep -F "| Defer |"`, `grep -F "dontAllowFDEDisable"`, `grep -F "DestroyFVKeyOnStandby"`, `grep -F "Recovery Key Rotation In Months"`, `grep -F "Local Password Policy"`, `grep -F "-2016341107"` returns a match.
    - check-phase-81 E7 anchor intact: `grep -F "](07-platform-sso-setup.md)" docs/admin-setup-macos/03-configuration-profiles.md` returns a match.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Formalized FileVault (Full Disk Encryption) and Local Password Policy (Passcode) sections under DEP-02" docs/admin-setup-macos/03-configuration-profiles.md | grep -qx 1 && grep -qF "XTS-AES 128" docs/admin-setup-macos/03-configuration-profiles.md && grep -qF "dontAllowFDEDisable" docs/admin-setup-macos/03-configuration-profiles.md && grep -qF "| Defer |" docs/admin-setup-macos/03-configuration-profiles.md && grep -qF "](07-platform-sso-setup.md)" docs/admin-setup-macos/03-configuration-profiles.md && grep -q "last_verified: 2026-06-27" docs/admin-setup-macos/03-configuration-profiles.md && echo PASS</automated>
  </verify>
  <done>Guide 03 has exactly one new 2026-06-28 DEP-02 formalization row at the top of the version-history table; all 8 DEP-02 needle tokens, the three spot-verify phrases, and the E7 link remain present; frontmatter is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Flip DEP-01/DEP-02 to Complete in REQUIREMENTS.md + record the Phase-100 needle-spec hand-off</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/97-enrollment-filevault-depth-formalization/97-NEEDLE-SPEC.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (DEP-01 line 26, DEP-02 line 27; Traceability table lines 57-76 with rows `| DEP-01 | Phase 97 | Pending |` line 65 and `| DEP-02 | Phase 97 | Pending |` line 66)
    - .planning/phases/97-enrollment-filevault-depth-formalization/97-RESEARCH.md (§ "Requirements Binding Convention"; § "Stable-Token Needle Inventory"; § "Phase-100 Needle-Spec Hand-Off" — the 16-check table to copy verbatim)
    - .planning/phases/97-enrollment-filevault-depth-formalization/97-CONTEXT.md (D-02: validator authoring is deferred to Phase 100; this task only RECORDS the spec)
  </read_first>
  <action>
    Requirements closure (mirror Phase 96 ACC-*/GLOS-01 two-touch pattern): in `.planning/REQUIREMENTS.md`, change the DEP-01 checkbox from `- [ ] **DEP-01**:` to `- [x] **DEP-01**:` (line 26) and DEP-02 from `- [ ] **DEP-02**:` to `- [x] **DEP-02**:` (line 27). In the Traceability table, change `| DEP-01 | Phase 97 | Pending |` to `| DEP-01 | Phase 97 | Complete |` (line 65) and `| DEP-02 | Phase 97 | Pending |` to `| DEP-02 | Phase 97 | Complete |` (line 66). Do not touch any other requirement's checkbox or status.

    Needle-spec hand-off (D-02 / D-03): create `.planning/phases/97-enrollment-filevault-depth-formalization/97-NEEDLE-SPEC.md`. Copy verbatim, from 97-RESEARCH.md, the § "Stable-Token Needle Inventory for Phase-100 Hand-Off" tables (both guide-02 and guide-03 needle tables + the precision notes) AND the § "Phase-100 Needle-Spec Hand-Off" content (the check-phase-97.mjs structure template, the 16-check assertion table V-97-PRESENCE-02 through V-97-SELF, and the CRLF/no-freshness/no-allowlist notes). Add a one-line header stating this file is the Phase-100 hand-off for authoring check-phase-97.mjs as part of the indivisible Atom 2 commit (HARN-02), and that Phase 97 itself authors NO validator (D-02 LOCKED). Do NOT create scripts/validation/check-phase-97.mjs and do NOT touch any chain-apex or CHAIN_PHASES — those are Phase-100 deliverables.
  </action>
  <acceptance_criteria>
    - `grep -F "[x] **DEP-01**" .planning/REQUIREMENTS.md` returns a match; `grep -F "[ ] **DEP-01**" .planning/REQUIREMENTS.md` returns NO match.
    - `grep -F "[x] **DEP-02**" .planning/REQUIREMENTS.md` returns a match; `grep -F "[ ] **DEP-02**" .planning/REQUIREMENTS.md` returns NO match.
    - `grep -F "| DEP-01 | Phase 97 | Complete |" .planning/REQUIREMENTS.md` and `grep -F "| DEP-02 | Phase 97 | Complete |" .planning/REQUIREMENTS.md` each return a match; no `| DEP-0[12] | Phase 97 | Pending |` rows remain.
    - File `.planning/phases/97-enrollment-filevault-depth-formalization/97-NEEDLE-SPEC.md` exists and is non-empty.
    - The hand-off records all 16 checks: `grep -c "V-97-CONTENT-DEP01-N" 97-NEEDLE-SPEC.md` >= 5, `grep -c "V-97-CONTENT-DEP02-N" 97-NEEDLE-SPEC.md` >= 8, and `grep -F "V-97-SELF"` returns a match.
    - No validator was authored: `test ! -f scripts/validation/check-phase-97.mjs` (the file must NOT exist).
  </acceptance_criteria>
  <verify>
    <automated>grep -qF "[x] **DEP-01**" .planning/REQUIREMENTS.md && grep -qF "[x] **DEP-02**" .planning/REQUIREMENTS.md && grep -qF "| DEP-01 | Phase 97 | Complete |" .planning/REQUIREMENTS.md && grep -qF "| DEP-02 | Phase 97 | Complete |" .planning/REQUIREMENTS.md && test -s .planning/phases/97-enrollment-filevault-depth-formalization/97-NEEDLE-SPEC.md && test ! -f scripts/validation/check-phase-97.mjs && echo PASS</automated>
  </verify>
  <done>REQUIREMENTS.md shows DEP-01 and DEP-02 as [x] and Complete in the traceability table; 97-NEEDLE-SPEC.md records the full 16-check Phase-100 hand-off; no validator was authored in this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only phase. No executable surface, no user input, no authentication, no network or data handling. The only artifacts are committed markdown (two version-history rows, one hand-off note) and a REQUIREMENTS.md checkbox/traceability edit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | n/a | Markdown doc edits + REQUIREMENTS.md flip | accept | No applicable threats at ASVS L1 — docs-only, no executable/input/network surface. Attack surface = none. |

No package-manager installs occur in this phase (no npm/pip/cargo), so the T-{phase}-SC supply-chain row is not applicable. Block-on-high is satisfied trivially: zero high threats.
</threat_model>

<verification>
- Both guides carry exactly one new 2026-06-28 formalization version-history row, newest-at-top, Author `--`, no heading.
- Frontmatter in both guides unchanged at 2026-06-27 / 2026-09-27 (no spot-verify correction → no bump).
- All 13 stable needle tokens (5 in guide 02, 8 in guide 03) remain present verbatim.
- REQUIREMENTS.md: DEP-01 and DEP-02 are `[x]` and traceability is Complete.
- 97-NEEDLE-SPEC.md exists with the full 16-check Phase-100 hand-off.
- No validator authored: scripts/validation/check-phase-97.mjs does NOT exist.
- Chain regression gate: `node scripts/validation/check-phase-95.mjs` still passes (confirms the existing guide-03 E7 needle and the rest of the [48..94] chain are intact).
</verification>

<success_criteria>
- ROADMAP Phase 97 SC#1 satisfied: guide 02 documents the Account Settings section, formalized under DEP-01 with file-level freshness (frontmatter + new version-history row) — per the D-01 SC reconciliation, file-level stamps + version-history row satisfy the "per-section freshness stamps" intent.
- ROADMAP Phase 97 SC#2 satisfied: guide 03 documents FileVault with all three sub-payloads, required `Defer`, Setup-Assistant enforcement, the recovery-key-escrow verification procedure, and the assignment target, formalized under DEP-02 with file-level freshness.
- ROADMAP Phase 97 SC#3 satisfied: guide 03 includes the Local Password Policy (Passcode) section (non-expiring best-practice + compliance expiration alternative), formalized with file-level freshness.
- ROADMAP Phase 97 SC#4 satisfied: both guides are committed with durable assertable anchors (the 13 stable tokens) intact and the Phase-100 needle-spec recorded, so chain validators CAN assert presence at Phase 100.
- DEP-01 and DEP-02 flipped to Complete in REQUIREMENTS.md.
</success_criteria>

<output>
Create `.planning/phases/97-enrollment-filevault-depth-formalization/97-01-SUMMARY.md` when done.
</output>
