---
phase: 93-harness-lineage-bump-terminal-re-audit-milestone-close
plan: 03
type: execute
wave: 3
depends_on: ["93-02"]
files_modified:
  - .planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-03-AUDIT-RESULTS.md
autonomous: false
requirements: [HARN-03]
must_haves:
  truths:
    - "Atom 2 is confirmed on origin/master (pre-flight ordering gate) before any axis runs"
    - "Axis 1 (local physical independence): a fresh git clone --no-hardlinks into $env:TEMP runs the 6 fast non-apex validators with HEAD==source assertion and zero-orphan cleanup"
    - "Axis 2 (cross-OS): gh workflow run audit-harness-v1.11-integrity.yml --ref master completes; per-job conclusions + Linux chain timing captured"
    - "Axis 3 (logical/context independence): the SAME fresh sub-agent serves zero-context-carryover (one dispatch, two dimensions)"
    - "Apex check-phase-93 count is Linux-GHA SOLE-authoritative (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 guaranteed at [48..92])"
    - "Cross-OS PASS/FAIL/SKIP EXACT MATCH across the 7-validator set is recorded in 93-03-AUDIT-RESULTS.md"
  artifacts:
    - path: ".planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-03-AUDIT-RESULTS.md"
      provides: "3-axis terminal re-audit evidence with cross-OS EXACT MATCH table"
      contains: "EXACT MATCH"
      min_lines: 80
  key_links:
    - from: ".planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-03-AUDIT-RESULTS.md"
      to: ".github/workflows/audit-harness-v1.11-integrity.yml"
      via: "Axis 2 gh workflow run --ref master"
      pattern: "audit-harness-v1.11-integrity"
---

<objective>
Ship Plan 93-03: HARN-03 part 1 — the 3-axis terminal re-audit (artifact-only commit, COMMIT 4). Replicate the executed v1.10 (88-03) recipe, v1.11-relabeled, across the 7-validator cross-OS-applicable set, and record cross-OS EXACT MATCH evidence in `93-03-AUDIT-RESULTS.md`.

The 7-validator cross-OS set (per D-03): `v1.11-milestone-audit.mjs` + `check-phase-88.mjs` (prior-apex continuity row) + the 5 net-new `check-phase-89/90/91/92/93.mjs`.

- **Axis 1 (local physical independence):** ONE fresh sub-agent runs a fresh `git clone --no-hardlinks` into `$env:TEMP\v1.11-audit-<rand8>`, asserts cloned HEAD == source HEAD, runs the 6 fast non-apex validators, captures exit/PASS/FAIL/SKIP, then `Remove-Item -Recurse -Force` + asserts zero orphans.
- **Axis 2 (cross-OS):** main session `gh workflow run audit-harness-v1.11-integrity.yml --ref master`; capture run URL + per-job conclusions + Linux chain timing.
- **Axis 3 (logical/context independence):** the SAME sub-agent as Axis 1 — zero-context-carryover. ONE dispatch covering two independence dimensions (do NOT spawn two agents).

Purpose: Prove the v1.11 harness produces identical PASS/FAIL/SKIP counts across a clean Windows clone and the Linux GHA runner. **Apex check-phase-93 count is Linux-GHA SOLE-authoritative** — `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` actually fired at [48..87] in v1.10; [48..92] is depth-monotone-worse → the Windows apex is guaranteed unreliable.
Output: `93-03-AUDIT-RESULTS.md` (artifact-only commit).

**HARD pre-flight ordering gate (D-03):** Plan 93-02 (Atom 2) MUST already be committed AND on `origin/master`. The workflow's check-phase-89..93 jobs FAIL (not skip) if absent from the dispatched ref.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-CONVENTIONS.md
@.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-CONTEXT.md
@.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-RESEARCH.md
@.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-PATTERNS.md
@.planning/milestones/v1.10-phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-03-AUDIT-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight ordering gate + Axis 2 cross-OS Linux GHA dispatch</name>
  <files>.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-03-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/milestones/v1.10-phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-03-AUDIT-RESULTS.md (the EXECUTED v1.10 recipe — Axis-2 gh command :34, pre-flight 3-part gate :143, cross-OS table format :108-123, deep-nest firing evidence :85-96)
    - .planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-CONVENTIONS.md (7-validator set + expected counts: harness 15/0/0, 88 42/0/1, 89/90/91 2/0/0, 92 9/0/0, 93 apex Linux-GHA sole-authoritative ~48/0/1)
    - .planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-RESEARCH.md (Target 8 — Axis-2 command + pre-flight gate; cross-OS EXACT MATCH format)
  </read_first>
  <action>
    **Pre-flight 3-part gate (HARD — abort if any fails):**
    1. `git log origin/master --oneline -1` shows the Atom-2 commit (`feat(93-02): … HARN-02 (atomic SC#1 Atom 2)`) on origin/master.
    2. `gh auth status` reports authenticated.
    3. `gh workflow list` shows `Audit Harness v1.11 Integrity` with `state: active`.
    If the Atom-2 commit is NOT on origin/master, STOP — Plan 93-02 must be pushed first (the workflow's check-phase-89..93 jobs FAIL, not skip, if absent from the dispatched ref).

    **Axis 2 dispatch:** Run `gh workflow run audit-harness-v1.11-integrity.yml --ref master`. Poll the run to completion (`gh run watch` or `gh run list --workflow=audit-harness-v1.11-integrity.yml -L1` then `gh run view <id>`). Capture: run URL, per-job conclusions (parse / path-match / harness-run / linux-chain-ubuntu-latest / check-phase-89/90/91/92/93 / rotting-external-quarterly [expect SKIP — negative control] / pin-helper-advisory), and the Linux chain timing from the `CHAIN_TIMING_LINUX` `::notice`. **The apex check-phase-93 PASS/FAIL/SKIP count from `linux-chain-ubuntu-latest` is the SOLE-authoritative apex count.**

    Begin authoring `93-03-AUDIT-RESULTS.md` (Path-A from 88-03): frontmatter (clone_path_pattern, gha_workflow_run URL, cross_os_exact_match) + the Axis-2 section with the captured Linux counts. Leave the Axis-1/Axis-3 Windows columns for Task 2.
  </action>
  <verify>
    <automated>gh run list --workflow=audit-harness-v1.11-integrity.yml -L1 --json conclusion,status,url 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight: `git log origin/master --oneline -1` contains the Atom-2 commit; `gh auth status` authenticated; v1.11 workflow `state: active`.
    - `gh workflow run audit-harness-v1.11-integrity.yml --ref master` dispatched; the run completes with linux-chain-ubuntu-latest + check-phase-89..93 jobs SUCCESS (or documented exact counts).
    - `rotting-external-quarterly` job conclusion = SKIP (negative-control confirmed — cron-only `if:` guard).
    - Linux apex check-phase-93 count captured (sole-authoritative); 93-03-AUDIT-RESULTS.md frontmatter records the run URL + `cross_os_exact_match` field.
  </acceptance_criteria>
  <done>Pre-flight gate passed; Axis-2 Linux GHA run dispatched and completed; per-job conclusions + Linux apex count captured into the in-progress 93-03-AUDIT-RESULTS.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Axis 1 + Axis 3 fresh-clone sub-agent (local physical + logical independence)</name>
  <what-built>
    The v1.11 audit harness (Atom 1 + Atom 2) is committed and on origin/master, and the Axis-2 Linux GHA run has produced cross-OS counts. This checkpoint runs the Axis-1/Axis-3 fresh-clone independence recipe — a fresh, zero-context PowerShell environment that clones the repo with its own `.git/`, runs the 6 fast non-apex validators, and proves the local Windows counts match Linux (except the apex, which is Linux-sole-authoritative).
  </what-built>
  <how-to-verify>
    Run this fresh-clone recipe in a clean PowerShell session (zero context carryover — this serves BOTH Axis 1 physical independence AND Axis 3 logical independence; ONE session, two dimensions — do NOT spawn two):

    1. Generate `$rand` = 8 chars from charset `[0-9a-z]`. Set `$dest = "$env:TEMP\v1.11-audit-$rand"`.
    2. `git clone --no-hardlinks D:\claude\Autopilot $dest` (auditor independence — own `.git/`, NOT a worktree; permitted under `use_worktrees:false`).
    3. Assert cloned HEAD == source HEAD: compare `git -C $dest rev-parse HEAD` to `git -C D:\claude\Autopilot rev-parse HEAD` — MUST be equal.
    4. From `$dest`, run the **6 fast non-apex validators**: `node scripts/validation/check-phase-88.mjs` (continuity), `check-phase-89/90/91/92.mjs` (net-new), plus `node scripts/validation/v1.11-milestone-audit.mjs --verbose` and `--self-test`. Capture each exit code + the `Result: N PASS, N FAIL, N SKIPPED` line. **Do NOT run check-phase-93.mjs from the Windows clone** — the apex count is Linux-GHA sole-authoritative (deep-nest timeout guaranteed at [48..92]).
    5. Expected (must match Linux from Axis 2): harness `15/0/0`; check-phase-88 `42/0/1`; check-phase-89/90/91 `2/0/0` each; check-phase-92 `9/0/0`; self-test `9 passed, 0 failed`.
    6. Cleanup: `Remove-Item -Recurse -Force $dest`; assert `(Get-ChildItem $env:TEMP -Filter "v1.11-audit-*" -Directory).Count -eq 0` (zero orphans).
    7. Finalize `93-03-AUDIT-RESULTS.md`: complete the cross-OS table (Windows Axis 1+3 column vs Linux Axis 2 column) for all 7 validators, mark each row Match, set `cross_os_exact_match: true`, and record the clone HEAD, `$rand`, and zero-orphan cleanup assertion. Document `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` as the reason the apex row is Linux-sole-authoritative (carry-forward note for DEFERRED-CLEANUP, depth → [48..92]).

    Confirm: cloned HEAD matched source, all 6 non-apex counts matched the Linux Axis-2 counts (EXACT MATCH), and the temp clone was removed with zero orphans.
  </how-to-verify>
  <resume-signal>Type "approved" once the fresh-clone recipe shows cross-OS EXACT MATCH (6 non-apex + apex from Linux), zero orphans, and 93-03-AUDIT-RESULTS.md is finalized — or describe any count divergence.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit the audit-results artifact (COMMIT 4, artifact-only)</name>
  <files>.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-03-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-03-AUDIT-RESULTS.md (the artifact finalized in Task 2)
    - .planning/milestones/v1.10-phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-03-AUDIT-RESULTS.md (Path-A structure reference)
  </read_first>
  <action>
    Verify `93-03-AUDIT-RESULTS.md` contains the complete 7-row cross-OS table (each row marked Match / EXACT MATCH), `cross_os_exact_match: true`, the Axis-2 run URL, the Axis-1 clone HEAD + `$rand` + zero-orphan assertion, and the `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` apex-sole-authoritative note. Then commit ONLY this file (artifact-only commit — no validators, no config changed).
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-03-AUDIT-RESULTS.md','utf8'); ['EXACT MATCH','check-phase-93','WINDOWS-CLONE-DEEPNEST-TIMEOUT-01','audit-harness-v1.11-integrity'].forEach(s=>{if(!f.includes(s)){console.error('AUDIT-RESULTS missing '+s);process.exit(1)}}); console.log('audit-results ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `93-03-AUDIT-RESULTS.md` contains a 7-validator cross-OS table with EXACT MATCH, `check-phase-93`, `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01`, and `audit-harness-v1.11-integrity`.
    - COMMIT 4 message is exactly: `docs(93-03): HARN-03 3-axis terminal re-audit results (artifact-only)`
    - COMMIT 4 touches ONLY `93-03-AUDIT-RESULTS.md` (artifact-only; no validators/config modified).
  </acceptance_criteria>
  <done>93-03-AUDIT-RESULTS.md finalized with cross-OS EXACT MATCH and committed as COMMIT 4 (artifact-only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Windows ↔ GitHub Actions Linux runner | The re-audit deliberately crosses the OS boundary to prove count-identity. No untrusted input crosses — both axes run the same in-repo validators against the same `master` ref. |
| source repo → fresh TEMP clone | Axis 1 clones into `$env:TEMP` with `--no-hardlinks` (own `.git/`), runs read-only validators, then removes the clone. No write-back to the source repo. |

## STRIDE Threat Register

ASVS L1 review found **no high-severity items**. This is a read-only verification plan.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03-T | Tampering | Fresh clone left as orphan in TEMP (stale-state leakage) | mitigate | Recipe asserts `Get-ChildItem $env:TEMP -Filter "v1.11-audit-*" -Directory` count == 0 after `Remove-Item -Recurse -Force`. |
| T-93-03-I | Integrity | Axis-2 dispatched against a ref missing the v1.11 jobs (false PASS/SKIP) | mitigate | HARD pre-flight gate: Atom 2 confirmed on origin/master + workflow `state: active` before dispatch; jobs FAIL (not skip) if absent. |
| T-93-03-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | NO installs — `git clone` + `node` (in-tree scripts) + `gh` only. No new deps. No Package Legitimacy Gate required. |
</threat_model>

<verification>
- Pre-flight gate passed (Atom 2 on origin/master + gh auth + workflow active).
- Axis 2 Linux GHA run completed; apex check-phase-93 count is Linux-sole-authoritative.
- Axis 1/3 fresh clone: HEAD==source, 6 non-apex counts EXACT MATCH Linux, zero orphans.
- 93-03-AUDIT-RESULTS.md records cross_os_exact_match: true; committed artifact-only.
</verification>

<success_criteria>
- ROADMAP SC#3 satisfied: 3-axis terminal re-audit completes (Axis 1 fresh clone + Axis 2 Linux GHA + Axis 3 zero-context sub-agent); cross-OS PASS/FAIL/SKIP counts EXACT MATCH across the 7-validator set.
- Apex check-phase-93 count Linux-GHA sole-authoritative (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 documented).
- 93-03-AUDIT-RESULTS.md committed as COMMIT 4 (artifact-only), consumed by the 93-04 close-gate.
</success_criteria>

<output>
Create `.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-03-SUMMARY.md` when done. Record: Axis-2 run URL + Linux apex count, Axis-1 clone HEAD + `$rand` + zero-orphan confirmation, the 7-row EXACT-MATCH outcome, and COMMIT-4 SHA (the `audit_results_sha` for the 93-04 close-gate frontmatter).
</output>
