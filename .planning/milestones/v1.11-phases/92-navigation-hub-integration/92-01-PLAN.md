---
phase: 92-navigation-hub-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.md
  - docs/common-issues.md
  - docs/quick-ref-l2.md
  - docs/decision-trees/06-macos-triage.md
autonomous: true
requirements: [NAV-01]
tags: [navigation, macos, mdm-migration, psso, documentation]

must_haves:
  truths:
    - "From docs/index.md, an L1 reader can reach 01-psso-provisioning-walkthrough.md AND 02-mdm-migration-psso.md (both walkthroughs appear as rows in the Service Desk L1 table)"
    - "From docs/index.md, an L2 reader can reach 01, 02, AND the L2 #30 migration-failure runbook (all three appear as rows in the Desktop Engineering L2 table; #30 in L2 table only)"
    - "From docs/common-issues.md macOS section, a reader hitting a non-dismissible full-screen migration deadline lockout reaches L2 #30; a reader hitting a post-migration PSSO re-registration failure reaches both L2 #27 and L2 #30"
    - "From docs/quick-ref-l2.md, an L2 engineer finds the two net-new migration diagnostic commands (ls /Library/Kandji/, sw_vers -productVersion) and a link to L2 #30, with existing app-sso/profiles commands cross-referenced by anchor (not copied)"
    - "From docs/decision-trees/06-macos-triage.md, a deadline-lockout device routes MAC1=Yes -> MAC3 migration branch -> MACE3 -> L2 #30 (NOT mis-routed to MAC2), with the MAC1 How-to-Check exception clause present and MACE3 styled red via escalateL2"
    - "All four hub edits ship in exactly ONE atomic commit, created only after D-05 pre-commit verification confirms all 3 content files exist at HEAD and every new link anchor/path resolves"
  artifacts:
    - path: "docs/index.md"
      provides: "L1+L2 table rows for 01/02; L2-only row for #30"
      contains: "macos-lifecycle/01-psso-provisioning-walkthrough.md"
    - path: "docs/common-issues.md"
      provides: "Two new macOS ### symptom subsections (MDM Migration Failure; PSSO Re-Registration Failure Post-Migration)"
      contains: "### MDM Migration Failure (Kandji/Iru"
    - path: "docs/quick-ref-l2.md"
      provides: "New #### MDM Migration Diagnostics block + L2 #30 runbook bullet"
      contains: "#### MDM Migration Diagnostics"
    - path: "docs/decision-trees/06-macos-triage.md"
      provides: "MACE3 migration leaf + click + escalateL2 class + MAC1 exception clause + Routing Verification row + Version History row"
      contains: "MACE3"
  key_links:
    - from: "docs/index.md"
      to: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md"
      via: "L1 + L2 table row markdown link"
      pattern: "macos-lifecycle/01-psso-provisioning-walkthrough\\.md"
    - from: "docs/index.md"
      to: "docs/l2-runbooks/30-macos-mdm-migration-failure.md"
      via: "L2 table row markdown link (L2 only)"
      pattern: "l2-runbooks/30-macos-mdm-migration-failure\\.md"
    - from: "docs/common-issues.md"
      to: "docs/l2-runbooks/27-macos-sso-investigation.md"
      via: "PSSO re-registration entry **L2:** bullet"
      pattern: "l2-runbooks/27-macos-sso-investigation\\.md"
    - from: "docs/quick-ref-l2.md"
      to: "#platform-sso-attestation-command"
      via: "cross-reference anchor (link-not-copy, NOT restated command)"
      pattern: "#platform-sso-attestation-command"
    - from: "docs/decision-trees/06-macos-triage.md"
      to: "../l2-runbooks/30-macos-mdm-migration-failure.md"
      via: "click MACE3 directive"
      pattern: "click MACE3 \"\\.\\./l2-runbooks/30-macos-mdm-migration-failure\\.md\""
---

<objective>
Wire the three already-committed v1.11 content files — `01-psso-provisioning-walkthrough.md`, `02-mdm-migration-psso.md`, and L2 runbook `30-macos-mdm-migration-failure.md` — into the four top-level navigation hubs (`docs/index.md`, `docs/common-issues.md`, `docs/quick-ref-l2.md`, `docs/decision-trees/06-macos-triage.md`) so every new macOS migration surface is reachable from every applicable hub. Satisfies NAV-01 and the navigation-last invariant.

Purpose: This is the NAVIGATION-LAST phase. Content (Phases 89-91) is shipped and frozen; this phase makes it discoverable. The five implementation decisions (D-01..D-05) are LOCKED via `/adversarial-review` — they are translated here into precise line-anchored edit tasks, NOT re-decided.

Output: Four edited hub files, shipped in ONE atomic commit (D-05) after pre-commit content-existence + anchor-resolution verification. No content files edited. No blob-pins regenerated (none of these 4 hubs are pinned). No validator authored (that is Phase 93).

CRITICAL EXECUTION SHAPE: All four edit tasks (1-4) modify their files but DO NOT commit. Task 5 runs the D-05 verification, then makes the single atomic commit touching exactly the four hub files. Do NOT commit per-hub — that would leave cross-hub-incoherent intermediate HEADs (Pitfall 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/92-navigation-hub-integration/92-CONTEXT.md
@.planning/phases/92-navigation-hub-integration/92-RESEARCH.md
@.planning/phases/92-navigation-hub-integration/92-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire 01/02/#30 into docs/index.md role tables (D-01)</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md lines 98-127 (the `## macOS Provisioning` block: `### Service Desk (L1)` table 102-110, `### Desktop Engineering (L2)` table 112-125, `### Admin Setup` at 127) — confirm the existing `00-ade-lifecycle.md` rows at lines 106 (L1) and 116 (L2) as the placement precedent, and the L2 runbook-row label style at lines 121-125.
    - 92-RESEARCH.md "Hub File 1: docs/index.md" (verified line table + exact insertion rows).
    - 92-PATTERNS.md "docs/index.md — nav table rows (D-01)" (exact rows to clone format from + new rows verbatim).
    - Confirm target files exist (link targets): docs/macos-lifecycle/01-psso-provisioning-walkthrough.md, docs/macos-lifecycle/02-mdm-migration-psso.md, docs/l2-runbooks/30-macos-mdm-migration-failure.md.
  </read_first>
  <action>
    Per D-01 (winner 1A-mod): add the two walkthrough rows to BOTH the L1 and L2 macOS tables, and the #30 runbook row to the L2 table ONLY. Clone the `| [Label](path) | When to Use |` two-column format of the existing `00-ade-lifecycle.md` rows (lines 106/116). Use root-relative paths (index.md sits at `docs/`, no `../` prefix): `macos-lifecycle/01-psso-provisioning-walkthrough.md`, `macos-lifecycle/02-mdm-migration-psso.md`, `l2-runbooks/30-macos-mdm-migration-failure.md`.

    L1 table — append two rows AFTER line 110 (the last current L1 row), before blank line 111: a `macOS PSSO Provisioning Walkthrough` row -> 01, and a `macOS MDM Migration Walkthrough (Kandji/Iru → Intune)` row -> 02. Use the exact row text given in 92-RESEARCH.md D-01 L1 insertion block / 92-PATTERNS.md L1 append block.

    L2 table — append three rows AFTER line 125 (the last current L2 row `macOS Graph Credential Investigation`), before blank line 126: the same two walkthrough rows (01, 02) PLUS a third `macOS MDM Migration Failure Runbook` row -> 30. The #30 "When to Use" cell MUST name the three tracks at a glance (Track A: deadline lockout / non-dismissible full-screen prompt + ABM admin recovery; Track B: profile-not-delivered / enrollment-failed / leftover Kandji/Iru agent; Track C: PSSO re-registration stuck) so a locked-device tech recognizes it. Use the exact L2 row text from 92-RESEARCH.md D-01 L2 insertion block / 92-PATTERNS.md L2 append block.

    Both walkthrough labels surface BOTH vendor names via `Kandji/Iru → Intune` (slash separator, right arrow). Do NOT add `01`/`02`/`#30` to the `### Admin Setup` table (line 127+). Do NOT re-add or restyle the existing `00-ade-lifecycle.md` rows (anti-rename rule). Do NOT commit in this task.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=docs/index.md; grep -c "macos-lifecycle/01-psso-provisioning-walkthrough.md" "$f" | grep -qx 2 && echo "01: 2 rows OK"; grep -c "macos-lifecycle/02-mdm-migration-psso.md" "$f" | grep -qx 2 && echo "02: 2 rows OK"; grep -c "l2-runbooks/30-macos-mdm-migration-failure.md" "$f" | grep -qx 1 && echo "30: 1 row OK"; grep -q "Kandji/Iru → Intune" "$f" && echo "both-names OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `macos-lifecycle/01-psso-provisioning-walkthrough.md` appears in exactly 2 rows (L1 + L2 tables).
    - `macos-lifecycle/02-mdm-migration-psso.md` appears in exactly 2 rows (L1 + L2 tables).
    - `l2-runbooks/30-macos-mdm-migration-failure.md` appears in exactly 1 row (L2 table only — NOT in L1, NOT in Admin Setup).
    - The #30 row "When to Use" cell textually names Track A, Track B, and Track C.
    - The 02 row label contains the literal string `Kandji/Iru → Intune`.
    - The existing `00-ade-lifecycle.md` rows at lines 106 and 116 are byte-unchanged (anti-rename).
    - The `### Admin Setup` table gained no new rows.
    - No git commit was created by this task.
  </acceptance_criteria>
  <done>index.md L1 table has 2 new walkthrough rows; L2 table has 2 walkthrough rows + 1 #30 row; both-names label present; 00 rows untouched; Admin Setup untouched; uncommitted.</done>
</task>

<task type="auto">
  <name>Task 2: Add two macOS symptom entries to docs/common-issues.md (D-02)</name>
  <files>docs/common-issues.md</files>
  <read_first>
    - docs/common-issues.md lines 157-227 (the `## macOS ADE Failure Scenarios` section through its hard boundary `## iOS/iPadOS Failure Scenarios` at line 227) — confirm the `### Kerberos SSO Extension Failure` block at 220-225 (the verbatim template: `###` heading, one symptom sentence, `- **L1:** No L1 runbook — escalate to L2` at line 224, `- **L2:**` link at 225) and the macOS-section open at line 157.
    - docs/common-issues.md line 138 (`### Migration Issues` — the Windows APv1->APv2 heading; the name-collision the new heading must disambiguate against).
    - 92-RESEARCH.md "Hub File 2: docs/common-issues.md" (Kerberos verbatim template + exact two-block insertion).
    - 92-PATTERNS.md "docs/common-issues.md — symptom-index entries (D-02)" (exact blocks + verbatim/no-assert constraints).
    - Link targets to confirm exist: docs/l2-runbooks/30-macos-mdm-migration-failure.md, docs/l2-runbooks/27-macos-sso-investigation.md.
  </read_first>
  <action>
    Per D-02 (winner 2A): append TWO new `###` symptom subsections AFTER line 225 (the Kerberos `**L2:**` link), before blank line 226 and `## iOS/iPadOS Failure Scenarios` at line 227 — both blocks must land INSIDE the `## macOS ADE Failure Scenarios` section. Preserve the file's one-symptom-per-`###` anatomy by giving each symptom its own heading. Use root-relative link paths (file sits at `docs/`): `l2-runbooks/30-macos-mdm-migration-failure.md`, `l2-runbooks/27-macos-sso-investigation.md`.

    Block 1 — heading `### MDM Migration Failure (Kandji/Iru → Intune)`. The symptom sentence MUST describe the non-dismissible full-screen deadline lockout (so a Track-A reader self-identifies) — also covering "migration completed but Intune shows device not enrolled" / "configuration profiles not delivered" per 92-RESEARCH.md. Then reuse the Kerberos `- **L1:** No L1 runbook — escalate to L2` line VERBATIM (character-for-character copy of line 224, em-dash included). Then `- **L2:**` linking to L2 #30.

    Block 2 — heading `### Platform SSO Re-Registration Failure (Post-Migration)`. Symptom sentence: "Registration Required" notification has not appeared after migration, or registration initiated but not completing; state that PSSO re-registration is ALWAYS required after MDM migration (MDM unenrollment = IdP unregistration; Secure Enclave key re-created on re-enrollment). Then TWO `- **L2:**` bullets: first -> L2 #27 (macOS Platform SSO Investigation), second -> L2 #30 with a `— Track C: PSSO re-registration stuck` qualifier.

    Use the exact two-block text from 92-RESEARCH.md D-02 insertion / 92-PATTERNS.md two-new-blocks. The `(Kandji/Iru → Intune)` qualifier + macOS-section scoping is the sole disambiguation from the Windows `### Migration Issues` (line 138) — add no extra banner. NO-ASSERT: Block 2 must NOT claim same-tenant Secure Enclave key survival; "always required" is the authoritative wording. Do NOT commit in this task.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=docs/common-issues.md; grep -q "### MDM Migration Failure (Kandji/Iru → Intune)" "$f" && echo "block1 heading OK"; grep -q "### Platform SSO Re-Registration Failure (Post-Migration)" "$f" && echo "block2 heading OK"; grep -c "No L1 runbook — escalate to L2" "$f" | grep -qx 2 && echo "verbatim L1 line now 2x OK"; grep -q "l2-runbooks/30-macos-mdm-migration-failure.md" "$f" && echo "links #30 OK"; grep -q "l2-runbooks/27-macos-sso-investigation.md" "$f" && echo "links #27 OK"; ! grep -iq "key survives\|key survival\|same-tenant key" "$f" && echo "no-assert OK"'</automated>
  </verify>
  <acceptance_criteria>
    - Heading `### MDM Migration Failure (Kandji/Iru → Intune)` exists and its symptom sentence references the non-dismissible full-screen deadline lockout.
    - Heading `### Platform SSO Re-Registration Failure (Post-Migration)` exists.
    - The literal `No L1 runbook — escalate to L2` line now appears at least twice (original Kerberos + new block 1), confirming verbatim reuse.
    - Block 1 links to `l2-runbooks/30-macos-mdm-migration-failure.md`.
    - Block 2 has two `**L2:**` bullets linking to `l2-runbooks/27-macos-sso-investigation.md` AND `l2-runbooks/30-macos-mdm-migration-failure.md`.
    - Block 2 states re-registration is "always required" and does NOT assert same-tenant key survival.
    - Both new headings sit between line 157 (`## macOS ADE Failure Scenarios`) and `## iOS/iPadOS Failure Scenarios` (inside the macOS section).
    - No git commit was created by this task.
  </acceptance_criteria>
  <done>Two new macOS symptom `###` blocks present inside the macOS section; verbatim L1 escalate line reused; #30 and #27 linked; no key-survival assertion; uncommitted.</done>
</task>

<task type="auto">
  <name>Task 3: Add MDM Migration Diagnostics block + #30 bullet to docs/quick-ref-l2.md (D-03)</name>
  <files>docs/quick-ref-l2.md</files>
  <read_first>
    - docs/quick-ref-l2.md lines 132-211 (the `## macOS ADE Quick Reference` section through boundary `## iOS/iPadOS Quick Reference` at 211) — confirm `### Key Terminal Commands` at 146 (with `profiles status -type enrollment` at 150 and `profiles show` at 152), the `#### Platform SSO Attestation Command` block at 180-188 (`app-sso platform -s` at 185), the `#### Kerberos SSO Diagnostics` block at 190-198, `### macOS Investigation Runbooks` at 200, and the six existing runbook bullets 202-207.
    - 92-RESEARCH.md "Hub File 3: docs/quick-ref-l2.md" (existing `####` sibling pattern, anchor `#platform-sso-attestation-command`, two exact insertion points).
    - 92-PATTERNS.md "docs/quick-ref-l2.md — `####` diagnostic block + runbook bullet (D-03)" (exact new `####` block + new bullet + link-not-copy constraint).
    - Link target to confirm exists: docs/l2-runbooks/30-macos-mdm-migration-failure.md.
  </read_first>
  <action>
    Per D-03 (winner 3A, hybrid link-not-copy):

    Insertion 1 — add a new `#### MDM Migration Diagnostics` block AFTER line 198 (after the Kerberos SSO Diagnostics closing prose), before blank line 199 and `### macOS Investigation Runbooks` at line 200. Match the existing `####` sibling nesting and structure (heading, one intro sentence, a single fenced `bash` block, then cross-reference prose) exactly like `#### Platform SSO Attestation Command` / `#### Kerberos SSO Diagnostics`. The fenced bash block adds ONLY the two net-new migration commands: `ls /Library/Kandji/` (leftover source-MDM agent check — Track B root cause) and `sw_vers -productVersion` (macOS-26 gate for B1 in-place). Then cross-reference the two ALREADY-PRESENT commands BY ANCHOR, link-not-copy (do NOT restate the command text in a code block): `profiles status -type enrollment` via `#key-terminal-commands`, and `app-sso platform -s` via `#platform-sso-attestation-command`. Close with a link to L2 #30 (`l2-runbooks/30-macos-mdm-migration-failure.md`) for Track A/B/C steps. Use the exact block text from 92-RESEARCH.md D-03 Step 1 / 92-PATTERNS.md new `####` block.

    Insertion 2 — append ONE new runbook bullet AFTER line 207 (after the last existing bullet `Kerberos SSO Investigation`), before blank line 208, matching the existing bullet format (`- [Label](path) -- note`): `macOS MDM Migration Failure` -> #30, with the `Track A: deadline lockout, Track B: profile-not-delivered / enrollment-failed, Track C: PSSO re-registration stuck` note.

    Do NOT add `profiles list` (the file already has `profiles show` at line 152 — inline mention only if needed, no new code block). Do NOT copy `app-sso platform -s` or `profiles status -type enrollment` into a new code block (link-not-copy / Pitfall 4). Use root-relative path `l2-runbooks/...` (file sits at `docs/`); the anchors `#platform-sso-attestation-command` and `#key-terminal-commands` are internal (no path). Do NOT commit in this task.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=docs/quick-ref-l2.md; grep -q "#### MDM Migration Diagnostics" "$f" && echo "heading OK"; grep -q "ls /Library/Kandji/" "$f" && echo "net-new cmd1 OK"; grep -q "sw_vers -productVersion" "$f" && echo "net-new cmd2 OK"; grep -q "#platform-sso-attestation-command" "$f" && echo "anchor xref OK"; grep -q "#key-terminal-commands" "$f" && echo "anchor xref2 OK"; grep -c "app-sso platform -s" "$f" | grep -qx 1 && echo "no app-sso dup OK"; grep -c "ls /Library/Kandji/" "$f" | grep -qx 1 && echo "single new block OK"; grep -c "l2-runbooks/30-macos-mdm-migration-failure.md" "$f" | grep -qx 2 && echo "30 linked in block+bullet OK"; ! grep -q "profiles list" "$f" && echo "no profiles list OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `#### MDM Migration Diagnostics` heading exists at the `####` sibling level, inserted before `### macOS Investigation Runbooks`.
    - The new block's bash fence contains exactly the two net-new commands `ls /Library/Kandji/` and `sw_vers -productVersion`.
    - `app-sso platform -s` still appears exactly once in the file (NOT duplicated into the new block) — cross-referenced via `#platform-sso-attestation-command` instead.
    - `profiles status -type enrollment` is cross-referenced via `#key-terminal-commands` (not restated in a new code block).
    - `l2-runbooks/30-macos-mdm-migration-failure.md` appears twice (once in the block's closing link, once in the new runbook bullet).
    - No `profiles list` command was added.
    - No git commit was created by this task.
  </acceptance_criteria>
  <done>New `#### MDM Migration Diagnostics` block with only the two net-new commands + anchor cross-refs present; #30 runbook bullet appended; no command duplication; uncommitted.</done>
</task>

<task type="auto">
  <name>Task 4: Add MACE3 migration leaf + MAC1 disambiguation to docs/decision-trees/06-macos-triage.md (D-04 + mandatory sleeper-flaw-4.2 fix)</name>
  <files>docs/decision-trees/06-macos-triage.md</files>
  <read_first>
    - docs/decision-trees/06-macos-triage.md lines 29-63 (the full mermaid block: root MAC1 at 31, MAC1->Yes->MAC3 at 33, MACSSO arms 43-46 incl. `MACE2` declaration at 46, `click` directives 48-56 incl. `click MACE2` at 56, `classDef escalateL2` at 59, `class MACE1,MACE2 escalateL2` at 62, fence close at 63) — MACE2 is the verbatim escalate-leaf pattern to mirror.
    - docs/decision-trees/06-macos-triage.md lines 65-110 (`## Routing Verification` table incl. `→` Step-2 convention rows 79-80; `## How to Check` MAC1 row at 86; `## Version History` rows 106-110 incl. the 2026-06-24 Phase 87 row at 108).
    - 92-RESEARCH.md "Hub File 4" + "D-04 Sleeper Flaw 4.2 Verification (CONFIRMED)" (the load-bearing reason MAC1 routing must be amended — a deadline-lockout device is NOT at the Finder desktop and must route MAC1=Yes, not No->MAC2).
    - 92-PATTERNS.md "docs/decision-trees/06-macos-triage.md — mermaid escalate leaf + prose amendments (D-04)" (exact three sub-edits + MAC1 amended cell + Routing/Version rows).
    - docs/l2-runbooks/30-macos-mdm-migration-failure.md Track A Step 1 (confirms full-screen non-dismissible presentation — the symptom vocabulary the branch label must use).
  </read_first>
  <action>
    Per D-04 (winner 4A-mod) with the NON-OPTIONAL MAC1 fix (confirmed sleeper flaw 4.2). Apply all SIX edits below in this single file (all ship in the one atomic commit). The new path is MAC1 (edge 1) -> MAC3 (edge 2) -> MACE3 (terminal) = 2 edges from root, within the ≤3-edge invariant; introduce NO sub-decision node.

    Sub-edit A (mermaid node) — insert AFTER line 46 (the last MACSSO arm `MACE2`), before blank line 47: a new MAC3 arm `MAC3 -->|"MDM migration /<br/>non-dismissible<br/>migration prompt"| MACE3(["Escalate to L2:<br/>MDM Migration Failure"])`. The branch LABEL must describe the migration-prompt PRESENTATION, NOT a desktop "primary symptom" (do not use "primary symptom" framing). Mirror the MACE2 declaration style.

    Sub-edit B (click directive) — insert AFTER line 56 (the last `click MACE2` directive), before blank line 57: `click MACE3 "../l2-runbooks/30-macos-mdm-migration-failure.md"` (note the `../` prefix — this file lives in `docs/decision-trees/`).

    Sub-edit C (class registration) — amend line 62 from `class MACE1,MACE2 escalateL2` to `class MACE1,MACE2,MACE3 escalateL2` so MACE3 renders red (Pitfall 3 — else unstyled white).

    Sub-edit D (MAC1 How-to-Check exception — MANDATORY) — amend the MAC1 row cell at line 86 by APPENDING an exception clause to the existing sentence (keep all current wording): `**Exception:** A device showing a full-screen Kandji/Iru → Intune MDM migration deadline prompt is NOT in OOBE — it is in migration deadline enforcement and routes as MAC1 = Yes → MDM migration leaf (MACE3).` This prevents the literal-reading mis-route to MAC1=No->MAC2 (device-not-appearing). Use the exact amended cell text from 92-RESEARCH.md "D-04 How to Check Table" / 92-PATTERNS.md amended cell.

    Sub-edit E (Routing Verification row) — append ONE row AFTER line 80 (the Kerberos row, current 10th): `| MDM migration — deadline prompt | Setup Assistant? Yes | Symptom: MDM migration / non-dismissible migration prompt | L2 escalation (#30) |`. No `→` Step-2 expression — MACE3 has no sub-decision, so the Step-2 cell is a direct symptom label.

    Sub-edit F (Version History row) — prepend ONE row as the new most-recent entry, AFTER the table header and BEFORE the current 2026-06-24 Phase 87 row at line 108: `| 2026-06-25 | Phase 92 (NAV-01): added MDM migration leaf (MACE3 → L2 #30) off MAC3; MAC1 "How to Check" disambiguation note for deadline-lockout routing; 1 Routing Verification row | -- |`.

    Use the both-names `Kandji/Iru → Intune` vocabulary in the MAC1 exception clause. Do NOT commit in this task.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=docs/decision-trees/06-macos-triage.md; grep -q "MAC3 -->|\"MDM migration" "$f" && echo "node A OK"; grep -q "click MACE3 \"../l2-runbooks/30-macos-mdm-migration-failure.md\"" "$f" && echo "click B OK"; grep -q "class MACE1,MACE2,MACE3 escalateL2" "$f" && echo "class C OK"; grep -q "is NOT in OOBE" "$f" && grep -q "routes as MAC1 = Yes" "$f" && echo "MAC1 exception D OK"; grep -q "MDM migration — deadline prompt | Setup Assistant? Yes" "$f" && echo "routing row E OK"; grep -q "Phase 92 (NAV-01): added MDM migration leaf" "$f" && echo "version row F OK"'</automated>
  </verify>
  <acceptance_criteria>
    - A new `MAC3 -->|...| MACE3([...])` mermaid arm exists with a label describing the migration-prompt presentation (contains "MDM migration" / "non-dismissible"; does NOT use "primary symptom").
    - `click MACE3 "../l2-runbooks/30-macos-mdm-migration-failure.md"` directive present (with `../` prefix).
    - The escalateL2 class line reads `class MACE1,MACE2,MACE3 escalateL2` (MACE3 added — renders red).
    - The MAC1 "How to Check" cell retains its original wording AND appends the exception clause routing the deadline prompt as MAC1 = Yes → MACE3 (NOT MAC2).
    - Exactly one new Routing Verification row for the MDM migration deadline prompt (Step-2 cell is a direct symptom label, no `→`).
    - Exactly one new Version History row dated 2026-06-25 citing Phase 92 / NAV-01 / MACE3.
    - 3-edge invariant intact: MACE3 is a direct MAC3 edge (no sub-decision node introduced).
    - No git commit was created by this task.
  </acceptance_criteria>
  <done>MACE3 node + click + escalateL2 class added; MAC1 exception clause appended; 1 Routing Verification row + 1 Version History row added; ≤3-edge invariant preserved; uncommitted.</done>
</task>

<task type="auto">
  <name>Task 5: D-05 navigation-last pre-commit verification + single atomic commit</name>
  <files>docs/index.md, docs/common-issues.md, docs/quick-ref-l2.md, docs/decision-trees/06-macos-triage.md</files>
  <read_first>
    - 92-RESEARCH.md "D-05 Navigation-Last Verification Protocol" (the pre-commit script + atomic commit shape + post-commit quick check).
    - 92-CONTEXT.md D-05 execution rules (anchor verification must include the in-tree click target, `#platform-sso-attestation-command`, and relative paths to 01/02/#30/#27; no V-63 pin; sequential on main tree per use_worktrees:false).
    - 92-PATTERNS.md "Single atomic commit shape (D-05)".
    - Confirm git working tree shows exactly the four hub files modified (and nothing else staged) before committing.
  </read_first>
  <action>
    Per D-05 (winner 5A-mod). This task performs verification THEN the single atomic commit. Do NOT touch content files; do NOT regenerate any V-63 blob pin (none of these 4 hubs are pinned — Pitfall 5); run sequentially on the main tree (use_worktrees:false).

    Step 1 — content-existence verification (navigation-last invariant): run `git cat-file -e HEAD:<path>` for all FOUR link targets — `docs/macos-lifecycle/01-psso-provisioning-walkthrough.md`, `docs/macos-lifecycle/02-mdm-migration-psso.md`, `docs/l2-runbooks/30-macos-mdm-migration-failure.md`, `docs/l2-runbooks/27-macos-sso-investigation.md`. ALL must exist at HEAD. If any is missing, ABORT (do not commit) — the nav-last invariant is violated.

    Step 2 — anchor/path resolution verification: confirm every new link target resolves. (a) Relative file paths from each hub resolve to a real file on disk: index.md `macos-lifecycle/01...`, `macos-lifecycle/02...`, `l2-runbooks/30...`; common-issues.md `l2-runbooks/30...`, `l2-runbooks/27...`; quick-ref-l2.md `l2-runbooks/30...`; triage tree `../l2-runbooks/30-macos-mdm-migration-failure.md` (resolved relative to `docs/decision-trees/`). (b) The two internal anchors used by quick-ref-l2.md resolve to in-file headings: `#platform-sso-attestation-command` (from `#### Platform SSO Attestation Command`) and `#key-terminal-commands` (from `### Key Terminal Commands`). If any path or anchor fails to resolve, ABORT.

    Step 3 — scope sanity: confirm `git status --porcelain` shows exactly the four hub files modified and NO other files (no content files, no validator, no blob-pin). 

    Step 4 — single atomic commit: stage exactly the four hub files and commit once. Commit message: `docs(phase-92): nav-hub integration — wire 01/02/30 into index/common-issues/quick-ref/triage (NAV-01)` followed by the standard co-author trailer. Do NOT make per-hub commits.

    Step 5 — post-commit quick check: `git show --stat HEAD` (or `git diff HEAD~1 --name-only`) lists EXACTLY the four hub files and nothing else.
  </action>
  <verify>
    <automated>bash -c 'set -e; for p in docs/macos-lifecycle/01-psso-provisioning-walkthrough.md docs/macos-lifecycle/02-mdm-migration-psso.md docs/l2-runbooks/30-macos-mdm-migration-failure.md docs/l2-runbooks/27-macos-sso-investigation.md; do git cat-file -e HEAD:"$p" || git cat-file -e HEAD~1:"$p"; done && echo "content files exist OK"; test -f docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && test -f docs/macos-lifecycle/02-mdm-migration-psso.md && test -f docs/l2-runbooks/30-macos-mdm-migration-failure.md && test -f docs/l2-runbooks/27-macos-sso-investigation.md && echo "paths resolve OK"; grep -q "#### Platform SSO Attestation Command" docs/quick-ref-l2.md && grep -q "### Key Terminal Commands" docs/quick-ref-l2.md && echo "anchors resolve OK"; n=$(git diff HEAD~1 --name-only | sort -u); echo "$n" | grep -qx "docs/index.md" && echo "$n" | grep -qx "docs/common-issues.md" && echo "$n" | grep -qx "docs/quick-ref-l2.md" && echo "$n" | grep -qx "docs/decision-trees/06-macos-triage.md" && test "$(echo "$n" | wc -l)" -eq 4 && echo "atomic commit = exactly 4 hub files OK"'</automated>
  </verify>
  <acceptance_criteria>
    - All four content/target files (`01`, `02`, `#30`, `#27`) confirmed present at HEAD before commit (or HEAD~1 after commit) — navigation-last invariant satisfied.
    - Every new hub link path resolves to a real file (including the triage tree's `../l2-runbooks/30-...` relative path).
    - The quick-ref internal anchors `#platform-sso-attestation-command` and `#key-terminal-commands` resolve to existing in-file headings.
    - Exactly ONE commit was created; `git diff HEAD~1 --name-only` lists exactly the four hub files (docs/index.md, docs/common-issues.md, docs/quick-ref-l2.md, docs/decision-trees/06-macos-triage.md) and nothing else.
    - No content file, validator, or blob-pin file is in the commit (no V-63 regeneration).
    - Commit message cites phase-92 / NAV-01.
  </acceptance_criteria>
  <done>D-05 pre-commit verification passed; single atomic commit touching exactly the four hub files created with NAV-01 message; no content/validator/pin files included.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a **documentation-only navigation-wiring phase**. It edits four existing Markdown navigation-hub files. It adds NO executable code, NO user-input handling, NO authentication or data flows, NO network surface, and NO new dependencies. There are therefore **no trust boundaries crossed by anything this phase produces** — the artifacts are static internal documentation links rendered by GitHub/Markdown viewers.

| Boundary | Description |
|----------|-------------|
| (none) | No untrusted input crosses any boundary introduced or modified by this phase. All edits are static internal cross-reference links between repository Markdown files. |

## STRIDE Threat Register

The only realistic failure class for a navigation-wiring phase is a **broken or dead internal link / dangling anchor** — this is a documentation-correctness concern, not a security threat. It is already mitigated by the D-05 pre-commit verification (content-existence via `git cat-file -e HEAD:<path>` + anchor/path resolution) gating the single atomic commit.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering (correctness only) | New hub link targets (01/02/#30/#27 paths + `#platform-sso-attestation-command` / `#key-terminal-commands` anchors + triage `../l2-runbooks/30-...` click) | mitigate | D-05 Task 5 pre-commit verification confirms every target file exists at HEAD and every path/anchor resolves before the atomic commit; per-task grep gates also assert link presence. |
| T-92-02 | Information Disclosure | Markdown content of edited hubs | accept | No PII, secrets, credentials, or sensitive data are added — only internal doc links and operator-facing diagnostic command names (`ls`, `sw_vers`) already public in the linked runbooks. No new disclosure surface. |
| T-92-03 | Spoofing / Repudiation / DoS / Elevation | — | accept | Not applicable — no code execution, no auth, no service, no input parsing. Static documentation cannot be spoofed, cannot elevate privilege, and presents no denial-of-service surface. |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (RESEARCH.md "Package Legitimacy Audit: Not applicable — installs no external packages"). No supply-chain surface; no legitimacy checkpoint required. |

**Security enforcement note:** Per the phase security gate, this block intentionally does not manufacture threats that do not exist. The single legitimate risk class (dead internal links) is a correctness concern fully covered by the D-05 anchor-resolution verification.
</threat_model>

<verification>
Phase-level verification (after Task 5 commit):

1. **Atomic-commit shape (D-05):** `git diff HEAD~1 --name-only` lists EXACTLY: `docs/index.md`, `docs/common-issues.md`, `docs/quick-ref-l2.md`, `docs/decision-trees/06-macos-triage.md` — no more, no less. No content file, validator, or blob-pin file present.
2. **index.md (D-01):** `01` and `02` each appear in 2 rows (L1+L2); `#30` in exactly 1 row (L2 only); `Kandji/Iru → Intune` both-names label present; `00-ade-lifecycle.md` rows byte-unchanged; Admin Setup table unchanged.
3. **common-issues.md (D-02):** Both new `###` headings present inside the macOS section; `No L1 runbook — escalate to L2` reused verbatim; #30 and #27 linked; no same-tenant key-survival assertion.
4. **quick-ref-l2.md (D-03):** `#### MDM Migration Diagnostics` block adds ONLY `ls /Library/Kandji/` + `sw_vers -productVersion`; `app-sso platform -s` still appears once (not duplicated); existing commands cross-referenced by anchor; #30 runbook bullet appended; no `profiles list` added.
5. **06-macos-triage.md (D-04 + 4.2 fix):** MACE3 node + `click MACE3` + `class MACE1,MACE2,MACE3 escalateL2`; MAC1 "How to Check" exception clause appended; 1 new Routing Verification row; 1 new Version History row (2026-06-25); ≤3-edge invariant preserved.
6. **Navigation-last invariant:** All four link targets (01/02/#30/#27) confirmed present at HEAD~1 (i.e., committed before the nav-hub commit).
</verification>

<success_criteria>
NAV-01 satisfied — all four ROADMAP Phase 92 success criteria met:

1. `docs/index.md` has rows for `01` and `02` in BOTH L1 and L2 macOS tables, and a row for L2 #30 in the L2 table; all referenced files confirmed to exist before the nav-hub commit. ✅
2. `docs/common-issues.md` macOS section has an MDM migration failure entry routing to L2 #30 and a post-migration PSSO re-registration failure entry cross-linking L2 #27 and L2 #30. ✅
3. `docs/quick-ref-l2.md` macOS section surfaces the net-new migration diagnostic commands and cross-references the existing `app-sso platform -s` / `profiles status -type enrollment` by anchor, plus an L2 #30 bullet. ✅
4. `docs/decision-trees/06-macos-triage.md` has a migration-failure leaf (MACE3) routing to L2 #30, with the mandatory MAC1 deadline-lockout disambiguation. ✅
5. All four edits shipped in a single atomic commit after D-05 pre-commit content-existence + anchor-resolution verification (navigation-last invariant fully satisfied). ✅
</success_criteria>

<output>
Create `.planning/phases/92-navigation-hub-integration/92-01-SUMMARY.md` when done.
</output>
