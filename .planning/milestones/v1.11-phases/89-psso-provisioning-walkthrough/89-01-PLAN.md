---
phase: 89-psso-provisioning-walkthrough
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/macos-lifecycle/01-psso-provisioning-walkthrough.md
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04]
must_haves:
  truths:
    - "Operator can identify which path (A1 standard / A2 ADE-during-SA) applies from a selector table that is the first content after frontmatter"
    - "Operator can follow the standard post-enrollment (A1) journey from enrollment profile to PSSO-registered end user through a shared stage spine"
    - "Operator sees a prominent, delimited macOS-26-gated A2 divergence callout beginning at the Company Portal stage with the three-policy same-static-group rule as the most-prominent risk"
    - "Operator runs app-sso platform -s verification gates at registration stages and a final per-path confirmation gate showing the full REGISTERED end-state"
    - "Operator is told userless devices never register (doc-level scope callout) and is routed to L1 #35/#36 + L2 #27 at the PSSO registration stage with no inline triage"
  artifacts:
    - path: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md"
      provides: "Consolidated PSSO provisioning walkthrough, both A1 and A2 paths"
      contains: "## Which Path"
    - path: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md"
      provides: "macOS-26-gated A2 divergence callout with freshness stamp"
      contains: "last_verified: 2026-06-24"
  key_links:
    - from: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md"
      to: "docs/admin-setup-macos/07-platform-sso-setup.md"
      via: "link-not-copy Behind the Scenes / Prerequisites reference"
      pattern: "07-platform-sso-setup\\.md"
    - from: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md"
      to: "docs/l1-runbooks/35-macos-sso-sign-in-failure.md"
      via: "failure-escalation cross-link at PSSO registration stage"
      pattern: "35-macos-sso-sign-in-failure\\.md"
    - from: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md"
      to: "docs/l2-runbooks/27-macos-sso-investigation.md"
      via: "failure-escalation cross-link at PSSO registration stage"
      pattern: "27-macos-sso-investigation\\.md"
---

<objective>
Author the new consolidated PSSO provisioning walkthrough `docs/macos-lifecycle/01-psso-provisioning-walkthrough.md` as a true sibling of `00-ade-lifecycle.md`, covering BOTH delivery paths in a single file:
- A1 — standard post-enrollment (all supported macOS)
- A2 — ADE-during-Setup-Assistant zero-click (macOS 26+ hard gate)

Implements all four locked authoring decisions: D-04 (selector-first opening), D-02 (single shared spine + delimited multi-stage macOS-26 divergence callout), D-01 (hybrid per-stage anatomy), and D-03 (`app-sso platform -s` gates at registration stages + per-path final gate). Covers PROV-01, PROV-02, PROV-03, and the cross-link half of PROV-04 (link-not-copy to 00/02/07 + L1 #35/#36 + L2 #27). The reciprocal See Also edits into 00/02/07 are Plan 89-02.

Purpose: An operator no longer assembles the macOS PSSO provisioning journey by hopping between guides 00/02/07 — one file threads the full journey with verification gates and escalation links.
Output: One new Markdown file (no code, no packages, no nav-hub edits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-psso-provisioning-walkthrough/89-CONTEXT.md
@.planning/phases/89-psso-provisioning-walkthrough/89-RESEARCH.md
@.planning/phases/89-psso-provisioning-walkthrough/89-PATTERNS.md
@docs/macos-lifecycle/00-ade-lifecycle.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author opening structure + shared spine (A1 path) with D-04 selector, D-02 spine, D-01 base anatomy, D-03 A1 gates</name>
  <files>docs/macos-lifecycle/01-psso-provisioning-walkthrough.md</files>
  <read_first>
    - docs/macos-lifecycle/00-ade-lifecycle.md (the sibling template — clone frontmatter shape, version-gate blockquote, How-to-Use, Prerequisites checklist, Mermaid pipeline, Stage Summary Table, per-stage 4-block anatomy; read lines 1-120 for the opening and any one full stage for the 4-block shape)
    - .planning/phases/89-psso-provisioning-walkthrough/89-PATTERNS.md (frontmatter/opening/anatomy/admonition/verification-gate/Stage-Summary-Table patterns with exact format for the new doc)
    - .planning/phases/89-psso-provisioning-walkthrough/89-RESEARCH.md (A1 Ordered Stages 8-stage table; A1 facts table; Verification Gate Block example; Path-Divergence Selector pattern; the userless scope callout text)
    - .planning/phases/89-psso-provisioning-walkthrough/89-CONTEXT.md (D-01, D-02, D-03, D-04 locked decisions and execution rules)
    - docs/admin-setup-macos/07-platform-sso-setup.md (link-not-copy target for PSSO Settings Catalog reference — link, do not copy)
    - docs/admin-setup-macos/02-enrollment-profile.md (link-not-copy target for enrollment-profile reference — link, do not copy)
  </read_first>
  <action>
    Create the new file with frontmatter mirroring guide-00 exactly: `last_verified: 2026-06-24`, `review_by: 2026-09-24`, `applies_to: ADE`, `audience: all`, `platform: macOS` (per D-04 sibling-consistency rule and RESEARCH frontmatter).

    Immediately after frontmatter emit a version/platform-gate blockquote in the `> **Label:** text` house admonition style (no callout fences), then the H1 title.

    Then emit content in this exact order (D-04 — selector is the FIRST content element after the gate/title, BEFORE Prerequisites):
    1. A one-liner "How to Use" sentence naming all three roles (L1 Service Desk / L2 Desktop Engineering / Intune Admin) — compressed from guide-00's full How-to-Use.
    2. `## Which Path Is Right for You?` — the path-divergence selector table with columns Path / macOS Requirement / Company Portal / PSSO Registers / Use When, with one A1 row and one A2 row (see RESEARCH Pattern 1 for the exact cell values: A1 = macOS 13+, CP 5.2404.0+ VPP, registers at desktop after "Registration Required"; A2 = macOS 26+ hard gate, CP 5.2604.0+ LOB-not-VPP, registers inside Setup Assistant). Immediately follow with the doc-level userless scope callout as a single-paragraph `>` blockquote: userless / no-user-affinity devices never reach PSSO registration; this walkthrough covers user-affinity enrollments only (D-03 execution rule — this is a scope callout, NOT a gate).
    3. `### Prerequisites` — a `- [ ]` checkbox list (guide-00 Prerequisites style). Enumerate every A2 hard-gate precondition (CP 5.2604.0 LOB, `EnableRegistrationDuringSetup`, three-policy same-Assigned-static-user-group, SmartCard exclusion) AND link (do NOT copy) guide-00's ADE prerequisites via `[macOS ADE Lifecycle](00-ade-lifecycle.md)` (D-04 execution rule).
    4. `## The PSSO Provisioning Pipeline` — a fenced `mermaid graph TD` diagram showing the shared spine stages then a diamond branch node to the A1 desktop-registration path and the A2 in-Setup-Assistant path, converging at the verify step (RESEARCH Mermaid example; exact labels are discretion).
    5. `## Stage Summary Table` — columns Stage / Actor / Location / What Happens / Key Pitfall PLUS a `Path` column (A1 / A2 / Both per D-04 execution rule). Shared spine rows = `Both`; A1-desktop-registration row = `A1`; A2 in-SA row = `A2`.

    Then author the shared-spine stages and the A1 registration stage using guide-00's per-stage anatomy. Each `## Stage N: [Name]` carries the 4 base blocks in order: `### What the Admin Sees`, `### What Happens`, `### Behind the Scenes`, `### Watch Out For` (D-01 base anatomy). Source the A1 8-stage sequence from RESEARCH "A1 Ordered Stages" (enrollment profile + PSSO policy → ADE token sync → PSSO policy assigned to static user groups → Company Portal deployed → ADE Setup Assistant → Await Configuration → Desktop + "Registration Required" → Verify). Keep every `### Behind the Scenes` block LINK-HEAVY: where it would repeat guide-00/07/02 reference content, instead write "See [target, section]" and link to `00-ade-lifecycle.md`, `../admin-setup-macos/07-platform-sso-setup.md`, or `../admin-setup-macos/02-enrollment-profile.md` (D-01 execution rule + link-not-copy; do NOT inline their settings tables).

    At the A1 registration stage (the "Registration Required" desktop stage where Device Registration and User Registration both transition to REGISTERED — the D-03 registration-state-change stage), add the two D-01 extra blocks immediately after `### Watch Out For`: `### What the User Sees` (paragraph describing the "Registration Required" Notification Center prompt → MFA → WPJ Secure Enclave key written) and `### How to Verify`. The How-to-Verify block instructs the operator to open Terminal and run `app-sso platform -s`, and to confirm BOTH lines `Device Registration: REGISTERED` and `User Registration: REGISTERED` (D-03 — these are the ONLY sourced output strings; NEVER fabricate partial/intermediate states such as PENDING or NOT REGISTERED; for incomplete states instruct "wait and re-run"). Label the gate explicitly as checking Device-Registration and User-Registration state. In the same How-to-Verify block, place the failure-escalation cross-links (D-03 + PROV-04): if either line differs, route to `[L1 #35](../l1-runbooks/35-macos-sso-sign-in-failure.md)`, `[L1 #36](../l1-runbooks/36-macos-secure-enclave-key.md)`, and `[L2 #27](../l2-runbooks/27-macos-sso-investigation.md)` — cross-link only, NO inline triage.

    Use the verification command `app-sso platform -s` (NOT `security find-certificate`, which returns false negatives for Secure-Enclave-stored keys — note this in the relevant Watch Out For). Do NOT edit any nav-hub file. Do NOT place the A2 divergence callout or A2 final gate here — those belong to Task 2 (this task ends the file mid-document; Task 2 appends the A2 divergence content and the sibling footers).
  </action>
  <verify>
    <automated>test -f docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "audience: all" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "## Which Path" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "app-sso platform -s" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "Device Registration: REGISTERED" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "35-macos-sso-sign-in-failure.md" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "27-macos-sso-investigation.md" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/macos-lifecycle/01-psso-provisioning-walkthrough.md` exists with frontmatter containing `last_verified: 2026-06-24`, `review_by: 2026-09-24`, `applies_to: ADE`, `audience: all`, `platform: macOS`.
    - The first content element after the version-gate blockquote + H1 + one-liner How-to-Use is the `## Which Path Is Right for You?` selector table, which appears BEFORE the `### Prerequisites` heading (D-04 ordering — selector precedes prereqs).
    - Selector table contains both an A1 row (macOS 13+, 5.2404.0, VPP) and an A2 row (macOS 26+, 5.2604.0, LOB).
    - A doc-level userless scope callout (`>` blockquote) states userless / no-user-affinity devices never register and is NOT phrased as a verification gate.
    - `### Prerequisites` is a `- [ ]` checkbox list enumerating CP 5.2604.0 LOB, `EnableRegistrationDuringSetup`, three-policy same-Assigned-static-user-group, and SmartCard exclusion, and links to `00-ade-lifecycle.md` rather than copying its ADE prereqs.
    - `## Stage Summary Table` contains a `Path` column with at least one `Both`, one `A1`, and one `A2` value.
    - Every shared-spine `## Stage` heading carries `### What the Admin Sees`, `### What Happens`, `### Behind the Scenes`, `### Watch Out For` in that order.
    - The A1 registration stage additionally carries `### What the User Sees` and `### How to Verify` after `### Watch Out For`.
    - The How-to-Verify block cites `app-sso platform -s` and both lines `Device Registration: REGISTERED` and `User Registration: REGISTERED`, and contains NO partial/intermediate state strings (e.g. `PENDING`, `NOT REGISTERED`).
    - Failure-escalation links to L1 #35, L1 #36, and L2 #27 are present at the registration stage with no inline troubleshooting prose duplicating those runbooks.
    - At least one `### Behind the Scenes` block links to `07-platform-sso-setup.md` or `02-enrollment-profile.md` rather than reproducing their settings tables (link-not-copy).
    - No nav-hub file (`docs/index.md`, `docs/common-issues.md`, `docs/quick-ref-l2.md`, `docs/decision-trees/06-macos-triage.md`) was modified.
  </acceptance_criteria>
  <done>The new walkthrough file exists with the D-04 selector-first opening, userless scope callout, A2-aware Prerequisites, Path-columned Stage Summary Table, Mermaid pipeline, and all shared-spine + A1 registration stages using the D-01 hybrid anatomy with the D-03 A1 verification gate and escalation cross-links. A2 divergence callout and sibling footers remain for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Append A2 divergence callout (D-02), A2 final gate (D-03), and sibling footers (See Also / Glossary Quick Reference / Version History)</name>
  <files>docs/macos-lifecycle/01-psso-provisioning-walkthrough.md</files>
  <read_first>
    - docs/macos-lifecycle/01-psso-provisioning-walkthrough.md (the file Task 1 created — append to it; do not rewrite Task 1 content)
    - docs/macos-lifecycle/00-ade-lifecycle.md (See Also three-subsection structure lines 378-396; Glossary Quick Reference lines 399-411; Version History lines 414-419 — clone these footer shapes)
    - docs/admin-setup-macos/07-platform-sso-setup.md (the ADE-during-Setup-Assistant "Advanced/Optional" section + SmartCard Exclusion section + freshness-stamp provenance line — link-not-copy target and stamp-format source)
    - .planning/phases/89-psso-provisioning-walkthrough/89-PATTERNS.md (A2 divergence callout pattern, freshness-stamp pattern, See Also / Glossary / Version History footer patterns with exact required entries)
    - .planning/phases/89-psso-provisioning-walkthrough/89-RESEARCH.md (Path A2 facts table; Pitfalls 1-3 for A2; Pattern 3 A2 divergence callout; Pattern 4 A2 gate; freshness-stamp example)
    - .planning/phases/89-psso-provisioning-walkthrough/89-CONTEXT.md (D-02 and D-03 execution rules — A2 is single-point divergence at CP onward, NOT scattered; A2 registers inside Setup Assistant with no desktop notification)
  </read_first>
  <action>
    Append to the file created in Task 1 (do not modify Task 1 sections). Implement D-02: a single clearly-delimited MULTI-STAGE macOS-26+ A2 divergence callout, anchored at the Company Portal stage and extending downstream — NOT scattered across the early shared stages (D-02 execution rule: A2 is single-point divergence, CP onward).

    Emit the A2 divergence as a delimited `>` multi-paragraph blockquote (house admonition style) introduced as the macOS-26+ ADE-during-Setup-Assistant path. It MUST cover, with the three-policy rule as the MOST PROMINENT element (highest-risk misconfig per RESEARCH Pitfall 1): (a) CP 5.2604.0 LOB floor vs VPP; (b) the three-policy same-Assigned-static-user-group rule — PSSO Settings Catalog policy + Company Portal LOB app + ADE enrollment profile all assigned to the SAME Assigned (static) user groups; dynamic groups break it; device groups break it; recovery = wipe; (c) `EnableRegistrationDuringSetup` (Settings Catalog field: Authentication > Extensible single sign-on > Platform SSO > Enable Registration During Setup: Enabled); (d) SmartCard exclusion — NOT supported on A2; use Secure Enclave (recommended) or Password; (e) wipe-only misconfiguration recovery — there is no in-place fix; (f) A2 reaches full REGISTERED INSIDE Setup Assistant with NO desktop "Registration Required" notification. Include a requirements table (e.g. A2 Requirement / Value rows) per RESEARCH Pattern 3. Keep the prose link-not-copy: for the CP LOB setup and the Settings Catalog field detail, link to `../admin-setup-macos/07-platform-sso-setup.md` rather than reproducing its config tables.

    Relocate the A2 final verification gate accordingly (D-02 (f) + D-03): because A2 registers in Setup Assistant with no desktop notification, the A2 per-path final gate is a `### How to Verify` block placed at the A2 SA-completion point (immediately after Setup Assistant exits) — instruct running `app-sso platform -s` and confirming BOTH `Device Registration: REGISTERED` and `User Registration: REGISTERED` (single-shot: both lines REGISTERED). Add the A2 `### What the User Sees` block noting the user is prompted for Entra credentials at least twice during Setup Assistant and arrives already signed in at the desktop (link guide-07's ADE-during-SA section rather than scripting the exact screen flow). Do NOT assert A2 sends a "Registration Required" notification. Do NOT fabricate partial `app-sso platform -s` output. The A2 gate must NOT be placed at a desktop notification (there is none on A2).

    Place the freshness stamp on the A2/macOS-26-gated content: the final line INSIDE the A2 divergence blockquote must be an italic provenance stamp matching guide-07's format, containing literally `last_verified: 2026-06-24` and `review_by: 2026-09-24` and a 90-day re-confirm instruction (re-confirm macOS 26 GA + CP 5.2604.0 LOB floor against current Microsoft Learn). Every macOS-26-gated section in the doc must carry this stamp.

    Then append the three sibling footers cloned from guide-00 (D-04 sibling-consistency / Claude's Discretion sibling rule):
    1. `## See Also` with the three bold sub-groups `**Terminology and Concepts:**`, `**Technical References:**`, `**Related Guides:**` (use ` -- ` double-dash-space separator before each description, matching guide-00). Related Guides MUST include: `[macOS ADE Lifecycle Overview](00-ade-lifecycle.md)`, `[Platform SSO Setup](../admin-setup-macos/07-platform-sso-setup.md)`, `[Enrollment Profile Configuration](../admin-setup-macos/02-enrollment-profile.md)`, `[L1 #35](../l1-runbooks/35-macos-sso-sign-in-failure.md)`, `[L1 #36](../l1-runbooks/36-macos-secure-enclave-key.md)`, `[L2 #27](../l2-runbooks/27-macos-sso-investigation.md)`.
    2. `## Glossary Quick Reference` with columns `Term | Definition | First Appears`, glossary-anchored term links into `../_glossary-macos.md`, and the intro sentence pointing to the full glossary. Include at minimum PSSO/Platform SSO, WPJ (Workplace Join), Secure Enclave, `app-sso platform -s`, ADE, Setup Assistant, LOB app.
    3. `## Version History` with a `| Date | Change |` two-column table whose initial row is `| 2026-06-24 | Phase 89 (PROV-01..04): initial PSSO provisioning walkthrough (A1 + A2 paths) |`.

    Do NOT edit any nav-hub file. Do NOT edit guides 00/02/07 in this task (their reciprocal See Also edits are Plan 89-02).
  </action>
  <verify>
    <automated>grep -q "last_verified: 2026-06-24" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "review_by: 2026-09-24" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "5.2604.0" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "EnableRegistrationDuringSetup\|Enable Registration During Setup" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -qi "static.*user group\|Assigned (static)" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -qi "SmartCard\|Smart Card" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "## See Also" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "## Glossary Quick Reference" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && grep -q "Phase 89 (PROV-01..04)" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md && ! grep -qi "Registration: PENDING\|Registration: NOT REGISTERED" docs/macos-lifecycle/01-psso-provisioning-walkthrough.md</automated>
  </verify>
  <acceptance_criteria>
    - A single delimited macOS-26+ A2 divergence callout (`>` multi-paragraph blockquote) is present, anchored at/after the Company Portal stage, and is NOT split into per-early-stage A2 notes (D-02 single-point divergence).
    - The callout names all six required elements: CP 5.2604.0 LOB-vs-VPP, three-policy same-Assigned-static-user-group rule (presented as the most prominent risk with "recovery = wipe"), `EnableRegistrationDuringSetup` / Enable Registration During Setup, SmartCard exclusion, wipe-only recovery, and A2-registers-inside-Setup-Assistant-with-no-desktop-notification.
    - An A2 `### How to Verify` final gate exists at the SA-completion point citing `app-sso platform -s` with both REGISTERED lines, and is NOT tied to a "Registration Required" desktop notification.
    - No partial/intermediate `app-sso platform -s` output strings appear anywhere in the file (grep for `Registration: PENDING` / `Registration: NOT REGISTERED` returns nothing).
    - A freshness stamp containing both `last_verified: 2026-06-24` and `review_by: 2026-09-24` appears inside the A2 divergence callout.
    - `## See Also` exists with the three guide-00 bold sub-groups and Related Guides includes 00, 07, 02, L1 #35, L1 #36, L2 #27.
    - `## Glossary Quick Reference` exists with the `Term | Definition | First Appears` schema and glossary-anchored links.
    - `## Version History` exists with the `Phase 89 (PROV-01..04)` initial row.
    - Guides 00/02/07 and all four nav-hub files are unmodified by this task.
  </acceptance_criteria>
  <done>The walkthrough is complete: A2 divergence callout (with three-policy rule prominent + freshness stamp), relocated A2 final gate, and the three sibling footers (See Also / Glossary Quick Reference / Version History) all present and matching guide-00 conventions. The file reads as a true sibling of 00-ade-lifecycle.md and is ready for the reciprocal See Also edits in Plan 89-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only phase. No executable code, no inputs, no auth surface, no network calls, no package installs. The only artifact is Markdown prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Tampering (content integrity) | Documented `app-sso platform -s` verification output | mitigate | D-03 execution rule enforced: cite ONLY the sourced full end-state (`Device Registration: REGISTERED` / `User Registration: REGISTERED`); fabricated partial states are gated out by a negative grep in Task 2 verify. |
| T-89-02 | Information disclosure / wrong-command harm | Verification command choice | mitigate | Doc mandates `app-sso platform -s` and explicitly warns against `security find-certificate` (false negatives for Secure-Enclave keys) — prevents operators acting on a misleading negative. |
| T-89-03 | Tampering (destructive guidance) | A2 misconfiguration recovery wording | accept | Wipe-only recovery is the Apple/Microsoft-authoritative procedure (RESEARCH VERIFIED); documenting it accurately is correct, not a risk. No softer in-place fix exists to recommend. |

No high-severity threats: no executable code, no inputs, no auth surface; documentation content only. Content-integrity risks (wrong/destructive command) are mitigated by the D-03 fabrication ban and the explicit verification-command guidance, both enforced by Task verify grep checks.
</threat_model>

<verification>
- New file exists at `docs/macos-lifecycle/01-psso-provisioning-walkthrough.md`.
- Selector table is the first content element after frontmatter/gate/title and precedes Prerequisites (D-04 / PROV-03).
- Both A1 and A2 paths are documented; A2 content is a single delimited macOS-26-gated callout (D-02 / PROV-02).
- `app-sso platform -s` gates appear at the A1 registration stage and the A2 SA-completion point, each citing only the full REGISTERED end-state (D-03 / PROV-03).
- Userless scope callout present as doc-level note, not a gate (PROV-03).
- Cross-links to 00/02/07 (link-not-copy) and L1 #35/#36 + L2 #27 present; no inline triage (PROV-04 cross-link half).
- Freshness stamps (`last_verified: 2026-06-24` / `review_by: 2026-09-24`) on the A2/macOS-26-gated section.
- No nav-hub files and no guide-00/02/07 content edits in this plan.
</verification>

<success_criteria>
Plan 89-01 is complete when `docs/macos-lifecycle/01-psso-provisioning-walkthrough.md` exists as a true sibling of guide-00, opens with the path-divergence selector, threads both A1 and A2 paths through the D-01 hybrid anatomy with D-03 verification gates, carries the D-02 A2 divergence callout with freshness stamps and the three-policy rule as the most prominent risk, links link-not-copy to 00/02/07 and escalates to L1 #35/#36 + L2 #27, and includes See Also / Glossary Quick Reference / Version History footers — all while leaving guides 00/02/07 and every nav-hub file untouched.
</success_criteria>

<output>
Create `.planning/phases/89-psso-provisioning-walkthrough/89-01-SUMMARY.md` when done.
</output>
