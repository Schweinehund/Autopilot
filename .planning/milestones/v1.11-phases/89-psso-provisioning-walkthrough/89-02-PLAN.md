---
phase: 89-psso-provisioning-walkthrough
plan: 02
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - docs/macos-lifecycle/00-ade-lifecycle.md
  - docs/admin-setup-macos/07-platform-sso-setup.md
  - docs/admin-setup-macos/02-enrollment-profile.md
autonomous: true
requirements: [PROV-04]
must_haves:
  truths:
    - "A reader of guide-00 can discover the new provisioning walkthrough via a See Also > Related Guides entry that resolves"
    - "A reader of guide-07 can discover the new provisioning walkthrough via a See Also entry that resolves and matches guide-07's See Also house style"
    - "A reader of guide-02 can discover the new provisioning walkthrough via a See Also entry that resolves and matches guide-02's bare-link house style"
    - "Every cross-link added in this phase resolves to an existing file (no broken links)"
  artifacts:
    - path: "docs/macos-lifecycle/00-ade-lifecycle.md"
      provides: "Reciprocal See Also link to the new walkthrough"
      contains: "01-psso-provisioning-walkthrough.md"
    - path: "docs/admin-setup-macos/07-platform-sso-setup.md"
      provides: "Reciprocal See Also link to the new walkthrough"
      contains: "01-psso-provisioning-walkthrough.md"
    - path: "docs/admin-setup-macos/02-enrollment-profile.md"
      provides: "Reciprocal See Also link to the new walkthrough"
      contains: "01-psso-provisioning-walkthrough.md"
  key_links:
    - from: "docs/macos-lifecycle/00-ade-lifecycle.md"
      to: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md"
      via: "See Also > Related Guides bullet"
      pattern: "01-psso-provisioning-walkthrough\\.md"
    - from: "docs/admin-setup-macos/07-platform-sso-setup.md"
      to: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md"
      via: "See Also bullet (cross-directory ../ path)"
      pattern: "\\.\\./macos-lifecycle/01-psso-provisioning-walkthrough\\.md"
    - from: "docs/admin-setup-macos/02-enrollment-profile.md"
      to: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md"
      via: "See Also bullet (cross-directory ../ path, bare link)"
      pattern: "\\.\\./macos-lifecycle/01-psso-provisioning-walkthrough\\.md"
---

<objective>
Add the reciprocal "See Also" entries pointing to the new `01-psso-provisioning-walkthrough.md` into the three content guides (`00-ade-lifecycle.md`, `07-platform-sso-setup.md`, `02-enrollment-profile.md`), completing the bidirectional cross-link half of PROV-04. Each entry MUST match the destination file's OWN See Also house style — the three files use three different styles (per PATTERNS.md), so the entries are not identical.

These are content-phase edits (append-only See Also additions to existing content guides), NOT navigation-last hub edits. Guides 00/02/07 are frozen for v1.11 EXCEPT for exactly these reciprocal See Also additions.

Purpose: The discovery is bidirectional — an operator reading any of the three reference guides finds the new consolidated walkthrough, and the walkthrough (authored in 89-01) links back to all three.
Output: Three single-line See Also additions; no new files, no nav-hub edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-psso-provisioning-walkthrough/89-PATTERNS.md
@.planning/phases/89-psso-provisioning-walkthrough/89-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reciprocal See Also entries to guides 00, 07, and 02 (each matching its own house style)</name>
  <files>docs/macos-lifecycle/00-ade-lifecycle.md, docs/admin-setup-macos/07-platform-sso-setup.md, docs/admin-setup-macos/02-enrollment-profile.md</files>
  <read_first>
    - .planning/phases/89-psso-provisioning-walkthrough/89-PATTERNS.md (the three reciprocal-edit sections — they give the EXACT insertion point, exact bullet text, and the per-file style rule for each of 00/07/02; the three styles differ)
    - .planning/phases/89-psso-provisioning-walkthrough/89-RESEARCH.md ("Existing Guide Inspection — Reciprocal See Also Edit Targets" — confirms insertion points and bullet wording per file)
    - docs/macos-lifecycle/00-ade-lifecycle.md (See Also > Related Guides sub-section ~lines 391-396 — the existing "Platform SSO Setup" bullet is the insertion anchor; uses three bold sub-groups + ` -- ` descriptions)
    - docs/admin-setup-macos/07-platform-sso-setup.md (flat See Also ~lines 140-147 — the "macOS ADE Lifecycle Overview" bullet ~line 146 is the insertion anchor; flat list, some entries carry ` -- ` descriptions)
    - docs/admin-setup-macos/02-enrollment-profile.md (flat See Also ~lines 128-133 — the "macOS ADE Lifecycle Overview" bullet ~line 132 is the insertion anchor; flat list, BARE links with NO descriptions)
  </read_first>
  <action>
    Make exactly three single-bullet additions, one per file. Do NOT change any other line, heading, or existing bullet in any of the three files (guides 00/02/07 are frozen except for these reciprocal See Also additions). Insert each new bullet immediately AFTER the named anchor bullet in that file.

    1. `docs/macos-lifecycle/00-ade-lifecycle.md` — under `**Related Guides:**`, immediately after the existing `[Platform SSO Setup](../admin-setup-macos/07-platform-sso-setup.md)` bullet, add a bullet linking `[PSSO Provisioning Walkthrough](01-psso-provisioning-walkthrough.md)` (same-directory bare filename — NO `../`) with a ` -- ` double-dash-space description noting it is the end-to-end operator walkthrough threading the ADE lifecycle through Platform SSO registration for both standard post-enrollment (A1) and ADE-during-Setup-Assistant (A2, macOS 26+) paths. This matches guide-00's three-sub-group + description house style.

    2. `docs/admin-setup-macos/07-platform-sso-setup.md` — in the flat `## See Also` list, immediately after the existing `[macOS ADE Lifecycle Overview](../macos-lifecycle/00-ade-lifecycle.md)` bullet, add a bullet linking `[PSSO Provisioning Walkthrough](../macos-lifecycle/01-psso-provisioning-walkthrough.md)` (cross-directory `../macos-lifecycle/` path) WITH a ` -- ` description (end-to-end operator walkthrough from enrollment profile through Platform SSO registration, standard and ADE-during-Setup-Assistant paths) — matching guide-07's style for entries that carry descriptions (e.g. its Capability Matrix bullet). Flat list — NO bold sub-group heading.

    3. `docs/admin-setup-macos/02-enrollment-profile.md` — in the flat `## See Also` list, immediately after the existing `[macOS ADE Lifecycle Overview](../macos-lifecycle/00-ade-lifecycle.md)` bullet, add a BARE bullet linking `[PSSO Provisioning Walkthrough](../macos-lifecycle/01-psso-provisioning-walkthrough.md)` with NO ` -- ` description — guide-02's See Also house style is bare links with no descriptions on any entry; adding a description would break that file's style (PATTERNS.md critical constraint #3).

    Use relative paths only: bare filename for the same-directory link in guide-00; `../macos-lifecycle/...` for the cross-directory links in 07 and 02. Do NOT edit any nav-hub file (`docs/index.md`, `docs/common-issues.md`, `docs/quick-ref-l2.md`, `docs/decision-trees/06-macos-triage.md`). Do NOT add a Version History row to these frozen guides (the reciprocal See Also edit is the only permitted change).
  </action>
  <verify>
    <automated>grep -q "(01-psso-provisioning-walkthrough.md)" docs/macos-lifecycle/00-ade-lifecycle.md && grep -q "(../macos-lifecycle/01-psso-provisioning-walkthrough.md)" docs/admin-setup-macos/07-platform-sso-setup.md && grep -q "(../macos-lifecycle/01-psso-provisioning-walkthrough.md)" docs/admin-setup-macos/02-enrollment-profile.md && test -f docs/macos-lifecycle/01-psso-provisioning-walkthrough.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/macos-lifecycle/00-ade-lifecycle.md` contains a new `**Related Guides:**` bullet linking `01-psso-provisioning-walkthrough.md` (bare same-directory filename, no `../`) WITH a ` -- ` description, placed directly after the Platform SSO Setup bullet.
    - `docs/admin-setup-macos/07-platform-sso-setup.md` contains a new flat See Also bullet linking `../macos-lifecycle/01-psso-provisioning-walkthrough.md` WITH a ` -- ` description, placed directly after the macOS ADE Lifecycle Overview bullet.
    - `docs/admin-setup-macos/02-enrollment-profile.md` contains a new flat See Also bullet linking `../macos-lifecycle/01-psso-provisioning-walkthrough.md` as a BARE link with NO ` -- ` description, placed directly after the macOS ADE Lifecycle Overview bullet.
    - The link target `docs/macos-lifecycle/01-psso-provisioning-walkthrough.md` exists (89-01 dependency satisfied), so all three links resolve.
    - No other line in any of the three files changed (only one bullet added per file); no Version History row added to the frozen guides.
    - No nav-hub file modified.
  </acceptance_criteria>
  <done>All three content guides carry a reciprocal See Also link to the new walkthrough, each in its own file's house style (00 = sub-grouped + description; 07 = flat + description; 02 = flat + bare), every link resolves to the existing new file, and no other content or nav-hub file was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only phase. No executable code, no inputs, no auth surface. Edits add three Markdown cross-link bullets to existing content guides. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04 | Tampering (broken link / dangling reference) | Reciprocal See Also links into the new file | mitigate | Plan ordered Wave 2 with `depends_on: 89-01` so the link target exists before the links are added; Task verify asserts both the links and the target file's existence. |
| T-89-05 | Tampering (frozen-surface violation) | Guides 00/02/07 are frozen for v1.11 except these See Also additions | mitigate | Action restricts the change to exactly one appended bullet per file with named insertion anchors; acceptance criteria assert no other line changed and no Version History row added. |

No high-severity threats: no executable code, no inputs, no auth surface; documentation content only. The only content-integrity risk (a dangling link) is eliminated by the Wave-2 dependency ordering and the existence assertion in the verify step.
</threat_model>

<verification>
- All three reciprocal See Also entries point to `01-psso-provisioning-walkthrough.md` and resolve (target exists from 89-01).
- Each entry matches its destination file's house style (00 sub-grouped+description; 07 flat+description; 02 flat+bare).
- Only one bullet added per file; no other content changed; no Version History rows added to frozen guides.
- No nav-hub file edited.
</verification>

<success_criteria>
Plan 89-02 is complete when guides 00, 07, and 02 each carry exactly one reciprocal See Also link to the new walkthrough in their own house style, every link resolves to the file created in 89-01, and no other content or navigation-hub file was modified — completing the bidirectional cross-link requirement of PROV-04.
</success_criteria>

<output>
Create `.planning/phases/89-psso-provisioning-walkthrough/89-02-SUMMARY.md` when done.
</output>
