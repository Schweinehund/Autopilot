---
phase: 91-glossary-capability-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_glossary-macos.md
  - docs/_glossary.md
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - "All 10 inbound anchors from guide-02 lines 541-550 resolve to a heading in _glossary-macos.md (9 newly minted + #platform-sso already present)"
    - "The Kandji/Iru entry heading slugifies to single-hyphen #kandji-iru (not #kandji--iru)"
    - "No new '## Migration' H2 exists anywhere in the glossary family; all 9 entries attach to existing H2s"
    - "_glossary.md#tenant-migration gains a reciprocal '> See also:' link to _glossary-macos.md#mdm-migration"
    - "No glossary text asserts same-tenant Secure Enclave key survival; PSSO re-registration is described as always required post-migration"
  artifacts:
    - path: "docs/_glossary-macos.md"
      provides: "9 new ### term entries + Alphabetical Index updates + Version History row"
      contains: "### Kandji-Iru"
    - path: "docs/_glossary.md"
      provides: "reciprocal See also on ### Tenant migration + Version History row"
      contains: "_glossary-macos.md#mdm-migration"
  key_links:
    - from: "docs/macos-lifecycle/02-mdm-migration-psso.md (lines 541-550)"
      to: "docs/_glossary-macos.md headings"
      via: "inbound anchor links"
      pattern: "#(assign-device-management|deadline|kandji-iru|filevault-recovery-key|activation-lock-bypass|acme|profile-based-enrollment|app-sso|delete-device-record)"
    - from: "docs/_glossary.md#tenant-migration"
      to: "docs/_glossary-macos.md#mdm-migration"
      via: "> See also: blockquote line"
      pattern: "_glossary-macos\\.md#mdm-migration"
---

<objective>
Mint glossary entries for all 9 currently-dead inbound anchors that guide 02 (Phase 90, `02-mdm-migration-psso.md` lines 541–550) already links to, plus the `MDM Migration` entry REF-01 names, and add the reciprocal `> See also:` from the Windows glossary. This satisfies REF-01 as expanded by D-06.

Per D-01: all entries attach to existing H2s (NO new `## Migration` H2). Per D-06: mint all 9 dead anchors (`#platform-sso` already resolves — do nothing for it). Per D-02: migration-flow terms use the explicit-negative Windows-equivalent callout. Per D-03: the Deadline entry is concise (link-not-copy to guide 02 and L2 #30).

Purpose: complete the inbound link contract guide 02 shipped before these entries existed — leaving 6+ dead links indefinitely is a quality defect.
Output: edited `docs/_glossary-macos.md` (9 entries + index + version history + freshness stamps) and `docs/_glossary.md` (reciprocal see-also + version history). NEITHER file is blob-pinned, so no validator BASELINE update is involved in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-glossary-capability-matrix/91-CONTEXT.md
@.planning/phases/91-glossary-capability-matrix/91-RESEARCH.md
@.planning/phases/91-glossary-capability-matrix/91-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint the 9 new ### entries in _glossary-macos.md and update the Alphabetical Index + Version History + freshness stamps</name>
  <files>docs/_glossary-macos.md</files>
  <read_first>
    - docs/_glossary-macos.md (the file being modified — read frontmatter lines 1-7, Alphabetical Index line 17, the existing `## Device Management` entries `### ABM`/`### ABM Token`/`### APNs` lines 62-81, `## Enrollment` H2 line 21, `## Authentication` H2 line 121, and the `## Version History` table near line 150)
    - .planning/phases/91-glossary-capability-matrix/91-CONTEXT.md (D-01, D-02, D-03, D-06 + EXECUTION RULES; the deferred-ideas note that {#id} is NOT supported)
    - .planning/phases/91-glossary-capability-matrix/91-RESEARCH.md (Q1 anchor finding, Q2 slug table, Q3 the 6 beyond-REF-01 term bodies, Architecture Patterns H2-placement table and Alphabetical-Index insertion points)
    - .planning/phases/91-glossary-capability-matrix/91-PATTERNS.md (entry anatomy analogs, negative-callout template, slug-risk table, Version History row, frontmatter stamps)
    - docs/macos-lifecycle/02-mdm-migration-psso.md lines 541-550 (the 10 inbound anchor targets the headings must satisfy byte-for-byte)
  </read_first>
  <action>
    Add 9 new `### Term` entries under existing H2s (NO new `## Migration` H2). Heading text MUST match the slug table EXACTLY:
    - Under `## Device Management` (line 60): `### MDM Migration` (→ #mdm-migration), `### Assign Device Management` (→ #assign-device-management), `### Deadline` (→ #deadline), `### Kandji-Iru` (→ #kandji-iru — bare dash, NEVER `### Kandji / Iru` which slugifies to #kandji--iru), `### Delete Device Record` (→ #delete-device-record), `### FileVault Recovery Key` (→ #filevault-recovery-key), `### Activation Lock Bypass` (→ #activation-lock-bypass).
    - Under `## Enrollment` (line 21): `### Profile-Based Enrollment` (→ #profile-based-enrollment), `### ACME` (→ #acme).
    - Under `## Authentication` (line 121): `### app-sso` (→ #app-sso — bare, NEVER `### app-sso platform -s` which slugifies to #app-sso-platform--s; explain the `platform -s` subcommand in the body).
    Do NOT mint `### platform-sso` — `#platform-sso` already resolves (line 123). Do NOT duplicate it.
    Entry anatomy per PATTERNS analogs: 2-4 sentence body + a `> **Windows equivalent:**` blockquote (label preserved VERBATIM per Phase-59 D-15 anti-rename). For the migration-flow terms (MDM Migration, Assign Device Management, Deadline, Kandji-Iru, Delete Device Record, Activation Lock Bypass) use the D-02 NEGATIVE callout form. For FileVault Recovery Key use the affirmative BitLocker-recovery-key parallel per RESEARCH Q3#1 (D-02 governs migration concepts, not per-platform encryption-key terms) — state explicitly the BitLocker key is NOT destroyed on device-record deletion in most Intune configs. Kandji-Iru body MUST surface both names + October-2025 rebrand + "support portal URL unchanged at support.kandji.io". Deadline entry per D-03: one-sentence def + the 1–90-day range stated once + cross-links to guide 02 and L2 #30 (`docs/l2-runbooks/30-macos-mdm-migration-failure.md`); do NOT restate the full lockout UX. app-sso body shows the expected `Device Registration: REGISTERED` / `User Registration: REGISTERED` output (this code block is load-bearing and permitted inside the entry body) and cross-links to #platform-sso and Stage 9. NO entry may assert same-tenant Secure Enclave key survival; where PSSO is referenced, state re-registration is ALWAYS required post-migration (Phase-90 carried fact). Insert all 10 new display names into the `## Alphabetical Index` (line 17) pipe-separated list in alpha order at the insertion points named in RESEARCH Architecture Patterns (ACME before ADE; Activation Lock Bypass before ADE; app-sso before APNs; Assign Device Management before Await Configuration; Deadline after ADE; Delete Device Record after Deadline; FileVault Recovery Key after Enterprise SSO Plug-in; Kandji-Iru after Kerberos SSO Extension; MDM Migration after MAM-WE; Profile-Based Enrollment after Platform SSO). Append one Version History row dated 2026-06-24 describing the 9 added terms. Update frontmatter `last_verified: 2026-06-24` and `review_by: 2026-09-24`.
  </action>
  <verify>
    <automated>grep -q '### Kandji-Iru' docs/_glossary-macos.md && ! grep -q '### Kandji / Iru' docs/_glossary-macos.md && grep -q '### app-sso' docs/_glossary-macos.md && ! grep -q '### app-sso platform' docs/_glossary-macos.md && ! grep -q '## Migration' docs/_glossary-macos.md && for h in '### MDM Migration' '### Assign Device Management' '### Deadline' '### Delete Device Record' '### FileVault Recovery Key' '### Activation Lock Bypass' '### Profile-Based Enrollment' '### ACME'; do grep -qF "$h" docs/_glossary-macos.md || { echo "MISSING: $h"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/_glossary-macos.md contains '### Kandji-Iru' and does NOT contain '### Kandji / Iru'
    - docs/_glossary-macos.md contains '### app-sso' and does NOT contain '### app-sso platform -s'
    - docs/_glossary-macos.md contains all of: '### MDM Migration', '### Assign Device Management', '### Deadline', '### Delete Device Record', '### FileVault Recovery Key', '### Activation Lock Bypass', '### Profile-Based Enrollment', '### ACME'
    - docs/_glossary-macos.md does NOT contain '## Migration' (no new H2)
    - For each of the 9 minted headings, the GitHub default slug equals the corresponding guide-02 anchor target at lines 541-550 (manual slug check per RESEARCH Q2 table — Kandji-Iru→#kandji-iru and app-sso→#app-sso are the two highest-risk)
    - Alphabetical Index (line 17) contains a '[Display](#anchor)' entry for each of the 10 new display names in alpha order
    - docs/_glossary-macos.md frontmatter shows last_verified: 2026-06-24 and review_by: 2026-09-24
    - docs/_glossary-macos.md contains no text asserting same-tenant Secure Enclave key survival
  </acceptance_criteria>
  <done>All 9 dead inbound anchors from guide-02 lines 541-550 now resolve to a heading; Kandji-Iru slugifies to single-hyphen #kandji-iru; app-sso slugifies to #app-sso; index, version history, and freshness stamps updated; no new H2; no key-survival claim.</done>
</task>

<task type="auto">
  <name>Task 2: Add the reciprocal '> See also:' to _glossary.md#tenant-migration and append its Version History row</name>
  <files>docs/_glossary.md</files>
  <read_first>
    - docs/_glossary.md (the file being modified — read the `### Tenant migration` entry near line 121-123, the `### TPM`/`### OOBE` bare-`> See also:` blockquote analogs, and the `## Version History` table)
    - .planning/phases/91-glossary-capability-matrix/91-RESEARCH.md (Q6 reciprocal see-also finding + recommended addition text)
    - .planning/phases/91-glossary-capability-matrix/91-PATTERNS.md (_glossary.md reciprocal-see-also analog and the bare `> See also:` TPM form — do NOT invent a `> **Windows equivalent:**` label on a Windows-glossary entry)
    - .planning/phases/91-glossary-capability-matrix/91-CONTEXT.md (REF-01 reciprocal requirement; D-02 wipe-only fact)
  </read_first>
  <action>
    Append a new bare `> See also:` blockquote directly after the existing `### Tenant migration` prose (blank line between prose and blockquote, matching the TPM-entry bare-see-also form — do NOT add a `> **Windows equivalent:**` label). The line points to `[MDM Migration](_glossary-macos.md#mdm-migration)` and notes the distinction: macOS in-place wipe-free re-enrollment on macOS 26+ vs Windows tenant migration's required full device reset. Do NOT modify the existing prose, the `device-operations/04-tenant-migration.md` link, or any other entry. Append one Version History row dated 2026-06-24 describing the reciprocal see-also addition. If `_glossary.md` has a freshness-stamp frontmatter, update `last_verified`/`review_by` to 2026-06-24 / 2026-09-24 consistent with sibling convention (only if the file already carries those keys).
  </action>
  <verify>
    <automated>grep -q '_glossary-macos.md#mdm-migration' docs/_glossary.md && grep -q '### Tenant migration' docs/_glossary.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/_glossary.md contains the string '_glossary-macos.md#mdm-migration'
    - The new link appears as a '> See also:' blockquote line within (or directly appended after) the '### Tenant migration' entry, NOT under a fabricated '> **Windows equivalent:**' label
    - The existing '### Tenant migration' prose and its 'device-operations/04-tenant-migration.md' link are unchanged
    - A Version History row dated 2026-06-24 describing the reciprocal see-also is appended
  </acceptance_criteria>
  <done>_glossary.md#tenant-migration links reciprocally to _glossary-macos.md#mdm-migration via a bare See-also blockquote; version history updated; existing prose untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — docs-only) | No network surface, no user input, no auth, no code execution. The only crossing is authoring-intent → committed markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Tampering (doc-correctness) | Kandji/Iru heading slug | mitigate | Task 1 acceptance gate asserts `### Kandji-Iru` present and `### Kandji / Iru` absent; a mis-slugged heading would ship a dead link from guide-02 line 547 |
| T-91-02 | Information disclosure (false fact) | PSSO key-survival claim | mitigate | Task 1 acceptance gate asserts no same-tenant Secure-Enclave key-survival text; PSSO re-registration stated as always-required (Phase-90 authoritative) |
| T-91-03 | Tampering (broken inbound contract) | 9 minted anchors vs guide-02 targets | mitigate | Manual slug verification per RESEARCH Q2 table for all 9 headings against lines 541-550 |

**Residual risk: LOW.** This plan touches no blob-pinned file, so no CI red-HEAD risk. No security-relevant (S/R/D/E) threats apply to a docs-only change with no execution path. Block-on-high is satisfied — zero `high` threats.
</threat_model>

<verification>
- `grep` gates in both tasks confirm the minted headings, the index, the reciprocal link, and the absence of the double-hyphen / new-H2 anti-patterns.
- Manual slug check (RESEARCH Q2 table) confirms all 9 headings resolve to the guide-02 inbound anchors. The two landmines are Kandji-Iru (#kandji-iru not #kandji--iru) and app-sso (#app-sso not #app-sso-platform--s).
- No automated anchor-resolution tool exists in-repo; slug verification is the documented manual process per RESEARCH Validation Architecture.
</verification>

<success_criteria>
- All 9 previously-dead inbound anchors from guide-02 lines 541-550 resolve to a heading in _glossary-macos.md.
- `#platform-sso` left untouched (already resolved).
- No new `## Migration` H2; all entries under existing H2s.
- _glossary.md#tenant-migration links reciprocally to _glossary-macos.md#mdm-migration.
- No same-tenant key-survival assertion anywhere.
- Alphabetical Index, Version History, and freshness stamps updated on both files per sibling convention.
</success_criteria>

<output>
Create `.planning/phases/91-glossary-capability-matrix/91-01-SUMMARY.md` when done.
</output>
