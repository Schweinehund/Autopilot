---
phase: 91-glossary-capability-matrix
plan: 03
type: execute
wave: 2
depends_on: ["91-02"]
files_modified:
  - docs/reference/macos-capability-matrix.md
  - docs/reference/4-platform-capability-comparison.md
  - scripts/validation/check-phase-63.mjs
autonomous: true
requirements: [REF-02]
must_haves:
  truths:
    - "macos-capability-matrix.md has ONE new row under existing ## Enrollment with Windows cell = n/a and a macOS cell carrying all coverage facts (OS-26 ABM migration + Deadline + pre-26 wipe fallback + PSSO re-registration always required)"
    - "4-platform-capability-comparison.md has ONE new dedicated row under ## Enrollment; macOS cell links to macos-capability-matrix.md#enrollment; all non-macOS cells are n/a — [matrix](...#enrollment)"
    - "check-phase-63.mjs V-63-08 and V-63-09 BASELINE values match the post-edit blob hashes, updated in the SAME atomic commit as both content edits"
    - "node scripts/validation/check-phase-63.mjs exits 0 with V-63-08 and V-63-09 both PASS (not SKIPPED, not FAIL)"
    - "No new ## Migration H2 in either matrix; the macOS cell links to #enrollment (no #migration anchor exists)"
  artifacts:
    - path: "docs/reference/macos-capability-matrix.md"
      provides: "macOS 26 in-place ABM migration row under ## Enrollment"
      contains: "macOS 26 in-place ABM migration"
    - path: "docs/reference/4-platform-capability-comparison.md"
      provides: "dedicated migration row under ## Enrollment, link-not-copy to matrix"
      contains: "macOS 26 in-place ABM migration"
    - path: "scripts/validation/check-phase-63.mjs"
      provides: "updated V-63-08 + V-63-09 BASELINE blob hashes"
      contains: "const BASELINE"
  key_links:
    - from: "docs/reference/4-platform-capability-comparison.md (macOS cell)"
      to: "docs/reference/macos-capability-matrix.md#enrollment"
      via: "[matrix](macos-capability-matrix.md#enrollment) link"
      pattern: "macos-capability-matrix\\.md#enrollment"
    - from: "scripts/validation/check-phase-63.mjs V-63-08 BASELINE"
      to: "post-edit git hash-object docs/reference/macos-capability-matrix.md"
      via: "same-commit atomic BASELINE update"
      pattern: "const BASELINE"
---

<objective>
Append the macOS 26 in-place ABM migration row to BOTH matrix files and update both V-63-08 and V-63-09 BASELINE blob hashes — all in ONE combined atomic commit (D-05 4C, LOCKED). Editing a blob-pinned file without updating its BASELINE in the SAME commit produces a red HEAD (V-63 FAIL at the intermediate commit boundary); splitting across commits is forbidden.

Per D-04: ONE row in `macos-capability-matrix.md` under existing `## Enrollment`, Windows cell = `n/a`, no new H2. Per D-05: ONE dedicated row in `4-platform-capability-comparison.md` under `## Enrollment`, macOS cell links to `#enrollment`, all non-macOS cells `n/a — [matrix](...#enrollment)`.

Purpose: make the new MDM-migration capability discoverable and harness-consistent in the reference matrices without breaking the byte-exact blob-hash guard.
Output: edited `macos-capability-matrix.md` + `4-platform-capability-comparison.md` + `check-phase-63.mjs` (both BASELINE lines) in a single commit, validator green.

DEPENDS ON Plan 91-02: the pre-edit anchor inventory MUST already be committed before this plan's edit (REF-02 ordering invariant). Do NOT start until 91-ANCHOR-INVENTORY.md exists at HEAD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-glossary-capability-matrix/91-CONTEXT.md
@.planning/phases/91-glossary-capability-matrix/91-RESEARCH.md
@.planning/phases/91-glossary-capability-matrix/91-PATTERNS.md
@.planning/phases/91-glossary-capability-matrix/91-ANCHOR-INVENTORY.md
@scripts/validation/check-phase-63.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the migration row to both matrix files, update both BASELINE hashes, and commit atomically with the validator green</name>
  <files>docs/reference/macos-capability-matrix.md, docs/reference/4-platform-capability-comparison.md, scripts/validation/check-phase-63.mjs</files>
  <read_first>
    - docs/reference/macos-capability-matrix.md (the file being modified — read the `## Enrollment` table lines 15-26, the row insert point after the `Re-enrollment fires blocking screen` row at line 26, frontmatter lines 1-7, and the `## Version History` table near line 126)
    - docs/reference/4-platform-capability-comparison.md (the file being modified — read the `## Enrollment` table and its `Windows 10 support / minimum OS` last row, the `n/a — [matrix](...#enrollment)` cell format, the line-12 note that the Windows column links to linux-capability-matrix.md#enrollment, frontmatter, and Version History)
    - scripts/validation/check-phase-63.mjs (the file being modified — V-63-08 comment line 202, name line 204, const BASELINE line 209; V-63-09 comment line 223, name line 225, const BASELINE line 230)
    - .planning/phases/91-glossary-capability-matrix/91-ANCHOR-INVENTORY.md (the committed pre-edit surface — confirm it exists at HEAD before editing)
    - .planning/phases/91-glossary-capability-matrix/91-RESEARCH.md (Q5 exact blob-hash edit procedure + the combined-commit form; matrix/4-platform row formats)
    - .planning/phases/91-glossary-capability-matrix/91-PATTERNS.md (matrix row format, 4-platform row format, the 10-step combined edit procedure, Version History rows)
    - .planning/phases/91-glossary-capability-matrix/91-CONTEXT.md (D-04, D-05 EXECUTION RULES — single row, no new H2, n/a Windows, #enrollment anchor, never-split atomicity; the no-key-survival constraint)
  </read_first>
  <action>
    Confirm 91-ANCHOR-INVENTORY.md is at HEAD (depends_on 91-02); if absent, STOP.
    Step A — edit `docs/reference/macos-capability-matrix.md`: append ONE row as the last data row inside the `## Enrollment` table (after the `Re-enrollment fires blocking screen | Yes (every enrollment) | No (first enrollment only)` row at line 26, before the blank line preceding `## Configuration`). Feature label `macOS 26 in-place ABM migration`; Windows cell `n/a`; macOS cell single-line (NO `<br>`/HTML) carrying ALL coverage facts in terse style: profile-based re-enrollment, wipe-free, PSSO re-registration always required post-migration, pre-macOS-26 devices use wipe-and-re-enroll fallback, ending with `— see [MDM Migration Walkthrough](../macos-lifecycle/02-mdm-migration-psso.md)`. Do NOT add a `## Migration` H2. Append a Version History row dated 2026-06-24 and set frontmatter `last_verified: 2026-06-24` / `review_by: 2026-09-24`.
    Step B — edit `docs/reference/4-platform-capability-comparison.md`: append ONE dedicated row as the last data row inside the `## Enrollment` table (after the `Windows 10 support / minimum OS` row). Feature label `macOS 26 in-place ABM migration`; Windows cell `n/a — [matrix](linux-capability-matrix.md#enrollment)` (Windows column targets linux matrix per file line 12); macOS cell `Supported — [matrix](macos-capability-matrix.md#enrollment)` (links to #enrollment, NOT #migration); iOS/Android/Linux cells `n/a — [matrix]({platform}-capability-matrix.md#enrollment)` using the em-dash `—` form. Append a Version History row dated 2026-06-24 and set frontmatter `last_verified: 2026-06-24` / `review_by: 2026-09-24`.
    Step C — compute and pin: run `git hash-object docs/reference/macos-capability-matrix.md` → replace the V-63-08 `const BASELINE` on line 209 AND the hash embedded in the comment (line 202) AND the `name:` string (line 204) with the new 40-char hash. Run `git hash-object docs/reference/4-platform-capability-comparison.md` → replace the V-63-09 `const BASELINE` on line 230 AND the comment (line 223) AND the `name:` string (line 225) with the new hash.
    Step D — atomic commit: `git add docs/reference/macos-capability-matrix.md docs/reference/4-platform-capability-comparison.md scripts/validation/check-phase-63.mjs` then a SINGLE `git commit` (all three files together — NEVER split a content edit from its BASELINE update). Suggested message: `docs(91): add macOS 26 in-place ABM migration rows to matrices (atomic V-63-08/09 pin)`.
    Step E — verify: run `node scripts/validation/check-phase-63.mjs` and confirm V-63-08 and V-63-09 both PASS (not SKIPPED, not FAIL) and overall exit 0.
    NO cell may assert same-tenant Secure Enclave key survival (Phase-90 carried fact: PSSO re-registration ALWAYS required).
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-63.mjs > /tmp/p63.out 2>&1; rc=$?; grep -q 'V-63-08:.*PASS' /tmp/p63.out && grep -q 'V-63-09:.*PASS' /tmp/p63.out && [ "$rc" = "0" ] && test "$(git hash-object docs/reference/macos-capability-matrix.md)" = "$(grep -oE \"const BASELINE = '[0-9a-f]{40}'\" scripts/validation/check-phase-63.mjs | sed -n '1s/.*'\\''\\([0-9a-f]*\\)'\\''.*/\\1/p')" && grep -qi 'macOS 26 in-place ABM migration' docs/reference/macos-capability-matrix.md && grep -qi 'macOS 26 in-place ABM migration' docs/reference/4-platform-capability-comparison.md && grep -q 'macos-capability-matrix.md#enrollment' docs/reference/4-platform-capability-comparison.md && ! grep -q '## Migration' docs/reference/macos-capability-matrix.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/validation/check-phase-63.mjs exits 0; V-63-08 shows PASS and V-63-09 shows PASS (neither SKIPPED nor FAIL)
    - git hash-object docs/reference/macos-capability-matrix.md == the V-63-08 const BASELINE value in scripts/validation/check-phase-63.mjs
    - git hash-object docs/reference/4-platform-capability-comparison.md == the V-63-09 const BASELINE value in scripts/validation/check-phase-63.mjs
    - docs/reference/macos-capability-matrix.md contains a row labelled 'macOS 26 in-place ABM migration' with Windows cell exactly 'n/a' under ## Enrollment; no '## Migration' heading added
    - docs/reference/4-platform-capability-comparison.md contains a dedicated 'macOS 26 in-place ABM migration' row whose macOS cell links to 'macos-capability-matrix.md#enrollment' and whose non-macOS cells use the 'n/a — [matrix](...#enrollment)' form
    - The single HEAD commit stages all three of docs/reference/macos-capability-matrix.md, docs/reference/4-platform-capability-comparison.md, scripts/validation/check-phase-63.mjs together (verify: `git show --name-only HEAD` lists exactly those three content/validator files); content edit and BASELINE update are NOT split across commits
    - Neither matrix cell asserts same-tenant Secure Enclave key survival; the macOS cell states PSSO re-registration always required post-migration
  </acceptance_criteria>
  <done>Both matrices carry the migration row; both BASELINE hashes updated to the post-edit blobs in the same atomic commit; check-phase-63.mjs exits 0 with V-63-08/09 PASS; no new H2; no key-survival claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content edit → blob-hash CI guard | Editing a blob-pinned file changes its hash; V-63-08/V-63-09 in check-phase-63.mjs run `git hash-object` at test time and FAIL unless the matching BASELINE is updated in the SAME commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-06 | Tampering (CI-integrity / red-HEAD) | matrix edit vs V-63-08/09 BASELINE | mitigate | Single atomic commit stages both content files + validator together; acceptance gate asserts `git hash-object` == BASELINE for both files AND that HEAD stages exactly the three files (never split — PITFALL-2) |
| T-91-07 | Tampering (doc-correctness) | macOS cell anchor + false fact | mitigate | Acceptance gate asserts macOS cell links to #enrollment (no #migration exists) and no new ## Migration H2; cell states PSSO re-registration always required (no key-survival claim) |
| T-91-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; docs + in-repo validator edit only. No `## Package Legitimacy Audit` required (no install tasks). |

**Residual risk: LOW, CI-integrity-bound.** The only material hazard is the blob-hash atomicity invariant (a split commit yields a red HEAD); it is mitigated by the single-commit gate and the post-commit `check-phase-63.mjs` green assertion. No `high` security threats — block-on-high satisfied.
</threat_model>

<verification>
- `node scripts/validation/check-phase-63.mjs` must exit 0 with V-63-08 and V-63-09 PASS — the authoritative byte-exact blob-hash guard.
- `git hash-object` of each pinned file must equal its in-validator BASELINE — confirms the atomic pin landed.
- `git show --name-only HEAD` must list exactly the three content/validator files in one commit — confirms no split (PITFALL-2 red-HEAD).
- `grep` confirms the migration rows, the #enrollment link target, and the absence of a new ## Migration H2.
</verification>

<success_criteria>
- macos-capability-matrix.md and 4-platform-capability-comparison.md each gain ONE migration row under existing ## Enrollment.
- Both V-63-08 and V-63-09 BASELINE hashes updated to the post-edit blobs in ONE atomic commit.
- check-phase-63.mjs exits 0; V-63-08/V-63-09 PASS.
- macOS cell links to #enrollment; no new ## Migration H2; no key-survival claim.
</success_criteria>

<output>
Create `.planning/phases/91-glossary-capability-matrix/91-03-SUMMARY.md` when done.
</output>
