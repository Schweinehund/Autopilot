---
phase: 65-apple-business-l1-l2-hub-navigation-integration
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md
  - docs/l1-runbooks/00-index.md
  - docs/l2-runbooks/26-apple-business-permission-denied.md
  - docs/l2-runbooks/00-index.md
autonomous: true
requirements: [ABNAV-01, ABNAV-02]

must_haves:
  truths:
    - "L1 staff can open `docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` and execute Path A (Apple Business UI) end-to-end without escalation"
    - "L1 staff can find the `#which-admin-owns-this-pool` lookup step BEFORE Path A execution and the cross-link resolves to `05-sub-org-admin-onboarding.md`"
    - "L1 staff can see Paths B and C as gated escalation rows pointing to L2 #26 + the canonical admin-context `12-shared-ipad-passcode-reset.md` (D-01 presence + gating, NOT reproduction)"
    - "L2 engineering can open `docs/l2-runbooks/26-apple-business-permission-denied.md` and route through the 7-leaf Mermaid tree (DA-9 LOCKED: role-lacks-permission / OU-boundary / Apple-Business-scope / Intune-scope / federation-state / quota-limit / Account-Holder-lockout)"
    - "Apple-Business-scoped leaves route via Mermaid `click` to existing corpus docs (01-/02-/05-/08-/16-/18-); Intune-scope leaf is inline-text-only C15-safe scope-boundary callout (NO `click`); Account-Holder-lockout routes to `01-role-permission-model.md` OP-2 callout per D-02"
    - "L1 #34 row appears in `docs/l1-runbooks/00-index.md` under a new `## Apple Business L1 Runbooks` H2 (exactly 1 v1.6 L1 runbook — #34 cap honored)"
    - "L2 #26 row appears in `docs/l2-runbooks/00-index.md` under a new `## Apple Business L2 Runbooks` H2 (exactly 1 v1.6 L2 runbook — #26 cap honored)"
  artifacts:
    - path: "docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md"
      provides: "L1 #34 Apple Business Shared iPad passcode reset runbook; compound platform frontmatter; Path A executable; Paths B/C escalation pointers; `#which-admin-owns-this-pool` lookup"
      contains: "platform: ios+macos+shared-ipad"
      min_lines: 120
    - path: "docs/l2-runbooks/26-apple-business-permission-denied.md"
      provides: "L2 #26 7-leaf Mermaid permission-denied decision tree; hybrid leaf behavior per D-02"
      contains: "graph TD"
      min_lines: 100
    - path: "docs/l1-runbooks/00-index.md"
      provides: "L1 index with #34 row appended under Apple Business L1 Runbooks H2"
      contains: "34-apple-business-shared-ipad-passcode-reset"
    - path: "docs/l2-runbooks/00-index.md"
      provides: "L2 index with #26 row appended under Apple Business L2 Runbooks H2"
      contains: "26-apple-business-permission-denied"
  key_links:
    - from: "docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md"
      to: "docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md"
      via: "C16 edge l1_34 → admin_12; substring `12-shared-ipad-passcode-reset`"
      pattern: "12-shared-ipad-passcode-reset"
    - from: "docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md"
      to: "docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md#which-admin-owns-this-pool"
      via: "ABNAV-01 lookup step (Phase 63 anchor); substring `#which-admin-owns-this-pool`"
      pattern: "#which-admin-owns-this-pool"
    - from: "docs/l2-runbooks/26-apple-business-permission-denied.md"
      to: "docs/cross-platform/apple-business/01-role-permission-model.md"
      via: "Mermaid click directive (ABPDR1 role-lacks-permission + ABPDR7 Account-Holder-lockout OP-2)"
      pattern: "click ABPDR[17]"
    - from: "docs/l2-runbooks/26-apple-business-permission-denied.md"
      to: "docs/cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md"
      via: "Intune-scope leaf C15-safe scope-boundary callout (inline link, no click)"
      pattern: "18-cross-org-boundary-cheat-sheet"
---

<objective>
Author the two new Apple Business tier runbooks (L1 #34 + L2 #26) and append their rows to the L1/L2 index files. All four files are NEW or APPEND-ONLY; no file overlap with any other plan in Wave 2.

Purpose: ABNAV-01 + ABNAV-02 — surface the Phase 62-64 Apple Business governance corpus to L1 service desk + L2 engineering through tier-appropriate entry points. L1 #34 is the Path A executable runbook (D-01 Option B: Path A in full, Paths B/C as escalate-to-L2 pointers). L2 #26 is the 7-leaf Mermaid decision tree (D-02 Option C hybrid: Apple-Business-scoped leaves route via `click`, Intune-scope leaf is inline-only C15-safe, Account-Holder-lockout routes to OP-2 callout). The two index files get matching append entries so navigation surfaces #34 and #26.
Output: 4 files — `docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` (new), `docs/l1-runbooks/00-index.md` (append), `docs/l2-runbooks/26-apple-business-permission-denied.md` (new), `docs/l2-runbooks/00-index.md` (append).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-CONTEXT.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-RESEARCH.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-PATTERNS.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-VALIDATION.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-CONVENTIONS.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-01-SUMMARY.md

<interfaces>
<!-- L1 envelope analog: 30-linux-enrollment-failed.md is the canonical L1 template. -->
@docs/l1-runbooks/30-linux-enrollment-failed.md

<!-- L1 index append pattern (existing H2 platform-group sections; insertion after Linux). -->
@docs/l1-runbooks/00-index.md

<!-- L2 index append pattern (existing H2 + version-gate blockquote + When to Use table). -->
@docs/l2-runbooks/00-index.md

<!-- Mermaid hybrid leaf precedent (route-via-click + inline escalation coexist). -->
@docs/decision-trees/07-ios-triage.md

<!-- Canonical admin-context doc the L1 #34 cross-links to (C16 edge l1_34 → admin_12).
     Read it to confirm Path A/B/C ordering + OP-11 destructive gating + ABAUDIT-07 line 116. -->
@docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md

<!-- L1 #34 lookup-step target (Phase 63 anchor — Wave-B gate). -->
@docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md

<!-- L2 #26 click-target docs (DA-9 LOCKED leaves). -->
@docs/cross-platform/apple-business/01-role-permission-model.md
@docs/cross-platform/apple-business/02-ous-architecture.md
@docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md
@docs/cross-platform/apple-business/16-managed-apple-account-runbook.md
@docs/cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md

<!-- Wave-1 contract artifacts (load-bearing strings + ABAUDIT-24 budget). -->

LOCKED frontmatter (from 65-CONVENTIONS.md / 65-PATTERNS.md):
- L1 #34: last_verified: 2026-05-22 / review_by: 2026-07-21 / applies_to: apple-business / audience: L1 / platform: ios+macos+shared-ipad
- L2 #26: last_verified: 2026-05-22 / review_by: 2026-07-21 / applies_to: apple-business / audience: L2 / platform: ios+macos+shared-ipad

LOAD-BEARING substrings (silent C16 edge break if absent — V-65-02/03/04/05/06 validator gates):
- L1 #34: `platform: ios+macos+shared-ipad`, `12-shared-ipad-passcode-reset`, `#which-admin-owns-this-pool`
- L2 #26: 7 Mermaid `([` leaf-node opens (V-65-06 grep `/\(\[/g` count ≥ 7)

DA-9 LOCKED 7 leaves with leaf-IDs (from 65-PATTERNS.md Mermaid template):
- ABPDR1 role-lacks-permission → `../cross-platform/apple-business/01-role-permission-model.md`
- ABPDR2 OU-boundary → `../cross-platform/apple-business/02-ous-architecture.md`
- ABPDR3 Apple-Business-scope → `../cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md` (or `01-role-permission-model.md` Edit-without-View per D-02)
- ABPDE1 Intune-scope → INLINE only; NO `click`; C15-safe callout
- ABPDR5 federation-state → `../cross-platform/apple-business/16-managed-apple-account-runbook.md`
- ABPDR6 quota-limit → `../cross-platform/apple-business/08-managed-apple-account-provisioning.md`
- ABPDR7 Account-Holder-lockout → `../cross-platform/apple-business/01-role-permission-model.md` (OP-2 callout `:39-58`)

C15 banned-phrase guards on the Intune-scope leaf (regex 1 + regex 4 from `v1.6-milestone-audit.mjs:847-856`):
- AVOID: "Intune RBAC", "Intune role", "Intune-side delegation/RBAC/role assign"
- PREFERRED (Option A from 65-RESEARCH.md:279-285): "MDM commands issued from the Intune admin center, outside the Apple Business permission surface"
- FALLBACK (Option B, ABAUDIT-24 budget): `<!-- ABAUDIT-24: next line ... C15 regex N false-positive exemption (...) -->` on the line immediately before the banned phrase

D-01 3-path matrix structure (LOCKED — 65-PATTERNS.md + 65-RESEARCH.md:177-186):
| Path | Description | Who Executes | Gating |
|------|-------------|--------------|--------|
| Path A — Apple Business UI | Shared iPad passcode reset via Apple Business portal | L1 / sub-org admin (this runbook) | No approval required |
| Path B — MDM ClearPasscode | MDM ClearPasscode command via Intune | L2 only | L2-only; route to L2 #26 |
| Path C — MDM EraseDevice | Full device erase via Intune | L2 with L2 approval required | OP-11 hard gate; route to L2 #26 |

L1 read-only convention (VERBATIM from `30-:21`): "L1 Triage Steps in this runbook are read-only checks. State-changing commands (...) appear ONLY in the per-cause `### Admin Action Required` sections — they are not L1 actions." Replace Linux-specific parenthetical with Apple Business equivalent.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| L1 staff (service desk) → Apple Business UI portal | Path A is L1-executable; Paths B/C are destructive MDM commands gated to L2 (OP-11) |
| L2 engineering → MDM destructive operations | Path C EraseDevice requires hard L2-approval gate per OP-11; L1 #34 MUST NOT inline destructive steps |
| Apple Business scope ↔ Intune scope | C15 banned-phrase regexes enforce the corpus-integrity invariant (REQUIREMENTS.md:89 + D-A8); the L2 #26 Intune-scope leaf is the highest-risk surface |
| L1 #34 ↔ canonical admin-context `12-` | C16 edge l1_34 → admin_12 substring `12-shared-ipad-passcode-reset` LOAD-BEARING |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-06 | Elevation of Privilege | L1 #34 Path C reproduction | mitigate | D-01 + REQUIREMENTS:93: L1 #34 documents Path A in full; Paths B/C as escalate-to-L2 pointers ONLY. The matrix is PRESENT (Path A executable, B/C as gated escalation rows). L1 read-only convention from `30-:21` reproduced verbatim. Destructive EraseDevice never reachable as an L1-executable step. |
| T-65-07 | Information Disclosure | Wrong admin contacted (cross-OU permission leak) | mitigate | ABNAV-01 `#which-admin-owns-this-pool` lookup is the FIRST step in L1 #34 before Path A execution. Cross-links to Phase 63 `05-sub-org-admin-onboarding.md#which-admin-owns-this-pool` anchor. |
| T-65-08 | Tampering | C15 banned-phrase landmine in L2 #26 Intune-scope leaf | mitigate | Option A (avoid banned phrases — e.g., "MDM commands issued from the Intune admin center, outside the Apple Business permission surface") is the preferred approach per 65-RESEARCH.md:293. If Option B chosen, allocate ABAUDIT-24 with colon-suffixed `<!-- ABAUDIT-24: reason -->` on the line immediately before the banned phrase (line-pair scoping per `v1.6-milestone-audit.mjs:859-862`). |
| T-65-09 | Tampering | C16 edge l1_34 → admin_12 silent break (substring `12-shared-ipad-passcode-reset` absent or reworded) | mitigate | The substring is satisfied by the explicit cross-link `[12-shared-ipad-passcode-reset.md](...)` in L1 #34's escalation-pointer rows + the "Before escalating, collect:" handoff section. V-65-03 grep-checks the substring. |
| T-65-10 | Repudiation | Account Holder DO-NOT-delegate violation (OP-2 invariant) | mitigate | L2 #26 ABPDR7 leaf routes to `01-role-permission-model.md` OP-2 callout `:39-58`; lockout recovery is OUT-OF-CORPUS for any new doc (deferred to v1.7+). |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan; pure markdown authoring. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author `docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` — L1 #34 with Path A executable + Paths B/C escalation-pointer rows</name>
  <files>docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md</files>
  <read_first>
    - docs/l1-runbooks/30-linux-enrollment-failed.md (full file — L1 envelope template; especially lines 1-7 frontmatter, line 9 platform gate, line 21 L1 scope note verbatim text, lines 188-198 "Before escalating, collect:" pattern, version-history pattern at end)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-CONVENTIONS.md (Wave 1 locked strings + ABAUDIT-24 budget)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-PATTERNS.md "docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md (L1 runbook, new)" section (frontmatter + platform gate + L1 scope note + 3-path matrix + Before escalating + C16 cross-link requirements + Version History)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-RESEARCH.md §Research Target 1 "L1 #34 Envelope and Body" (envelope structure 1-12; "Before Escalating, Collect:" Apple Business equivalent; Cross-Links REQUIRED table)
    - docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md (Path A/B/C definitions for the matrix description + OP-11 destructive gating + Required Role & Permission block + Verification — Phase 65 L1 #34 must NOT reproduce these in detail, only point to them)
    - docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md (verify `#which-admin-owns-this-pool` anchor exists — ABNAV-01 lookup target)
    - docs/l1-runbooks/00-index.md (confirm #34 is next free L1 number; envelope-style consistency)
    - scripts/validation/check-phase-65.mjs (V-65-01/02/03/04 — assertions this file's content must satisfy)
    - docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md (the file being created)
  </read_first>
  <action>
    Create `docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` following the `30-linux-enrollment-failed.md` envelope (per ABNAV-01 / D-01 / 65-PATTERNS.md L1 #34 section). Order:

    **(1) Frontmatter — exactly 5 fields, exact values:**
    `last_verified: 2026-05-22`, `review_by: 2026-07-21`, `applies_to: apple-business`, `audience: L1`, `platform: ios+macos+shared-ipad`. No spaces around `+`. Verbatim from 65-CONVENTIONS.md Section 1.

    **(2) Platform gate blockquote** (analog `30-:9`): "> **Platform gate:** This guide covers Apple Business Shared iPad passcode reset (iOS + iPadOS + Shared iPad). For Windows Autopilot, see [Windows L1 Runbooks](00-index.md#apv1-runbooks). For macOS ADE, see [macOS ADE Runbooks](00-index.md#macos-ade-runbooks). For Android Enterprise, see [Android L1 Runbooks](00-index.md#android-enterprise-l1-runbooks)." Adjust the inner platform-coverage list to whatever peer-group anchors actually exist in `docs/l1-runbooks/00-index.md` (verify by reading the file).

    **(3) H1 title:** `# Apple Business Shared iPad Passcode Reset`

    **(4) Introductory paragraph (2-3 sentences):** scope statement + which triage tree routes here (cross-link to the Apple Business decision tree if any exists; otherwise note the runbook is reachable directly from the Apple Business Quick Reference in `quick-ref-l1.md` and from L2 #26's Path A leaf route).

    **(5) L1 scope note blockquote — VERBATIM structure from `30-:21`:** "> **L1 scope note:** L1 Triage Steps in this runbook are read-only checks. State-changing commands (MDM ClearPasscode, MDM EraseDevice) appear ONLY in the per-cause `### Admin Action Required` sections — they are not L1 actions." Replace Linux parenthetical with the Apple Business equivalent shown.

    **(6) `## Which Admin Owns This Pool?` H2 (the FIRST procedural section before Path A):** Document the lookup step that points to `docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md#which-admin-owns-this-pool` (LOAD-BEARING substring `#which-admin-owns-this-pool` MUST appear here — V-65-04). One-paragraph rationale (OU-scoped passcode reset privileges; cross-OU action is a permission error) + the link.

    **(7) `## Prerequisites` H2:** portal roles needed (Apple Business sub-org admin with `Reset Shared iPad passcode` privilege per `01-role-permission-model.md` 7-subgroup catalog); device identifiers needed (Shared iPad serial number, User UPN); when NOT to use Path A (link to `12-shared-ipad-passcode-reset.md` for the full admin-context matrix — this satisfies LOAD-BEARING substring `12-shared-ipad-passcode-reset` — V-65-03).

    **(8) `## 3-Path Decision Matrix` H2:** Insert the LOCKED 4-column matrix from 65-CONVENTIONS.md / 65-PATTERNS.md L1 #34 section — `Path | Description | Who Executes | Gating` — exactly 3 rows (Path A / Path B / Path C). Paths B and C MUST explicitly say "route to L2 #26" and cross-link to `26-apple-business-permission-denied.md`.

    **(9) `## Path A — Apple Business UI (L1-executable)` H2:** Document Path A end-to-end as the L1-executable procedure. Sub-sections: `### L1 Triage Steps` (read-only checks: portal reachability, sub-org admin login confirmed, target Shared iPad visible in the OU device list, "Reset Shared iPad passcode" privilege visible in role assignment); `### Admin Action Required` (the actual reset step in the Apple Business UI — portal navigation path + click sequence + confirmation; this is L1-executable per D-01 because Path A goes through the Apple Business UI, not MDM destructive commands). Cite `12-shared-ipad-passcode-reset.md` Path A section as the canonical admin-context source.

    **(10) `## Path B — MDM ClearPasscode (L2-only, escalation pointer)` H2:** ONE paragraph: state Path B uses MDM ClearPasscode issued from the Intune admin center; it is NOT an L1 action (read-only convention from step 5); when Path A fails or is unreachable, escalate to L2 via `26-apple-business-permission-denied.md` and cite the full admin-context matrix at `12-shared-ipad-passcode-reset.md` Path B section. Avoid reproducing Path B's destructive command sequence here (D-01 rationale: importing 12- line 116 ABAUDIT-07-exempted Intune phrasing would require a duplicate ABAUDIT for zero benefit).

    **(11) `## Path C — MDM EraseDevice (L2-only with hard approval gate, escalation pointer)` H2:** ONE paragraph: state Path C is DESTRUCTIVE (full device erase) and requires hard L2-approval gating per OP-11 (point to `12-shared-ipad-passcode-reset.md:129-156` Path C section). NEVER attempt as L1. Escalate via `26-apple-business-permission-denied.md`. Avoid reproducing the EraseDevice command flow (D-01 + L1 read-only convention).

    **(12) `## Escalation Criteria` H2 + "Before escalating, collect:" sub-list** (mirror `30-:188-198` Apple Business-equivalent, per 65-PATTERNS.md "Before escalating, collect:" block). Bullet items: Device serial number; Shared iPad platform version (iPadOS); User UPN for the locked account; Which admin pool owns the device (from the `#which-admin-owns-this-pool` lookup); Screenshot of the error (Apple Business portal or MDM error message); Path attempted (B or C if escalating from a failed Path A); Timestamp of the failed attempt; User actions attempted and the outcome. Then a link `[Escalate to L2 — Apple Business Permission Denied Investigation (#26)](../l2-runbooks/26-apple-business-permission-denied.md)`.

    **(13) `## Cross-References` H2** (brief tail): bulleted links to `12-shared-ipad-passcode-reset.md` (canonical admin-context matrix), `01-role-permission-model.md` (permission catalog), `05-sub-org-admin-onboarding.md#which-admin-owns-this-pool` (admin-owner lookup), `26-apple-business-permission-denied.md` (L2 #26 7-leaf decision tree).

    **(14) `## Version History` H2** with one row: `| 2026-05-22 | Phase 65 plan 65-02: initial authoring — L1 Apple Business Shared iPad passcode reset; Path A executable (Apple Business UI); Paths B/C escalation-pointer only (OP-11); C16 edge l1_34↔admin_12 live | -- |`

    DO NOT include the substring `34-apple-business` referring to this file's own name as a self-reference link inside the content body (it appears in the filename automatically). DO NOT mention "Intune RBAC", "Intune role", "Intune-side delegation" anywhere in the file body — use the Option A scope-boundary phrasing ("MDM commands issued from the Intune admin center, outside the Apple Business permission surface") if the Paths B/C escalation pointers need any cross-scope phrasing. The L1 read-only convention block must mention "MDM ClearPasscode" and "MDM EraseDevice" by name (these are the state-changing commands referenced by Paths B/C); these names do NOT trip C15 (regex 1 requires `Intune\s+(RBAC|role|scope\s+tag|admin\s+role)` adjacent — bare "MDM ClearPasscode" is C15-safe).

    Run `node scripts/validation/check-phase-65.mjs` after authoring; V-65-01, V-65-02, V-65-03, V-65-04 should now PASS.
  </action>
  <acceptance_criteria>
    - File `docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` exists
    - Frontmatter contains verbatim: `last_verified: 2026-05-22`, `review_by: 2026-07-21`, `applies_to: apple-business`, `audience: L1`, `platform: ios+macos+shared-ipad`
    - Contains verbatim H2 `## Which Admin Owns This Pool?` (or equivalent H2 whose body includes `#which-admin-owns-this-pool`) such that the file contains the literal substring `#which-admin-owns-this-pool`
    - Contains the literal substring `12-shared-ipad-passcode-reset` (cross-link to canonical admin-context doc; C16 edge l1_34 → admin_12)
    - Contains H2 `## 3-Path Decision Matrix` or equivalent with exactly 3 path rows (Path A / Path B / Path C)
    - Path A section contains both `### L1 Triage Steps` and `### Admin Action Required` sub-sections
    - Paths B and C sections explicitly reference `26-apple-business-permission-denied.md` as the L2 escalation target
    - Contains the verbatim phrase `read-only checks` from the L1 scope note block
    - Contains a "Before escalating, collect:" bullet list with at least 6 items including "Device serial number" and "Which admin pool owns the device"
    - Contains a `## Version History` H2 with at least one row dated `2026-05-22`
    - Does NOT contain the literal phrase `Intune RBAC` (C15 regex 1 guard)
    - Does NOT contain the literal phrase `Intune-side` followed within 40 chars by `delegat`, `RBAC`, or `role assign` (C15 regex 4 guard)
    - `node scripts/validation/check-phase-65.mjs` reports V-65-01 PASS, V-65-02 PASS, V-65-03 PASS, V-65-04 PASS
    - `node scripts/validation/v1.6-milestone-audit.mjs` runs without a new C15 FAIL caused by this file (C16 admin_12 edge still EXEMPTED until Wave 4 — that is correct intermediate state)
  </acceptance_criteria>
  <done>
    L1 #34 ships. L1 staff have a single Path-A executable Shared iPad passcode reset runbook. The "which admin owns this pool" lookup precedes Path A. Paths B/C are L2-only escalation pointers that satisfy SC#1 via presence + gating (not reproduction). C16 edge l1_34 → admin_12 substring is in place (the file end of the edge); the admin_12 → l1_34 reverse edge waits for Wave 4 (atomic-trio).
  </done>
</task>

<task type="auto">
  <name>Task 2: Append `## Apple Business L1 Runbooks` H2 to `docs/l1-runbooks/00-index.md` with the #34 row</name>
  <files>docs/l1-runbooks/00-index.md</files>
  <read_first>
    - docs/l1-runbooks/00-index.md (full file — identify the existing platform-group H2 sections and the insertion point: after the last platform-group section, before `## Version History` if present)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-PATTERNS.md "docs/l1-runbooks/00-index.md (index append, ABNAV-01 side effect)" (exact append block)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-RESEARCH.md §"L1 00-index Append Pattern" lines 187-199
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-ANCHOR-INVENTORY.md (verify the pre-edit anchor set is captured for `docs/l1-runbooks/00-index.md` — Wave 3 ABNAV-01 side effect target; though this index file is not in the 5 hub files inventory, it's an append-only edit and PITFALL-6 still applies)
  </read_first>
  <action>
    Append to `docs/l1-runbooks/00-index.md` a new H2 section `## Apple Business L1 Runbooks` with a description paragraph and a 3-column table (Runbook | Scenario | Applies To). Exact append block from 65-PATTERNS.md (verbatim):

    ```
    ## Apple Business L1 Runbooks

    L1 runbook for the Apple Business Shared iPad passcode reset scenario. Start with the [Apple Business permission-denied triage](../l2-runbooks/26-apple-business-permission-denied.md) for complex escalation routing.

    | Runbook | Scenario | Applies To |
    |---------|----------|------------|
    | [34: Apple Business Shared iPad Passcode Reset](34-apple-business-shared-ipad-passcode-reset.md) | Shared iPad passcode reset via Apple Business UI (Path A primary); Paths B/C escalation to L2 | iOS+macOS+Shared iPad |
    ```

    Insertion point: AFTER the last existing platform-group H2 section in the file, BEFORE `## Version History` if a Version History H2 exists at the file's tail (otherwise append at end). Pre-edit anchor inventory check: zero existing H2/H3 anchors shift; this is a pure append. Add a Version History row (if the index has a Version History table) recording `| 2026-05-22 | Phase 65 plan 65-02: appended Apple Business L1 Runbooks H2 with #34 row | -- |`. Honor the #34-cap invariant: exactly ONE row in the new table (CI-5 L1 cap honored per `65-CONTEXT.md` D-A10).
  </action>
  <acceptance_criteria>
    - File `docs/l1-runbooks/00-index.md` contains the literal H2 heading `## Apple Business L1 Runbooks`
    - File contains the literal substring `34-apple-business-shared-ipad-passcode-reset.md` exactly once (as the row link)
    - File contains the literal substring `26-apple-business-permission-denied.md` exactly once (as the L2 cross-link in the description)
    - The new H2 appears AFTER any pre-existing platform-group H2 and BEFORE any `## Version History` H2 (PITFALL-6 append-only contract)
    - The new section's table contains exactly 1 data row (CI-5 L1 #34 cap honored)
    - Diff against the pre-edit file shows ONLY additions (no deletions, no reordering of existing H2s)
    - All H2/H3 anchors that existed pre-edit still exist post-edit (anchor-stability verified by re-grep of pre-edit anchor set)
  </acceptance_criteria>
  <done>
    L1 #34 is discoverable from the L1 index. ABNAV-01 navigation surface is complete. CI-5 L1 cap honored at exactly 1 v1.6 L1 runbook (#34).
  </done>
</task>

<task type="auto">
  <name>Task 3: Author `docs/l2-runbooks/26-apple-business-permission-denied.md` — L2 #26 with 7-leaf Mermaid decision tree (hybrid leaf behavior per D-02)</name>
  <files>docs/l2-runbooks/26-apple-business-permission-denied.md</files>
  <read_first>
    - docs/decision-trees/07-ios-triage.md (full file — Mermaid syntax analog; hybrid leaf precedent at lines 29-58; click directive form `click NODE "url"` two-argument; classDef resolved/escalateL2 color codes; inline-text escalation leaves at `:41-45`)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-CONVENTIONS.md (Section 3 ABAUDIT-24 + Section 5 C15 regex set the Intune-scope leaf must dodge)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-PATTERNS.md "docs/l2-runbooks/26-apple-business-permission-denied.md (L2 runbook + Mermaid tree, new)" (full template incl. Mermaid block + Intune-scope C15-safe callout Options A/B)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-RESEARCH.md §Research Target 2 "L2 #26 7-Leaf Mermaid Tree" (Mermaid syntax conventions; 7-leaf identity & target mapping table; Intune-scope leaf C15 risk; ABAUDIT line-pair scoping mechanics)
    - docs/cross-platform/apple-business/01-role-permission-model.md (target for ABPDR1 role-lacks-permission + ABPDR7 Account-Holder-lockout — verify OP-2 callout at `:39-58`)
    - docs/cross-platform/apple-business/02-ous-architecture.md (target for ABPDR2 OU-boundary)
    - docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md (target for ABPDR6 quota-limit)
    - docs/cross-platform/apple-business/16-managed-apple-account-runbook.md (target for ABPDR5 federation-state)
    - docs/cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md (target for ABPDR3 Apple-Business-scope + Intune-scope leaf scope-boundary callout cross-link)
    - docs/l2-runbooks/00-index.md (confirm #26 is next free L2 number)
    - scripts/validation/check-phase-65.mjs (V-65-05, V-65-06 — assertions this file's content must satisfy)
    - docs/l2-runbooks/26-apple-business-permission-denied.md (the file being created)
  </read_first>
  <action>
    Create `docs/l2-runbooks/26-apple-business-permission-denied.md` as an L2 runbook with an EMBEDDED 7-leaf Mermaid decision tree (per ABNAV-02 / D-02 / 65-PATTERNS.md L2 #26 section). Structure:

    **(1) Frontmatter — 5 fields verbatim from 65-CONVENTIONS.md / 65-PATTERNS.md L2 #26 section:**
    `last_verified: 2026-05-22`, `review_by: 2026-07-21`, `applies_to: apple-business`, `audience: L2`, `platform: ios+macos+shared-ipad`. (Same compound platform value as L1 #34.)

    **(2) Platform gate blockquote** noting this guide covers Apple Business permission errors across the Apple Business delegated governance surface (iOS/macOS/Shared iPad/Apple TV scoped via Apple Business OUs).

    **(3) H1 title:** `# Apple Business Permission Denied Investigation`

    **(4) Introductory paragraph (3-4 sentences):** scope statement — when L2 receives an Apple Business permission-denied symptom (from L1 escalation, from `quick-ref-l2.md` Apple Business Quick Reference, from `common-issues.md` Apple Business Governance Failure Scenarios routing); how to use the 7-leaf tree (start at root, follow the matching branch, click the leaf to the route target or follow the inline scope-boundary callout for Intune-scope); cap caveat (exactly 1 v1.6 L2 runbook — #26; per-leaf deep-dives live in the routed-to docs, not duplicated here per D-04 anti-redundancy).

    **(5) `## When to Use` H2:** brief 3-5 bullet list of triggering symptoms (Apple Business portal "permission denied" error, sub-org admin cannot perform an action they expect to be allowed, cross-OU action returns scope error, federation handshake failure, quota-limit error, Account Holder unable to perform action they should own).

    **(6) `## 7-Leaf Decision Tree` H2** containing a Mermaid fenced code block — use the LOCKED template from 65-PATTERNS.md "L2 runbook + Mermaid tree, new" / Mermaid tree template VERBATIM. Specifically:

    - Open: ` ```mermaid` on its own line followed by `graph TD` then the node definitions
    - Root: `ABPD1{"What type of<br/>permission error?"}` (diamond decision node)
    - 7 branches via `-->|"Edge label"|` to leaf IDs: ABPDR1 (role-lacks-permission, green/resolved), ABPDR2 (OU-boundary, green), ABPDR3 (Apple-Business-scope, green), ABPDE1 (Intune-scope, red/escalateL2 — INLINE TEXT, NO click directive), ABPDR5 (federation-state, green), ABPDR6 (quota-limit, green), ABPDR7 (Account-Holder-lockout, green)
    - 6 `click` directives (two-argument form `click NODE "url"`, NO tooltip third-arg per Pitfall 5): ABPDR1 → `../cross-platform/apple-business/01-role-permission-model.md`; ABPDR2 → `../cross-platform/apple-business/02-ous-architecture.md`; ABPDR3 → `../cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md`; ABPDR5 → `../cross-platform/apple-business/16-managed-apple-account-runbook.md`; ABPDR6 → `../cross-platform/apple-business/08-managed-apple-account-provisioning.md`; ABPDR7 → `../cross-platform/apple-business/01-role-permission-model.md`
    - ZERO click directives on ABPDE1 (inline-only, hybrid leaf per D-02)
    - classDef declarations: `classDef resolved fill:#28a745,color:#fff` and `classDef escalateL2 fill:#dc3545,color:#fff`
    - class assignments: `class ABPDR1,ABPDR2,ABPDR3,ABPDR5,ABPDR6,ABPDR7 resolved` and `class ABPDE1 escalateL2`
    - Close: ` ``` ` on its own line

    V-65-06 grep `/\(\[/g` MUST count ≥ 7. Each `([...])` terminal-node open contributes one match.

    **(7) `## Leaf Reference` H2** with per-leaf sub-sections explaining the route target and required L2 follow-up actions. One H3 per leaf in tree order:

    - `### ABPDR1 — Role lacks permission`: short paragraph + link to `01-role-permission-model.md` 7-subgroup catalog; "next step: verify user role assignment in Apple Business portal Settings > People > [user] > Role"
    - `### ABPDR2 — OU boundary violation`: link to `02-ous-architecture.md` + cross-link to `05-sub-org-admin-onboarding.md#which-admin-owns-this-pool`; "next step: confirm the failing action's OU vs the user's OU-scoped role"
    - `### ABPDR3 — Apple Business scope error`: link to `18-cross-org-boundary-cheat-sheet.md` + `01-role-permission-model.md` Edit-without-View (OP-3); "next step: determine whether the action is Apple-Business-scoped or Intune-scoped"
    - `### ABPDE1 — Intune-scope path (out of Apple Business surface)`: PREFERRED OPTION A SCOPE-BOUNDARY CALLOUT (no ABAUDIT needed) — quote from 65-RESEARCH.md:280-285 verbatim or adapt while strictly avoiding C15 banned phrases. Use phrasing like: "> **Scope boundary:** This path involves MDM commands (ClearPasscode / EraseDevice) that are issued from the Intune admin center, outside the Apple Business permission surface. See [18-cross-org-boundary-cheat-sheet.md](../cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md) for the full Apple-Business-vs-Intune responsibility table." DO NOT write "Intune RBAC" or "Intune role" or "Intune-side delegation/RBAC". If Option B becomes necessary, allocate ABAUDIT-24 with the colon-suffixed form `<!-- ABAUDIT-24: next line names the Intune-side scope to clarify the boundary; C15 regex N false-positive exemption (scope-boundary callout, not a delegation claim) -->` on the line immediately before the banned phrase (Pitfall 3 colon-required). Record any ABAUDIT-24 allocation in `65-CONVENTIONS.md` Section 3 (update if Option B chosen).
    - `### ABPDR5 — Federation state error`: link to `16-managed-apple-account-runbook.md` 60-day federation collision section; "next step: check SCIM/OIDC sync status in Apple Business Settings > Accounts"
    - `### ABPDR6 — Quota limit reached`: link to `08-managed-apple-account-provisioning.md`; "next step: check Managed Apple Account quota in provisioning runbook"
    - `### ABPDR7 — Account Holder lockout`: link to `01-role-permission-model.md:39-58` OP-2 callout; "next step: Account Holder DO-NOT-delegate per OP-2 — lockout recovery requires Apple Support engagement (deferred to v1.7+ dedicated runbook)"

    **(8) `## Investigation Checklist` H2:** numbered list of L2 diagnostic steps independent of leaf (collect: portal screenshot of error, exact error string, user UPN, user role assignment, OU of the action target, OU of the user role, timestamp, prior L1 escalation context if applicable).

    **(9) `## Cross-References` H2:** bullet links to the 6 leaf-target docs + `34-apple-business-shared-ipad-passcode-reset.md` (L1 #34 — the Path A handoff source) + `12-shared-ipad-passcode-reset.md` (canonical admin-context for Paths B/C).

    **(10) `## Version History` H2:** one row `| 2026-05-22 | Phase 65 plan 65-02: initial authoring — L2 Apple Business Permission Denied Investigation; 7-leaf Mermaid tree (DA-9 LOCKED); hybrid leaf behavior per D-02 (6 route + 1 inline Intune-scope) | -- |`.

    After authoring, run `node scripts/validation/check-phase-65.mjs`; V-65-05 + V-65-06 should now PASS. Run `node scripts/validation/v1.6-milestone-audit.mjs` to confirm C15 has not detected a banned phrase in this file.
  </action>
  <acceptance_criteria>
    - File `docs/l2-runbooks/26-apple-business-permission-denied.md` exists
    - Frontmatter contains verbatim: `audience: L2`, `platform: ios+macos+shared-ipad`, `last_verified: 2026-05-22`, `review_by: 2026-07-21`, `applies_to: apple-business`
    - Contains exactly one ` ```mermaid` fenced block opening followed by `graph TD`
    - Mermaid block contains the 7 leaf-node IDs: `ABPDR1`, `ABPDR2`, `ABPDR3`, `ABPDE1`, `ABPDR5`, `ABPDR6`, `ABPDR7`
    - Mermaid block contains at least 7 `([` substring occurrences (V-65-06 leaf count)
    - Mermaid block contains exactly 6 `click ABPDR` lines (NOT 7 — ABPDE1 has NO click)
    - Mermaid block does NOT contain a line starting with `click ABPDE1` (Intune-scope leaf inline-only per D-02)
    - Contains the substring `01-role-permission-model.md` at least twice (ABPDR1 + ABPDR7 route targets)
    - Contains the substring `02-ous-architecture.md` (ABPDR2 target)
    - Contains the substring `18-cross-org-boundary-cheat-sheet.md` (ABPDR3 target + Intune-scope callout)
    - Contains the substring `16-managed-apple-account-runbook.md` (ABPDR5 target)
    - Contains the substring `08-managed-apple-account-provisioning.md` (ABPDR6 target)
    - Contains a `> **Scope boundary:**` blockquote in the ABPDE1 Leaf Reference section
    - Does NOT contain the literal phrase `Intune RBAC` (C15 regex 1 — UNLESS preceded immediately by an `<!-- ABAUDIT-NN: ... -->` exemption line; Option A preferred → no ABAUDIT needed)
    - Does NOT contain the literal phrase `Intune role` (C15 regex 1 guard)
    - Does NOT contain `Intune-side` followed within 40 chars by `delegat`, `RBAC`, or `role assign` (C15 regex 4 guard)
    - Contains a `## Version History` H2 with a row dated `2026-05-22`
    - `node scripts/validation/check-phase-65.mjs` reports V-65-05 PASS, V-65-06 PASS
    - `node scripts/validation/v1.6-milestone-audit.mjs` C15 reports no NEW failures introduced by this file
  </acceptance_criteria>
  <done>
    L2 #26 ships with the LOCKED 7-leaf Mermaid tree. Apple-Business-scoped leaves route via Mermaid `click`; Intune-scope leaf is C15-safe inline-only; Account-Holder-lockout routes to OP-2 callout (no companion runbook per deferred v1.7+ scope). D-02 hybrid leaf pattern realized.
  </done>
</task>

<task type="auto">
  <name>Task 4: Append `## Apple Business L2 Runbooks` H2 to `docs/l2-runbooks/00-index.md` with the #26 row</name>
  <files>docs/l2-runbooks/00-index.md</files>
  <read_first>
    - docs/l2-runbooks/00-index.md (full file — identify the existing H2 sections + insertion point: after last platform-group H2, before any `## Version History` H2 at file tail)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-PATTERNS.md "docs/l2-runbooks/00-index.md (index append, ABNAV-02 side effect)" (exact append block)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-RESEARCH.md §"L2 00-index Append Pattern" lines 329-343
  </read_first>
  <action>
    Append to `docs/l2-runbooks/00-index.md` a new H2 section `## Apple Business L2 Runbooks` with a Version gate blockquote, a `### When to Use` H3, and a 3-column table (Runbook | When to Use | Prerequisite). Exact append block from 65-PATTERNS.md (verbatim):

    ```
    ## Apple Business L2 Runbooks

    > **Version gate:** The runbooks below cover Apple Business Delegated Governance through Apple Business portal and associated MDM surfaces (Phase 65 deliverables).

    ### When to Use

    | Runbook | When to Use | Prerequisite |
    |---------|-------------|--------------|
    | [Apple Business Permission Denied Investigation](26-apple-business-permission-denied.md) | Apple Business portal returns permission error across any delegation action; includes 7-leaf triage tree routing to per-cause runbooks | None |
    ```

    Insertion point: AFTER the last existing platform-group H2 (Linux L2 section, runbooks 24-25 per CONTEXT/RESEARCH), BEFORE any `## Version History` H2. PITFALL-6 append-only — zero existing H2/H3 anchors shift. If the index has a Version History table, add a row `| 2026-05-22 | Phase 65 plan 65-02: appended Apple Business L2 Runbooks H2 with #26 row | -- |`. Honor the #26-cap invariant: exactly ONE row in the new table.
  </action>
  <acceptance_criteria>
    - File `docs/l2-runbooks/00-index.md` contains the literal H2 heading `## Apple Business L2 Runbooks`
    - Contains the literal substring `26-apple-business-permission-denied.md` exactly once
    - Contains the `### When to Use` H3 immediately after the `## Apple Business L2 Runbooks` H2 + Version-gate blockquote
    - The new H2 appears AFTER any pre-existing platform-group H2 and BEFORE any `## Version History` H2
    - The new When-to-Use table contains exactly 1 data row (#26 cap honored)
    - Diff against the pre-edit file shows ONLY additions (no deletions, no reordering)
    - All H2/H3 anchors that existed pre-edit still exist post-edit
  </acceptance_criteria>
  <done>
    L2 #26 is discoverable from the L2 index. ABNAV-02 navigation surface is complete. The #26-cap invariant is honored at exactly 1 v1.6 L2 runbook.
  </done>
</task>

</tasks>

<verification>
- `node scripts/validation/check-phase-65.mjs` exits with V-65-01..V-65-06 ALL PASS (L1 #34 file + 3 substrings + L2 #26 file + Mermaid 7-leaf count); V-65-07..V-65-14 still report Wave 3+ pending
- `node scripts/validation/v1.6-milestone-audit.mjs` runs without NEW C15 failures (no Intune banned phrases in L1 #34 or L2 #26); C16 admin_12 edge still EXEMPTED via allowlist (Wave 4 not landed yet)
- `node scripts/validation/check-phase-64.mjs` exits 0 (Phase 64 chain still green at Wave 2; flip happens in Wave 4)
- All 4 files exist and contain their LOAD-BEARING substrings
</verification>

<success_criteria>
- L1 #34 documents Path A end-to-end as L1-executable; Paths B/C as escalate-to-L2 pointers per D-01 + REQUIREMENTS:93
- L1 #34 contains `#which-admin-owns-this-pool` lookup BEFORE Path A execution (ABNAV-01)
- L1 #34 + L2 #26 carry compound `platform: ios+macos+shared-ipad` frontmatter (Phase 62 `+` contract / D-A5)
- L2 #26 7-leaf Mermaid tree present with hybrid leaf behavior (6 route via `click` + 1 inline Intune-scope C15-safe) per D-02
- Account-Holder-lockout leaf routes to OP-2 callout, no companion runbook (deferred v1.7+)
- L1 + L2 index files each gain one append-only H2 section with their single runbook row (CI-5 caps honored: 1 L1 + 1 L2)
- Zero C15 banned-phrase failures introduced by Phase 65 deliverables in this plan
</success_criteria>

<output>
Create `.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-02-SUMMARY.md` when all 4 tasks complete. Record: (a) the 4 files created/modified; (b) V-65-01..V-65-06 assertion-state baseline post-Wave-2 (PASS/FAIL per assertion); (c) any C15 banned-phrase guards triggered (expected: zero); (d) whether ABAUDIT-24 was allocated (Option A preferred → expected: no allocation needed; if Option B chosen, record ABAUDIT-24 in 65-CONVENTIONS.md Section 3); (e) confirmation that CI-5 L1 cap (#34 only) and L2 cap (#26 only) are honored.
</output>
</content>
</invoke>