---
phase: 62
plan: 62-01
title: D-05 planning-doc count-correction + per-permission scrape preparation
type: execute
wave: 1
status: pending
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/62-apple-business-foundation-rebrand/62-SCRAPE-PREP.md
requirements:
  - AB-06
threats: []
autonomous: true

must_haves:
  truths:
    - "Planning files (REQUIREMENTS.md AB-06, ROADMAP.md Phase 62 SC#4, STATE.md D-A2) state '4 existing platform glossaries' with rationale footnote"
    - "Scrape-preparation artifact (`62-SCRAPE-PREP.md`) lists exact 7 Apple permission sub-page URLs + 7-column row schema + estimated row counts per subgroup so Plan 62-04 can execute the 1-hour manual scrape against a deterministic input list"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "AB-06 text patched '5'→'4 existing platform glossaries' + inline footnote"
      contains: "4 existing platform glossaries"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 62 SC#4 patched '5'→'4 existing platform glossaries'"
      contains: "4 existing platform glossaries"
    - path: ".planning/STATE.md"
      provides: "D-A2 patched '5'→'4 existing glossaries'"
      contains: "4 existing glossaries"
    - path: ".planning/phases/62-apple-business-foundation-rebrand/62-SCRAPE-PREP.md"
      provides: "Scrape input list + 7-column row schema for Plan 62-04 executor"
      min_lines: 50
  key_links:
    - from: ".planning/REQUIREMENTS.md AB-06"
      to: ".planning/STATE.md D-A2"
      via: "matching footnote referencing `_glossary-macos.md` line-9 header"
      pattern: "_glossary-macos\\.md covers macOS \\+ iOS/iPadOS"
    - from: ".planning/phases/62-apple-business-foundation-rebrand/62-SCRAPE-PREP.md"
      to: ".planning/phases/62-apple-business-foundation-rebrand/62-04-PLAN.md (Plan 62-04 scrape task)"
      via: "Plan 62-04 imports the 7-column row schema and URL list from this artifact"
      pattern: "62-SCRAPE-PREP\\.md"
---

<objective>
Land the D-05 inline count-correction patch across the 3 planning files (`REQUIREMENTS.md` AB-06, `ROADMAP.md` Phase 62 SC#4, `STATE.md` D-A2) replacing "5 existing platform glossaries" with "4 existing platform glossaries" + inline footnote. In the same plan, author the per-permission scrape preparation artifact (`62-SCRAPE-PREP.md`) so Plan 62-04's 1-hour manual scrape against Apple's JS-rendered permission sub-pages has a deterministic input list + row schema.

Purpose: Unblocks AB-06 traceability (count math is correct from Phase 62 forward; `_glossary-apple-business.md` becomes the 5th glossary node counted v1.6-forward) AND prepares the Plan 62-04 scrape so the executor does not waste context exploring the Apple URL surface.

Output: 3 patched planning files + 1 new scrape-prep artifact (4 commits or 1 batched commit).
</objective>

<execution_context>
@D:/claude/Autopilot/CLAUDE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-apple-business-foundation-rebrand/62-CONTEXT.md
@.planning/phases/62-apple-business-foundation-rebrand/62-RESEARCH.md
@.planning/phases/62-apple-business-foundation-rebrand/62-VALIDATION.md
@.planning/phases/62-apple-business-foundation-rebrand/62-PATTERNS.md

<interfaces>
<!-- Existing AB-06 / SC#4 / D-A2 text (verbatim from REQUIREMENTS.md line 17, ROADMAP.md Phase 62 SC#4 line 165, STATE.md D-A2 line 83) -->

REQUIREMENTS.md line 17 (verbatim, pre-patch):
> - [ ] **AB-06**: 5 existing platform glossaries each gain one reciprocal banner line pointing to `_glossary-apple-business.md`; `_glossary-macos.md` gains one inline see-also at the ABM entry per v1.5 CLEAN-08 pattern

ROADMAP.md Phase 62 SC#4 (verbatim, pre-patch):
> 4. 5 existing platform glossaries each gain exactly one reciprocal banner line pointing to `_glossary-apple-business.md`; `_glossary-macos.md` ABM entry gains one inline see-also; zero existing glossary anchors shift (PITFALL-6 anchor-stability invariant preserved; pre-edit anchor inventory artifact captured before any glossary edit)

STATE.md D-A2 (verbatim, pre-patch):
> **D-A2** Glossary architecture → Split (Option c) — NEW `_glossary-apple-business.md` + reciprocal banner lines to 5 existing glossaries

Footnote text (per CONTEXT.md `<specifics>` D-05):
> ¹ `_glossary-macos.md` covers macOS + iOS/iPadOS per its line-9 header — single Apple glossary, not separate macOS/iOS files. Count corrected from "5" to "4" during Phase 62 execution per D-05.

7 in-scope Apple permission subgroups (from RESEARCH.md §1, CONTEXT.md):
1. Basic Organization
2. Organization Access
3. API+OAuth
4. People
5. Devices
6. AppleCare
7. Apps & Books

(Brand-related subgroups explicitly EXCLUDED with "see Apple official docs" pointer per D-02; logged in `c13_rotting_external` sidecar.)

7-column row schema (from RESEARCH.md §1):
| subgroup | permission_name | scope | edit_vs_view | op1_whitelist_relevance | op3_dependency_notes | apple_url_anchor |

Apple URL inputs (from CONTEXT.md canonical_refs Apple-side sources):
- root: https://support.apple.com/guide/apple-business-manager/intro-to-roles-and-privileges-axm97dd59159/web
- (subgroup sub-page navigation paths to be transcribed during scrape; root page has the table of subgroup links)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 62-01-01: Patch REQUIREMENTS.md AB-06 + ROADMAP.md Phase 62 SC#4 + STATE.md D-A2 from "5"→"4 existing platform glossaries" with footnote</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Per D-05 (CONTEXT.md decisions section + RESEARCH.md `<user_constraints>` + PATTERNS.md "MODIFIED — planning"), patch 3 planning files using Edit tool (NOT corpus edits per Q5(b)):

1. **`.planning/REQUIREMENTS.md` line 17** — replace:
   ```
   - [ ] **AB-06**: 5 existing platform glossaries each gain one reciprocal banner line pointing to `_glossary-apple-business.md`; `_glossary-macos.md` gains one inline see-also at the ABM entry per v1.5 CLEAN-08 pattern
   ```
   with:
   ```
   - [ ] **AB-06**: 4 existing platform glossaries¹ each gain one reciprocal banner line pointing to `_glossary-apple-business.md`; `_glossary-macos.md` gains one inline see-also at the ABM entry per v1.5 CLEAN-08 pattern
   ```
   And add the footnote at the bottom of the Pillar 1 block (or inline right after the AB-06 bullet — pick whichever insertion point keeps Pillar 1 visually intact):
   ```
   ¹ `_glossary-macos.md` covers macOS + iOS/iPadOS per its line-9 header — single Apple glossary, not separate macOS/iOS files. Count corrected from "5" to "4" during Phase 62 execution per D-05.
   ```

2. **`.planning/ROADMAP.md` Phase 62 SC#4** (currently around line 165 — re-grep to locate the exact line at execution time; the SC#4 text starts "5 existing platform glossaries each gain exactly one reciprocal banner line"). Replace "5 existing platform glossaries" with "4 existing platform glossaries¹" and add the same footnote text below the SC list.

3. **`.planning/STATE.md` D-A2 line 83** — replace:
   ```
   - **D-A2** Glossary architecture → Split (Option c) — NEW `_glossary-apple-business.md` + reciprocal banner lines to 5 existing glossaries
   ```
   with:
   ```
   - **D-A2** Glossary architecture → Split (Option c) — NEW `_glossary-apple-business.md` + reciprocal banner lines to 4 existing glossaries¹
   ```
   And add the footnote text after the closing of the D-A1..D-A10 architectural decision block (preserve list visual integrity).

**Q5(b) compliance reminder:** Planning-file edits are NOT corpus edits. These are decision-document corrections, not v1.0–v1.5 documentation modifications. Do NOT touch any `docs/**` files in this task.

**Verification:** grep for "5 existing platform glossaries" must return ZERO matches across the 3 files post-edit; grep for "4 existing platform glossaries" must return EXACTLY 2 matches (REQUIREMENTS.md AB-06 + ROADMAP.md SC#4); grep for "4 existing glossaries" must return EXACTLY 1 match (STATE.md D-A2; differs in wording).
  </action>
  <verify>
    <automated>grep -v '^#' .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md | grep -c "5 existing platform glossaries"</automated>
    <expected>0</expected>
    <also-check>grep -v '^#' .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md | grep -E "4 existing (platform )?glossaries" -c → returns 3</also-check>
  </verify>
  <done>3 planning files patched; "5 existing platform glossaries" string gone from all 3; "4 existing" count + matching footnote present in all 3.</done>
</task>

<task type="auto">
  <name>Task 62-01-02: Author `62-SCRAPE-PREP.md` artifact — Apple permission sub-page URL list + 7-column row schema + per-subgroup row-count estimates for Plan 62-04</name>
  <files>.planning/phases/62-apple-business-foundation-rebrand/62-SCRAPE-PREP.md</files>
  <action>
Create new file `.planning/phases/62-apple-business-foundation-rebrand/62-SCRAPE-PREP.md` containing the deterministic input list Plan 62-04's executor will use during the 1-hour manual scrape. Structure:

```markdown
# Phase 62 Per-Permission Catalog Scrape Preparation

**Prepared:** 2026-05-21
**Consumed by:** Plan 62-04 (`01-role-permission-model.md` authoring task)
**Purpose:** Deterministic input list + row schema so the 1-hour manual scrape against
Apple's JS-rendered permission sub-pages produces canonical, machine-checkable output.

## In-Scope Subgroups (7) per D-02

| # | Subgroup | Apple Portal Path | Estimated Row Count | OP-1/OP-3 Flags |
|---|----------|-------------------|---------------------|-----------------|
| 1 | Basic Organization | Apple Business portal → Access Management → Roles → Basic Organization | 3-5 | Accept T&S = DENY-by-default |
| 2 | Organization Access | ...Access Management → Roles → Organization Access | 7-9 | Delete OUs / Assign roles = DENY-by-default |
| 3 | API+OAuth | ...Access Management → Roles → API+OAuth | 2-3 | Revoke = DENY-by-default |
| 4 | People | ...Access Management → Roles → People | 6-8 | Reset Shared iPad passcode = conditionally-grant (OP-11) |
| 5 | Devices | ...Access Management → Roles → Devices | 8-10 | Manage MDM Servers = DENY-by-default (OP-1 CRITICAL) |
| 6 | AppleCare | ...Access Management → Roles → AppleCare | 2 | N/A |
| 7 | Apps & Books | ...Access Management → Roles → Apps & Books | 7-8 | Download content tokens = conditionally-grant |

Estimated total rows: ~35-45 (RESEARCH.md §1 estimate: ~50; Plan 62-04 catalog size).

## Out-of-Scope Subgroups (Brand-related per D-02)

These subgroups are EXCLUDED from the Phase 62 catalog per D-02; `01-role-permission-model.md`
ships a pointer "see Apple official docs at `axm97dd59159`" and the omitted subgroups are
logged in `c13_rotting_external` sidecar (Plan 62-08).

- Brand Manager subgroup (any Marketing/Brand-related permissions Apple groups separately)

## 7-Column Row Schema (mandatory for every scraped permission)

```
| subgroup | permission_name | scope | edit_vs_view | op1_whitelist_relevance | op3_dependency_notes | apple_url_anchor |
```

- **subgroup**: one of the 7 in-scope subgroups (exact label from above table)
- **permission_name**: VERBATIM Apple portal label (do NOT paraphrase; if Apple uses "Manage MDM Servers" do not write "MDM Server Management")
- **scope**: {OU-scoped | tenant-wide | device-level | user-level}
- **edit_vs_view**: {View-only | Edit | Edit+View (paired) | Standalone | Edit+bundle (for superprivileges like Manage MDM Servers)}
- **op1_whitelist_relevance**: {DENY-by-default | conditionally-grant | always-grant} — OP-1 flag
- **op3_dependency_notes**: companion View permission required for non-blank UI (blank if Standalone); e.g., "Requires 'View Devices'"
- **apple_url_anchor**: Apple article ID (`axm97dd59159`, etc.) or `portal-only` if no static URL

## Apple URL Inputs

Root page (table of subgroup links):
- https://support.apple.com/guide/apple-business-manager/intro-to-roles-and-privileges-axm97dd59159/web

Sub-pages are navigated FROM the root; their URLs are JS-routed and may not have stable
deep-link anchors. Where Apple has assigned a stable `axm*` article ID, capture it in the
`apple_url_anchor` column; otherwise record `portal-only`.

## Known Cross-Subgroup Dependencies (Edit-without-View; OP-3 prevention)

Seed pairs from RESEARCH.md §1; manual scrape must verify and EXTEND if Apple lists additional pairs:

| Edit permission | Required companion View | Blank-UI symptom |
|----------------|------------------------|-----------------|
| Assign devices to MDM server | View Devices (Devices subgroup) | Devices tab shows empty list |
| Release devices | View Devices | Devices tab shows empty list |
| Assign content (device-licensed) | View content tokens (Apps & Books) | Apps & Books tab shows empty |
| Reclaim licenses | View purchase history (Apps & Books) | Can't see what to reclaim |
| Edit Managed Apple Accounts | View users (Organization Access) | People tab shows empty |
| Edit OUs | View OUs (Organization Access) | OU list shows empty |
| Configure SCIM provisioning | View federated auth status (Organization Access) | Federation settings blank |

## Scrape Execution Notes (for Plan 62-04 executor)

1. Open the root page in a real browser (NOT WebFetch — pages are JS-rendered).
2. Sign in to Apple Business portal with read-only credentials if available; if not, the
   public Apple Business User Guide also exposes the per-permission tables (lower fidelity
   but acceptable as fallback).
3. For each of the 7 subgroups, transcribe ALL permissions into the 7-column schema.
4. For "Manage MDM Servers" specifically — flag as DENY-by-default with OP-1 superprivilege
   note (bundles Add MDM server / Edit MDM server / Assign devices to MDM server / Download
   MDM server token; Apple does NOT separately gate these in the UI per OP-1).
5. For Brand-related subgroups encountered during navigation, do NOT scrape; instead, log
   the URL/subgroup name in a tail section "Brand-related subgroups (deferred per D-02)"
   for inclusion in `c13_rotting_external` sidecar.

## Output Location

The scraped rows land in `docs/cross-platform/apple-business/01-role-permission-model.md`
H2 subgroup sections (Plan 62-04 Task 02). The Edit-without-View dependency table lands in
the same file (Plan 62-04 Task 03).

---
*Scrape preparation finalized: 2026-05-21*
```

This artifact is consumed by Plan 62-04's executor as a deterministic input. Do not duplicate it into the published `01-role-permission-model.md` — it remains a planning-phase support artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/62-apple-business-foundation-rebrand/62-SCRAPE-PREP.md && grep -c "7-Column Row Schema" .planning/phases/62-apple-business-foundation-rebrand/62-SCRAPE-PREP.md</automated>
    <expected>file exists; "7-Column Row Schema" appears at least once</expected>
  </verify>
  <done>`62-SCRAPE-PREP.md` exists with all 7 in-scope subgroups listed + 7-column schema documented + 7-row seed Edit-without-View dependency table + execution notes for Plan 62-04 executor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning files ↔ corpus | D-05 patches affect ONLY planning files (REQUIREMENTS.md / ROADMAP.md / STATE.md); zero `docs/**` files touched. Q5(b) no-corpus-sweep invariant preserved. |

## STRIDE Threat Register

This plan has no threat references per VALIDATION.md (no PII, no external URLs in this plan's deliverables, no harness logic).

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| (none) | — | — | — | — |

T-62-A (PII exposure) — N/A (no `_admin-directory.md` work here).
T-62-B (C-check false-negative) — N/A (no harness work here).
T-62-C (Apple URL rot) — N/A (URL list captured in `62-SCRAPE-PREP.md` is a planning-phase artifact, not corpus; rot here only delays Plan 62-04 scrape — does not corrupt published docs).
</threat_model>

<verification>
Per VALIDATION.md V-62-01 (planning patches structural check):

| Task ID | V-62-XX | Command |
|---------|---------|---------|
| 62-01-01 | V-62-01 | `grep -v '^#' .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md \| grep -c "4 existing"` → returns 3 |
| 62-01-02 | (W0 prep) | `test -f .planning/phases/62-apple-business-foundation-rebrand/62-SCRAPE-PREP.md && grep -c "7-Column Row Schema" $_` → file present + schema present |

After plan completes: `node scripts/validation/check-phase-62.mjs` is NOT YET available (Plan 62-08 produces it); run the inline grep verifications above instead.
</verification>

<success_criteria>
1. All 3 planning files (REQUIREMENTS.md, ROADMAP.md, STATE.md) contain "4 existing" wording + matching D-05 footnote referencing `_glossary-macos.md` line-9 header.
2. Zero remaining "5 existing platform glossaries" or "5 existing glossaries" references in the 3 planning files.
3. `62-SCRAPE-PREP.md` exists with 7 in-scope subgroups + URL list + 7-column row schema + 7-row Edit-without-View seed + execution notes.
4. Zero `docs/**` files modified in this plan (Q5(b) compliance verified by `git diff --name-only` showing only planning paths).
</success_criteria>

<output>
After completion, create `.planning/phases/62-apple-business-foundation-rebrand/62-01-SUMMARY.md` recording:
- Patch confirmations (grep counts before/after for each file)
- Footnote location chosen for each file
- Scrape-prep artifact path + total subgroup count + URL list size
- Any deviations from this plan (with rationale)
</output>
