---
phase: 62
plan: 62-04
title: Author `01-role-permission-model.md` (Account Holder DO-NOT-DELEGATE + 7-subgroup permission catalog + Edit-without-View dependency table)
type: execute
wave: 3
status: pending
depends_on: [62-01, 62-02, 62-03]
files_modified:
  - docs/cross-platform/apple-business/01-role-permission-model.md
requirements:
  - AB-02
  - AB-03
threats: []
autonomous: false

must_haves:
  truths:
    - "Admin can identify which of the 4 top-level roles (Account Holder / IT Administrator / People Administrator / Content Administrator) and which of the 5 preset custom roles (People Manager / Content Manager / Device Enrollment Manager / Device API Manager / Brand Manager) apply to their delegation use case"
    - "Hard-bordered 'Account Holder — DO NOT DELEGATE' callout (OP-2 prevention) appears in the intro region; references PITFALLS.md OP-2 + DA-2"
    - "Full canonical per-permission catalog spans all 7 in-scope subgroups (Basic Organization / Organization Access / API+OAuth / People / Devices / AppleCare / Apps & Books) with the 7-column row schema from RESEARCH.md §1"
    - "Edit-without-View dependency table (OP-3 prevention) lists cross-subgroup Edit/View pairs"
    - "Brand-related subgroups are EXCLUDED with 'see Apple official docs at axm97dd59159' pointer per D-02; logged for `c13_rotting_external` sidecar"
    - "'Manage MDM Servers' superprivilege carries DENY-by-default flag per OP-1 whitelist-first invariant"
  artifacts:
    - path: "docs/cross-platform/apple-business/01-role-permission-model.md"
      provides: "AB-02 role overview + AB-03 per-permission catalog + OP-2/OP-3/OP-1 callouts; SOT for Phase 63 `04-custom-role-authoring.md`"
      contains: "Account Holder — DO NOT DELEGATE"
      min_lines: 200
  key_links:
    - from: "docs/cross-platform/apple-business/01-role-permission-model.md per-permission catalog"
      to: "7 subgroup sections"
      via: "H3 (or H2) headings matching subgroup labels from `62-SCRAPE-PREP.md`"
      pattern: "(Basic Organization|Organization Access|API\\+OAuth|People|Devices|AppleCare|Apps & Books)"
    - from: "docs/cross-platform/apple-business/01-role-permission-model.md Edit-without-View dependency table"
      to: "7+ cross-subgroup pairs"
      via: "table rows mapping Edit permission to required companion View"
      pattern: "Edit-without-View"
---

<objective>
Author `01-role-permission-model.md` — the canonical Apple Business role/permission reference. Per AB-02, ship 4 top-level roles + 5 preset custom roles + Account Holder DO-NOT-DELEGATE callout. Per AB-03, ship the full canonical per-permission catalog across 7 in-scope subgroups (35-50 rows from a 1-hour manual scrape) + the Edit-without-View dependency table (OP-3 prevention).

This plan is `autonomous: false` per RESEARCH.md "Critical Planner Alert" + CONTEXT.md `<specifics>` D-02 — the per-permission catalog requires a 1-hour manual scrape against Apple's JS-rendered permission sub-pages. The executor must perform this scrape using a real browser; WebFetch cannot extract these tables.

Purpose: Establishes the SOT that Phase 63 `04-custom-role-authoring.md` cites for the min-viable sub-org admin permission bundle template. Without this catalog, Phase 63 cannot ship.

Output: 1 new file at `docs/cross-platform/apple-business/01-role-permission-model.md` (~250-400 lines).
</objective>

<execution_context>
@D:/claude/Autopilot/CLAUDE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/phases/62-apple-business-foundation-rebrand/62-CONTEXT.md
@.planning/phases/62-apple-business-foundation-rebrand/62-RESEARCH.md
@.planning/phases/62-apple-business-foundation-rebrand/62-PATTERNS.md
@.planning/phases/62-apple-business-foundation-rebrand/62-SCRAPE-PREP.md
@docs/operations/co-management/00-overview.md

Frontmatter (use exactly):
```yaml
---
last_verified: 2026-05-21
review_by: 2026-07-20
applies_to: apple-business
audience: admin
platform: ios+macos
---
```

7 in-scope subgroups (from RESEARCH.md §1 + 62-SCRAPE-PREP.md):
1. Basic Organization (~3-5 perms)
2. Organization Access (~7-9 perms; includes Edit/Delete OUs, Assign roles)
3. API+OAuth (~2-3 perms)
4. People (~6-8 perms; INCLUDES Reset Shared iPad passcode — OP-11 conditionally-grant)
5. Devices (~8-10 perms; INCLUDES Manage MDM Servers — OP-1 DENY-by-default; superprivilege)
6. AppleCare (~2 perms)
7. Apps & Books (~7-8 perms; content token permissions)

7-column row schema:
| subgroup | permission_name | scope | edit_vs_view | op1_whitelist_relevance | op3_dependency_notes | apple_url_anchor |

Edit-without-View seed pairs (RESEARCH.md §1 lines 230-244; extend during scrape).
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 62-04-CHECKPOINT-0: Confirm Apple Business portal access OR Apple Business User Guide public-page access before scrape</name>
  <what-built>Plan 62-04 requires a 1-hour manual scrape against Apple's JS-rendered permission sub-pages per RESEARCH.md "Critical Planner Alert" + D-02. The executor needs either (a) read-only Apple Business portal credentials OR (b) public-internet access to https://support.apple.com/guide/apple-business-manager/intro-to-roles-and-privileges-axm97dd59159/web and its sub-pages from a real browser (NOT WebFetch).</what-built>
  <how-to-verify>
1. Open the root URL above in a real browser.
2. Verify the root page renders with a navigable table of contents listing the 7+ subgroups.
3. Click at least one in-scope subgroup (e.g., Devices) and confirm its permission table renders.
4. If steps 1-3 succeed: type "scrape-ready" and proceed.
5. If pages do not render (network blocked, Apple geofencing): the executor needs portal credentials. Without either, this plan blocks.
  </how-to-verify>
  <resume-signal>Type "scrape-ready" (browser access confirmed) or "blocked: need credentials"</resume-signal>
</task>

<task type="auto">
  <name>Task 62-04-01: Create `01-role-permission-model.md` with frontmatter + Account Holder DO-NOT-DELEGATE callout + 4 top-level roles + 5 preset custom roles</name>
  <files>docs/cross-platform/apple-business/01-role-permission-model.md</files>
  <action>
Author the intro + role overview — this does NOT require the manual scrape. Structure:

1. Frontmatter (as specified in context).
2. Platform-applicability blockquote citing the glossary at `../../_glossary-apple-business.md`.
3. H1 "Apple Business Role and Permission Model" + intro paragraph framing scope.
4. Hard-bordered Account Holder DO-NOT-DELEGATE callout (blockquote + bold) — references OP-2 / DA-2; must contain the literal string `Account Holder — DO NOT DELEGATE` for grep-based verification.
5. H2 `## Top-Level Roles (4)` — numbered-list pattern per `operations/co-management/00-overview.md` lines 40-66:
   1. Account Holder (DO NOT DELEGATE; tenant-level; lockout-recovery authority)
   2. IT Administrator (mid-tier; broad device + content + people scope)
   3. People Administrator (people/account scope tenant-wide)
   4. Content Administrator (content/VPP scope tenant-wide)
6. H2 `## Preset Custom Roles (5)`:
   1. People Manager (preset; per-OU people scope; first-mention: "formerly built-in role")
   2. Content Manager (preset; per-OU content scope)
   3. Device Enrollment Manager (preset; per-OU enrollment scope)
   4. Device API Manager (preset; Apple has not published developer.apple.com landing; v1.6 acknowledges-but-does-not-document-deeply per CONTEXT.md `<deferred>` line 233)
   5. Brand Manager (preset; **permissions EXCLUDED from catalog per D-02; pointer to Apple official docs at axm97dd59159**)

Cross-link the glossary at `../../_glossary-apple-business.md` and the OUs doc at `02-ous-architecture.md` (forward reference — Plan 62-05 creates it).
  </action>
  <verify>
    <automated>grep -c "Account Holder — DO NOT DELEGATE" docs/cross-platform/apple-business/01-role-permission-model.md && grep -cE "^## (Top-Level Roles|Preset Custom Roles)" docs/cross-platform/apple-business/01-role-permission-model.md</automated>
    <expected>Account Holder DO-NOT-DELEGATE string present 1+ times; both H2 role sections present</expected>
  </verify>
  <done>File created with frontmatter + Account Holder DO-NOT-DELEGATE callout + 4 top-level roles + 5 preset custom roles; Brand Manager marked EXCLUDED with Apple-docs pointer.</done>
</task>

<task type="auto">
  <name>Task 62-04-02: Perform 1-hour manual scrape + author 7 subgroup sections with per-permission catalog using 7-column row schema</name>
  <files>docs/cross-platform/apple-business/01-role-permission-model.md</files>
  <action>
**Manual-scrape task.** Open the Apple Business User Guide root in a browser; navigate each of the 7 in-scope subgroup sub-pages; transcribe ALL permissions into the 7-column row schema.

Append to the existing file:

1. H2 `## Per-Permission Catalog` with intro paragraph naming the 7-column schema (subgroup / permission_name / scope / edit_vs_view / op1_whitelist_relevance / op3_dependency_notes / apple_url_anchor).
2. **OP-1 superprivilege explainer** — single paragraph documenting that "Manage MDM Servers" in the Devices subgroup bundles 4 actions (Add MDM server / Edit MDM server / Assign devices to MDM server / Download MDM server token) and that Apple does not separately gate them. DENY-by-default contract applies to all non-Account-Holder reference roles.
3. 7 H3 subgroup sections, each with a 6-column display table (one row per permission). Use the schema-driven columns: Permission / Scope / Edit-or-View / OP-1 flag / OP-3 companion View / Apple anchor. The `subgroup` column is implicit (the H3 heading IS the subgroup label).
   - `### Basic Organization`
   - `### Organization Access`
   - `### API+OAuth`
   - `### People` (MUST include "Reset Shared iPad passcode" row flagged conditionally-grant per OP-11)
   - `### Devices` (MUST include "Manage MDM Servers" row flagged DENY-by-default + OP-1 superprivilege note)
   - `### AppleCare`
   - `### Apps & Books`
4. Tail H3 `### Brand-Related Subgroups (excluded per D-02)` — single paragraph pointing to Apple's official docs; record any Brand-subgroup URLs encountered during scrape for Plan 62-08 to add to `c13_rotting_external`.

**Scrape execution checklist (executor):**
- Use VERBATIM Apple portal labels (do NOT paraphrase permission names)
- Apply OP-1 / OP-3 / OP-11 flags from RESEARCH.md §1 + 62-SCRAPE-PREP.md seed pairs
- Capture Apple article IDs (`axm*`) where stable; record `portal-only` otherwise
- Total scraped row count target: 35-50 (RESEARCH.md §1 estimate)
- For Brand-subgroup sub-pages encountered: do NOT scrape; log subgroup name + URL in a tail note appended to this section
  </action>
  <verify>
    <automated>grep -cE "^## Per-Permission Catalog" docs/cross-platform/apple-business/01-role-permission-model.md && grep -cE "^### (Basic Organization|Organization Access|API\+OAuth|People|Devices|AppleCare|Apps & Books|Brand-Related)" docs/cross-platform/apple-business/01-role-permission-model.md && grep -c "Manage MDM Servers" docs/cross-platform/apple-business/01-role-permission-model.md && grep -c "DENY-by-default" docs/cross-platform/apple-business/01-role-permission-model.md</automated>
    <expected>Per-Permission Catalog H2 present (1); 8 H3 subgroup sections (7 in-scope + 1 Brand-excluded note); Manage MDM Servers row present; DENY-by-default flag present (1+)</expected>
  </verify>
  <done>7 subgroup sections present with scraped tables; Manage MDM Servers flagged DENY-by-default; Reset Shared iPad passcode flagged conditionally-grant; Brand-Related subgroup excluded with Apple-docs pointer; total row count 35-50.</done>
</task>

<task type="auto">
  <name>Task 62-04-03: Append Edit-without-View dependency table (OP-3) + Version History + commit</name>
  <files>docs/cross-platform/apple-business/01-role-permission-model.md</files>
  <action>
Append a top-level `## Edit-without-View Dependency Table (OP-3 Prevention)` H2 below the Per-Permission Catalog. Use the 7-row seed from RESEARCH.md §1 lines 230-244 + EXTEND with additional pairs surfaced during the scrape.

Table columns: Edit permission | Required companion View | Subgroup (Edit) | Subgroup (View) | Blank-UI symptom

Required seed rows (verbatim from RESEARCH.md §1):
- Assign devices to MDM server / View Devices (Devices / Devices / "Devices tab shows empty list")
- Release devices / View Devices (Devices / Devices / "Devices tab shows empty list")
- Assign content (device-licensed) / View content tokens (Apps & Books / Apps & Books / "Apps & Books tab shows empty")
- Reclaim licenses / View purchase history (Apps & Books / Apps & Books / "Can't see what to reclaim")
- Edit Managed Apple Accounts / View users (Organization Access / Organization Access / "People tab shows empty")
- Edit OUs / View OUs (Organization Access / Organization Access / "OU list shows empty")
- Configure SCIM provisioning / View federated auth status (Organization Access / Organization Access / "Federation settings blank")

Below the table, add a one-paragraph callout reminding readers that Phase 63 `04-custom-role-authoring.md` min-viable sub-org admin bundle template MUST include both halves of every Edit-without-View pair invoked by the bundle's intended admin tasks.

**OP-3 cross-check (mental):** For each row in the Edit-without-View table, the matching Edit permission row in the Per-Permission Catalog above MUST carry an `OP-3 companion View` value referencing the same View permission. Cross-verify before committing.

Append a Version History H2 with one row dated 2026-05-21 noting Phase 62 landing (record actual scraped row count).

Commit message: `docs(62): plan 62-04 — role/permission model + 7-subgroup catalog + Edit-without-View table (AB-02 + AB-03)`
  </action>
  <verify>
    <automated>grep -cE "^## Edit-without-View Dependency Table" docs/cross-platform/apple-business/01-role-permission-model.md && grep -cE "^\| (Assign devices to MDM server|Release devices|Edit OUs|Configure SCIM provisioning) \|" docs/cross-platform/apple-business/01-role-permission-model.md</automated>
    <expected>Edit-without-View H2 present (1); 4 of the 7 seed-row Edit permissions present as table rows</expected>
  </verify>
  <done>Edit-without-View dependency table present with all 7 seed rows + any additional pairs from scrape; Phase 63 cross-reference paragraph present; Version History row added; OP-3 mental cross-check passed; file committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apple Business portal ↔ scraped catalog | Manual scrape transcribes Apple portal labels VERBATIM; no Apple PII or tenant data captured; output is structural permission metadata only |
| Catalog ↔ Phase 63 `04-custom-role-authoring.md` | Phase 63 cites this catalog as SOT; integrity of the OP-3 dependency table directly affects whether Phase 63 sub-org admin bundles produce blank-UI symptoms in tenant deployments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| (none-blocking) | I (Information disclosure) | per-permission catalog | accept | Catalog is public Apple Business documentation; no PII or tenant data; D-A8 read-only-corpus contract applies |

T-62-A — N/A (no `_admin-directory.md` work).
T-62-B — N/A (no harness code).
T-62-C — Apple URL article IDs (`axm97dd59159`, etc.) cited in catalog `apple_url_anchor` column; rot tracked in `c13_rotting_external` sidecar (Plan 62-08).
</threat_model>

<verification>
Per VALIDATION.md V-62-03 (Account Holder callout) + V-62-04 (keyword scan) + V-62-04b (Edit-without-View structure):

| Task ID | V-62-XX | Command |
|---------|---------|---------|
| 62-04-01 | V-62-03 | `grep -c "Account Holder — DO NOT DELEGATE" docs/cross-platform/apple-business/01-role-permission-model.md` -> 1+ |
| 62-04-02 | V-62-04 | `grep -cE "(Manage MDM Servers\|Edit-without-View\|Requires)" docs/cross-platform/apple-business/01-role-permission-model.md` -> 3+ |
| 62-04-03 | V-62-04b | `grep -cE "^## Edit-without-View Dependency Table" docs/cross-platform/apple-business/01-role-permission-model.md` -> 1 |
</verification>

<success_criteria>
1. `docs/cross-platform/apple-business/01-role-permission-model.md` exists with valid frontmatter (`applies_to: apple-business`, `platform: ios+macos`, `audience: admin`).
2. Account Holder DO-NOT-DELEGATE callout present (literal grep string) referencing OP-2 / DA-2.
3. 4 top-level roles + 5 preset custom roles documented with numbered-list pattern; Brand Manager EXCLUDED.
4. Per-permission catalog present with 7 H3 subgroup sections + 35-50 scraped permission rows.
5. "Manage MDM Servers" flagged DENY-by-default with OP-1 superprivilege explainer.
6. "Reset Shared iPad passcode" flagged conditionally-grant per OP-11.
7. Edit-without-View dependency table contains all 7 seed pairs from RESEARCH.md §1.
8. Brand-Related subgroup excluded with Apple-docs pointer; any Brand URLs encountered during scrape logged for `c13_rotting_external`.
</success_criteria>

<output>
After completion, create `.planning/phases/62-apple-business-foundation-rebrand/62-04-SUMMARY.md` recording:
- Actual scraped row count per subgroup (7 numbers)
- Total catalog row count
- Brand-subgroup URLs encountered (list for Plan 62-08 c13_rotting_external entries)
- Any Edit-without-View pairs added during scrape beyond the 7-row seed
- Apple article IDs cited (deduplicated list)
</output>
