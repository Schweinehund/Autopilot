---
phase: 62
plan: 62-03
title: Author `00-overview.md` (rebrand callout site #1 with C14 token triad + style-guide HTML-comment convention section)
type: execute
wave: 2
status: pending
depends_on: [62-01]
files_modified:
  - docs/cross-platform/apple-business/00-overview.md
requirements:
  - AB-05
threats: [T-62-C]
autonomous: true

must_haves:
  truths:
    - "`docs/cross-platform/apple-business/00-overview.md` is the FIRST of the 3 canonical C14 rebrand-callout sites and contains all 3 required tokens ('Apple Business Manager', 'Apple Business', '2026-04-14') within the first 50 lines"
    - "Apple Business cross-platform tree (`docs/cross-platform/apple-business/`) is established with a top-level overview that cross-links to the glossary + role/permission model + OUs architecture + admin directory"
    - "The style-guide HTML-comment-block convention (`<!-- ABAUDIT-{##}: {intent} -->` form) is published in a dedicated section so Phase 64 cheat sheet authoring has the C15 exemption pattern to consume"
  artifacts:
    - path: "docs/cross-platform/apple-business/00-overview.md"
      provides: "Apple Business cross-platform tree top-level index + C14 callout site #1 + style-guide convention"
      contains: "Rebrand notice (2026-04-14)"
      min_lines: 80
  key_links:
    - from: "docs/cross-platform/apple-business/00-overview.md (first 50 lines)"
      to: "all 3 C14 token strings"
      via: "rebrand notice blockquote in intro region"
      pattern: "Apple Business Manager.*Apple Business.*2026-04-14"
    - from: "docs/cross-platform/apple-business/00-overview.md Governance Foundation H2 table"
      to: "docs/_glossary-apple-business.md, ../../cross-platform/apple-business/01-role-permission-model.md, ../../cross-platform/apple-business/02-ous-architecture.md, ../../cross-platform/apple-business/_admin-directory.md"
      via: "in-table markdown links"
      pattern: "_glossary-apple-business\\.md"
---

<objective>
Author the Apple Business cross-platform tree top-level index at `docs/cross-platform/apple-business/00-overview.md` — the FIRST of 3 canonical C14 rebrand-callout sites. Per AB-05, this file MUST contain all 3 C14 tokens ("Apple Business Manager", "Apple Business", "2026-04-14") within the first 50 lines. Also publishes the style-guide HTML-comment-block convention section that Phase 64 `18-cross-org-boundary-cheat-sheet.md` will consume for C15 allowlist exemptions.

Purpose: Establishes the cross-platform/apple-business directory's identity + provides the canonical entry point for downstream phases + ships the C15 exemption-comment convention definition before any actual exemption blocks land (Phase 64 work).

Output: 1 new file at `docs/cross-platform/apple-business/00-overview.md` (~80-150 lines).
</objective>

<execution_context>
@D:/claude/Autopilot/CLAUDE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/62-apple-business-foundation-rebrand/62-CONTEXT.md
@.planning/phases/62-apple-business-foundation-rebrand/62-RESEARCH.md
@.planning/phases/62-apple-business-foundation-rebrand/62-PATTERNS.md
@docs/operations/00-index.md
@docs/operations/co-management/00-overview.md

<interfaces>
<!-- Analog: docs/operations/00-index.md (PATTERNS.md exact match per D-A1) -->
<!-- C14 token-set membership check: all 3 tokens in first 50 lines per RESEARCH.md §2 -->
<!-- v1.4.1 AEAUDIT-04 precedent: <!-- ABAUDIT-{##}: ... --> form per CONTEXT.md <specifics> line 212 -->

Frontmatter (per PATTERNS.md cross-cutting + RESEARCH.md §8):
```yaml
---
last_verified: 2026-05-21
review_by: 2026-07-20
applies_to: apple-business
audience: admin
platform: ios+macos
---
```

C14 rebrand callout (MUST land in first 50 lines; contains all 3 tokens):
```markdown
> **Rebrand notice (2026-04-14):** Apple Business Manager (ABM) became **Apple Business** on
> 2026-04-14. This documentation uses the new terminology throughout. Legacy term "Apple Business
> Manager" is searchable via the [Apple Business Governance Glossary](../../_glossary-apple-business.md)
> rebrand-mapping table. See also: Apple's [transition announcement](https://support.apple.com/guide/apple-business-manager/apple-business-manager-is-now-apple-business-axmd79d79dea/web).
```

H2 Governance Foundation table (links to the 4 Phase 62 sibling docs — relative paths from `docs/cross-platform/apple-business/`):
```markdown
## Governance Foundation

This section establishes the Apple Business terminology canon, role/permission model, OU
architecture, and L1 admin-directory lookup convention. Downstream phases (delegation runbooks,
L1/L2, hub nav) cite these foundation docs as Source of Truth.

| Guide | Covers |
|-------|--------|
| [Apple Business Governance Glossary](../../_glossary-apple-business.md) | Terminology canon (8-pair rebrand mapping; 4 H2 categories) |
| [Role/Permission Model](01-role-permission-model.md) | 4 top-level roles + 5 preset custom roles + per-permission catalog + OP-2/OP-3 callouts |
| [Organizational Units Architecture](02-ous-architecture.md) | OU primitive + scoping table + Locations→OUs migration framing |
| [L1 Admin Directory (Lookup Convention)](_admin-directory.md) | Tenant-fillable lookup template for "which admin owns this device pool" |
```

Style-guide section pattern (Phase 62 publishes CONVENTION only; no actual exemption blocks):
```markdown
## Style Guide: HTML-Comment Allowlist Exemptions

For v1.6 Apple Business documentation that legitimately discusses both Apple Business surfaces
AND Intune surfaces (e.g., the future `18-cross-org-boundary-cheat-sheet.md` disambiguation
table in Phase 64), the audit harness's C15 banned-phrase guard provides an allowlist exemption
via HTML comments in this form:

\`\`\`html
<!-- ABAUDIT-01: intentional Apple-Business-vs-Intune disambiguation table; C15 false-positive allowlisted -->
\`\`\`

Rules:
- Numbering: `ABAUDIT-01`, `ABAUDIT-02`, ... sequentially across the v1.6 corpus.
- Intent line: one-sentence rationale (why C15 would otherwise flag this content).
- Placement: HTML comment immediately precedes the line(s) the exemption covers.
- Removal contract: exemptions do NOT have a sunset_phase field (unlike `c16_missing_endpoint_exemptions`);
  C15 exemptions persist for the lifetime of the published doc.

This convention is published in Phase 62. Actual exemption blocks ship in Phase 64
`18-cross-org-boundary-cheat-sheet.md`; no Phase 62 doc requires C15 exemptions.

Precedent: v1.4.1 AEAUDIT-04 HTML-comment exemption pattern.
```

Version History (per `operations/00-index.md` lines 62-66 pattern):
```markdown
## Version History

| Date | Change | Author |
|------|--------|--------|
| 2026-05-21 | Phase 62: tree founded; rebrand callout #1; style-guide HTML-comment convention published | -- |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 62-03-01: Create `docs/cross-platform/apple-business/00-overview.md` with frontmatter + C14 rebrand callout (all 3 tokens in first 50 lines) + H1 intro + Governance Foundation H2 table</name>
  <files>docs/cross-platform/apple-business/00-overview.md</files>
  <action>
Verify the directory `docs/cross-platform/apple-business/` exists (create it if needed — this is the first file in the new tree per D-A1 + RESEARCH.md Pitfall 1).

Create file with this structure (note: ALL 3 C14 tokens must appear within the first 50 lines per RESEARCH.md §2; the frontmatter + reciprocity blockquote together typically consume lines 1-10, then the C14 callout + H1 + intro occupy lines 11-30, leaving plenty of buffer):

```markdown
---
last_verified: 2026-05-21
review_by: 2026-07-20
applies_to: apple-business
audience: admin
platform: ios+macos
---

> **Platform applicability:** This tree covers Apple Business delegated governance on iOS,
> iPadOS, and macOS Apple-managed devices. Intune-side surfaces (Intune RBAC, scope tags,
> Intune role assignments) are out of scope per Q5(b); see the
> [Apple Business Governance Glossary](../../_glossary-apple-business.md) for terminology canon.

> **Rebrand notice (2026-04-14):** Apple Business Manager (ABM) became **Apple Business** on
> 2026-04-14. This documentation uses the new terminology throughout. Legacy term "Apple Business
> Manager" is searchable via the [Apple Business Governance Glossary](../../_glossary-apple-business.md)
> rebrand-mapping table. See also: Apple's [transition announcement](https://support.apple.com/guide/apple-business-manager/apple-business-manager-is-now-apple-business-axmd79d79dea/web).

# Apple Business Governance Overview

This index covers the foundational Apple Business delegated governance documentation for v1.6.
Guides establish the terminology canon, role and permission model, Organizational Unit (OU)
architecture, and the L1 admin-directory lookup convention. Delegation runbooks and L1/L2
routing build on this foundation in Phases 64-65.

Coverage boundary: Apple Business is the Apple-owned permission surface for Apple-managed
devices (iOS / iPadOS / macOS). It is distinct from Intune-side RBAC (which controls
Intune's MDM surface). The two surfaces interact at device-policy enforcement boundaries but
have separate role/permission models.

## Governance Foundation

This section establishes the Apple Business terminology canon, role/permission model, OU
architecture, and L1 admin-directory lookup convention. Downstream phases (delegation runbooks,
L1/L2, hub nav) cite these foundation docs as Source of Truth.

| Guide | Covers |
|-------|--------|
| [Apple Business Governance Glossary](../../_glossary-apple-business.md) | Terminology canon (8-pair rebrand mapping; 4 H2 categories) |
| [Role/Permission Model](01-role-permission-model.md) | 4 top-level roles + 5 preset custom roles + per-permission catalog + OP-2/OP-3 callouts |
| [Organizational Units Architecture](02-ous-architecture.md) | OU primitive + scoping table + Locations→OUs migration framing |
| [L1 Admin Directory (Lookup Convention)](_admin-directory.md) | Tenant-fillable lookup template for "which admin owns this device pool" |
```

**Self-check after authoring**: Use `head -50 docs/cross-platform/apple-business/00-overview.md` and confirm all 3 tokens ("Apple Business Manager", "Apple Business", "2026-04-14") appear. If any are missing inside the 50-line window, restructure the intro region to compress them in.

**Cross-link contract**: All Phase 62 sibling-doc links use relative paths from `docs/cross-platform/apple-business/` — `../../_glossary-apple-business.md` (two levels up to `docs/`), and `01-role-permission-model.md` / `02-ous-architecture.md` / `_admin-directory.md` (same directory). At the time Plan 62-03 runs, these sibling files do NOT YET exist (Plans 62-04/05/06 create them). Link target validation defers to Plan 62-08 (`check-phase-62.mjs`); for now, the link strings are correct relative paths.
  </action>
  <verify>
    <automated>head -50 docs/cross-platform/apple-business/00-overview.md | grep -c "Apple Business Manager" && head -50 docs/cross-platform/apple-business/00-overview.md | grep -c "Apple Business" && head -50 docs/cross-platform/apple-business/00-overview.md | grep -c "2026-04-14"</automated>
    <expected>each token appears ≥1 time within first 50 lines (C14 PASS)</expected>
  </verify>
  <done>File exists; C14 token triad present in first 50 lines (verified by `head -50` + grep); H1 + intro + Governance Foundation H2 table present; 4 sibling-doc cross-links use correct relative paths.</done>
</task>

<task type="auto">
  <name>Task 62-03-02: Append Style-Guide HTML-comment-block convention section + Version History + commit</name>
  <files>docs/cross-platform/apple-business/00-overview.md</files>
  <action>
Append to the file (after the Governance Foundation table):

```markdown
## Style Guide: HTML-Comment Allowlist Exemptions

For v1.6 Apple Business documentation that legitimately discusses both Apple Business surfaces
AND Intune surfaces (e.g., the future `18-cross-org-boundary-cheat-sheet.md` disambiguation
table in Phase 64), the audit harness's C15 banned-phrase guard provides an allowlist exemption
via HTML comments in this form:

```html
<!-- ABAUDIT-01: intentional Apple-Business-vs-Intune disambiguation table; C15 false-positive allowlisted -->
```

Rules:

- **Numbering:** `ABAUDIT-01`, `ABAUDIT-02`, ... sequentially across the v1.6 corpus.
- **Intent line:** one-sentence rationale describing why C15 would otherwise flag this content.
- **Placement:** the HTML comment immediately precedes the line(s) the exemption covers.
- **Removal contract:** C15 exemptions do NOT have a `sunset_phase` field (unlike
  `c16_missing_endpoint_exemptions` in the sidecar JSON); C15 exemptions persist for the lifetime
  of the published doc.

This convention is **published in Phase 62**. Actual exemption blocks ship in Phase 64
`18-cross-org-boundary-cheat-sheet.md`; no Phase 62 doc requires C15 exemptions at landing time.

Precedent: v1.4.1 AEAUDIT-04 HTML-comment exemption pattern.

## Version History

| Date | Change | Author |
|------|--------|--------|
| 2026-05-21 | Phase 62: tree founded; rebrand callout #1; style-guide HTML-comment convention published | -- |
```

**Critical anti-self-flag invariant:** The HTML-comment EXAMPLE inside the fenced code block above must NOT itself match the C15 banned-phrase regex. The example string `<!-- ABAUDIT-01: intentional Apple-Business-vs-Intune disambiguation table; C15 false-positive allowlisted -->` contains "Apple-Business-vs-Intune disambiguation" which is informational metadata, not a banned phrase. The C15 regex from RESEARCH.md §3 targets compounds like `\bIntune\s+(RBAC|role|scope\s+tag|admin\s+role)\b` — none of which appear in this example or anywhere else in `00-overview.md`. Self-check after authoring: run the conceptual C15 regex set against the file and confirm no matches.

Self-test (mental): grep for patterns from RESEARCH.md §3:
- `Intune RBAC` — NOT present in this doc
- `Intune role` — NOT present
- `Intune scope tag` — NOT present
- `delegated admin.*Intune` — NOT present (we say "Apple Business" delegation, never "Intune delegation")

If any of these patterns slipped in during authoring, rewrite the offending sentence to use Apple Business framing only (e.g., "Apple Business permission" not "Apple Business role" — though "role" alone is fine; the banned compound is "Intune role").

Commit message:
```
docs(62): plan 62-03 — Apple Business overview + rebrand callout #1 + style-guide convention (AB-05)
```
  </action>
  <verify>
    <automated>grep -c "ABAUDIT-01" docs/cross-platform/apple-business/00-overview.md && grep -cE "Intune\s+(RBAC|role|scope\s+tag|admin\s+role)" docs/cross-platform/apple-business/00-overview.md</automated>
    <expected>ABAUDIT-01 mention appears (style-guide example); Intune-banned-phrase regex returns 0 matches (no self-flag)</expected>
  </verify>
  <done>Style-guide section appended with `<!-- ABAUDIT-{##}: ... -->` example, 4 rules, AEAUDIT-04 precedent reference; Version History present; doc passes mental C15 regex self-check; file committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apple URL ↔ this doc | Apple transition announcement URL (`axmd79d79dea`) cited in rebrand callout; T-62-C residual risk (Apple URL rot) tracked in `c13_rotting_external` (Plan 62-08) |
| Style-guide example ↔ C15 regex | The HTML-comment example string MUST NOT itself match any C15 banned-phrase regex (self-flag avoidance per RESEARCH.md §3 design principle) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-62-C | I (Information disclosure / external rot) | Apple URL `axmd79d79dea` in rebrand callout | mitigate | URL cited via Apple article ID (Apple's stable identifier); legacy `support.apple.com/guide/apple-business-manager/` path still resolves per CONTEXT.md; full quarterly rot audit via `c13_rotting_external` sidecar landing in Plan 62-08 |

T-62-A — N/A (no `_admin-directory.md` work).
T-62-B — N/A (no harness code).
</threat_model>

<verification>
Per VALIDATION.md V-62-03 / V-62-STYLE + C14 (site #1):

| Task ID | V-62-XX | Command |
|---------|---------|---------|
| 62-03-01 | V-62-03 (C14 site #1) | `head -50 docs/cross-platform/apple-business/00-overview.md \| grep -E "Apple Business Manager\|Apple Business\|2026-04-14" -c` → at least 3 distinct token matches |
| 62-03-02 | V-62-STYLE | `grep -c "ABAUDIT-" docs/cross-platform/apple-business/00-overview.md` → ≥1 |
| 62-03-02 | (C15 self-flag) | `grep -cE "Intune\s+(RBAC\|role\|scope\s+tag\|admin\s+role)" docs/cross-platform/apple-business/00-overview.md` → 0 |
</verification>

<success_criteria>
1. `docs/cross-platform/apple-business/00-overview.md` exists with valid frontmatter (`applies_to: apple-business`, `platform: ios+macos`).
2. All 3 C14 tokens present in first 50 lines (`head -50` + grep verified).
3. Governance Foundation H2 table links to all 4 Phase 62 sibling docs with correct relative paths.
4. Style-guide HTML-comment convention section published with `<!-- ABAUDIT-{##}: {intent} -->` form + 4 rules + AEAUDIT-04 precedent reference.
5. Zero self-flag matches against C15 banned-phrase regex set (verified by mental + grep self-check).
6. Version History row present.
</success_criteria>

<output>
After completion, create `.planning/phases/62-apple-business-foundation-rebrand/62-03-SUMMARY.md` recording:
- Exact line numbers where each of the 3 C14 tokens appears (for C14 50-line window debug if Plan 62-08 fails)
- Style-guide section line range
- Confirmation that no C15 banned phrases appear in the doc
</output>
