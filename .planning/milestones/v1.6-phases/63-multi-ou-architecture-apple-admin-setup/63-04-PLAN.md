---
phase: 63-multi-ou-architecture-apple-admin-setup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md
  - docs/cross-platform/apple-business/10-apple-tv-lifecycle.md
autonomous: true
requirements: [OU-07, OU-08]
must_haves:
  truths:
    - "Admin can manage Shared iPad lifecycle (sessions, user provisioning, Find My disable) via 09-shared-ipad-lifecycle.md (OU-07)"
    - "The OP-12 Find My disable step is fully documented as a mandatory pre-deployment step — NOT hedged (OU-07 / D-04)"
    - "Admin can manage Apple TV lifecycle (Configurator-only retail path, OU assignment, content-token app deployment, CRD shared-physical-space heuristic) via 10-apple-tv-lifecycle.md (OU-08)"
  artifacts:
    - path: "docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md"
      provides: "Shared iPad lifecycle stages with mandatory OP-12 Find My disable callout"
      contains: "OP-12"
    - path: "docs/cross-platform/apple-business/10-apple-tv-lifecycle.md"
      provides: "Apple TV lifecycle: Configurator retail path + OU assignment + content-token deployment + OP-15 CRD heuristic"
      contains: "Conference Room Display"
  key_links:
    - from: "docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md"
      to: "docs/cross-platform/apple-business/02-ous-architecture.md"
      via: "OU scoping context cross-link"
      pattern: "02-ous-architecture.md"
---

<objective>
Author the two device-lifecycle docs (OU-07, OU-08) at core depth + hedge per D-04: Shared iPad lifecycle (`09-`) and Apple TV lifecycle (`10-`). These are Wave-B anchor-bearing docs — their existence (alongside `05-...#which-admin-owns-this-pool`) unlocks parallel Phase 64 runbook authoring.

Purpose: Admins manage the full documented Shared iPad and Apple TV surfaces; the OP-12 Find My disable step is mandatory (not hedged); the LOW-severity per-OU CRD deep-dive is deferred to v1.7+ with a tracking note.
Output: `09-shared-ipad-lifecycle.md`, `10-apple-tv-lifecycle.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-CONTEXT.md
@.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-PATTERNS.md

<interfaces>
<!-- Patterns and platform-atom rules. -->
Frontmatter platform atoms (validated by check-phase-62.mjs:346 ALLOWED set; '+' separator):
  09-shared-ipad-lifecycle.md -> `platform: ios+shared-ipad`
  10-apple-tv-lifecycle.md    -> `platform: apple-tv`
OP-12 Find My callout: model on OP-2 DO-NOT-DELEGATE hard-bordered callout style (01-role-permission-model.md:39-58); HIGH severity, NOT hedged, document as a blocking pre-deployment gate.
OP-15 CRD heuristic: LOW severity, use `> **Note:**` style (02-ous-architecture.md:63), NOT a hard-bordered callout. Ship the shared-physical-space HEURISTIC only; defer the per-OU CRD partitioning DEEP-DIVE to v1.7+ with a tracking note.
Depth calibration / "Apple does not publish" hedge pattern: 02-ous-architecture.md:46-52, :63.
NOTE: the C16 `## Which admin owns this pool?` anchor lives in 05-sub-org-admin-onboarding.md (Plan 63-02), NOT here — do not author it in 09-.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 09-shared-ipad-lifecycle.md (OU-07, mandatory OP-12 Find My disable)</name>
  <files>docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md</files>
  <read_first>
    - docs/cross-platform/apple-business/02-ous-architecture.md (analog — lifecycle-stages prose structure; "Apple does not publish" hedge pattern :46-52, :63; forward-ref :112)
    - docs/cross-platform/apple-business/01-role-permission-model.md (OP-2 callout style 39-58 to model the OP-12 callout)
    - docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md (target — does not exist yet)
    - 63-PATTERNS.md §09-shared-ipad-lifecycle.md
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md` per OU-07 / D-04. Frontmatter with `platform: ios+shared-ipad`. Platform-applicability callout + glossary cross-link. OU scoping via cross-link to 02-ous-architecture.md (depth <= 2 per D-06).
    Document the Shared iPad lifecycle stages at full depth: (1) Enrollment (supervised, ADE), (2) Shared iPad session configuration, (3) user provisioning with Managed Apple Account, (4) sign-in / sign-out, (5) wipe / re-provision.
    Author the OP-12 Find My disable as a HARD-BORDERED `> **Critical (OP-12):**` callout (style from 01-role-permission-model.md:39-58) documented as a MANDATORY pre-deployment step / blocking gate — explicitly NOT hedged. Use `> **Note:** Apple does not publish ...` only for genuinely thin areas. Add Cross-References + Version History. C15 Apple-side framing only.
    Do NOT place the `## Which admin owns this pool?` heading here (it belongs in 05-).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md','utf8'); const ok=c.includes('OP-12')&&/Find My/i.test(c)&&/mandatory|pre-deployment|blocking/i.test(c)&&/Intune|Entra|Conditional Access|Azure AD/i.test(c)===false; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with frontmatter `platform: ios+shared-ipad`.
    - Contains a hard-bordered `OP-12` Find My disable callout described as mandatory/pre-deployment/blocking (NOT hedged).
    - Documents the 5 lifecycle stages (enrollment, session config, user provisioning, sign-in/out, wipe/re-provision).
    - Does NOT contain the heading `## Which admin owns this pool?` (that anchor is owned by 05-).
    - Contains none of (case-insensitive): `Intune`, `Entra`, `Conditional Access`, `Azure AD` (C15).
  </acceptance_criteria>
  <done>09-shared-ipad-lifecycle.md authored at full documented depth with a mandatory (non-hedged) OP-12 Find My disable callout.</done>
</task>

<task type="auto">
  <name>Task 2: Author 10-apple-tv-lifecycle.md (OU-08, CRD heuristic + deep-dive deferral)</name>
  <files>docs/cross-platform/apple-business/10-apple-tv-lifecycle.md</files>
  <read_first>
    - docs/cross-platform/apple-business/02-ous-architecture.md (analog — depth-calibration / hedge pattern :46-52, :63)
    - docs/cross-platform/apple-business/10-apple-tv-lifecycle.md (target — does not exist yet)
    - 63-PATTERNS.md §10-apple-tv-lifecycle.md
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/10-apple-tv-lifecycle.md` per OU-08 / D-04. Frontmatter with `platform: apple-tv`. Platform-applicability callout + glossary cross-link.
    Document at full depth the documented surface: the Configurator-only retail-purchase path (Apple TVs bought at retail enroll via Apple Configurator, not ADE), OU assignment, and content-token-based app deployment. Document the OP-15 Conference Room Display shared-physical-space HEURISTIC as a `> **Note:**` (LOW severity, not hard-bordered).
    DEFER the per-OU CRD partitioning DEEP-DIVE to v1.7+ with an explicit tracking note (and note it will be logged to .planning/milestones/v1.6-DEFERRED-CLEANUP.md at Phase 66). Use `> **Note:** Apple does not publish ...` hedges for thin tvOS areas. Add Cross-References + Version History. C15 Apple-side framing only.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docs/cross-platform/apple-business/10-apple-tv-lifecycle.md','utf8'); const ok=/Configurator/i.test(c)&&c.includes('Conference Room Display')&&/content token/i.test(c)&&/defer|v1.7/i.test(c)&&/Intune|Entra|Conditional Access|Azure AD/i.test(c)===false; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with frontmatter `platform: apple-tv`.
    - Documents the Configurator-only retail path, OU assignment, and content-token app deployment.
    - Contains a `Conference Room Display` shared-physical-space heuristic note (OP-15, LOW severity, Note-style not hard-bordered).
    - Contains an explicit deferral note for the per-OU CRD deep-dive (v1.7+).
    - Contains none of (case-insensitive): `Intune`, `Entra`, `Conditional Access`, `Azure AD` (C15).
  </acceptance_criteria>
  <done>10-apple-tv-lifecycle.md authored with the documented Apple TV surface, the OP-15 CRD heuristic, and the per-OU deep-dive deferred to v1.7+.</done>
</task>

</tasks>

<threat_model>
Documentation deliverable — no runtime attack surface. Static markdown. Integrity surfaces: (1) OP-12 Find My step must remain a mandatory non-hedged gate (under-documenting it is a content-safety regression) — asserted by V-63 checks and task verify; (2) C15 framing — guarded by the live harness. No external input, no executable code introduced.
</threat_model>

<verification>
- `node scripts/validation/v1.6-milestone-audit.mjs` exits 0 (C15 + `+`-separator frontmatter parse pass for ios+shared-ipad / apple-tv).
- Both Task verify commands exit 0.
</verification>

<success_criteria>
- OU-07, OU-08 satisfied; Wave-B anchor docs `09-` and `10-` exist so Phase 64 runbook authoring can begin in parallel (alongside the 05- C16 anchor from Plan 63-02).
</success_criteria>

<output>
Create `.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-04-SUMMARY.md` when done.
</output>
