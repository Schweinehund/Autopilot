---
phase: 63-multi-ou-architecture-apple-admin-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cross-platform/apple-business/03-ous-vs-custom-roles.md
  - docs/cross-platform/apple-business/04-custom-role-authoring.md
autonomous: true
requirements: [OU-01, OU-02]
must_haves:
  truths:
    - "Admin can pick OUs-only / custom-roles-only / combined topology from a criteria comparison table (OU-01)"
    - "The 'most-permissive wins across overlapping assignments' callout sits adjacent to the OU-01 matrix (OP-4)"
    - "Admin can author ONE canonical Sub-Org Admin bundle of 4-6 permissions (OU-02)"
    - "The OU-02 bundle excludes Manage MDM Servers (OP-1) and Account Holder (OP-2), and pairs every Edit with its companion View (OP-3)"
  artifacts:
    - path: "docs/cross-platform/apple-business/03-ous-vs-custom-roles.md"
      provides: "OUs-vs-Custom-Roles decision matrix (3 topology columns) + adjacent OP-4 callout"
      contains: "most-permissive wins"
    - path: "docs/cross-platform/apple-business/04-custom-role-authoring.md"
      provides: "Single canonical Sub-Org Admin 4-6 permission bundle table citing 01-role-permission-model.md as SOT"
      contains: "DENY-by-default"
  key_links:
    - from: "docs/cross-platform/apple-business/04-custom-role-authoring.md"
      to: "docs/cross-platform/apple-business/01-role-permission-model.md"
      via: "SOT citation cross-link"
      pattern: "01-role-permission-model.md"
    - from: "docs/cross-platform/apple-business/03-ous-vs-custom-roles.md"
      to: "docs/cross-platform/apple-business/02-ous-architecture.md"
      via: "most-permissive-wins precedent cross-link"
      pattern: "02-ous-architecture.md"
---

<objective>
Author the two delegation-topology decision docs for Phase 63: the OUs-vs-Custom-Roles decision matrix (OU-01) and the canonical Sub-Org Admin custom-role bundle (OU-02).

Purpose: Admins must choose a delegation topology (OU-01) and then author the v1.6-differentiator min-viable sub-org admin permission bundle (OU-02) — both built on Phase 62's role/permission model SOT.
Output: `03-ous-vs-custom-roles.md` and `04-custom-role-authoring.md` under `docs/cross-platform/apple-business/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-CONTEXT.md
@.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-PATTERNS.md

<interfaces>
<!-- Exact permission names from the SOT 01-role-permission-model.md (the executor uses these verbatim — no codebase exploration needed). -->

Devices subgroup (01-role-permission-model.md:270-279):
- `View Devices` (View-only, always-grant, companion: —)
- `Assign devices to MDM server` (Edit, conditionally-grant, companion View: View Devices)
- `Manage MDM Servers` (Edit+bundle, **DENY-by-default (OP-1)** — MUST be EXCLUDED from the bundle)

Apps & Books subgroup (01-role-permission-model.md:312-319):
- `View content tokens` (View-only, always-grant, companion: —)
- `Assign content (device-licensed)` (Edit, conditionally-grant, companion View: View content tokens)
- `Manage content token location` (tenant-wide, DENY-by-default — EXCLUDE)

People subgroup (01-role-permission-model.md:240-243):
- `View users` (companion View target for the People Edit permissions)
- `Create Managed Apple Accounts` (Edit, conditionally-grant, companion View: View users)

Permission table six-column schema (01-role-permission-model.md:139-148 / 270):
| Permission | Scope | Edit / View | OP-1 flag | OP-3 companion View | Apple anchor |

Account Holder: singleton tenant role, structurally non-delegable (OP-2) — NEVER appears in the bundle.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 03-ous-vs-custom-roles.md (OU-01 decision matrix, D-03)</name>
  <files>docs/cross-platform/apple-business/03-ous-vs-custom-roles.md</files>
  <read_first>
    - docs/cross-platform/apple-business/02-ous-architecture.md (analog + lines 54-59 "most-permissive wins" prose+callout precedent; lines 9-15 platform-applicability callout; lines 114-122 table schema; lines 123-141 cross-refs)
    - docs/cross-platform/apple-business/03-ous-vs-custom-roles.md (target — does not exist yet)
    - 63-PATTERNS.md §03-ous-vs-custom-roles.md (frontmatter + table reshape + version-history patterns)
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/03-ous-vs-custom-roles.md` per D-01/D-03 and OU-01. Use the standard Phase 62 frontmatter block (last_verified: 2026-05-21, review_by: 2026-07-20, applies_to: apple-business, audience: admin, platform: ios+macos). Add the platform-applicability callout adapted from 02-ous-architecture.md:9-15 with a glossary cross-link to ../../_glossary-apple-business.md.
    Present the topology choice as a CRITERIA COMPARISON TABLE (D-03): rows = decision criteria (Claude's discretion on exact rows, e.g. "Partitions which devices/content an admin sees", "Partitions which actions an admin can perform", "Setup overhead", "Cross-OU access", "Best fit"), columns = exactly 3 topologies: `OUs-only` | `Custom-roles-only` | `Combined`. Do NOT use a Mermaid tree and do NOT add a combined tree+table (Phase 62 D-04 anti-redundancy precedent).
    Place a blockquote callout containing the exact phrase "most-permissive wins across overlapping assignments" IMMEDIATELY ADJACENT to (directly after) the matrix table, mirroring the 02-ous-architecture.md:54-59 prose+callout model; reference OP-4 and cross-link back to 02-ous-architecture.md.
    Add a Cross-References section and a Version History table per the shared patterns. Keep all framing Apple-side only (C15 — no Intune/Entra/Conditional Access/Azure AD).
    No fenced code blocks of full file contents in this plan; author directly into the file.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docs/cross-platform/apple-business/03-ous-vs-custom-roles.md','utf8'); const ok=c.includes('most-permissive wins across overlapping assignments') && c.includes('OUs-only') && c.includes('Custom-roles-only') && c.includes('Combined') && /Intune|Entra|Conditional Access|Azure AD/i.test(c)===false; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/cross-platform/apple-business/03-ous-vs-custom-roles.md` exists with the standard 5-field frontmatter block.
    - Contains a markdown table whose header columns include the exact strings `OUs-only`, `Custom-roles-only`, and `Combined`.
    - Contains the verbatim string `most-permissive wins across overlapping assignments` in a blockquote directly after the matrix table.
    - Cross-links to `02-ous-architecture.md` and `../../_glossary-apple-business.md`.
    - Contains NO Mermaid code fence (no ```mermaid block).
    - Contains none of (case-insensitive): `Intune`, `Entra`, `Conditional Access`, `Azure AD` (C15).
  </acceptance_criteria>
  <done>03-ous-vs-custom-roles.md authored with a 3-topology criteria comparison table, an adjacent most-permissive-wins (OP-4) callout, cross-links, and clean C15 framing.</done>
</task>

<task type="auto">
  <name>Task 2: Author 04-custom-role-authoring.md (OU-02 canonical Sub-Org Admin bundle, D-02)</name>
  <files>docs/cross-platform/apple-business/04-custom-role-authoring.md</files>
  <read_first>
    - docs/cross-platform/apple-business/01-role-permission-model.md (SOT — lines 15-21 training-data notice; 39-58 OP-2 callout; 139-148 + 270-279 permission six-column schema; 150-167 + 281-288 OP-1 superprivilege callout; 312-319 Apps&Books; 240-243 People; 343-370 Edit-without-View dependency table)
    - docs/cross-platform/apple-business/04-custom-role-authoring.md (target — does not exist yet)
    - 63-PATTERNS.md §04-custom-role-authoring.md
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/04-custom-role-authoring.md` per D-02 and OU-02. Standard frontmatter (platform: ios+macos). Open by stating this document cites `01-role-permission-model.md` as the source of truth for the 7-subgroup permission catalog (mirror the SOT-citation pattern). Add the training-data verification notice (adapted) noting the bundle composition pends portal verification.
    Author ONE canonical "Sub-Org Admin" bundle (singular — NOT multiple archetypes, NOT tiered add-ons) of 4-6 permissions using the six-column table schema (`Permission | Scope | Edit / View | OP-1 flag | OP-3 companion View | Apple anchor`). Compose from the SOT verbatim names: include `View Devices` + `Assign devices to MDM server`; `View content tokens` + `Assign content (device-licensed)`; and (for a 6-permission bundle) `View users` + `Create Managed Apple Accounts`. This yields 3 Manage(Edit) + 3 View = 6 permissions, within the 4-6 range. Per D-02/OP-3, every Edit permission row names its companion View in the same row.
    Mark exclusions explicitly: a row or callout showing `Manage MDM Servers` as `❌ EXCLUDED (OP-1)` with DENY-by-default rationale, and `Account Holder` as `❌ EXCLUDED (OP-2)` (singleton, non-delegable). Copy the OP-1 superprivilege callout (01-role-permission-model.md:281-288 style) verbatim after the bundle table, and reference the OP-2 DO-NOT-DELEGATE callout. Cross-link (do NOT duplicate) the Edit-without-View dependency table at `01-role-permission-model.md#edit-without-view-dependency-table-op-3-prevention` per Phase 62 D-04 anti-redundancy.
    Add Cross-References + Version History. C15 Apple-side framing only.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docs/cross-platform/apple-business/04-custom-role-authoring.md','utf8'); const ok=c.includes('DENY-by-default') && (c.includes('Manage MDM Servers')) && c.includes('EXCLUDED') && c.includes('Account Holder') && c.includes('01-role-permission-model.md') && /Intune|Entra|Conditional Access|Azure AD/i.test(c)===false; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/cross-platform/apple-business/04-custom-role-authoring.md` exists with standard frontmatter.
    - Contains exactly ONE Sub-Org Admin bundle table (singular) whose INCLUDED permission rows number between 4 and 6 inclusive.
    - Every included Edit/Manage permission row names a companion View permission in the same row (OP-3 — no orphan Manage).
    - Contains `Manage MDM Servers` flagged `EXCLUDED` / `DENY-by-default` (OP-1) — NOT listed as an included bundle permission.
    - Contains `Account Holder` flagged `EXCLUDED` (OP-2).
    - Contains the verbatim string `01-role-permission-model.md` (SOT citation) and a link to the `#edit-without-view-dependency-table-op-3-prevention` anchor.
    - Contains none of (case-insensitive): `Intune`, `Entra`, `Conditional Access`, `Azure AD` (C15).
  </acceptance_criteria>
  <done>04-custom-role-authoring.md authored with one canonical 4-6 permission Sub-Org Admin bundle, OP-1/OP-2 exclusions, OP-3 companion-View pairing, SOT citation, and clean C15 framing.</done>
</task>

</tasks>

<threat_model>
Documentation deliverable — no runtime attack surface. Both files are static markdown. The only integrity surface is C15 (no Intune-delegation framing leaking into Apple-side docs), enforced by the C15 guard in `v1.6-milestone-audit.mjs` and by acceptance-criteria assertions in this plan and in `check-phase-63.mjs` (Plan 63-06). No external input, no executable code introduced.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Tampering (content-integrity) | 04-custom-role-authoring.md bundle | mitigate | OP-1/OP-2/OP-3 exclusion + pairing asserted by check-phase-63.mjs V-63-03..06 |
| T-63-02 | Information disclosure (framing leak) | 03/04 prose | mitigate | C15 guard active in v1.6-milestone-audit.mjs; Apple-side-only framing |
</threat_model>

<verification>
- `node scripts/validation/v1.6-milestone-audit.mjs` exits 0 (C15 guard passes on the two new docs).
- Both Task verify commands exit 0.
</verification>

<success_criteria>
- OU-01 satisfied: `03-ous-vs-custom-roles.md` presents a 3-topology criteria comparison table with adjacent most-permissive-wins (OP-4) callout.
- OU-02 satisfied: `04-custom-role-authoring.md` ships one canonical 4-6 permission Sub-Org Admin bundle with OP-1/OP-2 exclusions and OP-3 pairing, citing the Phase 62 SOT.
</success_criteria>

<output>
Create `.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-01-SUMMARY.md` when done.
</output>
