---
phase: 63-multi-ou-architecture-apple-admin-setup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cross-platform/apple-business/06-mdm-server-assignment.md
  - docs/cross-platform/apple-business/07-vpp-content-token-consolidation.md
  - docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md
autonomous: true
requirements: [OU-04, OU-05, OU-06]
must_haves:
  truths:
    - "Admin can assign devices to MDM servers per OU with Manage MDM Servers DENY-by-default guidance (OU-04 / OP-1)"
    - "Admin can consolidate VPP content tokens per OU; the doc points to Phase 64's 11-vpp-catalog-runbook.md for the operational OP-9 callout without duplicating it (OU-05)"
    - "Admin can provision Managed Apple Accounts via a manual / SCIM / OIDC+JIT decision matrix (OU-06)"
  artifacts:
    - path: "docs/cross-platform/apple-business/06-mdm-server-assignment.md"
      provides: "Per-OU MDM server assignment + OP-1 DENY-by-default callout"
      contains: "DENY-by-default"
    - path: "docs/cross-platform/apple-business/07-vpp-content-token-consolidation.md"
      provides: "Per-OU content-token consolidation concepts; points to Phase 64 runbook for OP-9 callout"
      contains: "11-vpp-catalog-runbook.md"
    - path: "docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md"
      provides: "Manual / SCIM / OIDC+JIT provisioning decision matrix (Apple-side framing)"
      contains: "OIDC"
  key_links:
    - from: "docs/cross-platform/apple-business/06-mdm-server-assignment.md"
      to: "docs/cross-platform/apple-business/15-mdm-server-reassign-runbook.md"
      via: "forward reference to Phase 64 runbook"
      pattern: "15-mdm-server-reassign-runbook.md"
    - from: "docs/cross-platform/apple-business/07-vpp-content-token-consolidation.md"
      to: "docs/cross-platform/apple-business/11-vpp-catalog-runbook.md"
      via: "forward reference to Phase 64 OP-9 operational callout owner"
      pattern: "11-vpp-catalog-runbook.md"
    - from: "docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md"
      to: "docs/cross-platform/apple-business/16-managed-apple-account-runbook.md"
      via: "forward reference to Phase 64 runbook"
      pattern: "16-managed-apple-account-runbook.md"
---

<objective>
Author the three standalone per-OU admin-setup docs (D-01): MDM server assignment (OU-04), VPP content-token consolidation (OU-05), and Managed Apple Account provisioning (OU-06). These fill the 06/07/08 filename slot, preserving the clean 03-10 sequence.

Purpose: Per-OU setup procedures that complement (but do not duplicate) the Phase 64 operational runbooks; OU-05 specifically points to Phase 64 for the OP-9 hard-bordered callout per D-01.
Output: `06-mdm-server-assignment.md`, `07-vpp-content-token-consolidation.md`, `08-managed-apple-account-provisioning.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-CONTEXT.md
@.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-PATTERNS.md

<interfaces>
<!-- SOT references from 01-role-permission-model.md and 02-ous-architecture.md (use verbatim). -->
OP-1 DENY-by-default source: 01-role-permission-model.md:276 `Manage MDM Servers | tenant-wide | Edit+bundle | DENY-by-default (OP-1) | View Devices`
  callout style: 01-role-permission-model.md:281-288.
"Assign devices to MDM server" is the legitimate sub-org permission (NOT "Manage MDM Servers") — 01-role-permission-model.md:164.
Content token scope note (verbatim source): 01-role-permission-model.md:321-323 ("Each content token is OU-scoped...").
"Does not duplicate" deferral pattern: 02-ous-architecture.md:99-105.
Provisioning decision-tree structure (When / Tradeoff numbered list): 63-PATTERNS.md §08 lines 258-273.
C15 HTML-comment exemption: numbering continues from ABAUDIT-03 (last used in 00-overview.md); Phase 63 starts at ABAUDIT-04.
Phase 64 forward-reference targets (do not author here): 11-vpp-catalog-runbook.md (DELEG-01), 15-mdm-server-reassign-runbook.md (DELEG-05), 16-managed-apple-account-runbook.md (DELEG-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 06-mdm-server-assignment.md (OU-04, OP-1 DENY-by-default)</name>
  <files>docs/cross-platform/apple-business/06-mdm-server-assignment.md</files>
  <read_first>
    - docs/cross-platform/apple-business/02-ous-architecture.md (analog — frontmatter; platform callout; OU Scope Coverage table 114-122; forward-ref pattern :116)
    - docs/cross-platform/apple-business/01-role-permission-model.md (OP-1 source line 276; callout 281-288; line 164)
    - docs/cross-platform/apple-business/06-mdm-server-assignment.md (target — does not exist yet)
    - 63-PATTERNS.md §06-mdm-server-assignment.md
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/06-mdm-server-assignment.md` per OU-04 / D-01. Standard frontmatter (platform: ios+macos) + platform-applicability callout (Apple-Business-side MDM server assignment, NOT Intune MDM push-cert management — wrap any Intune mention in an ABAUDIT-04 exemption comment if needed for C15).
    Document the per-OU MDM server assignment pattern: each OU can have a default device-assignment server so newly-added devices auto-assign; sub-org admins use the OU-scoped `Assign devices to MDM server` permission (NOT `Manage MDM Servers`). Use a procedure table (Step / Action / Who / Notes).
    Add the OP-1 DENY-by-default callout: sub-org (OU-02) admins do NOT manage MDM servers; cite 01-role-permission-model.md:276 and copy the callout style from lines 281-288. Forward-reference Phase 64 `15-mdm-server-reassign-runbook.md` (DELEG-05) for the operational reassign procedure using the "does not duplicate" pattern. Add Cross-References + Version History.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docs/cross-platform/apple-business/06-mdm-server-assignment.md','utf8'); const ok=c.includes('DENY-by-default') && c.includes('Manage MDM Servers') && c.includes('15-mdm-server-reassign-runbook.md'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with standard frontmatter.
    - Contains an `OP-1` DENY-by-default callout naming `Manage MDM Servers` as reserved to tenant admins.
    - Documents the OU-scoped `Assign devices to MDM server` permission as the sub-org path.
    - Forward-references `15-mdm-server-reassign-runbook.md` (Phase 64) without duplicating its operational procedure.
    - Any Intune mention (if present) is wrapped in an `ABAUDIT-` exemption comment (C15).
  </acceptance_criteria>
  <done>06-mdm-server-assignment.md authored with per-OU assignment procedure, OP-1 DENY-by-default callout, and Phase 64 forward reference.</done>
</task>

<task type="auto">
  <name>Task 2: Author 07-vpp-content-token-consolidation.md (OU-05, points to Phase 64 OP-9)</name>
  <files>docs/cross-platform/apple-business/07-vpp-content-token-consolidation.md</files>
  <read_first>
    - docs/cross-platform/apple-business/02-ous-architecture.md (analog — "does not duplicate" deferral pattern 99-105)
    - docs/cross-platform/apple-business/01-role-permission-model.md (Apps&Books permissions 312-319; content-token scope note 321-323)
    - docs/cross-platform/apple-business/07-vpp-content-token-consolidation.md (target — does not exist yet)
    - 63-PATTERNS.md §07-vpp-content-token-consolidation.md
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/07-vpp-content-token-consolidation.md` per OU-05 / D-01. Standard frontmatter (platform: ios+macos) + platform callout. Document per-OU content-token consolidation CONCEPTS (each token is OU-scoped; consolidation = deciding to centralize at tenant root vs distribute per-OU; license transfer constraints). Include the content-token scope note verbatim from 01-role-permission-model.md:321-323.
    Include a clearly-anchored section (e.g. `## Content Token Consolidation Concepts`) so the D-05 reconciled reference in `02-ous-architecture.md` (repaired in Plan 63-05) can point here by file-level anchor.
    Per D-01: the OP-9 untouched-OU HARD-BORDERED callout is OWNED by Phase 64 `11-vpp-catalog-runbook.md` — this admin-setup doc POINTS TO it and does NOT duplicate it (mirror the 02-ous-architecture.md:99-105 "does not duplicate" pattern). Add Cross-References + Version History. C15 Apple-side framing only.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docs/cross-platform/apple-business/07-vpp-content-token-consolidation.md','utf8'); const ok=c.includes('11-vpp-catalog-runbook.md') && /does not duplicate/i.test(c) && /OU-scoped/i.test(c) && /Intune|Entra|Conditional Access|Azure AD/i.test(c)===false; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with standard frontmatter.
    - Contains an anchorable consolidation-concepts section (D-05 forward-reference target).
    - Forward-references `11-vpp-catalog-runbook.md` and contains a "does not duplicate" sentence (OP-9 callout owned by Phase 64).
    - Contains the OU-scoped content-token scope note.
    - Contains none of (case-insensitive): `Intune`, `Entra`, `Conditional Access`, `Azure AD` (C15).
  </acceptance_criteria>
  <done>07-vpp-content-token-consolidation.md authored with consolidation concepts, OU-scoped token note, and a non-duplicating pointer to the Phase 64 OP-9 callout owner.</done>
</task>

<task type="auto">
  <name>Task 3: Author 08-managed-apple-account-provisioning.md (OU-06, manual/SCIM/OIDC+JIT)</name>
  <files>docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md</files>
  <read_first>
    - docs/cross-platform/apple-business/01-role-permission-model.md (People subgroup 240-243; Managed Apple Account framing)
    - docs/cross-platform/apple-business/_admin-directory.md (provisioning-path decision structure)
    - docs/cross-platform/apple-business/00-overview.md (ABAUDIT exemption pattern lines 10-11)
    - docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md (target — does not exist yet)
    - 63-PATTERNS.md §08-managed-apple-account-provisioning.md
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md` per OU-06 / D-01. Standard frontmatter (platform: ios+macos) + platform callout. Author a manual / SCIM / OIDC+JIT provisioning DECISION MATRIX as a numbered list (When / Tradeoff sub-items per 63-PATTERNS.md §08), entirely Apple-side framed: "Managed Apple Account", "federated identity source" (generic, NOT a Microsoft product name), "SCIM provisioning endpoint", "federated authentication via OpenID Connect", "just-in-time account creation".
    This is the HIGHEST C15-risk surface. If a Microsoft identity concept must be named for disambiguation, wrap it with an `<!-- ABAUDIT-NN: ... C15 regex N false-positive exemption -->` comment (continue numbering from ABAUDIT-04, incrementing). Prefer Apple-only framing over any exemption.
    Forward-reference Phase 64 `16-managed-apple-account-runbook.md` (DELEG-06) for the operational runbook using the "does not duplicate" pattern. Add Cross-References + Version History.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md','utf8'); const lines=c.split(/\r?\n/); const allow=new Set(); lines.forEach((l,i)=>{if(/<!--\s*ABAUDIT-\d+:/.test(l)){allow.add(i);allow.add(i+1);}}); const rx=/\bIntune\s+(RBAC|role|scope\s+tag|admin\s+role)\b/i; const viol=lines.findIndex((l,i)=>!allow.has(i)&&rx.test(l)); const ok=c.includes('SCIM')&&c.includes('OIDC')&&c.includes('Managed Apple Account')&&c.includes('16-managed-apple-account-runbook.md')&&viol===-1; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with standard frontmatter.
    - Contains a manual / SCIM / OIDC+JIT decision matrix (all three paths named: `SCIM`, `OIDC`, manual).
    - Uses `Managed Apple Account` (not "Managed Apple ID").
    - Forward-references `16-managed-apple-account-runbook.md` (Phase 64).
    - C15: no bare `Intune (RBAC|role|scope tag|admin role)` outside an `ABAUDIT-` exemption comment; ABAUDIT numbering continues from ABAUDIT-04.
  </acceptance_criteria>
  <done>08-managed-apple-account-provisioning.md authored with an Apple-side manual/SCIM/OIDC+JIT decision matrix and Phase 64 forward reference, passing the C15 guard.</done>
</task>

</tasks>

<threat_model>
Documentation deliverable — no runtime attack surface. Static markdown. The highest integrity surface is C15 framing leakage in `08-managed-apple-account-provisioning.md` (SCIM/OIDC+JIT prose) — mitigated by Apple-side-only framing, ABAUDIT exemption comments where unavoidable, the live C15 guard in `v1.6-milestone-audit.mjs`, and per-task verify assertions replicating the C15 regex. No external input, no executable code introduced.
</threat_model>

<verification>
- `node scripts/validation/v1.6-milestone-audit.mjs` exits 0 (C15 guard passes on all three new docs).
- All three Task verify commands exit 0.
</verification>

<success_criteria>
- OU-04, OU-05, OU-06 satisfied via three standalone admin-setup docs filling the 06/07/08 slot, with correct Phase 64 forward references and OP-1 / OP-9 / C15 handling.
</success_criteria>

<output>
Create `.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-03-SUMMARY.md` when done.
</output>
