---
phase: 63-multi-ou-architecture-apple-admin-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md
autonomous: true
requirements: [OU-03]
must_haves:
  truths:
    - "Admin can onboard a sub-org admin (Managed Apple Account creation + role assignment + OU scoping) via 05-sub-org-admin-onboarding.md (OU-03)"
    - "The doc includes a paired offboarding section (OP-8)"
    - "The doc contains a heading rendering to the exact GitHub slug #which-admin-owns-this-pool (C16 gate)"
  artifacts:
    - path: "docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md"
      provides: "Sub-org admin onboarding + paired offboarding workflow with the C16 Wave-B/Phase-65 anchor"
      contains: "## Which admin owns this pool?"
  key_links:
    - from: "docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md"
      to: "docs/cross-platform/apple-business/_admin-directory.md"
      via: "which-admin-owns-this-pool cross-link to admin directory (C16)"
      pattern: "_admin-directory.md"
    - from: "docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md"
      to: "docs/cross-platform/apple-business/04-custom-role-authoring.md"
      via: "role-assignment cross-link"
      pattern: "04-custom-role-authoring.md"
---

<objective>
Author the sub-org admin onboarding workflow (OU-03), carrying the C16-gated `#which-admin-owns-this-pool` anchor that unlocks Phase 64 Wave B and is the Phase 65 L1 #34 cross-link target.

Purpose: Admins need a documented onboarding path (Managed Apple Account creation + role assignment + OU scoping) with paired offboarding (OP-8); the stable file-level anchor `#which-admin-owns-this-pool` is a hard cross-phase gate.
Output: `docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md`.

This plan is isolated to a single doc because its `#which-admin-owns-this-pool` anchor is the Wave-B unlock gate and the C16 target — it must land cleanly and independently.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-CONTEXT.md
@.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-PATTERNS.md

<interfaces>
<!-- C16 slug derivation (deterministic GitHub algorithm). -->
Heading `## Which admin owns this pool?` -> slug `which-admin-owns-this-pool`
  (lowercase, spaces -> hyphens, `?` punctuation dropped). This EXACT heading string is required.
  Authoritative placement: this doc 05-sub-org-admin-onboarding.md (per CONTEXT D-01/OU-03, ROADMAP SC#3,
  REQUIREMENTS OU-03, and the orchestrator C16 guardrail). NOTE: 63-PATTERNS.md text placing this
  heading in 09-shared-ipad-lifecycle.md is a timed-out-research artifact and is OVERRIDDEN — the
  anchor lives in 05-, not 09-.

_admin-directory.md cross-link target (Phase 62 deliverable, already shipped):
  Account Holder DO-NOT-DELEGATE callout pattern at _admin-directory.md:46-49
  Cross-References block at _admin-directory.md:76-80
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 05-sub-org-admin-onboarding.md with the C16 anchor + paired offboarding (OP-8)</name>
  <files>docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md</files>
  <read_first>
    - docs/cross-platform/apple-business/_admin-directory.md (analog — frontmatter lines 9-15; DO-NOT-DELEGATE callout 46-49; Cross-References 76-80; C16 cross-link target)
    - docs/cross-platform/apple-business/04-custom-role-authoring.md (peer — role-assignment reference; authored in Plan 63-01)
    - docs/cross-platform/apple-business/02-ous-architecture.md (OU scoping context)
    - docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md (target — does not exist yet)
    - 63-PATTERNS.md §05-sub-org-admin-onboarding.md
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md` per OU-03 / D-01. Standard frontmatter (platform: ios+macos). Add the platform-applicability callout + glossary cross-link.
    Document the onboarding workflow covering: (1) Managed Apple Account creation, (2) custom-role assignment (cross-link to 04-custom-role-authoring.md and the Phase 62 role/permission SOT), (3) OU scoping (cross-link to 02-ous-architecture.md). Author OU hierarchy as flat-by-default with optional one-level sub-OUs (depth <= 2) per D-06; carry a portal-verification note for depth > 2.
    Include the EXACT heading `## Which admin owns this pool?` (verbatim, including the `?`) — this is the C16-gated `#which-admin-owns-this-pool` anchor. Under it, cross-link to `_admin-directory.md` as the per-OU admin-ownership lookup target, and note this anchor is the Phase 64 Wave-B gate and the Phase 65 L1 #34 (ABNAV-01) cross-link target.
    Add a paired OFFBOARDING section (OP-8) documenting deactivation/role-revocation steps in parallel with the onboarding paths (manual / SCIM / OIDC+JIT); flag OP-8 auto-revoke-vs-stuck-state behavior as pending portal verification where uncertain.
    Copy the Account Holder DO-NOT-DELEGATE callout (style from _admin-directory.md:46-49). Add Cross-References + Version History. C15 Apple-side framing only.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md','utf8'); const ok=c.includes('## Which admin owns this pool?') && c.includes('_admin-directory.md') && /offboard/i.test(c) && c.includes('OP-8') && /Intune|Entra|Conditional Access|Azure AD/i.test(c)===false; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md` exists with standard frontmatter.
    - Contains the VERBATIM heading line `## Which admin owns this pool?` (slug -> `which-admin-owns-this-pool`, C16).
    - That section cross-links to `_admin-directory.md`.
    - Contains an offboarding section referencing `OP-8` (paired offboarding).
    - Onboarding covers Managed Apple Account creation + role assignment (cross-link `04-custom-role-authoring.md`) + OU scoping (cross-link `02-ous-architecture.md`).
    - Authors sub-OU depth <= 2 only; carries a depth > 2 portal-verification note (D-06).
    - Contains none of (case-insensitive): `Intune`, `Entra`, `Conditional Access`, `Azure AD` (C15).
  </acceptance_criteria>
  <done>05-sub-org-admin-onboarding.md authored with the verbatim C16 anchor heading, admin-directory cross-link, paired OP-8 offboarding, depth<=2 authoring, and clean C15 framing.</done>
</task>

</tasks>

<threat_model>
Documentation deliverable — no runtime attack surface. Static markdown. The critical integrity surface is the C16 anchor stability (`#which-admin-owns-this-pool`) which gates Phase 64 Wave B and Phase 65; a misspelled heading would silently break the cross-phase triangle. Mitigation: exact-string assertion in this plan's verify and in check-phase-63.mjs V-63-02. C15 framing guard active. No external input, no executable code.
</threat_model>

<verification>
- `node scripts/validation/v1.6-milestone-audit.mjs` exits 0 (C15/C16 surfaces unaffected).
- Task verify command exits 0 (C16 anchor + offboarding present).
</verification>

<success_criteria>
- OU-03 satisfied: onboarding + paired offboarding documented; `#which-admin-owns-this-pool` anchor present and stable for downstream phases.
</success_criteria>

<output>
Create `.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-02-SUMMARY.md` when done.
</output>
