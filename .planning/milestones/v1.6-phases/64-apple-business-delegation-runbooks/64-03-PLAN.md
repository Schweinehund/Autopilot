---
phase: 64-apple-business-delegation-runbooks
plan: 03
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - docs/cross-platform/apple-business/14-device-transfer-runbook.md
  - docs/cross-platform/apple-business/15-mdm-server-reassign-runbook.md
autonomous: true
requirements: [DELEG-04, DELEG-05]

must_haves:
  truths:
    - "Admin can transfer a device cross-OU via 14- with the OP-5 4-cell impact matrix (VPP license / enrollment profile / Intune config profile / audit entry) + pre-transfer dependency checklist + hard callout (no L2-approval gate)"
    - "Admin can reassign an MDM server via a SINGLE runbook 15- with an OS-version eligibility matrix (iOS/iPadOS/macOS/tvOS 26+ in-place; legacy factory erase) and exactly 2 sub-H2s"
    - "Exactly ONE MDM-reassign runbook ships (no 15b-) — SC#3 / CI-5 anti-proliferation"
  artifacts:
    - path: "docs/cross-platform/apple-business/14-device-transfer-runbook.md"
      provides: "DELEG-04 cross-OU transfer runbook with OP-5 hard callout + 4-cell matrix"
      contains: "⛔ Cross-OU Transfer — 4-cell impact matrix (OP-5)"
    - path: "docs/cross-platform/apple-business/15-mdm-server-reassign-runbook.md"
      provides: "DELEG-05 single MDM-reassign runbook with OS-version eligibility matrix + 2 sub-H2s"
      contains: "## Sub-H2"
  key_links:
    - from: "docs/cross-platform/apple-business/14-device-transfer-runbook.md"
      to: "docs/cross-platform/apple-business/10-apple-tv-lifecycle.md"
      via: "Apple TV transfer cross-link (same mechanics)"
      pattern: "10-apple-tv-lifecycle"
    - from: "docs/cross-platform/apple-business/15-mdm-server-reassign-runbook.md"
      to: "docs/cross-platform/apple-business/01-role-permission-model.md"
      via: "D-03 Required Role & Permission citation"
      pattern: "01-role-permission-model"
---

<objective>
Author the two matrix-heavy operational runbooks: `14-` (cross-OU device transfer with the OP-5 4-cell impact matrix) and `15-` (the SINGLE MDM-server reassign runbook with the OS-version eligibility matrix and 2 sub-H2s consolidating FEATURES Workflow 5.3).

Purpose: `14-` prevents the OP-5 silent-VPP-breakage hazard via an upfront impact matrix and pre-transfer checklist; `15-` honors the SC#3 / CI-5 anti-proliferation invariant — exactly one MDM-reassign runbook, not two.
Output: Two runbook markdown files honoring the D-01 envelope, D-02/D-03 blocks; `14-` carries the OP-5 hard callout (no L2-approval gate); `15-` uses a light Note tier (no destructive hard callout) with the OS matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/64-apple-business-delegation-runbooks/64-CONTEXT.md
@.planning/phases/64-apple-business-delegation-runbooks/64-RESEARCH.md
@.planning/phases/64-apple-business-delegation-runbooks/64-PATTERNS.md
@.planning/phases/64-apple-business-delegation-runbooks/64-CONVENTIONS.md

<interfaces>
<!-- Envelope analog + the forward-ref at 06- line 97 naming 15-mdm-server-reassign-runbook.md (anti-proliferation) -->
@docs/cross-platform/apple-business/06-mdm-server-assignment.md
<!-- Apple TV transfer cross-link target (14- same-mechanics) -->
@docs/cross-platform/apple-business/10-apple-tv-lifecycle.md
<!-- D-03 Required Role & Permission SOT -->
@docs/cross-platform/apple-business/01-role-permission-model.md

LOCKED hard-callout string (14- only — asserted by check-phase-64.mjs):
- 14- OP-5:  > **⛔ Cross-OU Transfer — 4-cell impact matrix (OP-5)**

15- uses NO destructive hard callout (it is not a single destructive action) — use the light `> **Note:**` tier from 09- for informational warnings.

Envelope frontmatter (both): last_verified: 2026-05-22 / review_by: 2026-07-21 / applies_to: apple-business / audience: admin
platform values: 14- = ios+ipados+macos+tvos ; 15- = ios+ipados+macos+tvos

15- anti-proliferation: EXACTLY ONE file. No 15b- / 15-mdm-server-reassign-2. Two sub-H2s only: Sub-H2 A (legacy factory-erase) and Sub-H2 B (OS-26+ in-place migration).

15- Required Role & Permission: this action maps to the Sub-Org Admin bundle ("Assign devices to MDM server"), so DO cite 04-custom-role-authoring.md (per D-03 differential rule, mirroring 06-).
14- Required Role & Permission: cite 01- Devices subgroup; do NOT cite 04-.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 14-device-transfer-runbook.md (DELEG-04)</name>
  <files>docs/cross-platform/apple-business/14-device-transfer-runbook.md</files>
  <read_first>
    - docs/cross-platform/apple-business/06-mdm-server-assignment.md (envelope template)
    - docs/cross-platform/apple-business/10-apple-tv-lifecycle.md (Apple TV transfer cross-link target)
    - docs/cross-platform/apple-business/01-role-permission-model.md (Devices subgroup transfer permission)
    - .planning/phases/64-apple-business-delegation-runbooks/64-RESEARCH.md (DELEG-04: transfer mechanics; OP-5 4-cell impact matrix exact rows; pre-transfer checklist)
    - .planning/phases/64-apple-business-delegation-runbooks/64-PATTERNS.md (14- section: OP-5 hard callout body with the 4-cell table, NO L2-approval gate)
    - .planning/phases/64-apple-business-delegation-runbooks/64-CONVENTIONS.md (locked OP-5 string + D-04 Refined-C no-gate rule)
    - docs/cross-platform/apple-business/14-device-transfer-runbook.md (the file being created)
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/14-device-transfer-runbook.md` with the D-01 envelope (platform: ios+ipados+macos+tvos; Platform-applicability block; D-02 scope-boundary callout + forward link to `18-`; Training-data notice citing FEATURES §5.2 + PITFALLS OP-5, 60-day target). Add `## Required Role & Permission` per D-03 citing `01-role-permission-model.md` Devices subgroup (OU-scoped) — do NOT cite `04-`; include OP-2. Body documents the cross-OU transfer procedure (Devices > select > Edit Location / Reassign to OU > confirm; single-admin click, no Apple-side approval workflow). Realize the OP-5 hard-bordered callout using the exact locked opening string `> **⛔ Cross-OU Transfer — 4-cell impact matrix (OP-5)**` with the full body from 64-CONVENTIONS.md: the 4-cell impact table (VPP device-licensed apps BREAK / enrollment profile does NOT follow / Intune config profiles survive / audit entry logged with author-scope semantics referencing OP-14) and the pre-transfer dependency checklist (identify VPP dependencies / revoke source-OU licenses with 30-day grace / target-OU catalog replacements / enrollment-profile compatibility / notify user). Per D-04 Refined-C, NO `L2 approval required` gate clause. Add a cross-link to `10-apple-tv-lifecycle.md` noting Apple TV transfers follow the same mechanics. Add `## Verification`. Tag portal-nav claims `[CITED: training; needs live verification]`.
  </action>
  <acceptance_criteria>
    - File exists with `platform: ios+ipados+macos+tvos`
    - Contains the exact string `> **⛔ Cross-OU Transfer — 4-cell impact matrix (OP-5)**`
    - Contains all 4 impact-matrix cells (VPP license, enrollment profile, Intune config profile, audit entry)
    - Contains a pre-transfer dependency checklist
    - Does NOT contain the string `L2 approval required` (D-04: no gate on 14-)
    - Contains a cross-link to `10-apple-tv-lifecycle.md`
    - Contains `## Required Role & Permission` + `## Verification` + forward link to `18-` + citation to `01-`
    - Passes C15 framing guard (the "Intune config profiles survive" row needs an ABAUDIT comment if it triggers a C15 regex)
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-64.mjs --verbose 2>&1 | grep -E "V-64-(01|06|07|08|09|10)" ; node -e "const fs=require('fs');const c=fs.readFileSync('docs/cross-platform/apple-business/14-device-transfer-runbook.md','utf8');const need=['platform: ios+ipados+macos+tvos','> **⛔ Cross-OU Transfer — 4-cell impact matrix (OP-5)**','enrollment profile','Intune config profile','audit entry','## Required Role & Permission','## Verification','18-cross-org-boundary-cheat-sheet','10-apple-tv-lifecycle','01-role-permission-model'];const miss=need.filter(s=>!c.toLowerCase().includes(s.toLowerCase()));if(miss.length){console.error('MISSING: '+miss.join(' | '));process.exit(1)}if(c.includes('L2 approval required')){console.error('14- must NOT carry L2-approval gate (D-04)');process.exit(1)}console.log('14- OK')"</automated>
  </verify>
  <done>14- ships with the OP-5 hard callout (no L2-approval gate), the 4-cell impact matrix, pre-transfer checklist, Apple TV cross-link, and full envelope.</done>
</task>

<task type="auto">
  <name>Task 2: Author 15-mdm-server-reassign-runbook.md (DELEG-05 — SINGLE file, 2 sub-H2s)</name>
  <files>docs/cross-platform/apple-business/15-mdm-server-reassign-runbook.md</files>
  <read_first>
    - docs/cross-platform/apple-business/06-mdm-server-assignment.md (envelope + the forward-ref at lines 97-100 naming 15-mdm-server-reassign-runbook.md; D-03 differential 04- citation pattern)
    - docs/cross-platform/apple-business/01-role-permission-model.md ("Assign devices to MDM server" permission — maps to Sub-Org Admin bundle)
    - docs/cross-platform/apple-business/04-custom-role-authoring.md (Sub-Org Admin bundle — cited per D-03 because the permission maps to it)
    - .planning/phases/64-apple-business-delegation-runbooks/64-RESEARCH.md (DELEG-05: Sub-H2 A legacy reassign; Sub-H2 B OS-26+ in-place migration; OS eligibility matrix; what is preserved; VPP content-token implication; mixed-fleet note)
    - .planning/phases/64-apple-business-delegation-runbooks/64-PATTERNS.md (15- section: anti-proliferation invariant, body structure, OS eligibility matrix table)
    - .planning/phases/64-apple-business-delegation-runbooks/64-CONVENTIONS.md (envelope spec; D-04 light-Note tier rule for 15-)
    - docs/cross-platform/apple-business/15-mdm-server-reassign-runbook.md (the file being created)
  </read_first>
  <action>
    Create EXACTLY ONE file `docs/cross-platform/apple-business/15-mdm-server-reassign-runbook.md` (no `15b-`, no `15-mdm-server-reassign-2`) with the D-01 envelope (platform: ios+ipados+macos+tvos; Platform-applicability block; D-02 scope-boundary callout + forward link to `18-`; Training-data notice citing FEATURES §5.3 + Apple Deployment Guide dep4acb2aa44, 60-day target). Add `## Required Role & Permission` per D-03 citing `01-role-permission-model.md` AND `04-custom-role-authoring.md` (this permission "Assign devices to MDM server" maps to the Sub-Org Admin bundle, so the 04- cite IS required here, mirroring 06-); include OP-1 DENY-by-default on "Manage MDM Servers" and OP-2. Body has EXACTLY two sub-H2s consolidating FEATURES Workflow 5.3: `## Sub-H2 A: Legacy Reassignment (iOS/iPadOS ≤ 25 / macOS ≤ 15 / tvOS ≤ 25)` documenting the factory-erase + re-run Setup Assistant path, and `## Sub-H2 B: In-Place Migration (iOS/iPadOS/macOS/tvOS 26+)` documenting the no-erase guided migration (preserves user data + declarative managed apps; new MDM redeploys configs; VPP content-token re-upload within 30 days). Include the OS-version eligibility matrix table from RESEARCH (iOS/iPadOS/macOS/tvOS 26+ → in-place; ≤ 25 → factory erase) and the mixed-fleet two-wave note. Use the light `> **Note:**` tier (not a `⛔` hard callout) for informational warnings — 15- is not a single destructive action. Add `## Verification`. Tag portal-nav claims `[CITED: training; needs live verification]`.
  </action>
  <acceptance_criteria>
    - EXACTLY ONE file `15-mdm-server-reassign-runbook.md` exists; no `15b-` or `15-mdm-server-reassign-2` file present
    - File has `platform: ios+ipados+macos+tvos`
    - Contains exactly two sub-H2 sections (Sub-H2 A legacy, Sub-H2 B in-place 26+)
    - Contains the OS-version eligibility matrix referencing `26+` and legacy `≤ 25`
    - Cites BOTH `01-role-permission-model.md` AND `04-custom-role-authoring.md` (D-03 — permission maps to bundle)
    - Contains `## Required Role & Permission` + `## Verification` + forward link to `18-`
    - Passes C15 framing guard
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-64.mjs --verbose 2>&1 | grep -E "V-64-(01|06|07|08|09|10)|anti-prolif|15b" ; node -e "const fs=require('fs');const path=require('path');const dir='docs/cross-platform/apple-business';const bad=fs.readdirSync(dir).filter(f=>/^15b-|15-mdm-server-reassign-2/.test(f));if(bad.length){console.error('ANTI-PROLIFERATION VIOLATION: '+bad.join(','));process.exit(1)}const c=fs.readFileSync(path.join(dir,'15-mdm-server-reassign-runbook.md'),'utf8');const need=['platform: ios+ipados+macos+tvos','Sub-H2 A','Sub-H2 B','26+','## Required Role & Permission','## Verification','18-cross-org-boundary-cheat-sheet','01-role-permission-model','04-custom-role-authoring'];const miss=need.filter(s=>!c.includes(s));if(miss.length){console.error('MISSING: '+miss.join(' | '));process.exit(1)}console.log('15- OK')"</automated>
  </verify>
  <done>Exactly one 15- file ships with two sub-H2s, the OS-version eligibility matrix, the differential 04- citation, and full envelope; no 15b- sibling exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook author → C15 banned-phrase guard | 14- 4-cell matrix references Intune config profiles; 15- references the Intune target MDM |
| runbook author → anti-proliferation invariant | 15- must be exactly one file; a second file silently breaks SC#3 / CI-5 |
| runbook content → operator action | 14- transfer without the impact matrix could silently break VPP licenses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-08 | Information Disclosure | 14- "Intune config profiles survive" matrix row | mitigate | ABAUDIT comment on the preceding line if the row triggers a C15 regex; framing is Apple-Business-vs-Intune disambiguation (allowlisted false-positive) |
| T-64-09 | Tampering | 15- file proliferation (15b-) | mitigate | check-phase-64 anti-proliferation assertion + Task 2 acceptance criteria forbid any 15b-/15-mdm-server-reassign-2 sibling |
| T-64-10 | Information Disclosure | 14- silent VPP-license breakage | mitigate | OP-5 hard callout with the 4-cell impact matrix + pre-transfer dependency checklist, asserted exact-string |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs — documentation only |
</threat_model>

<verification>
- `node scripts/validation/check-phase-64.mjs` — V-64-01..V-64-10 + anti-proliferation pass for 14-/15-
- `node scripts/validation/v1.6-milestone-audit.mjs` — C15 framing guard passes
- Exactly one 15- file exists (no 15b-); 15- has two sub-H2s + OS matrix
</verification>

<success_criteria>
- 14- ships with the OP-5 hard callout (no L2-approval gate), 4-cell impact matrix, pre-transfer checklist, and Apple TV cross-link
- 15- is exactly one file with two sub-H2s, the OS-version eligibility matrix, and the differential 04- citation
- C15 gate passes for both files
</success_criteria>

<output>
Create `.planning/phases/64-apple-business-delegation-runbooks/64-03-SUMMARY.md` when done
</output>
