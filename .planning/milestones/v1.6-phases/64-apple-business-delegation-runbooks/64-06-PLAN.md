---
phase: 64-apple-business-delegation-runbooks
plan: 06
type: execute
wave: 3
depends_on: ["64-01", "64-02", "64-03", "64-04", "64-05"]
files_modified:
  - .planning/phases/64-apple-business-delegation-runbooks/64-VERIFICATION.md
autonomous: true
requirements: [DELEG-01, DELEG-02, DELEG-03, DELEG-04, DELEG-05, DELEG-06, DELEG-07, DELEG-08]

must_haves:
  truths:
    - "check-phase-64.mjs exits 0 — all V-64-01..V-64-SELF assertions PASS"
    - "v1.6-milestone-audit.mjs exits 0 — C14/C15/C16 all PASS with the 8 new runbooks in the corpus"
    - "All 8 runbooks 11-..18- exist and satisfy the D-01 envelope + their per-requirement body must-haves"
    - "64-VERIFICATION.md records the green full-suite run before downstream Phase 65 consumes 12-"
  artifacts:
    - path: ".planning/phases/64-apple-business-delegation-runbooks/64-VERIFICATION.md"
      provides: "Verification-during-execution record (v1.2 retro lesson) confirming full-suite green"
      contains: "check-phase-64"
  key_links:
    - from: ".planning/phases/64-apple-business-delegation-runbooks/64-VERIFICATION.md"
      to: "scripts/validation/check-phase-64.mjs"
      via: "records the validator exit-0 result"
      pattern: "check-phase-64"
---

<objective>
Wave 3 close-gate: run the full validation suite green and author `64-VERIFICATION.md`. Confirm `check-phase-64.mjs` exits 0 with all 8 runbooks present, and `v1.6-milestone-audit.mjs` exits 0 with C14/C15/C16 PASS.

Purpose: Verification-during-execution (v1.2 retro lesson) — Phase 64 must produce a VERIFICATION record BEFORE Phase 65 consumes `12-` as the C16 canonical admin-context doc. This wave catches any cross-runbook envelope/ABAUDIT/C15 gap that only surfaces when all 8 files coexist in the corpus.
Output: `64-VERIFICATION.md` recording the green full-suite run, the per-requirement DELEG-01..08 coverage map, and any residual `[ASSUMED]`/`[verify in portal]` items routed to the 60-day re-verification window.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/64-apple-business-delegation-runbooks/64-CONTEXT.md
@.planning/phases/64-apple-business-delegation-runbooks/64-VALIDATION.md

<interfaces>
@scripts/validation/check-phase-64.mjs
@scripts/validation/v1.6-milestone-audit.mjs

Full suite command: `node scripts/validation/v1.6-milestone-audit.mjs && node scripts/validation/check-phase-64.mjs`
Both must exit 0. check-phase-64 includes V-64-AUDIT (subprocess-invokes the milestone harness) and V-64-CHAIN (prior-phase validators) — a green check-phase-64 implies a green harness, but run both explicitly for the record.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full suite green and author 64-VERIFICATION.md</name>
  <files>.planning/phases/64-apple-business-delegation-runbooks/64-VERIFICATION.md</files>
  <read_first>
    - scripts/validation/check-phase-64.mjs (the validator to run; assertion inventory)
    - scripts/validation/v1.6-milestone-audit.mjs (C14/C15/C16 gates)
    - .planning/phases/64-apple-business-delegation-runbooks/64-VALIDATION.md (per-task verification map; the table to confirm green)
    - .planning/phases/64-apple-business-delegation-runbooks/64-RESEARCH.md (Assumptions Log + Open Questions to route to the 60-day window)
    - All 8 runbooks docs/cross-platform/apple-business/11-..18- (confirm they exist and satisfy must-haves)
    - .planning/phases/64-apple-business-delegation-runbooks/64-VERIFICATION.md (the file being created)
  </read_first>
  <action>
    Run `node scripts/validation/check-phase-64.mjs --verbose` and `node scripts/validation/v1.6-milestone-audit.mjs`. Both must exit 0. If any assertion FAILs, STOP and report the failing assertion to the orchestrator (do NOT author a green VERIFICATION over a red suite, and do NOT modify a Wave 2 runbook from this plan — gaps route back to the owning Wave 2 plan). Once both exit 0, create `.planning/phases/64-apple-business-delegation-runbooks/64-VERIFICATION.md` recording: (1) the exact commands run + their exit codes + a PASS/FAIL line per V-64 assertion; (2) the milestone-audit C14/C15/C16 results; (3) a per-requirement coverage table mapping DELEG-01..08 to their runbook file and the V-64 assertion(s) that prove each; (4) confirmation that `12-` contains no `34-apple-business` reference and the C16 admin_12 exemption (sunset_phase 64-65) is intact; (5) confirmation that exactly one `15-` MDM-reassign file ships (no `15b-`); (6) the residual `[ASSUMED]` / `[verify in portal]` items from RESEARCH (2026 content-token nav path, SCIM 60-vs-90-day window, post-collision-window recovery, audit retention SLA) routed to the 60-day re-verification window (by 2026-07-21). Do NOT edit any runbook content from this plan — this is a read-and-record gate only.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-64.mjs` exits 0 (all V-64 assertions PASS or SKIPPED)
    - `node scripts/validation/v1.6-milestone-audit.mjs` exits 0 (C14/C15/C16 PASS)
    - `64-VERIFICATION.md` exists and records both commands, exit codes, and a per-requirement DELEG-01..08 coverage table
    - VERIFICATION records the 12- no-34-apple-business confirmation and the 15- single-file confirmation
    - VERIFICATION lists the residual [ASSUMED]/[verify in portal] items routed to the 60-day window
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/v1.6-milestone-audit.mjs && node scripts/validation/check-phase-64.mjs && node -e "const fs=require('fs');const c=fs.readFileSync('.planning/phases/64-apple-business-delegation-runbooks/64-VERIFICATION.md','utf8');const need=['check-phase-64','DELEG-01','DELEG-08','34-apple-business','15-mdm-server-reassign'];const miss=need.filter(s=>!c.includes(s));if(miss.length){console.error('MISSING: '+miss.join(' | '));process.exit(1)}console.log('64-VERIFICATION.md OK; full suite green')"</automated>
  </verify>
  <done>Both validators exit 0; 64-VERIFICATION.md records the green full-suite run, the DELEG-01..08 coverage map, the C16/anti-proliferation confirmations, and the residual 60-day-window items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 64 corpus → Phase 65 consumer | 12- becomes the C16 canonical admin-context doc Phase 65 cross-links to; a corrupt 12- propagates downstream |
| full-suite run → VERIFICATION record | A green VERIFICATION authored over a red suite would mask a corpus-integrity regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-17 | Repudiation | green VERIFICATION over red suite | mitigate | Task hard-stops and reports to orchestrator on any FAIL; VERIFICATION is authored only after both validators exit 0 |
| T-64-18 | Tampering | C16 admin_12 exemption regression | mitigate | VERIFICATION explicitly confirms 12- has no 34-apple-business and the sunset_phase 64-65 exemption is intact before Phase 65 consumes it |
| T-64-19 | Tampering | C15 cross-runbook banned-phrase leak | mitigate | v1.6-milestone-audit.mjs C15 runs across all 8 coexisting files; a leak that only surfaces with all files present is caught here, not at Phase 66 |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs — documentation + Node validator using existing runtime only |
</threat_model>

<verification>
- `node scripts/validation/v1.6-milestone-audit.mjs && node scripts/validation/check-phase-64.mjs` exits 0
- `64-VERIFICATION.md` records the green run + DELEG-01..08 coverage + C16/anti-proliferation confirmations + 60-day-window items
</verification>

<success_criteria>
- check-phase-64.mjs and v1.6-milestone-audit.mjs both exit 0 with all 8 runbooks in the corpus
- C14/C15/C16 PASS
- 64-VERIFICATION.md is the verification-during-execution record gating Phase 65's consumption of 12-
</success_criteria>

<output>
Create `.planning/phases/64-apple-business-delegation-runbooks/64-06-SUMMARY.md` when done
</output>
