# v1.7 Deferred Cleanup — v1.8+ Backlog

**Authored:** 2026-05-26 (Phase 68 close-gate stub) / Extended 2026-05-28 (Phase 69 close-gate) / **Finalized 2026-05-28 (Phase 70 HARNESS-06 close — v1.6 carry-overs promoted from stub to full sections + Phase 67 VPP-additional discoveries appended + Phase 69 final dispositions reaffirmed)**
**Milestone:** v1.7 (Deferred Backlog Closure + Validator Chain Hardening)
**Status:** FINALIZED at v1.7 milestone close (Phase 70 HARNESS-06 — Plan 70-05 Commit B). v1.8+ entry-phase planning reads this artifact as the single-source-of-truth deferred backlog.
**Cross-link from:** `.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-VERIFICATION.md` §Section E Discoveries + `.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md` §Section E Discoveries + `.planning/milestones/v1.7-MILESTONE-AUDIT.md` Discoveries Surfaced During Execution section

**Authoring rationale:** v1.7 introduces a standalone deferred-cleanup artifact (parallels `v1.6-DEFERRED-CLEANUP.md` authored at Phase 66-03 commit `c7a3973`) per `ROADMAP.md:242` + `REQUIREMENTS.md:64` AUDIT-15 lineage. Initial stub created at Phase 68 close per `68-CONTEXT.md` D-04 §"Deferred Ideas / Discovered during Phase 68 discussion" + 68-RESEARCH Open Question 3 resolution; extended at Phase 69 close with cross-OS discoveries (FETCH-DEPTH-01 + SCOPE-GAP-61 + D-04-OVERSPEC-01 + CHAIN-WRAPPER-01); finalized at Phase 70 HARNESS-06 with v1.6 carry-overs promoted from stub to full sections (CI-3 + Multi-tenant + Apple Business Device API + per-OU CRD + Account Holder + ASM) + Phase 67 VPP-additional discoveries.

**Trigger-to-sweep:** Each section below carries explicit criteria for when v1.8+ should pick up the work.

---

## ARCHIVE-01: cdcce23 archive-script garbage-insert root cause investigation

**Scope:** Investigate why commit `cdcce23` (`chore: archive v1.5 milestone files`, 2026-05-07) inserted a duplicate v1.5 H2 entry at `.planning/MILESTONES.md` lines 3-70 — scripted-extraction debris (`One-liner:`, `SUBSUMED BY PLAN 48-01.`, `NO COMMIT MADE.`, `Hash:`, `Pre-edit:`, `Total file size:`, `File:`, `Insertion position:`, `Single deliverable:`, `Plan goal:`, `Found during:` placeholders) rather than actual bullet content from per-plan SUMMARY files.

**Source-of-truth:** Phase 68 Plan 68-04 SUMMARY + `68-VERIFICATION.md` §Section E Discovery #4; commit `cdcce23` git log + working-tree at that commit.

**Symptom (Phase 68 fixed; root cause deferred):** Plan 68-04 (SHA `d142c7a`) DELETED lines 3-71 — surgical symptom fix. Root cause likely lives in `.claude/commands/gsd-complete-milestone.md` or related archive-automation skill (extraction-pattern logic that emits placeholder labels).

**Phase 70 disposition (per D-03 LOCKED):** ARCHIVE-01 recurrence-check is OUT OF SCOPE for HARNESS-05 AND HARNESS-06. The 1-line runbook diff-check belongs at the `/gsd-complete-milestone` skill invocation site (post-Phase-70-close archival), NOT inside Phase 70's milestone-audit-authoring scope. **v1.7-MILESTONE-AUDIT.md Sign-off section documents the hand-off to `/gsd-complete-milestone`** — that skill's post-archival step MUST run `git diff HEAD~1 HEAD -- .planning/MILESTONES.md` to detect cdcce23-class recurrence. If recurrence detected: file follow-up issue with archive-script root-cause analysis. **DO NOT mask via deletion — investigate the script.**

**Trigger-to-sweep (v1.8+):** Archive-script source audit + regression test (skill source inspection + synthetic milestone-close fixture + verification that placeholder-label extraction is corrected). The `/gsd-complete-milestone` skill's post-archival diff-check is the trip-wire for v1.8+ pickup.

**Estimated effort:** 1-2h for archive-script audit + regression test.

**RECURRENCE CONFIRMED — v1.7 milestone close 2026-05-29:** Per the ARCHIVE-01 hand-off contract, `/gsd-complete-milestone v1.7` was invoked post-Phase-70-close. The `gsd-sdk query milestone.complete` produced an `accomplishments` array of 14 entries — 11× literal `"One-liner:"` placeholder labels + 3 extraction-label fragments (`"ROADMAP SC#1 literal wording:"` / `"1. [Rule 3 - Blocking] In-session file revert during validation run"` / `"3 user-action checkpoints navigated:"`). `git diff HEAD -- .planning/MILESTONES.md` confirmed the garbage propagated to the v1.7 H2 entry verbatim. This is the **exact same cdcce23-class defect** that Plan 68-04 `d142c7a` deleted as a symptom fix in v1.7 Phase 68. Root cause **confirmed to live in** `.claude/commands/gsd-complete-milestone.md` (or the underlying `gsd-sdk query milestone.complete` SDK handler) — extraction-pattern logic that emits scripted-label fragments rather than per-plan SUMMARY one-liner content.

**Additional discovery surfaced 2026-05-29:** v1.6 entry was **entirely missing** from `.planning/MILESTONES.md` post-Plan-68-04 `d142c7a`. The previous symptom fix deleted lines 3-71 which included BOTH the cdcce23-inserted garbage AND any v1.6 placeholder content the archive script had emitted. v1.6 milestone completion via the same broken automation produced no salvageable content, and the symptom-fix deletion silently dropped the v1.6 H2 stub. **v1.6 entry retroactively authored 2026-05-29** during v1.7 milestone close per the "DO NOT mask via deletion — investigate the script" guidance (authoring correct content from `v1.6-MILESTONE-AUDIT.md` source-of-truth is replacement, not deletion).

**v1.7 milestone close response (per "DO NOT mask via deletion" guidance):** Authored v1.7 H2 entry manually in `.planning/MILESTONES.md` using `v1.7-MILESTONE-AUDIT.md` Executive Summary + 4-Pillar Closure Narrative + Methodology Highlights as source-of-truth. v1.6 H2 entry also retroactively authored from `v1.6-MILESTONE-AUDIT.md`. This is REPLACEMENT not DELETION — the garbage 14 lines are overwritten with content that accurately reflects the milestone's actual accomplishments.

**Status update:** v1.8+ root-cause investigation now has confirmed-active defect signature (not theoretical). Trigger-to-sweep promoted from "monitor for recurrence" to "fix or skill rewrite required before next milestone close." Sweep priority: **HIGH** (v1.8 close will produce same defect without intervention; manual authoring per-milestone is unsustainable carry-over).

---

## ARCHIVE-02: Pre-existing `One-liner:` placeholder residue in MILESTONES.md v1.2 section

**Scope:** Plan 68-04 surfaced 3 pre-existing `One-liner:` placeholder lines in `.planning/MILESTONES.md` v1.2 entry (lines 112/113/115 post-Plan-68-04 edit) from an EARLIER archive defect class (NOT cdcce23 — the v1.2 archive predates cdcce23 by several milestones).

**Source-of-truth:** Plan 68-04 SUMMARY §"Discoveries (out of scope — flagged for Plan 68-05 close-gate authoring)" + 68-04-SUMMARY.md §"Garbage Debris Elimination Confirmation" table footnote.

**Why deferred:** Out of Phase 68-04 scope per CONTEXT.md D-04 §"V-61-19 / V-61-20 are NOT content gaps" (only the cdcce23 garbage in the v1.5 entry was in scope). Bundled with ARCHIVE-01 for v1.8+ pickup since the defect class is the same (scripted-extraction debris from archive automation).

**Trigger-to-sweep (v1.8+):** When ARCHIVE-01 root-cause investigation lands, sweep ALL historical milestone entries (v1.0..v1.4.1) for similar residue and remediate in a coordinated commit. The v1.2 entry is the only known site today.

**Estimated effort:** 30min (sweep + 3-line surgical deletion + post-sweep validator re-run).

---

## HARNESS-FORWARD-01: v1.5-frozen-aware validator pattern carry-forward (Phase 70 application + v1.8+ retrospective)

**Scope:** Phase 68 Plan 68-03 Task 1 introduced the `git show ba2cbc0:.planning/REQUIREMENTS.md` mechanism in `check-phase-61.mjs` for V-61-01..04 (precondition commit `d7d7d5f`). This is a NEW architectural pattern: chain validators that assert on historical milestone state should read from frozen close-SHA via `execFileSync('git', ['show', '<SHA>:<path>'])` rather than from HEAD.

**Source-of-truth:** Plan 68-03 SUMMARY §Task 1 + `68-VERIFICATION.md` §Section F "v1.5-frozen-aware pattern as v1.7+ architectural precedent" + Plan 69-02 Fix-2 `2d61981` (`readRoadmapAtV15Close()` helper closing SCOPE-GAP-61).

**Rationale:** Post-milestone-close planning-doc reorganization (req migration to Validated section, active-section replacement for next milestone) breaks any validator that asserts on the structural shape of the active section as it existed at milestone close. HEAD-coupled validators surface FAILs after the next milestone's planning-doc edits, even though the milestone they're verifying never regressed. Worse, the FAILs may be suppressed by CHAIN_SKIP until a downstream phase tries to remove the suppression.

**Phase 70 application status (UPDATED at HARNESS-06 close):** applied-to-check-phase-67..70.mjs at Phase 70 Plan 70-01 Wave 1 scaffold + Plan 70-03 Wave 3 Atom 2 (per D-01 LOCKED Option C; per-V-NN-NN freshness routing matrix in 70-CONVENTIONS.md). Each of check-phase-67/68/70.mjs has v1.7-frozen-aware helpers (`readCorpusFileAtV17Close`, `readSidecarAtV17Close`, `readMilestoneAuditAtV17Close`, etc.) that read at `{phase_70_close_SHA}` placeholder via `execFileSync('git', ['show', ...])`. Plan 70-05 Commit A `14683de` substitutes the placeholder with Atom 2 SHA `aa6de68`.

**Retrospective audit for check-phase-{48..66}.mjs REMAINS deferred to v1.8+** per CONTEXT D-01 Option D REJECT + v1.7-DEFERRED-CLEANUP.md SCOPE-GAP-61 retrospective recommendation. Scope: scan check-phase-{48..66}.mjs for HEAD-coupled assertions whose docstrings cite milestone-close state. Class signature: any assertion whose validator name or docstring cites a milestone-close state but whose implementation reads HEAD via `readFile()`. Convert to v1.5/v1.6/v1.7-frozen-aware via SHA-pinned helpers parallel to `readRequirementsAtV15Close()` / `readRoadmapAtV15Close()` / `readCorpusFileAtV17Close()`.

**Trigger-to-sweep (v1.8+):** Bundled with SCOPE-GAP-61 retrospective + CHAIN-WRAPPER-01 fix into a v1.8+ chain-resilience pickup.

**Estimated effort (v1.8+ retrospective):** 2-4h for class-wide scan + per-validator conversion.

---

## TIMEOUT-01: Subprocess timeout 60s → 300s bundled in Plan 68-03 atomic commit `7b635ca` (CLOSED 2026-05-28)

**Scope:** With `CHAIN_SKIP = new Set([])`, `check-phase-66.mjs` must now recursively traverse the full chain {48..65} via `execFileSync('node', [path], { stdio: 'pipe', timeout })`. `check-phase-65.mjs` itself recursively traverses {48..64}, and standalone `time` measured `1m42.206s` (102 seconds) on the local Windows host — exceeding the original 60-second subprocess timeout.

**Source-of-truth:** Plan 68-03 SUMMARY §"Subprocess timeout bump (deviation Rule 3 auto-fix)" + `68-VERIFICATION.md` §Section E Discovery #2.

**Resolution (already landed in Plan 68-03):** Bumped `timeout: 60000` → `timeout: 300000` across 10 sites in 5 files (2 sites per file × 5 files: one for CHAIN regression-guards, one for v1.5-milestone-audit invocation). 300s budget gives ~3x headroom over 102s empirical max. Bundled into the atomic commit `7b635ca` since the timeout fix is part of the empty-Set transition's correctness requirement.

**Trigger-to-sweep:** Phase 69 CI-Linux first-run timing measurement. If ubuntu-latest runners are slower than the local Windows host's ~102s, even 300s may need additional headroom. v1.8+ may consider parallelizing chain execution or caching validator results, but neither is in v1.7 scope.

**Estimated effort (v1.8+):** 2-4h for parallel-execution refactor + cache-key invalidation logic; OR 30min for one-line timeout-budget extension if Phase 69 surfaces ubuntu-latest as slower.

**Phase 69 measured disposition (2026-05-28):** Linux runtime ~56s observed on B.1 iter 3 (run `26576405590`) — 54% of Windows ~102s reference; 5.4x headroom vs 300s subprocess cap; 32x headroom vs 1800s job cap; well within the 1500s escalation threshold for v1.8+ subprocess-timeout-architecture review. No v1.8+ work needed. **TIMEOUT-01 CLOSED 2026-05-28.**

**Phase 70 reaffirmation (2026-05-28):** Wave 4 Linux chain runtime 74s (Phase 69 ~56s baseline + 18s for Atom 2 additive validators) — still well within budget. CLOSED status preserved.

---

## FETCH-DEPTH-01: Shallow-clone broke v1.5-frozen-aware validators on GHA ubuntu-latest (CLOSED 2026-05-28)

**Scope:** Plan 68-03 Task 1 (commit `d7d7d5f`) introduced `readRequirementsAtV15Close()` in `scripts/validation/check-phase-61.mjs`: a helper that reads REQUIREMENTS.md state at the frozen v1.5-close SHA `ba2cbc0` via `execFileSync('git', ['show', 'ba2cbc0:.planning/REQUIREMENTS.md'])`. V-61-01..04 depend on this helper.

**Source-of-truth:** Plan 69-02 SUMMARY §B.1 iter 1 evidence + `69-VERIFICATION.md` §Section E Discovery #1 (FETCH-DEPTH-01).

**Root cause:** `actions/checkout@v4` defaults to `fetch-depth: 1` (shallow clone — only HEAD commit + tree present in runner's git object database). The historical SHA `ba2cbc0` is NOT resolvable from a shallow clone. `git show ba2cbc0:...` fails with `fatal: invalid object name 'ba2cbc0'.`. check-phase-61 exits 1; chain-66 cascades the failure through check-phase-{62..65} (each spawns full predecessor chain post-CHAIN-03 design).

**Evidence:**
- B.1 iter 1 run `26513528485` (https://github.com/Schweinehund/Autopilot/actions/runs/26513528485): FAIL `Result: 23 PASS, 5 FAIL, 0 SKIPPED` with `fatal: invalid object name 'ba2cbc0'` as proximate cause
- B.1 iter 3 run `26576405590` (https://github.com/Schweinehund/Autopilot/actions/runs/26576405590): SUCCESS `Result: 28 PASS, 0 FAIL, 0 SKIPPED` post-fix

**Resolution (Plan 69-02 Fix-1 commit `85521bb`):** Added `with: { fetch-depth: 0 }` to the `actions/checkout@v4` step in the `linux-chain-ubuntu-latest` job of `.github/workflows/audit-harness-v1.7-integrity.yml`. Pulls full git history (all commits + objects), making historical SHAs resolvable. Cost: ~5-20s extra checkout time on a ~3000-commit repo (vs ~2s shallow). Parse / path-match / harness-run jobs unchanged (don't need historical SHAs; default fetch-depth:1 preserved).

**Forward-coordination (Phase 70 HARNESS-04 inheritance contract — CLOSED):** Phase 70 HARNESS-04 EXTENDED the existing v1.7 workflow with v1.7-specific bits (v1.7 validator jobs check-phase-67..70 + v1.7 path-filter updates + v1.7 milestone-audit harness reference). The `fetch-depth: 0` setting on the `linux-chain-ubuntu-latest` job WAS preserved verbatim per HARNESS-04 9-edit spec a..i — any HARNESS-04 edit touching the checkout step left the `with: { fetch-depth: 0 }` line intact. Verified Wave 4 Plan 70-04 cross-OS run `26604414109` chain-apex 28/0/0 EXACT MATCH.

**Risk surface:** LOW. `fetch-depth: 0` is the standard GHA pattern for any workflow needing git history. v1.6 + v1.5 predecessor workflows unaffected (they don't reach check-phase-61).

**Status:** CLOSED 2026-05-28 (fix landed in-Phase as `85521bb`; Phase 70 HARNESS-04 preserved inheritance). No v1.8+ pickup required.

---

## SCOPE-GAP-61: V-61-05..08 left HEAD-coupled by Plan 68-03 Task 1 (CLOSED 2026-05-28)

**Scope:** Plan 68-03 Task 1 (commit `d7d7d5f`) extended `scripts/validation/check-phase-61.mjs` with `readRequirementsAtV15Close()` and rewired V-61-01..04 to use it (suffixed `[v1.5-frozen @ ba2cbc0]`). But V-61-05..08 — which assert v1.5-close §Progress state in ROADMAP.md — were left HEAD-coupled via `readFile(ROADMAP)`, even though their docstrings cite v1.5-close state.

**Source-of-truth:** Plan 69-02 SUMMARY §B.1 iter 2 evidence + `69-VERIFICATION.md` §Section E Discovery #2 (SCOPE-GAP-61).

**Attribution:** Plan 68-03 Task 1 **scope-gap closure**, NOT Phase 69 deliverable surface. The semantic class of V-61-05..08 (assertions on v1.5-close §Progress state) was the same as V-61-01..04 (assertions on v1.5-close §Active section state) — both should have been v1.5-frozen-aware. Plan 68-03 Task 1's pivot only covered the REQUIREMENTS.md path; the ROADMAP.md path was missed.

**Evidence:**
- B.1 iter 2 run `26574959797` (post-FETCH-DEPTH-01 fix): FAIL `Result: 23 PASS, 5 FAIL, 0 SKIPPED` with empty stderr (masked by CHAIN-WRAPPER-01)
- Standalone `node scripts/validation/check-phase-61.mjs` on Windows post-`85521bb` HEAD: `Result: 33 PASS, 1 FAIL, 0 SKIPPED` with `V-61-05 FAIL -- 1 In Progress rows remain`
- `git show ba2cbc0:.planning/ROADMAP.md | grep -c "In Progress"` → 0 (v1.5-close clean)
- `git show HEAD:.planning/ROADMAP.md | grep -c "In Progress"` → 1 (Plan 69-01 tracking-update `6e12a75` introduced)
- B.1 iter 3 run `26576405590`: SUCCESS `Result: 28 PASS, 0 FAIL, 0 SKIPPED` post-fix

**Resolution (Plan 69-02 Fix-2 commit `2d61981`):** Added `readRoadmapAtV15Close()` helper parallel to existing `readRequirementsAtV15Close()`; rewired V-61-05..08 to use it. Check names suffixed `[v1.5-frozen @ ba2cbc0]` to match V-61-01..04 naming convention. Same exact pattern as Plan 68-03 Task 1 `d7d7d5f` — same risk class (LOW), same proven mechanism.

**Retrospective audit recommendation (v1.8+):** Scan `check-phase-{48..66}.mjs` for other HEAD-coupled assertions whose semantics tie to milestone-close state. Class signature: any assertion whose validator name or docstring cites a milestone-close state (e.g., "4 stale rows reconciled per Plan 61-02", "deferred items LIN-DEFER-01..", "Methodology highlights from v1.5 close") but whose implementation reads HEAD via `readFile()`. Convert to v1.5/v1.6/v1.7-frozen-aware via SHA-pinned helpers parallel to `readRequirementsAtV15Close()` / `readRoadmapAtV15Close()`. v1.8+ retrospective audit recommended; not blocking Phase 70.

**Status:** CLOSED 2026-05-28 for the immediate V-61-05..08 surface (fix landed in-Phase as `2d61981`). Retrospective audit for V-NN-NN HEAD-coupled assertion class recommended for v1.8+ pickup (bundled with HARNESS-FORWARD-01 retrospective).

**Estimated effort (v1.8+ retrospective):** 2-4h for class-wide scan + per-validator conversion to v1.5/v1.6/v1.7-frozen-aware via SHA-pinned helpers.

---

## D-04-OVERSPEC-01: SC#5 D-04 synthetic-PR design over-spec (ACKNOWLEDGED 2026-05-28)

**Scope:** Plan 69-02 SC#5 D-04 design specified a synthetic CRLF-injected PR test (B.2) under the assumption that Phase 68 CHAIN-01 fix made check-phase-51 **reject** CRLF (regex updated to `\r?\n` to detect CRLF as a violation). Reality: Phase 68 CHAIN-01 Option B mechanism (commit `36a753d`) added `.replace(/\r\n/g, '\n')` to `readFile()` in check-phase-51.mjs:17, making the validator **CRLF-tolerant** (defensively normalizes inputs to LF before regex matching).

**Source-of-truth:** Plan 69-02 SUMMARY §B.2 evidence + `69-VERIFICATION.md` §Section E Discovery #3 (D-04-OVERSPEC-01).

**Evidence:**
- B.2 run `26577894505` (https://github.com/Schweinehund/Autopilot/actions/runs/26577894505) on PR #1 with CRLF-injected `docs/decision-trees/09-linux-triage.md`: SUCCESS `Result: 28 PASS, 0 FAIL, 0 SKIPPED` — check-phase-51 PASSED on CRLF-injected file
- Phase 68 CHAIN-01 fix source: `scripts/validation/check-phase-51.mjs:17` `readFileSync(abs, 'utf8').replace(/\r\n/g, '\n')` with inline comment `// CRLF normalization (CHAIN-01; mirrors check-phase-48.mjs:25)`

**Reframe (no code fix needed):** B.2-GREEN is the CORRECT outcome and positive evidence:
- Cross-OS verification confirms Phase 68 CHAIN-01 defense works on Linux
- The chain stays green even with CRLF injected — exactly what defense-in-depth means
- The synthetic-PR test changed semantic value from "negative regression test" to "positive cross-OS resilience confirmation"

SC#5 was satisfied via B.1+B.2+B.4 trio (workflow_dispatch baseline + synthetic CRLF PR positive resilience proof + Windows-vs-Linux chain-apex EXACT MATCH) rather than via B.2-as-negative-test + B.3-as-revert-positive-test pair. B.3 SKIPPED per reframe.

**Future SC#5 design guidance:** Synthetic regression tests must verify the defensive validator can actually fail under the test condition before assuming positive proof requires a negative case. For CRLF-tolerant validators, the positive case IS that the chain stays green under CRLF injection. Negative-case validators (those that would actually exit 1 on the injected condition) are a different class and require separate test design.

**Status:** ACKNOWLEDGED 2026-05-28; no fix needed (reality was right; design was over-spec). Documented for future-SC#5 design awareness.

**No trigger; no effort.**

---

## CHAIN-WRAPPER-01: check-phase-66.mjs:313 chain-apex wrapper stderr-only capture (DEFERRED to v1.8+)

**Scope:** `scripts/validation/check-phase-66.mjs` chain-apex wrapper at line 313 captures only `err.stderr` when a spawned validator fails:

```javascript
try {
  execFileSync('node', [path], { stdio: 'pipe', timeout: 300000 });
  return { pass: true, detail: 'exit 0' };
} catch (err) {
  return { pass: false, detail: `check-phase-${phaseNum} FAIL: ${(err.stderr || '').toString().trim()}` };
}
```

check-phase-61 (and most chain validators) writes per-validator FAIL detail to **stdout**, not stderr. The chain-66 wrapper truncates the diagnostic to empty stderr noise, masking the real failure cause in chain-apex output.

**Source-of-truth:** Plan 69-02 SUMMARY §SCOPE-GAP-61 forensic narrative + `69-VERIFICATION.md` §Section E Discovery #4 (CHAIN-WRAPPER-01).

**Impact:** Masked SCOPE-GAP-61 on Windows local for 2 weeks. Plan 69-01's tracking-update commit `6e12a75` was authored 2026-05-27; SCOPE-GAP-61 should have surfaced immediately on the next chain-66 run, but the stderr-only wrapper hid the V-61-05 detail. Only the GHA workflow_dispatch run B.1 iter 2 made the failure visible (because the GHA log shows the full job output, including chain-66's empty-stderr noise + the underlying check-phase-61's standalone diagnostic). On Windows local, the chain-66 output looked like a generic chain failure rather than a specific V-61-05 failure.

**Recommendation (v1.8+):** Fix wrapper to capture both stderr AND stdout, preserving diagnostic clarity in chain-apex output:

```javascript
} catch (err) {
  const out = ((err.stderr || '').toString() + (err.stdout || '').toString()).trim();
  return { pass: false, detail: `check-phase-${phaseNum} FAIL: ${out}` };
}
```

**Trigger-to-sweep (v1.8+):** Any future chain-apex masking incident OR v1.8+ chain-wrapper architecture review. Combined with SCOPE-GAP-61 retrospective audit for V-NN-NN HEAD-coupled assertion class + HARNESS-FORWARD-01 retrospective — all three belong to the same v1.8+ chain-resilience pickup.

**Status:** DEFERRED to v1.8+ per scope discipline. CILINUX-01 scope is workflow YAML, not chain wrapper. Surfaced as Phase 69 forensic discovery for v1.8+ pickup. Phase 70 reaffirms deferral; same scope discipline.

**Estimated effort:** 30min surgical wrapper edit + chain-apex regression sweep to confirm no false positives.

---

## CHAIN-31: check-phase-31.mjs silent-swallow data-integrity bug (CLOSED)

**Scope:** Pre-existing bug where `parseInventory()` at `scripts/validation/check-phase-31.mjs:32-33` silently returned `{ placeholders: [] }` on missing `placeholder-inventory.json` instead of failing. V-31-21 + V-31-24 callers silently PASSED on empty array → data-integrity bug.

**Status:** CLOSED in Phase 68 Plan 68-02 Wave 2 (SHA `79c65c6`) per D-02 advisor §4.7 STRETCH. Closure mechanism: `parseInventory()` now uses `resolveArchivedPhasePath('31-ios-l2-investigation/placeholder-inventory.json', ['v1.3-phases'])` and returns `{ _missing: true, placeholders: [] }` marker on missing file; V-31-21 + V-31-24 callers updated to FAIL on `_missing` (mirrors existing `_parseError` discriminator pattern). V-31-21 + V-31-24 continue to PASS post-fix because helper resolves the file at archived `.planning/milestones/v1.3-phases/31-ios-l2-investigation/placeholder-inventory.json`.

Recorded here for audit-trail completeness — **no v1.8+ pickup required.**

**No trigger; no effort.**

---

## VPP-location-token additional sites (Phase 67 SWEEP-02 discovery; DEFERRED to v1.8+)

**Scope:** Phase 67 Plan 67-02 SWEEP-02 (atomic commit `55260b3`) renamed 6 calibrated VPP-location-token occurrences per `REQUIREMENTS.md:14` + 67-CONTEXT.md line 9 (the v1.6-DEFERRED-CLEANUP.md `ci_2_vpp_location_token` 6-entry enumeration). During Plan 67-03 close-gate inspection + 67-VERIFICATION.md verifier cross-check, additional VPP-location-token sites surfaced **out of Phase 67 calibrated scope**:

- 3 additional VPP-location-token sites in `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` (lines 115, 149, 155)
- 1 additional VPP-location-token site noted in 67-VERIFICATION.md verifier cross-check observations (4th occurrence at L160; total 4 beyond calibrated scope)

**Source-of-truth:** Phase 67 Plan 67-03 SUMMARY §Discoveries + `67-VERIFICATION.md` Verifier Cross-Check Observations section + STATE.md:149 Out-of-band carry-over.

**Why deferred:** Out of Phase 67 calibrated 6-occurrence scope per `REQUIREMENTS.md:14` + 67-CONTEXT.md line 9 (the v1.6-DEFERRED-CLEANUP.md `ci_2_vpp_location_token` enumeration). Bundle into v1.8+ Pillar A-equivalent sweep with CI-3 (Managed Apple ID).

**Trigger-to-sweep (v1.8+):** Pillar A-equivalent corpus sweep schedule. Combined with CI-3 + 60-day re-verification cycles.

**Estimated effort:** 30min surgical rename (mirrors Plan 67-02 atomic-within-plan pattern: 4 D-03 line rewrites + 4 Version History rows + sidecar annotation; PITFALL-6 anchor inventory).

---

## CI-3: Managed Apple ID Corpus Rename (DEFERRED to v1.8+)

**Scope:** Managed Apple ID → Managed Apple Account corpus-wide rename. v1.6 Phase 66-03 commit `c7a3973` calibrated 45 occurrences across 16 files (`v1.6-DEFERRED-CLEANUP.md:62-92` enumeration). Apple has renamed "Managed Apple ID" to "Managed Apple Account" as part of the Apple Business rebrand, but Microsoft Intune portal still labels the field "Managed Apple ID" as of 2026-04-30 tutorial refresh.

**Source-of-truth:** `v1.6-DEFERRED-CLEANUP.md:62-92` enumeration; `STATE.md:141` Intune-portal label notation; CONTEXT D-22 Q5(b) no-corpus-sweep contract preserved.

**Rationale for deferral:** Sweeping the 45 occurrences across 16 files now would create term-mismatch between docs and admin's actual portal experience. Admin reads docs saying "Managed Apple Account" but sees "Managed Apple ID" in their Intune portal — confusion. Wait until Microsoft Intune adopts the rebrand portal-side.

**Affected files (v1.6 enumeration; carry-forward to v1.8+):**
- `docs/_glossary-apple-business.md` — Managed Apple ID entry (rebrand-mapping table)
- `docs/admin-setup-macos/01-abm-configuration.md` — multiple references
- `docs/admin-setup-macos/02-vpp-setup.md` — references
- `docs/admin-setup-ios/02-abm-token.md` — references
- `docs/admin-setup-ios/03-ade-enrollment.md` — references
- `docs/admin-setup-ios/05-app-deployment.md` — references
- `docs/cross-platform/apple-business/01-role-permission-model.md` — references
- `docs/cross-platform/apple-business/03-ous-vs-custom-roles.md` — references
- `docs/cross-platform/apple-business/05-sub-org-admin-onboarding.md` — references
- `docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md` — references
- `docs/cross-platform/apple-business/16-managed-apple-account-runbook.md` — references
- `docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` — references
- `docs/l2-runbooks/26-apple-business-permission-denied.md` — references
- `docs/_glossary-macos.md` — see-also entry
- `docs/_glossary.md` — see-also entry
- `docs/quick-ref-l1.md` — Apple Business Quick Reference section
- Total: 45 occurrences across 16 files per `v1.6-DEFERRED-CLEANUP.md:62-92`

**Trigger-to-sweep (v1.8+):** Microsoft Intune adopts the rebrand portal-side as of the next quarterly tutorial refresh post-2026-07-01. Manual trigger check: visit the Intune admin center → Devices → Apple → ABM/ASM connector configuration → check if the field is labeled "Managed Apple Account" or still "Managed Apple ID".

**Estimated effort:** 2-3h (corpus sweep + 16 Version History rows + glossary update + sidecar reset + harness re-run + chain validator re-runs).

---

## Multi-tenant Apple Business surfaces (DEFERRED indefinitely)

**Scope:** Multi-tenant Apple Business workflows (one organization managing multiple distinct ABM/ASM tenants; cross-tenant device transfer; multi-org-account scenarios where a single Managed Apple Account spans multiple Apple Business OUs across tenants).

**Source-of-truth:** v1.6 Q2 explicit scope-out per CONTEXT.md `D-A1` boundary; `v1.6-DEFERRED-CLEANUP.md` Multi-tenant section; v1.7 inherits the boundary per REQUIREMENTS.md:67 Out-of-Scope row.

**Rationale for deferral:** v1.6 + v1.7 explicitly scoped to single-tenant Apple Business governance. Multi-tenant requires:
- New architectural decisions (cross-tenant identity model; cross-tenant audit log scoping; cross-tenant VPP license accounting)
- New C16-equivalent cross-link surfaces (multi-tenant runbooks would need their own integrity triangle)
- Significantly expanded planning surface
- This is not cleanup work — it's net-new architectural scope

**Trigger-to-sweep (v1.8+):** Multi-org-account scenarios surface from customer support (acute customer demand for documented multi-tenant workflows). Until then, deferred indefinitely.

**Estimated effort:** 6-12 plans across 2-3 phases (architectural decisions + admin setup + delegation runbooks + L1/L2 + capability matrix updates + harness extensions).

---

## Apple Business Device API documentation (DEFERRED to v1.8+)

**Scope:** Document the Apple Business Device API (the programmatic interface to Apple Business Manager for device provisioning, OU assignment, license management, audit log retrieval). Currently acknowledge-but-do-not-document-deeply in `01-role-permission-model.md`.

**Source-of-truth:** v1.6 Phase 62 Plan 62-04 commit `b4aaafd` (AB-03) acknowledge-but-do-not-document-deeply note; `v1.6-DEFERRED-CLEANUP.md` Apple Business Device API section.

**Rationale for deferral:** Apple has not yet published developer.apple.com landing for the Apple Business Device API. Without official developer documentation, deep technical documentation would be speculative or based on reverse-engineering. Acknowledge-but-do-not-document-deeply protects against drift when Apple publishes.

**Trigger-to-sweep (v1.8+):** Apple publishes developer.apple.com landing for Apple Business Device API. Cron-monitor `https://developer.apple.com/documentation/applebusiness/` for landing-page existence.

**Estimated effort:** 1-2 plans (API reference doc + integration patterns + L2 troubleshooting + harness C17-equivalent for API-call patterns).

---

## Per-OU Conference Room Display deep-dive (TRACKER ONLY)

**Scope:** Per-OU Conference Room Display (CRD) partitioning — separate Apple TV CRD configurations per Organizational Unit, with OU-scoped content tokens + OU-scoped MDM server assignments + OU-scoped audit log retention.

**Source-of-truth:** v1.6 Phase 63 Plan 63-04 commit `b3adba9` (OU-08 Apple TV lifecycle with shared-physical-space heuristic OP-15); `v1.6-DEFERRED-CLEANUP.md` Per-OU CRD section.

**Rationale for tracker-only status:** Apple deployment guides are thin on per-OU CRD partitioning specifics. v1.6 OU-08 documented the shared-physical-space heuristic for CRD assignment, but did NOT cover per-OU partitioning patterns. No acute customer demand surfaced during v1.6 or v1.7.

**Trigger-to-sweep (v1.8+):** Acute customer demand for per-OU CRD partitioning surfaces (multi-tenant deployment scenarios; large enterprise with strict OU-boundary requirements for shared-physical-space devices).

**Estimated effort:** 1 plan (per-OU CRD configuration patterns + L2 troubleshooting addendum to L2 #26 Apple Business permission-denied tree).

---

## Account Holder lockout dedicated recovery runbook (CONDITIONAL — currently in L2 #26)

**Scope:** Standalone L1/L2 runbook for Account Holder lockout recovery. Currently handled as a 7-leaf branch in L2 #26 Apple Business permission-denied decision tree per Phase 65 D-02 hybrid.

**Source-of-truth:** v1.6 Phase 65 Plan 65-02 commit `699029a` (ABNAV-02 L2 #26 7-leaf Mermaid decision tree with Account-Holder-lockout leaf); `v1.6-DEFERRED-CLEANUP.md` Account Holder section.

**Rationale for conditional status:** v1.6 D-02 hybrid promoted Account Holder lockout to the 7th leaf of L2 #26 decision tree, on the rationale that:
- Account Holder lockouts are rare (1 per tenant per several months at most)
- 7-leaf decision tree handles the discriminator logic
- Standalone runbook would duplicate decision-tree logic

If customer support escalations surface (e.g., 5+ Account Holder lockout escalations per month), promote to standalone L1 #35 / L2 #27 runbook with dedicated procedure (Account Recovery sequence + Apple Support contact + cross-tenant Account Holder transfer if applicable).

**Trigger-to-sweep (v1.8+):** Customer support escalations surface. Quantitative threshold: 5+ escalations per quarter.

**Estimated effort:** 1 plan (standalone L1 #35 runbook + L2 #27 runbook + cross-link from L2 #26 7-leaf decision tree).

---

## Apple School Manager (ASM) education-specific surfaces (DEFERRED to v1.8+)

**Scope:** Apple School Manager (ASM) is Apple's identity / device / content management portal for K-12 + higher education tenants. Distinct from Apple Business Manager (ABM) but shares overlapping APIs + concepts (Managed Apple Account, OUs, VPP content tokens). ASM-specific surfaces include: student/teacher/staff role distinctions; classroom-mode device assignment; iPad sharing for education (shared iPad for classroom); School Configurator iPad pairing.

**Source-of-truth:** v1.6 Q1 explicit scope-out per CONTEXT.md (`v1.6-DEFERRED-CLEANUP.md` ASM section); v1.7 REQUIREMENTS.md:67 Out-of-Scope row enterprise-only.

**Rationale for deferral:** Outside enterprise scope. v1.0..v1.7 has consistently scoped to enterprise (corporate IT) deployments. Education-tenant scope requires separate planning + different audience model (school district IT vs corporate IT differ in role hierarchies + procurement + compliance frameworks).

**Trigger-to-sweep (v1.8+):** Education-tenant scope explicitly opens (customer demand from K-12 / higher-ed deployments OR product strategy decision to expand into education market segment).

**Estimated effort:** 6-12 plans across 2-3 phases (ASM glossary + admin setup + role-permission model + classroom-mode + shared iPad for classroom + School Configurator + L1/L2 + capability matrix updates).

---

## Cross-References

- **Phase 68 close-gate verification:** `.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-VERIFICATION.md`
- **Phase 69 close-gate verification:** `.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md`
- **Phase 70 close-gate verification:** `.planning/phases/70-v1-7-audit-harness-lineage-bump-milestone-close-pillar-d-clo/70-VERIFICATION.md`
- **v1.7 milestone audit:** `.planning/milestones/v1.7-MILESTONE-AUDIT.md`
- **v1.6 predecessor:** `.planning/milestones/v1.6-DEFERRED-CLEANUP.md` (authored Phase 66-03 commit `c7a3973`)
- **v1.5-frozen-aware REQUIREMENTS mechanism source:** `scripts/validation/check-phase-61.mjs` `readRequirementsAtV15Close()` helper (introduced Plan 68-03 Task 1 SHA `d7d7d5f`)
- **v1.5-frozen-aware ROADMAP mechanism source:** `scripts/validation/check-phase-61.mjs` `readRoadmapAtV15Close()` helper (introduced Plan 69-02 Fix-2 SHA `2d61981`; parallel to REQUIREMENTS helper; closes SCOPE-GAP-61)
- **v1.7-frozen-aware mechanism source:** `scripts/validation/check-phase-67.mjs` / `check-phase-68.mjs` / `check-phase-70.mjs` (introduced Plan 70-01 Wave 1 scaffold + populated at Plan 70-03 Atom 2 commit `aa6de68`; SHA placeholder substituted at Plan 70-05 Commit A `14683de`)
- **Archive-path helper source:** `scripts/validation/_lib/archive-path.mjs` (introduced Plan 68-02 SHA `79c65c6`)
- **Atomic-commit precedent:** Phase 66-02 commit `3a9a671` (Plan 68-03 Task 2 SHA `7b635ca` + Plan 70-02 Atom 1 `26a1ae9` + Plan 70-03 Atom 2 `aa6de68` inherit this pattern)
- **REQUIREMENTS.md frozen reference SHA:** `ba2cbc0` (v1.5-close — used by V-61-01..04 v1.5-frozen-aware helper)
- **ROADMAP.md frozen reference SHA:** `ba2cbc0` (v1.5-close — used by V-61-05..08 v1.5-frozen-aware helper post-SCOPE-GAP-61 closure)
- **fetch-depth:0 GHA pattern source:** `.github/workflows/audit-harness-v1.7-integrity.yml` linux-chain-ubuntu-latest checkout step (introduced Plan 69-02 Fix-1 SHA `85521bb`; closes FETCH-DEPTH-01; Phase 70 HARNESS-04 inheritance preserved per atomic commit `aa6de68`)
- **ARCHIVE-01 hand-off destination:** `/gsd-complete-milestone` skill (post-Phase-70-close milestone archival; runs `git diff HEAD~1 HEAD -- .planning/MILESTONES.md` to detect cdcce23-class recurrence per ARCHIVE-01 entry guidance)

---

*v1.7 deferred-cleanup stub authored 2026-05-26 at Phase 68 close. Extended 2026-05-28 at Phase 69 close: TIMEOUT-01 CLOSED + FETCH-DEPTH-01 CLOSED + SCOPE-GAP-61 CLOSED + D-04-OVERSPEC-01 ACKNOWLEDGED + CHAIN-WRAPPER-01 DEFERRED to v1.8+. **Finalized 2026-05-28 at Phase 70 HARNESS-06 close (Plan 70-05 Commit B):** v1.6 carry-overs promoted from stub to full sections (CI-3 + Multi-tenant + Apple Business Device API + per-OU CRD + Account Holder + ASM) + Phase 67 VPP-additional discoveries appended + Phase 69 final dispositions reaffirmed + HARNESS-FORWARD-01 status updated (applied to check-phase-67..70.mjs at Phase 70 Plan 70-01/70-03; retrospective audit of check-phase-{48..66}.mjs REMAINS deferred to v1.8+) + ARCHIVE-01 hand-off to /gsd-complete-milestone explicitly documented. v1.8+ entry-phase planning reads this artifact as the single-source-of-truth deferred backlog.*
