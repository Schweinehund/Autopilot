---
phase: 08-reference-anchors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reference/registry-paths.md
  - docs/_glossary.md
autonomous: true
gap_closure: true
requirements:
  - FOUND-01
  - FOUND-02
must_haves:
  truths:
    - "registry-paths.md contains a Winlogon row with the same 4 columns as existing rows"
    - "registry-paths.md has resolvable anchors for #autopilotsettings, #provisioning-diagnostics, and #winlogon"
    - "_glossary.md has ### Entra and ### Intune headings so #entra and #intune inbound links resolve"
  artifacts:
    - path: "docs/reference/registry-paths.md"
      provides: "Winlogon entry and 3 inline HTML anchors"
      contains: "Windows NT\\\\CurrentVersion\\\\Winlogon"
    - path: "docs/_glossary.md"
      provides: "Entra and Intune defined-term headings"
      contains: "### Entra"
  key_links:
    - from: "docs/error-codes/03-esp-enrollment.md"
      to: "docs/reference/registry-paths.md#winlogon"
      via: "fragment anchor"
      pattern: 'id="winlogon"'
    - from: "docs/error-codes/*.md"
      to: "docs/_glossary.md#entra"
      via: "fragment anchor on heading"
      pattern: "### Entra"
---

<objective>
Close 3 foundation-reference tech debt items from the v1.0 milestone audit: add the missing Winlogon registry entry, add inline HTML anchors to registry-paths.md so inbound fragment links resolve, and add Entra/Intune term headings to _glossary.md.

Purpose: Fix 9+ inbound anchor links (3 to registry-paths.md, 6 to _glossary.md) that currently fall back to file top, degrading the L1/L2 reading experience.
Output: Updated registry-paths.md (with Winlogon row + 3 anchors) and _glossary.md (with Entra + Intune headings).
</objective>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
@docs/reference/registry-paths.md
@docs/_glossary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Winlogon entry and inline anchors to registry-paths.md</name>
  <files>docs/reference/registry-paths.md</files>
  <read_first>
    - docs/reference/registry-paths.md (the file being modified)
    - docs/error-codes/03-esp-enrollment.md (to confirm the #winlogon inbound link text)
  </read_first>
  <action>
    Make two edits to `docs/reference/registry-paths.md`:

    1. **Add 3 inline HTML anchors in existing table rows.** Since GFM does not create anchors for table cells but DOES honor inline HTML, prepend an `<a id="...">` tag inside the first table cell for three existing rows. Edit the table as follows (only the Registry Path column of these 3 rows changes):

       - Row currently starting with `` | `HKLM:\SOFTWARE\Microsoft\Provisioning\Diagnostics\Autopilot` `` becomes:
         `` | <a id="provisioning-diagnostics"></a>`HKLM:\SOFTWARE\Microsoft\Provisioning\Diagnostics\Autopilot` ``
       - Row currently starting with `` | `HKLM:\SOFTWARE\Microsoft\Provisioning\AutopilotSettings` `` becomes:
         `` | <a id="autopilotsettings"></a>`HKLM:\SOFTWARE\Microsoft\Provisioning\AutopilotSettings` ``

    2. **Add a new Winlogon row** at the end of the table (after the ExpectedPolicies/Sidecar rows). The row MUST include an inline `<a id="winlogon"></a>` anchor and use the same 4 columns as existing rows (Registry Path | Purpose | Referenced By | Notes):

       ```
       | <a id="winlogon"></a>`HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon` | AutoAdminLogon and DefaultDomainName settings investigated during ESP policy conflict analysis; Interactive Logon Message banner values (LegalNoticeCaption, LegalNoticeText) also set here | `l2-runbooks/05-policy-conflicts.md` (Steps 2 and 6), `error-codes/03-esp-enrollment.md` | MEDIUM confidence — referenced by L2 policy conflict runbook for AutoAdminLogon investigation |
       ```

    Do NOT restructure the table, do NOT add section headings, do NOT modify any other rows. The anchor approach is non-invasive so the single-table layout is preserved.
  </action>
  <verify>
    <automated>grep -q 'id="winlogon"' docs/reference/registry-paths.md && grep -q 'id="autopilotsettings"' docs/reference/registry-paths.md && grep -q 'id="provisioning-diagnostics"' docs/reference/registry-paths.md && grep -q 'Windows NT\\CurrentVersion\\Winlogon' docs/reference/registry-paths.md</automated>
  </verify>
  <acceptance_criteria>
    - grep finds `id="winlogon"` in docs/reference/registry-paths.md (exactly once)
    - grep finds `id="autopilotsettings"` in docs/reference/registry-paths.md (exactly once)
    - grep finds `id="provisioning-diagnostics"` in docs/reference/registry-paths.md (exactly once)
    - grep finds the literal string `Windows NT\CurrentVersion\Winlogon` in docs/reference/registry-paths.md
    - grep finds the literal string `l2-runbooks/05-policy-conflicts.md` in docs/reference/registry-paths.md (the Referenced By column of the new row)
    - Total table row count (lines starting with `| \`HKLM` or `| <a id`) increased by exactly 1 compared to the original 9 data rows (now 10)
    - No existing table row content is altered except for the 2 rows receiving inline anchors
  </acceptance_criteria>
  <done>
    registry-paths.md has a Winlogon entry with the same 4-column schema as existing rows; three inline HTML id anchors (winlogon, autopilotsettings, provisioning-diagnostics) are present and non-disruptive to table rendering.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Entra and Intune term headings to _glossary.md</name>
  <files>docs/_glossary.md</files>
  <read_first>
    - docs/_glossary.md (the file being modified)
    - docs/error-codes/01-mdm-enrollment.md (to confirm how Entra/Intune are referenced in inbound links)
  </read_first>
  <action>
    Add two new term definition headings to `docs/_glossary.md`. GFM auto-generates lowercase anchor IDs from H3 heading text (`### Entra` → `#entra`, `### Intune` → `#intune`), so no inline HTML is needed.

    1. **Add `### Intune` under the existing `## Enrollment` section**, immediately after the existing `### MDM` subsection (before `### MDM enrollment`). Use this exact content:

       ```
       ### Intune

       Microsoft Intune — Microsoft's cloud MDM service and the concrete implementation of MDM used by Windows Autopilot to deliver policies, apps, and configuration profiles to enrolled devices.
       ```

    2. **Add `### Entra` under the existing `## Deployment Modes` section**, immediately before the existing `### Hybrid join` subsection. Use this exact content:

       ```
       ### Entra

       Microsoft Entra ID — the current product name for Azure Active Directory (renamed July 2023). The identity service that authenticates users and devices during Autopilot enrollment and stores device objects created by hybrid join and Autopilot registration.
       ```

    3. **Update the Alphabetical Index line** (line 14) to include both new terms in alphabetical order. Insert `[Entra](#entra) | ` between `[Device phase](#device-phase)` and `[ESP](#esp)` (Entra sorts before ESP because "En-" < "Es-"), and insert `[Intune](#intune) | ` between `[Hybrid join](#hybrid-join)` and `[MDM](#mdm)`.

    Do NOT restructure existing sections. Do NOT modify any other term definitions.
  </action>
  <verify>
    <automated>grep -q '^### Entra$' docs/_glossary.md && grep -q '^### Intune$' docs/_glossary.md && grep -qE '\[Device phase\]\(#device-phase\) \| \[Entra\]\(#entra\) \| \[ESP\]\(#esp\)' docs/_glossary.md && grep -q '\[Intune\](#intune)' docs/_glossary.md</automated>
  </verify>
  <acceptance_criteria>
    - grep finds `### Entra` as a standalone line (H3 heading) in docs/_glossary.md exactly once
    - grep finds `### Intune` as a standalone line (H3 heading) in docs/_glossary.md exactly once
    - grep finds the pattern `\[Device phase\]\(#device-phase\) \| \[Entra\]\(#entra\) \| \[ESP\]\(#esp\)` in the Alphabetical Index line (confirms Entra is positioned between Device phase and ESP, which is alphabetically correct)
    - grep finds `[Intune](#intune)` in the Alphabetical Index line
    - grep finds the literal string `Microsoft Entra ID` in the Entra definition
    - grep finds the literal string `Microsoft Intune` in the Intune definition
    - No existing H3 headings are removed or renamed
  </acceptance_criteria>
  <done>
    _glossary.md contains `### Entra` and `### Intune` defined-term headings in sensible sections, and the Alphabetical Index links to both in the correct alphabetical position (Entra before ESP). Inbound `#entra` and `#intune` fragment links from error-code files now resolve to specific term definitions instead of file top.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run from the repo root:

```bash
grep -c 'id="winlogon"\|id="autopilotsettings"\|id="provisioning-diagnostics"' docs/reference/registry-paths.md  # expect 3
grep -c '^### Entra$\|^### Intune$' docs/_glossary.md  # expect 2
grep -q 'Windows NT\\CurrentVersion\\Winlogon' docs/reference/registry-paths.md && echo "Winlogon row OK"
```

Manually verify registry-paths.md still renders as a single table (no broken rows), and the glossary Alphabetical Index line still has balanced `|` separators.
</verification>

<success_criteria>
- registry-paths.md has 10 data rows (was 9) including the new Winlogon row
- 3 inline HTML id anchors present in registry-paths.md
- _glossary.md has 2 new H3 headings (Entra, Intune) and updated Alphabetical Index
- ROADMAP Phase 8 success criteria 1, 2, and 3 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-reference-anchors/08-01-SUMMARY.md` documenting: exact rows/headings added, any deviations from planned content, and grep verification output.
</output>
