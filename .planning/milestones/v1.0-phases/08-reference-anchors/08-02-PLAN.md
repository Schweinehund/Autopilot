---
phase: 08-reference-anchors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/error-codes/01-mdm-enrollment.md
  - docs/error-codes/02-tpm-attestation.md
  - docs/error-codes/03-esp-enrollment.md
  - docs/error-codes/04-pre-provisioning.md
  - docs/error-codes/05-hybrid-join.md
autonomous: true
gap_closure: true
requirements:
  - ERRC-01
must_haves:
  truths:
    - "Every fragment link in error-codes/00-index.md Quick Lookup table resolves to a specific anchor in the target category file"
    - "All 23 hex-code fragments and 6 event-ID fragments from 00-index.md have matching id anchors in the correct category files"
  artifacts:
    - path: "docs/error-codes/01-mdm-enrollment.md"
      provides: "10 inline HTML id anchors for hex codes"
      contains: 'id="0x80180005"'
    - path: "docs/error-codes/02-tpm-attestation.md"
      provides: "8 inline HTML id anchors for hex codes"
      contains: 'id="0x80070490"'
    - path: "docs/error-codes/03-esp-enrollment.md"
      provides: "3 inline HTML id anchors for hex codes"
      contains: 'id="0x80004005"'
    - path: "docs/error-codes/04-pre-provisioning.md"
      provides: "1 inline HTML id anchor for hex code"
      contains: 'id="0xc1036501"'
    - path: "docs/error-codes/05-hybrid-join.md"
      provides: "1 hex anchor and 6 event-ID anchors"
      contains: 'id="event-807"'
  key_links:
    - from: "docs/error-codes/00-index.md"
      to: "docs/error-codes/01-mdm-enrollment.md#0x80180005"
      via: "inline HTML id anchor in table row"
      pattern: 'id="0x80180005"'
    - from: "docs/error-codes/00-index.md"
      to: "docs/error-codes/05-hybrid-join.md#event-807"
      via: "inline HTML id anchor in table row"
      pattern: 'id="event-807"'
---

<objective>
Close the ERRC-01 index anchor tech debt item: 29 fragment IDs in `error-codes/00-index.md` Quick Lookup table target rows in 5 category files, but GFM does not create anchors for table rows, so all 29 inbound links currently resolve to the file top of the wrong page.

Purpose: Make the master error-code index actually jump the reader to the specific error row instead of dumping them at the top of a long category file.
Output: 5 category files with inline HTML `<a id="...">` anchors placed immediately before each referenced error row in the existing error tables.

Approach: Use inline HTML anchors (non-invasive). GFM allows raw HTML in Markdown; `<a id="0x80180005"></a>` placed before or inside the first cell of a table row creates a valid jump target without restructuring the table.
</objective>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@docs/error-codes/00-index.md
@docs/error-codes/01-mdm-enrollment.md
@docs/error-codes/02-tpm-attestation.md
@docs/error-codes/03-esp-enrollment.md
@docs/error-codes/04-pre-provisioning.md
@docs/error-codes/05-hybrid-join.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add anchors to MDM enrollment and TPM attestation category files</name>
  <files>
    docs/error-codes/01-mdm-enrollment.md
    docs/error-codes/02-tpm-attestation.md
  </files>
  <read_first>
    - docs/error-codes/00-index.md (source of truth for required fragment IDs — use Ctrl+F on `#0x` and `#event-` to build the exact list)
    - docs/error-codes/01-mdm-enrollment.md (file being modified — locate each error row before editing)
    - docs/error-codes/02-tpm-attestation.md (file being modified — locate each error row before editing)
  </read_first>
  <action>
    For EACH error code listed below, locate the row in the target file's error table where the first cell contains that exact hex code (e.g., `` | `0x80180005` | ... ``). Prepend an inline HTML anchor to the first cell. The edited row's first cell MUST become:

    `` | <a id="{EXACT_CODE}"></a>`{EXACT_CODE}` | ``

    Where `{EXACT_CODE}` is the hex code including `0x` prefix, using the EXACT case shown in 00-index.md (this matters for `0x801C03F3` — uppercase preserved).

    **File 1: docs/error-codes/01-mdm-enrollment.md** — add 10 anchors for these codes (each code appears in the error table; 0x80180014 may have two rows for the two-cause pattern per STATE.md — add the anchor ONLY to the first occurrence row):
    - `0x8007064c`
    - `0x80180005`
    - `0x8018000a`
    - `0x80180014`
    - `0x80180018`
    - `0x80180019`
    - `0x80180020`
    - `0x80180022`
    - `0x80180026`
    - `0x8018002b`

    **File 2: docs/error-codes/02-tpm-attestation.md** — add 8 anchors for these codes:
    - `0x80070490`
    - `0x800705b4`
    - `0x80190190`
    - `0x801c03ea`
    - `0x801C03F3` (uppercase — preserve as-is)
    - `0x81039001`
    - `0x81039023`
    - `0x81039024`

    Do NOT change any other table cells. Do NOT add section headings. Do NOT reorder rows. If a hex code does not appear in the target file (because it was moved or renamed), STOP and report — do not guess.
  </action>
  <verify>
    <automated>for code in 0x8007064c 0x80180005 0x8018000a 0x80180014 0x80180018 0x80180019 0x80180020 0x80180022 0x80180026 0x8018002b; do grep -q "id=\"$code\"" docs/error-codes/01-mdm-enrollment.md || { echo "MISSING: $code"; exit 1; }; done && for code in 0x80070490 0x800705b4 0x80190190 0x801c03ea 0x801C03F3 0x81039001 0x81039023 0x81039024; do grep -q "id=\"$code\"" docs/error-codes/02-tpm-attestation.md || { echo "MISSING: $code"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep finds all 10 `id="{code}"` strings in docs/error-codes/01-mdm-enrollment.md exactly as listed
    - grep finds all 8 `id="{code}"` strings in docs/error-codes/02-tpm-attestation.md with exact case preserved (including `0x801C03F3` uppercase)
    - Each anchor appears inline in a table row's first cell: `| <a id="{code}"></a>\`{code}\` |`
    - Row count of the error tables in both files is unchanged from pre-edit
    - No existing backtick-wrapped code cells are removed
  </acceptance_criteria>
  <done>
    Every hex-code fragment ID from 00-index.md that routes to 01-mdm-enrollment.md or 02-tpm-attestation.md now resolves to the specific error row via inline HTML anchor.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add anchors to ESP, pre-provisioning, and hybrid join category files</name>
  <files>
    docs/error-codes/03-esp-enrollment.md
    docs/error-codes/04-pre-provisioning.md
    docs/error-codes/05-hybrid-join.md
  </files>
  <read_first>
    - docs/error-codes/00-index.md
    - docs/error-codes/03-esp-enrollment.md
    - docs/error-codes/04-pre-provisioning.md
    - docs/error-codes/05-hybrid-join.md
  </read_first>
  <action>
    Same approach as Task 1: prepend `<a id="{code}"></a>` to the first cell of each referenced error row. For event-ID fragments, use `event-{number}` (lowercase, hyphenated) as the id, matching 00-index.md.

    **File 1: docs/error-codes/03-esp-enrollment.md** — add 3 hex anchors:
    - `0x80004005`
    - `0x81036501`
    - `0x81036502`

    First cell pattern: `` | <a id="0x80004005"></a>`0x80004005` | ``

    **File 2: docs/error-codes/04-pre-provisioning.md** — add 1 hex anchor:
    - `0xc1036501`

    First cell pattern: `` | <a id="0xc1036501"></a>`0xc1036501` | ``

    **File 3: docs/error-codes/05-hybrid-join.md** — add 1 hex anchor and 6 event-ID anchors:
    - Hex: `0x80070774`
    - Events (first cell contains e.g. `Event 171` or `171`): `event-171`, `event-172`, `event-807`, `event-809`, `event-815`, `event-908`

    For the hex row, first cell becomes: `` | <a id="0x80070774"></a>`0x80070774` | ``

    For each event row, prepend the anchor to whatever first-cell content already exists. For example, if the row is `| Event 807 | ZtdDeviceIsNotRegistered | ... |`, it becomes `| <a id="event-807"></a>Event 807 | ZtdDeviceIsNotRegistered | ... |`. If the events are in a separate table from hex codes (cross-reference table per STATE.md Phase 3 notes), edit them wherever they live as the primary row.

    Important: 05-hybrid-join.md may contain cross-reference rows for shared codes (per STATE.md: "04-pre-provisioning.md uses cross-reference row pattern for shared TPM codes — primary entries stay in 02-tpm-attestation.md"). Add the anchor to the PRIMARY row only — the one where 00-index.md points. If unclear, locate the row whose Name column matches what 00-index.md shows for that event (e.g., `ZtdDeviceIsNotRegistered` for event-807).
  </action>
  <verify>
    <automated>for code in 0x80004005 0x81036501 0x81036502; do grep -q "id=\"$code\"" docs/error-codes/03-esp-enrollment.md || { echo "MISSING: $code in 03"; exit 1; }; done && grep -q 'id="0xc1036501"' docs/error-codes/04-pre-provisioning.md && grep -q 'id="0x80070774"' docs/error-codes/05-hybrid-join.md && for ev in event-171 event-172 event-807 event-809 event-815 event-908; do grep -q "id=\"$ev\"" docs/error-codes/05-hybrid-join.md || { echo "MISSING: $ev"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep finds `id="0x80004005"`, `id="0x81036501"`, `id="0x81036502"` in docs/error-codes/03-esp-enrollment.md (3 anchors)
    - grep finds `id="0xc1036501"` in docs/error-codes/04-pre-provisioning.md (1 anchor)
    - grep finds `id="0x80070774"` in docs/error-codes/05-hybrid-join.md (1 anchor)
    - grep finds all 6 event-ID anchors `id="event-171"` through `id="event-908"` (as listed) in docs/error-codes/05-hybrid-join.md
    - No rows in any of the 3 files are deleted or reordered
    - Every anchor is on a row whose first cell after the anchor contains the matching code or event label
  </acceptance_criteria>
  <done>
    All 11 remaining fragment IDs from 00-index.md (3 in 03, 1 in 04, 7 in 05) resolve to their intended error rows.
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-end anchor resolution verification</name>
  <files>docs/error-codes/00-index.md</files>
  <read_first>
    - docs/error-codes/00-index.md
    - docs/error-codes/01-mdm-enrollment.md
    - docs/error-codes/02-tpm-attestation.md
    - docs/error-codes/03-esp-enrollment.md
    - docs/error-codes/04-pre-provisioning.md
    - docs/error-codes/05-hybrid-join.md
  </read_first>
  <action>
    This is a verification-only task — do NOT edit 00-index.md or any category file unless a gap is found. Extract every fragment link from the Quick Lookup table in 00-index.md (patterns: `\.md#0x[0-9a-fA-F]+` and `\.md#event-[0-9]+`), then for each one verify that an `id="..."` with the matching (case-sensitive) value exists in the referenced target file.

    If any anchor is missing: go back and add it following the Task 1/Task 2 pattern. Do NOT change any existing anchor once verified.

    Expected totals:
    - Total fragment links in 00-index.md Quick Lookup: 30 (23 hex + 6 event + 1 policy-conflicts section link which is out-of-scope — it points to a heading which GFM auto-anchors)
    - Hex anchors to create across all 5 files: 23 hex + 6 event
    - Event anchors to create: 6
    - Grand total of new `id="..."` strings added by this plan: 29

    Note: The `#policy-conflicts-no-discrete-error-code` link in 00-index.md points to a markdown heading in 03-esp-enrollment.md which GFM auto-anchors from the heading text — do not touch this one.
  </action>
  <verify>
    <automated>total=$(grep -hoE 'id="(0x[0-9a-fA-F]+|event-[0-9]+)"' docs/error-codes/0[1-5]-*.md | sort -u | wc -l); test "$total" -eq 29 && echo "29 unique anchors OK" || { echo "FAIL: found $total unique anchors, expected 29"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - Across docs/error-codes/01-mdm-enrollment.md through 05-hybrid-join.md, exactly 29 unique `id="..."` values matching the pattern `0x[0-9a-fA-F]+` or `event-[0-9]+` exist
    - For each fragment link `file.md#fragment` in 00-index.md Quick Lookup table (hex or event pattern), the target file contains `id="fragment"` with exact case match
    - 00-index.md is unchanged by this task (verification-only)
    - No anchor is duplicated within a single file
  </acceptance_criteria>
  <done>
    All 29 hex + event fragment IDs from 00-index.md resolve to specific rows in category files. ROADMAP Phase 8 success criterion 4 satisfied.
  </done>
</task>

</tasks>

<verification>
From repo root:

```bash
# Hex anchors across all 5 category files
grep -hoE 'id="0x[0-9a-fA-F]+"' docs/error-codes/0[1-5]-*.md | sort -u | wc -l  # expect 23

# Event anchors in 05-hybrid-join.md
grep -oE 'id="event-[0-9]+"' docs/error-codes/05-hybrid-join.md | sort -u | wc -l  # expect 6

# Spot-check a known hard case (uppercase preserved)
grep 'id="0x801C03F3"' docs/error-codes/02-tpm-attestation.md
```

Visual spot-check: render 00-index.md and click a fragment link (e.g., `0x80180005`). Browser should jump to the corresponding row in 01-mdm-enrollment.md, not the file top.
</verification>

<success_criteria>
- 23 unique hex-code id anchors across category files 01-05
- 6 unique event-ID anchors in 05-hybrid-join.md
- All 29 inbound links from 00-index.md Quick Lookup hex/event rows resolve to specific targets
- ROADMAP Phase 8 success criterion 4 satisfied
- No existing table content modified beyond first-cell anchor insertion
</success_criteria>

<output>
After completion, create `.planning/phases/08-reference-anchors/08-02-SUMMARY.md` listing: exact anchors added per file, any codes not found in expected file (escalation required), and final grep-verified anchor count.
</output>
