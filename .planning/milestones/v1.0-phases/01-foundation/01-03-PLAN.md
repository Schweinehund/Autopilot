---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_templates/l1-template.md
  - docs/_templates/l2-template.md
autonomous: true
requirements:
  - FOUND-05

must_haves:
  truths:
    - "A writer creating any L1 doc can apply a template that enforces last_verified, review_by, applies_to, and audience frontmatter"
    - "A writer creating any L2 doc can apply a template that enforces the same 4 frontmatter fields"
    - "L1 template structure is Prerequisites, Steps, Escalation Criteria — no registry or PowerShell references"
    - "L2 template structure is Context, Investigation, Resolution, Tool References — includes links to reference files"
  artifacts:
    - path: "docs/_templates/l1-template.md"
      provides: "L1 document template with Prerequisites/Steps/Escalation structure"
      contains: "## Escalation Criteria"
    - path: "docs/_templates/l2-template.md"
      provides: "L2 document template with Context/Investigation/Resolution/Tool References structure"
      contains: "## Tool References"
  key_links:
    - from: "docs/_templates/l2-template.md"
      to: "docs/reference/powershell-ref.md"
      via: "Tool References section placeholder link"
      pattern: "powershell-ref\\.md"
    - from: "docs/_templates/l2-template.md"
      to: "docs/reference/registry-paths.md"
      via: "Tool References section placeholder link"
      pattern: "registry-paths\\.md"
---

<objective>
Create the L1 and L2 document templates that enforce consistent structure and review metadata across all documentation produced in Phases 2-7.

Purpose: Templates are the quality gate for every future doc. They enforce the 4 required frontmatter fields, version gate banner, and audience-appropriate structure. Without templates, every author invents their own layout.
Output: `docs/_templates/l1-template.md` (FOUND-05) and `docs/_templates/l2-template.md` (FOUND-05)
</objective>

<execution_context>
@C:/Users/joanderson/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joanderson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create L1 and L2 document templates</name>
  <files>docs/_templates/l1-template.md, docs/_templates/l2-template.md</files>
  <action>
First, create the `docs/_templates/` directory if it does not exist.

**l1-template.md:**

Create a template file that L1 runbook authors copy as their starting point. Use placeholder text in brackets to indicate what the author fills in.

```markdown
---
last_verified: YYYY-MM-DD
review_by: YYYY-MM-DD
applies_to: APv1 | APv2 | both
audience: L1
---

> **Version gate:** This guide applies to Windows Autopilot (classic).
> For Autopilot Device Preparation, see [APv1 vs APv2 disambiguation](../apv1-vs-apv2.md).

# [Issue Title]

## Prerequisites

- [What the L1 agent needs before starting — e.g., Intune portal access, device serial number]
- [Any information to collect from the user before beginning]

## Steps

1. [First action — use imperative voice, e.g., "Open the Intune portal"]
2. [Second action]
3. [Continue numbered steps — each step should have one clear action]

## Escalation Criteria

Escalate to L2 if:
- [Condition that indicates L1 cannot resolve — e.g., "Error persists after retry"]
- [Condition requiring technical investigation]

**Before escalating, collect:**
- [Data item 1 — e.g., "Device serial number"]
- [Data item 2 — e.g., "Screenshot of error message"]
- [Data item 3 — e.g., "Intune device status page"]
```

Add a comment block at the top of the template (above the frontmatter) explaining usage:

```markdown
<!-- L1 RUNBOOK TEMPLATE
     Usage: Copy this file as your starting point for any L1 Service Desk runbook.
     Rules:
     - Fill in all YYYY-MM-DD dates (review_by = last_verified + 90 days)
     - Set applies_to to APv1, APv2, or both
     - Replace all [bracketed placeholders] with actual content
     - Do NOT include PowerShell commands, registry paths, or log file references
     - Do NOT link to L2-audience content from within steps
     - Link to the glossary for any Autopilot-specific terms
     Reviewer: L1 Team Lead (role, not person name)
-->
```

**l2-template.md:**

Create a template file that L2 investigation guide authors copy as their starting point.

```markdown
---
last_verified: YYYY-MM-DD
review_by: YYYY-MM-DD
applies_to: APv1 | APv2 | both
audience: L2
---

> **Version gate:** This guide applies to Windows Autopilot (classic).
> For Autopilot Device Preparation, see [APv1 vs APv2 disambiguation](../apv1-vs-apv2.md).

# [Issue Title]

## Context

[What this issue is, when it typically occurs, and what triggers it. Include relevant background for an L2 engineer who may not have seen this specific failure before.]

## Investigation

### Step 1: [Investigation action]

[Detailed diagnostic steps. Include registry paths, event IDs, and PowerShell commands as needed.]

```powershell
# [Example command]
```

### Step 2: [Next investigation action]

[Continue with numbered investigation steps.]

## Resolution

### Scenario A: [Root cause description]

[Fix steps for this specific root cause.]

### Scenario B: [Alternative root cause]

[Fix steps for alternative root cause.]

## Tool References

- [PowerShell function](../reference/powershell-ref.md#function-name) — [what it does in this context]
- [Registry path](../reference/registry-paths.md) — [which path and why]
- [Network endpoints](../reference/endpoints.md) — [if connectivity is relevant]
```

Add a comment block at the top:

```markdown
<!-- L2 INVESTIGATION GUIDE TEMPLATE
     Usage: Copy this file as your starting point for any L2 Desktop Engineer guide.
     Rules:
     - Fill in all YYYY-MM-DD dates (review_by = last_verified + 90 days)
     - Set applies_to to APv1, APv2, or both
     - Replace all [bracketed placeholders] with actual content
     - Link to reference files for registry paths, PowerShell functions, and endpoints
       Do NOT define paths or function signatures inline
     - Include actual PowerShell commands and registry paths in investigation steps
     - Document multiple resolution scenarios where applicable
     Reviewer: L2 Desktop Lead (role, not person name)
-->
```

IMPORTANT: The L2 template Tool References section must use relative links to `../reference/powershell-ref.md`, `../reference/registry-paths.md`, and `../reference/endpoints.md`. These are the canonical reference files created by Plan 02. L2 guides link TO references, never repeat content from them.
  </action>
  <verify>
    <automated>bash -c "test -d docs/_templates && echo 'PASS: templates dir exists' || echo 'FAIL: no templates dir' && test -f docs/_templates/l1-template.md && echo 'PASS: l1-template exists' || echo 'FAIL: missing l1-template' && test -f docs/_templates/l2-template.md && echo 'PASS: l2-template exists' || echo 'FAIL: missing l2-template' && grep -q '## Prerequisites' docs/_templates/l1-template.md && grep -q '## Steps' docs/_templates/l1-template.md && grep -q '## Escalation Criteria' docs/_templates/l1-template.md && echo 'PASS: l1 has all 3 sections' || echo 'FAIL: l1 missing sections' && grep -q '## Context' docs/_templates/l2-template.md && grep -q '## Investigation' docs/_templates/l2-template.md && grep -q '## Resolution' docs/_templates/l2-template.md && grep -q '## Tool References' docs/_templates/l2-template.md && echo 'PASS: l2 has all 4 sections' || echo 'FAIL: l2 missing sections' && grep -q 'last_verified:' docs/_templates/l1-template.md && grep -q 'review_by:' docs/_templates/l1-template.md && grep -q 'applies_to:' docs/_templates/l1-template.md && grep -q 'audience:' docs/_templates/l1-template.md && echo 'PASS: l1 has all 4 frontmatter fields' || echo 'FAIL: l1 frontmatter incomplete' && grep -q 'powershell-ref.md' docs/_templates/l2-template.md && echo 'PASS: l2 links to reference files' || echo 'FAIL: l2 missing reference links'"</automated>
  </verify>
  <done>docs/_templates/ directory exists. l1-template.md has Prerequisites/Steps/Escalation Criteria sections, all 4 frontmatter fields, version gate banner, usage comment block, and no PowerShell/registry references. l2-template.md has Context/Investigation/Resolution/Tool References sections, all 4 frontmatter fields, version gate banner, usage comment block, and links to canonical reference files.</done>
</task>

</tasks>

<verification>
- `docs/_templates/` directory exists
- Both template files have YAML frontmatter with all 4 required fields
- Both template files have version gate banner
- l1-template has Prerequisites, Steps, Escalation Criteria sections
- l2-template has Context, Investigation, Resolution, Tool References sections
- l2-template links to reference files (powershell-ref.md, registry-paths.md, endpoints.md)
- l1-template does NOT contain PowerShell commands or registry paths
- Both templates have usage comment blocks explaining rules
</verification>

<success_criteria>
- A writer creating an L1 runbook can copy l1-template.md and have the correct structure with no guesswork
- A writer creating an L2 guide can copy l2-template.md and have the correct structure with reference file links pre-populated
- Both templates enforce the 4 required frontmatter fields (last_verified, review_by, applies_to, audience)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
