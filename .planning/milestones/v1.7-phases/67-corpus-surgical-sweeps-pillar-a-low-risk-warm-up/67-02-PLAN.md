---
phase: 67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up
plan: 02
type: execute
wave: 2
depends_on: [67-01]
files_modified:
  - docs/admin-setup-ios/05-app-deployment.md
  - docs/admin-setup-macos/04-app-deployment.md
  - docs/_glossary-macos.md
  - scripts/validation/v1.6-audit-allowlist.json
  - .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-ANCHOR-INVENTORY.md
autonomous: true
requirements:
  - SWEEP-02
tags:
  - corpus-edit
  - apple-business
  - atomic-commit
  - sweep-02
  - pitfall-6
  - phase-67

must_haves:
  truths:
    - "PITFALL-6 pre-edit anchor inventory captured for docs/_glossary-macos.md ONLY (the sole file in STATE.md:125 scope per CONTEXT.md D-01); admin-setup-ios/05-app-deployment.md and admin-setup-macos/04-app-deployment.md are NOT in PITFALL-6 scope and skip anchor inventory"
    - "All 6 D-03 line rewrites land per CONTEXT.md D-03 table verbatim (rows #1, #2b, #3, #4, #5, #6b — no paraphrasing, no renegotiation of text)"
    - "2 OP-10-style callout block inserts land per CONTEXT.md D-03 rows #2a and #6a verbatim (one ABOVE each ## Renewal / Maintenance table — one per doc); blockquote format `> **Note:** Apple calls this artifact a \"content token\" (formerly \"VPP location token\"); Microsoft Intune labels it \"Apple VPP token\" under `Tenant administration > Connectors and tokens > Apple VPP tokens`. Same artifact, different vendor terminology.`"
    - "Each of the 2 SWEEP-02 corpus files (admin-setup-ios/05-app-deployment.md + admin-setup-macos/04-app-deployment.md) gains ONE tail-table Version History row dated 2026-05-26 per CONTEXT.md D-03 VH-iOS / VH-mac rows VERBATIM"
    - "Frontmatter last_verified: bumped to 2026-05-26 on BOTH SWEEP-02 corpus files (was 2026-04-18 iOS / 2026-04-14 macOS)"
    - "docs/_glossary-macos.md ## Version History H2 table at line 121 gains ONE coordinating row dated 2026-05-26 (newest-first, immediately after the |------| separator at line 124); H2 line position preserved at line 121 post-edit (PITFALL-6 anti-regression boundary)"
    - "Frontmatter last_verified: bumped to 2026-05-26 on _glossary-macos.md (was 2026-05-05); discretionary per CONTEXT.md Claude's Discretion — bumped for traceability symmetry since the file's H2 table is edited"
    - "Sidecar c13_rotting_external.ci_2_vpp_location_token: each of the 6 entries gains the new field \"resolved_2026_05_26\": true (ANNOTATE-not-remove per CONTEXT.md D-04); 6-entry array shape preserved (V-66-02 shape stable)"
    - "Post-edit anchor inventory diff vs pre-edit (Wave 1 snapshot) confirms ZERO anchor shift at or above line 121 in _glossary-macos.md; the H2 ## Version History remains at line 121"
    - "Pre-commit dry-run triple (v1.6-milestone-audit.mjs + check-phase-62.mjs + check-phase-66.mjs) all exit 0 BEFORE git add (harness C11/C14/C15/C16 inert on SWEEP-02 edited surface per CONTEXT.md D-03 line 99; V-62-SIDECAR c16==0 unaffected; V-66-02 c13 shape stable)"
    - "ATOMIC commit: all 5 files (2 corpus + 1 glossary + 1 sidecar + 1 anchor-inventory artifact) land in ONE indivisible commit verified via `git log --name-only -1 HEAD`"
    - "Single git revert <Plan 67-02 SHA> cleanly restores v1.6-close corpus baseline for the 2 SWEEP-02 files + glossary + sidecar (per CONTEXT.md D-04 line 153-154 rollback semantics)"
  artifacts:
    - path: docs/admin-setup-ios/05-app-deployment.md
      provides: "SWEEP-02 surgical edits at L71 (D-03 row #1) + L198/199 callout insert (D-03 row #2a) + L201 table cell rename (D-03 row #2b); tail-table VH row appended (D-03 VH-iOS); frontmatter last_verified bumped 2026-04-18 → 2026-05-26"
      contains:
        - "content token (formerly VPP location token; still labeled \"Apple VPP token\" in Intune)"
        - "> **Note:** Apple calls this artifact a \"content token\""
        - "Same artifact, different vendor terminology"
        - "| 2026-05-26 | Phase 67 (SWEEP-02)"
        - "PITFALLS.md CI-2 closure"
        - "last_verified: 2026-05-26"
    - path: docs/admin-setup-macos/04-app-deployment.md
      provides: "SWEEP-02 surgical edits at L45 (D-03 row #3) + L46 (D-03 row #4) + L113 (D-03 row #5) + L145/146 callout insert (D-03 row #6a) + L148 table cell rename (D-03 row #6b); tail-table VH row appended (D-03 VH-mac); frontmatter last_verified bumped 2026-04-14 → 2026-05-26"
      contains:
        - "content token (formerly VPP location token; still labeled \"Apple VPP token\" in Intune)"
        - "Content token uploaded to Intune"
        - "Verify the content token"
        - "> **Note:** Apple calls this artifact a \"content token\""
        - "Same artifact, different vendor terminology"
        - "| 2026-05-26 | Phase 67 (SWEEP-02)"
        - "last_verified: 2026-05-26"
    - path: docs/_glossary-macos.md
      provides: "## Version History H2 table at line 121 gains ONE coordinating row at line 125 (newest-first); frontmatter last_verified bumped; PITFALL-6 anchor stable at line 121"
      contains:
        - "| 2026-05-26 | Phase 67 (SWEEP-02): coordinating row for VPP location token"
        - "last_verified: 2026-05-26"
        - "## Version History"
    - path: scripts/validation/v1.6-audit-allowlist.json
      provides: "c13_rotting_external.ci_2_vpp_location_token: 6 entries annotated with resolved_2026_05_26:true (ANNOTATE-not-remove); 6-entry shape preserved (V-66-02 stable)"
      contains:
        - "\"ci_2_vpp_location_token\""
        - "\"resolved_2026_05_26\": true"
    - path: .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-ANCHOR-INVENTORY.md
      provides: "PITFALL-6 pre-edit + post-edit anchor inventory for docs/_glossary-macos.md (single file in STATE.md:125 scope per CONTEXT.md D-01); table format mirrors Phase 65 ANCHOR-INVENTORY precedent (Line / Level / Heading Text / Slug / Notes); load-bearing anchor flags + post-edit verification protocol included inline"
      min_lines: 25
      contains:
        - "Phase 67 Pre-Edit Anchor Inventory"
        - "_glossary-macos.md"
        - "## Version History"
        - "version-history"
        - "PITFALL-6"
        - "line 121"
  key_links:
    - from: "docs/admin-setup-ios/05-app-deployment.md callout block above ## Renewal / Maintenance"
      to: "Phase 64 docs/cross-platform/apple-business/11-vpp-catalog-runbook.md:35-39 OP-10 callout precedent"
      via: "blockquote format conversion (Phase 64 used bold inline; Phase 67 uses > **Note:** blockquote per CONTEXT.md D-03 row 2a)"
      pattern: "> \\*\\*Note:\\*\\* Apple calls this artifact a \"content token\""
    - from: "docs/admin-setup-macos/04-app-deployment.md callout block above ## Renewal / Maintenance"
      to: "Same Phase 64 OP-10 precedent (callout pattern reused; CONTEXT.md D-03 row 6a)"
      via: "blockquote format"
      pattern: "> \\*\\*Note:\\*\\* Apple calls this artifact a \"content token\""
    - from: "docs/_glossary-macos.md ## Version History H2 row at line 125"
      to: "docs/admin-setup-ios/05-app-deployment.md + docs/admin-setup-macos/04-app-deployment.md (the 2 SWEEP-02 target files)"
      via: "coordinating row references both SWEEP-02 file names + L71+L201 (iOS) + L45+L46+L113+L148 (macOS)"
      pattern: "VPP location token → content token"
    - from: "scripts/validation/v1.6-audit-allowlist.json c13_rotting_external.ci_2_vpp_location_token[*]"
      to: "post-rename state in the 2 corpus files (line numbers preserved or shifted by callout-insert; reason field references rename completion)"
      via: "per-entry resolved_2026_05_26:true annotation"
      pattern: "\"resolved_2026_05_26\": true"
---

<objective>
Land the SWEEP-02 surgical "VPP location token" → "content token" rename in ONE indivisible commit per CONTEXT.md D-04 LOCKED Option E (per-plan atomic boundary; clean revert per requirement). The commit packages:

- 6 line rewrites + 2 callout block inserts per CONTEXT.md D-03 table VERBATIM (rows #1, #2a, #2b, #3, #4, #5, #6a, #6b)
- 2 tail-table Version History rows (one per SWEEP-02 file) per CONTEXT.md D-03 VH-iOS / VH-mac rows VERBATIM
- 2 frontmatter `last_verified: 2026-05-26` bumps (iOS + macOS SWEEP-02 files) per CONTEXT.md D-01
- 1 coordinating row appended to `docs/_glossary-macos.md` `## Version History` H2 table at line 121+ (PITFALL-6 anchor at line 121 preserved)
- 1 frontmatter `last_verified: 2026-05-26` bump on `_glossary-macos.md` (per Claude's Discretion for traceability symmetry — file's H2 table is edited)
- 6 sidecar `ci_2_vpp_location_token` entry annotations with `"resolved_2026_05_26": true` (ANNOTATE-not-remove per D-04; 6-entry array shape preserved)
- 1 PITFALL-6 pre-edit anchor inventory artifact (`67-ANCHOR-INVENTORY.md`) for `_glossary-macos.md` ONLY (the sole file in STATE.md:125 scope per CONTEXT.md D-01)

**Purpose:** SWEEP-02 contract per REQUIREMENTS.md:14 + ROADMAP.md:281 — surgically close the "VPP location token" → "content token" corpus drift per Apple's 2026-04-14 rebrand. The CI-2 calibrated enumeration is locked at 6 occurrences / 2 files at HEAD `ad5c9c9` per `v1.6-DEFERRED-CLEANUP.md`. PITFALLS.md:657 first-mention-per-H2 convention is honored via the compound `"content token (formerly VPP location token; still labeled \"Apple VPP token\" in Intune)"` form on first prose mention per H2; short `"content token"` form in table cells; one OP-10 callout above each `## Renewal / Maintenance` table delivers the Apple-vs-Intune label disambiguation to isolation-readers without click-through (per CONTEXT.md D-03 rationale lines 76-81).

**Validator inertia by construction:**
- C15 `appleBusinessDocPaths()` at `v1.6-milestone-audit.mjs:93-124` EXCLUDES both SWEEP-02 files — cannot fire
- C11 banned patterns (`System Center | SCCM…Intune | Autopatch rings | SafetyNet…compliance`) do NOT match `content token`, `Apple VPP token`, or `VPP location token`
- C14 rebrand-callout 3-site check is unaffected (not a callout-site edit on the 3 canonical Apple Business rebrand-callout pages)
- V-66-02 sidecar shape check enforces object + `quarterly_audit.cadence` literal only; per-entry annotations are free-text

Full chain `check-phase-{48..66}.mjs` expected to exit with identical status as v1.6 close (PASS modulo CHAIN_SKIP {48,51,58,60,61}); proven post-commit at Plan 67-03 close-gate.

**Output:**
1. ONE atomic git commit (single SHA) touching exactly 5 files: 2 corpus + 1 glossary + 1 sidecar + 1 new anchor-inventory artifact
2. `67-ANCHOR-INVENTORY.md` artifact with pre-edit + post-edit anchor enumeration for `_glossary-macos.md` confirming zero anchor shift ≥ line 121
3. Pre-commit dry-run triple (harness + check-phase-62 + check-phase-66) all exit 0 BEFORE staging; identical exit codes post-commit
4. Clean rollback semantics: `git revert <Plan 67-02 SHA>` restores v1.6-close baseline for the 2 SWEEP-02 files + glossary + sidecar byte-identically

**Atomicity is the work product.** This plan has ONE task — splitting the SWEEP-02 commit into per-file or per-wave tasks defeats the indivisibility contract per CONTEXT.md D-04 lines 152-154 (logical independence of requirements; clean revert per requirement).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-CONTEXT.md
@.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-RESEARCH.md
@.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-PATTERNS.md
@.planning/milestones/v1.6-DEFERRED-CLEANUP.md
@.planning/research/PITFALLS.md
@.planning/milestones/v1.6-phases/65-apple-business-l1-l2-hub-navigation-integration/65-ANCHOR-INVENTORY.md
@docs/admin-setup-ios/05-app-deployment.md
@docs/admin-setup-macos/04-app-deployment.md
@docs/_glossary-macos.md
@docs/cross-platform/apple-business/11-vpp-catalog-runbook.md
@scripts/validation/v1.6-audit-allowlist.json
@scripts/validation/check-phase-66.mjs
@scripts/validation/check-phase-62.mjs
@scripts/validation/v1.6-milestone-audit.mjs

<interfaces>
<!-- The 6 line rewrites + 2 callout inserts + 2 VH rows are LOCKED in CONTEXT.md D-03 table; pasted here for direct executor access (do NOT renegotiate). -->

**CONTEXT.md D-03 ROW #1 — `docs/admin-setup-ios/05-app-deployment.md:71` (first mention in `## Prerequisites` > `### VPP Prerequisites`):**

BEFORE (exact line 71 content at HEAD):
```
- VPP (Apps and Books) location token uploaded to Intune (Tenant administration > Connectors and tokens > Apple VPP tokens)
```

AFTER (substitute verbatim; first prose mention per H2 = full compound form):
```
- Content token (formerly VPP location token; still labeled "Apple VPP token" in Intune) uploaded to Intune (Tenant administration > Connectors and tokens > Apple VPP tokens)
```

**CONTEXT.md D-03 ROW #2a — `docs/admin-setup-ios/05-app-deployment.md` INSERT new callout block ABOVE `## Renewal / Maintenance` H2 at line 197:**

INSERT POINT: between the last line of the preceding `## Configuration-Caused Failures` table at line 195 and the H2 `## Renewal / Maintenance` at line 197. The callout block is inserted at what is currently line 196 (the blank line); after insertion the H2 shifts to a later line.

INSERT CONTENT (verbatim from CONTEXT.md D-03 row 2a; blockquote + blank line trailing):
```
> **Note:** Apple calls this artifact a "content token" (formerly "VPP location token"); Microsoft Intune labels it "Apple VPP token" under `Tenant administration > Connectors and tokens > Apple VPP tokens`. Same artifact, different vendor terminology.

```

Discipline: ONE blank line BEFORE the `>` block (separating from the preceding table's last row); ONE blank line AFTER (separating from the H2 `## Renewal / Maintenance` heading). Mirrors Phase 64 `11-vpp-catalog-runbook.md:30-40` precedent for blank-line discipline around the callout.

After this insert, the H2 `## Renewal / Maintenance` shifts from line 197 to ~line 199; the table row currently at line 201 shifts to ~line 203.

**CONTEXT.md D-03 ROW #2b — `docs/admin-setup-ios/05-app-deployment.md:201` (now ~line 203 after callout insert) — `## Renewal / Maintenance` table cell (subsequent mention in same H2 = short form):**

BEFORE (exact pre-insert line 201 content at HEAD, table cell from VPP token row):
```
| VPP (Apps and Books) location token | Annual (365 days) | VPP apps stop syncing from ABM; existing installs unaffected but no new assignments, revocations, or license management until renewed | 1. In ABM: **Preferences** > **Payments and Billing** > **Apps and Books** > **Content Tokens** > **Download**. 2. In Intune: **Tenant administration** > **Connectors and tokens** > **Apple VPP tokens** > [token] > **Edit** > **Upload new token** |
```

AFTER (rename the FIRST cell ONLY — `VPP (Apps and Books) location token` → `Content token`; rest of the row byte-identical):
```
| Content token | Annual (365 days) | VPP apps stop syncing from ABM; existing installs unaffected but no new assignments, revocations, or license management until renewed | 1. In ABM: **Preferences** > **Payments and Billing** > **Apps and Books** > **Content Tokens** > **Download**. 2. In Intune: **Tenant administration** > **Connectors and tokens** > **Apple VPP tokens** > [token] > **Edit** > **Upload new token** |
```

**CONTEXT.md D-03 ROW #3 — `docs/admin-setup-macos/04-app-deployment.md:45` (first mention in `## Prerequisites` > `### VPP/Apps and Books Prerequisites`):**

BEFORE (exact line 45 content at HEAD):
```
- Apple Business Manager account with active VPP location token
```

AFTER (full compound form on first prose mention per H2):
```
- Apple Business Manager account with an active content token (formerly VPP location token; still labeled "Apple VPP token" in Intune)
```

**CONTEXT.md D-03 ROW #4 — `docs/admin-setup-macos/04-app-deployment.md:46` (subsequent mention in same H2):**

BEFORE (exact line 46 content at HEAD):
```
- VPP location token uploaded to Intune (Tenant administration > Connectors and tokens > Apple VPP tokens)
```

AFTER (short form, subsequent mention):
```
- Content token uploaded to Intune (Tenant administration > Connectors and tokens > Apple VPP tokens)
```

**CONTEXT.md D-03 ROW #5 — `docs/admin-setup-macos/04-app-deployment.md:113` (first mention in `## VPP / Apps and Books` H2 — first mention in a DIFFERENT H2, so compound form fires again):**

BEFORE (exact line 113 content at HEAD):
```
2. Verify the VPP location token is active and synced.
```

AFTER (compound form on first prose mention in this H2):
```
2. Verify the content token (formerly VPP location token; still labeled "Apple VPP token" in Intune) is active and synced.
```

**CONTEXT.md D-03 ROW #6a — `docs/admin-setup-macos/04-app-deployment.md` INSERT new callout block ABOVE `## Renewal / Maintenance` H2 at line 144:**

INSERT POINT: between the last paragraph of the preceding `## Configuration-Caused Failures` section at line 142 and the H2 `## Renewal / Maintenance` at line 144. The callout block is inserted at what is currently line 143 (blank line); after insertion the H2 shifts to a later line.

INSERT CONTENT (verbatim from CONTEXT.md D-03 row 6a; SAME blockquote text as row 2a):
```
> **Note:** Apple calls this artifact a "content token" (formerly "VPP location token"); Microsoft Intune labels it "Apple VPP token" under `Tenant administration > Connectors and tokens > Apple VPP tokens`. Same artifact, different vendor terminology.

```

Same blank-line discipline as row 2a.

After this insert, the H2 `## Renewal / Maintenance` shifts from line 144 to ~line 146; the table row currently at line 148 shifts to ~line 150.

**CONTEXT.md D-03 ROW #6b — `docs/admin-setup-macos/04-app-deployment.md:148` (now ~line 150 after callout insert) — `## Renewal / Maintenance` table cell (subsequent mention in same H2):**

BEFORE (exact pre-insert line 148 content at HEAD, table cell from VPP token row):
```
| VPP location token | Annual (365 days) | VPP apps stop syncing from ABM; existing installs unaffected but no new assignments or license management | 1. In ABM: Preferences > Payments and Billing > Apps and Books > Content Tokens > Download. 2. In Intune: Tenant admin > Connectors and tokens > Apple VPP tokens > [token] > Edit > Upload new token |
```

AFTER (rename FIRST cell ONLY — `VPP location token` → `Content token`; rest of the row byte-identical):
```
| Content token | Annual (365 days) | VPP apps stop syncing from ABM; existing installs unaffected but no new assignments or license management | 1. In ABM: Preferences > Payments and Billing > Apps and Books > Content Tokens > Download. 2. In Intune: Tenant admin > Connectors and tokens > Apple VPP tokens > [token] > Edit > Upload new token |
```

**CONTEXT.md D-03 ROW VH-iOS — `docs/admin-setup-ios/05-app-deployment.md` tail-table at lines 224-227 — APPEND ONE NEW ROW at end of table (after existing last row at line 227):**

ROW TEXT (verbatim from CONTEXT.md D-03 VH-iOS row):
```
| 2026-05-26 | Phase 67 (SWEEP-02): renamed "VPP (Apps and Books) location token" to "content token" per Apple 2026-04-14 rebrand (L71, L201); added Apple-vs-Intune label disambiguation callout above Renewal/Maintenance table; PITFALLS.md CI-2 closure | -- |
```

Append rule: insert as the LAST row of the existing unheaded tail-table after the `---` separator at line 223. Do NOT add a new H2 (D-01 forbids H2 promotion in admin-setup-* files). Do NOT add blank lines between rows.

**CONTEXT.md D-03 ROW VH-mac — `docs/admin-setup-macos/04-app-deployment.md` tail-table at lines 162-165 — APPEND ONE NEW ROW at end of table (after existing last row at line 165):**

ROW TEXT (verbatim from CONTEXT.md D-03 VH-mac row):
```
| 2026-05-26 | Phase 67 (SWEEP-02): renamed "VPP location token" to "content token" per Apple 2026-04-14 rebrand (L45, L46, L113, L148); added Apple-vs-Intune label disambiguation callout above Renewal/Maintenance table; PITFALLS.md CI-2 closure | -- |
```

Same append rule as VH-iOS.

**Frontmatter bumps:**

- `docs/admin-setup-ios/05-app-deployment.md:2` — `last_verified: 2026-04-18` → `last_verified: 2026-05-26`
- `docs/admin-setup-macos/04-app-deployment.md:2` — `last_verified: 2026-04-14` → `last_verified: 2026-05-26`
- `docs/_glossary-macos.md:2` — `last_verified: 2026-05-05` → `last_verified: 2026-05-26` (per Claude's Discretion — file's H2 table is edited)

Other frontmatter fields (`review_by:`, `applies_to:`, `audience:`, `platform:`) remain byte-identical.

**Glossary `## Version History` H2 coordinating row at `docs/_glossary-macos.md` line 121+:**

Current state at lines 121-128 (verified per 67-PATTERNS.md:131-141):
```
## Version History

| Date | Change | Author |
|------|--------|--------|
| 2026-05-05 | Phase 59 (CLEAN-08): appended `> See also:` lines INSIDE existing `> **Windows equivalent:**` blockquotes for collision-matrix terms (Account-Driven User Enrollment, ADE, Await Configuration, Setup Assistant, Supervision, ABM, VPP, MAM-WE); existing `> **Windows equivalent:**` labels PRESERVED verbatim per D-15 anti-rename | -- |
| 2026-04-24 | Phase 42: added Android Enterprise Provisioning Glossary see-also to continuation banner (AEAUDIT-03) | -- |
| 2026-04-17 | Phase 32: added iOS/iPadOS terms (supervision, MAM-WE, APNs, account-driven user enrollment, jailbreak detection), updated VPP with iOS device-licensed vs user-licensed distinction, new ## App Protection (MAM) H2 | -- |
| 2026-04-13 | Initial version -- 6 macOS terms with Windows cross-references | -- |
```

INSERT new row at line 125 (immediately after `|------|------|--------|` separator at line 124; existing row at line 125 shifts to line 126; newest-first convention per 67-PATTERNS.md:146).

NEW ROW TEXT (per 67-PATTERNS.md:151; Claude's Discretion allowed phrasing; date + phase + SWEEP-NN + closure marker preserved):
```
| 2026-05-26 | Phase 67 (SWEEP-02): coordinating row for VPP location token → content token surgical rename in admin-setup-ios/05- + admin-setup-macos/04-app-deployment.md (PITFALLS.md CI-2 closure) | -- |
```

PITFALL-6 anti-regression: the H2 `## Version History` at line 121 MUST remain at line 121 post-edit. The row insertion happens at line 124-125 INSIDE the existing table; H2 line position is invariant to intra-table row inserts. NO new blank lines between rows.

**Sidecar `c13_rotting_external.ci_2_vpp_location_token` annotation pattern (per 67-PATTERNS.md:193-197 + 67-RESEARCH.md Example 5):**

Each of the 6 entries at sidecar lines 87-92 receives ONE new field `"resolved_2026_05_26": true` appended at the END of the entry object (before closing `}`). Single-line compact shape preserved. 6-entry array shape preserved.

Example AFTER for entry #1 (line 87):
```json
{ "file": "docs/admin-setup-ios/05-app-deployment.md", "line": 71, "term": "VPP (Apps and Books) location token", "context": "prerequisites bullet", "reason": "legacy 'VPP location token' term retained per Q5(b) no-corpus-sweep contract; rename deferred to v1.7+ CI-2", "category": "legacy_term_surgical_rename_candidate", "resolved_2026_05_26": true }
```

Repeat verbatim shape for all 6 entries (do NOT modify `file`, `line`, `term`, `context`, `reason`, or `category` fields — only append the new boolean).

JSON parse guard (MANDATORY after the batch edit per 67-RESEARCH.md Pitfall 1 + 67-PATTERNS.md:206-208):

```
node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); console.log('OK')"
```

Expected: prints `OK`.

**Anchor inventory artifact (`67-ANCHOR-INVENTORY.md`) — single file in scope: `docs/_glossary-macos.md`:**

Template per Phase 65 `.planning/milestones/v1.6-phases/65-apple-business-l1-l2-hub-navigation-integration/65-ANCHOR-INVENTORY.md` (verbatim format mirror; adapted per 67-PATTERNS.md:354-419):

Document body shape:
- H1: `# Phase 67 Pre-Edit Anchor Inventory`
- Frontmatter prose: Captured date + Purpose + Files covered (1 file)
- H2 `## File 1: docs/_glossary-macos.md` with Total line count + Role + Insertion point
- H3 `### H2/H3 Anchor Table` with 5-column table (Line | Level | Heading Text | Slug | Notes)
- H3 `### Load-Bearing Anchor Flags` with `#version-history` PITFALL-6 invariant
- H3 `### Post-Edit Verification Protocol` describing the `git diff --no-index` comparison

Capture mechanism (per 67-RESEARCH.md Example 7):

```
Select-String -Path docs/_glossary-macos.md -Pattern '^#' | ForEach-Object {
  "{0,4} | {1}" -f $_.LineNumber, $_.Line
} | Out-File .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-glossary-anchors-pre.txt -Encoding utf8
```

Post-edit (after Waves 2-5 land but BEFORE git add):

```
Select-String -Path docs/_glossary-macos.md -Pattern '^#' | ForEach-Object {
  "{0,4} | {1}" -f $_.LineNumber, $_.Line
} | Out-File .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-glossary-anchors-post.txt -Encoding utf8

git diff --no-index .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-glossary-anchors-pre.txt .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-glossary-anchors-post.txt
```

Expected: ZERO output lines (no anchor shift). If any output appears, ABORT and investigate before commit.

Post-edit diff is embedded inline at the end of `67-ANCHOR-INVENTORY.md` under a `## Post-Edit Verification` H2 (per Phase 65 precedent — inline rather than separate `67-ANCHOR-INVENTORY-POST.md` file; CONTEXT.md Claude's Discretion line 161 allows either).

**Pre-commit dry-run triple (MANDATORY before `git add` per CONTEXT.md D-04 Plan 67-02 Wave 7 lines 136-139):**

```
node scripts/validation/v1.6-milestone-audit.mjs   # expect exit 0 (C11/C14/C15/C16 ALL inert on SWEEP-02 surface per CONTEXT.md D-03 line 99)
node scripts/validation/check-phase-62.mjs         # expect exit 0 (V-62-SIDECAR c16==0 unchanged; c13 annotations free-text)
node scripts/validation/check-phase-66.mjs         # expect exit 0 modulo CHAIN_SKIP {48,51,58,60,61} (V-66-02 c13_rotting_external object + quarterly_audit.cadence stable)
```

ALL 3 MUST exit 0 (modulo SKIP) before staging. If ANY fail, fix the underlying issue and re-run; do NOT `git add` until green. Per 67-PATTERNS.md:566 expected pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 67-02-01: SWEEP-02 atomic-within-plan — PITFALL-6 anchor inventory + 6 verbatim D-03 rewrites + 2 callout inserts + 2 VH rows + glossary coordinating row + sidecar 6-entry annotation + pre-commit dry-run + atomic indivisible commit + post-commit verify</name>
  <files>docs/admin-setup-ios/05-app-deployment.md, docs/admin-setup-macos/04-app-deployment.md, docs/_glossary-macos.md, scripts/validation/v1.6-audit-allowlist.json, .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-ANCHOR-INVENTORY.md</files>
  <read_first>
    - .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-CONTEXT.md (read D-03 table at lines 73-100 — the 6 line rewrites + 2 callout block inserts + 2 VH rows verbatim; D-04 Plan 67-02 layout at lines 129-141; D-01 anchor-inventory scope at lines 30-44)
    - .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-RESEARCH.md (read §SWEEP-02 Execution Mechanics + §Code Examples #4-7 — sidecar annotation patterns + anchor inventory capture; §Common Pitfalls 1, 5, 6 — JSON parse + CRLF/LF + anchor drift)
    - .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-PATTERNS.md (read sidecar annotation pattern at lines 168-209; OP-10 callout pattern at lines 44-71; tail-table append pattern at lines 74-106; glossary H2 row pattern at lines 127-165; anchor inventory artifact pattern at lines 339-428)
    - .planning/milestones/v1.6-phases/65-apple-business-l1-l2-hub-navigation-integration/65-ANCHOR-INVENTORY.md (Phase 65 anchor inventory format precedent — Line / Level / Heading Text / Slug / Notes columns; read the first file entry as the template shape)
    - docs/admin-setup-ios/05-app-deployment.md (read lines 1-7 frontmatter + lines 65-75 for line 71 SWEEP-02 site + lines 195-210 for line 198/201 callout-insert + table-cell sites + lines 220-227 for tail-table)
    - docs/admin-setup-macos/04-app-deployment.md (read lines 1-7 frontmatter + lines 40-50 for line 45/46 SWEEP-02 sites + lines 110-115 for line 113 site + lines 140-150 for line 144/148 callout-insert + table-cell sites + lines 160-165 for tail-table)
    - docs/_glossary-macos.md (read lines 1-7 frontmatter + lines 115-128 for existing `## Version History` H2 at line 121 + 4 existing rows; this is the PITFALL-6 anchor-inventory file)
    - docs/cross-platform/apple-business/11-vpp-catalog-runbook.md (read lines 28-44 — Phase 64 OP-10 callout precedent that Phase 67 D-03 row 2a / 6a callouts adapt to blockquote format)
    - scripts/validation/v1.6-audit-allowlist.json (read lines 85-93 — the 6 `ci_2_vpp_location_token` entries to annotate; verify pre-edit shape matches the snippet in `<interfaces>`)
    - scripts/validation/check-phase-66.mjs (read lines 40-112 — verify V-66-02 sidecar shape check only enforces object + quarterly_audit.cadence; per-entry annotations are NOT shape-enforced)
    - scripts/validation/v1.6-milestone-audit.mjs (read lines 90-130 — confirm C15 `appleBusinessDocPaths()` scope EXCLUDES both SWEEP-02 files; verifies "harness C11/C15 re-run shows no false positives" SC#2 clause is satisfied by construction)
  </read_first>
  <action>
**WAVE 1 — PITFALL-6 PRE-EDIT ANCHOR INVENTORY (read-only snapshot of docs/_glossary-macos.md)**

1. Verify pre-edit state. Read `docs/_glossary-macos.md` at lines 115-128 and confirm:
   - `## Version History` H2 is at line 121
   - Table separator `|------|------|--------|` is at line 124
   - Existing newest row (`| 2026-05-05 | Phase 59 (CLEAN-08) ... | -- |`) is at line 125
   - File ends at line 128 (or 129 with trailing newline) per 67-PATTERNS.md:140

2. Capture pre-edit anchor enumeration via PowerShell:

```
Select-String -Path docs/_glossary-macos.md -Pattern '^#' | ForEach-Object {
  "{0,4} | {1}" -f $_.LineNumber, $_.Line
} | Out-File .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-glossary-anchors-pre.txt -Encoding utf8
```

3. Author `.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-ANCHOR-INVENTORY.md` (NEW file). Use the Phase 65 precedent format. Document body must contain:

```markdown
# Phase 67 Pre-Edit Anchor Inventory

**Captured:** 2026-05-26 (Plan 67-02 Wave 1 — BEFORE SWEEP-02 edits)
**Purpose:** PITFALL-6 / D-01 anti-regression baseline for the single file in STATE.md:125 scope ("capability matrices, glossaries, hub files"). Wave 6 post-edit diff verifies zero anchor shift at or above line 121 (`## Version History` H2). Any anchor present pre-edit that is missing or line-shifted post-edit constitutes a PITFALL-6 violation and a rollback condition before commit.

**Files covered:** 1 (`docs/_glossary-macos.md` — sole PITFALL-6 scope file per STATE.md:125 + CONTEXT.md D-01; admin-setup-ios/05- and admin-setup-macos/04- are NOT in PITFALL-6 scope per CONTEXT.md D-01 line 34)

## File 1: `docs/_glossary-macos.md`

**Total line count:** 128 (per 67-RESEARCH.md A6 verified read on 2026-05-26)
**Role:** macOS glossary; SWEEP-02 coordinating row appended to `## Version History` H2 table at line 121; frontmatter `last_verified:` bumped 2026-05-05 → 2026-05-26
**Insertion point:** INSIDE existing H2 table (between separator at line 124 and existing newest row at line 125), newest-first convention; new row becomes new line 125 (existing row shifts to line 126)

### H2/H3 Anchor Table

[Populate this table from the contents of _tmp-glossary-anchors-pre.txt. The Phase 65 format uses 5 columns: Line | Level | Heading Text | Slug | Notes. Convert each line of _tmp-glossary-anchors-pre.txt into a table row. Level is determined by leading `#` count: `#` = H1, `##` = H2, `###` = H3. Slug is the lowercased heading text with spaces → `-` and special chars stripped, per standard GitHub Markdown anchor slug conventions. Notes column references PITFALL-6 invariant for line 121.]

| Line | Level | Heading Text | Slug | Notes |
|------|-------|-------------|------|-------|
| <enumerate every line from _tmp-glossary-anchors-pre.txt as a row> | ... | ... | ... | ... |
| 121 | H2 | `Version History` | `version-history` | MUST remain at line 121 post-edit (PITFALL-6 anti-regression boundary per STATE.md:125 + CONTEXT.md D-01) |

### Load-Bearing Anchor Flags

- `#version-history` (line 121): SWEEP-02 coordinating row append target. The H2 slug MUST remain `version-history` post-edit (append does not rename the heading); line position MUST remain 121 (insert is INSIDE the table at L125, no new H2 promoted). Per CONTEXT.md D-01 + STATE.md:125 PITFALL-6 contract.
- Any anchor at or above line 121 must remain at its current line number; any anchor at or below line 121 is allowed to shift (the table grows by 1 row).

### Post-Edit Verification Protocol

After Wave 5 edits land but BEFORE `git add`:

```powershell
Select-String -Path docs/_glossary-macos.md -Pattern '^#' | ForEach-Object {
  "{0,4} | {1}" -f $_.LineNumber, $_.Line
} | Out-File .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-glossary-anchors-post.txt -Encoding utf8

git diff --no-index .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-glossary-anchors-pre.txt .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-glossary-anchors-post.txt
```

Expected: ZERO output lines (no anchor shift). Any output → rollback condition; fix the table edit before continuing. The post-edit diff is embedded inline below at Wave 6.
```

(The actual anchor enumeration table is filled in from the `_tmp-glossary-anchors-pre.txt` capture; the executor expands the placeholder `<enumerate every line ...>` into the actual rows it reads from the temp file.)

**WAVE 2 — EXECUTE 6 LINE REWRITES (verbatim per CONTEXT.md D-03 rows #1, #2b, #3, #4, #5, #6b — do NOT renegotiate text)**

Apply edits in the order below; use the `Edit` tool with exact-line precision. Each edit MUST match the BEFORE block in `<interfaces>` exactly; if pre-edit state differs from the snippet (e.g., line drifted to a different number), STOP and investigate before proceeding.

EDIT-A (D-03 row #1) — `docs/admin-setup-ios/05-app-deployment.md` line 71:
Replace the BEFORE line with the AFTER text from `<interfaces>` D-03 ROW #1. After this edit, the file is byte-equivalent to pre-edit except line 71 is now: `- Content token (formerly VPP location token; still labeled "Apple VPP token" in Intune) uploaded to Intune (Tenant administration > Connectors and tokens > Apple VPP tokens)`

EDIT-B (D-03 row #3) — `docs/admin-setup-macos/04-app-deployment.md` line 45:
Replace per `<interfaces>` D-03 ROW #3. After: `- Apple Business Manager account with an active content token (formerly VPP location token; still labeled "Apple VPP token" in Intune)`

EDIT-C (D-03 row #4) — `docs/admin-setup-macos/04-app-deployment.md` line 46:
Replace per `<interfaces>` D-03 ROW #4. After: `- Content token uploaded to Intune (Tenant administration > Connectors and tokens > Apple VPP tokens)`

EDIT-D (D-03 row #5) — `docs/admin-setup-macos/04-app-deployment.md` line 113:
Replace per `<interfaces>` D-03 ROW #5. After: `2. Verify the content token (formerly VPP location token; still labeled "Apple VPP token" in Intune) is active and synced.`

EDIT-E (D-03 row #2b) — `docs/admin-setup-ios/05-app-deployment.md` line 201 (table cell, FIRST cell rename only — `VPP (Apps and Books) location token` → `Content token`; rest of row byte-identical):
Replace per `<interfaces>` D-03 ROW #2b.

EDIT-F (D-03 row #6b) — `docs/admin-setup-macos/04-app-deployment.md` line 148 (table cell, FIRST cell rename only — `VPP location token` → `Content token`; rest of row byte-identical):
Replace per `<interfaces>` D-03 ROW #6b.

**WAVE 3 — INSERT 2 CALLOUT BLOCKS (verbatim per CONTEXT.md D-03 rows #2a and #6a — same blockquote text both files)**

INSERT-G (D-03 row #2a) — `docs/admin-setup-ios/05-app-deployment.md`:
Insert the callout block from `<interfaces>` D-03 ROW #2a (blockquote + trailing blank line) at the blank line currently at line 196 (between line 195 `Configuration-Caused Failures` table last row and line 197 H2 `## Renewal / Maintenance`). After insert, the H2 `## Renewal / Maintenance` shifts from line 197 to ~line 199; the table row currently at line 201 shifts to ~line 203 (which is where EDIT-E lands; ensure EDIT-E targets the post-INSERT-G line by reading the post-insert state OR applying EDIT-E BEFORE INSERT-G since the table cell line content is independent of the insert position).

**Edit ordering note:** EDIT-E and INSERT-G both touch `docs/admin-setup-ios/05-app-deployment.md`. The safest ordering is:
1. Apply EDIT-E first (target line 201 by its pre-insert content — the BEFORE block contains the full table row text, unambiguous)
2. Apply INSERT-G second (target the blank line at line 196 by its pre-insert position)

Same ordering applies for `docs/admin-setup-macos/04-app-deployment.md`: EDIT-F at line 148 first, INSERT-H at line 143 second.

INSERT-H (D-03 row #6a) — `docs/admin-setup-macos/04-app-deployment.md`:
Insert the callout block from `<interfaces>` D-03 ROW #6a (SAME blockquote text as INSERT-G) at the blank line currently at line 143 (between line 142 end-of-`## Configuration-Caused Failures` and line 144 H2 `## Renewal / Maintenance`).

**WAVE 4 — APPEND 2 TAIL-TABLE VH ROWS + 2 FRONTMATTER LAST_VERIFIED BUMPS (per CONTEXT.md D-03 VH-iOS + VH-mac rows)**

APPEND-VH-iOS — `docs/admin-setup-ios/05-app-deployment.md`:
Append the VH-iOS row from `<interfaces>` to the existing tail-table at lines 224-227. Position: insert as the LAST row of the table (after the existing line-227 row `| 2026-04-16 | Initial version ...`). Do NOT add blank lines between rows. Do NOT promote any heading.

APPEND-VH-mac — `docs/admin-setup-macos/04-app-deployment.md`:
Append the VH-mac row from `<interfaces>` to the existing tail-table at lines 162-165. Position: insert as the LAST row of the table (after the existing line-165 row `| 2026-04-14 | Initial version -- DMG, PKG ...`).

BUMP-FM-iOS — `docs/admin-setup-ios/05-app-deployment.md:2`:
Replace `last_verified: 2026-04-18` with `last_verified: 2026-05-26`. Other frontmatter fields byte-identical.

BUMP-FM-mac — `docs/admin-setup-macos/04-app-deployment.md:2`:
Replace `last_verified: 2026-04-14` with `last_verified: 2026-05-26`. Other frontmatter fields byte-identical.

**WAVE 5 — APPEND GLOSSARY COORDINATING ROW + GLOSSARY FRONTMATTER BUMP**

APPEND-GLOSSARY-ROW — `docs/_glossary-macos.md`:
Insert the new row (per `<interfaces>` "Glossary `## Version History` H2 coordinating row") at line 125, immediately after the separator at line 124. The existing row currently at line 125 (`| 2026-05-05 | Phase 59 (CLEAN-08) ...`) shifts to line 126.

Concrete insert text:
```
| 2026-05-26 | Phase 67 (SWEEP-02): coordinating row for VPP location token → content token surgical rename in admin-setup-ios/05- + admin-setup-macos/04-app-deployment.md (PITFALLS.md CI-2 closure) | -- |
```

PITFALL-6 anti-regression: do NOT add blank lines between rows; do NOT modify the H2 `## Version History` at line 121 (it stays at line 121 post-edit — intra-table insert).

BUMP-FM-GLOSSARY — `docs/_glossary-macos.md:2`:
Replace `last_verified: 2026-05-05` with `last_verified: 2026-05-26` (per Claude's Discretion for traceability symmetry — the file's H2 table is edited; per 67-PATTERNS.md:606 the planner's discretion to bump is exercised here for symmetry).

**WAVE 6 — SIDECAR ANNOTATE 6 ENTRIES (ci_2_vpp_location_token entries get `"resolved_2026_05_26": true`)**

EDIT-SIDECAR — `scripts/validation/v1.6-audit-allowlist.json` lines 87-92:

For EACH of the 6 entries in `c13_rotting_external.ci_2_vpp_location_token`, append the new field `, "resolved_2026_05_26": true` immediately before the closing `}`. The new field is the LAST property in the entry object (matching the precedent where `category` was the closing field). Net diff: exactly 6 lines changed; each line gains `, "resolved_2026_05_26": true` immediately before the `}` close brace. 6-entry array shape preserved; single-line compact shape preserved.

AFTER state (verbatim for verification — all 6 entries):

```json
"ci_2_vpp_location_token": [
  { "file": "docs/admin-setup-ios/05-app-deployment.md", "line": 71, "term": "VPP (Apps and Books) location token", "context": "prerequisites bullet", "reason": "legacy 'VPP location token' term retained per Q5(b) no-corpus-sweep contract; rename deferred to v1.7+ CI-2", "category": "legacy_term_surgical_rename_candidate", "resolved_2026_05_26": true },
  { "file": "docs/admin-setup-ios/05-app-deployment.md", "line": 201, "term": "VPP (Apps and Books) location token", "context": "Renewal/Maintenance table row", "reason": "PITFALLS.md:642 canonical CI-2 site #1", "category": "legacy_term_surgical_rename_candidate", "resolved_2026_05_26": true },
  { "file": "docs/admin-setup-macos/04-app-deployment.md", "line": 45, "term": "VPP location token", "context": "prerequisites bullet", "reason": "legacy term; rename deferred to v1.7+", "category": "legacy_term_surgical_rename_candidate", "resolved_2026_05_26": true },
  { "file": "docs/admin-setup-macos/04-app-deployment.md", "line": 46, "term": "VPP location token", "context": "prerequisites bullet", "reason": "legacy term; rename deferred to v1.7+", "category": "legacy_term_surgical_rename_candidate", "resolved_2026_05_26": true },
  { "file": "docs/admin-setup-macos/04-app-deployment.md", "line": 113, "term": "VPP location token", "context": "verification step", "reason": "legacy term; rename deferred to v1.7+", "category": "legacy_term_surgical_rename_candidate", "resolved_2026_05_26": true },
  { "file": "docs/admin-setup-macos/04-app-deployment.md", "line": 148, "term": "VPP location token", "context": "Renewal/Maintenance table row", "reason": "PITFALLS.md:642 canonical CI-2 site #2", "category": "legacy_term_surgical_rename_candidate", "resolved_2026_05_26": true }
],
```

Note: the sidecar `line` numbers (71, 201, 45, 46, 113, 148) are PRESERVED as-is even though the post-edit corpus lines have shifted by the callout inserts (line 201 → ~203 on iOS; line 148 → ~150 on macOS). The sidecar `line` field is a historical record of WHERE the rename was applied per the calibrated enumeration at HEAD `ad5c9c9` (v1.6 close baseline); per CONTEXT.md D-04 ANNOTATE-not-remove, entries are NOT re-derived to current state — they record the pre-rename state plus the resolution annotation. Phase 70 HARNESS-02 will fork `v1.7-audit-allowlist.json` with these entries reset to post-sweep state (or dropped entirely) per the v1.7 clean-slate; the v1.6 sidecar retains the annotated history.

JSON parse guard (MANDATORY immediately after edit):

```
node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); console.log('OK')"
```

Expected: prints `OK`. If parse error, fix BEFORE proceeding.

Line-ending sanity check:

```
git diff scripts/validation/v1.6-audit-allowlist.json
```

Expected: ~6 lines of diff (6 entries gained one field each). If the entire file shows as changed, CRLF/LF drift occurred — revert with `git checkout scripts/validation/v1.6-audit-allowlist.json` and re-edit using LF preservation.

**WAVE 7 — POST-EDIT ANCHOR INVENTORY DIFF (PITFALL-6 ZERO-SHIFT VERIFICATION)**

```powershell
Select-String -Path docs/_glossary-macos.md -Pattern '^#' | ForEach-Object {
  "{0,4} | {1}" -f $_.LineNumber, $_.Line
} | Out-File .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-glossary-anchors-post.txt -Encoding utf8

git diff --no-index .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-glossary-anchors-pre.txt .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-glossary-anchors-post.txt
```

EXPECTED: ZERO output lines. The `## Version History` H2 stays at line 121 (intra-table row append shifts only lines INSIDE and BELOW the table; H2 line position is unaffected). All other anchors at line ≤ 121 also unshifted. Lines ≥ 122 may shift by +1 (the new row pushes existing rows down).

Embed the post-edit diff result inline at the end of `67-ANCHOR-INVENTORY.md` under a new H2:

```markdown
## Post-Edit Verification (Wave 7 — captured 2026-05-26)

`git diff --no-index` between pre-edit and post-edit anchor snapshots:

```
<paste the actual diff output here — expected empty>
```

**Result:** ZERO anchor shift ≥ line 121 (PITFALL-6 invariant satisfied; commit may proceed).
```

If the diff is NON-EMPTY at lines ≤ 121, ABORT — revert the glossary edit and re-apply with care for blank-line discipline. Do NOT proceed to commit.

**WAVE 8 — PRE-COMMIT DRY-RUN TRIPLE (MANDATORY before `git add`)**

```
node scripts/validation/v1.6-milestone-audit.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 1 FAILED: harness exit $LASTEXITCODE — C11/C14/C15/C16 should be inert on SWEEP-02 surface per CONTEXT.md D-03 line 99" }
node scripts/validation/check-phase-62.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 2 FAILED: V-62-SIDECAR c16==0 broken?" }
node scripts/validation/check-phase-66.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 3 FAILED: V-66-02 c13 shape broken?" }
```

ALL 3 MUST exit 0 (check-phase-66.mjs may report 5 SKIPPED per CHAIN_SKIP {48,51,58,60,61}; SKIPPED is NOT failure). If ANY fail, fix the underlying issue and re-run; do NOT `git add` until green.

If `check-phase-66.mjs` fails with "V-66-02 ... unexpected shape" OR "JSON parse error": revert the sidecar edit and re-apply with care for the JSON closing-brace placement. If `v1.6-milestone-audit.mjs` fails with a C11 or C15 hit: surface the specific banned-phrase and window-keyword combination that fired; this would falsify CONTEXT.md D-03 line 99 harness-inertia claim and require planner re-engagement.

**WAVE 9 — ATOMIC INDIVISIBLE COMMIT**

```
git add docs/admin-setup-ios/05-app-deployment.md docs/admin-setup-macos/04-app-deployment.md docs/_glossary-macos.md scripts/validation/v1.6-audit-allowlist.json .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-ANCHOR-INVENTORY.md

git commit -m "$(cat <<'EOF'
docs(67-02): SWEEP-02 — VPP location token -> content token surgical rename (6 occurrences / 2 files) + sidecar resolved + 2 VH rows

Atomic-within-plan commit per CONTEXT.md D-04 LOCKED Option E (per-plan boundary; clean revert per requirement). Packages 6 line rewrites + 2 callout block inserts + 2 tail-table Version History rows + 1 glossary coordinating row + 3 frontmatter last_verified bumps + 6 sidecar entry annotations + 1 PITFALL-6 anchor inventory artifact.

Corpus edits per CONTEXT.md D-03 table VERBATIM (rows #1, #2a, #2b, #3, #4, #5, #6a, #6b):
- docs/admin-setup-ios/05-app-deployment.md
  * L71 (D-03 #1): "VPP (Apps and Books) location token" -> "Content token (formerly VPP location token; still labeled \"Apple VPP token\" in Intune)" -- first prose mention in ## Prerequisites > ### VPP Prerequisites H2 (compound form per PITFALLS:657)
  * L196 (D-03 #2a): INSERT blockquote callout above ## Renewal / Maintenance H2 -- Apple-vs-Intune label disambiguation (OP-10 pattern from Phase 64 11-vpp-catalog-runbook.md:35-39)
  * L201 -> ~L203 post-callout (D-03 #2b): table cell "VPP (Apps and Books) location token" -> "Content token" (short form in tables)
  * Tail-table L228 append (D-03 VH-iOS): Phase 67 (SWEEP-02) ... PITFALLS.md CI-2 closure
  * Frontmatter L2 bump: last_verified 2026-04-18 -> 2026-05-26

- docs/admin-setup-macos/04-app-deployment.md
  * L45 (D-03 #3): "Apple Business Manager account with active VPP location token" -> "...with an active content token (formerly VPP location token; still labeled \"Apple VPP token\" in Intune)" -- first prose mention in ## Prerequisites > ### VPP/Apps and Books Prerequisites H2
  * L46 (D-03 #4): "VPP location token uploaded to Intune" -> "Content token uploaded to Intune" (subsequent mention, short form)
  * L113 (D-03 #5): "Verify the VPP location token is active and synced" -> "Verify the content token (formerly VPP location token; still labeled \"Apple VPP token\" in Intune) is active and synced" -- first prose mention in ## VPP / Apps and Books H2 (compound form fires again at new H2)
  * L143 (D-03 #6a): INSERT same blockquote callout above ## Renewal / Maintenance H2 (one per doc)
  * L148 -> ~L150 post-callout (D-03 #6b): table cell "VPP location token" -> "Content token"
  * Tail-table L166 append (D-03 VH-mac): Phase 67 (SWEEP-02) ... PITFALLS.md CI-2 closure
  * Frontmatter L2 bump: last_verified 2026-04-14 -> 2026-05-26

- docs/_glossary-macos.md (PITFALL-6 scope file per STATE.md:125 + CONTEXT.md D-01)
  * L125 (newest-first insert into existing ## Version History H2 table at L121): coordinating row referencing both SWEEP-02 file edits
  * Frontmatter L2 bump: last_verified 2026-05-05 -> 2026-05-26 (Claude's Discretion for traceability symmetry)
  * PITFALL-6 invariant: ## Version History H2 remains at L121 post-edit (verified by Wave 7 anchor inventory zero-shift diff)

Sidecar update (scripts/validation/v1.6-audit-allowlist.json c13_rotting_external.ci_2_vpp_location_token):
- All 6 entries gained "resolved_2026_05_26": true (ANNOTATE-not-remove per D-04; 6-entry array shape preserved -- V-66-02 stable)
- Note: sidecar 'line' fields preserved as v1.6-close baseline (71, 201, 45, 46, 113, 148); post-edit corpus lines have shifted by callout inserts but the sidecar records the calibrated enumeration at HEAD ad5c9c9, not current-state
- Phase 70 HARNESS-02 forks v1.7-audit-allowlist.json with c13_rotting_external reset (Annotate->Reset transition)

PITFALL-6 anchor inventory (67-ANCHOR-INVENTORY.md NEW):
- Pre-edit + post-edit anchor enumeration for docs/_glossary-macos.md only (sole STATE.md:125 scope file)
- Wave 7 diff confirmed zero shift >= line 121

Validator inertia confirmed (by construction per CONTEXT.md D-03 line 99):
- C15 appleBusinessDocPaths() at v1.6-milestone-audit.mjs:93-124 EXCLUDES both SWEEP-02 files
- C11 banned patterns do NOT match 'content token' / 'Apple VPP token' / 'VPP location token'
- C14 rebrand-callout 3-site check unaffected (not a callout-site edit on the 3 canonical Apple Business pages)
- V-66-02 sidecar shape stable (c13_rotting_external still object + quarterly_audit.cadence preserved)

Pre-commit dry-run triple passed: v1.6-milestone-audit.mjs + check-phase-62.mjs + check-phase-66.mjs all exit 0 modulo CHAIN_SKIP {48,51,58,60,61}.

Rollback: git revert <THIS_SHA> cleanly restores v1.6-close corpus baseline for the 2 SWEEP-02 files + glossary + sidecar byte-identically (per-plan boundary per CONTEXT.md D-04 Option E line 153-154).

SWEEP-02
EOF
)"
```

**WAVE 10 — POST-COMMIT VERIFICATION**

1. Atomic commit boundary:

```
git log --name-only -1 HEAD
```

EXPECTED output (exactly 5 files):
- docs/admin-setup-ios/05-app-deployment.md
- docs/admin-setup-macos/04-app-deployment.md
- docs/_glossary-macos.md
- scripts/validation/v1.6-audit-allowlist.json
- .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-ANCHOR-INVENTORY.md

If any other file appears, revert + redo with correct file selection.

2. Identical exit codes vs dry-run (post-commit chain stability):

```
node scripts/validation/v1.6-milestone-audit.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT harness exit $LASTEXITCODE" }
node scripts/validation/check-phase-62.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT check-phase-62 exit $LASTEXITCODE" }
node scripts/validation/check-phase-66.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT check-phase-66 exit $LASTEXITCODE" }
```

ALL 3 MUST exit 0 modulo CHAIN_SKIP. If anything regresses post-commit (despite green dry-run), investigate immediately — chain green-gate has regressed.

3. Substring checks confirming the rename landed:

```powershell
# All 6 D-03 rewrites present in their target files
(Select-String -Path docs/admin-setup-ios/05-app-deployment.md -Pattern 'Content token \(formerly VPP location token; still labeled "Apple VPP token" in Intune\)' | Measure-Object).Count   # expect >= 1
(Select-String -Path docs/admin-setup-macos/04-app-deployment.md -Pattern 'content token \(formerly VPP location token; still labeled "Apple VPP token" in Intune\)' | Measure-Object).Count   # expect >= 2 (L45 + L113)
(Select-String -Path docs/admin-setup-macos/04-app-deployment.md -Pattern '^- Content token uploaded to Intune' | Measure-Object).Count   # expect >= 1 (L46)

# 2 callout blocks present
(Select-String -Path docs/admin-setup-ios/05-app-deployment.md -Pattern '> \*\*Note:\*\* Apple calls this artifact a "content token"' | Measure-Object).Count   # expect 1
(Select-String -Path docs/admin-setup-macos/04-app-deployment.md -Pattern '> \*\*Note:\*\* Apple calls this artifact a "content token"' | Measure-Object).Count   # expect 1

# 2 tail-table VH rows present
(Select-String -Path docs/admin-setup-ios/05-app-deployment.md -Pattern '\| 2026-05-26 \| Phase 67 \(SWEEP-02\)' | Measure-Object).Count   # expect 1
(Select-String -Path docs/admin-setup-macos/04-app-deployment.md -Pattern '\| 2026-05-26 \| Phase 67 \(SWEEP-02\)' | Measure-Object).Count   # expect 1

# Glossary coordinating row present
(Select-String -Path docs/_glossary-macos.md -Pattern '\| 2026-05-26 \| Phase 67 \(SWEEP-02\): coordinating row' | Measure-Object).Count   # expect 1

# Frontmatter bumps present
(Select-String -Path docs/admin-setup-ios/05-app-deployment.md -Pattern '^last_verified: 2026-05-26' | Measure-Object).Count   # expect 1
(Select-String -Path docs/admin-setup-macos/04-app-deployment.md -Pattern '^last_verified: 2026-05-26' | Measure-Object).Count   # expect 1
(Select-String -Path docs/_glossary-macos.md -Pattern '^last_verified: 2026-05-26' | Measure-Object).Count   # expect 1

# Sidecar 6 entries annotated
(Select-String -Path scripts/validation/v1.6-audit-allowlist.json -Pattern '"resolved_2026_05_26": true' | Measure-Object).Count   # expect 6

# Legacy term sites are GONE from the 2 SWEEP-02 corpus files
(Select-String -Path docs/admin-setup-ios/05-app-deployment.md -Pattern 'VPP \(Apps and Books\) location token' -SimpleMatch | Where-Object { $_.Line -notmatch 'Phase 67' -and $_.Line -notmatch 'formerly' } | Measure-Object).Count   # expect 0 (the only surviving mentions are inside the VH row text and the "formerly ..." parenthetical)
(Select-String -Path docs/admin-setup-macos/04-app-deployment.md -Pattern 'VPP location token' -SimpleMatch | Where-Object { $_.Line -notmatch 'Phase 67' -and $_.Line -notmatch 'formerly' } | Measure-Object).Count   # expect 0
```

If any of the substring checks fail, the rename did NOT land as specified — investigate and fix the failing edit; commit may need to be amended (per CONTEXT.md D-04 Option E, atomic boundary is at the requirement level — within-plan amend is acceptable IF still in the same atomic commit; if the amend would create a second commit, revert and redo).
  </action>
  <verify>
    <automated>(git log --name-only -1 HEAD | Select-String -Pattern '05-app-deployment\.md|04-app-deployment\.md|_glossary-macos\.md|v1\.6-audit-allowlist\.json|67-ANCHOR-INVENTORY\.md' | Measure-Object).Count ; if ($? -and (node scripts/validation/v1.6-milestone-audit.mjs ; $LASTEXITCODE) -ne 0) { Write-Error "harness FAIL post-commit" } ; if ((node scripts/validation/check-phase-62.mjs ; $LASTEXITCODE) -ne 0) { Write-Error "check-62 FAIL post-commit" } ; if ((node scripts/validation/check-phase-66.mjs ; $LASTEXITCODE) -ne 0) { Write-Error "check-66 FAIL post-commit" } ; node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); const a=j.c13_rotting_external.ci_2_vpp_location_token; if(a.length!==6) process.exit(1); if(a.filter(e=>e.resolved_2026_05_26===true).length!==6) process.exit(2)" ; if ($LASTEXITCODE -ne 0) { Write-Error "sidecar annotation count FAIL" } ; (Select-String -Path docs/_glossary-macos.md -Pattern '^## Version History' | Where-Object { $_.LineNumber -eq 121 } | Measure-Object).Count</automated>
    <expected>git log shows 5 file names from the regex set; harness + check-phase-62 + check-phase-66 all exit 0 (modulo CHAIN_SKIP); sidecar ci_2 entries=6 AND all 6 carry resolved_2026_05_26:true; `## Version History` H2 in `_glossary-macos.md` still at line 121 (PITFALL-6 invariant).</expected>
  </verify>
  <done>SWEEP-02 atomic-within-plan commit landed at HEAD with EXACTLY 5 files (2 corpus + 1 glossary + 1 sidecar + 1 anchor-inventory artifact); 6 D-03 line rewrites + 2 callout inserts + 2 tail-table VH rows + 1 glossary coordinating row + 3 frontmatter `last_verified:` bumps + 6 sidecar annotations + Phase 67 pre-edit + post-edit anchor inventory artifact all landed; PITFALL-6 anchor zero-shift verified at Wave 7 (## Version History H2 stays at line 121); pre-commit dry-run + post-commit verify identical exit codes (3 validators all exit 0 modulo CHAIN_SKIP); substring checks confirm rename completeness (0 surviving primary legacy-term occurrences in the 2 SWEEP-02 corpus files; "formerly ..." parentheticals + VH row mentions allowed); single `git revert <SHA>` cleanly restores v1.6-close baseline byte-identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-commit dry-run ↔ working-tree state | The 3-validator dry-run runs against unstaged working-tree edits (validators read from disk via fs.readFileSync); identical post-commit exit codes prove the dry-run was a faithful preview. |
| Atomic commit ↔ chain green-gate stability | Single git commit atomically transitions the chain from "Phase 67-01 close-gate baseline" to "Phase 67-02 SWEEP-02 post-state"; intermediate commits would red V-66-02 if sidecar JSON parses partial state. |
| PITFALL-6 anchor invariant ↔ glossary table row append | The `## Version History` H2 at line 121 must remain at line 121 post-edit; intra-table row inserts (not new H2 promotion) preserve this. Wave 7 diff is the empirical evidence. |
| Sidecar `ci_2_vpp_location_token` 6-entry shape ↔ V-66-02 object-shape assertion | V-66-02 enforces object + `quarterly_audit.cadence` literal at check-phase-66.mjs:85-112; per-entry annotations are free-text. Adding `resolved_2026_05_26:true` to 6 entries does NOT change shape — verified by post-commit V-66-02 PASS. |
| Validator inertia claim (C11/C14/C15 don't fire on SWEEP-02 surface) ↔ harness re-run | The claim from CONTEXT.md D-03 line 99 is empirically tested by Wave 8 dry-run + Wave 10 post-commit verify; any C11/C14/C15 hit at execution time would falsify the inertia claim and require planner re-engagement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-02-D03 | Tampering (text renegotiation drift) | The 6 D-03 line rewrites + 2 callout block inserts + 2 VH rows | mitigate | All 8 edit blocks are LOCKED in CONTEXT.md D-03 table verbatim; `<interfaces>` pastes them verbatim from D-03; executor uses Edit tool with exact BEFORE/AFTER text. Any paraphrase or substitution by the executor is a contract violation and must be reverted before commit. The plan explicitly forbids renegotiation: "do NOT renegotiate text in plan-phase". |
| T-67-02-PF6 | Tampering (PITFALL-6 anchor drift) | `docs/_glossary-macos.md` `## Version History` H2 line position | mitigate | Wave 1 pre-edit anchor inventory captures the baseline; Wave 7 post-edit diff via `git diff --no-index` empirically verifies zero shift ≥ line 121; non-empty diff is an explicit ABORT/rollback condition before commit. The H2 line position is stable by construction (intra-table row insert at L125 does NOT shift the H2 at L121). |
| T-67-02-JS | Tampering (JSON syntax corruption from sidecar edit) | `scripts/validation/v1.6-audit-allowlist.json` 6-entry annotation | mitigate | JSON parse guard `node -e "JSON.parse(...)"` runs immediately after Wave 6 edit (must print OK); `git diff` size check confirms ~6 lines changed (entire-file diff means CRLF/LF drift, triggers revert+redo); V-66-02 in check-phase-66.mjs fails fast on parse error with exact error message. 6-entry array shape preserved (V-66-02 doesn't enforce entry count but post-commit substring check verifies 6 `resolved_2026_05_26` matches). |
| T-67-02-VI | Tampering (silent validator regression from rename) | C11/C14/C15 banned-pattern + window-keyword firing on new prose | mitigate | Pre-commit dry-run STEP 1 (v1.6-milestone-audit.mjs) is the canary; CONTEXT.md D-03 line 99 + 67-RESEARCH.md A3 + the C15 scope read at execution time prove harness inertia by construction (both SWEEP-02 files outside C15 scope per appleBusinessDocPaths(); C11 banned patterns don't match 'content token' / 'Apple VPP token' / 'VPP location token'; C14 callout-site check unaffected). Post-commit verify re-confirms. |
| T-67-02-A1 | Tampering (atomic violation) | Splitting the SWEEP-02 commit across multiple SHAs | mitigate | Plan structure enforces ONE task with atomic indivisible commit per CONTEXT.md D-04 LOCKED Option E. Post-commit `git log --name-only -1 HEAD` confirms all 5 files in one SHA; if not, revert + redo. Within-plan amend (before push) is acceptable; second commit is forbidden (violates clean-revert-per-requirement). |
| T-67-02-AR | Tampering (anti-regression — accidental hub/capability matrix/older workflow edit) | Phase 67 MUST NOT touch hub files, capability matrices, or older workflow files | mitigate | `<files>` block enumerates 5 files explicitly (only these may be staged); STATE.md:122-130 anti-regression invariants explicitly cited; `git log --name-only -1 HEAD` post-verify confirms file set is the closed 5. The 3 additional VPP-location-token sites in `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` (lines 115/149/155) are OUT of Phase 67 scope per 67-CONTEXT.md "Out of scope" finding — Phase 70 v1.7-DEFERRED-CLEANUP.md will pick them up; Phase 67 stays at the calibrated 6 sites. |
| T-67-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO external packages. All edits are local Markdown rewrites + JSON additive annotation + planning artifact creation. No slopcheck applicable. |
</threat_model>

<verification>
**Plan 67-02 → SWEEP-02 verification targets:**

| Target | Command |
|--------|---------|
| Atomic commit boundary (exactly 5 files) | `(git log --name-only -1 HEAD \| Select-String -Pattern '05-app-deployment\.md\|04-app-deployment\.md\|_glossary-macos\.md\|v1\.6-audit-allowlist\.json\|67-ANCHOR-INVENTORY\.md' \| Measure-Object).Count` → expect `5` |
| Harness inertia confirmed | `node scripts/validation/v1.6-milestone-audit.mjs ; $LASTEXITCODE` → expect `0` |
| V-62-SIDECAR canary | `node scripts/validation/check-phase-62.mjs ; $LASTEXITCODE` → expect `0` |
| V-66-02 sidecar shape stable | `node scripts/validation/check-phase-66.mjs ; $LASTEXITCODE` → expect `0` (modulo CHAIN_SKIP) |
| Sidecar 6-entry shape + 6 annotations | `node -e "const a=JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')).c13_rotting_external.ci_2_vpp_location_token; console.log(a.length, a.filter(e=>e.resolved_2026_05_26===true).length)"` → expect `6 6` |
| PITFALL-6 anchor invariant (H2 still at line 121) | `(Select-String -Path docs/_glossary-macos.md -Pattern '^## Version History' \| Where-Object { $_.LineNumber -eq 121 } \| Measure-Object).Count` → expect `1` |
| Compound form at first-mention-per-H2 (iOS) | `(Select-String -Path docs/admin-setup-ios/05-app-deployment.md -Pattern 'Content token \(formerly VPP location token' \| Measure-Object).Count` → expect `>= 1` |
| Compound form at first-mention-per-H2 (macOS twice — 2 H2's) | `(Select-String -Path docs/admin-setup-macos/04-app-deployment.md -Pattern 'content token \(formerly VPP location token' \| Measure-Object).Count` → expect `>= 2` |
| Callout block present on iOS file | `(Select-String -Path docs/admin-setup-ios/05-app-deployment.md -Pattern '> \*\*Note:\*\* Apple calls this artifact a "content token"' \| Measure-Object).Count` → expect `1` |
| Callout block present on macOS file | `(Select-String -Path docs/admin-setup-macos/04-app-deployment.md -Pattern '> \*\*Note:\*\* Apple calls this artifact a "content token"' \| Measure-Object).Count` → expect `1` |
| Tail-table VH row (iOS) | `(Select-String -Path docs/admin-setup-ios/05-app-deployment.md -Pattern '\| 2026-05-26 \| Phase 67 \(SWEEP-02\)' \| Measure-Object).Count` → expect `1` |
| Tail-table VH row (macOS) | `(Select-String -Path docs/admin-setup-macos/04-app-deployment.md -Pattern '\| 2026-05-26 \| Phase 67 \(SWEEP-02\)' \| Measure-Object).Count` → expect `1` |
| Glossary coordinating row | `(Select-String -Path docs/_glossary-macos.md -Pattern '\| 2026-05-26 \| Phase 67 \(SWEEP-02\): coordinating row' \| Measure-Object).Count` → expect `1` |
| Frontmatter bumps all 3 files | `(Select-String -Path docs/admin-setup-ios/05-app-deployment.md,docs/admin-setup-macos/04-app-deployment.md,docs/_glossary-macos.md -Pattern '^last_verified: 2026-05-26' \| Measure-Object).Count` → expect `3` |
| Anchor inventory artifact exists | `Test-Path .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-ANCHOR-INVENTORY.md` → `True` |
</verification>

<success_criteria>
1. ONE git commit (single SHA) at HEAD touches exactly 5 files: docs/admin-setup-ios/05-app-deployment.md + docs/admin-setup-macos/04-app-deployment.md + docs/_glossary-macos.md + scripts/validation/v1.6-audit-allowlist.json + .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-ANCHOR-INVENTORY.md.
2. All 6 D-03 line rewrites land per CONTEXT.md D-03 table VERBATIM (rows #1, #2b, #3, #4, #5, #6b — first-cell-only on table cells; full-line replace on prose lines).
3. 2 OP-10-style blockquote callout blocks insert above each `## Renewal / Maintenance` H2 (one per doc) per CONTEXT.md D-03 rows #2a + #6a verbatim text.
4. `docs/admin-setup-ios/05-app-deployment.md` tail-table at L228 gains the VH-iOS row per D-03 verbatim; `docs/admin-setup-macos/04-app-deployment.md` tail-table at L166 gains the VH-mac row per D-03 verbatim. No new `## Version History` H2 promoted in either admin-setup-* file (D-01 invariant).
5. `docs/_glossary-macos.md` `## Version History` H2 at line 121 retains its line position; the table at line 121+ gains one coordinating row at line 125 (newest-first); PITFALL-6 anchor invariant satisfied (verified by Wave 7 zero-shift diff embedded in `67-ANCHOR-INVENTORY.md`).
6. Frontmatter `last_verified: 2026-05-26` is present on all 3 edited files (iOS + macOS SWEEP-02 + glossary).
7. `scripts/validation/v1.6-audit-allowlist.json` `c13_rotting_external.ci_2_vpp_location_token` retains its 6-entry array shape; each entry carries `"resolved_2026_05_26": true`; JSON parse via `node -e "JSON.parse(...)"` prints `OK`.
8. `67-ANCHOR-INVENTORY.md` exists with pre-edit anchor table + load-bearing anchor flags + Wave 7 post-edit zero-shift diff embedded inline.
9. Pre-commit dry-run triple (v1.6-milestone-audit.mjs + check-phase-62.mjs + check-phase-66.mjs) all exit 0 modulo CHAIN_SKIP {48,51,58,60,61}; identical exit codes post-commit.
10. NO hub files touched. NO capability matrices touched. NO older workflow files touched. NO PITFALL-6 anchor shift ≥ line 121 in `_glossary-macos.md` (anti-regression invariants per STATE.md:122-130 + REQUIREMENTS.md:64).
11. Single `git revert <Plan 67-02 SHA>` cleanly restores v1.6-close corpus baseline for the 2 SWEEP-02 files + glossary + sidecar byte-identically (per CONTEXT.md D-04 line 153-154 rollback semantics).
12. Substring check `(Select-String -Path docs/admin-setup-ios/05-app-deployment.md -Pattern 'VPP \(Apps and Books\) location token' -SimpleMatch | Where-Object { $_.Line -notmatch 'Phase 67' -and $_.Line -notmatch 'formerly' } | Measure-Object).Count` returns 0 (no surviving primary legacy mentions; "formerly ..." parentheticals + VH row + sidecar `term` field are the only surviving textual references and are intentional).
</success_criteria>

<output>
Create `.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-02-SUMMARY.md` with:
- Atomic commit SHA + `git log --name-only -1 HEAD` output verbatim (exactly 5 files listed)
- The 6 D-03 rewrites checklist (all 6 verbatim, with their actual post-edit line numbers — line 201 likely shifted to ~203 by INSERT-G, line 148 likely shifted to ~150 by INSERT-H)
- The 2 callout block insertions (one per doc, blank-line discipline preserved)
- The 2 tail-table VH rows + 1 glossary coordinating row
- The 3 frontmatter bumps (iOS 2026-04-18 → 2026-05-26 / macOS 2026-04-14 → 2026-05-26 / glossary 2026-05-05 → 2026-05-26)
- Sidecar diff summary: 6 entries annotated; entry count preserved at 6
- PITFALL-6 outcome: Wave 7 anchor zero-shift diff result (expected empty) embedded in 67-ANCHOR-INVENTORY.md
- Pre-commit dry-run + post-commit verification exit codes (3 validators)
- Wave 3 handoff: Plan 67-03 close-gate ready to execute the full chain re-run + 67-VERIFICATION.md finalization + traceability flips
- Anti-regression confirmation: zero hub/capability-matrix/older-workflow files touched
</output>
</content>
