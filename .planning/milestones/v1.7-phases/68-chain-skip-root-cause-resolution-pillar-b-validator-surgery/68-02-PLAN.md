---
phase: 68-chain-skip-root-cause-resolution-pillar-b-validator-surgery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/_lib/archive-path.mjs
  - scripts/validation/check-phase-31.mjs
  - scripts/validation/check-phase-48.mjs
  - scripts/validation/check-phase-60.mjs
  - scripts/validation/check-phase-62.mjs
  - scripts/validation/check-phase-63.mjs
  - scripts/validation/regenerate-supervision-pins.mjs
  - scripts/validation/v1.5-audit-allowlist.json
autonomous: true
requirements:
  - CHAIN-02
tags:
  - validation
  - archive-path-helper
  - sidecar-coord-rebase
  - self-test-lineage
  - chain-02
  - phase-68
  - pillar-b
  - validator-surgery

must_haves:
  truths:
    - "scripts/validation/_lib/archive-path.mjs is a NEW file exporting `resolveArchivedPhasePath(phaseSuffix, milestoneRoots = ['v1.5-phases'])` with `node:fs` `existsSync` + `node:path` `join` imports; backward-compatible single-arg form preferred; v1.6-archived callers in check-phase-62/63 pass `['v1.6-phases']` explicitly (per RESEARCH Open Question 1 + Edge Case §7 resolution)"
    - "Helper has zero side-effects (no shell, no eval, no network); pure filesystem `existsSync` probes; CALLER OWNS FAIL SEMANTICS (returns string or null, never throws) per CONTEXT D-02 docstring contract"
    - "check-phase-48.mjs:83 V-48-05 body uses `resolveArchivedPhasePath('48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md')` with explicit null-handling; check-phase-48 exits 0 with 7/7 PASS (was 5/7 pre-Plan-68-02; +2 from V-48-04 self-test cascade + V-48-05 archive-path)"
    - "check-phase-60.mjs:30 BROKEN_LINKS_INVENTORY + :32 CALIBRATION_DOC both replaced with helper calls; downstream consumers V-60-08 (line 152) + V-60-24 (line 257) + V-60-25 (line 274) handle `null` return cleanly; check-phase-60 exits 0 with 25/25 PASS"
    - "check-phase-31.mjs:32 silent-swallow bug closed (STRETCH per D-02 advisor + RESEARCH Open Question 4 resolution): parseInventory() now uses helper + returns `{ _missing: true, placeholders: [] }` marker; V-31-21 + V-31-24 callers updated to FAIL on `_missing` (mirrors existing `_parseError` discriminator pattern); both V-31-21 + V-31-24 continue to PASS post-fix (helper resolves `placeholder-inventory.json` at archived v1.3-phases path)"
    - "check-phase-62.mjs:41 ANCHOR_INVENTORY replaced with helper call passing `['v1.6-phases']` as milestoneRoots; check-phase-63.mjs:48 same; both validators retain existing PASS state without regression"
    - "regenerate-supervision-pins.mjs BASELINE_9 _glossary-android.md coords {79,81,181,198}→{80,82,182,199} at lines 408-411 (+1 Phase 62-07 banner shift); parseAllowlist() at line 422 + FAIL message at line 423 repointed from `v1.5-audit-allowlist.json` to `v1.6-audit-allowlist.json` (D-03 Option A-prime sidecar lineage repoint); --self-test exits 0 (was FAIL with 4 false-negatives + 7 false-positives + 1 un-pinned Tier-2 pre-edit)"
    - "Optional but recommended: new BASELINE_9 attribution comment line added after line 406 matching the existing 2026-04-26 / 2026-05-06 / 2026-05-25 attribution pattern, citing Phase 68 Plan 68-02 +1 banner-shift rebase + v1.5→v1.6 sidecar repoint rationale + forward-pointer to Phase 70 HARNESS-02 v1.7 sidecar repoint"
    - "v1.5-audit-allowlist.json BROAD rebase per RESEARCH Open Question 2 resolution + advisor D-04 §2.3: supervision_exemptions[] (9 _glossary-android.md entries +1 each at idx 0..8) + c7_knox_allowlist[] (planner enumerates via v1.6 sidecar diff; +1 each for _glossary-android.md entries) + cope_banned_phrases[] (planner enumerates; line 203 is empirical C9 FAIL location); reason field gains `Phase 62 carry-over: line N shifted +1 (Phase 62 Plan 62-07 added Apple Business banner line at line 13)` prefix matching v1.6 sidecar attribution shape verbatim; array shape + length unchanged (only `line` integer + `reason` string mutate per entry); non-_glossary-android.md entries UNTOUCHED"
    - "v1.5-milestone-audit.mjs exits 0 with 12/12 PASS in fully-blocking mode (C2 supervision + C7 bare-Knox + C9 COPE banned-phrase all PASS post-rebase; was 9/12 pre-Plan-68-02); V-60-23 PASS as cascade consequence (check-phase-60 subprocess line 240 is the v1.5-milestone-audit harness invocation)"
    - "v1.6-milestone-audit.mjs continues to exit 0 with 15/15 PASS (unchanged baseline; v1.6 sidecar untouched)"
    - "Cascade verification post-Plan-68-02 (BEFORE Plan 68-04 lands): check-phase-60 exits 0 (25/25 PASS — V-60-08 + V-60-23 + V-60-24 + V-60-25 all PASS via helper + sidecar rebase); check-phase-61 exits with 2 FAIL (V-61-19 + V-61-20 — these are Plan 68-04 scope, NOT Plan 68-02 scope)"
  artifacts:
    - path: scripts/validation/_lib/archive-path.mjs
      provides: "Shared helper `resolveArchivedPhasePath(phaseSuffix, milestoneRoots = ['v1.5-phases'])` — pure filesystem path resolver; returns string OR null; caller owns FAIL semantics"
      contains:
        - "export function resolveArchivedPhasePath"
        - "import { existsSync } from 'node:fs'"
        - "import { join } from 'node:path'"
        - "'.planning/phases/' + phaseSuffix"
        - "'.planning/milestones/'"
    - path: scripts/validation/check-phase-48.mjs
      provides: "V-48-05 check body uses helper instead of hardcoded archive path"
      contains:
        - "resolveArchivedPhasePath('48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md')"
        - "import { resolveArchivedPhasePath } from './_lib/archive-path.mjs'"
    - path: scripts/validation/check-phase-60.mjs
      provides: "BROKEN_LINKS_INVENTORY + CALIBRATION_DOC const declarations use helper"
      contains:
        - "BROKEN_LINKS_INVENTORY = resolveArchivedPhasePath"
        - "CALIBRATION_DOC = resolveArchivedPhasePath"
        - "import { resolveArchivedPhasePath } from './_lib/archive-path.mjs'"
    - path: scripts/validation/check-phase-31.mjs
      provides: "parseInventory() uses helper + returns _missing marker on missing file (silent-swallow bug closed)"
      contains:
        - "resolveArchivedPhasePath"
        - "_missing: true"
        - "import { resolveArchivedPhasePath } from './_lib/archive-path.mjs'"
    - path: scripts/validation/check-phase-62.mjs
      provides: "ANCHOR_INVENTORY const uses helper with explicit v1.6-phases milestoneRoots"
      contains:
        - "resolveArchivedPhasePath('62-apple-business-foundation-rebrand/62-ANCHOR-INVENTORY.md'"
        - "['v1.6-phases']"
        - "import { resolveArchivedPhasePath } from './_lib/archive-path.mjs'"
    - path: scripts/validation/check-phase-63.mjs
      provides: "ANCHOR_INVENTORY const uses helper with explicit v1.6-phases milestoneRoots"
      contains:
        - "resolveArchivedPhasePath('63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md'"
        - "['v1.6-phases']"
        - "import { resolveArchivedPhasePath } from './_lib/archive-path.mjs'"
    - path: scripts/validation/regenerate-supervision-pins.mjs
      provides: "BASELINE_9 +1 banner-shift rebase (4 _glossary-android.md coords) + parseAllowlist sidecar lineage repointed v1.5→v1.6 + new BASELINE_9 attribution comment"
      contains:
        - "['docs/_glossary-android.md', 80]"
        - "['docs/_glossary-android.md', 82]"
        - "['docs/_glossary-android.md', 182]"
        - "['docs/_glossary-android.md', 199]"
        - "v1.6-audit-allowlist.json"
        - "Phase 68 Plan 68-02"
        - "Phase 62-07"
    - path: scripts/validation/v1.5-audit-allowlist.json
      provides: "Broad rebase: supervision_exemptions[] + c7_knox_allowlist[] + cope_banned_phrases[] +1 line-coord shift for ALL _glossary-android.md entries (matches v1.6 sidecar post-Phase-62-07 state)"
      contains:
        - "\"supervision_exemptions\""
        - "\"c7_knox_allowlist\""
        - "\"cope_banned_phrases\""
        - "Phase 62 carry-over: line"
        - "shifted +1"
        - "Phase 62 Plan 62-07 added Apple Business banner line at line 13"
  key_links:
    - from: "scripts/validation/check-phase-{48,60,31,62,63}.mjs"
      to: "scripts/validation/_lib/archive-path.mjs `resolveArchivedPhasePath()`"
      via: "ESM import `import { resolveArchivedPhasePath } from './_lib/archive-path.mjs'`"
      pattern: "import\\s*\\{\\s*resolveArchivedPhasePath\\s*\\}\\s*from\\s*'\\./_lib/archive-path\\.mjs'"
    - from: "scripts/validation/regenerate-supervision-pins.mjs parseAllowlist() line 422"
      to: "scripts/validation/v1.6-audit-allowlist.json (post-Phase-66-02 +1-rebased coords)"
      via: "1-line lineage repoint v1.5 → v1.6"
      pattern: "parseAllowlist\\('scripts/validation/v1\\.6-audit-allowlist\\.json'\\)"
    - from: "scripts/validation/v1.5-audit-allowlist.json supervision_exemptions[] + c7_knox_allowlist[] + cope_banned_phrases[]"
      to: "docs/_glossary-android.md (post-Phase-62-07 banner-shifted coords)"
      via: "+1 line-coord rebase per _glossary-android.md entry; reason field gains Phase 62 attribution prefix matching v1.6 sidecar verbatim"
      pattern: "\"line\":\\s*(17|50|70|80|82|83|123|182|196|199|203)"
    - from: "scripts/validation/check-phase-60.mjs V-60-23 subprocess invocation at line 240"
      to: "scripts/validation/v1.5-milestone-audit.mjs (now PASS with 12/12 after broad rebase)"
      via: "subprocess exec; V-60-23 PASS as cascade consequence of v1.5 sidecar broad rebase"
      pattern: "v1\\.5-milestone-audit\\.mjs"
---

<objective>
Resolve CHAIN-02 per CONTEXT.md D-02 (Option B helper) + D-03 (Option A-prime self-test lineage repoint) + D-04 §2.3 (broad sidecar rebase for V-60-23). Single coordinated validator-surgery commit landing 3 surgical fixes in 4 waves:

1. **Wave 1 — new helper** `scripts/validation/_lib/archive-path.mjs` exports `resolveArchivedPhasePath(phaseSuffix, milestoneRoots = ['v1.5-phases'])` (extended signature per RESEARCH Open Question 1 + Edge Case §7 — backward-compatible single-arg form preferred; v1.6-archived callers in check-phase-62/63 pass `['v1.6-phases']` explicitly).

2. **Wave 2 — call-site replacements** across 5 validators: check-phase-48.mjs:83 (V-48-05 body) + check-phase-60.mjs:30+32 (BROKEN_LINKS_INVENTORY + CALIBRATION_DOC const declarations) + check-phase-31.mjs:32 (STRETCH per D-02 + RESEARCH Open Q4: parseInventory() silent-swallow bug closed via `_missing` discriminator marker) + check-phase-62.mjs:41 + check-phase-63.mjs:48 (ANCHOR_INVENTORY consts with explicit v1.6-phases milestoneRoots).

3. **Wave 3 — regenerate-supervision-pins.mjs** BASELINE_9 _glossary-android.md coords {79,81,181,198}→{80,82,182,199} at lines 408-411 (+1 Phase 62-07 banner shift); parseAllowlist() at line 422 + FAIL message at line 423 repointed v1.5→v1.6 sidecar; new BASELINE_9 attribution comment added after line 406 (D-03 advisor §"Edit 3 optional but recommended").

4. **Wave 4 — v1.5-audit-allowlist.json BROAD rebase** per advisor D-04 §2.3 + RESEARCH Open Q2: supervision_exemptions[] (9 _glossary-android.md entries +1 each) + c7_knox_allowlist[] (planner enumerates via v1.6 sidecar diff) + cope_banned_phrases[] (planner enumerates; line 203 is empirical C9 FAIL location). Each affected `line` field +1; each `reason` field gains Phase 62 attribution prefix matching v1.6 sidecar verbatim. Non-_glossary-android.md entries UNTOUCHED.

**Purpose:** Close V-60-23 (v1.5-milestone-audit.mjs 12/12 PASS in fully-blocking mode) + V-48-05 (archive-path resolution) + helper-level self-test lineage staleness + check-phase-31 pre-existing silent-swallow data-integrity bug — all in one coordinated commit per CONTEXT D-04 "single commit, validator-only-surgery" topology. The atomic-commit constraint applies to CHAIN-03 only; CHAIN-02 is single-coordinated-non-atomic with `commit_strategy: single` (executor may stage incremental sub-commits during debugging but landing intent is one commit).

**Empirical groundings (per 68-RESEARCH.md):**
- V-60-23 root cause CONFIRMED: `v1.5-milestone-audit.mjs` C2 outputs `_glossary-android.md:17 ("Supervision"), :17 ("supervision"), :50 ("supervised")`; v1.5 sidecar references `:16/:49`; +1 banner shift from Phase 62-07.
- regenerate-supervision-pins.mjs --self-test FAILs as advisor predicted: 4 false-negatives + 7 false-positives + 1 un-pinned Tier-2 at :80; diff is EXACTLY the +1 banner shift.
- check-phase-66.mjs CHAIN_SKIP line CONFIRMED at :64 NOT :73 (Claude's-discretion question resolved).
- _lib/ subdirectory does NOT exist yet; Plan 68-02 creates it.

**Output:** ONE commit landing 8 file edits in coordinated logical sequence; v1.5-milestone-audit.mjs 12/12 PASS; check-phase-48 7/7 PASS; check-phase-60 25/25 PASS; check-phase-31 PASS (V-31-21 + V-31-24 preserved); regenerate-supervision-pins.mjs --self-test exits 0; check-phase-61 still FAILs V-61-19 + V-61-20 (Plan 68-04 scope, NOT this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-CONTEXT.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-RESEARCH.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-PATTERNS.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-VALIDATION.md
@scripts/validation/check-phase-31.mjs
@scripts/validation/check-phase-48.mjs
@scripts/validation/check-phase-60.mjs
@scripts/validation/check-phase-62.mjs
@scripts/validation/check-phase-63.mjs
@scripts/validation/regenerate-supervision-pins.mjs
@scripts/validation/v1.5-audit-allowlist.json
@scripts/validation/v1.6-audit-allowlist.json

<interfaces>
<!-- Exact line refs + before/after snippets. Executor MUST read every source file listed before editing. -->

**Wave 1 — Helper file content (NEW file `scripts/validation/_lib/archive-path.mjs` — extended signature per RESEARCH Open Q1 resolution):**

```
// scripts/validation/_lib/archive-path.mjs
// Resolves a phase artifact path that may live at either:
//   - .planning/phases/PHASEDIR/FILENAME           (live, pre-archival)
//   - .planning/milestones/<MILESTONE>/PHASEDIR/FILENAME  (archived at milestone close)
//
// Returns the resolved relative path (string) or null if neither exists.
// CALLER OWNS FAIL SEMANTICS — this helper does not throw and does not swallow.
//
// Lineage: introduced Phase 68 CHAIN-02 per REQUIREMENTS.md:20
//   handles BOTH pre-archival path AND post-archival path
//
// Backward-compatible single-arg form: defaults to ['v1.5-phases'] (CONTEXT D-02 locked).
// Callers needing v1.6-archived resolution pass milestoneRoots = ['v1.6-phases'] explicitly
// (e.g., check-phase-62.mjs + check-phase-63.mjs ANCHOR_INVENTORY consts).

import { existsSync } from 'node:fs';
import { join } from 'node:path';

export function resolveArchivedPhasePath(phaseSuffix, milestoneRoots = ['v1.5-phases']) {
  const live = '.planning/phases/' + phaseSuffix;
  if (existsSync(join(process.cwd(), live))) return live;
  for (const root of milestoneRoots) {
    const archived = '.planning/milestones/' + root + '/' + phaseSuffix;
    if (existsSync(join(process.cwd(), archived))) return archived;
  }
  return null;
}
```

The single-arg form `resolveArchivedPhasePath('48-...')` behaves identically to the CONTEXT D-02 locked signature (defaults milestoneRoots to ['v1.5-phases']); the optional second-arg form `resolveArchivedPhasePath('62-...', ['v1.6-phases'])` covers the Edge Case §7 ambiguity.

**Wave 2 — call-site replacements table (per 68-RESEARCH.md §Verbatim Edit Targets Plan 68-02 Wave 2):**

| # | Validator | Line | BEFORE | AFTER | Helper-arg signature |
|---|-----------|------|--------|-------|----------------------|
| 1 | check-phase-31.mjs | 32-33 | `const raw = readFile('.planning/phases/31-ios-l2-investigation/placeholder-inventory.json');` + `if (!raw) return { placeholders: [] };` | `const relPath = resolveArchivedPhasePath('31-ios-l2-investigation/placeholder-inventory.json', ['v1.3-phases']);` + `if (relPath === null) return { _missing: true, placeholders: [] };` + `const raw = readFile(relPath);` + `if (!raw) return { _missing: true, placeholders: [] };` | single-arg (v1.3-phases archive — file exists at `.planning/milestones/v1.3-phases/31-...` per D-02 advisor §4.7) |
| 2 | check-phase-48.mjs | 83 | `const relPath = '.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md';` (inside V-48-05 check 5 body at lines 79-93) | `const relPath = resolveArchivedPhasePath('48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md');` + add null-handling `if (relPath === null) return { pass: false, detail: '48-VERIFICATION-broken-links.md not found at .planning/phases/ or .planning/milestones/v1.5-phases/' };` immediately after | single-arg (defaults v1.5-phases) |
| 3 | check-phase-60.mjs | 30 | `const BROKEN_LINKS_INVENTORY = '.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md';` | `const BROKEN_LINKS_INVENTORY = resolveArchivedPhasePath('48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md');` (consumers V-60-08 line 152 + V-60-25 line 274 must handle `null` — add `if (BROKEN_LINKS_INVENTORY === null) return { pass: false, detail: 'archived doc not found at .planning/phases/ or .planning/milestones/v1.5-phases/' };` to each consumer body) | single-arg (defaults v1.5-phases) |
| 4 | check-phase-60.mjs | 32 | `const CALIBRATION_DOC = '.planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md';` | `const CALIBRATION_DOC = resolveArchivedPhasePath('60-audit-harness-v1-5-finalization/60-CALIBRATION.md');` (V-60-24 line 257 must handle `null` — add same `if (CALIBRATION_DOC === null) ...` guard) | single-arg (defaults v1.5-phases) |
| 5 | check-phase-62.mjs | 41 | `const ANCHOR_INVENTORY = '.planning/phases/62-apple-business-foundation-rebrand/62-ANCHOR-INVENTORY.md';` | `const ANCHOR_INVENTORY = resolveArchivedPhasePath('62-apple-business-foundation-rebrand/62-ANCHOR-INVENTORY.md', ['v1.6-phases']);` (V-62-ANCHORS consumer must handle `null` if file truly missing — add null-handling guard) | two-arg with `['v1.6-phases']` (Edge Case §7 resolution; file currently archived at `.planning/milestones/v1.6-phases/62-...`) |
| 6 | check-phase-63.mjs | 48 | `const ANCHOR_INVENTORY = '.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md';` | `const ANCHOR_INVENTORY = resolveArchivedPhasePath('63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md', ['v1.6-phases']);` (V-63-ANCHOR-INVENTORY consumer same null-handling guard) | two-arg with `['v1.6-phases']` (63-ANCHOR-INVENTORY.md currently still live at `.planning/phases/63-...` but will drift to v1.6-phases at Phase 70; helper resolves either) |

**Each call site MUST add the import at the top of the file (after existing `node:fs` / `node:path` imports):**

```
import { resolveArchivedPhasePath } from './_lib/archive-path.mjs';
```

**Wave 2 — check-phase-31.mjs STRETCH detail (per 68-PATTERNS.md §"Silent-swallow STRETCH fix at check-phase-31.mjs:32"):**

Pre-edit state (verified by Read 2026-05-26 — lines 31-39):

```
function parseInventory() {
  const raw = readFile('.planning/phases/31-ios-l2-investigation/placeholder-inventory.json');
  if (!raw) return { placeholders: [] };  // <-- silent-swallow data-integrity bug
  try {
    return JSON.parse(raw);
  } catch (err) {
    return { _parseError: err.message, placeholders: [] };  // <-- already-existing discriminator pattern
  }
}
```

Post-edit (extends existing `_parseError` discriminator pattern with `_missing` marker):

```
function parseInventory() {
  const relPath = resolveArchivedPhasePath('31-ios-l2-investigation/placeholder-inventory.json', ['v1.3-phases']);
  if (relPath === null) return { _missing: true, placeholders: [] };  // <-- caller-decides FAIL
  const raw = readFile(relPath);
  if (!raw) return { _missing: true, placeholders: [] };
  try {
    return JSON.parse(raw);
  } catch (err) {
    return { _parseError: err.message, placeholders: [] };
  }
}
```

**Caller-side update for V-31-21 + V-31-24:** Before iterating `inv.placeholders`, add a guard:

```
const inv = parseInventory();
if (inv._missing) return { pass: false, detail: 'placeholder-inventory.json not resolvable at .planning/phases/ or .planning/milestones/v1.3-phases/' };
if (inv._parseError) return { pass: false, detail: 'placeholder-inventory.json parse error: ' + inv._parseError };
// ... existing inv.placeholders iteration logic
```

**Wave 3 — regenerate-supervision-pins.mjs verbatim edits (per 68-RESEARCH.md §Verbatim Edit Targets Plan 68-02 Wave 3 + 68-PATTERNS.md §"regenerate-supervision-pins.mjs:408-411 + :422-423"):**

| # | Line | BEFORE | AFTER |
|---|------|--------|-------|
| A | 408 | `  ['docs/_glossary-android.md', 79],   // ### Supervision heading (was 65/76; 79 at Phase 59; stable post-Plan-06 — H3 sits before line 127)` | `  ['docs/_glossary-android.md', 80],   // ### Supervision heading (was 79 at Phase 59; +1 Phase 62-07 banner shift; H3 sits before line 127)` |
| B | 409 | `  ['docs/_glossary-android.md', 81],   // Supervision disambiguation blockquote (was 67/78; 81 at Phase 59; stable post-Plan-06)` | `  ['docs/_glossary-android.md', 82],   // Supervision disambiguation blockquote (was 81 at Phase 59; +1 Phase 62-07 banner shift)` |
| C | 410 | `  ['docs/_glossary-android.md', 181],  // MHS cross-platform note (was 134/172/179; 181 post-Plan-06 +2 from #kme/#kpe shims)` | `  ['docs/_glossary-android.md', 182],  // MHS cross-platform note (was 181 at Phase 60-06; +1 Phase 62-07 banner shift)` |
| D | 411 | `  ['docs/_glossary-android.md', 198],  // Version History row (was 148/188/196; 198 post-Plan-06 +2)` | `  ['docs/_glossary-android.md', 199],  // Version History row (was 198 at Phase 60-06; +1 Phase 62-07 banner shift)` |
| E | 422 | `  const allow = parseAllowlist('scripts/validation/v1.5-audit-allowlist.json');` | `  const allow = parseAllowlist('scripts/validation/v1.6-audit-allowlist.json');` |
| F | 423 | `    process.stderr.write('FAIL: sidecar missing at scripts/validation/v1.5-audit-allowlist.json\n');` | `    process.stderr.write('FAIL: sidecar missing at scripts/validation/v1.6-audit-allowlist.json\n');` |

**Wave 3 — new BASELINE_9 attribution comment (insert AFTER line 406, BEFORE the array literal at line 407, matching the existing 2026-04-26 / 2026-05-06 / 2026-05-25 attribution shape per D-03 advisor §"Edit 3 optional but recommended"):**

```
// BASELINE_9 refreshed 2026-05-26 (Phase 68 Plan 68-02): _glossary-android.md entries +1 line-coord
// shift to absorb Phase 62-07 Apple Business reciprocal-governance banner at _glossary-android.md:13;
// parseAllowlist() lineage repointed v1.5 -> v1.6 sidecar (closes D-03 dogfood-test FAIL; helper
// honors anti-tolerance directive at line 499 -- tightens via lineage repoint, not relaxes via
// tolerance window). v1.6 sidecar already carries the +1-rebased coords per Phase 66-02 atomic
// commit 3a9a671 (BASELINE_10 freshness comment). Forward-pointer: Phase 70 HARNESS-02 repoints
// parseAllowlist() to v1.7 sidecar at that future commit via 1-line edit (line 422).
```

V-60-09's substring assertion `/BASELINE_9 refreshed 2026-05-06 \(Phase 60 Plan 08\)/` (at check-phase-60.mjs:170) continues to PASS because the existing 2026-05-06 attribution line is NOT removed; the new 2026-05-26 attribution is ADDED below it.

**Wave 4 — v1.5-audit-allowlist.json BROAD rebase (per 68-RESEARCH.md §Verbatim Edit Targets Plan 68-02 Wave 4 + 68-PATTERNS.md §"v1.5-audit-allowlist.json broad sidecar coord rebase"):**

**Diff-driven enumeration:** Use the v1.6 sidecar as the source-of-truth (it already carries the post-Phase-62-07 +1-rebased coords from Phase 66-02 commit 3a9a671). Run this enumeration helper to discover the exact array indices that need rebase:

```
node -e "const v15=JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8')); const v16=JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); for(const key of ['supervision_exemptions','c7_knox_allowlist','cope_banned_phrases','safetynet_exemptions']) { console.log('=== '+key+' ==='); const a15=v15[key]||[]; const a16=v16[key]||[]; for(let i=0;i<Math.max(a15.length,a16.length);i++){ const e15=a15[i]; const e16=a16[i]; if(e15 && e16 && e15.file===e16.file && e15.file==='docs/_glossary-android.md' && e15.line!==e16.line){ console.log('  idx '+i+' '+e15.file+' v1.5:'+e15.line+' v1.6:'+e16.line+' shift:'+(e16.line-e15.line)); } } }"
```

Expected output (per 68-RESEARCH.md §V-60-23 Diagnosis empirical confirmation):

```
=== supervision_exemptions ===
  idx 0 docs/_glossary-android.md v1.5:79 v1.6:80 shift:1
  idx 1 docs/_glossary-android.md v1.5:81 v1.6:82 shift:1
  idx 2 docs/_glossary-android.md v1.5:181 v1.6:182 shift:1
  idx 3 docs/_glossary-android.md v1.5:198 v1.6:199 shift:1
  idx 4 docs/_glossary-android.md v1.5:16 v1.6:17 shift:1
  idx 5 docs/_glossary-android.md v1.5:49 v1.6:50 shift:1
  idx 6 docs/_glossary-android.md v1.5:69 v1.6:70 shift:1
  idx 7 docs/_glossary-android.md v1.5:82 v1.6:83 shift:1
  idx 8 docs/_glossary-android.md v1.5:195 v1.6:196 shift:1
=== c7_knox_allowlist ===
  (planner enumerates; expect +1 for each _glossary-android.md entry)
=== cope_banned_phrases ===
  (planner enumerates; line 203 is empirical C9 FAIL location)
=== safetynet_exemptions ===
  (C1 currently PASSes — verify safetynet stays passing after rebase; rebase only if entries shifted)
```

**Per-entry rebase pattern (each affected entry in JSON):**

```
// BEFORE (entry idx 0 in supervision_exemptions[])
{"file": "docs/_glossary-android.md", "line": 79, "reason": "Phase 59 carry-over: line 76 shifted +3 (Phase 59 CLEAN-08 added see-also lines to BYOD/UE/ZTE before COBO section, net +3); ### Supervision H3 heading (term being defined; iOS supervision callout; intentional AEAUDIT-04 pattern)"},

// AFTER (mirrors v1.6 sidecar entry idx 0 verbatim)
{"file": "docs/_glossary-android.md", "line": 80, "reason": "Phase 62 carry-over: line 79 shifted +1 (Phase 62 Plan 62-07 added Apple Business banner line at line 13); Phase 59 carry-over: line 76 shifted +3 (Phase 59 CLEAN-08 added see-also lines to BYOD/UE/ZTE before COBO section, net +3); ### Supervision H3 heading (term being defined; iOS supervision callout; intentional AEAUDIT-04 pattern)"},
```

**Rebase rules:**
- Each affected `line` field: integer +1 (matches v1.6 sidecar `line` field exactly)
- Each affected `reason` field: PREPEND `Phase 62 carry-over: line N shifted +1 (Phase 62 Plan 62-07 added Apple Business banner line at line 13); ` (matches v1.6 sidecar verbatim — semicolon + space separator)
- Array shape + length UNCHANGED (only `line` integer + `reason` string mutate per entry)
- Non-_glossary-android.md entries UNTOUCHED (banner is at _glossary-android.md:13 only)
- After ALL entries edited across the 4 arrays, run JSON parse guard (mandatory per 67-PATTERNS.md sidecar pattern):

```
node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8')); console.log('OK')"
```

Expected: `OK`. If anything else, fix broken brace/comma BEFORE proceeding.

**Pre-edit baselines (MANDATORY capture before editing):**

```
node scripts/validation/regenerate-supervision-pins.mjs --self-test 2>&1 | Select-Object -Last 25 ; Write-Host "self-test exit=$LASTEXITCODE"
# Expected pre-edit: FAIL with 4 false-negatives + 7 false-positives + 1 un-pinned Tier-2 at :80; exit=1

node scripts/validation/v1.5-milestone-audit.mjs 2>&1 | Select-Object -Last 15 ; Write-Host "v1.5-harness exit=$LASTEXITCODE"
# Expected pre-edit: "Summary: 9 passed, 3 failed, 0 skipped" (C2, C7, C9 FAIL); exit=1

node scripts/validation/check-phase-48.mjs 2>&1 | Select-Object -Last 5 ; Write-Host "check-phase-48 exit=$LASTEXITCODE"
# Expected pre-edit: 5/7 PASS, 2 FAIL (V-48-04 self-test cascade + V-48-05 archive-path); exit=1

node scripts/validation/check-phase-31.mjs 2>&1 | Select-Object -Last 5 ; Write-Host "check-phase-31 exit=$LASTEXITCODE"
# Expected pre-edit: PASS (current state); silent-swallow bug means it passes even when file is missing

node scripts/validation/check-phase-60.mjs 2>&1 | Select-Object -Last 5 ; Write-Host "check-phase-60 exit=$LASTEXITCODE"
# Expected pre-edit: FAIL (cascades from V-60-08 + V-60-23 + V-60-24 + V-60-25 + self-test cascade)
```

**Post-edit verification (after Waves 1-4 ALL land):**

```
node scripts/validation/regenerate-supervision-pins.mjs --self-test 2>&1 | Select-Object -Last 10
# REQUIRED: "Diff: identical / Un-pinned Tier-2 count: 0 / Self-test: PASS"; exit=0

node scripts/validation/v1.5-milestone-audit.mjs 2>&1 | Select-Object -Last 3
# REQUIRED: "Summary: 12 passed, 0 failed, 0 skipped"; exit=0

node scripts/validation/check-phase-48.mjs 2>&1 | Select-Object -Last 3
# REQUIRED: "Result: 7 PASS, 0 FAIL, 0 SKIPPED"; exit=0

node scripts/validation/check-phase-31.mjs 2>&1 | Select-Object -Last 3
# REQUIRED: PASS; V-31-21 + V-31-24 continue to PASS (helper resolves file at archived v1.3-phases path; placeholders[] populated)

node scripts/validation/check-phase-60.mjs 2>&1 | Select-Object -Last 3
# REQUIRED: 25/25 PASS (V-60-08, V-60-23, V-60-24, V-60-25 all PASS); exit=0
```

**Cascade verification (must run AFTER Plan 68-02 lands, before Plan 68-03):**

```
node scripts/validation/check-phase-61.mjs 2>&1 | Select-Object -Last 3
# Expected post-Plan-68-02: 32/34 PASS, 2 FAIL (V-61-19, V-61-20 still FAIL until Plan 68-04 lands)

node scripts/validation/v1.6-milestone-audit.mjs 2>&1 | Select-Object -Last 3
# REQUIRED: 15/15 PASS (unchanged from baseline; v1.6 sidecar untouched)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 68-02-01: Create archive-path helper + 5 call-site replacements + check-phase-31 STRETCH silent-swallow fix + regenerate-supervision-pins BASELINE_9 + sidecar repoint + v1.5 sidecar broad rebase + pre/post verification + coordinated commit</name>
  <files>scripts/validation/_lib/archive-path.mjs, scripts/validation/check-phase-31.mjs, scripts/validation/check-phase-48.mjs, scripts/validation/check-phase-60.mjs, scripts/validation/check-phase-62.mjs, scripts/validation/check-phase-63.mjs, scripts/validation/regenerate-supervision-pins.mjs, scripts/validation/v1.5-audit-allowlist.json</files>
  <read_first>
    - scripts/validation/check-phase-48.mjs (read lines 1-100 — confirm V-48-05 check 5 body at lines 79-93 + readFile() helper at 22-26 + verify line 83 has the hardcoded path)
    - scripts/validation/check-phase-60.mjs (read lines 20-180 — confirm const cluster at lines 27-32 + BROKEN_LINKS_INVENTORY consumer at line 152 V-60-08 + CALIBRATION_DOC consumer at line 257 V-60-24 + V-60-25 consumer at line 274 + V-60-23 subprocess invocation at line 240)
    - scripts/validation/check-phase-31.mjs (read full file — confirm parseInventory() at lines 31-39 + V-31-21 caller + V-31-24 caller; identify exact location where `placeholders` is iterated for guard insertion)
    - scripts/validation/check-phase-62.mjs (read lines 30-50 — confirm ANCHOR_INVENTORY at line 41 + V-62-ANCHORS consumer; also `ls .planning/phases/62-apple-business-foundation-rebrand/ .planning/milestones/v1.6-phases/62-apple-business-foundation-rebrand/` to verify file location for helper resolution)
    - scripts/validation/check-phase-63.mjs (read lines 40-60 — confirm ANCHOR_INVENTORY at line 48 + V-63-ANCHOR-INVENTORY consumer; verify 63-ANCHOR-INVENTORY.md location)
    - scripts/validation/regenerate-supervision-pins.mjs (read lines 380-430 — confirm BASELINE_9 array at lines 407-417 + 4 _glossary-android.md entries at 408-411 + parseAllowlist call at line 422 + FAIL message at line 423 + existing attribution-comment pattern at 386-406 + pinned-Tier-2 doctrine at 472-476 + anti-tolerance directive at line 499)
    - scripts/validation/v1.5-audit-allowlist.json (read full file — enumerate all `_glossary-android.md` entries across supervision_exemptions[] + c7_knox_allowlist[] + cope_banned_phrases[] + safetynet_exemptions[] arrays; capture current `line` integers per array)
    - scripts/validation/v1.6-audit-allowlist.json (read full file — same enumeration; capture v1.6 `line` integers per array for diff-driven enumeration)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-CONTEXT.md (re-read D-02 at lines 65-103 + D-03 at lines 105-122 + D-04 §"Deferred Ideas / Discovered" at lines 331-336)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-RESEARCH.md (re-read §Verbatim Edit Targets Plan 68-02 Waves 1-4 at lines 144-230 + §V-60-23 Diagnosis at lines 606-642 + §Edge Cases §1, §4, §7 at lines 740-820 + §Open Questions 1, 2, 4 at lines 994-1013)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-PATTERNS.md (re-read §"scripts/validation/_lib/archive-path.mjs" at lines 57-121 + §"check-phase-48.mjs:83 + check-phase-60.mjs:30,32 + ..." at lines 188-292 + §"regenerate-supervision-pins.mjs" at lines 295-388 + §"scripts/validation/v1.5-audit-allowlist.json" at lines 391-446)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-VALIDATION.md (read lines 44-50 — Per-Task Verification Map entries 68-02-01 through 68-02-07)
  </read_first>
  <action>
**PRE-EDIT — Capture baselines (MANDATORY before any edit; per RESEARCH §Verification Harness Plan 68-02 pre-edit baselines):**

1. Capture pre-edit baselines:

```
node scripts/validation/regenerate-supervision-pins.mjs --self-test 2>&1 | Out-File -FilePath .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/_tmp-self-test-pre.txt
Write-Host "self-test pre-edit exit=$LASTEXITCODE"
# Expected: FAIL with 4 false-negatives + 7 false-positives + 1 un-pinned Tier-2 at :80; exit=1

node scripts/validation/v1.5-milestone-audit.mjs 2>&1 | Out-File -FilePath .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/_tmp-v15-harness-pre.txt
Write-Host "v1.5-harness pre-edit exit=$LASTEXITCODE"
# Expected: "Summary: 9 passed, 3 failed, 0 skipped" (C2, C7, C9 FAIL); exit=1

node scripts/validation/check-phase-48.mjs 2>&1 | Out-File -FilePath .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/_tmp-check-48-pre.txt
Write-Host "check-phase-48 pre-edit exit=$LASTEXITCODE"
# Expected: FAIL; exit=1

node scripts/validation/check-phase-60.mjs 2>&1 | Out-File -FilePath .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/_tmp-check-60-pre.txt
Write-Host "check-phase-60 pre-edit exit=$LASTEXITCODE"
# Expected: FAIL; exit=1
```

2. Run the v1.5 vs v1.6 sidecar diff enumeration to discover exact c7_knox_allowlist[] + cope_banned_phrases[] indices needing rebase (the supervision_exemptions[] map is already enumerated in RESEARCH §Verbatim Edit Targets Wave 4):

```
node -e "const v15=JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8')); const v16=JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); for(const key of ['supervision_exemptions','c7_knox_allowlist','cope_banned_phrases','safetynet_exemptions']) { console.log('=== '+key+' ==='); const a15=v15[key]||[]; const a16=v16[key]||[]; for(let i=0;i<Math.max(a15.length,a16.length);i++){ const e15=a15[i]; const e16=a16[i]; if(e15 && e16 && e15.file===e16.file && e15.file==='docs/_glossary-android.md' && e15.line!==e16.line){ console.log('  idx '+i+' '+e15.file+' v1.5:'+e15.line+' v1.6:'+e16.line+' shift:'+(e16.line-e15.line)); } else if(e15 && !e16 && e15.file==='docs/_glossary-android.md'){ console.log('  idx '+i+' '+e15.file+' v1.5:'+e15.line+' v1.6:MISSING'); } } }" | Out-File -FilePath .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/_tmp-sidecar-diff.txt -Encoding utf8

Get-Content .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/_tmp-sidecar-diff.txt
```

Use this enumeration as the authoritative rebase target list for Wave 4. If any array has entries showing `v1.6:MISSING`, that means v1.6 sidecar drops the entry entirely — verify by reading the v1.6 sidecar directly to decide whether to drop the v1.5 entry too.

**WAVE 1 — Create helper file `scripts/validation/_lib/archive-path.mjs`:**

3. Verify `scripts/validation/_lib/` directory does NOT exist yet:

```
Test-Path scripts/validation/_lib
# Expected: False
```

If True, the directory already exists — investigate before proceeding (this would suggest a prior partial implementation).

4. Create `scripts/validation/_lib/archive-path.mjs` with the extended-signature content (per `<interfaces>` Wave 1 — supports the `milestoneRoots = ['v1.5-phases']` default + optional v1.6-phases callers).

5. Verify the helper file works by importing it via a one-liner:

```
node -e "import('./scripts/validation/_lib/archive-path.mjs').then(m => { const r1 = m.resolveArchivedPhasePath('48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md'); const r2 = m.resolveArchivedPhasePath('62-apple-business-foundation-rebrand/62-ANCHOR-INVENTORY.md', ['v1.6-phases']); console.log('v1.5-archived:', r1); console.log('v1.6-archived:', r2); if (r1 === null) process.exit(1); if (r2 === null) console.warn('v1.6 file may have moved'); })"
# Expected: v1.5-archived: .planning/milestones/v1.5-phases/48-... / v1.6-archived: .planning/milestones/v1.6-phases/62-... OR null if 62-ANCHOR not archived yet
```

If r1 is null, the v1.5-phases file is missing — investigate (RESEARCH confirmed file exists at .planning/milestones/v1.5-phases/48-... per archived artifact sources). If r2 is null, check live `.planning/phases/62-...` location — file may still be live, in which case helper still works correctly (returns live path).

**WAVE 2 — Call-site replacements across 5 validators (per `<interfaces>` Wave 2 table):**

6. Edit `scripts/validation/check-phase-48.mjs`:
   - Add import at top (after existing `node:fs` import): `import { resolveArchivedPhasePath } from './_lib/archive-path.mjs';`
   - Edit V-48-05 check body at lines 79-93: replace the hardcoded `relPath` string-literal at line 83 with helper call (per `<interfaces>` Wave 2 table row #2); add null-handling guard immediately after

7. Edit `scripts/validation/check-phase-60.mjs`:
   - Add import at top (after existing `node:fs` import): `import { resolveArchivedPhasePath } from './_lib/archive-path.mjs';`
   - Replace `BROKEN_LINKS_INVENTORY` const at line 30 with helper call (per `<interfaces>` Wave 2 table row #3)
   - Replace `CALIBRATION_DOC` const at line 32 with helper call (per `<interfaces>` Wave 2 table row #4)
   - Update V-60-08 consumer body at line 152 to handle `null` return: add `if (BROKEN_LINKS_INVENTORY === null) return { pass: false, detail: 'archived doc not found at .planning/phases/ or .planning/milestones/v1.5-phases/' };` before existing readFile() call
   - Update V-60-24 consumer body at line 257 to handle `null` for CALIBRATION_DOC with same guard
   - Update V-60-25 consumer body at line 274 to handle `null` for BROKEN_LINKS_INVENTORY with same guard

8. Edit `scripts/validation/check-phase-31.mjs` (STRETCH per D-02 advisor + RESEARCH Open Q4):
   - Add import at top: `import { resolveArchivedPhasePath } from './_lib/archive-path.mjs';`
   - Edit parseInventory() at lines 31-39 per `<interfaces>` Wave 2 STRETCH detail block (use helper with `['v1.3-phases']` milestoneRoots; return `_missing` marker on missing file)
   - Update V-31-21 + V-31-24 callers: add the `_missing` + `_parseError` discriminator guards before iterating `inv.placeholders`
   - After edits, run `node scripts/validation/check-phase-31.mjs` and confirm V-31-21 + V-31-24 STILL PASS (the helper should resolve `placeholder-inventory.json` at `.planning/milestones/v1.3-phases/31-ios-l2-investigation/placeholder-inventory.json` per D-02 advisor §4.7; if it does NOT resolve, the STRETCH is too risky — revert STRETCH changes and only apply the silent-swallow fix to non-existent-file path)

9. Edit `scripts/validation/check-phase-62.mjs`:
   - Add import at top: `import { resolveArchivedPhasePath } from './_lib/archive-path.mjs';`
   - Edit line 41 ANCHOR_INVENTORY: use helper with `['v1.6-phases']` milestoneRoots per Edge Case §7 resolution
   - Update V-62-ANCHORS consumer to handle `null` return: add `if (ANCHOR_INVENTORY === null) return { pass: false, detail: '62-ANCHOR-INVENTORY.md not found at .planning/phases/ or .planning/milestones/v1.6-phases/' };` before existing usage

10. Edit `scripts/validation/check-phase-63.mjs`:
    - Same shape as step 9 for `63-ANCHOR-INVENTORY.md` at line 48 with `['v1.6-phases']` milestoneRoots

11. Verify Wave 2 edits via post-edit per-validator runs:

```
node scripts/validation/check-phase-48.mjs ; Write-Host "check-phase-48 post-Wave-2 exit=$LASTEXITCODE"
# Expected: 7/7 PASS, exit=0

node scripts/validation/check-phase-60.mjs ; Write-Host "check-phase-60 post-Wave-2 exit=$LASTEXITCODE"
# Expected: still FAIL until Wave 3 + Wave 4 land (V-60-23 needs v1.5 sidecar broad rebase)

node scripts/validation/check-phase-31.mjs ; Write-Host "check-phase-31 post-Wave-2 exit=$LASTEXITCODE"
# Expected: PASS (V-31-21 + V-31-24 preserved); silent-swallow bug closed

node scripts/validation/check-phase-62.mjs ; Write-Host "check-phase-62 post-Wave-2 exit=$LASTEXITCODE"
# Expected: PASS (modulo CHAIN_SKIP); V-62-ANCHORS resolves via helper

node scripts/validation/check-phase-63.mjs ; Write-Host "check-phase-63 post-Wave-2 exit=$LASTEXITCODE"
# Expected: PASS (modulo CHAIN_SKIP)
```

If check-phase-31 regresses (was PASS pre-edit, FAIL post-edit), the STRETCH is too risky given the existing silent-swallow PASS behavior. Per RESEARCH Open Q4 recommendation, STRETCH per D-02 advisor — but if V-31-21/V-31-24 fail, REVERT only the STRETCH changes (keep the helper import + silent-swallow fix that doesn't touch caller logic) and document deferred to v1.7-DEFERRED-CLEANUP.md (Plan 68-05 will capture).

**WAVE 3 — regenerate-supervision-pins.mjs BASELINE_9 + sidecar repoint:**

12. Edit `scripts/validation/regenerate-supervision-pins.mjs`:
    - Lines 408-411: update 4 _glossary-android.md coord entries per `<interfaces>` Wave 3 table edits A-D
    - Insert new BASELINE_9 attribution comment block AFTER line 406 BEFORE line 407 (the const declaration) per `<interfaces>` Wave 3 comment template
    - Line 422: update parseAllowlist arg from `v1.5-audit-allowlist.json` to `v1.6-audit-allowlist.json` (edit E)
    - Line 423: update FAIL message string from `v1.5-audit-allowlist.json` to `v1.6-audit-allowlist.json` (edit F)

13. Verify Wave 3 via self-test:

```
node scripts/validation/regenerate-supervision-pins.mjs --self-test 2>&1 | Select-Object -Last 15 ; Write-Host "self-test post-Wave-3 exit=$LASTEXITCODE"
# Expected: "Diff: identical / Un-pinned Tier-2 count: 0 / Self-test: PASS"; exit=0
```

If self-test still FAILS post-Wave-3 with a different diff pattern, investigate — likely BASELINE_9 array indices off (Read tool the array body to verify entries 1-9 in correct order; the 4 _glossary-android.md entries should be entries 1, 2, 3, 4 of the 9-entry array per RESEARCH 68-PATTERNS:319-326).

14. Verify V-60-09 substring assertion at check-phase-60.mjs:170 still passes (the 2026-05-06 attribution line MUST NOT be removed; the new 2026-05-26 attribution is ADDED below it):

```
node scripts/validation/check-phase-60.mjs 2>&1 | Select-String "V-60-09"
# Expected: PASS (still finds the 2026-05-06 attribution substring)
```

**WAVE 4 — v1.5-audit-allowlist.json BROAD rebase:**

15. Use the enumeration from step 2 (`_tmp-sidecar-diff.txt`) as the authoritative rebase target list. For EACH `_glossary-android.md` entry showing a `v1.5:N v1.6:N+1 shift:1` line in the enumeration, edit the corresponding JSON entry in `v1.5-audit-allowlist.json`:
    - Update `line` integer: from `N` to `N+1` (matches v1.6 sidecar exactly)
    - Update `reason` field: PREPEND `Phase 62 carry-over: line N shifted +1 (Phase 62 Plan 62-07 added Apple Business banner line at line 13); ` (note the trailing semicolon-space separator matches v1.6 sidecar verbatim)
    - DO NOT change `file` field
    - DO NOT alter array order or shape

Apply edits across these 4 array keys (process in this order):
- `supervision_exemptions[]` — 9 _glossary-android.md entries (idx 0..8 per RESEARCH §Verbatim Edit Targets Wave 4 entry-index map)
- `c7_knox_allowlist[]` — planner enumerates from step 2 output
- `cope_banned_phrases[]` — planner enumerates from step 2 output (verify line 203 is the empirical C9 FAIL location)
- `safetynet_exemptions[]` — planner enumerates from step 2 output; rebase ONLY if entries shifted (C1 currently PASSes — verify it stays passing after rebase)

For each batch of edits to an array, run the JSON parse guard:

```
node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8')); console.log('OK')"
# Expected: OK
```

If anything else, fix broken brace/comma BEFORE proceeding to next array.

16. Verify Wave 4 via v1.5-milestone-audit.mjs:

```
node scripts/validation/v1.5-milestone-audit.mjs 2>&1 | Select-Object -Last 15 ; Write-Host "v1.5-harness post-Wave-4 exit=$LASTEXITCODE"
# REQUIRED: "Summary: 12 passed, 0 failed, 0 skipped"; exit=0
# (C2 + C7 + C9 + C1 all PASS)
```

If v1.5 harness still reports any FAIL post-Wave-4, run with --verbose (if supported) to identify the un-rebased entry; fix and re-verify.

**WAVE 5 — Final verification + coordinated commit:**

17. Full post-edit verification suite:

```
node scripts/validation/regenerate-supervision-pins.mjs --self-test 2>&1 | Select-Object -Last 5 ; Write-Host "FINAL self-test exit=$LASTEXITCODE"
# REQUIRED: PASS, exit=0

node scripts/validation/v1.5-milestone-audit.mjs 2>&1 | Select-Object -Last 3 ; Write-Host "FINAL v1.5-harness exit=$LASTEXITCODE"
# REQUIRED: 12/12 PASS, exit=0

node scripts/validation/v1.6-milestone-audit.mjs 2>&1 | Select-Object -Last 3 ; Write-Host "FINAL v1.6-harness exit=$LASTEXITCODE"
# REQUIRED: 15/15 PASS, exit=0 (unchanged baseline)

node scripts/validation/check-phase-48.mjs 2>&1 | Select-Object -Last 3 ; Write-Host "FINAL check-phase-48 exit=$LASTEXITCODE"
# REQUIRED: 7/7 PASS, exit=0

node scripts/validation/check-phase-60.mjs 2>&1 | Select-Object -Last 3 ; Write-Host "FINAL check-phase-60 exit=$LASTEXITCODE"
# REQUIRED: 25/25 PASS, exit=0

node scripts/validation/check-phase-31.mjs 2>&1 | Select-Object -Last 3 ; Write-Host "FINAL check-phase-31 exit=$LASTEXITCODE"
# REQUIRED: PASS (V-31-21 + V-31-24 preserved)

node scripts/validation/check-phase-62.mjs 2>&1 | Select-Object -Last 3 ; Write-Host "FINAL check-phase-62 exit=$LASTEXITCODE"
# REQUIRED: PASS (modulo CHAIN_SKIP — still suppresses {48,51,58,60,61} until Plan 68-03)

node scripts/validation/check-phase-63.mjs 2>&1 | Select-Object -Last 3 ; Write-Host "FINAL check-phase-63 exit=$LASTEXITCODE"
# REQUIRED: PASS (modulo CHAIN_SKIP)

node scripts/validation/check-phase-61.mjs 2>&1 | Select-Object -Last 3 ; Write-Host "FINAL check-phase-61 exit=$LASTEXITCODE"
# Expected: 32/34 PASS, 2 FAIL (V-61-19 + V-61-20 — Plan 68-04 scope, NOT this plan)
```

If ANY required check fails, STOP and investigate before staging.

18. Coordinated commit (single SHA per CONTEXT D-04 "single commit, validator-only-surgery"):

```
git add scripts/validation/_lib/archive-path.mjs `
        scripts/validation/check-phase-31.mjs `
        scripts/validation/check-phase-48.mjs `
        scripts/validation/check-phase-60.mjs `
        scripts/validation/check-phase-62.mjs `
        scripts/validation/check-phase-63.mjs `
        scripts/validation/regenerate-supervision-pins.mjs `
        scripts/validation/v1.5-audit-allowlist.json

git status
# Expected: 8 files staged (1 new + 7 modified)
```

If `git status` shows any other modified files, STOP — investigate (this plan should touch ONLY these 8 files).

```
git commit -m "$(cat <<'EOF'
fix(validation): archive-path helper + self-test lineage repoint + v1.5 sidecar broad rebase (CHAIN-02)

Per CONTEXT.md D-02 Option B (helper) + D-03 Option A-prime (self-test lineage repoint) + D-04 §2.3 (broad sidecar rebase). Single coordinated validator-surgery commit landing 3 surgical fixes across 8 files:

1. NEW helper scripts/validation/_lib/archive-path.mjs exports resolveArchivedPhasePath(phaseSuffix, milestoneRoots=['v1.5-phases']). First inhabitant of scripts/validation/_lib/ subdirectory. Extended signature per RESEARCH Open Q1 — backward-compatible single-arg default + optional v1.6-phases callers in check-phase-62/63. Pure filesystem probe; CALLER OWNS FAIL SEMANTICS (returns string or null, never throws).

2. Call-site replacements across 5 validators:
   - check-phase-48.mjs:83 V-48-05 body — replaces hardcoded archive path with helper call; null-handling guard added
   - check-phase-60.mjs:30 BROKEN_LINKS_INVENTORY + :32 CALIBRATION_DOC — same helper replacement; V-60-08/24/25 consumer null-handling guards added
   - check-phase-31.mjs:32 STRETCH per D-02 advisor — parseInventory() silent-swallow data-integrity bug closed via _missing discriminator marker (extends existing _parseError discriminator pattern); V-31-21 + V-31-24 callers updated to FAIL on _missing instead of silently passing on empty placeholders; V-31-21/V-31-24 continue to PASS post-fix because helper resolves placeholder-inventory.json at archived v1.3-phases path
   - check-phase-62.mjs:41 + check-phase-63.mjs:48 ANCHOR_INVENTORY consts — helper called with ['v1.6-phases'] milestoneRoots per Edge Case §7 resolution

3. regenerate-supervision-pins.mjs BASELINE_9 _glossary-android.md coords {79,81,181,198}→{80,82,182,199} (+1 Phase 62-07 banner shift); parseAllowlist() at :422 + FAIL message at :423 repointed v1.5→v1.6 sidecar. Honors helper-author anti-tolerance directive at line 499 — tightens via lineage repoint, not relaxes via tolerance window. v1.6 sidecar already carries the +1-rebased coords per Phase 66-02 atomic commit 3a9a671 (BASELINE_10 freshness). New BASELINE_9 attribution comment added after line 406 matching the existing 2026-04-26 / 2026-05-06 / 2026-05-25 attribution pattern. V-60-09 substring assertion at check-phase-60.mjs:170 preserved (2026-05-06 attribution NOT removed).

4. v1.5-audit-allowlist.json BROAD rebase per advisor D-04 §2.3 + RESEARCH Open Q2: supervision_exemptions[] (9 _glossary-android.md entries +1 each) + c7_knox_allowlist[] + cope_banned_phrases[] (line 203 was empirical C9 FAIL location). Each affected entry's line field +1; reason field gains `Phase 62 carry-over: line N shifted +1 (Phase 62 Plan 62-07 added Apple Business banner line at line 13);` prefix matching v1.6 sidecar attribution shape verbatim. Array shape + length unchanged (only line integer + reason string mutate per entry). Non-_glossary-android.md entries UNTOUCHED.

Empirical confirmations (per 68-RESEARCH.md):
- V-60-23 root cause: v1.5-milestone-audit.mjs C2 outputs _glossary-android.md:17 ("Supervision"), :17 ("supervision"), :50 ("supervised"); v1.5 sidecar referenced :16/:49; +1 banner shift from Phase 62-07
- regenerate-supervision-pins.mjs --self-test pre-edit FAIL: 4 false-negatives + 7 false-positives + 1 un-pinned Tier-2 at :80; diff is EXACTLY the +1 banner shift
- _lib/ subdirectory was NEW; first inhabitant is archive-path.mjs

Post-edit verification:
- regenerate-supervision-pins.mjs --self-test: PASS (was FAIL)
- v1.5-milestone-audit.mjs: 12/12 PASS in fully-blocking mode (was 9/12; C2+C7+C9 all PASS)
- v1.6-milestone-audit.mjs: 15/15 PASS (unchanged baseline)
- check-phase-48.mjs: 7/7 PASS (was 5/7)
- check-phase-60.mjs: 25/25 PASS (V-60-08+23+24+25 all PASS via helper + sidecar rebase)
- check-phase-31.mjs: PASS (V-31-21+24 preserved; silent-swallow bug closed)
- check-phase-62/63.mjs: PASS (modulo CHAIN_SKIP — still suppresses {48,51,58,60,61} until Plan 68-03)
- check-phase-61.mjs: 32/34 PASS, 2 FAIL (V-61-19+20 — Plan 68-04 scope)

Forward-pointer: Phase 70 HARNESS-02 repoints parseAllowlist() to v1.7 sidecar via 1-line edit (line 422); HARNESS-03 Path-A copy inherits _lib/ helper transparently for check-phase-67..70.mjs.

Closed in this commit: check-phase-31.mjs silent-swallow data-integrity bug (pre-existing; D-02 advisor §4.7 bonus discovery; recorded in v1.7-DEFERRED-CLEANUP.md at Plan 68-05 as CHAIN-31 CLOSED entry).

Rollback: git revert <THIS_SHA> cleanly restores pre-edit baselines across all 8 files (per-plan boundary preserved per CONTEXT D-04 Option E).

CHAIN-02
EOF
)"
```

19. Post-commit verify:

```
git log --name-only -1 HEAD
# Expected: exactly 8 files (1 new + 7 modified per the staging list)
```

```
node scripts/validation/regenerate-supervision-pins.mjs --self-test ; Write-Host "POST-COMMIT self-test exit=$LASTEXITCODE"
node scripts/validation/v1.5-milestone-audit.mjs ; Write-Host "POST-COMMIT v1.5-harness exit=$LASTEXITCODE"
node scripts/validation/v1.6-milestone-audit.mjs ; Write-Host "POST-COMMIT v1.6-harness exit=$LASTEXITCODE"
node scripts/validation/check-phase-48.mjs ; Write-Host "POST-COMMIT check-phase-48 exit=$LASTEXITCODE"
node scripts/validation/check-phase-60.mjs ; Write-Host "POST-COMMIT check-phase-60 exit=$LASTEXITCODE"
node scripts/validation/check-phase-31.mjs ; Write-Host "POST-COMMIT check-phase-31 exit=$LASTEXITCODE"
node scripts/validation/check-phase-62.mjs ; Write-Host "POST-COMMIT check-phase-62 exit=$LASTEXITCODE"
node scripts/validation/check-phase-63.mjs ; Write-Host "POST-COMMIT check-phase-63 exit=$LASTEXITCODE"
# All MUST exit 0 (62/63 modulo CHAIN_SKIP)
```

Capture commit SHA for Plan 68-05 close-gate authoring (substituted as `{68_02_SHA}` in Plan 68-03's canonical CHAIN_SKIP comment block).
  </action>
  <verify>
    <automated>Test-Path scripts/validation/_lib/archive-path.mjs ; node -e "import('./scripts/validation/_lib/archive-path.mjs').then(m => { if (typeof m.resolveArchivedPhasePath !== 'function') process.exit(1) ; const r = m.resolveArchivedPhasePath('48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md') ; if (r === null) process.exit(2) ; console.log('helper ok:', r) })" ; if ($LASTEXITCODE -ne 0) { Write-Error "helper FAIL" } ; node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8')); console.log('JSON OK')" ; if ($LASTEXITCODE -ne 0) { Write-Error "v1.5 sidecar JSON broken" } ; node scripts/validation/regenerate-supervision-pins.mjs --self-test ; if ($LASTEXITCODE -ne 0) { Write-Error "self-test FAIL" } ; node scripts/validation/v1.5-milestone-audit.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "v1.5-harness FAIL (expect 12/12)" } ; node scripts/validation/v1.6-milestone-audit.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "v1.6-harness REGRESSION (expect 15/15)" } ; node scripts/validation/check-phase-48.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "check-phase-48 FAIL (expect 7/7)" } ; node scripts/validation/check-phase-60.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "check-phase-60 FAIL (expect 25/25)" } ; node scripts/validation/check-phase-31.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "check-phase-31 REGRESSION (V-31-21/24 must preserve)" } ; node scripts/validation/check-phase-62.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "check-phase-62 REGRESSION (modulo CHAIN_SKIP)" } ; node scripts/validation/check-phase-63.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "check-phase-63 REGRESSION (modulo CHAIN_SKIP)" }</automated>
    <expected>Helper file exists + exports resolveArchivedPhasePath + resolves 48-VERIFICATION at archived v1.5-phases; v1.5 sidecar JSON parses cleanly; self-test PASS; v1.5-harness 12/12 PASS; v1.6-harness 15/15 PASS unchanged; check-phase-48 7/7 PASS; check-phase-60 25/25 PASS; check-phase-31 PASS preserved; check-phase-62/63 PASS modulo CHAIN_SKIP.</expected>
  </verify>
  <done>scripts/validation/_lib/archive-path.mjs created (helper with extended signature); 5 call-site replacements landed across check-phase-{31,48,60,62,63}.mjs with null-handling guards; check-phase-31 silent-swallow bug CLOSED via _missing marker + V-31-21/V-31-24 preserved; regenerate-supervision-pins.mjs BASELINE_9 +1 banner shift (4 _glossary-android entries) + sidecar lineage repointed v1.5→v1.6 + new BASELINE_9 attribution comment added; v1.5-audit-allowlist.json BROAD rebase landed across supervision_exemptions+c7_knox_allowlist+cope_banned_phrases for all _glossary-android.md entries; v1.5-milestone-audit.mjs 12/12 PASS in fully-blocking mode (was 9/12); regenerate-supervision-pins.mjs --self-test PASS (was FAIL); check-phase-48 7/7 PASS; check-phase-60 25/25 PASS; check-phase-31 PASS preserved; v1.6-harness 15/15 PASS unchanged; ONE coordinated commit landed touching exactly 8 files; commit SHA captured for Plan 68-05 close-gate canonical-comment-block SHA substitution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New `_lib/` subdirectory ↔ existing validator import resolution | First inhabitant of scripts/validation/_lib/; ESM forward-slash import portable on Windows/Linux; no path-separator concerns per RESEARCH Edge Case §1 |
| Helper static-string input ↔ filesystem `existsSync` probe | Helper inputs are static phase-suffix strings from validator source code (never user-supplied); pure filesystem read; no path-traversal vector |
| Sidecar JSON broad-rebase ↔ v1.5 + v1.6 milestone harness consumers | v1.5 sidecar shape preserved (array length + per-entry keys unchanged); v1.6 sidecar untouched; cross-validator dependency surfaces unaffected |
| regenerate-supervision-pins.mjs --self-test ↔ v1.6 sidecar repoint | Sidecar lineage repoint is 1-line edit; schema keys compatible per RESEARCH Edge Case §4 (v1.6 has 2 extra keys c13_rotting_external + c16_missing_endpoint_exemptions but --self-test consumes only supervision_exemptions[] which is identically-shaped in both) |
| check-phase-31 STRETCH ↔ pre-existing PASS state | STRETCH could regress if helper resolution + caller guard interaction breaks V-31-21/V-31-24; mitigation = revert STRETCH if regression observed (per RESEARCH Open Q4 contingency) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02-PT | Tampering (path traversal via helper input) | scripts/validation/_lib/archive-path.mjs | mitigate | Helper inputs are static strings from validator source (never user-supplied); caller responsibility documented in helper docstring; pure-fs read with no shell/eval/network |
| T-68-02-JS | Tampering (JSON syntax corruption from broad-rebase) | scripts/validation/v1.5-audit-allowlist.json | mitigate | JSON parse guard runs immediately after each array's batch of edits: `node -e "JSON.parse(...)"` must print OK; if not OK, fix BEFORE proceeding |
| T-68-02-31 | Tampering (STRETCH regression of V-31-21/V-31-24 from caller-guard interaction) | scripts/validation/check-phase-31.mjs | mitigate | Run check-phase-31 immediately post-STRETCH; if V-31-21/V-31-24 FAIL where they previously PASSED, revert STRETCH and document in v1.7-DEFERRED-CLEANUP.md at Plan 68-05 |
| T-68-02-AR | Tampering (anti-regression — v1.6 sidecar accidental mutation) | scripts/validation/v1.6-audit-allowlist.json | mitigate | Plan 68-02 staging list explicitly excludes v1.6 sidecar; post-staging `git status` MUST show ONLY 8 files (1 new + 7 modified); v1.6-milestone-audit.mjs 15/15 PASS unchanged verifies no v1.6 mutation occurred |
| T-68-02-CASCADE | Tampering (cascade regression in chain validators 64/65/66) | scripts/validation/check-phase-{64,65,66}.mjs | mitigate | These validators NOT modified by Plan 68-02; their CHAIN_SKIP set {48,51,58,60,61} continues to suppress until Plan 68-03; pre + post Plan 68-02 exit codes IDENTICAL modulo CHAIN_SKIP semantics |
| T-68-02-SC | Tampering | no package installs in this plan | accept | Zero npm installs; helper uses node:fs + node:path built-ins only; no [ASSUMED]/[SUS] packages — no legitimacy checkpoint required |
</threat_model>

<verification>
**Plan 68-02 → CHAIN-02 verification targets (per 68-VALIDATION.md lines 44-50):**

| Target | Command | Expected |
|--------|---------|----------|
| Helper file exists + exports function | `Test-Path scripts/validation/_lib/archive-path.mjs ; node -e "import('./scripts/validation/_lib/archive-path.mjs').then(m => console.log(typeof m.resolveArchivedPhasePath))"` | `True` + `function` |
| Helper resolves v1.5-archived | `node -e "import('./scripts/validation/_lib/archive-path.mjs').then(m => console.log(m.resolveArchivedPhasePath('48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md')))"` | `.planning/milestones/v1.5-phases/48-...` |
| check-phase-48 PASS | `node scripts/validation/check-phase-48.mjs ; $LASTEXITCODE` | `0` (7/7 PASS) |
| check-phase-60 PASS | `node scripts/validation/check-phase-60.mjs ; $LASTEXITCODE` | `0` (25/25 PASS) |
| check-phase-31 PRESERVED | `node scripts/validation/check-phase-31.mjs ; $LASTEXITCODE` | `0` (V-31-21+24 preserve) |
| check-phase-62/63 modulo CHAIN_SKIP | `node scripts/validation/check-phase-62.mjs ; node scripts/validation/check-phase-63.mjs` | both exit `0` |
| self-test PASS | `node scripts/validation/regenerate-supervision-pins.mjs --self-test ; $LASTEXITCODE` | `0` |
| v1.5-harness 12/12 PASS | `node scripts/validation/v1.5-milestone-audit.mjs ; $LASTEXITCODE` | `0` (12/12 PASS in fully-blocking mode) |
| v1.6-harness UNCHANGED | `node scripts/validation/v1.6-milestone-audit.mjs ; $LASTEXITCODE` | `0` (15/15 PASS baseline) |
| v1.5 sidecar JSON valid | `node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8'))"` | (no error) |
| Atomic commit boundary | `git log --name-only -1 HEAD \| Measure-Object -Line` | 8 files (1 new + 7 modified) |
| Cascade verification (check-phase-61 pre-Plan-68-04 state) | `node scripts/validation/check-phase-61.mjs 2>&1 \| Select-String "V-61-19\|V-61-20"` | both FAIL (Plan 68-04 fixes) |
</verification>

<success_criteria>
1. `scripts/validation/_lib/archive-path.mjs` exists with `export function resolveArchivedPhasePath(phaseSuffix, milestoneRoots = ['v1.5-phases'])` + `node:fs` `existsSync` + `node:path` `join` imports; helper resolves both v1.5-archived (default) and v1.6-archived (explicit `['v1.6-phases']`) paths.
2. `check-phase-48.mjs:83` V-48-05 body uses helper with null-handling guard; check-phase-48 exits 0 with 7/7 PASS (was 5/7 pre-edit).
3. `check-phase-60.mjs:30 BROKEN_LINKS_INVENTORY` + `:32 CALIBRATION_DOC` both use helper with consumer null-handling guards at V-60-08/24/25; check-phase-60 exits 0 with 25/25 PASS.
4. `check-phase-31.mjs:32` parseInventory() silent-swallow bug closed via `_missing` marker + V-31-21/V-31-24 callers updated to FAIL on `_missing`; both V-31-21 + V-31-24 continue to PASS post-fix (helper resolves placeholder-inventory.json at archived v1.3-phases path).
5. `check-phase-62.mjs:41` + `check-phase-63.mjs:48` ANCHOR_INVENTORY consts use helper with `['v1.6-phases']` milestoneRoots; both validators retain existing PASS state (modulo CHAIN_SKIP).
6. `regenerate-supervision-pins.mjs` lines 408-411 contain `_glossary-android.md` coords `{80, 82, 182, 199}` (+1 banner shift); line 422 `parseAllowlist('scripts/validation/v1.6-audit-allowlist.json')`; line 423 FAIL message references v1.6 sidecar; new BASELINE_9 attribution comment dated 2026-05-26 / Phase 68 Plan 68-02 inserted after line 406.
7. `regenerate-supervision-pins.mjs --self-test` exits 0 with "Self-test: PASS" output (was FAIL with diff pre-edit).
8. `v1.5-audit-allowlist.json` BROAD rebase landed: supervision_exemptions[] + c7_knox_allowlist[] + cope_banned_phrases[] (and safetynet_exemptions[] if any _glossary-android.md entries shifted) each have +1 line-coord on all `_glossary-android.md` entries; reason fields gain `Phase 62 carry-over: line N shifted +1 (Phase 62 Plan 62-07 added Apple Business banner line at line 13);` prefix matching v1.6 sidecar verbatim; JSON parses cleanly; array shape + length unchanged; non-_glossary-android.md entries UNTOUCHED.
9. `v1.5-milestone-audit.mjs` exits 0 with 12/12 PASS in fully-blocking mode (was 9/12; C2 + C7 + C9 all PASS post-rebase).
10. `v1.6-milestone-audit.mjs` exits 0 with 15/15 PASS (UNCHANGED baseline; v1.6 sidecar not touched).
11. ONE coordinated git commit at HEAD touches EXACTLY 8 files (1 new `_lib/archive-path.mjs` + 7 modified per the staging list); commit message body cites CHAIN-02 + D-02 + D-03 + D-04 §2.3 + closed bugs.
12. Single `git revert <Plan 68-02 SHA>` cleanly restores pre-edit baselines across all 8 files byte-identically.
13. `check-phase-61.mjs` exits with V-61-19 + V-61-20 STILL FAIL (Plan 68-04 scope, NOT this plan); other V-61-NN PASS post-cascade.
14. NO files outside the 8 explicitly-staged targets touched. NO `docs/*` corpus edits (validator-only-surgery per CONTEXT D-04). NO hub files, NO capability matrices.
15. Commit SHA captured for Plan 68-05 close-gate authoring (substituted as `{68_02_SHA}` in Plan 68-03's canonical CHAIN_SKIP comment block).
</success_criteria>

<output>
Create `.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-02-SUMMARY.md` with:
- Commit SHA + `git log --name-only -1 HEAD` output verbatim (8 files: 1 new + 7 modified)
- Pre-edit + post-edit baselines table (self-test FAIL→PASS; v1.5-harness 9/12→12/12; check-phase-48 5/7→7/7; check-phase-60 FAIL→25/25; check-phase-31 PASS→PASS preserved)
- Helper file shape: signature `resolveArchivedPhasePath(phaseSuffix, milestoneRoots = ['v1.5-phases'])` + 2 import lines + export pattern
- BASELINE_9 +1 shift table (4 _glossary-android.md entries) + parseAllowlist v1.5→v1.6 lineage repoint
- v1.5 sidecar broad-rebase summary: N entries rebased per array (supervision_exemptions / c7_knox_allowlist / cope_banned_phrases / safetynet_exemptions); planner-discovered exact array indices from `_tmp-sidecar-diff.txt` enumeration
- STRETCH outcome: check-phase-31 silent-swallow bug closed AND V-31-21/V-31-24 preserved (note in 68-VERIFICATION.md Discoveries section at Plan 68-05)
- Cascade state: check-phase-60 PASS post-Plan-68-02; check-phase-61 still FAILs V-61-19/V-61-20 (Plan 68-04 scope)
- v1.6 sidecar UNCHANGED confirmation (15/15 PASS unchanged baseline)
- Wave 2 handoff to Plan 68-03 (CHAIN-03 atomic) + Plan 68-04 (MILESTONES.md cdcce23): Plan 68-02 is file-disjoint from 68-04; Plan 68-03 depends on Plan 68-02 + Plan 68-04 ALL landing first (chain must be cascade-green before CHAIN_SKIP removal)
</output>
</content>
</invoke>