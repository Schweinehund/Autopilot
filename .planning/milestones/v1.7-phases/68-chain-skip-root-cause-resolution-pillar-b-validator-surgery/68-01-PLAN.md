---
phase: 68-chain-skip-root-cause-resolution-pillar-b-validator-surgery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/check-phase-51.mjs
  - scripts/validation/check-phase-58.mjs
autonomous: true
requirements:
  - CHAIN-01
tags:
  - validation
  - crlf
  - readfile-centralization
  - chain-01
  - phase-68
  - pillar-b
  - validator-surgery

must_haves:
  truths:
    - "scripts/validation/check-phase-51.mjs readFile() body at line 17 returns `readFileSync(abs, 'utf8').replace(/\\r\\n/g, '\\n')` (CRLF normalization — verbatim Phase 31 ca40eb9 idiom mirrored from check-phase-48.mjs:25)"
    - "scripts/validation/check-phase-58.mjs readFile() body at line 21 returns `readFileSync(abs, 'utf8').replace(/\\r\\n/g, '\\n')` (CRLF normalization — same idiom)"
    - "`node scripts/validation/check-phase-51.mjs` exits 0 with 25 passed / 0 failed / 0 skipped (unchanged from pre-edit baseline; no false positives introduced)"
    - "`node scripts/validation/check-phase-58.mjs` exits 0 with 26 passed / 0 failed / 0 skipped (unchanged from pre-edit baseline; no false positives introduced)"
    - "Sister-validator regression sweep (check-phase-{49,52..57,59,62..66}.mjs) shows no new FAILs introduced by the readFile() centralization (62-66 still SKIP {48,51,58,60,61} until Plan 68-03)"
    - "Zero regex bodies change in either file (D-01 adversarial wedge protection — Option A would have injected ~9 semantic-bug edits to `[^\\n]*` negated character classes that are NOT line-ends)"
    - "ROADMAP SC#1 satisfied via INTENT-equivalence (read-time CRLF normalization renders the validator's `\\n` regex semantically equivalent to `\\r?\\n` under any input line-ending state; D-01 Caveat: Plan 68-05 close-gate notes this for Phase 69/70 forward coordination)"
  artifacts:
    - path: scripts/validation/check-phase-51.mjs
      provides: "readFile() helper with CRLF normalization (.replace(/\\r\\n/g, '\\n') appended at line 17)"
      contains:
        - "readFileSync(abs, 'utf8').replace(/\\r\\n/g, '\\n')"
    - path: scripts/validation/check-phase-58.mjs
      provides: "readFile() helper with CRLF normalization (.replace(/\\r\\n/g, '\\n') appended at line 21)"
      contains:
        - "readFileSync(abs, 'utf8').replace(/\\r\\n/g, '\\n')"
  key_links:
    - from: "scripts/validation/check-phase-51.mjs readFile()"
      to: "scripts/validation/check-phase-48.mjs:25 canonical CRLF idiom"
      via: "verbatim byte-copy of `.replace(/\\r\\n/g, '\\n')` suffix"
      pattern: "readFileSync\\(abs,\\s*'utf8'\\)\\.replace\\(/\\\\r\\\\n/g,\\s*'\\\\n'\\)"
    - from: "scripts/validation/check-phase-58.mjs readFile()"
      to: "scripts/validation/check-phase-48.mjs:25 canonical CRLF idiom"
      via: "verbatim byte-copy of `.replace(/\\r\\n/g, '\\n')` suffix"
      pattern: "readFileSync\\(abs,\\s*'utf8'\\)\\.replace\\(/\\\\r\\\\n/g,\\s*'\\\\n'\\)"
---

<objective>
Resolve CHAIN-01 per CONTEXT.md D-01 (Option B — readFile() centralization, score B=8 vs D=14/C=15/A=16). Add `.replace(/\r\n/g, '\n')` to the `readFile()` helper body at `scripts/validation/check-phase-51.mjs:17` and `scripts/validation/check-phase-58.mjs:21`. Zero regex changes. Verbatim idiom byte-copied from `check-phase-48.mjs:25` (Phase 31 `ca40eb9` lineage; also proven at `check-phase-60.mjs:24` and `regenerate-supervision-pins.mjs:77`).

**Purpose:** Hardening for the contributor-PR-on-CRLF-Windows-host scenario. Both target validators currently PASS today (51 → 25/25, 58 → 26/26) because Phase 67 corpus sweeps left target docs in LF form, but the regex literal `\n` was never hardened. The narrow ROADMAP SC#1 literal "regex updated to `\r?\n`" wording is satisfied via INTENT-equivalence (read-time normalization renders the validator's `\n` regex semantically equivalent to `\r?\n`).

**Adversarial-wedge protection:** Option A (regex inventory) would have injected ~9 semantic-bug edits to `[^\n]*` NEGATED character classes that are NOT line-ends; D-01 adversary identified this and Option B was selected. Additionally, `check-phase-58.mjs:188` carries a literal-string `c.indexOf("---\n", 4)` that Option A would NOT cover — Option B's read-time normalization covers it for free.

**Output:**
1. `scripts/validation/check-phase-51.mjs:17` — readFile() body returns the CRLF-normalized string (single-line edit; inline comment recommended for traceability)
2. `scripts/validation/check-phase-58.mjs:21` — same edit
3. ONE git commit (per CONTEXT.md D-04 Claude's Discretion: 2-file commit OR two single-file commits — recommended single 2-file commit per Phase 67 D-04 score E=7 per-plan-per-requirement precedent)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-CONTEXT.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-RESEARCH.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-PATTERNS.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-VALIDATION.md
@scripts/validation/check-phase-51.mjs
@scripts/validation/check-phase-58.mjs
@scripts/validation/check-phase-48.mjs

<interfaces>
<!-- Exact line refs + before/after snippets. Executor MUST read both target files to verify pre-edit state. -->

**Canonical CRLF idiom from `scripts/validation/check-phase-48.mjs` lines 22-26 (verbatim — read this first):**

```
function readFile(relPath) {
  const abs = join(process.cwd(), relPath);
  if (!existsSync(abs)) return null;
  return readFileSync(abs, 'utf8').replace(/\r\n/g, '\n');
}
```

The same idiom appears at `check-phase-60.mjs:21-25`, `regenerate-supervision-pins.mjs:73-78` (with explicit `// CRLF normalization per Phase 31 ca40eb9` comment), and `check-phase-31.mjs:15-19` (the earliest precedent).

**Plan 68-01 verbatim edits (per 68-RESEARCH.md §Verbatim Edit Targets Plan 68-01 + 68-PATTERNS.md):**

| # | File | Line | BEFORE (exact bytes) | AFTER (exact bytes) |
|---|------|------|----------------------|---------------------|
| 1 | `scripts/validation/check-phase-51.mjs` | 17 | `  return readFileSync(abs, 'utf8');` | `  return readFileSync(abs, 'utf8').replace(/\r\n/g, '\n');  // CRLF normalization (CHAIN-01; mirrors check-phase-48.mjs:25)` |
| 2 | `scripts/validation/check-phase-58.mjs` | 21 | `  return readFileSync(abs, 'utf8');` | `  return readFileSync(abs, 'utf8').replace(/\r\n/g, '\n');  // CRLF normalization (CHAIN-01; mirrors check-phase-48.mjs:25)` |

**Style preservation rules (per 68-PATTERNS.md §"check-phase-51.mjs:17 + check-phase-58.mjs:21 (CHAIN-01 — CRLF centralization)"):**

- Both target files use **single quotes** for the `'utf8'` literal (verified by Read 2026-05-26). The executor MUST preserve single-quote style; do NOT switch to double quotes.
- The inline trailing comment `// CRLF normalization (CHAIN-01; mirrors check-phase-48.mjs:25)` is RECOMMENDED for traceability (matches the `regenerate-supervision-pins.mjs:77` and `check-phase-31.mjs:18` comment-attribution style) but optional.
- Zero changes anywhere else in either file. No regex bodies change. No indexOf calls change. No imports change.
- Two-space indent on the `return ...` line is preserved.

**Pre-edit state of `scripts/validation/check-phase-51.mjs:14-18` (Read verified 2026-05-26):**

```
function readFile(relPath) {
  const abs = join(process.cwd(), relPath);
  if (!existsSync(abs)) return null;
  return readFileSync(abs, 'utf8');
}
```

**Pre-edit state of `scripts/validation/check-phase-58.mjs:18-22` (Read verified 2026-05-26):**

```
function readFile(relPath) {
  const abs = join(process.cwd(), relPath);
  if (!existsSync(abs)) return null;
  return readFileSync(abs, 'utf8');
}
```

(Same shape; the only difference is the surrounding line range — check-phase-51 starts the function 4 lines earlier in the file than check-phase-58.)

**check-phase-58.mjs:188 adversarial-wedge reference (D-01 dossier finding):**

The literal `c.indexOf("---\n", 4)` at line 188 was NOT covered by Option A regex inventory. Option B (this plan) covers it transparently because `c` is the output of `readFile()`, which now normalizes CRLF at read-time — so the literal `"---\n"` matches against pre-normalized LF content regardless of on-disk line endings.

**Pre-edit baseline (MANDATORY capture before editing — Phase 68 INTENT contract per ROADMAP SC#1):**

```
node scripts/validation/check-phase-51.mjs ; echo "exit=$LASTEXITCODE"
# Expected: 25 passed, 0 failed, 0 skipped; exit=0

node scripts/validation/check-phase-58.mjs ; echo "exit=$LASTEXITCODE"
# Expected: 26 passed, 0 failed, 0 skipped; exit=0
```

**Post-edit verification (same commands; must remain 25/25 + 26/26):**

```
node scripts/validation/check-phase-51.mjs ; echo "exit=$LASTEXITCODE"
node scripts/validation/check-phase-58.mjs ; echo "exit=$LASTEXITCODE"
```

**Sister-validator regression sweep (the ROADMAP SC#1 "other phases the regex touches" clause — verify no false positives introduced anywhere else in chain):**

```
foreach ($p in 49,52,53,54,55,56,57,59,62,63,64,65,66) {
  Write-Host "--- check-phase-$p.mjs ---"
  node scripts/validation/check-phase-$p.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "REGRESSION on $p" }
}
```

Expected post-Plan-68-01 chain state: check-phase-{49,52..57,59,62..66}.mjs all PASS (62-66 because CHAIN_SKIP suppresses 48/51/58/60/61 — still skipping until Plan 68-03). Phases 48, 60, 61 NOT in this sweep because they FAIL today (Plan 68-02 + 68-04 fix them).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 68-01-01: CRLF readFile() centralization in check-phase-51.mjs + check-phase-58.mjs + pre/post-edit verification + sister-validator regression sweep + atomic 2-file commit</name>
  <files>scripts/validation/check-phase-51.mjs, scripts/validation/check-phase-58.mjs</files>
  <read_first>
    - scripts/validation/check-phase-51.mjs (read full file — verify line 17 pre-edit state matches `  return readFileSync(abs, 'utf8');` exactly; confirm single-quote `'utf8'` literal; note that line numbers for regex calls at 79/107/120/153/190-192/205-208/221-223 are inside negated character classes and MUST NOT be touched — D-01 adversarial wedge)
    - scripts/validation/check-phase-58.mjs (read full file — verify line 21 pre-edit state matches `  return readFileSync(abs, 'utf8');` exactly; confirm line 188 `c.indexOf("---\n", 4)` literal-string CRLF risk is covered by this read-time normalization; note frontmatter regex at line 201 also benefits from read-time normalization)
    - scripts/validation/check-phase-48.mjs (read lines 22-26 — confirm the canonical CRLF idiom shape; this is the verbatim byte source Plans 68-01 copies)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-CONTEXT.md (read D-01 at lines 44-63 — Option B mechanism + adversarial-wedge rationale + SC#1 INTENT compliance caveat + sister-validator preservation note)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-RESEARCH.md (read §Verbatim Edit Targets Plan 68-01 at lines 127-138 + §Verification Harness Plan 68-01 at lines 348-378 — byte-exact edits + verification commands)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-PATTERNS.md (read §"check-phase-51.mjs:17 + check-phase-58.mjs:21 (CHAIN-01 — CRLF centralization)" at lines 124-185 — analog mapping + convention-preservation list)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-VALIDATION.md (read lines 42-43 — Per-Task Verification Map entries for 68-01-01 + 68-01-02 — automated commands + expected statuses)
  </read_first>
  <action>
**WAVE 1 — PRE-EDIT BASELINE CAPTURE (MANDATORY per ROADMAP SC#1 "no false positives introduced (verified against existing PASS state on other phases the regex touches)"):**

1. Capture pre-edit baselines for both target validators:

```
node scripts/validation/check-phase-51.mjs ; Write-Host "check-phase-51 pre-edit exit=$LASTEXITCODE"
# Expected: "25 passed, 0 failed, 0 skipped"; exit=0

node scripts/validation/check-phase-58.mjs ; Write-Host "check-phase-58 pre-edit exit=$LASTEXITCODE"
# Expected: "26 passed, 0 failed, 0 skipped"; exit=0
```

If either pre-edit baseline does NOT match the expected counts (25/25 for 51, 26/26 for 58), STOP — investigate the unexpected state BEFORE editing. Phase 67 close-gate baseline confirmed both at 25/25 and 26/26 (per 67-VERIFICATION.md). A drift indicates an upstream change requiring investigation.

2. Capture pre-edit chain-regression baseline (sister validators that should remain PASS):

```
foreach ($p in 49,52,53,54,55,56,57,59,62,63,64,65,66) {
  $exit = (node scripts/validation/check-phase-$p.mjs > $null 2>&1; $LASTEXITCODE)
  Write-Host "check-phase-$p.mjs pre-edit exit=$exit"
}
# Expected: all exit 0 (62-66 PASS modulo CHAIN_SKIP {48,51,58,60,61})
```

Phases 48, 60, 61 are NOT included in this sweep because they FAIL today per CHAIN_SKIP documentation; Plans 68-02 + 68-04 fix them, NOT Plan 68-01.

**WAVE 2 — VERBATIM EDITS (2 lines across 2 files; zero side-effects beyond the two append-suffix-edits):**

3. Edit `scripts/validation/check-phase-51.mjs` line 17. Use the `Edit` tool with exact line precision. Pre-edit state at line 17 (verified by Wave 1 reads):

```
  return readFileSync(abs, 'utf8');
```

After edit (per 68-RESEARCH.md §Verbatim Edit Targets Plan 68-01 #1):

```
  return readFileSync(abs, 'utf8').replace(/\r\n/g, '\n');  // CRLF normalization (CHAIN-01; mirrors check-phase-48.mjs:25)
```

**Style preservation rules (MANDATORY — per 68-PATTERNS.md §"Conventions preserved"):**
- Two-space leading indent preserved
- Single quotes around `'utf8'` preserved (do NOT switch to double quotes)
- The literal regex `/\r\n/g` uses unescaped slashes per JavaScript regex literal syntax (NOT a string with escaped backslashes); the `'\n'` replacement is a string literal with a single escaped newline
- Two-space gap before the inline `//` comment (matches `regenerate-supervision-pins.mjs:77` comment-attribution shape)

4. Edit `scripts/validation/check-phase-58.mjs` line 21. Pre-edit state at line 21 (verified by Wave 1 reads):

```
  return readFileSync(abs, 'utf8');
```

After edit (per 68-RESEARCH.md §Verbatim Edit Targets Plan 68-01 #2):

```
  return readFileSync(abs, 'utf8').replace(/\r\n/g, '\n');  // CRLF normalization (CHAIN-01; mirrors check-phase-48.mjs:25)
```

Same style-preservation rules apply (single quotes, two-space indent, two-space gap before comment).

**WAVE 3 — POST-EDIT VERIFICATION + SISTER-VALIDATOR REGRESSION SWEEP + ATOMIC COMMIT:**

5. Post-edit baseline re-capture (target validators must remain PASS at identical counts):

```
node scripts/validation/check-phase-51.mjs ; Write-Host "check-phase-51 post-edit exit=$LASTEXITCODE"
# REQUIRED: "25 passed, 0 failed, 0 skipped"; exit=0 (identical to pre-edit)

node scripts/validation/check-phase-58.mjs ; Write-Host "check-phase-58 post-edit exit=$LASTEXITCODE"
# REQUIRED: "26 passed, 0 failed, 0 skipped"; exit=0 (identical to pre-edit)
```

If either count differs from pre-edit, the edit broke something — revert with `git checkout scripts/validation/check-phase-51.mjs scripts/validation/check-phase-58.mjs` and investigate.

6. Sister-validator regression sweep (the ROADMAP SC#1 "no false positives introduced" assertion):

```
$failures = @()
foreach ($p in 49,52,53,54,55,56,57,59,62,63,64,65,66) {
  $exit = (node scripts/validation/check-phase-$p.mjs > $null 2>&1; $LASTEXITCODE)
  if ($exit -ne 0) { $failures += $p }
  Write-Host "check-phase-$p.mjs post-edit exit=$exit"
}
if ($failures.Count -ne 0) { Write-Error "REGRESSION: post-edit failures on phases $($failures -join ',')" }
# REQUIRED: empty failures list (all 13 sister validators exit 0; 62-66 modulo CHAIN_SKIP)
```

If any sister validator regressed (exit != 0 post-edit when it was 0 pre-edit), STOP — revert and investigate. The edit should have ZERO effect on sister validators (the readFile() helper is internal to each `.mjs` file; cross-file effects are impossible from this edit).

7. Sanity-check the edit text via grep (defense-in-depth):

```
Select-String -Path scripts/validation/check-phase-51.mjs -Pattern "readFileSync\(abs, 'utf8'\)\.replace\(/\\\\r\\\\n/g, '\\\\n'\)"
# Expected: 1 match at line 17

Select-String -Path scripts/validation/check-phase-58.mjs -Pattern "readFileSync\(abs, 'utf8'\)\.replace\(/\\\\r\\\\n/g, '\\\\n'\)"
# Expected: 1 match at line 21
```

If either grep returns 0 matches, the edit did not land — re-apply via `Edit` tool.

8. Atomic 2-file commit (per CONTEXT.md D-04 Claude's Discretion bullet 3: "single 2-file commit OR two single-file commits" — recommended single 2-file commit per Phase 67 D-04 score E=7 per-plan-per-requirement precedent):

```
git add scripts/validation/check-phase-51.mjs scripts/validation/check-phase-58.mjs

git status
# Expected: 2 files staged (Modified), no others
```

If `git status` shows any other modified files, STOP — investigate (the edit should have touched ONLY these 2 files).

```
git commit -m "$(cat <<'EOF'
fix(validation): centralize CRLF normalization in check-phase-51 + 58 readFile() (CHAIN-01)

Add `.replace(/\r\n/g, '\n')` to the `readFile()` helper body at
`check-phase-51.mjs:17` and `check-phase-58.mjs:21` — verbatim byte-copy of
the canonical CRLF-normalization idiom from `check-phase-48.mjs:25` (Phase 31
`ca40eb9` lineage; also proven at `check-phase-60.mjs:24` and
`regenerate-supervision-pins.mjs:77`).

Per CONTEXT.md D-01 Option B (score B=8 vs D=14/C=15/A=16). Adversarial-wedge
protection: Option A (regex inventory) would have injected ~9 semantic-bug
edits to `[^\n]*` NEGATED character classes that are NOT line-ends — D-01
adversary identified this and Option B was selected. Additionally,
`check-phase-58.mjs:188` carries a literal-string `c.indexOf("---\n", 4)`
that Option A would NOT cover; Option B's read-time normalization covers it
transparently because `c` is the readFile() output, which now normalizes CRLF
regardless of on-disk line endings.

Hardening for the contributor-PR-on-CRLF-Windows-host scenario. Both
validators currently PASS today (51 → 25/25, 58 → 26/26) on this LF-target
Windows host; the edit is defense-in-depth without observable behavior change
on the current corpus.

ROADMAP SC#1 satisfied via INTENT-equivalence (read-time normalization
renders the validator's `\n` regex semantically equivalent to `\r?\n`).
Plan 68-05 close-gate notes this for Phase 69/70 forward coordination
(check-phase-68.mjs HARNESS-03 self-verifier MUST verify CHAIN-01 via INTENT,
not literal-letter source grep — per D-01 §Caveat).

Pre-edit baseline: check-phase-51 = 25 passed / check-phase-58 = 26 passed
Post-edit verification: 25 passed / 26 passed (identical counts; no
regressions).

Sister-validator regression sweep PASS: check-phase-{49,52..57,59,62..66}
all exit 0 (62-66 modulo CHAIN_SKIP {48,51,58,60,61} until Plan 68-03).

Rollback: git revert <THIS_SHA> cleanly restores pre-edit `return
readFileSync(abs, 'utf8');` on both files (per-plan boundary preserved per
CONTEXT.md D-04 Option E).

CHAIN-01
EOF
)"
```

9. Post-commit verify:

```
git log --name-only -1 HEAD
```

Expected output: exactly 2 files (`scripts/validation/check-phase-51.mjs` + `scripts/validation/check-phase-58.mjs`); no others.

10. Final post-commit re-run of target validators (proves the dry-run was faithful):

```
node scripts/validation/check-phase-51.mjs ; Write-Host "POST-COMMIT check-phase-51 exit=$LASTEXITCODE"
node scripts/validation/check-phase-58.mjs ; Write-Host "POST-COMMIT check-phase-58 exit=$LASTEXITCODE"
# Expected: both exit 0 with 25/25 + 26/26
```

Capture the commit SHA for Plan 68-05 close-gate authoring (Plan 68-05 substitutes `{68_01_SHA}` in the canonical CHAIN_SKIP comment block of Plan 68-03 with this SHA).
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-51.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "check-phase-51 FAIL post-edit" } ; node scripts/validation/check-phase-58.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "check-phase-58 FAIL post-edit" } ; if ((Select-String -Path scripts/validation/check-phase-51.mjs -Pattern "readFileSync\(abs, 'utf8'\)\.replace\(/\\\\r\\\\n/g, '\\\\n'\)" | Measure-Object).Count -ne 1) { Write-Error "check-phase-51 missing CRLF normalize at line 17" } ; if ((Select-String -Path scripts/validation/check-phase-58.mjs -Pattern "readFileSync\(abs, 'utf8'\)\.replace\(/\\\\r\\\\n/g, '\\\\n'\)" | Measure-Object).Count -ne 1) { Write-Error "check-phase-58 missing CRLF normalize at line 21" } ; foreach ($p in 49,52,53,54,55,56,57,59,62,63,64,65,66) { node scripts/validation/check-phase-$p.mjs > $null 2>&1 ; if ($LASTEXITCODE -ne 0) { Write-Error "REGRESSION on check-phase-$p (post-edit FAIL)" } }</automated>
    <expected>check-phase-51 exits 0 with 25/25 + CRLF-normalize grep matches at line 17; check-phase-58 exits 0 with 26/26 + CRLF-normalize grep matches at line 21; all 13 sister validators (49, 52-57, 59, 62-66) exit 0 (62-66 modulo CHAIN_SKIP).</expected>
  </verify>
  <done>scripts/validation/check-phase-51.mjs:17 + check-phase-58.mjs:21 both return CRLF-normalized strings via verbatim Phase 31 ca40eb9 idiom (.replace(/\r\n/g, '\n')); pre-edit baselines (25/25 + 26/26) preserved post-edit (no regressions); sister-validator regression sweep across 13 phases all PASS (no false positives introduced anywhere in chain); ONE atomic 2-file commit landed with CHAIN-01 in message body; git revert restores pre-edit baseline byte-identically; commit SHA captured for Plan 68-05 close-gate canonical-comment-block SHA substitution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validator source edits ↔ Node.js runtime | 2-line append-suffix edits to the `readFile()` body in 2 `.mjs` files; helper function is internal to each file, no cross-file effects possible |
| Pre-edit baseline ↔ post-edit chain green-gate | 25/25 + 26/26 target preservation; 13-phase sister-validator regression sweep proves zero false positives introduced elsewhere |
| Local Windows host (LF-target corpus) ↔ hypothetical CRLF-target contributor PR | Edit is hardening for the contributor-PR-on-CRLF-Windows-host scenario; observable behavior on current LF corpus unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01-AR | Tampering (anti-regression — accidental sister-validator regression from over-aggressive regex edit) | scripts/validation/check-phase-{49,52..57,59,62..66}.mjs | mitigate | Strict 2-line edit-suffix protocol (zero regex bodies change); 13-phase post-edit regression sweep MUST exit 0 across all sister validators; D-01 adversarial-wedge rationale (Option A would have injected ~9 semantic-bug edits) explicitly cited in commit message body |
| T-68-01-QS | Tampering (quote-style drift from single→double in 'utf8' literal) | scripts/validation/check-phase-{51,58}.mjs | mitigate | Style-preservation rules explicitly cited in `<read_first>` + `<action>`; both files use single quotes (verified by Read); executor MUST preserve single-quote style |
| T-68-01-IN | Tampering (intent drift — read-time normalization vs literal-letter regex edit) | ROADMAP SC#1 literal "regex updated to `\r?\n`" interpretation | mitigate | INTENT-equivalence rationale explicitly cited in commit message body + Plan 68-05 close-gate notes for Phase 69/70 forward coordination (check-phase-68.mjs HARNESS-03 self-verifier MUST verify CHAIN-01 via INTENT) per D-01 §Caveat |
| T-68-01-FS | Information Disclosure (Phase 67 corpus state assumption) | Pre-edit baseline at 25/25 + 26/26 | mitigate | If pre-edit baseline differs from expected (upstream drift), STOP and investigate before editing; do not proceed assuming stale baselines |
</threat_model>

<verification>
**Plan 68-01 → CHAIN-01 verification targets (per 68-VALIDATION.md lines 42-43):**

| Target | Command | Expected |
|--------|---------|----------|
| check-phase-51 PASS preserved | `node scripts/validation/check-phase-51.mjs ; $LASTEXITCODE` | `0` (25/25 PASS) |
| check-phase-58 PASS preserved | `node scripts/validation/check-phase-58.mjs ; $LASTEXITCODE` | `0` (26/26 PASS) |
| CRLF normalize present in check-phase-51.mjs | `Select-String -Path scripts/validation/check-phase-51.mjs -Pattern "\.replace\(/\\\\r\\\\n/g, '\\\\n'\)"` | 1 match (line 17) |
| CRLF normalize present in check-phase-58.mjs | `Select-String -Path scripts/validation/check-phase-58.mjs -Pattern "\.replace\(/\\\\r\\\\n/g, '\\\\n'\)"` | 1 match (line 21) |
| Sister-validator regression sweep | `foreach ($p in 49,52..57,59,62..66) { node scripts/validation/check-phase-$p.mjs > $null ; if ($LASTEXITCODE -ne 0) { Write-Error "regression on $p" } }` | no errors (all 13 exit 0) |
| Atomic commit boundary | `git log --name-only -1 HEAD` | exactly 2 files (check-phase-51.mjs + check-phase-58.mjs) |
</verification>

<success_criteria>
1. `scripts/validation/check-phase-51.mjs` line 17 body returns `readFileSync(abs, 'utf8').replace(/\r\n/g, '\n')` with optional inline `// CRLF normalization (CHAIN-01; mirrors check-phase-48.mjs:25)` trailing comment; single-quote `'utf8'` and two-space indent preserved.
2. `scripts/validation/check-phase-58.mjs` line 21 body has the same edit shape with the same comment.
3. `node scripts/validation/check-phase-51.mjs` exits 0 with summary "25 passed, 0 failed, 0 skipped" (identical to pre-edit baseline; no regression).
4. `node scripts/validation/check-phase-58.mjs` exits 0 with summary "26 passed, 0 failed, 0 skipped" (identical to pre-edit baseline; no regression).
5. Sister-validator regression sweep (check-phase-{49,52..57,59,62..66}.mjs) all exit 0 (62-66 modulo CHAIN_SKIP {48,51,58,60,61}).
6. ONE atomic git commit at HEAD touches EXACTLY the 2 files `scripts/validation/check-phase-51.mjs` and `scripts/validation/check-phase-58.mjs`; commit message body cites CHAIN-01 + Phase 31 `ca40eb9` lineage + INTENT-equivalence rationale + Plan 68-05 close-gate forward coordination.
7. Single `git revert <Plan 68-01 SHA>` cleanly restores pre-edit `return readFileSync(abs, 'utf8');` on both files byte-identically (per-plan boundary preserved per CONTEXT.md D-04 Option E).
8. NO regex bodies change in either file (D-01 adversarial wedge protection — Option A would have injected ~9 semantic-bug edits to negated character classes that are NOT line-ends).
9. NO files outside the 2 explicitly-staged targets touched. NO hub files, NO capability matrices, NO older workflow files.
10. Commit SHA captured for Plan 68-05 close-gate authoring (substituted as `{68_01_SHA}` in Plan 68-03's canonical CHAIN_SKIP comment block).
</success_criteria>

<output>
Create `.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-01-SUMMARY.md` with:
- Commit SHA + `git log --name-only -1 HEAD` output verbatim
- Pre-edit + post-edit exit codes (check-phase-51 = 25/25; check-phase-58 = 26/26)
- Sister-validator regression sweep summary (13 phases all exit 0; 62-66 modulo CHAIN_SKIP)
- Grep confirmation: 1 match each for the CRLF-normalize idiom at line 17 (check-phase-51) and line 21 (check-phase-58)
- Style preservation confirmation: single-quote `'utf8'` preserved; two-space indent preserved; inline comment present
- Wave 2 handoff to Plan 68-02: file-disjoint (Plan 68-01 touched only 51 + 58; Plan 68-02 touches 31/48/60/62/63 + new _lib/ + regenerate-supervision-pins + v1.5 sidecar)
</output>
</content>
</invoke>