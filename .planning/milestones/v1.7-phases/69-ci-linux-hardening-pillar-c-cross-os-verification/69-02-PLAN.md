---
phase: 69
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/milestones/v1.7-DEFERRED-CLEANUP.md
  - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-02-SUMMARY.md
autonomous: false
requirements: [CILINUX-01]
requirements_addressed: [CILINUX-01]

must_haves:
  truths:
    - "workflow_dispatch baseline on master HEAD exits 0 with `::notice CHAIN_TIMING_LINUX` emission captured (SC#5 B.1)"
    - "Synthetic CRLF-injection PR on branch `phase-69/sc5-crlf-evidence` exits 1 with check-phase-51 catching regression (SC#5 B.2)"
    - "Revert commit on synthetic branch exits 0 (SC#5 B.3)"
    - "Windows local chain vs Linux GHA chain show identical per-validator PASS counts (SC#5 B.4)"
    - "Synthetic PR closed-without-merge + branch deleted; zero master tree churn from SC#5 evidence (T-69-SYNTHETIC-PR-CONTAMINATION mitigation)"
    - "69-VERIFICATION.md authored with sections A-G mirroring 68-VERIFICATION.md shape"
    - "REQUIREMENTS.md line 84 CILINUX-01 Pending→Complete; lines 24-26 checkbox flipped"
    - "STATE.md frontmatter + Performance Metrics + Pending Todos + Session Continuity + Decisions updated"
    - "ROADMAP.md Phase 69 row Pending→Complete with closing SHAs"
    - "PROJECT.md Validated section gains CILINUX-01 row"
    - "v1.7-DEFERRED-CLEANUP.md TIMEOUT-01 entry updated with measured Linux chain runtime"
    - "All 3 predecessor workflow files (v1.4 + v1.5 + v1.6) remain BYTE-UNCHANGED across Plan 69-01 + 69-02 cumulative SHAs"
  artifacts:
    - path: .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md
      provides: "Phase 69 close-gate evidence consolidation (sections A goal narrative + B.1-B.4 evidence + C SC#1-5 mapping + D SHA record + E discoveries + F Phase 70 forward-pointer + G sign-off)"
      contains: "Section A — Goal achievement narrative"
      min_lines: 80
    - path: .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-02-SUMMARY.md
      provides: "Plan 69-02 execution summary with Plan 69-01 + 69-02 SHAs, all evidence URLs, Linux runtime measurement"
      min_lines: 30
  key_links:
    - from: ".planning/REQUIREMENTS.md line 84 Traceability row"
      to: "CILINUX-01 status column flipped Pending → Complete with closing SHA"
      via: "table cell edit"
      pattern: "CILINUX-01 \\| Phase 69 \\| Complete"
    - from: ".planning/STATE.md Decisions section"
      to: "new H3 `### Phase 69 — Plan 02 (close-gate; 2026-MM-DD)` with all 5 SC mappings"
      via: "Markdown heading insert"
      pattern: "Phase 69 — Plan 02"
    - from: ".planning/ROADMAP.md Phase 69 row"
      to: "Complete checkbox + Plan 69-01 SHA + Plan 69-02 SHA"
      via: "checkbox + closing-SHA cell edit"
      pattern: "Phase 69.*Complete"
---

<objective>
Close-gate for Phase 69 CILINUX-01: capture SC#5 evidence via (1) `workflow_dispatch` baseline on master HEAD post-push of Plan 69-01 + (2) synthetic CRLF-injection PR negative + positive cases on throwaway branch `phase-69/sc5-crlf-evidence`, then (3) capture Windows local vs Linux GHA PASS-count diff. Author `69-VERIFICATION.md` with sections A-G mirroring 68-VERIFICATION.md. Flip traceability across REQUIREMENTS / STATE / ROADMAP / PROJECT. Update v1.7-DEFERRED-CLEANUP.md TIMEOUT-01 with measured Linux runtime. Final close-gate commit.

Purpose: Satisfies ROADMAP Phase 69 SC#3 (chain exits 0 with 0 SKIPPED on ubuntu-latest — runtime evidence) and SC#5 (cross-OS reproducibility on representative PR — synthetic evidence permitted per ROADMAP.md:356 "synthetic or real"). Synthetic PR closed-without-merge preserves zero master-tree-churn invariant.

Output: 69-VERIFICATION.md authored; traceability flipped; v1.7-DEFERRED-CLEANUP.md TIMEOUT-01 updated with Linux runtime; close-gate commit landed; synthetic PR closed + branch deleted.

**Plan 69-02 is NOT autonomous** — contains 3 `checkpoint:human-verify` gates: (1) confirm Plan 69-01 commit pushed to origin/master before triggering workflow_dispatch; (2) confirm synthetic PR closed-without-merge + branch deleted; (3) confirm Linux runtime measurement either within 1500s budget or escalate TIMEOUT-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.7-DEFERRED-CLEANUP.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-VERIFICATION.md
@.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-CONTEXT.md
@.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-RESEARCH.md
@.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VALIDATION.md
@.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-01-PLAN.md
@.github/workflows/audit-harness-v1.7-integrity.yml

<interfaces>
<!-- 69-VERIFICATION.md section structure — mirror 68-VERIFICATION.md exactly -->

Section A — Goal achievement narrative (≥ 10 lines)
Section B.1 — workflow_dispatch baseline on master HEAD
  - GHA run URL (https://github.com/<owner>/<repo>/actions/runs/<run_id>)
  - Job result: SUCCESS / exit 0
  - Per-validator PASS counts (parse / path-match / harness-run / linux-chain-ubuntu-latest)
  - `::notice CHAIN_TIMING_LINUX` captured value (in seconds)
  - Total job runtime (workflow-level)
Section B.2 — Synthetic PR negative case (CRLF injection)
  - Branch: `phase-69/sc5-crlf-evidence`
  - Injection commit SHA
  - GHA run URL (run on the negative-case commit push)
  - Job result: FAILURE / exit 1
  - Which validator caught the regression: `check-phase-51` (the CHAIN-01-hardened validator reading `docs/decision-trees/09-linux-triage.md`)
  - Log excerpt showing FAIL line
Section B.3 — Synthetic PR positive case (revert)
  - Revert commit SHA
  - GHA run URL (run on the revert commit push)
  - Job result: SUCCESS / exit 0
Section B.4 — Windows local vs Linux GHA PASS-count diff
  - Table: per-validator (48..66 + skip-echoes 67-70) Windows-PASS vs Linux-PASS counts
  - Expected: identical per SC#5 cross-OS reproducibility
Section C — SC#1-5 satisfaction mapping
  - SC#1 cites B.1 + workflow YAML structure
  - SC#2 cites workflow YAML `continue-on-error: false` declaration
  - SC#3 cites B.1 chain-apex log showing 0 SKIPPED in CHAIN_SKIP
  - SC#4 cites `git diff` showing v1.4/v1.5/v1.6 byte-unchanged
  - SC#5 cites B.2 + B.3 + B.4
Section D — Atomic commit SHA record
  - Plan 69-01 commit SHA on master
  - Plan 69-02 close-gate commit SHA
  - Synthetic PR number + closure URL (closed-without-merge)
  - Synthetic branch deletion confirmation
Section E — Discoveries
  - Linux runtime delta vs Windows reference (~102s)
  - Timeout headroom margin observed (vs 30-min job timeout + 300s subprocess timeout)
  - Any path-filter surprises during synthetic PR test
  - dos2unix install latency (apt-get on ubuntu-24.04)
Section F — Forward-pointer to Phase 70
  - HARNESS-04 inherits linux-chain-ubuntu-latest job verbatim (extends with crons + pin-helper-advisory + sidecar repoints v1.6→v1.7)
  - HARNESS-04(a) MUST remove `docs/decision-trees/09-linux-triage.md` from path-filter (D-04 sub-decision (a) handoff)
  - HARNESS-03 self-verifier already INTENT-verified per Phase 68 §F (carry-forward)
  - TIMEOUT-01 in v1.7-DEFERRED-CLEANUP.md updated with measured Linux runtime as forward-coordination data point
Section G — Sign-off
  - Phase 69 (Pillar C — CI-Linux Hardening) CLOSED
  - CILINUX-01 Active → Validated traceability flipped across PROJECT/REQUIREMENTS/STATE/ROADMAP

<!-- v1.7-DEFERRED-CLEANUP.md TIMEOUT-01 entry update format -->
The TIMEOUT-01 entry was authored at Phase 68 close as a "discovery flag for Phase 69 first-run timing measurement." Plan 69-02 updates this entry with the measured Linux chain runtime.

Locate the TIMEOUT-01 section in `.planning/milestones/v1.7-DEFERRED-CLEANUP.md` (added at Phase 68 close per STATE.md line 193). Append (or update) a "Phase 69 measurement" sub-block:
```
**Phase 69 measurement (2026-MM-DD via `::notice CHAIN_TIMING_LINUX`):**
- Linux GHA chain wall-clock: {N}s (Windows reference: ~102s; subprocess timeout: 300s)
- Headroom analysis: {N}s vs 300s subprocess cap = {ratio}× headroom; {N}s vs 1800s (30-min job cap) = {ratio}× headroom
- Disposition: {disposition}
  - If N ≤ 150s: "Within budget; no escalation; close TIMEOUT-01 as VALIDATED at Phase 70 close"
  - If 150s < N ≤ 1500s: "Within budget but >1.5× Windows reference; flag for Phase 70 HARNESS-04 timeout-budget review"
  - If N > 1500s: "Escalate TIMEOUT-01 to v1.8+ subprocess-timeout-architecture review per CONTEXT D-03 Pitfall 5"
```

<!-- REQUIREMENTS.md edit targets -->
Line 24-26 (Pillar C — CI-Linux Hardening section): flip `- [ ] **CILINUX-01**: ...` to `- [x] **CILINUX-01**: ... — completed 2026-MM-DD in Phase 69 (Plan 69-01 commit `{69_01_SHA}` workflow YAML + Plan 69-02 commit `{69_02_SHA}` close-gate; SC#5 evidence via workflow_dispatch baseline + synthetic CRLF PR closed-without-merge)`

Line 84 (Traceability table): `| CILINUX-01 | Phase 69 | Pending |` → `| CILINUX-01 | Phase 69 | Complete |`

<!-- STATE.md edit targets -->
Frontmatter:
- `completed_phases: 2` → `3`
- `completed_plans: 8` → `10` (Plan 69-01 + Plan 69-02 = +2)
- `percent: 22` → recalculate (10/8 of recorded total — actual percent depends on total_plans field; recompute)
- `stopped_at: "Phase 69 context gathered"` → `"Phase 69 closed"`
- `last_updated:` → current ISO timestamp

`## Current Position` block:
- `Phase: 69` (same) / `Plan: Not started` → `Plan: Complete` / `Status: Ready to plan` → `Status: Phase 69 closed; ready for Phase 70 planning`

`## Performance Metrics` table — add new line under v1.7:
- `Phase 69 (v1.7 Pillar C — CI-Linux Hardening): 2 plans, shipped 2026-MM-DD — CILINUX-01 closed (Plan 69-01 \`{69_01_SHA}\` workflow YAML skeleton + Plan 69-02 \`{69_02_SHA}\` close-gate); Linux chain runtime measured at {N}s via ::notice CHAIN_TIMING_LINUX`

`## Pending Todos` block — remove the Phase 69 planning entry (line ~137); update Phase 70 entry to remove "Phase 69 CI-Linux first-run timing measurement to validate 300s subprocess-timeout headroom" sub-bullet (now satisfied) — but keep Phase 70 main planning entry intact.

`## Session Continuity` block:
- `Last session:` → current ISO timestamp
- `Stopped at:` → `Phase 69 (Pillar C) closed`
- `Resume file:` → `.planning/phases/70-v17-audit-harness-lineage-bump-pillar-d-close-gate/70-CONTEXT.md` (if exists) OR placeholder pointing to Phase 70 directory
- `Next action:` → `/gsd:discuss-phase 70` (Pillar D — v1.7 Audit Harness Lineage Bump + Milestone Close)

`## Decisions` block — insert new H3 `### Phase 69 — Plan 02 (close-gate; 2026-MM-DD)` with 5 SC mappings, Linux runtime data point, synthetic PR closure narrative, forward-coordination flags for Phase 70 (mirroring Phase 68 Plan 68-05 H3 structure at STATE.md line 190).

<!-- ROADMAP.md edit targets -->
Phase 69 section (line 345-358):
- Find the `**Plans:** TBD` line (358 in current state) → update to `**Plans:** 2 plans`
- Add or update Plans list:
  ```
  Plans:
  - [x] 69-01-PLAN.md — workflow YAML skeleton (CILINUX-01 deliverable; closed via commit `{69_01_SHA}`)
  - [x] 69-02-PLAN.md — close-gate (SC#5 evidence + 69-VERIFICATION.md + traceability flips; closed via commit `{69_02_SHA}`)
  ```
- If there is a Phase 69 progress table row elsewhere in ROADMAP.md, flip its status from Pending → Complete with closing-SHA cell

<!-- PROJECT.md edit target -->
Locate "Validated" / "Key Decisions" / equivalent table; add row for CILINUX-01:
- Decision: "Add ubuntu-latest CI runner for cross-OS validator chain verification (Phase 69 Pillar C)"
- Outcome: "Workflow YAML landed (Plan 69-01 `{69_01_SHA}`); SC#5 evidence captured via workflow_dispatch baseline + synthetic CRLF PR (closed-without-merge); 69-VERIFICATION.md authored (Plan 69-02 `{69_02_SHA}`)"
- Status: "Validated"

<!-- Synthetic PR mechanics (per CONTEXT D-04 Option D + RESEARCH §Synthetic PR CRLF Injection lines 485-509) -->

Branch: `phase-69/sc5-crlf-evidence`
Target file for CRLF injection: `docs/decision-trees/09-linux-triage.md` (CHAIN-01-hardened surface read by check-phase-51; in Phase 69 path-filter per D-04 sub-decision (a))
CRLF tool: `unix2dos` from `dos2unix` apt package (NOT pre-installed on ubuntu-24.04 — see Pitfall 2)

Negative-case approach: Inject CRLF LOCALLY using PowerShell on the Windows dev machine (the executor is on Windows per env notes). Use `(Get-Content <file>) -join "`r`n" | Set-Content -NoNewline <file>` OR use Python `sys.stdout.write(open(f,'rb').read().replace(b'\n', b'\r\n'))`. Do NOT inject CRLF on the GHA runner — the WHOLE POINT is to push CRLF-tainted committed bytes to origin and have the GHA Linux runner detect them via check-phase-51.

Verification: After CRLF injection, run `file docs/decision-trees/09-linux-triage.md` locally (Git Bash) — expect `with CRLF line terminators` (or use `findstr /r /c:$'\r'` count via PowerShell).

Synthetic-PR workflow file injection (for dos2unix dependency on revert step): The negative-case commit injects CRLF; the positive-case commit reverts via `dos2unix` LOCALLY on the dev machine (not on GHA — since we are pushing post-revert bytes). NO need to inject `apt-get install dos2unix` into the v1.7 workflow file on the synthetic branch — the revert is a normal commit-and-push, not a CI-side script.

(Note: an earlier planning-guidance comment suggested apt-get install inside workflow on synthetic branch. RESEARCH §Synthetic PR CRLF Injection at lines 485-509 shows the simpler pattern — local CRLF injection + local revert + commits pushed; dos2unix only needed if reverting ON the runner, which is not the chosen flow.)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (CHECKPOINT): Confirm Plan 69-01 commit pushed to origin/master before workflow_dispatch trigger</name>
  <read_first>
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-01-PLAN.md (Task 3 ends with local-only commit; Plan 69-02 Wave 2 starts with push)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-RESEARCH.md (§Pitfall 8 lines 418-421 — workflow_dispatch requires workflow file to be on the target branch)
  </read_first>
  <what-built>
    Plan 69-01 (Wave 1) created `.github/workflows/audit-harness-v1.7-integrity.yml` as a single local commit. Plan 69-02 needs this file on origin/master to (a) enable `workflow_dispatch` from master (Pitfall 8), and (b) have a stable base SHA for the synthetic PR branch.
  </what-built>
  <how-to-verify>
    Before this task completes (before push), the human verifies:
    1. Local commit log shows Plan 69-01 commit at HEAD: `git log -1 --oneline` matches `feat(phase-69):`
    2. Working tree clean: `git status` shows "nothing to commit, working tree clean"
    3. `git push origin master` is the expected next action (or `git push origin HEAD:master` if on a different local branch name)

    Then the executor runs:
    ```
    git push origin master
    ```
    And verifies push succeeded via:
    ```
    git ls-remote origin master | head -1
    ```
    Should output the same SHA as local HEAD.

    Human confirms the new workflow file appears in GitHub web UI at `.github/workflows/audit-harness-v1.7-integrity.yml` and the Actions tab shows the workflow registered (may take 5-30 seconds for GHA to register a new workflow file after push).
  </how-to-verify>
  <resume-signal>Type "pushed" to confirm Plan 69-01 commit is on origin/master and workflow appears in GitHub Actions tab.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Trigger workflow_dispatch baseline on master HEAD; capture B.1 evidence</name>
  <read_first>
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VALIDATION.md (task 69-NN-11 — workflow_dispatch baseline assertion)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-CONTEXT.md (D-04 §workflow_dispatch baseline — line 30-31, 134-136; D-03 §::notice format — line 122)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-RESEARCH.md (§Pitfall 8 workflow_dispatch branch selector; §Pitfall 3 ::notice annotation order)
  </read_first>
  <files>(no file writes — runtime evidence capture only; records to executor working notes)</files>
  <action>
    Trigger the new workflow on master HEAD via GitHub CLI:

    1. Verify `gh` CLI auth: `gh auth status` — must show authenticated session. If not, surface auth gate to user.

    2. Trigger workflow_dispatch:
       ```
       gh workflow run audit-harness-v1.7-integrity.yml --ref master
       ```

    3. Wait briefly (2-5s) for the run to register, then identify the new run:
       ```
       gh run list --workflow=audit-harness-v1.7-integrity.yml --limit=1 --json databaseId,url,status,createdAt
       ```
       Record `databaseId` and `url`.

    4. Watch the run to completion:
       ```
       gh run watch <databaseId> --exit-status
       ```
       Exit code 0 = run succeeded; non-zero = run failed.
       NOTE: This may take several minutes; do NOT abort prematurely. Worst-case under D-03 LOCKED `timeout-minutes: 30` is 30min; typical expectation is 2-5min.

    5. Fetch logs for the `linux-chain-ubuntu-latest` job:
       ```
       gh run view <databaseId> --log --job=<job_id>
       ```
       (Job IDs can be enumerated via `gh run view <databaseId> --json jobs`)

    6. Extract `::notice CHAIN_TIMING_LINUX` value:
       ```
       gh run view <databaseId> --log | grep "CHAIN_TIMING_LINUX"
       ```
       Should output something like `::notice title=CHAIN_TIMING_LINUX::Full chain wall-clock: NNs (Windows reference: ~102s; subprocess timeout: 300s)`. Parse the `NN` value as Linux runtime.

    7. Capture per-validator PASS counts from the chain-apex log. The chain-apex `check-phase-66.mjs --verbose` recursively spawns `check-phase-{48..65}` and each emits `Result: NN PASS, M FAIL, K SKIPPED`. Record each phase's PASS count.

    8. Capture the GHA run permalink URL — use the `url` field from step 3 (format: `https://github.com/<owner>/<repo>/actions/runs/<databaseId>`). This is the B.1 evidence URL.

    9. Record all 4 pieces of B.1 evidence in executor working notes for use in Task 6 (69-VERIFICATION.md authoring):
       - GHA run URL
       - Run status (SUCCESS expected — exit 0)
       - `CHAIN_TIMING_LINUX` value (in seconds)
       - Per-validator PASS counts (phases 48-66; expect identical to Windows local — see Task 5)
  </action>
  <verify>
    <automated>gh run list --workflow=audit-harness-v1.7-integrity.yml --limit=1 --json conclusion,url | grep -E '"conclusion":\s*"success"' &amp;&amp; gh run view $(gh run list --workflow=audit-harness-v1.7-integrity.yml --limit=1 --json databaseId --jq '.[0].databaseId') --log | grep -q "CHAIN_TIMING_LINUX"</automated>
  </verify>
  <acceptance_criteria>
    - `gh run watch <run_id> --exit-status` exits 0 (run succeeded)
    - `gh run view <run_id> --log | grep "CHAIN_TIMING_LINUX"` outputs the `::notice` annotation line (task 69-NN-11)
    - Parsed `CHAIN_TIMING_LINUX` value is a positive integer (seconds)
    - All 4 jobs reported success in the run: `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`
    - Per-validator PASS counts captured for phases 48-66 (19 phases)
    - GHA run URL recorded (format: `https://github.com/<owner>/<repo>/actions/runs/<run_id>`)
    - Skip-echo lines present for phases 67-70 (validators not yet shipped — Phase 70 HARNESS-03)
  </acceptance_criteria>
  <done>B.1 evidence captured: GHA run URL, exit 0 status, CHAIN_TIMING_LINUX value, per-validator PASS counts.</done>
</task>

<task type="auto">
  <name>Task 3: Create synthetic CRLF-injection PR; capture B.2 (negative) + B.3 (positive) evidence</name>
  <read_first>
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-CONTEXT.md (D-04 §SC#5 verification proof lines 132-156; D-04 sub-decision (a) — synthetic PR target file lines 143-148)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-RESEARCH.md (§Synthetic PR CRLF Injection lines 485-509)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VALIDATION.md (tasks 69-NN-12 + 69-NN-13)
    - docs/decision-trees/09-linux-triage.md (target file for CRLF injection — read full file to confirm it exists + has LF endings currently)
    - scripts/validation/check-phase-51.mjs (the validator that will catch the regression; read lines 14-22 + lines 188-225 to understand its CRLF-hardened readFile + the assertions it makes about `docs/decision-trees/09-linux-triage.md`)
  </read_first>
  <files>(throwaway branch only — no master tree changes)</files>
  <action>
    Synthetic CRLF-injection PR mechanics per D-04 Option D + RESEARCH §Synthetic PR CRLF Injection. ALL operations on throwaway branch `phase-69/sc5-crlf-evidence`; master tree untouched.

    1. **Create synthetic branch** from current master HEAD (post-Plan 69-01 push):
       ```
       git checkout -b phase-69/sc5-crlf-evidence
       ```
       Confirm branch creation: `git branch --show-current` outputs `phase-69/sc5-crlf-evidence`.

    2. **Confirm target file has LF endings pre-injection**:
       Use Git Bash (since this is Windows host): `file docs/decision-trees/09-linux-triage.md` — expect output WITHOUT "CRLF line terminators".
       OR via PowerShell: `(Get-Content -Raw docs/decision-trees/09-linux-triage.md | Select-String -Pattern "\r\n" -AllMatches).Matches.Count` — expect 0.
       Record the pre-injection state.

    3. **Inject CRLF locally** (PowerShell, since dev machine is Windows per env notes):
       ```powershell
       $content = Get-Content -Raw -Path docs/decision-trees/09-linux-triage.md
       $crlf = $content -replace "`r?`n", "`r`n"
       [System.IO.File]::WriteAllText((Resolve-Path docs/decision-trees/09-linux-triage.md).Path, $crlf)
       ```
       (Equivalent: Python `open(f,'wb').write(open(f,'rb').read().replace(b'\n', b'\r\n').replace(b'\r\r\n', b'\r\n'))`)

       Verify injection: `(Get-Content -Raw docs/decision-trees/09-linux-triage.md | Select-String -Pattern "`r`n" -AllMatches).Matches.Count` — expect > 0 (every line now CRLF-terminated).

    4. **Commit + push negative-case** to synthetic branch:
       ```
       git add docs/decision-trees/09-linux-triage.md
       git commit -m "test(phase-69-sc5): inject CRLF into 09-linux-triage.md for negative case (NO-MERGE)"
       git push origin phase-69/sc5-crlf-evidence
       ```
       Record the commit SHA.

    5. **Open PR** (so the workflow's `pull_request.paths` trigger fires; `docs/decision-trees/09-linux-triage.md` is in the Phase 69 path-filter per D-04 sub-decision (a)):
       ```
       gh pr create --base master --head phase-69/sc5-crlf-evidence \
         --title "SC#5 evidence: CRLF injection negative case (NO-MERGE)" \
         --body "Throwaway PR for Phase 69 CILINUX-01 SC#5 evidence capture. NOT FOR MERGE. Will be closed-without-merge + branch deleted after evidence captured (per CONTEXT D-04 + threat T-69-SYNTHETIC-PR-CONTAMINATION mitigation)."
       ```
       Record PR number + URL.

    6. **Watch CI run for negative case**:
       ```
       gh pr checks <pr_number> --watch
       ```
       Expect the `audit-harness-v1.7-integrity.yml` workflow to fire (because `docs/decision-trees/09-linux-triage.md` is in path-filter). Expect `linux-chain-ubuntu-latest` job to FAIL with check-phase-51 catching the CRLF regression.

       Capture the GHA run URL for this run via:
       ```
       gh run list --workflow=audit-harness-v1.7-integrity.yml --branch=phase-69/sc5-crlf-evidence --limit=1 --json databaseId,url
       ```

    7. **Verify check-phase-51 caught the regression**:
       ```
       gh run view <run_id> --log | grep -E "(check-phase-51|FAIL)"
       ```
       Should show check-phase-51 emitting FAIL on at least one assertion related to `docs/decision-trees/09-linux-triage.md`.

       Record: B.2 evidence URL + which validator caught the regression + log excerpt.

    8. **Revert for positive case** — revert the CRLF-injection commit (on the SAME synthetic branch):
       ```
       git revert --no-edit HEAD
       git push origin phase-69/sc5-crlf-evidence
       ```
       Alternative: if `git revert` produces conflicts (shouldn't, since it's a self-clean revert), use `dos2unix docs/decision-trees/09-linux-triage.md` (if dos2unix installed locally) or PowerShell:
       ```powershell
       $content = Get-Content -Raw -Path docs/decision-trees/09-linux-triage.md
       $lf = $content -replace "`r`n", "`n"
       [System.IO.File]::WriteAllText((Resolve-Path docs/decision-trees/09-linux-triage.md).Path, $lf)
       git add docs/decision-trees/09-linux-triage.md
       git commit -m "test(phase-69-sc5): revert CRLF injection for positive case (NO-MERGE)"
       git push origin phase-69/sc5-crlf-evidence
       ```
       Record the revert/restore commit SHA.

    9. **Watch CI run for positive case**:
       Same PR; latest commit. `gh pr checks <pr_number> --watch`. Expect `linux-chain-ubuntu-latest` job to PASS this time (exit 0).

       Capture B.3 evidence URL.

    10. Record all evidence in executor working notes:
        - B.2: CRLF-injection commit SHA, GHA run URL, exit 1 status, "check-phase-51 FAIL" log excerpt
        - B.3: Revert commit SHA, GHA run URL, exit 0 status
        - PR number + URL
        - Synthetic branch name

    Do NOT close PR or delete branch yet — Task 5 (human checkpoint) handles PR closure to ensure deliberate human acknowledgment of "close-without-merge" rule.
  </action>
  <verify>
    <automated>gh run list --workflow=audit-harness-v1.7-integrity.yml --branch=phase-69/sc5-crlf-evidence --limit=2 --json conclusion --jq '[.[].conclusion]' | grep -E '"failure".*"success"|"success".*"failure"'</automated>
  </verify>
  <acceptance_criteria>
    - Branch `phase-69/sc5-crlf-evidence` exists on origin with 2 commits ahead of master baseline (CRLF inject + revert)
    - PR opened against master; PR has 2 check runs (negative + positive) on `audit-harness-v1.7-integrity.yml` workflow
    - Negative-case CI run conclusion: `failure` (task 69-NN-12); check-phase-51 produces FAIL output
    - Positive-case CI run conclusion: `success` (task 69-NN-13)
    - All 4 evidence URLs recorded (B.2 GHA run + B.3 GHA run + B.2 commit SHA + B.3 revert commit SHA)
    - Master tree NOT touched: `git diff origin/master master -- docs/decision-trees/09-linux-triage.md` returns empty (the synthetic branch lives in its own remote ref, not master)
  </acceptance_criteria>
  <done>B.2 (negative) and B.3 (positive) evidence captured; PR open but not yet closed.</done>
</task>

<task type="auto">
  <name>Task 4: Capture Windows-local chain run for B.4 cross-OS diff</name>
  <read_first>
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VALIDATION.md (task 69-NN-14 — Windows vs Linux PASS-count diff)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-CONTEXT.md (D-04 §B.4 table — line 165)
  </read_first>
  <files>(runtime evidence capture only — no file writes; output goes to executor working notes)</files>
  <action>
    Capture Windows-local chain output for comparison with Linux GHA chain (from Task 2 B.1 evidence).

    1. **Switch back to master** (after Task 3 left HEAD on synthetic branch):
       ```
       git checkout master
       ```
       Confirm: `git branch --show-current` outputs `master`.

    2. **Confirm working tree clean**: `git status` shows nothing to commit. If anything is dirty (shouldn't be — the synthetic branch lives separately), stash or surface to user.

    3. **Run Windows-local full chain** via PowerShell (per 69-VALIDATION.md `Full suite command — Windows local`):
       ```powershell
       cd D:\claude\Autopilot
       48..66 | % {
         $phase = $_
         Write-Output "=== check-phase-$phase ==="
         node "scripts/validation/check-phase-$phase.mjs"
       }
       ```

    4. **Extract per-validator PASS counts** from output. Each validator emits a `Result: NN PASS, M FAIL, K SKIPPED` summary line. Capture each phase's PASS / FAIL / SKIPPED triple.

    5. **Build B.4 cross-OS diff table** in executor working notes:
       | Phase | Windows PASS | Linux PASS | Windows FAIL | Linux FAIL | Windows SKIPPED | Linux SKIPPED |
       |---|---|---|---|---|---|---|
       | 48 | ... | ... | ... | ... | ... | ... |
       | ... | ... | ... | ... | ... | ... | ... |
       | 66 | ... | ... | ... | ... | ... | ... |

       Cross-reference Linux PASS values from Task 2 step 7 (Linux GHA chain log).

    6. **Cross-OS reproducibility verification**: All PASS counts MUST be identical between Windows and Linux (SC#5 requirement). If any mismatch, surface as discovery in 69-VERIFICATION.md §E and decide whether to:
       - Re-run both sides to confirm not a transient (e.g., rotting-external job fired during one run only — unlikely since Phase 69 file has no crons)
       - Investigate the validator that differs (could indicate hidden OS-specific behavior NOT covered by CHAIN-01)
       - If mismatch is persistent and unexplained, ABORT close-gate and escalate to user

    7. **Capture wall-clock runtime** on Windows: time the full PowerShell `48..66 | % {...}` loop using `Measure-Command`. Compare against Linux runtime captured in Task 2 step 6.

    8. Record:
       - Windows full chain wall-clock (Measure-Command output)
       - Per-validator PASS triples (Windows side)
       - Cross-OS diff verdict (identical / mismatch)
       - Windows reference for TIMEOUT-01 update (Task 8)
  </action>
  <verify>
    <automated>powershell -Command "$results = 48..66 | ForEach-Object { $output = node \"scripts/validation/check-phase-$_.mjs\" 2>&amp;1 | Out-String; $passMatch = [regex]::Match($output, 'Result:\s*(\d+)\s*PASS'); if ($passMatch.Success) { [int]$passMatch.Groups[1].Value } else { -1 } }; if ($results -contains -1) { Write-Output 'FAIL: some validators emitted no PASS count'; exit 1 } else { Write-Output \"OK: 19 PASS counts captured: $($results -join ',')\"; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - 19 Windows-local PASS counts captured (phases 48-66)
    - 19 Linux GHA PASS counts available from Task 2 B.1 evidence
    - B.4 diff table built with all 19 rows
    - Cross-OS verdict recorded: ALL IDENTICAL (success) OR specific mismatches enumerated (escalation case)
    - Windows full chain wall-clock measured (expected ~102s per Phase 68 TIMEOUT-01)
  </acceptance_criteria>
  <done>B.4 evidence captured; cross-OS reproducibility verdict recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 (CHECKPOINT): Close synthetic PR without merge + delete branch</name>
  <read_first>
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-CONTEXT.md (D-04 line 140 — "PR closed-without-merge → zero master tree churn"; line 141 — "Branch deleted post-evidence")
    - <security_threat_model> T-69-SYNTHETIC-PR-CONTAMINATION (MEDIUM) — explicit human confirmation required to prevent accidental merge
  </read_first>
  <what-built>
    The synthetic PR `phase-69/sc5-crlf-evidence` was opened in Task 3 with 2 commits (CRLF injection + revert) for SC#5 evidence capture. Both B.2 (negative) and B.3 (positive) GHA run URLs are now captured. The PR was NEVER intended for merge — only as an evidence-capture mechanism. Now it must be closed-without-merge and the branch deleted per CONTEXT D-04 + threat T-69-SYNTHETIC-PR-CONTAMINATION mitigation.
  </what-built>
  <how-to-verify>
    Human verifies the PR closure decision and confirms acceptance of the "close-without-merge" rule before executor proceeds:

    1. Open the PR URL recorded in Task 3 step 5. Verify it shows 2 commits (CRLF injection + revert).
    2. Verify the PR has NOT been merged (status should be OPEN at this point).
    3. Confirm with executor: "Close this PR via 'Close pull request' button (NOT 'Merge pull request'). Acknowledge: synthetic PR is throwaway evidence; merging would contaminate master tree with churn."

    Then executor runs:
    ```
    gh pr close <pr_number> --comment "SC#5 evidence captured (B.2 negative + B.3 positive). Closing without merge per Phase 69 CONTEXT D-04 + threat T-69-SYNTHETIC-PR-CONTAMINATION mitigation."
    gh api repos/{owner}/{repo}/git/refs/heads/phase-69/sc5-crlf-evidence -X DELETE
    ```
    (Alternative one-shot: `gh pr close <pr_number> --delete-branch`)

    Verify closure:
    ```
    gh pr view <pr_number> --json state | grep '"state":"CLOSED"'
    git ls-remote origin phase-69/sc5-crlf-evidence
    ```
    Second command should return empty (branch deleted from origin).

    Local cleanup: `git branch -D phase-69/sc5-crlf-evidence` (delete local branch).

    Final invariant check: `git log --all --grep='phase-69/sc5-crlf-evidence'` on master should show ZERO commits (T-69-SYNTHETIC-PR-CONTAMINATION acceptance).
  </how-to-verify>
  <resume-signal>Type "closed" to confirm PR closed-without-merge + branch deleted + master tree clean of synthetic commits.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Author 69-VERIFICATION.md (sections A-G mirroring 68-VERIFICATION.md)</name>
  <read_first>
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-VERIFICATION.md (FULL FILE — exact structure to mirror; read end-to-end to understand section headings, table shapes, narrative style)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-CONTEXT.md (D-04 §69-VERIFICATION.md evidence shape table line 157-170)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VALIDATION.md (task 69-NN-15 — heading inventory check)
    - .planning/ROADMAP.md (Phase 69 SC#1-5 — line 350-356 to cite in Section C)
    - .planning/REQUIREMENTS.md (line 24-26 CILINUX-01 binding contract to cite in Section A goal narrative)
  </read_first>
  <files>.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md</files>
  <action>
    Author 69-VERIFICATION.md with 7 sections (A-G) per the structure documented in <interfaces> block. Use Write tool. Mirror 68-VERIFICATION.md heading shape: `## Section A — Goal achievement narrative`, `## Section B — Commands + Evidence`, with B.1/B.2/B.3/B.4 as `### B.1 ...` subsections.

    **Section A — Goal achievement narrative (≥ 10 lines):**
    - Cite REQUIREMENTS.md:24-26 CILINUX-01 binding contract verbatim
    - Cite ROADMAP.md Phase 69 SC#1-5
    - State the goal achieved: "Workflow YAML skeleton landed (Plan 69-01 `{69_01_SHA}`); cross-OS reproducibility verified on master HEAD + on synthetic CRLF regression+revert; Linux runtime measured at {N}s (within {ratio}× of Windows ~102s reference)"
    - Note: Phase 70 HARNESS-04 extends this skeleton (does NOT recreate the file)

    **Section B.1 — workflow_dispatch baseline:**
    Table:
    | Property | Value |
    | GHA run URL | (URL from Task 2) |
    | Run conclusion | success (exit 0) |
    | CHAIN_TIMING_LINUX | {N}s |
    | Total workflow runtime | {M}s (workflow-level — sum of 4 jobs) |
    | parse job | success |
    | path-match job | success |
    | harness-run job | success |
    | linux-chain-ubuntu-latest job | success |

    **Section B.2 — Synthetic PR negative case:**
    - Branch: `phase-69/sc5-crlf-evidence` (closed-without-merge per Task 5)
    - CRLF-injection commit SHA: (from Task 3 step 4)
    - GHA run URL: (from Task 3 step 6)
    - Run conclusion: failure (exit 1)
    - Validator caught: check-phase-51 (CHAIN-01-hardened readFile centralization from Plan 68-01 `36a753d` correctly catches CRLF regression on Linux LF runner)
    - Log excerpt: (from Task 3 step 7 grep)

    **Section B.3 — Synthetic PR positive case (revert):**
    - Revert commit SHA: (from Task 3 step 8)
    - GHA run URL: (from Task 3 step 9)
    - Run conclusion: success (exit 0)
    - Confirms: CHAIN-01 readFile fix correctly tolerates LF-restored content (zero false-positive after revert)

    **Section B.4 — Windows local vs Linux GHA PASS-count diff:**
    19-row table per Task 4 step 5. Header: `| Phase | Windows PASS | Linux PASS | Match |`. For each phase 48-66, fill from Task 2 + Task 4 data. Match column: ✓ if identical, ✗ otherwise (and add discovery note in §E).

    Append 4 skip-echo rows for phases 67-70 with values like "skip-echo" / "skip-echo" / ✓ (since both Windows and Linux invoke the skip-if-missing stub identically — Windows via Path-A or local pwsh equivalent, Linux via the for-loop step).

    **Section C — SC#1-5 satisfaction mapping:**
    - SC#1 (new ubuntu-latest job runs full chain on Linux LF): cite B.1 + workflow YAML `linux-chain-ubuntu-latest` job declaration
    - SC#2 (PR-blocking via `continue-on-error: false`): cite explicit declaration in `linux-chain-ubuntu-latest` (D-A9 inheritance documentation)
    - SC#3 (chain exits 0 with NO CHAIN_SKIP entries reported): cite B.1 chain-apex log showing 0 SKIPPED in the in-validator CHAIN_SKIP Set (now empty per Phase 68 `7b635ca`)
    - SC#4 (predecessors byte-unchanged): cite `git diff PRE_69_SHA HEAD -- <3 predecessor paths>` empty (verified in Plan 69-01 Task 3 + cross-checked at Plan 69-02 commit)
    - SC#5 (cross-OS reproducibility on representative PR): cite B.2 + B.3 + B.4

    **Section D — Atomic commit SHA record:**
    - Plan 69-01 commit SHA on master: `{69_01_SHA}`
    - Plan 69-02 close-gate commit SHA: `{69_02_SHA}` (filled at Task 9 commit; left as literal `{69_02_SHA}` placeholder per Phase 68 Plan 68-05 §D §"SHA-placeholder handling" Option (a) chicken-and-egg resolution — readers can `git log --all --grep='Plan 69-02'` to find the actual SHA)
    - Synthetic PR number + closure URL (closed-without-merge per Task 5)
    - Synthetic branch deletion confirmation (deleted from origin + local)
    - 3 predecessor file `git hash-object` SHAs (pre-Phase-69 vs post-Phase-69 — IDENTICAL)

    **Section E — Discoveries:**
    - Linux runtime delta: {N}s Linux vs ~102s Windows = {ratio}× — comment on whether within budget or escalation needed
    - Timeout headroom margin: {N}s vs 300s subprocess cap = {subprocess_ratio}× headroom; {N}s vs 1800s (30-min job cap) = {job_ratio}× headroom
    - Path-filter behavior: synthetic PR fired CI on `docs/decision-trees/09-linux-triage.md` change as expected (D-04 sub-decision (a) path-filter entry working as designed)
    - If any cross-OS PASS-count mismatch surfaced in B.4, document here with hypothesis + Phase 70 follow-up
    - If `dos2unix` install latency on ubuntu-24.04 measured (Task 3 used local CRLF injection, so this is N/A unless executor took alternative path)

    **Section F — Forward-pointer to Phase 70:**
    - HARNESS-04 inherits `linux-chain-ubuntu-latest` job verbatim — DO NOT recreate; extend by adding crons + pin-helper-advisory + sidecar repoints
    - HARNESS-04(a) MUST remove `docs/decision-trees/09-linux-triage.md` from `pull_request.paths` (D-04 sub-decision (a) handoff — Phase 69 SC#5-evidence-only entry, no longer needed once Phase 70 lands v1.7-scoped path-filter)
    - HARNESS-04(b) adds 2 crons: weekly bitrot `0 8 * * 1` + quarterly rotting-external `0 8 1 1,4,7,10 *`
    - HARNESS-04(c) repoints `harness-run` job to `v1.7-milestone-audit.mjs` + repoints `parse` + `path-match` to `v1.7-audit-allowlist.json`
    - HARNESS-04(d) adds `pin-helper-advisory` job bound to v1.7 sidecar
    - HARNESS-03 self-verifier already INTENT-verified per Phase 68 §F (carry-forward; `check-phase-69.mjs` should verify CILINUX-01 via INTENT: file existence + 4 jobs + autocrlf-before-checkout, NOT literal grep of bytes that may legitimately change at HARNESS-04)
    - TIMEOUT-01 in v1.7-DEFERRED-CLEANUP.md updated with measured Linux runtime {N}s as forward-coordination data point (Task 8 below)

    **Section G — Sign-off:**
    - Phase 69 (Pillar C — CI-Linux Hardening) CLOSED 2026-MM-DD
    - CILINUX-01 Active → Validated; traceability flipped across PROJECT/REQUIREMENTS/STATE/ROADMAP (Task 7 below)
    - Three independence axes now stack at v1.7 terminal re-audit per CONTEXT line 110 + Phase 68 §G: D-03 fresh-clone + D-22 fresh-sub-agent + Phase 69 CI-Linux separate-OS

    Use Write tool. Target file: `.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md &amp;&amp; [ $(grep -cE "^## Section [A-G]" .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md) -ge 7 ] &amp;&amp; [ $(wc -l &lt; .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md) -ge 80 ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md` exists
    - File contains ≥ 7 `## Section <letter> —` headings (one each for A, B, C, D, E, F, G) — task 69-NN-15
    - File line count ≥ 80
    - All 4 GHA run URLs embedded (B.1 baseline + B.2 negative + B.3 positive + workflow file URL on master)
    - All commit SHAs embedded (Plan 69-01 SHA + CRLF inject SHA + revert SHA + Plan 69-02 SHA as literal `{69_02_SHA}` placeholder per chicken-and-egg resolution)
    - 19-row B.4 cross-OS PASS diff table present
    - C section maps all 5 ROADMAP SC criteria to specific evidence sub-section
    - F section enumerates 4 sub-points of HARNESS-04 forward-coordination
  </acceptance_criteria>
  <done>69-VERIFICATION.md authored with sections A-G; all 4 evidence URLs + commit SHAs + 19-row cross-OS table embedded.</done>
</task>

<task type="auto">
  <name>Task 7: Traceability flips — REQUIREMENTS / STATE / ROADMAP / PROJECT</name>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 24-26 + line 84 — exact targets for edit)
    - .planning/STATE.md (frontmatter + Current Position + Performance Metrics + Pending Todos + Session Continuity + Decisions sections — full file to understand current state)
    - .planning/ROADMAP.md (lines 345-358 Phase 69 section)
    - .planning/PROJECT.md (locate "Validated" / "Key Decisions" section)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-VERIFICATION.md (Section G sign-off shape to mirror)
    - <interfaces> block above for exact edit targets and replacement text patterns
  </read_first>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/PROJECT.md
  </files>
  <action>
    Apply 4 file edits per <interfaces> block edit targets. Use Edit tool (not Write — these are surgical edits to existing files).

    **(a) REQUIREMENTS.md edits (2 edits):**

    Edit 1: Line 24-26 checkbox flip. Find the line starting `- [ ] **CILINUX-01**:` and flip to `- [x] **CILINUX-01**:`. Append completion clause after the existing requirement body: `— completed 2026-MM-DD in Phase 69 (Plan 69-01 commit \`{69_01_SHA}\` workflow YAML + Plan 69-02 commit \`{69_02_SHA}\` close-gate; SC#5 evidence via workflow_dispatch baseline + synthetic CRLF PR closed-without-merge)`. Use actual SHA for `{69_01_SHA}` (recorded from Plan 69-01 close); leave `{69_02_SHA}` as literal placeholder.

    Edit 2: Line 84 traceability table cell flip. Find `| CILINUX-01 | Phase 69 | Pending |` and change to `| CILINUX-01 | Phase 69 | Complete |`.

    **(b) STATE.md edits (multiple surgical edits):**

    Frontmatter:
    - `completed_phases: 2` → `3`
    - `completed_plans: 8` → `10`
    - `percent: 22` → recalculate. Read current `total_phases` (4 per Phase 68 close edit STATE.md line 12; but original was 9 — verify which value is current; use whatever is in the file). Apply formula `round((completed_phases / total_phases) * 100)`. If total_phases is 4, percent = 75. If still 9, percent = 33.
    - `stopped_at:` → `"Phase 69 closed"`
    - `last_updated:` → current ISO 8601 UTC timestamp (use `Get-Date -Format "yyyy-MM-ddTHH:mm:ss.fffZ"` PowerShell or equivalent)

    `## Current Position` block (lines ~28-31):
    - `Plan: Not started` → `Plan: Complete (Plan 69-01 + Plan 69-02)`
    - `Status: Ready to plan` → `Status: Phase 69 closed; ready for Phase 70 planning`
    - `Last activity:` → current date

    `## Performance Metrics` block (line ~77+):
    Add a new bullet under v1.7 in the velocity list:
    `- Phase 69 (v1.7 Pillar C — CI-Linux Hardening): 2 plans, shipped 2026-MM-DD — CILINUX-01 closed (Plan 69-01 \`{69_01_SHA}\` workflow YAML skeleton + Plan 69-02 \`{69_02_SHA}\` close-gate); Linux chain runtime measured at {N}s via ::notice CHAIN_TIMING_LINUX ({ratio}× Windows ~102s reference; within {subprocess_ratio}× of 300s subprocess cap)`

    `## Pending Todos` block:
    - Remove the Phase 69 planning todo (line ~137 "Phase 69 plan authoring — staging strategy ...")
    - Update Phase 70 todo: remove the sub-bullet "Phase 69 CI-Linux first-run timing measurement to validate 300s subprocess-timeout headroom per Phase 68 TIMEOUT-01 discovery" (it's now satisfied — Plan 69-02 captured the measurement)
    - Leave the Phase 70 main planning entry intact

    `## Session Continuity` block (lines ~157-161):
    - `Last session:` → current ISO timestamp
    - `Stopped at:` → `Phase 69 (Pillar C) closed`
    - `Resume file:` → check if `.planning/phases/70-*/70-CONTEXT.md` exists; if so use that path; otherwise use `.planning/phases/69-.../69-VERIFICATION.md` as the most recent artifact
    - `Next action:` → `/gsd:discuss-phase 70` (Pillar D — v1.7 Audit Harness Lineage Bump + Milestone Close — HARNESS-01..06 per CONTEXT 70 phase scope: Path-A copy v1.6→v1.7 + terminal re-audit via fresh git clone --no-hardlinks + v1.7-MILESTONE-AUDIT.md + traceability closure)

    `## Decisions` block (line ~163+):
    Insert NEW H3 at top of Decisions list (above existing "### Phase 67 — Plan 01" or wherever the most-recent H3 lives — chronological order; new entries on top per file convention):
    ```
    ### Phase 69 — Plan 02 (close-gate; 2026-MM-DD)

    - Plan 69-02 close-gate satisfies all 5 ROADMAP.md Phase 69 SC#1-5 with consolidated evidence in `69-VERIFICATION.md`: SC#1 (linux-chain-ubuntu-latest job declared in `audit-harness-v1.7-integrity.yml` Plan 69-01 commit `{69_01_SHA}`; runs full chain via check-phase-66.mjs chain-apex per D-03 LOCKED) + SC#2 (`continue-on-error: false` explicit on linux-chain-ubuntu-latest job per D-A9 inheritance) + SC#3 (workflow_dispatch B.1 baseline log shows 0 SKIPPED in in-validator CHAIN_SKIP Set — empty per Phase 68 `7b635ca`) + SC#4 (predecessor workflow files v1.4 + v1.5 + v1.6 byte-unchanged across Plan 69-01 + 69-02 SHAs verified via `git diff PRE_69_SHA HEAD -- <3 paths>` empty) + SC#5 (synthetic CRLF PR negative case B.2 caught by check-phase-51 confirming Plan 68-01 `36a753d` readFile centralization correctness on Linux LF runner; revert positive case B.3 exits 0; Windows-vs-Linux PASS-count diff B.4 identical across 19 phases)
    - Linux runtime measured: {N}s via ::notice CHAIN_TIMING_LINUX vs ~102s Windows reference = {ratio}× delta; {subprocess_ratio}× headroom vs 300s subprocess cap; {job_ratio}× headroom vs 1800s 30-min job cap. v1.7-DEFERRED-CLEANUP.md TIMEOUT-01 updated with measurement (Task 8); disposition: {within budget / forward-coordination flag / escalation}
    - Synthetic CRLF PR `phase-69/sc5-crlf-evidence` closed-without-merge + branch deleted from origin + local per CONTEXT D-04 + threat T-69-SYNTHETIC-PR-CONTAMINATION mitigation; `git log --all --grep='phase-69/sc5-crlf-evidence'` on master returns ZERO commits (master tree clean)
    - Three auditor-independence axes now available at v1.7 terminal re-audit (Phase 70 HARNESS-05): D-03 fresh-clone + D-22 fresh-sub-agent + Phase 69 CI-Linux separate-OS
    - Forward-coordination flags for Phase 70: (1) HARNESS-04 extends `audit-harness-v1.7-integrity.yml` (do NOT recreate); (2) HARNESS-04(a) removes `docs/decision-trees/09-linux-triage.md` from path-filter (Phase 69 D-04 sub-decision (a) SC#5-evidence-only entry); (3) HARNESS-04(b) adds 2 crons + rotting-external-quarterly job; (4) HARNESS-04(c) repoints harness-run + parse + path-match v1.6→v1.7 sidecar references; (5) HARNESS-04(d) adds pin-helper-advisory bound to v1.7 sidecar; (6) HARNESS-03 self-verifier for check-phase-69.mjs verifies CILINUX-01 via INTENT (4 jobs + autocrlf-before-checkout + chain-apex invocation present) per Phase 68 §F carry-forward
    ```

    **(c) ROADMAP.md edits (lines 345-358 Phase 69 section):**
    - Change `**Plans:** TBD` → `**Plans:** 2 plans`
    - Add Plans list under the goal/SC#5 block:
      ```
      Plans:
      - [x] 69-01-PLAN.md — workflow YAML skeleton (CILINUX-01 deliverable; `{69_01_SHA}`)
      - [x] 69-02-PLAN.md — close-gate (SC#5 evidence + 69-VERIFICATION.md + traceability flips; `{69_02_SHA}`)
      ```
    - If ROADMAP.md has a "Phase 69" progress table row elsewhere (search for "Phase 69" across the file), flip its status `Pending → Complete` and add closing-SHAs cell

    **(d) PROJECT.md edits:**
    Locate the "Validated" / "Key Decisions" / equivalent table (read PROJECT.md to find the correct table — naming may vary). Append a new row for CILINUX-01:
    | Decision | Outcome | Status |
    | --- | --- | --- |
    | "Add ubuntu-latest CI runner for cross-OS validator chain verification (Phase 69 Pillar C — CILINUX-01)" | "Workflow YAML landed at `.github/workflows/audit-harness-v1.7-integrity.yml` (Plan 69-01 `{69_01_SHA}`); SC#5 evidence: workflow_dispatch baseline `::notice CHAIN_TIMING_LINUX::Full chain {N}s` + synthetic CRLF PR closed-without-merge + Windows-vs-Linux PASS-count diff identical (19 phases); 69-VERIFICATION.md Plan 69-02 `{69_02_SHA}`" | Validated |

    Use Edit tool for each surgical edit. Read each file before editing if needed for exact line context. Apply edits one file at a time.
  </action>
  <verify>
    <automated>grep -qF "| CILINUX-01 | Phase 69 | Complete |" .planning/REQUIREMENTS.md &amp;&amp; grep -q "completed_phases: 3" .planning/STATE.md &amp;&amp; grep -q "Phase 69 — Plan 02 (close-gate" .planning/STATE.md &amp;&amp; grep -q "Phase 69 (v1.7 Pillar C" .planning/STATE.md &amp;&amp; grep -E "^\*\*Plans:\*\* 2 plans" .planning/ROADMAP.md &amp;&amp; grep -q "CILINUX-01" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md line 24-26 `- [ ] **CILINUX-01**:` → `- [x] **CILINUX-01**:` with completion clause appended
    - REQUIREMENTS.md line 84 `| CILINUX-01 | Phase 69 | Pending |` → `| CILINUX-01 | Phase 69 | Complete |`
    - STATE.md frontmatter `completed_phases: 3`, `completed_plans: 10`, `stopped_at: "Phase 69 closed"`, `last_updated` is fresh ISO timestamp
    - STATE.md Decisions section contains new H3 `### Phase 69 — Plan 02 (close-gate; 2026-MM-DD)` with 5 SC mappings + Linux runtime data point + synthetic PR closure narrative + 6 Phase 70 forward-coordination flags
    - STATE.md Performance Metrics gains Phase 69 line
    - STATE.md Pending Todos no longer contains Phase 69 planning entry
    - STATE.md Session Continuity points to Phase 70 next action
    - ROADMAP.md Phase 69 section shows `**Plans:** 2 plans` + 2 checked plan entries
    - PROJECT.md Validated section gains CILINUX-01 row with closing SHA `{69_01_SHA}`
  </acceptance_criteria>
  <done>All 4 traceability files updated; CILINUX-01 fully flipped Pending → Complete across the project.</done>
</task>

<task type="auto">
  <name>Task 8: Update v1.7-DEFERRED-CLEANUP.md TIMEOUT-01 entry with measured Linux runtime</name>
  <read_first>
    - .planning/milestones/v1.7-DEFERRED-CLEANUP.md (TIMEOUT-01 entry — added at Phase 68 close per STATE.md line 193)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-CONTEXT.md (forward-coordination flag: Phase 69 first-run timing measurement as TIMEOUT-01 data point)
    - <interfaces> block above for TIMEOUT-01 entry update format
  </read_first>
  <files>.planning/milestones/v1.7-DEFERRED-CLEANUP.md</files>
  <action>
    Update the TIMEOUT-01 entry in `.planning/milestones/v1.7-DEFERRED-CLEANUP.md` with the measured Linux chain runtime captured in Task 2.

    1. Read the file; locate the `## TIMEOUT-01` or `### TIMEOUT-01` heading (added at Phase 68 close per STATE.md line 193 + Phase 68 Plan 68-05 close-gate).

    2. Append (after the existing entry body — do NOT delete prior content) a "Phase 69 measurement" sub-section with this exact format (substitute actual measured value `{N}`):
       ```
       **Phase 69 measurement (2026-MM-DD via `::notice CHAIN_TIMING_LINUX`):**
       - Linux GHA chain wall-clock: {N}s (Windows reference: ~102s; subprocess timeout: 300s)
       - GHA run URL: <B.1 evidence URL from Task 2>
       - Headroom analysis:
         - vs 300s subprocess cap = {N/300:.2f}× headroom
         - vs 1800s (30-min job cap) = {N/1800:.2f}× headroom
       - Cross-OS PASS-count diff (B.4): identical across 19 phases (Windows vs Linux PASS counts match per validator)
       - Disposition:
         - **If N ≤ 150s:** "Within budget; no escalation; TIMEOUT-01 to be CLOSED at Phase 70 v1.7-milestone close-gate"
         - **If 150s < N ≤ 1500s:** "Within budget but >1.5× Windows reference; flag for Phase 70 HARNESS-04 timeout-budget review (consider bumping `timeout-minutes: 30 → 45` defensively when Phase 70 extends the workflow)"
         - **If N > 1500s:** "ESCALATE TIMEOUT-01 to v1.8+ subprocess-timeout-architecture review per CONTEXT D-03 Pitfall 5 — investigate chain-apex parallelization or runner pinning (ubuntu-22.04 alternative)"
       - Actual disposition for measured {N}s: <select one of the 3 bullets above; remove the other 2>
       ```

    3. If the disposition selected is "ESCALATE" (i.e., N > 1500s), ALSO add a NEW item entry near the top of v1.7-DEFERRED-CLEANUP.md (above TIMEOUT-01 if convention is reverse-chronological) named `TIMEOUT-01-ESCALATED` and route it to v1.8+ with the relevant context (this would be unusual — typical expectation is N ~ 100-250s on Linux).
  </action>
  <verify>
    <automated>grep -q "Phase 69 measurement" .planning/milestones/v1.7-DEFERRED-CLEANUP.md &amp;&amp; grep -q "Linux GHA chain wall-clock" .planning/milestones/v1.7-DEFERRED-CLEANUP.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains the new "Phase 69 measurement" sub-section under TIMEOUT-01
    - Measured runtime `{N}s` value is a positive integer (not a placeholder)
    - B.1 evidence URL embedded
    - Headroom analysis ratios computed
    - Exactly ONE disposition bullet retained (the other 2 removed); selected bullet matches the actual measured runtime band
  </acceptance_criteria>
  <done>v1.7-DEFERRED-CLEANUP.md TIMEOUT-01 entry updated with Phase 69 runtime measurement + disposition.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9 (CHECKPOINT): Confirm Linux runtime disposition + ready to commit close-gate</name>
  <read_first>
    - .planning/milestones/v1.7-DEFERRED-CLEANUP.md (post-Task-8 TIMEOUT-01 update)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md (post-Task-6 evidence file)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-RESEARCH.md (§Pitfall 5 timeout-minutes vs theoretical worst-case)
  </read_first>
  <what-built>
    Tasks 2-8 captured all SC#5 evidence, authored 69-VERIFICATION.md, flipped traceability across 4 files, and updated TIMEOUT-01 with the measured Linux chain runtime. Before the close-gate commit, the human reviews the disposition decision: was Linux runtime within budget, flagged for Phase 70 review, or requires escalation to v1.8+?
  </what-built>
  <how-to-verify>
    Human reviews:
    1. Open `.planning/milestones/v1.7-DEFERRED-CLEANUP.md` and verify the TIMEOUT-01 entry now contains "Phase 69 measurement" with a concrete `{N}s` value and a single disposition bullet.
    2. Confirm the disposition matches the measured runtime:
       - If N ≤ 150s: confirm "Within budget" disposition selected — no further action.
       - If 150s < N ≤ 1500s: confirm "flag for Phase 70 HARNESS-04 timeout-budget review" disposition selected — Phase 70 planner reads this as a forward-coordination flag.
       - If N > 1500s: confirm "ESCALATE" disposition selected — this is unusual; double-check before proceeding.
    3. Open `.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md` and visually verify all 7 sections (A-G) are populated, all evidence URLs are real GHA run URLs (not placeholders), and the 19-row B.4 cross-OS table has actual PASS counts (not `TBD`).
    4. Confirm the synthetic PR is closed (re-check via `gh pr view <pr_number> --json state` — must be `CLOSED`).
    5. Confirm the executor is ready to commit the close-gate (all file edits from Tasks 6-8 staged, no untracked files outside expected set).
  </how-to-verify>
  <resume-signal>Type "approved" to confirm disposition + ready to commit, OR describe issues if anything looks off (e.g., "the runtime measurement seems suspiciously low" or "Section B.4 has TBD values").</resume-signal>
</task>

<task type="auto">
  <name>Task 10: Close-gate commit (single commit landing 69-VERIFICATION.md + 4 traceability files + DEFERRED-CLEANUP update + SUMMARY)</name>
  <read_first>
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-05-PLAN.md (Phase 68 close-gate Commit B precedent — same commit shape applies here)
    - .planning/templates/summary.md (SUMMARY.md authoring template — if exists; otherwise mirror 68-05-SUMMARY.md shape)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-05-SUMMARY.md (if exists; structure to mirror for 69-02-SUMMARY.md)
  </read_first>
  <files>
    .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-02-SUMMARY.md (NEW),
    (all files modified in Tasks 6-8 staged for this commit)
  </files>
  <action>
    1. **Author 69-02-SUMMARY.md** (NEW file) mirroring `68-05-SUMMARY.md` shape (read that file first for structure):
       - Plan 69-02 close-gate execution summary
       - Both SHAs (Plan 69-01 `{69_01_SHA}` already known; Plan 69-02 `{69_02_SHA}` left as literal placeholder per Phase 68 chicken-and-egg resolution)
       - All evidence URLs (B.1 baseline + B.2 negative + B.3 positive)
       - Synthetic PR number + closure confirmation
       - Linux runtime measurement {N}s + disposition
       - Files modified list (7 files: 69-VERIFICATION.md NEW + 69-02-SUMMARY.md NEW + REQUIREMENTS.md + STATE.md + ROADMAP.md + PROJECT.md + v1.7-DEFERRED-CLEANUP.md)
       - Forward handoff to Phase 70: HARNESS-04 inherits + extends; 6 forward-coordination flags enumerated

    2. **Verify pre-commit state**:
       - `git status` shows expected modified files: REQUIREMENTS.md, STATE.md, ROADMAP.md, PROJECT.md, v1.7-DEFERRED-CLEANUP.md
       - `git status` shows expected NEW files: 69-VERIFICATION.md, 69-02-SUMMARY.md
       - `git status` does NOT show any predecessor workflow files modified (T-69-PREDECESSOR-DRIFT defense)
       - Working tree contains NO untracked debris (check temp/working files from Tasks 2-4)

    3. **Final predecessor invariant check** (T-69-PREDECESSOR-DRIFT HIGH acceptance):
       ```
       git diff <PRE_69_SHA from Plan 69-01 Task 1> HEAD -- .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml
       ```
       MUST return empty across the Plan 69-01 + Plan 69-02 cumulative range. (If executor did not record PRE_69_SHA from Plan 69-01 Task 1, derive via: `git log --all --oneline | grep "feat(phase-69): add audit-harness" | awk '{print $1}' | xargs -I{} git rev-parse {}^`.)

    4. **Stage all expected files explicitly** (NOT `git add -A`):
       ```
       git add .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md
       git add .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-02-SUMMARY.md
       git add .planning/REQUIREMENTS.md
       git add .planning/STATE.md
       git add .planning/ROADMAP.md
       git add .planning/PROJECT.md
       git add .planning/milestones/v1.7-DEFERRED-CLEANUP.md
       ```

    5. **Confirm staged set is exactly 7 files**:
       ```
       git diff --cached --name-only | wc -l   # expect 7
       ```

    6. **Commit with this exact message** (HEREDOC; substitute {N}s and runtime band disposition before commit):
       ```
       docs(phase-69): close Phase 69 Pillar C — CILINUX-01 Validated (Plan 69-02 close-gate)

       Phase 69 (Pillar C — CI-Linux Hardening) CLOSED 2026-MM-DD. CILINUX-01 Active →
       Validated. 5 ROADMAP.md SC#1-5 satisfied with evidence consolidated in
       69-VERIFICATION.md sections A-G.

       Evidence summary:
       - SC#1: linux-chain-ubuntu-latest job declared in audit-harness-v1.7-integrity.yml
         (Plan 69-01 commit; chain-apex check-phase-66.mjs invoked recursively per
         Phase 68 CHAIN-03)
       - SC#2: continue-on-error: false explicit declaration on linux-chain-ubuntu-latest
         (D-A9 inheritance documented)
       - SC#3: workflow_dispatch B.1 baseline log shows 0 SKIPPED in in-validator
         CHAIN_SKIP Set (empty per Phase 68 7b635ca)
       - SC#4: git diff PRE_69_SHA HEAD on 3 predecessor workflow files (v1.4 + v1.5 +
         v1.6) returns empty — byte-unchanged invariant satisfied across Plan 69-01 +
         69-02 cumulative SHAs
       - SC#5: synthetic CRLF PR phase-69/sc5-crlf-evidence — negative case (B.2)
         exits 1 with check-phase-51 catching regression (confirming Plan 68-01 36a753d
         readFile centralization correctness on Linux LF runner); positive case (B.3)
         exits 0 post-revert; Windows-vs-Linux PASS-count diff B.4 identical across
         19 phases; PR closed-without-merge + branch deleted (T-69-SYNTHETIC-PR-
         CONTAMINATION mitigation)

       Linux runtime measurement: {N}s via ::notice CHAIN_TIMING_LINUX
       (Windows ref ~102s; {ratio}× delta; {subprocess_ratio}× headroom vs 300s
       subprocess cap). Disposition: {within budget / Phase 70 HARNESS-04 review flag
       / v1.8+ escalation}. v1.7-DEFERRED-CLEANUP.md TIMEOUT-01 updated.

       Three auditor-independence axes now stack at v1.7 terminal re-audit (Phase 70
       HARNESS-05): D-03 fresh-clone + D-22 fresh-sub-agent + Phase 69 CI-Linux
       separate-OS.

       Forward-coordination to Phase 70 (6 flags in STATE.md Decisions §):
       (1) HARNESS-04 extends audit-harness-v1.7-integrity.yml (do NOT recreate);
       (2) HARNESS-04(a) removes docs/decision-trees/09-linux-triage.md from
       path-filter; (3) HARNESS-04(b) adds 2 crons + rotting-external-quarterly;
       (4) HARNESS-04(c) repoints harness-run + parse + path-match v1.6→v1.7 sidecar;
       (5) HARNESS-04(d) adds pin-helper-advisory bound to v1.7 sidecar;
       (6) HARNESS-03 check-phase-69.mjs verifies CILINUX-01 via INTENT.

       Traceability flipped: REQUIREMENTS.md line 24-26 + line 84 + STATE.md
       frontmatter + Performance Metrics + Pending Todos + Session Continuity +
       Decisions + ROADMAP.md Phase 69 row + PROJECT.md Validated section + 69-02-
       SUMMARY.md authored.
       ```

    7. **Push close-gate commit** to origin/master:
       ```
       git push origin master
       ```

    8. **Post-commit verification**:
       - `git log -1 --pretty=format:"%H %s"` shows the close-gate commit on master
       - `git ls-remote origin master | head -1` matches local HEAD SHA
       - Plan 69-02 commit SHA recorded (for executor working notes + downstream Phase 70 reference; this is the `{69_02_SHA}` literal placeholder in 69-VERIFICATION.md Section D — readers can `git log --all --grep='close Phase 69 Pillar C'` to find it)
       - `git diff PRE_69_SHA HEAD -- <3 predecessor workflow paths>` STILL empty (final T-69-PREDECESSOR-DRIFT check)
  </action>
  <verify>
    <automated>git log -1 --pretty=format:"%H %s" HEAD | grep -E "^[0-9a-f]{40} docs\(phase-69\): close Phase 69 Pillar C" &amp;&amp; [ $(git log -1 --name-only --pretty=format:"" HEAD | grep -v "^$" | wc -l) -eq 7 ] &amp;&amp; [ $(git diff $(git log --all --oneline | awk '$2~/^feat\(phase-69\): add audit-harness/{print $1; exit}' | xargs -I{} git rev-parse {}^)..HEAD -- .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml | wc -l) -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - 69-02-SUMMARY.md authored (≥ 30 lines)
    - Close-gate commit lands with exactly 7 files (5 modified + 2 new)
    - Commit message matches `docs(phase-69): close Phase 69 Pillar C` regex
    - `git diff PRE_69_SHA HEAD -- <3 predecessor workflow paths>` returns empty (T-69-PREDECESSOR-DRIFT final acceptance)
    - Commit pushed to origin/master successfully
    - `git ls-remote origin master` matches local HEAD
  </acceptance_criteria>
  <done>Plan 69-02 close-gate commit landed on origin/master; Phase 69 (Pillar C) CLOSED; CILINUX-01 Validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → origin/master | Plan 69-01 push (Task 1 checkpoint) + close-gate push (Task 10); after these pushes, workflow becomes the actual CI gate |
| origin/master → GHA runner | workflow_dispatch (Task 2) + PR-trigger (Task 3 synthetic PR) both fire the workflow on ephemeral ubuntu-latest runners |
| synthetic branch `phase-69/sc5-crlf-evidence` → origin | Synthetic PR exists temporarily on origin; closed-without-merge + deleted in Task 5 prevents contamination |
| Plan 69-01 boundary → Plan 69-02 boundary | Plan 69-01 commit must be on origin/master BEFORE Plan 69-02 evidence capture starts (Pitfall 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-WORKFLOW-INJECTION | Tampering | `${{ github.event.* }}` interpolation | mitigate | Already verified in Plan 69-01 Task 2 acceptance (`! grep -E '\$\{\{\s*github\.event\.'`); Plan 69-02 makes no edits to the workflow file itself so the property is preserved by default. |
| T-69-ACTION-TYPOSQUAT | Tampering / EoP | Floating-version action references | mitigate | Same — verified in Plan 69-01; Plan 69-02 makes no workflow YAML edits. |
| T-69-PREDECESSOR-DRIFT | Tampering | Predecessor workflow files modified during Plan 69-02 close-gate commit | mitigate (HIGH SEVERITY — blocking) | Task 10 step 3 explicit `git diff PRE_69_SHA HEAD -- <3 paths>` MUST return empty across cumulative Plan 69-01 + Plan 69-02 range. Task 10 step 4 stages ONLY the 7 expected files (NOT `git add -A`). |
| T-69-SYNTHETIC-PR-CONTAMINATION | Tampering | Synthetic PR `phase-69/sc5-crlf-evidence` accidentally merged to master | mitigate (MEDIUM — blocking human checkpoint) | Task 5 is a `checkpoint:human-verify` BLOCKING gate that requires explicit human confirmation of "close without merge" + branch deletion. Acceptance: `git log --all --grep='phase-69/sc5-crlf-evidence'` on master shows zero commits. PR closed via `gh pr close --comment` not `gh pr merge`. Local branch deleted via `git branch -D` post-checkpoint. |
| T-69-CHECKPOINT-AUTH-GATE | Information Disclosure / Tampering | `gh` CLI unauthenticated when triggering workflow_dispatch | mitigate | Task 2 step 1 `gh auth status` check; if not authenticated, executor surfaces a dynamic auth gate (per role-spec "auth gates created dynamically"). User authenticates via `gh auth login`; executor retries. |
| T-69-EVIDENCE-LATENCY | Information Disclosure | GHA run URLs captured before run completes (race condition; URL exists but log is empty) | mitigate | Task 2 step 4 `gh run watch --exit-status` blocks until run completes; only then captures log. Task 3 step 6/9 same `gh pr checks --watch` pattern. No log-grep happens before run completion. |
</threat_model>

<verification>
**Plan 69-02 close-gate verification:**

1. All 4 GHA run URLs captured (B.1 baseline + B.2 negative + B.3 positive + workflow file URL)
2. All commit SHAs recorded (Plan 69-01 SHA + CRLF inject SHA + revert SHA + close-gate SHA)
3. Synthetic PR closed-without-merge; branch deleted from origin + local; master tree clean
4. 69-VERIFICATION.md has 7 sections A-G; 19-row B.4 cross-OS table; all evidence embedded
5. 4 traceability files updated (REQUIREMENTS / STATE / ROADMAP / PROJECT) per <interfaces> edit targets
6. v1.7-DEFERRED-CLEANUP.md TIMEOUT-01 entry contains Phase 69 measurement + disposition
7. Close-gate commit lands with exactly 7 files; predecessor invariant final-check empty
8. Linux runtime disposition (within budget / Phase 70 review flag / escalation) recorded in 3 locations: 69-VERIFICATION.md §E + STATE.md Decisions § + v1.7-DEFERRED-CLEANUP.md TIMEOUT-01

**Full chain verification commands (run from master HEAD post-close-gate push):**
- `grep -q "| CILINUX-01 | Phase 69 | Complete |" .planning/REQUIREMENTS.md`
- `grep -q "completed_phases: 3" .planning/STATE.md`
- `grep -q "Phase 69 — Plan 02 (close-gate" .planning/STATE.md`
- `[ $(grep -cE "^## Section [A-G]" .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VERIFICATION.md) -ge 7 ]`
- `git diff PRE_69_SHA HEAD -- .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml | wc -l` returns 0
- `git log --all --grep='phase-69/sc5-crlf-evidence'` on master returns ZERO commits (T-69-SYNTHETIC-PR-CONTAMINATION clean)
</verification>

<success_criteria>
Plan 69-02 satisfies ROADMAP Phase 69:
- **SC#1** (already satisfied by Plan 69-01; Plan 69-02 confirms via B.1 runtime evidence)
- **SC#2** (already satisfied by Plan 69-01; Plan 69-02 confirms via B.1 PR-blocking behavior observed)
- **SC#3** (chain exits 0 with 0 SKIPPED on ubuntu-latest) — **B.1 evidence**
- **SC#4** (predecessors byte-unchanged) — Task 10 step 3 + 8 final-check
- **SC#5** (cross-OS reproducibility on representative PR) — **B.2 + B.3 + B.4 evidence**

Phase 69 (Pillar C — CI-Linux Hardening) CLOSED. CILINUX-01 Active → Validated. Ready for Phase 70 (Pillar D — v1.7 Audit Harness Lineage Bump + Milestone Close).
</success_criteria>

<output>
Create `.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-02-SUMMARY.md` (authored in Task 10 step 1). Must include:
- Plan 69-01 SHA `{69_01_SHA}` + Plan 69-02 SHA literal placeholder
- All 4 GHA run URLs + 3 synthetic-branch commit SHAs (inject + revert + PR closure URL)
- Linux runtime measurement {N}s + disposition decision
- 7-file commit manifest
- Forward handoff narrative for Phase 70 HARNESS-04 (6 flags)
- Sign-off: Phase 69 (Pillar C) CLOSED 2026-MM-DD
</output>
