---
phase: 69
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/audit-harness-v1.7-integrity.yml
autonomous: true
requirements: [CILINUX-01]
requirements_addressed: [CILINUX-01]

must_haves:
  truths:
    - "New file .github/workflows/audit-harness-v1.7-integrity.yml exists on master HEAD after this plan's commit"
    - "Workflow declares exactly 4 jobs per D-03 LOCKED hybrid topology: parse + path-match + harness-run + linux-chain-ubuntu-latest"
    - "linux-chain-ubuntu-latest job sets `git config --global core.autocrlf false` as a step BEFORE actions/checkout@v4 (LF-fidelity contract per D-01 + Pitfall 1)"
    - "linux-chain-ubuntu-latest invokes `node scripts/validation/check-phase-66.mjs --verbose` wrapped in START/END date+%s capture and emits `::notice title=CHAIN_TIMING_LINUX::...` annotation"
    - "Skip-if-missing stubs for phases 67-70 use literal Phase 42 D-31 attribution string"
    - "Phase 69 file contains NO `on.schedule:` crons and NO `pin-helper-advisory` job (both deferred to Phase 70 HARNESS-04(b)/(d))"
    - "Transitional path-filter per D-04 sub-decision (a) includes `docs/decision-trees/09-linux-triage.md` (SC#5-evidence-only surface; Phase 70 HARNESS-04(a) removes)"
    - "Predecessor workflow files audit-harness-integrity.yml + audit-harness-v1.5-integrity.yml + audit-harness-v1.6-integrity.yml remain BYTE-UNCHANGED in this plan's commit"
  artifacts:
    - path: .github/workflows/audit-harness-v1.7-integrity.yml
      provides: "Phase 69 CILINUX-01 workflow skeleton (4 jobs; transitional sidecar references; no crons; no pin-helper-advisory)"
      contains: "name: Audit Harness v1.7 Integrity"
      min_lines: 70
  key_links:
    - from: ".github/workflows/audit-harness-v1.7-integrity.yml linux-chain-ubuntu-latest job"
      to: "scripts/validation/check-phase-66.mjs"
      via: "node invocation with --verbose flag"
      pattern: "node scripts/validation/check-phase-66.mjs --verbose"
    - from: ".github/workflows/audit-harness-v1.7-integrity.yml harness-run job"
      to: "scripts/validation/v1.6-milestone-audit.mjs"
      via: "transitional invocation (Phase 70 HARNESS-04(c) repoints)"
      pattern: "node scripts/validation/v1.6-milestone-audit.mjs --verbose"
    - from: ".github/workflows/audit-harness-v1.7-integrity.yml parse job"
      to: "scripts/validation/v1.6-audit-allowlist.json"
      via: "transitional JSON.parse (Phase 70 HARNESS-04 repoints to v1.7-audit-allowlist.json)"
      pattern: "v1.6-audit-allowlist.json"
---

<objective>
Author the new file `.github/workflows/audit-harness-v1.7-integrity.yml` as a Phase 69-scoped SKELETON per D-01 LOCKED Option B: 4 jobs (parse + path-match + harness-run + linux-chain-ubuntu-latest), transitional path-filter including `docs/decision-trees/09-linux-triage.md` per D-04 sub-decision (a), workflow_dispatch trigger, NO crons, NO pin-helper-advisory. Single non-atomic commit to master (no coordinated cross-file edits; predecessors byte-unchanged).

Purpose: Establishes the cross-OS verification surface for the v1.7 validator chain. The `linux-chain-ubuntu-latest` job is the THIRD auditor-independence axis (separate OS) stacking with D-03 fresh-clone and D-22 fresh-sub-agent. Phase 70 HARNESS-04 extends this skeleton (does NOT recreate the file).

Output: One new workflow YAML file landed on master via single commit. Predecessor workflows untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-CONTEXT.md
@.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-RESEARCH.md
@.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-PATTERNS.md
@.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VALIDATION.md
@.github/workflows/audit-harness-v1.6-integrity.yml

<interfaces>
<!-- Path-A source line ranges in `.github/workflows/audit-harness-v1.6-integrity.yml` (verified by reading file end-to-end during plan-phase). -->
<!-- Executor copies VERBATIM (with one-line repoints where the v1.7 path-filter / coexistence-comment differs). -->

v1.6 → v1.7 verbatim copy ranges (cite by line; do NOT paraphrase):

- `parse` job:           v1.6 lines 31-48 (18 lines; references `v1.6-audit-allowlist.json`)
- `path-match` job:      v1.6 lines 50-63 (14 lines; greps for `v1.6-audit-allowlist.json` reference inside `v1.6-milestone-audit.mjs`)
- `harness-run` job:     v1.6 lines 65-74 (10 lines; runs `node scripts/validation/v1.6-milestone-audit.mjs --verbose`)
- `name:` line:          v1.6 line 5 (`name: Audit Harness v1.6 Integrity` → rewrite to `name: Audit Harness v1.7 Integrity`)
- `workflow_dispatch:`   v1.6 line 28 (verbatim)
- Skip-if-missing stub canonical form: v1.6 lines 86-90 (the `if [ -f ... ]; then ... ; else echo "...skipping (graceful degradation per Phase 42 D-31)"; fi` shape)

v1.6 → v1.7 DELIBERATE OMISSIONS in Phase 69:
- `on.schedule:` block at v1.6 lines 25-27 — OMITTED (Phase 70 HARNESS-04(b))
- `rotting-external-quarterly` job at v1.6 lines 156-188 — OMITTED (Phase 70 HARNESS-04(b))
- `pin-helper-advisory` job at v1.6 lines 190-206 — OMITTED (Phase 70 HARNESS-04(d))
- 19-job per-validator parallel topology (v1.6 lines 76-154 = check-phase-62 through check-phase-66) — OMITTED; replaced by single `linux-chain-ubuntu-latest` chain-apex job per D-03 LOCKED

NEW in v1.7 (no v1.6 line range — synthesize from 69-RESEARCH §Pattern 1 + §Code Examples):
- `linux-chain-ubuntu-latest` job — synthesize from 69-RESEARCH.md lines 303-329 (Pattern 1 example YAML block) + 69-RESEARCH.md §Code Examples §"Pre-Checkout autocrlf Disable" + §"Wall-Clock Measurement + `::notice`" + §"Skip-if-Missing Stub"

v1.7 transitional path-filter (per D-01 LOCKED CONTEXT.md table line 65 + D-04 sub-decision (a)):
- `scripts/validation/check-phase-*.mjs`
- `.github/workflows/audit-harness-v1.7-integrity.yml`
- `.planning/REQUIREMENTS.md`
- `docs/decision-trees/09-linux-triage.md` (SC#5-evidence-only; Phase 70 HARNESS-04(a) removes)

Mandatory step ORDER inside `linux-chain-ubuntu-latest` job (Pitfall 1 — Pre-Checkout autocrlf):
  STEP 1: `git config --global core.autocrlf false` (run step)
  STEP 2: `actions/checkout@v4` (uses)
  STEP 3: `actions/setup-node@v4` with `node-version: '20'` (uses)
  STEP 4: chain-apex run wrapped in `START=$(date +%s); node scripts/validation/check-phase-66.mjs --verbose; END=$(date +%s); echo "::notice title=CHAIN_TIMING_LINUX::Full chain wall-clock: $((END-START))s (Windows reference: ~102s; subprocess timeout: 300s)"`
  STEP 5: skip-if-missing for-loop stub `for i in 67 68 69 70; do if [ -f scripts/validation/check-phase-$i.mjs ]; then node scripts/validation/check-phase-$i.mjs; else echo "check-phase-$i.mjs not present -- skipping (graceful degradation per Phase 42 D-31)"; fi; done`

Job-level configuration on `linux-chain-ubuntu-latest`:
- `runs-on: ubuntu-latest`
- `needs: harness-run` (NOT independent — sequential chain per D-03 topology)
- `timeout-minutes: 30`
- `continue-on-error: false` (explicit declaration per D-A9 inheritance documentation)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline predecessor SHAs + read Path-A source v1.6 workflow</name>
  <read_first>
    - .github/workflows/audit-harness-v1.6-integrity.yml (Path-A source; lines 1-207 — full file; cite line ranges from <interfaces> block)
    - .github/workflows/audit-harness-v1.5-integrity.yml (predecessor; byte-unchanged invariant target — read full file to capture baseline hash mentally)
    - .github/workflows/audit-harness-integrity.yml (v1.4 predecessor; byte-unchanged invariant target)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-CONTEXT.md (D-01/D-02/D-03/D-04 LOCKED)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-PATTERNS.md (verbatim YAML excerpts to copy)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VALIDATION.md (per-task verification map tasks 69-NN-01 through 69-NN-10)
  </read_first>
  <files>(read-only; no writes in this task)</files>
  <action>
    Establish the PRE_69_SHA baseline for predecessor-unchanged verification (Threat T-69-PREDECESSOR-DRIFT, HIGH).

    1. Capture current HEAD SHA: `git rev-parse HEAD` — record this as `PRE_69_SHA` for use in Task 3 verification (executor MUST record this in their working notes or as an inline comment for later use).

    2. Capture predecessor file SHAs (for sanity comparison):
       - `git hash-object .github/workflows/audit-harness-integrity.yml`
       - `git hash-object .github/workflows/audit-harness-v1.5-integrity.yml`
       - `git hash-object .github/workflows/audit-harness-v1.6-integrity.yml`
       Record all three.

    3. Confirm `.github/workflows/audit-harness-v1.7-integrity.yml` does NOT yet exist via `test ! -f .github/workflows/audit-harness-v1.7-integrity.yml && echo "absent (expected)" || echo "ALREADY EXISTS — abort"` — if it already exists, ABORT and surface to user (planning assumption violated).

    4. Verify the Path-A source is intact: `git diff HEAD -- .github/workflows/audit-harness-v1.6-integrity.yml` MUST return empty (source not dirty).

    5. Open `.github/workflows/audit-harness-v1.6-integrity.yml` and confirm the line ranges in <interfaces> block match observed line content:
       - Line 5: `name: Audit Harness v1.6 Integrity`
       - Lines 31-48: `parse:` job stanza
       - Lines 50-63: `path-match:` job stanza
       - Lines 65-74: `harness-run:` job stanza
       - Lines 86-90: skip-if-missing canonical form within `check-phase-62:` job
       Record any line-number drift (if v1.6 file has been modified since plan authoring — unlikely but possible) and adjust copy ranges before Task 2.

    No file writes in this task — pure baseline capture for downstream verification.
  </action>
  <verify>
    <automated>git rev-parse HEAD &amp;&amp; test ! -f .github/workflows/audit-harness-v1.7-integrity.yml &amp;&amp; git diff HEAD -- .github/workflows/audit-harness-v1.6-integrity.yml | grep -c . </automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse HEAD` returns a 40-char SHA (recorded as PRE_69_SHA in executor notes)
    - `test ! -f .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (file does NOT yet exist)
    - `git diff HEAD -- .github/workflows/audit-harness-v1.6-integrity.yml` outputs zero lines (Path-A source clean)
    - Three predecessor file `git hash-object` SHAs recorded
    - Line ranges in v1.6 file confirmed: line 5 contains `name: Audit Harness v1.6 Integrity`; line 31 starts the `parse:` block; line 50 starts `path-match:`; line 65 starts `harness-run:`; lines 86-90 contain the canonical `if [ -f ... ]; then ...; else echo "...Phase 42 D-31"; fi` idiom
  </acceptance_criteria>
  <done>PRE_69_SHA recorded; v1.7 target file confirmed absent; v1.6 Path-A source confirmed clean; line ranges verified against <interfaces> block.</done>
</task>

<task type="auto">
  <name>Task 2: Author .github/workflows/audit-harness-v1.7-integrity.yml skeleton (4 jobs per D-03)</name>
  <read_first>
    - .github/workflows/audit-harness-v1.6-integrity.yml (Path-A source; cite line ranges 5, 28, 31-48, 50-63, 65-74, 86-90 from <interfaces> block)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-CONTEXT.md (D-01 §Concrete file boundary table line 64-72; D-03 §4 jobs enumeration line 117-122; "Subtle gotchas captured for plan-phase" lines 81-86)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-RESEARCH.md (§Pattern 1 lines 232-330 — full example YAML; §Code Examples §"Pre-Checkout autocrlf Disable" lines 427-434; §"Wall-Clock Measurement + `::notice`" lines 450-460; §"Skip-if-Missing Stub" lines 436-448; §"Parse Job (Path-A from v1.6)" lines 462-483; §Common Pitfalls §1 lines 371-376 and §3 lines 385-390)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-PATTERNS.md (synthesized linux-chain-ubuntu-latest structure)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VALIDATION.md (tasks 69-NN-01 through 69-NN-09 — exact grep assertions this file must satisfy)
  </read_first>
  <files>.github/workflows/audit-harness-v1.7-integrity.yml</files>
  <action>
    Create the file `.github/workflows/audit-harness-v1.7-integrity.yml` with this exact top-to-bottom structure (use Write tool, NOT heredoc; LF line endings):

    1. **Header comments (3 lines)** — describe the file as "v1.7 Audit Harness Integrity (Phase 69 CILINUX-01 skeleton)" + "Phase 70 HARNESS-04 extends this file (adds crons + pin-helper-advisory + repoints sidecar references v1.6 → v1.7)" + "Path-filter `docs/decision-trees/09-linux-triage.md` entry is Phase 69 SC#5-evidence-only per D-04 sub-decision (a); Phase 70 HARNESS-04(a) removes it"

    2. **`name:` line** — `name: Audit Harness v1.7 Integrity` (NOT v1.6 — rewrite the one-character version digit verbatim)

    3. **`on:` block** — `pull_request.paths:` list with exactly 4 entries per D-01 LOCKED transitional surface + D-04 sub-decision (a):
       - `'scripts/validation/check-phase-*.mjs'`
       - `'.github/workflows/audit-harness-v1.7-integrity.yml'`
       - `'.planning/REQUIREMENTS.md'`
       - `'docs/decision-trees/09-linux-triage.md'`
       Then `workflow_dispatch:` (empty value, no `{}` — matches v1.6 line 28 verbatim)
       **NO `on.schedule:` block** — deliberately omitted per D-01 LOCKED + CONTEXT line 86

    4. **`jobs:` block** with exactly 4 jobs in this order:

       (a) **`parse:` job** — Path-A copy from v1.6 lines 31-48 VERBATIM. Same `name:` ("Parse v1.6 sidecar JSON"), same `runs-on: ubuntu-latest`, same checkout + setup-node + node-version: '20' + JSON.parse block referencing `scripts/validation/v1.6-audit-allowlist.json`. Transitional reference — Phase 70 HARNESS-04 repoints.

       (b) **`path-match:` job** — Path-A copy from v1.6 lines 50-63 VERBATIM. Same `name:`, `runs-on`, `needs: parse`, grep for `scripts/validation/v1.6-audit-allowlist.json` inside `scripts/validation/v1.6-milestone-audit.mjs`. Transitional reference.

       (c) **`harness-run:` job** — Path-A copy from v1.6 lines 65-74 VERBATIM. Same `name:`, `runs-on`, `needs: path-match`, checkout + setup-node, `run: node scripts/validation/v1.6-milestone-audit.mjs --verbose`. Transitional reference.

       (d) **`linux-chain-ubuntu-latest:` job** — SYNTHESIZED per 69-RESEARCH §Pattern 1 example (lines 303-329):
         - `name: Validator chain on Linux LF (Phase 69 CILINUX-01)`
         - `runs-on: ubuntu-latest`
         - `needs: harness-run`
         - `timeout-minutes: 30`
         - `continue-on-error: false` (explicit declaration per D-A9 inheritance documentation — NOT implicit; required for forward-coordination clarity per CONTEXT line 22)
         - `steps:` (5 steps in this exact order — per Pitfall 1 + Pitfall 3):
           STEP 1: `name: Disable autocrlf BEFORE checkout (LF-fidelity contract)` / `run: git config --global core.autocrlf false`
           STEP 2: `uses: actions/checkout@v4`
           STEP 3: `uses: actions/setup-node@v4` with `with: { node-version: '20' }`
           STEP 4: `name: Run chain-apex check-phase-66.mjs (recursively spawns 48..65)` / multiline `run:` block:
             ```
             START=$(date +%s)
             node scripts/validation/check-phase-66.mjs --verbose
             END=$(date +%s)
             echo "::notice title=CHAIN_TIMING_LINUX::Full chain wall-clock: $((END-START))s (Windows reference: ~102s; subprocess timeout: 300s)"
             ```
             (echo emitted AFTER node invocation completes per Pitfall 3 ordering)
           STEP 5: `name: check-phase-67..70 skip-if-missing stubs` / multiline `run:` block:
             ```
             for i in 67 68 69 70; do
               if [ -f scripts/validation/check-phase-$i.mjs ]; then
                 node scripts/validation/check-phase-$i.mjs
               else
                 echo "check-phase-$i.mjs not present -- skipping (graceful degradation per Phase 42 D-31)"
               fi
             done
             ```
             Use the LITERAL echo string `"check-phase-$i.mjs not present -- skipping (graceful degradation per Phase 42 D-31)"` — do NOT paraphrase the Phase 42 D-31 attribution (CONTEXT line 176; 24+ prior occurrences in v1.5/v1.6).

    5. **Deliberate omissions** (negative assertions tasks 69-NN-08 and 69-NN-09 verify):
       - NO `on.schedule:` block anywhere
       - NO `pin-helper-advisory:` job
       - NO `rotting-external-quarterly:` job
       - NO 19-job per-validator parallel topology (NO `check-phase-NN:` job names — only the single `linux-chain-ubuntu-latest` job runs the chain)

    6. **Final file length**: ≥ 70 lines (skeleton with 4 jobs + step bodies + comments). Write with Unix LF line endings (NOT CRLF — even though authored on Windows; the workflow file lives on Linux runners).

    Use the Write tool. Do NOT use heredoc/cat. After write, do not re-read the file in this task — Task 3 verifies grep assertions.
  </action>
  <verify>
    <automated>test -f .github/workflows/audit-harness-v1.7-integrity.yml &amp;&amp; python -c "import yaml; yaml.safe_load(open('.github/workflows/audit-harness-v1.7-integrity.yml'))" &amp;&amp; echo "YAML parse OK" || echo "FALLBACK: GHA platform parse on next push will validate"</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/audit-harness-v1.7-integrity.yml` exists
    - File parses as valid YAML (either `js-yaml` Node load OR `python -m yaml` succeeds; if neither tool installed locally, fall back to committing + observing GHA platform YAML parse on push — GHA itself is the canonical YAML validator)
    - File line count ≥ 70: `[ $(wc -l &lt; .github/workflows/audit-harness-v1.7-integrity.yml) -ge 70 ]`
    - Exactly 4 top-level job declarations: `[ $(grep -c "^  [a-z][a-z-]*:$" .github/workflows/audit-harness-v1.7-integrity.yml) -eq 4 ]`
    - `grep -q "name: Audit Harness v1.7 Integrity" .github/workflows/audit-harness-v1.7-integrity.yml` exits 0
    - `grep -q "node scripts/validation/check-phase-66.mjs --verbose" .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (task 69-NN-04)
    - `grep -q "CHAIN_TIMING_LINUX" .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (task 69-NN-05)
    - `grep -q "timeout-minutes: 30" .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (task 69-NN-06)
    - `grep -q "for i in 67 68 69 70" .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (task 69-NN-07)
    - `grep -q "check-phase-\$i.mjs not present -- skipping (graceful degradation per Phase 42 D-31)" .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (canonical attribution verbatim)
    - `grep -q "git config --global core.autocrlf false" .github/workflows/audit-harness-v1.7-integrity.yml` exits 0
    - `grep -q "continue-on-error: false" .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (explicit D-A9 declaration)
    - `grep -q "docs/decision-trees/09-linux-triage.md" .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (D-04 sub-decision (a) path-filter entry)
    - Step ordering: in `linux-chain-ubuntu-latest` job, the `git config --global core.autocrlf false` line MUST appear BEFORE the `actions/checkout@v4` line. Verify via: `awk '/linux-chain-ubuntu-latest:/,/^  [a-z]/' .github/workflows/audit-harness-v1.7-integrity.yml | grep -n -E 'autocrlf|actions/checkout@v4'` shows autocrlf line number < checkout line number (task 69-NN-03)
    - Negative assertions:
        - `! grep -E "^[[:space:]]*schedule:" .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (NO crons; task 69-NN-08)
        - `! grep -q "pin-helper-advisory" .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (NO pin-helper-advisory; task 69-NN-09)
        - `! grep -E '\$\{\{\s*github\.event\.' .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (NO workflow injection surface; T-69-WORKFLOW-INJECTION; task 69-NN-01)
        - `! grep -E '@(main|latest)' .github/workflows/audit-harness-v1.7-integrity.yml` exits 0 (NO floating-version actions; T-69-ACTION-TYPOSQUAT)
  </acceptance_criteria>
  <done>File written; YAML parses; all positive grep assertions satisfied; all negative grep assertions satisfied; step ordering (autocrlf before checkout) confirmed.</done>
</task>

<task type="auto">
  <name>Task 3: Verify predecessor byte-unchanged + commit single new file</name>
  <read_first>
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-VALIDATION.md (task 69-NN-10 git-diff predecessor invariant check)
    - .planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-CONTEXT.md (threat T-69-PREDECESSOR-DRIFT HIGH severity from <security_threat_model>)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-CONTEXT.md (atomic-commit precedent — Phase 69 single non-atomic commit since NEW file with no coordinated cross-file edits)
  </read_first>
  <files>(commit only; no further file writes)</files>
  <action>
    Pre-commit verification (Threat T-69-PREDECESSOR-DRIFT, HIGH severity — MUST PASS before commit):

    1. Run `git diff --name-only` to enumerate working-tree changes. Expected: exactly ONE entry: `.github/workflows/audit-harness-v1.7-integrity.yml` (the new file). If ANY other file appears, ABORT and surface to user. Particularly check that NO predecessor workflow files have been touched by accidental IDE auto-save or pre-commit hook normalization (Pitfall 6).

    2. Explicit predecessor invariant check:
       ```
       git diff HEAD -- .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml
       ```
       This MUST return empty output. If any byte changed in any of the 3 predecessor workflow files, ABORT.

    3. Cross-check predecessor hash-object SHAs against Task 1 baseline — all three MUST be identical.

    4. Stage ONLY the new file: `git add .github/workflows/audit-harness-v1.7-integrity.yml` (NOT `git add -A` or `git add .` — explicit per file to avoid contaminating commit with untracked debris from working tree).

    5. Confirm staged set: `git diff --cached --name-only` MUST output exactly one line — the new workflow file path.

    6. Commit with this exact message (HEREDOC):
       ```
       feat(phase-69): add audit-harness-v1.7-integrity.yml skeleton (CILINUX-01 Plan 69-01)

       Phase 69 Pillar C (CI-Linux Hardening) Plan 69-01 deliverable per D-01 Option B
       LOCKED: new file, 4-job skeleton, transitional path-filter, NO crons, NO
       pin-helper-advisory. Phase 70 HARNESS-04 extends this file.

       - 4 jobs per D-03 hybrid topology: parse + path-match + harness-run +
         linux-chain-ubuntu-latest (chain-apex; recursively spawns 48..65 via
         execFileSync per Phase 68 CHAIN-03)
       - linux-chain-ubuntu-latest job sets `git config --global core.autocrlf false`
         BEFORE actions/checkout@v4 per LF-fidelity contract (Pitfall 1 / actions/
         checkout discussions #976)
       - Wall-clock instrumentation emits `::notice title=CHAIN_TIMING_LINUX::...`
         answering Phase 68 TIMEOUT-01 forward-coordination flag
       - Skip-if-missing stubs for phases 67-70 use canonical Phase 42 D-31 attribution
       - Path-filter includes docs/decision-trees/09-linux-triage.md per D-04
         sub-decision (a) as SC#5-evidence-only surface; Phase 70 HARNESS-04(a)
         removes it
       - Predecessor workflow files (v1.4 + v1.5 + v1.6) BYTE-UNCHANGED per
         REQUIREMENTS.md:26 + ROADMAP SC#4 invariant

       Plan 69-02 (Wave 2) follows: SC#5 evidence capture (workflow_dispatch
       baseline + synthetic CRLF-injection PR) + 69-VERIFICATION.md + traceability
       flips for CILINUX-01.
       ```

    7. Post-commit verification (Threat T-69-PREDECESSOR-DRIFT acceptance):
       ```
       git diff <PRE_69_SHA from Task 1> HEAD -- .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml
       ```
       MUST return empty (task 69-NN-10).

    8. Record the Plan 69-01 commit SHA via `git rev-parse HEAD` — this becomes the `{69_01_SHA}` reference for Plan 69-02 close-gate (used in 69-VERIFICATION.md §D Atomic Commit SHA Record + traceability flips).

    9. Do NOT push in this task — pushing is part of Plan 69-02 Wave 2 (so the workflow file lands on origin/master atomically with the close-gate evidence capture trigger). Local commit only.
  </action>
  <verify>
    <automated>git log -1 --name-only --pretty=format:"%H %s" HEAD | grep -E "^[0-9a-f]{40} feat\(phase-69\)" &amp;&amp; git log -1 --name-only --pretty=format:"" HEAD | grep -E "^\.github/workflows/audit-harness-v1\.7-integrity\.yml$" &amp;&amp; [ $(git diff $(git log --all --oneline | awk '$2~/^feat\(phase-69\)/{print $1; exit}' | xargs -I{} git rev-parse {}^)..HEAD -- .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml | wc -l) -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --name-only HEAD` shows exactly 1 file changed: `.github/workflows/audit-harness-v1.7-integrity.yml`
    - `git log -1 --pretty=format:"%s" HEAD` matches regex `^feat\(phase-69\): add audit-harness-v1\.7-integrity\.yml skeleton`
    - `git diff PRE_69_SHA HEAD -- .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml` returns empty (task 69-NN-10; T-69-PREDECESSOR-DRIFT mitigation)
    - `git hash-object` on each of the 3 predecessor files returns IDENTICAL SHA to Task 1 baseline
    - Plan 69-01 commit SHA recorded for Plan 69-02 use (executor surfaces in summary as `{69_01_SHA}=<sha>`)
    - NOT pushed to origin (local commit only; push happens in Plan 69-02 Wave 2)
  </acceptance_criteria>
  <done>Single new file committed locally; predecessor invariant verified empty-diff; Plan 69-01 commit SHA captured for Plan 69-02 handoff; no push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → committed master | Workflow YAML lands as committed source; once pushed (Plan 69-02), becomes the actual workflow GitHub Actions executes |
| committed workflow file → GHA runner | GitHub-hosted ubuntu-latest runner reads workflow YAML, executes job steps; runner is ephemeral and isolated per run |
| Phase 69 file boundary → Phase 70 file extension | Phase 70 HARNESS-04 extends THIS file (does NOT recreate); the byte-boundary at Phase 69 close defines what Phase 70 inherits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-WORKFLOW-INJECTION | Tampering | `${{ github.event.* }}` interpolation in `run:` blocks | mitigate | Workflow file does NOT interpolate ANY user-controlled `github.event.*` fields into shell `run:` blocks. Acceptance: `! grep -E '\$\{\{\s*github\.event\.' .github/workflows/audit-harness-v1.7-integrity.yml` returns 0 lines (verified in Task 2). The only allowed `${{ ... }}` interpolations are static built-ins like `${{ github.token }}` (implicit in `actions/checkout@v4`; not in `run:` blocks). |
| T-69-ACTION-TYPOSQUAT | Tampering / EoP | Untrusted `@main` or floating-version action references | mitigate | Pin `actions/checkout@v4` and `actions/setup-node@v4` (semver tags, first-party only). Acceptance: `! grep -E '@(main|latest)' .github/workflows/audit-harness-v1.7-integrity.yml` returns 0 lines (verified in Task 2). All action references use `@v4` semver tag on `actions/*` first-party namespace. |
| T-69-PREDECESSOR-DRIFT | Tampering | Predecessor workflow files (v1.4, v1.5, v1.6) modified during Phase 69 commit | mitigate (HIGH SEVERITY — blocking) | Git-level diff check at Task 3 step 2. `git diff PRE_69_SHA HEAD -- .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml` MUST return empty. If non-empty, ABORT and revert. Also: `git add` only the new file path (not `git add -A`). |
| T-69-IDE-AUTO-SAVE | Tampering | IDE re-format / line-ending normalization touches predecessor YAML files | mitigate | Task 3 step 1 pre-commit `git diff --name-only` check confirms only ONE file in working tree. Task 1 step 2 baseline `git hash-object` of each predecessor; Task 3 step 7 confirms hashes unchanged. Defensive guidance to executor: do NOT open predecessor workflow files in any editor that auto-formats on save. |
</threat_model>

<verification>
**Plan 69-01 close checks:**

1. `test -f .github/workflows/audit-harness-v1.7-integrity.yml` exits 0
2. YAML parses (either local `js-yaml`/`python -m yaml` or GHA platform on push)
3. 4 jobs declared; all 12 positive grep assertions satisfied (per <acceptance_criteria> in Task 2)
4. All 4 negative grep assertions satisfied (no `schedule:`, no `pin-helper-advisory`, no `github.event.*` interpolation in run blocks, no `@main`/`@latest` references)
5. Predecessor byte-unchanged invariant: `git diff PRE_69_SHA HEAD -- <3 predecessor paths>` empty
6. Step ordering: `git config --global core.autocrlf false` appears before `actions/checkout@v4` inside `linux-chain-ubuntu-latest` job
7. Single-file commit landed; Plan 69-01 commit SHA captured for Plan 69-02 use
</verification>

<success_criteria>
Plan 69-01 satisfies ROADMAP Phase 69:
- **SC#1** (new ubuntu-latest runner job runs full chain on Linux LF) — `linux-chain-ubuntu-latest` job declared with `runs-on: ubuntu-latest`, autocrlf-false step, chain-apex invocation
- **SC#2** (PR-blocking via `continue-on-error: false`) — explicit declaration on `linux-chain-ubuntu-latest` job
- **SC#4** (predecessor byte-unchanged) — Task 3 step 7 verification empty-diff

SC#3 (chain exits 0 with 0 SKIPPED on ubuntu-latest) and SC#5 (cross-OS reproducibility on representative PR) are Plan 69-02 deliverables (require runtime evidence from GHA execution).
</success_criteria>

<output>
Create `.planning/phases/69-ci-linux-hardening-pillar-c-cross-os-verification/69-01-SUMMARY.md` when Plan 69-01 completes. Include:
- Plan 69-01 commit SHA (record as `{69_01_SHA}` for downstream traceability)
- 3 predecessor file `git hash-object` SHAs (pre vs post — MUST be identical)
- All 12 positive + 4 negative grep assertion results (PASS/FAIL each)
- Final workflow file line count
- Forward handoff to Plan 69-02: PRE_69_SHA recorded for SC#5 evidence baseline + workflow file ready for `workflow_dispatch` baseline (Plan 69-02 Wave 1 will push to origin/master before triggering)
</output>
