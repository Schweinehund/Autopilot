---
phase: 121-structural-retrofit-glossaries-lifecycle-end-user-guides
plan: 05
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - docs/android-lifecycle/00-enrollment-overview.md
  - docs/android-lifecycle/01-android-prerequisites.md
  - docs/android-lifecycle/02-provisioning-methods.md
  - docs/android-lifecycle/03-android-version-matrix.md
  - docs/ios-lifecycle/00-enrollment-overview.md
  - docs/linux-lifecycle/00-enrollment-overview.md
  - docs/linux-lifecycle/01-linux-prerequisites.md
autonomous: true
requirements: [RETRO-07]
must_haves:
  truths:
    - "All 7 Mermaid-free lifecycle files (android-lifecycle 4, ios-lifecycle/00, linux-lifecycle 2) carry the EEE header block (before H1), a net-new ≥30-word scope ## Summary as the first H2, doc_id/status: Approved/owner/doc_type: Guide frontmatter, and normalized Platform label"
    - "Every blockquote in these files is ≤200 chars (word-preserving splits where needed) — C17 #12 green"
    - "None of these 7 files contains a ```mermaid fence (they are the Mermaid-free subset); C17 exits 0 on the full corpus"
  artifacts:
    - path: "docs/android-lifecycle/00-enrollment-overview.md"
      provides: "EEE-retrofitted Android enrollment overview (Guide, Approved)"
      contains: "## Summary"
    - path: "docs/linux-lifecycle/01-linux-prerequisites.md"
      provides: "EEE-retrofitted Linux prerequisites (Guide, Approved)"
      contains: "## Summary"
  key_links:
    - from: "docs/android-lifecycle/*.md frontmatter"
      to: "RE-185..188 / RE-189 / RE-202..203 registry rows"
      via: "buildDocIdMap join-on-Path (fork injection)"
      pattern: "doc_id: RE-"
---

<objective>
EEE-retrofit the 7 Mermaid-free lifecycle files in android-lifecycle/, ios-lifecycle/00, and
linux-lifecycle/ to doc_type Guide, C17-green, Status Approved.

Purpose: RETRO-07 (partial, 7 of the 13 Mermaid-free lifecycle files). Zero-content-loss reformat — the
9 Mermaid-bearing lifecycle files are excluded (Phase 122); these 7 carry no ```mermaid.
Output: 7 C17-green Guide lifecycle files; RE-185..188, RE-189, RE-202..203 injected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the fork on the 7 files and author 7 net-new Summaries (Guide doc_type)</name>
  <files>docs/android-lifecycle/00-enrollment-overview.md, docs/android-lifecycle/01-android-prerequisites.md, docs/android-lifecycle/02-provisioning-methods.md, docs/android-lifecycle/03-android-version-matrix.md, docs/ios-lifecycle/00-enrollment-overview.md, docs/linux-lifecycle/00-enrollment-overview.md, docs/linux-lifecycle/01-linux-prerequisites.md</files>
  <read_first>
    - docs/android-lifecycle/00-enrollment-overview.md (platform: Android; the Platform gate blockquote sits AFTER H1 — the fork inserts ## Summary right after H1 and this blockquote stays in body)
    - docs/ios-lifecycle/00-enrollment-overview.md (platform: iOS → "iOS"; the Mermaid-free ios file — its siblings 01/02 carry mermaid and are deferred)
    - docs/linux-lifecycle/00-enrollment-overview.md, docs/linux-lifecycle/01-linux-prerequisites.md (platform: Linux)
    - scripts/pipeline/retrofit-structural.mjs (Guide route for the 6 lifecycle dirs; [FILL-IN] placeholder; whole-pre-H1-span relocation; VH prepend/create; platform injection only for keyless files — these 7 all have a platform: key)
    - docs/_standards/EEE-SOP-standard.md (Structure invariant: Summary = first H2, ≥30 words; D-07 lifecycle→Guide; D1 platform map)
    - docs/reference/00-index.md (RE-142 — the net-new-Summary layout shape)
  </read_first>
  <action>
    Run node scripts/pipeline/retrofit-structural.mjs on the 7 listed files (dry-run --all first, or
    dry-run the explicit 7 paths, to confirm each doc_id resolves — RE-185/186/187/188 for the four
    android-lifecycle files, RE-189 for ios-lifecycle/00, RE-202/203 for the two linux-lifecycle files —
    and each platform maps in D1_MAP: Android → "Android", iOS → "iOS", Linux → "Linux"; then write). The
    fork injects doc_id/status: Approved/owner: Intune Admin Lead/doc_type: Guide, emits the block line
    "**Doc Type:** Guide" before H1, relocates any pre-H1 span below a new ## Summary, and inserts the
    [FILL-IN] placeholder + v1.16 VH row. Replace each [FILL-IN] with a NET-NEW ≥30-word scope Summary that
    states what stage/topic the doc covers and its audience (draw scope from each file's existing intro/
    Context prose but write NEW sentences — do NOT move an existing paragraph up as the Summary). Leave all
    body content, existing headings, tables, and cross-links (e.g. ../_glossary.md#apv1) untouched.
  </action>
  <verify>
    <automated>for f in docs/android-lifecycle/00-enrollment-overview.md docs/android-lifecycle/01-android-prerequisites.md docs/android-lifecycle/02-provisioning-methods.md docs/android-lifecycle/03-android-version-matrix.md docs/ios-lifecycle/00-enrollment-overview.md docs/linux-lifecycle/00-enrollment-overview.md docs/linux-lifecycle/01-linux-prerequisites.md; do grep -q '\[FILL-IN' "$f" && echo "PLACEHOLDER-REMAINS:$f"; grep -q '^doc_id: RE-' "$f" || echo "NO-DOCID:$f"; done; echo CHECKED</automated>
  </verify>
  <acceptance_criteria>
    - None of the 7 files contains "[FILL-IN"; each has a net-new ## Summary as first H2 with ≥30 words
    - Each of the 7 frontmatters carries doc_id (RE-185..188 / RE-189 / RE-202..203 as mapped), status: Approved, owner: Intune Admin Lead, doc_type: Guide; block line reads "**Doc Type:** Guide"
    - No existing body paragraph was relocated to serve as the Summary; all tables and cross-links unchanged
  </acceptance_criteria>
  <done>All 7 Mermaid-free lifecycle files enrolled at Guide with net-new ≥30-word Summaries.</done>
</task>

<task type="auto">
  <name>Task 2: Split over-200-char blockquotes, confirm no ```mermaid, prove C17 green + reformat-only</name>
  <files>docs/android-lifecycle/00-enrollment-overview.md, docs/android-lifecycle/02-provisioning-methods.md, docs/android-lifecycle/03-android-version-matrix.md, docs/ios-lifecycle/00-enrollment-overview.md, docs/linux-lifecycle/00-enrollment-overview.md, docs/linux-lifecycle/01-linux-prerequisites.md, docs/android-lifecycle/01-android-prerequisites.md</files>
  <read_first>
    - The 7 files after Task 1 — the Platform/Version gate and See-also blockquotes exceeding 200 chars (light: ~1-3 per file)
    - scripts/validation/c17-eee-contract.mjs (assertion #12 ~L386 JOINS consecutive > lines; assertion #1 hard-fails on ```mermaid for enrolled files — confirm these 7 have none)
    - .planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-CONTEXT.md (R2 word-preserving splits; reformat-only discipline; D-01 — only the 13 Mermaid-free files are enrolled here)
  </read_first>
  <action>
    For each of the 7 files, split every blockquote whose joined text exceeds 200 chars into multiple
    ≤200-char blockquotes, preserving every word (Transform A — split at sentence/clause boundaries or " · "
    separators, blank line between resulting blockquotes so C17 #12 measures them independently). Reserve
    de-blockquote (Transform B) only for a code-fenced admonition or an unsplittable Markdown-link span.
    Confirm none of the 7 files contains a ```mermaid fence (they must not — enrolling one would trip C17
    #1). Prove reformat-only: word-set multiset BEFORE vs AFTER (strip leading ">" prefix before comparing)
    shows zero real words added/removed apart from the net-new Summaries + fork block line/VH rows. Run the
    full C17 corpus and confirm 0 violations.
  </action>
  <verify>
    <automated>for f in docs/android-lifecycle/*.md docs/ios-lifecycle/00-enrollment-overview.md docs/linux-lifecycle/*.md; do awk '/^>/{if(length($0)>200) c++} END{if(c>0) print "LONG-BQ:'"$f"'"}' "$f"; grep -q '```mermaid' "$f" && echo "MERMAID:$f"; done; echo CHECKED</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -E "checked, 0 with violations, 0 total violations"</automated>
  </verify>
  <acceptance_criteria>
    - No ">"-prefixed line exceeds 200 chars in any of the 7 files; every blockquote group joined text ≤200 chars (C17 #12 green)
    - None of the 7 files contains a ```mermaid fence
    - node scripts/validation/c17-eee-contract.mjs reports "0 with violations, 0 total violations" and exits 0 (full corpus)
    - Word-set reformat-only proof passes (zero real words added/removed apart from Summaries + fork emissions)
  </acceptance_criteria>
  <done>All 7 Mermaid-free lifecycle files C17-green, blockquotes ≤200 chars, no mermaid, proven reformat-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Reformat-only Markdown edits to 7 lifecycle files plus one pipeline invocation. No runtime, network,
auth, session, user-input, or cryptographic surface. Integrity concerns: silent content loss during
blockquote splitting, and accidentally enrolling a Mermaid-bearing file.

| Boundary | Description |
|----------|-------------|
| author/fork edit → lifecycle corpus | Reformat byte-faithful except net-new Summaries |
| enrollment gate → C17 #1 | Only Mermaid-free files may be enrolled this phase (these 7 carry none) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-05-01 | Tampering (silent content loss) | blockquote splitting | mitigate | Word-set multiset reformat-only proof; C17 full-corpus 0 violations |
| T-121-05-02 | Tampering (of enforcement) | enrolling a Mermaid file → C17 #1 | mitigate | Task 2 confirms no ```mermaid in the 7 files; fork MERMAID_DEFERRED_PATHS excludes the 9 |
| T-121-05-SC | Tampering | package installs | n/a | No installs; fork uses Node built-ins only |

Spoofing / Repudiation / Information-Disclosure / Denial-of-Service / Elevation-of-Privilege: no new
surface — documentation reformat with no runtime execution path.
</threat_model>

<verification>
Full C17 corpus reports 0 with violations, 0 total violations, exit 0; no ">"-prefixed line exceeds 200
chars and no ```mermaid in any of the 7 files; word-set reformat-only proof passes.
</verification>

<success_criteria>
- ROADMAP Phase 121 SC2 (partial): 7 of the 13 Mermaid-free lifecycle docs carry the EEE block +
  Summary-first + normalized Platform label + Status: Approved; C17 exits 0
- ROADMAP Phase 121 SC4 (partial): all 7 carry the "v1.16 EEE reformat — content not re-reviewed" VH row
</success_criteria>

<output>
Create `.planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-05-SUMMARY.md` when done
</output>
