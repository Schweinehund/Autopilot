---
phase: 121-structural-retrofit-glossaries-lifecycle-end-user-guides
plan: 02
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - docs/_glossary-android.md
  - docs/_glossary-network.md
autonomous: true
requirements: [RETRO-04]
must_haves:
  truths:
    - "docs/_glossary-android.md and docs/_glossary-network.md each carry the EEE header block (before H1), a net-new ≥30-word scope ## Summary as the first H2, and doc_id/status: Approved/owner/doc_type: Reference frontmatter"
    - "The original coverage blockquote is relocated byte-preserved below ## Summary and split so every blockquote (relocated coverage + all in-body See-also cross-refs) is ≤200 chars — C17 #12 green"
    - "docs/_glossary-network.md's ## Change History section is reconciled to ## Version History (R1) so the fork prepends the v1.16 reformat row into it rather than spawning a duplicate section"
    - "Every existing term anchor slug (e.g. #8021x, #eap, #radius) is unchanged and the ## Alphabetical Index links still resolve; C17 exits 0 on the full corpus"
  artifacts:
    - path: "docs/_glossary-android.md"
      provides: "EEE-retrofitted Android Enterprise glossary (Reference, Approved)"
      contains: "## Summary"
    - path: "docs/_glossary-network.md"
      provides: "EEE-retrofitted network-auth glossary with reconciled Version History (Reference, Approved)"
      contains: "## Version History"
  key_links:
    - from: "docs/_glossary-android.md ## Alphabetical Index links"
      to: "term ### anchors (#... slugs)"
      via: "plain-GitHub slug (no {#id} override)"
      pattern: "## Alphabetical Index"
---

<objective>
EEE-retrofit the two glossaries docs/_glossary-android.md (Mermaid-free, ~33 over-200 blockquotes — the
heaviest #12 file in the class) and docs/_glossary-network.md (light, but carries the R1 Change-History
hazard), to doc_type Reference, C17-green, term-surface preserved.

Purpose: RETRO-04 (glossaries) with the D-03 strict D3-A layout (net-new scope Summary + relocated
coverage blockquote), the R1 network Change-History reconciliation, and the R2 word-preserving
blockquote splits.
Output: 2 C17-green Reference glossaries; RE-179 (android), RE-183 (network) injected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile network Change History → Version History, run the fork, and author both net-new Summaries</name>
  <files>docs/_glossary-android.md, docs/_glossary-network.md</files>
  <read_first>
    - docs/_glossary-android.md (frontmatter platform: all; the coverage blockquote before H1; the ## Alphabetical Index; the many "> See also: ..." in-body blockquotes)
    - docs/_glossary-network.md (frontmatter platform: all; the ## Change History heading at ~L110 — the R1 hazard; coverage blockquote before H1)
    - docs/reference/00-index.md (RE-142 — the shipped Phase-118 net-new-Summary-above-relocated-coverage-blockquote layout to copy)
    - scripts/pipeline/retrofit-structural.mjs (the fork built in 121-01 — Reference route for _glossary*.md, [FILL-IN] Summary placeholder, whole-pre-H1-span relocation, VH prepend/create)
    - docs/_standards/EEE-SOP-standard.md (Structure invariant: ## Summary = first H2, no blockquote/H2/H3 between H1 and Summary; Summary ≥30 words; D-07 glossary→Reference)
  </read_first>
  <action>
    First, in docs/_glossary-network.md ONLY, rename the "## Change History" heading to "## Version History"
    (R1) so the fork's insertVersionHistoryRow PREPENDS the v1.16 row into the existing table instead of
    CREATE-ing a duplicate section; do not otherwise touch that section's rows. Then run
    node scripts/pipeline/retrofit-structural.mjs docs/_glossary-android.md docs/_glossary-network.md
    (dry-run first to confirm doc_id resolves to RE-179 / RE-183 and platform: all maps to "All Platforms",
    then write). The fork injects doc_id/status: Approved/owner: Intune Admin Lead/doc_type: Reference,
    emits the block line before H1, relocates the whole pre-H1 span (the coverage blockquote) to below a
    new ## Summary, and inserts the "[FILL-IN: >=30 words ...]" placeholder + the v1.16 VH row. Then replace
    each file's [FILL-IN] placeholder with a NET-NEW ≥30-word scope Summary in prose (author's discretion on
    sentences per D-03): for android, a scope statement covering Android Enterprise provisioning terminology
    (enrollment modes, work profile, COPE/COBO, Managed Google Play, etc.) and who the glossary serves; for
    network, a scope statement covering platform-neutral 802.1X / EAP / RADIUS / certificate-delivery
    terminology. The Summary is NET-NEW scope prose — do NOT repurpose the relocated coverage blockquote's
    see-also links as the Summary (D-03). Leave all term bodies, term ### headings, and the ## Alphabetical
    Index untouched (anchor-slug safety: every insertion is above the first term heading).
  </action>
  <verify>
    <automated>for f in docs/_glossary-android.md docs/_glossary-network.md; do grep -q '\[FILL-IN' "$f" && echo "PLACEHOLDER-REMAINS:$f"; done; grep -c '^## Version History' docs/_glossary-network.md</automated>
    <automated>grep -cE '^doc_id: RE-(179|183)' docs/_glossary-android.md docs/_glossary-network.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/_glossary-network.md contains exactly one "## Version History" heading and zero "## Change History" headings
    - Neither file contains a "[FILL-IN" placeholder; each has a net-new ## Summary as its first H2 with ≥30 words of scope prose (not the see-also coverage links)
    - Each file's frontmatter carries doc_id (RE-179 android / RE-183 network), status: Approved, owner: Intune Admin Lead, doc_type: Reference; the block line reads "**Doc Type:** Reference"
    - The relocated coverage blockquote sits below ## Summary; all term ### headings and the ## Alphabetical Index are byte-unchanged
  </acceptance_criteria>
  <done>Both glossaries enrolled at Reference with net-new ≥30-word Summaries; network's Version History reconciled; term surface untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Word-preserving-split every over-200-char blockquote and prove C17 green + reformat-only</name>
  <files>docs/_glossary-android.md, docs/_glossary-network.md</files>
  <read_first>
    - docs/_glossary-android.md, docs/_glossary-network.md (after Task 1 — the relocated coverage blockquote plus every "> See also: [a] · [b] · ..." blockquote that exceeds 200 chars)
    - scripts/validation/c17-eee-contract.mjs (assertion #12 at ~L386 — it JOINS consecutive > lines with a space and flags total >200; so a single long See-also line must be broken into SEPARATE blockquotes split by a blank line, not merely wrapped)
    - .planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-CONTEXT.md (R2 word-preserving splits; reformat-only discipline — relocated spans byte-identical, net-new Summary the only additive element)
  </read_first>
  <action>
    Split every blockquote whose joined text exceeds 200 chars into multiple ≤200-char blockquotes,
    preserving every word (Transform A). The See-also cross-reference blockquotes split naturally at the
    " · " separators; the relocated coverage blockquote splits at sentence/clause boundaries. Insert a blank
    line between each resulting blockquote so C17 #12 measures them independently (consecutive > lines are
    joined and re-measured as one). A scripted greedy splitter over the " · " / sentence boundaries is
    acceptable (the Phase-117-05 precedent) provided a word-set reformat-only proof passes. Do NOT
    de-blockquote (Transform B) unless a single unsplittable Markdown link would otherwise break mid-span.
    After splitting, prove reformat-only: for each file, take the multiset of words BEFORE and AFTER
    (stripping the leading ">" blockquote-prefix token before comparing — the Phase-118 lesson) and confirm
    zero real words added or removed (the only intended additions are the net-new Summary from Task 1 and
    the block line/VH row from the fork). Then run the full C17 corpus and confirm 0 violations.
  </action>
  <verify>
    <automated>awk '/^>/{if(length($0)>200) c++} END{exit (c>0)}' docs/_glossary-android.md && awk '/^>/{if(length($0)>200) c++} END{exit (c>0)}' docs/_glossary-network.md && echo NO_LONG_BQ</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -E "checked, 0 with violations, 0 total violations"</automated>
  </verify>
  <acceptance_criteria>
    - No line beginning with ">" exceeds 200 chars in either file, and every blockquote group's joined text is ≤200 chars (C17 #12 green)
    - node scripts/validation/c17-eee-contract.mjs reports "0 with violations, 0 total violations" and exits 0 (full corpus, both files now enrolled)
    - Word-set reformat-only proof passes: zero real words added/removed vs. the pre-retrofit files apart from the net-new Summary + fork-emitted block line/VH row
    - Every term ### anchor slug is unchanged (no {#id} overrides introduced); ## Alphabetical Index links still resolve
  </acceptance_criteria>
  <done>Both glossaries are C17-green with all blockquotes ≤200 chars, proven reformat-only, anchor slugs intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Reformat-only Markdown edits to two glossary files plus one pipeline invocation. No runtime, network,
auth, session, user-input, or cryptographic surface. The only integrity concern is silent content loss
during blockquote splitting or coverage-blockquote relocation.

| Boundary | Description |
|----------|-------------|
| author/fork edit → glossary corpus | Reformat must be byte-faithful except the net-new Summary; blockquote splits must preserve every word |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-02-01 | Tampering (silent content loss) | blockquote splitting / coverage relocation | mitigate | Word-set multiset reformat-only proof (strip ">" prefix before compare); C17 full-corpus 0 violations |
| T-121-02-02 | Tampering | R1 Change-History rename spawning a duplicate section | mitigate | Verify exactly one ## Version History and zero ## Change History in network glossary before final C17 run |
| T-121-02-SC | Tampering | package installs | n/a | No installs; fork uses Node built-ins only |

Spoofing / Repudiation / Information-Disclosure / Denial-of-Service / Elevation-of-Privilege: no new
surface — documentation reformat with no runtime execution path.
</threat_model>

<verification>
Full C17 corpus reports 0 with violations, 0 total violations, exit 0; no ">"-prefixed line exceeds 200
chars in either file; network glossary has exactly one ## Version History (zero ## Change History); word-set
reformat-only proof passes; all term anchor slugs unchanged.
</verification>

<success_criteria>
- ROADMAP Phase 121 SC1 (partial): both files carry the EEE block + Summary-first + normalized Platform
  label; term-surface + plain-GitHub anchor slugs preserved; C17 exits 0
- ROADMAP Phase 121 SC4 (partial): both carry the "v1.16 EEE reformat — content not re-reviewed" VH row
</success_criteria>

<output>
Create `.planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-02-SUMMARY.md` when done
</output>
