---
phase: 121-structural-retrofit-glossaries-lifecycle-end-user-guides
plan: 04
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - docs/_glossary-linux.md
  - docs/_glossary.md
autonomous: true
requirements: [RETRO-04]
must_haves:
  truths:
    - "docs/_glossary-linux.md and docs/_glossary.md each carry the EEE header block (before H1), a net-new ≥30-word scope ## Summary as the first H2, and doc_id/status: Approved/owner/doc_type: Reference frontmatter"
    - "The coverage blockquote is relocated byte-preserved below ## Summary and every blockquote (relocated coverage + all in-body See-also cross-refs) is ≤200 chars — C17 #12 green"
    - "Every existing term anchor slug (e.g. #apv1, #esp, #oobe) is unchanged and the ## Alphabetical Index links resolve; C17 exits 0 on the full corpus"
  artifacts:
    - path: "docs/_glossary-linux.md"
      provides: "EEE-retrofitted Linux provisioning glossary (Reference, Approved)"
      contains: "## Summary"
    - path: "docs/_glossary.md"
      provides: "EEE-retrofitted Windows Autopilot glossary (Reference, Approved)"
      contains: "## Summary"
  key_links:
    - from: "docs/_glossary.md ## Alphabetical Index links"
      to: "term ### anchors (#apv1, #esp, ...)"
      via: "plain-GitHub slug (no {#id} override)"
      pattern: "## Alphabetical Index"
---

<objective>
EEE-retrofit docs/_glossary-linux.md (~20 over-200 blockquotes) and docs/_glossary.md (the base Windows
Autopilot glossary, ~11 over-200 blockquotes) to doc_type Reference, C17-green, term-surface preserved.

Purpose: RETRO-04 (glossaries) with the D-03 strict D3-A layout and R2 word-preserving blockquote splits.
This plan completes the 6-glossary class (with 121-02 and 121-03).
Output: 2 C17-green Reference glossaries; RE-181 (linux), RE-184 (base) injected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the fork and author both net-new Summaries (Reference doc_type)</name>
  <files>docs/_glossary-linux.md, docs/_glossary.md</files>
  <read_first>
    - docs/_glossary-linux.md (frontmatter platform: Linux → "Linux"; coverage blockquote before H1; ## Version History at ~L182)
    - docs/_glossary.md (frontmatter platform: all → "All Platforms"; the base Autopilot glossary — term slugs #apv1/#esp/#oobe/#ztd/#hardware-hash referenced by lifecycle files; coverage blockquote; ## Version History at ~L243)
    - docs/reference/00-index.md (RE-142 — the net-new-Summary-above-relocated-coverage layout to copy)
    - scripts/pipeline/retrofit-structural.mjs (Reference route; [FILL-IN] placeholder; whole-pre-H1-span relocation; VH prepend/create)
    - docs/_standards/EEE-SOP-standard.md (Structure invariant: Summary = first H2, ≥30 words; D-07 glossary→Reference)
  </read_first>
  <action>
    Run node scripts/pipeline/retrofit-structural.mjs docs/_glossary-linux.md docs/_glossary.md (dry-run
    first to confirm doc_id resolves to RE-181 / RE-184 and platform maps: Linux → "Linux", all →
    "All Platforms", then write). The fork injects doc_id/status: Approved/owner: Intune Admin
    Lead/doc_type: Reference, emits the block line before H1, relocates the coverage blockquote below a new
    ## Summary, and inserts the [FILL-IN] placeholder + v1.16 VH row. Replace each [FILL-IN] with a NET-NEW
    ≥30-word scope Summary (author's discretion per D-03): for linux, a scope statement covering Linux
    (Ubuntu/RHEL) Intune enrollment and provisioning terminology (Intune Portal, compliance, Microsoft
    Authentication Library concepts); for the base glossary, a scope statement covering core Windows
    Autopilot terminology (hardware hash, OOBE, ESP, ZTD, APv1/APv2, profile assignment). Summary is
    NET-NEW scope prose, NOT the coverage see-also links (D-03). Leave every term ### heading and the
    ## Alphabetical Index byte-unchanged — the base glossary's #apv1/#esp/#oobe slugs are cross-linked from
    lifecycle files, so they must not shift (no {#id} overrides).
  </action>
  <verify>
    <automated>for f in docs/_glossary-linux.md docs/_glossary.md; do grep -q '\[FILL-IN' "$f" && echo "PLACEHOLDER-REMAINS:$f"; done; grep -cE '^doc_id: RE-(181|184)' docs/_glossary-linux.md docs/_glossary.md</automated>
    <automated>grep -c '^## Alphabetical Index' docs/_glossary.md</automated>
  </verify>
  <acceptance_criteria>
    - Neither file contains "[FILL-IN"; each has a net-new ## Summary as first H2 with ≥30 words of scope prose
    - Frontmatter carries doc_id (RE-181 linux / RE-184 base), status: Approved, owner: Intune Admin Lead, doc_type: Reference; block line reads "**Doc Type:** Reference"
    - All term ### headings and the ## Alphabetical Index in both files are byte-unchanged; base-glossary slugs (#apv1/#esp/#oobe/#ztd/#hardware-hash) unchanged (no {#id} overrides)
  </acceptance_criteria>
  <done>Both glossaries enrolled at Reference with net-new ≥30-word Summaries; cross-linked base slugs preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Word-preserving-split every over-200-char blockquote and prove C17 green + reformat-only</name>
  <files>docs/_glossary-linux.md, docs/_glossary.md</files>
  <read_first>
    - docs/_glossary-linux.md, docs/_glossary.md (after Task 1 — the relocated coverage blockquote plus every "> See also: ..." blockquote exceeding 200 chars)
    - scripts/validation/c17-eee-contract.mjs (assertion #12 ~L386 — JOINS consecutive > lines; split into SEPARATE blockquotes divided by a blank line)
    - .planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-CONTEXT.md (R2 word-preserving splits; reformat-only discipline)
  </read_first>
  <action>
    Split every blockquote whose joined text exceeds 200 chars into multiple ≤200-char blockquotes,
    preserving every word (Transform A), inserting a blank line between resulting blockquotes so C17 #12
    measures them independently. See-also blockquotes split at " · " separators; the coverage blockquote at
    sentence/clause boundaries. A scripted greedy splitter is acceptable (Phase-117-05 precedent) with a
    word-set reformat-only proof. Reserve de-blockquote (Transform B) only for an unsplittable single
    Markdown-link span. Prove reformat-only per file: multiset of words BEFORE vs AFTER (strip the leading
    ">" prefix token before comparing) shows zero real words added/removed apart from the net-new Summary +
    fork block line/VH row. Run the full C17 corpus and confirm 0 violations.
  </action>
  <verify>
    <automated>awk '/^>/{if(length($0)>200) c++} END{exit (c>0)}' docs/_glossary-linux.md && awk '/^>/{if(length($0)>200) c++} END{exit (c>0)}' docs/_glossary.md && echo NO_LONG_BQ</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -E "checked, 0 with violations, 0 total violations"</automated>
  </verify>
  <acceptance_criteria>
    - No ">"-prefixed line exceeds 200 chars in either file; every blockquote group joined text ≤200 chars (C17 #12 green)
    - node scripts/validation/c17-eee-contract.mjs reports "0 with violations, 0 total violations" and exits 0 (full corpus — all 6 glossaries now enrolled)
    - Word-set reformat-only proof passes (zero real words added/removed apart from Summary + fork emissions)
    - All term anchor slugs unchanged; ## Alphabetical Index links resolve
  </acceptance_criteria>
  <done>Both glossaries C17-green, all blockquotes ≤200 chars, proven reformat-only, slugs intact; RETRO-04 class complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Reformat-only Markdown edits to two glossary files plus one pipeline invocation. No runtime, network,
auth, session, user-input, or cryptographic surface. The integrity concern is silent content loss during
splitting/relocation, and shifting the base-glossary slugs that lifecycle files cross-link.

| Boundary | Description |
|----------|-------------|
| author/fork edit → glossary corpus | Reformat byte-faithful except net-new Summary; cross-linked base slugs must not change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-04-01 | Tampering (silent content loss) | blockquote splitting / coverage relocation | mitigate | Word-set multiset reformat-only proof; C17 full-corpus 0 violations |
| T-121-04-02 | Tampering | base-glossary anchor slugs cross-linked from lifecycle | mitigate | Verify all term ### headings + ## Alphabetical Index byte-unchanged; no {#id} overrides |
| T-121-04-SC | Tampering | package installs | n/a | No installs; fork uses Node built-ins only |

Spoofing / Repudiation / Information-Disclosure / Denial-of-Service / Elevation-of-Privilege: no new
surface — documentation reformat with no runtime execution path.
</threat_model>

<verification>
Full C17 corpus reports 0 with violations, 0 total violations, exit 0; no ">"-prefixed line exceeds 200
chars; word-set reformat-only proof passes; all six glossaries now enrolled and green.
</verification>

<success_criteria>
- ROADMAP Phase 121 SC1 met (with 121-02/03): all docs/_glossary-*.md carry the EEE block + Summary-first
  + normalized Platform label; term-surface + plain-GitHub anchor slugs preserved; C17 exits 0
- ROADMAP Phase 121 SC4 (partial): both carry the "v1.16 EEE reformat — content not re-reviewed" VH row
</success_criteria>

<output>
Create `.planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-04-SUMMARY.md` when done
</output>
