---
phase: 125-v115-pin-14th-path-a-lineage-bump-terminal-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md
autonomous: true
requirements: [HARN-05, HARN-06]
must_haves:
  truths:
    - "The Wave-0 pre-anchor SHA (the predecessor-byte-unchanged HARD-gate base + the BASELINE_20 back-anchor target) is captured and recorded before any harness file is authored"
    - "V115 is positively confirmed to be 29a3599 — its commit message carries BOTH 'MILESTONE-AUDIT' AND 'MILESTONE CLOSE' (not a SUMMARY follow-up, the f3959c8 trap class)"
    - "The flag-#6 FULL predecessor-chain SCOPING run is executed (delegated to a sub-agent) and records which predecessor validators — if any — drift on the retrofitted v1.16 corpus, sizing the Wave-5 emergent slot"
    - "The five C2/C7/C9 sidecar-pinned files are confirmed byte-unchanged since 29a3599 (expect a verbatim sidecar copy — NOT a v1.15-style repoint)"
  artifacts:
    - path: ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md"
      provides: "Recorded Wave-0 anchor SHA, confirmed V115=29a3599, flag-#6 chain-scoping drift worklist, sidecar byte-unchanged confirmation"
      contains: "Wave-0 anchor SHA"
  key_links:
    - from: "125-01 Wave-0 anchor SHA"
      to: "125-04 predecessor-byte-unchanged HARD gate (git diff <anchor> HEAD)"
      via: "recorded anchor SHA consumed at close-audit time"
      pattern: "git diff"
    - from: "125-01 flag-#6 chain-scoping drift worklist"
      to: "125-05 emergent remediation slot sizing"
      via: "recorded drift-candidate set"
      pattern: "check-phase"
---

<objective>
Wave-0 pre-anchor + flag-#6 plan-time chain-scoping reconnaissance. This plan establishes the SHA anchors the whole close depends on, positively confirms the V115 pin SHA, and runs the FULL predecessor chain at PLAN time to SCOPE which validators (if any) drift on the retrofitted v1.16 corpus — all BEFORE any harness file is copied.

Purpose: Per D-125-3 (D1 skeleton) the close needs a Wave-0 pre-anchor commit BEFORE Atom 1 — it is (a) the byte-unchanged HARD-gate base asserted at close (HARN-06) and (b) the target the BASELINE_20 freshness comment back-anchors to. Per D-125-1's MANDATORY flag-#6 rider (REQUIREMENTS L56), the FULL predecessor chain is run at PLAN time to SCOPE drift (not pre-commit conversion) so the Wave-5 emergent slot is correctly sized. This plan also positively recovers V115=29a3599 (a single point of failure for the entire lineage bump) and confirms the five sidecar-pinned files are byte-unchanged (expected verbatim copy, unlike v1.15's 11/4/0 RED).

Output: 125-01-SUMMARY.md recording the Wave-0 anchor SHA, the confirmed V115 SHA (29a3599), the flag-#6 chain-scoping drift worklist, and the sidecar byte-unchanged confirmation. NO harness files are authored in this plan (v1.15 119-01 / v1.14 112-01 precedent: Wave-0 is reconnaissance-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Wave-0 anchor SHA + positively confirm V115 = 29a3599 + sidecar byte-unchanged recon</name>
  <files>.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §1 (the exact V115 recovery method + the positive-confirmation requirement — message must contain BOTH tokens; the f3959c8 trap class) + §2b (the five C2/C7/C9 sidecar-pinned files + the "0 commits since 29a3599" verification)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §D-125-3 (Wave-0 pre-anchor BEFORE Atom 1; V115 = PAST v1.15 close SHA 29a3599, never the close-gate SHA) + §D-125-1 DEPENDENCY (V115 pin must land in Atom 2 before any readAtV115Close conversion)
    - scripts/validation/_lib/frozen-at-close.mjs lines 50-98 (the existing V114:'7d922a7' single-entry pin pattern at ~line 56 + the readAtV114Close export at ~line 98 — the V115 entry mirrors this in Atom 2 / Plan 125-03)
    - STATE.md "Accumulated Context" [119-01] note ("the SAME positive-confirmation method must be used to recover V115 at Phase 125"; naive --grep returned the wrong f3959c8 for V114)
  </read_first>
  <action>
    Capture the current HEAD via `git rev-parse HEAD` and record the full 40-char SHA in 125-01-SUMMARY.md labelled "Wave-0 anchor SHA (predecessor-byte-unchanged HARD-gate base for Plan 125-04)". Note explicitly that this is DISTINCT from the BASELINE_20 freshness anchor, which Plan 125-02 captures JIT immediately before the Atom-1 commit (an intervening Jira-sync auto-commit may land between; Pitfall 2 — v1.15 saw c6ea8d2 vs a323332). Record BOTH facts so 125-02/125-04 do not conflate them.

    Positively confirm the V115 pin SHA. Run `git log --all --grep="MILESTONE-AUDIT" --grep="MILESTONE CLOSE" --all-match -1 --format=%H` and assert it returns 29a3599 (full: 29a3599236fa9724404d1ee95227ab5679ac9d83). Then run `git log -1 --format=%s 29a3599` and assert the message contains BOTH "MILESTONE-AUDIT" AND "MILESTONE CLOSE" (it is the 119-07 close-gate). Record in the SUMMARY: "V115 = 29a3599 CONFIRMED substantive close-gate; do NOT pin a SUMMARY follow-up (the V114 f3959c8 trap class)." This disambiguation is load-bearing for Plan 125-03's frozen-at-close V115 entry.

    Sidecar byte-unchanged reconnaissance: for EACH of the five C2/C7/C9-pinned files — docs/reference/android-capability-matrix.md, docs/admin-setup-android/03-fully-managed-cobo.md, docs/l2-runbooks/20-android-app-install-investigation.md, docs/admin-setup-android/07-knox-mobile-enrollment.md, docs/admin-setup-android/02-zero-touch-portal.md — run `git log 29a3599..HEAD -- <file>` and confirm ZERO commits (the v1.16 retrofit touched glossaries/decision-trees/lifecycle/nav-hubs, NOT these five). Record the per-file result. This is the evidence Plan 125-02 consumes to copy the sidecar VERBATIM (no line-pin repoint) — the OPPOSITE of v1.15's 119-01 finding. Do NOT edit any harness or doc file in this plan.
  </action>
  <verify>
    <automated>bash -c 'git rev-parse HEAD && git log -1 --format=%s 29a3599 | grep -q "MILESTONE-AUDIT" && git log -1 --format=%s 29a3599 | grep -q "MILESTONE CLOSE" && echo V115_OK'</automated>
  </verify>
  <acceptance_criteria>
    - 125-01-SUMMARY.md contains a line "Wave-0 anchor SHA" followed by a 40-char hex SHA equal to the current HEAD
    - 125-01-SUMMARY.md states "V115 = 29a3599" AND records that the message contains both "MILESTONE-AUDIT" and "MILESTONE CLOSE" (positive-confirmation) AND warns against pinning a SUMMARY follow-up (f3959c8 trap class)
    - `git log -1 --format=%s 29a3599` output contains both "MILESTONE-AUDIT" and "MILESTONE CLOSE"
    - For all five sidecar-pinned files, `git log 29a3599..HEAD -- <file>` returns 0 commits, recorded per-file in the SUMMARY as the "sidecar copies verbatim" evidence for Plan 125-02
    - No file under scripts/validation/ or .github/ is modified (git status shows only the SUMMARY)
  </acceptance_criteria>
  <done>Wave-0 anchor SHA captured; V115=29a3599 positively confirmed via both-token assertion; the five sidecar-pinned files confirmed byte-unchanged since v1.15 close (verbatim-copy evidence for 125-02).</done>
</task>

<task type="auto">
  <name>Task 2: flag-#6 FULL predecessor-chain SCOPING run (sub-agent) + harness-on-HEAD baseline — record drift worklist</name>
  <files>.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §4 (flag-#6 SCOPING — the predecessor drift SET is a SCOPE not a pre-commit conversion list; §4a reader-drift candidate table with drift-likelihood; §4b the DOMINANT un-pre-convertible C15/ABAUDIT shape on _glossary-apple-business.md/RE-180) + §5b (the 9-firing-workflow cascade; 48..66 covered by apex recursion) + Open Questions 2/3
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §D-125-1 MANDATORY RIDER flag-#6 (run FULL chain at PLAN time to SCOPE; convert/exempt ONLY what trips; a broad proactive sweep is the out-of-scope FROZEN-AWARE-ADOPTION-SWEEP-01) + the three remediation shapes
    - STATE.md memory note reference_chain_baseline_run_kill (full chain ~5-9 min; background runs die at turn boundaries — delegate to a sub-agent, sequential-on-main, no worktrees)
    - scripts/validation/v1.15-milestone-audit.mjs usage header (the --verbose / --self-test invocation shape) — the copy-source harness to run against HEAD
  </read_first>
  <action>
    Run the CURRENT v1.15 harness against HEAD as the baseline: `node scripts/validation/v1.15-milestone-audit.mjs --verbose` and `node scripts/validation/v1.15-milestone-audit.mjs --self-test` on the post-Phase-124 working tree. Record the full PASS/FAIL/SKIP tally. Expectation (per §2b/§4): GREEN or near-green, unlike v1.15's 11/4/0 RED — the retrofit did not touch the sidecar-pinned files. If any check FAILs, record {check id, file, reason} as the first drift candidate.

    flag-#6 SCOPING run: delegate to a fresh sub-agent (the full recursive chain is ~5-9 min and dies at turn boundaries if backgrounded — sequential-on-main, no worktrees) the task of running the predecessor per-phase validators that read the retrofitted structural paths (the §4a candidate set: check-phase-30/49/51/52/57/59/62/65/67/101/108/109/110 and any others reading docs/_glossary*.md, docs/decision-trees/*, the 22 lifecycle files, and the 4 nav-hubs). Have the sub-agent run each `node scripts/validation/check-phase-NN.mjs` individually against HEAD and report PASS/FAIL per validator. This is a SCOPE run: record which validators actually trip on the retrofitted corpus, prioritizing the §4a high-drift candidates ({path,line} pins 67/101; exact-literal fragment scans 109/110). Do NOT convert or edit any validator here — conversion is Wave-5 (125-05) triggered-only work; a broad proactive conversion is the out-of-scope FROZEN-AWARE-ADOPTION-SWEEP-01.

    Record in 125-01-SUMMARY.md a "flag-#6 chain-scoping drift worklist" section: the harness-on-HEAD tally, the per-validator PASS/FAIL from the sub-agent scope run, and an explicit sizing verdict for the Wave-5 slot — either "no predecessor validator drifts on HEAD (slot likely a no-op; the dominant C15/ABAUDIT shape is un-pre-convertible and only surfaces on the assembled-corpus GHA scan — 125-04)" or "the following predecessor validators drift: <list> — candidate readAtV115Close conversions for 125-05". Note that the authoritative RED signal is still the GHA apex/cascade (125-04); this scope run sizes the slot, it does not pre-authorize edits.
  </action>
  <verify>
    <automated>bash -c 'node scripts/validation/v1.15-milestone-audit.mjs --self-test >/dev/null 2>&1; echo "v1.15 self-test exit: $?"; grep -q "flag-#6 chain-scoping drift worklist" ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md" && echo SCOPING_RECORDED'</automated>
  </verify>
  <acceptance_criteria>
    - 125-01-SUMMARY.md contains a "flag-#6 chain-scoping drift worklist" section with the harness-on-HEAD PASS/FAIL/SKIP counts (both --verbose and --self-test)
    - The section records the per-validator scope-run result for the §4a candidate set (delegated to a sub-agent), with any drifting validator named as a 125-05 conversion candidate
    - The section states an explicit Wave-5 slot sizing verdict (no-op-likely vs named-drift-list), and notes the dominant C15/ABAUDIT shape is un-pre-convertible (GHA-surfaced only)
    - No validator or harness file is edited by this plan (git status shows only the SUMMARY)
  </acceptance_criteria>
  <done>The v1.15 harness-on-HEAD baseline + the flag-#6 full-chain scope run are recorded; the drift-candidate set is enumerated (or confirmed empty) as the sizing basis for the Wave-5 emergent slot — with zero validator edits (triggered-only remediation deferred to 125-05).</done>
</task>

</tasks>

<threat_model>
This phase installs no external packages; all tooling is in-repo `.mjs` + git + node built-ins (RESEARCH: Package Legitimacy Audit N/A). The only trust surfaces this plan touches are read-only reconnaissance (git history + fs reads) plus one delegated sub-agent chain run — no CI config, no runtime, no doc mutation. Surfaces are stated honestly as minimal.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → V115 pin value | An ambiguous grep-recovery can resolve the wrong SHA; a wrong pin silently reads the wrong frozen corpus for every readAtV115Close consumer |
| retrofitted live corpus → predecessor validator assertions | A drifting live-reader assertion turns its containing apex RED; must be SCOPED (not swept) so the Wave-5 fix is triggered-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01-PIN | Tampering/Info-disclosure | V115 SHA selection | mitigate | Positive-confirmation: assert 29a3599's message contains BOTH "MILESTONE-AUDIT" AND "MILESTONE CLOSE"; reject SUMMARY follow-ups (f3959c8 trap class); recorded for 125-03 |
| T-125-01-SWEEP | Tampering | flag-#6 scope run | mitigate | Scope-only run — enumerate drift, edit NOTHING; a proactive conversion sweep is the out-of-scope FROZEN-AWARE-ADOPTION-SWEEP-01 (deferred v1.17+) |
| T-125-01-SC | Tampering | package installs | accept | No package-manager installs this phase (node built-ins + git/gh only) |
</threat_model>

<verification>
- `git rev-parse HEAD` recorded as Wave-0 anchor
- `git log -1 --format=%s 29a3599` contains "MILESTONE-AUDIT" and "MILESTONE CLOSE"
- Five sidecar-pinned files each show 0 commits since 29a3599
- v1.15 harness-on-HEAD --verbose/--self-test tally recorded; flag-#6 per-validator scope-run drift worklist recorded with Wave-5 sizing verdict
</verification>

<success_criteria>
Wave-0 anchor SHA captured; V115=29a3599 positively confirmed; sidecar byte-unchanged (verbatim-copy evidence); the flag-#6 full-chain scope run records the drift-candidate set (or confirms empty) to size the Wave-5 emergent slot; zero harness/validator files modified.
</success_criteria>

<output>
Create `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md` when done, recording the Wave-0 anchor SHA, the confirmed V115=29a3599, the sidecar byte-unchanged results, and the flag-#6 drift worklist + Wave-5 sizing verdict.
</output>
</content>
</invoke>
