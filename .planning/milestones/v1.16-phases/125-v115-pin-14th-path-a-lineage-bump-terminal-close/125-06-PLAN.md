---
phase: 125-v115-pin-14th-path-a-lineage-bump-terminal-close
plan: 06
type: execute
wave: 5
depends_on: ["125-04"]
files_modified:
  - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/PIPE-02-CLOSE-RUNBOOK.md
  - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/PIPE-02-CLOSE-FINDINGS.md
autonomous: false
requirements: [HARN-07]
must_haves:
  truths:
    - "The agent PREPARES a statistically representative .docx set of REAL shipped RE-NNN Status:Approved docs spanning all 5 platforms + the four RETARGETED v1.16-delta probe classes (decision-tree text-equiv / glossary anchor-slug / nav-hub link-table / descriptive-filename label) + an executable PIPE-02-CLOSE-RUNBOOK.md"
    - "The probes are RETARGETED to v1.16 deltas (NOT the v1.15 RETRO-03 wide-matrix / RETRO-02 Linux-admin riders — those test surfaces v1.16 never touched)"
    - "The OWNER executes the runbook live in Copilot Studio and records PIPE-02-CLOSE-FINDINGS.md, capturing the raw transcript in-repo"
    - "PASS = grounded answer + clickable document-level citation + no hallucination across all probes; the close-gate BLOCKS until the owner attests PASS"
  artifacts:
    - path: ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/PIPE-02-CLOSE-RUNBOOK.md"
      provides: "Owner-run grounding-confirmation procedure retargeted to the v1.16 structural corpus (decision-tree / glossary anchor / nav-hub / filename-label probes)"
      contains: "PASS"
    - path: ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/PIPE-02-CLOSE-FINDINGS.md"
      provides: "Owner-recorded results + raw transcript + PASS attestation"
      contains: "PASS"
  key_links:
    - from: "docs/_registry/RE-index.md Approved rows"
      to: "PIPE-02-CLOSE-RUNBOOK.md representative .docx set"
      via: "manual selection of real RE-NNN Approved structural docs per platform/probe-class"
      pattern: "RE-2"
---

<objective>
The PIPE-02 grounding-confirmation pass on the retrofitted structural corpus (HARN-07, D-125-2/A1). The agent PREPARES the representative real-corpus .docx set + an executable close-runbook with probes RETARGETED to the v1.16 deltas; the OWNER executes live in Copilot Studio (the agent has NO live Copilot Studio / SharePoint access — REQUIREMENTS L76) and records findings. The close-gate BLOCKS until the owner attests PASS.

Purpose: SC3 requires a PIPE-02 grounding confirmation on the retrofitted structural corpus. Per D-125-2's MANDATORY RETARGET rider, the probes target what v1.16 CHANGED — NOT the v1.15 riders (the RETRO-03 wide capability-matrix and RETRO-02 Linux-admin surfaces, which v1.16 never touched; re-running them is testing last milestone's homework). This plan is autonomous:false — it contains the blocking owner-execution checkpoint (the ONE close-gate leg no re-audit axis can reproduce, since the agent has no Copilot access).

Output: PIPE-02-CLOSE-RUNBOOK.md (agent-authored) + PIPE-02-CLOSE-FINDINGS.md (owner-recorded, in-repo transcript + PASS attestation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md
@docs/_registry/RE-index.md
@.planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md
@.planning/milestones/v1.15-phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/PIPE-02-CLOSE-RUNBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author PIPE-02-CLOSE-RUNBOOK.md + select the representative real-corpus .docx set (retargeted probes)</name>
  <files>.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/PIPE-02-CLOSE-RUNBOOK.md</files>
  <read_first>
    - .planning/milestones/v1.15-phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/PIPE-02-CLOSE-RUNBOOK.md (in full — the fork base: header block, Section 1 Prerequisites/agent-confirmed-preconditions table, Section 2 Upload Procedure, Section 3+ query list, Definition-of-PASS section)
    - .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md (the original convert→reindex-wait→Q1..Qn→record shape)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §5c (the four RETARGETED probes + the candidate RE-NNN table: (a) decision-tree Mermaid→text-equiv every leaf citable — RE-217 10-8021x-triage/RE-215 08-android-triage/RE-207 00-initial-triage; (b) glossary definition-list anchor-slug — RE-180 apple-business/RE-182 macos/RE-181 linux, watch the double-hyphen slug trap; (c) nav-hub link-table — RE-219 index/RE-218 common-issues/RE-220-221 quick-ref; (d) descriptive-filename citation-label — from filename-map.md) + the 5-platform coverage row set + "Do NOT re-run RETRO-03 wide-matrix (RE-143) or RETRO-02 Linux-admin (RE-129)"
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §D-125-2 (full owner-run, blocking, riders RETARGETED to v1.16 deltas; real shipped RE-NNN Status:Approved docs, not synthetic RE-T*) + memory reference_glossary_anchor_slugs (plain-GitHub slugs, double-hyphen trap)
    - docs/_registry/RE-index.md (select Approved structural rows only — the RE-179..221 range; confirm current path + status for each candidate)
  </read_first>
  <action>
    Fork the v1.15 PIPE-02-CLOSE-RUNBOOK.md → PIPE-02-CLOSE-RUNBOOK.md in the Phase-125 directory. Update the header block: Procedure-for → Phase 125 close-gate grounding pass on the retrofitted structural corpus; Authored-by → current date; Decision-basis → D-125-2 (A1) + REQUIREMENTS L76; Records-into → PIPE-02-CLOSE-FINDINGS.md.

    Select a representative set of REAL shipped RE-NNN Status:Approved structural docs from RE-index.md covering all 5 platforms (Windows _glossary.md RE-184 / lifecycle; macOS _glossary-macos.md RE-182 / macos-lifecycle; iOS ios-lifecycle; Android _glossary-android.md RE-179 / android-lifecycle; Linux _glossary-linux.md RE-181 / linux-lifecycle) AND the four RETARGETED probe classes per §5c: (a) a decision-tree text-equiv table (RE-217 10-8021x-triage.md — the STD-04 exemplar), (b) a glossary definition-list with anchor-slug citations (RE-180 _glossary-apple-business.md — mind the double-hyphen slug trap), (c) a nav-hub link-table (RE-219 index.md), (d) a descriptive-filename-labelled doc from Phase-124's filename-map.md. Do NOT include the v1.15 RETRO-03 wide-matrix (RE-143) or RETRO-02 Linux-admin (RE-129) — those surfaces are v1.16-untouched. For each selected doc, convert to .docx via the locked pipeline (scripts/pipeline/convert.ps1) and run guard-docx.mjs; record convert-OK + guard-exit-0 in the runbook's agent-confirmed-preconditions table (mechanical agent legs).

    Author the query-list section (N queries) covering the four RETARGETED PASS legs: (1) a decision-tree query where EVERY decision leaf must be citable from the text-equiv table (STD-04 / RETRO-05/08); (2) a glossary term query that must ground to a definition-list anchor-slug citation (RETRO-04, plain-GitHub slug); (3) a nav-hub routing query that must retrieve the correct link-table entry (RETRO-06); (4) a query whose citation label must reflect the descriptive filename, not RE-NNN (PIPE-04 / OQ1) — plus the 5-platform grounding spread. Include an explicit "Definition of PASS" section: grounded + clickable document-level citation + no hallucination across every probe, across N queries spanning all 5 platforms.
  </action>
  <verify>
    <automated>bash -c 'test -f ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/PIPE-02-CLOSE-RUNBOOK.md" && grep -q "PASS" ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/PIPE-02-CLOSE-RUNBOOK.md" && ! grep -q "RE-143" ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/PIPE-02-CLOSE-RUNBOOK.md" && echo RUNBOOK_OK'</automated>
  </verify>
  <acceptance_criteria>
    - PIPE-02-CLOSE-RUNBOOK.md exists, forked from the v1.15 close-runbook, records-into PIPE-02-CLOSE-FINDINGS.md
    - The representative set is real shipped RE-NNN Status:Approved structural docs spanning 5 platforms + the four retargeted probe-class docs (decision-tree text-equiv, glossary anchor-slug, nav-hub link-table, descriptive-filename label); it EXCLUDES the v1.15 RETRO-03 wide-matrix (RE-143) and RETRO-02 Linux-admin (RE-129) riders
    - Each selected doc has a recorded convert-OK + guard-exit-0 precondition
    - A query-list covers all four RETARGETED PASS legs + 5-platform grounding, with an explicit "Definition of PASS"
  </acceptance_criteria>
  <done>The close-runbook is authored with a real-corpus representative set retargeted to the four v1.16-delta probe classes across 5 platforms, all guard-passed, with a retargeted query list and a PASS definition; the v1.15 riders are excluded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: OWNER CHECKPOINT — execute the runbook live in Copilot Studio, record findings + PASS attestation</name>
  <what-built>The agent prepared PIPE-02-CLOSE-RUNBOOK.md + the representative real-corpus .docx set (converted + guard-passed) + an empty PIPE-02-CLOSE-FINDINGS.md template with the recording-table + raw-transcript sections. The live Copilot Studio / SharePoint legs are owner-run (agent has no access; REQUIREMENTS L76).</what-built>
  <read_first>
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/PIPE-02-CLOSE-RUNBOOK.md (the procedure the owner executes)
    - .planning/milestones/v1.15-phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/PIPE-02-CLOSE-FINDINGS.md (the recording-table + raw-transcript shape to fork into the empty CLOSE-FINDINGS template; the stale-index re-upload gotcha 124-03 recorded)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §D-125-2 RIDER + §specifics ("capture the owner transcript in-repo for auditability — the ONE close-gate leg no re-audit axis can reproduce")
  </read_first>
  <how-to-verify>
    1. Upload the representative .docx set to the test SharePoint indexed library per Section 2 of PIPE-02-CLOSE-RUNBOOK.md; wait for reindex. (Watch the same-filename stale-index gotcha 124-03 recorded — re-upload the verified build if a prior-indexed copy is served.)
    2. Run every query in the runbook's query list against the live Copilot Studio agent (5-platform grounding + the four retargeted probes: decision-tree leaf citability, glossary anchor-slug citation, nav-hub link-table retrieval, descriptive-filename label quality).
    3. For each query, confirm: a grounded answer, a clickable document-level citation, and no hallucination. The decision-tree probe must confirm EVERY decision leaf is citable from the text-equiv table; the glossary probe must ground to a definition-list anchor-slug; the filename probe must show the descriptive label (not RE-NNN).
    4. Record every query + response + citation-present + hallucination-free + PASS/FAIL into PIPE-02-CLOSE-FINDINGS.md, and PASTE THE RAW TRANSCRIPT in-repo (do not leave it owner-local — this is the one close-gate leg no re-audit axis can reproduce).
    5. Attest the overall verdict: PASS only if every probe is grounded + clickable-cited + hallucination-free. If any probe FAILs, record it as FAIL — the close-gate must NOT proceed on a FAIL.
  </how-to-verify>
  <action>Agent action: fork the v1.15 PIPE-02-CLOSE-FINDINGS.md recording-table + raw-transcript shape into an EMPTY PIPE-02-CLOSE-FINDINGS.md template (per-query table columns + a raw-transcript section) for the owner to fill. Then PAUSE for the owner to execute the live Copilot Studio legs and record + attest PASS. The agent does not (and cannot) run the live legs.</action>
  <acceptance_criteria>
    - PIPE-02-CLOSE-FINDINGS.md exists with a per-query recording table AND a pasted raw transcript section (in-repo)
    - Every retargeted probe leg (decision-tree leaf-citability, glossary anchor-slug, nav-hub link-table, descriptive-filename label) + the 5-platform grounding spread has a recorded verdict
    - An explicit overall "PIPE-02 CLOSE: PASS" (or FAIL) owner attestation line is present
    - On PASS, this artifact is the evidence the close-gate (125-07) consumes to flip HARN-07 / PIPE-02 to Validated
  </acceptance_criteria>
  <resume-signal>Owner types "PIPE-02 PASS" (with PIPE-02-CLOSE-FINDINGS.md committed) or describes the failing probe(s). The close-gate BLOCKS until PASS is attested.</resume-signal>
</task>

</tasks>

<threat_model>
This phase installs no external packages; the pandoc pipeline is already pinned (convert.ps1 3.7.0.2). The live-service surface is owner-run Copilot Studio / SharePoint (agent has no access — an asserted, not agent-verified, gate leg). No new installs, no runtime edits. Surfaces stated honestly.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → live Copilot Studio | The agent has NO access; the grounding result is an ASSERTED (owner-attested), not agent-verified, gate leg — the one leg no re-audit axis reproduces |
| representative set fidelity → HARN-07 | Selecting the wrong (v1.15-rider) surfaces or synthetic RE-T* fixtures would invalidate the retrofitted-corpus grounding proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-06-ASSERTED | Repudiation | owner-attested PIPE-02 PASS | mitigate | Capture the raw transcript in-repo (PIPE-02-CLOSE-FINDINGS.md) so the asserted leg has an auditable evidence artifact |
| T-125-06-RETARGET | Tampering | probe selection | mitigate | Retarget to the four v1.16-delta probe classes; explicitly exclude the v1.15 RETRO-03 wide-matrix (RE-143) / RETRO-02 Linux-admin (RE-129) riders and RE-T* synthetic fixtures; convert+guard each |
| T-125-06-STALEINDEX | Tampering | SharePoint reindex | mitigate | Runbook flags the 124-03 same-filename stale-index gotcha; owner re-uploads verified build if a prior copy is served |
| T-125-06-SC | Tampering | package installs | accept | No package-manager installs this phase (pandoc pipeline already pinned) |
</threat_model>

<verification>
- PIPE-02-CLOSE-RUNBOOK.md authored: real-corpus set retargeted to the four v1.16-delta probe classes + 5-platform spread, all guard-passed, retargeted query list, PASS definition; v1.15 riders excluded
- PIPE-02-CLOSE-FINDINGS.md owner-recorded with per-query verdicts + in-repo raw transcript + explicit PASS/FAIL attestation
</verification>

<success_criteria>
The agent-prepared close-runbook + representative real-corpus set (retargeted to decision-tree text-equiv / glossary anchor-slug / nav-hub link-table / descriptive-filename label across 5 platforms) is executed live by the owner; PIPE-02-CLOSE-FINDINGS.md records grounded + clickable-cited + hallucination-free results across all retargeted probe legs with an in-repo transcript; the owner attests PIPE-02 CLOSE: PASS. The close-gate blocks until PASS.
</success_criteria>

<output>
Create `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-06-SUMMARY.md` when done, recording the representative-set composition, the retargeted probe list, and the owner PASS attestation reference.
</output>
</content>
