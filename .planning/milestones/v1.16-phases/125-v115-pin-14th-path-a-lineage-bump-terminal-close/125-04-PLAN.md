---
phase: 125-v115-pin-14th-path-a-lineage-bump-terminal-close
plan: 04
type: execute
wave: 4
depends_on: ["125-03"]
files_modified:
  - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md
autonomous: true
requirements: [HARN-06, HARN-07]
must_haves:
  truths:
    - "The authoritative Axis-2 Linux GHA run (both chain validators on ubuntu-latest via check-phase-125 recursion 48..124 + linux-chain-ubuntu-latest) is dispatched, awaited, and its run ID + apex tally recorded"
    - "The 9-firing-workflow predecessor cascade (v1.7-v1.15 integrity + the new v1.16) is scanned for RED against retrofitted HEAD; 48..66 is understood as covered by apex recursion (v1.5/v1.6 workflows do NOT fire)"
    - "Axis 1 (fresh git clone --no-hardlinks, Windows) and Axis 3 (fresh zero-context sub-agent) reproduce the leaf-validator + --self-test results; cross-OS PASS/FAIL/SKIP EXACT MATCH is asserted for the reproducible surfaces"
    - "The predecessor-byte-unchanged HARD gate holds: git diff <Wave-0 anchor> HEAD over the v1.4-v1.15 frozen harness/sidecar/workflow surfaces + check-phase-48..119 is EMPTY"
    - "If any firing apex is RED, the plan HALTS and signals the remediation slot (125-05); if GREEN across the cascade it clears the close-gate precondition"
  artifacts:
    - path: ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md"
      provides: "3-axis re-audit record + 9-workflow cascade scan + cross-OS EXACT MATCH table + byte-unchanged gate proof"
      contains: "Cross-OS PASS-Count EXACT MATCH"
  key_links:
    - from: "Axis-2 GHA run + cascade scan conclusion"
      to: "125-05 remediation slot (fires only if any apex RED) / 125-07 close-gate (proceeds only if all GREEN)"
      via: "recorded run IDs + apex PASS/FAIL/SKIP tallies"
      pattern: "check-phase-125"
---

<objective>
The 3-axis terminal re-audit (HARN-07) + the 9-workflow predecessor-cascade RED scan + the predecessor-byte-unchanged HARD gate (HARN-06). This plan proves the retrofitted-corpus chain is atomically green at the new close SHAs across the whole firing cascade, and that non-Phase-125 predecessor surfaces remain byte-unchanged.

Purpose: SC3 requires a 3-axis re-audit with cross-OS EXACT MATCH: Axis 1 fresh clone (Windows), Axis 2 cross-OS Linux GHA authoritative (BOTH chain validators — the Windows apex times out at [48..124], WINDOWS-CLONE-DEEPNEST-TIMEOUT-01, one deeper than v1.15), Axis 3 fresh zero-context sub-agent. Per D-125-4's WIDENED scope, "authoritative green" spans the 9 firing predecessor workflows (v1.7-v1.15) + the new v1.16 — the close PR is the FIRST time those predecessor apexes see the retrofitted corpus, so the dominant C15/ABAUDIT drift (or any live-reader drift) surfaces here. The apex may come back RED on first push (v1.14's first Axis-2 was 44/22/1; v1.15's emergent RED was ad583fd) — this plan explicitly branches to the pre-authorized remediation slot (125-05) rather than improvising.

Output: 125-04-AUDIT-RESULTS.md with the run IDs, the 9-workflow cascade scan, the cross-OS EXACT MATCH table, and the byte-unchanged gate proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch + await the authoritative Axis-2 Linux GHA run; scan the 9-workflow cascade; record apex tallies</name>
  <files>.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-03-SUMMARY.md (the push branch/PR + the workflow run IDs already triggered by the Atom-2 push — v1.16 + firing predecessors)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md §"flag-#6 chain-scoping drift worklist" + §"Wave-5 slot sizing verdict" (the EMPIRICALLY-confirmed likely cascade-RED set — read this to know exactly which validators to expect RED and prioritize in the run-log scan)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §5a (the 3 axes + EXACT-MATCH gate; Linux GHA authoritative; Windows deep-nests at [48..124]) + §5b (the cascade CORRECTION: 9 firing workflows v1.7-v1.15 + v1.16, NOT 11; v1.5/v1.6 filter doc-globs so they do NOT fire on the close PR; 48..66 covered by apex recursion within the 9) + §4b (the RESEARCH-HYPOTHESIZED DOMINANT RED: C15/ABAUDIT on _glossary-apple-business.md/RE-180 — note 125-01 found this did NOT trip on the old harness; test, do not assume)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §D-125-4 (GHA green is authoritative; scan the entire firing cascade for RED before the close-gate; a shared frozen-aware conversion greens every apex at once) + §Common Pitfall 6 (do NOT wait for v1.5/v1.6 — deadlock)
  </read_first>
  <action>
    Confirm the Atom-2 push (from 125-03) triggered `audit-harness-v1.16-integrity.yml` AND the 9 firing predecessor integrity workflows (v1.7-v1.15). If the v1.16 workflow did not fire (paths: filter mismatch or PR not opened), dispatch via `gh workflow run audit-harness-v1.16-integrity.yml --ref <branch>` and/or open the PR so the pull_request paths: trigger matches. Await completion with `gh run watch` / `gh run list`. Record for the v1.16 workflow: the run ID, headSha, and per-job conclusions — specifically the apex `check-phase-125` result (the recursive [48..124] chain) from BOTH the standalone check-phase-125 job AND the linux-chain-ubuntu-latest job, plus each per-phase leaf job (120..124).

    Then scan the 9-workflow cascade: for EACH firing predecessor workflow (v1.7's check-phase-70 apex, v1.8, v1.9, v1.10, v1.11, v1.12, v1.13, v1.14's check-phase-112, v1.15's check-phase-119) record its GHA conclusion against retrofitted HEAD. Understand that 48..66 (v1.5/v1.6-era validators, e.g. check-phase-62 on _glossary-apple-business.md) is exercised via APEX RECURSION within these 9 — a drifting 48..66 validator turns its containing apex RED even though the v1.5/v1.6 workflows themselves do not fire. Do NOT wait for a v1.5/v1.6 workflow run (Pitfall 6 — they filter doc-globs not in the close-PR diff; waiting is a deadlock).

    EMPIRICAL expected-drift set (from 125-01's flag-#6 recon — use as the PRIMARY scan target, alongside — NOT instead of — the RESEARCH §4b C15/ABAUDIT hypothesis): 125-01 confirmed the LIKELY cascade-RED set is the 4 decision-tree validators `check-phase-30` (docs/decision-trees/07-ios-triage.md diamond-count), `check-phase-51` (docs/decision-trees/09-linux-triage.md Mermaid-block + click directives), `check-phase-92` (docs/decision-trees/06-macos-triage.md cross-link needle), `check-phase-99` (docs/decision-trees/06-macos-triage.md click-directive needles) — all driven by Phase-122's Mermaid→text-equivalent conversion (STD-04/RETRO-05) that legitimately removed the diamond/click/graph syntax they assert — PLUS `check-phase-62` and `check-phase-65`, which FAIL in default (non-NESTED) mode because their CHAIN-61/CHAIN-64 regression guards recurse into the v1.6 ancestor harness that re-runs C2 supervision against the same shifted `docs/_glossary-android.md`/`docs/android-lifecycle/*` (the D-125-4 cascade). A RED on ANY of {check-phase-30, 51, 92, 99, 62, 65} is the scoped drift → capture it precisely and hand the worklist to 125-05. Note: `check-phase-67`/`101`/`109`/`110` (RESEARCH §4a MED-HIGH candidates) all PASS on the current corpus — do not expect them RED. The RESEARCH §4b C15/ABAUDIT trip on `_glossary-apple-business.md` did NOT materialize on the OLD v1.15-scoped harness and is UN-CONFIRMED against the new v1.16 apex — TEST it in this run, do NOT assume it (nor assume it clear). Separately note that the v1.16-milestone-audit's OWN C2/C7/C9 checks are already cleared by the 125-02 sidecar repoint (they are NOT part of this cascade drift) — these 6 CHAIN validators drift independently and are the 125-05 targets.

    This GHA cascade's green is the AUTHORITATIVE Axis-2 result. Do NOT run the full apex chain locally on Windows (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 at [48..124]). Record every apex PASS/FAIL/SKIP tally verbatim. If ANY firing apex has FAIL > 0, set the AUDIT-RESULTS.md verdict to "CASCADE RED — remediation slot 125-05 REQUIRED" and capture the failing validator/assertion identities from the run logs (which check-phase-NN FAILed, in which workflow, and why — cross-check against the empirical {30,51,92,99,62,65} set first, and also confirm/deny a C15/ABAUDIT trip on _glossary-apple-business.md) so 125-05 has a precise worklist. If every firing apex is GREEN (FAIL 0), set "CASCADE GREEN — 125-05 is a no-op skip; close-gate precondition (Axis 2 + cascade) cleared".
  </action>
  <verify>
    <automated>bash -c 'test -f ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md" && grep -Eq "CASCADE (RED|GREEN)" ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md" && echo AXIS2_RECORDED'</automated>
  </verify>
  <acceptance_criteria>
    - 125-04-AUDIT-RESULTS.md records the v1.16 Axis-2 workflow run ID, headSha, and the apex check-phase-125 PASS/FAIL/SKIP tally (both standalone + linux-chain jobs)
    - It records the per-workflow conclusion for all 9 firing predecessor workflows (v1.7-v1.15) + notes 48..66 is covered by apex recursion (v1.5/v1.6 do NOT fire)
    - The run-log scan explicitly reports the status of the empirical expected-drift set {check-phase-30, 51, 92, 99, 62, 65} AND confirms/denies the un-confirmed C15/ABAUDIT trip on _glossary-apple-business.md
    - The verdict line is either "CASCADE RED — remediation slot 125-05 REQUIRED" (with the failing-validator/workflow worklist, empirical set cross-checked + C15/ABAUDIT confirm-or-deny) or "CASCADE GREEN — ... precondition cleared"
    - No full apex chain was run locally on Windows (only GHA is authoritative)
  </acceptance_criteria>
  <done>The authoritative Axis-2 Linux GHA run + the 9-workflow cascade scan are dispatched/awaited; run IDs + apex tallies recorded; the empirical {30,51,92,99,62,65} set + the un-confirmed C15/ABAUDIT hypothesis are both checked; the RED-vs-GREEN branch is set with a precise remediation worklist if RED.</done>
</task>

<task type="auto">
  <name>Task 2: Axis 1 (fresh clone) + Axis 3 (fresh sub-agent) reproducible surfaces; cross-OS EXACT MATCH table</name>
  <files>.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §D-125-4/B1 (Axis 1 fresh clone; Axis 3 fresh zero-context sub-agent; cross-OS EXACT MATCH) + §Claude's Discretion (optional local WSL/Docker corroborating pass — not authoritative)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §5a (the EXACT-MATCH gate; Windows N/A-cascades for the deep-nest apex) + §Environment Availability (WSL2/Docker local = corroborating only)
    - .planning/milestones/v1.15-MILESTONE-AUDIT.md (the prior close's Cross-OS PASS-Count EXACT MATCH table — the exact reusable column structure + which rows are "Linux sole-authoritative — Windows N/A cascades" for the chain validators)
    - STATE.md memory reference_chain_baseline_run_kill (delegate the byte-equiv/sub-agent run — background runs die at turn boundaries; sequential-on-main, no worktrees)
  </read_first>
  <action>
    Axis 1: fresh `git clone --no-hardlinks <repo> "$TEMP/v1.16-audit-<rand>"`, checkout the pushed branch, and run the REPRODUCIBLE-on-Windows surfaces: `node scripts/validation/v1.16-milestone-audit.mjs --verbose` + `--self-test`, and each leaf `node scripts/validation/check-phase-120.mjs`..`124.mjs`. Record PASS/FAIL/SKIP. Do NOT run the full apex (check-phase-125 full chain) on Windows — mark those rows "Windows N/A — cascades; Linux sole-authoritative" exactly as the prior close's table does.

    Axis 3: delegate the same reproducible-surface run to a fresh zero-context sub-agent (per the sequential-on-main + background-runs-die constraint). Record its independent tally.

    Author the "Cross-OS PASS-Count EXACT MATCH" table in 125-04-AUDIT-RESULTS.md using the prior-close column structure: | # | Validator | Type | Windows (Axis 1/3 fresh clone) | Linux (Axis 2 GHA) | Verdict |. One row per new leaf (120-124), one row for the harness (v1.16-milestone-audit --verbose + --self-test), one apex row (check-phase-125, Linux sole-authoritative), and one continuity-chain row (the prior apex check-phase-119, Linux sole-authoritative). Every reproducible row must read EXACT MATCH across axes; assert this explicitly. Any mismatch is a hard stop (do not proceed to close-gate).
  </action>
  <verify>
    <automated>bash -c 'grep -q "Cross-OS PASS-Count EXACT MATCH" ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md" && grep -q "EXACT MATCH" ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md" && echo XOS_TABLE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - 125-04-AUDIT-RESULTS.md contains a "Cross-OS PASS-Count EXACT MATCH" table with the prior-close column structure
    - Every reproducible surface (harness + 5 leaves 120-124) has an EXACT MATCH verdict across Axis 1, Axis 2, Axis 3
    - Apex check-phase-125 + the continuity-chain row (check-phase-119) are marked "Linux sole-authoritative — Windows N/A cascades"
    - Axis 3 was a genuinely fresh zero-context sub-agent (distinct from Axis 1)
  </acceptance_criteria>
  <done>Axis 1 + Axis 3 reproduce the harness + leaf results; the cross-OS EXACT MATCH table is authored with every reproducible surface matching and the chain rows correctly marked Linux-authoritative.</done>
</task>

<task type="auto">
  <name>Task 3: Predecessor-byte-unchanged HARD gate + set the close-gate precondition verdict</name>
  <files>.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md (the Wave-0 anchor SHA — the byte-unchanged gate base)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §domain "Key delta from Phase 119" (Phase 125 is the OPPOSITE of 119's re-baseline — predecessor surfaces stay byte-unchanged; drift absorbed by frozen-aware conversion, NOT re-pinning) + §HARN-06 (predecessor v1.4-v1.15 frozen surfaces byte-unchanged)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §HARN-06 (byte-unchanged invariant) + the frozen-at-close inventory (V14x..V115 harness lineage surfaces)
    - scripts/validation/ + .github/workflows/ directory listings (to enumerate the exact non-Phase-125 frozen surface set: every v1.N-milestone-audit.mjs, v1.N-audit-allowlist.json, audit-harness-v1.N-integrity.yml for N in 1.4..1.15, plus check-phase-48..119)
  </read_first>
  <action>
    Assert the predecessor-byte-unchanged HARD gate: run `git diff <Wave-0 anchor SHA from 125-01> HEAD --` scoped to the full set of non-Phase-125 predecessor frozen harness/sidecar/workflow surfaces (the v1.4-v1.15 harness lineage: every `scripts/validation/v1.N-milestone-audit.mjs`, every `v1.N-audit-allowlist.json`, every `.github/workflows/audit-harness-v1.N-integrity.yml` for N in 1.4..1.15, plus check-phase-48..119 which are predecessor validators). Enumerate the exact surface list from the frozen-at-close inventory + the directory listings and record each as byte-unchanged. The diff MUST be EMPTY — unlike Phase 119, Phase 125 does NOT re-pin any predecessor surface (§Key delta: drift is absorbed by frozen-aware conversion in the Wave-5 slot, which edits predecessor check-phase-NN validators ONLY if the cascade is RED, not the frozen harness surfaces). Any diff over a v1.N-milestone-audit.mjs / sidecar / workflow (a frozen harness surface) is a HARD STOP: the Atom-1/Atom-2 copies accidentally touched a frozen surface — do not proceed to close-gate. (Note: check-phase-48..119 will show a diff ONLY if the Wave-5 remediation slot has already fired a frozen-aware conversion on one of them; if 125-05 has not yet run, they must be byte-unchanged.)

    Then set the overall close-gate precondition verdict in 125-04-AUDIT-RESULTS.md: "Axis-2 GHA cascade = <GREEN|RED>; cross-OS EXACT MATCH = <yes|no>; predecessor-byte-unchanged = <EMPTY|VIOLATED>". The close-gate (125-07) may land ONLY when the cascade = GREEN AND EXACT MATCH = yes AND byte-unchanged = EMPTY (and, separately, owner PIPE-02 PASS from 125-06). If the cascade = RED, explicitly hand off to the remediation slot 125-05 and note this plan's verdict will be re-evaluated after 125-05 re-pushes to green.
  </action>
  <verify>
    <automated>bash -c 'grep -Eq "predecessor-byte-unchanged = (EMPTY|VIOLATED)" ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md" && echo BYTE_GATE_RECORDED'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff <Wave-0 anchor> HEAD` over every enumerated non-Phase-125 predecessor FROZEN HARNESS surface (v1.N-milestone-audit.mjs / sidecar / workflow, N in 1.4..1.15) is EMPTY (recorded surface-by-surface)
    - 125-04-AUDIT-RESULTS.md contains the verdict line "Axis-2 GHA cascade = ...; cross-OS EXACT MATCH = ...; predecessor-byte-unchanged = EMPTY|VIOLATED"
    - If the cascade = RED, the doc explicitly hands off to remediation slot 125-05
  </acceptance_criteria>
  <done>The predecessor-byte-unchanged HARD gate is proven EMPTY over all non-Phase-125 frozen harness surfaces; the composite close-gate precondition verdict is recorded, branching to 125-05 if RED.</done>
</task>

</tasks>

<threat_model>
This phase installs no external packages; all tooling is in-repo `.mjs` + git + gh + node built-ins. This plan is read/audit-only (records results, dispatches GHA, runs a fresh clone + sub-agent); it authors no code and edits no harness/frozen surface. The live-service surface is the GitHub Actions run consumption (read-only). Surfaces stated honestly.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Windows result → authoritative verdict | Windows cannot run the deep-nest apex [48..124]; trusting a local Windows apex run instead of GHA would mis-report the chain as green |
| firing cascade → "authoritative green" | Treating only the v1.16 workflow (not the 9 firing predecessors) as the gate would miss a drifting predecessor apex RED |
| Wave-0 anchor → byte-unchanged proof | A wrong/omitted anchor lets an accidental frozen-surface edit pass undetected (the milestone's dominant integrity risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-04-AUTHSURF | Spoofing/Repudiation | authoritative Axis-2 result | mitigate | Only the GHA run IDs are authoritative; Windows apex explicitly N/A-cascades; record run IDs + headSha across the firing cascade |
| T-125-04-CASCADE | Repudiation | 9-workflow cascade scan | mitigate | Scan all 9 firing apexes (v1.7-v1.15 + v1.16); do NOT wait for non-firing v1.5/v1.6 (Pitfall 6 deadlock); 48..66 covered by recursion; cross-check the empirical {30,51,92,99,62,65} set |
| T-125-04-BYTEGATE | Tampering | predecessor frozen surfaces | mitigate | git diff <Wave-0 anchor> HEAD over the enumerated non-Phase-125 frozen harness set MUST be EMPTY; any diff is a hard stop |
| T-125-04-REDSHORT | Tampering | RED-apex handling | mitigate | Explicit branch to the pre-authorized 125-05 slot (predecessor-only / ABAUDIT-on-v1.16-doc, no value-mask, CHAIN_SKIP stays ∅); no improvised shortcut |
| T-125-04-SC | Tampering | package installs | accept | No package-manager installs this phase (gh/git/node only) |
</threat_model>

<verification>
- v1.16 Axis-2 GHA run ID + apex PASS/FAIL/SKIP recorded; 9-workflow cascade scanned; empirical {30,51,92,99,62,65} set + C15/ABAUDIT hypothesis both checked; verdict CASCADE RED|GREEN set
- Cross-OS EXACT MATCH table authored; every reproducible surface matches across axes
- Predecessor-byte-unchanged gate EMPTY over all enumerated non-Phase-125 frozen harness surfaces
- Composite close-gate precondition verdict recorded
</verification>

<success_criteria>
The 3-axis terminal re-audit is complete with cross-OS EXACT MATCH; the 9-firing-workflow cascade is scanned for RED (empirical drift set cross-checked, C15/ABAUDIT confirmed-or-denied); the predecessor-byte-unchanged HARD gate is EMPTY; the close-gate precondition verdict is set (GREEN → proceed once owner PIPE-02 PASS; RED → hand off to 125-05).
</success_criteria>

<output>
Create `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-SUMMARY.md` when done (in addition to 125-04-AUDIT-RESULTS.md), recording the Axis-2 run IDs and the composite verdict.
</output>
</content>
