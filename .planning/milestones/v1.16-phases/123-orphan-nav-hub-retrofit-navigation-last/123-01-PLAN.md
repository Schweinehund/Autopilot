---
phase: 123-orphan-nav-hub-retrofit-navigation-last
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_registry/RE-index.md
  - scripts/pipeline/retrofit-nav-hub.mjs
autonomous: true
requirements: [RETRO-06]

must_haves:
  truths:
    - "RE-218..RE-221 rows exist (path-keyed, Doc Type Reference, Status Approved) in RE-index.md BEFORE the fork runs, so buildDocIdMap() join-on-Path resolves all 4 hubs"
    - "retrofit-nav-hub.mjs is a NEW file forked from retrofit-mermaid-structural.mjs (chain tip) — the Phase-122 fork is byte-unchanged"
    - "resolveDocType() returns 'Reference' for each of the 4 nav-hub paths and 'null' for a non-nav path (via the NAV_HUB_PATHS Set, not a directory prefix)"
    - "The fork keeps all inherited guards (MERMAID-STILL-PRESENT, DOC-ID-ALREADY-PRESENT, DOC-ID-UNRESOLVED, TEMPLATE-SENTINEL) and the auto-filled VH date — no guard is removed"
    - "A dry-run against the 4 explicit paths resolves doc_type=Reference, platform-injected=false (all carry platform: all -> All Platforms), vhBranch=PREPEND-3col — no doc file is written"
  artifacts:
    - path: "scripts/pipeline/retrofit-nav-hub.mjs"
      provides: "Phase-123 nav-hub fork: NAV_HUB_PATHS Set + resolveDocType() Reference branch; envelope-injection only (never splits #12)"
      contains: "NAV_HUB_PATHS"
    - path: "docs/_registry/RE-index.md"
      provides: "4 minted nav-hub rows RE-218..221 (Reference, Approved), path-alphabetical"
      contains: "RE-221"
  key_links:
    - from: "scripts/pipeline/retrofit-nav-hub.mjs"
      to: "docs/_registry/RE-index.md"
      via: "buildDocIdMap() join-on-Path"
      pattern: "buildDocIdMap"
---

<objective>
Build the Phase-123 nav-hub fork and mint the 4 nav-hub registry rows — the tooling + registry prep the retrofit plan (123-03) depends on. This plan writes NO doc file: it produces `scripts/pipeline/retrofit-nav-hub.mjs` and the `RE-218..221` rows, and proves via `--dry-run` that the fork resolves all 4 hubs fail-closed.

Purpose: Registry rows MUST precede the fork run (buildDocIdMap() fail-closes `DOC-ID-UNRESOLVED` otherwise, per 121 D-04/D-05). The fork must be a NEW file per the unbroken fork-don't-refactor convention (116->117->118->121->122->123, D-03).
Output: `scripts/pipeline/retrofit-nav-hub.mjs` (new fork) + 4 minted `RE-index.md` rows (RE-218..221).

## Locked decisions honored by this plan
- **D-03** (HIGH, OVERRULED "fork from 121"): fork from `retrofit-mermaid-structural.mjs` (Phase-122 chain tip) — it is a strict superset (auto-filled VH date closing DEFER-121-07-A; `DOC-ID-ALREADY-PRESENT` idempotency guard closing DEFER-121-07-B; `MERMAID-STILL-PRESENT` precondition). Change ONLY: add `NAV_HUB_PATHS` + a `resolveDocType()` Reference branch. Do NOT edit `main()`'s `--all` block — invoke by the 4 explicit paths instead (RESEARCH Pattern 1 sidesteps the noisy-ERROR `--all` hazard); document "never invoke with --all" in the fork header comment.
- **Registry** (121 D-04/D-05): mint RE-218=`common-issues.md`, RE-219=`index.md`, RE-220=`quick-ref-l1.md`, RE-221=`quick-ref-l2.md` — path-alphabetical, all `Reference`/`Approved`, hand-authored FIRST, path-keyed. RE-index max is RE-217 (verified contiguous).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-CONTEXT.md
@.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-RESEARCH.md
@scripts/pipeline/retrofit-mermaid-structural.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint RE-218..221 nav-hub registry rows</name>
  <read_first>
    - docs/_registry/RE-index.md (append after current max RE-217; confirm contiguity; row format `| RE-NNN | docs/path.md | Title | Reference | Approved |`)
    - 123-CONTEXT.md §Registry (RE-218..221 exact path->ID mapping; path-alphabetical; buildDocIdMap joins on Path not RE-number)
    - 123-RESEARCH.md §Sequencing step 1 + §"Verification of the 12..." (path list)
    - docs/index.md, docs/common-issues.md, docs/quick-ref-l1.md, docs/quick-ref-l2.md (read each file's H1 to source the Title column)
  </read_first>
  <action>
    Append 4 rows to `docs/_registry/RE-index.md` immediately after RE-217, path-alphabetical:
    RE-218 = `docs/common-issues.md`, RE-219 = `docs/index.md`, RE-220 = `docs/quick-ref-l1.md`, RE-221 = `docs/quick-ref-l2.md`.
    Every row: `Doc Type = Reference`, `Status = Approved` (these are already-live docs, not Pending). Title column = each file's actual H1 text. Verify RE-001..221 is zero-gap after the append. These rows are the join keys the fork reads — they MUST land before the fork runs in 123-03.
  </action>
  <verify>
    <automated>grep -cE '^\| RE-2(18|19|20|21) \| docs/(common-issues|index|quick-ref-l1|quick-ref-l2)\.md ' docs/_registry/RE-index.md</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 4 (all 4 rows present, path-keyed to the correct file)
    - RE-218 Path = docs/common-issues.md; RE-219 = docs/index.md; RE-220 = docs/quick-ref-l1.md; RE-221 = docs/quick-ref-l2.md
    - All 4 rows carry Doc Type = Reference and Status = Approved
    - `grep -c '^| RE-' docs/_registry/RE-index.md` shows a contiguous RE-001..221 count (zero gaps after RE-217)
  </acceptance_criteria>
  <done>4 path-keyed Reference/Approved rows RE-218..221 exist so buildDocIdMap() can resolve all 4 nav-hub paths.</done>
</task>

<task type="auto">
  <name>Task 2: Fork retrofit-nav-hub.mjs (NAV_HUB_PATHS + resolveDocType branch) and dry-run-prove it against the 4 hubs</name>
  <read_first>
    - scripts/pipeline/retrofit-mermaid-structural.mjs (992 lines — the fork base; DO NOT edit in place. Key sites: NAV_HUB insertion point after CA_ENROLLMENT_TIMING_PATH ~:173; resolveDocType() ~:176-183; the filePaths.length>0 explicit-path branch ~:911-915; buildDocIdMap join-on-Path; D1_MAP ~:97; the "Does NOT fix C17 #12" header note ~:55)
    - 123-RESEARCH.md §"Pattern 1" (invoke by 4 explicit paths, skip the --all edit) + §"Pattern 2" (copy-paste-ready resolveDocType diff) — both give exact code
    - 123-CONTEXT.md D-03 (keep/strip list; all inherited guards inert-but-kept; envelope-injection only)
    - docs/_registry/RE-index.md (the RE-218..221 rows from Task 1 — the fork reads these)
  </read_first>
  <action>
    Copy `retrofit-mermaid-structural.mjs` to a NEW file `scripts/pipeline/retrofit-nav-hub.mjs` (fork-don't-refactor). Then apply ONLY these two changes (RESEARCH Patterns 1-2):
    (1) Insert a `const NAV_HUB_PATHS = new Set([...])` with the 4 exact paths (`docs/index.md`, `docs/common-issues.md`, `docs/quick-ref-l1.md`, `docs/quick-ref-l2.md`) after the `CA_ENROLLMENT_TIMING_PATH` declaration (~:173).
    (2) Add one line `if (NAV_HUB_PATHS.has(rel)) return 'Reference';` to `resolveDocType()` immediately before its final `return null;` (~:176-183).
    Do NOT touch `main()`'s `--all` enumeration (~:890-906) — instead add a header comment near line 55 stating this fork is invoked by the 4 explicit paths and must NEVER be invoked with `--all` (the inherited `--all` would make every already-enrolled corpus file ERROR via the idempotency guard — noisy, masks the 4 targets). KEEP every inherited guard (MERMAID-STILL-PRESENT, TEMPLATE-SENTINEL, DOC-ID-ALREADY-PRESENT, DOC-ID-UNRESOLVED, keyless-Windows) and the `todayDate()` VH auto-fill unchanged — all are satisfied or inert for the 4 hubs (all carry `platform: all` -> `platformInjected=false` -> keyless guard never fires; none carry `doc_id`/`last_verified: 1970-01-01`/```mermaid).
    Then run `--dry-run` against the 4 explicit paths (do NOT write). Do NOT overwrite `retrofit-mermaid-structural.mjs`.
  </action>
  <verify>
    <automated>node scripts/pipeline/retrofit-nav-hub.mjs --dry-run docs/index.md docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md 2>&1 | grep -c -iE 'reference|all platforms'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/pipeline/retrofit-nav-hub.mjs --self-test` exits 0 (inherited self-test still passes — no ReferenceError)
    - The `--dry-run` against the 4 explicit paths resolves `doc_type=Reference` and `d1=All Platforms` (platform-injected false) for all 4, and reports vhBranch = PREPEND 3-col — with NO file written (`git status docs/` shows no hub modified by this task)
    - `grep -c 'NAV_HUB_PATHS' scripts/pipeline/retrofit-nav-hub.mjs` >= 2 (declaration + resolveDocType use)
    - `scripts/pipeline/retrofit-mermaid-structural.mjs` is byte-unchanged (fork did not overwrite the base)
    - The fork header comment documents "never invoke with --all"
  </acceptance_criteria>
  <done>The nav-hub fork exists (NAV_HUB_PATHS + Reference branch, all guards kept), the Phase-122 base is untouched, and a dry-run proves all 4 hubs resolve Reference/All-Platforms/PREPEND-3col fail-closed — ready for 123-03 to run for real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry mint -> buildDocIdMap() | a mis-keyed row silently mis-joins a doc_id to the wrong hub path |
| fork script -> enrolled hub | a script defect could enroll a hub with the wrong doc_type or a stale VH placeholder |

This phase has NO runtime/network/auth/user-input surface — it is a build-time Node.js tooling + static markdown corpus (RESEARCH §Security Domain: ASVS V2-V6 all N/A). The only integrity property that matters is fail-closed behavior.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Tampering | fork enrolling a hub with wrong doc_type or mis-joined doc_id | mitigate | path-keyed RE-218..221 verified before run; DOC-ID-UNRESOLVED fail-closed join; dry-run confirms Reference resolution per hub |
| T-123-02 | Tampering | inherited `--all` invocation masking the 4 targets with noisy ERRORs | mitigate | never invoke `--all` (invoke 4 explicit paths); header comment documents the ban |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | zero external packages — node built-ins only (RESEARCH §Standard Stack: Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `node scripts/pipeline/retrofit-nav-hub.mjs --self-test` exits 0
- RE-218..221 present, path-alphabetical, Reference/Approved, contiguous
- Dry-run resolves Reference / All Platforms / PREPEND-3col for all 4; no hub written
- `scripts/pipeline/retrofit-mermaid-structural.mjs` byte-unchanged
</verification>

<success_criteria>
The nav-hub fork and the 4 registry rows exist and are proven (via dry-run) to resolve all 4 hubs fail-closed, so 123-03 can inject the EEE envelope. No document is mutated in this plan.
</success_criteria>

<output>
Create `.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-01-SUMMARY.md` when done.
</output>
