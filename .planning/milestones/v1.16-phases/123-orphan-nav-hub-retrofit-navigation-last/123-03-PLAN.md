---
phase: 123-orphan-nav-hub-retrofit-navigation-last
plan: 03
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - docs/index.md
  - docs/common-issues.md
  - docs/quick-ref-l1.md
  - docs/quick-ref-l2.md
autonomous: true
requirements: [RETRO-06]

must_haves:
  truths:
    - "All 4 hubs carry the EEE header block (rendered from injected frontmatter doc_id/status/owner/doc_type) + a net-new >=30-word scope ## Summary as the FIRST H2 (D3-A layout, D-04 net-new sourcing)"
    - "Each hub's existing intro/coverage blockquote (with its [APv1 vs APv2]/[Windows vs macOS] see-also links) is RELOCATED below the Summary and retained verbatim — nothing deleted (SC2 link survival)"
    - "No hub ships a literal [FILL-IN] placeholder — the fork's placeholder is filled with real scope prose in the same commit"
    - "All 13 over-length #12 callouts are reflowed word-preserving: 11 A-split (incl index.md:9's internal 3-way at 182c/143c/132c) + 2 de-blockquote (quick-ref-l2.md:320, :371); zero reword (Option C FORBIDDEN)"
    - "c17-eee-contract.mjs exits 0 on all 4 hubs (assertions #1-13, incl #4 Summary-first, #5 >=30 words, #12 blockquote <=200c)"
  artifacts:
    - path: "docs/index.md"
      provides: "EEE-enrolled master routing hub: doc_id RE-219, net-new Summary, relocated coverage blockquote, index.md:9 3-way #12 split"
      contains: "doc_id: RE-219"
    - path: "docs/common-issues.md"
      provides: "EEE-enrolled triage hub: doc_id RE-218, net-new Summary, 5 #12 A-splits"
      contains: "doc_id: RE-218"
    - path: "docs/quick-ref-l1.md"
      provides: "EEE-enrolled L1 quick-ref: doc_id RE-220, net-new Summary (zero #12 violations)"
      contains: "doc_id: RE-220"
    - path: "docs/quick-ref-l2.md"
      provides: "EEE-enrolled L2 quick-ref: doc_id RE-221, net-new Summary, 5 A-split + 2 de-blockquote #12 fixes"
      contains: "doc_id: RE-221"
  key_links:
    - from: "docs/index.md"
      to: "docs/reference/00-index.md"
      via: "RE-142 exemplar shape (net-new Summary + retained intro relocated below)"
      pattern: "## Summary"
---

<objective>
Retrofit the 4 orphan nav-hubs to EEE and drive C17 to green (SC1): run the 123-01 fork to inject the EEE envelope, author the 4 net-new scope Summaries (filling the fork's `[FILL-IN]` placeholder in the same commit so no broken placeholder ships), then hand-apply the 13 over-length #12 callout reflows. This is the retrofit-drift work; the 12 pre-existing broken-link fixes are DELIBERATELY excluded here (they land as a separate git-blame-attributed commit in 123-04 per D-01).

Purpose: SC1 requires EEE header block + `## Summary`-first + C17-green on all 4. The fork does envelope-injection ONLY — it never splits #12 (`retrofit-mermaid-structural.mjs:55` "Does NOT fix C17 #12"); #12 reflow + Summary prose are hand-work AFTER the fork (D-02, D-03).
Output: 4 enrolled, #12-clean, Summary-first hubs; `c17-eee-contract.mjs` exits 0 on all 4.

## Locked decisions honored by this plan
- **D-04** (VERY HIGH, foreclosed-by-lock): net-new >=30-word scope Summary (platforms/frameworks + audience) as first H2 on all 4, matching the RE-142 exemplar (`docs/reference/00-index.md:17-19`). Reject Option B (repurpose the coverage blockquote) — its `[APv1 vs APv2]`/`[Windows vs macOS]` see-also links would poison the lead retrieval chunk. Retain the intro/coverage blockquote RELOCATED below the Summary (its links must survive for SC2). Exact prose = author discretion.
- **D-02** (HIGH, OVERRULED "Option B"): 13 over-length callouts. 11 -> A-split (blank-line-separated blockquotes; one truly-blank line breaks a #12 group, an empty `>` line does NOT — `c17-eee-contract.mjs:393-402`). The 2 `⚠️` ownership pointers at `quick-ref-l2.md:320` and `:371` -> full de-blockquote to a `⚠️ **...**` bold-led paragraph (single atomic sentences; A-split fractures subject-list from verb). Option C (reword-to-fit) FORBIDDEN — word-preserving is mandatory (`116-CONTEXT.md:137`, carried at `121-CONTEXT.md:112`).
- **index.md:9 landmine** (RESEARCH Pitfall 3): the relocated coverage callout is 459c ON ITS OWN LINE — needs an INTERNAL 3-way clause split at the two natural comma boundaries into 182c / 143c / 132c chunks, then a blank line before line-10 (144c, unchanged) = 4 A-split groups, all <=200c, all word-preserving.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-CONTEXT.md
@.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-RESEARCH.md
@scripts/validation/c17-eee-contract.mjs
@docs/reference/00-index.md
@docs/error-codes/00-index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the fork on the 4 hubs + author the 4 net-new >=30-word Summaries (one commit, no [FILL-IN] shipped)</name>
  <read_first>
    - scripts/pipeline/retrofit-nav-hub.mjs (the 123-01 fork — invoke by the 4 EXPLICIT paths, never --all)
    - docs/_registry/RE-index.md (RE-218..221 must already be present from 123-01, else the fork fail-closes DOC-ID-UNRESOLVED)
    - docs/reference/00-index.md:17-21 (RE-142 exemplar: net-new scope Summary + retained intro relocated immediately below — copy this SHAPE, not its words)
    - 123-CONTEXT.md D-04 (net-new Summary sourcing; retain-relocate the coverage blockquote; the exemplar) + §code_context (fork emits [FILL-IN])
    - docs/index.md, docs/common-issues.md, docs/quick-ref-l1.md, docs/quick-ref-l2.md (read each in full to write an accurate scope Summary and confirm the relocated intro/coverage blockquote survives)
  </read_first>
  <action>
    First `--dry-run` the fork against the 4 explicit paths to confirm doc_type=Reference / d1=All Platforms / vhBranch=PREPEND-3col; then run for real: `node scripts/pipeline/retrofit-nav-hub.mjs docs/index.md docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md`.
    This injects frontmatter (`doc_id` RE-218/219/220/221, `status: Approved`, `owner`, `doc_type: Reference`), the EEE header block before H1, a `## Summary` `[FILL-IN]` placeholder as first H2, the relocated pre-H1 intro/coverage blockquote below it, and a PREPEND 3-col VH row with an auto-filled date.
    Then, in the SAME commit, REPLACE each hub's `[FILL-IN]` with a net-new >=30-word scope Summary paragraph (the platforms/frameworks the hub routes + intended audience), matching the RE-142 shape. Do NOT repurpose the coverage blockquote text (D-04 chunk-poison). Confirm the relocated coverage blockquote (with its `[APv1 vs APv2]`/`[Windows vs macOS]` links) is retained verbatim below the Summary on every hub. Do NOT split any #12 callout in this task (that is Task 2). Commit once — no `[FILL-IN]` token may remain.
  </action>
  <verify>
    <automated>test $(grep -l 'FILL-IN' docs/index.md docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md 2>/dev/null | wc -l) -eq 0 && grep -c '^doc_id: RE-2' docs/index.md docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md</automated>
  </verify>
  <acceptance_criteria>
    - Zero `[FILL-IN]` tokens remain across the 4 hubs (`grep -l 'FILL-IN'` returns nothing)
    - Each hub's frontmatter carries its correct `doc_id` (index.md=RE-219, common-issues.md=RE-218, quick-ref-l1.md=RE-220, quick-ref-l2.md=RE-221), plus `status: Approved`, `owner`, `doc_type: Reference`
    - Each hub's `## Summary` is the FIRST H2, is >=30 words, and is net-new scope prose (NOT the coverage blockquote text)
    - Each hub still contains its `[APv1 vs APv2]` / `[Windows vs macOS]` coverage-blockquote links, now relocated below the Summary
    - `scripts/pipeline/retrofit-nav-hub.mjs` and `retrofit-mermaid-structural.mjs` are unchanged (fork was run, not edited)
  </acceptance_criteria>
  <done>All 4 hubs are EEE-enrolled with correct doc_ids and a net-new >=30-word Summary-first; no placeholder shipped; coverage links retained below the Summary.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-apply the 13 #12 callout reflows and drive C17 to green on all 4 hubs (SC1)</name>
  <read_first>
    - 123-RESEARCH.md §"Verification of the 13 Over-Length #12 Callouts" (per-line treatment table: common-issues 27/94/317/334/404; quick-ref-l2 124/237/273/288/320/339/371; index 9) + Pitfall 3 (index.md:9 internal 3-way 182c/143c/132c) + Pitfall 5 (do the reflows before the checker runs) + Assumption A1 (re-measure any A-split at task time; re-split further if a half still >200c)
    - 123-CONTEXT.md D-02 (A-split vs de-blockquote rule; the 2 de-blockquotes at quick-ref-l2:320/371; reword FORBIDDEN)
    - scripts/validation/c17-eee-contract.mjs:393-402 (the exact #12 join-then-measure grouping: consecutive `>` lines concatenate; a truly-blank line breaks the group; an empty `>` line does NOT) and :257-266 (#5 >=30 words), :236-246 (#4 Summary-first)
    - docs/error-codes/00-index.md:21-24 (the shipped A-split exemplar — blank-line-separated blockquotes, C17-green)
    - docs/common-issues.md, docs/quick-ref-l2.md, docs/index.md (the 3 files carrying over-length callouts; quick-ref-l1.md has ZERO — do not touch its callouts)
  </read_first>
  <action>
    Reflow all 13 over-length #12 callouts word-preserving, measuring each group's joined length against the C17 :393-402 algorithm:
    - `common-issues.md` lines 27, 94, 317, 334, 404 -> A-split at the sentence/em-dash/clause boundary given in the RESEARCH table; verify no contained markdown link (e.g. the `admin-setup-ios/09-...`, `_glossary-macos.md#mam-we`, `quick-ref-l1.md#apple-business-quick-reference`, `apv1-vs-apv2.md` links) is straddled by a split point; each resulting group <=200c.
    - `quick-ref-l2.md` lines 124, 237, 273, 288, 339 -> A-split at sentence boundaries (keep the `l2-runbooks/07-...` and PITFALL-7 references intact).
    - `quick-ref-l2.md` lines 320 and 371 -> full DE-BLOCKQUOTE to a `⚠️ **...**` bold-led normal paragraph (drop the `>`, retain the glyph + bold + every word); these read ahead of the existing plain "see" pointers at :322/:373.
    - `index.md` line 9 -> INTERNAL 3-way split at the two comma boundaries into the 182c / 143c / 132c chunks (RESEARCH Pitfall 3), then a blank line before the line-10 (144c) group = 4 A-split groups, each <=200c.
    Then run full C17 and confirm the 4 hubs are green. Every treatment must be word-preserving (prove via a git-diff word-multiset check that strips the leading `>` token — zero real words added/removed); reword is FORBIDDEN. Do NOT fix the 12 pre-existing broken links here (123-04, separate commit).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/index.md docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/c17-eee-contract.mjs` (full corpus, or scoped to the 4 hubs) reports zero violations on all 4 hubs — specifically #12=0, #5=0, #4=0
    - `quick-ref-l2.md:320` and `:371` are now bold-led normal paragraphs beginning `⚠️ **` (no leading `>`), each a single retained sentence
    - `index.md`'s relocated coverage callout is split into 4 blockquote groups each <=200c (the 459c single line no longer exists as one `>` group)
    - A git-diff word-multiset check (stripping `>` prefixes) shows zero real words added or removed across the 3 edited files (word-preserving proof)
    - `docs/quick-ref-l1.md` has no #12 callout edits (it had zero violations)
  </acceptance_criteria>
  <done>All 13 over-length callouts are reflowed word-preserving (11 A-split incl index.md:9's 3-way + 2 de-blockquote) and C17 exits 0 on all 4 hubs — SC1 satisfied for the retrofit; the 12 pre-existing link fixes remain for 123-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork run -> enrolled hub | a stale [FILL-IN] or a deleted coverage link could ship if the same-commit fill/retain discipline slips |
| hand #12 reflow -> callout content | a reword or mid-link split could silently change safety/provenance semantics or break an outbound link |

No runtime/network/auth surface — static markdown reformat (RESEARCH §Security Domain).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-05 | Tampering | shipping a literal [FILL-IN] Summary (C17 #5 red, broken lead chunk) | mitigate | fork-run + Summary authoring in ONE commit; grep gate asserts zero FILL-IN tokens |
| T-123-06 | Tampering | a #12 reflow rewording or splitting mid-link, changing meaning or breaking a link | mitigate | word-preserving git-diff multiset proof (strip `>`); Option C reword FORBIDDEN; verify no link straddles a split point |
| T-123-07 | Information Disclosure | deleting the relocated coverage blockquote (losing [APv1 vs APv2]/[Windows vs macOS] links, failing SC2) | mitigate | retain-relocate (never delete); acceptance criterion greps the surviving links; 123-04 link-checker re-proves |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | zero external packages — no install in this plan |
</threat_model>

<verification>
- `node scripts/validation/c17-eee-contract.mjs` exits 0 on all 4 hubs (#4/#5/#12 = 0)
- Zero `[FILL-IN]` tokens; each hub carries its correct doc_id + net-new >=30-word Summary-first
- 2 de-blockquotes at quick-ref-l2:320/371; index.md:9 split into 4 groups <=200c
- Word-preserving multiset proof (strip `>`) shows zero real-word deltas
- Coverage-blockquote see-also links retained below the Summary
</verification>

<success_criteria>
SC1 satisfied for the retrofit: all 4 hubs are EEE-enrolled, Summary-first, #12-clean, and C17-green — word-preserving throughout, with coverage links retained for SC2. The 12 pre-existing-rot link fixes are deliberately deferred to 123-04's separate commit.
</success_criteria>

<output>
Create `.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-03-SUMMARY.md` when done.
</output>
