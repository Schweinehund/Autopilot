---
phase: 123-orphan-nav-hub-retrofit-navigation-last
plan: 04
type: execute
wave: 3
depends_on: ["123-02", "123-03"]
files_modified:
  - docs/quick-ref-l2.md
  - docs/common-issues.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-VERIFICATION.md
autonomous: true
requirements: [RETRO-06]

must_haves:
  truths:
    - "All 12 pre-existing broken links are fixed: 11 `../` over-escapes dropped in quick-ref-l2.md (lines 316,317,318,320,322,366,367,368,369,371,373) + common-issues.md:360 repointed to #compliance-failure-or-access-blocked"
    - "The 12 fixes land as a SEPARATE git-blame-attributed pre-existing-rot commit, distinct from the 123-03 retrofit-drift commit (D-01)"
    - "check-nav-hub-links.mjs exits 0 on the final corpus — zero broken links in BOTH outbound (from the 4 hubs) and inbound (corpus-wide, into the 4 hubs) scans (SC2)"
    - "c17-eee-contract.mjs exits 0 on the FULL corpus — the 4 new hubs green AND all previously-enrolled files still green (SC1, full-corpus discipline)"
    - "Git history confirms the earliest Phase-123 hub commit post-dates the latest Phase-121/122 content commit (navigation-last, SC3)"
  artifacts:
    - path: "docs/quick-ref-l2.md"
      provides: "11 `../`-over-escape links fixed to docs-relative"
      contains: "operations/patch-management/04-android-patch-delivery.md"
    - path: ".planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-VERIFICATION.md"
      provides: "SC1/SC2/SC3 attestation (C17 full-corpus count, link-checker exit 0, navigation-last git evidence)"
      contains: "navigation-last"
  key_links:
    - from: "docs/quick-ref-l2.md"
      to: "docs/operations/patch-management/04-android-patch-delivery.md"
      via: "docs-relative link (../ dropped)"
      pattern: "operations/patch-management/04-android-patch-delivery.md#play-integrity-attestation"
---

<objective>
Close the phase: fix the 12 pre-existing broken links inside the 4 hubs as a SEPARATE git-blame-attributed commit (D-01), then run the 123-02 link-checker (SC2) and full-corpus C17 (SC1) to green, prove navigation-last via git history (SC3), author 123-VERIFICATION.md, and mark RETRO-06 complete.

Purpose: SC2 is a state assertion ("no broken links in the 4 hubs") that is unsatisfiable while the 12 pre-existing breaks live — `git blame` proved all 12 predate Phases 121/122 by 2+ months (2026-04-30 / 05-05), so they are pre-existing rot, committed separately so SC2 is met literally without misrepresenting them as retrofit-induced (D-01). The link-checker and full-corpus C17 must run AFTER the 123-03 Summary/#12 work and these fixes (RESEARCH Pitfall 5: a checker run before the fixes is a false negative).
Output: 12 links fixed; link-checker + full-corpus C17 green; navigation-last attested; RETRO-06 Complete.

## Locked decisions honored by this plan
- **D-01** (HIGH): the 12 fixes are IN-scope this phase (not deferred to 124) and committed SEPARATELY, git-blame-attributed as pre-existing-rot cleanup. 11 `../` over-escapes -> drop the `../` (dirs live under `docs/`; proven by `index.md:280` linking the identical target with no `../` and by `quick-ref-l2.md:326`'s own docs-relative convention). `common-issues.md:360`'s dead `#compliance-access-blocked` anchor is a content decision (Claude's Discretion) — RESEARCH recommends REPOINT to the real slug `#compliance-failure-or-access-blocked` (the macOS `### Compliance Failure or Access Blocked` section at :206 genuinely exists; removing the row would delete a correct cross-reference over an anchor typo).
- **Navigation-last** (SC3, satisfied by construction): Phases 121/122 committed 2026-07-07; Phase 123's commits post-date them. Needs only a git-history attestation at close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-CONTEXT.md
@.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-RESEARCH.md
@scripts/validation/check-nav-hub-links.mjs
@scripts/validation/c17-eee-contract.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the 12 pre-existing broken links (separate git-blame-attributed commit)</name>
  <read_first>
    - 123-RESEARCH.md §"Verification of the 12 Pre-Existing Broken Links" (the byte-verified fix table: rows 1-11 = drop `../`; row 12 = repoint common-issues.md:360; each target anchor confirmed valid, incl the `{#id}` overrides at play-integrity-attestation:50 / deadlines-cutover-dates:76 and the plain slugs for admin-setup-linux/03 Steps 2-5) + Pitfall 2 (cwd-vs-linking-dir root cause) + Assumption A2 (the repoint target is a low-risk inference)
    - 123-CONTEXT.md D-01 (the 12 links; separate commit; git-blame attribution) + §Claude's Discretion (common-issues.md:360 repoint-vs-remove)
    - docs/quick-ref-l2.md (lines 316,317,318,320,322,366,367,368,369,371,373 — note 320 and 371 were also de-blockquoted in 123-03; the `../` drop applies to the now-de-blockquoted paragraph text)
    - docs/common-issues.md (line 360 area; confirm the real macOS heading `### Compliance Failure or Access Blocked` and its GitHub slug before repointing)
  </read_first>
  <action>
    Apply the 12 fixes:
    (1) In `docs/quick-ref-l2.md`, drop the leading `../` on all 11 links at lines 316, 317, 318, 320, 322, 366, 367, 368, 369, 371, 373 — `../operations/patch-management/04-android-patch-delivery.md...` -> `operations/patch-management/04-android-patch-delivery.md...` and `../admin-setup-linux/03-compliance-policy.md...` -> `admin-setup-linux/03-compliance-policy.md...`. (Lines 320/371 are now the de-blockquoted `⚠️ **...**` paragraphs from 123-03; drop the `../` in that paragraph text. Re-locate by link target, not raw line number, since 123-03's edits may have shifted line numbers.)
    (2) In `docs/common-issues.md`, repoint the dead `[macOS: Compliance / Access Blocked](#compliance-access-blocked)` link to `#compliance-failure-or-access-blocked` (the real GitHub slug of the existing `### Compliance Failure or Access Blocked` heading; confirm the slug via the 123-02 checker's own algorithm or by inspection before writing).
    Commit these 12 fixes as ONE dedicated commit whose message identifies it as pre-existing-rot cleanup (git-blame-attributed, e.g. references the 2026-04/05 blame dates), SEPARATE from the 123-03 retrofit commit. Do NOT fold them into any envelope/Summary/#12 diff.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '(\.\./operations/\|(\.\./admin-setup-linux/' docs/quick-ref-l2.md` returns 0 (all 11 `../` over-escapes dropped)
    - `grep -c '#compliance-access-blocked' docs/common-issues.md` returns 0 (the dead anchor is gone) and `#compliance-failure-or-access-blocked` is now present
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 (all 12 formerly-broken links now resolve; this is the SC2 gate)
    - `git log --oneline -- docs/quick-ref-l2.md docs/common-issues.md` shows a dedicated pre-existing-rot commit distinct from the 123-03 retrofit commit
  </acceptance_criteria>
  <done>All 12 pre-existing broken links resolve, fixed in a separate git-blame-attributed commit; the link-checker exits 0 (SC2 outbound+inbound green).</done>
</task>

<task type="auto">
  <name>Task 2: Full-corpus C17 (SC1) + navigation-last git attestation (SC3) + author 123-VERIFICATION.md + mark RETRO-06 complete</name>
  <read_first>
    - 123-RESEARCH.md §Verification (SC1 full-corpus C17 command + "225/0"-style expectation; SC2 link-checker + separate-commit check; SC3 the exact two git-log commands: latest 121/122 content commit vs earliest Phase-123 hub commit) + §Sequencing steps 7-9
    - 123-CONTEXT.md §domain (SC1/SC2/SC3 restated) + §Registry (RE-218..221 already Approved from 123-01)
    - .planning/REQUIREMENTS.md (RETRO-06 traceability row `:90` — flip Pending -> Complete on green)
    - .planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-15-SUMMARY.md (the prior close-plan shape: full-corpus C17 count + requirement flip + VERIFICATION.md structure to mirror)
  </read_first>
  <action>
    (1) Run full-corpus C17 (`node scripts/validation/c17-eee-contract.mjs --verbose`, no path args — it walks all of `docs/` and self-selects enrolled files). Confirm 0 total violations: the 4 new hubs green AND all previously-enrolled files (~225+) still green (Phase-122-15 full-corpus discipline). If any previously-enrolled file regressed, STOP and report — do not mask.
    (2) Prove navigation-last (SC3): run the two git-log commands from RESEARCH §Verification — find the latest commit touching any Phase-121/122 content class, and the earliest Phase-123 commit touching any of the 4 hubs; confirm the hub commit's date is strictly later. Record both SHAs + ISO dates.
    (3) Author `.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-VERIFICATION.md` attesting SC1 (C17 full-corpus violation count), SC2 (link-checker exit 0 + the separate pre-existing-rot commit SHA), SC3 (navigation-last git evidence), and confirming RE-218..221 are Approved.
    (4) Flip RETRO-06 to Complete in `.planning/REQUIREMENTS.md` traceability table (only after SC1+SC2+SC3 are all green).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs; echo "c17=$?"; node scripts/validation/check-nav-hub-links.mjs; echo "links=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/c17-eee-contract.mjs` (full corpus) exits 0 with 0 total violations — 4 new hubs + all previously-enrolled files green
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 (SC2 confirmed on the final corpus)
    - `123-VERIFICATION.md` exists and records the SC3 evidence: earliest Phase-123 hub commit date > latest Phase-121/122 content commit date, with both SHAs
    - `.planning/REQUIREMENTS.md` shows `RETRO-06 | Phase 123 | Complete`
    - The VERIFICATION cites the separate pre-existing-rot commit SHA distinct from the retrofit commit (D-01 evidence)
  </acceptance_criteria>
  <done>SC1 (full-corpus C17 green), SC2 (link-checker green + separate rot commit), and SC3 (navigation-last git attestation) are all proven and recorded in 123-VERIFICATION.md; RETRO-06 is Complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| link fix -> commit history | folding the rot fixes into the retrofit diff would misrepresent pre-existing rot as retrofit-induced (D-01 violation) |
| C17 scope -> SC1 claim | running C17 on only the 4 hubs (not full corpus) could miss a regression a retrofit introduced in a previously-enrolled file |

No runtime/network/auth surface — static markdown + build-time linters (RESEARCH §Security Domain).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-08 | Repudiation | rot fixes folded into the retrofit commit, obscuring their pre-existing provenance | mitigate | dedicated pre-existing-rot commit, git-blame-attributed; VERIFICATION cites its distinct SHA (D-01) |
| T-123-09 | Tampering | a link-checker false-green passing SC2 with a still-broken link | mitigate | checker is GitHub-exact + self-tested (123-02); run AFTER all fixes (Pitfall 5); both outbound+inbound |
| T-123-10 | Tampering | SC1 declared green on a hub-only C17 run, masking a corpus regression | mitigate | full-corpus C17 (no path args), mirroring the Phase-122-15 "225/0" full-corpus discipline; STOP-on-regression, no masking |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | zero external packages — no install in this plan |
</threat_model>

<verification>
- `node scripts/validation/check-nav-hub-links.mjs` exits 0 (SC2 — outbound + inbound)
- `node scripts/validation/c17-eee-contract.mjs` exits 0 full-corpus (SC1)
- 11 `../` drops + common-issues.md:360 repoint landed as a separate git-blame-attributed commit
- 123-VERIFICATION.md records SC3 navigation-last git evidence (hub commit date > 121/122 content commit date)
- RETRO-06 flipped to Complete
</verification>

<success_criteria>
All 3 phase Success Criteria are proven: SC1 (C17 full-corpus green with the 4 hubs enrolled), SC2 (link-checker green + the 12 pre-existing breaks fixed in a separate commit), SC3 (navigation-last git attestation). RETRO-06 is Complete.
</success_criteria>

<output>
Create `.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-04-SUMMARY.md` when done.
</output>
