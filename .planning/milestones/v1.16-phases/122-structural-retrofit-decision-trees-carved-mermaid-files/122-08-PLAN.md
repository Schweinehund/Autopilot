---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 08
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - docs/admin-setup-apv1/00-overview.md
  - docs/admin-setup-apv2/00-overview.md
  - docs/admin-setup-linux/00-overview.md
  - docs/reference/ca-enrollment-timing.md
autonomous: true
requirements: [RETRO-08]

must_haves:
  truths:
    - "Each of the 4 LIGHT carve-outs is mermaid-free with its linear graph / sequenceDiagram converted per D-02"
    - "RE-128 (admin-setup-linux/00) enrolls as Guide; RE-147 (ca-enrollment-timing) enrolls as Reference — both move from keyless toward Approved"
    - "Each carries the EEE header block + doc_id + ## Summary-first; C17 exits 0"
  artifacts:
    - path: "docs/admin-setup-linux/00-overview.md"
      provides: "Numbered stage list (LOCKED — 5); RE-128 Guide"
      contains: "LOCKED — 5"
    - path: "docs/reference/ca-enrollment-timing.md"
      provides: "Ordered step list from sequenceDiagram (8 messages); RE-147 Reference"
      contains: "doc_id: RE-147"
  key_links:
    - from: "docs/reference/ca-enrollment-timing.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-147 (Reference) injected by fork"
      pattern: "doc_id:\\s*RE-147"
---

<objective>
Convert the 4 LIGHT-tier carve-outs: apv1/00 + apv2/00 (linear graphs -> numbered stage lists), admin-setup-linux/00 (RE-128, fan-out -> numbered list), and ca-enrollment-timing (RE-147, sequenceDiagram -> ordered step list, doc_type Reference). This plan directly satisfies Success Criterion 2 (RE-128 + RE-147 move from Pending/keyless toward Approved).

Purpose: RETRO-08 — the remaining carved-mermaid files retrofitted, including SC2's RE-128/RE-147.
Output: 4 enrolled, C17-green files (RE-076, RE-087 Guide; RE-128 Guide; RE-147 Reference).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
@docs/_standards/EEE-SOP-standard.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert + enroll apv1/00 + apv2/00 overviews (RE-076, RE-087; Guide; numbered stage lists)</name>
  <read_first>
    - docs/admin-setup-apv1/00-overview.md, docs/admin-setup-apv2/00-overview.md
    - 122-RESEARCH.md Class 2 rows for apv1/00 (linear graph LR, 8 nodes / 7 plain edges / 0 diamonds; 2 BQ) and apv2/00 (linear graph LR, 4 nodes / 3 plain edges / 0 diamonds; 1 BQ)
    - 122-PATTERNS.md §3 (EEE Guide envelope); D-02 (no diamond -> numbered stage list)
  </read_first>
  <action>
    For EACH file: `git show 71be4ab:<path>` to capture the element set (apv1/00 LOCKED-N 8 = 8 nodes + 0 labeled edges; apv2/00 LOCKED-N 4 = 4 nodes + 0 labeled edges). NO diamond -> ordered numbered stage list. Delete fence + stale prose. Annotate `LOCKED — 8 (nodes + labeled edges)` / `LOCKED — 4 (nodes + labeled edges)` respectively (R1, re-verify). Split the existing >200c blockquote groups (apv1/00: 2; apv2/00: 1). Enroll each: `node scripts/pipeline/retrofit-mermaid-structural.mjs <path>` (RE-076 / RE-087, Guide). Author each `## Summary` (>=30 words — APv1 / APv2 admin-setup overview).
  </action>
  <verify>
    <automated>for f in docs/admin-setup-apv1/00-overview.md docs/admin-setup-apv2/00-overview.md; do node scripts/validation/c17-eee-contract.mjs "$f"; echo "$f c17=$?"; grep -c '^```mermaid' "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid'` = 0 on both files; C17 exits 0 on both
    - `grep -c 'LOCKED — 8' docs/admin-setup-apv1/00-overview.md` >= 1 and `grep -c 'LOCKED — 4' docs/admin-setup-apv2/00-overview.md` >= 1
    - `grep -c '^doc_id: RE-076' docs/admin-setup-apv1/00-overview.md` = 1 and `grep -c '^doc_id: RE-087' docs/admin-setup-apv2/00-overview.md` = 1 (both Guide)
    - Stale-prose grep clean on both
  </acceptance_criteria>
  <done>apv1/00 + apv2/00 are mermaid-free, C17-green, RE-076/RE-087 (Guide) enrolled as numbered stage lists.</done>
</task>

<task type="auto">
  <name>Task 2: Convert + enroll admin-setup-linux/00-overview.md (RE-128, Guide, LOCKED — 5) — SC2</name>
  <read_first>
    - docs/admin-setup-linux/00-overview.md (registry row RE-128, Guide, Pending)
    - 122-RESEARCH.md Class 2 row for linux/00 (flowchart TD, 5 nodes / 4 plain edges / 0 diamonds; fan-out only B->C/D/E; 2 BQ)
    - 122-PATTERNS.md §3 + §4 (RE-128 registry row)
  </read_first>
  <action>
    `git show 71be4ab:docs/admin-setup-linux/00-overview.md` to capture the element set (LOCKED-N 5 = 5 nodes + 0 labeled edges; fan-out B->C/D/E — one parent to three children, NOT a reconvergence). NO diamond -> ordered numbered stage list; preserve the B->C/D/E fan-out (list the three children under B). Delete fence + stale prose. Annotate `LOCKED — 5 (nodes + labeled edges)` (R1, re-verify). Split both existing >200c blockquote groups. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/admin-setup-linux/00-overview.md` (doc_id RE-128, Guide). Author `## Summary` (>=30 words — Linux admin-setup overview).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/admin-setup-linux/00-overview.md; echo "c17=$?"; grep -c '^```mermaid' docs/admin-setup-linux/00-overview.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/admin-setup-linux/00-overview.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 5'` >= 1; `grep -c '^doc_id: RE-128'` = 1; doc_type Guide
    - The B->C/D/E fan-out preserved; both blockquotes ≤200c; stale-prose grep clean
  </acceptance_criteria>
  <done>admin-setup-linux/00-overview.md (RE-128) is mermaid-free, C17-green, enrolled as Guide — SC2 half met.</done>
</task>

<task type="auto">
  <name>Task 3: Convert + enroll ca-enrollment-timing.md (RE-147, Reference, sequenceDiagram -> step list) — SC2</name>
  <read_first>
    - docs/reference/ca-enrollment-timing.md (has platform: Windows; registry row RE-147, Reference, Pending; sequenceDiagram lines ~25-39)
    - 122-RESEARCH.md Class 2 row for ca-enrollment-timing (sequenceDiagram, 4 actors / 8 messages; 4 BQ) + D-02 sequence rule
    - 122-PATTERNS.md §3 (the RE-147-specific envelope note; sequence -> step list)
  </read_first>
  <action>
    `git show 71be4ab:docs/reference/ca-enrollment-timing.md` to capture the sequenceDiagram (4 actors, 8 messages). Per D-02, sequenceDiagram -> ORDERED NUMBERED STEP LIST (one step per message, preserving actor->actor direction + label). Delete fence + stale prose. Annotate `LOCKED — 8 (messages)` (re-verify all 8 present). The fork router must emit doc_type `Reference` for this single path (NOT Guide). This file already carries `platform: Windows` (not keyless). Split all 4 existing >200c blockquote groups. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/reference/ca-enrollment-timing.md` (doc_id RE-147, Reference). Author `## Summary` (>=30 words — CA enrollment timing chicken-and-egg problem).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/reference/ca-enrollment-timing.md; echo "c17=$?"; grep -c '^```mermaid' docs/reference/ca-enrollment-timing.md; grep -c '^doc_type: Reference' docs/reference/ca-enrollment-timing.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/reference/ca-enrollment-timing.md` = 0
    - C17 exits 0; `grep -c '^doc_id: RE-147'` = 1; `grep -c '^doc_type: Reference' docs/reference/ca-enrollment-timing.md` = 1
    - All 8 sequence messages present as ordered steps; all 4 blockquotes ≤200c; stale-prose grep clean
  </acceptance_criteria>
  <done>ca-enrollment-timing.md (RE-147) is mermaid-free, C17-green, enrolled as Reference — SC2 fully met (with RE-128).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-conversion -> converted list | linux/00 fan-out + ca-timing's 8 messages are the loss surface |
| fork router -> doc_type | ca-enrollment-timing must emit Reference (single-path), not Guide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | dropping a fan-out child or a sequence message | mitigate | list all children/steps; LOCKED-N; D-01 re-derivation in 122-13 |
| T-122-07 | Tampering | ca-enrollment-timing mis-typed as Guide | mitigate | single-path router rule -> Reference; acceptance grep asserts doc_type Reference |
| T-122-05 | Repudiation | stale diagram-prose left behind | mitigate | delete in-task + stale-prose grep gate |
| T-122-SC | Tampering | package installs | accept | none — content edits + node built-in fork only |
</threat_model>

<verification>
- C17 exits 0 on all 4 files; 0 ```mermaid fences; LOCKED-N 8/4/5/8 present; doc_id RE-076/087/128/147 injected; ca-timing doc_type Reference; all blockquotes ≤200c; stale-prose grep clean
</verification>

<success_criteria>
The 4 LIGHT carve-outs are mermaid-free, C17-green, enrolled; SC2 met (RE-128 Guide + RE-147 Reference both enrolled with doc_id). All 10 carved-mermaid files now converted.
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-08-SUMMARY.md` when done.
</output>
