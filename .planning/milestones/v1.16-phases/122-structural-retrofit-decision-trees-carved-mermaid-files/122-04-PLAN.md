---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 04
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - docs/decision-trees/05-device-lifecycle.md
  - docs/decision-trees/06-macos-triage.md
  - docs/decision-trees/07-ios-triage.md
autonomous: true
requirements: [RETRO-05]

must_haves:
  truths:
    - "Each decision-tree is a mermaid-free text-equivalent decision table preserving every node + labeled edge"
    - "05-device-lifecycle is enrolled as doc_type Reference (directory precedence over its -lifecycle filename)"
    - "Each carries the EEE header block + doc_id + ## Summary-first; C17 exits 0"
  artifacts:
    - path: "docs/decision-trees/05-device-lifecycle.md"
      provides: "Decision table (LOCKED — 18); the D-04 LOCKED directory-precedence worked case, doc_type Reference"
      contains: "LOCKED — 18"
    - path: "docs/decision-trees/06-macos-triage.md"
      provides: "Decision table (LOCKED — 31) incl. nested MACSSO diamond"
      contains: "LOCKED — 31"
    - path: "docs/decision-trees/07-ios-triage.md"
      provides: "Decision table (LOCKED — 23)"
      contains: "LOCKED — 23"
  key_links:
    - from: "docs/decision-trees/05-device-lifecycle.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-212 / doc_type Reference injected by fork"
      pattern: "doc_id:\\s*RE-212"
---

<objective>
Convert decision-trees 05/06/07 to decision tables and enroll them. 05-device-lifecycle is the D-04(a) LOCKED worked case (directory precedence -> Reference despite the `-lifecycle` filename). 06 carries a nested MACSSO diamond. Honor Plan-01 rulings R1/R2.

Purpose: RETRO-05 — decision-trees retrofitted with every leaf preserved.
Output: 3 enrolled, C17-green files (RE-212, RE-213, RE-214).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
@docs/_standards/EEE-SOP-standard.md
@docs/decision-trees/10-8021x-triage.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert + enroll 05-device-lifecycle.md (RE-212, Reference, LOCKED — 18, 4 diamonds Q1-Q4)</name>
  <read_first>
    - docs/decision-trees/05-device-lifecycle.md (already carries platform: Windows in frontmatter)
    - 122-RESEARCH.md Class 1 row for 05 (10 nodes / 8 labeled edges / 4 diamonds Q1-Q4, the LOCKED verified case; 2 existing >200c BQ groups)
    - 122-CONTEXT.md D-04(a) (directory precedence -> Reference; the LOCKED worked resolution) + docs/_standards/EEE-SOP-standard.md:178-187
    - 122-PATTERNS.md §2 + §3; docs/decision-trees/10-8021x-triage.md
  </read_first>
  <action>
    `git show 71be4ab:docs/decision-trees/05-device-lifecycle.md` to capture the element set (LOCKED-N 18 = 10 nodes + 8 labeled edges; 4 diamonds Q1-Q4). `{...}` present -> ordinal-column decision table, one row per node/labeled edge across Q1-Q4. Delete fence + stale prose. Annotate `LOCKED — 18 (nodes + labeled edges)` (R1, re-verify). This file ALREADY has `platform: Windows` in frontmatter (not keyless) — the fork router MUST emit doc_type `Reference` for it via DECISION_TREE_PATHS (directory precedence, NOT the `-lifecycle` filename). Split the 2 existing >200c blockquote groups (word-preserving Transform A/B). Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/decision-trees/05-device-lifecycle.md` (doc_id RE-212, doc_type Reference). Author `## Summary` (>=30 words — device lifecycle action decision tree, both APv1/APv2).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/decision-trees/05-device-lifecycle.md; echo "c17=$?"; grep -c '^```mermaid' docs/decision-trees/05-device-lifecycle.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/decision-trees/05-device-lifecycle.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 18'` >= 1; `grep -c '^doc_id: RE-212'` = 1; `grep -c '^doc_type: Reference' docs/decision-trees/05-device-lifecycle.md` = 1
    - All 4 diamonds (Q1-Q4) represented; the 2 pre-existing blockquotes are each ≤200c; stale-prose grep clean
  </acceptance_criteria>
  <done>05-device-lifecycle.md is mermaid-free, C17-green, enrolled RE-212 as Reference (directory precedence), all Q1-Q4 preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Convert + enroll 06-macos-triage.md (RE-213, LOCKED — 31, nested MACSSO diamond)</name>
  <read_first>
    - docs/decision-trees/06-macos-triage.md (carries platform key)
    - 122-RESEARCH.md Class 1 row for 06 (16 nodes / 15 labeled edges / 4 diamonds incl. nested MACSSO; no reconvergence; 0 BQ)
    - 122-PATTERNS.md §2 + §3; docs/decision-trees/10-8021x-triage.md
  </read_first>
  <action>
    `git show 71be4ab:docs/decision-trees/06-macos-triage.md` to capture the element set (LOCKED-N 31 = 16 nodes + 15 labeled edges; 4 diamonds including the nested MACSSO diamond — enumerate the nested node explicitly). `{...}` present -> ordinal-column decision table; the nested MACSSO decision must appear as its own row(s), not be flattened away. Delete fence + stale prose. Annotate `LOCKED — 31 (nodes + labeled edges)` (R1, re-verify). Re-scan blockquotes after authoring. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/decision-trees/06-macos-triage.md` (doc_id RE-213). Author `## Summary` (>=30 words — macOS ADE triage).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/decision-trees/06-macos-triage.md; echo "c17=$?"; grep -c '^```mermaid' docs/decision-trees/06-macos-triage.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/decision-trees/06-macos-triage.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 31'` >= 1; `grep -c '^doc_id: RE-213'` = 1
    - The nested MACSSO diamond is represented as an explicit decision row; stale-prose grep clean
  </acceptance_criteria>
  <done>06-macos-triage.md is mermaid-free, C17-green, RE-213 enrolled, nested MACSSO decision preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Convert + enroll 07-ios-triage.md (RE-214, LOCKED — 23)</name>
  <read_first>
    - docs/decision-trees/07-ios-triage.md (carries platform key)
    - 122-RESEARCH.md Class 1 row for 07 (12 nodes / 11 labeled edges / 3 diamonds; no reconvergence; 1 existing BQ group)
    - 122-PATTERNS.md §2 + §3; docs/decision-trees/10-8021x-triage.md
  </read_first>
  <action>
    `git show 71be4ab:docs/decision-trees/07-ios-triage.md` to capture the element set (LOCKED-N 23 = 12 nodes + 11 labeled edges; 3 diamonds). `{...}` present -> ordinal-column decision table. Delete fence + stale prose. Annotate `LOCKED — 23 (nodes + labeled edges)` (R1, re-verify). Split the 1 existing >200c blockquote group. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/decision-trees/07-ios-triage.md` (doc_id RE-214). Author `## Summary` (>=30 words — iOS/iPadOS triage).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/decision-trees/07-ios-triage.md; echo "c17=$?"; grep -c '^```mermaid' docs/decision-trees/07-ios-triage.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/decision-trees/07-ios-triage.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 23'` >= 1; `grep -c '^doc_id: RE-214'` = 1
    - All 3 diamonds represented; the pre-existing blockquote ≤200c; stale-prose grep clean
  </acceptance_criteria>
  <done>07-ios-triage.md is mermaid-free, C17-green, RE-214 enrolled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-conversion -> converted table | a dropped node/labeled edge (esp. nested MACSSO) is silent content loss |
| fork router -> doc_type | 05's `-lifecycle` filename could be mis-typed as Guide without directory precedence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | hand-conversion flattening the nested MACSSO diamond or a Q1-Q4 node | mitigate | one-row-per-node/edge table; LOCKED-N; D-01 re-derivation in 122-12 |
| T-122-07 | Tampering | 05 mis-enrolled as Guide instead of Reference | mitigate | DECISION_TREE_PATHS explicit Set -> Reference; RE-212 rationale recorded (122-01) |
| T-122-05 | Repudiation | stale diagram-prose left behind | mitigate | delete in-task + stale-prose grep gate |
| T-122-SC | Tampering | package installs | accept | none — content edits + node built-in fork only |
</threat_model>

<verification>
- C17 exits 0 on all 3 files; 0 ```mermaid fences; LOCKED-N 18/31/23 present; doc_id RE-212/213/214 injected; 05 doc_type Reference; stale-prose grep clean
</verification>

<success_criteria>
Decision-trees 05/06/07 are mermaid-free, C17-green, enrolled; 05 is Reference (directory precedence), nested MACSSO diamond preserved.
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-04-SUMMARY.md` when done.
</output>
