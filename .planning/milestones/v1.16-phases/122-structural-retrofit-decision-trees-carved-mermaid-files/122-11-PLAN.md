---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 11
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - docs/macos-lifecycle/00-ade-lifecycle.md
  - docs/macos-lifecycle/01-psso-provisioning-walkthrough.md
  - docs/macos-lifecycle/02-mdm-migration-psso.md
autonomous: true
requirements: [RETRO-07]

must_haves:
  truths:
    - "Each macos-lifecycle file is a mermaid-free decision table (all carry a diamond); reconvergence (00's S7/S6->S8) and dual non-converging pipelines (02) preserved"
    - "The corpus-max blockquote burden is remediated: macos/01's 5877-char block split, macos/02's 16 groups (incl. a 1480-char block) all <=200c"
    - "Each carries the EEE header block + doc_id (Guide) + ## Summary-first; C17 exits 0"
  artifacts:
    - path: "docs/macos-lifecycle/00-ade-lifecycle.md"
      provides: "Decision table (LOCKED — 10) with S6 diamond + S7/S6->S8 merge"
      contains: "LOCKED — 10"
    - path: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md"
      provides: "Decision table (LOCKED — 13) with the psso Branch diamond + dual terminals; 5877c block remediated"
      contains: "LOCKED — 13"
    - path: "docs/macos-lifecycle/02-mdm-migration-psso.md"
      provides: "Decision table (LOCKED — 17) with dual non-reconverging pipelines; 16 blockquotes remediated"
      contains: "LOCKED — 17"
  key_links:
    - from: "docs/macos-lifecycle/02-mdm-migration-psso.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-206 (Guide) injected by fork; flips Pending->Approved in 122-15"
      pattern: "doc_id:\\s*RE-206"
---

<objective>
Convert the 3 macos-lifecycle files — all carry a diamond (-> decision tables) and the corpus's heaviest blockquote burden (macos/01 = a single 5877-char block; macos/02 = 16 groups incl. a 1480-char block). macos/01's `Branch` is the "psso decision node" explicitly named in CONTEXT D-01 riders; macos/02 has confirmed dual non-reconverging pipelines.

Purpose: RETRO-07 remainder — the final 3 of the 9 Mermaid-bearing lifecycle files; completes all 22 lifecycle files.
Output: 3 enrolled, C17-green files (RE-204, RE-205, RE-206; Guide).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
@docs/_standards/EEE-SOP-standard.md
@docs/decision-trees/10-8021x-triage.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert + enroll macos-lifecycle/00-ade-lifecycle.md (RE-204, Guide, LOCKED — 10, S6 diamond + merge)</name>
  <read_first>
    - docs/macos-lifecycle/00-ade-lifecycle.md
    - 122-RESEARCH.md Class 3 row for macos-lifecycle/00 (linear + 1 diamond S6, 8 nodes / 2 labeled edges; reconvergence S7->S8 merges with direct S6->S8; 1 BQ)
    - 122-PATTERNS.md §2; docs/decision-trees/10-8021x-triage.md
  </read_first>
  <action>
    `git show 71be4ab:docs/macos-lifecycle/00-ade-lifecycle.md` to capture the element set (LOCKED-N 10 = 8 nodes + 2 labeled edges; S6 diamond; the S7->S8 / S6->S8 merge). `{...}` present (S6) -> ordinal-column decision table; preserve the S7->S8 + direct S6->S8 reconvergence as explicit rows. Delete fence + stale prose. Annotate `LOCKED — 10 (nodes + labeled edges)` (R1, re-verify). Split the 1 existing >200c blockquote. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/macos-lifecycle/00-ade-lifecycle.md` (doc_id RE-204, Guide). Author `## Summary` (>=30 words — macOS ADE lifecycle).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/macos-lifecycle/00-ade-lifecycle.md; echo "c17=$?"; grep -c '^```mermaid' docs/macos-lifecycle/00-ade-lifecycle.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/macos-lifecycle/00-ade-lifecycle.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 10'` >= 1; `grep -c '^doc_id: RE-204'` = 1; doc_type Guide
    - The S7->S8 / S6->S8 merge preserved as explicit rows; stale-prose grep clean
  </acceptance_criteria>
  <done>macos-lifecycle/00-ade-lifecycle.md is mermaid-free, C17-green, RE-204 enrolled, merge preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Convert + enroll macos-lifecycle/01-psso-provisioning-walkthrough.md (RE-205, Guide, LOCKED — 13, psso Branch + 5877c block)</name>
  <read_first>
    - docs/macos-lifecycle/01-psso-provisioning-walkthrough.md
    - 122-RESEARCH.md Class 3 row for macos-lifecycle/01 (linear + 1 diamond Branch — "the psso decision node"; 11 nodes / 2 labeled edges; dual non-converging terminals S9/S10; 4 BQ incl the corpus-max 5877-char block) + Pitfall 5
    - 122-CONTEXT.md D-01 riders (the psso decision node explicitly named); STATE.md [Phase 121-03] (greedy packer)
    - 122-PATTERNS.md §2
  </read_first>
  <action>
    `git show 71be4ab:docs/macos-lifecycle/01-psso-provisioning-walkthrough.md` to capture the element set (LOCKED-N 13 = 11 nodes + 2 labeled edges; the Branch/psso diamond; dual non-converging terminals S9 and S10 — do NOT force a merge). `{...}` present (Branch) -> ordinal-column decision table; represent both S9 and S10 terminals distinctly. Delete fence + stale prose. Annotate `LOCKED — 13 (nodes + labeled edges)` (R1, re-verify). Split all 4 existing >200c blockquote groups — critically the corpus-max 5877-char block (use Transform B de-blockquote if it carries embedded structure/code, else greedy sentence/clause packing); word-set multiset diff (strip `>` prefix) to prove zero real-word loss. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/macos-lifecycle/01-psso-provisioning-walkthrough.md` (doc_id RE-205, Guide). Author `## Summary` (>=30 words — macOS PSSO provisioning walkthrough).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/macos-lifecycle/01-psso-provisioning-walkthrough.md; echo "c17=$?"; grep -c '^```mermaid' docs/macos-lifecycle/01-psso-provisioning-walkthrough.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/macos-lifecycle/01-psso-provisioning-walkthrough.md` = 0
    - C17 exits 0 (proves the 5877c block + all 4 groups now ≤200c); `grep -c 'LOCKED — 13'` >= 1; `grep -c '^doc_id: RE-205'` = 1; doc_type Guide
    - The Branch/psso diamond + both S9/S10 terminals represented; word-set diff shows zero real-word loss; stale-prose grep clean
  </acceptance_criteria>
  <done>macos-lifecycle/01 is mermaid-free, C17-green (5877c block remediated), RE-205 enrolled, psso Branch + dual terminals preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Convert + enroll macos-lifecycle/02-mdm-migration-psso.md (RE-206, Guide, LOCKED — 17, dual pipelines + 16 blockquotes)</name>
  <read_first>
    - docs/macos-lifecycle/02-mdm-migration-psso.md
    - 122-RESEARCH.md Class 3 row for macos-lifecycle/02 (linear + 1 diamond Gate, dual pipelines; 15 nodes / 2 labeled edges; confirmed dual non-reconverging pipelines B1 7 stages / B2 5 stages, no merge; 16 existing >200c BQ groups incl. a 1480-char block — heaviest burden in the corpus) + Pitfall 5
    - STATE.md [Phase 121-03] (greedy packer); 122-PATTERNS.md §2
  </read_first>
  <action>
    `git show 71be4ab:docs/macos-lifecycle/02-mdm-migration-psso.md` to capture the element set (LOCKED-N 17 = 15 nodes + 2 labeled edges; the Gate diamond; the TWO dual non-reconverging pipelines B1 7-stage and B2 5-stage — represent both pipelines distinctly, they do NOT merge). `{...}` present (Gate) -> ordinal-column decision table capturing both pipelines. Delete fence + stale prose. Annotate `LOCKED — 17 (nodes + labeled edges)` (R1, re-verify). Split ALL 16 existing >200c blockquote groups (incl. the 1480c block) — the single heaviest blockquote workload in the phase; reuse the Phase-121 greedy packer, Transform B for code/structure-bearing blocks; word-set multiset diff (strip `>` prefix) to prove zero real-word loss. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/macos-lifecycle/02-mdm-migration-psso.md` (doc_id RE-206, Guide). Author `## Summary` (>=30 words — macOS MDM migration B1 in-place / B2 wipe-and-re-enroll).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/macos-lifecycle/02-mdm-migration-psso.md; echo "c17=$?"; grep -c '^```mermaid' docs/macos-lifecycle/02-mdm-migration-psso.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/macos-lifecycle/02-mdm-migration-psso.md` = 0
    - C17 exits 0 (proves all 16 blockquote groups now ≤200c); `grep -c 'LOCKED — 17'` >= 1; `grep -c '^doc_id: RE-206'` = 1; doc_type Guide
    - Both B1 (7-stage) and B2 (5-stage) pipelines represented distinctly (no forced merge); word-set diff shows zero real-word loss; stale-prose grep clean
  </acceptance_criteria>
  <done>macos-lifecycle/02 is mermaid-free, C17-green (16 blockquotes remediated), RE-206 enrolled, dual pipelines preserved — all 9 Mermaid-bearing lifecycle files converted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-conversion -> converted table | 02's dual non-reconverging pipelines are easy to wrongly merge; 00's merge easy to drop |
| blockquote remediation -> file | macos/01's 5877c + macos/02's 16 groups are the corpus's heaviest split load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | dropping 00's S6->S8 merge, or wrongly merging 02's dual pipelines | mitigate | explicit merge/dual-pipeline rows; LOCKED-N; D-01 re-derivation in 122-14 |
| T-122-08 | Tampering | word-loss in the 5877c / 1480c / 16-group splits | mitigate | reuse Phase-121 greedy packer + Transform B; word-set multiset diff proof per file |
| T-122-05 | Repudiation | stale diagram-prose left behind | mitigate | delete in-task + stale-prose grep gate |
| T-122-SC | Tampering | package installs | accept | none — content edits + node built-in fork only |
</threat_model>

<verification>
- C17 exits 0 on all 3 files; 0 ```mermaid fences; LOCKED-N 10/13/17 present; doc_id RE-204/205/206 (Guide) injected; merges + dual pipelines preserved; 5877c/1480c/16-group blockquotes remediated with zero word loss; stale-prose grep clean
</verification>

<success_criteria>
The 3 macos-lifecycle files are mermaid-free, C17-green, enrolled; reconvergence + dual pipelines preserved; the corpus's heaviest blockquote burden fully remediated. All 9 Mermaid-bearing lifecycle files (SC5) now converted.
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-11-SUMMARY.md` when done.
</output>
