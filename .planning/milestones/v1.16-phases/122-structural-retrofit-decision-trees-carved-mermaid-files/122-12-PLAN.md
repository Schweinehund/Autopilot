---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 12
type: execute
wave: 3
depends_on: ["122-02", "122-03", "122-04", "122-05"]
files_modified: []
autonomous: true
requirements: [RETRO-05]

must_haves:
  truths:
    - "For all 11 decision-trees, an independent re-derivation of the pre-122 Mermaid element set (nodes + labeled edges) matches the converted table's coverage — no dropped leaf, node, labeled edge, or reconvergence"
    - "Each edge's CONDITION SEMANTICS (not just presence) is preserved in the converted table (semantic-paraphrase check)"
    - "No stale diagram-prose (Legend / click-the-leaf / classDef / node-shape language) survives in any of the 11 files"
  artifacts:
    - path: ".planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-12-SUMMARY.md"
      provides: "Per-file D-01 re-derivation ledger (LOCKED-N re-derived vs annotated) for all 11 decision-trees; PASS or itemized gaps"
      contains: "D-01"
  key_links: []
---

<objective>
Independent D-01 leaf-parity verification of the 11 decision-trees (all FULL tier per CONTEXT). This plan is a SEPARATE agent from the ones that converted the files — it re-derives each pre-conversion diagram's element set from the pre-122 Mermaid bytes and diffs it against the converted table. READ-ONLY: `git show` diffs + `grep` only, NO validator chain, NO checkout, NO file writes (stays inside use_worktrees:false; does not trip chain_baseline_run_kill).

Purpose: RETRO-05 — prove every decision leaf/node/labeled edge was preserved (C17 has no diagram parser; this human obligation is the only gate on silent leaf loss).
Output: a per-file re-derivation ledger in the SUMMARY. Any mismatch is logged as a gap (drives /gsd-plan-phase --gaps), NOT silently accepted.

Base commit for pre-122 bytes: `71be4ab` (`git show 71be4ab:<path>`). The fork injected only frontmatter/VH/EEE-block, never diagram content, so the base bytes are the authoritative pre-conversion diagram. Do NOT read the converters' reasoning — re-derive independently from the base bytes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive + diff decision-trees 00,01,02,08 (heaviest / reconvergence-bearing)</name>
  <read_first>
    - `git show 71be4ab:docs/decision-trees/00-initial-triage.md` (and 01, 02, 08) — the pre-122 Mermaid source
    - working-tree docs/decision-trees/{00-initial-triage,01-esp-failure,02-profile-assignment,08-android-triage}.md — the converted tables
    - 122-RESEARCH.md Class 1 table (expected node/labeled-edge/diamond/reconvergence counts per file)
    - 122-CONTEXT.md D-01 riders (semantic-paraphrase, multi-block/subgraph enum, stale-prose grep)
  </read_first>
  <action>
    For EACH of 00, 01, 02, 08: independently enumerate from the `git show 71be4ab:<path>` base bytes every node, every labeled edge (with its condition label), every diamond, and every reconvergence/merge (00's 4-way TRD4; 08's TWO 4-way merges into ANDR25 and ANDE3). Compute LOCKED-N = nodes + labeled edges. Diff against the converted working-tree table: (a) COUNT — re-derived N equals the file's `LOCKED — N` annotation and the table covers every element; (b) SEMANTIC-PARAPHRASE — each labeled edge's condition semantics is faithfully represented in a row (guard against the 8021x-triage narrowing failure class); (c) STALE-PROSE — grep the whole converted file for `mermaid|click the leaf|decision tree|diagram above|diagram below|node shape|legend|classDef` and confirm nothing diagram-referential survives. Record per-file: re-derived N, annotated N, PASS/gap, and any specific dropped/narrowed element. Do NOT edit any file — log gaps to the SUMMARY.
  </action>
  <verify>
    <automated>for f in 00-initial-triage 01-esp-failure 02-profile-assignment 08-android-triage; do echo "== $f =="; grep -Eic 'click the leaf|node shape|classDef|^## Legend' docs/decision-trees/$f.md; done</automated>
  </verify>
  <acceptance_criteria>
    - Re-derived LOCKED-N for 00=36, 01=43, 02=35, 08=38 each match the file's annotation (or the discrepancy is logged as a gap)
    - 00's 4-way TRD4 merge and 08's two 4-way merges each appear as explicit table rows
    - Stale-prose grep returns 0 on all four files
    - SUMMARY records a per-file PASS/gap line for 00,01,02,08
  </acceptance_criteria>
  <done>The 4 heaviest decision-trees are independently re-derived and diffed; results (PASS or itemized gaps) recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive + diff decision-trees 03,04,05,06,07,09,10 (remaining) + roll-up ledger</name>
  <read_first>
    - `git show 71be4ab:docs/decision-trees/{03-tpm-attestation,04-apv2-triage,05-device-lifecycle,06-macos-triage,07-ios-triage,09-linux-triage,10-8021x-triage}.md` — pre-122 Mermaid source
    - working-tree docs/decision-trees/{03,04,05,06,07,09,10}*.md — converted
    - 122-RESEARCH.md Class 1 table + Open Question 1 (file 10 LOCKED-N upgrade to 11)
  </read_first>
  <action>
    For EACH of 03, 04, 05, 06, 07, 09, 10: independently re-derive nodes + labeled edges from the git-show base bytes and diff against the converted table (count + semantic-paraphrase + stale-prose, same discipline as Task 1). Special checks: 05 (verify all 4 Q1-Q4 diamonds preserved, doc_type Reference); 06 (verify the nested MACSSO diamond is a distinct row); 10 (verify the stale `## Legend` was removed AND the annotation is the upgraded `LOCKED — 11` nodes+labeled-edges convention, NOT the old 5-leaf count). Expected re-derived N: 03=33, 04=23, 05=18, 06=31, 07=23, 09=12, 10=11. Build a roll-up ledger in the SUMMARY: one row per all 11 decision-trees (file | re-derived N | annotated N | reconvergence preserved? | stale-prose clean? | PASS/gap). Do NOT edit any file.
  </action>
  <verify>
    <automated>for f in 03-tpm-attestation 04-apv2-triage 05-device-lifecycle 06-macos-triage 07-ios-triage 09-linux-triage 10-8021x-triage; do echo "== $f =="; grep -c '^```mermaid' docs/decision-trees/$f.md; grep -Eic 'click the leaf|^## Legend' docs/decision-trees/$f.md; done</automated>
  </verify>
  <acceptance_criteria>
    - Re-derived N for 03/04/05/06/07/09/10 = 33/23/18/31/23/12/11 each match the annotation (or gap logged)
    - 05 confirmed doc_type Reference with Q1-Q4 preserved; 06's nested MACSSO diamond preserved; 10's `## Legend` removed and annotation = LOCKED — 11
    - The 11-row roll-up ledger is present in the SUMMARY with a PASS/gap verdict per file
    - `grep -c '^```mermaid'` = 0 on all 7 files
  </acceptance_criteria>
  <done>All 11 decision-trees independently verified; the roll-up ledger records PASS or itemized gaps for each.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| converted table -> reader | a silently dropped leaf/edge is invisible to C17 (no diagram parser) — this plan is the only gate |
| verification agent -> git base bytes | must diff against pre-122 bytes, not the post-fork file, to catch conversion-time loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | a decision leaf/labeled edge/reconvergence silently dropped in conversion | mitigate | independent re-derivation from git-show 71be4ab base + count/semantic/stale-prose diff; gaps logged, not accepted |
| T-122-05 | Repudiation | stale diagram-prose green under C17 | mitigate | whole-file stale-prose grep per file |
| T-122-09 | Tampering | verification agent reusing conversion reasoning (not independent) | mitigate | separate plan = separate agent; re-derive from base bytes, do not read the converter's SUMMARY reasoning |
| T-122-SC | Tampering | package installs | accept | none — read-only git-show + grep only |
</threat_model>

<verification>
- Per-file re-derived LOCKED-N matches annotation for all 11 decision-trees (or gaps logged)
- All reconvergence merges preserved as explicit rows; stale-prose grep clean on all 11; file 10 Legend removed
- 11-row roll-up ledger present in SUMMARY
</verification>

<success_criteria>
All 11 decision-trees independently pass D-01 leaf-parity re-derivation, or every discrepancy is itemized as a gap for --gaps closure. No file edited (files_modified: []).
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-12-SUMMARY.md` with the 11-row D-01 ledger when done.
</output>
