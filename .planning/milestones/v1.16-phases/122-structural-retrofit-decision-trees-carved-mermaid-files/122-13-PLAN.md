---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 13
type: execute
wave: 3
depends_on: ["122-06", "122-07", "122-08"]
files_modified: []
autonomous: true
requirements: [RETRO-08]

must_haves:
  truths:
    - "For all 10 carved-mermaid files, an independent re-derivation of the pre-122 diagram matches the converted output with no dropped node, labeled edge, CHOOSE outcome, fan-in/fan-out, or sequence message"
    - "The 4 FULL-tier carve-outs get full adversarial re-derivation; the 6 LIGHT get a mechanical count-and-label diff"
    - "No stale diagram-prose survives in any of the 10 files"
  artifacts:
    - path: ".planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-13-SUMMARY.md"
      provides: "Per-file D-01 re-derivation ledger for all 10 carved-mermaid files; PASS or itemized gaps"
      contains: "D-01"
  key_links: []
---

<objective>
Independent D-01 leaf-parity verification of the 10 carved-mermaid files (RETRO-08). Separate agent, READ-ONLY (`git show` + `grep`, no validator chain, no checkout, no writes). Tier per RESEARCH: FULL for android/00, apv1/01, ios/00, macos/00 (RULING R3); LIGHT (mechanical count-and-label) for 8021x/00, 8021x/01, apv1/00, apv2/00, linux/00, ca-enrollment-timing.

Purpose: RETRO-08 — prove every node/labeled edge/CHOOSE outcome/sequence message preserved through conversion.
Output: a per-file re-derivation ledger in the SUMMARY; mismatches logged as gaps for --gaps closure.

Base commit: `71be4ab` (`git show 71be4ab:<path>`). Re-derive independently from the base bytes; do not read the converters' reasoning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FULL re-derivation — android/00, apv1/01, ios/00, macos/00</name>
  <read_first>
    - `git show 71be4ab:docs/admin-setup-android/00-overview.md` (and admin-setup-apv1/01-hardware-hash-upload, admin-setup-ios/00-overview, admin-setup-macos/00-overview) — pre-122 Mermaid source
    - the corresponding converted working-tree files
    - 122-RESEARCH.md Class 2 table (android/00 N=16 CHOOSE 6-outcome; apv1/01 N=10 diamonds OEM+BATCH; ios/00 N=18 CHOOSE 4-outcome; macos/00 N=11 reconvergence C/D/E->F)
    - 122-CONTEXT.md D-01 riders (semantic-paraphrase, multi-block/subgraph enum, stale-prose grep)
  </read_first>
  <action>
    For EACH FULL file: independently enumerate nodes + labeled edges + diamonds + CHOOSE outcomes + fan-in from the `git show 71be4ab:<path>` base bytes; compute LOCKED-N; diff against the converted table/list for count + semantic-paraphrase + stale-prose. Specific checks: android/00 (all 6 CHOOSE outcomes as rows); ios/00 (all 4 CHOOSE outcomes); apv1/01 (both OEM and BATCH diamonds present); macos/00 (the C/D/E->F reconvergence explicitly preserved as a merge note). Record per-file: re-derived N vs annotated N, PASS/gap, any dropped/narrowed element. Do NOT edit any file — log gaps to the SUMMARY.
  </action>
  <verify>
    <automated>for f in admin-setup-android/00-overview admin-setup-apv1/01-hardware-hash-upload admin-setup-ios/00-overview admin-setup-macos/00-overview; do echo "== $f =="; grep -c '^```mermaid' docs/$f.md; grep -Eic 'click the leaf|node shape|classDef|^## Legend' docs/$f.md; done</automated>
  </verify>
  <acceptance_criteria>
    - Re-derived LOCKED-N: android/00=16, apv1/01=10, ios/00=18, macos/00=11 each match annotation (or gap logged)
    - All 6 (android) and 4 (ios) CHOOSE outcomes present as rows; both apv1/01 diamonds present; macos/00 C/D/E->F merge preserved
    - `grep -c '^```mermaid'` = 0 and stale-prose grep = 0 on all four
    - SUMMARY records a per-file PASS/gap line for the 4 FULL files
  </acceptance_criteria>
  <done>The 4 FULL carve-outs independently re-derived and diffed; results recorded.</done>
</task>

<task type="auto">
  <name>Task 2: LIGHT count-and-label diff — 8021x/00, 8021x/01, apv1/00, apv2/00, linux/00, ca-enrollment-timing + roll-up</name>
  <read_first>
    - `git show 71be4ab:<path>` for the 6 LIGHT files (admin-setup-8021x/00-overview, admin-setup-8021x/01-eap-method-overview, admin-setup-apv1/00-overview, admin-setup-apv2/00-overview, admin-setup-linux/00-overview, reference/ca-enrollment-timing)
    - the corresponding converted working-tree files
    - 122-RESEARCH.md Class 2 table (8021x/00 N=3; 8021x/01 10 messages; apv1/00 N=8; apv2/00 N=4; linux/00 N=5 fan-out; ca-enrollment-timing 8 messages)
  </read_first>
  <action>
    For EACH LIGHT file: mechanical count-and-label diff from the git-show base bytes vs the converted numbered list / step list — confirm every node / fan-out child / sequence message is present and correctly labeled, and stale-prose grep is clean. Sequence-diagram files (8021x/01 = 10 messages; ca-enrollment-timing = 8 messages) must have all messages present as ordered steps. linux/00 (RE-128) must preserve the B->C/D/E fan-out; ca-enrollment-timing (RE-147) must be doc_type Reference. Build a roll-up ledger in the SUMMARY covering ALL 10 carved-mermaid files (Task 1 FULL + this task's LIGHT): file | re-derived N/messages | annotated | element-preservation PASS/gap | stale-prose clean?. Do NOT edit any file.
  </action>
  <verify>
    <automated>for f in admin-setup-8021x/00-overview admin-setup-8021x/01-eap-method-overview admin-setup-apv1/00-overview admin-setup-apv2/00-overview admin-setup-linux/00-overview reference/ca-enrollment-timing; do echo "== $f =="; grep -c '^```mermaid' docs/$f.md; grep -Eic 'click the leaf|^## Legend' docs/$f.md; done</automated>
  </verify>
  <acceptance_criteria>
    - Re-derived counts: 8021x/00=3, 8021x/01=10 messages, apv1/00=8, apv2/00=4, linux/00=5, ca-enrollment-timing=8 messages each match annotation (or gap logged)
    - linux/00 fan-out B->C/D/E preserved; ca-enrollment-timing confirmed doc_type Reference; both sequence files have all messages as ordered steps
    - `grep -c '^```mermaid'` = 0 and stale-prose grep = 0 on all six
    - A 10-row roll-up ledger (4 FULL + 6 LIGHT) is present in the SUMMARY with a PASS/gap verdict per file
  </acceptance_criteria>
  <done>All 10 carved-mermaid files independently verified; the roll-up ledger records PASS or itemized gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| converted output -> reader | a dropped CHOOSE outcome / fan-out child / sequence message is invisible to C17 |
| verification agent -> git base bytes | must diff against pre-122 bytes to catch conversion-time loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | a CHOOSE outcome, fan-in/fan-out, or sequence message silently dropped | mitigate | independent re-derivation from git-show 71be4ab base + count/semantic/stale-prose diff; gaps logged |
| T-122-05 | Repudiation | stale diagram-prose green under C17 | mitigate | whole-file stale-prose grep per file |
| T-122-07 | Tampering | ca-enrollment-timing verified as Guide instead of Reference | mitigate | doc_type Reference explicitly checked in Task 2 |
| T-122-SC | Tampering | package installs | accept | none — read-only git-show + grep only |
</threat_model>

<verification>
- Per-file re-derived counts match annotations for all 10 carved files (or gaps logged)
- CHOOSE outcomes / fan-outs / sequence messages preserved; stale-prose grep clean on all 10
- 10-row roll-up ledger present in SUMMARY
</verification>

<success_criteria>
All 10 carved-mermaid files independently pass D-01 leaf-parity re-derivation, or every discrepancy is itemized as a gap. No file edited (files_modified: []).
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-13-SUMMARY.md` with the 10-row D-01 ledger when done.
</output>
