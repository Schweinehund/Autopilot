---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 14
type: execute
wave: 3
depends_on: ["122-09", "122-10", "122-11"]
files_modified: []
autonomous: true
requirements: [RETRO-07]

must_haves:
  truths:
    - "For all 9 Mermaid-bearing lifecycle files, an independent re-derivation of the pre-122 diagram(s) matches the converted output — no dropped node, labeled edge, multi-block, subgraph partition, reconvergence, or dual-pipeline"
    - "Multi-block files (lifecycle/00, lifecycle-apv2/02) are verified PER BLOCK; the 5 dashed F_* failure edges and the Device/User subgraph partition are confirmed present"
    - "No stale diagram-prose survives in any of the 9 files"
  artifacts:
    - path: ".planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-14-SUMMARY.md"
      provides: "Per-file D-01 re-derivation ledger for all 9 lifecycle files; PASS or itemized gaps"
      contains: "D-01"
  key_links: []
---

<objective>
Independent D-01 leaf-parity verification of the 9 Mermaid-bearing lifecycle files (RETRO-07 remainder). Separate agent, READ-ONLY (`git show` + `grep`, no validator chain, no checkout, no writes). Tier: FULL for the 7 multi-block/subgraph/reconvergence/dual-pipeline files; LIGHT for ios-lifecycle/01 and ios-lifecycle/02 (simple linear diagrams — but confirm the heavy blockquote remediation did not drop diagram content).

Purpose: RETRO-07 — prove every node/labeled edge/block/partition/merge preserved through conversion, the final gate before RETRO-07 can close.
Output: a per-file re-derivation ledger in the SUMMARY; mismatches logged as gaps.

Base commit: `71be4ab` (`git show 71be4ab:<path>`). Re-derive independently from the base bytes; enumerate EVERY ```mermaid block per file (multi-block files have 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FULL re-derivation — lifecycle/00, lifecycle/03, lifecycle/04, lifecycle-apv2/02 (multi-block / subgraph / merge)</name>
  <read_first>
    - `git show 71be4ab:docs/lifecycle/00-overview.md` (and lifecycle/03-oobe, lifecycle/04-esp, lifecycle-apv2/02-deployment-flow) — pre-122 Mermaid source
    - the corresponding converted working-tree files
    - 122-RESEARCH.md Class 3 table (lifecycle/00 2 blocks; lifecycle/03 N=20 3-way ESP4 merge; lifecycle/04 N=8 Device/User subgraphs + D4->U1; lifecycle-apv2/02 2 blocks, B2 = 5 dashed F_* edges)
    - 122-CONTEXT.md D-02 Amendment 1/2a/2b + D-01 riders
  </read_first>
  <action>
    For EACH file: enumerate EVERY ```mermaid block from the git-show base bytes (lifecycle/00 and lifecycle-apv2/02 have TWO — verify per block, Amendment 1). Specific checks: lifecycle/00 (block 1 decision table with RESEAL->S3a + S3a+S3c->S4; block 2 = Stage|Failure table); lifecycle/03 (the AADUD/UDOOBE/AADSD 3-way merge into ESP4 preserved as rows, N=20); lifecycle/04 (BOTH Device Phase / User Phase subgraph partitions preserved as headed sub-lists + the D4->U1 cross-subgraph transition note, N=8); lifecycle-apv2/02 (block 2's ALL 5 dashed edges F_REG->S3, F_IME->S4, F_APP1->S7, F_SCRIPT->S8, F_APP2->S9 present in the Stage|Failure table). Count + semantic-paraphrase + stale-prose diff per block. Record per-file/per-block PASS/gap. Do NOT edit any file.
  </action>
  <verify>
    <automated>for f in lifecycle/00-overview lifecycle/03-oobe lifecycle/04-esp lifecycle-apv2/02-deployment-flow; do echo "== $f =="; grep -c '^```mermaid' docs/$f.md; grep -Eic 'click the leaf|node shape|classDef|^## Legend' docs/$f.md; done</automated>
  </verify>
  <acceptance_criteria>
    - lifecycle/00 and lifecycle-apv2/02 each verified per block (2 blocks each), `grep -c '^```mermaid'` = 0 on both
    - lifecycle/03's 3-way ESP4 merge (N=20) and lifecycle/04's Device/User partition + D4->U1 (N=8) confirmed preserved
    - lifecycle-apv2/02 block 2 contains all 5 F_* failure rows
    - stale-prose grep = 0 on all four; SUMMARY records per-file/per-block PASS/gap
  </acceptance_criteria>
  <done>The 4 FULL multi-block/subgraph lifecycle files independently re-derived per block; results recorded.</done>
</task>

<task type="auto">
  <name>Task 2: FULL macos-lifecycle {00,01,02} + LIGHT ios-lifecycle {01,02} + roll-up</name>
  <read_first>
    - `git show 71be4ab:docs/macos-lifecycle/00-ade-lifecycle.md` (and macos-lifecycle/01-psso-provisioning-walkthrough, macos-lifecycle/02-mdm-migration-psso, ios-lifecycle/01-ade-lifecycle, ios-lifecycle/02-mdm-migration) — pre-122 Mermaid source
    - the corresponding converted working-tree files
    - 122-RESEARCH.md Class 3 table (macos/00 N=10 S6+merge; macos/01 N=13 psso Branch + dual terminals S9/S10; macos/02 N=17 dual non-reconverging pipelines; ios/01 N=7; ios/02 N=7 + 10 blockquote groups)
  </read_first>
  <action>
    FULL for macos-lifecycle 00/01/02: re-derive nodes + labeled edges + diamond + merge/dual-pipeline from the git-show base bytes; diff against the converted table (count + semantic-paraphrase + stale-prose). Specific checks: macos/00 (S7->S8 / S6->S8 merge preserved, N=10); macos/01 (Branch/psso diamond + BOTH S9/S10 non-converging terminals distinct, N=13); macos/02 (BOTH B1 7-stage + B2 5-stage dual pipelines distinct, NO forced merge, N=17). LIGHT for ios-lifecycle 01/02: mechanical count-and-label (N=7 each) — AND confirm ios/02's simple 7-node diagram survived intact despite its 10 blockquote splits (the blockquote work must not have touched diagram content). Build a roll-up ledger in the SUMMARY covering ALL 9 lifecycle files (Task 1 + this task): file | block count | re-derived N | annotated N | partition/merge/dual-pipeline preserved? | stale-prose clean? | PASS/gap. Do NOT edit any file.
  </action>
  <verify>
    <automated>for f in macos-lifecycle/00-ade-lifecycle macos-lifecycle/01-psso-provisioning-walkthrough macos-lifecycle/02-mdm-migration-psso ios-lifecycle/01-ade-lifecycle ios-lifecycle/02-mdm-migration; do echo "== $f =="; grep -c '^```mermaid' docs/$f.md; grep -Eic 'click the leaf|^## Legend' docs/$f.md; done</automated>
  </verify>
  <acceptance_criteria>
    - Re-derived N: macos/00=10, macos/01=13, macos/02=17, ios/01=7, ios/02=7 each match annotation (or gap logged)
    - macos/00 merge preserved; macos/01 dual terminals S9/S10 distinct; macos/02 dual pipelines B1/B2 distinct (no forced merge)
    - `grep -c '^```mermaid'` = 0 and stale-prose grep = 0 on all five
    - A 9-row roll-up ledger is present in the SUMMARY with a PASS/gap verdict per file
  </acceptance_criteria>
  <done>All 9 Mermaid-bearing lifecycle files independently verified; the roll-up ledger records PASS or itemized gaps — RETRO-07 leaf-parity gate met (pending close plan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| converted output -> reader | a dropped block/partition/merge/dual-pipeline is invisible to C17 |
| blockquote remediation -> diagram content | ios/02's 10 splits + macos/01's 5877c split could have touched adjacent diagram content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | dropping lifecycle/00 block 2, the ESP4 merge, the Device/User partition, or wrongly merging macos/02's dual pipelines | mitigate | per-block re-derivation from git-show 71be4ab base + count/semantic/stale-prose diff; gaps logged |
| T-122-08 | Tampering | blockquote splits (ios/02, macos/01, macos/02) collaterally altering diagram content | mitigate | re-derive diagram element set independently of the blockquote work |
| T-122-05 | Repudiation | stale diagram-prose green under C17 | mitigate | whole-file stale-prose grep per file |
| T-122-SC | Tampering | package installs | accept | none — read-only git-show + grep only |
</threat_model>

<verification>
- Per-file re-derived N matches annotation for all 9 lifecycle files (or gaps logged)
- Multi-block/subgraph/merge/dual-pipeline preserved; stale-prose grep clean on all 9
- 9-row roll-up ledger present in SUMMARY
</verification>

<success_criteria>
All 9 Mermaid-bearing lifecycle files independently pass D-01 leaf-parity re-derivation, or every discrepancy is itemized as a gap. No file edited (files_modified: []).
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-14-SUMMARY.md` with the 9-row D-01 ledger when done.
</output>
