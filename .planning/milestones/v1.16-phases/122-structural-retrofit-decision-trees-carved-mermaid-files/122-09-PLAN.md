---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 09
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - docs/lifecycle/00-overview.md
  - docs/lifecycle/03-oobe.md
  - docs/lifecycle/04-esp.md
autonomous: true
requirements: [RETRO-07]

must_haves:
  truths:
    - "lifecycle/00 is classified PER BLOCK: block 1 (decision graph) -> decision table, block 2 (failure-annotation map) -> Stage | Failure Mode table"
    - "lifecycle/03's 3-way merge into ESP4 is preserved; lifecycle/04's Device Phase / User Phase subgraph partition + D4->U1 cross-edge preserved as headed sub-lists"
    - "Each carries the EEE header block + doc_id (Guide) + ## Summary-first; C17 exits 0"
  artifacts:
    - path: "docs/lifecycle/00-overview.md"
      provides: "Two converted blocks (decision table + Stage|Failure table); LOCKED-N per block"
      contains: "Failure Mode"
    - path: "docs/lifecycle/03-oobe.md"
      provides: "Decision table (LOCKED — 20) with the AADUD/UDOOBE/AADSD 3-way merge into ESP4"
      contains: "LOCKED — 20"
    - path: "docs/lifecycle/04-esp.md"
      provides: "Device Phase / User Phase headed sub-lists (LOCKED — 8) + D4->U1 transition note"
      contains: "LOCKED — 8"
  key_links:
    - from: "docs/lifecycle/00-overview.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-192 (Guide) injected by fork; flips Pending->Approved in 122-15"
      pattern: "doc_id:\\s*RE-192"
---

<objective>
Convert the 3 core `docs/lifecycle/` Mermaid-bearing files (RETRO-07 remainder). These carry the multi-block + subgraph + reconvergence structures CONTEXT cites as the highest silent-loss risk: lifecycle/00 has 2 heterogeneous blocks (Amendment 1 — classify per block), lifecycle/03 has a 3-way merge into ESP4, lifecycle/04 is CONTEXT's own cited triage-undercount example (2 subgraphs, D4->U1 cross-edge).

Purpose: RETRO-07 remainder — 3 of the 9 Mermaid-bearing lifecycle files; RETRO-07 closes only when all 22 lifecycle files are green.
Output: 3 enrolled, C17-green files (RE-192, RE-195, RE-196; Guide via existing GUIDE_DIRS).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
@docs/_standards/EEE-SOP-standard.md
@docs/decision-trees/10-8021x-triage.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert + enroll lifecycle/00-overview.md (RE-192, Guide, 2 blocks — classify per block)</name>
  <read_first>
    - docs/lifecycle/00-overview.md
    - 122-RESEARCH.md Class 3 row for lifecycle/00 (2 blocks: B1 decision graph 9 nodes/3 labeled, 1 diamond M, reconvergence RESEAL->S3a and S3a+S3c->S4; B2 failure-annotation map 10 nodes/5 dashed; 1 BQ)
    - 122-CONTEXT.md D-02 Amendment 1 (classify PER BLOCK — non-negotiable) + Amendment 2a (failure map -> Stage|Failure table)
    - 122-PATTERNS.md §2 (decision-table shape) + Pattern 2 (failure-annotation map -> Stage|Failure table) in RESEARCH.md lines 226-247
  </read_first>
  <action>
    `git show 71be4ab:docs/lifecycle/00-overview.md` to capture BOTH ```mermaid blocks (enumerate each separately — Amendment 1). BLOCK 1 (decision graph, has `{M}` diamond) -> ordinal-column decision table; preserve the RESEAL->S3a rejoin and the S3a+S3c->S4 reconvergence as explicit rows (LOCKED-N B1 = 12 = 9 nodes + 3 labeled edges). BLOCK 2 (failure-annotation map, 5 dashed stage->failure edges) -> a `Stage | Failure Mode` 2-column table (reuse RESEARCH.md's before/after shape). Delete BOTH fences + any stale prose. Annotate each block with its own `LOCKED — N (nodes + labeled edges)` line (re-verify). Split the 1 existing >200c blockquote. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/lifecycle/00-overview.md` (doc_id RE-192, Guide). Author `## Summary` (>=30 words — Windows Autopilot lifecycle overview).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/lifecycle/00-overview.md; echo "c17=$?"; grep -c '^```mermaid' docs/lifecycle/00-overview.md; grep -c 'Failure Mode' docs/lifecycle/00-overview.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/lifecycle/00-overview.md` = 0 (BOTH blocks removed)
    - C17 exits 0; `grep -c '^doc_id: RE-192'` = 1; doc_type Guide
    - Block 1 is a decision table with RESEAL->S3a and S3a+S3c->S4 preserved; Block 2 is a `Stage | Failure Mode` table; both carry a LOCKED-N annotation
    - Stale-prose grep clean
  </acceptance_criteria>
  <done>lifecycle/00-overview.md's 2 blocks are converted per-block, mermaid-free, C17-green, RE-192 enrolled.</done>
</task>

<task type="auto">
  <name>Task 2: Convert + enroll lifecycle/03-oobe.md (RE-195, Guide, LOCKED — 20, 3-way ESP4 merge)</name>
  <read_first>
    - docs/lifecycle/03-oobe.md
    - 122-RESEARCH.md Class 3 row for lifecycle/03 (decision graph flowchart LR, 15 nodes / 5 labeled edges / 2 diamonds M, PPRESULT; 3-way merge into ESP4 — AADUD, UDOOBE, AADSD all converge; 4 BQ incl a 1054-char L2 Note block)
    - 122-PATTERNS.md §2; docs/decision-trees/10-8021x-triage.md
  </read_first>
  <action>
    `git show 71be4ab:docs/lifecycle/03-oobe.md` to capture the element set (LOCKED-N 20 = 15 nodes + 5 labeled edges; 2 diamonds; the 3-way merge AADUD/UDOOBE/AADSD -> ESP4, including the PP-path resealing into ESP). `{...}` present -> ordinal-column decision table; preserve the 3-way ESP4 merge and the PP-path rejoin as explicit rows. Delete fence + stale prose. Annotate `LOCKED — 20 (nodes + labeled edges)` (R1, re-verify). Split all 4 existing >200c blockquote groups (incl. the 1054c L2 Note — reuse the Phase-121 greedy packer; Transform B de-blockquote if it carries embedded structure). Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/lifecycle/03-oobe.md` (doc_id RE-195, Guide). Author `## Summary` (>=30 words — OOBE / sign-in flow).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/lifecycle/03-oobe.md; echo "c17=$?"; grep -c '^```mermaid' docs/lifecycle/03-oobe.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/lifecycle/03-oobe.md` = 0
    - C17 exits 0 (all 4 blockquotes ≤200c); `grep -c 'LOCKED — 20'` >= 1; `grep -c '^doc_id: RE-195'` = 1; doc_type Guide
    - The AADUD/UDOOBE/AADSD 3-way merge into ESP4 preserved as explicit rows; stale-prose grep clean
  </acceptance_criteria>
  <done>lifecycle/03-oobe.md is mermaid-free, C17-green, RE-195 enrolled, 3-way ESP4 merge preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Convert + enroll lifecycle/04-esp.md (RE-196, Guide, LOCKED — 8, subgraph partition)</name>
  <read_first>
    - docs/lifecycle/04-esp.md
    - 122-RESEARCH.md Class 3 row for lifecycle/04 (subgraph-partitioned flow, 8 nodes / 7 plain edges / 0 diamonds; 2 subgraphs Device Phase / User Phase + cross-subgraph edge D4->U1; 3 BQ incl a 1317-char L2 Note) — CONTEXT's own cited triage-undercount example
    - 122-CONTEXT.md D-02 Amendment 2b (subgraph-partitioned -> grouped/headed sub-lists preserving the partition label) + Pattern 3 in RESEARCH.md lines 249-262
  </read_first>
  <action>
    `git show 71be4ab:docs/lifecycle/04-esp.md` to capture the element set (LOCKED-N 8 = 8 nodes + 0 labeled edges; 2 subgraphs Device Phase / User Phase; the D4->U1 cross-subgraph edge). NO diamond, but subgraph-partitioned -> TWO headed numbered sub-lists ("Device Phase" / "User Phase") preserving the partition labels as load-bearing (device-before-user-login), with an explicit transition note between them ("Device phase completes before user phase begins — D4 -> U1"). Do NOT flatten into one list (erases the partition — the exact D-04 loss). Delete fence + stale prose. Annotate `LOCKED — 8 (nodes + labeled edges)` (R1, re-verify — the D-01 verifier also checks all 7 plain edges + the cross-subgraph edge). Split all 3 existing >200c blockquote groups (incl. the 1317c L2 Note). Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/lifecycle/04-esp.md` (doc_id RE-196, Guide). Author `## Summary` (>=30 words — Enrollment Status Page phases).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/lifecycle/04-esp.md; echo "c17=$?"; grep -c '^```mermaid' docs/lifecycle/04-esp.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/lifecycle/04-esp.md` = 0
    - C17 exits 0 (all 3 blockquotes ≤200c); `grep -c 'LOCKED — 8'` >= 1; `grep -c '^doc_id: RE-196'` = 1; doc_type Guide
    - Two headed sub-lists (Device Phase / User Phase) present + explicit D4->U1 transition note; stale-prose grep clean
  </acceptance_criteria>
  <done>lifecycle/04-esp.md is mermaid-free, C17-green, RE-196 enrolled, subgraph partition + cross-edge preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-conversion -> converted table/list | multi-block, 3-way merge, and subgraph partition are the exact CONTEXT-cited loss surfaces |
| blockquote remediation -> file | large L2 Note blocks (1054c, 1317c) risk word drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | dropping lifecycle/00 block 2, the ESP4 3-way merge, or the Device/User partition | mitigate | per-block classification; explicit merge rows; headed sub-lists + transition note; D-01 re-derivation in 122-14 |
| T-122-08 | Tampering | word-loss in the 1054c/1317c blockquote splits | mitigate | reuse Phase-121 greedy packer; word-set multiset diff proof |
| T-122-05 | Repudiation | stale diagram-prose left behind | mitigate | delete in-task + stale-prose grep gate |
| T-122-SC | Tampering | package installs | accept | none — content edits + node built-in fork only |
</threat_model>

<verification>
- C17 exits 0 on all 3 files; 0 ```mermaid fences (incl. lifecycle/00's 2 blocks); per-block LOCKED-N present; doc_id RE-192/195/196 (Guide) injected; ESP4 merge + Device/User partition preserved; stale-prose grep clean
</verification>

<success_criteria>
The 3 core lifecycle files are mermaid-free, C17-green, enrolled; per-block classification, the ESP4 3-way merge, and the subgraph partition preserved for D-01 verification.
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-09-SUMMARY.md` when done.
</output>
