---
phase: 29-ios-admin-setup-byod-mam
plan: 03
type: execute
wave: 2
depends_on: [29-01, 29-02]
files_modified:
  - docs/admin-setup-ios/07-device-enrollment.md
autonomous: true
requirements: [ABYOD-01]

must_haves:
  truths:
    - "An admin can answer `can I do X on a BYOD device?` in under 30 seconds by scanning the Capabilities Available Without Supervision table"
    - "Both Company Portal enrollment and web-based enrollment flows are documented"
    - "Personal vs Corporate ownership-flag behavior is explained with consequence for wipe/retire"
    - "NO privacy-limit callouts (blockquote with `**Privacy limit:**`) appear — that pattern is ABYOD-02-scoped"
    - "Configuration-Caused Failures table uses the Phase 30 runbook placeholder text"
    - "Frontmatter uses standard schema (last_verified 2026-04-17, review_by 2026-07-16, applies_to: device-enrollment, audience: admin, platform: iOS)"
  artifacts:
    - path: "docs/admin-setup-ios/07-device-enrollment.md"
      provides: "ABYOD-01 Device Enrollment guide — Company Portal + web-based flows, capabilities-without-supervision table, ownership-flag section"
      contains: "Capabilities Available Without Supervision"
      min_lines: 280
  key_links:
    - from: "docs/admin-setup-ios/07-device-enrollment.md"
      to: "docs/admin-setup-ios/00-overview.md#intune-enrollment-restrictions"
      via: "cross-link in body (avoid duplication per D-15)"
      pattern: "00-overview.md#intune-enrollment-restrictions"
    - from: "docs/admin-setup-ios/07-device-enrollment.md"
      to: "docs/admin-setup-ios/03-ade-enrollment-profile.md"
      via: "contrast-prose for ADE-only capabilities per D-04"
      pattern: "03-ade-enrollment-profile.md"
---

<objective>
Create `docs/admin-setup-ios/07-device-enrollment.md` — the ABYOD-01 Device Enrollment admin guide covering iOS/iPadOS Device Enrollment (non-ABM) via Company Portal and web-based flows. The guide's defining feature is a capability-level "Capabilities Available Without Supervision" table placed at the top of the guide (before setup steps) that answers the scannability question in Success Criterion #1. Content coverage: both enrollment flows with distinct prereqs, personal-vs-corporate ownership-flag mechanics, and cross-links to the overview's shared Intune Enrollment Restrictions section (no duplication).

Purpose: Delivers ABYOD-01. Implements D-12 (both CP + web-based flows), D-13 (concept/outcome over click-paths), D-14 (capabilities table at top), D-15 (cross-link to overview restrictions section, no duplication), D-16 (ownership-flag section), D-17 (no profile-UE content — belongs to ABYOD-02), D-18 (NO privacy-limit callouts).

Output: A new 300-500 line guide file at `docs/admin-setup-ios/07-device-enrollment.md` following the iOS admin template structure, with the comparison-table/per-type-section pattern from `05-app-deployment.md` as the structural model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-ios-admin-setup-byod-mam/29-CONTEXT.md
@.planning/phases/29-ios-admin-setup-byod-mam/29-PATTERNS.md
@.planning/phases/29-ios-admin-setup-byod-mam/29-RESEARCH.md
@.planning/phases/29-ios-admin-setup-byod-mam/29-VALIDATION.md

<interfaces>
<!-- Structural analogs (READ these before writing) -->

Primary analog: `docs/admin-setup-ios/05-app-deployment.md` — same multi-path comparison-table + per-type-section structure ABYOD-01 needs.
Secondary analog: `docs/admin-setup-ios/03-ade-enrollment-profile.md` — Key Concepts Before You Begin, What Breaks callouts, supervised-only callout usage (for contrast understanding, NOT reuse — D-18).
Template: `docs/_templates/admin-template-ios.md` — section structure (Prerequisites → Key Concepts → Steps → What Breaks → Verification → Configuration-Caused Failures → See Also).

Frontmatter target (29-PATTERNS.md lines 32-42):

```yaml
---
last_verified: 2026-04-17
review_by: 2026-07-16
applies_to: device-enrollment
audience: admin
platform: iOS
---
```

Platform-gate banner (29-PATTERNS.md lines 48-53) adapted for non-ADE scope:

```markdown
> **Platform gate:** This guide covers iOS/iPadOS Device Enrollment via Intune (no ABM required).
> For corporate ADE setup, see [iOS/iPadOS Admin Setup Overview](00-overview.md).
> For iOS/iPadOS enrollment terminology, see the [Apple Provisioning Glossary](../_glossary-macos.md).
> Portal navigation may vary by Intune admin center version. See [Overview](00-overview.md#portal-navigation-note) for details.
```

Capabilities table — content source: 29-RESEARCH.md "Capabilities Available Without Supervision" section (planner discretion on which capabilities to surface; minimum coverage must include MDM policies, configuration profiles, compliance evaluation, app deployment modes, plus what is NOT available: silent install, supervised-only restrictions, OS update enforcement).

Phase 30 runbook placeholder (verbatim literal string): `iOS L1 runbooks (Phase 30)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold `07-device-enrollment.md` with frontmatter, Platform gate, title, opening paragraph, Capabilities Available Without Supervision table, and Key Concepts section</name>
  <files>docs/admin-setup-ios/07-device-enrollment.md</files>
  <read_first>
    - docs/_templates/admin-template-ios.md (template structure for iOS admin guides)
    - docs/admin-setup-ios/05-app-deployment.md (primary structural analog — multi-path comparison table + per-type sections; pay attention to lines 1-64 for frontmatter + opening + comparison-table pattern)
    - docs/admin-setup-ios/03-ade-enrollment-profile.md (Key Concepts Before You Begin pattern, lines 24-47)
    - docs/admin-setup-ios/00-overview.md (post-Plan-02 state — confirm `#intune-enrollment-restrictions` anchor exists for cross-link target)
    - .planning/phases/29-ios-admin-setup-byod-mam/29-PATTERNS.md lines 19-143 (ABYOD-01 pattern assignments, frontmatter/banner/comparison-table/per-flow-section/phase-30-placeholder/contrast-prose/see-also/version-history)
    - .planning/phases/29-ios-admin-setup-byod-mam/29-CONTEXT.md D-12/D-13/D-14/D-15/D-16/D-17/D-18
    - .planning/phases/29-ios-admin-setup-byod-mam/29-RESEARCH.md §"Capabilities Available Without Supervision" (for capability-list content); §"ABYOD-01 Device Enrollment" (for both-flows coverage)
    - .planning/phases/29-ios-admin-setup-byod-mam/29-VALIDATION.md rows 29-02-01 through 29-02-06
  </read_first>
  <action>
Implements D-14 (capabilities table at TOP before setup steps), D-15 (cross-link, not duplicate, restrictions section).

Create a new file `docs/admin-setup-ios/07-device-enrollment.md` starting with:

**Frontmatter** (verbatim — date values are fixed by Phase 29 convention):

```yaml
---
last_verified: 2026-04-17
review_by: 2026-07-16
applies_to: device-enrollment
audience: admin
platform: iOS
---
```

**Platform-gate banner** (verbatim):

```markdown
> **Platform gate:** This guide covers iOS/iPadOS Device Enrollment via Intune (no ABM required).
> For corporate ADE setup, see [iOS/iPadOS Admin Setup Overview](00-overview.md).
> For iOS/iPadOS enrollment terminology, see the [Apple Provisioning Glossary](../_glossary-macos.md).
> Portal navigation may vary by Intune admin center version. See [Overview](00-overview.md#portal-navigation-note) for details.
```

**Title**:

```markdown
# iOS/iPadOS Device Enrollment
```

**Opening paragraph** (2-3 sentences framing the guide scope):

```markdown
Device Enrollment is Intune's non-ABM enrollment path for iOS/iPadOS devices. Users initiate enrollment themselves — either by downloading the Intune Company Portal app or by visiting a web-based enrollment URL — and the resulting enrolled device runs in unsupervised mode with the full Intune MDM management capability set minus supervised-only features. Device Enrollment covers both personal-owned BYOD devices and corporate-owned devices that did not come through Apple Business Manager.
```

**Capabilities Available Without Supervision section (D-14 — CRITICAL, this must appear BEFORE any setup steps)**:

```markdown
## Capabilities Available Without Supervision

Use this table to answer "can I do X on a Device-Enrollment-enrolled iOS/iPadOS device?" in under 30 seconds. This guide's enrolled devices are unsupervised; capabilities marked **Yes** are available, **No** requires corporate ADE (see [ADE Enrollment Profile](03-ade-enrollment-profile.md)).

| Capability | Available on Device Enrollment (unsupervised)? | Notes |
|-----------|:---:|------|
| MDM configuration profiles (Wi-Fi, VPN, email, certificates, general device restrictions) | Yes | Settings not flagged supervised-only apply on Device Enrollment enrolled devices. |
| Compliance evaluation (OS version, passcode, jailbreak, account protection) | Yes | All compliance policies evaluate on unsupervised enrollment. |
| VPP user-licensed app deployment | Yes | User accepts app assignment via Apple Account prompt. |
| VPP device-licensed app deployment | Yes | No Apple Account prompt; app installs with user consent dialog. |
| LOB (.ipa) app deployment | Yes | Enterprise-signed apps deploy on supervised and unsupervised devices; user consent dialog required. |
| Store apps (without VPP) | Yes | Requires end-user Apple Account. |
| Silent app installation (no user consent) | **No** | ADE-only capability — see [ADE Enrollment Profile](03-ade-enrollment-profile.md). |
| OS update enforcement (forced install deadlines via DDM or device restrictions) | **No** | Supervised-only in iOS 17+ enforcement policies. |
| Supervised-only restrictions (block App Store, disable iCloud Backup, disable Find My, home-screen layout enforcement, etc.) | **No** | Category-level supervised-only settings do not apply. |
| Activation Lock bypass | **No** | Requires supervised mode. |
| Lost Mode | **No** | Supervised-only MDM command. |
| Remote device wipe (MDM RemoteWipe) | Yes | Full-device wipe supported on corporate-owned devices marked Corporate in Intune; Personal-owned devices receive retire/selective-wipe scope only. |
| Remote device lock (passcode clear or device lock) | Yes | Unsupervised MDM commands. |
| Personal vs Corporate ownership flag | Yes | See [Personal vs Corporate Ownership](#personal-vs-corporate-ownership) below. |

For the complete supervised/unsupervised capability comparison across all enrollment paths, see [iOS/iPadOS Enrollment Path Overview § Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
```

The table row count and specific capability wording may be expanded per Claude's discretion using content from 29-RESEARCH.md "Capabilities Available Without Supervision" — the above is the minimum coverage inventory satisfying D-14. Keep the capability-level granularity (not per-path; the per-path comparison lives in the enrollment overview).

**Key Concepts Before You Begin** section with two subsections:

```markdown
## Key Concepts Before You Begin

### Company Portal vs Web-Based Enrollment

iOS/iPadOS Device Enrollment has two user-initiated flows. Both produce the same unsupervised managed-device end-state and the same capability set. They differ in how the user starts enrollment and what the user experience looks like.

- **Company Portal enrollment** — User installs the Intune Company Portal app from the App Store, signs in with their work account, and follows the in-app prompts. Suitable for tenants that want a branded enrollment experience and plan to use Company Portal for ongoing app discovery.
- **Web-based enrollment** — User visits a tenant-specific enrollment URL in Safari, signs in, and downloads the management profile. Suitable for tenants that do not require the Company Portal app or prefer a lighter-weight end-user experience.

Choose one flow per enrollment campaign or let end users choose — both are supported simultaneously.

### Tenant-Level Enrollment Restrictions Apply

Intune enrollment restrictions (platform filtering, ownership flag, per-user device limits, and enrollment-type blocking) apply tenant-wide across Device Enrollment and all other enrollment paths. See [iOS/iPadOS Admin Setup Overview § Intune Enrollment Restrictions](00-overview.md#intune-enrollment-restrictions) for restriction configuration. Restrictions are NOT duplicated in this guide.
```

Do NOT include privacy-limit callouts (D-18). Do NOT include profile-based User Enrollment deprecation content (D-17 — that belongs to 08-user-enrollment.md).
  </action>
  <verify>
    <automated>bash -c "test -f docs/admin-setup-ios/07-device-enrollment.md && head -10 docs/admin-setup-ios/07-device-enrollment.md | grep -q 'last_verified: 2026-04-17' && head -10 docs/admin-setup-ios/07-device-enrollment.md | grep -q 'review_by: 2026-07-16' && head -10 docs/admin-setup-ios/07-device-enrollment.md | grep -q 'applies_to: device-enrollment' && head -10 docs/admin-setup-ios/07-device-enrollment.md | grep -q 'platform: iOS' && head -10 docs/admin-setup-ios/07-device-enrollment.md | grep -q 'audience: admin' && grep -q '# iOS/iPadOS Device Enrollment' docs/admin-setup-ios/07-device-enrollment.md && grep -q 'Capabilities Available Without Supervision' docs/admin-setup-ios/07-device-enrollment.md && grep -q '00-overview.md#intune-enrollment-restrictions' docs/admin-setup-ios/07-device-enrollment.md && [ $(grep -c 'Privacy limit:' docs/admin-setup-ios/07-device-enrollment.md) -eq 0 ] && echo OK"</automated>
  </verify>
  <done>
    - File exists at `docs/admin-setup-ios/07-device-enrollment.md`
    - Frontmatter has all 5 required fields with exact values (`last_verified: 2026-04-17`, `review_by: 2026-07-16`, `applies_to: device-enrollment`, `audience: admin`, `platform: iOS`)
    - Title is `# iOS/iPadOS Device Enrollment`
    - `## Capabilities Available Without Supervision` heading exists and precedes any `## Steps` section
    - Capabilities table has minimum 10 rows covering config profiles, compliance, app deployment modes (+ explicit **No** entries for silent install, OS update enforcement, supervised-only restrictions, Activation Lock bypass, Lost Mode)
    - Key Concepts Before You Begin section exists with Company Portal vs Web-Based subsection AND a Tenant-Level Enrollment Restrictions subsection cross-linking to `00-overview.md#intune-enrollment-restrictions`
    - ZERO instances of `Privacy limit:` string in the file (enforces D-18)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Prerequisites, both per-flow Steps sections (Company Portal + Web-Based), Personal vs Corporate Ownership section, Verification, Configuration-Caused Failures table, See Also, and version-history footer</name>
  <files>docs/admin-setup-ios/07-device-enrollment.md</files>
  <read_first>
    - docs/admin-setup-ios/07-device-enrollment.md (post-Task-1 state)
    - docs/admin-setup-ios/05-app-deployment.md lines 88-217 (per-type sections, Verification, Config-Caused Failures, See Also, version-history pattern)
    - docs/admin-setup-ios/03-ade-enrollment-profile.md lines 18-23 (Prerequisites pattern), 157-166 (Configuration-Caused Failures pattern)
    - .planning/phases/29-ios-admin-setup-byod-mam/29-PATTERNS.md lines 72-141 (per-flow structure, Phase 30 placeholder, contrast-to-ADE prose, See Also, footer)
    - .planning/phases/29-ios-admin-setup-byod-mam/29-CONTEXT.md D-12/D-13/D-16 (flow coverage, concept-over-click, ownership mechanics), specifics bullet at line 188 (`<30 seconds` scannability)
    - .planning/phases/29-ios-admin-setup-byod-mam/29-RESEARCH.md "ABYOD-01 Device Enrollment" setting inventories + flow mechanics
  </read_first>
  <action>
Implements D-12 (both flows), D-13 (concept-over-click; Phase 30 placeholder in Config-Caused Failures), D-16 (Personal vs Corporate ownership section), D-04 (contrast-prose for ADE-only capabilities).

Add the following sections to `docs/admin-setup-ios/07-device-enrollment.md` in order after the Key Concepts section:

**Prerequisites section**:

```markdown
## Prerequisites

Admin prerequisites for Device Enrollment:

- APNs certificate configured and active (see [APNs Certificate Guide](01-apns-certificate.md) — same APNs certificate serves ADE, Device Enrollment, and User Enrollment)
- Intune Administrator role, or a custom RBAC role with enrollment management permissions
- Microsoft Intune Plan 1 (or higher) subscription
- Tenant enrollment restrictions reviewed — see [iOS/iPadOS Admin Setup Overview § Intune Enrollment Restrictions](00-overview.md#intune-enrollment-restrictions)

End-user prerequisites:

- Work account (Microsoft Entra ID) with Intune license assigned
- iOS/iPadOS 14.0 or later (Intune minimum; verify current against Microsoft Learn `supported-devices-browsers-intune`)

Flow-specific prereqs are listed within each per-flow section below.
```

**Steps — two parallel per-flow sections**:

```markdown
## Steps

Device Enrollment has two parallel flows. Complete the portal-side configuration for the flows you want to support, then direct end users to the appropriate flow.

### Flow 1: Company Portal Enrollment

**Flow-specific prerequisites:**

- Intune Company Portal app available on the App Store in the end user's region
- End user can install apps (tenant App Store restrictions, if any, permit Company Portal)

#### In Intune admin center

1. Sign in to [Intune admin center](https://intune.microsoft.com).
2. Navigate to **Devices** > **Enrollment** > **Enrollment device platform restrictions** > **iOS/iPadOS restrictions**.
3. Confirm Personally-owned enrollment is allowed for the user groups you expect to enroll. If enrollment is blocked, end users see an "enrollment not allowed" error after signing in.

   > **What breaks if misconfigured:** A tenant-wide block on personally-owned enrollment prevents Company Portal completion for BYOD users even after Company Portal is installed. Symptom appears in: device (Company Portal shows "This device can't be managed" or "Your administrator hasn't made this option available") with no failure in the Intune admin center portal itself.

4. Navigate to **Apps** > **iOS/iPadOS** and confirm the Intune Company Portal app is assigned as a Required app to the enrollment user groups. Company Portal self-distributes via the App Store for unassigned users; explicit assignment ensures the app stays available and updates apply.

**End-user flow (outcome-level summary):**

After the admin configuration above is in place, end users complete Company Portal enrollment by installing Company Portal from the App Store, signing in with their work account, and completing the in-app enrollment prompts. The flow installs the MDM profile on the device and ends with the device registered as managed in Intune. The expected outcome is a device visible in Intune admin center under **Devices** > **All devices** with the user's UPN, a Management Name, and the Personal ownership designation (unless the device is later reclassified as Corporate — see § Personal vs Corporate Ownership below).

### Flow 2: Web-Based Enrollment

**Flow-specific prerequisites:**

- Safari browser on the enrolling device (web-based enrollment requires Safari; Chrome and other iOS browsers are not supported)
- End user can download and install configuration profiles (tenant Device Restrictions, if deployed, do not block profile installation)

#### In Intune admin center

1. Sign in to [Intune admin center](https://intune.microsoft.com).
2. Navigate to **Devices** > **Enrollment** > **Enrollment device platform restrictions** > **iOS/iPadOS restrictions** and confirm Personally-owned enrollment is allowed for the user groups you expect to enroll (same check as Flow 1).

   > **What breaks if misconfigured:** A tenant-wide enrollment-restriction block affects both flows identically. Symptom appears in: device (enrollment URL sign-in succeeds but profile download never starts, or download starts and fails mid-flow with "enrollment not allowed").

3. Communicate the web-based enrollment URL to end users: `https://enrollment.manage.microsoft.com/enrollmentserver/discovery.svc` (default tenant URL; confirm current URL against Microsoft Learn `ios-device-enrollment` at time of writing).

**End-user flow (outcome-level summary):**

End users open the enrollment URL in Safari, sign in with their work account, and follow the on-screen prompts to download and install the management profile. The flow does not require Company Portal. The expected outcome is a device visible in Intune admin center under **Devices** > **All devices** with the same artifacts as Company Portal flow (user UPN, Management Name, Personal ownership by default). Web-based enrollment is functionally equivalent to Company Portal enrollment for management-capability purposes.
```

Do NOT write click-by-click walkthroughs of the on-device flow (D-13 + REQUIREMENTS out-of-scope rule "document concepts and outcomes, not click paths"). Keep the end-user flow descriptions to outcome-level summaries.

**Personal vs Corporate Ownership section** (D-16):

```markdown
## Personal vs Corporate Ownership

Intune assigns every iOS/iPadOS device an ownership designation of **Personal** or **Corporate** that affects wipe scope, compliance evaluation options, and the default personal-data-protection posture. Device Enrollment enrolls devices as Personal by default.

### Ownership Designation Effects

| Effect | Personal | Corporate |
|--------|----------|-----------|
| **Wipe scope** | Retire removes corporate data and policies only; device remains usable | Wipe resets device to factory state (MDM RemoteWipe) |
| **Activation Lock bypass after wipe** | Not available (user Apple Account controls Activation Lock) | Can be cleared with supervised mode (not available on Device Enrollment even for Corporate) |
| **Inventory scope** | Intune inventories only managed content (managed apps, profiles) on personal-ownership devices; Apple Platform Deployment Reference defines this managed-content boundary | Intune can inventory full device scope including installed apps, but only insofar as iOS APIs expose this data to MDM |
| **Compliance action enforcement** | Retire is the terminal noncompliance action | Retire or Wipe available as terminal noncompliance actions |

### Reclassifying a Device as Corporate

A Device Enrollment enrolled device defaults to Personal. To reclassify as Corporate (for example, when an admin takes ownership of a BYOD device that will be repurposed as a shared corporate device), use one of:

- **Device identifier upload** — Upload a CSV of IMEI, serial number, or Apple Account identifiers to Intune via **Devices** > **Enrollment** > **Corporate device identifiers**. Devices matching the identifier are marked Corporate at next check-in.
- **Bulk-update via Microsoft Graph** — `PATCH` the device object's `managedDeviceOwnerType` field (advanced; verify current Graph schema against Microsoft Learn at time of writing).

Reclassification does NOT change the device's supervision state — the device remains unsupervised. To gain supervised capabilities, the device must be wiped and re-enrolled via ADE.
```

**Verification section**:

```markdown
## Verification

- [ ] Device appears in Intune admin center under **Devices** > **All devices** with the expected user UPN and a Management Name following the tenant's naming convention
- [ ] Device ownership designation matches expectation (Personal by default; Corporate after identifier upload)
- [ ] Compliance state moves from "Not evaluated" to "Compliant" or "Not compliant" within the expected window (see [Compliance Policy § Compliance Evaluation Timing](06-compliance-policy.md#compliance-evaluation-timing-and-conditional-access))
- [ ] Assigned configuration profiles arrive on the device (check **Settings** > **General** > **VPN & Device Management** on device to confirm the MDM profile is present)
- [ ] For Company Portal flow: Company Portal app shows the device as managed with corporate resources visible
```

**Configuration-Caused Failures table (D-13 — Phase 30 placeholder verbatim):**

```markdown
## Configuration-Caused Failures

| Misconfiguration | Portal | Symptom | Runbook |
|------------------|--------|---------|---------|
| Personally-owned enrollment blocked at tenant level when BYOD users attempt to enroll | Intune | Company Portal or web enrollment fails at the "this device can't be managed" step; no device appears in Intune | iOS L1 runbooks (Phase 30) |
| APNs certificate expired | Intune | All iOS enrollment attempts fail silently; no device arrives in Intune; MDM check-in on previously enrolled devices also fails | iOS L1 runbooks (Phase 30) |
| Per-user device limit reached | Intune | Enrollment fails with "device limit reached" user-visible message after sign-in | iOS L1 runbooks (Phase 30) |
| Intune license not assigned to enrolling user | Intune / Entra | Enrollment sign-in succeeds but MDM profile never downloads; Intune shows no device for the user | iOS L1 runbooks (Phase 30) |
| Web-based enrollment attempted from non-Safari browser | Device | Enrollment URL loads; sign-in succeeds; management-profile download button does nothing or errors | iOS L1 runbooks (Phase 30) |
| Compliance policy requires passcode of length greater than device-native limit | Intune | Device enrolls but immediately shows non-compliant for passcode; user cannot comply | iOS L1 runbooks (Phase 30) |
```

Every Runbook column entry uses the verbatim literal string `iOS L1 runbooks (Phase 30)` (no link, no markdown link syntax — Phase 32 will bulk-replace once Phase 30 delivers runbooks).

**Contrast-to-ADE prose (D-04 — inline prose only, no glyph):**

Where the body copy mentions silent install or supervised-only features, use the D-04 pattern — short contrast sentences, NO callout glyph, NO blockquote:

Example (already used in the Capabilities table notes column): "Silent install is an ADE-only capability — see [ADE Enrollment Profile](03-ade-enrollment-profile.md)."

Do NOT use `> 🔓 **Unsupervised:**` or any inverse-glyph callout (D-04 locked).

**See Also section:**

```markdown
## See Also

- [iOS/iPadOS Admin Setup Overview](00-overview.md) — Path router and shared Intune Enrollment Restrictions section
- [User Enrollment](08-user-enrollment.md) — Privacy-preserving account-driven enrollment alternative
- [MAM App Protection](09-mam-app-protection.md) — App-layer protection without device enrollment
- [ADE Enrollment Profile](03-ade-enrollment-profile.md) — Supervised corporate enrollment (prerequisite for supervised-only features)
- [Configuration Profiles](04-configuration-profiles.md) — Wi-Fi/VPN/Certificates/Device Restrictions delivery
- [App Deployment](05-app-deployment.md) — VPP / LOB / Store apps
- [Compliance Policies](06-compliance-policy.md) — OS version, passcode, jailbreak, CA timing
- [iOS/iPadOS Enrollment Path Overview](../ios-lifecycle/00-enrollment-overview.md) — Conceptual path comparison
- [Apple Provisioning Glossary](../_glossary-macos.md)
```

**Footer navigation + version-history:**

```markdown
---
*Previous: [Compliance Policies](06-compliance-policy.md) | Next: [User Enrollment](08-user-enrollment.md) | [Back to Overview](00-overview.md)*

---

| Date | Change | Author |
|------|--------|--------|
| 2026-04-17 | Initial version — iOS/iPadOS Device Enrollment admin guide covering Company Portal and web-based flows, capabilities available without supervision, and personal vs corporate ownership | -- |
```
  </action>
  <verify>
    <automated>bash -c "grep -qE '### Flow 1: Company Portal' docs/admin-setup-ios/07-device-enrollment.md && grep -qE '### Flow 2: Web-Based' docs/admin-setup-ios/07-device-enrollment.md && [ $(grep -ci 'web-based\|company portal' docs/admin-setup-ios/07-device-enrollment.md) -ge 4 ] && grep -qE '^##.*Ownership' docs/admin-setup-ios/07-device-enrollment.md && [ $(grep -c 'iOS L1 runbooks (Phase 30)' docs/admin-setup-ios/07-device-enrollment.md) -ge 3 ] && grep -q '## Configuration-Caused Failures' docs/admin-setup-ios/07-device-enrollment.md && grep -q '## Verification' docs/admin-setup-ios/07-device-enrollment.md && grep -q '## See Also' docs/admin-setup-ios/07-device-enrollment.md && grep -q '2026-04-17 | Initial version' docs/admin-setup-ios/07-device-enrollment.md && [ $(grep -c 'Privacy limit:' docs/admin-setup-ios/07-device-enrollment.md) -eq 0 ] && echo OK"</automated>
  </verify>
  <done>
    - Both `### Flow 1: Company Portal Enrollment` and `### Flow 2: Web-Based Enrollment` sections exist with flow-specific prereqs and admin configuration steps
    - Body text contains at least 4 occurrences combined of `web-based` or `company portal` (29-02-03 validation row)
    - `## Personal vs Corporate Ownership` section exists with the ownership-effects table and the reclassification-mechanism subsection
    - Configuration-Caused Failures table has at least 3 rows all using verbatim `iOS L1 runbooks (Phase 30)` in the Runbook column (29-02-06 validation row — expect ≥6 rows)
    - Verification checklist includes the standard iOS admin-guide checks (device-present-in-Intune, ownership, compliance-state, profile-arrival, Company-Portal-presence)
    - See Also section links to 00/03/04/05/06/08/09 guides plus enrollment-overview and glossary
    - Footer navigation + version-history table present; version-history has one row dated 2026-04-17
    - ZERO instances of `Privacy limit:` string in the file (29-02-04 validation row — hard negative)
    - At least one `03-ade-enrollment-profile.md` contrast link present (D-04 contrast-prose)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal authoring → published docs | Device Enrollment guide becomes authoritative admin reference for non-ABM iOS enrollment; misdescribed capabilities propagate to Phase 30 runbooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | Information Disclosure | Capabilities table + flow descriptions | mitigate | All capability statements cite Microsoft Learn `ios-device-enrollment` and `supervised-only reference`; no real tenant IDs, internal org names, or private roadmap references included. Enrollment-URL example uses the public default `enrollment.manage.microsoft.com` documented in Microsoft Learn. |
| T-29-03-02 | Tampering (roadmap-disclosure) | Version claims (iOS 14+, iOS 15+) | accept | Version-minimum claims cite Microsoft Learn at the capture timestamp 2026-04-17; guide includes the `last_verified: 2026-04-17` frontmatter and 90-day review cadence. Future version minimums will trigger scheduled review. |
</threat_model>

<verification>
After both tasks complete:
- Run full 29-VALIDATION.md row set for 29-02-01 through 29-02-06 individually via the VALIDATION.md-cited grep commands
- Open `docs/admin-setup-ios/07-device-enrollment.md` and visually confirm:
  1. Capabilities Available Without Supervision table is placed BEFORE the `## Steps` section
  2. No blockquote line starts with `> **Privacy limit:**`
  3. Flow 1 and Flow 2 sections each contain a `#### In Intune admin center` sub-heading
  4. Configuration-Caused Failures Runbook column uses literal `iOS L1 runbooks (Phase 30)` (no markdown link syntax, no backticks)
</verification>

<success_criteria>
- 29-02-01 (file exists), 29-02-02 (capabilities-table heading), 29-02-03 (both flows ≥4 mentions), 29-02-04 (zero privacy-limit callouts), 29-02-05 (ownership section), 29-02-06 (≥1 Phase 30 placeholder) validation rows all exit 0
- Must-have MH1 satisfied: an admin can answer "can I do X on BYOD?" by scanning the Capabilities Available Without Supervision table placed at the top of the guide
- Cross-link to `00-overview.md#intune-enrollment-restrictions` present (no duplication of restrictions content per D-15)
- Frontmatter (29-ALL-01, 29-ALL-02) and Phase 30 placeholder (29-ALL-03) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/29-ios-admin-setup-byod-mam/29-03-SUMMARY.md` documenting:
- File created: `docs/admin-setup-ios/07-device-enrollment.md`
- Section outline (Capabilities table, Key Concepts, Prerequisites, Flow 1, Flow 2, Ownership, Verification, Config-Caused Failures, See Also, version-history)
- Capabilities table row count and the specific **No** rows included
- Ownership-flag section summary
- Confirmation: zero `Privacy limit:` strings (D-18); zero profile-UE-deprecation content (D-17); Phase 30 placeholder usage count
- Cross-links present: `00-overview.md#intune-enrollment-restrictions`, `03-ade-enrollment-profile.md`
</output>
