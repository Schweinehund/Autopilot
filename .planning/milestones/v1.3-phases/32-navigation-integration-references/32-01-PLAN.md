---
phase: 32
plan: "01"
type: execute
wave: 1
depends_on: ["32-00"]
files_modified:
  - docs/_glossary-macos.md
autonomous: true
requirements: [NAV-01]
tags: [glossary, ios, apple, nav-01]
must_haves:
  truths:
    - "A user searching the Apple glossary finds definitions for supervision, MAM-WE, APNs, account-driven user enrollment, and jailbreak detection"
    - "The existing VPP entry explains the iOS-specific device-licensed vs user-licensed distinction with supervised-only silent install callout"
    - "All 5 new iOS terms + VPP update carry a > **Windows equivalent:** blockquote"
    - "MAM-WE is placed under a NEW ## App Protection (MAM) H2, NOT under Enrollment (per Phase 26 D-03)"
    - "The Alphabetical Index (line 16) expands from 6 to 11 entries in alphabetical order"
    - "File is NOT renamed (D-11 LOCK); all pre-existing anchors remain valid"
  artifacts:
    - path: "docs/_glossary-macos.md"
      provides: "5 new iOS H3 entries (Supervision, Account-Driven User Enrollment, APNs, Jailbreak Detection, MAM-WE) + updated VPP entry + new ## App Protection (MAM) H2"
      contains: "### MAM-WE"
      min_lines: 120
  key_links:
    - from: "docs/_glossary-macos.md Alphabetical Index"
      to: "#supervision, #mam-we, #apns, #account-driven-user-enrollment, #jailbreak-detection"
      via: "inline pipe-separated link list at line 16"
      pattern: "#supervision|#mam-we|#apns|#account-driven-user-enrollment|#jailbreak-detection"
    - from: "docs/_glossary-macos.md VPP entry"
      to: "admin-setup-ios/05-app-deployment.md, admin-setup-macos/04-app-deployment.md"
      via: "Cross-links paragraph within VPP entry"
      pattern: "admin-setup-ios/05-app-deployment.md"
---

<objective>
Extend the shared Apple provisioning glossary (`docs/_glossary-macos.md`) with exactly 5 net-new iOS terms + 1 updated entry (VPP) per SC #1 literal scope. Per D-11, the file is NOT renamed (76 references across 29 files; Phase 26 locked filename). Per D-13, 4 terms go into existing H2 sections (Supervision + Account-Driven UE under Enrollment; APNs + Jailbreak Detection under Device Management) and MAM-WE gets its own NEW H2 (`## App Protection (MAM)`) per Phase 26 D-03 separation of MAM from MDM paths.

Purpose: NAV-01 requirement. Phase 32 SC #1 literal: "The shared glossary (`_glossary-macos.md`) contains definitions for iOS-specific terms (supervision, MAM-WE, APNs, account-driven user enrollment, VPP, and jailbreak detection)." Also D-36 placeholder retrofit in Plan 03 links INTO the new anchors (#supervision, #mam-we, etc.), so these must ship with stable anchor text.

Output: `docs/_glossary-macos.md` grown from 73 lines → ~150-155 lines with 5 new H3 entries, 1 rewritten VPP entry, 1 new H2 section, expanded Alphabetical Index, bumped frontmatter, updated platform coverage blockquote, and new Version History row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-navigation-integration-references/32-CONTEXT.md
@.planning/phases/32-navigation-integration-references/32-RESEARCH.md
@.planning/phases/32-navigation-integration-references/32-PATTERNS.md
@docs/_glossary-macos.md
@docs/_glossary.md
@docs/admin-setup-ios/05-app-deployment.md
@docs/admin-setup-ios/08-user-enrollment.md
@docs/admin-setup-ios/09-mam-app-protection.md

<interfaces>
<!-- Existing glossary structure (extracted from docs/_glossary-macos.md current state) -->
<!-- Executor should extend this structure, NOT replace it -->

Current file: 73 lines
- Lines 1-7: frontmatter (last_verified, review_by, applies_to, audience, platform)
- Lines 9-10: platform coverage blockquote (update target: D-41 suffix)
- Line 12: # macOS Provisioning Glossary (H1 — DO NOT RENAME; already cross-platform-neutral per D-25 pattern)
- Line 14: ## Alphabetical Index H2
- Line 16: inline index `[ABM](#abm) | [ABM Token](#abm-token) | [ADE](#ade) | [Await Configuration](#await-configuration) | [Setup Assistant](#setup-assistant) | [VPP](#vpp)`
- Line 20: ## Enrollment H2 (ADE line 22, Await Configuration line 28, Setup Assistant line 34)
- Line 42: ## Device Management H2 (ABM line 44, ABM Token line 50)
- Line 58: ## App Distribution H2 (VPP line 60 — UPDATE target)
- Line 68: ## Version History H2 (table at lines 70-72)

Per-term entry pattern (from ADE line 22-26):
```
### [Term]

[Definition — 1-2 sentences. Cross-platform scope flag in opening sentence per D-16.]

> **Windows equivalent:** [description OR "No direct equivalent" OR closest parallel]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Frontmatter + platform coverage blockquote + Alphabetical Index expansion</name>
  <files>docs/_glossary-macos.md</files>
  <read_first>
    - docs/_glossary-macos.md lines 1-20 (frontmatter, blockquote, H1, Alphabetical Index)
    - .planning/phases/32-navigation-integration-references/32-CONTEXT.md (D-14, D-17, D-41)
    - .planning/phases/32-navigation-integration-references/32-PATTERNS.md §2 (Glossary) Insertion points
  </read_first>
  <behavior>
    - Frontmatter `last_verified: 2026-04-17`, `review_by: 2026-07-16` (90d later).
    - Platform coverage blockquote line 9 ends with "...**and iOS/iPadOS**" suffix per D-41.
    - Alphabetical Index line 16 contains EXACTLY 11 terms in alphabetical order.
  </behavior>
  <action>
Apply three surgical edits to the top of `docs/_glossary-macos.md`:

**Edit 1 — Frontmatter bump (lines 2-3):**
- `last_verified: 2026-04-13` → `last_verified: 2026-04-17`
- `review_by: 2026-07-12` → `review_by: 2026-07-16`

**Edit 2 — Platform coverage blockquote (line 9):**

Current:
```
> **Platform coverage:** This glossary covers macOS-specific provisioning and management terminology.
> For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md).
```

Replace with (per D-41 canonical suffix + D-17 platform coverage language):
```
> **Platform coverage:** This glossary covers Apple-platform provisioning and management terminology for macOS and iOS/iPadOS.
> For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md).
```

**Edit 3 — Alphabetical Index expansion (line 16):**

Current:
```
[ABM](#abm) | [ABM Token](#abm-token) | [ADE](#ade) | [Await Configuration](#await-configuration) | [Setup Assistant](#setup-assistant) | [VPP](#vpp)
```

Replace with (alphabetical order; 11 terms per D-14):
```
[ABM](#abm) | [ABM Token](#abm-token) | [Account-Driven User Enrollment](#account-driven-user-enrollment) | [ADE](#ade) | [APNs](#apns) | [Await Configuration](#await-configuration) | [Jailbreak Detection](#jailbreak-detection) | [MAM-WE](#mam-we) | [Setup Assistant](#setup-assistant) | [Supervision](#supervision) | [VPP](#vpp)
```

Note: "Account-Driven User Enrollment" comes before "ADE" alphabetically (A-c-c vs A-D); "APNs" comes after "ADE" (A-D vs A-P); "Jailbreak Detection" sits between "Await" and "MAM-WE"; "Supervision" is second-to-last before VPP.

Do NOT touch lines 18-72 in this task.
  </action>
  <verify>
    <automated>grep -c "and iOS/iPadOS" docs/_glossary-macos.md | grep -qE '^[1-9][0-9]*$' && head -20 docs/_glossary-macos.md | grep -c "#supervision\|#mam-we\|#apns\|#account-driven-user-enrollment\|#jailbreak-detection" | grep -qE '^[1-9][0-9]*$' && grep "last_verified: 2026-04-17" docs/_glossary-macos.md</automated>
  </verify>
  <done>Frontmatter shows `2026-04-17`/`2026-07-16`; platform blockquote contains "and iOS/iPadOS"; Alphabetical Index line has exactly 11 pipe-separated entries in alphabetical order with all 5 new anchors present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 4 new H3 entries to existing H2s (Supervision, Account-Driven UE, APNs, Jailbreak Detection) + update VPP entry</name>
  <files>docs/_glossary-macos.md</files>
  <read_first>
    - docs/_glossary-macos.md lines 20-65 (Enrollment H2, Device Management H2, App Distribution H2, existing VPP entry)
    - .planning/phases/32-navigation-integration-references/32-CONTEXT.md (D-13, D-15, D-16, D-18)
    - .planning/phases/32-navigation-integration-references/32-RESEARCH.md §Code Examples Example 1 (Supervision template) and Example 2 (VPP update template)
  </read_first>
  <behavior>
    - 4 new H3 entries added to existing H2s (Enrollment, Device Management) per D-13 placement map.
    - VPP entry at current line 60 rewritten to include device-licensed vs user-licensed distinction, supervised-only silent install callout, iOS + macOS app deployment cross-links. Existing `> **Windows equivalent:**` blockquote PRESERVED.
    - Every new H3 entry ends with `> **Windows equivalent:** ...` blockquote (D-15) and opens with cross-platform scope statement (D-16).
    - Existing ADE, Await Configuration, Setup Assistant, ABM, ABM Token entries are NOT modified (D-18).
  </behavior>
  <action>

**Under `## Enrollment` H2 (current line 20), insert 2 new H3 entries.** Placement: default alphabetical within section — "Account-Driven User Enrollment" (A-c) comes BEFORE "ADE" (A-D) and BEFORE existing "Await Configuration"; "Supervision" (S) comes AFTER "Setup Assistant" (S-e). So the final order is:

1. Account-Driven User Enrollment (NEW — first)
2. ADE (existing)
3. Await Configuration (existing)
4. Setup Assistant (existing)
5. Supervision (NEW — last)

Insert `### Account-Driven User Enrollment` BEFORE current `### ADE` (line 22):

```markdown
### Account-Driven User Enrollment

Apple's privacy-preserving BYOD enrollment method for iOS/iPadOS (iOS 15+) and macOS (Sonoma+). The user starts enrollment from Settings > General > VPN & Device Management on their personal device by signing in with their Managed Apple ID; the OS coordinates with the organization's MDM via the well-known `https://.well-known/com.apple.remotemanagement` discovery endpoint. Only the organization's "work" apps and data are managed; the user's personal content (photos, iCloud, personal apps) remains invisible to IT. Supersedes the deprecated profile-based User Enrollment (deprecated iOS 18).

> **Windows equivalent:** No direct equivalent. The closest parallel is [Intune MAM-WE](#mam-we) on Windows MAM-enrolled devices, but Account-Driven User Enrollment is device-level BYOD enrollment whereas Windows MAM-WE is app-layer only. Microsoft's "Work profile on personally-owned devices" concept applies to Android but has no iOS-Autopilot equivalent.
```

Insert `### Supervision` AFTER current `### Setup Assistant` (current lines 34-38 contain Setup Assistant):

```markdown
### Supervision

Apple's formal management designation for organization-owned iOS/iPadOS devices enrolled through [ADE](#ade) via Apple Business Manager. Supervision applies to iOS, iPadOS, and (to a lesser gating extent) macOS. Supervision is set at enrollment time and CANNOT be added retroactively without a full device erase. Supervised iOS/iPadOS devices unlock capabilities unavailable on unsupervised devices — silent app install (VPP device-licensed), home screen layout control, extensive restriction profiles, AirDrop/Camera/Safari restrictions, and DDM-enforced app install. Verify on device: Settings > General > About shows "This iPhone is supervised and managed by [organization]."

> **Windows equivalent:** No direct equivalent in Autopilot. The closest parallels are (a) Autopilot pre-provisioning (device enters with administrative context) for the enrollment-time supervision concept, and (b) macOS FileVault enforcement via ADE for the "set-at-enrollment, cannot-add-retroactively" lifecycle property. Neither Windows nor macOS gates as many capabilities on supervision state as iOS does.
```

**Under `## Device Management` H2 (current line 42), insert 2 new H3 entries.** Placement: alphabetical — "ABM" (current line 44), "ABM Token" (current line 50), then new "APNs" (A-P comes after A-B-M alphabetically) and "Jailbreak Detection" (J comes after all A entries but this section has only A-entries, so append both after ABM Token):

Insert `### APNs` AFTER current `### ABM Token` (lines 50-54):

```markdown
### APNs

Apple Push Notification service — Apple's push notification service used by all Apple platforms (iOS, iPadOS, macOS, tvOS) for MDM command delivery. Intune uses APNs to wake devices and deliver configuration profiles, app install commands, and compliance evaluation triggers. The Apple **APNs certificate** (Microsoft also calls it the "MDM Push Certificate") is provisioned per-tenant in Intune admin center at `Devices > Enrollment > Apple > MDM Push Certificate` and must be renewed annually. **Critical cross-platform blast radius:** a single expired or revoked APNs certificate breaks ALL iOS/iPadOS AND macOS MDM communication for the entire tenant until renewed. Must be renewed by the SAME Apple ID that created it — renewing with a different Apple ID forces re-enrollment of every managed Apple device.

> **Windows equivalent:** No direct equivalent for the certificate lifecycle. Windows Autopilot relies on Windows Notification Service (WNS) and HTTPS polling; there is no per-tenant certificate to renew. The closest parallel is Intune's automatic communication with Windows clients via Entra ID, which does not lapse annually.
```

Insert `### Jailbreak Detection` AFTER the new `### APNs` entry:

```markdown
### Jailbreak Detection

An iOS/iPadOS-specific compliance check in Intune that evaluates device integrity indicators (e.g., root filesystem writability, presence of jailbreak tool signatures) and marks the device non-compliant if jailbreak is detected. Configured in Intune admin center at `Devices > Compliance policies > [policy] > Device Health > Jailbroken devices`. Not a real-time protection — evaluation happens at compliance sync intervals. Users are notified via Company Portal if their device is flagged.

> **Windows equivalent:** No direct equivalent (jailbreaking is an iOS/iPadOS concept). The closest parallel in Windows is Attestation-based compliance (TPM attestation proving the device booted into a known good state), which addresses a different threat model (early boot integrity vs OS-level rooting).
```

**Under `## App Distribution` H2 (current line 58), REPLACE the VPP entry at lines 60-64 with updated iOS content.** Preserve the existing `> **Windows equivalent:**` blockquote at line 64 by integrating its content into the new version:

Current VPP entry (lines 60-64):
```markdown
### VPP

Volume Purchase Program -- Apple's bulk app licensing mechanism, now branded as "Apps and Books" within Apple Business Manager. Licenses are purchased in ABM and synced to Intune via a location token. Enables silent app installation on supervised devices without Apple ID sign-in on the device.

> **Windows equivalent:** Microsoft Store for Business (deprecated) and Intune app deployment. Windows uses Win32 app packaging (.intunewin), MSI, MSIX, and Microsoft Store apps. The VPP/Apps and Books licensing model has no direct Windows equivalent.
```

Replace with:
```markdown
### VPP

Volume Purchase Program — Apple's bulk app licensing mechanism, now branded as "Apps and Books" within Apple Business Manager. Applies to all Apple platforms (iOS, iPadOS, macOS, tvOS). Licenses are purchased in ABM and synced to Intune via a location token. The VPP licensing model has two variants that affect deployment behavior on iOS/iPadOS:

- **Device-licensed** — License is pinned to the device serial number. Enables silent install on supervised iOS/iPadOS devices (no Apple ID sign-in required on the device). Preferred for corporate-owned supervised (ADE) deployments.
- **User-licensed** — License is pinned to the user's Managed Apple ID. Requires the user to sign in on the device. Used in 1:1 education and personal-device scenarios.

Silent app installation on iOS/iPadOS requires BOTH (a) VPP device-licensed assignment AND (b) device supervision via ADE. On unsupervised iOS devices, even VPP apps prompt the user for confirmation. On macOS, VPP installs silently regardless of supervision state (macOS does not enforce the supervised-only silent-install gate).

Cross-links: [iOS App Deployment Guide](admin-setup-ios/05-app-deployment.md) | [macOS App Deployment Guide](admin-setup-macos/04-app-deployment.md)

> **Windows equivalent:** Microsoft Store for Business (deprecated) and Intune app deployment. Windows uses Win32 app packaging (.intunewin), MSI, MSIX, and Microsoft Store apps. The VPP/Apps and Books licensing model has no direct Windows equivalent — Intune's per-user vs per-device assignment in Windows app deployments is the closest conceptual parallel but uses Microsoft licensing infrastructure, not Apple's.
```

Do NOT modify lines containing ADE, Await Configuration, Setup Assistant, ABM, or ABM Token entries (D-18 enforcement).
  </action>
  <verify>
    <automated>grep -cE "^### (Account-Driven User Enrollment|Supervision|APNs|Jailbreak Detection)" docs/_glossary-macos.md | grep -qE '^4$' && grep -c "device-licensed" docs/_glossary-macos.md | grep -qE '^[1-9][0-9]*$' && grep -c "admin-setup-ios/05-app-deployment.md" docs/_glossary-macos.md | grep -qE '^[1-9][0-9]*$' && bash .planning/phases/32-navigation-integration-references/validation/link-check.sh docs/_glossary-macos.md</automated>
  </verify>
  <done>4 new H3 entries exist in correct H2 sections (Enrollment: Account-Driven UE + Supervision; Device Management: APNs + Jailbreak Detection); VPP entry contains "device-licensed" prose AND `admin-setup-ios/05-app-deployment.md` cross-link; link-check.sh exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: New ## App Protection (MAM) H2 with MAM-WE entry + Version History</name>
  <files>docs/_glossary-macos.md</files>
  <read_first>
    - docs/_glossary-macos.md (post-Task-2 state; verify H2 boundaries)
    - .planning/phases/32-navigation-integration-references/32-CONTEXT.md (D-13 MAM-WE placement rationale; Phase 26 D-03 separation)
    - .planning/phases/26-ios-ipados-foundation/26-CONTEXT.md (D-03 MAM-WE separation from MDM paths — LOAD-BEARING)
    - docs/admin-setup-ios/09-mam-app-protection.md (cross-link target)
  </read_first>
  <behavior>
    - NEW H2 `## App Protection (MAM)` inserted AFTER `## App Distribution` (VPP section), BEFORE `## Version History` H2.
    - Contains single H3 entry `### MAM-WE` with `#mam-we` anchor.
    - MAM-WE is NOT placed under Enrollment H2 (Phase 26 D-03 locked separation).
    - Version History table receives new row with Phase 32 summary.
  </behavior>
  <action>

**Edit 1 — Insert new H2 between App Distribution and Version History:**

Locate the end of the App Distribution H2 (after the updated VPP entry's Windows equivalent blockquote). Locate the `## Version History` H2 line. Insert between them:

```markdown
---

## App Protection (MAM)

### MAM-WE

Managed App Without Enrollment — Intune's **app-layer** data protection model for iOS/iPadOS where the Intune Company Portal (or line-of-business equivalent) enforces app protection policies on specific Microsoft 365 and third-party apps WITHOUT enrolling the device in Intune MDM. The device is NOT supervised, NOT registered in Intune Devices blade, and NOT visible to IT as a managed device — Intune only sees the protected apps. Provides capabilities like app-level PIN, per-app encryption at rest, copy/paste restrictions between managed and personal apps, and **selective wipe** (removes managed app data only, preserves personal data and the device). Primary use case: BYOD scenarios where the user does not want to enroll their personal device but the organization must protect data accessed through work apps.

**Scope boundary (per Phase 26):** MAM-WE is separate from MDM enrollment paths (ADE, Device Enrollment, User Enrollment). A device can have MAM-WE + MDM enrollment simultaneously, but MAM-WE alone grants NO device-level control. See [MAM-WE App Protection Policies](admin-setup-ios/09-mam-app-protection.md) for configuration and the three-level data protection framework.

> **Windows equivalent:** Intune App Protection Policies on Windows MAM-enrolled devices — functionally analogous (per-app PIN, selective wipe, copy/paste gates) but uses different underlying mechanisms (Windows Information Protection lineage vs iOS's App Extensions SDK). Microsoft does not use the "MAM-WE" branding on Windows.
```

Note the `---` horizontal rule above the new H2 for visual separation matching the existing section spacing.

**Edit 2 — Version History row:**

Locate the Version History table (current lines 70-72). INSERT a new row ABOVE the existing `2026-04-13 | Initial version...` row:

```markdown
| 2026-04-17 | Phase 32: added iOS/iPadOS terms (supervision, MAM-WE, APNs, account-driven user enrollment, jailbreak detection), updated VPP with iOS device-licensed vs user-licensed distinction, new ## App Protection (MAM) H2 | -- |
```

Do NOT delete or modify the 2026-04-13 row.
  </action>
  <verify>
    <automated>grep -c "^## App Protection (MAM)" docs/_glossary-macos.md | grep -qE '^1$' && grep -c "^### MAM-WE" docs/_glossary-macos.md | grep -qE '^1$' && grep -c "Selective Wipe\|selective wipe" docs/_glossary-macos.md | grep -qE '^[1-9][0-9]*$' && grep -c "2026-04-17 | Phase 32:" docs/_glossary-macos.md | grep -qE '^1$' && grep -c "^### (Supervision|Account-Driven User Enrollment|APNs|Jailbreak Detection|MAM-WE)" docs/_glossary-macos.md | grep -qE '^5$' && bash .planning/phases/32-navigation-integration-references/validation/link-check.sh docs/_glossary-macos.md && bash .planning/phases/32-navigation-integration-references/validation/anchor-collision.sh docs/_glossary-macos.md</automated>
  </verify>
  <done>New `## App Protection (MAM)` H2 exists exactly once; `### MAM-WE` H3 exists exactly once; all 5 new H3 entries present; Version History has Phase 32 row; link-check exits 0; anchor-collision.sh shows no duplicate H3s within the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| glossary content → reader's mental model | Readers rely on glossary for terminology; misleading definitions cause downstream configuration errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | Information Disclosure (Repudiation for admins) | Supervision / Silent-install capability claims | mitigate | D-05 + RESEARCH.md Pitfall 4 verified: VPP entry explicitly states "silent install requires BOTH (a) device-licensed AND (b) supervision" — does NOT conflate with DDM software updates. Supervision entry explicitly says "set at enrollment time, cannot be added retroactively without full device erase". No sweeping "supervised = silent install" generalizations. |
| T-32-04 | Information Disclosure | ABM Token / APNs certificate descriptions | mitigate | APNs entry describes certificate lifecycle abstractly; no example tokens, no example Apple IDs. ABM token entry (existing, not modified) already describes token shape ".p7m file" only. |
</threat_model>

<verification>
- `grep -c "^### (Account-Driven User Enrollment|Supervision|APNs|Jailbreak Detection|MAM-WE)" docs/_glossary-macos.md` returns 5
- `grep -c "^## App Protection (MAM)" docs/_glossary-macos.md` returns 1
- `grep -c "Windows equivalent:" docs/_glossary-macos.md` increases by at least 5 vs pre-Phase-32 state (5 new H3s each have one)
- `bash .planning/phases/32-navigation-integration-references/validation/link-check.sh docs/_glossary-macos.md` exits 0 (all internal anchors resolve, all external paths exist)
- `bash .planning/phases/32-navigation-integration-references/validation/anchor-collision.sh docs/_glossary-macos.md` exits 0 (no duplicate H3s — new iOS-only terms don't collide with macOS)
- Alphabetical Index (line 16) contains exactly 11 pipe-separated entries
</verification>

<success_criteria>
NAV-01 satisfied per SC #1 literal:
1. Glossary contains definitions for: supervision, MAM-WE, APNs, account-driven user enrollment, VPP (updated), jailbreak detection (6 terms total — 5 new + 1 updated)
2. MAM-WE is under a NEW `## App Protection (MAM)` H2, NOT under Enrollment (Phase 26 D-03 compliance)
3. Alphabetical Index contains all 11 entries in alphabetical order
4. Every new H3 ends with `> **Windows equivalent:** ...` blockquote (D-15)
5. VPP entry distinguishes device-licensed vs user-licensed with supervised-only silent install callout
6. File name unchanged (`_glossary-macos.md` per D-11)
7. No pre-existing anchors broken (existing ADE/ABM/Setup Assistant/etc. untouched per D-18)
</success_criteria>

<output>
After completion, create `.planning/phases/32-navigation-integration-references/32-01-SUMMARY.md` documenting:
- All 5 new term entries with their H2 placement and anchor names
- VPP entry update (diff of old vs new prose)
- Alphabetical Index expansion confirmation (6 → 11 entries)
- link-check.sh and anchor-collision.sh exit codes
- Any prose-level deviations from D-15/D-16 template (and reasoning)
</output>
