---
phase: 32
plan: "03"
type: execute
wave: 1
depends_on: ["32-00", "32-01"]
files_modified:
  - docs/decision-trees/07-ios-triage.md
autonomous: true
requirements: [NAV-01]
tags: [placeholder-retrofit, ios, decision-tree, nav-01]
must_haves:
  truths:
    - "Every Phase 32 forward-reference placeholder in docs/ is resolved to a concrete link"
    - "Planner grep run at plan time confirmed exactly 1 placeholder: docs/decision-trees/07-ios-triage.md:99"
    - "Post-retrofit, grep -rn 'Phase 32\\|NAV-0[123]' docs/ returns 0 matches"
  artifacts:
    - path: "docs/decision-trees/07-ios-triage.md"
      provides: "Placeholder at line 99 resolved with 5 iOS anchor links + 5 macOS anchor links to _glossary-macos.md"
      contains: "supervision"
  key_links:
    - from: "docs/decision-trees/07-ios-triage.md line 99"
      to: "docs/_glossary-macos.md#supervision, #mam-we, #apns, #account-driven-user-enrollment, #jailbreak-detection, #abm, #ade, #vpp, #await-configuration, #setup-assistant"
      via: "Related Resources entry with 10 inline anchor links"
      pattern: "_glossary-macos.md#supervision"
---

<objective>
Resolve the single known Phase 32 forward-reference placeholder in `docs/decision-trees/07-ios-triage.md:99` per D-35. The current line reads:

```
- [Apple Provisioning Glossary](../_glossary-macos.md) -- Shared Apple terminology (iOS glossary additions in Phase 32 NAV-01)
```

The parenthetical is a pre-Phase-32 note that must be rewritten to a concrete sentence referencing the glossary terms now live in `_glossary-macos.md` per Plan 01. Per D-36, the planner re-ran the grep at plan time and confirmed this is the ONLY Phase 32 placeholder — no additional occurrences discovered.

Purpose: Close the only Phase 32 forward-reference debt in the docs tree. Makes the decision tree's Related Resources entry directly usable (readers land on the specific iOS glossary anchors instead of a pointer to a future phase).

Output: One line rewritten in `07-ios-triage.md`, frontmatter bump, Version History entry. Single atomic commit per D-37 commit-grouping precedent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-navigation-integration-references/32-CONTEXT.md
@.planning/phases/32-navigation-integration-references/32-RESEARCH.md
@.planning/phases/32-navigation-integration-references/32-PATTERNS.md
@docs/decision-trees/07-ios-triage.md
@docs/_glossary-macos.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Placeholder grep re-verification at execution time</name>
  <files>docs/decision-trees/07-ios-triage.md</files>
  <read_first>
    - docs/decision-trees/07-ios-triage.md lines 85-107 (Related Resources + Version History area)
    - .planning/phases/32-navigation-integration-references/32-CONTEXT.md (D-35, D-36)
  </read_first>
  <behavior>
    - Grep `Phase 32|NAV-0[123]` across docs/ returns an enumerated list of all current placeholders.
    - Expected count at plan time: 1 match at `docs/decision-trees/07-ios-triage.md:99`.
    - If count > 1, each additional placeholder is added to Task 2 scope (same D-35 resolution pattern).
    - If count == 0, Plan 01 (glossary) commits may have resolved it already — abort Plan 03 with no-op and note in SUMMARY.
  </behavior>
  <action>
Run the grep and record the output:

```
grep -rnE "Phase 32|NAV-0[123]" docs/ 2>/dev/null
```

Expected result (from plan-time verification):
```
docs/decision-trees/07-ios-triage.md:99:- [Apple Provisioning Glossary](../_glossary-macos.md) -- Shared Apple terminology (iOS glossary additions in Phase 32 NAV-01)
```

**Decision logic:**
- If EXACTLY 1 match at line 99 → proceed to Task 2 as specified.
- If MORE than 1 match → Task 2 gets an extended scope: apply the D-35 resolution pattern (rewrite each placeholder to a concrete reference with explicit anchor links). Document each additional location in the SUMMARY.
- If 0 matches → abort (Plan 01 or another plan already resolved it). Skip Task 2; document no-op in SUMMARY.

Record the grep output verbatim in the task log for traceability.
  </action>
  <verify>
    <automated>grep -rnE "Phase 32|NAV-0[123]" docs/ 2>/dev/null | tee /tmp/phase32-placeholder-scan.txt; PLACEHOLDER_COUNT=$(wc -l < /tmp/phase32-placeholder-scan.txt | tr -d ' '); echo "placeholder_count=$PLACEHOLDER_COUNT"; test "$PLACEHOLDER_COUNT" -eq 1 || { echo "HALT: expected exactly 1 placeholder per D-36 plan-time verification, found $PLACEHOLDER_COUNT — autonomous execution cannot branch; request human review."; exit 1; }</automated>
  </verify>
  <done>
  Grep output captured and placeholder count recorded to /tmp/phase32-placeholder-scan.txt.
  **Deterministic assertion (autonomous safety):** placeholder count MUST equal exactly 1 (the known line 99 placeholder per D-36). If count != 1, the verify step exits non-zero and execution halts — deviation requires human review to determine whether the extra placeholders are (a) a new discovery to fold into Task 2 scope, or (b) the line 99 placeholder was already resolved by an earlier plan (Plan 01 glossary edits). The decision-logic prose above (0 / 1 / >1 branches) is retained as documentation for the human reviewer in the halt case. Task 2 proceeds ONLY after the automated assertion returns exit 0 with count=1.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite line 99 in 07-ios-triage.md with concrete glossary anchor links</name>
  <files>docs/decision-trees/07-ios-triage.md</files>
  <read_first>
    - docs/decision-trees/07-ios-triage.md lines 1-10 (frontmatter)
    - docs/decision-trees/07-ios-triage.md lines 90-107 (Related Resources + Version History)
    - docs/_glossary-macos.md (verify the 5 iOS anchors from Plan 01 exist: #supervision, #mam-we, #apns, #account-driven-user-enrollment, #jailbreak-detection; verify 5 macOS anchors exist: #abm, #ade, #vpp, #await-configuration, #setup-assistant)
    - .planning/phases/32-navigation-integration-references/32-CONTEXT.md (D-35 verbatim target text)
    - .planning/phases/32-navigation-integration-references/32-RESEARCH.md §Code Examples Example 4 (Placeholder Retrofit target text)
  </read_first>
  <behavior>
    - Line 99 rewritten to the D-35 verbatim target (with 10 inline anchor links to specific glossary terms).
    - Frontmatter `last_verified` + `review_by` bumped.
    - Version History row added ABOVE existing most-recent row.
    - Post-edit, `grep -rn "Phase 32|NAV-0[123]" docs/` returns 0 matches (or only matches inside the `.planning/` tree, which is excluded from docs/ scope).
  </behavior>
  <action>

**Edit 1 — Frontmatter bump:**
- Update `last_verified:` to `2026-04-17` and `review_by:` to `2026-07-16`.

**Edit 2 — Line 99 rewrite:**

Current line 99 (verified at plan time):
```
- [Apple Provisioning Glossary](../_glossary-macos.md) -- Shared Apple terminology (iOS glossary additions in Phase 32 NAV-01)
```

Replace with D-35 verbatim resolution text:
```
- [Apple Provisioning Glossary](../_glossary-macos.md) -- Shared Apple terminology covering iOS/iPadOS ([supervision](../_glossary-macos.md#supervision), [MAM-WE](../_glossary-macos.md#mam-we), [APNs](../_glossary-macos.md#apns), [account-driven user enrollment](../_glossary-macos.md#account-driven-user-enrollment), [jailbreak detection](../_glossary-macos.md#jailbreak-detection)) and macOS ([ABM](../_glossary-macos.md#abm), [ADE](../_glossary-macos.md#ade), [VPP](../_glossary-macos.md#vpp), [Await Configuration](../_glossary-macos.md#await-configuration), [Setup Assistant](../_glossary-macos.md#setup-assistant))
```

**Edit 3 — Version History row:**

Locate the Version History table (lines 101-106 approximately). INSERT a new row ABOVE the existing most-recent row (likely `2026-04-17 | Resolved Phase 31 L2 cross-references | -- |`):
```
| 2026-04-17 | Phase 32: resolved NAV-01 glossary placeholder at line 99 | -- |
```

If multiple rows share the date `2026-04-17`, order the new Phase 32 row FIRST (most recent semantic action).

**Extended scope (Task 1 decision):**
If Task 1's grep returned additional matches beyond line 99, apply the same D-35 pattern to each:
- If the placeholder is in a "Related Resources" / "See Also" context: rewrite with concrete anchor links to the glossary terms (as above).
- If the placeholder is in a runbook step or narrative prose: rewrite to inline concrete instruction / reference (e.g., "see [Apple Provisioning Glossary](../_glossary-macos.md) for iOS terminology" — concrete, not a Phase-reference promise).
- Each additional file edited gets a frontmatter bump + Version History entry per D-40.
  </action>
  <verify>
    <automated>grep -c "Phase 32 NAV-01" docs/decision-trees/07-ios-triage.md | grep -qE '^0$' && grep -c "_glossary-macos.md#supervision" docs/decision-trees/07-ios-triage.md | grep -qE '^1$' && grep -c "_glossary-macos.md#mam-we" docs/decision-trees/07-ios-triage.md | grep -qE '^1$' && grep -c "2026-04-17 | Phase 32: resolved NAV-01 glossary placeholder" docs/decision-trees/07-ios-triage.md | grep -qE '^1$' && test "$(grep -rnE "Phase 32|NAV-0[123]" docs/ 2>/dev/null | wc -l)" -eq 0 && bash .planning/phases/32-navigation-integration-references/validation/link-check.sh docs/decision-trees/07-ios-triage.md</automated>
  </verify>
  <done>Line 99 rewritten; old "Phase 32 NAV-01" string absent; 10 glossary anchor links present on line 99; Version History has new Phase 32 row; post-edit grep for `Phase 32|NAV-0[123]` in docs/ returns ZERO matches; link-check.sh exits 0 (all new anchors resolve to live targets in _glossary-macos.md per Plan 01's completion).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decision-tree line 99 → reader clicking into glossary | Reader follows the anchor link expecting content matching the anchor name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | Anchor link text | accept | Anchors are generic glossary terms, no tenant-specific content. No PII risk. |
</threat_model>

<verification>
- `grep -c "Phase 32 NAV-01" docs/decision-trees/07-ios-triage.md` returns 0 (placeholder resolved)
- `grep -rnE "Phase 32|NAV-0[123]" docs/ 2>/dev/null | wc -l` returns 0 (docs tree fully de-placeholdered)
- 10 glossary anchor links on line 99 (5 iOS + 5 macOS)
- link-check.sh exits 0 (all anchors resolve — depends on Plan 01 having completed)
- Version History has new Phase 32 row
</verification>

<success_criteria>
1. Single Phase 32 placeholder in docs/ resolved (per D-35 + D-36)
2. Post-retrofit grep returns zero matches for "Phase 32" or "NAV-01/02/03" across docs/
3. All 10 anchor links on line 99 resolve to existing glossary H3 anchors (depends on Plan 01 shipping first)
4. Frontmatter + Version History bumped per D-40
5. Mermaid block in 07-ios-triage.md UNCHANGED (D-38 additive-only posture; this is a single-line text edit, not a graph edit)
</success_criteria>

<output>
After completion, create `.planning/phases/32-navigation-integration-references/32-03-SUMMARY.md` documenting:
- Task 1 grep output (placeholder inventory)
- Task 2 actual scope executed (single line vs extended)
- Link-check confirmation that all 10 anchors on line 99 resolve
- Cross-reference: depends on Plan 01 having shipped the target anchors
</output>
