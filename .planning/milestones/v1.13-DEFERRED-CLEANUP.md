# v1.13 Deferred Cleanup — v1.14+ Backlog

**Authored:** 2026-06-29 (Phase 100 Plan 100-04 close-gate — canonical `.planning/milestones/` artifact)
**Milestone:** v1.13 (macOS Platform SSO Admin-Setup Documentation Accuracy & Depth)
**Status:** FINALIZED at Phase 100 Plan 100-04 close-gate `{phase_100_close_SHA}` (2026-06-29).

**Authoring rationale:** v1.13 inherits the standalone deferred-cleanup artifact convention from v1.12 close (Path-A from `.planning/milestones/v1.12-DEFERRED-CLEANUP.md`). Per the "**do NOT mask via deletion**" doctrine, all carried v1.12 open items are PRESERVED verbatim (they remain unresolved / indefinite; the validator chain is now deeper [48..99]) and three new v1.13 deferred items are ADDED (docs/index.md:108 stale count + WR-01 + IN-01, all docs-content fixes out of Phase-100 tooling/close-only scope). The two v1.12 Part A items (GLOSSARY-IRU-URL-FRESHNESS-01 and WR-02) are DROPPED because they were resolved — they are recorded Closed in `.planning/milestones/v1.13-MILESTONE-AUDIT.md`. The "do NOT mask via deletion" doctrine governs *open* items only; resolved items drop here and are transparently closed in the MILESTONE-AUDIT. (Mirrors v1.12's approach.)

**Scope:** This document enumerates deferred **requirements** and **open technical items** only. Working-tree cruft (`.claude/worktrees/agent-*`, `TEMPcp*.txt`, `docs.zip`, `.obsidian/`, `.agents/`, stale phase files) is out of scope per D-04 sub-4. Deletion of any stray pre-close `.planning/v1.13-MILESTONE-AUDIT.md` (if it existed) is deferred to `/gsd-complete-milestone` — no stray file existed at this close.

**Trigger-to-sweep:** Each section carries explicit criteria for when v1.14+ should pick up the work.

---

# Note: v1.12 Part A Items — DROPPED (Resolved during v1.13)

The two items that occupied Part A of `v1.12-DEFERRED-CLEANUP.md` are resolved:

- **GLOSSARY-IRU-URL-FRESHNESS-01** → closed as GLOS-01 (Phase 96 `b70d028` — `docs/_glossary-macos.md` Iru/Kandji entry updated to 3-URL reality: `support.iru.io` primary, `support.kandji.io` legacy redirect, `docs.iru.com` documentation source). Recorded Validated in MILESTONE-AUDIT. Note: the quarterly URL-liveness cadence was deliberately kept OUT of the durable glossary per Phase-96 D-04 (dated caveats live in guide 02) — this is NOT an unmet closure criterion.
- **WR-02** → fixed in Phase 99 (`5d6ee80` — `docs/decision-trees/06-macos-triage.md` corrected). Recorded Validated as part of RUN-01 closure. Do NOT re-touch `06-macos-triage.md`.

These items are **NOT carried here** because they are genuinely resolved. Their full original text can be found in `.planning/milestones/v1.12-DEFERRED-CLEANUP.md` Part A. The doctrine of "do NOT mask via deletion" applies to *open* items only — silently continuing to carry resolved items would itself mask their resolution.

---

# Part A — v1.13 New Deferrals

## docs/index.md:108 — Stale Runbook Count

**Scope:** `docs/index.md:108` contains a phrase reading "(6 runbooks…)" or equivalent text asserting 6 macOS L1 runbooks. This count is defensibly stale: v1.13 Phase 99 added L1 runbook #37, and v1.10 added L1 runbooks #35 and #36, bringing the current macOS L1 runbook count to at least 8. The stale count should be updated to reflect the actual runbook count at v1.14+ authoring time.

**Exact cite:** `docs/index.md:108` (the stale count row). This is NOT `docs/decision-trees/06-macos-triage.md:100/101` (which is an escalation-collect row — the Finder mis-cited this in Phase 97 deferred-items.md; the correct cite is `docs/index.md:108`).

**Omission reason:** Phase 100 is tooling/close-only. Corpus edits (including docs/index.md) are strictly out of scope for this phase per D-02 and the Phase-100 boundary. The stale count was discovered at Phase 99 scope review time.

**Where it belongs when resolved:** Update `docs/index.md:108` to reflect the actual macOS L1 runbook count at the time of the v1.14+ corpus-editing phase.

**Resolution trigger (v1.14+):** First corpus-editing v1.14+ phase. Single-line fix — bundle with any docs-editing phase rather than warranting its own standalone phase.

**Estimated effort:** < 1 plan (single line update + count verification).

**Status:** NEW DEFERRAL at v1.13 close-gate. A corpus follow-up for v1.14+. **NOT** a Phase-100 edit (Phase 100 is tooling-only).

---

## WR-01: quick-ref-l1.md:101 Mislabels L1 #36 as L2 Escalation Target

**Scope:** `docs/quick-ref-l1.md:101` still reads "Escalate L2 via [Secure Enclave Key Loss]…" or equivalent text, mislabeling L1 #36 as an L2 escalation target. L1 #36 (`36-macos-secure-enclave-key.md`) is an L1 runbook — it should be listed as an L1 "Try this first" entry in quick-ref-l1.md, not as an L2 escalation target.

**Omission reason:** Phase 100 is tooling/close-only. WR-01 was surfaced at Phase 99 scope review and confirmed OPEN (verified: the row at docs/quick-ref-l1.md:101 still carries the mislabeled escalation framing at Phase 100 close-gate time). Fixing this is a docs-content edit — strictly out of Phase-100 scope per D-02.

**Where it belongs when resolved:** Update `docs/quick-ref-l1.md:101` to correctly surface L1 #36 as an L1 "Try this first" entry (not an L2 escalation). Also check the surrounding rows for L1 #37 (new Phase 99 runbook) wiring.

**Resolution trigger (v1.14+):** First corpus-editing v1.14+ phase. Single-line fix — bundle with any docs-editing phase.

**Estimated effort:** < 1 plan (single row correction + cross-validation against the L1 runbook list).

**Status:** OPEN at Phase 100 close. NEW DEFERRAL at v1.13 close-gate. A corpus follow-up for v1.14+.

---

## IN-01: common-issues.md:242-247 Skips L1 #36 Mandatory-Re-Registration Intermediate

**Scope:** `docs/common-issues.md:242-247` covers the locked-out macOS account recovery scenario. The current entry goes L1 #37 → L2 #27 without surfacing the L1 #36 (`36-macos-secure-enclave-key.md`) mandatory PSSO re-registration intermediate. After recovering local access via the local-password-reset procedure (L1 #37), users on Secure Enclave PSSO devices MUST re-register their PSSO credentials — this is the L1 #36 step. Skipping it in the common-issues entry creates a recovery path that leaves the device without valid PSSO credentials.

**Omission reason:** Phase 100 is tooling/close-only. IN-01 was surfaced at Phase 99 scope review and confirmed OPEN. Fixing this is a docs-content edit — out of Phase-100 scope per D-02.

**Where it belongs when resolved:** Update `docs/common-issues.md:242-247` to insert the L1 #36 PSSO re-registration step as a mandatory intermediate between L1 #37 (local password reset) and L2 #27 escalation.

**Resolution trigger (v1.14+):** First corpus-editing v1.14+ phase. Multi-line edit to the locked-out scenario — bundle with any docs-editing phase.

**Estimated effort:** < 1 plan (3-5 line insertion + cross-validation against L1 #36 and L1 #37 content).

**Status:** OPEN at Phase 100 close. NEW DEFERRAL at v1.13 close-gate. A corpus follow-up for v1.14+.

---

## WINDOWS-CLONE-DEEPNEST-TIMEOUT-01: Chain-Apex Cold-Clone Deep-Nesting Subprocess Cost on Windows (CARRIED FORWARD — WORSE at Phase 100 + D-03 CORRECTED)

**Scope:** Running ANY chain validator from a cold fresh-clone on Windows exhibits an O(n²) cascade of cold Node subprocess spawns at the deepest nested guards, approaching the per-subprocess timeout and producing spurious failures or hangs. **Carried forward from v1.12; WORSE at Phase 100** because the apex chain is now `[48..99]` (+5 deeper than Phase 95's `[48..94]`, +12 deeper than Phase 88's `[48..87]`). **D-03 OS-SPLIT (v1.12 correction extended at v1.13):** BOTH chain validators cascade — the apex `check-phase-100.mjs` (chain [48..99]) AND the continuity validator `check-phase-95.mjs` (chain [48..94]).

**Evidence of correctness (not a regression):**
- Phase 88 (v1.10): Windows warm-tree `check-phase-88.mjs` runs 1 and 2: 41 PASS / 1 FAIL / 1 SKIPPED — identical FAIL on V-88-CHAIN-66 (check-phase-66 subprocess output truncated). `check-phase-66.mjs` standalone: 28/0/0 — confirms NOT a real failure.
- Phase 93 (v1.11): Windows cold-clone `check-phase-88.mjs` (continuity, [48..87]) did not return within 3+ minutes during the Axis-1 re-audit (12+ live `node` processes); empirically confirmed same cascade on cold clone.
- Phase 95 (v1.12): BOTH `check-phase-93.mjs` (continuity, [48..92]) AND `check-phase-95.mjs` (apex, [48..94]) confirmed as chain validators that cascade. D-03 corrected.
- Phase 100 (v1.13): BOTH `check-phase-95.mjs` (continuity, [48..94]) AND `check-phase-100.mjs` (apex, [48..99]) confirmed as chain validators that cascade. Neither was run on Windows cold-clone Axis-1. Linux GHA apex (54/0/1) and continuity (50/0/0) are exclusively authoritative.
- Linux GHA apex (check-phase-100, [48..99]): 54/0/1 — no truncation. Linux GHA is exclusively authoritative for BOTH chain validators.

**D-03 OS-split (v1.13 — extends v1.12 correction):**
- `check-phase-100.mjs` (v1.13 apex, CHAIN `[48..99]`, 52 phases) → Linux GHA **sole-authoritative**: **54/0/1**.
- `check-phase-95.mjs` (v1.12 apex / v1.13 continuity, CHAIN `[48..94]`, 47 phases) → Linux GHA **sole-authoritative**: **50/0/0** (V-95-AUDIT now PASS post-v1.12 close; confirmed CHECK_PHASE_NESTED=1 + GHA chain exits 0).

**Prior-misclassification caveat (documented per D-04 in MILESTONE-AUDIT):** Phase-97 `deferred-items.md` mis-read the Windows deep-nest cascade as a genuine check-phase-66 failure. The correct interpretation (confirmed by check-phase-66 standalone 28/0/0 exit 0 and Linux GHA PASS) is that exit-124 is a 60s external-`timeout` shell artifact, not a check-phase-66 internal failure.

**Source:** v1.9 `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` (Plan 82-03) → v1.10 carry → v1.11 carry → v1.12 carry (`95-03-AUDIT-RESULTS.md`) → v1.13 carry (`100-03-AUDIT-RESULTS.md`). Depth now **[48..99]** (+5 from v1.12's [48..94]).

**Mitigation adopted (Phase 100, per D-03 MANDATORY — extended from v1.12):** The authoritative chain validator counts are taken from **Linux GHA** (exclusively authoritative per D-03 corrected binding condition), NOT from a Windows warm-tree or cold-clone chain-validator run. The 5 genuine LEAF validators (v1.13-audit + check-phase-96..99) are taken from fast standalone fresh-clone runs and confirmed cross-OS EXACT MATCH.

**Remediation options (v1.14+):** (1) cache-warm clone before the apex run; (2) raise the per-subprocess timeout for cold-clone re-audits; (3) cache chain-validator subprocess results within a single apex invocation to avoid redundant sub-spawns at depth ≥ 2 (the O(n²) mitigation — increasingly attractive as the chain deepens past [48..99]).

**Trigger-to-sweep (v1.14+):** The next terminal re-audit setup (or when chain depth affects warm-tree performance). Note: as the chain grows past `[48..99]`, option (3) becomes the durable fix.

**Estimated effort:** 30min (cache-warm) / 15min (timeout config) / 2-4h (O(n²) subprocess caching).

**Status:** CARRIED FORWARD from v1.12 (still unresolved; chain now deeper [48..99], +5 from v1.12's [48..94]; D-03 corrected: BOTH chain validators Linux-authoritative). DEFERRED to v1.14+. Does NOT affect harness correctness or cross-OS parity.

---

# Part B — Carried-Forward v1.12 Deferred Items (PRESERVED verbatim per "do NOT mask via deletion")

*The following sections are preserved from `.planning/milestones/v1.12-DEFERRED-CLEANUP.md` Part B (which carried them from v1.11). They remain unresolved / indefinite at v1.13 close. Source-of-truth, per-site root-cause tables, and trigger gates are unchanged; see the v1.12 artifact for full detail.*

## MTPSSO-01: Cross-Tenant Platform Credential Registration Models

**Scope:** A macOS fleet registering Platform Credentials (Secure Enclave keys) against multiple Entra tenants simultaneously — cross-tenant device-registration architectures, shared-device scenarios where a single macOS device must authenticate to two or more organizations, and the associated Platform SSO extension configuration for multi-tenant environments.

**Omission reason:** Net-new architectural scope. v1.10 scoped to single-tenant PSSO follow-ons (PSSO-FUT-01/02/04). Multi-tenant PSSO (PSSO-FUT-03) was explicitly DEFERRED via `/adversarial-review` Area 3 decision: "net-new architecture, not authentication-doc cleanup; folding it in would break the single-feature-content-milestone convention."

**Source:** v1.10 scoping (REQUIREMENTS.md v2 Requirements §MTPSSO-01; adversarial-review Area 3 → DEFERRED).

**Where it belongs when resolved:** A new architectural decision doc + admin-setup section + L2 investigation runbook (multi-tenant requires its own planning surface — distinct from the single-tenant Platform SSO guide 07).

**Resolution trigger (v1.14+):** Multi-tenant PSSO deployment demand surfaces from customer support, or Microsoft publishes official multi-tenant Platform SSO guidance.

**Estimated effort:** 4-8 plans across 1-2 phases (architecture decisions + cross-tenant registration + CA scoping).

**Status:** DEFERRED to v1.14+. Area 3 DEFERRED from adversarial-review 2026-06-22. *(Carried from v1.10 → v1.11 → v1.12 → v1.13.)*

---

## MTPSSO-02: Multi-Tenant Conditional Access / Compliance Scoping for PSSO

**Scope:** Conditional Access policy scoping when a single macOS device registers Platform SSO credentials with multiple Entra tenants — cross-tenant compliance evaluation, tenant-specific CA policy enforcement, compliance state propagation across tenant boundaries.

**Omission reason:** Dependent on MTPSSO-01 architectural decisions and multi-tenant Platform SSO deployment patterns; out of scope for the single-tenant v1.10 content milestone.

**Source:** v1.10 scoping (REQUIREMENTS.md v2 Requirements §MTPSSO-02).

**Where it belongs when resolved:** Companion section to MTPSSO-01 architectural doc, or a dedicated CA policy guide for multi-tenant macOS deployments.

**Resolution trigger (v1.14+):** After MTPSSO-01 architectural doc lands; when multi-tenant CA scoping patterns are confirmed from Microsoft / Apple.

**Estimated effort:** 1-2 plans (CA policy scoping + compliance state + L2 troubleshooting patterns).

**Status:** DEFERRED to v1.14+. Dependent on MTPSSO-01. *(Carried from v1.10 → v1.11 → v1.12 → v1.13.)*

---

## MTPSSO-03: Multi-Tenant PSSO L2 Troubleshooting

**Scope:** L2 investigation runbook for multi-tenant Platform SSO failures — diagnosing authentication failures that span tenant boundaries, identifying which tenant's token/credential failed, log collection across multiple tenants, and the delete-and-re-register flow in a multi-tenant context.

**Omission reason:** Dependent on MTPSSO-01/02 architecture and deployment patterns; out of scope for v1.10.

**Source:** v1.10 scoping (REQUIREMENTS.md v2 Requirements §MTPSSO-03).

**Where it belongs when resolved:** New L2 runbook in `docs/l2-runbooks/` (numbered after L2 #30) alongside the multi-tenant admin setup content from MTPSSO-01/02.

**Resolution trigger (v1.14+):** After MTPSSO-01 and MTPSSO-02 land; when L2 engineers encounter multi-tenant PSSO escalations.

**Estimated effort:** 1 plan (L2 runbook authoring + 00-index extension + nav-hub integration).

**Status:** DEFERRED to v1.14+. Dependent on MTPSSO-01/02. *(Carried from v1.10 → v1.11 → v1.12 → v1.13.)*

---

## PSSO-FUT-03: Multi-Tenant PSSO — Deferral Notation

**Scope:** PSSO-FUT-03 covers multi-tenant Platform SSO deployment configuration — the Platform SSO extension settings and policy behavior when a single macOS device must register against multiple Entra tenants simultaneously. This is the configuration/admin-setup counterpart to MTPSSO-01/02/03's architecture and troubleshooting scope.

**Note:** PSSO-FUT-03 was not explicitly itemized as a standalone deferral in v1.12-DEFERRED-CLEANUP.md (it was subsumed within the MTPSSO-01/02/03 deferral narrative from v1.10 scoping). It is carried here as an explicit notation to ensure no loss of the multi-tenant PSSO deferral intent across v1.13 close.

**Status:** DEFERRED to v1.14+. Dependent on MTPSSO-01. *(Carried from v1.10 scoping; explicit notation at v1.13 close.)*

---

## KRBFUT-01: On-Prem-AD-Only Kerberos Realm Deep-Dive

**Scope:** Kerberos realm configuration for environments with on-premises Active Directory ONLY — without Entra ID cloud Kerberos (no `usePlatformSSOTGT`, no Azure Files Cloud-Kerberos). This includes: standalone on-prem KDC discovery (DNS SRV records, site-based routing), AD site topology for Kerberos, realm trust configurations for multi-forest environments, and Kerberos ticket lifecycle in air-gapped / non-internet-connected AD deployments.

**Omission reason:** v1.10 guide 10 (`10-kerberos-sso-extension.md`) is scoped to the Intune/Entra baseline — it documents the Microsoft Enterprise Kerberos (cloud) path and PSSO+Kerberos TGT integration as the primary deployment patterns. On-prem-AD-only deep-dive was explicitly kept out of scope.

**Source:** v1.10 scoping (REQUIREMENTS.md v2 Requirements §KRBFUT-01).

**Where it belongs when resolved:** An extended section in guide 10 or a companion advanced guide (`10b-kerberos-onprem-adonly.md`) covering purely on-prem-AD Kerberos (no Entra dependency).

**Resolution trigger (v1.14+):** Customer demand for on-prem-AD-only (no Entra) Kerberos SSO extension deployment patterns surfaces explicitly; or Apple / Microsoft publishes guidance specifically for this topology.

**Estimated effort:** 2-3 plans (on-prem AD Kerberos config + realm trust + diagnostics + L2 troubleshooting for this path).

**Status:** DEFERRED to v1.14+. *(Carried from v1.10 → v1.11 → v1.12 → v1.13.)*

---

## KRBFUT-02: Azure Files Cloud-Kerberos Full Coverage (GA Promotion)

**Scope:** Full documentation of Azure Files Cloud-Kerberos on macOS — replacing the v1.10 limited-preview callout in guide 10 with complete admin setup, L2 troubleshooting for Azure Files + Kerberos authentication failures, and the interaction between Platform SSO TGT and Azure Files Cloud-Kerberos.

**Omission reason:** Azure Files Cloud-Kerberos was in **limited preview** as of v1.10 authoring (2026-06-23). Shipping speculative limited-preview behavior as a full guide risks doc-reality mismatch at GA.

**Source:** v1.10 scoping (KRB-02 documents Azure Files Cloud-Kerberos as limited-preview callout).

**Where it belongs when resolved:** Extended section in guide 10 (replacing the limited-preview callout) + companion L2 runbook section.

**Resolution trigger (v1.14+):** Microsoft announces Azure Files Cloud-Kerberos for macOS as generally available (GA). Check quarterly: `https://learn.microsoft.com/en-us/azure/storage/files/storage-files-active-directory-enable-azure-files-cloud-kerberos`.

**Estimated effort:** 1-2 plans (guide 10 GA section + L2 runbook section + nav-hub update if new runbook).

**Status:** DEFERRED to v1.14+. Azure Files Cloud-Kerberos limited-preview as of 2026-06-23. *(Carried from v1.10 → v1.11 → v1.12 → v1.13.)*

---

## MIGFUT-01: iOS/iPadOS MDM Migration Walkthrough

**Scope:** Document the iOS/iPadOS ABM "Assign Device Management" + Deadline migration path from Kandji/Iru to Intune — specifically covering how iOS/iPadOS forced-restart + mandatory-enrollment deadline behavior differs from macOS full-screen lock enforcement, and any PSSO or enrollment profile re-registration requirements that differ from the macOS path.

**Omission reason:** v1.12 is macOS-scoped. The iOS/iPadOS migration path was not addressed because the MIGV gaps (MIGV-01/02/03) were all macOS-specific and the scope was locked to guide 02 (macOS migration walkthrough) only.

**Source:** v1.12 REQUIREMENTS.md Out of Scope section: "iOS/iPadOS migration depth (v1.12 is macOS-scoped — MIGFUT-01)."

**Where it belongs when resolved:** A new iOS/iPadOS migration walkthrough document (in `docs/ios-lifecycle/` or a cross-platform migration section) covering the iOS/iPadOS-specific deadline enforcement, forced-restart behavior, and post-migration enrollment verification.

**Resolution trigger (v1.14+):** Customer demand for iOS/iPadOS Kandji/Iru → Intune migration walkthroughs surfaces, or Apple publishes iOS/iPadOS-specific ABM migration guidance.

**Estimated effort:** 1-2 plans (iOS/iPadOS migration walkthrough + L2 runbook update if needed).

**Status:** NEW DEFERRAL at v1.11 close-gate; CARRIED from v1.11 → v1.12 → v1.13. DEFERRED to v1.14+.

---

## MIGFUT-02: Jamf Pro / Mosyle / Other Source-MDM-Specific Release Steps

**Scope:** The ABM "Assign Device Management" + Deadline migration path is MDM-agnostic on the ABM side, but source-MDM release steps (secret retrieval, device deletion, agent removal wait) vary by vendor. v1.11 / v1.12 document Kandji/Iru source-side steps only. Jamf Pro, Mosyle, SimpleMDM, and other common source MDMs have different admin console paths for FileVault key retrieval, Activation Lock bypass, and device record deletion.

**Omission reason:** v1.11 was scoped to Kandji/Iru (the specific source MDM named in the Phase-90 research gaps). v1.12 confirmed and corrected the Iru post-rebrand paths but did not expand to other source MDMs.

**Source:** v1.12 REQUIREMENTS.md Out of Scope section (MIGFUT-02).

**Where it belongs when resolved:** An addendum to `docs/macos-lifecycle/02-mdm-migration-psso.md` with source-MDM-specific tabs or subsections for Jamf Pro, Mosyle, etc. Or a companion reference doc for source-MDM release steps.

**Resolution trigger (v1.14+):** Customer demand for Jamf Pro / Mosyle source-MDM migration steps surfaces, or the Kandji/Iru section proves reusable as a template for other MDMs.

**Estimated effort:** 1 plan per additional source MDM (research + documentation + verification).

**Status:** NEW DEFERRAL at v1.11 close-gate; CARRIED from v1.11 → v1.12 → v1.13. DEFERRED to v1.14+.

---

# Part C — Carried-Forward v1.8 Deferred Items (PRESERVED verbatim per "do NOT mask via deletion")

*The following sections are preserved from `.planning/milestones/v1.12-DEFERRED-CLEANUP.md` Part C (which carried them from v1.11 → v1.10 → v1.9 → v1.8). They remain unresolved / indefinite at v1.13 close.*

## ARCHIVE-UPSTREAM-01: Upstream PR to get-shit-done-cc for extractOneLinerFromBody regex fix

Submit the corrected `extractOneLinerFromBody` regex as an upstream PR to `get-shit-done-cc` (`sdk/src/query/phase-lifecycle-policy.ts:54-58`). Phase 71 Plan 71-01 vendored the corrected regex locally (`scripts/archive/extract-summary-oneliners.mjs`, SHA `e4887b2`); the upstream defect still ships in published releases. **Trigger:** upstream releases a fixed version + the project pins to it → submit a deprecation commit removing the vendored fallback. **Status:** DEFERRED (vendored fallback canonical until upstream lands). *(Carried from v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13.)*

## v1.1 line 164 `Edit N --` token-class — extension-point flag for future archive-defect classes

The `check-phase-71.mjs` `PLACEHOLDER_TOKENS` list (11 entries, with a regex-fragment escape-branch for `Edit \d+ --`) is the canonical extension surface for future SDK-extractor placeholder classes. **Trigger:** reactive — when a future archive-defect class surfaces, add the new token. No proactive v1.14+ pickup required. **Status:** documented for awareness. *(Carried from v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13.)*

## HELPER-SPAWN-STDERR-01: Helper-spawn stderr-only wrapper surface (3 sites)

3 helper-spawn stderr-only catch-block sites in `check-phase-{48, 60, 61}.mjs` (spawning `regenerate-supervision-pins.mjs --self-test`) carry a distinct failure-mode + slice-budget profile from CHAIN+AUDIT wrappers. **Trigger:** v1.14+ chain-validator retrospective; apply the CHAIN-wrapper fix with a smaller slice budget (~200 chars) + `--self-test` discriminator. **Status:** DEFERRED to v1.14+. *(Carried from v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13.)*

## FROZEN-AWARE-ADOPTION-SWEEP-01: Inline frozen-aware helper refactor to _lib/frozen-at-close.mjs consumption

~13 inline frozen-aware helpers across `check-phase-{61, 67, 68, 70}.mjs` pre-date the centralized `_lib/frozen-at-close.mjs` module and have not been refactored to consume it (helper-signature drift: v1.5-family lacks `stdio` hardening; v1.7-family has it). **Trigger:** v1.14+ chain-validator retrospective, or when a new validator needs v1.5-family frozen-aware reads. **Status:** DEFERRED to v1.14+. *(Carried from v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13.)*

## EXEC-FAIL-DETAIL-EXTRACTION-01: DRY _lib/exec-fail-detail.mjs across all wrappers

Extract a `_lib/exec-fail-detail.mjs` helper DRYing the `(stdout + stderr).slice(0, NNN).trim()` pattern duplicated across ~18-21 CHAIN+AUDIT+helper-spawn wrapper sites. **Trigger:** v1.14+ clean tooling-refactor phase, or when adding a new wrapper class. **Status:** DEFERRED to v1.14+. *(Carried from v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13.)*

## CI-3: Managed Apple ID → Managed Apple Account Corpus Rename (45 occurrences / 16 files)

Corpus-wide rename pending Microsoft Intune adopting the rebrand portal-side (admin would read "Managed Apple Account" in docs but see "Managed Apple ID" in the Intune portal until then). **Trigger:** Intune adopts the rebrand portal-side (post-2026-07-01 quarterly tutorial-refresh check). **Status:** DEFERRED to v1.14+. *(Carried from v1.6 → v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13; adversarial-review at v1.12 scoping reconfirmed DEFERRED — byte-unchanged hazard + motivated scope-padding.)*

## Multi-tenant Apple Business surfaces (DEFERRED indefinitely)

Multi-tenant Apple Business workflows (cross-tenant device transfer, multi-org-account scenarios). Net-new architectural scope, not cleanup. **Trigger:** acute customer demand. **Status:** DEFERRED indefinitely. *(Carried from v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13.)* *(Distinct from MTPSSO-01/02/03, which is multi-tenant **Platform SSO**.)*

## Apple Business Device API documentation (DEFERRED to v1.14+)

Document the Apple Business Device API once Apple publishes developer.apple.com landing. **Trigger:** Apple publishes the developer landing page (cron-monitor `https://developer.apple.com/documentation/applebusiness/`). **Status:** DEFERRED to v1.14+. *(Carried from v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13.)*

## Per-OU Conference Room Display deep-dive (TRACKER ONLY)

Per-OU CRD partitioning patterns; Apple deployment guides are thin on specifics; no acute demand surfaced across v1.6-v1.13. **Trigger:** acute customer demand for per-OU CRD partitioning. **Status:** TRACKER ONLY. *(Carried from v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13.)*

## Account Holder lockout dedicated recovery runbook (CONDITIONAL)

Currently a 7-leaf branch in L2 #26. Promote to a standalone L1 #35-equivalent / L2 runbook only if escalations surface. **Trigger:** 5+ Account Holder lockout escalations per quarter. **Status:** CONDITIONAL. *(Carried from v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13.)*

## Apple School Manager (ASM) education-specific surfaces (DEFERRED to v1.14+)

ASM education-tenant surfaces (student/teacher/staff roles, classroom-mode, shared iPad for classroom, School Configurator). Outside the enterprise scope maintained across v1.0–v1.13. **Trigger:** education-tenant scope explicitly opens. **Status:** DEFERRED to v1.14+. *(Carried from v1.8 → v1.9 → v1.10 → v1.11 → v1.12 → v1.13.)*

---

## Cross-References

- **v1.13 milestone audit:** `.planning/milestones/v1.13-MILESTONE-AUDIT.md`
- **v1.12 predecessor (single-source-of-truth for Part B/C carried items):** `.planning/milestones/v1.12-DEFERRED-CLEANUP.md`
- **Phase 100 3-axis re-audit results (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 evidence + D-03 extension):** `.planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-03-AUDIT-RESULTS.md`
- **Phase 100 close-gate verification:** `.planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-VERIFICATION.md`
- **v1.13 requirements (HARN-01/02/03 Validated; ACC/TS/DEP/RUN/GLOS-01 Validated by Phases 96-99):** `.planning/REQUIREMENTS.md`

---

*Authored 2026-06-29 at Phase 100 Plan 100-04 close-gate (`{phase_100_close_SHA}`). Part A: docs/index.md:108 stale-count (NEW) + WR-01 (quick-ref-l1.md:101 mislabel, NEW) + IN-01 (common-issues.md:242-247 missing L1 #36 intermediate, NEW) + WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 (chain [48..99], +5 deeper, D-03 extended: BOTH chain validators Linux-authoritative). Part B: MTPSSO-01/02/03 + PSSO-FUT-03 (explicit notation) + KRBFUT-01/02 + MIGFUT-01/02 (carried from v1.12). Part C: 10 carried v1.8 items PRESERVED (do-NOT-mask-via-deletion doctrine). v1.12 Part A items (GLOSSARY-IRU-URL-FRESHNESS-01, WR-02) DROPPED — resolved during v1.13 (GLOS-01 Phase 96 `b70d028`; WR-02 Phase 99 `5d6ee80`) and recorded Closed (Validated) in v1.13-MILESTONE-AUDIT.md. Working-tree cruft cleanup is out of scope per D-04 sub-4 (deferred to /gsd-complete-milestone). v1.14+ routing confirmed for all open items.*
