---
phase: 119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal
plan: 02
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - scripts/validation/v1.15-milestone-audit.mjs
  - scripts/validation/v1.15-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
autonomous: true
requirements: [HARN-03]
must_haves:
  truths:
    - "v1.15-milestone-audit.mjs runs C1-C17 and exits 0 on the current post-Phase-118 tree"
    - "C17 is folded in as check id 17 via execFileSync subprocess-spawn of c17-eee-contract.mjs (NOT an inline copy of the 13 assertions)"
    - "v1.15-audit-allowlist.json line-pins are re-verified/repointed per the 119-01 baseline diagnostic (not copied verbatim)"
    - "BASELINE_19 freshness comment is back-anchored to the JIT pre-Atom-1 HEAD SHA, never the close-gate SHA"
    - "Atom 1 lands as ONE indivisible commit; Atom 1 is never folded into Atom 2"
  artifacts:
    - path: "scripts/validation/v1.15-milestone-audit.mjs"
      provides: "13th Path-A milestone audit harness, C1-C17"
      contains: "id: 17"
    - path: "scripts/validation/v1.15-audit-allowlist.json"
      provides: "repointed sidecar allow-list"
      contains: "119-frozen-surface-re-baseline"
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "BASELINE_19 freshness audit-trail comment"
      contains: "BASELINE_19"
  key_links:
    - from: "v1.15-milestone-audit.mjs check id 17"
      to: "scripts/validation/c17-eee-contract.mjs"
      via: "execFileSync('node', ['scripts/validation/c17-eee-contract.mjs'])"
      pattern: "execFileSync.*c17-eee-contract"
    - from: "v1.15-milestone-audit.mjs parseAllowlist"
      to: "scripts/validation/v1.15-audit-allowlist.json"
      via: "readFile of the sidecar path"
      pattern: "v1.15-audit-allowlist.json"
---

<objective>
Atom 1 (SC1, HARN-03) — ONE indivisible commit shipping the harness core of the 13th Path-A lineage bump: `v1.15-milestone-audit.mjs` (C1-C16 inherited verbatim from v1.14 + C17 folded in), the repointed `v1.15-audit-allowlist.json` sidecar, and the BASELINE_19 freshness comment in `regenerate-supervision-pins.mjs`.

Purpose: This is the first of the two locked atoms. It carries the corpus-wide lint surface. C17 (authored standalone in Phase 115) is folded in here per its own header directive — the first milestone where the Path-A harness gates a corpus-wide structural contract, not just terminology/link checks.

Output: three files, ONE commit `docs(119): Atom 1 — v1.15 milestone audit harness (C1-C17) + sidecar + BASELINE_19`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-CONTEXT.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-RESEARCH.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-PATTERNS.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author v1.15-milestone-audit.mjs — copy v1.14, repoint, fold C17 as check id 17</name>
  <files>scripts/validation/v1.15-milestone-audit.mjs</files>
  <read_first>
    - scripts/validation/v1.14-milestone-audit.mjs (in full, 982 lines — the Path-A copy source; C1-C16 bodies copied VERBATIM, do not re-derive)
    - .planning/phases/119-.../119-PATTERNS.md §Pattern 1 (exact header repoint diff, sidecar-load repoint at line 79, the check-17 subprocess-spawn block, the CRLF readFile helper) + §Shared Patterns "Subprocess-spawn (execFileSync)"
    - .planning/phases/119-.../119-RESEARCH.md §Code Examples #1 (exact string-repoint diff for header lines 1-36; the new check id 17 block; `import { execFileSync } from 'node:child_process'`) + Open Question 2 (use _lib/exec-fail-detail.mjs for failure formatting)
    - scripts/validation/c17-eee-contract.mjs lines 1-60 (the fold source: self-executing, no exports, header names Phase 119 as fold-consumer)
    - scripts/validation/_lib/exec-fail-detail.mjs (the execFailDetail signature to reuse for C17 failure detail)
    - scripts/validation/check-phase-112.mjs lines 126-149 (the existing AUDIT-HARNESS subprocess-spawn pattern the C17 fold mirrors exactly)
  </read_first>
  <action>
    Copy scripts/validation/v1.14-milestone-audit.mjs → scripts/validation/v1.15-milestone-audit.mjs. Repoint every v1.14/1.14/112 token in the header comment block (lines 1-36) to v1.15 and the 119 phase directory: advance the lineage list to end "→ v1.14 → v1.15"; state "C1-C16 inherited verbatim from v1.14 [no freshness-threshold change this milestone]" (v1.15 does NOT touch C5/C10 60d/90d logic — a diff touching C5/C10 is a regression); "C17 (EEE contract) FOLDED IN per Phase 119 D-119 [subprocess-spawn of c17-eee-contract.mjs — see check id 17]"; repoint the Source-of-truth line to the 119 CONTEXT.md path; repoint the Sidecar allow-list line to v1.15-audit-allowlist.json.

    Repoint the single sidecar-path literal at the parseAllowlist() call site (line ~79) from 'scripts/validation/v1.14-audit-allowlist.json' to 'scripts/validation/v1.15-audit-allowlist.json' (confirmed single call-site).

    Add `import { execFileSync } from 'node:child_process';` to the import block, and add `import { execFailDetail } from './_lib/exec-fail-detail.mjs';` if not already present. Correct the now-false header line "File reads only ... no shell invocations" — state honestly that C17 spawns a subprocess (honest-accounting requirement, not optional).

    Append a NEW check object with `id: 17`, name "C17: EEE document contract (13 assertions, all enrolled docs/ files)", whose run() calls `execFileSync('node', ['scripts/validation/c17-eee-contract.mjs'], { stdio: 'pipe', timeout: 300000, cwd: process.cwd() })` inside try/catch, returning pass on exit 0 and pass:false with execFailDetail(stdout, stderr, {...}) on non-zero. Use the argument-array form (never a concatenated shell string) — this is the project's one process-level security control. Do NOT inline-copy the 13 assertions (that creates a divergence-prone second copy of D1_MAP/assertion logic — explicitly rejected in RESEARCH Don't-Hand-Roll). Leave C1-C16 bodies and the runner loop byte-unchanged.
  </action>
  <verify>
    <automated>bash -c 'node scripts/validation/v1.15-milestone-audit.mjs --self-test >/dev/null 2>&1 && grep -q "id: 17" scripts/validation/v1.15-milestone-audit.mjs && grep -q "node:child_process" scripts/validation/v1.15-milestone-audit.mjs && echo ATOM1_HARNESS_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validation/v1.15-milestone-audit.mjs` contains the literal `id: 17`
    - The file contains `import { execFileSync } from 'node:child_process'` and an `execFileSync('node', ['scripts/validation/c17-eee-contract.mjs'` invocation (argument-array form; no string-concatenated shell)
    - The file references `scripts/validation/v1.15-audit-allowlist.json` and contains ZERO occurrences of `v1.14-audit-allowlist.json`
    - `node scripts/validation/v1.15-milestone-audit.mjs --self-test` exits 0
    - The header no longer claims "no shell invocations"; the lineage line ends "→ v1.14 → v1.15"
  </acceptance_criteria>
  <done>v1.15-milestone-audit.mjs exists as a faithful Path-A copy of v1.14 with C17 folded in via subprocess-spawn; --self-test exits 0; C1-C16 unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Author v1.15-audit-allowlist.json — repoint phase + re-verify shifted line-pins (no verbatim copy)</name>
  <files>scripts/validation/v1.15-audit-allowlist.json</files>
  <read_first>
    - scripts/validation/v1.14-audit-allowlist.json (in full, 531 lines — the copy source; all {file,line} pins)
    - .planning/phases/119-.../119-01-SUMMARY.md §"Baseline v1.14 harness diagnostic" (the authoritative shifted-pin worklist produced by Plan 119-01 Task 2)
    - .planning/phases/119-.../119-RESEARCH.md §Code Examples #2 (RE-144 android-capability-matrix pin-shift; next_review cadence) + §Pattern 2 CRITICAL note
    - docs/reference/android-capability-matrix.md (RE-144 — re-read the current line positions of any pinned SafetyNet/supervision/knox lines flagged in the 119-01 worklist)
  </read_first>
  <action>
    Copy scripts/validation/v1.14-audit-allowlist.json → scripts/validation/v1.15-audit-allowlist.json. Repoint the "phase" field from "112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl" to "119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal". Update the "generated" timestamp to the current date.

    For EACH shifted line-pin identified in the 119-01 baseline diagnostic worklist (primarily docs/reference/android-capability-matrix.md / RE-144, whose Phase-118 EEE header-block + Version-History-row insertions shifted its C1/C2/C7 SafetyNet/supervision/knox pins), repoint the {file,line} value to the ACTUAL current line number verified against the live file — do NOT copy the v1.14 line numbers verbatim. If 119-01 reported the v1.14 harness already exits 0 clean on HEAD, then only the phase-string repoint is required (no pin changes) — record that determination explicitly. Do NOT touch pins against files outside the Phase-1 retrofit scope (docs/_glossary-android.md and other v1.16-deferred structural files were NOT retrofitted — their pins are unchanged).

    Refresh `c13_rotting_external.quarterly_audit.next_review` only if the 119-01 cadence flag says it is stale relative to 2026-07-06 and the quarterly cron; otherwise leave unchanged. Preserve the 15-entry c13_broken_link_allowlist count (C13 count-asserts it).
  </action>
  <verify>
    <automated>bash -c 'node -e "JSON.parse(require(\"fs\").readFileSync(\"scripts/validation/v1.15-audit-allowlist.json\",\"utf8\"))" && grep -q "119-frozen-surface-re-baseline" scripts/validation/v1.15-audit-allowlist.json && ! grep -q "112-pillar-e" scripts/validation/v1.15-audit-allowlist.json && echo SIDECAR_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validation/v1.15-audit-allowlist.json` is valid JSON (parses without error)
    - The "phase" field equals "119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal"; ZERO occurrences of "112-pillar-e"
    - Every line-pin flagged as shifted in 119-01 is repointed to its actual current line (verified against the live file), OR the SUMMARY records that HEAD was clean and no pins needed changes
    - c13_broken_link_allowlist still has exactly 15 entries
  </acceptance_criteria>
  <done>The sidecar is repointed to Phase 119 with every EEE-shifted line-pin corrected per the 119-01 worklist; no non-Phase-1 pins touched; valid JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Append BASELINE_19 comment to regenerate-supervision-pins.mjs, then commit Atom 1 as ONE indivisible commit</name>
  <files>scripts/validation/regenerate-supervision-pins.mjs</files>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs lines 460-474 (the BASELINE_18 paragraph template + the `const BASELINE_9 = [` declaration line — do NOT re-read beyond this)
    - .planning/phases/119-.../119-PATTERNS.md §Pattern 3 (exact BASELINE_19 template to append; the do-NOT-touch list) + §Anti-pattern (v1.7-audit-allowlist self-test refs are intentional, not stale)
    - .planning/phases/119-.../119-RESEARCH.md §Code Examples #3 + §Common Pitfall 4 (JIT anchor capture) + §Common Pitfall 5 (do not touch lines 290/336/489 or BASELINE_9 array unless --self-test FAILs)
  </read_first>
  <action>
    IMMEDIATELY before authoring this commit, capture the current HEAD via `git rev-parse HEAD` — this is the pre-Atom-1 anchor for the BASELINE_19 freshness comment. It may DIFFER from the Wave-0 anchor recorded in 119-01 if an automated Jira-sync commit landed between (Pitfall 4); use this JIT-captured SHA, not the Wave-0 SHA, for the BASELINE_19 comment. (The Wave-0 SHA remains the base for 119-04's byte-unchanged gate — they are different invariants.)

    Append a new BASELINE_19 comment paragraph AFTER the BASELINE_18 block and BEFORE the `const BASELINE_9 = [` line, following the exact prose template of the 9 prior refresh paragraphs: reference HARN-03 + REQUIREMENTS.md + ROADMAP.md Phase 119 SC1; state "v1.15 line positions verified against HEAD <JIT pre-Atom-1 SHA>"; note "BASELINE_9 entries above remain unchanged — Phase 119 does NOT alter the line-coord array"; and the resolution path "... → v1.14 → BASELINE_18 → v1.15 → BASELINE_19; BASELINE_20 refreshes at the next milestone close". Do NOT touch the BASELINE_9 array itself, and do NOT touch the three `parseAllowlist('scripts/validation/v1.7-audit-allowlist.json')` self-test references at lines ~290/336/489 (these deliberately dogfood the Phase-43 v1.7-era fixture — repointing them is the forbidden anti-pattern) UNLESS a live `--self-test` run FAILs.

    Then commit all THREE Atom-1 files (v1.15-milestone-audit.mjs, v1.15-audit-allowlist.json, regenerate-supervision-pins.mjs) as ONE indivisible commit: `docs(119-02): Atom 1 — v1.15 milestone audit harness (C1-C17) + sidecar + BASELINE_19`. This is SC1's "indivisibly (one commit)". Do NOT include any check-phase validator, frozen-at-close, or CI-workflow file (those are Atom 2 / SC2 — folding Atom 1 into Atom 2 is the rejected D3).
  </action>
  <verify>
    <automated>bash -c 'node scripts/validation/regenerate-supervision-pins.mjs --self-test >/dev/null 2>&1 && grep -q "BASELINE_19" scripts/validation/regenerate-supervision-pins.mjs && echo BASELINE19_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `regenerate-supervision-pins.mjs` contains a "BASELINE_19" comment paragraph referencing HARN-03 and a JIT pre-Atom-1 HEAD SHA
    - The three lines ~290/336/489 still reference `v1.7-audit-allowlist.json` (unchanged); the BASELINE_9 array is unchanged
    - `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0
    - The Atom-1 commit contains EXACTLY the three files (v1.15-milestone-audit.mjs, v1.15-audit-allowlist.json, regenerate-supervision-pins.mjs) and NO check-phase/frozen-at-close/workflow file (`git show --stat HEAD`)
  </acceptance_criteria>
  <done>BASELINE_19 back-anchored to the JIT pre-Atom-1 SHA; self-test green; Atom 1 committed as one indivisible commit of exactly three files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| c17-eee-contract.mjs subprocess ← v1.15-milestone-audit.mjs | A mis-wired or inline-copied C17 could silently pass a non-conformant corpus (supply-chain-of-trust in the validator itself) |
| live corpus → sidecar line-pins | Stale pins from a verbatim sidecar copy false-pass a genuine violation |
| commit boundary (Atom 1 vs Atom 2) | Folding atoms violates the SC1/SC2 indivisibility contract that the frozen-surface audit depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-02-C17FOLD | Tampering | check id 17 fold | mitigate | Subprocess-spawn of the already-self-tested c17-eee-contract.mjs (no inline reimplementation); argument-array execFileSync (no shell interpolation); verify --self-test exits 0 |
| T-119-02-PINDRIFT | Tampering/Repudiation | v1.15-audit-allowlist.json pins | mitigate | Repoint only per the 119-01 verified worklist; JSON-parse + count-assert c13 entries; no verbatim copy |
| T-119-02-ATOMFOLD | Tampering | Atom-1 commit contents | mitigate | Commit contains exactly the 3 Atom-1 files; `git show --stat` asserts no Atom-2 file leaked in |
| T-119-02-SC | Tampering | package installs | accept | No package-manager installs this phase (node built-ins only) |
</threat_model>

<verification>
- `node scripts/validation/v1.15-milestone-audit.mjs --self-test` exits 0
- `grep "id: 17"` and `grep "node:child_process"` both match in v1.15-milestone-audit.mjs
- v1.15-audit-allowlist.json parses as JSON and contains the Phase-119 phase string
- BASELINE_19 present; regenerate-supervision-pins.mjs --self-test exits 0
- Atom-1 commit stat shows exactly 3 files
</verification>

<success_criteria>
Atom 1 ships as ONE indivisible commit: v1.15-milestone-audit.mjs (C1-C17, C17 via subprocess-spawn) + repointed v1.15-audit-allowlist.json (pins re-verified) + BASELINE_19 comment (JIT-anchored). Self-tests green; no Atom-2 file included.
</success_criteria>

<output>
Create `.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-02-SUMMARY.md` when done, recording the Atom-1 commit SHA and the JIT pre-Atom-1 anchor SHA.
</output>
