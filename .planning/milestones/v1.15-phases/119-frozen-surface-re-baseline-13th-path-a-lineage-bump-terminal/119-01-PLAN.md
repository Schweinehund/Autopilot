---
phase: 119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-01-SUMMARY.md
autonomous: true
requirements: [HARN-02, HARN-03]
must_haves:
  truths:
    - "The Wave-0 pre-anchor SHA (the predecessor-byte-unchanged HARD-gate base) is captured and recorded before any harness file is authored"
    - "The literal V114 close-gate SHA is positively confirmed to be 7d922a7 (the substantive v1.14 close-gate), not the ambiguous grep-recovery result f3959c8"
    - "A baseline run of the CURRENT v1.14 harness against HEAD is recorded, surfacing any stale sidecar line-pins the EEE retrofit shifted (RE-144 android-capability-matrix)"
  artifacts:
    - path: ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-01-SUMMARY.md"
      provides: "Recorded Wave-0 anchor SHA, confirmed V114=7d922a7, baseline v1.14-harness diagnostic result"
      contains: "Wave-0 anchor SHA"
  key_links:
    - from: "119-01 Wave-0 anchor SHA"
      to: "119-04 predecessor-byte-unchanged HARD gate (git diff <anchor> HEAD)"
      via: "recorded anchor SHA consumed at close-audit time"
      pattern: "git diff"
---

<objective>
Wave-0 pre-anchor + baseline reconnaissance. This plan establishes the two SHA anchors the whole close depends on and de-risks the single highest-probability silent-copy-error site BEFORE any harness file is copied.

Purpose: Per D-119-4 (D1 skeleton) the close needs a Wave-0 pre-anchor commit BEFORE Atom 1 — it is (a) the byte-unchanged HARD-gate base asserted at close (HARN-02) and (b) the target the BASELINE_19 freshness comment back-anchors to. This plan also positively verifies the V114 pin SHA (a single point of failure for the entire re-baseline) and runs the current v1.14 harness against HEAD so Atom 1 knows exactly which sidecar line-pins the EEE retrofit shifted.

Output: 119-01-SUMMARY.md recording the Wave-0 anchor SHA, the confirmed V114 SHA (7d922a7), and the baseline harness diagnostic. NO harness files are authored in this plan (v1.14 112-01 precedent: Wave-0 is minimal/reconnaissance-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-CONTEXT.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-RESEARCH.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Wave-0 anchor SHA + positively confirm V114 = 7d922a7</name>
  <files>.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/119-.../119-CONTEXT.md §"Execution ordering (the B↔C↔D interlock)" + D-119-3 RIDER (predecessor-byte-unchanged HARD gate base = Wave-0/pre-Atom-1 anchor)
    - .planning/phases/119-.../119-RESEARCH.md §Common Pitfall 4 (BASELINE_19 anchor is captured JIT at Atom-1 time, NOT here; the Wave-0 anchor is the SEPARATE byte-unchanged gate base) + §Code Examples #4 (V114 SHA verification is a single point of failure)
    - scripts/validation/_lib/frozen-at-close.mjs lines 31-44 (existing V110/V111/V112/V113 single-entry pin pattern — the V114 entry mirrors this)
    - .planning/milestones/v1.14-MILESTONE-AUDIT.md lines 90-92, 316, 386 (the {phase_112_close_SHA} grep-recovery command and its documented ambiguity)
  </read_first>
  <action>
    Capture the current HEAD as the Wave-0 anchor via `git rev-parse HEAD` and record the full 40-char SHA in 119-01-SUMMARY.md labelled "Wave-0 anchor SHA (predecessor-byte-unchanged HARD-gate base for Plan 119-04)". This is DISTINCT from the BASELINE_19 freshness anchor, which Plan 119-02 captures JIT immediately before the Atom-1 commit (an intervening Jira-sync auto-commit may land between; Pitfall 4). Record BOTH facts so the executor of 119-02/119-04 does not conflate them.

    Positively confirm the V114 pin SHA. The naive recovery command `git log --all --grep="112-05" --grep="close-gate" --all-match -1 --format=%H` is AMBIGUOUS — it returns f3959c8 (the "complete ... close-gate plan" SUMMARY follow-up commit that landed ~3 min later), NOT the substantive close-gate. The CORRECT V114 SHA is 7d922a7 whose message is "docs(112-05): Phase 112 close-gate — v1.14 MILESTONE-AUDIT + DEFERRED-CLEANUP + 4-doc traceability + v1.14 MILESTONE CLOSE". Confirm by running `git log -1 --format=%s 7d922a7` and asserting the message contains BOTH "MILESTONE-AUDIT" AND "MILESTONE CLOSE". Record in the SUMMARY: "V114 = 7d922a7 CONFIRMED substantive close-gate; f3959c8 is the ambiguous follow-up — DO NOT pin f3959c8." This disambiguation is load-bearing for Plan 119-03's frozen-at-close V114 entry.

    Confirm whether any NEW frozen-audit-vs-live-corpus collision class exists for the EEE-inherited freshness fields (RESEARCH §Recommended Plan/Commit Structure Wave-0 note + Open Question 3): note whether v1.14's Plan 112-06 NESTED-guard doctrine covers validators authored AFTER Phase 112 (i.e., 96-99/101-111 are pre-112; only the new 113-119 are post — and those are authored fresh in Atom 2 with the guard). Record the assessment; do NOT author guard code here (that is remediation-slot work IF the GHA apex comes back RED).
  </action>
  <verify>
    <automated>bash -c 'git rev-parse HEAD && git log -1 --format=%s 7d922a7 | grep -q "MILESTONE CLOSE" && echo V114_OK'</automated>
  </verify>
  <acceptance_criteria>
    - 119-01-SUMMARY.md contains a line "Wave-0 anchor SHA" followed by a 40-char hex SHA equal to the current HEAD
    - 119-01-SUMMARY.md states "V114 = 7d922a7" AND explicitly warns that the naive `--grep -1` recovery returns f3959c8 (wrong)
    - `git log -1 --format=%s 7d922a7` output contains both "MILESTONE-AUDIT" and "MILESTONE CLOSE"
  </acceptance_criteria>
  <done>Wave-0 anchor SHA captured and recorded; V114=7d922a7 positively confirmed as the substantive close-gate (f3959c8 explicitly excluded); no new collision class authored (deferred to conditional remediation slot).</done>
</task>

<task type="auto">
  <name>Task 2: Baseline diagnostic — run current v1.14 harness against HEAD to surface shifted sidecar line-pins</name>
  <files>.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/119-.../119-RESEARCH.md §Code Examples #2 (the android-capability-matrix RE-144 line-pins almost certainly shifted; run the v1.14 harness against HEAD BEFORE assuming a clean sidecar copy) + Assumption A3
    - .planning/phases/119-.../119-PATTERNS.md §Pattern 2 "CRITICAL — do not copy verbatim without re-verification"
    - scripts/validation/v1.14-audit-allowlist.json (in full, 531 lines — the {file,line} pins in safetynet_exemptions / supervision_exemptions / c7_knox_allowlist against docs/reference/android-capability-matrix.md and docs/_glossary-android.md)
    - docs/_registry/RE-index.md (confirm android-capability-matrix.md = RE-144, retrofitted in Phase 118)
  </read_first>
  <action>
    Run `node scripts/validation/v1.14-milestone-audit.mjs --verbose` and `node scripts/validation/v1.14-milestone-audit.mjs --self-test` against the current (post-Phase-118) working tree. Record the full PASS/FAIL/SKIP tally and — critically — the identity of ANY check that FAILs due to a stale sidecar line-pin (the EEE header-block + Version-History-row insertions in Phase 118's RETRO-03 retrofit of docs/reference/android-capability-matrix.md are exactly the edit shape that shifts C1/C2/C7 SafetyNet/supervision line-pins by +1/+2). For each FAIL, record the file, the pinned line vs the actual current line, and the delta. This is the authoritative worklist Plan 119-02 Task 2 consumes to repoint the v1.15 sidecar — do NOT let 119-02 copy the sidecar verbatim without this diagnostic.

    Also record the value of `c13_rotting_external.quarterly_audit.next_review` (currently "2027-01-01") and whether it needs a cadence refresh against the quarterly cron (`0 8 1 1,4,7,10 *`) given today is 2026-07-06 — flag for 119-02, do not change here.

    Do NOT edit any harness file in this plan. This is reconnaissance only. If the v1.14 harness already exits 0 clean on HEAD, record that too (means the sidecar can be copied with only the phase-string repoint).
  </action>
  <verify>
    <automated>bash -c 'node scripts/validation/v1.14-milestone-audit.mjs --self-test >/dev/null 2>&1; echo "self-test exit: $?"'</automated>
  </verify>
  <acceptance_criteria>
    - 119-01-SUMMARY.md contains a "Baseline v1.14 harness diagnostic" section with the exact PASS/FAIL/SKIP counts from both `--verbose` and `--self-test` runs
    - For every FAIL (if any), the SUMMARY lists {check id, file, pinned line, actual line, delta} as the repoint worklist for Plan 119-02
    - The SUMMARY explicitly states whether android-capability-matrix.md (RE-144) pins shifted, and records the next_review cadence flag
    - No file under scripts/validation/ or .github/ is modified by this plan (git status shows only the SUMMARY)
  </acceptance_criteria>
  <done>The current v1.14 harness diagnostic is recorded against HEAD; every shifted line-pin (or a clean-exit confirmation) is documented as Plan 119-02's sidecar-repoint worklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → V114 pin value | An ambiguous grep-recovery can resolve the wrong SHA; a wrong pin silently reads the wrong frozen corpus for any validator adopting readAtV114Close |
| live (evolved) corpus → frozen audit assertions | Stale sidecar line-pins can cause the v1.15 harness to false-fail or (worse) false-pass a genuine violation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01-PIN | Tampering/Info-disclosure | V114 SHA selection | mitigate | Positively confirm 7d922a7 via commit-message assertion (contains "MILESTONE CLOSE"); explicitly reject the ambiguous f3959c8 grep result — recorded in SUMMARY for 119-03 |
| T-119-01-PINSHIFT | Tampering | sidecar line-pins (RE-144) | mitigate | Baseline-diagnostic run surfaces every shifted pin before the v1.15 sidecar is authored; no verbatim copy permitted |
| T-119-01-SC | Tampering | package installs | accept | No package-manager installs this phase (RESEARCH Package Legitimacy Audit: N/A — node built-ins + git/gh only) |
</threat_model>

<verification>
- `git rev-parse HEAD` recorded as Wave-0 anchor
- `git log -1 --format=%s 7d922a7` contains "MILESTONE-AUDIT" and "MILESTONE CLOSE"
- Baseline v1.14 harness `--self-test` and `--verbose` results recorded, with shifted-pin worklist
</verification>

<success_criteria>
Wave-0 anchor SHA captured; V114=7d922a7 positively disambiguated; baseline harness diagnostic recorded as the 119-02 sidecar-repoint worklist; zero harness files modified.
</success_criteria>

<output>
Create `.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-01-SUMMARY.md` when done.
</output>
