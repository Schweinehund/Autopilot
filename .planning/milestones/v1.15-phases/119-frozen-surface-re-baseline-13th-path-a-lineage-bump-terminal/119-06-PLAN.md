---
phase: 119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal
plan: 06
type: execute
wave: 5
depends_on: ["119-04"]
files_modified:
  - .planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/PIPE-02-CLOSE-RUNBOOK.md
  - .planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/PIPE-02-CLOSE-FINDINGS.md
autonomous: false
requirements: [HARN-04]
must_haves:
  truths:
    - "The agent PREPARES a statistically representative .docx set of REAL shipped RE-NNN Status:Approved docs (5 platforms + Linux + a post-RETRO-03 wide matrix) + an executable PIPE-02-CLOSE-RUNBOOK.md"
    - "The Draft-label probe uses a test-artifact-only frontmatter mutation of ONE real Approved doc to draft (source .md untouched) — a plan-time decision requiring owner confirmation"
    - "The OWNER executes the runbook live in Copilot Studio and records PIPE-02-CLOSE-FINDINGS.md, capturing the raw transcript in-repo"
    - "PASS = grounded answer + clickable document-level citation + no hallucination across all probes; the close-gate BLOCKS until the owner attests PASS"
  artifacts:
    - path: ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/PIPE-02-CLOSE-RUNBOOK.md"
      provides: "Owner-run second grounding-confirmation procedure (real-corpus + wide-matrix + Linux + Draft riders)"
      contains: "PASS"
    - path: ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/PIPE-02-CLOSE-FINDINGS.md"
      provides: "Owner-recorded results + raw transcript + PASS attestation"
      contains: "PASS"
  key_links:
    - from: "docs/_registry/RE-index.md Approved rows"
      to: "PIPE-02-CLOSE-RUNBOOK.md representative .docx set"
      via: "manual selection of real RE-NNN Approved docs per platform/class"
      pattern: "RE-1"
---

<objective>
The PIPE-02 second grounding-confirmation pass (HARN-04, D-119-1/A1). The agent PREPARES the representative real-corpus .docx set + an executable close-runbook; the OWNER executes live in Copilot Studio (the agent has NO live Copilot Studio / SharePoint access — REQUIREMENTS L77) and records findings. The close-gate BLOCKS until the owner attests PASS.

Purpose: SC5 requires PIPE-02 grounding confirmation "included in the close-gate." This second pass differs from Phase 113's first pass in three locked ways: it uses REAL shipped RE-NNN Status:Approved docs (not the synthetic RE-T* fixtures), it probes a POST-RETRO-03 WIDE capability-matrix (testing exactly what v1.15's table remediation changed, which Phase 113 explicitly did not disprove), and it includes a dedicated Linux doc. This plan is autonomous:false — it contains a plan-time Draft-probe decision (owner confirmation) and the blocking owner-execution checkpoint.

Output: PIPE-02-CLOSE-RUNBOOK.md (agent-authored) + PIPE-02-CLOSE-FINDINGS.md (owner-recorded, in-repo transcript + PASS attestation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-CONTEXT.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-RESEARCH.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-PATTERNS.md
@docs/_registry/RE-index.md
@.planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md
@.planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-FINDINGS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Draft-label probe artifact decision (owner confirmation)</name>
  <decision>How to satisfy the mandatory Draft-label retrieval probe when no real shipped RE-NNN doc has status: Draft (every retrofitted content doc is Status: Approved)</decision>
  <context>D-119-1 mandates BOTH "use the REAL retrofitted corpus (actual shipped RE-NNN Status:Approved docs)" AND "a Draft-label retrieval probe." These two riders conflict at this granularity — verified: `grep -rl "^status: Draft" docs/` returns only the 7 born-Draft templates under docs/_templates/, never a shipped content doc. CONTEXT.md left this unreconciled (resolved at gray-area level, not this granularity). RESEARCH Open Question 1 / Assumption A4 recommend a test-artifact-only mutation. This decision affects what "PASS" means for one of the four mandatory probe legs.</context>
  <options>
    <option id="option-a">
      <name>Test-artifact-only frontmatter mutation of ONE real Approved doc (RECOMMENDED)</name>
      <pros>Satisfies "use real corpus content" for the Draft probe while genuinely exercising Draft-label retrieval behavior; the source .md stays untouched (only the uploaded .docx test artifact carries status: draft); analogous to Phase 113's draft-test-doc but with real content</pros>
      <cons>Requires a one-off convert of a real Approved doc with its frontmatter status flipped to draft for the upload only; must be clearly labelled a test artifact, never committed to the shipped corpus</cons>
    </option>
    <option id="option-b">
      <name>Use a real committed template file (docs/_templates/*.md — genuine status: Draft)</name>
      <pros>No mutation needed; the template files are real committed files with genuine status: Draft</pros>
      <cons>Templates are scaffolding, not shipped operator content; weaker "real retrofitted corpus" fidelity; a template's header block differs from a shipped doc's</cons>
    </option>
    <option id="option-c">
      <name>Reuse Phase 113's synthetic draft-test-doc verbatim</name>
      <pros>Zero new work</pros>
      <cons>Directly violates the "use the REAL retrofitted corpus, not the Phase-113 synthetic fixtures" rider — NOT recommended</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/119-.../119-RESEARCH.md §Common Pitfall 2 + §Open Question 1 + §Assumption A4 (the full tension + the recommended resolution)
    - .planning/phases/119-.../119-CONTEXT.md §D-119-1 MANDATORY RIDERS (real-corpus + Draft-probe)
    - .planning/phases/113-.../PIPE-02-FINDINGS.md OQ2 (Phase-113 empirical finding: Status:Draft is a label only; draft-test-doc WAS indexed and retrieved)
  </read_first>
  <action>Present the three options to the owner and record the selection. If option-a, record the specific real shipped RE-NNN Status:Approved doc chosen for the test-artifact-only frontmatter mutation (status → draft on the uploaded .docx only; source .md untouched). The recorded choice is consumed by Task 2 when assembling the representative set. Do not proceed to Task 2 until the owner selects.</action>
  <resume-signal>Select: option-a, option-b, or option-c (option-a recommended). Confirm the specific real Approved doc to mutate if option-a.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author PIPE-02-CLOSE-RUNBOOK.md + select the representative real-corpus .docx set</name>
  <files>.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/PIPE-02-CLOSE-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/113-.../PIPE-02-RUNBOOK.md (in full — the fork base: Section 1 Prerequisites, Section 2 Upload Procedure, Section 3+ query list)
    - .planning/phases/119-.../119-PATTERNS.md §Pattern 8 (the exact fork changes) + §Pattern 9 (FINDINGS shape)
    - .planning/phases/119-.../119-RESEARCH.md §Common Pitfall 3 (RE-128 / docs/admin-setup-linux/00-overview.md is mermaid-deferred + Pending + keyless — do NOT select it; use an Approved Linux doc e.g. RE-129 docs/admin-setup-linux/01-intune-linux-agent.md or RE-130 02-enrollment-profile.md) + §D-119-1 riders
    - docs/_registry/RE-index.md (select Approved rows only — one representative doc per platform: Windows, macOS, iOS/iPadOS, Android, Linux; plus a post-RETRO-03 WIDE capability-matrix from docs/reference/ that received the ≤25-row-cap + per-table prose remediation — NOT RE-128; NOT the mermaid-carved RE-147)
  </read_first>
  <action>
    Fork .planning/phases/113-.../PIPE-02-RUNBOOK.md → PIPE-02-CLOSE-RUNBOOK.md. Update the header block: Procedure-for → Phase 119 close-gate second grounding pass; Authored-by → current date; Decision-basis → D-119-1 (A1) + REQUIREMENTS L77; Records-into → PIPE-02-CLOSE-FINDINGS.md.

    Replace the 5 synthetic RE-T01..RE-T05 prerequisite rows with a representative set of REAL shipped RE-NNN Status:Approved docs selected from RE-index.md, one per platform (Windows, macOS, iOS/iPadOS, Android) + a dedicated Linux doc that is Approved (e.g. RE-129 docs/admin-setup-linux/01-intune-linux-agent.md or RE-130 02-enrollment-profile.md — NOT RE-128/00-overview.md which is mermaid-deferred/Pending/keyless) + a post-RETRO-03 WIDE capability-matrix reference doc (>25 rows, that received the table remediation) + the Draft-probe artifact per the Task-1 decision. For each selected doc, convert it to .docx via the locked pipeline (scripts/pipeline/convert.ps1) and run guard-docx.mjs; record convert-OK + guard-exit-0 in the runbook's agent-confirmed-preconditions table (mechanical agent legs, mirroring Phase 113).

    Author the query-list section (N queries) covering the four PASS legs: (1) grounded-answer + clickable document-level citation + no-hallucination across all 5 platforms; (2) a Draft-label retrieval probe (does the draft-labelled doc get indexed/retrieved — carrying the Phase-113 OQ2 finding); (3) a wide-capability-matrix chunk-survival probe (does the >25-row matrix's column/mode context survive the ~2000-char chunk window after RETRO-03 remediation — the thing Phase 113 explicitly did NOT disprove for a wide/flat matrix); (4) a dedicated Linux-doc grounding query. Inherit the Phase-113 runbook's query-list shape. Include an explicit "Definition of PASS" section: grounded + clickable citation + no hallucination across every probe.
  </action>
  <verify>
    <automated>bash -c 'test -f ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/PIPE-02-CLOSE-RUNBOOK.md" && grep -q "PASS" ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/PIPE-02-CLOSE-RUNBOOK.md" && ! grep -q "00-overview.md" ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/PIPE-02-CLOSE-RUNBOOK.md" && echo RUNBOOK_OK'</automated>
  </verify>
  <acceptance_criteria>
    - PIPE-02-CLOSE-RUNBOOK.md exists, forked from the Phase-113 runbook, records-into PIPE-02-CLOSE-FINDINGS.md
    - The representative set is real shipped RE-NNN Status:Approved docs spanning 5 platforms + an Approved Linux doc (NOT RE-128/00-overview.md) + a post-RETRO-03 wide capability matrix + the Task-1 Draft-probe artifact
    - Each selected doc has a recorded convert-OK + guard-exit-0 precondition
    - A query-list covers all four PASS legs (5-platform grounding, Draft-label, wide-matrix chunk-survival, Linux) with an explicit "Definition of PASS"
  </acceptance_criteria>
  <done>The close-runbook is authored with a real-corpus representative set (5 platforms + Approved Linux + wide matrix + Draft artifact), all guard-passed, with a four-leg query list and a PASS definition.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: OWNER CHECKPOINT — execute the runbook live in Copilot Studio, record findings + PASS attestation</name>
  <what-built>The agent prepared PIPE-02-CLOSE-RUNBOOK.md + the representative real-corpus .docx set (converted + guard-passed) + an empty PIPE-02-CLOSE-FINDINGS.md template with the recording-table + raw-transcript sections. The live Copilot Studio / SharePoint legs are owner-run (agent has no access; REQUIREMENTS L77).</what-built>
  <read_first>
    - .planning/phases/119-.../PIPE-02-CLOSE-RUNBOOK.md (the procedure the owner executes)
    - .planning/phases/113-.../PIPE-02-FINDINGS.md (the recording-table shape to fork into the empty CLOSE-FINDINGS template)
    - .planning/phases/119-.../119-CONTEXT.md §D-119-1 RIDER "capture the raw transcript in-repo" (Phase 113 left it owner-local; the close-gate leg is the ONE leg no re-audit axis can reproduce — give it an auditable evidence artifact)
  </read_first>
  <how-to-verify>
    1. Upload the representative .docx set to the test SharePoint indexed library per Section 2 of PIPE-02-CLOSE-RUNBOOK.md; wait for reindex.
    2. Run every query in the runbook's query list against the live Copilot Studio agent (5-platform grounding, Draft-label probe, wide-matrix chunk-survival probe, Linux-doc query).
    3. For each query, confirm: a grounded answer, a clickable document-level citation, and no hallucination. The wide-matrix probe must confirm the >25-row matrix column/mode context survives the chunk boundary after RETRO-03 remediation.
    4. Record every query + response + citation-present + hallucination-free + PASS/FAIL into PIPE-02-CLOSE-FINDINGS.md, and PASTE THE RAW TRANSCRIPT in-repo (do not leave it owner-local — this is the one close-gate leg no re-audit axis can reproduce).
    5. Attest the overall verdict: PASS only if every probe is grounded + clickable-cited + hallucination-free. If any probe FAILs, record it as FAIL — the close-gate must NOT proceed on a FAIL.
  </how-to-verify>
  <action>Agent action: fork the Phase-113 PIPE-02-FINDINGS.md recording-table shape into an EMPTY PIPE-02-CLOSE-FINDINGS.md template (per-query table columns + a raw-transcript section) for the owner to fill. Then PAUSE for the owner to execute the live Copilot Studio legs and record + attest PASS. The agent does not (and cannot) run the live legs.</action>
  <acceptance_criteria>
    - PIPE-02-CLOSE-FINDINGS.md exists with a per-query recording table AND a pasted raw transcript section (in-repo)
    - Every probe leg (5-platform, Draft-label, wide-matrix chunk-survival, Linux) has a recorded verdict
    - An explicit overall "PIPE-02 CLOSE: PASS" (or FAIL) owner attestation line is present
    - On PASS, this artifact is the evidence the close-gate (119-07) consumes to flip HARN-04 / PIPE-02 to Validated
  </acceptance_criteria>
  <resume-signal>Owner types "PIPE-02 PASS" (with PIPE-02-CLOSE-FINDINGS.md committed) or describes the failing probe(s). The close-gate BLOCKS until PASS is attested.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → live Copilot Studio | The agent has NO access; the grounding result is an ASSERTED (owner-attested), not agent-verified, gate leg — the one leg no re-audit axis reproduces |
| test artifact → shipped corpus | A Draft-probe frontmatter mutation must never leak into the committed corpus (test-artifact-only) |
| representative set fidelity → SC5 | Selecting a keyless/Pending doc (RE-128) or synthetic fixtures would invalidate the grounding proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-06-ASSERTED | Repudiation | owner-attested PIPE-02 PASS | mitigate | Capture the raw transcript in-repo (PIPE-02-CLOSE-FINDINGS.md) so the asserted leg has an auditable evidence artifact |
| T-119-06-DRAFTLEAK | Tampering | Draft-probe test artifact | mitigate | Test-artifact-only frontmatter mutation; source .md untouched; never committed to the shipped corpus (owner-confirmed decision) |
| T-119-06-SETFIDELITY | Tampering | representative set | mitigate | Real shipped RE-NNN Approved docs only; explicitly exclude RE-128 (mermaid/Pending/keyless) and RE-T* synthetic fixtures; convert+guard each |
| T-119-06-SC | Tampering | package installs | accept | No package-manager installs this phase (pandoc pipeline already pinned) |
</threat_model>

<verification>
- Draft-probe approach owner-confirmed (Task 1 decision)
- PIPE-02-CLOSE-RUNBOOK.md authored: real-corpus set, 5 platforms + Approved Linux + wide matrix + Draft artifact, all guard-passed, four-leg query list, PASS definition
- PIPE-02-CLOSE-FINDINGS.md owner-recorded with per-query verdicts + in-repo raw transcript + explicit PASS/FAIL attestation
</verification>

<success_criteria>
The agent-prepared close-runbook + representative real-corpus set is executed live by the owner; PIPE-02-CLOSE-FINDINGS.md records grounded + clickable-cited + hallucination-free results across all four probe legs with an in-repo transcript; the owner attests PIPE-02 CLOSE: PASS. The close-gate blocks until PASS.
</success_criteria>

<output>
Create `.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-06-SUMMARY.md` when done, recording the Draft-probe decision, the representative-set composition, and the owner PASS attestation reference.
</output>
