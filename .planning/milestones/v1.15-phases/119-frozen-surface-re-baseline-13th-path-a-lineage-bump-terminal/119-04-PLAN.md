---
phase: 119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal
plan: 04
type: execute
wave: 4
depends_on: ["119-03"]
files_modified:
  - .planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-AUDIT-RESULTS.md
autonomous: true
requirements: [HARN-02, HARN-04]
must_haves:
  truths:
    - "The authoritative Axis-2 Linux GHA run (both chain validators on ubuntu-latest) is dispatched, awaited, and its run ID + apex tally recorded"
    - "Axis 1 (fresh git clone --no-hardlinks, Windows) and Axis 3 (fresh zero-context sub-agent) reproduce the leaf-validator + --self-test results"
    - "Cross-OS PASS/FAIL/SKIP EXACT MATCH is asserted across all three axes for the reproducible surfaces"
    - "The predecessor-byte-unchanged HARD gate holds: git diff <Wave-0 anchor> HEAD over non-Phase-1 frozen surfaces is EMPTY"
    - "If the GHA apex is RED, the plan HALTS and signals the remediation slot (119-05); if GREEN it clears the close-gate precondition"
  artifacts:
    - path: ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-AUDIT-RESULTS.md"
      provides: "3-axis re-audit record + cross-OS EXACT MATCH table + byte-unchanged gate proof"
      contains: "Cross-OS PASS-Count EXACT MATCH"
  key_links:
    - from: "Axis-2 GHA run conclusion"
      to: "119-05 remediation slot (fires only if RED) / 119-07 close-gate (proceeds only if GREEN)"
      via: "recorded run ID + apex PASS/FAIL/SKIP tally"
      pattern: "check-phase-119"
---

<objective>
The 3-axis terminal re-audit (HARN-04) + the predecessor-byte-unchanged HARD gate (HARN-02). This plan proves the frozen-surface re-baseline is atomically green at the new close SHAs and that non-Phase-1 predecessor surfaces remain byte-unchanged.

Purpose: SC4 requires a 3-axis re-audit with cross-OS EXACT MATCH: Axis 1 fresh clone (Windows), Axis 2 cross-OS Linux GHA authoritative (BOTH chain validators — the Windows apex times out at [48..118], WINDOWS-CLONE-DEEPNEST-TIMEOUT-01), Axis 3 fresh zero-context sub-agent. The apex may come back RED on first push (v1.14's first Axis-2 run was 44/22/1) — this plan explicitly branches to the pre-authorized remediation slot (119-05) rather than improvising.

Output: 119-04-AUDIT-RESULTS.md (mirrors 112-04-AUDIT-RESULTS.md) with the run ID, the cross-OS EXACT MATCH table, and the byte-unchanged gate proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-CONTEXT.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-RESEARCH.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-PATTERNS.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-01-SUMMARY.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-03-SUMMARY.md
@.planning/milestones/v1.14-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch + await the authoritative Axis-2 Linux GHA run; record run ID and apex tally</name>
  <files>.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/phases/119-.../119-03-SUMMARY.md (the push branch/PR + the workflow run ID already triggered by the Atom-2 push)
    - .planning/phases/119-.../119-CONTEXT.md §D-119-2 (GHA green is authoritative; both chain validators on ubuntu-latest via linux-chain-ubuntu-latest + per-phase jobs) + §Execution ordering interlock
    - .planning/milestones/v1.14-MILESTONE-AUDIT.md lines 223-290 (the v1.14 precedent: first Axis-2 run 28621185019 RED 44/22/1, re-run 28625158404 GREEN 66/0/1 — the exact narrative + table shape to reproduce)
  </read_first>
  <action>
    Confirm the Atom-2 push (from 119-03) triggered `audit-harness-v1.15-integrity.yml`. If it did not fire (paths: filter mismatch or PR not opened), dispatch via `gh workflow run audit-harness-v1.15-integrity.yml --ref <branch>` and/or open the PR so the pull_request paths: trigger matches. Await completion with `gh run watch` / `gh run list --workflow=audit-harness-v1.15-integrity.yml`. Record the run ID, headSha, and the per-job conclusions — specifically the apex `check-phase-119` result (the recursive [48..118] chain) from BOTH the standalone check-phase-119 job AND the linux-chain-ubuntu-latest job, plus each per-phase leaf job.

    This GHA run's green is the AUTHORITATIVE Axis-2 result. Do NOT run the full apex chain locally on Windows — it hits WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 at depth [48..118] and is killed at turn boundaries. (An optional local WSL2/Docker corroborating run is Claude's Discretion per D-119-2 — NOT authoritative; skip unless desired.)

    Record the apex PASS/FAIL/SKIP tally verbatim. If FAIL > 0 on the apex, set the AUDIT-RESULTS.md verdict to "APEX RED — remediation slot 119-05 REQUIRED" and capture the failing validator/assertion identities from the run logs (which specific check-phase-NN FAILed and why) so 119-05 has a precise worklist. If the apex is GREEN (FAIL 0), set the verdict to "APEX GREEN — 119-05 is a no-op skip; close-gate precondition cleared for Axis 2".
  </action>
  <verify>
    <automated>bash -c 'test -f ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-AUDIT-RESULTS.md" && grep -Eq "APEX (RED|GREEN)" ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-AUDIT-RESULTS.md" && echo AXIS2_RECORDED'</automated>
  </verify>
  <acceptance_criteria>
    - 119-04-AUDIT-RESULTS.md records the Axis-2 workflow run ID, headSha, and the apex check-phase-119 PASS/FAIL/SKIP tally
    - The verdict line is either "APEX RED — remediation slot 119-05 REQUIRED" (with the failing-validator worklist) or "APEX GREEN — ... precondition cleared"
    - No full apex chain was run locally on Windows (only GHA is authoritative)
  </acceptance_criteria>
  <done>The authoritative Axis-2 Linux GHA run is dispatched/awaited; run ID + apex tally recorded; the RED-vs-GREEN branch is set with a precise remediation worklist if RED.</done>
</task>

<task type="auto">
  <name>Task 2: Axis 1 (fresh clone) + Axis 3 (fresh sub-agent) reproducible surfaces; cross-OS EXACT MATCH table</name>
  <files>.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/phases/119-.../119-CONTEXT.md §D-119-2/B1 (Axis 1 fresh clone; Axis 3 fresh zero-context sub-agent; cross-OS EXACT MATCH) + §Claude's Discretion
    - .planning/phases/119-.../119-PATTERNS.md §Pattern 10 "Cross-OS PASS-Count EXACT MATCH table pattern" (the exact reusable table column structure)
    - .planning/milestones/v1.14-MILESTONE-AUDIT.md lines 264-290 (the 14-row cross-OS table; which rows are "Linux sole-authoritative — Windows N/A cascades" for the chain validators)
  </read_first>
  <action>
    Axis 1: fresh `git clone --no-hardlinks <repo> "$TEMP/v1.15-audit-<rand>"`, checkout the pushed branch, and run the REPRODUCIBLE-on-Windows surfaces: `node scripts/validation/v1.15-milestone-audit.mjs --verbose` + `--self-test`, and each leaf `node scripts/validation/check-phase-113.mjs`..`118.mjs`. Record PASS/FAIL/SKIP. Do NOT run the full apex (check-phase-119 full chain) on Windows — mark those rows "Windows N/A — cascades; Linux sole-authoritative" exactly as the v1.14 table does.

    Axis 3: delegate the same reproducible-surface run to a fresh zero-context sub-agent (per the sequential-on-main + background-runs-die-at-turn-boundaries constraint, delegate the byte-equiv proof to a sub-agent). Record its independent tally.

    Author the "Cross-OS PASS-Count EXACT MATCH" table in 119-04-AUDIT-RESULTS.md using the v1.14 column structure: | # | Validator | Type | Windows (Axis 1/3 fresh clone) | Linux (Axis 2 GHA) | Verdict |. One row per new leaf (113-118), one row for the harness (v1.15-milestone-audit --verbose + --self-test), one apex row (check-phase-119, Linux sole-authoritative), and one continuity-chain row (confirm at audit time which chain validator sits immediately below the new apex — likely the prior apex check-phase-112; mark it Linux sole-authoritative). Every reproducible row must read EXACT MATCH across axes; assert this explicitly. Any mismatch is a hard stop (do not proceed to close-gate).
  </action>
  <verify>
    <automated>bash -c 'grep -q "Cross-OS PASS-Count EXACT MATCH" ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-AUDIT-RESULTS.md" && grep -q "EXACT MATCH" ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-AUDIT-RESULTS.md" && echo XOS_TABLE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - 119-04-AUDIT-RESULTS.md contains a "Cross-OS PASS-Count EXACT MATCH" table with the v1.14 column structure
    - Every reproducible surface (harness + 6 leaves) has an EXACT MATCH verdict across Axis 1, Axis 2, Axis 3
    - Apex + the continuity-chain row are marked "Linux sole-authoritative — Windows N/A cascades"
    - Axis 3 was a genuinely fresh zero-context sub-agent (distinct from Axis 1)
  </acceptance_criteria>
  <done>Axis 1 + Axis 3 reproduce the harness + leaf results; the cross-OS EXACT MATCH table is authored with every reproducible surface matching and the chain rows correctly marked Linux-authoritative.</done>
</task>

<task type="auto">
  <name>Task 3: Predecessor-byte-unchanged HARD gate + set the close-gate precondition verdict</name>
  <files>.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/phases/119-.../119-01-SUMMARY.md (the Wave-0 anchor SHA — the byte-unchanged gate base)
    - .planning/phases/119-.../119-CONTEXT.md §D-119-3 RIDER (predecessor-byte-unchanged HARD gate: git diff <pre-Atom-1 anchor> HEAD over non-Phase-1 frozen surfaces = EMPTY) + the FROZEN-SURFACE-INVERSION note (Phase-1 surfaces deliberately re-pinned; non-Phase-1 byte-unchanged)
    - .planning/phases/119-.../119-PATTERNS.md §Shared Patterns "Path-A copy-and-repoint discipline" (verify via git diff over the 32+ non-Phase-1 frozen surfaces = EMPTY)
    - .planning/milestones/v1.14-MILESTONE-AUDIT.md line 229 pattern (the v1.14 byte-unchanged assertion)
  </read_first>
  <action>
    Assert the predecessor-byte-unchanged HARD gate: run `git diff <Wave-0 anchor SHA from 119-01> HEAD --` scoped to the full set of non-Phase-1 predecessor frozen harness/sidecar/workflow surfaces (the v1.4–v1.14 harness lineage: every `scripts/validation/v1.N-milestone-audit.mjs`, every `v1.N-audit-allowlist.json`, every `.github/workflows/audit-harness-v1.N-integrity.yml` for N in 1.4..1.14, plus check-phase-48..112 which are predecessor validators). Enumerate the exact surface list from the frozen-at-close inventory + the workflow directory listing and record each as byte-unchanged. The diff MUST be EMPTY — this milestone deliberately re-pins ONLY Phase-1 content docs (docs/l1-runbooks/, docs/admin-setup-*/, docs/reference/, docs/error-codes/), never the predecessor harness surfaces. If ANY predecessor harness surface shows a diff, that is a HARD STOP: the Atom-1/Atom-2 copies accidentally touched a frozen surface — do not proceed to close-gate.

    Then set the overall close-gate precondition verdict in 119-04-AUDIT-RESULTS.md: "Axis-2 GHA = <GREEN|RED>; cross-OS EXACT MATCH = <yes|no>; predecessor-byte-unchanged = <EMPTY|VIOLATED>". The close-gate (119-07) may land ONLY when Axis-2 = GREEN AND EXACT MATCH = yes AND byte-unchanged = EMPTY (and, separately, owner PIPE-02 PASS from 119-06). If Axis-2 = RED, explicitly hand off to the remediation slot 119-05 and note this plan's verdict will be re-evaluated after 119-05 re-pushes to green.
  </action>
  <verify>
    <automated>bash -c 'grep -Eq "predecessor-byte-unchanged = (EMPTY|VIOLATED)" ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-AUDIT-RESULTS.md" && echo BYTE_GATE_RECORDED'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff <Wave-0 anchor> HEAD` over every enumerated non-Phase-1 predecessor frozen surface is EMPTY (recorded surface-by-surface)
    - 119-04-AUDIT-RESULTS.md contains the verdict line "Axis-2 GHA = ...; cross-OS EXACT MATCH = ...; predecessor-byte-unchanged = EMPTY|VIOLATED"
    - If Axis-2 = RED, the doc explicitly hands off to remediation slot 119-05
  </acceptance_criteria>
  <done>The predecessor-byte-unchanged HARD gate is proven EMPTY over all non-Phase-1 frozen surfaces; the composite close-gate precondition verdict is recorded, branching to 119-05 if RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Windows result → authoritative verdict | Windows cannot run the deep-nest apex; trusting a local Windows apex run instead of GHA would mis-report the chain as green |
| Wave-0 anchor → byte-unchanged proof | A wrong/omitted anchor lets an accidental frozen-surface edit pass undetected (the milestone's dominant risk) |
| RED apex → improvised shortcut | Without an explicit remediation branch, an executor might value-mask or add CHAIN_SKIP to force green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-04-AUTHSURF | Spoofing/Repudiation | authoritative Axis-2 result | mitigate | Only the GHA run ID is authoritative; Windows apex explicitly N/A-cascades; record run ID + headSha |
| T-119-04-BYTEGATE | Tampering | predecessor frozen surfaces | mitigate | git diff <Wave-0 anchor> HEAD over the enumerated non-Phase-1 surface set MUST be EMPTY; any diff is a hard stop |
| T-119-04-REDSHORT | Tampering | RED-apex handling | mitigate | Explicit branch to the pre-authorized 119-05 remediation slot (predecessor-only, no value-mask, CHAIN_SKIP stays ∅); no improvised shortcut |
| T-119-04-SC | Tampering | package installs | accept | No package-manager installs this phase (gh/git/node only) |
</threat_model>

<verification>
- Axis-2 GHA run ID + apex PASS/FAIL/SKIP recorded; verdict APEX RED|GREEN set
- Cross-OS EXACT MATCH table authored; every reproducible surface matches across axes
- Predecessor-byte-unchanged gate EMPTY over all enumerated non-Phase-1 surfaces
- Composite close-gate precondition verdict recorded
</verification>

<success_criteria>
The 3-axis terminal re-audit is complete with cross-OS EXACT MATCH; the predecessor-byte-unchanged HARD gate is EMPTY; the close-gate precondition verdict is set (GREEN → proceed to close-gate once owner PIPE-02 PASS; RED → hand off to 119-05).
</success_criteria>

<output>
Create `.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-SUMMARY.md` when done (in addition to 119-04-AUDIT-RESULTS.md), recording the Axis-2 run ID and the composite verdict.
</output>
