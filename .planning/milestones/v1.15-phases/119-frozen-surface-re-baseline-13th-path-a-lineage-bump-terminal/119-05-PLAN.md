---
phase: 119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal
plan: 05
type: execute
wave: 5
depends_on: ["119-04"]
files_modified:
  - scripts/validation/check-phase-48.mjs
  - scripts/validation/check-phase-96.mjs
  - scripts/validation/check-phase-100.mjs
autonomous: true
requirements: [HARN-02]
must_haves:
  truths:
    - "This slot fires ONLY IF the Plan 119-04 authoritative Axis-2 GHA apex came back RED; if GREEN it is a no-op skip"
    - "Remediation edits ONLY predecessor check-phase-NN.mjs validators (in-class chain maintenance) — NEVER a frozen surface, NEVER a value-mask"
    - "CHAIN_SKIP stays EMPTY everywhere (V-SELF hard-asserts size 0) — never add an entry to force green"
    - "Every RED-then-green intermediate state is recorded honestly; the re-pushed GHA apex concludes green"
  artifacts:
    - path: ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-05-SUMMARY.md"
      provides: "Honest remediation record: which predecessor validators were fixed, root cause, before/after GHA run IDs"
      contains: "no value-masking"
  key_links:
    - from: "119-04 APEX RED verdict + failing-validator worklist"
      to: "predecessor check-phase-NN NESTED-guard / frozen-aware-read fix"
      via: "in-class chain maintenance"
      pattern: "CHECK_PHASE_NESTED"
---

<objective>
The pre-authorized EMERGENT chain-health remediation slot (HARN-02, D-119-3/C1). Budgeted as a REAL plan because v1.14's first Axis-2 apex ran RED (44/22/1) and needed 3 remediation commits — the same failure mode is highly probable here (the EEE retrofit reformatted ~174 docs across shared surfaces predecessor validators assert against).

Purpose: If the Plan 119-04 authoritative Axis-2 GHA apex is RED, restore chain health by editing ONLY predecessor check-phase-NN validators (adding the NESTED guard / making content assertions frozen-aware) — never masking values, never editing a frozen surface, never adding a CHAIN_SKIP entry. Then re-push and re-audit to green. If 119-04 reported APEX GREEN, this plan is a documented no-op skip.

Output: 1-or-more remediation commits (predecessor validators only) + 119-05-SUMMARY.md honest-accounting record + a re-pushed green Axis-2 GHA run. The close-gate remains a SEPARATE single commit (119-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-CONTEXT.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-RESEARCH.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-PATTERNS.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-04-AUDIT-RESULTS.md
@.planning/milestones/v1.14-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate check — fire only if APEX RED; else record no-op skip</name>
  <files>.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/119-.../119-04-AUDIT-RESULTS.md (the APEX RED|GREEN verdict + the failing-validator worklist if RED)
    - .planning/phases/119-.../119-CONTEXT.md §D-119-3 (remediation fires ONLY if the authoritative Axis-2 GHA apex is RED; honest-atomicity note)
  </read_first>
  <action>
    Read the Plan 119-04 verdict. If it is "APEX GREEN", record in 119-05-SUMMARY.md: "No-op skip — Plan 119-04 Axis-2 GHA apex concluded GREEN on first push; no chain-health remediation required; close-gate precondition (Axis 2) already cleared." Do NOT edit any file. This plan then completes as a skip.

    If the verdict is "APEX RED", proceed to Task 2 using the failing-validator worklist (which specific check-phase-NN FAILed and the failing assertion class — typically an AUDIT-HARNESS/self-test step lacking the CHECK_PHASE_NESTED guard, or a content assertion that reads the live evolved corpus instead of readAtClose()). Record the pre-remediation RED run ID + tally verbatim as the honest-accounting baseline.
  </action>
  <verify>
    <automated>bash -c 'test -f ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-05-SUMMARY.md" && grep -Eq "No-op skip|APEX RED" ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-05-SUMMARY.md" && echo GATE_RECORDED'</automated>
  </verify>
  <acceptance_criteria>
    - 119-05-SUMMARY.md records either "No-op skip" (with the 119-04 GREEN verdict cited) or "APEX RED" (with the pre-remediation run ID + failing-validator worklist)
    - In the no-op case, git status shows no scripts/validation edits from this plan
  </acceptance_criteria>
  <done>The fire/skip gate is evaluated against the 119-04 verdict and recorded; remediation proceeds only on APEX RED.</done>
</task>

<task type="auto">
  <name>Task 2 (conditional): Fix predecessor validators, re-push, re-audit to green — honest record</name>
  <files>scripts/validation/check-phase-48.mjs, scripts/validation/check-phase-96.mjs, scripts/validation/check-phase-100.mjs</files>
  <read_first>
    - .planning/phases/119-.../119-04-AUDIT-RESULTS.md (the exact failing check-phase-NN identities — the files listed in files_modified above are PLACEHOLDER candidates; the ACTUAL files to edit are whichever predecessors 119-04 reported RED)
    - .planning/milestones/v1.14-MILESTONE-AUDIT.md lines 223-233 (the Plan 112-06 remediation honest-accounting template: first-run RED discarded, root cause, the remediation constraints, re-run GREEN) + §Pattern 10 honest-accounting voice in 119-PATTERNS.md
    - scripts/validation/check-phase-112.mjs lines 89, 96-98, 134-136 (the CHECK_PHASE_NESTED guard doctrine — the exact fix shape for a predecessor whose AUDIT-HARNESS/self-test re-runs against live corpus)
    - scripts/validation/_lib/frozen-at-close.mjs (readAtClose / readAtVNNClose — the frozen-aware-read helper a content assertion must use instead of reading live corpus)
    - .planning/phases/119-.../119-CONTEXT.md §D-119-3 MANDATORY RIDER (remediation constraints)
  </read_first>
  <action>
    ONLY IF Task 1 recorded APEX RED. For each RED predecessor validator identified in 119-04: apply the in-class chain-maintenance fix — either add the CHECK_PHASE_NESTED guard to its AUDIT-HARNESS/self-test step (so a nesting apex does not re-validate the evolved live corpus with a frozen audit), or convert a content assertion to a frozen-aware read via the appropriate readAtVNNClose() helper. Edit ONLY predecessor check-phase-NN.mjs validators (the actual set comes from 119-04, not the placeholder file list in frontmatter — update files_modified in the SUMMARY to the real set). NEVER edit a frozen surface (any v1.N-milestone-audit.mjs / sidecar / workflow). NEVER value-mask (do not bump an expected value to match the evolved corpus — that is T1 masking, the GA3-C self-disqualifier). CHAIN_SKIP stays `new Set([])` everywhere — never add an entry to force the chain green.

    Commit the remediation as 1-or-more commits `fix(119-05): chain-health remediation — <validator(s)> NESTED-guard/frozen-aware (no value-mask, CHAIN_SKIP ∅)`. Re-push to the branch/PR and re-run `audit-harness-v1.15-integrity.yml`; await the FRESH Axis-2 run and confirm the apex concludes green (FAIL 0). Record in 119-05-SUMMARY.md the honest-accounting narrative: the discarded pre-remediation RED run ID + tally, the root cause, the exact fix per validator (with an explicit "NO value-masking; NO frozen surface edited; CHAIN_SKIP left empty" statement), and the re-run GREEN run ID + tally. Signal Plan 119-04's verdict is now re-evaluated to GREEN for the close-gate.
  </action>
  <verify>
    <automated>bash -c 'grep -qi "no value-mask" ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-05-SUMMARY.md" && grep -Eq "GREEN|No-op skip" ".planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-05-SUMMARY.md" && echo REMEDIATION_RECORDED'</automated>
  </verify>
  <acceptance_criteria>
    - (RED path) Only predecessor check-phase-NN.mjs files are edited (git diff shows no v1.N-milestone-audit.mjs / sidecar / workflow / Phase-1 doc edits)
    - No CHAIN_SKIP entry was added anywhere (every touched validator keeps `new Set([])`)
    - No expected value was bumped to match the evolved corpus (no value-masking)
    - The re-pushed Axis-2 GHA apex concludes green (FAIL 0); the fresh run ID is recorded
    - 119-05-SUMMARY.md contains the honest-accounting narrative (discarded RED run ID + tally, root cause, per-validator fix, re-run GREEN run ID) with the explicit "NO value-masking / NO frozen surface / CHAIN_SKIP empty" statement
    - (GREEN path) This task is a documented no-op; no files edited
  </acceptance_criteria>
  <done>If fired: predecessor validators fixed in-class, re-pushed, Axis-2 apex green, honest record complete. If not fired: documented no-op skip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RED apex → remediation edit surface | The single largest integrity risk of the phase: an executor could "fix" the RED by value-masking, editing a frozen surface, or adding CHAIN_SKIP — all of which silently defeat the chain-of-trust |
| honest-accounting record → audit trail | Hiding the RED-then-green intermediate state produces a dishonest milestone-audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-05-VALUEMASK | Tampering | predecessor validator expected values | mitigate | Hard prohibition on value-masking (T1); git diff review asserts no expected-value bump; fix is NESTED-guard/frozen-aware-read only |
| T-119-05-FROZEN | Tampering | v1.N frozen surfaces | mitigate | Remediation edits ONLY predecessor check-phase-NN.mjs; frozen surfaces off-limits; byte-unchanged gate (119-04) re-runs after remediation |
| T-119-05-SKIP | Tampering | CHAIN_SKIP | mitigate | CHAIN_SKIP stays `new Set([])`; V-SELF hard-asserts size 0; adding an entry is the forbidden GA3-C shortcut |
| T-119-05-REPUDIATE | Repudiation | honest-accounting record | mitigate | Discarded RED run ID + tally recorded; re-run GREEN run ID recorded; no clean-first-pass fabrication |
| T-119-05-SC | Tampering | package installs | accept | No package-manager installs this phase |
</threat_model>

<verification>
- Fire/skip gate evaluated against 119-04 verdict
- (If fired) only predecessor check-phase-NN edited; no value-mask; CHAIN_SKIP empty; re-pushed Axis-2 apex green
- Honest-accounting narrative present with before/after run IDs
</verification>

<success_criteria>
Either a documented no-op skip (119-04 was GREEN) OR an honest, constraint-compliant remediation (predecessor validators only, no value-mask, CHAIN_SKIP empty) that re-pushes the Axis-2 apex to green. The close-gate remains a separate single commit.
</success_criteria>

<output>
Create `.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-05-SUMMARY.md` when done, recording fire-vs-skip, the real edited-file set (if fired), and before/after Axis-2 run IDs.
</output>
