---
phase: 118-reference-doc-retrofit-table-remediation-26-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pipeline/retrofit-reference.mjs
autonomous: true
requirements: [RETRO-03]
must_haves:
  truths:
    - "The forked helper resolves a doc_id for all 34 enrolled reference paths by joining RE-index.md on the Path column (never hand-transcribed) per D-118-4"
    - "The helper DETECTS each existing ## Version History table's column count (2-col | Date | Change | vs 3-col | Date | Change | Author |) and PREPENDS a matching-width v1.15 EEE reformat row — the load-bearing NEW mechanical fix (RESEARCH Pattern 2 / Pitfall 1); for files with NO existing VH section it CREATES a 3-column section (Open Question 1 lock)"
    - "The helper injects platform: Windows for the 10 confirmed keyless files (7 error-codes + reference/powershell-ref.md + reference/registry-paths.md + apv1-vs-apv2.md), resolves every present platform through the verbatim-copied D1_MAP, and stamps owner: Intune Admin Lead + doc_type: Reference uniformly (D-118-3, D-118-4)"
    - "The path allowlist accepts BOTH the two directories (docs/reference/, docs/error-codes/) AND the two bare single files (docs/apv1-vs-apv2.md, docs/windows-vs-macos.md); the single mermaid-deferred file docs/reference/ca-enrollment-timing.md (RE-147) fails CLOSED (ERROR) if invoked by explicit path and is never enumerated under --all (D-05)"
    - "A --dry-run across the 34 enrolled files writes nothing to the corpus or registry and reports zero unresolved doc_ids, zero unmapped platforms, correct VH branch (PREPEND-2col×8, PREPEND-3col×5, CREATE×21) per file, and a byte-length-preserved pre-H1 span for every file"
  artifacts:
    - path: "scripts/pipeline/retrofit-reference.mjs"
      provides: "Forked mechanical EEE retrofit helper (D-118-4): frontmatter key injection, EEE Reference block line, whole-pre-H1-span relocation, Version-History column-shape detection (2-col vs 3-col PREPEND + 3-col CREATE), platform: Windows injection for 10 keyless files, reference path allowlist (2 dirs + 2 single files) + 1-mermaid exclusion"
      contains: "const D1_MAP"
  key_links:
    - from: "scripts/pipeline/retrofit-reference.mjs"
      to: "docs/_registry/RE-index.md"
      via: "path-column join to resolve doc_id"
      pattern: "RE-\\d+"
    - from: "scripts/pipeline/retrofit-reference.mjs"
      to: "scripts/validation/c17-eee-contract.mjs"
      via: "D1_MAP copied verbatim (lines 26-47)"
      pattern: "D1_MAP"
---

<objective>
Fork a new `scripts/pipeline/retrofit-reference.mjs` from the Phase-117 helper `scripts/pipeline/retrofit-guide.mjs`
and add the three genuine deltas this phase requires before any reference doc is touched. This plan builds and proves
the script only — it writes NOTHING to the corpus or registry (dry-run + self-test). The four batch plans
(118-02..118-05) invoke it to perform the deterministic half of every reference retrofit.

Purpose: This is the third and final Phase-1 retrofit class; the mechanical skeleton is >90% identical to the shipped
Phase-117 helper (D1_MAP, buildDocIdMap, whole-pre-H1-span relocation, TEMPLATE-SENTINEL guard, self-test shape). Only
THREE things are genuinely new and MUST be added: (1) Version-History column-shape detection — 13/34 files already have
a `## Version History` section in two incompatible shapes (8 are 2-column, 5 are 3-column); the 117 helper's hardcoded
3-column row would silently misalign the 8 two-column files (RESEARCH Pattern 2 / Pitfall 1). (2) A path allowlist that
accepts two bare single files (`docs/apv1-vs-apv2.md`, `docs/windows-vs-macos.md`) in addition to two directories — 117's
allowlist was directory-only. (3) `doc_type: Reference` (not `Guide`) and the reference-template Summary lead. Fork (do
NOT refactor 117's script in place — it is a shipped, Approved deliverable) per D-118-4.
Output: One node-builtins-only script mirroring the `scripts/pipeline/` idiom, validated by --self-test (the 6 inherited
sub-tests + a 7th VH-column-detection sub-test) and a full 34-file --dry-run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-CONTEXT.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-RESEARCH.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork scripts/pipeline/retrofit-reference.mjs with VH column detection + reference path allowlist</name>
  <files>scripts/pipeline/retrofit-reference.mjs</files>
  <read_first>
    - scripts/pipeline/retrofit-guide.mjs (the Phase-117 helper to fork; COPY VERBATIM: shebang + node-builtins import block lines 1-45; D1_MAP lines 58-79; padLabel/readFile-CRLF-normalize/walkMd/relNormalize lines 112-153; buildDocIdMap lines 162-172; the frontmatter regex /^---\n([\s\S]*?)\n---/m line ~258; TEMPLATE-SENTINEL guard lines 264-269; doc-id-unresolved guard; platform-injection + D1_MAP guard lines 285-289; whole-pre-H1-span relocation. Study insertVersionHistoryRow line 182 — its PREPEND branch hardcodes a 3-column row and MUST be replaced; the ADMIN_SETUP_DIRS allowlist line 86 + the inAllowlist guard line 239 + the --all enumeration line 593 you must generalize; the doc_type literal you must change to Reference)
    - scripts/validation/c17-eee-contract.mjs (the immutable gate; D1_MAP lines 26-47 the fork's copy must byte-match; enrollment scan lines 519-533; assertion #9 lines 298-329; #10 lines 331-339 HARD FAIL on absent platform key; #11 lines 344-380; #12 lines 387-405)
    - docs/_registry/RE-index.md (RE-142..RE-167 skipping RE-147, RE-168..RE-174, RE-177, RE-178 — the 34 reference-class Path rows the helper joins to resolve doc_id)
    - docs/_templates/reference-template.md (generic Reference target: frontmatter key set doc_id/status/owner/doc_type/platform, EEE block line, ## Summary-first order, Summary prescription line 38, the "Tables exceeding 25 rows" per-table-prose worked example lines 44-55)
    - docs/_standards/EEE-SOP-standard.md (single-line block format; field order Platform · Doc Type · Doc ID · Status; D2 Last Reviewed = last_verified verbatim; owner-never-in-block rule; Version-History row exact string)
    - docs/error-codes/00-index.md (a real 2-column existing VH table — the PREPEND-2col shape the new logic must match) and docs/reference/android-capability-matrix.md (a real 3-column existing VH table — the PREPEND-3col shape)
  </read_first>
  <action>
    Create `scripts/pipeline/retrofit-reference.mjs` as a standalone FORK of retrofit-guide.mjs (do NOT refactor the 117
    script in place — it is a shipped, Approved deliverable; a shared refactor risks regressing it). Node built-ins ONLY
    (node:fs, node:path, node:process — zero npm packages, matching the scripts/pipeline/ invariant). Support argv flags
    `--dry-run` (compute and report, write nothing), `--verbose`, `--self-test`, positional file paths, and `--all`.

    COPY VERBATIM from retrofit-guide.mjs (per RESEARCH "Don't Hand-Roll" — never diverge or C17 #9/#10 break): the
    D1_MAP constant (must byte-match c17-eee-contract.mjs:26-47), padLabel, readFile (CRLF-normalize .replace(/\r\n/g,'\n')),
    walkMd (lstatSync, skip symlinks), relNormalize (backslash→forward-slash), buildDocIdMap (registry Path-column join →
    Map<relPath, RE-NNN>), the frontmatter regex /^---\n([\s\S]*?)\n---/m (m flag mandatory), the TEMPLATE-SENTINEL guard,
    the doc-id-unresolved guard, the platform-injection + D1_MAP guard (its /^platform:\s*.../m presence-only test is ALREADY
    correct for this corpus's applies_to: both/APv1/APv2 decoys — it never matches applies_to), and the whole-pre-H1-span
    relocation (this corpus is structurally simpler — at most 1 blockquote group, 0 HTML comments per file — but keep the
    general non-conditional implementation unchanged).

    Make these THREE fork-specific changes (the three genuine deltas per PATTERNS "Three Deltas"):
    1. VERSION-HISTORY COLUMN-SHAPE DETECTION (the load-bearing NEW fix): Add a detectVhColumnCount(lines, vhIdx) helper
       that scans the first non-separator table row after an existing `## Version History` heading and counts pipe-delimited
       cells (2 or 3). In insertVersionHistoryRow's PREPEND branch, select the new row matching the detected count:
       2-column row `| YYYY-MM-DD | v1.15 EEE reformat — content not re-reviewed |` OR 3-column row
       `| YYYY-MM-DD | v1.15 EEE reformat — content not re-reviewed | — |` (em-dash). For the CREATE branch (no existing
       section — 21 files) create a fresh 3-column section (Open Question 1 lock: matches 116/117 corpus-wide precedent).
       NEVER assume 3-column on PREPEND — the 8 two-column files (reference/00-index.md + all 7 error-codes/*.md) would
       silently misalign (Markdown does not error on a 3-cell row under a 2-column header).
    2. PATH ALLOWLIST (2 dirs + 2 single files) + MERMAID EXCLUSION: replace ADMIN_SETUP_DIRS with
       REFERENCE_DIRS = ['docs/reference/','docs/error-codes/'] plus a REFERENCE_SINGLE_FILES Set containing
       'docs/apv1-vs-apv2.md' and 'docs/windows-vs-macos.md'; the inAllowlist test becomes
       REFERENCE_DIRS.some(d => rel.startsWith(d)) || REFERENCE_SINGLE_FILES.has(rel). Set MERMAID_DEFERRED_PATHS to the
       single path 'docs/reference/ca-enrollment-timing.md' (RE-147) — fail CLOSED (return ERROR, not a silent skip) if
       invoked against it by explicit path, and never enumerate it under --all. Change the --all enumeration to merge
       REFERENCE_DIRS.flatMap(walkMd) with the two explicit REFERENCE_SINGLE_FILES absolute paths (walkMd takes directories
       only — a direct copy of 117's ADMIN_SETUP_DIRS.flatMap(walkMd) would miss the two bare files).
    3. DOC_TYPE + SUMMARY LEAD: literal `Reference` (not `Guide`) in both the new-frontmatter assembly and the EEE
       block-line assembly; change the [FILL-IN] Summary placeholder banner to reference the reference-template Summary
       lead (there is only ONE reference template — no per-platform Summary branching).

    Per enrolled target file, emit: frontmatter with prepended doc_id: RE-NNN (registry join), status: Approved,
    owner: Intune Admin Lead, doc_type: Reference; inject platform: Windows ONLY when the file has no platform: key (the
    10 keyless files); RETAIN last_verified, review_by, applies_to, audience verbatim. Body order out: EEE block line
    `**Platform:** <D1 label> · **Doc Type:** Reference · **Doc ID:** RE-NNN · **Status:** Approved` (middle-dot U+00B7;
    owner NEVER in the block) → blank → `# <existing H1>` → blank → `## Summary` → blank →
    `[FILL-IN: >=30 words, reference-template Summary lead]` placeholder → blank → [relocated whole pre-H1 span] →
    [remaining original sections unchanged]. Version History: PREPEND the matching-width row for the 13 files that already
    have a section; CREATE a new 3-column section (date `| YYYY-MM-DD | v1.15 EEE reformat — content not re-reviewed | — |`,
    date filled by the executor at commit) for the 21 that don't.

    In --dry-run print a per-file report (doc_id resolved, platform present-or-injected + D1 label, VH branch
    PREPEND-2col/PREPEND-3col/CREATE, pre-H1-span element count + original-vs-relocated byte length). Exit 0 when all
    resolve cleanly; exit 1 on any ERROR. Do NOT author Summary prose, do NOT author per-table D-118-1 prose, do NOT fix
    #12 blockquotes, do NOT edit RE-index.md — those are batch-plan hand-work.
  </action>
  <acceptance_criteria>
    - `node scripts/pipeline/retrofit-reference.mjs --all --dry-run` exits 0 and reports a resolved doc_id for exactly 34 files (grep the output for `RE-1`/`RE-177`/`RE-178` lines == 34), with zero unmapped-platform and zero unresolved-doc_id errors.
    - The dry-run report classifies the VH branch per file as exactly: PREPEND-2col for docs/reference/00-index.md + all 7 docs/error-codes/*.md (8 files); PREPEND-3col for docs/reference/{4-platform-capability-comparison,android-capability-matrix,aosp-oem-matrix,ios-capability-matrix,linux-capability-matrix}.md (5 files); CREATE for the remaining 21 files.
    - The dry-run report shows relocated-span byte length EQUAL to original pre-H1-span byte length for every file (no content dropped).
    - Invoking the script against docs/reference/ca-enrollment-timing.md by explicit arg returns ERROR (exit 1), not a write; `--all` output never lists that path.
    - The script source contains D1_MAP with all entries byte-identical to c17-eee-contract.mjs lines 26-47; imports only node:fs, node:path, node:process (grep confirms zero external package names).
    - `git diff --quiet docs/ docs/_registry/RE-index.md` after the dry-run (dry-run writes nothing).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-reference.mjs --all --dry-run; test $? -eq 0 &amp;&amp; git diff --quiet docs/ docs/_registry/RE-index.md</automated>
  </verify>
  <done>The forked helper exists, is node-builtins-only, detects VH column shape (2-col vs 3-col PREPEND, 3-col CREATE), accepts the 2-dir + 2-single-file allowlist, injects platform: Windows for the 10 keyless files, hard-excludes the 1 mermaid file, resolves all 34 doc_ids/platforms with byte-preserved spans, and its dry-run writes nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the fork with --self-test (incl. VH-column-detection sub-test) + full 34-file --dry-run</name>
  <files>scripts/pipeline/retrofit-reference.mjs</files>
  <read_first>
    - scripts/pipeline/retrofit-reference.mjs (the fork authored in Task 1)
    - scripts/pipeline/retrofit-guide.mjs (the 6 inherited self-test sub-tests lines 417-578 — fixture shape to adapt; the span-preservation sub-test to keep)
    - scripts/validation/c17-eee-contract.mjs (D1_MAP + platform-resolution logic the dry-run must agree with; #11 lines 344-380 to confirm error-codes/00-index.md's 30-row table already passes)
    - .planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-RESEARCH.md (the full per-file inventory: the 10 keyless files, the 13 PREPEND files and their column shapes, the 21 CREATE files)
    - docs/_registry/RE-index.md (RE-142..178 reference-class rows)
  </read_first>
  <action>
    Adapt the inherited --self-test fixtures to the reference corpus and KEEP the span-preservation sub-test. Assert:
    (a) buildDocIdMap parses a sample RE-index reference row to the correct RE-NNN; (b) a keyless error-codes fixture
    (frontmatter carrying applies_to: both but no platform: key) triggers platform: Windows injection and resolves the
    D1 label `Windows`; (c) an android-capability fixture resolves the D1 label `Android`; (d) a
    last_verified: 1970-01-01 fixture is REFUSED (sentinel guard); (e) an explicit path outside the allowlist AND the
    mermaid-deferred path docs/reference/ca-enrollment-timing.md are both REFUSED, while docs/apv1-vs-apv2.md and
    docs/windows-vs-macos.md (bare single files) ARE accepted (the new allowlist-union proof); (f) the inherited
    whole-pre-H1-span relocation byte-length-preservation proof. Add a SEVENTH sub-test (this phase's signature-defect
    regression): a synthetic fixture with an existing 2-column `## Version History` table asserts detectVhColumnCount
    returns 2 and the inserted row has exactly 2 pipe-delimited cells (NOT 3); a companion assertion on a 3-column
    fixture asserts a 3-cell row. Then run the full 34-file --dry-run and confirm agreement with RESEARCH: exactly 10
    files flagged for platform: Windows injection (7 error-codes + reference/powershell-ref + reference/registry-paths +
    apv1-vs-apv2), zero unmapped platforms, VH branch counts PREPEND-2col=8 / PREPEND-3col=5 / CREATE=21, and
    span-byte-length-preserved for all 34.
  </action>
  <acceptance_criteria>
    - `node scripts/pipeline/retrofit-reference.mjs --self-test` exits 0 (all 7 sub-tests pass, including the VH-column-detection regression and the span-preservation proof).
    - `node scripts/pipeline/retrofit-reference.mjs --all --dry-run --verbose` reports exactly 10 files flagged for platform: Windows injection and zero unmapped-platform errors.
    - The dry-run VH-branch tally is exactly PREPEND-2col=8, PREPEND-3col=5, CREATE=21 (sum 34).
    - `git diff --quiet docs/ docs/_registry/RE-index.md` (no corpus or registry writes from self-test/dry-run).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-reference.mjs --self-test &amp;&amp; node scripts/pipeline/retrofit-reference.mjs --all --dry-run &gt;/dev/null; test $? -eq 0 &amp;&amp; git diff --quiet docs/ docs/_registry/RE-index.md</automated>
  </verify>
  <done>--self-test passes all seven sub-tests (VH-column detection + span-preservation included), and the full 34-file dry-run confirms 10 injection files, zero unmapped platforms, VH branch counts 8/5/21, byte-preserved spans, and zero writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper script → corpus files | The script rewrites Markdown; a VH-column bug could silently MISALIGN a table (data-integrity), and a bug could write outside the reference allowlist |
| registry table → doc_id output | An incorrect path-join would stamp the wrong Doc ID (C17 #9 catches, but late) |

## STRIDE Threat Register

This is a PURE DOCUMENTATION-REFORMAT phase — no code executes against untrusted input, no network, no auth, no user
input; the only attack surface worth noting is that the build-time node-builtins-only helper must not corrupt or lose
content. That surface is mitigated by per-file C17 exit-0 + dry-run + self-test, which are already acceptance criteria.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Tampering | Version-History column-shape mismatch (silent table misalignment — RESEARCH Pitfall 1) | mitigate | detectVhColumnCount parses the existing header and emits a matching-width row; self-test sub-test 7 asserts a 2-column existing table yields a 2-cell row; --dry-run reports the VH branch per file for review |
| T-118-02 | Tampering | pre-H1 span capture (silent content loss — 117 lesson) | mitigate | Whole-pre-H1-span relocation copied verbatim from the 117 fix; self-test asserts relocated-span byte length == original; --dry-run reports per-file span byte lengths |
| T-118-03 | Tampering | frontmatter / block generation + write path | mitigate | doc_id and D1 label derived programmatically (never hand-transcribed); path allowlist (2 dirs + 2 files) + hard-exclusion of ca-enrollment-timing.md (fail closed); TEMPLATE-SENTINEL guard; last_verified carried verbatim; --dry-run writes nothing this plan |
| T-118-SC | Tampering | package installs | accept | node-builtins-only (node:fs/path/process); zero npm/pip/cargo installs; no third-party packages introduced, so no supply-chain surface |
</threat_model>

<verification>
- `node scripts/pipeline/retrofit-reference.mjs --self-test` exits 0 (7 sub-tests incl. VH-column detection + span-preservation).
- `node scripts/pipeline/retrofit-reference.mjs --all --dry-run` exits 0, resolves all 34 doc_ids, flags exactly 10 platform-injection files, tallies VH branches 8/5/21, preserves every pre-H1 span byte length, and writes nothing.
- Explicit invocation against docs/reference/ca-enrollment-timing.md returns ERROR; it never appears under --all.
- Script is node-builtins-only; D1_MAP byte-identical to c17-eee-contract.mjs lines 26-47.
</verification>

<success_criteria>
The forked retrofit-reference.mjs exists, is node-builtins-only, adds the three genuine deltas (VH column-shape
detection, 2-dir+2-file allowlist, doc_type Reference + reference Summary lead), injects platform: Windows for the 10
keyless files, hard-excludes the 1 mermaid file, and is proven by --self-test + a zero-write 34-file dry-run. Batch
plans 118-02..118-05 can now invoke it.
</success_criteria>

<output>
Create `.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-01-SUMMARY.md` when done.
</output>
