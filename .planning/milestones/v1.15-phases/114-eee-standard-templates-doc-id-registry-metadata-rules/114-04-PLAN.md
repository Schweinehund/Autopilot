---
phase: 114-eee-standard-templates-doc-id-registry-metadata-rules
plan: 04
type: execute
wave: 3
depends_on: ["114-02"]
files_modified: ["docs/_registry/RE-index.md"]
autonomous: true
requirements: [STD-03]
must_haves:
  truths:
    - "docs/_registry/RE-index.md exists outside the indexed SharePoint library with a self-warning header"
    - "All 178 Phase-1 in-scope docs are assigned RE-001..RE-178 in one collision-free sequential pass — no gaps, no duplicates"
    - "Each row maps RE-NNN → path + title + doc_type + status, with doc_type assigned per the D-02 taxonomy"
    - "No test fixture (RE-T0x), template, standard, or out-of-scope (D-04) doc appears in the registry"
  artifacts:
    - path: "docs/_registry/RE-index.md"
      provides: "The Phase-1 Doc ID Registry: RE-001..RE-178 → path/title/doc_type/status"
      contains: "| Doc ID | Path | Title | Doc Type | Status |"
      min_lines: 185
  key_links:
    - from: "docs/_registry/RE-index.md rows"
      to: "178 Phase-1 corpus files"
      via: "RE-NNN → path (one row per in-scope file)"
      pattern: "^\\| RE-\\d{3} \\|"
    - from: "docs/_registry/RE-index.md doc_type column"
      to: "EEE-SOP-standard.md D-02 taxonomy"
      via: "class → doc_type (runbooks=Runbook, admin-setup=Guide, reference-class=Reference, end-user=Guide)"
      pattern: "Runbook|Guide|Reference"
---

<objective>
Author `docs/_registry/RE-index.md` — the flat sequential Doc ID Registry (D-08) — outside the indexed SharePoint library, assigning all 178 Phase-1 in-scope docs a unique `RE-NNN` ID in ONE collision-free pass from `RE-001`. Each row maps `RE-NNN → path + title + doc_type + status`, with `doc_type` assigned by class per the D-02 taxonomy. The single-pass, sorted-enumeration assignment is collision-free by construction; a dedicated verification confirms 178 unique sequential IDs with no gaps.

Purpose: All three retrofit phases (116-118) draw Doc IDs from this registry; a collision or off-by-one would corrupt C17 and every retrofitted block. Assigning all IDs in one pass before authoring prevents RE-NNN conflicts across the three phases.
Output: `docs/_registry/RE-index.md` (new file in a new greenfield directory, outside the indexed library).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-RESEARCH.md
@.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-PATTERNS.md
@docs/_standards/EEE-SOP-standard.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the RE-index.md registry in one sequential collision-free pass</name>
  <read_first>
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-RESEARCH.md (§Doc ID Registry — the confirmed 178-file count table, the RE-NNN block-ordering ranges 1-14, §Registry Excludes)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-PATTERNS.md (§docs/_registry/RE-index.md — target format, self-warning header, column set, ordering rule; analog docs/error-codes/00-index.md)
    - docs/_standards/EEE-SOP-standard.md (§Doc Type Taxonomy — the D-02 class→doc_type rulings the Status/Doc Type columns follow)
    - docs/error-codes/00-index.md (analog: multi-column ID→detail lookup table format)
  </read_first>
  <action>
    Create `docs/_registry/` and author `docs/_registry/RE-index.md`. Start with `# Doc ID Registry — Phase-1` and a blockquote warning that this file lives OUTSIDE the indexed SharePoint library (per scripts/pipeline/README.md §SC3) and must never be uploaded — if indexed, "What does RE-047 cover?" returns the registry row instead of doc content. Then a table with header `| Doc ID | Path | Title | Doc Type | Status |`. Build the rows deterministically: enumerate the Phase-1 files in the fixed block order (sorted by filename WITHIN each class), assigning sequential IDs with NO gaps: RE-001..RE-042 = `docs/l1-runbooks/` (42), RE-043..RE-075 = `docs/l2-runbooks/` (33), RE-076..RE-086 = `docs/admin-setup-apv1/` (11), RE-087..RE-091 = `docs/admin-setup-apv2/` (5), RE-092..RE-105 = `docs/admin-setup-android/` (14), RE-106..RE-115 = `docs/admin-setup-ios/` (10), RE-116..RE-127 = `docs/admin-setup-macos/` (12), RE-128..RE-133 = `docs/admin-setup-linux/` (6), RE-134..RE-141 = `docs/admin-setup-8021x/` (8), RE-142..RE-167 = `docs/reference/` (26), RE-168..RE-174 = `docs/error-codes/` (7), RE-175..RE-176 = `docs/end-user-guides/` (2), RE-177 = `docs/apv1-vs-apv2.md`, RE-178 = `docs/windows-vs-macos.md`. For the Title column, extract each file's first H1 (`^# `) line. For the Doc Type column, assign per D-02: l1-runbooks + l2-runbooks → Runbook; all admin-setup-* → Guide; reference/ + error-codes/ + apv1-vs-apv2.md + windows-vs-macos.md → Reference; end-user-guides/ → Guide. Set the Status column to `Pending` for every row (docs are not yet retrofitted; Phases 116-118 flip these to Approved). Do NOT give the registry itself an RE-NNN Doc ID and do NOT place it in the indexed library. Use a scripted enumeration (e.g. a Node/shell one-pass over the sorted file list) so the pass is reproducible and collision-free by construction — do not hand-transcribe 178 rows. Include the 00-index.md / 00-overview.md navigation files (they are part of the confirmed 178 count and the roadmap "75 runbooks" figure).
  </action>
  <verify>
    <automated>test -f docs/_registry/RE-index.md && grep -Eq "^\| Doc ID \| Path \| Title \| Doc Type \| Status \|" docs/_registry/RE-index.md && grep -c "^| RE-" docs/_registry/RE-index.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/_registry/RE-index.md` exists with the header row `| Doc ID | Path | Title | Doc Type | Status |` and a blockquote warning it is OUTSIDE the indexed library.
    - `grep -c "^| RE-" docs/_registry/RE-index.md` = 178.
    - The first data row is `RE-001` (a `docs/l1-runbooks/` file) and the last is `RE-178` → `docs/windows-vs-macos.md`.
    - Doc Type column values follow D-02: runbooks=Runbook, admin-setup=Guide, reference/error-codes/comparison=Reference, end-user-guides=Guide.
    - Every Status cell is `Pending`.
    - No row references a template (`docs/_templates/`), the standard (`docs/_standards/`), a test fixture (`RE-T`, `test-fixtures`, `.pipeline-output`), or a D-04 out-of-scope path (`docs/operations/`, `docs/device-operations/`, `docs/cross-platform/apple-business/`).
  </acceptance_criteria>
  <done>The registry maps all 178 Phase-1 docs to sequential RE-001..RE-178 with correct doc_type and Pending status, outside the indexed library.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the registry is collision-free — 178 unique sequential IDs, no gaps</name>
  <read_first>
    - docs/_registry/RE-index.md (the file from Task 1 — verify only, do not edit unless a defect is found)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-RESEARCH.md (§Doc ID Registry — the 178 count and ordering to check against)
  </read_first>
  <action>
    Run collision and completeness checks over `docs/_registry/RE-index.md`: (1) extract all `RE-NNN` IDs; confirm exactly 178, all unique (no duplicates); (2) confirm the sequence is contiguous RE-001..RE-178 with no gaps and no out-of-range IDs (no RE-000, no RE-179+); (3) confirm no path is listed twice (each of the 178 corpus files appears exactly once); (4) confirm every path in the registry actually exists on disk. If any check fails, fix the offending rows in Task 1's file and re-run until all pass. Record the collision-free confirmation (178 unique, contiguous, no dup paths, all paths exist) in the SUMMARY.
  </action>
  <verify>
    <automated>grep -oE "RE-[0-9]{3}" docs/_registry/RE-index.md | sort | uniq -d | head; echo "dupes-above (empty=good)"; grep -oE "RE-[0-9]{3}" docs/_registry/RE-index.md | sort -u | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The count of UNIQUE `RE-NNN` IDs is exactly 178.
    - `grep -oE "RE-[0-9]{3}" ... | sort | uniq -d` returns empty (no duplicate IDs).
    - The IDs form a contiguous run RE-001 through RE-178 (no gaps, no RE-000, no RE-179+).
    - No file path appears in more than one row; every referenced path exists on disk (`test -f` for each Path cell passes).
  </acceptance_criteria>
  <done>The registry is proven collision-free and complete (178 unique, contiguous IDs; unique existing paths); STD-03 is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry → all three retrofit phases (116-118) | Every retrofitted block draws its Doc ID here; a collision/off-by-one corrupts C17 and the corpus |
| registry file → indexed SharePoint library | If indexed, doc-specific queries return the registry row instead of doc content |
| corpus scope → registry rows | Test fixtures (RE-T0x) and D-04 out-of-scope docs must not contaminate the Phase-1 registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-08 | Tampering | Doc ID collision / off-by-one in the RE-001..RE-178 pass | mitigate (high) | Single deterministic scripted pass over the sorted enumerated file list (collision-free by construction) + a dedicated Task-2 verification asserting 178 unique contiguous IDs, no gaps, unique existing paths |
| T-114-09 | Information Disclosure | registry placed in / uploaded to the indexed library | mitigate | File lives in `docs/_registry/` (underscore-prefixed, non-corpus) with a self-warning header; excluded from the conversion walk (documented in the standard's grounding notes) |
| T-114-10 | Tampering | test fixtures / out-of-scope docs contaminate the registry | mitigate | Fixtures live outside `docs/` (scripts/pipeline/test-fixtures/); acceptance asserts no `RE-T`/template/standard/D-04 path appears; enumeration walks only the 13 in-scope Phase-1 directories |
| T-114-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase |
</threat_model>

<verification>
- SC4 (STD-03): `docs/_registry/RE-index.md` committed outside the indexed library; `grep -c "^| RE-" docs/_registry/RE-index.md` = 178; IDs are unique and contiguous RE-001..RE-178; doc_type per D-02; every path exists.
</verification>

<success_criteria>
The Doc ID Registry assigns all 178 Phase-1 docs collision-free sequential IDs outside the indexed library, ready for the Phase 116-118 retrofit to consume.
</success_criteria>

<output>
Create `.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-04-SUMMARY.md` when done.
</output>
