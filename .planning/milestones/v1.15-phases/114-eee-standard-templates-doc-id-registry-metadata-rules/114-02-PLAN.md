---
phase: 114-eee-standard-templates-doc-id-registry-metadata-rules
plan: 02
type: execute
wave: 2
depends_on: ["114-01"]
files_modified: ["docs/_standards/EEE-SOP-standard.md"]
autonomous: true
requirements: [STD-01, META-02, META-03, META-04]
must_haves:
  truths:
    - "docs/_standards/EEE-SOP-standard.md exists outside the indexed library and is the single source of truth the templates and registry conform to"
    - "The standard specifies the single-line header block format Platform · Doc Type · Doc ID · Status (Platform+Doc Type first, · separator, no Owner, no Last Reviewed in block)"
    - "The D1 normalization map covers all 20 real platform variants with a stated unmapped = hard failure (no fallback) rule"
    - "The standard states D2 semantics (Last Reviewed = last_verified verbatim), the one-time v1.15 EEE-reformat Version-History row rule, the Doc Type taxonomy {Runbook,Guide,RCA,Reference}, and grounding notes (body-text-only indexing; Draft = label not gate)"
    - "The standard hands off the complete C17 needle-spec for Phase 115"
  artifacts:
    - path: "docs/_standards/EEE-SOP-standard.md"
      provides: "The canonical EEE SOP standard: block format, D1 map, D2 semantics, taxonomy, grounding notes, C17 needle-spec"
      contains: "D1 Platform Normalization Map"
      min_lines: 120
  key_links:
    - from: "docs/_standards/EEE-SOP-standard.md D1 map"
      to: "20 real corpus platform: variants"
      via: "raw value → clean visible label rows"
      pattern: "windows|macos|ios|android|linux"
    - from: "docs/_standards/EEE-SOP-standard.md header block spec"
      to: "PIPE-02 grounding-validated field set"
      via: "Platform · Doc Type · Doc ID · Status"
      pattern: "Platform.*Doc Type.*Doc ID.*Status"
---

<objective>
Author `docs/_standards/EEE-SOP-standard.md` — the canonical EEE SOP standard and the single source of truth that the templates (Plan 03) and the registry (Plan 04) conform to. It locks: the required frontmatter schema; the single-line visible header block format (D-05); the Doc Type taxonomy with edge-case rulings (D-02); the D1 platform-normalization map with the no-fallback rule (D-09/META-03); the D2 `Last Reviewed` = `last_verified` verbatim semantics + the one-time "v1.15 EEE reformat — content not re-reviewed" Version-History row rule (META-04); the Status vocabulary and supersedence convention; the grounding-notes section (OQ1-OQ4 empirical facts); the Phase-1 scope statement (D-03 in, D-04 out); and the complete C17 needle-spec handoff for Phase 115.

Purpose: Everything downstream in v1.15 (templates, registry, C17, all three retrofit phases) derives from this document. It must be precise and self-consistent.
Output: `docs/_standards/EEE-SOP-standard.md` (new file in a new greenfield directory, outside the indexed SharePoint library).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-CONTEXT.md
@.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-RESEARCH.md
@.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-PATTERNS.md
@.planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the EEE-SOP-standard.md core specification sections</name>
  <read_first>
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-CONTEXT.md (D-01 owner frontmatter-only; D-02 taxonomy + edge cases; D-03 Phase-1 scope; D-04 orphans OUT; D-05 block field set/order/separator; §Specific Ideas — exact Draft-wording)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-RESEARCH.md (§EEE-SOP-standard.md Structure — the ~10-section outline and per-section content)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-PATTERNS.md (§docs/_standards/EEE-SOP-standard.md — frontmatter, EEE block, section cadence, Version-History pattern; analog docs/error-codes/00-index.md)
    - .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-FINDINGS.md (OQ1 filename-driven citations; OQ2 Draft=label; OQ4 frontmatter→invisible custom properties; Q2 block-indexed-as-body-text proof)
    - docs/error-codes/00-index.md (analog: sparse-frontmatter + H1 + H2-section cadence + table format)
  </read_first>
  <action>
    Create `docs/_standards/` and author `docs/_standards/EEE-SOP-standard.md`. The file is itself EEE-conformant: frontmatter `doc_id: STD-001` (STD- prefix, NOT an RE-NNN slot), `status: Approved`, `owner:` (project owner or role), `doc_type: Reference`, `platform: all`, `last_verified: 2026-07-04`, `review_by: 2026-10-02` (90-day delta), followed immediately by the visible block `**Platform:** All Platforms · **Doc Type:** Reference · **Doc ID:** STD-001 · **Status:** Approved`, then `# EEE SOP Standard` (H1), then `## Summary` (≥30 words) as the first H2. Author these H2 sections (per RESEARCH §Structure): (a) Purpose and Scope; (b) Required Frontmatter Schema — the 7 keys `doc_id, status, owner, doc_type, platform, last_verified, review_by` with the TEMPLATE-SENTINEL `1970-01-01` explanation; (c) Visible Header Block Format — the D-05 single inline paragraph, field set `Platform · Doc Type · Doc ID · Status` in that order, `·` (U+00B7 middle-dot) separator, `## Summary` immediately follows with NO intervening content, and the OQ4 rationale (frontmatter is invisible custom properties → block is retrieval-necessary); explicitly state owner is NOT in the block (D-01) and Last Reviewed is NOT in the block (D-05); (d) Doc Type Taxonomy — `{Runbook, Guide, RCA, Reference}` with the D-02 edge-case rulings: comparison docs (`apv1-vs-apv2.md`, `windows-vs-macos.md`) → Reference, error-codes → Reference, end-user-guides → Guide, RCA retained for forward-compat (zero RCA docs today), and the note that doc_type is audience-agnostic; (e) D2 Last Reviewed Semantics — `Last Reviewed` carries `last_verified` VERBATIM on retrofit (no staleness-clock reset) plus the one-time "v1.15 EEE reformat — content not re-reviewed" Version-History row rule (META-04); (f) Status Values — controlled vocabulary `{Draft, Approved, Superseded}`, new docs default Draft, owner promotes to Approved, supersedence via registry + "Supersedes RE-NNN"/"see RE-NNN" + archive-scope-exclusion; (g) Grounding Notes — body-text-only indexing (OQ4), document-level-only citations (OQ1/SC4), Approved-only in the indexed library, and the exact Draft sentence from CONTEXT §Specific Ideas: "Status: Draft is a label, not an index gate — exclude Draft/superseded docs by library scoping (or SharePoint content-approval), never by relying on the body-text label"; (h) Phase-1 Scope — D-03 positive-named classes (L1/L2 runbooks + admin-setup-* + reference class) IN, D-04 (operations/, device-operations/, cross-platform/apple-business/) OUT to v1.16. End with a `## Version History` table (initial row dated 2026-07-04). Do NOT author the D1 map or C17 needle-spec here — those are Task 2 (same file).
  </action>
  <verify>
    <automated>test -f docs/_standards/EEE-SOP-standard.md && grep -Fq "**Platform:** All Platforms · **Doc Type:** Reference · **Doc ID:** STD-001 · **Status:** Approved" docs/_standards/EEE-SOP-standard.md && grep -qi "Draft is a label, not an index gate" docs/_standards/EEE-SOP-standard.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/_standards/EEE-SOP-standard.md` exists; frontmatter has `doc_id: STD-001` (not an RE-NNN value) and `doc_type: Reference`.
    - The visible block line is exactly `**Platform:** All Platforms · **Doc Type:** Reference · **Doc ID:** STD-001 · **Status:** Approved` and `## Summary` is the first H2 immediately after the H1.
    - Taxonomy section lists exactly `{Runbook, Guide, RCA, Reference}` and states: comparison docs → Reference, error-codes → Reference, end-user-guides → Guide (D-02).
    - D2 section states `Last Reviewed` = `last_verified` verbatim AND names the one-time "v1.15 EEE reformat — content not re-reviewed" Version-History row (META-04).
    - Grounding Notes contains the verbatim sentence "Status: Draft is a label, not an index gate ..." and states body-text-only indexing (OQ4) + document-level-only citations (OQ1).
    - Block-format section explicitly states owner is NOT in the block (D-01) and Last Reviewed is NOT in the block (D-05); field order is Platform · Doc Type · Doc ID · Status.
  </acceptance_criteria>
  <done>The standard's core specification sections (frontmatter schema, D-05 block format, D-02 taxonomy, D2 semantics, status vocabulary, grounding notes, Phase-1 scope) are authored and self-consistent; only the D1 map and C17 needle-spec remain (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Author the D1 normalization map (20 entries) and the C17 needle-spec handoff</name>
  <read_first>
    - docs/_standards/EEE-SOP-standard.md (the file from Task 1 — append these two sections)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-RESEARCH.md (§D1 Platform Normalization Map — the authoritative 20-row table; §C17 Needle-Spec Handoff — the 13 assertions + Phase-114 additions)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-CONTEXT.md (D-09 no-fallback rule; D-01 owner-in-FM-not-block C17 handoff; D-05 block field C17 handoff)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-PATTERNS.md (§EEE-SOP-standard.md table pattern for the D1 map)
  </read_first>
  <action>
    Re-run `grep -rhi "^platform:" docs --include="*.md" | sort | uniq -c` to reconfirm the live variant set before authoring (must match the 20 real variants; the three `Windows | macOS | ... | all` pipe-list rows are TEMPLATE placeholders, NOT corpus variants, and are excluded from the map — Plan 03 fixes them). Append a `## D1 Platform Normalization Map` H2 to `docs/_standards/EEE-SOP-standard.md` with a `| Raw platform: value | Clean visible label |` table carrying all 20 real corpus variants verbatim from RESEARCH §Full D1 Map: `Windows`→Windows, `windows`→Windows, `macOS`→macOS, `macos`→macOS, `iOS`→iOS, `ios`→iOS, `Android`→Android, `android`→Android, `Linux`→Linux, `linux`→Linux, `all`→All Platforms, `windows+macos+ios+android+linux`→All Platforms, `cross-platform`→Cross-Platform, `apple-tv`→Apple TV, `iOS,Android`→iOS + Android, `ios+macos`→iOS + macOS, `ios+ipados+macos`→iOS / iPadOS / macOS, `ios+ipados+macos+tvos`→iOS / iPadOS / macOS / tvOS, `ios+macos+shared-ipad`→iOS + macOS + Shared iPad, `ios+shared-ipad`→iOS + Shared iPad. State the rule immediately after the table: an unmapped `platform:` value is a HARD FAILURE (C17 FAILS the file) — there is NO silent fallback (D-09/META-03). Note that `all` and `windows+macos+ios+android+linux` intentionally share the "All Platforms" label (semantically identical; frontmatter preserves the raw value). Then append a `## C17 Enforcement Reference (Needle-Spec for Phase 115)` H2 enumerating the 13 C17 assertions from RESEARCH §C17 Needle-Spec Handoff plus the Phase-114 additions table (block field set = exactly {Platform, Doc Type, Doc ID, Status} in that order; owner required in frontmatter and ABSENT from the block; single inline paragraph with `·` separator; platform resolves in the D1 map, unmapped = FAIL; status ∈ {Draft, Approved, Superseded}; `## Summary` ≥ 30 words). Add a note that templates (Plan 03) carry placeholder `doc_id`/`owner` and `Status: Draft`, so C17 asserts key-PRESENCE + platform-resolution + structural rules on templates, not frontmatter-value-to-block equality. State plainly that Phase 114 authors the needle-spec ONLY and wires NO validator into scripts/validation's chain (validator-atom deferral — C17 is authored as one indivisible atom in Phase 115).
  </action>
  <verify>
    <automated>grep -c '`Linux`\|`linux`\|`Windows`\|`windows`\|`macOS`\|`macos`\|`iOS`\|`ios`\|`Android`\|`android`' docs/_standards/EEE-SOP-standard.md && grep -qi "unmapped.*hard failure\|hard failure.*no.*fallback\|no silent fallback" docs/_standards/EEE-SOP-standard.md && grep -qi "C17 Enforcement Reference\|Needle-Spec" docs/_standards/EEE-SOP-standard.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The D1 map table contains a row for each of the 20 real corpus variants, INCLUDING all five lowercase case-variants (`windows`, `macos`, `ios`, `android`, `linux`) — the easiest to miss (Pitfall 3).
    - The map does NOT contain the template pipe-list placeholders (`Windows | macOS | all`, etc.) — those are template-only and fixed in Plan 03.
    - Both `all` and `windows+macos+ios+android+linux` map to the label `All Platforms`.
    - The no-fallback rule is stated: an unmapped `platform:` value is a hard failure with no silent fallback (D-09).
    - A `## C17 Enforcement Reference` / needle-spec section enumerates the 13 assertions + the Phase-114 additions (block field set/order, owner-in-FM-not-block, `·` separator, platform-resolves, status vocabulary, Summary ≥30 words) and states no validator is wired into the chain this phase.
    - Re-running the corpus grep produces exactly the 20 real variants covered by the map (plus the 3 template pipe-list rows, which are excluded by design).
  </acceptance_criteria>
  <done>The D1 map (20 entries, no-fallback rule) and the complete C17 needle-spec handoff are appended to the standard; META-03 and the Phase-115 handoff are satisfied; no validator is wired into the chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| standard doc → downstream (templates, registry, C17, retrofit) | The standard is the single source of truth; an error here propagates to all ~178 corpus docs and the C17 gate |
| repo _standards/ path → indexed SharePoint library | The standard is meta-documentation and MUST NOT be uploaded to the grounded library |
| frontmatter → grounding index | Owner (a person name) must never reach the index via the visible block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-03 | Tampering | D1 map missing a real variant | mitigate (high) | Author all 20 verified variants incl. the 5 lowercase case-variants; re-run the corpus grep at author time; acceptance greps for each lowercase variant — a missed variant would FAIL every corpus file using it at Phase 116+ |
| T-114-04 | Information Disclosure | standard authored into indexed-library scope | mitigate | File lives in `docs/_standards/` (underscore-prefixed, non-corpus); grounding-notes section states `_standards`/`_registry` are excluded from the indexed library and from the conversion walk |
| T-114-05 | Information Disclosure | owner (person name) rendered in the visible block | mitigate | D-01: owner is frontmatter-only; block-format spec fixes the field set to exactly {Platform, Doc Type, Doc ID, Status}; acceptance asserts owner is NOT in the block |
| T-114-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase |
</threat_model>

<verification>
- SC2 (STD-01, META-02, META-03, META-04): `docs/_standards/EEE-SOP-standard.md` committed with the D-05 block format, the 20-entry D1 map + no-fallback rule, D2 verbatim semantics + Version-History row rule, the taxonomy, grounding notes, and the C17 needle-spec.
- Corpus grep confirms all 20 real platform variants are covered; the standard lives outside the indexed library.
</verification>

<success_criteria>
The EEE SOP standard is locked and self-consistent; it is the authoritative reference the templates and registry conform to, and it hands off a complete C17 needle-spec to Phase 115 without wiring any validator.
</success_criteria>

<output>
Create `.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-02-SUMMARY.md` when done.
</output>
