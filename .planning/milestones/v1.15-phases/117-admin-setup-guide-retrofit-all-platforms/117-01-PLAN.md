---
phase: 117-admin-setup-guide-retrofit-all-platforms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pipeline/retrofit-guide.mjs
autonomous: true
requirements: [RETRO-02]
must_haves:
  truths:
    - "The forked helper resolves a doc_id for all 57 enrolled admin-setup paths by joining RE-index.md on the Path column (never hand-transcribed) per D-03"
    - "The helper relocates the ENTIRE pre-H1 span (gate blockquote + any 2nd blockquote + HTML comments, in original order) to after ## Summary — not just the first /^>/ run — fixing the confirmed retrofit-runbook.mjs silent-content-loss defect (RESEARCH Pitfall 1)"
    - "The helper injects platform: Windows for the 13 enrolled keyless apv1/apv2 files, resolves every present platform through the verbatim-copied D1_MAP, and stamps owner: Intune Admin Lead + doc_type: Guide uniformly (D-04)"
    - "The helper hard-excludes the 9 mermaid-deferred files (RE-076/077/087/092/106/116/128/134/135) — fails closed (ERROR) if invoked against one by explicit path, never silently skips (D-05)"
    - "A --dry-run across the 57 enrolled files writes nothing to the corpus or registry and reports zero unresolved doc_ids, zero unmapped platforms, and a byte-length-preserved pre-H1 span for every file"
  artifacts:
    - path: "scripts/pipeline/retrofit-guide.mjs"
      provides: "Forked mechanical EEE retrofit helper (D-03): frontmatter key injection, EEE Guide block line, whole-pre-H1-span relocation, Version-History section creation, platform: Windows injection, admin-setup path allowlist + 9-mermaid exclusion"
      contains: "const D1_MAP"
  key_links:
    - from: "scripts/pipeline/retrofit-guide.mjs"
      to: "docs/_registry/RE-index.md"
      via: "path-column join to resolve doc_id"
      pattern: "RE-\\d+"
    - from: "scripts/pipeline/retrofit-guide.mjs"
      to: "scripts/validation/c17-eee-contract.mjs"
      via: "D1_MAP copied verbatim (lines 26-47)"
      pattern: "D1_MAP"
---

<objective>
Fork a new `scripts/pipeline/retrofit-guide.mjs` from the Phase-116 helper `scripts/pipeline/retrofit-runbook.mjs`
and FIX its confirmed silent-content-loss defect before any admin-setup guide is touched. This plan builds and
proves the script only — it writes NOTHING to the corpus or registry (dry-run + self-test). The eight batch plans
(117-02..117-09) invoke it to perform the deterministic half of every guide retrofit.

Purpose: The 116 helper captures only the FIRST pre-H1 `/^>/` run and silently drops everything else before the
H1 — verified to delete a substantive "Rebrand notice" blockquote on ios/02-abm-token.md and macos/01-abm-configuration.md,
and pre-H1 HTML authoring comments on the five AOSP android files (RESEARCH Pitfall 1). Reusing it in place would
breach the reformat-only / no-new-content envelope by LOSING content. Fork (do not refactor in place — protects
the shipped Phase-116 deliverable) and generalize gate relocation to whole-pre-H1-span relocation (RETRO-02, D-03).
Output: One node-builtins-only script mirroring the `scripts/pipeline/` idiom, validated by --self-test (6 sub-tests
including a span-byte-length-preservation proof) and a full 57-file --dry-run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-CONTEXT.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-RESEARCH.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork scripts/pipeline/retrofit-guide.mjs with the whole-pre-H1-span fix</name>
  <files>scripts/pipeline/retrofit-guide.mjs</files>
  <read_first>
    - scripts/pipeline/retrofit-runbook.mjs (the Phase-116 helper to fork; COPY VERBATIM: shebang + node-builtins import block lines 1-31; D1_MAP lines 44-65; padLabel/readFile-CRLF-normalize/walkMd/relNormalize lines 72-113; buildDocIdMap lines 122-132; insertVersionHistoryRow lines 141-179 — the vhIdx===-1 CREATE branch is the ONLY branch used here; frontmatter regex /^---\n([\s\S]*?)\n---/m line 209; TEMPLATE-SENTINEL guard lines 215-220; doc-id-unresolved guard lines 222-226; platform-injection+D1_MAP guard lines 228-238. Study the DEFECTIVE gate-capture lines 278-290 you must REPLACE, and the per-tier owner lines 241-242 / doc_type literal you must change)
    - scripts/validation/c17-eee-contract.mjs (the immutable gate; D1_MAP lines 26-47 the fork's copy must byte-match; enrollment scan lines 519-533; assertions #9 lines 298-329 and #12 lines 387-405)
    - docs/_registry/RE-index.md (RE-076..RE-141 Path-column rows the helper joins to resolve doc_id)
    - docs/_templates/admin-template.md (generic Guide target: frontmatter key set, EEE block line, D3-A order, Summary prescription line 41, reviewer role)
    - docs/_standards/EEE-SOP-standard.md (single-line block format; field order Platform · Doc Type · Doc ID · Status; D2 Last Reviewed = last_verified verbatim; owner-never-in-block rule line 54; Version-History row exact string)
    - docs/admin-setup-ios/02-abm-token.md and docs/admin-setup-android/09-aosp-realwear.md (the two confirmed Pitfall-1 pre-H1 shapes the fix must preserve: a 2nd Rebrand-notice blockquote; a gate blockquote followed by two HTML comments)
  </read_first>
  <action>
    Create `scripts/pipeline/retrofit-guide.mjs` as a standalone FORK of retrofit-runbook.mjs (do NOT refactor the
    116 script in place — it is a shipped, Approved deliverable; a shared refactor risks regressing it). Node
    built-ins ONLY (node:fs, node:path, node:process — zero npm packages, matching the scripts/pipeline/ invariant).
    Support argv flags `--dry-run` (compute and report, write nothing), `--verbose`, `--self-test`, positional file
    paths, and `--all` (process every enrolled .md under the 7 docs/admin-setup-*/ dirs).

    COPY VERBATIM from retrofit-runbook.mjs (per RESEARCH "Don't Hand-Roll" — never diverge or C17 #9/#10 break):
    the D1_MAP constant, padLabel, readFile (CRLF-normalize .replace(/\r\n/g,'\n')), walkMd (lstatSync, skip symlinks),
    relNormalize (backslash→forward-slash), buildDocIdMap (registry Path-column join → Map<relPath, RE-NNN>),
    insertVersionHistoryRow, the frontmatter regex /^---\n([\s\S]*?)\n---/m (m flag mandatory — files may have an
    HTML-comment preamble; NEVER content.startsWith('---')), the TEMPLATE-SENTINEL guard, the doc-id-unresolved guard,
    and the platform-injection + D1_MAP guard.

    Make these FOUR fork-specific changes:
    1. PATH ALLOWLIST + MERMAID EXCLUSION: replace the l1/l2 allowlist with the 7 dirs docs/admin-setup-apv1/,
       -apv2/, -android/, -ios/, -macos/, -linux/, -8021x/. Additionally hard-exclude the 9 mermaid-deferred paths
       (RE-076 apv1/00-overview, RE-077 apv1/01-hardware-hash-upload, RE-087 apv2/00-overview, RE-092 android/00-overview,
       RE-106 ios/00-overview, RE-116 macos/00-overview, RE-128 linux/00-overview, RE-134 8021x/00-overview,
       RE-135 8021x/01-eap-method-overview) — never enumerate them under --all, and fail CLOSED (return ERROR, not a
       silent skip) if invoked against one by explicit path, so the script enforces D-05 itself.
    2. OWNER: uniform constant `const owner = 'Intune Admin Lead';` (D-04) — replace the 116 per-tier ternary. Record
       in output that this deliberately diverges from the platform templates' per-platform reviewer role.
    3. DOC_TYPE: literal `Guide` (not `Runbook`) in both the new-frontmatter assembly and the EEE block-line assembly.
    4. WHOLE-PRE-H1-SPAN RELOCATION (the load-bearing defect fix): REPLACE the defective first-`/^>/`-run-only capture.
       Capture the ENTIRE span between the frontmatter close and the first `/^# [^#]/` H1 (blockquote runs, HTML
       comments, blank lines — regardless of shape), trim only genuinely leading/trailing blank lines, and re-emit
       that whole span, preserving internal ordering and blank-line structure, immediately AFTER the `## Summary`
       placeholder. Never match on the literal "Version gate"/"Platform gate"/"Scope" string — match by structural
       position only.

    Per enrolled target file, emit: frontmatter with prepended doc_id: RE-NNN (registry join), status: Approved,
    owner: Intune Admin Lead, doc_type: Guide; inject platform: Windows ONLY when the file has no platform: key (the
    13 enrolled apv1/apv2 files); RETAIN last_verified, review_by, applies_to, audience verbatim. Body order out:
    EEE block line `**Platform:** <D1 label> · **Doc Type:** Guide · **Doc ID:** RE-NNN · **Status:** Approved`
    (middle-dot U+00B7; owner NEVER in the block) → blank → `# <existing H1>` → blank → `## Summary` → blank →
    `[FILL-IN: >=30 words, per-platform-template (<PLATFORM>) Summary lead]` placeholder → blank → [relocated whole
    pre-H1 span] → [remaining original sections unchanged]. Create a new `## Version History` section at end of file
    (all 57 files LACK one — Pitfall 4; exercise the vhIdx===-1 CREATE branch) with the header row and the data row
    `| YYYY-MM-DD | v1.15 EEE reformat — content not re-reviewed | — |` (em-dash; date filled by the executor at commit).

    In --dry-run print a per-file report (doc_id resolved, platform injected Y/N, D1 label, pre-H1-span element count
    + original-vs-relocated byte length, Version-History created). Exit 0 when all resolve cleanly; exit 1 on any ERROR.
    Do NOT author Summary prose, do NOT fix #12 blockquotes, do NOT edit RE-index.md — those are batch-plan hand-work.
  </action>
  <acceptance_criteria>
    - `node scripts/pipeline/retrofit-guide.mjs --all --dry-run` exits 0 and reports a resolved doc_id for exactly 57
      files (grep the output for `RE-0`/`RE-1` lines == 57), with zero unmapped-platform and zero unresolved-doc_id errors.
    - The dry-run report shows, for every file, relocated-span byte length EQUAL to original pre-H1-span byte length
      (no content dropped) — in particular for ios/02-abm-token.md and android/09..13-aosp-*.md.
    - Invoking the script against any of the 9 mermaid-deferred paths by explicit arg returns ERROR (exit 1), not a write.
    - The script source contains D1_MAP with all entries byte-identical to c17-eee-contract.mjs lines 26-47; imports only
      node:fs, node:path, node:process (grep confirms zero external package names).
    - `git diff --quiet docs/ docs/_registry/RE-index.md` after the dry-run (dry-run writes nothing).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-guide.mjs --all --dry-run; test $? -eq 0 &amp;&amp; git diff --quiet docs/ docs/_registry/RE-index.md</automated>
  </verify>
  <done>The forked helper exists, is node-builtins-only, relocates the whole pre-H1 span with byte-length preservation, stamps uniform owner/Guide, hard-excludes the 9 mermaid files, resolves all 57 doc_ids/platforms, and its dry-run writes nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the fork with --self-test (incl. span-preservation) + full 57-file --dry-run</name>
  <files>scripts/pipeline/retrofit-guide.mjs</files>
  <read_first>
    - scripts/pipeline/retrofit-guide.mjs (the fork authored in Task 1)
    - scripts/pipeline/retrofit-runbook.mjs (the 5 inherited self-test sub-tests lines 352-477 — fixture shape to adapt)
    - scripts/validation/c17-eee-contract.mjs (D1_MAP + platform-resolution logic the dry-run must agree with)
    - .planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-RESEARCH.md (the 13 enrolled keyless apv1/apv2 files; the 7 Pitfall-1 files; the per-directory effort table)
    - docs/_registry/RE-index.md (RE-076..RE-141 rows)
  </read_first>
  <action>
    Add a `--self-test` mode with in-memory fixtures asserting: (a) buildDocIdMap parses a sample RE-index row to the
    correct RE-NNN; (b) a keyless apv1 fixture triggers platform: Windows injection; (c) an ios fixture resolves the
    D1 label `iOS`; (d) a last_verified: 1970-01-01 fixture is REFUSED (sentinel guard); (e) an explicit path outside
    the 7 admin-setup dirs — AND an explicit mermaid-deferred path (e.g. docs/admin-setup-ios/00-overview.md) — are
    both REFUSED (path allowlist + D-05 exclusion). Add a SIXTH sub-test (the fork's new proof): a synthetic pre-H1
    span with TWO blockquotes plus a trailing HTML comment relocates after ## Summary with relocated-span byte length
    EQUAL to the original span byte length (operationalizes the Pitfall-1 "warning sign" — diff pre-H1 span byte-for-byte
    against the post-Summary relocated span). Then run the full 57-file --dry-run and confirm agreement with RESEARCH:
    exactly 13 files flagged for platform: Windows injection (apv1 02-10 + apv2 01-04), zero unmapped platforms, and
    span-byte-length-preserved for all 57 (especially the 7 Pitfall-1 files).
  </action>
  <acceptance_criteria>
    - `node scripts/pipeline/retrofit-guide.mjs --self-test` exits 0 (all 6 sub-tests pass, including the span-preservation test).
    - `node scripts/pipeline/retrofit-guide.mjs --all --dry-run --verbose` reports exactly 13 files flagged for platform: Windows injection and zero unmapped-platform errors.
    - The dry-run output shows span-byte-length equality (original == relocated) for ios/02-abm-token.md, macos/01-abm-configuration.md, and android/09..13-aosp-*.md.
    - `git diff --quiet docs/ docs/_registry/RE-index.md` (no corpus or registry writes from self-test/dry-run).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-guide.mjs --self-test &amp;&amp; node scripts/pipeline/retrofit-guide.mjs --all --dry-run &gt;/dev/null; test $? -eq 0 &amp;&amp; git diff --quiet docs/ docs/_registry/RE-index.md</automated>
  </verify>
  <done>--self-test passes all six sub-tests (span-preservation included), and the full 57-file dry-run confirms 13 injection files, zero unmapped platforms, byte-preserved spans, and zero writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper script → corpus files | The script rewrites Markdown; the 116 defect silently DROPS pre-H1 content (data integrity), and a bug could write outside the admin-setup dirs |
| registry table → doc_id output | An incorrect path-join would stamp the wrong Doc ID into a guide (C17 #9 catches, but late) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Tampering | pre-H1 span capture (silent content loss — RESEARCH Pitfall 1) | mitigate | Whole-pre-H1-span relocation replaces the first-run-only capture; self-test #6 asserts relocated-span byte length == original; --dry-run reports per-file span byte lengths for review; confirmed against the 7 Pitfall-1 files |
| T-117-03 | Tampering | frontmatter / block generation + write path | mitigate | doc_id and D1 label derived programmatically (never hand-transcribed); path allowlist + hard-exclusion of the 9 mermaid files (fail closed); TEMPLATE-SENTINEL guard refuses 1970-01-01; last_verified carried verbatim; CRLF normalized deterministically; --dry-run writes nothing this plan |
| T-117-SC | Tampering | package installs | accept | node-builtins-only (node:fs/path/process); zero npm/pip/cargo installs; no third-party packages introduced, so no supply-chain surface (honest: this is a doc-reshape script) |
</threat_model>

<verification>
- `node scripts/pipeline/retrofit-guide.mjs --self-test` exits 0 (6 sub-tests incl. span-preservation).
- `node scripts/pipeline/retrofit-guide.mjs --all --dry-run` exits 0, resolves all 57 doc_ids, flags exactly 13 platform-injection files, preserves every pre-H1 span byte length, and writes nothing.
- Explicit invocation against any of the 9 mermaid-deferred paths returns ERROR.
- Script is node-builtins-only; D1_MAP byte-identical to c17-eee-contract.mjs lines 26-47.
</verification>

<success_criteria>
The forked retrofit-guide.mjs exists, is node-builtins-only, fixes the whole-pre-H1-span data-loss defect (proven by
byte-length equality), stamps uniform owner: Intune Admin Lead + doc_type: Guide, hard-excludes the 9 mermaid files,
and is proven by --self-test + a zero-write 57-file dry-run. Batch plans 117-02..117-09 can now invoke it.
</success_criteria>

<output>
Create `.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-01-SUMMARY.md` when done.
</output>
