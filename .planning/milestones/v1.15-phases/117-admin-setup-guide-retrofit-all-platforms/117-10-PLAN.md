---
phase: 117-admin-setup-guide-retrofit-all-platforms
plan: 10
type: execute
wave: 10
depends_on: [117-01, 117-02, 117-03, 117-04, 117-05, 117-06, 117-07, 117-08, 117-09]
files_modified: []
autonomous: true
requirements: [RETRO-02]
must_haves:
  truths:
    - "The enrollment-completeness precheck (Phase-115 D-02, scoped to 57) confirms every admin-setup guide EXCEPT the 9 mermaid-deferred files carries all four EEE keys (doc_id, status, owner, doc_type)"
    - "The 9 mermaid-deferred files (RE-076/077/087/092/106/116/128/134/135) remain keyless (no doc_id key) and their RE-index.md Status stays Pending (D-05)"
    - "C17 exits 0 with zero violations across the full admin-setup enrolled class (all 57 files gated in one full-class scan) — SC4"
    - "RE-index.md shows exactly 57 admin-setup Guide rows Approved and exactly 9 Pending"
  artifacts:
    - path: ".planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-10-SUMMARY.md"
      provides: "Phase-gate proof: enrollment-completeness precheck + full-class C17 exit-0 evidence for the 57 enrolled files"
      contains: "C17"
  key_links:
    - from: "full-class C17 run"
      to: "the 57 enrolled admin-setup guides"
      via: "enrollment scan opt-in by doc_id key presence (c17-eee-contract.mjs:519-533)"
      pattern: "exit 0"
---

<objective>
Final phase gate for Phase 117. Run the two-part per-phase success criteria (Phase-115 D-02) across the WHOLE
admin-setup Guide class in one pass: (a) the enrollment-completeness precheck scoped to 57 — every admin-setup guide
EXCEPT the 9 mermaid-deferred files carries all four EEE keys, and the 9 stay keyless — THEN (b) C17 exits 0 on the
57 enrolled files. This plan writes NO corpus/registry content; it is the verification gate proving RETRO-02 / SC4
before the phase closes and hands off to Phase 118.

Purpose: Individual batch plans gate their own files; this gate proves the class is COMPLETE and internally
consistent (no enrolled file missed, no mermaid file accidentally enrolled, registry Approved/Pending counts exact)
before the Phase-119 audit fold depends on it (RETRO-02, D-05, Phase-115 D-02).
Output: A phase-gate SUMMARY recording the precheck result, the full-class C17 exit-0 evidence, and the 57/9 counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-CONTEXT.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-RESEARCH.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrollment-completeness precheck scoped to 57 (9 mermaid files stay keyless/Pending)</name>
  <files></files>
  <read_first>
    - scripts/validation/c17-eee-contract.mjs (enrollment scan lines 519-533: opt-in by doc_id key presence — a keyless file is NOT scanned)
    - .planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-CONTEXT.md (D-05: the exact 9 mermaid-deferred paths RE-076/077/087/092/106/116/128/134/135)
    - docs/_registry/RE-index.md (RE-076..RE-141 rows; the 57 must be Approved, the 9 Pending)
  </read_first>
  <action>
    Enumerate all .md files under the 7 docs/admin-setup-*/ dirs (66 total). Partition into the 57 enrolled and the 9
    mermaid-deferred (apv1/00, apv1/01, apv2/00, android/00, ios/00, macos/00, linux/00, 8021x/00, 8021x/01). Assert:
    (1) every one of the 57 enrolled files has all four EEE frontmatter keys — grep -q "^doc_id:", "^status:",
    "^owner:", "^doc_type:" each succeed (the precheck returns ZERO missing-key lines for the 57); (2) each of the 9
    mermaid-deferred files is KEYLESS — grep -q "^doc_id:" FAILS for all 9 (never enrolled, D-05); (3) in
    docs/_registry/RE-index.md exactly 57 admin-setup Guide rows (RE-076..RE-141 minus the 9) show Status Approved and
    the 9 mermaid rows show Status Pending. This task changes NO files — it is read-only verification. If any assertion
    fails, STOP and report which file/row is inconsistent (do not paper over — a missing enrolled file or an
    accidentally-enrolled mermaid file is a phase-blocking defect).
  </action>
  <acceptance_criteria>
    - All 57 enrolled admin-setup files carry doc_id + status + owner + doc_type (precheck returns zero missing-key lines).
    - All 9 mermaid-deferred files have NO doc_id key (grep -L "^doc_id:" over the 9 lists all 9).
    - RE-index.md: exactly 57 admin-setup Guide rows Approved, exactly 9 Pending (count the RE-076..RE-141 range).
    - `git status --porcelain docs/` shows no changes from this task (read-only).
  </acceptance_criteria>
  <verify>
    <automated>miss=0; for f in $(ls docs/admin-setup-*/*.md | grep -Ev '/(00-overview|01-hardware-hash-upload|01-eap-method-overview)\.md$'); do for k in '^doc_id:' '^status:' '^owner:' '^doc_type:'; do grep -q "$k" "$f" || { echo "MISSING $k in $f"; miss=1; }; done; done; test $miss -eq 0</automated>
  </verify>
  <done>The 57 enrolled files all carry the four EEE keys; the 9 mermaid-deferred files are keyless; registry shows 57 Approved / 9 Pending.</done>
</task>

<task type="auto">
  <name>Task 2: Full-class C17 exit 0 across all 57 enrolled admin-setup guides</name>
  <files></files>
  <read_first>
    - scripts/validation/c17-eee-contract.mjs (the immutable gate; enrollment scan lines 519-533 — a full run auto-includes every doc_id-bearing docs/ file)
    - .planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-RESEARCH.md (all 13 C17 assertions the enrolled files must satisfy; the #12 measurement as belt-and-suspenders)
    - each 117 batch SUMMARY (117-02..117-09 SUMMARY.md — confirm each batch already exited C17 0 for its files)
  </read_first>
  <action>
    Run `node scripts/validation/c17-eee-contract.mjs` (full corpus). Confirm exit 0 with zero violations — this gates
    the 57 newly-enrolled admin-setup guides alongside the already-Approved Phase-113/114/116 files. As a
    belt-and-suspenders check, re-run the #12 blockquote measurement (c17-eee-contract.mjs:387-405 logic) across all 57
    enrolled admin-setup files and confirm zero over-limit top-level groups class-wide. Also confirm the run reports NO
    mermaid violation (#1) — proving the 9 mermaid files stayed un-enrolled (a #1 failure would mean one was
    accidentally given EEE keys). This task changes NO files. Record the full-class exit-0 evidence, the 57/9 counts,
    and the D-04 caveat (uniform owner: Intune Admin Lead deliberately diverges from the per-platform template reviewer
    roles — not an inconsistency) in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with zero violations reported.
    - The #12 re-measurement over the 57 enrolled admin-setup files reports zero over-limit top-level blockquote groups.
    - The C17 output reports zero assertion-#1 (mermaid) violations (the 9 mermaid files stayed un-enrolled).
    - `git status --porcelain docs/` shows no changes from this task (read-only).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs; test $? -eq 0 &amp;&amp; git diff --quiet docs/</automated>
  </verify>
  <done>C17 exits 0 across the full class with the 57 enrolled admin-setup guides gated, zero #12 over-limit groups, zero mermaid violations, and no file changes; SC4 proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict → phase close / Phase-119 audit fold | A false-green gate would let an incomplete/inconsistent class into the frozen-surface re-baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-04 | Repudiation | phase-gate completeness claim | mitigate | Two-part precheck (57-key presence + 9-keyless) AND full-class C17 exit 0 AND exact 57/9 registry counts — three independent proofs; STOP-and-report on any inconsistency rather than close |
| T-117-05 | Tampering | accidental mermaid enrollment | mitigate | Assertion-#1 (mermaid) violation count == 0 in the full run proves none of the 9 gained EEE keys; grep -L doc_id over the 9 confirms keyless |
| T-117-SC | Tampering | package installs | accept | node-builtins-only C17; zero installs; no supply-chain surface |
</threat_model>

<verification>
- Enrollment-completeness precheck: 57 enrolled files carry all four EEE keys; 9 mermaid files keyless.
- `node scripts/validation/c17-eee-contract.mjs` exits 0; zero violations class-wide; zero #12 over-limit; zero #1 mermaid.
- RE-index.md: exactly 57 admin-setup Guide rows Approved, exactly 9 Pending.
- No file changes from this plan (read-only gate).
</verification>

<success_criteria>
The full admin-setup Guide class is complete and internally consistent: all 57 enrolled files carry the four EEE keys
and pass C17 (exit 0), the 9 mermaid-deferred files remain keyless and Pending, and the registry shows exactly 57
Approved / 9 Pending. RETRO-02 / Phase-117 SC1-SC4 proven; ready for phase close and Phase-118 handoff.
</success_criteria>

<output>
Create `.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-10-SUMMARY.md` when done.
</output>
