---
phase: 117-admin-setup-guide-retrofit-all-platforms
plan: 07
type: execute
wave: 7
depends_on: [117-01, 117-06]
files_modified:
  - docs/admin-setup-macos/07-platform-sso-setup.md
  - docs/admin-setup-macos/08-auth-methods-deep-dive.md
  - docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md
  - docs/admin-setup-macos/10-kerberos-sso-extension.md
  - docs/admin-setup-macos/11-graph-api-platform-credential.md
  - docs/_registry/RE-index.md
autonomous: true
requirements: [RETRO-02]
must_haves:
  truths:
    - "All 5 macOS SSO files (RE-123..RE-127) carry the EEE header block: Platform: macOS from D1 map, doc_id from registry, Status: Approved (SC1)"
    - "Each Summary uses the admin-template-macos lead — Platform SSO / enrollment feature + ABM + Intune admin roles + macOS version prerequisite — ≥30 words (SC2, D-03)"
    - "Each file has D3-A structure: block line → H1 → ## Summary → relocated whole pre-H1 span → sections (SC2)"
    - "Each file carries the v1.15 EEE reformat Version-History row and Last Reviewed = last_verified verbatim (SC3)"
    - "C17 exits 0 on all 5 files with zero #12 over-limit blockquote groups (42 groups incl. corpus-worst 1892c on 07); RE-123..127 flipped Approved (SC4)"
  artifacts:
    - path: "docs/admin-setup-macos/07-platform-sso-setup.md"
      provides: "Retrofitted macOS SSO guide RE-123 (corpus-worst single #12 group: 1892c)"
      contains: "doc_id: RE-123"
    - path: "docs/_registry/RE-index.md"
      provides: "RE-123..RE-127 Status flipped Pending → Approved"
      contains: "RE-123"
  key_links:
    - from: "docs/admin-setup-macos/07-platform-sso-setup.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-123 resolved by path join"
      pattern: "RE-123"
    - from: "block line **Platform:** field"
      to: "D1_MAP[macOS]"
      via: "exact D1 label match (C17 #9)"
      pattern: "\\*\\*Platform:\\*\\* macOS"
---

<objective>
Retrofit the macOS Platform SSO admin-setup guides — files 07-11 (RE-123..RE-127, 5 files: Platform SSO setup, auth
methods deep-dive, Enterprise SSO plug-in migration, Kerberos SSO extension, Graph API Platform Credential) — to the
EEE standard so C17 exits 0 on every file. Fewer files but the HEAVIEST single-group #12 violations in the entire
corpus concentrate here (42 groups; 07-platform-sso-setup carries the corpus-worst 1892c group; 09 has 1392c, 08 has
1043c) — these are large multi-section/code-bearing callouts requiring careful Transform B de-blockquoting.

Purpose: The macOS core|SSO split (D-02) isolates these worst-offender single groups into their own reviewable
checkpoint so the mechanical Transform B work does not swamp the core-macOS batch (RETRO-02, D-02, D-03).
Output: 5 EEE-conformant guide files + registry Status updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-CONTEXT.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-RESEARCH.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-PATTERNS.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical transform of RE-123..RE-127 via the helper</name>
  <files>docs/admin-setup-macos/07-platform-sso-setup.md, docs/admin-setup-macos/08-auth-methods-deep-dive.md, docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md, docs/admin-setup-macos/10-kerberos-sso-extension.md, docs/admin-setup-macos/11-graph-api-platform-credential.md</files>
  <read_first>
    - scripts/pipeline/retrofit-guide.mjs (the forked helper from 117-01)
    - docs/_templates/admin-template-macos.md (target Guide shape; Summary prescription line 42)
    - docs/_registry/RE-index.md (RE-123..RE-127 path→doc_id rows)
    - scripts/validation/c17-eee-contract.mjs (assertions #8, #9, #10)
    - each of the 5 target files (confirm platform: macOS frontmatter, pre-H1 gate blockquote, no existing Version History)
  </read_first>
  <action>
    Run the helper on the 5 batch files (--dry-run first, review the diff + span-byte-length report, then write). The
    helper injects doc_id (RE-123..RE-127), status: Approved, owner: Intune Admin Lead, doc_type: Guide; carries
    `platform: macOS` verbatim. It emits `**Platform:** macOS · **Doc Type:** Guide · **Doc ID:** RE-NNN · **Status:**
    Approved`, relocates the WHOLE pre-H1 span after `## Summary`, and creates a `## Version History` section with the
    reformat row (fill commit date). Confirm last_verified carried verbatim.
  </action>
  <acceptance_criteria>
    - Enrollment precheck returns zero lines: each of the 5 files satisfies grep -q "^doc_id:", "^status:", "^owner: Intune Admin Lead", "^doc_type: Guide".
    - Every block line begins `**Platform:** macOS`; `grep -c "v1.15 EEE reformat — content not re-reviewed" <file>` == 1 for each.
    - No file contains last_verified: 1970-01-01; dry-run span-byte-length shows original == relocated for all 5.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-guide.mjs docs/admin-setup-macos/07-platform-sso-setup.md docs/admin-setup-macos/08-auth-methods-deep-dive.md docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md docs/admin-setup-macos/10-kerberos-sso-extension.md docs/admin-setup-macos/11-graph-api-platform-credential.md; test $? -eq 0</automated>
  </verify>
  <done>All 5 macOS SSO files carry the four EEE keys + Platform: macOS block line, relocated whole pre-H1 span, and Version-History row.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-author the ## Summary prose (macOS-template lead, ≥30 words)</name>
  <files>docs/admin-setup-macos/07-platform-sso-setup.md, docs/admin-setup-macos/08-auth-methods-deep-dive.md, docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md, docs/admin-setup-macos/10-kerberos-sso-extension.md, docs/admin-setup-macos/11-graph-api-platform-credential.md</files>
  <read_first>
    - docs/_templates/admin-template-macos.md (line 42: macOS Summary prescription — enrollment method/feature incl. Platform SSO, ABM + Intune roles, macOS version prerequisite)
    - scripts/validation/c17-eee-contract.mjs (assertion #5 ≥30 words; #4 Summary first H2)
    - each target file's existing intro + Prerequisites content (source to summarize — reformat only)
  </read_first>
  <action>
    Replace each `[FILL-IN]` Summary placeholder with ≥30 words to the D-03 macOS lead (admin-template-macos.md:42):
    state the SSO feature the file covers (07 = Platform SSO setup, 08 = auth-method selection deep-dive, 09 =
    Enterprise SSO plug-in & migration, 10 = Kerberos SSO extension, 11 = Graph API Platform Credential management),
    the required ABM/Intune admin roles, and any macOS version prerequisite — then summarize the file's existing scope
    (no new claims). Each Summary ≥30 words, first H2; no L1/L2 safety banner.
  </action>
  <acceptance_criteria>
    - Each file has exactly one `## Summary` as its first H2, ≥30 words (verified by C17 #5 in Task 3).
    - Each Summary names its SSO feature and the ABM/Intune roles / macOS version prereq; none uses the L1/L2 read-only banner text.
  </acceptance_criteria>
  <verify>
    <automated>for f in docs/admin-setup-macos/07-*.md docs/admin-setup-macos/08-*.md docs/admin-setup-macos/09-*.md docs/admin-setup-macos/1[01]-*.md; do awk '/^## /{print; exit}' "$f" | grep -q "## Summary" || { echo "Summary not first H2: $f"; exit 1; }; done</automated>
  </verify>
  <done>All 5 Summaries are macOS-template-led (SSO feature + ABM/Intune roles + macOS version prereq), ≥30 words, first H2.</done>
</task>

<task type="auto">
  <name>Task 3: #12 blockquote compliance (Transform B heavy), C17 exit 0, registry Approved</name>
  <files>docs/admin-setup-macos/07-platform-sso-setup.md, docs/admin-setup-macos/08-auth-methods-deep-dive.md, docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md, docs/admin-setup-macos/10-kerberos-sso-extension.md, docs/admin-setup-macos/11-graph-api-platform-credential.md, docs/_registry/RE-index.md</files>
  <read_first>
    - scripts/validation/c17-eee-contract.mjs (assertion #12 lines 387-405; bare `>` does NOT split; blockquoted code fences are NOT masked — every char counts)
    - .planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-RESEARCH.md (per-file #12 table: 07=11 (max 1892 corpus-worst), 08=10 (1043), 09=5 (1392), 10=12 (892), 11=4 (462) — 42 total; Pitfall 3 blockquoted-fence detail)
    - .planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-PATTERNS.md (Transform B de-blockquote for code-bearing/multi-section WARNING boxes)
    - docs/_registry/RE-index.md (Status column for RE-123..RE-127)
  </read_first>
  <action>
    Run the #12 measurement over the 5 files (RESEARCH counts 42 over-limit groups, dominated by very large single
    groups). For the corpus-worst 1892c group on 07 and the 1392c/1043c groups on 09/08: if the group is a single
    long continuous-prose "Key distinction" callout, use Transform A (sentence-boundary split with a TRULY empty line);
    if it is a multi-section WARNING/DANGER box with bare-`>` paragraph breaks and/or an embedded `> \`\`\`` code fence,
    use Transform B (de-blockquote the entire callout to a bold-led paragraph — this turns the embedded fence into a
    real top-level fence that inCodeFence then masks). Preserve EVERY word; escalate any unsplittable atomic >200c
    sentence. Re-run the measurement to prove zero over-limit groups remain, then run C17 → exit 0 for the 5 files.
    Finally flip RE-index.md Status Pending → Approved for RE-123..RE-127.
  </action>
  <acceptance_criteria>
    - The #12 measurement reports zero over-limit top-level blockquote groups across the 5 files.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with zero violations for any of the 5 batch paths.
    - A word-set diff (pre- vs post-#12-fix) shows no words added or removed; any de-blockquoted code fence is now a real top-level fence (still present, unchanged content).
    - RE-index.md rows RE-123..RE-127 show `Approved`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs; test $? -eq 0</automated>
  </verify>
  <done>Zero over-limit blockquotes remain (incl. the 1892c corpus-worst group), C17 exits 0 on all 5 files, and RE-123..RE-127 are Approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper output → indexed body text | EEE block line and Summary lead are indexed and recited by Copilot |
| #12 reformat → content envelope | Transform A/B must preserve every word; embedded code fences must survive de-blockquoting verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Tampering | helper write of 5 files (pre-H1 span loss) | mitigate | Whole-pre-H1-span relocation (117-01 fix); dry-run span-byte-length report; git diff review before commit; C17 gate before registry flip |
| T-117-02 | Tampering | #12 splits/de-blockquote (corpus-worst 1892c group) | mitigate | Word-preserving Transform A/B only; pre/post word-set diff proves zero change; #12 re-measurement proves zero over-limit; Transform B for code-embedded WARNING boxes so fences become real masked fences; escalate unsplittable atomic sentences |
| T-117-SC | Tampering | package installs | accept | node-builtins-only; zero installs; no supply-chain surface |
</threat_model>

<verification>
- Enrollment precheck returns zero lines for RE-123..RE-127.
- `node scripts/validation/c17-eee-contract.mjs` exits 0; zero violations on the 5 batch paths.
- #12 measurement reports zero over-limit groups (incl. the 1892c corpus-worst); word-set diff clean.
- RE-index.md RE-123..RE-127 = Approved.
</verification>

<success_criteria>
All 5 enrolled macOS SSO guides are EEE-conformant, C17-green, macOS-template-Summary-led, and Approved — including
the corpus-worst 1892c blockquote group resolved word-preservingly. SC1-SC4 satisfied for RE-123..RE-127.
</success_criteria>

<output>
Create `.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-07-SUMMARY.md` when done.
</output>
