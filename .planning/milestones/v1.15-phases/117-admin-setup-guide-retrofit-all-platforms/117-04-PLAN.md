---
phase: 117-admin-setup-guide-retrofit-all-platforms
plan: 04
type: execute
wave: 4
depends_on: [117-01, 117-03]
files_modified:
  - docs/admin-setup-android/09-aosp-realwear.md
  - docs/admin-setup-android/10-aosp-zebra.md
  - docs/admin-setup-android/11-aosp-pico.md
  - docs/admin-setup-android/12-aosp-htc-vive-focus.md
  - docs/admin-setup-android/13-aosp-meta-quest.md
  - docs/_registry/RE-index.md
autonomous: true
requirements: [RETRO-02]
must_haves:
  truths:
    - "All 5 AOSP-OEM files (RE-101..RE-105) carry the EEE header block: Platform: Android from D1 map, doc_id from registry, Status: Approved (SC1)"
    - "All 5 files exercise the Pitfall-1 fix: their pre-H1 HTML authoring comments (MGP/Zero-Touch intentional-omission notes) survive relocation after ## Summary with zero content loss (D-05-adjacent data integrity)"
    - "Each Summary uses the admin-template-android lead noting AOSP enrollment (QR-only, no Managed Google Play / no Zero-Touch) + Intune admin role — ≥30 words (SC2, D-03)"
    - "Each file carries the v1.15 EEE reformat Version-History row and Last Reviewed = last_verified verbatim (SC3)"
    - "C17 exits 0 on all 5 files with zero #12 over-limit blockquote groups (12 groups); RE-101..105 flipped Approved (SC4)"
  artifacts:
    - path: "docs/admin-setup-android/09-aosp-realwear.md"
      provides: "Retrofitted AOSP-OEM guide RE-101 (confirmed Pitfall-1 pre-H1 HTML-comment case)"
      contains: "doc_id: RE-101"
    - path: "docs/_registry/RE-index.md"
      provides: "RE-101..RE-105 Status flipped Pending → Approved"
      contains: "RE-101"
  key_links:
    - from: "docs/admin-setup-android/09-aosp-realwear.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-101 resolved by path join"
      pattern: "RE-101"
    - from: "block line **Platform:** field"
      to: "D1_MAP[Android]"
      via: "exact D1 label match (C17 #9)"
      pattern: "\\*\\*Platform:\\*\\* Android"
---

<objective>
Retrofit the Android AOSP-OEM admin-setup guides — files 09-13 (RE-101..RE-105, 5 files: RealWear, Zebra, Pico,
HTC VIVE Focus, Meta Quest) — to the EEE standard so C17 exits 0 on every file. This is a LIGHT #12 batch (12 groups)
but EVERY file is a confirmed Pitfall-1 case: each carries a gate blockquote FOLLOWED BY two pre-H1 HTML comments
(explaining AOSP's intentional MGP/Zero-Touch omission) that the un-fixed 116 helper would silently delete.

Purpose: These 5 files are the primary reason the 117-01 whole-pre-H1-span fix exists; the batch validates that fix
against every real HTML-comment case while keeping the Android split platform-homogeneous (RETRO-02, D-02, D-05-adjacent).
Output: 5 EEE-conformant guide files + registry Status updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-CONTEXT.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-RESEARCH.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-PATTERNS.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical transform of RE-101..RE-105 with pre-H1 HTML-comment preservation</name>
  <files>docs/admin-setup-android/09-aosp-realwear.md, docs/admin-setup-android/10-aosp-zebra.md, docs/admin-setup-android/11-aosp-pico.md, docs/admin-setup-android/12-aosp-htc-vive-focus.md, docs/admin-setup-android/13-aosp-meta-quest.md</files>
  <read_first>
    - scripts/pipeline/retrofit-guide.mjs (the forked helper — its whole-pre-H1-span relocation is the load-bearing behavior here)
    - docs/_templates/admin-template-android.md (target Guide shape; Summary prescription line 50)
    - docs/_registry/RE-index.md (RE-101..RE-105 path→doc_id rows)
    - each of the 5 target files (VERIFY the pre-H1 structure: a gate blockquote run PLUS two HTML comments about intentional MGP/Zero-Touch omission — all must survive)
    - .planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-RESEARCH.md (Pitfall 1: the exact confirmed shape of these 5 files)
  </read_first>
  <action>
    Run the helper on the 5 batch files (--dry-run first — CRITICALLY review the span-byte-length report: original
    pre-H1-span byte length MUST equal relocated-span byte length for each file, proving both HTML comments survived —
    then write). The helper injects doc_id (RE-101..RE-105), status: Approved, owner: Intune Admin Lead, doc_type: Guide;
    carries `platform: Android` verbatim. It emits the `**Platform:** Android · **Doc Type:** Guide · **Doc ID:** RE-NNN ·
    **Status:** Approved` block line, relocates the ENTIRE pre-H1 span (gate blockquote + BOTH HTML comments, in original
    order) after `## Summary`, and creates the `## Version History` section with the reformat row (fill commit date).
    Confirm last_verified carried verbatim. If any file's relocated-span byte length differs from its original, STOP and
    fix the helper — do not commit a file that dropped a comment.
  </action>
  <acceptance_criteria>
    - Enrollment precheck returns zero lines: each of the 5 files satisfies grep -q "^doc_id:", "^status:", "^owner: Intune Admin Lead", "^doc_type: Guide".
    - Each file still contains BOTH pre-H1 HTML comments (grep -c "intentionally omitted" per file matches its pre-retrofit count; now positioned after ## Summary).
    - Every block line begins `**Platform:** Android`; `grep -c "v1.15 EEE reformat — content not re-reviewed" <file>` == 1 for each.
    - The dry-run span-byte-length report shows original == relocated for all 5 files.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-guide.mjs docs/admin-setup-android/09-aosp-realwear.md docs/admin-setup-android/10-aosp-zebra.md docs/admin-setup-android/11-aosp-pico.md docs/admin-setup-android/12-aosp-htc-vive-focus.md docs/admin-setup-android/13-aosp-meta-quest.md; test $? -eq 0</automated>
  </verify>
  <done>All 5 AOSP-OEM files carry the four EEE keys + Platform: Android block line, both pre-H1 HTML comments preserved and relocated after ## Summary, and the Version-History row.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-author the ## Summary prose (Android-template lead, AOSP framing, ≥30 words)</name>
  <files>docs/admin-setup-android/09-aosp-realwear.md, docs/admin-setup-android/10-aosp-zebra.md, docs/admin-setup-android/11-aosp-pico.md, docs/admin-setup-android/12-aosp-htc-vive-focus.md, docs/admin-setup-android/13-aosp-meta-quest.md</files>
  <read_first>
    - docs/_templates/admin-template-android.md (line 50: Android Summary prescription)
    - scripts/validation/c17-eee-contract.mjs (assertion #5 ≥30 words; #4 Summary first H2)
    - each target file's existing intro (OEM-specific AOSP enrollment scope to summarize — reformat only)
  </read_first>
  <action>
    Replace each `[FILL-IN]` Summary placeholder with ≥30 words to the D-03 Android lead: state that the file covers
    AOSP (Android Open Source Project) device management for the specific OEM (RealWear/Zebra/Pico/HTC VIVE Focus/Meta
    Quest), that AOSP uses QR-only enrollment and does NOT use Managed Google Play or the Zero-Touch portal, and the
    required Intune admin role — then summarize the file's existing scope (no new claims). Each Summary ≥30 words, first H2.
  </action>
  <acceptance_criteria>
    - Each file has exactly one `## Summary` as its first H2, ≥30 words (verified by C17 #5 in Task 3).
    - Each Summary names AOSP and the specific OEM and an Intune admin role; none uses the L1/L2 read-only banner text.
  </acceptance_criteria>
  <verify>
    <automated>for f in docs/admin-setup-android/09-*.md docs/admin-setup-android/1[0-3]-*.md; do awk '/^## /{print; exit}' "$f" | grep -q "## Summary" || { echo "Summary not first H2: $f"; exit 1; }; done</automated>
  </verify>
  <done>All 5 Summaries are Android-template-led with AOSP framing, ≥30 words, first H2.</done>
</task>

<task type="auto">
  <name>Task 3: #12 blockquote compliance, C17 exit 0, registry Approved</name>
  <files>docs/admin-setup-android/09-aosp-realwear.md, docs/admin-setup-android/10-aosp-zebra.md, docs/admin-setup-android/11-aosp-pico.md, docs/admin-setup-android/12-aosp-htc-vive-focus.md, docs/admin-setup-android/13-aosp-meta-quest.md, docs/_registry/RE-index.md</files>
  <read_first>
    - scripts/validation/c17-eee-contract.mjs (assertion #12 lines 387-405; HTML comments are NOT `/^>/` lines so they do not count toward #12 — only the gate blockquote does)
    - .planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-RESEARCH.md (per-file #12 table: 09=1, 10=3, 11=3, 12=2, 13=3 groups — 12 total, all small, Transform A)
    - .planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-PATTERNS.md (Transform A word-preserving split)
    - docs/_registry/RE-index.md (Status column for RE-101..RE-105)
  </read_first>
  <action>
    Run the #12 measurement over the 5 files (RESEARCH counts 12 small over-limit groups, max 643c). Apply Transform A
    (sentence-boundary split with a TRULY empty line) to each — these are short prose callouts, no embedded code, so
    Transform B is not needed. Preserve EVERY word. Re-run the measurement to prove zero over-limit groups remain, then
    run C17 → exit 0 for the 5 files. Finally flip RE-index.md Status Pending → Approved for RE-101..RE-105.
  </action>
  <acceptance_criteria>
    - The #12 measurement reports zero over-limit top-level blockquote groups across the 5 files.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with zero violations for any of the 5 batch paths.
    - A word-set diff (pre- vs post-#12-fix) shows no words added or removed; both HTML comments still present per file.
    - RE-index.md rows RE-101..RE-105 show `Approved`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs; test $? -eq 0</automated>
  </verify>
  <done>Zero over-limit blockquotes remain, C17 exits 0 on all 5 files with both HTML comments intact, and RE-101..RE-105 are Approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper output → indexed body text | EEE block line and Summary lead are indexed and recited by Copilot |
| pre-H1 HTML comments → content envelope | The AOSP intentional-omission comments MUST survive relocation (data integrity — the exact 116-defect target) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Tampering | pre-H1 HTML-comment loss (RESEARCH Pitfall 1, all 5 files) | mitigate | Whole-pre-H1-span relocation (117-01 fix); mandatory dry-run span-byte-length equality check per file; grep -c "intentionally omitted" preserved count; STOP-and-fix if any span shrinks |
| T-117-02 | Tampering | #12 splits (12 small groups) | mitigate | Word-preserving Transform A only; pre/post word-set diff; #12 re-measurement proves zero over-limit |
| T-117-SC | Tampering | package installs | accept | node-builtins-only; zero installs; no supply-chain surface |
</threat_model>

<verification>
- Enrollment precheck returns zero lines for RE-101..RE-105.
- `node scripts/validation/c17-eee-contract.mjs` exits 0; zero violations on the 5 batch paths.
- Both pre-H1 HTML comments preserved per file (grep count unchanged); span-byte-length equality confirmed.
- #12 measurement reports zero over-limit groups; RE-index.md RE-101..RE-105 = Approved.
</verification>

<success_criteria>
All 5 enrolled AOSP-OEM guides are EEE-conformant, C17-green, Android-template-Summary-led, with every pre-H1 HTML
comment preserved, and Approved in the registry. SC1-SC4 satisfied for RE-101..RE-105; the whole-pre-H1-span fix is
validated against every confirmed HTML-comment case.
</success_criteria>

<output>
Create `.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-04-SUMMARY.md` when done.
</output>
