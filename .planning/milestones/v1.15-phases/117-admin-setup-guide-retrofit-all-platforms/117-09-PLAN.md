---
phase: 117-admin-setup-guide-retrofit-all-platforms
plan: 09
type: execute
wave: 9
depends_on: [117-01, 117-08]
files_modified:
  - docs/admin-setup-8021x/02-cert-delivery-foundation.md
  - docs/admin-setup-8021x/03-windows.md
  - docs/admin-setup-8021x/04-macos.md
  - docs/admin-setup-8021x/05-ios.md
  - docs/admin-setup-8021x/06-android.md
  - docs/admin-setup-8021x/07-linux.md
  - docs/_registry/RE-index.md
autonomous: true
requirements: [RETRO-02]
must_haves:
  truths:
    - "All 6 enrolled 802.1X files (RE-136..RE-141) carry the EEE header block with the CORRECT per-file D1 Platform label: 02=All Platforms, 03=Windows, 04=macOS, 05=iOS, 06=Android, 07=Linux (SC1)"
    - "Each Summary uses the platform template matched to that file's frontmatter platform (03→Windows generic, 04→macOS, 05→iOS, 06→Android, 02/07→generic scope minus framework clause) — ≥30 words (SC2, D-03)"
    - "Each file has D3-A structure: block line → H1 → ## Summary → relocated whole pre-H1 span → sections (SC2)"
    - "Each file carries the v1.15 EEE reformat Version-History row and Last Reviewed = last_verified verbatim (SC3)"
    - "C17 exits 0 on all 6 files with zero #12 over-limit blockquote groups (18 groups; 03-windows 1868c dot3svc + 07-linux 949c need Transform B); RE-136..141 flipped Approved (SC4)"
  artifacts:
    - path: "docs/admin-setup-8021x/03-windows.md"
      provides: "Retrofitted 802.1X Windows guide RE-137 (1868c dot3svc code-embedded WARNING — Transform B)"
      contains: "doc_id: RE-137"
    - path: "docs/_registry/RE-index.md"
      provides: "RE-136..RE-141 Status flipped Pending → Approved (RE-134/135 stay Pending)"
      contains: "RE-136"
  key_links:
    - from: "docs/admin-setup-8021x/04-macos.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-138 resolved by path join"
      pattern: "RE-138"
    - from: "each file's block **Platform:** field"
      to: "D1_MAP[per-file platform]"
      via: "exact per-file D1 label match (C17 #9)"
      pattern: "\\*\\*Platform:\\*\\* (All Platforms|Windows|macOS|iOS|Android|Linux)"
---

<objective>
Retrofit the 802.1X network-authentication admin-setup guides as ONE cohesive batch — enrolled files 02,03,04,05,06,07
(RE-136..RE-141, 6 files) — to the EEE standard so C17 exits 0 on every file. This is the locked-cohesive plan (D-02):
02/03/04/05/06/07 are heterogeneous (per-platform + shared foundation) but the 03-07 per-platform files depend on the
shared 00/01/02 foundations, so they are NOT scattered into the platform plans. 8021x/00-overview (RE-134) and
8021x/01-eap-method-overview (RE-135) are mermaid-deferred — leave them keyless/Pending. Structurally the trickiest
batch: per-file D1 label AND per-file Summary template differ, and 03-windows carries a 1868c code-embedded WARNING box.

Purpose: The 802.1X set is tightly cross-referential (03-07 require the 00/01/02 EAP-method + cert-delivery foundations);
its multi-label heterogeneity is a per-file mechanical match (C17 #9), NOT a correctness risk — only reviewer eyeballing
is harder, so it stays cohesive (RETRO-02, D-02, D-03).
Output: 6 EEE-conformant guide files + registry Status updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-CONTEXT.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-RESEARCH.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-PATTERNS.md
@.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical transform of RE-136..RE-141 via the helper (per-file D1 labels)</name>
  <files>docs/admin-setup-8021x/02-cert-delivery-foundation.md, docs/admin-setup-8021x/03-windows.md, docs/admin-setup-8021x/04-macos.md, docs/admin-setup-8021x/05-ios.md, docs/admin-setup-8021x/06-android.md, docs/admin-setup-8021x/07-linux.md</files>
  <read_first>
    - scripts/pipeline/retrofit-guide.mjs (the forked helper from 117-01)
    - docs/_templates/admin-template.md (generic Guide shape for 02/03/07; the per-platform templates govern 04/05/06 Summaries in Task 2)
    - docs/_registry/RE-index.md (RE-136..RE-141 path→doc_id rows; RE-134/135 stay Pending)
    - scripts/validation/c17-eee-contract.mjs (assertion #9 exact per-file block↔frontmatter match; #10 D1 resolution; the D1_MAP lowercase keys all/windows/macos/ios/android/linux)
    - each of the 6 target files (confirm the heterogeneous frontmatter platform values: 02=all, 03=windows, 04=macos, 05=ios, 06=android, 07=linux — all lowercase, all in D1_MAP)
  </read_first>
  <action>
    Run the helper on the 6 batch files (--dry-run first, review the diff + span-byte-length report + the per-file D1
    label mapping, then write). The helper injects doc_id (RE-136..RE-141), status: Approved, owner: Intune Admin Lead,
    doc_type: Guide; carries each file's existing lowercase platform verbatim and resolves the D1 label per file:
    02 `all`→All Platforms, 03 `windows`→Windows, 04 `macos`→macOS, 05 `ios`→iOS, 06 `android`→Android, 07 `linux`→Linux.
    It emits `**Platform:** <that D1 label> · **Doc Type:** Guide · **Doc ID:** RE-NNN · **Status:** Approved`, relocates
    the WHOLE pre-H1 span after `## Summary`, and creates a `## Version History` section with the reformat row (fill
    commit date). Confirm last_verified carried verbatim; confirm 8021x/00 and 8021x/01 untouched.
  </action>
  <acceptance_criteria>
    - Enrollment precheck returns zero lines: each of the 6 files satisfies grep -q "^doc_id:", "^status:", "^owner: Intune Admin Lead", "^doc_type: Guide".
    - The block Platform label matches the file: 02="All Platforms", 03="Windows", 04="macOS", 05="iOS", 06="Android", 07="Linux" (grep each block line).
    - `grep -c "v1.15 EEE reformat — content not re-reviewed" <file>` == 1 for each; 8021x/00-overview.md and 8021x/01-eap-method-overview.md remain keyless; no file contains last_verified: 1970-01-01.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-guide.mjs docs/admin-setup-8021x/02-cert-delivery-foundation.md docs/admin-setup-8021x/03-windows.md docs/admin-setup-8021x/04-macos.md docs/admin-setup-8021x/05-ios.md docs/admin-setup-8021x/06-android.md docs/admin-setup-8021x/07-linux.md; test $? -eq 0</automated>
  </verify>
  <done>All 6 802.1X files carry the four EEE keys + the CORRECT per-file Platform block label, relocated whole pre-H1 span, and Version-History row; the 2 mermaid-deferred 802.1X files untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-author the ## Summary prose (per-file platform-template lead, ≥30 words)</name>
  <files>docs/admin-setup-8021x/02-cert-delivery-foundation.md, docs/admin-setup-8021x/03-windows.md, docs/admin-setup-8021x/04-macos.md, docs/admin-setup-8021x/05-ios.md, docs/admin-setup-8021x/06-android.md, docs/admin-setup-8021x/07-linux.md</files>
  <read_first>
    - docs/_templates/admin-template.md (line 41), admin-template-macos.md (line 42), admin-template-ios.md (line 42), admin-template-android.md (line 50) — the per-file Summary prescriptions keyed by platform (D-03/3A′)
    - scripts/validation/c17-eee-contract.mjs (assertion #5 ≥30 words; #4 Summary first H2)
    - each target file's existing intro + Prerequisites content (source to summarize — reformat only)
  </read_first>
  <action>
    Replace each `[FILL-IN]` Summary placeholder with ≥30 words authored to the platform template that governs THAT file
    (D-03): 03-windows → generic Windows lead (admin-template.md:41) MINUS the APv1/APv2 framework clause (802.1X is not
    an Autopilot-framework topic) — state Windows + 802.1X task scope + Intune admin role; 04-macos → macOS lead
    (admin-template-macos.md:42, enrollment/profile method + ABM/Intune roles + macOS version prereq framed for 802.1X);
    05-ios → iOS lead (admin-template-ios.md:42); 06-android → Android lead (admin-template-android.md:50, enrollment
    mode + MGP/Zero-Touch + Intune role framed for 802.1X); 02-cert-delivery-foundation (platform: all) and 07-linux →
    generic scope-lead (platform + task scope + Intune admin role) MINUS the framework clause. Summarize each file's
    existing scope only — no new claims. Each Summary ≥30 words, first H2; no L1/L2 safety banner.
  </action>
  <acceptance_criteria>
    - Each file has exactly one `## Summary` as its first H2, ≥30 words (verified by C17 #5 in Task 3).
    - The macOS/iOS/Android files' Summaries carry their platform-specific required elements (ABM/Intune roles for 04/05; enrollment mode + Intune role for 06); the Windows/all/Linux files omit the APv1/APv2 framework clause; none uses the L1/L2 read-only banner text.
  </acceptance_criteria>
  <verify>
    <automated>for f in docs/admin-setup-8021x/0[2-7]-*.md; do awk '/^## /{print; exit}' "$f" | grep -q "## Summary" || { echo "Summary not first H2: $f"; exit 1; }; done</automated>
  </verify>
  <done>All 6 Summaries use the correct per-file platform-template lead, ≥30 words, first H2.</done>
</task>

<task type="auto">
  <name>Task 3: #12 blockquote compliance (Transform B for code-embedded WARNING boxes), C17 exit 0, registry Approved</name>
  <files>docs/admin-setup-8021x/02-cert-delivery-foundation.md, docs/admin-setup-8021x/03-windows.md, docs/admin-setup-8021x/04-macos.md, docs/admin-setup-8021x/05-ios.md, docs/admin-setup-8021x/06-android.md, docs/admin-setup-8021x/07-linux.md, docs/_registry/RE-index.md</files>
  <read_first>
    - scripts/validation/c17-eee-contract.mjs (assertion #12 lines 387-405; bare `>` does NOT split; blockquoted code fences `> \`\`\`powershell` are NOT masked — every char counts, Pitfall 3)
    - .planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-RESEARCH.md (per-file #12 table: 02=4 (966), 03=3 (1868 dot3svc), 04=2 (912), 05=2 (843), 06=2 (1072), 07=5 (949) — 18 total; the dot3svc Transform B worked example)
    - .planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-PATTERNS.md (Transform B de-blockquote for code-embedded WARNING/DANGER boxes)
    - docs/_registry/RE-index.md (Status column for RE-136..RE-141)
  </read_first>
  <action>
    Run the #12 measurement over the 6 files (RESEARCH counts 18 over-limit groups, several very large). For 03-windows'
    1868c dot3svc WARNING (prose + a `> \`\`\`powershell` Get-Service/Set-Service snippet + Remediations paragraph, all
    one group because bare `>` lines don't split and the blockquoted fence isn't masked) use Transform B: de-blockquote
    the entire callout to a bold-led admonition so the embedded fence becomes a REAL top-level fence (then masked). Apply
    the same to the other large code/multi-section groups on 06/07/04/05/02; use Transform A for single-paragraph prose
    callouts. Preserve EVERY word and every code line verbatim; escalate any unsplittable atomic >200c sentence. Re-run
    the measurement to prove zero over-limit groups remain, then run C17 → exit 0 for the 6 files. Finally flip
    RE-index.md Status Pending → Approved for RE-136..RE-141 (leave RE-134/135 Pending).
  </action>
  <acceptance_criteria>
    - The #12 measurement reports zero over-limit top-level blockquote groups across the 6 files.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with zero violations for any of the 6 batch paths.
    - A word-set diff (pre- vs post-#12-fix) shows no words added or removed; every de-blockquoted PowerShell/shell code line is still present verbatim as a real top-level fence.
    - RE-index.md rows RE-136..RE-141 show `Approved`; RE-134, RE-135 remain `Pending`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs; test $? -eq 0</automated>
  </verify>
  <done>Zero over-limit blockquotes remain (incl. the 1868c dot3svc group), C17 exits 0 on all 6 files, and RE-136..RE-141 are Approved while RE-134/135 stay Pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper output → indexed body text | Per-file EEE block label and Summary lead are indexed and recited by Copilot; a wrong per-file D1 label mis-grounds citations |
| #12 reformat → content envelope | Transform A/B must preserve every word and every embedded code line (reformat-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Tampering | helper write of 6 heterogeneous files (wrong per-file D1 label / span loss) | mitigate | Per-file D1 label resolved programmatically from each file's own frontmatter (never hand-set); whole-pre-H1-span relocation (117-01 fix); dry-run span-byte-length + label report reviewed before commit; C17 #9 exact match gate |
| T-117-02 | Tampering | #12 Transform B on code-embedded WARNING boxes (dot3svc 1868c) | mitigate | Word-preserving Transform B only; de-blockquoting turns embedded fence into a real masked fence with zero code-line change; pre/post word-set diff; #12 re-measurement proves zero over-limit |
| T-117-SC | Tampering | package installs | accept | node-builtins-only; zero installs; no supply-chain surface |
</threat_model>

<verification>
- Enrollment precheck returns zero lines for RE-136..RE-141.
- `node scripts/validation/c17-eee-contract.mjs` exits 0; zero violations on the 6 batch paths.
- Each file's block Platform label matches its frontmatter (02=All Platforms … 07=Linux); #12 measurement reports zero over-limit groups; word-set diff clean.
- RE-index.md RE-136..RE-141 = Approved; RE-134/135 = Pending.
</verification>

<success_criteria>
All 6 enrolled 802.1X guides are EEE-conformant, C17-green, per-file-platform-template-Summary-led with correct per-file
D1 labels, and Approved — including the 1868c dot3svc code-embedded WARNING resolved word-preservingly; the 2
mermaid-deferred 802.1X files remain keyless and Pending. SC1-SC4 satisfied for RE-136..RE-141.
</success_criteria>

<output>
Create `.planning/phases/117-admin-setup-guide-retrofit-all-platforms/117-09-SUMMARY.md` when done.
</output>
