---
phase: 47-integration-re-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/v1.4.1-milestone-audit.mjs
  - scripts/validation/v1.4.1-audit-allowlist.json
autonomous: true
requirements:
  - AEINTEG-02
must_haves:
  truths:
    - "C4 deferred-file guard regex catches Knox/KME/per-OEM/COPE link tokens (24-token alternation list per CONTEXT D-07)"
    - "C6 PITFALL-7 preservation check covers 6 AOSP-scoped target files (06-aosp-stub.md + 09-13 per-OEM admin guides) — placeholder comment removed"
    - "C7 bare-Knox suffix recognition expanded with 5-SKU + standalone forms (KPE / KME / KPE Standard / KPE Premium / on-device attestation / Mobile Enrollment Portal) per Phase 44 D-01"
    - "C9 sidecar cope_banned_phrases[] extended from 3 to 8 patterns (additive-only preserves 'removed' even though not in Phase 46 D-31 set; adds EOL / no longer supported / obsolete / sunset / retired)"
    - "v1.4 harness scripts/validation/v1.4-milestone-audit.mjs UNTOUCHED (Phase 43 D-01/D-02 frozen at commit 3c3a140); only v1.4.1 harness edited"
    - "Sidecar JSON parses cleanly post-edit"
    - "Informational-first contract preserved (CONTEXT D-10): C6/C7/C9 stay informational-first in v1.4.1; no promotion to blocking"
  artifacts:
    - path: "scripts/validation/v1.4.1-milestone-audit.mjs"
      provides: "Harness with extended C4 regex (line 228), expanded C6 targets (lines 293-296), expanded C7 suffix list (line 314)"
      contains: "knox|kme|kpe|realwear|zebra|pico|htc-vive-focus|meta-quest"
    - path: "scripts/validation/v1.4.1-audit-allowlist.json"
      provides: "Sidecar with 8-pattern cope_banned_phrases[] array"
      contains: "no longer supported"
  key_links:
    - from: "scripts/validation/v1.4.1-milestone-audit.mjs C6 targets array"
      to: "docs/admin-setup-android/{06-aosp-stub,09-aosp-realwear,10-aosp-zebra,11-aosp-pico,12-aosp-htc-vive-focus,13-aosp-meta-quest}.md"
      via: "C6 targets array file path string match"
      pattern: "09-aosp-realwear.md"
    - from: "scripts/validation/v1.4.1-audit-allowlist.json cope_banned_phrases[]"
      to: "harness C9 run() function"
      via: "ALLOWLIST.cope_banned_phrases array consumption"
      pattern: "obsolete"
---

<objective>
Ship the four C4 / C6 / C7 / C9 harness extensions enumerated in CONTEXT D-07 as a single atomic commit per AEINTEG-02. Three are harness code edits (C4 regex line 228, C6 targets array lines 293-296, C7 suffix-list literal line 314); one is a sidecar JSON additive extension (C9 cope_banned_phrases[]).

Per CONTEXT D-07 the harness file-versioning Path A is locked: v1.4 harness `scripts/validation/v1.4-milestone-audit.mjs` is FROZEN at commit `3c3a140` (Phase 43 D-01/D-02) — Plan 47-02 does NOT modify it. Only `scripts/validation/v1.4.1-milestone-audit.mjs` is editable.

Per CONTEXT D-10 + D-38 informational-first checks (C6/C7/C9) STAY informational-first in v1.4.1 — Plan 47-02 expands SCOPE (regex / targets / suffix / banned phrases), not severity. Promotion to blocking earns at v1.5+ after a milestone of false-positive-free signal per Phase 42 D-29 grace-period pattern.

Per CONTEXT D-07 + RESEARCH §Pitfall 5: the "no harness code edits for COPE phrases" rule is specifically about the BANNED-PHRASE LIST (C9). C7 suffix list IS in harness code (line 314) and editing it IS allowed within the SC#2-mandated regex/target/sidecar surface.

Purpose: Close AEINTEG-02 — informational signals from C6/C7/C9 are reliable post-extension. C4 catches Knox/per-OEM/COPE tokens in deferred files. C6 expands from 1 to 6 AOSP-scoped target files (placeholder comment removed). C7 calibrates to recognize legitimate Phase 44 D-01 5-SKU forms so bare-Knox count reflects actual ambiguity. C9 tunes from 3 to 8 banned-phrase patterns covering Phase 46 D-31 full set.

Output: Single atomic commit touching scripts/validation/v1.4.1-milestone-audit.mjs + scripts/validation/v1.4.1-audit-allowlist.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-integration-re-audit/47-CONTEXT.md
@.planning/phases/47-integration-re-audit/47-RESEARCH.md
@.planning/phases/47-integration-re-audit/47-PATTERNS.md
@.planning/phases/47-integration-re-audit/47-VALIDATION.md
@scripts/validation/v1.4.1-milestone-audit.mjs
@scripts/validation/v1.4.1-audit-allowlist.json

<interfaces>
<!-- Locked invariants and exact literals — Plan 47-02 MUST paste these verbatim -->

LOCKED INVARIANTS (MUST NOT BE TOUCHED):
- Phase 43 D-01/D-02: scripts/validation/v1.4-milestone-audit.mjs FROZEN at commit 3c3a140 — DO NOT MODIFY.
- Phase 43 D-26: additive-only sidecar JSON contract. Never delete entries; only append.
- Phase 42 D-25: no shared module between v1.4 and v1.4.1 harnesses; file-reads-only contract.
- Phase 42 D-29 + Phase 43 D-06 + CONTEXT D-10 + D-38: C6/C7/C9 stay informational-first in v1.4.1; no promotion to blocking.
- CONTEXT D-09 (REJECTED): C8 anti-paste-block marker drift detection NOT in scope; routed to v1.5 backlog.
- CONTEXT D-11: C9 reporter format stays hits-only count (no file:line surfacing — v1.5 backlog).

EDIT 1 — C4 regex expansion (scripts/validation/v1.4.1-milestone-audit.mjs line 228):

CURRENT (verbatim from RESEARCH live read):
    const re = /\]\([^)]*(android|aosp|byod-work-profile|zero-touch|managed-google-play|play-integrity|managed-home-screen|amapi)[^)]*\)/gi;

AFTER (CONTEXT D-07 locked 24-token list — paste verbatim):
    const re = /\]\([^)]*(android|aosp|byod-work-profile|zero-touch|managed-google-play|play-integrity|managed-home-screen|amapi|knox|kme|kpe|realwear|zebra|pico|htc-vive-focus|meta-quest|cope-full-admin|aosp-realwear|aosp-zebra|aosp-pico|aosp-htc-vive-focus|aosp-meta-quest|aosp-oem-matrix)[^)]*\)/gi;

Token order: original 8 tokens preserved in original order; 16 new tokens appended in exact CONTEXT D-07 sequence: knox / kme / kpe / realwear / zebra / pico / htc-vive-focus / meta-quest / cope-full-admin / aosp-realwear / aosp-zebra / aosp-pico / aosp-htc-vive-focus / aosp-meta-quest / aosp-oem-matrix.

EDIT 2 — C6 targets array expansion (scripts/validation/v1.4.1-milestone-audit.mjs lines 293-296):

CURRENT (verbatim from RESEARCH live read):
    const targets = [
      'docs/admin-setup-android/06-aosp-stub.md',
      // Phase 45 will add per-OEM files here (09-aosp-realwear.md, 10-aosp-zebra.md, etc.)
    ];

AFTER (CONTEXT D-07 + Phase 45 shipped file paths — REMOVE placeholder comment, ADD 5 per-OEM file paths):
    const targets = [
      'docs/admin-setup-android/06-aosp-stub.md',
      'docs/admin-setup-android/09-aosp-realwear.md',
      'docs/admin-setup-android/10-aosp-zebra.md',
      'docs/admin-setup-android/11-aosp-pico.md',
      'docs/admin-setup-android/12-aosp-htc-vive-focus.md',
      'docs/admin-setup-android/13-aosp-meta-quest.md',
    ];

Final array: 6 file paths. Placeholder comment "Phase 45 will add per-OEM files here ..." MUST be removed entirely.

EDIT 3 — C7 suffix-list expansion (scripts/validation/v1.4.1-milestone-audit.mjs line 314):

CURRENT (verbatim from RESEARCH live read):
    const suffixes = /(Mobile Enrollment|Platform for Enterprise|Suite|Manage|Configure)/;

AFTER (CONTEXT D-07 + Phase 44 D-01 5-SKU disambiguation table — paste verbatim):
    const suffixes = /(Mobile Enrollment|Platform for Enterprise|Suite|Manage|Configure|KPE|KME|KPE Standard|KPE Premium|on-device attestation|Mobile Enrollment Portal)/;

Final regex: 11 alternation forms. Original 5 preserved in original order; 6 new appended in exact CONTEXT D-07 sequence: KPE / KME / KPE Standard / KPE Premium / on-device attestation / Mobile Enrollment Portal.

CONTEXT-locked nuance (CONTEXT D-07 inline + RESEARCH §Pitfall 5): "C9 banned-phrase list lives in the sidecar JSON only" applies to C9 ONLY. C7 suffix list IS in harness code (line 314) — that edit IS allowed. These are different targets.

EDIT 4 — C9 sidecar cope_banned_phrases[] extension (scripts/validation/v1.4.1-audit-allowlist.json lines 31-35):

CURRENT (verbatim from RESEARCH live read):
    "cope_banned_phrases": [
      "\\bCOPE\\b[^.]*\\bdeprecated\\b",
      "\\bCOPE\\b[^.]*\\bend of life\\b",
      "\\bCOPE\\b[^.]*\\bremoved\\b"
    ]

AFTER (CONTEXT D-07 + Phase 46 D-31 banned set + additive-only per Phase 43 D-26 — KEEP "removed", add 5 D-31 patterns):
    "cope_banned_phrases": [
      "\\bCOPE\\b[^.]*\\bdeprecated\\b",
      "\\bCOPE\\b[^.]*\\bend of life\\b",
      "\\bCOPE\\b[^.]*\\bremoved\\b",
      "\\bCOPE\\b[^.]*\\bEOL\\b",
      "\\bCOPE\\b[^.]*\\bno longer supported\\b",
      "\\bCOPE\\b[^.]*\\bobsolete\\b",
      "\\bCOPE\\b[^.]*\\bsunset\\b",
      "\\bCOPE\\b[^.]*\\bretired\\b"
    ]

Final array: 8 patterns (3 existing + 5 added). "removed" stays per additive-only contract even though Phase 46 D-31 named set has 7 different items (deprecated / end of life / EOL / no longer supported / obsolete / sunset / retired). Three (deprecated / end of life) overlap with existing sidecar; 5 added (EOL / no longer supported / obsolete / sunset / retired) close the gap to D-31 named set.

CRITICAL NOT-DOING list:
- DO NOT modify scripts/validation/v1.4-milestone-audit.mjs (Phase 43 D-01/D-02 frozen at 3c3a140).
- DO NOT modify scripts/validation/v1.4-audit-allowlist.json (frozen v1.4 baseline).
- DO NOT modify the C9 run() function body in v1.4.1-milestone-audit.mjs (CONTEXT D-07 + D-11). The harness reads from ALLOWLIST.cope_banned_phrases with a 3-pattern fallback; the sidecar extension simply adds 5 more patterns.
- DO NOT add a C8 check (CONTEXT D-09 + Deferred Ideas — routed to v1.5).
- DO NOT change C9 reporter format (CONTEXT D-11 — file:line surfacing routed to v1.5).
- DO NOT change schema_version, generated, phase, safetynet_exemptions[], or supervision_exemptions[] in the sidecar — only cope_banned_phrases[] is edited by Plan 47-02.
- DO NOT promote any informational check to blocking (CONTEXT D-10 + D-38).
- DO NOT touch Plan 47-01 territory (docs/_glossary-android.md, capability matrix, 00-overview.md).
- DO NOT touch Plan 47-03 territory (.planning/PROJECT.md).
- DO NOT touch Plan 47-04 territory (.planning/milestones/v1.4-MILESTONE-AUDIT.md).
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply 3 harness code literal edits (C4 regex line 228, C6 targets array lines 293-296, C7 suffix list line 314) to v1.4.1-milestone-audit.mjs</name>
  <files>scripts/validation/v1.4.1-milestone-audit.mjs</files>
  <read_first>
    - .planning/phases/47-integration-re-audit/47-CONTEXT.md (D-07 token lists, D-09 C8 rejection, D-10 informational-first lock, D-33 v1.4 frozen rule)
    - .planning/phases/47-integration-re-audit/47-RESEARCH.md (Code Examples §C4 §C6 §C7 — exact line numbers + literal regex/array)
    - .planning/phases/47-integration-re-audit/47-PATTERNS.md (Edits 1-3 with surrounding context — copy-paste blocks)
    - scripts/validation/v1.4.1-milestone-audit.mjs (full file — confirm line 228 contains C4 regex; lines 293-296 contain C6 targets; line 314 contains C7 suffixes regex; confirm no drift since RESEARCH live read 2026-04-25)
    - scripts/validation/v1.4-milestone-audit.mjs (read header only — verify FROZEN status; this file is REFERENCE ONLY and MUST NOT be modified)
  </read_first>
  <action>
    EDIT 1 — Line 228 C4 regex expansion. Replace the current single-line regex with the 24-token regex (paste verbatim from `<interfaces>` EDIT 1 AFTER block). Token order: original 8 + 16 new appended in exact CONTEXT D-07 sequence (knox / kme / kpe / realwear / zebra / pico / htc-vive-focus / meta-quest / cope-full-admin / aosp-realwear / aosp-zebra / aosp-pico / aosp-htc-vive-focus / aosp-meta-quest / aosp-oem-matrix).

    EDIT 2 — Lines 293-296 C6 targets array expansion. Replace the entire 3-line array literal (including the placeholder comment line) with the 8-line block from `<interfaces>` EDIT 2 AFTER. Six file paths total. Trailing comma after the last entry preserved per JS array literal style. Placeholder comment "Phase 45 will add per-OEM files here ..." MUST be removed entirely.

    EDIT 3 — Line 314 C7 suffix-list expansion. Replace the current 5-form regex alternation with the 11-form regex from `<interfaces>` EDIT 3 AFTER. Original 5 forms preserved in original order; 6 new forms appended in exact CONTEXT D-07 + Phase 44 D-01 sequence (KPE / KME / KPE Standard / KPE Premium / on-device attestation / Mobile Enrollment Portal).

    Cross-check after edits:
    1. Line 228 starts with `const re = /\]\([^)]*` and ends with `)[^)]*\)/gi;` — contains the 16 new tokens
    2. Lines 293-300 (was 5-line block, becomes 8-line block) — placeholder comment GONE; 6 file paths present
    3. Line 314 contains the 11-form regex alternation including KPE / KME / KPE Standard / KPE Premium / on-device attestation / Mobile Enrollment Portal

    DO NOT modify any other line in the harness. The C1, C2, C3, C5, C6 run() bodies, C7 run() body (only the regex literal at line 314 changes), C9 run() body, helper functions, file-read utilities, reporter format, and shebang line ALL stay byte-identical.

    DO NOT modify scripts/validation/v1.4-milestone-audit.mjs (Phase 43 D-01/D-02 frozen).

    DO NOT promote any informational check to blocking — Plan 47-02 expands SCOPE not severity.
  </action>
  <verify>
    <automated>node scripts/validation/v1.4.1-milestone-audit.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - scripts/validation/v1.4.1-milestone-audit.mjs line 228 C4 regex contains all 24 tokens including knox / kme / kpe / realwear / zebra / pico / htc-vive-focus / meta-quest / cope-full-admin / aosp-realwear / aosp-zebra / aosp-pico / aosp-htc-vive-focus / aosp-meta-quest / aosp-oem-matrix (verifiable via grep)
    - C6 targets array contains exactly 6 file paths (06-aosp-stub.md + 09-13 per-OEM); placeholder comment "Phase 45 will add per-OEM files here" REMOVED
    - C7 suffixes regex contains 11 forms including KPE, KME, KPE Standard, KPE Premium, on-device attestation, Mobile Enrollment Portal
    - scripts/validation/v1.4-milestone-audit.mjs UNCHANGED (Phase 43 D-01/D-02 frozen at commit 3c3a140) — verifiable via git diff scope
    - Harness exits 0 (5 blocking C1-C5 PASS + 3 informational C6/C7/C9 PASS-by-design with new metric counts)
    - C6 informational reporter shows 6/6 (or N/6) AOSP-scoped files preserve PITFALL-7 framing
    - No C9 run() function body code change (only sidecar JSON edited in Task 2)
    - No new check (no C8) added
  </acceptance_criteria>
  <done>
    Three harness code literal edits applied verbatim. Harness runs end-to-end exit 0 with new metric counts visible in informational checks. v1.4 frozen harness untouched. No severity promotions. No new checks added.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend v1.4.1-audit-allowlist.json cope_banned_phrases[] from 3 to 8 patterns (additive-only)</name>
  <files>scripts/validation/v1.4.1-audit-allowlist.json</files>
  <read_first>
    - .planning/phases/47-integration-re-audit/47-CONTEXT.md (D-07 cope_banned_phrases extension, D-31 sidecar additive-only, D-37 invariants from Phase 46 D-31)
    - .planning/phases/47-integration-re-audit/47-RESEARCH.md (Code Examples §C9 — banned phrase list before/after; Pitfall 5)
    - .planning/phases/47-integration-re-audit/47-PATTERNS.md (sidecar JSON Edit 1)
    - scripts/validation/v1.4.1-audit-allowlist.json (full file — confirm cope_banned_phrases[] currently has exactly 3 entries starting at line 31; preserve schema_version, generated, phase, safetynet_exemptions[], supervision_exemptions[] untouched)
  </read_first>
  <action>
    Locate the `cope_banned_phrases` key in scripts/validation/v1.4.1-audit-allowlist.json (currently lines 31-35 per RESEARCH live read).

    Replace the current 3-entry array with the 8-entry array from `<interfaces>` EDIT 4 AFTER block. Order: 3 existing patterns preserved in original order (deprecated / end of life / removed); 5 added patterns appended in exact CONTEXT D-07 + Phase 46 D-31 sequence: EOL / no longer supported / obsolete / sunset / retired. "removed" KEPT per additive-only contract (Phase 43 D-26) even though not in D-31 named set.

    Each pattern uses the JSON-escaped form `\\bCOPE\\b[^.]*\\b<phrase>\\b`. Each `\b` regex word boundary is encoded as `\\b` in JSON. The patterns scope each banned phrase within a single sentence containing COPE (using `[^.]*` to mean "no period between the COPE token and the banned phrase").

    DO NOT touch:
    - Top-level keys: `schema_version` ("1.0"), `generated` (existing ISO date), `phase` ("43-v1-4-cleanup-audit-harness-fix")
    - `safetynet_exemptions[]` (4 entries — leave untouched)
    - `supervision_exemptions[]` (18 entries — leave untouched; Plan 47-01 owns line-shift updates if any)
    - Overall JSON structure / indentation pattern (match existing 2-space indentation)

    Verify JSON parses immediately after edit:
    `node -e "const j = JSON.parse(require('fs').readFileSync('scripts/validation/v1.4.1-audit-allowlist.json', 'utf8')); console.log('cope_banned_phrases.length =', j.cope_banned_phrases.length, '/ safetynet_exemptions.length =', j.safetynet_exemptions.length, '/ supervision_exemptions.length =', j.supervision_exemptions.length);"`

    Expected output: `cope_banned_phrases.length = 8 / safetynet_exemptions.length = 4 / supervision_exemptions.length = 18`

    Then run harness post-edit: `node scripts/validation/v1.4.1-milestone-audit.mjs --verbose` — must exit 0; C9 informational reporter shows updated count `(informational - N COPE banned-phrase occurrence(s))` reflecting the 8-pattern set scanned over Android docs. Per CONTEXT D-26 zero "deferred to v1.4.1" remains; Phase 46 D-31 banned-phrase discipline applies — expected N is small (likely 0 or close to 0).

    Per CONTEXT D-11: do NOT change the C9 reporter format (hits-only count, no file:line surfacing — that's v1.5 backlog).
  </action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.4.1-audit-allowlist.json','utf8')); if(j.cope_banned_phrases.length!==8) process.exit(1); if(j.safetynet_exemptions.length!==4) process.exit(2); if(j.supervision_exemptions.length!==18) process.exit(3);" &amp;&amp; node scripts/validation/v1.4.1-milestone-audit.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - scripts/validation/v1.4.1-audit-allowlist.json parses cleanly as JSON (Phase 43 Plan 08 CI parse-gate enforces)
    - cope_banned_phrases[] has exactly 8 entries
    - cope_banned_phrases[] contains all 8 patterns: deprecated, end of life, removed, EOL, no longer supported, obsolete, sunset, retired
    - safetynet_exemptions[] still has exactly 4 entries (untouched)
    - supervision_exemptions[] still has exactly 18 entries (untouched — Plan 47-01 territory)
    - schema_version / generated / phase top-level keys untouched
    - Harness exits 0 (5 blocking C1-C5 PASS + 3 informational C6/C7/C9 PASS-by-design)
    - C9 informational reporter format unchanged (hits-only count, no file:line surfacing)
    - scripts/validation/v1.4-audit-allowlist.json UNCHANGED (frozen v1.4 baseline)
  </acceptance_criteria>
  <done>
    Sidecar cope_banned_phrases[] extended to 8 patterns additively. JSON parses. Harness exits 0 with 3-info-PASS. No other sidecar key touched. v1.4 frozen baseline untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem | Plan 47-02 reads/writes a Node.js audit harness file + a sidecar JSON in repo; no network surface |
| Regex engine input | Harness reads markdown files via fs.readFileSync; matches against locally-defined regex literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02-01 | Denial of Service | C4 / C7 regex catastrophic backtracking | accept | [VERIFIED low risk per RESEARCH §Security Domain] Both regexes use simple alternation on bounded token sets; no unbounded `.*` cross-line; no nested quantifiers. Token list grows from 8 to 24 (C4) and 5 to 11 (C7) — both below any practical ReDoS threshold. |
| T-47-02-02 | Tampering | scripts/validation/v1.4.1-audit-allowlist.json | accept | [VERIFIED] Sidecar is committed to git; CI parse job (Phase 43 Plan 08 audit-harness-integrity.yml) enforces JSON validity on every commit; Plan 47-02 changes flow through this gate. No runtime user input. |
| T-47-02-03 | Tampering | scripts/validation/v1.4-milestone-audit.mjs (frozen predecessor) | mitigate | Plan 47-02 explicitly DOES NOT touch this file (Phase 43 D-01/D-02 frozen at commit 3c3a140). Acceptance criteria gate on git diff scope confirms it. |

ASVS L1 assessment per RESEARCH §Security Domain: V2/V3/V4/V5/V6 do NOT apply — Plan 47-02 modifies a Node.js validation harness and a static sidecar JSON only. No authentication, session, access-control, runtime input-validation, or cryptography surface introduced or modified.
</threat_model>

<verification>
After Task 2, run from repo root:

1. `node scripts/validation/v1.4.1-milestone-audit.mjs --verbose` → exit 0 mandatory; 5 blocking + 3 informational PASS
2. `node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.4.1-audit-allowlist.json','utf8')); console.log(JSON.stringify({cope: j.cope_banned_phrases.length, sn: j.safetynet_exemptions.length, sup: j.supervision_exemptions.length}))"` → `{"cope":8,"sn":4,"sup":18}`
3. `git diff --stat scripts/validation/v1.4-milestone-audit.mjs` → empty (frozen file untouched)
4. `git diff --stat scripts/validation/v1.4-audit-allowlist.json` → empty (frozen sidecar untouched)
5. `grep -c "knox\|kme\|kpe\|realwear\|zebra\|pico\|htc-vive-focus\|meta-quest\|cope-full-admin\|aosp-oem-matrix" scripts/validation/v1.4.1-milestone-audit.mjs` → at least 1 (line 228 carries the new tokens)
6. `grep -c "09-aosp-realwear.md\|10-aosp-zebra.md\|11-aosp-pico.md\|12-aosp-htc-vive-focus.md\|13-aosp-meta-quest.md" scripts/validation/v1.4.1-milestone-audit.mjs` → 5 (one per per-OEM file path in C6 targets)
7. `grep "Mobile Enrollment Portal" scripts/validation/v1.4.1-milestone-audit.mjs` → 1 match (C7 suffix list)
</verification>

<success_criteria>
- C4 regex (line 228) contains all 24 CONTEXT D-07 tokens
- C6 targets array (lines 293-300 post-edit) contains 6 AOSP-scoped file paths; placeholder comment removed
- C7 suffix list (line 314) contains 11 alternation forms including 5-SKU + standalone forms
- C9 sidecar cope_banned_phrases[] contains 8 patterns (additive-only from 3)
- v1.4 frozen harness + sidecar untouched (Phase 43 D-01/D-02)
- Harness exits 0 (5 blocking C1-C5 PASS + 3 informational C6/C7/C9 PASS-by-design)
- No C9 run() function body change (sidecar-driven)
- No new C8 check (CONTEXT D-09 / Deferred Ideas)
- C6/C7/C9 still informational-first (CONTEXT D-10 + D-38)
- Single atomic commit touching only scripts/validation/v1.4.1-milestone-audit.mjs + scripts/validation/v1.4.1-audit-allowlist.json
- AEINTEG-02 closure recorded in 47-02-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/47-integration-re-audit/47-02-SUMMARY.md` with:
- Files actually touched (2 files: harness + sidecar)
- Concrete diff summary (24 tokens in C4, 6 paths in C6, 11 forms in C7, 8 patterns in C9)
- Harness verbose output post-edit (5 blocking PASS + 3 informational PASS-by-design + counts)
- AEINTEG-02 closure evidence
- Atomic commit SHA
- Verification that v1.4 frozen files are untouched
</output>
