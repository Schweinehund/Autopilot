---
phase: 43
plan: 10
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09]
files_modified: []
autonomous: true
requirements: [AEAUDIT-05]
must_haves:
  truths:
    - "From clean checkout (git reset to HEAD), both harnesses run without silent-degradation"
    - "v1.4 harness exits 0 with all 5 checks PASSING (C1 + C2 post-expansion + C3 informational + C4 + C5 post-normalization)"
    - "v1.4.1 harness exits 0 with 8 checks — C1-C5 mechanical PASS + C6/C7/C9 informational PASS"
    - "regenerate-supervision-pins.mjs --self-test exits 0 (helper classifier reproduces Phase 43 hand-authored set)"
    - "regenerate-supervision-pins.mjs --report emits 0 un-pinned Tier-1 and 0 un-pinned Tier-2 on current tree"
    - "Phase 43 VERIFICATION.md captures the final harness + helper output as a completeness record"
  artifacts:
    - path: ".planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md"
      provides: "Terminal sanity record — both harnesses PASS, helper self-test PASS, all Phase 43 success criteria met"
      contains: "Terminal Verification"
  key_links:
    - from: ".planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md"
      to: "All Phase 43 plans (01-09) + their commit SHAs"
      via: "tabular record of each plan's commit + harness/helper output"
      pattern: "43-0[1-9]"
---

<objective>
Per D-27 step 10: terminal sanity. From a clean checkout of master (post Plans 01-09), run BOTH harnesses (v1.4 + v1.4.1) + helper --self-test. Expect:
- v1.4 harness: all 5 mechanical checks PASS (C1 post-expansion, C2 post-expansion, C3, C4, C5 unchanged from v1.4 scope).
- v1.4.1 harness: all 8 checks PASS — C1-C5 mechanical + C6/C7/C9 informational.
- Helper --self-test: PASS (14 Tier-1 + 0 Tier-2).

Capture the output into `.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md` as the Phase 43 terminal record, including a table of all 9 prior plans' commit SHAs.

If ANY check fails: loop back to the prior plan that owns it (e.g., C2 FAIL → Plan 03 line-drift; C5 FAIL → Plan 05 missing a runbook; C6 PASS but 0/1 framing → Plan 07 dropped PITFALL-7).

This is Wave 7 — depends on all prior plans. Final gate before Phase 43 is considered complete.

Purpose: Confirms Phase 43 delivers on all 9 must-haves from the plan_shape_guidance. Provides the commit-ready state for `/gsd-verify-work`.

Output: 1 new file (`43-VERIFICATION.md`). One commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VALIDATION.md

<interfaces>
<!-- No code changes; this plan VERIFIES + records the terminal state -->

Expected terminal-sanity assertions (per plan_shape_guidance must_haves):
1. node scripts/validation/v1.4-milestone-audit.mjs exits 0 or 1 cleanly (no silent-degradation)
2. scripts/validation/v1.4.1-milestone-audit.mjs exists and runs; C1-C5 baseline PASSES on current tree
3. Both sidecars parse + contain >= expected supervision pin count (23)
4. regenerate-supervision-pins.mjs exists + --self-test exits 0 + never auto-pins Tier-2
5. 4 L2 runbooks show review_by: 2026-06-22; Android template has sentinel
6. 06-aosp-stub.md body <= 900 words; PITFALL-7 framing preserved
7. PHASE-45-AOSP-SOURCE.md exists with >= 200 words of RealWear deep content
8. .github/workflows/audit-harness-integrity.yml exists + CI YAML asserts parse + non-empty
9. Phase 39 /gsd-validate-phase 39 VERIFICATION artifact exists OR DEFER-04 closure recorded

VERIFICATION.md structure (to be composed in Task 43-10-02):
  - YAML frontmatter with status: passed + verified: 2026-04-24
  - Summary paragraph
  - Wave Execution Record table (10 plans + commit SHAs)
  - Harness + Helper Terminal Output sections (4 embedded code blocks)
  - Success Criteria Table (9 must-haves with evidence)
  - Requirement Closure table (AEAUDIT-02..05 with plan attribution)
  - Sign-off paragraph
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 43-10-01: Run both harnesses + helper --self-test; capture output</name>
  <files>(no edits — capture step)</files>
  <read_first>
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VALIDATION.md tasks 43-10-01 / 43-10-02
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md §9 "Full suite command"
  </read_first>
  <action>
    Run full suite from repo root. Capture output into temp files for Task 43-10-02 assembly.

    ```
    # v1.4 harness — expected all-PASS terminal state (C1 + C2 + C3 + C4 + C5 all PASS)
    node scripts/validation/v1.4-milestone-audit.mjs --verbose > /tmp/v14-terminal.txt 2>&1
    V14_RC=$?
    echo "v1.4 exit: $V14_RC"

    # v1.4.1 harness — expected all-PASS (8 checks)
    node scripts/validation/v1.4.1-milestone-audit.mjs --verbose > /tmp/v141-terminal.txt 2>&1
    V141_RC=$?
    echo "v1.4.1 exit: $V141_RC"

    # Helper self-test
    node scripts/validation/regenerate-supervision-pins.mjs --self-test > /tmp/helper-selftest.txt 2>&1
    HELPER_RC=$?
    echo "helper self-test exit: $HELPER_RC"

    # Helper report (additional evidence — expected 0 un-pinned, 0 suspected-regressions)
    node scripts/validation/regenerate-supervision-pins.mjs --report > /tmp/helper-report.txt 2>&1
    echo "helper report exit: $?"

    # Gate check — all three MUST succeed
    test $V14_RC -eq 0 && test $V141_RC -eq 0 && test $HELPER_RC -eq 0 && echo "TERMINAL SANITY: PASS" || echo "TERMINAL SANITY: FAIL"
    ```

    Expected exit codes:
    - v1.4 harness: 0 (all 5 checks PASS after Plan 03 C2 expansion)
    - v1.4.1 harness: 0 (all 8 checks PASS — informational checks always pass per D-06)
    - Helper --self-test: 0 (classifier matches hand-authored set per Plan 04)

    If ANY exit code != 0, LOOP BACK to prior plan:
    - v1.4 C2 FAIL → Plan 03 (possible line drift; re-run Task 43-03-01 spot-check)
    - v1.4 C5 FAIL → check v1.4 scope (v1.4 harness doesn't have scope-filter; template in v1.4 scope shouldn't happen but verify)
    - v1.4.1 C2 FAIL → same as above
    - v1.4.1 C5 FAIL → Plan 05 missing a runbook
    - v1.4.1 C6 FAIL (unexpected — informational always PASS) → harness edit Plan 02 broken; check `pass: true` literal
    - v1.4.1 C6 PASS with 0/1 framing → Plan 07 dropped PITFALL-7; restore line 75 sentence
    - Helper --self-test FAIL → classifier drift; re-run Plan 04 Task 02 diagnostic branch

    If all three exit 0, proceed to Task 43-10-02. No file edits in this task.

    Additional sanity: verify pre-commit hook works on current tree:
    ```
    bash scripts/hooks/pre-commit.sh && echo "pre-commit: OK"
    ```

    Gather wave-execution record (commit SHAs per Plan):
    ```
    for plan in 01 02 03 04 05 07 08 09; do
      echo "Plan $plan:"
      git log --all --oneline --grep="($plan)" -n 3 -- scripts/validation/ docs/ .planning/ .github/ 2>&1 | head -3
    done
    # Plan 06 has no commit (integration-test only); note in VERIFICATION.md
    # Plan 10 commit doesn't exist yet (this plan)
    ```
  </action>
  <verify>
    <automated>node scripts/validation/v1.4-milestone-audit.mjs --verbose > /tmp/v14-terminal.txt 2>&1 && node scripts/validation/v1.4.1-milestone-audit.mjs --verbose > /tmp/v141-terminal.txt 2>&1 && node scripts/validation/regenerate-supervision-pins.mjs --self-test > /tmp/helper-selftest.txt 2>&1 && bash scripts/hooks/pre-commit.sh</automated>
  </verify>
  <acceptance_criteria>
    - v1.4 harness exit code 0 (all 5 PASS)
    - v1.4.1 harness exit code 0 (all 8 PASS)
    - Helper --self-test exit code 0 (PASS)
    - pre-commit hook exits 0 (both sidecars valid JSON)
    - /tmp/v14-terminal.txt contains 5 PASS lines and 0 FAIL lines
    - /tmp/v141-terminal.txt contains 8 PASS lines and 0 FAIL lines
    - /tmp/helper-selftest.txt contains "Self-test: PASS"
    - /tmp/helper-report.txt shows "Un-pinned Tier-1 (stub-eligible): 0" AND "Un-pinned Tier-2 (suspected regression): 0"
  </acceptance_criteria>
  <done>
    All three terminal-sanity assertions PASS. Output captured in /tmp/*.txt files ready for VERIFICATION.md assembly.
  </done>
</task>

<task type="auto">
  <name>Task 43-10-02: Write 43-VERIFICATION.md + commit + Phase 43 sign-off</name>
  <files>.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md</files>
  <read_first>
    - Task 43-10-01 outputs: /tmp/v14-terminal.txt, /tmp/v141-terminal.txt, /tmp/helper-selftest.txt, /tmp/helper-report.txt
    - All 10 Phase 43 PLAN.md files (reference for the Wave Execution Record table)
  </read_first>
  <action>
    Compose `.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md` with this structure (verbatim template — fill in bracketed placeholders with actual values from Task 43-10-01 output):

    File header (YAML frontmatter):
    ```
    ---
    phase: 43
    slug: v1-4-cleanup-audit-harness-fix
    status: passed
    verified: 2026-04-24
    ---
    ```

    Body sections to include (in order):

    1. H1 "Phase 43 Terminal Verification"
    2. H2 "Summary" — paragraph noting that all 4 requirements (AEAUDIT-02..05) are closed, DEFER-04 closed, both harnesses PASS, helper self-test PASS, CI bootstrapped. Note that this unblocks Phases 44/45/46/47.
    3. H2 "Wave Execution Record" — table with columns [Plan | Wave | Commit | Files Touched | Status]. One row per plan 01-10:
       - Plan 01 | Wave 1 | {SHA from `chore(43): rescue`} | v1.4 sidecar + harness line 57 + FROZEN | PASS
       - Plan 02 | Wave 2 | {SHA from `feat(43): scaffold`} | v1.4.1 harness + sidecar skeleton | PASS
       - Plan 03 | Wave 2 | {SHA from `feat(43): expand`} | Both sidecars 9 → 23 pins | PASS
       - Plan 04 | Wave 2 | {SHA from `feat(43): add regenerate`} | Helper + README | PASS
       - Plan 05 | Wave 3 | {SHA from `docs(43): normalize`} | 4 L2 runbooks + template sentinel | PASS
       - Plan 06 | Wave 3 | — | Integration test only (no commit) | PASS
       - Plan 07 | Wave 4 | {SHA from `docs(43): trim AOSP`} | Stub trim + PHASE-45-AOSP-SOURCE.md | PASS
       - Plan 08 | Wave 5 | {SHA from `ci(43)`} | GitHub Action + pre-commit + hooks README | PASS
       - Plan 09 | Wave 6 | {SHA from `docs(43): restore Phase 39`} | Phase 39 restored + DEFER-04 closure | PASS
       - Plan 10 | Wave 7 | (this commit) | 43-VERIFICATION.md | PASS
    4. H2 "Harness + Helper Terminal Output" — four H3 subsections with embedded /tmp/*.txt content in triple-backtick fenced code blocks:
       - H3 "v1.4 harness" — embed /tmp/v14-terminal.txt content
       - H3 "v1.4.1 harness" — embed /tmp/v141-terminal.txt content
       - H3 "Helper self-test" — embed /tmp/helper-selftest.txt content
       - H3 "Helper report" — embed /tmp/helper-report.txt content
    5. H2 "Success Criteria Table (9 must-haves)" — 9-row table with columns [# | Truth | Evidence | Status]:
       - 1 | v1.4 harness exits cleanly (no silent-degradation) | Exit 0; C1-C5 all PASS | PASS
       - 2 | v1.4.1-milestone-audit.mjs exists + C1-C5 PASS on current tree | Exit 0; 8 checks PASS | PASS
       - 3 | Both sidecars parse + contain >= 23 supervision pins | v1.4: 4+23; v1.4.1: 4+23+3 COPE | PASS
       - 4 | regenerate-supervision-pins.mjs --self-test exits 0 + never auto-pins Tier-2 | Self-test exit 0; helper read-only on sidecars | PASS
       - 5 | 4 L2 runbooks show review_by: 2026-06-22; Android template has sentinel | grep confirms all 5 files | PASS
       - 6 | 06-aosp-stub.md body <= 900 words; PITFALL-7 framing preserved | Word count: {N from harness C3 detail}; grep "not supported under AOSP" matches | PASS
       - 7 | PHASE-45-AOSP-SOURCE.md exists with >= 200 words of RealWear deep content | wc -w: {N words}; grep RealWear matches | PASS
       - 8 | .github/workflows/audit-harness-integrity.yml exists + CI YAML asserts parse + non-empty | grep supervision_exemptions + JSON.parse + 4 jobs | PASS
       - 9 | Phase 39 VERIFICATION artifact exists OR DEFER-04 closure recorded | 39-VALIDATION.md has 2026-04-24 trailer; v1.4-MILESTONE-AUDIT.md has resolution block | PASS
    6. H2 "Requirement Closure" — table with columns [REQ-ID | Requirement | Closure Plan(s) | Status]:
       - AEAUDIT-02 | Expand audit allow-list (~10 → ~37 pins) | 01 (rescue) + 03 (hand-author) + 04 (helper dogfood) | Closed
       - AEAUDIT-03 | Normalize 60-day freshness + template | 05 (metadata-only + sentinel) + 06 (integration gate) | Closed
       - AEAUDIT-04 | AOSP stub trim + migration + DEFER-04 | 07 (trim + prep shell) + 09 (re-gate) | Closed
       - AEAUDIT-05 | Harness versioning + sidecar path + helper + CI | 01 (path rescue) + 02 (v1.4.1 scaffold) + 04 (helper) + 08 (CI) + 10 (sanity) | Closed
    7. H2 "Sign-off" — paragraph: Phase 43 complete as of 2026-04-24. All 4 requirements closed. DEFER-04 closed. Both harnesses PASS. CI bootstrapped. Downstream Phases 44/45/46 can proceed on a stable baseline. Final commit SHA: {this commit SHA}.

    Fill in the {SHA} placeholders by running `git log --all --oneline --grep="(<pattern>)"` for each plan's commit pattern during assembly.

    Then commit:
    ```
    git add .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md
    git commit -m "docs(43): terminal verification — both harnesses + helper PASS; Phase 43 complete"
    ```

    Commit message body should cite:
    - Terminal sanity (D-27 step 10) complete
    - Both harnesses exit 0 with all checks PASSING
    - Helper --self-test PASS
    - All 4 requirements (AEAUDIT-02..05) closed
    - DEFER-04 closed (via Plan 09)
    - Unblocks Phases 44/45/46/47

    After commit, edit the VERIFICATION.md Sign-off section to insert this commit's SHA (amend or follow-up commit — author's choice; if follow-up: `docs(43): backfill final SHA in VERIFICATION.md`).
  </action>
  <verify>
    <automated>test -f .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md && grep -q "Phase 43 Terminal Verification" .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md && grep -q "status: passed" .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md && grep -qE "AEAUDIT-0[2-5].*Closed" .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md` returns 0
    - VERIFICATION.md has YAML frontmatter with `status: passed` and `verified: 2026-04-24`
    - VERIFICATION.md contains "Phase 43 Terminal Verification" H1
    - VERIFICATION.md contains Wave Execution Record table with 10 plan rows
    - VERIFICATION.md contains 4 embedded terminal-output code blocks (v1.4 harness + v1.4.1 harness + helper self-test + helper report)
    - VERIFICATION.md contains Success Criteria Table with exactly 9 rows (9 must-haves)
    - VERIFICATION.md contains Requirement Closure table with all 4 AEAUDIT-0{2,3,4,5} entries marked Closed
    - `git log -1 --pretty=%s` shows "docs(43): terminal verification" subject
    - `git show --stat HEAD 2>&1 | grep -q "43-VERIFICATION.md"` returns 0
  </acceptance_criteria>
  <done>
    43-VERIFICATION.md authored with complete terminal record + commit SHAs for all 9 prior plans + evidence for all 9 must-haves + closure for all 4 requirements. Final commit landed. Phase 43 is complete.
  </done>
</task>

</tasks>

<verification>
```
# 1. VERIFICATION.md exists + structured correctly
test -f .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md
grep -q "status: passed" .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md
grep -q "Phase 43 Terminal Verification" .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md

# 2. All 4 REQ-IDs marked Closed
for req in AEAUDIT-02 AEAUDIT-03 AEAUDIT-04 AEAUDIT-05; do
  grep -q "$req.*Closed" .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VERIFICATION.md || exit 1
done

# 3. Both harnesses + helper all exit 0 on current tree (re-run as final sanity)
node scripts/validation/v1.4-milestone-audit.mjs --verbose > /dev/null
node scripts/validation/v1.4.1-milestone-audit.mjs --verbose > /dev/null
node scripts/validation/regenerate-supervision-pins.mjs --self-test > /dev/null

# 4. pre-commit hook works
bash scripts/hooks/pre-commit.sh > /dev/null

# 5. Terminal commit landed
git log -1 --pretty=%s | grep -q "docs(43): terminal verification"
```
</verification>

<success_criteria>
- v1.4 harness exits 0 with all 5 checks PASS
- v1.4.1 harness exits 0 with all 8 checks PASS
- Helper --self-test exits 0
- Helper --report shows 0 un-pinned Tier-1 + 0 un-pinned Tier-2
- Pre-commit hook works locally
- 43-VERIFICATION.md documents the terminal state with commit SHAs + evidence for 9 must-haves
- All 4 requirements (AEAUDIT-02..05) marked Closed
- DEFER-04 closed (via Plan 09 re-gate)
- Phase 43 ready for `/gsd-verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-10-SUMMARY.md` documenting:
- Terminal sanity outcome (all green)
- Any loop-back remediation that occurred during Task 43-10-01 (expected: none)
- Final commit SHA
- Ready for `/gsd-verify-work` then `/gsd-plan-phase 44 45 46` (parallel)
</output>
