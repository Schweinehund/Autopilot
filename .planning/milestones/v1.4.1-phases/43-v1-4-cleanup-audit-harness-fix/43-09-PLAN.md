---
phase: 43
plan: 09
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08]
files_modified:
  - .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md
  - .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VERIFICATION.md
  - .planning/phases/39-zero-touch-enrollment-aosp-stub/39-01-SUMMARY.md
  - .planning/phases/39-zero-touch-enrollment-aosp-stub/39-02-SUMMARY.md
  - .planning/milestones/v1.4-MILESTONE-AUDIT.md
autonomous: false
requirements: [AEAUDIT-04]
must_haves:
  truths:
    - ".planning/phases/39-zero-touch-enrollment-aosp-stub/ directory restored from git commit ef7717b"
    - "/gsd-validate-phase 39 workflow invoked successfully (State A: existing VALIDATION.md detected)"
    - "39-VALIDATION.md has a fresh 'Validation Audit 2026-04-24' trailer recording AOSP stub body word count within 600-900 envelope"
    - "DEFER-04 closure recorded in 39-VALIDATION.md trailer OR re_audit_resolution block appended to v1.4-MILESTONE-AUDIT.md"
    - "Phase 39 status remains `passed` (trimmed stub validates against Phase 39 envelope)"
  artifacts:
    - path: ".planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md"
      provides: "Restored Phase 39 validation record + 2026-04-24 re-gate audit trailer"
      contains: "Validation Audit 2026-04-24"
    - path: ".planning/phases/39-zero-touch-enrollment-aosp-stub/39-VERIFICATION.md"
      provides: "Restored Phase 39 verification record (historical)"
      contains: "status: passed"
    - path: ".planning/milestones/v1.4-MILESTONE-AUDIT.md"
      provides: "Updated milestone audit with DEFER-04 closure record"
      contains: "DEFER-04"
  key_links:
    - from: ".planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md trailer"
      to: "docs/admin-setup-android/06-aosp-stub.md (trimmed to ~700 words per Plan 07)"
      via: "word-count assertion: body ≤ 900 words (Phase 39 envelope)"
      pattern: "Validation Audit 2026-04-24"
---

<objective>
Close DEFER-04 per D-21: restore the Phase 39 directory from git commit `ef7717b` (it was torn down during v1.4.1 milestone prep), invoke `/gsd-validate-phase 39` workflow to re-gate against the now-trimmed AOSP stub (Plan 07), and record closure either in the updated VALIDATION.md audit trailer or via a `re_audit_resolution:` block appended to `.planning/milestones/v1.4-MILESTONE-AUDIT.md`.

This is Wave 6 — depends on Plan 07 (AOSP stub must be trimmed before re-gate). Per plan_shape_guidance: `autonomous: false` because `/gsd-validate-phase 39` may itself need human judgment AND Phase 39 directory restoration is non-trivial.

Per PATTERN-K (git-history restore) + D-21 workflow: restore Phase 39 artifacts FIRST (read-only rehydration of historical records), then invoke the validator. Pattern-mapper flagged this dependency: "Phase 39 directory is currently deleted — Plan 09 MUST restore it from commit ef7717b before /gsd-validate-phase 39 can succeed."

Purpose: Finalizes AEAUDIT-04 by closing the DEFER-04 deferral (AOSP stub envelope violation was the sole open item on that deferral). Prevents a zombie deferral bleeding into v1.5.

Output: ~5 files restored from git + potentially 1 file appended to (v1.4-MILESTONE-AUDIT.md). One or more commits per validator workflow outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md
@.planning/milestones/v1.4-MILESTONE-AUDIT.md

<interfaces>
<!-- Pattern K: git-history restore via git show (PATTERNS §File 17 + §Pattern K Multi-file pattern) -->

Git restore source:
  Commit SHA: ef7717b (Phase 39 final state — 39-VERIFICATION.md header shows status: passed, score: 23/23 must-haves verified)
  Path prefix: .planning/phases/39-zero-touch-enrollment-aosp-stub/

Multi-file restore command (PATTERNS §Pattern K):
  git ls-tree -r --name-only ef7717b .planning/phases/39-zero-touch-enrollment-aosp-stub/ | while IFS= read -r path; do
    mkdir -p "$(dirname "$path")"
    git show "ef7717b:$path" > "$path"
  done

Expected restored files (per RESEARCH §7 + PATTERNS §File 17):
  - 39-VALIDATION.md
  - 39-VERIFICATION.md
  - 39-01-SUMMARY.md
  - 39-02-SUMMARY.md
  - (any PLAN/CONTEXT/other artifacts present in ef7717b tree for phase 39)

Validator workflow invocation:
  /gsd-validate-phase 39

Expected outcomes:
  - State A detected (existing VALIDATION.md)
  - Word-count re-gate against trimmed stub (now ~700 words — within 600-900 envelope)
  - VALIDATION.md trailer appended with "Validation Audit 2026-04-24" block
  - Exit: "PHASE 39 IS NYQUIST-COMPLIANT" OR "PHASE 39 VALIDATED (PARTIAL)"

If /gsd-validate-phase 39 reports NEW gaps (low probability):
  - Plan 09 MUST branch: trim further and re-run. Mitigation per RESEARCH §7 line 743.

Expected VALIDATION.md trailer shape (PATTERNS §File 17 / RESEARCH §7 lines 725-739):
  ## Validation Audit 2026-04-24

  | Metric | Count |
  |--------|-------|
  | Gaps found | 0 |
  | Resolved | 0 |
  | Escalated | 0 |

  ### DEFER-04 closure

  AOSP stub body word count (harness formula): {final-trimmed-count} words.
  Phase 39 envelope: 600-900 words. Status: PASS.
  Trim committed in Phase 43 (commit-7 of 10-commit plan).

Fallback: if /gsd-validate-phase 39 cannot record DEFER-04 closure inline, append a `re_audit_resolution:` block to v1.4-MILESTONE-AUDIT.md documenting:
  - commit SHA of Plan 07's stub trim
  - timestamp of Plan 09's re-gate
  - word count landed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 43-09-01: Restore Phase 39 directory from git commit ef7717b</name>
  <files>.planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md, .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VERIFICATION.md, .planning/phases/39-zero-touch-enrollment-aosp-stub/39-01-SUMMARY.md, .planning/phases/39-zero-touch-enrollment-aosp-stub/39-02-SUMMARY.md, (plus any additional files present in ef7717b tree)</files>
  <read_first>
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md D-21 (DEFER-04 closure mechanism)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md §7 "Option A RECOMMENDED" (restore commands lines 703-710)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §File 17 + §Pattern K Multi-file pattern
  </read_first>
  <action>
    Restore Phase 39 artifacts from commit ef7717b. Use the multi-file pattern from PATTERNS §Pattern K so any additional files in the ef7717b tree are rehydrated (not just the 4 listed in RESEARCH §7 — the tree may include PLAN/CONTEXT/other artifacts).

    From repo root:
    ```
    # Step 1: Enumerate all git-tracked files in the phase-39 directory at commit ef7717b
    git ls-tree -r --name-only ef7717b .planning/phases/39-zero-touch-enrollment-aosp-stub/ > /tmp/phase39-files.txt
    cat /tmp/phase39-files.txt

    # Step 2: Create the parent directory
    mkdir -p .planning/phases/39-zero-touch-enrollment-aosp-stub/

    # Step 3: Restore each file verbatim
    while IFS= read -r path; do
      mkdir -p "$(dirname "$path")"
      git show "ef7717b:$path" > "$path"
    done < /tmp/phase39-files.txt
    ```

    Verify restoration:
    ```
    ls .planning/phases/39-zero-touch-enrollment-aosp-stub/
    # Expect at minimum: 39-VALIDATION.md, 39-VERIFICATION.md, 39-01-SUMMARY.md, 39-02-SUMMARY.md
    test -f .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md
    test -f .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VERIFICATION.md
    # Check VERIFICATION shows the historical passed status
    head -20 .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VERIFICATION.md | grep -qi "passed" && echo "status: passed confirmed"
    ```

    If any files failed to restore (e.g., they were never committed under that path): note the missing files and proceed with whatever was restored. The validator workflow should still detect State A from the presence of 39-VALIDATION.md (which definitely existed at ef7717b).

    This task makes FILE restorations only — no commit yet. Task 43-09-03 commits.
  </action>
  <verify>
    <automated>test -d .planning/phases/39-zero-touch-enrollment-aosp-stub && test -f .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md && test -f .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -d .planning/phases/39-zero-touch-enrollment-aosp-stub` returns 0
    - `test -f .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md` returns 0
    - `test -f .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VERIFICATION.md` returns 0
    - At least 2 SUMMARY files present (`39-01-SUMMARY.md` + `39-02-SUMMARY.md`)
    - 39-VERIFICATION.md contains "passed" status in its frontmatter/header (verifies correct file restored)
    - Files are byte-identical to ef7717b content: `git show ef7717b:.planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md | diff - .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md` returns empty (no diff)
  </acceptance_criteria>
  <done>
    Phase 39 directory rehydrated from ef7717b. All historical artifacts available on disk for the validator workflow.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 43-09-02 (checkpoint): Invoke /gsd-validate-phase 39 + review outcome</name>
  <files>.planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md (will be appended by validator)</files>
  <read_first>
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md §7 full (expected trailer shape + branch for new gaps)
  </read_first>
  <action>
    This is a checkpoint:human-action task. Claude has already automated the restoration (Task 43-09-01). Now the human invokes the /gsd-validate-phase 39 workflow, reviews the outcome, and resumes per the resume-signal below. See <what-built>, <how-to-verify>, and <resume-signal> for the full checkpoint contract.
  </action>
  <what-built>
    Plan 09 Task 01 restored Phase 39 artifacts from git commit ef7717b to .planning/phases/39-zero-touch-enrollment-aosp-stub/. The validator workflow now sees State A (existing VALIDATION.md) and can re-gate against the trimmed AOSP stub (Plan 07).
  </what-built>
  <how-to-verify>
    1. From repo root, invoke the validator workflow:
       /gsd-validate-phase 39

    2. The workflow will:
       - Detect State A (existing 39-VALIDATION.md)
       - Run gap analysis (each requirement COVERED / PARTIAL / MISSING)
       - If all gaps COVERED (expected — trimmed stub is within envelope): append "Validation Audit 2026-04-24" trailer with 0 gaps/0 resolved/0 escalated + DEFER-04 closure block
       - If gaps found: prompt "fix/skip/cancel" — human must decide

    3. Expected trailer shape (per PATTERNS §File 17 + RESEARCH §7 lines 725-739):
       ```
       ## Validation Audit 2026-04-24
       | Metric | Count |
       |--------|-------|
       | Gaps found | 0 |
       | Resolved | 0 |
       | Escalated | 0 |

       ### DEFER-04 closure
       AOSP stub body word count (harness formula): {N} words.
       Phase 39 envelope: 600-900 words. Status: PASS.
       Trim committed in Phase 43 (commit-7 of 10-commit plan).
       ```

    4. Verify the trailer landed:
       grep -A 10 "Validation Audit 2026-04-24" .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md

    5. Confirm workflow reported "PHASE 39 IS NYQUIST-COMPLIANT" or equivalent.

    6. BRANCH: If workflow reports NEW gaps (low probability — stub now ~700 words, well within 600-900 envelope):
       - "Word count landed at 830, still need to trim 30 more" scenario (RESEARCH §7 line 743)
       - Return to Plan 07, trim Candidate 5 (H2-5 prose intro per RESEARCH §5 line 608)
       - Re-invoke /gsd-validate-phase 39
  </how-to-verify>
  <resume-signal>
    Type "validated" once /gsd-validate-phase 39 has written the 2026-04-24 trailer to 39-VALIDATION.md AND workflow reported PASS.
    Type "gaps-found: {description}" if workflow reports new gaps (trigger the Plan 07 re-trim branch).
  </resume-signal>
  <acceptance_criteria>
    - User types "validated" after workflow success
    - 39-VALIDATION.md contains "Validation Audit 2026-04-24" heading
    - Trailer shows 0 gaps (or human-approved gap resolution path)
    - DEFER-04 closure text present in trailer
  </acceptance_criteria>
  <done>
    /gsd-validate-phase 39 has run, produced the 2026-04-24 audit trailer, and reported PASS. DEFER-04 closure is recorded.
  </done>
</task>

<task type="auto">
  <name>Task 43-09-03: Append re_audit_resolution to v1.4-MILESTONE-AUDIT.md + commit Plan 09 payload</name>
  <files>.planning/milestones/v1.4-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v1.4-MILESTONE-AUDIT.md (current state; locate tech_debt / defer section to update)
    - .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md (post Task 02 — contains the validator's 2026-04-24 trailer; quote its word-count number)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md D-21 (closure mechanism)
  </read_first>
  <action>
    Belt-and-braces: even though Task 43-09-02 recorded DEFER-04 closure in 39-VALIDATION.md, append a concise `re_audit_resolution:` block to `.planning/milestones/v1.4-MILESTONE-AUDIT.md` so the milestone-level audit surfaces the closure (v1.4.1 Phase 47 will flip `tech_debt → passed` using this record).

    Locate the DEFER-04 entry in the audit file. Append (or update in place) the following block:

    ```yaml
    DEFER-04:
      resolution_phase: "43-v1-4-cleanup-audit-harness-fix"
      resolution_plan: "07 (AOSP stub trim) + 09 (re-gate)"
      resolution_commit: "{Plan 07 commit SHA}"
      resolution_timestamp: "2026-04-24T{HH:MM:SS}Z"
      final_word_count: "{N from 39-VALIDATION.md trailer}"
      envelope: "600-900 words (Phase 39 D-11)"
      status: "resolved"
      validator_artifact: ".planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md (Validation Audit 2026-04-24 trailer)"
    ```

    The `{Plan 07 commit SHA}` comes from `git log` — find the `docs(43): trim AOSP stub` commit SHA. The `{N}` comes from 39-VALIDATION.md's trailer (the number the validator emitted).

    If v1.4-MILESTONE-AUDIT.md has a schema for defer-resolution (check existing structure first), match that schema exactly. If no existing schema, use the YAML block above at the appropriate location (near the existing DEFER-04 row or in a "Resolutions" section at the bottom).

    Commit both the restored Phase 39 artifacts AND the v1.4-MILESTONE-AUDIT.md update as one commit:
    ```
    git add .planning/phases/39-zero-touch-enrollment-aosp-stub/ .planning/milestones/v1.4-MILESTONE-AUDIT.md
    git commit -m "docs(43): restore Phase 39 artifacts + DEFER-04 closure re-gate"
    ```

    Commit message body should cite:
    - D-21 DEFER-04 closure mechanism
    - Plan 07 AOSP stub trim → 39 envelope PASS
    - 39-VALIDATION.md trailer entry (2026-04-24)
    - re_audit_resolution block appended to v1.4-MILESTONE-AUDIT.md
    - AEAUDIT-04 final closure (stub trim in Plan 07 + re-gate in Plan 09)
  </action>
  <verify>
    <automated>grep -q "DEFER-04" .planning/milestones/v1.4-MILESTONE-AUDIT.md && grep -q "resolved\|resolution_phase.*43" .planning/milestones/v1.4-MILESTONE-AUDIT.md && grep -q "Validation Audit 2026-04-24" .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md && git log -1 --pretty=%s | grep -q "docs(43)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "DEFER-04" .planning/milestones/v1.4-MILESTONE-AUDIT.md` returns 0
    - Audit file contains a resolution block referencing Phase 43 (via "resolution_phase" or similar key)
    - Audit file references a commit SHA for the Plan 07 trim
    - Audit file records a word-count within 600-900 envelope
    - `grep -q "Validation Audit 2026-04-24" .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md` returns 0
    - `git log -1 --pretty=%s` shows "docs(43): restore Phase 39" subject
    - Commit includes both the Phase 39 directory files AND the v1.4-MILESTONE-AUDIT.md update
  </acceptance_criteria>
  <done>
    Phase 39 directory restored + /gsd-validate-phase 39 trailer committed + re_audit_resolution block in v1.4-MILESTONE-AUDIT.md. DEFER-04 is formally closed. AEAUDIT-04 fully resolved.
  </done>
</task>

</tasks>

<verification>
```
# 1. Phase 39 directory restored
test -d .planning/phases/39-zero-touch-enrollment-aosp-stub
test -f .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md
test -f .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VERIFICATION.md

# 2. Validator trailer present
grep -q "Validation Audit 2026-04-24" .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md
grep -qE "DEFER-04 closure|word count" .planning/phases/39-zero-touch-enrollment-aosp-stub/39-VALIDATION.md

# 3. Milestone-audit closure block present
grep -q "DEFER-04" .planning/milestones/v1.4-MILESTONE-AUDIT.md
grep -q "resolved\|resolution_phase" .planning/milestones/v1.4-MILESTONE-AUDIT.md

# 4. Commit
git log -1 --pretty=%s | grep -q "docs(43).*Phase 39"
```
</verification>

<success_criteria>
- Phase 39 artifacts rehydrated from git ef7717b
- /gsd-validate-phase 39 ran + recorded re-gate trailer (2026-04-24)
- DEFER-04 formally closed in both 39-VALIDATION.md AND v1.4-MILESTONE-AUDIT.md
- AEAUDIT-04 fully resolved (stub trim in Plan 07 + re-gate in Plan 09 = full closure)
- Phase 39 status remains `passed`
</success_criteria>

<output>
After completion, create `.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-09-SUMMARY.md` documenting:
- Files restored (count + names)
- /gsd-validate-phase 39 workflow result
- Word count recorded in validator trailer
- re_audit_resolution block content
- Commit SHA
- Unblocks: Plan 10 (terminal sanity — everything now ready for final validation)
</output>
