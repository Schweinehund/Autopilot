---
phase: 43
plan: 07
type: execute
wave: 4
depends_on: [02]
files_modified:
  - docs/admin-setup-android/06-aosp-stub.md
  - .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md
autonomous: true
requirements: [AEAUDIT-04]
must_haves:
  truths:
    - "docs/admin-setup-android/06-aosp-stub.md body word count ≤ 900 (target 700) per D-18 / AEAUDIT-04 envelope"
    - "PITFALL-7 'not supported under AOSP' framing preserved verbatim per D-17"
    - "9-H2 whitelist structure preserved (Phase 39 D-11 lock) + 8-OEM enumeration preserved (Phase 39 D-17)"
    - "Deferred-content table preserved intact (Phase 45 AEAOSPFULL-09 will collapse it; not Phase 43)"
    - "Trimmed stub does NOT forward-link to .planning/phases/45-*/PHASE-45-AOSP-SOURCE.md per D-19"
    - ".planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md exists with ≥200 words of RealWear deep content (lossless extract) per D-16 / D-20"
    - "v1.4.1 harness C6 (PITFALL-7 preservation) still emits informational pass with 1/1 files preserving framing"
  artifacts:
    - path: "docs/admin-setup-android/06-aosp-stub.md"
      provides: "Trimmed AOSP stub — PITFALL-7 locked + 9-H2 + 8-OEM preserved; ~700 word body target"
      contains: "not supported under AOSP"
    - path: ".planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md"
      provides: "Lossless extract of RealWear deep content for Phase 45 consumption"
      contains: "RealWear"
  key_links:
    - from: "docs/admin-setup-android/06-aosp-stub.md"
      to: "(no link — D-19 prep-shell invisible to readers)"
      via: "prose-only reference 'Phase 45 will expand per-OEM'"
      pattern: "Phase 45"
---

<objective>
Trim `docs/admin-setup-android/06-aosp-stub.md` body from ~1089 words (harness-formula) to ~700 words per D-18, while preserving Phase 39 locked invariants: PITFALL-7 "not supported under AOSP" framing (D-17); 9-H2 whitelist structure (Phase 39 D-11); 8-OEM enumeration (Phase 39 D-17); deferred-content table intact.

Per D-16: extract RealWear deep content (step-by-step, tables, detail paragraphs — lines 53-60 of current stub) to `.planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md` (Option A location per RESEARCH §6). Per D-20: this prep shell has lifecycle — Phase 45 deletes it in its final commit.

Per D-19: trimmed stub does NOT forward-link to the prep shell. Reference "Phase 45 will expand per-OEM" as prose only.

This is Wave 4 — depends on Plan 02 (v1.4.1 harness C6 informational check runs on the stub post-trim). Independent of Plans 03/04/05/06.

Purpose: Resolves AEAUDIT-04 (AOSP stub envelope discrepancy). Preps Phase 45 input artifact.

Output: 1 file trim (~389-word net reduction) + 1 new prep-shell file + 1 new phase-45 directory. One commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md
@docs/admin-setup-android/06-aosp-stub.md
@scripts/validation/v1.4.1-milestone-audit.mjs

<interfaces>
<!-- Pattern J: planning-space input-artifact lifecycle (D-20 — Phase 45 deletes the prep shell) -->

Target file H2 hierarchy (locked per Phase 39 D-11, verified via PATTERNS §File 12 table, line numbers as of 2026-04-24):
  Line 9   # AOSP Device Management Stub — Intune
  Line 24  ## Scope and Status
  Line 30  ## What AOSP Is
  Line 40  ## When to Use AOSP
  Line 49  ## Supported OEMs
           Line 53-60: H3 "RealWear (confirmed GA)" deep paragraphs — MIGRATE OUT
           Line 62-75: H3 "Other AOSP-Supported OEMs" + 8-OEM enumeration — PRESERVE
  Line 77  ## Enrollment Constraints
  Line 93  ## Prerequisites and Licensing
  Line 100 ## Deferred Content (table preserved; preface sentence may go)
  Line 114 ## See Also
  Line 124 ## Changelog (APPEND Phase 43 trim row)

Trim targets (RESEARCH §5):
  - H2-2 "What AOSP Is" bullets → 2-sentence prose (save ~60 words)
  - H2-4 RealWear deep H3 (lines 53-60) → 2-sentence compression + extract (save ~130 words)
  - H2-5 Enrollment Constraints callouts 2-line → 1-line compression (save ~30 words)
  - Candidate compressions (RESEARCH §5 lines 600-607): H2-1 2nd sentence, H2-3 closing prose, H2-6 tail, H2-7 preface (save ~107 words combined)
  - Total planned: ~327 words out of ~389 needed. Candidate 5 (H2-5 intro — save ~15 words) if needed; landing target 700-750 words.

PITFALL-7 invariant (D-17): /not supported under AOSP/i MUST match at least once in the trimmed stub.
9-H2 invariant (D-17): stub must still have >= 9 "^## " lines.
8-OEM invariant (D-17): stub must still enumerate RealWear + Zebra + PICO + HTC + Meta (and the other 3 OEMs — DigiLens, Lenovo, Vuzix).

Prep shell target path (D-16 + RESEARCH §6 Option A): `.planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md`

Prep shell content (per PATTERNS §File 13 template):
- Source + Consumer + Lifecycle frontmatter note
- "RealWear Hardware Scope" section (verbatim from current stub lines 53-60: HMT-1 11.2+, HMT-1Z1 11.2+, Navigator 500 1.1+)
- "Enrollment Mechanics" section (Wi-Fi-embedded-in-QR requirement)
- Confidence marker `[HIGH: MS Learn AOSP supported devices, last_verified 2026-04-23]`
- "Wi-Fi QR-Payload Embedding Walk-Through" placeholder (Phase 45 expands)
- "PITFALL-7 Invariant (Phase 39 D-17)" section (reiterate the invariant)
- "Cross-Links for Phase 45 Author" section

Word-count verification formula (PATTERNS §File 12):
  Strip frontmatter (^---[\s\S]*?\n---\n) + See Also section (^## See Also[\s\S]*$) + Changelog section (^## Changelog[\s\S]*$); count remaining whitespace-split tokens.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 43-07-01: Create Phase 45 directory + author PHASE-45-AOSP-SOURCE.md prep shell</name>
  <files>.planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md</files>
  <read_first>
    - docs/admin-setup-android/06-aosp-stub.md (lines 53-60 — RealWear H3 deep paragraphs to extract)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md D-16 / D-17 (migration target + scope)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md §6 "Prep shell authoring" (lines 619-669 — full template)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §File 13 (structure + content source pointers)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §Pattern J (lifecycle)
  </read_first>
  <action>
    Create the Phase 45 directory first:
    ```
    mkdir -p .planning/phases/45-per-oem-aosp-expansion
    ```

    Write `.planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md` per RESEARCH §6 / PATTERNS §File 13 template. Minimum structure:

    ```markdown
    # Phase 45 AOSP Per-OEM Input Artifact — RealWear Deep Content

    **Source:** Extracted from `docs/admin-setup-android/06-aosp-stub.md` during Phase 43 trim (2026-04-24).
    **Consumer:** Phase 45 per-OEM AOSP expansion (AEAOSPFULL-01 RealWear admin guide).
    **Lifecycle:** Phase 45 DELETES this file in its final commit per Phase 43 D-20. Do NOT link to it from shipped docs.

    ## RealWear Hardware Scope

    RealWear **HMT-1** (firmware 11.2+), **HMT-1Z1** (11.2+), and **Navigator 500** (1.1+) are confirmed GA for AOSP management in Intune — AR/VR headsets deployed for hands-free frontline work (field service, maintenance, remote expert assistance).

    [Include full verbatim extract from lines 53-60 of the current stub. Every sentence removed from the stub MUST land here OR be explicitly marked as consolidated.]

    ## Enrollment Mechanics

    Enrollment is QR-only, one device at a time. **Wi-Fi credentials MUST be embedded in the QR enrollment payload** because RealWear devices cannot join Wi-Fi interactively during enrollment — the device must come up already network-connected to reach the Intune enrollment server on first boot.

    `[HIGH: MS Learn AOSP supported devices, last_verified 2026-04-23]`

    ## Wi-Fi QR-Payload Embedding Walk-Through

    [Placeholder — Phase 45 researcher will expand with actual Intune UI path for embedding Wi-Fi in the AOSP QR generator: SSID field, password field, EAP config for enterprise networks, etc. Reference: Intune admin center > Devices > Android > Enrollment > Corporate-owned dedicated devices > Token > Wi-Fi.]

    ## PITFALL-7 Invariant (Phase 39 D-17)

    Every per-OEM "supported" assertion MUST pair with the AOSP baseline caveat: this hardware is supported *under AOSP mode* (not under Android Enterprise managed modes). Do NOT drop the "not supported under AOSP" framing from per-OEM content.

    ## Cross-Links for Phase 45 Author

    - Stub that trimmed this content: `docs/admin-setup-android/06-aosp-stub.md`
    - PITFALL-7 framing locked at: Phase 39 §CONTEXT D-17 (reference via git history if phase-39 directory is torn down)
    - AOSP supported-devices source: https://learn.microsoft.com/en-us/intune/fundamentals/aosp-supported-devices
    ```

    Replace `[Include full verbatim extract from lines 53-60 of the current stub...]` with the actual verbatim content from the current stub's lines 53-60. The lossless-extract contract (RESEARCH §6 line 666) means every sentence Phase 43 removes from the stub MUST land here.

    Verify prep shell has ≥200 words:
    ```
    wc -w .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md && grep -q "RealWear" .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md && grep -q "HMT-1" .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md && grep -q "PITFALL-7" .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md && test $(wc -w < .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md) -ge 200</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md` returns 0
    - `test -d .planning/phases/45-per-oem-aosp-expansion` returns 0
    - Prep shell contains "RealWear", "HMT-1", "HMT-1Z1", "Navigator 500" (verbatim hardware names preserved)
    - Prep shell contains "PITFALL-7" reference (re-asserts invariant)
    - Prep shell contains "Phase 45 DELETES this file" lifecycle note (D-20)
    - Prep shell contains "Wi-Fi" content (embedding walk-through placeholder or actual content)
    - Prep shell word count ≥ 200 words
    - Prep shell contains "[HIGH:" confidence marker
  </acceptance_criteria>
  <done>
    .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md exists with lossless extract of RealWear deep content + placeholders for Phase 45 expansion + lifecycle note.
  </done>
</task>

<task type="auto">
  <name>Task 43-07-02: Trim docs/admin-setup-android/06-aosp-stub.md to ~700 words (PITFALL-7 + structural locks preserved)</name>
  <files>docs/admin-setup-android/06-aosp-stub.md</files>
  <read_first>
    - docs/admin-setup-android/06-aosp-stub.md (full read before editing; confirm current line numbers for trim targets)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md D-17 / D-18 / D-19 (preservation rules + target + no forward-link)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md §5 "AOSP stub trim strategy" (sections to keep/compress/migrate + word-count math lines 556-617)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §File 12 (H2 hierarchy lock + verification command)
  </read_first>
  <action>
    Apply the RESEARCH §5 trim strategy. Structural invariants (MUST remain after trim):

    **KEEP verbatim (per D-17 + Phase 39 D-11/D-17):**
    - Frontmatter (lines 1-7)
    - Title "# AOSP Device Management Stub — Intune"
    - Platform gate banner
    - Subtractive-deletion HTML comments
    - H2-1 "Scope and Status" (compress 2nd sentence — save ~25 words)
    - H2-3 "When to Use AOSP" (compress closing prose — save ~30 words)
    - H2-4 "Supported OEMs" intro + H3 "Other AOSP-Supported OEMs" (8-OEM enumeration, PITFALL-7 closing sentence on line 75)
    - H2-5 "Enrollment Constraints" body (compress 3 callouts 2-line → 1-line — save ~30 words)
    - H2-6 "Prerequisites and Licensing" (compress paragraph tail — save ~45 words)
    - H2-7 "Deferred Content" table (keep table intact; strip preface sentence only — save ~12 words)
    - H2-8 "See Also" (no trim; excluded from word count)
    - H2-9 "Changelog" (APPEND Phase 43 trim row; excluded from word count)

    **COMPRESS / MIGRATE OUT (per D-17 migration scope):**
    - H2-2 "What AOSP Is" bullets (lines 30-38) → compress to 2-sentence prose (save ~60 words). Exact replacement text (RESEARCH §5 row 1):
      > "AOSP is Android without GMS (no FCM, no MGP, no Google sign-in handshake). Devices report via `intunecdnpeasd.manage.microsoft.com` rather than GMS-path endpoints."
    - H2-4 H3 "RealWear (confirmed GA)" deep paragraphs (lines 53-60) → compress to 2-sentence placeholder (save ~130 words). Exact replacement text (RESEARCH §5 row 2):
      > "RealWear HMT-1, HMT-1Z1, and Navigator 500 are confirmed GA for AOSP management in Intune. Per-OEM enrollment mechanics (including Wi-Fi credential embedding in the QR payload) are deferred to Phase 45."
      Ensure the original RealWear deep content (what you removed) is present in Task 43-07-01's PHASE-45-AOSP-SOURCE.md (lossless-extract contract).

    **INVARIANTS TO VERIFY POST-TRIM:**
    - PITFALL-7 framing: `grep -q "not supported under AOSP" docs/admin-setup-android/06-aosp-stub.md` returns 0
    - 9-H2 count: `grep -c "^## " docs/admin-setup-android/06-aosp-stub.md` ≥ 9
    - 8-OEM enumeration: `grep -qE "RealWear|Zebra|PICO|HTC|Meta" docs/admin-setup-android/06-aosp-stub.md` returns 0
    - NO forward-link to prep shell: `! grep -q "PHASE-45-AOSP-SOURCE" docs/admin-setup-android/06-aosp-stub.md` returns 0 (D-19)
    - Word count ≤ 900 (target 700): per PATTERNS §File 12 verification command

    Append a Changelog row (per PATTERNS §File 12 H2-9 line 124 "APPEND Phase 43 trim row"):
    ```
    | 2026-04-24 | v1.4.1 Phase 43 AEAUDIT-04: body trimmed to ~{final-count} words; RealWear deep content migrated to Phase 45 prep shell; PITFALL-7 framing preserved. |
    ```

    Verify word count:
    ```
    node -e "const fs=require('fs'); const c=fs.readFileSync('docs/admin-setup-android/06-aosp-stub.md','utf8').replace(/\r\n/g,'\n'); let b=c.replace(/^---[\s\S]*?\n---\n/,''); b=b.replace(/^## See Also[\s\S]*$/m,''); b=b.replace(/^## Changelog[\s\S]*$/m,''); const wc=b.split(/\s+/).filter(Boolean).length; if (wc < 600 || wc > 900) { console.error('AOSP body '+wc+' outside envelope 600-900'); process.exit(1); } console.log('AOSP body '+wc+' within envelope');"
    ```

    If word count is > 900: apply Candidate 5 (H2-5 prose intro — save ~15 words per RESEARCH §5 line 608). If still > 900, apply additional gentle compression — NEVER at the cost of PITFALL-7 framing, 9-H2 structure, or 8-OEM enumeration.
  </action>
  <verify>
    <automated>grep -q "not supported under AOSP" docs/admin-setup-android/06-aosp-stub.md && test $(grep -c "^## " docs/admin-setup-android/06-aosp-stub.md) -ge 9 && grep -qE "RealWear" docs/admin-setup-android/06-aosp-stub.md && grep -qE "Zebra" docs/admin-setup-android/06-aosp-stub.md && ! grep -q "PHASE-45-AOSP-SOURCE" docs/admin-setup-android/06-aosp-stub.md && node -e "const fs=require('fs'); const c=fs.readFileSync('docs/admin-setup-android/06-aosp-stub.md','utf8').replace(/\r\n/g,'\n'); let b=c.replace(/^---[\s\S]*?\n---\n/,''); b=b.replace(/^## See Also[\s\S]*\$/m,''); b=b.replace(/^## Changelog[\s\S]*\$/m,''); const wc=b.split(/\s+/).filter(Boolean).length; process.exit(wc<=900 ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "not supported under AOSP" docs/admin-setup-android/06-aosp-stub.md` returns 0 (PITFALL-7 preserved — D-17)
    - `test $(grep -c "^## " docs/admin-setup-android/06-aosp-stub.md) -ge 9` returns 0 (9-H2 structural lock)
    - All 5 primary OEMs present: `grep -qE "RealWear" && grep -qE "Zebra" && grep -qE "PICO" && grep -qE "HTC" && grep -qE "Meta" docs/admin-setup-android/06-aosp-stub.md`
    - `! grep -q "PHASE-45-AOSP-SOURCE" docs/admin-setup-android/06-aosp-stub.md` returns 0 (D-19 — no forward-link)
    - Body word count (frontmatter + See Also + Changelog excluded) ≤ 900 AND ≥ 600 words (D-18 envelope)
    - Target landing: ≤ 750 words preferred per D-18 "~200 words headroom"
    - Changelog shows a new 2026-04-24 Phase 43 row: `grep -q "2026-04-24.*Phase 43.*AEAUDIT-04" docs/admin-setup-android/06-aosp-stub.md` or similar pattern (row format flexible, but date + AEAUDIT reference required)
    - Deferred Content table (H2-7) still present with full rows intact
  </acceptance_criteria>
  <done>
    06-aosp-stub.md trimmed to ≤ 900 words (target ≤ 750). PITFALL-7 + 9-H2 + 8-OEM all preserved. No forward-link to prep shell. Changelog updated.
  </done>
</task>

<task type="auto">
  <name>Task 43-07-03: Run v1.4.1 harness C6 + verify envelope + commit</name>
  <files>(no edits — verification + commit step)</files>
  <read_first>
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VALIDATION.md tasks 43-07-01..04
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md §5 PITFALL-7 invariant (line 617)
  </read_first>
  <action>
    Run v1.4.1 harness and verify C6 (PITFALL-7 informational) still reports PASS with at least 1 file preserving framing:
    ```
    node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | tee /tmp/v141-plan07.txt
    grep -qE "C6.*PASS" /tmp/v141-plan07.txt && echo "C6 PASS" || echo "C6 REGRESSION — PITFALL-7 framing dropped"
    grep "C6" /tmp/v141-plan07.txt
    # Expected detail: "(informational — 1/1 AOSP-scoped files preserve PITFALL-7 framing)"
    ```

    If C6 detail shows "0/1", the trim accidentally removed the `not supported under AOSP` sentence — investigate and restore.

    Also run C3 to confirm word-count informational check reports the new lower count:
    ```
    grep "C3" /tmp/v141-plan07.txt
    # C3 always PASSes informationally; detail line shows the current word count
    ```

    Commit the Plan 07 payload:
    ```
    git add docs/admin-setup-android/06-aosp-stub.md .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md
    git commit -m "docs(43): trim AOSP stub to envelope + migrate RealWear deep content to Phase 45 prep shell"
    ```

    Commit message body should cite: D-16 (migration target); D-17 (PITFALL-7 + 9-H2 + 8-OEM preserved); D-18 (word-count target); D-19 (no forward-link); D-20 (lifecycle note present); AEAUDIT-04 closure delta (stub trim + migration — Phase 45 AEAOSPFULL-09 will close deferred-content table collapse).
  </action>
  <verify>
    <automated>node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | grep -qE "C6.*PASS" && node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | grep -qE "1/1.*PITFALL-7"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | grep -qE "C6.*PASS"` returns 0
    - C6 detail line contains "1/1" (one file preserves PITFALL-7 framing — the AOSP stub)
    - C3 detail line shows a word count ≤ 900 for the AOSP stub
    - v1.4.1 harness exit code 0 or 1
    - `git log -1 --pretty=%s` shows "docs(43): trim AOSP stub" subject
    - `git show --stat HEAD 2>&1 | grep -q "06-aosp-stub.md" && git show --stat HEAD 2>&1 | grep -q "PHASE-45-AOSP-SOURCE.md"` (both files in same commit)
  </acceptance_criteria>
  <done>
    Commit landed atomically with stub trim + prep shell creation. v1.4.1 C6 PASSES with 1/1 PITFALL-7 preserved. AEAUDIT-04 stub-trim delta complete (deferred-content table collapse remains Phase 45 AEAOSPFULL-09 scope).
  </done>
</task>

</tasks>

<verification>
```
# 1. Prep shell exists + has content
test -f .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md
test $(wc -w < .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md) -ge 200
grep -q "RealWear\|HMT-1" .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md

# 2. Stub trimmed + invariants
grep -q "not supported under AOSP" docs/admin-setup-android/06-aosp-stub.md
test $(grep -c "^## " docs/admin-setup-android/06-aosp-stub.md) -ge 9
! grep -q "PHASE-45-AOSP-SOURCE" docs/admin-setup-android/06-aosp-stub.md

# 3. Word count
node -e "const fs=require('fs');const c=fs.readFileSync('docs/admin-setup-android/06-aosp-stub.md','utf8').replace(/\r\n/g,'\n');let b=c.replace(/^---[\s\S]*?\n---\n/,'');b=b.replace(/^## See Also[\s\S]*$/m,'');b=b.replace(/^## Changelog[\s\S]*$/m,'');const wc=b.split(/\s+/).filter(Boolean).length;console.log('body:',wc);process.exit(wc<=900 && wc>=600 ? 0 : 1);"

# 4. v1.4.1 C6 PASS with framing preserved
node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | grep -qE "C6.*PASS"
node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | grep -qE "1/1"

# 5. Atomic commit
git show --stat HEAD 2>&1 | grep -cE "(06-aosp-stub.md|PHASE-45-AOSP-SOURCE.md)"  # expect 2
```
</verification>

<success_criteria>
- AOSP stub trimmed to 600-900 words (target ≤ 750)
- PITFALL-7 framing preserved
- 9-H2 whitelist + 8-OEM enumeration intact
- Deferred-content table intact (Phase 45 AEAOSPFULL-09 will collapse it)
- No forward-link from stub to prep shell (D-19)
- Prep shell exists with lossless RealWear extract + lifecycle note (D-20)
- v1.4.1 C6 PASS with 1/1 framing preservation
- AEAUDIT-04 stub-trim closure delta landed
</success_criteria>

<output>
After completion, create `.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-07-SUMMARY.md` documenting:
- Before/after word counts
- Sections preserved vs. compressed vs. migrated
- Prep shell content inventory
- Commit SHA
- Unblocks: Plan 09 (/gsd-validate-phase 39 re-gate — needs trimmed stub to land first), Plan 10 (terminal sanity)
</output>
