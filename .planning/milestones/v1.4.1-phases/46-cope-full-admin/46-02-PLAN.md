---
phase: 46-cope-full-admin
plan: 02
type: execute
wave: 2
depends_on:
  - "46-01"
files_modified:
  - docs/reference/android-capability-matrix.md
  - docs/admin-setup-android/03-fully-managed-cobo.md
  - docs/admin-setup-android/04-byod-work-profile.md
  - docs/_glossary-android.md
  - docs/android-lifecycle/03-android-version-matrix.md
autonomous: true
requirements:
  - AECOPE-02
  - AECOPE-03
  - AECOPE-04
must_haves:
  truths:
    - "Capability matrix has COPE column inserted at index 1 (between COBO and BYOD per D-20) across ALL 5 H2 sub-tables (Enrollment / Configuration / App Deployment / Compliance / Software Updates)"
    - "Capability matrix Cross-Platform Equivalences section (lines 72-88 pre-edit) is BYTE-IDENTICAL after this plan — zero new paired rows for COPE per D-22 / SC#4"
    - "Capability matrix has new `Private Space` row across ALL Android columns (COBO + COPE + BYOD + Dedicated + ZTE + AOSP) marked `N/A¹` with shared footnote per D-12"
    - "COBO `03-fully-managed-cobo.md` line 64 ONLY is replaced via γ3 sentence-scoped trim per D-21; lines 58-63 PRESERVED + lines 65-66 PRESERVED including HTML comment with `last_verified: 2026-04-21` per Pitfall 4"
    - "BYOD `04-byod-work-profile.md` line 167 ONLY is replaced via sentence-scoped retrofit per D-10"
    - "Glossary `_glossary-android.md` has new `### Private Space` H3 inserted under `## Ownership & Management Scope` H2 alphabetically between Fully Managed and Supervision per D-09"
    - "Glossary line 15 alphabetical index updated to include `Private Space` between `Play Integrity` and `Supervision`"
    - "Glossary COPE entry (line 47-51) and WPCO entry (line 75-79) each contain a forward-link to `08-cope-full-admin.md` per AECOPE-04"
    - "Version matrix `03-android-version-matrix.md` has new `### Android 15 — Private Space (Personal-Side, Unmanageable)` H3 in Version Breakpoint Details section per D-11"
    - "Version matrix line 30 BYOD-row updated to include anchor link `[Android 15: Private Space unsupported](#android-15-private-space-breakpoint)` per D-11"
    - "ALL 5 file edits ship in ONE atomic commit per D-23 / D-34 (the Wave 2 atomic bundle interpretation)"
    - "Audit harness `node scripts/validation/v1.4.1-milestone-audit.mjs` passes 8/8 after commit lands"
    - "Allow-list helper `node scripts/validation/regenerate-supervision-pins.mjs --report` reports zero unpinned findings (or all shifts hand-merged per Phase 43 D-12)"
  artifacts:
    - path: "docs/reference/android-capability-matrix.md"
      provides: "6-column Android capability matrix with COPE at index 1; Private Space row across all modes; Cross-Platform Equivalences UNCHANGED"
      contains:
        - "COPE (WPCO / Corp-Owned Work Profile)"
        - "Private Space"
    - path: "docs/admin-setup-android/03-fully-managed-cobo.md"
      provides: "COBO migration note H2 forward-links to 08-cope-full-admin.md (closes v1.4 forward-promise)"
      contains:
        - "08-cope-full-admin.md"
    - path: "docs/admin-setup-android/04-byod-work-profile.md"
      provides: "BYOD line 167 Private Space sentence retrofit (drift-elimination per Pitfall 1)"
      contains:
        - "_glossary-android.md#private-space"
    - path: "docs/_glossary-android.md"
      provides: "New Private Space H3 + alphabetical index update + COPE/WPCO see-also forward-links"
      contains:
        - "### Private Space"
        - "08-cope-full-admin.md"
    - path: "docs/android-lifecycle/03-android-version-matrix.md"
      provides: "Android 15 Private Space breakpoint H3 + line 30 BYOD-row anchor link"
      contains:
        - "### Android 15 — Private Space (Personal-Side, Unmanageable)"
        - "android-15-private-space-breakpoint"
  key_links:
    - from: "docs/admin-setup-android/03-fully-managed-cobo.md (line 64 retrofit)"
      to: "docs/admin-setup-android/08-cope-full-admin.md"
      via: "## COPE Migration Note H2 forward-link"
      pattern: "08-cope-full-admin.md"
    - from: "docs/admin-setup-android/04-byod-work-profile.md (line 167 retrofit)"
      to: "docs/_glossary-android.md#private-space"
      via: "Private Space personal-side prose retrofit"
      pattern: "_glossary-android.md#private-space"
    - from: "docs/_glossary-android.md (COPE entry + WPCO entry)"
      to: "docs/admin-setup-android/08-cope-full-admin.md"
      via: "see-also forward-links per AECOPE-04"
      pattern: "08-cope-full-admin.md"
    - from: "docs/reference/android-capability-matrix.md (Private Space row footnote)"
      to: "docs/_glossary-android.md#private-space"
      via: "shared footnote per D-12"
      pattern: "_glossary-android.md#private-space"
    - from: "docs/android-lifecycle/03-android-version-matrix.md (line 30)"
      to: "#android-15-private-space-breakpoint"
      via: "anchor-link per D-11"
      pattern: "android-15-private-space-breakpoint"
---

<objective>
Land all 5 atomic same-commit retrofits in ONE commit per D-23 / D-32 / D-34: capability matrix COPE column + Private Space row (AECOPE-02 + D-12 + D-20), COBO migration-note γ3 sentence-scoped trim (AECOPE-03 + D-21), BYOD line 167 sentence retrofit (D-10), glossary Private Space H3 + alphabetical index + COPE/WPCO see-also (AECOPE-04 + D-09), version-matrix Private Space breakpoint H3 + line 30 anchor (D-11).

Purpose: Closes AECOPE-02, AECOPE-03, AECOPE-04 atomically. Eliminates 4 future drift surfaces (Pitfall 1) by routing all Private Space surfaces to the glossary canonical anchor. Closes the v1.4 COBO `§COPE Migration Note` forward-promise (deferred-to-v1.4.1 sentence replaced with forward-link). Establishes the 3 anchors (`#private-space` in glossary + `#android-15-private-space-breakpoint` in version-matrix + Private Space row footnote in capability matrix) that Wave 1's `08-cope-full-admin.md` already forward-links to.

**Task structure (per checker revision Issue 4):** This plan splits the 5 file retrofits into 5 sequential auto tasks (Tasks 02-01 through 02-05), each owning ONE file with verbatim Replacement/Insertion content + acceptance criteria. A 6th task (Task 02-06) performs the atomic git commit + audit-harness verification. Wave 2 still ships as ONE atomic commit per D-23 / D-34.

Output: 5 file edits + ONE atomic git commit. Audit harness 8/8 PASS post-commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-cope-full-admin/46-CONTEXT.md
@.planning/phases/46-cope-full-admin/46-RESEARCH.md
@.planning/phases/46-cope-full-admin/46-VALIDATION.md
@.planning/phases/46-cope-full-admin/46-01-SUMMARY.md
@docs/admin-setup-android/03-fully-managed-cobo.md
@docs/admin-setup-android/04-byod-work-profile.md
@docs/_glossary-android.md
@docs/reference/android-capability-matrix.md
@docs/android-lifecycle/03-android-version-matrix.md

<interfaces>
<!-- Exact line numbers and surrounding context for each retrofit. Executor uses these directly. -->

**COBO `03-fully-managed-cobo.md` lines 57-66 (PRE-EDIT):**
```
57: <a id="cope-migration"></a>
58: ## COPE Migration Note
59: (blank)
60: If you have existing COPE (Corporate-Owned, Personally-Enabled) deployments, read this before creating a new COBO profile.
61: (blank)
62: **Google recommends WPCO (Work Profile on Corporate-Owned).** WPCO is the forward-looking Google terminology for the work-profile-on-company-owned-device pattern that replaced the original COPE experience beginning on Android 11. COPE remains functionally supported on Android 10+ fleets, and Google has not issued a formal deprecation notice. However, NFC and `afw#setup` lost their COPE path on Android 11+. COBO (Fully Managed) is a distinct enrollment mode and is not affected by the COPE method removals. For method-by-method version support, see [02-provisioning-methods.md](../android-lifecycle/02-provisioning-methods.md).
63: (blank)
64: The full COPE admin path (separate from COBO) is deferred to v1.4.1; see `.planning/PROJECT.md` deferred items. For net-new corporate-with-work-profile deployments, provision WPCO; for existing COPE fleets, continue running them until your v1.4.1 migration is in place.
65: (blank)
66: <!-- MEDIUM confidence: locked phrasing above paraphrases Google's technical direction (Android 11 removal of work-profile-on-fully-managed in favor of work-profile-on-company-owned) per _glossary-android.md and Bayton's Android 11 COPE changes article. No current Google AE Help source uses the verbatim phrase; none contradict. last_verified: 2026-04-21. See [_glossary-android.md#cope](../_glossary-android.md#cope), [_glossary-android.md#wpco](../_glossary-android.md#wpco). -->
```

**Lines that MUST stay verbatim (PRESERVE):** 57, 58, 59, 60, 61, 62, 63, 65, 66
**Line that gets replaced (line 64 ONLY):** the full sentence from `The full COPE admin path` through `migration is in place.`

**D-21 LOCKED replacement for line 64 (verbatim):**
```
The full COPE admin path (separate from COBO) is documented in [08-cope-full-admin.md](08-cope-full-admin.md). For net-new corporate-with-work-profile deployments, provision WPCO per [_glossary-android.md#wpco](../_glossary-android.md#wpco); for existing COPE fleets, see [08-cope-full-admin.md](08-cope-full-admin.md) for full-admin coverage including profile creation, token lifecycle, and Android 8-15 version breakpoints.
```

---

**BYOD `04-byod-work-profile.md` line 167 (PRE-EDIT):**
```
167: Android 15 introduced "private space" as a personal-side feature; Intune does not manage private space content or visibility, consistent with personal-side isolation. [HIGH: MS Learn setup-personal-work-profile Limitations, last_verified 2026-04-22]
```

**D-10 LOCKED replacement for line 167 (verbatim):**
```
Android 15 introduced [Private Space](../_glossary-android.md#private-space) as a personal-side feature on the work-profile partition; Intune cannot manage it. See [version matrix breakpoint](../android-lifecycle/03-android-version-matrix.md#android-15-private-space-breakpoint) for the cross-mode behavior.
```

**Lines 166 and 168+ MUST stay verbatim (PRESERVE).**

---

**Glossary `_glossary-android.md` line 15 (PRE-EDIT):**
```
15: [afw#setup](#afw-setup) | [AMAPI](#amapi) | [BYOD](#byod) | [COBO](#cobo) | [COPE](#cope) | [Corporate Identifiers](#corporate-identifiers) | [Dedicated](#dedicated) | [DPC](#dpc) | [EMM](#emm) | [Entra Shared Device Mode](#entra-shared-device-mode) | [Fully Managed](#fully-managed) | [Knox](#knox) | [KME](#kme) | [KPE](#kpe) | [Managed Google Play](#managed-google-play) | [Managed Home Screen](#managed-home-screen) | [OEMConfig](#oemconfig) | [Play Integrity](#play-integrity) | [Supervision](#supervision) | [User Enrollment](#user-enrollment) | [Work Profile](#work-profile) | [WPCO](#wpco) | [Zero-Touch Enrollment](#zero-touch-enrollment)
```

**D-09 LOCKED line 15 replacement (insert `[Private Space](#private-space) | ` between `Play Integrity` and `Supervision`):**
```
[afw#setup](#afw-setup) | [AMAPI](#amapi) | [BYOD](#byod) | [COBO](#cobo) | [COPE](#cope) | [Corporate Identifiers](#corporate-identifiers) | [Dedicated](#dedicated) | [DPC](#dpc) | [EMM](#emm) | [Entra Shared Device Mode](#entra-shared-device-mode) | [Fully Managed](#fully-managed) | [Knox](#knox) | [KME](#kme) | [KPE](#kpe) | [Managed Google Play](#managed-google-play) | [Managed Home Screen](#managed-home-screen) | [OEMConfig](#oemconfig) | [Play Integrity](#play-integrity) | [Private Space](#private-space) | [Supervision](#supervision) | [User Enrollment](#user-enrollment) | [Work Profile](#work-profile) | [WPCO](#wpco) | [Zero-Touch Enrollment](#zero-touch-enrollment)
```

**Glossary `## Ownership & Management Scope` H2 entries (current order):**
- `### COBO` (line 41)
- `### COPE` (line 47)
- `### Dedicated` (line 53)
- `### Fully Managed` (line 59)
- `### Supervision` (line 65)
- `### Work Profile` (line 69)
- `### WPCO` (line 75)

**D-09 LOCKED insertion: new `### Private Space` H3 alphabetically between `### Fully Managed` and `### Supervision`** (after line 63 cross-platform note; before line 65 `### Supervision`).

**D-09 + D-33 LOCKED Private Space H3 body (RESEARCH Open Question #2 / Pitfall 8 Option A — Intune-scoped, NOT absolute):**
```markdown
### Private Space

Private Space is an Android 15+ personal-side feature that creates a user-controlled, hidden profile partition on the device. It exists alongside the work profile but on the personal side; **Intune** cannot manage Private Space content, visibility, or settings on COPE, COBO, BYOD, or any other Android Enterprise mode. Admins should treat Private Space as out-of-scope for Intune MDM governance and adjust user-facing privacy messaging accordingly. Applies to Android 15.0+. [HIGH: MS Learn setup-corporate-work-profile Limitations + Bayton Private Space + Google AE Help, last_verified: <execute-date>]

> **Cross-platform note:** No iOS, macOS, or Windows equivalent — the "user-controlled hidden personal-side partition that admin policies cannot reach" model is Android 15-specific. Apple's analog "Hidden" album / Locked Notes are app-level features without OS-partition status.

> **EMM-tier nuance:** AMAPI-native EMMs may apply application allowlist/blocklist policies that originate at the parent profile within Private Space on COPE deployments per Google AMAPI behavior; this is outside Intune's customDPC management surface. [MEDIUM: Bayton Private Space article, last_verified: <execute-date>]
```

**Glossary COPE entry (lines 47-51) MUST be retrofitted with see-also forward-link to `08-cope-full-admin.md` per AECOPE-04:**
- Pre-edit line 51 ends after the cross-platform note paragraph (currently line 51).
- Post-edit: ADD a new line at the end of the cross-platform note: `> See also: [Corporate-Owned Work Profile (COPE / WPCO) Admin Setup](admin-setup-android/08-cope-full-admin.md).`
- Same retrofit applied to WPCO entry (lines 75-79): ADD same see-also after the cross-platform note line 79.

---

**Version matrix `03-android-version-matrix.md` line 30 (PRE-EDIT):**
```
30: | BYOD Work Profile | Android 5.0 (practical: Android 8+) | Android 9: built-in QR reader; Android 12: IMEI/serial removed from corporate identifiers; Android 15: Private Space unsupported |
```

**D-11 LOCKED line 30 replacement (anchor-link the Private Space reference):**
```
| BYOD Work Profile | Android 5.0 (practical: Android 8+) | Android 9: built-in QR reader; Android 12: IMEI/serial removed from corporate identifiers; [Android 15: Private Space unsupported](#android-15-private-space-breakpoint) |
```

**D-11 LOCKED new H3 placement: after `<a id="android-15-breakpoint"></a> ### Android 15 — Factory Reset Protection Hardening` H3 block (current lines 67-79). Insert NEW H3 between line 79 (end of FRP H3 References) and line 81 (`## Non-Version Breakpoints`).**

**D-11 LOCKED new H3 body:**
```markdown
<a id="android-15-private-space-breakpoint"></a>
### Android 15 — Private Space (Personal-Side, Unmanageable)

**Affected modes:** All modes — Private Space is a personal-side feature outside the Intune MDM management surface (informational, not an admin lever).

**What changed:** On Android 15, Google introduced **Private Space**, a user-controlled hidden profile partition on the personal side of the device. Users can hide apps, accounts, and data inside Private Space; the partition is locked behind a separate authentication factor and is invisible to OS-level inventory queries. Private Space coexists with the work profile on COPE / WPCO and BYOD modes but lives on the personal side of the profile boundary, not within the work-profile container.

**Admin action required:** None — Private Space is an OS-level personal-side feature without an admin policy lever in Intune. **Intune** cannot manage Private Space content, visibility, or installed apps on COPE, COBO, BYOD, or any other Android Enterprise mode. Admins should adjust user-facing privacy messaging to acknowledge that Private Space content is outside IT visibility regardless of enrollment mode. (Applies to Android 15.0+.)

> **EMM-tier nuance:** AMAPI-native EMMs may apply application allowlist/blocklist policies that originate at the parent profile within Private Space on COPE deployments per Google AMAPI behavior; this is outside Intune's customDPC management surface. [MEDIUM: Bayton Private Space article, last_verified: <execute-date>]

**References:**
- [Microsoft Learn — Set up Android Enterprise corporate-owned work profile (Limitations)](https://learn.microsoft.com/en-us/intune/device-enrollment/android/setup-corporate-work-profile)
- [Bayton — What is Android 15 Private Space?](https://bayton.org/android/what-is-android-15-private-space/)
- [_glossary-android.md#private-space](../_glossary-android.md#private-space) — glossary canonical entry
```

---

**Capability matrix `android-capability-matrix.md` STRUCTURE:**

5 H2 sub-tables, each currently 5-column (COBO | BYOD | Dedicated | ZTE | AOSP):
- `## Enrollment` (line 13) — table at lines 15-25
- `## Configuration` (line 27) — table at lines 29-38
- `## App Deployment` (line 40) — table at lines 42-50
- `## Compliance` (line 52) — table at lines 54-61
- `## Software Updates` (line 63) — table at lines 65-70

Cross-Platform Equivalences section (lines 72-88) — DO NOT MODIFY per D-22 / SC#4. **Verify zero diff in this section's content post-edit using the section-extracting `diff <(sed ...) <(sed ...)` pattern (NOT a fragile `grep -A100 | wc -l` which produces false positives from line-shift hunk headers).**

**D-20 LOCKED column insertion: new column `COPE (WPCO / Corp-Owned Work Profile)` at index 1 (between COBO and BYOD).** New column order across all 5 sub-tables:
```
| Feature | COBO (Fully Managed) | COPE (WPCO / Corp-Owned Work Profile) | BYOD (Work Profile) | Dedicated (COSU) | ZTE (Zero-Touch) | AOSP |
```

**D-12 LOCKED Private Space row added across ALL Android columns** as new last row of `## Enrollment` H2 sub-table (recommended placement; planner may pick another sub-table if more semantically natural). Row content:
```
| Android 15 Private Space | N/A¹ | N/A¹ | N/A¹ | N/A¹ | N/A¹ | N/A¹ |
```

Footnote (added at end of `## Enrollment` H2 sub-table block, before `## Configuration` H2):
```
¹ Personal-side feature outside Intune management surface across all modes; see [glossary](../_glossary-android.md#private-space) and [version matrix breakpoint](../android-lifecycle/03-android-version-matrix.md#android-15-private-space-breakpoint).
```
</interfaces>

<verbatim_replacements>
<!-- All verbatim replacement strings the executor MUST use byte-identically. -->

### Replacement R1: COBO line 64 (γ3 sentence-scoped trim per D-21)

**old_string (the entire current line 64):**
```
The full COPE admin path (separate from COBO) is deferred to v1.4.1; see `.planning/PROJECT.md` deferred items. For net-new corporate-with-work-profile deployments, provision WPCO; for existing COPE fleets, continue running them until your v1.4.1 migration is in place.
```

**new_string (D-21 LOCKED verbatim):**
```
The full COPE admin path (separate from COBO) is documented in [08-cope-full-admin.md](08-cope-full-admin.md). For net-new corporate-with-work-profile deployments, provision WPCO per [_glossary-android.md#wpco](../_glossary-android.md#wpco); for existing COPE fleets, see [08-cope-full-admin.md](08-cope-full-admin.md) for full-admin coverage including profile creation, token lifecycle, and Android 8-15 version breakpoints.
```

### Replacement R2: BYOD line 167 (sentence-scoped retrofit per D-10)

**old_string:**
```
Android 15 introduced "private space" as a personal-side feature; Intune does not manage private space content or visibility, consistent with personal-side isolation. [HIGH: MS Learn setup-personal-work-profile Limitations, last_verified 2026-04-22]
```

**new_string (D-10 LOCKED verbatim):**
```
Android 15 introduced [Private Space](../_glossary-android.md#private-space) as a personal-side feature on the work-profile partition; Intune cannot manage it. See [version matrix breakpoint](../android-lifecycle/03-android-version-matrix.md#android-15-private-space-breakpoint) for the cross-mode behavior.
```

### Replacement R3: Glossary line 15 (alphabetical index update per D-09)

**old_string:**
```
[afw#setup](#afw-setup) | [AMAPI](#amapi) | [BYOD](#byod) | [COBO](#cobo) | [COPE](#cope) | [Corporate Identifiers](#corporate-identifiers) | [Dedicated](#dedicated) | [DPC](#dpc) | [EMM](#emm) | [Entra Shared Device Mode](#entra-shared-device-mode) | [Fully Managed](#fully-managed) | [Knox](#knox) | [KME](#kme) | [KPE](#kpe) | [Managed Google Play](#managed-google-play) | [Managed Home Screen](#managed-home-screen) | [OEMConfig](#oemconfig) | [Play Integrity](#play-integrity) | [Supervision](#supervision) | [User Enrollment](#user-enrollment) | [Work Profile](#work-profile) | [WPCO](#wpco) | [Zero-Touch Enrollment](#zero-touch-enrollment)
```

**new_string (insert `[Private Space](#private-space) | ` between Play Integrity and Supervision):**
```
[afw#setup](#afw-setup) | [AMAPI](#amapi) | [BYOD](#byod) | [COBO](#cobo) | [COPE](#cope) | [Corporate Identifiers](#corporate-identifiers) | [Dedicated](#dedicated) | [DPC](#dpc) | [EMM](#emm) | [Entra Shared Device Mode](#entra-shared-device-mode) | [Fully Managed](#fully-managed) | [Knox](#knox) | [KME](#kme) | [KPE](#kpe) | [Managed Google Play](#managed-google-play) | [Managed Home Screen](#managed-home-screen) | [OEMConfig](#oemconfig) | [Play Integrity](#play-integrity) | [Private Space](#private-space) | [Supervision](#supervision) | [User Enrollment](#user-enrollment) | [Work Profile](#work-profile) | [WPCO](#wpco) | [Zero-Touch Enrollment](#zero-touch-enrollment)
```

### Replacement R4: Version matrix line 30 (BYOD-row anchor link per D-11)

**old_string:**
```
| BYOD Work Profile | Android 5.0 (practical: Android 8+) | Android 9: built-in QR reader; Android 12: IMEI/serial removed from corporate identifiers; Android 15: Private Space unsupported |
```

**new_string (D-11 LOCKED verbatim):**
```
| BYOD Work Profile | Android 5.0 (practical: Android 8+) | Android 9: built-in QR reader; Android 12: IMEI/serial removed from corporate identifiers; [Android 15: Private Space unsupported](#android-15-private-space-breakpoint) |
```

### Insertion I1: Glossary new `### Private Space` H3 (D-09 + D-33 / Pitfall 8 Option A)

INSERT after `### Fully Managed` H3 block ends (after the existing cross-platform note paragraph that closes the Fully Managed entry, currently around line 63) and BEFORE `### Supervision` H3 (currently line 65).

(Insertion content: see `<interfaces>` section "D-09 + D-33 LOCKED Private Space H3 body" above.)

### Insertion I2: Glossary COPE entry see-also (AECOPE-04)

INSERT after the COPE entry's cross-platform note (currently the last line of the COPE entry block ending at line 51), as a new blockquote line:
```
> See also: [Corporate-Owned Work Profile (COPE / WPCO) Admin Setup](admin-setup-android/08-cope-full-admin.md).
```

### Insertion I3: Glossary WPCO entry see-also (AECOPE-04)

INSERT after the WPCO entry's cross-platform note (currently the last line of the WPCO entry block ending at line 79), as a new blockquote line:
```
> See also: [Corporate-Owned Work Profile (COPE / WPCO) Admin Setup](admin-setup-android/08-cope-full-admin.md).
```

### Insertion I4: Version matrix new H3 (D-11)

INSERT after the closing line of the `### Android 15 — Factory Reset Protection Hardening` H3 References block (currently line 79) and BEFORE `## Non-Version Breakpoints` H2 (currently line 81).

(Insertion content: see `<interfaces>` section "D-11 LOCKED new H3 body" above.)

### Insertions I5-I9: Capability matrix COPE column across 5 H2 sub-tables (D-20)

For EACH of the 5 H2 sub-tables (`## Enrollment`, `## Configuration`, `## App Deployment`, `## Compliance`, `## Software Updates`):

1. **Header row:** insert new `COPE (WPCO / Corp-Owned Work Profile)` column header at index 1 (between `COBO (Fully Managed)` and `BYOD (Work Profile)`).
2. **Separator row:** add `|----------------------|` at the corresponding column position.
3. **Each data row:** insert a COPE-cell at index 1.

**COPE cell content per sub-table (HIGH-confidence from MS Learn `setup-corporate-work-profile.md` + `ref-corporate-methods.md` per RESEARCH §Standard Stack):**

#### Enrollment H2 sub-table COPE cells:
- Tri-portal admin surface required: `Intune + [MGP](../admin-setup-android/01-managed-google-play.md)` (no ZT portal required at this row baseline; ZT is method-specific)
- Ownership model: `Corporate-owned (work profile separated)`
- Provisioning methods: `QR / NFC (Android 8-10) / afw#setup (Android 8-10) / Token (Android 8-10) / Zero-Touch — see [08-cope-full-admin.md#provisioning-method-choice](../admin-setup-android/08-cope-full-admin.md#provisioning-method-choice)`
- Hardware identity / token model: `Enrollment token (default + staging up to 65 years) — see [08-cope-full-admin.md#enrollment-token](../admin-setup-android/08-cope-full-admin.md#enrollment-token)`
- Minimum Android version: `Android 8.0 — see [08-cope-full-admin.md#prerequisites](../admin-setup-android/08-cope-full-admin.md#prerequisites)`
- User affinity / userless support: `User-affinity required (work-profile attached to user identity)`
- DPC identity on device: `Android Device Policy (work-profile DPC + device-owner DPC)`
- Entra join / Shared Device Mode: `Entra-joined at enrollment (Chrome Custom Tab sign-in)`
- Factory Reset Protection (Android 15): `EFRP enforced; Android 15 Settings-app reset requires re-supplying Google account — see [08-cope-full-admin.md#android-15-frp](../admin-setup-android/08-cope-full-admin.md#android-15-frp)`

#### Configuration H2 sub-table COPE cells:
- Settings Catalog applicability: `Yes (work-profile scope + device-owner-side restrictions)`
- Restriction profile breadth: `Work-profile + device-owner restrictions (broader than BYOD; narrower than COBO — no personal-side reach)`
- Configuration Profiles vs OMA-URI (post-AMAPI): `Configuration Profiles + custom profiles (work-profile scope)`
- Policy Sets support: `Yes (work-profile-scoped)`
- App-config (Managed Configurations) targeting: `Yes (work-profile-targeted)`
- Per-mode restriction count / scope: `Work-profile keys + device-owner-side restrictions (between BYOD and COBO breadth)`
- Wi-Fi profile authentication: `Username/password or certificate (work-profile scope)`
- DDM (Declarative Device Management): `DDM: N/A (Android uses Play-integrated policy channel instead)`

#### App Deployment H2 sub-table COPE cells:
- Managed app channel: `[Managed Google Play](../_glossary-android.md#managed-google-play) (mandatory, work-profile-scoped)`
- Silent install (no user prompt): `Yes for MGP work-profile apps`
- LOB APK delivery path: `Private app via MGP (work-profile-scoped)`
- Managed Home Screen (MHS): `N/A`
- App-config (Managed Configurations): `Yes (work-profile-scoped)`
- Work-profile app isolation: `Yes — kernel-level container between personal and work apps on a corporate-owned device`
- AMAPI migration footnote: `Unaffected (AMAPI April 2025 affected BYOD specifically; COPE/WPCO unaffected)`

#### Compliance H2 sub-table COPE cells:
- Play Integrity verdict levels: `All three: Basic / Basic + Device / Strong integrity`
- Conditional Access attestation requirement: `Full Play Integrity on work-profile`
- Device-based restriction enforcement: `Work-profile + device-owner-side enforcement`
- Password / passcode policy enforcement: `Work-profile challenge + device PIN (corporate-owned device-owner side)`
- Default compliance posture (newly enrolled, not-yet-evaluated): `"Not evaluated" — CA grace period applies`
- L2 investigation runbook: `See [21-android-compliance-investigation.md](../l2-runbooks/21-android-compliance-investigation.md)`

#### Software Updates H2 sub-table COPE cells:
- System update policy support: `Yes — admin-defined update posture (Default / Automatic / Postpone up to 30 days / Windowed) on the device-owner side`
- Deferral window capabilities: `🔒 device-owner-side — forced deferral up to 30 days`
- Play Store app-update control: `Yes — per-app auto-update posture via MGP (work-profile apps)`
- OEM update enforcement: `🔒 device-owner-side — Android OS update posture respected by OEMs in managed mode`

#### Private Space row (D-12) — add at end of `## Enrollment` H2 sub-table (after the FRP row):
```
| Android 15 Private Space | N/A¹ | N/A¹ | N/A¹ | N/A¹ | N/A¹ | N/A¹ |
```

#### Footnote (added immediately after `## Enrollment` H2 sub-table closes, before `## Configuration` H2 starts):
```
¹ Personal-side feature outside Intune management surface across all modes; see [glossary](../_glossary-android.md#private-space) and [version matrix breakpoint](../android-lifecycle/03-android-version-matrix.md#android-15-private-space-breakpoint).
```
</verbatim_replacements>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 02-01: COBO migration-note retrofit (Replacement R1, line 64 sentence-scoped trim per D-21 / AECOPE-03)</name>

  <read_first>
    - .planning/phases/46-cope-full-admin/46-01-SUMMARY.md (Wave 1 plan summary — confirms `08-cope-full-admin.md` lives at the path used in Replacement R1 forward-link)
    - docs/admin-setup-android/08-cope-full-admin.md (Wave 1 output — confirms the file path target of the forward-link exists)
    - docs/admin-setup-android/03-fully-managed-cobo.md (lines 57-66 — BEFORE editing, verify line 64 EXACT text matches Replacement R1 old_string byte-for-byte; verify line 66 HTML comment ends with `last_verified: 2026-04-21`)
    - .planning/phases/46-cope-full-admin/46-CONTEXT.md (D-21 + D-23 LOCKED text; D-26 freshness rule)
    - .planning/phases/46-cope-full-admin/46-RESEARCH.md (Pitfall 4 sentence-scoped trim discipline)
  </read_first>

  <files>docs/admin-setup-android/03-fully-managed-cobo.md</files>

  <action>
Apply Replacement R1 from `<verbatim_replacements>` exactly. Use the Edit tool (or equivalent surgical replacement) with the EXACT old_string (the full current line 64) and the EXACT new_string (D-21 LOCKED verbatim text).

Do NOT touch lines 57, 58, 59, 60, 61, 62, 63, 65, or 66. Pitfall 4 lock: line 66 HTML comment with `last_verified: 2026-04-21` is LOAD-BEARING source attribution and MUST be byte-identical post-edit.

Update the file's frontmatter `last_verified` from `2026-04-25` to `<execute-time-date>` and `review_by` to `<execute-time-date + 60d>` to refresh the 60-day window per D-26.

DO NOT git add or git commit in this task — staging + commit happens in Task 02-06 atomically.
  </action>

  <acceptance_criteria>
    - `grep -F '08-cope-full-admin.md' docs/admin-setup-android/03-fully-managed-cobo.md` returns ≥2 hits (D-21 replacement contains 2 instances of the forward-link)
    - `grep -F 'last_verified: 2026-04-21' docs/admin-setup-android/03-fully-managed-cobo.md` returns 1 hit (HTML comment line 66 PRESERVED byte-identical)
    - `grep -F 'deferred to v1.4.1' docs/admin-setup-android/03-fully-managed-cobo.md` returns 0 hits (deferred-language sentence removed)
    - `git diff docs/admin-setup-android/03-fully-managed-cobo.md` shows changes ONLY in the line 64 region + frontmatter — no diff at lines 57, 58, 59, 60, 61, 62, 63, 65, 66
    - File frontmatter `last_verified` is updated to `<execute-time-date>` and `review_by` to `<execute-time-date + 60d>` per D-26
    - Banned-phrase grep (D-31) on this file post-edit shows ZERO new hits attached to COPE-mode introduced by this task: `grep -nE '\b(deprecated|end of life|EOL|no longer supported|obsolete|sunset|retired)\b' docs/admin-setup-android/03-fully-managed-cobo.md` returns only pre-existing factual / historical hits (if any)
  </acceptance_criteria>

  <verify>
    <automated>grep -F 'deferred to v1.4.1' docs/admin-setup-android/03-fully-managed-cobo.md; test $? -eq 1 && grep -c '08-cope-full-admin.md' docs/admin-setup-android/03-fully-managed-cobo.md</automated>
  </verify>

  <done>
COBO `03-fully-managed-cobo.md` line 64 sentence-scoped trim applied per D-21. Lines 57-63 + 65-66 byte-identical pre/post (T-46-03 mitigation). Frontmatter freshness updated per D-26. File staged for atomic commit in Task 02-06.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 02-02: BYOD line 167 retrofit (Replacement R2, sentence-scoped per D-10)</name>

  <read_first>
    - docs/admin-setup-android/04-byod-work-profile.md (lines 165-170 — BEFORE editing, verify line 167 EXACT text matches Replacement R2 old_string byte-for-byte; verify line 166 ends ".]" and line 168 starts blank then "What breaks" blockquote)
    - .planning/phases/46-cope-full-admin/46-CONTEXT.md (D-10 LOCKED text; D-26 freshness rule)
    - .planning/phases/46-cope-full-admin/46-RESEARCH.md (Pitfall 1 single-source-of-truth discipline; Pitfall 4 sentence-scoped trim discipline)
  </read_first>

  <files>docs/admin-setup-android/04-byod-work-profile.md</files>

  <action>
Apply Replacement R2 from `<verbatim_replacements>` exactly. Use the Edit tool with EXACT old_string from R2 (the current line 167 prose with `[HIGH: MS Learn setup-personal-work-profile Limitations, last_verified 2026-04-22]` marker) and EXACT new_string from D-10 LOCKED verbatim text.

Do NOT touch line 166 (end of privacy-boundary table) or line 168+ (`> **What breaks if privacy boundary is misrepresented:**` blockquote and below).

Update the file's frontmatter `last_verified` and `review_by` per D-26.

DO NOT git add or git commit in this task — staging + commit happens in Task 02-06 atomically.
  </action>

  <acceptance_criteria>
    - `grep -F '_glossary-android.md#private-space' docs/admin-setup-android/04-byod-work-profile.md` returns ≥1 hit
    - `grep -F 'android-15-private-space-breakpoint' docs/admin-setup-android/04-byod-work-profile.md` returns ≥1 hit
    - `grep -F 'consistent with personal-side isolation' docs/admin-setup-android/04-byod-work-profile.md` returns 0 hits (old prose removed)
    - `git diff docs/admin-setup-android/04-byod-work-profile.md` shows changes ONLY in line 167 region + frontmatter (T-46-04 mitigation: no collateral changes elsewhere in BYOD doc)
    - File frontmatter `last_verified` updated to `<execute-time-date>` and `review_by` to `<execute-time-date + 60d>` per D-26
  </acceptance_criteria>

  <verify>
    <automated>grep -F '_glossary-android.md#private-space' docs/admin-setup-android/04-byod-work-profile.md && grep -F 'consistent with personal-side isolation' docs/admin-setup-android/04-byod-work-profile.md; test $? -eq 1</automated>
  </verify>

  <done>
BYOD `04-byod-work-profile.md` line 167 sentence-scoped retrofit applied per D-10. Lines 166 + 168+ byte-identical pre/post. Frontmatter freshness updated per D-26. File staged for atomic commit in Task 02-06.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 02-03: Glossary updates (Insertions I1 + I2 + I3 + Replacement R3 — Private Space H3 + alphabetical index update + COPE/WPCO see-also forward-links per AECOPE-04 + D-09 + D-33)</name>

  <read_first>
    - docs/_glossary-android.md (lines 1-100 — BEFORE editing, verify line 15 alphabetical index EXACT text matches Replacement R3 old_string; verify Ownership & Management Scope H2 entries are in current alphabetical order COBO → COPE → Dedicated → Fully Managed → Supervision → Work Profile → WPCO)
    - .planning/phases/46-cope-full-admin/46-CONTEXT.md (D-09, D-33 LOCKED text — Pitfall 8 Option A scope-tightening; D-26 freshness rule)
    - .planning/phases/46-cope-full-admin/46-RESEARCH.md (Pitfall 8 Option A wording for D-09 — "Intune cannot manage..." NOT absolute "no admin policy lever in any mode")
  </read_first>

  <files>docs/_glossary-android.md</files>

  <action>
Apply 4 surgical edits to `docs/_glossary-android.md`:

**a. Replacement R3 — Line 15 alphabetical index update:**
Use Edit tool with R3 old_string and new_string (insert `[Private Space](#private-space) | ` between `[Play Integrity](#play-integrity) | ` and `[Supervision](#supervision) | `).

**b. Insertion I1 — New `### Private Space` H3 (D-09 + D-33 / Pitfall 8 Option A):**
Locate the END of the `### Fully Managed` H3 block — specifically the closing line of its `> **Cross-platform note:**` blockquote (at approximately line 63 — the line ending with `Do not conflate.`). Insert AFTER that line (before line 64 blank or before `### Supervision` H3 line 65) the new H3 block from `<interfaces>` "D-09 + D-33 LOCKED Private Space H3 body" verbatim, with `<execute-date>` substituted into the `last_verified` confidence markers in both the body paragraph and the EMM-tier nuance blockquote.

The Pitfall 8 Option A wording is MANDATORY: scope to "**Intune** cannot..." — do NOT use the absolute "no admin policy lever in any mode" phrasing.

The EMM-tier nuance blockquote acknowledging AMAPI behavior is MANDATORY per D-33.

**c. Insertion I2 — COPE entry see-also (AECOPE-04):**
Locate the closing `Do not conflate.` line of the COPE entry's cross-platform note (approximately line 51). Insert immediately after it: `> See also: [Corporate-Owned Work Profile (COPE / WPCO) Admin Setup](admin-setup-android/08-cope-full-admin.md).`

**d. Insertion I3 — WPCO entry see-also (AECOPE-04):**
Same pattern as I2 but for the WPCO entry's cross-platform note (approximately line 79).

**e. Update file frontmatter `last_verified` + `review_by` per D-26.**

DO NOT git add or git commit in this task — staging + commit happens in Task 02-06 atomically.
  </action>

  <acceptance_criteria>
    - `grep -F '[Private Space](#private-space)' docs/_glossary-android.md` returns ≥1 hit (alphabetical index line 15 updated per R3)
    - `grep -c '^### Private Space$' docs/_glossary-android.md` returns 1 (new H3 inserted per I1)
    - `grep -F '08-cope-full-admin.md' docs/_glossary-android.md` returns ≥2 hits (COPE entry see-also + WPCO entry see-also per I2 + I3)
    - The Private Space H3 body uses Pitfall 8 Option A / D-33 wording: `grep -F '**Intune** cannot manage Private Space content' docs/_glossary-android.md` returns 1 hit (NOT the absolute "no admin policy lever in any mode" phrasing)
    - The Private Space H3 body does NOT contain banned absolute claim: `grep -F 'no admin policy lever in COPE or any other mode' docs/_glossary-android.md` returns 0 hits
    - The Private Space H3 contains the EMM-tier nuance blockquote: `grep -F 'AMAPI-native EMMs may apply application allowlist/blocklist' docs/_glossary-android.md` returns 1 hit
    - Alphabetical placement of Private Space H3 verified: in `grep -n '^### ' docs/_glossary-android.md`, `### Private Space` line number falls strictly BETWEEN `### Fully Managed` line number and `### Supervision` line number
    - File frontmatter `last_verified` updated to `<execute-time-date>` and `review_by` to `<execute-time-date + 60d>` per D-26
  </acceptance_criteria>

  <verify>
    <automated>grep -c '^### Private Space$' docs/_glossary-android.md</automated>
  </verify>

  <done>
Glossary `_glossary-android.md` updated: line 15 alphabetical index includes Private Space; new `### Private Space` H3 inserted alphabetically with D-33 Option A wording + EMM-tier nuance blockquote; COPE + WPCO entries each carry see-also forward-link to `08-cope-full-admin.md`. Frontmatter freshness updated per D-26. File staged for atomic commit in Task 02-06.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 02-04: Version-matrix breakpoint H3 (Insertion I4 + Replacement R4 — Android 15 Private Space breakpoint H3 + line 30 anchor link per D-11)</name>

  <read_first>
    - docs/android-lifecycle/03-android-version-matrix.md (lines 25-85 — BEFORE editing, verify line 30 BYOD row EXACT text matches Replacement R4 old_string; verify the FRP H3 References block ends at the line before `## Non-Version Breakpoints` H2)
    - .planning/phases/46-cope-full-admin/46-CONTEXT.md (D-11 LOCKED text; D-26 freshness rule)
  </read_first>

  <files>docs/android-lifecycle/03-android-version-matrix.md</files>

  <action>
Apply 2 surgical edits to `docs/android-lifecycle/03-android-version-matrix.md`:

**a. Replacement R4 — Line 30 BYOD-row anchor-link update:**
Use Edit tool with R4 old_string and new_string (anchor-link the existing "Android 15: Private Space unsupported" text per D-11).

**b. Insertion I4 — New `### Android 15 — Private Space (Personal-Side, Unmanageable)` H3:**
Locate the END of the `### Android 15 — Factory Reset Protection Hardening` H3 block — specifically its References list closing line (approximately line 79, ending with `factory-reset-protection-emails-not-enforced)`). Insert AFTER that line (before line 80 blank, before line 81 `## Non-Version Breakpoints` H2) the new H3 block from `<interfaces>` "D-11 LOCKED new H3 body" verbatim, with `<execute-date>` substituted into the EMM-tier-nuance confidence marker.

**c. Update file frontmatter `last_verified` + `review_by` per D-26.**

DO NOT git add or git commit in this task — staging + commit happens in Task 02-06 atomically.
  </action>

  <acceptance_criteria>
    - `grep -c '^### Android 15 — Private Space' docs/android-lifecycle/03-android-version-matrix.md` returns 1 (new H3 inserted per I4)
    - `grep -F '<a id="android-15-private-space-breakpoint"></a>' docs/android-lifecycle/03-android-version-matrix.md` returns 1 (anchor present)
    - `grep -F '[Android 15: Private Space unsupported](#android-15-private-space-breakpoint)' docs/android-lifecycle/03-android-version-matrix.md` returns 1 hit (line 30 anchor-link added per R4)
    - The new H3 placement falls AFTER the existing FRP H3 and BEFORE `## Non-Version Breakpoints` H2: in `grep -n '^### Android 15\|^## Non-Version' docs/android-lifecycle/03-android-version-matrix.md`, the FRP H3 line < new Private Space H3 line < Non-Version Breakpoints H2 line
    - File frontmatter `last_verified` updated to `<execute-time-date>` and `review_by` to `<execute-time-date + 60d>` per D-26
  </acceptance_criteria>

  <verify>
    <automated>grep -c '^### Android 15 — Private Space' docs/android-lifecycle/03-android-version-matrix.md && grep -F '<a id="android-15-private-space-breakpoint"></a>' docs/android-lifecycle/03-android-version-matrix.md</automated>
  </verify>

  <done>
Version matrix `03-android-version-matrix.md` updated: line 30 BYOD row carries anchor link per R4; new Android 15 Private Space breakpoint H3 inserted between FRP H3 and `## Non-Version Breakpoints` H2 per I4. Frontmatter freshness updated per D-26. File staged for atomic commit in Task 02-06.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 02-05: Capability matrix COPE column + Private Space row (Insertions I5..I9 — column at index 1 in 5 sub-tables + Private Space row across all modes with footnote per AECOPE-02 + D-12 + D-20)</name>

  <read_first>
    - docs/reference/android-capability-matrix.md (all 144 lines — BEFORE editing, verify the 5 H2 sub-tables exist at lines 13/27/40/52/63 with current 5-col `COBO | BYOD | Dedicated | ZTE | AOSP` shape; verify Cross-Platform Equivalences H2 lives at lines 72-88 and is the SECTION that MUST NOT change)
    - .planning/phases/46-cope-full-admin/46-CONTEXT.md (D-12, D-20, D-22 LOCKED text)
    - .planning/phases/46-cope-full-admin/46-RESEARCH.md (Pitfall 5 atomic same-commit discipline; Standard Stack confirms COPE cell content)
  </read_first>

  <files>docs/reference/android-capability-matrix.md</files>

  <action>
This is the most complex single-file edit in Plan 02. Use the Edit tool sequentially for each H2 sub-table — DO NOT use a single bulk replacement.

For EACH of the 5 H2 sub-tables (`## Enrollment` line 13, `## Configuration` line 27, `## App Deployment` line 40, `## Compliance` line 52, `## Software Updates` line 63):

**a. Header row update:** Edit the header row to insert new column `COPE (WPCO / Corp-Owned Work Profile)` at index 1 (between `COBO (Fully Managed)` and `BYOD (Work Profile)`). Resulting header:
```
| Feature | COBO (Fully Managed) | COPE (WPCO / Corp-Owned Work Profile) | BYOD (Work Profile) | Dedicated (COSU) | ZTE (Zero-Touch) | AOSP |
```

**b. Separator row update:** Edit the separator row to add the additional `|----------------------|` segment at column index 2.

**c. Each data row update:** For every data row in the sub-table, insert the per-row COPE cell content from `<verbatim_replacements>` Insertions I5-I9. Use the per-sub-table cell-content lists EXACTLY.

After completing all 5 sub-tables:

**d. Insert Private Space row (D-12) at end of `## Enrollment` H2 sub-table** (after the existing FRP row, line 25 pre-edit):
```
| Android 15 Private Space | N/A¹ | N/A¹ | N/A¹ | N/A¹ | N/A¹ | N/A¹ |
```

**e. Insert footnote** immediately after the `## Enrollment` H2 sub-table closes (before the `## Configuration` H2):
```
¹ Personal-side feature outside Intune management surface across all modes; see [glossary](../_glossary-android.md#private-space) and [version matrix breakpoint](../android-lifecycle/03-android-version-matrix.md#android-15-private-space-breakpoint).
```

**f. Verify Cross-Platform Equivalences section UNTOUCHED (T-46-02 mitigation, per D-22 / SC#4) — section-extracting diff (NOT fragile grep wc -l):**

Run the following section-extracting diff command (per checker revision Issue 5 — replaces the fragile `git diff | grep -A100 | wc -l` pattern that produced false positives from line-shift hunk headers):

```bash
diff <(sed -n '/^## Cross-Platform Equivalences/,/^## /p' <(git show HEAD:docs/reference/android-capability-matrix.md)) <(sed -n '/^## Cross-Platform Equivalences/,/^## /p' docs/reference/android-capability-matrix.md)
```

Expected output: ZERO diff lines (Cross-Platform Equivalences section content byte-identical pre-/post-retrofit). If the diff shows any changes in this range, REVERT those changes before proceeding to Task 02-06.

**g. Update file frontmatter `last_verified` + `review_by` per D-26.**

DO NOT git add or git commit in this task — staging + commit happens in Task 02-06 atomically.
  </action>

  <acceptance_criteria>
    - `grep -c 'COPE (WPCO / Corp-Owned Work Profile)' docs/reference/android-capability-matrix.md` returns ≥5 (one per H2 sub-table header)
    - All 5 sub-tables show 6-col header order COBO → COPE → BYOD → Dedicated → ZTE → AOSP: `grep -E '\| COBO .* \| COPE .* \| BYOD' docs/reference/android-capability-matrix.md` returns ≥5 hits
    - Private Space row added: `grep -F '| Android 15 Private Space | N/A¹' docs/reference/android-capability-matrix.md` returns 1 hit
    - Footnote present: `grep -F '¹ Personal-side feature outside Intune management surface' docs/reference/android-capability-matrix.md` returns 1 hit
    - **T-46-02 mitigation (Cross-Platform Equivalences UNCHANGED per SC#4 / D-22) — section-extracting diff (fix per checker Issue 5):**
      ```
      diff <(sed -n '/^## Cross-Platform Equivalences/,/^## /p' <(git show HEAD:docs/reference/android-capability-matrix.md)) <(sed -n '/^## Cross-Platform Equivalences/,/^## /p' docs/reference/android-capability-matrix.md)
      ```
      Expected output: empty (zero changed lines in Cross-Platform Equivalences section). The fragile `git diff HEAD~1 ... | grep -A100 'Cross-Platform Equivalences' | grep -E '^[+-]' | wc -l` command is REPLACED with this section-extracting `diff <(sed ...) <(sed ...)` pattern per checker revision Issue 5.
    - File frontmatter `last_verified` updated to `<execute-time-date>` and `review_by` to `<execute-time-date + 60d>` per D-26
  </acceptance_criteria>

  <verify>
    <automated>grep -c 'COPE (WPCO / Corp-Owned Work Profile)' docs/reference/android-capability-matrix.md && diff <(sed -n '/^## Cross-Platform Equivalences/,/^## /p' <(git show HEAD:docs/reference/android-capability-matrix.md)) <(sed -n '/^## Cross-Platform Equivalences/,/^## /p' docs/reference/android-capability-matrix.md)</automated>
  </verify>

  <done>
Capability matrix `android-capability-matrix.md` updated: COPE column inserted at index 1 across all 5 H2 sub-tables (D-20); Private Space row added across all 6 columns in Enrollment sub-table with footnote (D-12); Cross-Platform Equivalences section content byte-identical (T-46-02 / D-22 / SC#4) verified via section-extracting diff (per checker Issue 5 fix). Frontmatter freshness updated per D-26. File staged for atomic commit in Task 02-06.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 02-06: Atomic same-commit (git stage all 5 files + ONE commit + audit-harness verification + allow-list helper per D-23 / D-34)</name>

  <read_first>
    - .planning/phases/46-cope-full-admin/46-CONTEXT.md (D-23 atomic same-commit; D-34 atomic-bundle-within-Wave-2 interpretation lock; D-29 append-only contract; D-31 banned-phrase discipline)
    - .planning/phases/46-cope-full-admin/46-RESEARCH.md (Pitfall 5 — NEVER commit subsets, even "almost done" partial commits violate D-23 atomicity)
    - Tasks 02-01 through 02-05 must be COMPLETE — verify all 5 files have unstaged modifications via `git status`
  </read_first>

  <files>
    docs/admin-setup-android/03-fully-managed-cobo.md,
    docs/admin-setup-android/04-byod-work-profile.md,
    docs/_glossary-android.md,
    docs/android-lifecycle/03-android-version-matrix.md,
    docs/reference/android-capability-matrix.md
  </files>

  <action>
This task performs the atomic git commit + post-commit audit verification per D-23 / D-34 (the Wave 2 atomic-bundle interpretation lock).

**Step 1 — Verify all 5 files have unstaged changes from Tasks 02-01..02-05:**

```bash
git status docs/admin-setup-android/03-fully-managed-cobo.md docs/admin-setup-android/04-byod-work-profile.md docs/_glossary-android.md docs/android-lifecycle/03-android-version-matrix.md docs/reference/android-capability-matrix.md
```

Expected: 5 files modified. If any file shows "no changes," it means a previous task did not complete — STOP and report the gap (do NOT proceed to commit).

**Step 2 — Banned-phrase compliance grep (D-31) across all 5 edited files BEFORE staging:**

```bash
grep -nE '\b(deprecated|end of life|EOL|no longer supported|obsolete|sunset|retired)\b' docs/admin-setup-android/03-fully-managed-cobo.md docs/admin-setup-android/04-byod-work-profile.md docs/_glossary-android.md docs/android-lifecycle/03-android-version-matrix.md docs/reference/android-capability-matrix.md
```

Existing pre-Phase-46 hits (e.g., glossary line 145 "Microsoft Store for Business deprecated" Windows-context, glossary line 179 "NOT formally deprecated COPE" corpus-canonical posture, BYOD line 96 AMAPI April 2025 migration historical, version-matrix SafetyNet "turned off January 2025") are PRESERVED — those are factual cross-platform / historical references, not banned-phrase violations. ANY NEW hit attached to COPE-mode introduced by this plan's edits → REVERT and reframe per D-17 positive-framing rule before commit.

**Step 3 — Section-extracting diff verification for Cross-Platform Equivalences (per checker Issue 5):**

```bash
diff <(sed -n '/^## Cross-Platform Equivalences/,/^## /p' <(git show HEAD:docs/reference/android-capability-matrix.md)) <(sed -n '/^## Cross-Platform Equivalences/,/^## /p' docs/reference/android-capability-matrix.md)
```

Expected: empty output (zero changed lines in Cross-Platform Equivalences section). If diff shows changes, REVERT capability-matrix changes in that section before commit.

**Step 4 — Stage all 5 files atomically:**

```bash
git add docs/admin-setup-android/03-fully-managed-cobo.md docs/admin-setup-android/04-byod-work-profile.md docs/_glossary-android.md docs/android-lifecycle/03-android-version-matrix.md docs/reference/android-capability-matrix.md
```

Verify ALL 5 files staged via `git diff --cached --stat`; expect 5 file entries. If any file missing, `git add` it before committing.

**Step 5 — Create ONE atomic commit per D-23 / D-34:**

```bash
git commit -m "feat(46): COPE full admin Wave 2 atomic retrofit (capability matrix + COBO migration trim + glossary + BYOD line 167 + version-matrix breakpoint)"
```

Or per D-23 atomic-commit precedent + Phase 45 Plan 10 commit 3400bff template:
```
docs(46-02): atomic Wave 2 retrofits — capability matrix COPE column + Private Space row + COBO migration trim + BYOD retrofit + glossary Private Space H3 + version-matrix breakpoint H3 (AECOPE-02 + AECOPE-03 + AECOPE-04 + D-10 + D-11)
```

DO NOT split into multiple commits. Pitfall 5 lock + D-34: ONE atomic commit covering all 5 surfaces.

**Step 6 — Post-commit audit-harness verification:**

```bash
node scripts/validation/v1.4.1-milestone-audit.mjs
```

Expected: exit 0 with 8/8 PASS. If any check FAILS, report the failure and STOP — do not push or proceed.

**Step 7 — Allow-list helper verification (D-12 line-shift discipline per Phase 43):**

```bash
node scripts/validation/regenerate-supervision-pins.mjs --report
```

Expected: zero un-pinned new findings, OR all line-shifts hand-merged per Phase 43 D-12 protocol. If shifts surface, hand-merge them and re-run before considering the task DONE.

**Step 8 — Final commit verification:**

```bash
git log -1 --stat
```

Expected: exactly 5 files modified in the most recent commit.
  </action>

  <acceptance_criteria>
    - **Atomic commit (D-23 / D-34):** `git log -1 --stat` shows exactly 5 files modified in the most recent commit (`docs/admin-setup-android/03-fully-managed-cobo.md`, `docs/admin-setup-android/04-byod-work-profile.md`, `docs/_glossary-android.md`, `docs/android-lifecycle/03-android-version-matrix.md`, `docs/reference/android-capability-matrix.md`)
    - **Banned-phrase compliance (D-31):** `grep -nE '\b(deprecated|end of life|EOL|no longer supported|obsolete|sunset|retired)\b' docs/admin-setup-android/03-fully-managed-cobo.md docs/admin-setup-android/04-byod-work-profile.md docs/_glossary-android.md docs/android-lifecycle/03-android-version-matrix.md docs/reference/android-capability-matrix.md` returns hits ONLY in pre-existing factual / historical context (Microsoft Store for Business Windows-context line 145, glossary line 179 corpus-canonical "NOT formally deprecated COPE" posture, BYOD AMAPI April 2025 historical, version-matrix SafetyNet "turned off January 2025") — ZERO new banned-phrase hits attached to COPE-mode
    - **Cross-Platform Equivalences UNCHANGED (per checker Issue 5 — section-extracting diff command):** the diff command above returns empty output (zero changed lines in Cross-Platform Equivalences section)
    - **Audit harness PASS:** `node scripts/validation/v1.4.1-milestone-audit.mjs` exits 0 with 8/8 PASS
    - **Allow-list helper:** `node scripts/validation/regenerate-supervision-pins.mjs --report` exits 0 reporting zero un-pinned new findings (or, if any line-shifts surfaced from the alphabetical-index update, all shifts hand-merged per Phase 43 D-12 protocol)
    - **All 5 forward-link targets resolve:** the Wave 1 file `08-cope-full-admin.md#provisioning-method-choice`, `#enrollment-token`, `#prerequisites`, `#android-15-frp`, `#cope-vs-cobo-decision`, `#cope-migration` all exist (verify by greping each anchor in `docs/admin-setup-android/08-cope-full-admin.md`)
  </acceptance_criteria>

  <verify>
    <automated>node scripts/validation/v1.4.1-milestone-audit.mjs && node scripts/validation/regenerate-supervision-pins.mjs --report && git log -1 --stat | grep -c '^ docs/' | grep -q '^5$'</automated>
  </verify>

  <done>
ONE atomic git commit landed covering 5 files (capability matrix + COBO + BYOD + glossary + version-matrix). Banned-phrase compliance verified (D-31). Cross-Platform Equivalences section byte-identical via section-extracting diff (T-46-02 / D-22 / SC#4 / checker Issue 5 fix). Audit harness 8/8 PASS. Allow-list helper reports zero un-pinned findings (or hand-merged). AECOPE-02, AECOPE-03, AECOPE-04 all CLOSED. Phase 46 terminal end-state achieved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → 5 markdown files | Multi-file edit risks scope-bleed beyond intended sentence-scoped retrofits (Pitfalls 4 + Pitfall 1 drift surface) |
| 5-file edit set → atomic git commit | Pitfall 5: ANY commit subset breaks atomicity contract D-23 + D-34 + creates transient broken-link windows within the Wave 2 bundle |
| capability matrix edit → Cross-Platform Equivalences section | T-46-02: column insertion across 5 H2 sub-tables risks accidental edits to lines 72-88 (D-22 NO new paired rows requirement) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering (banned-phrase prose injection) | All 5 edited files (especially glossary new Private Space H3 body in Task 02-03, capability matrix COPE column cells in Task 02-05, BYOD retrofit prose in Task 02-02) | mitigate | (1) Embed verbatim D-08 / D-10 / D-11 / D-21 / D-33 Pitfall 8 Option A wording from `<verbatim_replacements>` section — author has zero discretion on retrofit text; (2) author-time grep `grep -nE '\b(deprecated\|end of life\|EOL\|no longer supported\|obsolete\|sunset\|retired)\b'` across all 5 files in Task 02-06 Step 2 BEFORE commit; (3) C9 audit-harness check informational-first per D-29 / D-31; (4) acceptance criteria gates each retrofit task with explicit grep checks |
| T-46-02 | Tampering (Cross-Platform Equivalences row drift) | `docs/reference/android-capability-matrix.md` lines 72-88 (Cross-Platform Equivalences section) | mitigate | (1) Section-extracting `diff <(sed ...) <(sed ...)` command in Task 02-05 Step f AND Task 02-06 Step 3 verifies the section content is byte-identical pre/post (per checker Issue 5 — replaces fragile grep wc -l pattern that produced false positives from line-shift hunk headers); (2) acceptance criterion explicitly gates "Cross-Platform Equivalences section UNCHANGED" per SC#4 / D-22; (3) Task 02-05 `<read_first>` requires executor to verify the section's protected status before any matrix edit |
| T-46-03 | Tampering (over-trim of COBO source-attribution HTML comment) | `docs/admin-setup-android/03-fully-managed-cobo.md` line 66 (HTML comment with `last_verified: 2026-04-21` source attribution) | mitigate | (1) γ3 sentence-scoped trim discipline per D-21 + Pitfall 4 in Task 02-01: ONLY line 64 changes; (2) Edit tool with EXACT old_string from Replacement R1 captures only line 64 — wholesale H2 regeneration is forbidden; (3) Task 02-01 acceptance criterion `grep -F 'last_verified: 2026-04-21' docs/admin-setup-android/03-fully-managed-cobo.md returns 1 hit` confirms preservation; (4) Task 02-01 acceptance criterion `git diff` shows changes only in line 64 region + frontmatter |
| T-46-04 | Tampering (BYOD line 167 retrofit collateral changes) | `docs/admin-setup-android/04-byod-work-profile.md` lines 165-170 (retrofit region) | mitigate | (1) Edit tool in Task 02-02 with EXACT old_string from Replacement R2 captures only line 167; (2) Task 02-02 `<read_first>` requires executor to verify line 166 + line 168 boundaries pre-edit; (3) Task 02-02 acceptance criterion `git diff` filtered to BYOD doc shows changes only in line 167 region + frontmatter |
| T-46-05 | Tampering (KME→COPE false-pin reintroduction) | Capability matrix COPE-column Provisioning methods cell (Enrollment H2 sub-table, Task 02-05) — risks restating "KME does not support COPE" or similar false pin per Pitfall 3 / D-16 | mitigate | (1) Verbatim per-sub-table cell content provided in `<verbatim_replacements>` Insertions I5-I9 — Provisioning methods COPE cell text reads `QR / NFC (Android 8-10) / afw#setup (Android 8-10) / Token (Android 8-10) / Zero-Touch — see [08-cope-full-admin.md#provisioning-method-choice](../admin-setup-android/08-cope-full-admin.md#provisioning-method-choice)` with NO Knox row; KME context lives in the Wave 1 doc's decision matrix (D-16 LOCKED) and Samsung-admins Zero-Touch H3 callout (D-19); (2) executor has zero discretion to add cells about KME-COPE incompatibility; (3) Pitfall 3 documented in Task 02-05 `<read_first>` ensures author awareness |
| T-46-06 | Process (partial-commit subset breaks D-23 / D-34 atomicity) | Task 02-06 git commit boundary | mitigate | (1) Task 02-06 Step 1 verifies all 5 files have unstaged modifications BEFORE staging; if any file is missing, the task STOPS rather than committing a subset; (2) Task 02-06 Step 4 stages all 5 files in a single `git add` invocation; (3) Task 02-06 Step 5 produces ONE atomic commit; (4) Task 02-06 Step 8 verifies the commit contains exactly 5 files via `git log -1 --stat`; (5) Pitfall 5 explicitly invoked in Task 02-06 `<read_first>` |
</threat_model>

<verification>
After commit lands (Task 02-06 complete), run:
1. `node scripts/validation/v1.4.1-milestone-audit.mjs` — expect 8/8 PASS
2. `node scripts/validation/regenerate-supervision-pins.mjs --report` — expect zero un-pinned findings (or hand-merge if shifts surface)
3. `git log -1 --stat` — expect 5 files in the commit (per D-23 / D-34)
4. `git diff HEAD~1 docs/admin-setup-android/03-fully-managed-cobo.md` — expect changes only in line 64 region + frontmatter
5. `git diff HEAD~1 docs/admin-setup-android/04-byod-work-profile.md` — expect changes only in line 167 region + frontmatter
6. `git diff HEAD~1 docs/reference/android-capability-matrix.md` — expect column insertions in 5 sub-tables + Private Space row + footnote; Cross-Platform Equivalences section content byte-identical (verified via section-extracting `diff <(sed ...) <(sed ...)` pattern, NOT fragile `grep -A100 | wc -l` per checker Issue 5)
7. `grep -F '08-cope-full-admin.md' docs/admin-setup-android/03-fully-managed-cobo.md docs/admin-setup-android/04-byod-work-profile.md docs/_glossary-android.md docs/android-lifecycle/03-android-version-matrix.md docs/reference/android-capability-matrix.md` — expect hits in COBO retrofit (≥2) + glossary (≥2) + capability matrix (≥6 from cell forward-links)
8. `grep -nE '\b(deprecated|end of life|EOL|no longer supported|obsolete|sunset|retired)\b'` across all 5 files — expect ONLY pre-existing factual / historical hits; ZERO new hits attached to COPE-mode
</verification>

<success_criteria>
- AECOPE-02 ROADMAP closed: capability matrix has COPE column at index 1; Cross-Platform Equivalences UNCHANGED per SC#4
- AECOPE-03 ROADMAP closed: COBO `§COPE Migration Note` line 64 deferred-language replaced with forward-link to `08-cope-full-admin.md`. Per CONTEXT D-34, atomicity applies to the Wave 2 5-file bundle. Wave 1 commit and Wave 2 commit are separate per D-32 wave structure — D-34 LOCKS this interpretation as the resolved-decision form of D-23 atomicity. Wave 1 (Plan 01) commits `08-cope-full-admin.md` creation; Wave 2 (Plan 02) commits the 5-file retrofit bundle in ONE atomic commit. The transient broken-link window between the two commits is acceptable because Wave 2 declares `depends_on: [46-01]` ensuring sequential landing.
- AECOPE-04 ROADMAP closed: glossary COPE + WPCO entries forward-link to `08-cope-full-admin.md`; new Private Space H3 inserted alphabetically per D-09 with D-33 Pitfall 8 Option A scope-tightening
- D-10 closed: BYOD line 167 sentence-scoped retrofit lands
- D-11 closed: version-matrix new Private Space breakpoint H3 + line 30 anchor link
- D-12 closed: capability matrix Private Space row across all 6 columns + footnote
- D-22 honored: Cross-Platform Equivalences section content byte-identical (verified via section-extracting `diff <(sed ...) <(sed ...)` pattern per checker Issue 5)
- D-23 / D-34 honored: 5 files in ONE atomic commit (Wave 2 atomic bundle)
- D-31 honored: zero new banned-phrase hits attached to COPE-mode across all 5 files
- D-33 honored: Private Space H3 body (in glossary) and in-doc COPE callout (Wave 1) both use Pitfall 8 Option A scope-tightening ("**Intune** cannot manage…", NOT absolute "no admin policy lever in any mode")
- Audit harness 8/8 PASS post-commit
- Phase 44 reciprocal-pin discipline EXTENDED per D-24: COPE/WPCO admin doc is now the 3rd Samsung-admin reading-path node (after COBO `:162` and KME doc `:14`); Wave 1 `08-cope-full-admin.md` Samsung-admins callout was the carrier for this extension
</success_criteria>

<output>
After completion, create `.planning/phases/46-cope-full-admin/46-02-SUMMARY.md` documenting:
- Single atomic commit SHA covering 5 files (Task 02-06 outcome)
- Per-file diff summary (line ranges changed; line ranges preserved) for Tasks 02-01 through 02-05
- Cross-Platform Equivalences section unchanged-verification result via the section-extracting `diff <(sed ...) <(sed ...)` pattern (per checker Issue 5)
- Pitfall 4 verification: COBO line 66 HTML comment with `last_verified: 2026-04-21` byte-identical pre/post
- Pitfall 8 Option A application (D-33): Private Space H3 body uses "Intune cannot..." (NOT absolute "no admin policy lever in any mode")
- Banned-phrase grep result: zero new hits attached to COPE-mode
- Audit harness result: 8/8 PASS
- Allow-list helper result: zero un-pinned findings (or hand-merge details if shifts surfaced)
- AECOPE-02, AECOPE-03, AECOPE-04 closed; D-10, D-11, D-12, D-22, D-23, D-24, D-31, D-33, D-34 honored
- Phase 46 terminal end-state achieved; ready for `/gsd-verify-work`
</output>
