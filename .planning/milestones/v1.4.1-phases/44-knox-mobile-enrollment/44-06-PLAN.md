---
phase: 44-knox-mobile-enrollment
plan: 06
type: execute
wave: 3
depends_on:
  - 44-01
files_modified:
  - docs/_glossary-android.md
  - docs/android-lifecycle/02-provisioning-methods.md
autonomous: true
requirements:
  - AEKNOX-06
must_haves:
  truths:
    - "_glossary-android.md has 3 new H3 entries: `### Knox`, `### KME (Knox Mobile Enrollment)`, `### KPE (Knox Platform for Enterprise)`"
    - "_glossary-android.md alphabetical index at line ~15 includes `[Knox](#knox) | [KME](#kme) | [KPE](#kpe)` between Fully Managed and Managed Google Play"
    - "_glossary-android.md WPCO entry at line ~75 is NOT duplicated (single existing entry preserved)"
    - "_glossary-android.md AMAPI existing entry at line ~124 is NOT duplicated (single existing entry preserved); cross-link `[AMAPI](#amapi)` is added FROM new Knox entry"
    - "Each new entry includes a `> **Cross-platform note:**` blockquote with Samsung-specific framing per locked context"
    - "02-provisioning-methods.md anchor `#knox-mobile-enrollment-kme---deferred-to-v141` is RENAMED to `#knox-mobile-enrollment` and the H2 `## Knox Mobile Enrollment (KME) — Deferred to v1.4.1` is renamed to `## Knox Mobile Enrollment (KME)` (no Deferred)"
    - "02-provisioning-methods.md body of the renamed H2 is populated with live KME paragraph (not deferral text)"
    - "02-provisioning-methods.md top callout at line ~19 is updated to drop 'KME is deferred to v1.4.1' wording and forward-link to admin doc 07"
    - "Frontmatter dates of both files shifted to execute date and review_by + 60d"
  artifacts:
    - path: docs/_glossary-android.md
      provides: "Knox / KME / KPE glossary entries; alphabetical index updated"
      contains:
        - 'id="knox"'
        - 'id="kme"'
        - 'id="kpe"'
        - "### Knox"
        - "### KME (Knox Mobile Enrollment)"
        - "### KPE (Knox Platform for Enterprise)"
    - path: docs/android-lifecycle/02-provisioning-methods.md
      provides: "Live KME provisioning-method anchor; deferred-to-v1.4.1 wording removed"
      contains:
        - 'id="knox-mobile-enrollment"'
        - "## Knox Mobile Enrollment (KME)"
        - "07-knox-mobile-enrollment.md"
  key_links:
    - from: docs/_glossary-android.md (### Knox entry)
      to: docs/admin-setup-android/07-knox-mobile-enrollment.md#sku-disambiguation
      via: 5-SKU disambiguation cross-link
      pattern: "07-knox-mobile-enrollment.md#sku-disambiguation"
    - from: docs/_glossary-android.md (### KME entry)
      to: docs/_glossary-android.md (#cobo, #dedicated, #wpco, #zero-touch-enrollment, #amapi)
      via: In-doc cross-links
      pattern: "#(cobo|dedicated|wpco|zero-touch-enrollment|amapi)"
    - from: docs/android-lifecycle/02-provisioning-methods.md (#knox-mobile-enrollment)
      to: docs/admin-setup-android/07-knox-mobile-enrollment.md
      via: Cross-link in populated body
      pattern: "07-knox-mobile-enrollment.md"
---

<objective>
Two surgical edits to two existing files:

1. `docs/_glossary-android.md` — add 3 new H3 entries (`### Knox`, `### KME (Knox Mobile Enrollment)`, `### KPE (Knox Platform for Enterprise)`) under the existing **Provisioning Methods H2**; update the alphabetical index at line ~15. Do NOT add WPCO (already at line 75). Do NOT add AMAPI (already at line ~124) — only add cross-links FROM new Knox entry to AMAPI.

2. `docs/android-lifecycle/02-provisioning-methods.md` — rename anchor `#knox-mobile-enrollment-kme---deferred-to-v141` → `#knox-mobile-enrollment`; rename H2 from `## Knox Mobile Enrollment (KME) — Deferred to v1.4.1` to `## Knox Mobile Enrollment (KME)` (drop "Deferred to v1.4.1"); replace deferral body with populated KME paragraph; update the top callout at line ~19 to forward-link to admin doc 07 and drop "deferred to v1.4.1" wording.

Purpose: Fulfill AEKNOX-06 — close the v1.4 forward-promise embedded in the lifecycle reference's deferred-to-v1.4.1 anchor; complete glossary granularity per D-04 (Knox/KME/KPE entries; AMAPI cross-link from existing entry).

Output: Surgical edits to TWO existing files; no new files; bounded to specific line ranges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md
@.planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md
@.planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md
@.planning/phases/44-knox-mobile-enrollment/44-VALIDATION.md
@docs/_glossary-android.md
@docs/android-lifecycle/02-provisioning-methods.md
@docs/admin-setup-android/07-knox-mobile-enrollment.md

KEY VERBATIM SOURCE-OF-TRUTH (sourced from PATTERNS.md File 6 + File 7 + RESEARCH §7):

**Existing alphabetical index at line 15 of `_glossary-android.md` (verbatim — to be EDITED to add 3 new entries between Fully Managed and Managed Google Play):**

Existing:
```
[afw#setup](#afw-setup) | [AMAPI](#amapi) | [BYOD](#byod) | [COBO](#cobo) | [COPE](#cope) | [Corporate Identifiers](#corporate-identifiers) | [Dedicated](#dedicated) | [DPC](#dpc) | [EMM](#emm) | [Entra Shared Device Mode](#entra-shared-device-mode) | [Fully Managed](#fully-managed) | [Managed Google Play](#managed-google-play) | [Managed Home Screen](#managed-home-screen) | [Play Integrity](#play-integrity) | [Supervision](#supervision) | [User Enrollment](#user-enrollment) | [Work Profile](#work-profile) | [WPCO](#wpco) | [Zero-Touch Enrollment](#zero-touch-enrollment)
```

Phase 44 update — INSERT `[Knox](#knox) | [KME](#kme) | [KPE](#kpe) |` between `[Fully Managed](#fully-managed) |` and `[Managed Google Play](#managed-google-play)`:
```
[afw#setup](#afw-setup) | [AMAPI](#amapi) | [BYOD](#byod) | [COBO](#cobo) | [COPE](#cope) | [Corporate Identifiers](#corporate-identifiers) | [Dedicated](#dedicated) | [DPC](#dpc) | [EMM](#emm) | [Entra Shared Device Mode](#entra-shared-device-mode) | [Fully Managed](#fully-managed) | [Knox](#knox) | [KME](#kme) | [KPE](#kpe) | [Managed Google Play](#managed-google-play) | [Managed Home Screen](#managed-home-screen) | [Play Integrity](#play-integrity) | [Supervision](#supervision) | [User Enrollment](#user-enrollment) | [Work Profile](#work-profile) | [WPCO](#wpco) | [Zero-Touch Enrollment](#zero-touch-enrollment)
```

**Knox / KME / KPE glossary entries (D-04 LOCKED — verbatim from RESEARCH §7 sketches):**

```
### Knox

Samsung Knox is Samsung's mobile-device security platform spanning hardware (Knox eFuse, Knox Vault) and software (Knox Platform for Enterprise, Knox Mobile Enrollment, Knox Suite, Knox Manage, Knox Configure). For Intune-managed Samsung fleets, the relevant Knox SKUs are [KME](#kme) (the Samsung zero-touch-equivalent enrollment path) and [KPE](#kpe) (the Samsung security-extension layer that Intune Plan 1+ supplies transparently). Intune calls [AMAPI](#amapi) for Knox-provisioned device policy. See the [Knox SKU disambiguation table](../admin-setup-android/07-knox-mobile-enrollment.md#sku-disambiguation) in the KME admin guide for the 5-SKU breakdown.

> **Cross-platform note:** Samsung-specific. No Apple, Microsoft, or AOSP equivalent. Samsung Knox security features apply only to Samsung hardware; Knox SKUs are not portable to non-Samsung Android OEMs.
```

```
### KME (Knox Mobile Enrollment)

Knox Mobile Enrollment is Samsung's zero-touch-equivalent enrollment channel for Samsung devices. KME provisions Samsung devices into the Intune-managed [COBO](#cobo) / [Dedicated](#dedicated) / [WPCO](#wpco) modes via the Knox Admin Portal → Intune handoff (custom JSON containing the Intune enrollment token). KME is **free** and does not require a paid Knox license; it is mutually exclusive with [Zero-Touch Enrollment](#zero-touch-enrollment) on the same Samsung device (KME takes precedence at the device firmware level when both are configured).

> **Cross-platform note:** Samsung-specific. The Google ZT analog is [Zero-Touch Enrollment](#zero-touch-enrollment); the Apple analog is ADE; the Windows analog is Autopilot. KME is NOT portable to non-Samsung Android OEMs.
```

```
### KPE (Knox Platform for Enterprise)

Knox Platform for Enterprise is Samsung's security-extension layer adding device-policy capabilities beyond stock Android Enterprise (kiosk customization, advanced restriction policies, custom boot animations). KPE has historically been licensed in two tiers: Standard (free baseline) and Premium (per-device upgrade). Samsung made Premium licenses **free** in 2024 (Samsung confirmed 2024-03-21); Microsoft Intune Plan 1+ supplies KPE Premium transparently to enrolled Samsung devices. KPE is NOT required for [KME](#kme); the two are independent SKUs.

> **Cross-platform note:** Samsung-specific security-extension layer. No Apple or Windows analog at the SKU level.
```

NOTE: Original RESEARCH §7 sketch for KPE Cross-platform note ended with "the closest concept is Apple's MDM-controlled supervised-only restrictions (which are gated by ADE, not licensed separately)." That phrasing INTRODUCES a `supervised` token which would FAIL audit C2. The EXECUTOR MUST use the shorter form ABOVE ("No Apple or Windows analog at the SKU level.") to remain C2-compliant — KME is Samsung-only per locked context (no iOS bridge prose in Phase 44 per CONTEXT.md "Out of scope: Cross-platform analogs of KME").

**Section placement (per Phase 34 D-09 5-category H2 lock + RESEARCH §7):**
- All 3 new entries go under existing **Provisioning Methods H2** (currently at line ~81 of _glossary-android.md)
- Insert order: Knox → KME → KPE alphabetical block
- Insert position: between existing entries `### afw#setup` (line ~88) and `### Corporate Identifiers` (line ~90), OR another alphabetical-correct slot under Provisioning Methods H2 — executor reads the file to confirm exact line, since "Knox" sorts after "afw#setup" but before "Corporate Identifiers" alphabetically (case-insensitive a-c-K). Recommended slot: between `### afw#setup` and `### Corporate Identifiers`.
- AMAPI: NO new entry. Existing entry at line ~124 unchanged. New Knox entry contains the cross-link `Intune calls [AMAPI](#amapi) for Knox-provisioned device policy`.

**Frontmatter shift for `_glossary-android.md` (Phase 43 D-22):**
- `last_verified:` to execute date
- `review_by:` to execute date + 60d

---

**`02-provisioning-methods.md` edits:**

**Existing top callout at lines 18-19 (verbatim — to be EDITED):**
```
<a id="samsung-kme-mutual-exclusion"></a>
> **Samsung devices:** Knox Mobile Enrollment (KME) is mutually exclusive with Zero-Touch on the same Samsung device. Configure only one. KME is deferred to v1.4.1; see the [KME deferral note](#knox-mobile-enrollment-kme---deferred-to-v141) at the bottom of this page.
```

Phase 44 replacement (drop "deferred to v1.4.1" wording; forward-link to admin doc 07; update anchor reference to renamed in-doc anchor):
```
<a id="samsung-kme-mutual-exclusion"></a>
> **Samsung devices:** Knox Mobile Enrollment (KME) is mutually exclusive with Zero-Touch on the same Samsung device. Configure only one. For full KME admin coverage, see [Knox Mobile Enrollment](../admin-setup-android/07-knox-mobile-enrollment.md); for the within-this-doc record, see [Knox Mobile Enrollment](#knox-mobile-enrollment) below.
```

**Existing bottom block at lines 50-55 (verbatim — to be REPLACED entirely):**
```
<a id="knox-mobile-enrollment-kme---deferred-to-v141"></a>
## Knox Mobile Enrollment (KME) — Deferred to v1.4.1

Knox Mobile Enrollment is Samsung's Zero-Touch-equivalent for Samsung hardware. A KME row will be added to the matrix above in v1.4.1 per [PROJECT.md Key Decisions](../../.planning/PROJECT.md). For v1.4, treat Samsung devices as Zero-Touch-eligible per the [Samsung KME mutual-exclusion note](#samsung-kme-mutual-exclusion) at the top of this page. Do NOT configure both KME and Zero-Touch on the same Samsung device — KME takes precedence at the device firmware level.

Full KME admin documentation (binding Knox Admin Portal to Intune, Samsung reseller workflow, KME-specific failure modes) is tracked in the v1.4 deferred-items list and will ship in v1.4.1.
```

Phase 44 replacement (anchor renamed; H2 renamed; body populated):
```
<a id="knox-mobile-enrollment"></a>
## Knox Mobile Enrollment (KME)

Knox Mobile Enrollment is Samsung's Zero-Touch-equivalent for Samsung hardware. KME provisions Samsung devices into the existing Android Enterprise device-owner modes (Fully Managed COBO / Dedicated / WPCO) via the Knox Admin Portal → Intune handoff using a flat `EXTRA_ENROLLMENT_TOKEN` Custom JSON Data field. KME itself is free; KPE Premium has been free since Samsung's 2024-03-21 update; Microsoft Intune Plan 1+ supplies KPE Premium transparently. KME is mutually exclusive with Zero-Touch on the same Samsung device — see the [Samsung KME mutual-exclusion note](#samsung-kme-mutual-exclusion) at the top of this page. KME takes precedence at the device firmware level when both are configured.

For full admin coverage, see [Knox Mobile Enrollment Admin Guide](../admin-setup-android/07-knox-mobile-enrollment.md). For L1 enrollment-failure triage, see [L1 Runbook 28: Android Knox Enrollment Failed](../l1-runbooks/28-android-knox-enrollment-failed.md). For L2 investigation, see [L2 Runbook 22: Android Knox Investigation](../l2-runbooks/22-android-knox-investigation.md). For the cross-mode capability matrix, see [Knox Mobile Enrollment (Samsung)](../reference/android-capability-matrix.md#knox-mobile-enrollment-row).
```

**Frontmatter shift for `02-provisioning-methods.md` (Phase 43 D-22):**
- `last_verified:` to execute date
- `review_by:` to execute date + 60d
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Edit _glossary-android.md — insert Knox/KME/KPE entries under Provisioning Methods H2; update alphabetical index; shift frontmatter dates</name>
  <files>docs/_glossary-android.md</files>
  <read_first>
    - .planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md (D-04 glossary granularity; locked entry text)
    - .planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md (File 6 — entry skeleton; insertion location; AMAPI cross-link rule; C2-compliant KPE Cross-platform note)
    - .planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md (§7 Glossary Census — Section placement under Provisioning Methods H2)
    - docs/_glossary-android.md (read FULL file to identify exact line of alphabetical index, Provisioning Methods H2 boundaries, existing AMAPI line ~124, existing WPCO line ~75)
  </read_first>
  <behavior>
    - 3 new H3 entries under Provisioning Methods H2: `### Knox`, `### KME (Knox Mobile Enrollment)`, `### KPE (Knox Platform for Enterprise)` in alphabetical order (Knox → KME → KPE)
    - Each entry has the verbatim text from `<context>` (paragraph + Cross-platform note blockquote)
    - Alphabetical index at line ~15 has `[Knox](#knox) | [KME](#kme) | [KPE](#kpe) |` inserted between Fully Managed and Managed Google Play
    - Existing WPCO entry at line ~75 unchanged (single instance)
    - Existing AMAPI entry at line ~124 unchanged (single instance); new cross-link FROM Knox entry TO AMAPI present
    - Existing Supervision callout (audit C2 attribution discipline) unchanged
    - Frontmatter `last_verified` + `review_by` shifted
    - KPE Cross-platform note uses C2-compliant short form ("No Apple or Windows analog at the SKU level.") — does NOT contain `supervised` / `supervision`
  </behavior>
  <action>
    Use the Edit tool to make these edits to `docs/_glossary-android.md`:

    **Edit 1 — Frontmatter dates:**
    Compute dates: `node -e "const d=new Date(); console.log('last_verified: '+d.toISOString().slice(0,10)); d.setDate(d.getDate()+60); console.log('review_by: '+d.toISOString().slice(0,10))"`
    - Replace `last_verified: <old>` with `last_verified: <execute-date>`
    - Replace `review_by: <old>` with `review_by: <execute-date + 60d>`

    **Edit 2 — Alphabetical index update at line ~15:**
    Find the line containing the alphabetical index (the long pipe-separated string with `[afw#setup]` ... `[Zero-Touch Enrollment]`). Replace the substring `[Fully Managed](#fully-managed) | [Managed Google Play]` with `[Fully Managed](#fully-managed) | [Knox](#knox) | [KME](#kme) | [KPE](#kpe) | [Managed Google Play]`. The rest of the index line is preserved.

    **Edit 3 — Insert 3 new H3 entries under Provisioning Methods H2:**
    Read the file to identify the exact line of the `### afw#setup` H3 (which is the first entry under Provisioning Methods H2 alphabetically). Locate the END of that entry (next blank line + next H3 OR H2). Insert the 3 new entries Knox → KME → KPE in that order, BEFORE the next H3 (which should be `### Corporate Identifiers` per RESEARCH §7).

    Use the verbatim entry text from `<context>` for all 3 entries:
    - `### Knox` paragraph + Cross-platform note (Samsung-specific framing)
    - `### KME (Knox Mobile Enrollment)` paragraph + Cross-platform note
    - `### KPE (Knox Platform for Enterprise)` paragraph + Cross-platform note (use C2-compliant short form: "No Apple or Windows analog at the SKU level." — do NOT include the `supervised-only` phrasing from the RESEARCH §7 sketch)

    Each entry uses HTML anchor implicitly (markdown auto-anchors `#knox`, `#kme`, `#kpe`); no explicit `<a id="...">` tag needed unless sibling glossary entries use them — read `### afw#setup` and `### WPCO` patterns at lines 75/88 to confirm convention. If sibling entries use explicit `<a id="...">`, mirror; if they rely on auto-anchors, use auto-anchors.

    Verify alphabetical placement: after insertion, the H3 order under Provisioning Methods H2 should read: `### afw#setup` → `### Knox` → `### KME (Knox Mobile Enrollment)` → `### KPE (Knox Platform for Enterprise)` → `### Corporate Identifiers` → ...

    **CRITICAL: Do NOT add a new AMAPI entry.** The existing AMAPI entry at line ~124 (under Compliance & Attestation H2) stays unchanged. The new Knox entry contains an in-document cross-link `[AMAPI](#amapi)` pointing to that existing entry.

    **CRITICAL: Do NOT add a new WPCO entry.** The existing WPCO entry at line ~75 stays unchanged.
  </action>
  <verify>
    <automated>grep -c '^### Knox$' docs/_glossary-android.md | awk '$1 == 1 {exit 0} {exit 1}' && grep -q '^### KME (Knox Mobile Enrollment)$' docs/_glossary-android.md && grep -q '^### KPE (Knox Platform for Enterprise)$' docs/_glossary-android.md && grep -q '\[Knox\](#knox)' docs/_glossary-android.md && grep -q '\[KME\](#kme)' docs/_glossary-android.md && grep -q '\[KPE\](#kpe)' docs/_glossary-android.md && [ $(grep -c '^### WPCO' docs/_glossary-android.md) -eq 1 ] && [ $(grep -c '^### AMAPI' docs/_glossary-android.md) -eq 1 ] && ! grep -q "SafetyNet" docs/_glossary-android.md && ! grep -qE "supervis(ed|ion|or)" docs/_glossary-android.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### Knox$' docs/_glossary-android.md` returns exactly 1 (new Knox H3)
    - `grep -c '^### KME (Knox Mobile Enrollment)$' docs/_glossary-android.md` returns exactly 1
    - `grep -c '^### KPE (Knox Platform for Enterprise)$' docs/_glossary-android.md` returns exactly 1
    - Alphabetical index updated:
      - `grep -q '\[Knox\](#knox)' docs/_glossary-android.md` matches
      - `grep -q '\[KME\](#kme)' docs/_glossary-android.md` matches
      - `grep -q '\[KPE\](#kpe)' docs/_glossary-android.md` matches
    - WPCO single instance: `grep -c '^### WPCO' docs/_glossary-android.md` returns 1 (no duplicate added)
    - AMAPI single instance: `grep -c '^### AMAPI' docs/_glossary-android.md` returns 1 (no duplicate added)
    - Cross-link from Knox entry to AMAPI: `grep -A 5 '^### Knox$' docs/_glossary-android.md | grep -q '\[AMAPI\](#amapi)'`
    - Each new entry has Cross-platform note: `grep -B 0 -A 4 '^### Knox$\|^### KME\|^### KPE' docs/_glossary-android.md | grep -c 'Cross-platform note'` returns at least 3
    - **Audit C1 (zero SafetyNet):** `! grep -q 'SafetyNet' docs/_glossary-android.md` — NOTE: the existing Play Integrity entry may already mention SafetyNet in deprecation context; if pre-edit grep already matches "SafetyNet" in the file, the post-edit count must NOT INCREASE. Pre-check with: `BEFORE=$(grep -c 'SafetyNet' docs/_glossary-android.md); ... edit ...; AFTER=$(grep -c 'SafetyNet' docs/_glossary-android.md); [ "$BEFORE" -eq "$AFTER" ]`
    - **Audit C2 (zero new supervision):** the new Knox/KME/KPE entries must NOT contain `supervised` / `supervision` / `supervisor`. Existing Supervision entry at line ~65-67 is preserved unchanged. Verify NEW entries via: `awk '/^### Knox$/,/^### Managed Google Play$/' docs/_glossary-android.md | grep -qE 'supervis(ed|ion|or)' && exit 1 || exit 0` (i.e. the awk slice from `### Knox` to `### Managed Google Play` must NOT contain supervision tokens)
    - Frontmatter dates shifted: `grep -qE "last_verified: 20[0-9]{2}-[0-9]{2}-[0-9]{2}" docs/_glossary-android.md`
    - Audit harness 8/8 PASS post-edit: `node scripts/validation/v1.4.1-milestone-audit.mjs` exits 0 (especially C2 must remain green — supervision_exemptions sidecar count unchanged)
  </acceptance_criteria>
  <done>3 new glossary entries (Knox/KME/KPE) inserted alphabetically under Provisioning Methods H2; alphabetical index updated; WPCO and AMAPI not duplicated; Cross-platform notes are C2-compliant (no supervision tokens in new entries); frontmatter dates shifted. All 14 acceptance criteria pass. Audit harness 8/8 PASS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Edit 02-provisioning-methods.md — rename anchor + H2 + populate body; update top callout; shift frontmatter dates</name>
  <files>docs/android-lifecycle/02-provisioning-methods.md</files>
  <read_first>
    - .planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md (AEKNOX-06 — populate provisioning-methods anchor)
    - .planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md (File 7 — verbatim copy-from-source for anchor rename + H2 rename + body replace + top-callout edit)
    - .planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md (§5 Pin 3 — the 3 surgical edits to this file)
    - docs/android-lifecycle/02-provisioning-methods.md (lines 1-7 frontmatter; lines 18-19 top callout; lines 50-55 deferred bottom block)
    - docs/admin-setup-android/07-knox-mobile-enrollment.md (Plan 01 output — confirms cross-link target file exists)
  </read_first>
  <behavior>
    - Frontmatter dates shifted (Phase 43 D-22)
    - Top callout at line ~19 has anchor reference updated from `(#knox-mobile-enrollment-kme---deferred-to-v141)` to `(#knox-mobile-enrollment)` AND the "KME is deferred to v1.4.1" wording is dropped + replaced with forward-link to admin doc 07
    - Bottom block at lines 50-55 has anchor renamed (`#knox-mobile-enrollment-kme---deferred-to-v141` → `#knox-mobile-enrollment`)
    - Bottom block H2 renamed (`## Knox Mobile Enrollment (KME) — Deferred to v1.4.1` → `## Knox Mobile Enrollment (KME)`)
    - Bottom block body REPLACED with populated KME paragraph + cross-links to admin doc 07, L1 28, L2 22, capability matrix
    - Zero "Deferred to v1.4.1" wording remains in this file
  </behavior>
  <action>
    Use the Edit tool to make these surgical edits to `docs/android-lifecycle/02-provisioning-methods.md`:

    **Edit 1 — Frontmatter dates:**
    Compute dates as in Task 1. Replace `last_verified` and `review_by` lines with execute date and execute date + 60d.

    **Edit 2 — Top callout at lines 18-19 replacement:**
    Find this exact 2-line block:
    ```
    <a id="samsung-kme-mutual-exclusion"></a>
    > **Samsung devices:** Knox Mobile Enrollment (KME) is mutually exclusive with Zero-Touch on the same Samsung device. Configure only one. KME is deferred to v1.4.1; see the [KME deferral note](#knox-mobile-enrollment-kme---deferred-to-v141) at the bottom of this page.
    ```

    Replace with:
    ```
    <a id="samsung-kme-mutual-exclusion"></a>
    > **Samsung devices:** Knox Mobile Enrollment (KME) is mutually exclusive with Zero-Touch on the same Samsung device. Configure only one. For full KME admin coverage, see [Knox Mobile Enrollment](../admin-setup-android/07-knox-mobile-enrollment.md); for the within-this-doc record, see [Knox Mobile Enrollment](#knox-mobile-enrollment) below.
    ```

    **Edit 3 — Bottom block at lines 50-55 replacement (anchor rename + H2 rename + body populate):**
    Find this exact 5-line block (verbatim):
    ```
    <a id="knox-mobile-enrollment-kme---deferred-to-v141"></a>
    ## Knox Mobile Enrollment (KME) — Deferred to v1.4.1

    Knox Mobile Enrollment is Samsung's Zero-Touch-equivalent for Samsung hardware. A KME row will be added to the matrix above in v1.4.1 per [PROJECT.md Key Decisions](../../.planning/PROJECT.md). For v1.4, treat Samsung devices as Zero-Touch-eligible per the [Samsung KME mutual-exclusion note](#samsung-kme-mutual-exclusion) at the top of this page. Do NOT configure both KME and Zero-Touch on the same Samsung device — KME takes precedence at the device firmware level.

    Full KME admin documentation (binding Knox Admin Portal to Intune, Samsung reseller workflow, KME-specific failure modes) is tracked in the v1.4 deferred-items list and will ship in v1.4.1.
    ```

    Replace verbatim with the populated block from `<context>`:
    ```
    <a id="knox-mobile-enrollment"></a>
    ## Knox Mobile Enrollment (KME)

    Knox Mobile Enrollment is Samsung's Zero-Touch-equivalent for Samsung hardware. KME provisions Samsung devices into the existing Android Enterprise device-owner modes (Fully Managed COBO / Dedicated / WPCO) via the Knox Admin Portal → Intune handoff using a flat `EXTRA_ENROLLMENT_TOKEN` Custom JSON Data field. KME itself is free; KPE Premium has been free since Samsung's 2024-03-21 update; Microsoft Intune Plan 1+ supplies KPE Premium transparently. KME is mutually exclusive with Zero-Touch on the same Samsung device — see the [Samsung KME mutual-exclusion note](#samsung-kme-mutual-exclusion) at the top of this page. KME takes precedence at the device firmware level when both are configured.

    For full admin coverage, see [Knox Mobile Enrollment Admin Guide](../admin-setup-android/07-knox-mobile-enrollment.md). For L1 enrollment-failure triage, see [L1 Runbook 28: Android Knox Enrollment Failed](../l1-runbooks/28-android-knox-enrollment-failed.md). For L2 investigation, see [L2 Runbook 22: Android Knox Investigation](../l2-runbooks/22-android-knox-investigation.md). For the cross-mode capability matrix, see [Knox Mobile Enrollment (Samsung)](../reference/android-capability-matrix.md#knox-mobile-enrollment-row).
    ```

    **Edit 4 — Update Zero-Touch entry at line ~48 if it cross-references the old deferred anchor:**
    The existing Zero-Touch H3 (line ~46-48 per RESEARCH §5 Pin 3) currently ends with `Samsung devices: see the [Samsung KME mutual-exclusion note](#samsung-kme-mutual-exclusion) above.` This already uses the `#samsung-kme-mutual-exclusion` anchor (preserved unchanged). No edit needed UNLESS this line also references the old `#knox-mobile-enrollment-kme---deferred-to-v141` anchor — read the file to verify. If it does, replace any remaining old-anchor reference with `#knox-mobile-enrollment`.

    Verify NO references to `#knox-mobile-enrollment-kme---deferred-to-v141` remain anywhere in the file post-edit.
  </action>
  <verify>
    <automated>! grep -q "knox-mobile-enrollment-kme---deferred-to-v141" docs/android-lifecycle/02-provisioning-methods.md && grep -q 'id="knox-mobile-enrollment"' docs/android-lifecycle/02-provisioning-methods.md && grep -q '## Knox Mobile Enrollment (KME)$' docs/android-lifecycle/02-provisioning-methods.md && ! grep -q "Deferred to v1.4.1" docs/android-lifecycle/02-provisioning-methods.md && ! grep -q "deferred to v1.4.1" docs/android-lifecycle/02-provisioning-methods.md && grep -q "07-knox-mobile-enrollment.md" docs/android-lifecycle/02-provisioning-methods.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "knox-mobile-enrollment-kme---deferred-to-v141" docs/android-lifecycle/02-provisioning-methods.md` (old anchor REMOVED — both definition and any references)
    - `grep -c 'id="knox-mobile-enrollment"' docs/android-lifecycle/02-provisioning-methods.md` returns exactly 1 (new anchor definition)
    - `grep -q '## Knox Mobile Enrollment (KME)$' docs/android-lifecycle/02-provisioning-methods.md` matches (H2 renamed; trailing dollar prevents matching old "(KME) — Deferred to v1.4.1" line)
    - `! grep -q "Deferred to v1.4.1"` (zero "Deferred to v1.4.1" wording remains in this file — case sensitive)
    - `! grep -q "deferred to v1.4.1"` (zero lowercase variant remains either)
    - `! grep -q "KME deferral note"` (old top-callout wording removed)
    - `grep -q "07-knox-mobile-enrollment.md"` matches (cross-link to admin doc 07 in body)
    - `grep -q "28-android-knox-enrollment-failed.md"` matches (cross-link to L1 28)
    - `grep -q "22-android-knox-investigation.md"` matches (cross-link to L2 22)
    - `grep -q "android-capability-matrix.md#knox-mobile-enrollment-row"` matches (cross-link to capability matrix renamed anchor — depends on Plan 04 wave 3 completion; both plans in Wave 3 — order-independent for grep verification)
    - `grep -q 'samsung-kme-mutual-exclusion'` matches (top-callout anchor preserved)
    - Frontmatter dates shifted: `grep -qE "last_verified: 20[0-9]{2}-[0-9]{2}-[0-9]{2}"`
    - VALIDATION.md AEKNOX-06 unit-grep passes:
      ```
      grep -qE '^### Knox$|^### KME|^### KPE|^### AMAPI' docs/_glossary-android.md && \
        test $(grep -cE '^### WPCO' docs/_glossary-android.md) -eq 1 && \
        grep -q "knox-mobile-enrollment" docs/android-lifecycle/02-provisioning-methods.md && \
        ! grep -q "Deferred to v1.4.1" docs/android-lifecycle/02-provisioning-methods.md
      ```
    - Audit harness 8/8 PASS post-both-tasks: `node scripts/validation/v1.4.1-milestone-audit.mjs` exits 0
  </acceptance_criteria>
  <done>02-provisioning-methods.md anchor + H2 renamed; body populated with live KME paragraph + cross-links; top callout updated; frontmatter dates shifted; zero "Deferred to v1.4.1" wording. All 13 acceptance criteria pass. AEKNOX-06 VALIDATION unit-grep exits 0. Audit harness 8/8 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Glossary entry → admin doc cross-links | Cross-links from Knox entry to `07-knox-mobile-enrollment.md#sku-disambiguation` rely on Plan 01 anchor existing |
| Renamed anchor `#knox-mobile-enrollment` → external references | Plan 04 capability matrix and Plan 01 admin doc may cross-reference this anchor; rename must be ATOMIC within this file |
| Audit C2 boundary | New Knox/KME/KPE entries must NOT introduce supervision tokens (Samsung-only context per RESEARCH "out of scope: cross-platform analogs of KME") |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Information Disclosure (incorrect) | KPE entry — "free since 2024-03-21 + Plan 1+ transparency" | mitigate | Cite Samsung 2024-03-21; 60-day freshness; researcher OQ-1 re-verification at execute time |
| T-44-03 | Tampering (link rot) | Anchor rename in 02-provisioning-methods.md | mitigate | Two-step: replace anchor definition + replace top-callout reference; zero `knox-mobile-enrollment-kme---deferred-to-v141` references post-edit |
| T-44-04 | Tampering (silent C2 regression) | New glossary entries inadvertently introducing `supervised` token | mitigate | Use C2-compliant short form for KPE Cross-platform note ("No Apple or Windows analog at the SKU level."); audit harness C2 enforces; awk-slice grep gates new entries |
</threat_model>

<verification>
After both tasks complete:

1. Audit harness: `node scripts/validation/v1.4.1-milestone-audit.mjs` — expect 8/8 PASS; especially C2 must not regress.
2. AEKNOX-06 unit-grep predicate (from VALIDATION.md):
   ```bash
   grep -qE '^### Knox$|^### KME|^### KPE|^### AMAPI' docs/_glossary-android.md && \
     test $(grep -cE '^### WPCO' docs/_glossary-android.md) -eq 1 && \
     grep -q "knox-mobile-enrollment" docs/android-lifecycle/02-provisioning-methods.md && \
     ! grep -q "Deferred to v1.4.1" docs/android-lifecycle/02-provisioning-methods.md
   ```
   Expected: exit 0.
3. Anchor rename atomicity: `! grep -q "knox-mobile-enrollment-kme---deferred-to-v141" docs/android-lifecycle/02-provisioning-methods.md` (zero stragglers).
4. WPCO/AMAPI no-duplicate check: `[ $(grep -c '^### WPCO' docs/_glossary-android.md) -eq 1 ] && [ $(grep -c '^### AMAPI' docs/_glossary-android.md) -eq 1 ]` exits 0.
</verification>

<success_criteria>
- 3 new glossary entries (Knox / KME / KPE) inserted alphabetically under Provisioning Methods H2 in `_glossary-android.md`
- Alphabetical index at line ~15 has 3 new entries between Fully Managed and Managed Google Play
- WPCO and AMAPI single instances preserved (zero duplicates)
- Knox entry has `[AMAPI](#amapi)` cross-link
- KPE Cross-platform note uses C2-compliant short form (no supervision tokens)
- `02-provisioning-methods.md` anchor renamed `#knox-mobile-enrollment-kme---deferred-to-v141` → `#knox-mobile-enrollment` (zero old-anchor references remain)
- H2 renamed `## Knox Mobile Enrollment (KME) — Deferred to v1.4.1` → `## Knox Mobile Enrollment (KME)`
- Bottom block body populated with live KME paragraph + cross-links to admin doc 07, L1 28, L2 22, capability matrix
- Top callout updated (drops "deferred to v1.4.1" wording; forward-links to admin doc 07)
- Zero `Deferred to v1.4.1` wording in `02-provisioning-methods.md`
- Frontmatter dates shifted in both files
- AEKNOX-06 VALIDATION unit-grep exits 0
- Audit harness 8/8 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/44-knox-mobile-enrollment/44-06-SUMMARY.md` documenting:
- Glossary entries added (Knox/KME/KPE) with insertion line numbers
- Alphabetical index update
- AMAPI/WPCO no-duplicate confirmation
- 02-provisioning-methods.md anchor + H2 rename + body populate
- Audit harness 8/8 PASS state
- C2 supervision_exemptions sidecar count unchanged
</output>
