---
phase: 44-knox-mobile-enrollment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-android/07-knox-mobile-enrollment.md
autonomous: true
requirements:
  - AEKNOX-01
must_haves:
  truths:
    - "docs/admin-setup-android/07-knox-mobile-enrollment.md exists with Phase 34 admin template frontmatter (audience: admin, platform: Android, applies_to: KME, last_verified + review_by 60d apart)"
    - "5-SKU disambiguation H2 renders with the exact title 'Knox SKU disambiguation (5 SKUs)' and contains 5 rows (KME / KPE / Knox Manage / Knox Suite / Knox Configure) with KPE having adjacent Standard|Premium columns"
    - "Step 0 H2 renders with the exact title 'Step 0 — Samsung Knox Portal B2B account approval (1-2 business days)' at top of Setup Steps"
    - "DPC anti-paste blockquote bracketed by '<!-- AEKNOX-03-shared-anti-paste-block -->' / '<!-- /AEKNOX-03-shared-anti-paste-block -->' HTML comments at the JSON-paste step"
    - "Reciprocal KME/ZT mutex callout points to '02-zero-touch-portal.md#kme-zt-mutual-exclusion' (BACK-link, not forward)"
    - "Knox Custom JSON Data block contains the FLAT '{\"com.google.android.apps.work.clouddpc.EXTRA_ENROLLMENT_TOKEN\": ...}' (no PROVISIONING_ADMIN_EXTRAS_BUNDLE wrapper)"
    - "Zero SafetyNet token references except within +/-200 chars of Play Integrity deprecation phrasing (audit C1)"
    - "Zero 'supervision' / 'supervised' references (audit C2)"
  artifacts:
    - path: docs/admin-setup-android/07-knox-mobile-enrollment.md
      provides: "Samsung KME admin guide; 4th-portal overlay onto tri-portal pattern; closes v1.4 forward-promise pinned at 02-zero-touch-portal.md:16 and 03-fully-managed-cobo.md:162"
      min_lines: 200
      contains:
        - "applies_to: KME"
        - "Knox SKU disambiguation (5 SKUs)"
        - "Step 0 — Samsung Knox Portal B2B account approval (1-2 business days)"
        - "AEKNOX-03-shared-anti-paste-block"
        - "EXTRA_ENROLLMENT_TOKEN"
  key_links:
    - from: docs/admin-setup-android/07-knox-mobile-enrollment.md
      to: docs/admin-setup-android/02-zero-touch-portal.md#kme-zt-mutual-exclusion
      via: KME/ZT reciprocal mutex callout
      pattern: "02-zero-touch-portal.md#kme-zt-mutual-exclusion"
    - from: docs/admin-setup-android/07-knox-mobile-enrollment.md
      to: docs/admin-setup-android/01-managed-google-play.md
      via: Prerequisites checklist (MGP binding required)
      pattern: "01-managed-google-play.md"
    - from: docs/admin-setup-android/07-knox-mobile-enrollment.md
      to: scripts/validation/v1.4.1-milestone-audit.mjs
      via: C1 / C2 / C5 / C7 audit-harness compliance (audit harness reads frontmatter + body)
      pattern: "last_verified:.*\\nreview_by:"
---

<objective>
Author the new Samsung Knox Mobile Enrollment admin guide at `docs/admin-setup-android/07-knox-mobile-enrollment.md`. This is the foundational file for Phase 44 — every subsequent plan cross-links to or references this document.

Purpose: Close the v1.4 forward-promise pinned in `02-zero-touch-portal.md:16` ("Full KME coverage is tracked for v1.4.1") and `03-fully-managed-cobo.md:162` ("Full KME coverage is deferred to v1.4.1"). Prevent the "KME requires paid Knox license" misconception via D-01 5-SKU disambiguation H2. Surface the 1-2 business day Samsung B2B approval gate as a Step 0 H2 (D-02). Insert the AEKNOX-03 anti-paste blockquote at the JSON-paste step (D-03).

Output: One new file (`07-knox-mobile-enrollment.md`); zero modifications to other files in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md
@.planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md
@.planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md
@.planning/phases/44-knox-mobile-enrollment/44-VALIDATION.md
@docs/admin-setup-android/02-zero-touch-portal.md
@docs/admin-setup-android/03-fully-managed-cobo.md

<interfaces>
<!-- Verbatim copy-from-source markdown patterns the executor uses. -->
<!-- All sourced from PATTERNS.md File 1 + RESEARCH §Code Examples. -->

Frontmatter (Phase 34 admin template — adapt last_verified to execute date):
```yaml
---
last_verified: <YYYY-MM-DD execute date>
review_by: <YYYY-MM-DD execute date + 60 days>
audience: admin
platform: Android
applies_to: KME
---
```

Platform gate blockquote (mirror ZT line 11-12 shape, KME-specific phrasing):
```markdown
> **Platform gate:** Samsung Knox Mobile Enrollment (KME) — Knox Admin Portal account setup, EMM profile, Custom JSON Data, and Knox→Intune handoff for Samsung Android Enterprise enrollment. Samsung-only.
> For iOS, see [iOS Admin Guides](../admin-setup-ios/00-overview.md); for macOS, [macOS Admin Setup](../admin-setup-macos/00-overview.md); for terminology, [Android glossary](../_glossary-android.md).
```

Reciprocal KME/ZT mutex callout (BACK-link to ZT — verbatim shape per PATTERNS.md):
```markdown
> ⚠️ **KME/ZT mutual exclusion (Samsung):** For Samsung fleets, choose either Knox Mobile Enrollment (KME) or Zero-Touch Enrollment — never both. Configuring both causes out-of-sync enrollment state; KME takes precedence at the device firmware level. See [02-zero-touch-portal.md#kme-zt-mutual-exclusion](02-zero-touch-portal.md#kme-zt-mutual-exclusion) for the within-ZT-doc record and [KME/ZT Mutual Exclusion](#kme-zt-mutual-exclusion) below for the within-this-doc record.
```

D-02 Step 0 H2 (LOCKED VERBATIM TITLE):
```markdown
<a id="step-0-b2b-approval"></a>
## Step 0 — Samsung Knox Portal B2B account approval (1-2 business days)

Submit your Samsung Knox B2B account application TODAY (Day 0). Samsung approval typically takes 1-2 business days, and Knox Admin Portal access is gated behind approval.

**Submit your B2B account:**

1. Navigate to the [Samsung Knox Portal](https://www.samsungknox.com/en/solutions/it-solutions/knox-mobile-enrollment) and select **Get started** → **Apply for a B2B account**. <!-- verify UI at execute time -->
2. Submit the corporate-email-bound application; expect 1-2 business days for approval.
3. While waiting, productively use the wait window:
   - Read the rest of this guide (Steps 1-N) to align on Knox profile, EMM choice, and DPC JSON.
   - Identify the Samsung devices in scope: serial numbers, IMEI 1, manufacturer (Samsung), model.
   - Coordinate with your authorized Samsung reseller (existing-stock route) OR plan Knox Deployment App (existing-stock retroactive route).
   - Export the Intune enrollment token for use in Step N (DPC Custom JSON Data).

> **What breaks if misconfigured:** Submitting B2B late delays the entire enrollment cycle by 1-2 business days at the moment devices are needed. Recovery: submit on Day 0; if Samsung approval is delayed beyond 2 business days, contact Samsung Knox B2B support with the application ID. Symptom appears in: Knox Admin Portal sign-in returns "Account pending approval".
```

D-03 anti-paste blockquote (LOCKED VERBATIM — shared with ZT in Plan 07):
```markdown
<!-- AEKNOX-03-shared-anti-paste-block -->
> ⚠️ **DO NOT paste this JSON into the other portal**
> The KME and ZT DPC-extras JSON look similar but use different field names.
> Pasting ZT JSON into KME (or vice versa) silently produces a "stuck applying configuration" failure.
> If you maintain both portals: confirm the portal name in your browser tab before pasting.
<!-- /AEKNOX-03-shared-anti-paste-block -->
```

Knox Custom JSON Data (LOCKED VERBATIM from Microsoft Learn — FLAT, no wrapper):
```json
{"com.google.android.apps.work.clouddpc.EXTRA_ENROLLMENT_TOKEN": "enter Intune enrollment token string"}
```

D-01 5-SKU disambiguation table (LOCKED — H2 title and row order verbatim):
```markdown
<a id="sku-disambiguation"></a>
## Knox SKU disambiguation (5 SKUs)

The Samsung Knox SKU family is frequently confused. KME (the deliverable of this guide) is FREE and does NOT require a paid Knox license. KPE Premium has been free since Samsung's 2024-03-21 licensing update. Microsoft Intune Plan 1+ supplies KPE Premium transparently to enrolled Samsung devices. Knox Suite is a per-device bundle that gates advanced features beyond KPE Premium.

| SKU | KPE Standard | KPE Premium | Cost | Required for KME | Intune relationship |
|---|---|---|---|---|---|
| KME | — | — | Free | YES (the deliverable itself) | Knox portal → Intune handoff |
| KPE | Free baseline | Per-device upgrade (now free per Samsung 2024-03-21) | Free / Premium tier | NOT required for KME | Plan 1+ supplies Premium transparently |
| Knox Manage | — | — | Per-device | N/A — alternative MDM | Mutually exclusive with Intune |
| Knox Suite | — | — | Per-device bundle | NOT required for KME | Bundle that includes KPE Premium |
| Knox Configure | — | — | Per-device | NOT required for KME | Independent |
```

Per-portal H4 pattern (mirror ZT lines 46/70/81):
```markdown
#### In Knox Admin Portal

1. Navigate to [Knox Admin Portal](https://knox.samsung.com). <!-- verify UI at execute time -->
```

Inline What-breaks blockquote (verbatim shape from COBO line 79):
```markdown
   > **What breaks if misconfigured:** [specific cause]. Recovery: [specific recovery]. Symptom appears in: [Intune admin center | Knox Admin Portal | device-side observable].
```

Verification checklist (mirror ZT lines 198-205):
```markdown
## Verification

After Knox profile assignment + first-boot enrollment:

- [ ] Knox Admin Portal Devices view shows the Samsung device (after reseller upload OR Knox Deployment App enrollment) within 24 hours of upload.
- [ ] KME profile assigned to device set BEFORE first boot (assignment after first boot will not retro-trigger KME — device must be factory-reset).
- [ ] Custom JSON Data contains ONLY `{"com.google.android.apps.work.clouddpc.EXTRA_ENROLLMENT_TOKEN": "..."}` — no `PROVISIONING_ADMIN_EXTRAS_BUNDLE` wrapper.
- [ ] Test Samsung device boots into the Intune DPC via KME flow; appears in Intune admin center > Devices > Android within ~15 minutes of first-boot completion.
```

Renewal/Maintenance table (Knox-specific — adapt ZT lines 207-213 shape):
```markdown
<a id="renewal-maintenance"></a>
## Renewal / Maintenance

| Component | Renewal Period | Consequence of Lapse | Renewal Steps |
|---|---|---|---|
| Samsung Knox B2B account | Account-perpetual once approved; access tied to corporate email | Knox Admin Portal sign-in fails | Re-apply via Samsung Knox B2B portal; 1-2 business day approval |
| KME profile staging-token | Configurable per Intune enrollment profile | New KME enrollments fail; previously enrolled devices unaffected | Regenerate enrollment token in Intune admin center; update Custom JSON Data field in Knox Admin Portal profile |
| KPE Premium licensing | Free since Samsung 2024-03-21; Intune Plan 1+ supplies transparently | None expected at this time | Verify Plan 1+ activation via Knox Admin Portal device row license column |
```

See Also list (mirror ZT lines 215-222 shape):
```markdown
## See Also

- [Managed Google Play Binding](01-managed-google-play.md) — prerequisite for Knox Mobile Enrollment via Intune
- [Android Enterprise Admin Setup Overview](00-overview.md)
- [Zero-Touch Portal Configuration](02-zero-touch-portal.md) — for the KME/ZT mutual-exclusion record (Samsung devices choose ONE)
- [Android Enterprise Prerequisites](../android-lifecycle/01-android-prerequisites.md)
- [Android Provisioning Methods](../android-lifecycle/02-provisioning-methods.md#knox-mobile-enrollment)
- [Android Enterprise Provisioning Glossary](../_glossary-android.md#knox)
- [L1 Runbook 28: Android Knox Enrollment Failed](../l1-runbooks/28-android-knox-enrollment-failed.md)
- [L2 Runbook 22: Android Knox Investigation](../l2-runbooks/22-android-knox-investigation.md)
```

Changelog seed:
```markdown
## Changelog

| Date | Change | Author |
|------|--------|--------|
| <execute-date> | Initial version (Phase 44 scope) — Samsung Knox B2B account approval (Step 0 1-2 business day gate per D-02), reseller bulk upload + Knox Deployment App provisioning paths, KME EMM profile + Intune DPC Custom JSON Data (flat `EXTRA_ENROLLMENT_TOKEN` key per Microsoft Learn `setup-samsung-knox-mobile`), 5-SKU disambiguation table (D-01 5-SKU H2 lock), KME/ZT mutex (reciprocal of `02-zero-touch-portal.md#kme-zt-mutual-exclusion`), D-03 anti-paste blockquote (`AEKNOX-03-shared-anti-paste-block`). | -- |
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Knox admin guide skeleton with frontmatter, Platform gate, KME/ZT mutex callout, Prerequisites, and Step 0 (D-02)</name>
  <files>docs/admin-setup-android/07-knox-mobile-enrollment.md</files>
  <read_first>
    - .planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md (locked decisions D-01..D-04, especially D-02 Step 0 verbatim title)
    - .planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md (File 1 — verbatim copy-from-source markdown for frontmatter, Platform gate, Step 0, Prerequisites)
    - .planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md (§Common Pitfalls 1-6 for failure modes; §Code Examples for verbatim JSON; §Sibling Doc Pattern Analysis for line-by-line ZT mirror)
    - docs/admin-setup-android/02-zero-touch-portal.md (sibling structural model; lines 1-50)
    - docs/admin-setup-android/03-fully-managed-cobo.md (Prerequisites + What-breaks blockquote density model)
  </read_first>
  <behavior>
    - File MUST exist with Phase 34 admin template frontmatter (last_verified, review_by, audience: admin, platform: Android, applies_to: KME)
    - H1 = `# Configure Knox Mobile Enrollment` (parallel naming to ZT `# Configure Zero-Touch Portal`)
    - Platform gate blockquote present at line ~11-12 (Samsung-only KME phrasing per <interfaces>)
    - KME/ZT mutex blockquote present at line ~16 with BACK-link to `02-zero-touch-portal.md#kme-zt-mutual-exclusion` (NOT forward-link — this doc IS the destination)
    - `<a id="prerequisites"></a>` + `## Prerequisites` H2 with 5-item bulleted checklist (MGP binding, Samsung Knox B2B account, Samsung devices, reseller OR KDA pathway, Intune Plan 1+)
    - `<a id="step-0-b2b-approval"></a>` + `## Step 0 — Samsung Knox Portal B2B account approval (1-2 business days)` H2 (LOCKED VERBATIM TITLE)
    - Step 0 body tells admin to submit on Day 0, lists productive parallel work; includes one `> **What breaks if misconfigured:**` blockquote
  </behavior>
  <action>
    Use the Write tool to create `docs/admin-setup-android/07-knox-mobile-enrollment.md` with the following sections in order (use the verbatim markdown blocks in the `<interfaces>` block of `<context>`):

    1. **Frontmatter** — Phase 34 template; `applies_to: KME`; `last_verified` = today's date (execute date in YYYY-MM-DD); `review_by` = today + 60 days. Use `node -e "console.log(new Date(Date.now()+60*86400000).toISOString().slice(0,10))"` to compute review_by.
    2. **H1**: `# Configure Knox Mobile Enrollment` (line 9, single blank line after frontmatter).
    3. **Platform gate blockquote** at line 11-12 — verbatim from `<interfaces>` block.
    4. **KME/ZT mutex blockquote** at line ~16 — verbatim from `<interfaces>` block. CRITICAL: this is the BACK-link form (points to ZT doc + within-this-doc anchor), NOT the forward form ZT doc currently has.
    5. **Phase scope sentence** at line ~18: `Phase 44 scope: Samsung Knox Admin Portal B2B account, EMM profile, Knox Custom JSON Data, and Knox→Intune handoff. KME provisions Samsung devices into existing Android Enterprise modes (COBO / Dedicated / WPCO); KME is NOT a separate enrollment mode.`
    6. **`<a id="prerequisites"></a>` + `## Prerequisites`** H2 with bulleted checklist (5 items per Knox doc Prerequisites in `<interfaces>` block — MGP binding cross-link, Samsung Knox B2B account forward-link to Step 0, Samsung devices Knox-eligible hardware, reseller OR Knox Deployment App, Intune Plan 1+ Administrator role).
    7. **`<a id="step-0-b2b-approval"></a>` + `## Step 0 — Samsung Knox Portal B2B account approval (1-2 business days)`** H2 — verbatim title (D-02 LOCKED). Body MUST tell admin to submit on Day 0, list productive parallel work, and include one `> **What breaks if misconfigured:**` blockquote — verbatim from `<interfaces>` block.

    NO content beyond Step 0 in this task — Steps 1-N, JSON, SKU table, Verification, Renewal, See Also, Changelog all deferred to Task 2.
  </action>
  <verify>
    <automated>test -f docs/admin-setup-android/07-knox-mobile-enrollment.md && grep -q "applies_to: KME" docs/admin-setup-android/07-knox-mobile-enrollment.md && grep -q '^# Configure Knox Mobile Enrollment$' docs/admin-setup-android/07-knox-mobile-enrollment.md && grep -q "Step 0 — Samsung Knox Portal B2B account approval (1-2 business days)" docs/admin-setup-android/07-knox-mobile-enrollment.md && grep -q '02-zero-touch-portal.md#kme-zt-mutual-exclusion' docs/admin-setup-android/07-knox-mobile-enrollment.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/admin-setup-android/07-knox-mobile-enrollment.md` returns 0
    - `grep -q "applies_to: KME"` matches (frontmatter has correct applies_to value)
    - `grep -q "audience: admin"` matches
    - `grep -qE "last_verified: 20[0-9]{2}-[0-9]{2}-[0-9]{2}"` matches (frontmatter date format)
    - `grep -qE "review_by: 20[0-9]{2}-[0-9]{2}-[0-9]{2}"` matches
    - `grep -q '^# Configure Knox Mobile Enrollment$'` matches (exact H1)
    - `grep -q 'Platform gate'` matches
    - `grep -q "KME/ZT mutual exclusion (Samsung)"` matches
    - `grep -q '02-zero-touch-portal.md#kme-zt-mutual-exclusion'` matches (back-link present)
    - `grep -q '<a id="prerequisites"></a>'` matches
    - `grep -q "Step 0 — Samsung Knox Portal B2B account approval (1-2 business days)"` matches (D-02 verbatim)
    - `grep -q '<a id="step-0-b2b-approval"></a>'` matches
    - `grep -q "What breaks if misconfigured"` matches at least 1 occurrence in Step 0
    - `! grep -q "SafetyNet"` (zero SafetyNet — audit C1)
    - `! grep -qE "supervis(ed|ion|or)"` (zero supervision — audit C2)
  </acceptance_criteria>
  <done>File exists with frontmatter, H1, Platform gate, KME/ZT back-link mutex callout, Prerequisites H2, and Step 0 H2 (D-02 verbatim title). All 14 acceptance criteria pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Setup Steps 1-N (Knox Admin Portal + Intune admin center per-portal H4s), DPC Custom JSON Data with D-03 anti-paste blockquote, KME/ZT Mutual Exclusion H2, Verification, Renewal, See Also, Changelog</name>
  <files>docs/admin-setup-android/07-knox-mobile-enrollment.md</files>
  <read_first>
    - docs/admin-setup-android/07-knox-mobile-enrollment.md (file from Task 1 — append below Step 0)
    - .planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md (D-01 5-SKU table, D-03 anti-paste, D-04 glossary cross-link targets)
    - .planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md (File 1 — Verification checklist, Renewal table, See Also, Changelog patterns)
    - .planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md (§Code Examples for verbatim Knox JSON; §Pitfall 5 for KME profile mismatch; §Pitfall 4 for reseller upload latency)
    - docs/admin-setup-android/02-zero-touch-portal.md (lines 88-130 DPC JSON area; lines 119-126 KME/ZT Mutual Exclusion H2; lines 198-228 Verification/Renewal/See Also/Changelog)
  </read_first>
  <behavior>
    - Steps 1-N (recommendation: Step 1 = Sign in to Knox Admin Portal; Step 2 = Configure EMM profile / select Microsoft Intune; Step 3 = Add devices via reseller upload OR Knox Deployment App; Step 4 = Assign profile to device set; Step 5 = First-boot enrollment) — at least 4 numbered Step H2 sections beyond Step 0
    - Each Step has at least one `#### In Knox Admin Portal` or `#### In Intune admin center` per-portal sub-H4
    - At least 5 inline `> **What breaks if misconfigured:**` blockquotes across Steps 1-N (matching COBO doc density of 6 callouts at lines 79/89/93/119/121/123)
    - DPC Custom JSON Data section with FLAT JSON `{"com.google.android.apps.work.clouddpc.EXTRA_ENROLLMENT_TOKEN": "..."}` (no wrapper) and the `<!-- AEKNOX-03-shared-anti-paste-block -->` HTML-comment-wrapped blockquote IMMEDIATELY ABOVE the JSON code block
    - `<a id="sku-disambiguation"></a>` + `## Knox SKU disambiguation (5 SKUs)` H2 with the LOCKED 5-row table verbatim from D-01
    - `<a id="kme-zt-mutual-exclusion"></a>` + `## KME/ZT Mutual Exclusion (Samsung)` H2 with KME-side framing (KME takes precedence)
    - `## Verification` H2 with 4-item checklist matching `<interfaces>` block
    - `<a id="renewal-maintenance"></a>` + `## Renewal / Maintenance` H2 with 3-row table
    - `## See Also` list with cross-links to L1 28, L2 22, MGP, ZT, glossary anchors `#knox`, `#kme`, `#kpe`
    - `## Changelog` with Phase 44 seed entry
    - `## What Breaks Summary` table (per COBO supplementary analog) at end before See Also — Knox-specific misconfigurations table
  </behavior>
  <action>
    Use the Edit tool to append the following sections AFTER the Step 0 H2 created in Task 1, in this order:

    1. **Step 1 — Sign in to Knox Admin Portal**: `<a id="step-1-knox-portal"></a>` + H2; under it `#### In Knox Admin Portal` with numbered sign-in steps; one `> **What breaks if misconfigured:**` callout.
    2. **Step 2 — Create KME EMM profile (target Microsoft Intune)**: `<a id="step-2-emm-profile"></a>` + H2; `#### In Knox Admin Portal` with profile-creation steps; one What-breaks callout for "wrong EMM choice" (per Pitfall 5 — KME profile mismatch).
    3. **Step 3 — Add devices: reseller bulk upload OR Knox Deployment App**: `<a id="step-3-add-devices"></a>` + H2; sub-H3s `### Reseller bulk upload (new procurement)` and `### Knox Deployment App (existing stock)` per RESEARCH §Open Question 6 dual-path. Each sub-H3 has one What-breaks callout (Pitfall 4 — reseller upload latency).
    4. **Step 4 — Assign KME profile to device set**: `<a id="step-4-assign-profile"></a>` + H2; `#### In Knox Admin Portal`; one What-breaks callout — "If profile assigned AFTER first boot, KME does not retro-trigger; device must be factory-reset."
    5. **Step 5 — DPC Custom JSON Data**: `<a id="dpc-custom-json"></a>` + H2; introduction sentence; D-03 anti-paste blockquote VERBATIM (HTML-comment-wrapped) IMMEDIATELY ABOVE; FLAT JSON code block VERBATIM `{"com.google.android.apps.work.clouddpc.EXTRA_ENROLLMENT_TOKEN": "enter Intune enrollment token string"}`; field-reference text noting NO `PROVISIONING_ADMIN_EXTRAS_BUNDLE` wrapper (contrast with ZT JSON).
    6. **Step 6 — Verify first-boot enrollment**: `<a id="step-6-first-boot"></a>` + H2; `#### In Intune admin center`; one What-breaks callout — "If device shows wrong mode in Intune (Fully Managed vs Dedicated), the KME profile EMM template chose the wrong mode at Step 2."
    7. **`<a id="sku-disambiguation"></a>` + `## Knox SKU disambiguation (5 SKUs)`** — D-01 LOCKED 5-row table verbatim from `<interfaces>` block of `<context>`. Include the framing paragraph above the table ("KME is FREE... Intune Plan 1+ supplies KPE Premium transparently...").
    8. **`<a id="kme-zt-mutual-exclusion"></a>` + `## KME/ZT Mutual Exclusion (Samsung)`** — H2 body explains: "From the KME side: when both KME and ZT are configured on the same Samsung device, KME takes precedence at the device firmware level. To apply Zero-Touch on a Samsung device, remove the KME configuration for that device set in Knox Admin Portal." Cross-link to ZT doc's mutex anchor.
    9. **`## Verification`** — verbatim 4-item checklist from `<interfaces>` block.
    10. **`<a id="renewal-maintenance"></a>` + `## Renewal / Maintenance`** — verbatim 3-row table from `<interfaces>` block.
    11. **`## What Breaks Summary`** — Knox-specific table (model on COBO lines 197-216 shape) listing 5 misconfigurations: B2B account not approved before Day 0 (CRITICAL); KME profile EMM not Intune (HIGH); DPC JSON copy-pasted from ZT (HIGH — silent failure); Knox tripped (CRITICAL — non-recoverable for KME); KME/ZT dual-configured on Samsung (HIGH).
    12. **`## See Also`** — verbatim cross-link list from `<interfaces>` block.
    13. **`## Changelog`** — single-row Phase 44 seed entry verbatim from `<interfaces>` block.

    CRITICAL placement rules:
    - The `<!-- AEKNOX-03-shared-anti-paste-block -->` blockquote MUST sit IMMEDIATELY ABOVE the FLAT JSON code block (the JSON-paste step is the anti-paste warning's point-of-action target per D-03).
    - The 5-SKU H2 title MUST match `## Knox SKU disambiguation (5 SKUs)` exactly (whitespace and parens — REQUIREMENTS line 21 literal).
    - Use em-dash `—` in non-KPE Standard|Premium cells (no blanks; no `N/A` except Knox Manage's `N/A — alternative MDM`).
    - The Knox JSON contains `EXTRA_ENROLLMENT_TOKEN` and does NOT contain `PROVISIONING_ADMIN_EXTRAS_BUNDLE`.
    - Zero `SafetyNet` references (audit C1).
    - Zero `supervision` / `supervised` references (audit C2 — KME is Samsung-only; no iOS bridge prose per locked context).
  </action>
  <verify>
    <automated>grep -q "Knox SKU disambiguation (5 SKUs)" docs/admin-setup-android/07-knox-mobile-enrollment.md && grep -q "AEKNOX-03-shared-anti-paste-block" docs/admin-setup-android/07-knox-mobile-enrollment.md && grep -q "EXTRA_ENROLLMENT_TOKEN" docs/admin-setup-android/07-knox-mobile-enrollment.md && ! grep -q "PROVISIONING_ADMIN_EXTRAS_BUNDLE" docs/admin-setup-android/07-knox-mobile-enrollment.md && grep -c "What breaks if misconfigured" docs/admin-setup-android/07-knox-mobile-enrollment.md | awk '$1 >= 5 {exit 0} {exit 1}' && ! grep -q "SafetyNet" docs/admin-setup-android/07-knox-mobile-enrollment.md && ! grep -qE "supervis(ed|ion|or)" docs/admin-setup-android/07-knox-mobile-enrollment.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Knox SKU disambiguation (5 SKUs)"` matches (D-01 LOCKED H2 verbatim)
    - `grep -c "<!-- AEKNOX-03-shared-anti-paste-block -->"` returns at least 1 (anti-paste opening marker present)
    - `grep -c "<!-- /AEKNOX-03-shared-anti-paste-block -->"` returns at least 1 (anti-paste closing marker present)
    - `grep -q '"com.google.android.apps.work.clouddpc.EXTRA_ENROLLMENT_TOKEN"'` matches (FLAT JSON form)
    - `! grep -q "PROVISIONING_ADMIN_EXTRAS_BUNDLE"` (no ZT wrapper key — D-03 enforcement)
    - `grep -c "What breaks if misconfigured"` returns at least 5 (matches COBO density)
    - `grep -c "<a id=\"step-"` returns at least 6 (Step 0 from Task 1 + Steps 1-6 from Task 2 = 7; allow 6+ for flexibility)
    - `grep -q "<a id=\"sku-disambiguation\"></a>"` matches
    - `grep -q "<a id=\"kme-zt-mutual-exclusion\"></a>"` matches
    - `grep -q "## Verification"` matches
    - `grep -q "<a id=\"renewal-maintenance\"></a>"` matches
    - `grep -q "## What Breaks Summary"` matches
    - `grep -q "## See Also"` matches
    - `grep -q "## Changelog"` matches
    - Table row check: `grep -E "^\| KME \| — \| —" docs/admin-setup-android/07-knox-mobile-enrollment.md` matches (KME row of 5-SKU table)
    - Table row check: `grep -E "^\| KPE \| Free baseline" docs/admin-setup-android/07-knox-mobile-enrollment.md` matches (KPE row of 5-SKU table)
    - Table row check: `grep -E "^\| Knox Manage \|" docs/admin-setup-android/07-knox-mobile-enrollment.md` matches
    - Table row check: `grep -E "^\| Knox Suite \|" docs/admin-setup-android/07-knox-mobile-enrollment.md` matches
    - Table row check: `grep -E "^\| Knox Configure \|" docs/admin-setup-android/07-knox-mobile-enrollment.md` matches
    - `! grep -q "SafetyNet"` (audit C1)
    - `! grep -qE "supervis(ed|ion|or)"` (audit C2)
    - File total line count `wc -l` returns at least 200 (substantial admin guide; min_lines per must_haves)
  </acceptance_criteria>
  <done>File contains complete admin guide skeleton (Steps 0-6, DPC JSON with anti-paste, 5-SKU table, KME/ZT mutex H2, Verification, Renewal, What Breaks Summary, See Also, Changelog). All 21 acceptance criteria pass. Audit harness `node scripts/validation/v1.4.1-milestone-audit.mjs` runs without C1/C2/C5 regressions on this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown source → published doc | Content authored here is consumed by IT admins as authoritative procedure for KME setup |
| Knox/KME/KPE prose → Samsung product reality | Pricing/feature claims must reflect Samsung Knox SKU state at last_verified date; drift creates misinformation |
| DPC JSON example → admin paste action | The verbatim FLAT JSON is the operational artifact admins paste; drift causes silent enrollment failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Information Disclosure (incorrect) | 5-SKU H2 table (KPE Premium "free" claim) | mitigate | Embed 60-day freshness frontmatter (`last_verified` + `review_by`); cite Samsung 2024-03-21 update inline in KPE row Premium cell; researcher OQ-1 verification at execute time per RESEARCH §Open Questions; audit harness C5 enforces re-verification cadence |
| T-44-02 | Information Disclosure | Step 0 H2 ("1-2 business days") | mitigate | Cite Microsoft Learn `setup-samsung-knox-mobile` (updated 2026-04-14) as source-of-truth in research notes; researcher OQ-2 re-verification at execute time; if Samsung shifts timing, surgical edit to Step 0 title text only |
| T-44-03 | Tampering (link-rot) | Microsoft Learn / Samsung Knox URLs in Prerequisites and Step 1-3 | mitigate | Verify URLs at execute time via Bash curl HEAD checks; cite ms.date from Microsoft Learn frontmatter; URL liveness re-checked on next 60-day freshness cycle |
| T-44-04 | Tampering (silent drift) | DPC anti-paste blockquote (D-03) | mitigate | HTML-comment marker `<!-- AEKNOX-03-shared-anti-paste-block -->` enables `grep -r AEKNOX-03-shared-anti-paste-block docs/` drift detection; identical text required in this doc and ZT doc (Plan 07 retrofits ZT) |
</threat_model>

<verification>
After both tasks complete:

1. Run audit harness: `node scripts/validation/v1.4.1-milestone-audit.mjs` — expect 8/8 PASS (5 mechanical + 3 informational); no new C1/C2/C5 failures introduced by this plan.
2. Spot-check the AEKNOX-01 unit-grep predicate (from VALIDATION.md):
   ```bash
   test -f docs/admin-setup-android/07-knox-mobile-enrollment.md && \
     grep -q "Knox SKU disambiguation (5 SKUs)" docs/admin-setup-android/07-knox-mobile-enrollment.md && \
     grep -q "Step 0 — Samsung Knox Portal B2B account approval" docs/admin-setup-android/07-knox-mobile-enrollment.md && \
     grep -q "AEKNOX-03-shared-anti-paste-block" docs/admin-setup-android/07-knox-mobile-enrollment.md
   ```
   Expected: exit 0.
3. Manual visual check: render the file in a markdown previewer; confirm 5-SKU table renders as 5 rows with KPE row showing adjacent Standard|Premium columns.
</verification>

<success_criteria>
- File `docs/admin-setup-android/07-knox-mobile-enrollment.md` exists with all 13+ sections (frontmatter, H1, Platform gate, KME/ZT mutex back-link callout, Prerequisites, Step 0, Steps 1-6, DPC Custom JSON Data with anti-paste, 5-SKU H2, KME/ZT Mutual Exclusion H2, Verification, Renewal, What Breaks Summary, See Also, Changelog)
- 5-SKU H2 title is exactly `## Knox SKU disambiguation (5 SKUs)` (D-01 LOCKED)
- Step 0 H2 title is exactly `## Step 0 — Samsung Knox Portal B2B account approval (1-2 business days)` (D-02 LOCKED)
- D-03 anti-paste blockquote present, wrapped by `<!-- AEKNOX-03-shared-anti-paste-block -->` / `<!-- /AEKNOX-03-shared-anti-paste-block -->` HTML comments, immediately above the FLAT JSON code block
- FLAT JSON contains `EXTRA_ENROLLMENT_TOKEN` and does NOT contain `PROVISIONING_ADMIN_EXTRAS_BUNDLE`
- ≥5 inline `> **What breaks if misconfigured:**` blockquotes (matching COBO density)
- Zero SafetyNet references (audit C1) and zero supervision references (audit C2)
- Audit harness `node scripts/validation/v1.4.1-milestone-audit.mjs` passes 8/8 with this file present
- AEKNOX-01 unit-grep predicate from VALIDATION.md exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/44-knox-mobile-enrollment/44-01-SUMMARY.md` documenting:
- File created (path + line count)
- Locked decisions implemented (D-01, D-02, D-03 per task)
- Audit harness state (8/8 PASS expected)
- Foundational dependency for Plans 02-07 (every subsequent plan cross-links here)
</output>
