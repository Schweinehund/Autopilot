---
phase: 44-knox-mobile-enrollment
plan: 07
type: execute
wave: 4
depends_on:
  - 44-01
  - 44-02
  - 44-03
  - 44-04
  - 44-05
  - 44-06
files_modified:
  - docs/admin-setup-android/02-zero-touch-portal.md
  - docs/admin-setup-android/03-fully-managed-cobo.md
autonomous: true
requirements:
  - AEKNOX-07
must_haves:
  truths:
    - "02-zero-touch-portal.md line ~16 KME/ZT mutex callout forward-links to 07-knox-mobile-enrollment.md (closes v1.4 'tracked for v1.4.1' forward-promise)"
    - "03-fully-managed-cobo.md line ~162 Samsung-admins callout forward-links to 07-knox-mobile-enrollment.md (closes v1.4 'deferred to v1.4.1' forward-promise)"
    - "02-zero-touch-portal.md DPC JSON paste step has the AEKNOX-03-shared-anti-paste-block HTML-comment-wrapped blockquote inserted IMMEDIATELY ABOVE the JSON code block at line ~93"
    - "Anti-paste blockquote text is IDENTICAL between 07-knox-mobile-enrollment.md and 02-zero-touch-portal.md (D-03 lock; diff returns empty)"
    - "Zero 'tracked for v1.4.1' or 'deferred to v1.4.1' wording remains in either modified file (Knox-related lines only)"
    - "Frontmatter dates of both modified files shifted to execute date and review_by + 60d"
  artifacts:
    - path: docs/admin-setup-android/02-zero-touch-portal.md
      provides: "Reciprocal forward-link from KME/ZT mutex callout to admin doc 07; AEKNOX-03 shared anti-paste blockquote inserted at JSON-paste step"
      contains:
        - "07-knox-mobile-enrollment.md"
        - "AEKNOX-03-shared-anti-paste-block"
    - path: docs/admin-setup-android/03-fully-managed-cobo.md
      provides: "Reciprocal forward-link from Samsung-admins callout to admin doc 07"
      contains:
        - "07-knox-mobile-enrollment.md"
  key_links:
    - from: docs/admin-setup-android/02-zero-touch-portal.md (line ~16 KME/ZT mutex callout)
      to: docs/admin-setup-android/07-knox-mobile-enrollment.md
      via: Forward-link in mutex blockquote (closes v1.4 forward-promise)
      pattern: "07-knox-mobile-enrollment.md"
    - from: docs/admin-setup-android/03-fully-managed-cobo.md (line ~162 Samsung-admins callout)
      to: docs/admin-setup-android/07-knox-mobile-enrollment.md
      via: Forward-link in Samsung-admins blockquote (closes v1.4 forward-promise)
      pattern: "07-knox-mobile-enrollment.md"
    - from: docs/admin-setup-android/02-zero-touch-portal.md (anti-paste blockquote)
      to: docs/admin-setup-android/07-knox-mobile-enrollment.md (anti-paste blockquote inserted in Plan 01)
      via: AEKNOX-03 shared HTML-comment-marked block (D-03 drift detection)
      pattern: "AEKNOX-03-shared-anti-paste-block"
---

<objective>
Surgical edits to TWO existing sibling admin docs to close the v1.4 forward-promises and ship the D-03 shared anti-paste blockquote in the ZT doc:

1. `docs/admin-setup-android/02-zero-touch-portal.md` — line ~16 KME/ZT mutex callout: replace `Full KME coverage is tracked for v1.4.1.` with a forward-link to `07-knox-mobile-enrollment.md`. Insert the AEKNOX-03-shared-anti-paste-block blockquote IMMEDIATELY ABOVE the DPC JSON code block at line ~93 (D-03 — shared with Plan 01 admin doc 07).

2. `docs/admin-setup-android/03-fully-managed-cobo.md` — line ~162 Samsung-admins callout: replace `Full KME coverage is deferred to v1.4.1.` with a forward-link to `07-knox-mobile-enrollment.md`.

Purpose: Fulfill AEKNOX-07 — Phase 44's terminal closure of the v1.4 forward-promises pinned at ZT line 16 and COBO line 162. Plan 07 must run AFTER Plans 01-06 because the forward-link target file (admin doc 07) is created in Plan 01, and the anti-paste block content must be IDENTICAL to the one in admin doc 07.

Output: Surgical edits to TWO existing files; no new files; bounded to specific line ranges + frontmatter dates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md
@.planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md
@.planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md
@.planning/phases/44-knox-mobile-enrollment/44-VALIDATION.md
@docs/admin-setup-android/02-zero-touch-portal.md
@docs/admin-setup-android/03-fully-managed-cobo.md
@docs/admin-setup-android/07-knox-mobile-enrollment.md

KEY VERBATIM SOURCE-OF-TRUTH (sourced from PATTERNS.md File 8 + File 9 + RESEARCH §5 Pins 1-2):

**ZT line 16 — existing KME/ZT mutex callout (verbatim — to be EDITED):**
```
> ⚠️ **KME/ZT mutual exclusion (Samsung):** For Samsung fleets, choose either Knox Mobile Enrollment (KME) or Zero-Touch Enrollment — never both. Configuring both causes out-of-sync enrollment state. Full KME coverage is tracked for v1.4.1. See [KME/ZT Mutual Exclusion](#kme-zt-mutual-exclusion) below.
```

**Phase 44 ZT line 16 replacement (verbatim — closes v1.4 forward-promise):**
```
> ⚠️ **KME/ZT mutual exclusion (Samsung):** For Samsung fleets, choose either Knox Mobile Enrollment (KME) or Zero-Touch Enrollment — never both. Configuring both causes out-of-sync enrollment state. See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage and [KME/ZT Mutual Exclusion](#kme-zt-mutual-exclusion) below for the within-this-doc record.
```

**Diff:** Substring `Full KME coverage is tracked for v1.4.1. See [KME/ZT Mutual Exclusion](#kme-zt-mutual-exclusion) below.` → `See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage and [KME/ZT Mutual Exclusion](#kme-zt-mutual-exclusion) below for the within-this-doc record.`

**ZT DPC JSON paste step at lines 88-105 — anti-paste blockquote MUST be inserted immediately ABOVE the JSON code block (per PATTERNS.md "D-03 anti-paste insertion line 92 of ZT doc identified"):**

The current ZT structure at lines 88-105 is:
```
<a id="dpc-extras-json"></a>
## DPC Extras JSON Configuration

Paste the JSON below into the ZT portal configuration. Replace `YourEnrollmentToken` with the token exported from your Intune enrollment profile.

```json
{
  "android.app.extra.PROVISIONING_DEVICE_ADMIN_COMPONENT_NAME":
    ...
}
```

**Phase 44 INSERTS the anti-paste blockquote between the introductory paragraph "Paste the JSON below..." and the opening ` ```json ` fence (i.e., at line ~92 per PATTERNS.md). Block VERBATIM (must be identical to the one in admin doc 07 from Plan 01):**

```
<!-- AEKNOX-03-shared-anti-paste-block -->
> ⚠️ **DO NOT paste this JSON into the other portal**
> The KME and ZT DPC-extras JSON look similar but use different field names.
> Pasting ZT JSON into KME (or vice versa) silently produces a "stuck applying configuration" failure.
> If you maintain both portals: confirm the portal name in your browser tab before pasting.
<!-- /AEKNOX-03-shared-anti-paste-block -->
```

After insertion, the ZT structure at lines 88-110 becomes:
```
<a id="dpc-extras-json"></a>
## DPC Extras JSON Configuration

Paste the JSON below into the ZT portal configuration. Replace `YourEnrollmentToken` with the token exported from your Intune enrollment profile.

<!-- AEKNOX-03-shared-anti-paste-block -->
> ⚠️ **DO NOT paste this JSON into the other portal**
> The KME and ZT DPC-extras JSON look similar but use different field names.
> Pasting ZT JSON into KME (or vice versa) silently produces a "stuck applying configuration" failure.
> If you maintain both portals: confirm the portal name in your browser tab before pasting.
<!-- /AEKNOX-03-shared-anti-paste-block -->

```json
{
  "android.app.extra.PROVISIONING_DEVICE_ADMIN_COMPONENT_NAME":
    ...
}
```

---

**COBO line 162 — existing Samsung-admins callout (verbatim — to be EDITED):**
```
> ⚠️ **Samsung admins:** Choose Knox Mobile Enrollment (KME) or Zero-Touch — never both. Configuring both on the same devices causes out-of-sync enrollment state on Samsung hardware. Full KME coverage is deferred to v1.4.1. See [02-zero-touch-portal.md#kme-zt-mutual-exclusion](02-zero-touch-portal.md#kme-zt-mutual-exclusion) for the mutual-exclusion record and [_glossary-android.md#zero-touch-enrollment](../_glossary-android.md#zero-touch-enrollment) for the Zero-Touch definition and the iOS ADE cross-platform analog.
```

**Phase 44 COBO line 162 replacement (verbatim — closes v1.4 forward-promise):**
```
> ⚠️ **Samsung admins:** Choose Knox Mobile Enrollment (KME) or Zero-Touch — never both. Configuring both on the same devices causes out-of-sync enrollment state on Samsung hardware. See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage; [02-zero-touch-portal.md#kme-zt-mutual-exclusion](02-zero-touch-portal.md#kme-zt-mutual-exclusion) for the mutual-exclusion record; and [_glossary-android.md#zero-touch-enrollment](../_glossary-android.md#zero-touch-enrollment) for the Zero-Touch definition and the iOS ADE cross-platform analog.
```

**Diff:** Substring `Full KME coverage is deferred to v1.4.1. See [02-zero-touch-portal.md#kme-zt-mutual-exclusion]` → `See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage; [02-zero-touch-portal.md#kme-zt-mutual-exclusion]`. Replaces "Full KME coverage is deferred to v1.4.1. See" with "See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage;".

---

**Frontmatter shifts (Phase 43 D-22) for both files:**
- `last_verified:` to execute date
- `review_by:` to execute date + 60d

**Forbidden changes:**
- Do NOT modify any line in either file other than: frontmatter dates, line ~16 of ZT (mutex callout), line ~93 of ZT (anti-paste insertion), line ~162 of COBO (Samsung-admins callout)
- Do NOT modify any other Knox-related text elsewhere in either file (the existing KME/ZT Mutual Exclusion H2 at ZT line ~119-126 is preserved unchanged; the COBO Zero-Touch sub-section content above line 162 is preserved unchanged)
- The anti-paste block in ZT MUST be byte-identical to the one in admin doc 07 (Plan 01 output) — D-03 "diff returns empty" requirement
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Edit 02-zero-touch-portal.md — close KME/ZT mutex forward-promise at line ~16; insert AEKNOX-03 anti-paste block at line ~93; shift frontmatter dates</name>
  <files>docs/admin-setup-android/02-zero-touch-portal.md</files>
  <read_first>
    - .planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md (D-03 anti-paste lock; AEKNOX-07 reciprocal pin scope)
    - .planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md (File 8 — verbatim line 16 edit + line 93 anti-paste insertion + identity-with-admin-doc-07 requirement)
    - .planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md (§5 Pin 1 — diff sketch for line 16)
    - docs/admin-setup-android/02-zero-touch-portal.md (lines 1-7 frontmatter; line 16 mutex callout; lines 88-105 DPC JSON paste step)
    - docs/admin-setup-android/07-knox-mobile-enrollment.md (Plan 01 output — confirms admin doc exists; READ the AEKNOX-03 anti-paste blockquote inserted by Plan 01 to copy byte-for-byte for IDENTITY requirement)
  </read_first>
  <behavior>
    - Frontmatter dates shifted (Phase 43 D-22)
    - Line ~16 mutex callout has the substring `Full KME coverage is tracked for v1.4.1. See [KME/ZT Mutual Exclusion](#kme-zt-mutual-exclusion) below.` replaced with `See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage and [KME/ZT Mutual Exclusion](#kme-zt-mutual-exclusion) below for the within-this-doc record.`
    - AEKNOX-03 shared anti-paste blockquote (HTML-comment-wrapped) inserted at line ~93 — IMMEDIATELY ABOVE the ` ```json ` fence; IMMEDIATELY BELOW the introductory paragraph "Paste the JSON below..."
    - Anti-paste block content is byte-identical to the one in admin doc 07 (D-03 "diff returns empty" lock)
    - Line ~119-126 KME/ZT Mutual Exclusion H2 unchanged
    - Existing JSON code block + Fields reference table preserved verbatim
  </behavior>
  <action>
    Use the Edit tool to make these surgical edits to `docs/admin-setup-android/02-zero-touch-portal.md`:

    **Edit 1 — Frontmatter dates:**
    Compute dates as in prior plans:
    `node -e "const d=new Date(); console.log('last_verified: '+d.toISOString().slice(0,10)); d.setDate(d.getDate()+60); console.log('review_by: '+d.toISOString().slice(0,10))"`
    - Replace `last_verified: <old>` with `last_verified: <execute-date>`
    - Replace `review_by: <old>` with `review_by: <execute-date + 60d>`

    **Edit 2 — Line ~16 KME/ZT mutex callout substring replacement (closes forward-promise):**
    Find this exact substring within the line ~16 blockquote:
    ```
    Full KME coverage is tracked for v1.4.1. See [KME/ZT Mutual Exclusion](#kme-zt-mutual-exclusion) below.
    ```

    Replace with:
    ```
    See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage and [KME/ZT Mutual Exclusion](#kme-zt-mutual-exclusion) below for the within-this-doc record.
    ```

    The rest of the blockquote (`> ⚠️ **KME/ZT mutual exclusion (Samsung):**` opener through `Configuring both causes out-of-sync enrollment state.`) is preserved unchanged.

    Result line:
    ```
    > ⚠️ **KME/ZT mutual exclusion (Samsung):** For Samsung fleets, choose either Knox Mobile Enrollment (KME) or Zero-Touch Enrollment — never both. Configuring both causes out-of-sync enrollment state. See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage and [KME/ZT Mutual Exclusion](#kme-zt-mutual-exclusion) below for the within-this-doc record.
    ```

    **Edit 3 — AEKNOX-03 anti-paste blockquote insertion at line ~93 (D-03 LOCK):**

    Step 1: READ admin doc 07 (Plan 01 output) to extract the anti-paste blockquote VERBATIM. Use the Read tool on `docs/admin-setup-android/07-knox-mobile-enrollment.md` and locate the block bracketed by `<!-- AEKNOX-03-shared-anti-paste-block -->` and `<!-- /AEKNOX-03-shared-anti-paste-block -->`. Copy the entire block (opening comment through closing comment, inclusive) to use as the SOURCE for byte-identical insertion in ZT.

    Step 2: Find the line in ZT doc reading `Paste the JSON below into the ZT portal configuration. Replace \`YourEnrollmentToken\` with the token exported from your Intune enrollment profile.` (currently around line 91). Insert one blank line after this paragraph, then insert the byte-identical anti-paste block from admin doc 07. Insert another blank line after the closing `<!-- /AEKNOX-03-shared-anti-paste-block -->`. Then the existing ` ```json ` fence + JSON body + closing ``` ` fence follows verbatim.

    Final structure at lines ~88-110:
    ```
    <a id="dpc-extras-json"></a>
    ## DPC Extras JSON Configuration

    Paste the JSON below into the ZT portal configuration. Replace `YourEnrollmentToken` with the token exported from your Intune enrollment profile.

    <!-- AEKNOX-03-shared-anti-paste-block -->
    > ⚠️ **DO NOT paste this JSON into the other portal**
    > The KME and ZT DPC-extras JSON look similar but use different field names.
    > Pasting ZT JSON into KME (or vice versa) silently produces a "stuck applying configuration" failure.
    > If you maintain both portals: confirm the portal name in your browser tab before pasting.
    <!-- /AEKNOX-03-shared-anti-paste-block -->

    ```json
    {
      "android.app.extra.PROVISIONING_DEVICE_ADMIN_COMPONENT_NAME":
        ...
    }
    ```
    ```

    **D-03 IDENTITY VERIFICATION:** After insertion, run:
    ```bash
    diff <(sed -n '/AEKNOX-03-shared-anti-paste-block/,/AEKNOX-03-shared-anti-paste-block -->/p' docs/admin-setup-android/07-knox-mobile-enrollment.md) <(sed -n '/AEKNOX-03-shared-anti-paste-block/,/AEKNOX-03-shared-anti-paste-block -->/p' docs/admin-setup-android/02-zero-touch-portal.md)
    ```
    Expected: empty output (zero differences). If diff returns ANY content, re-copy from admin doc 07 and retry.

    Do NOT modify any other lines in 02-zero-touch-portal.md.
  </action>
  <verify>
    <automated>grep -A2 "KME/ZT mutual exclusion" docs/admin-setup-android/02-zero-touch-portal.md | grep -q "07-knox-mobile-enrollment.md" && grep -q "AEKNOX-03-shared-anti-paste-block" docs/admin-setup-android/02-zero-touch-portal.md && grep -q "/AEKNOX-03-shared-anti-paste-block" docs/admin-setup-android/02-zero-touch-portal.md && ! grep -q "Full KME coverage is tracked for v1.4.1" docs/admin-setup-android/02-zero-touch-portal.md && [ -z "$(diff <(sed -n '/AEKNOX-03-shared-anti-paste-block -->/,/\/AEKNOX-03-shared-anti-paste-block/p' docs/admin-setup-android/07-knox-mobile-enrollment.md) <(sed -n '/AEKNOX-03-shared-anti-paste-block -->/,/\/AEKNOX-03-shared-anti-paste-block/p' docs/admin-setup-android/02-zero-touch-portal.md))" ]</automated>
  </verify>
  <acceptance_criteria>
    - Forward-link to admin doc 07 in line ~16 mutex callout: `grep -A2 "KME/ZT mutual exclusion" docs/admin-setup-android/02-zero-touch-portal.md | grep -q "07-knox-mobile-enrollment.md"`
    - Old "tracked for v1.4.1" wording removed: `! grep -q "Full KME coverage is tracked for v1.4.1" docs/admin-setup-android/02-zero-touch-portal.md`
    - AEKNOX-03 opening marker present: `grep -c "<!-- AEKNOX-03-shared-anti-paste-block -->" docs/admin-setup-android/02-zero-touch-portal.md` returns at least 1
    - AEKNOX-03 closing marker present: `grep -c "<!-- /AEKNOX-03-shared-anti-paste-block -->" docs/admin-setup-android/02-zero-touch-portal.md` returns at least 1
    - Anti-paste block contains the canonical "DO NOT paste this JSON into the other portal" text: `grep -q "DO NOT paste this JSON into the other portal" docs/admin-setup-android/02-zero-touch-portal.md`
    - Anti-paste block sits BEFORE the JSON code block: line number of `<!-- AEKNOX-03-shared-anti-paste-block -->` < line number of first `\`\`\`json` fence after `## DPC Extras JSON Configuration`. Verify:
      ```bash
      ANTI=$(grep -n "<!-- AEKNOX-03-shared-anti-paste-block -->" docs/admin-setup-android/02-zero-touch-portal.md | head -1 | cut -d: -f1)
      JSON=$(grep -n "^\`\`\`json$" docs/admin-setup-android/02-zero-touch-portal.md | head -1 | cut -d: -f1)
      [ "$ANTI" -lt "$JSON" ]
      ```
    - **D-03 IDENTITY (CRITICAL):** anti-paste block byte-identical between admin doc 07 and ZT doc:
      ```bash
      diff <(sed -n '/<!-- AEKNOX-03-shared-anti-paste-block -->/,/<!-- \/AEKNOX-03-shared-anti-paste-block -->/p' docs/admin-setup-android/07-knox-mobile-enrollment.md) <(sed -n '/<!-- AEKNOX-03-shared-anti-paste-block -->/,/<!-- \/AEKNOX-03-shared-anti-paste-block -->/p' docs/admin-setup-android/02-zero-touch-portal.md)
      ```
      Expected: empty output (exit 0).
    - Existing KME/ZT Mutual Exclusion H2 at line ~119 preserved: `grep -q '<a id="kme-zt-mutual-exclusion"></a>' docs/admin-setup-android/02-zero-touch-portal.md` matches AND the H2 body is unchanged (verify by checking line count of file is original + ~7 lines for anti-paste + 0 for line-16 substring swap = expected delta)
    - Existing JSON code block content preserved: `grep -q "PROVISIONING_DEVICE_ADMIN_COMPONENT_NAME" docs/admin-setup-android/02-zero-touch-portal.md` AND `grep -q "PROVISIONING_ADMIN_EXTRAS_BUNDLE" docs/admin-setup-android/02-zero-touch-portal.md` (the ZT-form JSON wrapper is the contrast point that the anti-paste block warns about)
    - Frontmatter dates shifted: `grep -qE "last_verified: 20[0-9]{2}-[0-9]{2}-[0-9]{2}"`
  </acceptance_criteria>
  <done>ZT line 16 forward-link closes v1.4 forward-promise; AEKNOX-03 anti-paste block inserted at line ~93 byte-identical to admin doc 07; frontmatter shifted. All 10 acceptance criteria pass; D-03 IDENTITY diff is empty.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Edit 03-fully-managed-cobo.md — close Samsung-admins forward-promise at line ~162; shift frontmatter dates</name>
  <files>docs/admin-setup-android/03-fully-managed-cobo.md</files>
  <read_first>
    - .planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md (AEKNOX-07 reciprocal pin scope)
    - .planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md (File 9 — verbatim line 162 edit)
    - .planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md (§5 Pin 2 — diff sketch for line 162)
    - docs/admin-setup-android/03-fully-managed-cobo.md (lines 1-7 frontmatter; line 162 Samsung-admins callout)
    - docs/admin-setup-android/07-knox-mobile-enrollment.md (Plan 01 output — confirms admin doc 07 exists; verifies cross-link target)
  </read_first>
  <behavior>
    - Frontmatter dates shifted (Phase 43 D-22)
    - Line ~162 Samsung-admins callout has the substring `Full KME coverage is deferred to v1.4.1. See [02-zero-touch-portal.md#kme-zt-mutual-exclusion]` replaced with `See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage; [02-zero-touch-portal.md#kme-zt-mutual-exclusion]`
    - The rest of the blockquote (Samsung admins opener through Zero-Touch definition cross-link tail) is preserved verbatim
    - Zero "deferred to v1.4.1" wording related to KME remains in this file
  </behavior>
  <action>
    Use the Edit tool to make these surgical edits to `docs/admin-setup-android/03-fully-managed-cobo.md`:

    **Edit 1 — Frontmatter dates:**
    Compute dates as in prior plans. Replace `last_verified` and `review_by` lines.

    **Edit 2 — Line ~162 Samsung-admins callout substring replacement (closes forward-promise):**
    Find this exact substring within the line ~162 blockquote:
    ```
    Full KME coverage is deferred to v1.4.1. See [02-zero-touch-portal.md#kme-zt-mutual-exclusion](02-zero-touch-portal.md#kme-zt-mutual-exclusion)
    ```

    Replace with:
    ```
    See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage; [02-zero-touch-portal.md#kme-zt-mutual-exclusion](02-zero-touch-portal.md#kme-zt-mutual-exclusion)
    ```

    Result line ~162:
    ```
    > ⚠️ **Samsung admins:** Choose Knox Mobile Enrollment (KME) or Zero-Touch — never both. Configuring both on the same devices causes out-of-sync enrollment state on Samsung hardware. See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage; [02-zero-touch-portal.md#kme-zt-mutual-exclusion](02-zero-touch-portal.md#kme-zt-mutual-exclusion) for the mutual-exclusion record and [_glossary-android.md#zero-touch-enrollment](../_glossary-android.md#zero-touch-enrollment) for the Zero-Touch definition and the iOS ADE cross-platform analog.
    ```

    NOTE: The original line uses connector "for the mutual-exclusion record and"; the replacement preserves that wording. The substring replacement must keep the mutex-record + glossary cross-links unchanged. Verify the EXACT text after insertion matches the result line above.

    Do NOT modify any other lines in 03-fully-managed-cobo.md. The Zero-Touch sub-section content above line 162 (lines 158-160 of `### Zero-Touch` H3) is preserved unchanged. The `> **What breaks if misconfigured:**` blockquote at line 164 is preserved unchanged.
  </action>
  <verify>
    <automated>grep -A2 "Samsung admins" docs/admin-setup-android/03-fully-managed-cobo.md | grep -q "07-knox-mobile-enrollment.md" && ! grep -q "Full KME coverage is deferred to v1.4.1" docs/admin-setup-android/03-fully-managed-cobo.md && grep -q "Knox Mobile Enrollment (KME) or Zero-Touch — never both" docs/admin-setup-android/03-fully-managed-cobo.md && grep -q "kme-zt-mutual-exclusion" docs/admin-setup-android/03-fully-managed-cobo.md && grep -qE "last_verified: 20[0-9]{2}-[0-9]{2}-[0-9]{2}" docs/admin-setup-android/03-fully-managed-cobo.md</automated>
  </verify>
  <acceptance_criteria>
    - Forward-link to admin doc 07 present in Samsung-admins callout: `grep -A2 "Samsung admins" docs/admin-setup-android/03-fully-managed-cobo.md | grep -q "07-knox-mobile-enrollment.md"`
    - Old "deferred to v1.4.1" wording removed: `! grep -q "Full KME coverage is deferred to v1.4.1" docs/admin-setup-android/03-fully-managed-cobo.md`
    - Original Samsung-admins opening preserved: `grep -q "Samsung admins:.*Knox Mobile Enrollment (KME) or Zero-Touch — never both" docs/admin-setup-android/03-fully-managed-cobo.md`
    - Mutex-record cross-link preserved: `grep -q "02-zero-touch-portal.md#kme-zt-mutual-exclusion" docs/admin-setup-android/03-fully-managed-cobo.md`
    - Glossary cross-link preserved: `grep -q "_glossary-android.md#zero-touch-enrollment" docs/admin-setup-android/03-fully-managed-cobo.md`
    - Zero-Touch H3 sub-section above line 162 preserved (verify by reading line 158-160 area):
      `grep -q "COBO provisioning via Zero-Touch requires the Phase 35 Zero-Touch portal binding" docs/admin-setup-android/03-fully-managed-cobo.md`
    - "What breaks if misconfigured" blockquote at line ~164 preserved:
      `grep -q "captive-portal Wi-Fi at first boot" docs/admin-setup-android/03-fully-managed-cobo.md` (verbatim COBO line 164 text)
    - Frontmatter dates shifted: `grep -qE "last_verified: 20[0-9]{2}-[0-9]{2}-[0-9]{2}" docs/admin-setup-android/03-fully-managed-cobo.md`
    - VALIDATION.md AEKNOX-07 unit-grep passes:
      ```
      grep -A2 "KME/ZT mutual exclusion" docs/admin-setup-android/02-zero-touch-portal.md | grep -q "07-knox-mobile-enrollment.md" && \
        grep -A2 "Samsung admins" docs/admin-setup-android/03-fully-managed-cobo.md | grep -q "07-knox-mobile-enrollment.md" && \
        grep -q "AEKNOX-03-shared-anti-paste-block" docs/admin-setup-android/02-zero-touch-portal.md
      ```
    - Audit harness 8/8 PASS post-Plan-07: `node scripts/validation/v1.4.1-milestone-audit.mjs` exits 0
    - Phase 44 end-state check: zero "deferred to v1.4.1" or "tracked for v1.4.1" wording related to Knox/KME in any of the 8 modified files (admin doc 07 / L1 28 / L2 22 / capability matrix / 00-overview / glossary / 02-provisioning-methods / ZT / COBO):
      ```bash
      ! grep -rE "(deferred|tracked) to v1\.4\.1" docs/admin-setup-android/07-knox-mobile-enrollment.md docs/l1-runbooks/28-android-knox-enrollment-failed.md docs/l2-runbooks/22-android-knox-investigation.md docs/reference/android-capability-matrix.md docs/admin-setup-android/00-overview.md docs/_glossary-android.md docs/android-lifecycle/02-provisioning-methods.md docs/admin-setup-android/02-zero-touch-portal.md docs/admin-setup-android/03-fully-managed-cobo.md 2>&1 | grep -i "knox\|kme"
      ```
      Expected: empty output (zero matches; only non-Knox-related deferral text may legitimately remain elsewhere).
  </acceptance_criteria>
  <done>COBO line 162 forward-link closes v1.4 forward-promise; frontmatter shifted; zero KME-related "deferred/tracked to v1.4.1" wording in modified files. All 11 acceptance criteria pass. AEKNOX-07 VALIDATION unit-grep exits 0. Audit harness 8/8 PASS. Phase 44 end-state achieved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Forward-link cross-references → Plan 01 admin doc | ZT line 16 + COBO line 162 both forward-link to `07-knox-mobile-enrollment.md`; this Wave 4 plan runs AFTER Plan 01 (Wave 1) so target file is guaranteed to exist |
| AEKNOX-03 anti-paste block identity → Plan 01 source | The block in ZT must be byte-identical to the one Plan 01 inserted in admin doc 07; Task 1 reads admin doc 07 and copies VERBATIM |
| Mutex-record / glossary cross-links → existing in-place anchors | The COBO line 162 edit preserves the mutex-record + glossary cross-links; only the "Full KME coverage..." substring is replaced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | Tampering (silent drift between two anti-paste copies) | AEKNOX-03 anti-paste block in ZT vs admin doc 07 | mitigate | D-03 IDENTITY verification step in acceptance criteria — `diff` between sed-extracted blocks must return empty; HTML-comment markers `<!-- AEKNOX-03-shared-anti-paste-block -->` enable future drift detection via `grep -r AEKNOX-03-shared-anti-paste-block docs/` |
| T-44-03 | Tampering (link rot) | Forward-links to admin doc 07 from ZT line 16 + COBO line 162 | mitigate | Plan 01 (Wave 1) ships admin doc 07; Wave 4 dependency ordering guarantees existence; manual visual rendering verification at execute time |
| T-44-04 | Tampering (silent v1.4 forward-promise residual) | "deferred to v1.4.1" or "tracked for v1.4.1" wording remaining in any Phase 44 modified file | mitigate | Phase 44 end-state acceptance criterion at end of Task 2 — grep across all 8 Phase 44 modified files for residual `(deferred\|tracked) to v1\.4\.1` near `Knox\|KME`; expected zero matches |
</threat_model>

<verification>
After both tasks complete:

1. Audit harness: `node scripts/validation/v1.4.1-milestone-audit.mjs` — expect 8/8 PASS.
2. AEKNOX-07 unit-grep predicate (from VALIDATION.md):
   ```bash
   grep -A2 "KME/ZT mutual exclusion" docs/admin-setup-android/02-zero-touch-portal.md | grep -q "07-knox-mobile-enrollment.md" && \
     grep -A2 "Samsung admins" docs/admin-setup-android/03-fully-managed-cobo.md | grep -q "07-knox-mobile-enrollment.md" && \
     grep -q "AEKNOX-03-shared-anti-paste-block" docs/admin-setup-android/02-zero-touch-portal.md
   ```
   Expected: exit 0.
3. D-03 IDENTITY verification (Manual-Only per VALIDATION.md):
   ```bash
   diff <(sed -n '/<!-- AEKNOX-03-shared-anti-paste-block -->/,/<!-- \/AEKNOX-03-shared-anti-paste-block -->/p' docs/admin-setup-android/07-knox-mobile-enrollment.md) <(sed -n '/<!-- AEKNOX-03-shared-anti-paste-block -->/,/<!-- \/AEKNOX-03-shared-anti-paste-block -->/p' docs/admin-setup-android/02-zero-touch-portal.md)
   ```
   Expected: empty output.
4. Phase 44 end-state forward-promise audit:
   ```bash
   ! grep -rE "(deferred|tracked) to v1\.4\.1" docs/admin-setup-android/07-knox-mobile-enrollment.md docs/l1-runbooks/28-android-knox-enrollment-failed.md docs/l2-runbooks/22-android-knox-investigation.md docs/reference/android-capability-matrix.md docs/admin-setup-android/00-overview.md docs/_glossary-android.md docs/android-lifecycle/02-provisioning-methods.md docs/admin-setup-android/02-zero-touch-portal.md docs/admin-setup-android/03-fully-managed-cobo.md 2>&1 | grep -i "knox\|kme"
   ```
   Expected: empty output (zero Knox/KME-related "deferred/tracked to v1.4.1" residue).
</verification>

<success_criteria>
- ZT line ~16 KME/ZT mutex callout forward-links to admin doc 07; "tracked for v1.4.1" wording removed
- ZT DPC JSON paste step has the AEKNOX-03 shared anti-paste blockquote inserted IMMEDIATELY ABOVE the JSON code block; HTML-comment markers present (open + close)
- D-03 IDENTITY: diff between admin doc 07 anti-paste block and ZT anti-paste block returns empty
- COBO line ~162 Samsung-admins callout forward-links to admin doc 07; "deferred to v1.4.1" wording removed
- Existing mutex-record + glossary cross-links + What-breaks blockquote preserved verbatim in COBO
- Existing KME/ZT Mutual Exclusion H2 in ZT preserved verbatim
- Existing JSON code block content preserved in ZT
- Frontmatter dates shifted in both files
- AEKNOX-07 VALIDATION unit-grep exits 0
- Audit harness 8/8 PASS
- Phase 44 end-state: zero KME-related "deferred/tracked to v1.4.1" wording across all 9 modified files
</success_criteria>

<output>
After completion, create `.planning/phases/44-knox-mobile-enrollment/44-07-SUMMARY.md` documenting:
- Surgical edits (4: ZT frontmatter / ZT line 16 / ZT line 93 anti-paste / COBO frontmatter / COBO line 162)
- D-03 IDENTITY diff result (empty = success)
- Phase 44 end-state forward-promise audit result
- Audit harness 8/8 PASS state
- Closes v1.4 forward-promises pinned at ZT:16 + COBO:162
</output>
