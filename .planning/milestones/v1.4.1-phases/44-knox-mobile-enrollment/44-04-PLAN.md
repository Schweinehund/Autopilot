---
phase: 44-knox-mobile-enrollment
plan: 04
type: execute
wave: 3
depends_on:
  - 44-01
files_modified:
  - docs/reference/android-capability-matrix.md
autonomous: true
requirements:
  - AEKNOX-04
must_haves:
  truths:
    - "docs/reference/android-capability-matrix.md anchor `#deferred-knox-mobile-enrollment-row` is REMOVED and renamed to `#knox-mobile-enrollment-row`"
    - "H3 heading `### Deferred: Knox Mobile Enrollment row` is REMOVED and renamed to `### Knox Mobile Enrollment (Samsung)`"
    - "Body of the renamed H3 is populated with live row data (Samsung-only; free baseline; KME provisions into COBO/Dedicated/WPCO; cross-links to admin doc 07 + L1 28 + L2 22)"
    - "Cross-ref at line 86 changed from `[KME deferral footer](#deferred-knox-mobile-enrollment-row)` to `[Knox Mobile Enrollment](#knox-mobile-enrollment-row)`"
    - "Frontmatter `last_verified` shifted to execute date and `review_by` to +60d (Phase 43 D-22)"
  artifacts:
    - path: docs/reference/android-capability-matrix.md
      provides: "Live Knox Mobile Enrollment row in capability matrix; closes v1.4 deferred-row footer"
      contains:
        - 'id="knox-mobile-enrollment-row"'
        - "### Knox Mobile Enrollment (Samsung)"
        - "07-knox-mobile-enrollment.md"
  key_links:
    - from: docs/reference/android-capability-matrix.md
      to: docs/admin-setup-android/07-knox-mobile-enrollment.md
      via: Cross-link in populated row body
      pattern: "07-knox-mobile-enrollment.md"
    - from: docs/reference/android-capability-matrix.md (line ~86 cross-ref)
      to: docs/reference/android-capability-matrix.md (renamed anchor)
      via: In-doc anchor reference
      pattern: "#knox-mobile-enrollment-row"
---

<objective>
Surgically rename the deferred-row anchor and H3 heading in `docs/reference/android-capability-matrix.md`, populate the row body with live KME data, and update the in-doc cross-reference at line 86. Closes the v1.4 deferred-row commitment in PROJECT.md Key Decisions.

Purpose: Fulfill AEKNOX-04 — the matrix's `#deferred-knox-mobile-enrollment-row` anchor (lines 113-119 in current file) is renamed to `#knox-mobile-enrollment-row`, the "Deferred:" prefix is dropped from the H3, and the body text is replaced with populated row data. The cross-reference at line 86 (which currently points to the deferred footer) is updated to use the renamed anchor and drop "deferral footer" wording.

Output: Surgical edits to ONE existing file (`docs/reference/android-capability-matrix.md`); no new files; bounded to lines 86, 113-119, and frontmatter dates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md
@.planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md
@.planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md
@.planning/phases/44-knox-mobile-enrollment/44-VALIDATION.md
@docs/reference/android-capability-matrix.md
@docs/admin-setup-android/07-knox-mobile-enrollment.md

KEY VERBATIM SOURCE-OF-TRUTH (sourced from PATTERNS.md File 4 + RESEARCH §5 Pin 4):

**Existing anchor block (verbatim lines 113-119 — to be REPLACED):**
```
<a id="deferred-knox-mobile-enrollment-row"></a>
### Deferred: Knox Mobile Enrollment row

Knox Mobile Enrollment (KME) is a Samsung-specific zero-touch enrollment path
mutually exclusive with Google Zero-Touch on Samsung hardware. KME coverage —
including a provisioning-method row and capability mapping — is deferred to v1.4.1
per PROJECT.md Key Decisions. See [Knox Mobile Enrollment deferral note](../android-lifecycle/02-provisioning-methods.md#knox-mobile-enrollment).
```

**Phase 44 replacement (anchor rename + H3 rename + populated body — executor uses VERBATIM):**
```
<a id="knox-mobile-enrollment-row"></a>
### Knox Mobile Enrollment (Samsung)

Knox Mobile Enrollment (KME) is Samsung's zero-touch-equivalent enrollment channel for Samsung devices. KME provisions Samsung devices into the existing Android Enterprise device-owner modes (COBO / Dedicated / WPCO) via the Knox Admin Portal → Intune handoff using a flat `EXTRA_ENROLLMENT_TOKEN` Custom JSON Data field. KME itself is **free** and does not require a paid Knox license; KPE Premium has been free since Samsung's 2024-03-21 update, and Microsoft Intune Plan 1+ supplies KPE Premium transparently to enrolled Samsung devices. KME is **mutually exclusive with Google Zero-Touch** on the same Samsung device — KME takes precedence at the device firmware level when both are configured.

For full admin coverage, see [Knox Mobile Enrollment Admin Guide](../admin-setup-android/07-knox-mobile-enrollment.md). For L1 enrollment-failure triage, see [L1 Runbook 28: Android Knox Enrollment Failed](../l1-runbooks/28-android-knox-enrollment-failed.md). For L2 investigation, see [L2 Runbook 22: Android Knox Investigation](../l2-runbooks/22-android-knox-investigation.md). For provisioning-method placement in the lifecycle reference, see [Knox Mobile Enrollment](../android-lifecycle/02-provisioning-methods.md#knox-mobile-enrollment).
```

**Existing line 86 cross-ref (verbatim — to be EDITED):**
```
| **iOS Automated Device Enrollment (ADE) via Apple Business Manager** | **Google Zero-Touch Enrollment via ZT portal** |
| Apple ADE is hardware-vendor-chain enrollment via Apple Business Manager (ABM): ... | Google Zero-Touch Enrollment binds hardware to the organization via the [Zero-Touch portal](../admin-setup-android/02-zero-touch-portal.md) with an explicit reseller relationship as Step 0 — a distinct tri-portal administrative surface (Intune + Managed Google Play + Zero-Touch portal). Resellers upload IMEI / serial lists to the portal; devices enroll on first boot via portal → Intune handoff into Fully Managed or Dedicated. Samsung hardware: Zero-Touch is mutually exclusive with Knox Mobile Enrollment — see the [KME deferral footer](#deferred-knox-mobile-enrollment-row). |
```

**Replace** the substring `[KME deferral footer](#deferred-knox-mobile-enrollment-row)` with `[Knox Mobile Enrollment](#knox-mobile-enrollment-row)` (drop "deferral footer" wording AND update the anchor to the renamed one).

**Frontmatter shift (Phase 43 D-22 metadata-only):**
- `last_verified:` shift to today's execute date YYYY-MM-DD
- `review_by:` shift to today + 60 days YYYY-MM-DD

**Forbidden changes (per PATTERNS.md File 4):**
- Do NOT modify `last_verified` / `review_by` of files OTHER than this one
- Do NOT remove the `<!-- AEAUDIT-04: ... -->` HTML comment at lines 74-77 (Audit C2 supervision-attribution pin source-of-truth)
- Do NOT introduce a 4th deferred-row footer for Knox content (the deferred block converts to a live row with retained-but-renamed anchor)
- Surgical-edit boundary: changes confined to frontmatter dates + line ~86 substring + lines 113-119 block. No other line modifications.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename anchor + H3 + populate body at lines 113-119; update cross-ref at line 86; shift frontmatter dates</name>
  <files>docs/reference/android-capability-matrix.md</files>
  <read_first>
    - .planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md (AEKNOX-04 surgical-edit scope)
    - .planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md (File 4 — verbatim copy-from-source for replacement block + line 86 substring edit)
    - docs/reference/android-capability-matrix.md (lines 1-7 frontmatter; lines 80-90 cross-ref area; lines 110-125 deferred-row block)
    - docs/admin-setup-android/07-knox-mobile-enrollment.md (Plan 01 output — confirms cross-link target file exists)
  </read_first>
  <behavior>
    - Frontmatter `last_verified` and `review_by` updated to execute date and execute date + 60 days
    - Line ~86 substring `[KME deferral footer](#deferred-knox-mobile-enrollment-row)` is replaced with `[Knox Mobile Enrollment](#knox-mobile-enrollment-row)`
    - Lines 113-119 block (anchor + H3 + body paragraph) is REPLACED with the populated 7-line block per `<context>`
    - The `<!-- AEAUDIT-04: ... -->` HTML comment at lines 74-77 is preserved (Audit C2 invariant)
    - The `<a id="deferred-full-aosp-capability-mapping"></a>` block at line 121+ (AOSP deferred row) is preserved unchanged
    - Zero new lines added except the populated body paragraph (+1 line vs original 7-line block; net change ~0-2 lines)
  </behavior>
  <action>
    Use the Edit tool to make THREE surgical edits to `docs/reference/android-capability-matrix.md`:

    **Edit 1 — Frontmatter dates (Phase 43 D-22 shift):**
    Compute dates: `node -e "const d=new Date(); console.log('last_verified: '+d.toISOString().slice(0,10)); d.setDate(d.getDate()+60); console.log('review_by: '+d.toISOString().slice(0,10))"`
    - Replace existing `last_verified: <old-date>` line with `last_verified: <execute-date>`
    - Replace existing `review_by: <old-date>` line with `review_by: <execute-date + 60d>`

    **Edit 2 — Line ~86 cross-ref substring replacement:**
    - Find the line containing `Samsung hardware: Zero-Touch is mutually exclusive with Knox Mobile Enrollment — see the [KME deferral footer](#deferred-knox-mobile-enrollment-row).`
    - Replace the substring `[KME deferral footer](#deferred-knox-mobile-enrollment-row)` with `[Knox Mobile Enrollment](#knox-mobile-enrollment-row)`
    - The rest of the line text remains unchanged.
    - Result line: `Samsung hardware: Zero-Touch is mutually exclusive with Knox Mobile Enrollment — see the [Knox Mobile Enrollment](#knox-mobile-enrollment-row).`

    **Edit 3 — Lines 113-119 block replacement (anchor rename + H3 rename + populate body):**

    Find this exact 7-line block (deferred-row footer):
    ```
    <a id="deferred-knox-mobile-enrollment-row"></a>
    ### Deferred: Knox Mobile Enrollment row

    Knox Mobile Enrollment (KME) is a Samsung-specific zero-touch enrollment path
    mutually exclusive with Google Zero-Touch on Samsung hardware. KME coverage —
    including a provisioning-method row and capability mapping — is deferred to v1.4.1
    per PROJECT.md Key Decisions. See [Knox Mobile Enrollment deferral note](../android-lifecycle/02-provisioning-methods.md#knox-mobile-enrollment).
    ```

    REPLACE it verbatim with the populated block from `<context>`:
    ```
    <a id="knox-mobile-enrollment-row"></a>
    ### Knox Mobile Enrollment (Samsung)

    Knox Mobile Enrollment (KME) is Samsung's zero-touch-equivalent enrollment channel for Samsung devices. KME provisions Samsung devices into the existing Android Enterprise device-owner modes (COBO / Dedicated / WPCO) via the Knox Admin Portal → Intune handoff using a flat `EXTRA_ENROLLMENT_TOKEN` Custom JSON Data field. KME itself is **free** and does not require a paid Knox license; KPE Premium has been free since Samsung's 2024-03-21 update, and Microsoft Intune Plan 1+ supplies KPE Premium transparently to enrolled Samsung devices. KME is **mutually exclusive with Google Zero-Touch** on the same Samsung device — KME takes precedence at the device firmware level when both are configured.

    For full admin coverage, see [Knox Mobile Enrollment Admin Guide](../admin-setup-android/07-knox-mobile-enrollment.md). For L1 enrollment-failure triage, see [L1 Runbook 28: Android Knox Enrollment Failed](../l1-runbooks/28-android-knox-enrollment-failed.md). For L2 investigation, see [L2 Runbook 22: Android Knox Investigation](../l2-runbooks/22-android-knox-investigation.md). For provisioning-method placement in the lifecycle reference, see [Knox Mobile Enrollment](../android-lifecycle/02-provisioning-methods.md#knox-mobile-enrollment).
    ```

    Do NOT modify any other lines in the file. The `<a id="deferred-full-aosp-capability-mapping"></a>` block immediately after must remain unchanged.

    NOTE: Cross-links to L1 runbook 28 and L2 runbook 22 in the populated body REFERENCE files that are created by Plans 02 and 03 (Wave 2). Since this plan is in Wave 3, both files are guaranteed to exist when this plan executes.
  </action>
  <verify>
    <automated>! grep -q "deferred-knox-mobile-enrollment-row" docs/reference/android-capability-matrix.md && grep -q 'id="knox-mobile-enrollment-row"' docs/reference/android-capability-matrix.md && grep -q "### Knox Mobile Enrollment (Samsung)" docs/reference/android-capability-matrix.md && grep -q '07-knox-mobile-enrollment.md' docs/reference/android-capability-matrix.md && grep -q '#knox-mobile-enrollment-row' docs/reference/android-capability-matrix.md && ! grep -q "KME deferral footer" docs/reference/android-capability-matrix.md && ! grep -q "deferred to v1.4.1" docs/reference/android-capability-matrix.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "deferred-knox-mobile-enrollment-row"` (old anchor REMOVED — both definition and reference)
    - `grep -c 'id="knox-mobile-enrollment-row"'` returns exactly 1 (new anchor definition present)
    - `grep -c '#knox-mobile-enrollment-row'` returns at least 2 (anchor + at least one reference at line 86)
    - `grep -q "### Knox Mobile Enrollment (Samsung)"` matches (new H3)
    - `! grep -q "### Deferred: Knox Mobile Enrollment row"` (old H3 removed)
    - `! grep -q "KME deferral footer"` (old cross-ref text removed)
    - `! grep -qE "deferred to v1\.4\.1.*Knox"` (deferred phrase removed from Knox row)
    - `grep -q '07-knox-mobile-enrollment.md'` matches (cross-link to admin doc 07)
    - `grep -q '28-android-knox-enrollment-failed.md'` matches (cross-link to L1 28)
    - `grep -q '22-android-knox-investigation.md'` matches (cross-link to L2 22)
    - `grep -q '02-provisioning-methods.md#knox-mobile-enrollment'` matches (cross-link to lifecycle anchor)
    - `grep -qE "last_verified: 20[0-9]{2}-[0-9]{2}-[0-9]{2}"` matches (frontmatter shifted)
    - `grep -qE "review_by: 20[0-9]{2}-[0-9]{2}-[0-9]{2}"` matches
    - AEAUDIT-04 invariant preserved: `grep -q "AEAUDIT-04" docs/reference/android-capability-matrix.md` matches at least 1 (HTML-comment block at lines 74-77 not removed)
    - AOSP deferred-row preserved: `grep -q 'id="deferred-full-aosp-capability-mapping"' docs/reference/android-capability-matrix.md` matches (sibling deferred-row untouched)
    - VALIDATION.md AEKNOX-04 unit-grep passes:
      ```
      ! grep -q "deferred-knox-mobile-enrollment-row" docs/reference/android-capability-matrix.md && grep -q 'knox-mobile-enrollment-row' docs/reference/android-capability-matrix.md
      ```
    - Audit harness 8/8 PASS post-edit: `node scripts/validation/v1.4.1-milestone-audit.mjs` exits 0
  </acceptance_criteria>
  <done>Anchor renamed, H3 renamed and populated, line 86 cross-ref updated, frontmatter dates shifted. All 16 acceptance criteria pass. AEKNOX-04 VALIDATION unit-grep exits 0. Audit harness 8/8 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown anchor → cross-document references | Other files (e.g., `02-provisioning-methods.md` and ZT/COBO docs) may reference the renamed anchor; this plan touches the matrix only — Plan 06 handles `02-provisioning-methods.md` |
| Populated row body → reader expectation | Live row body must reflect KME operational reality (Samsung-only, free baseline, KME provisions into existing modes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Information Disclosure (incorrect) | Populated row body — KPE Premium "free since 2024-03-21" + Plan 1+ transparency claim | mitigate | Cite Samsung 2024-03-21 update; 60-day freshness re-verifies; researcher OQ-1 re-verification at execute time |
| T-44-03 | Tampering (link-rot) | Cross-links to admin doc 07 + L1 28 + L2 22 + lifecycle anchor | mitigate | Plans 01/02/03 ship those files in earlier waves before this Wave 3 plan executes; lifecycle anchor populated in Plan 06 (also Wave 3) — order is independent for grep verification |
| T-44-04 | Tampering (silent drift) | Anchor rename — must remove ALL `deferred-knox-mobile-enrollment-row` references in this file | mitigate | Two-step rename: (1) replace deferred-row block (anchor definition); (2) replace line 86 cross-ref. Acceptance criterion `! grep -q "deferred-knox-mobile-enrollment-row"` enforces zero stragglers |
</threat_model>

<verification>
After the task completes:

1. Audit harness: `node scripts/validation/v1.4.1-milestone-audit.mjs` — expect 8/8 PASS; no new C5 freshness failures (frontmatter shifted).
2. AEKNOX-04 unit-grep predicate (from VALIDATION.md):
   ```bash
   ! grep -q "deferred-knox-mobile-enrollment-row" docs/reference/android-capability-matrix.md && \
     grep -q 'knox-mobile-enrollment-row' docs/reference/android-capability-matrix.md
   ```
   Expected: exit 0.
3. Cross-link integrity:
   ```bash
   for ref in '07-knox-mobile-enrollment.md' '28-android-knox-enrollment-failed.md' '22-android-knox-investigation.md'; do
     grep -q "$ref" docs/reference/android-capability-matrix.md && echo "OK: $ref" || echo "MISSING: $ref"
   done
   ```
4. AEAUDIT-04 invariant: `grep -q "AEAUDIT-04" docs/reference/android-capability-matrix.md` exits 0 (HTML comment preserved).
</verification>

<success_criteria>
- Old anchor `#deferred-knox-mobile-enrollment-row` is FULLY REMOVED (zero references in file)
- New anchor `#knox-mobile-enrollment-row` is defined exactly once and referenced at least once (line 86 cross-ref)
- Old H3 `### Deferred: Knox Mobile Enrollment row` REPLACED with `### Knox Mobile Enrollment (Samsung)`
- Old line-86 substring `[KME deferral footer](#deferred-knox-mobile-enrollment-row)` REPLACED with `[Knox Mobile Enrollment](#knox-mobile-enrollment-row)`
- Body of the renamed H3 contains live row data: Samsung-only / free / provisions into COBO/Dedicated/WPCO / Plan 1+ supplies KPE Premium / KME wins on dual-config; cross-links to admin doc 07, L1 28, L2 22, lifecycle anchor
- Frontmatter `last_verified` + `review_by` shifted (D-22 metadata-only)
- AEAUDIT-04 HTML comment at lines ~74-77 preserved
- AOSP deferred-row at line ~121+ preserved unchanged
- AEKNOX-04 VALIDATION unit-grep exits 0
- Audit harness 8/8 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/44-knox-mobile-enrollment/44-04-SUMMARY.md` documenting:
- Surgical edits made (3: frontmatter dates / line 86 substring / lines 113-119 block)
- Old anchor confirmation (zero `deferred-knox-mobile-enrollment-row` references)
- New anchor confirmation (single definition + at least one reference)
- AEAUDIT-04 invariant preserved
- Audit harness 8/8 PASS state
</output>
