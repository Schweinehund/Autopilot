---
phase: 44-knox-mobile-enrollment
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - docs/l1-runbooks/28-android-knox-enrollment-failed.md
autonomous: true
requirements:
  - AEKNOX-02
must_haves:
  truths:
    - "docs/l1-runbooks/28-android-knox-enrollment-failed.md exists with Phase 34 admin template frontmatter (audience: L1, applies_to: KME)"
    - "Runbook contains 4 L1-diagnosable Causes (A B2B-pending / B device-not-in-KAP / C profile-unassigned / D KME-ZT-collision) plus Cause E (escalate-only)"
    - "Each Cause A-D follows D-10 sectioned actor-boundary structure: Entry condition / Symptom / L1 Triage Steps / Admin Action Required / Verify / Escalation (within Cause)"
    - "Escalation Criteria H2 includes D-12 three-part escalation packet (Before escalating, collect: Knox-specific data list)"
    - "Triage tree routing reference points to ANDR28 branch in 08-android-triage.md"
    - "Cross-link to L2 runbook 22 present in Escalation Criteria"
    - "Zero SafetyNet references (audit C1)"
    - "Zero supervision references (audit C2)"
  artifacts:
    - path: docs/l1-runbooks/28-android-knox-enrollment-failed.md
      provides: "L1 runbook for Samsung KME enrollment failures; closes Phase 40 ANDR28 placeholder"
      min_lines: 200
      contains:
        - "applies_to: KME"
        - "audience: L1"
        - "Cause A:"
        - "Cause B:"
        - "Cause C:"
        - "Cause D:"
        - "Admin Action Required"
        - "Escalation Criteria"
  key_links:
    - from: docs/l1-runbooks/28-android-knox-enrollment-failed.md
      to: docs/l2-runbooks/22-android-knox-investigation.md
      via: Escalation Criteria cross-link
      pattern: "22-android-knox-investigation.md"
    - from: docs/l1-runbooks/28-android-knox-enrollment-failed.md
      to: docs/admin-setup-android/07-knox-mobile-enrollment.md
      via: Admin Action Required cross-links into Plan 01 admin doc anchors
      pattern: "07-knox-mobile-enrollment.md"
    - from: docs/l1-runbooks/28-android-knox-enrollment-failed.md
      to: docs/decision-trees/08-android-triage.md
      via: Routed-here-from breadcrumb (ANDR28 branch)
      pattern: "08-android-triage.md"
---

<objective>
Author the new L1 runbook `docs/l1-runbooks/28-android-knox-enrollment-failed.md` for KME-specific enrollment failures. Mirrors the D-10 sectioned actor-boundary + D-12 three-part escalation packet pattern from sibling L1 runbook 27 (ZTE-failed).

Purpose: Close the Phase 40 ANDR28 placeholder in `08-android-triage.md`. Provide L1-diagnosable Causes A-D for the four most-common KME failure modes (B2B account approval pending; device not in Knox Admin Portal; KME profile not assigned; KME/ZT mutex collision) plus Cause E (escalate-only — DPC JSON malformation / Knox tripped / Knox license edge cases). Cross-link to Plan 03 L2 runbook 22 in Escalation Criteria.

Output: One new file (`28-android-knox-enrollment-failed.md`); zero modifications to other files in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md
@.planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md
@.planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md
@.planning/phases/44-knox-mobile-enrollment/44-VALIDATION.md
@docs/l1-runbooks/27-android-zte-enrollment-failed.md
@docs/admin-setup-android/07-knox-mobile-enrollment.md

KEY VERBATIM PATTERNS (sourced from PATTERNS.md File 2; executor uses these exactly):

**Frontmatter (Phase 34 L1 template):**
- last_verified: execute date YYYY-MM-DD
- review_by: execute date + 60 days YYYY-MM-DD
- applies_to: KME
- audience: L1
- platform: Android

**Platform gate (verbatim from runbook 27:9):**
"> **Platform gate:** This guide covers Android enrollment/compliance troubleshooting via Intune. For Windows Autopilot, see [Windows L1 Runbooks](00-index.md#apv1-runbooks). For macOS ADE, see [macOS ADE Runbooks](00-index.md#macos-ade-runbooks). For iOS/iPadOS, see [iOS L1 Runbooks](00-index.md#ios-l1-runbooks)."

**H1 + 1-paragraph context:**
"# Android Knox Mobile Enrollment Failed

L1 runbook for Knox Mobile Enrollment (KME) failures: Samsung device was expected to enroll automatically via KME but did not — device booted to consumer setup, looped back to first-time setup, or never arrived in Intune. Four L1-diagnosable causes plus Cause E (escalate-only)."

**Applies-to scope:**
"**Applies to KME only (Samsung).** For non-Samsung corporate Zero-Touch enrollment failures see runbook [27 ZTE](27-android-zte-enrollment-failed.md). For non-corporate enrollment failures see ([22](22-android-enrollment-blocked.md) / [23](23-android-work-profile-not-created.md) / [24](24-android-device-not-enrolled.md) / [25](25-android-compliance-blocked.md) / [26](26-android-mgp-app-not-installed.md))."

**Triage tree routing reference (closes Phase 40 ANDR28 placeholder):**
"Routed here from the [Android Triage Decision Tree](../decision-trees/08-android-triage.md) ANDR28 branch."

**Prerequisites + portal shorthand:**
"## Prerequisites

- Access to Intune admin center (https://intune.microsoft.com) — L1 read-only
- Device serial number, IMEI, or manufacturer identifier
- Confirmation that device is Samsung manufacturer
- Portal shorthand used in this runbook:
   - **P-INTUNE** = Intune admin center Devices / Tenant admin blades
   - **P-KAP** = Knox Admin Portal (knox.samsung.com) — **admin-only**; Samsung-specific"

**L1 scope note:**
"> **L1 scope note:** Knox Admin Portal is admin-only. L1 observes Intune-side symptoms (device absence / enrollment state) and hands the packet to the admin for KAP portal actions. All Knox Admin Portal click paths in this runbook are within `## Admin Action Required` sections."

**How to Use This Runbook (frequency-ordered Causes per RESEARCH §3 / Pitfalls 1-6):**

```
## How to Use This Runbook

Check the cause that matches your observation. Causes are independently diagnosable — you do not need to rule out prior causes. Cause ordering below reflects frequency (most common first):

- [Cause A: Samsung Knox B2B Account Approval Pending](#cause-a-b2b-account-pending) — Admin cannot sign in to Knox Admin Portal; B2B account submission still within or beyond 1-2 business day approval window
- [Cause B: Device Not in Knox Admin Portal](#cause-b-device-not-in-kap) — Reseller upload not done OR Knox Deployment App pathway not used; device serial absent from KAP Devices view
- [Cause C: KME Profile Not Assigned to Device Set](#cause-c-profile-unassigned) — Profile exists in KAP but device shows no profile assignment
- [Cause D: KME/ZT Mutual-Exclusion Conflict (Samsung)](#cause-d-kme-zt-mutex-collision) — Samsung device dual-configured in both Knox Mobile Enrollment and Zero-Touch; KME takes precedence at firmware level

If none of Causes A-D match and enrollment still fails, see [Escalation Criteria](#escalation-criteria) below for Cause E (DPC Custom JSON malformation / Knox tripped status / Knox license edge cases — admin-only investigation).
```

**Per-Cause sectioned actor-boundary skeleton (D-10 LOCK — verbatim shape from runbook 27 lines 41-83); Cause A example:**

```
## Cause A: Samsung Knox B2B Account Approval Pending {#cause-a-b2b-account-pending}

**Entry condition:** Admin reports they cannot sign in to Knox Admin Portal at knox.samsung.com — sign-in returns "Account pending approval" OR admin has not yet submitted the B2B application.

### Symptom
- User (or IT purchaser) reports the corporate Samsung device went through "normal" Setup Wizard instead of automatic corporate enrollment.
- Device does NOT appear in Intune after expected enrollment window (typically within 15 minutes of first power-on with internet).
- Admin reports Knox Admin Portal sign-in fails with "Account pending approval" OR admin has not yet submitted the B2B application.

### L1 Triage Steps
1. > **Say to the user:** "I'll verify whether your Samsung device was prepared correctly for automatic enrollment. If not, I'll coordinate with your IT administrator and Samsung Knox B2B support."
2. In Intune admin center, navigate to `Devices > All devices`, filter platform = Android, search by serial. Confirm device is NOT present. <!-- verify UI at execute time -->
3. Collect device identifiers for admin escalation: serial number, IMEI 1, model (e.g., Galaxy S23), Android OS version if known.

### Admin Action Required
**Ask the admin to:**
- Open the [Samsung Knox Portal](https://www.samsungknox.com) and confirm B2B account application status.
- If application not yet submitted: submit per [admin doc Step 0](../admin-setup-android/07-knox-mobile-enrollment.md#step-0-b2b-approval). Approval takes 1-2 business days.
- If application submitted but >2 business days pending: contact Samsung Knox B2B support with the application ID; escalate via Samsung support ticket.

**Verify:**
- After approval: admin can sign in to [Knox Admin Portal](https://knox.samsung.com) without "Account pending approval" error.
- After admin completes Steps 1-N of the [KME admin guide](../admin-setup-android/07-knox-mobile-enrollment.md), user can factory-reset device and retry first-boot enrollment.

**If the admin confirms B2B account is approved AND devices still fall through:** Route to [Cause B](#cause-b-device-not-in-kap).

### Escalation (within Cause A)
- B2B account approval >5 business days pending despite Samsung support ticket
- Application rejected with no clear remediation path (corporate email mismatch, region restriction)
```

**Causes B / C / D follow the same skeleton.** Specific entry conditions per RESEARCH §3:
- **Cause B (#cause-b-device-not-in-kap):** Entry: Admin confirms device serial NOT in KAP Devices view. Symptom: Samsung device booted to Setup Wizard; not in Intune; KAP Devices search returns no match. Admin Action: Contact reseller for upload OR use Knox Deployment App for existing-stock retroactive enrollment. Cross-link to admin doc Step 3.
- **Cause C (#cause-c-profile-unassigned):** Entry: Admin confirms device IS in KAP Devices view but Profile column is empty. Symptom: Samsung device booted to Setup Wizard; not in Intune; KAP shows device with empty Profile. Admin Action: Assign KME profile to device set per admin doc Step 4. Cross-link.
- **Cause D (#cause-d-kme-zt-mutex-collision):** Entry: Samsung device registered in BOTH Knox Mobile Enrollment AND Zero-Touch portal. Symptom: Device enrolls via KME (KME wins) when ZT was the desired path; or device enrolls via KME but admin misconfigured KME profile. Admin Action: Choose ONE portal (remove from the other); cross-link to admin doc KME/ZT Mutual Exclusion H2 + ZT doc kme-zt-mutual-exclusion anchor. Mirror inverse direction of runbook 27 Cause D.

**Escalation Criteria H2 (D-12 three-part escalation packet — verbatim shape from runbook 27 lines 207-234):**

```
## Escalation Criteria

(Overall — applies across all four L1-diagnosable causes plus Cause E admin-only path.)

Escalate to L2. See [Android Knox Investigation](../l2-runbooks/22-android-knox-investigation.md) for KME-specific Pattern A-E investigation and [Android Log Collection Guide](../l2-runbooks/18-android-log-collection.md).

Escalate to L2 if:

- Cause A: B2B account approval >5 business days pending; OR Samsung support ticket has no resolution path
- Cause B: Reseller denies device upload AND Knox Deployment App pathway also fails OR is not feasible
- Cause C: Admin assigned KME profile to device set in KAP AND user factory-reset device AND enrollment still falls through
- Cause D: Samsung device removed from one portal (KME or ZT) AND factory-reset; the kept-portal enrollment still doesn't initiate

**Cause E (DPC Custom JSON / Knox tripped / Knox license — admin-only, not an L1 diagnosis):**

If all Cause A-D checks pass but devices still fail at enrollment, escalate to Intune admin for investigation:
- DPC Custom JSON Data review (see [admin doc Step 5](../admin-setup-android/07-knox-mobile-enrollment.md#dpc-custom-json)) — silent failure if ZT JSON wrapper pasted into KME profile
- Knox eFuse tripped status — non-recoverable; device must use non-KME enrollment path
- Knox license edge cases — KPE Premium activation; Plan 1+ misalignment

L1 does NOT modify DPC Custom JSON, Knox eFuse status, or licensing. Hand to L2 + admin.

**Before escalating, collect:**
- Device serial number + IMEI 1 (or MEID for CDMA)
- Device manufacturer (must be Samsung), model, firmware / Android version
- Which Cause (A/B/C/D/E) closest matches observation
- For Cause A: Samsung Knox B2B account application date + status (admin-provided)
- For Cause B: screenshot of Knox Admin Portal Devices view (admin-provided) showing device absent OR present with empty Profile column
- For Cause C: screenshot of Knox Admin Portal Profile-to-Devices assignment view (admin-provided)
- For Cause D: confirmation of device presence/absence in BOTH Knox Admin Portal AND Zero-Touch portal (admin-provided)
- For Cause E: Knox eFuse status (admin-provided if KAP exposes it; otherwise Samsung support ticket reference); KME profile name; reseller name (if applicable)
- Number of devices affected (single device vs fleet — shape-of-problem signal)
- Timestamp of most recent failed enrollment attempt
```

**Back-to-triage footer (verbatim from runbook 27:237):**
"[Back to Android Triage](../decision-trees/08-android-triage.md)"

**Version History seed:**
"## Version History

| Date | Change | Author |
|------|--------|--------|
| <execute-date> | Initial version (Phase 44 scope) — 4 L1-diagnosable causes A-D (B2B account / device-not-in-KAP / KME profile unassigned / KME-ZT collision) + Cause E escalate-only (DPC JSON malformation / Knox tripped / Knox license edge cases). Closes Phase 40 ANDR28 placeholder. | -- |"
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create L1 runbook 28 skeleton (frontmatter through How to Use This Runbook with Cause A-D listing)</name>
  <files>docs/l1-runbooks/28-android-knox-enrollment-failed.md</files>
  <read_first>
    - .planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md (D-10 actor-boundary, D-12 escalation, KME L1 scope)
    - .planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md (File 2 — verbatim copy-from-source for runbook 28)
    - docs/l1-runbooks/27-android-zte-enrollment-failed.md (sibling structural model — full file is the analog)
    - docs/admin-setup-android/07-knox-mobile-enrollment.md (Plan 01 output — Admin Action Required cross-link targets must exist)
  </read_first>
  <behavior>
    - File MUST exist with Phase 34 L1 runbook template frontmatter (last_verified, review_by 60d apart, applies_to: KME, audience: L1, platform: Android)
    - Platform gate blockquote at line ~9 (verbatim from runbook 27:9)
    - H1 = `# Android Knox Mobile Enrollment Failed`
    - 1-paragraph context after H1 explaining KME failure scope (Samsung-only)
    - Applies-to scope line cross-linking to runbook 27 (ZTE) and runbooks 22-26
    - `Routed here from the [Android Triage Decision Tree](../decision-trees/08-android-triage.md) ANDR28 branch.` line
    - `## Prerequisites` H2 with portal shorthand (P-INTUNE + P-KAP)
    - L1 scope note blockquote (Knox Admin Portal is admin-only)
    - `## How to Use This Runbook` H2 with bulleted Causes A-D (frequency-ordered) plus Cause E pointer
  </behavior>
  <action>
    Use the Write tool to create `docs/l1-runbooks/28-android-knox-enrollment-failed.md` with these sections in order, using the verbatim markdown blocks from the `<context>` section above:

    1. **Frontmatter** (Phase 34 L1 template). Compute dates via Bash: `node -e "const d=new Date(); console.log('last_verified: '+d.toISOString().slice(0,10)); d.setDate(d.getDate()+60); console.log('review_by: '+d.toISOString().slice(0,10))"`. Set `applies_to: KME`, `audience: L1`, `platform: Android`.
    2. **Platform gate blockquote** at line ~9 — verbatim string starting `> **Platform gate:** This guide covers Android enrollment/compliance troubleshooting via Intune.` from `<context>`.
    3. **H1** + 1-paragraph context — verbatim from `<context>`.
    4. **Applies-to scope line** — verbatim from `<context>`.
    5. **Triage routing reference** — verbatim line.
    6. **`## Prerequisites`** H2 with portal shorthand bullets — verbatim from `<context>`.
    7. **L1 scope note blockquote** — verbatim.
    8. **`## How to Use This Runbook`** H2 with the verbatim Cause A-D bulleted listing block from `<context>` (frequency-ordered).

    Stop here. Cause A-D bodies and Escalation Criteria are deferred to Task 2.
  </action>
  <verify>
    <automated>test -f docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q "applies_to: KME" docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q "audience: L1" docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q '^# Android Knox Mobile Enrollment Failed$' docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q 'ANDR28 branch' docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q 'P-KAP' docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q 'How to Use This Runbook' docs/l1-runbooks/28-android-knox-enrollment-failed.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/l1-runbooks/28-android-knox-enrollment-failed.md` returns 0
    - `grep -q "applies_to: KME"` matches
    - `grep -q "audience: L1"` matches
    - `grep -q "platform: Android"` matches
    - `grep -qE "last_verified: 20[0-9]{2}-[0-9]{2}-[0-9]{2}"` matches
    - `grep -qE "review_by: 20[0-9]{2}-[0-9]{2}-[0-9]{2}"` matches
    - `grep -q '^# Android Knox Mobile Enrollment Failed$'` matches (exact H1)
    - `grep -q 'Platform gate'` matches (Platform gate blockquote present)
    - `grep -q 'Applies to KME only (Samsung)'` matches
    - `grep -q '27-android-zte-enrollment-failed.md'` matches (cross-link to sibling ZTE runbook)
    - `grep -q 'ANDR28 branch'` matches (closes Phase 40 placeholder)
    - `grep -q 'P-INTUNE'` matches
    - `grep -q 'P-KAP'` matches
    - `grep -q 'L1 scope note'` matches
    - `grep -q 'How to Use This Runbook'` matches
    - `grep -q 'Cause A: Samsung Knox B2B Account Approval Pending'` matches
    - `grep -q 'Cause B: Device Not in Knox Admin Portal'` matches
    - `grep -q 'Cause C: KME Profile Not Assigned to Device Set'` matches
    - `grep -q 'Cause D: KME/ZT Mutual-Exclusion Conflict (Samsung)'` matches
    - `! grep -q 'SafetyNet'` (audit C1)
    - `! grep -qE 'supervis(ed|ion|or)'` (audit C2)
  </acceptance_criteria>
  <done>File skeleton exists through `## How to Use This Runbook` with Cause A-D bullet listing. All 21 acceptance criteria pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append Cause A-D bodies (D-10 actor-boundary structure), Escalation Criteria (D-12 packet) with Cross-link to L2 runbook 22, Back-to-triage footer, Version History</name>
  <files>docs/l1-runbooks/28-android-knox-enrollment-failed.md</files>
  <read_first>
    - docs/l1-runbooks/28-android-knox-enrollment-failed.md (file from Task 1 — append below How to Use)
    - docs/l1-runbooks/27-android-zte-enrollment-failed.md (lines 41-244 — Cause A-D + Escalation pattern source)
    - .planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md (File 2 — Cause A skeleton verbatim; D-12 escalation packet verbatim)
    - docs/admin-setup-android/07-knox-mobile-enrollment.md (cross-link target anchors: #step-0-b2b-approval, #dpc-custom-json, #kme-zt-mutual-exclusion, #step-3-add-devices, #step-4-assign-profile)
  </read_first>
  <behavior>
    - 4 Cause H2 sections (A B2B-pending / B device-not-in-KAP / C profile-unassigned / D KME-ZT-collision); each with `{#cause-x-anchor}` ID
    - Each Cause has these sub-sections in order: `**Entry condition:**` paragraph / `### Symptom` (3-5 bullets) / `### L1 Triage Steps` (numbered, includes `> **Say to the user:**` script + `<!-- verify UI at execute time -->` HTML comment + identifier collection) / `### Admin Action Required` (`**Ask the admin to:**` bulleted with cross-links into admin doc 07) / `**Verify:**` bullets / `**If the admin confirms ... applies:**` route-to-next-Cause line / `### Escalation (within Cause X)` sub-H3
    - Cause D mirrors runbook 27 Cause D inverse direction (KME side; KME takes precedence)
    - `## Escalation Criteria` H2 with cross-link to L2 runbook 22 + per-Cause escalation triggers + Cause E (escalate-only) section + D-12 three-part escalation packet (`Before escalating, collect:` data list with Knox-specific items)
    - `[Back to Android Triage](../decision-trees/08-android-triage.md)` footer
    - `## Version History` table with Phase 44 seed entry
  </behavior>
  <action>
    Use the Edit tool to append the following sections AFTER the `## How to Use This Runbook` H2 created in Task 1, in this order. Use the verbatim Cause A skeleton in `<context>` as the template; replicate the same shape for B, C, D with the specific Entry conditions and Admin Actions enumerated below.

    1. **Cause A** (`{#cause-a-b2b-account-pending}`) — VERBATIM the skeleton block from `<context>`. Cross-link in Admin Action Required: `[admin doc Step 0](../admin-setup-android/07-knox-mobile-enrollment.md#step-0-b2b-approval)`.

    2. **Cause B** (`{#cause-b-device-not-in-kap}`) — same skeleton shape; specific values:
       - Entry condition: "Admin confirms the device serial is NOT visible in Knox Admin Portal Devices view. Device boots to consumer Setup Wizard on first power-on."
       - Symptom: 3-5 bullets including "Device does NOT appear in Intune after 15 minutes" + "Admin searches KAP Devices view by IMEI/serial — no match" + "Reseller has not uploaded device to KAP".
       - L1 Triage Steps: Say-to-user script + Intune `Devices > All devices` filter check (verify NOT present) + Collect identifiers.
       - Admin Action Required: "Open [Knox Admin Portal](https://knox.samsung.com); go to **Devices** tab; search by IMEI/serial. If absent: contact authorized Samsung reseller for upload (per [admin doc Step 3 — Reseller bulk upload](../admin-setup-android/07-knox-mobile-enrollment.md#step-3-add-devices)) OR use [Knox Deployment App](../admin-setup-android/07-knox-mobile-enrollment.md#step-3-add-devices) for existing-stock retroactive enrollment via Bluetooth/NFC."
       - Verify: "After reseller upload OR KDA enrollment: device appears in KAP Devices view within 24 hours."
       - Route-to: "If device listed in KAP but enrollment still fails — route to [Cause C](#cause-c-profile-unassigned)."
       - Escalation (within Cause B): "Reseller denies device upload AND device cannot use Knox Deployment App (e.g., no Bluetooth/NFC access)."

    3. **Cause C** (`{#cause-c-profile-unassigned}`) — same shape; specific values:
       - Entry condition: "Admin confirms device IS in Knox Admin Portal Devices view but Profile column is EMPTY."
       - Symptom: "Samsung device booted to Setup Wizard; not in Intune; KAP Devices view shows device row with empty Profile column."
       - Admin Action: "Open [Knox Admin Portal](https://knox.samsung.com); navigate to **Profiles**; verify a KME profile exists with EMM = Microsoft Intune. Assign profile to device set per [admin doc Step 4](../admin-setup-android/07-knox-mobile-enrollment.md#step-4-assign-profile). Profile MUST be assigned BEFORE first boot — assignment after first boot does not retro-trigger KME (device must be factory-reset)."
       - Route-to: "If profile assigned AND user factory-reset AND device still fails — route to [Cause D](#cause-d-kme-zt-mutex-collision)."
       - Escalation (within Cause C): "Profile assigned in KAP AND device factory-reset AND enrollment still falls through."

    4. **Cause D** (`{#cause-d-kme-zt-mutex-collision}`) — same shape; specific values (mirror inverse of runbook 27 Cause D):
       - Entry condition: "Samsung device registered in BOTH Knox Mobile Enrollment AND Zero-Touch portal. KME takes precedence at the device firmware level when both are configured (this is Samsung firmware-level behavior; cannot be overridden via Intune or ZT portal)."
       - Symptom: "Device enrolls via KME when ZT was the desired path; OR device enrollment shows wrong mode in Intune; OR ZT portal claim shows but no first-boot activity."
       - Admin Action: "Choose ONE portal for the device set: remove from KME (per [admin doc KME/ZT Mutual Exclusion](../admin-setup-android/07-knox-mobile-enrollment.md#kme-zt-mutual-exclusion)) OR remove from ZT (per [02-zero-touch-portal.md#kme-zt-mutual-exclusion](../admin-setup-android/02-zero-touch-portal.md#kme-zt-mutual-exclusion)). After removal: factory-reset device and retry first-boot enrollment via the kept portal."
       - Route-to: "If device removed from non-desired portal AND factory-reset AND enrollment still doesn't initiate — escalate to L2 (Cause E)."
       - Escalation (within Cause D): "Device removed from one portal but enrollment still routes through the removed portal (likely KAP/ZT-portal sync delay; retry after 24h)."

    5. **`## Escalation Criteria`** H2 — VERBATIM the Escalation Criteria block from `<context>` (cross-link to L2 runbook 22 + per-Cause triggers + Cause E section + D-12 `Before escalating, collect:` Knox-specific data list).

    6. **Back-to-triage footer** — verbatim `[Back to Android Triage](../decision-trees/08-android-triage.md)`.

    7. **`## Version History`** — verbatim table from `<context>`.

    CRITICAL audit constraints:
    - Zero `SafetyNet` references (audit C1).
    - Zero `supervision` / `supervised` references (audit C2 — Knox is Samsung-only; no iOS bridge prose per locked context).
    - Do NOT include device-side `adb logcat` or `setprop` commands (L1-scope is Intune-admin-center read-only).
    - Do NOT cite Knox SKU pricing (Knox-license-expired Cause E mention only — directs admin to admin doc 07 for SKU table).
  </action>
  <verify>
    <automated>grep -q 'Cause A: Samsung Knox B2B Account Approval Pending {#cause-a-b2b-account-pending}' docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q 'Cause B: Device Not in Knox Admin Portal {#cause-b-device-not-in-kap}' docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q 'Cause C: KME Profile Not Assigned to Device Set {#cause-c-profile-unassigned}' docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q 'Cause D: KME/ZT Mutual-Exclusion Conflict (Samsung) {#cause-d-kme-zt-mutex-collision}' docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q '## Escalation Criteria' docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q '22-android-knox-investigation.md' docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q 'Before escalating, collect' docs/l1-runbooks/28-android-knox-enrollment-failed.md && ! grep -q 'SafetyNet' docs/l1-runbooks/28-android-knox-enrollment-failed.md && ! grep -qE 'supervis(ed|ion|or)' docs/l1-runbooks/28-android-knox-enrollment-failed.md</automated>
  </verify>
  <acceptance_criteria>
    - 4 Cause headings present: `grep -c '^## Cause [A-D]:' docs/l1-runbooks/28-android-knox-enrollment-failed.md` returns 4
    - Each Cause has D-10 sub-sections: `grep -c 'Admin Action Required' docs/l1-runbooks/28-android-knox-enrollment-failed.md` returns at least 4
    - Each Cause has Symptom: `grep -c '### Symptom' docs/l1-runbooks/28-android-knox-enrollment-failed.md` returns at least 4
    - Each Cause has L1 Triage Steps: `grep -c '### L1 Triage Steps' docs/l1-runbooks/28-android-knox-enrollment-failed.md` returns at least 4
    - Each Cause has Escalation (within Cause X): `grep -cE '### Escalation \(within Cause [A-D]\)' docs/l1-runbooks/28-android-knox-enrollment-failed.md` returns at least 4
    - `Say to the user` script present in each Cause: `grep -c '> \*\*Say to the user:\*\*' docs/l1-runbooks/28-android-knox-enrollment-failed.md` returns at least 4
    - `<!-- verify UI at execute time -->` HTML comments present: `grep -c 'verify UI at execute time' docs/l1-runbooks/28-android-knox-enrollment-failed.md` returns at least 3
    - `## Escalation Criteria` H2 present
    - Cross-link to L2 runbook 22 in Escalation Criteria: `grep -q '22-android-knox-investigation.md' docs/l1-runbooks/28-android-knox-enrollment-failed.md`
    - D-12 escalation packet present: `grep -q 'Before escalating, collect:' docs/l1-runbooks/28-android-knox-enrollment-failed.md`
    - Cause E (escalate-only) section: `grep -q 'Cause E (DPC Custom JSON' docs/l1-runbooks/28-android-knox-enrollment-failed.md`
    - Cross-links to admin doc 07 anchors: `grep -c '07-knox-mobile-enrollment.md' docs/l1-runbooks/28-android-knox-enrollment-failed.md` returns at least 5
    - Back-to-triage footer: `grep -q '\[Back to Android Triage\](../decision-trees/08-android-triage.md)' docs/l1-runbooks/28-android-knox-enrollment-failed.md`
    - `## Version History` H2 with table present
    - Audit C1: `! grep -q 'SafetyNet'`
    - Audit C2: `! grep -qE 'supervis(ed|ion|or)'`
    - File total line count `wc -l` returns at least 200 (substantial L1 runbook)
    - VALIDATION.md AEKNOX-02 unit-grep passes:
      ```
      test -f docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -qE "Cause A|Cause B|Cause C|Cause D" docs/l1-runbooks/28-android-knox-enrollment-failed.md && grep -q "Admin Action Required" docs/l1-runbooks/28-android-knox-enrollment-failed.md && ! grep -q "SafetyNet" docs/l1-runbooks/28-android-knox-enrollment-failed.md
      ```
  </acceptance_criteria>
  <done>L1 runbook 28 is complete with 4 Causes (D-10 actor-boundary), Escalation Criteria (D-12 packet), Cause E pointer, cross-links to admin doc 07 + L2 runbook 22 + triage tree. All 17 acceptance criteria pass. AEKNOX-02 VALIDATION.md unit-grep exits 0. Audit harness 8/8 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| L1 worker → admin escalation | L1 hands a structured packet to the admin who has KAP/Intune-admin access; the runbook gates what L1 can verify vs what only admin can verify |
| Markdown source → L1 worker action | Cross-linked anchors in admin doc 07 must exist before this runbook ships (Plan 01 dependency); broken anchors fail the L1 worker mid-triage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Information Disclosure (incorrect) | Cause A B2B approval timing | mitigate | Cite "1-2 business days" verbatim from Microsoft Learn `setup-samsung-knox-mobile`; 60-day freshness re-verifies; researcher OQ-2 re-verification at execute time |
| T-44-02 | Tampering (link rot in cross-references) | Cross-links to admin doc 07 anchors (#step-0-b2b-approval, #dpc-custom-json, #kme-zt-mutual-exclusion, #step-3-add-devices, #step-4-assign-profile) | mitigate | Plan 01 (Wave 1) ships admin doc with these exact anchor names; verify all anchors exist in Plan 01 file before this plan executes; audit-harness link-resolution re-verifies on freshness cycle |
| T-44-03 | Information Disclosure (incorrect) | Knox eFuse "tripped" behavior cited in Cause E | mitigate | Cite Microsoft Q&A `learn.microsoft.com/answers/questions/2282160` and Samsung Knox docs as source-of-truth; researcher OQ-4 (does KAP expose tripped status?) re-verification at execute time |
</threat_model>

<verification>
After both tasks complete:

1. Audit harness: `node scripts/validation/v1.4.1-milestone-audit.mjs` — expect 8/8 PASS; no new C1/C2/C5 failures.
2. AEKNOX-02 unit-grep predicate (from VALIDATION.md):
   ```bash
   test -f docs/l1-runbooks/28-android-knox-enrollment-failed.md && \
     grep -qE "Cause A|Cause B|Cause C|Cause D" docs/l1-runbooks/28-android-knox-enrollment-failed.md && \
     grep -q "Admin Action Required" docs/l1-runbooks/28-android-knox-enrollment-failed.md && \
     ! grep -q "SafetyNet" docs/l1-runbooks/28-android-knox-enrollment-failed.md
   ```
   Expected: exit 0.
3. Cross-link integrity: every cross-link to admin doc 07 anchors must resolve. Spot-check by `grep -E '07-knox-mobile-enrollment.md#[a-z0-9-]+'` and confirm each anchor exists in `docs/admin-setup-android/07-knox-mobile-enrollment.md`.
</verification>

<success_criteria>
- File `docs/l1-runbooks/28-android-knox-enrollment-failed.md` exists with all sections (frontmatter, Platform gate, H1, Applies-to, ANDR28 routing, Prerequisites, L1 scope note, How to Use, 4 Causes A-D, Escalation Criteria with D-12 packet + Cause E, Back-to-triage footer, Version History)
- 4 Causes follow D-10 actor-boundary (Entry/Symptom/L1 Triage Steps/Admin Action Required/Verify/Escalation-within-Cause)
- D-12 three-part escalation packet present with Knox-specific `Before escalating, collect:` data list
- Cause D mirrors runbook 27 Cause D inverse direction (KME side)
- Cross-link to L2 runbook 22 in Escalation Criteria
- Cross-links to admin doc 07 anchors all resolve
- Zero SafetyNet (C1), zero supervision (C2)
- Audit harness 8/8 PASS
- AEKNOX-02 VALIDATION unit-grep exits 0
- Closes Phase 40 ANDR28 placeholder
</success_criteria>

<output>
After completion, create `.planning/phases/44-knox-mobile-enrollment/44-02-SUMMARY.md` documenting:
- File created (path + line count)
- 4 Causes with anchor IDs
- D-10 sub-section pattern verified
- D-12 escalation packet present
- Phase 40 ANDR28 placeholder closure note
</output>
