---
phase: 14-apv2-l2-runbooks
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/decision-trees/04-apv2-triage.md
  - docs/lifecycle-apv2/00-overview.md
  - docs/_templates/l2-template.md
  - docs/l2-runbooks/00-index.md
  - docs/l2-runbooks/06-apv2-log-collection.md
autonomous: true
requirements: [TROU-04, TROU-05]
gap_closure: true

must_haves:
  truths:
    - "All forward references containing 'Phase 14' placeholder text are replaced with real markdown links"
    - "L2 template version gate offers both APv1 and APv2 variant text with authoring instruction to remove inapplicable variant"
    - "L2 index last_verified date reflects 2026-04-12 (the date APv2 section was added)"
    - "APv2 log collection guide uses Get-CimInstance instead of deprecated Get-WmiObject"
  artifacts:
    - path: "docs/decision-trees/04-apv2-triage.md"
      provides: "Escalation Data table with real L2 runbook links for APE1, APE2, APE3"
      contains: "08-apv2-deployment-report.md"
    - path: "docs/lifecycle-apv2/00-overview.md"
      provides: "Corrected link to 06-apv2-log-collection.md without Phase 14 placeholder"
      contains: "06-apv2-log-collection.md"
    - path: "docs/_templates/l2-template.md"
      provides: "Framework-neutral version gate with APv1/APv2 variant choice"
      contains: "Remove inapplicable variant"
    - path: "docs/l2-runbooks/00-index.md"
      provides: "Updated last_verified metadata"
      contains: "last_verified: 2026-04-12"
    - path: "docs/l2-runbooks/06-apv2-log-collection.md"
      provides: "CIM-based cmdlet replacing deprecated WMI cmdlet"
      contains: "Get-CimInstance"
  key_links:
    - from: "docs/decision-trees/04-apv2-triage.md"
      to: "docs/l2-runbooks/08-apv2-deployment-report.md"
      via: "Escalation Data See Also column"
      pattern: "08-apv2-deployment-report\\.md"
    - from: "docs/lifecycle-apv2/00-overview.md"
      to: "docs/l2-runbooks/06-apv2-log-collection.md"
      via: "L2 detail collapsible section"
      pattern: "06-apv2-log-collection\\.md"
---

<objective>
Close all verification gaps from Phase 14 VERIFICATION.md: fix 4 unresolved forward references containing "Phase 14" placeholder text, replace hardcoded APv1 version gate in L2 template with framework-neutral variant choice, update stale last_verified metadata, and replace deprecated Get-WmiObject with Get-CimInstance.

Purpose: Phase 14 scored 7/9 truths verified. These fixes close the remaining 2 gaps (1 partial, 1 failed) and resolve 2 anti-patterns identified during verification.
Output: All 5 files updated with targeted text replacements. No new files created.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-apv2-l2-runbooks/14-VERIFICATION.md
@.planning/phases/14-apv2-l2-runbooks/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix forward references and stale metadata (3 files)</name>
  <files>docs/decision-trees/04-apv2-triage.md, docs/lifecycle-apv2/00-overview.md, docs/l2-runbooks/00-index.md</files>
  <read_first>
    - docs/decision-trees/04-apv2-triage.md (current Escalation Data table at lines 71-77)
    - docs/lifecycle-apv2/00-overview.md (current L2 detail section at lines 85-92)
    - docs/l2-runbooks/00-index.md (current frontmatter at lines 1-5, APv2 escalation mapping at lines 58-64)
  </read_first>
  <action>
**Fix 1 — docs/decision-trees/04-apv2-triage.md lines 75-77:**

Replace the "See Also" column values in the Escalation Data table for all three APE rows. The L2 escalation mapping in `00-index.md` (lines 62-64) shows which L2 runbook each APE node maps to. Apply matching links:

- Line 75 (APE1 row): Replace `L2 runbooks (Phase 14)` with `[APv2 Deployment Report Guide](../l2-runbooks/08-apv2-deployment-report.md)`
- Line 76 (APE2 row): Replace `L2 runbooks (Phase 14)` with `[APv2 Deployment Report Guide](../l2-runbooks/08-apv2-deployment-report.md)`
- Line 77 (APE3 row): Replace `L2 runbooks (Phase 14)` with `[APv2 Deployment Report Guide](../l2-runbooks/08-apv2-deployment-report.md), [APv2 Event ID Reference](../l2-runbooks/07-apv2-event-ids.md)`

These link targets match the escalation mapping already established in `docs/l2-runbooks/00-index.md` lines 62-64 (APE1 -> deployment report, APE2 -> deployment report, APE3 -> deployment report + event IDs).

**Fix 2 — docs/lifecycle-apv2/00-overview.md line 90:**

Replace the entire broken link and placeholder text on line 90. Current text:
```
The BootstrapperAgent process manages the APv2 deployment flow on-device. Event IDs are documented in the [L2 APv2 Log Collection Guide](../l2-runbooks/apv2-log-collection.md) (Phase 14).
```

Replace with:
```
The BootstrapperAgent process manages the APv2 deployment flow on-device. Event IDs are documented in the [L2 APv2 Log Collection Guide](../l2-runbooks/06-apv2-log-collection.md).
```

Two changes: (1) fix filename from `apv2-log-collection.md` to `06-apv2-log-collection.md`, (2) remove ` (Phase 14)` trailing text.

**Fix 3 — docs/l2-runbooks/00-index.md line 2:**

Replace `last_verified: 2026-03-21` with `last_verified: 2026-04-12` to reflect the date the APv2 section was added.

Also update `review_by` on line 3 to 90 days after new last_verified: replace `review_by: 2026-06-19` with `review_by: 2026-07-11` (2026-04-12 + 90 days).
  </action>
  <verify>
    <automated>cd D:/claude/Autopilot && grep -n "Phase 14" docs/decision-trees/04-apv2-triage.md docs/lifecycle-apv2/00-overview.md && echo "FAIL: Phase 14 placeholder still present" || echo "PASS: No Phase 14 placeholders remain" && grep -c "08-apv2-deployment-report.md" docs/decision-trees/04-apv2-triage.md && grep -c "06-apv2-log-collection.md" docs/lifecycle-apv2/00-overview.md && grep "last_verified: 2026-04-12" docs/l2-runbooks/00-index.md && grep "review_by: 2026-07-11" docs/l2-runbooks/00-index.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 14" docs/decision-trees/04-apv2-triage.md` returns 0
    - `grep -c "Phase 14" docs/lifecycle-apv2/00-overview.md` returns 0
    - `grep -c "08-apv2-deployment-report.md" docs/decision-trees/04-apv2-triage.md` returns 3 (one per APE row)
    - `grep -c "07-apv2-event-ids.md" docs/decision-trees/04-apv2-triage.md` returns 1 (APE3 row only)
    - `grep "06-apv2-log-collection.md" docs/lifecycle-apv2/00-overview.md` matches (correct filename with 06- prefix)
    - `grep -c "apv2-log-collection.md" docs/lifecycle-apv2/00-overview.md` returns 1 (only the corrected link, no bare filename)
    - `grep "last_verified: 2026-04-12" docs/l2-runbooks/00-index.md` matches
    - `grep "review_by: 2026-07-11" docs/l2-runbooks/00-index.md` matches
  </acceptance_criteria>
  <done>Zero occurrences of "Phase 14" placeholder text remain in 04-apv2-triage.md and 00-overview.md. All three APE rows link to real L2 runbook files. Overview link points to correct filename 06-apv2-log-collection.md. L2 index metadata updated to 2026-04-12 with correct 90-day review_by.</done>
</task>

<task type="auto">
  <name>Task 2: Fix L2 template version gate and replace deprecated WMI cmdlet</name>
  <files>docs/_templates/l2-template.md, docs/l2-runbooks/06-apv2-log-collection.md</files>
  <read_first>
    - docs/_templates/l2-template.md (full file — version gate at lines 21-22, authoring comment at lines 1-12)
    - docs/l2-runbooks/06-apv2-log-collection.md (line 66 with Get-WmiObject usage)
  </read_first>
  <action>
**Fix 4 — docs/_templates/l2-template.md lines 21-22:**

Replace the current hardcoded APv1 version gate:
```markdown
> **Version gate:** This guide applies to Windows Autopilot (classic).
> For Autopilot Device Preparation, see [APv1 vs APv2 disambiguation](../apv1-vs-apv2.md).
```

With a bracketed variant choice that authors select from:
```markdown
> **Version gate:** [This guide applies to Windows Autopilot (classic). | This guide covers Autopilot Device Preparation (APv2).] _Remove inapplicable variant._
> For [APv2 Device Preparation | APv1 (classic Autopilot)], see [APv1 vs APv2 disambiguation](../apv1-vs-apv2.md).
```

This gives template users an explicit choice: pick the variant matching their guide's framework, delete the other. The `_Remove inapplicable variant._` instruction in italics makes it clear this is authoring guidance, not content.

**Fix 5 — docs/l2-runbooks/06-apv2-log-collection.md line 66:**

Replace the deprecated WMI cmdlet with its CIM equivalent per CLAUDE.md performance guidance ("Use CIM sessions over WMI for better performance").

Replace:
```powershell
$serial = (Get-WmiObject -Class Win32_BIOS).SerialNumber
```

With:
```powershell
$serial = (Get-CimInstance -ClassName Win32_BIOS).SerialNumber
```

Changes: `Get-WmiObject` -> `Get-CimInstance`, `-Class` -> `-ClassName` (CIM parameter name).
  </action>
  <verify>
    <automated>cd D:/claude/Autopilot && grep "Remove inapplicable variant" docs/_templates/l2-template.md && grep -c "Autopilot (classic)" docs/_templates/l2-template.md | grep -q "1" && echo "PASS: variant choice present" && grep "Get-CimInstance" docs/l2-runbooks/06-apv2-log-collection.md && grep -c "Get-WmiObject" docs/l2-runbooks/06-apv2-log-collection.md | grep -q "0" && echo "PASS: WMI replaced with CIM"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Remove inapplicable variant" docs/_templates/l2-template.md` matches (authoring instruction present)
    - `grep -c "This guide covers Autopilot Device Preparation (APv2)" docs/_templates/l2-template.md` returns 1 (APv2 variant exists)
    - `grep -c "This guide applies to Windows Autopilot (classic)" docs/_templates/l2-template.md` returns 1 (APv1 variant exists as choice, not default)
    - The two variant texts appear on the same line separated by ` | ` inside brackets
    - `grep "Get-CimInstance" docs/l2-runbooks/06-apv2-log-collection.md` matches
    - `grep -c "Get-WmiObject" docs/l2-runbooks/06-apv2-log-collection.md` returns 0 (deprecated cmdlet fully removed)
    - `grep "ClassName" docs/l2-runbooks/06-apv2-log-collection.md` matches (CIM parameter name, not WMI -Class)
  </acceptance_criteria>
  <done>L2 template version gate offers bracketed APv1/APv2 variant choice with authoring instruction to remove inapplicable variant. No hardcoded APv1-only default. Log collection guide uses Get-CimInstance -ClassName instead of deprecated Get-WmiObject -Class.</done>
</task>

</tasks>

<verification>
After both tasks complete, run a comprehensive check:

1. **Zero Phase 14 placeholders across entire docs/ directory:**
   ```bash
   grep -r "Phase 14" docs/ --include="*.md" | grep -v "PLAN\|SUMMARY\|CONTEXT\|RESEARCH\|VERIFICATION\|REVIEW\|VALIDATION" || echo "CLEAN"
   ```

2. **No broken links to non-existent filenames:**
   ```bash
   grep -r "apv2-log-collection.md" docs/ --include="*.md" | grep -v "06-apv2-log-collection" || echo "CLEAN"
   ```

3. **Template version gate is framework-neutral:**
   ```bash
   grep -A1 "Version gate" docs/_templates/l2-template.md
   ```

4. **No deprecated Get-WmiObject in APv2 files:**
   ```bash
   grep -r "Get-WmiObject" docs/l2-runbooks/ --include="*.md" || echo "CLEAN"
   ```

5. **Metadata freshness:**
   ```bash
   grep "last_verified" docs/l2-runbooks/00-index.md
   ```
</verification>

<success_criteria>
- All 5 files modified with targeted text replacements
- Zero "Phase 14" placeholder occurrences in any docs/ markdown file outside of .planning/
- All APE1/APE2/APE3 See Also links resolve to real L2 runbook files
- Overview link to log collection guide uses correct 06- prefixed filename
- L2 template version gate offers both APv1 and APv2 as selectable variants
- L2 index last_verified reflects 2026-04-12, review_by reflects 2026-07-11
- Get-WmiObject fully replaced with Get-CimInstance in log collection guide
</success_criteria>

<output>
After completion, create `.planning/phases/14-apv2-l2-runbooks/14-03-SUMMARY.md`
</output>
