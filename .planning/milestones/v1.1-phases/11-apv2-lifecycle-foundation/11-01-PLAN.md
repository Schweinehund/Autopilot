---
phase: 11-apv2-lifecycle-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/lifecycle-apv2/00-overview.md
  - docs/lifecycle-apv2/01-prerequisites.md
  - docs/_templates/admin-template.md
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
must_haves:
  truths:
    - "Admin can read a complete APv2 model explanation with ETG as the central mechanism, distinct from APv1 ESP"
    - "Admin can find and apply an APv2 prerequisites checklist with deregistration as item 0 and OS version gate as item 1"
    - "Admin template exists with per-setting 'what breaks if misconfigured' callout pattern for Phases 15-16"
  artifacts:
    - path: "docs/lifecycle-apv2/00-overview.md"
      provides: "APv2 model explanation, ETG concept, how it differs from APv1"
      contains: "Enrollment Time Grouping"
    - path: "docs/lifecycle-apv2/01-prerequisites.md"
      provides: "Actionable prerequisites checklist with portal paths"
      contains: "Deregister device from Windows Autopilot"
    - path: "docs/_templates/admin-template.md"
      provides: "Admin guide template with what-breaks callout pattern"
      contains: "What breaks if misconfigured"
  key_links:
    - from: "docs/lifecycle-apv2/00-overview.md"
      to: "docs/lifecycle/00-overview.md"
      via: "version gate header cross-link"
      pattern: "lifecycle/00-overview"
    - from: "docs/lifecycle-apv2/01-prerequisites.md"
      to: "docs/apv1-vs-apv2.md"
      via: "See also footer"
      pattern: "apv1-vs-apv2"
---

<objective>
Create the APv2 lifecycle folder with the overview and prerequisites files, plus the admin template.

Purpose: Establishes the APv2 documentation space, explains the fundamental APv2 model (ETG vs ESP), provides the actionable prerequisites checklist that all downstream phases reference, and creates the admin guide template that Phases 15-16 depend on.

Output: Three new markdown files — `docs/lifecycle-apv2/00-overview.md`, `docs/lifecycle-apv2/01-prerequisites.md`, `docs/_templates/admin-template.md`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-apv2-lifecycle-foundation/11-CONTEXT.md
@.planning/phases/11-apv2-lifecycle-foundation/11-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor must follow -->

From docs/lifecycle/00-overview.md (frontmatter + version gate pattern):
```markdown
---
last_verified: 2026-03-14
review_by: 2026-06-12
applies_to: both
audience: both
---

> **Version gate:** This guide primarily covers Windows Autopilot (classic). APv2 (Device Preparation) differences are noted inline. For a full comparison, see [APv1 vs APv2 disambiguation](../apv1-vs-apv2.md).
```

From docs/_templates/l1-template.md (template comment + structure pattern):
```markdown
<!-- L1 RUNBOOK TEMPLATE
     Usage: Copy this file as your starting point...
     Rules:
     - Fill in all YYYY-MM-DD dates...
-->
---
last_verified: YYYY-MM-DD
review_by: YYYY-MM-DD
applies_to: APv1 | APv2 | both
audience: L1
---
```

From docs/_templates/l2-template.md (structure reference for admin template):
- Has: Context section, Investigation steps, Resolution scenarios, Tool References
- Admin template needs: Prerequisites, Steps with "what breaks" callouts, Verification, See Also
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create APv2 overview document</name>
  <files>docs/lifecycle-apv2/00-overview.md</files>
  <read_first>
    - docs/lifecycle/00-overview.md (pattern: frontmatter, version gate, Mermaid diagram style, section structure)
    - docs/apv1-vs-apv2.md (cross-link target — need exact filename and heading anchors)
    - .planning/phases/11-apv2-lifecycle-foundation/11-RESEARCH.md (authoritative content source)
  </read_first>
  <action>
Create `docs/lifecycle-apv2/` directory and `docs/lifecycle-apv2/00-overview.md` with the following structure:

**Frontmatter** (per D-12):
```yaml
---
last_verified: 2026-04-11
review_by: 2026-07-10
applies_to: APv2
audience: admin
---
```

**Version gate header** (per D-11):
```markdown
> **Version gate:** This guide covers Windows Autopilot Device Preparation (APv2).
> For Windows Autopilot (classic), see [Autopilot Lifecycle Overview](../lifecycle/00-overview.md).
> For framework selection, see [APv1 vs APv2](../apv1-vs-apv2.md).
```

**Title:** `# APv2: Windows Autopilot Device Preparation Overview`

**Sections to include:**

1. **How to Use This Guide** — Brief orientation paragraph (2-3 sentences). Audience is Intune admins deciding whether and how to deploy APv2. Link to `01-prerequisites.md` for readiness check, `02-deployment-flow.md` for the step-by-step process, `03-automatic-mode.md` for Cloud PC scenarios.

2. **What Is APv2 (Device Preparation)?** — Explain that APv2 is GA as of June 3, 2024 (user-driven mode). It replaces the hardware-hash pre-staging model with Enrollment Time Grouping (ETG). Key points:
   - No hardware hash registration required
   - No Enrollment Status Page (ESP) — integrated into policy
   - Device is added to a pre-defined security group at enrollment time (not pre-staged)
   - The Intune Provisioning Client (service principal AppID: `f1346770-5b25-470b-88bd-d5744ab7952c`) must be the owner of the ETG device security group

3. **Enrollment Time Grouping (ETG) — The Core Mechanism** — Dedicated section explaining ETG as the architectural difference from APv1. Include:
   - Phase 1: Admin pre-assigns apps/policies to the device security group
   - Phase 2: At enrollment, device is added to the group; selected apps/scripts install during OOBE
   - Why this matters: bypasses dynamic group evaluation delays entirely
   - The Device Preparation policy specifies which device group to use

4. **APv2 vs APv1: Key Differences** — Brief summary table (5-6 rows max, not the full comparison). Point readers to `../apv1-vs-apv2.md` for the complete comparison. Include: hardware hash (no vs yes), ESP (no vs yes), pre-provisioning (no vs yes), ETG (yes vs no), Windows 10 (no vs yes), GCCH/DoD (yes vs no).

5. **Deployment Modes** — Two subsections:
   - **User-driven (GA)**: User authenticates during OOBE, 10-step process covered in `02-deployment-flow.md`
   - **Automatic (Preview — Windows 365 only)**: Cloud PC agent triggers enrollment, no user interaction during OOBE, covered in `03-automatic-mode.md`

6. **Current Limits** — Table with:
   - Max apps selectable in Device Preparation policy: 25 (raised from 10, January 30, 2026)
   - Max PowerShell scripts: 10
   - Supported OS: Windows 11 22H2+ (with KB5035942), Windows 11 23H2+, Windows 11 24H2+
   - Windows 10: NOT supported

7. **L2 detail collapsible block** (per D-12 — HTML details/summary):
```html
<details>
<summary>L2 detail: Event log location for APv2 enrollment</summary>

Event log: `Microsoft-Windows-DeviceManagement-Enterprise-Diagnostics-Provider/Admin`

The BootstrapperAgent process manages the APv2 deployment flow on-device. Event IDs are documented in the [L2 APv2 Log Collection Guide](../runbooks-l2/apv2-log-collection.md) (Phase 14).

</details>
```

8. **See Also** (per D-11):
   - [APv2 Prerequisites Checklist](01-prerequisites.md)
   - [APv2 Deployment Flow (10-Step Process)](02-deployment-flow.md)
   - [APv2 Automatic Mode (Windows 365)](03-automatic-mode.md)
   - [APv1 vs APv2 Comparison](../apv1-vs-apv2.md)
   - [APv1 Lifecycle Overview](../lifecycle/00-overview.md)

**Source attribution:** Add footer: `*Content sourced from [Microsoft Learn — Autopilot Device Preparation Overview](https://learn.microsoft.com/en-us/autopilot/device-preparation/overview), verified April 2026.*`
  </action>
  <verify>
    <automated>grep -c "Enrollment Time Grouping" docs/lifecycle-apv2/00-overview.md && grep -c "f1346770-5b25-470b-88bd-d5744ab7952c" docs/lifecycle-apv2/00-overview.md && grep -c "version gate" docs/lifecycle-apv2/00-overview.md && grep -c "See Also" docs/lifecycle-apv2/00-overview.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/lifecycle-apv2/00-overview.md exists
    - File contains frontmatter with `applies_to: APv2` and `audience: admin`
    - File contains version gate blockquote with links to `../lifecycle/00-overview.md` and `../apv1-vs-apv2.md`
    - File contains heading `## Enrollment Time Grouping` (or similar with "Enrollment Time Grouping" in it)
    - File contains the Intune Provisioning Client AppID `f1346770-5b25-470b-88bd-d5744ab7952c`
    - File contains `<details>` collapsible block with L2 event log detail
    - File contains `## See Also` section with links to 01-prerequisites.md, 02-deployment-flow.md, 03-automatic-mode.md, ../apv1-vs-apv2.md, ../lifecycle/00-overview.md
    - File contains "June 3, 2024" (GA date)
    - File contains "25" as the current app limit (not 10)
  </acceptance_criteria>
  <done>APv2 overview document exists with ETG explanation, version gate, collapsible L2 detail, See Also footer, and all required cross-links</done>
</task>

<task type="auto">
  <name>Task 2: Create APv2 prerequisites checklist</name>
  <files>docs/lifecycle-apv2/01-prerequisites.md</files>
  <read_first>
    - docs/lifecycle-apv2/00-overview.md (just created — verify cross-link targets)
    - docs/apv1-vs-apv2.md (cross-link target for See Also)
    - .planning/phases/11-apv2-lifecycle-foundation/11-RESEARCH.md (authoritative prerequisites list in "Prerequisites (Complete List)" section)
  </read_first>
  <action>
Create `docs/lifecycle-apv2/01-prerequisites.md` with the following structure:

**Frontmatter:**
```yaml
---
last_verified: 2026-04-11
review_by: 2026-07-10
applies_to: APv2
audience: admin
---
```

**Version gate header** (per D-11):
```markdown
> **Version gate:** This guide covers Windows Autopilot Device Preparation (APv2).
> For Windows Autopilot (classic), see [Autopilot Lifecycle Overview](../lifecycle/00-overview.md).
> For framework selection, see [APv1 vs APv2](../apv1-vs-apv2.md).
```

**Title:** `# APv2 Prerequisites Checklist`

**Opening paragraph:** Brief statement that ALL prerequisites must be met before attempting APv2 deployment. If any are missing, the deployment will fail silently or with unclear errors.

**Warning callout** (per D-07 — FIRST item, before any other prerequisite):
```markdown
> **Warning:** If the device is registered as a Windows Autopilot device, the APv1 profile will take
> precedence over this APv2 policy — silently. Complete prerequisite 0 before any other step.
```

**Checklist format** (per D-06 — each item is a checkbox with: what to check, where to check it, what happens if missing):

- [ ] **0. Deregister device from Windows Autopilot (APv1)**
  - **Where to check:** Intune admin center > Devices > Windows > Windows enrollment > Devices
  - **What to do:** Search by serial number. If found, select > Delete.
  - **What happens if skipped:** Device deploys via APv1 ESP instead of APv2 — no error shown. The APv1 profile silently takes precedence.

- [ ] **1. Windows 11 OS version gate**
  - **Where to check:** Device spec sheet or `winver` command on device
  - **Minimum versions:** Windows 11 24H2 or later (no KB required); Windows 11 23H2 with KB5035942 or later; Windows 11 22H2 with KB5035942 or later. April 2024 media includes the required KB.
  - **What happens if missing:** APv2 Device Preparation policy is ignored; device enrolls without APv2 configuration. Windows 10 is NOT supported.

- [ ] **2. Microsoft Entra automatic enrollment configured**
  - **Where to check:** Entra admin center > Mobility (MDM and WIP) > Microsoft Intune > MDM user scope
  - **What to do:** MDM user scope must be set to "Some" or "All" (not "None").
  - **What happens if missing:** Device cannot auto-enroll in Intune during OOBE; APv2 deployment fails at enrollment step.

- [ ] **3. First user has Microsoft Entra join permissions**
  - **Where to check:** Entra admin center > Devices > Device settings > Users may join devices to Microsoft Entra
  - **What to do:** Ensure the setting is "All" or the user/group is included.
  - **What happens if missing:** User cannot join the device to Entra ID during OOBE; enrollment fails.

- [ ] **4. Licensing (one of the following)**
  - **Where to check:** Microsoft 365 admin center > Users > Active users > select user > Licenses
  - **Valid licenses:** Microsoft 365 Business Premium; Microsoft 365 F1 or F3; Microsoft 365 Academic A1, A3, or A5; Microsoft 365 Enterprise E3 or E5; Enterprise Mobility + Security E3 or E5; Intune for Education; Microsoft Entra ID P1/P2 + Microsoft Intune subscription.
  - **What happens if missing:** Intune enrollment succeeds but APv2 policy may not apply; behavior is inconsistent.

- [ ] **5. Network connectivity**
  - **Where to check:** Network/firewall configuration
  - **Required:** DNS resolution for internet names; Ports 80 (HTTP), 443 (HTTPS), 123 (UDP/NTP) open; Key URLs: `login.live.com`, Microsoft Entra ID endpoints, Intune endpoints, `lgmsapeweu.blob.core.windows.net` (diagnostic upload), `time.windows.com` (UDP 123), `*.msftconnecttest.com` (NCSI).
  - **Not supported during OOBE:** Smart card / certificate-based authentication; Intune policy-based proxy configuration for privileged access deployments.
  - **What happens if missing:** Device cannot reach enrollment or deployment service endpoints; OOBE hangs or fails at network step.
  - **Full endpoint list:** See [Network Endpoints Reference](../reference/endpoints.md)

- [ ] **6. RBAC permissions for APv2 administrator**
  - **Where to check:** Intune admin center > Tenant administration > Roles
  - **Required permission categories (5):** Device configurations (Read, Delete, Assign, Create, Update); Enrollment programs (Enrollment time device membership assignment); Managed apps (Read); Mobile apps (Read); Organization (Read).
  - **What happens if missing:** Admin cannot create or assign Device Preparation policies; enrollment time group assignment fails silently.

**See Also** (per D-11):
- [APv2 Overview](00-overview.md)
- [APv2 Deployment Flow](02-deployment-flow.md)
- [APv1 vs APv2 Comparison](../apv1-vs-apv2.md)
- [APv1 Lifecycle Overview](../lifecycle/00-overview.md)

**Source attribution:** `*Prerequisites sourced from [Microsoft Learn — APv2 Requirements](https://learn.microsoft.com/en-us/autopilot/device-preparation/requirements), verified April 2026.*`
  </action>
  <verify>
    <automated>grep -c "Deregister device from Windows Autopilot" docs/lifecycle-apv2/01-prerequisites.md && grep -c "Warning" docs/lifecycle-apv2/01-prerequisites.md && grep -c "What happens if" docs/lifecycle-apv2/01-prerequisites.md && grep -c "KB5035942" docs/lifecycle-apv2/01-prerequisites.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/lifecycle-apv2/01-prerequisites.md exists
    - File contains frontmatter with `applies_to: APv2` and `audience: admin`
    - File contains version gate blockquote linking to ../lifecycle/00-overview.md and ../apv1-vs-apv2.md
    - File contains warning blockquote about APv1 silently taking precedence BEFORE the first numbered prerequisite
    - File contains `- [ ] **0. Deregister device from Windows Autopilot` as the first checklist item
    - File contains `- [ ] **1. Windows 11 OS version gate` as the second checklist item
    - File contains "What happens if" for every checklist item (at least 7 occurrences)
    - File contains "KB5035942" in the OS version gate item
    - File contains "Windows 10" with "NOT supported" nearby
    - File contains `f1346770` or references the RBAC section for enrollment time group assignment
    - File contains `## See Also` with links to 00-overview.md, 02-deployment-flow.md, ../apv1-vs-apv2.md
  </acceptance_criteria>
  <done>Prerequisites checklist exists with APv1 deregistration as item 0, OS gate as item 1, all 7 prerequisite categories covered with "what happens if missing" for each, and cross-links in place</done>
</task>

<task type="auto">
  <name>Task 3: Create admin guide template</name>
  <files>docs/_templates/admin-template.md</files>
  <read_first>
    - docs/_templates/l1-template.md (pattern: HTML comment header with rules, frontmatter placeholders, section structure)
    - docs/_templates/l2-template.md (pattern: investigation/resolution structure — admin template diverges but follows same conventions)
  </read_first>
  <action>
Create `docs/_templates/admin-template.md` per D-03. This template is used by Phases 15 (APv2 Admin Setup) and 16 (APv1 Admin Setup). The critical element is the per-setting "what breaks if misconfigured" callout pattern (required by ADMN-06, ASET-03).

**Full file content:**

```markdown
<!-- ADMIN SETUP GUIDE TEMPLATE
     Usage: Copy this file as your starting point for any admin configuration guide.
     Rules:
     - Fill in all YYYY-MM-DD dates (review_by = last_verified + 90 days)
     - Set applies_to to APv1, APv2, or both
     - Replace all [bracketed placeholders] with actual content
     - Every configurable setting MUST have a "What breaks if misconfigured" callout
     - Use imperative voice for steps ("Navigate to...", "Select...", "Enter...")
     - Include full Intune portal navigation paths (e.g., Intune admin center > Devices > ...)
     - Link to relevant troubleshooting runbooks from "what breaks" callouts
     Reviewer: Intune Admin Lead (role, not person name)
-->
---
last_verified: YYYY-MM-DD
review_by: YYYY-MM-DD
applies_to: APv1 | APv2 | both
audience: admin
---

> **Version gate:** [This guide covers Windows Autopilot (classic) | This guide covers Windows Autopilot Device Preparation (APv2)].
> For [the other framework], see [link].
> For framework selection, see [APv1 vs APv2](../apv1-vs-apv2.md).

# [Admin Task Title]

## Prerequisites

- [Required admin role or RBAC permissions]
- [Required license or subscription]
- [Prior configuration that must be completed first — link to that guide]

## Steps

### Step 1: [Configuration action]

1. Navigate to **Intune admin center** > [full portal path].
2. Select **[option]**.
3. Configure **[setting name]**: [value or instruction].

   > **What breaks if misconfigured:** [Specific downstream failure this causes. Include: what the admin will see, what the end user will see, and link to the troubleshooting runbook that covers this failure.]
   > See: [Troubleshooting Runbook Title](../runbooks-l1/relevant-runbook.md)

4. [Next action in this step].

### Step 2: [Next configuration action]

1. [Portal action with full path].
2. Configure **[setting name]**: [value].

   > **What breaks if misconfigured:** [Consequence description.]

## Verification

- [ ] [How to confirm the configuration is correct — specific portal location to check, expected state]
- [ ] [Second verification check]

## Configuration-Caused Failures

| Misconfiguration | Symptom | Runbook |
|------------------|---------|---------|
| [Setting X set to wrong value] | [What admin or user sees] | [Link to runbook] |
| [Setting Y missing] | [What happens] | [Link to runbook] |

## See Also

- [Related admin guide](link)
- [Relevant lifecycle overview](link)
- [APv1 vs APv2 Comparison](../apv1-vs-apv2.md)
```

The template must be written exactly as shown above — it is a template with placeholders, not a filled-in guide.
  </action>
  <verify>
    <automated>grep -c "What breaks if misconfigured" docs/_templates/admin-template.md && grep -c "ADMIN SETUP GUIDE TEMPLATE" docs/_templates/admin-template.md && grep -c "Configuration-Caused Failures" docs/_templates/admin-template.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/_templates/admin-template.md exists
    - File starts with HTML comment `<!-- ADMIN SETUP GUIDE TEMPLATE`
    - File contains frontmatter block with `last_verified: YYYY-MM-DD`, `review_by: YYYY-MM-DD`, `applies_to:`, `audience: admin`
    - File contains version gate blockquote template
    - File contains `## Prerequisites` section
    - File contains `## Steps` section with numbered sub-steps
    - File contains at least 2 instances of `> **What breaks if misconfigured:**`
    - File contains `## Verification` section with checkbox items
    - File contains `## Configuration-Caused Failures` section with a table (Misconfiguration | Symptom | Runbook columns)
    - File contains `## See Also` section
    - File contains reference to `../apv1-vs-apv2.md`
  </acceptance_criteria>
  <done>Admin template exists with per-setting "what breaks if misconfigured" callout pattern, configuration-caused failures reverse-lookup table, and all required sections for Phases 15-16</done>
</task>

</tasks>

<verification>
All three files exist and follow established patterns:
1. `docs/lifecycle-apv2/00-overview.md` — APv2 model with ETG explanation, version gate, cross-links
2. `docs/lifecycle-apv2/01-prerequisites.md` — 7-item checklist with deregistration as item 0
3. `docs/_templates/admin-template.md` — Template with "what breaks" callout pattern
</verification>

<success_criteria>
- LIFE-01 partially addressed: APv2 overview explains the deployment model and ETG mechanism (flow diagram is in Plan 02)
- LIFE-02 fully addressed: Prerequisites checklist covers OS gate, licensing, auto-enrollment, APv1 deregistration
- Admin template created for Phase 16 dependency (D-03)
- All files have version gate headers (D-11) and See Also footers (D-11)
- All files use `audience: admin` frontmatter (D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/11-apv2-lifecycle-foundation/11-01-SUMMARY.md`
</output>
