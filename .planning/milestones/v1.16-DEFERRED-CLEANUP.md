# v1.16 Deferred Cleanup — v1.17+ Backlog

**Authored:** 2026-07-10 (Phase 125 Plan 125-07 close-gate — canonical `.planning/milestones/` artifact)
**Milestone:** v1.16 (EEE SOP Documentation-Standard Retrofit (Phase-2) + Pipeline/Structural Shelf-Clearing)
**Status:** FINALIZED at Phase 125 Plan 125-07 close-gate `{phase_125_close_SHA}` (2026-07-10).

**Authoring rationale:** v1.16 inherits the standalone deferred-cleanup artifact convention (Path-A from `.planning/milestones/v1.15-DEFERRED-CLEANUP.md`). Per the "**do NOT mask via deletion**" doctrine, all carried open items are PRESERVED verbatim and the new v1.16 deferrals are ADDED. Any item resolved during v1.16 is DROPPED here and recorded Closed in `.planning/milestones/v1.16-MILESTONE-AUDIT.md` (the "do NOT mask via deletion" doctrine governs *open* items only).

**Scope:** This document enumerates deferred **requirements** and **open technical items** only. Working-tree cruft (`.agents/`, `.obsidian/`, stray `graphify-out/`, `docs/graphify-out/`, `skills-lock.json`) is out of scope — deferred to `/gsd-complete-milestone`.

**Trigger-to-sweep:** Each section carries explicit criteria for when v1.17+ should pick up the work.

**Not deferred (resolved at close):** the Class-A chain-health remediation (`check-phase-51/92/99` → `readAtV115Close`, commit `ce62fe5`) is NOT a deferred item — it was resolved AT CLOSE via the Plan 125-05 one-round remediation (NO value-masking, NO frozen surface edited, CHAIN_SKIP ∅). Honestly recorded in `v1.16-MILESTONE-AUDIT.md`.

**Resolved during v1.16 (dropped from the carried v1.15 list, recorded Closed):** `DEFER-119-C` (pandoc YAML-alias nav-footer defect → fixed by PIPE-03/Phase 124), the PIPE-02 true-Draft-label probe (`PIPE-02-DRAFT-LABEL-PROBE` → executed and confirmed PASS by PIPE-05/Phase 124), `V1.16-STRUCTURAL-CLASSES` (the whole structural-retrofit scope of this milestone → Phases 121-123), `V1.16-DESCRIPTIVE-FILENAME-PASS` (→ PIPE-04/Phase 124), and `STALE-FROZEN-AT-CLOSE-HEADER-01` (→ HYG-01/Phase 120). All five are CLOSED, not carried forward. See `v1.16-MILESTONE-AUDIT.md` for the closing detail.

---

# Part A — v1.16 New Deferrals

## V116-PIN-DEFERRAL: Freeze the v1.16 Corpus (Back-Anchor Invariant — v1.17's Path-A Job)

**Scope:** The `scripts/validation/_lib/frozen-at-close.mjs` `MILESTONE_CLOSE_SHAS` map does NOT contain a `V116` entry after this close, and MUST NOT — a pin referencing the v1.16 close-gate SHA would create ordering circularity (the close-gate cannot reference its own not-yet-existing SHA; the back-anchor invariant requires every pin to reference a PAST close SHA). Freezing the v1.16 corpus (adding `V116` + `readAtV116Close`) is the **successor milestone's** job: v1.17's Atom 2 adds the `V116` pin the same way v1.16's Atom 2 added `V115`.

**Why it is NOT a v1.16 item:** back-anchor invariant (T-125-07-CIRCULAR mitigation). The close-gate SHA is recorded ONLY as a grep-recoverable placeholder in the MILESTONE-AUDIT, never as a pin.

**Where it belongs when resolved:** v1.17 Atom 2 — append `V116: '<v1.16 close-gate SHA>'` (recover via `git log --all --grep="MILESTONE-AUDIT" --grep="MILESTONE CLOSE" --all-match -1 --format=%H`) + `readAtV116Close` export, following the V18..V115 single-entry pattern.

**Resolution trigger (v1.17):** the v1.17 harness lineage bump (15th Path-A milestone). MANDATORY at that close.

**Estimated effort:** included in the v1.17 Atom 2 (additive; < 1 plan on its own).

**Status:** DEFERRED to v1.17 (structural — the successor milestone pins the predecessor). NEW at v1.16 close.

---

## DEFER-125-06-A: `docs/_glossary-android.md` (RE-179) Fails `guard-docx.mjs` CUSTOM-PROPS Check (Stale Frontmatter Key)

**Scope:** `node scripts/pipeline/guard-docx.mjs` on `docs/_glossary-android.md`'s converted `.docx` reports `[CUSTOM-PROPS/3] V-GUARD-CUSTOM-PROPS: FAIL -- unexpected custom property name(s) outside the known EEE key set: [phase_46_wave2_retrofit]`. Root cause: the file's frontmatter still carries a stale `phase_46_wave2_retrofit: 2026-04-25` key (line 11) predating the 9-key EEE custom-property set locked at Phase 113/D-04 OQ4. Pandoc promotes this non-standard key to a `docProps/custom.xml` Word custom property, and the guard's (correctly designed, lenient-but-strict) CUSTOM-PROPS check flags any key outside the known set.

**Why NOT fixed during v1.16:** discovered during Plan 125-06 (PIPE-02 CLOSE representative-set prep), whose `<files>` scope was limited to `PIPE-02-CLOSE-RUNBOOK.md` — editing a structural content doc from a prior milestone phase would be an unrelated-file fix, excluded per the executor's scope-boundary rule. The representative-set selection substituted `docs/android-lifecycle/00-enrollment-overview.md` (RE-185) for Android platform coverage instead — converts + guards clean.

**Where it belongs when resolved (v1.17):** remove the stale `phase_46_wave2_retrofit` frontmatter key from `docs/_glossary-android.md` (harmless historical artifact; the retrofit it marks is long complete) so the file guard-passes cleanly like its sibling glossaries.

**Resolution trigger (v1.17):** low priority — cosmetic, single-key removal; pick up opportunistically or when `docs/_glossary-android.md` is next touched for any other reason.

**Estimated effort:** < 1 plan (single frontmatter-key deletion + guard-docx.mjs re-run confirmation).

**Status:** NEW at v1.16 close. DEFERRED to v1.17. Does not affect grounding correctness (Android PIPE-02 coverage satisfied via the RE-185 substitute).

---

## Deployment — SharePoint Content-Approval (if Draft must gate retrieval)

**Scope:** PIPE-05 (v1.16) re-confirmed `Status: Draft` is a LABEL only on the shipped `·`-separated Platform-first EEE block — Draft docs ARE indexed and retrieved (both frontmatter and the visible body-text block now empirically exercised, closing the v1.15 Draft-probe caveat). If Draft must actually GATE retrieval (not just label), enabling + maintaining SharePoint content approval on the indexed library is required (tenant/ops config, not a doc change).

**Where it belongs when resolved:** a deployment-hardening task (operator responsibility).

**Resolution trigger:** when a genuine retrieval gate on Draft is required.

**Status:** CARRIED from v1.15 (unresolved — deployment lever, not a doc/harness fix). Re-confirmed still relevant at v1.16 close.

---

## Infra — Azure AI Search Structured Index

**Scope:** Upgrade from the native SharePoint knowledge source to an Azure AI Search + SharePoint indexer to make `platform`/`doc_type` filterable metadata (the D1 normalized labels are already the right term set).

**Resolution trigger:** when metadata-filtered retrieval is required at scale.

**Status:** CARRIED from v1.15/v1.14 (infra escalation). Unresolved at v1.16 close.

---

# Part B — Carried-Forward Open Items from v1.15 (PRESERVED verbatim per "do NOT mask via deletion")

*The following are preserved from `.planning/milestones/v1.15-DEFERRED-CLEANUP.md`. They remain unresolved / indefinite at v1.16 close. Source-of-truth, per-site root-cause detail, and trigger gates are unchanged; see the v1.15 artifact for full detail.*

## WINDOWS-CLONE-DEEPNEST-TIMEOUT-01: Chain-Apex Cold-Clone Deep-Nesting Subprocess Cost on Windows (CARRIED — DEEPER at Phase 125)

Running any chain validator from a cold fresh-clone on Windows exhibits an O(n²) cascade of cold Node subprocess spawns. **Carried forward from v1.15; DEEPER at Phase 125** — the apex chain is now `[48..124]` (+6 deeper than v1.15's `[48..118]`). **D-125-4 OS split:** the chain apex `check-phase-125.mjs` [48..124] cascades on Windows cold-clone; Linux GHA is sole-authoritative (apex 79/0/1 post-remediation, resolving to 80/0/0 at this close-gate). **Remediation options (v1.17+):** cache-warm clone / raise per-subprocess timeout / cache chain-validator subprocess results within a single apex invocation (the O(n²) durable fix — increasingly attractive as the chain deepens past [48..124]). **Status:** CARRIED FORWARD (still unresolved; chain now deeper [48..124]). DEFERRED to v1.17+. Does NOT affect harness correctness or cross-OS parity (leaf validators + harness still reproduce cleanly on Windows fresh clones).

## ACCEPTED-STANDALONE-CI-RED-01: Frozen Predecessor Integrity Workflows Go RED Standalone on the Evolved Corpus (CARRIED — NOW SPANS v1.4-v1.15)

The frozen `audit-harness-v1.4..v1.15-integrity.yml` workflows trigger on any PR touching `check-phase-*.mjs` and FAIL standalone against the evolved v1.16 corpus (frozen harnesses pin their own close-SHA corpus). Editing frozen workflows/harnesses is barred by D-00a; a frozen milestone-audit validates its OWN close-SHA corpus, not future evolved corpus. **v1.16 re-confirmation:** every predecessor `Run vX.Y milestone audit harness` job (v1.4…v1.15) failed C2/C7/C9 (v1.4 also C4/C5) against the retrofitted android-glossary/nav-hub corpus on the close PR — empirically proven pre-existing/architecturally-expected by running `v1.14-milestone-audit.mjs` against the v1.15-ship tree `3780c6f` (EXIT 1, 12/3), confirming v1.15 shipped with predecessor harnesses already red. Architecturally expected, not a regression; v1.16 chain correctness is guaranteed by the NEW `audit-harness-v1.16-integrity.yml` (all-green run 29068069953). **Status:** ACCEPTED, path-filter-scoped condition. CARRIED from v1.15/v1.14, now one milestone deeper. Not a blocker.

## O(n²)-CHAIN-RUNNER-REMEDIATION-01: Subprocess-Caching Rewrite for WINDOWS-CLONE-DEEPNEST

The durable fix for WINDOWS-CLONE-DEEPNEST-TIMEOUT-01: cache chain-validator subprocess results within a single apex invocation to avoid redundant sub-spawns at depth ≥ 2. Mitigation (Linux GHA sole-authoritative for the chain apex) makes this non-blocking. **Status:** CARRIED from v1.15/v1.14. DEFERRED to v1.17+. Estimated 2–4h (durable O(n²) fix); increasingly attractive as the chain deepens past [48..124].

## LATENT-NON-FROZEN-AWARE-CONTENT-ASSERTION-01: Predecessor Content Assertions Not Yet Frozen-Aware (PARTIALLY EXERCISED AGAIN at v1.16)

Predecessor `check-phase-NN.mjs` validators carrying DIRECT content assertions reading LIVE HEAD rather than their own milestone close-SHA can drift on a broad-corpus milestone. **v1.16 note:** the Plan 125-05 remediation converted three MORE (`check-phase-51/92/99` → `readAtV115Close`) that broke on the Phase-122 Mermaid→text-equivalent conversion — exactly the failure mode the v1.14/v1.15 latent-class prediction flagged. The flag-#6 plan-time scoping run (Plan 125-01) proactively identified this drift set BEFORE the close-gate, per the reaffirmed process lesson from v1.15's own carried item — a genuine process improvement (v1.15 discovered its drift only at the GHA apex; v1.16 pre-scoped it at plan time). The broader cohort still carries live-HEAD content assertions; only the ones that broke on each broad-corpus milestone have been converted so far. **Status:** CARRIED from v1.15/v1.14 (partially exercised again at v1.16; process lesson reaffirmed and demonstrably applied this milestone). DEFERRED to v1.17+ chain-validator retrospective. Estimated 2–4h.

## DEFER-119-A: `regenerate-supervision-pins.mjs --self-test` Advisory-RED (Frozen-Fixture vs Live-Corpus Divergence — ACCEPTED, re-confirmed at v1.16)

`node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 1 on the current tree. The `--self-test` classifier scans the LIVE Android corpus and compares its Tier-1 new-pin set against the FROZEN `scripts/validation/v1.7-audit-allowlist.json` fixture (immutable Phase-43/v1.7 baseline). The Phase-121/122 EEE retrofit shifted supervision line coordinates further, so the live classifier emits different line numbers than the frozen fixture. **v1.16 re-confirmation (Plan 125-02):** the identical FAIL was confirmed already present on the pre-edit committed file before the BASELINE_20 comment insertion — NOT introduced by this milestone's Atom 1. **Why it is NOT a v1.16 failure (accepted condition, unchanged):** the CI job that runs this helper is `pin-helper-advisory` (advisory, not a hard gate; it reported success on the authoritative Axis-2 run 29068069953). Editing the `v1.7-audit-allowlist.json` fixture is the forbidden anti-pattern; relaxing the classifier is forbidden. A frozen fixture validates its OWN v1.7-era corpus, not the evolved v1.16 corpus. **Status:** ACCEPTED advisory-RED condition. CARRIED from v1.15/v1.14. Not a blocker. Resolution trigger unchanged: only if the advisory-RED becomes operationally noisy.

---

## MTPSSO-01/02/03 + PSSO-FUT-03: Multi-Tenant Platform SSO (registration models, CA/compliance scoping, L2 troubleshooting, admin-setup notation)

Cross-tenant macOS Platform Credential registration, multi-tenant Conditional Access / compliance scoping, multi-tenant PSSO L2 troubleshooting, and the admin-setup counterpart notation. Net-new architectural scope. **Status:** DEFERRED to v1.17+. *(Carried v1.10 → … → v1.16.)*

## KRBFUT-01/02: On-Prem-AD-Only Kerberos Realm Deep-Dive + Azure Files Cloud-Kerberos GA

On-prem-AD-only Kerberos realm configuration; full Azure Files Cloud-Kerberos macOS coverage on GA promotion. **Status:** DEFERRED to v1.17+. *(Carried v1.10 → … → v1.16.)*

## CI-3: Managed Apple ID → Managed Apple Account Corpus Rename (45 occurrences / 16 files)

Corpus-wide rename pending Microsoft Intune adopting the rebrand portal-side. **Status:** DEFERRED to v1.17+. *(Carried v1.6 → … → v1.16; byte-unchanged hazard + motivated scope-padding — reconfirmed DEFERRED.)*

## AOSP-Wired 802.1X + Cloud PKI / Intune Certificate Connector Deep-Dive

Android AOSP-specific wired 802.1X depth; full Cloud PKI / Intune Certificate Connector cert-delivery deep-dive for 802.1X. **Status:** DEFERRED to v1.17+ (carried from v1.14 Out-of-Scope).

---

# Part C — Carried-Forward v1.8 Deferred Items (PRESERVED verbatim per "do NOT mask via deletion")

*Preserved from `.planning/milestones/v1.15-DEFERRED-CLEANUP.md` Part B/C. They remain unresolved / indefinite at v1.16 close.*

- **ARCHIVE-UPSTREAM-01:** Upstream PR to get-shit-done-cc for the `extractOneLinerFromBody` regex fix (vendored fallback canonical until upstream lands). DEFERRED. *(Carried v1.8 → … → v1.16.)*
- **v1.1 line 164 `Edit N --` token-class:** `check-phase-71.mjs` `PLACEHOLDER_TOKENS` extension surface; reactive pickup. Documented for awareness. *(Carried v1.8 → … → v1.16.)*
- **HELPER-SPAWN-STDERR-01:** residual slice-budget tuning at the 3 helper-spawn stderr sites (core resolved by v1.14 TOOL-03). DEFERRED to v1.17+ chain-validator retrospective. *(Carried v1.8 → … → v1.16.)*
- **FROZEN-AWARE-ADOPTION-SWEEP-01:** broad-cohort frozen-aware sweep (core resolved by v1.14 TOOL-02; residual bundles with `LATENT-NON-FROZEN-AWARE-CONTENT-ASSERTION-01` — now +3 more converted at v1.16, Shape-1 `readAtV115Close` on check-phase-51/92/99). Explicitly OUT OF SCOPE this milestone (D-125-1 mandatory rider: triggered-only remediation, not a proactive sweep). DEFERRED. *(Carried v1.8 → … → v1.16.)*
- **Multi-tenant Apple Business surfaces:** cross-tenant device transfer / multi-org-account. DEFERRED indefinitely. *(Carried v1.8 → … → v1.16.)*
- **Apple Business Device API documentation:** on Apple publishing the developer landing page. DEFERRED to v1.17+. *(Carried v1.8 → … → v1.16.)*
- **Per-OU Conference Room Display deep-dive:** TRACKER ONLY (no acute demand v1.6–v1.16). *(Carried v1.8 → … → v1.16.)*
- **Account Holder lockout dedicated recovery runbook:** CONDITIONAL (promote on 5+ escalations/quarter). *(Carried v1.8 → … → v1.16.)*
- **Apple School Manager (ASM) education-specific surfaces:** DEFERRED to v1.17+ (outside enterprise scope). *(Carried v1.8 → … → v1.16.)*

*(EXEC-FAIL-DETAIL-EXTRACTION-01 was closed at v1.14 by TOOL-01 — recorded there; not re-listed as open.)*

---

## Cross-References

- **v1.16 milestone audit:** `.planning/milestones/v1.16-MILESTONE-AUDIT.md`
- **v1.15 predecessor (single-source-of-truth for carried items):** `.planning/milestones/v1.15-DEFERRED-CLEANUP.md`
- **Phase 125 3-axis re-audit results (Axis-2 GREEN + byte-gate EMPTY + D-125-4 OS split):** `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md`
- **Phase 125 one-round chain-health remediation (Shape-1 readAtV115Close on check-phase-51/92/99):** `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-05-SUMMARY.md`
- **Phase 125 PIPE-02 CLOSE grounding pass (owner PASS, retargeted v1.16-delta probes):** `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/PIPE-02-CLOSE-FINDINGS.md` + `PIPE-02-CLOSE-TRANSCRIPT.txt`
- **Phase 125 deferred-items log (`DEFER-125-06-A`):** `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/deferred-items.md`
- **Phase 125 close-gate verification:** `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-VERIFICATION.md`
- **v1.16 requirements (14/14 Validated):** `.planning/REQUIREMENTS.md`

---

*Authored 2026-07-10 at Phase 125 Plan 125-07 close-gate (`{phase_125_close_SHA}`). Part A: 2 NEW v1.16 deferrals (V116-pin deferral + DEFER-125-06-A guard-docx.mjs stale-key) + 2 carried deployment/infra items (SharePoint content-approval, Azure AI Search). Part B: WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 (now [48..124], +6 deeper, D-125-4 chain-apex Linux-authoritative) + accepted-standalone-CI-RED (now spans v1.4-v1.15) + O(n²) chain-runner + latent-content-assertion class (partially exercised again at v1.16, process lesson reaffirmed) + DEFER-119-A advisory-RED + MTPSSO/PSSO-FUT-03 + KRBFUT-01/02 + CI-3 + AOSP-wired-802.1X + Cloud-PKI, all carried from v1.15/v1.14. Part C: carried v1.8 items PRESERVED. Resolved-at-v1.16 items (DEFER-119-C, PIPE-02-DRAFT-LABEL-PROBE, V1.16-STRUCTURAL-CLASSES, V1.16-DESCRIPTIVE-FILENAME-PASS, STALE-FROZEN-AT-CLOSE-HEADER-01) DROPPED — recorded Closed in v1.16-MILESTONE-AUDIT.md. Working-tree cruft cleanup deferred to /gsd-complete-milestone. v1.17+ routing confirmed for all open items.*
