# v1.8 Deferred Cleanup — v1.9+ Backlog

**Authored:** 2026-06-04 (Phase 71 Plan 71-03 close-gate stub — ARCHIVE-UPSTREAM-01 + CHAIN-DEGRADED-AT-HEAD-01)
**Milestone:** v1.8 (Tooling Debt Closure + Chain-Resilience Hardening)
**Status:** STUB at Phase 71 Plan 71-03 close (2026-06-04). **Phase 74 HARNESS-12 FINALIZES** this artifact at v1.8 milestone close with v1.7 carry-overs promoted to full sections (CI-3 + Multi-tenant + Apple Business Device API + per-OU CRD + Account Holder + ASM) + any v1.8 retrospective discoveries beyond scope.

**Cross-link from:** `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-VERIFICATION.md` §Section E Discoveries + `.planning/milestones/v1.7-DEFERRED-CLEANUP.md` (v1.7 predecessor — single-source-of-truth for v1.7 deferrals carried forward)

**Authoring rationale:** v1.8 inherits the standalone deferred-cleanup artifact convention from v1.7 close (Plan 70-05 Commit B `4df3a16` per `v1.7-DEFERRED-CLEANUP.md:3` FINALIZED status). Plan 71-03 drafts the stub with TWO Phase-71-surfaced entries (ARCHIVE-UPSTREAM-01 + CHAIN-DEGRADED-AT-HEAD-01). Phase 74 HARNESS-12 (per REQUIREMENTS.md:41) inherits this stub and finalizes with v1.7 carry-overs (CI-3 + Multi-tenant + ABDevice API + per-OU CRD + Account Holder + ASM) promoted from `v1.7-DEFERRED-CLEANUP.md` (lines 225-333) + any v1.8 retrospective discoveries surfaced during Phases 72/73.

**Trigger-to-sweep:** Each section below carries explicit criteria for when v1.9+ should pick up the work.

---

## ARCHIVE-UPSTREAM-01: Upstream PR to get-shit-done-cc for extractOneLinerFromBody regex fix

**Scope:** Submit corrected `extractOneLinerFromBody` regex as an upstream PR to the `get-shit-done-cc` repository at `sdk/src/query/phase-lifecycle-policy.ts` lines 54-58. The upstream defect captures the bolded LABEL ("One-liner:") instead of the VALUE that follows it; Phase 71 Plan 71-01 vendored the corrected regex locally at `scripts/archive/extract-summary-oneliners.mjs` (atomic SHA `e4887b2`), but the upstream defect still ships in published `get-shit-done-cc` releases. v1.8+ pickup: contribute the corrected regex back to upstream so future projects don't need to vendor a local fallback.

**Source-of-truth:**
- Plan 71-01 SUMMARY (atomic SHA `e4887b2`) — vendored fix landed at `scripts/archive/extract-summary-oneliners.mjs`
- `71-VERIFICATION.md` §Section E Discovery #2 — D-01 LOCKED Option B vendor-strategy rationale
- `71-CONTEXT.md` D-01 dossier — Option B (vendor + frontmatter pre-write) wins over Option A (block on upstream PR cadence)

**Upstream defect site (READ BEFORE SUBMITTING PR):**
- Repository: `get-shit-done-cc` (npm package; `C:\Users\<user>\AppData\Roaming\npm\node_modules\get-shit-done-cc\` install path on Windows)
- File: `sdk/src/query/phase-lifecycle-policy.ts`
- Lines: 54-58 (`extractOneLinerFromBody` function declaration)
- Buggy regex: `/^#[^\n]*\n+\*\*([^*]+)\*\*/m` — captures group 1 returns LABEL ("One-liner:") instead of VALUE
- Caller: `sdk/src/query/phase-lifecycle.ts:1693` — `(fm['one-liner'] as string | undefined) || extractOneLinerFromBody(content)` (frontmatter-first preference; routes around the broken fallback only if frontmatter `one-liner:` is pre-written)

**Fix proposal:**
- Corrected regex: `/^#[^\n]*\r?\n+\*\*One-liner:\*\*\s+([^\r\n]+)/m` — anchors on literal `**One-liner:**` label, captures VALUE up to end of line
- Tested empirically against all 97 archived SUMMARY.md files in this repo using vendored implementation at `scripts/archive/extract-summary-oneliners.mjs --self-test` (3 fixtures PASS: label-not-captured / pre-write idempotency / placeholder-allowlist scan)
- 3-case unit-test fixture co-located at `scripts/archive/test-extract-oneliner.mjs` (3 PASS — label-not-captured / pre-write idempotency / CRLF tolerance)

**In-repo precedent (Phase 71 Plan 71-01):**
- Vendored extractor at `scripts/archive/extract-summary-oneliners.mjs` (11,689 bytes)
- Companion test fixture at `scripts/archive/test-extract-oneliner.mjs` (2,642 bytes)
- Chain-apex validator at `scripts/validation/check-phase-71.mjs` (14,516 bytes) — V-71-FIX-01 asserts vendored extractor exists with corrected regex anchor; V-71-FIX-02 asserts test fixture exercises 3 cases
- All 3 files landed atomically in Plan 71-01 SHA `e4887b2` (ROADMAP SC#4 byte-exact contract; Phase 67 Plan 67-02 `55260b3` 5-file atomic-within-plan precedent)

**Trigger gate for deprecation of vendored fallback:**
1. Upstream `get-shit-done-cc` releases corrected version (e.g., v1.x.y published with `extractOneLinerFromBody` regex fix)
2. Autopilot project's `package.json` pins to the fixed upstream version (or `gsd-sdk` CLI release equivalent)
3. Phase 75+ (or later v1.9+ entry phase) submits a deprecation commit removing `scripts/archive/extract-summary-oneliners.mjs` + `scripts/archive/test-extract-oneliner.mjs` + V-71-FIX-01/02 assertions in `check-phase-71.mjs`
4. Phase 74 HARNESS-12 plan-phase MAY add a temporary Wave-1 pre-step `node scripts/archive/extract-summary-oneliners.mjs --milestone v1.8 --pre-write-frontmatter` before `/gsd:complete-milestone v1.8` for v1.8 close as defense-in-depth (per D-01 LOCKED + CONTEXT line 71); this pre-step is OPTIONAL at Phase 74 since the caller's frontmatter-first preference already routes around the broken upstream fallback

**Routing decision:** v1.9+ (NOT gated on for v1.8 close). The vendor-strategy localizes the fix in-repo per D-01 LOCKED Option B — upstream-PR cadence cannot block v1.8 milestone close. Phase 74 HARNESS-12 inherits this stub and finalizes the artifact at v1.8 milestone close per REQUIREMENTS.md:41 contract.

**Estimated effort (v1.9+):** 1-2h for upstream PR (fork + branch + regex edit + upstream test additions + PR description citing in-repo precedent + maintainer review cycle).

**Status:** STUB drafted 2026-06-04 at Phase 71 Plan 71-03 close. Upstream PR work DEFERRED to v1.9+; vendored fallback in `scripts/archive/extract-summary-oneliners.mjs` remains canonical until upstream lands. Phase 74 HARNESS-12 will preserve this entry as-is (no promotion to v1.8 scope).

---

## CHAIN-DEGRADED-AT-HEAD-01: Pre-existing HEAD-coupled chain-validator FAILs at v1.8-Phase-71 HEAD

**Scope:** 8 pre-existing chain-validator FAILs surfaced during Plan 71-01 + Plan 71-02 + Plan 71-03 chain re-runs at v1.8-Phase-71 HEAD: **V-71-CHAIN-{61, 62, 63, 64, 65, 66, 67, 70}**. These FAILs are NOT caused by Phase 71 deliverables — they pre-existed Plan 71-01 SHA `e4887b2` and persist post-Plan-71-02 sweep (per Plan 71-02 SUMMARY line 88 + Plan 71-01 SUMMARY lines 76-85 disclosures). Root cause: HEAD-coupled assertions in `check-phase-61.mjs` and `check-phase-67.mjs` become stale post-v1.7-close (2026-05-29) as planning-doc and corpus state drifts. The same defect class (SCOPE-GAP-RETRO-01) v1.7-DEFERRED-CLEANUP.md routes via HARNESS-FORWARD-01 retrospective + SCOPE-GAP-61 retrospective entries (`v1.7-DEFERRED-CLEANUP.md:52-66, 110-131`).

**Source-of-truth:**
- Plan 71-01 SUMMARY (`71-01-SUMMARY.md`) §"Pre-existing chain degradation (NOT caused by Phase 71)" lines 76-85 — disclosure at Plan 71-01 atomic-commit SHA `e4887b2`
- Plan 71-02 SUMMARY (`71-02-SUMMARY.md`) §"Interpretation" lines 84-88 — `V-71-CHAIN-{61..67, 70}` FAIL post-Plan-71-02 (sweep did NOT introduce nor resolve them)
- `71-VERIFICATION.md` §Section E (Plan 71-03 close-gate) — formal documentation
- v1.7-DEFERRED-CLEANUP.md §HARNESS-FORWARD-01 (lines 52-66) — class-wide HEAD-coupled assertion retrospective scope
- v1.7-DEFERRED-CLEANUP.md §SCOPE-GAP-61 (lines 110-131) — V-NN-NN HEAD-coupled assertion conversion mechanism precedent (`readRoadmapAtV15Close()` parallel to `readRequirementsAtV15Close()`)

**Per-validator root cause:**

| Validator | Failing assertion(s) | Root cause |
|-----------|---------------------|------------|
| V-71-CHAIN-61 | V-61-17 (MILESTONES.md top-H2 must be v1.5) | HEAD shows v1.7 H2 at top (manually re-authored 2026-05-29 + Plan 71-02 re-author also touches v1.1/v1.2 below) — `check-phase-61.mjs` asserts v1.5-close state at HEAD but HEAD has drifted |
| V-71-CHAIN-62 | Cascade via V-NN-CHAIN-61 chain wrapper | check-phase-62.mjs recursively spawns check-phase-61.mjs via subprocess; exit-1 propagates |
| V-71-CHAIN-63 | Cascade via V-NN-CHAIN-61 chain wrapper | Same — recursive chain traversal |
| V-71-CHAIN-64 | Cascade via V-NN-CHAIN-61 chain wrapper | Same |
| V-71-CHAIN-65 | Cascade via V-NN-CHAIN-61 chain wrapper | Same |
| V-71-CHAIN-66 | Cascade via V-NN-CHAIN-61 chain wrapper | Same |
| V-71-CHAIN-67 | V-67-05 + V-67-06 (SWEEP-02 OP-10 callouts + Version History rows at v1.7-frozen SHA `aa6de68`) | Per Plan 70-05 Commit A discovery (STATE.md:242) — `aa6de68` was substituted for `{phase_70_close_SHA}` but is NOT the Commit-B SHA that authors the corpus OP-10/VH content the validator expects. Validator asserts content at a frozen SHA the content was never authored at. |
| V-71-CHAIN-70 | Cascade via V-70-CHAIN-{61..67} | Same recursive chain traversal mechanism |

**Why deferred to Phase 73 (not Phase 71 or 72):**

1. Phase 71 scope per REQUIREMENTS.md:11 (Pillar A — ARCHIVE-01 + ARCHIVE-02) is "archive-automation root-cause fix + historical residue sweep" — converting HEAD-coupled chain assertions is **out of Phase 71 contract**.
2. Phase 72 scope per REQUIREMENTS.md:21 (Pillar B — WRAPPER-01) is "check-phase-66.mjs:313 stderr→stdout+stderr capture" — single-line wrapper fix, NOT a class-wide HEAD-coupled assertion conversion.
3. Phase 73 scope per REQUIREMENTS.md:25-27 (Pillar C — RETRO-01 + RETRO-02) is **EXACTLY** the class-wide scan + per-validator conversion described above. The 8 FAILs documented here are the empirical "tripwire" that will validate Phase 73's RETRO-01 inventory + RETRO-02 conversion landing.

**Resolution mechanism (Phase 73 Pillar C):**

- **RETRO-01** (`HARNESS-FORWARD-01`-class scan): Class-wide scan of `check-phase-{48..66}.mjs` (19 validators) for HEAD-coupled assertions whose validator name or docstring cites a milestone-close state. V-61-17 + V-67-05 + V-67-06 will be in this inventory.
- **RETRO-02** (per-validator conversion): For each identified assertion, convert to v1.5/v1.6/v1.7-frozen-aware via SHA-pinned helpers parallel to existing `readRequirementsAtV15Close()` / `readRoadmapAtV15Close()` / `readCorpusFileAtV17Close()` (introduced in Plan 68-03 Task 1 `d7d7d5f` + Plan 69-02 Fix-2 `2d61981` + Plan 70-01 Wave 1 scaffold). Mechanism: `execFileSync('git', ['show', '<frozen-SHA>:<path>'])` per milestone-close.

**Scope-discipline guardrail (per REQUIREMENTS.md:27):** If Phase 73 RETRO-01 scan surfaces SCOPE-GAP-class discoveries beyond the initial 8-FAIL inventory documented here, route to v1.9+ rather than ballooning v1.8.

**Why NOT a Phase 72 entry-gate blocker:** Plan 71-01 atomic deliverables (vendored extractor + fixture + chain-apex validator) all PASS at HEAD (V-71-FIX-01 + V-71-FIX-02 PASS). Plan 71-02 sweep PASS at HEAD (V-71-MILESTONES-01 + V-71-ARCHIVE02-01 PASS). The 8 FAILs are in OTHER chain validators (check-phase-{61..67, 70}) that pre-date Phase 71 — Phase 72 Pillar B WRAPPER-01 fix at `check-phase-66.mjs:313` does NOT depend on those 8 validators passing; the wrapper-stdout-capture fix is independent.

**Trigger-to-sweep (Phase 73 — IN-MILESTONE):** Phase 73 plan-phase consumes this entry as the empirical baseline inventory. RETRO-01 + RETRO-02 close at Phase 73 close-gate; this entry transitions from STUB → CLOSED at Phase 74 HARNESS-12 finalization (or alternately at Phase 73 close-gate, depending on Phase 73 plan structure).

**Estimated effort (Phase 73):** 2-4h per `v1.7-DEFERRED-CLEANUP.md:66` HARNESS-FORWARD-01 estimate (class-wide scan + per-validator conversion) + scope-discipline guardrail.

**Status:** STUB drafted 2026-06-04 at Phase 71 Plan 71-03 close. Resolution mechanism (RETRO-01 + RETRO-02 in Phase 73) is pre-specified in REQUIREMENTS.md:25-27.

**Post-WRAPPER-01-fix update (Phase 72 Plan 72-02 close-gate, 2026-06-06):** Post-fix empirical stdout-diagnostic signature now visible at chain-apex output per Plan 72-02 close-gate witness `.claude/tmp/72-chain-post.txt`. Pre-fix FAIL detail for V-71-CHAIN-61 was empty (stderr-only wrapper masked stdout); post-fix detail carries: `check-phase-61 FAIL: check-phase-61 -- Phase 61 deliverables\n\n[1/34] V-61-01: ...` (first 500 chars per uniform CHAIN-wrapper slice budget). V-61-17 root-cause text (`top H2 is not v1.5: ## v1.7 Deferred Backlog Closure + Validator Chain Hardening (Shipped: 2026-05-29)`) and V-67-06 root-cause text (`expected >= 2 files with Version History; got 1`) now visible in chain-apex output at the slice-budget window (V-67-05/06 at offset ~750-900 within 500-char window; V-61-17 at offset ~2252 requires full-line traversal of `.claude/tmp/72-chain-post.txt` per RESEARCH.md Section 9). Phase 73 RETRO-01 should start from `.claude/tmp/72-chain-post.txt` as the empirically-grounded class-signature inventory. **Status transition: STUB → PARTIALLY-RESOLVED-EMPIRICAL-BASELINE-CAPTURED.**

**CLOSED (Phase 73 Plan 73-03 close-gate, 2026-06-08):** Per RETRO-02 conversion of V-61-17..20 root-cause assertions (readFile(MILESTONES_DOC) → readAtV15Close at `ba2cbc0`) + V-67-05/06 wrong-pattern fix (content-token callout + SWEEP-02 date-row assertion at `aa6de68`) + V-70-24 wrong-SHA fix (readProjectAtV17Close → readProjectAtV17CloseGate at `4df3a16`) — all landed in Plan 73-02 atomic SHA `a85da77` (5 files in ONE commit). CASCADE_ONLY validators (check-phase-{62..65}.mjs) self-healed at `a85da77`. Post-fix chain witness `.claude/tmp/73-chain-post.txt`: 39 PASS / 0 FAIL / 1 SKIPPED. The 7th and final entry in the chicken-and-egg accepted-residual lineage (Plan 68-05 → 69-02 → 70-05 Commit B → 71-01 → 72-01 → 73-01 → **73-02 RESOLUTION**) is CLOSED. Plan 73-03 close-gate SHA: recoverable via `git log --all --grep="73-03" --grep="close-gate" --all-match -1 --format=%H`. **Status: CLOSED.**

---

## v1.1 line 164 `Edit N --` token-class — NEW DISCOVERY flag for future archive-defect classes

**Scope:** During D-03 advisor pre-sweep grep enumeration (2026-06-03), the v1.1 line 164 residue `- Edit 1 -- docs/error-codes/00-index.md:` was discovered as a NEW token-class NOT in the original `v1.7-DEFERRED-CLEANUP.md:38-48` ARCHIVE-02 known-sites list. The `Edit N --` pattern is structurally distinct from the original 10 placeholder-label tokens (`One-liner:` / `SUBSUMED BY PLAN` / `Hash:` / `Pre-edit:` / `Total file size:` / `File:` / `Insertion position:` / `Single deliverable:` / `Plan goal:` / `Found during:`). PLACEHOLDER_TOKENS list at `scripts/validation/check-phase-71.mjs` was expanded to 11 entries via the `Edit \\d+ --` regex-fragment override mechanism (`t === 'Edit \\d+ --' ? t : t.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')` escape-branch per RESEARCH Pitfall 5).

**Source-of-truth:**
- `71-CONTEXT.md` D-03 dossier lines 110-134 — NEW DISCOVERY adversarial wedge
- `71-RESEARCH.md` lines 438-463 — PLACEHOLDER_TOKENS expansion + escape-branch mechanism
- Plan 71-01 SUMMARY — V-71-MILESTONES-01 + V-71-ARCHIVE02-01 transient FAIL outputs confirm `Edit 1 --` was matched by the 11-token regex
- Plan 71-02 SUMMARY §"Auto-fixed Issues" + post-sweep PASS confirms `Edit 1 --` residue cleared from MILESTONES.md v1.1 H2

**Why flagged here:** Future archive-defect classes may surface ADDITIONAL token-classes beyond the current 11 (e.g., a future SDK extractor bug emits `### Step N --` or `> Note:` placeholders). The check-phase-71.mjs PLACEHOLDER_TOKENS list is the canonical extension point. The escape-branch mechanism at the regex-compilation step lets future authors add regex-fragment tokens (like `Edit \\d+ --`) alongside literal tokens (like `One-liner:`).

**Resolution mechanism (when needed):**

1. Future archive-defect class surfaces (e.g., new placeholder label like `### Step N --` emitted by a future SDK upgrade)
2. Plan author adds the new token to `PLACEHOLDER_TOKENS` array in `scripts/validation/check-phase-71.mjs`
3. If the token requires regex-fragment escape (e.g., `\d+`), add an escape-branch case at the regex-compilation step (`buildGarbageRegex()` per RESEARCH Pitfall 5)
4. Re-run `check-phase-71.mjs` to confirm new token is caught + post-sweep validator passes

**Trigger-to-sweep:** Reactive only — when a future archive-defect class surfaces during a milestone close, the executor running the close-gate adds the new token to PLACEHOLDER_TOKENS. No proactive v1.9+ pickup required.

**Estimated effort:** 5-30min per new token class (1-line array addition + optional 3-line escape-branch + chain validator re-run).

**Status:** Documented for awareness 2026-06-04. The 11-entry PLACEHOLDER_TOKENS list is the canonical extension surface; no v1.9+ work currently planned.

---

## HELPER-SPAWN-STDERR-01: Helper-spawn stderr-only wrapper surface (3 sites, DEFERRED to v1.9+)

**Scope:** 3 helper-spawn stderr-only catch-block sites across `check-phase-{48, 60, 61}.mjs` identified during Phase 73 D-01 advisor analysis and confirmed during Phase 72 Plan 72-02 Section E audit. These sites spawn helper scripts (`regenerate-supervision-pins.mjs --self-test`) rather than chain-validator subprocesses, and carry a distinct failure-mode and slice-budget profile from CHAIN+AUDIT wrappers.

**Source-of-truth:**
- Phase 72 72-VERIFICATION.md Section E — DEFERRED_DOCUMENTED dispositions (check-phase-{48, 60, 61}.mjs helper-spawn rows)
- Phase 73 73-CONTEXT.md D-01 advisor dossier (`.claude/tmp/phase73-D01-advisor.md`) — Option C refined class-type carve-out rationale
- Phase 73 73-01-SUMMARY.md §Deviations (V-73-WRAPPER-NEG false-positive Rule 1 fix demonstrates the class distinction is real)

**Per-site root-cause table:**

| File | Line (approx.) | Wrapper class | Root cause | Phase 73 disposition |
|------|----------------|---------------|------------|----------------------|
| check-phase-48.mjs | 72 | Helper-spawn | Spawns `regenerate-supervision-pins.mjs --self-test`; only captures `err.stderr` | DEFERRED to v1.9+ |
| check-phase-60.mjs | 188 | Helper-spawn | Same helper-spawn pattern; `PIN_HELPER --self-test` catch block; separate from CHAIN-wrapper at line 230 | DEFERRED to v1.9+ |
| check-phase-61.mjs | 403 | Helper-spawn | Same helper-spawn pattern; separate from CHAIN-wrapper at line 368 and AUDIT-wrapper at line 386 | DEFERRED to v1.9+ |

**Why deferred to v1.9+:**
1. **Different failure-mode:** Helper-spawn wrappers catch `regenerate-supervision-pins.mjs --self-test` failures — a deterministic supervision-pin-drift signal, NOT a chain-validator diagnostic. The `err.stderr`-only capture is less critical because the failure is self-contained and the error message is supervision-specific.
2. **Different slice-budget profile:** CHAIN+AUDIT wrappers use a 500-char slice budget tuned for multi-validator output (first 34+ assertion lines). Helper-spawn failures are typically 1-3 lines. A different slice budget design pass is needed.
3. **D-01 class-type carve-out LOCKED:** Phase 73 73-CONTEXT.md D-01 advisor confirmed Option C refined: fold 8 CHAIN+AUDIT sites in Plan 73-01; route 3 helper-spawn sites to v1.9+ as a class-type distinction. Changing this routing at Plan 73-03 close-gate would violate REQUIREMENTS.md:67 scope-discipline guardrail.
4. **V-73-WRAPPER-NEG correctly excludes these sites:** check-phase-73.mjs V-73-WRAPPER-NEG added `isChainWrapper` discriminator (Plan 73-01 Rule 1 fix) to exclude helper-spawn catch blocks. The 3 sites are NOT counted as CHAIN-wrapper failures.

**Resolution mechanism:**
- v1.9+ scope: For each site, apply the same CHAIN-wrapper fix pattern BUT with a smaller slice budget appropriate for 1-3-line helper output (recommend 200 chars) and a `--self-test` discriminator in the detail prefix.
- Design pass needed: review whether `err.stdout` is even populated on `regenerate-supervision-pins.mjs --self-test` failure (may be stderr-only by design).

**Trigger-to-sweep:** v1.9+ chain-validator retrospective scope picks this up automatically when the class-wide stderr-only audit is next run. No urgent urgency — these are low-diagnostic-value sites.

**Estimated effort:** 30min design-pass + 30min 3-site mechanical edit + chain re-run sweep = ~1.5h.

**Status:** STUB drafted at Phase 73 Plan 73-03 close-gate (2026-06-08). v1.9+ pickup deferred. Phase 74 HARNESS-12 preserves this entry as-is.

---

## FROZEN-AWARE-ADOPTION-SWEEP-01: Existing inline frozen-aware helper refactor to _lib/frozen-at-close.mjs consumption (DEFERRED to v1.9+)

**Scope:** 4 validator files in `check-phase-{61, 67, 68, 70}.mjs` contain a total of ~13 inline frozen-aware helper functions that pre-date the Phase 73 `_lib/frozen-at-close.mjs` centralized module. These inline helpers are byte-identical to (or functionally equivalent to) the exported functions in `_lib/frozen-at-close.mjs`, but they are not consuming the centralized module.

**Source-of-truth:**
- Phase 73 73-CONTEXT.md D-02 advisor dossier (`.claude/tmp/phase73-D02-advisor.md`) — Option C decision rationale; strict-scope-discipline carve-out
- Phase 73 73-CONTEXT.md D-02 section lines 129-195 — helper-signature drift documented (v1.5-family lacks `stdio` hardening; v1.7-family has it)
- `_lib/frozen-at-close.mjs` module docstring — "HYBRID STATUS: NEW helpers (Phase 73 onward) consume from here. EXISTING helpers in check-phase-{61, 67, 68, 70}.mjs REMAIN INLINE."

**Per-file table:**

| File | Lines (approx.) | Inline helpers | Notes |
|------|-----------------|----------------|-------|
| check-phase-61.mjs | 38-62 | `readRequirementsAtV15Close()` + `readRoadmapAtV15Close()` | v1.5-family; introduced Plan 68-03 `d7d7d5f` + Plan 69-02 Fix-2 `2d61981`; LACKS `stdio: ['ignore','pipe','pipe']` hardening |
| check-phase-67.mjs | 35-57 | `readCorpusFileAtV17Close()` + `readSidecarAtV17Close()` | v1.7-family; introduced Plan 70-02 Atom 1 `26a1ae9`; HAS `stdio` hardening |
| check-phase-68.mjs | 35-50 | `readCorpusFileAtV17Close()` | v1.7-family; 3rd duplication site; byte-identical to check-phase-67.mjs version |
| check-phase-70.mjs | 40-82 | `readCorpusFileAtV17Close()` + `readMilestoneAuditAtV17Close()` + `readDeferredCleanupAtV17Close()` + `readRequirementsAtV17Close()` + `readProjectAtV17Close()` (EXISTING) + `readProjectAtV17CloseGate()` (NEW — Plan 73-02) | Mix of v1.5-family signature (lacks `stdio`) and v1.7-family (has it) |

**Why deferred to v1.9+:**
1. **Strict-scope-discipline carve-out (D-02 LOCKED):** Phase 73 D-02 advisor determined that refactoring existing inline helpers is sweep-scope beyond the RETRO-02 conversion contract. REQUIREMENTS.md:67 anti-ballooning guardrail applies.
2. **Helper-signature drift normalization needed:** v1.5-family helpers (`readRequirementsAtV15Close`, `readRoadmapAtV15Close`) lack `stdio: ['ignore','pipe','pipe']` hardening present in v1.7-family helpers and the `_lib` module. A normalized refactor must decide whether to harden the v1.5-family signature (behavioral change) or preserve it (drift preserved).
3. **13 inline helpers across 4 files** — refactor surface is bounded but non-trivial; each helper needs import rewrite + any signature normalization + chain re-run verification.
4. **Chain validators are NOT in the predecessor-byte-unchanged invariant** (REQUIREMENTS.md:69 carve-out) — but Phase 73's atomic-commit discipline prefers not to entangle this refactor with the RETRO-02 conversion scope.

**Resolution mechanism:** v1.9+ adoption sweep — replace inline helper bodies with imports from `_lib/frozen-at-close.mjs`:
```javascript
// Before (inline):
export function readRequirementsAtV15Close() { ... }
// After (consuming _lib):
import { readAtV15Close } from './_lib/frozen-at-close.mjs';
const readRequirementsAtV15Close = () => readAtV15Close('.planning/REQUIREMENTS.md');
```
Harden v1.5-family helpers to consume the new module's hardened signature at the same time. Verify chain still exits 0.

**Trigger-to-sweep:** v1.9+ chain-validator retrospective scope; or when Phase 74+ adds a new validator that needs v1.5-family frozen-aware reads (would naturally consume `_lib` and make the inline duplication more visible).

**Estimated effort:** 2-3h for 4-file refactor + helper-signature normalization design + chain sweep.

**Status:** STUB drafted at Phase 73 Plan 73-03 close-gate (2026-06-08). v1.9+ pickup deferred. Phase 74 HARNESS-12 preserves this entry as-is.

---

## EXEC-FAIL-DETAIL-EXTRACTION-01: DRY catch-block stdout+stderr capture across all wrappers via _lib/exec-fail-detail.mjs (DEFERRED to v1.9+)

**Scope:** Extract a `_lib/exec-fail-detail.mjs` helper module that DRYs the `(stdout + stderr).slice(0, NNN).trim()` pattern now duplicated across all CHAIN+AUDIT+helper-spawn wrappers in the chain validator suite. This is the "deferred idea" from Phase 72 D-01 advisor (`.claude/tmp/phase72-D01-advisor.md`) handed off at Phase 72 close-gate, classified as "overkill for the immediate fix" by Phase 73 D-02 advisor.

**Source-of-truth:**
- Phase 72 72-VERIFICATION.md Section F — "Empirical slice-budget observation" + `_lib/exec-fail-detail.mjs` deferred-idea mention
- Phase 73 73-CONTEXT.md D-02 advisor rationale — classified as overkill for RETRO-02 immediate scope; route to v1.9+
- Phase 68 `scripts/validation/_lib/archive-path.mjs` (Plan 68-02 SHA `79c65c6`) — canonical `_lib` module precedent shape

**Sites (as of Phase 73 close-gate):**
- **CHAIN wrappers:** check-phase-{60..65}.mjs (6 sites, CHAIN — Plan 73-01 fold-in) + check-phase-{66..71}.mjs (6 sites, CHAIN — Plan 72-01 fix) + check-phase-72.mjs (1 site, self-dogfood) + check-phase-73.mjs (1 site, self-dogfood) = 14 CHAIN wrapper sites
- **AUDIT wrappers:** check-phase-{60, 61}.mjs (2 sites, AUDIT — Plan 73-01 fold-in) + any Phase 72 AUDIT sites = ~2-4 AUDIT wrapper sites
- **Helper-spawn wrappers:** check-phase-{48, 60, 61}.mjs (3 sites — HELPER-SPAWN-STDERR-01; still stderr-only, would also benefit from this extraction)
- **Total estimate:** ~18-21 sites with the `(stdout + stderr).slice(0, NNN).trim()` pattern

**Module shape (mirrors `_lib/archive-path.mjs` precedent):**
```javascript
// scripts/validation/_lib/exec-fail-detail.mjs
export function execFailDetail(err, sliceBudget = 500) {
  const stderr = err.stderr ? err.stderr.toString() : '';
  const stdout = err.stdout ? err.stdout.toString() : '';
  return (stdout + stderr).slice(0, sliceBudget).trim();
}
export const isMissingExecutable = (err) =>
  err.code === 'ENOENT' || err.status === 127 ||
  (err.stderr || '').toString().includes('not found') ||
  (err.stderr || '').toString().includes('Could not resolve');
```

**Why deferred to v1.9+:**
1. **D-02 advisor "overkill for the immediate fix":** Phase 73's RETRO-02 goal is frozen-aware assertion conversion. Extracting a DRY helper for catch-block stderr capture is a separate DRY-refactor scope that crosses file-ownership boundaries beyond Phase 73's contract.
2. **Slice-budget variation requires design:** CHAIN wrappers use 500 chars; AUDIT wrappers use 300 chars; helper-spawn wrappers may use a different budget. A DRY extraction needs to decide whether to use a uniform budget or parameterize.
3. **Phase 68 `_lib/archive-path.mjs` precedent (Plan 68-02 SHA `79c65c6`):** That module had a well-scoped 5-call-site fix target. The `exec-fail-detail.mjs` scope is 18-21 sites across more files — a broader DRY refactor requiring its own planning phase.

**Resolution mechanism:** v1.9+ clean DRY-refactor scope; module shape mirrors `_lib/archive-path.mjs`:
- Extract `execFailDetail(err, sliceBudget)` and `isMissingExecutable(err)` into `_lib/exec-fail-detail.mjs`
- Replace all CHAIN+AUDIT+helper-spawn catch blocks with imports from this module
- Unify slice budgets or parameterize per wrapper class
- Chain sweep to confirm 0 FAIL

**Trigger-to-sweep:** v1.9+ clean tooling refactor phase; or when adding a new wrapper class that would benefit from the DRY pattern (Phase 74 HARNESS-09 may add check-phase-74.mjs as a natural extension point).

**Estimated effort:** 2-3h for module authoring + 18-21-site mechanical edit + chain sweep.

**Status:** STUB drafted at Phase 73 Plan 73-03 close-gate (2026-06-08). Phase 72 D-01 deferred-idea handed off. v1.9+ pickup deferred. Phase 74 HARNESS-12 preserves this entry as-is.

---

## Cross-References

- **Phase 71 close-gate verification:** `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-VERIFICATION.md`
- **Phase 71 Plan 71-01 SUMMARY (atomic SC#4 witness; SHA `e4887b2`):** `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-01-SUMMARY.md`
- **Phase 71 Plan 71-02 SUMMARY (re-authoring + chicken-and-egg resolution; SHA `ff4514b`):** `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-02-SUMMARY.md`
- **v1.7 predecessor (single-source-of-truth for v1.7 carry-overs):** `.planning/milestones/v1.7-DEFERRED-CLEANUP.md`
- **Phase 73 Pillar C contract (RETRO-01 + RETRO-02):** `.planning/REQUIREMENTS.md:25-27` + `.planning/ROADMAP.md:345-355` (Phase 73)
- **Phase 74 HARNESS-12 finalization contract:** `.planning/REQUIREMENTS.md:41` + `.planning/ROADMAP.md:357-368` (Phase 74)
- **Vendored extractor (ARCHIVE-UPSTREAM-01 in-repo precedent):** `scripts/archive/extract-summary-oneliners.mjs` (Plan 71-01 SHA `e4887b2`)
- **Test fixture (3-case companion):** `scripts/archive/test-extract-oneliner.mjs` (Plan 71-01 SHA `e4887b2`)
- **Chain-apex validator (V-71-FIX-01/02 + V-71-MILESTONES-01 + V-71-ARCHIVE02-01):** `scripts/validation/check-phase-71.mjs` (Plan 71-01 SHA `e4887b2`)
- **Upstream defect site (READ BEFORE PR):** `C:\Users\<user>\AppData\Roaming\npm\node_modules\get-shit-done-cc\sdk\src\query\phase-lifecycle-policy.ts:54-58`

---

*Drafted 2026-06-04 at Phase 71 Plan 71-03 close. Finalized by Phase 74 HARNESS-12 per REQUIREMENTS.md HARNESS-12 contract — v1.7 carry-overs (CI-3 + Multi-tenant + ABDevice API + per-OU CRD + Account Holder + ASM) promoted from `v1.7-DEFERRED-CLEANUP.md` lines 225-333 to full sections at v1.8 milestone close.*
