---
phase: 109-802-1x-integration-capability-matrices-navigation-hubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reference/4-platform-capability-comparison.md
  - docs/reference/macos-capability-matrix.md
  - docs/reference/ios-capability-matrix.md
  - docs/reference/linux-capability-matrix.md
  - docs/reference/android-capability-matrix.md
autonomous: true
requirements: [DOT1X-11]
must_haves:
  truths:
    - "A reader viewing any of the 5 capability matrices sees one consolidated Network Authentication (802.1X) row with a verdict word + one link per platform cell (SC1)"
    - "The 4-platform comparison Windows cell links to linux-capability-matrix.md#configuration (Windows-as-column convention), not to the Windows admin guide (PITFALL-1)"
    - "Verdicts are Windows=Supported, macOS=Supported, iOS=Partial, Android=Mode-dependent (comparison) / Partial per-mode (android matrix), Linux=Partial"
  artifacts:
    - path: "docs/reference/4-platform-capability-comparison.md"
      provides: "802.1X row in ## Configuration (5-state lock cells, per-platform matrix links)"
      contains: "Network Authentication (802.1X)"
    - path: "docs/reference/macos-capability-matrix.md"
      provides: "802.1X row in ## Configuration"
      contains: "Network Authentication (802.1X)"
    - path: "docs/reference/ios-capability-matrix.md"
      provides: "802.1X row in ## Configuration"
      contains: "Network Authentication (802.1X)"
    - path: "docs/reference/linux-capability-matrix.md"
      provides: "802.1X row in ## Configuration"
      contains: "Network Authentication (802.1X)"
    - path: "docs/reference/android-capability-matrix.md"
      provides: "802.1X row in ## Configuration (6 mode columns, all Partial)"
      contains: "Network Authentication (802.1X)"
  key_links:
    - from: "docs/reference/4-platform-capability-comparison.md"
      to: "docs/reference/linux-capability-matrix.md#configuration"
      via: "Windows-column matrix link in the 802.1X row"
      pattern: "Network Authentication .802\\.1X.*linux-capability-matrix.md#configuration"
    - from: "docs/reference/macos-capability-matrix.md"
      to: "docs/admin-setup-8021x/04-macos.md"
      via: "macOS cell guide link"
      pattern: "admin-setup-8021x/04-macos.md"
---

<objective>
Add one consolidated **Network Authentication (802.1X)** row to all 5 capability matrices (satisfies ROADMAP SC1). This is append-only navigation wiring — no 802.1X theory is restated (link-not-copy, PITFALL-7); each cell = a 5-state verdict word + one link to the per-platform matrix (in the comparison) or admin guide (in the per-platform matrices).

**Resolved decision — macOS verdict = `Supported` (Open Question 1):** Both Wi-Fi and Wired native Intune profile types exist for macOS (`docs/admin-setup-8021x/04-macos.md`). The SCEP-only-wired constraint is a cert-delivery nuance captured in the linked guide (link-not-copy), not a capability gap. This matches the existing "Certificate deployment (SCEP / PKCS / ACME)" row precedent at `4-platform-capability-comparison.md:44`, where macOS is `Supported` despite the identical SCEP-only-wired reality. iOS is `Partial` for a materially different reason (hardware eligibility gap — wired 802.1X limited to M-series iPads), so macOS=Supported / iOS=Partial is internally consistent.

Purpose: SC1 — Network-Authentication rows discoverable in every platform matrix, verdicts derived from live guide content.
Output: 5 modified reference matrices, one 802.1X row each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-802-1x-integration-capability-matrices-navigation-hubs/109-CONTEXT.md
@.planning/phases/109-802-1x-integration-capability-matrices-navigation-hubs/109-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 802.1X row to 4-platform-capability-comparison.md</name>
  <files>docs/reference/4-platform-capability-comparison.md</files>
  <read_first>
    - docs/reference/4-platform-capability-comparison.md (the ## Configuration section; confirm the "Certificate deployment (SCEP / PKCS / ACME)" row and the 5-state lock at :11/:13/:15)
    - 109-RESEARCH.md § "Deliverable 2 › File 1" (exact row + Pitfall 1) and § "Common Pitfalls › Pitfall 1"
  </read_first>
  <action>
    In the `## Configuration` section, insert ONE new row immediately AFTER the existing `| Certificate deployment (SCEP / PKCS / ACME) | ... |` row (reflects the cert→802.1X deployment dependency). Insert exactly this row (5-state lock vocabulary; Windows column links to `linux-capability-matrix.md` per the Windows-as-column convention, NOT to the Windows admin guide — PITFALL-1):
    `| Network Authentication (802.1X) | Supported — [matrix](linux-capability-matrix.md#configuration) | Supported — [matrix](macos-capability-matrix.md#configuration) | Partial — [matrix](ios-capability-matrix.md#configuration) | Mode-dependent — [matrix](android-capability-matrix.md#configuration) | Partial — [matrix](linux-capability-matrix.md#configuration) |`
    Do NOT restate 802.1X theory in any cell. Do NOT add a freshness stamp (structural nav, not versioned content). Append-only: touch no other row.
  </action>
  <verify>
    <automated>grep -F "Network Authentication (802.1X)" docs/reference/4-platform-capability-comparison.md && grep -F "Mode-dependent — [matrix](android-capability-matrix.md#configuration)" docs/reference/4-platform-capability-comparison.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Network Authentication (802.1X)"` returns exactly one line in the file.
    - That line's Windows (first) cell contains `linux-capability-matrix.md#configuration` and does NOT contain `admin-setup-8021x/03-windows.md` (PITFALL-1 guard).
    - Cells use only the 5-state lock words: Supported / Supported / Partial / Mode-dependent / Partial (Windows→macOS→iOS→Android→Linux order).
    - The row sits within `## Configuration`, on the line directly after the "Certificate deployment (SCEP / PKCS / ACME)" row.
    - Link targets resolve: `macos-capability-matrix.md`, `ios-capability-matrix.md`, `android-capability-matrix.md`, `linux-capability-matrix.md` all exist in `docs/reference/`, each with a `## Configuration` heading (slug `#configuration`).
    - `git diff --stat` shows only this file's Configuration table changed (append-only).
  </acceptance_criteria>
  <done>The 5-platform comparison carries one 802.1X row with 5-state verdicts and correct Windows-as-column linking.</done>
</task>

<task type="auto">
  <name>Task 2: Add 802.1X row to macOS, iOS, and Linux capability matrices</name>
  <files>docs/reference/macos-capability-matrix.md, docs/reference/ios-capability-matrix.md, docs/reference/linux-capability-matrix.md</files>
  <read_first>
    - docs/reference/macos-capability-matrix.md (## Configuration section, last row = "Declarative Device Management (DDM)"; and the ## Authentication section preamble — do NOT put 802.1X there, PITFALL-2)
    - docs/reference/ios-capability-matrix.md (## Configuration section, last row = "Security baselines")
    - docs/reference/linux-capability-matrix.md (## Configuration section, last row = "Hardware/firmware policies")
    - docs/admin-setup-8021x/04-macos.md, 05-ios.md, 07-linux.md (link targets — confirm each exists)
    - 109-RESEARCH.md § "Deliverable 2 › Files 2, 3, 5" and § "Common Pitfalls › Pitfall 2"
  </read_first>
  <action>
    Insert ONE 802.1X row into the `## Configuration` section of each file, appended after that section's current last row (do NOT use the macOS `## Authentication` PSSO section — PITFALL-2). Match each file's column arity. Insert exactly:
    - macOS (`| Feature | Windows | macOS |`, after the DDM row): `| Network Authentication (802.1X) | Supported — [guide](../admin-setup-8021x/03-windows.md) | Supported — [guide](../admin-setup-8021x/04-macos.md) |`
    - iOS (`| Feature | Windows | macOS | iOS |`, after the "Security baselines" row): `| Network Authentication (802.1X) | Supported — [guide](../admin-setup-8021x/03-windows.md) | Supported — [guide](../admin-setup-8021x/04-macos.md) | Partial — [guide](../admin-setup-8021x/05-ios.md) |`
    - Linux (`| Feature | Windows | Linux |`, after the "Hardware/firmware policies" row): `| Network Authentication (802.1X) | Supported — [guide](../admin-setup-8021x/03-windows.md) | Partial — [guide](../admin-setup-8021x/07-linux.md) |`
    Verdict words (Supported/Partial) come from the resolved-decision in the objective and RESEARCH Deliverable 1. No theory restated; no freshness stamp; append-only.
  </action>
  <verify>
    <automated>for f in macos ios linux; do grep -F "Network Authentication (802.1X)" docs/reference/${f}-capability-matrix.md; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 files contains exactly one `Network Authentication (802.1X)` row (grep -F).
    - macOS row cell count = 2 platform cells; iOS = 3; Linux = 2 (matches each file's header arity).
    - macOS + iOS Windows cell verdict = `Supported`; macOS cell = `Supported`; iOS cell = `Partial`; Linux cell = `Partial`.
    - Each cell links to the correct admin guide path (`../admin-setup-8021x/0{3,4,5,7}-*.md`); every linked file exists on disk.
    - The macOS row is NOT inside the `## Authentication` section (grep the row appears after the DDM row within `## Configuration`).
    - `git diff` shows only additive single-row changes in each file.
  </acceptance_criteria>
  <done>macOS/iOS/Linux matrices each carry one correctly-placed, correctly-linked 802.1X Configuration row.</done>
</task>

<task type="auto">
  <name>Task 3: Add 802.1X row to the mode-columned Android capability matrix</name>
  <files>docs/reference/android-capability-matrix.md</files>
  <read_first>
    - docs/reference/android-capability-matrix.md (## Configuration section; 6-column header COBO | COPE | BYOD | Dedicated | ZTE | AOSP; last row = "DDM (Declarative Device Management)")
    - docs/admin-setup-8021x/06-android.md (link target; confirms Wi-Fi supported all modes, no native wired profile, AOSP out-of-scope)
    - 109-RESEARCH.md § "Deliverable 1 › Android" (per-mode = Partial) and § "Deliverable 2 › File 4" and § "Common Pitfalls › Pitfall 3"
  </read_first>
  <action>
    In `## Configuration`, insert ONE row after the last existing row (`| DDM (Declarative Device Management) | ... |`). This matrix is mode-columned (6 columns), not Windows-bilateral. All six mode cells are `Partial` — Wi-Fi 802.1X is supported in every mode but no native Intune wired profile exists for any mode; BYOD additionally carries the UPN-in-SAN deployment-failure risk (PITFALL-3: do NOT mark BYOD `Supported`); the linked guide holds the per-mode nuance (link-not-copy). Insert exactly:
    `| Network Authentication (802.1X) | Partial — [guide](../admin-setup-8021x/06-android.md) | Partial — [guide](../admin-setup-8021x/06-android.md) | Partial — [guide](../admin-setup-8021x/06-android.md) | Partial — [guide](../admin-setup-8021x/06-android.md) | Partial — [guide](../admin-setup-8021x/06-android.md) | Partial — [guide](../admin-setup-8021x/06-android.md) |`
    No theory restated; no freshness stamp; append-only.
  </action>
  <verify>
    <automated>grep -c -F "Network Authentication (802.1X)" docs/reference/android-capability-matrix.md; grep -o -F "Partial — [guide](../admin-setup-8021x/06-android.md)" docs/reference/android-capability-matrix.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `Network Authentication (802.1X)` row (grep -c -F returns 1).
    - The row has 6 mode cells, every cell = `Partial — [guide](../admin-setup-8021x/06-android.md)` (count of that literal substring on the row = 6).
    - BYOD (3rd) cell is `Partial`, NOT `Supported` (PITFALL-3 guard).
    - `../admin-setup-8021x/06-android.md` exists on disk.
    - Row placed after the DDM row within `## Configuration`; `git diff` additive only.
  </acceptance_criteria>
  <done>Android mode-columned matrix carries one 802.1X row with all 6 mode cells = Partial.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only navigation edits, no executable code, no user input, no data flow. No package-manager installs (no npm/pip/cargo), so no package-legitimacy gate applies. RESEARCH.md § Security Domain confirms ASVS V2–V6 do not apply; security review is N/A for a nav-wiring phase.
</threat_model>

<verification>
- All 5 matrices contain exactly one `Network Authentication (802.1X)` row (SC1).
- Comparison Windows cell links to `linux-capability-matrix.md#configuration` (Windows-as-column).
- All cell link targets (`admin-setup-8021x/0{3,4,5,6,7}-*.md`, `*-capability-matrix.md`) exist and are committed.
- `git diff --name-only` lists only the 5 reference matrices.
</verification>

<success_criteria>
SC1 satisfied: Network-Authentication rows appear in all five platform matrices + the 4-platform comparison, with accurate verdicts derived from per-platform guide content and link-not-copy cells.
</success_criteria>

<output>
Create `.planning/phases/109-802-1x-integration-capability-matrices-navigation-hubs/109-01-SUMMARY.md` when done.
</output>
