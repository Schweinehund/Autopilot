---
phase: 104-ios-ipados-802-1x-admin-setup-wi-fi-wired
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-8021x/05-ios.md
autonomous: true
requirements: [DOT1X-06]

must_haves:
  truths:
    - "GOAL + SC1 + SC2 + SC3: 05-ios.md follows the cloned A3 structure (Common Profile Mechanics -> Wi-Fi -> Wired -> See Also -> Change History) and lets an Intune admin create iOS/iPadOS Wi-Fi and wired 802.1X profiles covering EAP-TLS / PEAP / EAP-TTLS at co-equal depth, with no method ranked or recommended as a default"
    - "D-05 + D-06 + D-07: the three-separate-profiles model is stated in Common Mechanics as STRUCTURAL PROSE (no blockquote callout, no Mermaid) -- trusted-root profile + SCEP/PKCS client-cert profile + Wi-Fi/Wired network profile; no combined .mobileconfig; Apple Configurator explicitly excluded (Intune-managed-fleet only); the cert-delivery ordering rule is LINKED to 02-cert-delivery-foundation.md, never restated"
    - "Carried from 103 (D-04): an explicit 'No authentication-mode selector on iOS/iPadOS' note in Common Mechanics tells Windows-trained admins the User/Machine/User-or-machine selector is absent; iOS authenticates as the current user context only (machine-level pre-logon auth not available)"
    - "SC3 + carried 103 D-11/D-12: server validation is homed once in Common Mechanics framed as a SECURITY requirement (always populate Certificate server names + reference a Root certificate for server validation profile), surfaces the iOS dynamic-trust-dialog symptom and the A-05 'disabling server validation is flagged as a security violation on iOS and macOS' fact, and LINKS the rogue-RADIUS rationale to 01-/02-/_glossary-network.md#server-name-validation without restating it; no example ever shows validation disabled"
    - "SC1 + D-01 + D-02 + D-03 + D-04: the MAC-randomization control lives in the Wi-Fi subsection ONLY, as a PROMINENT PLAIN-PROSE note (NOT a blockquote callout), leading with the exact phrasing 'Disable MAC address randomization: Yes' (Yes forces the actual Wi-Fi MAC for NAC), gated to iOS 14.0+/iPadOS 14.0+ with an inline freshness stamp, and stating explicitly that wired is unaffected (USB-Ethernet adapter presents its physical MAC)"
    - "D-08 + D-12: each connection subsection carries a complete per-EAP-method config matrix with three co-equal columns (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS) and an 'Inner method' row -- iOS Wi-Fi: EAP-TLS = -- (n/a, cert-only); PEAP = -- (no inner-method selector; MS-CHAPv2 implicit); EAP-TTLS = PAP / CHAP / MS-CHAP / MS-CHAP v2"
    - "D-10 + D-11: a standalone 'What breaks' WARNING blockquote callout in the Wi-Fi PEAP context states iOS PEAP inner auth MUST be MS-CHAPv2 (no PAP option in the Intune UI; PAP injected via custom profile fails immediately with 'Authentication Failed'; same SSID with PEAP+PAP may succeed on Windows/macOS while iOS fails); it is NOT placed in Common Mechanics"
    - "SC2 + carried 103 D-06: the Wired subsection carries a prominent standalone NOTE callout stating wired client certificates are SCEP only (PKCS not supported, all three EAP types) and points PKCS-only shops to 02-cert-delivery-foundation.md#per-platform-cert-delivery-support-matrix"
    - "SC2 + D-09: the Wired subsection opens with a 'When to use this' use-case paragraph (M-series iPads with a USB-Ethernet adapter; multi-iPad shared-use scenarios such as classrooms/labs) and carries a full peer-depth per-EAP matrix equal to Wi-Fi"
    - "D-12 wired hedge (RESEARCH §4 live finding): the iOS WIRED matrix shows Client Authentication = Certificates: SCEP only (PKCS not supported) for ALL three EAP types; the wired PEAP and wired EAP-TTLS Inner-method cells read 'cert-only in the iOS wired Intune UI (Templates path)' with NO MS-CHAPv2/PAP/CHAP inner-method values asserted as selectable on wired -- the macOS 'Username and Password' wired cells are NOT cloned"
    - "D-11 wired guardrail: a one-line cross-reference near the wired PEAP row directs wired-only readers to the Wi-Fi PEAP 'What breaks' WARNING for the MS-CHAPv2 context, so the absence of a username/password path in the wired UI does not blindside them"
    - "Network interface (iOS wired): documented as automatically 'Any Ethernet' (no selector) -- the macOS Network Interface selector table (First active Ethernet, etc.) is NOT cloned"
    - "Strip list honored: NO macOS deployment-channel WARNING / User-Device-keychain prose, NO Windows dot3svc / enforcement-staging DANGER / TEAP / KB5014754, NO Mermaid for the three-profiles model; the ONLY blockquote callouts in the file are the B-05 'What breaks' WARNING and the wired SCEP-only NOTE (callout discipline)"
    - "Link-not-copy: shared concepts (cert-delivery ordering rule, EKU, server-name-validation theory, rogue-RADIUS rationale, EAP-method comparison, per-platform cert matrix) are linked to 01-/02-/_glossary-network.md, never restated"
    - "Identity privacy (outer identity) guidance is present for all three EAP methods (anonymous / anonymous@domain) to prevent cleartext identity leakage before the tunnel"
    - "Freshness: file front-matter carries the 90-day stamp (last_verified 2026-06-30, review_by 2026-09-28, platform: ios, applies_to: both); the MAC-randomization note carries an ADDITIONAL inline freshness stamp"
  artifacts:
    - path: "docs/admin-setup-8021x/05-ios.md"
      provides: "iOS/iPadOS Wi-Fi + wired 802.1X admin-setup guide, all three co-equal EAP methods"
      contains: "## Wired"
      min_lines: 90
  key_links:
    - from: "docs/admin-setup-8021x/05-ios.md"
      to: "docs/admin-setup-8021x/02-cert-delivery-foundation.md#canonical-scope-callout"
      via: "scope banner link"
      pattern: "02-cert-delivery-foundation\\.md#canonical-scope-callout"
    - from: "docs/admin-setup-8021x/05-ios.md"
      to: "docs/admin-setup-8021x/02-cert-delivery-foundation.md#per-platform-cert-delivery-support-matrix"
      via: "wired SCEP-only NOTE -> per-platform cert matrix link"
      pattern: "per-platform-cert-delivery-support-matrix"
    - from: "docs/admin-setup-8021x/05-ios.md"
      to: "docs/admin-setup-8021x/01-eap-method-overview.md#peap-mschapv2"
      via: "server-validation rogue-RADIUS link-not-copy reference"
      pattern: "01-eap-method-overview\\.md#peap-mschapv2"
    - from: "docs/admin-setup-8021x/05-ios.md"
      to: "docs/_glossary-network.md#server-name-validation"
      via: "server-validation rationale link"
      pattern: "_glossary-network\\.md#server-name-validation"
---

<objective>
Author `docs/admin-setup-8021x/05-ios.md` — the iOS/iPadOS per-platform 802.1X admin-setup guide covering Wi-Fi and wired Intune profiles for all three co-equal EAP methods (EAP-TLS / PEAP / EAP-TTLS), satisfying DOT1X-06 and ROADMAP Phase 104 success criteria SC1–SC3.

Purpose: Give an Intune admin a single iOS/iPadOS guide that walks through Wi-Fi and wired 802.1X profile creation, surfaces the high-consequence iOS-specific facts (MAC-address-randomization control for NAC, the three-separate-profiles model, the M-series iPad wired use case, the wired SCEP-only constraint, the PEAP-inner-auth-must-be-MS-CHAPv2 break) and links — never restates — the shared 802.1X foundation in `01-`/`02-`/`_glossary-network.md`. This guide clones the Phase-103 macOS A3 Hybrid deliverable as its scaffold and applies the iOS deltas, stripping macOS-only deployment-channel mechanics and Windows-only mechanics (dot3svc, enforcement-staging DANGER, TEAP, KB5014754) entirely.

Output: `docs/admin-setup-8021x/05-ios.md` (CREATE), following the A3 structure: Common Profile Mechanics [three-profiles prose → no-auth-mode note → server validation → identity privacy] → Wi-Fi [nav path → MAC-randomization prose note → Wi-Fi per-EAP matrix → B-05 WARNING → EAP-TLS cert-options note] → Wired ["When to use this" → SCEP-only NOTE → "Any Ethernet" note → wired per-EAP matrix with hedged cells → B-05 cross-ref] → See Also → Change History.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-ios-ipados-802-1x-admin-setup-wi-fi-wired/104-RESEARCH.md
@.planning/phases/104-ios-ipados-802-1x-admin-setup-wi-fi-wired/104-PATTERNS.md
@.planning/phases/104-ios-ipados-802-1x-admin-setup-wi-fi-wired/104-CONTEXT.md
@docs/admin-setup-8021x/04-macos.md
@docs/admin-setup-8021x/03-windows.md
@docs/admin-setup-8021x/01-eap-method-overview.md
@docs/admin-setup-8021x/02-cert-delivery-foundation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold + Common Profile Mechanics + Wi-Fi subsection</name>
  <files>docs/admin-setup-8021x/05-ios.md</files>
  <read_first>
    - docs/admin-setup-8021x/05-ios.md — the file being created (does not exist yet)
    - docs/admin-setup-8021x/04-macos.md — the NEAREST clone scaffold (Phase 103 deliverable): front-matter (lines 1-7), Prerequisites blockquote (9-10), H1 + Scope banner (12-14), `## Common Profile Mechanics` opener (16+), `### Server Validation` (39-45), `### Anonymous Outer Identity` (47-56), Wi-Fi `### In Intune admin center` + per-EAP matrix (59-77), EAP-TLS client-cert note (78-82). Do NOT clone the deployment-channel WARNING block (20-36) or the deployment-channel sentence in the no-auth-mode paragraph (37) — no iOS equivalent
    - docs/admin-setup-8021x/03-windows.md — the original A3 execution; supplies the `> **WARNING -- label`/`> **NOTE` blockquote callout block syntax (used for the iOS B-05 WARNING) and the inline-freshness-stamp format (line 181) for the MAC-randomization note
    - 104-RESEARCH.md — Live Verification Findings §1 (MAC-randomization label + values), §2 (Wi-Fi EAP types + PEAP/EAP-TTLS inner auth), §3 (authoritative Wi-Fi per-EAP field map); Section-by-Section Authoring Guidance: Front Matter, Prerequisites, Scope Banner, Common Profile Mechanics (three-profiles prose + no-auth-mode note + Server Validation + Anonymous Outer Identity), Wi-Fi MAC-randomization note, Wi-Fi B-05 WARNING, Wi-Fi per-EAP matrix, client-cert-options note
    - 104-PATTERNS.md — Front-matter pattern (change platform:macos->ios), Prerequisites/Scope copy-verbatim rules, Common Profile Mechanics three-profiles adaptation + strip-deployment-channel rules, no-auth-mode note, Server Validation iOS adaptation, Anonymous Outer Identity field-label, Wi-Fi nav-path delta, Wi-Fi matrix delta map (l.194-208), MAC-randomization prose rules (l.211-219), B-05 WARNING rules (l.221-236), client-cert-options note (l.238-247); Shared Patterns (blockquote callout syntax, A3 heading sequence, per-EAP matrix shape, freshness stamp, anchor-slug double-hyphen trap, link-not-copy)
    - 104-CONTEXT.md — locked decisions D-01 through D-12 + executor guardrails; co-equal EAP, link-not-copy, navigation-last, callout-discipline, no-macOS-mechanics, no-Windows-mechanics hard constraints
    - docs/admin-setup-8021x/02-cert-delivery-foundation.md — confirm the live `#canonical-scope-callout` and `#per-platform-cert-delivery-support-matrix` anchor slugs before linking
    - docs/admin-setup-8021x/01-eap-method-overview.md — confirm the live `#peap-mschapv2` anchor slug; link target for the rogue-RADIUS rationale
    - docs/_glossary-network.md — confirm the live `#server-name-validation` and `#inner-outer-identity` anchor slugs before linking
  </read_first>
  <action>
    Create `docs/admin-setup-8021x/05-ios.md`. Start with the 6-key YAML front matter cloned from `04-macos.md` lines 1-7 with `platform: ios` (all other keys identical: `last_verified: 2026-06-30`, `review_by: 2026-09-28`, `applies_to: both`, `audience: admin`). Follow with the Prerequisites blockquote (copy verbatim from `04-macos.md` lines 9-10, linking `01-eap-method-overview.md` and `02-cert-delivery-foundation.md`), the H1 `# iOS/iPadOS 802.1X Admin Setup: Wi-Fi and Wired` (use ` and `, not ` / `, to avoid the double-hyphen anchor-slug trap), then the one-line Scope banner copied verbatim from `04-macos.md` line 14 linking `02-cert-delivery-foundation.md#canonical-scope-callout`.

    Author a `## Common Profile Mechanics` section. Order the content:
    1. Three-separate-profiles STRUCTURAL PROSE (D-05/D-06/D-07): a brief paragraph (no `> **Label:**` blockquote callout, no Mermaid) stating iOS/iPadOS 802.1X requires three distinct Intune profiles — one Trusted Certificate profile (RADIUS CA for server validation), one SCEP or PKCS client-certificate profile (client identity), and one Wi-Fi or Wired network profile; that there is no combined `.mobileconfig` and Apple Configurator is explicitly excluded (this guide covers Intune-managed-fleet only). LINK the cert-delivery ordering rule / EKU / per-platform cert options to `02-cert-delivery-foundation.md` (never restate the ordering rule inline — D-07 link-not-copy).
    2. A short "No authentication-mode selector on iOS/iPadOS" note (carried 103 D-04, plain prose, NOT a callout) stating iOS/iPadOS does not expose a User / Machine / User-or-machine authentication mode selector in Intune profiles, that Windows-trained admins will not find it, and that iOS authenticates as the current user context only (machine-level pre-logon authentication is not available through Intune iOS 802.1X profiles). Do NOT carry over any macOS deployment-channel sentence — no iOS equivalent.
    3. A `### Server Validation` subsection (carried 103 D-11/D-12) homed once for both connections and all three EAP methods: frame populating **Certificate server names** + referencing a **Root certificate for server validation** profile as a SECURITY requirement; surface the iOS dynamic-trust-dialog symptom (without Certificate server names populated, iOS presents a per-connection trust prompt; populating the field bypasses it) and the A-05 fact verbatim ("On iOS and macOS, disabling server validation in a managed profile is flagged as a security violation by the OS"); LINK the rogue-RADIUS / credential-harvest rationale to `01-eap-method-overview.md#peap-mschapv2` and `../_glossary-network.md#server-name-validation` (never restate the theory); end with "No example in this guide shows server validation disabled."
    4. A `### Anonymous Outer Identity (Identity Privacy)` subsection cloned from `04-macos.md` lines 47-56 with "macOS" changed to "iOS/iPadOS" in the opener; the three-EAP-method breakdown (EAP-TLS cert subject, PEAP username, EAP-TTLS username) and the realm-routing suffix note apply verbatim; field label **Identity privacy (outer identity)**.

    Author a `## Wi-Fi` section opening with a `### In Intune admin center` header whose bold navigation path is **Devices** > **Configuration** > **New policy** > **iOS/iPadOS** > **Templates** > **Wi-Fi**, noting selection of **Wi-Fi type: Enterprise** to access EAP settings, with the co-equal-EAP preamble and a link to `01-eap-method-overview.md` for when-to-choose.

    Add the MAC-randomization PROMINENT PLAIN-PROSE note (D-01/D-02/D-03/D-04) either immediately before or after the Wi-Fi per-EAP matrix (your discretion — keep it prominent). It is NOT a `> **Label:**` blockquote callout. It MUST: name the exact control "Disable MAC address randomization" with option "Yes"; state that "Yes" forces the device to present its actual Wi-Fi MAC instead of a randomized one; state this is required for NAC environments where the RADIUS policy is keyed to MAC; state the iOS 14.0+/iPadOS 14.0+ version gate; carry an inline freshness stamp (compact `*last_verified: 2026-06-30 · review_by: 2026-09-28*` style per `03-windows.md` line 181); and state that wired connections are unaffected — the USB-Ethernet adapter presents its physical MAC automatically (D-03). Do NOT write "set MAC randomization to Yes" — that is the STACK ~l.226 error (D-04 guardrail).

    Add the Wi-Fi per-EAP-method config matrix (settings as rows; three columns EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS; minimal `|---|---|---|---|` separator) per RESEARCH.md §3: EAP type field value; Certificate server names; Root certificate for server validation; Client authentication (EAP-TLS = Certificates: SCEP, PKCS, or Derived credential; PEAP = Username and Password (implicit MS-CHAPv2; no inner-method selector); EAP-TTLS = Username and Password); Inner method per D-12 (EAP-TLS = -- (cert-only; no inner method); PEAP = -- (no inner-method selector; MS-CHAPv2 implicit — see WARNING below); EAP-TTLS = PAP / CHAP / MS-CHAP / MS-CHAP v2); Identity privacy (outer identity). Do NOT add a "Deployment channel" row and do NOT add a per-EAP "Disable MAC address randomization" row (it is profile-level prose, not a per-EAP matrix row). Keep all three methods co-equal — no "recommended"/"default" language.

    Add the B-05 "What breaks" WARNING blockquote callout (D-10/D-11) within the Wi-Fi PEAP context (alongside or immediately after the matrix). Use the `> **WARNING -- <label>**` blockquote syntax from `03-windows.md`. It MUST convey: iOS/iPadOS PEAP inner authentication is always MS-CHAPv2; the Intune UI shows no inner-method selector for Wi-Fi PEAP (no PAP option presented); PAP injected via custom profile or imported config causes immediate "Authentication Failed"; macOS/Windows devices on the same SSID with PEAP+PAP may succeed while iOS devices fail; symptom = "Authentication Failed" on iOS with RADIUS logs showing EAP-NAK from the device. Do NOT place this callout in Common Mechanics (PEAP-inner is method-specific — D-11).

    Add the EAP-TLS client-certificate-options note after the matrix (clone pattern from `04-macos.md` lines 78-82): for Wi-Fi EAP-TLS, both SCEP and PKCS certificate profiles are supported, plus Derived credential as a third iOS option; link `02-cert-delivery-foundation.md` for cert-profile configuration / ordering rule.

    Use `--` (double hyphen) for em-dashes throughout. Do NOT introduce any macOS deployment-channel content, any Windows-only mechanics (dot3svc, DANGER, TEAP, KB5014754), or any Mermaid diagram.
  </action>
  <verify>
    <automated>grep -q "platform: ios" docs/admin-setup-8021x/05-ios.md && grep -q "review_by: 2026-09-28" docs/admin-setup-8021x/05-ios.md && grep -q "## Common Profile Mechanics" docs/admin-setup-8021x/05-ios.md && grep -q "## Wi-Fi" docs/admin-setup-8021x/05-ios.md && grep -q "Disable MAC address randomization: Yes" docs/admin-setup-8021x/05-ios.md && grep -q "canonical-scope-callout" docs/admin-setup-8021x/05-ios.md && grep -q "01-eap-method-overview.md#peap-mschapv2" docs/admin-setup-8021x/05-ios.md && grep -q "_glossary-network.md#server-name-validation" docs/admin-setup-8021x/05-ios.md && grep -qi "WARNING" docs/admin-setup-8021x/05-ios.md && ! grep -qi "dot3svc\|DANGER\|KB5014754\|TEAP\|deployment channel\|mermaid\|apple configurator" docs/admin-setup-8021x/05-ios.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `05-ios.md` exists with front-matter `platform: ios`, `applies_to: both`, `last_verified: 2026-06-30`, `review_by: 2026-09-28`
    - A Prerequisites blockquote linking `01-eap-method-overview.md` and `02-cert-delivery-foundation.md` and a one-line Scope banner linking `02-cert-delivery-foundation.md#canonical-scope-callout` are present
    - The H1 is `# iOS/iPadOS 802.1X Admin Setup: Wi-Fi and Wired` (uses ` and `, no ` / `)
    - `## Common Profile Mechanics` contains three-separate-profiles prose naming the trusted-root + SCEP/PKCS client-cert + Wi-Fi/Wired profiles, excluding combined `.mobileconfig` and Apple Configurator, with NO blockquote callout and NO Mermaid for this content (D-05); the cert-ordering rule is linked to `02-cert-delivery-foundation.md`, not restated (D-07)
    - A "No authentication-mode selector" note is present (mentions the User/Machine/User-or-machine selector is absent on iOS/iPadOS), is plain prose not a blockquote callout, and carries NO macOS deployment-channel content
    - A `### Server Validation` subsection frames validation as a security requirement, contains the verbatim "flagged as a security violation" fact, mentions the iOS trust dialog, links `01-eap-method-overview.md#peap-mschapv2` and `../_glossary-network.md#server-name-validation`, and contains no example showing validation disabled
    - Anonymous outer identity guidance uses the label `Identity privacy (outer identity)` and covers all three EAP methods
    - A `## Wi-Fi` section has a `### In Intune admin center` nav path containing `iOS/iPadOS`, `Templates`, and `Wi-Fi`
    - The MAC-randomization content is PLAIN PROSE (NOT a `> **Label:**` blockquote), contains the exact string `Disable MAC address randomization: Yes`, states wired uses the physical USB-Ethernet MAC, includes an iOS 14.0+ version gate, and carries an inline freshness stamp; the file does NOT contain the erroneous phrasing "set MAC randomization to Yes"
    - The Wi-Fi matrix has three EAP-method columns (`EAP-TLS`, `PEAP-MSCHAPv2`, `EAP-TTLS`), an `Inner method` row showing `PAP / CHAP / MS-CHAP / MS-CHAP v2` for EAP-TTLS, no `Deployment channel` row, no per-EAP `Disable MAC address randomization` row, and no "recommended"/"default" ranking
    - A `> **WARNING` "What breaks" blockquote callout is present in the Wi-Fi PEAP context conveying iOS PEAP inner auth MUST be MS-CHAPv2 (no PAP), symptom "Authentication Failed"; it is NOT in Common Mechanics
    - The macOS/Windows-only tokens `dot3svc`, `DANGER`, `KB5014754`, `TEAP`, `deployment channel`, `Apple Configurator`, and any Mermaid block do NOT appear in the file
    - Cross-references to `01-`/`02-`/`_glossary-network.md` are present; shared concepts are linked, not restated
  </acceptance_criteria>
  <done>The scaffold, Common Profile Mechanics (three-profiles prose + no-auth-mode note + server validation + identity privacy), and the Wi-Fi half of the guide are authored; SC1 (Wi-Fi 3-EAP + MAC-randomization) and the SC3 server-validation/PEAP-MS-CHAPv2/three-profiles portions are satisfied; D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-10, D-11, D-12 (Wi-Fi matrix) and carried 103 D-04/D-11/D-12 are in place.</done>
</task>

<task type="auto">
  <name>Task 2: Wired subsection + SCEP-only NOTE + "Any Ethernet" note + hedged wired matrix + closeout</name>
  <files>docs/admin-setup-8021x/05-ios.md</files>
  <read_first>
    - docs/admin-setup-8021x/05-ios.md — the file created in Task 1 (append the Wired subsection and closeout; keep front matter, Common Mechanics, and Wi-Fi intact)
    - docs/admin-setup-8021x/04-macos.md — Wired structural slots being cloned/adapted: `## Wired` + `### In Intune admin center` (86-93), SCEP-only NOTE callout (94-104), Network Interface selector table (106-120), wired per-EAP matrix (124-137), `## See Also` (141-145), `## Change History` (149-154). Do NOT clone the Network Interface selector table (iOS = "Any Ethernet", no selector) and do NOT clone the macOS wired PEAP/EAP-TTLS "Username and Password" inner-method cells
    - 104-RESEARCH.md — Live Verification Findings §4 (CRITICAL iOS wired field map: EAP types EAP-TLS/EAP-TTLS/PEAP only; PKCS not supported all three; "Any Ethernet" not configurable; wired PEAP + EAP-TTLS = Certificates SCEP-only with NO inner-method selector); Section-by-Section Authoring Guidance: Wired "When to use this" (D-09), Wired SCEP-only NOTE, Wired Network Interface note, Wired per-EAP matrix (with hedged cells), Wired B-05 cross-reference (D-11), See Also footer, Change History footer
    - 104-PATTERNS.md — Wired section "When to use this" paragraph (l.255-256), Wired nav-path delta (l.258-268), SCEP-only NOTE clone+adapt (l.270-288), Network-Interface "Any Ethernet" single-sentence note / do-NOT-clone-table rule (l.290-294), wired matrix clone structure (l.296-314) and iOS wired matrix cell-difference table (l.316-331), D-11 wired cross-reference (l.333), See Also footer (l.337-349), Change History footer (l.353-368), What-to-Strip-vs-Keep delta table (l.536-554)
    - 104-CONTEXT.md — D-08, D-09, D-11, D-12 exact scope boundaries + the RESEARCH REFINEMENT extending D-12 to wired PEAP (cert-only in iOS wired UI); callout-discipline (only B-05 WARNING + wired SCEP-only NOTE), no-DANGER, no-macOS-deployment-channel constraints
  </read_first>
  <action>
    Append a `## Wired` section to `05-ios.md`. Immediately after the heading, add the D-09 "When to use this" use-case paragraph (2-3 sentences, plain prose): M-series iPads equipped with a USB-Ethernet adapter; typical scenario is multi-iPad shared-use environments such as classrooms or labs where wired Ethernet is available. This grounds the narrower hardware applicability before the configuration steps. Then open a `### In Intune admin center` block with the bold navigation path **Devices** > **Configuration** > **New policy** > **iOS/iPadOS** > **Templates** > **Wired network**.

    Add the prominent standalone SCEP-only NOTE callout (carried 103 D-06) using `> **NOTE -- Wired client certificates: SCEP only (PKCS not supported)**` blockquote format (clone and adapt from `04-macos.md` lines 94-104, "macOS" -> "iOS/iPadOS"): the iOS/iPadOS wired network profile supports only SCEP certificate profiles for client authentication across all three EAP types; PKCS certificate profiles are not supported for the wired profile type; Wi-Fi supports both SCEP and PKCS; PKCS-only shops must configure SCEP infrastructure before deploying wired 802.1X on iOS/iPadOS; link `02-cert-delivery-foundation.md#per-platform-cert-delivery-support-matrix`. Severity is NOTE — no DANGER callout. Strip any macOS deployment-channel sentence.

    Add the Network Interface note (iOS-specific) as a single sentence of plain prose after the SCEP-only NOTE: Network Interface is automatically set to **Any Ethernet** — the iOS/iPadOS wired profile targets any available USB-Ethernet interface; there is no interface selector (unlike macOS). Do NOT clone the macOS Network Interface selector table (`04-macos.md` lines 106-120) — iOS has no selection to make.

    Add the per-EAP-method Wired config matrix (settings as rows; three columns EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS; minimal `|---|---|---|---|` separator) at full peer depth (D-08), using the live-verified field map from RESEARCH.md §4: EAP type field value; `Server Trust -- Certificate server names`; Root certificate for server validation; Client Authentication method — **Certificates: SCEP only (PKCS not supported)** for ALL THREE EAP types (EAP-TLS, PEAP, EAP-TTLS); Inner method per the D-12 wired hedge (EAP-TLS = -- (cert-only; no inner method); PEAP = -- (cert-only in the iOS wired Intune UI / Templates path; see Wi-Fi PEAP note for the MS-CHAPv2 context); EAP-TTLS = -- (cert-only in the iOS wired Intune UI / Templates path; username/password inner auth is a Wi-Fi-only path on iOS — verify in the Intune console if required)); Identity privacy (outer identity). Do NOT clone the macOS "Username and Password" wired PEAP/EAP-TTLS cells and do NOT assert MS-CHAPv2/PAP/CHAP as selectable wired inner methods (RESEARCH §4 live finding). Keep all three methods co-equal.

    Add the D-11 wired cross-reference as a one-line prose pointer near the wired PEAP row/section directing wired-only readers to the Wi-Fi PEAP "What breaks" WARNING for the MS-CHAPv2 context, noting the wired Intune UI exposes Certificates (SCEP) as the client auth method so the MS-CHAPv2 constraint is surfaced on the Wi-Fi profile.

    Close the file with a `## See Also` section linking `01-eap-method-overview.md`, `02-cert-delivery-foundation.md`, and `../_glossary-network.md` (clone from `04-macos.md` lines 141-145); and a `## Change History` table whose initial-version row is dated 2026-06-30 and summarizes the iOS guide content: "Initial version -- iOS/iPadOS 802.1X admin setup: Wi-Fi + wired profiles for EAP-TLS / PEAP / EAP-TTLS; MAC-address randomization note (iOS 14+); M-series iPad wired use case; wired SCEP-only callout; PEAP 'What breaks' callout". Confirm no macOS deployment-channel content and no Windows-only section (dot3svc, enforcement-staging DANGER, TEAP, KB5014754) was introduced anywhere in the file, and that the only two blockquote callouts in the file are the Wi-Fi B-05 WARNING and the wired SCEP-only NOTE. Use `--` (double hyphen) for em-dashes.
  </action>
  <verify>
    <automated>grep -q "## Wired" docs/admin-setup-8021x/05-ios.md && grep -q "Wired network" docs/admin-setup-8021x/05-ios.md && grep -qi "USB.Ethernet" docs/admin-setup-8021x/05-ios.md && grep -qi "M-series" docs/admin-setup-8021x/05-ios.md && grep -qi "SCEP only" docs/admin-setup-8021x/05-ios.md && grep -qi "Any Ethernet" docs/admin-setup-8021x/05-ios.md && grep -q "per-platform-cert-delivery-support-matrix" docs/admin-setup-8021x/05-ios.md && grep -q "## Change History" docs/admin-setup-8021x/05-ios.md && grep -q "## See Also" docs/admin-setup-8021x/05-ios.md && ! grep -qi "dot3svc\|DANGER\|KB5014754\|TEAP\|deployment channel\|First active Ethernet" docs/admin-setup-8021x/05-ios.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - A `## Wired` section exists, opening with a "When to use this" use-case paragraph naming M-series iPads + USB-Ethernet + multi-iPad shared-use (D-09), as plain prose
    - The Wired `### In Intune admin center` nav path contains `iOS/iPadOS`, `Templates`, and `Wired network`
    - A `> **NOTE` SCEP-only callout states `SCEP only` / `PKCS` not supported for wired and links `02-cert-delivery-foundation.md#per-platform-cert-delivery-support-matrix`
    - A single-sentence `Any Ethernet` Network Interface note is present; the macOS Network Interface selector table (`First active Ethernet` etc.) is ABSENT
    - The Wired matrix has three EAP-method columns, a `Server Trust -- Certificate server names` row label, and a Client Authentication row showing `Certificates: SCEP only (PKCS not supported)` for ALL THREE EAP types
    - The wired PEAP and wired EAP-TTLS Inner-method cells read cert-only-in-the-iOS-wired-UI (Templates path) and do NOT assert `MS-CHAPv2`/`PAP`/`CHAP` as selectable wired inner methods; the macOS `Username and Password` wired cells are NOT cloned (D-12 wired hedge)
    - A one-line wired cross-reference to the Wi-Fi PEAP "What breaks" WARNING is present near the wired PEAP row (D-11 guardrail)
    - `## See Also` links `01-`, `02-`, and `../_glossary-network.md`; a `## Change History` table dated 2026-06-30 with the iOS initial-version row is present
    - The macOS/Windows-only tokens `dot3svc`, `DANGER`, `KB5014754`, `TEAP`, `deployment channel`, `First active Ethernet` do not appear anywhere in the file
    - The file contains exactly two blockquote callouts: the Wi-Fi B-05 `> **WARNING` and the wired `> **NOTE` (SCEP-only) — MAC-randomization and three-profiles content remain prose
  </acceptance_criteria>
  <done>The Wired subsection ("When to use this" paragraph, SCEP-only NOTE, "Any Ethernet" note, full hedged per-EAP matrix, B-05 cross-reference) and the closeout are authored; SC2 (wired M-series iPad + SCEP-only) is satisfied; D-08, D-09, D-11 (wired cross-ref), D-12 (wired hedge) are in place. DOT1X-06 is fully delivered in `05-ios.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supplicant (managed iOS/iPadOS device) → authenticator (switch/AP) → RADIUS/NPS | 802.1X credential exchange; the guide's configuration guidance determines whether this boundary is hardened (server validation, MAC-randomization-for-NAC, MS-CHAPv2 inner auth) |
| authoring artifact | `05-ios.md` is a static Markdown documentation file with no executable surface, no input parsing, no runtime — it ships no scripts and no code |

This is a pure-documentation phase. There are NO high-severity code/runtime threats — no code, no runtime, no user input, no data handling. The only realistic threat surface is documentation-integrity: publishing an insecure configuration as guidance. The content-integrity obligations are that the guide must never instruct disabling server validation (the A-05 rogue-RADIUS risk), must steer NAC environments to force the actual MAC, and must mandate MS-CHAPv2 inner auth for PEAP (never cleartext PAP). The locked decisions encode all three.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | Spoofing | Rogue RADIUS / credential harvest if server-validation guidance were wrong (A-04/A-05) | mitigate | Common Mechanics homes server validation as a security requirement, surfaces the "disabling server validation is flagged as a security violation" fact, shows NO disabled-validation example, links rationale to 01-/02-/glossary (Task 1 acceptance) |
| T-104-02 | Information Disclosure | PEAP with PAP inner auth (cleartext password in tunnel) on iOS | mitigate | B-05 "What breaks" WARNING in the Wi-Fi PEAP context: iOS PEAP inner auth MUST be MS-CHAPv2; PAP is not supported and fails (Task 1 acceptance) |
| T-104-03 | Information Disclosure | Cleartext identity leakage before tunnel | mitigate | Identity privacy (outer identity) guidance for all three EAP methods (Task 1 acceptance) |
| T-104-04 | Elevation of Privilege | Randomized MAC bypasses MAC-keyed NAC allow-list / RADIUS policy | mitigate | MAC-randomization prose note: "Disable MAC address randomization: Yes" for NAC environments (iOS 14+); wired unaffected (Task 1 acceptance) |
| T-104-05 | Denial of Service | PKCS cert silently unusable on iOS wired → failed/blocked wired auth | mitigate | Prominent SCEP-only NOTE callout in the Wired subsection before the config matrix (Task 2 acceptance) |
| T-104-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no shipped scripts in this phase; documentation-only file creation — no supply-chain surface |

Block-on severity: high. No high-severity code/runtime threats exist for this docs phase. The content obligations T-104-01..05 are encoded as task acceptance criteria; absence of T-104-01 (the genuine server-validation content-integrity risk) is a blocking gap.
</threat_model>

<verification>
- `05-ios.md` exists and follows the cloned A3 structure: Common Profile Mechanics → Wi-Fi → Wired → See Also → Change History.
- Three-separate-profiles model is structural prose (no callout, no Mermaid); cert-ordering rule linked to `02-`, not restated.
- Both per-EAP-method matrices present three co-equal columns (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS) with an Inner-method row; no recommended-default ranking.
- MAC-randomization is prominent prose in the Wi-Fi subsection only, with "Disable MAC address randomization: Yes", iOS 14+ gate, inline freshness stamp, and a wired-unaffected statement; NOT a callout.
- The B-05 "What breaks" WARNING is in the Wi-Fi PEAP context; the wired matrix shows SCEP-only client auth for all three EAP types with hedged inner-method cells and a cross-reference to the Wi-Fi WARNING.
- Server validation is homed once in Common Mechanics with security-violation framing and links to 01-/02-/glossary; no example shows validation disabled.
- The Wired subsection carries the "When to use this" paragraph, the SCEP-only NOTE, and the single "Any Ethernet" note (no macOS interface selector table).
- macOS-only mechanics (deployment-channel WARNING / keychain prose) and Windows-only mechanics (dot3svc, enforcement-staging DANGER, TEAP, KB5014754) are entirely absent; the only two callouts are the B-05 WARNING and the wired SCEP-only NOTE.
- File-level 90-day freshness stamp present; the MAC-randomization note carries an additional inline stamp.
</verification>

<success_criteria>
- DOT1X-06 fully delivered by `docs/admin-setup-8021x/05-ios.md`.
- SC1: iOS/iPadOS Wi-Fi 802.1X profile, three co-equal EAP methods, "Disable MAC address randomization: Yes" for NAC (iOS 14+, freshness-stamped) — present.
- SC2: iOS/iPadOS wired profile targeting M-series iPads + USB Ethernet, with SCEP-only / PKCS-not-supported NOTE — present.
- SC3: PEAP inner = MS-CHAPv2 (B-05 WARNING) + three separate Intune profiles required (Common Mechanics prose) — present.
- The content-integrity obligation (T-104-01: never instruct disabling server validation) is encoded and verifiable.
</success_criteria>

<output>
Create `.planning/phases/104-ios-ipados-802-1x-admin-setup-wi-fi-wired/104-01-SUMMARY.md` when done.
</output>
</invoke>
