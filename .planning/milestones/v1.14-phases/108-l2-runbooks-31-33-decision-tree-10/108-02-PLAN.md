---
phase: 108-l2-runbooks-31-33-decision-tree-10
plan: 02
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - docs/l2-runbooks/32-8021x-cert-investigation.md
autonomous: true
requirements: [DOT1X-10]

must_haves:
  truths:
    - "An L2 engineer can validate the certificate chain, EKU, SAN, and expiry per platform in #32 (SC2)"
    - "An L2 engineer can check SCEP profile deployment status per platform in #32 (SC2)"
    - "#32 opens with a Before-starting link to #31 and LINKS cert-delivery theory to 02-cert-delivery-foundation.md (link-not-copy, D-02/D-04)"
  artifacts:
    - path: "docs/l2-runbooks/32-8021x-cert-investigation.md"
      provides: "Cross-platform 802.1X L2 certificate-chain investigation runbook (#32)"
      contains: "platform: windows+macos+ios+android+linux"
  key_links:
    - from: "docs/l2-runbooks/32-8021x-cert-investigation.md"
      to: "docs/l2-runbooks/31-8021x-log-collection.md"
      via: "Before-starting prerequisite link"
      pattern: "31-8021x-log-collection\\.md"
    - from: "docs/l2-runbooks/32-8021x-cert-investigation.md"
      to: "docs/admin-setup-8021x/02-cert-delivery-foundation.md"
      via: "link-not-copy foundation reference (EKU/SAN/ordering)"
      pattern: "02-cert-delivery-foundation\\.md"
---

<objective>
Author `docs/l2-runbooks/32-8021x-cert-investigation.md` — the cross-platform L2 certificate-chain investigation runbook (SC2, DOT1X-10).

Structure = D-01 hybrid: shared investigation-flow prose (validate SCEP profile status → device cert store → chain/EKU/SAN/expiry) + per-platform deep-dive subsections. Per link-not-copy, the cert-delivery ordering rule, EKU requirement (Client Authentication OID `1.3.6.1.5.5.7.3.2`), SAN, and server-name-validation theory are LINKED to `02-cert-delivery-foundation.md`, never restated. Opens with a Before-starting link to #31.

Purpose: Enable L2 to determine whether an 802.1X failure is a certificate problem (chain, EKU, SAN, expiry, or SCEP-delivery failure) and where per platform.
Output: One new Markdown runbook file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-CONTEXT.md
@.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-RESEARCH.md
@.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author #32 cross-platform 802.1X L2 certificate-chain investigation runbook</name>
  <files>docs/l2-runbooks/32-8021x-cert-investigation.md</files>
  <read_first>
    - Target (to create): docs/l2-runbooks/32-8021x-cert-investigation.md
    - Primary analog: docs/l2-runbooks/27-macos-sso-investigation.md lines 13-25 (Context + "Before starting"), lines 189-207 (Related Resources + Version History)
    - Sibling analog (opener): docs/l2-runbooks/31-8021x-log-collection.md (created in plan 01 — the Before-starting prerequisite target)
    - Shared-methodology prose analog: docs/l2-runbooks/24-linux-log-collection.md lines 39-44 (Method Selection rhythm)
    - Frontmatter clone source: docs/l1-runbooks/38-8021x-certificate-failure.md lines 1-7 (change audience to L2)
    - Cert inspection commands (verified): 108-RESEARCH.md §"Per-Platform Certificate Inspection Commands (#32 Core Deliverable)"
    - Section-by-section skeleton: 108-PATTERNS.md §"docs/l2-runbooks/32-8021x-cert-investigation.md" (platform gate + foundation callout, Context, Investigation Overview, per-platform subsections, EKU/SAN/Expiry checklist)
    - Link-not-copy target (do NOT restate): docs/admin-setup-8021x/02-cert-delivery-foundation.md (ordering rule, EKU, SAN, server-name validation); Phase 106 macOS keychain caveat: 108-RESEARCH.md §"Common Pitfalls" 6
  </read_first>
  <action>
    Create `docs/l2-runbooks/32-8021x-cert-investigation.md`. Per DOT1X-10.

    Frontmatter: identical compound token to #31 — `platform: windows+macos+ios+android+linux`, `audience: L2`, `applies_to: both`, `last_verified: 2026-07-01`, `review_by: 2026-09-29`.

    Platform gate + foundation-reference callout (PATTERNS §"Platform gate + foundation link callout"): gate covering all five platforms; cross-link #33 and the decision tree; a link-not-copy NOTE pointing cert-delivery ordering rule + EKU (`1.3.6.1.5.5.7.3.2`) + server-name validation to `../admin-setup-8021x/02-cert-delivery-foundation.md` with an explicit "do not restate that theory here" sentence.

    Context section (clone 27 lines 13-18): describe the two entry conditions (Intune cert profile shows Error/Pending, OR profiles show Succeeded but device cannot authenticate — possible expiry/EKU/SAN mismatch). Add "Before starting: collect 802.1X logs per `[#31](31-8021x-log-collection.md)`." Add "From L1 escalation?" prose naming L1 #38 as the escalation source.

    Investigation Overview (shared methodology, D-01 pre-subsection layer): numbered platform-agnostic sequence — (1) Intune portal: confirm Trusted Certificate + SCEP/PKCS profiles both show Succeeded (link ordering rule to foundation, do not restate); (2) device cert store: chain, EKU includes Client Authentication `1.3.6.1.5.5.7.3.2`, SAN matches expected UPN/device name, not expired (note Intune "Succeeded" ≠ cert present on device); (3) CAPI2 log (Windows only) for chain-validation failures during active attempts.

    Five per-platform subsections, H2 with COLON separators (e.g. `## Windows: Certificate Inspection`) for clean slugs:
    - Windows: `certutil -v -silent -store MY` (machine), `certutil -v -silent -user -store MY` (user), `certutil -v -silent -store ROOT`, `certutil -verify -urlfetch <cert.cer>`; interpret the -v output fields (thumbprint, Not Before/After, EKU OID `1.3.6.1.5.5.7.3.2`, SAN); reference the CAPI2 channel (enabled in #31) for chain failures.
    - macOS: NOTE — keychain location depends on deployment channel (device-based SCEP → System keychain; user-based → Login keychain), one sentence + link to `../admin-setup-8021x/04-macos.md` (do NOT restate the immutable-channel caveat — Pitfall 6). Commands: `security find-certificate -a -Z`, `security find-certificate -a -c "<CN fragment>" -p`, `security verify-cert -c <client.pem>`, `openssl x509 -in <client.pem> -text -noout`; Keychain Access GUI alternative.
    - iOS/iPadOS: NOTE — no device-side cert-inspection command. Intune portal SCEP status path (Devices > iOS/iPadOS > [device] > Device configuration); Troubleshooting + Support delivery timestamp; on-device Settings > General > VPN & Device Management payload view.
    - Android: Intune portal path (Devices > Android Enterprise > [device] > Device configuration) primary; limited on-device Settings > Security > Encryption & credentials > User certificates; adb OMADM log `adb logcat -s "omadm" "SyncML"` (escalation-collected, reuse the USB-debugging WARNING pattern from #31).
    - Linux: NOTE — certs are script-deployed (not via Intune SCEP profile), link `../admin-setup-8021x/06-linux.md`; SCEP profile status check is N/A. Commands: `openssl x509 -in <client.pem> -text -noout`, `openssl verify -CAfile <ca.pem> <client.pem>`, `nmcli connection show <name>` (inspect `802-1x.client-cert`, `802-1x.ca-cert`, `802-1x.identity`, `802-1x.eap`).

    SCEP Profile Deployment Status subsection and shared EKU / SAN / Expiry checklist table (PATTERNS §"EKU / SAN / Expiry checklist"): the checklist rows (EKU Client Auth OID, SAN match, expiry, issuer chain) with a link to the foundation for authoritative requirements — link, do not restate.

    Related Resources + Version History (row: "Phase 108 plan 02: initial authoring — 802.1X cross-platform L2 certificate-chain investigation (#32)").

    Callout vocabulary: NOTE / WARNING / DANGER / CRITICAL only — never `IMPORTANT`. No `{#id}` overrides. Co-equal EAP preserved (no method ranking).
  </action>
  <verify>
    <automated>test -f docs/l2-runbooks/32-8021x-cert-investigation.md && grep -q "platform: windows+macos+ios+android+linux" docs/l2-runbooks/32-8021x-cert-investigation.md && grep -q "audience: L2" docs/l2-runbooks/32-8021x-cert-investigation.md && grep -q "31-8021x-log-collection.md" docs/l2-runbooks/32-8021x-cert-investigation.md && grep -q "02-cert-delivery-foundation.md" docs/l2-runbooks/32-8021x-cert-investigation.md && grep -q "1.3.6.1.5.5.7.3.2" docs/l2-runbooks/32-8021x-cert-investigation.md && grep -q "certutil" docs/l2-runbooks/32-8021x-cert-investigation.md && grep -q "security find-certificate" docs/l2-runbooks/32-8021x-cert-investigation.md && grep -q "openssl x509" docs/l2-runbooks/32-8021x-cert-investigation.md && ! grep -q "> \*\*Important:\*\*" docs/l2-runbooks/32-8021x-cert-investigation.md && ! grep -q "{#" docs/l2-runbooks/32-8021x-cert-investigation.md</automated>
  </verify>
  <done>
    File exists with compound frontmatter + `audience: L2` + 90-day stamps; five per-platform cert-inspection subsections present; EKU OID `1.3.6.1.5.5.7.3.2`, `certutil`, `security find-certificate`, `openssl x509` present; Before-starting link to #31 and link-not-copy reference to `02-cert-delivery-foundation.md` present; SCEP status + EKU/SAN/expiry checklist present; only NOTE/WARNING/DANGER/CRITICAL callouts; no `{#id}` overrides.
  </done>
  <acceptance_criteria>
    - `docs/l2-runbooks/32-8021x-cert-investigation.md` exists.
    - Frontmatter: `platform: windows+macos+ios+android+linux` + `audience: L2` + `applies_to: both` + `last_verified`/`review_by` (90-day pair).
    - Five per-platform subsections present (Windows, macOS, iOS/iPadOS, Android, Linux).
    - Exact strings present: `1.3.6.1.5.5.7.3.2` (Client Auth EKU OID), `certutil` (with `-store MY`/`ROOT`), `security find-certificate`, `openssl x509`, `nmcli connection show`.
    - Before-starting prerequisite links `31-8021x-log-collection.md`.
    - Foundation theory is LINKED (grep finds `02-cert-delivery-foundation.md`) and NOT restated — no full ordering-rule/EKU prose block copied from the foundation.
    - macOS keychain-location note is one sentence + link (Pitfall 6), not a restatement of the immutable-channel caveat.
    - No `> **Important:**` callout; no `{#` anchor overrides.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan authors a single Markdown documentation file. No executable code, no user input handling, no runtime attack surface is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-02 | N/A | docs/l2-runbooks/32-8021x-cert-investigation.md | accept | Documentation-only; no code paths, no new attack surface. Documented commands (certutil, security, openssl, nmcli) are read-only cert-inspection commands. No high-severity threats expected. |
</threat_model>

<verification>
- File created and passes the automated grep gate in Task 1.
- Manual spot-read: confirm D-01 hybrid shape and that no foundation theory is restated (link-not-copy).
</verification>

<success_criteria>
SC2 satisfied: #32 enables per-platform certificate-chain validation, SCEP profile status checks, and EKU/SAN/expiry diagnosis. Link-not-copy upheld.
</success_criteria>

<output>
Create `.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-02-SUMMARY.md` when done.
</output>
