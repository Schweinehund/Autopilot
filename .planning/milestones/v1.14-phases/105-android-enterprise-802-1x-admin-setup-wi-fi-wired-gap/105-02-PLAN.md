---
phase: 105-android-enterprise-802-1x-admin-setup-wi-fi-wired-gap
plan: 02
type: execute
wave: 2
depends_on: ["105-01"]
files_modified:
  - docs/admin-setup-8021x/00-overview.md
autonomous: true
requirements: [DOT1X-07]

must_haves:
  truths:
    - "GOAL discoverability: the local 00-overview.md platform-guide list gains item 6 as a descriptive one-liner linking 06-android.md, so a reader navigating the 802.1X folder entry point can discover the newly authored Android Enterprise guide"
    - "SC1 + SC2 + SC3 surfaced in the entry: the item-6 description names the Android-distinguishing content (Wi-Fi profiles for all three EAP methods across COBO/COPE/COSU/BYOD work-profile modes; UPN-in-SAN deployment requirement for personally-owned work profile; version-gated RADIUS server-name behavior Android 11+/14+; no native wired profile gap), matching the existing item-3/4/5 descriptive-one-liner style"
    - "D-01 reflected in nav: the item-6 entry signals the no-native-wired gap so a reader knows the Android guide is Wi-Fi-primary with a documented wired gap (consistent with the existing wired-availability note at line 36)"
    - "Navigation-last (local-only): only the in-folder 00-overview.md platform-list entry + Change History row are edited; no capability-matrix rows and no global nav-hub files (index.md, quick-refs, decision-trees) are touched (those are Phase 109)"
    - "The placeholder line '6–7. Platform guides (Phase 105–106) -- entries added as each guide is authored.' is replaced by item 6 (Android Enterprise) plus a '7. Platform guide (Phase 106) -- entry added when guide is authored.' continuation, matching the existing item-1/2/3/4/5 numbered-list link style (bold linked title, double-dash, no trailing period)"
    - "The Mermaid '3–7. Platform Guides' diagram node is left unchanged after inspection — it references a range that stays valid until all guides are added; the Scope section, wired-availability note, and See Also are unchanged"
    - "Harness allowlisting: 00-overview.md is a pre-existing file; the edit must be recorded for harness allowlisting, the same pattern used for the Phase 104 item-5 edit, the Phase 103 item-4 edit, and the Phase 102 item-3 edit"
  artifacts:
    - path: "docs/admin-setup-8021x/00-overview.md"
      provides: "Local 802.1X folder entry point with the Android Enterprise platform-guide entry (item 6)"
      contains: "](06-android.md)"
  key_links:
    - from: "docs/admin-setup-8021x/00-overview.md"
      to: "docs/admin-setup-8021x/06-android.md"
      via: "platform-guide list item 6"
      pattern: "\\]\\(06-android\\.md\\)"
---

<objective>
Add the Android Enterprise platform-guide entry to the local `docs/admin-setup-8021x/00-overview.md` numbered list (item 6), replacing the reserved "6–7. Platform guides (Phase 105–106)" placeholder line with the authored Android Enterprise entry plus a "7. Platform guide (Phase 106)" continuation for the still-pending Linux guide, and appending a Change History row.

Purpose: Make the just-authored `06-android.md` discoverable from the 802.1X folder entry point. This is the ONLY in-scope navigation edit for Phase 105 — capability matrices and global nav hubs are deferred to Phase 109 (navigation-last).

Output: `docs/admin-setup-8021x/00-overview.md` (EDIT — one numbered-list line replaced with the item-6 entry + a "7." continuation line, plus a Change History row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/admin-setup-8021x/00-overview.md
@.planning/phases/105-android-enterprise-802-1x-admin-setup-wi-fi-wired-gap/105-PATTERNS.md
@.planning/phases/105-android-enterprise-802-1x-admin-setup-wi-fi-wired-gap/105-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Android Enterprise platform-guide entry (item 6) to 00-overview.md</name>
  <files>docs/admin-setup-8021x/00-overview.md</files>
  <read_first>
    - docs/admin-setup-8021x/00-overview.md — the live file (the source of truth): locate the numbered-list line `6–7. Platform guides (Phase 105–106) -- entries added as each guide is authored.` (the single edit target, ~line 34); confirm the item-1/2/3/4/5 link style above it (item 5 is the Phase-104 precedent, ~line 32); inspect the Mermaid node `C[3–7. Platform<br/>Guides]` (~line 21) to confirm it stays valid as a range; note the existing wired-availability note (~line 36) already previews the Android no-wired gap; locate the Change History table (~lines 53-60) whose last row is the Phase-104 item-5 row
    - 105-PATTERNS.md — `docs/admin-setup-8021x/00-overview.md (EDIT)` section: Edit 1 platform-list replacement (the exact item-6 entry text + "7." continuation, lines 454-476) and Edit 2 Change-History-row pattern + new row to append (lines 480-495); the double-hyphen-dash convention and the leave-Mermaid-unchanged note
    - 105-RESEARCH.md — Pattern 9 (`00-overview.md Item-6 Edit`, lines 347-364): the exact current line-34 target text, the item-6 replacement block, and the "7." continuation form
    - docs/admin-setup-8021x/06-android.md — confirm the link target filename exists (created in Plan 105-01) before linking to it
  </read_first>
  <action>
    Edit `docs/admin-setup-8021x/00-overview.md`. Replace the single numbered-list line `6–7. Platform guides (Phase 105–106) -- entries added as each guide is authored.` with two lines: (1) item 6, a descriptive one-liner matching the item-1/2/3/4/5 style — bold linked title `**[Android Enterprise 802.1X Admin Setup (Wi-Fi)](06-android.md)**`, then `--`, then a single description naming the key content (Wi-Fi profiles for all three EAP methods across COBO/COPE/COSU/BYOD work profile modes; UPN-in-SAN deployment requirement for personally-owned work profile; version-gated RADIUS server-name behavior (Android 11+/14+); no native wired profile (gap documented)), with no trailing period; and (2) a continuation line `7. Platform guide (Phase 106) -- entry added when guide is authored.` Keep the title parenthetical free of ` / ` and the double-dash em-dash style throughout. Preserve the existing en-dash characters used elsewhere in the file's numbered list and headings; the item-6 title uses `(Wi-Fi)` only (no "and Wired") to match the guide's own H1.

    Inspect the Mermaid `C[3–7. Platform<br/>Guides]` node: leave it unchanged — it references a range that remains valid until all platform guides are authored. Do NOT modify the Mermaid diagram, the Scope section, the wired-availability note (line 36), the See Also section, or any other file; this edit is strictly the numbered-list line plus the Change History row below.

    Append a Change History row dated 2026-06-30 following the existing pattern: `| 2026-06-30 | Added item 6 -- Android Enterprise platform-guide entry linking 06-android.md; narrowed placeholder range from 6--7 to 7 | -- |`.

    Record in the SUMMARY that `00-overview.md` is a pre-existing file and this edit must be added to the harness allowlist (same handling as the Phase 104 item-5 edit, the Phase 103 item-4 edit, and the Phase 102 item-3 edit).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q "](06-android.md)" docs/admin-setup-8021x/00-overview.md && grep -q "7. Platform guide (Phase 106)" docs/admin-setup-8021x/00-overview.md && ! grep -q "6–7. Platform guides (Phase 105–106)" docs/admin-setup-8021x/00-overview.md && grep -q "](05-ios.md)" docs/admin-setup-8021x/00-overview.md && grep -q "](04-macos.md)" docs/admin-setup-8021x/00-overview.md && grep -q "](03-windows.md)" docs/admin-setup-8021x/00-overview.md && grep -q "Added item 6 -- Android Enterprise" docs/admin-setup-8021x/00-overview.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `00-overview.md` numbered list contains item `6.` with a bold link `](06-android.md)` and a single description (no trailing period), matching the item-1/2/3/4/5 format and naming the COBO/COPE/COSU/BYOD modes, the UPN-in-SAN personally-owned-work-profile requirement, the Android 11+/14+ version-gated RADIUS behavior, and the no-native-wired gap
    - A `7. Platform guide (Phase 106)` continuation line is present
    - The old `6–7. Platform guides (Phase 105–106)` numbered-list line is gone
    - The pre-existing item 3 (`](03-windows.md)`), item 4 (`](04-macos.md)`), and item 5 (`](05-ios.md)`) entries remain intact
    - The Mermaid `3–7. Platform Guides` node is unchanged, and the Scope section, the wired-availability note (line 36), and See Also are unchanged
    - No other file is modified (no capability-matrix rows, no `docs/index.md` / quick-ref / decision-tree edits — navigation-last, Phase 109)
    - A Change History row dated 2026-06-30 documenting the item-6 addition and the 6–7 → 7 narrowing is appended (uses `--` double-hyphen dashes)
    - The SUMMARY records that `00-overview.md` is a pre-existing file requiring harness-allowlist registration (Phase 104 item-5 / Phase 103 item-4 / Phase 102 item-3 pattern)
  </acceptance_criteria>
  <done>The Android Enterprise guide is discoverable from the local 802.1X folder entry point via item 6; the placeholder range is correctly narrowed to 7; the Mermaid node is confirmed unchanged; the pre-existing-file edit is flagged for harness allowlisting. DOT1X-07 navigation is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoring artifact | `00-overview.md` is a static Markdown navigation file; the edit adds one link line, one continuation line, and one Change History row. No executable surface, no input parsing, no runtime. |

This is a documentation navigation edit with no executable attack surface. No high-severity code/runtime threats exist for this docs phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-02-01 | Tampering | Pre-existing file edited outside harness allowlist (integrity-tracking drift) | mitigate | Edit is scoped to the single numbered-list line + one Change History row; SUMMARY flags the file for harness-allowlist registration (Phase 104 item-5 / Phase 103 item-4 / Phase 102 item-3 pattern) |
| T-105-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no scripts; documentation-only one-line edit — no supply-chain surface |

Block-on severity: high. No high-severity content-security obligations apply to a navigation link edit; the only tracked concern is allowlist-integrity hygiene (T-105-02-01).
</threat_model>

<verification>
- `00-overview.md` links to `06-android.md` as numbered item 6 in the platform-guide list.
- The placeholder range is updated from `6–7 (Phase 105–106)` to item 6 (Android) + a standalone `7. Platform guide (Phase 106)`.
- The existing item 3 (`03-windows.md`), item 4 (`04-macos.md`), item 5 (`05-ios.md`), and the Mermaid range node are unchanged.
- No global nav-hub or capability-matrix files are touched (navigation-last invariant honored for Phase 105).
- The pre-existing-file edit is recorded for harness allowlisting.
</verification>

<success_criteria>
- The Android Enterprise 802.1X guide is reachable from the local 802.1X overview entry point.
- Edit is surgical (one list line replaced with item 6 + a "7." continuation, plus one Change History row); nothing else in `00-overview.md` changes.
- Harness-allowlist registration is flagged in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/105-android-enterprise-802-1x-admin-setup-wi-fi-wired-gap/105-02-SUMMARY.md` when done.
</output>
