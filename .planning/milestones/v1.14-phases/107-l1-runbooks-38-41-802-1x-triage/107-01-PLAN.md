---
phase: 107-l1-runbooks-38-41-802-1x-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l1-runbooks/38-8021x-certificate-failure.md
  - docs/l1-runbooks/39-8021x-radius-reject.md
autonomous: true
requirements: [DOT1X-09]

must_haves:
  truths:
    - "L1 runbook #38 lets a technician verify cert-profile status in Intune, check the deployment-ordering constraint (LINK to 02-cert-delivery-foundation.md, not restated), and identify the per-platform diagnostic signal (SC1)"
    - "L1 runbook #39 provides a symptom description, First Checks (All Platforms), a per-platform diagnostic table, and a clear escalation trigger (SC2)"
    - "Both runbooks use structure 1C per D-01: shared symptom + First Checks + compact per-platform diagnostic-signal table + per-platform escalation-divergence notes (SC2:213 four-part ordering)"
    - "Both runbooks carry compound frontmatter per D-02: platform: windows+macos+ios+android+linux + audience: L1 + applies_to + 90-day last_verified/review_by pair"
    - "Per-platform depth is calibrated per D-05: Windows/macOS/Linux name the signal plus one read-only command; iOS = Intune-portal inspection only; Android = adb logcat named as escalation-collected, never an L1 action; all under collect-don't-interpret"
    - "Windows signal uses the EXACT verified strings Microsoft-Windows-WLAN-AutoConfig/Operational (Wi-Fi) and Microsoft-Windows-Wired-AutoConfig/Operational (wired); NO occurrence of Dot3Svc/Operational as an event channel (D-05 / RESEARCH correction)"
    - "L2 references are prose-only forward-refs with no live links per D-06; the routing map per D-07 is baked in as prose: #38 -> L2 #31 -> #32; #39 -> L2 #31 -> #33"
    - "Neither runbook edits docs/l1-runbooks/00-index.md; the conscious defer to Phase 109 (D-08, override of the 4x legacy in-phase-index habit) is recorded as a plan truth"
  artifacts:
    - path: "docs/l1-runbooks/38-8021x-certificate-failure.md"
      provides: "L1 #38 802.1X certificate-failure triage runbook (cross-platform, symptom-primary)"
      contains: "platform: windows+macos+ios+android+linux"
      min_lines: 60
    - path: "docs/l1-runbooks/39-8021x-radius-reject.md"
      provides: "L1 #39 802.1X RADIUS-reject triage runbook (cross-platform, symptom-primary)"
      contains: "platform: windows+macos+ios+android+linux"
      min_lines: 60
  key_links:
    - from: "docs/l1-runbooks/38-8021x-certificate-failure.md"
      to: "docs/admin-setup-8021x/02-cert-delivery-foundation.md"
      via: "markdown link to deployment-ordering constraint (link-not-copy)"
      pattern: "02-cert-delivery-foundation\\.md"
    - from: "docs/l1-runbooks/38-8021x-certificate-failure.md"
      to: "docs/decision-trees/10-8021x-triage.md"
      via: "Back to 802.1X Triage Decision Tree footer link"
      pattern: "10-8021x-triage\\.md"
    - from: "docs/l1-runbooks/39-8021x-radius-reject.md"
      to: "L2 #33 (prose only)"
      via: "escalation prose naming L2 #33, no markdown link"
      pattern: "#33"
---

<objective>
Author the first two cross-platform, symptom-primary 802.1X L1 triage runbooks:
`38-8021x-certificate-failure.md` (#38, SC1) and `39-8021x-radius-reject.md` (#39, SC2).

Purpose: Give L1 technicians read-only triage + escalation runbooks for the two
most common 802.1X failure classes, spanning all five platforms via a per-platform
diagnostic-signal table. This is the first fully cross-platform, symptom-primary L1
runbook pair in the corpus.

Output: Two new Markdown runbook files under `docs/l1-runbooks/`, structured per the
locked decisions D-01 (structure 1C), D-02 (compound frontmatter), D-05 (calibrated
per-platform depth), D-06 (prose-only L2 forward-refs), D-07 (routing map), D-08
(conscious index defer). Documentation only — no code, no runtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-CONTEXT.md
@.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-RESEARCH.md
@.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author L1 runbook #38 — 802.1X Certificate Failure (SC1)</name>
  <files>docs/l1-runbooks/38-8021x-certificate-failure.md</files>
  <read_first>
    - docs/l1-runbooks/38-8021x-certificate-failure.md (the file being created — confirm it does not yet exist)
    - docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md lines 1-17 (D-02 compound frontmatter + platform gate + L1 scope-note analog)
    - docs/l1-runbooks/35-macos-sso-sign-in-failure.md (D-01 section skeleton + collect-don't-interpret pattern + Escalation Criteria / "Before escalating, collect:" / Version History)
    - docs/admin-setup-8021x/02-cert-delivery-foundation.md (link target: deployment-ordering constraint, EKU, server-name validation — LINK, never restate)
    - docs/admin-setup-8021x/03-windows.md lines 103-118 (dot3svc detect/remediate — link, do not reproduce the WARNING block)
    - docs/_glossary-network.md (term anchors: 802.1X / EAP / RADIUS / supplicant — link targets)
    - .planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-RESEARCH.md §"Per-Platform Diagnostic Signal Table" + §"#38 Certificate Failure" (exact signal strings)
    - .planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-PATTERNS.md (skeleton extraction + strips/anti-patterns)
  </read_first>
  <action>
    Create `docs/l1-runbooks/38-8021x-certificate-failure.md` implementing SC1 under structure 1C (D-01).
    Frontmatter (D-02): `last_verified: 2026-06-30`, `review_by: 2026-09-28`, `applies_to: both`,
    `audience: L1`, `platform: windows+macos+ios+android+linux`. Immediately after frontmatter add the
    `> **Platform gate:**` blockquote (cross-platform 802.1X cert-failure; return to the 802.1X Triage
    Decision Tree) and the `> **L1 scope note:**` blockquote (read-only checks; state-changing remediation
    is L2 only), cloning the #34 label formats.
    Sections in SC2:213 order: `## Symptom` (Intune shows the 802.1X profile Succeeded but the device
    cannot connect; the Trusted Certificate or SCEP/PKCS cert profile shows Error / Pending / Not applicable,
    or shows Succeeded but the cert was not actually enrolled) -> `## Prerequisites` (Intune admin center
    access first; device serial; user UPN; device platform) -> `## First Checks (All Platforms)` (verify
    cert-profile status in Intune in order: Trusted Certificate must be Succeeded, then SCEP/PKCS client
    cert must be Succeeded, then the Wi-Fi/Wired network profile; and LINK the deployment-ordering
    constraint to `../admin-setup-8021x/02-cert-delivery-foundation.md` — do NOT restate the ordering rule)
    -> `## Per-Platform Diagnostic Signal` table -> `## Per-Platform Escalation Notes` -> `## Escalation Criteria`
    with `**Before escalating, collect:**` -> `## Version History`. End with the footer link
    `[Back to 802.1X Triage Decision Tree](../decision-trees/10-8021x-triage.md)` above Version History.
    Per-platform table cells use the EXACT verified strings (D-05 / RESEARCH): Windows (Wi-Fi) =
    `Microsoft-Windows-WLAN-AutoConfig/Operational`; Windows (Wired) = `Microsoft-Windows-Wired-AutoConfig/Operational`
    (NEVER `Dot3Svc/Operational` as a channel); macOS = one read-only command
    `log show --predicate 'subsystem contains "com.apple.eapol"' --info --last 30m` with the collect-don't-interpret
    note and the `--last 2h` empty-output fallback; iOS/iPadOS = Intune-portal inspection ONLY, no device command;
    Android = name `adb logcat -s "wpa_supplicant"` as ESCALATION-collected (do NOT run at L1 — requires USB
    debugging + tethered PC; L1 checks the Intune portal); Linux = `journalctl -u NetworkManager`
    (supplement `journalctl -u wpa_supplicant`). All cells stay collect-don't-interpret.
    Escalation prose is PROSE-ONLY forward-refs (D-06) with the D-07 routing baked in: escalate via
    L2 Log Collection (#31) then L2 Certificate Chain Investigation (#32); name #31 and #32 in prose with
    NO markdown links to `../l2-runbooks/`. Add a Version History row dated `2026-06-30` noting
    "Phase 107 plan 01: initial authoring — 802.1X certificate failure (D-01/D-02/D-05)". Author `--`.
    Callouts use only NOTE/WARNING/DANGER/CRITICAL (no IMPORTANT). Plain GitHub auto-slug anchors, no `{#id}`,
    avoid the double-hyphen trap in headings. Do NOT edit `docs/l1-runbooks/00-index.md` (D-08 — Phase 109).
    Use placeholder identifiers only; never embed real credentials, keys, or device serials.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && test -f docs/l1-runbooks/38-8021x-certificate-failure.md && grep -q "platform: windows+macos+ios+android+linux" docs/l1-runbooks/38-8021x-certificate-failure.md && grep -q "Microsoft-Windows-WLAN-AutoConfig/Operational" docs/l1-runbooks/38-8021x-certificate-failure.md && grep -q "Microsoft-Windows-Wired-AutoConfig/Operational" docs/l1-runbooks/38-8021x-certificate-failure.md && ! grep -q "Dot3Svc/Operational" docs/l1-runbooks/38-8021x-certificate-failure.md && grep -q "02-cert-delivery-foundation.md" docs/l1-runbooks/38-8021x-certificate-failure.md && ! grep -q "l2-runbooks/3" docs/l1-runbooks/38-8021x-certificate-failure.md && ! grep -qi "IMPORTANT:" docs/l1-runbooks/38-8021x-certificate-failure.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/l1-runbooks/38-8021x-certificate-failure.md` exists.
    - Frontmatter contains the exact line `platform: windows+macos+ios+android+linux`, plus `audience: L1`, `last_verified: 2026-06-30`, `review_by: 2026-09-28`, `applies_to: both`.
    - Contains the exact string `Microsoft-Windows-WLAN-AutoConfig/Operational` AND `Microsoft-Windows-Wired-AutoConfig/Operational`.
    - NO occurrence of `Dot3Svc/Operational` anywhere in the file.
    - Contains the exact macOS command `log show --predicate 'subsystem contains "com.apple.eapol"' --info --last 30m`.
    - Contains a markdown link to `02-cert-delivery-foundation.md` (deployment-ordering constraint linked, not restated).
    - iOS cell states Intune-portal inspection only / no device command; Android cell names `adb logcat -s "wpa_supplicant"` as escalation-collected with an explicit "do NOT run at L1" qualifier.
    - Escalation prose names L2 #31 and L2 #32 with NO markdown link to `../l2-runbooks/` (no substring `l2-runbooks/3`).
    - Callouts use only NOTE/WARNING/DANGER/CRITICAL — no `IMPORTANT:` callout label.
    - Footer link `[Back to 802.1X Triage Decision Tree](../decision-trees/10-8021x-triage.md)` present; `## Version History` present with a 2026-06-30 row.
    - No real credentials, keys, or serials — placeholder identifiers only.
  </acceptance_criteria>
  <done>Runbook #38 exists, satisfies SC1 (verify cert-profile status + deployment-ordering link + per-platform signal), honors D-01/D-02/D-05/D-06/D-07/D-08, and passes the automated string checks.</done>
</task>

<task type="auto">
  <name>Task 2: Author L1 runbook #39 — 802.1X RADIUS Reject (SC2)</name>
  <files>docs/l1-runbooks/39-8021x-radius-reject.md</files>
  <read_first>
    - docs/l1-runbooks/39-8021x-radius-reject.md (the file being created — confirm it does not yet exist)
    - docs/l1-runbooks/38-8021x-certificate-failure.md (sibling authored in Task 1 — reuse the identical 1C skeleton, frontmatter, and per-platform table shape)
    - docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md lines 1-17 (frontmatter + platform gate + scope note)
    - docs/l1-runbooks/35-macos-sso-sign-in-failure.md (section skeleton + Escalation Criteria / "Before escalating, collect:")
    - docs/_glossary-network.md (term anchors for RADIUS / EAP / supplicant — link targets)
    - .planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-RESEARCH.md §"#39 RADIUS Reject" + §"Per-Platform Diagnostic Signal Table" (exact signal strings + discriminator)
    - .planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-PATTERNS.md (shared skeleton + strips/anti-patterns)
  </read_first>
  <action>
    Create `docs/l1-runbooks/39-8021x-radius-reject.md` under structure 1C (D-01), cloning the exact
    skeleton, frontmatter, platform-gate, and L1 scope-note produced in Task 1. Frontmatter (D-02):
    identical compound token `platform: windows+macos+ios+android+linux` + `audience: L1` + `applies_to: both`
    + `last_verified: 2026-06-30` / `review_by: 2026-09-28`.
    `## Symptom`: all cert profiles show Succeeded; the device attempts to connect and authentication fails
    with NO trust prompt and NO cert error; the RADIUS server is reachable (not a connectivity issue); the
    connection is cleanly rejected (Access-Reject). `## First Checks (All Platforms)`: confirm ALL cert
    profiles show Succeeded in Intune (rules out #38); confirm the RADIUS server-name field is populated in
    the Wi-Fi/Wired profile (rules out a server-trust misconfiguration -> #40); note the EAP method configured
    in the profile. Reuse the SAME `## Per-Platform Diagnostic Signal` table with the EXACT strings per D-05:
    Windows `Microsoft-Windows-WLAN-AutoConfig/Operational` / `Microsoft-Windows-Wired-AutoConfig/Operational`
    (never `Dot3Svc/Operational`), noting the Windows log shows event 8003 (failure) WITHOUT a certificate-chain
    error event; macOS `log show --predicate 'subsystem contains "com.apple.eapol"' --info --last 30m` (EAPOL
    rejection after the TLS tunnel is established); iOS Intune-portal only; Android `adb logcat -s "wpa_supplicant"`
    named escalation-collected (never run at L1); Linux `journalctl -u NetworkManager`. All collect-don't-interpret.
    Escalation prose PROSE-ONLY (D-06) with D-07 routing: L2 Log Collection (#31) then L2 RADIUS/EAP
    Investigation (#33); name #31 and #33 in prose with NO `../l2-runbooks/` markdown links. Footer link
    `[Back to 802.1X Triage Decision Tree](../decision-trees/10-8021x-triage.md)`; Version History row dated
    `2026-06-30` "Phase 107 plan 01: initial authoring — 802.1X RADIUS reject (D-01/D-02/D-05)"; author `--`.
    Callouts NOTE/WARNING/DANGER/CRITICAL only. Do NOT edit `00-index.md` (D-08). Placeholder identifiers only.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && test -f docs/l1-runbooks/39-8021x-radius-reject.md && grep -q "platform: windows+macos+ios+android+linux" docs/l1-runbooks/39-8021x-radius-reject.md && grep -q "Microsoft-Windows-WLAN-AutoConfig/Operational" docs/l1-runbooks/39-8021x-radius-reject.md && ! grep -q "Dot3Svc/Operational" docs/l1-runbooks/39-8021x-radius-reject.md && grep -q "#33" docs/l1-runbooks/39-8021x-radius-reject.md && ! grep -q "l2-runbooks/3" docs/l1-runbooks/39-8021x-radius-reject.md && ! grep -qi "IMPORTANT:" docs/l1-runbooks/39-8021x-radius-reject.md && grep -q "10-8021x-triage.md" docs/l1-runbooks/39-8021x-radius-reject.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/l1-runbooks/39-8021x-radius-reject.md` exists with the exact frontmatter line `platform: windows+macos+ios+android+linux` and `audience: L1`.
    - Provides all four SC2 parts: a `## Symptom` description, `## First Checks (All Platforms)`, a `## Per-Platform Diagnostic Signal` table (per-platform diagnostic commands), and a `## Escalation Criteria` trigger.
    - Contains the exact string `Microsoft-Windows-WLAN-AutoConfig/Operational`; NO occurrence of `Dot3Svc/Operational`.
    - First Checks explicitly rule out #38 (cert profiles Succeeded) and #40 (RADIUS server-name populated).
    - Escalation prose names L2 #31 and L2 #33 with NO markdown link to `../l2-runbooks/` (no substring `l2-runbooks/3`).
    - Android cell marks `adb logcat -s "wpa_supplicant"` as escalation-collected, not an L1 action.
    - Footer link to `10-8021x-triage.md` present; no `IMPORTANT:` callout; no edit to `00-index.md`.
    - Placeholder identifiers only.
  </acceptance_criteria>
  <done>Runbook #39 exists, satisfies SC2 (symptom + first-checks + per-platform diagnostic commands + escalation trigger), honors D-01/D-02/D-05/D-06/D-07/D-08, and passes the automated string checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan authors Markdown documentation only. No code, no runtime, no auth, no user input, no network surface. Zero attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Information Disclosure | Runbook example identifiers | mitigate | Runbooks MUST use placeholder identifiers only (device serial, UPN, tenant); never embed real credentials, keys, or serials — consistent with the suite "never commit credentials" rule. Acceptance criteria enforce placeholder-only content. |
| T-107-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — pure documentation phase, no package installs (RESEARCH §Package Legitimacy Audit: NOT APPLICABLE). |
</threat_model>

<verification>
- Both files exist under `docs/l1-runbooks/`.
- Structure 1C (D-01), compound frontmatter (D-02), calibrated per-platform depth (D-05) present in both.
- Exact Windows channel strings present; no `Dot3Svc/Operational`; no live `../l2-runbooks/` links; no `IMPORTANT:` callout; no `00-index.md` edit.
- #38 links the deployment-ordering constraint to `02-cert-delivery-foundation.md` (SC1); #39 provides all four SC2 parts.
</verification>

<success_criteria>
- SC1 satisfied by #38: verify cert-profile status in Intune + deployment-ordering constraint linked + per-platform diagnostic signal identified.
- SC2 satisfied by #39: symptom description + first-check steps + per-platform diagnostic commands + clear escalation trigger.
- D-01, D-02, D-05, D-06, D-07, D-08 all honored and cited; DOT1X-09 advanced.
</success_criteria>

<output>
Create `.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-01-SUMMARY.md` when done.
</output>
