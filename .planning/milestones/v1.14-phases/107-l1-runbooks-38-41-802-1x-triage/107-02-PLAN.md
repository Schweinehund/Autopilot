---
phase: 107-l1-runbooks-38-41-802-1x-triage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l1-runbooks/40-8021x-server-trust-failure.md
  - docs/l1-runbooks/41-8021x-eap-negotiation-failure.md
autonomous: true
requirements: [DOT1X-09]

must_haves:
  truths:
    - "L1 runbook #40 (server-trust/validation failure) provides a symptom description, First Checks, a per-platform diagnostic table, and a clear escalation trigger (SC2)"
    - "L1 runbook #41 (EAP negotiation failure) provides a symptom description, First Checks, a per-platform diagnostic table, and a clear escalation trigger (SC2)"
    - "Both runbooks use structure 1C per D-01: shared symptom + First Checks + compact per-platform diagnostic-signal table + per-platform escalation-divergence notes (SC2:213 four-part ordering)"
    - "Both runbooks carry compound frontmatter per D-02: platform: windows+macos+ios+android+linux + audience: L1 + applies_to + 90-day last_verified/review_by pair"
    - "Per-platform depth is calibrated per D-05: Windows/macOS/Linux name the signal plus one read-only command; iOS = Intune-portal inspection only; Android = adb logcat named as escalation-collected; collect-don't-interpret throughout"
    - "Windows signal uses the EXACT strings Microsoft-Windows-WLAN-AutoConfig/Operational and Microsoft-Windows-Wired-AutoConfig/Operational; NO occurrence of Dot3Svc/Operational (D-05 / RESEARCH correction)"
    - "L2 references are prose-only forward-refs with no live links per D-06; the routing map per D-07 is baked in as prose: #40 -> L2 #31 -> #33 primary + #32 cross-ref; #41 -> L2 #31 -> #33"
    - "#41 links EAP-method context to 01-eap-method-overview.md (link-not-copy); #40 links server-name-validation to 02-cert-delivery-foundation.md (link-not-copy)"
    - "Neither runbook edits docs/l1-runbooks/00-index.md; the conscious defer to Phase 109 (D-08) is recorded as a plan truth"
  artifacts:
    - path: "docs/l1-runbooks/40-8021x-server-trust-failure.md"
      provides: "L1 #40 802.1X server-trust/validation-failure triage runbook (cross-platform, symptom-primary)"
      contains: "platform: windows+macos+ios+android+linux"
      min_lines: 60
    - path: "docs/l1-runbooks/41-8021x-eap-negotiation-failure.md"
      provides: "L1 #41 802.1X EAP-negotiation-failure triage runbook (cross-platform, symptom-primary)"
      contains: "platform: windows+macos+ios+android+linux"
      min_lines: 60
  key_links:
    - from: "docs/l1-runbooks/40-8021x-server-trust-failure.md"
      to: "docs/admin-setup-8021x/02-cert-delivery-foundation.md"
      via: "markdown link to server-name validation (link-not-copy)"
      pattern: "02-cert-delivery-foundation\\.md"
    - from: "docs/l1-runbooks/41-8021x-eap-negotiation-failure.md"
      to: "docs/admin-setup-8021x/01-eap-method-overview.md"
      via: "markdown link to EAP method overview (link-not-copy)"
      pattern: "01-eap-method-overview\\.md"
    - from: "docs/l1-runbooks/40-8021x-server-trust-failure.md"
      to: "L2 #33 primary + #32 cross-ref (prose only)"
      via: "escalation prose naming L2 #33 and #32, no markdown link"
      pattern: "#33"
---

<objective>
Author the second pair of cross-platform, symptom-primary 802.1X L1 triage runbooks:
`40-8021x-server-trust-failure.md` (#40, SC2) and `41-8021x-eap-negotiation-failure.md`
(#41, SC2).

Purpose: Complete the four-runbook L1 triage set with the server-trust/validation and
EAP-negotiation failure classes, each spanning all five platforms via a per-platform
diagnostic-signal table. #40 carries the dual-destination L2 routing (#33 primary +
#32 cross-ref, D-07); #41 is the EAP-method-mismatch class linking to the co-equal EAP
overview.

Output: Two new Markdown runbook files under `docs/l1-runbooks/`, structured identically
to the Plan 01 pair (D-01/D-02/D-05/D-06/D-07/D-08). Documentation only — no code, no runtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-CONTEXT.md
@.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-RESEARCH.md
@.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author L1 runbook #40 — 802.1X Server Trust / Validation Failure (SC2)</name>
  <files>docs/l1-runbooks/40-8021x-server-trust-failure.md</files>
  <read_first>
    - docs/l1-runbooks/40-8021x-server-trust-failure.md (the file being created — confirm it does not yet exist)
    - docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md lines 1-17 (D-02 compound frontmatter + platform gate + L1 scope note)
    - docs/l1-runbooks/35-macos-sso-sign-in-failure.md (D-01 section skeleton + Escalation Criteria / "Before escalating, collect:" / Version History)
    - docs/admin-setup-8021x/02-cert-delivery-foundation.md lines 98-104 (server-name validation link target — LINK, never restate)
    - docs/_glossary-network.md (server-name-validation / RADIUS term anchors — link targets)
    - .planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-RESEARCH.md §"#40 Server Trust / Validation Failure" + §"Per-Platform Diagnostic Signal Table" (exact strings, discriminator, dual routing)
    - .planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-PATTERNS.md (shared skeleton + strips/anti-patterns)
  </read_first>
  <action>
    Create `docs/l1-runbooks/40-8021x-server-trust-failure.md` under structure 1C (D-01) using the same
    skeleton, platform-gate, and L1 scope-note as the Plan 01 runbooks. Frontmatter (D-02): compound
    `platform: windows+macos+ios+android+linux` + `audience: L1` + `applies_to: both` + `last_verified: 2026-06-30`
    / `review_by: 2026-09-28`.
    `## Symptom`: on Windows the user sees an intermittent certificate trust dialog ("Trust this certificate?");
    on macOS/iOS the connection fails immediately with "Authentication Failed" and NO prompt (managed profiles
    suppress the dialog, so trust failures are silent); the Trusted Certificate profile for the RADIUS server
    root CA may be missing, wrong, or not yet deployed. `## First Checks (All Platforms)`: in Intune check
    whether a Trusted Certificate profile for the RADIUS root CA is assigned to the device group AND shows
    Succeeded; verify the Certificate server-names field in the Wi-Fi/Wired profile is populated (empty field
    with server validation nominally enabled is a misconfiguration producing this symptom) — LINK the
    server-name-validation concept to `../admin-setup-8021x/02-cert-delivery-foundation.md` (do NOT restate).
    Per-platform table (D-05, exact strings): Windows `Microsoft-Windows-WLAN-AutoConfig/Operational` /
    `Microsoft-Windows-Wired-AutoConfig/Operational` (never `Dot3Svc/Operational`), noting event 8001
    "server certificate chain couldn't be validated"; macOS `log show --predicate 'subsystem contains "com.apple.eapol"' --info --last 30m`;
    iOS Intune-portal Trusted Certificate profile status only; Android `adb logcat -s "wpa_supplicant"` named
    escalation-collected (never run at L1); Linux `journalctl -u NetworkManager` (TLS handshake failure).
    Collect-don't-interpret throughout. Escalation prose PROSE-ONLY (D-06) with the DUAL D-07 routing:
    L2 Log Collection (#31) then L2 RADIUS/EAP Investigation (#33, PRIMARY — server-name validation) with
    L2 Certificate Chain Investigation (#32) as a cross-ref (trusted-root chain is the root-cause mechanism);
    name #31, #33, and #32 in prose with NO `../l2-runbooks/` markdown links. #33 MUST be stated as primary.
    (Claude's discretion per D-07: the #32 cross-ref may be inline in the escalation note or in a See-Also.)
    Footer `[Back to 802.1X Triage Decision Tree](../decision-trees/10-8021x-triage.md)`; Version History row
    dated `2026-06-30` "Phase 107 plan 02: initial authoring — 802.1X server trust failure (D-01/D-02/D-05/D-07)";
    author `--`. Callouts NOTE/WARNING/DANGER/CRITICAL only. Do NOT edit `00-index.md` (D-08). Placeholder
    identifiers only.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && test -f docs/l1-runbooks/40-8021x-server-trust-failure.md && grep -q "platform: windows+macos+ios+android+linux" docs/l1-runbooks/40-8021x-server-trust-failure.md && grep -q "Microsoft-Windows-WLAN-AutoConfig/Operational" docs/l1-runbooks/40-8021x-server-trust-failure.md && ! grep -q "Dot3Svc/Operational" docs/l1-runbooks/40-8021x-server-trust-failure.md && grep -q "#33" docs/l1-runbooks/40-8021x-server-trust-failure.md && grep -q "#32" docs/l1-runbooks/40-8021x-server-trust-failure.md && grep -q "02-cert-delivery-foundation.md" docs/l1-runbooks/40-8021x-server-trust-failure.md && ! grep -q "l2-runbooks/3" docs/l1-runbooks/40-8021x-server-trust-failure.md && ! grep -qi "IMPORTANT:" docs/l1-runbooks/40-8021x-server-trust-failure.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/l1-runbooks/40-8021x-server-trust-failure.md` exists with the exact frontmatter line `platform: windows+macos+ios+android+linux` and `audience: L1`.
    - Provides all four SC2 parts (symptom, First Checks, per-platform diagnostic table, escalation trigger).
    - Contains the exact string `Microsoft-Windows-WLAN-AutoConfig/Operational`; NO occurrence of `Dot3Svc/Operational`.
    - Symptom prose captures the platform divergence: Windows trust dialog vs silent "Authentication Failed" on macOS/iOS.
    - Escalation prose names L2 #33 as PRIMARY and L2 #32 as cross-ref, plus L2 #31 prerequisite, with NO markdown link to `../l2-runbooks/` (no substring `l2-runbooks/3`).
    - Contains a markdown link to `02-cert-delivery-foundation.md` (server-name validation linked, not restated).
    - No `IMPORTANT:` callout; no edit to `00-index.md`; placeholder identifiers only.
  </acceptance_criteria>
  <done>Runbook #40 exists, satisfies SC2, carries the dual D-07 routing (#33 primary + #32 cross-ref), honors D-01/D-02/D-05/D-06/D-08, and passes the automated string checks.</done>
</task>

<task type="auto">
  <name>Task 2: Author L1 runbook #41 — 802.1X EAP Negotiation Failure (SC2)</name>
  <files>docs/l1-runbooks/41-8021x-eap-negotiation-failure.md</files>
  <read_first>
    - docs/l1-runbooks/41-8021x-eap-negotiation-failure.md (the file being created — confirm it does not yet exist)
    - docs/l1-runbooks/40-8021x-server-trust-failure.md (sibling authored in Task 1 — reuse the identical 1C skeleton, frontmatter, and per-platform table shape)
    - docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md lines 1-17 (frontmatter + platform gate + scope note)
    - docs/l1-runbooks/35-macos-sso-sign-in-failure.md (section skeleton + Escalation Criteria / "Before escalating, collect:")
    - docs/admin-setup-8021x/01-eap-method-overview.md (EAP method overview — LINK, never restate)
    - docs/_glossary-network.md (EAP / EAPOL term anchors — link targets)
    - .planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-RESEARCH.md §"#41 EAP Negotiation Failure" + §"Per-Platform Diagnostic Signal Table" (exact strings + iOS-PEAP-PAP discriminator)
    - .planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-PATTERNS.md (shared skeleton + strips/anti-patterns)
  </read_first>
  <action>
    Create `docs/l1-runbooks/41-8021x-eap-negotiation-failure.md` under structure 1C (D-01), cloning the
    Task 1 skeleton, platform-gate, and L1 scope-note. Frontmatter (D-02): compound
    `platform: windows+macos+ios+android+linux` + `audience: L1` + `applies_to: both` + `last_verified: 2026-06-30`
    / `review_by: 2026-09-28`.
    `## Symptom`: the connection fails after a longer delay than a cert-rejection would produce (EAP negotiation
    starts but the method is rejected); cert profiles all show Succeeded; no trust dialog; the symptom is
    consistent across users on a given device type/platform. Common cause: iOS with PEAP where the RADIUS side
    is configured for PAP (iOS PEAP inner auth is always MS-CHAPv2 — PAP causes an immediate EAP-NAK), surfacing
    as "Authentication Failed on iOS only while other platforms succeed on the same SSID"; another cause is an
    EAP-TTLS inner-auth mismatch between the Intune profile and the RADIUS policy. `## First Checks (All Platforms)`:
    confirm cert profiles are Succeeded (rules out #38); ask which EAP method is configured in the Intune profile,
    whether it worked before, and whether it affects one device type or all platforms — LINK the EAP-method
    context to `../admin-setup-8021x/01-eap-method-overview.md` (co-equal EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS;
    do NOT restate). Per-platform table (D-05, exact strings): Windows
    `Microsoft-Windows-WLAN-AutoConfig/Operational` / `Microsoft-Windows-Wired-AutoConfig/Operational` (never
    `Dot3Svc/Operational`) showing EAP negotiation failure with method-specific text; macOS
    `log show --predicate 'subsystem contains "com.apple.eapol"' --info --last 30m`; iOS = NO device log,
    Intune-portal only, with the "iOS fails, others succeed" symptom pattern itself as the discriminating
    signal; Android `adb logcat -s "wpa_supplicant"` named escalation-collected (never run at L1); Linux
    `journalctl -u NetworkManager`. Collect-don't-interpret. Escalation prose PROSE-ONLY (D-06) with D-07
    routing: L2 Log Collection (#31) then L2 RADIUS/EAP Investigation (#33); name #31 and #33 in prose with
    NO `../l2-runbooks/` markdown links. Footer `[Back to 802.1X Triage Decision Tree](../decision-trees/10-8021x-triage.md)`;
    Version History row dated `2026-06-30` "Phase 107 plan 02: initial authoring — 802.1X EAP negotiation
    failure (D-01/D-02/D-05)"; author `--`. Callouts NOTE/WARNING/DANGER/CRITICAL only. Do NOT edit
    `00-index.md` (D-08). Placeholder identifiers only.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && test -f docs/l1-runbooks/41-8021x-eap-negotiation-failure.md && grep -q "platform: windows+macos+ios+android+linux" docs/l1-runbooks/41-8021x-eap-negotiation-failure.md && grep -q "Microsoft-Windows-WLAN-AutoConfig/Operational" docs/l1-runbooks/41-8021x-eap-negotiation-failure.md && ! grep -q "Dot3Svc/Operational" docs/l1-runbooks/41-8021x-eap-negotiation-failure.md && grep -q "01-eap-method-overview.md" docs/l1-runbooks/41-8021x-eap-negotiation-failure.md && grep -q "#33" docs/l1-runbooks/41-8021x-eap-negotiation-failure.md && ! grep -q "l2-runbooks/3" docs/l1-runbooks/41-8021x-eap-negotiation-failure.md && ! grep -qi "IMPORTANT:" docs/l1-runbooks/41-8021x-eap-negotiation-failure.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/l1-runbooks/41-8021x-eap-negotiation-failure.md` exists with the exact frontmatter line `platform: windows+macos+ios+android+linux` and `audience: L1`.
    - Provides all four SC2 parts (symptom, First Checks, per-platform diagnostic table, escalation trigger).
    - Contains the exact string `Microsoft-Windows-WLAN-AutoConfig/Operational`; NO occurrence of `Dot3Svc/Operational`.
    - Symptom captures the iOS-PEAP-PAP / "iOS fails, others succeed" discriminator; iOS cell states no device log (portal-only, pattern is the signal).
    - Contains a markdown link to `01-eap-method-overview.md` (EAP context linked, not restated).
    - Escalation prose names L2 #31 and L2 #33 with NO markdown link to `../l2-runbooks/` (no substring `l2-runbooks/3`).
    - Android cell marks `adb logcat -s "wpa_supplicant"` as escalation-collected; no `IMPORTANT:` callout; no edit to `00-index.md`.
    - Placeholder identifiers only.
  </acceptance_criteria>
  <done>Runbook #41 exists, satisfies SC2, links the co-equal EAP overview, honors D-01/D-02/D-05/D-06/D-07/D-08, and passes the automated string checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Markdown documentation only. No code, no runtime, no auth, no user input, no network surface. Zero attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Information Disclosure | Runbook example identifiers | mitigate | Runbooks MUST use placeholder identifiers only; never embed real credentials, keys, or serials — consistent with the suite "never commit credentials" rule. Acceptance criteria enforce placeholder-only content. |
| T-107-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — pure documentation phase, no package installs (RESEARCH §Package Legitimacy Audit: NOT APPLICABLE). |
</threat_model>

<verification>
- Both files exist under `docs/l1-runbooks/`.
- Structure 1C (D-01), compound frontmatter (D-02), calibrated per-platform depth (D-05) present in both.
- Exact Windows channel strings present; no `Dot3Svc/Operational`; no live `../l2-runbooks/` links; no `IMPORTANT:` callout; no `00-index.md` edit.
- #40 carries dual routing (#33 primary + #32 cross-ref); #41 links `01-eap-method-overview.md`.
</verification>

<success_criteria>
- SC2 satisfied by #40 and #41: each provides symptom description + first-check steps + per-platform diagnostic commands + clear escalation trigger.
- D-01, D-02, D-05, D-06, D-07, D-08 all honored and cited; DOT1X-09 advanced.
</success_criteria>

<output>
Create `.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-02-SUMMARY.md` when done.
</output>
