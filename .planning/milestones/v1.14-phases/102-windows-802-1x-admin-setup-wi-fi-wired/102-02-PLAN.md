---
phase: 102-windows-802-1x-admin-setup-wi-fi-wired
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - docs/admin-setup-8021x/00-overview.md
autonomous: true
requirements: [DOT1X-04]

must_haves:
  truths:
    - "The local 00-overview.md platform-guide list gains item 3 as a descriptive one-liner linking 03-windows.md, so a reader navigating the 802.1X folder entry point can discover the newly authored Windows guide"
    - "Navigation-last (local-only): only the in-folder 00-overview.md platform-list entry is edited; no capability-matrix rows and no global nav-hub files (index.md, quick-refs, decision-trees) are touched (those are Phase 109)"
    - "The placeholder '3-7. Platform guides (Phase 102-106)' line is replaced by item 3 (Windows) plus a '4-7. Platform guides (Phase 103-106)' continuation, matching the existing numbered-list link style (bold title, double-dash, no trailing period)"
    - "Harness allowlisting: 00-overview.md is a pre-existing file; the edit must be recorded for harness allowlisting, the same pattern used for the Phase 101 glossary see-also edits"
  artifacts:
    - path: "docs/admin-setup-8021x/00-overview.md"
      provides: "Local 802.1X folder entry point with the Windows platform-guide entry (item 3)"
      contains: "](03-windows.md)"
  key_links:
    - from: "docs/admin-setup-8021x/00-overview.md"
      to: "docs/admin-setup-8021x/03-windows.md"
      via: "platform-guide list item 3"
      pattern: "\\]\\(03-windows\\.md\\)"
---

<objective>
Add the Windows platform-guide entry to the local `docs/admin-setup-8021x/00-overview.md` numbered list (item 3), replacing the reserved "3-7. Platform guides (Phase 102-106)" placeholder line with the authored Windows entry plus a "4-7." continuation for the still-pending platform guides.

Purpose: Make the just-authored `03-windows.md` discoverable from the 802.1X folder entry point. This is the ONLY in-scope navigation edit for Phase 102 — capability matrices and global nav hubs are deferred to Phase 109 (navigation-last).

Output: `docs/admin-setup-8021x/00-overview.md` (EDIT — one numbered-list line replaced with two lines + a Change History row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/admin-setup-8021x/00-overview.md
@.planning/phases/102-windows-802-1x-admin-setup-wi-fi-wired/102-PATTERNS.md
@.planning/phases/102-windows-802-1x-admin-setup-wi-fi-wired/102-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Windows platform-guide entry (item 3) to 00-overview.md</name>
  <files>docs/admin-setup-8021x/00-overview.md</files>
  <read_first>
    - docs/admin-setup-8021x/00-overview.md — the live file; locate the numbered-list line `3–7. Platform guides (Phase 102–106) -- entries added as each guide is authored.` (the single edit target) and confirm the item 1/2 link style above it
    - 102-PATTERNS.md — `00-overview.md (EDIT — add item 3)` section: exact region to edit, descriptive-one-liner link format (bold title, `--`, single sentence, no trailing period), and the rule that the `3–7.` line becomes item 3 + a new `4–7.` continuation
    - 102-RESEARCH.md — `00-overview.md Platform Entry (one-line edit, item 3)`: suggested replacement text and rationale
    - docs/admin-setup-8021x/03-windows.md — confirm the link target filename exists (created in Plan 102-01) before linking to it
  </read_first>
  <action>
    Edit `docs/admin-setup-8021x/00-overview.md`. Replace the single numbered-list line `3–7. Platform guides (Phase 102–106) -- entries added as each guide is authored.` with two lines: (1) item 3, a descriptive one-liner matching the item-1/item-2 style — bold linked title `**[Windows 802.1X Admin Setup (Wi-Fi + Wired)](03-windows.md)**`, then `--`, then a single description sentence naming the key content (Wi-Fi and wired profiles for all three EAP methods; dot3svc dependency and Remediation pattern; enforcement staging; KB5014754 strong certificate mapping), with no trailing period; and (2) a continuation line `4–7. Platform guides (Phase 103–106) -- entries added as each guide is authored.` Keep the title parenthetical free of ` / ` (use `+`) to respect the corpus slug convention. Do NOT modify the Mermaid `3–7. Platform Guides` node, the Scope section, the wired-availability note, or any other file; this edit is strictly the numbered-list line and (below) the Change History.

    Append a Change History row dated 2026-06-30 noting the addition of the Windows (item 3) platform-guide entry.

    Record in the SUMMARY that `00-overview.md` is a pre-existing file and this edit must be added to the harness allowlist (same handling as the Phase 101 glossary see-also edits).
  </action>
  <verify>
    <automated>grep -q "](03-windows.md)" docs/admin-setup-8021x/00-overview.md && grep -q "4–7. Platform guides (Phase 103–106)" docs/admin-setup-8021x/00-overview.md && ! grep -q "3–7. Platform guides (Phase 102–106)" docs/admin-setup-8021x/00-overview.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `00-overview.md` numbered list contains item `3.` with a bold link `](03-windows.md)` and a single description sentence (no trailing period), matching the item-1/item-2 format
    - A `4–7. Platform guides (Phase 103–106)` continuation line is present
    - The old `3–7. Platform guides (Phase 102–106)` numbered-list line is gone
    - No other file is modified (no capability-matrix rows, no `docs/index.md` / quick-ref / decision-tree edits — navigation-last, Phase 109)
    - The Mermaid diagram, Scope section, and wired-availability note in `00-overview.md` are unchanged
    - A Change History row dated 2026-06-30 is added
    - The SUMMARY records that `00-overview.md` is a pre-existing file requiring harness-allowlist registration (Phase 101 glossary-edit pattern)
  </acceptance_criteria>
  <done>The Windows guide is discoverable from the local 802.1X folder entry point via item 3; the placeholder range is correctly narrowed to 4-7; the pre-existing-file edit is flagged for harness allowlisting. DOT1X-04 navigation is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoring artifact | `00-overview.md` is a static Markdown navigation file; the edit adds one link and one Change History row. No executable surface, no input parsing, no runtime. |

This is a documentation navigation edit with no executable attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-02-01 | Tampering | Pre-existing file edited outside harness allowlist (integrity-tracking drift) | mitigate | Edit is scoped to the single numbered-list line + one Change History row; SUMMARY flags the file for harness-allowlist registration (Phase 101 glossary-edit pattern) |
| T-102-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no scripts; documentation-only one-line edit — no supply-chain surface |

Block-on severity: high. No high-severity content-security obligations apply to a navigation link edit; the only tracked concern is allowlist-integrity hygiene (T-102-02-01).
</threat_model>

<verification>
- `00-overview.md` links to `03-windows.md` as numbered item 3 in the platform-guide list.
- The placeholder range is updated from `3–7 (Phase 102–106)` to `4–7 (Phase 103–106)`.
- No global nav-hub or capability-matrix files are touched (navigation-last invariant honored for Phase 102).
- The pre-existing-file edit is recorded for harness allowlisting.
</verification>

<success_criteria>
- The Windows 802.1X guide is reachable from the local 802.1X overview entry point.
- Edit is surgical (one list line replaced + one Change History row); nothing else in `00-overview.md` changes.
- Harness-allowlist registration is flagged in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/102-windows-802-1x-admin-setup-wi-fi-wired/102-02-SUMMARY.md` when done.
</output>
