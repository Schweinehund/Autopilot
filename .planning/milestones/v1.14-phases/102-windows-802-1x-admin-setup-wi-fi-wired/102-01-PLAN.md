---
phase: 102-windows-802-1x-admin-setup-wi-fi-wired
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-8021x/03-windows.md
autonomous: true
requirements: [DOT1X-04]

must_haves:
  truths:
    - "SC1 + D-01 + D-03: 03-windows.md follows the A3 structure (Common Profile Mechanics -> Wi-Fi -> Wired) and lets an admin create a Windows Wi-Fi 802.1X profile covering EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS at co-equal depth with a User / Machine / User-or-machine authentication-mode decision table"
    - "D-01: each connection subsection carries a compact per-EAP-method config matrix with three method columns (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS) and no recommended-default ranking"
    - "D-02: wired-only concerns (dot3svc, enforcement staging, TEAP awareness note) are homed exactly once, inside the Wired subsection, not repeated per EAP method"
    - "SC2 + D-04: the Wired subsection documents the dot3svc (Wired AutoConfig) service dependency as a detect->remediate pattern naming sc query dot3svc, Get-Service dot3svc, Set-Service -StartupType Automatic, Start-Service dot3svc, and the Intune Remediations UI path"
    - "SC2 + D-05: the dot3svc deliverable is a documented pattern only; no productionized Detect-*.ps1 / Remediate-*.ps1 file pair is shipped"
    - "SC2: a DANGER callout warns that setting 802.1X enforcement to Enforce before RADIUS reachability and cert delivery are confirmed locks out all wired devices simultaneously; staged Do-not-enforce-first rollout and a break-glass procedure are documented"
    - "SC3 + D-03: PerformServerValidation is documented at guide altitude as always-on with populated Certificate server names and a trusted-root profile reference; no example ever shows PerformServerValidation = false (mitigates rogue-RADIUS A-05/C-02)"
    - "SC3: SCEP and PKCS client-cert options appear in both connection matrices and PFX Import (PKCS Imported) appears as a Wired-only EAP-TLS client-cert option"
    - "SC4 + D-06 + D-07 + D-08: a standalone NOTE-style Hybrid Entra Joined callout states KB5014754 enforcement since 2025-02-11, the SID-in-SAN requirement for EAP-TLS, that Intune SCEP/PKCS profiles can include the SID, and that cloud-only Entra Joined devices are unaffected, carrying its own inline 180-day freshness stamp (review_by 2026-12-27)"
    - "D-06: file front-matter carries the 90-day freshness stamp (last_verified 2026-06-30, review_by 2026-09-28); the inline KB5014754 callout carries the separate 180-day stamp"
    - "D-09 + D-10: Templates is the primary authoring path for both Wi-Fi and Wired profiles, with a single sentence (not a second walkthrough) noting the Settings Catalog also exposes these settings"
    - "D-11: TEAP appears as a one-paragraph awareness note in the Wired subsection (unique to Windows wired, chains machine+user via Primary/Secondary EAP), not as a co-equal config section"
    - "Link-not-copy: shared concepts (deployment ordering rule, EKU, server-name-validation theory, EAP-method comparison) are linked to 01-eap-method-overview.md / 02-cert-delivery-foundation.md / _glossary-network.md, never restated"
    - "C-01: anonymous outer identity (Identity privacy) guidance is present for all three EAP methods to prevent cleartext UPN leakage before the tunnel"
  artifacts:
    - path: "docs/admin-setup-8021x/03-windows.md"
      provides: "Windows Wi-Fi + wired 802.1X admin-setup guide, all three EAP methods"
      contains: "## Wired"
      min_lines: 120
  key_links:
    - from: "docs/admin-setup-8021x/03-windows.md"
      to: "docs/admin-setup-8021x/02-cert-delivery-foundation.md#canonical-scope-callout"
      via: "scope banner link"
      pattern: "02-cert-delivery-foundation\\.md#canonical-scope-callout"
    - from: "docs/admin-setup-8021x/03-windows.md"
      to: "docs/admin-setup-8021x/01-eap-method-overview.md"
      via: "prerequisites + link-not-copy cross-references"
      pattern: "01-eap-method-overview\\.md"
---

<objective>
Author `docs/admin-setup-8021x/03-windows.md` — the Windows per-platform 802.1X admin-setup guide covering Wi-Fi and wired Intune profiles for all three co-equal EAP methods (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS), satisfying DOT1X-04 and ROADMAP Phase 102 success criteria SC1–SC4.

Purpose: Give an Intune admin a single Windows guide that walks through Wi-Fi and wired 802.1X profile creation, surfaces the high-consequence Windows-specific pitfalls (dot3svc silent failure, enforcement-staging lockout, auth-mode mismatch, disabled server validation, cleartext identity leakage), and links — never restates — the shared 802.1X foundation in `01-`/`02-`.

Output: `docs/admin-setup-8021x/03-windows.md` (CREATE), following the locked A3 structure (Common Profile Mechanics → Wi-Fi → Wired → standalone KB5014754 callout → See Also → Change History).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-windows-802-1x-admin-setup-wi-fi-wired/102-RESEARCH.md
@.planning/phases/102-windows-802-1x-admin-setup-wi-fi-wired/102-PATTERNS.md
@.planning/phases/102-windows-802-1x-admin-setup-wi-fi-wired/102-CONTEXT.md
@docs/admin-setup-8021x/01-eap-method-overview.md
@docs/admin-setup-8021x/02-cert-delivery-foundation.md
@docs/admin-setup-macos/03-configuration-profiles.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold + Common Profile Mechanics + Wi-Fi subsection</name>
  <files>docs/admin-setup-8021x/03-windows.md</files>
  <read_first>
    - 102-RESEARCH.md — Guide Content Facts: Front Matter, Scope Banner, Common Profile Mechanics (auth-mode decision table, PerformServerValidation, anonymous outer identity, Settings Catalog one-sentence note), Wi-Fi Subsection (nav path, per-EAP-method matrix, cert options)
    - 102-PATTERNS.md — YAML front-matter block, Prerequisites blockquote, Scope banner (copy verbatim), `#### In Intune admin center` header, matrix/table format, link-not-copy cross-reference style, double-dash em-dash rule, double-hyphen slug trap
    - docs/admin-setup-8021x/02-cert-delivery-foundation.md — confirm the live `#canonical-scope-callout`, `#the-deployment-ordering-rule`, `#eku-requirement-client-authentication`, `#per-platform-cert-delivery-support-matrix` anchor slugs before linking; front-matter shape
    - docs/admin-setup-8021x/01-eap-method-overview.md — EAP-method comparison table pattern; link target for EAP concepts and PEAP security rationale
    - docs/admin-setup-macos/03-configuration-profiles.md — `#### In Intune admin center` compact-subsection precedent and `> **Label:**` callout convention
  </read_first>
  <action>
    Create `docs/admin-setup-8021x/03-windows.md`. Start with the 6-key YAML front matter: `last_verified: 2026-06-30`, `review_by: 2026-09-28`, `applies_to: both`, `audience: admin`, `platform: windows`. Follow with an H1, a Prerequisites blockquote linking `01-eap-method-overview.md` and `02-cert-delivery-foundation.md`, then the one-line Scope banner copied verbatim from `02-` (`> **Scope:** Intune client-side configuration only. RADIUS/NPS server assumed to exist.` linking to `02-cert-delivery-foundation.md#canonical-scope-callout`).

    Author a `## Common Profile Mechanics` section that (per D-03): renders the authentication-mode decision table with rows User / Machine / User or machine / Guest and columns for device-credential use, user-credential use, and when-to-use guidance (cloud-native Entra Joined → User; pre-logon domain → Machine; Hybrid Entra Joined → User or machine), explicitly noting auth mode is not prominently surfaced in the Intune UI (pitfall B-03) and that Machine / User-or-machine requires a device cert profile alongside the user cert profile; documents the PerformServerValidation requirement at guide altitude — always enabled, always populate Certificate server names with the RADIUS FQDN or CN suffix, reference a trusted-root profile, and link to `01-` for the security rationale (never restate it, never show a `PerformServerValidation = false` example — this is the rogue-RADIUS / NT-hash-crack surface A-05/C-02); documents anonymous outer identity via the "Identity privacy" field (value `anonymous` or `anonymous@domain`) applying to all three EAP methods (pitfall C-01); and adds the single-sentence Settings Catalog note (D-09/D-10) that the Settings Catalog also exposes these settings and may offer more granular options — exactly one sentence, Windows-scoped, NOT a second walkthrough. Link cert-delivery prerequisites (ordering rule, EKU, server-name-validation theory, per-platform cert matrix) to `02-`/`_glossary-network.md` rather than restating them.

    Author a `## Wi-Fi` section opening with a `#### In Intune admin center` header whose bold navigation path is **Devices** > **Configuration** > **New policy** > **Windows 10 and later** > **Templates** > **Wi-Fi**, followed by the per-EAP-method config matrix from RESEARCH.md (settings as rows; three columns EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS) — including EAP type field value, Certificate server names, Root cert for server validation, Perform server validation, Client auth method (SCEP cert / PKCS cert / Derived credential for EAP-TLS; Username and Password for PEAP/TTLS), Inner method (MSCHAPv2 for PEAP; PAP/MS-CHAP/MS-CHAPv2 for TTLS), and Identity privacy. Keep all three EAP methods co-equal — no "recommended" or "default" language. Use `--` (double hyphen) for em-dashes throughout; avoid ` / ` inside any heading that will serve as a link-target slug (double-hyphen trap).
  </action>
  <verify>
    <automated>grep -q "review_by: 2026-09-28" docs/admin-setup-8021x/03-windows.md && grep -q "## Common Profile Mechanics" docs/admin-setup-8021x/03-windows.md && grep -q "## Wi-Fi" docs/admin-setup-8021x/03-windows.md && grep -q "canonical-scope-callout" docs/admin-setup-8021x/03-windows.md && ! grep -qi "PerformServerValidation = false\|PerformServerValidation=false" docs/admin-setup-8021x/03-windows.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `03-windows.md` exists with front-matter `platform: windows`, `applies_to: both`, `last_verified: 2026-06-30`, `review_by: 2026-09-28`
    - File contains a one-line Scope banner linking `02-cert-delivery-foundation.md#canonical-scope-callout`
    - A `## Common Profile Mechanics` section exists containing an authentication-mode table with User, Machine, and `User or machine` rows
    - The PerformServerValidation guidance appears and no occurrence of `PerformServerValidation = false` (or `=false`) exists anywhere in the file
    - Anonymous outer identity / `Identity privacy` guidance is present and references all three EAP methods
    - The Settings Catalog acknowledgment is a single sentence (one `Settings catalog`/`Settings Catalog` mention in Common Mechanics, no `#### In Intune admin center` Settings-Catalog walkthrough block)
    - A `## Wi-Fi` section exists with a `#### In Intune admin center` nav path containing `Templates` and `Wi-Fi`
    - The Wi-Fi config matrix has three EAP-method columns (`EAP-TLS`, `PEAP-MSCHAPv2`, `EAP-TTLS`) and no "recommended"/"default" method ranking in prose
    - Cross-references to `01-eap-method-overview.md` and `02-cert-delivery-foundation.md` are present; shared concepts are linked, not restated
  </acceptance_criteria>
  <done>Common Mechanics + Wi-Fi half of the guide is authored; SC1 and the SC3 PerformServerValidation/SCEP/PKCS portions are satisfied; D-01 (Wi-Fi matrix), D-03, D-09, D-10, and C-01 guidance are in place.</done>
</task>

<task type="auto">
  <name>Task 2: Wired subsection + dot3svc/enforcement callouts + KB5014754 callout + closeout</name>
  <files>docs/admin-setup-8021x/03-windows.md</files>
  <read_first>
    - docs/admin-setup-8021x/03-windows.md — the file created in Task 1 (append the Wired subsection and closeout; keep front matter and Wi-Fi intact)
    - 102-RESEARCH.md — Wired Subsection (nav path, dot3svc dependency + detection signal + cmdlets + Remediations UI path + detect/remediate pattern, enforcement-staging field values + DANGER callout content, Wired per-EAP-method matrix incl. PFX Import + Require cryptographic binding, TEAP awareness-note exact content), KB5014754 callout content + two-tier freshness stamp, Code Examples (enforcement DANGER template, KB5014754 NOTE template)
    - 102-PATTERNS.md — callout severity labels (DANGER / WARNING / NOTE), inline freshness-stamp line, Change History footer, double-hyphen slug trap (use `and`/`+`, not ` / `, in the KB callout heading)
    - 102-CONTEXT.md — D-02, D-04, D-05, D-07, D-08, D-11 exact scope boundaries
  </read_first>
  <action>
    Append a `## Wired` section to `03-windows.md` opening with a `#### In Intune admin center` header whose bold navigation path is **Devices** > **Configuration** > **New policy** > **Windows 10 and later** > **Templates** > **Wired network** (note it uses the WiredNetwork CSP).

    Add a WARNING callout for the dot3svc (Wired AutoConfig) service dependency (pitfall B-01): state that dot3svc ships as Manual startup on Windows 10/11 and that the wired profile reports "Succeeded" in Intune even when the service is stopped, so the supplicant never engages. Document the detect→remediate pattern (D-04, NOT a productionized script per D-05): the detection signal `sc query dot3svc` (look for STATE: STOPPED / Manual start type); the load-bearing cmdlets `Get-Service dot3svc`, `Set-Service -Name dot3svc -StartupType Automatic`, `Start-Service dot3svc`; the detection rule (exit non-zero when StartType is not Automatic or Status is not Running) and the remediation action (set StartupType Automatic then start the service); and the Intune Remediations UI path **Devices** > **Remediations** > **+ Create** (Windows 10 and later; supply detection + remediation scripts; assign to device groups). Present the cmdlets as inline code in prose only — do NOT create or reference any `Detect-*.ps1` / `Remediate-*.ps1` files.

    Add a DANGER callout for 802.1X enforcement staging (pitfall B-02): name the three enforcement field values Not configured / Do not enforce / Enforce; require deploying with Do not enforce first and switching to Enforce only after RADIUS reachability and the cert pipeline are confirmed; warn that Enforce-before-readiness blocks all wired-connected devices simultaneously and that removing the policy needs network access (chicken-and-egg); document a break-glass procedure (manual policy removal via a non-802.1X port or local admin / USB Ethernet).

    Add the per-EAP-method Wired config matrix (settings as rows; three columns EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS) per RESEARCH.md — including the Wired-specific rows "Disable user prompts for server validation" (Yes), "Require cryptographic binding" (available for PEAP only), and a Client auth method row that lists **PFX Import (PKCS Imported)** as an EAP-TLS option unique to the Windows wired profile UI (alongside SCEP / PKCS / Derived credential). Keep all three methods co-equal.

    Add the TEAP one-paragraph awareness note (D-11) inside the Wired subsection using the exact-content guidance from RESEARCH.md: TEAP appears as a fourth wired EAP type, is unique to Windows wired, chains machine+user credentials via Primary/Secondary EAP, and is explicitly NOT a co-equal configuration path in this guide set (consult the RADIUS/NPS team before deployment). One paragraph only — no config steps, no matrix column.

    Add a standalone `## Hybrid Entra Joined -- Strong Certificate Mapping` section (heading uses `--` / `and`-style words, not ` / `, to avoid the double-hyphen slug trap) containing a NOTE-style callout (D-07/D-08): as of 2025-02-11 Windows Domain Controllers enforce KB5014754 strong certificate mapping; Hybrid Entra Joined devices using EAP-TLS (Wi-Fi or wired) must include the device or user SID in the certificate SAN; Intune SCEP and PKCS profiles can include the SID; cloud-only Entra Joined devices are unaffected. End the callout with the inline two-tier freshness stamp line `*last_verified: 2026-06-30 · review_by: 2026-12-27*` (D-06, 180-day) — this is the only callout carrying its own stamp.

    Close the file with a `## See Also` section linking `01-eap-method-overview.md`, `02-cert-delivery-foundation.md`, and `../_glossary-network.md`, and a `## Change History` table whose initial-version row is dated 2026-06-30 and summarizes the Windows 802.1X guide content. Confirm no wired-only concern (dot3svc, enforcement, TEAP) is duplicated outside the Wired subsection (D-02).
  </action>
  <verify>
    <automated>grep -q "## Wired" docs/admin-setup-8021x/03-windows.md && grep -q "Wired network" docs/admin-setup-8021x/03-windows.md && grep -q "Set-Service -Name dot3svc -StartupType Automatic" docs/admin-setup-8021x/03-windows.md && grep -qi "Remediations" docs/admin-setup-8021x/03-windows.md && grep -q "Do not enforce" docs/admin-setup-8021x/03-windows.md && grep -q "review_by: 2026-12-27" docs/admin-setup-8021x/03-windows.md && grep -qi "PFX Import" docs/admin-setup-8021x/03-windows.md && grep -qi "TEAP" docs/admin-setup-8021x/03-windows.md && grep -q "## Change History" docs/admin-setup-8021x/03-windows.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - A `## Wired` section exists with a `#### In Intune admin center` nav path containing `Templates` and `Wired network`
    - The dot3svc callout is WARNING-severity and names `Set-Service -Name dot3svc -StartupType Automatic` and `Start-Service dot3svc`, the `sc query dot3svc` detection signal, and the Intune Remediations UI path (contains `Remediations`)
    - No `Detect-*.ps1` or `Remediate-*.ps1` file is created in the repo and the guide ships the dot3svc remediation as inline cmdlets/pattern only (D-05)
    - A DANGER-severity enforcement-staging callout names both `Do not enforce` and `Enforce` and documents a break-glass procedure
    - The Wired config matrix has three EAP-method columns and includes a `PFX Import` (PKCS Imported) EAP-TLS client-cert row absent from the Wi-Fi matrix
    - A one-paragraph TEAP awareness note is present in the Wired subsection and TEAP has no matrix column and no `#### In Intune admin center` config block
    - A standalone Hybrid Entra Joined / KB5014754 NOTE callout contains `2025-02-11`, `SID`, `SAN`, the cloud-only-unaffected statement, and the inline stamp `review_by: 2026-12-27`
    - File-level front-matter `review_by` (2026-09-28) and the inline KB5014754 `review_by` (2026-12-27) are both present and distinct (D-06 two-tier)
    - `## See Also` links `01-`, `02-`, and `../_glossary-network.md`; a `## Change History` table dated 2026-06-30 is present
    - dot3svc, enforcement staging, and TEAP appear only inside the Wired subsection (D-02 — homed once)
  </acceptance_criteria>
  <done>The Wired subsection, both wired-only pitfall callouts, the standalone KB5014754 callout with its 180-day stamp, and the closeout are authored; SC2, SC4, and the SC3 PFX-Import portion are satisfied; D-02, D-04, D-05, D-06, D-07, D-08, D-11 are in place. DOT1X-04 is fully delivered in `03-windows.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supplicant (managed device) → authenticator (switch/AP) → RADIUS/NPS | 802.1X credential exchange; the guide's configuration choices determine whether this boundary is hardened |
| authoring artifact | `03-windows.md` is a static Markdown documentation file with no executable surface, no input parsing, no runtime — it ships no scripts (D-05) |

This is a documentation artifact with no executable attack surface. The threats below are CONTENT-presence obligations: the guide must include the security guidance that mitigates real misconfiguration risks. A missing obligation is a high-severity gap.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Spoofing | Rogue RADIUS / PEAP-MSCHAPv2 NT-hash crack (pitfalls A-05/C-02) | mitigate | Common Mechanics documents PerformServerValidation always-on + populated Certificate server names + trusted-root profile reference; NO example ever shows `PerformServerValidation = false` (Task 1 acceptance) |
| T-102-02 | Denial of Service | Fleet-wide wired lockout from premature Enforce (pitfall B-02) | mitigate | DANGER enforcement-staging callout: Do-not-enforce-first staged rollout + break-glass procedure (Task 2 acceptance) |
| T-102-03 | Information Disclosure | Cleartext UPN / identity leakage before tunnel (pitfall C-01) | mitigate | Anonymous outer-identity (Identity privacy) guidance for all three EAP methods (Task 1 acceptance) |
| T-102-04 | Tampering | Forged client cert via weak DC cert mapping (KB5014754) | mitigate | Standalone Hybrid Entra Joined NOTE callout: SID-in-SAN requirement, enforced since 2025-02-11, 180-day stamp (Task 2 acceptance) |
| T-102-05 | Repudiation/Spoofing | Silent unauthenticated wired port (dot3svc stopped, "Succeeded" false-positive, pitfall B-01) | mitigate | WARNING callout + detect→remediate pattern ensuring dot3svc Automatic + Running (Task 2 acceptance) |
| T-102-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no shipped scripts in this phase; documentation-only file touch — no supply-chain surface |

Block-on severity: high. All five content obligations (T-102-01..05) are encoded as task acceptance criteria; absence of any is a blocking gap.
</threat_model>

<verification>
- `03-windows.md` exists and follows the A3 structure: Common Profile Mechanics → Wi-Fi → Wired → standalone KB5014754 callout → See Also → Change History.
- Both per-EAP-method matrices present three co-equal columns (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS); no recommended-default ranking anywhere.
- File-level 90-day stamp (review_by 2026-09-28) and inline KB5014754 180-day stamp (review_by 2026-12-27) coexist and are distinct.
- Shared concepts are linked to `01-`/`02-`/`_glossary-network.md`, not restated (link-not-copy).
- No `PerformServerValidation = false` example; no `Detect-*.ps1`/`Remediate-*.ps1` files shipped.
- dot3svc, enforcement staging, and TEAP are homed once, inside the Wired subsection.
</verification>

<success_criteria>
- DOT1X-04 fully delivered by `docs/admin-setup-8021x/03-windows.md`.
- SC1: Windows Wi-Fi 802.1X profile, three co-equal EAP methods, auth-mode decision table — present.
- SC2: Windows wired profile with dot3svc dependency + Intune Remediation pattern + enforcement-staging DANGER callout — present.
- SC3: SCEP / PKCS / PFX Import client-cert options + PerformServerValidation requirement — present.
- SC4: KB5014754 Hybrid Entra Joined strong-mapping callout with freshness stamp — present.
- All security content obligations (T-102-01..05) encoded and verifiable.
</success_criteria>

<output>
Create `.planning/phases/102-windows-802-1x-admin-setup-wi-fi-wired/102-01-SUMMARY.md` when done.
</output>
