---
phase: 112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl
plan: 02
type: execute
wave: 1
depends_on: ["112-01"]
files_modified:
  - scripts/validation/v1.14-milestone-audit.mjs
  - scripts/validation/v1.14-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
autonomous: true
requirements: [HARN-01]
must_haves:
  truths:
    - "v1.14-milestone-audit.mjs is a Path-A copy of v1.13-milestone-audit.mjs — C1-C16 inherited verbatim EXCEPT the two MANDATORY D-01a threshold bumps: C5 (line ~406) and C10 (line ~542) change `diffDays > 60` → `diffDays > 90`; the 1970-01-01 template-sentinel lines are preserved unchanged"
    - "The v1.14 harness runs GREEN on the LIVE v1.14 corpus: default run 15 PASS / 0 FAIL / 0 SKIP and --self-test 9/9 (without the D-01a bump the audit would FAIL its OWN 90-day corpus on C5/C10)"
    - "v1.14-audit-allowlist.json is a Path-A copy of v1.13's sidecar with the D-04 pin repoints: ALL docs/_glossary-android.md pins +1 (~16 entries, Phase 101 eae49f7 banner at line 14) and ALL docs/reference/android-capability-matrix.md pins +1 (~7 entries, Phase 109 6306da8 row shift); index.md/quick-ref-l1.md/common-issues.md carry ZERO pins → no repoint there"
    - "C2/C7/C9 (line-pin checks) PASS against live corpus after the repoint (proves the sidecar coordinate map is correct, not born-stale)"
    - "regenerate-supervision-pins.mjs gains a BASELINE_18 comment block inserted after line 466 (before `const BASELINE_9 = [`) closing the BASELINE_17 v1.13 carry-over; the anchor is the pre-Atom-1 HEAD (the 112-01 Wave-0 commit); the BASELINE_9 line-coord array is byte-unchanged"
    - "Atom 1 lands as ONE indivisible commit touching EXACTLY 3 files (v1.14-milestone-audit.mjs + v1.14-audit-allowlist.json + regenerate-supervision-pins.mjs) — V113 does NOT ride Atom 1 (it rides Atom 2 per HARN-02); _lib/frozen-at-close.mjs is NOT touched here"
    - "No predecessor frozen surface is modified (D-00a): git diff of the v1.4-v1.13 milestone-audit .mjs / sidecar / integrity workflow set is EMPTY"
  artifacts:
    - path: "scripts/validation/v1.14-milestone-audit.mjs"
      provides: "12th Path-A milestone-audit harness (lineage v1.4→v1.14; C1-C16 verbatim; C5/C10 90d)"
      contains: "v1.14-audit-allowlist.json"
    - path: "scripts/validation/v1.14-audit-allowlist.json"
      provides: "v1.14 sidecar (Path-A from v1.13; glossary-android + capability-matrix pins +1)"
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "BASELINE_18 freshness comment (audit-trail; BASELINE_9 array unchanged)"
      contains: "BASELINE_18"
  key_links:
    - from: "scripts/validation/v1.14-milestone-audit.mjs"
      to: "scripts/validation/v1.14-audit-allowlist.json"
      via: "readFile sidecar repoint (the CI path-match needle)"
      pattern: "v1\\.14-audit-allowlist\\.json"
---

<objective>
Ship Plan 112-02: **HARN-01 Atom 1** — the 12th Path-A milestone-audit in the lineage v1.4 → v1.4.1 → … → v1.13 → **v1.14**. ONE indivisible commit, EXACTLY 3 files.

Files (per locked D-01a / D-04):
- `v1.14-milestone-audit.mjs` — Path-A copy of `v1.13-milestone-audit.mjs`; relabel v1.13→v1.14 in the header/sidecar/usage lines + the functional sidecar `readFile` line; **C1-C16 inherited verbatim EXCEPT the two MANDATORY C5/C10 threshold bumps** (`> 60` → `> 90`, D-01a); self-test 9/9 preserved; NO new checks.
- `v1.14-audit-allowlist.json` — Path-A copy of `v1.13-audit-allowlist.json` with the D-04 pin repoints (glossary-android +1 across the board; capability-matrix +1). The frozen predecessor sidecar is NOT touched.
- BASELINE_18 freshness comment appended in `regenerate-supervision-pins.mjs` after line 466, mirroring the BASELINE_17 block, anchored to the pre-Atom-1 HEAD.

Purpose: ship the v1.14 harness core as a clean revertable atom that runs GREEN on its OWN 90-day corpus. **D-01a is non-optional** — inheriting C5/C10 verbatim would make v1.14's own audit FAIL its own corpus. **V113 does NOT ride Atom 1** — it rides Atom 2 (112-03), mirroring the Phase-100 V112-rides-Atom-2 divergence.
Output: `v1.14-milestone-audit.mjs`, `v1.14-audit-allowlist.json`, BASELINE_18 comment; 1 commit (3 files).

**HARD invariant (D-00a):** do NOT edit any `v1.4-v1.13-milestone-audit.mjs`, `*-audit-allowlist.json`, or `audit-harness-*-integrity.yml`. The new v1.14 harness + new v1.14 sidecar are in-scope; editing them (including the C5/C10 bump) is free because they are NEW files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-CONTEXT.md
@.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-RESEARCH.md
@.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author v1.14-milestone-audit.mjs (Path-A + C5/C10 90d bump) + v1.14-audit-allowlist.json (D-04 repoint)</name>
  <files>scripts/validation/v1.14-milestone-audit.mjs, scripts/validation/v1.14-audit-allowlist.json</files>
  <read_first>
    - scripts/validation/v1.13-milestone-audit.mjs (PATH-A SOURCE — copy verbatim; note the header/sidecar/usage relabel sites, the functional `readFile('scripts/validation/v1.13-audit-allowlist.json')` line, the C5 block `if (diffDays > 60)` at line 406 + sentinel at line 401, the C10 block `if (diffDays > 60)` at line 542 + sentinel at line 537, and the `join(process.cwd(), relPath)` path resolution at line 51)
    - scripts/validation/v1.13-audit-allowlist.json (PATH-A SOURCE for the sidecar — copy verbatim then apply the +1 repoints; note _glossary-android.md pins in safetynet_exemptions/supervision_exemptions/c7_knox_allowlist/c9_exemptions and android-capability-matrix.md pins in c9_exemptions/supervision_exemptions)
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-RESEARCH.md (§"TARGET 2" exact C5/C10 line numbers + sentinel-preservation; §"TARGET 4" the complete per-section pin repoint tables for both files + the zero-pin confirmation for index/quick-ref/common-issues)
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-PATTERNS.md (§"v1.14-milestone-audit.mjs" 4-token relabel + 2 threshold bumps; §"v1.14-audit-allowlist.json" the two repoint tables with v1.13→v1.14 pin columns)
    - docs/_glossary-android.md, docs/reference/android-capability-matrix.md (the LIVE corpus the repointed pins must resolve against — spot-confirm the +1 shifts before writing)
  </read_first>
  <action>
    **File 1 — `v1.14-milestone-audit.mjs`:** Copy `v1.13-milestone-audit.mjs` verbatim, then apply ONLY these deltas (per 112-PATTERNS §"v1.14-milestone-audit.mjs" + 112-RESEARCH §"TARGET 2"):
    - Relabel v1.13→v1.14 in: (a) the top-line lineage/header comment, (b) the sidecar-reference comment, (c) the Usage comment line, (d) the FUNCTIONAL sidecar `readFile` line → `readFile('scripts/validation/v1.14-audit-allowlist.json')` (this exact string is the line the CI path-match job greps). No stale `v1.13-audit-allowlist` reference may remain.
    - **C5 bump (D-01a, MANDATORY):** at the C5 block (~line 406) change `if (diffDays > 60)` → `if (diffDays > 90)` and update the violation-reason string `(>60)` → `(>90)`; update the C5 block header comment from the "Phase 34 D-14 Android cadence" wording to reference the v1.14 90-day cadence (discuss-flag #7 / 101-CONTEXT 90-day lock). PRESERVE the `if (lvMatch[1] === '1970-01-01') continue;` template-sentinel line (~401) unchanged.
    - **C10 bump (D-01a, MANDATORY):** at the C10 block (~line 542) change `if (diffDays > 60)` → `if (diffDays > 90)` and `(>60)` → `(>90)`. PRESERVE the C10 template-sentinel line (~537) unchanged.
    - Everything else verbatim: the `checks` array stays C1-C16 (NO new check — v1.14 has no corpus rename), the self-test block stays 9/9, all helpers + the `join(process.cwd(), relPath)` resolver + runner loop unchanged.

    **File 2 — `v1.14-audit-allowlist.json`:** Copy `v1.13-audit-allowlist.json` verbatim, update the header fields (`generated` → authoring date; `phase` → the v1.14 close phase, mirroring the v1.13 sidecar's field TYPE — bare number `112` if v1.13 used a number, else the 112 slug; carry `schema_version` verbatim), then apply the D-04 pin repoints EXACTLY per the 112-RESEARCH §"TARGET 4" / 112-PATTERNS tables:
    - `docs/_glossary-android.md` — **+1 to EVERY pin** (Phase 101 banner at line 14 sits above all pins): safetynet_exemptions 186→187, 201→202; supervision_exemptions 17→18, 50→51, 70→71, 80→81, 82→83, 83→84, 182→183, 196→197, 199→200; c7_knox_allowlist 122→123, 124→125, 126→127, 198→199; c9_exemptions 203→204.
    - `docs/reference/android-capability-matrix.md` — **+1 to all pins** (Phase 109 row before line 54): c9_exemptions 54→55; supervision_exemptions 88→89, 90→91, 91→92, 93→94, 97→98, 98→99.
    - Do NOT add/repoint any pin for `docs/index.md`, `docs/quick-ref-l1.md`, `docs/common-issues.md` (ZERO pins — Phase 110's edits need no repoint). Carry every other array (cope_banned_phrases, c11_ops_exemptions, c13_broken_link_allowlist, c13_rotting_external, c16_missing_endpoint_exemptions, ci_3_managed_apple_id count-entries) byte-identical.

    Verify the harness runs GREEN against the LIVE corpus before proceeding (C5/C10 pass on 90d; C2/C7/C9 pass on the repointed pins).
  </action>
  <verify>
    <automated>node scripts/validation/v1.14-milestone-audit.mjs --self-test && echo "---DEFAULT---" && node scripts/validation/v1.14-milestone-audit.mjs | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/v1.14-milestone-audit.mjs --self-test` exits 0 with "9 passed, 0 failed".
    - `node scripts/validation/v1.14-milestone-audit.mjs` (default) exits 0 and reports 15 PASS / 0 FAIL / 0 SKIP against the live v1.14 corpus (capture the exact PASS count in SUMMARY; C2/C7/C9 line-pin + C5/C10 freshness all PASS).
    - The harness contains `if (diffDays > 90)` at BOTH the C5 and C10 blocks (`grep -c "diffDays > 90" scripts/validation/v1.14-milestone-audit.mjs` == 2) and NO `diffDays > 60` remains (`grep -c "diffDays > 60" scripts/validation/v1.14-milestone-audit.mjs` == 0); both `1970-01-01` sentinel lines are present (`grep -c "1970-01-01" scripts/validation/v1.14-milestone-audit.mjs` == 2).
    - `grep -v '^#' scripts/validation/v1.14-milestone-audit.mjs | grep -c "v1.14-audit-allowlist.json"` >= 1; `grep -c "v1.13-audit-allowlist" scripts/validation/v1.14-milestone-audit.mjs` == 0.
    - `node -e "const j=require('./scripts/validation/v1.14-audit-allowlist.json'); JSON.stringify(j)"` exits 0 (valid JSON); the sidecar contains the repointed pins (e.g. 187, 204, 55, 99 present) and NOT the stale v1.13 pins where they shifted.
  </acceptance_criteria>
  <done>v1.14-milestone-audit.mjs authored (relabel + C5/C10 90d bump, sentinels preserved, self-test 9/9, default 15/0/0 on live corpus); v1.14-audit-allowlist.json repointed per D-04. NOT yet committed (commit in Task 2 with BASELINE_18 as one atom).</done>
</task>

<task type="auto">
  <name>Task 2: Insert BASELINE_18 comment in regenerate-supervision-pins.mjs + commit Atom 1 (exactly 3 files)</name>
  <files>scripts/validation/regenerate-supervision-pins.mjs</files>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs (the file being MODIFIED — the BASELINE_17 block at lines 460-466 is the mirror template; the `const BASELINE_9 = [` at line 467 must NOT move or change)
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-RESEARCH.md (§"TARGET 5 — BASELINE_18" the exact 7-line block text + insertion after line 466 + the anchor rule: the [ATOM_1_SHA] slot is filled with the pre-Atom-1 HEAD, NOT a future close SHA)
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-01-SUMMARY.md (read at execution time — the pre-Atom-1 HEAD is the 112-01 Wave-0 commit SHA; recapture via git rev-parse to be safe)
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-PATTERNS.md (§"regenerate-supervision-pins.mjs" — 7-line additive comment, BASELINE_9 not moved)
  </read_first>
  <action>
    Capture the pre-Atom-1 anchor: run `git rev-parse --short HEAD` (this is the 112-01 Wave-0 commit — the KNOWN-PAST HEAD before Atom 1 lands; this is the "Atom 1 constants lock" anchor, mirroring how BASELINE_17 anchored to the pre-Atom-1 HEAD `ea24467`, NOT the Atom-1 commit itself — this resolves the chicken-and-egg).

    Insert a 7-line BASELINE_18 comment block immediately AFTER line 466 (the BASELINE_17 forward-pointer line) and BEFORE `const BASELINE_9 = [`, mirroring the BASELINE_17 block format exactly (per 112-RESEARCH §"TARGET 5"). The block records:
    - `BASELINE_18 refreshed <today> (Phase 112 Plan 112-02): closes BASELINE_17 v1.13 carry-over per HARN-01 contract (REQUIREMENTS.md + ROADMAP.md Phase 112 SC#1)`;
    - `v1.14 line positions verified against HEAD <pre-Atom-1 short SHA>` (REPLACE the [ATOM_1_SHA] placeholder with the real anchor SHA captured above — do NOT leave a literal placeholder and do NOT anchor to a future Phase-112 close SHA);
    - `BASELINE_9 entries above remain unchanged -- Phase 112 does NOT alter the line-coord array`;
    - resolution path mentioning BASELINE_19 at the next milestone close (`... -> v1.13 -> BASELINE_17 -> v1.14 -> BASELINE_18`).
    Do NOT alter the BASELINE_9 array (comment-additions only).

    Then stage all 3 Atom-1 files (`v1.14-milestone-audit.mjs` + `v1.14-audit-allowlist.json` from Task 1 + `regenerate-supervision-pins.mjs`) and commit as ONE indivisible atom. V113 is NOT added here (it rides Atom 2 in 112-03) — do NOT touch `_lib/frozen-at-close.mjs`. Commit message: `feat(112-02): v1.14 harness-core Path-A + C5/C10 90d — HARN-01 (atomic SC#1 Atom 1)`.
  </action>
  <verify>
    <automated>echo "baseline18=$(grep -c BASELINE_18 scripts/validation/regenerate-supervision-pins.mjs)"; echo "placeholder=$(grep -c 'ATOM_1_SHA' scripts/validation/regenerate-supervision-pins.mjs)"; echo "atom1files=$(git show --stat HEAD --format='' | grep -cE 'v1\.14-milestone-audit\.mjs|v1\.14-audit-allowlist\.json|regenerate-supervision-pins\.mjs')"; echo "frozentouched=$(git show --stat HEAD --format='' | grep -cE 'v1\.(4|4\.1|5|6|7|8|9|10|11|12|13)-milestone-audit\.mjs|v1\.(4|4\.1|5|6|7|8|9|10|11|12|13)-audit-allowlist\.json|frozen-at-close')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BASELINE_18" scripts/validation/regenerate-supervision-pins.mjs` >= 1; the block contains a real 7-char SHA anchor and NO literal placeholder (`grep -c "ATOM_1_SHA" scripts/validation/regenerate-supervision-pins.mjs` == 0).
    - `git diff HEAD~1 HEAD -- scripts/validation/regenerate-supervision-pins.mjs` shows comment-additions only; the BASELINE_9 line-coord array is byte-unchanged.
    - COMMIT message is exactly: `feat(112-02): v1.14 harness-core Path-A + C5/C10 90d — HARN-01 (atomic SC#1 Atom 1)`.
    - The commit touches EXACTLY 3 files (verify prints `atom1files=3`): `v1.14-milestone-audit.mjs`, `v1.14-audit-allowlist.json`, `regenerate-supervision-pins.mjs`. `_lib/frozen-at-close.mjs` is NOT touched (V113 rides Atom 2) and NO predecessor frozen surface appears (verify prints `frozentouched=0`).
  </acceptance_criteria>
  <done>BASELINE_18 comment present with a real past-SHA anchor; BASELINE_9 array unchanged; Atom 1 (exactly 3 files) committed; V113 deliberately absent; predecessor frozen surfaces untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — local CLI tooling) | Atom 1 is tooling-ONLY: a new validation `.mjs` harness + new sidecar JSON + a comment-only edit to a generator script. No user input, no network endpoints, no auth, no secrets, no runtime data. The harness's own `join(process.cwd(), relPath)` reads only in-repo doc paths. |

## STRIDE Threat Register

ASVS L1 review found **no high-severity items** — the realistic threat surface for this close/tooling phase is harness-integrity (a false-green or born-stale harness), not web/app threats.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02-T | Tampering | The C5/C10 bump silently mutating other checks, or a born-stale sidecar (naive copy without the D-04 repoint) false-failing C2/C7/C9 | mitigate | Acceptance gate asserts self-test 9/9 + default 15/0/0 on live corpus + exactly 2× `diffDays > 90` and 0× `diffDays > 60` + both sentinels preserved; a born-stale sidecar fails the live default run. |
| T-112-02-I | Integrity | A stale `v1.13-audit-allowlist` reference left in the v1.14 harness (sidecar points at the wrong/frozen file) | mitigate | Acceptance gate asserts `grep -c "v1.13-audit-allowlist"` == 0 and the v1.14 sidecar repoint present (comment-filtered grep). |
| T-112-02-D | Integrity/Tampering | A predecessor frozen surface (v1.4-v1.13 audit/sidecar/workflow) mutated while authoring the new v1.14 files | mitigate | Commit-stat grep asserts `frozentouched=0`; the full 32-surface byte-unchanged gate runs at close (112-05). |
| T-112-02-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | NO package installs (pure Path-A copy of in-tree scripts; no new deps). No Package Legitimacy Gate required. |
</threat_model>

<verification>
- `node scripts/validation/v1.14-milestone-audit.mjs --self-test` → 9 passed, 0 failed (exit 0).
- `node scripts/validation/v1.14-milestone-audit.mjs` → 15 PASS / 0 FAIL / 0 SKIP on live corpus (C2/C7/C9 + C5/C10 green; capture exact count).
- C5/C10 = `diffDays > 90` (×2), no `> 60`; both 1970-01-01 sentinels preserved.
- v1.14 sidecar valid JSON with the +1 repoints; no stale v1.13-audit-allowlist reference in the harness.
- BASELINE_18 comment present with a real past-SHA anchor; BASELINE_9 array byte-unchanged.
- Atom 1 = exactly 3 files, one commit; V113 absent; predecessor frozen surfaces untouched.
</verification>

<success_criteria>
- ROADMAP SC#1 satisfied: Atom 1 ships as ONE indivisible 3-file commit — `v1.14-milestone-audit.mjs` (Path-A, C1-C16, C5/C10 90d) + `v1.14-audit-allowlist.json` (repointed) + BASELINE_18 in `regenerate-supervision-pins.mjs`.
- D-01a honored: C5/C10 bumped to 90d so the v1.14 audit is GREEN on its OWN corpus.
- D-04 honored: glossary-android + capability-matrix pins +1; index/quick-ref/common-issues untouched.
- V113 deliberately absent (rides Atom 2); pre-Atom-1 anchor SHA recorded for the BASELINE_18 audit trail.
</success_criteria>

<output>
Create `.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-02-SUMMARY.md` when done. Record: the Atom-1 COMMIT SHA, the pre-Atom-1 anchor SHA used for BASELINE_18, the self-test/default PASS counts observed (self-test 9/9, default N/0/0), confirmation the C5/C10 90d bump is present, and confirmation V113 was deliberately not added.
</output>
</content>
