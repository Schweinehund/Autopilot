---
phase: 112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl
plan: 04
type: execute
wave: 3
depends_on: ["112-03"]
files_modified:
  - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-04-AUDIT-RESULTS.md
autonomous: false
requirements: [HARN-03]
must_haves:
  truths:
    - "Atom 2 is confirmed on origin/master (pre-flight ordering gate) before any axis runs — the v1.14 workflow's check-phase-101..112 jobs FAIL (not skip) if absent from the dispatched ref"
    - "Axis 1 (local physical independence): a fresh git clone --no-hardlinks into $env:TEMP\\v1.14-audit-<rand8> runs ONLY the 12 leaf validators (v1.14-milestone-audit.mjs --verbose + --self-test + check-phase-101..111), MUST cd INTO the clone (harness resolves via process.cwd()), with HEAD==source assertion and zero-orphan cleanup"
    - "Axis 2 (cross-OS): gh workflow run audit-harness-v1.14-integrity.yml --ref master completes; per-job conclusions + Linux chain timing captured"
    - "Axis 3 (logical/context independence): the SAME fresh zero-context sub-agent serves Axis 1 + Axis 3 (one dispatch, two dimensions)"
    - "BOTH chain validators (apex check-phase-112 [48..111] AND continuity check-phase-95 [48..94]) are Linux-GHA SOLE-authoritative — both cascade on cold Windows (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 now at depth [48..111], 12 deeper than v1.13's [48..99])"
    - "Cross-OS PASS/FAIL/SKIP EXACT MATCH on the 12 leaf rows + Linux-authoritative counts on the 2 chain rows recorded in 112-04-AUDIT-RESULTS.md; with the NESTED-guard in place the apex is GREEN on both OSes for the corpus dimension (a GREEN match, not RED/RED)"
    - "The predecessor v1.12/v1.13 standalone-CI RED (60d threshold vs 90d corpus) is noted as an accepted, path-filter-scoped condition to be documented at 112-05 — NOT treated as a re-audit failure"
  artifacts:
    - path: ".planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-04-AUDIT-RESULTS.md"
      provides: "3-axis terminal re-audit evidence with 14-row cross-OS table"
      contains: "EXACT MATCH"
      min_lines: 90
  key_links:
    - from: ".planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-04-AUDIT-RESULTS.md"
      to: ".github/workflows/audit-harness-v1.14-integrity.yml"
      via: "Axis 2 gh workflow run --ref master"
      pattern: "audit-harness-v1.14-integrity"
---

<objective>
Ship Plan 112-04: **HARN-03 part 1** — the 3-axis terminal re-audit (artifact-only commit). Replicate the Phase-100 (100-03) recipe, v1.14-relabeled, across the **14-row** cross-OS set (per D-04), and record cross-OS EXACT MATCH evidence in `112-04-AUDIT-RESULTS.md`.

The 14-row cross-OS set (per D-04 — 12 leaf + 2 chain; counts to CAPTURE):
| Validator | Type | Windows Axis-1/3 | Linux Axis-2 | Authority |
|-----------|------|------------------|--------------|-----------|
| `v1.14-milestone-audit.mjs` (--verbose + --self-test) | leaf | YES | YES | cross-OS EXACT MATCH |
| `check-phase-101.mjs` … `check-phase-111.mjs` (11) | leaf | YES | YES | cross-OS EXACT MATCH |
| `check-phase-95.mjs` (continuity, CHAIN [48..94]) | chain | NO (cascades) | YES | **Linux sole-authoritative** |
| `check-phase-112.mjs` (apex, CHAIN [48..111], 64 entries) | chain | NO (cascades) | YES | **Linux sole-authoritative** |

- **Axis 1 (local physical independence):** ONE fresh sub-agent runs `git clone --no-hardlinks D:\claude\Autopilot $env:TEMP\v1.14-audit-<rand8>`, asserts cloned HEAD == source HEAD, `cd`s INTO the clone, runs ONLY the 12 leaf validators, captures exit/PASS/FAIL/SKIP, then `Remove-Item -Recurse -Force` + asserts zero orphans.
- **Axis 2 (cross-OS):** main session `gh workflow run audit-harness-v1.14-integrity.yml --ref master`; capture run URL + per-job conclusions + Linux chain timing. BOTH chain rows come from Linux GHA sole-authoritatively.
- **Axis 3 (logical/context independence):** the SAME sub-agent as Axis 1 — zero-context-carryover. ONE dispatch, two dimensions (do NOT spawn two agents).

Purpose: prove the v1.14 harness produces identical PASS/FAIL/SKIP counts across a clean Windows clone and the Linux GHA runner on the 12 leaf rows. **D-04 OS split:** BOTH `check-phase-112` (apex [48..111]) AND continuity `check-phase-95` (CHAIN [48..94]) cascade on cold Windows (O(n²) deep-nest, depth-monotone-worse than v1.13's [48..99]) → both counts come EXCLUSIVELY from Linux GHA. With the NESTED-guard (112-01) in place, the apex is GREEN on both OSes for the corpus dimension.
Output: `112-04-AUDIT-RESULTS.md` (artifact-only commit).

**HARD pre-flight ordering gate (D-03):** Plan 112-03 (Atom 2) MUST already be committed AND on `origin/master`. The v1.14 workflow's check-phase-101..112 jobs FAIL (not skip) if absent from the dispatched ref.
**cwd trap (from the adversarial-review referee's false-negative):** `v1.14-milestone-audit.mjs` resolves all paths via `join(process.cwd(), relPath)`. You MUST `cd` INTO the clone before running any validator — running from the main tree reads the WRONG corpus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-CONTEXT.md
@.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-RESEARCH.md
@.planning/milestones/v1.13-phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-03-AUDIT-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight ordering gate + Axis 2 cross-OS Linux GHA dispatch</name>
  <files>.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-04-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/milestones/v1.13-phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-03-AUDIT-RESULTS.md (the EXECUTED v1.13 recipe — Axis-2 gh command, pre-flight 3-part gate, cross-OS table format, deep-nest firing evidence, both-chains-Linux-authoritative accounting)
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-RESEARCH.md (§"TARGET 7" — the 3-axis format, Axis-2 command, 14-row cross-OS table, expected leaf counts, WINDOWS-CLONE-DEEPNEST at [48..111])
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-03-SUMMARY.md (the leaf counts captured at Atom-2 authoring time — the EXACT MATCH targets for Axis 1)
  </read_first>
  <action>
    **Pre-flight 3-part gate (HARD — abort if any fails):**
    1. `git log origin/master --oneline -1` shows the Atom-2 commit (`feat(112-03): … HARN-02 (atomic SC#2 Atom 2)`) on origin/master.
    2. `gh auth status` reports authenticated (Schweinehund).
    3. `gh workflow list` shows `Audit Harness v1.14 Integrity` with `state: active`.
    If the Atom-2 commit is NOT on origin/master, STOP — Plan 112-03 must be pushed first (the workflow's check-phase-101..112 jobs FAIL, not skip, if absent).

    **Axis 2 dispatch:** Run `gh workflow run audit-harness-v1.14-integrity.yml --ref master`. Poll to completion (`gh run watch` or `gh run list --workflow=audit-harness-v1.14-integrity.yml -L1` then `gh run view <id>`). Capture: run URL, per-job conclusions (parse / path-match / harness-run / linux-chain-ubuntu-latest / check-phase-101 … check-phase-112 / rotting-external-quarterly [expect SKIP — negative control] / pin-helper-advisory), and the Linux chain timing from the `CHAIN_TIMING_LINUX` `::notice`. **BOTH chain counts are Linux-sole-authoritative:** the apex check-phase-112 [48..111] count from `linux-chain-ubuntu-latest`, AND the continuity check-phase-95 [48..94] count (capture on Linux — it cascades on Windows). Record the OS split explicitly: both chain validators Linux-only; only the 12 leaf rows run on Windows Axis-1.

    Begin authoring `112-04-AUDIT-RESULTS.md` (Path-A from 100-03-AUDIT-RESULTS.md): frontmatter (clone_path_pattern `v1.14-audit-<rand8>`, gha_workflow_run URL, cross_os_exact_match) + the Axis-2 section with the captured Linux counts for all 14 rows. Note the predecessor v1.12/v1.13 standalone-CI RED (60d vs 90d corpus) as an ACCEPTED, path-filter-scoped condition (documented at 112-05) — NOT a re-audit failure. Leave the Axis-1/Axis-3 Windows leaf columns for Task 2.
  </action>
  <verify>
    <automated>gh run list --workflow=audit-harness-v1.14-integrity.yml -L1 --json conclusion,status,url 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight: `git log origin/master --oneline -1` contains the Atom-2 commit; `gh auth status` authenticated; v1.14 workflow `state: active`.
    - `gh workflow run audit-harness-v1.14-integrity.yml --ref master` dispatched; the run completes with linux-chain-ubuntu-latest + check-phase-101..112 jobs SUCCESS (or documented exact counts).
    - `rotting-external-quarterly` job conclusion = SKIP (negative-control confirmed — cron-only `if:` guard).
    - Both Linux chain counts captured (apex check-phase-112 [48..111] + continuity check-phase-95 [48..94], both sole-authoritative); 112-04-AUDIT-RESULTS.md frontmatter records the run URL + `cross_os_exact_match` field.
    - The predecessor v1.12/v1.13 standalone-CI RED is noted as an accepted condition (not counted as a re-audit failure).
  </acceptance_criteria>
  <done>Pre-flight gate passed; Axis-2 Linux GHA run dispatched and completed; per-job conclusions + both Linux chain counts captured into the in-progress 112-04-AUDIT-RESULTS.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Axis 1 + Axis 3 fresh-clone sub-agent (12 leaf rows only — local physical + logical independence)</name>
  <files>.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-04-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/milestones/v1.13-phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-03-PLAN.md (the EXECUTED v1.13 fresh-clone checkpoint recipe — Task 2 how-to-verify: rand generation, clone, HEAD==source assertion, leaf-only run, zero-orphan cleanup, table finalization)
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-RESEARCH.md (§"TARGET 7 — Axis 1" the cwd-trap warning + the 12-leaf validator set + the WINDOWS-CLONE-DEEPNEST [48..111] both-chains-Linux-authoritative note)
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-03-SUMMARY.md (the per-validator leaf counts captured at Atom-2 authoring time — the EXACT MATCH targets for the 12 Windows leaf rows)
  </read_first>
  <what-built>
    The v1.14 audit harness (Atom 1 + Atom 2) is committed and on origin/master, and the Axis-2 Linux GHA run has produced cross-OS counts for all 14 rows. This checkpoint runs the Axis-1/Axis-3 fresh-clone independence recipe — a fresh, zero-context PowerShell environment that clones the repo with its own `.git/`, `cd`s INTO the clone, runs ONLY the 12 leaf validators (the 2 chain validators are deliberately NOT run on Windows — both cascade at depth [48..111]), and proves the local Windows leaf counts match Linux.
  </what-built>
  <action>
    Run the fresh-clone recipe in a clean PowerShell session (zero context carryover — this serves BOTH Axis 1 physical independence AND Axis 3 logical independence; ONE session, two dimensions — do NOT spawn two): generate `$rand` (8 chars `[0-9a-z]`); `git clone --no-hardlinks D:\claude\Autopilot $env:TEMP\v1.14-audit-$rand`; assert cloned HEAD == source HEAD; `cd` INTO the clone; run ONLY the 12 leaf validators and capture each `Result: N PASS, N FAIL, N SKIPPED`; clean up with `Remove-Item -Recurse -Force` and assert zero orphans; then finalize the 14-row table. Full step-by-step recipe is in `<how-to-verify>` below. Do NOT run check-phase-95.mjs or check-phase-112.mjs on Windows (both cascade — Linux-sole-authoritative).
  </action>
  <how-to-verify>
    Run this fresh-clone recipe in a clean PowerShell session (zero context carryover — serves BOTH Axis 1 physical + Axis 3 logical independence; ONE session, two dimensions — do NOT spawn two):

    1. Generate `$rand` = 8 chars from charset `[0-9a-z]`. Set `$dest = "$env:TEMP\v1.14-audit-$rand"`.
    2. `git clone --no-hardlinks D:\claude\Autopilot $dest` (auditor independence — own `.git/`, NOT a worktree; permitted under `use_worktrees:false`).
    3. Assert cloned HEAD == source HEAD: compare `git -C $dest rev-parse HEAD` to `git -C D:\claude\Autopilot rev-parse HEAD` — MUST be equal.
    4. `cd $dest` (CRITICAL — the harness resolves paths via `process.cwd()`, NOT `__dirname`; running from the main tree reads the WRONG corpus and produces a false result). Then run the **12 leaf validators**: `node scripts/validation/v1.14-milestone-audit.mjs --verbose` (and `--self-test`), then `node scripts/validation/check-phase-101.mjs` through `check-phase-111.mjs`. Capture each exit code + the `Result: N PASS, N FAIL, N SKIPPED` line. **Do NOT run check-phase-95.mjs OR check-phase-112.mjs from the Windows clone** — BOTH are chain validators and cascade to O(n²) deep-nest at [48..94] / [48..111] (multi-minute hang, many node procs); their counts are Linux-GHA sole-authoritative.
    5. Expected (must match Linux from Axis 2 on the 12 leaf rows): harness 15/0/0 + self-test `9 passed, 0 failed`; check-phase-101..111 each matching their Atom-2 counts from 112-03-SUMMARY.
    6. Cleanup: `Remove-Item -Recurse -Force $dest`; assert `(Get-ChildItem $env:TEMP -Filter "v1.14-audit-*" -Directory).Count -eq 0` (zero orphans).
    7. Finalize `112-04-AUDIT-RESULTS.md`: complete the 14-row cross-OS table (Windows Axis 1+3 column populated for the 12 leaf rows ONLY; the 2 chain rows show "Windows: N/A — cascades, Linux-sole-authoritative" with the Linux count from Axis 2), mark each leaf row EXACT MATCH, set `cross_os_exact_match: true`, and record the clone HEAD, `$rand`, and zero-orphan assertion. Document `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` as the reason BOTH chain rows are Linux-sole-authoritative (carry-forward note for DEFERRED-CLEANUP, depth → [48..111], and the low-risk OOM-vs-timeout-flip caveat unmeasured at the new depth).

    Confirm: cloned HEAD matched source, all 12 leaf counts matched the Linux Axis-2 counts (EXACT MATCH), and the temp clone was removed with zero orphans.
  </how-to-verify>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-04-AUDIT-RESULTS.md','utf8'); if(!/cross_os_exact_match:\s*true/.test(f)){console.error('cross_os_exact_match not true');process.exit(1)} ['v1.14-audit-','EXACT MATCH'].forEach(s=>{if(!f.includes(s)){console.error('missing '+s);process.exit(1)}}); console.log('axis-1/3 recorded')"</automated>
  </verify>
  <resume-signal>Type "approved" once the fresh-clone recipe shows cross-OS EXACT MATCH on the 12 leaf rows (harness 15/0/0 + self-test 9/9 + check-phase-101..111 matching their Atom-2 counts), both chain rows recorded Linux-sole-authoritative, WINDOWS-CLONE-DEEPNEST documented at [48..111], zero orphans, and 112-04-AUDIT-RESULTS.md finalized — or describe any count divergence.</resume-signal>
  <done>Axis-1/Axis-3 fresh-clone recipe executed in a zero-context session; `cd`-INTO-clone honored; cloned HEAD == source; all 12 leaf counts EXACT MATCH the Linux Axis-2 counts; both chain rows recorded Linux-sole-authoritative at [48..111]; zero orphans; 112-04-AUDIT-RESULTS.md finalized with cross_os_exact_match: true.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the audit-results artifact (artifact-only)</name>
  <files>.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-04-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-04-AUDIT-RESULTS.md (the artifact finalized in Task 2)
    - .planning/milestones/v1.13-phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-03-AUDIT-RESULTS.md (Path-A structure reference)
  </read_first>
  <action>
    Verify `112-04-AUDIT-RESULTS.md` contains the complete 14-row cross-OS table (12 leaf rows marked EXACT MATCH with both columns populated; 2 chain rows marked Linux-sole-authoritative with the Linux count), `cross_os_exact_match: true`, the Axis-2 run URL, the Axis-1 clone HEAD + `$rand` + zero-orphan assertion, the `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` both-chains-Linux-authoritative note (depth [48..111]), and the accepted predecessor-standalone-CI-RED note. Then commit ONLY this file (artifact-only commit — no validators, no config changed). Commit message: `docs(112-04): HARN-03 3-axis terminal re-audit results (artifact-only)`.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-04-AUDIT-RESULTS.md','utf8'); ['EXACT MATCH','check-phase-101','check-phase-111','check-phase-112','check-phase-95','WINDOWS-CLONE-DEEPNEST-TIMEOUT-01','48..111','audit-harness-v1.14-integrity'].forEach(s=>{if(!f.includes(s)){console.error('AUDIT-RESULTS missing '+s);process.exit(1)}}); console.log('audit-results ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `112-04-AUDIT-RESULTS.md` contains a 14-row cross-OS table with EXACT MATCH (12 leaf rows), `check-phase-95`, `check-phase-112`, `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` at depth `48..111`, and `audit-harness-v1.14-integrity`.
    - The table records BOTH chain rows (95 + 112) as Linux-sole-authoritative (Windows N/A — cascades); the predecessor v1.12/v1.13 standalone-CI RED is recorded as accepted.
    - COMMIT message is exactly: `docs(112-04): HARN-03 3-axis terminal re-audit results (artifact-only)`.
    - The commit touches ONLY `112-04-AUDIT-RESULTS.md` (artifact-only; no validators/config modified).
  </acceptance_criteria>
  <done>112-04-AUDIT-RESULTS.md finalized with cross-OS EXACT MATCH on the 12 leaf rows + both chain rows Linux-sole-authoritative at [48..111], committed artifact-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Windows ↔ GitHub Actions Linux runner | The re-audit deliberately crosses the OS boundary to prove count-identity on the leaf rows. No untrusted input crosses — both axes run the same in-repo validators against the same `master` ref. |
| source repo → fresh TEMP clone | Axis 1 clones into `$env:TEMP` with `--no-hardlinks` (own `.git/`), `cd`s in, runs read-only validators, then removes the clone. No write-back to the source repo. |

## STRIDE Threat Register

ASVS L1 review found **no high-severity items**. This is a read-only verification plan; the realistic threat surface is honest-accounting (a milestone marked Validated without a genuine cross-OS EXACT MATCH, or a cwd false-negative).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-04-I | Integrity | Axis-2 dispatched against a ref missing the v1.14 jobs (false PASS/SKIP) | mitigate | HARD pre-flight gate: Atom 2 confirmed on origin/master + workflow `state: active` before dispatch; jobs FAIL (not skip) if absent. |
| T-112-04-R | Repudiation | cwd false-negative — running the harness from the main tree instead of the clone (the referee's documented error) | mitigate | Recipe asserts `cd $dest` before any validator; harness resolves via `process.cwd()`; AUDIT-RESULTS records the clone HEAD and that cwd was inside the clone. |
| T-112-04-T | Tampering | Fresh clone left as orphan in TEMP (stale-state leakage) | mitigate | Recipe asserts `Get-ChildItem $env:TEMP -Filter "v1.14-audit-*" -Directory` count == 0 after `Remove-Item -Recurse -Force`. |
| T-112-04-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | NO installs — `git clone` + `node` (in-tree scripts) + `gh` only. No Package Legitimacy Gate required. |
</threat_model>

<verification>
- Pre-flight gate passed (Atom 2 on origin/master + gh auth + workflow active).
- Axis 2 Linux GHA run completed; BOTH chain counts (apex check-phase-112 + continuity check-phase-95) Linux-sole-authoritative.
- Axis 1/3 fresh clone: `cd`-into-clone honored; HEAD==source; all 12 leaf counts EXACT MATCH Linux; zero orphans.
- 112-04-AUDIT-RESULTS.md records cross_os_exact_match: true + accepted predecessor-standalone-RED note; committed artifact-only.
</verification>

<success_criteria>
- ROADMAP SC#3 (part 1) satisfied: 3-axis terminal re-audit completes (Axis 1 fresh clone + Axis 2 Linux GHA + Axis 3 zero-context sub-agent); cross-OS PASS/FAIL/SKIP counts EXACT MATCH across the 12 leaf rows; both chain rows Linux-authoritative.
- D-04 OS split documented: BOTH chain validators (95 + 112) Linux-GHA sole-authoritative (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 at [48..111]).
- 112-04-AUDIT-RESULTS.md committed artifact-only, consumed by the 112-05 close-gate.
</success_criteria>

<output>
Create `.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-04-SUMMARY.md` when done. Record: Axis-2 run URL + both Linux chain counts (apex-112 + continuity-95), Axis-1 clone HEAD + `$rand` + zero-orphan confirmation, the 14-row outcome (12 leaf EXACT MATCH + 2 chain Linux-authoritative), the WINDOWS-CLONE-DEEPNEST [48..111] note, the accepted predecessor-standalone-RED note, and the audit-results COMMIT SHA (the `audit_results_sha` for the 112-05 close-gate frontmatter).
</output>
</content>
