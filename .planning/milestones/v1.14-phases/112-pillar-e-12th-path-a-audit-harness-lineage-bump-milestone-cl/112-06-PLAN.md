---
plan: 112-06
phase: 112
wave: 5
depends_on: ["112-03"]
autonomous: true
requirements: [HARN-02, HARN-03]
gap_closure: true
files_modified:
  - scripts/validation/check-phase-67.mjs
  - scripts/validation/check-phase-68.mjs
  - scripts/validation/check-phase-69.mjs
  - scripts/validation/check-phase-70.mjs
  - scripts/validation/check-phase-71.mjs
  - scripts/validation/check-phase-72.mjs
  - scripts/validation/check-phase-73.mjs
  - scripts/validation/check-phase-74.mjs
  - scripts/validation/check-phase-82.mjs
  - scripts/validation/check-phase-88.mjs
  - scripts/validation/check-phase-93.mjs
  - scripts/validation/check-phase-49.mjs
  - scripts/validation/check-phase-57.mjs
  - scripts/validation/check-phase-59.mjs
  - scripts/validation/check-phase-63.mjs
  - scripts/validation/check-phase-48.mjs
  - scripts/validation/check-phase-60.mjs
---

# Plan 112-06: Chain-Health Remediation — complete the D-00 doctrine + frozen-aware the 802.1X-drifted assertions

<objective>
The 3-axis terminal re-audit (112-04, Axis 2 Linux GHA run 28621185019) found the v1.14 apex chain
`check-phase-112 [48..111]` is RED: **44 PASS / 22 FAIL / 1 SKIP** on Linux. This is NOT the
Windows-deep-nest artifact — it is real, reproduced on the Linux runner. The 22 failures are all
older validators broken by v1.14's broad 802.1X corpus edits (Phase 101 `_glossary-android.md`
banner + 90d freshness; Phase 109 capability-matrix / index.md rows). The D-00 NESTED-guard shipped
in 112-01 was **incomplete** (it covered only check-phase-95/100), and a latent class of
non-frozen-aware per-phase content assertions was triggered for the first time since v1.6.

This plan greens the apex chain by completing the D-00 doctrine (extend the guard to all predecessor
AUDIT steps) and frozen-aware-converting the drifted content assertions — WITHOUT masking any real
regression and WITHOUT touching the byte-frozen milestone-audit.mjs / sidecars / integrity workflows.
The check-phase-NN validators are LIVING code (Phase 111 `4a2d0b6` + 112-01 already edited them), so
editing them is in-doctrine per D-00a.

**This is the chain-green precondition the close-gate (112-05) requires. HARN-03 cannot pass until
the apex is genuinely green on Linux.**
</objective>

<failure_manifest>
Complete classification (22 = 11 + 6 + 5), from Linux GHA run 28621185019 + local nested reproduction:

**Class A — AUDIT step re-runs a frozen milestone-audit against live corpus (11):**
67, 68, 69, 70, 71, 72, 73, 74 (run v1.7/v1.8-milestone-audit as AUDIT / predecessor-byte-unchanged),
82, 88, 93 (v1.9/v1.10/v1.11 apex AUDIT-HARNESS). Each fails SOLELY on that step (inner cause:
`C2 supervision — 33 un-exempted references, docs/_glossary-android.md:18`). Confirmed sole failure.

**Class B — direct content-drift assertions (6):**
- 49: `V-49-18` C10 frontmatter — Linux glossary 60-day last_verified/review_by (v1.14 = deliberate 90d)
- 57: `V-57-06` docs/index.md Android sub-table row counts (L1 expected 4, got 5 — Phase 109 802.1X row)
- 59: `V-59-07` docs/index.md Linux H2 row counts (L1 expected 4, got 5 — Phase 109 802.1X row)
- 63: `V-63-08` macos-capability-matrix.md byte-unchanged vs baseline blob 732588a… (Phase 109 added Network-Auth row)
- 48: `regenerate-supervision-pins.mjs --self-test` exits 0 (AUDIT-07) — BASELINE vs Phase-101-evolved corpus
- 60: `V-60-10` same regenerate-supervision-pins --self-test

**Class C — cascade via check-phase-48 CHAIN regression-guard (5): 61, 62, 64, 65, 66.**
Auto-resolve once 48 (Class B) is green — no direct edit needed; VERIFY only.
</failure_manifest>

<tasks>

<task id="1" wave="0">
<name>Class A — extend the AUDIT-step NESTED-guard to the 11 predecessor validators</name>
<requirements>HARN-02</requirements>
<read_first>
- scripts/validation/check-phase-95.mjs and check-phase-100.mjs (the 112-01 guard is the EXACT template — the 3-line `if (NESTED) { return {skipped…} }` block on the AUDIT-HARNESS step, reusing the already-declared NESTED const)
- .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-01-SUMMARY.md (guard shape + rationale)
- Each target: scripts/validation/check-phase-{67,68,69,70,71,72,73,74,82,88,93}.mjs — find its AUDIT / AUDIT-HARNESS / predecessor-byte-unchanged step (the one that shells out to a vX.Y-milestone-audit.mjs)
</read_first>
<action>
For each of check-phase-67, 68, 69, 70, 71, 72, 73, 74, 82, 88, 93: locate the step that runs a
`vX.Y-milestone-audit.mjs` via execFileSync/spawn (labeled AUDIT, AUDIT-HARNESS, or
predecessor-byte-unchanged), and insert the SAME NESTED short-circuit added in 112-01 — mirror each
file's own existing CHAIN-step guard, reusing the already-declared `NESTED` const (do NOT redeclare).
When `CHECK_PHASE_NESTED === '1'`, the audit step returns a skipped/pass result instead of running the
milestone-audit against live corpus. This applies the D-00 principle (a frozen audit validates its own
close-SHA corpus, not future live corpus) that 112-01 established for 95/100. Do NOT touch CHAIN_SKIP.
Do NOT modify any vX.Y-milestone-audit.mjs, sidecar, or integrity workflow. If a validator has its
audit step already guarded, leave it. Commit as one atomic commit.
</action>
<verify><automated>
for p in 67 68 69 70 71 72 73 74 82 88 93; do CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-$p.mjs >/dev/null 2>&1 && echo "$p OK" || echo "$p STILL-RED"; done
</automated></verify>
<acceptance_criteria>
- All 11 validators exit 0 under `CHECK_PHASE_NESTED=1` (nested audit step skipped).
- Standalone (non-nested) each still runs its audit step (guard is nested-only) — standalone-RED on 90d corpus is the ACCEPTED D-00-companion condition; do not "fix" standalone.
- `git diff --stat 0a7699f HEAD -- scripts/validation/v1.*-milestone-audit.mjs scripts/validation/v1.*-audit-allowlist.json .github/workflows/audit-harness-v1.*-integrity.yml` remains EMPTY (frozen surfaces byte-unchanged; the v1.14 files added in Atom 1/2 are expected but no v1.4-v1.13 file changes).
- One atomic commit `fix(112-06): extend AUDIT-step NESTED-guard to 11 predecessor validators (D-00 completion)`.
</acceptance_criteria>
<done>11 Class-A validators green under nested invocation; frozen surfaces untouched.</done>
</task>

<task id="2" wave="1">
<name>Class B — frozen-aware-convert the 6 drifted content assertions</name>
<requirements>HARN-02</requirements>
<read_first>
- scripts/validation/_lib/frozen-at-close.mjs (readAtVYClose helpers + MILESTONE_CLOSE_SHAS — the frozen-read pattern; pick the SHA for each validator's OWN milestone: check-phase-49/57/59 → v1.5 close; check-phase-63 → v1.6 close; check-phase-48/60 → v1.5/v1.7 close as appropriate)
- Each target validator to see how it ALREADY does frozen-aware reads elsewhere (many were partially converted in v1.8 — match the existing idiom, e.g. readRoadmapAtV15Close / git show SHA:path):
  scripts/validation/check-phase-{49,57,59,63,48,60}.mjs
- The drifted live surfaces (to confirm what changed): docs/_glossary-linux.md frontmatter, docs/index.md Android+Linux sub-tables, docs/reference/macos-capability-matrix.md, scripts/validation/regenerate-supervision-pins.mjs --self-test
</read_first>
<action>
Convert each drifted assertion to read its surface at the validator's OWN milestone close-SHA (frozen)
instead of live HEAD, using the existing `readAtVYClose` / `git show <SHA>:path` idiom already present
in these files. Do NOT bump expected values to the live (802.1X-evolved) state — that would mask a
real future regression. Concretely:
- check-phase-49 (V-49-18 freshness): read `_glossary-linux.md` frontmatter at v1.5-close; assert the
  60-day cadence that was true at phase-49's close (the live 90d is v1.14's deliberate supersession,
  correctly out of phase-49's scope).
- check-phase-57 (V-57-06) + check-phase-59 (V-59-07): read `docs/index.md` at v1.5-close and count the
  Android / Linux sub-table rows there (4/4/3) — the +1 802.1X rows are v1.14 additions, not phase 57/59
  deliverables.
- check-phase-63 (V-63-08 byte-blob): compare the `macos-capability-matrix.md` blob AT v1.6-close to the
  baseline hash (frozen-to-frozen, always equal) rather than the live file (which Phase 109 legitimately
  changed).
- check-phase-48 + check-phase-60 (regenerate-supervision-pins --self-test AUDIT-07): investigate the
  self-test failure. If the self-test reads live corpus, route it to the frozen corpus at the relevant
  close-SHA (or pass a frozen-input flag) so the reproduction is deterministic against the pinned state;
  do NOT weaken the self-test's assertion. If the correct fix is a BASELINE refresh, document why the
  new baseline reflects frozen (not live-drifted) state. Keep the self-test genuinely testing the tool.
Commit as one atomic commit. If any Class-B fix proves to need a judgment call you cannot resolve from
the existing frozen-aware idiom (e.g., the self-test needs a design decision), STOP and report rather
than guessing.
</action>
<verify><automated>
for p in 49 57 59 63 48 60; do CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-$p.mjs >/dev/null 2>&1 && echo "$p OK" || echo "$p STILL-RED"; done
</automated></verify>
<acceptance_criteria>
- All 6 Class-B validators exit 0 under `CHECK_PHASE_NESTED=1`.
- No expected value was bumped to the live 802.1X-evolved state; each fix reads the frozen close-SHA surface.
- Frozen milestone-audit / sidecar / workflow surfaces remain byte-unchanged.
- One atomic commit `fix(112-06): frozen-aware-convert 6 drifted content assertions (802.1X corpus evolution)`.
</acceptance_criteria>
<done>6 Class-B validators green under nested invocation via frozen-aware reads.</done>
</task>

<task id="3" wave="2">
<name>Verify Class-C cascades cleared + full apex green locally (nested), then SUMMARY</name>
<requirements>HARN-03</requirements>
<read_first>
- scripts/validation/check-phase-{61,62,64,65,66}.mjs (cascade validators — should now pass once 48 is green)
- .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-04-PLAN.md (the re-audit that must be re-run after this remediation)
</read_first>
<action>
Confirm the 5 Class-C cascade validators (61, 62, 64, 65, 66) now exit 0 under nested invocation (they
depend on check-phase-48 which Task 2 greened). Do NOT edit them unless one has an independent failure
(if so, report it). Then write 112-06-SUMMARY.md recording the full before/after (22 RED → 0 RED
nested), the two atomic commit SHAs, the frozen-surface byte-unchanged proof, and an explicit note that
the 3-axis terminal re-audit (112-04) MUST be re-run after this plan (the prior Axis-2 run 28621185019
is now stale). Update STATE.md + ROADMAP.md (plan 112-06 complete). Do NOT run the full 5-9 min chain
locally (Windows deep-nest) — the authoritative re-audit is Linux GHA in the re-run of 112-04.
</action>
<verify><automated>
for p in 61 62 64 65 66; do CHECK_PHASE_NESTED=1 timeout 60 node scripts/validation/check-phase-$p.mjs >/dev/null 2>&1 && echo "$p OK" || echo "$p CHECK"; done
</automated></verify>
<acceptance_criteria>
- Cascade validators 61, 62, 64, 65, 66 exit 0 nested (or any residual failure is reported, not masked).
- 112-06-SUMMARY.md created with before/after + commit SHAs + byte-unchanged proof + "re-run 112-04" note.
- STATE.md + ROADMAP.md updated.
</acceptance_criteria>
<done>All 22 originally-RED validators green under nested invocation; ready to re-run the 3-axis audit.</done>
</task>

</tasks>

<must_haves>
truths:
  - id: apex-green-nested
    truth: "check-phase-101..112 and all predecessor validators [48..111] exit 0 under CHECK_PHASE_NESTED=1 (the apex chain is green when nested-invoked as the apex does)"
  - id: no-masking
    truth: "no drifted assertion was greened by bumping its expected value to the live 802.1X-evolved state; Class-A via NESTED-guard, Class-B via frozen-aware reads"
  - id: frozen-byte-unchanged
    truth: "v1.4-v1.13 milestone-audit.mjs + sidecars + integrity workflows are byte-unchanged (git diff empty since anchor 0a7699f)"
  - id: reaudit-required
    truth: "the 3-axis terminal re-audit (112-04) is re-run after this plan and the prior stale Axis-2 result is discarded"
</must_haves>

<threat_model>
- T1 (false-green via masking): bumping a drifted assertion to live state hides future regressions. MITIGATION: frozen-aware reads at close-SHA only; explicit acceptance criterion forbidding value-bumps.
- T2 (byte-frozen violation): accidentally editing a vX.Y-milestone-audit.mjs / sidecar / workflow. MITIGATION: only check-phase-NN.mjs edited; git-diff byte-unchanged gate on frozen surfaces.
- T3 (vacuous frozen-read): reading the wrong SHA so the assertion passes trivially. MITIGATION: each validator reads ITS OWN milestone close-SHA matching its existing frozen-aware idiom; reviewer verifies the SHA is the phase's milestone, not an arbitrary green.
</threat_model>
