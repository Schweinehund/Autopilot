---
phase: 112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/validation/check-phase-95.mjs
  - scripts/validation/check-phase-100.mjs
autonomous: true
requirements: [HARN-02]
must_haves:
  truths:
    - "The tree is chain-RED at HEAD BEFORE this plan (D-00): check-phase-95 and check-phase-100 each run their frozen v1.12/v1.13 milestone audit via an UN-guarded AUDIT-HARNESS step against evolved live corpus → apex check-phase-112 would be RED without this fix"
    - "check-phase-95.mjs AUDIT-HARNESS step gains a 3-line NESTED guard (CHECK_PHASE_NESTED==='1' → SKIP-PASS) mirroring its own CHAIN-step guard at lines 79-81; NESTED var already declared at line 72"
    - "check-phase-100.mjs AUDIT-HARNESS step gains the identical 3-line NESTED guard mirroring its own CHAIN-step guard at lines 91-93; NESTED var already declared at line 84"
    - "Nested invocation is GREEN: CHECK_PHASE_NESTED=1 node check-phase-95.mjs and CHECK_PHASE_NESTED=1 node check-phase-100.mjs both exit 0 with AUDIT-HARNESS SKIPPED (this is what the apex check-phase-112 sees for both classes: line-pin drift C2/C7/C9 + freshness C5/C10)"
    - "Standalone RED is preserved-by-design: node check-phase-95.mjs / check-phase-100.mjs WITHOUT the env var still runs AUDIT-HARNESS against live corpus and exits 1 (a frozen milestone audit validates its own close-SHA corpus, not future live corpus — documented, not a regression)"
    - "The pre-Phase-112 anchor SHA (HEAD before any Phase-112 edit) is captured and recorded — it is the byte-unchanged gate base consumed by Plan 112-05 and the BASELINE_18 anchor consumed by Plan 112-02"
    - "No frozen surface is touched (D-00a): check-phase-95/100 are living chain validators — Phase 111 4a2d0b6 already edited both; NO v1.4-v1.13 milestone-audit .mjs / sidecar / integrity workflow is modified; NO phase is added to CHAIN_SKIP"
  artifacts:
    - path: "scripts/validation/check-phase-95.mjs"
      provides: "NESTED-guarded AUDIT-HARNESS step (continuity apex, v1.12)"
      contains: "CHECK_PHASE_NESTED"
    - path: "scripts/validation/check-phase-100.mjs"
      provides: "NESTED-guarded AUDIT-HARNESS step (v1.13 apex)"
      contains: "CHECK_PHASE_NESTED"
  key_links:
    - from: "scripts/validation/check-phase-100.mjs"
      to: "AUDIT-HARNESS run() short-circuit"
      via: "if (NESTED) return skip before execFileSync"
      pattern: "NESTED.*AUDIT-HARNESS|AUDIT-HARNESS.*NESTED"
---

<objective>
Ship Plan 112-01: the **D-00 chain-green precondition** (NESTED-guard) — the mandatory Wave-0 fix that MUST land BEFORE either atom. Add a `CHECK_PHASE_NESTED === '1'` short-circuit to the **AUDIT-HARNESS step** of `check-phase-95.mjs` and `check-phase-100.mjs`, mirroring each file's own already-working CHAIN-step guard.

Why this is first (D-00 / D-00-RESOLUTION): the tree is **chain-RED at HEAD**. `check-phase-95` and `check-phase-100` each carry an AUDIT-HARNESS step that runs the frozen v1.12/v1.13 milestone audit via `execFileSync(..., {cwd: process.cwd()})` against the **LIVE** corpus — and unlike the CHAIN step, that AUDIT-HARNESS step has NO `NESTED` guard. Phase 101 (`eae49f7`, +1 glossary banner + 90-day review_by) and Phase 109 (`6306da8`, capability-matrix row shift) evolved the corpus, so the frozen audits now FAIL C2/C5/C7/C9/C10. When the future apex `check-phase-112` nest-invokes 95/100, their AUDIT-HARNESS re-runs → EXIT 1 → apex RED. The NESTED guard greens the apex for **BOTH** failure classes (line-pin drift + freshness) at once, without touching any frozen surface. This expresses the correct principle: *a frozen milestone audit validates its own close-SHA corpus, not future live corpus.*

Purpose: unblock Atom 1 (112-02) and Atom 2 (112-03) so apex `check-phase-112` can reach GREEN on both OSes for the corpus dimension.
Output: 2 edited chain validators (3-line guard each) + the recorded pre-Phase-112 anchor SHA; ONE commit.

**HARD constraints (D-00a):** editing `check-phase-95/100` is in-class chain maintenance (Phase 111 `4a2d0b6` already edited both). Do NOT touch any `v1.4-v1.13-milestone-audit.mjs` / `*-audit-allowlist.json` / `audit-harness-*-integrity.yml` frozen surface. Do NOT add any phase to `CHAIN_SKIP` (V-SELF hard-asserts `CHAIN_SKIP.size === 0` — the disqualified force-green path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-CONTEXT.md
@.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-RESEARCH.md
@.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-Phase-112 anchor + add the NESTED guard to the AUDIT-HARNESS step of check-phase-95.mjs and check-phase-100.mjs</name>
  <files>scripts/validation/check-phase-95.mjs, scripts/validation/check-phase-100.mjs</files>
  <read_first>
    - scripts/validation/check-phase-100.mjs (the file being MODIFIED — read the NESTED var at line 84, the CHAIN-step guard at lines 91-93 [the exact 3-line template to replicate], and the AUDIT-HARNESS step at lines 121-141 [the step lacking a guard]; the insert point is immediately after the existsSync graceful-skip return block, ~after line 128, before `try {`)
    - scripts/validation/check-phase-95.mjs (the file being MODIFIED — NESTED var at line 72, CHAIN-step guard at lines 79-81 [template], AUDIT-HARNESS step at lines 109-129; insert point ~after line 116, before `try {`)
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-RESEARCH.md (§"TARGET 1: NESTED-Guard Implementation" — exact 3-line guard body, env-var name, insert semantics, and the "standalone RED preserved" residual-risk note)
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-PATTERNS.md (§"Shared Patterns — Surgical Edits": the two AUDIT-HARNESS-guard entries with the copy-from-CHAIN-step template and the exact insert-line callouts)
  </read_first>
  <action>
    First capture the pre-Phase-112 anchor: run `git rev-parse --short HEAD` and record the SHA (this is the KNOWN-PAST HEAD before ANY Phase-112 edit — it is BOTH the byte-unchanged gate base for Plan 112-05 AND the BASELINE_18 anchor for Plan 112-02). Store it in this plan's SUMMARY.

    **Edit 1 — `check-phase-95.mjs` AUDIT-HARNESS step (per D-00-RESOLUTION):** In the `run()` body of the `AUDIT-HARNESS` check (the `id: 'AUDIT-HARNESS'` push near lines 109-129), insert the guard IMMEDIATELY after the `existsSync(join(process.cwd(), HARNESS))` graceful-skip `return {...}` block and BEFORE the `try {`. The guard is exactly the 3-line body copied from this file's own CHAIN-step guard (lines 79-81), with the detail string adjusted to the AUDIT-HARNESS context:
    - `if (NESTED) { return { pass: true, skipped: true, detail: 'nested invocation (CHECK_PHASE_NESTED=1): skip AUDIT-HARNESS re-run against evolved corpus' }; }`
    Use the `NESTED` const already declared at line 72 — do NOT declare a new variable. Do NOT change the `existsSync` block, the `try/catch`, the `execFileSync` call, or any other step.

    **Edit 2 — `check-phase-100.mjs` AUDIT-HARNESS step (identical fix):** Same insertion in the `id: 'AUDIT-HARNESS'` step near lines 121-141 — after the existsSync graceful-skip `return` block (~after line 128), before `try {`. Use the exact same 3-line guard body; reuse the `NESTED` const at line 84.

    Do NOT edit anything else in either file (no CHAIN_SKIP change, no CHAIN_PHASES change, no frozen surface). The only net change per file is the 3-line `if (NESTED)` block inside the AUDIT-HARNESS `run()`.
  </action>
  <verify>
    <automated>CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-95.mjs >/dev/null 2>&1; echo "95_nested_exit=$?"; CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-100.mjs >/dev/null 2>&1; echo "100_nested_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-95.mjs` exits 0 (prints `95_nested_exit=0`); running with `--verbose` shows `V-95-AUDIT-HARNESS` = SKIPPED with the "nested invocation (CHECK_PHASE_NESTED=1)" detail.
    - `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-100.mjs` exits 0 (prints `100_nested_exit=0`); `--verbose` shows `V-100-AUDIT-HARNESS` = SKIPPED.
    - Standalone RED preserved-by-design: `node scripts/validation/check-phase-100.mjs` (NO env var) still exits 1 with V-100-AUDIT-HARNESS FAIL against live corpus (do NOT try to make standalone green — that is the documented condition, cured for the chain by the nested guard).
    - `grep -v '^#' scripts/validation/check-phase-95.mjs | grep -c "skip AUDIT-HARNESS re-run against evolved corpus"` == 1; same == 1 for check-phase-100.mjs.
    - `CHAIN_SKIP` is unchanged in both files (`grep -c "new Set(\[\])" scripts/validation/check-phase-100.mjs` >= 1; no phase numbers added to any CHAIN_SKIP).
    - The pre-Phase-112 anchor SHA is captured via `git rev-parse --short HEAD` and recorded in SUMMARY.
  </acceptance_criteria>
  <done>Pre-Phase-112 anchor captured; both AUDIT-HARNESS steps gain the 3-line NESTED guard; nested invocation exits 0 (AUDIT-HARNESS SKIPPED) for 95 and 100; standalone RED preserved; CHAIN_SKIP untouched. NOT yet committed (commit in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Commit the NESTED-guard fix (exactly 2 files) and prove no frozen surface moved</name>
  <files>scripts/validation/check-phase-95.mjs, scripts/validation/check-phase-100.mjs</files>
  <read_first>
    - scripts/validation/check-phase-95.mjs (confirm only the 3-line guard was added)
    - scripts/validation/check-phase-100.mjs (confirm only the 3-line guard was added)
    - .planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-CONTEXT.md (§D-00a — the frozen-surface scope: milestone-audit .mjs + sidecars + integrity workflows are frozen; check-phase validators are NOT)
  </read_first>
  <action>
    Confirm `git diff --stat` shows ONLY `scripts/validation/check-phase-95.mjs` and `scripts/validation/check-phase-100.mjs` modified, each with a small additive diff (the 3-line guard). Then stage exactly those 2 files and commit as ONE commit. Commit message: `fix(112-01): NESTED-guard AUDIT-HARNESS step of check-phase-95/100 — D-00 chain-green precondition (HARN-02 Wave 0)`.

    Do NOT stage or commit any other file. In particular, verify no `v1.4-v1.13` milestone-audit `.mjs`, `*-audit-allowlist.json`, or `audit-harness-*-integrity.yml` appears in the diff (D-00a byte-unchanged invariant).
  </action>
  <verify>
    <automated>git show --stat HEAD --format="" | grep -E "check-phase-(95|100)\.mjs" | wc -l; echo "---frozen-check---"; git show --stat HEAD --format="" | grep -E "milestone-audit\.mjs|audit-allowlist\.json|audit-harness-.*integrity\.yml" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The commit touches EXACTLY 2 files: `git show --stat HEAD --format=""` lists only `check-phase-95.mjs` and `check-phase-100.mjs` (the frozen-check grep prints `0`).
    - COMMIT message is exactly: `fix(112-01): NESTED-guard AUDIT-HARNESS step of check-phase-95/100 — D-00 chain-green precondition (HARN-02 Wave 0)`.
    - No frozen surface (`v1.4-v1.13-milestone-audit.mjs`, `*-audit-allowlist.json`, `audit-harness-*-integrity.yml`) appears in the commit.
    - Re-confirm post-commit: `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-100.mjs; echo exit=$?` prints `exit=0`.
  </acceptance_criteria>
  <done>NESTED-guard fix committed as a 2-file commit; frozen-surface grep returns 0; nested check-phase-100 exits 0 post-commit; Wave 0 complete, Atom 1 (112-02) unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — local CLI validator maintenance) | Wave 0 edits two in-tree Node chain validators. No user input, no network, no auth, no secrets, no runtime data. The only external call is the validators' own `execFileSync('node', [HARNESS], ...)` — which the NESTED guard now SHORT-CIRCUITS under nested invocation. |

## STRIDE Threat Register

ASVS L1 review found **no high-severity items**. The realistic threat surface is chain-integrity: a guard that force-greens the apex by masking a real regression, or a frozen surface mutated while editing living validators.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01-E | Elevation/Integrity | The NESTED guard force-greening the apex the wrong way (e.g., via CHAIN_SKIP) and masking a genuine chain regression | mitigate | Fix is scoped to the AUDIT-HARNESS step ONLY (the frozen-audit-vs-evolved-corpus mismatch); CHAIN_SKIP is asserted unchanged (`new Set([])`); standalone RED is preserved so the frozen audits still self-validate their own close-SHA corpus. |
| T-112-01-T | Tampering | A frozen v1.4-v1.13 milestone-audit/sidecar/workflow silently mutated while editing the two living validators | mitigate | Acceptance gate greps the commit stat and asserts ZERO frozen-surface files present; the full 32-surface byte-unchanged gate runs at close (112-05). |
| T-112-01-I | Integrity | execFileSync in the AUDIT-HARNESS step invoking an attacker-controlled path | accept | HARNESS is a hardcoded in-repo relative path (`scripts/validation/v1.1x-milestone-audit.mjs`); args are literals; no user/network input reaches the call. No unsanitized interpolation. |
| T-112-01-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | NO installs — pure in-tree edits + `node` + `git`. No Package Legitimacy Gate required. |
</threat_model>

<verification>
- `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-95.mjs` → exit 0 (AUDIT-HARNESS SKIPPED).
- `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-100.mjs` → exit 0 (AUDIT-HARNESS SKIPPED).
- `node scripts/validation/check-phase-100.mjs` standalone → still exit 1 (documented standalone RED; frozen audit vs evolved corpus).
- CHAIN_SKIP unchanged (`new Set([])`); commit = exactly 2 files; zero frozen surfaces in the diff.
- Pre-Phase-112 anchor SHA recorded for the 112-05 byte-unchanged gate + 112-02 BASELINE_18 anchor.
</verification>

<success_criteria>
- D-00 chain-green precondition satisfied: apex `check-phase-112` (authored in 112-03) will see 95/100 GREEN when it nest-invokes them, for BOTH the line-pin-drift (C2/C7/C9) and freshness (C5/C10) failure classes.
- D-00a honored: only the two living chain validators changed; no frozen surface touched; CHAIN_SKIP force-green path avoided.
- Pre-Phase-112 anchor recorded (byte-unchanged gate base + BASELINE_18 anchor for downstream plans).
</success_criteria>

<output>
Create `.planning/phases/112-pillar-e-12th-path-a-audit-harness-lineage-bump-milestone-cl/112-01-SUMMARY.md` when done. Record: the Wave-0 COMMIT SHA, the captured **pre-Phase-112 anchor SHA** (byte-unchanged gate base for 112-05 AND BASELINE_18 anchor for 112-02), the nested-exit-0 confirmation for both 95 and 100, and confirmation that standalone RED is preserved-by-design and CHAIN_SKIP is untouched.
</output>
</content>
