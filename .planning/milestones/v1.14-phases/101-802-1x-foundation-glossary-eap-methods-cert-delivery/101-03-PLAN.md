---
phase: 101-802-1x-foundation-glossary-eap-methods-cert-delivery
plan: 03
type: execute
wave: 3
depends_on: [101-01, 101-02]
files_modified:
  - docs/admin-setup-8021x/02-cert-delivery-foundation.md
autonomous: true
requirements: [DOT1X-03]
must_haves:
  truths:
    - "An admin can follow the hard deployment ordering rule (trusted-root profile → SCEP/PKCS client cert → 802.1X network profile) as a prominent CRITICAL callout"
    - "EKU = Client Authentication (OID 1.3.6.1.5.5.7.3.2), RADIUS server-name validation, and Cloud PKI as an alternative are documented"
    - "The per-platform cert-delivery support matrix carries exactly DOT1X-03's cells (macOS/iOS wired = SCEP-only / no PKCS; Windows wired adds PFX Import; Linux = no Intune cert delivery; trusted-root per platform) — concise asymmetry matrix at foundation altitude, exhaustive UI detail deferred to per-platform guides (B3 / D-03)"
    - "The canonical Intune-client-side-only scope callout (SC4) is defined once here with the full exclusion list, plus a one-line banner template for downstream guides"
  artifacts:
    - path: "docs/admin-setup-8021x/02-cert-delivery-foundation.md"
      provides: "Cert-delivery foundation: canonical scope callout + ordering rule + cert-profile sections + EKU + server-name validation + Cloud PKI + per-platform cert matrix + boundary note"
      contains: "## Canonical Scope Callout"
  key_links:
    - from: "docs/admin-setup-8021x/02-cert-delivery-foundation.md"
      to: "docs/_glossary-network.md (#scep etc.) and 01-eap-method-overview.md"
      via: "prerequisites banner + inline term links (link-not-copy)"
      pattern: "_glossary-network.md#scep"
---

<objective>
Author `docs/admin-setup-8021x/02-cert-delivery-foundation.md` (DOT1X-03 + SC4): the certificate-delivery foundation. It homes the most consequential fact in the doc set — the hard deployment ordering rule — plus the canonical Intune-client-side-only scope callout (SC4) and the per-platform cert matrix (B3). Runs after the glossary (anchor links) and the EAP overview (prerequisite link), per link-not-copy build order.

Purpose: Single source of truth for cert-delivery ordering, EKU, server-name validation, Cloud PKI alternative, the per-platform cert asymmetry matrix, and the reusable scope-callout template.
Output: One new file with the canonical scope callout + banner template, the CRITICAL ordering callout, cert-profile sections, EKU/server-name/Cloud PKI sections, and the per-platform matrix with a link-not-copy boundary note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-CONTEXT.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-PATTERNS.md
@docs/_glossary-network.md
@docs/admin-setup-8021x/01-eap-method-overview.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 02-cert + author canonical scope callout (SC4), banner template, and the CRITICAL ordering rule</name>
  <files>docs/admin-setup-8021x/02-cert-delivery-foundation.md</files>
  <read_first>
    - `docs/_glossary-network.md` and `docs/admin-setup-8021x/01-eap-method-overview.md` — to confirm the prerequisites-banner link targets (`../_glossary-network.md#scep`, `01-eap-method-overview.md`).
    - `docs/reference/macos-capability-matrix.md` (lines 1–27) — front-matter + table-structure analog.
    - `docs/admin-setup-macos/08-auth-methods-deep-dive.md` (lines 9–34, 70+) — multi-line `> **Label:**` blockquote callout style.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md` §Canonical Scope Callout Text (exact exclusion list D-07 + banner template) and §Per-File Authoring Spec File 4 (ordering-rule callout text, A-01 prominence).
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-CONTEXT.md` decisions B3, C3 (D-06/D-07), D-04, D-05.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-PATTERNS.md` §`02-cert-delivery-foundation.md` (callout patterns, `> **Scope — ...**`, `> **CRITICAL — Ordering:**`).
  </read_first>
  <action>
    Create `docs/admin-setup-8021x/02-cert-delivery-foundation.md`. Front-matter: `last_verified: 2026-06-29`, `review_by: 2026-09-27`, `applies_to: both`, `audience: admin`, `platform: all`. Prerequisites blockquote `> **Prerequisites:**` linking to `01-eap-method-overview.md` and `../_glossary-network.md#scep`. H1 `# 802.1X Certificate Delivery Foundation`.
    Author `## Canonical Scope Callout` as the canonical SC4 home (C3 / D-06): a `> **Scope — Intune client-side configuration only.**` multi-line blockquote enumerating the EXACT exclusion list from RESEARCH §Canonical Scope Callout Text (RADIUS/NPS config; PKI/CA build-out ADCS/NDES; Certificate Connector install; switch/AP port config; MAB; Conditional Access network policies; non-co-equal EAP types EAP-SIM/EAP-FAST/LEAP/TEAP-as-a-path) plus the "Assumed: RADIUS already exists" line. Immediately after, provide the one-line banner TEMPLATE (for Phases 102–106 to place in their guides) as a fenced ```markdown block containing the `> **Scope:** Intune client-side configuration only ... [Full scope exclusion list](02-cert-delivery-foundation.md#canonical-scope-callout).` line — this fenced block is a verbatim template artifact for downstream use, not implementation code. Do NOT include any RADIUS/NPS configuration step, even "for reference" (E-01).
    Then author `## The Deployment Ordering Rule` as the FIRST substantive section (A-01 prominence) using a `> **CRITICAL — Deployment ordering:**` multi-line blockquote with the numbered sequence (1. Trusted Root profile → Succeeded; 2. SCEP or PKCS client cert profile → Succeeded + enrolled; 3. 802.1X Wi-Fi or Wired network profile) and the silent-"Succeeded"-but-fails warning, per RESEARCH.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q '^last_verified: 2026-06-29' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q '^# 802.1X Certificate Delivery Foundation' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q '^## Canonical Scope Callout' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -qi 'MAC Authentication Bypass\|MAB' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -qi 'EAP-SIM' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q 'CRITICAL — Deployment ordering' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q '#canonical-scope-callout' docs/admin-setup-8021x/02-cert-delivery-foundation.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists with 5-field front-matter (`last_verified: 2026-06-29` / `review_by: 2026-09-27`, `audience: admin`).
    - `## Canonical Scope Callout` present with a `> **Scope — Intune client-side configuration only.**` blockquote listing the full exclusion set (RADIUS/NPS, ADCS/NDES, Certificate Connector, switch/AP port config, MAB, Conditional Access, EAP-SIM/EAP-FAST/LEAP/TEAP-as-a-path) and the "Assumed: RADIUS already exists" line.
    - A downstream one-line banner template referencing `#canonical-scope-callout` is present.
    - `## The Deployment Ordering Rule` is the first substantive section and uses a `> **CRITICAL — Deployment ordering:**` callout with the three-step sequence and the silent-success warning.
    - No RADIUS/NPS configuration steps appear anywhere (E-01).
  </acceptance_criteria>
  <done>The scope callout (SC4), reusable banner template, and the prominent ordering rule are present at the top of the file.</done>
</task>

<task type="auto">
  <name>Task 2: Author cert-profile sections + EKU + RADIUS server-name validation + Cloud PKI</name>
  <files>docs/admin-setup-8021x/02-cert-delivery-foundation.md</files>
  <read_first>
    - `docs/admin-setup-8021x/02-cert-delivery-foundation.md` (from Task 1) — append after the ordering rule.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md` §Per-File Authoring Spec File 4 (Trusted Root / SCEP / PKCS / PFX Import / EKU / RADIUS Server-Name Validation / Cloud PKI section content), §House-Style Conventions (inline freshness-stamp blockquote for the Android 11+ server-name note).
    - `docs/_glossary-network.md` — to link EKU/server-name/SCEP/PKCS/trusted-root sections back to their glossary anchors (link-not-copy).
  </read_first>
  <action>
    Append foundation-altitude sections (link to glossary anchors, do not redefine terms): `## Trusted Root Certificate Profile`, `## SCEP Certificate Profile` (Required EKU = Client Authentication OID 1.3.6.1.5.5.7.3.2; Android personally-owned-work-profile UPN-in-SAN note; renewal threshold), `## PKCS Certificate Profile`, `## PFX Import (PKCS Imported) Certificate Profile` (Windows-wired-unique), `## EKU Requirement: Client Authentication` (OID 1.3.6.1.5.5.7.3.2; missing EKU → Access-Reject), `## RADIUS Server-Name Validation` (always populate server names; server validation enabled; reference trusted root; Android 11+ required, Android 14+ ≤256 chars / no special chars — wrap the version-gated note in an inline freshness blockquote `> *Freshness: last verified 2026-06-29 against Microsoft Learn. Review by 2026-09-27.*`), and `## Cloud PKI (Alternative)` (Intune Suite Cloud PKI as managed CA alternative; full config out of scope → Microsoft Learn). Keep at foundation altitude — no per-platform Intune UI field walk-throughs (those are Phases 102–106).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q '^## Trusted Root Certificate Profile' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q '^## SCEP Certificate Profile' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q '^## PKCS Certificate Profile' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q 'PFX Import' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q '1.3.6.1.5.5.7.3.2' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q '^## RADIUS Server-Name Validation' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -qi 'Cloud PKI' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q 'Freshness: last verified 2026-06-29' docs/admin-setup-8021x/02-cert-delivery-foundation.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Sections present: `## Trusted Root Certificate Profile`, `## SCEP Certificate Profile`, `## PKCS Certificate Profile`, `## PFX Import (PKCS Imported) Certificate Profile`, `## EKU Requirement: Client Authentication`, `## RADIUS Server-Name Validation`, `## Cloud PKI (Alternative)`.
    - EKU section states OID `1.3.6.1.5.5.7.3.2` and that a missing EKU → Access-Reject.
    - RADIUS server-name section mandates populated server names + enabled validation; the Android 11+/14+ version-gated note carries an inline freshness blockquote dated 2026-06-29.
    - Cloud PKI noted as an alternative (not a full guide); content stays at foundation altitude (no per-platform UI walk-throughs).
  </acceptance_criteria>
  <done>All cert-profile, EKU, server-name-validation, and Cloud PKI sections are present at foundation altitude with the version-gated freshness stamp.</done>
</task>

<task type="auto">
  <name>Task 3: Author the per-platform cert-delivery support matrix (B3) + boundary note + change history</name>
  <files>docs/admin-setup-8021x/02-cert-delivery-foundation.md</files>
  <read_first>
    - `docs/admin-setup-8021x/02-cert-delivery-foundation.md` (from Tasks 1–2) — append after the Cloud PKI section.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md` §Cert Asymmetry Matrix (the EXACT table + key-asymmetry bullets — this table belongs ONLY here) and the `> **Boundary:**` callout text.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-CONTEXT.md` decision B3 / D-05.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-PATTERNS.md` §Change History Table.
  </read_first>
  <action>
    Append `## Per-Platform Cert-Delivery Support Matrix` with the EXACT table from RESEARCH §Cert Asymmetry Matrix (columns: Platform | Wired Profile | SCEP (client cert) | PKCS (client cert) | PFX Import / PKCS Imported | Trusted Root; rows Windows / macOS / iOS-iPadOS / Android Enterprise / Linux), preserving the bolded asymmetry cells (`**NOT supported for wired profiles**`, `**Yes — wired only, unique to Windows wired profile UI**`, `**NO — no Intune cert profiles for Linux**`, `**NO — not supported via Intune**`). Add the key-asymmetry bullet list (macOS/iOS wired = SCEP-only / no PKCS; Windows wired adds PFX Import; Linux = no Intune cert delivery). Immediately after the matrix add a `> **Boundary:**` callout stating per-platform guides (Phases 102–106) own the exact Intune UI fields/steps and that detail must not be duplicated here (D-05 / E-02). End with a `---` rule and a `## Change History` row: `| 2026-06-29 | Initial version -- cert-delivery foundation: ordering rule, scope callout, EKU, server-name validation, per-platform cert matrix | -- |`.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q '^## Per-Platform Cert-Delivery Support Matrix' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q 'NOT supported for wired profiles' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -qi 'unique to Windows wired' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -qi 'no Intune cert' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q '> \*\*Boundary:\*\*' docs/admin-setup-8021x/02-cert-delivery-foundation.md && grep -q '2026-06-29 | Initial version' docs/admin-setup-8021x/02-cert-delivery-foundation.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `## Per-Platform Cert-Delivery Support Matrix` present with all five platform rows and the six columns from RESEARCH.
    - Bolded asymmetry cells present: macOS/iOS PKCS `**NOT supported for wired profiles**`; Windows PFX Import `unique to Windows wired`; Linux `no Intune cert` delivery.
    - A `> **Boundary:**` callout asserts per-platform UI detail lives in Phases 102–106 (link-not-copy).
    - `## Change History` row dated 2026-06-29 present.
  </acceptance_criteria>
  <done>The per-platform cert matrix (the sole home of this table), boundary note, and change history are present; the file is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| reader → documentation | Static Markdown; no executable surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-05 | Information (silent misconfig) | deployment ordering rule | mitigate | Prominent `> **CRITICAL — Deployment ordering:**` callout placed as the first substantive section (A-01) |
| T-101-06 | Elevation (scope creep) | scope callout | mitigate | Canonical exclusion list (D-07); no RADIUS/NPS or PKI build-out steps (E-01) |
| T-101-07 | Information (anchor drift) | `#canonical-scope-callout` banner target | mitigate | Banner template references the literal GitHub auto-slug of the canonical H2 |
No HIGH-severity application threats: static documentation only.
</threat_model>

<verification>
- File exists with valid freshness front-matter and all required sections.
- Ordering rule is the first substantive section; scope callout has the full D-07 exclusion list.
- Per-platform matrix present with DOT1X-03 cells; boundary note present; no RADIUS/NPS config (E-01); no per-platform UI walk-throughs.
</verification>

<success_criteria>
Maps to Phase 101 SC3 (ordering rule + EKU + server-name validation + per-platform cert matrix) and SC4 (canonical Intune-client-side-only scope callout template present and reusable).
</success_criteria>

<output>
Create `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-03-SUMMARY.md` when done.
</output>
