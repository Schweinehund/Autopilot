---
phase: 128-v116-pin-15th-path-a-lineage-bump-terminal-close
plan: 04
type: execute
wave: 4
depends_on: ["128-03"]
files_modified:
  - scripts/validation/check-phase-126.mjs
  - scripts/validation/check-phase-127.mjs
  - scripts/validation/check-phase-128.mjs
  - .github/workflows/audit-harness-v1.17-integrity.yml
autonomous: true
requirements: [HARN-09, HARN-10]
must_haves:
  truths:
    - "check-phase-126.mjs and check-phase-127.mjs are lightweight non-apex validators (CHAIN_PHASES empty, CHAIN_SKIP empty) whose presence/content needles derive from 126-VERIFICATION.md and 127-VERIFICATION.md; each exits 0"
    - "check-phase-128.mjs is the apex: CHAIN_PHASES = integers 48..127 (exactly 80 entries), CHAIN_SKIP empty, HARNESS = v1.17-milestone-audit.mjs, with hard module-load throws on length not-equal-80 AND terminus not-equal-127 (the copied 77/124 literals MUST be updated or it throws on its own load)"
    - "audit-harness-v1.17-integrity.yml is the 14th coexistence workflow: a copy of v1.16's with paths repointed v1.16-* to v1.17-* (incl. the MILESTONE-AUDIT/DEFERRED-CLEANUP path entries), the linux-chain-ubuntu-latest job preserved verbatim (core.autocrlf false, fetch-depth 0, continue-on-error false, timeout-minutes 30), and its chain-apex step running check-phase-128.mjs"
    - "Atom 2b lands as ONE commit; then Atom 2a + Atom 2b are PUSHED to a branch/PR firing the audit-harness-v1.17-integrity.yml paths trigger (the authoritative Axis-2 GHA run — its run ID is consumed by 128-05/128-07)"
    - "Predecessor v1.4-v1.16 frozen surfaces (v1.N-milestone-audit.mjs, v1.N sidecars, prior workflow files, check-phase-48..125 except the 8 D-128-C conversions) remain byte-unchanged"
  artifacts:
    - path: "scripts/validation/check-phase-128.mjs"
      provides: "chain apex, 80-entry chain 48..127, throw 80/127"
      contains: "check-phase-127"
    - path: ".github/workflows/audit-harness-v1.17-integrity.yml"
      provides: "14th CI coexistence workflow, paths repointed v1.17, linux-chain preserved"
      contains: "check-phase-128"
  key_links:
    - from: "check-phase-128.mjs HARNESS constant"
      to: "scripts/validation/v1.17-milestone-audit.mjs"
      via: "AUDIT-HARNESS subprocess check"
      pattern: "v1.17-milestone-audit"
    - from: "check-phase-128.mjs CHAIN loop 48..127"
      to: "the 8 D-128-C converted validators (from 128-03)"
      via: "NESTED-aware execFileSync recursion"
      pattern: "check-phase-"
    - from: "Atom 2b PUSH"
      to: "authoritative Axis-2 GHA run (128-05)"
      via: "audit-harness-v1.17-integrity.yml paths trigger"
      pattern: "audit-harness-v1.17"
---

<objective>
Atom 2b (ONE commit) + PUSH: author the 3 new per-phase validators (check-phase-126/127 non-apex, check-phase-128 apex with the corrected 80/127 throws) and the 14th CI coexistence workflow, then push both Atom-2 commits to fire the authoritative Axis-2 Linux-GHA run. This is the B-C-D interlock ordering rider: the push MUST precede the close-gate so the close-gate can consume the run ID.

Purpose: Complete the lineage bump's validator + CI surface and initiate the authoritative cross-OS re-audit.
Output: check-phase-126/127/128.mjs + audit-harness-v1.17-integrity.yml — one commit, then pushed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-RESEARCH.md
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-PATTERNS.md
@scripts/validation/check-phase-125.mjs
@scripts/validation/check-phase-123.mjs
@.github/workflows/audit-harness-v1.16-integrity.yml
@.planning/phases/126-publish-bundle-pipeline-guard-blocker-corpus-fixes/126-VERIFICATION.md
@.planning/phases/127-automated-milestone-completion-trigger/127-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author check-phase-126.mjs + check-phase-127.mjs (non-apex)</name>
  <read_first>
    - scripts/validation/check-phase-123.mjs (CHAIN_PHASES empty shape lines 34-36, presence helper lines 44-55, V-SELF guard lines 98-112) + check-phase-124.mjs (alternate needle example)
    - .planning/phases/128-*/128-PATTERNS.md section 5 (non-apex template)
    - .planning/phases/126-*/126-VERIFICATION.md + .planning/phases/127-*/127-VERIFICATION.md (Required Artifacts / Observable Truths — the needle source)
  </read_first>
  <files>scripts/validation/check-phase-126.mjs, scripts/validation/check-phase-127.mjs</files>
  <action>
    Copy the check-phase-123.mjs Path-A shape to check-phase-126.mjs and check-phase-127.mjs. Set CHAIN_PHASES empty and CHAIN_SKIP empty in both; repoint the apex reference comment to check-phase-128.mjs. Repoint all 123/124 self-references to 126/127 respectively (including the V-126-SELF / V-127-SELF dual-invariant guard that asserts CHAIN_PHASES does not include its own phase number and CHAIN_SKIP size is 0). Derive the presence/content needles for each from the matching 126-VERIFICATION.md / 127-VERIFICATION.md Required-Artifacts and Observable-Truths entries — regression-guard Phase 126's publish-bundle pipeline (build-publish-bundle.mjs, filename-map, the versioned zip logic) and Phase 127's Stop-hook (publish-bundle-gate.cjs, deriveZipName). Keep the shape mechanical; do not design new validator architecture.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && node scripts/validation/check-phase-126.mjs >/dev/null 2>&1 && echo 126:PASS; node scripts/validation/check-phase-127.mjs >/dev/null 2>&1 && echo 127:PASS</automated>
  </verify>
  <acceptance_criteria>
    - check-phase-126.mjs and check-phase-127.mjs each exit 0 standalone
    - grep confirms an empty CHAIN_PHASES and empty CHAIN_SKIP in both
    - Each contains a V-126-SELF / V-127-SELF guard asserting its own phase number is absent and CHAIN_SKIP size is 0
    - Needles reference real Phase 126/127 deliverables (build-publish-bundle / publish-bundle-gate)
  </acceptance_criteria>
  <done>Two lightweight non-apex validators exist, exit 0, empty-chain + SELF-guard skeleton correct.</done>
</task>

<task type="auto">
  <name>Task 2: Author check-phase-128.mjs apex (80 entries, throw 80/127)</name>
  <read_first>
    - scripts/validation/check-phase-125.mjs (apex model: CHAIN_PHASES lines 56-75, throw literals 77/124 at lines 70-75, V-125-SELF lines 163-180, V-AUDIT/CHAIN loop/AUDIT-HARNESS blocks, runner loop)
    - .planning/phases/128-*/128-PATTERNS.md section 4 + 128-RESEARCH.md Pattern 2 + Pitfall 2 (the 77 to 80 / 124 to 127 update is MANDATORY or it throws on load)
  </read_first>
  <files>scripts/validation/check-phase-128.mjs</files>
  <action>
    Copy check-phase-125.mjs to check-phase-128.mjs. Set CHAIN_PHASES to the literal continuous integer array 48..127 (exactly 80 entries — extend the 125 array with 125,126,127). Update BOTH module-load throws: length not-equal-80 (was 77) and terminus not-equal-127 (was 124). Set CHAIN_SKIP empty. Repoint the HARNESS constant to scripts/validation/v1.17-milestone-audit.mjs. Repoint every 125 self-reference to 128 (V-128-SELF guard: assert CHAIN_PHASES does not include 128 and CHAIN_SKIP empty; pass-detail string reads chain 48..127 80 entries 128 absent). Repoint the V-128-AUDIT VERIFICATION-heading check, the V-128-CHAIN-NN NESTED-aware execFileSync loop (isPeer/600s timeout preserved), the V-128-AUDIT-HARNESS subprocess check (targets the renamed harness), and the runner banner string. Update the file header narrative + source-of-truth path to 128-CONTEXT.md / 128-RESEARCH.md.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && node -c scripts/validation/check-phase-128.mjs && grep -c "!== 80" scripts/validation/check-phase-128.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node -c scripts/validation/check-phase-128.mjs parses AND does not throw at load (the 80/127 throws are satisfied by the 80-entry array)
    - grep for "!== 80" and "!== 127" each return 1 or more; grep for "!== 77" and "!== 124" return 0
    - CHAIN_PHASES literal ends with 125,126,127 and has 80 comma-separated integers
    - HARNESS references v1.17-milestone-audit.mjs; running check-phase-128.mjs --verbose standalone exits 0
  </acceptance_criteria>
  <done>Apex check-phase-128.mjs authored with 80-entry chain 48..127 and corrected 80/127 throws; standalone exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Author the 14th CI workflow, commit Atom 2b, and PUSH</name>
  <read_first>
    - .github/workflows/audit-harness-v1.16-integrity.yml (full — copy source: header lines 1-25, paths trigger, linux-chain-ubuntu-latest job lines 77-95, per-phase job list, parse/path-match/harness-run jobs)
    - .planning/phases/128-*/128-PATTERNS.md section 6 (exact repoint instructions; 13th to 14th coexistence label) + 128-CONTEXT.md D-128-B (mandatory rider) + grounding correction #7
    - project memory: gsd-sdk write-verbs hang — use direct git
  </read_first>
  <files>.github/workflows/audit-harness-v1.17-integrity.yml</files>
  <action>
    Copy audit-harness-v1.16-integrity.yml to audit-harness-v1.17-integrity.yml. Repoint all v1.16 tokens to v1.17 (name; the paths entries scripts/validation/v1.16-* and the .planning/milestones/v1.16-MILESTONE-AUDIT.md + v1.16-DEFERRED-CLEANUP.md path entries; every v1.16-audit-allowlist.json / v1.16-milestone-audit.mjs string in parse/path-match/harness-run jobs). Change the coexistence-count label from 13th to 14th (v1.17 is the 15th Path-A lineage bump but only the 14th CI workflow — do NOT mislabel). Repoint the DUAL-APEX comment phase numbers check-phase-125 / 48..124 to check-phase-128 / 48..127. In the linux-chain-ubuntu-latest job, change ONLY the chain-apex step (check-phase-125.mjs to check-phase-128.mjs, the notice comment 48..124 to 48..127) and PRESERVE verbatim: core.autocrlf false, fetch-depth 0, continue-on-error false, timeout-minutes 30. Replace the per-phase job list (check-phase-120..125) with check-phase-126, check-phase-127, check-phase-128 (apex, recursively spawns 48..127, timeout-minutes 30) jobs, each needs harness-run, continue-on-error false. Keep the generic check-phase glob line and the .planning/REQUIREMENTS.md path unchanged. Then commit Atom 2b as ONE indivisible commit (the 3 validators + the workflow) via direct git, and PUSH both Atom-2a and Atom-2b commits to a branch/PR to fire the workflow's paths trigger. Record the resulting GHA run ID for 128-05.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -c "check-phase-128" .github/workflows/audit-harness-v1.17-integrity.yml</automated>
  </verify>
  <acceptance_criteria>
    - audit-harness-v1.17-integrity.yml contains check-phase-128 and core.autocrlf false, fetch-depth 0, continue-on-error false, timeout-minutes 30 preserved
    - grep for the string "v1.16-" in the new workflow returns 0 (no stale v1.16 path/label leaked; the check-phase glob and REQUIREMENTS.md path are generic)
    - The workflow header label reads "14th" coexistence workflow
    - Atom 2b commit contains exactly the 4 named files; both Atom-2 commits are pushed and the GHA run ID is recorded in the SUMMARY
  </acceptance_criteria>
  <done>14th CI workflow authored (linux-chain preserved), Atom 2b committed, both Atom-2 commits pushed, Axis-2 GHA run initiated and its run ID recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow config | audit-harness-v1.17-integrity.yml is a paths/version repoint of an existing trusted workflow, not a new privileged action. Runs on ubuntu-latest with the same permissions as the 13 prior coexistence workflows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-07 | Denial of Service | Apex throw on own load (77/124 literals) | mitigate | Acceptance criteria assert the 80/127 throws are updated and node -c does not throw; grep proves 77/124 absent |
| T-128-08 | Tampering | CI workflow permission drift | accept | Copy-and-repoint of an existing trusted workflow; paths/version-only change, no new secrets or privileged steps introduced |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Package Legitimacy Audit N/A); attack surface nil |

No high-severity threats: this plan authors validator scripts and a paths/version repoint of an existing CI workflow; no new privileged action, no external input.
</threat_model>

<verification>
- check-phase-126/127 non-apex (empty chain), each exits 0
- check-phase-128 apex: 80-entry 48..127 chain, throw 80/127, HARNESS = v1.17-milestone-audit.mjs, standalone exits 0
- audit-harness-v1.17-integrity.yml: 14th workflow, paths repointed, linux-chain preserved verbatim, no stale v1.16- token
- Atom 2b = one 4-file commit; both Atom-2 commits pushed; Axis-2 GHA run ID recorded
</verification>

<success_criteria>
The full v1.17 validator + CI surface exists and the authoritative Axis-2 Linux-GHA run is initiated by the push — providing the run ID that 128-05 and 128-07 consume for the 3-axis EXACT-MATCH and the MILESTONE-AUDIT record.
</success_criteria>

<output>
Create `.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-04-SUMMARY.md` when done
</output>
