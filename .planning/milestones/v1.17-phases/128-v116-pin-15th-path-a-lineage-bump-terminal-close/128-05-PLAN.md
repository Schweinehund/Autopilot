---
phase: 128-v116-pin-15th-path-a-lineage-bump-terminal-close
plan: 05
type: execute
wave: 5
depends_on: ["128-04"]
files_modified:
  - .planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-05-AUDIT-RESULTS.md
autonomous: true
requirements: [HARN-10]
must_haves:
  truths:
    - "The authoritative Axis-2 Linux GHA run (from the 128-04 push) is awaited and its run ID + apex PASS/FAIL/SKIP tally recorded; Linux GHA is authoritative for BOTH chain validators (D-03 OS split), Windows corroborates only and MUST NOT block on a Windows apex deep-nest timeout"
    - "The CI cascade is scanned for RED: the 10 chain-RUNNING predecessors (v1.7-v1.16) + the new v1.17 + the base audit-harness-integrity.yml (11 firing, 10 chain-running); v1.5/v1.6 do NOT fire and the base has no chain job — do NOT chase RED there"
    - "Axis-1 (fresh git clone --no-hardlinks) and Axis-3 (fresh zero-context sub-agent) reproduce the reproducible-surface results; cross-OS/cross-context PASS/FAIL/SKIP EXACT MATCH is asserted; the re-audit does NOT assert docs-library-v1.17.zip presence (dist/ is gitignored — Sub-Q1)"
    - "The predecessor-byte-unchanged HARD gate holds: git diff from the 128-01 Wave-0 anchor to HEAD over the v1.4-v1.16 frozen harness/sidecar/workflow surfaces + check-phase-48..125 (EXCLUDING the 8 D-128-C converted validators) is EMPTY"
    - "If any firing apex is RED, the plan HALTS and signals the 128-06 emergent remediation slot; if GREEN across the cascade it clears the close-gate precondition"
  artifacts:
    - path: ".planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-05-AUDIT-RESULTS.md"
      provides: "3-axis re-audit record + cascade RED scan + cross-OS EXACT MATCH table + byte-unchanged gate proof + GHA run ID"
      contains: "EXACT MATCH"
  key_links:
    - from: "128-04 Axis-2 GHA run ID"
      to: "128-05 authoritative Axis-2 tally"
      via: "recorded run ID consumed"
      pattern: "check-phase-128"
    - from: "128-05 cascade verdict"
      to: "128-06 emergent slot (if RED) / 128-07 close-gate (if all GREEN)"
      via: "recorded apex PASS/FAIL/SKIP tallies"
      pattern: "check-phase"
---

<objective>
The 3-axis terminal re-audit (HARN-10): consume the authoritative Axis-2 Linux GHA run from the 128-04 push, reproduce on Axis-1 (fresh clone) + Axis-3 (fresh zero-context sub-agent), assert cross-OS EXACT MATCH, scan the firing cascade for RED, and hold the predecessor-byte-unchanged HARD gate. This proves the v1.17 chain is atomically green at the new close state across the whole firing cascade and that non-Phase-128 predecessor surfaces remain byte-unchanged — clearing (or, if RED, routing to the emergent slot) the close-gate precondition.

Purpose: The independent cross-OS/cross-context verification that gates the single close-gate commit.
Output: 128-05-AUDIT-RESULTS.md (3-axis table + cascade scan + byte-unchanged proof + run ID).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-RESEARCH.md
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-CONTEXT.md
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-01-SUMMARY.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consume authoritative Axis-2 GHA + cascade RED scan</name>
  <read_first>
    - .planning/phases/128-*/128-04-SUMMARY.md (the recorded Axis-2 GHA run ID)
    - .planning/phases/128-*/128-CONTEXT.md D-128-B (Linux GHA authoritative for BOTH chain validators; cascade scan) + grounding correction #4 (11 firing, 10 chain-running; v1.5/v1.6 do not fire; base has no chain job)
    - .planning/phases/128-*/128-RESEARCH.md Pitfall 4 (ordering dependency — run ID must exist)
  </read_first>
  <files>.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-05-AUDIT-RESULTS.md</files>
  <action>
    Await the authoritative Axis-2 Linux GHA run triggered by the 128-04 push (do NOT background a local full-chain run — it dies at turn boundaries per STATE; delegate any local corroboration to a fresh sub-agent or rely on the GHA run). Record its run ID and the apex check-phase-128 PASS/FAIL/SKIP tally + the linux-chain-ubuntu-latest job result. Linux GHA is authoritative for BOTH chain validators (D-03 OS split); the Windows-local apex deep-nests and times out at the 80-entry range — record it as corroboration only and do NOT block on the Windows timeout. Scan the CI cascade for RED across the 10 chain-running predecessors (v1.7-v1.16 integrity workflows) + the new v1.17 workflow + confirm the base audit-harness-integrity.yml fired but has no chain job (cannot go RED on chain content); understand 48..66 as covered by apex recursion and that v1.5/v1.6 workflows do NOT fire — do NOT chase RED there. Because v1.17 corpus drift is ~nil (D-128-C pre-scoped), the cascade is expected all-green.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -c "run ID" .planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-05-AUDIT-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - 128-05-AUDIT-RESULTS.md records the Axis-2 GHA run ID and the apex + linux-chain PASS/FAIL/SKIP tallies
    - The cascade scan lists the 10 chain-running predecessors + v1.17 + base with their statuses; v1.5/v1.6 correctly noted as not-firing
    - The Windows apex timeout is recorded as corroboration-only, not a blocker
    - If any firing apex is RED, the record HALTS to 128-06; if all GREEN, it states the close-gate precondition is cleared for that leg
  </acceptance_criteria>
  <done>Authoritative Axis-2 GHA run + cascade scan recorded; Linux-GHA-authoritative; Windows non-blocking.</done>
</task>

<task type="auto">
  <name>Task 2: Axis-1 fresh clone + Axis-3 sub-agent EXACT MATCH + byte-unchanged gate</name>
  <read_first>
    - .planning/phases/128-*/128-01-SUMMARY.md (the recorded Wave-0 anchor SHA — the byte-unchanged gate base)
    - .planning/phases/128-*/128-CONTEXT.md D-128-A (single close-gate; NO PIPE-02 leg) + D-128-D Sub-Q1 (do NOT assert zip presence in the fresh clone)
    - .planning/phases/128-*/128-03-PLAN.md files_modified (the 8 D-128-C converted validators to EXCLUDE from the byte-unchanged gate)
  </read_first>
  <files>.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-05-AUDIT-RESULTS.md</files>
  <action>
    Axis-1: fresh git clone --no-hardlinks of the pushed branch into a scratch dir, run check-phase-128.mjs + check-phase-126/127 + the 8 converted validators + v1.17-milestone-audit.mjs --verbose (Windows), record PASS/FAIL/SKIP. Do NOT assert docs-library-v1.17.zip presence — dist/ is gitignored so a fresh clone has no zip (Sub-Q1). Axis-3: dispatch a fresh zero-context sub-agent to independently reproduce the reproducible-surface validator results. Assert cross-OS/cross-context PASS/FAIL/SKIP EXACT MATCH across Axis-1 (fresh clone Windows) vs Axis-2 (Linux GHA) vs Axis-3 (sub-agent) for the reproducible surfaces — the apex full-chain being Linux-GHA-authoritative where Windows times out. Then run the predecessor-byte-unchanged HARD gate: git diff from the 128-01 Wave-0 anchor SHA to HEAD restricted to the v1.4-v1.16 frozen surfaces (v1.N-milestone-audit.mjs, v1.N-audit-allowlist.json, prior audit-harness-v1.N-integrity.yml, check-phase-48..125) EXCLUDING the 8 D-128-C converted validators (49/58/59/62/101/109/118/121) — assert the diff is EMPTY. Record the full 3-axis table + the byte-unchanged proof. NO PIPE-02 / grounding leg attaches (D-128-A).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -c "EXACT MATCH" .planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-05-AUDIT-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - 128-05-AUDIT-RESULTS.md contains a 3-axis PASS/FAIL/SKIP table with an explicit cross-OS EXACT MATCH assertion for the reproducible surfaces
    - The byte-unchanged HARD gate proof shows an EMPTY diff over the frozen predecessor surfaces (excluding the 8 D-128-C converted validators)
    - No leg asserts docs-library-v1.17.zip presence in the fresh clone (Sub-Q1)
    - No PIPE-02 / grounding-confirmation leg is present (D-128-A)
  </acceptance_criteria>
  <done>3-axis EXACT MATCH asserted; predecessor-byte-unchanged gate EMPTY; no zip-presence and no PIPE-02 leg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Read-only re-audit: runs existing validators against a fresh clone + consumes a GHA run + writes one planning-doc record. No runtime, network-write, user input, auth, or data surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-09 | Repudiation | Axis-2 authenticity (ad-hoc re-run vs push-triggered) | mitigate | Axis-2 is defined as the specific GHA run triggered by the 128-04 push; its run ID is recorded and cited (Pitfall 4) |
| T-128-10 | Tampering | Undetected predecessor frozen-surface drift | mitigate | The byte-unchanged HARD gate diffs from the Wave-0 anchor over all frozen surfaces and asserts EMPTY |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Package Legitimacy Audit N/A); attack surface nil |

No high-severity threats: this plan runs read-only validators and writes an audit record; no privileged action, no external input.
</threat_model>

<verification>
- Axis-2 GHA run ID + apex tally recorded; Linux authoritative, Windows non-blocking
- Cascade scan: 10 chain-running + v1.17 + base; v1.5/v1.6 correctly excluded
- 3-axis cross-OS EXACT MATCH asserted for reproducible surfaces
- Predecessor-byte-unchanged HARD gate EMPTY (excluding the 8 conversions)
- No zip-presence leg; no PIPE-02 leg
</verification>

<success_criteria>
The v1.17 chain is proven atomically green across the firing cascade with cross-OS/cross-context EXACT MATCH and byte-unchanged predecessor surfaces — the close-gate precondition is cleared (or, if any apex is RED, routed to the 128-06 emergent slot).
</success_criteria>

<output>
Create `.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-05-SUMMARY.md` when done (in addition to the 128-05-AUDIT-RESULTS.md deliverable)
</output>
