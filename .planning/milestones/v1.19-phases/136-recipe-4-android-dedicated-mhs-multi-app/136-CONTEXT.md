# Phase 136: Recipe #4 — Android Dedicated, MHS Multi-App - Context

**Gathered:** 2026-08-03
**Status:** Ready for planning

<domain>
## Phase Boundary

Phase 136 authors **`docs/recipes/04-android-dedicated-mhs-multi-app.md`** (RE-225) — the fourth live instantiation of the Phase-129 Device Recipe doc class — plus **HYG-06**, the spot-verification of `docs/admin-setup-android/05-dedicated-devices.md` before authoring depends on it. The recipe supplies the `## Steps` / `## Verification` / Unsupported-and-Anti-Feature scaffold the anchor structurally lacks, plus an inlined recipe-scoped MHS app-deployment step, authored strictly as a **delta**.

Delivers (MHS-01..05, HYG-06):
1. **Linear happy-path spine:** confirm Standard token → deploy MHS Required → deploy allow-listed apps → sign-in mode decision → author + assign the MHS App Configuration policy payload → exit-PIN cross-link + hardening.
2. **The worked MHS payload** as a column-0 ```` ```json ```` fence plus a key-decomposition table, bounded to ~8 keys.
3. **Three Case-1/Case-2 decision blocks plus two Case-3 lead-in prompts** — token type (Case 1, SDM arm routing to the anchor), provisioning method (Case 2), sign-in mode (Case 2), `grid_size` and `managed_folders` (Case 3).
4. **`## Rollback/Recovery`** as a named documented divergence, carrying the three recovery procedures the anchor does not own.
5. **Admin-executable verification** — positive observable checks only; both deliberately-break-it checks demote to `## Configuration-Caused Failures`.
6. **HYG-06** — three fact clusters spot-verified against current Learn, with a branch-split freshness-stamp disposition.

**Out of scope** (REQUIREMENTS.md Out-of-Scope table): re-authoring anything `05-dedicated-devices.md` owns (the dominant review failure mode); screensaver / digital-signage configuration (RCPFUT-04); MHS widgets, wallpaper and branding (RCPFUT-05); any line-shifting edit to `docs/_glossary-android.md` or `docs/reference/android-capability-matrix.md`; navigation/registry wiring (Phase 137, navigation-last).

**Decision provenance:** Resolved through `/grill-me` (30 questions across 4 gray areas) followed by **three full `/adversarial-review` rounds**. R1: 3 parallel Finders (82 findings) → Adversary (69 disproofs claimed) → Referee (27 confirmed / 55 false positives, + 3 missed by all agents). R2 on the amended set: 3 Finders (69) → Adversary (54 claimed) → Referee (23 confirmed, + 3 missed). R3 on the twice-amended set: 3 Finders (52) → Adversary (43 claimed) → Referee (13 confirmed, + 1 missed). **Confirmed counts ran 27 → 23 → 13; zero architectural defects survived R3, and R3 produced exactly one substantive finding.** A fourth round was assessed as negative expected value.

**⚠ R1's fixes introduced FIVE fresh false premises** — the exact defect class they were fixing. R2 caught all five and imposed two process rules (below). **Downstream agents MUST NOT regress to any superseded version — see the ⚠ markers.**

</domain>

<decisions>
## Implementation Decisions

Decision IDs are grouped by area. **⚠** = a ruling that corrects an earlier ruling in this review — do not regress. **[SRC]** = a claim verified against a file:line during R3's sourcing audit.

### ⚠ Anti-regression traps (these overturn earlier drafts of this same CONTEXT)

- **T-1 — There is NO "the editor replaces the entire payload" mechanism.** R1 asserted it as the basis of a CRITICAL rewrite; a repo-wide grep for `replaces the entire|entire policy payload|whole payload|Enter JSON data` returns **zero hits**. It was invented. Do not reintroduce it, and do not build a ruling on the replace-vs-merge semantics of the Intune JSON editor without a first-party fetch.
- **T-2 — The anchor DOES state the two exit PINs must match, four times** (`05:245`, `05:251`, `05:253`, `05:255` — the last reads *"Both must be set to the same value."*). R1 asserted it "never states they are the same value"; that was false and made a delta claim into re-authoring. What the anchor does **not** state is that the two **GUI labels** name the same field — that, and only that, is RE-225's delta.
- **T-3 — `ROADMAP.md:84`'s definite singular is NOT "textual and unanswerable".** R2 grounded the Case-2 ruling on *"**The** Case-1 decision block … and **a second fork**"* declining to type the second. R3's Referee ruled that silence is a gap, not a lock. **[SRC]** The decisive ground is `EEE-SOP-standard.md:479`: Case 2 is *"pick one of a fixed set, **no procedure fork**"*, and the provisioning method does not fork RE-225's remaining steps (`STACK.md:95`: RE-225 starts post-enrollment).
- **T-4 — "Un-anchored" means the ANCHOR does not own it, not that it is unsourced.** R3's Finders read R4.3's "genuinely un-anchored" as "no first-party source exists" and concluded the section was unauthorable. Wrong: anchor-unowned content is precisely what delta discipline **licenses** RE-225 to author.
- **T-5 — `PROJECT.md:912`'s "Case-1b" is a disposed party, not an uncited counter-source.** **[SRC]** `REQUIREMENTS.md:96` names that exact string and rules it *"not a defined STD-05 case type — pick one."*
- **T-6 — `c17-eee-contract.mjs:150` is the `inCodeFence` IIFE opener, NOT the fence regex.** **[SRC]** The `^`-anchored regex is `:158` (a second at `:166`). `REQUIREMENTS.md:68` has it right; **six** sites have it wrong — see D1.3.
- **T-7 — Indented blockquotes are INVISIBLE to C17 `#12`.** **[SRC]** `c17-eee-contract.mjs:393`/`:396` test `/^>/`, column-0 anchored. Recipe 02 indents three of its four callouts (`02:88`, `:146`, `:219`) and all three `Ask the admin` lead-ins (`02:102`, `:107`, `:120`) by three spaces — as does `recipe-template.md:112-113`, the worked runbook-link instance D3.8 makes live. Missed by all nine agents across three rounds. See D3.9.

### Area 0 — Cross-cutting rules

- **D0.1 — The SC1/SC2 tension is named, not straddled.** SC1 says the exit-PIN sync is *"cross-linked (never re-authored)"*; SC2 and MHS-02 mandate it ship `[MEDIUM: MS Q&A community]` with only the date refreshed. A marker cannot attach to a bare link. **One marked host sentence (D1.7) is the minimum satisfying both; everything beyond it is a link.** State this reconciliation in the phase SUMMARY so a verifier reading SC1 verbatim does not read the sentence as re-authoring.
- **D0.2 — Permitted in-body explanation.** **[SRC]** D6.3 (`135-CONTEXT.md:140`) bars self-narration of **precedent**; D2.2 (`:76`) bars a live doc annotating its own **template divergence**. Neither bars reader-facing explanation of why a value is stated or omitted — recipe 03 ships that class at `03:190` (*"Not in the worked payload"*) and **`03:197`** (*"Omitted here, and absence gives the reapply-every-login behaviour a kiosk wants"*). This settles the R1.6-vs-R2.6 dispute R2 identified.
- **D0.3 — Sourcing discipline for this phase's own artifacts.** Every claim in `136-CONTEXT.md`, `136-01/02-PLAN.md`, `136-VERIFICATION.md` **and `136-01/02-SUMMARY.md`** of the form *"the repo supplies X"* / *"X is the only Y"* / *"no source states Z"* carries a file:line or is struck. SUMMARY is bound because it is the DEFERRED-CLEANUP carrier Phase 138 transcribes verbatim into a shipped milestone deliverable. This is the milestone's own D2.5/B-6 cite-it-or-cut-it rule applied to the planning surface — the one it had never been applied to, and the surface on which R1 produced five false premises.
- **D0.4 — Conditional-ruling budget.** Any deferral states, in the same sentence, why the answer is not already in the repo. Four R2 deferrals failed that test and are closed. **Only two conditionals survive** (D1.5's fence key, D2.7's sibling key name), both keyed to one named Plan-1 output.

### Area 1 — MHS config surface + JSON payload

- **D1.1 — Sole worked surface: the App Configuration policy** (Apps → Configuration → Managed devices → Android → Managed Home Screen, `com.microsoft.launcher.enterprise`), with one cross-linked exception — the exit-lock-task password, which the anchor places in the Device Restrictions profile (`05:251`, cross-linked whole; the recipe does not re-enumerate its settings). **[SRC]** A real source conflict exists and is Plan-1-gated: `STACK.md:55` types `exit_lock_task_mode_code` **"(device-restrictions-profile only)"** — verbatim exclusive — while `05:249-255` says the PIN *"requires configuration in two separate Intune policies"* and `05:253` places it in the MHS policy. The self-consistency clauses do not resolve it: `:43`'s *"(matches what 05 already documents)"* attaches to the DR-profile requirement, `:55`'s *"already documented in 05"* attaches to the GUI **label**. `05:249-255` is already SC6's third chartered cluster, so the gate costs zero extra work. **On the `05:253`-confirmed branch, the `STACK.md:55` correction lands as a named research correction per D8.2** (`135-CONTEXT.md:160`, the in-flight `457adc25` pattern); on the other branch it is HYG-06 drift → D4.5's DRIFT branch.
- **D1.2 — Works the "Enter JSON data" editor**; names the Configuration designer in one line as the equivalent. **[SRC]** Precedent in the same directory: `02-zero-touch-portal.md:116` and `07-knox-mobile-enrollment.md:157` ship column-0 ```` ```json ```` fences each followed by a **Fields reference:** table (`02:130`, `07:161`). The *"Paste the JSON below…"* lead-in idiom is `02:106` **only** — `07:147` uses a different lead-in.
- **D1.3 — Column 0, mandatory** (inherits D1.2 from Phase 135). **[SRC]** C17's fence regex is `c17-eee-contract.mjs:158`, `^`-anchored; `convert.ps1:108` uses `^\s*`; column 0 is the only agreeing position. **The wrong `:150` coordinate appears at SIX sites** — `135-CONTEXT.md:48`, `:204` (inside `<canonical_refs>`), `:287` (the `<deferred>` entry), `135-01-SUMMARY.md:113` (itself a DEFERRED-CLEANUP contribution), `135-02-SUMMARY.md:196`, `135-PATTERNS.md:312`. The correction against all six is appended to the existing fence-mask `v1.19-DEFERRED-CLEANUP.md` entry, carried as a flagged `136-01-SUMMARY.md` contribution.
- **D1.4 — STD-05 D-03/D-04 do not bar the fence.** The JSON is a payload artifact; D-04 rule 2's subject is "Branch bodies" and RE-225 authors no `### Branch` heading. **[SRC]** `EEE-SOP-standard.md:496-498` defines decision content as *"the prompt, options, or consequences"* — a recorded **value** is none of the three, which also settles the boundary after D2.8 moved keys across it. HYG-05 landed: `:496-497` now reads *"fenced content **is** indexed, but as non-prose runs that retrieve poorly."*
- **D1.5 — ONE column-0 fence** carrying the App Configuration payload. ⚠ The R1 "complete payload" framing and its replace-the-entire-payload basis are STRUCK (T-1). Whether `exit_lock_task_mode_code` belongs in it is D1.1's Plan-1 question: **if Plan 1 confirms `05:253`**, the key ships with a **placeholder** value (never a concrete PIN) plus an adjacent cross-link to `#exit-kiosk-pin-synchronization`, per the corpus placeholder-lead-in idiom **[SRC]** `docs/admin-setup-macos/10-kerberos-sso-extension.md:92`; **if Plan 1 confirms `STACK.md:55`**, the key is absent and the prose line after the fence states the PIN is set in the **Device Restrictions profile**. ⚠ R1 asserted the opposite ("set on this same policy") on precisely the branch that disproves it — do not regress. — **Reversibility:** costly — the fence's key set determines the decomposition table, the Verification lines and the failures rows.
- **D1.6 — `enable_mhs_signin: false` ships in the fence; `Enable easy access debug menu = FALSE` does not** and demotes to a Verification line. **[SRC]** Locked at requirement level, not editorially: `REQUIREMENTS.md:24` calls `enable_mhs_signin = FALSE` *"the worked value"*; `:25` mandates the debug-menu demotion *"from a Step to a Verification line"*. R1's "must be justified in-text" clause stays deleted on the **unfalsifiability** ground alone; ⚠ its second ground (a D6.3 collision) was struck — D0.2 governs.
- **D1.7 — One host sentence, CLOSED (no longer conditional).** ⚠ R1's premise was false (T-2) and R2's escape hatch was already satisfied by four lines it itself cited. Ruling: RE-225 carries **one host sentence** in the exit-PIN step naming both GUI labels ("Leave kiosk mode code" / "Exit lock task mode password"), stating the values must match, carrying `[MEDIUM: MS Q&A community, last_verified <fresh date>]` and the cross-link to `#exit-kiosk-pin-synchronization`. **This single sentence is SC2 clause 2.3's carrier** (D0.1). No separate label-equivalence line; no anti-feature row.
- **D1.8 — Decomposition table:** `| Setting / JSON key | Configuration designer label | What it does | Worked value |`. ⚠ R1's "the repo supplies labels for only three keys" was FALSE — **[SRC]** `FEATURES.md:93` ("Enable sign in", "Sign in type"), `:99` ("Lock Home Screen", "Application order enabled"), `:82` (**three** labels incl. "Enable easy access debug menu"), `:72` ("Create Managed Folder for grouping apps"). Only `applications` and `grid_size` lack labels. **The column always exists**; only cells vary (label where first-party-documented, em-dash otherwise). Column 1 is headed `Setting / JSON key` so a GUI-only row can carry a label with an em-dash key. **[SRC]** Recipe 03's table is `| # | Field | What it is | Decision-relevant semantics |` (`03:179`) — its first column is `#`. **Rule the `#` column IN** for parity with the precedent.
- **D1.9 — Markers never inside the fence** (`//` is not legal JSON). **[SRC]** `PITFALLS.md:310` offers exactly two forms and `REQUIREMENTS.md:24` positively mandates the split blockquote for `[ASSUMED]`: so **`[ASSUMED]` = split blockquote ONLY; `[MEDIUM]` = inline or table-row permitted.**
- **D1.10 — Fence placement inherits D1.7** (Phase 135, corrected form): column-0 fence immediately after the numbered click-list inside the authoring step, then the table. Order per D2.9.

### Area 2 — Fork taxonomy + decision blocks

- **D2.1 — The four-way provisioning fork is Case 2.** ⚠ Grounded on **[SRC]** `EEE-SOP-standard.md:479` — Case 2 is *"pick one of a fixed set, no procedure fork"* — and the method does not fork RE-225's remaining steps, which start post-enrollment (`STACK.md:95`). Supporting: `FEATURES.md:127` types it Enumerable; `REQUIREMENTS.md:96` charters the phase to pick and disposes of "Case-1b" (T-5). ⚠ Do NOT re-ground on `ROADMAP.md:84`'s definite singular (T-3), on "no branch section exists" (false — `05:198/206/212/220`), on arm convergence (D-05 permits both), or on a "zero worked arms" arithmetic floor (invented; `135-02-SUMMARY.md:216` imposes none). — **Reversibility:** costly — the case type determines the table shape and where the Knox/ZT exclusion can live.
- **D2.2 — The Knox/Zero-Touch exclusion ships via two carriers, both link-only:** an anti-feature row (**[SRC]** the shape `FEATURES.md:100` prescribes, annotated *"Cross-link, don't re-author"*) and one routing sentence in plain prose immediately after the Case-2 table — **not** a blockquote (Pitfall 18's house remedy). **[SRC]** `PITFALLS.md:217` bars the strings *"Knox Mobile Enrollment"/"KME"* **outside a link** — it does **not** bar "Samsung" or "CRITICAL", so a row reading *"Configuring Zero-Touch and [Knox Mobile Enrollment](…#provisioning-method-choice) simultaneously on Samsung hardware | a CRITICAL-severity mutual exclusion …"* satisfies the constraint while carrying the severity and the Samsung scoping SC2 clause 2.4 requires.
- **D2.3 — The token-type Case-1 block's SDM arm routes its `Branch` cell straight to the anchor** (`#scenarios` plus Delta 1 at `05:116`). **[SRC]** An **independent ruling that declines to extend D4.3**, NOT a divergence from an inherited lock — the D6.1 handoff rows are (a) `135-02-SUMMARY.md:214` … (g) `:220` and **D4.3 is not among them**; (c) locks the shape, not the destination. D4.3's premise ("no destination otherwise exists") is false here: `05:74`, `05:121`, `_glossary-android.md#entra-shared-device-mode` (linked live at `05:58`).
- **D2.4 — Token type is Step 1**, a confirm-and-route step ("confirm the enrollment profile that provisioned this device uses the Standard token; if it uses SDM, this recipe is not your path"), not profile creation — re-documents no token-type content and routes to the anchor, honouring `REQUIREMENTS.md:22`'s "enrollment-profile deltas … cross-linked, never re-authored".
- **D2.5 — The sign-in Case-2 block is its own step immediately before the payload step.** Arms: FALSE (worked) / TRUE + `Sign in type = Other` / TRUE + `Sign in type = Microsoft Entra ID`. **[SRC]** The Entra-ID negative carries its **account-type** scope verbatim per `FEATURES.md:93`'s corrected text; `FEATURES.md:128`'s "zero SSO benefit" framing is **withdrawn** by `:93` and must not be imported. The anti-feature row anchored on the documented default (`signin_type` defaults to "Microsoft Entra ID") ships per `REQUIREMENTS.md:24`, which supplies its content word-for-word — D3.3's OUT list governs the fence only, not this row.
- **D2.6 — MHS-04's "physically separated"** = sign-in block before the payload step, exit-PIN + hardening after it. The hardening step is explanation-and-verification for keys already in the payload; the exit-PIN half is a cross-link on both D1.5 branches because **[SRC]** `REQUIREMENTS.md:22` and `FEATURES.md:71` bar re-authoring the sync requirement. Its "and says so" statement is permitted under D0.2. The silent no-op dependency ships verbatim as a one-clause `> **What breaks if misconfigured:**` with detail in plain prose beneath. The retry-delay's missing unit is stated as an explicit unknown.
- **D2.7 — RESEARCH FLAG (surviving conditional).** **[SRC]** `STACK.md:55` names only `max_number_of_attempts_for_exit_PIN` and `:54` lists no `max_number_of_attempts_*` among the sign-in keys, so MHS-04's *"two near-identically-named settings with opposite gates"* is unsourced on its second term. **Genuinely unanswerable from the repo** — `grep -rn "max_number_of_attempts" --include=*.md .` returns exactly two hits (`REQUIREMENTS.md:25`, `STACK.md:55`). Plan 1 names the sibling first-party from `configure-managed-home-screen` **or** the framing softens to what is sourced; on the soften branch the correction lands as a named requirement per `ROADMAP.md:90`'s HYG-04 pattern.
- **D2.8 — Decision-point dispositions.** `grid_size` and `managed_folders` are **Case-3 free-value prompts** (worked; **[SRC]** `EEE-SOP-standard.md:481` — lead-in alone, no table). **Wallpaper and screensaver decline with a reason** (RCPFUT-05/04). ⚠ R2 declined all four of `FEATURES.md:131`'s #6 while D3.3 sets two of them — the recipe would have declined in prose what it sets in the fence. Decision #7 (virtual home button — **[SRC]** `FEATURES.md:132` types it **Enumerable**, not free-value) is declined with a reason recording it was a Case-2 candidate. #5 is sign-in-TRUE-scoped (`FEATURES.md:84`, `:130`) and #8 likewise (`FEATURES.md:133`); both land as one line in the sign-in block's non-worked arms. **[SRC]** Declined items ship as one sentence each in the payload step's lead-in — the `135-02-SUMMARY.md:230` idiom (*"were also omitted, with one sentence saying so"*) — **not** as anti-feature rows.
- **D2.9 — `## Steps` skeleton, stated once:** Step 1 token-type confirm-and-route (+ the provisioning-method Case-2 block; **[SRC]** its `Recorded as` artifacts are anchor-named at `05:172` "QR payload, NFC tag, or Zero-Touch DPC extras JSON" and `05:196` "afw#setup (DPC identifier)") → Step 2 deploy MHS as Required → Step 3 deploy the allow-listed apps → Step 4 sign-in mode Case-2 block → Step 5 author **and assign** the App Configuration policy payload (+ the two Case-3 prompts) → Step 6 exit-PIN cross-link + hardening. **[SRC]** Multiple decision blocks in one step is corpus-precedented: recipe 01 ships two `Ask the admin` blocks in one step (`01:168`, `01:175`), recipe 02 ships three (`02:102`, `:107`, `:120`).
  - **⚠ D2.9a — The Step-2→Step-6 no-exit-PIN window is the phase's one substantive R3 finding and must be closed or called out.** **[SRC]** `05:145`: a Required assignment makes the device boot into MHS. `STACK.md:95` + D2.4: a real device is already enrolled and in the target group when Step 2 lands. `05:255`: a device with no exit PIN shows *"A PIN to exit kiosk mode has not been set by your IT admin"* at exit attempt; `05:302` ranks that CRITICAL, first row; `05:257` puts sync at *"typically within 8 hours"*. `FEATURES.md:82`: the 15-press gesture leads to the exit-PIN prompt — so during the window there is **no exit affordance at all**. Resolution: **either** author and assign both PIN surfaces before Step 2's Required assignment, **or** carry an explicit Step-2 callout naming the window plus the `05:257` forced-sync remediation, and a `## Configuration-Caused Failures` row. This is a failure RE-225's own steps cause, which is D3.4's table scope. — **Reversibility:** one-way within the phase — shipping the window unaddressed puts a documented CRITICAL state in an Approved recipe.
- **D2.10 — Pitfall 20 carry-forward:** **every** Step line and decision-table cell whose underlying fact comes from a `05` MEDIUM-tagged section carries an equivalent marker forward (inline, or the `> **[ASSUMED]:**` idiom per D1.9). **[SRC]** `05:114` tags the entire Delta-1..4 block (scope per `PITFALLS.md:308`) — which includes Delta 3, the source for Step 2 — so the rule is unrestricted; the token-type content is the instance R1's Referee confirmed was missing, not a carve-out. **A marker qualifies the SOURCE, not the SEVERITY:** MHS-02's `05:129` revoke/recreate/redistribute cell ships its full operational consequence **and** its marker. Pitfall 11 bars softening the consequence; Pitfall 20 bars stripping the signal — orthogonal axes.
- **D2.11 — Step 5 states the App Configuration policy's own assignment** — the action that makes the payload take effect, and a distinct Intune blade from the app assignment (Step 2). Target: **[SRC]** the static Entra device group `FEATURES.md:70` mandates and `05:145` names. Unruled in every prior round; **[SRC]** the anchor's own Delta-3 failure mode (`05:149`) is an assignment mistake, so leaving the policy's assignment unstated reproduces the class one blade over.

### Area 3 — Tables + C17 budget

- **D3.1 — No merge.** `## Unsupported and Anti-Feature Callouts` keeps its 3-column shape. **[SRC]** `FEATURES.md:93-101` is exactly 9 rows and every item MHS-05 names is already among them, so MHS-05 adds zero. D1.7's host sentence is a Steps line and D2.8's declined items are payload-step sentences — **neither is a row**. Range **9-10** (recipe 03 shipped 12 at `03:45-56`); the 10th is D3.3's M-A item if it lands as a row rather than a lead-in sentence — **rule which at author time and record it**.
- **D3.2 — A plain-prose summary ships under the decomposition table unconditionally.** **[SRC]** C17 `#11` counts the **header** row (`c17-eee-contract.mjs:347-360` excludes only separators), so recipe 03's 22-content-row table is 23 `dataRows` and real headroom against `>25` is two rows.
- **D3.3 — Bounded key set.** **IN the payload:** `applications` (sub-keys per D3.6), `grid_size`, `lock_home_screen`, `managed_folders`, `max_number_of_attempts_for_exit_PIN`, `amount_of_time_before_try_exit_PIN_again`, `enable_mhs_signin: false`, plus `exit_lock_task_mode_code` per D1.5's gated resolution. **IN the decomposition table but NOT the payload** (recipe 03 rows 10/16/17 precedent): the debug-menu setting, as a GUI-label row with an em-dash key. **OUT with a stated reason:** screensaver keys (RCPFUT-04), widgets/wallpaper/branding (RCPFUT-05), `app_orders`/`app_order_enabled`/`icon_size`/`app_folder_icon`/`screen_orientation` (cosmetic — an anti-feature "Do this instead" cell may still name `Application order enabled`, as recipe 03 does repeatedly), sign-in sub-keys (D3.6 states its own carve-out). **M-A: the OEMConfig Overlay/exact-alarm grant is OUT** — **[SRC]** `REQUIREMENTS.md:26` MHS-05 adjudicates it at requirement level: the Notification-windows row is phrased conditionally *"because **zero dependents exist among the settings this recipe sets**"*, and the dependents (screensaver, virtual home button, auto-sign-out, notification badges, auto-relaunch) are RCPFUT-04/05-deferred or live only in non-worked arms. **[SRC]** `STACK.md:83` (once, not twice) calls it "new prerequisite content". Row total in D3.2's units: **10-16 `dataRows`**.
- **D3.4 — The `## Configuration-Caused Failures` table does not merge with the anchor's 12-row What-breaks summary**; RE-225 authors its own covering only failures its own steps cause (including D2.9a's window and D4.6's two demoted checks). D6.1(g) bars *fabricated* runbook links and its disposition is *"Inherit as an explicit named non-inheritance"*. **[SRC] The real candidate set** (R1 and R2 both surveyed only the four L1 runbooks `05:30` names): `l1-runbooks/00-index.md:88-89` also has **27** (ZTE Enrollment Failed) and **28** (Knox Enrollment Failed) — direct fits for D2.2's carriers — and `l2-runbooks/00-index.md:168-169` has **19** Android Enrollment Investigation ("Dedicated QR failure") and **20** Android App Install Investigation ("MGP or LOB app shows Failed, Not installed, or Pending"), the latter a direct fit for Steps 2/3's dominant failure mode. Route to a real runbook only where it genuinely covers the symptom, otherwise in-recipe anchors (`03:311→#step-5` idiom); verify every target at author time.
- **D3.5 — Fourth column is `Worked value`;** defaults stated in the "What it does" cell where decision-relevant (`enable_mhs_signin` FALSE, `lock_home_screen` TRUE, debug-menu FALSE in its non-payload row). Permitted reader-facing explanation per D0.2.
- **D3.6 — `applications` sub-keys, ruled explicitly.** **[SRC]** `STACK.md:50` types it a bundleArray of `{package, enable_app_offline, app_available_prior_to_sign_in}`. `package` is worked; `enable_app_offline` and `app_available_prior_to_sign_in` are omitted with the reason — **[SRC]** `FEATURES.md:84` and `:130` both scope offline/pre-sign-in app access *"only meaningful if `Enable sign in = TRUE`"*, so both are inert at `enable_mhs_signin: false`. **This is a named carve-out from D3.3's "sign-in sub-keys are OUT"**, since `app_available_prior_to_sign_in` sits inside a key D3.3 puts IN — the collision is stated, not latent.
- **D3.7 — C17 `#11` general rule:** a `> **Note:**` summary does NOT satisfy it — **[SRC]** the validator rejects lines starting with `|`, `#`, `>`, or a fence within a 5-line window in which blank lines consume slots (`c17-eee-contract.mjs:361-372`). Plain prose only, pre-emptively at 20+ `dataRows` for every table other than the decomposition table (D3.2 governs that one unconditionally).
- **D3.8 — Steps-callout runbook links.** **[SRC]** `recipe-template.md:29` reads verbatim *"Link to relevant troubleshooting runbooks from 'what breaks' callouts"*, worked instance at `:112-113` (`135-CONTEXT.md:200` cites the pair). ⚠ R2's "live for the first time" is STRUCK as false: **[SRC]** iOS L1 runbooks 16-21 and `l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` exist, and `grep 'l1-runbooks\|l2-runbooks' docs/recipes/*.md` returns **0** across all three shipped recipes — RE-223 had live targets and declined. Ruling: the corpus has silently declined `:29` twice with targets available; **RE-225's compliance is a deliberate, named first**, recorded the way D6.1(g) records its non-inheritance. D3.4's routing discipline governs.
- **D3.9 — Blockquote discipline, with the indented-vs-column-0 question ruled.** **[SRC]** ⚠ T-7: C17 `#12` is column-0-anchored, so recipe 02's indented callouts and lead-ins escape the cap entirely while recipe 01's and 03's do not. **Ruling: column 0 for every blockquote RE-225 ships** — consistency with the immediately-preceding recipe and with the D1.3 column-0 fence rule beats cap relief, and an indented callout that silently escapes `#12` is a latent defect for the next retrofit. Consequently every blockquote is live: Scope banner, ~6 `What breaks if misconfigured` callouts (**[SRC]** corpus practice is per-**step** — recipe 01 = 1, 02 = 4, 03 = 6 against a 22-field payload — not per-setting), **five `Ask the admin` lead-ins** (D2.9's three blocks + D2.8's two Case-3 prompts; **[SRC]** `EEE-SOP-standard.md:472-473` warns a faithful lead-in *"measures roughly 228 characters and hard-fails C17 #12"*), and any `[ASSUMED]` note. For each: measure after the sentinel strip and split into blank-line-separated sub-200-char runs per the `01:101`/`01:103` idiom rather than truncating — **[SRC]** Phase 135 measured four callouts at 220/213/223/245, four for four over the cap. The one-clause + plain-prose-beneath remedy (`PITFALLS.md:282`) is scoped to `What breaks if misconfigured` callouts **only**; `[ASSUMED]` notes take the split-run remedy alone (D1.9). D3.8's `> See: [runbook]` line lands as its own run inside the callout.

### Area 4 — Anchor, Rollback/Recovery, identity, HYG-06

- **D4.1 — No new taxonomy anchor.** One taxonomy sentence in the Scope banner plus a cross-link to `_glossary-android.md#dedicated` — **[SRC]** structurally what the anchor does at `05:32-36`, and it *also* repeats the link in See Also (`05:344`), so D4.9(iii) is additive not a relocation. Zero frozen-file edits; the anchor is live at `_glossary-android.md:107`. **Option B is barred at requirement level:** `ROADMAP.md:104` SC2 locks the canary at "223 → 225" and `REQUIREMENTS.md:30` calls it a named deliverable, so a third RE-NNN falsifies a locked SC. Option C is ruled out at `REQUIREMENTS.md:98`. Option B → a `v1.19-DEFERRED-CLEANUP.md` entry, carried as a flagged `136-01-SUMMARY.md` contribution (HARN-16 creates the file in Phase 138; the `135-02-SUMMARY.md:193` pattern).
- **D4.2 — Scope banner:** assume overflow (recipe 03 measured 249 chars and shipped as three runs); measure after the sentinel strip; split, never truncate. General rule at D3.9.
- **D4.3 — `## Rollback/Recovery` SHIPS**, as a named documented divergence in RE-224's shape (`03:275-306`), positioned **between `## Verification` and `## Configuration-Caused Failures`** — the only corpus instance and therefore the insertion point, giving RE-225 a deterministic 8-H2 set. ⚠ R1 ruled it omitted and R2 ruled it conditional; the conditional was **self-discharged** — its own premise named four un-anchored facts, so the gate was satisfied before Plan 1 could run. **[SRC]** The three procedures the anchor does not own: (i) unassigning the MHS app-config policy / setting MHS to Uninstall to return a device to the standard launcher — `05:151` gives the *assignment* procedure and `05:316-330` covers renewal, neither the reverse; (ii) a **forgotten** (not mismatched) exit PIN — `05:257` says *"Verify both policies carry the same code"*, presupposing a known code; (iii) an offline device with a wrong `applications` allow-list. ⚠ **(iii) is the set's one un-cited premise — cite it first-party at author time or drop it.** T-4 governs: un-anchored means the anchor does not own it, which is delta discipline's licence to author, not a bar. **[SRC]** No `REQUIREMENTS.md` line is needed — `135-02-SUMMARY.md:219` D6.1(f) verbatim hands the case to *"Phase 136 rules its own case"*, and D4.7's gate governs deferrals, of which this is none. R1's "a failures-table row" destination stays STRUCK for un-anchored **procedures**: **[SRC]** that table is fixed at `| Misconfiguration | Symptom | Runbook |` and `03:309-319`'s Runbook cells route to in-recipe **sections**. (It remains available for rows that point at an existing section — see D4.6.) Honest comparison: RE-224 carries the section because `REQUIREMENTS.md:17` KIOSK-04 **mandates** it (`135-CONTEXT.md:78` D2.4: *"KIOSK-04 + SC4 lock it"*) — the grep was supporting evidence, not the cause. **The divergence is named in `136-02-SUMMARY.md` + a `v1.19-DEFERRED-CLEANUP.md` entry, never as body meta-commentary** (D2.2; `03:275-306` contains zero self-annotation). — **Reversibility:** costly — adding or removing an H2 after publish re-runs the bundle and changes the H2 set Phase 138 pins.
- **D4.4 — File identity:** `docs/recipes/04-android-dedicated-mhs-multi-app.md`, `doc_id: RE-225`, `status: Draft`, `doc_type: Guide` (from `recipe-template.md:37`; `:13-14` forbids any other value), **and `platform: Android`** — **[SRC]** C17 `#10` (`c17-eee-contract.mjs:334-341`) is the only assertion whose unmapped value is *"a hard failure … no fallback"*, and D4.4's own success criterion is C17-green between waves. Registry verified at 223 rows, tail RE-223, RE-224/225 absent; Phase 137 owns both rows, the Draft→Approved two-site flip (C17 `#9`), and the canary. **"File identity" = a sentinel-free, independently C17-clean shell** — **[SRC]** `ROADMAP.md:69` says *"sentinel-free frontmatter, EEE block, H1 and Summary"*; the "exactly one H1" and "≥30-word Summary" specifics come from C17 `#2`/`#4`/`#5` and commit `ef155268`, which shipped exactly that 19-line shape.
- **D4.5 — HYG-06 scope:** the three SC6 clusters (`05:116-131`, `05:143-153`, `05:249-255`) plus the `enable_mhs_signin`/`signin_type` default, the two exit-PIN retry keys and D2.7's unnamed sibling, from `configure-managed-home-screen` (updated 2026-06-22, fresher than the anchor's `last_verified: 2026-04-23`). ⚠ The R1 designer-label extension is deleted — D1.8 shows the labels are already in the repo.
  - **NO-DRIFT branch:** no `last_verified` bump, **and the no-op is recorded explicitly** — `136-01-SUMMARY.md` records the fetch date, the three cluster names and "no drift", and `136-VERIFICATION.md` asserts it. ⚠ R1 deleted this carrier; **[SRC]** SC6 (`ROADMAP.md:88`) and HYG-06 both require *"or the no-op recorded explicitly"*. The anchor's past-due `review_by: 2026-06-22` gets a `v1.19-DEFERRED-CLEANUP.md` entry **on this branch only**.
  - **DRIFT branch:** the named correction updates `last_verified` — **[SRC]** `v1.18-REQUIREMENTS.md:43` HYG-04 verbatim (*"this is a content correction, so `last_verified` updates"*) — **and bumps `review_by` with it** at the doc's own 60-day cadence (`05:324`); bumping one without the other ships `review_by` earlier than `last_verified`. No cleanup entry on this branch.
  - Never an unlogged drive-by edit. **[SRC]** The anchor is pin-free — `grep -rl '05-dedicated-devices' scripts/validation/` returns nothing — and Pitfall 16 / CARVE-1 are scoped to `_glossary-android.md` and `android-capability-matrix.md` only (`ROADMAP.md:94`).
- **D4.6 — Both deliberately-break-it checks demote.** The mismatched-PIN case and the max-attempts lockout each ship as a `## Configuration-Caused Failures` row (the lockout may instead be a `What breaks if misconfigured` note on the hardening keys); each row's Runbook cell points at the hardening step, the `03:311→#step-5` idiom. **⚠ The Verification checklist keeps every positive form** — the six non-negative items at `FEATURES.md:175-182` (grid shows exactly the allow-listed apps; Home/Overview/back does not surface the standard launcher; the exit-PIN prompt appears after the back-button gesture and accepts the configured code; folder/app immobility; Wi-Fi radio behaviour; admin center confirms the Standard token type) **plus D1.6's debug-menu line**, which `REQUIREMENTS.md:25` mandates. ⚠ R2's "carries **only** positive forms: [two items]" read as a closed list that deleted four checks and a requirement-mandated line — the word "only" is struck.
- **D4.7 — Two plans, no third pre-allocated.** Plan 1 (wave 1, `depends_on: []`): HYG-06 spot-verification at D4.5's scope + every landing-spot decision + the D4.4 shell. Plan 2 (wave 2, `depends_on: ["136-01"]`): author the body. **[SRC]** `ROADMAP.md:90` permits material drift as a named requirement and says *"no third plan is **pre-allocated**"* — a budgeting statement; `135-CONTEXT.md:156` D8.1 draws the same distinction. **Only two structural parameters remain Plan-1-gated:** the fence's exit-PIN key (D1.1/D1.5) and D2.7's framing. The gate — *"no deferral leaves the phase without a REQUIREMENTS.md line or a DEFERRED-CLEANUP entry"* — governs D4.1 (Option B), D4.5 (`review_by`, no-drift branch), D1.3 (the six-site coordinate correction) and D4.3's naming entry; D2.7 and D1.1's research correction resolve in-phase as named requirements/corrections per `ROADMAP.md:90`.
- **D4.8 — `## Prerequisites` opens with `**This recipe is NOT:**`** (Phase-130 H-LOCK-5 is permissive; **[SRC]** `PITFALLS.md:240` charters *"the Android recipe's equivalent"* to Phase 136). Terms: Entra shared device mode (**[SRC]** Pitfall 15's **headline** collision — `PITFALLS.md:227`), single-app kiosk / Lock Task Mode, digital signage / screensaver mode (RCPFUT-04), and Windows multi-app kiosk (RE-224's colliding name; no link — recipe 03 is `status: Draft` until Phase 137, and `01:29`'s own idiom links none of its terms). COSU/COBO is **[SRC]** not carried: `PITFALLS.md:236` scopes the duty to *"whichever of these terms appears in its own prose"* and `:234` states COSU's risk as a future capability-matrix author. **Cross-link targets corrected:** **[SRC]** `_glossary-android.md` has `#dedicated`, `#entra-shared-device-mode`, `#managed-home-screen` but **no** anchor for Lock Task Mode, single-app kiosk or digital signage — the anchor doc proves it, since `05:62`'s "Lock Task Mode" bullet is the one terminology bullet with no link. Those three route to `05-dedicated-devices.md#scenarios`. Zero new glossary terms.
- **D4.9 — `## See Also`.** ⚠ R2's *"the only other candidate home"* was false — `## Verification` is mandated unconditionally. **[SRC]** `PITFALLS.md:201` (Pitfall 13, Phase-136-assigned) names *"RE-225's **Verification or Recovery** section"* for the Android-15-FRP cross-link; **primary placement is `## Verification`**, See Also additive. Contents: (i) `#android-15-frp-reprovisioning` — SC1 mandates this cross-link verbatim, and `FEATURES.md:155` bars re-authoring the 3-pathway table, not the link; (ii) `01-managed-google-play.md#bind-mgp` (`ARCHITECTURE.md:61`); (iii) `_glossary-android.md#dedicated`; (iv) the anchor sections the body cross-links; (v) `EEE-SOP-standard.md` STD-05. **[SRC]** Section-anchor entries are precedented — recipe 03's last See Also entry is `[Step 5a: Kiosk configuration](../recipes/01-…)` and `05:344-347` carries four glossary anchors its body already links. Link syntax is `../`-prefixed; D3.4's author-time verification and Phase 137's link-checker 0/0 are complementary gates.

### Closure — the artifact's global properties, re-derived from the rulings above

Written because three rounds of pairwise fixes never re-counted, which is how "three decision blocks" survived contradicting its own Case-3 ruling one sentence later. Phase 138 authors `check-phase-136.mjs` from `136-VERIFICATION.md`; these are the exact-count needles it can assert.

| Property | Value | Source |
|---|---|---|
| H2 set | **8**, template order + `## Rollback/Recovery` between `## Verification` and `## Configuration-Caused Failures` | D4.3 |
| `### Step` headings | **6** | D2.9 |
| Decision blocks | **5** — 1 Case-1 (token type), 2 Case-2 (provisioning, sign-in), 2 Case-3 (`grid_size`, `managed_folders`) | D2.1/D2.5/D2.8 |
| `> **Ask the admin:**` lead-ins | **5**, one per block, all column-0, each ≤200 chars per run | D3.9 |
| `What breaks if misconfigured` callouts | **~6**, one per step, column-0 | D3.9 |
| Anti-feature rows | **9-10** (10th = M-A if ruled a row) | D3.1 |
| Decomposition table | **10-16 `dataRows`** (header counts), + mandatory prose summary | D3.2/D3.3 |
| Verification checklist | **7** — 6 positives + debug-menu line | D4.6 |
| Failures-table rows | ≥3 — mismatched PIN, max-attempts lockout, D2.9a's window | D4.6/D2.9a |
| Code fences | **exactly 1**, column-0, ```` ```json ```` | D1.5 |
| Frontmatter | slug / `RE-225` / `Draft` / `Guide` / `Android` | D4.4 |
| Conditionals | **2** — fence exit-PIN key, D2.7 framing | D4.7 |

### Claude's Discretion

Genuinely open, to be ruled at plan or author time:
- Exact prose wording throughout — step text, callout phrasing, table cell wording, row order beyond what is fixed.
- Whether M-A lands as an anti-feature row or a payload-step lead-in sentence (D3.1's 9-vs-10).
- Which of D2.9a's two resolutions to take (reorder vs. callout-plus-failures-row).
- The concrete worked values in the payload beyond the bounded key set.
- Which failures rows route to a real runbook vs. an in-recipe anchor (D3.4).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The anchor (RE-225 is a delta over this — read it whole before authoring)
- `docs/admin-setup-android/05-dedicated-devices.md` (RE-097, `last_verified: 2026-04-23`, `review_by: 2026-06-22` — **past due, HYG-06's subject**) — Scenarios `:64-86`; Enrollment profile Deltas 1-4 `:110-168` (**`:114` tags the whole block `[MEDIUM]`**); Enrollment token + token-type table `:170-192`; Provisioning method choice `:193-242` (four H3s at `:198/206/212/220`; Knox/ZT warning `:228`); **Exit-kiosk PIN synchronization `:243-260`** (`:245/251/253/255` — the four sync statements; `:257` remediation); Android 15 FRP `:261-293`; What-breaks summary `:296-315` (`:302` the CRITICAL first row); Renewal `:316-330`; See Also `:344-347`. **Pin-free** — `grep -rl '05-dedicated-devices' scripts/validation/` returns nothing.

### Locked format & class foundation (post-archive paths — the v1.18 dirs moved)
- `.planning/milestones/v1.18-phases/129-device-recipe-doc-class-foundation/129-CONTEXT.md` — D-06 = the fixed H2 skeleton (`:39`); TEMPLATE-SENTINEL skips C17 `#9`/`#12` only.
- `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-CONTEXT.md` — D1.2 column-0 fence; D1.5 row budget; **D2.2 (`:76`) bars body meta-commentary**; D2.4 (`:78`) KIOSK-04 locks RE-224's section; D4.2 (`:105`) the D-01 parenthetical is a case-**selection** criterion; **D6.3 (`:140`) bars self-narration of precedent**; D8.1 (`:156`) the 2-plan split; D8.2 (`:160`) in-flight research corrections; `:179-185` what Phase 135 left open. ⚠ `:48`, `:204`, `:287` carry the wrong `c17:150` coordinate.
- `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-02-SUMMARY.md` — **the D6.1 handoff table `:212-222`** ((a) `:214` … (g) `:220`); D4.2's three grounds `:198-206`; the two DEFERRED-CLEANUP entries `:193-196`; `:227` the 12-vs-10 author-time judgment; `:230` the one-sentence-omission idiom. ⚠ `:196` carries the wrong `c17:150`.
- `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-01-SUMMARY.md:113` and `135-PATTERNS.md:312` — two more wrong-coordinate sites (D1.3).
- `docs/_templates/recipe-template.md` — 7 H2s (`:49/92/98/104/124/129/136`); `:26` a callout per configurable setting; **`:29` + `:112-113` runbook links from callouts**; `:37` `doc_type: Guide`; `:13-19` the frontmatter rules incl. `platform`; `:80-85` the worked Case-3 example.
- `docs/_standards/EEE-SOP-standard.md` — STD-05 `:455-545`: **`:470`** every decision point opens with an `Ask the admin` lead-in; **`:472-473`** a faithful lead-in ≈228 chars and hard-fails `#12`; **`:476-478`** Case 1; **`:479-480`** Case 2 ("no procedure fork"); **`:481-482`** Case 3; `:496-498` **decision content = the prompt, options, or consequences**; D-04 `:502-511`; D-05 `:513-522`.

### The gates (stay green; DO NOT EDIT the validators)
- `scripts/validation/c17-eee-contract.mjs` — **`#10` at `:334-341`** (unmapped `platform` = hard failure, no fallback); **`#11` at `:344-372`** (header counts as a data row; prose-summary scan rejects `|`/`#`/`>`/fence within 5 lines, blank lines consume slots); **`#12` at `:388-407`** (200-char cap on contiguous **column-0** `^>` runs — `:393`/`:396`); **the fence mask is `:158`, `^`-anchored — NOT `:150`**, which is the IIFE opener.
- `scripts/validation/check-phase-129.mjs` — asserts template existence + TEMPLATE-SENTINEL and a `docs/recipes/* → Guide` regex **inside `EEE-SOP-standard.md`**, not recipe frontmatter. No validator asserts an H2 set or order.
- `scripts/validation/check-phase-130.mjs:64,67` — pins recipe 01's Step 5a/5b headings against live HEAD. **Zero edits to recipe 01.**
- `scripts/pipeline/build-filename-map.mjs:281-282` — the `rows.length === 223` canary; Phase 137 bumps it to 225.

### Cross-link targets
- `docs/admin-setup-android/01-managed-google-play.md#bind-mgp` (`:96`) — the MGP binding prerequisite.
- `docs/_glossary-android.md` — `#dedicated` (`:107`), `#entra-shared-device-mode` (`:265`), `#managed-home-screen`. **No anchor exists for Lock Task Mode, single-app kiosk or digital signage** — `05:62` proves it. **Zero edits (365 pin coordinates).**
- `docs/recipes/01-shared-windows-avd-client.md` — `:83` the inline app-assignment click-path precedent (RE-222); `:101`/`:103` the split-blockquote idiom; `:29` the `This recipe is NOT` idiom; `:168`/`:175` two decision blocks in one step.
- `docs/recipes/02-shared-ipad-full-provisioning.md` — `:102`/`:107`/`:120` three decision blocks in one step; **`:88`/`:146`/`:219` indented callouts (T-7)**.
- `docs/recipes/03-windows-11-multi-app-kiosk.md` — `:179` the decomposition table header; `:190`/`:196`/`:197` the non-payload rows; `:275-306` the Rollback/Recovery shape and position; `:309-319` the failures table's in-recipe routing; `:45-56` twelve anti-feature rows.
- `docs/admin-setup-android/02-zero-touch-portal.md:106,116,130` and `07-knox-mobile-enrollment.md:157,161` — the two column-0 ```` ```json ```` + Fields-reference precedents.
- `docs/admin-setup-macos/10-kerberos-sso-extension.md:92` — the placeholder-lead-in idiom.
- `docs/l1-runbooks/00-index.md:88-89` (27 ZTE, 28 Knox) and `docs/l2-runbooks/00-index.md:168-169` (19 Android Enrollment, 20 Android App Install) — the real runbook candidate set.

### Planning inputs
- `.planning/ROADMAP.md` Phase 136 (`:76-94`) — goal `:78`, SC1-SC6 `:83-88`, Plans `:90`, Hard constraints `:94`. **`:84` SC2's definite singular is NOT a case-type lock (T-3).**
- `.planning/REQUIREMENTS.md` — MHS-01..05 `:22-26` (**`:24` "the worked value"; `:25` the demotion + no-unit rule; `:26` "zero dependents exist among the settings this recipe sets"**), HYG-06 `:36`, RCPFUT-04/05 `:55-56`, Out of Scope `:74-87`, gray areas `:89-99` (**`:96` disposes of "Case-1b"**), `:68` the fence-mask coordinate (correct at `:158`).
- `.planning/research/STACK.md` — GATE 2 `:38-59`; the MHS key inventory `:47-55` (**`:50` the `applications` bundleArray; `:55` "(device-restrictions-profile only)"**); `:57` the OEMConfig note; `:83` "new prerequisite content"; `:95` "assumes enrollment is already complete".
- `.planning/research/FEATURES.md` — table stakes `:65-75` (**`:70` static device group; `:72` apps must be Required-assigned first; `:73` OEMConfig**); differentiators `:79-85` (**`:82` the three retry/debug labels**); anti-features `:87-101` (**exactly 9 rows**); decision points `:122-133` (**`:127` Enumerable; `:131` #6; `:132` #7 Enumerable; `:133` #8**); already-covered `:149-158`; **verification `:173-183` (8 items)**.
- `.planning/research/PITFALLS.md` — **`:193` Pitfall 12 is CLOSED, do NOT carry it**; Pitfall 13 `:201`; Pitfall 14 `:213-221`; Pitfall 15 `:227-240`; Pitfall 17 `:264-272`; Pitfall 18 `:282`; Pitfall 19 `:296`; Pitfall 20 `:308-312`; Pitfall 21 `:320-332`.
- `.planning/research/ARCHITECTURE.md` — RE-225 cross-link table `:52-65`; the missing-target problem + inline recommendation `:67-76`; frozen-surface hazards `:138-150`; taxonomy option space `:152-162`.
- `.planning/milestones/v1.18-REQUIREMENTS.md:43` — the HYG-04 drift-branch precedent (`last_verified` updates on a content correction).

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `docs/_templates/recipe-template.md` — instantiated wholesale; recipes 01/02 conform 7/7, recipe 03 ships 8 H2s with the named Rollback divergence.
- The two Android ```` ```json ```` fences (`02-zero-touch-portal.md`, `07-knox-mobile-enrollment.md`) — the exact fence + Fields-reference shape RE-225 inherits, in the same directory.
- C17 self-test + full-corpus run (`node scripts/validation/c17-eee-contract.mjs`) — the green gate; baseline 233 files after RE-224, going to 234.
- The registry → filename-map → publish pipeline is data-driven; this phase invents zero pipeline hooks and does not touch the registry.

### Established Patterns
- Recipes are `doc_type: Guide`, `platform: Android` here; value-live once the sentinel is stripped (`#4/#5/#9/#10/#11/#12` all fire on real values).
- Frozen-surface doctrine: the anchor is pin-free, but `_glossary-android.md` (365 coords) and `android-capability-matrix.md` (139) are not.
- Two-plan wave structure (`depends_on`) is the ordering mechanism `/gsd-execute-phase` consumes — not commit ordering.
- Research files are corrected in-flight in this milestone (commit `457adc25`), so D1.1's research-correction branch is conventional, not novel.

### Integration Points
- Phase 137 wires `docs/index.md` (both surfaces, same commit — the WR-01 defect class), flips the RE-index row to Approved, and regenerates filename-map with the 223→225 canary bump. This phase writes content only.
- Phase 138 authors `check-phase-136.mjs` with needles derived from `136-VERIFICATION.md` — the closure table above is what it can assert exactly — and creates `v1.19-DEFERRED-CLEANUP.md`, the landing spot for this phase's four entries.

</code_context>

<specifics>
## Specific Ideas

- **The bounded key set (8 payload keys):** `applications` (with `package` only), `grid_size`, `lock_home_screen`, `managed_folders`, `max_number_of_attempts_for_exit_PIN`, `amount_of_time_before_try_exit_PIN_again`, `enable_mhs_signin: false`, ± `exit_lock_task_mode_code` per D1.5.
- **The host sentence (D1.7/D0.1):** names both GUI labels, states the values must match, carries `[MEDIUM: MS Q&A community, last_verified <fresh>]` and the `#exit-kiosk-pin-synchronization` link. One sentence, in the exit-PIN step.
- **The Knox/ZT anti-feature row (D2.2):** *"Configuring Zero-Touch and [Knox Mobile Enrollment](…#provisioning-method-choice) simultaneously on Samsung hardware | a CRITICAL-severity mutual exclusion documented in the anchor | pick one per Samsung device"* — KME strings inside the link only.
- **The Step-2 window callout (D2.9a):** names the interval between the MHS Required assignment and the exit PIN existing, the `05:255` symptom, and the `05:257` forced-sync remediation.
- **Verification, seven lines (D4.6):** grid shows exactly the allow-listed apps · Home/Overview/back does not surface the standard launcher · the 15-press gesture reaches the exit-PIN prompt and the configured code works · folders/apps cannot be dragged or renamed · Wi-Fi switching works but the radio toggle does not · admin center confirms the Standard token type · the debug menu is not easily accessible.

</specifics>

<deferred>
## Deferred Ideas

- **Option B — a shared kiosk/dedicated taxonomy Reference doc** (`docs/reference/device-lockdown-taxonomy.md`, new RE-NNN). Barred here by CLASS-05's locked 223→225 canary and Phase 137's two-row budget. → `v1.19-DEFERRED-CLEANUP.md`, when a third lockdown recipe lands and the canary budget is free.
- **The anchor's past-due `review_by: 2026-06-22`** — on the no-drift branch only. → `v1.19-DEFERRED-CLEANUP.md`.
- **The `c17-eee-contract.mjs:150` → `:158` coordinate correction at all six sites** — appended to the existing fence-mask cleanup entry. → `v1.19-DEFERRED-CLEANUP.md`.
- **The second `## Rollback/Recovery` template divergence.** `135-02-SUMMARY.md:195` set the trigger at *"when a **third** recipe needs the slot"*; RE-225 is the second, so the trigger stays unmet — but record that the count moved to 2-of-4, so the successor does not re-derive it.
- **A `docs/admin-setup-android/` app-deployment guide** (ANDROID-APPDEPLOY-01) — RE-225 inlines a recipe-scoped step per RE-222's precedent rather than waiting on it.
- **RCPFUT-04** (screensaver / digital-signage) and **RCPFUT-05** (widgets, wallpaper, branding) — both already carry requirement-level entries; the OEMConfig Overlay/exact-alarm grant is out because every dependent feature is.
- **A mechanical `[SRC]` re-resolution check** — a script re-resolving every file:line claim in a planning artifact against HEAD would have caught 8 of round 3's 13 confirmed defects at zero agent cost. Candidate for a future tooling phase.

### Reviewed Todos (not folded)
None — `todo.match-phase 136` returned 0 matches.

</deferred>

---

*Phase: 136-Recipe #4 — Android Dedicated, MHS Multi-App*
*Context gathered: 2026-08-03*
