---
phase: 136-recipe-4-android-dedicated-mhs-multi-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/recipes/04-android-dedicated-mhs-multi-app.md
  - .planning/research/STACK.md
autonomous: true
requirements: [HYG-06]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low   # 2 samples (Phase 135 P01/P02); no calibration store in this repo

must_haves:
  truths:
    - "The three `05-dedicated-devices.md` fact clusters RE-225 cross-links — token-type semantics (`05:116-131`), MHS Required-assignment (`05:143-153`), exit-PIN two-policy locations (`05:249-255`) — are spot-verified against current Microsoft Learn BEFORE authoring depends on them (SC6, HYG-06)."
    - "Any drift found lands as a named correction in the v1.18 HYG-04 pattern (`last_verified` updates AND `review_by` bumps with it at the doc's own 60-day cadence); if no drift, the no-op is recorded explicitly with the fetch date, the three cluster names and the words no drift (SC6's `or the no-op recorded explicitly` clause)."
    - "D1.1/D1.5's `exit_lock_task_mode_code` question is ruled, with BOTH live Learn statements quoted (Pitfall 27), and the ruling names which of D1.5's two pre-specified branches Plan 2 executes."
    - "D2.7's sibling-key conditional is closed by name, so MHS-04's near-identical-names / opposite-gates / blocked-from-retry-vs-automatic-logout framing ships unsoftened."
    - "Every deferral this phase makes has an explicit landing spot — a REQUIREMENTS.md line or a named `v1.19-DEFERRED-CLEANUP.md` entry carried as a flagged contribution in `136-01-SUMMARY.md` (D4.7's gate)."
    - "`docs/recipes/04-android-dedicated-mhs-multi-app.md` exists as an independently C17-clean, sentinel-free shell — the full-corpus gate stays green BETWEEN waves, not only after Plan 2 (D4.4)."
  artifacts:
    - path: "docs/recipes/04-android-dedicated-mhs-multi-app.md"
      provides: "RE-225 file identity — frontmatter, EEE block, exactly one H1, >=30-word Summary"
      contains: "doc_id: RE-225"
      min_lines: 15
    - path: ".planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-01-SUMMARY.md"
      provides: "HYG-06 disposition record + the four DEFERRED-CLEANUP contributions + the two conditional rulings"
      contains: "no drift"
  key_links:
    - from: "136-01-SUMMARY.md"
      to: "v1.19-DEFERRED-CLEANUP.md (created by Phase 138 / HARN-16)"
      via: "flagged DEFERRED-CLEANUP contributions transcribed verbatim at milestone close"
      pattern: "DEFERRED-CLEANUP"
    - from: "136-01-SUMMARY.md D1.5 ruling"
      to: "136-02-PLAN.md Task 3 (the column-0 json fence)"
      via: "the ruled branch determines the fence key set, the decomposition rows, the Verification lines and the failures rows"
      pattern: "exit_lock_task_mode_code"
    - from: "docs/recipes/04-android-dedicated-mhs-multi-app.md"
      to: "scripts/validation/c17-eee-contract.mjs"
      via: "full-corpus C17 run must report 234 files, 0 violations after this plan"
      pattern: "0 total violations"
---

<objective>
Discharge everything Plan 2 depends on before a single line of the recipe body is authored: the HYG-06 spot-verification at D4.5's scope, the two surviving Plan-1-gated conditionals (D1.1/D1.5's fence key and D2.7's framing), every landing-spot decision required by D4.7's gate, and the D4.4 file-identity shell.

Purpose: `docs/admin-setup-android/05-dedicated-devices.md` is 33+ days past its own `review_by: 2026-06-22`, and RE-225 is a delta authored on top of it. Verifying the anchor's facts AFTER the delta is written is worthless. D4.7 also leaves exactly two structural parameters open — the fence's exit-PIN key and MHS-04's sibling-key framing — and both determine Plan 2's table shapes, so both close here.

Output: an independently C17-clean RE-225 shell, a `136-01-SUMMARY.md` carrying the HYG-06 disposition and four flagged `v1.19-DEFERRED-CLEANUP.md` contributions, and (on the `05:253` branch) one named in-flight research correction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-CONTEXT.md
@.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-RESEARCH.md
@.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-PATTERNS.md
</context>

<guardrails>
Carried into every task in this plan. Violating any one of these fails the phase, not just the task.

1. **Zero edits to `docs/_glossary-android.md` and `docs/reference/android-capability-matrix.md`** — 365 and 139 pin coordinates across 16 frozen sidecars. All three concepts RE-225 needs already have anchors (verified live this planning pass: `### Dedicated` at `:107`, `### Entra Shared Device Mode` at `:265`, `### Managed Home Screen` at `:299`).
2. **Zero edits to `docs/recipes/01-shared-windows-avd-client.md`** — `check-phase-130.mjs:64,67` pins its Step 5a/5b headings as literal strings against live HEAD inside every apex chain.
3. **Zero edits to `docs/admin-setup-android/05-dedicated-devices.md` unless HYG-06 finds drift** — and then only as a named correction under Task 2's disposition, never an unlogged drive-by edit to an Approved doc. On the DRIFT branch `last_verified` AND `review_by` both move; bumping one without the other ships `review_by` earlier than `last_verified`.
4. **Zero re-authoring of anything the anchor owns.** `REQUIREMENTS.md:84` names this the dominant review failure mode for this recipe.
5. **This phase is not Phase 137 or Phase 138.** No registry row, no `Draft`→`Approved` flip, no filename-map regeneration, no 223→225 canary, no validator authoring, no `v1.19-DEFERRED-CLEANUP.md` file creation. Cleanup entries are carried as flagged text inside `136-01-SUMMARY.md`; HARN-16 creates the file.
6. **D0.3 sourcing discipline binds this plan's own SUMMARY.** Every claim of the form *the repo supplies X* / *X is the only Y* / *no source states Z* carries a `file:line` or is struck. `136-01-SUMMARY.md` is bound because Phase 138 transcribes its cleanup contributions verbatim into a shipped milestone deliverable.
7. **T-1 stays struck.** There is no "the editor replaces the entire payload" mechanism. RESEARCH.md Finding 5 re-confirmed the absence on a live fetch. Do not reintroduce it.
</guardrails>

<artifacts_this_phase_produces>
Symbols created by this plan (Plan 2 extends the same file; nothing here is re-created there).

**New file:** `docs/recipes/04-android-dedicated-mhs-multi-app.md`

| Symbol | Value |
|---|---|
| `doc_id` | `RE-225` |
| `status` | `Draft` (Phase 137 owns the flip) |
| `doc_type` | `Guide` (closed 4-value enum, `recipe-template.md:13-14`) |
| `platform` | `Android` (C17 `#10` hard-fails an unmapped value, `c17-eee-contract.mjs:334-341`) |
| `owner` | `Intune Admin Lead` |
| `last_verified` | the authoring date |
| `review_by` | authoring date + 90 days (`recipe-template.md:5`) |
| H1 | one only — a concrete device end-state with a colon-subtitle |
| H2 | `## Summary` only at this wave; the remaining seven arrive in Plan 2 |

**Modified (conditional, `05:253` branch only):** `.planning/research/STACK.md` — the `:55` `(device-restrictions-profile only)` exclusivity parenthetical, corrected in flight per D8.2 / commit `457adc25`.

**Rulings this plan emits** (consumed by Plan 2, recorded in `136-01-SUMMARY.md`): the D1.1/D1.5 fence-key branch; the D2.7 framing closure; the HYG-06 branch (NO-DRIFT vs DRIFT); four DEFERRED-CLEANUP contributions.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: HYG-06 — spot-verify the three SC6 clusters and the PLUS list against current Microsoft Learn</name>
  <precondition>Outbound network access to `learn.microsoft.com` is available; if a fetch fails, halt and report rather than substituting RESEARCH.md's transcript as if it were this plan's own fetch.</precondition>
  <read_first>
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-CONTEXT.md` — D4.5 (HYG-06 scope and the two branches), D2.10 (Pitfall 20 marker carry-forward), D0.3 (sourcing discipline)
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-RESEARCH.md` — Finding 1 in full; it is the expected result, not a substitute for performing the check
    - `docs/admin-setup-android/05-dedicated-devices.md` lines 110-168 (Cluster A, Deltas 1-4 with the `:114` block-scope `[MEDIUM]` tag), 143-153 (Cluster B), 243-260 (Cluster C)
    - `.planning/REQUIREMENTS.md:36` — HYG-06 verbatim
    - `.planning/ROADMAP.md:88` — SC6 verbatim
  </read_first>
  <action>
Fetch the four first-party pages RESEARCH.md Finding 1 names and compare each against the anchor's claims. Perform the fetch — do not transcribe RESEARCH.md's quotes as this plan's own evidence; RESEARCH.md's verdict is the expected outcome and a contradiction is the signal that matters.

Pages: `learn.microsoft.com/en-us/intune/device-enrollment/android/setup-dedicated` (Clusters A and B), `learn.microsoft.com/en-us/intune/app-management/deployment/add-managed-google-play` (Cluster B), `learn.microsoft.com/en-us/intune/device-configuration/templates/ref-device-restrictions-android-enterprise` (Cluster C), `learn.microsoft.com/en-us/intune/app-management/configuration/configure-managed-home-screen` (the PLUS list).

Compare, per cluster:
- **Cluster A, `05:116-131`** — both Dedicated token-type names and their semantics (Standard = no per-user credentials; the Entra shared-mode token auto-configures shared device mode); the mandatory-expiry / no-never-expires claim; the irreversibility and revoke-recreate-redistribute consequence at `05:123`/`05:129`.
- **Cluster B, `05:143-153`** — that only Required-assigned apps install on Dedicated devices, that Managed Home Screen (`com.microsoft.launcher.enterprise`) needs an assignment, and the `05:145` second-sentence consequence that the device boots to the standard Android launcher without one.
- **Cluster C, `05:249-255`** — the Device Restrictions profile labels **Leave kiosk mode** and **Leave kiosk mode code**, the MHS App Configuration setting **Exit lock task mode password**, and — separately and critically — whether any first-party source now states the two values must match. Three prior passes found none. If a first-party source has appeared, that is drift and it re-opens MHS-02's `[MEDIUM: MS Q&A community]` tag; if not, the tag ships unchanged with only the date refreshed.
- **PLUS list** — `enable_mhs_signin` default FALSE; `signin_type` default and its account-type-scoped negative; the two exit-PIN retry keys including the absence of any unit on the retry-delay integer; and D2.7's sign-in-side sibling key.

Record a per-cluster disposition with the fetch date and a verbatim quote supporting each. Where the anchor's claim is NOT independently re-sourceable (RESEARCH.md names two: the irreversibility/redistribution cost, and the standard-launcher fallback consequence), record it as *not re-confirmed, not contradicted* and leave the anchor's existing marker in place — that is neither drift nor a licence to upgrade a marker.

Then take the branch:
- **NO-DRIFT** — make no edit to the anchor. Do not touch `last_verified`. Record the no-op explicitly: fetch date, the three cluster names, and the words `no drift`. Add a flagged DEFERRED-CLEANUP contribution for the anchor's past-due `review_by: 2026-06-22`. This branch is what RESEARCH.md predicts.
- **DRIFT** — the correction is named, and `last_verified` updates with `review_by` moved to +60 days per the doc's own cadence at `05:324`. No cleanup entry on this branch.

D2.10 output for Plan 2: list every Step line and decision-table cell whose underlying fact comes from an anchor section carrying a `[MEDIUM]` tag, so Plan 2 carries an equivalent marker forward. The `05:114` tag scopes the whole Delta-1..4 block, which includes Delta 3 — the source for Step 2 — so the carry-forward is unrestricted rather than token-type-only.
  </action>
  <acceptance_criteria>
    - Four Learn URLs fetched this session; each cluster disposition names its source URL and the fetch date.
    - Each of the three clusters carries at least one verbatim first-party quote and one of exactly two dispositions: `NO DRIFT` or `DRIFT`.
    - The exit-PIN *synchronization requirement* is dispositioned separately from the exit-PIN *locations*; if no first-party source is found, the record states that MHS-02's `[MEDIUM: MS Q&A community]` tag is unchanged with only the date refreshed.
    - The two claims RESEARCH.md flags as not independently re-sourceable are recorded as `not re-confirmed, not contradicted`, and neither is treated as drift.
    - On the NO-DRIFT branch: `git diff --quiet HEAD -- docs/admin-setup-android/05-dedicated-devices.md` exits 0 (the anchor is byte-unchanged).
    - On the DRIFT branch: the anchor's `last_verified` and `review_by` both move in the same edit, and `review_by` is later than `last_verified`.
    - A D2.10 carry-forward list exists naming the anchor sections whose facts Plan 2 consumes under a `[MEDIUM]` tag, including the `05:114` block scope covering Delta 3.
  </acceptance_criteria>
  <verify>
    <automated>git diff --quiet HEAD -- docs/_glossary-android.md docs/reference/android-capability-matrix.md docs/recipes/01-shared-windows-avd-client.md && echo FROZEN-SURFACES-CLEAN</automated>
  </verify>
  <done>All three SC6 clusters and the PLUS list are dispositioned against dated live fetches, the branch is taken, and the anchor is either byte-unchanged (no-drift) or corrected with both date fields moved (drift).</done>
</task>

<task type="auto">
  <name>Task 2: Close the two surviving conditionals and land every deferral</name>
  <reversibility rating="costly">The D1.1/D1.5 branch determines the fence's key set, which in turn determines Plan 2's decomposition rows, Verification lines and failures rows — reversing it after Plan 2 lands means re-cutting four coupled structures in one file.</reversibility>
  <read_first>
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-CONTEXT.md` — D1.1, D1.5, D2.7, D4.7's gate, D4.1 (Option B), D1.3 (the six wrong-coordinate sites), D4.3 (the naming entry), D0.4 (conditional budget)
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-RESEARCH.md` — Finding 2 in full (both conflicting statements), Finding 3 (the sibling key), Pitfall 27
    - `docs/admin-setup-android/05-dedicated-devices.md:243-260` — the anchor's own two-policy framing
    - `.planning/research/STACK.md:47-59` — the MHS key inventory, `:50` the `applications` bundleArray, `:55` the exclusivity parenthetical under challenge
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-02-SUMMARY.md:193-196` — the pattern for a flagged cleanup contribution carried inside a SUMMARY
  </read_first>
  <action>
**Ruling A — D1.1/D1.5, the `exit_lock_task_mode_code` location.** RESEARCH.md Finding 2 establishes that the live MHS configuration page carries both signals: a setting-specific Note stating the password must be configured through a device configuration profile, and Microsoft's own worked JSON sample carrying the identical key as a settable `managedProperty` entry. Pitfall 27 is that citing one half as if it were the whole page is the failure mode. Rule the branch and record it, quoting BOTH statements verbatim regardless of which branch wins.

The two branches are pre-specified by D1.5 and no third exists:
- **`05:253` branch** — the key ships in the fence with a placeholder value, never a concrete PIN, plus an adjacent cross-link to `#exit-kiosk-pin-synchronization` per the placeholder-lead-in idiom at `docs/admin-setup-macos/10-kerberos-sso-extension.md:92`, plus a one-line callout carrying the device-configuration-profile Note's own caveat. On this branch the `STACK.md:55` exclusivity parenthetical is contradicted and a named in-flight research correction lands in `.planning/research/STACK.md` per D8.2 and commit `457adc25` — the correction is named in the SUMMARY, not silently applied.
- **`STACK.md:55` branch** — the key is absent from the fence and the prose line after it states the PIN is set in the Device Restrictions profile. No research correction on this branch; the divergence from `05:253` becomes HYG-06 drift and routes to Task 1's DRIFT branch.

Do not invent a third branch, and do not regress to the struck claim that the PIN is set on this same policy alone.

**Ruling B — D2.7's framing.** RESEARCH.md Finding 3 names the sibling first-party with HIGH confidence: `max_number_of_attempts_for_session_PIN`, designer label *Maximum number of attempts for session PIN*, gated on **Enable session PIN** and **Enable sign in** being True, consequence automatic logout — against `max_number_of_attempts_for_exit_PIN`, gated on **Exit lock task mode password** being configured, consequence blocked from attempting to exit lock task mode. Record the conditional as CLOSED and state that MHS-04's near-identical-names / opposite-gates / different-consequences framing ships as written and does NOT soften. Record that the sibling stays OUT of the payload fence — it is inert under the worked `enable_mhs_signin` FALSE, the same category as D3.3's other excluded sign-in sub-keys — but is available, fully cited, for MHS-04's contrastive prose.

**Landing spots — D4.7's gate.** No deferral leaves this phase without a REQUIREMENTS.md line or a DEFERRED-CLEANUP entry. Draft four flagged contributions for `136-01-SUMMARY.md`, each in the `135-02-SUMMARY.md:193-196` shape (a named entry with its trigger, not a loose note):
1. **Option B**, the shared kiosk/dedicated taxonomy Reference doc — barred here because the canary is locked at 223 to 225 and Phase 137 has a two-row budget. Trigger: a third lockdown recipe lands and the canary budget is free.
2. **The anchor's past-due `review_by`** — on Task 1's NO-DRIFT branch only.
3. **The `c17-eee-contract.mjs` fence-mask coordinate correction at all six sites** (`135-CONTEXT.md:48`, `:204`, `:287`; `135-01-SUMMARY.md:113`; `135-02-SUMMARY.md:196`; `135-PATTERNS.md:312`) — the `^`-anchored fence regex is at `:158`, with a second at `:166`; `:150` is the enclosing IIFE opener. Append to the existing fence-mask entry rather than opening a new one.
4. **The `## Rollback/Recovery` template-divergence count** — `135-02-SUMMARY.md:195` set the trigger at a third recipe needing the slot; RE-225 is the second, so the trigger stays unmet, but record that the count moved to 2-of-4 so the successor does not re-derive it.
  </action>
  <acceptance_criteria>
    - Ruling A names exactly one of D1.5's two pre-specified branches and quotes BOTH live Learn statements verbatim; neither is cited alone.
    - If the `05:253` branch is ruled: `.planning/research/STACK.md` is edited and the correction is named in the SUMMARY as an in-flight research correction. If the `STACK.md:55` branch is ruled: `git diff --quiet HEAD -- .planning/research/STACK.md` exits 0.
    - Ruling A states, in the same sentence as any residual uncertainty, why the answer is not already in the repo (D0.4).
    - Ruling B records D2.7 as CLOSED, names the sibling key and its designer label, states both gates and both consequences, and states that the framing does not soften and the key stays out of the fence.
    - Exactly four flagged DEFERRED-CLEANUP contributions are drafted, each carrying a named trigger; entry 2 appears only on the NO-DRIFT branch.
    - No `v1.19-DEFERRED-CLEANUP.md` file is created by this plan: `test ! -f .planning/v1.19-DEFERRED-CLEANUP.md`.
    - Every "the repo supplies X" / "X is the only Y" / "no source states Z" claim in the rulings carries a `file:line` (D0.3).
  </acceptance_criteria>
  <verify>
    <automated>test ! -f .planning/v1.19-DEFERRED-CLEANUP.md && test ! -f v1.19-DEFERRED-CLEANUP.md && git diff --quiet HEAD -- docs/_glossary-android.md docs/reference/android-capability-matrix.md docs/recipes/01-shared-windows-avd-client.md && echo GATE-CLEAN</automated>
  </verify>
  <done>Both surviving conditionals are ruled with first-party evidence on both sides where a conflict exists, and all four deferrals have a named landing spot drafted for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Create the RE-225 file-identity shell and prove full-corpus C17 goes 233 to 234 at zero violations</name>
  <read_first>
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-PATTERNS.md` lines 24-76 — the verbatim RE-224 shell shipped at commit `ef155268`, with the RE-225 substitution list
    - `docs/_templates/recipe-template.md` lines 1-55 — the frontmatter rules and the Summary shape
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-CONTEXT.md` — D4.4 (file identity, and why a frontmatter-only file turns the corpus red)
    - `docs/recipes/03-windows-11-multi-app-kiosk.md` lines 1-26 — the live sibling's frontmatter, EEE block, H1 and Summary
  </read_first>
  <action>
Create `docs/recipes/04-android-dedicated-mhs-multi-app.md` as a shell in the exact shape `136-PATTERNS.md:29-49` reproduces from commit `ef155268` — roughly 19 lines, nothing more. Author only: frontmatter, the EEE header line, exactly one H1, and `## Summary`. The remaining seven H2s belong to Plan 2.

Frontmatter, same field order as the analog: `doc_id: RE-225`, `status: Draft`, `owner: Intune Admin Lead`, `doc_type: Guide`, `platform: Android`, `last_verified` set to the authoring date, `review_by` set to authoring date + 90 days, `applies_to` a one-line description of the MHS multi-app Dedicated end-state, `audience: admin`. The template's harness-skip date sentinel must not survive the copy — a sentinel-bearing file skips C17 `#9` and `#12` entirely, which would make every blockquote measurement Plan 2 performs void.

EEE header line, immediately after the frontmatter close, matching the corpus format exactly: platform label, doc type, doc ID and status separated by the middle-dot separator the sibling recipes use.

H1: one only, naming the concrete device end-state with a colon-subtitle, in the shape of the sibling recipes' H1s.

`## Summary`: opens with the corpus idiom *Following this recipe yields…*, at least 30 words (C17 `#5` counts), and states the Android version floor and the Intune role required to author and assign both the app assignment and the App Configuration policy.

Ship no blockquote in this shell — the Scope banner is Plan 2's, and D3.9's measure-and-split discipline applies there.

Then run the full-corpus C17 gate and confirm the file count moved from 233 (measured at plan time) to 234 with every assertion at zero.
  </action>
  <acceptance_criteria>
    - `test -f docs/recipes/04-android-dedicated-mhs-multi-app.md`
    - `grep -c 'TEMPLATE-SENTINEL' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 0.
    - `grep -c '^doc_id: RE-225$' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 1; the same file yields 1 for `^status: Draft$`, 1 for `^doc_type: Guide$`, and 1 for `^platform: Android$`.
    - `grep -c '^# ' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns exactly 1.
    - `grep -c '^## ' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns exactly 1 (`## Summary`; Plan 2 raises this to 8).
    - The Summary body is >= 30 words, measured with `wc -w` over the paragraph following the `## Summary` heading.
    - `review_by` parses as later than `last_verified`, and `last_verified` is the authoring date.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 and its summary line reports `234 files checked, 0 with violations, 0 total violations`.
    - `git diff --quiet HEAD -- docs/_glossary-android.md docs/reference/android-capability-matrix.md docs/recipes/01-shared-windows-avd-client.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs && node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -E '234 files checked, 0 with violations, 0 total violations'</automated>
  </verify>
  <done>The shell exists, is sentinel-free, carries the D4.4 frontmatter tuple, and the full-corpus C17 gate is green at 234 files between waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Microsoft Learn -> the corpus | Untrusted external content is transcribed into an Approved-track document; a mis-transcribed fact ships to an admin who executes it against production devices |
| this phase -> the frozen validator surface | Content edits can shift line coordinates that 16 frozen sidecars pin, breaking every apex chain |
| planning artifacts -> the shipped milestone deliverable | `136-01-SUMMARY.md` contributions are transcribed verbatim by Phase 138 into `v1.19-DEFERRED-CLEANUP.md` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136-01 | Tampering | `docs/admin-setup-android/05-dedicated-devices.md` (Approved) | high | mitigate | Task 1 branches explicitly; the NO-DRIFT branch asserts the anchor byte-unchanged via `git diff --quiet`, the DRIFT branch requires a named correction with both date fields moved. No unlogged drive-by edit path exists. |
| T-136-02 | Denial of Service | frozen pin surfaces (`_glossary-android.md`, `android-capability-matrix.md`, `recipes/01-*.md`) | critical | mitigate | Every task's `<verify>` runs `git diff --quiet HEAD` against all three paths; a line-shifting edit fails the task rather than the next milestone's apex chain. |
| T-136-03 | Information Disclosure | the shell's `applies_to` and Summary | low | accept | The shell carries no tenant identifiers, no PIN and no secret; the payload placeholder risk is Plan 2's (T-136-06). |
| T-136-04 | Repudiation | deferrals with no landing spot | high | mitigate | D4.7's gate is an acceptance criterion: exactly four flagged contributions with named triggers, drafted in Task 2, asserted in the SUMMARY contract below. |
| T-136-05 | Spoofing | a fabricated first-party quote presented as a live fetch | high | mitigate | Task 1's precondition forbids substituting RESEARCH.md's transcript for this plan's own fetch; each disposition names its URL and fetch date, and D0.3 binds every sourcing claim to a `file:line`. |

No package-manager install occurs in this phase (`npm`/`pip`/`cargo` install count is zero), so the supply-chain threat row and the Package Legitimacy Gate do not apply.
</threat_model>

<verification>
Run after all three tasks:

1. `node scripts/validation/c17-eee-contract.mjs` exits 0 and reports `234 files checked, 0 with violations, 0 total violations`.
2. `git diff --quiet HEAD -- docs/_glossary-android.md docs/reference/android-capability-matrix.md docs/recipes/01-shared-windows-avd-client.md` exits 0.
3. `git status --porcelain docs/_registry/ scripts/pipeline/ scripts/validation/` is empty — Phase 137's and Phase 138's surfaces are untouched.
4. `grep -c 'TEMPLATE-SENTINEL' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 0.
5. `136-01-SUMMARY.md` satisfies the mandatory-contents contract in `<output>` below.
</verification>

<success_criteria>
- HYG-06 is discharged at D4.5's exact scope — three clusters plus the PLUS list — against dated live fetches, with a per-cluster disposition and a branch taken.
- SC6's `or the no-op recorded explicitly` clause is satisfied literally on the no-drift branch: fetch date, three cluster names, `no drift`.
- D1.1/D1.5 is ruled with both conflicting live statements quoted; D2.7 is closed by name.
- All four deferrals carry a named landing spot; no cleanup file is created.
- The RE-225 shell is sentinel-free and independently C17-clean at 234 files, 0 violations.
- Zero edits to the three frozen surfaces and, on the no-drift branch, to the anchor.
</success_criteria>

<output>
Create `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-01-SUMMARY.md` when done.

**Mandatory contents** — Phase 138 transcribes parts of this file verbatim, and `136-VERIFICATION.md` asserts against it:

1. **HYG-06 disposition** — the fetch date, the three cluster names (`05:116-131` token-type semantics, `05:143-153` MHS Required-assignment, `05:249-255` exit-PIN two-policy locations), one verbatim quote per cluster, and the branch taken. On the no-drift branch the words `no drift` appear literally.
2. **The exit-PIN synchronization requirement's own disposition**, separate from the locations — whether MHS-02's `[MEDIUM: MS Q&A community]` tag stands with only the date refreshed.
3. **Ruling A (D1.1/D1.5)** — the branch, both live Learn statements quoted, and the downstream consequence for Plan 2's fence key set.
4. **Ruling B (D2.7)** — CLOSED, the sibling key and label named, both gates and both consequences stated, and the statement that the framing does not soften.
5. **Four flagged `v1.19-DEFERRED-CLEANUP.md` contributions**, each with its trigger (entry 2 on the no-drift branch only).
6. **The D2.10 marker carry-forward list** for Plan 2.
7. **The D0.1 reconciliation statement** — that SC1's *cross-linked (never re-authored)* and SC2's marked exit-PIN sync are both satisfied by exactly one marked host sentence, so a verifier reading SC1 verbatim does not read that sentence as re-authoring.
8. Any in-flight research correction, named (`05:253` branch only).

Every claim of the form *the repo supplies X* / *X is the only Y* / *no source states Z* carries a `file:line` (D0.3).
</output>
