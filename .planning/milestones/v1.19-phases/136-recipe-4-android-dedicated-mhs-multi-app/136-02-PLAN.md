---
phase: 136-recipe-4-android-dedicated-mhs-multi-app
plan: 02
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - docs/recipes/04-android-dedicated-mhs-multi-app.md
autonomous: true
requirements: [MHS-01, MHS-02, MHS-03, MHS-04, MHS-05]

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 4
  confidence: low   # 2 samples (Phase 135 P01/P02); no calibration store in this repo

must_haves:
  truths:
    # --- ROADMAP SC1-SC5, carried without reducing a clause ---
    - "SC1: `docs/recipes/04-*.md` supplies the missing `## Steps` / `## Verification` / Unsupported-and-Anti-Feature scaffold PLUS an inlined, recipe-scoped MHS app-deployment step with a concrete click-path, while enrollment-profile deltas, token types, provisioning methods, Knox/Zero-Touch exclusion, exit-PIN sync, and Android 15 FRP are cross-linked (never re-authored) from `05-dedicated-devices.md`."
    - "SC2: The Case-1 decision block is the irreversible token-type choice (Standard vs. Entra SDM, SDM as a routing cross-link only), and a second fork covers the four-way provisioning method with its CRITICAL Knox/Zero-Touch mutual exclusion; the exit-PIN sync requirement ships `[MEDIUM: MS Q&A community]` with only the date refreshed."
    - "SC3: A Case-2 sign-in-mode block covers `enable_mhs_signin` FALSE (worked default) / TRUE+Other / TRUE+Entra ID, anchored on the documented Entra-ID default and its real first-party negative, with any `[ASSUMED]` note shipped as a split blockquote per the `01:101`/`01:103` idiom."
    - "SC4: Exit-lock-task hardening (max-attempts + retry-delay) is physically separated from the sign-in section, carries the silent no-op dependency verbatim as a `What breaks if misconfigured` callout, states no unit for the retry-delay integer, and demotes `Enable easy access debug menu` to a Verification line."
    - "SC5: The recipe leads with its unsupported/anti-feature set documented with reasons — Notification-windows=Disable phrased conditionally, folders trimmed to the uncontradicted user-capability half, plus exposed system navigation, per-identity personalization on a sign-in-disabled device, and unavailable Wi-Fi/first-time-Enterprise-network actions."
    # --- CONTEXT closure table: the artifact's global property contract ---
    - "The finished file carries exactly 8 H2 headings, in template order with `## Rollback/Recovery` between `## Verification` and `## Configuration-Caused Failures`."
    - "The finished file carries exactly 6 `### Step` headings."
    - "The finished file carries exactly 5 decision blocks — 1 Case-1 (token type), 2 Case-2 (provisioning method, sign-in mode), 2 Case-3 (`grid_size`, `managed_folders`) — and exactly 5 `Ask the admin` lead-ins, one per block, all at column 0."
    - "The finished file carries approximately 6 `What breaks if misconfigured` callouts, one per step, all at column 0."
    - "The Unsupported and Anti-Feature Callouts table keeps its 3-column shape and carries 9-10 data rows; it does not merge with any other table."
    - "The decomposition table carries 10-16 `dataRows` counting its header, uses the 5-column header `| # | Setting / JSON key | Configuration designer label | What it does | Worked value |`, and is followed by a plain-prose summary unconditionally."
    - "The Verification checklist carries exactly 7 lines — 6 positive observable checks plus the demoted debug-menu line — and zero deliberately-break-it checks."
    - "The Configuration-Caused Failures table carries at least 3 rows including the mismatched PIN, the max-attempts lockout, and the Step-2-to-Step-6 no-exit-PIN window."
    - "The file carries exactly one code fence, at column 0, tagged `json`, in the `kind`/`productId`/`managedProperty` envelope."
    - "Every blockquote in the file is at column 0 and every contiguous run measures at most 200 characters after the `>` prefix strip."
    - "Full-corpus C17 exits 0 at 234 files, 0 violations, with the file value-live (sentinel-free)."
  artifacts:
    - path: "docs/recipes/04-android-dedicated-mhs-multi-app.md"
      provides: "RE-225 — the complete Android Dedicated MHS multi-app device recipe"
      contains: "## Rollback/Recovery"
      min_lines: 250
  key_links:
    - from: "docs/recipes/04-android-dedicated-mhs-multi-app.md"
      to: "docs/admin-setup-android/05-dedicated-devices.md"
      via: "delta cross-links to #scenarios, #enrollment-profile, #provisioning-method-choice, #exit-kiosk-pin-synchronization, #android-15-frp-reprovisioning"
      pattern: "05-dedicated-devices\\.md#"
    - from: "docs/recipes/04-android-dedicated-mhs-multi-app.md"
      to: "docs/admin-setup-android/01-managed-google-play.md#bind-mgp"
      via: "the MGP binding hard prerequisite"
      pattern: "01-managed-google-play\\.md#bind-mgp"
    - from: "docs/recipes/04-android-dedicated-mhs-multi-app.md"
      to: "docs/l1-runbooks/ and docs/l2-runbooks/"
      via: "recipe-template.md:29 runbook links from What-breaks callouts — a deliberate, named first for the recipes corpus"
      pattern: "l[12]-runbooks/"
    - from: "the Step 5 json fence"
      to: "the decomposition table"
      via: "every key in the fence has a decomposition row; the table may carry additional non-payload rows"
      pattern: "managedProperty"
---

<objective>
Author the body of `docs/recipes/04-android-dedicated-mhs-multi-app.md` (RE-225) on top of Plan 1's shell: Scope banner, `## Prerequisites`, the anti-feature table, six `### Step` sections carrying five STD-05 decision blocks and the single column-0 `json` payload fence with its decomposition table, then `## Verification`, the named `## Rollback/Recovery` divergence, `## Configuration-Caused Failures` and `## See Also`.

Purpose: `05-dedicated-devices.md` has no `## Steps` section, no verification checklist and no anti-feature table. RE-225 supplies exactly that scaffold and nothing the anchor already owns — re-authoring anchor content is the dominant review failure mode for this recipe (`REQUIREMENTS.md:84`).

Output: a complete, C17-green, Draft-status recipe whose global properties match CONTEXT's closure table exactly, because Phase 138 authors `check-phase-136.mjs` against those counts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-CONTEXT.md
@.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-RESEARCH.md
@.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-PATTERNS.md
@.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-01-SUMMARY.md
</context>

<guardrails>
Carried into every task. Violating any one fails the phase, not just the task.

1. **Zero edits to `docs/_glossary-android.md` and `docs/reference/android-capability-matrix.md`** — 365 and 139 pin coordinates across 16 frozen sidecars. All three concepts RE-225 needs already have anchors (verified live at plan time: `#dedicated`, `#entra-shared-device-mode`, `#managed-home-screen`). Zero new glossary terms.
2. **Zero edits to `docs/recipes/01-shared-windows-avd-client.md`** — `check-phase-130.mjs:64,67` pins its Step 5a/5b headings against live HEAD inside every apex chain.
3. **Zero edits to `docs/admin-setup-android/05-dedicated-devices.md`.** Plan 1 already dispositioned it. This plan only cross-links.
4. **Zero re-authoring of anything the anchor owns** — enrollment-profile deltas, token-type semantics, all four provisioning methods, the Knox/Zero-Touch exclusion, the exit-PIN sync requirement, Android 15 FRP. Every one is a cross-link.
5. **This plan is not Phase 137 or Phase 138.** `status` stays `Draft`. No registry row, no `docs/index.md` edit, no filename-map, no canary, no validator.
6. **D2.2 bars body meta-commentary.** The `## Rollback/Recovery` divergence, the runbook-link first, and the M-A row-vs-sentence call are all named in `136-02-SUMMARY.md`, never inside the recipe. `03:275-306` contains zero self-annotation; match that.
7. **T-7: every blockquote at column 0.** Recipe 02 indents its callouts and lead-ins by three spaces, which silently escapes C17 `#12`'s `^>`-anchored cap. Do not copy that even inside a numbered step's sub-item. Measure after the prefix strip and split into blank-line-separated runs rather than truncating — Phase 135 measured four callouts at 220/213/223/245, four for four over the cap.
8. **T-1 stays struck.** No "the editor replaces the entire payload" mechanism exists; RESEARCH.md Finding 5 re-confirmed its absence on a live fetch.
9. **T-2: the anchor DOES state the two exit PINs must match, four times** (`05:245`, `:251`, `:253`, `:255` — the last reads *"Both must be set to the same value."*). RE-225's delta is that the two GUI labels name the same field, and nothing more.
</guardrails>

<artifacts_this_phase_produces>
Every symbol this plan creates. Phase 138 asserts against these; keep the strings exact.

**File:** `docs/recipes/04-android-dedicated-mhs-multi-app.md` (`doc_id: RE-225`, created by Plan 1)

**The 8 H2 headings, in file order:**

| # | Heading | Anchor slug |
|---|---|---|
| 1 | `## Summary` (Plan 1) | `#summary` |
| 2 | `## Prerequisites` | `#prerequisites` |
| 3 | `## Unsupported and Anti-Feature Callouts` | `#unsupported-and-anti-feature-callouts` |
| 4 | `## Steps` | `#steps` |
| 5 | `## Verification` | `#verification` |
| 6 | `## Rollback/Recovery` | `#rollbackrecovery` |
| 7 | `## Configuration-Caused Failures` | `#configuration-caused-failures` |
| 8 | `## See Also` | `#see-also` |

**The 6 `### Step` headings and their slugs** (apostrophe-free by construction — a stray apostrophe breaks the auto-slug and every in-recipe route that targets it):

| # | Heading | Anchor slug |
|---|---|---|
| 1 | `### Step 1: Confirm the token type on the enrollment profile` | `#step-1-confirm-the-token-type-on-the-enrollment-profile` |
| 2 | `### Step 2: Deploy Managed Home Screen as a Required app` | `#step-2-deploy-managed-home-screen-as-a-required-app` |
| 3 | `### Step 3: Deploy the allow-listed apps` | `#step-3-deploy-the-allow-listed-apps` |
| 4 | `### Step 4: Choose the sign-in mode` | `#step-4-choose-the-sign-in-mode` |
| 5 | `### Step 5: Author and assign the App Configuration policy` | `#step-5-author-and-assign-the-app-configuration-policy` |
| 6 | `### Step 6: Set the exit-kiosk PIN and harden exit retries` | `#step-6-set-the-exit-kiosk-pin-and-harden-exit-retries` |

**JSON keys the single fence carries** — envelope `kind`, `productId`, `managedProperty`; payload `applications` (bundleArray, sub-key `package` only), `grid_size`, `lock_home_screen`, `managed_folders`, `max_number_of_attempts_for_exit_PIN`, `amount_of_time_before_try_exit_PIN_again`, `enable_mhs_signin`, plus `exit_lock_task_mode_code` if and only if Plan 1 ruled the `05:253` branch.

**Keys named in prose or the decomposition table but NOT in the fence:** the debug-menu setting (GUI-label row, em-dash key), `max_number_of_attempts_for_session_PIN` (MHS-04's contrastive prose), `signin_type` (the Case-2 block's non-worked arms), `enable_app_offline` and `app_available_prior_to_sign_in` (D3.6's named carve-out).

**External anchors this file targets** (all verified to exist at plan time): `05-dedicated-devices.md#scenarios`, `#enrollment-profile`, `#enrollment-token`, `#provisioning-method-choice`, `#exit-kiosk-pin-synchronization`, `#android-15-frp-reprovisioning`; `01-managed-google-play.md#bind-mgp`; `_glossary-android.md#dedicated`, `#entra-shared-device-mode`, `#managed-home-screen`; `_standards/EEE-SOP-standard.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scope banner, Prerequisites, and the anti-feature table</name>
  <read_first>
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-PATTERNS.md` lines 83-123 — the Scope-banner multi-run shape (`03:21-25`), the `This recipe is NOT` opener (`03:29`), the 3-column anti-feature table header and representative row (`03:41-56`)
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-CONTEXT.md` — D4.1 (taxonomy sentence, no new anchor), D4.2 (Scope banner overflow), D4.8 (the `This recipe is NOT` term list and its corrected cross-link targets), D2.2 (the Knox/Zero-Touch two carriers), D3.1 (no merge, 9-10 rows), D2.5 (the sign-in default anti-feature row), D3.3's M-A ruling
    - `.planning/research/FEATURES.md` lines 87-101 — the nine anti-feature rows, which are the row source
    - `.planning/REQUIREMENTS.md:26` — MHS-05 verbatim, including the word-for-word content for the Notification-windows and folders rows
    - `docs/admin-setup-android/05-dedicated-devices.md` lines 32-36, 64-86, 86-108 — the anchor's taxonomy blockquotes, Scenarios table and Prerequisites, so nothing here is re-authored
  </read_first>
  <action>
Author three sections between the existing `## Summary` and a `## Steps` heading that Task 2 will add.

**Scope banner** — a column-0 blockquote immediately after the Summary, in `03:21-25`'s multi-run shape: several blank-line-separated `>` paragraphs, never one long run. It carries one taxonomy sentence distinguishing an Android Dedicated multi-app device from the adjacent lockdown models, plus a cross-link to `../_glossary-android.md#dedicated` — this is structurally what the anchor does at `05:32-36`. No new taxonomy anchor is created and no glossary file is touched. Assume overflow and split: recipe 03's banner measured 249 characters before splitting.

**`## Prerequisites`** — opens with a `**This recipe is NOT:**` bullet in `03:29`'s shape (bolded lead-in, comma-separated terms, a parenthetical reason per term). The four terms are: Entra shared device mode, single-app kiosk / Lock Task Mode, digital signage / screensaver mode, and Windows multi-app kiosk. Cross-link targets: Entra shared device mode routes to `../_glossary-android.md#entra-shared-device-mode`; the other three route to `05-dedicated-devices.md#scenarios`, because the glossary has no anchor for Lock Task Mode, single-app kiosk or digital signage — the anchor proves it, since `05:62`'s Lock Task Mode bullet is the one terminology bullet carrying no link. Windows multi-app kiosk gets no link (recipe 03 is Draft until Phase 137, and `01:29`'s own idiom links none of its terms). Do not carry COSU/COBO — `PITFALLS.md:236` scopes that duty to terms appearing in the recipe's own prose.

Then the actual prerequisites, each a cross-link rather than a restatement: MGP binding via `../admin-setup-android/01-managed-google-play.md#bind-mgp`; the static Entra device group both the app assignment and the policy assignment target; a device already enrolled as Dedicated on a Standard token — this recipe starts post-enrollment and routes enrollment itself to the anchor; the Intune role required.

**`## Unsupported and Anti-Feature Callouts`** — the 3-column table `| Feature | Why it's unsupported / what breaks | Do this instead |`, 9 or 10 data rows, each cell a full sentence naming the mechanism rather than a fragment. It does not merge with any other table. Rows come from `FEATURES.md:87-101`, which is exactly nine rows and already contains every item MHS-05 names. Required content:
- Notification windows set to Disable, phrased conditionally on later enabling screensaver, virtual home button or automatic sign-out — conditional because zero dependents exist among the settings this recipe sets.
- Folders, trimmed to the uncontradicted user-capability half; the alphabetical-ordering clause is dropped because that ordering is a configurable default, not a constraint.
- Exposed system navigation bypassing the sign-in gate.
- Per-identity personalization on a sign-in-disabled device.
- The Wi-Fi radio toggle and first-time Enterprise-network connection being unavailable from inside MHS.
- The sign-in default row: `signin_type` defaults to Microsoft Entra ID, so an admin who turns sign-in on and touches nothing else lands there; quote the real first-party negative with its account-type scope intact. Do not import the withdrawn zero-SSO-benefit framing, and do not claim sign-in TRUE is unavailable on a Standard token.
- The Knox/Zero-Touch row: a CRITICAL-severity mutual exclusion on Samsung hardware. `PITFALLS.md:217` bars the Knox product name and its initialism from appearing outside a link, so both occur only as link text pointing at `05-dedicated-devices.md#provisioning-method-choice`. It does not bar the vendor name or the severity word, so the row still carries both.

The OEMConfig Overlay and exact-alarm grant is OUT. Rule at author time whether it lands as the 10th row or as a sentence in Step 5's lead-in, and record which in the SUMMARY — that call is what makes the row count 9 or 10.

The second Knox/Zero-Touch carrier is one routing sentence in plain prose immediately after Step 2's provisioning-method table. It is plain prose, not a blockquote — that placement belongs to Task 2, but rule the wording consistently with this row so the two carriers do not contradict.
  </action>
  <acceptance_criteria>
    - `grep -c '^## ' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 3 at the end of this task (`Summary`, `Prerequisites`, `Unsupported and Anti-Feature Callouts`).
    - `grep -c '^| ' docs/recipes/04-android-dedicated-mhs-multi-app.md` minus the separator row yields 10 or 11 for the anti-feature table (header plus 9 or 10 data rows); the table's header line matches `^| Feature | Why it.s unsupported / what breaks | Do this instead |$`.
    - Every occurrence of the Knox product name and of its three-letter initialism sits inside link text: the count of the bare string equals the count of the same string immediately preceded by an opening square bracket. Verify with two `grep -o | wc -l` counts and compare them.
    - `grep -cE '^[[:space:]]+>' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 0 — no indented blockquote anywhere in the file.
    - The blockquote measurement command in `<verify>` reports `max` at most 200 and an empty over-cap list.
    - `grep -c 'l_glossary-android' docs/recipes/04-android-dedicated-mhs-multi-app.md` is irrelevant; instead assert the three glossary links resolve: each of `#dedicated`, `#entra-shared-device-mode` used in this file corresponds to a heading in `docs/_glossary-android.md` (`### Dedicated` at `:107`, `### Entra Shared Device Mode` at `:265`).
    - `git diff --quiet HEAD -- docs/_glossary-android.md docs/reference/android-capability-matrix.md docs/recipes/01-shared-windows-avd-client.md docs/admin-setup-android/05-dedicated-devices.md` exits 0.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const f='docs/recipes/04-android-dedicated-mhs-multi-app.md';const L=require('fs').readFileSync(f,'utf8').split(/\r?\n/);let i=0,m=0,over=[];while(i<L.length){if(/^>/.test(L[i])){const s=i,b=[];while(i<L.length&&/^>/.test(L[i])){b.push(L[i].replace(/^>\s?/,''));i++}const n=b.join(' ').length;if(n>m)m=n;if(n>200)over.push((s+1)+':'+n)}else i++}console.log('max='+m+' over='+JSON.stringify(over));if(over.length)process.exit(1)" && node scripts/validation/c17-eee-contract.mjs</automated>
  </verify>
  <done>Scope banner, Prerequisites and the 9-10-row anti-feature table are authored; every blockquote is column-0 and under the cap; C17 is green.</done>
</task>

<task type="auto">
  <name>Task 2: Steps 1-4 — token type, provisioning method, MHS deployment, app deployment, sign-in mode</name>
  <reversibility rating="one-way">The Step-2 to Step-6 window in which a Required MHS assignment has landed but no exit PIN exists yet leaves a real device with no exit affordance at all; shipping it unaddressed puts a documented CRITICAL state into a published recipe. CONTEXT names both acceptable resolutions and delegates the pick, so the door is closed either way — no checkpoint re-asks a delegated choice.</reversibility>
  <read_first>
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-PATTERNS.md` lines 125-181 — the Case-1, Case-2 and Case-3 block shapes, the two-blocks-in-one-step precedent, and the `### Step N` plus What-breaks-callout shape (`03:60-70`)
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-CONTEXT.md` — D2.1 through D2.11 in full, plus D2.9a, D1.6, D3.9, D1.9
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-RESEARCH.md` — Finding 4(a) the MHS Required-assignment click-path, Finding 1 Cluster A verbatim token quotes, the PLUS list's `enable_mhs_signin` and `signin_type` quotes
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-01-SUMMARY.md` — the D2.10 marker carry-forward list
    - `docs/recipes/01-shared-windows-avd-client.md` lines 78-91 — RE-222's inline app-assignment click-path precedent, the shape MHS-01's inlined step mirrors
    - `docs/admin-setup-android/05-dedicated-devices.md` lines 110-168, 170-192, 193-242 — everything Steps 1 and 2 must route to rather than restate
  </read_first>
  <action>
Open `## Steps` and author Steps 1 through 4 using the exact headings in this plan's artifacts table. Imperative voice, full Intune portal paths, numbered click-lists.

**Step 1 — confirm the token type on the enrollment profile.** This is a confirm-and-route step, not profile creation: confirm the enrollment profile that provisioned this device uses the Standard token, and if it uses the Entra shared-mode token, this recipe is not the path. It re-documents no token-type content.

It carries **two** decision blocks back to back, which is corpus-precedented (`01:168`/`01:175`, `02:102`/`:107`/`:120`):
- **The Case-1 token-type block** — `| Option | When to choose | Consequence if wrong | Branch |`. The Standard arm is worked and continues to Step 2. The Entra shared-mode arm routes its Branch cell straight to the anchor: `05-dedicated-devices.md#scenarios` plus the Delta-1 target at `#enrollment-profile`. The consequence cell carries the full operational cost the anchor documents at `05:129` — revoke the token, recreate the enrollment profile, redistribute the QR to every field site — together with its `[MEDIUM]` marker. A marker qualifies the source, not the severity: Pitfall 11 bars softening the consequence, Pitfall 20 bars stripping the signal, and they are orthogonal.
- **The Case-2 provisioning-method block** — `| Option | When to choose | Recorded as |`, four arms: QR code, NFC, `afw#setup`, Zero-Touch. Case 2 because it is a pick from a fixed set with no procedure fork: the method does not fork this recipe's remaining steps, which start post-enrollment. The `Recorded as` cells use the artifacts the anchor names at `05:172` and `05:196`. Every arm's detail routes to `05-dedicated-devices.md#provisioning-method-choice`; none is re-authored.

Immediately after that table, one plain-prose routing sentence — not a blockquote — carrying the Knox/Zero-Touch mutual exclusion consistently with Task 1's anti-feature row, with the Knox product name and initialism appearing only as link text.

Then Step 1's `What breaks if misconfigured` callout at column 0.

**Step 2 — deploy Managed Home Screen as a Required app.** The inlined, recipe-scoped app-deployment step SC1 requires, with the concrete click-path RESEARCH.md Finding 4(a) sourced: Apps, then All apps, then select Managed Home Screen — it is already present because Intune auto-adds it on Managed Google Play binding, so there is no Create or Add step, which is where this diverges structurally from RE-222's Windows precedent — then Properties, then Edit beside Assignments, then under Required select Add group and pick the static Entra device group. Name the package `com.microsoft.launcher.enterprise`. The facts under this step come from the anchor's Delta 3, which sits inside the block `05:114` tags `[MEDIUM]`, so carry the marker forward.

This step closes **D2.9a's window**. A Required assignment makes the device boot into MHS, and the device is already enrolled and in the target group when this lands; a device with no exit PIN shows the anchor's documented no-PIN message at exit attempt, which `05:302` ranks CRITICAL and first; policy sync is typically within eight hours; and the 15-press gesture leads to the exit-PIN prompt, so during the window there is no exit affordance at all. Take one of exactly two resolutions and record which in the SUMMARY:
- author and assign both PIN surfaces before this step's Required assignment, or
- carry an explicit callout in this step naming the window, the symptom, and the anchor's forced-sync remediation at `05:257`, plus a matching Configuration-Caused Failures row in Task 4.

Do not ship the window unaddressed.

**Step 3 — deploy the allow-listed apps.** Every app that will appear on the MHS grid must already be installed on the device, and on Dedicated devices only Required-assigned apps install at all. Same assignment click-path, same static device group. Its What-breaks callout carries a runbook link per `recipe-template.md:29` — route the app-install failure mode to `../l2-runbooks/20-android-app-install-investigation.md`, whose own index row covers an MGP or LOB app showing Failed, Not installed or Pending. Format the link as its own `>` run inside the callout, in the `> See: [title](path)` shape `recipe-template.md:112-113` works.

**Step 4 — choose the sign-in mode.** Its own step, immediately before the payload step, which is what physically separates it from Step 6's hardening. One Case-2 block, three arms: `enable_mhs_signin` FALSE, which is worked and is the documented default; TRUE with `Sign in type` set to Other; TRUE with `Sign in type` set to Microsoft Entra ID. The Entra-ID arm carries the real first-party negative with its account-type scope verbatim — users signing in with a non-Entra account do not get single sign-on to Entra-shared-device-mode-integrated apps but still sign in to Managed Home Screen. Do not import the withdrawn zero-SSO-benefit framing. The two non-worked arms each carry one line for the decision points that are only meaningful when sign-in is TRUE.

Any `[ASSUMED]` note in this step ships as a split blockquote across two blank-line-separated sub-200-character column-0 runs, per the `01:101`/`01:103` idiom — never as plain prose, and never as one long run.

Every step ends with its column-0 `What breaks if misconfigured` callout. Where a consequence is long, use the one-clause bold lead-in with the detail in plain prose beneath, which is the house remedy for that callout type only.
  </action>
  <acceptance_criteria>
    - `grep -c '^### Step ' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 4 at the end of this task.
    - The four step headings match the artifacts table verbatim, including the absence of any apostrophe: `grep -c "^### Step .*'" docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 0.
    - `grep -c '^> \*\*Ask the admin:\*\*' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 3 at the end of this task (token type, provisioning method, sign-in mode).
    - `grep -c '^> \*\*What breaks if misconfigured:\*\*' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 4 at the end of this task.
    - The Case-1 table header is `| Option | When to choose | Consequence if wrong | Branch |` and the two Case-2 table headers are `| Option | When to choose | Recorded as |`.
    - The provisioning-method table has exactly 4 data rows beneath its header.
    - The routing sentence carrying the Knox/Zero-Touch exclusion is not a blockquote: the line does not begin with a `>` character.
    - Every occurrence of the Knox product name and its three-letter initialism is inside link text — the bare-string count equals the bracket-preceded count.
    - `grep -c 'l2-runbooks/20-android-app-install-investigation.md' docs/recipes/04-android-dedicated-mhs-multi-app.md` is at least 1, and `test -f docs/l2-runbooks/20-android-app-install-investigation.md` passes.
    - The D2.9a window is addressed by one of the two named resolutions — either both PIN surfaces are authored before Step 2's assignment instruction, or Step 2 carries a callout naming the window and the forced-sync remediation.
    - `grep -cE '^[[:space:]]+>' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 0.
    - The blockquote measurement reports `max` at most 200 with an empty over-cap list.
    - `git diff --quiet HEAD -- docs/_glossary-android.md docs/reference/android-capability-matrix.md docs/recipes/01-shared-windows-avd-client.md docs/admin-setup-android/05-dedicated-devices.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const f='docs/recipes/04-android-dedicated-mhs-multi-app.md';const L=require('fs').readFileSync(f,'utf8').split(/\r?\n/);let i=0,m=0,over=[];while(i<L.length){if(/^>/.test(L[i])){const s=i,b=[];while(i<L.length&&/^>/.test(L[i])){b.push(L[i].replace(/^>\s?/,''));i++}const n=b.join(' ').length;if(n>m)m=n;if(n>200)over.push((s+1)+':'+n)}else i++}console.log('max='+m+' over='+JSON.stringify(over));if(over.length)process.exit(1)" && node scripts/validation/c17-eee-contract.mjs</automated>
  </verify>
  <done>Steps 1-4 are authored with three decision blocks, the inlined MHS and app deployment click-paths, the D2.9a window addressed by a named resolution, and every blockquote column-0 and under the cap.</done>
</task>

<task type="auto">
  <name>Task 3: Steps 5-6 — the json payload fence, the decomposition table, and the exit-PIN and hardening step</name>
  <reversibility rating="costly">The fence's key set drives the decomposition rows, the Verification lines and the failures rows; changing it after Task 4 lands means re-cutting four coupled structures in one file.</reversibility>
  <read_first>
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-01-SUMMARY.md` — Ruling A, which decides whether the exit-PIN key is in the fence, and Ruling B's sibling-key closure
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-RESEARCH.md` — Finding 5 in full (the envelope and every verbatim key and value-type pairing), Finding 6 (the two designer labels), Finding 4(b) (the policy creation and assignment click-path and its device-group nuance), Finding 3 (the sibling key), Finding 1's PLUS list (the two retry-key quotes and the no-unit contrast)
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-PATTERNS.md` lines 182-212 — the column-0 fence plus decomposition-table pairing and the non-payload-row idiom
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-CONTEXT.md` — D1.2 through D1.10, D2.6, D2.8, D2.11, D3.2 through D3.7
    - `docs/admin-setup-android/05-dedicated-devices.md` lines 243-260 — the exit-PIN section this step cross-links whole and never restates
    - `docs/admin-setup-macos/10-kerberos-sso-extension.md:92` — the placeholder-lead-in idiom
  </read_first>
  <action>
**Step 5 — author and assign the App Configuration policy.** Use RESEARCH.md Finding 4(b)'s click-path: Apps, Configuration, Create, Managed devices; Basics; Platform set to Android Enterprise; Targeted app set to Managed Home Screen; Next to Settings; Configuration settings format set to the JSON entry method, naming the Configuration designer in one line as the equivalent UI path; Next through Scope tags; Assignments, where Assign to takes Add groups and the static Entra device group; Review and create.

State the policy's **own assignment** explicitly. It is the action that makes the payload take effect and a different Intune blade from Step 2's app assignment. Leaving it unstated reproduces the anchor's own Delta-3 failure class one blade over. Finding 4(b) flags that this article's introductory prose says user groups while its own wizard offers Add groups, Add all users and Add all devices — do not let the stale introductory phrasing narrow the instruction, and carry the device-group-in-this-policy-type detail at MEDIUM confidence with an author-time tenant-verification note.

**The fence.** Exactly one code fence in the whole file, at column 0, tagged `json`, placed immediately after the numbered click-list, with the decomposition table after it. The lead-in sentence uses the paste-the-JSON-below idiom from `02-zero-touch-portal.md:106`; do not borrow `07`'s different lead-in.

The envelope is `kind`, `productId` and `managedProperty` — an array of objects each carrying `key` plus one of `valueBool`, `valueString`, `valueInteger` or `valueBundleArray`. It is **not** a flat key-and-value object; RESEARCH.md Finding 5 is the only correct source for this shape and every key-to-value-type pairing there is verbatim first-party. The two existing Android fences in this corpus are flat DPC-extras objects and are structurally different — copy their fence formatting, not their payload shape.

Keys IN the fence: `applications` (bundleArray; `package` only), `grid_size`, `lock_home_screen`, `managed_folders`, `max_number_of_attempts_for_exit_PIN`, `amount_of_time_before_try_exit_PIN_again`, `enable_mhs_signin` set false — plus `exit_lock_task_mode_code` if and only if Plan 1 ruled the `05:253` branch, and then with a placeholder value, never a concrete PIN, with an adjacent cross-link to `#exit-kiosk-pin-synchronization` and the one-line caveat callout Plan 1's ruling specifies. On the other branch the key is absent and the prose line after the fence states the PIN is set in the Device Restrictions profile.

No source markers inside the fence — a comment is not legal JSON. `[ASSUMED]` markers take the split-blockquote form only; `[MEDIUM]` may be inline or in a table row.

**The decomposition table**, header `| # | Setting / JSON key | Configuration designer label | What it does | Worked value |`. The `#` column is in for parity with `03:179`. The label column always exists; cells carry the first-party designer label where one is documented and an em-dash where none is. The two labels RESEARCH.md Finding 6 closed are the allow-listed-applications label and the grid-size label; note that grid size has a real named default rather than being blank, which makes the worked value an explicit override worth stating. Total 10 to 16 data rows counting the header. Include the debug-menu setting as a GUI-label row with an em-dash key — in the table, not the payload. Defaults go in the What-it-does cell where decision-relevant.

A plain-prose summary ships under this table unconditionally. It must be plain prose: the C17 prose scan rejects any line starting with a pipe, hash, angle bracket or fence within its five-line window, and blank lines consume slots, so a Note blockquote does not satisfy it.

**Two Case-3 free-value prompts** live in this step — grid size and managed folders. Case 3 is a lead-in alone with no table: one `> **Ask the admin:**` run, then the routing or recording prose immediately below and outside the blockquote.

**Declined items ship as one sentence each in this step's lead-in**, not as anti-feature rows: the screensaver keys, the widgets/wallpaper/branding keys, the cosmetic ordering and icon keys, and the virtual home button, which is declined with a reason recording that it was a Case-2 candidate. Wallpaper and screensaver decline with their deferral reason. The `applications` sub-keys `enable_app_offline` and `app_available_prior_to_sign_in` are omitted with the reason that both are only meaningful when sign-in is TRUE and are therefore inert here — state that this is a named carve-out from the general rule that sign-in sub-keys are out, since one of them sits inside a key that is in.

**Step 6 — set the exit-kiosk PIN and harden exit retries.** The exit-PIN half is a cross-link on both branches, because re-authoring the sync requirement is barred. It carries exactly **one host sentence**: it names both GUI labels — the Device Restrictions label and the MHS App Configuration label — states the values must match, carries `[MEDIUM: MS Q&A community, last_verified <the fresh date>]` and links `#exit-kiosk-pin-synchronization`. That single sentence is the marked carrier SC2 requires; everything beyond it is a link. There is no separate label-equivalence line and no anti-feature row for it.

The hardening half explains and verifies keys already in the payload. It carries the silent no-op dependency **verbatim** as a one-clause `> **What breaks if misconfigured:**` run with the detail in plain prose beneath: setting max-attempts alone does nothing, with no error and no admin-side signal, unless the retry-delay is also set. State explicitly that **no unit is documented** for the retry-delay integer — sibling timers on the same first-party page say in seconds and this one does not. State that both zeros mean no limit.

Then the contrastive prose MHS-04 requires, now fully sourced: name both `max_number_of_attempts_for_exit_PIN` and `max_number_of_attempts_for_session_PIN` by their exact JSON keys and designer labels, with their opposite gates — the exit key gated on the exit password being configured, the session key gated on session PIN and sign-in both being TRUE — and their different consequences, blocked from retrying the exit versus automatically logged out. The session key stays out of the fence.

Carry the label-spelling inconsistency the first-party page actually publishes rather than silently normalizing it: the max-attempts label and the retry-delay label do not spell the setting name the same way.
  </action>
  <acceptance_criteria>
    - `grep -c '^### Step ' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 6.
    - `grep -c '^```' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns exactly 2, and `grep -c '^```json' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns exactly 1 — one column-0 fence, tagged json.
    - The fence content parses as JSON: extract the fence body and run it through `node -e` with `JSON.parse`, which exits 0.
    - The parsed fence has top-level keys `kind`, `productId` and `managedProperty`, and `managedProperty` is an array in which every element has a `key` property.
    - Every payload key named in this plan's artifacts table appears in the fence; `exit_lock_task_mode_code` appears if and only if `136-01-SUMMARY.md` ruled the `05:253` branch.
    - No line inside the fence contains a `//` comment sequence.
    - The decomposition table header matches `| # | Setting / JSON key | Configuration designer label | What it does | Worked value |` and the table has 10 to 16 rows counting the header.
    - A plain-prose line — not starting with a pipe, hash, angle bracket or backtick — appears within 5 lines after the decomposition table's last row.
    - `grep -c '^> \*\*Ask the admin:\*\*' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 5.
    - `grep -c 'exit-kiosk-pin-synchronization' docs/recipes/04-android-dedicated-mhs-multi-app.md` is at least 1.
    - `grep -c 'MS Q&A community' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns exactly 1 — one marked host sentence, not a repeated marker.
    - Both `max_number_of_attempts_for_exit_PIN` and `max_number_of_attempts_for_session_PIN` appear in the file; the session key does not appear inside the fence body.
    - `grep -cE '^[[:space:]]+>' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 0.
    - The blockquote measurement reports `max` at most 200 with an empty over-cap list.
    - `git diff --quiet HEAD -- docs/_glossary-android.md docs/reference/android-capability-matrix.md docs/recipes/01-shared-windows-avd-client.md docs/admin-setup-android/05-dedicated-devices.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const f='docs/recipes/04-android-dedicated-mhs-multi-app.md';const L=fs.readFileSync(f,'utf8').split(/\r?\n/);const o=L.findIndex(l=>l.startsWith('\`\`\`json'));if(o<0)throw new Error('no column-0 json fence');const c=L.findIndex((l,i)=>i>o&&l.startsWith('\`\`\`'));const body=L.slice(o+1,c).join('\n');const j=JSON.parse(body);if(!j.kind||!j.productId||!Array.isArray(j.managedProperty))throw new Error('envelope missing kind/productId/managedProperty');j.managedProperty.forEach(p=>{if(!('key' in p))throw new Error('managedProperty entry without key')});console.log('fence OK keys='+j.managedProperty.map(p=>p.key).join(','))" && node scripts/validation/c17-eee-contract.mjs</automated>
  </verify>
  <done>Steps 5 and 6 are authored, the single column-0 json fence parses in the confirmed envelope, the decomposition table and its prose summary ship, and the exit-PIN host sentence carries its marker exactly once.</done>
</task>

<task type="auto">
  <name>Task 4: Verification, Rollback/Recovery, Configuration-Caused Failures, See Also, and the closure-table count assertions</name>
  <read_first>
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-PATTERNS.md` lines 213-276 — the Verification checkbox idiom, the Rollback/Recovery shape and position, the failures-table routing idiom, and the See Also entry format
    - `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-CONTEXT.md` — D4.6 (the seven Verification lines and both demotions), D4.3 (Rollback/Recovery ships, its three procedures, its position), D3.4 (the failures table's scope and the real runbook candidate set), D4.9 (See Also contents and the primary FRP placement), D3.7, plus the closure table
    - `.planning/research/FEATURES.md` lines 173-183 — the verification item source
    - `docs/recipes/03-windows-11-multi-app-kiosk.md` lines 256-328 — the four closing sections in file order, the sole corpus instance of the Rollback position
    - `docs/admin-setup-android/05-dedicated-devices.md` lines 243-260 and 296-315 — the remediation this recipe routes to, and the anchor's own 12-row what-breaks table, which this recipe does not merge with or duplicate
  </read_first>
  <action>
**`## Verification`** — exactly seven `- [ ]` lines, all positive observable checks in the density and tone of `03:256-273`. Six from the first-party verification set: the grid shows exactly the allow-listed apps; Home, Overview and back do not surface the standard Android launcher; the 15-press back-button gesture reaches the exit-PIN prompt and the configured code works; folders and apps cannot be dragged or renamed; Wi-Fi switching works but the radio toggle does not; the admin center confirms the Standard token type. The seventh is the demoted debug-menu line — it is already the default on both surfaces, so instructing it would be a no-op, and the line states that only the easy-access entry point can be closed while the 15-press gesture remains.

Zero deliberately-break-it checks in this section. Both demote to the failures table.

Place the Android 15 FRP cross-link here as its primary placement, targeting `05-dedicated-devices.md#android-15-frp-reprovisioning`. The three-pathway table is not re-authored; only the link ships.

**`## Rollback/Recovery`**, positioned between `## Verification` and `## Configuration-Caused Failures`. Shape per `03:275-306`: one lead sentence stating the section's own scope-limiting premise, then bold pseudo-heading groups each followed by a plain bullet list. Zero self-narration — the divergence from the template is named in the SUMMARY, never in the body.

Three procedures the anchor does not own:
1. Returning a device to the standard launcher — unassign the MHS app-config policy or set the MHS app assignment to Uninstall. The anchor gives the assignment procedure and the renewal procedure, neither the reverse.
2. A **forgotten** exit PIN, as distinct from a mismatched one. The anchor's remediation presupposes a known code.
3. An offline device carrying a wrong `applications` allow-list. This is the set's one un-cited premise — cite it first-party at author time or drop the bullet. Dropping it is acceptable; asserting it uncited is not.

**`## Configuration-Caused Failures`** — the 3-column table `| Misconfiguration | Symptom | Runbook |`, at least three rows: the mismatched-PIN case, the max-attempts lockout, and the Step-2-to-Step-6 window if Task 2 took the callout resolution. It covers only failures this recipe's own steps cause and does not merge with or duplicate the anchor's 12-row summary.

Runbook cells: route to a real runbook only where the symptom genuinely fits, otherwise to an in-recipe section anchor in the `03:311` style. The real candidates verified at plan time are `../l1-runbooks/27-android-zte-enrollment-failed.md`, `../l1-runbooks/28-android-knox-enrollment-failed.md`, `../l2-runbooks/19-android-enrollment-investigation.md` and `../l2-runbooks/20-android-app-install-investigation.md`. Verify every target path resolves before shipping; a fabricated runbook link is the specific defect this discipline exists to prevent.

**`## See Also`** — every entry `[Link Text](path)` plus an em-dash plus a one-sentence rationale, `../`-prefixed paths, first entry the STD-05 spec. Contents: the STD-05 standard; the Android 15 FRP anchor section, additive to its primary Verification placement; `01-managed-google-play.md#bind-mgp`; `_glossary-android.md#dedicated`; and the anchor sections the body cross-links. Section-anchor entries are precedented.

**Then assert the closure table.** These are the exact-count needles Phase 138's `check-phase-136.mjs` will read out of `136-VERIFICATION.md`, so every one must hold on the finished file before this plan closes.
  </action>
  <acceptance_criteria>
    - `grep -c '^## ' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns exactly 8, and their order is Summary, Prerequisites, Unsupported and Anti-Feature Callouts, Steps, Verification, Rollback/Recovery, Configuration-Caused Failures, See Also.
    - `grep -c '^### Step ' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns exactly 6.
    - `grep -c '^> \*\*Ask the admin:\*\*' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns exactly 5.
    - `grep -c '^> \*\*What breaks if misconfigured:\*\*' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 5 to 7.
    - `grep -c '^- \[ \] ' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns exactly 7, and all 7 sit under `## Verification`.
    - The Configuration-Caused Failures table has at least 3 data rows and its header is `| Misconfiguration | Symptom | Runbook |`.
    - Every relative link target in the file resolves to an existing path: extract each `](...)` target, strip any `#fragment`, and confirm the file exists relative to `docs/recipes/`. Zero unresolved targets.
    - `grep -c '^```' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns exactly 2.
    - `grep -cE '^[[:space:]]+>' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 0, and the blockquote measurement reports `max` at most 200 with an empty over-cap list.
    - `grep -c 'TEMPLATE-SENTINEL' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 0 and `grep -c '^status: Draft$' docs/recipes/04-android-dedicated-mhs-multi-app.md` returns 1.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 and reports `234 files checked, 0 with violations, 0 total violations`.
    - `git diff --quiet HEAD -- docs/_glossary-android.md docs/reference/android-capability-matrix.md docs/recipes/01-shared-windows-avd-client.md docs/admin-setup-android/05-dedicated-devices.md` exits 0.
    - `git status --porcelain docs/_registry/ docs/index.md scripts/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const f='docs/recipes/04-android-dedicated-mhs-multi-app.md';const t=fs.readFileSync(f,'utf8');const L=t.split(/\r?\n/);const c=(re)=>L.filter(l=>re.test(l)).length;const chk=[['H2',c(/^## /),8,8],['Step',c(/^### Step /),6,6],['Ask',c(/^> \*\*Ask the admin:\*\*/),5,5],['Breaks',c(/^> \*\*What breaks if misconfigured:\*\*/),5,7],['Verify',c(/^- \[ \] /),7,7],['Fence',c(/^\`\`\`/),2,2],['Indented',c(/^[ \t]+>/),0,0]];let bad=chk.filter(([n,v,lo,hi])=>v<lo||v>hi);console.log(chk.map(([n,v])=>n+'='+v).join(' '));const miss=[...t.matchAll(/\]\((\.\.?\/[^)#]+)(#[^)]*)?\)/g)].map(m=>m[1]).filter(r=>!fs.existsSync(p.join('docs/recipes',r)));if(miss.length){console.log('UNRESOLVED='+JSON.stringify([...new Set(miss)]));bad.push(['links'])}if(bad.length){console.log('FAIL='+JSON.stringify(bad));process.exit(1)}console.log('CLOSURE-TABLE OK')" && node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -E '234 files checked, 0 with violations, 0 total violations'</automated>
  </verify>
  <done>All four closing sections ship, every closure-table count holds on the finished file, every relative link resolves, and full-corpus C17 is green at 234 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the recipe -> a production device fleet | An admin executes this document against real Dedicated hardware; a wrong worked value or an unaddressed configuration window has physical-device consequences |
| live Microsoft Learn -> the corpus | Externally sourced click-paths and key spellings are transcribed into an Approved-track document |
| this phase -> the frozen validator surface | Content edits can shift line coordinates that 16 frozen sidecars pin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136-06 | Information Disclosure | the `exit_lock_task_mode_code` value in the json fence | high | mitigate | On the `05:253` branch the key ships a placeholder only, never a concrete PIN, with an adjacent cross-link to the sync section. Task 3's acceptance criteria assert the key's presence is branch-conditional; a real PIN in a published recipe would be a copyable kiosk-exit credential. |
| T-136-07 | Denial of Service | a real device fleet during the Step-2-to-Step-6 window | critical | mitigate | Task 2 must take one of two named resolutions; a Required MHS assignment with no exit PIN yet leaves no exit affordance at all, which the anchor ranks CRITICAL and first in its own failure table. Shipping it unaddressed is barred by acceptance criteria. |
| T-136-08 | Denial of Service | frozen pin surfaces and the apex validator chain | critical | mitigate | Every task's `<verify>` runs `git diff --quiet HEAD` against the three frozen paths plus the anchor; a line-shifting edit fails the task rather than the next milestone's chain. |
| T-136-09 | Spoofing | fabricated runbook or anchor cross-links | high | mitigate | Task 4's verify extracts every relative link target and asserts the path exists. The four real runbook candidates were verified at plan time; D3.4's discipline forbids routing to a plausible-looking runbook that does not cover the symptom. |
| T-136-10 | Tampering | the anchor and the two frozen reference docs | high | mitigate | Guardrails 1-4 plus the per-task `git diff --quiet` assertion; this plan cross-links and never edits. |
| T-136-11 | Repudiation | an unrecorded author-time judgment | medium | mitigate | Three calls are delegated to author time — the M-A row-or-sentence disposition, the D2.9a resolution, and which failures rows route to a real runbook. Each is recorded in `136-02-SUMMARY.md`, never as body meta-commentary. |

No package-manager install occurs in this phase, so the supply-chain threat row and the Package Legitimacy Gate do not apply.
</threat_model>

<verification>
Run after all four tasks:

1. The closure-table assertion command in Task 4's `<verify>` prints `CLOSURE-TABLE OK` and exits 0.
2. `node scripts/validation/c17-eee-contract.mjs` exits 0 and reports `234 files checked, 0 with violations, 0 total violations`.
3. The blockquote measurement reports `max` at most 200 with an empty over-cap list.
4. `git diff --quiet HEAD -- docs/_glossary-android.md docs/reference/android-capability-matrix.md docs/recipes/01-shared-windows-avd-client.md docs/admin-setup-android/05-dedicated-devices.md` exits 0.
5. `git status --porcelain docs/_registry/ docs/index.md scripts/` is empty — Phase 137's and Phase 138's surfaces are untouched.
6. The anti-feature table has 9 or 10 data rows and the decomposition table has 10 to 16 rows counting its header.
</verification>

<success_criteria>
- SC1-SC5 hold on the finished file with no clause reduced.
- Every count in CONTEXT's closure table holds exactly: 8 H2s, 6 `### Step` headings, 5 decision blocks and 5 `Ask the admin` lead-ins, ~6 What-breaks callouts, 9-10 anti-feature rows, 10-16 decomposition rows plus a prose summary, 7 Verification lines, at least 3 failures rows, exactly 1 column-0 json fence, frontmatter tuple intact.
- The fence parses as JSON in the confirmed `kind`/`productId`/`managedProperty` envelope.
- Every blockquote is column-0 and under the 200-character cap after the prefix strip.
- Every relative link target resolves.
- Zero edits to the anchor and the three frozen surfaces; zero Phase-137 or Phase-138 work.
</success_criteria>

<output>
Create `.planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-02-SUMMARY.md` when done.

**Mandatory contents** — `136-VERIFICATION.md` asserts against it and Phase 138 derives needles from it:

1. **The named `## Rollback/Recovery` template divergence** — recorded here, never in the recipe body.
2. **The named runbook-link first** — `recipe-template.md:29` has been silently declined by all three shipped recipes with live targets available; RE-225's compliance is deliberate. Record which failures rows and callouts route to a real runbook and which route to an in-recipe anchor.
3. **The three author-time judgments**: the M-A row-or-sentence disposition and the resulting 9-or-10 anti-feature row count; the D2.9a resolution taken; and whether the offline-wrong-allow-list Rollback bullet was cited first-party or dropped.
4. **The closure-table actuals** — the measured value of every count, so Phase 138 writes needles against reality rather than against the planned range.
5. **The D0.1 reconciliation**, restated: SC1's *cross-linked (never re-authored)* and SC2's marked exit-PIN sync are both satisfied by exactly one marked host sentence.
6. **Any `[MEDIUM]`-confidence click-path detail** carried forward for author-time tenant verification.

Every claim of the form *the repo supplies X* / *X is the only Y* / *no source states Z* carries a `file:line` (D0.3).
</output>
