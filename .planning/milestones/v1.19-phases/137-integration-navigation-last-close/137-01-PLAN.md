---
phase: 137-integration-navigation-last-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/recipes/03-windows-11-multi-app-kiosk.md
  - docs/recipes/04-android-dedicated-mhs-multi-app.md
  - docs/_registry/RE-index.md
  - scripts/pipeline/filename-map.md
  - scripts/pipeline/build-filename-map.mjs
  - scripts/pipeline/build-publish-bundle.mjs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CLASS-05]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "Both recipes convert cleanly through pandoc + guard-docx BEFORE the flip commit: guard-docx.mjs reports `3 PASS, 0 FAIL, 0 SKIPPED` for each (D-24)"
    - "Both recipes read Approved at BOTH metadata sites: `grep -c '^status: Approved'` returns 1 and `grep -c 'Status:\\*\\* Approved'` returns 1 in each of docs/recipes/03-windows-11-multi-app-kiosk.md and docs/recipes/04-android-dedicated-mhs-multi-app.md"
    - "docs/_registry/RE-index.md carries RE-224 and RE-225 rows at `| Guide | Approved |` INSIDE the table: `grep -cE '^\\| RE-[0-9]+ \\|' docs/_registry/RE-index.md` returns 225, and both new rows have a lower line number than the `## Review Notes` heading"
    - "scripts/pipeline/filename-map.md is regenerated by the generator (never hand-edited) and carries the exact stems `windows-11-multi-app-kiosk-assigned-access-provisioning.docx` and `android-dedicated-multi-app-kiosk-managed-home-screen-provisioning.docx`"
    - "`node scripts/pipeline/build-filename-map.mjs --self-test` exits 0 printing `8 passed, 0 failed`"
    - "`node scripts/pipeline/build-publish-bundle.mjs --self-test` exits 0 printing `15 passed, 0 failed` (HEAD baseline was `14 passed, 1 failed` with `rows.length=223`)"
    - "The filename-map regeneration and BOTH canary bumps land in ONE commit: `git show --stat` on the registry commit lists RE-index.md, filename-map.md, build-filename-map.mjs and build-publish-bundle.mjs together (SC2 binding atom, D-15)"
    - "`node scripts/validation/c17-eee-contract.mjs` exits 0 with `234 files checked, 0 with violations` after EVERY commit in this plan (D-17)"
    - "REQUIREMENTS.md CLASS-05 and ROADMAP.md Phase-137 SC2 each name BOTH canary scripts and both bump values (D-23); STATE.md no longer asserts `zero pipeline code changes expected` for Phase 137 (D-25)"
    - "All 17 predecessor leaf validators green at HEAD (54, 57, 59, 87, 92, 99, 110, 114, 123, 124, 125, 126, 127, 129, 130, 131, 132) still exit 0"
  artifacts:
    - path: "docs/recipes/03-windows-11-multi-app-kiosk.md"
      provides: "RE-224 recipe, Approved at frontmatter :3 and byline :13; body prose frozen"
      contains: "status: Approved"
    - path: "docs/recipes/04-android-dedicated-mhs-multi-app.md"
      provides: "RE-225 recipe, Approved at frontmatter :3 and byline :13; body prose frozen"
      contains: "status: Approved"
    - path: "docs/_registry/RE-index.md"
      provides: "RE-224 + RE-225 rows at Guide/Approved, inside the table"
      contains: "| RE-225 | docs/recipes/04-android-dedicated-mhs-multi-app.md |"
    - path: "scripts/pipeline/filename-map.md"
      provides: "Regenerated 225-row map carrying both new .docx output stems"
      contains: "android-dedicated-multi-app-kiosk-managed-home-screen-provisioning.docx"
    - path: "scripts/pipeline/build-filename-map.mjs"
      provides: "Row-count drift canary bumped 223 -> 225 at 3 sites"
      contains: "rows.length === 225"
    - path: "scripts/pipeline/build-publish-bundle.mjs"
      provides: "Second (previously-missed) row-count drift canary bumped 221 -> 225 at 4 literal sites"
      contains: "rows.length === 225"
  key_links:
    - from: "docs/_registry/RE-index.md"
      to: "scripts/pipeline/filename-map.md"
      via: "node scripts/pipeline/build-filename-map.mjs (parseRegistry reads every /^\\|\\s*RE-\\d+\\s*\\|/ row; Title column drives the .docx stem)"
      pattern: "RE-22[45]"
    - from: "docs/recipes/0[34]-*.md frontmatter status: Approved"
      to: "scripts/pipeline/build-publish-bundle.mjs Approved-only selection"
      via: "build-publish-bundle.mjs:312 status === 'Approved' filter; the flip is what drags both recipes into the publish set"
      pattern: "status === 'Approved'"
---

<objective>
Register both already-authored recipes into the publish pipeline and close the two-canary drift class. Pre-flight both recipes through pandoc + guard-docx (D-24), flip RE-224/RE-225 from Draft to Approved at both metadata sites (Commit A), then append their registry rows, regenerate `filename-map.md`, and bump BOTH row-count drift canaries in one atomic commit (Commit B). Finally correct the three planning-doc prose defects that the second canary's year-long invisibility depended on (D-23/D-25). This satisfies CLASS-05.

Purpose: This is the wave-1 half of D-13's two-plan split, carrying Commits A and B of D-14's three-commit flip-first topology. It proves the registry -> filename-map -> publish-bundle pipeline end-to-end on real recipe content, and closes the FILENAME-MAP-SELFTEST-DRIFT recurring-lesson class by making BOTH canaries move together as a named deliverable rather than a post-close audit finding.
Output: Two Approved recipes, two registry rows, a regenerated 225-row map, two green `--self-test` harnesses, and three corrected planning-doc claims.

<must_honor priority="ABSOLUTE">
1. **Recipe body prose is FROZEN.** The ONLY edits to `docs/recipes/03-*.md` and `04-*.md` are the two metadata sites each (frontmatter `status:` at `:3`, `**...Status:**` byline at `:13`). Nothing else in either file changes — not `doc_type`, not `applies_to`, not one word of body content.
2. **`scripts/pipeline/filename-map.md` is GENERATED — never hand-edit it.** Regenerate by running `node scripts/pipeline/build-filename-map.mjs`.
3. **`scripts/validation/check-phase-132.mjs` is a v1.18 frozen surface — do NOT open or edit it** (D-04). Its `V-132-HUBSNOTWIRED` regex provably does not match `recipes/03-` / `recipes/04-`; the fix is additive and belongs to the Phase-138 needle-spec (handled in plan 137-02), not here.
4. **Do NOT write "zero pipeline code changes"** into any commit message, SUMMARY, or verification note. Phase 137 DOES change pipeline code (the two canary integers). Phase 132's commit body used that phrase and `STATE.md:307` still repeats it — that stale claim is the most plausible reason the second canary went unnoticed for an entire milestone, and Task 4 deletes it. Do not re-import it (D-25).
5. **Do NOT run `git diff --quiet scripts/pipeline/build-filename-map.mjs` as a gate.** It was a Phase-132 truth and is EXPECTED TO FAIL here. Its replacement is the two `--self-test` runs.
6. **`build-publish-bundle.mjs --self-test` is ALREADY RED at HEAD** (`14 passed, 1 failed`, `(a) ... rows.length=223` against a hardcoded 221). This is known, named, pre-existing drift that this plan is chartered to close — not a regression to investigate.
</must_honor>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/137-integration-navigation-last-close/137-CONTEXT.md
@.planning/phases/137-integration-navigation-last-close/137-RESEARCH.md
@.planning/phases/137-integration-navigation-last-close/137-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-24 pandoc + guard-docx pre-flight on BOTH recipes (gates the flip)</name>
  <files>(no tracked file changes — output lands in .pipeline-output/, which is gitignored)</files>
  <read_first>
    - .planning/phases/137-integration-navigation-last-close/137-CONTEXT.md (D-24 — why this gate exists: `build-publish-bundle.mjs:312` selects on `status === 'Approved'`, so the Commit-A flip is precisely the act that drags RE-225 into the publish set, and Phase 138 has no content-remediation budget)
    - .planning/phases/137-integration-navigation-last-close/137-RESEARCH.md (Pattern 5 — the exact `pwsh -NoProfile -File` invocation, and the recorded result that both recipes already pass this gate)
    - scripts/pipeline/convert.ps1 (lines 1-15: `#Requires -Version 7.0`, the pinned pandoc version at :3, and the `-InputMd` / `-OutputDocx` parameter names)
    - scripts/pipeline/guard-docx.mjs (lines 1-40: the three checks it runs — YAML-leak, heading-style, custom-props)
  </read_first>
  <action>
    Run the full conversion pre-flight against BOTH recipe files, before any file in this plan is modified.

    For `docs/recipes/03-windows-11-multi-app-kiosk.md`:
    run `pwsh -NoProfile -File scripts/pipeline/convert.ps1 -InputMd docs/recipes/03-windows-11-multi-app-kiosk.md -OutputDocx .pipeline-output/03-windows-11-multi-app-kiosk.docx`, then `node scripts/pipeline/guard-docx.mjs .pipeline-output/03-windows-11-multi-app-kiosk.docx`.

    For `docs/recipes/04-android-dedicated-mhs-multi-app.md`:
    run `pwsh -NoProfile -File scripts/pipeline/convert.ps1 -InputMd docs/recipes/04-android-dedicated-mhs-multi-app.md -OutputDocx .pipeline-output/04-android-dedicated-mhs-multi-app.docx`, then `node scripts/pipeline/guard-docx.mjs .pipeline-output/04-android-dedicated-mhs-multi-app.docx`.

    RE-225 is the higher-risk of the two: it ships a column-0 JSON fence, and `grep -rli 'guard-docx|pandoc'` over the phase artifacts returns 9 Phase-135 hits and ZERO Phase-136 hits — nobody has ever converted this file. Capture the full stdout of all four commands verbatim into `137-01-SUMMARY.md`; those strings are the phase's only evidence this gate ran.

    Delete the two `.docx` files from `.pipeline-output/` afterwards. Leave no tracked-file residue and do not add `.pipeline-output/` to git.

    FALLBACK BRANCH (D-24, only if `pwsh` or `pandoc` is absent from PATH): record a NOT-RUN in `137-01-SUMMARY.md` naming which binary was missing, draft a `v1.19-DEFERRED-CLEANUP.md` candidate entry stating that RE-225's conversion is unproven entering Phase 138, and PROCEED to Task 2 anyway — the flip is not blocked by a missing toolchain. Do NOT take this branch if both binaries are present and a check merely fails; a real guard-docx failure means the recipe body needs remediation and the phase must halt for a decision.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/pipeline/convert.ps1 -InputMd docs/recipes/03-windows-11-multi-app-kiosk.md -OutputDocx .pipeline-output/03-windows-11-multi-app-kiosk.docx && node scripts/pipeline/guard-docx.mjs .pipeline-output/03-windows-11-multi-app-kiosk.docx && pwsh -NoProfile -File scripts/pipeline/convert.ps1 -InputMd docs/recipes/04-android-dedicated-mhs-multi-app.md -OutputDocx .pipeline-output/04-android-dedicated-mhs-multi-app.docx && node scripts/pipeline/guard-docx.mjs .pipeline-output/04-android-dedicated-mhs-multi-app.docx</automated>
  </verify>
  <acceptance_criteria>
    - `convert.ps1` exits 0 for BOTH recipes, printing a pandoc version-guard PASS line
    - `node scripts/pipeline/guard-docx.mjs` exits 0 for BOTH `.docx` outputs, printing `3 PASS, 0 FAIL, 0 SKIPPED` each
    - all four command outputs are quoted verbatim in `137-01-SUMMARY.md`
    - `git status --porcelain` shows no new tracked or untracked file under `.pipeline-output/` after cleanup
    - `git diff --quiet docs/recipes/03-windows-11-multi-app-kiosk.md docs/recipes/04-android-dedicated-mhs-multi-app.md` succeeds — this task modifies nothing
    - this task completed (or recorded its NOT-RUN branch) BEFORE Task 2's commit exists
  </acceptance_criteria>
  <done>Both recipes proven pandoc-convertible and guard-docx-clean, with verbatim evidence recorded; no tracked file changed; the flip is unblocked.</done>
</task>

<task type="auto">
  <name>Task 2: Commit A — two-site Draft to Approved flip on both recipes</name>
  <files>docs/recipes/03-windows-11-multi-app-kiosk.md, docs/recipes/04-android-dedicated-mhs-multi-app.md</files>
  <read_first>
    - docs/recipes/03-windows-11-multi-app-kiosk.md (lines 1-15: frontmatter block with `status:` at :3, and the `**Platform:** Windows · **Doc Type:** Guide · **Doc ID:** RE-224 · **Status:**` byline at :13)
    - docs/recipes/04-android-dedicated-mhs-multi-app.md (lines 1-15: same two sites, `**Platform:** Android ... **Doc ID:** RE-225 · **Status:**` at :13)
    - .planning/phases/137-integration-navigation-last-close/137-PATTERNS.md (the `996dcead` analog diff — 2 insertions / 2 deletions per file, doc_type untouched)
  </read_first>
  <action>
    In EACH of the two recipe files, change exactly two lines:
    - line 3, the frontmatter value: `status: Draft` becomes `status: Approved`
    - line 13, the byline's trailing token: `· **Status:** Draft` becomes `· **Status:** Approved`

    Four line edits total across the two files. Change nothing else. `doc_type: Guide` stays `Guide` in both (locked Phase 129 D-02 — never a "Recipe" value). `applies_to`, `last_verified`, `review_by`, `owner`, `platform`, `audience`, and every line of body prose stay byte-identical.

    Then run the full-corpus C17 validator and commit. Commit subject: `docs(137-01): flip RE-224/RE-225 recipes Draft to Approved`. Body: one bullet per file naming the two sites, plus a line stating doc_type is unchanged and no body content was edited. Do NOT mention the pipeline in this commit body.

    This commit MUST land before Task 3's commit — flip-first is the D-14 topology and the safe direction for the `build-publish-bundle.mjs:137-149` divergence guard (an Approved doc absent from the registry is merely unselected; an Approved registry row pointing at Draft frontmatter fails closed).
  </action>
  <verify>
    <automated>test $(grep -c '^status: Approved' docs/recipes/03-windows-11-multi-app-kiosk.md) -eq 1 && test $(grep -c '^status: Approved' docs/recipes/04-android-dedicated-mhs-multi-app.md) -eq 1 && test $(grep -c 'Status:\*\* Approved' docs/recipes/03-windows-11-multi-app-kiosk.md) -eq 1 && test $(grep -c 'Status:\*\* Approved' docs/recipes/04-android-dedicated-mhs-multi-app.md) -eq 1 && test $(grep -c 'doc_type: Guide' docs/recipes/03-windows-11-multi-app-kiosk.md) -eq 1 && test $(grep -c 'doc_type: Guide' docs/recipes/04-android-dedicated-mhs-multi-app.md) -eq 1 && node scripts/validation/c17-eee-contract.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^status: Approved'` returns 1 for each recipe file
    - `grep -c 'Status:\*\* Approved'` returns 1 for each recipe file
    - `grep -c 'doc_type: Guide'` still returns 1 for each recipe file
    - `git diff --numstat HEAD~1 -- docs/recipes/03-windows-11-multi-app-kiosk.md` reports exactly `2	2`; same for the 04 file — proving only the two metadata sites moved and body prose is frozen
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 reporting `234 files checked, 0 with violations, 0 total violations`
    - the commit contains exactly two files and no pipeline or registry file
  </acceptance_criteria>
  <done>Commit A exists: both recipes Approved at frontmatter and byline, doc_type Guide unchanged, 2/2 line churn per file, C17 green.</done>
</task>

<task type="auto">
  <name>Task 3: Commit B — registry rows + filename-map regeneration + BOTH canary bumps (the SC2 atom)</name>
  <files>docs/_registry/RE-index.md, scripts/pipeline/filename-map.md, scripts/pipeline/build-filename-map.mjs, scripts/pipeline/build-publish-bundle.mjs</files>
  <read_first>
    - docs/_registry/RE-index.md (lines 236-243: the RE-222/RE-223 rows at :238-239, the blank line at :240, and the `## Review Notes` prose heading at :241 — the table does NOT end the file)
    - scripts/pipeline/build-filename-map.mjs (lines 74-81 for the slug algorithm; lines 100-110 for `parseRegistry`'s `/^\|\s*RE-\d+\s*\|/` row filter; lines 272-284 for the canary comment/label/assertion)
    - scripts/pipeline/build-publish-bundle.mjs (line 7 header comment; lines 512-522 for the `stTry`/`stAssert` label pair and the assertion)
    - scripts/pipeline/filename-map.md (lines 1-11: the GENERATED FILE header naming the registry Title column as the source of truth for the .docx output name)
    - .planning/phases/137-integration-navigation-last-close/137-PATTERNS.md (the `fb179bfa` registry+regen analog and the `6acc429b` canary-bump analog)
  </read_first>
  <reversibility rating="costly">The two registry Title strings drive the published `.docx` filenames, which become Copilot citation titles — changing a Title after publish orphans existing citations. The Titles are therefore copied verbatim from D-09 (themselves inherited from Phases 135/136 and never re-derived), not composed here.</reversibility>
  <action>
    Perform these four steps in this exact order, then make ONE commit containing all four files.

    STEP 1 — append two rows to `docs/_registry/RE-index.md`, immediately after the RE-223 row at line 239 and BEFORE the blank line preceding `## Review Notes`. The rows are verbatim, 5-column, byte-shape-identical to the RE-222/RE-223 rows above them:

    `| RE-224 | docs/recipes/03-windows-11-multi-app-kiosk.md | Windows 11 Multi-App Kiosk: Assigned Access Provisioning | Guide | Approved |`
    `| RE-225 | docs/recipes/04-android-dedicated-mhs-multi-app.md | Android Dedicated Multi-App Kiosk: Managed Home Screen Provisioning | Guide | Approved |`

    The Title column values are fixed by D-09 and are each recipe's H1 verbatim — do NOT re-derive, reword, or re-case them. Do NOT add a `## Review Notes` entry: the RE-224/RE-225 template divergence is already recorded per 135-CONTEXT.md D2.2 as a phase-SUMMARY item plus a deferred-cleanup candidate, and no corpus precedent exists for a live doc annotating its own divergence in the registry.

    Placement matters and no tool will catch a mistake: `parseRegistry`'s row filter would happily count a row pasted after the prose heading, so the canary is blind to misplacement. The rows must be inside the contiguous table block.

    STEP 2 — regenerate the map by running `node scripts/pipeline/build-filename-map.mjs` (no flags). Never hand-edit `scripts/pipeline/filename-map.md`. The generator derives each `.docx` stem from the registry Title via its 5-step slug; the expected new stems are `windows-11-multi-app-kiosk-assigned-access-provisioning.docx` and `android-dedicated-multi-app-kiosk-managed-home-screen-provisioning.docx`, both pre-verified collision-free against the existing map, so neither row takes the disambiguation branch.

    STEP 3 — bump the first canary in `scripts/pipeline/build-filename-map.mjs`, 223 to 225, at all three sites: the comment at :274, the assertion label string at :281, and the numeric assertion at :282. Extend the existing provenance comment at :275 so it reads as a chain, e.g. `(bumped 221 -> 223 at v1.18 close: RE-222/RE-223 recipes added in Phase 132; bumped 223 -> 225 in Phase 137: RE-224/RE-225 recipes added;`.

    STEP 4 — bump the second canary in `scripts/pipeline/build-publish-bundle.mjs`, 221 to 225, at all four literal sites: the header comment at :7 (`(221 today)`), the comment at :515, the `stTry` label at :516, and the `stAssert` label plus its `rows.length === 221` comparison at :519. Add one provenance line near :515 recording BOTH bumps so the next milestone greps it — that this canary was missed at the v1.18 close (221 -> 223 never applied, leaving it RED for a full milestone) and is taken straight to 225 here. Exact comment placement is yours.

    Then run BOTH self-tests and C17, and commit all four files together. Splitting this into multiple commits violates the SC2 binding atom. Commit subject: `docs(137-01): register RE-224/RE-225, regenerate filename-map, bump both row-count canaries`. In the commit body, name both canary files with their before/after values — and do NOT use the phrase Phase 132's analog commit used to describe an unchanged generator, because this commit changes generator code on purpose.
  </action>
  <verify>
    <automated>test $(grep -cE '^\| RE-[0-9]+ \|' docs/_registry/RE-index.md) -eq 225 && test $(awk '/^\| RE-225 \|/{r=NR} /^## Review Notes/{n=NR} END{print (r>0 && n>0 && r<n)?1:0}' docs/_registry/RE-index.md) -eq 1 && grep -q 'windows-11-multi-app-kiosk-assigned-access-provisioning.docx' scripts/pipeline/filename-map.md && grep -q 'android-dedicated-multi-app-kiosk-managed-home-screen-provisioning.docx' scripts/pipeline/filename-map.md && node scripts/pipeline/build-filename-map.mjs --self-test && node scripts/pipeline/build-publish-bundle.mjs --self-test && node scripts/validation/c17-eee-contract.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\| RE-[0-9]+ \|' docs/_registry/RE-index.md` returns 225 (was 223)
    - `grep -c '^| RE-224 |' docs/_registry/RE-index.md` returns 1 and `grep -c '^| RE-225 |' docs/_registry/RE-index.md` returns 1
    - both new rows appear at a LOWER line number than the `## Review Notes` heading (awk order check passes) — inside the table, not after the prose
    - `grep -c '^## Review Notes' docs/_registry/RE-index.md` still returns 1 and the RE-212 entry beneath it is unchanged
    - `scripts/pipeline/filename-map.md` contains the exact stem `windows-11-multi-app-kiosk-assigned-access-provisioning.docx` and the exact stem `android-dedicated-multi-app-kiosk-managed-home-screen-provisioning.docx`
    - `node scripts/pipeline/build-filename-map.mjs --self-test` exits 0 printing `8 passed, 0 failed`
    - `node scripts/pipeline/build-publish-bundle.mjs --self-test` exits 0 printing `15 passed, 0 failed` (HEAD baseline: `14 passed, 1 failed` with `rows.length=223`)
    - `grep -c 'rows.length === 225' scripts/pipeline/build-publish-bundle.mjs` returns 1; `grep -c 'exactly 225 rows' scripts/pipeline/build-publish-bundle.mjs` returns 3 (comment at :515 + `stTry` label + `stAssert` label); `grep -c '(225 today)' scripts/pipeline/build-publish-bundle.mjs` returns 1
    - `grep -c 'rows.length === 225' scripts/pipeline/build-filename-map.mjs` returns 1 and `grep -c 'exactly 225 rows' scripts/pipeline/build-filename-map.mjs` returns 2 (comment + label)
    - no absence-of-old-value grep is used as a gate here: the provenance comment this task deliberately adds must name the superseded counts, so the pair of green `--self-test` runs above is the proof that no live assertion still carries a stale integer
    - `git show --stat HEAD` for this commit lists exactly these four paths: `docs/_registry/RE-index.md`, `scripts/pipeline/filename-map.md`, `scripts/pipeline/build-filename-map.mjs`, `scripts/pipeline/build-publish-bundle.mjs`
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 reporting `234 files checked, 0 with violations`
  </acceptance_criteria>
  <done>Commit B exists as one atom: 225 registry rows, a regenerated map carrying both expected stems, both canaries at 225, both self-tests green, C17 green.</done>
</task>

<task type="auto">
  <name>Task 4: Correct the three planning-doc claims that hid the second canary (D-23 + D-25)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 30, CLASS-05 — the sentence beginning "The `build-filename-map.mjs --self-test` row-count drift-canary is **bumped 223 -> 225 in the same commit as the regeneration**")
    - .planning/ROADMAP.md (line 112, Phase-137 Success Criterion 2 — "`build-filename-map.mjs --self-test`'s row-count drift-canary is bumped 223 -> 225 in the SAME commit as the filename-map regeneration.")
    - .planning/STATE.md (line 307 — the bullet ending "Phase 137 is standard mechanical execution, zero pipeline code changes expected")
    - .planning/phases/137-integration-navigation-last-close/137-RESEARCH.md (the finding that no validator pins any of these three strings: check-phase-70 reads REQUIREMENTS/ROADMAP/STATE at the FROZEN v1.7 close SHA via `readAtV17Close`, not at HEAD; check-phase-66's ROADMAP reference is a comment, not an assertion)
  </read_first>
  <action>
    Three scoped prose corrections. Use Edit with tight anchors — never rewrite any of these files wholesale.

    (1) `.planning/REQUIREMENTS.md` CLASS-05: replace the single-canary sentence with one naming both. Replacement text: "Both registry-row-count drift canaries are **bumped in the same commit as the regeneration** — `build-filename-map.mjs --self-test` 223 -> 225 and `build-publish-bundle.mjs --self-test` 221 -> 225 (the second canary was missed at the v1.18 close and has been failing ever since) — this is a named deliverable, not a post-close audit finding, per the milestone's own #1 recurring lesson". Keep the surrounding sentences of CLASS-05 intact, including the trailing clause about descriptive filenames driving Copilot citation titles.

    (2) `.planning/ROADMAP.md` Phase-137 SC2: replace the criterion body with "BOTH registry-row-count drift canaries are bumped in the SAME commit as the filename-map regeneration — `build-filename-map.mjs --self-test` 223 -> 225 and `build-publish-bundle.mjs --self-test` 221 -> 225 (the latter has been failing since the v1.18 close)." Preserve the `  2. ` numbering and indentation exactly; SC1 and SC3-SC5 are untouched.

    (3) `.planning/STATE.md` line 307: replace everything after the em-dash so the bullet reads "Registry -> filename-map -> publish-bundle pipeline (v1.15-v1.18) is unchanged and generic — Phase 137's only pipeline edits are the two registry-row-count drift canaries (`build-filename-map.mjs` 223 -> 225 and `build-publish-bundle.mjs` 221 -> 225), which are named CLASS-05 deliverables". The claim being deleted directly contradicted CLASS-05, ROADMAP SC2, and STATE.md:345, and is the most plausible reason the second canary went unnoticed for a milestone. Do not touch STATE.md:334 or :345 in this task.

    Commit these three files alone. Subject: `docs(137-01): name both drift canaries in CLASS-05/SC2 and correct the STATE pipeline claim`.

    Then run the regression gate: the 17 predecessor leaf validators verified green at HEAD during planning must still exit 0 — `for n in 54 57 59 87 92 99 110 114 123 124 125 126 127 129 130 131 132; do node scripts/validation/check-phase-$n.mjs >/dev/null || echo "REGRESSED: $n"; done`. Record the result in `137-01-SUMMARY.md`.

    Recorded exclusions from that gate, with reasons — do NOT try to make these green:
    - `check-phase-61.mjs` is ALREADY RED at HEAD (`30 PASS, 4 FAIL`, a `docs/_glossary-android.md:145` supervision-classifier divergence) and is unrelated to this phase's change surface. Re-run it once and confirm the count is still exactly `4 FAIL` — a no-regression check, not a pass gate.
    - `check-phase-65/66/70.mjs` are chain runners that chain over the red 61 and are therefore also red at HEAD (65 measured at `26 PASS, 7 FAIL`); they each exceed 120s. Of the three, only `check-phase-65.mjs`'s `V-65-11` reads a Phase-137 mutation target at HEAD, and it belongs to plan 137-02's file, where it is gated directly.
  </action>
<!-- planner-discipline-allow: zero pipeline code changes expected -->
  <verify>
    <automated>grep -q 'build-publish-bundle.mjs --self-test` 221' .planning/REQUIREMENTS.md && grep -q 'build-publish-bundle.mjs --self-test` 221' .planning/ROADMAP.md && test $(grep -c 'zero pipeline code changes expected' .planning/STATE.md) -eq 0 && grep -q 'build-publish-bundle.mjs` 221' .planning/STATE.md && for n in 54 57 59 87 92 99 110 114 123 124 125 126 127 129 130 131 132; do node scripts/validation/check-phase-$n.mjs >/dev/null 2>&1 || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'build-publish-bundle' .planning/REQUIREMENTS.md` returns at least 1, inside the CLASS-05 bullet
    - `grep -c 'build-publish-bundle' .planning/ROADMAP.md` returns at least 1, inside Phase 137's Success Criterion 2
    - `grep -c 'zero pipeline code changes expected' .planning/STATE.md` returns 0
    - `grep -c 'build-publish-bundle' .planning/STATE.md` returns at least 1
    - `grep -c '^- \[ \] \*\*CLASS-05\*\*' .planning/REQUIREMENTS.md` still returns 1 (the requirement is amended, not re-checkboxed — the close-gate flip is Phase 138's job)
    - ROADMAP Phase-137 still lists exactly 5 numbered success criteria; SC1 and SC3-SC5 are byte-unchanged (`git diff` shows a single-line hunk for SC2)
    - all 17 of check-phase-{54,57,59,87,92,99,110,114,123,124,125,126,127,129,130,131,132}.mjs exit 0
    - `node scripts/validation/check-phase-61.mjs` still reports exactly `4 FAIL` (pre-existing, unrelated, unchanged)
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 reporting `234 files checked, 0 with violations`
  </acceptance_criteria>
  <done>Both canaries are named in CLASS-05 and ROADMAP SC2, the STATE pipeline claim is corrected, and no predecessor validator regressed.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 137-01's share)

| Artifact | Exact value | Where |
|----------|-------------|-------|
| Registry row RE-224 | `\| RE-224 \| docs/recipes/03-windows-11-multi-app-kiosk.md \| Windows 11 Multi-App Kiosk: Assigned Access Provisioning \| Guide \| Approved \|` | `docs/_registry/RE-index.md`, inside the table, after RE-223 |
| Registry row RE-225 | `\| RE-225 \| docs/recipes/04-android-dedicated-mhs-multi-app.md \| Android Dedicated Multi-App Kiosk: Managed Home Screen Provisioning \| Guide \| Approved \|` | `docs/_registry/RE-index.md`, inside the table, after RE-224 |
| filename-map entry RE-224 | maps `docs/recipes/03-windows-11-multi-app-kiosk.md` to its `.docx` stem | `scripts/pipeline/filename-map.md` (generated) |
| filename-map entry RE-225 | maps `docs/recipes/04-android-dedicated-mhs-multi-app.md` to its `.docx` stem | `scripts/pipeline/filename-map.md` (generated) |
| `.docx` output stem RE-224 | `windows-11-multi-app-kiosk-assigned-access-provisioning.docx` | derived by `build-filename-map.mjs` slug from the registry Title |
| `.docx` output stem RE-225 | `android-dedicated-multi-app-kiosk-managed-home-screen-provisioning.docx` | derived by `build-filename-map.mjs` slug from the registry Title |
| Canary 1 | row count 223 -> 225 at 3 sites | `scripts/pipeline/build-filename-map.mjs` :274, :281, :282 |
| Canary 2 | row count 221 -> 225 at 4 literal sites | `scripts/pipeline/build-publish-bundle.mjs` :7, :515, :516, :519 |

Produced by plan 137-02 (listed for phase completeness): the two `docs/index.md` table rows, the replaced line-38 quick-nav bullet, and the "Needle-spec handoff" section handed to Phase 138.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (docs + build-time constants) | The change surface is Markdown metadata/registry edits, a deterministic regeneration of a committed build artifact, and two integer literals inside build-time Node self-test harnesses. No network, auth, session, user-input, or runtime execution path is introduced. Follows the `132-01-PLAN.md:124` precedent for this same job. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-137-01 | Tampering | `RE-index.md` registry integrity — malformed, duplicated, or misplaced RE-NNN row | medium | mitigate | Row-count assertion (225), per-ID `grep -c` == 1, and an awk line-order assertion that both rows precede `## Review Notes`; `build-filename-map.mjs` is fail-closed on Title collisions and both new stems were pre-verified collision-free |
| T-137-02 | Tampering | Pipeline drift canaries — a bump reverted, half-applied, or applied to only one of the two scripts | high | mitigate | All 6+1 literal sites named explicitly by line; both `--self-test` runs asserted green (`8 passed, 0 failed` and `15 passed, 0 failed`) in the same commit as the regeneration; `git show --stat` asserts the four-file atom |
| T-137-03 | Tampering | `filename-map.md` hand-edited instead of regenerated, desyncing it from the registry | medium | mitigate | Task action forbids hand-editing; the two exact expected `.docx` stems are asserted by `grep`, which only the generator produces from the registry Titles |
| T-137-04 | Tampering | Recipe body prose modified under cover of the metadata flip | medium | mitigate | `git diff --numstat` asserts exactly `2	2` line churn per recipe file in Commit A |
| T-137-05 | Spoofing | Published `.docx` filename (Copilot citation title) diverging from the recipe's own H1 | low | mitigate | Titles copied verbatim from D-09, which inherits them from the recipes' H1 lines; the identity chain index-link-text == registry Title == slug source is asserted end-to-end |
| T-137-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs in this phase; every tool is pre-existing and unmodified. No supply-chain surface. |
</threat_model>

<verification>
- pandoc + guard-docx pre-flight: both recipes `3 PASS, 0 FAIL, 0 SKIPPED`, evidence quoted in SUMMARY, recorded before Commit A
- Both recipes: `status: Approved` at :3 and `Status:** Approved` at :13; `doc_type: Guide` unchanged; `2	2` numstat per file
- `grep -cE '^\| RE-[0-9]+ \|' docs/_registry/RE-index.md` == 225, both new rows above `## Review Notes`
- `scripts/pipeline/filename-map.md` carries both exact expected `.docx` stems
- `node scripts/pipeline/build-filename-map.mjs --self-test` == `8 passed, 0 failed`
- `node scripts/pipeline/build-publish-bundle.mjs --self-test` == `15 passed, 0 failed` (was `14 passed, 1 failed`)
- Commit B is a single four-file atom (registry + map + both canary scripts)
- `node scripts/validation/c17-eee-contract.mjs` exits 0 at `234 files checked, 0 with violations` after EVERY commit
- CLASS-05, ROADMAP SC2 name both canaries; `zero pipeline code changes expected` no longer appears in STATE.md
- 17 predecessor leaf validators exit 0; check-phase-61 still exactly `4 FAIL` (pre-existing, unrelated)
- Commit order in `git log`: flip commit strictly precedes the registry/regen commit
</verification>

<success_criteria>
CLASS-05 complete: both recipes Approved, registered at 225 rows, in the regenerated publish set, with BOTH drift canaries bumped in the same commit as the regeneration and both `--self-test` harnesses green. ROADMAP SC1 and SC2 satisfied; SC5's C17 arm green after every commit. The three planning-doc claims that concealed the second canary are corrected.
</success_criteria>

<output>
Create `.planning/phases/137-integration-navigation-last-close/137-01-SUMMARY.md` when done.

It MUST carry: (a) verbatim stdout of all four pre-flight commands; (b) the before/after `--self-test` output of BOTH harnesses, so the RED-to-green delta is legible as intentional rather than as a regression; (c) the three commit SHAs with their author timestamps, labelled Commit A and Commit B (Task 4's commit is a third, non-binding commit); (d) if the D-24 fallback branch was taken, the NOT-RUN record plus a `v1.19-DEFERRED-CLEANUP.md` candidate entry drafted verbatim as a flagged contribution.
</output>
