---
phase: 135-recipe-3-windows-11-multi-app-kiosk
plan: 02
type: execute
wave: 2
depends_on: ["135-01"]
files_modified:
  - docs/recipes/03-windows-11-multi-app-kiosk.md
autonomous: false
requirements: [KIOSK-01, KIOSK-02, KIOSK-03, KIOSK-04, KIOSK-05]
must_haves:
  truths:
    - "SC1 — the recipe walks a linear happy path: self-deploying Autopilot enrollment, kiosk account model, apps pre-installed device-context Required, AssignedAccessConfiguration XML authored, pushed via an Intune Custom profile (OMA-URI) to ./Vendor/MSFT/AssignedAccess/Configuration, then verification (KIOSK-01, D4.0)"
    - "SC1 — the single-app case is a ONE-LINE cross-link to docs/recipes/01-shared-windows-avd-client.md#step-5a-kiosk-configuration and recipe 01 receives ZERO edits (check-phase-130.mjs:64,67 pins its Step 5a/5b headings as literal strings against live HEAD inside every apex chain)"
    - "SC2 — the worked XML ships as ONE column-0 xml fence carrying the complete assembled payload PLUS a field-decomposition table (D1.1 BOTH surfaces, D1.2 column 0, D1.3 one fence); the fence's element order follows the XSD profile_t sequence with Taskbar after v5:StartPins (D1.12)"
    - "SC2 — the field set is bounded to AllAppsList/AllowedApps, Taskbar/ShowTaskbar and a minimal v5:StartPins with the Windows 11 22H2 floor stated; BreakoutSequence, managed folders and v5:TaskbarLayout are excluded on correctness grounds (KIOSK-02)"
    - "SC2 — the namespace/version-floor table is EXACTLY D1.6's three rows labelled by namespace YEAR (2017 root / 2021 v4 / 2022 v5), notes the published table's own v5 double-binding defect, and asserts NO mismatch failure mode (genuinely undocumented — KIOSK-02)"
    - "T-7 — the 2017 namespace is the unprefixed root xmlns; no element anywhere carries a default: prefix (default is an XSD-internal XPath prefix, never writable in an instance document)"
    - "T-8 / research Concern #10 — the payload uses the XSD element spelling AllAppsList; the one-letter-shorter profile-type spelling never appears inside angle brackets, because a reader who writes it ships an unparseable document. One explicit caution line distinguishes the two registers"
    - "T-1 / D1.9 — backslashes are doubled ONLY inside the v5:StartPins JSON string and SINGLE in every XML attribute including App/@DesktopAppPath; both forms are present in the one payload and the rule is stated explicitly"
    - "D1.10 — the JSON uses the sample casing packagedAppId, with one caution line naming the table-vs-sample divergence; D1.11/Concern #9 — the applyOnce JSON key is OMITTED with the one caution line saying so"
    - "SC3 — the kiosk account-model decision block is STD-05 Case 1 with the four-column Option / When to choose / Consequence if wrong / Branch header, the autologon arm's Branch cell routing to a new anti-feature row rather than a worked branch (D4.2, D4.3/B-2)"
    - "SC3 — the anti-feature table covers interactive CA/MFA hard-break, the Windows-11-wrong-GUI trap, group Configs requiring the restricted-user-experience profile, nested UserGroup, hardcoded AUMIDs, Configuration silently superseding KioskModeApp while still returning SUCCESS, SharedPC layering, and AssignedAccess/Status as not-deliverable (never as impossible) — plus D4.3's autologon-tradeoff row and D4.5's app-provisioning-timing row (KIOSK-03)"
    - "SC4 — a ## Rollback/Recovery H2 exists BETWEEN ## Verification and ## Configuration-Caused Failures as a named documented divergence from recipe-template.md, bounded to first-party facts, shaped as bold pseudo-headings plus plain bullets with ZERO blockquotes (KIOSK-04, D2.1-D2.5)"
    - "SC5 — Verification retains the clean AssignedAccess > Operational event log line VERBATIM (D3.1 AUGMENT-NOT-SUBSTITUTE; a substitution would fail SC5 as written, in-phase, and cannot be repaired after the close-gate flips SC5 to Validated)"
    - "SC5 — Verification ADDS the enable-the-Operational-channel-before-the-first-kiosk-sign-in precondition with the one-time-events caveat (D3.1(2)/T-3) and ADDS absence of Microsoft-Windows-AssignedAccess/Admin Event ID 31000 as a further secondary line (D3.1(3))"
    - "SC5 — the observable checks are admin-executable end-to-end: restricted Start menu with only allow-listed apps, a non-allow-listed app failing to launch, and a representative allow-listed app's secondary flow completing without an app-blocked error; the D3.2 seam splits admin-at-console-before-first-sign-in from during-and-after-first-sign-in (KIOSK-05)"
    - "SC5 / T-4 — AppNotFound is DELETED from ## Verification and ships ONLY as a named prerequisite symptom under the apps-must-be-pre-installed prerequisite (D3.4/D3.5)"
    - "D5.5 as resolved by research §3.D — the user-account-control and console-only/no-remote-desktop requirements are NOT stated, because both self-scope to the kiosk experience in their own opening clause while RE-224 is the restricted user experience; the recipe is silent, not reassuring"
    - "D5.4 — zero inline external URLs in the recipe body; first-party citations live in this plan's SUMMARY/VERIFICATION artifact (both shipped recipes carry zero inline externals)"
    - "The full H2 set is exactly Summary, Prerequisites, Unsupported and Anti-Feature Callouts, Steps, Verification, Rollback/Recovery, Configuration-Caused Failures, See Also — the Phase-129 D-06 skeleton plus the one named KIOSK-04 divergence, no other H2"
    - "C17 exits 0 at 233 files / 0 violations with C17 #9 and #12 LIVE (the sentinel was stripped in Plan 135-01); check-phase-114/120/129/130 exit 0; check-nav-hub-links reports 0 outbound / 0 inbound failures; build-filename-map --self-test still asserts 223 registry rows"
    - "HARN-16 de-risked in-phase: convert.ps1 produces a .docx from the new recipe and guard-docx.mjs exits 0 on it, with pandoc confirmed on PATH first so the check is not a vacuous pass"
  artifacts:
    - path: "docs/recipes/03-windows-11-multi-app-kiosk.md"
      provides: "RE-224 — the Windows 11 multi-app kiosk recipe, third live instantiation of the Phase-129 Device Recipe doc class, delivering ROADMAP SC1-SC5"
      contains: "## Rollback/Recovery"
      min_lines: 170
  key_links:
    - from: "docs/recipes/03-windows-11-multi-app-kiosk.md autologon anti-feature row (Do this instead)"
      to: "docs/recipes/01-shared-windows-avd-client.md#step-5a-kiosk-configuration"
      via: "one-line outbound cross-link FROM recipe 03 only, explicitly framed as the single-app case; zero edits to recipe 01 (D4.3/B-2, SC1)"
      pattern: "01-shared-windows-avd-client.md#step-5a-kiosk-configuration"
    - from: "docs/recipes/03-windows-11-multi-app-kiosk.md Prerequisites and Steps"
      to: "docs/admin-setup-apv1/08-self-deploying.md, 03-esp-policy.md, 04-dynamic-groups.md"
      via: "direct relative links to admin-setup-apv1/* — never chained through recipe 01 (ARCHITECTURE.md:181 Anti-Pattern 8, D4.6)"
      pattern: "../admin-setup-apv1/08-self-deploying.md"
    - from: "the single column-0 xml fence's Profile/@Id and Configs/Config/DefaultProfile/@Id"
      to: "the XSD guid_t braced-GUID pattern"
      via: "one identical braced GUID in both places — the braces are part of the required pattern, so an unbraced GUID is silently schema-invalid (research §1.A, P5)"
      pattern: "DefaultProfile Id="
    - from: "the Case-1 decision-table's Branch cell for the autologon arm"
      to: "the new ## Unsupported and Anti-Feature Callouts row"
      via: "in-doc #fragment anchor resolving against the file's own GitHub-slugified headings"
      pattern: "#unsupported-and-anti-feature-callouts"
---

<objective>
Author the body of `docs/recipes/03-windows-11-multi-app-kiosk.md` (RE-224) — the third live instantiation of the Phase-129 Device Recipe doc class — walking an Intune admin from zero to a working, verified Windows 11 **restricted user experience** delivered through the AssignedAccess CSP `Configuration` node via an Intune custom OMA-URI profile. Delivers ROADMAP SC1-SC5 / KIOSK-01..05: the linear happy-path spine, the bounded schema-valid worked payload on both surfaces, the account-model decision block plus the anti-feature table, the named `## Rollback/Recovery` divergence, and admin-executable verification.

Purpose: this plan IMPLEMENTS locked decisions — it does not re-open them. `135-CONTEXT.md` `<decisions>` are binding, resolved through `/grill-me` (33 questions) plus **two full `/adversarial-review` rounds**, and round 2 CORRECTED four of round 1's own rulings. Its eight numbered anti-regression traps (T-1..T-8) overturn earlier drafts AND locked requirement text; several rulings are marked "SUPERSEDED" with the corrected version inline. **Never implement a superseded version.** `135-RESEARCH.md` supplies the worked payload (§1.A), the 22-row decomposition table (§1.B), the namespace verification (§1.C), the OMA-URI click-path (§2) and a verbatim first-party quote for every deferred item (§3) — authoring is assembly-within-locks, not new research. Its `## Concerns` (11 items, 2 HIGH) is a pre-authoring checklist: each item is a shipped-documentation defect if it passes through unqualified.

Output: one complete recipe file, C17-green at 233/0 with `#9`/`#12` live, link-checker clean, and pandoc/guard-docx clean. `status: Draft` stays Draft — Phase 137 flips it. Zero registry edits, zero navigation edits, zero validator edits, zero edits to recipe 01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-CONTEXT.md
@.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-PATTERNS.md
@docs/_templates/recipe-template.md
@docs/recipes/03-windows-11-multi-app-kiosk.md

Note: `135-RESEARCH.md` (1183 lines) and `EEE-SOP-standard.md` are deliberately NOT @-referenced —
each task's `<read_first>` names the exact line ranges, so no task loads more than it needs.
`docs/recipes/01-shared-windows-avd-client.md` is a READ-ONLY style reference: zero edits, ever.
</context>

<verified_at_plan_time>
Read from live HEAD on 2026-07-30, not copied from research:

- C17 baseline `232 files checked, 0 with violations`; after Plan 135-01 creates RE-224 the expected count is **233**.
- `check-phase-114`, `-120`, `-129`, `-130` all exit 0; `check-nav-hub-links` reports 0 outbound / 0 inbound failures; `build-filename-map.mjs --self-test` 8 passed / 0 failed at **223** registry rows.
- `pandoc --version` reports **3.7.0.2** — the `convert.ps1` pin — so the §6.D guard-docx run is a real check and not the `guard-docx.mjs:234-236` vacuous pass.
- C17 assertions `#2` (exactly one H1), `#4` (`## Summary` present and first H2) and `#5` (Summary at least 30 words) fire **unconditionally on every enrolled file** — read from `c17-eee-contract.mjs:214-246`. Plan 135-01 therefore ships the H1 and Summary with the identity; this plan appends from the Scope banner down and must not add a second H1 or displace `## Summary` as the first H2.
- The slug, H1 and frontmatter are RULED in Plan 135-01 (`docs/recipes/03-windows-11-multi-app-kiosk.md`; `# Windows 11 Multi-App Kiosk: Assigned Access Provisioning`; `doc_id: RE-224`, `status: Draft`, `last_verified: 2026-07-30`, `review_by: 2026-10-28`). **Inherit them. Do not re-derive or change them.**
</verified_at_plan_time>

<structural_gates>
Two automated structural gates run inside the tasks below. Both are read-only `node -e` checks — no writes, no network, no new dependency, and neither touches any validator.

**Gate A — payload integrity (Task 2).** `c17-eee-contract.mjs` has no XML logic and `check-nav-hub-links.mjs` never scans this file, so nothing in the existing harness can catch a silently schema-invalid payload. Gate A extracts the single fence and asserts: exactly one closed column-0 xml fence (D1.2/D1.3); the required token set is present; the banned token set is absent, including the T-8 element-spelling trap and the T-7 `default:` prefix trap; the XSD `profile_t` sequence order AllAppsList before v5:StartPins before Taskbar (D1.12); `Profile/@Id` and `DefaultProfile/@Id` are ONE identical braced `guid_t` GUID (the braces are part of the required pattern — P5); the JSON uses the sample casing (D1.10); the JSON carries doubled backslashes while every `App/@DesktopAppPath` carries single ones (T-1/D1.9, and P1 is the inverse that genuinely breaks).

**Gate B — H2 skeleton and section-scoped rules (Task 3).** Asserts the exact 8-H2 set and order in one shot — which is simultaneously the Phase-129 D-06 skeleton check, the D2.4 `## Rollback/Recovery` placement check, and a no-extra-H2 check. Then, **section-scoped** (never a whole-file negative grep, because both strings are legitimate elsewhere in the file): `AppNotFound` must be absent from `## Verification` and present somewhere in the file (T-4/D3.4 vs D3.5); `## Rollback/Recovery` must contain zero top-level blockquotes (D2.5); `## Verification` must contain the SC5 Operational line and Event ID 31000 (D3.1).
</structural_gates>

<tasks>

<task type="auto">
  <name>Task 1: Scope banner, Prerequisites, and the ## Unsupported and Anti-Feature Callouts table</name>
  <files>docs/recipes/03-windows-11-multi-app-kiosk.md</files>
  <read_first>
    - `docs/recipes/03-windows-11-multi-app-kiosk.md` — the shell Plan 135-01 created (frontmatter, EEE block, H1, `## Summary`). Read it first; append from the Scope banner down and change nothing above it.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-PATTERNS.md` lines **76-105** (the `> **Scope:**` banner form with recipe 01's 175-char measurement and the split idiom) and lines **86-96** (the anti-feature table's exact header row and a real shipped row) and lines **98-105** (the Prerequisites bullet shape, opening with the permissive `**This recipe is NOT:**` bullet).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-CONTEXT.md` **Area 5** (D5.1-D5.5, lines 118-128, including all four inline research amendments), **D6.2** (lines 136-139, the Scope-banner taxonomy sentence scoped to RE-224 ONLY — do not bind RE-225), **D6.3** (no self-narration of precedent in the shipped body), **D4.3** including its research-Concern-#11 amendment (lines 106-107), and **D4.5** including its research-Concern-#2 amendment (lines 111-113).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **451-471** (§3.B — the VM / Hyper-V vTPM `0x800705B4` limitation verbatim, the confirmed zero-match grep proving it is not anchor-owned, the TPM-2.0-plus-attestation requirement, and the Ethernet-vs-Wi-Fi precision fix), lines **520-553** (§3.D — the user-account-control and console-only ruling in the NEGATIVE, the verbatim edition list, and the Shell-Launcher-is-not-Pro fact), lines **576-597** (§3.F — a verbatim first-party quote for every anti-feature row, plus two clean bonus rows), lines **599-614** (§3.G — the two correctly-scoped app-provisioning quotes and the dependency rule), lines **616-638** (§3.H — the account-model scoping re-verified and the autologon row's content), and **`## Concerns` lines 1027-1053** in full.
    - `docs/recipes/01-shared-windows-avd-client.md` lines 1-50 — READ-ONLY style reference for the banner, Prerequisites bullets and anti-feature table as actually shipped. **Zero edits to this file.**
  </read_first>
  <action>
    Append three sections to `docs/recipes/03-windows-11-multi-app-kiosk.md`, immediately after `## Summary`.

    **1. The `> **Scope:**` banner.** One top-level blockquote naming the recipe's own kiosk/dedicated-device taxonomy sentence (D6.2, scoped to RE-224's banner only — gray area #8's corpus-wide question is chartered to Phase 136 by `ROADMAP.md:89`; do not bind RE-225 and do not duplicate the sentence into RE-225's banner). Measure the contiguous top-level blockquote run's character count **now**, after Plan 135-01's sentinel strip, because the sentinel would have made C17 skip `#12`. Recipe 01's own banner is 175 chars against the 200-char cap, so overflow is near-certain: if it exceeds 200, split into two blank-line-separated sub-200-char runs — the `01:101`/`01:103` idiom. Do not narrate the first-lander precedent in the body (D6.3); that lives in CONTEXT and the SUMMARY.

    **2. `## Prerequisites`** — plain bullets, no verify column (D5.5). Open with the permissive `**This recipe is NOT:**` bullet (Phase-130 H-LOCK-5) absorbing: not the single-app kiosk, not Shell Launcher, not SharedPC, not Entra "Shared device mode". Then RBAC and licensing owned inline (recipe 01's bullets 2-3 shape), then linked prior-configuration bullets. Link `../admin-setup-apv1/08-self-deploying.md`, `../admin-setup-apv1/03-esp-policy.md` and `../admin-setup-apv1/04-dynamic-groups.md` **directly** — never chain a prerequisite through recipe 01 (`ARCHITECTURE.md:181` Anti-Pattern 8, D4.6). Content rules, each already amended:
    - **Edition floors (D5.1/D5.2):** the four families with LTSC spelled out — Pro, Enterprise / Enterprise LTSC, Education, IoT Enterprise / IoT Enterprise LTSC — plus the mandated clause that the floors are identical for both single-app and multi-app Assigned Access. Per research Concern #7 that clause is a correct **characterization** of one undifferentiated published list, not a verbatim first-party sentence: ship it, but never inside quotation marks. Restore the C-2e Pro Education footnote verbatim as instructed: assumed same as Pro, unconfirmed by name.
    - **22H2 (D5.3, T-6):** state that this recipe's worked Start layout uses `v5:StartPins`, which requires 22H2 or later. Then one caution line: whether a 21H2 device can obtain an equivalent multi-app Start layout via the 2017 or v4 namespace is **not documented** — do not assume dropping `StartPins` buys 21H2 support. Do NOT assert that 22H2 is categorical for any Windows 11 multi-app kiosk; that would override a HIGH-rated locked fact by inference-from-silence.
    - **TPM 2.0 with attestation** and the **VM / Hyper-V vTPM `0x800705B4`** limitation (A-7): the grep in research §3.B confirms zero matches for the VM caveat in both `08-self-deploying.md` and recipe 01, so it is not anchor-owned and stating it in-recipe is delta-legal. Ship it as one Prerequisites bullet — it is the single most likely reason a reader's first attempt fails, and it fails at the enrollment stage before any kiosk configuration is in play.
    - **Ethernet at OOBE (research Concern #8):** frame it as the **zero-touch precondition**, not a hard requirement — Ethernet means no user interaction is required, while Wi-Fi works with the user selecting language/locale/keyboard and making a network connection. This matches Phase 130's D-LOCK-8 framing.
    - **`AppNotFound` (T-4/D3.5):** name it here, as a symptom of the apps-must-be-pre-installed prerequisite. It is a `Status`-node status code, not an event-log entry, and `Status` is the node this milestone already ruled unreadable — so it must never appear as a verification check.
    - **RULED explicitly per D5.5, in the negative (research §3.D):** do NOT state the user-account-control requirement and do NOT state the console-only / no-remote-desktop requirement. Both self-scope in their own opening clause to the kiosk experience, which the source page defines as the single-app case, while RE-224 is the restricted user experience. Nothing establishes the opposite either, so the recipe is silent rather than reassuring. Record the ruling and its two-sided NOT-FOUND in the SUMMARY.
    - Autologon breakers are stated **only** in the autologon anti-feature row, not here.

    **3. `## Unsupported and Anti-Feature Callouts`** — the frozen three-column header `| Feature | Why it's unsupported / what breaks | Do this instead |`. Every row's "Why" cell is one scannable clause backed by research §3.F's verbatim quote for that row. Rows:
    - Interactive Conditional Access / MFA hard-breaks Assigned Access sign-in **by design** — label it **Entra account/group only** and as interactive CA (MFA **or** Terms of Use), because `AADSTS50158` is an external-security-challenge failure, i.e. Terms of Use, not MFA. Disclose the remedy's blast radius (D3.6): with a `UserGroup` `Config`, excluding the kiosk users from interaction-requiring CA policies is a per-member identity-security change for **every** member of the kiosk group, not a per-device setting.
    - The Intune Templates GUI is the silently-wrong platform on Windows 11 — name the concrete click-path from research §2.A so the trap is recognisable, since the option is reachable on a Windows 11 target while being documented Windows-10-only.
    - Group `Configs` can only reference a restricted-user-experience profile, never a kiosk profile.
    - Nested `UserGroup` is unsupported.
    - Hardcoded AUMIDs break on UWP app update. Note that this quote is kiosk-experience-scoped in its source list; the mechanism is general, so state it without implying the source scoped it to a restricted user experience. Route to AUMID discovery via `Get-StartApps` on a reference device rather than a guessed value.
    - `Configuration` silently supersedes the legacy `KioskModeApp` node **while still returning SUCCESS** to the MDM server.
    - SharedPC layering — NOT-FOUND in both directions on every page fetched; treat as mutually exclusive and say so without asserting a documented failure.
    - `AssignedAccess/Status` health monitoring — **not deliverable** as a custom OMA-URI row (`Status` is `Get`-only and Intune requires Add, Replace and Get). Write "not deliverable", never "impossible".
    - **The autologon-arm row (D4.3/B-2)** — this row is the Case-1 decision block's Branch-cell destination, so it must exist before Task 2 links to it. Its "Why" cell states the autologon-vs-Entra tradeoff in one line, using research Concern #11's first-party content, which discharges that concern: the local-autologon arm's advantage is blast-radius containment on a public-facing device, since an Active Directory or Entra user might allow an attacker to reach domain resources; the Entra-group arm's advantage is that authenticated apps work at all. Its "Do this instead" cell carries the one-line cross-link to `../recipes/01-shared-windows-avd-client.md#step-5a-kiosk-configuration` **explicitly framed as the single-app case** — routing an account-model choice there without that framing is a category error. The corpus-native precedent for stating a non-worked mode in place with its reason is `02-shared-ipad-full-provisioning.md:194`.
    - **The app-provisioning-timing row (D4.5)** — pins for apps not installed for the signing-in user simply do not appear, with no error surfaced. Use research §3.G's two correctly-scoped quotes. Per research Concern #2, do NOT use the must-be-provisioned-or-installed sentence as the support: it is kiosk-experience-scoped and singular single-app language.
    - Optionally, if the table has budget: new UWP installs are not blocked until the next sign-in, and the generated AppLocker rules cannot be managed in MMC snap-ins. Both are first-party and genuinely surprising (research §3.F).

    Blank-line-chunk every multi-clause blockquote so each contiguous top-level run stays under 200 chars (C17 `#12`, now live). Add no inline external URL anywhere (D5.4) — first-party citations go in this plan's SUMMARY. Author no Mermaid and no code fence in this task; the single payload fence is Task 2's.
  </action>
  <acceptance_criteria>
    - `grep -c '^## Prerequisites'` returns 1 and `grep -c '^## Unsupported and Anti-Feature Callouts'` returns 1.
    - The anti-feature table's frozen header is present: `grep -c "| Feature | Why it's unsupported / what breaks | Do this instead |"` returns 1.
    - `grep -c '\*\*This recipe is NOT:\*\*'` returns 1, and that bullet names Shell Launcher, SharedPC and Shared device mode.
    - Prerequisites link `admin-setup-apv1/*` directly: `grep -c '\.\./admin-setup-apv1/08-self-deploying\.md'`, `'\.\./admin-setup-apv1/03-esp-policy\.md'` and `'\.\./admin-setup-apv1/04-dynamic-groups\.md'` each return at least 1.
    - `grep -c '06-user-driven'` returns **0** — D4.0 rules self-deploying and marks that overdue anchor AVOID.
    - `grep -c '0x800705B4'` returns at least 1 and `grep -ci 'Hyper-V'` returns at least 1 — the A-7 VM/vTPM Prerequisites bullet exists.
    - `grep -c 'IoT Enterprise LTSC'` returns at least 1 and `grep -ci 'Pro Education'` returns at least 1 — the four LTSC-spelled families and the C-2e footnote both landed.
    - `grep -cE '"identical for both'` returns **0** — the mandated clause ships as plain prose, never presented as a Microsoft quotation (research Concern #7).
    - `grep -c '22H2'` returns at least 1; `grep -c '21H2'` returns at least 1 — the floor is stated and the 21H2-equivalence caution exists.
    - `grep -ciE 'User Account Control|\bUAC\b'` returns **0** and `grep -ciE 'not supported over a remote desktop|must sign in from the console'` returns **0** — D5.5 as resolved in research §3.D rules both OUT.
    - `grep -c 'AppNotFound'` returns at least 1 (named in Prerequisites; Task 3's Gate B proves it is absent from `## Verification`).
    - Every mandated anti-feature row is present by its distinguishing token: `AADSTS50158` and `AADSTS50076` each at least 1; `Multi app kiosk` at least 1; `KioskModeApp` at least 1; `AUMID` at least 1; `AssignedAccess/Status` at least 1; and a case-insensitive `nested` at least 1.
    - `grep -c '01-shared-windows-avd-client.md#step-5a-kiosk-configuration'` returns at least 1 and that link's surrounding cell text contains `single-app`.
    - `grep -ciE 'learn\.microsoft\.com|docs\.microsoft\.com'` returns **0** — zero inline external citations in the body (D5.4).
    - `grep -c '^```'` returns **0** — no fence yet.
    - `grep -c '^# '` returns 1 and `grep -c '^## Summary'` returns 1 — Plan 135-01's H1 and Summary are untouched and `## Summary` is still the first H2.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with `233 files checked, 0 with violations` — including `#12` on every blockquote authored here, now that the sentinel is gone.
    - `git status --porcelain docs/recipes/01-shared-windows-avd-client.md docs/index.md docs/_registry/RE-index.md scripts/validation/c17-eee-contract.mjs` returns empty, and `node scripts/validation/check-phase-130.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && f=docs/recipes/03-windows-11-multi-app-kiosk.md && grep -q '^## Prerequisites' "$f" && grep -q '^## Unsupported and Anti-Feature Callouts' "$f" && grep -qF "| Feature | Why it's unsupported / what breaks | Do this instead |" "$f" && grep -q '\*\*This recipe is NOT:\*\*' "$f" && grep -q '\.\./admin-setup-apv1/08-self-deploying\.md' "$f" && grep -q '\.\./admin-setup-apv1/03-esp-policy\.md' "$f" && grep -q '\.\./admin-setup-apv1/04-dynamic-groups\.md' "$f" && [ "$(grep -c '06-user-driven' "$f")" = "0" ] && grep -q '0x800705B4' "$f" && grep -qi 'Hyper-V' "$f" && grep -q 'IoT Enterprise LTSC' "$f" && grep -qi 'Pro Education' "$f" && [ "$(grep -cE '"identical for both' "$f")" = "0" ] && grep -q '22H2' "$f" && grep -q '21H2' "$f" && [ "$(grep -ciE 'User Account Control|\bUAC\b' "$f")" = "0" ] && [ "$(grep -ciE 'not supported over a remote desktop|must sign in from the console' "$f")" = "0" ] && grep -q 'AppNotFound' "$f" && grep -q 'AADSTS50158' "$f" && grep -q 'AADSTS50076' "$f" && grep -q 'Multi app kiosk' "$f" && grep -q 'KioskModeApp' "$f" && grep -q 'AUMID' "$f" && grep -q 'AssignedAccess/Status' "$f" && grep -qi 'nested' "$f" && grep -q '01-shared-windows-avd-client.md#step-5a-kiosk-configuration' "$f" && [ "$(grep -ciE 'learn\.microsoft\.com|docs\.microsoft\.com' "$f")" = "0" ] && [ "$(grep -c '^```' "$f")" = "0" ] && [ "$(grep -c '^# ' "$f")" = "1" ] && [ "$(grep -c '^## Summary' "$f")" = "1" ] && node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -q '233 files checked, 0 with violations' && [ -z "$(git status --porcelain docs/recipes/01-shared-windows-avd-client.md docs/index.md docs/_registry/RE-index.md scripts/validation/c17-eee-contract.mjs)" ] && node scripts/validation/check-phase-130.mjs >/dev/null 2>&1 && echo PASS || echo CHECK</automated>
  </verify>
  <done>Scope banner (measured post-sentinel-strip, split if over 200 chars), Prerequisites (the permissive NOT bullet, inline RBAC/licensing, direct admin-setup-apv1 links, four LTSC-spelled edition families plus the Pro Education footnote, the 22H2 floor with the 21H2 caution, TPM-2.0-with-attestation, the VM/vTPM 0x800705B4 bullet, Ethernet framed as the zero-touch precondition, AppNotFound as a prerequisite symptom, and the user-account-control / console-only requirements explicitly NOT stated), and the anti-feature table with every mandated row plus D4.3's autologon-tradeoff row carrying the single-app cross-link and D4.5's timing row. Zero inline externals, zero fences, C17 233/0 with #12 live, recipe 01 untouched.</done>
</task>

<task type="auto" >
  <name>Task 2: ## Steps — the happy-path spine, the account-model Case-1 block, the two-scope step, the single column-0 payload fence with its decomposition and namespace tables, and the OMA-URI delivery step</name>
  <files>docs/recipes/03-windows-11-multi-app-kiosk.md</files>
  <reversibility rating="costly">D1.1 (both surfaces: one fence PLUS a field-decomposition table) and D4.1 (the Entra named/grouped Config account model) are both rated costly in CONTEXT.md. Reversing D1.1 after publish means re-authoring the whole Steps section and re-running the publish bundle, and it sets the corpus convention Phase 136 inherits; reversing D4.1 changes the anti-feature table, the verification lines and the recovery differential. Both are LOCKED — implement, do not re-litigate.</reversibility>
  <read_first>
    - `docs/recipes/03-windows-11-multi-app-kiosk.md` — everything Task 1 authored. Read it to align the Case-1 Branch cell's in-doc anchor with the actual `## Unsupported and Anti-Feature Callouts` heading slug.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **87-253** — §1.A in full: the complete copy-pasteable payload to lift verbatim into the fence, the placeholder lead-in naming exactly the three values to replace, the XSD `profile_t` sequence confirmation, the braced-GUID pattern requirement, the two-namespace justification, the four-way backslash confirmation table, the `pinnedList` casing counts, the `applyOnce` omit ruling with its one caution line, and the honest position on XSD validation (do not promise it; ship the well-formedness check plus device-side schema acceptance instead).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **255-286** (§1.B — the 22-row field-decomposition table to lift, plus the prose summary line to place within 5 lines of it) and lines **288-308** (§1.C — the published version table verbatim, the confirmed `v5` double-binding defect, the deliberate label-by-year divergence, and the confirmed absence of any documented mismatch failure mode).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **312-401** — §2 in full: GATE 1 re-cited fresh, the mechanism (Setting and Value) verbatim, the Data-type inference with its honest MEDIUM label, the 11-step click-path, the OMA-URI row fields including the case-sensitivity note, the device-group assignment resolution, and the drift-prone nav segment plus the obscured-stored-value gotcha.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **599-668** (§3.G app provisioning and the first-sign-in timing hazard, §3.H the account-model scoping and the autologon element, §3.I the two-scope distinction with the exact C-2c / C-2b scoping, §3.J the self-deploying and interactive-Entra-sign-in compatibility) and lines **1027-1053** (`## Concerns`).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-CONTEXT.md` **all of Area 1** (D1.1-D1.12, lines 45-71 — note D1.6 and D1.7 and D1.9 are marked SUPERSEDED with the corrected version inline; T-1, T-7 and T-8 at lines 34, 43, 41), **Area 4** (D4.0-D4.6, lines 99-114, including the C-2b and C-2c amendments), and the `<specifics>` block (lines 269-280).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-PATTERNS.md` lines **107-122** (the `### Step N:` plus `> **What breaks if misconfigured:**` idiom in both shipped forms), lines **147-180** (the shipped STD-05 Case-1 block verbatim, the mandatory blank line before the table, and D4.2's routing-cross-link variant), and lines **184-219** (the payload-fence idiom: the placeholder lead-in, and the fence-immediately-after-a-numbered-click-list placement precedent).
    - `docs/_standards/EEE-SOP-standard.md` lines **455-545** — STD-05 as corrected by Plan 135-01. D-01's case definitions, D-02's mandatory blank line, D-03's normative rule, D-04's three rules. Do NOT copy the D-07 fenced spec sample's form into the live recipe.
    - `docs/admin-setup-apv1/09-intune-connector-ad.md` lines **78-102** and `docs/admin-setup-macos/10-kerberos-sso-extension.md` lines **85-159** — the two payload-fence precedents, placement and magnitude respectively. READ-ONLY.
  </read_first>
  <action>
    Append `## Steps` and its `### Step N:` sequence, in the ROADMAP SC1 order: enrollment, account model, apps, XML authored, pushed via OMA-URI, then verification (Task 3). Every step gets a `> **What breaks if misconfigured:**` callout, blank-line-chunked so each contiguous top-level run stays under 200 chars.

    - **Enrollment (D4.0):** self-deploying, cross-linking `../admin-setup-apv1/08-self-deploying.md` directly. Device-phase-only ESP, cross-linking `../admin-setup-apv1/03-esp-policy.md` **only** (D4.6). Dynamic device group via `../admin-setup-apv1/04-dynamic-groups.md`. Note the D4.0/D4.1 compatibility fact from research §3.J — remaining at the sign-on screen where any organization member signs in with Entra credentials is the **first** documented self-deploying outcome, and the combination already ships in this corpus.
    - **The account-model decision block (D4.2, KIOSK-03, SC3):** an STD-05 **Case 1** block. One-sentence `> **Ask the admin:**` lead-in blockquote, then a **mandatory blank line**, then the four-column table `| Option | When to choose | Consequence if wrong | Branch |`. Keep the consequence column — `FEATURES.md:114` rates this decision CRITICAL, and Case 2's three-column form drops it, which is why Case 2 is wrong here. The worked arm is the **Entra named/grouped `Config`** (D4.1): the source sentence sits in a list whose lead-in covers a kiosk **or a restricted user experience**, while the competing use-a-local-nonadministrator-account sentence is scoped to the kiosk profile, i.e. single-app. The **autologon arm's Branch cell is a routing cross-link, not a worked branch** — it points at Task 1's autologon anti-feature row via an in-doc `#fragment` anchor that must match the file's own GitHub-slugified heading exactly. Record D4.2's three grounds in the SUMMARY (Phase 135 is the first lander): D-01's fully-worked-downstream-path parenthetical is a case-**selection** criterion and not a constraint on arm content; `ROADMAP.md:82` locks the identical shape at SC level for Phase 136; and the fails-STD-05 objection is scoped to the SharedPC NOT-FOUND case, whereas the autologon arm CAN be authored.
    - **Apps (D4.5):** device-context, **Required**, assigned to the device group, pre-installed before any kiosk sign-in. State the first-sign-in timing hazard — on a self-deploying device the kiosk identity has never signed in, so provisioned packages register per-user only at that first sign-in and the first sign-in can legitimately show an empty or partial pin set **with no error surfaced**, which the verification lines would otherwise read as a genuine failure. Use research §3.G's two correctly-scoped quotes as the support; per research Concern #2 do NOT use the must-be-provisioned-or-installed sentence, which is kiosk-experience-scoped and singular. Also carry the dependency rule: if an app depends on another app, both must be in the allowed-apps list. Note that ESP tracks apps, not the custom OMA-URI profile, so config-vs-app ordering is not guaranteed by anything the recipe controls.
    - **The two-scope step (D4.4, Pitfall 8) — its own named step.** Intune *policy delivery* scope is the device group; *effective configuration* scope is the accounts or groups named inside the XML. A device can receive the policy successfully and still show no restricted experience. Its `What breaks if misconfigured` callout carries **only** the group-path fact — the kiosk device must have internet connectivity when users belonging to the group sign in (C-2c). The account-availability requirement governs individual `Account` entries and is scoped by its own next sentence to local accounts, so mention it **only** as the local-arm contrast, never as a precondition of the worked group path. Add C-2b's caution line: apply the restricted user experience to standard users only — associating an admin user with an Assigned Access profile is not supported — and note this hazard turns on **who is named inside the `Config`**, not on enrollment mode, so do NOT claim self-deploying structurally prevents it. Fill `UserGroup/@Name` with the Entra group's **object ID**, not its display name, and give the click-path for finding it.
    - **The XML-authoring step — the fence (D1.1/D1.2/D1.3/D1.7).** Placement is the SUPERSEDED-corrected D1.7: the fence sits at **column 0 immediately after the numbered click-list inside this Step**, with prose continuing after — NOT deferred to the end of `## Steps`. The earlier defer rule rested on a premise **T-2** empirically disproved (a column-0 fence inside a numbered list does NOT restart numbering — reproduced twice with the pinned pandoc: `-t native` yields an OrderedList starting at 3, and the built `.docx` carries the matching start and startOverride values) and would have inverted SC1's locked order. Any rule justified by "numbering restarts" is void. The precedent is `09-intune-connector-ad.md:84-96`. Ship **exactly one** fence, opened as a column-0 xml fence, carrying research §1.A's complete assembled payload verbatim. Column 0 is mandatory: it is the only position where C17's caret-anchored mask and `convert.ps1`'s leading-whitespace-tolerant mask agree; an indented fence would be unmasked by C17 and the payload's own lines would be read as content. Log that mask divergence to `v1.19-DEFERRED-CLEANUP.md` per `REQUIREMENTS.md:68`.
      The fence's **lead-in** must (a) name the exactly three values to replace, per research §1.A's placeholder lead-in and the `10-kerberos-sso-extension.md:92` idiom — **D1.4** rules concrete worked values throughout, with explicit placeholders ONLY for the LOB-app AUMID, the Entra group identifier and the profile GUID, so everything else in the fence is a working value a reader can paste unchanged — and (b) state the element-order fact (D1.12): `Taskbar` follows `v5:StartPins`, and both `Profile Id` and `DefaultProfile Id` carry the same braced GUID. State that the braces are part of the schema-required pattern, so an unbraced GUID is silently schema-invalid.
      Cautions to carry near the fence, each one line: the **backslash rule** (T-1/D1.9 — doubled ONLY inside the `v5:StartPins` JSON string because JSON escapes them, single in every XML attribute including `App/@DesktopAppPath`; the two forms sit twelve lines apart in this one payload, and doubling in an XML attribute is the inversion that genuinely breaks); the **`pinnedList` casing** divergence (D1.10 — every worked sample uses the lowercase-d form, Learn's key table uses the capital-D form, JSON keys are case-sensitive, so the payload uses the sample casing); the desktop-pin-by-AUMID key whose casing no sample confirms, so prefer the `.lnk`-path key; the **`applyOnce`** omission (D1.11/Concern #9 — it now requires 24H2 with a named KB and is ignored on earlier Windows 11, so at this recipe's own 22H2 floor it is a documented no-op; whether `v5:StartPins` honours it at all is NOT-FOUND, and an `Export-StartLayout` JSON from a 24H2 device may contain the key); and the **T-8 / Concern #10** caution distinguishing the XSD element spelling from the profile-**type** name Learn's prose uses — both are correct in their own register, but writing the profile-type spelling inside angle brackets ships an unparseable document.
      Do not promise XSD validation: the five schema documents import each other without resolvable locations and the add-ons need an XSD 1.1 processor. Ship instead the two genuinely admin-executable checks from research §1.A — a PowerShell well-formedness cast before pasting, and device-side schema acceptance surfacing as a profile-parse or apply error in the Operational channel.
      Do not author the excluded elements: the breakout-sequence element (schema-illegal in this profile branch — it exists only in the other branch of the XSD choice), managed folders, the taskbar-layout element, the classic-app-path attributes, the file-explorer-namespace-restrictions element, the Windows-10 start-layout element, or the auto-launch attribute. The first-party samples also declare two extra namespaces that are unused in the instance — do not copy them, because a bounded payload declaring namespaces it never uses invites the reader to think they are required.
    - **The field-decomposition table (D1.1 second surface, D1.9(3)):** lift research §1.B's 22 rows. Its lead-in must say the table is explicitly **not** a schema statement and repeat the `Taskbar`-after-`v5:StartPins` order fact. Row 6 uses the **element** spelling (Concern #10). The JSON key set ships as rows here while the raw payload ships in the fence — both, not either; Learn's own `start/layout` key table is the precedent for decomposing this identical JSON into a table. Place research §1.B's prose summary line within 5 lines of the table: C17 `#11` fires only above 25 data rows AND with no prose within 5 lines, and at 22 rows it cannot fire — but a genuine prose summary is the validator's own sanctioned remedy and good writing regardless (D1.5).
    - **The namespace / version-floor table (D1.6, SUPERSEDED row set):** **exactly** the three rows in CONTEXT.md D1.6's table — 2017 (root) / unprefixed default `xmlns` / Windows 10 1709+ and Windows 11 baseline; 2021 / `v4:` / Windows 11 21H2+; 2022 / `v5:` / Windows 11 22H2+. Do NOT apply any drop-the-Windows-10-rows arithmetic: that deletes the mandatory root namespace and leaves fewer than KIOSK-02's locked three rows. Label the first column by namespace **year**, not Windows build, and keep the "(root)" qualifier — that is the only way to state three rows without implying a Windows 11 admin can drop the 2017 row. Note the published table's own defect of binding the `v5` alias to two different namespaces. State that the Windows-10 start-layout element lives in the 2017 root namespace and not in v4. **Must NOT assert a mismatch failure mode** — it is genuinely undocumented (KIOSK-02, confirmed NOT-FOUND in research §1.C). Per T-7, state that the 2017 namespace is the unprefixed root written bare on every child element, and that the alias cell Learn labels `default` is an XSD-internal XPath prefix an instance document never writes.
    - **The OMA-URI delivery step (§2, KIOSK-01):** the Setting is the `./Vendor/MSFT/AssignedAccess/Configuration` node and the Value is the content of the XML configuration file. Ship the OMA-URI string as an **inline code span in a table row, never a fence** (D1.8). Give the 11-step click-path from research §2.D, writing the nav path once with a hedge phrase for the drift-prone segment. Note that the OMA-URI field is case-sensitive. **Data type = `String`**, tagged **MEDIUM confidence** — this is the one item in the delivery step that must not be presented as verified: no first-party sentence states which Intune Data type to select for this node, and the recommendation rests on the node's `chr` Format plus a direct first-party analogue for another XML-payload-valued node. Name the upload-a-file variant as the plausible wrong pick without claiming first-party support for the exclusion. Assign to a **device** group — the node is device-scope, so the AssignedAccess page's device instruction governs over the generic custom-profile page's users-or-groups wording, and that tension is D4.4's two-scope distinction in its most concrete form. Add a `What breaks if misconfigured` callout for the obscured-stored-value gotcha: after saving, an admin without the named role or permissions cannot read the XML back, so keep the authored XML in source control because the portal is not a reliable copy of it. Re-cite GATE 1 fresh (KIOSK-01): the Templates multi-app option is documented for Windows 10 devices, and Windows 11 routes to the AssignedAccess CSP article family — the gate is already discharged and this phase records the discharge without re-litigating it.

    Zero inline external URLs (D5.4). Zero edits to recipe 01. Author no Mermaid.
  </action>
  <acceptance_criteria>
    - `grep -c '^## Steps'` returns 1 and `grep -c '^### Step '` returns at least 5.
    - **Gate A passes** (see `<verify>`): exactly one closed column-0 xml fence; the required payload token set present; the banned token set absent — the profile-type spelling inside angle brackets, any `default:` prefix, the breakout-sequence element, the taskbar-layout element, the Windows-10 start-layout element, the `rs5` prefix and the classic-app-path attribute; XSD `profile_t` order AllAppsList before v5:StartPins before Taskbar; `Profile/@Id` and `DefaultProfile/@Id` are one identical braced GUID; the JSON uses the lowercase-d sample casing; the JSON carries doubled backslashes; at least one `App/@DesktopAppPath` exists and no `DesktopAppPath` contains a doubled backslash.
    - `grep -c '^```'` returns exactly **2** — one opening ```` ```xml ```` and one closing fence, both at column 0 (D1.2/D1.3).
    - `grep -c '^```xml'` returns exactly **1**.
    - Both namespace URIs are present verbatim: `grep -cF 'xmlns="http://schemas.microsoft.com/AssignedAccess/2017/config"'` returns 1 and `grep -cF 'xmlns:v5="http://schemas.microsoft.com/AssignedAccess/2022/config"'` returns 1.
    - `grep -cF './Vendor/MSFT/AssignedAccess/Configuration'` returns at least 1, and no line containing it starts with a fence marker (D1.8 — inline code span, never a fence).
    - The Case-1 block is present with the four-column header: `grep -cF '| Option | When to choose | Consequence if wrong | Branch |'` returns 1, and `grep -c '> \*\*Ask the admin:\*\*'` returns at least 1 with a blank line between that blockquote and the table (STD-05 D-02 — GFM lazy continuation otherwise swallows the table and it vanishes from the retrieval body).
    - The three-row namespace table is present and labelled by year: `grep -c '2017 (root)'` returns at least 1, and `` grep -c '`v4:`' `` and `` grep -c '`v5:`' `` each return at least 1.
    - `grep -ci 'mismatch'` returns **0**, or every occurrence is an explicit statement that no mismatch failure mode is documented — never an asserted failure mode (KIOSK-02).
    - The MEDIUM tag on the Data type is present: `grep -ciE 'MEDIUM' ` returns at least 1 in the delivery step's vicinity, and `grep -c 'String'` returns at least 1.
    - `grep -ci 'object ID'` returns at least 1 — the `UserGroup/@Name` trap is stated.
    - `grep -cE 'internet connectiv'` returns at least 1 — C-2c's group-path fact is in the two-scope callout.
    - `grep -ci 'standard users only'` returns at least 1 — C-2b's caution line landed.
    - `grep -ci 'packagedAppID'` returns at least 1 (the casing-divergence caution names Learn's table form in prose) **and** Gate A confirms the payload itself uses the sample casing — these are complementary, not contradictory.
    - `grep -ciE 'learn\.microsoft\.com|docs\.microsoft\.com'` returns **0** (D5.4).
    - `grep -c '^# '` returns 1 and `grep -c '^## Summary'` returns 1 — the H1 and Summary are still untouched and Summary is still the first H2.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with `233 files checked, 0 with violations` — C17 `#1` does not match a column-0 xml fence (only a mermaid opener does), and the fence is masked out of the heading assertions precisely because it is at column 0.
    - `git status --porcelain docs/recipes/01-shared-windows-avd-client.md docs/index.md docs/_registry/RE-index.md scripts/validation/c17-eee-contract.mjs` returns empty, and `node scripts/validation/check-phase-130.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && f=docs/recipes/03-windows-11-multi-app-kiosk.md && grep -q '^## Steps' "$f" && [ "$(grep -c '^### Step ' "$f")" -ge 5 ] && [ "$(grep -c '^```' "$f")" = "2" ] && [ "$(grep -c '^```xml' "$f")" = "1" ] && grep -qF 'xmlns="http://schemas.microsoft.com/AssignedAccess/2017/config"' "$f" && grep -qF 'xmlns:v5="http://schemas.microsoft.com/AssignedAccess/2022/config"' "$f" && grep -qF './Vendor/MSFT/AssignedAccess/Configuration' "$f" && grep -qF '| Option | When to choose | Consequence if wrong | Branch |' "$f" && grep -q '> \*\*Ask the admin:\*\*' "$f" && grep -q '2017 (root)' "$f" && grep -qi 'object ID' "$f" && grep -qE 'internet connectiv' "$f" && grep -qi 'standard users only' "$f" && [ "$(grep -ciE 'learn\.microsoft\.com|docs\.microsoft\.com' "$f")" = "0" ] && [ "$(grep -c '^# ' "$f")" = "1" ] && [ "$(grep -c '^## Summary' "$f")" = "1" ] && node -e 'const fs=require("fs");const f="docs/recipes/03-windows-11-multi-app-kiosk.md";const L=fs.readFileSync(f,"utf8").replace(/\r\n/g,"\n").split("\n");const op=[],cl=[];let fen=false;for(let i=0;i<L.length;i++){if(/^(```|~~~)/.test(L[i])){if(!fen){if(!/^```xml\s*$/.test(L[i])){console.error("FENCE: line "+(i+1)+" is not a column-0 xml opener: "+L[i]);process.exit(1)}op.push(i);fen=true}else{cl.push(i);fen=false}}}if(fen||op.length!==1||cl.length!==1){console.error("FENCE: expected exactly one closed fence (D1.3); openers="+op.length+" closers="+cl.length);process.exit(1)}const p=L.slice(op[0]+1,cl[0]).join("\n");for(const n of ["<AllAppsList>","<AllowedApps>","<v5:StartPins","<Taskbar ","ShowTaskbar=","<Configs>","AzureActiveDirectoryGroup","<DefaultProfile "]){if(!p.includes(n)){console.error("PAYLOAD missing required token: "+n);process.exit(1)}}for(const b of ["<AllAppList>","default:","BreakoutSequence","TaskbarLayout","<StartLayout>","rs5:","ClassicAppPath"]){if(p.includes(b)){console.error("PAYLOAD contains banned token: "+b);process.exit(1)}}if(/"applyOnce"/.test(p)){console.error("PAYLOAD contains the applyOnce JSON key (D1.11: omit)");process.exit(1)}const iA=p.indexOf("<AllAppsList>"),iS=p.indexOf("<v5:StartPins"),iT=p.indexOf("<Taskbar ");if(!(iA<iS&&iS<iT)){console.error("PAYLOAD XSD profile_t sequence order violated (D1.12): need AllAppsList < v5:StartPins < Taskbar");process.exit(1)}const g=[...p.matchAll(/\{[0-9a-fA-F]{8}-([0-9a-fA-F]{4}-){3}[0-9a-fA-F]{12}\}/g)].map(m=>m[0]);if(g.length!==2||g[0]!==g[1]){console.error("PAYLOAD Profile/@Id and DefaultProfile/@Id must be ONE identical braced guid_t GUID; found "+JSON.stringify(g));process.exit(1)}if(!/"packagedAppId"/.test(p)){console.error("PAYLOAD v5:StartPins JSON must use the sample casing packagedAppId (D1.10)");process.exit(1)}if(!/\\\\/.test(p)){console.error("PAYLOAD JSON must carry doubled backslashes (T-1/D1.9(1))");process.exit(1)}const at=[...p.matchAll(/DesktopAppPath="([^"]*)"/g)].map(m=>m[1]);if(!at.length){console.error("PAYLOAD needs at least one App/@DesktopAppPath to teach the single-backslash XML form (D1.9(2))");process.exit(1)}if(at.some(a=>a.includes("\\\\"))){console.error("PAYLOAD DesktopAppPath must use SINGLE backslashes - doubling in an XML attribute is the T-1 inversion that genuinely breaks");process.exit(1)}console.log("Gate A OK: one column-0 xml fence, XSD order, matched braced GUIDs, sample casing, both backslash forms")' && node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -q '233 files checked, 0 with violations' && [ -z "$(git status --porcelain docs/recipes/01-shared-windows-avd-client.md docs/index.md docs/_registry/RE-index.md scripts/validation/c17-eee-contract.mjs)" ] && node scripts/validation/check-phase-130.mjs >/dev/null 2>&1 && echo PASS || echo CHECK</automated>
  </verify>
  <done>`## Steps` carries the SC1 spine in order; the STD-05 Case-1 account-model block has its four-column header, its mandatory blank line and its autologon Branch cell routing to Task 1's anti-feature row; the two-scope step is its own named step with only the group-path fact in its callout plus C-2b's caution; exactly one column-0 xml fence carries research §1.A's payload and passes Gate A on element order, matched braced GUIDs, key casing and both backslash forms; the 22-row decomposition table and the exact 3-row year-labelled namespace table ship with no asserted mismatch failure mode; the OMA-URI delivery step ships the node as an inline code span with the Data type tagged MEDIUM and the device-group assignment; C17 233/0; recipe 01 untouched.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: ratify the D3.1 verification disposition before ## Verification is authored</name>
  <decision>How does `## Verification` treat the `AssignedAccess > Operational` event log — AUGMENT the ROADMAP SC5 line (keep it verbatim, add the enable precondition and the Admin-channel Event 31000 line), or SUBSTITUTE a different channel for it?</decision>
  <context>
    CONTEXT.md **D3.1 is rated one-way within the phase** and is marked SUPERSEDED-and-corrected: an earlier round-1 ruling replaced the `Operational` channel with `Admin`, and round 2 overturned that.

    Why it is one-way: `gsd-verifier.md:169` states that plan must-haves may ADD but never SUBTRACT a roadmap Success Criterion, and `130-VERIFICATION.md:18` shows the verifier enumerating ROADMAP SCs **verbatim** with zero overrides applied. ROADMAP SC5 and KIOSK-05 both name *a clean `AssignedAccess > Operational` event log*. A substitution therefore fails SC5 **as written, in-phase** — and cannot be repaired after Phase 138's close-gate flips SC5 to Validated.

    The problem the substitution was trying to solve is real (T-3): the `Operational` channel is **disabled by default**, so a fresh kiosk's channel is empty, and "clean" reads an empty channel as PASS — a guaranteed false pass. Research §3.C resolves it without substituting: the enable procedure is first-party (Event Viewer path plus right-click then Enable Log), and the one-time-events caveat is first-party too (recorded with its own H2 per Concern #4, since the two quotes sit under different H2s on the same page).

    This checkpoint exists because REVERSIBILITY_GATES is on and the rating is one-way — not because the decision is open. It is LOCKED. The gate is here so the disposition is consciously confirmed before the section that cannot be repaired later is written.
  </context>
  <options>
    <option id="augment-locked">
      <name>AUGMENT, do not substitute — the CONTEXT.md D3.1 ruling (RECOMMENDED, and what the plan implements)</name>
      <pros>SC5 and KIOSK-05 are literally satisfied, so no verification `overrides:` entry is needed and Phase 138's close-gate flip stays truthful. The false-pass hazard is closed by the added enable-before-first-kiosk-sign-in precondition, which is what makes the SC5 line executable. Event ID 31000 in the Admin channel adds a check that needs no enable step. The logged correction is honest: SC5 and KIOSK-05 are incomplete, not wrong, and the named correction is the missing precondition.</pros>
      <cons>The Verification section carries three log-related lines rather than one, so it is slightly longer.</cons>
    </option>
    <option id="substitute-admin">
      <name>SUBSTITUTE the Admin channel for Operational — the SUPERSEDED round-1 ruling</name>
      <pros>One log line instead of three; the Admin channel needs no enable step.</pros>
      <cons>Fails ROADMAP SC5 as written, in-phase, at this phase's own verifier — not at Phase 138. Would require a verification override. Unrepairable after the close-gate flips SC5 to Validated. Round 2 explicitly overturned it and CONTEXT.md marks it SUPERSEDED.</cons>
    </option>
  </options>
  <resume-signal>Type `augment-locked` to confirm the locked disposition and proceed, or `substitute-admin` with your reasoning if you are deliberately overriding a locked, one-way decision.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: ## Verification, ## Rollback/Recovery, ## Configuration-Caused Failures, ## See Also, and the full gate sweep</name>
  <files>docs/recipes/03-windows-11-multi-app-kiosk.md</files>
  <reversibility rating="one-way">D3.1's disposition is one-way within the phase — the wrong disposition fails verification and cannot be repaired after the close-gate flips SC5 to Validated. The preceding `checkpoint:decision` gates this task.</reversibility>
  <precondition>`pandoc --version` reports 3.7.0.2 (the `convert.ps1` pin) and PowerShell 7+ is available as `pwsh` — `convert.ps1` declares `#Requires -Version 7.0`, and `guard-docx.mjs:234-236` vacuous-passes when pandoc is absent, so an unverified pandoc turns the HARN-16 de-risking check into a no-op. Verified present at plan time (2026-07-30): pandoc 3.7.0.2. Assert it again before running the conversion; if either is missing, halt and record the gap rather than reporting a pass.</precondition>
  <read_first>
    - `docs/recipes/03-windows-11-multi-app-kiosk.md` — the full file from Tasks 1-2. Read it to reference the exact step anchors the failures table routes to and to word the per-phase verification checks against what the steps actually instruct.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **473-518** — §3.C in full: the verbatim enable procedure with its Event Viewer path and the right-click-then-Enable-Log action, the disabled-by-default statement with its H2, the one-time-events caveat with **its own different H2**, Event ID 31000 verbatim, the Admin-channel default-state `[ASSUMED]` and the wording that avoids asserting it, the registry key to use as the profile-arrived check, and the two sign-in sequencing facts.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **406-449** (§3.A — the three `Ctrl+Alt+Del Options` GPO rows verbatim under their restricted-user-experience scoping sentence, the device-scope statement that the profile changes the device for every user including administrators, the 21 blocked keyboard shortcuts that make excellent verification lines, and the `IdleTimeOut` key with its mixed-scope qualification) and lines **555-574** (§3.E — removal-is-not-rollback verbatim, removal is channel-scoped, Settings self-service removal closed, the self-deploying no-automatic-re-enrollment sentence, the autologon breakers as separate citations, and the confirmed NOT-FOUND for the excluded removal cmdlet).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **616-638** — §3.H's D3.3 and D3.6 halves: the Entra-arm scoping is first-party, both error strings verbatim with `AADSTS50158` confirmed as Terms of Use and `AADSTS50076` as MFA, both Event ID 1098 in a **different** channel from either AssignedAccess log, and the CA-remedy blast radius.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **937-1023** — §6, the copy-pasteable validation commands, and lines **1027-1053** — `## Concerns` (**#1** the HIGH unsourced shortcuts clause with its fully-sourced replacement, **#3** the mixed-scope `IdleTimeOut` paragraph, **#4** the two Operational quotes' different H2s).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-CONTEXT.md` **Area 2** (D2.1-D2.5, lines 73-84, including the amended shortcuts wording, the DISCHARGED B-6, the A-4 temporary-vs-permanent pairing, and the zero-blockquote shape rule) and **Area 3** (D3.1-D3.6, lines 86-97).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-PATTERNS.md` lines **124-143** (the `## Verification` checklist idiom and the `## Configuration-Caused Failures` frozen three-column header) and lines **223-246** (the shipped `## Rollback/Recovery` internal shape — bold lead sentence, bold pseudo-subheadings, plain bullets, zero blockquotes — with the explicit warning that the analog's *position* before Verification does NOT apply here).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-CONTEXT.md` **D6.1(g)** (line 134) — the failures table's Runbook-column routing: in-recipe anchors or existing refs only, no fabricated runbook links, because no Windows-kiosk runbook exists corpus-wide.
  </read_first>
  <action>
    Append the four closing sections, then run the full gate sweep.

    **`## Verification`** — flat `- [ ]` checklist, no sub-headings (neither shipped recipe uses them). D3.2's corrected seam groups the items into two implicit phases:
    - *Admin at console, before the first kiosk sign-in:* enable the `Operational` channel — give the Event Viewer path and the right-click-then-Enable-Log action from research §3.C — and confirm the profile arrived using the device-scope AssignedAccess CSP registry key, which is readable without the kiosk account signing in and distinguishes *policy did not arrive* from *policy arrived but the account is not in scope*, i.e. D4.4's two-scope failure mode. Carry the one-time-events caveat, recorded per Concern #4 as applicable-by-placement rather than co-located, since the two quotes sit under different H2s on the same page.
    - *During and after the kiosk account's first sign-in:* the device signs in and lands in the restricted Start menu showing only allow-listed apps; a non-allow-listed app fails to launch, confirming the generated AppLocker rules; a representative allow-listed app's **secondary** flow — file picker, print, or OAuth redirect — completes without an app-blocked error. Note the D4.5 timing hazard next to the Start-menu line so a partial or empty pin set at first sign-in is not read as a genuine failure. Several of the 21 blocked keyboard shortcuts from research §3.A make excellent extra lines: they are genuinely admin-executable, unambiguous, and need no event log.
    - **Per the ratified checkpoint (D3.1 AUGMENT-NOT-SUBSTITUTE):** retain SC5's clean `AssignedAccess > Operational` line **verbatim**, and ADD absence of Event ID 31000 in `Microsoft-Windows-AssignedAccess/Admin` as a further secondary line. Phrase that line as an instruction to check the channel rather than an assertion about its default state, since the Admin channel's default is `[ASSUMED]` and no page states it. Name the specific negatives — Event 31000 and profile parse/apply errors — rather than leaving "clean" undefined, while keeping SC5's word "clean" satisfiable by stating what clean means (D3.4).
    - The interactive-CA check ships as an **Entra-arm-scoped** line, labelled *Entra account/group only* and as interactive CA (MFA **or** Terms of Use), because `AADSTS50158` is an external-security-challenge failure and `AADSTS50076` is the MFA sample; both are Event ID 1098 in a **different** channel from either AssignedAccess log — do not conflate them (D3.3). KIOSK-03 requires the decision block to present both arms, so an unlabelled line would have no referent on the autologon arm.
    - `AppNotFound` is **DELETED** from this section (T-4/D3.4). It already ships as a named prerequisite symptom from Task 1. Gate B asserts this section-scoped.

    **`## Rollback/Recovery`** — the heading text is **D2.3**-locked verbatim (it is the corpus-native form, shipping in three `docs/reference/` docs, while a bare `## Recovery` has zero matches corpus-wide), placed **between** `## Verification` and `## Configuration-Caused Failures` (D2.4, SC4; Gate B asserts both the exact text and the position). Shape: bold lead sentence, bold pseudo-subheadings, plain bullets, **zero blockquotes** — the convention in all four corpus rollback sections, and it keeps C17 `#12` inapplicable. Copy only the analog's internal shape, never its position. Content, bounded to first-party facts:
    - The shortcuts sentence — ship **research Concern #1's fully-sourced rewording**, not the wording the earlier draft mandated. The four shortcuts are not blocked for a restricted-user-experience account; Ctrl+Alt+Del is the documented way to exit the kiosk experience; and the profile automatically applies the `Ctrl+Alt+Del Options` policies `Remove Logoff`, `Remove Task Manager` and `Remove Change Password`, which strip those affordances. The clause tying Ctrl+Alt+Del to a <!-- planner-discipline-allow: security screen --> "security screen" has **no first-party sentence** behind it and must NOT ship — research Concern #1 rates it HIGH, the source page never mentions such a screen at all (which is exactly why T-5 is right), and no page ties Ctrl+Alt+Del to one either. The phrase is negative-grepped to zero in `<acceptance_criteria>`. State the three GPO settings as **consequences of deploying the profile**, never as steps the admin performs.
    - The A-4 temporary-vs-permanent pairing: Ctrl+Alt+Del exits the **running session only** and the kiosk relaunches on re-sign-in or after the sign-in-screen time-out; the **permanent** path is Intune policy unassignment, which has a network-connectivity precondition. Do **not** import the configurable-by-default parenthetical — that configurability is the breakout-sequence element, which KIOSK-02 bars as not authorable in this profile branch.
    - The sign-in-screen relaunch time-out and its registry override (B-6 is DISCHARGED — cite, do not cut): give the key, path, type and units. Per Concern #3, attribute the default to the multi-app kiosk page rather than asserting it verified for a multi-app profile — that paragraph is demonstrably mixed-scope, using singular single-app prose and making a breakout-sequence claim that is schema-illegal in this profile branch. Note the override does not apply to the Edge kiosk mode.
    - Settings self-service removal is closed for a restricted user experience; removal is channel-scoped; **removal is not rollback** — it cannot revert all the changes, and in a multi-app kiosk scenario the Start menu configuration is maintained.
    - Autopilot Reset gets **one negative sentence**: it retains provisioning packages and MDM enrollment and re-applies the lockdown, so unassign the policy first. Add the self-deploying-specific fact that a device cannot automatically re-enroll after an initial self-deploying deployment — the device record must be deleted in the admin center instead.
    - The autologon-vs-Entra recovery differential ships as **two separately-cited facts under `[ASSUMED]`**, never welded into a claim about which recovers better.
    - Do **not** author the unsourced single-app scoping claim about the PowerShell removal cmdlet that D2.5 excludes: research §3.E confirms zero occurrences across every page fetched.
    - Worth one line from research §3.A: deploying this profile applies device-scope policy settings to **every** user of the device, administrators included. That is genuinely surprising and it is first-party.
    - Record in the SUMMARY, per D2.1/D2.2/D8.3, that this H2 is a **named documented divergence** from `docs/_templates/recipe-template.md`, and that the reason amendment was rejected is the retroactive non-conformance ripple onto recipes 01 and 02 — foundation-class work v1.19 is not scoped for — **not** a harness block, since no validator asserts an H2 set or order. Add the matching `v1.19-DEFERRED-CLEANUP.md` entry (HARN-16 creates that file in Phase 138 — a real landing spot).

    **`## Configuration-Caused Failures`** — the frozen three-column header `| Misconfiguration | Symptom | Runbook |`. Recipe-owned rows only. Per D6.1(g), route every Runbook cell to an in-recipe step anchor or an existing linked reference — **never a fabricated runbook link**, because no Windows-kiosk runbook exists corpus-wide and `REQUIREMENTS.md:81` puts one out of scope. Good rows come straight from the research pitfalls: the doubled-backslash inversion in an XML attribute; an empty `Operational` channel read as a pass; a display name used instead of the group object ID (the policy applies and no restricted experience appears); a partial Start menu at first sign-in; an unbraced `Profile Id`; assuming the policy landing means the kiosk is configured; a group containing admin principals; and the Start-layout-not-as-expected diagnostic of checking whether the Start-layout apps are installed for the assigned-access user.

    **`## See Also`** — link in-corpus targets only (D5.4): the corrected STD-05 standard, `../admin-setup-apv1/08-self-deploying.md`, `../admin-setup-apv1/03-esp-policy.md`, `../admin-setup-apv1/04-dynamic-groups.md`, `../apv1-vs-apv2.md`, and the one-line single-app cross-link to `../recipes/01-shared-windows-avd-client.md#step-5a-kiosk-configuration`.

    Then run the full sweep from research §6: C17 full-corpus and self-test; `check-phase-114`, `-120`, `-129` and `-130`; `check-nav-hub-links` and its self-test; `build-filename-map.mjs --self-test` (must still read 223 — if it fails, the registry was edited out of turn); and the pandoc plus guard-docx conversion, after confirming pandoc's version first so the check is not a vacuous pass. Record in the SUMMARY: every first-party citation supporting the body (they live here, not in the recipe, per D5.4); the MEDIUM-confidence Data-type item; each remaining `[ASSUMED]` (the Admin channel's default state, the autologon-vs-Entra differential, the well-formedness-cast idiom, the desktop-pin-by-AUMID key casing); and the D6.1 first-lander handoff list with its inherit-or-justify-divergence gate for Phase 136, using **post-archive** paths under `.planning/milestones/v1.18-phases/` (A-5 — the recurring archival-drift class).
  </action>
  <acceptance_criteria>
    - **Gate B passes** (see `<verify>`): the H2 set and order is exactly Summary, Prerequisites, Unsupported and Anti-Feature Callouts, Steps, Verification, Rollback/Recovery, Configuration-Caused Failures, See Also — which simultaneously proves the Phase-129 D-06 skeleton, D2.4's `## Rollback/Recovery` placement between Verification and Configuration-Caused Failures, and that no extra H2 was introduced. Section-scoped: `AppNotFound` is absent from `## Verification` and present elsewhere in the file; `## Rollback/Recovery` contains zero top-level blockquotes; `## Verification` contains both `AssignedAccess > Operational` and `31000`.
    - `grep -c '^## Verification'` returns 1 and `grep -c '^- \[ \] '` returns at least 8.
    - `grep -cF 'AssignedAccess > Operational'` returns at least 1 — ROADMAP SC5's line is retained verbatim (D3.1, ratified at the checkpoint).
    - `grep -cF 'Enable Log'` returns at least 1 and the Verification section states the enable step happens **before** the first kiosk sign-in — the precondition that makes the SC5 line executable (D3.1(2)/T-3).
    - `grep -cF 'Microsoft-Windows-AssignedAccess/Admin'` returns at least 1 and `grep -c '31000'` returns at least 1 (D3.1(3)).
    - `grep -c 'AADSTS50158'` and `grep -c 'AADSTS50076'` each return at least 1, and `grep -cF 'Microsoft-Windows-AAD/Operational'` returns at least 1 — the three channels are distinguished, not conflated (D3.3).
    - `grep -c '^## Rollback/Recovery'` returns 1 and `grep -cF '| Misconfiguration | Symptom | Runbook |'` returns 1.
    - `grep -c 'Remove Logoff'`, `grep -c 'Remove Task Manager'` and `grep -c 'Remove Change Password'` each return at least 1; `grep -c 'Remove-Logoff'` returns **0** (the hyphenated form reads as a nonexistent cmdlet — T-5).
    - `grep -ciE 'security screen'` returns **0** — research Concern #1's unsourced clause does not ship; the fully-sourced rewording does.
    - `grep -c 'Clear-AssignedAccess'` returns **0** — D2.5's unsourced exclusion holds (confirmed zero occurrences across every page fetched, research §3.E).
    - `grep -ciE '\bIdleTimeOut\b'` returns at least 1 and `grep -c '30 second'` or `grep -c '30-second'` returns at least 1, attributed to the multi-app kiosk page rather than asserted as verified for a multi-app profile (Concern #3).
    - `grep -ci 'removal is not rollback'` returns at least 1, or the equivalent first-party sentence about the Start menu configuration being maintained is present.
    - `grep -ciE '\]\([^)]*runbook[^)]*\)'` returns **0** — no fabricated runbook link (D6.1(g), the Phase-130 G-LOCK-5 guard).
    - `grep -ciE 'learn\.microsoft\.com|docs\.microsoft\.com'` returns **0** (D5.4).
    - Every in-doc `#fragment` link in the file resolves against the file's own GitHub-slugified headings — asserted by the mirrored-slugify anchor gate in `<verify>`, which computes slugs the way `check-nav-hub-links.mjs` does (strip-in-place, so the double-hyphen artifact is reproduced and a mis-slugified anchor is genuinely caught). Do NOT edit `check-nav-hub-links.mjs` or its frozen `HUB_PATHS` roster.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with `233 files checked, 0 with violations`, and `--self-test` exits 0 with `0 failed`.
    - `node scripts/validation/check-phase-114.mjs`, `check-phase-120.mjs`, `check-phase-129.mjs` and `check-phase-130.mjs` each exit 0.
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 reporting `0 outbound failure(s), 0 inbound failure(s)`, and `--self-test` exits 0.
    - `node scripts/pipeline/build-filename-map.mjs --self-test` exits 0 with `0 failed` and `grep -cE '^\|[[:space:]]*RE-[0-9]+[[:space:]]*\|' docs/_registry/RE-index.md` returns **223**.
    - `pandoc --version` reports `3.7.0.2`; `pwsh -File scripts/pipeline/convert.ps1` converts the recipe to a `.docx` with exit 0; `node scripts/pipeline/guard-docx.mjs <the .docx>` exits 0 with both checks passing (no `---` in the first ~500 chars of extracted body text, and Heading1/Heading2/Heading3 pStyle IDs present). This de-risks HARN-16 in-phase, since the payload fence is exactly the novel content that could break it.
    - `git status --porcelain docs/recipes/01-shared-windows-avd-client.md docs/index.md docs/_registry/RE-index.md docs/_templates/recipe-template.md scripts/validation/ scripts/pipeline/` returns empty — zero edits to recipe 01, to navigation, to the registry, to the template, or to any validator or pipeline script.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && f=docs/recipes/03-windows-11-multi-app-kiosk.md && [ "$(grep -c '^## Verification' "$f")" = "1" ] && [ "$(grep -c '^- \[ \] ' "$f")" -ge 8 ] && grep -qF 'AssignedAccess > Operational' "$f" && grep -qF 'Enable Log' "$f" && grep -qF 'Microsoft-Windows-AssignedAccess/Admin' "$f" && grep -q '31000' "$f" && grep -q 'AADSTS50158' "$f" && grep -q 'AADSTS50076' "$f" && grep -qF 'Microsoft-Windows-AAD/Operational' "$f" && [ "$(grep -c '^## Rollback/Recovery' "$f")" = "1" ] && grep -qF '| Misconfiguration | Symptom | Runbook |' "$f" && grep -q 'Remove Logoff' "$f" && grep -q 'Remove Task Manager' "$f" && grep -q 'Remove Change Password' "$f" && [ "$(grep -c 'Remove-Logoff' "$f")" = "0" ] && [ "$(grep -ciE 'security screen' "$f")" = "0" ] && [ "$(grep -c 'Clear-AssignedAccess' "$f")" = "0" ] && grep -qiE '\bIdleTimeOut\b' "$f" && [ "$(grep -ciE '\]\([^)]*runbook[^)]*\)' "$f")" = "0" ] && [ "$(grep -ciE 'learn\.microsoft\.com|docs\.microsoft\.com' "$f")" = "0" ] && node -e 'const fs=require("fs");const f="docs/recipes/03-windows-11-multi-app-kiosk.md";const L=fs.readFileSync(f,"utf8").replace(/\r\n/g,"\n").split("\n");const m=Array(L.length).fill(false);let fen=false;for(let i=0;i<L.length;i++){if(/^(```|~~~)/.test(L[i])){fen=!fen;continue}m[i]=fen}const h2=[];for(let i=0;i<L.length;i++){if(!m[i]&&/^## /.test(L[i]))h2.push([L[i].slice(3).trim(),i])}const exp=["Summary","Prerequisites","Unsupported and Anti-Feature Callouts","Steps","Verification","Rollback/Recovery","Configuration-Caused Failures","See Also"];const got=h2.map(x=>x[0]);if(JSON.stringify(got)!==JSON.stringify(exp)){console.error("H2 SET/ORDER MISMATCH\n got: "+JSON.stringify(got)+"\n exp: "+JSON.stringify(exp));process.exit(1)}const sec=n=>{const i=h2.findIndex(x=>x[0]===n);const s=h2[i][1];const e=i+1<h2.length?h2[i+1][1]:L.length;return L.slice(s+1,e)};const ver=sec("Verification").join("\n");const rb=sec("Rollback/Recovery");const all=L.join("\n");if(/AppNotFound/.test(ver)){console.error("AppNotFound appears inside ## Verification - T-4/D3.4 delete it there; it ships ONLY as a named prerequisite symptom");process.exit(1)}if(!/AppNotFound/.test(all)){console.error("AppNotFound missing from the file - D3.5/KIOSK-05 require it as a named prerequisite symptom");process.exit(1)}if(rb.some(l=>/^>/.test(l))){console.error("## Rollback/Recovery contains a top-level blockquote - D2.5 mandates zero (bold pseudo-heading + plain bullets)");process.exit(1)}if(!/AssignedAccess > Operational/.test(ver)){console.error("## Verification must retain ROADMAP SC5 verbatim: a clean AssignedAccess > Operational event log (D3.1 augment-not-substitute)");process.exit(1)}if(!/31000/.test(ver)){console.error("## Verification must add the AssignedAccess/Admin Event ID 31000 secondary line (D3.1(3))");process.exit(1)}const slug=t=>t.toLowerCase().replace(/[^a-z0-9 _-]/g,"").replace(/ /g,"-");const set=new Set(),seen=new Map();for(let i=0;i<L.length;i++){if(m[i])continue;for(const x of L[i].matchAll(/\{#([a-zA-Z0-9_-]+)\}/g))set.add(x[1])}for(let i=0;i<L.length;i++){if(m[i])continue;const h=L[i].match(/^#{1,6}\s+(.*)$/);if(!h)continue;if(/\s*\{#[a-zA-Z0-9_-]+\}\s*$/.test(h[1]))continue;const b=slug(h[1].replace(/\*\*/g,"").replace(/`/g,"").trim());const n=seen.get(b)||0;seen.set(b,n+1);set.add(n===0?b:b+"-"+n)}const bad=[];for(let i=0;i<L.length;i++){if(m[i])continue;const re=/\[[^\]]*\]\(#([^)]+)\)/g;let x;while((x=re.exec(L[i])))if(!set.has(x[1]))bad.push((i+1)+": #"+x[1])}if(bad.length){console.error("UNRESOLVED IN-DOC ANCHORS:\n"+bad.join("\n"));process.exit(1)}console.log("Gate B OK: exact 8-H2 skeleton with Rollback/Recovery between Verification and Configuration-Caused Failures; AppNotFound out of Verification; Rollback blockquote-free; SC5 line + Event 31000 present; "+set.size+" headings, all in-doc anchors resolve")' && node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -q '233 files checked, 0 with violations' && node scripts/validation/c17-eee-contract.mjs --self-test 2>&1 | grep -q '0 failed' && node scripts/validation/check-phase-114.mjs >/dev/null 2>&1 && node scripts/validation/check-phase-120.mjs >/dev/null 2>&1 && node scripts/validation/check-phase-129.mjs >/dev/null 2>&1 && node scripts/validation/check-phase-130.mjs >/dev/null 2>&1 && node scripts/validation/check-nav-hub-links.mjs 2>&1 | grep -q '0 outbound failure(s), 0 inbound failure(s)' && node scripts/pipeline/build-filename-map.mjs --self-test 2>&1 | grep -q '0 failed' && [ "$(grep -cE '^\|[[:space:]]*RE-[0-9]+[[:space:]]*\|' docs/_registry/RE-index.md)" = "223" ] && pandoc --version 2>&1 | grep -q '3\.7\.0\.2' && pwsh -File scripts/pipeline/convert.ps1 -InputMd docs/recipes/03-windows-11-multi-app-kiosk.md -OutputDocx .pipeline-output/03-windows-11-multi-app-kiosk.docx >/dev/null 2>&1 && node scripts/pipeline/guard-docx.mjs .pipeline-output/03-windows-11-multi-app-kiosk.docx >/dev/null 2>&1 && [ -z "$(git status --porcelain docs/recipes/01-shared-windows-avd-client.md docs/index.md docs/_registry/RE-index.md docs/_templates/recipe-template.md scripts/validation/ scripts/pipeline/)" ] && echo PASS || echo CHECK</automated>
  </verify>
  <done>`## Verification` retains SC5's clean Operational line verbatim and ADDS the enable-before-first-kiosk-sign-in precondition, the one-time-events caveat recorded as applicable-by-placement, and Event 31000 in the Admin channel — with `AppNotFound` proven absent from the section and the interactive-CA line scoped to the Entra arm across three distinct channels. `## Rollback/Recovery` sits between Verification and Configuration-Caused Failures as a named divergence, blockquote-free, carrying the fully-sourced shortcuts rewording (no security-screen clause), the temporary-vs-permanent pairing without the barred parenthetical, the cited relaunch time-out and its registry override with its mixed-scope qualification, removal-is-not-rollback, the one Autopilot Reset sentence plus the self-deploying re-enrollment fact, and the two separately-cited `[ASSUMED]` recovery facts. The failures table routes only to in-recipe anchors and existing refs; See Also is in-corpus only. Gate B, C17 233/0 and self-test, all four check-phase validators, the link checker 0/0, the 223-row drift canary, and the pandoc plus guard-docx conversion all pass; every in-doc anchor resolves; recipe 01, navigation, the registry, the template and every validator and pipeline script are untouched.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 135-02)

**New files:** none. This plan authors the body of the file Plan 135-01 created.

**Modified file**

| Path | What this plan adds |
|---|---|
| `docs/recipes/03-windows-11-multi-app-kiosk.md` | Everything from the Scope banner down. The H1, `## Summary` and frontmatter created by Plan 135-01 are read-only here. |

**New named sections / headings created in `docs/recipes/03-windows-11-multi-app-kiosk.md`**

| Heading | Level | Contents |
|---|---|---|
| `> **Scope:**` banner | blockquote | The RE-224-scoped kiosk/dedicated-device taxonomy sentence, measured post-sentinel-strip against the 200-char cap and split into two runs if it overflows |
| `## Prerequisites` | H2 | `**This recipe is NOT:**` opener; inline RBAC + licensing; direct `../admin-setup-apv1/*` links; four LTSC-spelled edition families + Pro Education footnote; the 22H2 floor + 21H2 caution; TPM 2.0 with attestation; the VM / Hyper-V vTPM `0x800705B4` bullet; Ethernet as the zero-touch precondition; `AppNotFound` as a named symptom |
| `## Unsupported and Anti-Feature Callouts` | H2 | Frozen 3-column table: interactive CA/MFA (+ blast radius), the Windows-11-wrong-GUI trap, group `Configs` profile restriction, nested `UserGroup`, hardcoded AUMIDs, `Configuration` superseding `KioskModeApp`, SharedPC layering, `AssignedAccess/Status` not-deliverable, **the autologon-tradeoff row carrying the single-app cross-link**, **the app-provisioning-timing row** |
| `## Steps` | H2 | The SC1 spine |
| `### Step N:` — enrollment / ESP / dynamic group | H3 | Self-deploying, device-phase-only ESP, dynamic device group; all cross-linked directly |
| `### Step N:` — kiosk account model | H3 | The **STD-05 Case-1 decision block** (`> **Ask the admin:**`, mandatory blank line, `| Option | When to choose | Consequence if wrong | Branch |`) with the autologon arm's Branch cell as a routing cross-link |
| `### Step N:` — apps | H3 | Device-context, Required, device group; the first-sign-in timing hazard; the app-dependency rule |
| `### Step N:` — the two-scope distinction | H3 | Policy-delivery scope vs effective-configuration scope, with only the group-path internet-connectivity fact in its callout plus the standard-users-only caution |
| `### Step N:` — author the XML | H3 | The numbered click-list, then **the single column-0 ```` ```xml ```` fence** carrying the complete payload, then the **22-row field-decomposition table** with its prose summary line, then the **3-row namespace/version-floor table**, plus the backslash / key-casing / `applyOnce` / element-spelling / braced-GUID cautions |
| `### Step N:` — deliver via custom OMA-URI | H3 | The 11-step click-path, the node as an inline code span, Data type `String` tagged MEDIUM, device-group assignment, the obscured-stored-value callout, GATE 1 re-cited fresh |
| `## Verification` | H2 | Flat `- [ ]` checklist across the D3.2 seam; SC5's Operational line verbatim; the enable precondition; Event 31000; the Entra-arm-scoped interactive-CA line |
| `## Rollback/Recovery` | H2 | **The named KIOSK-04 divergence**, between Verification and Configuration-Caused Failures; bold pseudo-headings + plain bullets, zero blockquotes |
| `## Configuration-Caused Failures` | H2 | Frozen `| Misconfiguration | Symptom | Runbook |`, routing only to in-recipe anchors and existing refs |
| `## See Also` | H2 | In-corpus links only |

**Deliberately NOT produced** (Phase 137, navigation-last): no `docs/_registry/RE-index.md` row, no `docs/index.md` table row or quick-nav bullet, no `filename-map.md` regeneration, no 223-to-225 canary bump, no `Status: Approved` flip. And zero edits to `docs/recipes/01-shared-windows-avd-client.md`, `docs/_templates/recipe-template.md`, or any validator or pipeline script.
</artifacts_this_phase_produces>

<threat_model>
Documentation-only plan. No executable surface is added, no data flow, no trust boundary crossed, no runtime code, no auth/session/input-validation/crypto, and no npm/pip/cargo installs — the **Package Legitimacy Gate does not fire**, so no `[ASSUMED]`/`[SUS]` install checkpoint is required. The deliverable is one static Markdown Guide in a C17-validated corpus; it *documents* Microsoft's Intune and Windows platform-security surface (AssignedAccess, Conditional Access, Entra join) but introduces no such surface into this repository. Follows the Phase-130 precedent (`130-02-PLAN.md:181`).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author to validated corpus | Markdown edits cross into a surface frozen validators and predecessor milestone audits read; the integrity risk is corpus/validator tampering, not input handling |
| recipe to reader's tenant | The recipe instructs identity-security changes in the reader's tenant (CA exclusion, group membership), so a mis-scoped instruction is a real-world blast-radius defect even though it is inert in this repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-135-05 | Tampering | `docs/recipes/01-shared-windows-avd-client.md` (Step 5a/5b headings are literal-string assertions against live HEAD inside every apex chain) | high | mitigate | Read-only in every task. Both `git status --porcelain` on the file and a `check-phase-130` exit-0 run are acceptance criteria in all three tasks |
| T-135-06 | Tampering | `scripts/validation/*` and `scripts/pipeline/*` (C17 is subprocess-spawned by every milestone audit — an edit retroactively mutates predecessor audits) | high | mitigate | Zero edits. Task 3 asserts `git status --porcelain scripts/validation/ scripts/pipeline/` is empty; the two structural gates are read-only `node -e` checks that mirror `check-nav-hub-links.mjs` slugify logic without touching it |
| T-135-07 | Information disclosure | the CA-exclusion remedy as documented (with a `UserGroup` `Config` it is a per-member identity-security change for every group member, not a per-device setting) | medium | mitigate | D3.6 requires the blast radius be stated explicitly in the anti-feature row; C-2b requires the standard-users-only caution so a group holding admin principals is not silently named |
| T-135-08 | Tampering | the worked payload itself — a reader copy-pastes it into a production tenant, so a schema-invalid or backslash-inverted payload is a defect that ships | high | mitigate | Gate A asserts XSD sequence order, matched braced GUIDs, the required and banned token sets, sample key casing, and both backslash forms — the P1 inversion and the T-8 element-spelling trap are each a hard failure |
| T-135-09 | Repudiation | an unsourced mechanism shipping in a recipe (the exact defect class HYG-05 exists to close) | medium | mitigate | research `## Concerns` #1/#2/#3/#7 are bound to acceptance criteria: the security-screen clause is negative-grepped to zero, the mandated edition clause may not ship inside quotation marks, and the MEDIUM Data-type item must be tagged |
| T-135-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan; the gate does not fire |
</threat_model>

<verification>
- **SC1:** the linear spine ships in ROADMAP order with the single-app case as a one-line cross-link to recipe 01's Step 5a anchor; `git status --porcelain` plus a `check-phase-130` exit-0 run prove zero edits to recipe 01.
- **SC2:** Gate A proves exactly one closed column-0 xml fence, the XSD `profile_t` sequence order, one identical braced GUID in both places, sample key casing, both backslash forms, and the absence of every banned token including the T-8 element-spelling trap and the T-7 `default:` prefix. The 22-row decomposition table and the exact 3-row year-labelled namespace table are present, with no asserted mismatch failure mode.
- **SC3:** the STD-05 Case-1 header, the `> **Ask the admin:**` lead-in with its mandatory blank line, and every mandated anti-feature row's distinguishing token are each asserted; the autologon Branch cell's in-doc anchor resolves via the mirrored-slugify gate.
- **SC4:** Gate B's exact 8-H2 order proves `## Rollback/Recovery` sits between `## Verification` and `## Configuration-Caused Failures` with no extra H2; the section is proven blockquote-free; the unsourced security-screen clause and the excluded removal cmdlet are both negative-grepped to zero.
- **SC5:** Gate B proves, section-scoped, that `## Verification` retains `AssignedAccess > Operational` and carries Event 31000, and that `AppNotFound` is absent from it while present elsewhere in the file. The three log channels are each asserted present and are distinguished, not conflated.
- **Gates:** C17 233/0 and self-test 0 failed with `#9`/`#12` live; `check-phase-114/120/129/130` exit 0; `check-nav-hub-links` 0 outbound / 0 inbound plus self-test; `build-filename-map.mjs --self-test` still at 223 rows; pandoc 3.7.0.2 confirmed, then `convert.ps1` and `guard-docx.mjs` both exit 0 — de-risking HARN-16 in-phase.
- **Scope discipline:** zero inline external URLs in the body; zero navigation or registry wiring; zero edits to the template or to any validator or pipeline script.
</verification>

<success_criteria>
`docs/recipes/03-windows-11-multi-app-kiosk.md` delivers ROADMAP SC1-SC5 as one coherent, C17-green Guide: a linear happy path from self-deploying enrollment to a verified Windows 11 restricted user experience delivered through the AssignedAccess CSP `Configuration` node via an Intune custom OMA-URI profile, with the single-app case as a one-line cross-link and zero edits to recipe 01; a bounded, schema-valid worked payload on both surfaces (one column-0 fence plus a field-decomposition table) with the exact 3-row namespace table and no asserted mismatch failure mode; the Case-1 account-model block and the full anti-feature table including the autologon-routing and app-timing rows; the named `## Rollback/Recovery` divergence in its locked position, blockquote-free and bounded to first-party facts; and admin-executable verification that **retains** SC5's clean `AssignedAccess > Operational` line verbatim while adding the enable precondition and the Admin-channel Event 31000 line — implementing every locked CONTEXT.md decision, never a superseded version, and never shipping an unsourced mechanism in the very phase that exists to correct one.
</success_criteria>

<output>
Create `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-02-SUMMARY.md` when done, recording: every first-party citation supporting the body (they live here, not in the recipe, per D5.4); the MEDIUM-confidence Intune Data-type item flagged as the one delivery-step value not presented as verified; each remaining `[ASSUMED]` (the Admin channel's default state, the autologon-vs-Entra recovery differential, the PowerShell well-formedness-cast idiom, the desktop-pin-by-AUMID key casing); D5.5's explicit two-sided NOT-FOUND ruling that the user-account-control and console-only requirements are NOT stated; D2.1/D2.2's named `## Rollback/Recovery` template divergence with its real reason (the recipes-01/02 non-conformance ripple, not a harness block) plus its `v1.19-DEFERRED-CLEANUP.md` entry; the C17-vs-`convert.ps1` fence-mask divergence entry per `REQUIREMENTS.md:68`; D4.2's three grounds for a Case-1 block with a routing-cross-link arm, so Phase 136 does not re-litigate them; and the **D6.1 first-lander handoff list** with its inherit-or-justify-divergence gate for Phase 136 — items (a) through (g), using post-archive paths under `.planning/milestones/v1.18-phases/` per A-5, and recording explicitly that the CDATA-JSON tension is native to RE-224 and is NOT inherited, and that gray area #8 stays chartered to Phase 136.
</output>
