---
phase: 135-recipe-3-windows-11-multi-app-kiosk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_standards/EEE-SOP-standard.md
  - docs/recipes/03-windows-11-multi-app-kiosk.md
  - .planning/research/STACK.md
  - .planning/research/FEATURES.md
  - .planning/research/SUMMARY.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HYG-05]
must_haves:
  truths:
    - "HYG-05 site 1 (STD-05 section intro, ~:462) no longer carries the false invisibility mechanism; the causal clause states that fenced content IS indexed but retrieves poorly as non-prose runs (per D7.1/D7.2)"
    - "HYG-05 site 2 (D-03 case-boundary rule, ~:496-498) no longer carries the false invisibility mechanism, and D-03's normative sentence 'Decision content is never placed inside a code fence in a live recipe' survives byte-identical and unchanged in force (per D7.2, ROADMAP SC6)"
    - "ADDITIVE THIRD SITE (per D7.1 / A-3, NOT named by ROADMAP SC6): HYG-05 site 3 (D-07 worked-example preamble, ~:538-539) no longer contains the string 'silently hide the decision from retrieval' — without this must-have the verifier will never look for it"
    - "The corpus-wide count of the false fenced-content mechanism in docs/ is ZERO; the two DO-NOT-TOUCH sites survive — STD-04 D-01's 'lands verbatim as garbage in the citation' (~:415, per D7.3) and the Grounding Notes metadata claim 'metadata is invisible to Copilot at query time' (~:268, per D7.4)"
    - "docs/_standards/EEE-SOP-standard.md '## Version History' has exactly 4 dated data rows — one new v1.19 HYG-05 row appended after the 2026-07-17 row, which is itself byte-unchanged because check-phase-129's D-02 regex is satisfied by it (per D7.6)"
    - "STD-001's last_verified is NOT bumped (Phase 129 D-12 governs, per D7.6)"
    - "The Operational-channel enable-before-first-kiosk-sign-in precondition is appended at all four D8.2 sites — research/STACK.md:16, research/STACK.md:72, research/FEATURES.md:169, research/SUMMARY.md:55 — each tagged CORRECTED 2026-07-30 (per D8.2; STACK.md:72 is the critical one because KIOSK-01 and the ROADMAP Hard constraints point the phase at STACK.md:13-16)"
    - "research/FEATURES.md:44 carries the unhyphenated first-party GPO name 'Remove Logoff', and its existing Ctrl+Alt+Del-scoped security-screen clause is UNCHANGED — that clause is already the corrected form, so editing it would be a wrong edit (per T-5 as amended by research Concern #6)"
    - "REQUIREMENTS.md:17's unscoped security-screen claim is replaced by the fully-sourced rewording and its hyphenated GPO name is corrected (Corrections items 1 and 2); REQUIREMENTS.md:15's namespace-alias paraphrase is corrected to the unprefixed root 2017 xmlns with 'default' named as a non-writable XSD-internal prefix (Corrections item 4, per T-7)"
    - "docs/recipes/03-windows-11-multi-app-kiosk.md exists with doc_id RE-224, status Draft, platform Windows, doc_type Guide, real last_verified/review_by dates and NO TEMPLATE-SENTINEL — the sentinel is stripped BEFORE any C17 #9/#12 measurement is taken anywhere in this phase (per D5.6/D5.7)"
    - "C17 reports 233 files checked, 0 with violations — the 232-file baseline plus the newly enrolled RE-224 (a count of 232 means the file was not enrolled)"
    - "check-phase-114, check-phase-120, check-phase-129 and check-phase-130 all exit 0 after the edits; zero edits to docs/recipes/01-shared-windows-avd-client.md"
    - "build-filename-map.mjs --self-test still asserts 223 registry rows — docs/_registry/RE-index.md is untouched, the 223 to 225 bump is CLASS-05's named Phase-137 deliverable"
  artifacts:
    - path: "docs/_standards/EEE-SOP-standard.md"
      provides: "Corrected fenced-content rationale at three sites plus a dated v1.19 Version-History row, with the normative D-03/D-04 rules unchanged in force"
      contains: "Decision content is never placed inside a code fence in a live recipe"
    - path: "docs/recipes/03-windows-11-multi-app-kiosk.md"
      provides: "The RE-224 file identity — sentinel-free frontmatter, EEE header block, H1 and a C17-green Summary — the enrolled, gate-proven shell Plan 135-02 authors the body into"
      contains: "doc_id: RE-224"
  key_links:
    - from: "docs/_standards/EEE-SOP-standard.md (three edited rationale clauses + appended Version-History row)"
      to: "check-phase-114 / check-phase-120 / check-phase-129 content-string needles"
      via: "all needles are content strings, never line coordinates, and none falls inside an edit range (research §4.F)"
      pattern: "Admin Decision-Point Block Format (STD-05)"
    - from: "docs/recipes/03-windows-11-multi-app-kiosk.md frontmatter doc_id"
      to: "c17-eee-contract.mjs enrollment (doc_id present AND path under docs/)"
      via: "frontmatter doc_id key makes the file C17-enrolled the moment it is created; 232 becomes 233"
      pattern: "doc_id: RE-224"
    - from: "docs/recipes/03-windows-11-multi-app-kiosk.md (created here, body authored in 135-02)"
      to: "the wave-2 dependency that structurally enforces D7.7"
      via: "HYG-05 lands in wave 1 so the corpus never self-contradicts at the commit that introduces the payload fence"
      pattern: "wave: 2"
---

<objective>
HYG-05 plus the RE-224 file identity — the tracer slice for Phase 135. Correct `docs/_standards/EEE-SOP-standard.md`'s empirically-false fenced-content rationale at **three** sites (D7.1), append the dated v1.19 `## Version History` row (D7.6), land the four D8.2 Operational-channel precondition appends and the three named requirement/research corrections (D8.2/D8.3), and create `docs/recipes/03-windows-11-multi-app-kiosk.md` with real, sentinel-free frontmatter (D5.6/D5.7) — proving C17 goes 232 to 233 files at 0 violations before any body content exists.

Purpose: D7.7 requires HYG-05 to land BEFORE the recipe's payload fence, enforced at **wave** granularity (D8.1) — authoring the fence first would make the corpus self-contradict at that very commit. Stripping the TEMPLATE-SENTINEL here is equally load-bearing: the sentinel makes C17 skip `#9` and `#12`, so every author-time blockquote measurement Plan 135-02 takes would be void if the identity were created later (D5.7).

Output: EEE-SOP-standard.md corrected at three sites + one Version-History row; four research appends + two REQUIREMENTS corrections; one new enrolled, C17-green recipe shell (RE-224, Draft). Zero registry edits, zero navigation edits, zero validator edits, zero edits to recipe 01.

This plan IMPLEMENTS locked decisions — it does not re-open them. `135-CONTEXT.md` `<decisions>` are binding, and its eight numbered anti-regression traps (T-1..T-8) correct earlier drafts AND locked requirement text; several rulings are marked "SUPERSEDED" with the corrected version inline. Never implement a superseded version.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-CONTEXT.md
@.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-PATTERNS.md
@docs/_standards/EEE-SOP-standard.md

Note: `135-RESEARCH.md` is deliberately NOT @-referenced here — it is 1183 lines and carries the verbatim
current/replacement text for every edit site. Each task's `<read_first>` names the exact line ranges to read,
so no task loads more of it than it needs.
</context>

<verified_at_plan_time>
Every offset and count below was read from live HEAD on 2026-07-30, not copied from research:

- C17 baseline: `232 files checked, 0 with violations, 0 total violations`, exit 0.
- `docs/_registry/RE-index.md` registry rows: **223** — next ID is **RE-224**. `build-filename-map.mjs --self-test`: 8 passed / 0 failed.
- `check-phase-114`, `-120`, `-129`, `-130` all exit 0. `check-nav-hub-links`: 0 outbound / 0 inbound failures.
- `check-phase-54` exits 0 (its `V-54-21` assertion is a NEGATIVE on `05-compliance-policy.md`, which none of these edits introduces — research §5.A).
- EEE-SOP edit sites confirmed at the researched offsets: site 1 at `:462`, site 2 spanning `:496-498`, site 3 spanning `:538-539`, `## Version History` H2 at `:609` with its `| Date | Change |` header at `:611` and three dated rows at `:614-616`.
- **CORRECTION to research §6.F:** the DO-NOT-TOUCH needle for `:415` wraps across two physical lines — the greppable single-line substring is `lands verbatim as garbage in the citation` (WITHOUT the trailing word `body`). Research's longer needle returns 0 matches and would read as a false regression.
- **CORRECTION to the expected pre-edit count in research §4.A:** the negative-grep set matches **4 physical lines** (462, 497, 498, 539) representing **3 claim sites**, because site 2's sentence spans two lines that each match a different alternative. Post-edit the count is 0.
- All four D8.2 append sites confirmed present with the exact tails research §5.B quotes. `CORRECTED 2026-07-29` does not yet appear anywhere in `.planning/research/` — this plan tags its appends `CORRECTED 2026-07-30`, the day it lands.
- `research/FEATURES.md:44` confirmed: it is the `BreakoutSequence` table row, and the hyphenated GPO name sits near the END of that long row, inside the final cell. Research Concern #6 is correct — that line's security-screen clause is already the corrected, Ctrl+Alt+Del-scoped form.
- pandoc on PATH reports `3.7.0.2` (the `convert.ps1` pin), so the Plan 135-02 guard-docx run will not be a vacuous pass.
</verified_at_plan_time>

<tasks>

<task type="auto">
  <name>Task 1: HYG-05 — correct the fenced-content rationale at all three sites and append the v1.19 Version-History row</name>
  <files>docs/_standards/EEE-SOP-standard.md</files>
  <read_first>
    - `docs/_standards/EEE-SOP-standard.md` — the file being edited. Read the three edit regions and the Version History region: lines 455-470 (site 1, STD-05 section intro), 490-505 (site 2, D-03 case-boundary rule), 530-545 (site 3, D-07 worked-example preamble), 605-620 (`## Version History`). Also read 405-420 and 250-296 so you can see the two DO-NOT-TOUCH sites you must leave alone.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **672-817** — §4 in full. §4.B, §4.C and §4.D give the exact current text and the exact proposed replacement block for each of the three sites (line counts preserved, wrap width ~95 chars). §4.E is the sites-NOT-to-touch table. §4.F is the per-validator needle clearance. §4.G is the Version-History row. §4.H is the extrapolation to log.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-CONTEXT.md` decisions **D7.1 through D7.6** (lines 144-151) — the governing contract, including A-3 (the third site is additive) and D7.2's flag that HYG-05's own mandated wording is itself an unfalsified extrapolation.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-PATTERNS.md` lines **250-278** — the `## Version History` table shape (exact 2-column header, existing row wording) and the three HYG-05 target sites as quoted from the corpus.
  </read_first>
  <action>
    Edit `docs/_standards/EEE-SOP-standard.md` at exactly three rationale sites plus one table append. Replace each site's block with RESEARCH §4.B / §4.C / §4.D's **proposed replacement** block verbatim — those blocks are line-count-preserving and wrap-width-matched, so no surrounding line reflows.

    - **Site 1** (STD-05 section intro, the parenthetical after `no-key-info-in-code-fences rule`, ~:461-463): swap the parenthetical's causal clause for the true mechanism per RESEARCH §4.B. Keep the trailing `— see Grounding Notes above` pointer: D7.4 rules it becomes a correct citation once the proposition is true.
    - **Site 2** (D-03 case-boundary rule, ~:494-500): this is the site that carries D-03's normative force. Its normative sentence — the one beginning `Decision content is never placed inside a code fence` — must survive **byte-identical**. Rewrite only the causal clause after the em dash, per RESEARCH §4.C. The rule must survive on the new, true rationale (poor retrievability of non-prose runs), never weakened. RESEARCH §4.C notes a `Copilot` spelling typo was already corrected in its draft block — verify that spelling once in the block you ship.
    - **Site 3** (D-07 worked-example preamble, ~:536-539) — **the ADDITIVE edit, per D7.1 / A-3**: rewrite the final clause per RESEARCH §4.D. This is the recipe-specific instance of the false mechanism, i.e. the one governing exactly the file class this phase creates. ROADMAP SC6 does not name it, which is precisely why it is a `must_haves` entry above.
    - **`## Version History`**: append ONE new row after the existing `2026-07-17` row, dated **2026-07-30**, in the existing 2-column `| Date | Change |` shape, naming v1.19 / HYG-05, the three sites by their section names (STD-05 section intro, D-03 case-boundary rule, D-07 worked-example preamble), the corrected mechanism, and the facts that the normative D-03/D-04 rules are unchanged in force and the Grounding Notes pointers are retained. Model the wording on RESEARCH §4.G. Write the row so it **describes** the removed rationale without reproducing any of the removed strings — the corpus-wide negative grep in `<acceptance_criteria>` runs over this same file, so quoting a removed string in the row would fail the task.

    Touch NOTHING else in this file. Specifically: do not edit STD-04 D-01's mechanism sentence (~:415 — D7.3; editing it re-opens the ratified v1.16 Mermaid policy), do not edit the Grounding Notes (~:252-296, including the metadata claim at ~:268 — D7.4; that claim is TRUE and is what makes the corrected proposition true), do not alter the existing `2026-07-17` Version-History row (check-phase-129's D-02 regex is satisfied by it — D7.6), and do not bump STD-001's `last_verified` (Phase 129 D-12 governs — D7.6). Do not add a `## Rollback/Recovery` slot to `docs/_templates/recipe-template.md`: D2.1 rules divergence, not amendment, and the reason is the recipes-01/02 non-conformance ripple, not a harness block.

    Record in this plan's SUMMARY, per D7.2/D8.3 (each correction needs a carrier or it evaporates): (a) the shipped `retrieve poorly` clause is an **unfalsified extrapolation** — `PIPE-02-FINDINGS.md` has zero occurrences of `fence`, `code block`, `SourceCode` or `VerbatimChar` and never tested retrieval of fenced content; ship the locked wording, log the gap; (b) D7.5's durable in-repo proof, correctly qualified — `scripts/pipeline/guard-docx.mjs:286` builds a fenced fixture and proves fenced strings reach `.docx` body text, but say "runnable wherever pandoc is on PATH" because `:234-236` vacuous-passes when pandoc is absent, and name the run styles as `SourceCode` plus `NormalTok`/`KeywordTok`/`StringTok`/`OtherTok` with `VerbatimChar` at zero (that is pandoc's *inline* code-span style, which appears for the OMA-URI span, never for a fence); (c) research Concern #5 — a fourth occurrence of the false mechanism exists in an archived v1.18 research artifact under `.planning/milestones/v1.18-phases/`, which is deliberately NOT edited, so a successor's grep does not read as a regression; (d) both `v1.19-DEFERRED-CLEANUP.md` candidates this phase generates: the extrapolation above and the C17-vs-pipeline fence-mask divergence (`c17-eee-contract.mjs` column-0 anchored vs `convert.ps1:108` leading-whitespace-tolerant), per REQUIREMENTS.md:68's own instruction.
  </action>
  <acceptance_criteria>
    - `grep -cE 'invisible to the Copilot|invisible to the retrieval body text|silently hide the decision from retrieval|silently remove the decision' docs/_standards/EEE-SOP-standard.md` returns **0** (pre-edit it returns 4 physical lines across the 3 claim sites).
    - The same grep run over all of `docs/` returns **0** — no other shipped file carries the false mechanism.
    - `grep -c 'Decision content is never placed inside a code fence in a live recipe' docs/_standards/EEE-SOP-standard.md` returns **1** — D-03's normative sentence survives byte-identical (ROADMAP SC6's "normative D-03/D-04 rules untouched").
    - `grep -c 'lands verbatim as garbage in the citation' docs/_standards/EEE-SOP-standard.md` returns **1** — the `:415` DO-NOT-TOUCH site survives (note: this needle deliberately omits the trailing word `body`, which wraps to the next physical line).
    - `grep -c 'metadata is invisible to Copilot at query time' docs/_standards/EEE-SOP-standard.md` returns **1** — the `:268` Grounding-Notes DO-NOT-TOUCH site survives.
    - `grep -c '^| 2026-' docs/_standards/EEE-SOP-standard.md` returns **4** (was 3), and `grep -c '^| 2026-07-17 | v1.18 STD-05' docs/_standards/EEE-SOP-standard.md` returns **1** — the new row is appended and the v1.18 row is unaltered.
    - `grep -c '^last_verified: 2026-07-04' docs/_standards/EEE-SOP-standard.md` is unchanged from baseline — STD-001's `last_verified` is not bumped. (Confirm the baseline value by reading the frontmatter before editing; the assertion is "unchanged", not a specific date.)
    - `git status --porcelain docs/_templates/recipe-template.md` returns empty — the template gains no `## Rollback/Recovery` slot.
    - `node scripts/validation/check-phase-114.mjs`, `check-phase-120.mjs` and `check-phase-129.mjs` each exit 0.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 and reports `232 files checked, 0 with violations` (still 232 at this task — the recipe shell is Task 3).
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && f=docs/_standards/EEE-SOP-standard.md && [ "$(grep -rcE 'invisible to the Copilot|invisible to the retrieval body text|silently hide the decision from retrieval|silently remove the decision' docs/ | grep -v ':0$' | wc -l)" = "0" ] && grep -q 'Decision content is never placed inside a code fence in a live recipe' "$f" && grep -q 'lands verbatim as garbage in the citation' "$f" && grep -q 'metadata is invisible to Copilot at query time' "$f" && [ "$(grep -c '^| 2026-' "$f")" = "4" ] && grep -q '^| 2026-07-17 | v1.18 STD-05' "$f" && [ -z "$(git status --porcelain docs/_templates/recipe-template.md)" ] && node scripts/validation/check-phase-114.mjs >/dev/null 2>&1 && node scripts/validation/check-phase-120.mjs >/dev/null 2>&1 && node scripts/validation/check-phase-129.mjs >/dev/null 2>&1 && node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -q '232 files checked, 0 with violations' && echo PASS || echo CHECK</automated>
  </verify>
  <done>All three HYG-05 sites carry the true mechanism; D-03's normative sentence and both DO-NOT-TOUCH sites are byte-intact; the corpus-wide count of the false mechanism in docs/ is zero; one dated v1.19 row is appended and the v1.18 row is unaltered; STD-001's last_verified is unbumped; recipe-template.md is untouched; the three EEE-SOP-reading validators and C17 (232/0) are green; the extrapolation, the qualified guard-docx proof, the archived fourth occurrence and both DEFERRED-CLEANUP candidates are recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: D8.2 Operational-precondition appends + the three named requirement/research corrections</name>
  <files>.planning/research/STACK.md, .planning/research/FEATURES.md, .planning/research/SUMMARY.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **838-862** — §5.B in full: the exact current text of all four D8.2 append sites with their line-ending tails, the precise one-sentence precondition to append, which cell to append inside, the single-token fix at `FEATURES.md:44`, and the explicit list of sites left textually intact.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **473-518** — §3.C: the verbatim first-party enable procedure (Event Viewer path plus the right-click action), the disabled-by-default statement, the one-time-events caveat with its own H2, and Event ID 31000. This is the source of the appended sentence's content.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **1027-1053** — `## Concerns`, specifically **#1** (the HIGH-severity unsourced clause plus its fully-sourced minimal-fix rewording, which is the replacement text for the REQUIREMENTS.md correction), **#4** (the two Operational quotes sit under different H2s), and **#6** (the MEDIUM finding that following T-5 verbatim at `FEATURES.md:44` would cause a WRONG EDIT).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-CONTEXT.md` — **T-5 as amended** (lines 38-40), **D3.1** (lines 88-92), **D8.2/D8.3** (lines 160-161), and the numbered **Corrections** list (lines 163-169). T-5's original "both defects at both sites" instruction is AMENDED: at `FEATURES.md:44` only the hyphenated GPO name is a defect.
    - `.planning/research/STACK.md` lines 10-20 and 68-76 — the two append sites in context, each already carrying a prior `CORRECTED 2026-07-25 (adversarial review)` tag, which is the house style to match.
    - `.planning/research/FEATURES.md` lines 40-48 and 165-172; `.planning/research/SUMMARY.md` lines 50-58 — the remaining append sites.
    - `.planning/REQUIREMENTS.md` lines 14-19 — KIOSK-02 (the namespace-alias paraphrase) and KIOSK-04 (the un-blockable-shortcuts clause and the hyphenated GPO name).
  </read_first>
  <action>
    Four appends, one token fix, two requirement corrections. All in one commit, tagged `CORRECTED 2026-07-30`.

    **The four D8.2 appends** — under D3.1's augment-not-substitute disposition this is an append, never a rewrite. Append the one-sentence precondition from RESEARCH §5.B to each site: the channel is disabled by default and must be enabled via the Event Viewer path in RESEARCH §3.C (right-click the channel, then Enable Log) **before** the first kiosk sign-in, because for some failures events are captured only once.
    - `.planning/research/STACK.md:16` — append at end of line.
    - `.planning/research/STACK.md:72` — **the critical one.** KIOSK-01 and the ROADMAP Hard constraints point the phase at `STACK.md:13-16` as the discharged mechanism gate, so this row is what the planner and researcher actually read. It is a table row: append **inside the final cell**, before the closing pipe.
    - `.planning/research/FEATURES.md:169` — append at end of line. The line already carries the full Event Viewer path, so append only the precondition, not a second copy of the path.
    - `.planning/research/SUMMARY.md:55` — table row; append **inside the final cell**, before the closing pipe.

    Leave `ROADMAP.md:64`, `REQUIREMENTS.md:18`, `REQUIREMENTS.md:93` and `STATE.md:58` **textually intact** — D8.2 rules them correct as written once the precondition exists. Do not edit them.

    **`research/FEATURES.md:44`** — a single-token fix and nothing else. That line is the `BreakoutSequence` table row; near the end of its final cell the GPO name is written with a hyphen, which reads as a nonexistent PowerShell cmdlet. Correct it to the unhyphenated first-party form `Remove Logoff` (confirmed verbatim on `assigned-access/policy-settings` under `Ctrl+Alt+Del Options`, RESEARCH §3.A). **Do NOT touch the security-screen clause on that line** — research Concern #6 establishes it already scopes the security screen to Ctrl+Alt+Del alone, i.e. the corrected form; editing it would be a wrong edit. The `<acceptance_criteria>` asserts that clause still present.

    **`REQUIREMENTS.md` corrections (items 1, 2, 4):**
    - `:17` (KIOSK-04): replace the un-blockable-shortcuts clause with Concern #1's fully-sourced rewording — the four shortcuts are not blocked for a restricted-user-experience account; Ctrl+Alt+Del is the documented way to exit the kiosk experience; and the profile automatically applies the `Ctrl+Alt+Del Options` policies `Remove Logoff`, `Remove Task Manager` and `Remove Change Password`, which strip those affordances. Every clause of that rewording is verbatim-sourced. This single edit discharges Corrections items 1 and 2 at this site: the unsourced security-screen mechanism goes, and the GPO name lands unhyphenated.
    - `:15` (KIOSK-02): correct the namespace-alias paraphrase. The 2017 namespace is the **unprefixed root** `xmlns`, and Learn's published version table labels its alias cell `default` — but `default` is an XSD-internal XPath prefix, never writable in an instance document (T-7, confirmed in RESEARCH §1.C). Keep the row's other locked content — `StartLayout` lives in the 2017 root namespace and not in v4 — intact.

    Both REQUIREMENTS.md edits are verified safe: the three validators reading that file assert content strings that none of these edits disturbs, and `check-phase-54`'s `V-54-21` is a NEGATIVE on a filename none of them introduces (RESEARCH §5.A). The precedent for correcting planning artifacts in flight in this milestone is commit `457adc25`.

    Per D8.3, record every correction's carrier in the SUMMARY: Corrections items 1, 2 and 4 are discharged by the edits above; item 3 (the Operational channel) is discharged by the four appends plus D3.1's reframing that ROADMAP SC5 and KIOSK-05 are **incomplete, not wrong** — the named correction is the missing enable-precondition, so no verification override is needed and Phase 138's close-gate flip stays truthful; item 5 (HYG-05's own extrapolated clause) is carried by Task 1's SUMMARY entry.
  </action>
  <acceptance_criteria>
    - `grep -c 'CORRECTED 2026-07-30' .planning/research/STACK.md` returns **2**; the same grep returns at least **1** for each of `.planning/research/FEATURES.md` and `.planning/research/SUMMARY.md`.
    - Each of the four appended sentences names the enable action and the timing: `grep -c 'Enable Log' .planning/research/STACK.md` is at least 1, and each of the four edited lines contains a phrase placing the enable step before the first kiosk sign-in.
    - `.planning/research/STACK.md:72` and `.planning/research/SUMMARY.md:55` are still well-formed single-line table rows — the appended text sits INSIDE the final cell, so neither line gains or loses a `|` count versus baseline. Record the baseline pipe count per line before editing and assert it unchanged.
    - `grep -c 'Remove-Logoff' .planning/research/FEATURES.md` returns **0**; `grep -c 'Remove Logoff' .planning/research/FEATURES.md` returns at least **1**.
    - `grep -c 'reaches the security screen with the sign-out affordance stripped' .planning/research/FEATURES.md` returns **1** — that clause is UNCHANGED (research Concern #6's wrong-edit guard).
    - `grep -c 'Remove-Logoff' .planning/REQUIREMENTS.md` returns **0** and `grep -c 'reach the security screen but' .planning/REQUIREMENTS.md` returns **0** — the unsourced mechanism at `:17` is gone.
    - `.planning/REQUIREMENTS.md` line 17 contains `Remove Task Manager` and `Remove Change Password` — the sourced rewording landed, not a bare deletion.
    - `.planning/REQUIREMENTS.md` line 15 no longer contains `base 2017`, and contains `default` described as non-writable; the locked `StartLayout` clause on that line is still present (`grep -c 'StartLayout' .planning/REQUIREMENTS.md` is at least 1).
    - `grep -c '05-compliance-policy' .planning/REQUIREMENTS.md` returns **0** and `node scripts/validation/check-phase-54.mjs` exits 0 — the `V-54-21` negative assertion is undisturbed.
    - `git status --porcelain .planning/ROADMAP.md .planning/STATE.md` shows no change from this task — D8.2 leaves those textually intact. (If a later orchestrator step edits ROADMAP's plan list, that is outside this task's diff.)
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && [ "$(grep -c 'CORRECTED 2026-07-30' .planning/research/STACK.md)" = "2" ] && grep -q 'CORRECTED 2026-07-30' .planning/research/FEATURES.md && grep -q 'CORRECTED 2026-07-30' .planning/research/SUMMARY.md && grep -q 'Enable Log' .planning/research/STACK.md && [ "$(grep -c 'Remove-Logoff' .planning/research/FEATURES.md)" = "0" ] && grep -q 'Remove Logoff' .planning/research/FEATURES.md && [ "$(grep -c 'reaches the security screen with the sign-out affordance stripped' .planning/research/FEATURES.md)" = "1" ] && [ "$(grep -c 'Remove-Logoff' .planning/REQUIREMENTS.md)" = "0" ] && [ "$(grep -c 'reach the security screen but' .planning/REQUIREMENTS.md)" = "0" ] && grep -q 'Remove Task Manager' .planning/REQUIREMENTS.md && grep -q 'Remove Change Password' .planning/REQUIREMENTS.md && [ "$(grep -c 'base 2017' .planning/REQUIREMENTS.md)" = "0" ] && [ "$(grep -c '05-compliance-policy' .planning/REQUIREMENTS.md)" = "0" ] && node scripts/validation/check-phase-54.mjs >/dev/null 2>&1 && echo PASS || echo CHECK</automated>
  </verify>
  <done>All four D8.2 sites carry the enable-before-first-kiosk-sign-in precondition tagged CORRECTED 2026-07-30, with both table-row appends inside their final cells and pipe counts unchanged; FEATURES.md:44 carries the unhyphenated GPO name with its security-screen clause untouched; REQUIREMENTS.md:17 carries the fully-sourced rewording and REQUIREMENTS.md:15 the corrected namespace-alias statement; check-phase-54 green; ROADMAP.md and STATE.md textually intact; every correction's carrier recorded in the SUMMARY per D8.3.</done>
</task>

<task type="auto">
  <name>Task 3: Create the RE-224 file identity — sentinel-free frontmatter, EEE block, H1, Summary — and prove C17 goes 232 to 233 at 0 violations</name>
  <files>docs/recipes/03-windows-11-multi-app-kiosk.md</files>
  <read_first>
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-RESEARCH.md` lines **864-933** — §5.C (the verified `doc_id: RE-224` derivation, the recommended slug with its rationale, the concrete filled frontmatter block, the exact EEE header block line, and the per-value verification behind each) and §5.E (all 13 C17 assertions with the pre-emption for each, plus the column-0 fence-mask rule).
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-CONTEXT.md` **D5.6 and D5.7** (lines 129-130) — rule the file identity HERE, and strip the sentinel BEFORE measuring anything.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-PATTERNS.md` lines **23-49** and **279-298** — recipe 01's real frontmatter and EEE header block shape, copied exactly, plus the shared frontmatter pattern with RE-224's values already substituted.
    - `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-PATTERNS.md` lines **66-74** — the `## Summary` end-state statement idiom (Phase-129 D-13): one concrete end-state sentence, then platform and required admin role, minimum 30 words.
    - `docs/recipes/01-shared-windows-avd-client.md` lines 1-24 — the shipped analog: frontmatter, EEE block line, H1 and Summary as actually published. Read-only; **zero edits to this file** (`check-phase-130.mjs:64,67` pins its Step 5a/5b headings as literal strings against live HEAD inside every apex chain).
    - `docs/_templates/recipe-template.md` — the skeleton's frontmatter block and its usage notes, including the closed 4-value `doc_type` enum (never introduce a `Recipe` value) and the `review_by = last_verified + 90 days` cadence.
  </read_first>
  <action>
    Create `docs/recipes/03-windows-11-multi-app-kiosk.md`. The slug is RULED here per D5.6 — `03-windows-11-multi-app-kiosk` matches the siblings' end-state shape, uses the exact phrase Microsoft and admins use so it drives a good Copilot citation title, and satisfies ROADMAP SC1's `docs/recipes/03-*.md` wildcard. Write exactly four things and stop:

    1. **Frontmatter** — RESEARCH §5.C's filled block: `doc_id: RE-224`, `status: Draft`, `owner: Intune Admin Lead`, `doc_type: Guide`, `platform: Windows`, `last_verified: 2026-07-30`, `review_by: 2026-10-28` (last_verified + 90 days), the `applies_to` line naming the restricted user experience via the AssignedAccess CSP `Configuration` node delivered by an Intune custom OMA-URI profile, and `audience: admin`. Real dates only — the authoring-scaffold sentinel date must not appear, and neither must the template's own sentinel marker <!-- planner-discipline-allow: TEMPLATE-SENTINEL --> (`TEMPLATE-SENTINEL`, which `recipe-template.md` carries and every live doc strips). This ordering is load-bearing per D5.7: the sentinel makes C17 skip `#9` and `#12`, so leaving it in place would void every blockquote measurement Plan 135-02 takes.
    2. **The EEE header block line**, one blank line after the frontmatter close: RESEARCH §5.C's exact line, with `Platform` and `Doc Type` as the first two fields (C17 `#7`), values byte-equal to frontmatter (C17 `#9`, live the moment the sentinel is gone), as a single inline paragraph and never a table (C17 `#6`).
    3. **One H1**, after the block line (C17 `#2`), a concrete device end-state and not a bare doc-ID pattern (C17 `#3`): `# Windows 11 Multi-App Kiosk: Assigned Access Provisioning`. RULED here so Plan 135-02 does not re-decide it.
    4. **`## Summary`** as the first and only H2 in the file (C17 `#4`), at least 30 words (C17 `#5`), following the Phase-129 D-13 idiom: one concrete end-state sentence naming the restricted user experience delivered through the AssignedAccess CSP `Configuration` node via an Intune custom OMA-URI profile, then the platform floor and the required admin role.

    Stop there. The Scope banner, Prerequisites, anti-feature table, Steps, payload fence, Verification, Rollback/Recovery, Configuration-Caused Failures and See Also are all Plan 135-02's work. Write no Mermaid and no code fence in this task.

    Then run the gates. `status: Draft` is correct and stays Draft — Phase 137 flips it to Approved as a two-site edit (frontmatter plus the block line), per D5.6. Do NOT touch `docs/_registry/RE-index.md`: `build-filename-map.mjs --self-test` sub-test (c) asserts 223 registry **rows** (not files), so creating this file does not trip the canary, and the 223-to-225 bump is CLASS-05's named Phase-137 deliverable. Add no navigation wiring — no `docs/index.md` edit, no `RE-index.md` row, no filename-map regeneration; that is Phase 137, navigation-last.

    Record in the SUMMARY: the ruled slug and H1 (so Plan 135-02 and Phase 137 inherit rather than re-derive them), the assigned `doc_id: RE-224` with its Phase-137 registry-row and status-flip handoff, and the confirmed C17 file count.
  </action>
  <acceptance_criteria>
    - `docs/recipes/03-windows-11-multi-app-kiosk.md` exists.
    - `grep -c '^doc_id: RE-224$'` returns 1; `grep -c '^status: Draft$'` returns 1; `grep -c '^platform: Windows$'` returns 1; `grep -c '^doc_type: Guide$'` returns 1; `grep -c '^last_verified: 2026-07-30$'` returns 1; `grep -c '^review_by: 2026-10-28$'` returns 1.
    - `grep -c '1970-01-01'` returns **0** and `grep -c 'TEMPLATE-SENTINEL'` returns **0** — the sentinel is stripped, so C17 `#9` and `#12` are live on this file (D5.7).
    - `grep -c 'FILL-IN'` returns **0** — no template placeholder survives (C17 `#8`).
    - The EEE block line is present with all four fields: `grep -c '\*\*Doc ID:\*\* RE-224'` returns 1, `grep -c '\*\*Platform:\*\* Windows'` returns 1, `grep -c '\*\*Status:\*\* Draft'` returns 1. Exact field order and frontmatter equality are asserted authoritatively by C17 `#6`/`#7`/`#9` in the run below.
    - `grep -c '^# '` returns **1** and `grep -c '^## '` returns **1** — exactly one H1 and exactly one H2 (`## Summary`) at this stage.
    - `grep -c '^## Summary'` returns 1, and the Summary body is at least 30 words (C17 `#5` asserts this in the run below).
    - `grep -c '^\`\`\`'` returns **0** — no code fence exists yet; the single payload fence is Plan 135-02's deliverable.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 and reports exactly `233 files checked, 0 with violations`. A count of 232 means the file was not enrolled (missing `doc_id`, or not under `docs/`) — that is itself the useful signal.
    - `node scripts/validation/c17-eee-contract.mjs --self-test` exits 0 with `0 failed`.
    - `grep -cE '^\|[[:space:]]*RE-[0-9]+[[:space:]]*\|' docs/_registry/RE-index.md` returns **223** and `node scripts/pipeline/build-filename-map.mjs --self-test` exits 0 with `0 failed` — the registry is untouched and the drift canary has not moved.
    - `git status --porcelain docs/index.md docs/_registry/RE-index.md docs/recipes/01-shared-windows-avd-client.md scripts/validation/c17-eee-contract.mjs` returns empty — no navigation wiring, no registry edit, no edit to recipe 01, no validator edit.
    - `node scripts/validation/check-phase-130.mjs` exits 0 — the zero-edits-to-recipe-01 tripwire.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && f=docs/recipes/03-windows-11-multi-app-kiosk.md && test -f "$f" && grep -q '^doc_id: RE-224$' "$f" && grep -q '^status: Draft$' "$f" && grep -q '^platform: Windows$' "$f" && grep -q '^doc_type: Guide$' "$f" && [ "$(grep -c '1970-01-01' "$f")" = "0" ] && [ "$(grep -c 'TEMPLATE-SENTINEL' "$f")" = "0" ] && [ "$(grep -c 'FILL-IN' "$f")" = "0" ] && grep -q '\*\*Doc ID:\*\* RE-224' "$f" && [ "$(grep -c '^# ' "$f")" = "1" ] && [ "$(grep -c '^## ' "$f")" = "1" ] && grep -q '^## Summary' "$f" && [ "$(grep -c '^```' "$f")" = "0" ] && node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -q '233 files checked, 0 with violations' && node scripts/validation/c17-eee-contract.mjs --self-test 2>&1 | grep -q '0 failed' && [ "$(grep -cE '^\|[[:space:]]*RE-[0-9]+[[:space:]]*\|' docs/_registry/RE-index.md)" = "223" ] && node scripts/pipeline/build-filename-map.mjs --self-test 2>&1 | grep -q '0 failed' && [ -z "$(git status --porcelain docs/index.md docs/_registry/RE-index.md docs/recipes/01-shared-windows-avd-client.md scripts/validation/c17-eee-contract.mjs)" ] && node scripts/validation/check-phase-130.mjs >/dev/null 2>&1 && echo PASS || echo CHECK</automated>
  </verify>
  <done>The RE-224 shell exists at the ruled slug with sentinel-free real frontmatter, the exact EEE block line, one concrete-end-state H1 and a 30-plus-word Summary; C17 reports 233 files / 0 violations and self-test 0 failed, proving the file is enrolled and green with C17 #9/#12 live before any body content exists; the registry, the drift canary, docs/index.md, recipe 01 and every validator are untouched; check-phase-130 green; the ruled slug, H1 and doc_id handoff are recorded in the SUMMARY.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 135-01)

**New files**

| Path | What it contains at the end of this plan |
|---|---|
| `docs/recipes/03-windows-11-multi-app-kiosk.md` | Frontmatter (`doc_id: RE-224`, `status: Draft`, `owner: Intune Admin Lead`, `doc_type: Guide`, `platform: Windows`, `last_verified: 2026-07-30`, `review_by: 2026-10-28`, `applies_to`, `audience: admin`); the EEE header block line; `# Windows 11 Multi-App Kiosk: Assigned Access Provisioning`; `## Summary`. Nothing else — the body is Plan 135-02. |

**New named sections / headings created**

- `docs/recipes/03-windows-11-multi-app-kiosk.md` → `# Windows 11 Multi-App Kiosk: Assigned Access Provisioning` (H1) and `## Summary` (H2).
- `docs/_standards/EEE-SOP-standard.md` → one new dated data row in the existing `## Version History` table (no new heading).

**Modified files and the exact sites touched**

| Path | Sites |
|---|---|
| `docs/_standards/EEE-SOP-standard.md` | STD-05 section-intro rationale clause (~:462); D-03 case-boundary rationale clause (~:496-498); D-07 worked-example-preamble clause (~:538-539); one appended `## Version History` row dated 2026-07-30 |
| `.planning/research/STACK.md` | `:16` end-of-line append; `:72` append inside the final table cell |
| `.planning/research/FEATURES.md` | `:44` single-token GPO-name fix; `:169` end-of-line append |
| `.planning/research/SUMMARY.md` | `:55` append inside the final table cell |
| `.planning/REQUIREMENTS.md` | `:15` namespace-alias correction; `:17` un-blockable-shortcuts rewording plus GPO-name fix |

**Deliberately NOT produced in this phase** (Phase 137, navigation-last): no `docs/_registry/RE-index.md` row, no `docs/index.md` table row or quick-nav bullet, no `filename-map.md` regeneration, no 223-to-225 canary bump, no `Status: Approved` flip.
</artifacts_this_phase_produces>

<threat_model>
Documentation-only plan. No executable surface is added, no data flow, no trust boundary crossed, no runtime code, no auth/session/crypto, and no npm/pip/cargo installs — the **Package Legitimacy Gate does not fire**, so no `[ASSUMED]`/`[SUS]` install checkpoint is required. Deliverables are static Markdown in a C17-validated corpus plus in-flight corrections to planning artifacts. Follows the Phase-130 precedent (`130-01-PLAN.md:111`).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author to validated corpus | Markdown edits cross into a surface that frozen validators and predecessor milestone audits read; the only integrity risk is corpus/validator tampering, not input handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-135-01 | Tampering | `scripts/validation/*` (C17 + check-phase validators, subprocess-spawned by every milestone audit — an edit retroactively mutates predecessor audits) | high | mitigate | Zero validator edits. Task 3's `<acceptance_criteria>` asserts `git status --porcelain scripts/validation/c17-eee-contract.mjs` is empty; `check-phase-114/120/129/130` are each run to exit 0 rather than assumed |
| T-135-02 | Tampering | `docs/recipes/01-shared-windows-avd-client.md` (its Step 5a/5b headings are literal-string assertions against live HEAD inside every apex chain) | high | mitigate | Read-only in every task. `git status --porcelain` on the file plus a `check-phase-130` exit-0 run are both acceptance criteria |
| T-135-03 | Tampering | `docs/_registry/RE-index.md` (an out-of-turn row breaks CLASS-05's locked 223-to-225 canary in Phase 137) | medium | mitigate | Zero registry edits; the 223-row count and `build-filename-map.mjs --self-test` are asserted directly |
| T-135-04 | Repudiation | corrections with no carrier (D8.3 — unanchored corrections evaporate) | low | mitigate | Every correction is bound to a `must_haves` truth, an acceptance criterion, or a named SUMMARY / `v1.19-DEFERRED-CLEANUP.md` entry, per D8.3 |
| T-135-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan; the gate does not fire |
</threat_model>

<verification>
- Corpus-wide grep over `docs/` for the false fenced-content mechanism returns zero; D-03's normative sentence and both DO-NOT-TOUCH sites (`~:415`, `~:268`) are byte-intact; the third, ADDITIVE site (`~:538-539`) is verified explicitly because ROADMAP SC6 does not name it (D7.1 / A-3).
- `## Version History` carries exactly 4 dated rows with the `2026-07-17` row unaltered; STD-001's `last_verified` is unbumped.
- All four D8.2 append sites carry the enable-before-first-kiosk-sign-in precondition tagged `CORRECTED 2026-07-30`; both table-row appends sit inside their final cells with unchanged pipe counts.
- `research/FEATURES.md:44` carries the unhyphenated GPO name with its Ctrl+Alt+Del-scoped security-screen clause unchanged (research Concern #6's wrong-edit guard); `REQUIREMENTS.md:17` and `:15` carry the sourced rewording and the corrected namespace-alias statement.
- `docs/recipes/03-windows-11-multi-app-kiosk.md` exists with sentinel-free real frontmatter; C17 reports `233 files checked, 0 with violations` and self-test `0 failed`.
- `check-phase-54`, `-114`, `-120`, `-129` and `-130` all exit 0. `build-filename-map.mjs --self-test` still asserts 223 registry rows.
- `git status --porcelain` proves zero edits to recipe 01, `docs/index.md`, `docs/_registry/RE-index.md`, `docs/_templates/recipe-template.md` and any validator.
</verification>

<success_criteria>
HYG-05 is corrected at all three sites — including the additive third site that ROADMAP SC6 does not name — with D-03's normative force and both DO-NOT-TOUCH sites intact and a dated v1.19 Version-History row appended; the four D8.2 Operational-precondition appends and the three named requirement/research corrections have landed with carriers recorded; and `docs/recipes/03-windows-11-multi-app-kiosk.md` exists as a sentinel-free, C17-enrolled, C17-green (233/0) shell — so the wave-2 dependency structurally guarantees D7.7 (the standard is corrected before the payload fence exists) and D5.7 (the sentinel is gone before any `#9`/`#12` measurement).
</success_criteria>

<output>
Create `.planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-01-SUMMARY.md` when done, recording: the ruled slug and H1 (Plan 135-02 and Phase 137 inherit, never re-derive); the assigned `doc_id: RE-224` and its Phase-137 registry-row / Draft-to-Approved two-site-flip handoff; the confirmed C17 file count (233/0); D7.2's unfalsified-extrapolation log and D7.5's correctly-qualified guard-docx proof; research Concern #5's archived fourth occurrence, deliberately not edited; the carrier for each of Corrections items 1-5 per D8.3; and both `v1.19-DEFERRED-CLEANUP.md` candidates (the `retrieve poorly` extrapolation and the C17-vs-`convert.ps1` fence-mask divergence).
</output>
