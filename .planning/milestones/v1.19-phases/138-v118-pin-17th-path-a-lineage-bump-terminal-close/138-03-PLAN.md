---
phase: 138-v118-pin-17th-path-a-lineage-bump-terminal-close
plan: 03
type: execute
wave: 3
depends_on: [138-02]
files_modified:
  - scripts/validation/check-phase-135.mjs
  - scripts/validation/check-phase-136.mjs
  - scripts/validation/check-phase-137.mjs
  - scripts/validation/check-phase-138.mjs
  - .github/workflows/audit-harness-v1.19-integrity.yml
autonomous: true
requirements: [HARN-15]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "check-phase-135/136/137 exist as lightweight leaf validators — empty CHAIN_PHASES, empty CHAIN_SKIP, no archive resolver, no audit-harness step — and each exits 0 against the live corpus"
    - "check-phase-137's needle is per-recipe and LINE-SCOPED: it extracts the single matching docs/index.md table-row line and the single quick-nav bullet line and tests each in isolation, never a whole-file substring search"
    - "check-phase-138 is the new apex with CHAIN_PHASES spanning 48..137 inclusive — 90 entries, independently derived, not copied forward — and 93 total checks (90 CHAIN + AUDIT + AUDIT-HARNESS + SELF)"
    - "[EDGE HARN-15/adjacency] The apex excludes itself: 138 is absent from CHAIN_PHASES, the chain terminates at 137, and V-138-SELF hard-fails if 138 ever appears in the chain array"
    - "[EDGE HARN-15/empty] CHAIN_SKIP is an empty Set in all four new validators, and V-138-SELF asserts CHAIN_SKIP.size === 0 as the second half of its dual invariant"
    - "[EDGE HARN-15/ordering] Chain-array integrity is guarded at module load by three throws — a duplicate-entry guard on the unique-value count, a length guard against 90, and a termini guard against 48 and 137 — so a duplicated, dropped or reordered interior entry aborts import rather than silently validating the wrong span"
    - "check-phase-138 fixes three latent hazards rather than inheriting them: an explicit output-buffer bound on the chain subprocess call, a corrected missing-child classifier that no longer converts module-load throws into green skips, and a non-existent chain child that produces a FAIL rather than a pass"
    - "check-phase-138 exits 0 standalone, non-nested, with the full 48..137 recursion, before anything is pushed"
    - "audit-harness-v1.19-integrity.yml exists as the 16th coexistence workflow with three leaf jobs plus the dual apex, restating the inherited dual-apex contract verbatim in its header, and its paths filter lists both new milestone documents and the live requirements document"
  prohibitions:
    - "CHAIN_SKIP must never gain entries to force a green result, and no frozen predecessor validator may be edited to silence a failure the new apex surfaces"
    - "No nested-guard short-circuit may be presented as evidence of a chain result; the guard is a performance short-circuit, never a drift-detection gate"
  assumptions:
    - "[EDGE HARN-15/unclassified - UNRESOLVED, flagged, not auto-backstopped] The edge probe returned `unclassified` for HARN-15 and could not name an edge category for a 17th-lineage-bump requirement. Planner assumption, surfaced for human review at verification: the load-bearing HARN-15 edges are the chain-array boundary (apex excludes itself), the empty-CHAIN_SKIP invariant, and chain-array interior de-duplication - all three are authored as explicit truths above. If verification finds a fourth HARN-15 edge this assumption missed, that is a gap in this plan, not in the requirement."
  artifacts:
    - path: "scripts/validation/check-phase-138.mjs"
      provides: "New chain apex for v1.19 — CHAIN[48..137], AUDIT, AUDIT-HARNESS, SELF"
      contains: "v1.19-phases"
    - path: "scripts/validation/check-phase-137.mjs"
      provides: "Leaf validator — line-scoped index.md co-presence + hubs-not-wired"
    - path: ".github/workflows/audit-harness-v1.19-integrity.yml"
      provides: "16th CI coexistence workflow"
  key_links:
    - from: "scripts/validation/check-phase-138.mjs"
      to: "scripts/validation/v1.19-milestone-audit.mjs"
      via: "the AUDIT-HARNESS subprocess spawn"
      pattern: "v1\\.19-milestone-audit"
    - from: "scripts/validation/check-phase-138.mjs"
      to: "resolveArchivedPhasePath"
      via: "the AUDIT check's milestone-root token"
      pattern: "v1\\.19-phases"
---

<objective>
HARN-15 (Atom 2) — author the four new chain validators (three leaves for phases 135, 136 and 137, plus the new apex for 138) and the 16th CI coexistence workflow, and prove the apex green standalone before anything leaves this machine.

Purpose: the validator-atom deferral means the content phases shipped no validators of their own; the close phase authors the whole block as one indivisible atom. The apex continues the span-to-the-phase-before invariant. Its two load-bearing numbers — the 90-entry chain and the 93 total checks — are derived here from arithmetic, never copied from the predecessor apex, and are enforced by module-load throws so an off-by-one aborts rather than false-greens. Three latent hazards in the predecessor apex are fixed in the new file rather than inherited a seventh time.
Output: four `check-phase-*.mjs` files plus `audit-harness-v1.19-integrity.yml`; the apex exits 0 standalone non-nested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-RESEARCH.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-PATTERNS.md
@scripts/validation/check-phase-132.mjs
@scripts/validation/check-phase-133.mjs
@scripts/validation/check-phase-134.mjs
@.github/workflows/audit-harness-v1.18-integrity.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the three leaf validators check-phase-135, -136 and -137</name>
  <files>scripts/validation/check-phase-135.mjs, scripts/validation/check-phase-136.mjs, scripts/validation/check-phase-137.mjs</files>
  <read_first>
    - scripts/validation/check-phase-132.mjs (the leaf template — the empty chain arrays at lines 33-35, the line-scoped co-presence checks at lines 74-104 that check-phase-137 mirrors, the SELF dual-invariant, and the runner-loop / label-padding / exit-code tail at lines 106-149 to copy byte-for-byte; also its header comment at lines 5-8 establishing that needles derive from a phase's own verification document)
    - scripts/validation/check-phase-133.mjs (the second leaf template, lines 81-124 — confirms the identical tail)
    - .planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-VERIFICATION.md (the Observable Truths table — the sole needle source for check-phase-135)
    - .planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-02-SUMMARY.md lines 150-164 (the Section 4 closure table — the sole needle source for check-phase-136)
    - .planning/phases/137-integration-navigation-last-close/137-02-SUMMARY.md lines 122-146 (the pre-specified needle-spec handoff for check-phase-137, including the false-match warning)
    - .planning/STATE.md line 349 (the same needle-spec restated with the literals, including the corrected hubs-not-wired literals)
  </read_first>
  <action>
    Author three lightweight leaf validators on the check-phase-132 shape. Each declares an empty chain array and an empty skip Set, carries no archive resolver, carries no audit-harness step, ends with a SELF check asserting its own phase number is absent from the chain array and the skip Set is empty, and reuses the runner-loop and exit-code tail byte-for-byte. Leaves never chain; chain logic lives only in the apex.

    Needles are transcribed from each phase's own shipped measurements, not re-derived from the recipe prose.

    For check-phase-135, source the Observable Truths table in that phase's verification document: the recipe file exists at 329 lines; the rollback-and-recovery H2 sits between the verification H2 and the configuration-caused-failures H2; the shared Windows AVD-client recipe file has zero edits; exactly one column-0 XML fence exists at the shipped line span; the namespace table has 3 rows at the shipped line span; the anti-feature table has 8 rows.

    For check-phase-136, source the Section 4 closure table in that phase's second summary: the recipe file is 301 lines; 8 H2 headings in fixed order; 6 step headings; 5 decision blocks; 5 admin-question lead-ins; 6 misconfiguration callouts; 9 anti-feature rows; 10 decomposition-table rows; 7 verification-checklist lines; 4 failures-table rows; exactly one JSON fence at column 0.

    For check-phase-137, the needle is fully pre-specified — copy the literals, do not compose them. The invariant is per-recipe and LINE-SCOPED. For each recipe, first assert that a line matching the anchored table-row pattern for that recipe exists in `docs/index.md`; second, extract the single line matching the anchored quick-nav bullet pattern and assert that that one extracted line contains the recipe's fixed prose fragment. Table targets are the literals `recipes/03-windows-11-multi-app-kiosk.md` and `recipes/04-android-dedicated-mhs-multi-app.md`. Bullet fragments are the literals `Windows 11 multi-app kiosk` and `Android Dedicated multi-app kiosk`. Add the hubs-not-wired assertion using the corrected literals `recipes/03-` and `recipes/04-` against the three troubleshooting hub documents — this leaf owns that assertion because the frozen predecessor leaf's own pattern does not cover these two recipes and must not be edited.

    A whole-file substring search is the specific trap here: searching the whole of `docs/index.md` for a bare word would false-match an unrelated line two lines above the bullet. Extract the single matching line and test that line, exactly as the predecessor leaf does.
  </action>
  <acceptance_criteria>
    - `for n in 135 136 137; do node scripts/validation/check-phase-$n.mjs || exit 1; done` exits 0 for all three.
    - `grep -L "resolveArchivedPhasePath" scripts/validation/check-phase-135.mjs scripts/validation/check-phase-136.mjs scripts/validation/check-phase-137.mjs` lists all three (no leaf carries the resolver).
    - Each of the three contains `const CHAIN_PHASES = [];` and `const CHAIN_SKIP = new Set([]);`.
    - Each of the three has a SELF check whose failure message names its own phase number, and which asserts the skip Set is empty.
    - `grep -c "recipes/03-windows-11-multi-app-kiosk.md" scripts/validation/check-phase-137.mjs` and `grep -c "recipes/04-android-dedicated-mhs-multi-app.md" scripts/validation/check-phase-137.mjs` are each at least 1.
    - `grep -c "Android Dedicated multi-app kiosk" scripts/validation/check-phase-137.mjs` is at least 1, and the surrounding code operates on a single extracted line — a mutation test confirms this: temporarily inserting that fragment into an unrelated line of `docs/index.md` while removing it from the quick-nav bullet must make check-phase-137 exit non-zero. Revert the mutation afterwards and record the observed exit codes in the SUMMARY.
    - `grep -Ec "recipes/0[34]-" scripts/validation/check-phase-137.mjs` is at least 2 (the hubs-not-wired literals present).
    - `git diff --name-only` shows zero modifications to any pre-existing `check-phase-*.mjs`.
  </acceptance_criteria>
  <verify>
    <automated>for n in 135 136 137; do node scripts/validation/check-phase-$n.mjs || exit 1; done; echo OK</automated>
  </verify>
  <done>Three needle-based leaf validators exist with empty chain and skip arrays and no resolver, each exits 0 against the live corpus, and check-phase-137's line-scoped invariant is proven by a reverted mutation test rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Author the apex check-phase-138 with derived numbers and three hazard fixes; prove it green standalone</name>
  <files>scripts/validation/check-phase-138.mjs</files>
  <read_first>
    - scripts/validation/check-phase-134.mjs (the structural template — the chain array at lines 75-79; the module-load dedup, length and termini throws at lines 87-98; the resolver and skip-pass shape at lines 110-120; the nesting guards at lines 134-136 and 172-174; the subprocess call with its timeouts at lines 141-150 and its missing output-buffer bound; the missing-child classifier at lines 155-157; the non-existent-child early return at lines 138-140; the SELF dual invariant at lines 189-206; and the header comment convention at lines 1-30)
    - scripts/validation/_lib/archive-path.mjs (the whole file — `resolveArchivedPhasePath` returns null without throwing and documents that the caller owns fail semantics; this file is byte-frozen this phase, import and reuse only)
    - scripts/validation/_lib/exec-fail-detail.mjs (the failure-output helper the apex imports; byte-frozen this phase)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md (D-29 for the two derived numbers and the retained invariants; D-30 for the three hazards to fix; D-19 for the archive-root token and why the pre-gate skip-pass stays; D-14 for the buffer bound and the timeout priorities; D-13 for the depth label)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-PATTERNS.md (section "check-phase-138.mjs" — the explicit do-not-copy-verbatim instruction with the three hazards quoted from the template)
  </read_first>
  <action>
    Take the predecessor apex as a structural template only — four check groups in order: the phase's own verification-document existence check, the recursive chain, the current-milestone harness run, and the self invariant.

    Derive the chain array by arithmetic, not by copying: the integers from 48 through 137 inclusive, which is 137 minus 48 plus 1, equal to 90 entries. Generate it rather than transcribing it. Update all three module-load throws to the new numbers: the duplicate guard compares the unique-value count against the array length; the length guard compares against 90; the termini guard compares the first and last entries against 48 and 137. These throw at import, before any check runs, so an off-by-one aborts with no summary line — that is the guard working, not a bug. Keep the skip Set empty; adding entries to force green is self-disqualifying.

    Total checks are 93: the 90 chain entries plus the audit check, the audit-harness check and the self check. Report the derived count from the verbose summary rather than asserting a hand-typed figure, and record it in the SUMMARY.

    In the audit check, pass the milestone-root token for this milestone's own phase root — the literal `['v1.19-phases']`. Never a predecessor-copied token; three earlier apexes carry a wrong one frozen and it must not be copied a fourth time. Before the close-gate exists this phase's verification document does not exist, so the resolver legitimately returns null and the check skip-passes; that is correct pre-gate behaviour and is what lets this task's exit-0 criterion hold. Add a reviewable inline comment at that call site documenting both halves: that the token is deliberately this milestone's own root, and that a null resolver result skip-passes legitimately before the close-gate but is asserted to be a real pass in the post-close-gate confirmatory run of Plan 138-06. Do not add a throw here.

    Point the audit-harness step at the 17th harness authored in the previous plan. The self check asserts 138 is absent from the chain array and the skip Set is empty.

    Now fix the three hazards in the new file rather than inheriting them.

    First, set an explicit output-buffer bound on the chain subprocess call. The template passes piped stdio with no bound, so the runtime's one-mebibyte default applies and a chatty child is silently converted into a caught failure that no hand sweep can reproduce. Choose a bound generous enough for the most verbose child and record the chosen value with its rationale in the file's header comment.

    Second, correct the missing-child classifier. The template treats a child whose error output merely contains certain generic phrases as a missing file, which converts a genuine module-load throw into a green skip. Restrict the classification to unambiguous signals — the file-not-found error code and the shell's command-not-found status — and let everything else fall through to a failure. A module-load throw is exactly what the new chain-array guards produce, so mis-classifying it would defeat this apex's own primary safeguard.

    Third, make a non-existent chain child a failure, not a pass. The template returns a passing skip when a chain child's file is absent, so a deleted predecessor validator is structurally undetectable. The array is a fixed, asserted span; a member of it that does not exist on disk is a regression.

    Copy the timeout budgets forward as they stand — the per-peer and per-subprocess figures — and note in the header comment that they are the sole coupling point with the deep-nest cold-clone timeout item, whose depth label this milestone advances to the 48-through-137 span. Do not chain the apex to itself; that is the exact self-reference the self check must hard-fail on.

    Write the header comment on the template's convention: document the chain span, the empty-skip-Set invariant, the nesting guard on both the chain loop and the harness step, the three hazard fixes with their reasons, and which frozen predecessor defects are deliberately not fixed here.

    Finally prove it: run the apex verbose with the nesting-guard environment variable unset, so it performs the full recursion, and confirm exit 0 with zero failures. Run it with no competing local load — a concurrent sweep has flaked a chain entry once before. Record the derived total-check count and the full summary line.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-138.mjs --verbose` exits 0 with the nesting-guard environment variable unset, and its summary reports 0 FAIL across the full 48-through-137 recursion.
    - The derived total check count reported by the verbose run is 93 and the chain portion is 90; both are recorded verbatim in the SUMMARY.
    - Importing the apex after temporarily truncating the chain array throws at module load with a message naming the expected length — verified by a reverted mutation, with the observed error recorded in the SUMMARY.
    - `grep -c "v1\.19-phases" scripts/validation/check-phase-138.mjs` is at least 1 and `grep -Ec "v1\.1[5-8]-phases" scripts/validation/check-phase-138.mjs` is 0.
    - `grep -c "maxBuffer" scripts/validation/check-phase-138.mjs` is at least 1, and the chosen value appears in the header comment with its rationale.
    - `grep -c "CHAIN_SKIP = new Set(\[\])" scripts/validation/check-phase-138.mjs` is 1, and the self check asserts the Set's size is zero.
    - `grep -c "'not found'" scripts/validation/check-phase-138.mjs` is 0 (the loose classifier phrase is not inherited).
    - A temporarily renamed chain child produces a failure from the apex, not a skip — verified by a reverted mutation, with the observed result recorded in the SUMMARY.
    - The reviewable inline comment near the audit call site covers both the token choice and the pre-gate skip-pass semantics: `grep -Eic "resolver|skip-pass" scripts/validation/check-phase-138.mjs` is at least 1.
    - `git diff --name-only` shows zero modifications to any pre-existing `check-phase-*.mjs`, to `_lib/archive-path.mjs`, to `_lib/exec-fail-detail.mjs`, or to `c17-eee-contract.mjs`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-138.mjs --verbose && grep -q "v1.19-phases" scripts/validation/check-phase-138.mjs && grep -q "maxBuffer" scripts/validation/check-phase-138.mjs && echo OK</automated>
  </verify>
  <done>The apex exists with a generated 90-entry chain spanning 48 to 137, three module-load guards proven by reverted mutation, 93 derived checks, this milestone's own archive-root token, an empty skip Set, and all three hazard fixes applied; it exits 0 standalone non-nested with no competing load.</done>
</task>

<task type="auto">
  <name>Task 3: Author the 16th CI coexistence workflow</name>
  <files>.github/workflows/audit-harness-v1.19-integrity.yml</files>
  <read_first>
    - .github/workflows/audit-harness-v1.18-integrity.yml (the full 237-line template — lines 11-14 the dual-apex contract to restate verbatim; lines 15-18 the no-ref checkout whose root cause stays deferred; lines 22-34 the trigger block, which carries no push trigger; lines 26-31 the planning-document paths set; lines 56-69 the path-match grep job; lines 102-114 the leaf-job skeleton to replicate; line 172 the standalone predecessor-apex job that must NOT be duplicated here)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md (D-08 part iii for why no predecessor-apex job belongs here; D-09 for the verbatim dual-apex restatement; D-23 for the mandatory paths entries; D-14 for copying the workflow-level timeout forward unchanged; D-31 for the corrected push semantics)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-RESEARCH.md ("Pattern 4: 16th CI workflow (job-set delta)" — the verified job-set delta and the note that a phase-specific header block is added only if a new deferred issue actually surfaces)
  </read_first>
  <action>
    Copy the predecessor integrity workflow wholesale and apply the version-string delta set. Change the workflow name, the four version-scoped entries in the paths filter, the parse job's sidecar target, the path-match job's grep pair (which must confirm the 17th harness references the 17th sidecar by filename), the harness-run job's node invocation, and the Linux chain job's apex filename and chain-range strings in both its display name and its timing notice.

    Job set: three leaf jobs for the three new leaf validators, each an exact copy of the template's leaf skeleton — Ubuntu runner, needing the harness-run job, a fifteen-minute timeout, no continue-on-error, checkout with full history, Node twenty, then the single node invocation. Plus one apex job for the new apex mirroring the template's apex block with its thirty-minute timeout. Copy that workflow-level timeout forward exactly as it stands; the Linux chain has run in about two seconds since the timeout item was remediated and the figure is not worth a plan step.

    Do NOT add a job for the predecessor apex. The predecessor workflow already carries the only standalone job for it, and that job is how the predecessor apex gets its cross-OS evidence; duplicating it here adds no coverage and breaks the convention that a workflow never re-runs a frozen predecessor's apex.

    Restate the inherited dual-apex contract verbatim in the header: the standalone apex job and the Linux chain job both run the full recursion; they are not to be deduplicated, and neither top-level invocation may carry the nesting-guard environment variable.

    The paths filter must include, alongside the version-scoped script globs and this workflow's own filename, all three planning-document paths the predecessor lists in its own version-scoped form: the live requirements document, this milestone's audit document under the milestones directory, and this milestone's deferred-cleanup document under the milestones directory. Omitting either milestone document silently narrows the trigger surface.

    Leave the trigger set as the template has it — pull-request with the paths filter, two schedule crons, and manual dispatch. No push trigger exists anywhere in this repository's workflows and none is added here; the manual-dispatch entry is what makes the owner's explicit dispatch in Plan 138-05 possible, and it requires this file to be present on the default branch.

    Leave the harness-run checkout without a ref exactly as the template has it — that deferred root cause is not fixed in this phase. Keep the advisory pin-helper job's continue-on-error setting.
  </action>
  <acceptance_criteria>
    - The workflow parses as valid YAML: `node -e "require('fs').readFileSync('.github/workflows/audit-harness-v1.19-integrity.yml','utf8')"` succeeds and a YAML lint or GitHub-side parse reports no error.
    - `grep -c "v1\.19-milestone-audit" .github/workflows/audit-harness-v1.19-integrity.yml` is at least 1 and `grep -c "v1\.19-audit-allowlist\.json" .github/workflows/audit-harness-v1.19-integrity.yml` is at least 1.
    - Job blocks exist for `check-phase-135`, `check-phase-136`, `check-phase-137` and `check-phase-138`; `grep -c "check-phase-134" .github/workflows/audit-harness-v1.19-integrity.yml` is 0.
    - `grep -c "CHECK_PHASE_NESTED" .github/workflows/audit-harness-v1.19-integrity.yml` is 0.
    - `grep -c "^on:" .github/workflows/audit-harness-v1.19-integrity.yml` is 1 and `grep -Ec "^\s+push:" .github/workflows/audit-harness-v1.19-integrity.yml` is 0; `grep -c "workflow_dispatch" .github/workflows/audit-harness-v1.19-integrity.yml` is 1.
    - The paths filter contains `.planning/REQUIREMENTS.md`, `.planning/milestones/v1.19-MILESTONE-AUDIT.md` and `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` — all three grep-confirmed.
    - The header's dual-apex paragraph is byte-identical to the predecessor's apart from the renumbered apex citation, confirmed by diffing the two header blocks.
    - `git diff --name-only .github/workflows/` lists only the new file; all 15 pre-existing workflows are untouched.
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/workflows/audit-harness-v1.19-integrity.yml && grep -q "v1.19-milestone-audit" .github/workflows/audit-harness-v1.19-integrity.yml && grep -q "v1.19-DEFERRED-CLEANUP.md" .github/workflows/audit-harness-v1.19-integrity.yml && test "$(grep -c 'check-phase-134' .github/workflows/audit-harness-v1.19-integrity.yml)" = "0" && echo OK</automated>
  </verify>
  <done>The 16th coexistence workflow exists with three leaf jobs plus the dual apex, no predecessor-apex job, the dual-apex contract restated verbatim, all three planning-document paths present, manual dispatch enabled, and every prior workflow byte-unchanged.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged planner assumptions (spec-less probe fallback)

One probe row could not be resolved and is surfaced here rather than dropped or auto-backstopped:

- **HARN-15 / `unclassified` — UNRESOLVED.** The probe returned no edge category. This plan assumes the load-bearing HARN-15 edges are (a) the chain-array boundary — does the apex include itself; (b) the empty-skip-Set invariant; (c) chain-array interior de-duplication. All three are authored as explicit `must_haves.truths` and enforced by module-load throws. Reviewer action: confirm at verification that no fourth HARN-15 edge exists. Never auto-dismissed.
</flagged_assumptions>

<artifacts_produced>
## Artifacts this phase produces

| Symbol / artifact | Kind | Owner plan |
|---|---|---|
| `MILESTONE_CLOSE_SHAS.V118` (key `'7af8a147'`) | new object key in `_lib/frozen-at-close.mjs` | 138-01 |
| `readAtV118Close` | new export | 138-01 |
| `scripts/validation/v1.19-milestone-audit.mjs` | new file — 17th Path-A harness (C1–C17) | 138-02 |
| `scripts/validation/v1.19-audit-allowlist.json` | new file — sidecar, 59 line-pins in 5 arrays | 138-02 |
| `BASELINE_23` comment block | append-only comment in `regenerate-supervision-pins.mjs` | 138-02 |
| `scripts/validation/check-phase-135.mjs` — `V-135-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-136.mjs` — `V-136-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-137.mjs` — `V-137-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-138.mjs` — `V-138-AUDIT`, `V-138-CHAIN-48`…`V-138-CHAIN-137`, `V-138-AUDIT-HARNESS`, `V-138-SELF` (93 checks) | new chain apex | 138-03 |
| `.github/workflows/audit-harness-v1.19-integrity.yml` — jobs `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`, `check-phase-135`, `check-phase-136`, `check-phase-137`, `check-phase-138`, `rotting-external-quarterly`, `pin-helper-advisory` | new file — 16th CI coexistence workflow | 138-03 |
| `dist/docs-library-v1.19.zip` | build artifact (gitignored) | 138-04 |
| `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` | new doc | 138-06 |
| `.planning/milestones/v1.19-MILESTONE-AUDIT.md` | new doc | 138-06 |
| `.planning/phases/138-.../138-VERIFICATION.md` | new doc — the apex AUDIT resolver target | 138-06 |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chain array → the span actually validated | A stale, duplicated or off-by-one array silently validates the wrong set of phases while reporting green |
| archive resolver token → the AUDIT check's verdict | A wrong milestone-root token resolves to a predecessor's phase directory and skip-passes forever, producing a permanent false green |
| chain subprocess boundary → PASS/FAIL/SKIP classification | Buffer limits and error-classification heuristics decide whether a child's real crash is reported as a failure or laundered into a green skip |
| CI workflow job set → cross-OS evidence | Duplicating a frozen predecessor's apex job, or narrowing the paths filter, changes what evidence the milestone can claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-10 | Tampering | `CHAIN_PHASES` span | high | mitigate | Array generated from arithmetic, not copied; three module-load throws (dedup, length 90, termini 48 and 137) proven by a reverted truncation mutation |
| T-138-11 | Spoofing | apex AUDIT archive-root token | high | mitigate | This milestone's own root token asserted present and every predecessor phase-root form asserted absent by regex; inline comment records the deliberate correction |
| T-138-12 | Tampering | `CHAIN_SKIP` | high | mitigate | Empty Set asserted by grep and by the SELF check's size assertion; adding entries is recorded as self-disqualifying in the prohibitions block |
| T-138-13 | Denial of Service | unbounded chain subprocess output | high | mitigate | Explicit output-buffer bound set with its value and rationale in the header comment, replacing the runtime default that silently converts a chatty child into a caught failure |
| T-138-14 | Spoofing | missing-child classifier laundering crashes into skips | high | mitigate | Classification narrowed to the file-not-found error code and the command-not-found status; the loose error-text phrase is grep-asserted absent |
| T-138-15 | Tampering | a deleted predecessor validator | high | mitigate | A non-existent chain child produces a failure, proven by a reverted rename mutation |
| T-138-16 | Repudiation | cross-OS evidence provenance | medium | mitigate | No predecessor-apex job is added here (grep-asserted absent); the dual-apex contract is restated verbatim and the nesting-guard variable is grep-asserted absent from the workflow |
| T-138-17 | Tampering | CI harness-run checkout without a ref | low | accept | The root cause is knowingly deferred to a dedicated tooling milestone; the template's shape is preserved unchanged |

No package-manager installs occur in this plan, so no supply-chain threat (`T-138-SC`) applies. All five files are Node built-ins and GitHub Actions YAML over the trusted local repository; there is no runtime service, no auth surface, and no user-supplied input.
</threat_model>

<verification>
- All three leaves exit 0; none carries the resolver; each has empty chain and skip arrays and a SELF check naming its own phase.
- check-phase-137's line-scoping is proven by a reverted false-match mutation, not asserted.
- The apex exits 0 standalone non-nested with the full 48-through-137 recursion; derived counts are 90 chain and 93 total.
- All three module-load guards, the corrected classifier and the missing-child failure are proven by reverted mutations.
- This milestone's archive-root token present, every predecessor phase-root form absent, output-buffer bound present.
- The 16th workflow exists with three leaf jobs plus the dual apex, no predecessor-apex job, no nesting-guard variable, no push trigger, and all three planning-document paths; the 15 prior workflows are untouched.
</verification>

<success_criteria>
HARN-15 (Atom 2) satisfied: four validators authored — three needle-based leaves plus an apex whose 90-entry span and 93 checks are independently derived and guarded at module load, carrying this milestone's own archive-root token, an empty skip Set and all three hazard fixes — plus the 16th CI coexistence workflow, with the apex proven green standalone before any push.
</success_criteria>

<output>
Create `.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-03-SUMMARY.md` when done. It MUST record the apex's derived total check count and chain count taken from the verbose summary, the three reverted mutation-test outcomes (truncated chain array, renamed chain child, false-match index line), and confirmation that this milestone's own archive-root token is used with no predecessor form present.
</output>
