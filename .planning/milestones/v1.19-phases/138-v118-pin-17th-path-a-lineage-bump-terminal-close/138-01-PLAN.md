---
phase: 138-v118-pin-17th-path-a-lineage-bump-terminal-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/_lib/frozen-at-close.mjs
autonomous: true
requirements: [HARN-14]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "MILESTONE_CLOSE_SHAS.V118 pins the subject-line-confirmed v1.18 close-gate SHA 7af8a147, and that SHA is reachable on origin/master"
    - "readAtV118Close is exported and resolves through readAtClose('V118', p) — a real file read at 7af8a147 returns non-empty bytes"
    - "[EDGE HARN-14/adjacency] MILESTONE_CLOSE_SHAS carries V117 ('b56bba5') and V118 ('7af8a147') as two distinct adjacent keys — neither overwrites the other, and readAtV117Close / readAtV118Close resolve to different SHAs"
    - "[EDGE HARN-14/empty] readAtV118Close on a path that does not exist at 7af8a147 fails loud (non-zero / throw), never returning an empty string as a successful read"
    - "[EDGE HARN-14/ordering] The V118 entry is appended immediately after V117; every pre-existing entry V18..V117 and every pre-existing export is byte-unchanged (git diff shows additions only)"
    - "The WAVE0_ANCHOR SHA is captured via git rev-parse HEAD BEFORE any Phase-138 edit lands and is recorded verbatim in 138-01-SUMMARY.md"
    - "The 47-surface frozen-set inventory (16 milestone-audit .mjs + 16 sidecar JSON + 15 integrity workflows) plus check-phase-48..134.mjs plus the four byte-frozen _lib dependencies is independently derived at Wave 0 and recorded, so Plan 138-04's HARD gate has a pre-committed target list"
  prohibitions:
    - "No frozen predecessor surface may be edited to silence a failing check, and CHAIN_SKIP must never gain entries to force a green result — value-masking is self-disqualifying"
    - "No open deferred item may be silently dropped; masking-by-deletion is barred at every stage of this phase"
  artifacts:
    - path: "scripts/validation/_lib/frozen-at-close.mjs"
      provides: "V118 back-anchor pin + readAtV118Close export"
      contains: "V118:"
    - path: ".planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-01-SUMMARY.md"
      provides: "WAVE0_ANCHOR SHA + the derived 47-surface frozen-set inventory"
  key_links:
    - from: "scripts/validation/_lib/frozen-at-close.mjs"
      to: "MILESTONE_CLOSE_SHAS.V118"
      via: "readAtClose('V118', path) -> git show 7af8a147:<path>"
      pattern: "readAtV118Close"
---

<objective>
HARN-14 — add the mandatory V118 back-anchor pin that freezes the v1.18 corpus, and capture the Wave-0 anchor SHA plus the frozen-surface inventory that Plan 138-04's byte-unchanged HARD gate will diff against.

Purpose: the back-anchor invariant requires that at the close of milestone N the PREVIOUS milestone's close SHA is pinned. A wrong SHA silently corrupts every v1.18-frozen read, and `check-phase-118.mjs:87-88` shows the unreachable-SHA failure mode is `pass: false` — a FAIL, not a skip. The SHA is therefore recovered by the subject-line discriminator (D-31 / STATE.md:32), never by the naive dual-`--grep --all-match` form which returns 2 candidates.
Output: `scripts/validation/_lib/frozen-at-close.mjs` gains the V118 entry + `readAtV118Close` export (append-only); `138-01-SUMMARY.md` records `WAVE0_ANCHOR` and the derived frozen-set inventory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-PATTERNS.md
@scripts/validation/_lib/frozen-at-close.mjs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end V118 back-anchor — one frozen read proven from SHA to bytes</name>
  <precondition>The v1.18 close-gate SHA 7af8a14766d346a348f7adf05d260676dbe4c1b2 is reachable on the remote: `git branch -r --contains 7af8a147` lists `origin/master` (the owner's PIPE-02 push has landed).</precondition>
  <reversibility rating="reversible">The pin is one object key plus one export line; a wrong value is corrected by editing two lines before anything downstream reads it.</reversibility>
  <files>scripts/validation/_lib/frozen-at-close.mjs</files>
  <read_first>
    - scripts/validation/_lib/frozen-at-close.mjs (the V117 entry at line 76 and the `readAtV117Close` export at line 123 — the two exact insertion points and comment shape to mirror; also `readAtClose()` at lines 89-106 for the CRLF-normalized, 10s-timeout read semantics)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-PATTERNS.md (section "scripts/validation/_lib/frozen-at-close.mjs" — the verbatim V117 comment block to copy the shape of, and the append-only guardrail)
    - .planning/STATE.md lines 28-36 (the PIPE-02 discharge record and the verified subject-line recovery method with its documented false positive)
  </read_first>
  <action>
    This is the phase's tracer: it walks the whole back-anchor path once, end to end, on a single real read — git object DB, `MILESTONE_CLOSE_SHAS`, `readAtClose`, the convenience export, real bytes back.

    FIRST, before touching any file, capture the Wave-0 anchor with `git rev-parse HEAD` and hold it verbatim for the SUMMARY. Nothing in Phase 138 may be edited before this value exists — it is the diff base for the whole-phase byte-unchanged HARD gate.

    Re-confirm the pin target with the subject-line discriminator recorded at STATE.md:32: run `git log --all --format="%H|%s"` piped through an awk filter requiring the SUBJECT to match both the version token and the milestone-close token, and confirm the result count is exactly 1 and the SHA is 7af8a14766d346a348f7adf05d260676dbe4c1b2 with subject `docs(134-05): v1.18 MILESTONE CLOSE — single close-gate commit, 20/20 requirements Validated`. Do NOT use the bare `git log --all --grep=... --all-match -1` form: it returns 2 candidates (it also matches the v1.17 close-gate b56bba5e) and `-1` silently resolves to the wrong one. Also confirm reachability on the remote with `git branch -r --contains`.

    Then make the append, and only the append: insert `V118: '7af8a147',` into `MILESTONE_CLOSE_SHAS` immediately after the existing V117 entry, mirroring the V117 comment block — annotate it as the Phase 134 Plan 134-05 close-gate, record the full recovered SHA, record that recovery used the SUBJECT-LINE pair discriminator (count = 1) rather than the dual-token grep, and close with the back-anchor invariant sentence stating that the V119 pin is deferred to the v1.20 close. Append `export const readAtV118Close = (p) => readAtClose('V118', p);` immediately after the existing `readAtV117Close` export line, matching its column alignment.

    Prove it end to end: import the module and call `readAtV118Close('README.md')` (or another path certain to exist at that SHA), asserting the returned string is non-empty. Then call it on a path that does not exist at 7af8a147 and confirm the failure is loud — a throw or a non-zero exit — not an empty string returned as success. Record both outcomes in the SUMMARY.

    Make NO other edit to this file. It is append-only for the whole of Phase 138: do not touch V18..V117, do not touch `readAtClose`, do not reformat, do not reorder.
  </action>
  <acceptance_criteria>
    - `git branch -r --contains 7af8a14766d346a348f7adf05d260676dbe4c1b2` lists `origin/master`.
    - The subject-line recovery filter returns exactly ONE line, and its SHA is `7af8a14766d346a348f7adf05d260676dbe4c1b2`.
    - `grep -c "V118: '7af8a147'" scripts/validation/_lib/frozen-at-close.mjs` returns 1.
    - `grep -c "readAtV118Close" scripts/validation/_lib/frozen-at-close.mjs` returns 2 (the export definition plus, if present, its own doc-comment mention) and the definition body is `readAtClose('V118', p)`.
    - `node -e "import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>process.exit(m.MILESTONE_CLOSE_SHAS.V118==='7af8a147' && m.MILESTONE_CLOSE_SHAS.V117==='b56bba5' && typeof m.readAtV118Close==='function' && m.readAtV118Close('README.md').length>0 ?0:1))"` exits 0 — this single assertion covers the adjacency edge (both keys distinct and present) and the end-to-end read.
    - A read of a path absent at 7af8a147 through `readAtV118Close` produces a throw or non-zero exit; the SUMMARY records the observed behaviour verbatim.
    - `git diff --numstat scripts/validation/_lib/frozen-at-close.mjs` shows deletions = 0 (append-only; V117 and every earlier entry byte-unchanged).
    - `git diff --name-only` lists `scripts/validation/_lib/frozen-at-close.mjs` as the only modified file.
    - SUMMARY records `WAVE0_ANCHOR=<sha>` captured before the edit.
  </acceptance_criteria>
  <verify>
    <automated>node -e "import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>process.exit(m.MILESTONE_CLOSE_SHAS.V118==='7af8a147' && m.MILESTONE_CLOSE_SHAS.V117==='b56bba5' && typeof m.readAtV118Close==='function' && m.readAtV118Close('README.md').length>0 ?0:1))"</automated>
  </verify>
  <done>V118 is pinned to the subject-line-confirmed, remote-reachable 7af8a147; readAtV118Close returns real bytes from that SHA and fails loud on an absent path; the diff is additions-only; WAVE0_ANCHOR is captured and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Derive and record the 47-surface frozen-set inventory + the four byte-frozen _lib dependencies</name>
  <files>(no file edits — inventory recorded in 138-01-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md (D-18 in full — the independent 47-surface derivation, the shared-mutable-dependency omission it closes, the append-only and byte-frozen guardrails, and the scripts/pipeline exclusion re-ruled for v1.19)
    - .planning/milestones/v1.18-MILESTONE-AUDIT.md line 294 (the predecessor's 44-surface enumeration — read it to confirm it is silent on scripts/pipeline and on the shared _lib dependencies, then do NOT copy its count)
  </read_first>
  <action>
    Derive the frozen-surface target list from the working tree as it stands right now, at Wave 0, before any Phase-138 file exists. Deriving it later — after this phase's own new files land — makes "which files are predecessor-frozen" ambiguous, which is exactly how a real drift gets waved through.

    Enumerate four groups by listing, not by trusting a carried number: the per-milestone audit harnesses (`scripts/validation/v1.*-milestone-audit.mjs`), the sidecar allowlists (`scripts/validation/v1.*-audit-allowlist.json`), every workflow file under `.github/workflows/`, and the predecessor chain validators from the forty-eighth through the one hundred thirty-fourth. Record each group's count. The first three groups must total 47 — that is the independently derived figure D-18 mandates. Do NOT copy the predecessor's 44.

    Then record the four shared mutable dependencies that the 47-surface diff alone does NOT protect, with their importer counts as stated in CONTEXT: `c17-eee-contract.mjs` (8 importers), `_lib/archive-path.mjs` (23), `_lib/exec-fail-detail.mjs` (31), `_lib/frozen-at-close.mjs` (25). The first three are BYTE-FROZEN for the whole of Phase 138 with no carve-out; the fourth is append-only and its sole sanctioned Phase-138 change is Task 1's V118 entry plus its export. A one-line edit to any of the first three silently changes four frozen harnesses while a 47-surface diff still reports clean — that is the hole this inventory closes.

    Record that `scripts/pipeline/` is OUT of the gate as a v1.19 ruling made here, not as an inherited fact: the only prior citation lives inside commit 6acc429b's own message, and the predecessor audit's enumeration is silent on it.

    Write the complete inventory into 138-01-SUMMARY.md as a checkable list Plan 138-04 can execute against `git diff <WAVE0_ANCHOR>..HEAD` without re-deriving anything.
  </action>
  <acceptance_criteria>
    - SUMMARY records four group counts derived by listing: milestone-audit harnesses = 16, sidecar allowlists = 16, workflow files = 15, and the `check-phase-48..134.mjs` predecessor range enumerated.
    - SUMMARY states the 16 + 16 + 15 = 47 arithmetic explicitly and states that the predecessor's 44 was NOT carried.
    - SUMMARY names all four shared `_lib` dependencies with their importer counts, marks three as byte-frozen with no carve-out, and marks `_lib/frozen-at-close.mjs` as append-only with its one sanctioned change.
    - SUMMARY records the `scripts/pipeline/` exclusion as a v1.19 ruling with the reason it is not an inherited fact.
    - SUMMARY records `WAVE0_ANCHOR=<sha>` once, matching Task 1's captured value, alongside the exact `git diff <WAVE0_ANCHOR>..HEAD` command Plan 138-04 will run.
  </acceptance_criteria>
  <verify>
    <automated>test "$(ls scripts/validation/v1.*-milestone-audit.mjs | wc -l)" = "16" && test "$(ls scripts/validation/v1.*-audit-allowlist.json | wc -l)" = "16" && test "$(ls .github/workflows/*.yml | wc -l)" = "15" && echo "47-surface groups confirmed at Wave 0"</automated>
  </verify>
  <done>The 47-surface frozen set is independently derived and recorded with its arithmetic, the four shared _lib dependencies are named with their freeze status, the pipeline exclusion is ruled fresh, and Plan 138-04 has a pre-committed diff target list.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

| Symbol / artifact | Kind | Owner plan |
|---|---|---|
| `MILESTONE_CLOSE_SHAS.V118` (key `'7af8a147'`) | new object key in `_lib/frozen-at-close.mjs` | 138-01 |
| `readAtV118Close` | new export | 138-01 |
| `scripts/validation/v1.19-milestone-audit.mjs` | new file — 17th Path-A harness (C1–C17) | 138-02 |
| `scripts/validation/v1.19-audit-allowlist.json` | new file — sidecar, 59 line-pins in 5 arrays | 138-02 |
| `BASELINE_23` comment block | append-only comment in `regenerate-supervision-pins.mjs` | 138-02 |
| `scripts/validation/check-phase-135.mjs` — `V-135-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-136.mjs` — `V-136-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-137.mjs` — `V-137-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-138.mjs` — `V-138-AUDIT`, `V-138-CHAIN-48`…`V-138-CHAIN-137`, `V-138-AUDIT-HARNESS`, `V-138-SELF` (93 checks) | new chain apex | 138-03 |
| `.github/workflows/audit-harness-v1.19-integrity.yml` — jobs `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`, `check-phase-135`, `check-phase-136`, `check-phase-137`, `check-phase-138`, `rotting-external-quarterly`, `pin-helper-advisory` | new file — 16th CI coexistence workflow | 138-03 |
| `dist/docs-library-v1.19.zip` | build artifact (gitignored) | 138-04 |
| `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` | new doc | 138-06 |
| `.planning/milestones/v1.19-MILESTONE-AUDIT.md` | new doc | 138-06 |
| `.planning/phases/138-.../138-VERIFICATION.md` | new doc — the apex AUDIT resolver target | 138-06 |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → frozen-read pin | The recovered SHA becomes the authoritative anchor for every v1.18-frozen `readAtClose`; a wrong value corrupts every downstream frozen read with no visible symptom |
| remote reachability → pin validity | A SHA present locally but absent on the remote fails as `pass: false` in CI, not as a skip |
| shared `_lib` module → 47 "byte-unchanged" frozen harnesses | A one-line edit to a shared dependency changes four frozen harnesses' behaviour while the 47-surface diff still reports CLEAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-01 | Tampering | V118 pin value | high | mitigate | Subject-line pair discriminator with count = 1 asserted; the naive dual-`--grep --all-match` form (2 candidates) explicitly barred; SHA compared verbatim against 7af8a14766d346a348f7adf05d260676dbe4c1b2 |
| T-138-02 | Tampering | pre-existing entries in `_lib/frozen-at-close.mjs` | high | mitigate | Append-only edit; `git diff --numstat` deletions = 0 as an acceptance criterion; V117 value re-asserted at runtime alongside V118 |
| T-138-03 | Spoofing | an unreachable pin masquerading as valid | medium | mitigate | `git branch -r --contains` asserts remote reachability before the pin lands; the precondition halts the task if the owner push has not arrived |
| T-138-04 | Tampering | shared `_lib` dependencies outside the 47-surface diff | high | mitigate | Wave-0 inventory names all four with importer counts and freeze status, so Plan 138-04's gate covers them explicitly rather than by omission |
| T-138-05 | Information Disclosure | frozen read of an absent path returning empty-as-success | low | mitigate | The empty-input edge is asserted directly: an absent-path read must throw or exit non-zero, never return `''` |

No package-manager installs occur in this plan, so no supply-chain threat (`T-138-SC`) applies. This is internal validation tooling with no runtime service, no auth surface, no network listener, and no user-supplied input.
</threat_model>

<verification>
- The V118 SHA is subject-line-confirmed (count = 1) and reachable on `origin/master`.
- The module imports clean; `MILESTONE_CLOSE_SHAS.V118 === '7af8a147'`, `V117 === 'b56bba5'`, and `readAtV118Close` returns non-empty bytes for a real path.
- An absent-path read fails loud; the observed behaviour is recorded.
- `git diff --numstat` on the pin file shows zero deletions; no other file is modified.
- The 47-surface inventory is derived by listing (16 / 16 / 15) and recorded with the four shared `_lib` dependencies and their freeze status.
</verification>

<success_criteria>
HARN-14 satisfied: the V118 back-anchor pin and `readAtV118Close` export land as an append-only edit, proven end to end by a real frozen read; the Wave-0 anchor and the independently derived 47-surface frozen-set inventory are recorded for Plan 138-04's HARD gate.
</success_criteria>

<output>
Create `.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-01-SUMMARY.md` when done. It MUST record `WAVE0_ANCHOR=<sha>` (captured before any edit), the confirmed V118 SHA with its recovery count, the observed absent-path failure mode, and the complete 47-surface frozen-set inventory with the four shared `_lib` dependencies.
</output>
