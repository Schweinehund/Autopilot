---
phase: 138-v118-pin-17th-path-a-lineage-bump-terminal-close
plan: 02
type: execute
wave: 2
depends_on: [138-01]
files_modified:
  - scripts/validation/v1.19-milestone-audit.mjs
  - scripts/validation/v1.19-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
autonomous: true
requirements: [HARN-15]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "v1.19-milestone-audit.mjs exists as a Path-A copy of the v1.18 harness with C1–C17 inherited verbatim, and exits 0 --verbose on the live v1.19 corpus"
    - "v1.19-milestone-audit.mjs reads scripts/validation/v1.19-audit-allowlist.json; no predecessor sidecar filename survives anywhere in the copy"
    - "v1.19-audit-allowlist.json differs from its predecessor in exactly two header fields (generated, phase); all 59 line-pins across the 5 arrays carry forward byte-identical"
    - "Allowlist pin validity is proven by the ~2s pinned-file drift instrument — git diff --name-only 7af8a147..HEAD over docs, scripts and .github, intersected with the sidecar-derived pinned file set — yielding zero pinned files changed"
    - "regenerate-supervision-pins.mjs gains a BASELINE_23 audit-trail comment citing the JIT pre-Atom-1 HEAD (captured fresh, NOT the Wave-0 anchor), and its BASELINE_9 line-coordinate array is byte-unchanged"
  prohibitions:
    - "regenerate-supervision-pins.mjs --report must NOT be cited as the allowlist drift proof — it hardcodes the v1.7 sidecar and walks only 26 of the 59 line-pins, so a clean report proves nothing about v1.19"
    - "No frozen predecessor surface may be edited to silence a failing check; the pin generator is append-only-comment for this phase and its coordinate array is untouchable"
  artifacts:
    - path: "scripts/validation/v1.19-milestone-audit.mjs"
      provides: "17th Path-A audit harness, C1–C17 inherited"
      contains: "v1.19-audit-allowlist.json"
    - path: "scripts/validation/v1.19-audit-allowlist.json"
      provides: "17th harness sidecar — 59 line-pins across 5 arrays"
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "BASELINE_23 refresh audit-trail comment"
      contains: "BASELINE_23"
  key_links:
    - from: "scripts/validation/v1.19-milestone-audit.mjs"
      to: "scripts/validation/v1.19-audit-allowlist.json"
      via: "the sidecar readFile() path string"
      pattern: "v1\\.19-audit-allowlist\\.json"
---

<objective>
HARN-15 (Atom 1) — author the 17th Path-A audit harness: `v1.19-milestone-audit.mjs` (C1–C17 inherited verbatim from the v1.18 harness), `v1.19-audit-allowlist.json` (header-fields-only copy), and the BASELINE_23 refresh comment — with allowlist pin validity positively proven by the correct instrument.

Purpose: every milestone gets its own harness plus sidecar. The C1–C17 contract is inherited unedited because this is a pure lineage bump, not a content bump. The sidecar's 59 line-pins are coordinate-sensitive, so zero drift must be positively proven rather than assumed — and proven with an instrument that actually covers all five pin arrays.
Output: three files — the harness, the sidecar, and an append-only BASELINE_23 audit-trail comment in the otherwise-frozen pin generator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-RESEARCH.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-PATTERNS.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Path-A copy the 17th harness + its sidecar</name>
  <files>scripts/validation/v1.19-milestone-audit.mjs, scripts/validation/v1.19-audit-allowlist.json</files>
  <read_first>
    - scripts/validation/v1.18-milestone-audit.mjs (the Path-A template — the header comment block, the `// Usage:` line, the sidecar `readFile(...)` path string, and the C1–C17 check bodies including C17's subprocess spawn of `c17-eee-contract.mjs`; note line 4 carries a claim about `--report` confirmation that must NOT be carried forward, per CONTEXT D-20)
    - scripts/validation/v1.18-audit-allowlist.json (the sidecar template — the `generated` and `phase` header fields plus the five pin arrays: `supervision_exemptions` 26, `safetynet_exemptions` 4, `c7_knox_allowlist` 10, `c9_exemptions` 4, `c13_broken_link_allowlist` 15)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-RESEARCH.md ("Pattern 3: Path-A milestone-harness copy-forward" — the verified 3-logical-spot harness diff and the verified 2-line sidecar diff)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md (D-20 in full — the false byte-verbatim dichotomy, the three-item v1.19 action list, and the correction of record about the predecessor harness's `--report` claim)
  </read_first>
  <action>
    Copy `scripts/validation/v1.18-milestone-audit.mjs` to `scripts/validation/v1.19-milestone-audit.mjs` and change exactly the three logical spots the verified predecessor-to-predecessor diff identifies, and nothing else: the header comment block (its lineage-chain sentence, its source-of-truth pointer — which now points at this phase's CONTEXT file — and its sidecar-description line), the `// Usage:` line's filename, and the sidecar `readFile(...)` path string, which must now resolve to `scripts/validation/v1.19-audit-allowlist.json`. The predecessor sidecar filename must not survive anywhere in the new file. C1 through C17 are inherited byte-for-byte: do not touch any check body, do not change any freshness threshold, do not re-tune any count.

    Do NOT carry forward the predecessor header's assertion that zero pin drift was positively confirmed by the pin generator's report mode. That claim could not have proven what it claims (see Task 2), and repeating it would launder a false premise into the 17th generation. Replace it with a pointer to the instrument Task 2 actually runs.

    Copy the predecessor sidecar named in this task's read-first list to `scripts/validation/v1.19-audit-allowlist.json`, changing only the two header fields — the generation timestamp and the phase string. Byte-verbatim is impossible and is not the target; the two-field delta is. All 59 pins across the five arrays carry forward unchanged: 26 supervision exemptions, 4 safetynet exemptions, 10 Knox allowlist entries, 4 C9 exemptions, 15 broken-link allowlist entries.

    Then run the new harness with `--verbose` against the live corpus and confirm it exits 0. Record its summary line in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/v1.19-milestone-audit.mjs --verbose` exits 0.
    - `grep -c "v1\.19-audit-allowlist\.json" scripts/validation/v1.19-milestone-audit.mjs` is at least 1.
    - `grep -c "v1\.18-audit-allowlist" scripts/validation/v1.19-milestone-audit.mjs` is 0 (no predecessor sidecar reference survives).
    - `diff scripts/validation/v1.18-audit-allowlist.json scripts/validation/v1.19-audit-allowlist.json` reports changes on exactly two keys — the generation timestamp and the phase string — and no pin-array line.
    - The five pin-array lengths in the new sidecar are 26 / 4 / 10 / 4 / 15, totalling 59; the counts are asserted programmatically, not eyeballed.
    - `diff <(grep -v '^//' scripts/validation/v1.18-milestone-audit.mjs) <(grep -v '^//' scripts/validation/v1.19-milestone-audit.mjs)` shows changes confined to the usage line and the sidecar path string — every C1–C17 check body is identical.
    - SUMMARY records the harness `--verbose` summary line verbatim.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/v1.19-milestone-audit.mjs --verbose && node -e "const a=require('./scripts/validation/v1.19-audit-allowlist.json');const n=a.supervision_exemptions.length+a.safetynet_exemptions.length+a.c7_knox_allowlist.length+a.c9_exemptions.length+a.c13_broken_link_allowlist.length;process.exit(n===59?0:1)"</automated>
  </verify>
  <done>The 17th Path-A harness and its sidecar exist, C1–C17 are inherited unedited, the harness resolves the v1.19 sidecar with zero predecessor-sidecar residue, all 59 pins carry forward, and the harness exits 0 on the live corpus.</done>
</task>

<task type="auto">
  <name>Task 2: Prove pinned-file zero-drift with the correct instrument + append the BASELINE_23 comment</name>
  <files>scripts/validation/regenerate-supervision-pins.mjs</files>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs (lines 503-514 — the BASELINE_22 comment block whose shape BASELINE_23 mirrors; lines 107-124 — `androidDocPaths()`; line 290 and lines 336, 530 — the hardcoded v1.7 sidecar reads inside the report path; line 415 — the never-honoured lineage-repoint forward pointer; and the BASELINE_9 line-coordinate array, which is untouchable)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md (D-20 — why the report mode cannot prove anything about v1.19, and the replacement instrument with its verified result; D-24 for the self-test's unchanged-inputs finding)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-PATTERNS.md (section "regenerate-supervision-pins.mjs" — the BASELINE_23 skeleton and the Wave-0-vs-pre-Atom-1 anchor distinction)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-01-SUMMARY.md (the recorded WAVE0_ANCHOR — read it precisely so the BASELINE_23 comment can state that it is deliberately NOT citing that value)
  </read_first>
  <action>
    Prove the sidecar's pins are still valid, using the instrument that actually covers all five arrays. Derive the set of files the v1.18 sidecar pins by extracting every file path from all five pin arrays. Then run `git diff --name-only 7af8a147..HEAD -- docs scripts .github` and intersect the two sets. The intersection must be empty: zero pinned files changed between the v1.18 close and now. This runs in roughly two seconds and covers all 59 pins.

    Do NOT use the pin generator's report mode as this proof. Its report path hardcodes a read of the v1.7 sidecar at line 290 — with the same hardcoding repeated at lines 336 and 530 — so it walks only the 26 supervision exemptions and never sees the other 33 pins across the safetynet, Knox, C9 and broken-link arrays. The lineage repoint promised at line 415 was never honoured. A clean report from that path is not evidence about v1.19, and the predecessor harness's header claim that it was is a false premise this phase records rather than repeats.

    Record in the SUMMARY: the derived pinned-file set size, the changed-file list the diff returned, and the empty intersection. If the intersection is NOT empty, halt — a changed pinned file means real coordinate drift, and the remedy (a targeted coordinate re-pin under the CARVE-1 option-(a) budget) is a decision for the owner, not an autonomous edit.

    Then capture the just-in-time pre-Atom-1 HEAD with `git rev-parse HEAD` immediately before editing the generator. This is deliberately NOT the Wave-0 anchor recorded in Plan 138-01's SUMMARY: Task 1's own harness-authoring commit landed between Wave 0 and this moment, so the true pre-Atom-1 predecessor is the current HEAD. This Wave-0-versus-pre-Atom-1 distinction has now recurred at four consecutive closes and is a load-bearing convention, not an incidental note.

    Append a BASELINE_23 audit-trail comment mirroring the BASELINE_22 block's shape: record that it closes the BASELINE_22 carry-over per the HARN-15 contract; cite the captured JIT HEAD and state explicitly that it is not the Wave-0 anchor and why; state that the coordinate array is NOT altered and that this comment records an audit-trail event only; state the resolution path forward, that BASELINE_24 refreshes at the next milestone close, continuing the Path-A inheritance chain. The generator is frozen apart from this comment: do not touch the coordinate array, do not repoint the hardcoded v1.7 sidecar read (that is logged tooling debt this phase does not fix), do not convert anything to a frozen read.
  </action>
  <acceptance_criteria>
    - The pinned-file set derived from all five sidecar arrays is recorded in the SUMMARY with its size, and the derivation covers all 59 pins (not the 26-pin subset).
    - `git diff --name-only 7af8a147..HEAD -- docs scripts .github` is captured verbatim in the SUMMARY, and its intersection with the pinned-file set is empty.
    - `grep -c "BASELINE_23" scripts/validation/regenerate-supervision-pins.mjs` is at least 1.
    - `git diff --numstat scripts/validation/regenerate-supervision-pins.mjs` shows deletions = 0 (comment append only; the coordinate array byte-unchanged).
    - `git diff scripts/validation/regenerate-supervision-pins.mjs` contains no added or removed line matching a coordinate-array entry — every added line is a comment line.
    - The BASELINE_23 comment contains the captured JIT pre-Atom-1 SHA, and that SHA differs from the WAVE0_ANCHOR recorded in 138-01-SUMMARY.md.
    - `grep -c "regenerate-supervision-pins\.mjs --report" scripts/validation/v1.19-milestone-audit.mjs` is 0 (the disproven proof instrument is not cited in the new harness header).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "BASELINE_23" scripts/validation/regenerate-supervision-pins.mjs && test "$(git diff --numstat scripts/validation/regenerate-supervision-pins.mjs | awk '{print $2}')" = "0" && echo OK</automated>
  </verify>
  <done>Pinned-file zero-drift is proven across all 59 pins by the two-second diff instrument; the disproven report-mode claim is not carried forward; BASELINE_23 is appended citing the JIT pre-Atom-1 HEAD with the coordinate array byte-unchanged.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

| Symbol / artifact | Kind | Owner plan |
|---|---|---|
| `MILESTONE_CLOSE_SHAS.V118` (key `'7af8a147'`) | new object key in `_lib/frozen-at-close.mjs` | 138-01 |
| `readAtV118Close` | new export | 138-01 |
| `scripts/validation/v1.19-milestone-audit.mjs` | new file — 17th Path-A harness (C1–C17) | 138-02 |
| `scripts/validation/v1.19-audit-allowlist.json` | new file — sidecar, 59 line-pins in 5 arrays | 138-02 |
| `BASELINE_23` comment block | append-only comment in `regenerate-supervision-pins.mjs` | 138-02 |
| `scripts/validation/check-phase-135.mjs` — `V-135-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-136.mjs` — `V-136-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-137.mjs` — `V-137-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-138.mjs` — `V-138-AUDIT`, `V-138-CHAIN-48`…`V-138-CHAIN-137`, `V-138-AUDIT-HARNESS`, `V-138-SELF` (93 checks) | new chain apex | 138-03 |
| `.github/workflows/audit-harness-v1.19-integrity.yml` — jobs `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`, `check-phase-135`, `check-phase-136`, `check-phase-137`, `check-phase-138`, `rotting-external-quarterly`, `pin-helper-advisory` | new file — 16th CI coexistence workflow | 138-03 |
| `dist/docs-library-v1.19.zip` | build artifact (gitignored) | 138-04 |
| `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` | new doc | 138-06 |
| `.planning/milestones/v1.19-MILESTONE-AUDIT.md` | new doc | 138-06 |
| `.planning/phases/138-.../138-VERIFICATION.md` | new doc — the apex AUDIT resolver target | 138-06 |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar line-pins → corpus coordinates | Pins are line-coordinate-sensitive; a shifted corpus line silently invalidates an exemption and either hides a real finding or fires a false one |
| proof instrument → drift claim | A proof instrument that inspects the wrong sidecar produces a confident, worthless clean result — the exact defect inherited from the predecessor harness header |
| pin generator → 16 frozen sidecars | The coordinate array is read across the whole lineage; any edit propagates beyond this milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-06 | Spoofing | allowlist zero-drift claim | high | mitigate | Proof runs the sidecar-derived-set × `git diff --name-only 7af8a147..HEAD` intersection covering all 59 pins; the 26-pin report path is explicitly barred as evidence and the predecessor's claim is not carried forward |
| T-138-07 | Tampering | `regenerate-supervision-pins.mjs` coordinate array | high | mitigate | Comment-append only; `git diff --numstat` deletions = 0; every added line asserted to be a comment line |
| T-138-08 | Tampering | C1–C17 check bodies during the Path-A copy | high | mitigate | Comment-stripped diff between predecessor and new harness must show changes confined to the usage line and the sidecar path string |
| T-138-09 | Repudiation | BASELINE_23 anchor provenance | low | mitigate | The comment cites the JIT pre-Atom-1 HEAD and asserts it differs from the recorded Wave-0 anchor, preserving the four-close-old distinction in the audit trail |

No package-manager installs occur in this plan, so no supply-chain threat (`T-138-SC`) applies. All three files are Node built-ins only, matching the repo's zero-external-dependency convention for validation and pipeline scripts.
</threat_model>

<verification>
- The 17th harness exits 0 `--verbose`; it references the v1.19 sidecar and carries zero predecessor-sidecar residue.
- The sidecar diff is exactly two header fields; all five pin arrays total 59 and are byte-identical.
- Pinned-file zero-drift is proven by the all-59-pin diff instrument, with the changed-file list recorded.
- BASELINE_23 is present, the coordinate array shows zero deletions and no value changes, and the cited JIT SHA differs from the Wave-0 anchor.
</verification>

<success_criteria>
HARN-15 (Atom 1) satisfied: the 17th Path-A harness and sidecar exist with C1–C17 inherited verbatim and the sidecar correctly wired, pin validity is positively proven across all five arrays by the correct instrument, and BASELINE_23 lands as an append-only audit-trail comment with the coordinate array untouched.
</success_criteria>

<output>
Create `.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-02-SUMMARY.md` when done. It MUST record the harness `--verbose` summary line, the derived pinned-file set size with the empty-intersection drift proof, the changed-file list the diff returned, and the captured JIT pre-Atom-1 SHA embedded in BASELINE_23.
</output>
