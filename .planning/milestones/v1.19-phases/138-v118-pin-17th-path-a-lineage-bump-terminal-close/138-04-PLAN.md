---
phase: 138-v118-pin-17th-path-a-lineage-bump-terminal-close
plan: 04
type: execute
wave: 4
depends_on: [138-01, 138-02, 138-03]
files_modified: []
autonomous: true
requirements: [HARN-15, HARN-16]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "HARN-15 gate part 1: apex-138 runs green NON-NESTED standalone with no competing local load, and the summary line is captured verbatim"
    - "HARN-15 gate part 2: a NON-NESTED run of the drift-bearing band is performed and its RED set is enumerated BY NAME with each member's root cause classified"
    - "HARN-15 gate part 3: the record states explicitly that a full non-nested sweep of all 90 chain entries is exponential — the measured doubling is 5, 10, 19, 37, 75, 148 and about 300 seconds across phases 60 to 66, because 9 validators in that band do not propagate the nesting guard to their children — and is deliberately not attempted"
    - "The standalone-RED set is enumerated as exactly ten validators — 30, 31, 48, 60, 61, 62, 63, 64, 65, 66 — across four root-cause classes, and the record states explicitly that fixing the first two classes would NOT green the set"
    - "The predecessor byte-unchanged HARD gate passes: git diff from the Wave-0 anchor to HEAD over the 47-surface set plus the four shared _lib dependencies shows no unsanctioned drift, with this phase's own new and appended files enumerated as the only expected entries"
    - "The archival-drift PRE-scan covers both the archived-phase-path class and the roadmap-collapse / requirements-overwrite class that check-phase-54 reads live"
    - "Axis 1 (Windows fresh clone) is captured as ADVISORY with any deep-nest stall documented explicitly, never silently omitted, and the record states not-run is not the same as clean"
    - "Axis 3 attempts the FULL non-nested apex first; if the shallow form is used it is entered as shape-match, NOT count-match, with the prior rejection of that form cited and rebutted"
    - "The publish bundle regenerates for this milestone version under the Stop-hook gate, with both new recipes pandoc-convertible and clean through the docx guard"
  prohibitions:
    - "No nested-guard run may be reported as a chain result; the record must never claim a zero-failure outcome for a full non-nested sweep of all 90 entries"
    - "No frozen predecessor surface may be edited to silence any member of the standalone-RED set, and no standalone-RED member may be dropped from the enumeration to make the set look smaller"
    - "The pin generator's report mode must not be used as the drift proof at any point in this gate"
  artifacts:
    - path: ".planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-04-SUMMARY.md"
      provides: "The complete pre-push evidence base: 4-part HARN-15 gate, byte-unchanged verdict, archival-drift pre-scan, Axis 1 and Axis 3 dispositions, publish-bundle result, and the enumerated 16-workflow dispatch list for the owner checkpoint"
  key_links:
    - from: "the Wave-0 anchor recorded in 138-01-SUMMARY.md"
      to: "the 47-surface byte-unchanged verdict"
      via: "git diff ANCHOR..HEAD restricted to the pre-committed frozen-set inventory"
      pattern: "WAVE0_ANCHOR"
---

<objective>
HARN-15 and HARN-16 (pre-push half) — establish the complete local evidence base before anything is pushed: the four-part HARN-15 gate in its first three parts, the predecessor byte-unchanged HARD gate against the Wave-0 anchor, the widened archival-drift pre-scan, the two local re-audit axes, and the regenerated publish bundle.

Purpose: the close-gate must not be authored on an unproven chain, and the push in Plan 138-05 is one-way. Everything that can be proven locally is proven here first, so the owner is asked to push a state whose local evidence is already complete and recorded. The fourth part of the HARN-15 gate — the post-close-gate confirmatory apex run — is deliberately not here; it belongs after the close-gate rewrites the live planning documents, and lives in Plan 138-06.
Output: no source files modified; the whole evidence base recorded in this plan's SUMMARY, plus the freshly enumerated workflow dispatch list the owner checkpoint hands over.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-RESEARCH.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-01-SUMMARY.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-02-SUMMARY.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HARN-15 gate parts 1 to 3 — apex green non-nested, drift band enumerated by name, exponential non-attempt stated</name>
  <files>(no file edits — evidence recorded in 138-04-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md (D-15 in full — why the nested-sweep gate was replaced, and the four parts with part 2 explicitly non-nested; D-16 in full — the ten-member RED set and the four root-cause classes including the cascading chain-guard class; D-24 — the correction of record about the predecessor's zero-failure claim)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-03-SUMMARY.md (the apex's derived counts and its recorded standalone summary line, so this run's result can be compared against it)
    - .planning/STATE.md line 343 (the standing warning against inheriting the predecessor's full-sweep phrasing)
    - scripts/validation/check-phase-134.mjs lines 141-150 (the child spawn that sets the nesting guard on every child unconditionally — the structural reason the apex cannot itself produce a non-nested chain result)
  </read_first>
  <action>
    Run the three locally provable parts of the HARN-15 gate and record each as evidence, not as a conclusion.

    Part 1. Run the new apex verbose with the nesting-guard environment variable unset, on an otherwise idle machine — no concurrent sweep, no parallel validator run, nothing else spawning Node. A concurrent local sweep has flaked a chain entry once before, and a flake here would be indistinguishable from a real regression. Capture the summary line verbatim and compare it against the line recorded in the previous plan's summary; a difference between two idle runs is itself a finding worth recording.

    Part 2. Run the drift-bearing band NON-NESTED, one validator at a time, with the nesting guard unset. This is the only branch where live-HEAD content drift surfaces at all: the apex sets the nesting guard on every child it spawns, so a top-level nested sweep is redundant with the apex rather than corroborating it, and running the apex itself under the guard short-circuits both the chain loop and the harness step, validating nothing. Enumerate the RED set by name with each member's root cause classified into one of four classes: the pin-generator self-test classifier failing against a frozen fixture in the Android glossary; a superseded freshness rule in the earliest harness generation whose threshold was formally raised at a later phase; cascading chain-guards, where one red validator makes its dependents red in turn; and pre-chain content drift in the two lowest-numbered members. The expected set is exactly ten members — 30, 31, 48, 60, 61, 62, 63, 64, 65 and 66. If the observed set differs from ten members, record the difference as a finding rather than reconciling it silently.

    For the sixty-first validator specifically, record its four individual failures by their check identifiers and attribute each to its cause: two are cascades from red predecessors, one is the superseded freshness rule, one is the self-test classifier. Earlier records named only the last two; naming all four is what stops a successor treating this as a two-line fix.

    State explicitly, in the SUMMARY, that fixing the self-test class and the superseded-freshness class would NOT turn the set green, because the cascading chain-guard class and the content-drift class survive both fixes. Without that sentence a successor picks the deferral up as trivial and fails.

    Part 3. Record that a full non-nested sweep of all 90 chain entries is exponential and is deliberately not attempted. Give the measured wall-clock doubling across phases 60 through 66 — five, ten, nineteen, thirty-seven, seventy-five, one hundred forty-eight and about three hundred seconds — and the reason: nine validators in that band do not propagate the nesting guard to their own children, while the seventy-first does. This is a bounded, honestly labelled gate, and it must be described as such.

    Never write, in the SUMMARY or anywhere downstream, a zero-failure claim about a full non-nested chain. That exact phrasing was already false at the predecessor's close and is structurally impossible to produce.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-138.mjs --verbose` with the nesting-guard variable unset exits 0; the summary line is recorded verbatim in the SUMMARY and matches the line recorded in 138-03-SUMMARY.md, or any difference is recorded as a finding.
    - The SUMMARY records, for the drift band, one line per validator run with its exit code, so the RED set is derived from observed output rather than asserted.
    - The enumerated standalone-RED set in the SUMMARY has exactly ten named members: 30, 31, 48, 60, 61, 62, 63, 64, 65, 66 — or the observed difference is recorded as an explicit finding.
    - Each RED member is assigned one of the four named root-cause classes, and the cascading chain-guard class is present by name.
    - The sixty-first validator's four failing check identifiers are listed individually with a cause attributed to each.
    - The SUMMARY contains an explicit sentence stating that fixing the self-test class and the superseded-freshness class would not green the set.
    - The SUMMARY contains the measured doubling figures and an explicit statement that the full 90-entry non-nested sweep is not attempted.
    - `grep -c "0 FAIL across the non-nested" .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-04-SUMMARY.md` is 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-138.mjs --verbose && test "$(grep -c '0 FAIL across the non-nested' .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-04-SUMMARY.md)" = "0" && echo OK</automated>
  </verify>
  <done>Gate parts 1 to 3 are complete: the apex is green non-nested on an idle machine, the ten-member RED set is enumerated by name with four root causes and the will-not-green-on-partial-fix statement, and the exponential full-sweep non-attempt is stated with its measured figures.</done>
</task>

<task type="auto">
  <name>Task 2: The 47-surface byte-unchanged HARD gate + the widened archival-drift pre-scan</name>
  <files>(no file edits — verdict recorded in 138-04-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-01-SUMMARY.md (the recorded Wave-0 anchor SHA and the pre-committed 47-surface inventory with the four shared _lib dependencies — this is the gate's target list, already derived, not to be re-derived here)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md (D-18 in full for the gate's scope and the shared-dependency omission it closes; D-19 in full for both halves of the archival-drift scan)
    - scripts/validation/check-phase-54.mjs lines 30-31 and lines 344-355 and lines 568-586 (the live reads of the requirements and roadmap documents with their negative assertions — the reason the scan must widen beyond archived phase paths)
    - scripts/validation/check-phase-124.mjs lines 46 and 97 (the second sanctioned option — a hardcoded phase path that survives archival because it is read at a frozen close SHA)
  </read_first>
  <action>
    Run the predecessor byte-unchanged HARD gate. Diff from the Wave-0 anchor recorded in Plan 138-01's summary to the current HEAD, restricted to the pre-committed 47-surface inventory: the sixteen predecessor milestone-audit harnesses, the sixteen predecessor sidecar allowlists, the fifteen pre-existing workflow files, and the predecessor chain validators from the forty-eighth through the one hundred thirty-fourth. Any diff on a file in that set is a HARD-gate failure. Halt and surface it rather than remediating autonomously — the remedy for real frozen-surface drift is a scoped carve-out recorded in the milestone audit, and that is the owner's call, not the executor's.

    Extend the same diff over the four shared mutable dependencies the 47-surface set does not cover on its own: the EEE contract module, the archive-path helper, the exec-failure-detail helper and the frozen-at-close library. The first three must show zero diff with no carve-out. The fourth must show additions only, and those additions must be exactly the V118 entry and its convenience export from Plan 138-01 — nothing else. A one-line edit to any of the first three would silently change four supposedly byte-unchanged frozen harnesses while the 47-surface diff still reported clean; that is the hole this extension closes, and the corpus this phase's predecessors just grew makes it live rather than theoretical.

    Enumerate this phase's own new and appended files explicitly as the expected entries in the broader diff — the V118 append, the 17th harness, its sidecar, the BASELINE_23 comment, the four new validators and the 16th workflow. Anything appearing outside both that list and the sanctioned-exception set is unexplained drift.

    Then run the archival-drift PRE-scan, widened. First half: scan for predecessor validators that read hardcoded phase directory paths which the milestone-archival step will move, and confirm the four new leaves and the apex contain no such reads — the predecessor generation's successful sidestep, which held. Record the second sanctioned option too, so a successor knows it exists: a hardcoded phase path is safe when it is read at a frozen close SHA rather than live. Second half, which a phase-path-only enumeration would miss entirely: scan for predecessor validators that read the live requirements and roadmap documents, since the close-gate rewrites both and the milestone-archival step collapses the roadmap and overwrites the requirements. The fifty-fourth validator does exactly this with a negative assertion attached. Confirm which validators read those two documents live versus which read them at a frozen close SHA, and record both lists — the frozen-aware readers are safe, the live readers are the exposure.

    Record the verdict, the enumerated expected-entry list, and both halves of the scan in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `git diff --name-only <WAVE0_ANCHOR>..HEAD` restricted to the 47-surface inventory returns an empty list; the exact command and its empty output are recorded in the SUMMARY.
    - `git diff --name-only <WAVE0_ANCHOR>..HEAD -- scripts/validation/c17-eee-contract.mjs scripts/validation/_lib/archive-path.mjs scripts/validation/_lib/exec-fail-detail.mjs` returns an empty list.
    - `git diff --numstat <WAVE0_ANCHOR>..HEAD -- scripts/validation/_lib/frozen-at-close.mjs` shows deletions = 0, and the added lines are only the V118 entry, its comment block and its export.
    - `git diff --name-only <WAVE0_ANCHOR>..HEAD` in full matches the enumerated expected-entry list for this phase with no unexplained member; the full list is recorded in the SUMMARY.
    - The archival-drift pre-scan records both halves: a list of predecessor validators reading hardcoded phase directory paths, and a list reading the live requirements and roadmap documents, with each entry marked live-reading or frozen-aware.
    - The four new leaves and the apex are confirmed to contain zero hardcoded archived-phase-path reads.
    - Any HARD-gate failure results in a halt with the offending path named, and no autonomous remediation is attempted.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git diff --name-only $WAVE0_ANCHOR..HEAD -- scripts/validation/c17-eee-contract.mjs scripts/validation/_lib/archive-path.mjs scripts/validation/_lib/exec-fail-detail.mjs)" && test "$(git diff --numstat $WAVE0_ANCHOR..HEAD -- scripts/validation/_lib/frozen-at-close.mjs | awk '{print $2}')" = "0" && echo OK</automated>
  </verify>
  <done>The 47-surface gate is clean, the three byte-frozen shared dependencies show zero diff, the pin library shows additions only, this phase's own new files are the sole other entries, and the archival-drift pre-scan covers both the phase-path class and the roadmap-collapse class.</done>
</task>

<task type="auto">
  <name>Task 3: Axis 1 and Axis 3 local re-audit, publish-bundle regeneration, and the fresh 16-workflow dispatch enumeration</name>
  <precondition>pandoc and PowerShell are both resolvable on PATH (`pandoc --version` and `pwsh -NoProfile -Command 'exit 0'` each exit 0) — the publish-bundle pipeline shells out to both.</precondition>
  <files>(no source files edited; the bundle lands in the gitignored dist directory)</files>
  <read_first>
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md (D-10 for the Windows axis staying advisory and the correction of record that not-run is not clean; D-11 for Axis 3 being corroborating in weight but mandatory in execution; D-12 for full-first and the shallow form as a labelled fallback that must cite and rebut its own prior rejection; D-13 for the depth label; D-08 part i for enumerating the dispatch set fresh)
    - scripts/pipeline/build-publish-bundle.mjs (the version flag handling and the anchored version pattern at lines 40 to 59 — the default without the flag is a stale earlier version, so the flag is mandatory; the Approved-row canary, already bumped by the previous phase)
    - .claude/hooks/publish-bundle-gate.cjs (the Stop-hook gate the regeneration runs under, and its own anchored version pattern)
    - .planning/milestones/v1.15-MILESTONE-AUDIT.md line 221 (the record that the apex was not run on Windows at that close — the honest history this axis must state)
  </read_first>
  <action>
    Capture the two local re-audit axes and regenerate the publish bundle. Neither axis is authoritative — the cross-OS axis in Plan 138-05 is — but both are mandatory in execution and both must be dispositioned honestly.

    Axis 1, the Windows fresh clone, is ADVISORY. Clone the local repository with hardlinks disabled into a scratch directory, then inside the clone run the 17th harness verbose, the three new leaf validators, and the apex verbose with the full recursion. If the deep-nest chain recursion stalls, do not silently omit it: record that it stalled, at what depth, that the item is a known recurring advisory condition whose depth label this milestone advances to the forty-eight-through-one-hundred-thirty-seven span, and that it is non-blocking. Two clean cycles do not retire a known platform timeout, and promoting this axis to blocking would make a flaky platform property a close-blocker against the settled operating-system split. Write the honest history into the record: this axis was not run at two earlier closes and clean at the two most recent — not-run is not the same as clean, and calling it four clean cycles would be false.

    Note that the per-subprocess budgets inside the frozen predecessor apex sit within the byte-unchanged gate and must not be edited; only the workflow-level timeout in the new workflow was ever a free authoring choice, and that was settled in the previous plan.

    Axis 3, the fresh zero-context reproduction, is corroborating in weight but mandatory in execution — it is a named leg in the requirement and in the roadmap's success criterion, so declaring it redundant would un-meet a written criterion by fiat. It runs regardless of what the cross-OS axis later shows. Dispatch a fresh sub-agent carrying no context from the other axes and have it attempt the FULL non-nested apex first. The shallow nesting-guarded form is a labelled fallback, not a default: if it is used, enter it in the cross-axis table as a shape match and explicitly not a count match, scope the exact-match claim to the non-shallow axes, and cite the earlier rejection of that shallow form — that a shallow run changes what is validated, making an exact match vacuous — then rebut it rather than adopting it silently. State plainly that a same-host sub-agent inherits the platform timeout and is therefore not host-independent; do not inflate it.

    Regenerate the publish bundle for this milestone version, passing the version flag explicitly — the builder's default without the flag is a stale earlier version. Run it under the Stop-hook gate. Confirm both new recipes are pandoc-convertible and clean through the docx guard; both were already pre-flighted clean at the previous phase on the first attempt, so a failure here is a regression, not an expected remediation round.

    Finally, enumerate the workflow dispatch set FRESH by listing the workflow directory, and record the resulting names in the SUMMARY as the exact list the owner checkpoint in Plan 138-05 will hand over. There should be sixteen once the new workflow is counted. Never carry a count forward from a prior close: a paths-filtered pull-request cascade would have matched a different, smaller set, and under explicit dispatch the set is chosen rather than inferred.
  </action>
  <acceptance_criteria>
    - Axis 1 is recorded with either the apex summary line from inside the scratch clone or an explicit stall disposition naming the depth and the advisory, non-blocking status — never a silent omission.
    - The Axis 1 record contains the honest-history sentence distinguishing not-run from clean at the two earlier closes.
    - Axis 3 is recorded with an explicit statement of what it attempted first; if the shallow form was used, the record labels it a shape match and not a count match, scopes the exact-match claim accordingly, and cites and rebuts the prior rejection of that form.
    - Axis 3's host-independence is stated plainly, including that a same-host run inherits the platform timeout.
    - The publish bundle for this milestone version exists in the gitignored dist directory, and the build reports zero errors; the recipe conversion and docx-guard results for both new recipes are recorded.
    - `ls .github/workflows/*.yml | wc -l` returns 16, and all sixteen filenames are recorded in the SUMMARY as the owner's dispatch list.
    - The SUMMARY's final section is a hand-over block for the owner checkpoint containing the sixteen workflow filenames and the two exact commands the owner will run.
  </acceptance_criteria>
  <verify>
    <automated>test "$(ls .github/workflows/*.yml | wc -l)" = "16" && ls dist/docs-library-v1.19.zip && echo OK</automated>
  </verify>
  <done>Both local axes are captured with honest dispositions and no silent omissions, the publish bundle regenerates cleanly for this milestone version with both new recipes convertible and guard-clean, and the sixteen-workflow dispatch list is freshly enumerated and handed over.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged planner assumptions

- **Reversibility gating placement.** CONTEXT rates the frozen-surface constraint as one-way. No `<reversibility>` element is emitted on Task 2, because Task 2 does not *implement* that decision — it is a read-only gate that *guards* it, and editing a frozen surface is barred outright by this plan's prohibitions block rather than being a choice the plan offers. The one-way rating and its confirming `checkpoint:decision` live in Plan 138-05, on the push, which is where the actual door is. Recorded here so a reviewer does not read the absent element as an omission.
</flagged_assumptions>

<artifacts_produced>
## Artifacts this phase produces

| Symbol / artifact | Kind | Owner plan |
|---|---|---|
| `MILESTONE_CLOSE_SHAS.V118` (key `'7af8a147'`) | new object key in `_lib/frozen-at-close.mjs` | 138-01 |
| `readAtV118Close` | new export | 138-01 |
| `scripts/validation/v1.19-milestone-audit.mjs` | new file — 17th Path-A harness (C1–C17) | 138-02 |
| `scripts/validation/v1.19-audit-allowlist.json` | new file — sidecar, 59 line-pins in 5 arrays | 138-02 |
| `BASELINE_23` comment block | append-only comment in `regenerate-supervision-pins.mjs` | 138-02 |
| `scripts/validation/check-phase-135.mjs` — `V-135-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-136.mjs` — `V-136-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-137.mjs` — `V-137-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-138.mjs` — `V-138-AUDIT`, `V-138-CHAIN-48`…`V-138-CHAIN-137`, `V-138-AUDIT-HARNESS`, `V-138-SELF` (93 checks) | new chain apex | 138-03 |
| `.github/workflows/audit-harness-v1.19-integrity.yml` — jobs `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`, `check-phase-135`, `check-phase-136`, `check-phase-137`, `check-phase-138`, `rotting-external-quarterly`, `pin-helper-advisory` | new file — 16th CI coexistence workflow | 138-03 |
| `dist/docs-library-v1.19.zip` | build artifact (gitignored) | 138-04 |
| `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` | new doc | 138-06 |
| `.planning/milestones/v1.19-MILESTONE-AUDIT.md` | new doc | 138-06 |
| `.planning/phases/138-.../138-VERIFICATION.md` | new doc — the apex AUDIT resolver target | 138-06 |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave-0 anchor → byte-unchanged verdict | The gate is the only thing proving no unsanctioned frozen-surface drift crept in across four plans; a mis-scoped target list would wave real drift through |
| shared `_lib` module → four frozen harnesses | Outside the 47-surface set, a one-line edit changes frozen behaviour while the primary diff still reports clean |
| axis independence → the exact-match claim | Presenting two same-host runs as independent axes, or a shallow run as a count match, fabricates evidence for a written success criterion |
| local evidence → the one-way push | Everything provable locally must be proven before the irreversible act; a gap here becomes a post-push discovery |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-18 | Tampering | frozen predecessor surfaces | high | mitigate | Diff from the pre-committed Wave-0 anchor over the 47-surface inventory must be empty; any hit halts with the path named and no autonomous remediation |
| T-138-19 | Tampering | shared `_lib` dependencies | high | mitigate | The gate is extended over all four by name; three must be zero-diff, the fourth additions-only with the additions enumerated |
| T-138-20 | Spoofing | a nested run presented as a chain result | high | mitigate | Part 2 is run non-nested one validator at a time; the forbidden full-sweep phrasing is grep-asserted absent from the SUMMARY |
| T-138-21 | Repudiation | the standalone-RED enumeration | medium | mitigate | The set is derived from observed per-validator exit codes rather than asserted; all ten members named, four root-cause classes named, and the will-not-green-on-partial-fix sentence mandatory |
| T-138-22 | Repudiation | axis-independence and axis-omission claims | high | mitigate | Any stall recorded explicitly with depth and status; the not-run-is-not-clean history stated; a shallow Axis 3 entered as shape-match only with its prior rejection cited and rebutted |
| T-138-23 | Denial of Service | the deep-nest cold-clone stall on the Windows axis | medium | accept | Known recurring advisory condition, non-blocking by settled ruling; the depth label advances with the milestone and the frozen per-subprocess budgets are not edited |
| T-138-24 | Tampering | version string flowing into a bundle filename | low | mitigate | The builder's anchored version pattern and the Stop-hook gate's own anchored pattern both already guard path traversal; reuse them, add no new validation |
| T-138-25 | Spoofing | a stale carried cascade count | medium | mitigate | The dispatch set is enumerated fresh by listing the workflow directory and asserted at sixteen; no count is carried forward |

No package-manager installs occur in this plan, so no supply-chain threat (`T-138-SC`) applies. The publish-bundle step shells out to pandoc and PowerShell over the trusted local corpus with no network input.
</threat_model>

<verification>
- Apex green non-nested on an idle machine, summary line captured and compared against the prior plan's record.
- Drift band run non-nested per validator; the ten-member RED set derived from observed exit codes, four root causes named, the partial-fix caveat stated.
- The exponential full-sweep non-attempt stated with measured figures; the forbidden phrasing grep-asserted absent.
- The 47-surface diff empty; three shared dependencies zero-diff; the pin library additions-only with additions enumerated; this phase's own files the sole other entries.
- Archival-drift pre-scan covers both the phase-path class and the live requirements-and-roadmap class.
- Axis 1 and Axis 3 recorded with explicit dispositions and no silent omissions; the publish bundle regenerated for this milestone version; the sixteen-workflow dispatch list enumerated fresh.
</verification>

<success_criteria>
The pre-push evidence base is complete and recorded: the first three parts of the HARN-15 gate with the RED set honestly enumerated, a clean byte-unchanged HARD gate extended over the shared dependencies, a widened archival-drift pre-scan, two honestly dispositioned local axes, a regenerated publish bundle, and a freshly enumerated dispatch list ready for the owner checkpoint.
</success_criteria>

<output>
Create `.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-04-SUMMARY.md` when done. It MUST record the apex summary line, the per-validator drift-band exit codes with the ten-member RED set and its four root-cause classes, the measured doubling figures and the non-attempt statement, the byte-unchanged verdict with its enumerated expected-entry list, both halves of the archival-drift pre-scan, the Axis 1 and Axis 3 dispositions, the publish-bundle result, and a final hand-over block listing the sixteen workflow filenames plus the two exact commands the owner will run.
</output>
