---
phase: 95-harness-lineage-bump-terminal-re-audit-milestone-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONVENTIONS.md
  - scripts/validation/v1.12-milestone-audit.mjs
  - scripts/validation/v1.12-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
autonomous: true
requirements: [HARN-01]
must_haves:
  truths:
    - "v1.12-milestone-audit.mjs runs default with 15 PASS / 0 FAIL / 0 SKIP (C1-C16 inherited verbatim from v1.11; no C17 added)"
    - "v1.12-milestone-audit.mjs --self-test exits 0 with 9/9 passed (self-test preserved, no tests added)"
    - "v1.12-milestone-audit.mjs reads scripts/validation/v1.12-audit-allowlist.json (functional sidecar repoint at line 79 — the CI path-match needle)"
    - "regenerate-supervision-pins.mjs gains a BASELINE_16 comment block closing BASELINE_15 v1.11 carry-over; the BASELINE_9 line-coord array is byte-unchanged"
    - "Atom 1 lands as ONE indivisible commit touching EXACTLY 3 files (milestone-audit + allowlist + regenerate-supervision-pins) — V111 does NOT ride Atom 1"
    - "95-CONVENTIONS.md records the Phase 95 constants lock (V111=919b23b, BASELINE_16 anchor, 4-row cross-OS set, 5 V-94-CONTENT needles, apex [48..94], 26 frozen surfaces, pre-95 anchor ddf1355)"
  artifacts:
    - path: ".planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONVENTIONS.md"
      provides: "Phase 95 constants lock (freshness/SHA matrix + locked strings)"
      contains: "919b23b"
    - path: "scripts/validation/v1.12-milestone-audit.mjs"
      provides: "10th Path-A milestone-audit harness (C1-C16)"
      contains: "v1.12-audit-allowlist.json"
      min_lines: 970
    - path: "scripts/validation/v1.12-audit-allowlist.json"
      provides: "v1.12 sidecar allowlist (c13 reset; quarterly_audit carried)"
      min_lines: 400
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "BASELINE_16 freshness comment (audit-trail only)"
      contains: "BASELINE_16"
  key_links:
    - from: "scripts/validation/v1.12-milestone-audit.mjs"
      to: "scripts/validation/v1.12-audit-allowlist.json"
      via: "readFile sidecar repoint at line 79"
      pattern: "v1\\.12-audit-allowlist\\.json"
---

<objective>
Ship Plan 95-01: the Phase 95 constants-lock document plus HARN-01 Atom 1 — the harness-core Path-A bump (v1.11 → v1.12), the 10th milestone-audit in the lineage v1.4 → … → v1.11 → **v1.12**.

This plan produces TWO commits (per locked D-04 / SD-1):
- **Wave 1a (COMMIT 1, docs):** `95-CONVENTIONS.md` — the freshness/SHA matrix + locked strings every downstream plan (95-02/03/04) reads, so they never re-derive a constant.
- **Wave 1b (COMMIT 2, ATOM 1, HARN-01, INDIVISIBLE, exactly 3 files):** `v1.12-milestone-audit.mjs` (Path-A from v1.11, C1-C16 verbatim, self-test 9/9 preserved, exactly 4 relabel sites) + `v1.12-audit-allowlist.json` (c13_rotting_external carried/reset for v1.12; quarterly_audit carried) + BASELINE_16 freshness comment appended in `regenerate-supervision-pins.mjs`.

Purpose: Lock the Phase 95 constants once, and ship the harness core as a clean revertable atom. **V111 does NOT ride Atom 1** (locked divergence — it rides Atom 2 per ROADMAP SC#2 / HARN-02 / D-04). Atom 1 is exactly 3 files, NOT 4.
Output: `95-CONVENTIONS.md`, `v1.12-milestone-audit.mjs`, `v1.12-audit-allowlist.json`, BASELINE_16 comment in `regenerate-supervision-pins.mjs`; 2 commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONTEXT.md
@.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-RESEARCH.md
@.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 95-CONVENTIONS.md (Phase 95 constants lock) — COMMIT 1</name>
  <files>.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONVENTIONS.md</files>
  <read_first>
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-RESEARCH.md (the source-of-truth for EVERY value below: V111=919b23b, pre-95 anchor ddf1355, 5 needle table at Verification Target 4, 26 frozen surfaces at Verification Target 6, commit map at Verification Target 7, D-01 apex patch text at the "ROADMAP/STATE/REQUIREMENTS D-01 Apex Correction" section)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONTEXT.md (D-01..D-04 locked decisions + Claude's-Discretion lines 259-272)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-PATTERNS.md (per-file copy-from map: the 4-relabel-site table for milestone-audit; the 3-field diff for the allowlist; the BASELINE_16 comment text)
    - .planning/milestones/v1.11-phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-CONVENTIONS.md (Path-A SOURCE — mirror its structure; its 23-frozen-surface list grows to 26 for v1.12 by adding the 3 newly-frozen v1.11 surfaces)
  </read_first>
  <action>
    Author `95-CONVENTIONS.md` as a Path-A copy of `93-CONVENTIONS.md`'s structure (freshness/SHA matrix + locked strings), recording EVERY constant downstream plans (95-02/03/04) consume. Do NOT inline full file bodies — this is a constants reference document; mirror 93-CONVENTIONS.md's table-and-heading format. Sections to include (all values from 95-RESEARCH.md):

    1. **SHAs:** V111 = `919b23b` (v1.11 close-gate `docs(93-04)`, single-commit close — the SUMMARY `f7a7e6b` and state-update `c0be124` that follow are post-close chores, NOT the atom; V111 rides ATOM 2 per HARN-02); pre-Phase-95 anchor = `ddf1355` (`docs(phase-94): complete phase execution` — the predecessor-byte-unchanged diff base); BASELINE_16 anchor = the COMMIT-1 (95-CONVENTIONS) SHA, a known-PAST SHA captured via `git rev-parse --short HEAD` at Atom-1 task time (Task 2); `close_commit: "{phase_95_close_SHA}"` literal placeholder (recover via `git log --all --grep="95-04" --grep="close-gate" --all-match -1 --format=%H`).
    2. **Counts/structural constants:** self-test = 9/9 (preserve, do NOT add tests); milestone-audit default = 15 PASS / 0 FAIL / 0 SKIP; C13 category counts DO NOT CHANGE (6 transient_external + 9 template_placeholder = 15-entry c13_broken_link_allowlist); `c16_missing_endpoint_exemptions: []`; apex CHAIN_PHASES = **[48..94] (47 entries)** per D-01 (NOT [48..93] — that is the corrected off-by-one); CHAIN_SKIP = `new Set([])`; Atom 1 = 3 files; Atom 2 = **4 files** (not 7); close-gate = 7 files; total **5 commits**.
    3. **4-row cross-OS validator set + expected counts (per D-03):** `v1.12-milestone-audit.mjs` (leaf, Windows YES + Linux YES) `15/0/0` cross-OS EXACT MATCH; `check-phase-94.mjs` (leaf, Windows YES + Linux YES) `7/0/0` cross-OS EXACT MATCH (2 base + 5 needles); `check-phase-93.mjs` (chain [48..92], Windows NO/cascades + Linux YES) `~47/0/1` Linux sole-authoritative; `check-phase-95.mjs` (apex [48..94], Windows NO/cascades + Linux YES) `~49/0/1` Linux sole-authoritative (capture at audit time). Record explicitly: BOTH chain rows cascade on cold Windows (corrected OS split — the v1.11 "continuity = fast non-apex" label was WRONG).
    4. **Version-relabel sites (milestone-audit, exactly 4):** line 2 (header lineage v1.11→v1.12, "Path A copy of v1.11", chain "… → v1.11 → v1.12"), line 4 (sidecar comment → `v1.12-audit-allowlist.json` … per Phase 95 close-state), line 35 (Usage → `v1.12-milestone-audit.mjs`), line 79 (functional sidecar repoint → `readFile('scripts/validation/v1.12-audit-allowlist.json')` — the CI path-match needle). DO NOT bump line 5 (frozen-predecessor anchor) or line 90 (Apple Business doc paths). Workflow `name:` → `Audit Harness v1.12 Integrity`; "8th coexistence" → "9th". Lineage label: `phases 62→66→70→74→82→88→93→95, lineage v1.4→v1.12 — 10th entry`.
    5. **The 5 V-94-CONTENT needles (D-02; all VERIFIED present in `docs/macos-lifecycle/02-mdm-migration-psso.md` per 95-RESEARCH Verification Target 4 — case-sensitive verbatim):** `V-94-CONTENT-URLS-IRU` = `support.iru.io` (grep-c 5); `V-94-CONTENT-URLS-KANDJI` = `support.kandji.io` (grep-c 6); `V-94-CONTENT-DOCS-IRU` = `docs.iru.com` (grep-c 6); `V-94-CONTENT-MIGV03` = `Supervision status (MEDIUM confidence)` (grep-c 1 — capital S, lowercase "status"); `V-94-CONTENT-MIGV01` = `learn.microsoft.com` (grep-c 1). PRESENCE target for check-phase-94 = `docs/macos-lifecycle/02-mdm-migration-psso.md`.
    6. **26 predecessor frozen surfaces (byte-unchanged invariant, base anchor ddf1355):** 8 workflow YAMLs (`audit-harness-integrity.yml` v1.4 base + v1.5/v1.6/v1.7/v1.8/v1.9/v1.10/v1.11) + 9 milestone-audit MJS (v1.4/v1.4.1/v1.5/v1.6/v1.7/v1.8/v1.9/v1.10/v1.11) + 9 sidecar JSON (same versions). Note: chain validators `check-phase-{48..94}.mjs` are NOT in the invariant. (Enumerate the full 26-path list verbatim from 95-RESEARCH Verification Target 6 — it is the close-gate gate command.)
    7. **D-01 apex correction patch text (close-gate, Plan 95-04):** record the exact `[48..93]`→`[48..94]` (46→47 entries) patches due in ROADMAP SC#2, STATE.md dep-summary + pending-todo (line 173), REQUIREMENTS HARN-02 (apex range) + HARN-03 (deep-nest depth `[48..93]`→`[48..94]`). Source text in 95-RESEARCH "ROADMAP/STATE/REQUIREMENTS D-01 Apex Correction" section.
    8. **Locked commit-message strings (all 5):** COMMIT 1 `docs(95-01): 95-CONVENTIONS.md — Phase 95 constants lock`; COMMIT 2 `feat(95-01): v1.12 harness-core Path-A — HARN-01 (atomic SC#1 Atom 1)`; COMMIT 3 `feat(95-02): v1.12 validators + V111 pin + CI surface — HARN-02 (atomic SC#1 Atom 2)`; COMMIT 4 `docs(95-03): HARN-03 3-axis terminal re-audit results (artifact-only)`; COMMIT 5 `docs(95-04): Phase 95 close-gate — v1.12 MILESTONE-AUDIT + DEFERRED-CLEANUP + 4-doc traceability + v1.12 MILESTONE CLOSE`.
    9. **DEFERRED-CLEANUP ledger (SD-3) for 95-04:** DROP the 3 resolved MIGV items (INTUNE-PROFILE-ENROLLMENT-01→MIGV-01, IRU-CONSOLE-DELETE-01→MIGV-02, SUPERVISION-STATUS-POST-MIGRATION-01→MIGV-03 — recorded Closed in MILESTONE-AUDIT, not silently deleted); CARRY open items (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 depth→[48..94]; MTPSSO-01/02/03; KRBFUT-01/02; MIGFUT-01/02; all v1.8 Part C incl. CI-3 Managed-Apple-ID→Account rename); ADD GLOSSARY-IRU-URL-FRESHNESS-01 (corpus follow-up, NOT a tooling-close edit).
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONVENTIONS.md','utf8'); const need=['919b23b','ddf1355','BASELINE_16','48..94','v1.12-audit-allowlist.json','support.iru.io','support.kandji.io','docs.iru.com','Supervision status (MEDIUM confidence)','learn.microsoft.com','62→66→70→74→82→88→93→95','GLOSSARY-IRU-URL-FRESHNESS-01']; const miss=need.filter(n=>!f.includes(n)); if(miss.length){console.error('MISSING:',miss);process.exit(1)} console.log('95-CONVENTIONS.md constants present')"</automated>
  </verify>
  <acceptance_criteria>
    - `95-CONVENTIONS.md` exists and contains: `919b23b`, `ddf1355`, `BASELINE_16`, `48..94`, `v1.12-audit-allowlist.json`, all 5 needle strings (incl. verbatim `Supervision status (MEDIUM confidence)` and `learn.microsoft.com`), the lineage string `62→66→70→74→82→88→93→95`, and `GLOSSARY-IRU-URL-FRESHNESS-01`.
    - The document records apex `[48..94]` (47 entries), NOT `[48..93]` — and explicitly notes the D-01 off-by-one correction owed to ROADMAP/STATE/REQUIREMENTS at close-gate.
    - The full 26-frozen-surface list is enumerated (8 YAMLs + 9 milestone-audit MJS + 9 sidecar JSON).
    - COMMIT 1 message is exactly: `docs(95-01): 95-CONVENTIONS.md — Phase 95 constants lock`
    - COMMIT 1 touches ONLY `95-CONVENTIONS.md` (docs-only; cannot pollute either atom): `git show --stat HEAD --format=` lists exactly one file.
  </acceptance_criteria>
  <done>95-CONVENTIONS.md authored and committed as COMMIT 1; constants verification passes; apex recorded as [48..94] per D-01.</done>
</task>

<task type="auto">
  <name>Task 2: Atom 1 — v1.12 harness-core Path-A (HARN-01, INDIVISIBLE, exactly 3 files) — COMMIT 2</name>
  <files>scripts/validation/v1.12-milestone-audit.mjs, scripts/validation/v1.12-audit-allowlist.json, scripts/validation/regenerate-supervision-pins.mjs</files>
  <read_first>
    - scripts/validation/v1.11-milestone-audit.mjs (PATH-A SOURCE for v1.12-milestone-audit.mjs — 979 lines; copy verbatim, relabel ONLY lines 2/4/35/79 per 95-PATTERNS.md diff table)
    - scripts/validation/v1.11-audit-allowlist.json (PATH-A SOURCE for v1.12-audit-allowlist.json; copy verbatim, change ONLY `generated` and `phase` header fields per 95-PATTERNS.md)
    - scripts/validation/regenerate-supervision-pins.mjs (the file being MODIFIED — BASELINE_15 block at lines 446-452 is the mirror template; the `const BASELINE_9 = [` array at line 453 must NOT change)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-PATTERNS.md (the exact 4-relabel-site table for milestone-audit; the 3-field allowlist diff; the BASELINE_16 comment text with the {Wave-1-commit-SHA} anchor rule)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-RESEARCH.md (Verification Target 2 + Verification Target 3 — exact line anchors)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONVENTIONS.md (constants lock authored in Task 1)
  </read_first>
  <action>
    Create the 3 Atom-1 files as Path-A copies. This is ONE indivisible commit (exactly 3 files).

    **File 1 — `v1.12-milestone-audit.mjs`:** Copy `v1.11-milestone-audit.mjs` verbatim, then apply the 4 relabel sites ONLY (per 95-PATTERNS.md diff table):
    - Line 2 (header lineage): `v1.11` → `v1.12`; "Path A copy of v1.10" → "Path A copy of v1.11"; extend chain `… → v1.10 → v1.11` → `… → v1.11 → v1.12`; C1-C16 inherited verbatim.
    - Line 4 (sidecar comment): sidecar name → `scripts/validation/v1.12-audit-allowlist.json`; "Path-A from v1.10 … per Phase 93 close-state" → "Path-A from v1.11 … per Phase 95 close-state".
    - Line 35 (Usage line): `node scripts/validation/v1.12-milestone-audit.mjs [--verbose] [--self-test]`.
    - Line 79 (functional sidecar repoint — the line the CI path-match job greps): `readFile('scripts/validation/v1.12-audit-allowlist.json')`.
    DO NOT touch: line 5 (frozen-predecessor anchor), line 90 (Apple Business doc paths), the `checks` array (stays exactly 15 entries — NO C17), the self-test block (count MUST stay 9/9), the C13 category-count assertion (expects 6 transient_external / 9 template_placeholder). Line count stays 979. Default run MUST stay 15 PASS / 0 FAIL / 0 SKIP.

    **File 2 — `v1.12-audit-allowlist.json`:** Copy `v1.11-audit-allowlist.json` verbatim, then change ONLY the 3 header fields:
    - `generated`: → `"2026-06-26T00:00:00Z"` (authoring day — today).
    - `phase`: → `"95-harness-lineage-bump-terminal-re-audit-milestone-close"` (long-slug form).
    - (the sidecar filename itself is `v1.12-audit-allowlist.json`).
    Carry forward verbatim: `schema_version: "1.1"`; all `safetynet_exemptions`, `supervision_exemptions` (C13 hard-asserts exact count of 15), `cope_banned_phrases`, `c7_knox_allowlist`, `c9_exemptions`; `c13_broken_link_allowlist` (15 entries = 6 transient_external + 9 template_placeholder); `c13_rotting_external` block (`ci_1`, `ci_2_vpp_location_token`, `ci_3` — carried/reset for v1.12); `quarterly_audit.cadence`/`scope`/`tool`; `quarterly_audit.next_review: "2027-01-01"`; `c16_missing_endpoint_exemptions: []`. Changing any carried count breaks C-checks.

    **File 3 — `regenerate-supervision-pins.mjs` (EDIT, comment-only append):** Insert a 7-line BASELINE_16 comment block immediately after line 452 (before `const BASELINE_9 = [` at line 453), mirroring the BASELINE_15 wording exactly (per 95-PATTERNS.md / 95-RESEARCH Verification Target 3): "BASELINE_16 refreshed 2026-06-26 (Phase 95 Plan 95-01): closes BASELINE_15 v1.11 carry-over per HARN-01 contract … v1.12 line positions verified against HEAD `<COMMIT-1 95-CONVENTIONS SHA>` (Phase 95 Wave-1 commit — 95-CONVENTIONS.md constants lock)"; "BASELINE_9 entries above remain unchanged — Phase 95 does NOT alter the line-coord array"; resolution path mentions BASELINE_17 at next milestone close (… → v1.11 → BASELINE_15 → v1.12 → BASELINE_16). Capture the anchor SHA via `git rev-parse --short HEAD` AFTER COMMIT 1 landed (this is a known-PAST SHA). REPLACE the `{Wave-1-commit-SHA}` placeholder with the actual SHA before committing — do NOT leave a placeholder, and do NOT anchor to the future Phase-95 close SHA. DO NOT alter the BASELINE_9 line-coord array.

    Commit all 3 files as ONE atom with the locked COMMIT 2 message. V111 is NOT added here (it rides Atom 2 in Plan 95-02).
  </action>
  <verify>
    <automated>node scripts/validation/v1.12-milestone-audit.mjs --self-test && echo "---DEFAULT---" && node scripts/validation/v1.12-milestone-audit.mjs | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/v1.12-milestone-audit.mjs --self-test` exits 0 with "9 passed, 0 failed" (9/9 preserved).
    - `node scripts/validation/v1.12-milestone-audit.mjs` (default) reports 15 PASS / 0 FAIL / 0 SKIP and exits 0.
    - `grep -c "v1.12-audit-allowlist.json" scripts/validation/v1.12-milestone-audit.mjs` >= 2 (line 4 comment + line 79 repoint); `grep -c "v1.11-audit-allowlist" scripts/validation/v1.12-milestone-audit.mjs` == 0 (no stale v1.11 sidecar reference).
    - `node -e "const j=require('./scripts/validation/v1.12-audit-allowlist.json'); if(j.phase!=='95-harness-lineage-bump-terminal-re-audit-milestone-close')process.exit(1)"` exits 0 (valid JSON, phase repointed).
    - `grep -c "BASELINE_16" scripts/validation/regenerate-supervision-pins.mjs` >= 1; the BASELINE_16 comment block contains a real 7-char SHA anchor (no literal `{Wave-1-commit-SHA}` placeholder remains: `grep -c "{Wave-1-commit-SHA}" scripts/validation/regenerate-supervision-pins.mjs` == 0); `git diff --stat` shows regenerate-supervision-pins.mjs as comment-additions only (BASELINE_9 array byte-unchanged).
    - COMMIT 2 message is exactly: `feat(95-01): v1.12 harness-core Path-A — HARN-01 (atomic SC#1 Atom 1)`
    - COMMIT 2 touches EXACTLY 3 files: `git show --stat HEAD --format=` lists only `v1.12-milestone-audit.mjs`, `v1.12-audit-allowlist.json`, `regenerate-supervision-pins.mjs`. `_lib/frozen-at-close.mjs` is NOT touched here (V111 rides Atom 2).
  </acceptance_criteria>
  <done>Atom 1 (exactly 3 files) committed as COMMIT 2; self-test 9/9 and default 15/0/0 both green; sidecar repoint confirmed; BASELINE_16 comment present with a real past-SHA anchor and BASELINE_9 array unchanged; V111 deliberately absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — local CLI tooling) | Phase 95 Plan 95-01 is tooling-ONLY: validation `.mjs` scripts + sidecar JSON + a planning markdown. No user input, no network endpoints, no auth, no secrets, no runtime data handling. |

## STRIDE Threat Register

ASVS L1 review of this plan found **no high-severity items** — a docs/validation-tooling plan with a near-zero threat surface. No untrusted input, no secrets, no auth surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01-T | Tampering | Atom 1 silently mutating C1-C16 / self-test count | mitigate | Acceptance gate asserts self-test 9/9 + default 15/0/0 + C13 counts unchanged; any drift fails the gate. |
| T-95-01-I | Integrity | Stale `v1.11-audit-allowlist` reference left in v1.12 harness | mitigate | Acceptance gate asserts `grep -c "v1.11-audit-allowlist"` == 0 in the v1.12 harness. |
| T-95-01-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | NO package installs in this plan (pure Path-A copy of existing in-tree scripts; no new deps). RESEARCH confirms no install tasks → no Package Legitimacy Gate required. |
</threat_model>

<verification>
- `node scripts/validation/v1.12-milestone-audit.mjs --self-test` → 9 passed, 0 failed (exit 0).
- `node scripts/validation/v1.12-milestone-audit.mjs` → 15 PASS / 0 FAIL / 0 SKIP (exit 0).
- COMMIT 1 = docs-only (95-CONVENTIONS.md). COMMIT 2 = exactly 3 files (the Atom-1 set), `_lib/frozen-at-close.mjs` absent.
- `node -e "require('./scripts/validation/v1.12-audit-allowlist.json')"` parses with `phase` repointed to 95.
- BASELINE_16 comment present with a real past-SHA anchor; BASELINE_9 array byte-unchanged.
</verification>

<success_criteria>
- ROADMAP SC#1 satisfied: Atom 1 ships as ONE indivisible commit — `v1.12-milestone-audit.mjs` (Path-A, C1-C16) + `v1.12-audit-allowlist.json` + BASELINE_16 in `regenerate-supervision-pins.mjs`.
- 95-CONVENTIONS.md locks all Phase 95 constants (separate docs-only COMMIT 1), recording apex `[48..94]` per D-01.
- self-test 9/9 and default 15/0/0 both green; sidecar repointed; BASELINE_9 array unchanged.
- Atom 1 = exactly 3 files; V111 deliberately absent (rides Atom 2 per locked divergence).
</success_criteria>

<output>
Create `.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-01-SUMMARY.md` when done. Record: COMMIT-1 SHA (the BASELINE_16 anchor), COMMIT-2 (Atom-1) SHA, the captured BASELINE_16 anchor SHA, self-test/default counts observed, and the pre-Phase-95 anchor `ddf1355` for the downstream byte-unchanged gate.
</output>
</content>
</invoke>
