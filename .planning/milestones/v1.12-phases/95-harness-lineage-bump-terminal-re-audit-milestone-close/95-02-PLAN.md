---
phase: 95-harness-lineage-bump-terminal-re-audit-milestone-close
plan: 02
type: execute
wave: 2
depends_on: ["95-01"]
files_modified:
  - scripts/validation/check-phase-94.mjs
  - scripts/validation/check-phase-95.mjs
  - scripts/validation/_lib/frozen-at-close.mjs
  - .github/workflows/audit-harness-v1.12-integrity.yml
autonomous: true
requirements: [HARN-02]
must_haves:
  truths:
    - "check-phase-94 is a leaf validator (CHAIN_PHASES=[]) carrying V-94-PRESENCE + 5 V-94-CONTENT-* needles + V-94-SELF → 7 PASS / 0 FAIL / 0 SKIP"
    - "check-phase-94 hard-asserts all 5 MIGV needles (support.iru.io, support.kandji.io, docs.iru.com, 'Supervision status (MEDIUM confidence)', learn.microsoft.com) against docs/macos-lifecycle/02-mdm-migration-psso.md with no allowlist"
    - "check-phase-95 is chain-apex: CHAIN_PHASES covers 48..94 (47 entries) per D-01, CHAIN_SKIP=new Set([]), V-95-SELF asserts 95 absent; HARNESS repointed to v1.12-milestone-audit.mjs"
    - "_lib/frozen-at-close.mjs gains V111:'919b23b' entry + readAtV111Close export (single entry, no V111_CLOSEGATE)"
    - "audit-harness-v1.12-integrity.yml ships as the 9th coexistence workflow with check-phase-94 + check-phase-95 jobs; predecessors v1.4-v1.11 byte-unchanged; 4 linux-chain contracts intact"
    - "Atom 2 lands as ONE indivisible commit touching EXACTLY 4 files; V111 rides Atom 2 (locked divergence)"
    - "Atom 2 is committed AND pushed to origin/master (hard ordering gate for Plan 95-03)"
  artifacts:
    - path: "scripts/validation/check-phase-95.mjs"
      provides: "chain-apex validator CHAIN_PHASES=[48..94]"
      contains: "CHAIN_SKIP = new Set([])"
    - path: "scripts/validation/check-phase-94.mjs"
      provides: "V-94-PRESENCE + V-94-CONTENT-* 5-needle leaf validator"
      contains: "Supervision status (MEDIUM confidence)"
    - path: "scripts/validation/_lib/frozen-at-close.mjs"
      provides: "V111 frozen-close pin + readAtV111Close"
      contains: "readAtV111Close"
    - path: ".github/workflows/audit-harness-v1.12-integrity.yml"
      provides: "9th CI coexistence workflow"
      contains: "check-phase-95"
  key_links:
    - from: ".github/workflows/audit-harness-v1.12-integrity.yml"
      to: "scripts/validation/check-phase-95.mjs"
      via: "linux-chain-ubuntu-latest job runs the apex"
      pattern: "check-phase-95"
    - from: "scripts/validation/_lib/frozen-at-close.mjs"
      to: "MILESTONE_CLOSE_SHAS.V111"
      via: "readAtClose('V111', p)"
      pattern: "V111.*919b23b"
---

<objective>
Ship Plan 95-02: HARN-02 Atom 2 — the v1.12 per-phase validators + V111 frozen-close pin + 9th CI coexistence workflow. ONE indivisible commit, EXACTLY 4 files (COMMIT 3).

Files (all Path-A copies / minimal appends per locked D-01/D-02/D-04):
- `check-phase-94.mjs` — leaf validator (base shape from `check-phase-84.mjs` + `{id,file,needle}` FORM from `check-phase-92.mjs`): V-94-PRESENCE + 5 V-94-CONTENT-* MIGV needles + V-94-SELF (D-02). check-phase-94 is a CONTENT-PATCH validator (guide 02 pre-exists v1.11) — bare PRESENCE is meaningless, so the 5 content needles are load-bearing.
- `check-phase-95.mjs` — chain-apex from `check-phase-93.mjs`, range bumped [48..92] → **[48..94] (47 entries)** per D-01 (CORRECTS the ROADMAP/STATE off-by-one), CHAIN_SKIP empty Set, V-95-SELF dual-invariant, HARNESS → v1.12-milestone-audit.mjs.
- `_lib/frozen-at-close.mjs` (EDIT) — add `V111: '919b23b'` entry + `readAtV111Close` export. **LOCKED DIVERGENCE: V111 rides ATOM 2 here, NOT Atom 1** (per ROADMAP SC#2 + HARN-02).
- `audit-harness-v1.12-integrity.yml` — 9th coexistence workflow from `audit-harness-v1.11-integrity.yml`, jobs repointed to check-phase-94 + check-phase-95.

Purpose: Land the complete v1.12 validator surface + frozen pin + CI as one revertable atom. **Ordering-safe:** `919b23b` is a known-PAST SHA and check-phase-95 reads only PRIOR-milestone closes via `_lib/frozen-at-close.mjs` → no Commit A.
Output: 2 net-new validators + frozen-at-close edit + CI workflow; ONE commit; pushed to origin/master.

**HARD ordering gate (D-03):** This atom MUST be committed AND pushed to `origin/master` before Plan 95-03 runs — the v1.12 workflow's check-phase-94/95 jobs FAIL (not skip) if absent from the dispatched ref.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONVENTIONS.md
@.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONTEXT.md
@.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-RESEARCH.md
@.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: check-phase-94 (leaf, V-94-PRESENCE + 5 V-94-CONTENT-* + V-94-SELF)</name>
  <files>scripts/validation/check-phase-94.mjs</files>
  <read_first>
    - scripts/validation/check-phase-84.mjs (PATH-A SOURCE for the base leaf shape — CHAIN_PHASES=[], CHAIN_SKIP=new Set([]), DELIVERABLE const, V-NN-PRESENCE, V-NN-SELF dual-invariant, readFile CRLF-normalize, runner loop)
    - scripts/validation/check-phase-92.mjs (PATH-A SOURCE FORM for the content needles — {id,file,needle} array + loop at lines 46-74, CRLF-normalized read, forward-slash includes() substring, NO allowlist/sidecar)
    - scripts/validation/check-phase-81.mjs (confirms the {id,file,needle} FORM consistency — SSO_EDGES array at lines 45-54)
    - docs/macos-lifecycle/02-mdm-migration-psso.md (the target file the 5 needles assert against — all 5 verified present per 95-RESEARCH Verification Target 4)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONVENTIONS.md (the 5 V-94-CONTENT needles + PRESENCE target — locked in Plan 95-01)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-PATTERNS.md (the check-phase-94 composite assembly: header, CONTENT_NEEDLES array, PRESENCE/CONTENT/SELF blocks, expected 7/0/0)
  </read_first>
  <action>
    Create `check-phase-94.mjs` as a leaf validator: start from the `check-phase-84.mjs` base shape, then splice in the `check-phase-92.mjs` `{id,file,needle}` FORM for the 5 V-94-CONTENT checks. Substitute phase 84→94 throughout (id strings, V-NN-PRESENCE / V-NN-SELF names, SELF guard `.includes(94)`, header, console label). NO chain — `CHAIN_PHASES = []`, `CHAIN_SKIP = new Set([])` (chain lives ONLY in apex check-phase-95).

    **DELIVERABLE / PRESENCE:** `DELIVERABLE = 'docs/macos-lifecycle/02-mdm-migration-psso.md'` (pre-existing v1.11 file, patched by Phase 94). V-94-PRESENCE asserts file exists + non-empty.

    **CONTENT_NEEDLES array (D-02 — exactly these 5, case-sensitive verbatim from 95-CONVENTIONS.md):**
    - `{ id: 'URLS-IRU', file: DELIVERABLE, needle: 'support.iru.io' }`
    - `{ id: 'URLS-KANDJI', file: DELIVERABLE, needle: 'support.kandji.io' }`
    - `{ id: 'DOCS-IRU', file: DELIVERABLE, needle: 'docs.iru.com' }`
    - `{ id: 'MIGV03', file: DELIVERABLE, needle: 'Supervision status (MEDIUM confidence)' }` (capital S, lowercase "status")
    - `{ id: 'MIGV01', file: DELIVERABLE, needle: 'learn.microsoft.com' }`
    Loop over CONTENT_NEEDLES pushing `CONTENT-${e.id}` checks named `V-94-CONTENT-${e.id}`; read via the CRLF-normalizing readFile; assert `c.includes(e.needle)`. NO allowlist/sidecar (mirrors C16's empty `c16_missing_endpoint_exemptions: []`). Add a header note explaining WHY content needles are load-bearing (guide 02 pre-exists; bare PRESENCE is trivially green on old bytes).

    **V-94-SELF:** dual-invariant guard — `if (CHAIN_PHASES.includes(94))` FAIL + `if (CHAIN_SKIP.size !== 0)` FAIL (the Phase 68 7b635ca empty-Set invariant). Copy the shared readFile CRLF-normalizer and runner loop verbatim from check-phase-84.mjs. Total = 7 checks (PRESENCE + 5 CONTENT + SELF) → 7 PASS / 0 FAIL / 0 SKIP.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-94.mjs | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-94.mjs` exits 0 with `Result: 7 PASS, 0 FAIL, 0 SKIPPED`.
    - check-phase-94 contains all 5 needle strings verbatim: `grep -c "support.iru.io" scripts/validation/check-phase-94.mjs` >= 1, and likewise for `support.kandji.io`, `docs.iru.com`, `Supervision status (MEDIUM confidence)`, `learn.microsoft.com`.
    - check-phase-94 contains `V-94-CONTENT-URLS-IRU` … `V-94-CONTENT-MIGV01` ids (5 distinct), a `V-94-PRESENCE`, and a `V-94-SELF` with `.includes(94)`.
    - check-phase-94 contains `CHAIN_PHASES = []` and `CHAIN_SKIP = new Set([])` (leaf — no chain).
    - DELIVERABLE = `docs/macos-lifecycle/02-mdm-migration-psso.md`.
  </acceptance_criteria>
  <done>check-phase-94.mjs created as a leaf validator; reports 7/0/0; all 5 MIGV needles hard-asserted; SELF dual-invariant present.</done>
</task>

<task type="auto">
  <name>Task 2: check-phase-95 apex [48..94] + frozen-at-close V111 + 9th CI workflow; commit Atom 2 (4 files) and push</name>
  <files>scripts/validation/check-phase-95.mjs, scripts/validation/_lib/frozen-at-close.mjs, .github/workflows/audit-harness-v1.12-integrity.yml</files>
  <read_first>
    - scripts/validation/check-phase-93.mjs (PATH-A SOURCE for check-phase-95.mjs — chain-apex: HARNESS const line 38, CHAIN_PHASES [48..92] line 41, CHAIN_SKIP line 44, V-NN-AUDIT lines 48-64, NESTED child-spawn line 71 + loop, V-NN-AUDIT-HARNESS lines 108-128, V-NN-SELF lines 130-144, runner)
    - scripts/validation/_lib/frozen-at-close.mjs (the file being MODIFIED — V110 entry at lines 31-33 + readAtV110Close export at line 66 are the mirror templates; last entry is currently V110='a3617e9', no V111)
    - .github/workflows/audit-harness-v1.11-integrity.yml (PATH-A SOURCE for the 9th workflow; job structure + the 4 linux-chain contract values fetch-depth:0 / core.autocrlf false / continue-on-error:false / timeout-minutes:30 + CHAIN_TIMING_LINUX ::notice)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-PATTERNS.md (apex constants + NESTED block + frozen-at-close V111 insert + workflow relabel diff table)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONVENTIONS.md (V111=919b23b, apex [48..94], lineage label, 9th-coexistence string)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-RESEARCH.md (Verification Target 1 V111 entry shape + Verification Target 2 CHAIN_PHASES 47-entry array)
  </read_first>
  <action>
    Create the apex validator + frozen pin + CI workflow, then commit all 4 Atom-2 files together and push.

    **check-phase-95.mjs (copy from check-phase-93.mjs):** all `93`→`95` in id/name/header/SELF strings and console label. `HARNESS = 'scripts/validation/v1.12-milestone-audit.mjs'`. `CHAIN_PHASES` = explicit 47-entry integer array `[48,49,…,94]` (every integer 48 through 94 inclusive — adds 93 and 94 to the v1.11 apex per D-01; this is `[48..94]` NOT `[48..93]`). `CHAIN_SKIP = new Set([])` (verbatim, Phase 68 `7b635ca` empty-Set invariant). V-95-AUDIT → `resolveArchivedPhasePath('95-harness-lineage-bump-terminal-re-audit-milestone-close/95-VERIFICATION.md', ['v1.12-phases'])`, regex `/Phase 95/i` (SKIP-PASS until Plan 95-04 authors 95-VERIFICATION.md). Copy the NESTED child-spawn block (`CHECK_PHASE_NESTED=1`, child env, execFileSync, peer-timeout logic) VERBATIM. V-95-AUDIT-HARNESS runs `v1.12-milestone-audit.mjs`. V-95-SELF dual-invariant: `if (CHAIN_PHASES.includes(95))` FAIL + `if (CHAIN_SKIP.size !== 0)` FAIL. Runner loop verbatim (LABEL_WIDTH=60, `Result: N PASS, N FAIL, N SKIPPED`, `process.exit(failed>0?1:0)`).

    **_lib/frozen-at-close.mjs (EDIT — 2 appends, NO second entry):** After the V110 entry (line 33), inside `MILESTONE_CLOSE_SHAS`, add `V111: '919b23b',` with a comment mirroring the V110 comment shape (Phase 93 Plan 93-04 — v1.11 milestone close-gate `docs(93-04)`; single entry; no separate V111_CLOSEGATE). After the `readAtV110Close` export (line 66), add `export const readAtV111Close = (p) => readAtClose('V111', p);` (mirror the V110 export spacing). NO `V111_CLOSEGATE` (v1.11 closed in ONE commit → atom == close-gate; the post-close `f7a7e6b`/`c0be124` commits are chores, NOT the atom). This is the LOCKED DIVERGENCE — V111 rides Atom 2.

    **audit-harness-v1.12-integrity.yml (copy from audit-harness-v1.11-integrity.yml):** all `v1.11`→`v1.12` repoints. Header "8th coexistence" → "9th"; `name:` → `Audit Harness v1.12 Integrity`. `on.pull_request.paths` repoint v1.11-* → v1.12-* (harness `v1.12-*`, `audit-harness-v1.12-integrity.yml`, `v1.12-MILESTONE-AUDIT.md`, `v1.12-DEFERRED-CLEANUP.md`). Crons verbatim. `parse` job → validate `v1.12-audit-allowlist.json`. `path-match` job → grep `v1.12-audit-allowlist.json` in `v1.12-milestone-audit.mjs`. `harness-run` → `node scripts/validation/v1.12-milestone-audit.mjs --verbose`. `linux-chain-ubuntu-latest` → run apex `check-phase-95.mjs --verbose` ("recursively spawns 48..94"), KEEP all 4 contract values (fetch-depth:0, core.autocrlf false, continue-on-error:false, timeout-minutes:30) + CHAIN_TIMING_LINUX `::notice`. Per-validator standalone jobs: REPLACE the 5 check-phase-89..93 jobs with exactly 2 jobs `check-phase-94` + `check-phase-95` (fetch-depth:0, timeout-minutes:15, continue-on-error:false, needs: harness-run). `rotting-external-quarterly` → carry verbatim (cron-only `if:` guard SKIPs on workflow_dispatch — negative control); repoint sidecar to `v1.12-audit-allowlist.json`. `pin-helper-advisory` → carry verbatim (continue-on-error:true).

    Commit all 4 files (check-phase-94.mjs from Task 1 + check-phase-95.mjs + _lib/frozen-at-close.mjs + audit-harness-v1.12-integrity.yml) as ONE atom with the locked COMMIT 3 message, then `git push origin master`. Verify the push reached origin (hard ordering gate for 95-03).
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('scripts/validation/check-phase-95.mjs','utf8'); if(!/CHAIN_PHASES\s*=\s*\[48,/.test(f)||!f.includes(',94]')||!f.includes('new Set([])')||!f.includes('v1.12-milestone-audit.mjs')){console.error('apex shape wrong');process.exit(1)} if(f.includes(',95]')){console.error('apex must NOT include 95');process.exit(1)} console.log('apex ok')" && node -e "const f=require('fs').readFileSync('scripts/validation/_lib/frozen-at-close.mjs','utf8'); if(!f.includes('V111:')||!f.includes('919b23b')||!f.includes('readAtV111Close')){console.error('frozen V111 missing');process.exit(1)} if(f.includes('V111_CLOSEGATE')){console.error('forbidden V111_CLOSEGATE present');process.exit(1)} console.log('frozen V111 ok')" && node -e "const y=require('fs').readFileSync('.github/workflows/audit-harness-v1.12-integrity.yml','utf8'); ['check-phase-94','check-phase-95','fetch-depth: 0','continue-on-error: false','timeout-minutes: 30','Audit Harness v1.12'].forEach(s=>{if(!y.includes(s)){console.error('workflow missing '+s);process.exit(1)}}); console.log('workflow ok')"</automated>
  </verify>
  <acceptance_criteria>
    - check-phase-95 contains a `CHAIN_PHASES` array covering every integer 48..94 (47 entries) and `CHAIN_SKIP = new Set([])`; it does NOT include 95 (`grep -c ",95]" scripts/validation/check-phase-95.mjs` == 0 for the CHAIN_PHASES literal).
    - check-phase-95 `V-95-SELF` asserts `CHAIN_PHASES.includes(95)` is false; HARNESS const = `scripts/validation/v1.12-milestone-audit.mjs`. (Note: running check-phase-95 on warm-tree Windows may show deep-nest cascade noise — that is expected; the authoritative apex count comes from Linux GHA in Plan 95-03.)
    - `_lib/frozen-at-close.mjs` contains `V111:`, `'919b23b'`, and `readAtV111Close`; contains NO `V111_CLOSEGATE`.
    - `audit-harness-v1.12-integrity.yml` contains `check-phase-94`, `check-phase-95`, `Audit Harness v1.12`, `fetch-depth: 0`, `continue-on-error: false`, `timeout-minutes: 30`; the standalone-job section references exactly check-phase-94 + check-phase-95 (no check-phase-89..93 jobs).
    - Predecessor byte-unchanged so far: `git diff HEAD~1 HEAD -- .github/workflows/audit-harness-v1.11-integrity.yml scripts/validation/v1.11-milestone-audit.mjs scripts/validation/v1.11-audit-allowlist.json` is EMPTY (no v1.11 surface modified by Atom 2).
    - COMMIT 3 message is exactly: `feat(95-02): v1.12 validators + V111 pin + CI surface — HARN-02 (atomic SC#1 Atom 2)`
    - COMMIT 3 touches EXACTLY 4 files: `check-phase-94.mjs` + `check-phase-95.mjs` + `_lib/frozen-at-close.mjs` + `audit-harness-v1.12-integrity.yml` (`git show --stat HEAD --format=` lists only these 4).
    - Atom 2 is pushed: `git log origin/master --oneline -1` shows the Atom-2 commit (HARD ordering gate for Plan 95-03 satisfied).
  </acceptance_criteria>
  <done>Atom 2 (exactly 4 files) committed as COMMIT 3 and pushed to origin/master; apex CHAIN=[48..94] (47 entries, 95 absent); V111='919b23b' pin present (no V111_CLOSEGATE); 9th workflow in place with 4 contracts intact; v1.11 surfaces byte-unchanged; ordering gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → GitHub Actions runner | The only boundary: `audit-harness-v1.12-integrity.yml` runs in GitHub-hosted Actions. It executes in-repo `.mjs` validators against a pinned ref; no external input, no secrets, no network calls beyond the checkout. |

## STRIDE Threat Register

ASVS L1 review found **no high-severity items**. The integrity controls are predecessor-byte-unchanged + `continue-on-error: false`. The only relevant consideration is CI-workflow supply-chain hygiene — the v1.12 workflow mirrors the v1.11 predecessor's posture (pinned `actions/checkout@v4` + `actions/setup-node@v4`, least-privilege default token, no secrets), which is itself the frozen-surface byte-unchanged precedent.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-02-T | Tampering | A v1.4-v1.11 frozen surface mutated while authoring the 9th workflow | mitigate | Acceptance gate asserts `git diff` of the v1.11 workflow/harness/sidecar is EMPTY; full 26-surface gate runs at close-gate (95-04). |
| T-95-02-E | Elevation/Integrity | CI workflow with `continue-on-error: true` masking a real validator failure | mitigate | linux-chain + check-phase-94/95 jobs keep `continue-on-error: false` (verified in acceptance); only `pin-helper-advisory` is advisory (true) by design — carried verbatim from the frozen v1.11 precedent. |
| T-95-02-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | NO package installs (Path-A copies of in-tree scripts; workflow uses pinned `actions/checkout@v4` + `actions/setup-node@v4` + node only, no new npm deps). No Package Legitimacy Gate required. |
</threat_model>

<verification>
- check-phase-94 → 7/0/0 (verified in Task 1).
- check-phase-95 CHAIN_PHASES=[48..94] (47 entries) + CHAIN_SKIP empty + HARNESS→v1.12; 95 absent.
- frozen-at-close: V111='919b23b' + readAtV111Close, single entry, no V111_CLOSEGATE.
- 9th workflow: check-phase-94/95 jobs + 4 contract values intact; v1.11 surfaces byte-unchanged.
- Atom 2 = exactly 4 files, one commit, pushed to origin/master.
</verification>

<success_criteria>
- ROADMAP SC#2 satisfied: Atom 2 ships as ONE indivisible commit — check-phase-94 + check-phase-95 + frozen-at-close V111 (`919b23b`) + audit-harness-v1.12-integrity.yml (9th); predecessors v1.4-v1.11 byte-unchanged.
- D-01 correction applied: apex CHAIN_PHASES=[48..94] (47 entries), NOT [48..93].
- V111 rides Atom 2 (locked divergence); no Commit A; ordering-safe.
- Atom 2 pushed to origin/master — hard precondition for Plan 95-03 met.
</success_criteria>

<output>
Create `.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-02-SUMMARY.md` when done. Record: COMMIT-3 (Atom-2) SHA, origin/master push confirmation, check-phase-94 local count (7/0/0), the apex CHAIN_PHASES=[48..94] confirmation, and a note that the apex-95 + continuity-93 counts are deferred to Linux GHA (Plan 95-03, sole-authoritative — both cascade on cold Windows).
</output>
</content>
