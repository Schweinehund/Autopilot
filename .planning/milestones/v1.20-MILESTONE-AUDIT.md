---
milestone: v1.20
milestone_name: Frozen-Aware CI Remediation & Chain-Validator Debt Closure
audited: 2026-08-18T00:00:00Z
status: passed
scores:
  requirements: 28/28
  phases: 6/6
  integration: clean
  flows: clean
mechanical_checks:
  harness: scripts/validation/v1.20-milestone-audit.mjs
  allowlist: scripts/validation/v1.20-audit-allowlist.json
  baseline: "BASELINE_24 (regenerate-supervision-pins.mjs, refreshed 2026-08-13, Phase 144 Plan 06)"
  last_run: "2026-08-18"
  v119_pin_sha: "a7bda73e (full a7bda73e23efc5e3f9607c3fef37abf8ec4030aa)"
  v119_pin_commit: "dba21d0b (Phase 144 Plan 02, Task 2 — TRACER)"
  close_commit: "{phase_144_close_SHA}"
  gha_authoritative_sha: "2858c0b5e6d3c05133fd1bcc4c5c12f97e01c55c"
  gha_workflow_run: "32094133344 (Audit Harness v1.20 Integrity, the second/green dispatch)"
  gha_run_id: "32094133344"
  cross_os_exact_match: "Axis 1 (Windows fresh clone) + Axis 2 (Linux GHA, both the standalone apex job and the linux-chain-ubuntu-latest DUAL-APEX job, sole cross-OS-authoritative for both chain validators) + Axis 3 (Windows independent second fresh clone, disclosed same-host proxy) — ALL THREE independently-invoked measurements EXACT MATCH at 100 PASS / 0 FAIL / 1 SKIPPED (101 total checks), every one citing the ONE shared SHA 2858c0b5e6d3c05133fd1bcc4c5c12f97e01c55c. A fourth, genuinely dispatched context-independent Axis-3 attempt reproduced 7 of 8 legs exactly and left the apex leg INCOMPLETE — see Auditor-Independence section."
  apex_count_standalone_pre_close: "100 PASS / 0 FAIL / 1 SKIP (101 total checks; pre-close-gate; the 1 SKIP is V-144-AUDIT — a legitimate pre-close-gate resolver-null skip-pass, since 144-VERIFICATION.md does not yet exist at this document's authoring time)"
  apex_skip_resolved_at_close: "PENDING — Plan 144-12's post-close-gate confirmatory apex run, the terminal HARN-19 discharge point; expected 101 PASS / 0 FAIL / 0 SKIPPED per D-10 once 144-VERIFICATION.md exists (not yet authored or run as of this document)"
  raw_exit_code: 0
  self_test: "9/9 passed (v1.20-milestone-audit.mjs --self-test; confirmed independently on both the Axis-1 and the same-host-proxy Axis-3 fresh clones; no --self-test job exists in CI — a legitimate Windows-only tooling-surface leg, not a mismatch)"
  c17_run: "inherited verbatim from v1.19 (C1-C17 byte-identical, no functional check change this milestone; C17 EEE contract already folded in per Phase 119 D-119, not re-folded)"
  pin_drift: "ZERO real drift against V119 (a7bda73e): sidecar-derived pinned-file set 33 distinct docs/ files; changed-file set since the V119 close (measured at Phase 144 Plan 06) 106 files; candidate intersection 5 files (3 line-pinned); 21 hunks under a zero-context diff, all 1:1 line-neutral (the Phase 143 anchor-id insertions/removals) — see Pin-Drift Verdict section"
  axis_2_disposition: "CAPTURED — all 17 audit-harness-* workflows dispatched green at the shared SHA, job-level JSON quoted for all 211 jobs (196 success, 15 classified event-gated skips, 0 failures, 0 dependency-cascade gaps). ONE remediation round (D-22) was spent and it succeeded: first dispatch at 32aaae63 was 16 of 17 green (check-nav-hub-links.mjs self-test case G, a host-dependent traversal fixture); fix 2858c0b5; second dispatch at the shared SHA was 17 of 17 green."
  pin_helper_advisory: "17 (one per workflow, including base) — continue-on-error: true, structurally always success, excluded from both the pass tally and the skip tally as NON-EVIDENCE (D-21d)"
  notes: |
    v1.20 is a tooling-only milestone (NO content pillar; NO-TOOLING-PILLAR's converse discipline
    applies — no corpus documentation of any kind ships this milestone). It repairs the CARVE-1
    frozen-aware architecture, the ten-member standalone-RED validator set, corpus link coverage, the
    cold-clone cost measurement, and frozen-surface governance, then closes with the mandatory
    back-anchor pin (HARN-17), the 18th Path-A harness lineage bump (HARN-18), and a 3-axis terminal
    re-audit plus single close-gate commit (HARN-19).

    BACK-ANCHOR COMPLETION (the milestone's stated mandatory deliverable): v1.20 completes the
    back-anchor invariant v1.19 deliberately left open (`V119-PIN-DEFERRAL`) — `frozen-at-close.mjs`
    gains `V119: 'a7bda73e'` + `readAtV119Close`/`lsTreeAtV119Close` (Phase 144 Plan 02, commit
    `dba21d0b`), and — in the same plan, because the conversion cannot exist before the pin it reads —
    the v1.19 milestone-audit harness converts to frozen-aware corpus reads, the seventeenth and last
    unconverted harness (D-01). Unlike v1.19's C17 leg, which stayed live-HEAD as a stated
    SUCCESS-CRITERION AMENDMENT (D-02), the v1.20 close carries no C17 conversion obligation of its own
    — v1.20 does not ship its own milestone-audit harness's C17 check as frozen-aware, because v1.20 is
    not itself frozen against any future milestone yet (`V120-PIN-DEFERRAL`, this milestone's own
    successor obligation, mirrors `V119-PIN-DEFERRAL`).

    THREE-AXIS TERMINAL RE-AUDIT — Axis 1 (Windows fresh clone) and Axis 2 (Linux GHA, cross-OS
    authoritative for both chain validators) both CAPTURED clean at the shared SHA. Axis 3 was
    satisfied at the Plan 144-10 checkpoint by a disclosed same-host independent-clone proxy (this
    executor's toolset exposed no agent-dispatch primitive), and a SEPARATE, genuinely
    context-independent dispatched-agent attempt was subsequently obtained to strengthen that leg —
    it reproduced 7 of 8 legs exactly and left the apex leg incomplete, attributed by owner ruling to
    the carried `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` hazard rather than counted as a new defect or as a
    bar failure. See the Auditor-Independence section for the full, honestly-scoped record.

    ONE REMEDIATION ROUND, SPENT AND SUCCEEDED (D-22): the first 17-workflow dispatch surfaced a
    genuine Phase-143 defect in `check-nav-hub-links.mjs`'s own self-test — a traversal fixture whose
    not-found leg measured a property of the CI runner's filesystem (`/etc/passwd` present on Linux,
    absent on Windows) rather than a property of the resolver under test. Fixed in one commit,
    retargeting the fixture leaf to a name absent on every platform; the second dispatch at the new
    shared SHA was clean across all 17 workflows. This is recorded here in full, per this project's own
    standing discipline that a remediation round is part of HARN-19's honest history, not a detail to
    omit once it succeeds.

    HARNESS-LINEAGE 18TH GENERATION: `v1.20-milestone-audit.mjs` (Path-A from v1.19, C1-C17 inherited
    verbatim, no functional check change this milestone) + `v1.20-audit-allowlist.json`
    (header-fields-only copy) + BASELINE_24 (refreshed 2026-08-13, closing BASELINE_23's own forward
    reference) + `check-phase-139..143.mjs` (5 lightweight leaves, no chain, no AUDIT, no
    AUDIT-HARNESS) + `check-phase-144.mjs` (apex, `CHAIN_PHASES` independently re-derived by arithmetic
    at `[48..143]`, 96 entries, plus the `CHAIN_EXTRA = [30, 31]` sidecar and its own fourth
    disjointness guard — the guard the predecessor apex structurally cannot have) + the 17th CI
    coexistence workflow, born with `fetch-depth: 0` on every one of its 13 checkout steps.

    Phase 144 close-gate lands as TWO commits, not one (a deliberate split from the predecessor's
    single-commit convention): this document and the absorb-and-append deferred-cleanup artifact land
    at Plan 144-11, and the SINGLE 28-requirement flip lands at Plan 144-12 — the flip itself stays
    atomic (D-24), the authoring of the record that the flip is read against does not. The literal
    `{phase_144_close_SHA}` placeholder above is recoverable, once Plan 144-12 lands, via the same
    subject-line pair discriminator this milestone used to recover V119 — never the naive dual-token
    `--grep --all-match` form, which has returned multiple candidates at every prior milestone this
    method was checked against.
performed_by: |
  Phase 144 Plans 144-01 (CARVE allowlist amendment) + 144-02 (V119 pin + 17th harness conversion,
  TRACER) + 144-03 (check-phase-67.mjs ten fail-loud sites) + 144-04/144-05 (five lightweight leaves,
  139-143) + 144-06 (18th Path-A harness + sidecar + BASELINE_24 + pin-drift proof) + 144-07 (apex
  check-phase-144.mjs) + 144-08 (17th CI workflow + two stale-figure corrections) + 144-09 (pre-push
  readiness census + owner checkpoint: push, dispatch, atom-branch disposition) + 144-10 (job-level
  Axis-2 evidence for all 17 runs + Axis-1/Axis-3 local evidence + human-verify checkpoint) + 144-11
  (this document + the absorb-and-append deferred-cleanup artifact) — gsd-executor sequential
  main-tree execution per `use_worktrees:false`.

  Axis 1 (physical): fresh `git -c core.longpaths=true clone --no-hardlinks`, full depth confirmed
  (`git rev-parse --is-shallow-repository` → `false`), checked out the shared SHA explicitly. 18th
  harness + 5 new leaves all exit 0 (`v1.20-milestone-audit.mjs --verbose` 16/0/0, `--self-test` 9/9;
  `check-phase-139` 5/0/0, `-140` 5/0/0, `-141` 6/0/0, `-142` 6/0/0, `-143` 9/0/0); apex
  `check-phase-144.mjs` (full chain `[48..143]` plus `CHAIN_EXTRA=[30,31]`) — **100 PASS / 0 FAIL / 1
  SKIPPED (101 total checks)**, 28.8s, no deep-nest stall. Clone removed post-audit; working tree
  confirmed unchanged (`git status --porcelain --untracked-files=no` empty before and after).

  Axis 2 (cross-OS, AUTHORITATIVE for both chain validators per the standing D-03 disposition):
  CAPTURED at Plan 144-10 Task 1, run `32094133344`, headSha `2858c0b5...` (read back and quoted for
  all 17 runs, never assumed). Job-level 13 PASS / 0 FAIL / 1 SKIP for the v1.20 workflow specifically;
  across all 17 workflows, 211 total jobs, 196 success, 15 classified skips, 0 failures. Apex's own
  internal check-level result, fetched directly from raw GHA logs on BOTH jobs that run the full
  recursion (the standalone `check-phase-144 validator` job and the `linux-chain-ubuntu-latest`
  DUAL-APEX job) — **100 PASS, 0 FAIL, 1 SKIPPED (total checks: 101), character-for-character
  identical on both jobs and identical to both local axes.**

  Axis 3 (context-independent reproduction): TWO measurements. (a) A same-host independent second
  fresh clone (Plan 144-10 Task 2, 44.3s), disclosed honestly as NOT genuinely context-independent —
  this executor's toolset exposed no agent-dispatch primitive at that plan — reproduced **100 PASS / 0
  FAIL / 1 SKIPPED (101 total)**, byte-identical to Axis 1. (b) A genuinely context-independent
  dispatched agent, barred from reading any `.planning/` document, obtained after the Plan 144-10
  checkpoint to strengthen the disclosed gap: fresh `file://` full-depth clone at the shared SHA,
  reproduced 7 of 8 legs exactly (harness 16/0/0, self-test 9/9, and all five leaves: 5/0/0, 5/0/0,
  6/0/0, 6/0/0, 9/0/0) — the apex leg is INCOMPLETE, stalling at the `[CHAIN-142/101]` progress marker
  with no further output growth. OWNER-RULED 2026-08-17: recorded as incomplete and ATTRIBUTED to the
  carried `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` hazard (scoped corroboration, not resolution, of an item
  this project has tracked as Advisory/non-blocking since v1.8) — not a new defect, and not treated as
  a bar failure, since HARN-19's Axis-3 bar was already satisfied at the Plan 144-10 checkpoint by
  measurement (a). See the Auditor-Independence section for the two false-positive anomalies this
  dispatched run also raised, both adjudicated and recorded so neither leaks forward as an open defect.

  Pin-Drift Verdict (this milestone's applicable form of the predecessor's byte-unchanged HARD gate —
  see the dedicated section below for why the two are not the same instrument this milestone): ZERO
  real drift against the V119 pin (`a7bda73e`). v1.20 is itself a milestone that edits
  `scripts/validation/` extensively by design (SWEEP/RED/LINK/NEST/GOV), so a byte-unchanged assertion
  over the full validator surface does not apply the way it did to a content-only predecessor close —
  the applicable gate is that the SIDECAR'S OWN PINS have not silently drifted underneath it, which is
  what Plan 144-06's line-granular adjudication proves.
deferred_items:
  - id: "DEFERRED-CLEANUP"
    title: "See v1.20-DEFERRED-CLEANUP.md (absorb-and-append: 15 pre-existing routed rows preserved verbatim; Part A NEW — C17 frozen-aware residue across all 5 C17-bearing harnesses (v1.15-v1.19), V120-PIN-DEFERRAL; Part B CARRIED — the eight previously-omitted still-open v1.19 entries plus FENCE-AXIS-02, the ten unowned tooling items, V-132-HUBSNOTWIRED-REGEX-BROKEN, RECIPE-OUTBOUND-LINK-COVERAGE; Part C DROPPED-and-Closed — ACCEPTED-STANDALONE-CI-RED (17/17 green discharge) and ACCEPTED-SCOPED-RED (ten-member re-confirmation discharge), plus DEFER-119-A moved here rather than carried.)"
    routing: "v1.21+"
    required_by: "v1.21 entry-phase planning"
    cross_link: ".planning/milestones/v1.20-DEFERRED-CLEANUP.md"
tags: [milestone, v1.20, close-gate, 3-axis-stacking, harness-lineage-bump, tooling-only-milestone, no-content-pillar, back-anchor-completion, v119-pin, two-commit-close, both-accepted-red-dispositions-deleted, remediation-round-recorded, pin-drift-proof, deferred-cleanup-absorb-and-append]
---

# v1.20 Milestone Audit — Frozen-Aware CI Remediation & Chain-Validator Debt Closure

**Audited:** 2026-08-18
**Scope:** Phases 139–144 (6 phases, 28 requirements — 9 SWEEP + 7 RED + 6 LINK + 1 NEST + 2 GOV + 3 HARN)
**Verdict:** PASSED — 28/28 v1.20 requirements evidenced complete; the v1.20 audit harness (C1–C17 inherited verbatim from v1.19, no functional check change this milestone) runs GREEN; auditor independence confirmed on **all 3 axes** (Axis 1 fresh `git clone --no-hardlinks`, Axis 2 cross-OS Linux GHA sole-authoritative for both chain validators, Axis 3 same-host independent second clone, disclosed), all citing the SAME shared SHA `2858c0b5` and matching character-for-character at **100 PASS / 0 FAIL / 1 SKIPPED (101 total checks)**. Apex `check-phase-144` `[48..143]` plus `CHAIN_EXTRA=[30,31]` is GREEN standalone (pre-close-gate) and reproduces byte-identically across every completed axis. All 17 `audit-harness-*` integrity workflows are dispatched green at one shared SHA, job-level. Both accepted-red dispositions (`ACCEPTED-STANDALONE-CI-RED`, `ACCEPTED-SCOPED-RED`) are DISCHARGED with live evidence and move to Dropped-and-Closed in the companion deferred-cleanup artifact, satisfying the milestone bar's own wording rather than carrying either a seventh milestone.
**Closing (pending Plan 144-12):** the single 28-requirement flip.

## Executive Summary

v1.20 carries **no content pillar** — the converse of every predecessor's NO-TOOLING-PILLAR discipline applies here: no corpus documentation of any kind ships this milestone. Instead it repairs five classes of accumulated validator/CI debt (the CARVE-1 frozen-aware conversion, the ten-member standalone-RED validator set, corpus link coverage plus fence-mask unification, the cold-clone cost measurement, and frozen-surface governance), then closes with the mandatory back-anchor pin, the 18th Path-A harness lineage bump, and a 3-axis terminal re-audit. Coverage: 6 phases (139–144) and 28 requirements:

- **SWEEP-01..09, GOV-01/02** (Phase 139) — `fetch-depth: 0` retrofitted across all 16 pre-existing workflows (97 previously-shallow checkouts), a dependency-free `frozen-read-probe` job proving the 11 already-frozen-aware validators read successfully in CI, four silent-swallow fallbacks fail loud instead of returning `null`/`""`, `lsTreeAtClose()` shipped, and the milestone's own CARVE governance instrument (allowlist + byte-unchanged gate + grep-before-edit discipline) recorded.
- **SWEEP-05..08** (Phase 140) — the v1.4 through v1.18 frozen milestone-audit harnesses converted to read their own close-SHA corpus rather than live HEAD; the v1.4 `TEMPLATE-SENTINEL` remedy and the `V14` pin landed with recorded rationale. SWEEP-05/06 stay Pending in the requirements document until this phase's own close-gate, because both need the seventeenth (v1.19) harness's conversion and measurement — landed at Phase 144 Plan 02.
- **RED-01..03, SWEEP-09** (Phase 141) — the v1.5–v1.13 C5/C10 freshness assertions pass without editing glossary metadata, the supervision-pin self-test classifier fixed with the v1.7 fixture unedited, `check-phase-{48,60..66}` all exit 0 standalone, and 13 of 19 remaining silent-swallow frozen-read sites fail loud (the remaining 7, in `check-phase-67.mjs`, deferred to Phase 144 behind its own CARVE amendment).
- **RED-04..07, NEST-01** (Phase 142) — `check-phase-30`/`check-phase-31` exit 0 standalone (including a vacuous-regex fix) and join the apex's executed-chain set via a hand-authored `CHAIN_EXTRA` sidecar rather than the generated `CHAIN_PHASES` span; `check-phase-68`'s two guardrail assertions still pass; the cold-clone apex cost is measured on Windows with a stated method, threshold and decision rule, and ruled Advisory.
- **LINK-01..06** (Phase 143) — a corpus-wide link/anchor checker ships with no accepted-violation baseline, HTML `<a id>` anchors recognised (closing 75% of the apparent anchor-failure population), 13 genuine broken file targets and 132 genuine broken anchors fixed, and fence-mask behaviour unified across all 15 call sites in 9 files including both C17 sites, with c17's before/after violation counts proven identical.
- **HARN-17..19** (Phase 144) — the **V119 back-anchor pin** lands (`a7bda73e`), the **18th Path-A** lineage bump ships (5 new leaves + apex + sidecar + 17th CI workflow), and the **3-axis terminal re-audit** plus the single close-gate commit (Plan 144-12, following this document) close the milestone with both accepted-red dispositions deleted rather than carried.

## v1.20 Phase Closure Narrative

v1.20 is a **tooling-only milestone with no content pillar**: governance-and-fetch-depth repair (139) → frozen-aware conversion (140) → standalone-red remediation (141) → chain adoption and cold-clone measurement (142) → link coverage and fence-mask unification (143) → the mandatory harness cluster (144), never batched with content because none exists this milestone.

### Phase 139 — Governance CARVE + Fetch-Depth Retrofit + Shallow-Job Repair (SWEEP-01..04, GOV-01/02)

Landed the milestone's own governance instrument first: a named CARVE allowlist covering the frozen harnesses, the workflows, and the nine Pillar-C files, with a byte-unchanged gate on everything off-list (GOV-01), plus the grep-before-edit + regression-gate discipline every later frozen-surface edit this milestone follows (GOV-02). `fetch-depth: 0` landed on 97 previously-shallow checkouts across the three worst-affected workflows (SWEEP-01) — the mandatory precondition for SWEEP-05's frozen-aware conversion, since a depth-1 clone fatals `readAtClose()`. A dependency-free `frozen-read-probe` job proved the 11 already-frozen-aware validators read successfully once un-shallowed (SWEEP-02); four silent-swallow fallbacks in `check-phase-49.mjs`/`check-phase-51.mjs` fail loud (SWEEP-03); `lsTreeAtClose()` shipped as the frozen enumeration API (SWEEP-04).

### Phase 140 — Frozen-Aware Conversion + TEMPLATE-SENTINEL + V14 Pin (SWEEP-05..08)

Converted the v1.4 through v1.18 frozen milestone-audit harnesses (sixteen of the seventeen existing at the time) to read their own close-SHA corpus via `createFrozenCorpusReader`, leaving only the C17 contract-presence guard on live HEAD in the five harnesses that carry a C17 check (v1.15–v1.18 at this phase; v1.19 joins that set at Phase 144). The v1.4 `TEMPLATE-SENTINEL` assertion — provably never green at v1.4's own close SHA, so frozen-awareness cannot fix it — got a named, recorded remedy distinct from frozen-awareness (SWEEP-07), and a `V14` pin landed with an explicitly chosen SHA and recorded rationale (SWEEP-08). SWEEP-05 and SWEEP-06 stay Pending in the requirements document at this phase's own close, deliberately: the seventeenth harness (v1.19) had not yet been converted or measured.

### Phase 141 — Standalone-Red Validator Set, Chain Members Green (RED-01..03, SWEEP-09)

Fixed the true prerequisite chain the `ACCEPTED-SCOPED-RED` set's own root-cause analysis named: the v1.5–v1.13 C5/C10 freshness assertions pass without touching either glossary's metadata (RED-01), the supervision-pin self-test classifier's backward-only scan window is repaired without editing the frozen v1.7 fixture or relaxing the classifier (RED-02, discharging `DEFER-119-A`), and `check-phase-{48,60,61,62,63,64,65,66}` all exit 0 standalone (RED-03) — closing three of the four root-cause classes the `ACCEPTED-SCOPED-RED` disposition named. SWEEP-09 landed 13 of 19 remaining silent-swallow/chicken-and-egg frozen-read sites (`check-phase-61`'s 1, `check-phase-68`'s 2, `check-phase-70`'s 10) fail loud, deferring `check-phase-67.mjs`'s remaining 7 to Phase 144 behind its own CARVE amendment, and proved the fix end-to-end with the milestone's first-ever CI fan-out (three runs, one shared SHA, 41 jobs, zero content/timeout/environment reds).

### Phase 142 — Archival-Path Fix, Chain Adoption, Cold-Clone Threshold (RED-04..07, NEST-01)

Closed the fourth `ACCEPTED-SCOPED-RED` root-cause class: `check-phase-30` and `check-phase-31` — outside every `CHAIN_PHASES` array and outside the byte-unchanged gate — exit 0 standalone (RED-04/05, including a vacuous double-quoted-regex fix in `check-phase-30` that had never actually inspected a Mermaid block), and join the apex's executed-chain set via a hand-authored `CHAIN_EXTRA = [30, 31]` sidecar deliberately excluded from `CHAIN_PHASES` and its three module-load span invariants, since the span itself must stay generated by arithmetic (RED-06). `check-phase-68`'s two guardrail assertions (`V-68-04`, `V-68-08`) still pass after the `check-phase-31` changes (RED-07). The cold-clone apex cost is measured on Windows with a stated method (full-depth clone, cache state, Defender state, runner, tree identity, verbose declaration), a ratio-based threshold, and an explicit decision rule — measured healthy, ruled Advisory and non-blocking (NEST-01). Twelve items surfaced during this phase's own scoping that no v1.20 requirement covers were routed into `v1.20-DEFERRED-CLEANUP.md` mid-milestone rather than left to evaporate.

### Phase 143 — Link Coverage + Fence-Mask Unification (LINK-01..06)

Shipped a corpus-wide relative-link and anchor checker with **no accepted-violation baseline** (LINK-04) — a day-one baseline would have frozen 201 of 271 apparent anchor failures as permanently accepted and invisible, the exact disposition class this milestone exists to delete. HTML `<a id>` anchor recognition closed 75% of the apparent anchor-failure population before any corpus edit (LINK-01); the shipped checker scope (274 files / 6252 links, templates included, inline masking on) found 13 genuine broken file targets and 132 genuine broken anchors, all fixed (LINK-02/03); fence-mask behaviour unified across all 15 call sites in 9 files, including both C17 sites, with c17's before/after file and violation counts proven identical and the newly-masked lines proven to hide no suppressed violation (LINK-05/06). Two further deferred items (`FENCE-AXIS-02`'s two axes, and four out-of-census divergent fence-handling sites) were named and routed rather than folded into this phase's own unification scope.

### Phase 144 — V119 Pin + 18th Path-A Lineage Bump + Terminal Close (HARN-17, HARN-18, HARN-19)

The terminal close-gate phase, executed across twelve plans. **Plan 01** landed the CARVE allowlist amendment alone and first, pre-authorizing the six new validator paths and three pipeline paths this phase's later plans create. **Plan 02** (TRACER) recovered and subject-line-verified the V119 SHA (count=1), landed the pin plus its two convenience exports, converted the seventeenth and last unconverted milestone-audit harness (v1.19) to frozen-aware reads, and measured its wall-clock runtime at 1,260ms median against the 60-second subprocess budget — 4.75× faster than Phase 140's already-comfortable slowest-of-sixteen figure. **Plan 03** landed `check-phase-67.mjs`'s ten remaining fail-loud sites (7 chicken-and-egg returns, 3 partial-null accumulators) behind a GOV-02 census row, proven by a standalone negative-proof script run outside the working tree. **Plans 04/05** shipped the five lightweight leaf validators (`check-phase-139..143.mjs`), each needle-checked against its own phase's measured actuals, `check-phase-143.mjs` implementing the Phase 143 hand-off spec verbatim and declining its corpus-level-invariant recommendation. **Plan 06** shipped the 18th Path-A harness, the header-fields-only sidecar copy, BASELINE_24, and the pin-drift proof (real drift ZERO, line-granular verdict). **Plan 07** authored the apex `check-phase-144.mjs` — `CHAIN_PHASES` independently re-derived by arithmetic at `[48..143]` (96 entries), four module-load guards proven fail-first by mutation testing outside the working tree (the fourth, `CHAIN_EXTRA` disjointness, is a guard the predecessor apex structurally cannot have), and the literal `'v1.20-phases'` archive-root token. **Plan 08** shipped the 17th CI coexistence workflow, born with `fetch-depth: 0` on all 13 checkout steps, plus the two stale `~102s` figure corrections landed in the same commit as the workflow's own authoring. **Plan 09** ran the pre-push readiness census (ten standalone-red members re-confirmed fresh, Class-1 archival-drift census at zero survivors, working-tree hygiene ruling) and halted at the owner checkpoint for the push and dispatch decision. **Plan 10** recorded job-level Axis-2 evidence for all 17 dispatched runs at the shared SHA (including the one remediation round, spent and succeeded), Axis-1's fresh clone, and Axis-3's disclosed same-host proxy, halting at a human-verify checkpoint on the one open question the disclosed proxy could not resolve — subsequently answered by a genuinely dispatched context-independent Axis-3 attempt (7 of 8 legs clean, apex leg incomplete, owner-ruled attribution to the carried Windows deep-nest hazard). **This document, and its companion absorb-and-append deferred-cleanup artifact (Plan 144-11)**, author the milestone-audit and deferred-cleanup canon this close-gate is read against. **Plan 144-12**, next, regenerates the publish bundle `--version=v1.20`, lands the single 28-requirement flip, and runs the terminal post-close-gate confirmatory apex.

**Key Phase 144 novelty vs. Phase 138 (v1.19's close):**
- v1.19's close authored its milestone-audit and deferred-cleanup canon in the SAME plan as the 17-requirement flip (single commit, no Commit A). v1.20 splits this deliberately across two plans (144-11 authors the record, 144-12 lands the flip) — the FLIP itself stays atomic (D-24); the authoring of the record it is read against does not need to share its commit.
- v1.19 needed zero D-00a coordinate re-pins and found zero pin drift on its first check. v1.20's pin-drift proof (Plan 06) likewise found zero real drift, using a line-granular adjudication method this milestone corrected onto (the file-granular candidate set alone is not the answer — see the dedicated section below).
- v1.19 captured all three axes clean on the first attempt. v1.20 captures Axis 1 and Axis 2 clean, satisfies Axis 3's bar via a disclosed same-host proxy at the Plan 144-10 checkpoint, and additionally records a genuinely dispatched Axis-3 attempt whose apex leg did not complete — an honest gap this document names rather than smooths over, attributed to a hazard this project has carried and measured as non-blocking since v1.8.
- v1.19 exercised the `ACCEPTED-STANDALONE-CI-RED` disposition once more (discharged-then-extended). v1.20 DISCHARGES BOTH remaining accepted-red dispositions with live evidence and DROPS both from the backlog — the milestone bar's own wording (`REQUIREMENTS.md:6`) requires deletion, not another extension.

## Auditor-Independence Verification (3-axis stacking)

**Auditor Agent ID:** `gsd-executor` (Plans 144-09/144-10/144-11 — dispatched sequential main-tree per `use_worktrees:false`).

**Axis 1 — Fresh clone (Windows):**
- **Clone Mechanism:** Fresh `git -c core.longpaths=true clone --no-hardlinks` — separate `.git/`, full depth confirmed (`git rev-parse --is-shallow-repository` → `false`), the `core.longpaths=true` override a Windows `MAX_PATH` necessity for this repo's deep `.planning/milestones/` paths, not a scope change.
- **Validators run:** `v1.20-milestone-audit.mjs --verbose` (16/0/0) + `--self-test` (9/9); 5 new leaves (`check-phase-139` 5/0/0, `-140` 5/0/0, `-141` 6/0/0, `-142` 6/0/0, `-143` 9/0/0); apex `check-phase-144.mjs` (full chain `[48..143]` plus `CHAIN_EXTRA=[30,31]`) — **100 PASS / 0 FAIL / 1 SKIPPED (101 total checks)**, 28.8s, no deep-nest stall.

**Axis 2 — Cross-OS Linux GHA (AUTHORITATIVE for both chain validators): CAPTURED.**
- Run `32094133344`, conclusion `success`, headSha `2858c0b5e6d3c05133fd1bcc4c5c12f97e01c55c` — read back and independently quoted for all 17 runs via `gh run view <id> --json headSha`, never assumed.
- Job-level (v1.20 workflow): **13 PASS / 0 FAIL / 1 SKIP (14 jobs)** — the skip is the quarterly `c13_rotting_external` link-check, `if:`-gated to the quarterly schedule and expected under manual `workflow_dispatch`, not the apex's own internal skip.
- Apex's own internal check-level result — fetched directly from raw GHA logs by job ID, independently on BOTH jobs that run the full recursion, confirming the DUAL-APEX contract held:
  ```
  check-phase-144 validator (apex; recursively spawns 48..143)              ->  Result: 100 PASS, 0 FAIL, 1 SKIPPED (total checks: 101)
  Validator chain on Linux LF (Phase 69 CILINUX-01)                          ->  Result: 100 PASS, 0 FAIL, 1 SKIPPED (total checks: 101)
  ```
  Character-for-character identical on both jobs, and identical to both local axes.
- Across all 17 workflows: **211 jobs total — 196 success, 15 skipped (all classified — the quarterly-guard job, absent from base and v1.5, present in the other 15), 0 failed.** Zero dependency-cascade skips: every job whose `needs:` includes `harness-run` succeeded in all 17 runs. The `pin-helper-advisory` job (17 instances, one per workflow, structurally always `success` under `continue-on-error: true`) is excluded from both the pass and skip tallies as NON-EVIDENCE.

**Axis 3 — Fresh zero-context reproduction: TWO measurements, one satisfying the bar, one additional and partial.**
- **Measurement (a), same-host proxy (Plan 144-10 Task 2):** a second, fully independent fresh clone — a distinct `git clone` into a separate scratchpad directory, a distinct `node` process per check, sharing no state with Axis 1's clone or the working tree. Disclosed honestly, per the exact precedent v1.19's Plan 138-04 set for the identical toolset gap, as NOT a claim of LLM-context independence: this executor's toolset exposes no subagent/Task-dispatch primitive. Reproduced **100 PASS / 0 FAIL / 1 SKIPPED (101 total)**, byte-identical to Axis 1, in 44.3s. **This measurement satisfies HARN-19's Axis-3 bar** — it was the evidence base the Plan 144-10 human-verify checkpoint presented and, per the resulting owner ruling, the disclosed gap was accepted rather than treated as blocking.
- **Measurement (b), genuinely dispatched context-independent agent:** obtained after the Plan 144-10 checkpoint, to strengthen rather than replace measurement (a) — barred from reading any `.planning/` document, fresh `file://` full-depth clone at the shared SHA. Reproduced **7 of 8 legs exactly**: harness `--verbose` 16/0/0, `--self-test` 9/9, and all five leaves (`check-phase-139` 5/0/0, `-140` 5/0/0, `-141` 6/0/0, `-142` 6/0/0, `-143` 9/0/0). **The apex leg (the 8th) is INCOMPLETE** — it stalled at the `[CHAIN-142/101]` progress marker with no further output growth and was not waited out to completion.
  - **OWNER-RULED 2026-08-17:** recorded as incomplete, ATTRIBUTED to the carried `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` hazard (this project's own standing, measured-non-blocking-since-v1.8 platform timeout class, corroboration not resolution) — not counted as a new defect, and not treated as a bar failure, since Axis-3's bar was already satisfied by measurement (a). This mirrors the v1.15/v1.16 precedent of recording "apex NOT run on Windows" honestly rather than folding a non-run into a claimed clean cycle.
  - **Two anomalies this dispatched run raised are FALSE POSITIVES, adjudicated here so neither leaks forward as an open defect:** (1) the `[CHAIN-142/101]` label is not a broken counter — the label format is `[check-id/total-checks]`, and chain checks are id'd by the phase number they invoke, the same idiom as `[TOOLPRESENCE/9]`; (2) a `V-144-AUDIT: SKIPPED` read is the documented, designed pre-close-gate skip-pass (legitimate because `144-VERIFICATION.md` does not yet exist), not a defect — it is a designed component of the expected `100/0/1` triple, not evidence of a broken resolver.
- Genuine host independence for Axis 3 is not claimed anywhere in this record, at either measurement — both ran on win32. Measurement (a) is same-host but independently-invoked; measurement (b) is context-independent (no `.planning/` read) but still same-host. The distinction is stated plainly, matching the exact discipline the v1.19 record applied to its own Axis-3 measurements.

| Layer | Mechanism | Verified |
|-------|-----------|----------|
| Logical | `gsd-executor` sequential main-tree | Re-audit dispatched after atom-authoring plans |
| Physical | Fresh `git clone --no-hardlinks` (Axis 1) | Separate `.git/`; full depth confirmed |
| OS-environment | Cross-OS Linux GHA (Axis 2) | **CAPTURED — run `32094133344`, headSha `2858c0b5`** |
| Context-independence (satisfying) | Same-host proxy (Axis 3a) | Independently-invoked, NOT claimed context-independent — satisfies the bar per the Plan 144-10 checkpoint ruling |
| Context-independence (additional) | Dispatched no-`.planning/`-read agent (Axis 3b) | Context-independent; 7/8 legs clean, apex leg incomplete, owner-attributed |
| Cross-axis tally (completed legs) | apex-144 `--verbose` PASS/FAIL/SKIP count | **100/0/1 IDENTICAL across Axis 1, Axis 2 (both jobs), and Axis 3a** |

## Cross-OS PASS/FAIL/SKIP EXACT MATCH (completed axes, one shared SHA)

| # | Validator | Type | Windows (Axis 1 fresh clone) | Windows (Axis 3a, same-host proxy) | Windows (Axis 3b, dispatched agent) | Linux (Axis 2 GHA) | Verdict |
|---|-----------|------|-------------------------------|-------------------------------------|--------------------------------------|----------------------|---------|
| 1 | `v1.20-milestone-audit.mjs --verbose` + `--self-test` | leaf | 16/0/0 + 9/9 | 16/0/0 + 9/9 | 16/0/0 + 9/9 | 16/0/0 (job-level, `--verbose` only — no `--self-test` job exists in CI) | EXACT MATCH (self-test is a legitimate Windows-only leg, not a mismatch) |
| 2 | `check-phase-139.mjs` .. `check-phase-143.mjs` (5 leaves) | leaf | exit 0 each, 5/0/0 5/0/0 6/0/0 6/0/0 9/0/0 | identical | identical (all 5) | success (job-level, identical counts from raw logs) | EXACT MATCH |
| 3 | `check-phase-144.mjs` (apex, `CHAIN_PHASES=[48..143]` + `CHAIN_EXTRA=[30,31]`, 101 total checks incl. SELF+AUDIT+AUDIT-HARNESS) | chain | **100 PASS / 0 FAIL / 1 SKIP** | **100 PASS / 0 FAIL / 1 SKIP** | **INCOMPLETE — stalled at `[CHAIN-142/101]`, attributed to `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01`** | **100 PASS / 0 FAIL / 1 SKIP** (both the standalone apex job and the `linux-chain-ubuntu-latest` DUAL-APEX job, raw logs) | **EXACT MATCH across every axis that completed** — Axis 3b's incompleteness is recorded, not smoothed into a false "all identical" claim |

**Result: EXACT MATCH across every axis and every measurement that ran to completion.** Every completed row above cites the SAME single SHA `2858c0b5e6d3c05133fd1bcc4c5c12f97e01c55c` — no differing SHA anywhere, satisfying the single-shared-SHA discipline (D-20) and its read-back requirement (independently quoted for all 17 Axis-2 runs and both fresh clones). This document does not smooth Axis 3b's incomplete apex leg into the match table as though it agreed — the cell states plainly what happened and cites the owner ruling.

**Expected SKIP, stated:** every completed apex row carries exactly one SKIP (`V-144-AUDIT`) — the legitimate pre-close-gate resolver-null skip-pass, since `144-VERIFICATION.md` does not exist at the time these measurements ran. See Plan 144-12's post-close-gate confirmatory run for the moment this SKIP resolves to a real PASS.

## Predecessor-Workflow Cascade Scan / `ACCEPTED-STANDALONE-CI-RED` — Discharged, Not Extended

The `ACCEPTED-STANDALONE-CI-RED` disposition (frozen predecessor integrity workflows going red standalone against the evolved corpus, root-caused to the unresolved CARVE-1 ref-less checkout) previously spanned v1.4–v1.18 (discharged-then-carried at v1.19) with a corrected 5 PASS / 10 FAIL baseline across the fifteen pre-existing workflows at that close. **At this close, the disposition is fully DISCHARGED rather than exercised again:** the first 17-workflow dispatch (SHA `32aaae63`) found 16 of 17 green, with the sole failure attributable to a genuine Phase-143 defect (`check-nav-hub-links.mjs`'s own self-test, not any predecessor harness), not to the CARVE-1 ref-less-checkout pattern this disposition names. After the one-round fix, the second dispatch at the shared SHA (`2858c0b5`) was **17 of 17 green, job-level, zero failures across all 211 jobs.**

Since the underlying pattern this disposition tracks (a frozen predecessor harness's `harness-run` job validating live HEAD rather than its own pinned corpus, and going red as the corpus evolves) produced ZERO red jobs at this close — not a smaller red count, zero — the disposition is DISCHARGED with live per-run evidence rather than re-extended a seventh milestone. Consuming the routed `pull_request.paths` cascade concern (`v1.20-DEFERRED-CLEANUP.md` item 7, carried mid-milestone from Phase 142): the concern was that a future PR touching `check-phase-*.mjs` would fan out to 13 of the pre-existing workflows; this close exercised all 17 directly via `workflow_dispatch` (not a PR), and all 17 came back green regardless of trigger surface — a strictly stronger result than the routed concern anticipated. The full discharge record, with its Dropped-and-Closed entry and cited evidence, lives in the companion `v1.20-DEFERRED-CLEANUP.md`.

**`ACCEPTED-SCOPED-RED`** (the ten-member standalone-RED chain-validator set: `check-phase-30, 31, 48, 60, 61, 62, 63, 64, 65, 66`) is likewise DISCHARGED, not extended: Phase 141 (RED-01/02/03) and Phase 142 (RED-04/05/06/07) fixed all four of the disposition's named root-cause classes. Re-confirmed fresh at Phase 144 Plan 09 (HEAD `7ad104bf`, not inherited from Phase 141/142 evidence): all ten exit 0, with exactly two classified environment skips (`check-phase-30`/`check-phase-31`'s optional `markdown-link-check`/`mermaid-cli` unavailability, not a project dependency). Per D-26, the discharge must hold AT THE CLOSE SHA, not merely at Plan 09's measurement point — confirmed directly: `git diff --name-only 7ad104bf..HEAD` (the close SHA, and this document's own authoring HEAD) touches exactly one code file, `scripts/validation/check-nav-hub-links.mjs` (the unrelated remediation-round fix), plus `.planning/` documents; none of the ten members' own source files changed between the two points, so the Plan 09 re-confirmation stands unmodified at the close SHA.

## Pin-Drift Verdict (V119 Back-Anchor)

This milestone's applicable form of a predecessor close's byte-unchanged HARD gate is different in kind, not merely in degree: v1.20 is itself a milestone that deliberately edits `scripts/validation/` extensively (SWEEP/RED/LINK/NEST/GOV), so an assertion that the full validator surface stayed byte-unchanged since the V119 pin would trivially and correctly fail — that is not a defect, it is the milestone's own point. The applicable gate instead asks whether the SIDECAR'S OWN PINNED COORDINATES have drifted underneath it since V119.

**Method (Plan 144-06):** the sidecar-derived pinned-file set intersected with the changed-file set since the V119 close SHA (`a7bda73e`), adjudicated LINE-granular under a zero-context diff — the file-granular intersection is only the candidate set, not the verdict.

| Quantity | Value |
|---|---|
| Sidecar-derived pinned-file set (recursing into `c13_rotting_external`'s nested-array structure) | **33** distinct `docs/` files |
| Changed-file set since V119 close (`git diff --name-only a7bda73e..HEAD -- docs scripts .github`) | **106** files |
| Candidate intersection (pinned ∩ changed) | **5** files, **3** line-pinned |
| Hunks in the candidate set under a zero-context diff | **21**, all 1:1 line-neutral (Phase 143's anchor-id insertions and `{#id}` removals — added-line-count equals deleted-line-count at identical line coordinates) |

**Verdict: real pin drift is ZERO.** No pin coordinate in `v1.20-audit-allowlist.json` (or its v1.19 predecessor, from which it was copied header-fields-only) points at a line whose content moved. The generator's own report-mode instrument (`regenerate-supervision-pins.mjs --report`) is explicitly NOT cited as proof — it hardcodes the v1.7 sidecar and walks only 26 of the 59 total line-pins, so it cannot speak to the other 33 pins or to the `c13_rotting_external` file set at all; this repeats the same correction v1.19's own harness header first stated.

## Requirements Traceability — 28/28

| Requirement | Phase | Closing Reference | Status |
|-------------|-------|--------------------|--------|
| SWEEP-01 | 139 | `fetch-depth: 0` on 97 previously-shallow checkouts, dispatched-proven | Validated |
| SWEEP-02 | 139 | `frozen-read-probe` job, dependency-free, 11 validators proven in CI | Validated |
| SWEEP-03 | 139 | 4 silent-swallow fallbacks fail loud, proven by negative test | Validated |
| SWEEP-04 | 139 | `lsTreeAtClose()` frozen enumeration API shipped | Validated |
| SWEEP-05 | Phase 140, Phase 144 | v1.4–v1.18 converted (140); v1.19 converted, Plan 02 commit `dba21d0b` (144) | Validated |
| SWEEP-06 | 140, 144 | 16 harnesses measured (140); 17th measured 1,260ms median vs. 60,000ms budget (144 Plan 02) | Validated |
| SWEEP-07 | 140 | v1.4 `TEMPLATE-SENTINEL` remedy, recorded distinct from frozen-awareness | Validated |
| SWEEP-08 | 140 | `V14` pin, explicit SHA and rationale | Validated |
| RED-01 | 141 | v1.5–v1.13 C5/C10 freshness pass with zero glossary edits | Validated |
| RED-02 | 141 | self-test classifier fix, v1.7 fixture unedited | Validated |
| RED-03 | 141 | `check-phase-{48,60..66}` all exit 0 standalone | Validated |
| RED-04 | 142 | `check-phase-30` exits 0, incl. vacuous-regex fix | Validated |
| RED-05 | 142 | `check-phase-31` exits 0, `V-31-23`/`V-31-29` resolved | Validated |
| RED-06 | 142 | `CHAIN_EXTRA=[30,31]` sidecar, apex-144 executes both | Validated |
| RED-07 | 142 | `check-phase-68`'s `V-68-04`/`V-68-08` still pass | Validated |
| LINK-01 | 143 | HTML `<a id>` anchor recognition, 75% of apparent gap closed | Validated |
| LINK-02 | 143 | corpus-wide checker, 274 files / 6252 links, no baseline | Validated |
| LINK-03 | 143 | 13 broken file targets + 132 broken anchors fixed | Validated |
| LINK-04 | 143 | checker exits 0, zero accepted-violation baseline | Validated |
| LINK-05 | 143 | fence-mask unified across all 15 call sites, 9 files | Validated |
| LINK-06 | 143 | c17 before/after counts identical, no suppressed violation | Validated |
| NEST-01 | 142 | cold-clone cost measured, ratio threshold, ruled Advisory | Validated |
| GOV-01 | 139 | milestone-scoped CARVE allowlist + byte-unchanged gate | Validated |
| GOV-02 | 139 | grep-before-edit + regression-gate discipline, applied every frozen edit this milestone | Validated |
| HARN-17 | 144 | V119 pin + `readAtV119Close`/`lsTreeAtV119Close`, commit `dba21d0b` | Validated |
| HARN-18 | 144 | 18th Path-A harness + 5 leaves + apex + sidecar + BASELINE_24 + 17th CI workflow (Plans 04–08) | Validated |
| HARN-19 | 144 | 3-axis re-audit + 17/17 dispatched green + bundle + close-gate (Plans 09–12, this document + Plan 144-12) | Validated |
| SWEEP-09 | Phase 141, Phase 144 | 13 of 19 sites (141); `check-phase-67.mjs`'s remaining 7 sites, Plan 03 (144) | Validated |

**9/9 SWEEP + 7/7 RED + 6/6 LINK + 1/1 NEST + 2/2 GOV + 3/3 HARN requirements. Total: 28/28.**

*Note on this table's Status column:* every row above reads "Validated" as the terminal state this document and Plan 144-12's single close-gate commit together establish — the flip itself is Plan 144-12's own atomic commit (D-24), landing immediately after this document. The two rows corrected to span two phases (SWEEP-05, already spanning two in the live requirements document; SWEEP-09, corrected here) match the two traceability cells Plan 144-12's own action names for correction in the same gate commit.

## Mechanical Checks Detail

| # | Validator | Windows (Axis 1 fresh clone) | Windows (Axis 3a same-host proxy) | Windows (Axis 3b dispatched, partial) | Linux GHA (Axis 2) | Match |
|---|-----------|-------------------------------|-------------------------------------|-------------------------------------|----------------------|-------|
| 1 | `v1.20-milestone-audit.mjs --verbose` + `--self-test` | 16/0/0 + 9/9 | 16/0/0 + 9/9 | 16/0/0 + 9/9 | 16/0/0 (job-level, no `--self-test` job in CI) | EXACT |
| 2 | `check-phase-139.mjs` .. `check-phase-143.mjs` | exit 0 each | exit 0 each | exit 0 each | success (job-level) | EXACT |
| 3 | `check-phase-144.mjs` (apex `[48..143]` + `CHAIN_EXTRA`) | **100/0/1** | **100/0/1** | **INCOMPLETE (attributed, D-see Auditor-Independence)** | **100/0/1** (both apex job + linux-chain job, raw logs) | **EXACT across completed axes** |
| C1-C17 | `v1.20-milestone-audit.mjs` checks (inherited verbatim from v1.19) | (via full-harness run) | (via full-harness run) | (via full-harness run) | (via full-harness run) | EXACT |

**EXACT MATCH across the full cross-OS surface among every measurement that ran to completion, both axes required by the bar (Axis 1, Axis 2) plus the completed Axis-3 leg captured this close.**

## Corrections of Record

### 1. No Severity Ranking of Workflows Exists Anywhere in This Repository

`grep -ri 'reddest' .planning/` returns **zero hits** repo-wide. The red set of record is the base workflow plus the nine versioned ones (v1.5 through v1.13), with no ordering among them — and seven of those ten were outside the three-workflow sample a prior phase drew from when characterizing the set. No claim in this document, or in any prior v1.20 artifact, ranks these workflows by severity; any such framing found elsewhere is not sourced to a measurement.

### 2. The Stale Runtime Estimate Was a Two-Site Class, Misfiled by Line Number

The stale `~102s` Windows wall-clock figure lived in **two** workflows — `audit-harness-v1.7-integrity.yml:96` and `audit-harness-v1.8-integrity.yml:95` — not one. Five routing documents (including a prior version of `v1.20-DEFERRED-CLEANUP.md` item 11, and two phase-context files) named only the v1.7 file while citing line `:95`, which is the v1.8 file's line number, not v1.7's. Both sites were corrected in the same commit as the 17th workflow's own authoring (Phase 144 Plan 08), never alone.

### 3. The Predecessor Apex Mis-States Its Own Harness's Check Count

`check-phase-138.mjs:41` describes the v1.19 harness as "17-check"; the harness registers **16** checks (C8 was never defined, and the count is taken directly from the live `checks` array, not from any predecessor's header prose). This document does not repeat that mis-statement for the v1.20 harness — the check count cited throughout this document (16) is counted from the live array.

### 4. A Prior Plan Summary's Row-Count Pin on the Deferred-Cleanup Document Was Already Stale When Written

`142-01-SUMMARY.md:91`'s `grep -c '^| ' == 14` assertion against `v1.20-DEFERRED-CLEANUP.md` was a passed-at-the-time assertion, not a re-runnable gate — by the time Phase 143 Plan 08 extended the same document with rows 13–15, the true count was already 17 (14 table-formatting pipe-rows plus the 3 new rows' own table syntax), and by this close it reads 15 routed data rows across the extended table. No document in this milestone treats that count-14 assertion as a live invariant; it is recorded here as a documented, non-blocking staleness, consistent with the discipline that a `[MEASURED]` figure is dated, not eternal.

### 5. The Post-Gate Audit Check Cannot Detect a Wrong Archive-Root Token

`_lib/archive-path.mjs:23-24` resolves the LIVE path first and only falls through to the archived `.planning/milestones/<root>/` path — so a post-close-gate confirmatory run asserting `V-NN-AUDIT` reports PASS proves only that the resolver's live-path branch succeeded, never that its archived-path token is correct. The counterexample stands unchanged: `check-phase-125.mjs:86` carries the wrong token `['v1.15-phases']` while `125-VERIFICATION.md` actually lives under `v1.16-phases/`, and has emitted a permanently-green `V-125-AUDIT: SKIPPED -- not yet authored` detail string for two milestones running. `check-phase-144.mjs` closes this specific hole for v1.20's own token by asserting the literal string `'v1.20-phases'` separately in its source (Plan 07), rather than relying on the resolver's success/failure signal to prove token correctness — the same instrument Plan 144-12's post-gate run will exercise, honestly scoped to what it does and does not prove.

## Audit Harness Lineage (phases 62→66→70→74→82→88→93→95→100→112→119→125→128→134→138→144, lineage v1.4→v1.20 — 18th entry, 17th CI coexistence workflow)

The v1.20 audit harness follows a Path-A-copy lineage from the v1.19 frozen harness, with NO net-new check this milestone (C1-C17 fully inherited):

1. **v1.19-milestone-audit.mjs (FROZEN, except its own Phase 144 Plan 02 frozen-aware conversion — the seventeenth and final harness to convert)** — C1-C17 present; self-test 9/9.
2. **v1.20-milestone-audit.mjs — Plan 06 copy**: Path-A relabel, C1-C17 preserved byte-verbatim, no functional check change. Lineage extends v1.4 → v1.4.1 → v1.5 → ... → v1.19 → v1.20 (**18th** milestone-audit).
3. **`v1.20-audit-allowlist.json`** (Plan 06): header-fields-only copy of v1.19's — all pins carried byte-identical. Pin-drift proof: real drift ZERO (see the dedicated section above).
4. **BASELINE_24 refresh** (Plan 06): closes BASELINE_23's own forward reference, refreshed 2026-08-13. `BASELINE_9` array byte-unchanged.
5. **5 NET-NEW leaf validators + 1 apex shipped** (Plans 04/05/07): 5 leaf (`check-phase-139..143.mjs`, `CHAIN_PHASES=[]`, needle-checked against each phase's own measured actuals) + 1 chain-apex (`check-phase-144.mjs`). Apex `CHAIN_PHASES=[48..143]` GENERATED via `Array.from` (96 entries) — INDEPENDENTLY DERIVED, never copied from `check-phase-138`'s 90-entry array; four module-load guards (dedup, length, termini, and the NEW `CHAIN_EXTRA` disjointness guard the predecessor structurally cannot have), each proven fail-first by mutation testing outside the working tree (Plan 07 Task 2). `CHAIN_EXTRA = [30, 31]` carries forward verbatim from Phase 142's RED-06 sidecar, outside all four guards' coverage per its own declared purpose, plus the new disjointness guard closing the one gap `check-phase-138.mjs` could not structurally have.
6. **`_lib/frozen-at-close.mjs` V119 entry** (Plan 02, commit `dba21d0b`): `V119: 'a7bda73e'` (v1.19 close-gate SHA, positively confirmed via the subject-line pair discriminator, count=1) + `readAtV119Close`/`lsTreeAtV119Close` exports — the MANDATORY back-anchor invariant v1.19 deliberately deferred (`V119-PIN-DEFERRAL`). **NO V120 pin** — deferred to v1.21 per the back-anchor invariant (`V120-PIN-DEFERRAL`).
7. **CI workflow `audit-harness-v1.20-integrity.yml`** (Plan 08): Path-A from v1.19 with every hardcoded v1.19 string re-derived, all `paths:` filter entries mirrored (collapsed to 5 entries from v1.19's 6 by folding the two milestone-specific documents into one `.planning/milestones/v1.20-*` glob covering all four v1.20 governance documents), `check-phase-139..144` jobs, the DUAL-APEX header paragraph restated, and deliberately NO standalone `check-phase-138` job (the v1.19 workflow already carries the sole standalone job for the predecessor apex). Every checkout step born with `fetch-depth: 0` (13 of 13). **17th** parallel coexistence file.
8. **Archive-root token confirmation** (Plan 07): the new apex uses the OBJECTIVELY CORRECT `'v1.20-phases'` token, asserted as a literal string in the apex source rather than relied upon via resolver success/failure (Correction of Record 5, above) — confirmed present, no predecessor form (`v1.1[5-9]-phases`) leaked in.

**Predecessor v1.4/v1.4.1/.../v1.19 harnesses + sidecars + workflows:** pin-drift ZERO against V119 (see the dedicated section above — this milestone's applicable form of the byte-unchanged gate, distinct in kind from a content-predecessor's full-surface freeze since v1.20 itself deliberately edits `scripts/validation/`). **CHAIN_PHASES**=`[48..143]` in `check-phase-144.mjs` (96 entries) plus `CHAIN_EXTRA=[30,31]`. **CHAIN_SKIP** is EMPTY.

## Deferred Items Summary

See `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` for the canonical deferred backlog — absorbed-and-appended over the 15 pre-existing routed rows (authored mid-milestone by Phase 142 Plan 01, extended by Phase 143 Plan 08), never rewritten.

- **Part A, NEW this milestone:** the C17 frozen-aware residue, now confirmed across all 5 of the 5 C17-bearing harnesses (v1.15 through v1.19 — not 5 of 18, since only those five carry a C17 check at all); `V120-PIN-DEFERRAL` (the back-anchor invariant's own successor obligation, structurally deferred to v1.21).
- **Part B, CARRIED:** the eight previously-omitted still-open v1.19 entries (`HUB-WIRING-NON-BARRED-SURFACE`, `HYG-05`, the Part-A `CI-3` scope correction, `SHARED-TAXONOMY-DOC`, `ANCHOR-REVIEW-BY-PAST-DUE`, `ROLLBACK-RECOVERY-DIVERGENCE-COUNT`, `AOSP-Wired 802.1X`, and both Correction-of-Record sections) plus `FENCE-AXIS-02`; `V-132-HUBSNOTWIRED-REGEX-BROKEN` (a negative assertion no link-resolution checker can discharge) and `RECIPE-OUTBOUND-LINK-COVERAGE` (a durability complaint the new leaf only partially answers via the apex chain, not as a standalone job) both stay explicitly open, not dropped; the ten unowned tooling items from the mid-milestone routing document (rows 1-6, 9, 10, 14, 15).
- **Part C, DROPPED-and-Closed:** `ACCEPTED-STANDALONE-CI-RED` (17/17 green discharge at the shared SHA) and `ACCEPTED-SCOPED-RED` (ten-member re-confirmation discharge at the close SHA) — both satisfying the milestone bar's requirement that they be **deleted** rather than carried a seventh milestone; `DEFER-119-A` (moved here rather than carried, since `regenerate-supervision-pins.mjs --self-test` now exits 0 and the requirement that owned it, RED-02, is Complete — carrying the owner while dropping the entry it solely owned would be incoherent).

## Milestone Close

**Pending Plan 144-12's single close-gate commit** — the record below states the terminal state this document and that commit together establish, not a claim that the flip has already landed at this document's own commit.

- **SWEEP-01 through SWEEP-09 (9):** Validated (Phases 139–141, 144)
- **RED-01 through RED-07 (7):** Validated (Phases 141–142)
- **LINK-01 through LINK-06 (6):** Validated (Phase 143)
- **NEST-01:** Validated (Phase 142)
- **GOV-01, GOV-02:** Validated (Phase 139)
- **HARN-17:** Validated (Plan 144-02 `dba21d0b` V119 pin + exports)
- **HARN-18:** Validated (Plans 144-04 through 144-08, 18th Path-A lineage bump)
- **HARN-19:** Validated (Plans 144-09 through 144-12, 3-axis re-audit + 17/17 dispatch + bundle + close-gate)
- **28/28 v1.20 requirements:** Validated (cumulative; see Requirements Traceability table)
- **6/6 v1.20 phases:** Complete (Phases 139 / 140 / 141 / 142 / 143 / 144)
- **v1.20 milestone:** shipped 2026-08-18 — Frozen-Aware CI Remediation & Chain-Validator Debt Closure

### Post-close hand-off to `/gsd-complete-milestone`

**Next:** Plan 144-12 lands the single close-gate commit and the post-close-gate confirmatory apex run — the terminal, non-optional part of the HARN-19 gate. After that, archival (`/gsd-complete-milestone`) is the SEPARATE next step: it moves phase dirs to `.planning/milestones/v1.20-phases/`, archives REQUIREMENTS/ROADMAP to `milestones/`, tags `v1.20`, and closes the Jira story. Read `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` as the v1.21 backlog source. Whether this close-gate commit lands local-only or is pushed immediately is the owner's call, mirroring the push-is-owner-gated discipline (D-18) that governed this phase's own Axis-2 capture.

**Grep-recovery for `{phase_144_close_SHA}` placeholder — the subject-line pair discriminator, NOT the naive dual-token form:**

```bash
git log --all --format="%H|%s" | awk -F'|' '$2 ~ /v1\.20/ && $2 ~ /MILESTONE CLOSE/'
```

Positively confirm the returned commit's **SUBJECT LINE** (not merely a body-text match) carries both the version token and "MILESTONE CLOSE" before trusting it — the same discipline this milestone applied recovering V119 (count=1, never the naive dual-token `--grep --all-match` form, which has returned multiple candidates at every prior milestone this method was checked against).

---

_Authored: 2026-08-18 — Phase 144 Plan 144-11 (gsd-executor sequential main-tree execution)_
