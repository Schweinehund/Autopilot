---
phase: 145-corpus-correction-validator-gate-archival-drift-fix
plan: 02
type: execute
wave: 2
depends_on: ["145-01"]
files_modified:
  - docs/operations/patch-management/00-overview.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, FIX-04, FIX-06, FIX-10]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The Ring Terminology section names the two current Autopatch default rings and no falsified cohort names"
    - "No mutual-exclusivity claim survives anywhere in 00-overview.md; the containment position replaces it"
    - "The driver/firmware sentence states both the true independence clause and the corrected per-ring gating fact"
    - "The Apple DDM identifiers appear as Intune settings-catalog display names, and OfferPrograms is present but recategorised"
    - "review_by minus last_verified is at most 60 days, computed by arithmetic from the commit date"
    - "check-phase-54.mjs stays at 32 passed / 0 failed and v1.20-milestone-audit.mjs at 16 passed / 0 failed"
  artifacts:
    - "docs/operations/patch-management/00-overview.md — one commit carrying FIX-01, FIX-02, FIX-03, FIX-04, FIX-06 (2 sites) and FIX-10"
  key_links:
    - "C11 (\\bAutopatch rings\\b, ±200-char window) in v1.20-milestone-audit.mjs -> the three plural hits in the Ring Terminology section and their mutual-exclusivity keepers, which die together"
    - "V-54-09's ~1500-byte window after the Ring Terminology H2 -> the compound WUfB-deployment-ring and Autopatch-ring literals that must survive the FIX-04 rename"
    - "V-54-29's body-prose ban -> the hotpatch, VBS and Android-integrity tokens that must not enter this file"
---

<objective>
Land the single most constrained file in the milestone as one indivisible commit: every correction
`00-overview.md` owns — FIX-01, FIX-02, FIX-03, FIX-04, FIX-06 (two sites) and FIX-10.

Purpose: D-17 class 1 forbids splitting this file across commits, because the C11 anti-pattern hits
and the prose that currently keeps them compliant are interleaved in the same twenty lines. Removing
the mutual-exclusivity wording in one commit and fixing the ring naming in another leaves an
intermediate commit where the live C11 audit is red.

This plan carries **one task** deliberately. The commit atom is indivisible, the executor commits per
task, and this file is recorded in STATE.md as the likeliest source of a mid-execution remediation
round in the whole milestone — so it gets a plan to itself rather than sharing context with another
hostile file.

Output: one commit touching exactly one file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-CONTEXT.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-RESEARCH.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-PATTERNS.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-01-SUMMARY.md
</context>

<artifacts>
## Artifacts this phase produces

No new symbols and no new files. This plan modifies one existing document. The phase-level artifacts
(the `V-59-14` assertion renamed with `[v1.5-frozen @ ba2cbc0]`, and `COVERAGE.md`) are listed in
`145-01-PLAN.md`. No SPEC.md exists for this phase, so there is no `## Edge Coverage` /
`## Prohibitions` section to lift.
</artifacts>

<tasks>

<!-- planner-discipline-allow: First -->
<!-- planner-discipline-allow: Fast -->
<!-- planner-discipline-allow: Broad -->
<!-- planner-discipline-allow: Autopatch rings -->
<!-- planner-discipline-allow: mutually exclusive -->
<!-- planner-discipline-allow: mutual-exclusion -->

<task type="auto">
  <name>Task 1: The 00-overview.md correction atom — FIX-01 + FIX-02 + FIX-03 + FIX-04 + FIX-06 + FIX-10, one commit</name>
  <precondition>`node scripts/validation/check-phase-54.mjs` exits 0 with 32 passed and `node scripts/validation/v1.20-milestone-audit.mjs` exits 0 with 16 passed BEFORE any edit — these are the baselines every acceptance criterion is measured against, and a pre-existing red would be misattributed to this task.</precondition>
  <reversibility rating="costly">Undoing the evidence-line convention means re-editing every corrected claim across all five patch-management documents, not just this one (D-01).</reversibility>
  <files>docs/operations/patch-management/00-overview.md</files>
  <read_first>
    - `docs/operations/patch-management/00-overview.md` — the whole file (218 lines).
    - `scripts/validation/check-phase-54.mjs` — **mandatory**. Read `V-54-07` (frontmatter platform value plus the ≤60-day interval arithmetic), `V-54-08` (the four-platform table regex), `V-54-09` (the Ring Terminology window), `V-54-10` (deferral + enforcement tokens), `V-54-29` (the body-prose token ban and exactly what it strips before testing) and `V-54-30`. Locate each with `grep -n "id: 7,\|id: 8,\|id: 9,\|id: 10,\|id: 29,\|id: 30," scripts/validation/check-phase-54.mjs`.
    - `scripts/validation/v1.20-milestone-audit.mjs` — the `C11: Ops-domain anti-pattern regex` assertion. Read the pattern list, the ±200-character window arithmetic and the `windowKeywords` alternation verbatim. Find it with `grep -n "C11: Ops-domain" scripts/validation/v1.20-milestone-audit.mjs`.
    - `docs/admin-setup-macos/10-kerberos-sso-extension.md` — read the one line carrying the `**Source:**` citation shape (`grep -n '\*\*Source:\*\*' docs/admin-setup-macos/10-kerberos-sso-extension.md`). This is the exact shape to copy (D-01); do not invent an inline parenthetical.
    - `145-CONTEXT.md` decisions **D-01** and **D-02** and **D-05** and **D-07** (evidence lines), **D-04** (what "dated after the event" is read as), **D-06** (dates), **D-08** and **D-09** and **D-10** (correction shape, per-file token obligations, preservation list), **D-11** (no separate dated correction marker, and why no anchor exists for one here), **D-13** (singularise, do not substitute), **D-14** (the named fallback), **D-15** and **D-16** (only one live C11 enforcer), **D-17** (this is atom class 1), **D-39** and **D-40** and **D-41** and **D-42** and **D-43** (rename scope and the disproved justification for it), **D-51** (the FIX-03 sentence split and why it cannot leave this commit), **D-52** (recategorise, never delete).
    - `145-RESEARCH.md` — the claim-to-source table rows for FIX-01, FIX-02, FIX-03, FIX-04 and FIX-06 (article titles, URLs and each source's own date, for the evidence lines).
  </read_first>
  <action>
**Re-measure before editing. Every coordinate is derived at execution time; none is transcribed.**
An adversarial review found a systematic off-by-one/two drift in the draft that produced these
documents. Run these first and work from the output:

```
grep -nio "Autopatch rings" docs/operations/patch-management/00-overview.md
grep -nE '\b(First|Fast|Broad)\b' docs/operations/patch-management/00-overview.md
grep -niE 'mutually[[:space:]]+exclusive|mutual-exclusion|mutual exclusion' docs/operations/patch-management/00-overview.md
grep -n 'OfferPrograms' docs/operations/patch-management/00-overview.md
grep -c 'WUfB' docs/operations/patch-management/00-overview.md
```

Two measured facts that correct the research documents and that you should confirm rather than
assume: the C11 pattern is applied **case-insensitively**, so the census must use `grep -i`; and the
cohort-name sweep must key on the three names **individually**, because the falsified parenthetical
in this file **wraps across a line break** and a comma-and grep does not match it.

Then make all of the following edits, and commit them together as one commit.

**A. FIX-01 — the falsified Autopatch cohort names.**
Replace the four-name parenthetical in the Autopatch bullet of the Ring Terminology section with the
two current default rings, `Test` and `Last`, and state the first-party facts FIX-01 requires:
they are automatically present, cannot be removed or renamed, carry one Entra group each, and the
service supports up to 15 rings per group and 300 groups per tenant. This is a **delete-and-replace**
correction, not an attribution: no validator pins the three cohort names (confirmed by grepping
`scripts/validation/` for them), and ROADMAP Success Criterion 2 requires a reader to find no
surviving instance of that naming. Do **not** write the old names into a "previously called…"
clause — that would reintroduce exactly the literals the success criterion bans.

**B. FIX-02 — the mutual-exclusivity claim.**
Remove every exclusivity assertion in this file and replace it with the first-party containment
position: an Autopatch group is a container that *includes* an `Update rings policy for Windows 10
and later` among the policies it creates and assigns, and the documented consequence is **loss of
direct authorship**, not exclusion. The claim appears in three places in this file (measure them
with the `grep -ni` command above): the opening sentence of the Ring Terminology section, a clause
inside the Autopatch bullet, and a standalone paragraph after the bullet list. There is also a
routing-pointer sentence near the end of the section that forwards the reader to the Windows guide
"for mutual-exclusion guidance" — reword that pointer so it no longer asserts the claim, while
keeping both the `PITFALL-9` label and the markdown link to `01-windows-wufb-rings.md` intact
(`PITFALL-9` is a corpus-wide cross-reference; its defining occurrence lives in the Windows guide and
is preserved there by plan 03).

**C. FIX-03 — the driver-surface sentence (D-51).**
Split it, do not strike it. The independence clause is verbatim-true first-party text and must
survive; the "not gated by rings" clause is false. Author (1) the independence statement — drivers
and firmware are an independent policy surface that admins approve per-update — and (2) the
corrected gating statement — approval mode and the 0-30 day deferral are set per deployment ring,
and the quality-update deadline and grace period **do** apply to drivers. Drop the plural form in
doing so.

**D. C11 — the ordering constraint, and why singularising discharges it (D-13, superseding the
ROADMAP blast-radius note).**
The live C11 audit flags each `Autopatch rings` plural whose ±200-character window contains no
allowlisted keyword. Today all three plural hits in this file are kept compliant **solely** by the
exclusivity wording that edits A-C remove. D-13 rules: **singularise, do not substitute a keyword.**
After edits A-C, every plural in this file must be gone — then the pattern has nothing to match and
the keeper question does not arise. Confirm with `grep -cio "Autopatch rings"` returning 0 **before**
you commit. Only if a site genuinely must keep the plural does D-14's fallback apply, and then it
requires **two** separate `disambiguation` insertions (one per surviving window), with the noun spelt
exactly — the verb form does not match the keyword regex. The same-commit ordering rule is discharged
structurally here: this is one commit, so no intermediate state ever reaches git history. Verify with
`node scripts/validation/v1.20-milestone-audit.mjs`, which is the **only** audit that reads live
`docs/` (D-15, corrected post-research) — the fifteen `v1.5`-through-`v1.19` audits read a frozen
corpus at their own close SHA and are structurally incapable of observing this edit.

**E. FIX-04 — the product rename (D-39, D-41, D-42, D-43).**
Rename the product in **body prose only**, to `Windows Update client policies`. Scope rules, all
owner-ruled or measured:
  - **Retain the coined compound** `WUfB deployment ring` everywhere it appears — it is 11 of this
    file's ~30 tokens and is the corpus's own coined term (D-39).
  - `WUfB` also legitimately survives in the product name for the reporting surface
    (Windows Update for Business **reports**), which FIX-04 names as its one carve-out.
  - **Do not rename headings or markdown link labels.** The navigation label pointing at the Windows
    guide mirrors that document's H1, which D-42 leaves untouched this milestone; D-43 records the
    resulting split label across pages as an accepted cost.
  - **Do not widen the scope on a pin-risk argument.** Per **D-40** the "the other files' pins were
    never surveyed" reasoning is disproved and must not be reused: only one validator file mentions
    the old product name at all. The binding authority for the five-document scope is ROADMAP Success
    Criterion 1's file list plus D-39's owner ruling, nothing else.
  - `V-54-09` requires both a `WUfB deployment ring` literal and an `Autopatch ring` literal within
    ~1500 bytes after the Ring Terminology H2. Both are compound forms you are retaining anyway —
    confirm they are still inside that window after the rewrite.

**F. FIX-06 — the Apple DDM identifiers and the two `OfferPrograms` sites (D-52).**
Present the six Intune settings-catalog **display names** (Delay in Days, Install Time, Details URL,
Target Build Version, Target Date Time, Target OS Version) as what an admin sees in Intune, alongside
the underlying declarative payload keys. At both `OfferPrograms` sites in this file — one inside a
table cell, one in body prose — **recategorise, never delete**: it is a real first-party Apple key
that lives in the `Beta` dictionary and governs beta-programme enrolment, not update enforcement.
FIX-06's requirement text governs over the `V-54-18` requirements-row remark that the key "may be
dropped freely"; that row describes pin latitude, not a content directive (D-52 — the row is flagged
for a one-line clarification at milestone close, not for action here).

**G. FIX-10 — evidence lines and dates (D-01, D-02, D-05, D-06, D-07).**
Add one `**Source:**` line per corrected claim, as a standalone paragraph directly beneath the
paragraph carrying that claim, in the exact shape used by the citation precedent you read. Use the
article titles, URLs and dates from `145-RESEARCH.md`'s claim-to-source table. Constraints:
  - The URL is **markdown-linked, never bare** (D-02). `V-54-29` strips markdown links before testing
    its banned tokens, so a linked article title is safe here where a bare URL would not be.
  - **What "dated after the event it describes" means (D-04, owner-ruled).** Read it as *dated after
    the corpus assertion being corrected*: the source's own publication date must post-date the
    `last_verified` stamp under which the falsified claim shipped. Three of this phase's corrected
    claims describe **future** events, for which no source can post-date the event itself, so the
    literal reading is unsatisfiable. No requirement text is edited to record this.
  - **No separate dated correction marker (D-11).** The re-stamped `last_verified` plus the
    `**Source:**` line carry the date between them. Do not invent a bracketed "corrected on" token:
    none of the five files has a version-history section, so the placeholder-token assertion's
    version-history carve-out does not exist here and there is no anchor a marker could hide behind.
  - Evidence lines go **outside** the leading platform-applicability blockquote (D-07). The routing
    pointers inside that blockquote are pointers, not claims; their evidence attaches to the body.
  - Re-stamp the frontmatter: `last_verified` is the **actual date this commit lands**, not a date
    copied from any planning document, and `review_by` is that date plus exactly 60 days computed by
    arithmetic. Compute both with:
    `python -c "import datetime; d=datetime.date.today(); print(d.isoformat(), (d+datetime.timedelta(days=60)).isoformat())"`
  - Leave the frontmatter `platform:` value as it is. `V-54-07` pins it for this file and it is
    already truthful for a cross-platform document; never add Linux to it.

**H. Standing negatives for this file.**
  - `V-54-29` bars the hotpatch, VBS and Android-integrity tokens from this file's **body prose**
    (table rows, fenced code, markdown links and inline code are stripped before the test). The
    hotpatch correction belongs in the Windows guide, which plan 03 owns; the Android integrity
    verdict belongs in the Android guide, which plan 04 owns. Do not import either here.
  - `V-54-08` requires the four-platform comparison table's header row to keep all four platform
    names. Cell-value edits are safe; do not restructure the table.
  - `V-54-30` bars placeholder tokens. Do not leave one behind.

**I. Gate, then commit.** Run every command in `<verify>` and confirm each is at its baseline before
committing. The commit touches exactly one file.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs   # expect: "Summary: 32 passed, 0 failed, 0 skipped", exit 0</automated>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs   # expect: "Summary: 16 passed, 0 failed, 0 skipped", exit 0</automated>
    <automated>grep -cio "Autopatch rings" docs/operations/patch-management/00-overview.md   # expect: 0</automated>
    <automated>grep -cE '\b(First|Fast|Broad)\b' docs/operations/patch-management/00-overview.md   # expect: 0</automated>
    <automated>grep -ciE 'mutually[[:space:]]+exclusive|mutual-exclusion|mutual exclusion' docs/operations/patch-management/00-overview.md   # expect: 0</automated>
    <automated>grep -c 'OfferPrograms' docs/operations/patch-management/00-overview.md   # expect: 2</automated>
    <automated>grep -c '^\*\*Source:\*\* \[' docs/operations/patch-management/00-overview.md   # expect: 4 or more</automated>
    <automated>python -c "import re,datetime,io; c=io.open('docs/operations/patch-management/00-overview.md',encoding='utf-8').read(); lv=datetime.date.fromisoformat(re.search(r'^last_verified: (\S+)',c,re.M).group(1)); rb=datetime.date.fromisoformat(re.search(r'^review_by: (\S+)',c,re.M).group(1)); print((rb-lv).days, lv==datetime.date.today())"   # expect: "60 True"</automated>
    <automated>node scripts/validation/check-phase-53.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-57.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-59.mjs   # expect: "Summary: 36 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs   # expect: "234 files checked, 0 with violations, 0 total violations"</automated>
    <automated>node scripts/validation/check-nav-hub-links.mjs   # expect: "0 hub-presence failure(s), 0 corpus-link failure(s), 0 total"</automated>
    <automated>git show --name-only HEAD   # expect: exactly one path, docs/operations/patch-management/00-overview.md</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-54.mjs` exits 0 and prints `Summary: 32 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0 and prints `Summary: 16 passed, 0 failed, 0 skipped`.
    - `grep -cio "Autopatch rings" docs/operations/patch-management/00-overview.md` returns `0`.
    - `grep -cE '\b(First|Fast|Broad)\b' docs/operations/patch-management/00-overview.md` returns `0`.
    - `grep -ciE 'mutually[[:space:]]+exclusive|mutual-exclusion|mutual exclusion' docs/operations/patch-management/00-overview.md` returns `0`.
    - `grep -c 'OfferPrograms' docs/operations/patch-management/00-overview.md` returns `2` — recategorised at both sites, deleted at neither.
    - `grep -c '^\*\*Source:\*\* \[' docs/operations/patch-management/00-overview.md` returns at least `4`, and every one of those lines carries a markdown-linked URL and a parenthesised update date.
    - The frontmatter arithmetic check prints `60 True` — a 60-day interval, stamped with today's date.
    - `grep -c 'Update rings policy for Windows 10 and later' docs/operations/patch-management/00-overview.md` returns at least `1` (the containment position is stated, not merely implied by deletion).
    - `git show --name-only HEAD` lists exactly one path.
    - `check-phase-53`, `check-phase-57`, `check-phase-59`, `c17-eee-contract` and `check-nav-hub-links` are all at their recorded baselines.
  </acceptance_criteria>
  <done>The overview hub states the current first-party position on Autopatch default rings, WUfB-versus-Autopatch containment, the driver gating split, the product name and the Apple DDM display names — each with a dated evidence line — inside a 60-day freshness window, with check-phase-54 and the live C11 audit both green, in one commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external documentation source → corpus prose | First-party Microsoft and Apple article content is transcribed into `docs/` as cited claims. No code fetches it; a human reader follows the link. |
| repo working tree → local validator execution | `scripts/validation/*.mjs` execute against the working tree locally and in GitHub Actions. |

This plan edits one markdown document. There is **no network surface, no authentication, no user
input and no secret** in scope. No threat is invented to fill a category.

## STRIDE Threat Register (OWASP ASVS Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-02 | Repudiation | corrected claim provenance | low | mitigate | Every corrected claim carries a `**Source:**` line naming the article, its markdown-linked URL and the source's own update date (D-01), so a later reader can audit which correction rests on which first-party page and when. |
| T-145-05 | Tampering | cited source content drifting after citation | low | accept | Sources are dated at citation time and the file's `review_by` is set 60 days out, which is the corpus's own re-verification mechanism. RESEARCH.md assumption A1 records the residual risk and bounds it to the short window between the research pass and execution. |
| T-145-04 | Information Disclosure | edited prose | low | accept | The corpus is a published knowledge base. The edits carry no tenant identifiers, credentials, customer data or secrets. |
| T-145-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install occurs in this phase (RESEARCH.md Package Legitimacy Audit: "Not applicable"). No dependency enters the tree. |

Spoofing, Denial of Service and Elevation of Privilege have no applicable component in this plan:
nothing authenticates, serves traffic, or acts on behalf of a principal.
</threat_model>

<verification>
- `node scripts/validation/check-phase-54.mjs` → `Summary: 32 passed, 0 failed, 0 skipped`
- `node scripts/validation/v1.20-milestone-audit.mjs` → `Summary: 16 passed, 0 failed, 0 skipped`
- `node scripts/validation/check-phase-53.mjs` → `Summary: 26 passed, 0 failed, 0 skipped`
- `node scripts/validation/check-phase-57.mjs` → `Summary: 26 passed, 0 failed, 0 skipped`
- `node scripts/validation/check-phase-59.mjs` → `Summary: 36 passed, 0 failed, 0 skipped`
- `node scripts/validation/c17-eee-contract.mjs` → `234 files checked, 0 with violations, 0 total violations`
- `node scripts/validation/check-nav-hub-links.mjs` → `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`
</verification>

<success_criteria>
- ROADMAP Success Criterion 1 is satisfied for this file: the current first-party position on Autopatch default rings, WUfB-versus-Autopatch containment, the driver deferral-versus-deadline split, the product rename and the Apple settings-catalog display names, each with a per-claim evidence line and a ≤60-day freshness interval computed by arithmetic.
- ROADMAP Success Criterion 2 is satisfied for this file at both of its named sites.
- The live C11 enforcer is green with zero plural anti-pattern hits remaining in this file.
</success_criteria>

<output>
Create `.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-02-SUMMARY.md` when done.
Record in it: the pre-edit and post-edit census outputs for the plural anti-pattern, the cohort
names and the exclusivity wording; the stamped date pair; the list of `**Source:**` lines added with
the article each cites; and the commit hash.
</output>
