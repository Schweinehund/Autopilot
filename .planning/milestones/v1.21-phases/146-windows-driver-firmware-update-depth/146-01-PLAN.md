---
phase: 146-windows-driver-firmware-update-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/patch-management/06-windows-driver-firmware-updates.md
autonomous: false
requirements: [DRV-01, DRV-02, DRV-03, DRV-04, DRV-05, DRV-06]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/operations/patch-management/06-windows-driver-firmware-updates.md exists and documents the driver/firmware update policy as its own surface — approval modes, the approval workflow, OEM-published catalog behavior and reporting (SC#1, DRV-01)."
    - "The guide states the deferral/deadline asymmetry verbatim-sourced: the quality-update deferral does NOT apply to policy-approved drivers, the quality-update deadline and grace period DO, and the automatically-approved scoping note rides alongside it (SC#2, DRV-02)."
    - "The guide documents no driver rollback with Microsoft's own named mitigations and the once-Approved-never-Declined constraint (SC#3, DRV-03)."
    - "The guide documents BOTH halves of the Autopilot absence together — driver policies do not apply during Autopilot AND Windows still applies critical, possibly unapproved, driver updates (SC#3, DRV-04, D-42)."
    - "The guide documents the Automatic-to-Manual mode switch as destructive of all approvals, paused and declined drivers (SC#3, DRV-05)."
    - "The guide documents the Configuration Manager co-existence procedure as a supported path carrying its first-party undefined-and-unpredictable-device-state warning (SC#4, DRV-06)."
    - "node scripts/validation/v1.20-milestone-audit.mjs reports 16 passed, 0 failed with 06 present in C11's live walkMd('docs')."
    - "node scripts/validation/check-nav-hub-links.mjs reports 0 hub-presence failures, 0 corpus-link failures — every outbound link and anchor fragment in 06 resolves (D-36)."
    - statement: "Every first-party claim in 06 carries a **Source:** line whose URL and (updated YYYY-MM-DD) value can be re-fetched and diffed against the quoted string after the two-codepoint fold (D-53)."
      verification: backstop
    - statement: "01:177's 'automatic firmware delivery for OEM-published catalogs' claim is corrected rather than relocated — 06's OEM section states the two sourced facts (firmware arrives on the Other drivers list; other-list updates require explicit approval) instead of the contradicted sentence (RESEARCH U-3)."
      verification: backstop
    - statement: "Q6's 'If it can't halt the installation' quote is attributed to driver-updates-faq, not to configure-driver-update-policy, correcting the FEATURES B-5 misattribution (RESEARCH §2.2)."
      verification: backstop
  artifacts:
    - docs/operations/patch-management/06-windows-driver-firmware-updates.md
  key_links:
    - "06's eight own-line <a id=\"...\"></a> anchors — the stable cross-link contract Phase 148 consumes (D-66) and the targets plans 02 and 03 link to in this phase."
    - "06's H1 '# Windows Driver and Firmware Updates' — Phase 152's registry Title (D-67)."
    - "06 -> 00-overview.md / 01-windows-wufb-rings.md / ../co-management/*.md relative links, all checked corpus-wide with no allowlist (D-36)."
---

<objective>
Author `docs/operations/patch-management/06-windows-driver-firmware-updates.md` — the dedicated
Windows driver and firmware update guide — from the verbatim quote bank in `146-RESEARCH.md` §2,
and land it as commit 1 of D-61's three-commit sequence.

Purpose: DRV-01..DRV-06. An admin planning driver and firmware updates gets one guide covering what
the policy does, what it silently does not do, and what happens during Autopilot. D-62 records that
this is the **riskiest** commit, not the safest: it puts ~500 lines of new prose under C11's live
`walkMd('docs')` walk and puts `06`'s outbound links into a corpus-wide checker that has no
allowlist, ratchet or expected-failure list of any kind.

Output: one new file. No registry row, no filename-map row, no canary bump, no ops-index row, no
`## Version History` (D-64, D-65) — all of that is Phase 152's atomic unit and adding any of it here
takes both pipeline self-tests to 225-vs-226 RED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-windows-driver-firmware-update-depth/146-CONTEXT.md
@.planning/phases/146-windows-driver-firmware-update-depth/146-RESEARCH.md
@.planning/phases/146-windows-driver-firmware-update-depth/146-PATTERNS.md
</context>

<phase_wide_rules>

These bind every task in this plan. They are locked decisions, not suggestions.

**R1 — Codepoint fold (RESEARCH Open Question 1, RESOLVED). State once, apply uniformly.**
In every quoted string, fold exactly two codepoints: `U+2019` (curly apostrophe) becomes the ASCII
`'`, and `U+2011` (non-breaking hyphen) becomes the ASCII `-`. **No other codepoint is folded** — em
dashes, en dashes, arrows, section signs, `<=`, `>=` and multiplication signs ship byte-verbatim
from the source. Measured basis: `U+2019` and `U+2011` each occur in **0** files under `docs/`
against 278 files carrying `U+2014`. Affected bank quotes: Q11, Q19, Q31 (U+2019) and Q18 (U+2011).
D-53's verifier applies the same two-codepoint fold to the re-fetched page text before diffing, so a
normalized comparison is still a real comparison.

**R2 — C11 defence is avoidance, not keyword-riding (D-01/D-02/D-04, RESEARCH §5.4).**
`06` never writes the strings `SCCM` or `System Center`. Every first-party source fetched says
*Configuration Manager*, which matches none of C11's four patterns. This is also the D-44-consistent
choice. Belt-and-braces, the Configuration Manager H2 carries the allowlisted keyword
`Co-management` anyway, and Q37's own preamble uses *"cloud attach or co-management"* verbatim so the
keyword arrives naturally in the section's first sentence rather than being planted.
**Verify by running the audit, never by reading the keyword list (D-04).**

**R3 — `06` never writes the plural `Autopatch rings` (D-43d).** That is C11's third pattern and it
is corpus-wide. Singular `Autopatch ring` is safe.

**R4 — Ring discipline in `06` (D-43, corrected rule).** (a) Verbatim quotes are **never
re-qualified** — they ship exactly as sourced inside blockquotes, including Q1's bare
`driver deployment rings` and F-5's bare `rings`. (b) In the guide's **own prose**, qualify every
`ring` as `WUfB deployment ring` or `Autopatch ring`, singular preferred. (c) **Do not coin
`driver deployment ring`** outside a blockquote — it appears in no first-party quote. Note that
`V-54-11` is scoped to `PATCH_FILES` (`check-phase-54.mjs:35`, five hardcoded paths) and therefore
does **not** reach `06`; this is an editorial rule with only C11's plural as a hard gate.

**R5 — `V-54-27` does reach `06` (D-05).** `check-phase-54.mjs:437-486` walks `docs/` **and**
`.planning/` for a line-start `> **Platform:**`. Use the full lexicon lead-in
`**Platform applicability:**` in the blockquote. The framing "06 is outside `PATCH_FILES` therefore
unreachable" is true of `V-54-07`/`V-54-11`/`V-54-26` and **false as a general claim**.

**R6 — Link discipline (D-36).** `06` links only to `00-`, `01-`, `02-`, `03-`, `04-` and existing
`docs/` targets. No `05-` (Phase 147), no `07-`/`08-` (Phase 148), no firmware/BIOS guide (Phases
149/150). `check-nav-hub-links.mjs:6-8` states verbatim that it runs with no accepted-violation
baseline, allowlist, ratchet file or expected-failure list of any kind. It also resolves **anchor
fragments** (`:295-297`, `anchor not found: #<frag>`), so every `file.md#fragment` link must hit a
real `<a id>` value or a real GitHub heading slug.

**R7 — Citation shape.** `**Source:** [Title](url) (updated YYYY-MM-DD)` as a standalone paragraph,
never inside the blockquote, never inline. The date is the **`updated_at`** value from RESEARCH §1,
not `ms.date` — confirmed by matching `01:165-166` against their sources. Two consecutive
`**Source:**` lines with no blank line between them is an existing corpus shape (`01:165-166`) and is
the required form for the B-5 dual-attribution case.

**R8 — No diagram, no Mermaid fence, no decision tree (D-68).** A Mermaid fence would block any
future C17 enrollment.

**R10 — The filename is `06-windows-driver-firmware-updates.md` and it is NOT renumbered (D-32).**
Do not renumber despite `05-` not existing until Phase 147. Measured: **no validator enumerates the
directory** — the five paths are hardcoded constants at `check-phase-54.mjs:23-27` and `PATCH_FILES`
at `:35`, and the only `readdirSync` in that file is `V-54-27`'s corpus walk. This ruling is what
discharges Phase 148's dependency on a stable filename, so a "tidier" renumbering here would break a
later phase's cross-links for no gain.

**R9 — No `doc_id` key in frontmatter (D-33/D-34).** `c17-eee-contract.mjs:5,:526-537` gates
enrollment on `doc_id` and `docs/operations/` is enrolled by directory, never cherry-picked by
filename. C17 baseline is 234 files / 0 violations and this plan does not move it.

**Baselines at HEAD `9b913cba`, all seven re-run and confirmed at plan time (D-63):**
apex `check-phase-144` **101 PASS / 0 FAIL / 0 SKIPPED** (18.9s) · `v1.20-milestone-audit` **16/0** ·
`c17-eee-contract` **234 files / 0 violations** · `check-nav-hub-links` **0/0** · `check-phase-54`
**32/0/0** · `build-publish-bundle --self-test` **15/0** · `build-filename-map --self-test` **8/0**.
Note the two pipeline scripts live at `scripts/pipeline/`, not `scripts/`.

</phase_wide_rules>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end skeleton — 06 exists, is C11-clean, and every anchor and link resolves</name>

  <files>docs/operations/patch-management/06-windows-driver-firmware-updates.md</files>

  <read_first>
    - `docs/operations/patch-management/01-windows-wufb-rings.md:1-26` — the five frontmatter fields
      in order, the full-lexicon applicability blockquote, the H1, and the two-paragraph opening.
    - `docs/operations/patch-management/01-windows-wufb-rings.md:215-231` — the
      `## Related Resources` and `## External References` shapes and their link-text conventions.
    - `docs/operations/patch-management/00-overview.md:63-64` and `:209-229` — the own-line
      `<a id>`-above-H2 convention and the second Related/External pair.
    - `146-PATTERNS.md` §1 (frontmatter + blockquote + H1 + opening-paragraph shape) and §2
      (`**Source:**` placement).
    - `146-CONTEXT.md` D-32, D-33, D-34, D-35, D-36, D-37, D-39, D-42, D-65, D-66, D-67, D-70.
    - `146-RESEARCH.md` §5.3 (Windows-version applicability) and §2.7 (Q29, Q30).
    - `scripts/validation/check-nav-hub-links.mjs:142-200` and `:269-300` — the anchor-set model
      (explicit `<a id>` values verbatim, unioned with GitHub heading slugs) and the inbound-link
      scan that reports `anchor not found`.
  </read_first>

  <behavior>
    This is the tracer slice: the thinnest path that touches every layer this phase will modify,
    wired end to end and proven green before ~500 lines of prose exist to bisect. The layers are
    (1) the file itself, (2) C11's live corpus walk, (3) the corpus-wide link and anchor checker,
    (4) the cross-file anchor contract that plans 02 and 03 link into. All four are exercised here.
    - The file parses as a sibling: five frontmatter fields in `01`'s order, `platform: Windows`.
    - C11 sees new prose under `walkMd('docs')` and stays at 16/0.
    - Every own-line anchor plans 02 and 03 will target already exists, so their links cannot dangle.
    - Every outbound relative link resolves under a checker with no allowlist.
  </behavior>

  <action>
**First, before touching any file, capture D-63's pre-phase apex baseline.** Run
`node scripts/validation/check-phase-144.mjs` and confirm it prints
`Result: 101 PASS, 0 FAIL, 0 SKIPPED (total checks: 101)`. This is the "once before commit 1" half of
D-63 and it is the only run that can distinguish a regression this phase caused from one that was
already at HEAD. If it is not 101/0/0, stop and report — do not proceed and do not treat a lower
number as the new baseline.

Then create the file at exactly `docs/operations/patch-management/06-windows-driver-firmware-updates.md`.
Per R10 and D-32, **do not renumber it** even though `05-` will not exist until Phase 147 — no
validator enumerates this directory, and the stable filename is what Phase 148's dependency rests on.

Give it its complete structural skeleton and its fully-authored link surface. Content
sections stay as one-line placeholders that Tasks 2 and 3 replace; the link-bearing and
frontmatter-bearing parts are authored for keeps in this task, because those are exactly what the two
gates test.

Frontmatter — the five sibling fields only, same names, same order as `01:1-7`, per D-34:
`last_verified` set to the actual execution date (D-70; if execution spans midnight all three of
this phase's files take the date of commit 1), `review_by` computed as `last_verified + 60` by
arithmetic per 145 D-06, `applies_to: all`, `audience: admin`, `platform: Windows`. Add no `doc_id`
(R9). If the execution date is 2026-08-19 the stamps are `last_verified: 2026-08-19` /
`review_by: 2026-10-18`, matching all five siblings exactly.

Applicability blockquote — every line `>`-prefixed, opening with the full-lexicon bold lead-in
`**Platform applicability:**` per R5/D-05. Per D-35 the **content** must differ from `01`'s: `01`'s
blockquote is a per-sibling link list and `06` may not link forward, so describe scope instead —
this guide covers the Windows driver and firmware update policy surface, and route back to
`00-overview.md` for the cross-platform overview and to `01-windows-wufb-rings.md` for WUfB
deployment ring topology. Do not enumerate macOS/iOS/Android sibling links.

H1 — exactly `# Windows Driver and Firmware Updates` per D-67, American spelling, plain `# Title`
with no anchor above it. This string becomes Phase 152's registry `Title`, which
`scripts/pipeline/build-filename-map.mjs:4` treats as the sole source of truth with an unresolved
collision a hard `exit 1` at `:145`/`:175`, so it must stay distinct from `01`'s
`# Windows WUfB Rings + Hotpatch + Driver/Firmware` and from every other registry Title.

Opening — two paragraphs before the first anchor, matching `01:18-25`. Paragraph one states what the
guide covers. Paragraph two routes back to `00-overview.md`. Then, per D-42, a **one-line warning
near the top** carrying the Autopilot pitfall: driver update policies do not apply during Windows
Autopilot, and Windows still applies critical updates during Autopilot which may include unapproved
critical driver updates. D-42 requires both halves always together — this top-of-guide line is the
first half of that pairing and Task 3 authors the second half as the first item of the callouts
section. Also place here D-71's one-sentence Windows-version statement, sourced in RESEARCH §5.3: the
policy surface is not Windows-11-only (it covers Windows 10 and later, scoped by edition, LTSC
excluded) and **only** the Configuration Manager co-existence procedure carries the Windows-11
restriction.

The eight content H2s, each preceded by its own-line `<a id="..."></a>` on the line immediately
above, exactly per D-37's skeleton with D-39's American spelling. Use these anchor ids verbatim —
they are the D-66 contract that Phase 148 cross-links and that plans 02 and 03 target in this phase,
so a rename here breaks two later plans and a later milestone:

`what-this-policy-does` / `## What This Policy Does`;
`approval-modes` / `## Approval Modes`;
`approval-workflow` / `## The Approval Workflow`;
`deferral-deadline-behavior` / `## Deferral and Deadline Behavior`;
`oem-catalog-firmware` / `## OEM Catalog and Firmware Delivery`;
`driver-update-reporting` / `## Reporting`;
`configmgr-coexistence` / `## Configuration Manager Co-management and Co-existence`;
`unsupported-callouts` / `## Unsupported and Anti-Feature Callouts`.

The Configuration Manager H2 name is Claude's Discretion constrained by D-04, and this spelling
satisfies it two ways: it contains the allowlisted window keyword `Co-management`, and per R2 the
section will contain no C11 hit for that keyword to have to keep green in the first place. Do not
name it `## ConfigMgr Co-existence` — `co-existence` is not an allowlisted keyword and `co-management`
is. Do not use the invented `## Limitations and Absences` for the callouts H2; the corpus convention
confirmed in all four recipes and `docs/_templates/recipe-template.md:98` is
`## Unsupported and Anti-Feature Callouts` (D-38).

Then `## Related Resources` and `## External References`, both **without** an own-line anchor —
the sibling convention measured across `00`, `01` and `02` is that only content H2s carry anchors.
Author both fully now. Related Resources links only to `00-overview.md`,
`01-windows-wufb-rings.md`, `../co-management/02-windows-workload-sliders.md` and
`../co-management/03-cocmgmt-migration-paths.md` (R6/D-36; the co-management file is cross-link-only
and read-only for this phase per D-72). External References carries the Learn URLs from RESEARCH §1
S1-S8 and may duplicate the siblings' entries freely (D-69) — note that
`check-nav-hub-links.mjs:242` returns null for `http(s)` targets, so **no tool verifies those URLs**
and the orchestrator confirms each one manually during verification.

Add no `## Version History` (D-65: 249 of 282 corpus files carry it but only 2 of 20 under
`docs/operations/` and none of the five patch siblings; domain convention wins). Add no Mermaid
fence and no decision tree (R8/D-68).

Leave each of the eight content sections holding a single one-line sentence stating what the section
will cover. Do not write `TBD`, `TODO`, `FIXME`, `XXX` or `PLACEHOLDER` — `V-54-30` bars those tokens
and although it is scoped to `PATCH_FILES` and cannot see `06`, the habit is what matters and Tasks 2
and 3 overwrite these lines anyway.
  </action>

  <acceptance_criteria>
    - `test -f docs/operations/patch-management/06-windows-driver-firmware-updates.md` succeeds.
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `Summary: 16 passed, 0 failed`
      and exits 0.
    - `node scripts/validation/check-nav-hub-links.mjs` prints
      `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total` and exits 0.
    - `node scripts/validation/check-phase-54.mjs` still prints `Summary: 32 passed, 0 failed,
      0 skipped` (creating `06` must not perturb it — `PATCH_FILES` at `:35` is five hardcoded paths
      and only `V-54-27`'s corpus walk sees the new file).
    - `node scripts/validation/c17-eee-contract.mjs` still prints `234 files checked, 0 with
      violations` — the count must NOT become 235, confirming D-33's no-`doc_id` rule held.
    - `grep -c '^doc_id:' docs/operations/patch-management/06-windows-driver-firmware-updates.md`
      returns `0`.
    - `grep -n '^# ' docs/operations/patch-management/06-windows-driver-firmware-updates.md` returns
      exactly one line and it is `# Windows Driver and Firmware Updates`.
    - `grep -c '^<a id=' docs/operations/patch-management/06-windows-driver-firmware-updates.md`
      returns `8`, and `grep -o 'id="[a-z-]*"' ...` lists exactly: `what-this-policy-does`,
      `approval-modes`, `approval-workflow`, `deferral-deadline-behavior`, `oem-catalog-firmware`,
      `driver-update-reporting`, `configmgr-coexistence`, `unsupported-callouts`.
    - `grep -n '^## ' ...` returns exactly 10 headings in D-37's order, ending with
      `## Related Resources` then `## External References`.
    - `grep -c '^## Version History' ...` returns `0` (D-65).
    - `grep -cE '^\s*```mermaid' ...` returns `0` (D-68/R8).
    - `grep -n 'Platform applicability' ...` matches, and
      `grep -cE '^> \*\*Platform:\*\*' ...` returns `0` (R5/D-05/V-54-27).
    - `grep -cE '\bSCCM\b|\bSystem Center\b' ...` returns `0` (R2/D-04).
    - `grep -c 'Autopatch rings' ...` returns `0` (R3/D-43d).
    - `grep -cE '\(0?5-|\(0?7-|\(0?8-|firmware-bios' ...` returns `0` — no forward link to Phase
      147/148/149/150 targets (R6/D-36).
  </acceptance_criteria>

  <verify>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs &amp;&amp; node scripts/validation/check-nav-hub-links.mjs &amp;&amp; node scripts/validation/check-phase-54.mjs &amp;&amp; node scripts/validation/c17-eee-contract.mjs</automated>
  </verify>

  <done>
    `06` exists with its complete ten-H2 skeleton, its eight D-66 contract anchors, its fully
    authored Related Resources and External References sections, its D-42 top-of-guide Autopilot
    warning and its D-71 applicability sentence. All four gates are green at their exact HEAD
    baselines: audit 16/0, nav-hub-links 0/0, check-phase-54 32/0/0, C17 234/0. Nothing is committed
    yet — this plan lands as one commit at the end of Task 3 per D-61.
  </done>

  <reversibility rating="reversible">
    A skeleton file with no prose is trivially editable or deletable; nothing outside it references
    it until plans 02 and 03 run.
  </reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>
    Confirm the DRV-03 sourcing contract before any first-party quote is authored into `06`.
  </decision>
  <context>
    D-50 is **owner-ruled and rated one-way**: if the re-fetch fails, the phase blocks and escalates
    to the owner — it does not ship. Both drafted fallbacks were rejected as unsafe. Branch 1 rested
    on a guess (`grep -rn "never be Declined" .planning/` returns exactly one hit, the B-5
    search-summary cell itself). Branch 2 was worse, because DRV-03 **and** SC#3 both mandate the
    constraint's **presence**, so shipping it "as a stated absence" fails the phase. The recorded
    one-way risk is that a fabricated or absent DRV-03 constraint is a close-gate failure discovered
    late.

    RESEARCH §4 records that the re-fetch **SUCCEEDED**: `configure-driver-update-policy` returned
    HTTP 200 as both a rendered page (80,680 B) and source markdown (29,935 B), and DRV-03's
    once-Approved-never-Declined constraint is confirmed verbatim at source `:218` (Q5). **The
    block-and-escalate branch therefore does not fire. Do not re-run the fetch and do not plan
    around a failure that did not happen.**

    Two live sourcing traps remain and this checkpoint exists to force them into view before ~200
    lines of quoted prose are written:
    1. **`FEATURES.md` misattributes B-5.** Q6's *"If it can't halt the installation, the update
       completes its installation"* is on `driver-updates-faq`, **not** on
       `configure-driver-update-policy` — a case-insensitive grep for `halt` over that page's full
       29,935-byte source markdown returns **0 hits**. Q5 **is** on `configure-driver-update-policy`
       at `:218`. Citing both halves to one page is a fabricated attribution even though both quotes
       are real.
    2. **R1's codepoint fold** must be settled before the first quote is typed, not after. Q11, Q19
       and Q31 carry `U+2019`; Q18 carries `U+2011`.
  </context>
  <options>
    <option id="proceed-as-planned">
      <name>Proceed — quote from the §2 bank, cite each half to its real page, apply the R1 fold</name>
      <pros>Matches D-50, D-53 and RESEARCH §4 exactly. The re-fetch already succeeded, so no
        network work is required and no fabrication risk is taken.</pros>
      <cons>None identified. This is the recorded intent.</cons>
    </option>
    <option id="escalate">
      <name>Escalate to the owner — a bank quote cannot be reproduced from RESEARCH §2</name>
      <pros>Honours D-50's block-and-escalate rule if the quote bank turns out to be incomplete for
        a claim DRV-03 or SC#3 mandates.</pros>
      <cons>Halts the phase. Only legitimate if a required quote is genuinely absent from §2 — never
        because a quote is inconvenient. A different first-party page carrying the claim, attributed
        to that page, is the permitted alternative; an unsourced assertion never is.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed-as-planned or escalate</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author sections 1-4 — what the policy does, approval modes, the approval workflow, deferral and deadline behavior</name>

  <files>docs/operations/patch-management/06-windows-driver-firmware-updates.md</files>

  <read_first>
    - `146-RESEARCH.md` §2.1 (Q1, Q2, Q3, Q3b, Q3c and the AF-6 first-party range table), §2.2 (Q4,
      Q5, Q6, Q28, Q28b, Q28c and the four-status transition block at S1 `:214-218`), §2.3 (Q7-Q18
      and the four-stage architecture at S3 `:33-36`), §2.6 (Q32, Q32b, Q36b), §2.10 (Q55-Q63).
    - `146-RESEARCH.md` §1 — the eight-page table, for the exact `updated_at` value each
      `**Source:**` line must carry.
    - `146-RESEARCH.md` §3 (DRV-01, DRV-02, DRV-03 rows) — the per-section claim contract.
    - `146-RESEARCH.md` §5.5 — the minimum viable citation set, one primary plus one secondary per H2.
    - `146-PATTERNS.md` §2 (Source-line placement, including the two-consecutive-lines shape) and §3
      (blockquote-plus-Source-line rendering, new to this directory).
    - `146-CONTEXT.md` D-40, D-43, D-44, D-46, D-47, D-53, D-54.
    - `docs/operations/patch-management/01-windows-wufb-rings.md:165-166` — the two-consecutive-
      `**Source:**`-lines precedent, byte-exact.
  </read_first>

  <behavior>
    - `## What This Policy Does` names the driver update policy as a dedicated surface distinct from
      quality and feature updates, walks the four-stage Intune to Autopatch to Windows Update to
      reporting flow, states that client-side install behavior stays with Windows Update client
      policies, that only drivers published to Windows Update are in scope, and that Intune keeps no
      inventory of installed drivers.
    - `## Approval Modes` contrasts Automatic (the recommended default, reaching recommended drivers
      only) with Manual (nothing installs without explicit approval), and states plainly that the
      approval type is **immutable after policy creation** and that Other-drivers always require
      manual approval in both modes.
    - `## The Approval Workflow` carries the four statuses and the complete transition rule set,
      approved-always-wins, one policy per device, the `Make available in Windows Update` date
      semantics, the no-downgrade guarantee, the bulk-action limits, and pause-is-best-effort.
    - `## Deferral and Deadline Behavior` resolves the asymmetry as a complete triad rather than a
      bare negative: the ring quality deferral does **not** reach drivers, the deadline and grace
      period **do**, and user-experience settings **do**.
  </behavior>

  <action>
Replace the four placeholder lines under `what-this-policy-does`, `approval-modes`,
`approval-workflow` and `deferral-deadline-behavior` with authored prose. Every first-party claim
gets a `**Source:**` line in R7's shape. Apply R1's two-codepoint fold to every quoted string, R2's
Configuration-Manager-only naming, R3's no-plural-`Autopatch rings` rule and R4's ring discipline
throughout.

`## What This Policy Does` — open with Q17 (S3 `:13`) naming driver update policies as a dedicated
policy surface for reviewing, approving and deploying driver updates. Reproduce the four-stage
architecture from S3 `:33-36` as the section's spine — Intune supplies identity, assignment and
approval; Autopatch configures Windows Update behavior and coordinates deployment; Windows Update
evaluates applicability and installs only approved updates during regular scans; reporting data
returns through Autopatch into Intune. Add Q18 (client-side install behavior — restarts and user
notifications — remains governed by standard Windows Update policy settings; this quote carries the
`U+2011` fold per R1). Add Q23b (only drivers currently published to Windows Update and applicable
to a device in the policy are available through the policy) and Q36b (the driver list is not a record
of installed versions; Intune collects no inventory of installed drivers). Close with the practical
prerequisites from §2.10 — Q55 editions with LTSC excluded, Q56 licensing, Q57 the cloud-scope limit
where GCC High and DoD are absent, Q58 device requirements including the Required telemetry minimum
and the `wlidsvc` service, Q59/Q60 the RBAC split between managing and viewing reports, and Q61's
two not-blocked prerequisites. Per RESEARCH §5.5 cite S3 `manage-driver-updates` as primary and S1
`configure-driver-update-policy` as secondary; PATTERNS §2 records `00-overview.md:90-94` as the
three-consecutive-`**Source:**`-lines precedent for a multi-source block like this one.

Use the current product name **Windows Update client policies** in prose per D-44 (FIX-04's naming
rule carried into `06` by owner ruling), while retaining the `WUfB deployment` compound where the
compound is the term — 52 of the domain's 78 `WUfB` matches are that deliberately-retained compound.

`## Approval Modes` — Q8 for Automatic as the recommended default for standard OEM devices with no
recent driver or hardware issues, Q10 for what Automatic actually does (all new **recommended**
driver updates enter with status Approved and begin installing without review), Q7 and Q9 for Manual
(nothing installs without explicit approval; each new update enters as Needs review and an admin must
change it to Approved). Then Q11, which is the section's highest-value sentence and carries the
`U+2019` fold: after a policy is created the approval type cannot be edited, and only the
`Make updates available after (days)` value remains editable on an automatic policy. Close the
recommended-versus-other distinction with Q20 (recommended is the best match for required updates,
and the OEM must mark the update as required and it must be the most recent such version) and Q21
(other drivers are everything else the OEM offers, **including firmware updates** and optional
updates the OEM does not intend for all devices), then Q12 — other-list updates enter as Needs review
and **must be manually approved before they can be deployed**, which is true even under an automatic
policy. Primary source S1, secondary S7.

`## The Approval Workflow` — lead with the four-status transition block quoted verbatim from S1
`:214-218` as a blockquote (only new updates can be Needs review, though a new recommended update on
an Automatic policy is added as Approved; Needs review can go to Approved or Declined; Approved can
be Paused; Paused can be Approved; and once Approved an update can never be Declined but can be
Paused indefinitely). Q5 is DRV-03's mandated constraint and is the last line of that block — call it
out explicitly in prose beneath the blockquote so a reader cannot miss it. Then Q14 for the
`Make available in Windows Update` date semantics on a manual approval, Q13 for the no-downgrade
guarantee, Q15 and Q16 for the bulk-action limits (100 drivers at a time; actions cannot be mixed),
and Q32 with Q32b for cross-policy behavior — approved always wins, so the driver installs despite
any other status in any other policy, which is precisely why Microsoft recommends one policy per
device.

Then D-46's placement: Q6 lands **here**, in this section, because it is what makes DRV-03's
rollback-absence story honest. Quote it as a blockquote — pause is a best effort, Autopatch removes
the approval, devices do not know an update is paused until their next scan, and if the device is
already downloading, installing or waiting to restart then Windows Update attempts a best effort to
remove the driver update and **if it cannot halt the installation the update completes its
installation**. Pair it with Q28 (pausing does not roll back a completed installation but can stop an
active install underway) and Q28c (Microsoft's own cross-policy guidance: monitor an automatic policy
for early signs of problems and pause the same update in your other policies).

**Cite Q5 and Q6 to different pages.** Q5 is on `configure-driver-update-policy` (S1 `:218`); Q6 is
on `driver-updates-faq` (S2 `:128-130`). `FEATURES.md:83` states both come from
`configure-driver-update-policy` and that is measurably false — a case-insensitive grep for `halt`
over that page's full source markdown returns 0 hits. Use the two-consecutive-`**Source:**`-lines
shape from `01:165-166` where both pages back one passage, but never put one `**Source:**` line under
both quotes as if one page carried them (RESEARCH §5.7's second named anti-pattern).

`## Deferral and Deadline Behavior` — this is DRV-02 and SC#2, so it ships verbatim-sourced. Q1 as a
blockquote: the quality-update deferral set in the update ring policy does **not** apply to drivers
approved by the driver update policy, and the driver policy's own deferral setting is the lever
instead. Q1 contains the bare literal `driver deployment rings` — ship it **inside the blockquote**
and never repeat the phrase in the guide's own prose (R4b/R4c, D-43). Q2 is the scoping note DRV-02
mandates rides alongside it: the deferral applies **only** to automatically approved driver and
firmware updates, and an admin must specify the start date for any manual approval. Q3 is the other
half of the asymmetry — the quality-update deadline and grace period **do** apply to drivers — with
Q3b for how the deadline is calculated (from the time the client's scan initially discovered the
update). Q3c gives the driver policy's own deferral: zero to 30 days, counted from the day the update
is added to the policy, **not** from when the OEM published it. Complete the boundary with Q63:
user-experience settings such as automatic update behavior, active hours and notifications **do**
apply to driver updates. That triad — deferral no, deadline and grace yes, user experience yes — is
the whole section and is what makes it more than a bare negative.

**AF-6 is one of the two live search-summary rows this phase had to resolve, and it never appeared
in the draft at all (D-47).** The search-summary identification was reversed: `ROADMAP.md` names two
rows, `FEATURES.md:11` names them B-5, AF-6 and X-3, and X-3 is already resolved — so the two live
rows are **B-5 and AF-6**, both Phase 146 content and both on `configure-driver-update-policy`.
AF-6's `driver deferral 0-30d` range is already shipped at `00-overview.md:87` and is exactly what
this section restates, now upgraded from search-summary to first-party.

Include the AF-6 range table from RESEARCH §2.1 sourced first-party to S5 `ref-update-ring-settings`
(quality deferral 0-30, feature deferral 0-365, both deadlines 2-30, grace 0-7), plus Q3c's driver
deferral. AF-6 carries a standing **"generic, not tenant-specific"** constraint that binds here: give
ranges and branch criteria, never a worked tenant schedule such as a named cohort at a fixed
percentage on a fixed day. Primary source S2, secondary S5.

Do **not** tell the verifier the sections map one-to-one onto DRV-01..06. D-40 deletes that claim as
false on its own table: DRV-01 maps to five sections and DRV-03 and DRV-05 map to two each. Telling
a verifier to expect a bijection it will not find is worse than saying nothing.
  </action>

  <acceptance_criteria>
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `Summary: 16 passed, 0 failed`.
    - `node scripts/validation/check-nav-hub-links.mjs` prints
      `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`.
    - The literal `can never be Declined` appears in the file (DRV-03 / SC#3 / Q5, the D-50 one-way
      constraint).
    - The literal `If it can't halt the installation, the update completes its installation`
      appears in the file (Q6 / B-5 / D-46).
    - The literal `does not apply to drivers that are approved using the Driver Update Policy`
      appears in the file (Q1 / DRV-02 / SC#2).
    - The literal `The deferral period only applies to automatically approved driver and firmware
      updates` appears in the file (Q2, the scoping note DRV-02 mandates rides with it).
    - The literal `The Quality Update deadline and grace period settings apply to drivers` appears
      in the file (Q3, the second half of the asymmetry).
    - `grep -c 'driver-updates-faq' ...` is at least 1 **and** the `**Source:**` line nearest the Q6
      quote names `driver-updates-faq`, not `configure-driver-update-policy` (the FEATURES B-5
      misattribution correction).
    - `grep -cP '[\x{2019}\x{2011}]' ...` returns `0` — the R1 fold was applied and no `U+2019` or
      `U+2011` reached the file. Every other non-ASCII codepoint is permitted and unmodified.
    - `grep -cE '\bSCCM\b|\bSystem Center\b' ...` returns `0` (R2).
    - `grep -c 'Autopatch rings' ...` returns `0` (R3).
    - `grep -v '^>' <file> | grep -c 'driver deployment ring'` returns `0` — the phrase appears only
      inside blockquoted verbatim quotes, never in the guide's own prose (R4b/R4c, D-43c).
    - Every `**Source:**` line in the four authored sections matches
      `^\*\*Source:\*\* \[.+\]\(https://learn\.microsoft\.com/.+\) \(updated [0-9]{4}-[0-9]{2}-[0-9]{2}\)$`
      and its date equals that page's `updated_at` value in RESEARCH §1 (R7).
    - `grep -c 'one-to-one' ...` returns `0` (D-40 — the bijection claim is deleted, not softened).
  </acceptance_criteria>

  <verify>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs &amp;&amp; node scripts/validation/check-nav-hub-links.mjs &amp;&amp; grep -q "can never be Declined" docs/operations/patch-management/06-windows-driver-firmware-updates.md &amp;&amp; grep -q "The Quality Update deadline and grace period settings apply to drivers" docs/operations/patch-management/06-windows-driver-firmware-updates.md &amp;&amp; test "$(grep -cP '[\x{2019}\x{2011}]' docs/operations/patch-management/06-windows-driver-firmware-updates.md)" = "0"</automated>
  </verify>

  <done>
    Sections 1-4 are authored and sourced. DRV-01's approval-modes and approval-workflow clauses,
    DRV-02 in full with its scoping note, and DRV-03's once-Approved-never-Declined constraint and
    pause semantics are all present verbatim with correct per-page attribution. Audit is 16/0 and
    nav-hub-links is 0/0. Not yet committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author sections 5-8 — OEM catalog and firmware, reporting, Configuration Manager co-existence, the callouts — then land commit 1</name>

  <files>docs/operations/patch-management/06-windows-driver-firmware-updates.md</files>

  <read_first>
    - `146-RESEARCH.md` §2.4 (Q19-Q23c and the U-3 correction of record), §2.5 (Q24-Q27c and the
      admin-center navigation strings), §2.6 (Q31-Q36b), §2.7 (Q29, Q30), §2.8 (Q37-Q53, the
      complete unelided procedure), §2.10 (Q55, Q57).
    - `146-RESEARCH.md` §5.2 — the section-fill assessment for the two thin H2s and the ten available
      callout items.
    - `146-RESEARCH.md` §4 — U-1 (rendered-page provenance), U-3, U-4 (the FAQ-versus-GPO name
      divergence) and the Assumptions Log entry A2.
    - `146-PATTERNS.md` §4 (the bold sub-label form at `01:186`), §5 (the three-column recipe table
      shape) and §6 (the numbered multi-step procedure form at
      `docs/operations/co-management/03-cocmgmt-migration-paths.md:34-47`).
    - `docs/_templates/recipe-template.md:98-102` — the callouts table header, byte-exact.
    - `docs/recipes/01-shared-windows-avd-client.md:39-45` — the same table filled.
    - `146-CONTEXT.md` D-23, D-26, D-27, D-28, D-29, D-30, D-31, D-38, D-41, D-42, D-45, D-49, D-61.
    - `docs/operations/patch-management/01-windows-wufb-rings.md:171-178` — the movable block whose
      substance lands in `06`, including `:176-178`'s firmware claim that must NOT be carried across.
  </read_first>

  <behavior>
    - `## OEM Catalog and Firmware Delivery` tells the honest shape of the answer: the OEM catalog
      reaches the fleet through Windows Update, firmware arrives as an Other driver requiring
      explicit approval, Windows Update may beat the vendor's own tool, and the policy does **not**
      honour the OEM's CHID targeting.
    - `## Reporting` names the three reports and where they live, the worst-status-wins collapsing
      rule, the six-status priority ordering, the drill-in limitation, six-month retention, the
      failure-report fields and Last Scan Time as the diagnostic column.
    - `## Configuration Manager Co-management and Co-existence` presents the complete six-step
      procedure — four required, two optional — with the Windows-11 scoping note, the
      undefined-and-unpredictable-device-state warning attached to step 3, and both spellings of the
      scan-source policy name.
    - `## Unsupported and Anti-Feature Callouts` leads with the Autopilot pair and covers the
      remaining absences, using the recipe table for binary unsupported rows and the bold sub-label
      form for absence-type items.
  </behavior>

  <action>
Replace the four remaining placeholder lines with authored prose, then run the D-61 commit-1 gates
and commit.

`## OEM Catalog and Firmware Delivery` — build it around RESEARCH §5.2's recommended shape. Q20 for
how recommended status is conferred (the OEM must mark the update required and it must be the most
recent such version). Q21 for the Other-drivers list explicitly including **firmware updates** and
optional updates the OEM does not intend for all devices. Q23b for the scope boundary (only drivers
currently published to Windows Update and applicable to a policy device). Q22 for the security
mechanism (updates published to Windows Update must use a Windows mechanism that securely updates
firmware or driver without requiring the BIOS/UEFI to be unlocked). Q23 for the availability lag (the
delay depends on the vendor or OEM, and because driver updates are signed by the same portal they may
reach Windows Update **before** the vendor's own tools). Q23c for plug-in devices. Then Q19 — the
CHID exception, which is the single highest-value new claim this research pass produced and appears
in no other research file in this repo: driver update policies do **not** enforce OEM-defined Computer
Hardware ID targeting even for recommended drivers, so managed devices can receive newer recommended
driver versions instead of CHID-targeted ones. Q19 carries the `U+2019` fold per R1.

**This section carries RESEARCH's U-3 correction and it is the single most important authoring
instruction in this plan.** `01:176-178` currently reads that driver and firmware policies have their
own approval cadence — *"manual driver approval; automatic firmware delivery for OEM-published
catalogs"* — decoupled from any quality/feature update deferral on a WUfB deployment ring. That
parenthetical sits inside the movable block that plan 02 deletes, so it is on its way here. It is
**contradicted by first-party text**: Q21 puts firmware on the Other-drivers list and Q12 says
other-list updates must be manually approved before deployment, even under an automatic-approval
policy, because automatic approval reaches only *recommended* drivers (Q10). **Do not carry the
sentence.** Per Assumptions-Log A2, do **not** replace it with a second unsourced assertion either:
state the two sourced facts — firmware updates arrive on the Other drivers list (Q21), and other-list
updates always require explicit approval (Q12) — and let the reader draw the conclusion, rather than
quoting a sentence Microsoft did not write. Moving a wrong claim unchanged into a brand-new guide is
RESEARCH §5.7's first named anti-pattern and would violate the premise of the milestone whose Pillar
E exists to stop exactly that. Primary source S2, secondary S1.

Do **not** describe an OEM-catalog *integration* surface, because none exists. AF-5's Dell Repository
Manager, HP Image Assistant and Lenovo Update Retriever are a parallel non-Intune channel and belong
in the callouts section beside AF-9, not here.

`## Reporting` — Q24 for the three named reports and the fact that this data is used only for these
reports and appears in no other Intune report. Give the admin-center navigation verbatim: the summary
and driver reports at **Reports > Windows Updates**, the failures report at
**Devices > Monitor > Driver update policies with alerts**. Q25 for the counter-intuitive collapsing
rule — each device is represented once, in a single status column, based on the **worst** status
across all updates applying to it — and Q26 for the six-status priority ordering from Success down to
NeedsReview. Q26b for the summary report's stated inability to drill in. Q27 for the six-month
retention window measured from the last event received for the update. Q27b for the failure-report
field list and Q27c for Last Scan Time as the column that explains why approved updates have not
installed. Primary source S4 `monitor-driver-updates`; there is no secondary, and note in the
`**Source:**` treatment that this page is absent from the FEATURES Sources table entirely — it is
what closes D-52's reporting gap.

`## Configuration Manager Co-management and Co-existence` — this is DRV-06 and SC#4. D-23 places the
procedure **here** rather than in `01`, on the corrected rationale: SC#4 assigns the procedure to the
new-guide criteria, and PITFALLS C1-6's "amend in place" instruction governs the existing prose only.
Use PATTERNS §6's numbered-procedure form — a lead sentence naming the count (six steps, four
required and two optional), each item opening with a bolded action name followed by an em-dashed
explanation.

Open with Q37's preamble (continue using Configuration Manager for updates other than drivers, or
move update types to cloud management one at a time; first enable cloud attach or co-management in
the Configuration Manager hierarchy to enroll managed devices in Intune) — this is where the
allowlisted keyword `co-management` arrives naturally per R2, in the section's first sentence,
because the source itself uses the phrase. Then Q38's preferred-path framing: moving the Windows
Update workload to Intune is the recommended and preferred path, and the procedure below is for
organizations not ready for that.

Ship Q39 as a verbatim `*Note:*` block adjacent to the procedure with its own `**Source:**` line per
D-29 — the procedure is supported for Windows 11 devices, and for Windows 10 the recommendation is to
move the Windows Update workload to Intune in the Configuration Manager co-management settings, or
alternatively to set that workload to Pilot and specify a collection containing the in-scope Windows
10 devices. C1-6 already renders this quoted, so verbatim is available.

**D-28 and D-49 are already discharged and this is why you quote rather than reconstruct.** D-28
recorded that the procedure was **elided in-repo** — step 2 ended at "...get them ready for
management...", step 3 ended at "...using the [Specify source for specific classes of Windows Updates
policy]..." with that link target recorded nowhere in the repo, and steps 5-6 were paraphrased rather
than quoted — and required the source be re-read before quoting, because reconstructing elided text
from memory is fabrication. D-49 required re-opening `driver-updates-faq` for exactly that reason and
recording the Windows Autopatch FAQ and the `wufb-wsus` scan-source page as DRV-06's real sources,
neither of which appears in the FEATURES Sources table. RESEARCH §2.8 closed all of it: the complete
six-step procedure is present with **zero elisions**, the step-3 link target is recovered as
`/windows/deployment/update/wufb-wsus`, and both extra sources are fetched with their dates recorded.
**Quote from §2.8. Do not re-fetch and do not recall.**

The six steps, all verbatim from RESEARCH §2.8 with zero reconstruction: Q40 step 1 (leave the
Windows Update workload set to Configuration Manager); Q41 step 2 (configure driver policies in
Intune to enroll devices and get them ready for management, as detailed at the named Manage-policy
article); Q42 step 3 (configure a domain-based group policy to set Windows Update as the source for
Driver Updates using the scan-source policy, whose link target is
`/windows/deployment/update/wufb-wsus`); Q44 step 4 (enable data collection in Intune for the devices
you intend to deploy drivers and firmware to); Q45 step 5, optional (enforce allowing diagnostic data
submission using a policy, because diagnostic data submission enables the use of Windows Update
reports for Microsoft Intune); Q46 step 6, optional (enable device name collection in diagnostic
data). If you find yourself paraphrasing step 2 or step 3 you have lost the quote bank and must
re-read §2.8 rather than recall — RESEARCH §5.7's third named anti-pattern.

Attach Q43 to step 3 as a **verbatim blockquote with its own `**Source:**` line** per D-31 — because
Configuration Manager uses a local group policy to configure the update source policy, using Intune
or a CSP to configure these same settings results in an undefined and unpredictable device state.
C1-6 calls this the warning that makes the topic a Class-1 pitfall rather than a recipe, and SC#4
names it explicitly. It is not optional and it is not paraphrasable.

Per D-30, steps 5 and 6 use **the source's own wording** — "prerequisites for Windows Update reports"
is not it, and the corpus's "get empty reporting" gloss was an inference beyond the source. Add
Q45b's note (diagnostic data submission is allowed by default; disabling collection prevents Windows
Update reports from reporting any update information; configure Allow Diagnostic data to Optional or
Required via domain group policy or Intune) and clarify that step 4's `data collection` is the
driver-policy prerequisite at the Intune tenant layer while step 5's `Allow Diagnostic data` is a
device-layer setting — different settings at different layers, and therefore **additive, not
duplicative**.

Add **Q46b as a bold sub-label**, because it is a genuinely new finding the corpus has never
recorded: using Intune to configure any of those diagnostic-data settings requires moving the
**Device Configuration** co-management workload to Intune. On a fleet that has deliberately left
everything on Configuration Manager, the optional steps therefore carry a co-management-workload
prerequisite that the rest of the procedure's premise silently contradicts.

Close with Q47 shipped **verbatim** — using Update Ring policies in Intune for Quality or Feature
Updates requires moving the Windows Update workload to Intune. D-17 cleared this for verbatim
shipping: the draft's claim that it would need qualifying was disproved by measurement, and it is
going into `06` where `V-54-11` does not reach in any case. Add Q47b for the feature-update
extension.

Then give the scan-source policy its own short treatment from S6 `wufb-wsus`: Q48 for what the policy
does, Q49 for the four update classes it can route including **Driver and firmware updates**, Q51 for
the **real Group Policy name and path**, Q52 for the four CSP nodes, and Q53 for the step-by-step
transition framing. Per RESEARCH U-4, **ship both spellings, attributed** — the FAQ's link text is
*"Specify source for specific classes of Windows Updates policy"* and REQUIREMENTS DRV-06 uses that
spelling, while the target page calls the setting *"Specify source service for specific classes of
Windows Updates"*. Use the FAQ's phrasing inside the quoted step and the GPO's own name where the
guide tells an admin what to look for in the Group Policy editor. Flattening the divergence produces
a guide that does not match the console (RESEARCH §5.7's fourth named anti-pattern), and treating
`wufb-wsus` as Windows-10-only is its fifth: Q50's dual-scan statement is version-split, but the
scan-source policy itself applies to Windows 10 version 2004 and above **and** Windows 11.

Per D-26, do **not** author any "the corpus previously asserted X" attribution for the withdrawn
"only Autopatch-compatible answer" claim. DRV-06 and PITFALLS C1-6 attach that phrase to different
mitigations; C1-6 governs, and the claim is measurably absent from `docs/` at 0 hits. It is
discharged by not authoring it — writing an attribution for a phantom manufactures the very claim the
milestone withdrew. Primary source S2, secondary S6.

`## Unsupported and Anti-Feature Callouts` — the H2 string is fixed to the corpus convention
(D-38/PATTERNS §5) and this is the guide's **fullest** section, not its thinnest. Per D-41, absences
are bold sub-labels inside this one section and **no new `PITFALL-N` identifier is minted** — the
identifier namespace is a per-milestone research ledger, and `PITFALL-5`, `-8` and `-13` have zero
prose occurrences precisely because they are live NEGATIVE regression guards at
`check-phase-49.mjs:288`, `check-phase-53.mjs:156` and `check-phase-51.mjs:292`, so zero occurrences
*is* the assertion. `PITFALL-13` exists, so the ceiling is not 10 and minting `PITFALL-11` would land
inside an in-use range. The bold-sub-label precedent is `01:186`'s
`**Dual-scan source conflict pitfall:**` — bold short label, colon, then the explanatory sentence in
the same paragraph.

Per D-42, **DRV-04 is the first item** and both halves stay together: Q29 (driver update policies are
not supported during Windows Autopilot) and Q30 (Windows applies critical updates during Autopilot,
which may include critical driver updates not yet approved by an admin). This pairs with the
one-line warning Task 1 placed near the top of the guide; the two must never be separated.

Then the remaining items from RESEARCH §5.2, choosing per item between the two shapes PATTERNS
§4 and §5 supply. Use the **three-column recipe table** — header exactly
`| Feature | Why it's unsupported / what breaks | Do this instead |` per
`docs/_templates/recipe-template.md:98` — for the genuinely binary unsupported rows: assignment
filters (Q33), Windows Enterprise LTSC (Q55, with "use update ring policies instead" as the source's
own alternative), GCC High and DoD (Q57, absent from the supported cloud environments list), and
AF-9's manual-packaging boundary — `.inf` sideloading, manual driver packaging and driver injection
into images are out of band, and driver policies surface only driver updates currently published to
Windows Update (Q23b). D-45 places AF-9 in scope by owner ruling and `FEATURES.md:170` names both its
content and its location.

Use the **bold sub-label prose form** for the absence-type items that need a sentence rather than a
row: no driver rollback (Q4 verbatim as a blockquote, carrying Microsoft's two named mitigations —
deployment rings to limit installation to small initial groups, and manual removal via PowerShell —
plus Q28b as an independent second statement of the absence on a different page, because a
single-page claim is the fragile kind); the destructive Automatic-to-Manual mode switch (Q31
verbatim, which carries the `U+2019` fold and whose bolding is the source's own); Extension drivers
(Q34, with Q35's Autopatch-side corroboration); Plug-and-Play drivers (Q36 — future updates to
PnP-installed drivers do require approval); no installed-driver inventory (Q36b, which is why an
admin cannot audit their way out of the preceding items); the immutable approval type (Q11); and CHID
targeting not enforced (Q19, cross-referenced to the OEM section).

Per RESEARCH §3's scope note, keep these attributed to their own surfaces rather than implying one
page said all of them: Q31 is on the **Autopatch** page and is scoped to Autopatch-managed driver
profiles and their groups and deployment rings, while Q33, Q34 and Q36 are on the **Intune**
driver-policy FAQ. DRV-05 packages them as one requirement; the guide must not.

Per RESEARCH U-1, quotes taken from rendered pages rather than doc-repo bytes (Q7, Q8, Q31, Q35,
Q48-Q54) are acceptable as-is — the disposition is no action, since the corpus tracks no
rendered-versus-source provenance marker.

**Then land commit 1.** Run the two D-61 gates plus the two regression gates, confirm all four
numbers, and commit with a message scoped to this phase and plan. Commit only
`docs/operations/patch-management/06-windows-driver-firmware-updates.md`. Do **not** touch
`01-windows-wufb-rings.md`, `00-overview.md`, `.planning/REQUIREMENTS.md`, `docs/_registry/`,
`docs/operations/00-index.md` or either pipeline script in this commit (D-61, D-64).
  </action>

  <acceptance_criteria>
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `Summary: 16 passed, 0 failed`
      (D-61 commit-1 gate, D-08).
    - `node scripts/validation/check-nav-hub-links.mjs` prints
      `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total` (D-61 commit-1 gate).
    - `node scripts/validation/check-phase-54.mjs` prints `Summary: 32 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations`
      — still 234, not 235 (D-33).
    - `node scripts/pipeline/build-filename-map.mjs --self-test` prints `8 passed, 0 failed` and
      `node scripts/pipeline/build-publish-bundle.mjs --self-test` prints `15 passed, 0 failed` —
      both unchanged, confirming D-64 held and no registry or filename-map row was added.
    - The literal `undefined and unpredictable device state` appears in the file (Q43 / SC#4 /
      DRV-06 / D-31).
    - The literal `Driver updates aren't supported during Windows Autopilot` appears in the file
      (Q29 / DRV-04 / SC#3).
    - The literal `These updates may include critical driver updates that have not yet been approved
      by an admin` appears in the file (Q30 — DRV-04's second half, which D-42 requires always
      accompany the first).
    - The literal `new policies are generated to` appears in the file (Q31 / DRV-05 / SC#3, the
      destructive mode switch).
    - The literal `Computer Hardware ID` appears in the file (Q19, the CHID claim).
    - The exact heading `## Unsupported and Anti-Feature Callouts` is present, and
      `grep -cE '^## .*(Limitation|Absence)' ...` returns `0` (D-38 — the invented heading is not
      used).
    - `grep -cE 'PITFALL-(5|8|11|13)' ...` returns `0` — no new or in-use `PITFALL-N` identifier was
      minted into `06` (D-41).
    - `grep -c 'automatic firmware delivery for OEM-published catalogs' ...` returns `0` — the
      contradicted `01:177` claim was corrected, not relocated (RESEARCH U-3, §5.7).
    - The table header `| Feature | Why it's unsupported / what breaks | Do this instead |` is
      present, matching `docs/_templates/recipe-template.md:98` (PATTERNS §5).
    - `grep -cP '[\x{2019}\x{2011}]' ...` returns `0` (R1).
    - `grep -cE '\bSCCM\b|\bSystem Center\b' ...` returns `0` (R2/D-04) and
      `grep -c 'Autopatch rings' ...` returns `0` (R3/D-43d).
    - `git show --stat HEAD` lists exactly one changed file and it is
      `docs/operations/patch-management/06-windows-driver-firmware-updates.md` (D-61 commit 1,
      D-64).
  </acceptance_criteria>

  <verify>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs &amp;&amp; node scripts/validation/check-nav-hub-links.mjs &amp;&amp; node scripts/validation/check-phase-54.mjs &amp;&amp; node scripts/validation/c17-eee-contract.mjs &amp;&amp; node scripts/pipeline/build-filename-map.mjs --self-test &amp;&amp; node scripts/pipeline/build-publish-bundle.mjs --self-test</automated>
  </verify>

  <done>
    `06` is complete and covers DRV-01 through DRV-06. Commit 1 of D-61's three-commit sequence has
    landed, carrying exactly one file. All six gates are at their HEAD baselines: audit 16/0,
    nav-hub-links 0/0, check-phase-54 32/0/0, C17 234/0, filename-map self-test 8/0, publish-bundle
    self-test 15/0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none at runtime) | This plan's sole deliverable is a Markdown document rendered by GitHub and read by a human. It defines no endpoint, parses no untrusted input, executes no code and holds no credential. There is no boundary for untrusted data to cross. |

## STRIDE Threat Register

Stated honestly rather than padded: a prose-only documentation deliverable has a near-empty threat
surface, and inventing entries would obscure the one item that is real. ASVS level 1; nothing rises
to `high`, so nothing blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146-01 | Tampering | The ~20 outbound `https://learn.microsoft.com` targets in `06`'s `**Source:**` lines and `## External References` | low | accept | `check-nav-hub-links.mjs:242`'s `resolveLinkTarget` returns null for `http(s)`, so **no tool in this repository verifies any Learn URL** (D-69). A mistyped or stale target misroutes a reader to the wrong page. Compensating control: the orchestrator confirms each URL manually during verification, which is the Phase-145 precedent (7 live URLs, zero fabrications). An automated gate for `http(s)` targets is an open deferred item awaiting the next tooling-debt phase. |
| T-146-02 | Information Disclosure | `06`'s worked examples and ranges | low | mitigate | AF-6's standing "generic, not tenant-specific" constraint is carried into `## Deferral and Deadline Behavior` as an authoring rule: give first-party ranges and branch criteria, never a worked tenant schedule. No tenant id, device name, serial or account appears anywhere in the guide. |

**Supply chain (`T-{phase}-SC`): not applicable.** This plan runs no `npm`, `pip` or `cargo` install
and adds no dependency; RESEARCH records "there are **no external packages** in this phase", so the
Package Legitimacy Gate has nothing to audit and no legitimacy checkpoint is warranted. The only tool
dependency is `node`, already present and green at HEAD.

**Schema:** no schema is touched in this plan — no migration, no model, no table, no API contract.

**Repudiation / Elevation of Privilege / Denial of Service / Spoofing:** no applicable component. The
deliverable has no actor, no session, no request path and no privilege model.
</threat_model>

<verification>
Run all six gates from the repository root after Task 3:

1. `node scripts/validation/v1.20-milestone-audit.mjs` -> `Summary: 16 passed, 0 failed` (D-08's
   addition to the per-commit gate; `check-phase-54` alone is measurably blind to the C11 failure
   mode this file could introduce).
2. `node scripts/validation/check-nav-hub-links.mjs` -> `0 hub-presence failure(s), 0 corpus-link
   failure(s), 0 total`.
3. `node scripts/validation/check-phase-54.mjs` -> `Summary: 32 passed, 0 failed, 0 skipped`.
4. `node scripts/validation/c17-eee-contract.mjs` -> `234 files checked, 0 with violations`.
5. `node scripts/pipeline/build-filename-map.mjs --self-test` -> `8 passed, 0 failed`.
6. `node scripts/pipeline/build-publish-bundle.mjs --self-test` -> `15 passed, 0 failed`.

Any deviation from these exact numbers is a regression, not a new baseline.

D-63's apex run is split across the phase: **Task 1 runs `node scripts/validation/check-phase-144.mjs`
as its very first action, before creating any file**, and it must print
`Result: 101 PASS, 0 FAIL, 0 SKIPPED (total checks: 101)` in roughly 18-22 seconds. That is the
"once before commit 1" half. The "once after the last commit" half is plan 03's final task. Do not
run the apex between them — it is redundant with the per-commit gates and costs 19 seconds each time.
</verification>

<success_criteria>
- `docs/operations/patch-management/06-windows-driver-firmware-updates.md` exists, carries the H1
  `# Windows Driver and Firmware Updates`, ten H2s in D-37's order and eight own-line `<a id>`
  anchors matching the D-66 contract.
- DRV-01 through DRV-06 each have authored, first-party-sourced content, with every quote carrying a
  `**Source:**` line in the `[Title](url) (updated YYYY-MM-DD)` shape.
- SC#1, SC#2, SC#3 and SC#4 are satisfied by this plan's file alone.
- All six gates in `<verification>` are at their exact HEAD baselines.
- Exactly one file is in commit 1's diff.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The contract downstream phases consume. Every id below is stable and a rename breaks a named
consumer.

### New file

| Path | Produced by | Consumed by |
|---|---|---|
| `docs/operations/patch-management/06-windows-driver-firmware-updates.md` | plan 01 | Phase 148 (cross-links), Phase 152 (registry row, filename-map row, ops-index row, nav-hub wiring — all one atomic unit, none of it this phase's per D-64) |

### H1 — Phase 152's registry `Title` (D-67)

`# Windows Driver and Firmware Updates`

`scripts/pipeline/build-filename-map.mjs:4` treats the registry Title as the sole source of truth,
and an unresolved collision is a hard `exit 1` at `:145`/`:175`. This string is distinct from every
existing registry Title and from `01`'s `# Windows WUfB Rings + Hotpatch + Driver/Firmware`.

### H2 anchor ids — Phase 148's cross-link contract (D-66)

Eight own-line `<a id="..."></a>` anchors, one per content H2. Heading slugs alone are the fragile
option, which is why D-66 mandates explicit anchors; `check-nav-hub-links.mjs` builds its resolvable
set from explicit `<a id>` values unioned with GitHub heading slugs, and hard-fails an anchor miss.

| Anchor id | H2 | Requirements |
|---|---|---|
| `what-this-policy-does` | `## What This Policy Does` | DRV-01 |
| `approval-modes` | `## Approval Modes` | DRV-01 |
| `approval-workflow` | `## The Approval Workflow` | DRV-01, DRV-03 |
| `deferral-deadline-behavior` | `## Deferral and Deadline Behavior` | DRV-02 |
| `oem-catalog-firmware` | `## OEM Catalog and Firmware Delivery` | DRV-01 |
| `driver-update-reporting` | `## Reporting` | DRV-01 |
| `configmgr-coexistence` | `## Configuration Manager Co-management and Co-existence` | DRV-06 |
| `unsupported-callouts` | `## Unsupported and Anti-Feature Callouts` | DRV-03, DRV-04, DRV-05 |

`## Related Resources` and `## External References` carry **no** anchor, matching the measured
sibling convention across `00`, `01` and `02`.

**In-phase consumers:** plan 02's fourth mitigation item links
`06-windows-driver-firmware-updates.md#configmgr-coexistence`, and plan 02's precondition asserts
that anchor exists at HEAD before it edits. A rename during execution silently breaks that link, and
`check-nav-hub-links.mjs` is what catches it.

### Modified files

| Path | Produced by | Note |
|---|---|---|
| `docs/operations/patch-management/01-windows-wufb-rings.md` | plan 02 | stub-and-move; the only v1.21 phase that edits it |
| `docs/operations/patch-management/00-overview.md` | plan 03 | touch 2 of 4; Phase 147 is touch 3 |
| `.planning/REQUIREMENTS.md` | plan 03 | the `:128` C11 row only |

### Deliberately NOT produced (D-64, D-65)

No registry row, no filename-map row, no canary bump, no `docs/operations/00-index.md` row, no
nav-hub wiring, no `## Version History`. All of it is Phase 152's single atomic unit. Adding any one
of them here takes both pipeline self-tests to 225-versus-226 RED.
</artifacts_this_phase_produces>

<assumption_delta_decision>
`gsd_run query assumption-delta scan "146" --json` returned `detected: true`, so this is recorded
rather than skipped. The three returned signals are lexical false positives from ROADMAP prose —
`alongside` in SC#2's wording, `plural` in the blast-radius note about C11's plural-only regex, and
`also` in "this phase also touches `00-overview.md`". None is a cardinality change.

The genuine delta the detector did not name:

- **Noun now primary:** the canonical documentation location of the Windows driver and firmware
  update policy. Before this phase it is a section inside `01-windows-wufb-rings.md`; after it, it is
  `06-windows-driver-firmware-updates.md`.
- **Decision: `promote`.** `06` becomes the primary surface and `01` retains a stub that forwards to
  it, which is precisely what DRV-07 specifies and what D-13 apportions ("the stub keeps *what this
  is and what it is not*; `06` keeps *how to configure it*"). No plan change is required — the
  promotion is already the phase's shape.
- **Rationale:** the promotion is a locked requirement rather than an emergent naming drift, so
  `add-alongside` would understate it and `no-change` would be false. The one consequence worth
  carrying is that every existing reference describing `01` as the driver/firmware surface becomes
  stale on promotion, which is exactly why D-55 raised `00-overview.md` from two edit sites to three.
</assumption_delta_decision>

<output>
Create `.planning/phases/146-windows-driver-firmware-update-depth/146-01-SUMMARY.md` when done.
</output>
