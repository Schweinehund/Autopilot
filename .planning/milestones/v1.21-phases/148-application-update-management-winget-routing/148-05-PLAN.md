---
phase: 148-application-update-management-winget-routing
plan: 05
type: execute
wave: 2
depends_on: ["148-02", "148-04"]
files_modified:
  - docs/operations/patch-management/08-windows-app-updates.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [APP-03, APP-06]
gap_closure: true

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `## Setting the Channel from Intune` section of `08-windows-app-updates.md` no longer asserts a settings-count comparison between the two Intune profile types. The Administrative Templates paragraph now ends at the words `two different names.` and every remaining clause in it is carried by the `**Source:**` line that follows it (APP-03, D-63, D-64, R7 evidence-line contract)."
    - "The remediation chosen is REMOVAL, not re-citation, and the rationale is recorded: the deleted clause is decorative — the paragraph's operational instruction (do not collapse the two settings names into one; they are one capability under two profile types) survives whole — while a replacement citation would add a new external URL to the file and therefore a new `V-54-11` banned-token surface, a new `check-nav-hub-links` target, and a new citation date to keep fresh, all to carry a claim no reader acts on. This matches `148-REVIEW.md` CR-01's suggested fix and the verifier's own `missing:` option 2 (APP-03)."
    - "Fail-closed on invention: no new Microsoft Learn URL is introduced anywhere in this plan. The `change-update-channels` Source line at the end of that paragraph stays exactly as it ships, and after the deletion its page was re-confirmed by live raw-HTML `curl` plus tag-strip plus literal `grep` to carry the claims that now remain above it — so the line is honest rather than merely un-contradicted (D-62)."
    - "`EnableMicrosoftStoreSource` is named ONE way inside `08-windows-app-updates.md`: the `DesktopAppInstaller` table cell now reads the same App-Installer-prefixed name the prose already uses and the live ADMX friendly name confirms, so a reader cannot conclude the table row and the settings-catalog trap paragraph describe two different policies (WR-03, APP-06)."
    - "The five other `DesktopAppInstaller` table rows are byte-unchanged and remain in their original source order; only the one cell WR-03 names moved (APP-06)."
    - "WR-01 (`00-overview.md:101` citation-date mismatch), WR-02 (`00-overview.md:79-83` ambiguous ring antecedent) and WR-04 (`00-overview.md:266` internal planning-ledger leak) are each filed as their own evidence-carrying bullet under `.planning/REQUIREMENTS.md` `## Future Requirements`, closing the backlog-completeness shortfall `148-VERIFICATION.md` named. Each bullet carries the authoring commit, the measured evidence, and a trigger."
    - "`00-overview.md` is byte-unchanged by this plan. Filing a pre-existing defect to the backlog is the in-radius disposition this phase already used for the falsified hotpatch cell at `00:57` and the co-management defects (`148-03-SUMMARY.md`); editing `00-overview.md` is not, because all four of its D-52 edit sites are consumed and the phase is additive-only there."
    - "Every string replacement in this plan was match-counted before and after, with the expected delta asserted rather than the tool's exit status trusted — a zero-match replacement that reports success is a failure mode this repository has already shipped (D-56)."
    - "Line endings survive byte-exact: `git ls-files --eol` still reports `w/lf` for `08-windows-app-updates.md` and for `.planning/REQUIREMENTS.md`, and `00-overview.md` keeps its `w/crlf` because it is not opened at all (D-56)."
    - "Running this plan's replacements a second time is a detectable no-op, not a double-application: the search literal each one targets returns a match count of 0 on the post-edit file, so a re-run halts on the match-count assertion instead of corrupting the paragraph (D-56)."
    - "The structural contract did not move: `08-windows-app-updates.md` still measures 10 H2s, 8 own-line `<a id=` anchors, 0 code fences, no `doc_id` field; the phase total stays 16 anchors; the file's `last_verified` / `review_by` stamps are NOT advanced, matching the `148-04` precedent of a content-only edit with no re-stamp (D-70)."
    - "Every gate that reads either changed file was re-run after the edits and each count recorded as a MEASURED number, never asserted: `check-phase-53` 26/0/0, `check-phase-54` 32/0/0, `check-phase-59` 36/0/0, `check-nav-hub-links` 0/0, `c17-eee-contract` 234 files / 0 violations, `v1.20-milestone-audit` 16/0, apex `check-phase-144` 101/0/0 — the apex run as its own invocation, after the others (D-67)."
    - "All eight of this phase's prohibitions remain HELD: `git diff a161a43c HEAD --stat` stays empty on `docs/operations/co-management/**`, `docs/operations/app-lifecycle/**`, `docs/operations/admin-setup-apv2/**`, `docs/reference/**` and `01-windows-wufb-rings.md`; the Autopatch-implies-Hotpatch conclusion is not inverted; the WinGet FAQ negative is not strengthened."
    - statement: "A reader who reaches the `DesktopAppInstaller` table and then the settings-catalog trap paragraph cannot form the belief that two distinct Microsoft Store source policies exist — the two occurrences of the policy name are now the same string, and no sentence between them introduces a second referent."
      verification: backstop
    - statement: "No sentence anywhere in the edited paragraph hedges the removed comparison back into the document — there is no 'may have', 'reportedly', 'generally', or comparable softener standing where the deleted clause was. An unsourced hedge is still an unsourced claim and would re-trip the same evidence-line contract."
      verification: backstop
  artifacts:
    - "docs/operations/patch-management/08-windows-app-updates.md — one truncated sentence (3 lines become 1) plus one table-cell rename; nothing else."
    - ".planning/REQUIREMENTS.md — three appended `## Future Requirements` bullets (WR-01, WR-02, WR-04); nothing else."
  key_links:
    - "The Administrative Templates paragraph → the `change-update-channels` Source line already shipping directly beneath it. No new link target, no new URL token, no new nav-hub link-check surface is created anywhere in this plan."
    - "`08-windows-app-updates.md` table row `DesktopAppInstaller/EnableMicrosoftStoreSource` → the settings-catalog trap paragraph later in the same H2, which names the same policy; the rename is what makes that link readable."
  prohibitions:
    - inherited_from: "148-02-PLAN.md `must_haves.prohibitions` (six entries, APP-03/APP-04/APP-05) plus the phase blast-radius prohibitions listed in `148-04-PLAN.md` `<phase_wide_rules>`"
      statement: "This gap-closure plan inherits the parent plan's full prohibition set rather than restating it, exactly as 148-04 did. All of them were measured HELD at verification time and this plan is required to keep every one held; the corresponding truth is asserted above and gated in Task 3."
    - requirement_id: APP-03
      category: transparency
      statement: "MUST NOT replace the removed comparison with a hedged or softened version of the same claim, and MUST NOT attach any new external citation to the Administrative Templates paragraph. If a page carrying the comparison is stumbled upon, it is filed to the backlog, not cited here — introducing a new URL is out of this plan's scope."
    - requirement_id: APP-06
      category: values
      statement: "MUST NOT re-audit or rename the other five `DesktopAppInstaller` table rows against their ADMX friendly names. WR-03 names exactly one cell; a six-row sweep is a different, unplanned change with its own live-fetch burden."
    - requirement_id: APP-03
      category: transparency
      statement: "MUST NOT open, read for editing, or modify `docs/operations/patch-management/00-overview.md`. Its three pre-existing defects are dispositioned by backlog filing in `.planning/REQUIREMENTS.md`, which is a planning file and not part of the docs blast radius."
---

<objective>
Close the single blocking gap in `148-VERIFICATION.md`: `08-windows-app-updates.md:132-134` states that one
Intune profile type has more settings than the other, attributed to the `change-update-channels` Source
line at `:136` — and the verifier proved by live raw-HTML fetch that neither that page nor the other page
cited in the same section carries the claim. Zero pages support it. This is the same defect class 148-04
just closed elsewhere in this file, so the same ruling applies: it blocks SC#3.

Purpose: the phase's evidence-line contract (R7, D-63, D-64, and plan 02 Task 2's acceptance criterion)
exists so a reader can trace any confidently-stated claim to a page that carries it. A claim with no
supporting page anywhere is the strongest possible violation of it.

Alongside the blocking fix, this plan discharges all four advisory Warnings the verifier adjudicated: WR-03
is fixed in place (it is this phase's own new content), and WR-01, WR-02 and WR-04 are filed to the backlog
— closing the "gap in backlog completeness" the verifier explicitly asked to see closed in a follow-up.

Output: one truncated sentence and one renamed table cell in `08-windows-app-updates.md`, three appended
backlog bullets in `.planning/REQUIREMENTS.md`, and the full seven-gate battery re-measured behind them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-application-update-management-winget-routing/148-VERIFICATION.md
@.planning/phases/148-application-update-management-winget-routing/148-REVIEW.md
@docs/operations/patch-management/08-windows-app-updates.md
</context>

<phase_wide_rules>

**Blast radius is two files, and only one of them is a document.**
`docs/operations/patch-management/08-windows-app-updates.md` and `.planning/REQUIREMENTS.md`. Do not open
`00-overview.md`, `01-windows-wufb-rings.md`, `07-windows-autopatch.md`,
`docs/operations/co-management/**`, `docs/operations/app-lifecycle/**`,
`docs/operations/admin-setup-apv2/**` or `docs/reference/**`. All eight of this phase's prohibitions were
measured HELD at verification time; this plan keeps every one held.

**The remediation is removal, and that choice is already made.** The verifier offered two options. Option 2
(remove the unsupported clause) is chosen, for the reasons recorded in `must_haves.truths`. Do not go
hunting for a page that makes the comparison, and under no circumstance invent or approximate one. If the
executor happens to encounter such a page, file it to the backlog and move on — introducing a new URL into
this file is explicitly prohibited by this plan.

**Do not hedge the claim back in.** "may have", "reportedly", "generally", "is understood to" and every
sibling construction are barred from the edited paragraph. An unsourced hedge is still an unsourced claim
and re-trips the same contract that produced this gap.

**Do not re-plan APP-01, APP-02, APP-04 or APP-05.** All four are `✓ SATISFIED` on independent
re-verification. APP-03 carries only the citation defect this plan closes; APP-06 carries only the naming
inconsistency this plan fixes.

**Re-fetch, never trust a rendered summary.** Live-page confirmation uses raw HTML bytes via `curl`, a
tag-strip, and a literal `grep`. A rendered-summary fetch tool has twice returned contradictory answers for
the same URL in this project (D-62).

**Match-count every string replacement.** Read the count before, read the count after, assert the expected
delta. A zero-match replacement reports success (D-56).

**Do not re-stamp `08-windows-app-updates.md`.** Its frontmatter reads `last_verified: 2026-08-23` /
`review_by: 2026-10-22` — a 60-day interval with zero slack under `V-54-07`'s `> 60` test. 148-04 edited
this file's content and correctly left both stamps alone. Advancing either one here would break this plan's
`git diff --numstat` gates and, if only one moved, would either drive the interval negative and silently
pass or push it to 61 and fail loudly.

**One commit per task.** Three tasks, three commits, each touching exactly the files that task names. The
`--numstat` gates in this plan depend on that separation.

</phase_wide_rules>

<tasks>

<task type="tracer">
  <name>Task 1: Remove the unsupported comparison clause end-to-end, gate it, commit it</name>
  <files>docs/operations/patch-management/08-windows-app-updates.md</files>
  <precondition>`curl` can reach `learn.microsoft.com` over HTTPS and returns HTTP 200 for the Administrative Templates channel article; if it cannot, halt and report rather than shipping a Source line whose remaining coverage was never confirmed.</precondition>
  <read_first>
    Read `docs/operations/patch-management/08-windows-app-updates.md` lines 118-140 in full before editing.
    At plan time the region measured: `:126` a settings-catalog Source line, `:128-134` the Administrative
    Templates paragraph, `:135` blank, `:136` the Administrative Templates Source line naming
    `change-update-channels`, `:137` blank. Line numbers in a verification report go stale — 148-04 already
    shifted this file by two lines. Locate the paragraph by its opening literal
    `The older **Administrative Templates** route` and by its closing sentence terminator, never by the
    number 132.

    Read `.planning/phases/148-application-update-management-winget-routing/148-VERIFICATION.md` lines 15-23
    (the `gaps:` frontmatter block) so the exact failing text and the two remediation options are in context
    before any byte moves.

    Read `.planning/phases/148-application-update-management-winget-routing/148-04-PLAN.md` Task 1 for the
    established shape of a match-counted, LF-preserving replacement in this exact file.
  </read_first>
  <action>
    Truncate the Administrative Templates paragraph so it ends at the words `two different names` followed
    by a period.

    Concretely: the paragraph's final sentence currently begins `Do not collapse the two settings names into
    one;` and runs across three physical lines to a terminator on the line whose entire content is
    `Templates profile type.`. Everything from the comma immediately after `two different names` through
    that terminator is removed, and the sentence is closed with a period directly after `two different
    names`. The result is a single physical line reading
    `different Intune configuration profile types under two different names.` — the two trailing physical
    lines disappear entirely.

    Do not reflow the paragraph. The preceding lines already sit under the file's ~100-column prose wrap and
    the truncated line lands at 74 characters; re-wrapping would inflate a 1-insertion/3-deletion diff into
    a whole-paragraph rewrite and destroy the `--numstat` gate below.

    Change nothing else in the paragraph. The clause about not collapsing the two settings names into one,
    and the statement that they are the same underlying capability exposed by two different Intune
    configuration profile types, both stay — they are the paragraph's operational point and they are carried
    by the Source line beneath it.

    Do not touch the Source line at the end of the paragraph, and do not add one. Its URL stays
    `https://learn.microsoft.com/en-us/microsoft-365-apps/updates/change-update-channels` with its existing
    `(updated 2026-07-18)` suffix. After the deletion it governs a paragraph whose every remaining claim it
    actually carries, which is the whole point of the fix.

    Then discharge D-62 on the residue rather than assuming it. Fetch
    `https://learn.microsoft.com/en-us/microsoft-365-apps/updates/change-update-channels` as raw HTML bytes
    with `curl`, strip tags, and `grep` literally for `Update Channel (2.0)` and for
    `Microsoft Office 2016 (Machine)` — the two distinctive strings the surviving paragraph attributes to
    that page. Both must return a non-zero hit count. If either returns zero, HALT: the Source line would
    then be carrying a second unsupported claim and the fix is incomplete. Record both hit counts in the
    SUMMARY as measured numbers.

    Per D-56, this file is LF in both index and worktree (`git ls-files --eol` reported `i/lf w/lf` at plan
    time). Re-confirm that before using any literal newline in a replacement string, and count matches
    before and after the replacement rather than trusting the tool's exit status. The search literal must
    return exactly 1 match before the edit and exactly 0 after; assert both.

    Per D-53, do not let the edit wrap or unwrap any `[text](url)` construct — there is none inside the
    deleted span, which is why this fix carries no wrapped-link exposure at all. Note that discharge
    explicitly in the SUMMARY rather than leaving it implied.

    Per the V-54-11 URL-ring trap, note that this edit removes prose only and adds no URL, so the file's URL
    token set is strictly unchanged. State that in the SUMMARY.

    Then run `node scripts/validation/check-phase-54.mjs` and the apex
    `node scripts/validation/check-phase-144.mjs` as two separate invocations, in that order, before
    committing — these are the two gates whose traps this file has historically tripped. Commit only if both
    are at baseline. Message: `docs(148-05): drop the unsupported profile-type comparison clause`.
  </action>
  <acceptance_criteria>
    - `grep -c 'more settings available' docs/operations/patch-management/08-windows-app-updates.md` returns `0` (was `1`).
    - `grep -c 'two different names\.$' docs/operations/patch-management/08-windows-app-updates.md` returns `1` (was `0`).
    - `grep -c 'companion article' docs/operations/patch-management/08-windows-app-updates.md` returns `1` (was `2`) — the surviving occurrence is the unrelated one earlier in the file, which is correctly sourced and must not be touched.
    - `git diff --numstat -- docs/operations/patch-management/08-windows-app-updates.md` reports exactly `1` insertion and `3` deletions.
    - `grep -c 'change-update-channels' docs/operations/patch-management/08-windows-app-updates.md` is unchanged at `2` — the Source line and the Related Resources entry both survive.
    - `grep -c 'may have\|reportedly\|is understood to' docs/operations/patch-management/08-windows-app-updates.md` returns `0`.
    - Live fetch of the Administrative Templates channel article, tag-stripped, returns a non-zero literal hit count for both `Update Channel (2.0)` and `Microsoft Office 2016 (Machine)`; both counts recorded in the SUMMARY.
    - `grep -c '^## ' …/08-windows-app-updates.md` = `10`; `grep -c '^<a id=' …` = `8`; `grep -c '^```' …` = `0`; `grep -c '^doc_id' …` = `0`.
    - `git ls-files --eol docs/operations/patch-management/08-windows-app-updates.md` still contains `w/lf`.
    - `grep -c '^last_verified: 2026-08-23' …/08-windows-app-updates.md` = `1` and `grep -c '^review_by: 2026-10-22' …` = `1` — neither stamp advanced.
    - `node scripts/validation/check-phase-54.mjs` prints `32 passed, 0 failed` and exits 0; `node scripts/validation/check-phase-144.mjs` prints `101 PASS, 0 FAIL, 0 SKIPPED` and exits 0.
    - `git show --stat --format= HEAD` lists exactly one changed file and it is `docs/operations/patch-management/08-windows-app-updates.md`.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot &amp;&amp; test "$(grep -c 'more settings available' docs/operations/patch-management/08-windows-app-updates.md)" = "0" &amp;&amp; test "$(grep -c 'two different names\.$' docs/operations/patch-management/08-windows-app-updates.md)" = "1" &amp;&amp; test "$(grep -c 'companion article' docs/operations/patch-management/08-windows-app-updates.md)" = "1" &amp;&amp; test "$(grep -c '^## ' docs/operations/patch-management/08-windows-app-updates.md)" = "10" &amp;&amp; test "$(grep -c '^&lt;a id=' docs/operations/patch-management/08-windows-app-updates.md)" = "8" &amp;&amp; test "$(grep -c '^```' docs/operations/patch-management/08-windows-app-updates.md)" = "0" &amp;&amp; test "$(grep -c 'change-update-channels' docs/operations/patch-management/08-windows-app-updates.md)" = "2" &amp;&amp; test "$(grep -c '^last_verified: 2026-08-23' docs/operations/patch-management/08-windows-app-updates.md)" = "1" &amp;&amp; git ls-files --eol docs/operations/patch-management/08-windows-app-updates.md | grep -q 'w/lf' &amp;&amp; node scripts/validation/check-phase-54.mjs &amp;&amp; node scripts/validation/check-phase-144.mjs &amp;&amp; git show --stat --format= HEAD | grep -q '08-windows-app-updates.md' &amp;&amp; echo TASK1_OK</automated>
  </verify>
  <done>
    The settings-count comparison is gone from the file and the Administrative Templates paragraph ends at
    `two different names.`. The `change-update-channels` Source line beneath it is unchanged and was
    re-confirmed by live raw-byte fetch to carry both distinctive strings the surviving paragraph attributes
    to it. `git diff --numstat` for the commit is exactly `1 3`. Structural contract, frontmatter stamps and
    `w/lf` all unchanged. `check-phase-54` at 32/0 and apex `check-phase-144` at 101/0/0, run as separate
    invocations. One commit, one file.
  </done>
  <reversibility rating="reversible">Deleting one decorative clause from a git-tracked Markdown file; `git revert` restores the prior bytes exactly, and no external reference or link target changes.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Fix WR-03's duplicate policy name, then file WR-01/WR-02/WR-04 to the backlog</name>
  <files>docs/operations/patch-management/08-windows-app-updates.md, .planning/REQUIREMENTS.md</files>
  <precondition>`curl` can reach `learn.microsoft.com` and return the `policy-csp-desktopappinstaller` page; the ADMX friendly name must be read from live bytes, not carried over from the verification report.</precondition>
  <read_first>
    Read `docs/operations/patch-management/08-windows-app-updates.md` lines 298-336. At plan time the
    `DesktopAppInstaller` table sat at `:300-306` with six rows, its Source line at `:308`, and the
    settings-catalog trap paragraph at `:329-334`. Locate the table row by its CSP cell literal
    `DesktopAppInstaller/EnableMicrosoftStoreSource`, not by line number — Task 1 shifts this whole region
    up by two lines.

    Note before editing: the prose occurrence of this policy name is WRAPPED across two physical lines
    (the bold run opens at the end of one line and closes on the next), which is why a single-line grep for
    the full name currently returns 0. Grep for the trailing fragment `Installer Microsoft Store Source
    policy` instead — that fragment sits whole on one line and measured exactly 1 hit at plan time. Do not
    "fix" the prose wrap; per D-53 the wrapped construct here is bold emphasis, not a `[text](url)` link,
    so it carries no `check-phase-54` leak, and rewrapping it would enlarge the diff for nothing.

    Read `.planning/REQUIREMENTS.md` lines 135-153 — the `## Future Requirements` section. The bullet list
    ends with the glossary-terminology entry, followed by a blank line and the `## Out of Scope` H2. New
    bullets append after that last bullet. Study the existing bullets' shape before writing: bold lead-in,
    the measured evidence, the authoring commit where known, and a `Trigger:` clause. Match it.

    Read `.planning/phases/148-application-update-management-winget-routing/148-VERIFICATION.md` lines
    111-114 for the verifier's independently-confirmed evidence on each of WR-01, WR-02 and WR-04,
    including its explicit correction that WR-04 is filed nowhere and that the existing "planning-ledger
    citation" bullet names a different file.
  </read_first>
  <action>
    **Part A — WR-03, in `08-windows-app-updates.md`.** First discharge D-62: fetch
    `https://learn.microsoft.com/en-us/windows/client-management/mdm/policy-csp-desktopappinstaller` as raw
    HTML bytes with `curl`, strip tags, and `grep` literally for the ADMX friendly name associated with the
    `EnableMicrosoftStoreSource` setting. It must return a non-zero hit count for the App-Installer-prefixed
    form. If it does not, HALT and report rather than renaming a cell to a string the vendor page does not
    use.

    Then, in the six-row `DesktopAppInstaller` table, locate the single row whose CSP cell is
    `DesktopAppInstaller/EnableMicrosoftStoreSource` and set its first cell to exactly
    `Enable App Installer Microsoft Store Source policy`. That target string is chosen deliberately: it
    matches the prose occurrence later in the same H2 byte-for-byte, and it matches the live ADMX friendly
    name plus the trailing word `policy` that all five sibling rows in this table already carry — so it
    resolves the internal inconsistency WR-03 names without breaking the table's own uniform labelling
    convention.

    Change nothing else in the row: the CSP cell and the description cell stay byte-identical, the row stays
    on one physical line, and it stays in its current position between the configuration-policy row and the
    default-source row. Do not touch the other five rows — renaming them against their own ADMX names is a
    different change with its own fetch burden and is prohibited by this plan.

    Match-count before and after per D-56: the old first-cell label must return exactly 1 match before and
    exactly 0 after; the new label must return 0 before and 1 after. Assert all four counts.

    Commit Part A alone. Message: `docs(148-05): align the Store-source policy label with its ADMX name`.

    **Part B — WR-01, WR-02 and WR-04, in `.planning/REQUIREMENTS.md`.** Append three bullets to the end of
    the `## Future Requirements` bullet list, immediately after the glossary-terminology bullet and before
    the blank line that precedes `## Out of Scope`. Do not renumber, reorder or reword any existing bullet.

    Bullet one, WR-01: the citation-date mismatch. `docs/operations/patch-management/00-overview.md:101`
    carries citation date `2026-06-19` for the Autopatch groups overview article, while the six citations of
    the same article in `07-windows-autopatch.md` carry `2025-06-17`. The live `ms.date` on that page was
    independently fetched during verification and reads `2025-06-17`, so the overview is the wrong one.
    Record that `git log -S` blames the line to Phase 54 (`be7f59db`), pre-dating Phase 148, and that Phase
    148 did not touch that line — it sits outside D-52's four edit sites. Trigger: a corpus-hygiene
    milestone, or the next phase that edits this file.

    Bullet two, WR-02: the ambiguous antecedent. `docs/operations/patch-management/00-overview.md:79-83`
    states that a group supports up to 15 rings without an unambiguous antecedent for which kind of group is
    meant. Record that `git log -S` blames the text to Phase 145 (`68dfc378`), pre-existing and outside this
    phase's edit sites. Trigger: the same corpus-hygiene milestone.

    Bullet three, WR-04: the internal planning reference. `docs/operations/patch-management/00-overview.md:266`
    leaks an internal GSD phase and roadmap reference into a published document's footer. Record explicitly
    that this is NOT the same item as the existing `co-management/03:55` planning-ledger bullet already in
    this section — that one names a different file and a different defect, and the verifier corrected a task
    brief that had conflated them. Record that `git log -S` blames the line to Phase 54 (`be7f59db`).
    Trigger: the FIX-11 successor sweep, which is the same sweep the `co-management/03:55` bullet names, and
    which currently escapes both because neither carries a `.planning/` path prefix.

    Every one of the three bullets must state its evidence as measured, name its authoring commit, and carry
    a trigger — that is the shape every existing bullet in this section uses, and a bullet without evidence
    is the kind of untracked note the verifier objected to in the first place.

    Do NOT open `00-overview.md` for any part of this. Reading it is unnecessary — the verifier already
    measured every coordinate and this plan cites those measurements — and editing it is prohibited.

    `.planning/REQUIREMENTS.md` is LF in both index and worktree (`git ls-files --eol` reported `i/lf w/lf`
    at plan time). Confirm that before writing, and count the section's bullet total before and after: it
    must grow by exactly 3.

    Commit Part B alone. Message: `docs(148-05): file the three pre-existing overview defects to backlog`.
  </action>
  <acceptance_criteria>
    - Live tag-stripped fetch of `policy-csp-desktopappinstaller` returns a non-zero literal hit count for the App-Installer-prefixed friendly name; the count is recorded in the SUMMARY.
    - `grep -c 'Enable Microsoft Store source policy' docs/operations/patch-management/08-windows-app-updates.md` returns `0` (was `1`).
    - `grep -c 'Installer Microsoft Store Source policy' docs/operations/patch-management/08-windows-app-updates.md` returns `2` (was `1`) — one in the table cell, one in the trap paragraph's wrapped prose.
    - `grep -c 'DesktopAppInstaller/' docs/operations/patch-management/08-windows-app-updates.md` is unchanged at its pre-edit value, and `grep -c '^| Enable ' …` is unchanged at `5`.
    - The six CSP rows still appear in their original source order — `grep -n 'DesktopAppInstaller/Enable\|DesktopAppInstaller/Source' …` lists `EnableAppInstaller`, `EnableWindowsPackageManagerCommandLineInterfaces`, `EnableWindowsPackageManagerConfiguration`, `EnableMicrosoftStoreSource`, `EnableDefaultSource`, `SourceAutoUpdateInterval` in that sequence.
    - The Part A commit's `git diff --numstat` on the doc is exactly `1` insertion and `1` deletion.
    - `grep -c '^- \*\*' .planning/REQUIREMENTS.md` within the `## Future Requirements` section grew by exactly `3`.
    - The three new bullets each match `2026-06-19`, `15 rings`, and `00-overview.md:266` respectively; each contains the substring `Trigger:`; each names a commit hash (`be7f59db` twice, `68dfc378` once).
    - `grep -c 'co-management/03:55' .planning/REQUIREMENTS.md` returns `2` — the pre-existing bullet plus the WR-04 bullet's explicit disambiguation from it.
    - The Part B commit's `git diff --numstat` on `.planning/REQUIREMENTS.md` is exactly `3` insertions and `0` deletions.
    - `git ls-files --eol` reports `w/lf` for both changed files.
    - `git status --porcelain docs/operations/patch-management/00-overview.md` is empty and `git diff a161a43c HEAD --stat -- docs/operations/patch-management/00-overview.md` shows only the additive Phase-148 edit sites already shipped by 148-03, with no new hunk.
    - `git log --oneline -2` shows exactly two commits from this task, the first touching only the doc and the second touching only `.planning/REQUIREMENTS.md`.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot &amp;&amp; test "$(grep -c 'Enable Microsoft Store source policy' docs/operations/patch-management/08-windows-app-updates.md)" = "0" &amp;&amp; test "$(grep -c 'Installer Microsoft Store Source policy' docs/operations/patch-management/08-windows-app-updates.md)" = "2" &amp;&amp; test "$(grep -c '^| Enable ' docs/operations/patch-management/08-windows-app-updates.md)" = "5" &amp;&amp; test "$(grep -c '15 rings' .planning/REQUIREMENTS.md)" -ge "1" &amp;&amp; test "$(grep -c '00-overview.md:266' .planning/REQUIREMENTS.md)" = "1" &amp;&amp; test "$(grep -c 'co-management/03:55' .planning/REQUIREMENTS.md)" = "2" &amp;&amp; test "$(grep -c 'be7f59db' .planning/REQUIREMENTS.md)" = "2" &amp;&amp; test "$(grep -c '68dfc378' .planning/REQUIREMENTS.md)" = "1" &amp;&amp; git ls-files --eol .planning/REQUIREMENTS.md | grep -q 'w/lf' &amp;&amp; test -z "$(git status --porcelain docs/operations/patch-management/00-overview.md)" &amp;&amp; echo TASK2_OK</automated>
  </verify>
  <done>
    The `DesktopAppInstaller` table names the Store-source policy exactly as the prose and the live ADMX
    friendly name do; the other five rows are byte-unchanged and in source order. Three evidence-carrying
    bullets — each with its measured coordinate, its authoring commit and a trigger — sit at the end of
    `## Future Requirements`, with WR-04 explicitly disambiguated from the pre-existing `co-management/03:55`
    entry. `00-overview.md` is byte-unchanged. Two commits, one file each.
  </done>
  <reversibility rating="reversible">A one-cell table label change plus three appended backlog bullets; both `git revert` cleanly and neither alters any published URL, anchor or link target.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Re-run the full gate battery with MEASURED counts and re-assert every prohibition</name>
  <files>docs/operations/patch-management/08-windows-app-updates.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    Read `.planning/phases/148-application-update-management-winget-routing/148-VERIFICATION.md` lines 92-104
    — the gate battery table and, critically, the paragraph beneath it recording D-68's honest statement that
    these gates assert almost nothing about this phase's deliverable. The counts are a regression floor, not
    evidence the citations are correct.

    Read `.planning/phases/148-application-update-management-winget-routing/148-04-PLAN.md` Task 2 for the
    established invocation order and the two named count-defence rationales.
  </read_first>
  <action>
    Re-run every gate that reads either changed file and record the actual number each one printed. Do not
    write "passes" or "should pass" anywhere in the SUMMARY — record counts, per this repository's MEASURED
    discipline.

    Run, from the repo root: `node scripts/validation/check-phase-53.mjs`,
    `node scripts/validation/check-phase-54.mjs`, `node scripts/validation/check-phase-59.mjs`,
    `node scripts/validation/check-nav-hub-links.mjs`, `node scripts/validation/c17-eee-contract.mjs`,
    `node scripts/validation/v1.20-milestone-audit.mjs`.

    Then run the apex `node scripts/validation/check-phase-144.mjs` as a SEPARATE invocation, after the
    others have finished. This repository's own lesson is to run the apex chain separately: a prose change
    has tripped banned-phrase guards after an otherwise-passing run.

    The baselines to match, all measured at verification time: `check-phase-53` 26 passed / 0 failed /
    0 skipped; `check-phase-54` 32 / 0 / 0; `check-phase-59` 36 / 0 / 0; `check-nav-hub-links` 0 hub-presence
    failures and 0 corpus-link failures; `c17-eee-contract` 234 files checked and 0 with violations — the
    file count must stay 234, it must not become 233 or 235; `v1.20-milestone-audit` 16 passed / 0 failed;
    apex `check-phase-144` 101 PASS / 0 FAIL / 0 SKIPPED.

    Three things the counts specifically defend against here, each to be stated rather than merely numbered.
    First, DELETING two physical lines can break a validator pinning a line number below the deletion point
    inside this file — Task 1 removes lines, where 148-04 only added them, so this is a genuinely different
    exposure from the previous gap-closure run. Confirm by grepping `scripts/validation/*.mjs` for
    `app-updates` (zero hits at plan time) rather than inheriting D-68's finding. Second, `V-54-27` is a bare
    `> **Platform:**` negative across the whole corpus, which neither a prose deletion nor a table-cell
    rename can trip — say so, do not just report the number. Third, `c17-eee-contract` walks the live corpus,
    so its 234-file count is the cheapest possible proof that no file was accidentally created or removed.

    Then re-assert every prohibition explicitly, as measured commands and not as a claim. Run
    `git diff a161a43c HEAD --stat` scoped to each of `docs/operations/co-management/`,
    `docs/operations/app-lifecycle/`, `docs/operations/admin-setup-apv2/`, `docs/reference/` and
    `docs/operations/patch-management/01-windows-wufb-rings.md`; every one must be empty. Run
    `grep -ci 'if you have autopatch you have hotpatch'` across `07` and `08`; both must be 0. Confirm the
    WinGet FAQ negative is byte-unchanged by diffing it against `a161a43c`. Confirm
    `docs/operations/patch-management/00-overview.md` has no hunk beyond what 148-03 already shipped.

    If any count regresses, do not paper over it. Diagnose it against the specific commit that moved it,
    fix or revert, and record the finding.

    This task produces no commit of its own unless a regression required a fix. If it did, commit that fix
    alone with a message naming the regressed gate.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-53.mjs` exits 0 and prints `26 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/check-phase-54.mjs` exits 0 and prints `32 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/check-phase-59.mjs` exits 0 and prints `36 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0` hub-presence failures and `0` corpus-link failures.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 and prints `234` files checked with `0` violations.
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0 and prints `16 passed, 0 failed`.
    - `node scripts/validation/check-phase-144.mjs`, run as its own invocation after the six above, exits 0 and prints `101 PASS, 0 FAIL, 0 SKIPPED`.
    - `grep -rl 'app-updates' scripts/validation/*.mjs` returns nothing — no validator pins a coordinate inside the edited file.
    - `git diff a161a43c HEAD --stat -- docs/operations/co-management/ docs/operations/app-lifecycle/ docs/operations/admin-setup-apv2/ docs/reference/ docs/operations/patch-management/01-windows-wufb-rings.md` produces empty output.
    - `grep -ci 'if you have autopatch you have hotpatch'` returns `0` for both `07-windows-autopatch.md` and `08-windows-app-updates.md`.
    - `git status --porcelain` shows no modified file outside the two this plan declares.
    - Every count above appears in `148-05-SUMMARY.md` as a recorded number; the strings `should pass` and `passes` do not appear as substitutes for a count.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot &amp;&amp; node scripts/validation/check-phase-53.mjs &amp;&amp; node scripts/validation/check-phase-54.mjs &amp;&amp; node scripts/validation/check-phase-59.mjs &amp;&amp; node scripts/validation/check-nav-hub-links.mjs &amp;&amp; node scripts/validation/c17-eee-contract.mjs &amp;&amp; node scripts/validation/v1.20-milestone-audit.mjs &amp;&amp; node scripts/validation/check-phase-144.mjs &amp;&amp; test -z "$(git diff a161a43c HEAD --stat -- docs/operations/co-management/ docs/operations/app-lifecycle/ docs/operations/admin-setup-apv2/ docs/reference/ docs/operations/patch-management/01-windows-wufb-rings.md)" &amp;&amp; test "$(grep -ci 'if you have autopatch you have hotpatch' docs/operations/patch-management/08-windows-app-updates.md)" = "0" &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; echo TASK3_OK</automated>
  </verify>
  <done>
    All seven gates exit 0 and each printed count is recorded in the SUMMARY as a measured number matching
    its baseline: 26/0/0, 32/0/0, 36/0/0, 0+0, 234 files / 0 violations, 16/0, and apex 101/0/0. The apex ran
    as its own invocation after the others. The line-deletion exposure was checked by grepping the validator
    directory rather than inherited. All eight prohibitions re-measured HELD, each by a named command. Working
    tree clean.
  </done>
  <reversibility rating="reversible">Read-only verification; produces no commit unless a regression forces a fix, which would itself be a revertible one-file change.</reversibility>
</task>

</tasks>

<edge_case_disposition>

`workflow.specless_probe_fallback` produced 19 edge candidates across APP-01..APP-06, all `unresolved`.
Disposition below, resolved to this plan's actual scope — one clause deletion, one table-cell rename, three
appended backlog bullets — rather than padded to 19 manufactured predicates. Nothing is silently dropped and
nothing is auto-dismissed.

**Resolved, `verification: explicit` — lifted into `must_haves.truths` as plain strings with runnable
acceptance criteria (4 rows).**

- `APP-03 / idempotency` and `APP-06 / idempotency` — "what happens if this runs twice?" has a real analogue
  here: a string replacement re-applied is the classic double-application bug, and this repository has
  already shipped a zero-match replacement that reported success. Resolved as the truth that each search
  literal returns 0 matches on the post-edit file, so a second run halts on the match-count assertion rather
  than corrupting the paragraph. Gated in Tasks 1 and 2.
- `APP-05 / encoding` — "whose definition of equality applies, bytes or something looser?" is real: this
  corpus mixes LF and CRLF within one directory, and a bare-LF pattern silently zero-matches on the CRLF
  file. Resolved as the `git ls-files --eol` truth plus the explicit prohibition on opening the CRLF file at
  all. Gated in Tasks 1 and 2.
- `APP-04 / ordering` and `APP-05 / ordering` — "is output order specified and stable?" is real for the
  `DesktopAppInstaller` table, whose six rows follow the source page's order. Resolved as the truth that the
  five untouched rows keep their positions, gated by an ordered grep in Task 2.

**Resolved, `verification: backstop` — structured flat-scalar markers in `must_haves.truths` (2 rows).**

- `APP-06 / adjacency` — "when two things are exactly equal or just touch, do they merge or separate?" maps
  precisely onto WR-03: two labels for one policy, sitting in the same H2. The desired end state is that they
  merge into one referent in the reader's head. That is a comprehension property with no runnable assertion,
  so it ships as a backstop marker rather than a fake grep.
- `APP-03 / adjacency` — the deleted clause and a hedged rewrite of it "just touch": both would satisfy every
  literal grep in Task 1 while the second re-trips the evidence-line contract. The no-hedge property is
  partially gated (the softener grep) but not fully expressible, so it ships as a backstop marker.

**Not applicable to this plan's artifact — reasoned, not waved through (12 rows).** Every remaining row is
`concurrency` ("if interrupted or run in parallel, what is guaranteed?"), `empty` ("what is the result for
empty, single-element or null input?") or a duplicate `adjacency` on a requirement this plan does not touch.
This plan's deliverable is static Markdown: there is no execution, no concurrency, no input and no empty
case for those categories to predicate over. Where those categories DO have corpus analogues they were
already discharged at their proper altitude inside `148-02-PLAN.md` and re-verified by the phase verifier as
prose properties of the shipped guide — APP-03/concurrency as the one-channel-per-device rule,
APP-04/empty as the eight-limitation completeness check, APP-05/empty as the stated cadence *absence*.
Re-predicating them here would manufacture checks for a clause deletion.

**Surfaced, not resolved (1 row).** `APP-01 / unclassified` ("unclassified — review manually") carries no
category the probe could assign and therefore no claim this plan can honestly discharge. APP-01 is owned by
`148-01`, is `✓ SATISFIED` on independent verification, and is outside this plan's blast radius. Recorded as
a flagged open assumption, not marked resolved.

</edge_case_disposition>

<assumption_delta_decision>

**Outcome: `no-change`.** The deterministic scanner returned `detected: true` with exactly one signal —
`kind: chosen`, `term: "choose"`, matched in the ROADMAP goal prose *"an admin can choose an app-update
channel"*. That is a false positive: the word describes an administrator selecting a channel at runtime in
the product, as documented for a reader, not a derived value becoming a chosen one in any artifact this plan
touches. This plan deletes one clause, renames one table cell and appends three backlog bullets; it
introduces no value of either kind. Advisory and non-blocking — not raised with the user.

</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan deletes prose from a public-facing Markdown file, renames one table cell in it, and appends three bullets to an internal planning file. No code executes, no input is parsed, no data crosses a boundary, no dependency is installed. Stating that honestly is more useful than inventing a boundary that does not exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-148-05-01 | Information Disclosure | The three appended `.planning/REQUIREMENTS.md` bullets | low | accept | The bullets name public file paths, line numbers, public Microsoft Learn article slugs and this repository's own commit hashes. No credential, tenant identifier, customer name or internal hostname is introduced. `.planning/` is already tracked in this repository and this section already carries a dozen bullets of the same shape. |
| T-148-05-02 | Repudiation | The deleted comparison clause | low | accept | Deleting a claim from a published document without a record would leave a reader unable to reconstruct why the guidance changed. Mitigated by construction: the deletion is a single reverted-with-`git revert` commit whose message names the reason, the verifier's gap entry records the measured zero-hit evidence, and `148-05-SUMMARY.md` records both live-fetch hit counts that justified it. |
| T-148-05-03 | Tampering | Package-manager installs | low | accept | No `npm`/`pip`/`cargo` install task exists in this plan, so no package-legitimacy gate applies. The plan runs only validators already committed to this repository. |
| T-148-05-04 | Spoofing | The live `curl` fetches in Tasks 1 and 2 | low | accept | Both fetches go to `https://learn.microsoft.com` over TLS with certificate validation left at `curl`'s default. The fetches inform a deletion and a rename, not a new citation, so a hostile response's worst outcome is a halt — both tasks fail closed on a zero hit count rather than proceeding. |

ASVS level 1; blocking threshold `high`. No threat in this plan reaches `medium` or above.
</threat_model>

<artifacts_this_phase_produces>

| Artifact | Change | Produced by |
|---|---|---|
| `docs/operations/patch-management/08-windows-app-updates.md` | The Administrative Templates paragraph loses its unsupported settings-count comparison and ends at `two different names.`; net `1` insertion / `3` deletions. | Task 1 |
| `docs/operations/patch-management/08-windows-app-updates.md` | The `DesktopAppInstaller` table's Store-source row is relabelled to match the prose and the live ADMX friendly name; net `1` insertion / `1` deletion. | Task 2, Part A |
| `.planning/REQUIREMENTS.md` | Three evidence-carrying `## Future Requirements` bullets filing WR-01, WR-02 and WR-04; net `3` insertions / `0` deletions. | Task 2, Part B |
| `.planning/phases/148-application-update-management-winget-routing/148-05-SUMMARY.md` | Records both live-fetch hit-count pairs, the ADMX friendly-name hit count, the before/after match counts for all three replacements, all seven gate counts, and the per-path prohibition sweep results. | Task 3 |

Three commits total (a fourth only if Task 3 finds a regression). No new file is created in `docs/`; the
`c17-eee-contract` file count stays 234. No new URL, anchor or link target enters the corpus; the phase
anchor total stays 16.

</artifacts_this_phase_produces>

<verification>
The gap is closed when no claim in `08-windows-app-updates.md`'s `## Setting the Channel from Intune`
section outruns the Source line governing it. Concretely:

1. `grep -c 'more settings available'` on the file returns `0`, and `grep -c 'two different names\.$'`
   returns `1`.
2. No hedged replacement was substituted — `grep -c 'may have\|reportedly\|is understood to'` returns `0`.
3. No new URL entered the file — `grep -c 'change-update-channels'` is unchanged at `2`, and the total count
   of `https://learn.microsoft.com` occurrences is unchanged from `a161a43c..HEAD` before this plan.
4. The surviving Administrative Templates Source line was re-confirmed by live raw-byte fetch to carry both
   `Update Channel (2.0)` and `Microsoft Office 2016 (Machine)`.
5. `EnableMicrosoftStoreSource` resolves to exactly one label in the file:
   `grep -c 'Installer Microsoft Store Source policy'` returns `2` and the older label returns `0`.
6. WR-01, WR-02 and WR-04 each appear as their own bullet under `## Future Requirements`, each with a
   measured coordinate, an authoring commit and a `Trigger:`; WR-04's bullet explicitly distinguishes itself
   from the pre-existing `co-management/03:55` entry.
7. `git status --porcelain` shows no file outside the two this plan declares — every phase prohibition still
   HELD, `00-overview.md` byte-unchanged.
8. All seven gates at their recorded baselines, the apex run separately and last.
</verification>

<success_criteria>
- The unsupported comparison is gone from `08-windows-app-updates.md`, removed rather than re-cited or
  hedged, with the removal rationale recorded.
- The surviving `change-update-channels` Source line is honest — re-confirmed live to carry what now sits
  above it.
- One label for `EnableMicrosoftStoreSource` in the file; the other five CSP rows byte-unchanged and in
  source order.
- Three evidence-carrying backlog bullets filed; `00-overview.md` byte-unchanged.
- Structural contract unchanged: 10 H2s, 8 `<a id=` anchors, 0 code fences, no `doc_id`, `w/lf`, stamps not
  advanced, 16 phase anchors.
- Gate battery re-measured, every count recorded as a number, apex 101 PASS / 0 FAIL / 0 SKIPPED.
- Three commits, each touching exactly one file.
</success_criteria>

<output>
Create `.planning/phases/148-application-update-management-winget-routing/148-05-SUMMARY.md` when done.
Record: the live-fetch hit counts for `Update Channel (2.0)` and `Microsoft Office 2016 (Machine)` on the
Administrative Templates article; the live ADMX friendly-name hit count from `policy-csp-desktopappinstaller`;
the before/after match counts for all three string replacements; the `--numstat` figure for each of the three
commits; every gate's printed number; and the per-path result of the prohibition sweep.
</output>
