---
phase: 148-application-update-management-winget-routing
plan: 03
type: execute
wave: 3
depends_on: ["148-01", "148-02"]
files_modified:
  - docs/operations/patch-management/00-overview.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [APP-01, APP-02, APP-03, APP-04, APP-05, APP-06]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/operations/patch-management/00-overview.md routes a reader to both new guides: two routing bullets inserted after the existing Windows ring bullet in the order 07 then 08, and two Related Resources entries inserted so the Windows entries stay contiguous (D-52)."
    - "All four insertions are additive. No existing line in 00-overview.md is reworded, no comparison-table row is added or changed, and the platform-applicability blockquote lead-in is untouched (D-52, D-54)."
    - "The reason no comparison-table row is added is editorial, not mechanical: the header-row check is presence-only and a one-line data row was measured passing at 32/0/0, so the table could technically absorb one — but the table is concept-keyed across five platforms and an application row would be Windows-only in four of five cells (D-13, D-54)."
    - "check-phase-54 is the only validator that PINS 00-overview.md, but not the only one that READS it: the milestone audit walks live docs/ and the link checker scans every relative link in the file, so a green run of one is not a green run of all three (D-10)."
    - "Two accepted inconsistencies are recorded rather than fixed, so neither the verifier nor code review surfaces them as new defects: the sealed Windows ring guide will never point at either new guide although it does point at the driver guide, and the overview's Windows routing blockquote line stays true while not naming the two new guides. Both are incomplete rather than wrong and no v1.21 phase owns either (D-75)."
    - "Every inserted [text](url) construct sits entirely on one physical line, because the anti-scope-creep strip in check-phase-54 removes links with a regex that does not cross a newline — a wrapped link leaks its text into what the validator treats as body prose (D-53)."
    - "No insertion contains any of the three tokens the anti-scope-creep check bans from 00-overview body prose, in prose, in link text, or in the em-dash description after a link — the description is not stripped at all, so a natural summary of the Autopatch guide would fail the check (D-53)."
    - "The frontmatter re-stamp is conditional and computed by arithmetic: if the execution date differs from the current last_verified, both stamps advance together with review_by equal to last_verified plus exactly 60 days; if it matches, the frontmatter is a deliberate no-op and no diff is manufactured (D-55)."
    - "Neither stamp is ever advanced without the other. The frontmatter check has no lower bound, so advancing last_verified past review_by drives the interval negative and passes silently, while advancing review_by alone fails loudly at 61 days (D-55)."
    - "Every string replacement against 00-overview.md is verified to have matched, by counting before and after. The file's worktree copy is CRLF while its structural siblings are LF, and a zero-match replacement reports success (D-56)."
    - "The seven occurrences of the word five in 00-overview.md are mapped before editing and none is changed. The sentence stating that each of the five guides routes back via its own blockquote was already made contestable by Phase 146's sixth guide and was deliberately left then; changing it now would be an unowned scope expansion (D-57)."
    - "The falsified hotpatch cell in the comparison table is left standing and filed to the backlog with its evidence, because no APP requirement and no Success Criterion covers it and Phase 145 excluded it deliberately from its own atom (D-03)."
    - "The co-management guide is cross-linked and never edited by any task in this phase; its falsified workload-count wording, its pre-April-2025 entitlement parenthetical and its planning-ledger citation all go to the backlog unedited (D-49, D-51)."
    - "The backlog entries carry their evidence, not just their names — each names the file and the measurement that establishes the defect, so the item is owned rather than forgotten (D-03)."
    - "REQUIREMENTS.md still contains zero occurrences of the purged off-by-one filename literal after the backlog edit, so the errata literal-purge check and its atomicity coupling both stay green."
    - "A prose-accuracy pass is run over both new guides and the overview independently of the validators, because the validators structurally cannot see wrong prose: a prior phase shipped two Critical defects that only code review caught, and a CONTEXT prose rule was once broken four times inside one guide's own prose while seven gates read green (D-69)."
    - "node scripts/validation/check-phase-54.mjs reports 32 passed, 0 failed, 0 skipped after the overview edits — the anti-scope-creep check and the frontmatter interval check both bind this file."
    - "node scripts/validation/check-nav-hub-links.mjs reports 0 hub-presence failures and 0 corpus-link failures — the four new links resolve, so the phase never lands the four-corpus-link-failure state that would take the apex to 100 PASS / 1 FAIL."
    - "node scripts/validation/check-phase-144.mjs reports 101 PASS, 0 FAIL, 0 SKIPPED as the final measurement of the phase (D-67)."
  prohibitions:
    - requirement_id: APP-02
      category: transparency
      statement: "MUST NOT correct, reword or delete the falsified in-memory-patching cell in the overview's comparison table. No requirement owns it, the governing correction requirement is already closed, and the prior phase's atom list excluded it on purpose. Record it in the backlog with its evidence instead."
    - requirement_id: APP-01
      category: values
      statement: "MUST NOT edit any file under docs/operations/co-management/ for any reason, including to fix a wording this phase has measured as falsified. The prohibition is an owner ruling and is independently sufficient on five separate grounds recorded in CONTEXT."
    - requirement_id: APP-01
      category: transparency
      statement: "MUST NOT file a backlog entry as a bare name. Every entry carries the file, the measurement that establishes the defect, and why no phase in this milestone owns it — an entry without its evidence is forgotten rather than deferred."
    - requirement_id: APP-03
      category: transparency
      statement: "MUST NOT treat a green validator run as evidence that the two new guides are correct. The measured gate coverage on those files is three assertions; every quoted claim they carry is unenforced by any script in this repository."
  artifacts:
    - docs/operations/patch-management/00-overview.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "00-overview.md -> 07-windows-autopatch.md and 08-windows-app-updates.md — two routing bullets plus two Related Resources entries; four new corpus links, all resolved because both targets exist by wave 3 (D-52, D-65)."
    - "00-overview.md frontmatter last_verified / review_by — a 60-day interval computed by arithmetic and asserted by the frontmatter check, which has no lower bound and therefore cannot catch a negative interval (D-55)."
    - "REQUIREMENTS.md ## Future Requirements — the project's backlog mechanism; the file is live-read by the errata literal-purge check and by its atomicity-coupling check, so the apex must be re-run after the edit."
---

<objective>
Land the fourth and **final** content touch of `docs/operations/patch-management/00-overview.md`, file
this phase's measured-but-unowned defects to the backlog with their evidence, run the prose-accuracy
pass the validators cannot perform, and close the phase as **commit 3 of three** (D-65).

Purpose: the reachability half of all six APP requirements. Neither the ROADMAP Success Criteria nor
the requirement text assigns the overview edits to a specific APP identifier; what they assign is that
both new guides exist and are findable from the domain's hub. This plan contributes that half and no
new subject-matter content.

Output: four additive insertions plus a conditional frontmatter re-stamp in `00-overview.md`, and a set
of evidence-carrying backlog entries in `REQUIREMENTS.md`. **No** registry row, filename-map row,
canary bump, ops-index row or nav-hub wiring — all Phase 152's single atomic commit (D-73). **No** edit
to `01-windows-wufb-rings.md`, `co-management/**`, `app-lifecycle/**`, `admin-setup-apv2/**` or any
`docs/reference/` matrix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/148-application-update-management-winget-routing/148-CONTEXT.md
@.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md
@.planning/phases/148-application-update-management-winget-routing/148-PATTERNS.md
@.planning/phases/148-application-update-management-winget-routing/148-01-SUMMARY.md
@.planning/phases/148-application-update-management-winget-routing/148-02-SUMMARY.md
</context>

<phase_wide_rules>

R1 through R12 from `148-01-PLAN.md` bind this plan unchanged. Re-read them there. The additions below
are specific to the overview and to the backlog edit.

**R17 — The three tokens banned from every `00-overview.md` insertion.** The anti-scope-creep check
reads the overview's body prose after stripping frontmatter, table rows, fenced code, `[text](url)`
constructs and inline code — and **nothing else**. In particular it does **not** strip the em-dash
description that follows a link in a Related Resources entry, and its link-strip regex does not cross a
newline. So both an ordinary description and a link wrapped across two physical lines leak into what
the check treats as body prose. The three banned substrings are, verbatim, the ones the check's own
`banned` array holds: `Hotpatch`, `VBS`, `MEETS_STRONG_INTEGRITY`. None may appear in any insertion —
not in prose, not in link text, not in a description. The corpus already models the workaround: the
existing `01` Related Resources entry describes the same subject as "in-memory kernel patching
servicing model". Reuse that register.
<!-- planner-discipline-allow: Hotpatch, VBS, MEETS_STRONG_INTEGRITY -->

**R18 — One physical line per `[text](url)` construct.** A measured probe on this exact file recorded a
one-line entry at 32/0/0 and the identical text wrapped mid-link at 31/1. The **description after** the
link may wrap — every existing entry in this file does — but the bracket-and-parenthesis construct
itself must not. Table rows are likewise one physical line each; the measured maximum in this file is
403 characters unwrapped.

**R19 — The file is CRLF in the worktree.** Confirmed at plan time by `od -c`: the file's first bytes
are `- - - \r \n`. Its structural siblings `05` and `06` are LF. A replacement pattern anchored with
`$`, or one carrying a literal `\n`, will match nothing and report success. **Count occurrences before
and after every replacement and assert the delta.** On this host `cat -A` misreports the line endings
inside Git Bash — measured at plan time, it printed a bare `$` for this very file. Use
`git ls-files --eol` or `od -c`.

**R20 — The frontmatter re-stamp is conditional and paired.** Current state, measured at plan time:
`last_verified: 2026-08-21`, `review_by: 2026-10-20` — exactly 60 days, zero slack under a check that
fails when the interval exceeds 60. If this phase's execution date (the date of commit 1, recorded in
`148-01-SUMMARY.md`, per D-76) differs from `2026-08-21`, advance **both** stamps in the same edit,
computing `review_by = last_verified + 60` by arithmetic. If it matches, do nothing — do not
manufacture a diff. Never advance one without the other: the check has no lower bound, so a negative
interval passes silently, while a 61-day interval fails loudly.

**R21 — No new comparison-table row and no blockquote change.** The comparison table is concept-keyed
across five platforms; an application row would be Windows-only in four of five cells. The
platform-applicability blockquote lead-in is pinned as a literal within the file's first 50 body lines,
and a reworded lead-in was measured failing at 31/1 in a prior phase. `07` and `08` are Windows
surfaces, not platforms, so the blockquote's platform enumeration does not change either.

**R22 — `REQUIREMENTS.md` is live-read by two checks.** The errata literal-purge check asserts the file
contains **zero** occurrences of a purged off-by-one filename, and an atomicity-coupling check asserts
that result together with two others. Neither is at risk from an append, but both must be re-run after
the edit, and no backlog entry may introduce that filename literal. `check-phase-61` reads this file at
a frozen commit and is unaffected by live edits; the apex covers it regardless.

**R23 — Backlog entries carry evidence.** Each entry names the file, the specific measurement that
establishes the defect, and why no phase in this milestone owns it. The existing
`## Future Requirements` bullets are the shape to match: a bold lead-in, then the measurement, then a
trigger. Note the corpus-wide convention that a decision bullet's bold lead-in must not contain a
colon — a `file:line` or a trailing `paths:` inside the bold segment breaks downstream parsing. Put any
coordinate outside the bold segment.

</phase_wide_rules>

<tasks>

<task type="auto">
  <name>Task 1: The four additive insertions in 00-overview.md, plus the conditional frontmatter re-stamp</name>

  <precondition>Both `docs/operations/patch-management/07-windows-autopatch.md` and `docs/operations/patch-management/08-windows-app-updates.md` exist and `check-nav-hub-links` reports 0/0 (plans 148-01 and 148-02 complete).</precondition>

  <files>docs/operations/patch-management/00-overview.md</files>

  <read_first>
    - `docs/operations/patch-management/00-overview.md` in full — the current frontmatter, the
      `## Routing to Per-Platform Guides` bullet list, and the `## Related Resources` entry list. Read
      the whole file rather than the two sections, because Task 1 also needs the map of every
      occurrence of the word `five`.
    - `docs/operations/patch-management/07-windows-autopatch.md` and
      `docs/operations/patch-management/08-windows-app-updates.md` as shipped — the two H1s and the
      opening scope paragraphs, which are what the routing bullets and the descriptions must
      faithfully summarize.
    - `.planning/phases/148-application-update-management-winget-routing/148-01-SUMMARY.md` — the
      execution date the whole phase shares.
    - `scripts/validation/check-phase-54.mjs` — read the anti-scope-creep check's strip chain and its
      banned-substring array directly, and read the frontmatter interval check. Reading the code is the
      only reliable way to see that the description after a link is never stripped.
  </read_first>

  <action>
Make four additive insertions and one conditional frontmatter change. Change nothing else.

**Step 0 — measure first.** Record, before any edit: `grep -c 'five' `, `grep -c '^- \[' `,
`grep -c '07-windows-autopatch.md' `, `grep -c '08-windows-app-updates.md' ` and the exact frontmatter
stamps. Every one of these is asserted again after the edit.

**Step 1 — the two routing bullets.** In `## Routing to Per-Platform Guides`, insert two bullets
immediately after the existing bullet that ends by linking the Windows ring guide, and immediately
before the bullet that begins by routing driver and firmware approval workflows. Order: the Autopatch
guide first, then the application-updates guide. Match the existing bullet grammar exactly — each
existing bullet opens with a for-whom clause and ends with the link. Keep each `[text](url)` construct
entirely on one physical line; the surrounding sentence may wrap, as every existing bullet does.

The Autopatch bullet routes readers planning Autopatch enrollment, the group model or the entitlement
comparison. The application-updates bullet routes readers choosing an application update mechanism,
setting a Microsoft 365 Apps channel, or deciding what the enterprise catalog and the Store app type
can and cannot do.

**Step 2 — the two Related Resources entries.** Insert two entries so the Windows entries stay
contiguous: after the existing driver-and-firmware entry and before the macOS entry. Match the existing
shape — `- [Title](file.md) — description`. The description may wrap onto continuation lines, exactly
as every existing entry does; the link construct may not.

**The description constraint is load-bearing and is why this step is not mechanical.** The natural
one-line summary of the Autopatch guide names the in-memory patching feature by its product name, and
that name is one of the three substrings the anti-scope-creep check bans from this file's body prose.
The description is **not** stripped before that check runs. Describe the same material the way the
existing `01` entry already does — by the servicing model rather than the feature name — and describe
the entitlement material as an entitlement comparison rather than by naming the feature.

**Step 3 — the frontmatter, conditionally.** Compare the file's current `last_verified` against this
phase's execution date. If they differ, set `last_verified` to the execution date and set `review_by`
to that date plus exactly 60 days, computed rather than copied, editing both in the same change. If
they match, leave the frontmatter untouched and record the deliberate no-op in the summary.

**Step 4 — the `five` site map, and the decision not to act on it.** The word occurs at seven sites.
Record all seven in the summary with their surrounding sentence. Change **none** of them. Specifically,
the sentence stating that each of the five guides routes back to this overview via its own
applicability blockquote was already contestable after Phase 146 shipped a sixth guide that also routes
back, and Phase 146 deliberately left it; changing it now would be an unowned scope expansion in a file
whose blast radius is explicitly limited to additive insertions. The parenthetical naming the file
number range describes the five per-platform files, and the three later-numbered guides are not
per-platform, so it is correct as written.

**Step 5 — verify every replacement matched.** Re-run every count from Step 0 and assert the delta:
`five` unchanged at 7, `^- \[` up by exactly 2, each new filename at exactly 2 occurrences (one routing
bullet plus one Related Resources entry). A replacement that silently matched nothing is the recorded
failure mode for this specific file.

Add no comparison-table row. Do not touch the platform-applicability blockquote. Do not reword any
existing line.
  </action>

  <verify>
    <automated>node scripts/validation/check-phase-54.mjs | tail -3 && node scripts/validation/check-nav-hub-links.mjs</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '07-windows-autopatch.md' docs/operations/patch-management/00-overview.md` returns
      exactly `2`, and `grep -c '08-windows-app-updates.md'` returns exactly `2`.
    - `grep -c 'five' docs/operations/patch-management/00-overview.md` returns exactly `7` — unchanged
      from the plan-time measurement.
    - `grep -c '(file numbers 01 through 05)' docs/operations/patch-management/00-overview.md` returns
      `1` — the parenthetical is untouched.
    - Zero banned substrings anywhere in the file's insertions:
      `grep -cE 'Hotpatch|VBS|MEETS_STRONG_INTEGRITY' docs/operations/patch-management/00-overview.md`
      returns exactly `1` — the single pre-existing occurrence inside the comparison table row, which
      is stripped by the check and is left standing per the backlog ruling. If this returns anything
      other than `1`, an insertion introduced one.
    - Every line containing a new filename also contains a complete `[text](url)` construct on that
      same physical line:
      `grep -n '0[78]-windows' docs/operations/patch-management/00-overview.md | grep -c ']('` equals
      `4`.
    - `git ls-files --eol docs/operations/patch-management/00-overview.md` still reports `i/lf w/crlf`
      — the edit did not convert the file's line endings.
    - The frontmatter interval is exactly 60 days:
      `python -c "import datetime,re,sys; s=open('docs/operations/patch-management/00-overview.md',encoding='utf-8').read(); lv=re.search(r'last_verified: (\S+)',s).group(1); rb=re.search(r'review_by: (\S+)',s).group(1); d=(datetime.date.fromisoformat(rb)-datetime.date.fromisoformat(lv)).days; print(d); sys.exit(0 if d==60 else 1)"`
      prints `60` and exits 0.
    - `node scripts/validation/check-phase-54.mjs` exits 0 and prints
      `32 passed, 0 failed, 0 skipped` — this is the run that catches an anti-scope-creep leak, and no
      inspection of the file can substitute for it.
    - `node scripts/validation/check-nav-hub-links.mjs` prints
      `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total` — the four new links resolve.
    - `git diff --stat docs/operations/patch-management/00-overview.md` shows insertions only, with no
      more than 2 deleted lines (the two frontmatter stamp lines when the re-stamp fires) and 0 deleted
      lines when it does not.
  </acceptance_criteria>

  <done>Four additive insertions land, both guides are reachable from the hub, the frontmatter interval
  is exactly 60 days by arithmetic, every replacement is proven to have matched, and the anti-scope-creep
  check is still 32/0/0.</done>
</task>

<task type="auto">
  <name>Task 2: File this phase's measured-but-unowned defects to the backlog, each with its evidence</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` `## Future Requirements` in full — the existing bullet shape, and
      specifically the bullet deferring the Intune-side application-channel policy surface, which this
      phase's research has now superseded by fetching it and shipping the section.
    - `.planning/phases/148-application-update-management-winget-routing/148-CONTEXT.md`
      `<deferred>` section — the nine items, each already carrying its evidence and its ruling.
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md`
      §`Corrections to CONTEXT.md` and the entry answering the Autopatch workload-count question — the
      measurement that falsifies the co-management guide's wording.
    - `docs/operations/00-index.md` — confirm the description wording that is now out of date, so the
      backlog entry quotes what is actually there. **Read only; the index is Phase 152's file.**
  </read_first>

  <action>
Append evidence-carrying entries to `.planning/REQUIREMENTS.md` under `## Future Requirements`. This
task edits no file under `docs/`.

Each entry follows the existing bullet shape: a bold lead-in with **no colon inside the bold segment**,
then the measurement that establishes the defect, then why no phase in this milestone owns it, then a
trigger. Coordinates go outside the bold segment.

File these:

1. **The falsified comparison-table cell in the patch-management overview.** The cell asserts a default
   activation and a prerequisite that the current first-party articles do not support. It is left
   standing on an owner ruling: no APP requirement and no Success Criterion covers it, the governing
   correction requirement is already closed, and the prior corpus-correction phase's own atom list
   excluded this file from that correction deliberately. Trigger: a corpus-hygiene milestone, or an
   amendment to the closed correction requirement.
2. **The falsified workload-count wording in the co-management migration-paths guide.** The guide
   attributes to the Autopatch prerequisites page a claim that it specifies all three workloads. The
   page was fetched this phase: it names **two** workloads and then refers to three without naming a
   third, and the two application-suite terms occur zero times on it. The corpus guide's wording is
   therefore falsified in its specifics. It is not edited — the do-not-edit ruling on that file is an
   owner ruling with five independently sufficient grounds. Trigger: a corpus-hygiene milestone.
3. **The downstream sites of the same three-workload claim** in the co-management overview and the
   workload-slider guide. If a future fetch establishes two, these become live falsehoods in files no
   phase in this milestone owns; one of them is itself covered by a separate owner ruling.
4. **The pre-April-2025 entitlement parenthetical in the co-management migration-paths guide**, which
   excludes two subscription families that are currently eligible per the research ledger. Not fixed
   here.
5. **The planning-ledger citation inside that same shipped corpus guide** — a parenthetical naming a
   planning file and a requirement identifier inside a published document. It escapes the existing
   path-prefix sweep by omitting the path, but it is the same defect class that sweep exists to
   prevent.
6. **The package-manager conflation asserted in a reference matrix's own voice**, which the obvious
   term grep misses because the row names the product in full rather than by its tool name; plus the
   bare unlinked form in the device-group guide, which stands unchanged and unlinked so the correction
   authored this phase is reachable only by a reader who already found it. Trigger: a scoped sweep.
7. **The operations index's stale hub description**, which still describes a four-platform comparison
   after a prior phase made it five, and which a later phase in this milestone will take to nine rows
   underneath without correcting the description — that phase is scoped to adding rows.
8. **The unresolved Windows 11 Pro eligibility question for in-memory patching.** Re-fetched this
   phase: the sole first-party page carries zero occurrences of that edition name and states the
   prerequisite as an entitlement list rather than an edition list, so it neither supports nor refutes
   eligibility. The research ledger's plan-time task on this is **not** discharged. Both new guides
   ship it flagged unconfirmed. No phase owns resolving it.
9. **Glossary terminology for this phase's new vocabulary** — the group model, the application update
   channels, the enterprise catalog and the Store application type. No requirement in this milestone
   covers glossary additions; the same unassigned gap a prior phase recorded for its own terminology.

Then **supersede, do not delete**, the existing `## Future Requirements` bullet that defers the
Intune-side application-channel policy surface on the grounds it was never fetched. Append a bracketed
marker to that bullet in the style the roadmap already uses for a superseded entry, stating that the
fetch succeeded in this phase, naming the canonical article path the section cites, and recording that
the section shipped. This resolves the governing-document conflict that owner ruling D-02 required be
stated rather than silently resolved — the roadmap named the surface under this phase's research flag
while this file deferred it past the milestone.

Introduce no new requirement identifiers. Do not renumber or reword any existing requirement. Do not
write the purged off-by-one filename literal.
  </action>

  <verify>
    <automated>node scripts/validation/check-phase-54.mjs | tail -3 && node scripts/validation/check-phase-144.mjs | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` `## Future Requirements` section gained at least 9 new bullets:
      `awk '/^## Future Requirements/,/^## Out of Scope/' .planning/REQUIREMENTS.md | grep -c '^- '`
      returns at least 14 (5 existing plus 9 new).
    - Every new bullet contains at least one of the words `measured`, `fetched`, `occurrences`, `zero`
      or a quoted string — an entry with no evidence is not acceptable:
      the count of new bullets equals the count of new bullets matching
      `-iE 'measured|fetched|occurrenc|zero|"'`.
    - No new bullet's bold lead-in contains a colon:
      `awk '/^## Future Requirements/,/^## Out of Scope/' .planning/REQUIREMENTS.md | grep -c '^- \*\*[^*]*:[^*]*\*\*'`
      returns `0`.
    - The superseded marker is present on the pre-existing deferral bullet:
      `grep -c 'SUPERSEDED' .planning/REQUIREMENTS.md` returns >= 1, and the same bullet contains
      `device-configuration/settings-catalog/update-office`.
    - `grep -c '05-compliance-policy.md' .planning/REQUIREMENTS.md` returns `0` — the errata
      literal-purge check and its atomicity coupling both depend on this.
    - `grep -cE '^- \[ \] \*\*[A-Z]+-[0-9]+\*\*' .planning/REQUIREMENTS.md` is unchanged from before the
      edit — no new requirement identifier was introduced.
    - `git status --porcelain docs/` returns empty — this task touched no corpus file.
    - `node scripts/validation/check-phase-54.mjs` prints `32 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/check-phase-144.mjs` prints `101 PASS, 0 FAIL, 0 SKIPPED`.
  </acceptance_criteria>

  <done>Nine evidence-carrying backlog entries are filed and the stale deferral bullet is marked
  superseded rather than deleted; no corpus file was touched; the errata purge and the apex are green.</done>
</task>

<task type="auto">
  <name>Task 3: The prose-accuracy pass the validators cannot perform, the full gate battery, and commit 3</name>

  <files>docs/operations/patch-management/00-overview.md, docs/operations/patch-management/07-windows-autopatch.md, docs/operations/patch-management/08-windows-app-updates.md</files>

  <read_first>
    - `docs/operations/patch-management/07-windows-autopatch.md` and
      `docs/operations/patch-management/08-windows-app-updates.md` in full, end to end, in one pass.
      This is the point of the task; do not sample.
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md`
      §`Verbatim Quote Bank` and the `## Plan-Time Fetch Addendum (Plan 148-01)` — the two sources
      every quoted string in both guides must trace back to.
    - `.planning/phases/148-application-update-management-winget-routing/148-CONTEXT.md`
      `<decisions>` — the prose negatives listed under the decision requiring each of them to carry a
      runnable check.
    - `.planning/phases/148-application-update-management-winget-routing/148-01-SUMMARY.md` and
      `148-02-SUMMARY.md` — the anchor ids as actually shipped, and the deliberate choices recorded
      there.
  </read_first>

  <action>
Verify the content, not just the gates, then land commit 3 of three.

**A green gate run proves nothing about correctness here, and that is measured, not rhetorical.** The
two new guides sit outside the patch-file array that carries most of the frontmatter, table and
placeholder-token assertions; they carry no enrollment identifier so the document-contract audit does
not reach them; and they have no registry row so neither publish canary sees them. The live assertions
on them number three. Every quoted claim they carry is unenforced by any script in this repository.
Two Critical defects in a prior phase were caught only by code review, and a prose rule was once broken
four times inside a guide's own prose while seven gates read green.

**Step 1 — the runnable negative battery.** Run every negative check across all three files and record
the counts in the summary. At minimum: the two on-premises product name forms; the plural ring form;
the inverted entitlement conclusion; the three British spellings; the placeholder tokens; the numbered
pitfall prefix; the enrollment identifier field; the third-party product and project names; the client
repository issue reference; the code-fence count; and the three tokens banned from the overview's body
prose. Any non-zero count is a defect to fix in this commit, not a finding to record.

**Step 2 — the quote trace.** For every blockquote in both new guides, confirm three things: that the
string appears in the quote bank or the fetch addendum; that the `**Source:**` line beneath it points
at the single page that string came from; and that the Source line does not also sit beneath material
from a different page. This phase quotes eleven-plus pages and carries the milestone's heaviest
multi-page load, and a Source line silently widening to cover an unsourced neighbor is the exact defect
a prior phase's review found. Fix any that has drifted.

**Step 3 — the four content assertions the Success Criteria turn on.** Confirm by reading, not by
grep: that both entitlement lists are shown differing and the necessary-but-not-sufficient statement is
present; that the channel material carries the default and its rollback value, the unsupported channel,
the one-channel-per-device rule, the device-scoped rule and the two excluded collaboration clients;
that all eight catalog limitations are present under their source heading names in source order and the
blocking-app positive is filed as a trade-off outside the callouts section; and that the Store material
carries the split, the preview status, the ARM64 exclusion, the stated cadence absence **and** its
contrast against the published objectives, plus the settings-catalog trap.

**Step 4 — coordinate freshness.** Every coordinate this phase's artifacts cite was measured at one
commit and this phase has since made three. Re-check any line number either new guide states in its own
prose (there should be none) and any coordinate the summaries carry. A coordinate that went stale
inside its own phase is a recorded failure mode here.

**Step 5 — the full battery.** Run all nine gates plus the apex and record every figure.

**Step 6 — commit.** Land commit 3 of three with a `docs(148):`-shaped message naming the overview
edits and the backlog entries. Then write the summary, including the hand-forward contracts: the 16
anchor ids for the recipe phase, the corrected containment position it must be written on, the channel
table with the default's rollback value, the catalog's no-rings and no-rollback pair, and the
observation that this phase is the only one authoring Autopatch content; plus the integration list for
the close phase — registry rows and filename-map rows for both new guides, both canary bumps computed
from the registry after the rows land rather than from a document count, the operations index rows, all
inbound navigation wiring, the return links from the application-lifecycle guides, and the inbound link
from the device-preparation pages to the reconciliation authored this phase.
  </action>

  <verify>
    <automated>node scripts/validation/check-phase-54.mjs | tail -3 && node scripts/validation/check-phase-53.mjs | tail -3 && node scripts/validation/check-phase-59.mjs | tail -3 && node scripts/validation/check-nav-hub-links.mjs && node scripts/validation/c17-eee-contract.mjs | tail -2 && node scripts/validation/v1.20-milestone-audit.mjs | tail -3 && node scripts/pipeline/build-filename-map.mjs --self-test | tail -2 && node scripts/pipeline/build-publish-bundle.mjs --self-test | tail -2 && node scripts/validation/check-phase-144.mjs | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - Across all three files, this returns `0`:
      `grep -rcE 'SCCM|System Center|Autopatch rings|enrolment|licence|behaviour|TBD|TODO|FIXME|PLACEHOLDER|PITFALL-|winget-cli|PatchMyPC|Chocolatey|Ninite|Scappman|Robopack'
      docs/operations/patch-management/00-overview.md docs/operations/patch-management/07-windows-autopatch.md docs/operations/patch-management/08-windows-app-updates.md | grep -v ':0$' | wc -l`
    - `grep -ci 'if you have autopatch you have hotpatch' docs/operations/patch-management/07-windows-autopatch.md`
      returns `0`, and `grep -c 'necessary but not sufficient'` on the same file returns >= 1 — the
      barred conclusion is absent **and** its positive replacement is present (D-18).
    - Per D-47, across all three files
      `grep -ciE 'patchmypc|chocolatey|ninite|scappman|robopack|ivanti|techdirect'` returns `0` for
      each — community tooling and third-party patch products appear only as a category, never named.
    - Per D-62 the summary records the phase verifier's re-fetch list by name, and per D-66 a
      remediation commit after code review is recorded as expected rather than as a plan failure.
    - `grep -c '^```' ` returns `0` for each of the three files.
    - `grep -c 'doc_id' ` returns `0` for both new guides.
    - `grep -cE 'Hotpatch|VBS|MEETS_STRONG_INTEGRITY' docs/operations/patch-management/00-overview.md`
      returns exactly `1` — the single pre-existing table-row occurrence, unchanged.
    - Anchor counts hold: `grep -c '<a id=' ` returns `8` for each new guide, 16 in total, and every id
      recorded in `148-01-SUMMARY.md` and `148-02-SUMMARY.md` is present exactly once in its file.
    - Every `**Source:**` line in both new guides is at the start of its own line:
      for each file, `grep -c '^\*\*Source:\*\*'` equals `grep -c '\*\*Source:\*\*'`.
    - `node scripts/validation/check-phase-54.mjs` → `32 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/check-phase-53.mjs` → `26 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/check-phase-59.mjs` → `36 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/check-nav-hub-links.mjs` →
      `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`.
    - `node scripts/validation/c17-eee-contract.mjs` → `234 files checked, 0 with violations`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` → `16 passed, 0 failed`.
    - `node scripts/pipeline/build-filename-map.mjs --self-test` → `8 passed, 0 failed`.
    - `node scripts/pipeline/build-publish-bundle.mjs --self-test` → `15 passed, 0 failed`.
    - `node scripts/validation/check-phase-144.mjs` → `101 PASS, 0 FAIL, 0 SKIPPED`.
    - `git status --porcelain docs/_registry/ docs/operations/00-index.md docs/operations/co-management/
      docs/operations/app-lifecycle/ docs/admin-setup-apv2/ docs/reference/` returns empty.
    - `git log --oneline -3` shows exactly three `docs(148)` commits for this phase.
  </acceptance_criteria>

  <done>Every negative check is zero across all three files, every blockquote traces to a single-page
  Source line, the four Success-Criteria content assertions are confirmed by reading, all nine gates and
  the apex are at their plan-time baselines, and commit 3 of three is landed.</done>
</task>

</tasks>

<artifacts_this_phase_produces>

This plan creates **no** new files, H2 headings or anchors. It adds rows to two existing files. The
source-grounding pass should treat the two new guides referenced here as symbols created by plans 01
and 02, already enumerated in those plans.

**New nav / overview rows in `docs/operations/patch-management/00-overview.md`**
- One new routing bullet under `## Routing to Per-Platform Guides` linking
  `07-windows-autopatch.md`
- One new routing bullet under `## Routing to Per-Platform Guides` linking
  `08-windows-app-updates.md`
- One new `## Related Resources` entry linking `07-windows-autopatch.md`
- One new `## Related Resources` entry linking `08-windows-app-updates.md`

**New rows in `.planning/REQUIREMENTS.md`**
- Nine new `## Future Requirements` bullets, each carrying its evidence
- One bracketed superseded marker appended to the pre-existing bullet deferring the Intune-side
  application-channel policy surface

**Changed values (not new symbols)**
- `00-overview.md` frontmatter `last_verified` and `review_by`, advanced together and only if the
  execution date differs from the current stamp

</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new corpus prose → the anti-scope-creep firewall on the domain hub | The overview is the one file in this phase that a live validator meaningfully constrains; an insertion crossing that firewall is invisible on inspection and only the validator sees it. |
| measured-but-unowned defects → the project backlog | A defect this phase found but is forbidden to fix must reach a durable, evidence-carrying record, or it is lost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-148-11 | Tampering | `00-overview.md` insertions vs the anti-scope-creep check | high | mitigate | R17 and R18: no banned substring in any insertion including the un-stripped description; one physical line per link construct. Task 1's criteria assert an exact banned-substring count of 1 (the pre-existing table cell) and assert the check at 32/0/0 — the only thing that can see the leak. |
| T-148-12 | Tampering | a silent zero-match string replacement on a CRLF file | high | mitigate | R19: count before and after every replacement and assert the delta. Task 1's criteria assert exact post-edit counts for both filenames and for the bullet total, so a no-op edit fails loudly. |
| T-148-13 | Tampering | frontmatter stamps advanced singly | medium | mitigate | R20 and a runnable 60-day arithmetic assertion in Task 1's criteria. The frontmatter check has no lower bound, so a negative interval would otherwise pass silently. |
| T-148-14 | Repudiation | a measured defect fixed outside the blast radius | medium | mitigate | The prohibition block bars editing the co-management guide and bars correcting the falsified table cell. Task 2 and Task 3 criteria assert `git status --porcelain` is empty for every read-only path. |
| T-148-15 | Repudiation | a backlog entry filed without its evidence | medium | mitigate | R23 and an acceptance criterion requiring every new bullet to carry a measurement, a count or a quoted string. |
| T-148-16 | Tampering | a fabricated or mis-scoped quote surviving into the shipped corpus | high | mitigate | Task 3 Step 2 traces every blockquote to the quote bank and checks every Source line's page scope. No script in this repository parses evidence lines; this pass and the phase verifier's re-fetch are the only gates. |
| T-148-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs; `148-RESEARCH.md` §Package Legitimacy Audit records "Not applicable". |

ASVS level 1; blocking threshold `high`. All three `high` items are dispositioned `mitigate` with a
named, asserted mechanism.
</threat_model>

<assumptions_and_open_items>

**Edge-probe arithmetic (no silent drops).** The deterministic probe returned **19** applicable edges
across APP-01..APP-06. Plans 01 and 02 own all 19 by requirement: **16** authored as resolved/explicit
truths, **2** as resolved/backstop structured markers, **1** surfaced as a flagged assumption (plan
01's APP-01 `unclassified` row). 16 + 2 + 1 = **19**. Plan 03 contributes the reachability half and
introduces no additional edge rows; its truths are integration and gate invariants.

**Open items this plan deliberately does not close.**
- The falsified comparison-table cell in `00-overview.md` stays wrong on purpose. It is an owner
  ruling, and both the reason and the evidence go to the backlog.
- The co-management guide's falsified workload-count wording stays wrong on purpose, for the same
  reason and with the measurement attached.
- The operations-index hub description stays stale; the phase that adds rows beneath it is scoped to
  adding rows, and no phase in this milestone owns the description.
- The Windows 11 Pro eligibility question stays unresolved; the research ledger's plan-time task on it
  is explicitly **not** discharged by this phase.
- Two accepted inconsistencies are recorded so neither the verifier nor code review surfaces them as
  new defects: the sealed Windows ring guide's own Related Resources list will never point at either
  new guide although it does point at the driver guide, and the overview's Windows routing blockquote
  line stays true while not mentioning the two new guides. Both are incomplete rather than wrong, and
  no phase in this milestone owns either.

**Remediation is expected.** The overview is named in the requirements as the likeliest source of a
mid-execution remediation round, and neither predecessor phase finished in the number of plans it
started with. A remediation commit after code review is inside this plan's budget and is not a plan
failure.

</assumptions_and_open_items>

<verification>
The nine gates, run from the repository root as the phase's final measurement:

1. `node scripts/validation/check-phase-54.mjs` → `32 passed, 0 failed, 0 skipped`
2. `node scripts/validation/check-phase-53.mjs` → `26 passed, 0 failed, 0 skipped`
3. `node scripts/validation/check-phase-59.mjs` → `36 passed, 0 failed, 0 skipped`
4. `node scripts/validation/check-nav-hub-links.mjs` → `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`
5. `node scripts/validation/c17-eee-contract.mjs` → `234 files checked, 0 with violations`
6. `node scripts/validation/v1.20-milestone-audit.mjs` → `16 passed, 0 failed`
7. `node scripts/pipeline/build-filename-map.mjs --self-test` → `8 passed, 0 failed`
8. `node scripts/pipeline/build-publish-bundle.mjs --self-test` → `15 passed, 0 failed`
9. `node scripts/validation/check-phase-144.mjs` → `101 PASS, 0 FAIL, 0 SKIPPED`

Beyond the gates, the phase verifier must re-fetch and diff. Name these pages explicitly in its
re-fetch list, because a revision to any of them changes what the corpus should say: the Microsoft 365
Apps channels overview and the channel-unification article (a revision dissolves the documented cadence
conflict), and the Microsoft Store apps article (a preview exit breaks a Success Criterion clause). Add
the Autopatch prerequisites page and the in-memory-patching page, whose two entitlement lists are the
phase's single most checkable deliverable.
</verification>

<success_criteria>
- `00-overview.md` routes to both new guides through four additive, single-line-link insertions; no
  existing line is reworded; no comparison-table row is added; the applicability blockquote is
  unchanged.
- The frontmatter interval is exactly 60 days by arithmetic, with both stamps advanced together or
  neither advanced.
- Nine evidence-carrying backlog entries exist and the stale deferral bullet is marked superseded
  rather than deleted, resolving the governing-document conflict the owner ruling required be stated.
- Every negative check across all three files returns zero, and every blockquote in both new guides
  traces to a single-page Source line.
- All nine gates and the apex read at their plan-time baselines, measured after the final commit.
- Three `docs(148)` commits exist and nothing outside the phase's declared file set has changed.
</success_criteria>

<output>
Create `.planning/phases/148-application-update-management-winget-routing/148-03-SUMMARY.md` when done.

Record in it: the four insertion sites as landed; whether the frontmatter re-stamp fired and the two
dates used; the seven-site map for the word `five` and the recorded decision not to change any of them;
the nine backlog entries as filed; every negative-check count from Step 1; the nine gate figures; and
the two hand-forward contracts — the 16 anchor ids and the content the recipe phase consumes, and the
integration list for the close phase including the rule that both canary targets are computed from the
registry after the rows land, never hard-coded from a document count.
</output>
