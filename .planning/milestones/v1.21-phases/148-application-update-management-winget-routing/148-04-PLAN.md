---
phase: 148-application-update-management-winget-routing
plan: 04
type: execute
wave: 1
depends_on: ["148-02"]
files_modified:
  - docs/operations/patch-management/08-windows-app-updates.md
autonomous: true
requirements: [APP-03]
gap_closure: true

estimate:
  tokens: 18000
  raw_tokens: 9000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The cadence-conflict passage in `08-windows-app-updates.md` — the paragraph beginning 'Neither reading is supportable' and ending 'remain on Version 2508.' — is followed by its own standalone `**Source:**` line naming the SAEC/MEC unification article, so a reader tracing the build-number discriminator quote or the September 8 2026 escape-hatch quote lands on the page that actually carries them (APP-03, D-63, D-64, R7)."
    - "That Source line is byte-identical to the one already shipping at `08:96` — same bold label, same link text, same URL, same `(updated YYYY-MM-DD)` suffix — so the file's one-Source-line-per-contiguous-quote-per-page convention is copied, not re-composed (D-63)."
    - "The Source line occupies exactly one physical line and its `[text](url)` is not wrapped, so `check-phase-54.mjs`'s `/\\[.*?\\]\\(.*?\\)/g` strip removes it whole and no link text leaks into what the validator treats as body prose (D-53 wrapped-link leak)."
    - "Both quoted strings were re-confirmed present byte-for-byte on the live unification page by raw-HTML `curl` plus tag-strip plus literal `grep` — this project's own established method — before the Source line shipped, and the `(updated ...)` date the line carries was confirmed against that same fetch (D-62)."
    - "The insertion is purely additive: `08-windows-app-updates.md` still measures 10 H2s, 8 own-line `<a id=` anchors, 0 code fences, no `doc_id` field, and `git ls-files --eol` still reports `w/lf` (D-56, D-70, 16-anchor contract)."
    - "The string replacement that performed the insertion was match-counted before and after — a zero-match replacement reporting success is the failure mode this repo has already been bitten by (D-56)."
    - "Every gate that reads this file was re-run after the edit and its count recorded as a measured number, not asserted as 'should pass': `check-phase-53` 26/0/0, `check-phase-54` 32/0/0, `check-phase-59` 36/0/0, `check-nav-hub-links` 0/0, `c17-eee-contract` 234 files / 0 violations, `v1.20-milestone-audit` 16/0, apex `check-phase-144` 101/0/0 (D-67)."
    - "All eight of this phase's prohibitions remain HELD — no file outside `docs/operations/patch-management/08-windows-app-updates.md` is touched, and in particular `00-overview.md` is byte-unchanged (its two pre-existing conditions stay filed to backlog per the owner ruling recorded in `148-03-SUMMARY.md`)."
  artifacts:
    - "docs/operations/patch-management/08-windows-app-updates.md — one added `**Source:**` line plus its separating blank line; no other change."
  key_links:
    - "The cadence-conflict passage → `https://learn.microsoft.com/en-us/microsoft-365-apps/updates/unified-update-channels` — the URL is not new to the file (it already ships at `08:96`), so no new link target, no new banned-token exposure, and no new nav-hub link-check surface is created."
---

<objective>
Close the single gap `148-VERIFICATION.md` found: two verbatim quotes in `08-windows-app-updates.md`'s
cadence-conflict passage carry no `**Source:**` line of their own, and the nearest preceding Source line
(`08:104`) names a page that provably does not contain either string.

Purpose: the phase's own evidence-line contract (R7, D-63, D-64, and plan 02 Task 2's acceptance
criterion) exists precisely so a reader can trace any quoted claim to a page that carries it. The content
is genuine and byte-accurate — the verifier re-fetched and matched both strings on the unification
article. This is a citation-attribution defect, not a fabrication, and the verifier's closing judgment was
verbatim: "The fix is a single added Source line, not a re-plan."

Output: one added Source line in one file, with the full gate battery re-measured behind it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-application-update-management-winget-routing/148-VERIFICATION.md
@docs/operations/patch-management/08-windows-app-updates.md
</context>

<phase_wide_rules>

**Blast radius is one file.** `docs/operations/patch-management/08-windows-app-updates.md` and nothing
else. Do not open `00-overview.md`, `01-windows-wufb-rings.md`, `07-windows-autopatch.md`,
`docs/operations/co-management/**`, `docs/operations/app-lifecycle/**`,
`docs/operations/admin-setup-apv2/**`, or `docs/reference/**`. All eight of this phase's prohibitions were
verified HELD at verification time; this plan must keep every one of them held.

**Do not re-plan APP-01, APP-02, APP-04, APP-05 or APP-06.** All five are `✓ SATISFIED` and were
independently re-verified against live source bytes. APP-03 is also SATISFIED — it carries only the
citation defect this plan closes.

**Do not touch the two pre-existing `00-overview.md` conditions** — the falsified Hotpatch/VBS
comparison-table cell at `:56-57` and the three negative-battery hits at `:56`, `:108`, `:133`. Both were
left standing on an explicit owner ruling, are documented in `148-03-SUMMARY.md` Deviations, and are filed
under `REQUIREMENTS.md` `## Future Requirements`. Touching them violates this phase's additive-only blast
radius.

**The code review's two Warnings are out of scope.** They are pre-existing content this phase did not
introduce and `148-REVIEW.md` classifies them as such. They are not gaps.

**Re-fetch, never trust the report.** Live-page confirmation uses raw HTML bytes via `curl`, a tag-strip,
and a literal `grep`. Do not use a rendered-summary fetch tool — it has twice returned contradictory
answers for the same URL in this project.

**Match-count every string replacement.** Read the count before, read the count after, and fail loudly on
a delta of anything other than the expected one. A zero-match replacement reports success (D-56).

</phase_wide_rules>

<tasks>

<task type="tracer">
  <name>Task 1: Re-confirm the two quotes live, then insert the one Source line end-to-end</name>
  <files>docs/operations/patch-management/08-windows-app-updates.md</files>
  <precondition>`curl` can reach `learn.microsoft.com` over HTTPS; if the fetch fails or returns non-200, halt and report rather than shipping an unconfirmed citation.</precondition>
  <read_first>
    Read `docs/operations/patch-management/08-windows-app-updates.md` lines 90-120 in full before editing.
    At plan time the region measured: `:95` and `:96` a Source pair (overview page, then unification
    page), `:98-102` the comparison-table paragraph, `:104` a lone overview-page Source line, `:106-113`
    the cadence-conflict paragraph, `:114` blank, `:115` the
    `&lt;a id="setting-the-channel-from-intune"&gt;&lt;/a&gt;` anchor. Line numbers in a verification report can go
    stale — confirm the live positions yourself, anchor on the literal text `remain on Version 2508.` and
    not on the number 113.

    Also read `:96` byte-for-byte. It is already exactly the Source line this task must add; at plan time a
    `diff` between `:96` and the verifier's recommended remediation string reported IDENTICAL. Copy that
    line — do not compose a new one.
  </read_first>
  <action>
    First, confirm the evidence is still live, per D-62. Fetch
    `https://learn.microsoft.com/en-us/microsoft-365-apps/updates/unified-update-channels` as raw HTML
    bytes with `curl`, strip tags, and `grep` literally for the build-number discriminator string and for
    the escape-hatch date string quoted in the cadence-conflict paragraph. Both must return a non-zero hit
    count. Separately, extract that page's own rendered update date (or its `ms.date`) and compare it
    against the `(updated ...)` suffix already carried at `:96`.

    If the live date differs from the suffix at `:96`, do NOT silently give the new line a different date —
    two Source lines naming the same article with different dates in one file is a worse defect than the
    one being closed. Either move both lines together to the new date in the same commit, or halt and
    report the divergence. Record whichever you did in the SUMMARY.

    Then perform the insertion. Take the exact bytes of the existing Source line at `:96`. Insert a blank
    line followed by that line immediately after the physical line whose full content is
    `remain on Version 2508.`, so the resulting shape is: paragraph, blank, Source, blank, anchor — the
    same shape `:103`-`:105` already uses around the lone overview-page Source line.

    Per D-56, the file is LF in both index and worktree (`git ls-files --eol` reported `i/lf w/lf` at plan
    time) — re-confirm that before using any literal newline in a replacement string, and count matches
    before and after the replacement rather than trusting the tool's exit status.

    Per D-53, the inserted line must sit entirely on one physical line. `check-phase-54.mjs` strips
    `[text](url)` with a regex whose `.` does not cross a newline, so a link wrapped across two physical
    lines leaks its link text into body prose and turns the validator red. Do not reflow it to the file's
    ~95-column prose wrap; Source lines in this file are already long and unwrapped by convention.

    Per the V-54-11 URL-ring trap, do not assume a Learn URL is inert — link targets are not stripped by
    every validator. Here the check is trivially discharged by construction: the URL being added is the
    identical string already present at `:96`, so the file's URL token set is unchanged in content and only
    its multiplicity changes. Note that discharge explicitly in the SUMMARY rather than leaving it implied.

    Change nothing else. No prose edit, no reflow, no heading change, no frontmatter change.
  </action>
  <verify>
    <automated>cd /d/claude/Autopilot &amp;&amp; test "$(grep -c '^\*\*Source:\*\* \[Microsoft 365 Apps: SAEC and MEC unification\]' docs/operations/patch-management/08-windows-app-updates.md)" = "2" &amp;&amp; awk '/^remain on Version 2508\.$/{getline; getline nxt; print nxt; found=1} END{exit !found}' docs/operations/patch-management/08-windows-app-updates.md | grep -q 'unified-update-channels' &amp;&amp; test "$(grep -c '^## ' docs/operations/patch-management/08-windows-app-updates.md)" = "10" &amp;&amp; test "$(grep -c '^&lt;a id=' docs/operations/patch-management/08-windows-app-updates.md)" = "8" &amp;&amp; test "$(grep -c '^```' docs/operations/patch-management/08-windows-app-updates.md)" = "0" &amp;&amp; git ls-files --eol docs/operations/patch-management/08-windows-app-updates.md | grep -q 'w/lf' &amp;&amp; test "$(git diff --numstat -- docs/operations/patch-management/08-windows-app-updates.md | awk '{print $1"/"$2}')" = "2/0" &amp;&amp; echo TASK1_OK</automated>
  </verify>
  <done>
    The unification-article Source line count in the file is 2 (was 1). The line immediately following the
    blank line after `remain on Version 2508.` is that Source line. `git diff --numstat` on the file reports
    exactly 2 insertions and 0 deletions — the added Source line and its separating blank line, nothing
    else. H2 count 10, `&lt;a id=` count 8, code-fence count 0, `w/lf` all unchanged. Both quoted strings were
    confirmed present on the live unification page by raw-byte fetch, and the `(updated ...)` date matches
    the line at `:96` (or the divergence was handled and recorded).
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run the full gate battery with measured counts, then commit</name>
  <files>docs/operations/patch-management/08-windows-app-updates.md</files>
  <action>
    Re-run every gate that reads this file and record the actual number each one printed. Do not write
    "passes" or "should pass" anywhere in the SUMMARY — record counts, per this repo's MEASURED discipline.

    Run, from the repo root: `node scripts/validation/check-phase-53.mjs`,
    `node scripts/validation/check-phase-54.mjs`, `node scripts/validation/check-phase-59.mjs`,
    `node scripts/validation/check-nav-hub-links.mjs`, `node scripts/validation/c17-eee-contract.mjs`,
    `node scripts/validation/v1.20-milestone-audit.mjs`.

    Then run the apex `node scripts/validation/check-phase-144.mjs` as a SEPARATE invocation, after the
    others have finished. This repo's own lesson is to run the apex chain separately from the verifier: a
    URL-fragment change is a prose change and has tripped banned-phrase guards after an otherwise-passing
    run.

    The baselines to match, all measured at verification time: `check-phase-53` 26 passed / 0 failed /
    0 skipped; `check-phase-54` 32 / 0 / 0; `check-phase-59` 36 / 0 / 0; `check-nav-hub-links` 0
    hub-presence failures and 0 corpus-link failures; `c17-eee-contract` 234 files checked and 0 with
    violations — the file count must stay 234, it must not become 235; `v1.20-milestone-audit` 16 passed /
    0 failed; apex `check-phase-144` 101 PASS / 0 FAIL / 0 SKIPPED.

    Two things the counts specifically defend against. First, inserting a line can break a validator that
    pins a line number inside this file — at plan time a grep across `scripts/validation/*.mjs` for
    `app-updates` returned zero hits and D-68 records that `PATCH_FILES` in `check-phase-54.mjs:35` excludes
    `07` and `08` entirely, leaving only `V-54-27`, the `check-nav-hub-links` target-existence arm and C11
    via `v1.20-milestone-audit.mjs`'s live `walkMd('docs')` reading these files. Confirm that still holds
    rather than inheriting it. Second, `V-54-27` is a bare `> **Platform:**` negative across the whole
    corpus, which an added Source line structurally cannot trip — say so, do not just report the number.

    If any count regresses, do not paper over it. Diagnose it against the one added line, fix or revert, and
    record the finding.

    Commit only after every gate is at baseline. Use a single commit touching only this one file. Message:
    `docs(148-04): cite unification article for the cadence-conflict quote pair`.
  </action>
  <verify>
    <automated>cd /d/claude/Autopilot &amp;&amp; node scripts/validation/check-phase-53.mjs &amp;&amp; node scripts/validation/check-phase-54.mjs &amp;&amp; node scripts/validation/check-phase-59.mjs &amp;&amp; node scripts/validation/check-nav-hub-links.mjs &amp;&amp; node scripts/validation/c17-eee-contract.mjs &amp;&amp; node scripts/validation/v1.20-milestone-audit.mjs &amp;&amp; node scripts/validation/check-phase-144.mjs &amp;&amp; test "$(git show --stat --format= HEAD | grep -c '|')" = "1" &amp;&amp; git show --stat --format= HEAD | grep -q '08-windows-app-updates.md' &amp;&amp; echo TASK2_OK</automated>
  </verify>
  <done>
    All seven gates exit 0 and each printed count is recorded in the SUMMARY as a measured number matching
    its baseline: 26/0/0, 32/0/0, 36/0/0, 0+0, 234 files / 0 violations, 16/0, and apex 101/0/0. The apex ran
    as its own invocation after the others. `git show --stat HEAD` lists exactly one changed file, which is
    `docs/operations/patch-management/08-windows-app-updates.md`.
  </done>
  <reversibility rating="reversible">A single added documentation line in a git-tracked file; `git revert` restores the prior state exactly, and the added URL was already present in the same file.</reversibility>
</task>

</tasks>

<edge_case_disposition>

`workflow.specless_probe_fallback` produced 19 unresolved edge candidates across APP-01..APP-06. Disposition,
recorded explicitly rather than auto-dismissed:

**18 of 19 rows: NOT APPLICABLE to this plan's artifact — reasoned, not waved through.** Every one of those
rows is drawn from the probe engine's fixed software-runtime vocabulary — `concurrency` ("if interrupted or
run in parallel, what is guaranteed?"), `idempotency` ("what happens if this runs twice?"), `encoding`,
`empty`, `ordering`, `adjacency`. This plan's deliverable is one added line of static Markdown in a
documentation corpus. There is no execution, no input, no state and no second run for those categories to
predicate over. The categories that DO have real analogues in this corpus were already discharged as
must-have truths inside `148-02-PLAN.md` and re-verified by the phase verifier as prose properties of the
shipped guide — APP-03/concurrency became the multi-product one-channel-per-device rule, APP-03/idempotency
became the channel-change latency statement, APP-04/ordering became the eight limitations in source order,
APP-05/adjacency became the three-referents disambiguation, APP-06/idempotency became the
`SourceAutoUpdateInterval` disqualifier. They are satisfied at their proper altitude; re-predicating them
here would manufacture 18 checks for a citation insertion.

**1 of 19 rows: SURFACED, NOT RESOLVED.** The `APP-01 / unclassified` row ("unclassified — review manually")
carries no category the probe could assign and therefore no claim this plan can honestly discharge. APP-01 is
owned by `148-01`, is `✓ SATISFIED` on independent verification, and is outside this plan's blast radius. It
is recorded here as a flagged open assumption rather than marked resolved.

</edge_case_disposition>

<assumption_delta_decision>

**Outcome: `no-change`.** The deterministic scanner raised one `kind: "chosen"` signal on the term `choose`,
matched in the ROADMAP goal prose *"an admin can choose an app-update channel"*. That is a false positive:
the word describes an administrator selecting a channel at runtime in the product, not a derived value
becoming a chosen one in an architecture. This plan adds one citation line to a Markdown file and introduces
no value of either kind. Advisory checkpoint, non-blocking.

</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan adds one line of static Markdown to a public-facing documentation file. No code executes, no input is parsed, no data crosses a boundary, no dependency is installed. Stating that honestly is more useful than inventing a boundary that does not exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-148-04-01 | Information Disclosure | The added `**Source:**` line | low | accept | The line contains only a public Microsoft Learn article title, its public URL and a publication date. No credential, tenant identifier, hostname or internal path is introduced. The URL is already present verbatim elsewhere in the same file. |
| T-148-04-02 | Tampering | Package-manager installs | low | accept | No `npm`/`pip`/`cargo` install task exists in this plan, so no package-legitimacy gate applies. The plan runs only validators already committed to this repository. |

ASVS level 1; blocking threshold `high`. No threat in this plan reaches `medium` or above.
</threat_model>

<verification>
The gap is closed when a reader following any Source line in the `## Microsoft 365 Apps Update Channels`
cadence-conflict passage reaches a page that carries the quote they are checking. Concretely:

1. `grep -c` of the unification-article Source line in `08-windows-app-updates.md` returns 2, not 1.
2. The Source line immediately follows the paragraph ending `remain on Version 2508.`.
3. `git diff --numstat` on that file for this plan's commit is exactly `2 0` — two insertions, zero
   deletions.
4. Both quoted strings are confirmed present byte-for-byte on the live unification page by raw-byte fetch.
5. `git status --porcelain` shows no file outside `docs/operations/patch-management/08-windows-app-updates.md`
   modified — every phase prohibition still HELD.
6. All seven gates at their recorded baselines, apex run separately and last.
</verification>

<success_criteria>
- One added `**Source:**` line plus one blank line in `08-windows-app-updates.md`; no other byte in the
  repository changed.
- The added line is byte-identical to the existing line at `08:96` (or both lines carry the same updated
  date if the live page's date moved and both were updated together).
- Structural shape unchanged: 10 H2s, 8 `<a id=` anchors, 0 code fences, no `doc_id`, `w/lf`.
- Gate battery re-measured, counts recorded, apex 101 PASS / 0 FAIL / 0 SKIPPED.
- One commit, one file.
</success_criteria>

<output>
Create `.planning/phases/148-application-update-management-winget-routing/148-04-SUMMARY.md` when done.
Record: the raw-fetch hit counts for both quoted strings, the live page date compared against the `:96`
suffix, the before/after match counts for the string replacement, and every gate's printed number.
</output>
