---
phase: 153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a
plan: 03
type: execute
wave: 2
depends_on: ["153-01"]
files_modified:
  - scripts/validation/v1.20-milestone-audit.mjs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/milestones/v1.20-MILESTONE-AUDIT.md
  - .planning/milestones/v1.20-DEFERRED-CLEANUP.md
  - .planning/milestones/v1.20-phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
autonomous: false
requirements: [HARN-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The C17-bearing harness set is SIX, not five, by owner ruling dated 2026-08-29, and the widening is recorded as an inline success-criterion amendment across the FULL enumerated list of stale surfaces rather than on the requirement alone (D-08)."
    - "The success criterion the verifier actually scores is amended, not just the requirement text — an unamended criterion makes a six-harness conversion record a false result or fail its own criterion (D-08)."
    - "The sixth harness needs the entire corpus conversion, not only the C17 leg. [MEASURED] `v1.20-milestone-audit.mjs` imports the frozen-read library ZERO times while its five siblings import it twice each, so every one of its C1 through C16 corpus reads is live-HEAD (D-09)."
    - "Converting this harness is the EIGHTEENTH application of the frozen-aware pattern, not the nineteenth; nineteenth is the Path-A lineage ordinal for the new harness, a different sequence, and both ordinals survive into the prose (D-10)."
    - "The pre-conversion form of this harness is captured — commit and blob hash both recorded — BEFORE any edit lands, because the next plan's copy source must be that pre-conversion form (D-45)."
    - "The C17 leg is converted by the same working-directory swap the tracer proved, with the contract validator byte-unchanged and the existence guard live, and carries its own milestone-unique known-member guard (D-11, D-12, D-15, D-16)."
    - "The frozen carve-out check goes false-but-permanently-green as a consequence of this conversion; it is corrected additively in a successor validator and the frozen file is never edited (D-20)."
    - statement: "HARN-03 adjacency edge: the V119 and V120 enrolled counts are both 234 while the trees differ, so this harness's known-member path is proved present in the V120 tree and absent from the V119 tree by quoted tree listings."
      verification: explicit
    - statement: "HARN-03 empty edge (probe row, resolved here): the contract validator exits 0 on an empty corpus. The known-member assertion runs before the spawn and a non-zero materialized-count assertion runs alongside it."
      verification: explicit
    - statement: "HARN-03 ordering edge: the corpus conversion and the C17 leg conversion are separate tasks in one plan; the harness is run green after each, so neither result depends on the other having been done first."
      verification: explicit
  artifacts:
    - scripts/validation/v1.20-milestone-audit.mjs
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/PROJECT.md
    - .planning/milestones/v1.20-MILESTONE-AUDIT.md
    - .planning/milestones/v1.20-DEFERRED-CLEANUP.md
    - .planning/milestones/v1.20-phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-03-SUMMARY.md
  key_links:
    - "`check-phase-54.mjs` live-reads the requirements and roadmap documents and is an apex chain member, so amending either file is a live-gate edit, not a documentation edit."
    - "The pre-conversion blob of this harness is the ONLY correct copy source for the new lineage; once this plan commits, that form exists only in git history."
    - "The frozen carve-out check's assertion body is a bare positive name reference and survives this conversion; its name and detail string do not, which is the same defect class already sitting green for two milestones elsewhere in the validator tree."
  prohibitions:
    - "`scripts/validation/c17-eee-contract.mjs` must be byte-unchanged."
    - "`scripts/validation/check-phase-140.mjs` must be byte-unchanged — the successor validator is authored in the apex plan, and the frozen file is never edited."
    - "The two governance files the phase-139 validator live-reads must be neither edited nor moved by this plan."
    - "No worktree creation and no external archive or extraction binary."
    - "No `docs/` content file is created, edited or deleted."
    - "No requirement status row is flipped to Validated by this plan — the close gate is a single later commit and owns every flip."
    - "Neither glossary frontmatter date field is touched."
    - "No summary or commit message may claim a chain result gathered without the nested environment flag (D-75)."
    - "Commit message bodies must not literally quote governance-carve paths or the paths of amended files in prose (D-76)."
    - "No frozen validator line is edited before grepping the validator tree for that exact call-site string (D-79)."
    - "No bare-noun platform blockquote token at line start outside a fence in any `.planning/` file this plan writes (D-43)."
---

<objective>
Convert the sixth C17-bearing harness — which needs the whole corpus conversion, not just the C17
leg — and land the owner-ruled scope amendment that makes the harness set six across every surface
that still says five.

Purpose: the requirement text and the success criterion both say five. A six-harness conversion
against an unamended criterion either records a false result or fails its own criterion. And this
harness is genuinely different: it reads its entire corpus live-HEAD, so the C17 leg is the smaller
half of its job.

Output: an amended scope on seven surfaces, a fully frozen-aware sixth harness, and the pre-conversion
blob recorded so the next plan can copy the right thing.
</objective>

<scope_amendment>
**HARN-03's requirement text is STALE, and this plan is where that is recorded.**

`.planning/REQUIREMENTS.md` HARN-03 and `.planning/ROADMAP.md` Success Criterion 2 both say "all
five" C17-bearing harnesses (v1.15 through v1.19). `153-CONTEXT.md` D-08 is an OWNER RULING dated
2026-08-29 widening the set to **six** — v1.20's harness is added.

The context document wins. A downstream checker reading the requirements document alone will
otherwise score the sixth harness as out of scope, which is exactly why the amendment is written onto
the enumerated surfaces and not only onto the requirement. This mirrors Phase 144, which amended
requirement text and traceability cells together.

Reversibility of the amendment: **one-way**. The amended criterion is read by the verification pass
and by the milestone audit; once evidence is captured against it there is no clean revert.
</scope_amendment>

## Artifacts this phase produces

Created by Phase 153, absent at the phase base; treat as new, not as unacknowledged references.

- **New exports in `scripts/validation/_lib/frozen-at-close.mjs`:** `MILESTONE_CLOSE_SHAS.V120`,
  `readAtV120Close`, `lsTreeAtV120Close`, `materializeDocsAtClose`, `withDocsAtClose`.
- **New validators:** `scripts/validation/check-phase-145.mjs` through `check-phase-152.mjs` and
  `scripts/validation/check-phase-153.mjs`.
- **New check ids:** `V-145-*` through `V-152-*`; on the apex `V-153-AUDIT`, `V-153-CHAIN-<n>`,
  `V-153-AUDIT-HARNESS`, `V-153-SELF`, `V-153-C17CARVEOUT-SUCCESSOR`.
- **New harness lineage:** `scripts/validation/v1.21-milestone-audit.mjs`,
  `scripts/validation/v1.21-audit-allowlist.json`, `BASELINE_25` in
  `scripts/validation/regenerate-supervision-pins.mjs`.
- **New CI workflow:** `.github/workflows/audit-harness-v1.21-integrity.yml`.
- **New planning artifacts:** `.planning/milestones/v1.21-MILESTONE-AUDIT.md`,
  `.planning/milestones/v1.21-DEFERRED-CLEANUP.md`, this phase's summaries, `153-EVIDENCE.md`,
  `153-VERIFICATION.md`, and the gitignored `dist/docs-library-v1.21.zip`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-PATTERNS.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-01-SUMMARY.md
@scripts/validation/v1.20-milestone-audit.mjs
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Write the owner-ruled scope amendment onto the success criterion the verification pass scores, and onto the six other surfaces that still state five harnesses.</decision>
  <context>
The requirement text and the roadmap success criterion both state that five harnesses are converted.
The phase context document carries an owner ruling dated 2026-08-29 widening the set to six by adding
the predecessor milestone's own harness, measured at seven contract references against the other five
harnesses' eight, the difference being exactly one comment block.

This is a one-way step. The amended criterion is read by the verification pass and by the milestone
audit; once evidence is captured against it there is no clean revert, and a six-harness conversion
scored against an unamended criterion records a false result or fails its own criterion.

The ruling has already been made. This checkpoint exists so the irreversible edit is confirmed rather
than assumed, and so a change of mind is possible before seven documents are rewritten.
  </context>
  <options>
    <option id="amend-all-seven">
      <name>Amend all seven surfaces (the recorded ruling)</name>
      <pros>The criterion the verifier scores matches what the phase actually delivers; the traceability surfaces stay consistent with it; mirrors how the predecessor milestone handled the same shape.</pros>
      <cons>Irreversible once evidence is captured; rewrites text in two archived predecessor documents.</cons>
    </option>
    <option id="amend-criterion-only">
      <name>Amend only the roadmap success criterion</name>
      <pros>Smallest edit; the scored criterion is correct.</pros>
      <cons>Six other surfaces keep stating five, so the next reader finds a contradiction with no ruling attached — the exact failure the ruling names.</cons>
    </option>
    <option id="convert-five-only">
      <name>Do not amend; convert five harnesses and leave the sixth</name>
      <pros>No document edits; the requirement text stays literally true.</pros>
      <cons>Leaves the identical drift class on the one harness that reads its entire corpus live, which is the largest of the six; contradicts the recorded owner ruling.</cons>
    </option>
  </options>
  <resume-signal>Select: amend-all-seven, amend-criterion-only, or convert-five-only</resume-signal>
</task>

<task type="auto">
  <name>Task 2: The six-not-five scope amendment across all seven stale surfaces</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/milestones/v1.20-MILESTONE-AUDIT.md, .planning/milestones/v1.20-DEFERRED-CLEANUP.md, .planning/milestones/v1.20-phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md</files>
  <read_first>
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-08 in full, including its enumerated surface list and its measured hit counts (eight C17 references in each of v1.15 through v1.19, seven in v1.20, the delta being exactly the exception comment block)
    - .planning/REQUIREMENTS.md around line 107 — HARN-03's full text, and the "Validator Constraints on Requirement Text" table that follows the requirement list
    - .planning/ROADMAP.md around line 323 — Phase 153's Success Criterion 2, the criterion the verification pass scores
    - .planning/PROJECT.md lines 13, 26, 811 and 1003 — the four project-document occurrences; line 26 is Pillar H and carries the count twice
    - .planning/milestones/v1.20-MILESTONE-AUDIT.md around line 171 — the sentence naming the five-harness residue
    - .planning/milestones/v1.20-DEFERRED-CLEANUP.md lines 103-126 — the deferral entry: its heading, its scope paragraph, and the later sentence around line 124 that also carries the count
    - .planning/milestones/v1.20-phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md lines 60-70 — the predecessor context's statement of the same count
    - scripts/validation/check-phase-54.mjs lines 28-34 and 340-360 — the live reads of the requirements and roadmap documents and the negative literal assertion, so the amendment does not trip a live gate
  </read_first>
  <action>
Write the amendment onto seven surfaces. Each edit carries an inline marker of the form
`[SUCCESS-CRITERION AMENDMENT, D-08]` so a later reader sees the ruling rather than an unexplained
number change, and each states the measured ground: six harnesses carry a C17 spawn, five of them
with eight references each and the sixth with seven, the difference being the exception comment block
alone.

The seven surfaces:

1. `.planning/REQUIREMENTS.md` HARN-03 — widen the harness set to six and name the sixth. Keep every
   already-measured literal in the requirement intact; the amendment adds, it does not rewrite.
2. `.planning/ROADMAP.md` Success Criterion 2 for Phase 153 — this is the criterion the verification
   pass scores, and it is the single most important of the seven.
3. `.planning/PROJECT.md` line 13 — the milestone status paragraph's Pillar H clause.
4. `.planning/PROJECT.md` line 26 — Pillar H itself, which carries the count twice in one paragraph.
5. `.planning/PROJECT.md` lines 811 and 1003 — the milestone-history paragraphs.
6. `.planning/milestones/v1.20-MILESTONE-AUDIT.md` — the residue sentence.
7. `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` — the deferral entry's **heading** and both
   in-body occurrences of the count, and
   `.planning/milestones/v1.20-phases/144-.../144-CONTEXT.md` — the predecessor's statement.

Before editing the deferred-cleanup heading, grep the validator tree for that heading string: a
frozen validator can pin a heading verbatim. The phase-139 validator reads two governance files in
that directory and neither is one of these, but run the grep and record it rather than assuming.

Record the ordinal distinction explicitly wherever the prose invites confusion (D-10): converting
this harness is the **eighteenth** application of the frozen-aware pattern; **nineteenth** is the
Path-A lineage ordinal belonging to the new harness. Both ordinals are real and they count different
sequences.

Line endings differ across these files — assert every replacement matched.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-54.mjs` exits 0 after the edits, with no regression in its PASS count against the run taken before the edits. Both counts are quoted in the summary.
    - `grep -rn "SUCCESS-CRITERION AMENDMENT, D-08" .planning/` returns at least one hit in each of: the requirements document, the roadmap, the project document, the v1.20 milestone audit, the v1.20 deferred-cleanup document, and the phase-144 context document.
    - The roadmap's Phase 153 Success Criterion 2 states six harnesses and names v1.20 as the sixth.
    - The deferred-cleanup entry's heading no longer states a count of five.
    - The grep-before-edit result for the deferred-cleanup heading string across `scripts/validation/` is quoted in the summary; if any validator pins it, the edit is restructured rather than forced.
    - Both ordinals appear on the record with their distinct meanings — eighteenth for the conversion, nineteenth for the lineage.
    - No requirement status row is flipped by this task: `git diff .planning/REQUIREMENTS.md` shows no line changing from an unchecked to a checked box.
  </acceptance_criteria>
  <reversibility rating="one-way">The amended criterion is read by the verification pass and the milestone audit; once evidence is captured against it there is no clean revert. Owner-ruled 2026-08-29.</reversibility>
  <done>Seven surfaces state six harnesses with an inline amendment marker and the measured ground, both ordinals are distinguished on the record, and the live requirements-and-roadmap gate is still green.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the pre-conversion form, then convert every C1 through C16 corpus read to the frozen readers</name>
  <files>scripts/validation/v1.20-milestone-audit.mjs</files>
  <read_first>
    - scripts/validation/v1.20-milestone-audit.mjs — the whole file. It is the only harness with zero frozen-read imports; every corpus read in it resolves live against the working directory
    - scripts/validation/v1.19-milestone-audit.mjs — its import of the frozen-read library and the call shape its C1 through C16 checks use for the frozen corpus reader. This is the structural template
    - scripts/validation/_lib/frozen-at-close.mjs — `readAtV120Close` and `lsTreeAtV120Close` as added in plan 153-01, and the batched reader they delegate to
    - scripts/validation/check-phase-70.mjs around lines 390-420 — a chain validator already delegating to a frozen reader rather than inlining a git invocation, for the call shape
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-09, D-10, D-45
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-PATTERNS.md — the v1.20 harness section
  </read_first>
  <action>
**Capture before you edit — this is the single highest-value step in the plan (D-45).** Record, in
the summary and before any modification:

- the current commit object name, obtained from `git rev-parse HEAD`;
- the blob hash of the unmodified harness, obtained from
  `git hash-object scripts/validation/v1.20-milestone-audit.mjs`;
- the exact command the next plan must use to recover the pre-conversion source, spelled out with the
  recorded commit substituted in.

The next plan copies this harness forward as the new lineage's source. Copying the CONVERTED form
would make the new harness audit its predecessor's frozen corpus instead of its own live one — a
green, structurally wrong artifact that no gate in this repository detects.

**Then convert.** Import the frozen-read library and repoint every C1 through C16 corpus read from a
live working-directory read to `readAtV120Close` for file content and `lsTreeAtV120Close` for
enumeration, mirroring how the sibling harnesses and the chain validators already delegate. Do not
inline a git invocation anywhere; the readers already carry byte-exact slicing, line-ending
normalization and fail-loud semantics.

Scope discipline: this task converts corpus reads only. Checks that are correctly live questions —
does a validator file exist, does a script parse — stay live, exactly as the existence guard does in
the C17 leg. State in a comment which reads were deliberately left live and why.

Record the ordinal in the file header: this conversion is the eighteenth application of the pattern.

Run the harness and record its triple before and after.
  </action>
  <verify>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - The pre-conversion commit object name and the pre-conversion blob hash are both recorded verbatim in the summary, together with the literal recovery command the next plan must run.
    - `node scripts/validation/v1.20-milestone-audit.mjs --verbose` exits 0 and its triple is quoted; the pre-edit triple is quoted alongside it and the two are compared explicitly.
    - `grep -c "frozen-at-close" scripts/validation/v1.20-milestone-audit.mjs` is at least 1, where it was 0 at the phase base.
    - `grep -c "readAtV120Close\|lsTreeAtV120Close" scripts/validation/v1.20-milestone-audit.mjs` is greater than 1.
    - No inlined git subprocess invocation is introduced for corpus reading: the count of subprocess-module references in the file is unchanged from the phase base except where the C17 spawn already used one.
    - The file header states the eighteenth-application ordinal and distinguishes it from the lineage ordinal.
    - A comment names which reads were deliberately left live and why.
    - `git diff --stat` for this task lists exactly one file.
  </acceptance_criteria>
  <reversibility rating="costly">Once committed, the pre-conversion source exists only in git history; a copy made from the wrong form produces a green and structurally wrong harness that nothing detects.</reversibility>
  <done>Every corpus read in the sixth harness resolves at its own close SHA, the deliberately-live reads are named, and the pre-conversion commit and blob are on the record.</done>
</task>

<task type="auto">
  <name>Task 4: Convert the sixth C17 leg and prove the six-harness set green</name>
  <files>scripts/validation/v1.20-milestone-audit.mjs</files>
  <read_first>
    - scripts/validation/v1.20-milestone-audit.mjs — as converted in Task 2, and its C17 leg around lines 814-834. Note it carries no exception comment block, unlike v1.15
    - scripts/validation/v1.15-milestone-audit.mjs — the converted leg shape from the tracer
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-02-SUMMARY.md — the four derived known-member paths and the derivation command, for the V120 derivation
    - scripts/validation/check-phase-140.mjs lines 149-165 — the carve-out check. Run it; do not edit it
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-11, D-12, D-15, D-16, D-20
  </read_first>
  <action>
Apply the tracer's converted-leg shape to this harness's C17 leg with the V120 tag: absolutize the
contract path before the swap, keep the existence guard live, wrap the spawn in `withDocsAtClose`,
assert a V120-unique known-member path before the spawn, carry the non-zero materialized-count
assertion as the secondary guard, and leave the timeout, stdio mode, argument-array invocation and
failure-detail call shape untouched.

Derive the V120 known-member path the same way the four others were derived, and note the adjacency
hazard concretely: the V119 and V120 enrolled counts are both 234 while the trees differ, so the
chosen path must be proved present in the V120 tree and absent from the V119 tree by quoted tree
listings.

Record the carve-out consequence on the record (D-20): the frozen carve-out check's assertion body is
a bare positive name reference and survives all six conversions, but its name and its detail string
claim a scope of v1.15 through v1.18 and claim each still carries an exception comment — and the
tracer removed that comment. The check is now false-but-permanently-green. It is corrected additively
in a successor authored in the apex plan, and the frozen file is never edited. Note that this is the
same defect class already sitting green with a false detail string elsewhere in the validator tree.

Run all six harnesses in one sequence and record all six triples.
  </action>
  <verify>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs --verbose &amp;&amp; node scripts/validation/check-phase-140.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/v1.20-milestone-audit.mjs --verbose` exits 0 with its C17 line PASS and a detail naming the V120 tag.
    - The V120 known-member path is proved present in the V120 tree and absent from the V119 tree by quoted `git ls-tree` results — the count-identical adjacency case is disproved by path.
    - All six harnesses v1.15 through v1.20 are run in one sequence, all six exit 0, and all six triples are quoted in the summary.
    - `node scripts/validation/check-phase-140.mjs` exits 0 and its carve-out line still reports PASS.
    - `git diff 246fa3dd..HEAD -- scripts/validation/c17-eee-contract.mjs` is empty.
    - `git diff 246fa3dd..HEAD -- scripts/validation/check-phase-140.mjs` is empty.
    - `for f in scripts/validation/v1.1{5,6,7,8,9}-milestone-audit.mjs scripts/validation/v1.20-milestone-audit.mjs; do grep -c withDocsAtClose "$f"; done` returns 1 for each of the six files.
    - The carve-out consequence is recorded in the summary with its check id, the two claims that are now false, and the statement that the successor lands in the apex plan.
  </acceptance_criteria>
  <reversibility rating="reversible">One harness leg; revertible by file checkout.</reversibility>
  <done>All six C17-bearing harnesses spawn the byte-unchanged contract validator against their own frozen corpora, each with a milestone-unique guard, and the carve-out consequence is on the record with its successor named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents to live validator | The requirements and roadmap documents are read live by an apex chain member; amending them is a live-gate edit. |
| pre-conversion source to next plan | The correct copy source stops existing on disk the moment this plan commits. |
| frozen tree to temp directory | A sixth materialization writes git blobs outside the repository. |
| frozen validator prose to truth | A frozen check's name and detail string outlive their accuracy while the assertion body stays green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-11 | Repudiation | the pre-conversion copy source | high | mitigate | The commit object name, the blob hash and the literal recovery command are recorded in the summary before any edit lands, so the next plan recovers a verifiable artifact rather than reconstructing one. The blob hash makes the recovered file checkable against a fixed value. |
| T-153-12 | Tampering | the requirements and roadmap documents under a live gate | high | mitigate | The live gate is run before and after the amendment and both PASS counts are quoted; the amendment adds text and preserves every already-measured literal rather than rewriting. |
| T-153-13 | Information Disclosure | a sixth vacuously green C17 run | high | mitigate | The V120 known-member path is proved unique against the count-identical V119 neighbour by quoted tree listings, and runs before the spawn; a non-zero materialized-count assertion runs alongside it. |
| T-153-14 | Spoofing | a frozen check whose prose no longer matches its behaviour | medium | mitigate | The consequence is recorded with the check id and the two false claims named, the successor validator is scheduled in the apex plan, and the frozen file is asserted byte-unchanged against the predecessor close. |
| T-153-15 | Elevation of Privilege | subprocess invocation in the sixth leg | high | mitigate | Argument-array invocation only, contract path absolutized before the swap, no worktree, no external extraction binary; the corpus conversion introduces no new subprocess mechanism, which is asserted by an unchanged reference count. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs. Document edits, one harness edit, and Node validators using built-ins only. |

ASVS level: 1. Blocking threshold: high. All four high-severity entries are mitigated by computed
acceptance criteria — a recorded blob hash, a before-and-after gate count, quoted tree listings, and
a grep-proved invocation shape.
</threat_model>

<verification>
- All six harnesses v1.15 through v1.20 exit 0 with C17 PASS.
- `node scripts/validation/check-phase-54.mjs` exits 0 with no regression against its pre-edit count.
- `node scripts/validation/check-phase-140.mjs` exits 0.
- `git diff 246fa3dd..HEAD -- scripts/validation/c17-eee-contract.mjs` is empty.
- `git diff 246fa3dd..HEAD -- scripts/validation/check-phase-140.mjs` is empty.
- The pre-conversion commit and blob hash of the sixth harness are recorded.
</verification>

<success_criteria>
- Six harnesses are converted, not five, and every surface that stated five now states six with an
  inline amendment marker (HARN-03, SC#2 as amended).
- The sixth harness reads its entire corpus at its own close SHA, not only its C17 leg (D-09).
- The pre-conversion source is recoverable by a recorded command and checkable against a recorded
  blob hash (D-45).
- The carve-out consequence is named with its successor scheduled (D-20).
</success_criteria>

<output>
Create `.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-03-SUMMARY.md` when done.

Record: the seven amended surfaces with their marker lines; the pre-conversion commit, blob hash and
recovery command; the before-and-after triples for this harness; all six harness triples; the V120
known-member path with its uniqueness proof; and the carve-out consequence with its successor named.
</output>
