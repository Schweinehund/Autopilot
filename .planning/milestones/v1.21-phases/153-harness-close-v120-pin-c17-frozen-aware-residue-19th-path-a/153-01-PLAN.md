---
phase: 153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/_lib/frozen-at-close.mjs
  - .planning/PROJECT.md
  - scripts/validation/v1.15-milestone-audit.mjs
autonomous: true
requirements: [HARN-01, HARN-02, HARN-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `V120` close-SHA entry is INSERTED BEFORE the `V14` entry inside the `MILESTONE_CLOSE_SHAS` object literal, never appended — appending fails `V-140-V14PIN`, which sits inside the apex chain (D-01, HARN-01)."
    - "`readAtV120Close` and `lsTreeAtV120Close` follow the V18..V119 single-entry one-line thin-wrapper pattern, appended to the existing export blocks — append is correct for the exports and wrong only for the pin (D-03, HARN-02)."
    - "The pin is proven end to end by a real `readAtV120Close` call against a known path at the v1.20 close SHA, with the returned bytes asserted non-empty — a separate obligation from the placement (D-04, SC#1)."
    - "The subject-line pair discriminator is re-run and recorded against V117, V118 and V119 before the V120 result is trusted; no v1.20 false positive is claimed, because none occurred (D-05, HARN-02)."
    - "D-31's APPEND-ONLY ruling on `_lib/frozen-at-close.mjs` is explicitly amended, once, for this phase, in writing — the phase needs two mid-file edits and the amendment is named rather than silently taken (D-06)."
    - "`V-120-HYG01` and `V-73-LIB-EXISTS` are both green before and after every edit to the frozen library (D-07)."
    - "`PROJECT.md` Pillar G's falsified `Append` instruction is corrected in the same commit as the pin — correcting the requirement while leaving the higher-authority project document is not a fix (D-02)."
    - "The conversion mechanism is a working-directory swap, not a code change: the frozen `docs/` tree is materialized into a temp directory and the live contract is spawned with that directory as its cwd (D-11)."
    - "Only `docs/**` is materialized — in normal mode the contract validator reads nothing else, and the fixture reads are gated behind the self-test flag the harness never passes (D-12)."
    - "The helper is `withDocsAtClose(tag, fn)`, a callback that owns creation and deletion in one place, built on the existing tree-listing and batched-read primitives; a path-returning helper would push cleanup into all six call sites (D-13)."
    - "The contract path is absolutized BEFORE the spawn — a relative path resolved against the swapped cwd dies with module-not-found, which the catch converts into a red that reads as a corpus violation (D-15)."
    - "The anti-vacuous-green guard is a milestone-unique known-member path, not a file count — enrolled counts collide across milestones while the trees genuinely differ, so a count cannot distinguish a harness materializing its own corpus from one materializing its neighbour's (D-16)."
    - "Temp-directory creation and removal live inside the helper, in a `finally`, with removal carrying retry options — both cited precedents use a bare non-retrying removal and Windows is this repository's primary authoring platform (D-18)."
    - "The module's first write verbs are recorded in its header: a documented read-only frozen-read module becomes one that writes to disk (D-19)."
    - "The materialized set, not just the count, is a silent contract with a byte-frozen validator; the coupling is recorded in the helper header so a future milestone's contract reading outside `docs/` cannot take six harnesses vacuously green at once (D-21)."
    - statement: "HARN-02 boundary edge: the pin lands strictly earlier in the object body than the `V14` key, and the object's last key is still `V14` after the edit. The boundary is asserted by re-running the pin check, not by eyeballing the diff."
      verification: explicit
    - statement: "HARN-02 precision edge: the recovered close SHA is the full 40-character object name, the pinned literal is its 8-character prefix, and the frozen read is proven against the pinned prefix rather than the full SHA — so an abbreviation-length mismatch cannot pass silently."
      verification: explicit
    - statement: "HARN-03 empty edge (probe row, resolved here): the contract validator exits 0 on an empty corpus, so an empty or wrong-milestone materialization is a silent green. The known-member assertion runs BEFORE the spawn and a non-zero materialized-file assertion runs alongside it."
      verification: explicit
    - statement: "HARN-01 unclassified edge (probe row remains unresolved and is surfaced, never dismissed): the deterministic probe could not classify HARN-01's edge family. Flagged planner assumption — the pin's failure modes are operationalized as placement, abbreviation length, and reachability of the object at that SHA; there may be a fourth failure mode no gate covers. Residual risk recorded, not dismissed."
      verification: backstop
  artifacts:
    - scripts/validation/_lib/frozen-at-close.mjs
    - .planning/PROJECT.md
    - scripts/validation/v1.15-milestone-audit.mjs
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-01-SUMMARY.md
  key_links:
    - "`V-140-V14PIN` regex-tests that no V-tag key follows the `V14` literal inside the object body; the pin's position is therefore load-bearing on an apex chain member, not a style choice."
    - "The tree-listing and batched-read primitives already normalize line endings and carry fail-loud semantics, so the materialize helper needs no new git plumbing — only the temp-directory wrapper."
    - "The contract validator resolves every read through the process working directory, which is exactly what makes the cwd swap sufficient without editing it."
  prohibitions:
    - "`scripts/validation/c17-eee-contract.mjs` must be byte-unchanged from the phase base. This plan reads it and never edits it."
    - "No temp-directory materialization via `git worktree add` — it mutates the shared repository's worktree metadata and applies the line-ending conversion this repository has configured on."
    - "No temp-directory materialization via an archive piped through an external extraction binary — the validator suite's stated convention is Node built-ins only."
    - "The existence guard for the contract validator stays live against the real repository root; only the data the spawned validator reads is materialized at the frozen tree."
    - "No `docs/` content file is created, edited or deleted by this phase in either direction."
    - "Neither glossary frontmatter date field is touched anywhere in this phase. [MEASURED 2026-08-29] all three glossaries sit at exactly 90 days between the two stamps against a strictly-greater-than-90 test — the margin is zero and any edit to either field flips it."
    - "No summary, evidence or commit message produced by this plan may claim a chain result gathered without the nested environment flag; the apex sets that flag on every child and structurally cannot produce one (D-75)."
    - "Commit message bodies must not literally quote governance-carve paths or the paths of amended files in prose — the exclusion check greps the message body and the file list together (D-76)."
    - "No line of a frozen validator is edited before grepping the validator tree for that exact call-site string; a later check-phase can pin an earlier one's call site verbatim (D-79)."
    - "No bare-noun platform blockquote token at line start outside a fence in any `.planning/` file this plan writes, including its own summary — the corpus-wide negative in `check-phase-54.mjs` walks the whole planning tree, not only `docs/` (D-43)."
---

<objective>
Land the `V120` back-anchor pin, build the `withDocsAtClose` materialize helper, and prove the whole
mechanism end to end by converting the first C17-bearing harness and running it green against its own
frozen corpus.

Purpose: this is the phase tracer. Every other plan in Phase 153 consumes the pin, the helper, or
both. Proving the working-directory swap on one harness before replicating it five more times is the
difference between finding an architectural dead end after one commit and finding it after six.

Output: a pinned and export-complete frozen-read library, a corrected project document, a materialize
helper with retry-safe cleanup, and one converted harness exiting 0 against its frozen corpus.
</objective>

<tracer_note>
**Task 3 is the tracer slice.** Tasks 1 and 2 are its two mechanical preconditions and cannot be
lifted into an earlier plan — the pin must exist before the helper can resolve a SHA, and the helper
must exist before the harness leg can swap its working directory. The tracer property this plan
carries is at plan granularity: Plan 153-01 is wave 1, it wires one path through every layer this
phase touches (git object store → close-SHA map → batched reader → temp materialization → live
subprocess spawn → frozen-corpus assertion), and no expansion plan runs until Task 3's `<verify>` is
green. This is production code for keeps, not a prototype: the helper Task 2 writes is the same helper
all six harnesses use.
</tracer_note>

<deviation_note>
**On D-09's "two plans, not one".** D-09 rules the conversion into two plans because the work is 5:1
asymmetric — five harnesses need one change each, the sixth needs a full corpus conversion as well.
That asymmetry is preserved exactly: `v1.20-milestone-audit.mjs` gets its own dedicated plan (153-03)
and is never fused with the identical-leg work. What this plan does additionally is carve **one** of
the five identical legs out as the tracer, because TRACER_MODE requires the architecture be proven
end to end before it is replicated. The remaining four land in 153-02, in wave 2, immediately after.
D-08's simultaneity requirement is unaffected: all six conversions land before any close evidence is
captured (153-10), which is what "simultaneously" governs.
</deviation_note>

## Artifacts this phase produces

Every symbol below is CREATED by Phase 153 and does not exist at the phase base. Source-grounding and
drift verification must treat these as new, not as unacknowledged references.

- **New exports in `scripts/validation/_lib/frozen-at-close.mjs`:** `MILESTONE_CLOSE_SHAS.V120`,
  `readAtV120Close`, `lsTreeAtV120Close`, `materializeDocsAtClose`, `withDocsAtClose`.
- **New validator files:** `scripts/validation/check-phase-145.mjs` through `check-phase-152.mjs`
  (eight content leaves) and `scripts/validation/check-phase-153.mjs` (the apex).
- **New check ids:** `V-145-*` through `V-152-*` (each leaf's needles plus its `SELF` invariant); on
  the apex `V-153-AUDIT`, `V-153-CHAIN-<n>`, `V-153-AUDIT-HARNESS`, `V-153-SELF` and
  `V-153-C17CARVEOUT-SUCCESSOR`.
- **New harness lineage:** `scripts/validation/v1.21-milestone-audit.mjs`,
  `scripts/validation/v1.21-audit-allowlist.json`, and the `BASELINE_25` block appended to
  `scripts/validation/regenerate-supervision-pins.mjs`.
- **New CI workflow:** `.github/workflows/audit-harness-v1.21-integrity.yml` with jobs `parse`,
  `path-match`, `harness-run`, `linux-chain-ubuntu-latest`, `check-phase-145` through
  `check-phase-152`, `check-phase-153`, `rotting-external-quarterly`, `pin-helper-advisory`,
  `frozen-read-probe`.
- **New planning artifacts:** `.planning/milestones/v1.21-MILESTONE-AUDIT.md`,
  `.planning/milestones/v1.21-DEFERRED-CLEANUP.md`, this phase's `153-*-SUMMARY.md`,
  `153-EVIDENCE.md` and `153-VERIFICATION.md`, and the gitignored `dist/docs-library-v1.21.zip`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-PATTERNS.md
@scripts/validation/_lib/frozen-at-close.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: The V120 pin — recover, regression-check, insert before V14, export, correct the project document, prove by a real frozen read</name>
  <files>scripts/validation/_lib/frozen-at-close.mjs, .planning/PROJECT.md</files>
  <read_first>
    - scripts/validation/_lib/frozen-at-close.mjs — the whole file. Specifically: the module header comment block (lines 1-24, which documents the module as read-only and which Task 2 amends), the import block at lines 26-31, the `MILESTONE_CLOSE_SHAS` object literal opening at line 69, the `V119` entry at line 135 with its deferral comment at lines 133 and 145, the `V14` entry at line 147, the convenience-reader export block containing `readAtV119Close` at line 219, and the tree-listing export block containing `lsTreeAtV119Close` at line 287
    - scripts/validation/check-phase-140.mjs lines 118-148 — `V-140-V14PIN` in full. It asserts the literal pin string, the `AUDIT-CLOSE PIN` rationale text, and then regex-tests the object body slice after the `V14` literal for any further V-tag key. Read the regex before choosing an insertion point
    - scripts/validation/check-phase-120.mjs lines 102-113 — `V-120-HYG01`, which asserts one literal is ABSENT from the frozen library and another is PRESENT. Any comment text added by this task or Task 2 is constrained by it
    - scripts/validation/check-phase-73.mjs line 94 — `V-73-LIB-EXISTS`
    - .planning/PROJECT.md line 25 — Pillar G in full, including its falsified instruction word
    - .planning/REQUIREMENTS.md lines 105-106 — HARN-01 and HARN-02
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-01 through D-07, D-79
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-PATTERNS.md — the `frozen-at-close.mjs` section, which carries the exact predecessor lines to insert above and the exact export shape to copy
  </read_first>
  <action>
Recover the SHA first, then edit.

**Recovery and regression check (D-05).** Run the subject-line pair discriminator — a `git log --all`
with a `%H|%s` format piped through an `awk` that requires BOTH the version token and the milestone
close token to be present in the **subject field only**. Run it four times: for v1.17, v1.18, v1.19
and v1.20. Each must return exactly one row, and the v1.17/v1.18/v1.19 rows must equal the SHAs
already pinned in the file for `V117`, `V118` and `V119`. Record all four rows verbatim in the
summary. Do not use the dual-token `--grep --all-match` form; it matches the commit body. Record as a
correction of record that no v1.20 false positive occurred and none is claimed.

**The pin (D-01).** Add `V120: '246fa3dd',` with a rationale comment naming the v1.20 close-gate
commit and the phase and plan that produced it, following the shape of the existing `V119` entry.
Place it **strictly before** the `V14` entry in the object body. The natural position is immediately
after the `V119` entry and its deferral comment. Amend the existing V120-deferral comment text
(currently stating the V120 pin is deferred) so the file does not carry a deferral note for a pin it
now holds, and replace it with the successor back-anchor note that `V121` defers to v1.22 — the
back-anchor invariant is that a pin always references a PAST close SHA. Keep the `AUDIT-CLOSE PIN`
rationale literal on the `V14` entry untouched.

**The exports (D-03).** Append `readAtV120Close` and `lsTreeAtV120Close` as one-line thin wrappers
delegating to `readAtClose('V120', …)` and `lsTreeAtClose('V120', …)`, in the same column-aligned
shape as the `V118`/`V119` pairs, at the end of each of the two existing export blocks. Append is
correct here; only the pin is insert-before.

**The amendment on the record (D-06).** Phase 144's D-31 ratified this module APPEND-ONLY with one
carved exception, and Phase 144 spent that exception. Write a one-time amendment note into the module
header stating that Phase 153 takes two mid-file edits — the insert-before-`V14` pin and the
`withDocsAtClose` helper Task 2 adds — and that the amendment is scoped to this phase. Name it; do
not take it silently.

**The project document (D-02).** `.planning/PROJECT.md` line 25 instructs an executor to APPEND the
pin. That is falsified by the requirement and by the check that guards the object. Correct the
instruction word to state insert-before-`V14`, and state why in the same sentence. Do this in the same
commit as the pin.

**The proof (D-04).** After the edits, call `readAtV120Close` on a path known to exist in the corpus
at the v1.20 close and assert the returned string is non-empty; then call `lsTreeAtV120Close('docs')`
and assert the array is non-empty. Do this through a real Node import of the module, not by
re-implementing the read.

Grep the validator tree for the exact strings on the lines you are about to change before changing
them; a later check-phase can pin an earlier one's call site verbatim.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-140.mjs &amp;&amp; node scripts/validation/check-phase-120.mjs &amp;&amp; node scripts/validation/check-phase-73.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The discriminator returns exactly one row for each of v1.17, v1.18, v1.19 and v1.20; the v1.20 row's object name begins `246fa3dd` and the other three equal the SHAs already pinned as `V117`, `V118` and `V119`. All four rows are quoted in the summary.
    - `node -e "import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>{const b=m.MILESTONE_CLOSE_SHAS; const k=Object.keys(b); if(b.V120!=='246fa3dd') process.exit(1); if(k[k.length-1]!=='V14') process.exit(1); if(k.indexOf('V120')>=k.indexOf('V14')) process.exit(1); console.log('OK')})"` prints `OK` and exits 0.
    - `node scripts/validation/check-phase-140.mjs` exits 0 and its `V14PIN` line reports PASS.
    - `node scripts/validation/check-phase-120.mjs` exits 0 and its `HYG01` line reports PASS.
    - `node scripts/validation/check-phase-73.mjs` exits 0.
    - `node -e "import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>{const c=m.readAtV120Close('docs/index.md'); const t=m.lsTreeAtV120Close('docs'); if(!c||c.length===0) process.exit(1); if(!Array.isArray(t)||t.length===0) process.exit(1); console.log('frozen read OK: '+c.length+' bytes, '+t.length+' tree entries')})"` prints a non-zero byte count and a non-zero entry count, and exits 0.
    - `git show 246fa3dd --format=%s --no-patch` prints a subject line containing both the version token and the milestone close token.
    - `.planning/PROJECT.md` line 25 no longer instructs appending the pin and states the insert-before position with its reason.
    - The module header carries the one-time append-only amendment note naming both mid-file edits.
    - `git diff --stat` for this task lists exactly two files.
  </acceptance_criteria>
  <reversibility rating="costly">A wrong pin placement takes every apex run red until corrected, and once Area A lands the pin is read by six harnesses.</reversibility>
  <done>The `V120` pin sits before `V14`, both convenience exports resolve, a real frozen read returns bytes, both live guards on the file are green, and the project document no longer carries the falsified instruction.</done>
</task>

<task type="auto">
  <name>Task 2: `withDocsAtClose` — the materialize helper, retry-safe cleanup, and the module's first write verbs</name>
  <files>scripts/validation/_lib/frozen-at-close.mjs</files>
  <read_first>
    - scripts/validation/_lib/frozen-at-close.mjs — as edited by Task 1. Specifically `lsTreeAtClose` starting at line 244 (its signature, its `-z` name-only invocation, its error enrichment), `readManyAtClose` starting at line 301 (its batched `git cat-file --batch` input construction, its byte-exact slicing, and its line-ending normalization at line 334), the fail-loud semantics at lines 233-236, the frozen cause classifier and how the three existing readers enrich a caught error, the bare non-retrying temp removal at line 461, and the import block at lines 26-31
    - scripts/validation/frozen-read-negative-test.mjs around line 290 — the second bare non-retrying removal, cited as a precedent and explicitly rejected as insufficient
    - scripts/validation/c17-eee-contract.mjs lines 15-17, 60-74, 420-450, 520-550 and 580-590 — the built-ins-only import convention, the two working-directory-relative read helpers, the self-test gate around the two fixture reads, the empty-corpus early exit, and the summary line. Read to confirm what must be materialized and what must not
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-11 through D-21
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-PATTERNS.md — the "No Analog Found" row for this helper, and the Shared Patterns section on the cause classifier
  </read_first>
  <action>
Export two functions from `scripts/validation/_lib/frozen-at-close.mjs`.

`materializeDocsAtClose(tag, destDir)` — enumerate the frozen tree with `lsTreeAtClose(tag, 'docs')`,
batch-read every enumerated path with `readManyAtClose(tag, paths)`, create each path's parent
directory under `destDir` with a recursive directory creation, and write each file's content. Return
the array of relative paths written so callers can assert on it. Materialize `docs/**` and nothing
else (D-12). Route any git-object read failure through the same cause-classifier enrichment the three
existing readers use, so a materialize failure reads the same way as a frozen-read failure.

`withDocsAtClose(tag, fn)` — a callback wrapper (D-13), never a path-returning function. Create a
temp directory under the OS temp root with a milestone-tagged prefix, call `materializeDocsAtClose`
into it, call `fn(tmpDir, writtenPaths)`, and remove the temp directory in a `finally`. The removal
must be recursive, forced, and carry retry options — both existing precedents in this repository use
a bare non-retrying removal, which is EPERM/EBUSY-flaky on Windows immediately after writing a few
hundred files, and Windows is the primary authoring platform (D-18). The realistic collision is
several harnesses spawned concurrently on one machine by a local apex run, not separate CI runners.
Return whatever `fn` returns.

Both functions use Node built-ins only and argument-array subprocess invocation — no shell string, no
external archive or extraction binary, and no worktree creation (D-14).

Extend the import at line 27 with the write verbs the module now needs. This is the module's first
write verb (D-19): amend the header comment block to state that the module is no longer read-only,
naming the two new exports and the fact that they create and remove a temp directory. Keep that
amendment inside the constraint `V-120-HYG01` places on this file's comment text — read the check
before writing the comment.

Add one more line to the header recording the coupling D-21 names: the materialized set is a silent
contract with a byte-frozen validator, so if a future milestone's contract validator reads a path
outside `docs/`, every harness using this helper goes vacuously green at once. State the coupling and
name where the residue is tracked.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>{const n=m.withDocsAtClose('V120',(d,p)=>{if(!fs.existsSync(d+'/docs'))throw new Error('no docs dir under tmp');return p.length;});if(!n||n<100)process.exit(1);console.log('materialized '+n+' files');}).catch(e=>{console.error(e);process.exit(1);})"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints a materialized file count of at least 100 and exits 0.
    - `node -e "import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>{if(typeof m.withDocsAtClose!=='function'||typeof m.materializeDocsAtClose!=='function')process.exit(1);console.log('OK')})"` prints `OK`.
    - After the verify command completes, the temp directory it created no longer exists: capture the directory path inside the callback, print it, and assert with a follow-up existence check that it is gone.
    - `grep -n "maxRetries" scripts/validation/_lib/frozen-at-close.mjs` returns at least one line inside the new helper's cleanup block.
    - `grep -c "child_process" scripts/validation/_lib/frozen-at-close.mjs` is unchanged from the phase base — the helper adds no new subprocess mechanism beyond the two existing readers it composes.
    - `grep -v '^ *//' scripts/validation/_lib/frozen-at-close.mjs | grep -c "worktree"` returns 0.
    - `git diff --stat` for this task lists exactly one file.
    - `node scripts/validation/check-phase-120.mjs` exits 0 — the header amendment did not violate the comment-text guard on this file.
    - `node scripts/validation/check-phase-140.mjs` exits 0 — the mid-file helper insertion did not disturb the pin invariant.
    - The module header states that the module now writes to disk, names both new exports, and records the materialized-set coupling with a byte-frozen validator.
  </acceptance_criteria>
  <reversibility rating="reversible">A helper added to a library with no call sites yet; removable without touching any consumer.</reversibility>
  <done>`withDocsAtClose` materializes the frozen corpus into a temp directory, hands it to a callback, and removes it with retry-safe cleanup; both guards on the library are still green and the header records the character change.</done>
</task>

<task type="tracer">
  <name>Task 3: TRACER — convert the v1.15 C17 leg end to end and prove it green against its own frozen corpus</name>
  <files>scripts/validation/v1.15-milestone-audit.mjs</files>
  <read_first>
    - scripts/validation/v1.15-milestone-audit.mjs lines 810-850 — the C17 leg in full: the contract path constant, the exception comment block, the live existence guard, the subprocess spawn with its timeout and working directory, the success detail string, and the catch that routes through the shared failure-detail helper
    - scripts/validation/_lib/frozen-at-close.mjs — the `withDocsAtClose` helper as written in Task 2, and the import line the harness already uses for this module
    - scripts/validation/c17-eee-contract.mjs — the two working-directory-relative read helpers, the empty-corpus early exit, and the summary line. Confirm what the child process will and will not read
    - scripts/validation/check-phase-140.mjs lines 149-165 — the carve-out check whose assertion body survives this edit but whose name and detail string do not. Do not edit it; the successor is authored in the apex plan
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-PATTERNS.md — the v1.15 leg excerpt with its exact current form, and the Shared Patterns entry on the failure-detail call shape
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-11, D-12, D-15, D-16, D-17, D-20, D-79
  </read_first>
  <action>
Convert exactly one leg, run it for real, and stop.

Rewrite the C17 leg's runner in `scripts/validation/v1.15-milestone-audit.mjs` so that:

1. The contract path is **absolutized before the spawn** using the live repository root, not left as a
   relative string (D-15). Compute it once, before the working directory is swapped.
2. The existence guard stays against the **live** repository root and keeps its current failure
   string (D-11) — whether the validator exists is correctly a live question.
3. The spawn is wrapped in `withDocsAtClose('V115', …)` and the child's working directory is the
   temp directory the callback receives (D-11). Nothing else about the spawn changes: same timeout,
   same piped stdio, same argument-array invocation.
4. **Before** the spawn, inside the callback, assert a **milestone-unique known-member path** exists
   under the materialized tree (D-16). Derive that path by diffing the frozen documentation tree at
   the v1.15 close against its immediate neighbours' trees and choosing a path present in v1.15's and
   absent from both — a count cannot do this job because enrolled counts collide across milestones
   while the trees genuinely differ. Carry a secondary assertion that the materialized file count is
   non-zero: it is the cheap catch for the empty-materialize case, and it is explicitly not the
   primary guard.
5. If any count is parsed from the child's standard output at all, pin the exact summary-line wording
   it parses and state in a comment that the wording is unpinned upstream (D-17). Prefer not to parse
   a count.
6. Remove the stale exception comment block that declares this leg intentionally live-HEAD — it is
   now false. Record in its place a short comment naming the conversion, the milestone tag, and the
   known-member guard.
7. Keep the success and failure detail strings routed through the same shared failure-detail helper
   with the same call shape the file already uses.

Then run the harness for real and record the C17 leg's result and the harness's overall triple.

Grep the validator tree for the exact call-site strings on the lines you change before changing them.
The carve-out check in `check-phase-140.mjs` asserts only that this file references the contract by
name, which survives — but run it and confirm rather than assuming.
  </action>
  <verify>
    <automated>node scripts/validation/v1.15-milestone-audit.mjs --verbose &amp;&amp; node scripts/validation/check-phase-140.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/v1.15-milestone-audit.mjs --verbose` exits 0 and its C17 line reports PASS.
    - The C17 line's detail names the frozen milestone tag, so the evidence distinguishes a frozen run from a live one.
    - `node scripts/validation/check-phase-140.mjs` exits 0 and its carve-out line still reports PASS.
    - `git diff --stat` for this task lists exactly one file, and `git diff scripts/validation/c17-eee-contract.mjs` is empty.
    - `git diff --numstat 246fa3dd..HEAD -- scripts/validation/c17-eee-contract.mjs` is empty — the contract validator is byte-unchanged against the v1.20 close as well as against the working tree.
    - A deliberate negative probe is run and recorded: temporarily point the wrapper at a different milestone tag, confirm the known-member assertion FAILS, then revert. The probe proves the guard is failable rather than decorative. Record both the failing output and the reverted green run in the summary.
    - The exception-comment block that declared this leg intentionally live-HEAD is gone: a grep for its marker token over the file with comment lines retained returns 0.
    - The absolutized contract path is computed before the callback opens, not inside it.
  </acceptance_criteria>
  <reversibility rating="reversible">One harness leg; revertible with a single file checkout, and nothing consumes it until the evidence plan.</reversibility>
  <done>The v1.15 harness spawns the byte-unchanged contract validator against its own frozen corpus in a temp directory, the known-member guard is proved failable by a negative probe, the harness exits 0, and the carve-out check is still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git object store to temp directory | Frozen blobs are written to a filesystem location outside the repository. Path components come from a git tree listing; a path escaping the temp root would write outside it. |
| harness process to spawned contract validator | A child process is launched with a swapped working directory and an interpolated script path. |
| milestone tag to git command line | The tag selects a SHA that is interpolated into git subprocess arguments. |
| temp directory lifetime to concurrent processes | Several harnesses can materialize concurrently on one machine under a local apex run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-01 | Tampering | `materializeDocsAtClose` path join | high | mitigate | Every written path comes from a git tree listing under an explicit documentation prefix, is resolved against the temp root, and is asserted to remain inside it before any write. A path resolving outside the temp root aborts the materialization rather than writing. |
| T-153-02 | Elevation of Privilege | subprocess invocation in the helper and in the converted C17 leg | high | mitigate | Argument-array `execFileSync` only, no shell string, no external extraction binary, no worktree creation. The contract path is absolutized from the live repository root before the working directory is swapped, so the child never resolves its own script through the materialized tree. |
| T-153-03 | Spoofing | milestone tag interpolated into git arguments | medium | mitigate | The tag is looked up in the close-SHA map and throws when absent — the existing readers already do this — so only a known key ever reaches the argument array. Tags in this plan are literals. |
| T-153-04 | Denial of Service | temp-directory cleanup on Windows | medium | mitigate | Removal is recursive, forced and retry-carrying, inside a `finally`, so a transient file lock does not leak a directory or abort the harness. Cleanup is asserted by a post-run existence check. |
| T-153-05 | Information Disclosure | a vacuously green C17 run | high | mitigate | The empty-corpus early exit in the contract validator makes an empty or wrong-milestone materialization exit 0 silently. The known-member assertion runs before the spawn and is proved failable by a deliberate negative probe; a non-zero file-count assertion runs alongside it as the cheap empty-case catch. |
| T-153-06 | Repudiation | the pin's provenance | medium | mitigate | The close SHA is recovered by a subject-line discriminator regression-checked against three already-pinned milestones, all four rows are quoted verbatim in the summary, and the pin is proved by a real frozen read rather than by inspection. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | This plan performs zero package-manager installs. It edits three files and runs Node validators that use built-ins only. No package is introduced, so no legitimacy audit applies. |

ASVS level: 1. Blocking threshold: high. All four high-severity entries are mitigated by computed
acceptance criteria — a containment assertion, an argument-array constraint proved by grep, and a
negative probe that must fail before it is allowed to pass.
</threat_model>

<verification>
- `node scripts/validation/check-phase-140.mjs` exits 0 with its pin and carve-out lines PASS.
- `node scripts/validation/check-phase-120.mjs` exits 0.
- `node scripts/validation/check-phase-73.mjs` exits 0.
- `node scripts/validation/v1.15-milestone-audit.mjs --verbose` exits 0 with C17 PASS.
- A real `readAtV120Close` call returns non-empty bytes and `lsTreeAtV120Close('docs')` returns a
  non-empty array.
- `git diff 246fa3dd..HEAD -- scripts/validation/c17-eee-contract.mjs` is empty.
- Exactly three files are modified by this plan.
</verification>

<success_criteria>
- The `V120` pin sits before `V14`, both convenience exports resolve, and the pin is proved by a real
  frozen read (SC#1, HARN-01, HARN-02).
- `withDocsAtClose` exists, materializes only the documentation tree, and cleans up with retry-safe
  removal.
- The first C17-bearing harness runs green against its own frozen corpus with a known-member guard
  proved failable by a negative probe (the tracer's end-to-end proof for HARN-03).
- The project document no longer carries the falsified append instruction.
- The append-only amendment on the frozen library is named on the record, once.
</success_criteria>

<output>
Create `.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-01-SUMMARY.md` when done.

Record in it: all four discriminator rows verbatim; the known-member path chosen for v1.15 and how it
was derived; the negative-probe output and the reverted green run; the harness triple; and the
append-only amendment as a named, scoped exception.
</output>
