---
phase: 153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a
plan: 08
type: execute
wave: 5
depends_on: ["153-05"]
files_modified:
  - scripts/validation/check-phase-151.mjs
  - scripts/validation/check-phase-152.mjs
autonomous: true
requirements: [HARN-04]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "These are CONTENT leaves templated on the content-leaf family; the predecessor milestone's script-paths-only needle-target clause does not carry (D-22)."
    - "Leaves stay LIGHTWEIGHT — empty chain array, empty skip set, no harness check, no nested guard, needles plus a self invariant (D-23)."
    - "The arithmetic-not-transcription rule binds the apex; the empty literals here are not a transcription and the reconciliation is stated in writing on each leaf (D-24)."
    - "The recipe-hub three-file negative is justified by the RATIFIED HUB-UNWIRING INVARIANT, not by the file count. A three-file negative fails the durability test just as a corpus-wide one does if a later milestone legitimately wires a hub; the invariant is what makes it safe (D-26)."
    - "The recipe phase's emptiness assertions expressed against phase-local base commits are NOT TRANSCRIBABLE and positive deliverable needles are derived instead. That phase's verification document also asserts that this very validator was not created — an assertion this phase deliberately inverts, and the inversion is recorded rather than silently contradicted (D-28)."
    - "The integration leaf is TRANSCRIBED VERBATIM from its pre-specified needle-spec — every literal, re-derive nothing. Every literal in that spec was re-verified live and is not stale (D-29)."
    - "CORRECTION OF RECORD: the spec's claim that its self-reference guard is the same shape the structural twin uses is FALSE — the twin carries only the dual invariant. Implement the spec as written, file presence AND dual invariant, and record that the twin claim is false so the next reader does not correct the leaf toward the twin (D-30)."
    - "Needles derive at AUTHORING time; the leaves make ZERO planning-directory reads at RUNTIME, stated separately on each leaf (D-27)."
    - "The gate-respawn decision is explicit and appears in each leaf header: these leaves do NOT re-spawn corpus gates (D-38)."
    - statement: "HARN-04 boundary edge: the quick-navigation needle is LINE-SCOPED — extract the lines matching the bullet pattern, assert the match count is exactly one, then test the fragment against that isolated line only. A whole-file substring test false-matches lines above the bullet. Measured: whole-file 2, isolated line 1."
      verification: explicit
    - statement: "HARN-04 empty edge: a needle that greps a file which does not exist must FAIL, never pass vacuously. Every read goes through a null-sentinel helper and a null is treated as a failure."
      verification: explicit
    - statement: "HARN-03 adjacency edge: the negative needle bars a path fragment from three named hub files while the same fragment must be PRESENT in the master hub. Both assertions are file-scoped and are shown to hold simultaneously."
      verification: explicit
  artifacts:
    - scripts/validation/check-phase-151.mjs
    - scripts/validation/check-phase-152.mjs
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-08-SUMMARY.md
  key_links:
    - "The pre-specified needle-spec is the authoritative source for the integration leaf; it exists because the phase that would have written the validator deliberately shipped a spec instead."
    - "The frozen recipe-hub validator's generic arm cannot match a relative hub-to-recipe link, so recipe coverage is enforced by nothing today; this leaf is that enforcement, added additively and never by editing the frozen file."
    - "The filename-map needle is not optional — the structural twin carries one, and omitting it is a straight regression against the twin."
  prohibitions:
    - "The frozen recipe-hub validator must be byte-unchanged. Coverage is added additively in the new leaf, never by amending the frozen file."
    - "No leaf may read anything under the planning directory at runtime."
    - "No leaf may assert against a phase-local base commit, a commit range, or any moving comparison."
    - "No leaf may carry a non-empty chain array, a non-empty skip set, a harness check or a nested guard."
    - "No leaf re-spawns a corpus gate as a subprocess."
    - "No literal in the integration leaf may be re-derived; the spec is transcribed."
    - "The filename-map needle must not be omitted from the integration leaf."
    - "No `docs/` content file is created, edited or deleted — a content defect found while deriving needles is recorded, not fixed here."
    - "No requirement status row is flipped by this plan."
    - "Neither glossary frontmatter date field is touched."
    - "No summary or commit message may claim a chain result gathered without the nested environment flag (D-75)."
    - "Commit message bodies must not literally quote governance-carve paths or the paths of amended files in prose (D-76)."
    - "No frozen validator line is edited before grepping the validator tree for that exact call-site string (D-79)."
    - "No bare-noun platform blockquote token at line start outside a fence in any `.planning/` file this plan writes (D-43)."
---

<objective>
Author the last two content leaves — for the recipe phase and for the integration, registry and
navigation phase — completing the eight chain members the apex requires.

Purpose: one of these is derived, one is transcribed. The transcribed one exists because the phase
that would have written it deliberately shipped a fully specified needle-spec instead, and that spec
carries one false claim about its structural twin which must not be "corrected" away.

Output: two lightweight leaves, each exiting 0 standalone; the eight-leaf set complete.
</objective>

<needle_doctrine>
Same doctrine as the earlier leaf plans, restated so this plan stands alone.

- **Durability, not scope.** A negative is safe when a ratified invariant makes the absence permanent,
  not when the file count is small. Name the invariant in the check.
- **Derive at authoring time, never read at runtime** — except for the integration leaf, which is
  transcribed rather than derived.
- **Target the phase's own deliverables.**
- **Reconcile the arithmetic rule in writing** on every leaf.
- **State the gate-respawn decision in the header.** These leaves do not re-spawn corpus gates.
- **A null read is a failure**, never an empty string that satisfies a negative.
</needle_doctrine>

## Artifacts this phase produces

Created by Phase 153, absent at the phase base; treat as new, not as unacknowledged references.

- **New exports in `scripts/validation/_lib/frozen-at-close.mjs`:** `MILESTONE_CLOSE_SHAS.V120`,
  `readAtV120Close`, `lsTreeAtV120Close`, `materializeDocsAtClose`, `withDocsAtClose`.
- **New validators:** `scripts/validation/check-phase-145.mjs` through `check-phase-152.mjs` and
  `scripts/validation/check-phase-153.mjs`.
- **New check ids:** `V-145-*` through `V-152-*`; on the apex `V-153-AUDIT`, `V-153-CHAIN-<n>`,
  `V-153-AUDIT-HARNESS`, `V-153-SELF`, `V-153-C17CARVEOUT-SUCCESSOR`.
- **New harness lineage:** `scripts/validation/v1.21-milestone-audit.mjs`,
  `scripts/validation/v1.21-audit-allowlist.json`, `BASELINE_25` in
  `scripts/validation/regenerate-supervision-pins.mjs`.
- **New CI workflow:** `.github/workflows/audit-harness-v1.21-integrity.yml`.
- **New planning artifacts:** `.planning/milestones/v1.21-MILESTONE-AUDIT.md`,
  `.planning/milestones/v1.21-DEFERRED-CLEANUP.md`, this phase's summaries, `153-EVIDENCE.md`,
  `153-VERIFICATION.md`, and the gitignored `dist/docs-library-v1.21.zip`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-PATTERNS.md
@.planning/phases/152-integration-registry-navigation-last-close/152-04-SUMMARY.md
@scripts/validation/check-phase-132.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: `check-phase-151.mjs` — the recipe leaf, with positive needles replacing the non-transcribable assertions and the inverted self-assertion recorded</name>
  <files>scripts/validation/check-phase-151.mjs</files>
  <read_first>
    - scripts/validation/check-phase-126.mjs — the complete lightweight-leaf shape: null-sentinel read helper, empty chain constants, presence factory, needle pattern, self invariant, runner loop
    - .planning/phases/151-recipe-5-the-enterprise-update-plan/151-VERIFICATION.md, and specifically its prohibition block around lines 150-180 — read it in full. Identify every assertion expressed against a phase-local base commit, and note the assertion around line 158 stating that this very validator was not created. This phase inverts that assertion deliberately
    - .planning/phases/151-recipe-5-the-enterprise-update-plan/151-01-SUMMARY.md through 151-05-SUMMARY.md and 151-REVIEW.md — the shipped artifacts and the review findings
    - .planning/ROADMAP.md Phase 151 — the deliverables: the recipe as the corpus's first configuration artifact, the decision points with their reversibility ratings including the two most severe, the platform-applicability markers with their minimum count, the truthful rollback and recovery section built largely of absences, and the template divergence resolved on the record
    - docs/recipes/05-enterprise-update-plan.md — its heading list and the exact shipped wording of the decision-point ratings and the applicability markers
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-22 through D-28, D-38
  </read_first>
  <action>
Author `scripts/validation/check-phase-151.mjs` on the content-leaf template, with the four header
clauses: derivation rule, runtime prohibition, arithmetic reconciliation, gate-respawn decision.

First, enumerate the phase's non-transcribable assertions and derive a positive needle for each,
recording the mapping in the summary. Then handle the inverted assertion explicitly: that phase's
verification document asserts this validator was not created and attributes its creation to this
phase. Creating it is correct and intended. Record the inversion in this leaf's header — one sentence
naming the source assertion and stating that this phase discharges it — so a later reader does not
treat the new file as a contradiction of a shipped verification.

Needles to author:

- **Presence and non-emptiness** of the recipe document.
- **The decision-point set** with their reversibility ratings: assert the count of rated decision
  points taken from the document, and assert the two most severe ratings are present. Measure the
  count from the document rather than recalling it.
- **The platform-applicability markers**: assert a marker is present for every decision point and that
  the count carrying the platform-restricted marker meets the ratified minimum.
- **The rollback and recovery section** present, and the documented absence it turns on present —
  the default channel having no rollback path at all is the section's hardest truth and the thing a
  later editor is most likely to "improve" away.
- **The configuration-artifact claim** the recipe makes about itself, asserted as shipped.
- **The template divergence resolution** recorded in the document or the template, asserted as
  present — this is the positive form of the prohibition that said the divergence must not be left
  unresolved.
- **The self invariant** in the template's exact shape.

Every read goes through the null-sentinel helper and a null fails. Measure every literal with a real
grep before baking it and record the count.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-151.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-151.mjs` exits 0 with zero failures and zero skips.
    - The summary contains a table mapping every non-transcribable assertion in the phase's verification document to the positive needle that replaced it.
    - The header records the inverted assertion in one sentence naming the source and stating that this phase discharges it.
    - No needle references a commit object name, a commit range or a change-set comparison; a grep for a commit-shaped literal returns nothing outside comment prose.
    - The decision-point count and the platform-restricted marker count are both taken from the document at authoring time and both measuring greps are quoted.
    - The rollback-and-recovery needle is proved failable by a scratch probe shown red then green.
    - The header carries all four clauses; the chain array and skip set are empty; the self invariant asserts both.
    - `grep -c "child_process" scripts/validation/check-phase-151.mjs` returns 0.
  </acceptance_criteria>
  <reversibility rating="reversible">A new leaf validator with no consumer until the apex is authored.</reversibility>
  <done>The leaf asserts the recipe's shipped structure through durable positive needles, the non-transcribable assertions are replaced with a recorded mapping, the deliberate inversion is on the record, and the leaf exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: `check-phase-152.mjs` — TRANSCRIBED VERBATIM from the pre-specified needle-spec, with the twin claim corrected on the record</name>
  <files>scripts/validation/check-phase-152.mjs</files>
  <read_first>
    - .planning/phases/152-integration-registry-navigation-last-close/152-04-SUMMARY.md lines 343-414 — the complete pre-specified needle-spec. This is the authoritative source. Read all of it before writing anything
    - .planning/STATE.md, the Plan-Time Research Flags block — the compressed mirror of the same spec, useful as a cross-check that nothing was missed in transcription
    - scripts/validation/check-phase-132.mjs lines 100-125 — the structural twin's self invariant. Read it to confirm for yourself that it carries ONLY the dual invariant and no file-presence assertion; the spec's claim that the twin carries both is false and this is where you verify that
    - scripts/validation/check-phase-126.mjs — the mechanical leaf scaffold: null-sentinel read helper, empty chain constants, presence factory, needle pattern, runner loop
    - docs/index.md, docs/operations/00-index.md, scripts/pipeline/filename-map.md, docs/common-issues.md, docs/quick-ref-l1.md, docs/quick-ref-l2.md — the six files the spec's needles read
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-26, D-29, D-30, D-38
  </read_first>
  <action>
**Transcribe. Re-derive nothing.** Every literal in the spec was re-verified live and is not stale.
Use the leaf scaffold as the mechanical shape only; the content comes from the spec.

The spec's needles, transcribed:

- **Master hub**: a recipes-table row whose link target is the recipe path; the quick-navigation
  bullet tested **line-scoped** — extract the lines matching the bullet pattern, assert the match
  count is exactly one, then test the fragment against that isolated line only. A whole-file substring
  test false-matches lines above the bullet; the measured difference is two matches whole-file versus
  one isolated. Also the sub-heading literal.
- **Operations index**: the heading literal; all four patch path fragments; a version-history row
  citing the requirement identifier.
- **Filename map**: all eleven registry identifiers present. **Carrying this is not optional** — the
  structural twin carries a filename-map needle and omitting it is a straight regression.
- **Negative needle**: the three named troubleshooting hub files must not contain the recipe path
  fragment. Justify this negative in the check's detail by the **ratified hub-unwiring invariant**,
  not by the file count — a three-file negative would fail the durability test just as a corpus-wide
  one would if a later milestone legitimately wired a hub, and it is the invariant that makes it safe.
- **Self-reference guard**: the spec requires the leaf to assert **its own file is present at its own
  path** **plus** the dual chain invariant, and claims that is the same shape the twin uses.
  **Implement the spec as written — both parts.** And record, in the leaf header and in the summary,
  that the twin claim is FALSE: the twin carries only the dual invariant. Without that note the next
  reader "corrects" this leaf toward the twin and deletes a real assertion.

The frozen recipe-hub validator is never edited. Its generic arm cannot match a relative hub-to-recipe
link, which is why recipe coverage is enforced by nothing today and why this leaf is the enforcement.

After writing, cross-check the transcription against the compressed mirror in the project state
document and record any discrepancy rather than silently reconciling it.

Then run all eight leaves in one sequence and record all eight results.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-151.mjs &amp;&amp; node scripts/validation/check-phase-152.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-152.mjs` exits 0 with zero failures and zero skips.
    - Every needle named in the spec is present in the file; the summary lists the spec's needles and the implemented check ids side by side, with no spec row unimplemented.
    - The quick-navigation needle is line-scoped: the summary quotes both the whole-file match count and the isolated-line match count, and the check asserts on the isolated line.
    - All eleven registry identifiers are asserted; the count is proved by a grep over the leaf file.
    - The negative needle names the ratified hub-unwiring invariant in its detail, and the summary shows the same path fragment is simultaneously PRESENT in the master hub and ABSENT from the three named hubs.
    - The self-reference guard asserts both file presence at its own path and the dual chain invariant.
    - The leaf header records that the spec's twin claim is false, with the twin's actual content named.
    - `git diff 246fa3dd..HEAD -- scripts/validation/check-phase-132.mjs` is empty.
    - All eight leaves, 145 through 152, are run in one sequence, all eight exit 0, and all eight result lines are quoted.
    - `git diff --stat` for this plan lists exactly two new files.
  </acceptance_criteria>
  <reversibility rating="reversible">A new leaf validator with no consumer until the apex is authored.</reversibility>
  <done>The integration leaf is a faithful transcription of its spec including the filename-map needle and the two-part self guard, the twin claim is corrected on the record, and the complete eight-leaf set exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-specified spec to implementation | The spec is authoritative and carries one known-false claim; a faithful transcription must keep the claim's implementation and reject its rationale. |
| validator to corpus | Leaves read documentation and pipeline files; a missing file must fail rather than degrade. |
| line-scoped assertion to whole-file assertion | A whole-file substring test silently passes where the line-scoped one is the real contract. |
| frozen validator to new coverage | The gap this leaf closes exists because a frozen validator's arm cannot fire; editing it is prohibited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-36 | Information Disclosure | a whole-file substring test standing in for a line-scoped one | high | mitigate | The needle extracts the matching lines, asserts the match count is exactly one, and tests the fragment on that isolated line; both counts are quoted in the summary so the difference is visible. |
| T-153-37 | Tampering | a later reader "correcting" the leaf toward its twin | high | mitigate | The leaf header records that the spec's twin claim is false and names what the twin actually carries, so the next reader has the correction in front of them rather than the claim. |
| T-153-38 | Spoofing | a negative needle that looks scoped but is really incidental | medium | mitigate | The negative names the ratified invariant that makes the absence permanent, and the summary shows the fragment simultaneously present in the master hub and absent from the three named hubs. |
| T-153-39 | Repudiation | a dropped spec row | medium | mitigate | The summary lists the spec's needles and the implemented check ids side by side, and the transcription is cross-checked against the compressed mirror in the project state document with discrepancies recorded rather than reconciled silently. |
| T-153-40 | Denial of Service | editing the frozen recipe-hub validator to close the gap | high | mitigate | Byte-equality of the frozen validator against the predecessor close is an acceptance criterion; coverage is added additively in the new leaf. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs. Two new Node files using built-ins only. |

ASVS level: 1. Blocking threshold: high. All three high-severity entries are mitigated by computed
acceptance criteria — a two-count line-scoping proof, a recorded correction in the file itself, and a
byte-equality assertion against the predecessor close.
</threat_model>

<verification>
- Both leaves exit 0 standalone with zero failures and zero skips.
- All eight leaves 145 through 152 exit 0 in one sequence.
- `git diff 246fa3dd..HEAD -- scripts/validation/check-phase-132.mjs` is empty.
- The spec-to-implementation table has no unimplemented row.
- No leaf spawns a subprocess or reads a planning path at runtime.
</verification>

<success_criteria>
- The eight-leaf set is complete and every leaf exits 0 standalone (HARN-04, SC#3).
- The integration leaf is a faithful transcription including the filename-map needle and the two-part
  self guard.
- The false twin claim is corrected on the record in the file itself, not only in a summary.
- The frozen recipe-hub validator is byte-unchanged and coverage was added additively.
</success_criteria>

<output>
Create `.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-08-SUMMARY.md` when done.

Record: both leaves' result lines and all eight leaves' result lines; the non-transcribable-to-positive
mapping table for the recipe leaf; the inverted-assertion note; the spec-to-implementation table; both
quick-navigation match counts; the present-and-absent proof for the negative needle; and the corrected
twin claim.
</output>
