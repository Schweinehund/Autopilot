---
phase: 153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a
plan: 11
type: execute
wave: 8
depends_on: ["153-10"]
files_modified:
  - .claude/settings.local.json
  - .planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-PATTERNS.md
  - .planning/phases/146-windows-driver-firmware-update-depth/146-PATTERNS.md
  - .planning/phases/147-linux-update-delivery/147-PATTERNS.md
  - .planning/research/PER-OEM-BIOS-GAP.md
  - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md
autonomous: false
requirements: [HARN-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The governance-carve regime of the predecessor milestone is RETIRED for this milestone by owner ruling, and the machine-local hook that enforces it is de-registered for the duration of this phase. It was never re-adopted for this milestone, and all eight content phases already shipped past it red (D-39)."
    - "CORRECTION OF RECORD: the claim that the carve gate had zero execution sites was measured over the scripts, workflow and package surfaces and MISSED the machine-local agent directory. The hook there executes the gate and is registered as the third stop hook. The retirement ruling is right; the reasoning that produced it was not, and the corrected reasoning goes on the record (D-40)."
    - "The obligation that actually survives is the governance-artifact assertion, not byte-equality on the gate script — the gate's blob is pinned at a fixed PAST commit, so its current bytes are structurally unconstrained, while the two governance files it names are live-read by a chain member of the new apex and may be neither edited nor moved (D-41)."
    - "The four orphan planning artifacts are committed before archival. All four are untracked inside directories the archival step will move, and untracked files do not travel with a move and are in no commit, so they would silently detach from their archived phases (D-62)."
    - "Every hygiene measurement in this phase uses the all-untracked form. [MEASURED 2026-08-29] the bare form reports 14 lines and the all-untracked form reports 108 — the bare form collapses untracked directories (D-63)."
    - "Only the third audit axis runs against the dirty tree; the first axis is a fresh clone and by construction cannot carry untracked files. The predecessor's claim that both axes run against this state was inherited without re-checking and is wrong (D-64)."
    - "The four remote atom branches are AUDITED AND REPORTED, and the delete-or-keep call goes to the owner at the push checkpoint — one of them holds the head commit on which a completed sweep's evidence rests, and a deleted remote branch has no reflog (D-65)."
    - "The three registered stop hooks are sequenced against the terminal ordering as ONE decision, not three scattered ones: the issue-tracker hook nudges at execution-complete which is pre-verification, the publish-bundle hook blocks on the milestone-complete transition demanding this milestone's archive, and the carve hook is retired. Two of the three block (D-69)."
    - "The documentation style pass SHIPS with this milestone and is recorded as scope by owner ruling: its directory is tracked with unpushed commits inside the push set, so it cannot be ruled on and not pushed, and the milestone audit must own it explicitly rather than leaving a tag carrying unnarrated work (D-61)."
    - "The owner push is a NAMED BLOCKING CHECKPOINT with an explicit decline, defer and partial branch. Pre-flights are run at the checkpoint and the ahead-count is re-measured there, never carried — [MEASURED 2026-08-29] 210 ahead, 0 behind. A predecessor milestone sat blocked for weeks on exactly this shape (D-60)."
    - statement: "HARN-06 empty edge: if the working tree is clean of the four named orphans before the commit task runs, the task records that and does not fabricate a commit; the criterion is that all four are tracked afterwards, not that a commit was made."
      verification: explicit
    - statement: "HARN-06 concurrency edge: two of the three stop hooks block, and one of them fires on the milestone-complete transition. The sequencing decision states which hook may fire at which step so a hook does not block a step it was not meant to gate."
      verification: explicit
  artifacts:
    - .claude/settings.local.json
    - .planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-PATTERNS.md
    - .planning/phases/146-windows-driver-firmware-update-depth/146-PATTERNS.md
    - .planning/phases/147-linux-update-delivery/147-PATTERNS.md
    - .planning/research/PER-OEM-BIOS-GAP.md
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-11-SUMMARY.md
  key_links:
    - "The push is the precondition for the entire dispatch axis: no workflow in the set carries a push trigger, so a push fires nothing by itself, but a dispatch needs the workflow file present on the default branch."
    - "Untracked files inside a directory that is about to be moved do not travel with the move, which is why the orphan commit must precede archival rather than follow it."
    - "The style-pass directory is tracked and already carries commits inside the push set, so the scope decision is about narration, not about inclusion."
  prohibitions:
    - "No hygiene measurement uses the bare status form."
    - "No remote branch is deleted by this plan; the audit reports and the owner decides."
    - "The two governance files the phase-139 validator live-reads are neither edited nor moved."
    - "No amendment plan for the retired carve regime gates this phase; the predecessor's hard blocker does not carry."
    - "The other untracked populations reviewed and not ruled in this phase are not swept in: no blanket add-all."
    - "No `docs/` content file is created, edited or deleted."
    - "No requirement status row is flipped by this plan."
    - "Neither glossary frontmatter date field is touched."
    - "No summary, evidence file or commit message may claim a chain result gathered without the nested environment flag (D-75)."
    - "Commit message bodies must not literally quote governance-carve paths or the paths of amended files in prose — the exclusion check greps the message body and the file list together (D-76)."
    - "No frozen validator line is edited before grepping the validator tree for that exact call-site string (D-79)."
    - "No bare-noun platform blockquote token at line start outside a fence in any `.planning/` file this plan writes (D-43)."
---

<objective>
Make the local state genuinely ready to push, then halt for the owner.

Purpose: every axis of the terminal re-audit is captured at one shared commit, and that commit only
exists once the owner has pushed. Everything downstream waits here, so the checkpoint is specified
with real branches rather than assumed away.

Output: a retired carve regime with its correction of record, four orphan artifacts tracked, a
measured hygiene state, an atom-branch audit, a hook-sequencing decision, and a blocking owner
checkpoint.
</objective>

## Artifacts this phase produces

Created by Phase 153, absent at the phase base; treat as new, not as unacknowledged references.

- **New exports in `scripts/validation/_lib/frozen-at-close.mjs`:** `MILESTONE_CLOSE_SHAS.V120`,
  `readAtV120Close`, `lsTreeAtV120Close`, `materializeDocsAtClose`, `withDocsAtClose`.
- **New validators:** `scripts/validation/check-phase-145.mjs` through `check-phase-152.mjs` and
  `scripts/validation/check-phase-153.mjs`.
- **New check ids:** `V-145-*` through `V-152-*`; on the apex `V-153-AUDIT`, `V-153-CHAIN-<n>`,
  `V-153-AUDIT-HARNESS`, `V-153-SELF`, `V-153-C17CARVEOUT-SUCCESSOR`.
- **New harness lineage:** `scripts/validation/v1.21-milestone-audit.mjs`,
  `scripts/validation/v1.21-audit-allowlist.json`, `BASELINE_25` in
  `scripts/validation/regenerate-supervision-pins.mjs`.
- **New CI workflow:** `.github/workflows/audit-harness-v1.21-integrity.yml`.
- **New planning artifacts:** `.planning/milestones/v1.21-MILESTONE-AUDIT.md`,
  `.planning/milestones/v1.21-DEFERRED-CLEANUP.md`, this phase's summaries, `153-EVIDENCE.md`,
  `153-VERIFICATION.md`, and the gitignored `dist/docs-library-v1.21.zip`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-10-SUMMARY.md
@.planning/milestones/v1.20-phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-09-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire the carve regime, record the corrected reasoning, and name the obligation that survives</name>
  <files>.claude/settings.local.json, .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md</files>
  <read_first>
    - .claude/settings.local.json — the registered stop hooks in order. This file is machine-local and gitignored, which is why a source-tree grep does not find it
    - .claude/hooks/v1.20-carve-gate.cjs around lines 80-95 and 125-135 — the site where it executes the gate, and its fail-open guard
    - scripts/validation/carve-gate.mjs around lines 30-40 — its path constant and its default base. Note the file is classified binary by grep and needs the text-forcing flag for a census
    - scripts/validation/check-phase-139.mjs around line 68 and lines 130-175 — the fixed past-commit blob pin, and the governance-artifact assertions that are actually live
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-39, D-40, D-41
  </read_first>
  <action>
Run the gate once and record what it actually reports at the current head, using the text-forcing grep
flag where a census over the gate script is needed — the script is classified binary and a plain grep
silently skips it.

De-register the carve hook from the machine-local settings file for the duration of this phase, leaving
the other two stop hooks registered and in their existing order. The file is machine-local and
gitignored, so this is a reversible local change and not a repository edit.

Record three things in the evidence file:

1. **The ruling and its ground.** The regime belongs to the predecessor milestone: the gate's path
   constant and its default base both name that milestone, it reports a failure with a large off-list
   population at the current head, and all eight content phases of this milestone already shipped past
   it red. It was never re-adopted. No amendment plan gates this phase — the predecessor's hard
   blocker does not carry.
2. **The correction of record.** The claim that the gate had zero execution sites was measured over
   the scripts, workflow and package surfaces and missed the machine-local agent directory entirely.
   The hook there executes the gate and is registered as the third stop hook; because the gate exits
   non-zero with a non-empty off-list, the hook's fail-open guard does not engage, so it blocks on the
   first stop and warns on every subsequent one. The ruling is right; the reasoning that produced it
   was not, and the corrected reasoning is what goes in the record.
3. **The surviving obligation.** It is the governance-artifact assertion, not byte-equality on the
   gate script. The blob pin targets a fixed past commit, so the script's current bytes are
   structurally unconstrained. What is live is the set of assertions over the two governance files —
   the fenced allowlist count, the amendment-procedure heading with its rule count, the grep-procedure
   heading, and the ledger row floor — and the validator carrying them is a chain member of the new
   apex, so neither file may be edited or moved.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-139.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-139.mjs` exits 0 and its governance-artifact line reports PASS with its measured values quoted.
    - The gate's own run output at the current head is quoted in the evidence file, including its exit status and its off-list population size.
    - The carve hook is no longer registered in the machine-local settings file, and the other two hooks are still registered in their original order; the before and after hook lists are both quoted.
    - The correction of record is written in full, naming the surface that was missed and the two block behaviours the hook produces.
    - The surviving obligation is named with all four of its assertions and their measured values, and the constraint that the two governance files may be neither edited nor moved is restated.
    - The census over the gate script used the text-forcing grep flag; the command is quoted.
  </acceptance_criteria>
  <reversibility rating="reversible">The hook registration is machine-local and gitignored; re-registering is a one-line change.</reversibility>
  <done>The carve regime is retired locally, the corrected reasoning is on the record, and the obligation that actually survives is named with its measured values.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the four orphan artifacts, measure hygiene the correct way, audit the atom branches, and sequence the stop hooks</name>
  <files>.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-PATTERNS.md, .planning/phases/146-windows-driver-firmware-update-depth/146-PATTERNS.md, .planning/phases/147-linux-update-delivery/147-PATTERNS.md, .planning/research/PER-OEM-BIOS-GAP.md, .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-61, D-62, D-63, D-64, D-65, D-69, and the deferred-ideas block naming the untracked populations that were reviewed and NOT ruled in this phase
    - .claude/settings.local.json — the three stop hooks in registration order
    - .claude/hooks/publish-bundle-gate.cjs — what transition it blocks on and what archive filename its idempotency probe looks for
    - .claude/hooks/jira-milestone-gate.cjs — when it nudges, and why holding the active item until verification passes matters
    - scripts/docs-style/ — confirm it is tracked and count its unpushed commits inside the push set
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md — as written so far
  </read_first>
  <action>
**Commit the four orphans (D-62).** Add and commit exactly these four files: the three phase pattern
maps and the research gap document. All four are untracked inside directories the archival step will
move, and untracked files do not travel with a move and are in no commit, so they would silently
detach from their archived phases. Add them by explicit path. Do not use a blanket add — the other
untracked populations were reviewed and deliberately not ruled in this phase, and one of them is
roughly ninety files in a directory that is not ignored.

**Measure hygiene correctly (D-63).** Record the working-tree state using the all-untracked form and
state the bare form's number alongside it so the difference is visible. The bare form collapses
untracked directories and under-reports by a factor of several. Enumerate what remains untracked after
the orphan commit, grouped by population, and mark each group as ruled or deliberately unruled in this
phase.

**Record the axis asymmetry (D-64).** Only the third audit axis runs against the dirty tree. The first
axis is a fresh clone and by construction cannot carry untracked files. State this correction
explicitly; the predecessor's claim that both axes run against this state was inherited without
re-checking and is wrong.

**Audit the atom branches (D-65).** For each of the four remote atom branches, record its tip commit,
whether that tip is reachable from the default branch, and what evidence rests on it. One of them
holds the head commit on which a completed sweep's evidence rests, and a deleted remote branch has no
reflog. Report; do not delete. The delete-or-keep call goes to the owner at the next task's
checkpoint.

**Record the style-pass scope ruling (D-61).** Its directory is tracked and already carries unpushed
commits inside the push set, so it cannot be ruled on and not pushed. It lands on the default branch
and appears in this milestone's tag history. Record that the milestone audit must own it explicitly
rather than leaving a tag carrying unnarrated work, and record why splitting it out is not an option:
it would mean rewriting the entire unpushed history at a close whose evidence is keyed to a shared
commit.

**Sequence the stop hooks as one decision (D-69).** Write down, in order, which of the three hooks may
fire at which step of the terminal ordering: the issue-tracker hook nudges at execution-complete which
is pre-verification, so the active item is held until verification passes; the publish-bundle hook
blocks on the milestone-complete transition and demands this milestone's archive, and its idempotency
probe will not recognise the previous phase's differently-named archive; the carve hook is retired by
the previous task. Two of the three block. State the sequencing as a single decision so the hooks are
not each rediscovered at the moment they fire.
  </action>
  <verify>
    <automated>git ls-files --error-unmatch .planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-PATTERNS.md .planning/phases/146-windows-driver-firmware-update-depth/146-PATTERNS.md .planning/phases/147-linux-update-delivery/147-PATTERNS.md .planning/research/PER-OEM-BIOS-GAP.md</automated>
  </verify>
  <acceptance_criteria>
    - All four named files are tracked: the verify command exits 0 and lists all four.
    - The commit that added them contains exactly those four paths and no others; `git show --stat HEAD` is quoted.
    - The commit message body does not quote a governance-carve path or an amended file path in prose; `git show --name-only HEAD` is inspected as a combined body-plus-file-list block and the exclusion check is run against that combined output.
    - The hygiene measurement is recorded in the all-untracked form with the bare form's number quoted alongside; the remaining untracked populations are enumerated and each is marked ruled or deliberately unruled.
    - The axis asymmetry correction is recorded in one sentence naming which axis is dirty and why the other cannot be.
    - All four atom branches are recorded with tip commit, reachability from the default branch, and the evidence resting on each. No branch is deleted.
    - The style-pass scope ruling is recorded with its tracked status, its unpushed commit count inside the push set, and the reason splitting it out is not an option.
    - The hook sequencing is recorded as one ordered decision covering all three hooks and naming which two block.
  </acceptance_criteria>
  <reversibility rating="reversible">One commit of four planning artifacts; revertible before the push.</reversibility>
  <done>The four orphans are tracked in a four-path commit, hygiene is measured the correct way with populations enumerated, the atom branches are audited without deletion, and the hook sequencing is a single written decision.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Push the local default branch to the remote, or defer, decline, or push partially — the resulting head commit becomes the single shared commit for all three audit axes.</decision>
  <context>
No workflow in the set carries a push trigger, so the push fires nothing by itself. Its purpose is to
make the workflow files present on the default branch so the dispatch axis can run at all. Nothing
downstream of this point can proceed without it: the dispatch, both reproduction axes, the close
artifacts and the close gate all key to the commit this produces.

This is one-way. There is no un-push, and the push publishes the entire unpushed set — including the
documentation style pass, whose directory is tracked with commits already inside that set and which
therefore cannot be ruled on and not pushed.

**Run these pre-flights AT THE CHECKPOINT and never carry a figure from planning time.** Fetch from
the remote, then measure the ahead and behind counts between the default branch and its remote
tracking branch. Re-measure the working tree in the all-untracked form — the bare form collapses
untracked directories and under-reports by a factor of several. Rule on working-tree hygiene first: an
untracked population nobody has ruled on is a question, not a detail, because the third audit axis
reproduces against exactly this state.

**Present, in this order:** the measured ahead and behind counts taken now; the working-tree state
with every untracked population named and marked ruled or unruled; the style-pass scope ruling and
the fact that its commits are already inside the push set; the four remote atom branches with their
tip commits and the evidence resting on each, with the explicit delete-or-keep question — one of them
holds the head commit on which a completed sweep's evidence rests and a deleted remote branch has no
reflog; and the consequence of declining, which is that the dispatch axis cannot run and the milestone
cannot close.

A predecessor milestone sat blocked for weeks at exactly this shape. The defer branch exists so the
phase survives that rather than failing at it.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed — push the full unpushed set</name>
      <pros>Produces the single shared commit every axis keys to; unblocks the dispatch, both reproduction axes and the close.</pros>
      <cons>Irreversible; publishes everything unpushed, including the style pass and any population the owner has not individually reviewed.</cons>
    </option>
    <option id="defer">
      <name>Defer — not ready yet</name>
      <pros>Nothing is published before the owner is satisfied; the phase halts cleanly with a named blocker rather than half-executing.</pros>
      <cons>Everything downstream waits; the milestone does not close until it is revisited.</cons>
    </option>
    <option id="partial">
      <name>Partial — push some commits and not others</name>
      <pros>Lets the owner hold back specific work while still producing a shared commit.</pros>
      <cons>Changes what the three axes are measuring; the resulting head commit must be recorded explicitly because the shared-commit rule binds all three axes to it.</cons>
    </option>
    <option id="decline">
      <name>Decline — rule the push out entirely</name>
      <pros>Explicit and recorded rather than indefinitely deferred.</pros>
      <cons>The terminal re-audit criterion is recorded unmet and the milestone does not ship this phase.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, defer, partial, or decline — and answer the atom-branch delete-or-keep question</resume-signal>
  <read_first>
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md — everything recorded by Tasks 1 and 2, which is the material the owner rules on
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-57, D-60, D-61, D-65
  </read_first>
  <acceptance_criteria>
    - The ahead and behind counts are measured at the checkpoint and quoted with their commands; the figure carried from planning time is not reused.
    - The working-tree state is re-measured at the checkpoint in the all-untracked form, with every population named and marked ruled or unruled.
    - All five presentation items are put to the owner.
    - The owner's selection is recorded verbatim in the evidence file, together with the resulting head commit if one exists.
    - The atom-branch delete-or-keep call is recorded with the owner's answer; no branch is deleted by this task.
    - No push occurs without a recorded proceed or partial selection.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tree to public remote | The push publishes everything in the unpushed set, including work not authored by this phase. |
| untracked files to archival | Files not in a commit do not travel when their directory is moved. |
| machine-local hook config to repository state | A gitignored settings file governs blocking behaviour that a source-tree review cannot see. |
| remote branch to evidence | A branch tip may be the only reference holding evidence for a completed obligation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-52 | Information Disclosure | pushing unreviewed local content to a public remote | high | mitigate | The working tree is enumerated in the all-untracked form, every population is named and marked ruled or unruled, the orphan commit adds four explicit paths rather than using a blanket add, and the owner rules on hygiene before the push at a blocking checkpoint. |
| T-153-53 | Repudiation | evidence lost with a deleted remote branch | medium | mitigate | No branch is deleted by this plan; all four are audited with tip commit, reachability and the evidence resting on each, and the call is put to the owner explicitly. |
| T-153-54 | Tampering | a gitignored hook silently blocking or permitting a step | medium | mitigate | All three hooks are read, sequenced against the terminal ordering as one decision, and the before-and-after registration lists are quoted. |
| T-153-55 | Denial of Service | orphan artifacts detaching at archival | high | mitigate | The four are committed by explicit path before archival, and tracked status is asserted by a command that fails on any untracked path. |
| T-153-56 | Spoofing | a carried ahead-count standing in for a measured one | medium | mitigate | The pre-flights are re-run at the checkpoint with their commands quoted, and the planning-time figure is explicitly not reused. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs. One commit of planning artifacts and one machine-local settings edit. |

ASVS level: 1. Blocking threshold: high. Both high-severity entries are mitigated by an enumerated
working-tree census with per-population rulings, an explicit-path commit, and a blocking human
checkpoint that rules on hygiene before anything is published.
</threat_model>

<verification>
- All four orphan artifacts are tracked and were added in a four-path commit.
- `node scripts/validation/check-phase-139.mjs` exits 0.
- The hygiene measurement uses the all-untracked form and enumerates every remaining population.
- The four atom branches are audited and none is deleted.
- The owner's decision and the resulting head commit are recorded.
</verification>

<success_criteria>
- The predecessor's carve regime is retired with its correction of record and its surviving obligation
  named (D-39, D-40, D-41).
- The four orphan artifacts are tracked before archival (D-62).
- Hygiene is measured the correct way and every untracked population is ruled or explicitly unruled
  (D-63).
- The owner checkpoint is reached with real branches and a recorded decision (D-60, SC#5).
</success_criteria>

<output>
Create `.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-11-SUMMARY.md` when done, appending to `153-EVIDENCE.md`.

Record: the gate's run output and the corrected reasoning; the surviving obligation with measured
values; the four-path commit; both hygiene numbers with populations enumerated; the axis asymmetry
correction; the atom-branch audit; the style-pass scope ruling; the hook sequencing; and the owner's
decision with the resulting head commit.
</output>
