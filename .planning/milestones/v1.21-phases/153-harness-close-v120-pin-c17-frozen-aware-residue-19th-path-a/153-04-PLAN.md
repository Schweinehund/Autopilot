---
phase: 153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a
plan: 04
type: execute
wave: 3
depends_on: ["153-03"]
files_modified:
  - scripts/validation/v1.21-milestone-audit.mjs
  - scripts/validation/v1.21-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
autonomous: true
requirements: [HARN-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Path-A copy source is the UNCONVERTED `v1.20-milestone-audit.mjs`, retrieved from git history at the commit recorded before the previous plan converted it, and its recovered blob hash matches the hash recorded there (D-45)."
    - "The new harness inherits C1 through C17 verbatim; no eighteenth check and no new check is folded in, because new corpus content is already gated by the contract validator, the eight leaves and the link checker (D-44)."
    - "The new harness is born live-HEAD with a live-HEAD C17 leg, because there is no close SHA for the current milestone to freeze against; it becomes the successor milestone's sole unconverted harness and that residue is stated in its header (D-46)."
    - "This is the NINETEENTH Path-A lineage bump; the frozen-aware conversion ordinal is a different sequence and both survive into the prose (D-10)."
    - "The allowlist sidecar is a header-fields-only copy — the generated timestamp and the phase field are re-derived, every pin array is carried byte-identical — and the new harness's sidecar reference is repointed to it (D-47)."
    - "`BASELINE_25` is appended following `BASELINE_24`'s shape, at the position the existing forward pointer names (D-47)."
    - "The pin-regeneration helper's report mode is NEVER cited as pin-drift proof — it hardcodes an old sidecar and walks fewer than half the pins. Whether the self-test is run is decided explicitly and recorded either way (D-48)."
    - "Harness, sidecar and baseline land as ONE indivisible plan; the CI workflow is a separate plan (D-49)."
    - statement: "HARN-04 unclassified edge (probe row remains unresolved and is surfaced, never dismissed): the deterministic probe could not classify HARN-04's edge family. Flagged planner assumption — the lineage bump's failure modes are operationalized as wrong copy source, unrepointed sidecar reference, and a stale count or span string carried from the predecessor. Residual risk that a fourth mode exists is recorded, not dismissed."
      verification: backstop
    - statement: "HARN-04 empty edge: the new sidecar's pin arrays are non-empty where the predecessor's were non-empty, and the harness's sidecar parse fails loud rather than degrading to an empty allowlist if the file is missing or malformed."
      verification: explicit
  artifacts:
    - scripts/validation/v1.21-milestone-audit.mjs
    - scripts/validation/v1.21-audit-allowlist.json
    - scripts/validation/regenerate-supervision-pins.mjs
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-04-SUMMARY.md
  key_links:
    - "The copy source determines which corpus the new harness audits; a converted source would silently point the new harness at the predecessor's frozen tree, and no gate in this repository detects it."
    - "The CI workflow's sidecar-reference job greps the harness for the sidecar path literal, so an unrepointed reference is a CI failure in the next plan rather than a silent one."
    - "The apex authored later runs this harness by path, so the harness filename is a contract with the apex's harness constant."
  prohibitions:
    - "The copy source must never be the on-disk post-conversion form of the predecessor harness."
    - "No eighteenth corpus check is added to the new harness."
    - "The pin-regeneration helper's hardcoded old-sidecar paths are not edited — they are out of scope and named only as a warning about which instrument may be cited as proof."
    - "The predecessor harness and its sidecar must be byte-unchanged by this plan."
    - "No `docs/` content file is created, edited or deleted."
    - "No requirement status row is flipped by this plan."
    - "Neither glossary frontmatter date field is touched."
    - "No summary or commit message may claim a chain result gathered without the nested environment flag (D-75)."
    - "Commit message bodies must not literally quote governance-carve paths or the paths of amended files in prose (D-76)."
    - "No frozen validator line is edited before grepping the validator tree for that exact call-site string (D-79)."
    - "No bare-noun platform blockquote token at line start outside a fence in any `.planning/` file this plan writes (D-43)."
---

<objective>
Land the nineteenth Path-A lineage generation as one indivisible unit: the new milestone-audit
harness forked from the correct source, its allowlist sidecar, and the appended baseline block.

Purpose: the apex authored two plans later runs this harness by name. If the harness does not exist,
the apex fails at its hard-fail branch; if it exists but was forked from the converted predecessor, it
audits the wrong corpus and every gate stays green.

Output: a live-HEAD harness that exits 0 against the current corpus, a repointed sidecar, and a
baseline block at the position the existing forward pointer names.
</objective>

## Artifacts this phase produces

Created by Phase 153, absent at the phase base; treat as new, not as unacknowledged references.

- **New exports in `scripts/validation/_lib/frozen-at-close.mjs`:** `MILESTONE_CLOSE_SHAS.V120`,
  `readAtV120Close`, `lsTreeAtV120Close`, `materializeDocsAtClose`, `withDocsAtClose`.
- **New validators:** `scripts/validation/check-phase-145.mjs` through `check-phase-152.mjs` and
  `scripts/validation/check-phase-153.mjs`.
- **New check ids:** `V-145-*` through `V-152-*`; on the apex `V-153-AUDIT`, `V-153-CHAIN-<n>`,
  `V-153-AUDIT-HARNESS`, `V-153-SELF`, `V-153-C17CARVEOUT-SUCCESSOR`.
- **New harness lineage:** `scripts/validation/v1.21-milestone-audit.mjs`,
  `scripts/validation/v1.21-audit-allowlist.json`, `BASELINE_25` in
  `scripts/validation/regenerate-supervision-pins.mjs`.
- **New CI workflow:** `.github/workflows/audit-harness-v1.21-integrity.yml`.
- **New planning artifacts:** `.planning/milestones/v1.21-MILESTONE-AUDIT.md`,
  `.planning/milestones/v1.21-DEFERRED-CLEANUP.md`, this phase's summaries, `153-EVIDENCE.md`,
  `153-VERIFICATION.md`, and the gitignored `dist/docs-library-v1.21.zip`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-PATTERNS.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-03-SUMMARY.md
@.planning/milestones/v1.20-phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Path-A copy from the UNCONVERTED predecessor source, recovered from git history and hash-verified</name>
  <files>scripts/validation/v1.21-milestone-audit.mjs</files>
  <precondition>Plan 153-03 recorded the pre-conversion commit object name and the pre-conversion blob hash of `scripts/validation/v1.20-milestone-audit.mjs` in `153-03-SUMMARY.md`; both are present and the recovery command is spelled out there.</precondition>
  <read_first>
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-03-SUMMARY.md — the recorded pre-conversion commit, the recorded blob hash, and the literal recovery command. This is the precondition for the whole task
    - .planning/milestones/v1.20-phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-06-PLAN.md lines 119-136 — the ratified statement of this rule, which the predecessor put in a task title precisely so it could not be skimmed past
    - scripts/validation/v1.20-milestone-audit.mjs lines 1-40 — the predecessor's header block, its usage line, its lineage chain, its check-count statement, its sidecar clause, and its frozen-predecessor reproducibility anchor. Every count and span string in it is re-derived, never copied
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-10, D-44, D-45, D-46
  </read_first>
  <action>
Recover the source, verify it, then fork it.

**Recover.** Run the recovery command recorded in the previous plan's summary —
`git show <recorded-commit>:scripts/validation/v1.20-milestone-audit.mjs` — and write the result to
`scripts/validation/v1.21-milestone-audit.mjs`. Immediately hash the new file and compare the hash to
the blob hash recorded in that summary. If they differ, stop: you have the wrong source. Do not
proceed on a mismatch and do not reconstruct the source by reverting the on-disk file.

Why this matters and nothing catches it: the on-disk predecessor now reads its corpus at its own
frozen close SHA. A fork of that form would make the new harness audit the previous milestone's
frozen tree instead of the current live one — green, and structurally wrong. Because the previous
plan converted two things in that file (the corpus reads and the C17 leg), the unconverted-source
rule is more load-bearing here than it was at the predecessor's own close.

**Fork.** Then repoint every literal that names the predecessor milestone:

- the header's title line, the lineage chain extended by one generation, and the ordinal statement —
  this is the **nineteenth** Path-A lineage bump, distinct from the eighteenth frozen-aware
  conversion ordinal, and both distinctions belong in the prose;
- the source-of-truth line, repointed to this phase's context document;
- the sidecar clause, repointed to the new sidecar file authored in Task 2, restated for this
  milestone rather than carried;
- the usage line;
- the sidecar read path inside the harness body;
- every count and span string in the header, **re-derived from the live checks array rather than
  carried from the predecessor's header prose**. The predecessor's own header says its count was
  taken from the live array for exactly this reason, and a sibling apex once shipped three stale
  runtime strings by copying instead.

Inherit C1 through C17 verbatim (D-44). Do not add an eighteenth check.

**State the residue (D-46).** Add a header clause recording that this harness is born live-HEAD,
including its C17 leg, because there is no close SHA for the current milestone to freeze against —
the back-anchor invariant is that a pin always references a past close. State that it therefore
becomes the successor milestone's sole unconverted harness, and name where that residue is tracked.
  </action>
  <verify>
    <automated>node scripts/validation/v1.21-milestone-audit.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - `git hash-object scripts/validation/v1.21-milestone-audit.mjs` taken immediately after the recovery, before any repointing, equals the blob hash recorded in `153-03-SUMMARY.md`. Both values are quoted in this plan's summary.
    - `node scripts/validation/v1.21-milestone-audit.mjs --verbose` exits 0 and its triple is quoted.
    - `grep -c "frozen-at-close" scripts/validation/v1.21-milestone-audit.mjs` returns 0 — the new harness is live-HEAD by construction, exactly as the recovered source was.
    - `grep -c "v1.20" scripts/validation/v1.21-milestone-audit.mjs` returns only hits that are deliberate lineage-history references; every operational literal names the current milestone. Each remaining hit is enumerated in the summary with a one-line justification.
    - The header states the nineteenth-lineage ordinal and distinguishes it from the conversion ordinal.
    - The header states the born-live-HEAD residue with its tracking location.
    - The registered check count in the header equals the length of the live checks array, proved by a runtime count printed from the file rather than by reading the header.
    - `git diff --stat` for this task lists exactly one new file, and `git status --porcelain scripts/validation/v1.20-milestone-audit.mjs` is empty.
  </acceptance_criteria>
  <reversibility rating="costly">A fork from the wrong source produces a green, structurally wrong harness that no gate detects; correcting it after the workflow and apex reference it means redoing three plans.</reversibility>
  <done>The new harness exists, was proved to come from the unconverted source by hash, runs green live-HEAD, and states both its ordinal and its residue.</done>
</task>

<task type="auto">
  <name>Task 2: The allowlist sidecar — header-fields-only copy, pins carried byte-identical, harness reference repointed</name>
  <files>scripts/validation/v1.21-audit-allowlist.json, scripts/validation/v1.21-milestone-audit.mjs</files>
  <read_first>
    - scripts/validation/v1.20-audit-allowlist.json — the whole file: its schema version, its generated timestamp, its phase field, and every pin array including the nested rotting-external object whose arrays a naive top-level walk misses
    - scripts/validation/v1.21-milestone-audit.mjs — the sidecar read and parse site, and the failure behaviour when the file is missing or malformed
    - scripts/validation/v1.20-milestone-audit.mjs line 4 — the predecessor's sidecar clause, for what a correctly-restated clause contains
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-47, D-48
  </read_first>
  <action>
Copy the predecessor sidecar to `scripts/validation/v1.21-audit-allowlist.json` and change **only the
header fields**: the generated timestamp, set to this phase's authoring date, and the phase field, set
to this phase's directory slug. Every pin array is carried byte-identical. The nested
rotting-external section is an object of nested arrays — verify after the copy that its nested arrays
survived, because a naive top-level walk undercounts the pinned file set by roughly half.

Repoint the harness's sidecar read to the new file. The CI workflow authored in the next plan greps
the harness for this exact path literal, so a missed repoint becomes a CI failure there.

Prove the parse fails loud rather than degrading: temporarily rename the sidecar, run the harness,
confirm it reports a failure rather than silently proceeding with an empty allowlist, then restore.

Record the sidecar's pinned-file-set size derived by a **recursive** walk, and state the naive
top-level number alongside it so the difference is on the record rather than rediscovered.
  </action>
  <verify>
    <automated>node -e "const j=require('./scripts/validation/v1.21-audit-allowlist.json');if(!j.schema_version||!j.generated||!j.phase)process.exit(1);console.log('sidecar OK '+Object.keys(j).length+' top-level keys')" &amp;&amp; node scripts/validation/v1.21-milestone-audit.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const a=require('./scripts/validation/v1.20-audit-allowlist.json'),b=require('./scripts/validation/v1.21-audit-allowlist.json');const sa=JSON.stringify(a),sb=JSON.stringify(b);const ka=Object.keys(a).join(','),kb=Object.keys(b).join(',');if(ka!==kb)process.exit(1);console.log('same top-level key set')"` prints the confirmation and exits 0.
    - A diff of the two sidecars with the two header fields excluded is empty — every pin array is byte-identical. The command used and its empty output are quoted.
    - The generated timestamp and the phase field both name this phase, not the predecessor's.
    - `grep -c "v1.21-audit-allowlist.json" scripts/validation/v1.21-milestone-audit.mjs` returns at least 1, and `grep -c "v1.20-audit-allowlist.json" scripts/validation/v1.21-milestone-audit.mjs` returns 0.
    - The fail-loud probe is recorded: the harness's output with the sidecar renamed is quoted, it is a failure and not a silent pass, and the restored run is green.
    - The recursive pinned-file-set size and the naive top-level size are both recorded, with the recursive one identified as correct.
    - `git status --porcelain scripts/validation/v1.20-audit-allowlist.json` is empty.
  </acceptance_criteria>
  <reversibility rating="reversible">A new JSON file and one path literal; revertible without touching any consumer that exists yet.</reversibility>
  <done>The new sidecar differs from its predecessor only in the two header fields, the harness reads it by name, and the parse is proved fail-loud.</done>
</task>

<task type="auto">
  <name>Task 3: Append BASELINE_25 and rule explicitly on the pin-drift instrument</name>
  <files>scripts/validation/regenerate-supervision-pins.mjs</files>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs around lines 540-575 — the `BASELINE_24` block in full, its opening comment line, its body shape, and the forward pointer at the end naming `BASELINE_25` as the next refresh
    - scripts/validation/regenerate-supervision-pins.mjs lines 285-295, 330-340 and 575-590 — the four hardcoded references to an old sidecar. Read them to know what NOT to edit and why the report mode cannot be cited as proof
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-47, D-48
    - .planning/STATE.md — the entry recording that a previously-deferred self-test item was dropped and closed at the predecessor close because the self-test now exits 0
  </read_first>
  <action>
Append a `BASELINE_25` block immediately following `BASELINE_24`, in the same shape: an opening
comment naming the refresh date, this phase and plan, and what carry-over it closes; then the body
following the predecessor block's structure; then a forward pointer naming `BASELINE_26` as the next
refresh at the next milestone close. Do not edit the four hardcoded old-sidecar references — they are
out of scope.

Then rule explicitly, in writing, on the instrument question (D-48):

- The helper's report mode is **never** cited as pin-drift proof anywhere in this phase. It hardcodes
  an old sidecar in four places and walks fewer than half of the pins. If a pin-drift claim is needed,
  it is made the way the predecessor made it: intersect the recursively-derived pinned file set with
  the changed-file set since the back-anchor SHA and adjudicate the resulting hunks line-granularly.
- Decide whether the helper's self-test is **run** in this phase and record the decision either way.
  The relevant history is that a previously-deferred self-test item was dropped and closed at the
  predecessor close precisely because that self-test now exits 0, and this task edits the file that
  owns it. Running it is cheap; the decision is that it is run and its result recorded, so that an
  edit to this file cannot silently break it.

Record both rulings in the summary, not only in a code comment.
  </action>
  <verify>
    <automated>node scripts/validation/regenerate-supervision-pins.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0 after the append, and its output is quoted in the summary alongside the same command's output taken before the append.
    - `grep -n "BASELINE_25" scripts/validation/regenerate-supervision-pins.mjs` returns the new block's opening line and the position is after the predecessor block, not before it.
    - `grep -n "BASELINE_26" scripts/validation/regenerate-supervision-pins.mjs` returns the new forward pointer.
    - The four hardcoded old-sidecar line references are unchanged: a diff of the file limited to those line regions is empty.
    - The summary records, in prose, that the helper's report mode is not cited as pin-drift proof anywhere in this phase, and states the method that is used instead if such a claim is made.
    - The summary records the explicit decision on running the self-test and its result.
    - `git diff --stat` for this task lists exactly one file.
  </acceptance_criteria>
  <reversibility rating="reversible">A comment block append; revertible by file checkout.</reversibility>
  <done>`BASELINE_25` sits where the forward pointer named it, the self-test still exits 0, and both instrument rulings are on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history to new file | The new harness's content comes from a historical blob rather than from the working tree. |
| harness to sidecar | The harness's allowlist behaviour is driven entirely by a JSON file read by path. |
| pin-drift instrument to evidence | A helper with hardcoded stale paths can be mistaken for a drift proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-16 | Tampering | the Path-A copy source | high | mitigate | The recovered file's blob hash is compared to the hash recorded before the predecessor was converted, and a mismatch stops the task. Reconstructing the source by reverting the on-disk file is prohibited. |
| T-153-17 | Spoofing | an unrepointed sidecar reference | high | mitigate | A grep asserts the new path is present and the predecessor path absent in the harness, and the next plan's CI job independently greps the same literal. |
| T-153-18 | Information Disclosure | a silently empty allowlist | medium | mitigate | The parse is proved fail-loud by renaming the sidecar and quoting the resulting failure, and the recursive pinned-file-set size is recorded so an undercounting walk is visible. |
| T-153-19 | Repudiation | citing an unreliable instrument as drift proof | medium | mitigate | The instrument's four hardcoded stale references are read and named; a written ruling bars citing its report mode and states the method used instead. |
| T-153-20 | Denial of Service | a stale count or span string carried from the predecessor header | medium | mitigate | Every count and span string is re-derived from the live checks array and the count is proved by a runtime print rather than by reading the header. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs. One recovered file, one JSON file, one comment append, and Node validators using built-ins only. |

ASVS level: 1. Blocking threshold: high. Both high-severity entries are mitigated by computed
acceptance criteria — a blob-hash equality and a two-sided grep.
</threat_model>

<verification>
- `node scripts/validation/v1.21-milestone-audit.mjs --verbose` exits 0.
- `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0.
- The recovered blob hash equals the hash recorded before the predecessor was converted.
- The two sidecars differ only in their two header fields.
- The predecessor harness and its sidecar are untouched by this plan.
</verification>

<success_criteria>
- The nineteenth Path-A generation exists as one unit — harness, sidecar and baseline (HARN-04, SC#3).
- The harness was forked from the unconverted source, proved by hash, and is live-HEAD by
  construction with its residue stated.
- No eighteenth check was folded in.
- Both instrument rulings are written down rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-04-SUMMARY.md` when done.

Record: the recovery command, both blob hashes and their equality; the harness triple; the enumerated
remaining predecessor-milestone references with justifications; the sidecar diff-minus-header-fields
result; the fail-loud probe output; the recursive and naive pinned-set sizes; the self-test result
before and after; and both instrument rulings in prose.
</output>
