---
phase: 153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a
verified: 2026-08-30T17:29:08Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: passed
  previous_score: 6/6 must-haves verified
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 153: Harness Close — V120 Pin, C17 Frozen-Aware Residue & 19th Path-A Lineage Bump — INDEPENDENT Verification Report

**Note on this report:** a prior `153-VERIFICATION.md` existed, authored by the same executor that
performed the close gate. This report was produced INDEPENDENTLY — every claim below was
re-derived directly from the codebase, git history, and the GitHub API in a fresh verification
pass, not read off the prior report's prose. The prior report is cited only twice below, both times
to note it agreed with something I independently re-measured. No claim here rests on the prior
report's word.

**Phase Goal:** The milestone can close with an apex that is genuinely green **and** genuinely
frozen — both carried harness debts discharged, and the close evidence read at job level rather
than from a run colour.
**Verified:** 2026-08-30T17:29:08Z (independent pass)
**Status:** passed
**Re-verification:** Yes — independent re-derivation superseding the self-graded initial pass

## Goal Achievement

### Observable Truths (the five ROADMAP.md Success Criteria plus the close-gate flip)

| # | Truth | Status | Evidence (independently reproduced) |
|---|-------|--------|--------------------------------------|
| 1 | `V120` close-SHA entry inserted **before** `V14`, never appended; reader/enumerator exports follow the established pattern; pin proven end to end by a real frozen read | ✓ VERIFIED | `_lib/frozen-at-close.mjs:171` `V120: '246fa3dd'` precedes `V14` at `:188`. `git log --format='%H %s' -1 246fa3dd` resolves to a real commit (`docs(144-12): v1.20 MILESTONE CLOSE...`) — the SHA is not fabricated. Live ESM import + call: `readAtV120Close('docs/index.md')` returned 37,078 non-empty bytes; `lsTreeAtV120Close('docs')` returned 296 entries. Both exports present at `:261`/`:330`. `V-140-V14PIN` (`check-phase-140.mjs`) independently re-run: PASS. |
| 2 | All **six** C17-bearing harnesses (v1.15-v1.20) converted simultaneously via a `cwd` swap; `c17-eee-contract.mjs` byte-unchanged; existence guard live | ✓ VERIFIED | Ran all six harnesses directly, myself, this session: `node scripts/validation/v1.1{5,6,7,8,9}-milestone-audit.mjs` and `v1.20-milestone-audit.mjs` — **all six exit "16 passed, 0 failed, 0 skipped"**, each C17 leg (`[17/16]`) reporting PASS against its own frozen corpus via `withDocsAtClose('V11N', …)`/`withDocsAtClose('V120', …)`. Confirmed `c17-eee-contract.mjs` byte-unchanged: `git diff 246fa3dd -- scripts/validation/c17-eee-contract.mjs` is empty, and its last touching commit (`e9a505be`, Phase 143) predates this phase entirely. v1.20's harness additionally shows the full SWEEP-05 conversion (`createFrozenCorpusReader`, `MILESTONE_TAG = 'V120'`) for its C1-C16 legs, not just the C17 leg (D-09's asymmetric scope). |
| 3 | The 19th Path-A lineage bump exists — harness, allowlist sidecar, baseline — plus leaf validators for Phases 145-152 and the apex, chain range generated by arithmetic, never transcribed | ✓ VERIFIED | `v1.21-milestone-audit.mjs` (53,391 bytes) and `v1.21-audit-allowlist.json` (35,056 bytes) both on disk. **Load-bearing property independently confirmed:** `grep -n "frozen-at-close\|AtClose" v1.21-milestone-audit.mjs` returns **zero matches** — the harness is genuinely live-HEAD, using plain `fs.readFileSync` and a bare `execFileSync` C17 spawn (no `withDocsAtClose`). Ran it myself: 16 passed, 0 failed, 0 skipped. `BASELINE_25` present in `regenerate-supervision-pins.mjs:567`. All 8 leaves (`check-phase-145.mjs`..`-152.mjs`) run individually by me: all PASS (16/7/8/9/9/17/7/9 checks respectively, 0 FAIL each). `check-phase-153.mjs` (`:151`): `CHAIN_PHASES = Array.from({ length: CHAIN_END - CHAIN_START + 1 }, (_, i) => CHAIN_START + i)` — genuine arithmetic, not a literal array. |
| 4 | The 18th CI workflow born with full fetch depth; publish bundle regenerated at this milestone's version | ✓ VERIFIED | `.github/workflows/audit-harness-v1.21-integrity.yml` exists (18th of 18 `.yml`/`.yaml` files in `.github/workflows/`). Counted independently: 16 `uses: actions/checkout@v4` steps, 16 `fetch-depth: 0` lines — exact parity. `harness-run` job's `run:` line actually invokes `node scripts/validation/v1.21-milestone-audit.mjs --verbose` (not a copied predecessor command). `frozen-read-probe` job carries no `needs:` key (line 297) — `V-139-PROBEJOB` and `V-139-FETCHDEPTH`, independently re-run, both PASS across all 18 workflow files. `dist/docs-library-v1.21.zip` exists (4,070,799 bytes; unzip lists 238 files, 237-line manifest.csv = 236 rows + header, matching the D-68 236-canary claim); `dist/docs-library-v1.17.zip` is absent from `dist/`. |
| 5 | A 3-axis terminal re-audit captured at **one shared SHA**, every workflow dispatched, skips read as gaps not passes, job-level not run-colour | ✓ VERIFIED | Independently queried GitHub via `gh run list --json … | filter headSha==478e633e78e9670b31db1f39e7660c9f0e9c888c && event==workflow_dispatch` — **exactly 18 runs**, all `conclusion: success` at the run level, all at that one shared SHA (confirmed on `origin/master`'s tip). Pulled `gh run view --json jobs` for all 18 runs and tallied at job level myself (not from any narrative): **227 total jobs, 211 `success`, 16 `skipped`, 0 `failure`/`cancelled`/`timed_out`.** The 16 skips are all named `Quarterly c13_rotting_external link-check`, present in exactly 16 of the 18 workflow files (the two base/earliest workflows carry no such job at all) — matches the documented "16 of 18" legitimate cron-gated skip anchor. Of the 211 successes, 18 are a `pin-helper-advisory`/`Supervision-pin drift advisory (CI)` job with `continue-on-error: true` (one per workflow, confirmed by direct grep and by name-matching every job list) — these are structurally-always-success non-evidence jobs per D-54, leaving 193 genuine evidence-bearing successes. `211 - 18 = 193`; `193 + 16 + 18 = 227` reconciles exactly against the independently-observed total. Zero illegitimate (cascade) skips anywhere. |
| 6 | Close gate is a SINGLE commit flipping all 58 requirements to Validated; `REQUIREMENTS.md` never removed | ✓ VERIFIED | `git show --stat e129081e`: touches exactly `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` (4 files). `grep -c "^- \[x\]" .planning/REQUIREMENTS.md` = 58; `grep -c "^- \[ \]"` = 0. `git ls-files .planning/REQUIREMENTS.md` confirms it is tracked (never `git rm`'d). A separate commit `fd0a783a` (post-gate apex record + this VERIFICATION.md) touches **zero** gate files — confirmed via `git show --stat fd0a783a`, which lists only `153-14-SUMMARY.md`, `153-EVIDENCE.md`, `153-VERIFICATION.md`. Commit message of `e129081e` contains no CARVE-path or amended-file-path prose (D-76 check). |

**Score:** 6/6 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/validation/_lib/frozen-at-close.mjs` | `V120` before `V14`, `readAtV120Close`/`lsTreeAtV120Close`, `materializeDocsAtClose`/`withDocsAtClose` | ✓ VERIFIED | All four symbols exported; live frozen read against 246fa3dd returns real bytes |
| Six C17-bearing harnesses (v1.15-v1.20) | `withDocsAtClose` cwd-swap spawn, byte-unchanged contract | ✓ VERIFIED | All six ran live by me, 16/0/0 each |
| `v1.21-milestone-audit.mjs` + `v1.21-audit-allowlist.json` | 19th Path-A lineage, live-HEAD | ✓ VERIFIED | Zero `frozen-at-close` references; ran live, 16/0/0 |
| `check-phase-145.mjs`..`-152.mjs` | 8 content leaves | ✓ VERIFIED | All 8 ran individually, all PASS, real (non-vacuous) needles spot-checked |
| `check-phase-153.mjs` | Apex, arithmetic chain `[48..152]`+`[30,31]` | ✓ VERIFIED | Ran non-nested (`CHECK_PHASE_NESTED` confirmed unset): **111 PASS, 0 FAIL, 0 SKIPPED** |
| `.github/workflows/audit-harness-v1.21-integrity.yml` | 18th CI workflow, full fetch depth | ✓ VERIFIED | 16/16 checkout parity; runs the correct harness and apex |
| `dist/docs-library-v1.21.zip` | Publish bundle at `--version=v1.21` | ✓ VERIFIED | 4,070,799 bytes, 238-file archive, valid zip listing |
| `.planning/milestones/v1.21-MILESTONE-AUDIT.md` / `v1.21-DEFERRED-CLEANUP.md` | Terminal close artifacts | ✓ VERIFIED | Both on disk; deferred-cleanup correctly closes `V120-PIN-DEFERRAL`/`C17-FROZEN-AWARE-RESIDUE-V15-V19` and opens `V121-PIN-DEFERRAL`/`C17-FROZEN-AWARE-RESIDUE-V21` in writing (D-67) |
| Close-gate commit | Exactly 4 files, 58/58 Validated | ✓ VERIFIED | `e129081e`, confirmed |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `check-phase-153.mjs` | `check-phase-{30,31,48..152}.mjs` | subprocess spawn, `subEnv.CHECK_PHASE_NESTED='1'` | ✓ WIRED | Non-nested run by me: 105 CHAIN_PHASES + 2 CHAIN_EXTRA children, all PASS |
| `check-phase-153.mjs` | `v1.21-milestone-audit.mjs` | `V-153-AUDIT-HARNESS` | ✓ WIRED | PASS, independently re-run; `HARNESS` const correctly repointed (not the D-46-analogue defect) |
| Six C17 harnesses | `c17-eee-contract.mjs` | `withDocsAtClose` cwd-swap | ✓ WIRED | `V-153-C17CARVEOUT-SUCCESSOR` PASS; all six also individually re-run standalone by me with real corpora |
| 18th workflow's `harness-run` job | `v1.21-milestone-audit.mjs` | `run:` step | ✓ WIRED | Confirmed by reading the literal `run:` line, not inferred — it is not a copy of a predecessor's command |
| Close-gate commit | `check-phase-54.mjs` live reads | direct file reads of `REQUIREMENTS.md`/`ROADMAP.md` | ✓ WIRED | Re-run by me post-gate: 32 PASS, 0 FAIL, 0 SKIPPED |

### Data-Flow / Evidence-Chain Trace

| Claim | Source queried directly | Independently reproduced value | Matches phase's own record? |
|-------|--------------------------|--------------------------------|------------------------------|
| Shared dispatch SHA | `git log origin/master -1` | `478e633e78e9670b31db1f39e7660c9f0e9c888c` | Yes |
| Dispatch run count | `gh run list --json headSha,event` | 18 runs, all `workflow_dispatch`, all matching SHA, all `conclusion: success` | Yes |
| Job-level total | `gh run view --json jobs` × 18, summed by me | 227 jobs | Yes |
| Job-level success/skip/fail | same, tallied by me | 211 success / 16 skipped / 0 failed | Reconciles to the phase's own 193(evidence)+18(non-evidence)+16(skip) split once the 18 always-green advisory jobs are excluded |
| Atom branches | `git ls-remote --heads origin` | `phase-119-atom-2`→`652f48e7…`, `phase-125-atom-2`→`ce62fe58…`, `phase-128-atom-2`→`4e89d68c…`, `phase-139-atom-5`→`c2450efa…` | Yes, exact SHA match to `153-EVIDENCE.md`'s table |
| Non-nested apex triple | `node scripts/validation/check-phase-153.mjs` (env confirmed clean) | 111 PASS / 0 FAIL / 0 SKIPPED | Yes |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All six frozen-aware harnesses actually execute against real frozen corpora | `node scripts/validation/v1.1{5..9}-milestone-audit.mjs`, `v1.20-milestone-audit.mjs` | 6× "16 passed, 0 failed, 0 skipped" | ✓ PASS |
| v1.21 harness is genuinely live-HEAD, not silently frozen | `grep -n "frozen-at-close\|AtClose" v1.21-milestone-audit.mjs` | exit 1 (no match) | ✓ PASS |
| v1.21 harness executes clean | `node scripts/validation/v1.21-milestone-audit.mjs` | 16 passed, 0 failed, 0 skipped | ✓ PASS |
| Apex is non-nested and green | `echo $CHECK_PHASE_NESTED` (empty) then `node scripts/validation/check-phase-153.mjs` | 111 PASS, 0 FAIL, 0 SKIPPED | ✓ PASS — the apex's own required `Phase 153` heading (`V-153-AUDIT`) is satisfied by this file's title, confirmed by re-reading the check's regex requirement (`/Phase 153/i`) |
| Dispatch evidence is job-level, not run-colour | `gh run view <id> --json jobs` for all 18 IDs | 227 jobs enumerated, tallied by conclusion | ✓ PASS |
| `check-phase-139` binds the new workflow | `node scripts/validation/check-phase-139.mjs` | `V-139-PROBEJOB` PASS, `V-139-FETCHDEPTH` PASS, 5/5 total | ✓ PASS |
| No debt markers in phase-153-authored files | `grep -n -E "TBD\|FIXME\|XXX"` across the 20 changed `scripts/`/`.github/` files | 0 matches | ✓ PASS |
| No unresolved TODO/HACK/PLACEHOLDER (excluding pre-existing domain vocabulary) | `grep -n -E "TODO\|HACK\|PLACEHOLDER"` | 1 hit — a pre-existing `'TODO: '` string-template inside a human-facing stub-suggestion generator (`regenerate-supervision-pins.mjs:352`), not new debt | ✓ PASS (not a gap) |
| Anti-vacuous-green known-member guard is honest about its own limit | read `v1.20-milestone-audit.mjs:847-860` | Explicitly discloses it cannot distinguish V120 from V119 (byte-identical `docs/` trees) and instead distinguishes from V118 — matches the pre-disclosed known deviation exactly | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention applies to this phase (harness/validator phase, not a migration/tooling-probe phase); none declared in any PLAN/SUMMARY. Skipped — no probes to run.

### Requirements Coverage

| Requirement | Source Plan(s) | Status | Evidence |
|-------------|-----------------|--------|----------|
| HARN-01 | 153-01 | ✓ SATISFIED | Truth 1 |
| HARN-02 | 153-01 | ✓ SATISFIED | Truth 1 |
| HARN-03 | 153-01/02/03/10 | ✓ SATISFIED | Truth 2 |
| HARN-04 | 153-04/06/07/08/09 | ✓ SATISFIED | Truth 3 |
| HARN-05 | 153-05/14 | ✓ SATISFIED | Truth 4 |
| HARN-06 | 153-10/11/12/13/14 | ✓ SATISFIED | Truth 5, Truth 6 |

**No orphaned requirements** — all 6 phase requirement IDs are claimed by at least one PLAN's `requirements:` frontmatter, cross-checked against `.planning/REQUIREMENTS.md` §HARN (lines 105-110), all six marked `[x]`.

### Anti-Patterns Found

None found as a BLOCKER or WARNING. The single `TODO:` hit and the several `template_placeholder`/`placeholder` hits are pre-existing domain vocabulary (link-allowlist categorization, human-review stub generator) untouched in character by this phase's edits — confirmed by reading the surrounding code, not just the grep line.

### Known Deviations (pre-disclosed, confirmed handled as described — not new findings)

All eight items pre-disclosed in the task's `<known_deviations_already_documented>` block were checked against the actual code and confirmed present and handled honestly:
- The known-member-path guard's V119/V120 (and V115/V116/V117) unsatisfiability is disclosed **in the code itself** (`v1.20-milestone-audit.mjs:847-856`), with the residual gap named and the nearest-differing-predecessor fallback documented and correctly implemented.
- `check-phase-145`'s archival-drift check uses a markdown-link-syntax regex (`/\]\([^)]*\.planning\/(research|phases)\//`), not a bare grep — confirmed by reading the assertion body.
- `check-phase-148`/`151` carry no live, unpinnable `git diff <SHA>` assertions — confirmed via grep (only comment-text mentions remain).
- The apex derives 111 checks (not CONTEXT's 110) — confirmed: 1 AUDIT + 105 CHAIN + 2 CHAIN_EXTRA + 1 AUDIT-HARNESS + 1 C17CARVEOUT-SUCCESSOR + 1 SELF = 111.
- Axis-3's non-context-independence disclosure survives in `.planning/milestones/v1.21-MILESTONE-AUDIT.md:47-93` and `:368-390` — confirmed present, not dropped.
- `.planning/REQUIREMENTS.md` is tracked, never `git rm`'d; `dist/docs-library-v1.21.0.zip` (Phase 152's UAT artifact) is untouched and distinct from this phase's `v1.21.zip`.

### Human Verification Required

None. This phase is entirely code/harness/CI infrastructure with no user-facing surface; every truth was verifiable by direct execution, git history inspection, or GitHub API query.

## Lifecycle Handoff (unchanged from the prior self-assessment, independently confirmed accurate)

`.planning/STATE.md` and `.planning/PROJECT.md` both correctly represent code review, user-acceptance
testing, `/gsd-audit-milestone`, and `/gsd-complete-milestone` as **still pending**, owner-run steps
after this phase — this phase does not overclaim completion of any of them. This was independently
confirmed by reading the current STATE.md/PROJECT.md prose, not merely accepting the prior
VERIFICATION.md's identical claim.

## Verdict

All 6 must-haves independently re-derived as VERIFIED. Every load-bearing claim — the SHA
resolution, the six-harness conversion, the live-HEAD property of the 19th lineage, the workflow's
job identities, the job-level dispatch tally, the close-gate commit shape, and the non-nested apex
triple — was reproduced by direct command execution or API query in this pass, not read from any
prior report. No gaps found. No fabricated measurements found. The one apparent numeric
discrepancy investigated (193 vs. 211 "successes") resolved to a documented, correctly-implemented
distinction (evidence-bearing vs. non-evidence advisory jobs), not an error.

---
*Phase: 153-Harness Close — V120 Pin, C17 Frozen-Aware Residue & 19th Path-A Lineage Bump*
*Verified independently: 2026-08-30T17:29:08Z*
*Verifier: Claude (gsd-verifier, independent re-derivation pass)*
