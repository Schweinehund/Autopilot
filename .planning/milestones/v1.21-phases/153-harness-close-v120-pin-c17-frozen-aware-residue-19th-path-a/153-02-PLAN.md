---
phase: 153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a
plan: 02
type: execute
wave: 2
depends_on: ["153-01"]
files_modified:
  - scripts/validation/v1.16-milestone-audit.mjs
  - scripts/validation/v1.17-milestone-audit.mjs
  - scripts/validation/v1.18-milestone-audit.mjs
  - scripts/validation/v1.19-milestone-audit.mjs
autonomous: true
requirements: [HARN-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The four remaining identical C17 legs — v1.16, v1.17, v1.18, v1.19 — are converted by the same working-directory swap the tracer proved, with the contract validator byte-unchanged and the existence guard live (D-11, HARN-03)."
    - "Each converted leg carries its OWN milestone-unique known-member path, derived per milestone; a count is never the primary guard, because enrolled counts collide across these four milestones while the trees genuinely differ (D-16)."
    - "The contract path is absolutized before the working directory is swapped in every one of the four legs, not only in the tracer (D-15)."
    - "Only the documentation tree is materialized in every leg; nothing outside it is written into the temp directory (D-12)."
    - "The conversion is a working-directory swap and not a code change to the contract validator, and no leg reaches for a worktree or an external extraction binary (D-11, D-14)."
    - "The five identical-leg harnesses (v1.15 from the tracer plus these four) are complete before the sixth harness's evidence is captured, honouring the simultaneity the requirement actually governs (D-08, D-09)."
    - statement: "HARN-03 adjacency edge: two milestones whose enrolled counts are exactly equal (v1.16 and v1.17 both 229; v1.19 and v1.20 both 234) must not be distinguishable only by count. Each known-member path is asserted present in its own milestone's frozen tree AND absent from both neighbours' before it is accepted."
      verification: explicit
    - statement: "HARN-03 ordering edge: the four legs are independent and their conversion order is not specified; each is proved by its own harness run, so no leg's result depends on another leg having run first."
      verification: explicit
    - statement: "HARN-03 empty edge (probe row, resolved here): an empty materialization exits the contract validator 0 silently. Each leg carries a non-zero materialized-count assertion alongside its known-member assertion."
      verification: explicit
  artifacts:
    - scripts/validation/v1.16-milestone-audit.mjs
    - scripts/validation/v1.17-milestone-audit.mjs
    - scripts/validation/v1.18-milestone-audit.mjs
    - scripts/validation/v1.19-milestone-audit.mjs
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-02-SUMMARY.md
  key_links:
    - "All four legs are byte-identical to each other at the phase base, so a divergence introduced here is a maintenance liability across four files at once — the four edits must produce four structurally identical legs differing only in milestone tag and known-member path."
    - "The carve-out check in `check-phase-140.mjs` scopes to v1.15 through v1.18 and asserts only a positive name reference, which survives every one of these edits; its name and detail string do not, and the successor is authored in the apex plan."
  prohibitions:
    - "`scripts/validation/c17-eee-contract.mjs` must be byte-unchanged. This plan reads it and never edits it."
    - "`scripts/validation/check-phase-140.mjs` must be byte-unchanged. Its stale name and detail string are corrected additively in a successor, never by editing the frozen file."
    - "No worktree creation and no external archive or extraction binary in any leg."
    - "No `docs/` content file is created, edited or deleted."
    - "Neither glossary frontmatter date field is touched."
    - "No summary or commit message may claim a chain result gathered without the nested environment flag (D-75)."
    - "Commit message bodies must not literally quote governance-carve paths or the paths of amended files in prose (D-76)."
    - "No frozen validator line is edited before grepping the validator tree for that exact call-site string (D-79)."
    - "No bare-noun platform blockquote token at line start outside a fence in any `.planning/` file this plan writes (D-43)."
---

<objective>
Replicate the proven conversion across the remaining four identical C17 legs — v1.16, v1.17, v1.18
and v1.19 — each against its own frozen corpus, each with its own milestone-unique known-member
guard.

Purpose: HARN-03's simultaneity requirement exists because converting a subset leaves the identical
drift class on the remainder. The tracer proved one; this plan completes the identical-leg set so the
five are indistinguishable in shape.

Output: four converted harnesses, each exiting 0 against its own frozen corpus with its C17 leg
naming the milestone tag it read.
</objective>

<deviation_note>
**On D-09's "two plans, not one".** D-09 splits the conversion because the work is 5:1 asymmetric.
That split is intact: the sixth harness has its own dedicated plan (153-03) and is never fused with
this identical-leg work. The five identical legs are spread across plan 01 (the tracer, one leg) and
this plan (four legs) only because TRACER_MODE requires the architecture be proven end to end before
replication. D-08's simultaneity governs evidence capture, not commit count: all six conversions land
before plan 153-10 captures any close evidence.
</deviation_note>

## Artifacts this phase produces

Created by Phase 153, absent at the phase base; treat as new, not as unacknowledged references.

- **New exports in `scripts/validation/_lib/frozen-at-close.mjs`:** `MILESTONE_CLOSE_SHAS.V120`,
  `readAtV120Close`, `lsTreeAtV120Close`, `materializeDocsAtClose`, `withDocsAtClose`.
- **New validators:** `scripts/validation/check-phase-145.mjs` through `check-phase-152.mjs` and
  `scripts/validation/check-phase-153.mjs`.
- **New check ids:** `V-145-*` through `V-152-*`; on the apex `V-153-AUDIT`, `V-153-CHAIN-<n>`,
  `V-153-AUDIT-HARNESS`, `V-153-SELF`, `V-153-C17CARVEOUT-SUCCESSOR`.
- **New harness lineage:** `scripts/validation/v1.21-milestone-audit.mjs`,
  `scripts/validation/v1.21-audit-allowlist.json`, `BASELINE_25` in
  `scripts/validation/regenerate-supervision-pins.mjs`.
- **New CI workflow:** `.github/workflows/audit-harness-v1.21-integrity.yml`.
- **New planning artifacts:** `.planning/milestones/v1.21-MILESTONE-AUDIT.md`,
  `.planning/milestones/v1.21-DEFERRED-CLEANUP.md`, this phase's summaries, `153-EVIDENCE.md`,
  `153-VERIFICATION.md`, and the gitignored `dist/docs-library-v1.21.zip`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-PATTERNS.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-01-SUMMARY.md
@scripts/validation/v1.15-milestone-audit.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the four known-member paths, then convert the v1.16 and v1.17 legs</name>
  <files>scripts/validation/v1.16-milestone-audit.mjs, scripts/validation/v1.17-milestone-audit.mjs</files>
  <read_first>
    - scripts/validation/v1.15-milestone-audit.mjs — the C17 leg AS CONVERTED by plan 153-01 Task 3. This is the shape to replicate exactly; the only per-file differences are the milestone tag and the known-member path
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-01-SUMMARY.md — how the v1.15 known-member path was derived, and the negative-probe procedure
    - scripts/validation/v1.16-milestone-audit.mjs and scripts/validation/v1.17-milestone-audit.mjs — each file's C17 leg in full (around lines 816-845), including the exact detail strings and the shared failure-detail call shape. Confirm neither carries the exception comment block that only v1.15 had
    - scripts/validation/_lib/frozen-at-close.mjs — `withDocsAtClose`'s signature and what it hands the callback
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-11, D-12, D-15, D-16, D-17, D-79
  </read_first>
  <action>
Derive first, edit second.

**Derivation (D-16).** For each of the four milestones V116, V117, V118 and V119, list that
milestone's frozen documentation tree and both neighbours' trees, and choose one path present in the
milestone's own tree and absent from both neighbours'. The adjacency hazard is real and measured:
enrolled counts are 174, 229, 229, 232, 234, 234 for V115 through V120, so V116 and V117 are
count-identical and V119 and V120 are count-identical while the trees themselves differ. Record all
four chosen paths and the command that produced them in the summary. If a milestone has no uniquely
present path against both neighbours, widen the comparison to the full set of six frozen trees and
record that you did.

**Conversion.** Apply the tracer's converted leg shape to `v1.16-milestone-audit.mjs` and
`v1.17-milestone-audit.mjs`: absolutize the contract path before the swap, keep the existence guard
against the live repository root with its current failure string, wrap the spawn in
`withDocsAtClose` with the file's own milestone tag, assert the milestone-unique known-member path
before the spawn, carry the non-zero materialized-count assertion as the secondary guard, and leave
the timeout, stdio mode, argument-array invocation and failure-detail call shape untouched. The two
converted legs must be structurally identical to each other and to v1.15's, differing only in tag and
known-member path.

Each file's line endings may differ from its siblings — assert every replacement matched rather than
assuming a bare string replace found its target.

Grep the validator tree for the exact call-site strings on the lines you change before changing them.
  </action>
  <verify>
    <automated>node scripts/validation/v1.16-milestone-audit.mjs --verbose &amp;&amp; node scripts/validation/v1.17-milestone-audit.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/v1.16-milestone-audit.mjs --verbose` exits 0 with its C17 line PASS, and the detail names the V116 tag.
    - `node scripts/validation/v1.17-milestone-audit.mjs --verbose` exits 0 with its C17 line PASS, and the detail names the V117 tag.
    - The four derived known-member paths are recorded in the summary together with the command that produced them, and each is shown present in its own milestone's tree and absent from both neighbours' by a quoted `git ls-tree` result.
    - The V116 path and the V117 path are different from each other, and neither is present in the other's frozen tree — the count-identical adjacency case is disproved by path, not by count.
    - `git diff --stat` for this task lists exactly two files.
    - `git diff scripts/validation/c17-eee-contract.mjs` and `git diff scripts/validation/check-phase-140.mjs` are both empty.
    - A negative probe is run on one of the two legs: point the wrapper at a different milestone tag, confirm the known-member assertion FAILS, revert, confirm green. Both outputs are quoted in the summary.
  </acceptance_criteria>
  <reversibility rating="reversible">Two harness legs; revertible by file checkout with no consumer downstream until the evidence plan.</reversibility>
  <done>Both legs spawn the byte-unchanged contract validator against their own frozen corpora, each guarded by its own milestone-unique path, and both harnesses exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the v1.18 and v1.19 legs and prove the five-leg set is structurally uniform</name>
  <files>scripts/validation/v1.18-milestone-audit.mjs, scripts/validation/v1.19-milestone-audit.mjs</files>
  <read_first>
    - scripts/validation/v1.16-milestone-audit.mjs — the leg as converted in Task 1, the immediate template
    - scripts/validation/v1.18-milestone-audit.mjs and scripts/validation/v1.19-milestone-audit.mjs — each file's C17 leg in full, including its exact detail strings
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-02-SUMMARY.md drafts or Task 1 notes — the derived V118 and V119 known-member paths
    - scripts/validation/check-phase-140.mjs lines 149-165 — the carve-out check, to run and confirm rather than to edit
  </read_first>
  <action>
Apply the same converted-leg shape to `v1.18-milestone-audit.mjs` and `v1.19-milestone-audit.mjs`
using the V118 and V119 tags and the V118 and V119 known-member paths derived in Task 1.

Then prove uniformity across the five identical legs rather than asserting it: extract each of the
five converted legs and compare them pairwise, confirming the only differences are the milestone tag
literal and the known-member path literal. Any third difference is a divergence and must be removed
before this task is done — five files diverging in five ways is exactly the maintenance liability the
shared helper exists to prevent.

Run all five harnesses and record all five triples.

Grep the validator tree for the exact call-site strings on the lines you change before changing them.
  </action>
  <verify>
    <automated>node scripts/validation/v1.18-milestone-audit.mjs --verbose &amp;&amp; node scripts/validation/v1.19-milestone-audit.mjs --verbose &amp;&amp; node scripts/validation/check-phase-140.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/v1.18-milestone-audit.mjs --verbose` and `node scripts/validation/v1.19-milestone-audit.mjs --verbose` each exit 0 with C17 PASS and a detail naming their own tag.
    - All five harnesses v1.15 through v1.19 are run in one sequence and all five exit 0; the five triples are quoted in the summary.
    - The pairwise leg comparison is recorded: the only per-file differences across the five converted legs are the milestone tag literal and the known-member path literal.
    - `node scripts/validation/check-phase-140.mjs` exits 0 and its carve-out line reports PASS.
    - `git diff --stat` for this task lists exactly two files.
    - `git diff 246fa3dd..HEAD -- scripts/validation/c17-eee-contract.mjs` is empty.
    - `for f in scripts/validation/v1.1{5,6,7,8,9}-milestone-audit.mjs; do grep -c withDocsAtClose "$f"; done` returns 1 for each of the five files.
  </acceptance_criteria>
  <reversibility rating="reversible">Two harness legs; revertible by file checkout.</reversibility>
  <done>All five identical-leg harnesses are converted, structurally uniform, and green against their own frozen corpora.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen tree to temp directory | Four more materializations write git blobs outside the repository. |
| harness process to spawned contract validator | Four more child processes are launched with a swapped working directory. |
| milestone tag to known-member path | The guard that distinguishes a correct materialization from a neighbour's is a single path literal per file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-07 | Information Disclosure | a leg materializing a neighbouring milestone's corpus | high | mitigate | Each of the four legs carries its own known-member path proved present in its own frozen tree and absent from both neighbours' by quoted tree listings. The count-identical adjacency pairs are disproved by path, and one leg is proved failable by a negative probe. |
| T-153-08 | Tampering | four near-identical edits diverging silently | medium | mitigate | Task 2 compares the five converted legs pairwise and requires that the only differences are the tag and the known-member path; a third difference blocks the task. |
| T-153-09 | Elevation of Privilege | subprocess invocation in four more legs | high | mitigate | Every leg reuses the tracer's argument-array invocation with the contract path absolutized before the swap; no shell string, no worktree, no external extraction binary. Proved by grep across all five files. |
| T-153-10 | Denial of Service | four more temp directories on a Windows host | medium | mitigate | Cleanup is inside the shared helper with retry options; no leg owns its own cleanup path, so there is one implementation to get right rather than five. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs. Four file edits and Node validators using built-ins only. |

ASVS level: 1. Blocking threshold: high. Both high-severity entries are mitigated by computed
acceptance criteria — quoted tree listings per milestone and a grep-proved invocation shape.
</threat_model>

<verification>
- All five harnesses v1.15 through v1.19 exit 0 with C17 PASS.
- `node scripts/validation/check-phase-140.mjs` exits 0.
- `git diff 246fa3dd..HEAD -- scripts/validation/c17-eee-contract.mjs` is empty.
- `git diff 246fa3dd..HEAD -- scripts/validation/check-phase-140.mjs` is empty.
- Exactly four files are modified by this plan.
</verification>

<success_criteria>
- The identical-leg set is complete: five C17-bearing harnesses converted by working-directory swap,
  the contract validator byte-unchanged, every existence guard live (HARN-03).
- Each leg carries a milestone-unique known-member guard, not a count.
- The five legs are structurally uniform, proved by comparison rather than asserted.
</success_criteria>

<output>
Create `.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-02-SUMMARY.md` when done.

Record: the four derived known-member paths with the tree listings proving uniqueness; all five
harness triples; the pairwise uniformity comparison; and the negative-probe output.
</output>
