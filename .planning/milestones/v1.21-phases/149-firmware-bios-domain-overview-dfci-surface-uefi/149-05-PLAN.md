---
phase: 149-firmware-bios-domain-overview-dfci-surface-uefi
plan: 05
type: execute
wave: 4
depends_on: ["149-04"]
files_modified:
  - docs/operations/firmware-bios/01-windows-dfci.md
autonomous: false
requirements: [BIOS-11]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "01-windows-dfci.md documents retiring a device and reusing a device as TWO separate H2s with a written discriminator, not merely an implied one — reuse wipes the device but does not remove the Autopilot record; retire unlocks through the profile first, then wipes, then deletes the record last. [ROADMAP SC#5, BIOS-11, D-40; edge BIOS-11/adjacency]"
    - "The default retire sequence is stated in its correct order — unlock through the profile, then wipe, then delete the Autopilot record last — and the order is presented as load-bearing rather than incidental. [SC#5, BIOS-11, D-33; edge BIOS-11/ordering]"
    - "The Surface removal path ships with an explicit ordering warning immediately adjacent to it, because the vendor page's own step order deletes the Autopilot registration before unlocking, which is the destructive sequence this corpus's pitfalls ledger calls the highest-consequence mistake in the milestone. [D-33]"
    - "A section on recovering a device locked in the wrong order ships, with its ground stated on the record as a deliberate over-delivery covered by no requirement clause and no Success Criterion. [D-40]"
    - "01-windows-dfci.md finishes at 12 H2s and 10 hand-authored anchors, with `## Related Resources` and `## External References` unanchored; 00-overview.md stands at 9 H2s and 7 anchors. [D-24]"
    - "The hand-authored anchor-id list of both files, the Phase-150 fetch record, the Phase-152 wiring list and the Phase-153 needle-spec are all recorded in the SUMMARY. [D-65, D-66, D-67]"
    - "Every prose negative named in 149-CONTEXT.md D-60 returns zero across docs/operations/firmware-bios/. [D-34, D-37, D-43, D-49, D-58, D-59, D-31]"
    - "node scripts/validation/check-phase-144.mjs exits 0 at the end of the phase, and its measured pass/fail/skip counts are recorded rather than transcribed. [D-01, D-19]"
  artifacts:
    - docs/operations/firmware-bios/01-windows-dfci.md
  key_links:
    - "01-windows-dfci.md's `## Related Resources` links to 00-overview.md, ../../lifecycle/01-hardware-hash.md, ../../admin-setup-apv1/01-hardware-hash-upload.md and ../patch-management/01-windows-wufb-rings.md, all at file level"
    - "The inbound link from ../../admin-setup-apv1/01-hardware-hash-upload.md back to this file is NOT written here — it is handed to Phase 152 by name, because the integration requirement is scoped to two index files only (D-44, D-66)"
  prohibitions:
    - "The Surface removal path is never placed adjacent to the default retire sequence without the ordering warning between them. [D-33]"
    - "Retire and reuse are never merged into one section or one sequence. [BIOS-11, D-40]"
    - "No registry row, filename-map row, canary bump, ops-index row or docs/index.md entry is written — all of it is Phase 152's single atomic commit. [D-66]"
    - "No validator script is authored or edited — content phases hand off a needle-spec. [D-67]"
    - "No outbound link to any docs/operations/firmware-bios/02-, 03- or 04- file. [D-26]"
    - "No string ships inside quotation marks that was not captured from a live page. [D-37, D-52]"
    - "No single **Source:** line covers claims drawn from two different pages. [D-56]"
    - "The deferred vendor tool named in 149-CONTEXT.md D-34 is not mentioned."
    - "The barred OEM phrasings named in 149-CONTEXT.md D-49 appear nowhere under docs/operations/firmware-bios/."
    - "The British spelling forms named in 149-CONTEXT.md D-59 appear nowhere under docs/operations/firmware-bios/."
    - "The banned-phrase forms named in 149-CONTEXT.md D-58 appear nowhere under docs/operations/firmware-bios/."
    - "No fenced code block and no version-history section exists in either new file. [D-31]"
---

<objective>
Close the DFCI guide: the device lifecycle, the callouts, the tail sections, the phase gate sweep and
the three hand-forward contracts.

Purpose: BIOS-11 requires retire and reuse be documented as **distinct**, and D-40 requires the
discriminator be written rather than implied. The Surface removal path is the milestone's single
highest-consequence content — the vendor's own step order performs, at step 2, the exact operation
this corpus's pitfalls ledger defines as the destructive mistake — and no validator can catch a wrong
ordering, so the warning has to be authored and checked by criteria.

Output: `## Retiring a Device`, `## Reusing a Device`, `## Recovering a Device Locked in the Wrong
Order` and `## Unsupported and Anti-Feature Callouts` in
`docs/operations/firmware-bios/01-windows-dfci.md`, both tail sections completed, and the phase's
hand-forward record.

Decisions this plan implements or records: D-01 (the apex subsumes the five individual gates), D-12
(hand-authored short anchor ids, which become the contract Phase 151's recipe consumes), D-19
(re-measure counts at execution time), D-24 (the anchor count is the contract), D-26 (zero links to
Phase-150 targets; hand the link-adding forward explicitly), D-27 (relative paths), D-31 (zero fences,
no version-history section), D-33 (the Surface removal path ships with an explicit ordering warning),
D-34 (say nothing about the deferred vendor tool), D-37 (nothing quoted that was not read live), D-40
(retire, reuse and recover stay THREE separate H2s, and the recovery section's over-delivery ground is
stated), D-43 (the TPM and Secure Boot absence is an enumeration fact), D-44 (the inbound link is
handed to Phase 152 by name), D-49 (the barred-phrasing grep), D-55 (the verifier re-fetches and
diffs), D-56 (one page per evidence line), D-57 (the three gates that genuinely bind), D-58 (banned-phrase
discipline, verified by running the audit), D-59 (American spelling with a grep that catches the
risk), D-60 (every prose negative carries a runnable grep), D-65 (the contract handed to Phase 150),
D-66 (the contract handed to Phase 152, including the rule-never-a-number instruction and the
undefined barred-heading flag), D-67 (the needle-spec handed to Phase 153), D-68 (the accepted
inconsistencies), D-69 (files take the date of commit 1).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-PATTERNS.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-04-SUMMARY.md
@docs/operations/patch-management/06-windows-driver-firmware-updates.md
</context>

## Artifacts this phase produces

New symbols created by Phase 149 (excluded from drift verification — none exists at HEAD `d9f3c4c5`):

**Files:** `docs/operations/firmware-bios/00-overview.md`,
`docs/operations/firmware-bios/01-windows-dfci.md`.

**Hand-authored anchor ids added by THIS plan:** `retiring-a-device`, `reusing-a-device`,
`recovering-locked-device`, `unsupported-callouts` (in `01-windows-dfci.md`).

**Complete anchor-id contract for the phase**, which this plan's SUMMARY hands to Phases 150 and 151:
- `00-overview.md` — `who-holds-the-secret`, `native-bios-surfaces`, `choosing-a-path`,
  `domain-boundary`, `updates-vs-configuration`, `before-you-start`, `unsupported-callouts`
- `01-windows-dfci.md` — `what-dfci-is`, `dfci-prerequisites`, `dfci-oem-support`,
  `surface-eligibility`, `dfci-settings-surface`, `bricking-irreversible`, `retiring-a-device`,
  `reusing-a-device`, `recovering-locked-device`, `unsupported-callouts`

**New glossary terms** (plan 02): `### Device Firmware Configuration Interface (DFCI)`, `### UEFI CSP`,
`### BIOS password`, `### BIOS configuration and other settings`.

**New nav/hub entries:** none — Phase 152 owns all registry, filename-map, canary, ops-index and
`docs/index.md` wiring (D-66).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: the Surface removal-path ordering is one-way in the field</name>
  <reversibility rating="one-way">A reader who follows the Surface removal order on a non-Surface DFCI fleet bricks the device beyond Intune's reach. The hardware is not recovered by a later documentation fix. 149-CONTEXT.md D-33 rates this one-way in the field, and no validator can catch a wrong ordering.</reversibility>
  <decision>How does the Surface DFCI removal path ship, given that its own step order deletes the Autopilot registration before the device is unlocked — the exact operation this corpus's pitfalls ledger calls the single highest-consequence mistake in the milestone?</decision>
  <options>
    <option id="ship-with-warning">
      <name>Ship the vendor's steps with an explicit ordering warning immediately adjacent (D-33's ruling)</name>
      <pros>Keeps the highest-value recovery content in the phase; the warning states that the order deletes the registration before unlocking, that it is not safe on a non-Surface DFCI fleet, and that the default retire sequence is the inverse; the delete-record step and the unlock step never sit next to each other unflagged</pros>
      <cons>Requires the warning to be authored and checked by an adjacency acceptance criterion, because no validator can see a wrong ordering</cons>
    </option>
    <option id="omit-path">
      <name>Omit the Surface removal path entirely</name>
      <pros>D-32 bounds Surface and the removal path is not in D-32's ship list</pros>
      <cons>Loses the highest-value recovery content in the phase, and leaves a reader who finds the vendor page directly with no counterweight at all</cons>
    </option>
    <option id="ship-as-written">
      <name>Ship the vendor's steps as written, with no warning</name>
      <pros>Reproduces the vendor's own prescribed order without editorialising</pros>
      <cons>This is the failure mode D-33 exists to prevent — a reader can reasonably infer the order is safe because it is the vendor's own, and applying it to a non-Surface DFCI fleet destroys the hardware's manageability</cons>
    </option>
  </options>
  <recommendation>ship-with-warning.</recommendation>
  <resume-signal>Select: ship-with-warning, omit-path, or ship-as-written</resume-signal>
  <action>
Present the decision above to the developer and wait for a ruling before task 2 writes any prose.
Do not proceed on the recommendation alone — D-33 rates this one-way in the field.

For context the developer needs: the vendor's four steps are (1) retire or wipe the device in Intune,
(2) delete the Autopilot registration, (3) connect wired Ethernet through a Surface-branded adapter,
(4) open the UEFI menu and select the refresh-from-network command quoted verbatim in task 2. The default DFCI
retire sequence is the inverse — unlock through the profile first, then wipe, then delete the record
last. Surface's refresh-from-network step is what makes the vendor's order recoverable on Surface
hardware specifically, which is why it does not generalize.

Record the ruling verbatim in the SUMMARY, then proceed. If the ruling is `omit-path`, task 2 ships the
recovery section without the Surface steps and its acceptance criteria for those steps are struck.
  </action>
  <verify>
    <automated>test -f .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - The developer's ruling is recorded verbatim in the SUMMARY before task 2 writes any prose
    - Plan 04's summary exists, so the bricking section this content depends on is already committed
  </acceptance_criteria>
  <done>The developer has ruled on how the Surface removal path ships and the ruling is recorded.</done>
</task>

<task type="auto">
  <name>Task 1: Author the retire and reuse sequences as two distinct sections</name>
  <files>docs/operations/firmware-bios/01-windows-dfci.md</files>
  <read_first>
    - docs/operations/firmware-bios/01-windows-dfci.md — the current state after plan 04, especially `## Bricking and Irreversible Configuration`, whose closing pointer these sections answer.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources item (b), which confirms the source page carries its own reuse/retire/recover H2 with three named sub-steps.
    - .planning/research/FEATURES.md line 68 — the discriminator between the two sequences. Read for the coordinate; write the discriminator in the corpus's own voice, not as a quotation.
    - .planning/research/PITFALLS.md lines 220-242 — the irreversible retire order and why the order is load-bearing. Read for the coordinate; this is researcher prose and may not be quoted.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-33, D-40, D-56.
  </read_first>
  <action>
Add two anchored H2s, each with its short hand-authored anchor id on its own line immediately above the
heading, after `## Bricking and Irreversible Configuration`:

`retiring-a-device` / `## Retiring a Device` — the device leaves the fleet permanently. Ship the
sequence as an ordered list in this order, and say in prose that the order is load-bearing rather than
incidental:
1. Unlock the device through the DFCI profile (the profile is the only thing that can re-enable the
   UEFI menus while the device is still under management).
2. Wipe or retire the device in Intune.
3. Delete the Autopilot record **last**.
State the consequence of reversing steps 1 and 3 directly: once the Autopilot record is gone, the
device can no longer be reached by the profile that would unlock it, and the firmware stays locked.
This is the sequence the next section's recovery content exists for.

`reusing-a-device` / `## Reusing a Device` — the device stays in the fleet and goes to a new user or a
new configuration. Write the discriminator explicitly rather than leaving it implied: reuse wipes the
device but **does not** remove the Autopilot record, so DFCI management survives and the device
re-applies its profile; retire unlocks first, then wipes, then removes the record. BIOS-11 requires
these two be documented as distinct, and 149-CONTEXT.md D-40 requires the discriminator be written.
Keep them as two separate H2s; do not merge them into one lifecycle section with a shared sequence.

Carry a `**Source:**` line for the page that documents these sequences, in the two-date form
`(ms.date 2026-06-23, updated 2026-07-01)`, placed after the claim. One page per line. Any string
placed inside quotation marks must come from 149-RESEARCH.md's raw-byte captures or a live re-fetch;
the pitfalls-ledger wording for the destructive mistake is researcher prose and ships paraphrased.

Do not author the Surface removal path here. It ships in task 2 with its ordering warning.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^<a id="retiring-a-device"></a>' docs/operations/firmware-bios/01-windows-dfci.md` returns 1, and the line immediately below it is `## Retiring a Device`
    - `grep -c '^<a id="reusing-a-device"></a>' docs/operations/firmware-bios/01-windows-dfci.md` returns 1, and the line immediately below it is `## Reusing a Device`
    - the retire section contains an ordered list whose first step is the unlock and whose last step deletes the Autopilot record (`awk '/^## Retiring a Device$/,/^<a id="reusing/' … | grep -E '^[0-9]+\. '` shows the unlock before the delete)
    - the reuse section contains an explicit sentence stating that the Autopilot record is NOT removed, and naming that as the difference from retiring
    - `awk '/^## Reusing a Device$/,/^<a id="recovering/' docs/operations/firmware-bios/01-windows-dfci.md | grep -ciE "retir"` returns at least 1 (the discriminator names the other sequence)
    - `awk '/^## Retiring a Device$/,/^<a id="recovering/' docs/operations/firmware-bios/01-windows-dfci.md | grep -ci 'Refresh from Network'` returns 0 (the Surface path is task 2's)
    - at least one `**Source:**` line exists across the two sections, carrying both dates and one URL
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0 corpus-link failure(s)`
  </acceptance_criteria>
  <done>Retire and reuse are two separate H2s with a written discriminator and the retire order stated as load-bearing.</done>
</task>

<task type="auto">
  <name>Task 2: Author the recovery section and the Surface removal path with its ordering warning</name>
  <files>docs/operations/firmware-bios/01-windows-dfci.md</files>
  <read_first>
    - docs/operations/firmware-bios/01-windows-dfci.md — the current state after task 1, especially the retire sequence this section contrasts against.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources items (b) and (e), and Pitfall 7, which records the vendor page's own numbered step order verbatim.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-33, D-40, D-56, and the developer's ruling recorded by the checkpoint task above.
  </read_first>
  <action>
Add the anchored H2 `## Recovering a Device Locked in the Wrong Order` (anchor id
`recovering-locked-device`) after `## Reusing a Device`.

Open with the situation: the Autopilot record was deleted before the device was unlocked, so the
profile that would re-enable the UEFI menus can no longer reach it. Then give what recovery is
available, sourced to the page that documents it, with its own `**Source:**` line.

Then ship the Surface removal path, per the developer's ruling recorded by the checkpoint task above.
Give the vendor's four steps as an ordered list — retire or wipe the device in Intune; delete the
Autopilot registration; connect wired Internet through a Surface-branded Ethernet adapter; open the
UEFI menu and select the verbatim string <!-- planner-discipline-allow: Refresh from Network -->
`Management > Configure > Refresh from Network` — under a
`**Source:**` line for the Surface guide in the two-date form `(ms.date 2026-07-14, updated
2026-07-14)`.

Place the ordering warning **immediately adjacent** to those steps, between the delete-record step and
the unlock step or directly above the list — never let the two sit next to each other unflagged. The
warning states three things concretely: that this order deletes the Autopilot registration before the
device is unlocked; that on a non-Surface DFCI fleet that order leaves the firmware locked beyond
Intune's reach; and that the default DFCI retire sequence documented above is the inverse and stays
the default. Surface's UEFI refresh-from-network step is what makes the vendor's order recoverable on
Surface hardware specifically, and that is why the order is not generalizable.

Then state the over-delivery ground on the record, in prose, exactly as the Surface section states its
bound: this recovery section is covered by no BIOS requirement clause and no Phase-149 Success
Criterion. It ships because it is the highest-value content in the phase for a service-desk reader and
because a first-party page sources it. Naming the ground is what stops a verifier asking why a section
exists that no criterion requires (D-40).

Any string placed inside quotation marks comes from 149-RESEARCH.md's raw-byte captures or a live
re-fetch (D-37, D-52). Ship no fenced block.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; node scripts/validation/v1.20-milestone-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^<a id="recovering-locked-device"></a>' docs/operations/firmware-bios/01-windows-dfci.md` returns 1, and the line immediately below it is `## Recovering a Device Locked in the Wrong Order`
    - the file contains the literal `Management > Configure > Refresh from Network`
    - the Surface removal steps appear as an ordered list of four steps in the vendor's order
    - an ordering warning appears within 10 lines of the delete-registration step and states all three of: that this order deletes the registration before unlocking; that the order is not safe on a non-Surface DFCI fleet; and that the default retire sequence is the inverse
    - a sentence states that this section is covered by no requirement clause and no Success Criterion, and gives the ground for shipping it anyway
    - `grep -c '^```' docs/operations/firmware-bios/01-windows-dfci.md` returns 0
    - the Surface removal path's `**Source:**` line cites only the Surface guide URL and carries both dates
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0 corpus-link failure(s)`
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0 with `16 passed, 0 failed`
  </acceptance_criteria>
  <done>The recovery section ships with the Surface removal path, its adjacent ordering warning and its over-delivery ground stated.</done>
</task>

<task type="auto">
  <name>Task 3: Close the file, sweep the phase and record the three hand-forward contracts</name>
  <files>docs/operations/firmware-bios/01-windows-dfci.md</files>
  <read_first>
    - docs/operations/firmware-bios/01-windows-dfci.md and docs/operations/firmware-bios/00-overview.md — the current committed state of both files.
    - docs/operations/patch-management/06-windows-driver-firmware-updates.md lines 663-700 and 773-800 — the callouts section's bold-sub-label convention and the two tail sections' link-plus-description shape.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-24 (the anchor contract), D-44, D-60, D-65, D-66, D-67, D-68.
    - .planning/ROADMAP.md — the Phase 150, 151, 152 and 153 sections, so each hand-forward names the phase that receives it.
  </read_first>
  <action>
Add the anchored H2 `## Unsupported and Anti-Feature Callouts` (anchor id `unsupported-callouts`) —
the pinned corpus literal heading, byte-for-byte as the four `patch-management/05-08` siblings write
it — using the sibling's bold-sub-label convention. Content is documented first-party *absences*, not
user-caused misconfigurations: the category-versus-granular loop and the boot-and-port trap already
live under the settings surface and do not move here. Candidates that do belong: the DFCI setting
that has no Disabled value, so an administrator cannot turn platform virtualization off through a
profile; the absence of TPM and Secure Boot from the enumerated categories, written as an enumeration
fact rather than a sourced limitation (D-43); and the fact that no vendor list names Dell, HP or
Lenovo, so DFCI is unavailable there rather than declined. Introduce no new pitfall identifiers.

Then complete `## Related Resources` and `## External References`. Related Resources links
`00-overview.md`, `../../lifecycle/01-hardware-hash.md`,
`../../admin-setup-apv1/01-hardware-hash-upload.md` and
`../patch-management/01-windows-wufb-rings.md`, each with a one-line description in the sibling's
style, all at file level with no heading anchor. External References lists the Microsoft Learn and
vendor URLs actually cited in this file. Neither tail section is anchored.

Then verify the anchor contract (D-24): `00-overview.md` at 9 H2s and 7 anchors,
`01-windows-dfci.md` at 12 H2s and 10 anchors, with `## Related Resources` and
`## External References` unanchored in both.

Then run every prose negative across the whole directory and fix any hit in place, then run the apex.
Record the apex's measured pass/fail/skip counts rather than transcribing any number.

Finally, record the three hand-forward contracts in the SUMMARY, each naming its receiving phase:

**To Phase 150** (D-65): the complete hand-authored anchor-id list of both files, as given in the
Artifacts section of this plan; the fetch record for the BIOS-configuration page, the HP Connect User
Guide and the Lenovo CDRT post, including the Graph password-details endpoint and the fact that it is
**beta**, both role-based access-control retrieval paths, the minimum role required to author the
policy, the subscription-end trap and the no-existing-BIOS-password prerequisite — all of which are
150's requirements sourced from pages this phase fetched; the `02-`/`03-`/`04-` numbering (D-21); the
ruling that the overview's outbound links to the per-OEM guides are 150's to add, not this phase's
(D-26); the write-`and`-never-an-ampersand convention (D-23); the matrix column naming which
operating-system features are gated by the virtualization setting (D-14); the two vendor glossary
terms deliberately not added here (D-64); and a **falsification clause** — if 150's re-fetch corrects
a custody position, 150 owns the correction to this phase's overview, and 150's context must diff
against this phase's shipped claims. This risk is live: the HP portal returned a 403 before the
browser User-Agent method, and the Lenovo comparative claim ships as inference.

**To Phase 152** (D-66): registry rows and filename-map rows for both new files, with **no document
identifier**; ops-index rows and the Firmware H2; the `docs/index.md` Operations entry; the **inbound**
link from `../../admin-setup-apv1/01-hardware-hash-upload.md` to this file's prerequisites, which the
integration requirement scoped to two index files does not reach and which is therefore nobody's job
unless named (D-44); and the write-`and`-never-an-ampersand convention for the ops-index H2. Hand
forward the **rule, never a number** — the canary target is computed from the registry after the rows
land, and the architecture ledger bars its own estimate from becoming a literal. Flag two live risks:
zero of the existing registry rows is an operations path and the registry parser keys on the document
identifier, so a row with no identifier is unprecedented and needs the scratch-row publish-bundle
probe; and the barred literal heading the integration requirement names for Firmware is **undefined**
in the roadmap, the requirements document and the state file — flag it, do not guess. The one barred
literal heading on record is the patch-management one.

**To Phase 153** (D-67): a needle-spec for `check-phase-149.mjs`. Content phases hand off a needle-spec
and never author validators; `grep -rln "firmware-bios" scripts/` returns 0 today. Suggested needles:
both files exist; the nine OEM names are present; the pending sentence is present; the barred phrasings
are absent; both bricking quotes are present in their assigned files and neither is duplicated across
them; the four registration channels are named; and the anchor counts hold at 7 and 10.

Also re-record the accepted inconsistencies (D-68) and re-measure the two corpus term counts at
execution time, noting the metric trap — a line count and an occurrence count are not comparable.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-144.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Unsupported and Anti-Feature Callouts$' docs/operations/firmware-bios/01-windows-dfci.md` returns 1, byte-identical to the four `patch-management/05-08` instances
    - `grep -c '^## ' docs/operations/firmware-bios/01-windows-dfci.md` returns 12
    - `grep -c '^<a id=' docs/operations/firmware-bios/01-windows-dfci.md` returns 10
    - `grep -c '^## ' docs/operations/firmware-bios/00-overview.md` returns 9 and `grep -c '^<a id=' docs/operations/firmware-bios/00-overview.md` returns 7
    - `## Related Resources` and `## External References` are not preceded by an anchor line in either file
    - `grep -rnE '\]\([^)]*#' docs/operations/firmware-bios/ | grep -v '_glossary.md#secure-boot'` returns nothing (the only anchor-bearing cross-file link in the phase is the glossary one)
    - `grep -rn "SEMM" docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rniE "the six($|[^-])|most business OEMs" docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rniE "enrolment|enrols|enrolling\b|licence|behaviour|organisation|programme|labelled|honour|catalogue|recognise|analyse" docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rc '^```' docs/operations/firmware-bios/*.md` returns 0 for every file
    - `grep -rn "^## Version History" docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rniE "\bSystem Center\b|\bSCCM\b|Autopatch rings" docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rn '^> \*\*Platform:\*\*' docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rnE '\]\((\./)?0[234]-' docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rniE 'DFCI (cannot|can.t|does not|doesn.t) (set|configure|manage) (TPM|Secure Boot)' docs/operations/firmware-bios/ | wc -l` returns 0
    - `git status --porcelain docs/_registry/ docs/index.md docs/operations/00-index.md` shows no modification
    - `git status --porcelain scripts/` shows no modification
    - `node scripts/validation/check-phase-144.mjs` exits 0, and its measured counts are recorded verbatim in the SUMMARY
    - the SUMMARY contains three clearly labelled hand-forward sections naming Phase 150, Phase 152 and Phase 153, each carrying every item listed in this task's action
    - the SUMMARY records the re-measured corpus term counts and the accepted inconsistencies
  </acceptance_criteria>
  <done>Both files are structurally complete at their contracted anchor counts, every prose negative is zero, the apex is green, and the three hand-forward contracts are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream vendor documentation → this corpus | The Surface removal path is transcribed from a vendor page whose own order is hazardous outside Surface hardware |
| This corpus → a service-desk operator retiring live hardware | Following the wrong order destroys the hardware's manageability permanently |
| This phase → Phases 150, 152 and 153 | Anchor ids, fetch records, wiring items and needle-specs cross a phase boundary as an unwritten contract unless recorded |

This plan authors static Markdown. There is no application code, no authentication surface, no user
input handling and no data storage — **there is no ASVS/STRIDE attack surface**, the conclusion
149-RESEARCH.md's Security Domain section reaches, stated here rather than omitted. ASVS level 1,
block on `high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-149-25 | Tampering | The device retire and removal sequences | high | mitigate | The one-way checkpoint above forces an explicit ruling; the ordering warning is required by named acceptance criteria to sit within 10 lines of the delete-registration step and to state all three of its points; the default retire order is documented separately with its own ordering rationale |
| T-149-26 | Spoofing | The Surface removal steps and the recovery content | medium | mitigate | Every quoted string traces to 149-RESEARCH.md section Sources item (e) or a live re-fetch; the pitfalls-ledger wording ships paraphrased (D-37) |
| T-149-27 | Repudiation | Cross-phase contract provenance | medium | mitigate | Three labelled hand-forward sections in the SUMMARY, each naming its receiving phase and its items, so no contract crosses the boundary unwritten |
| T-149-28 | Information disclosure | — | low | accept | No secret or tenant-specific value is authored |
| T-149-29 | Denial of service | — | low | accept | Static Markdown; no runtime |
| T-149-30 | Elevation of privilege | — | low | accept | No permission model is implemented; the role-based access-control paths are handed to Phase 150 as content, not implemented |
| T-149-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs — 149-RESEARCH.md's Package Legitimacy Audit records "Not applicable" |

T-149-25 is rated `high` and the configured block threshold is `high`; it is disposed `mitigate`, and
its mitigation is enforced by the blocking checkpoint task and by an adjacency acceptance criterion,
because no validator in this repository can see a wrong ordering.
</threat_model>

<verification>
- `node scripts/validation/check-phase-144.mjs` — the apex, green; counts recorded, not transcribed
- `node scripts/validation/check-nav-hub-links.mjs` — 0 hub-presence and 0 corpus-link failures
- `node scripts/validation/check-phase-54.mjs` — 32 passed, 0 failed
- `node scripts/validation/v1.20-milestone-audit.mjs` — 16 passed, 0 failed
- `node scripts/validation/c17-eee-contract.mjs` — 0 violations
- All prose negatives in task 3 return zero
- `00-overview.md` at 9 H2 / 7 anchors and `01-windows-dfci.md` at 12 H2 / 10 anchors
- Nothing under `docs/_registry/`, `docs/index.md`, `docs/operations/00-index.md` or `scripts/` is modified
</verification>

<success_criteria>
- ROADMAP SC#5 is fully satisfied: the bricking surface is quoted first-party (plan 04), retire and reuse are documented as distinct with a written discriminator, and deleting the profile or the group assignment is stated to remove nothing
- The Surface removal path ships with its adjacent ordering warning, and the recovery section's over-delivery ground is on the record
- Both new files stand at their contracted H2 and anchor counts with zero fences and no version-history section
- The Phase 150, Phase 152 and Phase 153 hand-forward contracts are recorded in the SUMMARY
- The apex validator is green at the end of the phase
</success_criteria>

<output>
Create `.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-05-SUMMARY.md` when done
</output>
