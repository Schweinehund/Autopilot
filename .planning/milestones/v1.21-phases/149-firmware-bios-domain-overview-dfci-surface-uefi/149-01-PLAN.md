---
phase: 149-firmware-bios-domain-overview-dfci-surface-uefi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/firmware-bios/00-overview.md
  - docs/operations/firmware-bios/01-windows-dfci.md
autonomous: true
requirements: [BIOS-01, BIOS-02, BIOS-11]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/operations/firmware-bios/00-overview.md names Dell, HP and Lenovo and states for each where the BIOS secret lives — Dell: Intune holds it; HP: HP's cloud vault outside the tenant; Lenovo: the customer holds it (encrypted INI file, or a private key in the customer's own Azure Key Vault). [ROADMAP SC#1, BIOS-01, D-02]"
    - "The HP custody position carries the verbatim sentence 'Passwords are managed by HP Connect and stored in a cloud vault.' under its own standalone **Source:** line naming the HP Connect User Guide. [SC#1, D-02, D-56]"
    - "The Lenovo custody position carries the verbatim Azure Key Vault sentence from the Lenovo CDRT post under its own standalone **Source:** line. [SC#1, D-02, D-56]"
    - statement: "The 'only vendor whose signing key can live under the customer's own RBAC, logging and rotation' comparison is written as this corpus's own inference from Azure Key Vault platform behavior and is never attributed to Lenovo, never inside quotation marks, and never placed under the Lenovo **Source:** line. [SC#1, D-02, D-37, D-52; edge BIOS-01/unclassified]"
      verification: backstop
    - "Both native Intune BIOS surfaces are named in 00-overview.md and stated as disjoint — DFCI, and the Dell-only 'BIOS configuration and other settings' Templates policy. [SC#2, BIOS-02, D-25]"
    - "The Dell-only Templates surface states its configuration-file type and its exact size threshold, quoted verbatim: 'For Dell, upload the Dell Client Configuration Tool Kit file (.cctk). The file size limit is 2 MB.' [SC#2, BIOS-02; edge BIOS-02/boundary — the only min/max threshold in this phase's scope]"
    - "00-overview.md quotes the Bitlocker bricking Warning verbatim from configure-bios-windows, and 01-windows-dfci.md does not repeat that same quote — one bricking quote per file, each with its own **Source:** line. [SC#5, BIOS-11, D-38]"
    - "Both new files carry review_by equal to last_verified plus exactly 60 days, computed by arithmetic at execution time and never mirrored from a sibling file. [D-30, D-69; edge BIOS-02/precision — the only arithmetic surface in this phase]"
    - "The '## What This Domain Does Not Own' section cross-links four distinct owners — TPM, Secure Boot, BitLocker and firmware updates — using source-relative paths. [D-41, D-27]"
    - "The TPM and Secure Boot absence is written as an enumeration fact over the eight DFCI setting categories, with the **Source:** line on the enumeration, never as a first-party statement that DFCI cannot set them. [D-43]"
    - "node scripts/validation/check-nav-hub-links.mjs reports 0 hub-presence failures and 0 corpus-link failures after the commit that lands both files. [D-26, D-28, D-57]"
  artifacts:
    - docs/operations/firmware-bios/00-overview.md
    - docs/operations/firmware-bios/01-windows-dfci.md
  key_links:
    - "00-overview.md links to 01-windows-dfci.md and 01-windows-dfci.md links back to 00-overview.md — both created in ONE commit so neither direction dangles (D-28, D-29)"
    - "00-overview.md links to ../../_glossary.md#secure-boot — the only anchor-bearing cross-file link this phase writes (D-41)"
    - "00-overview.md links to ../patch-management/06-windows-driver-firmware-updates.md — the updates-versus-configuration seam (D-42)"
    - "00-overview.md links to ../../decision-trees/03-tpm-attestation.md and ../../reference/security-baseline-conflicts.md (D-41, D-45)"
  prohibitions:
    - "Neither new file carries a document-identifier front-matter key, a version-history section, or any fenced code block. [D-30, D-31]"
    - "Neither new file contains an outbound link to any docs/operations/firmware-bios/02-, 03- or 04- file — Phase 150 adds those links when it authors the targets. [D-26]"
    - "No cross-file link written by this phase carries a heading anchor, except ../../_glossary.md#secure-boot which already exists at HEAD. [D-27, D-28]"
    - "No string appears inside quotation marks in either new file unless it is one of the raw-byte captures in 149-RESEARCH.md section Sources, or a string re-fetched live during execution. [D-37, D-52]"
    - "No single **Source:** line covers claims drawn from two different pages. [D-56]"
    - "Neither new file uses an ampersand in any H1 or H2 — the slug function produces a double hyphen. [D-11, D-23]"
    - "The barred OEM phrasings named in 149-CONTEXT.md D-49 appear nowhere under docs/operations/firmware-bios/."
    - "The deferred vendor tool named in 149-CONTEXT.md D-34 is not mentioned anywhere, not even to caveat it."
    - "The British spelling forms named in 149-CONTEXT.md D-59 appear nowhere under docs/operations/firmware-bios/."
    - "The banned-phrase forms named in 149-CONTEXT.md D-58 appear nowhere under docs/operations/firmware-bios/."
    - "No file outside files_modified is edited — in particular docs/operations/patch-management/06-windows-driver-firmware-updates.md, docs/index.md, docs/operations/00-index.md, docs/_registry/RE-index.md, docs/admin-setup-apv1/01-hardware-hash-upload.md and docs/decision-trees/03-tpm-attestation.md are cross-linked, never edited. [D-42, D-44, D-45, D-66]"
---

<objective>
Found the `docs/operations/firmware-bios/` domain and author its routing hub in full.

This plan lands BOTH new files in ONE commit (D-28/D-29) — a thin, production-quality end-to-end
slice first (frontmatter, platform-applicability blockquote, H1, one anchored H2 per file carrying a
real first-party quote and a two-date evidence line, bidirectional cross-links, tail sections), then
expands `00-overview.md` to its full nine-H2 shape.

Purpose: BIOS-01 routes a reader by **who holds the BIOS secret** rather than by tool; BIOS-02's
disjointness statement and Dell-template mechanics land here; BIOS-11's Dell-template bricking quote
lands here and only here (D-38). D-25 confines BIOS-03/BIOS-04 and the DFCI half of BIOS-02 to
`01-windows-dfci.md`, which later plans author.

Output: `docs/operations/firmware-bios/00-overview.md` complete, and
`docs/operations/firmware-bios/01-windows-dfci.md` seeded with its frontmatter, blockquote, H1,
`## What DFCI Is` section and its two tail sections.

Decisions this plan implements or records: D-01 (the apex subsumes the five individual gates —
`check-phase-144.mjs:65-69`), D-02 (the two new vendor sources), D-07 (ship the inverse: DFCI cannot
turn virtualization off), D-08 (the Surface "reports noncompliant" consequence is fabricated — do not
inherit), D-11 (the ampersand slug is a double hyphen), D-16 (the UEFI/BIOS counts live in STATE.md,
not STACK.md:70), D-18 (new files are born LF), D-19 (re-measure corpus counts; never transcribe),
D-20 (TWO files, Surface folds into `01`), D-21 (Phase 150 takes `02-`/`03-`/`04-`), D-22 (the LOCKED
naming flag is discharged: the directory is `firmware-bios/`, because BIOS-01 states the path
literally), D-23 (the two H1s, and `and` never an ampersand), D-24 (the H2 skeleton; the anchor count
is the contract, not the H2 count), D-25 (the overview carries BIOS-01, BIOS-02's disjointness and
BIOS-11's Dell-template quote — nothing more), D-26 (zero outbound links to Phase-150 targets), D-27
(source-relative paths; never a bare `01`), D-28 (one commit, both files), D-29 (bidirectional
cross-links), D-30 (frontmatter shape and the honest note that nothing enforces the 60-day interval on
these files), D-31 (the platform-applicability blockquote; zero fences; no version history), D-33
(referenced forward — the Surface ordering warning lands in plan 05), D-34 (say nothing about the
deferred vendor tool), D-37 (nothing ships in quotation marks that was not read from a live page),
D-38 (two bricking warnings on two pages, one per file), D-41 (the domain boundary cross-links FOUR
owners), D-42 (the updates-versus-configuration seam), D-43 (the TPM/Secure-Boot absence is an
enumeration fact, never a sourced absence), D-44 (`admin-setup-apv1/01-hardware-hash-upload.md` is
cross-linked, never edited; the inbound link is Phase 152's), D-45 (`decision-trees/03-tpm-attestation.md`
is the TPM target, with its audience mismatch recorded), D-46 (the bounded fetch list of seven, every
item graded — discharged by 149-RESEARCH.md), D-49 (a runnable grep for the barred phrasings), D-50
(retrieval is curl plus a browser User-Agent), D-51 (record both `ms.date` and `updated_at` beside
every quote), D-52 (the `[DIRECT]`/`[RELAYED]` quotation gate), D-53 (pin every quote by its
surrounding heading — five decoys), D-54 (the `device-configuration/templates/` canonical URL paths),
D-56 (the evidence-line contract and its one-page scope rule), D-57 (THREE gates genuinely bind this
phase), D-58 (banned-phrase discipline; verify by running the audit), D-59 (American spelling with a
grep that catches the risk), D-60 (every prose negative carries a runnable grep), D-68 (accepted
inconsistencies), D-69 (all files take the date of commit 1), D-72 (citations use full relative paths).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-PATTERNS.md
@docs/operations/patch-management/06-windows-driver-firmware-updates.md
</context>

## Artifacts this phase produces

New symbols created by Phase 149 (excluded from drift verification — none of these exists at HEAD
`d9f3c4c5`):

**Files**
- `docs/operations/firmware-bios/00-overview.md` (new, this plan)
- `docs/operations/firmware-bios/01-windows-dfci.md` (new, this plan seeds it; plans 03/04/05 expand it)

**Hand-authored anchor ids in `00-overview.md`** (7 anchors / 9 H2s)
- `who-holds-the-secret`, `native-bios-surfaces`, `choosing-a-path`, `domain-boundary`,
  `updates-vs-configuration`, `before-you-start`, `unsupported-callouts`
- `## Related Resources` and `## External References` stay unanchored (D-24)

**Hand-authored anchor ids in `01-windows-dfci.md`** (10 anchors / 12 H2s, completed across plans 01/03/04/05)
- `what-dfci-is` (this plan), `dfci-prerequisites`, `dfci-oem-support`, `surface-eligibility`,
  `dfci-settings-surface`, `bricking-irreversible`, `retiring-a-device`, `reusing-a-device`,
  `recovering-locked-device`, `unsupported-callouts`
- `## Related Resources` and `## External References` stay unanchored

**New glossary terms** (plan 02): `### Device Firmware Configuration Interface (DFCI)`, `### UEFI CSP`,
`### BIOS password`, `### BIOS configuration and other settings`

**New nav/hub entries:** none. Registry rows, filename-map rows, canary bumps, ops-index rows and
`docs/index.md` wiring are Phase 152's single atomic commit (D-66, INT-01..INT-04).

## Flagged assumptions — edge probe ledger (13 rows, 0 silently dropped)

No `-SPEC.md` exists for this phase, so the deterministic edge probe returned 13 applicable rows, all
`unresolved`. Every row is accounted for below. Equality holds: 13 = 11 authored into `must_haves`
truths + 2 surfaced here as not-applicable with a stated reason.

| # | Requirement | Category | Destination |
|---|---|---|---|
| 1 | BIOS-01 | unclassified | `must_haves` truth (this plan) — the Lenovo superlative is the real edge: it is an inference, not a first-party claim, and is marked `verification: backstop` because "written as inference, not attribution" is a judgment a grep cannot settle |
| 2 | BIOS-02 | boundary | `must_haves` truth (this plan) — the 2 MB configuration-file limit is the only min/max threshold in this phase's scope; the exact value ships quoted |
| 3 | BIOS-02 | precision | `must_haves` truth (this plan) — the only arithmetic surface is `review_by` = `last_verified` + 60 days; no rounding or tie-breaking exists in prose |
| 4 | BIOS-02 | concurrency | **FLAGGED NOT APPLICABLE.** This phase authors static Markdown. There is no execution, no interruption and no parallel run to make a guarantee about. Retained here rather than dropped. |
| 5 | BIOS-03 | adjacency | `must_haves` truth (plan 03) — the six-OEM list is a strict subset of the nine-OEM list; the two "touch" and must be presented as a documented conflict, separately cited, never merged |
| 6 | BIOS-03 | empty | `must_haves` truth (plan 03) — the empty case is Dell, HP and Lenovo, which appear on none of the lists; they are presented as fleets where DFCI is unavailable, not declined |
| 7 | BIOS-03 | ordering | `must_haves` truth (plan 03) — the nine names ship in the source's own order; a re-sorted list is a silent divergence from the quoted source |
| 8 | BIOS-04 | unclassified | `must_haves` truth (plan 03) — the four registration channels and the APv2 no-registration case (D-05) |
| 9 | BIOS-11 | adjacency | `must_haves` truth (plan 05) — retire and reuse sequences are adjacent and near-identical; the written discriminator is what keeps them distinct |
| 10 | BIOS-11 | empty | `must_haves` truth (plan 04) — the null-operation case: deleting the profile or removing the group assignment removes nothing |
| 11 | BIOS-11 | ordering | `must_haves` truth (plan 05) — the destructive-order hazard is the entire content of D-33 |
| 12 | BIOS-11 | idempotency | `must_haves` truth (plan 04) — re-running or re-imaging does not undo the change; the settings change the device hardware |
| 13 | BIOS-11 | concurrency | **FLAGGED NOT APPLICABLE.** A prose runbook has no parallel-execution surface; the ordering hazard is covered by row 11 rather than by a concurrency guarantee. Retained here rather than dropped. |

<tasks>

<task type="tracer">
  <name>Task 1: Found the domain end to end — both files, one commit</name>
  <precondition>Today's date is on or before 2026-09-23, the re-fetch deadline 149-RESEARCH.md states under "Valid until". If it is later, re-fetch the seven sources listed in 149-RESEARCH.md section Sources with `curl -A "Mozilla/5.0 ..."` before any quoted string is written, and record the re-fetch in the SUMMARY.</precondition>
  <files>docs/operations/firmware-bios/00-overview.md, docs/operations/firmware-bios/01-windows-dfci.md</files>
  <read_first>
    - docs/operations/patch-management/06-windows-driver-firmware-updates.md — lines 1-20 (frontmatter, platform-applicability blockquote, H1), 379-380 (anchor/H2 pairing), 773-800 (the two tail sections). This is the exact structural analog; 149-PATTERNS.md names it as an exact match.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources, items (c), (d), (f), (g). Every quoted string in this task comes from there verbatim.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-PATTERNS.md — the Shared Patterns section, for the four validator bindings.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-23, D-24, D-26, D-27, D-28, D-29, D-30, D-31, D-51, D-56.
  </read_first>
  <action>
Create the directory `docs/operations/firmware-bios/` and both files, then make ONE commit containing
both. D-28 is load-bearing: the two files cross-link each other, so committing either alone leaves a
dangling link and takes `check-nav-hub-links` to a corpus-link failure. Write LF line endings (D-18).

Both files get the same five-key front matter, in this key order and with no document-identifier key:
`last_verified` = the actual execution date; `review_by` = that date plus exactly 60 days computed by
arithmetic (never mirrored from a sibling, per D-30 and 147 D-62); `applies_to: APv1` (not `all` — DFCI's
central gate is hardware-hash Autopilot registration, an APv1 construct, and both DFCI-adjacent
neighbours RE-077 and RE-210 carry `applies_to: APv1`); `audience: admin`; `platform: Windows`. If
execution spans midnight, both files take the date of commit 1 (D-69).

Both files get a line-start blockquote opening exactly `> **Platform applicability:**` — the full
two-word form. The bare short form is what `check-phase-54.mjs` validator 27 negative-asserts against
corpus-wide, and that validator walks `.planning/` as well as `docs/`. The blockquote ends with the
cross-link to the sibling file, matching the analog's shape at its lines 9-14 and satisfying D-29.

H1s, exactly (D-23 — write the word `and`, never an ampersand; the slug function strips the ampersand
in place and maps each remaining space to one hyphen, producing a double hyphen):
- `00-overview.md`: `# Firmware and BIOS Governance`
- `01-windows-dfci.md`: `# Device Firmware Configuration Interface (DFCI)`

`00-overview.md` in this task gets: front matter, blockquote, H1, a short intro paragraph, the
anchored H2 `## Who Holds the BIOS Secret` (anchor id `who-holds-the-secret`, on its own line
immediately above the heading), then `## Related Resources` and `## External References` (both
unanchored). `## Who Holds the BIOS Secret` is BIOS-01's routing spine and carries a three-row table
plus prose naming each vendor and its custody position:

- **Dell** — Intune holds the secret. Reached through the "BIOS configuration and other settings"
  Templates policy. Ship the verbatim string `Currently, only Dell is supported.` with its own
  standalone `**Source:**` line for `configure-bios-windows`.
- **HP** — the secret lives in HP's cloud vault, outside the tenant, reached through HP Connect for
  Microsoft Endpoint Manager, a vendor connector. Ship the verbatim string `Passwords are managed by
  HP Connect and stored in a cloud vault.` with its own `**Source:**` line for the HP Connect User
  Guide.
- **Lenovo** — the customer holds the secret, in an encrypted INI file or as a private key in the
  customer's own Azure Key Vault, reached through Think BIOS Config Tool V2 and Lenovo BIOS
  Certificate Tool V2. Ship the verbatim Lenovo sentence from 149-RESEARCH.md item (g) with its own
  `**Source:**` line.

Then, as a separate unquoted sentence NOT under the Lenovo `**Source:**` line, state the comparative
position that SC#1 requires — that Lenovo is the only one of the three whose signing key can sit
under the customer's own Azure role-based access control, audit logging and key rotation — and
attribute it explicitly to this corpus's inference from Azure Key Vault's documented platform
behavior. 149-RESEARCH.md Pitfall 8 confirms all four of those terms are absent from both fetched
Lenovo pages. Shipping this as a Lenovo attribution would be this milestone's third fabricated
citation.

Per D-26, name each vendor in prose and in the table; write no link to any `02-`, `03-` or `04-` file
in this directory. Phase 150 adds those links when it authors the targets.

`**Source:**` lines follow the analog's placement rule — a standalone, line-start paragraph AFTER the
claim, never inside a leading blockquote and never an inline parenthetical — but upgrade the analog's
single-date shape to the two-date form D-51 requires:
`**Source:** [Title](url) (ms.date YYYY-MM-DD, updated YYYY-MM-DD)`. Use the canonical
`device-configuration/templates/` URL paths (D-54); the older `intune/intune-service/configuration/`
paths are a 301 for one page and genuine 404s for two others. For the HP PDF, which carries neither
field, use the missing-date shape precedented at
`docs/operations/patch-management/02-macos-update-enforcement.md:27` — `(published YYYY-MM-DD)` or the
document's own version and date line. For the Lenovo post, `(published 2025-11-04)`. One `**Source:**`
line may cover one contiguous blockquote of quotes from ONE page; it may never span two pages (D-56) —
so the Dell, HP and Lenovo positions carry three separate lines.

`01-windows-dfci.md` in this task gets: front matter, blockquote, H1, a short intro paragraph, the
anchored H2 `## What DFCI Is` (anchor id `what-dfci-is`), then `## Related Resources` and
`## External References`. `## What DFCI Is` is BIOS-02's DFCI half — the configuration-service-provider
layer, per-setting reporting, and the certificate trust chain. Ship the verbatim string `These settings
use the UEFI CSP.` under a `**Source:**` line for `ref-dfci-settings-windows`, and the verbatim string
`Through UEFI CSP using the DFCI layer, which is isolated from the OS` under a separate `**Source:**`
line for `configure-bios-windows`. Two pages, two lines. Per 149-RESEARCH.md Pitfall 4, the string
`UEFI CSP` does not occur on `configure-dfci-windows` at all — never attribute it to that page.

Both files' `## Related Resources` sections open with the reciprocal link to each other by relative
path — `01-windows-dfci.md` from the overview, `00-overview.md` from the guide (D-27: always the full
relative path, never a bare number). No link written in this phase carries a heading anchor except
`../../_glossary.md#secure-boot`, which is added in task 2; keeping every other cross-file link at
file level removes the ordering hazard entirely, because a later plan adding an anchor cannot strand an
earlier commit.

Commit both files together with a single message.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; node scripts/validation/check-phase-54.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `git show --stat HEAD` lists both `docs/operations/firmware-bios/00-overview.md` and `docs/operations/firmware-bios/01-windows-dfci.md` in the SAME commit
    - `head -7 docs/operations/firmware-bios/00-overview.md` shows exactly the keys `last_verified`, `review_by`, `applies_to`, `audience`, `platform` between two `---` fences
    - `grep -c "^doc_id" docs/operations/firmware-bios/00-overview.md docs/operations/firmware-bios/01-windows-dfci.md` returns 0 for both files
    - `grep -rn "applies_to: APv1" docs/operations/firmware-bios/` returns 2 lines
    - the `review_by` value in each file equals its `last_verified` value plus exactly 60 days (verify with `date -d "<last_verified> +60 days" +%F`)
    - `git ls-files --eol docs/operations/firmware-bios/` shows `w/lf` for both files
    - `grep -c '^> \*\*Platform applicability:\*\*' docs/operations/firmware-bios/00-overview.md` returns 1, and the same for `01-windows-dfci.md`
    - `grep -rn '^> \*\*Platform:\*\*' docs/operations/firmware-bios/` returns nothing
    - `grep -n '^# ' docs/operations/firmware-bios/00-overview.md` returns exactly `# Firmware and BIOS Governance`
    - `grep -n '^# ' docs/operations/firmware-bios/01-windows-dfci.md` returns exactly `# Device Firmware Configuration Interface (DFCI)`
    - `grep -rn '&' docs/operations/firmware-bios/*.md | grep -E '^[^:]+:[0-9]+:#'` returns nothing (no ampersand in any heading)
    - `grep -c '^<a id="who-holds-the-secret"></a>' docs/operations/firmware-bios/00-overview.md` returns 1, and the line immediately below it is `## Who Holds the BIOS Secret`
    - `grep -c '^<a id="what-dfci-is"></a>' docs/operations/firmware-bios/01-windows-dfci.md` returns 1, and the line immediately below it is `## What DFCI Is`
    - `docs/operations/firmware-bios/00-overview.md` contains the literal `Currently, only Dell is supported.`
    - `docs/operations/firmware-bios/00-overview.md` contains the literal `Passwords are managed by HP Connect and stored in a cloud vault.`
    - `docs/operations/firmware-bios/01-windows-dfci.md` contains the literal `These settings use the UEFI CSP.`
    - `docs/operations/firmware-bios/01-windows-dfci.md` contains the literal `Through UEFI CSP using the DFCI layer, which is isolated from the OS`
    - `grep -c '^\*\*Source:\*\* ' docs/operations/firmware-bios/00-overview.md` returns at least 3, and every one of those lines matches `(ms.date `, `(updated `, `(published ` or a document version-and-date parenthetical
    - `grep -n 'RBAC\|role-based access control' docs/operations/firmware-bios/00-overview.md` shows the comparative Lenovo sentence, and that sentence contains no quotation marks and is not the line immediately preceding a Lenovo `**Source:**` line
    - `grep -rn 'intune/intune-service/configuration' docs/operations/firmware-bios/` returns nothing
    - `grep -rnE '\]\((\./)?0[234]-' docs/operations/firmware-bios/` returns nothing
    - `grep -c '01-windows-dfci.md' docs/operations/firmware-bios/00-overview.md` returns at least 1, and `grep -c '00-overview.md' docs/operations/firmware-bios/01-windows-dfci.md` returns at least 1
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 and prints `0 hub-presence failure(s), 0 corpus-link failure(s)`
    - `node scripts/validation/check-phase-54.mjs` exits 0 and prints `32 passed, 0 failed`
  </acceptance_criteria>
  <done>Both new files exist, cross-link each other, are in one commit, and `check-nav-hub-links` plus `check-phase-54` are both green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the remaining six sections of the routing overview</name>
  <files>docs/operations/firmware-bios/00-overview.md</files>
  <read_first>
    - docs/operations/firmware-bios/00-overview.md — the current state after task 1.
    - docs/operations/patch-management/06-windows-driver-firmware-updates.md — lines 663-700 (the `## Unsupported and Anti-Feature Callouts` section, for its bold-sub-label convention) and 773-800 (the two tail sections).
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources items (c) and (d); the Sources list is the only permitted origin for a quoted string.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-24, D-25, D-26, D-38, D-41, D-42, D-43, D-45.
    - docs/_glossary.md lines 199-205 — the existing `### Secure Boot` entry that D-41 links to. Linking to it is not editing it; do not edit it.
  </read_first>
  <action>
Add six anchored H2s to `00-overview.md`, each with its short hand-authored anchor id on its own line
immediately above the heading, in this order and between `## Who Holds the BIOS Secret` and
`## Related Resources`. The anchor count is the contract, not the H2 count (D-24): nine H2s, seven
anchored, with `## Related Resources` and `## External References` unanchored.

1. `native-bios-surfaces` / `## The Two Native Intune BIOS Surfaces` — BIOS-02's disjointness
   statement. Name both surfaces and state plainly that they are disjoint: they share no device.
   DFCI reaches the nine OEMs listed on `autopilot/dfci-management` and is routed to
   `01-windows-dfci.md`; "BIOS configuration and other settings" is a Templates policy that reaches
   Dell alone. Ship the Dell-template mechanics here, quoted verbatim from `configure-bios-windows` —
   `For Dell, upload the Dell Client Configuration Tool Kit file (.cctk). The file size limit is 2 MB.`
   — as one contiguous blockquote under a single `**Source:**` line for that one page. This is the
   phase's only threshold value; write the limit exactly as the source states it, never rounded or
   restated in other units.

2. `choosing-a-path` / `## Choosing a Path` — routing only. Answer "which surface applies to my
   fleet" by naming the vendor and its custody position, and route DFCI questions to
   `01-windows-dfci.md` at file level. Per D-26 this section writes no link to a Phase-150 target;
   BIOS-01's verb is "routes", which prose satisfies, and SC#1 requires no links.

3. `domain-boundary` / `## What This Domain Does Not Own` — cross-link FOUR owners, not one
   (D-41). TPM goes to `../../decision-trees/03-tpm-attestation.md`; Secure Boot goes to
   `../../_glossary.md#secure-boot`; BitLocker goes to
   `../../reference/security-baseline-conflicts.md`; firmware *updates* go to
   `../patch-management/06-windows-driver-firmware-updates.md`. Write the TPM and Secure Boot
   exclusion as an ENUMERATION FACT and nothing else (D-43): the DFCI settings reference enumerates
   eight categories — UEFI access, Security features, Cameras, Microphones and speakers, Radios, Boot
   Options, Ports, and Wake settings — and neither TPM nor Secure Boot appears among them. Put the
   `**Source:**` line on the enumeration, citing `ref-dfci-settings-windows`. No first-party sentence
   says DFCI cannot set TPM or Secure Boot; asserting one would be an argument from silence.
   Record honestly, in prose, that the TPM target is an `audience: L1` triage artifact while this
   document's audience is `admin` (D-45).

4. `updates-vs-configuration` / `## Where This Domain Fits` — the seam statement (D-42). One or two
   sentences separating firmware *updates* (delivery of vendor payloads through the Intune driver and
   firmware update policy, owned by `../patch-management/06-windows-driver-firmware-updates.md`) from
   firmware *configuration* (which BIOS settings a device is allowed to have, owned by this domain).
   Both carry the word "firmware" in their names; without this statement they read as duplicates. Do
   not edit the patch-management file. This is the ninth H2 that D-24 leaves to discretion — it ships,
   because the seam is a positive placement statement rather than an exclusion and reads poorly under
   `## What This Domain Does Not Own`.

5. `before-you-start` / `## Before You Start` — BIOS-11's Dell-template bricking quote, and only that
   one (D-38). Ship verbatim from `configure-bios-windows`: `BIOS configuration changes can impact
   device functionality and operability, including the ability to boot or access Bitlocker encrypted
   drives.` with its own `**Source:**` line for that page. Then point the reader to
   `01-windows-dfci.md` for DFCI's own, distinct warning — do not restate or paraphrase the DFCI
   warning here. One quote per file is the rule; duplicating a load-bearing first-party quote across
   two files is the defect `146-REVIEW.md` records as WR-01.

6. `unsupported-callouts` / `## Unsupported and Anti-Feature Callouts` — the pinned corpus literal
   heading, byte-for-byte as the four `patch-management/05-08` siblings write it (148 D-24). Use the
   sibling's bold-sub-label convention. Content is documented first-party *absences* at overview
   scope: DFCI is unavailable on Dell, HP and Lenovo because they appear on none of the DFCI OEM
   lists — unavailable, not an option declined; the Templates surface supports one vendor only; and
   the TPM/Secure-Boot exclusion is an enumeration fact rather than a stated limitation, so no
   first-party sentence exists to cite for it. Do not introduce new pitfall identifiers.

Finally, complete `## Related Resources` (the sibling `01-windows-dfci.md` plus the four
domain-boundary targets, each with a one-line description in the analog's style) and
`## External References` (the Microsoft Learn and vendor URLs actually cited above). Keep every
cross-file link at file level; the single exception in the whole phase is `../../_glossary.md#secure-boot`.

Write American spelling in every heading and every line of prose (D-59) — an H2 becomes a slug Phase
151 must match byte-for-byte. Use the full product name `Configuration Manager` if the product is
mentioned at all, and the singular ring noun if Autopatch is mentioned (D-58). Ship no fenced code
block (D-31) and no version-history section.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; node scripts/validation/v1.20-milestone-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' docs/operations/firmware-bios/00-overview.md` returns 9
    - `grep -c '^<a id=' docs/operations/firmware-bios/00-overview.md` returns 7
    - `grep -n '^<a id=' docs/operations/firmware-bios/00-overview.md` shows the ids `who-holds-the-secret`, `native-bios-surfaces`, `choosing-a-path`, `domain-boundary`, `updates-vs-configuration`, `before-you-start`, `unsupported-callouts` in that order
    - the two H2s `## Related Resources` and `## External References` are NOT preceded by an anchor line
    - `grep -c '^## Unsupported and Anti-Feature Callouts$' docs/operations/firmware-bios/00-overview.md` returns 1 (byte-identical to the four `patch-management/05-08` instances)
    - `docs/operations/firmware-bios/00-overview.md` contains the literal `For Dell, upload the Dell Client Configuration Tool Kit file (.cctk). The file size limit is 2 MB.`
    - `docs/operations/firmware-bios/00-overview.md` contains the literal `BIOS configuration changes can impact device functionality and operability, including the ability to boot or access Bitlocker encrypted drives.`
    - `grep -c 'lock the device beyond repair' docs/operations/firmware-bios/00-overview.md` returns 0 (that quote belongs to `01-windows-dfci.md` only, per D-38)
    - `grep -c '\.\./\.\./decision-trees/03-tpm-attestation\.md' docs/operations/firmware-bios/00-overview.md` returns at least 1
    - `grep -c '\.\./\.\./_glossary\.md#secure-boot' docs/operations/firmware-bios/00-overview.md` returns at least 1
    - `grep -c '\.\./\.\./reference/security-baseline-conflicts\.md' docs/operations/firmware-bios/00-overview.md` returns at least 1
    - `grep -c '\.\./patch-management/06-windows-driver-firmware-updates\.md' docs/operations/firmware-bios/00-overview.md` returns at least 1
    - the eight DFCI category names `UEFI access`, `Security features`, `Cameras`, `Microphones and speakers`, `Radios`, `Boot Options`, `Ports`, `Wake settings` all appear in `00-overview.md`, and the sentence naming them is followed by a `**Source:**` line citing `ref-dfci-settings-windows`
    - `grep -niE 'DFCI (cannot|can.t|does not|doesn.t) (set|configure|manage) (TPM|Secure Boot)' docs/operations/firmware-bios/00-overview.md` returns nothing
    - `git diff --name-only HEAD~1 HEAD` lists only files under `docs/operations/firmware-bios/`
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0 corpus-link failure(s)`
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0 with `16 passed, 0 failed`
  </acceptance_criteria>
  <done>`00-overview.md` is complete at nine H2s and seven anchors, carries BIOS-01, BIOS-02's disjointness statement and BIOS-11's Dell-template bricking quote, and both gates are green.</done>
</task>

<task type="auto">
  <name>Task 3: Prose-negative sweep and the full gate run for the overview</name>
  <files>docs/operations/firmware-bios/00-overview.md, docs/operations/firmware-bios/01-windows-dfci.md</files>
  <read_first>
    - docs/operations/firmware-bios/00-overview.md and docs/operations/firmware-bios/01-windows-dfci.md — the current committed state.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-34, D-49, D-57, D-58, D-59, D-60 for the exact barred forms each grep is testing, and D-01 for why the apex is the gate that matters.
  </read_first>
  <action>
Run every prose negative as a command and fix any hit in place before committing. D-60 exists because
`146-REVIEW.md` WR-06 found a CONTEXT prose rule broken four times inside one shipped file while seven
gates read green — a validator cannot see wrong prose, so the greps are the gate.

Then run the apex, `check-phase-144.mjs`. D-01 measured that the apex subsumes all five other gates:
it chains `check-phase-54` and `check-phase-59` at its lines 65-69, reaches `check-nav-hub-links`
through `check-phase-143.mjs`'s corpus-run validator, and covers the milestone audit plus the document
contract through its harness validator. Run the individual gates only to localise a failure. Record
the measured pass/fail/skip counts in the SUMMARY; do not transcribe the counts from CONTEXT.md — D-19
records that this milestone's counts have gone stale inside their own phase twice.

Record in the SUMMARY the accepted inconsistencies D-68 names, so the verifier and code review do not
re-surface them as new defects: `ROADMAP.md:185`'s "none in the existing corpus" blast-radius claim is
inaccurate because plan 02 edits `docs/_glossary.md`; `STATE.md:157`'s greenfield constraint is
imprecise, since the re-measured counts at HEAD are `UEFI` = 3 occurrences and `BIOS` = 52, not zero;
`docs/operations/00-index.md:32`'s hub description stays stale and unowned; and
`ARCHITECTURE.md:329` still calls one validator a live read when it reads at a frozen close SHA.
Re-measure the two corpus counts at execution time rather than copying the numbers above, and note the
metric trap D-19 records — a line count and an occurrence count are not comparable.

Also record in the SUMMARY that D-22's LOCKED naming and numbering flag is discharged: the directory
is `firmware-bios/`, chosen over the two alternatives `ARCHITECTURE.md:697` lists because BIOS-01
states the path literally and requirement text is the authority.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-144.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "SEMM" docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rniE "the six($|[^-])|most business OEMs" docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rniE "enrolment|enrols|enrolling\b|licence|behaviour|organisation|programme|labelled|honour|catalogue|recognise|analyse" docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rc '^```' docs/operations/firmware-bios/*.md` returns 0 for every file
    - `grep -rn "^## Version History" docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rniE "\bSystem Center\b|\bSCCM\b|Autopatch rings" docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rn '^> \*\*Platform:\*\*' docs/operations/firmware-bios/ | wc -l` returns 0
    - `grep -rnE '\]\((\./)?0[234]-' docs/operations/firmware-bios/ | wc -l` returns 0
    - `node scripts/validation/check-phase-144.mjs` exits 0, and its measured pass/fail/skip counts are recorded verbatim in the SUMMARY
    - the SUMMARY records the four D-68 accepted inconsistencies and the two corpus counts re-measured at execution time
    - the SUMMARY records the D-22 disposition naming `firmware-bios/` and its ground
  </acceptance_criteria>
  <done>Every prose negative returns zero, the apex is green, and the SUMMARY carries the re-measured counts, the accepted inconsistencies and the D-22 disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream vendor documentation → this corpus | First-party Microsoft/HP/Lenovo pages are read and quoted; the quoted bytes cross into a published artifact |
| This corpus → service-desk reader | A reader executes a firmware procedure on real hardware based on what this document says |

There is no application code, no authentication surface, no user input handling and no data storage in
this phase — the deliverables are static Markdown files. 149-RESEARCH.md's Security Domain section
reaches the same conclusion: **there is no ASVS/STRIDE attack surface here**, and the register below
exists to state that explicitly rather than to omit the block. ASVS level 1, block on `high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-149-01 | Spoofing | Quoted first-party strings in both new files | medium | mitigate | Every quoted string traces to a raw-byte capture in 149-RESEARCH.md section Sources or a live re-fetch (D-37, D-52); the Lenovo comparative claim ships as inference, never as attribution; task 1's precondition forces a re-fetch past the stated staleness date |
| T-149-02 | Tampering | Reader-executed firmware procedures | medium | mitigate | Task 2 ships the Bitlocker bricking Warning verbatim under `## Before You Start`; plans 04 and 05 carry the DFCI bricking warning and the destructive-order warning, each behind a one-way decision checkpoint |
| T-149-03 | Repudiation | Claim provenance | low | mitigate | Two-date `**Source:**` lines (D-51) make each claim's page and staleness visible in the shipped document; one page per line (D-56) |
| T-149-04 | Information disclosure | — | low | accept | No secret, credential or tenant-specific value is authored; the documents describe where secrets live, never a secret value |
| T-149-05 | Denial of service | — | low | accept | Static Markdown; no runtime |
| T-149-06 | Elevation of privilege | — | low | accept | No permission model is implemented; RBAC is described, not enforced |
| T-149-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs in this phase — 149-RESEARCH.md's Package Legitimacy Audit records "Not applicable"; no legitimacy checkpoint is required |

No threat is rated `high` or `critical`, so no blocking security checkpoint is inserted.
</threat_model>

<verification>
- `node scripts/validation/check-phase-144.mjs` (the apex) exits 0; counts recorded, not transcribed
- `node scripts/validation/check-nav-hub-links.mjs` — 0 hub-presence and 0 corpus-link failures
- `node scripts/validation/check-phase-54.mjs` — 32 passed, 0 failed (validator 27 walks `.planning/` too)
- `node scripts/validation/v1.20-milestone-audit.mjs` — 16 passed, 0 failed (this is the only gate that runs the banned-phrase check over `docs/`)
- All eight prose-negative greps in task 3 return zero
- Both new files land in one commit, cross-link each other, and neither direction dangles
</verification>

<success_criteria>
- `docs/operations/firmware-bios/00-overview.md` exists at 9 H2s / 7 hand-authored anchors / 0 fences / no version-history section, and routes by secret custody for Dell, HP and Lenovo with three separate two-date `**Source:**` lines
- `docs/operations/firmware-bios/01-windows-dfci.md` exists with front matter, the platform-applicability blockquote, its H1, `## What DFCI Is` and both tail sections
- ROADMAP SC#1 and SC#2 are satisfied by the overview's content; SC#5's Dell-template half is satisfied by `## Before You Start`
- The apex validator is green and its measured counts are in the SUMMARY
</success_criteria>

<output>
Create `.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-01-SUMMARY.md` when done
</output>
