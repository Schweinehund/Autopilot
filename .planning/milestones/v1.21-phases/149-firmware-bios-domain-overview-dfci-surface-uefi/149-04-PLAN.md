---
phase: 149-firmware-bios-domain-overview-dfci-surface-uefi
plan: 04
type: execute
wave: 3
depends_on: ["149-03"]
files_modified:
  - docs/operations/firmware-bios/01-windows-dfci.md
autonomous: true
requirements: [BIOS-02, BIOS-11]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "01-windows-dfci.md names the eight DFCI setting categories — UEFI access, Security features, Cameras, Microphones and speakers, Radios, Boot Options, Ports, Wake settings — under one **Source:** line for ref-dfci-settings-windows, and does not reproduce the full per-setting option matrix. [BIOS-02, D-35]"
    - "01-windows-dfci.md states the ship-or-route rule by name — a setting name ships when it carries a durable, high-consequence behavior, and high-churn enumerable data routes to the vendor or Microsoft page — and states honestly that roughly ten setting names ship under it. [D-35]"
    - "All THREE documented interaction traps ship: the category-versus-granular conflict loop, the boot-and-port trap in both halves, and the radios-disabled manageability trap. [D-36]"
    - "The category-versus-granular trap ships under the settings-surface H2 as a callout, not under the callouts H2, and its worked example is a numbered list rather than a fenced block. [D-36, 148 D-48]"
    - "The boot-and-port trap carries TWO **Source:** lines because its compliance half and its recovery half come from two different pages. [D-36, D-56]"
    - "01-windows-dfci.md states that the CPU and IO virtualization setting has no Disabled value, so DFCI structurally cannot turn platform virtualization off, and locates the virtualization-based-security exposure in firmware settings set outside DFCI. [D-07 — the inverse of the reversed draft claim]"
    - "The virtualization-based-security to update-eligibility chain ships in full, with its first-party sentence quoted from a live fetch and its own **Source:** line. [D-14]"
    - "01-windows-dfci.md quotes configure-dfci-windows's distinct bricking warning — that configuring and assigning DFCI profiles can lock the device beyond repair — under its own **Source:** line, and does not repeat 00-overview.md's Bitlocker Warning. [ROADMAP SC#5, BIOS-11, D-38]"
    - "01-windows-dfci.md carries the settings-reference page's additional sentence that the profile settings change the device hardware and can't be fixed by re-imaging the OS, under its OWN **Source:** line because it comes from a different page. [SC#5, 149-RESEARCH.md finding 1; edge BIOS-11/idempotency — re-running or re-imaging does not undo the change]"
    - "01-windows-dfci.md states in its own sentence that deleting the DFCI profile, or removing the device from the group assigned to the profile, does not remove DFCI settings or re-enable the UEFI menus — quoted first-party — and notes that deleting the profile is the common instinct and the wrong move. [SC#5 clause 3, BIOS-11, D-39; edge BIOS-11/empty — the null-operation case]"
    - "node scripts/validation/check-nav-hub-links.mjs and node scripts/validation/v1.20-milestone-audit.mjs are both green after every commit in this plan. [D-57]"
  artifacts:
    - docs/operations/firmware-bios/01-windows-dfci.md
  key_links:
    - "01-windows-dfci.md links to ../patch-management/01-windows-wufb-rings.md for the update-eligibility half of the virtualization chain — the driver and firmware policy anchor lives in that file, not in 06 (D-17)"
    - "01-windows-dfci.md links back to 00-overview.md for the Dell-template bricking Warning it deliberately does not repeat (D-38)"
  prohibitions:
    - "The sentence about disabling CPU and IO virtualization through DFCI is not written in any form — the setting has no Disabled value and the operation is impossible. [D-07]"
    - "The Surface model-gated consequence that a device reports noncompliant is not written — it is a fabrication with no first-party source. [D-08]"
    - "No string ships inside quotation marks that was not captured from a live page in this session or in 149-RESEARCH.md section Sources; in particular the recovery-consequence sentence about opening the device is unquoted researcher prose in the research ledger and either ships paraphrased or is re-fetched first. [D-37, D-52]"
    - "No single **Source:** line covers claims drawn from two different pages. [D-56]"
    - "The full per-setting option matrix for the eight categories does not ship. [D-35]"
    - "No fenced code block is added — the worked example is a numbered list. [D-31, D-36]"
    - "The deferred vendor tool named in 149-CONTEXT.md D-34 is not mentioned."
    - "The banned-phrase forms named in 149-CONTEXT.md D-58 appear nowhere in this file; if the update-management service is named, the singular ring noun is used."
    - "The British spelling forms named in 149-CONTEXT.md D-59 appear nowhere in this file."
    - "The Bitlocker Warning quoted in 00-overview.md is not repeated here. [D-38]"
---

<objective>
Author the DFCI guide's consequence half: what the settings surface actually exposes, the three
documented interaction traps, and the bricking surface.

Purpose: this is where the phase's highest-consequence content lives. Two of its three traps have
field consequences a reader cannot undo, and ROADMAP Success Criterion 5's bricking clause and its
third clause both land here. D-38 confines this file to `configure-dfci-windows`'s warning — the
overview owns the Dell-template Warning — so no load-bearing quote is duplicated.

Output: two new anchored H2s in `docs/operations/firmware-bios/01-windows-dfci.md` —
`## The Settings Surface` and `## Bricking and Irreversible Configuration`.

Decisions this plan implements or records: D-01 (the apex subsumes the individual gates), D-07
(REVERSED — ship the inverse; the virtualization setting has no Disabled value so DFCI cannot be the
mechanism that turns it off), D-14 (CORRECTED — the virtualization-to-update-eligibility hop IS
first-party sourced; ship the full chain on the corrected mechanism and hand the matrix column to
Phase 150), D-33 (referenced forward — the Surface removal-order warning lands in plan 05), D-35 (the
settings surface ships as categories only plus the three traps, and the ship-or-route rule is stated
by name because roughly ten setting names ship anyway), D-36 (all THREE traps; trap 1 as a callout
under the settings H2 and not under the callouts H2; trap 2 needs TWO evidence lines), D-37 (nothing
ships in quotation marks that was not read from a live page), D-38 (two bricking warnings on two
pages, one per file), D-39 (SC#5's third clause had zero decisions — it gets its own sentence), D-40
(referenced forward — retire, reuse and recover stay three separate H2s in plan 05), D-43 (the
TPM/Secure-Boot absence is an enumeration fact), D-55 (the verifier re-fetches and diffs, and this
plan says so), D-57 (three gates genuinely bind this phase), D-60 (every prose negative carries a
runnable grep), D-65 (referenced forward — the matrix column is Phase 150's), D-66 (referenced
forward — Phase 152 owns all wiring), D-67 (referenced forward — the needle-spec is Phase 153's).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-PATTERNS.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-03-SUMMARY.md
@docs/operations/patch-management/06-windows-driver-firmware-updates.md
</context>

## Artifacts this phase produces

New symbols created by Phase 149 (excluded from drift verification — none exists at HEAD `d9f3c4c5`):

**Files:** `docs/operations/firmware-bios/00-overview.md`,
`docs/operations/firmware-bios/01-windows-dfci.md`.

**Hand-authored anchor ids added by THIS plan:** `dfci-settings-surface`, `bricking-irreversible`.

**Other new anchor ids in the phase:** `who-holds-the-secret`, `native-bios-surfaces`,
`choosing-a-path`, `domain-boundary`, `updates-vs-configuration`, `before-you-start`,
`unsupported-callouts` (in `00-overview.md`); `what-dfci-is`, `dfci-prerequisites`,
`dfci-oem-support`, `surface-eligibility`, `retiring-a-device`, `reusing-a-device`,
`recovering-locked-device`, `unsupported-callouts` (in `01-windows-dfci.md`).

**New glossary terms** (plan 02): `### Device Firmware Configuration Interface (DFCI)`, `### UEFI CSP`,
`### BIOS password`, `### BIOS configuration and other settings`.

**New nav/hub entries:** none — Phase 152 owns all registry, filename-map, canary, ops-index and
`docs/index.md` wiring (D-66).

<tasks>

<task type="auto">
  <name>Task 1: Author the settings surface and the category-versus-granular trap</name>
  <files>docs/operations/firmware-bios/01-windows-dfci.md</files>
  <read_first>
    - docs/operations/firmware-bios/01-windows-dfci.md — the current state after plan 03.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources item (d), which lists the eight confirmed category headings, and Pitfall 6, which records the impossible-operation trap.
    - .planning/research/STACK.md lines 169-188 — the settings table and the three-trap analysis. Read for coordinates and setting names only; this file is a research ledger and nothing in it may be rendered inside quotation marks.
    - .planning/REQUIREMENTS.md — the `## Out of Scope` section and its trailing sentence scoping the bar to not rewriting vendor manuals, which is why that bar does NOT reach the Intune settings reference.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-35, D-36, D-37, D-56.
  </read_first>
  <action>
Add the anchored H2 `## The Settings Surface` (anchor id `dfci-settings-surface`, on its own line
immediately above the heading) after `## Surface Eligibility`.

Open by naming the eight categories the DFCI settings reference enumerates — UEFI access, Security
features, Cameras, Microphones and speakers, Radios, Boot Options, Ports, and Wake settings — under one
standalone `**Source:**` line for `ref-dfci-settings-windows` in the two-date form
`(ms.date 2026-06-23, updated 2026-07-01)`, using the canonical
`intune/device-configuration/templates/ref-dfci-settings-windows` URL path. Do not reproduce the
per-setting option matrix; link to that page for it.

Then state the ship-or-route rule by name, in one short paragraph, so a verifier can check the section
against it rather than against a vague intention: **a setting name ships in this guide when it carries
a durable, high-consequence behavior — bricking, an interaction trap, or a compliance conflict — and
routes out to the vendor or Microsoft page when it is high-churn enumerable data with no independent
narrative value.** Then be honest that roughly ten individual setting names ship in this file under
that rule, across this section, the Surface section and the retire runbook. D-35 records that an
earlier draft's claim of "no settings table" was illusory for exactly this reason; naming the rule and
its consequence is what keeps the claim true. Note that the requirements document's per-model-matrices
bar does not reach this page — its trailing sentence scopes it to not rewriting vendor manuals, and
this is a Microsoft Intune policy reference. The precedent is 148 D-45, which shipped a bounded table
plus a link-not-copy pointer.

Then ship trap 1, the **category-versus-granular conflict loop**, as a callout under THIS H2 — not
under `## Unsupported and Anti-Feature Callouts`, which every sibling instance reserves for documented
first-party *absences*, and a user-caused misconfiguration is neither unsupported nor an anti-feature
(D-36). State the failure: configuring both a category setting and one of its granular members makes
the two flip each other noncompliant on every sync, indefinitely. Then give Learn's own worked example
for allowing only Wi-Fi as a **numbered list, never a fenced block** (148 D-48, and this file ships zero
fences): leave the radios category at Not configured, set Wi-Fi to Enabled, set every other granular
radio to Disabled. Carry a `**Source:**` line for the page the worked example comes from.

Do not write any sentence describing the disabling of CPU and IO virtualization through DFCI. The
settings reference lists only Not configured and Enabled for that setting — there is no Disabled value
and the operation is structurally impossible (D-07, 149-RESEARCH.md Pitfall 6). The reversed claim is
recorded in CONTEXT so no later reader re-imports it.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; node scripts/validation/v1.20-milestone-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^<a id="dfci-settings-surface"></a>' docs/operations/firmware-bios/01-windows-dfci.md` returns 1, and the line immediately below it is `## The Settings Surface`
    - all eight category names appear in this section: `awk '/^## The Settings Surface$/,/^<a id="bricking/' docs/operations/firmware-bios/01-windows-dfci.md` contains `UEFI access`, `Security features`, `Cameras`, `Microphones and speakers`, `Radios`, `Boot Options`, `Ports` and `Wake settings`
    - a sentence in this section states the ship-or-route rule and contains both the durable-consequence half and the high-churn half
    - a sentence in this section acknowledges the approximate number of individual setting names the file ships
    - `grep -c '^```' docs/operations/firmware-bios/01-windows-dfci.md` returns 0
    - the worked example appears as a numbered list (`awk '/^## The Settings Surface$/,/^<a id="bricking/' docs/operations/firmware-bios/01-windows-dfci.md | grep -cE '^[0-9]+\. '` returns at least 3)
    - `grep -niE "disabl[a-z]* (the )?CPU and IO virtualization" docs/operations/firmware-bios/01-windows-dfci.md` returns nothing
    - `grep -niE "no Disabled value|only Not configured and Enabled|cannot (turn|be used to turn).*virtualization off" docs/operations/firmware-bios/01-windows-dfci.md` returns at least one line
    - every `**Source:**` line added by this task carries both dates and cites exactly one URL
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0 corpus-link failure(s)`
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0 with `16 passed, 0 failed`
  </acceptance_criteria>
  <done>The eight categories, the named ship-or-route rule and trap 1 with its numbered worked example are in place, and the impossible-operation claim is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Author traps 2 and 3 and the virtualization-to-update-eligibility chain</name>
  <files>docs/operations/firmware-bios/01-windows-dfci.md</files>
  <read_first>
    - docs/operations/firmware-bios/01-windows-dfci.md — the current state after task 1.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources items (d) and (e); item (d) carries the verbatim compliance sentence for trap 2 and item (e) is the page the storage-replacement case comes from.
    - .planning/research/PITFALLS.md lines 288-290 — the sourced virtualization-based-security sentence and its prescribed matrix column. Read for the coordinate and the URL only; the string must be re-fetched before it is quoted.
    - docs/operations/patch-management/01-windows-wufb-rings.md — the driver and firmware policy anchor at line 170 and the hotpatch anchor at line 119. Read for the link target; do not edit this file, which Phase 146 sealed.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-07, D-14, D-17, D-36, D-37, D-56.
  </read_first>
  <action>
Add traps 2 and 3 under the same `## The Settings Surface` H2, after trap 1.

**Trap 2, the boot-and-port trap, ships in BOTH halves and carries TWO `**Source:**` lines** because
its two halves come from two different pages, and one evidence line may never span two pages (D-56;
this is the defect `146-REVIEW.md` records as WR-01).
- The compliance half, quoted verbatim from `ref-dfci-settings-windows`: `When set to Disabled, don't
  set the Boot from network adapters setting to Enabled. It causes the Boot from external media (USB,
  SD) setting or Boot from network adapters setting to become noncompliant.` Under its own
  `**Source:**` line, two dates, that page's URL.
- The recovery half: disabling all external boot options or all external ports significantly
  complicates operating-system recovery, and on an unbootable Surface with both external-media boot
  and the USB port disabled, recovery means replacing the internal storage. The research ledger's
  wording for this is unquoted researcher prose, not a first-party string (D-37 records that an earlier
  draft rendered it as italicised verbatim). Either re-fetch the two pages and quote the exact
  sentences, or write this half as a paraphrase with no quotation marks. Either way it carries its own
  `**Source:**` line for the Surface guide, separate from the compliance half's.

**Trap 3, the radios trap:** setting the radios category to Disabled requires a wired network
connection or the device can become unmanageable. One sentence, its own `**Source:**` line.

Then ship the **virtualization chain** in full, on the corrected mechanism (D-07 plus D-14). Three
steps, in this order:
1. The DFCI setting for CPU and IO virtualization exposes only Not configured and Enabled, so DFCI can
   turn platform virtualization on and structurally cannot turn it off.
2. The exposure therefore lives in firmware settings configured outside DFCI — at the vendor's BIOS
   surface — not in a DFCI profile.
3. A device without virtualization-based security enabled and running can be temporarily ineligible for
   in-memory update servicing. Quote the first-party sentence for this step and give it its own
   `**Source:**` line. The sentence is available from the update-management service's own frequently
   asked questions page; fetch it as raw bytes with `curl -A "Mozilla/5.0 ..."` before quoting it,
   because 149-RESEARCH.md did not re-fetch that page and D-52 bars shipping a quoted string that was
   not read from a live page. If the fetch fails, write step 3 unquoted with a `**Source:**` line
   carrying the URL, and record the fetch failure in the SUMMARY.

Link the eligibility half to `../patch-management/01-windows-wufb-rings.md` at file level. The
driver and firmware policy anchor lives in that file, not in `06-windows-driver-firmware-updates.md`
(D-17 records that the draft printed the right coordinate under the wrong antecedent). If the
update-management service is named in prose, use the singular ring noun (D-58).

Record in the SUMMARY that the matrix column naming which operating-system features are gated by this
setting is handed to Phase 150, not authored here (D-14, D-65).

Do not write the Surface model-gated compliance consequence this plan's acceptance criteria
negative-grep for. D-08 measured it
as a fabrication with no first-party source; the only two noncompliance mechanisms on record are the
category-versus-granular loop and the boot-versus-network-adapter conflict, both of which ship above.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; node scripts/validation/v1.20-milestone-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - the file contains the literal `When set to Disabled, don't set the Boot from network adapters setting to Enabled.`
    - the file contains the literal `to become noncompliant.`
    - the boot-and-port trap is followed by TWO distinct `**Source:**` lines citing two different URLs
    - a sentence names the storage-replacement recovery consequence, and if it appears inside quotation marks the SUMMARY records the live fetch it came from
    - a sentence states that the radios category set to Disabled requires a wired connection or the device can become unmanageable, with its own `**Source:**` line
    - the virtualization chain has all three steps present, in order, and the third carries its own `**Source:**` line
    - `grep -c '\.\./patch-management/01-windows-wufb-rings\.md' docs/operations/firmware-bios/01-windows-dfci.md` returns at least 1, with no `#` fragment
    - `grep -niE "reports? noncompliant" docs/operations/firmware-bios/01-windows-dfci.md` returns nothing that attributes a model-gated consequence to Surface
    - `grep -niE "Autopatch rings" docs/operations/firmware-bios/01-windows-dfci.md` returns nothing
    - `git diff --name-only HEAD~1 HEAD` lists only `docs/operations/firmware-bios/01-windows-dfci.md`
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0 corpus-link failure(s)`
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0 with `16 passed, 0 failed`
    - the SUMMARY records the outcome of the frequently-asked-questions page fetch and the Phase-150 hand-off of the matrix column
  </acceptance_criteria>
  <done>All three traps ship, trap 2 with two evidence lines, and the virtualization chain reads in the corrected direction.</done>
</task>

<task type="auto">
  <name>Task 3: Author the bricking and irreversible configuration section</name>
  <files>docs/operations/firmware-bios/01-windows-dfci.md</files>
  <read_first>
    - docs/operations/firmware-bios/01-windows-dfci.md — the current state after task 2.
    - docs/operations/firmware-bios/00-overview.md — the `## Before You Start` section, so the Bitlocker Warning quoted there is not repeated here.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources items (b) and (d), and the Summary's finding 1 about the expanded warning on page (d) that CONTEXT.md's quoted set did not carry.
    - .planning/research/PITFALLS.md line 240 — that deleting the profile is the common instinct and the wrong move. Read for the coordinate; write the point in the corpus's own voice.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-38, D-39, D-55, D-56.
  </read_first>
  <action>
Add the anchored H2 `## Bricking and Irreversible Configuration` (anchor id `bricking-irreversible`)
after the settings surface.

Ship three things, each with its own standalone `**Source:**` line, because they come from two
different pages and one line may never span two (D-56):

<!-- planner-discipline-allow: lock the device beyond repair -->
1. `configure-dfci-windows`'s distinct warning, quoted verbatim: `Configuring and assigning DFCI
   profiles can lock the device beyond repair. So, pay attention to the values you configure.`
   `**Source:**` line for that page, two dates
   `(ms.date 2026-06-23, updated 2026-07-01)`. This is `01`'s bricking quote and the only one it gets.
   Do NOT repeat `00-overview.md`'s Bitlocker Warning here — one quote per file (D-38); instead link to
   `00-overview.md` for the Dell-template surface's own warning.

2. The settings-reference page's additional sentence, quoted verbatim: `The DFCI profile settings
   change the device hardware, and can't be fixed by re-imaging the OS.` This is a stronger, separate
   first-party sentence that 149-RESEARCH.md surfaced and CONTEXT.md's quoted set does not carry, and
   it lives on a different page — so it needs its own `**Source:**` line for
   `ref-dfci-settings-windows`, two dates `(ms.date 2026-06-23, updated 2026-07-01)`. It is the
   sentence that makes the irreversibility concrete: a reader's default recovery instinct is to
   re-image, and re-imaging does not touch this.

3. Success Criterion 5's third clause, as its own sentence under its own subheading or bold lead-in,
   quoted verbatim from `configure-dfci-windows`: `Deleting the DFCI profile, or removing a device from
   the group assigned to the profile doesn't remove DFCI settings or re-enable the UEFI (BIOS) menus.`
   Then state, in the corpus's own voice, that deleting the profile is the common instinct and is
   exactly the wrong move — the settings stay and the menus stay locked, and the device must be
   released through the retire sequence documented below instead. This clause had zero decisions before
   D-39; it is graded by SC#5 and must not be folded into a general bricking paragraph.

Close the section with a forward pointer to the retire, reuse and recover sections that plan 05
authors. Do not author those sections here.

Record in the SUMMARY that the verifier is expected to re-fetch and diff every quoted string in this
file. `grep -rn "Source:" scripts/` returns zero — no validator in this repository parses evidence
lines, so the verifier is the only gate that can catch a fabricated quote, and this phase quotes seven
pages (D-55).
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; node scripts/validation/check-phase-144.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^<a id="bricking-irreversible"></a>' docs/operations/firmware-bios/01-windows-dfci.md` returns 1, and the line immediately below it is `## Bricking and Irreversible Configuration`
    - the file contains the literal `Configuring and assigning DFCI profiles can lock the device beyond repair.`
    - the file contains the literal `The DFCI profile settings change the device hardware, and can't be fixed by re-imaging the OS.`
    - the file contains the literal `Deleting the DFCI profile, or removing a device from the group assigned to the profile doesn't remove DFCI settings or re-enable the UEFI (BIOS) menus.`
    - `grep -c 'ability to boot or access Bitlocker encrypted drives' docs/operations/firmware-bios/01-windows-dfci.md` returns 0 (that quote belongs to `00-overview.md` alone)
    - three distinct `**Source:**` lines exist in this section, and the URLs cited are `configure-dfci-windows`, `ref-dfci-settings-windows` and `configure-dfci-windows` respectively — no line cites two pages
    - a sentence states that deleting the profile is the common instinct and the wrong move
    - `grep -c '00-overview.md' docs/operations/firmware-bios/01-windows-dfci.md` returns at least 1
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0 corpus-link failure(s)`
    - `node scripts/validation/check-phase-144.mjs` exits 0, and its measured counts are recorded verbatim in the SUMMARY
    - the SUMMARY records the expectation that the verifier re-fetches and diffs every quoted string
  </acceptance_criteria>
  <done>Both bricking sentences and Success Criterion 5's third clause ship, each with its own evidence line, and the overview's Warning is not duplicated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream Microsoft documentation → this corpus | Bricking warnings and trap mechanics are quoted across three pages |
| This corpus → a reader configuring a live fleet | A misread trap or a missing warning results in unrecoverable hardware |

This plan authors static Markdown. There is no application code, no authentication surface, no user
input handling and no data storage — **there is no ASVS/STRIDE attack surface**, the conclusion
149-RESEARCH.md's Security Domain section reaches, stated here rather than omitted. ASVS level 1,
block on `high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-149-19 | Tampering | Reader-applied DFCI settings | high | mitigate | All three traps ship with their consequences; both bricking sentences ship verbatim; the null-operation clause states that deleting the profile changes nothing |
| T-149-20 | Spoofing | Quoted warning strings | high | mitigate | Every quoted string traces to 149-RESEARCH.md section Sources; the recovery-consequence sentence and the update-eligibility sentence are either re-fetched live or shipped unquoted, with the outcome recorded (D-37, D-52) |
| T-149-21 | Repudiation | Claim provenance across three pages | medium | mitigate | One page per `**Source:**` line; trap 2 carries two lines because it spans two pages |
| T-149-22 | Information disclosure | — | low | accept | No secret or tenant-specific value is authored |
| T-149-23 | Denial of service | Device manageability | medium | mitigate | Trap 3 states that a radios-disabled device needs a wired connection or becomes unmanageable — this is documented rather than mitigated in code, because the document is the control |
| T-149-24 | Elevation of privilege | — | low | accept | No permission model is implemented |
| T-149-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs — 149-RESEARCH.md's Package Legitimacy Audit records "Not applicable" |

T-149-19 and T-149-20 are rated `high` and the configured block threshold is `high`; both are disposed
`mitigate`, enforced by named acceptance criteria in tasks 2 and 3 rather than by a validator, because
no validator in this repository parses evidence lines.
</threat_model>

<verification>
- `node scripts/validation/check-nav-hub-links.mjs` — 0 corpus-link failures after every commit
- `node scripts/validation/v1.20-milestone-audit.mjs` — 16 passed, 0 failed
- `node scripts/validation/check-phase-144.mjs` — the apex, green; counts recorded, not transcribed
- `grep -c '^\`\`\`' docs/operations/firmware-bios/01-windows-dfci.md` returns 0
- Every `**Source:**` line added cites exactly one URL and carries both dates
</verification>

<success_criteria>
- ROADMAP SC#5's bricking half is satisfied: the surface is quoted first-party, with the settings-reference page's stronger irreversibility sentence carried under its own evidence line
- ROADMAP SC#5's third clause is satisfied: deleting the profile or the group assignment removes nothing, quoted, with the wrong-instinct note
- All three interaction traps ship, trap 1 as a callout under the settings H2 with a numbered worked example, trap 2 with two evidence lines
- The virtualization chain reads in the corrected direction and the matrix column is handed to Phase 150
- `docs/operations/firmware-bios/01-windows-dfci.md` stands at six anchored H2s plus its two tail sections
</success_criteria>

<output>
Create `.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-04-SUMMARY.md` when done
</output>
