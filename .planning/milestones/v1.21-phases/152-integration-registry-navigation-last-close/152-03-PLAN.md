---
phase: 152-integration-registry-navigation-last-close
plan: 03
type: execute
wave: 3
depends_on: ["152-01", "152-02"]
files_modified:
  - docs/operations/00-index.md
  - docs/index.md
autonomous: true
requirements: [INT-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Navigation is wired only after the registry knows the documents exist: the cleanliness gate at the head of this plan proves exactly one commit touched docs/_registry/ or scripts/pipeline/ since the phase base, and that the filename map is byte-identical to its committed blob (D-07, INT-04)."
    - "docs/operations/00-index.md gains a Firmware and BIOS Governance H2 placed after Apple Business Governance and before Version History, carrying a Guide/Covers table of exactly six rows — the five firmware-bios documents plus a cross-directory row for the reference matrix (D-29, D-30, D-31)."
    - "The operations-index Patch table grows from five rows to nine, appending the four new patch-management documents in filename order after the existing Android row, each opening with a markdown link and one terse Covers clause (D-32)."
    - "docs/index.md gains a Firmware and BIOS Governance H3 under the Operations H2, mirroring the operations-index placement, carrying exactly three rows (D-38, D-39)."
    - "The docs/index.md Patch sub-table gains exactly one row, for Linux update delivery — the one platform with a shipped guide and zero presence anywhere in the master hub's Operations block (D-41)."
    - "The docs/index.md recipes table gains exactly one row for Recipe #5, appended after the RE-225 row in filename order, with a description that does not imply a device end state (D-42)."
    - "The recipes quick-nav bullet remains exactly one line: its lead-in prose is amended and a lowercase fragment is appended to its parenthetical list; the line is never split (D-43, D-45, D-46, INT-04)."
    - "The Operations quick-nav bullet is byte-unchanged — the direct structural precedent deliberately left its sibling gap unfixed, and half-fixing it here is out of scope (D-43)."
    - "The operational-domain clause is appended to the banner blockquote paragraph that enumerates operational domains, not to the paragraph that lists provisioning platforms; the edited paragraph measures under the 200-character cap after the edit (D-44)."
    - "Both files gain a Version History row at the top of their reverse-chronological tables, dated at execution, naming the edits and the requirement identifier, with the Author cell set to the repository's existing placeholder (D-36, D-47)."
    - "Full-corpus C17 reports 236 files checked, 0 with violations, 0 total violations — the same count as before the phase, which is the cheapest observable proving no operations document accidentally acquired a document identifier (D-68)."
    - "The link checker reports zero hub-presence failures and zero corpus-link failures; every new row's link target and anchor fragment resolves (D-50, D-70)."
    - "INT-04 adjacency edge: the recipes quick-nav bullet is one line and only one — the count of lines matching its line-start anchor is exactly 1 both before and after."
    - "INT-04 empty edge: no new table is empty — the operations-index Firmware table has exactly 6 rows, the master-hub Firmware sub-table exactly 3, the operations-index Patch table exactly 9, the master-hub Patch sub-table exactly 4, and the master-hub recipes table exactly 5."
    - "INT-04 ordering edge: new Patch rows append in filename order after the existing Android row; the new heading is appended after Apple Business Governance and before Version History on both files, preserving the existing one-to-one section-order mirror; Version History rows insert at the top."
  artifacts:
    - docs/operations/00-index.md
    - docs/index.md
  key_links:
    - "The operations index is read by six validators and is NOT C17-enrolled; docs/index.md is C17-enrolled and is bound by all thirteen C17 assertions plus the link checker plus seventeen milestone audits. Every gate argument must distinguish them."
    - "The banner blockquote is measured by the C17 blockquote-length assertion, which strips the marker and joins only consecutive marker runs — the four paragraphs measure separately, and the provisioning-platform paragraph has only eighteen characters of headroom while the operational-domain paragraph has fifty-seven."
    - "The recipes quick-nav assertion in the frozen Phase 137 validator anchors at line start and then tests the recipe fragments on the isolated line; splitting the line takes the count off 1 and reddens it."
  prohibitions:
    - "The recipes quick-nav bullet is never split into two lines."
    - "The Operations quick-nav bullet line is byte-unchanged."
    - "The banner blockquote paragraph listing provisioning platforms is byte-unchanged; the operational-domain clause goes on the operational-domain paragraph only."
    - "The stale four-platform description cells are NOT corrected — owner-ruled out of this phase, and a blanket replace would make two genuinely four-platform rows less accurate while fixing one."
    - "D-48: no docs/index.md row for the firmware OEM matrix — four of the eight reference matrix and comparison files are absent from that hub entirely, including the other OEM matrix, so the precedent for an OEM matrix is absence. D-74: no docs/reference/00-index.md row for it either — four of seven reference matrices have no row there, and that index is not a registered nav hub so omission cannot redden the link checker."
    - "The operations-index frontmatter is not re-stamped; its review window already sits at exactly sixty days with zero margin."
    - "No H2 rename on docs/index.md — the Operations and Cross-Platform References anchors must remain stable."
    - "No commit in this plan; the two hub edits are staged into Commit B by Plan 04."
    - "No TBD, TODO, FIXME, XXX or PLACEHOLDER token anywhere in the new operations-index content outside its Version History table."
    - "The Firmware heading must not be one of the three barred literal headings on the operations index, none of which concerns firmware."
    - "D-66: no bare-noun platform blockquote token (the form barred by V-54-27 in check-phase-54.mjs) at line start outside a fence, in any file this plan writes, including this plan — the corpus-wide negative scans .planning/ as well as docs/."
    - "No accepted-violation baseline: scripts/validation/v1.20-audit-allowlist.json is not edited."
---

<objective>
Wire navigation on both index surfaces, last — after the registry has proved the eleven documents
exist and the bundle has proved they are reachable.

Purpose: INT-04. A hub row pointing at a document the registry does not know about is the ordering
failure this phase exists to avoid; a hub row pointing at a document that does not exist is
immediate link-checker red. Both risks are retired by the time this plan runs.

Output: edits to `docs/operations/00-index.md` and `docs/index.md`, left uncommitted for Plan 04's
single Commit B.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md
@.planning/phases/152-integration-registry-navigation-last-close/152-PATTERNS.md
@docs/operations/00-index.md
@docs/index.md
</context>

<commit_contract>
**This overrides the executor's default per-task commit behaviour. Read it before Task 1.**

This plan makes **no commit at all**. Both hub files are left modified in the working tree. Plan 04
adds four inbound-link edits and then lands all six files in a single Commit B, which is what
INT-04's navigation-last mandate requires — the phase has exactly two content commits, and this is
the second one's first half.

Do not run `git add` and do not run `git commit` in this plan. At the end of this plan,
`git status --porcelain -- docs/` must list exactly two modified paths and nothing staged.
</commit_contract>

<patterns_correction>
**Correction of record — read before using 152-PATTERNS.md.** That file's "Patch table
row-growth pattern" section labels its excerpt `docs/operations/00-index.md:301-308` and shows a
`| Resource | Description |` table under a `### Patch & Update Management` H3. That excerpt is
`docs/index.md` content, not the operations index. The operations index is 79 lines long, its
Patch section is a **top-level H2**, and its table header is the Guide/Covers pair. Use the
coordinates in this plan, which were measured against the files at the phase base, and treat the
PATTERNS.md coordinate as an error rather than a second opinion.
</patterns_correction>

<tasks>

<task type="auto">
  <name>Task 1: Cleanliness gate, then the operations index — new domain section, four Patch rows, Version History</name>
  <files>docs/operations/00-index.md</files>
  <precondition>Commit A has landed: exactly one commit exists between the phase base `56f55307` and HEAD touching `docs/_registry/` or `scripts/pipeline/`, and `scripts/pipeline/filename-map.md` is byte-identical to its committed blob.</precondition>
  <read_first>
    - docs/operations/00-index.md — the whole file; it is 79 lines. Note its six H2s in order (Co-Management, Patch & Update Management, App Lifecycle Automation, Compliance Drift Detection + Tenant Migration, Apple Business Governance, Version History), the Guide/Covers table shape, the em-dash convention in the lead-in sentences, and the Version History table's reverse-chronological order with a two-hyphen Author placeholder
    - docs/operations/firmware-bios/00-overview.md, 01-windows-dfci.md, 02-dell-bios-configuration.md, 03-hp-bios-configuration.md, 04-lenovo-bios-configuration.md — read each H1 and enough of each Summary to write a truthful one-clause Covers cell
    - docs/reference/firmware-oem-matrix.md — same, for the cross-directory row
    - docs/operations/patch-management/05-linux-update-delivery.md, 06-windows-driver-firmware-updates.md, 07-windows-autopatch.md, 08-windows-app-updates.md — same, for the four new Patch rows
    - .planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md — D-07, D-29, D-30, D-31, D-32, D-33, D-34, D-35, D-36, D-37, D-65
  </read_first>
  <action>
**Step 0 — the cleanliness gate, and it has teeth.** Before editing anything, assert:
  - `git log --oneline 56f55307..HEAD -- docs/_registry/ scripts/pipeline/` returns exactly one
    line;
  - `git hash-object scripts/pipeline/filename-map.md` equals
    `git rev-parse HEAD:scripts/pipeline/filename-map.md`.
A bare porcelain check is nearly a no-op right after a commit, but the bundle run in Plan 02
rewrites that tracked file as a build step, so a dirty `scripts/pipeline/` is genuinely reachable
here. If either assertion fails, stop and report; do not edit hub files over an unclean pipeline.

**Step 1 — append the new domain section.** Insert a top-level heading whose literal text is
`## Firmware and BIOS Governance`, placed **after** the `## Apple Business Governance` section's
table and **before** `## Version History`. Two things fix this literal: the recorded heading in the
milestone architecture artifact carries an ampersand form, and Phase 149's ratified
and-not-ampersand convention converts it to exactly the string above. Its GitHub slug is
`firmware-and-bios-governance` — one hyphen between each word, no double hyphen, because no
punctuation is being stripped from between two spaces.

Placement is appended, not domain-adjacent: no validator asserts H2 order on this file — both
readers use substring presence — so the choice is unforced, appending is the only precedent on this
file, it gives the smaller diff, and it preserves the existing one-to-one ordering mirror against
the master hub's Operations sub-headings. The same placement rule applies to `docs/index.md` in
Task 2.

Under the heading write a lead-in sentence in the same voice and dash convention as the five
sibling sections, then a `| Guide | Covers |` table with the same separator row shape and
**exactly six rows**, in this order:

  1. `firmware-bios/00-overview.md` — the domain overview
  2. `firmware-bios/01-windows-dfci.md` — the DFCI guide
  3. `firmware-bios/02-dell-bios-configuration.md`
  4. `firmware-bios/03-hp-bios-configuration.md`
  5. `firmware-bios/04-lenovo-bios-configuration.md`
  6. `../reference/firmware-oem-matrix.md` — a cross-directory row

Paths are relative to `docs/operations/`. Row 6 is beyond the literal wording of INT-04 and is
flagged as such in the summary; it is the matrix's only operations-side reachability, because the
master hub deliberately carries no row for it. The Apple Business section establishes
cross-directory rows on this file at five of five, so the shape is precedented.

Each row's link text is a short label in the style of the existing rows (not the full H1), and each
Covers cell is one terse clause drawn from what the guide actually covers — read the guide, do not
invent.

**Step 2 — grow the Patch table from five rows to nine.** Append four rows after the existing
Android row, in filename order: `05-linux-update-delivery.md`,
`06-windows-driver-firmware-updates.md`, `07-windows-autopatch.md`,
`08-windows-app-updates.md`. Each row opens with a markdown link and carries one terse Covers
clause matching the density of the existing five. Paths are relative to `docs/operations/`.

INT-04 and SC#4 read a singular "a Patch row"; the requirements text resolves it in favour of four,
and Phase 145 converted the row-count equality that guards this table into a frozen read
specifically so this growth is safe.

**Step 3 — the prose constraint, which is a hard gate, not a style note.** The milestone audit
walks `docs/` **live** with four case-insensitive proximity patterns, each cleared only by a
disambiguation keyword inside a 200-character window. The one this phase can actually reach is the
adjacent phrase formed by the Autopatch product name immediately followed by the word for a
deployment ring, in that order with a single space between. Your description cell for the Autopatch
guide is *about* groups and deployment rings, so it is live exposure. Break the adjacency: write
about Autopatch groups and the Test and Last deployment rings, so the product name is never
immediately followed by the ring word. The same bar applies to the legacy Configuration Manager
product name in its two barred forms — the cheapest defence is to write `Configuration Manager`
throughout — and to a mobile attestation service name near the word compliance. A red here is fixed
in the prose, never in an allowlist.

**Step 4 — Version History.** Insert one new row at the **top** of the Version History table,
beneath the separator row. The table is reverse-chronological. The Date cell is the execution date.
The Change cell names **both** edits made in this task — the new domain section and the four
appended Patch rows — and cites `INT-04`. The Author cell uses the two-hyphen placeholder every
existing row in this table uses.

**Step 5 — leave alone.** Do not correct the stale four-platform description cell in the Patch
table. It is owner-ruled out of this phase, the requirements text scopes this phase to adding rows,
and Phase 149 lists this exact cell among accepted inconsistencies that stay stale and unowned. Do
not re-stamp the frontmatter: the live frontmatter gate asserts a review window of at most sixty
days and this file sits at exactly sixty with zero margin, so not touching it is the safe move and
any future re-stamp must keep the window at sixty days or fewer.

**Step 6 — line endings.** This file uses CRLF. Use the file's own terminator so the diff shows the
added lines only, not a whole-file rewrite. Assert the match count on every replacement rather than
assuming a bare string replace matched — a mismatched terminator zero-matches silently.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && f=docs/operations/00-index.md && grep -q "^## Firmware and BIOS Governance$" $f && [ "$(awk "/^## Patch . Update Management$/,/^## App Lifecycle Automation$/" $f | grep -c "^| \[")" = "9" ] && node scripts/validation/check-nav-hub-links.mjs 2>&1 | tail -2'</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline 56f55307..HEAD -- docs/_registry/ scripts/pipeline/ | wc -l` returned exactly `1` before any edit, and `git hash-object scripts/pipeline/filename-map.md` equalled `git rev-parse HEAD:scripts/pipeline/filename-map.md`.
    - `grep -c '^## Firmware and BIOS Governance$' docs/operations/00-index.md` returns exactly `1`.
    - The H2 order in `docs/operations/00-index.md`, read by `grep -n '^## '`, is: Co-Management, Patch & Update Management, App Lifecycle Automation, Compliance Drift Detection + Tenant Migration, Apple Business Governance, Firmware and BIOS Governance, Version History — in that order.
    - The new Firmware section's table has exactly `6` rows beginning with `| [`, and one of them links to `../reference/firmware-oem-matrix.md`.
    - The Patch section's table has exactly `9` rows beginning with `| [`, and the four new ones link to `patch-management/05-linux-update-delivery.md`, `patch-management/06-windows-driver-firmware-updates.md`, `patch-management/07-windows-autopatch.md`, `patch-management/08-windows-app-updates.md` in that order.
    - `grep -icE 'Autopatch rings|System Center|SafetyNet' docs/operations/00-index.md` returns `0`, and `grep -c 'SCCM' docs/operations/00-index.md` returns `0`.
    - `grep -cE '\bTBD\b|\bTODO\b|\bFIXME\b|\bXXX\b|\bPLACEHOLDER\b' docs/operations/00-index.md` returns `0` outside the Version History table; run the count against the file with the Version History section excluded.
    - The Version History table's first data row (the line immediately after the separator row) is the new row, its Date cell is today's date, its Change cell contains both `Firmware and BIOS Governance` and `INT-04` and mentions the four Patch rows, and its Author cell matches the placeholder used by every other row in that table.
    - `grep -c '4-platform comparison hub' docs/operations/00-index.md` returns the same value it returned at `56f55307` — the stale cell is untouched. Prove it with `git diff 56f55307 -- docs/operations/00-index.md` showing no change to that line.
    - `git diff 56f55307 -- docs/operations/00-index.md | grep -c '^-'` returns `1` (the diff header only) — this edit is pure addition with no deleted content lines.
    - `sed -n '1,7p' docs/operations/00-index.md` is byte-identical to the same range at `56f55307` — the frontmatter is not re-stamped.
    - `file docs/operations/00-index.md` still reports CRLF line terminators.
    - `node scripts/validation/check-nav-hub-links.mjs` reports `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`.
    - Nothing is staged and nothing is committed.
  </acceptance_criteria>
  <done>The operations index carries a Firmware and BIOS Governance H2 with six rows placed between Apple Business Governance and Version History, a nine-row Patch table, and a new top Version History row; the stale description and the frontmatter are untouched; the link checker is green; nothing is committed.</done>
</task>

<task type="auto">
  <name>Task 2: The master hub — banner clause, quick-nav line, recipes row, Firmware sub-heading, Linux Patch row, Version History</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md — the whole file. Note especially: it is C17-enrolled with a document identifier in its frontmatter, so all thirteen C17 assertions bind it; the four banner blockquote paragraphs are separate paragraphs separated by blank lines; the Choose Your Platform bullet list; the Device Configuration Recipes table with its Resource/When-to-Use header; the Operations H2 and its five H3 sub-sections ending in Apple Business Governance; the horizontal rule that closes the Operations block before Cross-Platform References; the Version History table
    - scripts/validation/check-phase-137.mjs — the quick-nav bullet assertion: its line-start anchor regex, its requirement that exactly one line match, and its two isolated-line fragment tests
    - scripts/validation/c17-eee-contract.mjs — the blockquote-length assertion and how it strips the marker and joins only consecutive marker runs
    - docs/recipes/05-enterprise-update-plan.md — its H1 verbatim and enough of its Summary to write a truthful When-to-Use cell
    - docs/operations/patch-management/05-linux-update-delivery.md, docs/operations/firmware-bios/00-overview.md, 01-windows-dfci.md, 02-dell-bios-configuration.md — for the new row descriptions
    - .planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md — D-38 through D-50, D-65
  </read_first>
  <action>
Six surgical edits to `docs/index.md`. The file is CRLF; preserve it, and assert every replacement
matched.

**Edit 1 — the banner clause, on the operational-domain paragraph only.** The banner is four
separate blockquote paragraphs. The C17 blockquote assertion strips the marker and joins only
**consecutive** marker runs, so each paragraph measures on its own. Measured at the phase base: the
paragraph that lists provisioning platforms is 182 stripped characters against a 200-character cap
— eighteen characters of headroom, and any realistic clause reds C17 there. The paragraph that
enumerates operational domains is 143 characters with fifty-seven of headroom, and it is also the
semantically correct target.

Append the firmware and BIOS governance domain to the operational-domain paragraph's parenthetical
list, comma-joined, before the closing parenthesis, matching the existing list's lowercase style.
After the edit, re-measure that paragraph's stripped length and confirm it is under 200. Do not
touch the provisioning-platform paragraph. This line choice is a plan-time constant, not an
executor judgement call, because the failure is a hard C17 red on an enrolled file.

**Edit 2 — the recipes quick-nav bullet, one line, still one line.** The bullet begins with a
line-start anchor consisting of the recipes link text and its self-fragment; the frozen Phase 137
validator extracts the single line matching that anchor and requires the match count to be exactly
1, then tests the recipe-03 and recipe-04 fragments on that isolated line. Two changes to that one
line:
  - amend its lead-in prose to read `End-to-end provisioning and governance recipes with embedded
    admin decision points` — Recipe #5 is a governance recipe and the parenthetical is otherwise a
    device-end-state list it does not belong to. No validator anywhere pins this lead-in prose;
  - append `enterprise update plan`, lowercase, as the final item of the parenthetical list,
    matching the existing list's style.
Leave the anchor prefix and both existing recipe fragments intact. **Never split this line.**

**Edit 3 — do not touch the Operations quick-nav bullet.** The direct structural precedent for this
phase — the commit that added an Operations sub-heading exactly as this one does — amended the
banner blockquote, inserted the sub-heading with a three-row table, appended two Cross-Platform
References rows, and deliberately did not touch the Operations quick-nav bullet. That bullet has
been missing one domain since 2026-05-22 by ratified precedent. Half-fixing it here is out of scope;
its stale enumeration is recorded as deferred.

**Edit 4 — the recipes table row.** Append one row to the Device Configuration Recipes table, after
the existing recipe-04 row, in filename order. The link text is the Recipe #5 H1 verbatim, matching
how the four existing rows carry their documents' full H1s. The link target is
`recipes/05-enterprise-update-plan.md`, relative to `docs/`. Write the When-to-Use cell fresh; it
must **not** imply a device end state — Recipe #5 produces a governed update posture across a
fleet, not a provisioned device.

**Edit 5 — the Firmware and BIOS Governance sub-heading.** Insert an H3 whose literal text is
`### Firmware and BIOS Governance`, placed after the Apple Business Governance table and before the
horizontal rule that closes the Operations block — mirroring the operations-index placement from
Task 1 and preserving the one-to-one section-order mirror between the two files. Give it a lead-in
sentence in the same voice as the sibling sub-sections, then a `| Resource | Description |` table
with **exactly three rows**: the domain overview, the DFCI guide, and the Dell guide. Targets are
`operations/firmware-bios/00-overview.md`, `operations/firmware-bios/01-windows-dfci.md`,
`operations/firmware-bios/02-dell-bios-configuration.md`, relative to `docs/`. Three is the right
number because these sub-tables are curated samples rather than exhaustive lists, and the precedent
sub-section shipped exactly three.

**No row for the firmware OEM matrix anywhere on this file.** Four of the eight reference matrix and
comparison files are absent from this hub entirely, including the other OEM matrix — the precedent
for an OEM matrix here is absence, and Task 1's cross-directory operations-index row already gives
it reachability.

**Edit 6 — the Patch sub-table, exactly one new row.** Append one row for
`operations/patch-management/05-linux-update-delivery.md`. Linux is the one platform with a shipped
guide and zero presence anywhere in this hub's Operations block; the sub-table's incompleteness for
macOS and iOS is pre-existing and is not this phase's to close.

**Edit 7 — Version History.** Insert one row at the top of the table, beneath the separator row,
dated at execution, naming the edits and citing `INT-04`, with the Author placeholder this file's
existing rows use. Essentially every edit to this file has received a row; the table is
reverse-chronological by convention rather than by a sort invariant, so top placement is right.

**Do not rename any H2.** The must-remain-stable anchor rule binds this file by name for the
Operations and Cross-Platform References anchors.

**Prose constraint.** The same live corpus-walk patterns from Task 1 bind every new cell, heading,
lead-in and history line on this file. Never write the Autopatch product name immediately followed
by the ring word; write `Configuration Manager` rather than either barred legacy form.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && node scripts/validation/c17-eee-contract.mjs 2>&1 | tail -2 && node scripts/validation/check-nav-hub-links.mjs 2>&1 | tail -2'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations, 0 total violations`.
    - `node scripts/validation/check-nav-hub-links.mjs` reports `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`.
    - `grep -cE '^- \[Device Configuration Recipes\]\(#device-configuration-recipes\)' docs/index.md` returns exactly `1`.
    - That single line contains all four of: `Windows 11 multi-app kiosk`, `Android Dedicated multi-app kiosk`, `governance recipes`, `enterprise update plan`. Test the extracted line in isolation, never the whole file.
    - The Operations quick-nav bullet line is byte-identical to its `56f55307` version — prove with `git diff 56f55307 -- docs/index.md` showing no hunk touching it.
    - The banner blockquote paragraph listing provisioning platforms is byte-identical to its `56f55307` version.
    - The banner blockquote paragraph enumerating operational domains contains a firmware/BIOS governance clause, and its stripped length (marker removed) is strictly less than 200.
    - `grep -c '^### Firmware and BIOS Governance$' docs/index.md` returns exactly `1`, and `grep -n '^### '` shows it immediately after the Apple Business Governance sub-section within the Operations H2.
    - The new Firmware sub-table has exactly `3` rows beginning with `| [`.
    - The Device Configuration Recipes table has exactly `5` rows beginning with `| [`, the fifth linking to `recipes/05-enterprise-update-plan.md`, and its link text equals the Recipe #5 H1 with `# ` stripped, byte for byte.
    - The Patch & Update Management sub-table has exactly `4` rows beginning with `| [`, the fourth linking to `operations/patch-management/05-linux-update-delivery.md`.
    - `grep -c 'firmware-oem-matrix' docs/index.md` returns `0`.
    - `grep -icE 'Autopatch rings|System Center|SafetyNet' docs/index.md` returns `0`, and `grep -c 'SCCM' docs/index.md` returns `0`.
    - The Version History table's first data row is the new row, dated today, citing `INT-04`.
    - `grep -c '^## ' docs/index.md` returns the same value as at `56f55307` — no H2 added, removed or renamed.
    - `git diff 56f55307 -- docs/index.md | grep -c '^-[^-]'` returns `3` or fewer — only the amended banner line, the amended quick-nav line, and at most one Version History separator context line are replaced; everything else is pure addition.
    - `file docs/index.md` still reports CRLF line terminators.
    - Nothing is staged and nothing is committed.
  </acceptance_criteria>
  <done>The master hub carries the banner clause on the correct paragraph, an amended single-line recipes quick-nav bullet, a fifth recipes row, a three-row Firmware and BIOS Governance sub-section mirroring the operations index, a Linux Patch row, and a new top Version History row — with C17 at 236/0/0 and the link checker green, and nothing committed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the two-file working state and the untouched surfaces, without committing</name>
  <files>none written -- assertion-only task over docs/index.md, docs/operations/00-index.md, the eleven authored documents, and scripts/validation/</files>
  <read_first>
    - .planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md — D-01, D-40, D-49, D-60, D-64, D-68, D-69
    - The `<commit_contract>` block at the top of this plan
  </read_first>
  <action>
Prove that this plan changed exactly what it was supposed to and nothing else, and that the state
is correctly staged for Plan 04's single Commit B.

Assert:
1. `git status --porcelain -- docs/` lists exactly two modified paths, `docs/index.md` and
   `docs/operations/00-index.md`, with no staged entries and no untracked files.
2. `git status --porcelain -- scripts/` is empty.
3. Every one of the eleven documents Phases 146-151 authored is byte-unchanged against the phase
   base. Use a per-path `git diff --quiet 56f55307 -- <path>` over all eleven; a bare
   `git diff --stat` reports only the unstaged tree at one instant and would not catch a file
   edited and committed mid-phase.
4. `scripts/validation/check-phase-132.mjs` is byte-unchanged against the phase base. This is the
   frozen recipe-hub validator SC#5 names; it is never edited, and its coverage gap for later
   recipes is closed additively by a successor validator, which Phase 153 authors.
5. `scripts/validation/check-phase-137.mjs` is byte-unchanged against the phase base. Asserting
   this one too is strictly stronger than SC#5 requires; label it as extra rather than as the
   requirement.
6. `scripts/validation/v1.20-audit-allowlist.json` is byte-unchanged against the phase base. That
   file carries line-pinned exemptions plus two empty buckets whose names are the reflex fix for the
   two failure classes this phase can cause — filling either is precisely what INT-06 forbids, and
   the allowlist bump belongs to Phase 153.
7. `scripts/validation/` as a whole is byte-unchanged against the phase base — this phase authors
   zero validators.

Record in the summary that the C17 count is 236 files with 0 violations, **unchanged from before
the phase**, and label it distinctly from the post-phase registry row count, which is also 236. The
collision is live and will confuse a reviewer; name which 236 is which every time either appears.

Make no commit. Leave both files modified for Plan 04.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && git diff --quiet 56f55307 -- scripts/validation/ && echo VALIDATORS_UNCHANGED && n=$(git status --porcelain -- docs/ | wc -l | tr -d " ") && echo "docs_dirty=$n" && [ "$n" = "2" ] && test -z "$(git status --porcelain -- scripts/)" && echo SCRIPTS_CLEAN'</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain -- docs/` produces exactly two lines, both ` M`, for `docs/index.md` and `docs/operations/00-index.md`.
    - `git status --porcelain -- scripts/` produces no output.
    - `git diff --quiet 56f55307 -- <path>` exits 0 for each of the eleven authored documents, run as eleven separate assertions with the path named in each result.
    - `git diff --quiet 56f55307 -- scripts/validation/check-phase-132.mjs` exits 0.
    - `git diff --quiet 56f55307 -- scripts/validation/check-phase-137.mjs` exits 0, recorded as an extra assertion beyond SC#5's subject.
    - `git diff --quiet 56f55307 -- scripts/validation/v1.20-audit-allowlist.json` exits 0.
    - `git diff --quiet 56f55307 -- scripts/validation/` exits 0.
    - `ls scripts/validation/check-phase-152.mjs` fails — this phase authored no validator.
    - The summary states the C17 result as `236 files checked, 0 with violations` and explicitly distinguishes that 236 from the registry's 236 rows.
    - `git log --oneline 56f55307..HEAD -- docs/` returns no lines — this plan committed nothing.
  </acceptance_criteria>
  <done>Exactly two hub files are modified and uncommitted, every authored document and every validator is byte-unchanged against the phase base, no allowlist was touched, and the two 236s are named apart in the record.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New heading literals (2):** `## Firmware and BIOS Governance` in `docs/operations/00-index.md`
and `### Firmware and BIOS Governance` in `docs/index.md`. Both slugify to
`firmware-and-bios-governance`.

**New index rows (15):** six in the operations-index Firmware table, four appended to the
operations-index Patch table, three in the master-hub Firmware sub-table, one appended to the
master-hub Patch sub-table, one appended to the master-hub recipes table.

**New Version History rows (2):** one per hub file, at the top of each table.

**Amended existing lines (2):** the master hub's operational-domain banner blockquote paragraph
(clause appended) and its recipes quick-nav bullet (lead-in amended, one lowercase fragment
appended). Both remain single lines.

**No new files.** No registry row, no filename-map row, no validator, no `doc_id`.

A drift-verification pass must exclude every symbol above from any "did this predate the phase"
check — all of them are created here.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hub file -> reader routing | These two files are the only routing surface a reader has; a wrong or dangling row sends a service-desk operator to the wrong procedure. |
| hub prose -> live corpus-walk audit | The milestone audit walks `docs/` live with proximity patterns; any newly written cell is immediately in scope. |
| C17-enrolled vs unenrolled hub | One of the two files is bound by all thirteen C17 assertions and the other by none; a mitigation that assumes both are enrolled is wrong for one of them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-12 | Tampering | the recipes quick-nav bullet on `docs/index.md` | high | mitigate | The frozen Phase 137 validator requires exactly one line matching the bullet anchor; splitting the line takes that count off 1 and reddens a chain member. The plan makes the line a single-line amendment, and an acceptance criterion asserts the match count is exactly 1 and that the isolated line still carries both prior recipe fragments. |
| T-152-13 | Tampering | the banner blockquote on `docs/index.md` | high | mitigate | The C17 blockquote assertion caps each paragraph at 200 stripped characters; the provisioning-platform paragraph has only eighteen characters of headroom. The plan pins the target paragraph as a plan-time constant, requires a post-edit re-measure under 200, and asserts the wrong paragraph is byte-identical to the phase base. |
| T-152-14 | Spoofing | new hub rows whose link target or anchor does not resolve | medium | mitigate | The operations index is not a registered nav hub, so its rows are policed by the corpus-wide target-existence and anchor-resolution scan rather than by the hub roster; the link checker is a task `<verify>` on both tasks and must report zero of both failure classes. |
| T-152-15 | Repudiation | the live corpus-walk proximity patterns in the milestone audit | medium | mitigate | The plan prescribes the exact prose shape that breaks the reachable adjacency and bars the two legacy product-name forms; acceptance criteria grep both hub files for all four patterns and require zero hits. A red is fixed in the prose, never in an allowlist. |
| T-152-16 | Elevation of Privilege | an accepted-violation baseline introduced to green a gate | high | mitigate | `scripts/validation/v1.20-audit-allowlist.json` byte-equality against the phase base is a Task 3 acceptance criterion, and the whole `scripts/validation/` tree is asserted byte-unchanged. The two empty buckets in that file are named as the reflex fix INT-06 forbids. |
| T-152-17 | Denial of Service | an H2 rename breaking a must-remain-stable anchor | medium | mitigate | No H2 rename is permitted; the H2 count on `docs/index.md` is asserted equal to its phase-base value, and the link checker resolves every inbound fragment. |
| T-152-SC | Tampering | npm/pip/cargo installs | low | accept | This plan performs zero package-manager installs. It edits two markdown files and runs two Node validators that use built-ins only. No package is introduced, so no legitimacy audit applies. |

ASVS level: 1. Blocking threshold: high. All three high-severity entries are mitigated by
computed acceptance criteria that fail the task rather than by review vigilance.
</threat_model>

<verification>
- `node scripts/validation/c17-eee-contract.mjs` -> `236 files checked, 0 with violations, 0 total violations`.
- `node scripts/validation/check-nav-hub-links.mjs` -> `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`.
- Exactly one line matches the recipes quick-nav anchor on `docs/index.md`, and it carries all four required fragments.
- The Operations quick-nav bullet and the provisioning-platform banner paragraph are byte-identical to `56f55307`.
- Row counts: operations-index Firmware 6, operations-index Patch 9, master-hub Firmware 3, master-hub Patch 4, master-hub recipes 5.
- Zero hits for all four live corpus-walk patterns across both hub files.
- `git diff --quiet 56f55307 -- scripts/validation/` exits 0; no `check-phase-152.mjs` exists.
- All eleven authored documents byte-unchanged against `56f55307`.
- `git status --porcelain -- docs/` lists exactly two modified paths; nothing staged, nothing committed.
</verification>

<success_criteria>
Navigation is wired on both index surfaces in the ruled order and nowhere else, the recipes
quick-nav line is still one line, the banner clause landed on the paragraph with headroom, both
gates are green, every frozen surface and every authored document is byte-unchanged, and the work
sits uncommitted ready for Plan 04's single Commit B.
</success_criteria>

<output>
Create `.planning/phases/152-integration-registry-navigation-last-close/152-03-SUMMARY.md` when done.

The summary must carry:
- the cleanliness-gate results with their actual command output;
- the six row counts, each with the command that produced it;
- the post-edit stripped character length of the amended banner paragraph;
- the extracted recipes quick-nav line, in full, with its match count;
- the four corpus-walk pattern greps with their zero results;
- the eleven per-path byte-unchanged assertions and the four validator/allowlist ones;
- the C17 line, explicitly labelled as the file count and distinguished from the registry row count;
- a note that the cross-directory matrix row on the operations index is beyond INT-04's literal
  wording and is the matrix's only operations-side reachability;
- a note that the stale four-platform description cells and the Operations quick-nav bullet were
  deliberately left alone, with the ruling that put them out of scope.
</output>
