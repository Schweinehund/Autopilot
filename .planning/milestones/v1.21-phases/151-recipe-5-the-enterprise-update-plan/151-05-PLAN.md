---
phase: 151-recipe-5-the-enterprise-update-plan
plan: 05
type: execute
wave: 5
depends_on: ["151-04"]
files_modified:
  - docs/recipes/05-enterprise-update-plan.md
  - docs/recipes/01-shared-windows-avd-client.md
  - docs/recipes/02-shared-ipad-full-provisioning.md
  - .planning/milestones/v1.19-DEFERRED-CLEANUP.md
autonomous: true
requirements: [RCP-01, RCP-04, RCP-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The recipe's '## Rollback/Recovery' opens with the TRUE count — four of the nine mechanisms below have no rollback path — and never a vague quantifier (RCP-04, D-41)."
    - "All nine mechanisms appear in '## Rollback/Recovery' as pairwise-distinct bold pseudo-headings in worst-first order, and the nine-versus-six relationship to RCP-04 is stated: RCP-04 enumerates six absences, research lists nine mechanisms, and the three extras — DFCI, the Dell BIOS password and Linux unattended-upgrades — are precisely the ones with documented recovery paths, which is the arithmetic that makes the count of four true (D-42, D-45)."
    - "The two RCP-04 flagship absences are expanded: the default Microsoft 365 Apps channel having no rollback at all, and uninstalling hotpatch requiring the very restart hotpatch exists to avoid. Hotpatch's placement respects that it HAS a documented uninstall path (RCP-04, D-45)."
    - "The Linux entry is written as an open gap, not a sourced absence, and 05-linux-update-delivery.md is NOT cited as support for any rollback claim (D-43, D-44)."
    - "The expedite entry cites RCP-04 directly and states the absence of corpus coverage, because the expedite restart range was dropped and no guide covers it (D-60)."
    - "'## Verification' carries one check per decision point across all five platforms — not a Windows-only section in a five-platform plan — plus an entry for the tenth Step (D-26, D-37)."
    - "'## Configuration-Caused Failures' is authored with its Runbook column filled by intra-recipe anchors reconciled with the hand-authored anchor scheme, because the update domain has no L1 or L2 runbook coverage at all (D-38)."
    - "'## See Also' carries 5 or 6 entries, matching the class-measured range, and its membership is reconciled with the decision link roster (D-66)."
    - "docs/recipes/01-shared-windows-avd-client.md and docs/recipes/02-shared-ipad-full-provisioning.md each carry exactly one '## Rollback/Recovery' H2, positioned strictly between '## Verification' and '## Configuration-Caused Failures', with both files' last_verified and review_by re-stamped (D-47, D-49)."
    - "Every needle check-phase-130.mjs, check-phase-131.mjs and V-135-RECIPE01ZEROEDIT pin survives the retrofit, and recipes 03 and 04 are byte-unchanged at 328 and 301 lines (D-03, D-48)."
    - "The ROLLBACK-RECOVERY-DIVERGENCE-COUNT entry in .planning/milestones/v1.19-DEFERRED-CLEANUP.md has its Status line flipped to closed, with the verbatim-transcription blockquote above it byte-unchanged (D-55)."
    - "Commit B lands exactly four files together: the new recipe, the two retrofitted recipes and the deferred-cleanup file (D-52)."
    - "After Commit B: c17-eee-contract.mjs reports 236 files checked, 0 with violations; check-nav-hub-links.mjs reports 0 hub-presence failures and 0 corpus-link failures; check-phase-144.mjs reports 101 PASS, 0 FAIL, 0 SKIPPED; and v1.20-milestone-audit.mjs exits 0 (D-77, D-78, D-79, D-80, SC#5)."
    - "grep -c '^## Rollback/Recovery' returns exactly 1 in each of recipes 01, 02, 03, 04 and 05 — the class is uniform at 5-of-5 and the retrofit is idempotent (RCP-05, D-47)."
    - statement: "The nine mechanism pseudo-headings inside '## Rollback/Recovery' are pairwise distinct: the sort-unique count of those lines equals their raw count."
      verification: backstop
    - statement: "RECIPE-OUTBOUND-LINK-COVERAGE in .planning/milestones/v1.20-DEFERRED-CLEANUP.md is left carried unchanged, because its trigger is conjunctive and the second conjunct is unmet (D-81)."
      verification: backstop
  artifacts:
    - docs/recipes/05-enterprise-update-plan.md (completed and committed)
    - docs/recipes/01-shared-windows-avd-client.md (retrofitted, re-stamped)
    - docs/recipes/02-shared-ipad-full-provisioning.md (retrofitted, re-stamped)
    - .planning/milestones/v1.19-DEFERRED-CLEANUP.md (ROLLBACK-RECOVERY-DIVERGENCE-COUNT Status flipped)
    - .planning/phases/151-recipe-5-the-enterprise-update-plan/151-05-SUMMARY.md (carrying the Phase 152 AND Phase 153 hand-forwards)
  key_links:
    - "Rollback/Recovery hotpatch entry -> ../operations/patch-management/01-windows-wufb-rings.md#hotpatch"
    - "Rollback/Recovery driver entries -> ../operations/patch-management/06-windows-driver-firmware-updates.md#approval-workflow"
    - "Rollback/Recovery DFCI entry -> ../operations/firmware-bios/01-windows-dfci.md#recovering-locked-device and #retiring-a-device"
    - "Rollback/Recovery Dell BIOS password entry -> ../operations/firmware-bios/02-dell-bios-configuration.md#offboarding"
    - "Configuration-Caused Failures Runbook column -> the recipe's own decision anchors (D-38)"
  prohibitions:
    - statement: "docs/recipes/03-windows-11-multi-app-kiosk.md and docs/recipes/04-android-dedicated-mhs-multi-app.md are not edited — their line-count pins make any edit an immediate red."
      status: flagged-unverified
      verification: manual
    - statement: "docs/operations/patch-management/05-linux-update-delivery.md is not cited as support for any rollback claim."
      status: flagged-unverified
      verification: manual
    - statement: "The source's Not applicable cell is not silently rewritten as a stronger claim; the stronger reading is attributed as this corpus's inference."
      status: flagged-unverified
      verification: manual
    - statement: "No vague quantifier stands in for the true count of four in the Rollback/Recovery opening sentence."
      status: flagged-unverified
      verification: manual
    - statement: "docs/operations/app-lifecycle/00-overview.md is neither re-stamped nor added to See Also."
      status: flagged-unverified
      verification: manual
    - statement: "RECIPE-OUTBOUND-LINK-COVERAGE is not closed and not edited."
      status: flagged-unverified
      verification: manual
    - statement: "No back-link to the new recipe is added to any operations guide or to recipes 01 through 04."
      status: flagged-unverified
      verification: manual
    - statement: "No row for RE-227 is added to docs/_registry/RE-index.md; the filename map is not regenerated; neither canary literal is bumped; docs/index.md and docs/operations/00-index.md are not edited — all of that is Phase 152."
      status: flagged-unverified
      verification: manual
    - statement: "scripts/validation/check-phase-151.mjs is not created — Phase 153 owns it."
      status: flagged-unverified
      verification: manual
    - statement: "The verbatim-transcription blockquote in the v1.19 deferred-cleanup entry is byte-unchanged; only the Status line is edited."
      status: flagged-unverified
      verification: manual
    - statement: "No code fence is added to any recipe."
      status: flagged-unverified
      verification: manual
    - statement: "None of the four C11 ops anti-pattern literals is written."
      status: flagged-unverified
      verification: manual
---

<objective>
Finish the artifact and close the divergence: complete the recipe's four tail sections, retrofit
`## Rollback/Recovery` into recipes 01 and 02 so the class is genuinely uniform at 5-of-5, flip the
tracked divergence entry, and land everything as Commit B with every gate green.

Purpose: `## Rollback/Recovery` is this milestone's flagship honesty artifact and it must be **true**
before it is impressive — four of nine mechanisms have no rollback, and the section says four, not a
vague quantifier. RCP-05 is only resolved when three independently-checkable facts all hold: the
template carries the section (Plan 01), recipes 01 and 02 carry it, and the tracked entry is closed.
Promoting into the template alone would invert an additive 2-of-4 divergence into a 2-of-5 mandated-
section gap that a future auditor reads as two violations, permanently grandfathered by a disclaimer.

Output: the recipe complete and committed, the class uniform at 5-of-5, the divergence closed, and a
SUMMARY carrying the hand-forwards to **both** Phase 152 and Phase 153.

**Decisions this plan implements:** D-02 (the app-lifecycle overview stays out of See Also), D-03
(recipes 01 and 02 are cheap to retrofit), D-19 (the breaks-callout budget holds), D-26 (the tenth
Step gets a Verification entry), D-37 (Verification across all five platforms), D-38 (the
Configuration-Caused Failures Runbook column), D-39 (bold-pseudo-heading prose), D-40 (STD-05 D-04
rule 2 does not bind this section — recorded so a successor does not think a constraint drove D-39),
D-41 (the true count of four), D-42 (all nine mechanisms and the nine-versus-six statement), D-43
(the Linux entry as an open gap), D-44 (the Linux guide is not cited), D-45 (the two flagship
absences, worst-first, hotpatch placed correctly), D-46 (the Not-applicable cell attributed as
inference), D-47 (the retrofit), D-48 (recipes 03 and 04 untouched), D-49 (the insertion point),
D-52 (Commit B), D-55 (the Status-line flip), D-56 (a successor tracking entry for any residue),
D-60 (the expedite entry cites RCP-04 directly), D-66 (See Also sized to the class), D-67 (no
back-links), D-71 (the date pair), D-75 (the hand-forward to both 152 and 153), D-77 (run the link
checker and the apex by hand), D-78 (the apex run separately from the verifier), D-79 (the C17 count
lands at 236), D-80 (the link audit covers every outbound link), D-81
(`RECIPE-OUTBOUND-LINK-COVERAGE` left carried).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-01-PLAN.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-04-SUMMARY.md
@.planning/research/FEATURES.md
</context>

## Phase-wide rules

**Read the `## Phase-wide rules` section of `151-01-PLAN.md` before executing this plan.** It carries
the commit contract, the measured gate baselines, the fourteen verified gate facts, the locked H2
skeleton, the ten-Step property table, the decision-block shapes, the verified anchor roster and the
barred-prose list. Nothing in it is restated here.

<!-- planner-discipline-allow: 05-linux-update-delivery.md -->
<!-- planner-discipline-allow: app-lifecycle -->
<!-- planner-discipline-allow: RE-227 -->
<!-- planner-discipline-allow: most -->
<!-- planner-discipline-allow: many -->
<!-- planner-discipline-allow: generally -->
<!-- planner-discipline-allow: largely -->
<!-- planner-discipline-allow: Autopatch rings -->
<!-- planner-discipline-allow: Not applicable -->
<!-- planner-discipline-allow: Reversibility: -->

The rules that bite hardest in this plan:

- **Commit B is the ONE content commit and it carries exactly four files.** Everything Plans 01
  through 04 authored is still uncommitted; this plan finishes it and lands it. Do not split it into
  three commits (D-52).
- **The intermediate red state ends here.** After Commit B, `check-nav-hub-links.mjs` must report
  `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total` — the checker has no baseline,
  allowlist, ratchet or expected-failure list, and SC#5 requires every outbound link to resolve.
- **Recipes 03 and 04 are untouchable.** `[MEASURED]` `check-phase-135.mjs:49` pins 328 lines and
  `check-phase-136.mjs:44` pins 301, plus an exact 8-element H2 order, a callout count of 6 and a
  failures-table row count of 4. Verify byte-equality with `git status` before committing (D-48).

## Artifacts this phase produces

See the `## Artifacts this phase produces` section of `151-01-PLAN.md`. The symbols this plan adds
from that list are the `## Rollback/Recovery` H2 in each of recipes 01 and 02.

<tasks>

<task type="auto">
  <name>Task 1: Complete the recipe's four tail sections</name>
  <files>docs/recipes/05-enterprise-update-plan.md</files>
  <read_first>
    - `docs/recipes/05-enterprise-update-plan.md` — read the whole file as Plan 04 left it. You need
      every decision anchor id (for the Verification entries and the Runbook column), every Step's
      consequence text (so the Rollback entries do not contradict it), and the single Microsoft 365
      Apps rollback entry Plan 01 already authored, which stays first and is not rewritten.
    - `docs/recipes/03-windows-11-multi-app-kiosk.md` lines 256-320 — the shipped `## Verification`
      checkbox shape, the `## Rollback/Recovery` bold-pseudo-heading prose shape, and the
      `## Configuration-Caused Failures` three-column table whose Runbook column carries intra-recipe
      step anchors. **Read-only — this file is hard-pinned at 328 lines.**
    - `docs/recipes/04-android-dedicated-mhs-multi-app.md` lines 261-301 — the shorter
      `## Rollback/Recovery` variant with its one-line framing sentence, and the four-row
      `## Configuration-Caused Failures` table. **Read-only — hard-pinned at 301 lines.**
    - `.planning/research/FEATURES.md` section 5.3 — the full nine-row mechanism table with its
      Confidence column. Note that the Linux row's Confidence cell is **empty**, uniquely among the
      nine, and its `[PREMISE]` tag prefixes the whole cell.
    - `.planning/REQUIREMENTS.md` RCP-04 — the six absences it enumerates by name, which the
      nine-versus-six statement reconciles against research's nine mechanisms.
    - `docs/operations/patch-management/01-windows-wufb-rings.md` — the `hotpatch` anchored section,
      for hotpatch's absent automatic rollback and its restart-requiring uninstall path. **Read-only.**
    - `docs/operations/firmware-bios/01-windows-dfci.md` — `recovering-locked-device` and
      `retiring-a-device`, the DFCI recovery path. **Read-only.**
    - `docs/operations/firmware-bios/02-dell-bios-configuration.md` — `offboarding`, the Dell BIOS
      password recovery path. **Read-only.**
    - `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md` — D-37 through D-46,
      D-60, D-66, and D-02's See Also exclusion.
  </read_first>
  <action>
Complete the four tail sections of `docs/recipes/05-enterprise-update-plan.md`. **Do not commit** —
Task 3 lands Commit B.

**`## Verification`.** Expand from Plan 01's single checkbox to **one check per decision point plus
one for the tenth Step** — ten checks in total (nine decision points plus Step 10). Each check proves that the **plan is in place**,
names the specific portal location to inspect and the expected state, and links the decision it
verifies by its hand-authored anchor. Coverage must span **all five platforms**: a Windows-only
Verification section in a five-platform plan is the RCP-03 failure mode landing in the section whose
job is to prove the plan exists (D-37). The tenth Step gets a check like every other, because RCP-01
names deadlines as a deliverable and this is where its verification lives (D-26).

**`## Rollback/Recovery`.** This is the section RCP-04 designates as the milestone's flagship honesty
artifact. Author it as prose with **one bold pseudo-heading per mechanism**, matching what recipes 03
and 04 ship in this same section (D-39). Record honestly, in a single sentence somewhere in the
plan's SUMMARY rather than in the document, that this **declines** research item X-9's prioritised
"no-rollback summary table", and that the choice stands on house precedent — STD-05 D-04 rule 2 scopes
*branch bodies*, so a table would in fact have been permitted here (D-40).

Required content, in order:

1. **The opening sentence states the true count: four of the nine mechanisms below have no rollback
   path.** Not a vague quantifier. Shipping one would put a false quantifier in the first line of the
   section RCP-04 designates as the flagship (D-41). The four are: the driver update, the Autopatch
   driver mode switch, Enterprise App Catalog auto-update, and Current Channel.
2. **The nine-versus-six relationship is stated**: RCP-04 enumerates six absences; research lists nine
   mechanisms; the three extras — DFCI, the Dell BIOS password, and Linux `unattended-upgrades` — are
   precisely the ones with documented recovery paths, which is the arithmetic that makes the count of
   four true (D-42).
3. **All nine mechanisms, worst-first** (D-45). The Microsoft 365 Apps channel entry Plan 01 already
   authored stays first and is not rewritten — it is RCP-04's flagship absence and it is already
   correctly ordered. The tie rule: a mechanism holding a documented uninstall path is ordered **after**
   one holding none, which is exactly why hotpatch sits behind Current Channel despite being the second
   flagship.
4. **The hotpatch entry is expanded** as RCP-04's second flagship: automatic rollback is not supported,
   but hotpatches **can** be uninstalled — and the uninstall path requires **a device restart**, the
   very disruption hotpatch exists to avoid. Link
   `../operations/patch-management/01-windows-wufb-rings.md#hotpatch`.
5. **The expedite entry cites RCP-04 directly and states the absence of corpus coverage.** Deleting an
   expedite policy does not uninstall an installed update, and in-progress cancellation is best-effort.
   Because D-60 dropped the expedite restart range as uncorroborated, **there is no guide to link for
   this mechanism** — say so rather than inventing a target (D-60).
6. **The Linux entry is written as an open gap, not a sourced absence.** Its research row's Confidence
   column is **empty**, uniquely among the nine, and its `[PREMISE]` tag prefixes the whole cell — there
   is no sourced half to promote to fact (D-43). And do **not** cite
   `../operations/patch-management/05-linux-update-delivery.md` as support: `[MEASURED]` that file
   contains zero matches for all seven of `rollback`, `roll back`, `recover`, `revert`, `downgrade`,
   `snapshot` and `apt pin`, so citing it would be an unsourced claim wearing a citation (D-44).
7. **The `Not applicable` cell is never silently rewritten.** The source table's Rollback-support cell
   for Current Channel reads `Not applicable`; the recipe's stronger reading is an inference and is
   attributed as one rather than presented as a quotation (D-46). Keep Plan 01's wording consistent.

The nine pseudo-headings must be **pairwise distinct** — a second authoring pass must not duplicate
one.

**`## Configuration-Caused Failures`.** Expand from Plan 01's single row. The Runbook column has no L1
or L2 target: `[MEASURED]` the update domain has **no** runbook coverage at all, so the template's
Runbook column cannot be filled the usual way. Follow recipe 04's precedent and fill it with
**intra-recipe anchors**, reconciled with this recipe's hand-authored anchor scheme — link the
`decision-…` and `deferrals-and-deadlines` anchors, not heading slugs (D-38). Author one row per
misconfiguration that a reader of this plan can realistically make; the shipped class carries 4 rows in
recipe 04 and this artifact is roughly twice the class size, so somewhere in the range of 6 to 10 rows
is in proportion. Every row's Symptom cell names what the admin or the end user actually sees.

**`## See Also`.** Reconcile to **5 or 6 entries** — the class-measured range (`[MEASURED]` 5, 6, 6, 6
across the shipped recipes) — and make its membership consistent with the decision link roster, not a
second, different set (D-66). `docs/operations/app-lifecycle/00-overview.md` is **excluded**: its
`review_by` is `2026-06-27`, 60 days past due, and re-stamping it is not this phase's work (D-02).

Then run every gate in the `<verify>` block. `check-nav-hub-links.mjs` should now be at **0 corpus-link
failures** — the recipe's internal fragments all exist and every outbound target is on the verified
anchor roster. If any failure remains, it is a real defect; fix it here.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&amp;1 | tail -2; node scripts/validation/check-nav-hub-links.mjs 2>&amp;1 | tail -6; node scripts/validation/v1.20-milestone-audit.mjs 2>&amp;1 | tail -2; wc -l docs/recipes/05-enterprise-update-plan.md; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^## Rollback\/Recovery/,/^## Configuration-Caused Failures/p' docs/recipes/05-enterprise-update-plan.md | grep -cE '^\*\*[^*]+:\*\*'`
      returns `9` — nine mechanism pseudo-headings (D-42).
    - Those nine are pairwise distinct:
      `sed -n '/^## Rollback\/Recovery/,/^## Configuration-Caused Failures/p' docs/recipes/05-enterprise-update-plan.md | grep -E '^\*\*[^*]+:\*\*' | sort -u | wc -l`
      returns `9` as well (edge-probe row 10).
    - `sed -n '/^## Rollback\/Recovery/,/^## Configuration-Caused Failures/p' docs/recipes/05-enterprise-update-plan.md | grep -c 'four'`
      returns at least `1`, and the first non-blank prose line after the H2 contains that count. Read
      the line to confirm it is the count sentence, not an incidental use (D-41).
    - `sed -n '/^## Rollback\/Recovery/,/^## Configuration-Caused Failures/p' docs/recipes/05-enterprise-update-plan.md | grep -ciE '\b(most|many|generally|largely)\b'`
      returns `0` — no vague quantifier stands in for the count (D-41).
    - `sed -n '/^## Rollback\/Recovery/,/^## Configuration-Caused Failures/p' docs/recipes/05-enterprise-update-plan.md | grep -c '05-linux-update-delivery.md'`
      returns `0` — the Linux guide is not cited for a rollback claim (D-44).
    - `sed -n '/^## Rollback\/Recovery/,/^## Configuration-Caused Failures/p' docs/recipes/05-enterprise-update-plan.md | grep -c 'RCP-04'`
      returns at least `1` — the expedite entry cites the requirement directly, since no guide covers
      it (D-60).
    - `sed -n '/^## Rollback\/Recovery/,/^## Configuration-Caused Failures/p' docs/recipes/05-enterprise-update-plan.md | grep -ci 'restart'`
      returns at least `1` — hotpatch's restart-requiring uninstall path is stated (RCP-04).
    - `sed -n '/^## Verification/,/^## Rollback\/Recovery/p' docs/recipes/05-enterprise-update-plan.md | grep -c '^- \[ \]'`
      returns `10` — nine decision points plus the tenth Step (D-26, D-37).
    - The Verification section spans five platforms: the same range contains at least one hit for each
      of `macOS`, `iOS`, `Android` and `Linux`, in addition to Windows (D-37).
    - `sed -n '/^## Configuration-Caused Failures/,/^## See Also/p' docs/recipes/05-enterprise-update-plan.md | grep -c '^| '`
      returns a value between `8` and `12` (a header row, a separator row and 6 to 10 data rows), and
      every Runbook cell in that range matches `#decision-` or `#deferrals-and-deadlines` (D-38).
    - `sed -n '/^## See Also/,$p' docs/recipes/05-enterprise-update-plan.md | grep -c '^- \['` returns
      `5` or `6` (D-66), and
      `sed -n '/^## See Also/,$p' docs/recipes/05-enterprise-update-plan.md | grep -c 'app-lifecycle'`
      returns `0` (D-02).
    - `grep -c '^## ' docs/recipes/05-enterprise-update-plan.md` still returns `8`, and
      `grep -c '<a id=' docs/recipes/05-enterprise-update-plan.md` still returns `10`.
    - `grep -c '```' docs/recipes/05-enterprise-update-plan.md` returns `0`.
    - No contiguous top-level blockquote run exceeds 200 characters:
      `node -e "const fs=require('fs');const L=fs.readFileSync('docs/recipes/05-enterprise-update-plan.md','utf8').replace(/\r\n/g,'\n').split('\n');let i=0,max=0;while(i<L.length){if(/^>/.test(L[i])){const b=[];while(i<L.length&&/^>/.test(L[i])){b.push(L[i].replace(/^>\s?/,''));i++;}max=Math.max(max,b.join(' ').length);}else i++;}console.log(max)"`
      prints a number `<= 200`.
    - `node scripts/validation/check-nav-hub-links.mjs` reports `0 hub-presence failure(s), 0
      corpus-link failure(s), 0 total` — the intermediate red state is over (SC#5, D-80).
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0.
    - `git status --porcelain` shows only `docs/recipes/05-enterprise-update-plan.md` untracked — no
      commit yet.
  </acceptance_criteria>
  <done>
    The recipe is complete: ten Verification checks spanning five platforms, nine pairwise-distinct
    worst-first rollback mechanisms opening on the true count of four with the nine-versus-six
    arithmetic stated, both flagship absences expanded, the Linux entry as an open gap with no
    citation dressing, the expedite entry citing RCP-04 directly, a Failures table whose Runbook column
    carries intra-recipe anchors, and a 5-to-6-entry See Also excluding the past-due app-lifecycle
    overview. The link checker is at 0/0 and the file is still uncommitted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Retrofit Rollback/Recovery into recipes 01 and 02, and re-stamp both</name>
  <files>docs/recipes/01-shared-windows-avd-client.md, docs/recipes/02-shared-ipad-full-provisioning.md</files>
  <read_first>
    - `docs/recipes/01-shared-windows-avd-client.md` — read the whole file. You are inserting a section
      into a shipped Approved document, so you need its actual content to write a truthful rollback
      story: the self-deploying profile, the Enrollment Status Page, the dynamic device group, the
      app assignment, and the kiosk-versus-Shared-PC fork at `### Step 5a` / `### Step 5b`. Read lines
      1-14 for the frontmatter date pair, and lines 206-242 for the `## Verification`,
      `## Configuration-Caused Failures` and `## See Also` blocks that bracket the insertion point.
    - `docs/recipes/02-shared-ipad-full-provisioning.md` — read the whole file, same reasons. Read
      lines 1-14 for the date pair and lines 262-289 for the bracketing blocks.
    - `docs/recipes/03-windows-11-multi-app-kiosk.md` lines 275-306 — the donor prose pattern for
      recipe 01's retrofit: one bold pseudo-heading per mechanism, opening with a framing sentence
      that distinguishes removing the configuration from returning the device to its prior state.
      **Read-only.**
    - `docs/recipes/04-android-dedicated-mhs-multi-app.md` lines 273-283 — the shorter donor pattern
      for recipe 02's retrofit, two mechanisms in the same shape. **Read-only.**
    - `scripts/validation/check-phase-130.mjs` — the four needles pinning recipe 01: file non-empty,
      the `### Step 5a` / `### Step 5b` kiosk fork, `## Unsupported and Anti-Feature Callouts`, and an
      unrelated correction in another file. All must survive.
    - `scripts/validation/check-phase-131.mjs` — the four needles pinning recipe 02:
      `## Unsupported and Anti-Feature Callouts`; the guest-sessions decision phrase; the
      `Ask the admin` lead-in; and the `Step 6: Apply the device-group baseline` /
      `Step 7: Apply the per-role user-group overlay` headings. All must survive.
    - `scripts/validation/check-phase-135.mjs` lines 104-115 — `V-135-RECIPE01ZEROEDIT`, which pins the
      exact strings `### Step 5a: Kiosk configuration` and `### Step 5b: Shared PC configuration` in
      recipe 01. Neither may change by a byte.
    - `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md` — D-03, D-47, D-48,
      D-49, D-71.
  </read_first>
  <action>
Insert a `## Rollback/Recovery` H2 into **each** of `docs/recipes/01-shared-windows-avd-client.md` and
`docs/recipes/02-shared-ipad-full-provisioning.md`, positioned **strictly between** that file's
existing `## Verification` block and its existing `## Configuration-Caused Failures` heading. That
slot is machine-enforced by `V-135-ROLLBACKORDER` and `V-136-H2SKELETON` in recipes 03 and 04, so it
is not eyeballed (D-49).

**Insert only. Do not rewrite, reflow, reorder or renumber anything else in either file.** These are
shipped Approved documents pinned by content needles, and D-03's whole point is that they are cheap to
retrofit **because** the pins are needles rather than line counts — that cheapness survives only if
the edit is an insertion.

Before editing either file, confirm its line endings. A silent zero-match string replace is invisible,
and this repository has mixed-terminator files.

**Content.** Each section is genuine, file-specific rollback content in the donor prose shape — one
bold pseudo-heading per mechanism, opening with a framing sentence that distinguishes **removing the
configuration** from **returning the device to its prior state**. Write it from what the recipe
actually configures:

- For recipe 01, the mechanisms are the self-deploying deployment profile, the Enrollment Status Page,
  the dynamic device group membership, the app assignment, and the kiosk-versus-Shared-PC
  configuration. Where a mechanism has no clean rollback — a self-deployed device generally requires a
  reset rather than a policy change — **say so** rather than omitting it. That honesty is the whole
  reason the section is being promoted.
- For recipe 02, the mechanisms are the Shared iPad enrolment itself and the federated sign-in
  configuration, in the shorter recipe-04 shape.

Do not copy recipe 03's or recipe 04's prose. Copy the **shape**.

**Re-stamp both files' `last_verified` and `review_by`** (D-47). Set `last_verified` to the execution
date and `review_by` to **`last_verified` + 60 days**, computed by arithmetic. Rationale to record in
the SUMMARY: D-71's ground is FIX-10's ratified in-milestone cadence, and every file this milestone
re-stamps follows it; the template's +90 rule at line 5 is deliberately left unedited (D-50) and now
diverges from the two retrofitted files, which is a recorded consequence rather than a defect. This
interval choice is a plan-time discretionary reading of D-47 combined with D-71 — record it
explicitly so a reviewer does not read it as drift.

**Do not touch recipes 03 or 04.** `[MEASURED]` their line counts are hard-pinned at 328 and 301, and
`V-136-H2SKELETON` pins an exact 8-element H2 order in recipe 04. Any edit is an immediate red (D-48).

**Do not commit.** Task 3 lands Commit B.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&amp;1 | tail -2; node scripts/validation/check-phase-130.mjs 2>&amp;1 | tail -2; node scripts/validation/check-phase-131.mjs 2>&amp;1 | tail -2; node scripts/validation/check-phase-135.mjs 2>&amp;1 | tail -2; node scripts/validation/check-phase-136.mjs 2>&amp;1 | tail -2; node scripts/validation/check-nav-hub-links.mjs 2>&amp;1 | tail -2; git status --porcelain docs/recipes/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Rollback/Recovery' docs/recipes/01-shared-windows-avd-client.md` returns `1`, and
      the same for `docs/recipes/02-shared-ipad-full-provisioning.md` (RCP-05, and idempotent per
      edge-probe row 1).
    - In each retrofitted file the section sits in the pinned slot:
      `grep -n '^## ' docs/recipes/01-shared-windows-avd-client.md` shows `## Verification` before
      `## Rollback/Recovery` before `## Configuration-Caused Failures`, and the same for recipe 02
      (D-49).
    - `grep -c '^## ' docs/recipes/01-shared-windows-avd-client.md` returns `8` (was 7) and the same
      for recipe 02.
    - `grep -c '### Step 5a: Kiosk configuration' docs/recipes/01-shared-windows-avd-client.md` returns
      `1` and `grep -c '### Step 5b: Shared PC configuration' docs/recipes/01-shared-windows-avd-client.md`
      returns `1` — `V-135-RECIPE01ZEROEDIT`'s two pinned strings are byte-unchanged.
    - `node scripts/validation/check-phase-130.mjs` reports 0 failed;
      `node scripts/validation/check-phase-131.mjs` reports 0 failed;
      `node scripts/validation/check-phase-135.mjs` reports 0 failed;
      `node scripts/validation/check-phase-136.mjs` reports 0 failed.
    - Recipes 03 and 04 are byte-unchanged:
      `git status --porcelain docs/recipes/03-windows-11-multi-app-kiosk.md docs/recipes/04-android-dedicated-mhs-multi-app.md`
      returns empty output, and `wc -l` on them still reports `328` and `301` (D-48).
    - Both date pairs are exactly 60 days apart:
      `node -e "for (const f of ['docs/recipes/01-shared-windows-avd-client.md','docs/recipes/02-shared-ipad-full-provisioning.md']) {const c=require('fs').readFileSync(f,'utf8');const lv=c.match(/^last_verified: (\d{4}-\d{2}-\d{2})/m)[1];const rb=c.match(/^review_by: (\d{4}-\d{2}-\d{2})/m)[1];console.log(f,(new Date(rb)-new Date(lv))/86400000);}"`
      prints `60` for both.
    - Neither retrofitted file gained a code fence:
      `grep -c '```' docs/recipes/01-shared-windows-avd-client.md` and the same for recipe 02 both
      return their pre-edit values — record those values before editing.
    - No contiguous top-level blockquote run in either retrofitted file exceeds 200 characters (the
      same node one-liner as Task 1, run against each file).
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations` —
      recipes 01 and 02 were already enrolled, so the count does not move (D-79).
    - `node scripts/validation/check-nav-hub-links.mjs` reports `0 hub-presence failure(s), 0
      corpus-link failure(s), 0 total`.
    - `git status --porcelain docs/recipes/` lists exactly three paths: the two retrofitted recipes as
      modified and `05-enterprise-update-plan.md` as untracked. No commit has been made.
  </acceptance_criteria>
  <done>
    Recipes 01 and 02 each carry exactly one `## Rollback/Recovery` section in the machine-pinned slot,
    with file-specific truthful content in the donor prose shape and both date pairs re-stamped at 60
    days. Every needle in `check-phase-130`, `check-phase-131`, `check-phase-135` and
    `check-phase-136` still passes, recipes 03 and 04 are byte-unchanged, and the class is uniform at
    5-of-5.
  </done>
  <reversibility rating="costly">
    D-47 edits two shipped Approved documents and re-stamps their verification dates. Reverting means
    un-editing published corpus content and re-opening RCP-05 with the template already carrying the
    promoted section — which would leave the 2-of-5 mandated-section gap D-47 exists to prevent. The
    decision is `[OWNER-RULED 2026-08-26]`; no checkpoint is inserted.
  </reversibility>
</task>

<task type="auto">
  <name>Task 3: Flip the divergence entry, run every gate, and land Commit B</name>
  <files>.planning/milestones/v1.19-DEFERRED-CLEANUP.md, docs/recipes/05-enterprise-update-plan.md, docs/recipes/01-shared-windows-avd-client.md, docs/recipes/02-shared-ipad-full-provisioning.md</files>
  <read_first>
    - `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` lines 200-220 — the
      `ROLLBACK-RECOVERY-DIVERGENCE-COUNT` entry. Note precisely which lines are inside the
      verbatim-transcription blockquote (they begin with `>`) and which is the `**Status:**` line
      below it. **Only the Status line is edited.** The file's own Correction of Record names in-place
      editing as a live option a successor could take.
    - `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` — the `RECIPE-OUTBOUND-LINK-COVERAGE` entry.
      Read its trigger: it is **conjunctive** — a fifth or later recipe lands **and** the coverage gap
      becomes an operational cost — and the entry names a future tooling milestone as its home. The
      second conjunct is unmet, so this entry is **left carried, unedited** (D-81). Read it so you can
      confirm you did not touch it.
    - `.github/workflows/audit-harness-v1.19-integrity.yml` lines 22-33 — the `paths:` trigger list.
      `[MEASURED]` `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` is already one of six path entries
      and the workflow also runs weekly on a cron against live HEAD, so editing this file adds zero
      marginal CI exposure (D-55). **Read-only.**
    - `.planning/ROADMAP.md` Phase 152 and Phase 153 sections — the boundaries the two hand-forwards
      must respect. Phase 152 owns every registry row, the filename-map regeneration and both canary
      bumps; Phase 153 authors the leaf validators for Phases 145 through 152.
    - `.planning/phases/150-per-oem-bios-guides-capability-matrix/150-04-SUMMARY.md` — the shape of a
      Phase-152 hand-forward section, which D-75 follows and extends to a second recipient.
    - `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md` — D-55, D-56, D-75,
      D-77, D-78, D-79, D-80, D-81.
  </read_first>
  <action>
Three moves: flip the entry, run every gate, commit.

**1. Flip the divergence entry.** In `.planning/milestones/v1.19-DEFERRED-CLEANUP.md`, edit **only**
the `**Status:**` line of the `ROLLBACK-RECOVERY-DIVERGENCE-COUNT` entry, changing it from OPEN at
2-of-4 to CLOSED, and naming what closed it: v1.21 Phase 151 promoted `## Rollback/Recovery` into
`docs/_templates/recipe-template.md` and retrofitted recipes 01 and 02, making the class uniform at
**5-of-5**. Name the phase and the decisions (D-47, D-55).

The verbatim-transcription blockquote directly above it — every line beginning with `>` — must be
**byte-unchanged**. The Status line sits outside that transcription, which is exactly why this edit is
permitted (D-55).

Per D-56, open a successor tracking entry for anything the promotion leaves unresolved. With D-47's
retrofit there should be **none** — verify that by confirming all five recipes carry the section — and
record that verification rather than opening an empty entry. If, and only if, something is left
unresolved, write it as a new tracked entry rather than grandfathering it silently.

Leave `RECIPE-OUTBOUND-LINK-COVERAGE` in `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` **untouched
and carried** (D-81).

**2. Run every gate by hand, and run the apex separately from the verifier** (D-77, D-78). The link
checker has no standalone CI execution site, so its coverage arrives only through the apex chain, and
SC#5 requires every outbound link to resolve — no draft answer assigned this run, which is exactly how
a success criterion gets graded against an unrun gate. The apex is run as its own invocation because a
prose or link change can trip a gate a passing verifier does not exercise.

Audit **every** outbound link the recipe makes, not just the decision roster: the See Also entries, the
per-branch links and the Verification links all count (D-80). The link checker proves resolution; you
additionally confirm that each fragment target is on the verified anchor roster in `151-01-PLAN.md`.

**3. Commit B.** Stage and commit **exactly four files together**:

- `docs/recipes/05-enterprise-update-plan.md`
- `docs/recipes/01-shared-windows-avd-client.md`
- `docs/recipes/02-shared-ipad-full-provisioning.md`
- `.planning/milestones/v1.19-DEFERRED-CLEANUP.md`

One commit, not three. Commit message:
`docs(151): ship Recipe #5 the enterprise update plan, retrofit recipes 01-02, close the divergence (RCP-01..RCP-05)`.

Nothing else is staged. In particular: no registry row, no filename-map regeneration, no canary bump,
no index edit, no new validator.

Then re-run the apex one final time against the committed tree and record the verdict.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&amp;1 | tail -2; node scripts/validation/check-nav-hub-links.mjs 2>&amp;1 | tail -2; node scripts/validation/v1.20-milestone-audit.mjs 2>&amp;1 | tail -2; node scripts/validation/check-phase-144.mjs 2>&amp;1 | tail -2; git log -1 --name-only --format=%s; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^## ROLLBACK-RECOVERY-DIVERGENCE-COUNT/,/^---$/p' .planning/milestones/v1.19-DEFERRED-CLEANUP.md | grep -c '5-of-5'`
      returns at least `1`, and the same range's `**Status:**` line no longer says OPEN (D-55).
    - The transcription blockquote is byte-unchanged:
      `git diff HEAD~1 -- .planning/milestones/v1.19-DEFERRED-CLEANUP.md | grep -cE '^[-+]>'` returns
      `0` — no line beginning with `>` was added or removed (D-55).
    - `git diff HEAD~1 --stat -- .planning/milestones/v1.20-DEFERRED-CLEANUP.md` returns empty output —
      `RECIPE-OUTBOUND-LINK-COVERAGE` is left carried (D-81).
    - `grep -c '^## Rollback/Recovery' docs/recipes/0{1,2,3,4,5}*.md` — run per file; each of the five
      recipes returns exactly `1`. The class is uniform at 5-of-5 (RCP-05).
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations, 0
      total violations` — the corpus moved from 235 to 236 exactly as D-79 predicted.
    - `node scripts/validation/check-nav-hub-links.mjs` reports `0 hub-presence failure(s), 0
      corpus-link failure(s), 0 total` — SC#5 (D-80).
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0 — C11 and C13 both clean (D-51,
      D-76).
    - `node scripts/validation/check-phase-144.mjs` reports `101 PASS, 0 FAIL, 0 SKIPPED`, run as its
      own invocation after the commit (D-78).
    - `node scripts/validation/check-phase-129.mjs`, `check-phase-130.mjs`, `check-phase-131.mjs`,
      `check-phase-135.mjs` and `check-phase-136.mjs` each report 0 failed.
    - `git log -1 --name-only --format=%s` lists exactly four files: the new recipe, the two
      retrofitted recipes and the v1.19 deferred-cleanup file (D-52).
    - `git log -2 --format=%s` shows exactly two content commits for this phase — Commit A from Plan
      01 and Commit B from this task. No third content commit exists.
    - `git status --porcelain` returns empty output for `docs/` — nothing is left uncommitted.
    - `git status --porcelain docs/_registry/ scripts/pipeline/ docs/index.md docs/operations/00-index.md`
      returns empty output, and `test ! -f scripts/validation/check-phase-151.mjs` succeeds — every
      Phase 152 and Phase 153 boundary held.
    - `grep -c 'RE-227' docs/_registry/RE-index.md` returns `0` — no registry row was added.
  </acceptance_criteria>
  <done>
    The `ROLLBACK-RECOVERY-DIVERGENCE-COUNT` entry is closed at 5-of-5 with its verbatim transcription
    byte-unchanged; `RECIPE-OUTBOUND-LINK-COVERAGE` is carried unchanged; every gate is green — C17
    236/0, the link checker 0/0/0, the milestone audit exit 0, and the apex 101 PASS / 0 FAIL / 0
    SKIPPED; and Commit B carries exactly four files, making exactly two content commits for the whole
    phase.
  </done>
  <reversibility rating="reversible">
    Flipping a Status line in a tracked deferred-cleanup entry is a one-line documentation change that
    a successor can flip back if the closure turns out to be premature. Commit B itself is a normal
    revertible commit. The genuinely one-way and costly decisions in this phase were taken in Plan 01
    Task 1 and Plan 05 Task 2.
  </reversibility>
</task>

</tasks>

<threat_model>
See the `<threat_model>` block in `151-01-PLAN.md` for the phase's trust boundaries and full STRIDE
register. The threats this plan directly mitigates:

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151-01 | Tampering | `## Rollback/Recovery` overstating a recovery path — the highest-severity failure this document can have | high | mitigate | D-41's true count of four is an acceptance-criterion grep and vague quantifiers are negative-grepped to zero; D-42's nine-versus-six statement makes the arithmetic auditable; D-43 and D-44 force the Linux entry to ship as an open gap with the Linux guide negative-grepped out of the section; D-46 keeps the source's `Not applicable` wording; D-60 makes the expedite entry cite the requirement rather than invent a target. |
| T-151-07 | Spoofing | A dangling or wrong outbound link routing a reader to a document that does not say what the recipe claims | high | mitigate | D-77 assigns the link-checker run to this phase by hand; D-78 runs the apex as a separate invocation because a prose or link change can trip a gate a passing verifier does not exercise; D-80 widens the audit to every outbound link including See Also, per-branch and Verification links; the acceptance criteria require a literal `0 corpus-link failure(s)`. |
| T-151-04 | Tampering | The retrofit silently breaking a shipped Approved recipe | medium | mitigate | Insertion only, never a rewrite; every needle in `check-phase-130`, `check-phase-131`, `check-phase-135` and `check-phase-136` is re-run; `V-135-RECIPE01ZEROEDIT`'s two pinned heading strings are grepped byte-exact; recipes 03 and 04 are proved byte-unchanged by `git status` and by their pinned line counts. |
| T-151-04 | Tampering | Editing the archived v1.19 deferred-cleanup file's verbatim transcription | medium | mitigate | Only the Status line is edited, and the acceptance criteria assert that `git diff` adds and removes zero lines beginning with a blockquote marker. |
| T-151-SC | Tampering | npm, pip or cargo installs | low | accept | No package-manager install task exists in this plan. |
</threat_model>

<verification>
Run after Commit B, each as its own invocation (D-78):

- `node scripts/validation/c17-eee-contract.mjs` → `236 files checked, 0 with violations, 0 total violations`.
- `node scripts/validation/check-nav-hub-links.mjs` → `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`.
- `node scripts/validation/v1.20-milestone-audit.mjs` → exit 0.
- `node scripts/validation/check-phase-144.mjs` → `101 PASS, 0 FAIL, 0 SKIPPED`.
- `node scripts/validation/check-phase-129.mjs` / `130` / `131` / `135` / `136` → 0 failed each.
- `git log -1 --name-only` → exactly four files.
- `git log -2 --format=%s` → exactly two content commits for the phase.
- `git status --porcelain` → empty for `docs/`.
- `wc -l docs/recipes/03-windows-11-multi-app-kiosk.md docs/recipes/04-android-dedicated-mhs-multi-app.md` → `328` and `301`.
</verification>

<success_criteria>
- The recipe ships ring topology, deadlines, driver and firmware approval cadence and app-update
  channels as one prescriptive artifact, C17-green at 236/0 — the corpus's first configuration
  artifact of any kind (RCP-01, SC#1).
- Nine decision points carry reversibility ratings, exactly one **Destructive** and exactly one
  **Effectively irreversible**, with the Autopatch-versus-standalone-rings and hotpatch decisions on
  their corrected positions (RCP-02, SC#2).
- Every decision point carries a platform-applicability marker and six are Windows-only (RCP-03, SC#3).
- `## Rollback/Recovery` reads truthfully as largely a catalogue of absences, stating the count of
  four, including that the default Microsoft 365 Apps channel has no rollback and that uninstalling
  hotpatch requires the very restart hotpatch exists to avoid (RCP-04, SC#4).
- The recipe-template divergence is resolved on the record — promoted into the template, retrofitted
  into recipes 01 and 02, and the tracked entry closed at 5-of-5 — and every outbound link resolves
  (RCP-05, SC#5).
- Exactly two content commits; every Phase 152 and Phase 153 boundary held.
</success_criteria>

<output>
Create `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-05-SUMMARY.md` when done.

Record in it: the final verdict of every gate listed in `<verification>`; the Commit B SHA and its
four-file name list; the final recipe line count against D-33's 600-700 budget; the measured longest
blockquote run in each of the three edited recipes; the execution date and computed `review_by` used
for the two retrofits, with the D-47-plus-D-71 interval rationale stated explicitly; the recorded
decline of research item X-9 with D-40's note that a table would in fact have been permitted; and the
D-56 verification that the promotion left no residue.

**Then write the two hand-forward sections D-75 requires — the hand-forward carries the RULE, not just
a row, and it goes to BOTH phases.**

`## Phase 152 hand-forward`

- The registry row for `docs/recipes/05-enterprise-update-plan.md` at `RE-227`, `Status: Approved`,
  with its **Title copied verbatim from the recipe's H1** — the registry Title column is the sole
  source of the `.docx` output filename, so a paraphrased title silently renames the published file.
  Quote the H1 exactly as shipped.
- The instruction that the filename map is **regenerated by its generator**
  (`scripts/pipeline/build-filename-map.mjs`), never hand-edited.
- The instruction that **both** canary targets are **recomputed from the registry after the rows
  land**, never hard-coded from a document count — `build-filename-map.mjs` and
  `build-publish-bundle.mjs` each carry their own canary and they count different sets.
- The ops-index, `docs/index.md` and quick-nav entries this recipe needs, wired **last**.

`## Phase 153 hand-forward`

The needle-spec for `check-phase-151.mjs`, which Phase 153 authors along with the leaf validators for
Phases 145 through 152. Propose needles that are cheap, stable and non-overlapping with existing
pins — at minimum: the recipe exists and is non-empty; its eight-element H2 skeleton in order; the
ten hand-authored anchor ids; the nine `**Applies to:**` marker lines with `Destructive` and
`Effectively irreversible` each occurring exactly once; the `## Rollback/Recovery` section present in
all five recipes; and the widened class-definition string in `docs/_standards/EEE-SOP-standard.md`, so
a future edit that narrows it back is caught by the apex rather than by a reader. Do **not** propose a
line-count pin — this artifact is expected to grow, and a line-count pin is what makes recipes 03 and
04 untouchable today.
</output>
