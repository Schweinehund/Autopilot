---
phase: 151-recipe-5-the-enterprise-update-plan
plan: 03
type: execute
wave: 3
depends_on: ["151-02"]
files_modified:
  - docs/recipes/05-enterprise-update-plan.md
autonomous: true
requirements: [RCP-01, RCP-02, RCP-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Step 4 (BIOS and firmware surface) exists as a Case 1 block with the anchor decision-bios-firmware-surface, the marker line '**Applies to:** Windows-only · **Reversibility:** Effectively irreversible', and a four-column Option/When to choose/Consequence if wrong/Branch table carrying exactly four option rows (D-09, D-14, D-27)."
    - "'Effectively irreversible' appears exactly once in a Reversibility field across the whole recipe, on Step 4 (D-12, D-13, D-14)."
    - "Step 4 carries four sibling H3 branch bodies '### Step 4a:' through '### Step 4d:', including a body for the 'none' branch, and each reconverges with a one-line routing sentence forward to Step 5 (D-28, D-29, D-30, D-31)."
    - "The 'none' branch body states that DFCI is UNAVAILABLE on Dell, HP and Lenovo rather than declined — those three are not on the DFCI OEM list, which is exactly why the other branches exist (D-30)."
    - "The Dell branch body states the hard blocker that devices must NOT already have a BIOS password configured, and the Prerequisites gate authored in Plan 01 is cross-referenced rather than restated a second time (D-35)."
    - "The DCU-versus-Autopatch driver-policy conflict is named in ONE clause, routed to ../operations/firmware-bios/02-dell-bios-configuration.md, NOT to 06-windows-driver-firmware-updates.md which contains zero Dell Command content, and is presented as an unadjudicated conflict rather than resolved (D-64)."
    - "Step 5 (Microsoft 365 Apps channel) exists as a Case 2 block with the anchor decision-m365-apps-channel, the marker line '**Applies to:** Windows-only · **Reversibility:** Reversible — disruptive', and a three-column table with exactly three option rows (D-09, D-11, D-16, D-17)."
    - "Step 5's rating is assigned at the RECOMMENDED branch (Monthly Enterprise's three-month rollback window), and Current Channel's total absence of rollback support is stated in full inside Step 5's consequence text as well as in ## Rollback/Recovery (D-11, RCP-04)."
    - "Step 5 states the Windows-only ground first-party: Microsoft 365 Apps update channels are device-specific and apply only to installations on devices running Windows (D-17)."
    - "Step 6 (application patch mechanism) exists as a Case 2 block with the anchor decision-app-patch-mechanism, the marker line '**Applies to:** Windows-only · **Reversibility:** Reversible', and a three-column table with exactly three option rows (D-09, D-16, D-17)."
    - "Step 6 states that Enterprise App Catalog auto-update has no rings and no rollback, and that mixing mechanisms on one app causes a version race (RCP-04)."
    - "Each of Steps 4, 5 and 6 carries exactly one What-breaks-if-misconfigured blockquote, and no contiguous top-level blockquote run in the file exceeds 200 characters (D-18, D-19, D-20)."
    - "Every substantive claim added by this plan carries a per-claim Source evidence line covering one page only (D-58)."
    - "Every outbound link added by this plan resolves to an id on the verified anchor roster in 151-01-PLAN.md, using a full relative path with no numeric-prefix shorthand (D-65)."
    - "docs/recipes/05-enterprise-update-plan.md is still UNCOMMITTED when this plan finishes (D-52)."
    - statement: "After this plan the file carries 6 Windows-only marker lines, one per Step 1 through 6, satisfying RCP-03's floor of three with margin."
      verification: backstop
  artifacts:
    - docs/recipes/05-enterprise-update-plan.md (expanded — Steps 4, 5, 6 plus Step 4's four branch bodies)
  key_links:
    - "Step 4 -> ../operations/firmware-bios/00-overview.md#choosing-a-path (the secret-custody routing that decides the branch)"
    - "Step 4a -> ../operations/firmware-bios/01-windows-dfci.md#dfci-prerequisites and #dfci-oem-support"
    - "Step 4b -> ../operations/firmware-bios/02-dell-bios-configuration.md#prerequisites (the no-pre-existing-password blocker) and #authentication"
    - "Step 4c -> ../operations/firmware-bios/03-hp-bios-configuration.md#delivery and ../operations/firmware-bios/04-lenovo-bios-configuration.md#delivery"
    - "Step 4d -> ../operations/firmware-bios/01-windows-dfci.md#dfci-oem-support (DFCI unavailable, not declined) and ../reference/firmware-oem-matrix.md#key-gaps"
    - "Step 5 -> ../operations/patch-management/08-windows-app-updates.md#m365-apps-update-channels and #setting-the-channel-from-intune"
    - "Step 6 -> ../operations/patch-management/08-windows-app-updates.md#enterprise-app-management and #choosing-an-app-update-mechanism"
  prohibitions:
    - statement: "No commit is made by this plan; the recipe stays in the working tree for Plan 05's Commit B."
      status: flagged-unverified
      verification: manual
    - statement: "No file other than docs/recipes/05-enterprise-update-plan.md is modified."
      status: flagged-unverified
      verification: manual
    - statement: "No BIOS token table, no vendor cmdlet syntax and no per-model matrix is authored — that content belongs to the Phase 150 guides."
      status: flagged-unverified
      verification: manual
    - statement: "The DCU-versus-Autopatch conflict is not adjudicated; no first-party adjudication exists."
      status: flagged-unverified
      verification: manual
    - statement: "docs/operations/patch-management/06-windows-driver-firmware-updates.md is not cited as the DCU conflict target."
      status: flagged-unverified
      verification: manual
    - statement: "None of the four C11 ops anti-pattern literals is written."
      status: flagged-unverified
      verification: manual
    - statement: "No visible D-NN label appears in any Step heading."
      status: flagged-unverified
      verification: manual
    - statement: "No code fence is added to the recipe."
      status: flagged-unverified
      verification: manual
    - statement: "No operations guide or reference matrix is edited; linking into one is not editing it."
      status: flagged-unverified
      verification: manual
---

<objective>
Author the three decisions that carry the phase's heaviest branch fan-out and its second RCP-02 named
rating: the BIOS and firmware surface (four branches, **Effectively irreversible**), the Microsoft 365
Apps update channel, and the application patch mechanism.

Purpose: Step 4 is the widest decision in the recipe and the only one whose wrong branch is
unrecoverable at retirement time — it spans five firmware-bios files plus the capability matrix, and
the draft never accounted for its branch bodies at all. Step 5 is where D-11's recommended-branch
rating rule earns its keep: rated at Monthly Enterprise's rollback window it is
`Reversible — disruptive`, while the default channel's total absence of rollback is stated in full
rather than allowed to force a second `Effectively irreversible` and break RCP-02's count.

Output: `docs/recipes/05-enterprise-update-plan.md` expanded with Steps 4, 5 and 6 plus Step 4's four
sibling branch bodies. **Left uncommitted.**

**Decisions this plan implements:** D-09 (no new columns), D-10 (the marker line), D-11
(recommended-branch ratings, which is what keeps Step 5 off a second irreversible rating), D-12 (nine
ratings total), D-13 (the closed enum), D-14 (`Effectively irreversible` is Step 4), D-16 (marker
vocabulary), D-17 (all three Steps are Windows-only, and Step 5's ground is first-party), D-18
(single-line lead-ins), D-19 (one measured breaks-callout per Step), D-23 (anchor-only internal
references), D-27 (Step 4 is Case 1; Steps 5 and 6 are Case 2), D-28 (sibling H3 branches), D-29
(Step 4's four branch bodies), D-30 (the "none" branch gets a body), D-31 (reconvergence sentences),
D-32 (full click-paths), D-35 (the Dell BIOS-password gate), D-52 (no commit), D-57
(restate-and-link), D-58 (evidence lines), D-64 (the DCU conflict routes to the Dell guide and stays
unadjudicated), D-65 (full relative paths), D-76 (C11 defense), D-77 (run the gates by hand).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-01-PLAN.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-02-SUMMARY.md
@.planning/research/FEATURES.md
</context>

## Phase-wide rules

**Read the `## Phase-wide rules` section of `151-01-PLAN.md` before executing this plan.** It carries
the commit contract, the measured gate baselines, the fourteen verified gate facts, the locked H2
skeleton, the ten-Step property table, the decision-block shapes, the verified anchor roster and the
barred-prose list. Nothing in it is restated here.

<!-- planner-discipline-allow: 06-windows-driver-firmware-updates.md -->
<!-- planner-discipline-allow: Consequence if wrong -->
<!-- planner-discipline-allow: declined -->
<!-- planner-discipline-allow: Not applicable -->
<!-- planner-discipline-allow: Autopatch rings -->
<!-- planner-discipline-allow: SCCM -->
<!-- planner-discipline-allow: System Center -->
<!-- planner-discipline-allow: Reversibility: -->

Two rules bite hardest in this plan:

- **No commit (D-52).** This plan appends to an uncommitted file and leaves it uncommitted.
- **The three firmware-bios per-OEM guides carry identical anchor ids** (`delivery`,
  `authentication`, `scope`, `prerequisites`, `offboarding`, `recovery`, `unsupported-callouts`), so
  every fragment link into one of them must carry its full distinct file path.

## Artifacts this phase produces

See the `## Artifacts this phase produces` section of `151-01-PLAN.md`. The symbols this plan adds
from that list are the anchors `decision-bios-firmware-surface`, `decision-m365-apps-channel` and
`decision-app-patch-mechanism`, and the H3 headings `### Step 4:` through `### Step 6:` plus
`### Step 4a:` through `### Step 4d:`.

<tasks>

<task type="auto">
  <name>Task 1: Author Step 4 — the BIOS and firmware surface, with all four branch bodies</name>
  <files>docs/recipes/05-enterprise-update-plan.md</files>
  <read_first>
    - `docs/recipes/05-enterprise-update-plan.md` — read the whole file as Plan 02 left it, in
      particular Steps 1 and 3 (the two Case 1 blocks whose anatomy Step 4 mirrors) and the
      `## Prerequisites` section, which already carries the Dell BIOS-password entitlement gate that
      Step 4b cross-references rather than restates.
    - `docs/operations/firmware-bios/00-overview.md` — read the `who-holds-the-secret`,
      `native-bios-surfaces` and `choosing-a-path` anchored sections in full. `choosing-a-path` is the
      routing this decision restates, and `who-holds-the-secret` is the custody framing each branch
      body must name rather than leave to a link. **Read-only.**
    - `docs/operations/firmware-bios/01-windows-dfci.md` — read `what-dfci-is`, `dfci-prerequisites`,
      `dfci-oem-support` and `bricking-irreversible`. The OEM-support section is what makes the "none"
      branch honest: DFCI is unavailable on Dell, HP and Lenovo, not declined. **Read-only.**
    - `docs/operations/firmware-bios/02-dell-bios-configuration.md` — read `delivery`,
      `authentication` and `prerequisites`. The prerequisites section carries the hard blocker that
      devices must not already have a BIOS password set. **Read-only.**
    - `docs/operations/firmware-bios/03-hp-bios-configuration.md` and
      `docs/operations/firmware-bios/04-lenovo-bios-configuration.md` — read `delivery` and
      `authentication` in each. HP Connect is a vendor connector, not a Win32 agent; Lenovo has no
      native Intune policy surface. **Read-only.**
    - `docs/reference/firmware-oem-matrix.md` — read `summary` and `key-gaps`. The matrix is the
      transposition Step 4's table restates in miniature. **Read-only.**
    - `.planning/research/FEATURES.md` section 5.2 row **D-4** — the four branches verbatim, including
      the Dell no-pre-existing-BIOS-password quote and the `[SOURCED]` `Effectively irreversible`
      rating whose stated consequence is that the wrong branch is unrecoverable at retirement time.
    - `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md` — D-14, D-29, D-30,
      D-35, D-64.
  </read_first>
  <action>
Append `### Step 4:` to `## Steps`, immediately after `### Step 3b:`, at the Case 1 shape locked in
`151-01-PLAN.md`'s Phase-wide rules.

Structure, in order:

- The hand-authored anchor `decision-bios-firmware-surface`, bare and self-closing, with no visible
  D-NN label in the heading.
- A one-sentence `> **Ask the admin:**` blockquote lead-in carrying the prompt only.
- The marker line, on its own non-blockquote bold line, byte-exact:
  `**Applies to:** Windows-only · **Reversibility:** Effectively irreversible`
  This is the **only** occurrence of `Effectively irreversible` in a Reversibility field in the whole
  recipe. SC#2 and RCP-02 are graded by counting it (D-12, D-14).
- A mandatory blank line.
- The Case 1 table `| Option | When to choose | Consequence if wrong | Branch |` with **exactly four**
  option rows — DFCI; the Dell BIOS configuration policy; a vendor connector or Win32 tool (HP
  Connect / HP Sure Admin, Lenovo Think BIOS Config); and none. Each Branch cell links by heading slug
  to its sibling H3.

The Consequence-if-wrong content must state, in the recipe's own prose, that the wrong branch is
unrecoverable at **retirement** time — the secret custody chosen here determines whether a device can
be de-provisioned at all, and losing the management plane means losing the secret (D-57).

Then four sibling H3 branch bodies. Each is a **prose step sequence** — never a table-as-procedure,
never fenced pseudo-config — each carrying its full Intune click-path from the admin center down to
the blade (D-32), each naming that vendor's **secret custody** in one clause, and each closing with a
one-line routing sentence forward to Step 5 (D-31). Branch headings carry a colon and no other
punctuation.

- **`### Step 4a:` DFCI.** Its prerequisites are disqualifying for most fleets: state that DFCI
  requires a supported OEM plus an OEM or CSP Autopilot registration path, and link
  `../operations/firmware-bios/01-windows-dfci.md#dfci-prerequisites`. Name the irreversibility with
  the weight the DFCI guide gives it and link
  `../operations/firmware-bios/01-windows-dfci.md#bricking-irreversible` — do not soften it, and do
  not re-author the bricking treatment here.
- **`### Step 4b:` the Dell BIOS configuration policy.** State the hard blocker in this body: the
  devices must **not** already have a BIOS password configured, because the feature requires that
  Intune hold the password. Cross-reference the `## Prerequisites` entitlement gate Plan 01 already
  authored rather than restating it a second time (D-35). Name the custody: Intune holds the secret.
  Link `../operations/firmware-bios/02-dell-bios-configuration.md#prerequisites` and `#authentication`.

  In **one clause**, name the Dell Command Update versus Autopatch driver-policy conflict as an
  **unadjudicated** conflict — there is no first-party adjudication, so do not resolve it, do not pick
  a side and do not add a recommendation. Route it to
  `../operations/firmware-bios/02-dell-bios-configuration.md`, **not** to
  `../operations/patch-management/06-windows-driver-firmware-updates.md`, which contains zero Dell
  Command content (D-64).
- **`### Step 4c:` the vendor connector or Win32 tool branch.** HP Connect is a **vendor connector**,
  not a Win32 agent, and its secret lives in HP's cloud vault; Lenovo has no native Intune policy
  surface and the secret is yours to hold. There is no native Intune BIOS policy for either vendor.
  Link `../operations/firmware-bios/03-hp-bios-configuration.md#delivery` and
  `../operations/firmware-bios/04-lenovo-bios-configuration.md#delivery`.
- **`### Step 4d:` none.** This branch gets a real body, not a one-liner (D-30). "No BIOS management"
  is the honest default for a Dell, HP or Lenovo fleet, and this is where the
  DFCI-is-unavailable-not-declined fact lands: Dell, HP and Lenovo are **not** on the DFCI OEM list,
  which is exactly why the other branches exist. State what the fleet gives up by choosing it and what
  it keeps. Link `../operations/firmware-bios/01-windows-dfci.md#dfci-oem-support` and
  `../reference/firmware-oem-matrix.md#key-gaps`.

Do **not** author a BIOS token table, vendor cmdlet syntax, or a per-model matrix — that content
belongs to the Phase 150 guides and this recipe links to it (D-57).

Close Step 4 with exactly one `> **What breaks if misconfigured:**` blockquote, **measured** under 200
characters, naming what the admin will see and what the end user will see, with a `See:` line linking
the firmware-bios overview.

Every substantive claim carries a per-claim `**Source:**` evidence line covering one page only (D-58).

**Do not commit.**
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&amp;1 | tail -2; node scripts/validation/v1.20-milestone-audit.mjs 2>&amp;1 | tail -2; node scripts/validation/check-nav-hub-links.mjs 2>&amp;1 | tail -6; grep -c '^### Step ' docs/recipes/05-enterprise-update-plan.md; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<a id="decision-bios-firmware-surface"' docs/recipes/05-enterprise-update-plan.md`
      returns `1`.
    - `grep -c 'Reversibility:\*\* Effectively irreversible$' docs/recipes/05-enterprise-update-plan.md`
      returns exactly `1` — RCP-02's single Effectively-irreversible rating (D-12, D-14).
    - `grep -c 'Reversibility:\*\* Destructive$' docs/recipes/05-enterprise-update-plan.md` still
      returns exactly `1` — Plan 02's rating is not disturbed.
    - `grep -c '^### Step 4[abcd]:' docs/recipes/05-enterprise-update-plan.md` returns `4`.
    - `grep -c '^### Step ' docs/recipes/05-enterprise-update-plan.md` returns `12` — Steps 1, 1a, 1b,
      2, 3, 3a, 3b, 4, 4a, 4b, 4c, 4d.
    - The Step 4 table is the four-column Case 1 shape with exactly four option rows:
      `sed -n '/<a id="decision-bios-firmware-surface"/,/^### Step 4a/p' docs/recipes/05-enterprise-update-plan.md | grep -c '^| '`
      returns `6` (header, separator, four option rows), and the same range contains
      `Consequence if wrong`.
    - `sed -n '/^### Step 4b:/,/^### Step 4c:/p' docs/recipes/05-enterprise-update-plan.md | grep -ci 'BIOS password'`
      returns at least `1` — the hard blocker is in the Dell branch body (D-35).
    - `sed -n '/^### Step 4b:/,/^### Step 4c:/p' docs/recipes/05-enterprise-update-plan.md | grep -c '02-dell-bios-configuration.md'`
      returns at least `1`, and
      `sed -n '/^### Step 4b:/,/^### Step 4c:/p' docs/recipes/05-enterprise-update-plan.md | grep -c '06-windows-driver-firmware-updates.md'`
      returns `0` — the DCU conflict routes to the Dell guide, not the driver guide (D-64).
    - `sed -n '/^### Step 4d:/,/^### Step 5:/p' docs/recipes/05-enterprise-update-plan.md | grep -ci 'dfci'`
      returns at least `1`, and the same range's word count is greater than 60 — the "none" branch has
      a real body, not a one-liner (D-30).
    - `grep -ci 'declined' docs/recipes/05-enterprise-update-plan.md` — if it returns a hit, that hit
      must be inside Step 4d and must be part of the not-declined framing, not an assertion that a
      vendor declined DFCI. Read the hit before accepting.
    - All four Branch-cell link targets resolve: `node scripts/validation/check-nav-hub-links.mjs`
      reports no failure naming `#step-4a-`, `#step-4b-`, `#step-4c-` or `#step-4d-`.
    - `grep -c '> \*\*Ask the admin:\*\*' docs/recipes/05-enterprise-update-plan.md` returns `4` and
      `grep -c '> \*\*What breaks if misconfigured:\*\*' docs/recipes/05-enterprise-update-plan.md`
      returns `4`.
    - No contiguous top-level blockquote run exceeds 200 characters:
      `node -e "const fs=require('fs');const L=fs.readFileSync('docs/recipes/05-enterprise-update-plan.md','utf8').replace(/\r\n/g,'\n').split('\n');let i=0,max=0;while(i<L.length){if(/^>/.test(L[i])){const b=[];while(i<L.length&&/^>/.test(L[i])){b.push(L[i].replace(/^>\s?/,''));i++;}max=Math.max(max,b.join(' ').length);}else i++;}console.log(max)"`
      prints a number `<= 200`.
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0.
    - `git status --porcelain` shows only `docs/recipes/05-enterprise-update-plan.md` untracked.
  </acceptance_criteria>
  <done>
    Step 4 exists as a four-row Case 1 block with the correct anchor, the byte-exact Windows-only /
    Effectively-irreversible marker line landing exactly once, four sibling H3 branch bodies including
    a real "none" body carrying the DFCI-unavailable fact, the Dell BIOS-password blocker, the
    unadjudicated DCU conflict routed to the Dell guide, full click-paths, custody named per branch,
    one measured breaks-callout and per-claim evidence lines — and the file is still uncommitted.
  </done>
  <reversibility rating="costly">
    D-14's assignment of `Effectively irreversible` to this Step is the second half of what SC#2 is
    counted against, and D-30's four-branch fan-out is the widest structure in the recipe. Moving
    either later re-rates the set and re-shapes the Steps spine. Both are settled in `151-CONTEXT.md`;
    no checkpoint is inserted.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Author Step 5 — the Microsoft 365 Apps update channel</name>
  <files>docs/recipes/05-enterprise-update-plan.md</files>
  <read_first>
    - `docs/recipes/05-enterprise-update-plan.md` — read the whole file as Task 1 left it, in
      particular Step 2 (the Case 2 shape Step 5 mirrors) and the `## Rollback/Recovery` section,
      whose Microsoft 365 Apps entry Plan 01 already authored. Step 5's consequence text must be
      consistent with that entry, including its attribution of the stronger reading as inference
      (D-46).
    - `docs/operations/patch-management/08-windows-app-updates.md` — read the
      `m365-apps-update-channels` and `setting-the-channel-from-intune` anchored sections in full.
      These carry the three channels, the rollback-window figures, the Intune-side channel-setting
      surface, and the first-party quotation establishing that channels apply only to installations on
      devices running Windows. **Read-only.**
    - `.planning/research/FEATURES.md` section 5.2 row **D-5** and section 5.3 the **M365 Apps
      channel** row — the three branches, the one-channel-per-device constraint, and the
      Rollback-support figures: Current Channel `Not applicable`, Monthly Enterprise three months,
      Semi-Annual Enterprise two months.
    - `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md` — D-11 (why this Step
      is rated at the recommended branch rather than the worst-case branch, and why that keeps
      RCP-02's count literally true), D-17 (the first-party Windows-only ground), D-46 (do not
      silently rewrite the source's cell).
  </read_first>
  <action>
Append `### Step 5:` to `## Steps`, immediately after `### Step 4d:`, at the Case 2 shape.

Structure, in order:

- The hand-authored anchor `decision-m365-apps-channel`, with no visible D-NN label in the heading.
- A one-sentence `> **Ask the admin:**` blockquote lead-in carrying the prompt only.
- The marker line, byte-exact:
  `**Applies to:** Windows-only · **Reversibility:** Reversible — disruptive`
- A mandatory blank line.
- The Case 2 table `| Option | When to choose | Recorded as |` with **exactly three** option rows —
  Current Channel, Monthly Enterprise Channel, Semi-Annual Enterprise Channel. No fourth column.

Content requirements:

- **The rating is assigned at the branch this plan RECOMMENDS**, which is Monthly Enterprise Channel
  and its three-month rollback window — hence `Reversible — disruptive`. State that explicitly in the
  Step's prose so a reader understands the rating is not a claim about every branch (D-11).
- **Current Channel's absence of rollback support is stated in full inside this Step**, not deferred
  to `## Rollback/Recovery` alone. Current Channel is the **default** for Microsoft 365 Apps for
  enterprise, so the out-of-the-box posture for most fleets is no application rollback. Restate the
  first-party comparison table's Rollback-support row for all three channels, keeping the source's own
  `Not applicable` wording for Current Channel — the stronger reading is this corpus's inference and
  is attributed as one, never presented as a quotation (D-46). This is what lets D-11's rule keep
  RCP-02's count literally true while softening nothing.
- State the one-channel-per-device constraint and the real consequence of getting it wrong: changing
  channel can remove features users depend on.
- State the Windows-only ground first-party rather than asserting it: guide 08 quotes that Microsoft
  365 Apps update channels are device-specific and apply only to installations on devices running
  Windows (D-17).
- Then the full Intune click-path for setting the channel from Intune, from the admin center down to
  the blade (D-32), linking
  `../operations/patch-management/08-windows-app-updates.md#setting-the-channel-from-intune`.

Close Step 5 with exactly one `> **What breaks if misconfigured:**` blockquote, measured under 200
characters, with a `See:` line linking guide 08.

Every substantive claim carries a per-claim `**Source:**` evidence line covering one page only.

Step 5 has **no branch bodies**. Do not add `### Step 5a` or `### Step 5b`.

**Do not commit.**
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&amp;1 | tail -2; node scripts/validation/check-nav-hub-links.mjs 2>&amp;1 | tail -6; grep -c '^### Step ' docs/recipes/05-enterprise-update-plan.md; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<a id="decision-m365-apps-channel"' docs/recipes/05-enterprise-update-plan.md` returns
      `1`.
    - `grep -c '^\*\*Applies to:\*\* Windows-only · \*\*Reversibility:\*\* Reversible — disruptive$' docs/recipes/05-enterprise-update-plan.md`
      returns `2` — Step 1's and Step 5's.
    - `grep -c 'Reversibility:\*\* Effectively irreversible$' docs/recipes/05-enterprise-update-plan.md`
      still returns exactly `1` — the worst-case-branch rule was NOT applied, so no second
      irreversible rating appeared (D-11).
    - `grep -c '^### Step 5[ab]:' docs/recipes/05-enterprise-update-plan.md` returns `0`.
    - The Step 5 table is the three-column Case 2 shape with exactly three option rows:
      `sed -n '/<a id="decision-m365-apps-channel"/,/^### Step 6/p' docs/recipes/05-enterprise-update-plan.md | grep -c '^| '`
      returns `5`, and the same range contains no `Consequence if wrong`.
    - `sed -n '/<a id="decision-m365-apps-channel"/,/^### Step 6/p' docs/recipes/05-enterprise-update-plan.md | grep -c 'Not applicable'`
      returns at least `1` — the source's own cell wording is preserved (D-46).
    - `sed -n '/<a id="decision-m365-apps-channel"/,/^### Step 6/p' docs/recipes/05-enterprise-update-plan.md | grep -ci 'default'`
      returns at least `1` — Current Channel is named as the default.
    - `grep -c 'setting-the-channel-from-intune' docs/recipes/05-enterprise-update-plan.md` returns at
      least `1`.
    - `grep -c '> \*\*Ask the admin:\*\*' docs/recipes/05-enterprise-update-plan.md` returns `5` and
      `grep -c '> \*\*What breaks if misconfigured:\*\*' docs/recipes/05-enterprise-update-plan.md`
      returns `5`.
    - No contiguous top-level blockquote run exceeds 200 characters (same node one-liner as Task 1).
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations`.
    - `git status --porcelain` shows only `docs/recipes/05-enterprise-update-plan.md` untracked.
  </acceptance_criteria>
  <done>
    Step 5 exists as a three-row Case 2 block rated at the recommended branch, with Current Channel's
    default status and its `Not applicable` rollback cell stated in full inside the Step and attributed
    correctly, the one-channel-per-device constraint, the first-party Windows-only ground, a full
    click-path, one measured breaks-callout — and no second Effectively-irreversible rating anywhere.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author Step 6 — the application patch mechanism</name>
  <files>docs/recipes/05-enterprise-update-plan.md</files>
  <read_first>
    - `docs/recipes/05-enterprise-update-plan.md` — read the whole file as Task 2 left it.
    - `docs/operations/patch-management/08-windows-app-updates.md` — read the
      `choosing-an-app-update-mechanism`, `enterprise-app-management`, `microsoft-store-apps` and
      `eam-store-winget-routing` anchored sections in full. Enterprise App Management's reachability
      gates are as hard as its licence, and all eight of its first-party limitations live there
      alongside the Enterprise App Catalog positive, so the guidance does not invert. **Read-only.**
    - `.planning/research/FEATURES.md` section 5.2 row **D-6** — the three branches (Enterprise App
      Catalog auto-update with its add-on licence and Required assignment; Enterprise App Catalog
      guided supersedence; hand-packaged Win32 supersedence) and the consequence: auto-update has no
      rings and no rollback, and mixing mechanisms on one app causes a version race.
    - `.planning/research/FEATURES.md` section 5.3 the **EAC auto-update** row — no rollback and no
      automatic uninstall remediation. Step 6's consequence text references this; Plan 05 authors the
      `## Rollback/Recovery` entry.
    - `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md` — D-17 (Enterprise App
      Management is Windows-only, which is the sixth Windows-only marker) and D-57 (restate-and-link).
  </read_first>
  <action>
Append `### Step 6:` to `## Steps`, immediately after Step 5, at the Case 2 shape.

Structure, in order:

- The hand-authored anchor `decision-app-patch-mechanism`, with no visible D-NN label.
- A one-sentence `> **Ask the admin:**` blockquote lead-in carrying the prompt only.
- The marker line, byte-exact:
  `**Applies to:** Windows-only · **Reversibility:** Reversible`
  This is the sixth and last `Windows-only` marker, satisfying D-17's count of six and RCP-03's floor
  of three with margin.
- A mandatory blank line.
- The Case 2 table `| Option | When to choose | Recorded as |` with **exactly three** option rows —
  Enterprise App Catalog auto-update; Enterprise App Catalog guided supersedence; hand-packaged Win32
  supersedence.

Content requirements:

- Enterprise App Catalog auto-update requires an **add-on licence** and a **Required** assignment, and
  it has **no rings and no rollback** — an update reaches every assigned device without a phased
  cohort, and there is no automatic uninstall remediation.
- **Mixing mechanisms on one app causes a version race**: state this as the consequence of getting the
  decision wrong, in the recipe's own prose.
- Name that Enterprise App Management's reachability gates are as hard as its licence — a tenant that
  cannot reach the required endpoints cannot use it regardless of entitlement — and link
  `../operations/patch-management/08-windows-app-updates.md#enterprise-app-management` for the eight
  limitations rather than re-authoring them (D-57).
- Do not invert the guidance: the Enterprise App Catalog positive is real and belongs in the
  When-to-choose cell, not only the limitations.

Then the full Intune click-path for each mechanism from the admin center down to the blade (D-32),
linking `#choosing-an-app-update-mechanism`.

Close Step 6 with exactly one `> **What breaks if misconfigured:**` blockquote, measured under 200
characters, with a `See:` line linking guide 08.

Every substantive claim carries a per-claim `**Source:**` evidence line covering one page only.

Step 6 has **no branch bodies**.

**Do not commit.** Then run every gate in the `<verify>` block and read the output; record the exact
`check-nav-hub-links` failure list in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&amp;1 | tail -2; node scripts/validation/v1.20-milestone-audit.mjs 2>&amp;1 | tail -2; node scripts/validation/check-nav-hub-links.mjs 2>&amp;1 | tail -6; grep -c '^\*\*Applies to:\*\* Windows-only' docs/recipes/05-enterprise-update-plan.md; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<a id="decision-app-patch-mechanism"' docs/recipes/05-enterprise-update-plan.md`
      returns `1`.
    - `grep -c '^\*\*Applies to:\*\* Windows-only' docs/recipes/05-enterprise-update-plan.md` returns
      `6` — Steps 1 through 6, satisfying D-17 and clearing RCP-03's floor of three (D-17, RCP-03).
    - `grep -c '<a id=' docs/recipes/05-enterprise-update-plan.md` returns `6` — six of the ten
      planned anchors exist; Plan 04 adds the remaining four.
    - `grep -c '^### Step ' docs/recipes/05-enterprise-update-plan.md` returns `14` — Steps 1, 1a, 1b,
      2, 3, 3a, 3b, 4, 4a, 4b, 4c, 4d, 5, 6.
    - `grep -c '^### Step 6[ab]:' docs/recipes/05-enterprise-update-plan.md` returns `0`.
    - The Step 6 table is the three-column Case 2 shape with exactly three option rows:
      `sed -n '/<a id="decision-app-patch-mechanism"/,/^## Verification/p' docs/recipes/05-enterprise-update-plan.md | grep -c '^| '`
      returns `5`.
    - `sed -n '/<a id="decision-app-patch-mechanism"/,/^## Verification/p' docs/recipes/05-enterprise-update-plan.md | grep -ci 'version race'`
      returns at least `1`.
    - `grep -c 'enterprise-app-management' docs/recipes/05-enterprise-update-plan.md` returns at least
      `1`.
    - `grep -c '> \*\*Ask the admin:\*\*' docs/recipes/05-enterprise-update-plan.md` returns `6` and
      `grep -c '> \*\*What breaks if misconfigured:\*\*' docs/recipes/05-enterprise-update-plan.md`
      returns `6`.
    - `grep -c '^## ' docs/recipes/05-enterprise-update-plan.md` returns `8` — the H2 skeleton is
      unchanged.
    - `grep -c '```' docs/recipes/05-enterprise-update-plan.md` returns `0`.
    - `grep -c 'Autopatch rings' docs/recipes/05-enterprise-update-plan.md` returns `0`,
      `grep -c 'SCCM' docs/recipes/05-enterprise-update-plan.md` returns `0`,
      `grep -c 'System Center' docs/recipes/05-enterprise-update-plan.md` returns `0`.
    - `grep -cE '\]\((0[0-9]|[0-9])#' docs/recipes/05-enterprise-update-plan.md` returns `0`.
    - No contiguous top-level blockquote run exceeds 200 characters (same node one-liner as Task 1).
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0.
    - `node scripts/validation/check-nav-hub-links.mjs` reports 0 hub-presence failures, and every
      corpus-link failure names a fragment inside `docs/recipes/05-enterprise-update-plan.md` that
      Plan 04 or Plan 05 still creates.
    - `git status --porcelain` shows only `docs/recipes/05-enterprise-update-plan.md` untracked — no
      commit was made (D-52).
  </acceptance_criteria>
  <done>
    Step 6 exists as a three-row Case 2 block with the correct anchor and marker line, the no-rings /
    no-rollback and version-race consequences in the recipe's own prose, the reachability-gate framing
    without inverting the Enterprise App Catalog positive, full click-paths and one measured
    breaks-callout. Six Windows-only markers, six anchors, fourteen Step headings, C17 236/0, and the
    file is still uncommitted.
  </done>
</task>

</tasks>

<threat_model>
See the `<threat_model>` block in `151-01-PLAN.md` for the phase's trust boundaries and full STRIDE
register. The threats this plan directly mitigates:

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151-02 | Denial of service | Step 4 — recommending a BIOS or firmware branch that cannot be undone at retirement | high | mitigate | Rated `Effectively irreversible` (D-14); the "none" branch gets a real body carrying the DFCI-unavailable-not-declined fact (D-30); the Dell branch leads with the no-pre-existing-BIOS-password hard blocker (D-35); each branch names its secret custody in one clause so a reader evaluates the custody before choosing. |
| T-151-01 | Tampering | Step 5 overstating Microsoft 365 Apps rollback | high | mitigate | The source's `Not applicable` cell is preserved verbatim and the stronger reading is attributed as this corpus's inference (D-46); all three channels' rollback windows are restated; the acceptance criteria grep for the preserved cell wording. |
| T-151-06 | Information disclosure | A vendor branch routing a reader to a credential-custody path without naming who holds the secret | medium | mitigate | Each of the four Step 4 branch bodies names its custody in one clause and links the firmware-bios guide that owns the full statement. |
| T-151-04 | Tampering | Adjudicating the Dell Command Update versus Autopatch driver-policy conflict when no first-party adjudication exists | medium | mitigate | D-64 — one clause, named as unadjudicated, routed to the Dell guide and not to the driver guide, which contains zero Dell Command content. The acceptance criteria negative-grep the wrong target. |
| T-151-SC | Tampering | npm, pip or cargo installs | low | accept | No package-manager install task exists in this plan. |
</threat_model>

<verification>
- `node scripts/validation/c17-eee-contract.mjs` → `236 files checked, 0 with violations`.
- `node scripts/validation/v1.20-milestone-audit.mjs` → exit 0.
- `node scripts/validation/check-nav-hub-links.mjs` → 0 hub-presence failures; every corpus-link
  failure confined to fragments Plans 04 and 05 create.
- `grep -c '^### Step ' docs/recipes/05-enterprise-update-plan.md` → `14`.
- `grep -c '<a id=' docs/recipes/05-enterprise-update-plan.md` → `6`.
- `grep -c '^\*\*Applies to:\*\* Windows-only' docs/recipes/05-enterprise-update-plan.md` → `6`.
- `grep -c 'Reversibility:\*\* Destructive$'` → `1`; `grep -c 'Reversibility:\*\* Effectively irreversible$'` → `1`.
- `git status --porcelain` → the recipe untracked, nothing else modified, no new commit.
</verification>

<success_criteria>
- Steps 4, 5 and 6 exist at the locked block anatomy with correct anchors, byte-exact marker lines and
  the correct STD-05 case shape for each.
- `Effectively irreversible` lands exactly once, on Step 4, and D-11's recommended-branch rule keeps
  Step 5 off a second one while stating Current Channel's absence in full.
- Step 4's four branch bodies all exist and resolve, including a substantive "none" body carrying the
  DFCI-unavailable fact.
- The DCU conflict is one clause, unadjudicated, routed to the Dell guide.
- Six Windows-only markers exist; C17 is 236/0; the milestone audit exits 0; the file is still
  uncommitted.
</success_criteria>

<output>
Create `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-03-SUMMARY.md` when done.

Record in it: the exact `check-nav-hub-links` failure list after each task; the measured longest
blockquote run; the running Step-heading, anchor and marker counts; the running counts of the four
Reversibility enum values; the current file line count against D-33's 600-700 budget; and confirmation
that no commit was made.
</output>
