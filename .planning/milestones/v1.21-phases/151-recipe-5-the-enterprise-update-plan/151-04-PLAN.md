---
phase: 151-recipe-5-the-enterprise-update-plan
plan: 04
type: execute
wave: 4
depends_on: ["151-03"]
files_modified:
  - docs/recipes/05-enterprise-update-plan.md
autonomous: true
requirements: [RCP-01, RCP-03]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Step 7 (non-Windows enforcement primitive) exists as a Case 2 block with the anchor decision-non-windows-enforcement, the marker line '**Applies to:** macOS, iOS and Android · **Reversibility:** Reversible', and a three-column table with exactly three option rows — deferral, DDM enforcement, attestation gate (D-09, D-16)."
    - "Step 7 links all three non-Windows platform guides — 02-macos-update-enforcement.md, 03-ios-update-lifecycle.md and 04-android-patch-delivery.md — which the draft omitted from the roster, the path shapes and the See Also list entirely (D-63)."
    - "Step 7 states that choosing deferral on Apple's forward path picks a primitive Apple is deprecating in favour of declarative device management (D-63)."
    - "Step 8 (Linux posture) exists as a Case 2 block with the anchor decision-linux-posture, the marker line '**Applies to:** Linux-only · **Reversibility:** Reversible', and a three-column table with exactly three option rows — shell-script unattended-upgrades, compliance policy plus Conditional Access gate, explicitly out of the update plan (D-09, D-16)."
    - "Step 8 states that the compliance-plus-Conditional-Access branch is enforceable only by access denial and the shell-script branch is unenforceable, and does not claim enforcement the platform does not have."
    - "Step 9 (exception placement) exists as a Case 2 block with the anchor decision-exception-placement, the marker line '**Applies to:** All platforms · **Reversibility:** Reversible' using the lowercase-p prose marker vocabulary, and a three-column table with exactly two option rows (D-09, D-16)."
    - "Step 10 exists as a Case 3 free-value prompt with the anchor deferrals-and-deadlines, a lead-in and NO table, and carries NO platform marker and NO reversibility rating because RCP-02 and RCP-03 scope those to decision points (D-26, D-34)."
    - "Step 10 delivers RCP-01's named 'deadlines' deliverable and carries an anchor, a Verification entry and outbound links — the draft excluded it from every downstream structure (D-26)."
    - "Every numeric range Step 10 restates either verifies in the corpus and preserves its dash fidelity, or ships tagged '[SOURCED, search-summary]', or is dropped (D-59, D-61)."
    - "The expedite restart range is DROPPED and does not appear anywhere in the recipe (D-60)."
    - "After this plan the recipe carries exactly 10 hand-authored anchors, 9 Applies-to marker lines, 9 Reversibility fields, 10 Ask-the-admin lead-ins, 10 What-breaks-if-misconfigured closers and 18 Step headings (D-12, D-19, D-21, D-26, D-29)."
    - "No contiguous top-level blockquote run in the file exceeds 200 characters (D-18, D-19, D-20)."
    - "docs/recipes/05-enterprise-update-plan.md is still UNCOMMITTED when this plan finishes (D-52)."
    - statement: "Every outbound link added by this plan resolves to an id on the verified anchor roster in 151-01-PLAN.md, using a full relative path with no numeric-prefix shorthand — which matters most here because two different 05-prefixed documents live in this link web (D-65)."
      verification: backstop
  artifacts:
    - docs/recipes/05-enterprise-update-plan.md (expanded — Steps 7, 8, 9 and 10; the Steps spine complete)
  key_links:
    - "Step 7 -> ../operations/patch-management/02-macos-update-enforcement.md#ddm-enforcement (D-63)"
    - "Step 7 -> ../operations/patch-management/03-ios-update-lifecycle.md#ddm-update-keys (D-63)"
    - "Step 7 -> ../operations/patch-management/04-android-patch-delivery.md#play-integrity-attestation (D-63)"
    - "Step 8 -> ../operations/patch-management/05-linux-update-delivery.md#deferral-enforcement-attestation and #compliance-conditional-access"
    - "Step 10 -> ../operations/patch-management/00-overview.md#ring-terminology (the verified deferral ranges)"
    - "Step 10 -> ../operations/patch-management/06-windows-driver-firmware-updates.md#deferral-deadline-behavior (the verified deadline and grace ranges)"
    - "Step 10 -> ../operations/patch-management/02-macos-update-enforcement.md#deadlines-cutover-dates and ../operations/patch-management/04-android-patch-delivery.md#deadlines-cutover-dates"
  prohibitions:
    - statement: "No commit is made by this plan; the recipe stays in the working tree for Plan 05's Commit B."
      status: flagged-unverified
      verification: manual
    - statement: "No file other than docs/recipes/05-enterprise-update-plan.md is modified."
      status: flagged-unverified
      verification: manual
    - statement: "The expedite restart range is not written anywhere in the recipe."
      status: flagged-unverified
      verification: manual
    - statement: "No numeric range is shipped as a bare first-party figure when its only provenance is a search summary."
      status: flagged-unverified
      verification: manual
    - statement: "Step 10 carries no platform marker and no reversibility rating."
      status: flagged-unverified
      verification: manual
    - statement: "The five-minute cap belonging to compliance discovery scripts is not attributed to the Linux platform-script surface."
      status: flagged-unverified
      verification: manual
    - statement: "None of the four C11 ops anti-pattern literals is written."
      status: flagged-unverified
      verification: manual
    - statement: "No visible D-NN label appears in any Step heading."
      status: flagged-unverified
      verification: manual
    - statement: "No code fence is added to the recipe."
      status: flagged-unverified
      verification: manual
    - statement: "No operations guide is edited; linking into one is not editing it."
      status: flagged-unverified
      verification: manual
---

<objective>
Complete the Steps spine: the three non-Windows decisions, and the tenth Step that carries RCP-01's
named "deadlines" deliverable.

Purpose: RCP-03's named failure mode is "a flat plan with implicitly-universal steps", and a
five-platform plan that never routes to three of the five platforms' guides fails SC#1 and SC#5 in
substance — which is exactly what the draft's roster would have shipped (D-63). Step 10 is the other
half of the same problem: it is the only home for deferrals and deadlines, RCP-01 names them as a
deliverable, research supplies no decision point for them, and the draft excluded the Step from every
downstream structure, leaving that deliverable with no anchor, no verification entry and no link.

Output: `docs/recipes/05-enterprise-update-plan.md` with all ten Steps present and the anchor, marker,
lead-in and closer counts at their final phase values. **Left uncommitted.**

**Decisions this plan implements:** D-09 (no new columns), D-10 (the marker line), D-16 (the marker
vocabulary, including the deliberate lowercase-p prose form), D-18 (single-line lead-ins), D-19 (one
measured breaks-callout per Step), D-23 (anchor-only internal references), D-26 (the tenth Step is in
scope for anchors, verification and links but carries no marker and no rating), D-32 (full
click-paths), D-34 (Case 3 free-value prompts are permitted), D-52 (no commit), D-57
(restate-and-link), D-58 (evidence lines), D-59 (relayed ranges tagged or dropped), D-60 (the expedite
range is dropped and its rollback entry has no guide to link), D-61 (dash fidelity), D-63 (the three
non-Windows guides join the roster), D-65 (full relative paths), D-76 (C11 defense), D-77 (run the
gates by hand).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-01-PLAN.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-03-SUMMARY.md
@.planning/research/FEATURES.md
</context>

## Phase-wide rules

**Read the `## Phase-wide rules` section of `151-01-PLAN.md` before executing this plan.** It carries
the commit contract, the measured gate baselines, the fourteen verified gate facts, the locked H2
skeleton, the ten-Step property table, the decision-block shapes, the verified anchor roster and the
barred-prose list. Nothing in it is restated here.

<!-- planner-discipline-allow: expedit -->
<!-- planner-discipline-allow: expedite -->
<!-- planner-discipline-allow: five-minute -->
<!-- planner-discipline-allow: 5 minutes -->
<!-- planner-discipline-allow: Autopatch rings -->
<!-- planner-discipline-allow: SCCM -->
<!-- planner-discipline-allow: System Center -->
<!-- planner-discipline-allow: SafetyNet -->
<!-- planner-discipline-allow: Reversibility: -->

Three rules bite hardest in this plan:

- **No commit (D-52).**
- **Two different `05-`-prefixed documents live in this link web** — this recipe and
  `docs/operations/patch-management/05-linux-update-delivery.md`. Every link Step 8 makes must carry
  the full relative path `../operations/patch-management/05-linux-update-delivery.md`; numeric-prefix
  shorthand is barred (D-65) and would be ambiguous even to a human reader.
- **Gate fact 14 — the corpus writes deferral ranges with an en-dash (U+2013).** `[MEASURED]`
  `00-overview.md:92` carries a `0–30 day` driver deferral and `:120-121` carries `0–30 days` quality
  and `0–365 days` feature. Restating one of those with a hyphen strips the marking that
  distinguishes a paraphrased range from a quoted one (D-61).

## Artifacts this phase produces

See the `## Artifacts this phase produces` section of `151-01-PLAN.md`. The symbols this plan adds
from that list are the anchors `decision-non-windows-enforcement`, `decision-linux-posture`,
`decision-exception-placement` and `deferrals-and-deadlines`, and the H3 headings `### Step 7:`
through `### Step 10:`.

<tasks>

<task type="auto">
  <name>Task 1: Author Steps 7 and 8 — the non-Windows enforcement primitive and the Linux posture</name>
  <files>docs/recipes/05-enterprise-update-plan.md</files>
  <read_first>
    - `docs/recipes/05-enterprise-update-plan.md` — read the whole file as Plan 03 left it, in
      particular Steps 2, 5 and 6 (the three Case 2 blocks whose anatomy Steps 7 and 8 mirror).
    - `docs/operations/patch-management/02-macos-update-enforcement.md` — read the `ddm-enforcement`
      anchored section in full. **Read-only.** This guide and the next two were absent from the
      draft's roster, path shapes and See Also list; D-63 puts them in.
    - `docs/operations/patch-management/03-ios-update-lifecycle.md` — read the `ddm-update-keys`
      anchored section in full. **Read-only.**
    - `docs/operations/patch-management/04-android-patch-delivery.md` — read the
      `play-integrity-attestation` anchored section in full. **Read-only.**
    - `docs/operations/patch-management/00-overview.md` — read the `ring-terminology` anchored section
      and the deferral-versus-enforcement-versus-attestation discussion that follows it. That
      three-way distinction is exactly what Step 7's three options are. It also records that modern
      Apple platforms are deprecating deferral primitives in favour of declarative device management.
      **Read-only.**
    - `docs/operations/patch-management/05-linux-update-delivery.md` — read
      `what-intune-can-and-cannot-do`, `deferral-enforcement-attestation`,
      `bash-platform-script-delivery`, `configuring-unattended-upgrades` and
      `compliance-conditional-access` in full. There is no native Intune Linux update policy;
      compliance plus Conditional Access is attestation enforced by access denial, not update
      enforcement. **Read-only.**
    - `.planning/research/FEATURES.md` section 5.2 rows **D-7** and **D-8** — the three branches each,
      and the two consequences: choosing deferral on Apple's forward path picks a deprecated
      primitive, and the Linux failure mode is claiming enforcement you do not have.
    - `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md` — D-16 (the marker
      vocabulary, including that `macOS, iOS and Android` is one of only four permitted values) and
      D-63.
  </read_first>
  <action>
Append `### Step 7:` and `### Step 8:` to `## Steps`, immediately after Step 6, both at the Case 2
shape locked in `151-01-PLAN.md`'s Phase-wide rules.

**Step 7 — the enforcement primitive per non-Windows platform.**

- The hand-authored anchor `decision-non-windows-enforcement`, with no visible D-NN label.
- A one-sentence `> **Ask the admin:**` blockquote lead-in carrying the prompt only.
- The marker line, byte-exact:
  `**Applies to:** macOS, iOS and Android · **Reversibility:** Reversible`
  `macOS, iOS and Android` is one of exactly four permitted marker values; do not invent a variant
  (D-16).
- A mandatory blank line, then the Case 2 table `| Option | When to choose | Recorded as |` with
  **exactly three** option rows — deferral, declarative device management enforcement, and an
  attestation gate.

Content requirements:

- This is the same three-way distinction the cross-platform overview draws between deferral,
  enforcement and attestation. Restate the distinction in one or two sentences and link
  `../operations/patch-management/00-overview.md#ring-terminology` for the full treatment (D-57).
- State the consequence of getting it wrong: **choosing deferral on Apple's forward path picks a
  primitive Apple is deprecating in favour of declarative device management.** Do not soften it and do
  not present the legacy primitive as a live equal.
- **Link all three platform guides by full relative path** — the macOS guide's `ddm-enforcement`
  anchor, the iOS guide's `ddm-update-keys` anchor, and the Android guide's
  `play-integrity-attestation` anchor. A five-platform plan that never links three of the five
  platforms' guides fails SC#1 and SC#5 in substance, which is what the draft's roster would have
  shipped (D-63).
- Then the Intune click-path for each of the three primitives, from the admin center down to the blade
  (D-32).

**Step 8 — the Linux posture.**

- The hand-authored anchor `decision-linux-posture`, with no visible D-NN label.
- A one-sentence `> **Ask the admin:**` blockquote lead-in.
- The marker line, byte-exact:
  `**Applies to:** Linux-only · **Reversibility:** Reversible`
- A mandatory blank line, then the Case 2 table with **exactly three** option rows — a shell-script
  `unattended-upgrades` posture; a compliance policy plus a Conditional Access gate; and explicitly
  out of the update plan.

Content requirements:

- State plainly that there is **no native Intune Linux update policy**, and that the shell-script
  posture is therefore **unenforceable** from the Intune side.
- State that the compliance-plus-Conditional-Access branch is enforceable only **by access denial** —
  it is attestation, not update enforcement. Getting this wrong means **claiming enforcement you do
  not have**, which is the named consequence.
- Do **not** attribute the five-minute cap that belongs to compliance discovery scripts to the Linux
  platform-script surface; those are different surfaces.
- Link `../operations/patch-management/05-linux-update-delivery.md#deferral-enforcement-attestation`
  and `#compliance-conditional-access` by full relative path — never numeric-prefix shorthand, because
  a second `05-` document exists in this link web (D-65).
- Then the Intune click-path for delivering the platform script and for the compliance-plus-access
  gate (D-32).

Close **each** Step with exactly one `> **What breaks if misconfigured:**` blockquote, **measured**
under 200 characters, naming what the admin will see and what the end user will see, with a `See:`
line linking the guide that covers the failure.

Every substantive claim in both Steps carries a per-claim `**Source:**` evidence line covering one
page only (D-58).

Neither Step has branch bodies — both are Case 2.

**Do not commit.**
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&amp;1 | tail -2; node scripts/validation/v1.20-milestone-audit.mjs 2>&amp;1 | tail -2; node scripts/validation/check-nav-hub-links.mjs 2>&amp;1 | tail -6; grep -c '<a id=' docs/recipes/05-enterprise-update-plan.md; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<a id="decision-non-windows-enforcement"' docs/recipes/05-enterprise-update-plan.md`
      returns `1` and `grep -c '<a id="decision-linux-posture"' docs/recipes/05-enterprise-update-plan.md`
      returns `1`.
    - `grep -c '^\*\*Applies to:\*\* macOS, iOS and Android · \*\*Reversibility:\*\* Reversible$' docs/recipes/05-enterprise-update-plan.md`
      returns `1`.
    - `grep -c '^\*\*Applies to:\*\* Linux-only · \*\*Reversibility:\*\* Reversible$' docs/recipes/05-enterprise-update-plan.md`
      returns `1`.
    - `grep -c '^\*\*Applies to:\*\* Windows-only' docs/recipes/05-enterprise-update-plan.md` still
      returns `6` — Plan 03's count is not disturbed.
    - All three non-Windows guides are linked (D-63):
      `grep -c '02-macos-update-enforcement.md' docs/recipes/05-enterprise-update-plan.md` returns at
      least `1`; the same for `03-ios-update-lifecycle.md` and `04-android-patch-delivery.md`.
    - `grep -c '05-linux-update-delivery.md' docs/recipes/05-enterprise-update-plan.md` returns at
      least `1`, and `grep -cE '\]\(\.\./operations/patch-management/05-linux-update-delivery\.md' docs/recipes/05-enterprise-update-plan.md`
      equals that same count — every Linux link uses the full relative path (D-65).
    - `sed -n '/<a id="decision-non-windows-enforcement"/,/<a id="decision-linux-posture"/p' docs/recipes/05-enterprise-update-plan.md | grep -ciE 'deprecat'`
      returns at least `1` — the Apple forward-path consequence is stated.
    - `sed -n '/<a id="decision-linux-posture"/,/^### Step 9/p' docs/recipes/05-enterprise-update-plan.md | grep -ci 'access denial'`
      returns at least `1`.
    - `grep -c '5 minutes' docs/recipes/05-enterprise-update-plan.md` returns `0` and
      `grep -c 'five-minute' docs/recipes/05-enterprise-update-plan.md` returns `0` — the compliance
      discovery-script cap is not misattributed.
    - Both tables are the three-column Case 2 shape with exactly three option rows:
      `sed -n '/<a id="decision-non-windows-enforcement"/,/<a id="decision-linux-posture"/p' docs/recipes/05-enterprise-update-plan.md | grep -c '^| '`
      returns `5`, and the equivalent range for Step 8 also returns `5`.
    - `grep -c '<a id=' docs/recipes/05-enterprise-update-plan.md` returns `8`.
    - `grep -c '> \*\*Ask the admin:\*\*' docs/recipes/05-enterprise-update-plan.md` returns `8` and
      `grep -c '> \*\*What breaks if misconfigured:\*\*' docs/recipes/05-enterprise-update-plan.md`
      returns `8`.
    - No contiguous top-level blockquote run exceeds 200 characters:
      `node -e "const fs=require('fs');const L=fs.readFileSync('docs/recipes/05-enterprise-update-plan.md','utf8').replace(/\r\n/g,'\n').split('\n');let i=0,max=0;while(i<L.length){if(/^>/.test(L[i])){const b=[];while(i<L.length&&/^>/.test(L[i])){b.push(L[i].replace(/^>\s?/,''));i++;}max=Math.max(max,b.join(' ').length);}else i++;}console.log(max)"`
      prints a number `<= 200`.
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0.
    - `git status --porcelain` shows only `docs/recipes/05-enterprise-update-plan.md` untracked.
  </acceptance_criteria>
  <done>
    Steps 7 and 8 exist as three-row Case 2 blocks with correct anchors and byte-exact marker lines in
    the closed vocabulary; all three non-Windows platform guides are linked by full relative path; the
    Apple deprecation consequence and the Linux access-denial framing are stated; the compliance
    discovery-script cap is not misattributed; eight anchors exist; and the file is still uncommitted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Step 9 and Step 10 — exception placement, then deferrals and deadlines</name>
  <files>docs/recipes/05-enterprise-update-plan.md</files>
  <read_first>
    - `docs/recipes/05-enterprise-update-plan.md` — read the whole file as Task 1 left it.
    - `docs/operations/patch-management/00-overview.md` — read the deferral discussion around the
      `ring-terminology` anchor, in particular the passages carrying the Windows Update client policy
      deferral ranges and the separate driver-policy deferral. **`[MEASURED]` these ranges are written
      with an en-dash (U+2013) at `:92` and `:120-121`; restate them with an en-dash.** **Read-only.**
    - `docs/operations/patch-management/06-windows-driver-firmware-updates.md` — read the
      `deferral-deadline-behavior` anchored section in full: the deferral and deadline asymmetry and
      the grace behavior, which is the Windows driver half of this Step. **Read-only.**
    - `docs/operations/patch-management/02-macos-update-enforcement.md` — the `deadlines-cutover-dates`
      anchored section, and `docs/operations/patch-management/04-android-patch-delivery.md` — the
      `deadlines-cutover-dates` anchored section. Both are named `deadlines-cutover-dates` but live in
      different files, so both links carry their full distinct paths. **Read-only.**
    - `.planning/research/FEATURES.md` section 5.2 row **D-9** (the two exception-placement branches
      and the fragmentation consequence) and the **AF-6** entry (the numeric ranges and their
      search-summary provenance).
    - `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md` — D-26 (the tenth Step
      is in scope for anchors, verification and links but carries no marker and no rating), D-34 (Case
      3 is permitted and this is its textbook shape), D-59 (tag or drop), D-60 (the expedite range is
      dropped, and its rollback entry therefore has no guide to link), D-61 (dash fidelity).
  </read_first>
  <action>
Append `### Step 9:` and `### Step 10:` to `## Steps`, immediately after Step 8. Step 9 is Case 2;
Step 10 is Case 3.

**Step 9 — where exceptions live.**

- The hand-authored anchor `decision-exception-placement`, with no visible D-NN label.
- A one-sentence `> **Ask the admin:**` blockquote lead-in.
- The marker line, byte-exact:
  `**Applies to:** All platforms · **Reversibility:** Reversible`
  Note the **lowercase p**. The prose marker vocabulary deliberately reads `All platforms`, while the
  frontmatter and header block carry the D1 map's `All Platforms`. The two are different surfaces and
  the casing difference is intentional, recorded so a reviewer does not read it as drift (D-16).
- A mandatory blank line, then the Case 2 table with **exactly two** option rows — a compliance-policy
  exclusion, and an update-policy exclusion.
- The consequence of getting it wrong: **update-layer exclusions fragment per platform**, so an
  exception recorded at the update layer has to be re-recorded once per platform and drifts.
- The Intune click-path for each placement (D-32), and one measured
  `> **What breaks if misconfigured:**` closer under 200 characters.

**Step 10 — deferrals and deadlines. This is NOT a decision point.**

- The hand-authored anchor `deferrals-and-deadlines`, with no visible D-NN label. The Step **is** in
  scope for anchors, verification entries and outbound links — RCP-01 names deadlines as a deliverable
  and this Step is its only home, so excluding it from those structures would leave that deliverable
  with no anchor, no verification entry and no link (D-26).
- **No platform marker and no reversibility rating.** RCP-02 and RCP-03 scope those to decision
  points, and this Step is not one. Adding either would break the count of nine that both requirements
  are graded on (D-12, D-26).
- A `> **Ask the admin:**` blockquote lead-in **and no table** — this is a Case 3 free-value prompt,
  and a one-row table would tabulate nothing. A deferral or deadline range the admin must choose a
  value from is the textbook Case 3 shape (D-34).
- Prose covering, per platform where the corpus supports it: the Windows Update client policy deferral
  periods; the driver update policy's own separate deferral, which the ring deferral does not reach;
  the driver policy's deferral-versus-deadline asymmetry and grace behavior; and the Apple and Android
  deadline and cutover-date surfaces.

Range handling is the part most likely to go wrong, so handle it explicitly:

- **Ranges that verify in the corpus are restated with their dash fidelity preserved.** The corpus
  uses an en-dash (U+2013) for paraphrased ranges and a hyphen for the two that sit inside verbatim
  Microsoft quotations. Restating a quoted range **outside** quotation marks strips the marking that
  made it safe — so either keep the quotation marks or restate it in the paraphrased en-dash form
  (D-61).
- **Any range whose only provenance is a search summary of a page that was never fully fetched ships
  tagged `[SOURCED, search-summary]`, or it does not ship at all.** This phase runs no external
  research pass, so nobody re-fetches them, and shipping one untagged is the fabricated-citation shape
  this milestone has already been burned by (D-59).
- **The expedite restart range is DROPPED.** `[MEASURED]` `expedit` appears exactly twice in the whole
  corpus and neither hit is about restart grace, so restating that range would mean **originating** the
  number rather than restating it. Do not write it. The consequence to carry forward: the expedite
  rollback mechanism has no guide to link, so its `## Rollback/Recovery` entry — which Plan 05 authors —
  cites RCP-04 directly and states the absence of corpus coverage (D-60).

Link `../operations/patch-management/00-overview.md#ring-terminology`,
`../operations/patch-management/06-windows-driver-firmware-updates.md#deferral-deadline-behavior`,
`../operations/patch-management/02-macos-update-enforcement.md#deadlines-cutover-dates` and
`../operations/patch-management/04-android-patch-delivery.md#deadlines-cutover-dates`, each by full
relative path. The last two share an anchor id but live in different files, so both need their
distinct paths.

Then the Intune click-path for setting deferrals and deadlines (D-32), and one measured
`> **What breaks if misconfigured:**` closer under 200 characters — Step 10 configures settings, so it
carries a closer like every other Step, bringing the count to ten (D-19).

Every substantive claim in both Steps carries a per-claim `**Source:**` evidence line covering one
page only (D-58).

**Do not commit.** Then run every gate in the `<verify>` block and read the output; record the exact
`check-nav-hub-links` failure list in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&amp;1 | tail -2; node scripts/validation/v1.20-milestone-audit.mjs 2>&amp;1 | tail -2; node scripts/validation/check-nav-hub-links.mjs 2>&amp;1 | tail -8; grep -c '<a id=' docs/recipes/05-enterprise-update-plan.md; wc -l docs/recipes/05-enterprise-update-plan.md; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<a id="decision-exception-placement"' docs/recipes/05-enterprise-update-plan.md`
      returns `1` and `grep -c '<a id="deferrals-and-deadlines"' docs/recipes/05-enterprise-update-plan.md`
      returns `1`.
    - `grep -c '<a id=' docs/recipes/05-enterprise-update-plan.md` returns exactly `10` — nine
      decision anchors plus the tenth Step's (D-21, D-26).
    - `grep -c '^\*\*Applies to:\*\* All platforms · \*\*Reversibility:\*\* Reversible$' docs/recipes/05-enterprise-update-plan.md`
      returns `1` — lowercase `p` in the prose marker (D-16).
    - `grep -c '^\*\*Platform:\*\* All Platforms' docs/recipes/05-enterprise-update-plan.md` returns
      `1` — the header block still carries the D1 uppercase render, and the two surfaces have not
      merged (D-16, edge-probe row 4).
    - `grep -c '^\*\*Applies to:\*\*' docs/recipes/05-enterprise-update-plan.md` returns exactly `9`
      and `grep -c '\*\*Reversibility:\*\*' docs/recipes/05-enterprise-update-plan.md` returns exactly
      `9` — one per decision point, none on Step 10 (D-12, D-26).
    - The four enum values sum to nine and the two counted values land once each:
      `grep -c 'Reversibility:\*\* Reversible$'`, `grep -c 'Reversibility:\*\* Reversible — disruptive$'`,
      `grep -c 'Reversibility:\*\* Destructive$'` and
      `grep -c 'Reversibility:\*\* Effectively irreversible$'` on the recipe return values summing to
      `9`, with the last two each returning exactly `1` (D-12, D-13, D-14, RCP-02).
    - `sed -n '/<a id="deferrals-and-deadlines"/,$p' docs/recipes/05-enterprise-update-plan.md | sed -n '/^## Verification/q;p' | grep -c '^| '`
      returns `0` — Step 10 has no table, as Case 3 requires (D-34).
    - `grep -ci 'expedit' docs/recipes/05-enterprise-update-plan.md` returns `0` — the expedite restart
      range is dropped (D-60).
    - `grep -c '^### Step ' docs/recipes/05-enterprise-update-plan.md` returns `18` — ten Step headings
      plus eight branch bodies (D-29).
    - `grep -c '> \*\*Ask the admin:\*\*' docs/recipes/05-enterprise-update-plan.md` returns `10` and
      `grep -c '> \*\*What breaks if misconfigured:\*\*' docs/recipes/05-enterprise-update-plan.md`
      returns `10` (D-19, D-34).
    - `grep -c '^## ' docs/recipes/05-enterprise-update-plan.md` returns `8` — the H2 skeleton is
      unchanged.
    - Dash fidelity holds: for every numeric range Step 10 restates from
      `00-overview.md`, the character between the two numbers is U+2013 unless the range sits inside
      quotation marks. Verify by reading the source line and the recipe line side by side; record both
      in the SUMMARY (D-61).
    - `grep -c '```' docs/recipes/05-enterprise-update-plan.md` returns `0`.
    - `grep -c 'Autopatch rings' docs/recipes/05-enterprise-update-plan.md` returns `0`,
      `grep -c 'SCCM' docs/recipes/05-enterprise-update-plan.md` returns `0`,
      `grep -c 'System Center' docs/recipes/05-enterprise-update-plan.md` returns `0`,
      `grep -c 'SafetyNet' docs/recipes/05-enterprise-update-plan.md` returns `0`.
    - `grep -cE '\]\((0[0-9]|[0-9])#' docs/recipes/05-enterprise-update-plan.md` returns `0`.
    - No contiguous top-level blockquote run exceeds 200 characters (same node one-liner as Task 1).
    - `wc -l docs/recipes/05-enterprise-update-plan.md` reports a line count; record it in the SUMMARY
      against D-33's 600-700 budget. A count materially below 500 at this point means click-paths or
      branch bodies were skimped and should be reported, not silently accepted.
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0.
    - `node scripts/validation/check-nav-hub-links.mjs` reports 0 hub-presence failures, and every
      corpus-link failure names a fragment inside `docs/recipes/05-enterprise-update-plan.md` that
      Plan 05 still creates — by this point that set should be small and confined to the tail sections.
    - `git status --porcelain` shows only `docs/recipes/05-enterprise-update-plan.md` untracked — no
      commit was made (D-52).
  </acceptance_criteria>
  <done>
    Step 9 exists as a two-row Case 2 block with the lowercase-p All-platforms marker; Step 10 exists
    as a tableless Case 3 free-value prompt carrying an anchor but no marker and no rating, with every
    restated range either dash-faithful, tagged or dropped, and the expedite range absent. The Steps
    spine is complete at ten Steps, eighteen Step headings, ten anchors, nine markers, nine ratings
    summing correctly with one Destructive and one Effectively irreversible, ten lead-ins and ten
    closers. C17 is 236/0 and the file is still uncommitted.
  </done>
</task>

</tasks>

<threat_model>
See the `<threat_model>` block in `151-01-PLAN.md` for the phase's trust boundaries and full STRIDE
register. The threats this plan directly mitigates:

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151-05 | Repudiation | Step 10 originating a numeric range the corpus does not carry | high | mitigate | D-60 drops the expedite restart range outright, `[MEASURED]` as absent from the corpus, and the acceptance criteria negative-grep it to zero; D-59 tags any search-summary-provenance range or drops it; D-61 preserves dash fidelity so a restated range keeps the marking that distinguishes paraphrase from quotation. |
| T-151-04 | Tampering | Step 8 claiming Intune-side Linux update enforcement that does not exist | high | mitigate | The compliance-plus-Conditional-Access branch is stated as enforceable only by access denial; the shell-script branch is stated as unenforceable; the five-minute compliance discovery-script cap is negative-grepped so it cannot be misattributed to the platform-script surface. |
| T-151-07 | Spoofing | A Linux link resolving to the wrong `05-` document | medium | mitigate | D-65's full-relative-path rule, enforced by an acceptance criterion that requires every `05-linux-update-delivery.md` mention to be a full `../operations/patch-management/` path, plus a corpus-wide negative grep for numeric-prefix shorthand. |
| T-151-04 | Tampering | Step 7 presenting a primitive Apple is deprecating as a live equal | medium | mitigate | The deprecation consequence is required in the Step's own prose and is an acceptance-criterion grep, and all three non-Windows guides are linked so a reader reaches the full treatment. |
| T-151-SC | Tampering | npm, pip or cargo installs | low | accept | No package-manager install task exists in this plan. |
</threat_model>

<verification>
- `node scripts/validation/c17-eee-contract.mjs` → `236 files checked, 0 with violations`.
- `node scripts/validation/v1.20-milestone-audit.mjs` → exit 0.
- `node scripts/validation/check-nav-hub-links.mjs` → 0 hub-presence failures; every corpus-link
  failure confined to fragments Plan 05 creates.
- `grep -c '<a id=' docs/recipes/05-enterprise-update-plan.md` → `10`.
- `grep -c '^\*\*Applies to:\*\*' docs/recipes/05-enterprise-update-plan.md` → `9`.
- `grep -c '^### Step ' docs/recipes/05-enterprise-update-plan.md` → `18`.
- `grep -c '> \*\*Ask the admin:\*\*'` → `10`; `grep -c '> \*\*What breaks if misconfigured:\*\*'` → `10`.
- `grep -ci 'expedit' docs/recipes/05-enterprise-update-plan.md` → `0`.
- `git status --porcelain` → the recipe untracked, nothing else modified, no new commit.
</verification>

<success_criteria>
- All ten Steps exist; the Steps spine is complete.
- Steps 7, 8 and 9 carry the three non-Windows marker values from the closed vocabulary, and Step 9's
  prose marker uses the deliberate lowercase-p form while the header block keeps the D1 uppercase
  render.
- All three non-Windows platform guides are in the link roster, by full relative path.
- Step 10 carries an anchor but no marker and no rating, has no table, drops the expedite range, and
  preserves dash fidelity on every range it restates.
- The nine ratings sum to nine with exactly one Destructive and one Effectively irreversible.
- C17 is 236/0, the milestone audit exits 0, and the file is still uncommitted.
</success_criteria>

<output>
Create `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-04-SUMMARY.md` when done.

Record in it: the exact `check-nav-hub-links` failure list after each task; the measured longest
blockquote run; the final anchor, marker, rating, lead-in, closer and Step-heading counts; the source
line and the recipe line for every range Step 10 restates, so dash fidelity is auditable; the file's
line count against D-33's 600-700 budget; and confirmation that no commit was made.
</output>
