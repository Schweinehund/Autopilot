---
phase: 55
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/app-lifecycle/03-ios-vpp-licensing.md
autonomous: true
requirements:
  - APP-06
requirements_addressed:
  - APP-06

must_haves:
  truths:
    - "docs/operations/app-lifecycle/03-ios-vpp-licensing.md exists with `platform: iOS` (D-19) + `audience: admin` (CD-10 default) + `last_verified: 2026-04-28` + `review_by: 2026-06-27` frontmatter (V-55-07)"
    - "File body opens with verbatim `> **Platform applicability:**` blockquote at TOP within first 50 lines of body routing back to 00-overview + 3 sibling per-platform files (D-13 + V-55-26)"
    - "File contains a 2-column VPP attribute comparison table per D-07 3A winner with column headers `VPP Device-Licensed` AND `VPP User-Licensed` (V-55-18)"
    - "Table contains reclamation rows with literals `retire/wipe` (or `retire and wipe`) AND `device license` (or `device-license`) within reclamation context (V-55-19; APP-06 reclamation conjunct A)"
    - "Table contains reclamation rows with literals `remove app` (or `remove-app`) AND `user license` (or `user-license`) within reclamation context (V-55-19; APP-06 reclamation conjunct B)"
    - "File contains ZERO Mermaid code blocks (` ```mermaid ` patterns) per D-09 + CDI-Phase55-06 + V-55-20 NEGATIVE regression-guard (Phase 54 iOS sibling pattern parity)"
    - "File contains literal cross-link to v1.3 iOS app deployment guide via relative path `../../admin-setup-ios/05-app-deployment.md` per D-07 anti-duplication mitigation + V-55-21"
    - "File body covers 6+ rows in the 2-column comparison table per D-07: license source / Apple ID requirement / supervised silent install / reclamation on retire-wipe / reclamation on remove-app / BYOD applicability (CD-07 plan-author discretion on row labels)"
    - "File encodes the Microsoft Learn 3-step reclamation workflow (remove assignment → change to Uninstall → Revoke license) either as sub-section under reclamation rows OR narrative beneath table — per RESEARCH §7 + 55-VALIDATION.md Manual-Only Verifications row 4"
    - "File explicitly distinguishes BYOD/Corporate × Supervised/Unsupervised dimensions per D-08 — value-cell qualifiers (e.g., 'Yes (supervised) / Partial (unsupervised)') rather than separate sub-matrix; cross-link to v1.3 silent-install scenarios at admin-setup-ios/05-app-deployment.md:36-43"
    - "File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens (V-55-30)"
    - "File does NOT contain bare `> **Platform:**` token (V-55-27)"
  artifacts:
    - path: "docs/operations/app-lifecycle/03-ios-vpp-licensing.md"
      provides: "iOS VPP device-vs-user licensing comparison with reclamation rows (net-new vs v1.3) per D-07 3A 2-column attribute table; cross-link to v1.3 admin-setup-ios for full 4-column app-type matrix (APP-06)"
      contains:
        - "platform: iOS"
        - "audience: admin"
        - "last_verified: 2026-04-28"
        - "review_by: 2026-06-27"
        - "> **Platform applicability:**"
        - "VPP Device-Licensed"
        - "VPP User-Licensed"
        - "retire/wipe"
        - "device license"
        - "remove app"
        - "user license"
        - "../../admin-setup-ios/05-app-deployment.md"
      forbidden:
        - "```mermaid"
  key_links:
    - from: "docs/operations/app-lifecycle/03-ios-vpp-licensing.md"
      to: "docs/admin-setup-ios/05-app-deployment.md"
      via: "Cross-link to v1.3 iOS app deployment for full 4-column app-type comparison + silent-install scenarios (D-07 + D-08 anti-duplication mitigation)"
      pattern: "\\.\\./\\.\\./admin-setup-ios/05-app-deployment\\.md"
    - from: "docs/operations/app-lifecycle/03-ios-vpp-licensing.md"
      to: "docs/operations/app-lifecycle/00-overview.md"
      via: "Platform applicability blockquote routing back to overview hub"
      pattern: "00-overview\\.md"
---

<objective>
Author `docs/operations/app-lifecycle/03-ios-vpp-licensing.md` — the iOS-specific VPP licensing guide covering APP-06 (device-licensing: no Apple ID required, silent install on supervised; user-licensing: requires Apple ID, user must accept; reclamation behavior for retire/wipe and remove-app scenarios). This file is the **3A winner per CONTEXT D-07**: 2-column VPP-only attribute comparison table (`VPP Device-Licensed | VPP User-Licensed`) with reclamation rows added. Reclamation behavior is verified net-new in Phase 55 (no `reclaim/reclamation` token appears anywhere in v1.3 admin-setup-ios per file-system verification — see CONTEXT D-07).

Per CONTEXT D-09 + CDI-Phase55-06: **Mermaid is FORBIDDEN** in this file (Phase 54 iOS sibling pattern parity — `docs/operations/patch-management/03-ios-update-lifecycle.md` has zero Mermaid; entire `docs/operations/patch-management/` tree has zero Mermaid; v1.3 admin-setup-ios has zero Mermaid). V-55-20 NEGATIVE regression-guard enforces.

Per CONTEXT D-07 anti-duplication mitigation: cross-link to v1.3 `docs/admin-setup-ios/05-app-deployment.md` for the broader 4-column app-type comparison covering VPP Device-Licensed + VPP User-Licensed + LOB + Store Apps × 12 attributes; Phase 55 file is **deepening** the VPP slice (reclamation behavior + decision-support framing) NOT duplicating.

Per CONTEXT D-08: BYOD/Corporate × Supervised/Unsupervised dimensions reused from v1.3 cross-reference (silent-install boundary table at `05-app-deployment.md:36-43`); Phase 55 file embeds these as value-cell qualifiers (e.g., "Yes (supervised) / Partial (unsupervised)") rather than authoring a separate sub-matrix. User Enrollment as third state (per `09-mam-app-protection.md` + `08-user-enrollment.md`) covered in BYOD-applicability row prose with cross-link.

Purpose: Implements CONTEXT D-07 (3A 2-column VPP attribute table with reclamation rows) + D-08 (BYOD/supervision dimensions reused from v1.3 cross-reference) + D-09 (Mermaid forbidden) + D-13 (cross-platform inline blockquote) + D-19 (`platform: iOS` frontmatter) + D-20 (pinned anchor strings) + CDI-Phase55-06 (Mermaid pattern parity). Validator V-55-18/19/20/21/26/27/30/31 enforce structural correctness at commit time.

This file maps to APP-06 per REQ traceability line 174. Wave 1 — parallel with 55-01 / 55-02 / 55-03 / 55-05 (no inter-file authoring dependency; cross-links resolve at validator runtime when full atomic commit lands per D-21).

**Plan-author resolution per RESEARCH §7 + 55-VALIDATION.md Manual-Only Verifications row 4:** The REQ APP-06 wording "retire/wipe returns device license; remove app returns user license" is OVERSIMPLIFIED — the actual Microsoft Learn workflow is a 3-step manual process (remove assignment → change to Uninstall → Revoke license action). Plan-author SHOULD encode the full 3-step workflow either as (a) a sub-section under the reclamation rows, or (b) narrative beneath the comparison table. Validator V-55-19 accepts either via literal-token coverage of the 4 reclamation literals; CD-07 preserves plan-author row-label discretion.

Output: 1 new file at `docs/operations/app-lifecycle/03-ios-vpp-licensing.md`.

DO NOT commit yet — atomic commit owned by 55-07 author per CONTEXT D-21 + CDI-Phase55-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-app-lifecycle-automation/55-CONTEXT.md
@.planning/phases/55-app-lifecycle-automation/55-RESEARCH.md
@.planning/phases/55-app-lifecycle-automation/55-VALIDATION.md
@docs/operations/patch-management/03-ios-update-lifecycle.md
@docs/admin-setup-ios/05-app-deployment.md

<interfaces>
**File this plan creates:** `docs/operations/app-lifecycle/03-ios-vpp-licensing.md`

**Frontmatter contract (D-19 — single-string `platform: iOS`):**
```yaml
---
last_verified: 2026-04-28
review_by: 2026-06-27
applies_to: all
audience: admin
platform: iOS
---
```

**Cross-link contract (D-07 anti-duplication mitigation):**

| Target | Relative path from this file | Purpose |
|--------|------------------------------|---------|
| v1.3 iOS app deployment | `../../admin-setup-ios/05-app-deployment.md` | Full 4-column app-type comparison covering LOB and Store Apps alongside VPP Device-Licensed and VPP User-Licensed; silent-install boundary table at lines 36-43; device-vs-user prose at lines 24-28 |
| 00-overview hub | `00-overview.md` | Cross-platform overview (back-link from Platform applicability blockquote) |

**Validator V-55-NN assertions enforced against this file:**

| V-NN | Assertion |
|------|-----------|
| V-55-04 | File exists |
| V-55-07 | Frontmatter: `platform: iOS`, `audience:` non-empty, 60-day cycle |
| V-55-18 | 2-column VPP comparison table with `VPP Device-Licensed` AND `VPP User-Licensed` column headers |
| V-55-19 | Reclamation literals: `retire/wipe` AND `device license` AND `remove app` AND `user license` |
| V-55-20 | NEGATIVE: ZERO Mermaid code blocks |
| V-55-21 | Literal cross-link `../../admin-setup-ios/05-app-deployment.md` |
| V-55-26 | `> **Platform applicability:**` blockquote within first 50 lines of body |
| V-55-27 | NEGATIVE: NO bare `> **Platform:**` token |
| V-55-30 | NEGATIVE: NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens |
| V-55-31 | SC#5 multi-platform frontmatter |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/operations/app-lifecycle/03-ios-vpp-licensing.md (frontmatter + Platform applicability blockquote at TOP + H1 + intro + 2-column VPP attribute comparison table with 6+ rows including reclamation conjuncts + 3-step reclamation workflow narrative + cross-link to v1.3 admin-setup-ios + Related Resources/External References footer; ZERO Mermaid blocks per D-09)</name>
  <files>docs/operations/app-lifecycle/03-ios-vpp-licensing.md</files>
  <read_first>
    - .planning/phases/55-app-lifecycle-automation/55-CONTEXT.md (D-07 3A winner scope; D-08 BYOD/supervision dimensions reused from v1.3; D-09 Mermaid forbidden; D-13 cross-platform inline blockquote; D-19 platform: iOS; CD-07 plan-author row-label discretion; CDI-Phase55-06 Mermaid pattern parity)
    - .planning/phases/55-app-lifecycle-automation/55-RESEARCH.md (§7 iOS VPP Device-vs-User Licensing + Reclamation — Microsoft Learn ms.date 2026-01-13 vpp-apps-ios verbatim facts; 3-step reclamation workflow correction; macOS 30-day grace period vs iOS no-grace; supervised silent install scenarios)
    - .planning/phases/55-app-lifecycle-automation/55-VALIDATION.md (Per-Task Verification Map rows 55-04-01 through 55-04-05 — 5 grep-verifiable acceptance criteria for this file; Manual-Only Verifications row 4 plan-author resolution guidance for 3-step reclamation workflow)
    - docs/operations/patch-management/03-ios-update-lifecycle.md (Phase 54 iOS sibling — zero-Mermaid pattern parity source; Platform applicability blockquote at lines 9-14 token verbatim; iOS-specific frontmatter shape `platform: iOS`)
    - docs/admin-setup-ios/05-app-deployment.md (v1.3 cross-link target — verify the file exists and contains the 4-column app-type comparison table at lines 51-64 + silent-install boundary scenarios at lines 36-43 + device-vs-user prose at lines 24-28; the cross-link target lines must be stable for the relative-path link to land correctly)
  </read_first>
  <behavior>
    - File `docs/operations/app-lifecycle/03-ios-vpp-licensing.md` exists
    - Frontmatter (D-19): `last_verified: 2026-04-28`, `review_by: 2026-06-27`, `applies_to: all`, `audience: admin` (CD-10 default), `platform: iOS`
    - Platform applicability blockquote at TOP (immediately after frontmatter `---` closing fence, before H1) routing back to 00-overview + 3 sibling per-platform files using verbatim `> **Platform applicability:**` token (D-13 + V-55-26)
    - H1 `# iOS VPP Licensing: Device vs User + Reclamation Behavior` (or equivalent) followed by intro paragraph cross-linking to 00-overview hub + v1.3 admin-setup-ios/05-app-deployment.md for the broader 4-column app-type matrix
    - 2-column VPP attribute comparison table with column headers `VPP Device-Licensed | VPP User-Licensed` per D-07 + V-55-18
    - Table contains 6+ rows: license source / Apple ID requirement / supervised silent install / reclamation on retire-wipe / reclamation on remove-app / BYOD applicability (CD-07 plan-author may add reclamation grace period or ABM revocation behavior rows; row labels are plan-author discretion)
    - Reclamation rows contain literals `retire/wipe` (or `retire and wipe`) AND `device license` (or `device-license`) within reclamation row context per V-55-19 conjunct A
    - Reclamation rows contain literals `remove app` (or `remove-app`) AND `user license` (or `user-license`) within reclamation row context per V-55-19 conjunct B
    - Microsoft Learn 3-step reclamation workflow encoded as sub-section under reclamation rows OR narrative beneath table (RESEARCH §7 + Manual-Only Verifications row 4): step 1 = remove assignment from device/user/group; step 2 = change app assignment to Uninstall; step 3 = use Revoke license action in Intune admin center
    - BYOD/Corporate × Supervised/Unsupervised dimensions encoded as value-cell qualifiers per D-08 (e.g., "Yes (supervised) / Partial (unsupervised)") — NOT a separate sub-matrix
    - User Enrollment as third state covered in BYOD-applicability row prose with cross-link to v1.3 09-mam-app-protection.md or 08-user-enrollment.md
    - Cross-link to v1.3 iOS app deployment via relative path `../../admin-setup-ios/05-app-deployment.md` per V-55-21
    - ZERO Mermaid code blocks (` ```mermaid ` patterns) per D-09 + V-55-20 NEGATIVE regression-guard
    - NO bare `> **Platform:**` token (FORBIDDEN per V-55-27)
    - NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens
  </behavior>
  <action>
Create the file `docs/operations/app-lifecycle/03-ios-vpp-licensing.md` with the following content sections IN ORDER. The directory `docs/operations/app-lifecycle/` is created by 55-01 in the same atomic commit.

**Section 1 — Frontmatter (D-19; copy verbatim):**

```yaml
---
last_verified: 2026-04-28
review_by: 2026-06-27
applies_to: all
audience: admin
platform: iOS
---
```

**Section 2 — Platform applicability blockquote at TOP (D-13 + V-55-26; pointer-routing back to 00-overview + 3 sibling files; mirrors Phase 54 docs/operations/patch-management/03-ios-update-lifecycle.md:9-14 verbatim token discipline):**

```markdown
> **Platform applicability:** This guide is iOS/iPadOS-specific (VPP Device-Licensed vs
> User-Licensed distinction; reclamation behavior for retire/wipe and remove-app scenarios).
> For the cross-platform overview, see [App Lifecycle Automation Overview](00-overview.md).
> **Windows:** See [Win32 / MSIX at Scale](01-windows-win32-msix-scale.md).
> **macOS:** See [macOS PKG / DMG Pipeline](02-macos-pkg-dmg-pipeline.md).
> **Android:** See [Android MGP Lifecycle](04-android-mgp-lifecycle.md).
```

**Section 3 — H1 + intro paragraph:**

```markdown
# iOS VPP Licensing: Device vs User + Reclamation Behavior

This guide is the iOS/iPadOS-specific VPP (Apple Volume Purchase Program, now Apple Business
Manager / Apple School Manager apps and books) licensing reference. It covers the two
licensing models (Device-Licensed vs User-Licensed), their attribute differences, the
supervised-vs-unsupervised silent-install boundary, and — critically — the **manual
reclamation workflow** that admins must execute to return licenses to the available pool when
devices are retired or apps are unassigned.

For the broader 4-column app-type comparison covering LOB apps and Store Apps alongside VPP
Device-Licensed and VPP User-Licensed (12 attributes including silent-install scenarios per
BYOD/Corporate × Supervised/Unsupervised), see
[iOS App Deployment](../../admin-setup-ios/05-app-deployment.md). This guide deepens the VPP
slice with reclamation behavior — content that is **net-new in Phase 55** and not present in
the v1.3 admin-setup-ios runbook.
```

**Section 4 — VPP Licensing Models H2 + 2-column comparison table (V-55-18 + V-55-19; per D-07 3A; minimum 6 rows; CD-07 row-label discretion):**

```markdown
## VPP Licensing Models

The two VPP licensing models are device-licensing and user-licensing. The model is selected
per-app at assignment time in the Intune admin center. The table below compares the two models
across the attributes that matter for admin decision-support: license source, Apple ID
requirement, silent-install behavior, **reclamation on retire/wipe**, **reclamation on
remove-app**, and BYOD applicability.

| Attribute                          | VPP Device-Licensed                                              | VPP User-Licensed                                              |
|------------------------------------|------------------------------------------------------------------|----------------------------------------------------------------|
| License source                     | Token from Apple Business Manager (ABM) / Apple School Manager (ASM); 1 license per device | Token from Apple Business Manager (ABM) / Apple School Manager (ASM); 1 license / up to 5 devices per user |
| Apple ID requirement               | Not required — the license attaches to the device, not a user account | Required — each end user must use a unique personal or Managed Apple Account when prompted to sign in to App Store |
| Supervised silent install          | Yes (supervised devices in CORP / Kiosk modes — zero prompts) / Partial (unsupervised devices may prompt for VPP invitation) | Yes only after user accepts the VPP invitation in App Store; supervised vs unsupervised does NOT change this baseline behavior |
| Reclamation on retire/wipe         | License returns to the available pool when the device is wiped or removed from Intune (note: per Microsoft Learn, App licenses aren't automatically reclaimed when a device is removed from Intune management — see 3-step reclamation workflow below) | Same caveat: license is NOT automatically reclaimed; admin must execute the 3-step reclamation workflow |
| Reclamation on remove app          | Removing the app assignment does NOT automatically reclaim the device license — admin must change assignment to Uninstall + Revoke license to return the device license to the available pool | Removing the app assignment does NOT automatically reclaim the user license — admin must execute the 3-step reclamation workflow to return the user license |
| BYOD applicability                 | Not supported on User Enrollment (BYOD enrollment); supported on Corporate-owned (CORP) and Kiosk supervised devices — see [iOS User Enrollment](../../admin-setup-ios/08-user-enrollment.md) and [iOS MAM App Protection](../../admin-setup-ios/09-mam-app-protection.md) for the BYOD scenarios | Supported on User Enrollment (BYOD) using Managed Apple Accounts; preferred model for BYOD scenarios |
```

The 6 rows above satisfy V-55-18 (2 column headers `VPP Device-Licensed | VPP User-Licensed`) and V-55-19 (reclamation literals `retire/wipe` + `device license` in row 4; `remove app` + `user license` in row 5). CD-07 plan-author discretion preserves freedom to add a 7th row (e.g., "Reclamation grace period" — macOS has 30-day; iOS has none — or "ABM revocation behavior") at execution time. Adding a 7th row is encouraged for decision-support depth; not required for V-55-19 satisfaction.

**Section 5 — Reclamation Workflow H2 (Microsoft Learn 3-step manual workflow per RESEARCH §7 + 55-VALIDATION.md Manual-Only Verifications row 4):**

```markdown
## Reclamation Workflow

The REQ APP-06 wording "retire/wipe returns device license; remove app returns user license" is
**oversimplified** — Microsoft Learn confirms that App licenses are NOT automatically reclaimed
when a device is removed from Intune management or when an app assignment is removed. Returning
a license to the available pool requires a 3-step manual workflow in the Intune admin center:

1. **Remove the assignment** from the target device (or user, or group). Intune > Apps > All
   apps > [select VPP app] > Properties > Assignments > Remove the device/user/group from
   Required, Available, or Uninstall intent.
2. **Change the app's assignment to Uninstall** for the same target. The Uninstall intent
   triggers Intune to instruct the MDM channel to remove the app from the device. Wait for the
   uninstall to complete (verify in the device's app inventory or in Intune > Devices >
   [device] > Managed apps).
3. **Use the Revoke license action.** Intune > Apps > Apple VPP tokens > [select token] >
   Licenses > [select app] > select the device or user → click **Revoke license**. The license
   returns to the available pool and can be re-assigned.

> **Cross-platform note:** macOS VPP has a 30-day grace period before licenses are reclaimed
> after device removal. iOS VPP has NO grace period — the manual workflow above is the only
> mechanism. This asymmetry is why the reclamation conjunct is verified net-new in Phase 55
> and not deferred to a cross-platform shared section.

For the underlying VPP token + app management surfaces (token sync, license assignment,
managed-vs-unmanaged distribution), see
[iOS App Deployment](../../admin-setup-ios/05-app-deployment.md).
```

**Section 6 — Decision Support H2 (when to choose Device-Licensed vs User-Licensed):**

```markdown
## Decision Support: Device-Licensed vs User-Licensed

Choose the licensing model based on the device-ownership model and the user-experience target:

- **Pick Device-Licensed when:** the deployment is corporate-owned and supervised; the admin
  wants the silent-install user experience (no Apple ID prompts, no VPP invitation acceptance);
  the use case is shared-shift kiosks, dedicated devices, or fleet-distributed CORP devices
  where user identity is irrelevant to app access. License count is per-device; one device =
  one license consumed.
- **Pick User-Licensed when:** the deployment is BYOD (User Enrollment with Managed Apple
  Accounts); each user has their own personal Apple Account; a single user has multiple
  enrolled devices (up to 5 devices share the user license); the use case is hybrid-work
  knowledge workers or Education students with personal devices.
- **Avoid mixing within a single app assignment:** an Intune VPP app can be configured for
  Device-Licensed OR User-Licensed at deployment time; switching mid-deployment requires
  un-assigning + re-assigning + reclaiming all currently-issued licenses (the 3-step workflow
  in Section 5 applies to every device or user that switches between models).

For the BYOD enrollment paths (User Enrollment), see
[iOS User Enrollment](../../admin-setup-ios/08-user-enrollment.md). For the silent-install
boundary table covering all 6 BYOD/Corp × user/device-licensed × supervised/unsupervised
scenarios, see [iOS App Deployment §Silent Install Boundaries](../../admin-setup-ios/05-app-deployment.md)
(lines 36-43 in the v1.3 file).
```

**Section 7 — Related Resources + External References footer:**

```markdown
## Related Resources

- [App Lifecycle Automation Overview](00-overview.md) — Cross-platform hub
- [iOS App Deployment](../../admin-setup-ios/05-app-deployment.md) — v1.3 — Full 4-column
  app-type comparison (LOB / Store Apps / VPP Device-Licensed / VPP User-Licensed × 12
  attributes); silent-install boundary scenarios; device-vs-user prose
- [iOS User Enrollment](../../admin-setup-ios/08-user-enrollment.md) — v1.3 — BYOD enrollment
  with Managed Apple Accounts
- [iOS MAM App Protection](../../admin-setup-ios/09-mam-app-protection.md) — v1.3 — App
  Protection Policies for BYOD scenarios
- [iOS Update Lifecycle](../patch-management/03-ios-update-lifecycle.md) — Phase 54 — DDM
  update enforcement (sibling ops-domain)

## External References

- [Apple VPP via Intune (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/intune-service/apps/vpp-apps-ios) — Canonical VPP doc; ms.date 2026-01-13
- [Manage iOS/iPadOS volume purchased apps (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/intune-service/apps/vpp-apps-ios#manage-volume-purchased-apps)
- [Apple Business Manager](https://business.apple.com) — VPP token source for Corporate-owned
  deployments
- [Apple School Manager](https://school.apple.com) — VPP token source for Education deployments
- [Operations Documentation Index](../00-index.md) — Cross-reference only; Phase 55 does NOT amend the operations index per ROADMAP line 457
```

**Mermaid prohibition self-check (D-09 + V-55-20):** After authoring, the file MUST NOT contain any ` ```mermaid ` code-fence opener. The Phase 54 iOS sibling pattern parity is the load-bearing constraint — `docs/operations/patch-management/03-ios-update-lifecycle.md` has zero Mermaid; v1.3 admin-setup-ios has zero Mermaid; this file inherits the pattern. If a diagram is genuinely needed for decision-support, use an ASCII tree or a markdown table. V-55-20 NEGATIVE regression-guard will FAIL the validator if any ` ```mermaid ` opener is present.

After authoring, verify the following grep checks pass against the new file:

```bash
grep -c "platform: iOS" docs/operations/app-lifecycle/03-ios-vpp-licensing.md             # expect 1
grep -c "^> \*\*Platform applicability:\*\*" docs/operations/app-lifecycle/03-ios-vpp-licensing.md  # expect 1
grep -E "VPP Device-Licensed.*VPP User-Licensed|Device-Licensed.*User-Licensed" docs/operations/app-lifecycle/03-ios-vpp-licensing.md  # expect match (V-55-18)
grep -E "(retire/wipe|retire and wipe).*(device license|device-license)|(device license|device-license).*(retire/wipe|retire and wipe)" docs/operations/app-lifecycle/03-ios-vpp-licensing.md  # expect match (V-55-19 conjunct A)
grep -E "(remove app|remove-app).*(user license|user-license)|(user license|user-license).*(remove app|remove-app)" docs/operations/app-lifecycle/03-ios-vpp-licensing.md  # expect match (V-55-19 conjunct B)
grep -c "^\`\`\`mermaid" docs/operations/app-lifecycle/03-ios-vpp-licensing.md  # MUST be 0 (V-55-20)
grep -c "../../admin-setup-ios/05-app-deployment.md" docs/operations/app-lifecycle/03-ios-vpp-licensing.md  # expect >=1 (V-55-21)
grep -c "^> \*\*Platform:\*\*" docs/operations/app-lifecycle/03-ios-vpp-licensing.md  # MUST be 0 (V-55-27)
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/app-lifecycle/03-ios-vpp-licensing.md  # MUST be empty (V-55-30)
```

DO NOT commit yet — atomic commit owned by 55-07 author per CONTEXT D-21 + CDI-Phase55-05.
  </action>
  <verify>
    <automated>test -f docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; grep -q "platform: iOS" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; grep -q "^> \*\*Platform applicability:\*\*" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; grep -qE "VPP Device-Licensed|Device-Licensed" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; grep -qE "VPP User-Licensed|User-Licensed" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; grep -qE "retire/wipe|retire and wipe" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; grep -qE "device license|device-license" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; grep -qE "remove app|remove-app" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; grep -qE "user license|user-license" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; ! grep -q "^\`\`\`mermaid" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; grep -q "../../admin-setup-ios/05-app-deployment.md" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; ! grep -q "^> \*\*Platform:\*\*$" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; ! grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/app-lifecycle/03-ios-vpp-licensing.md &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/operations/app-lifecycle/03-ios-vpp-licensing.md` exists
    - File contains frontmatter with `platform: iOS`, `audience: admin` (or `admin,L2`), `last_verified: 2026-04-28`, `review_by: 2026-06-27`, `applies_to: all`
    - File contains literal `> **Platform applicability:**` blockquote within first 50 lines of body (V-55-26)
    - File contains 2-column VPP comparison table with column headers `VPP Device-Licensed` AND `VPP User-Licensed` (or `Device-Licensed` AND `User-Licensed`) per V-55-18 → grep `\| (VPP )?Device-Licensed \| (VPP )?User-Licensed` returns ≥1 match
    - File contains literal `retire/wipe` (or `retire and wipe`) AND `device license` (or `device-license`) within reclamation row context per V-55-19 conjunct A
    - File contains literal `remove app` (or `remove-app`) AND `user license` (or `user-license`) within reclamation row context per V-55-19 conjunct B
    - File contains ZERO Mermaid code blocks per V-55-20 → `grep -c "^\`\`\`mermaid"` returns 0
    - File contains literal cross-link `../../admin-setup-ios/05-app-deployment.md` per V-55-21 (≥1 occurrence)
    - File does NOT contain bare `> **Platform:**` token per V-55-27 → `grep -c "^> \*\*Platform:\*\*$"` returns 0
    - File does NOT contain any of: `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` per V-55-30
    - 3-step reclamation workflow narrative present (remove assignment / change to Uninstall / Revoke license) — content correctness check; not validator-asserted but required by RESEARCH §7 + Manual-Only Verifications row 4
    - BYOD/Corporate × Supervised/Unsupervised dimensions encoded as value-cell qualifiers per D-08 (e.g., "Yes (supervised) / Partial (unsupervised)") — content correctness check
  </acceptance_criteria>
  <done>03-ios-vpp-licensing.md authored with all D-07/D-08/D-09/D-13/D-19 surfaces; V-55-18/19/20/21/26/27/30/31 assertions for this file pass; no commit (55-07 owns atomicity); zero Mermaid blocks confirmed.</done>
</task>

</tasks>

<threat_model>
None — Phase 55 is documentation-only. No code paths modified, no API surface added, no authentication/authorization changes, no data handling, no third-party dependency additions. Documentation accuracy is the only safety surface and is enforced by literal-token regex validator (check-phase-55.mjs) + Microsoft Learn citation discipline + 60-day last_verified frontmatter cycle.
</threat_model>

<verification>
After Task 1 completes:

```bash
# File exists and contains required structural elements
test -f docs/operations/app-lifecycle/03-ios-vpp-licensing.md && echo "exists: PASS"

# Frontmatter
head -7 docs/operations/app-lifecycle/03-ios-vpp-licensing.md | grep -E "^(platform|audience|last_verified|review_by|applies_to):"

# Platform applicability blockquote in first 50 lines of body (V-55-26)
sed -n '/^---$/,/^---$/!p' docs/operations/app-lifecycle/03-ios-vpp-licensing.md | head -50 | grep -c "^> \*\*Platform applicability:\*\*"
# expect 1

# V-55-18 — 2-column VPP comparison table
grep -E "\| (VPP )?Device-Licensed \| (VPP )?User-Licensed|\| (VPP )?User-Licensed \| (VPP )?Device-Licensed" docs/operations/app-lifecycle/03-ios-vpp-licensing.md && echo "V-55-18: PASS"

# V-55-19 — reclamation conjuncts
grep -E "(retire/wipe|retire and wipe).*(device license|device-license)|(device license|device-license).*(retire/wipe|retire and wipe)" docs/operations/app-lifecycle/03-ios-vpp-licensing.md && echo "V-55-19 conjunct A: PASS"
grep -E "(remove app|remove-app).*(user license|user-license)|(user license|user-license).*(remove app|remove-app)" docs/operations/app-lifecycle/03-ios-vpp-licensing.md && echo "V-55-19 conjunct B: PASS"

# V-55-20 — Mermaid NEGATIVE
mermaid_count=$(grep -c "^\`\`\`mermaid" docs/operations/app-lifecycle/03-ios-vpp-licensing.md)
[ "$mermaid_count" -eq 0 ] && echo "V-55-20 (zero Mermaid): PASS" || echo "V-55-20 FAIL: $mermaid_count Mermaid blocks found"

# V-55-21 — cross-link to v1.3 iOS app deployment
grep -c "\\.\\./\\.\\./admin-setup-ios/05-app-deployment\\.md" docs/operations/app-lifecycle/03-ios-vpp-licensing.md
# expect >=1

# V-55-27 — bare-Platform NEGATIVE
grep -c "^> \*\*Platform:\*\*$" docs/operations/app-lifecycle/03-ios-vpp-licensing.md
# expect 0

# V-55-30 — TBD scan
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/app-lifecycle/03-ios-vpp-licensing.md
# expect empty

# Cross-link target verification (anti-duplication mitigation per D-07)
test -f docs/admin-setup-ios/05-app-deployment.md && echo "cross-link target exists: PASS"
```

If 55-06 validator is also authored at this point (Wave 2):

```bash
node scripts/validation/check-phase-55.mjs --verbose 2>&1 | grep "V-55-04\|V-55-07\|V-55-18\|V-55-19\|V-55-20\|V-55-21\|V-55-26\|V-55-27\|V-55-30\|V-55-31"
# All file-specific assertions for 03-ios-vpp-licensing.md should PASS once all sibling files also land
```

DO NOT commit — atomic commit owned by 55-07 per CONTEXT D-21.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 55 Success Criteria + REQUIREMENTS.md APP-06:

- **SC#4** (admin can distinguish iOS VPP device-licensing from user-licensing with reclamation behavior): Satisfied by 2-column comparison table with reclamation rows + 3-step reclamation workflow narrative
- **APP-06** (VPP device-licensing + user-licensing + reclamation behavior): Satisfied by Sections 4-6 (VPP Licensing Models table + Reclamation Workflow + Decision Support)

Plan complete when:

- [ ] File exists at `docs/operations/app-lifecycle/03-ios-vpp-licensing.md`
- [ ] V-55-04 (file exists), V-55-07 (frontmatter), V-55-18 (2-column comparison), V-55-19 (reclamation conjuncts), V-55-20 (no Mermaid), V-55-21 (cross-link to v1.3), V-55-26 (blockquote at TOP), V-55-27 (no bare-Platform), V-55-30 (TBD scan), V-55-31 (multi-platform frontmatter) all pass
- [ ] 3-step reclamation workflow encoded (remove assignment / change to Uninstall / Revoke license)
- [ ] BYOD/Corp × Supervised/Unsupervised dimensions encoded as value-cell qualifiers (D-08 reuse from v1.3 cross-reference)
- [ ] Zero Mermaid blocks confirmed (Phase 54 iOS sibling pattern parity per D-09 + CDI-Phase55-06)
- [ ] No commit — 55-07 owns atomicity per CONTEXT D-21
</success_criteria>

<output>
After completion, create `.planning/phases/55-app-lifecycle-automation/55-04-SUMMARY.md` documenting:

- File path + line count
- Sections authored (frontmatter, Platform applicability blockquote, H1 + intro, VPP Licensing Models, Reclamation Workflow, Decision Support, Related Resources, External References)
- V-55-NN assertions this file satisfies (V-55-04, V-55-07, V-55-18, V-55-19, V-55-20, V-55-21, V-55-26, V-55-27, V-55-30, V-55-31)
- Cross-link paths used (../../admin-setup-ios/05-app-deployment.md per V-55-21; 00-overview.md back-link; sibling Related Resources + External References)
- Reclamation workflow encoding choice (sub-section under reclamation rows OR narrative beneath table) per RESEARCH §7 + Manual-Only Verifications row 4
- 7th row choice if added (CD-07 plan-author discretion)
- Note: NO COMMIT — atomic commit owned by 55-07
</output>
</context>
</content>
</invoke>