---
phase: 61-gap-closure-terminal-re-audit-milestone-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/check-phase-53.mjs
  - docs/index.md
autonomous: true
requirements:
  - CHAIN-V53-06    # validator alignment per CONTEXT D-02 (refresh V-53-06)
  - CHAIN-V53-22    # validator alignment per CONTEXT D-03 (refresh V-53-22)
  - UX-FOLD-TODO    # docs/index.md jump-link bullets fold per D-20

must_haves:
  truths:
    - "node scripts/validation/check-phase-53.mjs exits 0 (26/26 V-53-NN PASS post-refresh)"
    - "node scripts/validation/check-phase-60.mjs V-60-16 flips FAIL→PASS (25/25 V-60-NN PASS) — chain regression-guard satisfied"
    - "node scripts/validation/check-phase-59.mjs exits 0 (regression-guard — Phase 59 H2 deliverables byte-identical)"
    - "docs/index.md Choose Your Platform jump-link list contains 7 bullets (5 pre-existing + Linux Provisioning + Operations) per CONTEXT D-20 todo fold-in"
    - "docs/index.md platform-coverage blockquote at line 9 references Linux + Operations per CONTEXT D-20 todo body"
  artifacts:
    - path: "scripts/validation/check-phase-53.mjs"
      provides: "V-53-06 per-file scope-exemption + V-53-22 SCOPE_RESTRICTED to CO_MGMT_FILES (mirrors V-53-10 precedent at lines 149-165)"
      contains: "CO_MGMT_FILES iteration in V-53-22; per-file conditional in V-53-06"
    - path: "docs/index.md"
      provides: "7-bullet Choose Your Platform jump-link list + updated platform-coverage blockquote"
      contains: "linux-provisioning jump-link + #operations jump-link + Linux mention in line 9 blockquote"
  key_links:
    - from: "scripts/validation/check-phase-53.mjs"
      to: "docs/operations/00-index.md (IDX)"
      via: "per-file conditional in V-53-06; CO_MGMT_FILES exclusion in V-53-22"
      pattern: "f === IDX|CO_MGMT_FILES"
    - from: "docs/index.md jump-link list"
      to: "## Linux Provisioning H2 + ## Operations H2"
      via: "anchor jump-links #linux-provisioning + #operations"
      pattern: "linux-provisioning|#operations"
---

<objective>
Refresh `scripts/validation/check-phase-53.mjs` V-53-06 + V-53-22 assertions to align with Phase 59 D-10's deliberate architectural evolution of `docs/operations/00-index.md` from a co-management-only stub into a 4-domain cross-platform index, AND fold in the score-0.9 pending todo (`2026-05-06-choose-your-platform-linux-operations-bullets.md`) by appending 2 bullets to the `docs/index.md` "Choose Your Platform" jump-link list and updating the platform-coverage blockquote at line 9.

Purpose: Closes the V-60-16 chain regression-guard inherited from Phase 60 (out-of-scope per orchestrator boundary at 60-VERIFICATION.md:325-345). Validator catch-up — Phase 59 D-10 was the superseding decision; Phase 61 records the alignment per CONTEXT D-01..D-07. Also completes the hub navigation UX promise after Phase 59 added 2 new H2 sections without updating the jump-link list (informational-only flag at Phase 59 close per todo file:29).

Output: 2 atomic commits per CONTEXT D-04 — (1) `fix(check-phase-53): align V-53-06 + V-53-22 with Phase 59 D-10 superseding decision`, (2) `docs(61-01): fold Linux + Operations bullets into docs/index.md jump-link list`. After Plan 61-01 lands: 25/25 V-60-NN PASS in check-phase-60.mjs (V-60-16 flips), 26/26 V-53-NN PASS in check-phase-53.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/61-gap-closure-terminal-re-audit-milestone-close/61-CONTEXT.md
@.planning/phases/61-gap-closure-terminal-re-audit-milestone-close/61-RESEARCH.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-VERIFICATION.md
@.planning/todos/pending/2026-05-06-choose-your-platform-linux-operations-bullets.md

<interfaces>
<!-- Key implementation patterns extracted from check-phase-53.mjs source. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From scripts/validation/check-phase-53.mjs (line 21-29 — file constants):
```javascript
const OV  = "docs/operations/co-management/00-overview.md";
const TA  = "docs/operations/co-management/01-windows-tenant-attach.md";
const WS  = "docs/operations/co-management/02-windows-workload-sliders.md";
const MP  = "docs/operations/co-management/03-cocmgmt-migration-paths.md";
const IDX = "docs/operations/00-index.md";

const CO_MGMT_FILES = [OV, TA, WS, MP];                  // V-53-10 NEGATIVE scope
const CONTENT_FILES = [OV, TA, WS, MP, IDX];             // V-53-06 frontmatter + V-53-25 TBD scan
```

From scripts/validation/check-phase-53.mjs:86 (CURRENT failing V-53-06 line):
```javascript
if (!/^platform: Windows\s*$/m.test(fm)) issues.push("platform: Windows missing");
```

From scripts/validation/check-phase-53.mjs:149-165 (V-53-10 SCOPE_RESTRICTED PRECEDENT — mirror for V-53-22 refresh):
```javascript
{
  id: 10, name: "V-53-10: NEGATIVE — no 'partially/fully migrated' variants in 4 co-management files (PITFALL-8 regression-guard; SCOPE RESTRICTED to CO_MGMT_FILES, NOT glob)",
  run() {
    const failures = [];
    const banned = [/* ...patterns... */];
    for (const f of CO_MGMT_FILES) {  // SCOPE RESTRICTION: only docs/operations/co-management/
      const c = readFile(f);
      if (c === null) { failures.push(f + ": file missing"); continue; }
      const found = banned.filter(r => r.test(c)).map(r => r.toString());
      if (found.length > 0) failures.push(f + ": PITFALL-8 violation: " + found.join(", "));
    }
    if (failures.length > 0) return { pass: false, detail: failures.join(" | ") };
    return { pass: true, detail: "no partially/fully-migrated phrasings in 4 co-management files" };
  }
},
```

From scripts/validation/check-phase-53.mjs:331-355 (CURRENT failing V-53-22 — refresh target):
```javascript
{
  id: 22, name: "V-53-22: 00-index.md has POSITIVE ## Co-Management H2 + NEGATIVE no scaffold/Phase 54-56 H2s (NOVEL single-H2 contract)",
  run() {
    const c = readFile(IDX);
    if (c === null) return { pass: false, detail: "File missing: " + IDX };
    if (!/^## Co-Management\s*$/m.test(c))
      return { pass: false, detail: "POSITIVE: ## Co-Management H2 missing from 00-index.md" };
    const banPatterns = [
      { p: /^## Patch Management/m, name: "## Patch Management H2" },
      { p: /^## App Lifecycle/m, name: "## App Lifecycle H2" },
      { p: /^## Drift/m, name: "## Drift H2" },
      { p: /^## Tenant Migration/m, name: "## Tenant Migration H2" },
      { p: /Phase 5[4-6]/m, name: "'Phase 54-56' reference" },
      { p: /\bTBD\b/m, name: "'TBD' token" },
      { p: /Coming in Phase/m, name: "'Coming in Phase' placeholder" }
    ];
    for (const { p, name } of banPatterns) {
      if (p.test(c)) return { pass: false, detail: "NEGATIVE regression-guard violated: " + name + " found (forbidden per D-02 + ROADMAP line 448)" };
    }
    return { pass: true, detail: "POSITIVE H2 present + no scaffold/future-phase H2s (single-H2-only contract honored)" };
  }
}
```

From docs/index.md (lines 9 + 16-22 — current state, append-only edit targets):
```
Line 9 blockquote (CURRENT):
> **Platform coverage:** This index covers Windows Autopilot (classic/APv1 and Device Preparation/APv2), macOS ADE, and iOS/iPadOS provisioning.

Lines 16-22 jump-link list (5 bullets — append-only contract per Phase 57 D-31 / Phase 59 D-11):
- [Windows Autopilot](#windows-autopilot) -- ...
- [macOS Provisioning](#macos-provisioning) -- ...
- [iOS/iPadOS Provisioning](#iosipados-provisioning) -- ...
- [Android Enterprise Provisioning](#android-enterprise-provisioning) -- ...
- [Cross-Platform References](#cross-platform-references) -- ...
```

From .planning/todos/pending/2026-05-06-choose-your-platform-linux-operations-bullets.md (§Solution lines 35-40 — VERBATIM bullet text):
```markdown
- [Linux Provisioning](#linux-provisioning) -- Linux device provisioning via Microsoft Intune Linux client (Ubuntu 22.04 / 24.04 LTS, intune-portal package, web-app conditional access)
- [Operations](#operations) -- Cross-platform operational depth (co-management, patch & update management, app lifecycle automation, drift detection + tenant migration)
```

From .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md:76 (D-10 superseding decision — load-bearing argument):
"`docs/operations/00-index.md` completion is MANDATORY same-commit per DPO-Phase56-01 hand-off chain. Phase 59 fills out the 3 currently-empty sub-domain sections..."

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refresh check-phase-53.mjs V-53-06 + V-53-22 (atomic harness commit per D-04)</name>
  <files>scripts/validation/check-phase-53.mjs</files>
  <read_first>
    - scripts/validation/check-phase-53.mjs (READ FULL FILE — lines 1-375 — see current V-53-06 at line 86, V-53-10 SCOPE_RESTRICTED precedent at lines 149-165, V-53-22 at lines 331-354)
    - .planning/phases/61-gap-closure-terminal-re-audit-milestone-close/61-CONTEXT.md §"Chain validator alignment" lines 36-46 (D-01..D-07)
    - .planning/phases/61-gap-closure-terminal-re-audit-milestone-close/61-RESEARCH.md §"Architecture Patterns" lines 167-211 (V-53-06 + V-53-22 refresh code patterns)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md lines 63-90 (D-10 authoritative supersession)
    - docs/operations/00-index.md (READ FULL FILE — confirm `platform: cross-platform` frontmatter + 4 H2s `## Co-Management`, `## Patch & Update Management`, `## App Lifecycle Automation`, `## Compliance Drift Detection + Tenant Migration`)
  </read_first>
  <action>
    Apply two surgical edits to `scripts/validation/check-phase-53.mjs` per CONTEXT D-02 + D-03 (per-file scope-exemption preferred mechanism).

    **EDIT 1 — V-53-06 refresh (line 86 region, inside the `for (const f of CONTENT_FILES) { ... }` loop):**

    REPLACE the single-line check at line 86:
    ```javascript
    if (!/^platform: Windows\s*$/m.test(fm)) issues.push("platform: Windows missing");
    ```

    WITH a per-file conditional that exempts IDX (Phase 59 D-10 expanded IDX to cross-platform; 4 co-management files retain strict `platform: Windows` contract):
    ```javascript
    if (f === IDX) {
      // Phase 61 D-02 alignment per Phase 59 D-10 supersession: docs/operations/00-index.md
      // expanded to cross-platform 4-domain index; accept any non-empty platform: value.
      if (!/^platform:\s*\S/m.test(fm)) issues.push("platform field missing or empty");
    } else {
      // 4 co-management content files retain strict platform: Windows contract per Phase 53 D-02
      if (!/^platform: Windows\s*$/m.test(fm)) issues.push("platform: Windows missing");
    }
    ```

    Also UPDATE the V-53-06 `name:` literal (line 76) to record the alignment:
    ```javascript
    id: 6, name: "V-53-06: all 5 content files have platform field + audience: <non-empty> + 60-day cycle (V-53-06 SCOPED per Phase 61 D-02: IDX accepts any platform value per Phase 59 D-10; CO_MGMT_FILES retain strict platform: Windows)",
    ```

    **EDIT 2 — V-53-22 refresh (lines 331-355, full check body):**

    REPLACE the V-53-22 `run()` body so it iterates `CO_MGMT_FILES` instead of checking IDX. Mirror the V-53-10 SCOPE_RESTRICTED precedent at lines 149-165 verbatim. The 4 co-management files legitimately should NOT contain ops-domain H2s (that is the right regression-guard surface). IDX is exempt because Phase 59 D-10 authored ops-domain H2s there as a deliberate architectural decision.

    ```javascript
    {
      id: 22, name: "V-53-22: NEGATIVE — 4 co-management files have no scaffold/ops-domain H2s (SCOPE RESTRICTED to CO_MGMT_FILES per Phase 61 D-03 alignment with Phase 59 D-10 supersession; IDX exempted because Phase 59 D-10 authored ops-domain H2s in 00-index.md as deliberate architectural evolution from Phase 53 D-02 single-H2 stub state)",
      run() {
        const failures = [];
        const banPatterns = [
          { p: /^## Patch Management/m, name: "## Patch Management H2" },
          { p: /^## App Lifecycle/m, name: "## App Lifecycle H2" },
          { p: /^## Drift/m, name: "## Drift H2" },
          { p: /^## Tenant Migration/m, name: "## Tenant Migration H2" },
          { p: /Phase 5[4-6]/m, name: "'Phase 54-56' reference" },
          { p: /\bTBD\b/m, name: "'TBD' token" },
          { p: /Coming in Phase/m, name: "'Coming in Phase' placeholder" }
        ];
        for (const f of CO_MGMT_FILES) {  // SCOPE RESTRICTION per Phase 61 D-03 (mirrors V-53-10 precedent at lines 149-165)
          const c = readFile(f);
          if (c === null) { failures.push(f + ": file missing"); continue; }
          for (const { p, name } of banPatterns) {
            if (p.test(c)) failures.push(f + ": NEGATIVE regression-guard violated: " + name);
          }
        }
        if (failures.length > 0) return { pass: false, detail: failures.join(" | ") };
        return { pass: true, detail: "4 co-management files have no scaffold/ops-domain H2s (IDX exempted per Phase 59 D-10 supersession)" };
      }
    },
    ```

    NOTE the H2 ban-pattern list MUST be preserved verbatim (all 7 patterns: Patch Management / App Lifecycle / Drift / Tenant Migration / Phase 5[4-6] / TBD / Coming in Phase). Only the iteration target changes (IDX → CO_MGMT_FILES). Do NOT add or remove patterns.

    **POST-EDIT VERIFICATION (run as part of task before commit):**
    1. `node scripts/validation/check-phase-53.mjs --verbose` — MUST exit 0; ALL 26 V-53-NN PASS (was 24 PASS / 2 FAIL pre-edit)
    2. `node scripts/validation/check-phase-60.mjs --verbose` — MUST exit 0; V-60-16 (which subprocess-invokes check-phase-53.mjs) flips FAIL→PASS; 25/25 V-60-NN PASS
    3. `node scripts/validation/check-phase-59.mjs --verbose` — MUST exit 0 (regression-guard; Phase 59 deliverables unaffected)

    **COMMIT (atomic single-commit per D-04):**
    Commit message: `fix(check-phase-53): align V-53-06 + V-53-22 with Phase 59 D-10 superseding decision`

    Body should reference: Phase 59 D-10 (`59-CONTEXT.md:76`); Phase 61 D-01..D-07; V-53-10 SCOPE_RESTRICTED precedent; closes V-60-16 chain regression-guard documented at 60-VERIFICATION.md:325-345.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-53.mjs && node scripts/validation/check-phase-60.mjs && node scripts/validation/check-phase-59.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-53.mjs` exits 0
    - `grep -c "V-53-" scripts/validation/check-phase-53.mjs` ≥ 26 (all 26 V-53-NN entries preserved; no assertion deleted)
    - `grep -c "for (const f of CO_MGMT_FILES)" scripts/validation/check-phase-53.mjs` ≥ 2 (V-53-10 existing iteration + V-53-22 new iteration)
    - `grep -c "Phase 61 D-0" scripts/validation/check-phase-53.mjs` ≥ 2 (cite Phase 61 D-02 in V-53-06 + Phase 61 D-03 in V-53-22)
    - `grep -c "Phase 59 D-10" scripts/validation/check-phase-53.mjs` ≥ 1 (cite supersession reference per CONTEXT D-28 mandate)
    - `grep -c "f === IDX" scripts/validation/check-phase-53.mjs` ≥ 1 (V-53-06 per-file conditional present)
    - `grep -c "## Patch Management H2" scripts/validation/check-phase-53.mjs` = 1 (V-53-22 banPatterns preserved verbatim — exactly one occurrence)
    - `grep -c "## App Lifecycle H2" scripts/validation/check-phase-53.mjs` = 1 (banPatterns preserved)
    - `grep -c "## Drift H2" scripts/validation/check-phase-53.mjs` = 1 (banPatterns preserved)
    - `grep -c "## Tenant Migration H2" scripts/validation/check-phase-53.mjs` = 1 (banPatterns preserved)
    - `node scripts/validation/check-phase-60.mjs --verbose` exits 0; verbose output shows "V-60-16:" line containing "PASS" (not "FAIL")
    - `node scripts/validation/check-phase-59.mjs` exits 0 (regression-guard — no impact on Phase 59 deliverables)
    - `git log -1 --format=%s` matches `fix(check-phase-53): align V-53-06 + V-53-22 with Phase 59 D-10 superseding decision`
  </acceptance_criteria>
  <done>
    All 4 verification commands exit 0; V-60-16 chain regression-guard FAIL→PASS confirmed; atomic harness commit landed per D-04; no V-53-NN assertion count regression (still 26 entries).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fold Linux + Operations bullets into docs/index.md (todo fold-in per D-20; append-only contract per Phase 57 D-31)</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md (READ FULL FILE — current state lines 1-25 contain blockquote at line 9 + Choose Your Platform 5-bullet jump-link list at lines 16-22)
    - .planning/todos/pending/2026-05-06-choose-your-platform-linux-operations-bullets.md (READ FULL FILE — §Solution body at lines 31-40 specifies VERBATIM bullet text; §Verification at lines 48-54 lists exact post-fix grep checks)
    - .planning/phases/61-gap-closure-terminal-re-audit-milestone-close/61-CONTEXT.md lines 92-98 (D-20 fold scope mandate + verbatim bullet text)
    - scripts/validation/check-phase-59.mjs (regression-guard target — must NOT regress)
  </read_first>
  <action>
    Apply two surgical APPEND-ONLY edits to `docs/index.md` per CONTEXT D-20 todo fold mandate. Both edits are within the same file in the same atomic commit.

    **EDIT 1 — Append 2 bullets to Choose Your Platform jump-link list (lines 21-22 region):**

    The current list at lines 18-22 has 5 bullets. Insert the 2 new bullets AFTER the Android Enterprise bullet (line 21) and BEFORE the Cross-Platform References bullet (line 22). Final order MUST be:

    ```markdown
    - [Windows Autopilot](#windows-autopilot) -- Windows device provisioning via Autopilot (classic/APv1) and Device Preparation (APv2)
    - [macOS Provisioning](#macos-provisioning) -- macOS device provisioning via Automated Device Enrollment (ADE) through Apple Business Manager
    - [iOS/iPadOS Provisioning](#iosipados-provisioning) -- iOS/iPadOS device provisioning via Microsoft Intune (ADE, Device Enrollment, Account-Driven User Enrollment, MAM-WE)
    - [Android Enterprise Provisioning](#android-enterprise-provisioning) -- Android device provisioning via Intune (Zero-Touch, Fully Managed, Work Profile, Dedicated, AOSP stub)
    - [Linux Provisioning](#linux-provisioning) -- Linux device provisioning via Microsoft Intune Linux client (Ubuntu 22.04 / 24.04 LTS, intune-portal package, web-app conditional access)
    - [Operations](#operations) -- Cross-platform operational depth (co-management, patch & update management, app lifecycle automation, drift detection + tenant migration)
    - [Cross-Platform References](#cross-platform-references) -- Glossaries, concept comparison, and shared resources
    ```

    The 2 NEW bullets MUST be VERBATIM the text specified in the todo file §Solution lines 36-37:
    - `- [Linux Provisioning](#linux-provisioning) -- Linux device provisioning via Microsoft Intune Linux client (Ubuntu 22.04 / 24.04 LTS, intune-portal package, web-app conditional access)`
    - `- [Operations](#operations) -- Cross-platform operational depth (co-management, patch & update management, app lifecycle automation, drift detection + tenant migration)`

    The existing 5 bullets MUST remain BYTE-IDENTICAL (append-only contract per Phase 57 D-31 / Phase 59 D-11).

    **EDIT 2 — Update platform-coverage blockquote at line 9:**

    Current line 9 (from previous Read):
    ```
    > **Platform coverage:** This index covers Windows Autopilot (classic/APv1 and Device Preparation/APv2), macOS ADE, and iOS/iPadOS provisioning.
    ```

    REPLACE with (extends scope to mention Linux + Operations per CONTEXT D-20 mandate; preserves the existing platform list verbatim then extends):
    ```
    > **Platform coverage:** This index covers Windows Autopilot (classic/APv1 and Device Preparation/APv2), macOS ADE, iOS/iPadOS, Android Enterprise, and Linux (Ubuntu LTS) provisioning, plus cross-platform operational depth (co-management, patch & update management, app lifecycle automation, drift detection + tenant migration).
    ```

    Note: The existing line 9 omits "Android Enterprise" too (it predates Phase 42's Android stub). The corrected blockquote MUST mention all 5 platforms PLUS the Operations layer to honor CONTEXT D-20 + the todo §Solution body.

    Line 10 (the trailing blockquote line about APv1 vs APv2 / Windows vs macOS) MUST remain BYTE-IDENTICAL.

    **POST-EDIT VERIFICATION (run as part of task before commit):**
    1. `grep -c "linux-provisioning" docs/index.md` ≥ 2 (jump-link target #linux-provisioning + Phase 59 H2 anchor target — same slug)
    2. `grep -c "#operations" docs/index.md` ≥ 2 (jump-link + Phase 59 H2 anchor)
    3. `grep -c "^- \[" docs/index.md` ≥ 7 (7 bullets total post-fix; was 5 pre-fix)
    4. `grep -c "Linux (Ubuntu LTS)" docs/index.md` ≥ 1 (blockquote update landed)
    5. `node scripts/validation/check-phase-59.mjs --verbose` MUST exit 0 (regression-guard — Phase 59 H2 deliverables byte-identical at lines 199 + 231)
    6. `node scripts/validation/v1.5-milestone-audit.mjs --verbose` MUST exit 0 (12/12 PASS preserved)

    **COMMIT (separate atomic content commit per CONTEXT D-04 + D-20 — DO NOT combine with Task 1's harness commit):**
    Commit message: `docs(61-01): fold Linux + Operations bullets into docs/index.md jump-link list`

    Body should reference: closes pending todo `2026-05-06-choose-your-platform-linux-operations-bullets.md`; CONTEXT D-20 todo fold-in mandate; append-only contract preserved (5 existing bullets + line 10 blockquote trailing line byte-identical).
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-59.mjs && node scripts/validation/v1.5-milestone-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "linux-provisioning" docs/index.md` ≥ 2
    - `grep -c "#operations" docs/index.md` ≥ 2
    - `grep -c "^- \[" docs/index.md` ≥ 7 (7 bullets total in jump-link list)
    - `grep -c "(#linux-provisioning)" docs/index.md` ≥ 1 (jump-link bullet present)
    - `grep -c "(#operations)" docs/index.md` ≥ 1 (jump-link bullet present)
    - `grep -c "intune-portal package, web-app conditional access" docs/index.md` ≥ 1 (verbatim Linux bullet text from todo §Solution)
    - `grep -c "co-management, patch & update management, app lifecycle automation, drift detection + tenant migration" docs/index.md` ≥ 1 (verbatim Operations bullet text from todo §Solution)
    - `grep -c "Linux (Ubuntu LTS)" docs/index.md` ≥ 1 (blockquote update landed)
    - `grep -c "Windows Autopilot](#windows-autopilot)" docs/index.md` ≥ 1 (existing bullet preserved byte-identical)
    - `grep -c "Cross-Platform References](#cross-platform-references)" docs/index.md` ≥ 1 (existing bullet preserved byte-identical)
    - `node scripts/validation/check-phase-59.mjs` exits 0
    - `node scripts/validation/v1.5-milestone-audit.mjs` exits 0
    - `git log -1 --format=%s` matches `docs(61-01): fold Linux + Operations bullets into docs/index.md jump-link list`
    - `git log -2 --format=%s` first line is `docs(61-01)` and second line is `fix(check-phase-53)` (commit ordering per D-04: validator first, content second)
  </acceptance_criteria>
  <done>
    7-bullet jump-link list landed (5 existing byte-identical + 2 new); blockquote at line 9 mentions Linux + Operations; check-phase-59.mjs + v1.5-milestone-audit.mjs both exit 0 (regression-guard satisfied); atomic content commit landed per D-04 separate-commit rule; pending todo can be moved to .planning/todos/done/ in subsequent housekeeping.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Closing/traceability phase — no new attack surface; all edits are static-file (markdown + Node.js validator); no user input, no network calls, no credential handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01-01 | n/a | n/a | accept | severity: none — closing/traceability phase; no new code attack surface; no user input; no external integrations. Edits are read-only file content modifications (Node.js regex check + markdown bullet append). v1.5 close per CONTEXT §domain phase boundary statement. |

</threat_model>

<verification>
**Per CONTEXT D-04 atomic-commit contract + D-20 fold-in mandate:**

1. `node scripts/validation/check-phase-53.mjs --verbose` exits 0 with 26/26 V-53-NN PASS (V-53-06 + V-53-22 refreshed per D-02 + D-03)
2. `node scripts/validation/check-phase-60.mjs --verbose` exits 0 with 25/25 V-60-NN PASS — V-60-16 chain regression-guard flips FAIL→PASS (closes Phase 60 carry-over per 60-VERIFICATION.md:325-345)
3. `node scripts/validation/check-phase-59.mjs --verbose` exits 0 (regression-guard — Phase 59 H2 deliverables byte-identical)
4. `node scripts/validation/v1.5-milestone-audit.mjs --verbose` exits 0 with 12/12 PASS in fully-blocking mode (no regression vs Phase 60 close state)
5. `grep -c "^- \[" docs/index.md` ≥ 7 (todo fold-in landed)
6. `grep -c "linux-provisioning" docs/index.md` ≥ 2 (todo §Verification line 51 satisfied)
7. `grep -c "#operations" docs/index.md` ≥ 2 (todo §Verification line 52 satisfied)
8. 2 atomic commits landed per D-04: `fix(check-phase-53)` (Task 1) + `docs(61-01)` (Task 2)
</verification>

<success_criteria>
- check-phase-53.mjs V-53-06 + V-53-22 refreshed per CONTEXT D-02 + D-03; per-file scope-exemption mechanism preferred (mirrors V-53-10 precedent at lines 149-165); Phase 59 D-10 supersession cited verbatim in name: literals
- check-phase-60.mjs V-60-16 chain regression-guard flips FAIL→PASS post-Plan-61-01 landing
- docs/index.md has 7-bullet Choose Your Platform jump-link list (5 existing byte-identical + Linux Provisioning + Operations from todo §Solution lines 36-37)
- docs/index.md line 9 platform-coverage blockquote mentions Linux + Operations per CONTEXT D-20 mandate
- 2 atomic commits per CONTEXT D-04 — validator commit first (`fix(check-phase-53)`), content commit second (`docs(61-01)`)
- All chain validators (check-phase-{48..60}.mjs) exit 0 post-Plan-61-01 landing — Plan 61-04 fresh-worktree audit will independently confirm this
- No Phase 49-60 V-NN-NN assertion regressions (D-27 mandate)
- Pending todo `2026-05-06-choose-your-platform-linux-operations-bullets.md` is closed (executor SHOULD move to `.planning/todos/done/` post-commit; not strictly enforced as task acceptance criterion since todo housekeeping is not in CONTEXT decision surface)
</success_criteria>

<output>
After completion, create `.planning/phases/61-gap-closure-terminal-re-audit-milestone-close/61-01-SUMMARY.md` per CONTEXT D-23 progressive-landing per Phase 60 D-25 model. SUMMARY MUST include:
- Both commit SHAs (validator + content)
- Pre-edit V-60-16 state (FAIL) and post-edit state (PASS)
- Pre-edit jump-link bullet count (5) and post-edit count (7)
- Confirmation that check-phase-59.mjs + v1.5-milestone-audit.mjs both exit 0 post-Plan-61-01
- Reference to Phase 59 D-10 supersession + V-53-10 SCOPE_RESTRICTED precedent (audit trail)
</output>
