---
phase: 54
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/patch-management/03-ios-update-lifecycle.md
autonomous: true
requirements: [PATCH-05]
requirements_addressed: [PATCH-05]

must_haves:
  truths:
    - "docs/operations/patch-management/03-ios-update-lifecycle.md exists with `platform: iOS` + `audience: admin` + `last_verified: 2026-04-28` + `review_by: 2026-06-27` frontmatter (D-19 + V-54-07)"
    - "File body opens with verbatim `> **Platform applicability:**` blockquote at TOP within first 50 lines (D-04 + V-54-26)"
    - "File contains literal `unsupervised` AND `iOS 17` AND `August 2025` (or `Aug 2025`) AND ≥2 of the 4 DDM update keys (`TargetOSVersion`, `TargetBuildVersion`, `TargetLocalDateTime`, `OfferPrograms`) (V-54-18)"
    - "File contains a single inline `> ⚠️` blockquote at the supervised-only-DDM-retraction site (NOT a full Deadlines H2 per D-12 — single Aug 2025 retraction date is single-callout overhead per D-14)"
    - "File contains explicit retraction prose stating the prior supervised-only constraint has been retracted; DDM update keys work on UNSUPERVISED iOS 17+ devices effective August 2025"
    - "File does NOT contain a `## Deadlines & Cutover Dates` H2 (D-12 — soft cutover; single Aug 2025 retraction; inline callout only)"
    - "File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens (V-54-30)"
    - "File does NOT contain bare `> **Platform:**` token (V-54-27)"
  artifacts:
    - path: "docs/operations/patch-management/03-ios-update-lifecycle.md"
      provides: "iOS DDM update lifecycle guide; supervised-only constraint retraction (Aug 2025) for iOS 17+ unsupervised devices (PATCH-05)"
      contains:
        - "platform: iOS"
        - "audience: admin"
        - "last_verified: 2026-04-28"
        - "review_by: 2026-06-27"
        - "> **Platform applicability:**"
        - "unsupervised"
        - "iOS 17"
        - "August 2025"
        - "TargetOSVersion"
        - "TargetBuildVersion"
        - "TargetLocalDateTime"
        - "OfferPrograms"
        - "DDM"
        - "00-overview.md"
  key_links:
    - from: "docs/operations/patch-management/03-ios-update-lifecycle.md"
      to: "docs/operations/patch-management/00-overview.md"
      via: "Platform applicability blockquote back-link"
      pattern: "00-overview\\.md"
    - from: "docs/operations/patch-management/03-ios-update-lifecycle.md"
      to: "docs/operations/patch-management/01-windows-wufb-rings.md"
      via: "Platform applicability blockquote sibling cross-link"
      pattern: "01-windows-wufb-rings\\.md"
    - from: "docs/operations/patch-management/03-ios-update-lifecycle.md"
      to: "docs/operations/patch-management/02-macos-update-enforcement.md"
      via: "Platform applicability blockquote sibling cross-link (Apple-platform sibling)"
      pattern: "02-macos-update-enforcement\\.md"
    - from: "docs/operations/patch-management/03-ios-update-lifecycle.md"
      to: "docs/operations/patch-management/04-android-patch-delivery.md"
      via: "Platform applicability blockquote sibling cross-link"
      pattern: "04-android-patch-delivery\\.md"
---

<objective>
Author `docs/operations/patch-management/03-ios-update-lifecycle.md` — the iOS-specific patch management guide covering PATCH-05: DDM update keys (`TargetOSVersion`, `TargetBuildVersion`, `TargetLocalDateTime`, `OfferPrograms`) work on UNSUPERVISED iOS 17+ devices effective August 2025; supervised-only constraint retracted explicitly. NO Deadlines H2 (single Aug 2025 retraction date is single-callout overhead per D-14); single `> ⚠️` inline blockquote at the supervised-only retraction site.

Purpose: Implements CONTEXT D-04 (cross-platform inline blockquote pattern at TOP) + D-12 (NO Deadlines H2 — soft cutover only; single inline `> ⚠️` callout) + D-14 (soft cutover items get single-callout treatment, not three-layer) + D-19 (`platform: iOS`) + CD-10 (callout framing plan-author discretion within V-54-18 token coverage). Validator V-54-18/26/27/30 enforce structural correctness.

This file maps to PATCH-05 per REQ traceability line 165. Wave 1 — parallel with 54-01 / 54-02 / 54-03 / 54-05 / 54-09.

**CRITICAL — same-commit-atomic with PATCH-06 retrofit (PLAN 54-06)** per ROADMAP line 271 + RETROSPECTIVE.md line 166 v1.4.1 atomicity lesson. The validator's V-54-19 NEGATIVE+POSITIVE retrofit-cell-text pair MECHANICALLY enforces single-commit landing: this file must exist (POSITIVE half cross-link target) AND `07-device-enrollment.md:35` cell must be edited (NEGATIVE half pre-edit text absent). Splitting the commit fails V-54-19 in commit-1 regardless of order.

Output: 1 new file at `docs/operations/patch-management/03-ios-update-lifecycle.md`.

DO NOT commit yet — atomic commit owned by 54-09 author per CONTEXT D-21 + CDI-Phase54-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-patch-update-management/54-CONTEXT.md
@.planning/phases/54-patch-update-management/54-RESEARCH.md
@.planning/phases/54-patch-update-management/54-PATTERNS.md
@.planning/phases/54-patch-update-management/54-VALIDATION.md
@docs/admin-setup-ios/06-compliance-policy.md
@docs/admin-setup-ios/04-configuration-profiles.md

<interfaces>
**File this plan creates:** `docs/operations/patch-management/03-ios-update-lifecycle.md`

**Frontmatter contract (D-19 verbatim):**
```yaml
---
last_verified: 2026-04-28
review_by: 2026-06-27
applies_to: all
audience: admin
platform: iOS
---
```

**Validator V-54-NN assertions enforced against this file:**

| V-NN | Assertion |
|------|-----------|
| V-54-04 | File exists |
| V-54-07 | Frontmatter: `platform: iOS`, `audience: admin`, 60-day cycle |
| V-54-18 | Literal coverage: `unsupervised` AND `iOS 17` AND (`August 2025` OR `Aug 2025`) AND ≥2 of {`TargetOSVersion`, `TargetBuildVersion`, `TargetLocalDateTime`, `OfferPrograms`} |
| V-54-26 | `> **Platform applicability:**` blockquote within first 50 lines |
| V-54-27 | NEGATIVE: NO bare `> **Platform:**` token |
| V-54-30 | NEGATIVE: NO TBD/TODO/FIXME/XXX/PLACEHOLDER |

**Cross-commit dependency (CDI-Phase54-05):** This file's existence is the POSITIVE half of V-54-19 retrofit pair (54-06 retrofits `docs/admin-setup-ios/07-device-enrollment.md:35` with cross-link to this file `../../operations/patch-management/03-ios-update-lifecycle.md`). Single atomic commit FORCES atomic landing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/operations/patch-management/03-ios-update-lifecycle.md (frontmatter + Platform applicability blockquote + H1 + iOS Update Enforcement Model H2 + DDM Update Keys H2 + Aug 2025 supervised-only retraction inline callout + Supervision Matrix H2 + Related Resources)</name>
  <files>docs/operations/patch-management/03-ios-update-lifecycle.md</files>
  <read_first>
    - .planning/phases/54-patch-update-management/54-CONTEXT.md (D-04 cross-platform inline blockquote; D-12 NO Deadlines H2 for iOS; D-14 single inline callout for soft cutover; D-19 platform: iOS; CD-10 callout framing discretion within V-54-18 coverage)
    - .planning/phases/54-patch-update-management/54-RESEARCH.md (DDM update keys + Aug 2025 unsupervised retraction details; supervised vs unsupervised matrix)
    - .planning/phases/54-patch-update-management/54-PATTERNS.md (Section "03-ios-update-lifecycle.md" lines 265-301 — paste-ready snippets including Platform applicability blockquote, single-inline `> ⚠️` callout pattern with August 2025 retraction text, V-54-18 token coverage requirements)
    - .planning/phases/54-patch-update-management/54-VALIDATION.md (Per-Task Verification Map row 54-04-01)
    - docs/admin-setup-ios/06-compliance-policy.md (iOS admin tone analog — frontmatter shape lines 1-7; Compliance-vs-Configuration H2 narrative shape)
    - docs/admin-setup-ios/04-configuration-profiles.md (line 128 — DDM-on-unsupervised retraction precedent prose; "works on both supervised and unsupervised devices (iOS 17.0+)" — this is the existing source-of-truth statement that 54-04 expands into a full guide)
  </read_first>
  <behavior>
    - File `docs/operations/patch-management/03-ios-update-lifecycle.md` exists
    - Frontmatter (D-19): `platform: iOS`, `audience: admin`, 60-day cycle dates
    - Platform applicability blockquote at TOP routing to 00-overview + 3 sibling per-platform files
    - H1 followed by intro paragraph
    - iOS Update Enforcement Model H2 explaining DDM-as-canonical iOS 17+ enforcement path
    - DDM Update Keys H2 enumerating all 4 keys (`TargetOSVersion`, `TargetBuildVersion`, `TargetLocalDateTime`, `OfferPrograms`) with semantic explanation
    - Single `> ⚠️` inline blockquote at the supervised-only-DDM-retraction site containing literal `unsupervised` AND `iOS 17` AND (`August 2025` OR `Aug 2025`) AND ≥2 DDM update keys (V-54-18 token coverage requirement)
    - Explicit retraction prose stating the prior supervised-only constraint has been retracted
    - Supervision Matrix H2 distinguishing supervised vs unsupervised behavior pre-Aug-2025 vs post-Aug-2025
    - Related Resources + External References footer
    - NO `## Deadlines & Cutover Dates` H2 (D-12 — single inline callout sufficient)
    - NO bare `> **Platform:**` token
    - NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens
  </behavior>
  <action>
Create the file `docs/operations/patch-management/03-ios-update-lifecycle.md` with the following content sections IN ORDER.

**Section 1 — Frontmatter (D-19):**

```yaml
---
last_verified: 2026-04-28
review_by: 2026-06-27
applies_to: all
audience: admin
platform: iOS
---
```

**Section 2 — Platform applicability blockquote at TOP (D-04 pointer-only; paste verbatim from 54-PATTERNS.md lines 282-289):**

```markdown
> **Platform applicability:** This guide is iOS/iPadOS-specific (DDM update policies;
> unsupervised iOS 17+ retraction effective Aug 2025). For the cross-platform overview, see
> [Patch Management Overview](00-overview.md).
> **Windows:** See [Windows WUfB Rings](01-windows-wufb-rings.md).
> **macOS:** See [macOS DDM Update Enforcement](02-macos-update-enforcement.md).
> **Android:** See [Android Patch Delivery](04-android-patch-delivery.md).
```

**Section 3 — H1 + intro paragraph:**

```markdown
# iOS Update Lifecycle: DDM-Based Enforcement

This guide is the iOS/iPadOS-specific patch and update lifecycle reference. It covers the
**Declarative Device Management (DDM)** update keys configured via Intune Apple update policies
(Intune > Devices > Apple updates > iOS/iPadOS update policies) and the August 2025 retraction
of the prior supervised-only-DDM constraint — DDM update enforcement now works on **unsupervised**
iOS 17+ devices.

For the cross-platform comparison, see [Patch Management Overview](00-overview.md). For macOS
DDM (the related Apple platform), see [macOS DDM Update Enforcement](02-macos-update-enforcement.md).
```

**Section 4 — iOS Update Enforcement Model H2:**

```markdown
## iOS Update Enforcement Model

iOS update enforcement uses **DDM (Declarative Device Management)** as the canonical mechanism for
iOS 17 and later. The DDM update model differs from legacy MDM update commands:

- **Declarative:** The MDM server (Intune) declares the target version + enforcement deadline; the
  OS itself enforces installation. The server does NOT push an explicit install command per device.
- **Per-device assertion:** Each enrolled device receives a DDM update assertion describing the
  required iOS version and deadline.
- **OS-managed user notification:** The OS displays update prompts to the user per the DDM
  assertion's grace-period configuration. The user cannot defer past the declared deadline.

Configuration path: **Intune > Devices > Apple updates > iOS/iPadOS update policies > Create**.
Profile uses the DDM update keys enumerated in [DDM Update Keys](#ddm-update-keys) below.

**Eligibility:** iOS 17.0 and later. Earlier iOS versions cannot receive DDM update enforcement;
they continue to use legacy MDM device-restriction "Defer software updates" (supervised-only) and
the legacy `ScheduleOSUpdate` MDM command. Both legacy mechanisms are being deprecated by Apple.
```

**Section 5 — DDM Update Keys H2 (V-54-18 — must contain ≥2 of the 4 DDM keys):**

```markdown
## DDM Update Keys {#ddm-update-keys}

The DDM iOS update assertion uses four basic keys to declare the enforcement target:

| Key | Type | Description |
|-----|------|-------------|
| `TargetOSVersion` | string | Required iOS version (e.g. `18.2`). The device must reach this version by `TargetLocalDateTime`. |
| `TargetBuildVersion` | string | Required build version (e.g. `22C152`). Used when a specific build is required (rare; typically used for security-fix tracking). Optional if `TargetOSVersion` is present. |
| `TargetLocalDateTime` | string (ISO 8601 local) | Hard deadline for the device to be on `TargetOSVersion`. Format: `YYYY-MM-DDTHH:MM:SS` (device-local time). |
| `OfferPrograms` | array | Optional. Lists update programs offered to the user (developer beta, public beta, etc.). Default empty (production updates only). |

The Intune Apple update policy blade exposes these as form fields. The underlying DDM assertion
JSON shows the four keys directly when inspected via Intune Configuration Audit logs.

**Minimum required keys:** `TargetOSVersion` + `TargetLocalDateTime`. The other two are optional
qualifiers.
```

**Section 6 — Aug 2025 supervised-only retraction inline callout (D-12 + D-14 single inline; V-54-18 token coverage; CD-10 framing discretion — this implementation uses the "August 2025 retraction" framing from 54-PATTERNS.md lines 293-297):**

```markdown
## Supervised-Only Retraction (Effective August 2025)

The DDM iOS update enforcement model originally constrained DDM update keys to **supervised**
devices only (Apple Configurator, Apple Business Manager / Apple School Manager auto-enrolled
devices). That constraint has been retracted as of August 2025.

> ⚠️ **August 2025 retraction:** As of August 2025, the basic DDM update keys (TargetOSVersion,
> TargetBuildVersion, TargetLocalDateTime, OfferPrograms) work on **unsupervised** iOS 17+
> devices — the prior supervised-only constraint has been retracted. Both ADE-supervised and
> Device-Enrollment-unsupervised iOS 17+ devices can now receive DDM update enforcement.

**Implications for BYOD fleets:** Tenants enrolling iOS devices via User Enrollment (BYOD) or
Device Enrollment (corporate-owned non-supervised) can now apply DDM update enforcement —
previously they would have been gated on the supervised-only constraint and forced to use legacy
MDM `ScheduleOSUpdate` (now deprecated).

**Implications for documentation:** This guide formally retracts the v1.3 supervised-only-DDM
callout that previously appeared in `docs/admin-setup-ios/07-device-enrollment.md:35` (now
corrected to reflect DDM-on-unsupervised). Cross-references from iOS admin-setup content forward
here as the canonical iOS update enforcement reference.
```

The blockquote in this section satisfies V-54-18 token coverage requirement: `unsupervised` ✅, `iOS 17` ✅, `August 2025` ✅, all 4 DDM update keys (`TargetOSVersion`, `TargetBuildVersion`, `TargetLocalDateTime`, `OfferPrograms`) ✅.

**Section 7 — Supervision Matrix H2:**

```markdown
## Supervision Matrix: DDM Update Enforcement

| Enrollment Type | Supervision | Pre-Aug-2025 | Post-Aug-2025 |
|-----------------|-------------|--------------|---------------|
| ADE / Apple Business Manager | Supervised | ✅ DDM update enforcement | ✅ DDM update enforcement |
| ADE / Apple School Manager | Supervised | ✅ DDM update enforcement | ✅ DDM update enforcement |
| Device Enrollment (corporate, non-supervised) | Unsupervised | ❌ DDM blocked | ✅ DDM update enforcement |
| User Enrollment (BYOD) | Unsupervised | ❌ DDM blocked | ✅ DDM update enforcement |
| Account-driven Device Enrollment | Unsupervised | ❌ DDM blocked | ✅ DDM update enforcement |

**Forward-compatibility note:** The supervised-only constraint retraction is part of Apple's
broader move toward DDM as the canonical update enforcement primitive. Tenants planning long-term
update enforcement should standardize on DDM regardless of supervision state. Legacy MDM
`ScheduleOSUpdate` (the pre-DDM enforcement primitive) is being deprecated and may be removed in
future iOS releases.
```

**Section 8 — Rollout Patterns H2 (admin-actionable iOS update rollout guidance):**

```markdown
## Rollout Patterns

Admins typically deploy iOS DDM update policies via Intune in three rollout stages:

1. **Pilot ring (devices in pilot Azure AD group):** TargetLocalDateTime = +14 days from policy
   assignment. Validate update success rate before promotion.
2. **Broad ring (production fleet):** TargetLocalDateTime = +30 days from major iOS release.
   Standard cadence for non-security-critical updates.
3. **Critical ring (security-driven):** TargetLocalDateTime = +7 days. Used when Apple ships an
   urgent security update (RSR — Rapid Security Response — for example).

Pre-Aug-2025 these rings could only contain supervised devices. Post-Aug-2025 the same rings can
include unsupervised BYOD and corporate-non-supervised devices. The DDM assertion does not
distinguish supervision state in the assertion payload itself.
```

**Section 9 — Related Resources + External References footer:**

```markdown
## Related Resources

- [Patch Management Overview](00-overview.md) — Cross-platform comparison + Ring Terminology hub
- [Windows WUfB Rings](01-windows-wufb-rings.md) — Windows forward path
- [macOS DDM Update Enforcement](02-macos-update-enforcement.md) — Related Apple-platform DDM guide
- [Android Patch Delivery](04-android-patch-delivery.md) — Android attestation gate

## External References

- [Software Updates Guide for iOS/iPadOS (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/intune-service/protect/software-updates-guide-ios-ipados)
- [iOS Device Restrictions (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/intune-service/protect/device-restrictions-ios)
- [DDM Software Update (Apple Developer)](https://developer.apple.com/documentation/devicemanagement/softwareupdateenforcement)
- [Apple Platform Deployment — Software Update](https://support.apple.com/guide/deployment/software-update-management-depc4c80847a/web)
```

After authoring, verify with grep:

```bash
grep -c "platform: iOS" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect 1
grep -c "^> \*\*Platform applicability:\*\*" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect 1
grep -c "unsupervised" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect >=2
grep -c "iOS 17" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect >=2
grep -cE "August 2025|Aug 2025" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect >=1
grep -c "TargetOSVersion" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect >=2
grep -c "TargetBuildVersion" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect >=2
grep -c "TargetLocalDateTime" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect >=2
grep -c "OfferPrograms" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect >=2
grep -c "DDM" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect >=5
grep -c "^## Deadlines" docs/operations/patch-management/03-ios-update-lifecycle.md  # MUST be 0 (D-12 NO Deadlines H2)
grep -c "^> \*\*Platform:\*\*" docs/operations/patch-management/03-ios-update-lifecycle.md  # MUST be 0
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/patch-management/03-ios-update-lifecycle.md  # MUST be empty
```

DO NOT commit yet — atomic commit owned by 54-09 author per CONTEXT D-21 + CDI-Phase54-05.
  </action>
  <verify>
    <automated>test -f docs/operations/patch-management/03-ios-update-lifecycle.md &amp;&amp; grep -q "unsupervised" docs/operations/patch-management/03-ios-update-lifecycle.md &amp;&amp; grep -q "iOS 17" docs/operations/patch-management/03-ios-update-lifecycle.md &amp;&amp; grep -qE "August 2025|Aug 2025" docs/operations/patch-management/03-ios-update-lifecycle.md &amp;&amp; grep -q "TargetOSVersion" docs/operations/patch-management/03-ios-update-lifecycle.md &amp;&amp; grep -q "TargetLocalDateTime" docs/operations/patch-management/03-ios-update-lifecycle.md &amp;&amp; grep -q "platform: iOS" docs/operations/patch-management/03-ios-update-lifecycle.md &amp;&amp; ! grep -q "^## Deadlines" docs/operations/patch-management/03-ios-update-lifecycle.md &amp;&amp; ! grep -q "^> \*\*Platform:\*\*" docs/operations/patch-management/03-ios-update-lifecycle.md &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/operations/patch-management/03-ios-update-lifecycle.md` exists
    - File contains frontmatter with `platform: iOS`, `audience: admin`, `last_verified: 2026-04-28`, `review_by: 2026-06-27`, `applies_to: all`
    - File contains literal `> **Platform applicability:**` blockquote within first 50 lines of body
    - File contains literal `unsupervised` AND `iOS 17` AND (`August 2025` OR `Aug 2025`) (V-54-18)
    - File contains ≥2 of the 4 DDM update keys: `TargetOSVersion`, `TargetBuildVersion`, `TargetLocalDateTime`, `OfferPrograms` (V-54-18) — this implementation includes all 4
    - File contains a `> ⚠️` blockquote at the supervised-only-DDM-retraction site (Section 6)
    - File contains explicit retraction prose stating the prior supervised-only constraint has been retracted
    - File does NOT contain literal `## Deadlines & Cutover Dates` H2 (D-12 — single inline callout sufficient)
    - File does NOT contain bare `> **Platform:**` token (V-54-27)
    - File does NOT contain any of: `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` (V-54-30)
    - File contains cross-link to 00-overview.md AND each of the 3 sibling per-platform files
  </acceptance_criteria>
  <done>03-ios-update-lifecycle.md authored with all D-04/D-12/D-14/D-19 surfaces; PATCH-05 covered with single-inline callout; V-54-18/26/27/30 assertions for this file pass; no commit (54-09 owns atomicity).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → reader | Documentation content; reader trust depends on factual Aug 2025 retraction date + correct DDM key enumeration |
| Atomic commit boundary (CDI-Phase54-05) | This file's existence is the POSITIVE half of V-54-19 retrofit pair; cross-link target for `07-device-enrollment.md:35` retrofit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04-01 | Tampering (V-54-18 token coverage) | Author writes only `unsupervised` + `iOS 17` but skips August 2025 date or DDM keys → V-54-18 fails | mitigate | Section 6 blockquote contains all required tokens explicitly; Section 5 DDM Update Keys H2 enumerates all 4 keys |
| T-54-04-02 | Tampering (atomicity violation with 54-06) | This file ships in commit-1 but `07-device-enrollment.md:35` retrofit ships in commit-2 → V-54-19 NEGATIVE half fails (pre-edit text still present) | mitigate | Single atomic commit per CONTEXT D-21 — 54-09 author orchestrates atomic commit covering all 9 plans; CDI-Phase54-05 mechanical enforcement |
| T-54-04-03 | Tampering (factual drift Aug 2025 retraction) | Apple may rescind retraction or supervised constraint may resurface in later iOS | accept | 60-day `review_by` cycle; cite Microsoft Learn updated 2026-01-29 (research SUMMARY 2026-04-15) |
| T-54-04-04 | Information disclosure (admins still treating DDM as supervised-only) | Old documentation patterns + tribal knowledge cause admins to bypass DDM in BYOD fleets | mitigate | Explicit retraction prose Section 6; Supervision Matrix H2 Section 7 distinguishes pre-/post-Aug-2025 cells; D-08 forward-link from `04-configuration-profiles.md:128` (54-07) reinforces |

Severity: LOW. Documentation phase; no application security surface introduced. Atomicity violation is the only non-LOW risk, mitigated by single-commit enforcement.
</threat_model>

<verification>
After Task 1 completes:

```bash
test -f docs/operations/patch-management/03-ios-update-lifecycle.md && echo "exists: PASS"

head -7 docs/operations/patch-management/03-ios-update-lifecycle.md | grep -E "^(platform|audience|last_verified|review_by|applies_to):"

sed -n '/^---$/,/^---$/!p' docs/operations/patch-management/03-ios-update-lifecycle.md | head -50 | grep -c "^> \*\*Platform applicability:\*\*"
# expect 1

# V-54-18 token coverage
for tok in "unsupervised" "iOS 17" "TargetOSVersion" "TargetBuildVersion" "TargetLocalDateTime" "OfferPrograms"; do
  count=$(grep -c "$tok" docs/operations/patch-management/03-ios-update-lifecycle.md)
  echo "$tok: $count"
done
grep -cE "August 2025|Aug 2025" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect >=1

# D-12 NO Deadlines H2
! grep -q "^## Deadlines" docs/operations/patch-management/03-ios-update-lifecycle.md && echo "no Deadlines H2 (D-12): PASS"

# V-54-27 NEGATIVE
grep -c "^> \*\*Platform:\*\*" docs/operations/patch-management/03-ios-update-lifecycle.md  # expect 0

# V-54-30 TBD scan
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/patch-management/03-ios-update-lifecycle.md
# expect empty
```

If 54-09 validator is also authored at this point:

```bash
node scripts/validation/check-phase-54.mjs --verbose 2>&1 | grep "V-54-0[47]\|V-54-18\|V-54-26\|V-54-27\|V-54-30"
# All file-specific assertions for 03-ios-update-lifecycle.md should PASS
```

DO NOT commit — atomic commit owned by 54-09.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 54 SC#3 (first conjunct) + REQUIREMENTS.md PATCH-05:

- **SC#3 first conjunct** (admin reading 03-ios-update-lifecycle.md sees supervised-only DDM constraint retracted for iOS 17+ effective Aug 2025; explicit DDM-on-unsupervised statement): Fully covered in Section 6
- **SC#3 second conjunct** (v1.3 supervised-only-DDM callout surgically retrofitted same-commit): Owned by 54-06 (PATCH-06 retrofit at `07-device-enrollment.md:35`); atomic commit ensures both ship together
- **PATCH-05** (DDM update keys + iOS 17+ unsupervised + Aug 2025): Fully covered

Plan complete when:

- [ ] File exists at `docs/operations/patch-management/03-ios-update-lifecycle.md`
- [ ] V-54-04 (file exists), V-54-07 (frontmatter), V-54-18 (token coverage), V-54-26 (blockquote at TOP), V-54-27 (no bare-Platform), V-54-30 (TBD scan) all pass
- [ ] All 4 DDM update keys enumerated in Section 5 table
- [ ] Single inline `> ⚠️` callout at retraction site (Section 6); NO Deadlines H2
- [ ] No commit — 54-09 owns atomicity per CONTEXT D-21
</success_criteria>

<output>
After completion, create `.planning/phases/54-patch-update-management/54-04-SUMMARY.md` documenting:

- File path + line count
- Sections authored (frontmatter, Platform applicability blockquote, H1 + intro, iOS Update Enforcement Model, DDM Update Keys, Supervised-Only Retraction with inline callout, Supervision Matrix, Rollout Patterns, Related Resources, External References)
- V-54-NN assertions this file satisfies (V-54-04, V-54-07, V-54-18, V-54-26, V-54-27, V-54-30)
- PATCH-05 traceability + SC#3 first conjunct satisfaction
- Atomic commit dependency note: same-commit-atomic with 54-06 (V-54-19 retrofit pair coupling)
- Note: NO COMMIT — atomic commit owned by 54-09
</output>
</content>
</invoke>