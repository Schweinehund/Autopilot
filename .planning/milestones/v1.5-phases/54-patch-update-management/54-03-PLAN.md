---
phase: 54
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/patch-management/02-macos-update-enforcement.md
autonomous: true
requirements: [PATCH-04]
requirements_addressed: [PATCH-04]

must_haves:
  truths:
    - "docs/operations/patch-management/02-macos-update-enforcement.md exists with `platform: macOS` + `audience: admin` + `last_verified: 2026-04-28` + `review_by: 2026-06-27` frontmatter (D-19 + V-54-07)"
    - "File body opens with verbatim `> **Platform applicability:**` blockquote at TOP within first 50 lines (D-04 + V-54-26)"
    - "File contains literal H2 `## Deadlines & Cutover Dates` (D-12 — macOS gets dedicated Deadlines H2 because Apple OS 26 is HARD-DEADLINE) (V-54-14)"
    - "File contains HARD-DEADLINE three-layer Layer 1: legacy-MDM-command table row containing literal `forceDelayedSoftwareUpdates` AND literal `**[HARD-DEADLINE]**` (V-54-14)"
    - "File contains HARD-DEADLINE three-layer Layer 2: literal `> ⚠️ **Hard deadline (Apple OS 26):**` blockquote with all 3 legacy-command tokens (`forceDelayedSoftwareUpdates`, `com.apple.SoftwareUpdate`, `ScheduleOSUpdate`) AND `DDM` AND `Apple OS 26` within blockquote (V-54-15)"
    - "File contains HARD-DEADLINE three-layer Layer 3: ≥2 per-occurrence `[HARD-DEADLINE` inline reminders adjacent to legacy-command token mentions (V-54-16). Note: total `[HARD-DEADLINE` token count must be ≥5 (3 in Layer 1 table cells + ≥2 in Layer 3 inline) per validator algorithm in PATTERNS.md"
    - "File contains DDM-only literal coverage: `Software Update Enforce Latest`, `Intune Settings Catalog`, `DDM`, `forward-compatible` (or equivalent) (V-54-17)"
    - "File distinguishes deferral (MDM restriction for delay still functional pre-OS-26) from enforcement (DDM only forward) per CONTEXT deliverable 3"
    - "File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens (V-54-30)"
    - "File does NOT contain bare `> **Platform:**` token (V-54-27)"
  artifacts:
    - path: "docs/operations/patch-management/02-macos-update-enforcement.md"
      provides: "macOS DDM 'Software Update Enforce Latest' enforcement guide + Apple OS 26 legacy-MDM-command HARD-DEADLINE three-layer (PATCH-04)"
      contains:
        - "platform: macOS"
        - "audience: admin"
        - "last_verified: 2026-04-28"
        - "review_by: 2026-06-27"
        - "> **Platform applicability:**"
        - "## Deadlines & Cutover Dates"
        - "**[HARD-DEADLINE]**"
        - "> ⚠️ **Hard deadline (Apple OS 26):**"
        - "[HARD-DEADLINE — see Deadlines H2]"
        - "forceDelayedSoftwareUpdates"
        - "com.apple.SoftwareUpdate"
        - "ScheduleOSUpdate"
        - "Software Update Enforce Latest"
        - "Intune Settings Catalog"
        - "DDM"
        - "Apple OS 26"
        - "forward-compatible"
        - "00-overview.md"
  key_links:
    - from: "docs/operations/patch-management/02-macos-update-enforcement.md"
      to: "docs/operations/patch-management/00-overview.md"
      via: "Platform applicability blockquote back-link"
      pattern: "00-overview\\.md"
    - from: "docs/operations/patch-management/02-macos-update-enforcement.md"
      to: "docs/operations/patch-management/01-windows-wufb-rings.md"
      via: "Platform applicability blockquote sibling cross-link"
      pattern: "01-windows-wufb-rings\\.md"
    - from: "docs/operations/patch-management/02-macos-update-enforcement.md"
      to: "docs/operations/patch-management/03-ios-update-lifecycle.md"
      via: "Platform applicability blockquote sibling cross-link"
      pattern: "03-ios-update-lifecycle\\.md"
    - from: "docs/operations/patch-management/02-macos-update-enforcement.md"
      to: "docs/operations/patch-management/04-android-patch-delivery.md"
      via: "Platform applicability blockquote sibling cross-link"
      pattern: "04-android-patch-delivery\\.md"
---

<objective>
Author `docs/operations/patch-management/02-macos-update-enforcement.md` — the macOS-specific patch management guide covering PATCH-04: DDM "Software Update Enforce Latest" in Intune Settings Catalog as the only forward-compatible enforcement path; legacy MDM commands (`forceDelayedSoftwareUpdates`, `com.apple.SoftwareUpdate` payload, `ScheduleOSUpdate`) deprecated AND removed with Apple OS 26 — distinguish deferral (MDM restriction for delay still functional pre-OS-26) from enforcement (DDM only, forward).

Purpose: Implements CONTEXT D-04 (cross-platform inline blockquote) + D-12 (YES dedicated `## Deadlines & Cutover Dates` H2 for macOS — content density warrants Apple OS 26 HARD-DEADLINE) + D-13 (HARD-DEADLINE three-layer for Apple OS 26 removal: Layer 1 table-cell + Layer 2 `> ⚠️` blockquote + Layer 3 ≥2 per-occurrence inline reminders) + D-19 (`platform: macOS`) + D-20 (pinned H2 `## Deadlines & Cutover Dates`). Validator V-54-14/15/16/17/26/27/30 enforce structural correctness.

This file maps to PATCH-04 per REQ traceability line 164. Wave 1 — parallel with 54-01 / 54-02 / 54-04 / 54-05 / 54-09.

**HARD-DEADLINE three-layer pattern is INHERITED from Phase 53 D-05 EP HIGH-RISK three-layer** (CDI-Phase54-02 + DPO-Phase52-07 cross-domain transferability endorsement). Token swaps from Phase 53 analog:
- `**HIGH-RISK**` → `**[HARD-DEADLINE]**` (square brackets)
- `> ⚠️ **Endpoint Protection HIGH-RISK:**` → `> ⚠️ **Hard deadline (Apple OS 26):**`
- `[HIGH-RISK — see callout above]` → `[HARD-DEADLINE — see Deadlines H2]`

Output: 1 new file at `docs/operations/patch-management/02-macos-update-enforcement.md`.

DO NOT commit yet — atomic commit owned by 54-09 author per CONTEXT D-21 + CDI-Phase54-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-patch-update-management/54-CONTEXT.md
@.planning/phases/54-patch-update-management/54-RESEARCH.md
@.planning/phases/54-patch-update-management/54-PATTERNS.md
@.planning/phases/54-patch-update-management/54-VALIDATION.md
@docs/operations/co-management/02-windows-workload-sliders.md
@docs/admin-setup-macos/05-compliance-policy.md

<interfaces>
**File this plan creates:** `docs/operations/patch-management/02-macos-update-enforcement.md`

**Frontmatter contract (D-19 verbatim):**
```yaml
---
last_verified: 2026-04-28
review_by: 2026-06-27
applies_to: all
audience: admin
platform: macOS
---
```

**HARD-DEADLINE three-layer literal forms (inheritance from Phase 53 D-05 + token swap per CDI-Phase54-02):**

| Layer | Form |
|-------|------|
| Layer 1 (table cell) | `**[HARD-DEADLINE]** — see callout` |
| Layer 2 (blockquote) | `> ⚠️ **Hard deadline (Apple OS 26):** ...` |
| Layer 3 (inline reminder) | `[HARD-DEADLINE — see Deadlines H2]` |

**Validator V-54-NN assertions enforced against this file:**

| V-NN | Assertion |
|------|-----------|
| V-54-03 | File exists |
| V-54-07 | Frontmatter: `platform: macOS`, `audience: admin`, 60-day cycle |
| V-54-14 | Literal H2 `## Deadlines & Cutover Dates` AND `forceDelayedSoftwareUpdates` table row contains `**[HARD-DEADLINE]**` |
| V-54-15 | Literal `> ⚠️ **Hard deadline (Apple OS 26):**` blockquote with `forceDelayedSoftwareUpdates`, `com.apple.SoftwareUpdate`, `ScheduleOSUpdate`, `DDM`, `Apple OS 26` tokens within blockquote |
| V-54-16 | ≥2 per-occurrence inline `[HARD-DEADLINE` reminders (total `[HARD-DEADLINE` count ≥5: 3 in Layer 1 table + ≥2 in Layer 3 inline) |
| V-54-17 | DDM-only literals: `Software Update Enforce Latest`, `Intune Settings Catalog`, `DDM`, `forward-compatible` (or equivalent) |
| V-54-26 | `> **Platform applicability:**` blockquote within first 50 lines |
| V-54-27 | NEGATIVE: NO bare `> **Platform:**` token |
| V-54-30 | NEGATIVE: NO TBD/TODO/FIXME/XXX/PLACEHOLDER |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/operations/patch-management/02-macos-update-enforcement.md (frontmatter + Platform applicability blockquote + H1 + DDM Software Update Enforcement H2 + Deferral vs Enforcement clarification + Deadlines & Cutover Dates H2 with HARD-DEADLINE three-layer Layers 1+2 + Deprecation Migration H2 with Layer 3 inline reminders + Related Resources)</name>
  <files>docs/operations/patch-management/02-macos-update-enforcement.md</files>
  <read_first>
    - .planning/phases/54-patch-update-management/54-CONTEXT.md (D-04 cross-platform inline blockquote; D-12 YES Deadlines H2 for macOS; D-13 three-layer Layer 1/2/3 contract verbatim text; D-19 platform: macOS; CDI-Phase54-02 three-layer inheritance from Phase 53 D-05; specifics section polarity transfer rationale)
    - .planning/phases/54-patch-update-management/54-RESEARCH.md (Apple OS 26 deprecation timeline + DDM Software Update Enforce Latest details + Intune Settings Catalog path verification)
    - .planning/phases/54-patch-update-management/54-PATTERNS.md (Section "02-macos-update-enforcement.md" lines 201-263 — paste-ready snippets including frontmatter, Platform applicability blockquote, three-layer Layer 1/2/3 patterns with verbatim Layer 2 text, DDM-only literal-coverage pattern)
    - .planning/phases/54-patch-update-management/54-VALIDATION.md (Per-Task Verification Map row 54-03-01: V-54-14/15/16/17 three-layer + DDM literals)
    - docs/operations/co-management/02-windows-workload-sliders.md (Phase 53 EP HIGH-RISK three-layer analog — Layer 1 table-cell + Layer 2 `> ⚠️` blockquote + Layer 3 inline reminders; CDI-Phase54-02 inheritance source)
    - docs/admin-setup-macos/05-compliance-policy.md (macOS admin tone analog — frontmatter shape lines 1-7; intro paragraph + H2 structure)
  </read_first>
  <behavior>
    - File `docs/operations/patch-management/02-macos-update-enforcement.md` exists
    - Frontmatter (D-19): `platform: macOS`, `audience: admin`, 60-day cycle dates
    - Platform applicability blockquote at TOP routing to 00-overview + 3 sibling per-platform files
    - H1 followed by intro paragraph
    - DDM Software Update Enforcement H2 with all 4 V-54-17 literals (`Software Update Enforce Latest`, `Intune Settings Catalog`, `DDM`, `forward-compatible`)
    - Deferral vs Enforcement clarification (MDM restriction for delay still functional pre-OS-26 vs DDM enforcement only forward)
    - `## Deadlines & Cutover Dates` H2 (literal exact match) containing legacy-MDM-command table with `forceDelayedSoftwareUpdates`, `com.apple.SoftwareUpdate`, `ScheduleOSUpdate` rows, each with `**[HARD-DEADLINE]** — see callout` Layer 1 marker (V-54-14)
    - Layer 2 `> ⚠️ **Hard deadline (Apple OS 26):**` blockquote immediately below the legacy-command table; blockquote contains all 3 legacy-command tokens + `DDM` + `Apple OS 26` (V-54-15)
    - Layer 3 ≥2 per-occurrence inline `[HARD-DEADLINE — see Deadlines H2]` reminders elsewhere in body adjacent to legacy-command token mentions (V-54-16)
    - Total `[HARD-DEADLINE` token count ≥5 (3 from Layer 1 table cells + ≥2 from Layer 3 inline)
    - Related Resources + External References footer
    - NO bare `> **Platform:**` token
    - NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens
  </behavior>
  <action>
Create the file `docs/operations/patch-management/02-macos-update-enforcement.md` with the following content sections IN ORDER.

**Section 1 — Frontmatter (D-19):**

```yaml
---
last_verified: 2026-04-28
review_by: 2026-06-27
applies_to: all
audience: admin
platform: macOS
---
```

**Section 2 — Platform applicability blockquote at TOP (D-04 pointer-only; paste verbatim from 54-PATTERNS.md lines 218-225):**

```markdown
> **Platform applicability:** This guide is macOS-specific (DDM "Software Update Enforce Latest"
> in Intune Settings Catalog; legacy MDM command deprecation + Apple OS 26 removal). For the
> cross-platform overview, see [Patch Management Overview](00-overview.md).
> **Windows:** See [Windows WUfB Rings](01-windows-wufb-rings.md).
> **iOS/iPadOS:** See [iOS Update Lifecycle](03-ios-update-lifecycle.md).
> **Android:** See [Android Patch Delivery](04-android-patch-delivery.md).
```

**Section 3 — H1 + intro paragraph:**

```markdown
# macOS Update Enforcement: DDM Software Update Enforce Latest

This guide is the macOS-specific patch and update enforcement reference. It covers the
**Declarative Device Management (DDM)** "Software Update Enforce Latest" enforcement primitive
configured via Intune Settings Catalog — the only forward-compatible enforcement path for macOS
update enforcement post-Apple-OS-26 release. Legacy MDM commands and configuration profile
payloads (`forceDelayedSoftwareUpdates`, `com.apple.SoftwareUpdate`, `ScheduleOSUpdate`) are
deprecated and removed with Apple OS 26.

For the cross-platform comparison, see [Patch Management Overview](00-overview.md).
```

**Section 4 — DDM Software Update Enforcement H2 (V-54-17; all 4 literal tokens; paste structure verbatim from 54-PATTERNS.md lines 256-261):**

```markdown
## DDM Software Update Enforcement {#ddm-enforcement}

DDM "**Software Update Enforce Latest**" (configured in **Intune Settings Catalog** under macOS
configuration profiles) is the **DDM** declarative-management primitive for macOS update
enforcement. This is the only **forward-compatible** enforcement path post-Apple-OS-26 — Apple
deprecates and removes the legacy MDM-command-based enforcement primitives with Apple OS 26 (see
[Deadlines & Cutover Dates](#deadlines-cutover-dates) below).

**Configuration path (Intune Settings Catalog):**

1. Intune > Devices > macOS > Configuration profiles > Create profile
2. Profile type: **Settings catalog**
3. Add settings > Categories > **Declarative Device Management**
4. Select **Software Update** > **Enforce Latest** assertion
5. Configure target version + grace period + deadline behavior
6. Assign to macOS device group

**DDM enforcement model:** The OS itself enforces the target version declaratively. The MDM server
(Intune) declares "device must be on macOS X.Y by date Z" and the OS handles installation,
restart, and user notification per the declared assertion. This differs from legacy MDM command
sending where the server pushed an explicit command to install.

**Eligibility:** macOS 14.0+ supports the DDM software update assertion. macOS 13 and earlier
must use legacy MDM commands [HARD-DEADLINE — see Deadlines H2] until upgrading to macOS 14+.
```

**Section 5 — Deferral vs Enforcement clarification (CONTEXT deliverable 3 narrative):**

```markdown
## Deferral vs Enforcement (macOS)

macOS update tooling has historically conflated two distinct primitives:

- **Deferral** — Tenant-side delay before user can install an update. Configured via the legacy
  MDM restriction `forceDelayedSoftwareUpdates` [HARD-DEADLINE — see Deadlines H2] (set in
  configuration profile payload). Pre-Apple-OS-26 this restriction still functions: tenants can
  defer minor updates 1-90 days. With Apple OS 26 release this restriction is removed.
- **Enforcement** — Tenant-side hard requirement that OS install update by deadline. Pre-Apple-OS-26
  enforcement was via the `ScheduleOSUpdate` MDM command. Post-Apple-OS-26 enforcement is via DDM
  "Software Update Enforce Latest" only.

The DDM model collapses both primitives: the declarative "enforce latest by date Z" assertion
implies both deferral (until date Z) and enforcement (by date Z). Tenants migrating from legacy
MDM commands to DDM should plan to retire the deferral-as-restriction pattern and adopt the
DDM-as-declarative-deadline pattern.
```

**Section 6 — Deadlines & Cutover Dates H2 (D-12 + V-54-14 — literal H2 string `## Deadlines & Cutover Dates`; HARD-DEADLINE three-layer Layer 1 = table cells with `**[HARD-DEADLINE]**` tokens):**

```markdown
## Deadlines & Cutover Dates {#deadlines-cutover-dates}

The Apple OS 26 release deprecates and removes legacy MDM update primitives. Tenants must migrate
to DDM "Software Update Enforce Latest" before Apple OS 26 ships.

| Legacy Command / Payload | Status | Forward Path |
|--------------------------|--------|--------------|
| `forceDelayedSoftwareUpdates` (MDM restriction; configuration profile payload) | **[HARD-DEADLINE]** — see callout | DDM "Software Update Enforce Latest" (Settings Catalog) |
| `com.apple.SoftwareUpdate` (configuration profile payload) | **[HARD-DEADLINE]** — see callout | DDM enforcement key (Settings Catalog) |
| `ScheduleOSUpdate` (MDM command) | **[HARD-DEADLINE]** — see callout | DDM update enforcement (Apple OS 26 forward) |
```

Immediately following the table, insert Layer 2 (verbatim text per CONTEXT D-13 + CD-06; paste verbatim from 54-PATTERNS.md lines 240-243). The blockquote MUST literally start with `> ⚠️ **Hard deadline (Apple OS 26):**`:

```markdown
> ⚠️ **Hard deadline (Apple OS 26):** forceDelayedSoftwareUpdates, com.apple.SoftwareUpdate
> payload, and ScheduleOSUpdate MDM command are deprecated AND removed with Apple OS 26. DDM
> "Software Update Enforce Latest" in Intune Settings Catalog is the only forward-compatible
> enforcement path. Migration MUST land before Apple OS 26 release.
```

V-54-15 verifies all 3 legacy-command tokens (`forceDelayedSoftwareUpdates`, `com.apple.SoftwareUpdate`, `ScheduleOSUpdate`) AND `DDM` AND `Apple OS 26` are within this blockquote text. The above blockquote contains all 5.

**Section 7 — Deprecation Migration H2 (with Layer 3 ≥2 per-occurrence inline reminders per V-54-16 — paste pattern from 54-PATTERNS.md lines 247-251):**

```markdown
## Deprecation Migration: Legacy MDM → DDM

Tenants currently using legacy MDM update primitives must migrate to DDM before Apple OS 26.
Migration plan:

**Step 1 — Inventory legacy command usage.** The legacy `forceDelayedSoftwareUpdates`
[HARD-DEADLINE — see Deadlines H2] MDM restriction configured in current macOS configuration
profiles needs to be identified and inventoried. Use Intune > Devices > Configuration profiles >
filter by macOS to find all profiles using the deferral restriction.

**Step 2 — Inventory `com.apple.SoftwareUpdate` payload usage.** Configuration profiles deploying
the `com.apple.SoftwareUpdate` payload [HARD-DEADLINE — see Deadlines H2] must be migrated to DDM
Settings Catalog assertions. Profiles using this payload typically configure update server URLs
or update catalog sources.

**Step 3 — Inventory `ScheduleOSUpdate` MDM command usage.** The `ScheduleOSUpdate` MDM command
[HARD-DEADLINE — see Deadlines H2] previously used by Intune to push install commands no longer
functions on Apple OS 26. Replace with DDM "Software Update Enforce Latest" assertion.

**Step 4 — Author DDM assertions.** Create new Intune Settings Catalog profiles with DDM Software
Update assertions targeting the desired macOS version + deadline. Pilot on a test ring before
rolling out to production.

**Step 5 — Decommission legacy profiles.** After DDM profiles are deployed and reporting healthy,
decommission the legacy `forceDelayedSoftwareUpdates` / `com.apple.SoftwareUpdate` / `ScheduleOSUpdate`
profiles. Apple OS 26 release is the hard deadline for completion.

**Critical scheduling note:** Migration MUST complete before Apple OS 26 release. Devices
upgrading to Apple OS 26 with legacy command profiles still attached will silently lose update
enforcement (the legacy commands no-op post-OS-26).
```

This section provides the Layer 3 ≥2 per-occurrence inline reminders V-54-16 requires. Counts: at least 3 inline `[HARD-DEADLINE — see Deadlines H2]` reminders in this section (Step 1, Step 2, Step 3). Combined with 3 Layer 1 table cells = total `[HARD-DEADLINE` count ≥6 per V-54-16 algorithm.

**Section 8 — Related Resources + External References footer:**

```markdown
## Related Resources

- [Patch Management Overview](00-overview.md) — Cross-platform comparison + Ring Terminology hub
- [Windows WUfB Rings](01-windows-wufb-rings.md) — Windows forward path
- [iOS Update Lifecycle](03-ios-update-lifecycle.md) — iOS DDM (related Apple platform)
- [Android Patch Delivery](04-android-patch-delivery.md) — Android attestation gate

## External References

- [Software Updates Guide for macOS (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/intune-service/protect/software-updates-guide-macos)
- [Deprecated MDM Policies for macOS (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/intune-service/protect/deprecated-mdm-policies-macos)
- [DDM Software Update (Apple Developer)](https://developer.apple.com/documentation/devicemanagement/softwareupdateenforcement)
- [Apple Platform Deployment — Software Update](https://support.apple.com/guide/deployment/software-update-management-depc4c80847a/web)
```

After authoring, verify with grep:

```bash
grep -c "platform: macOS" docs/operations/patch-management/02-macos-update-enforcement.md  # expect 1
grep -c "^> \*\*Platform applicability:\*\*" docs/operations/patch-management/02-macos-update-enforcement.md  # expect 1
grep -c "^## Deadlines & Cutover Dates" docs/operations/patch-management/02-macos-update-enforcement.md  # expect 1
grep -c "\*\*\[HARD-DEADLINE\]\*\*" docs/operations/patch-management/02-macos-update-enforcement.md  # expect >=3 (3 table rows)
grep -c "\[HARD-DEADLINE" docs/operations/patch-management/02-macos-update-enforcement.md  # expect >=5 (3 Layer 1 + >=2 Layer 3)
grep -c "Hard deadline (Apple OS 26)" docs/operations/patch-management/02-macos-update-enforcement.md  # expect >=1
grep -c "forceDelayedSoftwareUpdates" docs/operations/patch-management/02-macos-update-enforcement.md  # expect >=2
grep -c "com.apple.SoftwareUpdate" docs/operations/patch-management/02-macos-update-enforcement.md  # expect >=2
grep -c "ScheduleOSUpdate" docs/operations/patch-management/02-macos-update-enforcement.md  # expect >=2
grep -c "Software Update Enforce Latest" docs/operations/patch-management/02-macos-update-enforcement.md  # expect >=1
grep -c "Intune Settings Catalog" docs/operations/patch-management/02-macos-update-enforcement.md  # expect >=1
grep -c "DDM" docs/operations/patch-management/02-macos-update-enforcement.md  # expect >=3
grep -c "Apple OS 26" docs/operations/patch-management/02-macos-update-enforcement.md  # expect >=3
grep -c "forward-compatible" docs/operations/patch-management/02-macos-update-enforcement.md  # expect >=1
grep -c "^> \*\*Platform:\*\*" docs/operations/patch-management/02-macos-update-enforcement.md  # MUST be 0
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/patch-management/02-macos-update-enforcement.md  # MUST be empty
```

DO NOT commit yet — atomic commit owned by 54-09 author per CONTEXT D-21 + CDI-Phase54-05.
  </action>
  <verify>
    <automated>test -f docs/operations/patch-management/02-macos-update-enforcement.md &amp;&amp; grep -q "^## Deadlines &amp; Cutover Dates" docs/operations/patch-management/02-macos-update-enforcement.md &amp;&amp; grep -q "Hard deadline (Apple OS 26)" docs/operations/patch-management/02-macos-update-enforcement.md &amp;&amp; grep -q "forceDelayedSoftwareUpdates" docs/operations/patch-management/02-macos-update-enforcement.md &amp;&amp; grep -q "com.apple.SoftwareUpdate" docs/operations/patch-management/02-macos-update-enforcement.md &amp;&amp; grep -q "ScheduleOSUpdate" docs/operations/patch-management/02-macos-update-enforcement.md &amp;&amp; grep -q "Software Update Enforce Latest" docs/operations/patch-management/02-macos-update-enforcement.md &amp;&amp; grep -q "Intune Settings Catalog" docs/operations/patch-management/02-macos-update-enforcement.md &amp;&amp; grep -q "platform: macOS" docs/operations/patch-management/02-macos-update-enforcement.md &amp;&amp; [ "$(grep -c '\[HARD-DEADLINE' docs/operations/patch-management/02-macos-update-enforcement.md)" -ge 5 ] &amp;&amp; ! grep -q "^> \*\*Platform:\*\*" docs/operations/patch-management/02-macos-update-enforcement.md &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/operations/patch-management/02-macos-update-enforcement.md` exists
    - File contains frontmatter with `platform: macOS`, `audience: admin`, `last_verified: 2026-04-28`, `review_by: 2026-06-27`, `applies_to: all`
    - File contains literal `> **Platform applicability:**` blockquote within first 50 lines of body
    - File contains literal H2 `## Deadlines & Cutover Dates` (case-sensitive exact match)
    - File contains legacy-MDM-command table with `forceDelayedSoftwareUpdates` row AND that row contains literal `**[HARD-DEADLINE]**` (V-54-14)
    - File contains Layer 2 blockquote opener literal `> ⚠️ **Hard deadline (Apple OS 26):**` (V-54-15)
    - Within Layer 2 blockquote: all 3 legacy-command tokens (`forceDelayedSoftwareUpdates`, `com.apple.SoftwareUpdate`, `ScheduleOSUpdate`) AND `DDM` AND `Apple OS 26` (V-54-15)
    - Total `[HARD-DEADLINE` token count is ≥5 (3 Layer 1 table cells + ≥2 Layer 3 inline reminders) — `grep -c "\[HARD-DEADLINE" file` returns ≥5 (V-54-16 algorithm in PATTERNS.md lines 558-567)
    - File contains all 4 V-54-17 DDM-only literals: `Software Update Enforce Latest`, `Intune Settings Catalog`, `DDM`, `forward-compatible`
    - File distinguishes deferral from enforcement (Section 5 narrative)
    - File does NOT contain bare `> **Platform:**` token (V-54-27)
    - File does NOT contain any of: `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` (V-54-30)
    - File contains cross-link to 00-overview.md AND each of the 3 sibling per-platform files
  </acceptance_criteria>
  <done>02-macos-update-enforcement.md authored with all D-04/D-12/D-13/D-19 surfaces; PATCH-04 covered with HARD-DEADLINE three-layer; V-54-14/15/16/17/26/27/30 assertions for this file pass; no commit (54-09 owns atomicity).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → reader | Documentation content; reader trust depends on factual deprecation timeline + correct DDM Settings Catalog path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03-01 | Tampering (HARD-DEADLINE three-layer Layer drift) | Author writes Layer 2 blockquote with different opener (e.g. `> ⚠️ **Apple OS 26 deadline:**`) → V-54-15 literal-match FAILS | mitigate | V-54-15 pinned literal `> ⚠️ **Hard deadline (Apple OS 26):**` in validator + PATTERNS.md verbatim text; D-13 mandates exact form |
| T-54-03-02 | Tampering (HARD-DEADLINE inline reminder count) | Author writes only 1 inline reminder; V-54-16 requires ≥2 (total ≥5 per algorithm) | mitigate | Section 7 prescribes 3 inline reminders (Step 1, Step 2, Step 3) — exceeds minimum |
| T-54-03-03 | Tampering (factual drift on Apple OS 26 release) | Apple OS 26 release date may shift; legacy command cutover timing depends on Apple shipping date | accept | 60-day `review_by` cycle; cite Microsoft Learn deprecated-mdm-policies-macos page |
| T-54-03-04 | Tampering (Settings Catalog path drift) | Intune blade may rename "Settings Catalog" or move DDM Software Update assertion | mitigate | RESEARCH SUMMARY line 248 flagged this verification at plan time; cited path is Intune > Devices > macOS > Configuration profiles > Settings catalog (current as of 2026-04-28); 60-day review_by surface drift |
| T-54-03-05 | Information disclosure (cross-platform admin reading wrong file) | macOS-specific guidance pulled into iOS context (Apple sibling) | mitigate | D-04 Platform applicability blockquote routes Apple-fleet admins to correct sibling; iOS guide has its own DDM section per 54-04 |

Severity: LOW. Documentation phase; no application security surface introduced.
</threat_model>

<verification>
After Task 1 completes:

```bash
test -f docs/operations/patch-management/02-macos-update-enforcement.md && echo "exists: PASS"

head -7 docs/operations/patch-management/02-macos-update-enforcement.md | grep -E "^(platform|audience|last_verified|review_by|applies_to):"

sed -n '/^---$/,/^---$/!p' docs/operations/patch-management/02-macos-update-enforcement.md | head -50 | grep -c "^> \*\*Platform applicability:\*\*"

# V-54-14: Deadlines H2 + Layer 1 token in legacy-command row
grep -q "^## Deadlines & Cutover Dates" docs/operations/patch-management/02-macos-update-enforcement.md && echo "Deadlines H2: PASS"
grep -E "forceDelayedSoftwareUpdates.*\*\*\[HARD-DEADLINE\]\*\*|\*\*\[HARD-DEADLINE\]\*\*.*forceDelayedSoftwareUpdates" docs/operations/patch-management/02-macos-update-enforcement.md && echo "Layer 1 forceDelayedSoftwareUpdates row: PASS"

# V-54-15: Layer 2 blockquote + 5 tokens
grep -q "Hard deadline (Apple OS 26)" docs/operations/patch-management/02-macos-update-enforcement.md && echo "Layer 2 opener: PASS"

# V-54-16: total [HARD-DEADLINE >= 5
total=$(grep -c "\[HARD-DEADLINE" docs/operations/patch-management/02-macos-update-enforcement.md)
[ "$total" -ge 5 ] && echo "Layer 3 reminders ($total tokens >= 5): PASS"

# V-54-17: DDM-only literals
for tok in "Software Update Enforce Latest" "Intune Settings Catalog" "DDM" "forward-compatible"; do
  grep -q "$tok" docs/operations/patch-management/02-macos-update-enforcement.md && echo "$tok: PASS"
done

# V-54-27 NEGATIVE
grep -c "^> \*\*Platform:\*\*" docs/operations/patch-management/02-macos-update-enforcement.md  # expect 0

# V-54-30 TBD scan
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/patch-management/02-macos-update-enforcement.md
# expect empty
```

If 54-09 validator is also authored at this point:

```bash
node scripts/validation/check-phase-54.mjs --verbose 2>&1 | grep "V-54-0[37]\|V-54-1[4567]\|V-54-26\|V-54-27\|V-54-30"
# All file-specific assertions for 02-macos-update-enforcement.md should PASS
```

DO NOT commit — atomic commit owned by 54-09.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 54 SC#2 + REQUIREMENTS.md PATCH-04:

- **SC#2** (legacy MDM commands deprecated + removed with Apple OS 26; DDM only forward path): Fully covered by Deadlines & Cutover Dates H2 + three-layer + DDM-only literals
- **PATCH-04** (DDM "Software Update Enforce Latest" + Intune Settings Catalog + legacy MDM command deprecation): Fully covered

Plan complete when:

- [ ] File exists at `docs/operations/patch-management/02-macos-update-enforcement.md`
- [ ] V-54-03 (file exists), V-54-07 (frontmatter), V-54-14 (Deadlines H2 + Layer 1), V-54-15 (Layer 2 verbatim + 5 tokens), V-54-16 (Layer 3 ≥2 inline; total ≥5), V-54-17 (DDM-only literals), V-54-26 (blockquote at TOP), V-54-27 (no bare-Platform), V-54-30 (TBD scan) all pass
- [ ] HARD-DEADLINE three-layer applied to Apple OS 26 removal (Phase 53 D-05 inheritance per CDI-Phase54-02)
- [ ] No commit — 54-09 owns atomicity per CONTEXT D-21
</success_criteria>

<output>
After completion, create `.planning/phases/54-patch-update-management/54-03-SUMMARY.md` documenting:

- File path + line count
- Sections authored (frontmatter, Platform applicability blockquote, H1 + intro, DDM Software Update Enforcement, Deferral vs Enforcement, Deadlines & Cutover Dates with Layer 1+2, Deprecation Migration with Layer 3, Related Resources, External References)
- V-54-NN assertions this file satisfies (V-54-03, V-54-07, V-54-14, V-54-15, V-54-16, V-54-17, V-54-26, V-54-27, V-54-30)
- HARD-DEADLINE three-layer counts (3 Layer 1 + 1 Layer 2 + ≥2 Layer 3 inline)
- PATCH-04 traceability
- Note: NO COMMIT — atomic commit owned by 54-09
</output>
</content>
</invoke>