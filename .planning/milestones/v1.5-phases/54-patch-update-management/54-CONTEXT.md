# Phase 54: Patch & Update Management - Context

**Gathered:** 2026-04-28
**Status:** Ready for planning
**Methodology:** Adversarial review (Finder / Adversary / Referee scored pattern) on 4 gray areas covering 16 candidate sub-options across 30 enumerated defects (90 raw points: 7 CRIT + 19 MED + 4 LOW). Adversary attempted 14 disproves (5 MED + 9 partial-LOW). Referee adopted 12 disproves (with file-system verification) and rejected 2 (notably the F-2C-01 token-novelty disprove — verbatim corpus scan returned 28+ `> **Platform <qualifier>:**` instances and ZERO bare `> **Platform:**`, so the claimed "methodology family member" status was refuted). Net surviving defect score after rulings: 19.5 points across 13 confirmed defects, with per-area winners: GA-1 = 1D Hybrid (5 surviving — concept terminology + cross-platform comparison table + routing); GA-2 = 2B-prime (11 surviving — Phase 53 D-08 `> **Platform applicability:**` token reused on per-platform files routing back to 00-overview, NO bare-noun token novelty); GA-3 = 3B-with-addendum (0 surviving — file-system verification proved `06-compliance-policy.md` contains ZERO supervised-only-DDM claim and the actual residual contradiction lives at `07-device-enrollment.md:35` cell; REQ literal `05-compliance-policy.md` is off-by-one wrong); GA-4 = 4A+4C-hybrid (~3.5 surviving — per-file H2 decision differentiated by content density; HARD-DEADLINE items get Phase 53 D-05 three-layer inheritance, soft cutovers get single inline callout).

<domain>
## Phase Boundary

Phase 54 delivers Multi-Platform Patch & Update Management documentation as a 5-file suite under `docs/operations/patch-management/` plus a surgical v1.3 retrofit at `docs/admin-setup-ios/07-device-enrollment.md:35` (PATCH-06 same-commit-atomic per ROADMAP line 271 v1.4.1 lesson) plus a PITFALL-13 forward-link insertion at `docs/admin-setup-ios/04-configuration-profiles.md:128`. **Phase 54 is the FIRST ops-domain phase to span all 4 existing platforms natively** (Phase 53 was Windows-led with cross-platform contextual notes; Phase 54 has 4 platform-specific files where each platform IS one file). Phase 53 (Co-Management) is the directly-precedent methodology source for cross-platform inline-blockquote shape (D-08), single-atomic-commit Goldilocks-rule (D-14), three-layer HIGH-RISK callout (D-05), and the operations/00-index.md cross-reference contract (DPO-Phase53-01). Linux is OUT of scope for Phase 54 (no Linux update enforcement REQ; Linux Intune client patch surface is OS-vendor-controlled per Phase 49 PITFALL-7 whitelist).

**Eight deliverables (8 active requirements: PATCH-01..08):**

1. `docs/operations/patch-management/00-overview.md` (no specific REQ; SUMMARY line 189 deliverable framing) — **1D Hybrid winner**: cross-platform patch-delivery comparison table (4 platforms × 5 concept rows minimum) + PITFALL-9 ring-disambiguation H2 (`WUfB deployment ring` vs `Autopatch ring` mutual-exclusion narrative + driver/firmware policy separation) + deferral-vs-enforcement terminology section (cross-platform: WUfB deferral / DDM enforcement / Play Integrity attestation) + routing cross-links to 01-04. Size budget 200-350 lines hard cap. NO PATCH-01..08 substantive content duplicated (REQ traceability firewall enforced by V-54-NN NEGATIVE assertion). Cross-platform inline `> **Platform applicability:**` blockquote at TOP per Phase 53 D-08 inheritance + 2B-prime winner.

2. `docs/operations/patch-management/01-windows-wufb-rings.md` (PATCH-01 + PATCH-02 fold + PATCH-03 fold) — Per REQ traceability lines 161-163 verbatim: PATCH-01 (WUfB ring topology + Autopatch disambiguation + PITFALL-9 mutual-exclusion explicit) AS PRIMARY SCOPE; PATCH-02 ("Hotpatch H2 inside") AS DEDICATED H2 covering Windows 11 Enterprise 24H2+ default-from-May-2026 + VBS prereqs + April 2026 Intune admin center opt-out toggle + reboot-reduction impact on compliance reporting; PATCH-03 ("Driver/firmware update policy H2 inside") AS DEDICATED H2 separate from quality/feature rings + dual-scan source conflict pitfall when SCCM co-management still controls WU workload. **NO separate Deadlines & Cutover Dates H2** (Hotpatch H2 absorbs Windows soft cutovers May 2026 + April 2026 by REQ design — F-4A-03 redundancy avoidance). PITFALL-9 strict per-occurrence ring-qualifier discipline (every "ring" mention prefixed `WUfB deployment` or `Autopatch`). Cross-platform inline `> **Platform applicability:**` blockquote at TOP per 2B-prime winner. `platform: Windows` frontmatter.

3. `docs/operations/patch-management/02-macos-update-enforcement.md` (PATCH-04) — DDM "Software Update Enforce Latest" in Intune Settings Catalog as the only forward-compatible enforcement path; legacy MDM commands (`forceDelayedSoftwareUpdates`, `com.apple.SoftwareUpdate` payload, `ScheduleOSUpdate`) deprecated AND removed with Apple OS 26 — distinguish deferral (MDM restriction for delay still functional pre-OS-26) from enforcement (DDM only, forward). **YES dedicated `## Deadlines & Cutover Dates` H2** containing Apple OS 26 removal as **HARD-DEADLINE three-layer callout per Phase 53 D-05 + Phase 52 D-01 inheritance** (Layer 1: legacy-MDM-command table-row cell `[HARD-DEADLINE]` token; Layer 2: adjacent `> ⚠️ **Hard deadline:**` blockquote with Apple OS 26 removal date + DDM-forward-path mandate; Layer 3: ≥2 per-occurrence inline `[HARD-DEADLINE — see Deadlines H2]` reminders adjacent to legacy-token mentions in body). Cross-platform inline `> **Platform applicability:**` at TOP. `platform: macOS` frontmatter.

4. `docs/operations/patch-management/03-ios-update-lifecycle.md` (PATCH-05) — DDM update keys (TargetOSVersion, TargetBuildVersion, TargetLocalDateTime, OfferPrograms) work on UNSUPERVISED iOS 17+ devices effective August 2025; supervised-only constraint retracted explicitly. **NO Deadlines H2** (single Aug 2025 retraction date is single-callout overhead — `> ⚠️` blockquote inline at supervised-only retraction site only). Cross-platform inline `> **Platform applicability:**` at TOP. `platform: iOS` frontmatter. **CRITICAL — same-commit-atomic with PATCH-06 retrofit** at `docs/admin-setup-ios/07-device-enrollment.md:35` (see deliverable 7 below) per ROADMAP line 271 v1.4.1 atomicity lesson.

5. `docs/operations/patch-management/04-android-patch-delivery.md` (PATCH-07 + PATCH-08 fold) — Per REQ traceability lines 167-168: PATCH-07 (MEETS_STRONG_INTEGRITY enforcement timeline: Google May 2025 / Intune September 30 2025 / fleet October 31 2026 hard deadline; Android 13+ security patch ≤12 months requirement) AS PRIMARY SCOPE; PATCH-08 (Zebra LifeGuard OTA via Intune GA January 2026; Samsung KSP analog OEM mechanism) AS H2 SECTION inside the same file. **YES dedicated `## Deadlines & Cutover Dates` H2** containing MEETS_STRONG_INTEGRITY Oct 31 2026 fleet deadline as **HARD-DEADLINE three-layer callout per Phase 53 D-05 inheritance** (Layer 1: integrity-attestation table-row cell `[HARD-DEADLINE]`; Layer 2: adjacent `> ⚠️ **Hard deadline:**` blockquote with full May 2025/Sept 30 2025/Oct 31 2026 cascade + Android 13+ patch-age requirement; Layer 3: ≥2 per-occurrence inline `[HARD-DEADLINE — see Deadlines H2]` reminders adjacent to MEETS_STRONG_INTEGRITY mentions). LifeGuard Jan 2026 GA + Samsung KSP referenced as table rows in same Deadlines H2 (soft cutover; no three-layer). Cross-platform inline `> **Platform applicability:**` at TOP. `platform: Android` frontmatter.

6. `docs/admin-setup-ios/04-configuration-profiles.md:128` — PITFALL-13 reader-friction forward-link insertion (NOT content retraction). Existing line 128 already substantively correct ("works on both supervised and unsupervised devices (iOS 17.0+)"); append forward-link sentence to new `03-ios-update-lifecycle.md` for canonical update-enforcement guidance.

7. `docs/admin-setup-ios/07-device-enrollment.md:35` — **PATCH-06 SURGICAL RETROFIT TARGET** (the actual residual supervised-only-DDM-shaped claim; off-ballot Referee resolution overriding REQ literal `05-compliance-policy.md`). Cell text edit retracting "Supervised-only in iOS 17+ enforcement policies" → DDM-on-unsupervised-iOS-17+ correction with cross-link to `03-ios-update-lifecycle.md`. **MUST be same-commit-atomic with deliverable 4** per ROADMAP line 271 + RETROSPECTIVE.md line 166 v1.4.1 lesson.

8. `scripts/validation/check-phase-54.mjs` — AUDIT-06 validator-as-deliverable per Phase 48 D-25 file-reads-only contract + Phase 49 D-26 + Phase 51 D-20 + Phase 52 D-11 + Phase 53 D-10 regex-based pattern lineage. Full validator scope per Phase 53 D-10 precedent; estimated 28-32 V-54-NN structural assertions (file existence × 6 + frontmatter local contract × 5 + per-platform literal-token coverage + ring-qualifier dual-defense + Hotpatch H2 + driver/firmware H2 + DDM-only literal × 2 + MEETS_STRONG_INTEGRITY cascade literal coverage + LifeGuard literal + KSP literal + three-layer × 2 hard-deadlines × 3 layers = 6 assertions + retrofit cell-text NEGATIVE+POSITIVE pair + REQ/ROADMAP errata literal-purge regression-guard + 04:128 forward-link literal + ops/00-index.md NOT-amended NEGATIVE regression-guard + cross-platform inline blockquote × 5 files + bare-`> **Platform:**` NEGATIVE regression-guard + TBD/TODO scan).

**Same-commit errata** (in addition to deliverables): `.planning/REQUIREMENTS.md` lines 55 + 166 + `.planning/ROADMAP.md` line 267 — replace literal `docs/admin-setup-ios/05-compliance-policy.md` with `docs/admin-setup-ios/07-device-enrollment.md` (the actual retrofit target file per file-system verification — REQ literal off-by-one was 3-way replicated).

Phase 55 (App Lifecycle Automation) consumes `operations/00-index.md` per ROADMAP line 457 + 277 (cross-reference-only); Phase 56 (Drift Detection + Tenant Migration) consumes per ROADMAP line 290; Phase 59 (Hub Navigation Integration) consumes Phase 54 anchors for `docs/index.md` Operations H2. Phase 54 MUST cross-reference `operations/00-index.md` from its own files, MUST NOT amend it (DPO-Phase53-01 + ROADMAP line 457 hotspot ownership rule). Phase 60 (Audit Harness v1.5 Finalization) registers `check-phase-54.mjs` in CI workflow + runs C13 broken-link automation against Phase 54 cross-link surface. Phase 58 (DEFER-08 4-platform capability comparison) WILL consume Phase 54 anchors for its Software Updates domain row (one of the canonical 6 axes per ROADMAP line 322; link-not-copy per PITFALL-7).

</domain>

<decisions>
## Implementation Decisions

### 00-overview.md scope shape (Gray Area 1 — winner: 1D Hybrid)

- **D-01:** **`docs/operations/patch-management/00-overview.md` follows 1D Hybrid shape: concept-terminology hub + cross-platform comparison table + per-platform routing.** Per Adversary-Referee adjudication (5 surviving defect points; 1A=24, 1B=25, 1C=15). Reject 1A: F-1A-01 CRIT (PITFALL-9 mutual-exclusion narrative cannot fit minimal-prose comparison-only shape) + F-1A-02 CRIT (Phase 53 D-08 inheritance violation). Reject 1B: F-1B-02 CRIT (SUMMARY line 189 "guide" framing implies substantive content per file; pure router duplicates `operations/00-index.md` Phase 53-owned routing surface). Reject 1C: F-1C-02 + F-1C-04 (loses multi-platform aspect; cross-platform "elsewhere" target unspecified — admin must synthesize across 4 files for fleet-level decisions).
  - **Content scope:** (a) cross-platform patch-delivery comparison table (rows = concepts: cadence model, deferral mechanism, enforcement primitive, hotpatch/EOL surface, hard-deadline timeline; columns = Windows / macOS / iOS / Android — 4 platforms × 5+ concept rows minimum); (b) PITFALL-9 ring-disambiguation H2 (`WUfB deployment ring` vs `Autopatch ring` mutual-exclusion narrative + driver/firmware policy separation note); (c) deferral-vs-enforcement terminology section (cross-platform: Windows WUfB deferral semantics vs macOS DDM enforcement vs iOS supervised-only-DDM-retraction vs Android MEETS_STRONG_INTEGRITY enforcement); (d) routing cross-links to all 4 per-platform files.
  - **Size budget:** 200-350 lines hard cap (1.4-2.4× Phase 53 145-line baseline; F-1D-01 confirmed shape-drift mitigation).
  - **Anti-scope-creep regression-guard:** V-54-NN NEGATIVE assertion: 00-overview MUST NOT contain Hotpatch-VBS-opt-out content (PATCH-02 territory in `01-windows-wufb-rings.md`), driver/firmware ring content (PATCH-03 territory), Zebra LifeGuard content (PATCH-08 territory inside `04-android-patch-delivery.md`), or supervised-only retraction content (PATCH-05 territory in `03-ios-update-lifecycle.md`). REQ traceability table at REQUIREMENTS lines 161-168 maps zero PATCH requirements to 00-overview — structural firewall.
- **D-02:** **F-1D-02 disprove sustained: `operations/00-index.md` 2-column "Guide | Covers" routing table is categorically different from 1D's N-column concept × platform comparison matrix.** Phase 52 D-01 + Phase 53 D-03 differentiated-shape rationale endorses table coexistence; Phase 53 00-overview already ships 7-workload table + slider-state table without collision.
- **D-03:** **F-1D-03 disprove sustained: PITFALL-13 token-density not 1D-specific.** V-53-25 scans for TBD/TODO/FIXME/XXX/PLACEHOLDER, NOT "deprecated"/"removed"/"EOL". Phase 53 00-overview ships "Deprecated since CB 2203" without validator collision. SC#2 verbatim REQUIRES the deprecated tokens — they exist regardless of 1A/1B/1C/1D shape. Phase 53 D-06 line 53 explicitly defers PITFALL-13 same-commit-allowlist seeding under YAGNI to first-false-positive surface (CDI-Phase54-03 inheritance from CDI-Phase53-03).

### Cross-platform routing pattern across per-platform files (Gray Area 2 — winner: 2B-prime)

- **D-04:** **Each of `00-overview.md` / `01-windows-wufb-rings.md` / `02-macos-update-enforcement.md` / `03-ios-update-lifecycle.md` / `04-android-patch-delivery.md` carries an inline `> **Platform applicability:**` blockquote at the TOP of the file** (line 9, after frontmatter) per Phase 53 D-08 token inheritance + 2B-prime winner. Content per file: brief one-line platform identifier + back-link to `00-overview.md` for full cross-platform comparison + ≤3 sub-bullets pointing to the other three sibling per-platform files (mirrors `01-windows-tenant-attach.md:9-20` Phase 53 implementation shape). The blockquote is pointer-only (link-not-copy contract — full comparison content lives in 00-overview per D-01).
  - **Critical token discipline:** Use established `> **Platform applicability:**` token verbatim. Bare `> **Platform:**` is **forbidden** (zero corpus precedent; F-2C-01 Adversary disprove rejected by Referee — corpus has 28+ `> **Platform <qualifier>:**` instances and ZERO bare `> **Platform:**`). Lexicon-family preservation enforced by V-54-NN NEGATIVE regression-guard.
  - Reject 2A (Phase 53 D-08 with full embedded cross-link routing per file): F-2A-01 CRIT (no Phase 54 REQ analog of REQ-COMG-04; embedding full routing without REQ anchor fails validator-as-deliverable discipline) + F-2A-02 CRIT (PITFALL-9 collision in `01-windows-wufb-rings.md` opening real estate — file's opening must host PITFALL-9 mutual-exclusion narrative).
  - Reject 2B-pure (NO inline blockquote in per-platform files; all routing centralized in 00-overview only): F-2B-02 MED (Google-landing audience friction — admin lands on `02-macos-update-enforcement.md` from search, no platform body-prose signal) + F-2B-06 LOW (frontmatter `platform:` invisible at GitHub render). 2B-prime resolves this by reusing Phase 53 D-08 token in lighter form.
  - Reject 2C-as-stated (`> **Platform:**` bare-noun token): F-2C-01 MED upheld by Referee — bare-noun is novel token forking the established two-word-qualifier methodology family.
  - Reject 2D (Phase 51 D-08 platform-gate banner): F-2D-01 CRIT (Phase 53 D-08 explicit rejection for admin audience; Phase 54 audience also admin per ROADMAP line 261) + F-2D-04 MED (same-shape collision risk).
- **D-05:** **Cross-platform routing surface centralization at `00-overview.md`** per 1D + 2B-prime coupling. The full 4-platform comparison table + concept terminology + PITFALL-9 ring-disambiguation H2 + deferral-vs-enforcement section live in 00-overview only; per-platform files carry only pointer-shape inline blockquote. Avoids 2A's PITFALL-9 collision in `01-windows-wufb-rings.md` opening real estate (the opening of the Windows file is reserved for PITFALL-9 mutual-exclusion narrative per PITFALLS line 200 mandate).

### PATCH-06 v1.3 iOS retrofit target (Gray Area 3 — winner: 3B-with-addendum; off-ballot file resolution)

- **D-06:** **PATCH-06 RETROFIT TARGET = `docs/admin-setup-ios/07-device-enrollment.md:35`** (NOT `06-compliance-policy.md` as REQ literal off-by-one would otherwise suggest). **OFF-BALLOT REFEREE OVERRIDE based on file-system verification.** Verified facts:
  - `docs/admin-setup-ios/06-compliance-policy.md` contains ZERO supervised-only-DDM constraint — only "supervised" mention is line 27 (Block App Store / Block installing apps configuration-profile context). Line 25 already says "DDM-based on iOS 17+" without supervised qualifier. **No retraction needed in 06.**
  - `docs/admin-setup-ios/04-configuration-profiles.md:128` already substantively correct ("works on both supervised and unsupervised devices (iOS 17.0+)"). **No content retraction needed in 04.**
  - `docs/admin-setup-ios/07-device-enrollment.md:35` cell verbatim: `| OS update enforcement (forced install deadlines via DDM or device restrictions) | **No** | Supervised-only in iOS 17+ enforcement policies. |` — **THIS IS the actual residual v1.3 supervised-only-DDM-shaped claim.** Direct contradiction of PATCH-05 retraction.
  - `docs/admin-setup-ios/05-app-deployment.md` is the actual file at slot 05 (NOT compliance-policy). REQ literal `05-compliance-policy.md` is off-by-one wrong; 3-way replicated across REQUIREMENTS:55, REQUIREMENTS:166, ROADMAP:267.
- **D-07:** **Surgical edit at `07-device-enrollment.md:35`** — replace cell content with: `| OS update enforcement (forced install deadlines via DDM) | **Yes (DDM, iOS 17+)** / **No (legacy MDM device restrictions)** | DDM-based "iOS/iPadOS update policies" works on unsupervised iOS 17+ devices (basic keys: TargetOSVersion, TargetBuildVersion, TargetLocalDateTime, OfferPrograms). Legacy device-restrictions "Defer software updates" remains supervised-only and is being deprecated. See [iOS Update Lifecycle](../../operations/patch-management/03-ios-update-lifecycle.md). |` (exact wording is plan-author discretion within retraction-content + cross-link contract).
- **D-08:** **PITFALL-13 forward-link insertion at `04-configuration-profiles.md:128`** — append sentence: "For full iOS update enforcement guidance including DDM key reference, supervision matrix, and rollout patterns, see [iOS Update Lifecycle](../../operations/patch-management/03-ios-update-lifecycle.md)." NOT a content retraction (line 128 already correct); is a reader-friction reduction add-on per PITFALL-13 navigation gap closure.
- **D-09:** **Same-commit REQUIREMENTS.md + ROADMAP.md errata** correcting all three off-by-one literals from `05-compliance-policy.md` (or `06-compliance-policy.md` if read as semantic-preserving) → `07-device-enrollment.md` (the actual retrofit target file). Errata locations:
  - `.planning/REQUIREMENTS.md` line 55 — replace path literal in PATCH-06 verbatim text + update verb anchor to "v1.3 supervised-only-DDM cell at line 35"
  - `.planning/REQUIREMENTS.md` line 166 — replace path literal in traceability table
  - `.planning/ROADMAP.md` line 267 — replace path literal in SC#3 verbatim conjunct (b)
- **D-10:** **Same-commit-atomic landing per ROADMAP line 271 + RETROSPECTIVE line 166 v1.4.1 lesson** ("Atomic same-commit forward-promise closure beats deferred retrofits. Validated 6× in v1.4.1"). Single commit covers: (a) all 5 new patch-management content files (00-overview + 01..04); (b) `07-device-enrollment.md:35` retrofit cell edit; (c) `04-configuration-profiles.md:128` forward-link addition; (d) REQ + ROADMAP errata; (e) `check-phase-54.mjs` validator. Splitting any of these into separate commits violates v1.4.1 atomicity contract + SC#3 conjunction-fail.
- **D-11:** **SC#3 conjunction analysis** (ROADMAP:267 verbatim "Admin reading ... AND ... v1.3 ... callout is surgically retrofitted"): two conjuncts — (a) new `03-ios-update-lifecycle.md` states retraction; (b) v1.3 callout retrofitted same-commit. 3B-with-addendum satisfies BOTH; 3D (forward-link only) satisfies (a) only. The "AND" requires (b). Validator V-54-NN-A enforces the (b) edit lands.
- Reject 3A (retrofit `06-compliance-policy.md` only): F-3A-01 CRIT confirmed by file-system scan — file has zero supervised-only-DDM claim; retrofit edit would be net-new fictional retraction insertion (PITFALL-13 false-positive risk + SC#3 verb "retrofitted" semantically fails on net-new content).
- Reject 3C (retrofit BOTH 06 + 04): inherits 3A's CRIT; doubled commit surface; doubles validator pin coordinates.
- Reject 3D (forward-link only, no retrofit): F-3D-01 CRIT (REQ verb "surgically retrofitted" hard-failed) + F-3D-02 CRIT (SC#3 conjunction-fail) + violates v1.4.1 atomicity lesson.

### Deadline/deprecation callout shape per platform file (Gray Area 4 — winner: 4A+4C-hybrid 4E)

- **D-12:** **Per-platform "Deadlines & Cutover Dates" H2 differentiated by content density:**
  - **`01-windows-wufb-rings.md`: NO separate Deadlines H2.** PATCH-02-mandated Hotpatch H2 (REQUIREMENTS line 51 verbatim "a dedicated Windows Hotpatch H2 inside the WUfB rings guide covering... default-from-May-2026 behavior, VBS prerequisites, opt-out toggle (April 2026 Intune admin center addition)") IS the deadline-bearing surface for both Windows soft cutovers (May 2026 default + April 2026 toggle) by REQ design. Adding a separate Deadlines H2 would duplicate the same dates in two H2s (F-4A-03 redundancy).
  - **`02-macos-update-enforcement.md`: YES dedicated `## Deadlines & Cutover Dates` H2.** Apple OS 26 removal of `forceDelayedSoftwareUpdates` + `com.apple.SoftwareUpdate` payload + `ScheduleOSUpdate` is a HARD-DEADLINE warranting full three-layer treatment (D-13 below).
  - **`03-ios-update-lifecycle.md`: NO H2.** Single August 2025 retraction date is single-callout overhead — `> ⚠️` blockquote inline at the supervised-only retraction site only.
  - **`04-android-patch-delivery.md`: YES dedicated `## Deadlines & Cutover Dates` H2.** MEETS_STRONG_INTEGRITY cascade timeline (Google May 2025 / Intune Sept 30 2025 / fleet Oct 31 2026 hard deadline + Android 13+ patch ≤12 months requirement) is a HARD-DEADLINE warranting full three-layer treatment (D-13 below). Zebra LifeGuard Jan 2026 GA + Samsung KSP referenced as table rows in same Deadlines H2 (soft cutovers; no three-layer).
- **D-13:** **HARD-DEADLINE three-layer callout pattern (Phase 53 D-05 + Phase 52 D-01 inheritance per CDI-Phase54-02 + DPO-Phase52-07 cross-domain transferability endorsement).** Applied to TWO items only: (a) macOS Apple OS 26 removal of legacy MDM commands; (b) Android MEETS_STRONG_INTEGRITY Oct 31 2026 fleet deadline. Three layers per item:
  - **Layer 1 (table-cell):** Risk/Status column entry = `**[HARD-DEADLINE]** — see callout` (bold + cross-reference to Layer 2 within same file). Validator V-54-NN-D asserts literal pattern in macOS legacy-command table row + Android integrity-attestation table row.
  - **Layer 2 (adjacent `> ⚠️` blockquote):** Placed immediately below the relevant table; literal text:
    - macOS: `> ⚠️ **Hard deadline (Apple OS 26):** forceDelayedSoftwareUpdates, com.apple.SoftwareUpdate payload, and ScheduleOSUpdate MDM command are deprecated AND removed with Apple OS 26. DDM "Software Update Enforce Latest" in Intune Settings Catalog is the only forward-compatible enforcement path. Migration MUST land before Apple OS 26 release.`
    - Android: `> ⚠️ **Hard deadline (Oct 31 2026):** MEETS_STRONG_INTEGRITY enforcement: Google enforced May 2025; Intune enforced September 30 2025; fleet compliance deadline October 31 2026. Android 13+ devices must have a security patch ≤12 months old. Devices not meeting this threshold will fail Intune compliance after Oct 31 2026.`
  - **Layer 3 (per-occurrence inline):** Every later text reference to the legacy commands (macOS) or MEETS_STRONG_INTEGRITY (Android) within the same file carries an inline `[HARD-DEADLINE — see Deadlines H2]` reminder. Validator V-54-NN-E requires ≥2 such inline reminders per file.
- **D-14:** **Soft cutover items (Hotpatch May 2026 default, April 2026 Intune opt-out toggle, LifeGuard Jan 2026 GA, iOS Aug 2025 unsupervised-DDM retraction):** **Single-callout, NOT three-layer.** Hotpatch May/April 2026 lives inside Hotpatch H2 (PATCH-02 mandate); LifeGuard Jan 2026 GA lives as a row in Android Deadlines H2 (no per-occurrence inline reminders); iOS Aug 2025 retraction lives as a single `> ⚠️` inline blockquote at the supervised-only-DDM correction site. Avoids over-engineering soft cutovers per F-4C-05 polarity-mismatch concern.
- **D-15:** **Frontmatter `valid_as_of` pin: SKIP.** Reuse existing `last_verified` per Phase 49/50/51/52/53 D-04/D-12 60-day cycle. Adding new frontmatter key is untested-precedent risk vs. zero added information for deadline content. F-4D-01 CRIT (frontmatter-only is reader-invisible per Phase 52 GA-1 D-01 reject precedent) confirms frontmatter alone is insufficient; body content is mandatory.
- **D-16:** **PITFALL-13 same-commit allowlist seeding: DEFERRED, NOT same-commit.** Per Phase 53 D-06 + Phase 48 D-15 YAGNI precedent + CDI-Phase54-03 inheritance: validator V-54-NN scope is restricted to `docs/operations/patch-management/` (file-path-prefix filter), so no false-positive surface exists at Phase 54 ship. Lazy-add forward-extension only on first false-positive surface per Phase 60 DPO-Phase54-04.
- Reject 4B (per-occurrence inline `> ⚠️` only): F-4B-01 CRIT (Phase 53 D-05 three-layer inheritance violation for hard deadlines) + F-4B-04 MED (60-day staleness multi-edit cost).
- Reject 4C-pure (three-layer for hard-deadline only; softer = inline) without 4A H2 hub: F-4C-03 MED (cross-platform consistency drift — 02 + 04 use three-layer; 01 + 03 use single inline; mixed shapes); the hybrid 4E resolves by adding centralized H2 surface where content density warrants.
- Reject 4D (frontmatter pin only): F-4D-01 + F-4D-02 + F-4D-03 CRIT triple — SC#2/3/4 require body content "admin reading the file"; frontmatter is invisible at GitHub/SharePoint/Confluence render; single scalar can't encode 3 dates for SC#4 cascade.

### Validator scope + commit atomicity + frontmatter platform contract

- **D-17:** **`check-phase-54.mjs` validator scope = FULL** per Phase 53 D-10 + Phase 52 D-10 22-26 V-NN floor precedent. File-reads-only / no-shared-module per Phase 48 D-25; regex-based parsing per Phase 49 D-26 + Phase 51 D-20 + Phase 52 D-11 + Phase 53 D-11. Estimated 28-32 V-54-NN structural assertions:
  - **V-54-01..06 File existence:** 5 patch-management files + retrofit target + validator self-reference + `04-configuration-profiles.md` + `07-device-enrollment.md`
  - **V-54-07 Frontmatter local contract (per D-12 Phase 53 inheritance):** 5 patch-management files carry single-string `platform: <Windows|macOS|iOS|Android|cross-platform>` + `audience: admin` + 60-day `last_verified` + `review_by`. NOTE: 00-overview platform-tag question is sub-decision (CD-01); recommended single-string `platform: cross-platform` (NEW token, intentional for 00-overview only) OR `platform: Windows,macOS,iOS,Android` (comma-string; matches REQUIREMENTS line 269 SC#5 "correct multi-platform frontmatter"). Validator V-54-07 accepts either.
  - **V-54-08 7-platform comparison table presence (per D-01 1D):** `00-overview.md` contains a table with columns matching `Windows | macOS | iOS | Android` (or transpose with platforms as rows; validator accepts either orientation via literal-token regex).
  - **V-54-09 PITFALL-9 ring-disambiguation H2 (per D-01 + PITFALLS line 200):** `00-overview.md` contains literal H2 `## Ring Terminology` (or equivalent regex-pinned pattern) with both `WUfB deployment ring` AND `Autopatch ring` literal tokens within ~10-line window of that H2.
  - **V-54-10 Deferral-vs-enforcement terminology section (per D-01):** `00-overview.md` contains literal `deferral` AND `enforcement` distinguishing prose.
  - **V-54-11 PITFALL-9 strict ring qualifier in 01-windows-wufb-rings (per SC#1 + PITFALLS line 200):** every "ring" token in body of `01-windows-wufb-rings.md` must be preceded within ~30-char window by `WUfB deployment` OR `Autopatch` qualifier (POSITIVE assertion). NEGATIVE regression-guard: zero bare `ring` tokens outside qualifier scope.
  - **V-54-12 Hotpatch H2 presence (per PATCH-02 + D-12):** `01-windows-wufb-rings.md` contains literal H2 `## Hotpatch` (or equivalent regex-pinned pattern) with all 4 PATCH-02 sub-topic tokens: `default`, `May 2026` (or `2026`), `VBS`, `opt-out` (or `April 2026`).
  - **V-54-13 Driver/firmware H2 presence (per PATCH-03):** `01-windows-wufb-rings.md` contains literal H2 covering driver/firmware update policy + `dual-scan` token (or equivalent for source-conflict pitfall).
  - **V-54-14 macOS Deadlines H2 + Apple OS 26 three-layer Layer 1 (per D-12 + D-13):** `02-macos-update-enforcement.md` contains literal H2 `## Deadlines & Cutover Dates` AND Apple OS 26 legacy-command table row contains literal `**[HARD-DEADLINE]**`.
  - **V-54-15 macOS Apple OS 26 three-layer Layer 2 (per D-13):** `02-macos-update-enforcement.md` contains literal `> ⚠️ **Hard deadline (Apple OS 26):**` blockquote with all 3 legacy-command tokens (`forceDelayedSoftwareUpdates`, `com.apple.SoftwareUpdate`, `ScheduleOSUpdate`) AND `DDM` AND `Apple OS 26` within the blockquote.
  - **V-54-16 macOS Apple OS 26 three-layer Layer 3 (per D-13):** `02-macos-update-enforcement.md` contains ≥2 per-occurrence `[HARD-DEADLINE` inline reminders adjacent to legacy-command tokens.
  - **V-54-17 macOS DDM-only literal coverage (per SC#2 + PATCH-04):** `02-macos-update-enforcement.md` contains literal `Software Update Enforce Latest` AND `Intune Settings Catalog` AND `DDM` AND `forward-compatible` (or equivalent token).
  - **V-54-18 iOS DDM-on-unsupervised retraction literal coverage (per SC#3 + PATCH-05):** `03-ios-update-lifecycle.md` contains literal `unsupervised` AND `iOS 17` AND `August 2025` (or `Aug 2025`) AND at least 2 of 4 DDM update keys (`TargetOSVersion`, `TargetBuildVersion`, `TargetLocalDateTime`, `OfferPrograms`).
  - **V-54-19 PATCH-06 retrofit cell-text NEGATIVE+POSITIVE pair (per D-07 + SC#3 conjunct b):** `docs/admin-setup-ios/07-device-enrollment.md:35` cell does NOT contain literal `Supervised-only in iOS 17+ enforcement policies` (NEGATIVE regression-guard) AND DOES contain regex `(?i)DDM.*unsupervised.*iOS\s*17` AND a cross-link to `03-ios-update-lifecycle.md`.
  - **V-54-20 PITFALL-13 forward-link at 04:128 (per D-08):** `docs/admin-setup-ios/04-configuration-profiles.md` contains a cross-link to `03-ios-update-lifecycle.md` AND existing literal `supervised and unsupervised devices (iOS 17.0+)` is preserved byte-identical.
  - **V-54-21 REQ/ROADMAP errata literal-purge (per D-09):** `.planning/REQUIREMENTS.md` AND `.planning/ROADMAP.md` contain ZERO occurrences of literal `05-compliance-policy.md` (off-by-one fully purged across the planning corpus).
  - **V-54-22 Android Deadlines H2 + MEETS_STRONG_INTEGRITY three-layer Layer 1 (per D-12 + D-13):** `04-android-patch-delivery.md` contains literal H2 `## Deadlines & Cutover Dates` AND integrity-attestation table row contains literal `**[HARD-DEADLINE]**`.
  - **V-54-23 Android MEETS_STRONG_INTEGRITY three-layer Layer 2 (per D-13):** `04-android-patch-delivery.md` contains literal `> ⚠️ **Hard deadline (Oct 31 2026):**` blockquote with all 3 cascade dates (`May 2025`, `September 30 2025` or `Sept 30 2025`, `October 31 2026` or `Oct 31 2026`) AND `Android 13+` AND `12 months` (security patch age).
  - **V-54-24 Android MEETS_STRONG_INTEGRITY three-layer Layer 3 (per D-13):** `04-android-patch-delivery.md` contains ≥2 per-occurrence `[HARD-DEADLINE` inline reminders adjacent to MEETS_STRONG_INTEGRITY mentions.
  - **V-54-25 Android Zebra LifeGuard + Samsung KSP coverage (per SC#4 + PATCH-08):** `04-android-patch-delivery.md` contains literal `Zebra LifeGuard` AND `January 2026` (or `Jan 2026`) AND `Samsung KSP` (or `Knox Service Plugin`) AND framing as analog OEM mechanism.
  - **V-54-26 Cross-platform inline blockquote at TOP (per D-04 + 2B-prime winner):** each of 5 patch-management files contains literal `> **Platform applicability:**` blockquote within first 50 lines of file body (post-frontmatter).
  - **V-54-27 Bare-`> **Platform:**` NEGATIVE regression-guard (per D-04):** zero files (across the entire `docs/` + `.planning/` tree) match literal `> **Platform:**` (with no qualifier word) — lexicon-family preservation.
  - **V-54-28 ops/00-index.md NOT amended NEGATIVE regression-guard (per DPO-Phase53-01):** `docs/operations/00-index.md` does NOT contain `## Patch Management` H2 (Phase 53 owns the file; Phase 54 cross-references only). Phase 59 will add this H2 in its own commit per ROADMAP line 463.
  - **V-54-29 Anti-scope-creep regression-guard at 00-overview (per D-01):** `00-overview.md` does NOT contain `Hotpatch`, `VBS`, `opt-out`, `MEETS_STRONG_INTEGRITY` (full literal — 1+ occurrences in body content beyond ring-disambiguation H2 + cross-platform comparison table cells). REQ traceability firewall enforcement.
  - **V-54-30 TBD/TODO/PLACEHOLDER scan (per V-53-25 inheritance):** none of Phase 54 files contain literal `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` outside Version History tables.
  - **V-54-31 SC#5 multi-platform frontmatter (per ROADMAP line 269):** all 5 patch-management ops files carry valid `platform:` frontmatter; 00-overview's value is whatever D-17 + CD-01 resolves.
  - **V-54-32 v1.4.1 atomicity lesson cross-link (per D-10 + RETROSPECTIVE.md line 166):** plan-time check that all retrofit files (07:35 + 04:128 + REQ + ROADMAP errata) are staged in same commit as new patch-management files (verified at pre-commit gate; not a runtime literal-pin assertion).
- **D-18:** **`check-phase-54.mjs` implementation pattern matches `check-phase-53.mjs` / `check-phase-52.mjs` / `check-phase-51.mjs` / `check-phase-50.mjs` / `check-phase-49.mjs`** (Phase 48 D-25 + lineage). File-reads-only / no-shared-module; markdown parsing is regex-based.
- **D-19:** **All 5 patch-management content files use single-string `platform:` frontmatter (per Phase 53 D-12 inheritance lineage).** 01 = `Windows`, 02 = `macOS`, 03 = `iOS`, 04 = `Android`. 00-overview.md is a sub-decision (CD-01) — recommended `platform: cross-platform` (intentional new token for cross-platform overview shape only) per SC#5 ROADMAP line 269 verbatim "correct multi-platform frontmatter." NOTE on C10: the harness C10 check has hardcoded scope `linuxDocPaths()` per `scripts/validation/v1.5-milestone-audit.mjs` lines 141-178; operations/patch-management/ files are NOT in C10 scope, so frontmatter is enforced ONLY by the local V-54-07 validator assertion, not by C10 (CDI-Phase54-04 inheritance from CDI-Phase53-06).
- **D-20:** **Hardcoded H2/anchor strings + literal-token regexes pinned in BOTH the validator (`check-phase-54.mjs`) and CONTEXT.md.** Brittleness trade-off accepted per Phase 49 D-25 + Phase 50 D-25 + Phase 51 D-21 + Phase 52 D-12 + Phase 53 D-13 precedent. Renaming any pinned H2, deadline date, legacy command token, or DDM key requires same-commit validator update.
- **D-21:** **Commit atomicity = SINGLE atomic commit** per Phase 51 D-22 + Phase 52 D-13 + Phase 53 D-14 + CDI-Phase54-05 inheritance + ROADMAP line 271 v1.4.1 atomicity lesson. The PATCH-06 same-commit-atomic mandate at `07-device-enrollment.md:35` + REQ/ROADMAP errata + new `03-ios-update-lifecycle.md` content + new `04-configuration-profiles.md:128` forward-link FORCE single-commit landing — splitting any of these into separate commits violates ROADMAP:271 "no separate commit for the retrofit" mandate. Single atomic commit covers: 5 new content files (patch-management/00-overview + 01-windows-wufb-rings + 02-macos-update-enforcement + 03-ios-update-lifecycle + 04-android-patch-delivery) + 1 retrofit cell edit (admin-setup-ios/07-device-enrollment.md:35) + 1 forward-link addition (admin-setup-ios/04-configuration-profiles.md:128) + REQ + ROADMAP errata + 1 validator (check-phase-54.mjs).

### Plan-level ordering and atomicity

- **D-22:** **Phase 54 plan order (single-commit atomicity per D-21):**
  1. **Authoring wave (parallel-safe across files; sequential within hot-spot):** All content + retrofit + validator can be authored concurrently in worktrees. Phase 54 has `Depends on: Phase 53` per ROADMAP line 262 (operations/00-index.md must exist for cross-reference). Phase 53 closed on 2026-04-27. Suggested plan-level decomposition:
     - 54-01: `docs/operations/patch-management/00-overview.md` (D-01 1D Hybrid scope; cross-platform comparison table + PITFALL-9 ring-disambiguation H2 + deferral-vs-enforcement terminology + routing cross-links to 01-04 + cross-platform inline blockquote per D-04 + frontmatter platform-tag per D-19 CD-01)
     - 54-02: `docs/operations/patch-management/01-windows-wufb-rings.md` (PATCH-01/02/03 fold; D-12 NO Deadlines H2; PATCH-02 Hotpatch H2 + PATCH-03 driver/firmware H2 + PITFALL-9 strict per-occurrence ring qualifier + cross-platform inline blockquote per D-04 + frontmatter `platform: Windows`)
     - 54-03: `docs/operations/patch-management/02-macos-update-enforcement.md` (PATCH-04; D-12 YES Deadlines H2 + D-13 HARD-DEADLINE three-layer for Apple OS 26 removal + V-54-15/16/17 literal coverage + cross-platform inline blockquote + frontmatter `platform: macOS`)
     - 54-04: `docs/operations/patch-management/03-ios-update-lifecycle.md` (PATCH-05; D-12 NO H2; single inline `> ⚠️` blockquote at supervised-only retraction site + V-54-18 literal coverage + cross-platform inline blockquote + frontmatter `platform: iOS`)
     - 54-05: `docs/operations/patch-management/04-android-patch-delivery.md` (PATCH-07/08 fold; D-12 YES Deadlines H2 + D-13 HARD-DEADLINE three-layer for MEETS_STRONG_INTEGRITY Oct 2026 + Zebra LifeGuard H2 sub-section per PATCH-08 + Samsung KSP analog reference + V-54-22/23/24/25 literal coverage + cross-platform inline blockquote + frontmatter `platform: Android`)
     - 54-06: PATCH-06 retrofit at `docs/admin-setup-ios/07-device-enrollment.md:35` (D-07 cell edit; V-54-19 NEGATIVE+POSITIVE pair literal pin)
     - 54-07: PITFALL-13 forward-link at `docs/admin-setup-ios/04-configuration-profiles.md:128` (D-08; V-54-20 byte-identical preservation + cross-link literal)
     - 54-08: REQ + ROADMAP errata at `.planning/REQUIREMENTS.md:55` + `.planning/REQUIREMENTS.md:166` + `.planning/ROADMAP.md:267` (D-09; V-54-21 NEGATIVE literal-purge regression-guard)
     - 54-09: `scripts/validation/check-phase-54.mjs` (D-17 V-54-01..32 structural assertions; D-18 file-reads-only pattern; estimated 28-32 checks)
  2. **Pre-commit gate:**
     - `node scripts/validation/check-phase-54.mjs` exits 0 (all 28-32 V-54-NN checks pass)
     - `node scripts/validation/v1.5-milestone-audit.mjs --verbose` exits 0 (C1-C12 PASS; C13 informational PASS-or-noise within accepted tolerance per Phase 48 D-08; C11 informational — no false positives expected at Phase 54 ship per CDI-Phase54-03)
     - `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0 (regression-prevention; should be unaffected since Phase 54 files are not in supervision sidecar)
     - `markdown-link-check` against the 5 new content files + 2 retrofit targets (informational; not blocking per Phase 48 D-08)
     - Verify all 5 new files contain `last_verified` + `review_by` frontmatter on 60-day cycle
     - Verify all 5 new files contain `> **Platform applicability:**` blockquote at TOP (per D-04 + V-54-26)
  3. **Single atomic commit:** All 5 new patch-management files + retrofit cell edit + forward-link + REQ + ROADMAP errata + validator per D-21 + commit message (suggested):
     ```
     docs(54): patch-management 5-file suite + PATCH-06 v1.3 retrofit + check-phase-54 validator

     - 5 new ops files: operations/patch-management/00-overview through 04-android-patch-delivery
     - PATCH-06 surgical retrofit at admin-setup-ios/07-device-enrollment.md:35
       (retract supervised-only-DDM cell; DDM works on unsupervised iOS 17+ post-Aug 2025)
     - PITFALL-13 forward-link addition at admin-setup-ios/04-configuration-profiles.md:128
     - REQ/ROADMAP errata: 05-compliance-policy.md (off-by-one, fictional target) →
       07-device-enrollment.md (actual residual claim site) at REQ:55, REQ:166, ROADMAP:267
     - HARD-DEADLINE three-layer callouts: macOS Apple OS 26 + Android MEETS_STRONG_INTEGRITY
     - Per v1.4.1 RETROSPECTIVE lesson 166: atomic same-commit forward-promise closure
     ```
  4. **Post-commit verification:** Re-run `check-phase-54.mjs` + `v1.5-milestone-audit.mjs` from clean workspace.
  5. **VERIFICATION.md authored** (separate commit per Phase 49/50/51/52/53 close pattern) documenting validator pass + DPO inheritance verification + 5 SC# satisfaction proof + DPO-Phase54-NN propagation summary for Phase 55+ plan authors.

### Claude's Discretion

- **CD-01:** Frontmatter `platform:` value for `00-overview.md` — `cross-platform` (single new token) OR `Windows,macOS,iOS,Android` (comma-string aligning with SC#5 ROADMAP line 269 "correct multi-platform frontmatter"). Validator V-54-07 + V-54-31 accept either; recommendation: `platform: cross-platform` (cleaner, single-token forward-compatible with future Phase 60 C-check expansion). Plan-author discretion.
- **CD-02:** Exact wording of the 4 per-platform sub-bullets inside the `> **Platform applicability:**` blockquote on each per-platform file (D-04 specifies form + back-link to 00-overview; specific verb choice and brevity is plan-author discretion within "files all carry their own platform; pointer-only routing" trim rationale).
- **CD-03:** Exact column count of the cross-platform comparison table in `00-overview.md` (D-01 mandates 4-platform × 5-row minimum; plan-author may add 6th row for "Hard deadline" or 7th row for "Validator anchor" at discretion). Validator V-54-08 accepts any orientation.
- **CD-04:** Whether `00-overview.md` includes a Mermaid diagram (e.g., 4-platform-cadence-comparison or update-flow-visualization) — Phase 53 author discretion CD-04 inheritance. Validator does NOT assert Mermaid presence.
- **CD-05:** PITFALL-9 ring-disambiguation H2 placement order in `00-overview.md` — first H2 (post-applicability blockquote) vs deeper H2 — plan-author discretion; PITFALLS line 200 says "opening section of the patch management doc" which the 00-overview as-the-suite-entry-point satisfies regardless of intra-file H2 ordering.
- **CD-06:** Exact wording of the macOS Apple OS 26 Layer 2 blockquote (D-13 specifies form + 3 legacy-command tokens + DDM-forward-path mandate; specific phrasing within the Apple-OS-26-removal narrative is plan-author discretion).
- **CD-07:** Exact wording of the Android MEETS_STRONG_INTEGRITY Layer 2 blockquote (D-13 specifies form + 3 cascade dates + Android 13+ patch-age requirement; specific phrasing within the cascade-timeline narrative is plan-author discretion).
- **CD-08:** Whether the macOS Deadlines H2 includes a separate row for "iOS unsupervised DDM retraction (Aug 2025) — see iOS guide" cross-platform pointer, OR whether the cross-platform comparison table in 00-overview suffices for surfacing this — plan-author discretion. Same for whether the Android Deadlines H2 cross-references macOS Apple OS 26 timeline.
- **CD-09:** Exact text of the cell at `07-device-enrollment.md:35` post-edit (D-07 specifies retraction content + cross-link contract; specific wording is plan-author discretion within the DDM-on-unsupervised-iOS-17+ correction + legacy-restriction-still-supervised-only contract).
- **CD-10:** Whether the 03-ios-update-lifecycle.md `> ⚠️` inline blockquote at the supervised-only-DDM-retraction site uses literal `> ⚠️ **August 2025 retraction:**` framing OR `> ⚠️ **Update enforcement scope (iOS 17+):**` framing — plan-author discretion. Validator V-54-18 accepts either via literal-token coverage of `unsupervised` + `iOS 17` + August 2025 date.
- **CD-11:** Whether `00-overview.md` carries `audience: admin` (default per Phase 53 D-12 lineage) OR `audience: admin,L2` (cross-platform overview may serve L2 escalation cross-references) — plan-author discretion. Validator V-54-07 accepts any non-empty `audience:` field.

### Cross-decision integration callouts (Referee Part C)

- **CDI-Phase54-01:** **D-04 (cross-platform inline at top of all 5 patch-management files) × D-21 (single atomic commit) × V-54-26/27.** Plan author for 54-01..05 MUST include `> **Platform applicability:**` blockquote at TOP of every file (per Phase 53 D-08 token inheritance + 2B-prime winner). V-54-26 enforces presence; V-54-27 enforces NEGATIVE regression-guard against bare `> **Platform:**` token (lexicon-family preservation). The bare-noun token is a load-bearing FORBIDDEN pattern per Adversary disprove rejection.
- **CDI-Phase54-02:** **D-13 (HARD-DEADLINE three-layer for macOS + Android) × Phase 53 D-05 + Phase 52 D-01 inheritance + DPO-Phase52-07 cross-domain transferability.** Phase 54 HARD-DEADLINE three-layer (table-cell + blockquote + per-occurrence inline) inherits the Phase 52 freshness-caveat / Phase 53 EP HIGH-RISK layered-defense pattern verbatim, transferred to deadline-callout domain. Plan authors for 54-03 (macOS) and 54-05 (Android) MUST implement all three layers; V-54-14/15/16 (macOS) and V-54-22/23/24 (Android) enforce per-layer.
- **CDI-Phase54-03:** **D-16 (PITFALL-13 allowlist deferred) × Phase 53 D-06 + Phase 48 D-15 YAGNI inheritance.** Phase 54 inherits Phase 53's PITFALL-13 lazy-add policy. PITFALL-13 same-commit-allowlist seeding NOT required at Phase 54 ship per file-path-prefix scope restriction to `docs/operations/patch-management/`. If new false positives surface during execution, lazy-add per Phase 48 D-15 YAGNI. Phase 60 (audit harness finalization) handles allowlist seeding when C11 promotes from informational to blocking.
- **CDI-Phase54-04:** **D-19 (`platform:` frontmatter for all 5 patch-management files) × C10 hardcoded `linuxDocPaths()` × harness C-check expansion at Phase 60.** Phase 54 frontmatter is enforced ONLY by V-54-07 local assertion. C10 doesn't run on operations/patch-management/ files (verified at `scripts/validation/v1.5-milestone-audit.mjs` lines 141-178). Phase 60 (audit harness finalization) MAY expand a C-check to ops files; if so, Phase 60 plan author handles allowlist seeding per Phase 48 D-15 lazy-add at THAT time, not at Phase 54. Phase 54 is forward-compatible.
- **CDI-Phase54-05:** **D-21 (single atomic commit) × D-10 (PATCH-06 same-commit-atomic) coupling.** ROADMAP line 271 ("PATCH-06 iOS v1.3 retrofit MUST be same-commit-atomic with new iOS DDM enforcement guide content. No separate commit for the retrofit.") FORCES single-commit landing per v1.4.1 atomicity lesson. The validator's V-54-19 retrofit-cell-text NEGATIVE+POSITIVE pair assertion FORCES atomic landing — splitting the commit would FAIL the validator in commit-1 because either (a) the new iOS guide hasn't landed (POSITIVE half fails) OR (b) the retrofit cell hasn't been edited (NEGATIVE half fails). Plan author for 54-04 + 54-06 MUST stage these in the same atomic commit; pre-commit hook enforces.
- **CDI-Phase54-06:** **D-09 (REQ + ROADMAP errata in same commit) × V-54-21 NEGATIVE literal-purge.** The `05-compliance-policy.md` literal must be PURGED from REQUIREMENTS.md and ROADMAP.md in the same atomic commit as the retrofit edit at 07:35. V-54-21 NEGATIVE-regression-guard enforces purge; future audit re-reading REQ literals must resolve to the actual file. Without same-commit errata, F-3B-01 (REQ literal mismatch unresolved) re-emerges as a real defect.
- **CDI-Phase54-07:** **D-04 (cross-platform inline blockquote per file) × D-12 (per-file Deadlines H2 differentiated) × V-54-26/14/22 coupling.** Each per-platform file's TOP blockquote is short pointer-routing only (D-04); the macOS + Android Deadlines H2 surfaces (D-12) live in body content. The two are NOT coupled at the same surface; Phase 52 D-01 differentiated-shape rationale is satisfied (TOP blockquote = pointer routing; mid-file `## Deadlines & Cutover Dates` = full table + three-layer + sub-rows). No same-shape collision risk.
- **CDI-Phase54-08:** **D-01 (00-overview Hybrid 1D scope) × D-29 anti-scope-creep regression-guard × REQ traceability firewall.** REQ traceability table (REQUIREMENTS lines 161-168) maps every PATCH-01..08 to a specific per-platform file; ZERO PATCH requirements are mapped to 00-overview. Plan author for 54-01 MUST NOT include Hotpatch + VBS + opt-out (PATCH-02 territory in 01-windows-wufb-rings.md) or MEETS_STRONG_INTEGRITY full timeline (PATCH-07 territory in 04-android-patch-delivery.md) in 00-overview body content beyond cross-platform comparison table cells. V-54-29 NEGATIVE regression-guard enforces.

### Downstream phase obligations (Referee Part C — for next-phase plan authors)

- **DPO-Phase54-01:** **Phase 55 (App Lifecycle Automation) inherits Phase 54 cross-platform inline blockquote pattern.** Phase 55 documentation under `docs/operations/app-lifecycle/` SHOULD adopt the same `> **Platform applicability:**` token reuse + 4-file-per-platform shape with 00-overview hub. NOT mandated; per Phase 53 DPO-Phase53-02 inheritance (per-phase author discretion within methodology family). Phase 55 plan author considers adopting the same shape for v1.5 ops-domain consistency.
- **DPO-Phase54-02:** **Phase 55 + Phase 56 inherit Phase 54 ops/00-index.md cross-reference contract.** Phase 55 + Phase 56 documentation under `docs/operations/app-lifecycle/` and `docs/operations/drift-migration/` MUST cross-reference `docs/operations/00-index.md` from their own files; MUST NOT amend `00-index.md` per ROADMAP line 457 verbatim "Phase 53 creates; Phases 54–56 cross-reference only." Phase 59 (Hub Navigation Integration) appends Operations H2 to `docs/index.md` linking to `00-index.md` AND adds Phase 54/55/56 sub-references in `00-index.md` body content.
- **DPO-Phase54-03:** **Phase 56 (Drift Detection + Tenant Migration) MAY cross-reference Phase 54 deadlines for compliance-drift signals.** Phase 56 ROADMAP line 293 covers cross-platform compliance drift signals (Android Play Integrity verdict change among others). Phase 56 plan author should consider cross-referencing Phase 54 `04-android-patch-delivery.md` MEETS_STRONG_INTEGRITY HARD-DEADLINE three-layer surface as a drift-signal source. NOT mandated; per-phase author discretion.
- **DPO-Phase54-04:** **Phase 60 (Audit Harness v1.5 Finalization) registers `check-phase-54.mjs` in CI workflow + runs C13 broken-link automation against Phase 54 cross-link surface.** Phase 60 plan author adds `check-phase-54.mjs` to `.github/workflows/audit-harness-v1.5-integrity.yml` per AUDIT-06 contract. C13 informational broken-link automation runs against Phase 54 cross-link surface (~15-20 cross-links per file: forward-links to per-platform files + back-links to 00-overview + cross-platform inline blockquote sub-bullets + Phase 53 cross-management cross-references + Apple Learn/Microsoft Learn/Google Developer external URLs). Phase 60 plan author runs C13 sweep against Phase 54 outputs as part of v1.5 close audit. **Optional sidecar amendment**: if Phase 60 expands C10 scope to operations/patch-management/ files, lazy-add `c10_ops_allowlist[]` sidecar entry per Phase 48 D-15 YAGNI at THAT time (NOT at Phase 54 ship per CDI-Phase54-04). Also: if Phase 60 expands C11 ops-domain regex to flag deadline-token clusters, sidecar `c11_patch_deadline_allowlist[]` lazy-add per CDI-Phase54-03.
- **DPO-Phase54-05:** **Phase 58 (DEFER-08 4-platform capability comparison) consumes Phase 54 anchors for Software Updates domain row.** ROADMAP line 322 lists 6 canonical domain axes (Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access) — Software Updates IS in the canonical 6-axis list, so Phase 58 WILL consume Phase 54 anchors. Link-not-copy contract (PITFALL-7) means cells must hyperlink to Phase 54 H2 anchors (`00-overview.md#cross-platform-comparison`, `00-overview.md#ring-terminology`, `01-windows-wufb-rings.md#hotpatch`, `01-windows-wufb-rings.md#driver-firmware-policy`, `02-macos-update-enforcement.md#deadlines-cutover-dates`, `03-ios-update-lifecycle.md` (full file), `04-android-patch-delivery.md#deadlines-cutover-dates`, `04-android-patch-delivery.md#zebra-lifeguard`) — NOT duplicate prose. Phase 58 plan author MUST verify these anchors stable post-Phase-54 close.
- **DPO-Phase54-06:** **Phase 59 (Hub Navigation Integration) appends operations Patch Management H2 to `docs/index.md` linking to `00-overview.md` + `00-index.md`.** Phase 59 ownership per ROADMAP line 457 navigation-files-last principle. Phase 54 does NOT touch `docs/index.md`. Phase 59 also adds patch-management quick-ref entries in `docs/quick-ref-l2.md` Operations sections IF those exist (per Phase 59 ROADMAP line 332-336 hub integration scope).
- **DPO-Phase54-07:** **Phase 54 internal — single-commit atomicity (per D-21 + CDI-Phase54-05).** Phase 54 ships in a single atomic commit because (a) the validator's V-54-19 retrofit-cell-text NEGATIVE+POSITIVE pair forces atomic PATCH-06 landing, (b) the validator's V-54-21 REQ/ROADMAP errata literal-purge NEGATIVE regression-guard forces same-commit errata, and (c) ROADMAP line 271 explicitly mandates "no separate commit for the retrofit." Goldilocks-rule per Phase 51 DPO-07 + Phase 52 DPO-Phase52-06 + Phase 53 DPO-Phase53-07 precedent + RETROSPECTIVE.md line 166 v1.4.1 lesson "Atomic same-commit forward-promise closure beats deferred retrofits — Validated 6× in v1.4.1."

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents (researcher, planner, executor, reviewer) MUST read these before authoring or implementing.**

### Phase 54 success criteria + traceability (authoritative scope)

- `.planning/ROADMAP.md` §"Phase 54: Patch & Update Management" (lines 260-272) — Goal, dependencies, requirements (PATCH-01..08), 5 success criteria, methodology notes (DDM, MEETS_STRONG_INTEGRITY timeline, Hotpatch May 2026, Zebra LifeGuard, Apple OS 26 deprecation)
- `.planning/ROADMAP.md` line 265 — SC#1 verbatim: every "ring" qualified as "WUfB deployment ring" or "Autopatch ring" + driver/firmware separate from quality/feature rings + mutual-exclusion explicit + Hotpatch H2 with VBS prereqs (PATCH-02, PITFALL-9)
- `.planning/ROADMAP.md` line 266 — SC#2 verbatim: macOS legacy MDM commands (`forceDelayedSoftwareUpdates`, `com.apple.SoftwareUpdate` payload, `ScheduleOSUpdate`) deprecated AND removed with Apple OS 26 — DDM "Software Update Enforce Latest" only forward-compatible enforcement path
- `.planning/ROADMAP.md` line 267 — SC#3 verbatim: iOS supervised-only DDM constraint retracted for iOS 17+ (Aug 2025) — DDM update keys work on UNSUPERVISED devices; AND v1.3 supervised-only-DDM callout surgically retrofitted same-commit (PATCH-06 atomic per v1.4.1 lesson)
- `.planning/ROADMAP.md` line 268 — SC#4 verbatim: Android MEETS_STRONG_INTEGRITY enforcement timeline (May 2025 / Sept 30 2025 / Oct 31 2026 fleet deadline) + Android 13+ ≤12 month patch + Zebra LifeGuard OTA via Intune (GA Jan 2026) + Samsung KSP analog OEM mechanism
- `.planning/ROADMAP.md` line 269 — SC#5 verbatim: `check-phase-54.mjs` passes; all patch-management ops files carry correct multi-platform frontmatter
- `.planning/ROADMAP.md` line 271 — Critical note: PATCH-06 same-commit-atomic mandate
- `.planning/ROADMAP.md` line 272 — Parallelism note: Phases 54, 55, 56 disjoint file sets within operations/ subdirectories; Wave B after Phase 53
- `.planning/ROADMAP.md` line 457 — Shared Write Hotspot Ownership row for `docs/operations/00-index.md`: "Phase 53 creates; Phases 54-56 cross-reference only" (load-bearing for DPO-Phase54-02 + V-54-28)
- `.planning/REQUIREMENTS.md` §PATCH-01..08 (lines 50-57) — full requirement text with all date literals + token literals
- `.planning/REQUIREMENTS.md` lines 161-168 — Traceability mapping per requirement (load-bearing for D-22 plan order + D-29 anti-scope-creep regression-guard):
  - PATCH-01 → `operations/patch-management/01-windows-wufb-rings.md` (WUfB ring topology + Autopatch disambiguation + PITFALL-9)
  - PATCH-02 → Hotpatch H2 inside `01-windows-wufb-rings.md` (default May 2026 + VBS + opt-out toggle 2c FOLD)
  - PATCH-03 → Driver/firmware update policy H2 inside `01-windows-wufb-rings.md` (separate from quality/feature rings)
  - PATCH-04 → `operations/patch-management/02-macos-update-enforcement.md` (DDM only; legacy MDM commands deprecated + removed Apple OS 26)
  - PATCH-05 → `operations/patch-management/03-ios-update-lifecycle.md` (supervised-only DDM constraint retracted iOS 17+)
  - PATCH-06 → `docs/admin-setup-ios/05-compliance-policy.md` (LITERAL OFF-BY-ONE; D-09 errata corrects to `docs/admin-setup-ios/07-device-enrollment.md` per file-system verification)
  - PATCH-07 → `operations/patch-management/04-android-patch-delivery.md` (MEETS_STRONG_INTEGRITY enforcement timeline + Android 13+)
  - PATCH-08 → Zebra LifeGuard OTA section inside `04-android-patch-delivery.md` (GA January 2026 + Samsung KSP analog)
- `.planning/REQUIREMENTS.md` §AUDIT-06 (line 87) — Validator-as-deliverable + CI registration
- `.planning/REQUIREMENTS.md` §"Out of Scope" (lines 104-117) — explicit exclusions

### v1.5 research (authoritative for ConfigMgr scope, pitfalls, and tooling)

- `.planning/research/SUMMARY.md` lines 38-46 — DDM (current canonical enforcement path) + iOS unsupervised DDM (Aug 2025) + MEETS_STRONG_INTEGRITY timeline (May 2025 / Sept 30 2025 / Oct 31 2026) + Zebra LifeGuard GA Jan 2026 + Hotpatch May 2026
- `.planning/research/SUMMARY.md` line 138 — iOS DDM v1.3 retrofit scope (small but mandatory)
- `.planning/research/SUMMARY.md` lines 187-193 — Phase 54 deliverable framing (5 files; macOS deferral-vs-enforcement; iOS retraction; Android MEETS_STRONG_INTEGRITY + OEM delays; Windows WUfB-vs-Autopatch + Hotpatch)
- `.planning/research/SUMMARY.md` line 190 — Critical signal: iOS unsupervised DDM retraction requires targeted retrofit callout to existing v1.3 iOS compliance guide (assigned in Phase 54)
- `.planning/research/SUMMARY.md` lines 248-249 — Research flags: verify Intune Settings Catalog path for Software Update Enforce Latest at plan time; verify Zebra LifeGuard OTA admin center path at plan time
- `.planning/research/SUMMARY.md` lines 266-269 — Confidence table: HIGH for DDM + iOS unsupervised DDM + MEETS_STRONG_INTEGRITY enforcement timeline
- `.planning/research/SUMMARY.md` line 284 — iOS DDM v1.3 retrofit explicit assignment to Phase 54
- `.planning/research/PITFALLS.md` §Pitfall 9 (lines 191-205) — **WUfB Ring Naming Collision: Autopatch Rings vs WUfB Rings** — load-bearing pitfall for Phase 54. Drives: (1) PITFALL-9 ring-disambiguation H2 in 00-overview (D-01 + V-54-09); (2) strict per-occurrence ring qualifier in `01-windows-wufb-rings.md` (V-54-11 POSITIVE + NEGATIVE dual-defense). Mitigation prose at line 200: "Every mention of 'ring' in the patch management docs must be qualified: 'WUfB deployment ring' or 'Autopatch ring' — never bare 'ring.' The opening section of the patch management doc must disambiguate the two systems explicitly, including the mutual-exclusion."
- `.planning/research/PITFALLS.md` §Pitfall 13 (lines 278-295) — **Ops-domain false-positive risk** — relevant to V-54-NN allowlist deferral (CDI-Phase54-03). Pitfall mandate at line 287: "every new token-scan check ... must either: (a) ship informational-first with a documented promotion date, or (b) ship with a context window check ... must have an allow-list from day one, seeded with known-legitimate occurrences."
- `.planning/research/PITFALLS.md` line 399 — Patch management guide: WUfB rings and Autopatch rings unambiguously labeled with system qualifier in every occurrence (drives V-54-11 strict regex)
- `.planning/research/PITFALLS.md` line 436 — Pitfall-to-mitigation table: P9 → ops-depth patch management phase (Phase 54)

### v1.5 audit harness + sidecar (Phase 48 + Phase 51-53 deliverables; Phase 54 consumes)

- `scripts/validation/v1.5-milestone-audit.mjs` — C1-C13 checks. C10 hardcoded scope `linuxDocPaths()` at lines 141-178 (verified during scout; load-bearing for D-19 + CDI-Phase54-04 — operations/patch-management/ files NOT in C10 scope). C11 ops-domain anti-pattern at lines 463-489 (informational-first; promotes blocking at Phase 60).
- `scripts/validation/v1.5-audit-allowlist.json` — sidecar with 7 top-level keys. Phase 54 does NOT amend the sidecar at ship per Phase 48 D-15 YAGNI lazy-add (no false-positive surface). Lazy-add deferred to first-occurrence per CDI-Phase54-03.
- `scripts/validation/regenerate-supervision-pins.mjs` — `--report` mode invoked pre-commit; `--self-test` invoked post-commit; should be unaffected since Phase 54 files are not in supervision sidecar.
- `scripts/validation/check-phase-49.mjs` / `check-phase-50.mjs` / `check-phase-51.mjs` / `check-phase-52.mjs` / `check-phase-53.mjs` — direct file-reads-only / regex-based pattern reference for `check-phase-54.mjs` implementation per D-18.

### Phase 51 / Phase 52 / Phase 53 methodology precedent (Phase 54 inherits patterns)

- `.planning/phases/51-linux-l1-triage-runbooks-30-33/51-CONTEXT.md` D-08 (platform-gate banner pattern; rejected for Phase 54 per F-2D-01 confirming Phase 53 D-08 admin-audience precedent) / V-51-09 + V-51-19 (POSITIVE+NEGATIVE dual-defense pattern; inherited by V-54-11 ring-qualifier and V-54-19 retrofit cell-text NEGATIVE+POSITIVE pair) / D-22 (single atomic commit Goldilocks-rule)
- `.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-CONTEXT.md` D-01 (three-layer freshness-caveat pattern: matrix-cell + adjacent blockquote + per-occurrence inline; **directly inherited by D-13** for HARD-DEADLINE three-layer per CDI-Phase54-02 + Adversary cross-check confirming same-shape across phases is endorsed precedent) / D-04 (frontmatter C10 contract; informs D-19 local enforcement) / D-10 (V-52-NN full validator scope precedent ~22 checks; informs D-17 28-32 V-54-NN) / D-11 (file-reads-only / no-shared-module pattern; inherited by D-18) / D-13 (single atomic commit forced by validator append-assertion; inherited by D-21) / DPO-Phase52-07 (three-layer pattern explicitly documented as cross-domain transferable; **load-bearing for 4C+4A hybrid winner adoption**)
- `.planning/phases/53-co-management-operational-docs/53-CONTEXT.md` D-01 (4-file split + 00-overview concept hub shape; informs D-01 1D Hybrid scope) / D-05 (three-layer EP HIGH-RISK; **directly inherited by D-13** for HARD-DEADLINE callout per CDI-Phase54-02) / D-06 (PITFALL-13 same-commit-allowlist deferred under YAGNI; inherited by D-16 + CDI-Phase54-03) / D-08 (cross-platform inline blockquote at TOP; **directly inherited by D-04** for `> **Platform applicability:**` token reuse on per-platform files) / D-12 (frontmatter `platform: <single-string>`; inherited by D-19) / D-14 (single atomic commit forced by validator; inherited by D-21) / DPO-Phase53-01 (operations/00-index.md cross-reference contract; inherited by DPO-Phase54-02 + V-54-28) / DPO-Phase53-02 (cross-platform inline-blockquote pattern as candidate shape; **adopted by Phase 54 D-04 with token discipline**) / V-53-25 (TBD/TODO/FIXME/XXX/PLACEHOLDER scan; inherited by V-54-30) / 53-CONTEXT.md line 53 (PITFALL-13 lazy-add documentation; CDI-Phase54-03 inheritance source)

### v1.4.1 RETROSPECTIVE (atomic same-commit lesson)

- `.planning/RETROSPECTIVE.md` line 166 + line 198 — v1.4.1 lesson "Atomic same-commit forward-promise closure beats deferred retrofits. Validated 6× in v1.4.1." (load-bearing for D-10 + D-21 + CDI-Phase54-05 + CDI-Phase54-06)

### v1.4 / v1.3 / v1.2 admin-setup precedents (Phase 54 retrofit + cross-references)

- `docs/admin-setup-ios/04-configuration-profiles.md:128` — D-08 PITFALL-13 forward-link insertion target. Existing line: "The legacy 'Defer software updates' setting in device restrictions remains supervised-only but is being deprecated by Apple. For software update enforcement, use the dedicated DDM-based path at **Devices** > **Apple updates** > **iOS/iPadOS update policies**, which works on both supervised and unsupervised devices (iOS 17.0+). Software update policies are not covered in this guide." Already substantively correct; D-08 appends forward-link sentence to new `03-ios-update-lifecycle.md`.
- `docs/admin-setup-ios/06-compliance-policy.md` — Verified by file-system scan: ZERO supervised-only-DDM constraint. Line 25 says "DDM-based on iOS 17+" without qualifier. Line 27 only "supervised" mention is about Block App Store / Block installing apps configuration profile context (NOT DDM updates). **NO retrofit edit here.**
- `docs/admin-setup-ios/07-device-enrollment.md:35` — **D-07 PATCH-06 SURGICAL RETROFIT TARGET.** Verbatim cell pre-edit: `| OS update enforcement (forced install deadlines via DDM or device restrictions) | **No** | Supervised-only in iOS 17+ enforcement policies. |`. This IS the actual residual v1.3 supervised-only-DDM-shaped claim. D-07 cell edit lands here.
- `docs/admin-setup-ios/05-app-deployment.md` — Confirmed slot 05 is App Deployment, NOT compliance-policy. REQ literal `05-compliance-policy.md` is off-by-one wrong (D-06 + D-09 errata).

### Phase 53 cross-management cross-references (Phase 54 forward-promise retrofit close)

- `docs/operations/co-management/02-windows-workload-sliders.md` — Phase 53 V-53-23 forward-link to Phase 54 PATCH-02 patch-management surface (Windows Update Policies row). Phase 54 ship retroactively closes this forward-promise (Phase 30 D-22 placeholder retrofit precedent inheritance).
- `docs/operations/co-management/03-cocmgmt-migration-paths.md` — Phase 53 forward-link to Phase 54 patch-management for WUfB-vs-Autopatch ring disambiguation (PITFALL-9). Phase 54 ship retroactively closes this forward-promise.
- `docs/operations/00-index.md` — Phase 53-owned operations-tree index. Phase 54 cross-references FROM patch-management/* files TO this index, NOT the inverse (DPO-Phase53-01 + V-54-28 NEGATIVE regression-guard).

### External canonical Microsoft + Apple + Google documentation (HIGH confidence per SUMMARY.md line 269)

- https://learn.microsoft.com/en-us/intune/intune-service/protect/device-restrictions-ios — iOS device restrictions (legacy `Defer software updates` deprecation context)
- https://learn.microsoft.com/en-us/intune/intune-service/protect/software-updates-guide-ios-ipados — iOS DDM software update enforcement (PATCH-05 source — research SUMMARY 2026-04-15 update)
- https://learn.microsoft.com/en-us/intune/intune-service/protect/deprecated-mdm-policies-macos — macOS legacy MDM update commands deprecation (PATCH-04 source — Apple OS 26 removal context)
- https://learn.microsoft.com/en-us/intune/intune-service/protect/software-updates-guide-macos — macOS DDM Software Update Enforce Latest (PATCH-04 enforcement path source)
- https://learn.microsoft.com/en-us/windows/deployment/windows-autopatch — Windows Autopatch (PATCH-01 + PATCH-02 source; coexistence/disambiguation with WUfB)
- https://learn.microsoft.com/en-us/windows/deployment/update/waas-configure-wufb — WUfB rings configuration (PATCH-01 source)
- https://learn.microsoft.com/en-us/windows/deployment/update/hotpatch-overview — Hotpatch (PATCH-02 source — May 2026 default for Windows 11 Enterprise 24H2+, VBS prereqs, opt-out toggle)
- https://learn.microsoft.com/en-us/windows/deployment/update/waas-driver-wufb — WUfB driver/firmware update policy (PATCH-03 source)
- https://learn.microsoft.com/en-us/intune/intune-service/protect/compliance-policy-create-android — Android compliance policy (PATCH-07 MEETS_STRONG_INTEGRITY context)
- https://developer.android.com/google/play/integrity — Play Integrity API overview (PATCH-07 attestation source)
- https://techcommunity.microsoft.com/category/microsoftintune/blog/intunecustomersuccess — Microsoft Intune TechCommunity blog (MEETS_STRONG_INTEGRITY enforcement dates announcement; SUMMARY.md line 268 HIGH-confidence source)
- https://techdocs.zebra.com/lifeguard/ — Zebra LifeGuard OTA (PATCH-08 source — GA January 2026)
- https://docs.samsungknox.com/admin/knox-platform-for-enterprise/kbas/kba-114-ksp/ — Samsung Knox Service Plugin (PATCH-08 analog OEM mechanism)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **Phase 51-53 V-NN validator-as-deliverable pattern** — `scripts/validation/check-phase-49.mjs` / `check-phase-50.mjs` / `check-phase-51.mjs` / `check-phase-52.mjs` / `check-phase-53.mjs` provide direct implementation templates for `check-phase-54.mjs` (file-reads-only / no-shared-module / regex-based parsing per D-18)
- **Phase 53 D-05 EP HIGH-RISK three-layer pattern** — `docs/operations/co-management/02-windows-workload-sliders.md` provides reference implementation: Layer 1 table-cell `**HIGH-RISK** — see callout`, Layer 2 `> ⚠️ **Endpoint Protection HIGH-RISK:**` blockquote, Layer 3 `[HIGH-RISK — see callout above]` per-occurrence inline. **DIRECTLY INHERITED by D-13** for HARD-DEADLINE callout (macOS Apple OS 26 + Android MEETS_STRONG_INTEGRITY) per CDI-Phase54-02 + DPO-Phase52-07 cross-domain transferability endorsement
- **Phase 53 D-08 cross-platform inline blockquote token** — `> **Platform applicability:**` reused at TOP of all 5 Phase 54 patch-management files per D-04 + 2B-prime winner. Implementation reference: `docs/operations/co-management/00-overview.md:9-20` + `01-windows-tenant-attach.md:9-20` + `02-windows-workload-sliders.md:9-20`
- **Phase 51 V-51-09 + V-51-19 POSITIVE+NEGATIVE dual-defense pattern** — DIRECTLY INHERITED by V-54-11 ring-qualifier (POSITIVE: every "ring" preceded by qualifier within window; NEGATIVE: zero bare-`ring` outside qualifier scope) + V-54-19 retrofit cell-text (POSITIVE: DDM-on-unsupervised regex; NEGATIVE: NO `Supervised-only in iOS 17+` literal)
- **v1.5 audit harness + sidecar** — `scripts/validation/v1.5-milestone-audit.mjs` C1-C13 checks (Phase 54 satisfies C1-C12 blocking; C10 hardcoded `linuxDocPaths()` scope means Phase 54 files NOT enforced by C10 — frontmatter is locally enforced by V-54-07 per CDI-Phase54-04); `regenerate-supervision-pins.mjs --self-test` exits 0 unaffected (Phase 54 files not pinned)

### Established Patterns

- **Single atomic commit Goldilocks-rule** — Phase 50 D-19 → Phase 51 D-22 → Phase 52 D-13 + CDI-Phase52-07 + DPO-Phase52-06 → Phase 53 D-14 + CDI-Phase53-04 → Phase 54 D-21 + CDI-Phase54-05 (validator append-assertions force single commit; PATCH-06 same-commit-atomic mandate at ROADMAP:271 reinforces)
- **File-reads-only / no-shared-module / regex-based validator pattern** — Phase 48 D-25 + Phase 49 D-26 + Phase 51 D-20 + Phase 52 D-11 + Phase 53 D-11 → Phase 54 D-18
- **Frontmatter local contract** — Phase 49 D-04 + Phase 50 + Phase 51 + Phase 52 D-04 + Phase 53 D-12 establish `platform: <single-string>` + `audience: <L1|L2|admin>` + 60-day `last_verified` + `review_by`; Phase 54 D-19 inherits with `platform: Windows|macOS|iOS|Android|cross-platform` + `audience: admin` for all 5 patch-management files
- **Cross-platform inline blockquote at TOP of ops-domain files** (Phase 53 D-08) — `> **Platform applicability:**` token discipline; Phase 54 D-04 inherits with pointer-routing payload (back-link to 00-overview + sub-bullets to sibling files) instead of analog-migration payload
- **Three-layer HIGH-RISK / HARD-DEADLINE callout** (Phase 52 D-01 → Phase 53 D-05 → Phase 54 D-13) — table-cell + adjacent blockquote + per-occurrence inline; cross-domain transferable per DPO-Phase52-07 explicit endorsement
- **Lazy-add forward-extension on sidecar** — Phase 48 D-15 YAGNI; Phase 53 D-06 deferral; Phase 54 D-16 + CDI-Phase54-03 inheritance (no false-positive surface at Phase 54 ship)
- **REQ traceability table as anti-scope-creep firewall** — REQUIREMENTS lines 161-168 maps each PATCH-NN to specific file; ZERO PATCH-NN mapped to 00-overview; V-54-29 NEGATIVE regression-guard mechanically enforces

### Integration Points

- **`docs/operations/00-index.md`** (Phase 53-owned) — Cross-referenced by Phase 54 patch-management files; Phase 54 MUST NOT amend per ROADMAP line 457. V-54-28 NEGATIVE regression-guard enforces no `## Patch Management` H2 added at Phase 54 ship.
- **`docs/admin-setup-ios/07-device-enrollment.md:35`** — PATCH-06 SURGICAL RETROFIT TARGET (D-07; off-ballot Referee resolution overriding REQ literal off-by-one). Cell edit lands in same atomic commit as new `03-ios-update-lifecycle.md`.
- **`docs/admin-setup-ios/04-configuration-profiles.md:128`** — PITFALL-13 forward-link insertion site (D-08). Byte-identical preservation of existing line 128 + appended cross-link sentence to new `03-ios-update-lifecycle.md`.
- **`.planning/REQUIREMENTS.md` lines 55 + 166 + `.planning/ROADMAP.md` line 267** — REQ + ROADMAP errata target (D-09). Off-by-one literal `05-compliance-policy.md` purged + replaced with `07-device-enrollment.md` (or whatever final retrofit target). V-54-21 NEGATIVE literal-purge regression-guard enforces.
- **`docs/operations/co-management/02-windows-workload-sliders.md`** — Phase 53 V-53-23 forward-link to Phase 54 PATCH-02 ring topology + Hotpatch H2. Phase 54 ship closes this forward-promise (Windows Update Policies row in Phase 53 workload table now resolves to canonical Phase 54 anchor).
- **`docs/operations/co-management/03-cocmgmt-migration-paths.md`** — Phase 53 forward-link to Phase 54 patch-management for Autopatch ring topology + WUfB-vs-Autopatch disambiguation. Phase 54 ship closes this forward-promise.
- **`docs/index.md` Operations H2 (Phase 59 ownership)** — Phase 59 appends Operations H2 linking to `operations/00-index.md` + sub-references to Phase 54-56 ops-domain content. Phase 54 does NOT touch `docs/index.md` (out of scope per Phase 59 navigation-files-last principle + ROADMAP line 457 hotspot ownership).
- **Phase 58 (DEFER-08 4-platform capability comparison) Software Updates row** — Will consume Phase 54 anchors per DPO-Phase54-05. Phase 54 H2 anchors (`#cross-platform-comparison`, `#ring-terminology`, `#hotpatch`, `#deadlines-cutover-dates`, `#zebra-lifeguard`) MUST remain stable post-ship for Phase 58 link-not-copy contract (PITFALL-7).

</code_context>

<specifics>
## Specific Ideas

- **PITFALL-9 verbatim mandate is the load-bearing PITFALLS-line-200 obligation triple**: (1) every "ring" qualified `WUfB deployment ring` or `Autopatch ring`; (2) opening section disambiguates two systems with mutual-exclusion narrative; (3) driver/firmware policy called out as SEPARATE policy from quality/feature update rings. All three obligations have dedicated Phase 54 deliverable surfaces (D-01 + D-12 + V-54-09 + V-54-11 + V-54-13).
- **"Ring" qualifier strict per-occurrence regex** is novel discipline for v1.5 ops-domain (Phase 51 V-51-09/19 dual-defense pattern adapted to per-token regex). Risk: prose flows that say "ring topology" or "rings" plural without immediate qualifier — plan author must use "WUfB deployment ring topology" or "Autopatch ring topology" verbatim. CD-12 (not enumerated above; could be added): plan-author discretion on whether to allow `WUfB rings` (plural noun, no "deployment") as a qualified form. Recommend: NO, require literal `WUfB deployment ring(s)` for full PITFALL-9 hygiene.
- **REQ literal off-by-one is 3-way replicated** (REQ:55 + REQ:166 + ROADMAP:267) — D-09 errata must purge ALL three locations in same commit; V-54-21 NEGATIVE literal-purge regression-guard scans both files for zero `05-compliance-policy.md` occurrences post-edit. The 3-way replication is itself evidence that the off-by-one was a roadmapper-time mistake, not a deliberate slot-renumbering.
- **The actual residual supervised-only-DDM-shaped claim was found via grep, NOT via REQ literal trust** — file-system scan of `docs/admin-setup-ios/` for "supervised" + "DDM" + "OS update" + "enforcement" co-located surfaced `07-device-enrollment.md:35` as the ONLY file containing such a co-located claim outside `04-configuration-profiles.md:128` (which is already correct). This validates the gsd-discuss-phase methodology of "verify spec literals against file-system reality before locking decisions."
- **Phase 53 EP HIGH-RISK three-layer is NEGATIVE polarity ("don't do this until X")** while Phase 54 HARD-DEADLINE three-layer is **action-polarity ("act by X or fail")** — the polarity distinction was raised as F-4C-05 LOW concern by Finder; Adversary cross-check + Referee ruled the polarity transfer is sustainable because hard-deadline content like "fleet compliance deadline October 31 2026 — devices not on Android 13+ with security patch ≤12 months will fail compliance" IS negative-polarity at the action layer ("you will fail if not done by"). DPO-Phase52-07 cross-domain-transfer endorsement covers this transfer.
- **Bare `> **Platform:**` token is FORBIDDEN** — corpus scan returned 28+ instances of `> **Platform <qualifier>:**` (applicability / gate / coverage / note) and ZERO bare `> **Platform:**`. V-54-27 NEGATIVE regression-guard enforces lexicon-family preservation. The Adversary's claim that bare-noun is a family-member was definitively refuted by file-system evidence.
- **PATCH-06 same-commit-atomic mandate is the v1.4.1 atomicity lesson** — RETROSPECTIVE.md line 166 verbatim "Atomic same-commit forward-promise closure beats deferred retrofits. Validated 6× in v1.4.1." Phase 54 D-21 + CDI-Phase54-05 inherit this discipline; ROADMAP line 271 reinforces ("no separate commit for the retrofit").
- **HARD-DEADLINE three-layer is applied to TWO items only** — macOS Apple OS 26 removal + Android MEETS_STRONG_INTEGRITY Oct 31 2026. Soft cutovers (Hotpatch May 2026 default, April 2026 Intune toggle, LifeGuard Jan 2026 GA, iOS Aug 2025 retraction) get single-callout treatment per D-14. The threshold is "fleet-wide failure mode if action not taken" (hard) vs "feature change without forced action" (soft).
- **00-overview platform-tag CD-01 has SC#5 implication** — ROADMAP line 269 SC#5 verbatim says "all patch-management ops files carry **correct multi-platform frontmatter**." If 00-overview uses single-string `platform: Windows` or `platform: macOS`, that fails the "multi-platform" requirement. Recommended `platform: cross-platform` (single-token, semantically multi-platform) OR `platform: Windows,macOS,iOS,Android` (comma-string, literal multi-platform). Plan author resolves at 54-01 ship.
- **Phase 51-53 adversarial-review methodology** — Phase 54 follows the same Finder/Adversary/Referee scored pattern (per project owner's invocation). 12 agents (4 + 4 + 4) across 4 gray areas. Adversary disprove evidence verified via direct file reads (07-device-enrollment.md:35 + 04-configuration-profiles.md:128 + 06-compliance-policy.md scan + REQ + ROADMAP literal verification + corpus-wide `> **Platform <qualifier>:**` token scan + Phase 53 D-08 implementation reference reads).

</specifics>

<deferred>
## Deferred Ideas

- **Add `c10_ops_allowlist[]` or `c11_patch_deadline_allowlist[]` sidecar entries** — Deferred to Phase 60 audit harness finalization OR first-false-positive surface (lazy-add per Phase 48 D-15 YAGNI). Not authored at Phase 54 ship because C10 doesn't run on operations/patch-management/ files (verified at `v1.5-milestone-audit.mjs:141-178`) and C11 is informational-first through Phase 60.
- **Mermaid update-flow diagram in `00-overview.md`** — Author discretion per CD-04. Validator does NOT assert Mermaid presence. Could be added for visual learners (4-platform-cadence-comparison or update-enforcement-flow); defer if scope-tight to 200-350 line budget.
- **Software Updates row in Phase 58 4-platform capability comparison doc** — Phase 58 ROADMAP line 322 lists 6 canonical axes (Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access); Software Updates IS canonical. Phase 58 ship will consume Phase 54 anchors per DPO-Phase54-05 (link-not-copy per PITFALL-7).
- **Phase 55 APP-04 cross-reference to Phase 54 PATCH-04 macOS DDM** — Phase 55 macOS .pkg/.dmg pipeline content (per ROADMAP line 281) may reference Phase 54 macOS update enforcement for app-update-vs-OS-update distinction. Phase 55 plan author discretion.
- **Phase 56 DRIFT-02 cross-reference to Phase 54 PATCH-07 Android Play Integrity** — Phase 56 cross-platform compliance drift signals (ROADMAP line 293) include Android Play Integrity verdict change. Phase 56 plan author cross-references Phase 54 `04-android-patch-delivery.md` MEETS_STRONG_INTEGRITY HARD-DEADLINE three-layer surface as drift-signal source per DPO-Phase54-03.
- **Phase 59 hub navigation Operations H2** — `docs/index.md` Operations H2 + `quick-ref-l1/l2.md` Operations sections. Phase 59 ownership per ROADMAP line 457 + 463; Phase 54 does NOT touch hub navigation.
- **Phase 60 CI registration of `check-phase-54.mjs`** — Add to `.github/workflows/audit-harness-v1.5-integrity.yml`. Phase 60 ownership per AUDIT-06 + DPO-Phase54-04.
- **CD-12 PITFALL-9 plural-form ring qualifier discipline** — Whether `WUfB rings` (plural noun, no "deployment") is acceptable as qualified form vs requiring literal `WUfB deployment ring(s)`. Plan-author discretion at 54-02 ship; recommended: require literal `WUfB deployment ring(s)` for full PITFALL-9 hygiene.

### Reviewed Todos (not folded)

None — Phase 54 scope review at discuss-phase identified no pending todos relevant to this phase. The /gsd-todo backlog was checked at workflow init via `gsd-tools todo match-phase 54` (returned `todo_count: 0, matches: []`).

</deferred>

---

*Phase: 54-patch-update-management*
*Context gathered: 2026-04-28*
