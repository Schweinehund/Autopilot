---
phase: 48
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/v1.5-milestone-audit.mjs
  - scripts/validation/v1.5-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-07
nyquist_compliant: true
must_haves:
  truths:
    - "scripts/validation/v1.5-milestone-audit.mjs exists as Path A copy of v1.4.1 with header/sidecar-path updated"
    - "scripts/validation/v1.5-audit-allowlist.json exists with v1.4.1 schema verbatim, phase metadata refreshed, 9 stale supervision_exemptions line coordinates updated"
    - "BASELINE_9 in regenerate-supervision-pins.mjs refreshed; doSelfTest() reads v1.5-audit-allowlist.json"
    - "node scripts/validation/regenerate-supervision-pins.mjs --self-test exits 0"
    - "node scripts/validation/v1.5-milestone-audit.mjs runs (existing C1-C7 + C9 still pass against existing corpus)"
  artifacts:
    - path: "scripts/validation/v1.5-milestone-audit.mjs"
      provides: "v1.5 audit harness (Path A copy + minimum updates: header lines 1-8, sidecar path at line 58)"
      min_lines: 391
    - path: "scripts/validation/v1.5-audit-allowlist.json"
      provides: "v1.5 sidecar allowlist (inherits v1.4.1; updated phase + generated metadata; 9 pin coords refreshed)"
      contains: "v1.5 inherit baseline 2026-04-26"
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "Updated BASELINE_9 (line 390) + doSelfTest sidecar path (line 405)"
      contains: "v1.5-audit-allowlist.json"
  key_links:
    - from: "scripts/validation/v1.5-milestone-audit.mjs"
      to: "scripts/validation/v1.5-audit-allowlist.json"
      via: "parseAllowlist() readFile call at line ~58"
      pattern: "v1.5-audit-allowlist\\.json"
    - from: "scripts/validation/regenerate-supervision-pins.mjs (doSelfTest, line 405)"
      to: "scripts/validation/v1.5-audit-allowlist.json"
      via: "parseAllowlist call inside doSelfTest"
      pattern: "v1.5-audit-allowlist\\.json"
---

<objective>
Rescue-style atomic Wave-1 commit (D-23 step 1 + D-14): create the v1.5 audit harness as a Path A copy of v1.4.1, create the v1.5 sidecar allowlist with refreshed pin coordinates, refresh BASELINE_9 + doSelfTest sidecar path in regenerate-supervision-pins.mjs. All four file edits land in ONE atomic commit so `--self-test` exits 0 on first v1.5 run, closing AUDIT-07 (STATE.md out-of-band carry-over from v1.4.1 close).

Purpose: This is the load-bearing commit of Phase 48. Every subsequent plan depends on the v1.5 harness existing and the self-test passing. Atomicity per D-14 + Phase 43 D-07 atomicity-contract precedent.

Output: v1.5-milestone-audit.mjs (391 lines, Path A from v1.4.1 with header + line 58 sidecar path updated), v1.5-audit-allowlist.json (41 lines, v1.4.1 schema verbatim with metadata refresh + 9 line-coord refreshes), regenerate-supervision-pins.mjs modified at lines 390-400 (BASELINE_9) and line 405 (doSelfTest sidecar path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-CONTEXT.md
@.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-RESEARCH.md
@.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-PATTERNS.md
@scripts/validation/v1.4.1-milestone-audit.mjs
@scripts/validation/v1.4.1-audit-allowlist.json
@scripts/validation/regenerate-supervision-pins.mjs

<interfaces>
<!-- Critical interfaces extracted from v1.4.1 harness so executor doesn't need to re-explore -->

From scripts/validation/v1.4.1-milestone-audit.mjs (lines 57-65, parseAllowlist):
```javascript
function parseAllowlist() {
  const raw = readFile('scripts/validation/v1.4.1-audit-allowlist.json');
  if (!raw) return { safetynet_exemptions: [], supervision_exemptions: [] };
  try {
    return JSON.parse(raw);
  } catch (err) {
    return { _parseError: err.message, safetynet_exemptions: [], supervision_exemptions: [] };
  }
}
```
v1.5 change: line 58 path → 'scripts/validation/v1.5-audit-allowlist.json'. All other lines verbatim.

From scripts/validation/v1.4.1-audit-allowlist.json schema (top-level keys):
```json
{
  "schema_version": "1.0",
  "generated": "2026-04-24T00:00:00Z",
  "phase": "43-v1-4-cleanup-audit-harness-fix",
  "safetynet_exemptions": [ /* 4 entries */ ],
  "supervision_exemptions": [ /* 18 entries with {file, line, reason} */ ],
  "cope_banned_phrases": [ /* 8 entries */ ]
}
```

From scripts/validation/regenerate-supervision-pins.mjs:
- Line 390: `const BASELINE_9 = [ ... ];` — 9-element array of [filePath, lineNum] tuples
- Line 405: `const allow = parseAllowlist('scripts/validation/v1.4-audit-allowlist.json');` (inside doSelfTest)
- Currently: `--self-test` FAILS due to BASELINE_9 staleness (5 of 9 entries drifted)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Path A copy + create v1.5 audit harness</name>
  <files>scripts/validation/v1.5-milestone-audit.mjs</files>
  <read_first>
    - scripts/validation/v1.4.1-milestone-audit.mjs (FULL — 391 lines; this is the Path A source)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-PATTERNS.md (Change 1 + Change 2 sections)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-RESEARCH.md §"Exact Header Comment Changes"
  </read_first>
  <action>
**Step 1.** Verbatim copy `scripts/validation/v1.4.1-milestone-audit.mjs` → `scripts/validation/v1.5-milestone-audit.mjs`. Preserve CRLF/LF as in source. Do NOT alter content yet.

**Step 2 — Replace lines 1-6 (header comment) with the v1.5 8-line header:**

Old (v1.4.1 lines 1-6):
```
// v1.4.1 Milestone Audit Harness (copy of v1.4 + C6/C7/C9 informational-first per Phase 42 D-29 + _*-prefix scope-filter + TEMPLATE-SENTINEL parse)
// Source of truth: .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md (D-01..D-08, D-24, D-26)
// Sidecar allow-list: scripts/validation/v1.4.1-audit-allowlist.json  (Knox / per-OEM AOSP / COPE exemptions)
// Frozen-predecessor reproducibility anchor: v1.4-milestone-audit.mjs pinned at commit 3c3a140
// File reads only: all content loaded via fs.readFileSync; no shell invocations.
```

New (v1.5 lines 1-13 — note: this is 13 lines because the per-check promotion-schedule comments add 7 lines per D-08 "schedule context goes in header comments"):
```
// v1.5 Milestone Audit Harness (Path A copy of v1.4.1 + C10 blocking + C11/C12/C13 informational-first + C6/C7 promoted to blocking)
// Source of truth: .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-CONTEXT.md (D-01..D-23)
// Sidecar allow-list: scripts/validation/v1.5-audit-allowlist.json  (inherited v1.4.1 schema + Linux/ops-domain arrays added lazily)
// Frozen-predecessor reproducibility anchor: v1.4.1-milestone-audit.mjs pinned at Phase 47 close
// File reads only: all content loaded via fs.readFileSync; no shell invocations.
//
// C6 (PITFALL-7) + C7 (bare-Knox): PROMOTED to blocking per Phase 48 D-04/D-05.
// C9 (cope_banned_phrases): INFORMATIONAL through Phase 60 per Phase 48 D-06 (ops-domain false-positive risk).
// C10 (Linux frontmatter): BLOCKING from Phase 48 -- new Linux docs must have platform: Linux + 60d last_verified.
// C11 (ops-domain anti-patterns): INFORMATIONAL-FIRST per AUDIT-03; promotes to blocking Phase 60.
// C12 (4-platform comparison structure): INFORMATIONAL-FIRST per AUDIT-04; promotes to blocking once file exists (Phase 58+).
// C13 (broken-link automation): INFORMATIONAL-FIRST per AUDIT-05; promotes to blocking after Phase 60 second-pass triage.
```

**Step 3 — Update parseAllowlist() sidecar path (the only line-58 area change in this plan):**

Locate the line in `parseAllowlist()`:
`const raw = readFile('scripts/validation/v1.4.1-audit-allowlist.json');`

Change to:
`const raw = readFile('scripts/validation/v1.5-audit-allowlist.json');`

**Step 4 — Do NOT add C10/C11/C12/C13 yet. Do NOT touch C6/C7 informational flag yet.** Those are scoped to plans 02, 03, 04 respectively. This plan ships ONLY: Path A copy + header + sidecar path. Verbatim everything else.

**Why scope-locked:** Atomicity-contract Phase 43 D-07 — first commit creates the file with minimal additive changes; subsequent plans add per-check changes. This keeps the rescue commit small + reviewable.

**Implementation note (Windows + CRLF):** Use Read+Write tools (not shell) for the verbatim copy to preserve line endings. After write, run `node -e "console.log(require('fs').readFileSync('scripts/validation/v1.5-milestone-audit.mjs','utf8').split('\n').length)"` — should report a line count within 1-2 of v1.4.1's 391 lines (extra lines from new header).
  </action>
  <verify>
    <automated>test -f scripts/validation/v1.5-milestone-audit.mjs && grep -c "v1.5-audit-allowlist.json" scripts/validation/v1.5-milestone-audit.mjs | grep -qE "^[1-9]" && grep -q "v1.5 Milestone Audit Harness" scripts/validation/v1.5-milestone-audit.mjs && ! grep -q "v1.4.1-audit-allowlist.json" scripts/validation/v1.5-milestone-audit.mjs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/validation/v1.5-milestone-audit.mjs` exits 0
    - `grep -q "v1.5 Milestone Audit Harness" scripts/validation/v1.5-milestone-audit.mjs` exits 0
    - `grep -q "Path A copy of v1.4.1" scripts/validation/v1.5-milestone-audit.mjs` exits 0
    - `grep -q "Frozen-predecessor reproducibility anchor: v1.4.1-milestone-audit.mjs" scripts/validation/v1.5-milestone-audit.mjs` exits 0
    - `grep -c "v1.5-audit-allowlist.json" scripts/validation/v1.5-milestone-audit.mjs` returns >= 1
    - `! grep -q "v1.4.1-audit-allowlist.json" scripts/validation/v1.5-milestone-audit.mjs` exits 0 (no stale references)
    - `node --check scripts/validation/v1.5-milestone-audit.mjs` exits 0 (syntactically valid JS)
  </acceptance_criteria>
  <done>v1.5 harness exists as Path A copy of v1.4.1; only header (lines 1-13) and parseAllowlist sidecar path updated; no other modifications</done>
</task>

<task type="auto">
  <name>Task 2: Inherit v1.4.1 sidecar verbatim + refresh metadata + refresh 9 stale pin coordinates</name>
  <files>scripts/validation/v1.5-audit-allowlist.json</files>
  <read_first>
    - scripts/validation/v1.4.1-audit-allowlist.json (FULL — 41 lines; verbatim copy source)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-PATTERNS.md §"v1.5-audit-allowlist.json" section (the metadata + line-refresh tables)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-RESEARCH.md §"BASELINE_9 Refresh — Concrete Values"
  </read_first>
  <action>
**Step 1.** Verbatim copy `scripts/validation/v1.4.1-audit-allowlist.json` → `scripts/validation/v1.5-audit-allowlist.json`.

**Step 2 — Update metadata fields (3 changes):**
- `"generated"`: change `"2026-04-24T00:00:00Z"` → `"2026-04-26T00:00:00Z"`
- `"phase"`: change `"43-v1-4-cleanup-audit-harness-fix"` → `"48-audit-harness-bootstrap-broken-link-sweep-first-pass"`
- ALL `reason` strings inside `supervision_exemptions[]` and `safetynet_exemptions[]`: replace existing reason text with `"v1.5 inherit baseline 2026-04-26"` per D-13 ("clear stale provenance comments").

**Step 3 — Refresh 9 stale supervision_exemptions line coordinates (per 48-PATTERNS.md table):**

| File | Old Line | New Line |
|------|----------|----------|
| docs/admin-setup-android/03-fully-managed-cobo.md | 35 | 36 |
| docs/_glossary-android.md | 15 | 16 |
| docs/_glossary-android.md | 45 | 46 |
| docs/_glossary-android.md | 63 | 66 |
| docs/reference/android-capability-matrix.md | 74 | 78 |
| docs/reference/android-capability-matrix.md | 76 | 80 |
| docs/reference/android-capability-matrix.md | 77 | 81 |
| docs/reference/android-capability-matrix.md | 79 | 87 |
| docs/reference/android-capability-matrix.md | 84 | 88 |

For each row: locate the entry in `supervision_exemptions[]` matching `{file: "<File>", line: <Old Line>}` and update its `line` field to `<New Line>`.

**Step 4 — Do NOT modify other entries.** All 4 `safetynet_exemptions[]`, plus `supervision_exemptions[]` entries for: enrollment-overview lines 51/53/83, l2-runbook line 21, glossary lines 76/78/172/188 retain existing line values.

**Step 5 — Do NOT add new arrays** (no `c11_ops_patterns[]`, no `c7_knox_allowlist[]`, no `linux_exemptions[]`). YAGNI per D-15. The schema stays at v1.4.1's 6 top-level keys (`schema_version`, `generated`, `phase`, `safetynet_exemptions`, `supervision_exemptions`, `cope_banned_phrases`).

**Step 6 — Validate JSON parseability:**
`node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json', 'utf8'))"` must exit 0.

**Step 7 — Verify pin counts:**
`node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8')); console.log('supervision:', j.supervision_exemptions.length, 'safetynet:', j.safetynet_exemptions.length, 'cope:', j.cope_banned_phrases.length)"` — expect `supervision: 18 safetynet: 4 cope: 8`.
  </action>
  <verify>
    <automated>test -f scripts/validation/v1.5-audit-allowlist.json && node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8')); if (j.phase !== '48-audit-harness-bootstrap-broken-link-sweep-first-pass') process.exit(1); if (j.generated !== '2026-04-26T00:00:00Z') process.exit(2); if (j.supervision_exemptions.length !== 18) process.exit(3); if (j.safetynet_exemptions.length !== 4) process.exit(4); if (j.cope_banned_phrases.length !== 8) process.exit(5); const cobo = j.supervision_exemptions.find(e => e.file === 'docs/admin-setup-android/03-fully-managed-cobo.md'); if (!cobo || cobo.line !== 36) process.exit(6); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/validation/v1.5-audit-allowlist.json` exits 0
    - JSON parses successfully (no SyntaxError)
    - `phase` field equals `"48-audit-harness-bootstrap-broken-link-sweep-first-pass"`
    - `generated` field equals `"2026-04-26T00:00:00Z"`
    - `supervision_exemptions.length === 18` (inherited count preserved)
    - `safetynet_exemptions.length === 4` (inherited count preserved)
    - `cope_banned_phrases.length === 8` (inherited count preserved)
    - Pin coord `docs/admin-setup-android/03-fully-managed-cobo.md` is at line 36 (not 35)
    - At least one supervision_exemption entry has `reason` containing `"v1.5 inherit baseline 2026-04-26"`
    - No top-level keys exist beyond the 6 expected (schema_version, generated, phase, safetynet_exemptions, supervision_exemptions, cope_banned_phrases)
  </acceptance_criteria>
  <done>v1.5 sidecar exists with v1.4.1 schema verbatim, metadata refreshed, 9 pin coords updated; YAGNI-compliant (no new arrays)</done>
</task>

<task type="auto">
  <name>Task 3: Refresh BASELINE_9 + doSelfTest sidecar path; verify --self-test exits 0</name>
  <files>scripts/validation/regenerate-supervision-pins.mjs</files>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs (lines 380-449 covering BASELINE_9 + doSelfTest)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-PATTERNS.md §"regenerate-supervision-pins.mjs (modified)"
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-RESEARCH.md §"BASELINE_9 Refresh" + §"Pitfall 3" + §"Pitfall 5"
  </read_first>
  <action>
**Critical ordering (Pitfall 3):** Tasks 1 + 2 MUST be complete before this task starts. The v1.5 sidecar with refreshed pin coordinates must already exist on disk; Task 3 reads it via the updated `doSelfTest()`.

**Step 1 — Update line 405 (doSelfTest sidecar path):**

Current (v1.4-pinned):
`const allow = parseAllowlist('scripts/validation/v1.4-audit-allowlist.json');`

Change to:
`const allow = parseAllowlist('scripts/validation/v1.5-audit-allowlist.json');`

This is a one-line change. The function name and surrounding code are untouched.

**Step 2 — Run --report against the updated v1.5 sidecar to verify stale count = 0:**

```bash
node scripts/validation/regenerate-supervision-pins.mjs --report 2>&1 | tee /tmp/pin-report.txt
```

Expected: `Stale pins (0)` in output. If stale count > 0, STOP — sidecar from Task 2 has incorrect coordinates; investigate which entry is stale before proceeding.

**Step 3 — Identify the 9 BASELINE_9 entries from --report Tier-1 output:**

After Step 1 of this task is committed, the --report output's "Tier-1 occurrences" section lists current line positions of the original S1..S9 bridge-prose pins. Per 48-RESEARCH.md BASELINE_9 section, the 9 original entries map to:

Current BASELINE_9 (lines 390-400, stale):
```javascript
const BASELINE_9 = [
  ['docs/_glossary-android.md', 65],
  ['docs/_glossary-android.md', 67],
  ['docs/_glossary-android.md', 134],
  ['docs/_glossary-android.md', 148],
  ['docs/android-lifecycle/00-enrollment-overview.md', 51],
  ['docs/android-lifecycle/00-enrollment-overview.md', 53],
  ['docs/android-lifecycle/00-enrollment-overview.md', 83],
  ['docs/admin-setup-android/03-fully-managed-cobo.md', 35],
  ['docs/l2-runbooks/20-android-app-install-investigation.md', 21]
];
```

Replacement procedure: For each currently-stale entry (`_glossary-android.md` lines 65/67/134/148, `03-fully-managed-cobo.md:35`), find the new current line by content inspection of the file (use Read on the docs file at the approximate line range, identify the bridge-prose match). Stable entries (`enrollment-overview.md:51/53/83`, `l2-runbook:21`) retain values.

**Reference values from --report stale-pin diagnosis:**
- `docs/_glossary-android.md:65` → likely current line 66 (cross-check via Read of `_glossary-android.md` lines 60-80; identify the bridge-prose context)
- `docs/_glossary-android.md:67` → likely current line 78 (per --report Tier-1 list)
- `docs/_glossary-android.md:134` → re-locate (no obvious match in --report; verify via Read)
- `docs/_glossary-android.md:148` → re-locate (no obvious match in --report; verify via Read)
- `docs/admin-setup-android/03-fully-managed-cobo.md:35` → 36

**Pragmatic procedure:** Run `node scripts/validation/regenerate-supervision-pins.mjs --self-test` AFTER updating BASELINE_9 to candidate values. Read the failure output — it precisely identifies which BASELINE_9 entries don't match current file content. Iterate values until exit code is 0.

**Step 4 — Update BASELINE_9 array (line 390-400) with the verified replacement values.**

**Step 5 — Run final verification:**

```bash
node scripts/validation/regenerate-supervision-pins.mjs --self-test
echo "exit code: $?"
```

Required: exit code = 0. If non-zero, iterate Step 3-4 until pass.

**Step 6 — Run smoke test on the v1.5 harness (existing checks should still pass; no new checks added yet by this plan):**

```bash
node scripts/validation/v1.5-milestone-audit.mjs --verbose
echo "exit code: $?"
```

Expected: exit 0. Existing C1-C7 + C9 still pass against existing corpus (Path A means structural copy of checks). No regressions.
  </action>
  <verify>
    <automated>node scripts/validation/regenerate-supervision-pins.mjs --self-test && grep -q "v1.5-audit-allowlist.json" scripts/validation/regenerate-supervision-pins.mjs && node scripts/validation/v1.5-milestone-audit.mjs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "v1.5-audit-allowlist.json" scripts/validation/regenerate-supervision-pins.mjs` returns >= 1
    - `! grep -q "scripts/validation/v1.4-audit-allowlist.json" scripts/validation/regenerate-supervision-pins.mjs` (no v1.4 sidecar references in doSelfTest)
    - `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0 (AUDIT-07 closed)
    - `node scripts/validation/regenerate-supervision-pins.mjs --report` reports `Stale pins (0)` (after sidecar refresh from Task 2 lands)
    - `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 (existing checks still pass via Path A)
    - `node --check scripts/validation/regenerate-supervision-pins.mjs` exits 0 (syntactically valid)
  </acceptance_criteria>
  <done>BASELINE_9 array refreshed; doSelfTest reads v1.5 sidecar; `--self-test` exits 0; existing v1.5 harness checks pass against current corpus</done>
</task>

</tasks>

<threat_model>
N/A — docs-engineering tooling; no user input flows; sidecar JSON parsing handled by existing graceful-degradation pattern (parseAllowlist try/catch from Phase 42 D-25 file-reads-only contract).
</threat_model>

<verification>
After all 3 tasks complete:
- `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 (existing C1-C7 + C9 still pass)
- `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0 (AUDIT-07 closed; STATE.md carry-over lifted)
- `node scripts/validation/regenerate-supervision-pins.mjs --report` reports zero stale pins
- All four file edits land in ONE atomic commit (D-14 atomicity contract)
</verification>

<success_criteria>
- AUDIT-01 (partial): v1.5-milestone-audit.mjs exists as Path A copy + sidecar exists
- AUDIT-07 (full): BASELINE_9 refreshed, --self-test exits 0
- Subsequent plans (02-06) can now add C10/C11/C12/C13 + graduate C6/C7 against this baseline harness
</success_criteria>

<output>
After completion, create `.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-01-SUMMARY.md` documenting:
- Final BASELINE_9 array values (post-refresh)
- Final 9 sidecar pin coordinates updated
- `--self-test` and v1.5 harness exit-0 evidence
- Atomic commit SHA
</output>
