# Phase 52: Linux L2 Investigation Runbooks 24–25 - Context

**Gathered:** 2026-04-27
**Status:** Ready for planning
**Methodology:** Adversarial review (Finder / Adversary / Referee scored pattern) on 4 gray areas covering 19 candidate options across 86 enumerated defects (7 CRITICAL / 6 HIGH / 18 MEDIUM / ~55 LOW; 275 raw points). Adversary attempted 7 disproves (3 MED + 4 LOW) targeting the apparent winner GA-1A and high-confidence boundaries. Referee adopted 6 disproves (1A-1 + 1A-2 + 2A-2 + 2A-3 + 3A-1 + 3A-3 = 14 points removed) and rejected 1 disprove (GA-4-COUP-2 = 5 points retained as the L2 audience-contract coupling defect, on grep-evidence that `> **Say to the user:**` appears 0 times across all 23 existing L2 runbooks). Net surviving defect score after rulings: 261 points across 80 confirmed defects, with per-area winners: GA-1 = Option 1A (matrix-cell + adjacent blockquote layered freshness caveat); GA-2 = Option 2A (anchor-indexed H2-per-trap mirroring Phase 51 GA-2A precedent); GA-3 = layered three-surface caveat (Phase 51 V-51-09/18/19 inherited verbatim); GA-4-i = L2-direct framing (no `> **Say to the user:**`); GA-4-ii = reference-only L1 deep-link consumption (no cause-topology import).

<domain>
## Phase Boundary

Phase 52 delivers L2 Desktop Engineering investigation capability for Linux Intune client failures: 2 new runbooks (`docs/l2-runbooks/24-linux-log-collection.md` + `docs/l2-runbooks/25-linux-agent-investigation.md`), 1 append-only edit to `docs/l2-runbooks/00-index.md` (new `## Linux L2 Runbooks` H2 + 2-row table appended after the Android L2 section), and the AUDIT-06-mandated `scripts/validation/check-phase-52.mjs` validator. Phase 51 (Linux L1 Triage + Runbooks 30-33) is the locked prerequisite: 51-VERIFICATION.md confirms PASSED (25/25 V-51-NN structural assertions, 5/5 ROADMAP success criteria, 5/5 LIN requirements satisfied). DPO-01 (51-CONTEXT line 139) mandates Phase 52 L2 runbooks consume L1 cause anchors as locked surfaces: `30-linux-enrollment-failed.md#cause-a-package-install` / `#cause-b-sign-in-failure` / `#cause-c-enrollment-timeout`; `31-linux-compliance-non-compliant.md#cause-a-distro-version-out-of-range` / `#cause-b-disk-not-encrypted` / `#cause-c-password-policy-not-met` / `#cause-d-custom-compliance-failure`; `32-linux-ca-blocking-web-access.md#cause-a-not-enrolled` / `#cause-b-non-compliant` / `#cause-c-edge-not-signed-in`; `33-linux-agent-service-failure.md` (single-cause, no anchors). DPO-02 (51-CONTEXT line 140) mandates read-vs-write apt distinction propagates to L2 (no sudo on `apt list`/`dpkg -l`/`systemctl --user`/`journalctl --user`; sudo only on root-required state-changing commands or `journalctl --system`). Phase 49 (Linux Foundation) glossary anchors and Phase 50 (admin-setup-linux) capability-matrix anchors are upstream consumption surfaces.

**Four deliverables (1 active requirement: LIN-12):**

1. `docs/l2-runbooks/24-linux-log-collection.md` (LIN-12 part 1) — Decision-Matrix-with-method-sections shape mirroring 18-android-log-collection.md / 14-ios-log-collection.md per D-03; 3 method sections (Section 1 = `journalctl` HIGH-confidence primary; Section 2 = file-based paths LOW-MEDIUM with three-layer caveat surface per D-01; Section 3 = `dpkg`/`apt` package-state queries HIGH); SC#1 verbatim coverage of `journalctl -u intune-agent` + `journalctl | grep intune-portal` + `/var/log/intune-update.log` + `/var/log/dpkg.log`; ROADMAP:222 mandated `> LOW-MEDIUM confidence` blockquote on `/var/log/microsoft/intune/` paths
2. `docs/l2-runbooks/25-linux-agent-investigation.md` (LIN-12 part 2) — anchor-indexed H2-per-trap shape per GA-2A winner + D-05; 4 traps (`## Trap A: Ubuntu HWE vs GA Kernel Mismatch {#trap-a-kernel-track}`, `## Trap B: Snap-vs-Deb Delivery Path Confusion {#trap-b-delivery-path}`, `## Trap C: Service-State User-Scope Confusion {#trap-c-service-state}`, `## Trap D: Identity Broker v2.0.2+ Re-enrollment {#trap-d-identity-broker}`); SC#2 verbatim coverage of HWE/GA + `uname -r` + snap-vs-deb detection + `systemctl status intune-agent` + `systemctl enable --user --now intune-agent.timer`; per-Trap "Escalated from L1?" reference blocks consuming Phase 51 cause anchors (DPO-01 inheritance) per D-09
3. Append `## Linux L2 Runbooks` H2 + 2-row table to `docs/l2-runbooks/00-index.md` (append-only contract per Phase 42 D-03; new H2 follows the last existing H2 at the Android L2 section). Mirrors macOS/iOS/Android L2 H2-with-When-to-Use-table shape from existing index sections.
4. `scripts/validation/check-phase-52.mjs` — AUDIT-06 validator-as-deliverable per Phase 48 D-25 file-reads-only contract + Phase 49 D-26 + Phase 51 D-20 regex-based detection; full validator scope per D-10 (file existence + frontmatter C10 + three-layer caveat assertion + PITFALL-3 negative regression-guard + journalctl/file-path positive coverage + 4 Trap H2 anchors + SC#2 literal token coverage + L1 cross-link coverage + L2 audience-contract negative regression-guard inverse of V-51-24 + Phase 51 read-vs-write apt regex + glossary anchor consumption + append-only assertion at 00-index.md + Phase 51 mode-axis token regression-guard + Phase 51 PITFALL-2 negative regression-guard + TBD/TODO scan); estimated 18-22 V-52-NN checks per Phase 51 (25) precedent scaled to smaller deliverable count.

Phase 56 (DRIFT-07 Linux drift fold) inherits read-vs-write apt distinction (DPO-Phase52-01) + freshness-caveat layered-defense pattern (DPO-Phase52-07) for any new file-path claims; Phase 58 (DEFER-08 4-platform comparison Linux row) consumes Runbook 24/25 anchor surface for ops/log columns per PITFALL-7 link-not-copy (DPO-Phase52-02); Phase 59 (CLEAN-08 hub navigation + ops sections) consumes Phase 52 anchor surface for `docs/index.md` + `docs/common-issues.md` + quick-ref hub files (DPO-Phase52-03); Phase 60 (audit harness finalization) registers `check-phase-52.mjs` in `audit-harness-v1.5-integrity.yml` and runs C13 broken-link automation against Phase 52 cross-link surface (DPO-Phase52-04); v1.5.1 (LIN-DEFER-01 Bash deep-dive) inherits L2 actor-framing + read-vs-write apt distinction (DPO-Phase52-05).

</domain>

<decisions>
## Implementation Decisions

### Runbook 24 freshness caveat surface (Gray Area 1 — winner: 1A layered matrix-cell + adjacent blockquote + per-line confidence token)

- **D-01:** **Runbook 24 carries a layered LOW-MEDIUM-confidence caveat surface for any `/var/log/microsoft/intune/` file-based path claim.** Per ROADMAP:222 SC#1 literal `> LOW-MEDIUM confidence` freshness caveat mandate + Phase 51 V-51-09/18/19 layered-defense precedent (51-CONTEXT D-04: "Tree-level CA callout + Runbook 32 entry-condition CA callout + validator regex are layered defenses, not duplication"). Three layers required:
  - **Layer 1 (Decision Matrix table cell):** any row referencing `/var/log/microsoft/intune/` carries `[LOW-MEDIUM, last_verified YYYY-MM-DD]` confidence token in the Confidence column (mirroring 18-android-log-collection.md:31 confidence-token-in-cell pattern)
  - **Layer 2 (adjacent `> **Source confidence:**` blockquote):** placed near the Method Selection or Section 2 (file-based paths) opening; literal text `> **Source confidence:** /var/log/microsoft/intune/ paths are LOW-MEDIUM confidence — Microsoft Learn does not document this path for Ubuntu intune-portal. journalctl is the confirmed primary surface. Verify file-path presence at \`review_by\` cadence.` (mirror 18-android-log-collection.md:126 `> **Source confidence:**` blockquote shape; specific verb choice is author discretion per CD-01)
  - **Layer 3 (per-command per-line confidence token):** every per-command file-path reference within investigation steps carries inline `[LOW-MEDIUM, last_verified YYYY-MM-DD]` token (mirror 18-android-log-collection.md:145 per-tag MEDIUM-marker pattern)
  - Reject 1B (blockquote-only): fails ROADMAP:222 matrix-cell structural caveat surface + Layer 1 absence
  - Reject 1C (frontmatter-only): 3D-1 CRIT confirmed — SC#1 verbatim "carries a `> LOW-MEDIUM confidence` freshness caveat" mandates blockquote shape (the `>` token literally specifies blockquote markdown), not frontmatter; 3D-2 HIGH (PITFALL-7 reader-invisible — frontmatter often hidden by SharePoint/Confluence/GitHub UI export); 3D-3 HIGH (path-specificity loss: frontmatter is whole-file scoped while SC#1 specifies per-path)
  - Reject 1D (matrix-cell only, no blockquote): 1D-1/1D-2 CRIT confirmed (Anti-Pattern 1 cause-topology cross-tier + V-52-NN check explosion 12 anchors → 50+ checks)
- **D-02:** **`journalctl -u intune-agent` is the HIGH-confidence primary surface; `/var/log/microsoft/intune/` is LOW-MEDIUM-confidence secondary surface.** Per SUMMARY.md:282 + STACK.md:78 explicit gap-flag. Runbook 24 Section 1 documents `journalctl -u intune-agent` + `journalctl | grep intune-portal` as canonical primary (HIGH); Section 2 documents file-based paths `/var/log/intune-update.log` + `/var/log/dpkg.log` (HIGH for the two confirmed paths) and `/var/log/microsoft/intune/` (LOW-MEDIUM with the layered caveat per D-01); Section 3 documents `dpkg -l intune-portal` + `apt list --installed | grep intune-portal` package-state queries (HIGH).
- **D-03:** **Runbook 24 mirrors 18-android-log-collection.md / 14-ios-log-collection.md decision-matrix-with-method-sections shape** per GA-1A winner. File structure: frontmatter → `> **Platform gate:**` blockquote → `# Linux Log Collection Guide` H1 → `## Context` H2 (cross-references SC#2 Runbook 25 prerequisite + 4 L1 escalation runbooks) → `## Tool Landscape` H2 with `> **Tool landscape:**` blockquote (Linux's narrowness vs Android's 3-method fragmentation; explicit "single primary surface" framing) → `## Decision Matrix` H2 with table (columns: Method | Who Triggers | Data Scope | Access Path | Physical Requirements | **Confidence (Layer 1)** | Typical Latency) → `## Method Selection` H2 (linear narrative; Linux has no enrollment-mode axis) → `## Section 1: journalctl` H2 (HIGH) → `## Section 2: File-based paths` H2 (with Layer 2 blockquote) → `## Section 3: Package-state queries` H2 (HIGH) → `## Common Artifacts Cross-Reference` H2 (cross-link to Phase 51 L1 runbook causes; DPO-01 inheritance) → `## Related Resources` H2 → `## Version History` H2.
- **D-04:** **Runbook 24/25 frontmatter:** `last_verified: <plan-execution-date>` + `review_by: <last_verified + 60d>` + `applies_to: all` + `audience: L2` + `platform: Linux`. C10 gate per ROADMAP:225 SC#4 + Phase 48 D-08 frontmatter contract. 60-day cycle per Phase 51 inheritance (Phase 34 D-14 cycle locked at 60-day for L1/L2 runbook fragility-on-claims).

### Runbook 25 multi-trap structure (Gray Area 2 — winner: 2A anchor-indexed H2-per-trap)

- **D-05:** **Runbook 25 uses anchor-indexed H2-per-trap shape mirroring `docs/l1-runbooks/25-android-compliance-blocked.md` anchor-indexed cause precedent + Phase 51 GA-2A winner (51-CONTEXT D-09).** SC#2 mandates 4 trap families (per ROADMAP:223 verbatim — 3 SC-explicit + Identity Broker as Phase 50 LIN-05 inherited surface): `## Trap A: Ubuntu HWE vs GA Kernel Mismatch {#trap-a-kernel-track}`, `## Trap B: Snap-vs-Deb Delivery Path Confusion {#trap-b-delivery-path}`, `## Trap C: Service-State User-Scope Confusion {#trap-c-service-state}`, `## Trap D: Identity Broker v2.0.2+ Re-enrollment {#trap-d-identity-broker}`. Each `## Trap X:` H2 contains: (a) `**Entry condition:**` paragraph (observable signal), (b) `### Symptom`, (c) `### Investigation Steps` (numbered list with code-fence snippets; NO `> **Say to the user:**` per D-08 + GA-4-i winner), (d) `### Resolution`, (e) `### Verification`. Reject 2B (numbered-sequenced flat investigation): forces walking check-tree (same SC#2 violation as Phase 51 D-09 2B rejection). Reject 2C (Pattern A/B/C imported from 19-android-enrollment-investigation.md): 2C-1 CRIT confirmed (PITFALL-1 V-51-07 mode-axis token regression — imports BYOD/COBO/COPE/COSU/ZTE tokens which validator V-52-18 forbids per D-10 + Phase 51 V-51-07 inheritance); 2C-2 CRIT (Pattern A/B/C orthogonal to trap families — would force traps duplicated 5x or split across patterns); 2C-3 HIGH (validator-impossible regex: no anchor surface for kernel/snap/service tokens). Reject 2D (sub-runbook split into 25a/25b/25c): 2D-1 CRIT confirmed (REQUIREMENTS.md:154 LIN-12 → `24-linux-log-collection.md + 25-linux-agent-investigation.md` 1:1 file-name lock; 2 files exactly, not 4); 2D-2 HIGH (REQUIREMENTS.md traceability lock; renaming costs cross-doc cascade); 2D-3 HIGH (V-52-NN scope bloat 4 frontmatters/files/anchor sets ~2x complexity).
- **D-06:** **Trap anchor naming convention: `{#trap-x-[short-name]}`.** Validator V-52-NN (D-10) pins these literal anchors. Phase 56 + Phase 58 + Phase 59 cross-link these per DPO-Phase52-NN obligations. Renaming any Phase 52 trap anchor in a future phase requires same-commit Phase 52 validator update. Phase 51 D-21 brittleness contract inherited.
- **D-07:** **Trap ordering by likelihood + observability proximity.** Trap A (kernel HWE/GA mismatch) and Trap B (snap-vs-deb delivery confusion) have immediate observability via `uname -r` / `apt list --installed | grep intune-portal` / `apt list --installed | grep snapd`. Trap C (service-state) requires `systemctl --user status intune-agent.timer` + `systemctl is-active`. Trap D (Identity Broker) requires `journalctl --user -u microsoft-identity-broker`. Author may reorder if telemetry/research suggests different real-world frequency; validator pins LITERAL anchors, not ordering (CD-02).
- **D-08:** **Per-Trap body uses L2-direct framing — NO `> **Say to the user:**` blockquotes.** GREP-EVIDENCE: `> **Say to the user:**` appears 0 times across all 23 existing L2 runbooks (`docs/l2-runbooks/01..23-*.md`). Pattern is L1-exclusive (Phase 30 D-10 contract; V-51-24 anchored to `audience: L1`). L2 audience executes commands directly; L1 narration-pattern is structurally inappropriate. Investigation steps use numbered list with command snippets in fenced code blocks (mirror 02-esp-deep-dive.md Steps 2-5 + 19-android-enrollment-investigation.md Data Collection Step 1-4 patterns). Optional `> **Note:**` / `> **Source confidence:**` / `> **Device-owner constraint:**`-style L2 callouts permitted (18-android-log-collection.md:124 precedent).

### L1→L2 cross-link surface (Gray Area 4-ii — winner: 4ii-A reference-only deep-link consumption)

- **D-09:** **Runbook 25 imports L1 cause anchors as reference-only deep-links — NO cause-topology import.** Phase 51 DPO-01 mandates Phase 52 treats L1 cause anchors as locked surfaces. Pattern: at top of each `## Trap X:` H2 (or in a top-of-runbook "From L1 escalation?" block per 19-android-enrollment-investigation.md:19-25 precedent — see CD-04), include reference text like `**From L1 escalation?** L1 runbook 33 may have routed here from a flagged service-state symptom. L1 collected: serial number, user UPN, distro+kernel version, dpkg-l output. Skip to Investigation Step 2 if L1 collected baseline data.` Each cross-link uses literal anchor: `30-linux-enrollment-failed.md#cause-a-package-install`, `30-linux-enrollment-failed.md#cause-b-sign-in-failure`, `30-linux-enrollment-failed.md#cause-c-enrollment-timeout`, `33-linux-agent-service-failure.md`, etc. (full anchor surface enumerated in `<canonical_refs>` below). Per Anti-Pattern 1 (cross-tier cause-topology duplication forbidden), Phase 52 Runbook 25 does NOT re-author L1 cause shapes; it CONSUMES them. The L2 Trap content is orthogonal to L1 Cause content (kernel/HWE, snap-vs-deb, service-state, Identity Broker are L2-investigation surfaces; L1 causes are user-facing observable symptoms — package-install / sign-in / enrollment-timeout / agent-service-failure). Reject 4ii-B (inline-only): no top-of-runbook landing pad for L1-escalated tickets; loses Phase 51 V-51-22 analog cross-link discoverability surface. Reject 4ii-C (both top + inline): Anti-Pattern 1 within-doc duplication risk + 2x maintenance cost on every L1 anchor rename + reader navigation noise.

### Validator depth + commit atomicity (Gray Area 4 follow-on — full validator + single atomic commit)

- **D-10:** **`check-phase-52.mjs` validator scope = FULL** (per Phase 51 4C precedent + DPO-07 inheritance). File-reads-only / no-shared-module per Phase 48 D-25; regex-based parsing per Phase 49 D-26 + Phase 51 D-20. Validator implements the following V-52-NN structural assertions (estimated 18-22 checks):
  - **V-52-01..04 File existence:** `docs/l2-runbooks/24-linux-log-collection.md` + `docs/l2-runbooks/25-linux-agent-investigation.md` + `scripts/validation/check-phase-52.mjs` + (append target) `docs/l2-runbooks/00-index.md`
  - **V-52-05 Frontmatter (C10 contract per D-04):** both new files carry `platform: Linux` + `audience: L2` + 60-day `last_verified` + `review_by`
  - **V-52-06 Runbook 24 layered caveat — Layer 1 (per D-01 GA-1A winner):** Runbook 24 Decision Matrix contains `[LOW-MEDIUM` confidence-token pattern in any row referencing `/var/log/microsoft/intune/` (block-scoped regex per V-51-20 file-reads-only-block-extraction precedent for table-rendering robustness)
  - **V-52-07 Runbook 24 layered caveat — Layer 2 (per D-01):** Runbook 24 contains literal `> **Source confidence:**` blockquote AND nearby `LOW-MEDIUM confidence` token AND `/var/log/microsoft/intune/` path reference within ~10-line window
  - **V-52-08 Runbook 24 layered caveat — Layer 3 (per D-01):** Runbook 24 contains ≥2 per-line `[LOW-MEDIUM, last_verified` tokens at command-snippet level
  - **V-52-09 Runbook 24 NEGATIVE assertion (PITFALL-3 regression-guard):** Runbook 24 does NOT contain `snap install`, `/var/snap/intune-portal/`, `snap container` text (PITFALL-3 line 73 warning signs — snap delivery is NOT GA per Phase 49 D-04)
  - **V-52-10 Runbook 24 SC#1 positive coverage (per ROADMAP:222 verbatim):** literal `journalctl -u intune-agent` AND `journalctl | grep intune-portal` AND `/var/log/intune-update.log` AND `/var/log/dpkg.log` all present
  - **V-52-11 Runbook 25 anchor-indexed Trap H2s (per D-05 + D-06):** 4 H2s with literal anchors `{#trap-a-kernel-track}`, `{#trap-b-delivery-path}`, `{#trap-c-service-state}`, `{#trap-d-identity-broker}` (anchor-string-pinned validator-CONTEXT brittleness contract per Phase 51 D-21)
  - **V-52-12 Runbook 25 SC#2 kernel-trap content (per ROADMAP:223 verbatim):** Trap A H2 body contains literal `Ubuntu HWE` AND `GA kernel` AND `uname -r`
  - **V-52-13 Runbook 25 SC#2 service-state content (per ROADMAP:223 verbatim):** Trap C H2 body contains literal `systemctl status intune-agent` AND `systemctl enable --user --now intune-agent.timer`
  - **V-52-14 Runbook 25 SC#2 snap-vs-deb detection content (per ROADMAP:223 verbatim):** Trap B H2 body contains literal `snap` AND `deb` (snap-vs-deb detection)
  - **V-52-15 Runbook 25 L1 cross-link literal coverage (per D-09 + Phase 51 DPO-01 inheritance):** Runbook 25 contains literal cross-links to ≥3 L1 cause anchors from the locked surface set in `<canonical_refs>` (e.g., `30-linux-enrollment-failed.md#cause-a-package-install` + `30-linux-enrollment-failed.md#cause-b-sign-in-failure` + `33-linux-agent-service-failure.md`)
  - **V-52-16 L2 audience-contract negative regression-guard (per D-08 + GA-4-i winner; coupling-defect retained per GA-4-COUP-2 REJECTED disprove):** Runbook 24 AND Runbook 25 do NOT contain `> **Say to the user:**` blockquotes (V-51-24 inverse polarity — L1-pattern forbidden in L2-audience docs, anchored to `audience: L2` in frontmatter)
  - **V-52-17 Read-vs-write apt regex (per Phase 51 DPO-02 inheritance):** both runbooks scanned for `sudo apt list`, `sudo dpkg -l`, `sudo systemctl --user`, `sudo journalctl --user` — these MUST NOT appear; `sudo journalctl --system`, `sudo cryptsetup`, `sudo apt install`, `sudo systemctl restart` (root-required state-changing) MAY appear but only inside `### Resolution` H3 sections per Phase 30 D-10 L1-readonly-scope-extended-to-L2-via-DPO-02 contract
  - **V-52-18 Glossary-anchor consumption (per Phase 49 DPO-04 inheritance):** both runbooks contain ≥1 link each to `../_glossary-linux.md#<anchor>` (sanity-check that runbooks consume Phase 49 glossary; ~10 candidates: `intune-portal`, `microsoft-identity-broker`, `intune-agent.timer`, `journalctl`, `systemd`, `dpkg`, `apt`, `web-app CA`, `Edge for Linux`, `dm-crypt`, `LUKS`)
  - **V-52-19 Append-only assertion at `00-index.md`:** file contains literal H2 `## Linux L2 Runbooks` AND 2-runbook entries (24/25) AND H2 follows last existing H2 (Android L2 section currently per D-14 step 2 enumeration)
  - **V-52-20 Mode-axis token regression-guard (PITFALL-1 cascade per Phase 51 V-51-07 inheritance):** neither runbook structural-text contains `BYOD`, `COBO`, `COPE`, `Dedicated`, `ZTE`, `AOSP`, `COSU` tokens (Linux has no enrollment-mode axis per Phase 49 + Phase 51 D-01 whitelist-first)
  - **V-52-21 PITFALL-2 inheritance (Runbook 25 Identity Broker trap does NOT re-introduce device-CA framing per Phase 51 V-51-19 inheritance):** Runbook 25 does NOT contain literal `Require device to be marked as compliant` (the device-level CA grant phrasing per Phase 50 D-24); allowlist surface preserved per PITFALL-13 same-commit-allowlist precedent
  - **V-52-22 TBD/TODO scan:** files do NOT contain `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` outside Version History tables (per v1.2 retro carry-forward)
- **D-11:** **`check-phase-52.mjs` implementation pattern matches `check-phase-51.mjs` / `check-phase-50.mjs` / `check-phase-49.mjs`** (Phase 48 D-25 + Phase 49 D-26 + Phase 51 D-20 lineage). File-reads-only / no-shared-module; markdown parsing is regex-based (heading detection via `^## ` / `^### `; blockquote detection via `^> `; fenced-code-block boundary detection; H2-anchor-suffix detection via `\{#[a-z-]+\}` regex); no markdown-AST dependency.
- **D-12:** **Hardcoded H2/anchor strings pinned in BOTH the validator and CONTEXT.md.** Trap anchor renames (D-06) require same-commit validator update. CDI-Phase49-style brittleness trade-off accepted per Phase 49 D-25 + Phase 50 D-25 + Phase 51 D-21 precedent.
- **D-13:** **Commit atomicity = SINGLE atomic commit** (per Phase 51 D-22 + DPO-07 + CDI-Phase52-04 below). Phase 51 Goldilocks-rule transfers: Phase 52 append target `docs/l2-runbooks/00-index.md` is NOT in v1.5 supervision_exemptions sidecar (Android files only), so PITFALL-12 motivation does not apply. Validator V-52-19 append-only assertion at `00-index.md` would FAIL in commit-1 of any 2-commit split (atomicity-contract violation per Phase 48 D-14 + Phase 51 CDI-Phase51-06 precedent). Single atomic commit forced by validator's append-assertion shape — this is the load-bearing reason for D-13 distinct from PITFALL-12 absence (CDI-Phase52-04). Single atomic commit covers: 2 new content files (Runbook 24 + Runbook 25) + 1 validator + 1 append-only edit at 00-index.md.

### Plan-level ordering and atomicity

- **D-14:** **Phase 52 plan order (single-commit atomicity per D-13):**
  1. **Authoring wave (parallel-safe):** All 3 deliverables can be authored concurrently in worktrees. Phase 51 VERIFICATION.md confirms PASSED at startup; L1 cause anchors locked per DPO-01.
     - 52-01: `docs/l2-runbooks/24-linux-log-collection.md` (LIN-12 part 1; D-01 three-layer caveat + D-02 surface confidence ranking + D-03 decision-matrix-with-method-sections shape + D-04 frontmatter; SC#1 verbatim coverage)
     - 52-02: `docs/l2-runbooks/25-linux-agent-investigation.md` (LIN-12 part 2; D-05 anchor-indexed Trap A/B/C/D + D-06 anchor naming + D-07 ordering by likelihood + D-08 L2-direct framing + D-09 L1 cross-link reference-only consumption; SC#2 verbatim coverage)
     - 52-03: `scripts/validation/check-phase-52.mjs` (D-10 V-52-01..22 structural assertions; D-11 file-reads-only pattern; estimated 18-22 checks)
  2. **Append wave (sequential after authoring wave):**
     - Append `## Linux L2 Runbooks` H2 + intro paragraph + table to `docs/l2-runbooks/00-index.md` after the Android L2 section (locate by greppable last-Android-L2-table boundary). Mirror the Android L2 H2 shape (intro paragraph + 2-row table with `Runbook | When to Use | Prerequisite` columns).
  3. **Pre-commit gate:**
     - `node scripts/validation/check-phase-52.mjs` exits 0 (all 18-22 V-52-NN checks pass)
     - `node scripts/validation/v1.5-milestone-audit.mjs --verbose` exits 0 (C1-C12 PASS; C13 informational PASS-or-noise within accepted tolerance)
     - `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0 (regression-prevention; should be unaffected since append target is not pinned)
     - `markdown-link-check` against the 2 new content files + 1 modified hub file (informational; not blocking per Phase 48 D-08)
     - Verify both new content files contain `last_verified` + `review_by` frontmatter on 60-day cycle
  4. **Single atomic commit:** All 2 new content files + validator + 1 append-only edit per D-13 + commit message `docs(52): linux L2 log-collection + agent-investigation runbooks 24-25 + check-phase-52 validator + 00-index append-only edit` per Phase 51 commit-message-shape precedent.
  5. **Post-commit verification:** Re-run `check-phase-52.mjs` + `v1.5-milestone-audit.mjs` from clean workspace.
  6. **VERIFICATION.md authored** (separate commit per Phase 49/50/51 close pattern) documenting validator pass + Phase 51 DPO-01/02 inheritance verification + 4 SC# satisfaction proof + DPO-Phase52-NN propagation summary for Phase 56+ plan authors.

### Claude's Discretion

- **CD-01:** Exact wording of the Layer 2 `> **Source confidence:**` blockquote in Runbook 24 (D-01 specifies form + scope; specific verb choice is author discretion within "explains LOW-MEDIUM confidence is path-related Microsoft Learn coverage gap, not journalctl-related" trim rationale).
- **CD-02:** Exact ordering of Trap A/B/C/D in Runbook 25 (D-07 says "by likelihood + observability proximity"; author may reorder if telemetry suggests different real-world frequency. Validator pins the LITERAL anchors, not ordering).
- **CD-03:** Section depth in Runbook 24 (Decision Matrix row count, Method Selection list-item count, Section 1/2/3 body length) — author may scale to coverage need; minimum is the SC#1 file-path enumeration coverage per V-52-10.
- **CD-04:** Whether Runbook 25 includes a top-of-runbook "From L1 escalation?" reference block (mirror 19-android-enrollment-investigation.md:19-25 precedent) OR places the L1-escalation references at the per-Trap H2 level (mirror per-Trap entry-condition embedded format). Recommend the per-Trap format for parity with anchor-indexed Trap structure (D-05) — but optional. Validator V-52-15 accepts ≥3 cross-link tokens regardless of placement.
- **CD-05:** Per-Trap body length in Runbook 25 — Phase 51 Runbook 25 (Android) Cause A is ~50 lines (heavy); Cause C is ~40 lines (medium); Cause B+D are ~25-30 lines each. Phase 52 author may scale each Trap body proportionate to L2-actionable surface depth.
- **CD-06:** Mermaid usage in Runbook 24/25 — Phase 51 trees use `graph TD` + `click` directives; Phase 52 runbooks may include focused Mermaid blocks (e.g., a kernel-track-decision Mermaid in Trap A) but neither runbook structurally requires them. Author discretion. Validator does NOT assert Mermaid presence (no SC# mandates it).
- **CD-07:** Whether Runbook 25 Trap D (Identity Broker) cross-links to Phase 50 LIN-05 admin-side Identity Broker callout in `01-intune-linux-agent.md` OR via `_glossary-linux.md#microsoft-identity-broker` — D-09 mandates literal cross-link; specific anchor surface is author discretion within Phase 49 + Phase 50 anchor menu.
- **CD-08:** Whether the optional `> **Note:**` / `> **Source confidence:**` / `> **Device-owner constraint:**` L2 callouts use one consistent label or mixed labels — author discretion. Validator does NOT assert label uniformity.

### Cross-decision integration callouts (Referee Part C)

- **CDI-Phase52-01:** **D-01 (layered caveat) × D-08 (L2-direct framing) coupling.** Layered caveats use `> **Source confidence:**` blockquote shape (L2-appropriate), NOT `> **Say to the user:**` (L1-appropriate). Caveat blockquote is authoritative reader-facing surface; the `> **Source confidence:**` tag mirrors 18-android-log-collection.md:126 precedent and is L2-audience-native. Plan author for 52-01 (Runbook 24) MUST use `> **Source confidence:**` or `> **Note:**` framing — NEVER `> **Say to the user:**`. V-52-16 enforces.
- **CDI-Phase52-02:** **D-05 (anchor-indexed Trap shape) × Phase 51 DPO-01 (L1 anchor consumption) coupling.** Runbook 25's 4 Traps each include a "From L1 escalation?" reference block (or per-Trap embedded reference per CD-04) consuming Phase 51 L1 cause anchors as IMMUTABLE surfaces. Plan author MUST treat L1 cause anchors as locked: full anchor surface enumerated in `<canonical_refs>` below. Renaming any L1 anchor requires same-commit Phase 52 update (Phase 51 CDI-Phase51-06 transferred shape).
- **CDI-Phase52-03:** **D-09 (L1 cross-link reference-only) × Anti-Pattern 1 (cross-tier cause-topology duplication forbidden).** Runbook 25's L1-escalation reference blocks DO NOT re-author L1 cause shapes — they DEEP-LINK to cause anchors. The L2 Trap content (kernel/HWE, snap-vs-deb, service-state, Identity Broker) is L2-investigation-surface-distinct from L1-cause-symptom content (package-install / sign-in / enrollment-timeout / agent-service-failure). Plan author for 52-02 (Runbook 25) MUST verify each Trap body content is cross-tier-distinct from L1 Cause body content via overlap audit at authoring time. Validator V-52-15 enforces cross-link presence; cross-tier-distinctness is human verifiable (no validator regex).
- **CDI-Phase52-04:** **D-10 (full validator scope) × D-13 (single atomic commit) coupling.** V-52-19 append-only assertion at `00-index.md` would FAIL in commit-1 of any 2-commit split (atomicity-contract violation per Phase 48 D-14 + Phase 51 CDI-Phase51-06 precedent). Single atomic commit is forced by the validator's append-assertion shape; this is the load-bearing reason for D-13 distinct from PITFALL-12 absence. Phase 51 Goldilocks-rule precedent: when neither PITFALL-12 motivation nor append-edit-isolation transfers, single-commit governs.
- **CDI-Phase52-05:** **D-08 (L2-direct framing) × D-10 V-52-16 (audience-contract regression-guard).** V-52-16 is a NEGATIVE assertion (Runbook 24/25 must NOT contain `> **Say to the user:**`) anchored to L2 audience contract. This is the inverse of V-51-24 (L1 audience MUST contain pattern) — same regex literal, opposite polarity, audience-keyed via frontmatter. Plan author MUST not import L1 narration patterns even when cross-linking L1 anchors per CDI-Phase52-02. GA-4-COUP-2 coupling defect explicitly retained as REAL ISSUE (REJECTED Adversary disprove); evidence: 0/23 occurrences of L1-pattern across existing L2 corpus.
- **CDI-Phase52-06:** **D-01 (layered caveat) × Phase 51 V-51-09/18/19 layered-defense precedent.** Phase 52's three-layer freshness-caveat surface (Layer 1 matrix-cell + Layer 2 blockquote + Layer 3 per-line confidence token) inherits the Phase 51 PITFALL-2 layered enforcement pattern verbatim. Drift between layers caught by validator string-pinning per Phase 51 D-21 precedent. PITFALL-13 false-positive surface acknowledged: same-commit `c5_collision_allowlist[]`-style lazy-add per Phase 48 D-15 YAGNI principle if Runbook 25 quotes Runbook 24's caveat verbatim during cross-tier reference.
- **CDI-Phase52-07:** **D-13 (single atomic commit) × Phase 51 D-22 + DPO-07 inheritance.** Phase 51 single-atomic-commit pattern transfers; the load-bearing reason in Phase 51 (validator V-51-21/V-51-22 append-assertions) applies to Phase 52 (validator V-52-19 append-assertion at 00-index.md) by analogy. Phase 52 inherits Goldilocks-rule via the same logic.

### Downstream phase obligations (Referee Part C — for next-phase plan authors)

- **DPO-Phase52-01:** **Phase 56 (DRIFT-07 Linux drift fold) inherits read-vs-write apt distinction (Phase 51 DPO-02 cascade) + freshness-caveat surface pattern (D-01).** Phase 56 L2 cryptsetup/dm-crypt drift commands preserve sudo-on-state-changing-only discipline. If Phase 56 documents new file paths with similar Microsoft Learn coverage gap, Phase 56 plan author MUST replicate the three-layer caveat surface from D-01 (matrix-cell + blockquote + per-line token) per CDI-Phase52-06.
- **DPO-Phase52-02:** **Phase 58 (DEFER-08 4-platform comparison Linux row) consumes Runbook 24/25 anchor surface for ops/log columns.** L2-tier columns of `4-platform-capability-comparison.md` Linux row (e.g., "Log Collection," "Service Investigation") link to `24-linux-log-collection.md#section-1-journalctl` / `25-linux-agent-investigation.md#trap-a-kernel-track` per PITFALL-7 link-not-copy. Phase 58 plan author MUST treat Phase 52 Section/Trap anchors as immutable surfaces.
- **DPO-Phase52-03:** **Phase 59 (CLEAN-08 hub navigation + Linux + Operations sections) consumes Phase 52 anchor surface.** `docs/index.md` Linux H2 ops section + `docs/common-issues.md` Linux symptom routing + `docs/quick-ref-l2.md` Linux quick-reference must link to Runbook 24/25 anchors by stable name. Phase 59 plan author MUST run anchor inventory against Phase 52 outputs BEFORE authoring hub Linux ops sections (Phase 51 DPO-05 transferred shape).
- **DPO-Phase52-04:** **Phase 60 (audit-harness finalization) registers `check-phase-52.mjs` in CI.** Add to `.github/workflows/audit-harness-v1.5-integrity.yml` per AUDIT-06 contract + Phase 48 D-18 lazy-skip pattern. C13 informational broken-link automation runs against Phase 52 cross-link surface (~6-8 cross-links per runbook: L1 cause anchors + glossary anchors + Phase 50 capability-matrix anchors + within-doc Trap/Section anchors). Phase 60 plan author runs C13 sweep against Phase 52 outputs as part of the v1.5 close audit.
- **DPO-Phase52-05:** **v1.5.1 (LIN-DEFER-01 Linux Bash custom-compliance deep-dive) inherits L2 actor-framing + read-vs-write apt distinction.** The Bash deep-dive guide MUST use L2-direct framing (no `> **Say to the user:**`) per CDI-Phase52-05; Bash custom-compliance scripts run in Intune-agent-context (typically root for system-state inspection), but L2 inspection commands documented as L2 reference must preserve user-context read-only discipline per Phase 51 DPO-06 + Phase 52 D-08. v1.5.1 plan author inherits the Phase 51 GA-3B sub-rule + Phase 52 D-08 audience contract.
- **DPO-Phase52-06:** **Phase 52 internal — single-commit atomicity (per D-13 + CDI-Phase52-04 + CDI-Phase52-07).** Phase 52 ships in a single atomic commit because the validator's V-52-19 append-assertion forces it. PITFALL-12 motivation does not apply to Phase 52 (append target is not in supervision sidecar). Goldilocks-rule per Phase 51 DPO-07 precedent.
- **DPO-Phase52-07:** **Phase 56 inherits Phase 52 freshness-caveat layered-defense pattern for any new file-path claims.** If Phase 56 documents `/var/log/microsoft/intune/` extensions for drift detection or if any new Linux ops file-path is added with similar Microsoft Learn coverage gap, Phase 56 plan author MUST replicate the three-layer caveat surface from D-01 per CDI-Phase52-06.
- **DPO-Phase52-08:** **Phase 58/59 Linux row CA cell inherits PITFALL-2 layered defense (Phase 51 DPO-04 cascade).** Phase 51 DPO-04: Phase 58 4-platform Linux CA cell MUST contain "web-app CA only — no device-level CA grant" verbatim AND link to `linux-capability-matrix.md#conditional-access` per CDI-Phase51-03. Phase 52 does NOT re-introduce CA topic in Runbook 24/25 (V-52-21 negative regression-guard enforces) but downstream phases inheriting via Phase 52 CDI must preserve Phase 51 DPO-04. Anti-regression chain: Phase 51 V-51-19 → Phase 52 V-52-21 → Phase 58 carry-forward.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents (researcher, planner, executor, reviewer) MUST read these before authoring or implementing.**

### Phase 52 success criteria + traceability (authoritative scope)

- `.planning/ROADMAP.md` §"Phase 52: Linux L2 Investigation Runbooks 24–25" (lines 217-226) — Goal, dependencies, requirements (LIN-12), 4 success criteria
- `.planning/ROADMAP.md` line 222 — SC#1 verbatim: log surface enumeration + `> LOW-MEDIUM confidence` blockquote mandate on `/var/log/microsoft/intune/` paths
- `.planning/ROADMAP.md` line 223 — SC#2 verbatim: kernel HWE/GA + `uname -r` + snap-vs-deb detection + `systemctl status intune-agent` + `systemctl enable --user --now intune-agent.timer` mandate
- `.planning/ROADMAP.md` line 506 — append-only H2-block contract attribution (Phase 42 D-03)
- `.planning/REQUIREMENTS.md` §LIN-12 (line 35) — Linux L2 investigation guide; log paths + service commands mandate
- `.planning/REQUIREMENTS.md` §"Traceability" line 154 — LIN-12 → `docs/l2-runbooks/24-linux-log-collection.md + 25-linux-agent-investigation.md` (1:1 file-name lock; 2 files exactly)
- `.planning/REQUIREMENTS.md` §AUDIT-06 (line 87) — Validator-as-deliverable + CI registration
- `.planning/REQUIREMENTS.md` §"Out of Scope" lines 104-117 — explicit exclusions

### Phase 51 (Linux L1 Triage + Runbooks 30-33) — Phase 52 directly consumes

- `.planning/phases/51-linux-l1-triage-runbooks-30-33/51-VERIFICATION.md` — Phase 51 close gate (PASSED 2026-04-27 per status frontmatter); 25/25 V-51-NN PASS + 5/5 ROADMAP SC + 5/5 LIN reqs satisfied + spot-checked CONTEXT decisions
- `.planning/phases/51-linux-l1-triage-runbooks-30-33/51-CONTEXT.md` D-04 (PITFALL-2 layered defense — tree level + runbook level + validator regex) / D-09 (anchor-indexed H2-per-cause shape — Phase 52 GA-2A inheritance source) / D-11 (cause anchor naming convention) / D-13 (read-vs-write apt distinction; Adversary disprove 3B-5 ADOPTED) / D-15 (per-runbook actor boundary) / D-19 (full validator scope precedent) / D-20 (file-reads-only validator pattern) / D-21 (anchor-string-pinned validator-CONTEXT brittleness contract) / D-22 (single atomic commit Goldilocks-rule) / DPO-01 (L2 cross-links L1 cause anchors as locked surfaces) / DPO-02 (read-vs-write apt distinction propagated to L2) / DPO-04 (CA-row wording inheritance for Phase 58) / CDI-Phase51-05 (read-vs-write × per-runbook actor-boundary) / CDI-Phase51-06 (validator scope × single atomic commit forced by append-assertion)
- `docs/decision-trees/09-linux-triage.md` — Phase 51 tree; cross-reference target for Runbook 24/25 "From L1 escalation?" reference blocks (DPO-01 anchor surface upstream)
- `docs/l1-runbooks/30-linux-enrollment-failed.md` — L1 cause anchors (DPO-01 locked surfaces): `#cause-a-package-install` / `#cause-b-sign-in-failure` / `#cause-c-enrollment-timeout`. Phase 52 Runbook 25 Trap A (kernel) cross-links Cause A (package-install) when kernel-track mismatch causes deb install failure; Trap D (Identity Broker) cross-links Cause B (sign-in-failure)
- `docs/l1-runbooks/31-linux-compliance-non-compliant.md` — L1 cause anchors: `#cause-a-distro-version-out-of-range` / `#cause-b-disk-not-encrypted` / `#cause-c-password-policy-not-met` / `#cause-d-custom-compliance-failure`. Phase 52 Runbook 24 Section 2 (file-based paths) cross-links Cause D (custom-compliance Bash failure) for `/var/log/intune-update.log` reading
- `docs/l1-runbooks/32-linux-ca-blocking-web-access.md` — L1 cause anchors: `#cause-a-not-enrolled` / `#cause-b-non-compliant` / `#cause-c-edge-not-signed-in`. Phase 52 generally does NOT cross-link Runbook 32 (CA-blocking is portal-observable; no L2 escalation typically required) but Runbook 25 Trap C (service-state) may cross-link Cause B (non-compliant) when service-state failure causes compliance regression
- `docs/l1-runbooks/33-linux-agent-service-failure.md` — Single-cause file (no anchors); Phase 52 Runbook 25 Trap C (service-state) is the primary L2 escalation target from L1 Runbook 33

### Phase 50 (Linux Admin Setup + Capability Matrix) — Phase 52 references

- `.planning/phases/50-linux-admin-setup-capability-matrix/50-CONTEXT.md` — Phase 50 admin setup contract; Phase 52 Trap D (Identity Broker) cross-links Phase 50 LIN-05 admin-side Identity Broker callout
- `docs/admin-setup-linux/01-intune-linux-agent.md` — Phase 50 LIN-05 Identity Broker `> ⚠️ Known admin pitfall` callout; Phase 52 Runbook 25 Trap D back-links via `_glossary-linux.md#microsoft-identity-broker` anchor (CD-07)
- `docs/admin-setup-linux/03-compliance-policy.md` — Phase 50 file with PITFALL-2 architectural callout (literal "Require device to be marked as compliant"); Phase 52 V-52-21 negative regression-guard at Runbook 25 prevents reintroduction
- `docs/reference/linux-capability-matrix.md` — Phase 50 bilateral matrix; Phase 52 Runbook 24/25 may cross-link `#conditional-access` (PITFALL-2 cascade), `#compliance-policies`, `#device-enrollment` H2 anchors

### Phase 49 (Linux Foundation) — Phase 52 directly consumes

- `.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VERIFICATION.md` — Phase 49 close gate; Phase 52 reads at startup to confirm glossary anchors exist
- `docs/_glossary-linux.md` — Phase 49 Linux glossary (~21 native term anchors); Phase 52 Runbook 24/25 cross-link ~10-12 of these (intune-portal, microsoft-identity-broker, intune-agent.timer, journalctl, systemd, dpkg, apt, web-app CA, Edge for Linux, dm-crypt, LUKS, custom-compliance) per V-52-18
- `docs/linux-lifecycle/00-enrollment-overview.md` — Phase 49 whitelist H2 + Out-of-Scope H2; Phase 52 may reference for full Linux capability boundary in `## Tool Landscape` blockquote
- `docs/linux-lifecycle/01-linux-prerequisites.md` — Phase 49 version matrix (Ubuntu 20.04 dropped Aug 2025; 22.04 LTS supported; 24.04 LTS supported) + Identity Broker Non-version Breakpoints H3; Phase 52 Runbook 25 Trap A (kernel HWE/GA) references this matrix for canonical version-track wording

### v1.5 research (authoritative for Linux scope, pitfalls, and tooling)

- `.planning/research/SUMMARY.md` lines 25-35 — Linux Intune client scope (Ubuntu 22.04/24.04 supported; Identity Broker v2.0.2+ behavior; web-app CA via Edge 102.x+ ONLY)
- `.planning/research/SUMMARY.md` lines 65-75 — Pillar 2 Linux must-haves (LIN-12 surface)
- `.planning/research/SUMMARY.md` line 282 — `/var/log/microsoft/intune/` LOW-MEDIUM confidence rationale (Microsoft Learn coverage gap; journalctl is canonical primary surface)
- `.planning/research/SUMMARY.md` lines 250-285 — confidence ratings, open gaps; PITFALL-3 snap-vs-deb confidence framing
- `.planning/research/PITFALLS.md` §Pitfall 1 (lines 11-31) — Linux capability parity framing; PITFALL-7 whitelist-first lock (drives V-52-20 mode-axis token regression-guard via Phase 51 V-51-07 inheritance)
- `.planning/research/PITFALLS.md` §Pitfall 2 (lines 33-56) — CA bait; literal "Not supported — web-app CA only" cell phrasing; warning sign re. blank/Partial CA cell (drives V-52-21 PITFALL-2 negative regression-guard via Phase 51 V-51-19 inheritance)
- `.planning/research/PITFALLS.md` §Pitfall 3 (lines 58-77) — Snap vs deb confusion; deb-only via packages.microsoft.com; `last_verified` discipline (drives V-52-09 PITFALL-3 negative regression-guard + Runbook 25 Trap B content)
- `.planning/research/PITFALLS.md` §Pitfall 4 (lines 81-99) — kernel HWE vs GA divergence; `uname -r` verification step (drives V-52-12 + Runbook 25 Trap A content)
- `.planning/research/PITFALLS.md` §Pitfall 5 (lines 101-122) — glossary collision: agent/compliance/enrollment/portal at-risk terms (drives V-52-18 glossary anchor consumption check)
- `.planning/research/PITFALLS.md` §Pitfall 7 (lines 147-166) — link-not-copy contract (drives DPO-Phase52-02 Phase 58 link-not-copy + D-09 reference-only L1 cross-link)
- `.planning/research/PITFALLS.md` §Pitfall 12 (lines 258-274) — sidecar pin coordinate stability; D-13 commit atomicity acknowledges PITFALL-12 motivation does NOT apply to Phase 52 (append target not pinned)
- `.planning/research/PITFALLS.md` §Pitfall 13 (lines 278-295) — ops-domain false-positive risk; relevant to V-52-21 PITFALL-2 negative-assertion via PITFALL-13 same-commit allowlist precedent

### v1.4 / v1.3 / v1.2 L2 runbook precedents (Phase 52 mirrors structurally)

- `docs/l2-runbooks/00-index.md` — Phase 52 append target; Android L2 H2 + table shape pattern (currently last existing H2 — Phase 52 appends after this)
- `docs/l2-runbooks/18-android-log-collection.md` — Decision-Matrix-with-method-sections shape (D-03 GA-1A winner template); per-row Confidence column shape (Layer 1 D-01); `> **Source confidence:**` blockquote shape (Layer 2 D-01 mirror at line 124-126); per-line confidence-token pattern (Layer 3 D-01 mirror at lines 137-149); Tool Landscape blockquote shape (D-03 mirror at line 22)
- `docs/l2-runbooks/14-ios-log-collection.md` — iOS friction-ordered shape (rejected per 1B); Tool Landscape blockquote pattern (D-03 mirror); cross-platform L2 Tier shape reference
- `docs/l2-runbooks/01-log-collection.md` — Windows L2 single-method narrative (rejected per 1C); reference for understanding what "comprehensive single-tool" pattern looks like (Linux LACKS this)
- `docs/l2-runbooks/10-macos-log-collection.md` — macOS IntuneMacODC + Terminal artifacts shape; reference for cross-platform L2 corpus consistency
- `docs/l2-runbooks/02-esp-deep-dive.md` — Windows flat investigation sequence (Runbook 25 GA-2B candidate, rejected); reference for Step-1 / Step-2 / Step-3 shape (rejected for Phase 52 in favor of anchor-indexed Trap shape per D-05)
- `docs/l2-runbooks/19-android-enrollment-investigation.md` — Android Pattern A/B/C/D/E per-mode shape (Runbook 25 GA-2C candidate, rejected per 2C-1 CRIT PITFALL-1 mode-axis token regression); "From L1 escalation?" block pattern at lines 19-25 (D-09 + CD-04 reference; permissible for Phase 52 reference-only consumption)
- `docs/l1-runbooks/25-android-compliance-blocked.md` — anchor-indexed H2-per-cause shape (D-05 GA-2A winner template; mirror lines 33-44 anchor index pattern + lines 48-181 per-cause body shape); line 35 verbatim "Causes are independently diagnosable" framing inheritance for Phase 52 multi-trap structure

### v1.5 audit harness + sidecar (Phase 48 + Phase 51 deliverables; Phase 52 consumes)

- `scripts/validation/v1.5-milestone-audit.mjs` — C10 blocking check Linux frontmatter; C13 informational broken-link automation; Phase 52 files MUST satisfy all C1-C12 blocking
- `scripts/validation/v1.5-audit-allowlist.json` — sidecar; Phase 52 MAY add `c5_collision_allowlist[]` entry IF first false-positive surfaces in Runbook 25 Trap D (Identity Broker) referencing PITFALL-5 named at-risk terms (lazy-add per Phase 48 D-15 YAGNI principle)
- `scripts/validation/regenerate-supervision-pins.mjs` — `--report` mode invoked pre-commit; `--self-test` invoked post-commit; should be unaffected since Phase 52 append target is not pinned
- `scripts/validation/check-phase-49.mjs` — direct file-reads-only / regex-based pattern reference
- `scripts/validation/check-phase-50.mjs` — direct file-reads-only / regex-based pattern reference + V-50-NN check style
- `scripts/validation/check-phase-51.mjs` — primary template for `check-phase-52.mjs` (V-51-07 mode-axis regression-guard inherited as V-52-20; V-51-19 PITFALL-2 negative-assertion inherited as V-52-21; V-51-20 read-vs-write apt regex inherited as V-52-17; V-51-23 glossary anchor consumption inherited as V-52-18; V-51-24 audience-pattern positive-assertion INVERTED as V-52-16 negative-assertion polarity)
- `.github/workflows/audit-harness-v1.5-integrity.yml` — `check-phase-52.mjs` registered with lazy-skip per Phase 48 D-18; Phase 52 commit completes the registration

### Project-level context

- `.planning/PROJECT.md` §"Current Milestone: v1.5 Linux Platform, Operational Depth & Cross-Platform Cleanup" — full v1.5 scope; Pillar 2 Linux ops (line 187-192)
- `.planning/STATE.md` — current milestone state; v1.5 carry-forward patterns (lines 64-83); Phase 52 dependency in v1.5 dependency summary
- `.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-CONTEXT.md` D-08 (frontmatter contract; 60-day cycle for content files; markdown-link-check informational not blocking) / D-15 (YAGNI lazy-array-population for sidecar) / D-18 (`check-phase-NN.mjs` validator-as-deliverable contract; CI registration with lazy-skip) / D-25 (file-reads-only / no-shared-module contract)

### Downstream-phase dependencies (what Phase 52 unblocks)

- **Phase 56 (Drift Detection + Tenant Migration)** depends on: read-vs-write apt distinction inheritance (DPO-Phase52-01); freshness-caveat layered-defense pattern (DPO-Phase52-07) for any new file-path claims
- **Phase 58 (DEFER-08 4-Platform Capability Comparison)** depends on: Phase 52 Section/Trap anchor surface for Linux row ops/log column cells (DPO-Phase52-02); PITFALL-2 layered defense inheritance via Phase 51 DPO-04 cascade (DPO-Phase52-08)
- **Phase 59 (Hub Navigation Integration — Linux + Operations Sections)** depends on: Phase 52 anchor surface for `docs/index.md` Linux H2 + `docs/common-issues.md` Linux symptom routing + `docs/quick-ref-l2.md` Linux quick-reference card (DPO-Phase52-03)
- **Phase 60 (Audit Harness Finalization)** depends on: `check-phase-52.mjs` registered in `audit-harness-v1.5-integrity.yml` (Phase 48 D-18 lazy-skip pattern); C13 broken-link graduation considers Phase 52 cross-link surface (DPO-Phase52-04)
- **v1.5.1 (LIN-DEFER-01 Bash deep-dive)** depends on: L2 actor-framing inheritance (DPO-Phase52-05) + read-vs-write apt distinction propagation (Phase 51 DPO-06 + Phase 52 D-08)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- `scripts/validation/check-phase-51.mjs` — Phase 51 validator; primary template for `check-phase-52.mjs` (V-51-07/19/20/23/24 transferable shapes per D-10)
- `scripts/validation/check-phase-49.mjs` + `check-phase-50.mjs` — file-reads-only validator pattern exemplars (per Phase 48 D-25)
- `scripts/validation/v1.5-milestone-audit.mjs` (Phase 48 deliverable) — C10 blocking + C13 informational; Phase 52 files validated against both at commit time
- `scripts/validation/v1.5-audit-allowlist.json` (Phase 48 deliverable) — sidecar; Phase 52 may lazy-add `c5_collision_allowlist[]` if first false-positive surfaces
- `scripts/validation/regenerate-supervision-pins.mjs` — `--report` + `--self-test` modes used pre-commit + post-commit (no Phase 52 pin-coord modification expected)
- `docs/l2-runbooks/18-android-log-collection.md` — primary structural template for `24-linux-log-collection.md` (Decision-Matrix-with-method-sections shape; per-row Confidence column; `> **Source confidence:**` blockquote at line 124-126; per-line confidence-token pattern at lines 137-149)
- `docs/l1-runbooks/25-android-compliance-blocked.md` — primary structural template for `25-linux-agent-investigation.md` Trap H2 shape (lines 33-44 anchor index; lines 48-181 per-cause body)
- `docs/l2-runbooks/00-index.md` — append target; Android L2 section pattern (last existing H2 — Phase 52 appends after this)
- `docs/_glossary-linux.md` — Phase 49 Linux glossary; ~21 native term anchors that runbooks consume per Phase 49 DPO-04 + V-52-18
- `docs/decision-trees/09-linux-triage.md` — Phase 51 tree; Phase 52 may cross-reference branches via `09-linux-triage.md#linux-l1-triage-decision-tree` H2 anchor for L1→L2 routing context

### Established Patterns

- **L2 runbook frontmatter** (`audience: L2` + `platform: <platform>` + `applies_to: all` + 60-day `last_verified` / `review_by`): all 23 existing L2 runbooks (01-23) honor; Phase 52 inherits
- **`> **Platform gate:**` blockquote at top of L2 runbooks** (mirror 18-android-log-collection.md line 9 pattern): Phase 52 runbooks open with cross-platform-disambiguation banner
- **Append-only H2-block contract (Phase 42 D-03 / ROADMAP line 506):** D-14 step 2 honors at `00-index.md` by appending new H2 within existing list-blockquote sections without displacing content below
- **Atomicity-same-commit (Phase 42 D-20/D-22, Phase 43 D-07, Phase 48 D-14, Phase 50 D-18, Phase 51 D-22):** D-13 single atomic commit; CDI-Phase52-04 forces single-commit because validator V-52-19 append-assertion requires append target present
- **Validator-as-deliverable (v1.3+ pattern, Phase 48 D-18):** `check-phase-52.mjs` ships in single atomic commit; CI workflow (`audit-harness-v1.5-integrity.yml`) registers per Phase 48 D-18 lazy-skip pattern
- **Anchor-indexed H2-per-cause/trap (Phase 30 multi-cause runbooks; Runbook 25 Phase 40 precedent; Phase 51 D-09 inheritance):** D-05 honors with literal `## Trap X: [name] {#trap-x-anchor}` shape
- **Layered defense for architectural callouts (Phase 51 V-51-09/18/19):** D-01 inherits with three-layer freshness-caveat surface (matrix-cell + blockquote + per-line token)
- **L2-direct framing for L2 audience (existing 23 L2 runbooks; 0 occurrences of `> **Say to the user:**`):** D-08 inherits as net-zero L1-pattern import contract
- **L1 cross-link reference-only consumption (Anti-Pattern 1 + Phase 51 DPO-01):** D-09 inherits with literal anchor deep-links + Trap content cross-tier-distinct from L1 cause topology
- **60-day `last_verified` cycle (Phase 34 D-14):** All Phase 52 files carry `last_verified` + `review_by` frontmatter on 60-day cycle
- **`platform: Linux` frontmatter (C10 blocking from Phase 48):** All 2 new Linux content files carry `platform: Linux`
- **Read-vs-write apt distinction (Phase 51 D-13 / DPO-02):** Linux-specific pattern introduced in Phase 51 + propagated to Phase 52 (V-52-17 inverse polarity at L2 layer) + v1.5.1 LIN-DEFER-01

### Integration Points

- **Runbook 24 → Runbook 25 cross-reference:** Runbook 24 `## Context` H2 references Runbook 25 as next-step ("After collecting logs, see Runbook 25 for trap-by-trap investigation"); Runbook 25 `## Context` H2 references Runbook 24 as prerequisite ("Collect logs per Runbook 24 before starting investigation")
- **L1 → L2 routing surface:** Phase 51 L1 runbook 33 (single-cause agent service failure) routes to Phase 52 Runbook 25 Trap C (service-state); Phase 51 L1 runbook 30 Cause A (package-install) routes to Phase 52 Runbook 24 Section 2 + Section 3 (file-based paths + dpkg/apt queries) and Runbook 25 Trap B (snap-vs-deb)
- **Trap → Glossary cross-references:** Trap A → `_glossary-linux.md#hwe-kernel` + `#ga-kernel`; Trap B → `_glossary-linux.md#deb-repository` + `#snap`; Trap C → `_glossary-linux.md#systemd` + `#intune-agent-timer`; Trap D → `_glossary-linux.md#microsoft-identity-broker` + `#identity-broker-v2`
- **Section → capability-matrix cross-references:** Runbook 24 Section 1 (journalctl) may reference `linux-capability-matrix.md#monitoring` H2; Runbook 25 Trap A may reference `linux-capability-matrix.md#supported-management-surface` H2 for Ubuntu version-track context
- **`docs/l2-runbooks/00-index.md` Linux L2 Runbooks H2** is the bilateral discoverability surface; Phase 59 CLEAN-08 hub navigation extends this with `docs/index.md` + `docs/common-issues.md` + quick-ref entries
- **`check-phase-52.mjs`** is registered in `.github/workflows/audit-harness-v1.5-integrity.yml` (Phase 48 D-18 lazy-skip pattern); CI runs the validator on every PR + scheduled bitrot
- **VERIFICATION.md** is the gate Phase 56 plan author reads to confirm Phase 52 closed; literal validator output + Phase 51 DPO-01/02 inheritance verification + 4 SC# satisfaction proof + DPO-Phase52-NN propagation summary

</code_context>

<specifics>
## Specific Ideas

- **Adversarial-review traceability:** All 4 gray areas resolved via Finder/Adversary/Referee scored pattern (Phase 48/49/50/51 lineage). Finder enumerated 86 defects across 19 candidate options for 275 raw points (7 CRIT / 6 HIGH / 18 MED / ~55 LOW). Adversary attempted 7 disproves (3 MED + 4 LOW); Referee adopted 6 (1A-1 + 1A-2 + 2A-2 + 2A-3 + 3A-1 + 3A-3 = 14 points removed) and rejected 1 (GA-4-COUP-2 = 5 points retained as REAL ISSUE; grep-evidence shows `> **Say to the user:**` appears 0 times across all 23 existing L2 runbooks, confirming the L2 audience contract coupling defect). Net post-ruling defect score: 261 surviving points across 80 confirmed defects. Per-area winners locked: GA-1 = Option 1A (matrix-cell + adjacent blockquote layered freshness caveat); GA-2 = Option 2A (anchor-indexed H2-per-trap mirroring Phase 51 GA-2A precedent); GA-3 = layered three-surface caveat (Phase 51 V-51-09/18/19 inherited verbatim); GA-4-i = L2-direct framing (no `> **Say to the user:**`); GA-4-ii = reference-only L1 deep-link consumption (no cause-topology import). Downstream planner should read DISCUSSION-LOG.md alongside this CONTEXT to understand the rejected alternatives + the specific evidence chain that locked each winner.

- **"L2 audience-contract grep-evidence (GA-4-COUP-2 retained)":** Across all 23 existing L2 runbooks (`docs/l2-runbooks/01..23-*.md`), `> **Say to the user:**` appears 0 times. Pattern is exclusive to L1 (Phase 30 D-10, Runbook 22:31, V-51-24 anchored to `audience: L1`). Phase 52 importing this pattern would be a net-new audience-contract violation. Validator V-52-16 enforces NEGATIVE assertion (mirror-inverse of V-51-24) anchored to `audience: L2`. Adversary's claim that Phase 52 L2-audience scoping eliminates coupling was REJECTED on this evidence — actor-framing winner MUST be L2-direct AND cross-link surface MUST be reference-only — together, not separately.

- **"ROADMAP:222 SC#1 verbatim shape mandate (GA-1A winner driver)":** "carries a `> LOW-MEDIUM confidence` freshness caveat on any `/var/log/microsoft/intune/` file-based path claim" — the `>` token literally specifies blockquote shape. Frontmatter (1C) + matrix-cell-only (1D) options structurally fail to satisfy this literal. Validator V-52-07 pins Layer 2 blockquote shape; SC#1 satisfaction requires blockquote presence within ~10-line proximity to `/var/log/microsoft/intune/` reference. Layered defense (V-52-06 + V-52-07 + V-52-08) ensures all three caveat layers are present and prevents drift via string-pinning per Phase 51 D-21 precedent.

- **"Anti-Pattern 1 cross-tier scoping (D-09 + CDI-Phase52-03)":** Phase 51 D-04 explicitly defines Anti-Pattern 1 as "authored content-block duplication," NOT multi-layer assertion (51-CONTEXT line 35). Phase 52's L1-cross-link reference blocks DEEP-LINK to L1 cause anchors but do NOT re-author L1 cause topology. Trap content (kernel/HWE, snap-vs-deb, service-state, Identity Broker) is L2-investigation-surface-distinct from L1-cause-symptom content (package-install / sign-in / enrollment-timeout / agent-service-failure). Plan author for 52-02 (Runbook 25) MUST verify each Trap body content is cross-tier-distinct from L1 Cause body content via overlap audit at authoring time — overlap-audit is human-verifiable, not validator-asserted.

- **"Phase 51 V-51-09/18/19 layered-defense precedent transferability (GA-3 winner)":** Three independent assertion surfaces on identical PITFALL-2 anchor (tree Mermaid + runbook positive + runbook negative regression-guard) shipped in Phase 51 with all 25 V-51-NN PASS in independent verification (51-VERIFICATION lines 21-47). String-pinning at the validator layer prevents drift between content layers — pinning IS the anti-drift surface (Adversary disprove 3A-3 ADOPTED reasoning). Phase 52 inherits this pattern verbatim for freshness caveats: Layer 1 (matrix-cell confidence token) + Layer 2 (Source confidence blockquote) + Layer 3 (per-line confidence tokens) all pinned by validator regex; drift between layers is caught by the validator at commit time.

- **"REQUIREMENTS.md:154 LIN-12 file-name lock (GA-2 2D rejection)":** `LIN-12 | 52 | docs/l2-runbooks/24-linux-log-collection.md + 25-linux-agent-investigation.md` — the traceability table pins a 1:1 mapping (LIN-12 → 2 files exactly). Splitting Runbook 25 into 25a/25b/25c gives 4 files (24 + 25a + 25b + 25c) and breaks the 1:1 traceability lock. SC#3 (ROADMAP:224) pins 2 runbooks. SC#4 (ROADMAP:225) pins "both runbooks" — exactly two. 2D-1 CRIT confirmed; option 2D structurally infeasible without modifying REQUIREMENTS.md (which is finalized 2026-04-26 per traceability frontmatter).

- **"PITFALL-1 V-51-07 mode-axis token regression-guard inheritance (GA-2 2C rejection)":** Phase 51 V-51-07 forbids tokens BYOD/COBO/COPE/Dedicated/ZTE/AOSP in the Linux triage tree (per CONTEXT line 71 + check-phase-51.mjs:117-130). Phase 52 V-52-20 inherits the same regression-guard at the L2 layer — Pattern A/B/C imported from 19-android-enrollment-investigation.md would carry these forbidden tokens (the Pattern names themselves are mode-axis vocabulary), triggering validator FAIL. PITFALL-1 whitelist-first cascade from Phase 49 foundation → Phase 51 tree shape → Phase 52 L2 runbook structure is the multi-phase enforcement chain.

- **"Read-vs-write apt distinction at L2 layer (DPO-02 inheritance + V-52-17)":** Phase 51 GA-3B-5 sub-rule winner (Adversary disprove 3B-5 ADOPTED at Phase 51) propagates to Phase 52 via DPO-02. L2 engineer's read-only diagnostic queries (`apt list --installed | grep intune-portal`, `dpkg -l intune-portal`, `systemctl --user status`, `journalctl --user`) MUST NOT carry sudo prefixes — same as L1 layer. Sudo prefixes appear ONLY on root-required state-changing commands (`apt install`, `apt update`, `systemctl restart`, `systemctl daemon-reload`) or `journalctl --system` reads of root-only journals, AND only inside `### Resolution` H3 sections per Phase 30 D-10 read-only-scope-extended-to-L2 contract. Validator V-52-17 regex-enforces.

</specifics>

<deferred>
## Deferred Ideas

- **PITFALL-5 collision-audit graduation/generalization to v1.5 harness:** Phase 51 deferred forward (per 51-CONTEXT line 307); Phase 52 may surface false-positives if Runbook 25 Trap D (Identity Broker) references PITFALL-5 named at-risk terms (agent / compliance / enrollment / portal); first-occurrence triggers lazy `c5_collision_allowlist[]` addition per Phase 48 D-15 YAGNI. If ≥3 false-positives accumulate across Phases 51-58, Phase 60 considers C5 promotion to v1.5 harness check (e.g., C14).

- **PITFALL-13 false-positive allowlist for V-52-21 negative-assertion edge case:** If Runbook 25 Trap D (Identity Broker) ever quotes the architectural callout from Phase 50 `03-compliance-policy.md` (which contains the literal "Require device to be marked as compliant" string per Phase 51 V-51-19 inheritance) to remind L2 of the Linux constraint, V-52-21 negative-regression-guard would fire false-positive. First-occurrence triggers same-commit allowlist addition; lazy-add per Phase 48 D-15 YAGNI; if it doesn't surface in Phase 52 authoring, no allowlist entry needed.

- **C13 broken-link graduation against Phase 52 cross-links:** Per DPO-Phase52-04; Phase 60 Audit Harness Finalization considers graduating C13 to blocking after Phase 51-60 cross-link surface stabilizes. Phase 52 introduces ~6-8 new cross-link surfaces per runbook; Phase 60 evaluates aggregate v1.5 cross-link count for graduation decision.

- **Hub navigation extensions (Phase 59):** `docs/index.md` Linux Operations H2 + `docs/common-issues.md` Linux symptom routing + `docs/quick-ref-l2.md` Linux quick-reference card. These are Phase 59 CLEAN-08 work; Phase 52 only delivers the bilateral discoverability append at `docs/l2-runbooks/00-index.md`. The wider hub integration is explicitly NOT in scope for Phase 52.

- **DEFER-08 4-platform comparison Linux row L2 cells (Phase 58):** Per DPO-Phase52-02. Phase 58 inherits Phase 52 Section/Trap anchor surface for ops/log column cells; Phase 52 does not author the comparison doc itself.

- **DRIFT-07 Linux drift section (Phase 56):** Per DPO-Phase52-01 + DPO-Phase52-07. Phase 56 owns the cross-platform encryption-drift content + Linux-specific drift detection runbooks; Phase 52 Runbook 25 may reference the future Phase 56 surface via `_glossary-linux.md` glossary anchors but does NOT author full drift narrative.

- **v1.5.1 LIN-DEFER-01 Linux Bash custom-compliance deep-dive guide:** Per DPO-Phase52-05 + Phase 51 DPO-06. Phase 52 Runbook 25 Trap C (service-state) may reference custom-compliance Bash exit-code reading at `/var/log/intune-update.log` at L2 orientation level only; standalone deep-dive ships in v1.5.1 with full Bash-vs-PowerShell mapping per Phase 50 Cross-Platform Equivalences.

- **L2 actor-framing pattern documentation in CLAUDE.md or contributing guide:** Phase 52 establishes V-52-16 as a NEGATIVE assertion (no `> **Say to the user:**` in L2 audience). Phase 56+ should inherit; if a future phase author imports L1 framing into L2 docs, validator catches at commit time. Long-term, the L1/L2 audience-framing distinction could be promoted to a top-level project contributing-guide doc — deferred to milestone close.

- **PITFALL-3 negative regression-guard (V-52-09 snap-install detection) re-evaluation at Phase 60:** Phase 60 considers whether V-52-09 should be promoted from Phase 52-scoped to v1.5 harness-wide (e.g., as C14 or C15) since snap-vs-deb confusion may surface in Phase 56 DRIFT-07 or v1.5.1 Bash deep-dive.

### Reviewed Todos (not folded)

*(No pending todos matched Phase 52 scope per `todo match-phase 52` (todo_count: 0 at orchestrator startup). No todos reviewed-but-deferred.)*

</deferred>

---

*Phase: 52-linux-l2-investigation-runbooks-24-25*
*Context gathered: 2026-04-27*
*Adversarial review applied: Finder/Adversary/Referee on 4 gray areas covering 19 candidate options*
*Finder 275 raw → Adversary attempted 7 disproves → Referee adopted 6 (14 points removed; 261 surviving across 80 confirmed defects)*
