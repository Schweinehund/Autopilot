---
phase: 52
plan: 01
type: execute
wave: A
depends_on: []
files_modified:
  - docs/l2-runbooks/24-linux-log-collection.md
autonomous: true
requirements: [LIN-12]
requirements_addressed: [LIN-12]

must_haves:
  truths:
    - "docs/l2-runbooks/24-linux-log-collection.md exists with frontmatter (platform: Linux, audience: L2, applies_to: all, last_verified: 2026-04-27, review_by: 2026-06-26) — 60-day cycle per CONTEXT D-04 + ROADMAP:225 SC#4"
    - "Runbook 24 mirrors 18-android-log-collection.md / 14-ios-log-collection.md Decision-Matrix-with-method-sections shape per CONTEXT D-03 (frontmatter -> Platform gate -> H1 -> Context -> Tool Landscape -> Decision Matrix -> Method Selection -> Section 1 journalctl -> Section 2 file-based paths -> Section 3 package-state queries -> Common Artifacts Cross-Reference -> Related Resources -> Version History)"
    - "Section 1 (journalctl HIGH) contains literal `journalctl -u intune-agent` AND literal `journalctl | grep intune-portal` per CONTEXT D-02 + ROADMAP:222 SC#1 + V-52-10"
    - "Section 2 (file-based paths) contains literal `/var/log/intune-update.log` AND literal `/var/log/dpkg.log` per CONTEXT D-02 + ROADMAP:222 SC#1 + V-52-10"
    - "Section 2 carries the 3-layer LOW-MEDIUM caveat surface on `/var/log/microsoft/intune/` per CONTEXT D-01 + CDI-Phase52-06: Layer 1 = Decision Matrix table cell with `[LOW-MEDIUM, last_verified 2026-04-27]` confidence token (V-52-06); Layer 2 = `> **Source confidence:**` blockquote near Section 2 opening with `LOW-MEDIUM confidence` token AND `/var/log/microsoft/intune/` path within ~10-line proximity (V-52-07); Layer 3 = at least 2 per-line `[LOW-MEDIUM, last_verified` inline tokens at command-snippet level (V-52-08)"
    - "Runbook 24 does NOT contain forbidden PITFALL-3 strings per CONTEXT V-52-09: NO `snap install`, NO `/var/snap/intune-portal/`, NO `snap container` (snap-vs-deb regression-guard; only acceptable snap reference is `snap list 2>/dev/null | grep -i intune` as a NEGATIVE-SPACE detection command)"
    - "Runbook 24 contains NO `> **Say to the user:**` blockquotes per CONTEXT D-08 + V-52-16 + CDI-Phase52-05 (L2-direct framing; this pattern is L1-exclusive — 0 occurrences across all 23 existing L2 runbooks per CONTEXT specifics line 269)"
    - "Runbook 24 honors read-vs-write apt distinction per CONTEXT DPO-Phase52-01 + V-52-17: NO `sudo apt list`, NO `sudo dpkg -l`, NO `sudo systemctl --user`, NO `sudo journalctl --user` anywhere in file"
    - "Runbook 24 contains NO Android mode-axis tokens per V-52-20 (no `BYOD`, `COBO`, `COPE`, `Dedicated`, `ZTE`, `AOSP`, `COSU` — Linux has single delivery path per Phase 49 whitelist-first)"
    - "Runbook 24 contains at least 1 link to `../_glossary-linux.md#<anchor>` per V-52-18 (Phase 49 DPO-04 inheritance)"
    - "Runbook 24 contains NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens per V-52-22"
    - "Runbook 24 references Runbook 25 in Context H2 as next-step (cross-reference per Pattern Map integration-points line 254) — `25-linux-agent-investigation.md`"
  artifacts:
    - path: "docs/l2-runbooks/24-linux-log-collection.md"
      provides: "Linux L2 log collection guide — Decision Matrix + 3 method sections (journalctl HIGH / file paths mixed / package-state HIGH)"
      contains: ["platform: Linux", "audience: L2", "last_verified: 2026-04-27", "review_by: 2026-06-26", "> **Platform gate:**", "# Linux Log Collection Guide", "## Tool Landscape", "## Decision Matrix", "## Method Selection", "## Section 1: journalctl", "## Section 2: File-based paths", "## Section 3: Package-state queries", "journalctl -u intune-agent", "journalctl | grep intune-portal", "/var/log/intune-update.log", "/var/log/dpkg.log", "[LOW-MEDIUM, last_verified 2026-04-27]", "> **Source confidence:**", "LOW-MEDIUM confidence", "/var/log/microsoft/intune/", "../_glossary-linux.md#"]
  key_links:
    - from: "docs/l2-runbooks/24-linux-log-collection.md"
      to: "docs/l2-runbooks/25-linux-agent-investigation.md"
      via: "## Context H2 cross-reference (next-step) per Pattern Map integration-points"
      pattern: "25-linux-agent-investigation.md"
    - from: "docs/l2-runbooks/24-linux-log-collection.md"
      to: "docs/_glossary-linux.md"
      via: "glossary anchor consumption per CONTEXT DPO-04 inheritance + V-52-18"
      pattern: "../_glossary-linux.md#"
    - from: "docs/l2-runbooks/24-linux-log-collection.md"
      to: "docs/l1-runbooks/30-linux-enrollment-failed.md / 31-linux-compliance-non-compliant.md"
      via: "Common Artifacts Cross-Reference H2 (DPO-01 inheritance from Phase 51)"
      pattern: "../l1-runbooks/3"
---

<objective>
Author `docs/l2-runbooks/24-linux-log-collection.md` — the Linux L2 log collection guide (LIN-12 part 1; SC#1). Decision-Matrix-with-method-sections shape mirroring `docs/l2-runbooks/18-android-log-collection.md` per CONTEXT D-03. Three method sections: Section 1 = `journalctl` (HIGH), Section 2 = file-based paths (mixed: `/var/log/dpkg.log` HIGH, `/var/log/intune-update.log` HIGH, `/var/log/microsoft/intune/` LOW-MEDIUM with 3-layer caveat per D-01), Section 3 = `dpkg`/`apt` package-state queries (HIGH).

Purpose: Satisfies SC#1 (covers all 4 confirmed log surfaces verbatim + carries `> LOW-MEDIUM confidence` blockquote on `/var/log/microsoft/intune/` per ROADMAP:222). Layered freshness-caveat surface (matrix-cell + blockquote + per-line tokens) inherits Phase 51 V-51-09/18/19 layered-defense pattern per CDI-Phase52-06. Provides the prerequisite log-collection surface for Runbook 25 investigation traps.

Output: 1 markdown file at `docs/l2-runbooks/24-linux-log-collection.md` that passes V-52-01, V-52-05, V-52-06, V-52-07, V-52-08, V-52-09, V-52-10, V-52-16 (negative for RB24), V-52-17 (RB24 scope), V-52-18 (RB24 scope), V-52-20 (RB24 scope), V-52-22 (RB24 scope) in `check-phase-52.mjs`.

DO NOT commit yet — atomic commit owned by 52-05 per CONTEXT D-13 + DPO-Phase52-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-CONTEXT.md
@.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-RESEARCH.md
@.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-PATTERNS.md
@docs/l2-runbooks/18-android-log-collection.md
@docs/l2-runbooks/14-ios-log-collection.md
@docs/_glossary-linux.md
@docs/l1-runbooks/30-linux-enrollment-failed.md
@docs/l1-runbooks/31-linux-compliance-non-compliant.md

<interfaces>
The runbook consumes:
- Phase 49 anchors: `docs/_glossary-linux.md#journalctl`, `#systemd`, `#dpkg`, `#apt`, `#intune-portal` (V-52-18 — at least 1 link required)
- Phase 51 L1 cause anchors (DPO-01 locked surfaces — referenced from Common Artifacts Cross-Reference H2):
  - `../l1-runbooks/30-linux-enrollment-failed.md#cause-a-package-install` (Section 2 + Section 3 cross-reference for package-install diagnostics)
  - `../l1-runbooks/31-linux-compliance-non-compliant.md#cause-d-custom-compliance-failure` (Section 2 cross-reference for /var/log/intune-update.log custom-compliance reading)
- 18-android-log-collection.md analog (CONTEXT D-03; verified read):
  - Lines 1-7: frontmatter shape
  - Line 9: `> **Platform gate:**` blockquote shape
  - Line 11: H1 shape
  - Lines 13-19: `## Context` H2 shape
  - Lines 21-23: `## Tool Landscape` H2 + blockquote
  - Lines 25-34: `## Decision Matrix` H2 + table with Confidence column (Layer 1 D-01)
  - Lines 36-46: `## Method Selection` H2 (mode-axis routing — REPLACED for Linux narrow surface)
  - Line 48: `## Section 1:` H2 shape
  - Lines 120-128: Section opening with `> **Source confidence:**` blockquote (Layer 2 D-01)
  - Lines 137-149: Per-tag inline `[CONFIDENCE, last_verified DATE]` token pattern (Layer 3 D-01)
  - Lines 182-191: `## Common Artifacts Cross-Reference` H2 + table
  - Lines 193-204: `## Related Resources` + `## Version History`

The runbook provides (consumed downstream):
- Anchor `24-linux-log-collection.md` for Runbook 25 Context H2 prerequisite reference (52-02)
- Anchor `24-linux-log-collection.md#section-1-journalctl` for 00-index.md table prerequisite (52-04)
- Anchor surface for Phase 58 4-platform comparison Linux row ops/log column cells (DPO-Phase52-02 future)
- Anchor surface for Phase 59 hub navigation (docs/index.md, docs/common-issues.md, docs/quick-ref-l2.md) (DPO-Phase52-03 future)

Author discretion (CD per CONTEXT):
- CD-01: Exact wording of Layer 2 `> **Source confidence:**` blockquote (form + scope locked; specific verb choice author discretion within "explains LOW-MEDIUM confidence is path-related Microsoft Learn coverage gap, not journalctl-related")
- CD-03: Section depth — Decision Matrix row count, Method Selection list-item count, Section 1/2/3 body length scale to coverage need; minimum is V-52-10 SC#1 file-path enumeration coverage
- CD-06: Mermaid usage — optional; validator does NOT assert Mermaid presence
- CD-08: L2 callout label uniformity (`> **Note:**` vs `> **Source confidence:**`) — author discretion; validator does NOT assert label uniformity

Forbidden patterns (validator-enforced negative regression-guards):
- `> **Say to the user:**` (V-52-16 — L2 audience contract)
- `sudo apt list`, `sudo dpkg -l`, `sudo systemctl --user`, `sudo journalctl --user` (V-52-17 — DPO-Phase52-01 read-vs-write apt distinction)
- `snap install`, `/var/snap/intune-portal/`, `snap container` (V-52-09 — PITFALL-3)
- `BYOD`, `COBO`, `COPE`, `Dedicated`, `ZTE`, `AOSP`, `COSU` (V-52-20 — PITFALL-1 mode-axis regression-guard)
- `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` (V-52-22)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/l2-runbooks/24-linux-log-collection.md (frontmatter + Platform gate + Context + Tool Landscape + Decision Matrix with 3 confidence-token rows)</name>
  <files>docs/l2-runbooks/24-linux-log-collection.md</files>
  <read_first>
    - docs/l2-runbooks/18-android-log-collection.md (PRIMARY analog — read FULL FILE; frontmatter lines 1-7; platform gate line 9; H1 line 11; Context lines 13-19; Tool Landscape lines 21-23; Decision Matrix lines 25-34; Method Selection lines 36-46)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-PATTERNS.md (Section "docs/l2-runbooks/24-linux-log-collection.md" — paste-ready frontmatter, platform gate, H1, Tool Landscape blockquote, Decision Matrix shape with confidence tokens; lines 26-185)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-RESEARCH.md (Research Area 7 lines 429-449 — 3-row Decision Matrix table; Research Area 1 lines 197-232 — canonical journalctl command set)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-CONTEXT.md (D-01 layered caveat; D-02 surface confidence ranking; D-03 shape; D-04 frontmatter; D-08 L2-direct framing; CDI-Phase52-01/06)
  </read_first>
  <behavior>
    - File exists at `docs/l2-runbooks/24-linux-log-collection.md`
    - Frontmatter contains exactly `platform: Linux`, `audience: L2`, `applies_to: all`, `last_verified: 2026-04-27`, `review_by: 2026-06-26`
    - `> **Platform gate:**` blockquote appears immediately after closing `---` of frontmatter
    - H1 `# Linux Log Collection Guide` appears once
    - `## Context` H2 references Runbook 25 + 4 Phase 51 L1 escalation runbooks
    - `## Tool Landscape` H2 contains `> **Tool landscape:**` blockquote with single-primary-surface framing
    - `## Decision Matrix` H2 contains a 3-row markdown table with the column header `Confidence (Layer 1)` AND a row with the `/var/log/microsoft/intune/` path AND a `[LOW-MEDIUM, last_verified 2026-04-27]` token
    - `## Method Selection` H2 uses linear narrative (data-scope routing, NOT mode-axis routing)
    - Forbidden tokens absent (V-52-09/16/17/20/22 negative regression-guards)
  </behavior>
  <action>
Create `docs/l2-runbooks/24-linux-log-collection.md` containing the following sections IN ORDER. Sections 8-10 are added in Task 2; finish Sections 1-7 in this task.

**Section 1: Frontmatter (paste verbatim — CONTEXT D-04 + per V-52-05):**
```
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: all
audience: L2
platform: Linux
---
```

**Section 2: Platform gate blockquote (paste verbatim per PATTERNS.md line 47 adapted for Linux + 18-android-log-collection.md line 9 shape):**
```
> **Platform gate:** This guide covers Linux Intune client (`intune-portal`) L2 investigation via Microsoft Intune. For Windows Autopilot, see [Windows L2 Runbooks](00-index.md). For macOS ADE, see [macOS ADE Runbooks](00-index.md#macos-ade-runbooks). For iOS/iPadOS, see [iOS L2 Runbooks](00-index.md#ios-l2-runbooks). For Android Enterprise, see [Android L2 Runbooks](00-index.md#android-l2-runbooks).
```

**Section 3: H1 + Context H2 (paste verbatim — content adapted per PATTERNS.md lines 65-71):**
```
# Linux Log Collection Guide

## Context

This guide is the **prerequisite for all Linux L2 investigation runbooks** — collect log surfaces named here before opening [Linux Agent Investigation (Runbook 25)](25-linux-agent-investigation.md). It also serves as the L2 reading point for tickets escalated from L1, including:

- [L1 30: Linux Enrollment Failed](../l1-runbooks/30-linux-enrollment-failed.md) — package install / sign-in / enrollment-timeout symptoms
- [L1 31: Linux Compliance Non-Compliant](../l1-runbooks/31-linux-compliance-non-compliant.md) — distro / encryption / password / custom-compliance failures
- [L1 32: Linux CA Blocking Web-App Access](../l1-runbooks/32-linux-ca-blocking-web-access.md) — Edge for Linux web-app CA blocking (PITFALL-2)
- [L1 33: Linux Agent Service Failure](../l1-runbooks/33-linux-agent-service-failure.md) — `intune-agent.timer` not running

Linux Intune client has **a single confirmed primary log surface** (`journalctl`) — unlike Android (3-method fragmentation by enrollment mode) or Windows (`mdmdiagnosticstool.exe` single-archive) or macOS (IntuneMacODC zip). The Decision Matrix below routes by data-scope need, not enrollment mode (Linux has one supported delivery path: deb from `packages.microsoft.com` on Ubuntu 22.04/24.04 LTS).

> **From L1 escalation?** L1 collected the escalation packet (serial number, user UPN, `lsb_release -a`, `uname -r`, `dpkg -l intune-portal`, journalctl snapshot). Skip to the Decision Matrix to choose your method.
```

**Section 4: Tool Landscape H2 (paste verbatim — PATTERNS.md lines 85-89):**
```
## Tool Landscape

> **Tool landscape:** Linux Intune client (`intune-portal`) has a **single confirmed primary log surface: `journalctl`** (the systemd journal). Unlike Android (3-method fragmentation gated by enrollment mode) or macOS (IntuneMacODC zip) or Windows (`mdmdiagnosticstool.exe` archive), Linux has no Intune admin center per-device diagnostic download. The decision matrix below routes by data-scope need; there is no enrollment-mode axis (Linux has one supported delivery mode: deb from `packages.microsoft.com`). File-based log paths are secondary surfaces — `/var/log/dpkg.log` and `/var/log/intune-update.log` are HIGH/MEDIUM confidence; `/var/log/microsoft/intune/` is LOW-MEDIUM confidence (see Section 2 caveat).
```

**Section 5: Decision Matrix H2 (paste verbatim — Layer 1 caveat per CONTEXT D-01; PATTERNS.md lines 100-113 + RESEARCH.md Area 7 lines 440-444):**
```
## Decision Matrix

| Method | Who Triggers | Data Scope | Access Path | Physical Requirements | Confidence (Layer 1) | Typical Latency |
|--------|-------------|------------|-------------|----------------------|----------------------|----------------|
| **journalctl (systemd journal)** — Section 1 | L2 engineer (commands run by user on enrolled Linux endpoint) | Agent events, auth events, timer activations, broker errors, intune-portal GUI events | Direct terminal (SSH or physical) | Terminal access | `[HIGH, last_verified 2026-04-27]` | Seconds |
| **File-based paths** (`/var/log/dpkg.log`, `/var/log/intune-update.log`, `/var/log/microsoft/intune/`) — Section 2 | L2 engineer (read-only commands run on endpoint) | Package install history, agent update events, (LOW-MEDIUM) intune-specific component logs | Direct terminal (read-only) | Terminal access | `/var/log/dpkg.log`: `[HIGH, last_verified 2026-04-27]`; `/var/log/intune-update.log`: `[MEDIUM, last_verified 2026-04-27]`; `/var/log/microsoft/intune/`: `[LOW-MEDIUM, last_verified 2026-04-27]` | Seconds |
| **Package-state queries** (`dpkg -l`, `apt list --installed`) — Section 3 | L2 engineer (read-only) | Installed package version, delivery path (deb confirmation; snap-detection negative-space) | Direct terminal (read-only) | Terminal access | `[HIGH, last_verified 2026-04-27]` | Seconds |
```

**Section 6: Method Selection H2 (paste verbatim — narrow-surface linear narrative per RESEARCH.md Area 7 line 446 + PATTERNS.md lines 116-122):**
```
## Method Selection

Linux's single supported delivery path means method selection is by **data-type need**, not by enrollment mode:

1. **Start with Section 1 (journalctl)** for ALL investigations. The systemd journal contains agent events, timer activations, broker errors, GUI launch events — virtually every L2-relevant signal Microsoft documents.
2. **Move to Section 2 (file-based paths)** when journalctl output is inconclusive — for example, to read `/var/log/dpkg.log` for the package install timeline, `/var/log/intune-update.log` for custom-compliance script exit codes, or (LOW-MEDIUM confidence) `/var/log/microsoft/intune/` for any intune-component file logs the device may have written.
3. **Use Section 3 (package-state queries)** to confirm the delivery path (deb from `packages.microsoft.com`), package version, and to detect erroneous snap installations (negative-space check per PITFALL-3).
```

**Section 7: Section 1 H2 + journalctl content (paste verbatim — RESEARCH.md Area 1 lines 200-220 + V-52-10 literal coverage):**
````
## Section 1: journalctl

`journalctl` is the HIGH-confidence primary log surface for Linux Intune client. The systemd journal aggregates events from `intune-agent`, `intune-agent.timer`, `microsoft-identity-broker` (user scope), and `microsoft-identity-device-broker` (system scope), plus GUI launch events from `intune-portal`.

See [journalctl](../_glossary-linux.md#journalctl) for the systemd logging tool used as the primary log surface and [systemd](../_glossary-linux.md#systemd) for the service-management framework.

### 1.1 Agent service events `[HIGH, last_verified 2026-04-27]`

Run on the enrolled Linux endpoint:

```bash
# System-scope agent events (the canonical SC#1 literal command)
journalctl -u intune-agent --no-pager
journalctl -u intune-agent --since "1 hour ago" --no-pager
```

> **Note:** `intune-agent.timer` is the confirmed user-scope unit per Phase 51 Runbook 33 walkthrough. The `journalctl -u intune-agent` form (without `--user`) is documented in ROADMAP SC#1 and may target a system-scope unit or alias on some Ubuntu installs; if it returns "Unit not found," use the user-scope form below.

```bash
# User-scope timer events
journalctl --user -u intune-agent.timer --since "1 hour ago" --no-pager
```

### 1.2 intune-portal GUI events `[HIGH, last_verified 2026-04-27]`

```bash
# SC#1 literal — captures intune-portal GUI launch events and error signals
journalctl | grep intune-portal
journalctl --since "1 day ago" | grep intune-portal
```

### 1.3 Identity Broker events `[HIGH, last_verified 2026-04-27]`

The Microsoft Identity Broker has TWO scopes (see [microsoft-identity-broker](../_glossary-linux.md#microsoft-identity-broker)):

```bash
# User-scope broker — authentication, SSO sessions, PRT acquisition (no sudo per DPO-Phase52-01)
journalctl --user -u microsoft-identity-broker --since "1 hour ago" --no-pager

# System-scope broker — device registration state (may require sudo for full system journal visibility)
sudo journalctl -u microsoft-identity-device-broker --since "1 hour ago" --no-pager
```

### 1.4 Useful flags

- `--no-pager`: essential for L2 — output streams without interactive pager (`q` to quit) so you can pipe to a file or clipboard
- `--since "1 hour ago"` / `--since "1 day ago"` / `--since "1 week ago"`: time-bounded fetch; human-readable relative form is supported
- `--output=cat` or `--output=short-iso`: useful for log archiving in escalation packets
- `--user`: scope flag for user-instance services; mutually exclusive with `sudo` per the read-vs-write apt distinction
````

DO NOT use the `> **Say to the user:**` blockquote pattern anywhere — that pattern is L1-exclusive (V-52-16 / CONTEXT D-08). Use `> **Note:**` or plain prose for L2 callouts.

DO NOT prefix any of these with `sudo`: `apt list`, `dpkg -l`, `systemctl --user`, `journalctl --user` (V-52-17 / DPO-Phase52-01).

DO NOT include any of: `BYOD`, `COBO`, `COPE`, `Dedicated`, `ZTE`, `AOSP`, `COSU` (V-52-20 — Linux has no enrollment-mode axis per Phase 49 whitelist-first).

DO NOT include `snap install`, `/var/snap/intune-portal/`, or `snap container` (V-52-09 — PITFALL-3 regression-guard).

DO NOT commit yet — atomic commit owned by 52-05.
  </action>
  <verify>
    <automated>test -f docs/l2-runbooks/24-linux-log-collection.md && grep -c "journalctl -u intune-agent" docs/l2-runbooks/24-linux-log-collection.md && grep -c "journalctl | grep intune-portal" docs/l2-runbooks/24-linux-log-collection.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/l2-runbooks/24-linux-log-collection.md`
    - File contains literal lines matching `/^platform: Linux\s*$/m` AND `/^audience: L2\s*$/m` AND `/^applies_to: all\s*$/m` AND `/^last_verified: 2026-04-27\s*$/m` AND `/^review_by: 2026-06-26\s*$/m` (V-52-05)
    - File contains `> **Platform gate:**` blockquote within first 20 lines after frontmatter
    - File contains H1 `# Linux Log Collection Guide`
    - File contains H2 `## Context`, `## Tool Landscape`, `## Decision Matrix`, `## Method Selection`, `## Section 1: journalctl`
    - File contains literal `journalctl -u intune-agent` (V-52-10)
    - File contains literal `journalctl | grep intune-portal` (V-52-10)
    - Decision Matrix table contains a row referencing `/var/log/microsoft/intune/` AND that row's Confidence column contains literal `[LOW-MEDIUM` (V-52-06 — Layer 1)
    - File contains at least one `[LOW-MEDIUM, last_verified 2026-04-27]` confidence token (Layer 1 contribution toward V-52-08)
    - File does NOT contain literal `> **Say to the user:**` (V-52-16)
    - File does NOT match regex `/\bsudo\s+apt\s+list\b/` AND does NOT match `/\bsudo\s+dpkg\s+-l\b/` AND does NOT match `/\bsudo\s+systemctl\s+--user\b/` AND does NOT match `/\bsudo\s+journalctl\s+--user\b/` (V-52-17)
    - File does NOT contain literal `snap install`, `/var/snap/intune-portal/`, or `snap container` (V-52-09)
    - File does NOT contain any of: `BYOD`, `COBO`, `COPE`, `Dedicated`, `ZTE`, `AOSP`, `COSU` (V-52-20)
    - File does NOT contain any of: `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` (V-52-22)
    - File contains at least 1 link matching regex `/\.\.\/_glossary-linux\.md#[a-z0-9-]+/` (V-52-18 — partial; full check after Task 2 adds Section 2 + Common Artifacts)
  </acceptance_criteria>
  <done>Sections 1-7 of Runbook 24 authored (frontmatter + platform gate + H1 + Context + Tool Landscape + Decision Matrix with Layer 1 confidence tokens + Method Selection + Section 1 journalctl with V-52-10 SC#1 literals); no commit; ready for Task 2 (Section 2 file-based paths + 3-layer caveat + Section 3 + footer).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append Section 2 file-based paths (Layer 2 + Layer 3 caveats) + Section 3 package-state queries + Common Artifacts Cross-Reference + Related Resources + Version History</name>
  <files>docs/l2-runbooks/24-linux-log-collection.md</files>
  <read_first>
    - docs/l2-runbooks/24-linux-log-collection.md (verify Sections 1-7 from Task 1 are present and end at the close of Section 1.4)
    - docs/l2-runbooks/18-android-log-collection.md (lines 120-128 — Section 3 opening with `> **Source confidence:**` blockquote = D-01 Layer 2 template; lines 137-149 — per-tag inline confidence-token pattern = D-01 Layer 3 template; lines 182-191 Common Artifacts table; lines 193-204 Related Resources + Version History)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-PATTERNS.md (Section 2 layered caveats; lines 137-185)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-RESEARCH.md (Research Area 2 confirms `/var/log/microsoft/intune/` is NOT documented by Microsoft Learn; Research Area 4 confirms snap detection commands)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-CONTEXT.md (D-01 Layers 1/2/3; CDI-Phase52-06 layered-defense; CDI-Phase52-01 D-01 × D-08 coupling)
  </read_first>
  <behavior>
    - Section 2 H2 opens with the Layer 2 `> **Source confidence:**` blockquote within ~10 lines of the `## Section 2:` heading
    - Section 2 contains literal `/var/log/dpkg.log` AND literal `/var/log/intune-update.log` (V-52-10)
    - Section 2 contains at least 2 inline `[LOW-MEDIUM, last_verified 2026-04-27]` tokens at command-snippet level (V-52-08 Layer 3)
    - Section 3 contains snap-vs-deb DETECTION commands (read-only) but NO forbidden snap-install strings
    - Common Artifacts Cross-Reference table cross-links to Phase 51 L1 cause anchors per DPO-01
    - Related Resources H2 lists Phase 49 glossary anchors + Phase 51 L1 runbooks + Phase 50 capability matrix anchor
    - Version History H2 has 1 row with the plan-execution date
  </behavior>
  <action>
APPEND the following sections to `docs/l2-runbooks/24-linux-log-collection.md` (immediately after Section 1.4 from Task 1).

**Section 8: Section 2 File-based paths H2 + Layer 2 caveat blockquote + per-path body (paste verbatim — CONTEXT D-01 Layers 2/3 + V-52-07 + V-52-08 + RESEARCH Area 2):**
````
## Section 2: File-based paths

File-based log paths are secondary surfaces. `/var/log/dpkg.log` and `/var/log/intune-update.log` are HIGH/MEDIUM confidence; `/var/log/microsoft/intune/` is LOW-MEDIUM confidence and carries the layered caveat below.

> **Source confidence:** `/var/log/microsoft/intune/` paths are LOW-MEDIUM confidence — Microsoft Learn does not document this path for Ubuntu `intune-portal` as of 2026-04-27. `journalctl` is the confirmed primary surface (Section 1). `/var/log/dpkg.log` and `/var/log/intune-update.log` are documented or community-confirmed. Verify `/var/log/microsoft/intune/` presence on the affected device before citing any files from this path in an escalation packet, and re-verify at `review_by` cadence.

See [dpkg](../_glossary-linux.md#dpkg) and [apt](../_glossary-linux.md#apt) for the Debian package toolchain context.

### 2.1 /var/log/dpkg.log `[HIGH, last_verified 2026-04-27]`

Standard Ubuntu dpkg event log — every package install/remove/configure event lands here. Use this to confirm the timing of an `intune-portal` install and any failed configure stages cross-referenced from L1 [Cause A: Package Install](../l1-runbooks/30-linux-enrollment-failed.md#cause-a-package-install).

```bash
# Read-only; no sudo (file is world-readable on Ubuntu)
grep intune-portal /var/log/dpkg.log
grep -E "intune-portal|microsoft-identity-broker" /var/log/dpkg.log | tail -50
```

### 2.2 /var/log/intune-update.log `[MEDIUM, last_verified 2026-04-27]`

Agent update events — community-confirmed surface (referenced in PITFALLS.md §Pitfall 3 warning signs and Phase 49 glossary). Used for L1 [Cause D: Custom Compliance Failure](../l1-runbooks/31-linux-compliance-non-compliant.md#cause-d-custom-compliance-failure) cross-reference when reading custom-compliance Bash script exit codes.

```bash
# Read-only; tail for recent updates
tail -200 /var/log/intune-update.log
grep -E "compliance|update|fail" /var/log/intune-update.log | tail -50
```

### 2.3 /var/log/microsoft/intune/ directory `[LOW-MEDIUM, last_verified 2026-04-27]`

LOW-MEDIUM confidence — path is NOT documented by Microsoft Learn for Ubuntu `intune-portal` (Source confidence blockquote above). Some community troubleshooting posts reference this path; verify presence on the affected device before relying on it in an escalation packet.

```bash
# Read-only; presence check first `[LOW-MEDIUM, last_verified 2026-04-27]`
ls -la /var/log/microsoft/intune/ 2>/dev/null
# If present, list any log files; tail individually `[LOW-MEDIUM, last_verified 2026-04-27]`
find /var/log/microsoft/intune/ -type f -name "*.log" 2>/dev/null
```

If this path does not exist on the affected device (likely on a clean Ubuntu 22.04/24.04 install with `intune-portal` 2.0.x deb), document the absence in the escalation packet and proceed with `journalctl` as primary surface. Re-verify path documentation at `review_by` cadence.
````

**Section 9: Section 3 Package-state queries H2 + body (paste verbatim — RESEARCH Area 4 lines 308-331; V-52-10 dpkg.log already in Section 2; this section adds dpkg/apt state queries; PITFALL-3 negative-space detection only):**
````
## Section 3: Package-state queries

Package-state queries confirm the delivery path (deb from `packages.microsoft.com` is GA per Phase 49 prerequisites) and detect any erroneous snap installation. All commands are read-only — no `sudo` per the read-vs-write apt distinction (DPO-Phase52-01).

See [intune-portal](../_glossary-linux.md#intune-portal) for the deb package context.

### 3.1 deb installation confirmation `[HIGH, last_verified 2026-04-27]`

```bash
# Confirm intune-portal deb is installed; expected GA output: intune-portal/jammy,now 2.0.X amd64 [installed]
apt list --installed | grep intune-portal

# Confirm dpkg state directly; expected GA: ii  intune-portal  2.0.X  amd64  ...
# (ii = installed+configured; un = not installed; iF = install-failed)
dpkg -l intune-portal
```

### 3.2 Snap negative-space detection `[HIGH, last_verified 2026-04-27]`

`intune-portal` is delivered as a deb from `packages.microsoft.com` only. There is no GA snap distribution. Use these commands to DETECT an erroneous snap installation (PITFALL-3 negative space):

```bash
# Detection-only: check whether snap is installed and whether intune-related snap is present
snap list 2>/dev/null | grep -i intune
# Expected: no output (snap not involved in GA install path)
# Unexpected: any output = user is on the deprecated preview snap path; route to Runbook 25 Trap B

# Binary path verification — GA deb places binary under /opt/microsoft/ or similar system path
which intune-portal 2>/dev/null
readlink -f $(which intune-portal 2>/dev/null) 2>/dev/null
```

### 3.3 Identity Broker version state `[HIGH, last_verified 2026-04-27]`

```bash
# Confirm Identity Broker version (relevant for Runbook 25 Trap D — v2.0.2+ re-enrollment behavior)
dpkg -l microsoft-identity-broker
dpkg -l microsoft-identity-device-broker
```

> **Note:** If `dpkg -l microsoft-identity-broker` shows version `2.0.2` or later AND L1 reports CA assignment drift, escalate via [Runbook 25 Trap D: Identity Broker v2.0.2+ Re-enrollment](25-linux-agent-investigation.md#trap-d-identity-broker).
````

**Section 10: Common Artifacts Cross-Reference H2 (paste verbatim — DPO-01 inheritance + integration-points line 254-256):**
````
## Common Artifacts Cross-Reference

Use this table to map the artifact you collected to the investigation runbook(s) that consume it. Cross-links land at Phase 51 L1 cause anchors (DPO-01 locked surfaces) for return-routes when L2 confirms an L1-symptom root cause.

| Artifact | Collection Method | Used by |
|----------|-------------------|---------|
| `journalctl -u intune-agent` output | Section 1.1 | [Runbook 25 Trap C: Service-State](25-linux-agent-investigation.md#trap-c-service-state) — service activation failures; [L1 30 Cause C: Enrollment Timeout](../l1-runbooks/30-linux-enrollment-failed.md#cause-c-enrollment-timeout) — return-route |
| `journalctl --user -u microsoft-identity-broker` output | Section 1.3 | [Runbook 25 Trap D: Identity Broker](25-linux-agent-investigation.md#trap-d-identity-broker) — re-enrollment events; [L1 30 Cause B: Sign-In Failure](../l1-runbooks/30-linux-enrollment-failed.md#cause-b-sign-in-failure) — return-route |
| `journalctl \| grep intune-portal` output | Section 1.2 | [Runbook 25 Trap B: Snap-vs-Deb](25-linux-agent-investigation.md#trap-b-delivery-path) — GUI launch errors; [Runbook 25 Trap A: Kernel Track](25-linux-agent-investigation.md#trap-a-kernel-track) — startup failures |
| `/var/log/dpkg.log` excerpt | Section 2.1 | [Runbook 25 Trap A: Kernel Track](25-linux-agent-investigation.md#trap-a-kernel-track) — package install timeline; [L1 30 Cause A: Package Install](../l1-runbooks/30-linux-enrollment-failed.md#cause-a-package-install) — return-route |
| `/var/log/intune-update.log` excerpt | Section 2.2 | [L1 31 Cause D: Custom Compliance](../l1-runbooks/31-linux-compliance-non-compliant.md#cause-d-custom-compliance-failure) — Bash exit-code reading |
| `dpkg -l intune-portal` / `apt list --installed` output | Section 3.1 | [Runbook 25 Trap A: Kernel Track](25-linux-agent-investigation.md#trap-a-kernel-track), [Runbook 25 Trap B: Snap-vs-Deb](25-linux-agent-investigation.md#trap-b-delivery-path) |
| `snap list \| grep intune` output | Section 3.2 | [Runbook 25 Trap B: Snap-vs-Deb](25-linux-agent-investigation.md#trap-b-delivery-path) — primary detection signal |
| `dpkg -l microsoft-identity-broker` output | Section 3.3 | [Runbook 25 Trap D: Identity Broker](25-linux-agent-investigation.md#trap-d-identity-broker) — version threshold check |
````

**Section 11: Related Resources + Version History H2 (paste verbatim):**
```
## Related Resources

- [L2 Runbook Index — Linux L2 Runbooks](00-index.md#linux-l2-runbooks) — Linux L2 runbook set
- [Linux Agent Investigation (Runbook 25)](25-linux-agent-investigation.md) — Trap-by-trap investigation; consumes log surfaces collected here
- [Linux Provisioning Glossary](../_glossary-linux.md) — Canonical Linux Intune terminology (`journalctl`, `systemd`, `dpkg`, `apt`, `intune-portal`, `microsoft-identity-broker`)
- [Linux L1 Triage Decision Tree](../decision-trees/09-linux-triage.md) — Phase 51 L1 routing surface
- [Linux Capability Matrix — Monitoring](../reference/linux-capability-matrix.md#monitoring) — Architectural detail for Linux monitoring surface narrowness
- [L1 30: Linux Enrollment Failed](../l1-runbooks/30-linux-enrollment-failed.md) — L1 escalation source
- [L1 31: Linux Compliance Non-Compliant](../l1-runbooks/31-linux-compliance-non-compliant.md) — L1 escalation source (custom-compliance Bash)
- [L1 33: Linux Agent Service Failure](../l1-runbooks/33-linux-agent-service-failure.md) — L1 escalation source (primary route to Runbook 25 Trap C)

## Version History

| Date | Change | Author |
|------|--------|--------|
| 2026-04-27 | Initial version (Phase 52 — Linux L2 log collection guide; 3 method sections; 3-layer LOW-MEDIUM caveat on `/var/log/microsoft/intune/` per CONTEXT D-01 / CDI-Phase52-06) | -- |
```

DO NOT modify Sections 1-7 from Task 1.

DO NOT commit yet — atomic commit owned by 52-05.
  </action>
  <verify>
    <automated>test -f docs/l2-runbooks/24-linux-log-collection.md && grep -c "/var/log/intune-update.log" docs/l2-runbooks/24-linux-log-collection.md && grep -c "/var/log/dpkg.log" docs/l2-runbooks/24-linux-log-collection.md && grep -c "> \*\*Source confidence:\*\*" docs/l2-runbooks/24-linux-log-collection.md && grep -c "\[LOW-MEDIUM, last_verified" docs/l2-runbooks/24-linux-log-collection.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/l2-runbooks/24-linux-log-collection.md` exists and now contains H2 `## Section 2: File-based paths` AND H2 `## Section 3: Package-state queries` AND H2 `## Common Artifacts Cross-Reference` AND H2 `## Related Resources` AND H2 `## Version History`
    - File contains literal `/var/log/intune-update.log` (V-52-10)
    - File contains literal `/var/log/dpkg.log` (V-52-10)
    - File contains literal `> **Source confidence:**` blockquote (V-52-07 Layer 2)
    - The `> **Source confidence:**` blockquote AND a literal `/var/log/microsoft/intune/` reference appear within ~10 lines of each other (V-52-07 proximity check)
    - File contains literal `LOW-MEDIUM confidence` text (V-52-07)
    - File contains at least 2 occurrences of `[LOW-MEDIUM, last_verified` token at command-snippet level (V-52-08 Layer 3)
    - File contains literal `[LOW-MEDIUM, last_verified 2026-04-27]` in a Decision Matrix table cell referencing `/var/log/microsoft/intune/` (V-52-06 Layer 1)
    - File does NOT contain literal `> **Say to the user:**` (V-52-16)
    - File does NOT contain `snap install`, `/var/snap/intune-portal/`, or `snap container` (V-52-09)
    - File does NOT match `/\bsudo\s+apt\s+list\b/`, `/\bsudo\s+dpkg\s+-l\b/`, `/\bsudo\s+systemctl\s+--user\b/`, or `/\bsudo\s+journalctl\s+--user\b/` (V-52-17)
    - File contains at least 1 link to `../_glossary-linux.md#<anchor>` (V-52-18)
    - File contains literal `25-linux-agent-investigation.md` (cross-reference per Pattern Map integration-points)
    - File does NOT contain any of: BYOD, COBO, COPE, Dedicated, ZTE, AOSP, COSU (V-52-20)
    - File does NOT contain any of: TBD, TODO, FIXME, XXX, PLACEHOLDER (V-52-22)
  </acceptance_criteria>
  <done>Runbook 24 complete with all 11 logical sections; satisfies V-52-01/05/06/07/08/09/10/16/17/18/20/22 (RB24 scope); ready for 52-03 validator authoring; no commit yet (52-05 owns atomicity per CONTEXT D-13).</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-52-01 | Tampering (regression) | `/var/log/microsoft/intune/` 3-layer caveat surface | mitigate | V-52-06 (Layer 1) + V-52-07 (Layer 2) + V-52-08 (Layer 3) — each layer string-pinned by validator regex per CDI-Phase52-06 layered-defense pattern (Phase 51 V-51-09/18/19 inheritance). Drift between layers caught at commit time. |
| T-52-02 | Tampering (regression) | Snap-vs-deb framing in Section 3 | mitigate | V-52-09 negative regex enforces forbidden strings (`snap install`, `/var/snap/intune-portal/`, `snap container`) absent. PITFALL-3 inheritance from Phase 49 D-04. |
| T-52-03 | Tampering (regression) | L2-direct framing | mitigate | V-52-16 negative regex enforces `> **Say to the user:**` absent. CDI-Phase52-05 / GA-4-COUP-2 retained coupling defect. Evidence: 0/23 occurrences across existing L2 corpus. |
| T-52-04 | Tampering (regression) | Read-vs-write apt distinction at L2 | mitigate | V-52-17 enforces no sudo on read-only commands. DPO-Phase52-01 inheritance from Phase 51 D-13. |
| T-52-05 | Information disclosure | Cross-link to Phase 51 L1 cause anchors | accept | Phase 51 DPO-01 locks anchor surface; same-commit Phase 52 update required for any L1 anchor rename per CDI-Phase52-02 brittleness contract. |

No application security threats — runbook is documentation-only.
</threat_model>

<verification>
After both tasks complete:

```bash
# File existence + frontmatter check
test -f docs/l2-runbooks/24-linux-log-collection.md
grep -E "^(platform|audience|applies_to|last_verified|review_by):" docs/l2-runbooks/24-linux-log-collection.md

# SC#1 literal coverage (V-52-10)
grep -c "journalctl -u intune-agent" docs/l2-runbooks/24-linux-log-collection.md         # expect >=1
grep -c "journalctl | grep intune-portal" docs/l2-runbooks/24-linux-log-collection.md   # expect >=1
grep -c "/var/log/intune-update.log" docs/l2-runbooks/24-linux-log-collection.md         # expect >=1
grep -c "/var/log/dpkg.log" docs/l2-runbooks/24-linux-log-collection.md                  # expect >=1

# Layer 1/2/3 caveat surface (V-52-06, V-52-07, V-52-08)
grep -c "\[LOW-MEDIUM, last_verified" docs/l2-runbooks/24-linux-log-collection.md       # expect >=2 (Layer 3 floor)
grep -c "> \*\*Source confidence:\*\*" docs/l2-runbooks/24-linux-log-collection.md      # expect >=1 (Layer 2)
grep -c "LOW-MEDIUM confidence" docs/l2-runbooks/24-linux-log-collection.md             # expect >=1

# Negative regression-guards (V-52-09, V-52-16, V-52-17, V-52-20, V-52-22)
grep -E "snap install|/var/snap/intune-portal/|snap container" docs/l2-runbooks/24-linux-log-collection.md && echo "FAIL V-52-09" || echo "PASS V-52-09"
grep "> \*\*Say to the user:\*\*" docs/l2-runbooks/24-linux-log-collection.md && echo "FAIL V-52-16" || echo "PASS V-52-16"
grep -E "sudo apt list|sudo dpkg -l|sudo systemctl --user|sudo journalctl --user" docs/l2-runbooks/24-linux-log-collection.md && echo "FAIL V-52-17" || echo "PASS V-52-17"
grep -E "\bBYOD\b|\bCOBO\b|\bCOPE\b|\bDedicated\b|\bZTE\b|\bAOSP\b|\bCOSU\b" docs/l2-runbooks/24-linux-log-collection.md && echo "FAIL V-52-20" || echo "PASS V-52-20"
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/l2-runbooks/24-linux-log-collection.md && echo "FAIL V-52-22" || echo "PASS V-52-22"

# Glossary anchor consumption (V-52-18)
grep -c "../_glossary-linux.md#" docs/l2-runbooks/24-linux-log-collection.md             # expect >=1
```

If `check-phase-52.mjs` exists (52-03 authored in parallel), run:
```bash
node scripts/validation/check-phase-52.mjs --verbose
# expect V-52-01, V-52-05 (RB24), V-52-06, V-52-07, V-52-08, V-52-09, V-52-10, V-52-16 (RB24), V-52-17 (RB24), V-52-18 (RB24), V-52-20 (RB24), V-52-22 (RB24) PASS
```

DO NOT commit — atomic commit owned by 52-05 per CONTEXT D-13.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 52 Success Criteria:
- **SC#1 (Runbook 24 covers `journalctl -u intune-agent` + `journalctl | grep intune-portal` + `/var/log/intune-update.log` + `/var/log/dpkg.log` + `> LOW-MEDIUM confidence` blockquote on `/var/log/microsoft/intune/` paths):** Section 1 covers the 2 journalctl literals; Section 2 covers the 2 file-path literals; Section 2 Layer 2 blockquote satisfies the `> LOW-MEDIUM confidence` mandate (V-52-07)
- **SC#4 (frontmatter on 60-day cycle, platform: Linux, audience: L2):** Frontmatter block satisfies V-52-05 with exact dates (`last_verified: 2026-04-27`, `review_by: 2026-06-26`)

Plan complete when:
- [ ] File exists at `docs/l2-runbooks/24-linux-log-collection.md`
- [ ] All 13 acceptance criteria pass for Task 1 + 14 for Task 2 (verified by 52-03 validator V-52-01/05/06/07/08/09/10/16/17/18/20/22 once authored)
- [ ] No forbidden tokens (V-52-09/16/17/20/22)
- [ ] No commit — 52-05 owns atomicity
</success_criteria>

<output>
After completion, create `.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-01-SUMMARY.md` documenting:
- File path + line count
- Section structure (1-11 H2/section enumeration)
- Layer 1/2/3 caveat surface placement (line numbers + literal token counts)
- V-52-NN coverage achieved (V-52-01, V-52-05, V-52-06, V-52-07, V-52-08, V-52-09, V-52-10 + RB24-scope V-52-16/17/18/20/22)
- Glossary anchor links used (count + which anchors)
- Phase 51 L1 cause anchors cross-linked (Common Artifacts table)
- Note: NO COMMIT — atomic commit owned by 52-05
</output>
