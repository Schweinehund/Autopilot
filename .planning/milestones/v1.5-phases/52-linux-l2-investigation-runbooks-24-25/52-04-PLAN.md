---
phase: 52
plan: 04
type: execute
wave: B
depends_on: [52-01, 52-02]
files_modified:
  - docs/l2-runbooks/00-index.md
autonomous: true
requirements: [LIN-12]
requirements_addressed: [LIN-12]

must_haves:
  truths:
    - "docs/l2-runbooks/00-index.md retains all pre-existing content (every line that existed before this plan must be byte-identical after this plan) per Phase 42 D-03 append-only contract + ROADMAP:506 attribution"
    - "docs/l2-runbooks/00-index.md contains new H2 `## Linux L2 Runbooks` appended AFTER the last existing `## Android L2 Runbooks` H2 (currently at line 132) and AFTER the `### Android MAM-WE Investigation Advisory` block (currently around line 161-163), but BEFORE `## Related Resources` (currently at line 165) per RESEARCH Area 9 lines 480-517 + V-52-19 byte-position ordering"
    - "Linux L2 Runbooks H2 includes: a `> **Version gate:**` blockquote naming Linux scope (Ubuntu 22.04/24.04 LTS deb delivery from packages.microsoft.com); an intro paragraph naming Runbook 24 as the prerequisite log-collection guide; a `### When to Use` H3 with a 2-row Runbook | When to Use | Prerequisite table referencing Runbooks 24 and 25; a `### Linux L1 Escalation Mapping` H3 with L1->L2 routing per Phase 51 inheritance"
    - "Edits do NOT introduce TBD/TODO/FIXME/XXX/PLACEHOLDER tokens"
    - "V-52-19 will pass (literal `## Linux L2 Runbooks` H2 present; both `24-linux-log-collection.md` and `25-linux-agent-investigation.md` entries present; byte-position of Linux H2 strictly greater than byte-position of Android L2 H2)"
  artifacts:
    - path: "docs/l2-runbooks/00-index.md"
      provides: "Bilateral discoverability surface for Linux L2 Runbooks (append-only Linux H2 + 2-row table + L1 Escalation Mapping)"
      contains: ["## Linux L2 Runbooks", "[Linux Log Collection Guide](24-linux-log-collection.md)", "[Linux Agent Investigation](25-linux-agent-investigation.md)", "### When to Use", "### Linux L1 Escalation Mapping", "Ubuntu 22.04/24.04 LTS"]
  key_links:
    - from: "docs/l2-runbooks/00-index.md#linux-l2-runbooks"
      to: "docs/l2-runbooks/24-linux-log-collection.md"
      via: "When to Use table prerequisite cell"
      pattern: "[Linux Log Collection Guide](24-linux-log-collection.md)"
    - from: "docs/l2-runbooks/00-index.md#linux-l2-runbooks"
      to: "docs/l2-runbooks/25-linux-agent-investigation.md"
      via: "When to Use table runbook cell"
      pattern: "[Linux Agent Investigation](25-linux-agent-investigation.md)"
    - from: "docs/l2-runbooks/00-index.md"
      to: "docs/l1-runbooks/30-linux-enrollment-failed.md / 33-linux-agent-service-failure.md"
      via: "Linux L1 Escalation Mapping table"
      pattern: "../l1-runbooks/3"
---

<objective>
Append a `## Linux L2 Runbooks` H2 + Version-gate blockquote + intro paragraph + `### When to Use` H3 (2-row table) + `### Linux L1 Escalation Mapping` H3 to `docs/l2-runbooks/00-index.md`. The new H2 must be inserted AFTER the existing `## Android L2 Runbooks` section (currently the last platform H2; verified in CONTEXT canonical_refs line 186 + RESEARCH Area 9) and BEFORE `## Related Resources` (currently line 165). All edits append-only per Phase 42 D-03 contract + ROADMAP:506 attribution.

Purpose: Satisfies SC#3 (`l2-runbooks/00-index.md` has a Linux section appended; append-only contract). Provides bilateral discoverability for the 2 Phase 52 runbooks. Validator V-52-19 enforces byte-position ordering (Linux H2 must follow Android L2 H2) at commit time.

This plan is sequenced AFTER 52-01 + 52-02 in Wave B because the table entries reference filenames that 52-01 + 52-02 must have authored; the validator (52-03) reads the append-only assertion in V-52-19. 52-03 may run in parallel with 52-01 + 52-02 in Wave A; only this append plan is sequential.

Output: 1 modified existing file (no new files); the file retains all pre-existing content + receives Linux additions in append-only positions.

DO NOT commit — atomic commit owned by 52-05 per CONTEXT D-13 + DPO-Phase52-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-CONTEXT.md
@.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-RESEARCH.md
@.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-PATTERNS.md
@docs/l2-runbooks/00-index.md
@docs/l1-runbooks/00-index.md

<interfaces>
**`docs/l2-runbooks/00-index.md`** — append target current state (verified at planning time):

- Line 19: `## When to Use` (Windows APv1 section)
- Line 29: `## L1 Escalation Mapping` (Windows APv1 escalation)
- Line 44: `## APv2 (Autopilot Device Preparation) Runbooks`
- Line 71: `## macOS ADE Runbooks`
- Line 99: `## iOS L2 Runbooks`
- Line 132: `## Android L2 Runbooks` (LAST EXISTING PLATFORM H2 — INSERT AFTER THIS SECTION)
- Lines 132-163 (approx): Android L2 H2 + Version gate blockquote + intro paragraph + `### When to Use` table + `### Android L1 Escalation Mapping` table + `### Android MAM-WE Investigation Advisory`
- Line 165: `## Related Resources` (INSERT BEFORE THIS LINE)
- Line 178: `## Version History`

**Insertion point:** Immediately AFTER the end of the `### Android MAM-WE Investigation Advisory` block (currently around line 163-164), and immediately BEFORE `## Related Resources` (line 165). Insert blank line, then the new Linux L2 H2 block, then blank line, then the existing `## Related Resources` line continues unchanged.

V-52-19 assertions (from 52-03 validator):
1. Literal `## Linux L2 Runbooks` H2 present
2. Both `24-linux-log-collection.md` AND `25-linux-agent-investigation.md` entries present
3. `indexOf("## Linux L2 Runbooks") > indexOf("## Android L2 Runbooks")` (byte-position ordering — Linux follows Android)

Append-only contract (Phase 42 D-03 / ROADMAP:506):

- Do NOT modify any existing line in the file
- Do NOT delete any existing entries
- Insert new content immediately AFTER the listed insertion-point line; preserve all content below byte-identical
- Any line that existed before this plan must be byte-identical after this plan

**Mirror Android L2 H2 shape** (verified at lines 132-163):

- H2 header `## Android L2 Runbooks`
- `> **Version gate:**` blockquote
- Intro paragraph
- `### When to Use` H3 with 3-column table
- `### Android L1 Escalation Mapping` H3
- Optional `### MAM-WE Investigation Advisory` H3 (Linux does NOT need this — Android-specific)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify current 00-index.md state and append `## Linux L2 Runbooks` H2 + intro + tables (append-only edit)</name>
  <files>docs/l2-runbooks/00-index.md</files>
  <read_first>
    - docs/l2-runbooks/00-index.md (FULL FILE — verify current end-of-Android section line; confirm Android L2 H2 + Version gate + intro + When to Use + L1 Escalation Mapping + MAM-WE Advisory pattern at lines 132-163; identify the precise line BEFORE which `## Related Resources` appears; the Linux H2 inserts BEFORE that line)
    - docs/l1-runbooks/00-index.md (verify Phase 51 Linux L1 H2 added by Phase 51 — same pattern; confirms the Phase 42 D-03 append-only contract precedent at sibling file)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-PATTERNS.md (Section "docs/l2-runbooks/00-index.md (append-only edit)" lines 531-624 — paste-ready Linux L2 H2 + table)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-RESEARCH.md (Research Area 9 lines 480-553 — append position confirmation; alternate text for Linux section)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-CONTEXT.md (D-14 step 2 append-wave; D-13 single atomic commit; CDI-Phase52-04 — V-52-19 force atomicity)
  </read_first>
  <behavior>
    - File `docs/l2-runbooks/00-index.md` retains all pre-existing content unchanged (no lines modified or deleted)
    - File contains new H2 `## Linux L2 Runbooks` AFTER the last Android L2 section content but BEFORE `## Related Resources`
    - The new section contains: H2 + Version gate blockquote + intro paragraph + `### When to Use` H3 + 2-row table (Runbook | When to Use | Prerequisite) + `### Linux L1 Escalation Mapping` H3 + 3-row table
    - V-52-19 will pass against the modified file (when 52-03 validator is run via 52-05 pre-commit gate)
  </behavior>
  <action>
**Step 1: Read and verify current state.**

Read `docs/l2-runbooks/00-index.md` FULLY to confirm:

- The exact line number of `## Android L2 Runbooks` (per planning data: line 132)
- The exact line number of the LAST line of the Android section content (per planning data: ~line 163-164; this is typically the closing of `### Android MAM-WE Investigation Advisory`)
- The exact line number of `## Related Resources` (per planning data: line 165)

If current state differs from these line-number references, ADAPT the insertion point but preserve the rule: "Linux H2 follows the END of Android L2 section AND precedes `## Related Resources`."

**Step 2: Insert the Linux L2 H2 block.**

After the LAST line of the Android L2 section (the line ending the `### Android MAM-WE Investigation Advisory` block), and BEFORE the line `## Related Resources`, insert a blank line, then the following block VERBATIM, then a blank line:

```markdown
## Linux L2 Runbooks

> **Version gate:** The runbooks below cover Linux Intune client (`intune-portal`) on Ubuntu 22.04 LTS and 24.04 LTS, delivered as a deb package from `packages.microsoft.com`. There is no GA snap distribution.
> For Android L2 runbooks, see above. For iOS L2 runbooks, see above. For macOS ADE runbooks, see above. For Windows Autopilot runbooks, see the tables above.

The [Linux Log Collection Guide](24-linux-log-collection.md) is a **prerequisite for all Linux L2 investigation runbooks** — collect `journalctl` output, file-based log snapshots, and `dpkg`/`apt` package-state queries before beginning any investigation.

### When to Use

| Runbook | When to Use | Prerequisite |
|---------|-------------|--------------|
| [Linux Log Collection Guide](24-linux-log-collection.md) | Before starting any Linux L2 investigation -- collect `journalctl` output, `/var/log/dpkg.log` and `/var/log/intune-update.log` snapshots, and `dpkg -l intune-portal` package state | None |
| [Linux Agent Investigation](25-linux-agent-investigation.md) | `intune-agent.timer` not running after L1 remediation, HWE kernel compatibility suspected, snap vs deb delivery path confusion, or Identity Broker v2.0.2+ re-enrollment detected | [Linux Log Collection](24-linux-log-collection.md) |

### Linux L1 Escalation Mapping

| L1 Runbook Source | L2 Runbook |
|-------------------|------------|
| [L1 30: Linux Enrollment Failed](../l1-runbooks/30-linux-enrollment-failed.md) | [Linux Log Collection](24-linux-log-collection.md) (Section 3 package-state queries) + [Linux Agent Investigation](25-linux-agent-investigation.md) Trap A (kernel) / Trap B (snap-vs-deb) / Trap D (Identity Broker) based on cause |
| [L1 31: Linux Compliance Non-Compliant](../l1-runbooks/31-linux-compliance-non-compliant.md) | [Linux Log Collection](24-linux-log-collection.md) Section 2 (file-based paths) for `/var/log/intune-update.log` custom-compliance Bash exit-code reading |
| [L1 33: Linux Agent Service Failure](../l1-runbooks/33-linux-agent-service-failure.md) | [Linux Agent Investigation](25-linux-agent-investigation.md) Trap C (service-state user-scope — direct primary L2 escalation target) |
```

**Step 3: Optionally update the Version History table (CD per analog precedent).**

If the file has a `## Version History` table at the bottom, append a new row at the TOP of the table (reverse-chronological — newest first):

```markdown
| 2026-04-27 | Added Linux L2 runbook section (Runbooks 24-25 + Linux L1 Escalation Mapping) — Phase 52 append-only edit per Phase 42 D-03 | -- |
```

**Step 4: Verify append-only constraint.**

After editing, run a diff against the original file (or against git's index) to confirm:

- Every line that existed before this edit is byte-identical
- New content appears ONLY in the inserted section + (optionally) the new Version History row
- No deletions or modifications to pre-existing content

```bash
git diff -- docs/l2-runbooks/00-index.md | head -100
# Confirm only additions (lines starting with `+`); no deletions (no lines starting with `-` other than `---` diff headers)
```

DO NOT modify Android/iOS/macOS/APv1/APv2 sections (Phase 42 D-03 contract — append-only).

DO NOT introduce TBD/TODO/FIXME/XXX/PLACEHOLDER tokens.

DO NOT commit — atomic commit owned by 52-05.
  </action>
  <verify>
    <automated>grep -c "## Linux L2 Runbooks" docs/l2-runbooks/00-index.md &amp;&amp; grep -c "24-linux-log-collection.md" docs/l2-runbooks/00-index.md &amp;&amp; grep -c "25-linux-agent-investigation.md" docs/l2-runbooks/00-index.md &amp;&amp; awk '/^## Android L2 Runbooks/{a=NR} /^## Linux L2 Runbooks/{l=NR} END{print (l>a)?"PASS":"FAIL"}' docs/l2-runbooks/00-index.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/l2-runbooks/00-index.md` exists and contains literal H2 `## Linux L2 Runbooks` (V-52-19 condition 1)
    - File contains literal `[Linux Log Collection Guide](24-linux-log-collection.md)` (V-52-19 condition 2 — entry 1)
    - File contains literal `[Linux Agent Investigation](25-linux-agent-investigation.md)` (V-52-19 condition 2 — entry 2)
    - awk-based byte-position check confirms `## Linux L2 Runbooks` line number > `## Android L2 Runbooks` line number (V-52-19 condition 3)
    - File contains literal H3 `### When to Use` AND H3 `### Linux L1 Escalation Mapping` (within the new Linux L2 section)
    - File contains literal `> **Version gate:**` blockquote within the new Linux L2 section
    - File contains literal `Ubuntu 22.04` AND `24.04 LTS` references
    - File contains literal cross-link `../l1-runbooks/30-linux-enrollment-failed.md` (Linux L1 Escalation Mapping)
    - File contains literal cross-link `../l1-runbooks/33-linux-agent-service-failure.md`
    - `git diff -- docs/l2-runbooks/00-index.md` shows ONLY additions (no `- ` deletion lines outside of diff metadata)
    - File does NOT contain any of: TBD, TODO, FIXME, XXX, PLACEHOLDER (in the new Linux section)
    - File still contains all pre-existing H2s: `## When to Use` (Windows APv1), `## L1 Escalation Mapping`, `## APv2 (Autopilot Device Preparation) Runbooks`, `## macOS ADE Runbooks`, `## iOS L2 Runbooks`, `## Android L2 Runbooks`, `## Related Resources`, `## Version History` (append-only contract — pre-existing structure preserved)
  </acceptance_criteria>
  <done>00-index.md modified with append-only Linux L2 H2 + 2-row table + L1 Escalation Mapping; V-52-19 satisfied; pre-existing content byte-identical; no commit (52-05 owns atomicity per CONTEXT D-13).</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-52-16 | Tampering | Append-only contract | mitigate | V-52-19 byte-position assertion enforces Linux H2 follows Android H2; pre-existing content preservation is human-verifiable via `git diff` audit. Phase 42 D-03 + ROADMAP:506 attribution. |
| T-52-17 | Tampering | Cross-link references to in-flight runbooks 24/25 | accept | The validator V-52-19 only checks index-level entries (filename presence + byte-position); the actual existence of `24-linux-log-collection.md` and `25-linux-agent-investigation.md` files is enforced by V-52-01 + V-52-02. Atomic commit ensures all 4 files (2 content + validator + index) land together. |

No application security threats — modification is documentation-only.
</threat_model>

<verification>
After Task 1 completes:

```bash
# V-52-19 conditions
grep -c "## Linux L2 Runbooks" docs/l2-runbooks/00-index.md           # expect 1
grep -c "24-linux-log-collection.md" docs/l2-runbooks/00-index.md     # expect >=2 (table + Escalation Mapping)
grep -c "25-linux-agent-investigation.md" docs/l2-runbooks/00-index.md # expect >=2

# Byte-position ordering
awk '/^## Android L2 Runbooks/{a=NR} /^## Linux L2 Runbooks/{l=NR} END{print "Android L2 line", a, "/ Linux L2 line", l, (l>a) ? "PASS" : "FAIL"}' docs/l2-runbooks/00-index.md

# Append-only diff audit
git diff --stat -- docs/l2-runbooks/00-index.md
git diff -- docs/l2-runbooks/00-index.md | grep -E "^- " | grep -v "^--- " && echo "FAIL: deletion present" || echo "PASS: append-only"

# No placeholder tokens introduced
grep -E "\\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\\b" docs/l2-runbooks/00-index.md && echo "INFO: placeholder present (verify it is in pre-existing content, not Linux section)" || echo "PASS"
```

If 52-03 validator is authored before this plan, V-52-19 may be tested directly:

```bash
node scripts/validation/check-phase-52.mjs --verbose 2>&1 | grep "V-52-19"
# expect "V-52-19" line shows PASS (or FAIL if 24-linux-log-collection.md / 25-linux-agent-investigation.md are not yet authored, in which case re-run after 52-01 + 52-02 complete)
```

DO NOT commit — atomic commit owned by 52-05 per CONTEXT D-13.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 52 Success Criteria:

- **SC#3** (`l2-runbooks/00-index.md` has a Linux section appended; append-only contract): Linux L2 Runbooks H2 + 2-row table + L1 Escalation Mapping appended after Android L2 section per Phase 42 D-03 attribution

Plan complete when:

- [ ] `docs/l2-runbooks/00-index.md` modified with new Linux L2 section
- [ ] All 12 acceptance criteria pass
- [ ] V-52-19 byte-position ordering satisfied (verified via awk)
- [ ] `git diff` shows only additions (append-only contract honored)
- [ ] No commit — 52-05 owns atomicity
</success_criteria>

<output>
After completion, create `.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-04-SUMMARY.md` documenting:

- File path + diff stats (lines added; should be ~25-35 lines for the Linux L2 section + optional Version History row)
- Confirmation of append-only contract (no pre-existing content modified)
- V-52-19 conditions satisfied (literal H2 + 2 entries + byte-position ordering)
- Linux L1 Escalation Mapping rows (3 — for Runbooks 30, 31, 33)
- Note: NO COMMIT — atomic commit owned by 52-05
</output>
