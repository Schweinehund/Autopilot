---
phase: 56
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/drift-migration/00-overview.md
autonomous: true
requirements: [DRIFT-03]

must_haves:
  truths:
    - "docs/operations/drift-migration/00-overview.md exists with `platform: cross-platform` (D-03 + D-20) + `audience: admin` + `applies_to: all` + `last_verified: 2026-04-29` + `review_by: 2026-06-28` frontmatter (D-19 + V-56-07)"
    - "File body opens with verbatim `> **Platform applicability:**` blockquote at TOP within first 50 lines of body (post-frontmatter) routing to all 4 sibling per-platform files + tenant-migration runbook (D-14 + V-56-27)"
    - "File contains 4-platform cross-platform comparison table (Windows | macOS | iOS/iPadOS | Android columns) with ≥5 concept rows; each row covers a drift signal class so all 6 signal tokens collectively appear within table cells (D-02 + V-56-09)"
    - "File contains literal H2 `## Drift terminology` (or regex-pinned variant `^## Drift terminology(\\s+\\{#[a-z0-9-]+\\})?\\s*$`) with ≥3 cross-platform terminology tokens within ~30-line window (D-02 + V-56-10)"
    - "File body collectively contains all 6 DRIFT-03 signal literal tokens: `policy conflict` (Windows) AND `app install regression` (Windows) AND `profile revocation` (macOS) AND `jailbreak detection` (iOS) AND `OS downgrade` (iOS) AND `Play Integrity verdict` (Android) — DRIFT-03 6-signal coverage per V-56-15"
    - "File body prose (excluding cross-platform comparison-table cells and cross-link `[...](...)` text) does NOT contain anti-scope-creep tokens: `supersedence`, `Win32ContentPrepTool`, `BitLocker re-key`, `ABM token`, `MGP re-binding`, `exit 1`, `exit 0`, `Log Analytics`, `Quest On Demand Migration` (D-02 firewall + V-56-11 NEGATIVE regression-guard)"
    - "File contains routing cross-links to all 4 sibling files: `01-windows-drift-detection.md`, `02-macos-drift-detection.md`, `03-ios-android-drift-detection.md`, `04-tenant-migration-runbook.md` (each ≥2 occurrences)"
    - "File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens (V-56-32)"
    - "File does NOT contain bare `> **Platform:**` token (V-56-28 NEGATIVE regression-guard)"
    - "File body length is between 200 and 350 lines (D-01 size budget per Phase 54/55 1D Hybrid inheritance)"
  artifacts:
    - path: "docs/operations/drift-migration/00-overview.md"
      provides: "Cross-platform Compliance Drift Detection overview hub (DRIFT-03 PRIMARY) with cross-platform comparison table + ## Drift terminology concept H2 + routing cross-links to all 4 sibling files"
      contains:
        - "platform: cross-platform"
        - "audience: admin"
        - "last_verified: 2026-04-29"
        - "review_by: 2026-06-28"
        - "> **Platform applicability:**"
        - "## Drift terminology"
        - "policy conflict"
        - "app install regression"
        - "profile revocation"
        - "jailbreak detection"
        - "OS downgrade"
        - "Play Integrity verdict"
        - "01-windows-drift-detection.md"
        - "02-macos-drift-detection.md"
        - "03-ios-android-drift-detection.md"
        - "04-tenant-migration-runbook.md"
  key_links:
    - from: "docs/operations/drift-migration/00-overview.md"
      to: "docs/operations/drift-migration/01-windows-drift-detection.md"
      via: "Platform applicability blockquote + comparison-table cell + Routing H2 + Related Resources"
      pattern: "01-windows-drift-detection\\.md"
    - from: "docs/operations/drift-migration/00-overview.md"
      to: "docs/operations/drift-migration/02-macos-drift-detection.md"
      via: "Platform applicability blockquote + comparison-table cell + Routing H2 + Related Resources"
      pattern: "02-macos-drift-detection\\.md"
    - from: "docs/operations/drift-migration/00-overview.md"
      to: "docs/operations/drift-migration/03-ios-android-drift-detection.md"
      via: "Platform applicability blockquote + comparison-table cell + Routing H2 + Related Resources"
      pattern: "03-ios-android-drift-detection\\.md"
    - from: "docs/operations/drift-migration/00-overview.md"
      to: "docs/operations/drift-migration/04-tenant-migration-runbook.md"
      via: "Platform applicability blockquote + Routing H2 + Related Resources"
      pattern: "04-tenant-migration-runbook\\.md"
---

<objective>
Author `docs/operations/drift-migration/00-overview.md` — the cross-platform Compliance Drift Detection overview hub for Windows / macOS / iOS / Android. This file is the **DRIFT-03 PRIMARY surface** per CONTEXT D-01 (5-file SUMMARY-aligned suite, 1B-modified winner) + REQUIREMENTS line 179 traceability lock: cross-platform comparison table + cross-platform-only `## Drift terminology` concept H2 + routing cross-links to all 4 sibling files (3 per-platform + 1 tenant-migration runbook).

Per CONTEXT D-02 anti-duplication firewall, this file MUST NOT contain DRIFT-01/02/04/05/06/07 substantive content (those literals belong in the per-platform / runbook files). V-56-11 NEGATIVE regression-guard catches `supersedence`, `Win32ContentPrepTool`, `BitLocker re-key`, `ABM token`, `MGP re-binding`, `exit 1`, `exit 0`, `Log Analytics`, `Quest On Demand Migration` literal tokens in body prose (table-cell tokens permitted; body-prose tokens forbidden — same-pattern as Phase 55 V-55-10 + Phase 54 V-54-29 inheritance).

Purpose: Implements CONTEXT D-01 (5-file suite, 1B-modified winner; 200-350 line size budget) + D-02 (DRIFT-03 cross-platform signal coverage authored at 00-overview only — V-56-15 enforces all 6 literal tokens) + D-03 (frontmatter `platform: cross-platform` per Phase 54 D-19 + Phase 55 D-03 inheritance) + D-14 (cross-platform inline blockquote at TOP per Phase 53 D-08 + Phase 54 D-04 + Phase 55 D-13 inheritance) + D-15 (cross-platform routing surface centralization at 00-overview) + D-19 (frontmatter contract) + D-21 (pinned anchor strings — `## Drift terminology` H2 string locked into validator). Validator V-56-01/07/09/10/11/15/27/28/32 enforce structural correctness at commit time.

This file is the SOLE owner of DRIFT-03 cross-platform signal coverage per REQUIREMENTS line 179 + CONTEXT D-02 lock. Wave 1 — parallel-safe with 56-02 / 56-03 / 56-04 / 56-05 (no inter-file authoring dependency; all sibling cross-links resolve at validator runtime when full atomic commit lands per D-22).

Output: 1 new file at `docs/operations/drift-migration/00-overview.md`. Creates new directory `docs/operations/drift-migration/` (parent `docs/operations/` already exists from Phase 53; sibling `docs/operations/patch-management/` exists from Phase 54; sibling `docs/operations/app-lifecycle/` exists from Phase 55).

DO NOT commit yet — atomic commit owned by 56-07 author per CONTEXT D-22 + CDI-Phase56-05 (single atomic commit covers 5 content files + validator).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-drift-detection-tenant-migration/56-CONTEXT.md
@.planning/phases/56-drift-detection-tenant-migration/56-RESEARCH.md
@.planning/phases/56-drift-detection-tenant-migration/56-PATTERNS.md
@.planning/phases/56-drift-detection-tenant-migration/56-VALIDATION.md
@docs/operations/app-lifecycle/00-overview.md
@docs/operations/patch-management/00-overview.md

<interfaces>
**File this plan creates:** `docs/operations/drift-migration/00-overview.md`

**Sibling cross-link targets (created in same atomic commit by 56-02..05):**
- `01-windows-drift-detection.md` (Wave 1 — 56-02)
- `02-macos-drift-detection.md` (Wave 1 — 56-03)
- `03-ios-android-drift-detection.md` (Wave 1 — 56-04)
- `04-tenant-migration-runbook.md` (Wave 1 — 56-05)

**Frontmatter contract (D-19 + D-03 + D-20 — single-token `cross-platform`):**
```yaml
---
last_verified: 2026-04-29
review_by: 2026-06-28
applies_to: all
audience: admin
platform: cross-platform
---
```

**Validator V-56-NN assertions enforced against this file:**

| V-NN | Assertion |
|------|-----------|
| V-56-01 | File exists |
| V-56-07 | Frontmatter: `platform: cross-platform`, `audience:` non-empty, 60-day cycle |
| V-56-09 | 4-platform comparison table with `Windows`, `macOS`, `iOS`, `Android` literal columns (or transposed rows) |
| V-56-10 | Literal H2 `## Drift terminology` + ≥3 cross-platform terminology tokens within ~30-line window |
| V-56-11 | NEGATIVE: body prose (excluding table cells + cross-link text + code blocks) does NOT contain `supersedence`, `Win32ContentPrepTool`, `BitLocker re-key`, `ABM token`, `MGP re-binding`, `exit 1`, `exit 0`, `Log Analytics`, `Quest On Demand Migration` |
| V-56-15 | All 6 cross-platform signal tokens present in body: `policy conflict`, `app install regression`, `profile revocation`, `jailbreak detection`, `OS downgrade`, `Play Integrity verdict` |
| V-56-27 | `> **Platform applicability:**` blockquote within first 50 lines of body |
| V-56-28 | NEGATIVE: NO bare `> **Platform:**` token (corpus-wide regression-guard) |
| V-56-32 | NEGATIVE: NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/operations/drift-migration/00-overview.md (frontmatter + Platform applicability blockquote at TOP + H1 + intro + Cross-Platform Comparison table covering 6 DRIFT-03 signal tokens + Drift terminology H2 + Routing H2 + Related Resources/External References footer)</name>
  <files>docs/operations/drift-migration/00-overview.md</files>
  <read_first>
    - .planning/phases/56-drift-detection-tenant-migration/56-CONTEXT.md (D-01 5-file suite scope; D-02 DRIFT-03 sole-owner lock + 6-signal-token list; D-03 + D-20 platform: cross-platform; D-14 inline blockquote token; D-15 cross-platform routing centralization; D-19 frontmatter contract; D-21 pinned anchor strings; V-56-11 anti-scope-creep firewall enumerated tokens)
    - .planning/phases/56-drift-detection-tenant-migration/56-RESEARCH.md §5 (Cross-platform compliance drift signals matrix at lines 553-572 — verbatim per-platform mapping with REQ literals; locked tokens for V-56-15 enforcement)
    - .planning/phases/56-drift-detection-tenant-migration/56-PATTERNS.md (00-overview pattern assignment at lines 24-129; cross-platform comparison table shape at lines 84-93; H2 ordering convention at lines 70-82; NEGATIVE pattern at lines 114-128)
    - .planning/phases/56-drift-detection-tenant-migration/56-VALIDATION.md (Per-Task Verification Map rows 56-01-01 through 56-01-05 — 5 grep-verifiable acceptance criteria for this file)
    - docs/operations/app-lifecycle/00-overview.md (Phase 55 1D Hybrid implementation reference — most-direct precedent; Platform applicability blockquote at lines 9-21; Cross-Platform Comparison table; concept H2; Related Resources/External References footer)
    - docs/operations/patch-management/00-overview.md (Phase 54 1D Hybrid implementation reference — secondary; same shape as Phase 55)
  </read_first>
  <behavior>
    - File `docs/operations/drift-migration/00-overview.md` exists
    - Frontmatter (D-19 + D-03 + D-20): `last_verified: 2026-04-29`, `review_by: 2026-06-28`, `applies_to: all`, `audience: admin`, `platform: cross-platform`
    - Platform applicability blockquote at TOP (immediately after frontmatter `---` closing fence, before H1) routing to all 4 sibling files + tenant-migration runbook using verbatim `> **Platform applicability:**` token (D-14 + V-56-27)
    - H1 `# Compliance Drift Detection Overview: Cross-Platform Hub` followed by intro paragraph cross-linking to all 4 sibling files
    - Cross-Platform Comparison H2 + 4-platform table (Windows | macOS | iOS/iPadOS | Android columns) with ≥5 concept rows; ALL 6 DRIFT-03 signal tokens (`policy conflict`, `app install regression`, `profile revocation`, `jailbreak detection`, `OS downgrade`, `Play Integrity verdict`) collectively present in body (V-56-15 enforces — table cells satisfy because V-56-15 scans full body NOT body-prose-only)
    - `## Drift terminology` H2 (LOCKED literal per V-56-10) with cross-platform terminology disambiguating ≥3 cross-platform terminology tokens within ~30-line window — candidate tokens: `compliance`, `policy conflict`, `attestation`, `revocation`, `verdict`, `regression`, `downgrade`, `jailbreak`, `drift`, `signal`. Pick a cross-platform-disambiguation framing (configuration drift vs registration drift; signal vs symptom; compliance state vs attestation verdict)
    - `## Routing to Per-Platform Guides` H2 with cross-links to all 4 sibling files
    - Related Resources + External References footer with cross-links to all 4 sibling files + canonical Microsoft Learn URLs + ops/00-index.md cross-reference-only line
    - File body length between 200-350 lines (D-01 size budget per Phase 54/55 1D Hybrid inheritance)
    - NO bare `> **Platform:**` token (FORBIDDEN per V-56-28 corpus-wide regression-guard)
    - NO `supersedence`, `Win32ContentPrepTool`, `BitLocker re-key`, `ABM token`, `MGP re-binding`, `exit 1`, `exit 0`, `Log Analytics`, `Quest On Demand Migration` substantive prose in body (V-56-11 firewall — these may appear ONLY in comparison-table cells with cross-link routing, NOT in body prose)
    - NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens (V-56-32)
  </behavior>
  <action>
Create the file `docs/operations/drift-migration/00-overview.md` with the following content sections IN ORDER. The directory `docs/operations/drift-migration/` does not yet exist — create it implicitly via the file write (parent `docs/operations/` already exists from Phase 53).

**Section 1 — Frontmatter (D-19 + D-03 + D-20; copy verbatim per PATTERNS.md lines 31-39):**

```yaml
---
last_verified: 2026-04-29
review_by: 2026-06-28
applies_to: all
audience: admin
platform: cross-platform
---
```

**Section 2 — Platform applicability blockquote at TOP (D-14 + V-56-27; pointer-routing to all 4 sibling files + tenant-migration runbook; mirrors Phase 55 docs/operations/app-lifecycle/00-overview.md:9-21 token discipline; per PATTERNS.md lines 45-59):**

```markdown
> **Platform applicability:** This guide is the cross-platform compliance drift detection
> overview hub for Windows, macOS, iOS/iPadOS, and Android — covering drift terminology
> (configuration drift vs registration drift; signal vs symptom), the cross-platform comparison
> table, and per-platform routing.
> **Windows:** See [Windows Drift Detection](01-windows-drift-detection.md) for Intune
> Remediations + canonical script-authoring pattern.
> **macOS:** See [macOS Drift Detection](02-macos-drift-detection.md) for profile-revocation
> + DDM compliance signals.
> **iOS/Android:** See [iOS + Android Drift Detection](03-ios-android-drift-detection.md) for
> jailbreak / OS downgrade / Play Integrity verdict signals.
> **Tenant migration:** See [Tenant Migration Runbook](04-tenant-migration-runbook.md) for
> cross-platform tenant-to-tenant migration with MEDIUM-confidence framing.
```

**Section 3 — H1 + intro paragraph:**

```markdown
# Compliance Drift Detection Overview: Cross-Platform Hub

This guide is the cross-platform overview for compliance drift detection across Windows, macOS,
iOS/iPadOS, and Android. It covers drift terminology (configuration drift vs registration drift;
signal vs symptom; compliance state vs attestation verdict), the cross-platform comparison
table, and routing to per-platform guides plus the tenant-to-tenant migration runbook.

For Windows-specific drift detection (Intune Remediations + canonical script-authoring pattern),
see [Windows Drift Detection](01-windows-drift-detection.md). For macOS drift signals, see
[macOS Drift Detection](02-macos-drift-detection.md). For iOS jailbreak / OS downgrade and
Android Play Integrity verdict-change drift signals, see
[iOS + Android Drift Detection](03-ios-android-drift-detection.md). For cross-platform
tenant-to-tenant migration with cross-platform encryption-drift coverage, see
[Tenant Migration Runbook](04-tenant-migration-runbook.md).

This overview is intentionally a concept hub — it does not contain platform-specific
configuration steps, screenshots, or detailed runbooks. Each per-platform guide owns its own
configuration surface, and the tenant-migration runbook owns the migration procedure detail.
Read this hub first to interpret cross-platform drift signals; then follow the per-platform
guide for day-2 operations or the tenant-migration runbook for cross-tenant migration.
```

**Section 4 — Cross-Platform Comparison H2 + 4-platform table (V-56-09; ≥5 concept rows × 4 platform columns; ALL 6 V-56-15 signal tokens MUST collectively appear within table body — RESEARCH.md §5 lines 553-572 provides verbatim mapping):**

```markdown
## Cross-Platform Comparison

The following table compares compliance drift signals and detection surfaces across all four
platforms. Use this as a routing reference; per-platform detection mechanics live in the four
sibling guides. Substantive treatment of each row's drift remediation steps belongs exclusively
in the per-platform guides linked below — this overview keeps the comparison high-level so
admins can route to the correct guide quickly.

The columns are ordered Windows, macOS, iOS/iPadOS, Android to match the navigation order used
by the per-platform sibling files. Rows are signal-keyed: each row names a drift signal and
shows where it surfaces on each platform (with `—` indicating that signal is not applicable on
that platform).

| Drift signal class             | Windows                            | macOS                          | iOS/iPadOS                            | Android                                  |
|--------------------------------|------------------------------------|--------------------------------|---------------------------------------|------------------------------------------|
| Configuration drift            | policy conflict (two policies target same setting) | profile revocation (DDM declaration expired / user-removed) | profile revocation (configuration profile removed) | per-policy compliance state               |
| App-state drift                | app install regression (uninstalled / reverted) | app delivery regression       | app delivery regression               | app delivery regression                   |
| Integrity / attestation drift  | Endpoint Analytics integrity signals | DDM declaration verdicts      | jailbreak detection (compliance signal) | Play Integrity verdict (MEETS_STRONG_INTEGRITY → lower) |
| OS-state drift                 | feature-update rollback signal     | OS downgrade signal           | OS downgrade (rare; restore-driven)  | OS downgrade signal                       |
| Encryption drift               | BitLocker key escrow drift         | FileVault escrow drift         | device-level encryption (compliance only) | dm-crypt per-OEM variance                 |
| Detection surface              | Intune Remediations + Endpoint Analytics + Graph reports | Intune compliance reports + DDM declaration status | Intune compliance policy + Software Update policy | Intune compliance policy + Play Integrity attestation |
```

The required V-56-15 literal tokens MUST collectively appear in the table body: `policy conflict` (Windows row), `app install regression` (Windows row), `profile revocation` (macOS row), `jailbreak detection` (iOS row), `OS downgrade` (iOS / OS-state row), `Play Integrity verdict` (Android row).

**Section 5 — `## Drift terminology` H2 (LOCKED literal per V-56-10; ≥3 cross-platform terminology tokens within ~30-line window):**

```markdown
## Drift terminology

Compliance drift terminology spans three orthogonal axes that apply across all four platforms.
Conflating these axes is a common source of mis-diagnosis. This section disambiguates the three
axes and the cross-platform vocabulary used in the per-platform guides:

- **Configuration drift vs registration drift** — Configuration drift is the runtime state where
  a managed device has diverged from its assigned policy/profile (e.g., a user removed a
  configuration profile, a setting was overridden locally, or two policies target the same
  setting with different values causing a `policy conflict`). Registration drift is the
  enrollment-side state where a device has diverged from its expected enrollment posture (e.g.,
  Autopilot profile assignment is `Not assigned` when expected `Assigned`). Phase 56 covers
  configuration drift; for Autopilot **registration** drift, see the v1.2 SSoT
  [Registration Drift Detection](../../reference/drift-detection.md).
- **Signal vs symptom** — A drift signal is a measurable platform-level indicator (an
  attestation `verdict`, a compliance `state`, a remediation `regression`); a symptom is the
  user-visible consequence (an app missing, a device blocked from CA, a passcode prompt).
  Per-platform guides describe how to read signals; the cross-platform comparison table above
  maps each signal class to its native-platform surface.
- **Compliance state vs attestation verdict** — Compliance state is the Intune-evaluated boolean
  per device per policy (`Compliant` / `Non-compliant`); attestation verdict is a deeper
  cryptographic signal from the platform attestation surface (e.g., Play Integrity
  `MEETS_STRONG_INTEGRITY`, `MEETS_DEVICE_INTEGRITY`, `MEETS_BASIC_INTEGRITY`; iOS
  `jailbreak detection`). Compliance state may aggregate multiple attestation verdicts plus
  policy-level checks. The per-platform guides describe verdict thresholds and signal-to-state
  mapping.

For Windows-specific drift detection mechanics (Intune Remediations detect+remediate
script pairs), see [Windows Drift Detection](01-windows-drift-detection.md). For macOS, see
[macOS Drift Detection](02-macos-drift-detection.md). For iOS jailbreak / OS downgrade and
Android Play Integrity verdict-change, see
[iOS + Android Drift Detection](03-ios-android-drift-detection.md). For cross-platform tenant
migration including a cross-platform encryption-drift fold section, see
[Tenant Migration Runbook](04-tenant-migration-runbook.md).
```

The H2 string `## Drift terminology` is pinned by validator V-56-10. The token list (`compliance`, `policy conflict`, `attestation`, `revocation`, `verdict`, `regression`, `downgrade`, `jailbreak`, `drift`, `signal`) collectively provides ≥3 cross-platform terminology tokens within the H2's ~30-line window. The H2 anchor topic is `drift terminology` (cross-platform); not Win32-only / Android-only / iOS-only single-platform topics.

**Section 6 — Routing H2 (companion to comparison table; explicit per-platform routing block):**

```markdown
## Routing to Per-Platform Guides

Choose the per-platform guide based on the platform whose drift signals you are investigating:

- **Windows** — Intune Remediations (Proactive Remediations) detect+remediate script pairs +
  canonical script-authoring pattern + Log Analytics surface for full output: see
  [Windows Drift Detection](01-windows-drift-detection.md).
- **macOS** — Profile revocation when MDM trust breaks; DDM declaration verdicts; Intune
  Settings Catalog drift detection: see
  [macOS Drift Detection](02-macos-drift-detection.md).
- **iOS/iPadOS + Android** — iOS jailbreak detection compliance signals + iOS OS downgrade
  signal; Android Play Integrity verdict-change drift signals (cross-link to Phase 54 Play
  Integrity SSoT for substance): see
  [iOS + Android Drift Detection](03-ios-android-drift-detection.md).
- **Tenant migration (cross-platform)** — Windows BitLocker re-key options + macOS/iOS ABM
  token re-issue + Android MGP re-binding + cross-platform encryption-drift section folded
  inside: see [Tenant Migration Runbook](04-tenant-migration-runbook.md). MEDIUM confidence
  surface.
```

**Section 7 — Related Resources + External References footer:**

```markdown
## Related Resources

- [Windows Drift Detection](01-windows-drift-detection.md) — Intune Remediations + canonical
  script-authoring pattern (DRIFT-01 + DRIFT-02 fold)
- [macOS Drift Detection](02-macos-drift-detection.md) — Profile-revocation + DDM compliance
  signals
- [iOS + Android Drift Detection](03-ios-android-drift-detection.md) — iOS jailbreak / OS
  downgrade + Android Play Integrity verdict-change
- [Tenant Migration Runbook](04-tenant-migration-runbook.md) — Cross-platform tenant-to-tenant
  migration (DRIFT-04 + DRIFT-05 + DRIFT-06 + DRIFT-07 fold; MEDIUM confidence)
- [Registration Drift Detection](../../reference/drift-detection.md) — v1.2 SSoT — Autopilot
  registration drift (scope-distinct: registration vs configuration)
- [Patch & Update Management Overview](../patch-management/00-overview.md) — Sibling ops-domain
  (Phase 54)
- [App Lifecycle Automation Overview](../app-lifecycle/00-overview.md) — Sibling ops-domain
  (Phase 55)
- [Co-Management Overview](../co-management/00-overview.md) — Sibling ops-domain (Phase 53)

## External References

- [Use Remediations to detect and fix support issues (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/device-management/tools/deploy-remediations) — DRIFT-01 portal path source
- [Microsoft Graph deviceManagement reports (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/intune-service/fundamentals/reports-export-graph-available-reports) — Graph drift report names
- [iOS/iPadOS device compliance settings (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/intune-service/protect/compliance-policy-create-ios) — iOS jailbreak detection compliance surface
- [Operations Documentation Index](../00-index.md) — Cross-reference only; Phase 56 does NOT amend the operations index per the Phase 59 ROADMAP entry
```

After authoring, verify the following grep checks pass against the new file:

```bash
grep -c "platform: cross-platform" docs/operations/drift-migration/00-overview.md             # expect 1
grep -c "^> \*\*Platform applicability:\*\*" docs/operations/drift-migration/00-overview.md   # expect 1
grep -c "^## Drift terminology" docs/operations/drift-migration/00-overview.md                 # expect 1
grep -c "policy conflict" docs/operations/drift-migration/00-overview.md                       # expect >=1
grep -c "app install regression" docs/operations/drift-migration/00-overview.md                # expect >=1
grep -c "profile revocation" docs/operations/drift-migration/00-overview.md                    # expect >=1
grep -c "jailbreak detection" docs/operations/drift-migration/00-overview.md                   # expect >=1
grep -c "OS downgrade" docs/operations/drift-migration/00-overview.md                          # expect >=1
grep -c "Play Integrity verdict" docs/operations/drift-migration/00-overview.md                # expect >=1
grep -c "01-windows-drift-detection.md" docs/operations/drift-migration/00-overview.md         # expect >=2
grep -c "02-macos-drift-detection.md" docs/operations/drift-migration/00-overview.md           # expect >=2
grep -c "03-ios-android-drift-detection.md" docs/operations/drift-migration/00-overview.md     # expect >=2
grep -c "04-tenant-migration-runbook.md" docs/operations/drift-migration/00-overview.md        # expect >=2
grep -c "^> \*\*Platform:\*\*" docs/operations/drift-migration/00-overview.md                  # MUST be 0 (V-56-28)
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/drift-migration/00-overview.md  # MUST be empty (V-56-32)
wc -l docs/operations/drift-migration/00-overview.md  # expect 200-350 lines
```

**V-56-11 Anti-scope-creep self-check (CRITICAL — D-02 + per CONTEXT line 99):** After authoring, manually scan the body prose (excluding the comparison-table cells in Section 4 and any cross-link `[...](...)` text) for the tokens `supersedence`, `Win32ContentPrepTool`, `BitLocker re-key`, `ABM token`, `MGP re-binding`, `exit 1`, `exit 0`, `Log Analytics`, `Quest On Demand Migration`. If any of these tokens appears in body prose (sentences/paragraphs not inside table rows or markdown links), MOVE that prose to the appropriate per-platform file (per-platform-file territory). V-56-11 NEGATIVE regression-guard will FAIL the validator if these tokens appear in body prose.

The comparison-table cells may use platform-specific tokens at the cell level; the validator strips lines starting with `|` before scanning. Cross-link `[...](...)` text is also stripped.

DO NOT commit yet — atomic commit owned by 56-07 author per CONTEXT D-22 + CDI-Phase56-05.
  </action>
  <verify>
    <automated>test -f docs/operations/drift-migration/00-overview.md &amp;&amp; grep -q "^## Drift terminology" docs/operations/drift-migration/00-overview.md &amp;&amp; grep -q "platform: cross-platform" docs/operations/drift-migration/00-overview.md &amp;&amp; grep -q "^> \*\*Platform applicability:\*\*" docs/operations/drift-migration/00-overview.md &amp;&amp; grep -q "policy conflict" docs/operations/drift-migration/00-overview.md &amp;&amp; grep -q "app install regression" docs/operations/drift-migration/00-overview.md &amp;&amp; grep -q "profile revocation" docs/operations/drift-migration/00-overview.md &amp;&amp; grep -q "jailbreak detection" docs/operations/drift-migration/00-overview.md &amp;&amp; grep -q "OS downgrade" docs/operations/drift-migration/00-overview.md &amp;&amp; grep -q "Play Integrity verdict" docs/operations/drift-migration/00-overview.md &amp;&amp; ! grep -q "^> \*\*Platform:\*\*$" docs/operations/drift-migration/00-overview.md &amp;&amp; ! grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/drift-migration/00-overview.md &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/operations/drift-migration/00-overview.md` exists
    - File contains frontmatter with `platform: cross-platform`, `audience: admin`, `last_verified: 2026-04-29`, `review_by: 2026-06-28`, `applies_to: all`
    - File contains literal `> **Platform applicability:**` blockquote within first 50 lines of body (post-frontmatter) (V-56-27)
    - File contains literal H2 `## Drift terminology` (case-sensitive exact match) (V-56-10)
    - Within ~30 lines after `## Drift terminology` H2: ≥3 cross-platform terminology tokens present (`compliance`, `policy conflict`, `attestation`, `revocation`, `verdict`, `regression`, `downgrade`, `jailbreak`, `drift`, `signal` — pick at least 3)
    - File contains 4-platform comparison table with all 4 platform names (`Windows`, `macOS`, `iOS`, `Android`) as columns or rows; minimum 5 concept rows (V-56-09)
    - File body contains all 6 DRIFT-03 signal literal tokens (V-56-15): `policy conflict`, `app install regression`, `profile revocation`, `jailbreak detection`, `OS downgrade`, `Play Integrity verdict`
    - File contains cross-link to each of: `01-windows-drift-detection.md`, `02-macos-drift-detection.md`, `03-ios-android-drift-detection.md`, `04-tenant-migration-runbook.md` (≥2 occurrences each)
    - File body prose (excluding comparison-table cells in Section 4 and cross-link `[...](...)` text) does NOT contain `supersedence`, `Win32ContentPrepTool`, `BitLocker re-key`, `ABM token`, `MGP re-binding`, `exit 1`, `exit 0`, `Log Analytics`, `Quest On Demand Migration` (V-56-11 firewall)
    - File does NOT contain bare `> **Platform:**` token (V-56-28)
    - File does NOT contain any of: `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` (V-56-32)
    - File body length is between 200 and 350 lines (D-01 size budget; `wc -l` reports ≥200 and ≤350)
    - `grep -c "platform: cross-platform"` returns 1
    - `grep -c "^> \*\*Platform applicability:\*\*"` returns 1
    - `grep -c "^## Drift terminology"` returns 1
  </acceptance_criteria>
  <done>File ships with cross-platform comparison table covering 6 DRIFT-03 signal tokens, ## Drift terminology H2 with ≥3 cross-platform terminology tokens, routing cross-links to all 4 sibling files, frontmatter platform: cross-platform + 60-day cycle, Platform applicability blockquote at TOP, and zero V-56-11 firewall violations / zero V-56-28 bare-Platform tokens / zero V-56-32 TBD scan hits. File body 200-350 lines per D-01 size budget. NO commit yet — 56-07 owns atomic commit.</done>
</task>

</tasks>

<verification>
After Task 1 completes, verify:

```bash
# File exists with correct content shape
test -f docs/operations/drift-migration/00-overview.md
grep -c "^## Drift terminology" docs/operations/drift-migration/00-overview.md  # expect 1
grep -c "platform: cross-platform" docs/operations/drift-migration/00-overview.md  # expect 1
grep -c "^> \*\*Platform applicability:\*\*" docs/operations/drift-migration/00-overview.md  # expect 1

# 6 DRIFT-03 signal tokens (V-56-15)
for tok in "policy conflict" "app install regression" "profile revocation" "jailbreak detection" "OS downgrade" "Play Integrity verdict"; do
  grep -q "$tok" docs/operations/drift-migration/00-overview.md && echo "OK: $tok" || echo "MISSING: $tok"
done

# Sibling cross-links
for f in 01-windows-drift-detection.md 02-macos-drift-detection.md 03-ios-android-drift-detection.md 04-tenant-migration-runbook.md; do
  grep -c "$f" docs/operations/drift-migration/00-overview.md
done

# NEGATIVE checks
! grep -q "^> \*\*Platform:\*\*" docs/operations/drift-migration/00-overview.md  # V-56-28
! grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/drift-migration/00-overview.md  # V-56-32

# Size budget
LINES=$(wc -l < docs/operations/drift-migration/00-overview.md); test "$LINES" -ge 200 && test "$LINES" -le 350
```

Validator V-56-01 + V-56-07 + V-56-09 + V-56-10 + V-56-11 + V-56-15 + V-56-27 + V-56-28 + V-56-32 will run during 56-07 close gate against this file.
</verification>

<success_criteria>
- [x] DRIFT-03 cross-platform compliance drift signal coverage authored at 00-overview only (per REQUIREMENTS line 179 lock + V-56-15 enforces 6 literal tokens)
- [x] All 6 cross-platform signal tokens (`policy conflict`, `app install regression`, `profile revocation`, `jailbreak detection`, `OS downgrade`, `Play Integrity verdict`) appear in file body
- [x] ## Drift terminology H2 present with ≥3 cross-platform terminology tokens
- [x] Cross-platform comparison table (4 platforms × ≥5 concept rows) present
- [x] Routing cross-links to all 4 sibling files present (≥2 occurrences each)
- [x] Frontmatter `platform: cross-platform` + 60-day cycle + audience: admin
- [x] Platform applicability blockquote at TOP within first 50 body lines
- [x] V-56-11 anti-scope-creep firewall holds (no per-platform substance literals in body prose)
- [x] V-56-28 NEGATIVE: no bare `> **Platform:**` token
- [x] V-56-32 NEGATIVE: no TBD/TODO/FIXME/XXX/PLACEHOLDER tokens
- [x] File body length 200-350 lines per D-01 size budget
</success_criteria>

<output>
After completion, create `.planning/phases/56-drift-detection-tenant-migration/56-01-SUMMARY.md` (after atomic commit per Phase 49/50/51/52/53/54/55 close pattern; not yet — VERIFICATION in 56-07).
</output>