---
phase: 50
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-linux/03-compliance-policy.md
  - docs/admin-setup-linux/04-app-delivery.md
  - docs/admin-setup-linux/05-conditional-access.md
autonomous: true
requirements: [LIN-04]
requirements_addressed: [LIN-04]

must_haves:
  truths:
    - "Admin reading 03-compliance-policy.md finds an architectural callout in the opening section stating 'Require device to be marked as compliant' is NOT available on Linux (PITFALL-2)"
    - "Admin reading 04-app-delivery.md finds the PITFALL-1 scope callout stating Linux app delivery is script-based only (no Win32/MSIX/.pkg analog)"
    - "Admin reading 05-conditional-access.md sees web-app CA via Edge ONLY framing — no device-level CA H2 section exists"
  artifacts:
    - path: "docs/admin-setup-linux/03-compliance-policy.md"
      provides: "4 settings-catalog compliance categories + PITFALL-2 architectural callout"
      contains: ["platform: Linux", "audience: admin", "Require device to be marked as compliant", "not available", "Allowed Distributions", "Custom Compliance"]
    - path: "docs/admin-setup-linux/04-app-delivery.md"
      provides: "Concept-level Linux app delivery + PITFALL-1 scope callout"
      contains: ["platform: Linux", "audience: admin", "script-based only"]
    - path: "docs/admin-setup-linux/05-conditional-access.md"
      provides: "Web-app CA via Edge framing + PITFALL-2 inheritance"
      contains: ["platform: Linux", "audience: admin", "Edge", "web-app CA"]
  key_links:
    - from: "docs/admin-setup-linux/03-compliance-policy.md"
      to: "docs/admin-setup-linux/05-conditional-access.md"
      via: "in-text reference within PITFALL-2 callout"
      pattern: "05-conditional-access.md"
    - from: "docs/admin-setup-linux/05-conditional-access.md"
      to: "docs/reference/linux-capability-matrix.md#conditional-access"
      via: "See Also link (CDI-Phase50-04 anchor stability)"
      pattern: "linux-capability-matrix.md#conditional-access"
---

<objective>
Author 3 of 6 admin-setup-linux files: 03-compliance-policy (LIN-04), 04-app-delivery, and 05-conditional-access. These three files carry the architectural callouts that anchor PITFALL-1 (script-only app delivery) and PITFALL-2 (web-app CA only).

Purpose: The compliance-policy file is the LIN-04 deliverable with the PITFALL-2 architectural callout that prevents admins from misconfiguring CA grants. App-delivery is concept-only (Bash deep-dive deferred to v1.5.1 LIN-DEFER-01). Conditional-access carries forward PITFALL-2 with an explicit absence-of-device-CA framing.
Output: 3 admin-setup-linux markdown files with locked PITFALL-1/2 callouts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-linux-admin-setup-capability-matrix/50-CONTEXT.md
@.planning/phases/50-linux-admin-setup-capability-matrix/50-PATTERNS.md
@.planning/phases/50-linux-admin-setup-capability-matrix/50-VALIDATION.md
@docs/admin-setup-macos/05-compliance-policy.md
@docs/admin-setup-android/04-byod-work-profile.md
@docs/admin-setup-macos/04-app-deployment.md
@docs/reference/ca-enrollment-timing.md
@docs/linux-lifecycle/00-enrollment-overview.md

<interfaces>
<!-- PITFALL-2 anchor: Phase 49 V-49-08 contract sets the literal `Not supported — web-app CA only`. -->
<!-- The CA H2 anchor in linux-capability-matrix.md (#conditional-access) is owned by plan 04 — but its file path is referenced here. -->

Phase 49 anchors usable in cross-references:
- `docs/linux-lifecycle/00-enrollment-overview.md#supported-management-surface`
- `docs/_glossary-linux.md#web-app-ca`

Plan 04 (Wave B) creates `docs/reference/linux-capability-matrix.md` with `## Conditional Access` H2 (D-02) — references resolve at atomic D-18 commit, not before.

Frontmatter contract (D-03):
```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: enrollment        # 03 + 04
applies_to: conditional-access  # 05
audience: admin
platform: Linux
---
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/admin-setup-linux/03-compliance-policy.md (LIN-04 + PITFALL-2)</name>
  <files>docs/admin-setup-linux/03-compliance-policy.md</files>
  <read_first>
    - docs/admin-setup-macos/05-compliance-policy.md (PRIMARY analog — H2 structure: Compliance vs Configuration distinction table; Steps; Verification; Configuration-Caused Failures; lines 19-142)
    - docs/admin-setup-android/04-byod-work-profile.md (secondary analog — PITFALL-2 callout shape at lines 18-19)
    - .planning/phases/50-linux-admin-setup-capability-matrix/50-PATTERNS.md (lines 200-231 — 03-compliance-policy.md pattern map)
    - .planning/phases/50-linux-admin-setup-capability-matrix/50-CONTEXT.md (D-24 PITFALL-2 callout assertion: literal `Require device to be marked as compliant` AND ("not available" OR "not supported"))
    - .planning/research/PITFALLS.md §Pitfall 2 (lines 33-56) — CA architectural callout mandate
  </read_first>
  <behavior>
    - File exists with C10-compliant frontmatter
    - Contains MANDATORY PITFALL-2 architectural callout in the OPENING section with literal `Require device to be marked as compliant` and qualifier "not available" or "not supported"
    - Documents 4 settings-catalog categories: Allowed Distributions / Custom Compliance / Device Encryption / Password Policy
    - Cross-links to 05-conditional-access.md and ../reference/linux-capability-matrix.md#conditional-access
  </behavior>
  <action>
Create `docs/admin-setup-linux/03-compliance-policy.md` mirroring `docs/admin-setup-macos/05-compliance-policy.md` structure.

**Frontmatter (D-03):**
```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: enrollment
audience: admin
platform: Linux
---
```

**H1:** `# Linux Compliance Policy — Admin Configuration`

**Platform-gate blockquote:**
```
> **Platform gate:** This guide covers configuration of Intune compliance policies for Linux devices (Ubuntu 22.04/24.04 LTS) across the 4 supported settings-catalog categories.
> For Linux provisioning terminology, see the [Linux Provisioning Glossary](../_glossary-linux.md).
> For the locked Linux management surface, see [Linux Enrollment Overview](../linux-lifecycle/00-enrollment-overview.md#supported-management-surface).
```

**MANDATORY PITFALL-2 architectural callout (opening section — IMMEDIATELY after Platform gate; before any H2):**

EXACT format (must contain literal strings `Require device to be marked as compliant` and `not available`):
```
> ⚠️ **Architecture callout — compliance reporting is NOT a Conditional Access grant on Linux:** A Linux device that reports `compliant` via an Intune compliance policy does NOT receive Conditional-Access-level access grants. The CA grant control `Require device to be marked as compliant` is **not available** on Linux — the only CA enforcement path on Linux is web-app CA via Microsoft Edge for Linux 102.x+. Compliance policy on Linux is **detect-only** (it produces a compliance verdict admins can monitor and use for reporting), not enforce-grant.
>
> See [Conditional Access](05-conditional-access.md) and [Linux Capability Matrix — Conditional Access](../reference/linux-capability-matrix.md#conditional-access) for the architectural detail.
```
Validator (V-50-21) asserts: file contains `Require device to be marked as compliant` AND regex `/not available|not supported/i` matches.

**H2: `## Compliance vs. Configuration: Critical Distinction`**

Mirroring macOS analog lines 19-28:
```
| Action | Compliance Policy | Configuration Profile |
|--------|------------------|-----------------------|
| Verify a setting | Yes (detect-only on Linux) | n/a — Linux has no MDM configuration profile concept in Intune |
| Enforce a setting | n/a | n/a |

**Note for Linux admins:** Linux differs from Windows/macOS — there is no MDM configuration profile concept. Compliance policies are detect-only; enforcement is via custom-compliance Bash discovery scripts that REPORT a verdict (the script does not push a fix; it returns compliant/non-compliant). For tenant-side CA enforcement, see PITFALL-2 architecture callout above and [Conditional Access](05-conditional-access.md).
```

**H2: `## Steps`** with H3 sub-steps for each of the 4 settings-catalog categories:

```
### Step 1: Create the Linux compliance policy

#### In Intune admin center
1. Navigate to **Devices > Compliance > Policies > Create Policy**
2. Platform: **Linux**
3. Name: e.g., `Linux-Baseline-Compliance`
4. Click **Create**

### Step 2: Configure Allowed Distributions

[Description: which Ubuntu LTS distros + version constraints. Reference Phase 49 prerequisites.]

### Step 3: Configure Custom Compliance (Bash discovery scripts)

[Concept-level only. LIN-DEFER-01 defers the deep-dive to v1.5.1. State that the script returns a compliance verdict; do NOT include a multi-line script library.]

### Step 4: Configure Device Encryption (dm-crypt + LUKS)

[Reference to Phase 49 glossary entries `dm-crypt` and `LUKS`.]

### Step 5: Configure Password Policy

[Length / complexity / lock interval — settings-catalog-category 4.]
```

**H2: `## Verification`** — checklist (compliance policy saved; assigned to a test group; test device reports correct compliance verdict).

**H2: `## Configuration-Caused Failures`**:
```
| Misconfiguration | Portal | Symptom | Runbook |
|------------------|--------|---------|---------|
| Allowed Distribution excludes user's distro version | Intune admin center | Device reports non-compliant | [31-linux-compliance-non-compliant.md](../l1-runbooks/31-linux-compliance-non-compliant.md) |
| Custom-compliance Bash script syntax error | Intune admin center | Compliance evaluation fails | [31-linux-compliance-non-compliant.md](../l1-runbooks/31-linux-compliance-non-compliant.md) |
| Password-policy threshold exceeds device baseline | Intune admin center | Device reports non-compliant immediately | [31-linux-compliance-non-compliant.md](../l1-runbooks/31-linux-compliance-non-compliant.md) |
| Admin expects CA enforcement | Entra portal | No CA grant — see PITFALL-2 above | [32-linux-ca-blocking-web-access.md](../l1-runbooks/32-linux-ca-blocking-web-access.md) |
```

**H2: `## See Also`**
- [Conditional Access](05-conditional-access.md)
- [Linux Capability Matrix — Compliance](../reference/linux-capability-matrix.md#compliance)
- [Linux Capability Matrix — Conditional Access](../reference/linux-capability-matrix.md#conditional-access)
- [Linux Glossary — web-app CA](../_glossary-linux.md#web-app-ca)
- (Phase 51 runbook) [31-linux-compliance-non-compliant.md](../l1-runbooks/31-linux-compliance-non-compliant.md)

**Version History:**
```
---

| Date | Change | Author |
|------|--------|--------|
| 2026-04-27 | Initial version — Linux compliance policy + PITFALL-2 architectural callout (Phase 50) | -- |
```

Per CONTEXT.md D-24 (PITFALL-2 validator assertion: `Require device to be marked as compliant` + `not available|not supported`).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/admin-setup-linux/03-compliance-policy.md','utf8');const ok=c.includes('platform: Linux')&&c.includes('audience: admin')&&c.includes('Require device to be marked as compliant')&&/not available|not supported/i.test(c)&&c.includes('Allowed Distributions')&&c.includes('Custom Compliance')&&c.includes('Device Encryption')&&c.includes('Password Policy')&&c.includes('05-conditional-access.md');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/admin-setup-linux/03-compliance-policy.md`
    - Frontmatter: `platform: Linux`, `audience: admin`, `applies_to: enrollment`, 60-day cycle
    - Contains literal string `Require device to be marked as compliant` (V-50-21 PITFALL-2 literal)
    - Matches regex `/not available|not supported/i` (V-50-21 PITFALL-2 qualifier)
    - Contains all 4 settings-catalog category names: `Allowed Distributions`, `Custom Compliance`, `Device Encryption`, `Password Policy`
    - Contains relative link `05-conditional-access.md` and `../reference/linux-capability-matrix.md#conditional-access`
    - Contains 4-row Configuration-Caused Failures table referencing Phase 51 runbook 31 + 32 paths
  </acceptance_criteria>
  <done>03-compliance-policy.md exists with PITFALL-2 architectural callout in opening section, all 4 settings-catalog categories covered, See Also cross-links present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author docs/admin-setup-linux/04-app-delivery.md (PITFALL-1)</name>
  <files>docs/admin-setup-linux/04-app-delivery.md</files>
  <read_first>
    - docs/admin-setup-macos/04-app-deployment.md (PRIMARY analog — opens with wide-scope comparison table at lines 18-29; Linux INVERTS this with a scope RESTRICTION callout)
    - .planning/phases/50-linux-admin-setup-capability-matrix/50-PATTERNS.md (lines 233-249 — 04-app-delivery.md pattern map)
    - .planning/phases/50-linux-admin-setup-capability-matrix/50-CONTEXT.md (D-24 PITFALL-1 callout assertion: "script-based only" or "no Win32"; CD-07 concept-only + at most 1 minimal Bash example)
    - .planning/research/PITFALLS.md §Pitfall 1 (lines 11-31) — Linux capability parity framing; partial-mapping discipline
  </read_first>
  <behavior>
    - File exists with C10-compliant frontmatter
    - Opens (BEFORE first H2) with PITFALL-1 scope callout containing literal "script-based only" or "no Win32"
    - Concept-level overview only — no full Bash script library (LIN-DEFER-01 v1.5.1 deferral)
    - At most 1 minimal Bash example (CD-07)
  </behavior>
  <action>
Create `docs/admin-setup-linux/04-app-delivery.md` with PITFALL-1 scope-restriction opening.

**Frontmatter (D-03):**
```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: enrollment
audience: admin
platform: Linux
---
```

**H1:** `# Linux App Delivery — Admin Overview`

**Platform-gate blockquote:**
```
> **Platform gate:** This guide covers app delivery to Linux devices managed by Intune (Ubuntu 22.04/24.04 LTS).
> For Linux provisioning terminology, see the [Linux Provisioning Glossary](../_glossary-linux.md).
```

**MANDATORY PITFALL-1 scope callout (BEFORE first H2):**

EXACT format (must contain literal "script-based only" — V-50-20 assertion):
```
> 📋 **Scope note — Linux app delivery is script-based only.** Intune does NOT deliver binary packages to Linux. There is no Win32 / MSIX / `.pkg` / `.deb` direct-delivery analog for Linux. Intune delivers Shell/Bash scripts to the device; the script itself is responsible for downloading and installing whatever package the org needs (e.g., `apt install` from a trusted repo, `curl | sh` from a vendor URL, etc.).
>
> See [Linux Capability Matrix — App Deployment](../reference/linux-capability-matrix.md#app-deployment) for the bilateral-comparison-context.
```

**H2: `## Prerequisites`**
- Linux device enrolled and reporting healthy
- Intune Linux client installed (see [01-intune-linux-agent.md](01-intune-linux-agent.md))
- Bash available on target distro (default on Ubuntu LTS)
- Trusted package repository configured (admin's choice — packages.microsoft.com for Microsoft software; org-internal apt mirror; vendor URLs)

**H2: `## Concept Overview`**

```
Intune delivers Bash scripts to Linux devices as the only app-delivery mechanism. The script content is org-authored; Intune handles delivery, schedule, and exit-code reporting. The Linux client executes the script and surfaces success/failure to the Intune admin center based on script exit code.

Common patterns (concept-level — full deep-dive deferred to v1.5.1 LIN-DEFER-01 Bash custom-compliance guide):
- Install a deb from a Microsoft repo: script runs `apt update && apt install <package>`
- Install a vendor binary: script `curl`s the binary, verifies signature, runs installer
- Configure a service: script writes config files, runs `systemctl enable --now <unit>`
```

**(OPTIONAL — CD-07 max 1 example) H2: `## Minimal Example`**

ONE 5-10 line Bash example for illustration only. Do NOT author a script library.

**H2: `## Verification`** — checklist (script saved in Intune admin center; assigned to test group; test device reports successful exit code).

**H2: `## See Also`**
- [Linux Capability Matrix — App Deployment](../reference/linux-capability-matrix.md#app-deployment)
- [Compliance Policy](03-compliance-policy.md) — custom-compliance Bash scripts use the same execution surface
- (v1.5.1 LIN-DEFER-01) Bash custom-compliance deep-dive — when shipped, will cross-link here

**Version History:**
```
---

| Date | Change | Author |
|------|--------|--------|
| 2026-04-27 | Initial version — Linux app delivery concept overview + PITFALL-1 scope callout (Phase 50) | -- |
```

Per CONTEXT.md D-24 (PITFALL-1 validator assertion: "script-based only" or "no Win32"), CD-07 (concept-only; at most 1 minimal example).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/admin-setup-linux/04-app-delivery.md','utf8');const ok=c.includes('platform: Linux')&&c.includes('audience: admin')&&(c.includes('script-based only')||c.includes('no Win32'))&&c.includes('../reference/linux-capability-matrix.md#app-deployment');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/admin-setup-linux/04-app-delivery.md`
    - Frontmatter: `platform: Linux`, `audience: admin`, `applies_to: enrollment`, 60-day cycle
    - Contains literal substring `script-based only` OR `no Win32` (V-50-20 PITFALL-1 assertion)
    - Contains relative link `../reference/linux-capability-matrix.md#app-deployment`
    - Contains literal string `## See Also`
    - Total Bash examples ≤ 1 (concept-only per CD-07)
  </acceptance_criteria>
  <done>04-app-delivery.md exists with PITFALL-1 scope callout BEFORE first H2, concept-level overview only, no full Bash script library.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author docs/admin-setup-linux/05-conditional-access.md (PITFALL-2 inheritance)</name>
  <files>docs/admin-setup-linux/05-conditional-access.md</files>
  <read_first>
    - docs/reference/ca-enrollment-timing.md (PRIMARY analog — CA-constraint problem-statement framing at lines 13-55)
    - docs/admin-setup-macos/05-compliance-policy.md (CA framing within compliance-policy file — useful for the architectural blockquote)
    - .planning/phases/50-linux-admin-setup-capability-matrix/50-PATTERNS.md (lines 251-273 — 05-conditional-access.md pattern map)
    - .planning/phases/50-linux-admin-setup-capability-matrix/50-CONTEXT.md (D-02/D-04 — CA elevated; PITFALL-2 inheritance; CDI-Phase50-04 — anchor stability for Phase 51 runbook 32)
    - .planning/research/PITFALLS.md §Pitfall 2 (lines 33-56) — CA architectural callout mandate
  </read_first>
  <behavior>
    - File exists with C10-compliant frontmatter (`applies_to: conditional-access`)
    - Opens with PITFALL-2 architecture callout (web-app CA only, no device-level CA)
    - File does NOT contain H2 `## Device-Level Conditional Access` or similar
    - Documents Microsoft Edge for Linux 102.x+ as the only CA enforcement vehicle
    - See Also links to ../reference/linux-capability-matrix.md#conditional-access (anchor-stability contract for Phase 51 runbook 32)
  </behavior>
  <action>
Create `docs/admin-setup-linux/05-conditional-access.md` with PITFALL-2 inheritance and web-app-CA-only framing.

**Frontmatter (D-03 with CA-specific applies_to):**
```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: conditional-access
audience: admin
platform: Linux
---
```

**H1:** `# Linux Conditional Access — Admin Configuration`

**Platform-gate blockquote:**
```
> **Platform gate:** This guide covers Conditional Access (CA) configuration for Linux devices in Intune (Ubuntu 22.04/24.04 LTS). Linux supports web-app CA via Microsoft Edge for Linux 102.x+ ONLY — there is no device-level CA grant on Linux.
> For Linux provisioning terminology, see the [Linux Provisioning Glossary](../_glossary-linux.md).
> For the locked Linux management surface, see [Linux Enrollment Overview](../linux-lifecycle/00-enrollment-overview.md#supported-management-surface).
```

**MANDATORY PITFALL-2 inheritance callout (BEFORE first H2):**
```
> ⚠️ **Architecture: Web-app CA only.** Linux does NOT support device-level Conditional Access. The CA grant control `Require device to be marked as compliant` is not available on Linux. The only CA enforcement path on Linux is web-app CA via Microsoft Edge for Linux 102.x+ (the user signs into Edge with their org account; Edge surfaces the CA challenge; the org's CA policies enforce the grant or block). For cross-platform CA architectural reference, see [Linux Capability Matrix — Conditional Access](../reference/linux-capability-matrix.md#conditional-access).
```

**Negative architectural assertion:** This file MUST NOT contain an H2 titled `## Device-Level Conditional Access` (or any H2 implying device-level CA exists on Linux). The CA capability is web-app-only.

**H2: `## Prerequisites`**
- Intune license assigned to user
- Entra ID P1 (P2 if risk-based CA)
- Microsoft Edge for Linux 102.x+ installed on device (see [01-intune-linux-agent.md](01-intune-linux-agent.md) for related package install patterns)
- Linux device enrolled (see [02-enrollment-profile.md](02-enrollment-profile.md))

**H2: `## How Web-App CA Works on Linux`**

Problem-statement + why-it-happens framing modeled after `docs/reference/ca-enrollment-timing.md` lines 13-55:
```
**The problem (architectural):** Linux's Intune integration does not surface device-state-attestation to Conditional Access in the way Windows/macOS/iOS/Android do. The `Require device to be marked as compliant` grant control is not part of the Linux CA evaluation surface.

**The supported pattern:** Web-app CA via Microsoft Edge for Linux 102.x+. Edge presents the CA challenge as part of org-app sign-in; CA policies evaluate based on user, session, and risk context — but NOT on Linux device compliance state.

**What this means in practice:**
- Compliance policy on Linux is **detect-only** (admins can monitor compliance verdicts; no CA-driven block)
- Web-app access (Outlook web, Teams web, SharePoint, etc.) via Edge IS protected by CA policies that target user/session/risk
- Native Linux apps that connect to org resources are NOT protected by Linux device CA — orgs must ensure such apps use web-app CA flows or are otherwise scoped
```

**H2: `## Steps`** — concept-level configuration steps:
```
### Step 1: Verify Edge for Linux 102.x+ deployed to target devices

#### On device
1. Run: `microsoft-edge --version` — confirm 102.x or higher

### Step 2: Configure CA policy targeting Linux web-app sign-ins

#### In Entra portal
1. Navigate to **Protection > Conditional Access > Policies > New policy**
2. Targeted resource: applicable cloud apps (e.g., Office 365, SharePoint, Teams)
3. Conditions > Device platforms: include **Linux**
4. (Note) Grant controls: avoid `Require device to be marked as compliant` for Linux scope — that grant is not available
5. Use session-based or other grant-control mechanisms compatible with browser-based access

### Step 3: Test the CA policy

[Test from a Linux device with Edge; verify the CA challenge surfaces correctly]
```

**H2: `## Verification`** — checklist (Edge version verified; CA policy saved; test sign-in surfaces CA challenge).

**H2: `## See Also`**
- [Linux Capability Matrix — Conditional Access](../reference/linux-capability-matrix.md#conditional-access) — anchor-stability contract for Phase 51 runbook 32 per CDI-Phase50-04
- [Compliance Policy](03-compliance-policy.md) — PITFALL-2 architectural callout (compliance is detect-only on Linux)
- [Enrollment Profile](02-enrollment-profile.md)
- [Linux Glossary — web-app CA](../_glossary-linux.md#web-app-ca)
- (Phase 51 runbook) [32-linux-ca-blocking-web-access.md](../l1-runbooks/32-linux-ca-blocking-web-access.md)

**Version History:**
```
---

| Date | Change | Author |
|------|--------|--------|
| 2026-04-27 | Initial version — Linux Conditional Access (web-app CA via Edge only) + PITFALL-2 inheritance (Phase 50) | -- |
```

Per CONTEXT.md D-02 (CA elevated; anchor stability), D-24 (PITFALL-2 inheritance), CDI-Phase50-04 (anchor-stability for Phase 51 runbook 32 + Phase 58).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/admin-setup-linux/05-conditional-access.md','utf8');const ok=c.includes('platform: Linux')&&c.includes('audience: admin')&&c.includes('applies_to: conditional-access')&&c.includes('Require device to be marked as compliant')&&c.includes('Edge')&&!/^## Device-Level Conditional Access\s*$/m.test(c)&&c.includes('../reference/linux-capability-matrix.md#conditional-access');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/admin-setup-linux/05-conditional-access.md`
    - Frontmatter: `platform: Linux`, `audience: admin`, `applies_to: conditional-access`, 60-day cycle
    - Contains literal `Require device to be marked as compliant` AND substring `Edge`
    - Does NOT match regex `^## Device-Level Conditional Access\s*$` (negative assertion — V-50 anti-Device-CA-H2 probe)
    - Contains literal `../reference/linux-capability-matrix.md#conditional-access` (CDI-Phase50-04 anchor-stability link)
    - Documents Microsoft Edge for Linux 102.x+ as the CA enforcement vehicle
  </acceptance_criteria>
  <done>05-conditional-access.md exists with PITFALL-2 inheritance callout BEFORE first H2, web-app-CA-only framing throughout, no device-level CA H2, anchor-stability link to capability matrix CA H2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation-only | No application code; documentation provides architectural guidance that prevents misconfiguration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-03 | Elevation of Privilege | PITFALL-2 architectural callout in 03-compliance-policy.md + 05-conditional-access.md | mitigate | Callouts MUST surface that `Require device to be marked as compliant` is not available on Linux. Without the callout, an admin may configure a CA policy expecting Linux device-state to gate access — leading to an unenforced policy and an unintended access path. |
| T-50-04 | Tampering (script supply chain) | PITFALL-1 scope callout in 04-app-delivery.md | mitigate | Callout MUST direct admins that Bash scripts run unconstrained on Linux devices; orgs are responsible for verifying script source/signatures. Documentation-as-supply-chain-hygiene mitigation. |

Phase 50 is documentation-only — these are documentation-fidelity risks rather than application security risks.
</threat_model>

<verification>
After all 3 tasks complete:

```bash
# All 3 files exist
test -f docs/admin-setup-linux/03-compliance-policy.md \
  && test -f docs/admin-setup-linux/04-app-delivery.md \
  && test -f docs/admin-setup-linux/05-conditional-access.md

# PITFALL probes
grep -F "Require device to be marked as compliant" docs/admin-setup-linux/03-compliance-policy.md
grep -F "Require device to be marked as compliant" docs/admin-setup-linux/05-conditional-access.md
grep -E "script-based only|no Win32" docs/admin-setup-linux/04-app-delivery.md

# Frontmatter probe
node -e "const fs=require('fs');const files=['docs/admin-setup-linux/03-compliance-policy.md','docs/admin-setup-linux/04-app-delivery.md','docs/admin-setup-linux/05-conditional-access.md'];let ok=true;for(const f of files){const c=fs.readFileSync(f,'utf8');if(!c.includes('platform: Linux'))ok=false;if(!c.includes('audience: admin'))ok=false;}process.exit(ok?0:1)"
```

DO NOT commit — atomic D-18 commit owned by plan 06.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 50 Success Criteria:
- **SC#2 (PITFALL-2 architectural callout in 03-compliance-policy.md):** ✅ Task 1 delivers
- **SC#5 (check-phase-50.mjs frontmatter cycle on admin files):** ✅ All 3 files carry C10-compliant frontmatter

Plan complete when:
- [ ] 3 files exist at locked paths
- [ ] All frontmatter is C10-compliant
- [ ] PITFALL-2 callout in 03-compliance-policy.md uses literal `Require device to be marked as compliant` + qualifier
- [ ] PITFALL-1 callout in 04-app-delivery.md uses "script-based only" or "no Win32"
- [ ] 05-conditional-access.md has PITFALL-2 inheritance + no `## Device-Level Conditional Access` H2
- [ ] All cross-links to plan-04 capability matrix anchors (#compliance, #conditional-access, #app-deployment) present (resolution at atomic commit time)
</success_criteria>

<output>
After completion, create `.planning/phases/50-linux-admin-setup-capability-matrix/50-02-SUMMARY.md` documenting:
- 3 files authored with line counts
- PITFALL-2 callout exact text (in both 03 and 05)
- PITFALL-1 callout exact text (in 04)
- D-02 anchor-stability link verification (05-conditional-access.md → matrix #conditional-access)
- 4 settings-catalog categories enumerated in 03
- Note: NO COMMIT — atomic D-18 commit owned by plan 06
</output>
