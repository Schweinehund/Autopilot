---
phase: 60
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l1-runbooks/02-esp-stuck-or-failed.md
  - docs/l1-runbooks/21-ios-compliance-blocked.md
autonomous: true
requirements: [AUDIT-05]
must_haves:
  truths:
    - "All 6 broken anchor refs in docs/l1-runbooks/{02,21}.md (per 48-VERIFICATION lines 38-40 + 51-53) resolve to existing in-document anchors after fix"
    - "Each fix follows D-08 HTML-shim cluster-edit pattern (2 commits = 1 per file)"
    - "Pre-Plan-09 second-pass mlc sweep finds 0 NEW findings against 48-VERIFICATION baseline for these 2 files"
    - "No Phase 49-59 V-NN-NN structural invariant breaks (regression-guard satisfied per D-26)"
  artifacts:
    - path: "docs/l1-runbooks/02-esp-stuck-or-failed.md"
      provides: "Windows ESP L1 runbook with 3 valid quick-nav anchors"
      contains: "<a id=\"device-phase-steps\"></a>"
    - path: "docs/l1-runbooks/21-ios-compliance-blocked.md"
      provides: "iOS compliance L1 runbook with 3 valid quick-nav anchors"
      contains: "<a id=\"cause-a-ca-gap\"></a>"
  key_links:
    - from: "Lines 26-31 of each runbook (How to Use This Runbook bullet list)"
      to: "Per-section H2 in same file with {#explicit-id}"
      via: "<a id=\"...\"></a> shim above H2"
      pattern: "<a id=\"(device-phase-steps|user-phase-steps|error-code-steps|cause-a-ca-gap|cause-b-policy-mismatch|cause-c-default-posture)\"></a>"
---

<objective>
Fix 6 Category A broken anchor references in 2 mixed-platform l1-runbooks (Windows ESP + iOS compliance). Apply D-08 HTML-shim pattern. 2 commits per D-25 progressive-landing.

Purpose: Closes 6 of the 51 Category A baseline findings before Plan 09's atomic harness commit promotes C13 to blocking.
Output: 2 modified .md files (1 commit each)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-CONTEXT.md
@.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md
@docs/l1-runbooks/02-esp-stuck-or-failed.md

<interfaces>
<!-- Verified file structure: docs/l1-runbooks/02-esp-stuck-or-failed.md -->
<!-- Lines 35, 62, 87 host the H2 headings: -->
<!--   ## Device Phase Steps {#device-phase-steps} -->
<!--   ## User Phase Steps {#user-phase-steps} -->
<!--   ## Error Code Steps {#error-code-steps} -->
<!-- Quick-nav at lines 29-31 references these as #device-phase-steps / #user-phase-steps / #error-code-steps -->
<!-- mlc-tool 3.14.2 doesn't honor {#explicit-id}; HTML shim required. -->

48-VERIFICATION-broken-links.md baseline rows for this plan:
  - 02-esp-stuck-or-failed.md lines 29-31 (3 anchors): #device-phase-steps, #user-phase-steps, #error-code-steps
  - 21-ios-compliance-blocked.md lines 26-28 (3 anchors): #cause-a-ca-gap, #cause-b-policy-mismatch, #cause-c-default-posture
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix 02-esp-stuck-or-failed.md (3 anchor shims)</name>
  <files>docs/l1-runbooks/02-esp-stuck-or-failed.md</files>
  <read_first>
    - docs/l1-runbooks/02-esp-stuck-or-failed.md (full file; H2s confirmed at lines 35, 62, 87 with {#device-phase-steps}, {#user-phase-steps}, {#error-code-steps})
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md lines 38-40
  </read_first>
  <action>
    Insert 3 HTML shims at the EXACT verified line positions:

    Above line 35 (`## Device Phase Steps {#device-phase-steps}`):
    ```
    <a id="device-phase-steps"></a>

    ## Device Phase Steps {#device-phase-steps}
    ```

    Above line 62 (`## User Phase Steps {#user-phase-steps}`): insert `<a id="user-phase-steps"></a>` plus blank line.

    Above line 87 (`## Error Code Steps {#error-code-steps}`): insert `<a id="error-code-steps"></a>` plus blank line.

    DO NOT modify the quick-nav bullets at lines 29-31. DO NOT modify the {#explicit-id} attributes on the H2 lines. DO NOT modify frontmatter or any other content.

    Note: Line numbers of H2 headings will shift by +2 each as shims+blank-lines insert above (so final positions become ~37, 66, 92 — but these line shifts do NOT trigger any sidecar pin updates because docs/l1-runbooks/02-esp-stuck-or-failed.md is NOT in any sidecar pin list per RESEARCH PITFALL-12 surface watched).

    Commit: `fix(60-04): repair 3 broken anchors in 02-esp-stuck-or-failed.md (cluster-edit per D-08)`

    Regression-guard: `node scripts/validation/v1.5-milestone-audit.mjs` must exit 0 post-commit.
  </action>
  <verify>
    <automated>grep -c 'id="device-phase-steps"\|id="user-phase-steps"\|id="error-code-steps"' docs/l1-runbooks/02-esp-stuck-or-failed.md | awk '{ if ($1 >= 3) print "PASS shims="$1; else print "FAIL shims="$1 }' && grep -c '<a id="device-phase-steps"></a>' docs/l1-runbooks/02-esp-stuck-or-failed.md | awk '{ if ($1 == 1) print "PASS exact-shim-1"; else print "FAIL shim-1="$1 }' && node scripts/validation/v1.5-milestone-audit.mjs > /dev/null 2>&1 && echo "PASS harness-green" || echo "FAIL harness"</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 3 net new HTML shim lines: `<a id="device-phase-steps"></a>`, `<a id="user-phase-steps"></a>`, `<a id="error-code-steps"></a>`
    - Each shim sits immediately before (or one blank line before) the corresponding H2 (verified: line offset between shim and H2 is 1 or 2 lines)
    - Quick-nav bullets at original lines 29-31 unchanged (`grep '^- \[Device Phase Steps\]' file == 1`, `grep '^- \[User Phase Steps\]' file == 1`, `grep '^- \[Error Code Steps\]' file == 1`)
    - Frontmatter unchanged (`grep -c 'last_verified:' file == 1`)
    - Commit message starts with `fix(60-04): repair 3 broken anchors in 02-esp-stuck-or-failed.md`
    - `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 post-commit
  </acceptance_criteria>
  <done>3 shims added at lines 35/62/87; harness regression-guard PASSES; commit landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix 21-ios-compliance-blocked.md (3 anchor shims)</name>
  <files>docs/l1-runbooks/21-ios-compliance-blocked.md</files>
  <read_first>
    - docs/l1-runbooks/21-ios-compliance-blocked.md (lines 1-150; locate ## Cause A/B/C H2s with {#cause-...} attributes)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md lines 51-53
  </read_first>
  <action>
    Insert 3 HTML shims for cause-a-ca-gap, cause-b-policy-mismatch, cause-c-default-posture immediately above the corresponding H2 headings (each is `## Cause X: ... {#cause-X-...}` form).

    Concrete edits:
    ```
    <a id="cause-a-ca-gap"></a>

    ## Cause A: ... {#cause-a-ca-gap}
    ```

    Repeat for `cause-b-policy-mismatch` and `cause-c-default-posture`.

    DO NOT modify quick-nav bullets at lines 26-28. DO NOT modify {#cause-...} attributes. DO NOT modify frontmatter.

    Commit: `fix(60-04): repair 3 broken anchors in 21-ios-compliance-blocked.md (cluster-edit per D-08)`

    Regression-guard: `node scripts/validation/v1.5-milestone-audit.mjs` must exit 0 post-commit.
  </action>
  <verify>
    <automated>grep -c 'id="cause-a-ca-gap"\|id="cause-b-policy-mismatch"\|id="cause-c-default-posture"' docs/l1-runbooks/21-ios-compliance-blocked.md | awk '{ if ($1 >= 3) print "PASS shims="$1; else print "FAIL shims="$1 }' && grep -c '<a id="cause-a-ca-gap"></a>' docs/l1-runbooks/21-ios-compliance-blocked.md | awk '{ if ($1 == 1) print "PASS exact-shim-a"; else print "FAIL shim-a="$1 }' && node scripts/validation/v1.5-milestone-audit.mjs > /dev/null 2>&1 && echo "PASS harness-green" || echo "FAIL harness"</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 3 net new HTML shim lines with the 3 expected ids
    - Frontmatter unchanged (`grep -c 'last_verified:' file == 1`)
    - Commit message starts with `fix(60-04): repair 3 broken anchors in 21-ios-compliance-blocked.md`
    - `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 post-commit
    - Total in plan: 2 commits with `fix(60-04):` prefix; 6 anchor shims total
  </acceptance_criteria>
  <done>3 shims added; harness regression-guard PASSES; commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown content -> harness scope | Both files in walkMd('docs') scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04-01 | T (Tampering) | docs/l1-runbooks/{02,21}.md content | mitigate | Per-commit harness regression-guard ensures Phase 49-59 V-NN-NN invariants preserved. |
</threat_model>

<verification>
- 2 commits land with `fix(60-04):` prefix
- 6 net new `<a id="..."></a>` shim lines across 2 files
- `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 after each commit
</verification>

<success_criteria>
- 6 broken anchor refs in 48-VERIFICATION baseline rows 38-40 + 51-53 are now resolvable
- 2 commits per D-25 progressive-landing
- No PITFALL-12 trigger
- Harness regression-guard GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/60-audit-harness-v1-5-finalization/60-04-SUMMARY.md`
</output>
