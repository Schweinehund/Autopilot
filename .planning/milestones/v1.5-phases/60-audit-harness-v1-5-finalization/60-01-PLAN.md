---
phase: 60
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md
autonomous: true
requirements: [AUDIT-03]
# context_notes:
#   - "Phase 48 D-06 carry-over honored: Section A of 60-CALIBRATION.md serves the C9
#     promotion target documented in Phase 48 CONTEXT D-06. C9 has no dedicated AUDIT-NN
#     ID per REQUIREMENTS.md traceability; the C9 promotion lifts a Phase 48 carry-over
#     rather than satisfying a literal AUDIT-NN requirement. Plan 01 therefore covers BOTH
#     the C11 (AUDIT-03 calibration) AND the C9 (Phase 48 D-06 carry-over) corpus surfaces."
must_haves:
  truths:
    - "60-CALIBRATION.md exists with two sections: Section A C9 cope_banned_phrases corpus scan and Section B C11 ops-domain anti-pattern corpus scan with proximity-window evaluation (per D-27)"
    - "Section A enumerates every match of the 8 cope_banned_phrases regex against docs/admin-setup-android/**/*.md + docs/operations/**/*.md + docs/android-lifecycle/**/*.md with disposition column (legitimate-disambiguation vs anti-pattern)"
    - "Section B enumerates every match of the 4 c11 ops patterns against docs/**/*.md with proximity-window keyword match result (windowed-exempt vs requires-pinning vs anti-pattern)"
    - "Section B confirms the 6 currently-hit lines (docs/operations/patch-management/00-overview.md:77,82,85 + 01-windows-wufb-rings.md:60,63,75) all pass the D-01 keyword window OR are listed for c11_ops_exemptions[] pinning (per D-05)"
    - "Calibration step is READ-ONLY commit produced BEFORE any harness or content changes (per D-28)"
    - "Artifact informs c9_exemptions[] pin set + final C11 keyword list (which is the central calibration knob per D-01 Claude's Discretion)"
    - "Plan 01 covers BOTH corpus surfaces in a single calibration pass: Section A serves the C9 promotion (Phase 48 D-06 carry-over -- no dedicated AUDIT-NN ID per REQUIREMENTS.md traceability) AND Section B serves the C11 promotion (AUDIT-03 literal). The requirements field cites AUDIT-03 only because C9 has no AUDIT-NN; the actual scope is dual-corpus per CONTEXT D-17/D-27."
  artifacts:
    - path: ".planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md"
      provides: "Pre-promotion C9 + C11 corpus scan output"
      contains: "## Section A -- C9, ## Section B -- C11"
  key_links:
    - from: "60-CALIBRATION.md Section A"
      to: "Plan 09 atomic harness commit's c9_exemptions[] pin set"
      via: "pin-list table rows tagged 'legitimate-disambiguation'"
      pattern: "legitimate-disambiguation.*Phase 53|54|55|56"
    - from: "60-CALIBRATION.md Section B"
      to: "Plan 09 atomic harness commit's C11 keyword regex final shape"
      via: "windowed-exempt evaluation per pattern hit"
      pattern: "windowed-exempt|requires-pinning"
---

<objective>
Run the pre-promotion calibration corpus scan against Phase 53/54/55/56 + Android scope content. Produce `.planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md` enumerating every C9 cope_banned_phrases hit and every C11 ops-domain anti-pattern hit, with disposition + proximity-window evaluation. This is the evidence base for Plan 09's atomic harness commit (c9_exemptions[] pin set + C11 final keyword list).

Purpose: Per D-27/D-28, calibration MUST run BEFORE harness changes so we have the exemption-pinning evidence to land C9/C11 promotion safely. Without this, C9 ships blocking with no escape valve and C11 may FAIL on legitimate disambiguation prose.
Output: 60-CALIBRATION.md (read-only artifact)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-CONTEXT.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-RESEARCH.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-PATTERNS.md
@.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md
@scripts/validation/v1.5-milestone-audit.mjs
@scripts/validation/v1.5-audit-allowlist.json

<interfaces>
<!-- C9 scan source: harness lines 417-437 + sidecar lines 33-42 (8 cope_banned_phrases) -->
<!-- C11 scan source: harness lines 477-504 (4 ops patterns) -->
<!-- Disposition output schema: matches 48-VERIFICATION-broken-links.md frontmatter + Category sections + Summary table shape -->

C9 cope_banned_phrases regex set (verbatim from sidecar lines 33-42):
```
"\\bCOPE\\b[^.]*\\bdeprecated\\b"
"\\bCOPE\\b[^.]*\\bend of life\\b"
"\\bCOPE\\b[^.]*\\bremoved\\b"
"\\bCOPE\\b[^.]*\\bEOL\\b"
"\\bCOPE\\b[^.]*\\bno longer supported\\b"
"\\bCOPE\\b[^.]*\\bobsolete\\b"
"\\bCOPE\\b[^.]*\\bsunset\\b"
"\\bCOPE\\b[^.]*\\bretired\\b"
```

C11 ops patterns (verbatim from harness lines 487-490):
```
'\\bSystem Center\\b'
'\\bSCCM\\b[^.]*\\bIntune\\b'
'\\bAutopatch rings\\b'
'\\bSafetyNet\\b[^.]*\\bcompliance\\b'
```

C11 D-01 starting proximity-window keyword regex:
```
/successor|deprecated|historical|disambiguation|mutual-exclusion|PITFALL-9|first-occurrence|callout/i
```
(±200 char window per match; refine at this plan against actual hits.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run C9 corpus scan + C11 corpus scan; record raw hits</name>
  <files>.planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md</files>
  <read_first>
    - .planning/phases/60-audit-harness-v1-5-finalization/60-CONTEXT.md (D-27 + D-28 calibration mandate)
    - .planning/phases/60-audit-harness-v1-5-finalization/60-RESEARCH.md (HIGH-confidence "Note on regex semantic" claim that 4 file-level C9 hits surface in current scan)
    - scripts/validation/v1.5-milestone-audit.mjs lines 417-504 (C9 + C11 implementations to mirror in scan)
    - scripts/validation/v1.5-audit-allowlist.json lines 33-42 (cope_banned_phrases verbatim)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md lines 1-30 (frontmatter + Header pattern to mirror)
  </read_first>
  <action>
    Step 1: Run C9 corpus scan. For EACH of the 8 cope_banned_phrases regex (sidecar lines 33-42, compiled as `new RegExp(p, 'gi')`), iterate every file matching `docs/admin-setup-android/**/*.md` + `docs/operations/**/*.md` + `docs/android-lifecycle/**/*.md` (Android scope per harness `androidDocPaths()` + ops corpus). For each match, capture {file, line, pattern, surrounding 80-char context}.

    Concrete invocation pattern (run this from repo root via PowerShell or bash):
    ```bash
    # Generates raw hit list; dispositions assigned in Task 2
    for pattern in 'COPE.*deprecated' 'COPE.*end of life' 'COPE.*removed' 'COPE.*EOL' 'COPE.*no longer supported' 'COPE.*obsolete' 'COPE.*sunset' 'COPE.*retired'; do
      grep -rn -i "$pattern" docs/admin-setup-android/ docs/operations/ docs/android-lifecycle/ 2>/dev/null
    done
    ```

    Step 2: Run C11 corpus scan. For EACH of the 4 ops patterns (harness lines 487-490, compiled as `new RegExp(p, 'gi')`), iterate every `docs/**/*.md` file. For each match, capture {file, line, pattern, ±200-char window content}.

    Concrete invocation pattern:
    ```bash
    for pattern in 'System Center' 'SCCM.*Intune' 'Autopatch rings' 'SafetyNet.*compliance'; do
      grep -rn -i -E "$pattern" docs/ 2>/dev/null
    done
    ```

    Step 3: For EACH C11 hit captured in step 2, evaluate the ±200-char window using D-01 keyword regex `/successor|deprecated|historical|disambiguation|mutual-exclusion|PITFALL-9|first-occurrence|callout/i`. Record disposition:
    - `windowed-exempt` if any keyword present in ±200-char window
    - `requires-pinning` if NO keyword in window AND hit is legitimate disambiguation (manual judgment)
    - `anti-pattern` if NO keyword AND hit is genuinely incorrect ops-domain prose (would be a Phase 49-59 regression — should be 0 entries)

    Per RESEARCH HIGH-confidence claim (60-RESEARCH.md lines 173-220): the 6 currently-hit lines `docs/operations/patch-management/00-overview.md:77,82,85` + `docs/operations/patch-management/01-windows-wufb-rings.md:60,63,75` MUST all classify as `windowed-exempt` if D-01 keyword set is sufficient. If any of these 6 fall outside the window, refine the keyword set OR add to `requires-pinning` for c11_ops_exemptions[] population in Plan 09.

    Per RESEARCH HIGH-confidence claim (60-RESEARCH.md "Note on regex semantic" at lines 380-398): C9 may produce 4 file-level false positives at `_glossary-android.md:196` + `admin-setup-android/03-fully-managed-cobo.md:149` + `android-lifecycle/03-android-version-matrix.md:37` + `reference/android-capability-matrix.md:50`. Each of these gets disposition `legitimate-disambiguation` if the pattern hit spans a paragraph boundary (greedy `[^.]*` matching past period across newlines). These become c9_exemptions[] entries in Plan 09.

    Write all raw hits + dispositions into `.planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md` as draft.
  </action>
  <verify>
    <automated>test -f .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md && grep -c '^| docs/' .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md | awk '{ if ($1 >= 1) print "PASS rows="$1; else print "FAIL no-rows" }' && grep -c '^## Section [AB]' .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md | awk '{ if ($1 == 2) print "PASS sections=2"; else print "FAIL sections="$1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md` exists
    - File contains exactly two `## Section A` and `## Section B` H2s (`grep -c '^## Section [AB] ' 60-CALIBRATION.md` returns 2)
    - Section A row count >= 0 (zero hits OK; mirrors `48-VERIFICATION-broken-links.md` zero-finding documentation pattern)
    - Section B row count >= 6 (the 6 known WUfB/Autopatch hits MUST appear)
    - Every Section B row contains either `windowed-exempt`, `requires-pinning`, or `anti-pattern` in the Disposition column (verifiable: `grep -c 'windowed-exempt\|requires-pinning\|anti-pattern' 60-CALIBRATION.md` >= 6)
    - The 6 known C11 hit lines all appear in Section B: `grep -c 'patch-management/00-overview.md.*7[7]\|patch-management/00-overview.md.*8[25]\|01-windows-wufb-rings.md.*6[03]\|01-windows-wufb-rings.md.*75' 60-CALIBRATION.md` >= 6
    - No edits to any production file (`docs/**/*.md` or `scripts/**/*.mjs` or `scripts/**/*.json`); pure analysis artifact
  </acceptance_criteria>
  <done>Raw hits enumerated; dispositions assigned; both sections populated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author 60-CALIBRATION.md frontmatter + Summary table + lock dispositions</name>
  <files>.planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md</files>
  <read_first>
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md (frontmatter shape lines 1-8 + Summary table shape lines 139-146 — mirror exactly)
    - .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md (Task 1 output — append frontmatter + Summary)
  </read_first>
  <action>
    Prepend frontmatter to 60-CALIBRATION.md mirroring 48-VERIFICATION-broken-links.md:1-8 shape:
    ```markdown
    ---
    phase: 60-audit-harness-v1-5-finalization
    slug: audit-harness-v1-5-finalization
    generated: 2026-05-06
    calibration_scope: docs/admin-setup-android/**/*.md, docs/operations/**/*.md, docs/android-lifecycle/**/*.md, docs/**/*.md (C11)
    total_c9_hits: <N>
    total_c11_hits_in_window: <N>
    total_c11_hits_requires_pinning: <N>
    total_c11_hits_anti_pattern: 0
    ---

    # Phase 60: Calibration Corpus Scan -- C9 + C11 Pre-Promotion

    > **Lifecycle:** Read-only artifact produced BEFORE Plan 09 atomic harness commit per CONTEXT D-28. Informs c9_exemptions[] pin set + C11 final keyword list.

    ## Header

    - **Phase:** 60
    - **Generated:** 2026-05-06
    - **Calibration tools:** node scripts/validation/v1.5-milestone-audit.mjs (C9 lines 417-437, C11 lines 477-504) + grep-based corpus scan
    - **Scope:** Android + ops-depth corpus for C9; full docs/ for C11
    ```

    Append the Section A + Section B tables (from Task 1) using this exact structure:

    Section A:
    ```markdown
    ## Section A -- C9 cope_banned_phrases corpus scan

    | File | Line | Pattern Match | Disposition | Pin reason (if pinning) |
    |------|------|---------------|-------------|--------------------------|
    | <path> | <line> | <pattern> | <legitimate-disambiguation\|anti-pattern> | <text or em-dash> |
    ```

    Section B:
    ```markdown
    ## Section B -- C11 ops-domain anti-pattern corpus scan with proximity-window evaluation

    | File | Line | Pattern Match | Window Keywords Found | Disposition |
    |------|------|---------------|------------------------|-------------|
    | <path> | <line> | <pattern> | <comma-separated keywords or em-dash> | <windowed-exempt\|requires-pinning\|anti-pattern> |
    ```

    Append Summary table at end:
    ```markdown
    ## Summary

    | Section | Hits | Disposition Breakdown |
    |---------|------|------------------------|
    | A: C9 corpus | <N> | <M> legitimate (-> c9_exemptions[]) / <K> anti-pattern (REGRESSION; must be 0) |
    | B: C11 corpus | <N> | <M> windowed-exempt / <K> requires-pinning (-> c11_ops_exemptions[]) / <J> anti-pattern (REGRESSION; must be 0) |
    | **Total** | **<N+N>** | **<combined breakdown>** |

    ## Plan 09 Inputs

    **c9_exemptions[] seed entries (Plan 09 atomic harness commit):**
    <bulleted list of {file, line, reason} tuples; reason cites Phase 53/54/55/56 commit + PITFALL-13 disambiguation per CONTEXT D-18c>

    **C11 final keyword regex (Plan 09 atomic harness commit):**
    `/successor|deprecated|historical|disambiguation|mutual-exclusion|PITFALL-9|first-occurrence|callout/i`
    <plus any added keywords from Section B requires-pinning analysis>

    **c11_ops_exemptions[] seed entries (Plan 09 atomic harness commit):**
    <bulleted list; if Section B yields 0 requires-pinning entries, this section reads "Reserved-empty per CONTEXT D-02 — initial population = 0 entries.">
    ```

    Pin reason discipline: cite originating Phase + structural justification + PITFALL reference per RESEARCH lines 192-195 (mirror sidecar reason-string format from `c7_knox_allowlist[]` lines 44-53).

    The "anti-pattern" disposition counts MUST be 0 in both Section A and Section B (Phase 49-59 V-NN-NN structural assertions guarantee no regressions). If any "anti-pattern" entries surface, fail the task and report the specific file:line — these would be Phase 49-59 regressions requiring pre-Plan-09 content fix.
  </action>
  <verify>
    <automated>head -10 .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md | grep -c '^phase: 60' | awk '{ if ($1 == 1) print "PASS frontmatter"; else print "FAIL no-frontmatter" }' && grep -c '^## Summary' .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md | awk '{ if ($1 == 1) print "PASS summary"; else print "FAIL no-summary" }' && grep -c 'anti-pattern' .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md | awk '{ print "info anti-pattern-mentions="$1 }' && (grep -E '^\| [^|]+ \| [0-9]+ \|.*\| anti-pattern \|' .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md && echo 'FAIL anti-pattern-rows-found' || echo 'PASS no-anti-pattern-rows')</automated>
  </verify>
  <acceptance_criteria>
    - File starts with YAML frontmatter containing `phase: 60-audit-harness-v1-5-finalization` (line 2)
    - File contains exactly one `## Summary` H2 (`grep -c '^## Summary' 60-CALIBRATION.md == 1`)
    - File contains exactly one `## Plan 09 Inputs` H2 (`grep -c '^## Plan 09 Inputs' 60-CALIBRATION.md == 1`)
    - Zero rows have Disposition cell value `anti-pattern` (regex `\| anti-pattern \|$` returns 0 matches against table-data rows in Section A and Section B). Anti-pattern mentions in prose/headings/keys are OK; only data rows are forbidden.
    - Plan 09 Inputs section explicitly names c9_exemptions[] seed count and C11 final keyword regex
    - Frontmatter `total_c11_hits_anti_pattern: 0` (literal value)
  </acceptance_criteria>
  <done>Frontmatter + Summary + Plan 09 Inputs sections all present; zero anti-pattern data rows; ready for Plan 09 consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo->planning artifact | Calibration runs on local docs corpus; output is markdown artifact in `.planning/`. No external trust crossing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01-01 | I (Information Disclosure) | 60-CALIBRATION.md row content | accept | Hits cite docs/**/*.md line text already in repo; no new disclosure. |
| T-60-01-02 | T (Tampering) | grep-based scan output | mitigate | Use exact regex from sidecar `cope_banned_phrases[]` + harness `c11_ops_patterns` literals; no manual hit-list editing post-scan. |
| T-60-01-03 | D (DoS via regex) | C9 `[^.]*` greedy regex | accept | Risk LOW — corpus scan is one-shot read-only; Phase 60 atomic harness commit (Plan 09) will optionally tighten regex per RESEARCH HIGH-confidence claim if false-positive rate excessive. |
</threat_model>

<verification>
- `test -f .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md`
- File has frontmatter + Section A + Section B + Summary + Plan 09 Inputs
- Zero `anti-pattern` data rows (regression-guard satisfied)
- No edits to production files (`docs/**`, `scripts/**`); pure analysis artifact
- Pre-commit gate: `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 (regression-guard — calibration is read-only and must not perturb harness state)
</verification>

<success_criteria>
- 60-CALIBRATION.md exists with two sections (A: C9, B: C11) + Summary + Plan 09 Inputs
- All 6 known C11 hits enumerated in Section B with proximity-window evaluation
- Zero anti-pattern data rows (Phase 49-59 V-NN-NN no-regression confirmation)
- c9_exemptions[] seed list ready for Plan 09 atomic commit consumption
- C11 final keyword list confirmed (D-01 starting set OR refined per Section B evidence)
</success_criteria>

<output>
After completion, create `.planning/phases/60-audit-harness-v1-5-finalization/60-01-SUMMARY.md`
</output>
