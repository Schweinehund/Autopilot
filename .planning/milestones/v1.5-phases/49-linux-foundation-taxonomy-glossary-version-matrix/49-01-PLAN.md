---
phase: 49-linux-foundation-taxonomy-glossary-version-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/linux-lifecycle/00-enrollment-overview.md
autonomous: true
requirements:
  - LIN-01
  - LIN-02
user_setup: []

must_haves:
  truths:
    - "An admin landing on docs/linux-lifecycle/00-enrollment-overview.md sees a locked '## Supported Management Surface' H2 with a 3-status capability table whose status cells are restricted to the closed set {Supported, Partial, Not supported}"
    - "The Conditional Access row in the capability table reads exactly 'Not supported — web-app CA only' per PITFALL-2 line 48 mandate"
    - "The '## Out of Scope for Linux via Intune' H2 is a peer-level H2 (not nested) immediately following the whitelist H2, satisfying SC#1 'equally prominent' contract"
    - "The '## Enrollment Constraints' H2 contains exactly one H3 '### BYOD vs Corporate-Owned Caveat' with a '> Known caveat' (warning emoji) blockquote surfacing the Microsoft Learn enrollment-framing inconsistency per LIN-01 1c FOLD"
    - "The '## For Admins Familiar with Windows / macOS / Android' bridge subsection contains 3 platform-analog paragraphs each carrying explicit 'the mapping is partial' framing per Phase 34 D-03 precedent"
  artifacts:
    - path: "docs/linux-lifecycle/00-enrollment-overview.md"
      provides: "PITFALL-7 whitelist gate document defining the canonical Linux capability boundary; cross-referenced by Phase 50/51/52 docs via #supported-management-surface and #out-of-scope-for-linux-via-intune anchors"
      contains:
        - "platform: Linux"
        - "last_verified: 2026-04-26"
        - "review_by: 2026-06-25"
        - "# Linux Device Management Enrollment Overview"
        - "> **Platform gate:**"
        - "## How to Use This Guide"
        - "## Supported Management Surface"
        - "| Capability | Linux Status |"
        - "Not supported — web-app CA only"
        - "## Out of Scope for Linux via Intune"
        - "## Enrollment Constraints"
        - "### BYOD vs Corporate-Owned Caveat"
        - "> ⚠️ Known caveat"
        - "## For Admins Familiar with Windows / macOS / Android"
        - "the mapping is partial"
        - "## See Also"
  key_links:
    - from: "docs/linux-lifecycle/00-enrollment-overview.md"
      to: "docs/_glossary-linux.md"
      via: "Platform gate blockquote near top of file"
      pattern: "_glossary-linux\\.md"
    - from: "docs/linux-lifecycle/00-enrollment-overview.md"
      to: "docs/linux-lifecycle/01-linux-prerequisites.md"
      via: "See Also section markdown link"
      pattern: "01-linux-prerequisites\\.md"
    - from: "docs/linux-lifecycle/00-enrollment-overview.md#supported-management-surface"
      to: "docs/linux-lifecycle/00-enrollment-overview.md#out-of-scope-for-linux-via-intune"
      via: "peer-H2 placement (D-03 'equally prominent' contract)"
      pattern: "## Out of Scope for Linux via Intune"
---

<objective>
Create the PITFALL-7 whitelist gate document `docs/linux-lifecycle/00-enrollment-overview.md` that locks the canonical Linux management surface BEFORE any downstream Linux content phase begins. This plan honors D-01 (3-status capability table), D-02 (closed status string set), D-03 (Out-of-Scope peer H2), D-04 (BYOD caveat in dedicated Enrollment Constraints H3), D-05 + D-06 (cross-platform bridge with partial-mapping discipline), CD-01 (bridge wording within partial-mapping discipline), and CD-03 (600-1000 word target). The file's anchors are load-bearing for Phases 50-58 via DPO-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-CONTEXT.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-RESEARCH.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-PATTERNS.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VALIDATION.md
@docs/android-lifecycle/00-enrollment-overview.md
</context>

<threat_model>
No new attack surface — documentation phase + file-reads-only validator. No user input, no network calls, no auth/secret handling. Existing CI runs validator in isolated sandbox per .github/workflows/audit-harness-v1.5-integrity.yml per Phase 48 D-18.
</threat_model>

<tasks>
<task>
<task_id>49-01-01</task_id>
<read_first>
- docs/android-lifecycle/00-enrollment-overview.md (lines 1-15 for frontmatter/H1/Platform gate pattern)
- 49-PATTERNS.md (NEW FILES - Commit-1 -> 00-enrollment-overview.md section)
- 49-RESEARCH.md (Section 2A frontmatter shape lines 79-89)
- 49-CONTEXT.md (D-04, D-05, CD-03)
</read_first>

<action>
Create the directory `docs/linux-lifecycle/` if it does not yet exist (use `mkdir -p docs/linux-lifecycle`). Then create the file `docs/linux-lifecycle/00-enrollment-overview.md` with this exact opening content (frontmatter + H1 + Platform gate blockquote). Do not add any H2s in this task — those land in subsequent tasks.

Exact frontmatter (lines 1-7, between `---` fences):
```yaml
---
last_verified: 2026-04-26
review_by: 2026-06-25
applies_to: both
audience: all
platform: Linux
---
```

Then a blank line, then the H1 (line 9):
```markdown
# Linux Device Management Enrollment Overview
```

Then a blank line, then the Platform gate blockquote (line 11):
```markdown
> **Platform gate:** This guide covers Microsoft Intune device management for Ubuntu LTS Linux endpoints (22.04 / 24.04). For Windows Autopilot, see [Autopilot Lifecycle Overview](../lifecycle/00-overview.md). For macOS ADE, see [macOS ADE Lifecycle](../macos-lifecycle/00-ade-lifecycle.md). For iOS/iPadOS, see [iOS/iPadOS Enrollment Overview](../ios-lifecycle/00-enrollment-overview.md). For Android Enterprise, see [Android Enrollment Overview](../android-lifecycle/00-enrollment-overview.md). For Linux terminology, see the [Linux Provisioning Glossary](../_glossary-linux.md). For prerequisites and version matrix, see [Linux Intune Client Prerequisites](01-linux-prerequisites.md).
```

End of task content. The file ends with a single trailing newline after the blockquote.
</action>

<acceptance_criteria>
- File `docs/linux-lifecycle/00-enrollment-overview.md` exists (`test -f docs/linux-lifecycle/00-enrollment-overview.md` exits 0)
- Frontmatter line `platform: Linux` is present (`grep -c "^platform: Linux$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- Frontmatter line `last_verified: 2026-04-26` is present (`grep -c "^last_verified: 2026-04-26$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- Frontmatter line `review_by: 2026-06-25` is present (`grep -c "^review_by: 2026-06-25$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- H1 `# Linux Device Management Enrollment Overview` is present exactly once (`grep -c "^# Linux Device Management Enrollment Overview$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- Platform gate blockquote contains the literal string `> **Platform gate:**` (`grep -c "^> \*\*Platform gate:\*\*" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- Platform gate blockquote contains the literal substring `[Linux Provisioning Glossary](../_glossary-linux.md)` (`grep -c "Linux Provisioning Glossary\](../_glossary-linux.md)" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 1)
- No H2 sections exist yet (`grep -c "^## " docs/linux-lifecycle/00-enrollment-overview.md` returns 0)
</acceptance_criteria>
</task>

<task>
<task_id>49-01-02</task_id>
<read_first>
- docs/linux-lifecycle/00-enrollment-overview.md (file from prior task)
- docs/android-lifecycle/00-enrollment-overview.md (How to Use pattern)
- 49-CONTEXT.md (CD-01 audience routing precedent)
</read_first>

<action>
Append the `## How to Use This Guide` H2 audience-routing section to `docs/linux-lifecycle/00-enrollment-overview.md` after the Platform gate blockquote. This section provides L1 / L2 / Admin tier routing per Phase 34 D-04 precedent. Append exactly this content:

```markdown

## How to Use This Guide

This document is the canonical capability gate for Linux device management in Microsoft Intune. Read it as follows based on your role:

- **Admins (planning Linux rollout):** Start at [Supported Management Surface](#supported-management-surface) to confirm what Linux can and cannot do via Intune; then read [Out of Scope for Linux via Intune](#out-of-scope-for-linux-via-intune) for explicit exclusions; then proceed to [Linux Intune Client Prerequisites](01-linux-prerequisites.md) for distro / kernel / hardware gates.
- **L2 engineers (investigating Linux failures):** Read [Enrollment Constraints](#enrollment-constraints) for the BYOD vs corporate-owned framing caveat; cross-reference the [Linux Provisioning Glossary](../_glossary-linux.md) for terminology used in L2 runbooks.
- **L1 responders (triaging Linux tickets):** Skim [Supported Management Surface](#supported-management-surface) for the high-level capability table; defer detailed troubleshooting to L1 runbooks shipped in Phase 51.
- **Admins coming from Windows / macOS / Android:** Read [For Admins Familiar with Windows / macOS / Android](#for-admins-familiar-with-windows--macos--android) for cross-platform mental-model bridges, but treat each bridge as a partial mapping only — Linux's capability surface is genuinely narrower than the other four platforms.
```
</action>

<acceptance_criteria>
- H2 `## How to Use This Guide` exists exactly once (`grep -c "^## How to Use This Guide$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- Section contains all 4 audience markers (`grep -c "Admins (planning Linux rollout)" docs/linux-lifecycle/00-enrollment-overview.md` returns 1; `grep -c "L2 engineers" docs/linux-lifecycle/00-enrollment-overview.md` returns 1; `grep -c "L1 responders" docs/linux-lifecycle/00-enrollment-overview.md` returns 1; `grep -c "Admins coming from Windows / macOS / Android" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- Section contains internal anchor link `(#supported-management-surface)` (`grep -c "(#supported-management-surface)" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 2)
</acceptance_criteria>
</task>

<task>
<task_id>49-01-03</task_id>
<read_first>
- docs/linux-lifecycle/00-enrollment-overview.md (file from prior tasks)
- 49-CONTEXT.md (D-01, D-02, CDI-01)
- 49-RESEARCH.md (Section 2A Supported Management Surface table lines 120-138)
- 49-PATTERNS.md (Adaptation Notes 1-2 for the 00-enrollment-overview.md section)
</read_first>

<action>
Append the `## Supported Management Surface` H2 with a 3-status capability table per D-01 + D-02. The table header MUST be exactly `| Capability | Linux Status |`. Every status cell MUST start with one of the closed set strings: `Supported`, `Partial`, or `Not supported`. The Conditional Access row MUST read exactly `Not supported — web-app CA only` per PITFALL-2 line 48 mandate.

Append exactly this content (note the leading blank line):

```markdown

## Supported Management Surface

The following table is the canonical Linux capability whitelist. Status cells use a closed 3-string set: **Supported**, **Partial**, or **Not supported**. Cells that warrant additional locked phrasing carry the qualifier inline (e.g., the Conditional Access row reads `Not supported — web-app CA only` per the PITFALL-2 architectural callout requirement).

| Capability | Linux Status |
|---|---|
| Device enrollment (user-initiated via `intune-portal`) | Supported |
| Compliance policies (4 settings-catalog categories) | Partial |
| Conditional Access — device-level | Not supported — web-app CA only |
| App delivery — Bash custom scripts | Supported |
| App delivery — binary packages (deb / snap / Win32 analog) | Not supported |
| Configuration profiles (settings catalog beyond compliance) | Not supported |
| Declarative Device Management (DDM) | Not supported |
| Zero-touch enrollment (Autopilot / ADE / ZTE analog) | Not supported |
| Hybrid Entra Join | Not supported |
| Certificate profiles (SCEP / PKCS) | Not supported |
| Per-app VPN | Not supported |
| OS update enforcement (DDM / WUfB analog) | Not supported |
| Hardware attestation (TPM / DeviceCheck analog) | Not supported |

**Status string contract:** This 3-status set (`Supported` / `Partial` / `Not supported`) is the canonical contract inherited by `docs/reference/linux-capability-matrix.md` (Phase 50 LIN-13). No additional status strings may be introduced downstream — `check-phase-49.mjs` enforces the closed set per CDI-01.
```
</action>

<acceptance_criteria>
- H2 `## Supported Management Surface` exists exactly once (`grep -c "^## Supported Management Surface$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- Capability table header row exists exactly (`grep -c "^| Capability | Linux Status |$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- The Conditional Access row uses the exact locked phrasing (`grep -c "Not supported — web-app CA only" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 1)
- All status cells use only the 3 canonical strings (manual scan: every non-header table row's status cell starts with `Supported`, `Partial`, or `Not supported`); validated automatically by V-49-07 in Phase 49-04
- The status string contract paragraph references CDI-01 (`grep -c "CDI-01" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 1)
</acceptance_criteria>
</task>

<task>
<task_id>49-01-04</task_id>
<read_first>
- docs/linux-lifecycle/00-enrollment-overview.md (file from prior tasks)
- 49-CONTEXT.md (D-03 Out-of-Scope peer H2 contract)
- .planning/REQUIREMENTS.md (Out of Scope lines 104-117)
- 49-RESEARCH.md (Section 2A Out-of-Scope content sources line 116)
</read_first>

<action>
Append the `## Out of Scope for Linux via Intune` H2 as a PEER to the Supported Management Surface H2 (NOT nested under it) per D-03 + SC#1 'equally prominent' contract. Use a bulleted explicit-exclusion list drawn from REQUIREMENTS.md §Out of Scope lines 104-117 and STACK.md "What NOT to Build" guidance.

Append exactly this content (leading blank line is required):

```markdown

## Out of Scope for Linux via Intune

The following capabilities are explicitly NOT in scope for Linux device management in Microsoft Intune. This list is the inverse of [Supported Management Surface](#supported-management-surface) — these items are documented as exclusions so admins do not search for them in downstream Phase 50-52 content.

- **Non-Ubuntu distributions (RHEL / Rocky / Alma / Debian / SUSE / Fedora):** Ubuntu LTS only per project scope; RHEL 9/10 deferred to v1.6+.
- **Ubuntu Server / IoT / headless devices:** The Microsoft Intune Linux client requires GNOME graphical desktop + interactive sign-in; headless workloads are unsupported.
- **App delivery via deb / snap packages:** Intune does not deliver `.deb` packages; app delivery is Bash-script-based only. There is no Win32 / MSI / MSIX / `.pkg` analog for Linux.
- **Configuration profiles beyond compliance:** Linux has no MDM configuration profile concept in Intune. Compliance policies + Bash custom scripts are the only configuration mechanisms.
- **Snap-based Intune agent delivery:** The agent is delivered as `intune-portal` deb from `packages.microsoft.com`. There is no snap distribution.
- **Device-level Conditional Access:** The "Require device to be marked as compliant" CA grant is not available for Linux. Only web-app CA via Microsoft Edge for Linux is supported (see the Supported Management Surface CA row).
- **Zero-touch enrollment / Autopilot / ADE / Android ZTE analog:** No reseller-driven pre-provisioning surface exists for Linux. Enrollment is exclusively user-initiated.
- **Hybrid Entra Join:** Linux does not participate in Hybrid Entra Join. Devices are Entra-joined directly via the `intune-portal` enrollment flow.
- **Hardware attestation (TPM / DeviceCheck analog):** No hardware-backed attestation surface for Linux device compliance.
- **Certificate profiles (SCEP / PKCS):** Linux compliance does not consume certificate-deployment profiles.
- **OS update enforcement:** No DDM / WUfB / managed-update analog. OS patching is host-managed (`apt`).
- **Localization / non-English content:** Consistent with project-wide English-only policy.
```
</action>

<acceptance_criteria>
- H2 `## Out of Scope for Linux via Intune` exists exactly once (`grep -c "^## Out of Scope for Linux via Intune$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- The H2 is a PEER (top-level `## `) — not nested under Supported Management Surface (verified by line-order: the H2 appears at column 0 with prefix `## `, not `### `)
- The bulleted list contains at least 10 explicit exclusion bullets (`grep -c "^- \*\*" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 10)
- Specific exclusions present: `Non-Ubuntu distributions` (`grep -c "Non-Ubuntu distributions" docs/linux-lifecycle/00-enrollment-overview.md` returns 1); `Snap-based Intune agent delivery` (`grep -c "Snap-based Intune agent delivery" docs/linux-lifecycle/00-enrollment-overview.md` returns 1); `Hybrid Entra Join` (`grep -c "Hybrid Entra Join" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 1)
</acceptance_criteria>
</task>

<task>
<task_id>49-01-05</task_id>
<read_first>
- docs/linux-lifecycle/00-enrollment-overview.md (file from prior tasks)
- 49-CONTEXT.md (D-04 BYOD caveat in Enrollment Constraints H3 — explicitly NOT inside whitelist H2)
- .planning/REQUIREMENTS.md (LIN-01 line 24 — `> ⚠️ Known caveat` literal wording)
- 49-RESEARCH.md (Section 2A Enrollment Constraints content sources)
</read_first>

<action>
Append the `## Enrollment Constraints` H2 with a single H3 `### BYOD vs Corporate-Owned Caveat` containing a `> ⚠️ Known caveat` blockquote per D-04 + LIN-01 1c FOLD wording. The caveat surfaces the Microsoft Learn enrollment-framing inconsistency.

Append exactly this content (leading blank line is required):

```markdown

## Enrollment Constraints

Linux enrollment in Intune is exclusively user-initiated via the `intune-portal` GUI on Ubuntu LTS desktops. There is no zero-touch / pre-provisioning / corporate-owned automation pathway. The capability table above (Supported Management Surface) is capability-pure; the conceptual constraint about ownership-model framing lives here in its own H2 because it is a documentation-and-policy concern rather than a capability gap.

### BYOD vs Corporate-Owned Caveat

> ⚠️ **Known caveat:** Microsoft Learn documentation for Linux device management uses the BYOD vs corporate-owned framing inconsistently across pages. Some Microsoft Learn pages describe Linux Intune enrollment as a BYOD path; other pages describe it as appropriate for corporate-owned Linux workstations. **In practice, both ownership models share the same enrollment mechanism** (user-initiated `intune-portal` install + sign-in). There is no API, MDM payload, or compliance setting that distinguishes a BYOD Linux device from a corporate-owned Linux device in Intune. Admins designing CA / compliance / asset-tracking flows should treat all Linux enrollments as a single ownership category and rely on Entra group membership, device tagging, or upstream procurement records for ownership classification — not on Intune's enrollment-time framing.

This caveat is a documentation-framing artifact, not a product behavior gap. The capability whitelist in [Supported Management Surface](#supported-management-surface) is the authoritative scope; this H3 records the framing inconsistency so admins do not architect compliance policies that assume Intune knows the difference.
```
</action>

<acceptance_criteria>
- H2 `## Enrollment Constraints` exists exactly once (`grep -c "^## Enrollment Constraints$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- H3 `### BYOD vs Corporate-Owned Caveat` exists exactly once (`grep -c "^### BYOD vs Corporate-Owned Caveat$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- The `> ⚠️ Known caveat` blockquote is present (`grep -c "^> ⚠️ \*\*Known caveat:\*\*" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- The blockquote references "Microsoft Learn" (`grep -c "Microsoft Learn" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 1)
- The blockquote contains the BYOD vs corporate-owned framing language (`grep -c "BYOD vs corporate-owned" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 1)
</acceptance_criteria>
</task>

<task>
<task_id>49-01-06</task_id>
<read_first>
- docs/linux-lifecycle/00-enrollment-overview.md (file from prior tasks)
- docs/android-lifecycle/00-enrollment-overview.md (lines 49-54 — bridge subsection precedent)
- 49-CONTEXT.md (D-05, D-06 — bridge scope + forbidden patterns + required template; CD-01)
- 49-RESEARCH.md (Section 2A bridge content guidance lines 140-146)
- 49-PATTERNS.md (Adaptation Notes 4 for 00-enrollment-overview.md)
</read_first>

<action>
Append the `## For Admins Familiar with Windows / macOS / Android` cross-platform bridge subsection per D-05 + D-06. Each platform-analog paragraph MUST contain the explicit "the mapping is partial" framing AND deflect to the whitelist for canonical Linux scope. Forbidden patterns (must NOT appear): "Linux CA is similar to iOS CA"; "Linux app delivery works like Win32".

Required pattern per D-06: "[Concept] on Linux behaves like [closest analog] in [narrow respect], but [specific divergence]; see [Supported Management Surface](#supported-management-surface) for the canonical Linux scope."

Append exactly this content (leading blank line required):

```markdown

## For Admins Familiar with Windows / macOS / Android

Cross-platform mental-model bridges below help admins onboarding from a different platform locate where Linux resembles their existing experience. Each bridge is a **partial mapping only** — Linux's capability surface is narrower than the other four platforms in concrete, structural ways. Treat the analogies as orientation cues, not feature-parity claims.

**For Windows admins:** Enrollment on Linux behaves like Windows BYOD MDM enrollment in the narrow respect that the user signs in to a portal app to bind the device, but the mapping is partial — there is no Autopilot / hardware-hash zero-touch path, no Group Policy / settings-catalog configuration profiles beyond compliance, and Conditional Access for Linux is web-app-only (no device-level CA equivalent to the "Require device to be marked as compliant" grant). See [Supported Management Surface](#supported-management-surface) for the canonical Linux scope.

**For macOS admins:** The `intune-portal` agent on Linux behaves like the macOS Intune management extension in the narrow respect that both run as a user-space service brokering device-state signals to Intune, but the mapping is partial — there is no ADE / Bootstrap Token / SCEP analog on Linux, no Setup Assistant / Awaiting Configuration flow, and Linux compliance covers only 4 settings-catalog categories versus macOS's broader settings catalog. See [Supported Management Surface](#supported-management-surface) for the canonical Linux scope.

**For Android admins:** Linux compliance reporting behaves like Android Enterprise compliance reporting in the narrow respect that both produce a compliant / non-compliant device-state signal Intune can consume, but the mapping is partial — there is no DPC concept on Linux, no Managed Google Play app distribution surface, no work-profile container, no dedicated (COSU / kiosk) mode, and no Play Integrity attestation analog. See [Supported Management Surface](#supported-management-surface) for the canonical Linux scope.
```
</action>

<acceptance_criteria>
- H2 `## For Admins Familiar with Windows / macOS / Android` exists exactly once (`grep -c "^## For Admins Familiar with Windows / macOS / Android$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- All 3 platform-analog paragraphs are present (`grep -c "^\*\*For Windows admins:\*\*" docs/linux-lifecycle/00-enrollment-overview.md` returns 1; `grep -c "^\*\*For macOS admins:\*\*" docs/linux-lifecycle/00-enrollment-overview.md` returns 1; `grep -c "^\*\*For Android admins:\*\*" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- Each platform-analog paragraph contains the literal phrase `the mapping is partial` (`grep -c "the mapping is partial" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 3)
- Each platform-analog paragraph deflects to the whitelist (`grep -c "(#supported-management-surface) for the canonical Linux scope" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 3)
- Forbidden D-06 anti-patterns absent: `grep -c "Linux CA is similar to iOS CA" docs/linux-lifecycle/00-enrollment-overview.md` returns 0; `grep -c "Linux app delivery works like Win32" docs/linux-lifecycle/00-enrollment-overview.md` returns 0
</acceptance_criteria>
</task>

<task>
<task_id>49-01-07</task_id>
<read_first>
- docs/linux-lifecycle/00-enrollment-overview.md (file from prior tasks)
- 49-RESEARCH.md (Section 2A H1->H2->H3 hierarchy lines 92-111)
- 49-CONTEXT.md (DPO-03 — Phase 50 must NOT duplicate this bridge subsection)
</read_first>

<action>
Append the `## See Also` H2 closing section with cross-references to the Linux Provisioning Glossary, the prerequisites + version matrix doc, and the sibling platform overviews. Append exactly this content (leading blank line required):

```markdown

## See Also

- [Linux Intune Client Prerequisites](01-linux-prerequisites.md) — Ubuntu version matrix (20.04 / 22.04 / 24.04), GA vs HWE kernel breakpoints, hardware/software prerequisites, Identity Broker non-version breakpoint
- [Linux Provisioning Glossary](../_glossary-linux.md) — ~20 Linux-native terms (`intune-portal`, `microsoft-identity-broker`, `journalctl`, `dm-crypt`, `LUKS`, `HWE kernel`, `GA kernel`, etc.) plus 9 absent-concept callout entries (Supervision, DPC, Work Profile, MGP, ZTE, VPP, Hardware Hash, ABM, COBO/COPE/WPCO)
- [Autopilot Lifecycle Overview](../lifecycle/00-overview.md) — Windows analog
- [macOS ADE Lifecycle](../macos-lifecycle/00-ade-lifecycle.md) — macOS analog
- [iOS/iPadOS Enrollment Overview](../ios-lifecycle/00-enrollment-overview.md) — iOS analog
- [Android Enrollment Overview](../android-lifecycle/00-enrollment-overview.md) — Android analog

> **Note for Phase 50 plan authors (DPO-03):** Do not duplicate the cross-platform bridge subsection above in `docs/admin-setup-linux/00-overview.md`. Phase 50 overview back-links to the Phase 49 anchor [#for-admins-familiar-with-windows--macos--android](#for-admins-familiar-with-windows--macos--android) instead. Anti-Pattern 1 (single-canonical-doc) discipline per Phase 34 D-26.
```
</action>

<acceptance_criteria>
- H2 `## See Also` exists exactly once (`grep -c "^## See Also$" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- See Also section links to all 5 sibling docs: prerequisites (`grep -c "(01-linux-prerequisites.md)" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 2 — one in Platform gate, one in See Also); glossary (`grep -c "(../_glossary-linux.md)" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 2); Windows/macOS/iOS/Android overview links present (`grep -c "(../lifecycle/00-overview.md)" docs/linux-lifecycle/00-enrollment-overview.md` returns at least 1; etc.)
- DPO-03 note for Phase 50 authors is present (`grep -c "Note for Phase 50 plan authors (DPO-03)" docs/linux-lifecycle/00-enrollment-overview.md` returns 1)
- Final file structure has exactly 6 H2 sections (`grep -c "^## " docs/linux-lifecycle/00-enrollment-overview.md` returns 6) — How to Use, Supported Management Surface, Out of Scope, Enrollment Constraints, For Admins Familiar, See Also (6) plus none extra. Re-count: How to Use, Supported, Out of Scope, Enrollment Constraints, For Admins, See Also = 6.
- Final word count is in 600-1500 range (`wc -w docs/linux-lifecycle/00-enrollment-overview.md` returns a value between 600 and 1500)
</acceptance_criteria>
</task>
</tasks>

<verification>
End-of-plan verification (each individual task's acceptance criteria above must pass; the plan-level integration check is):

1. File `docs/linux-lifecycle/00-enrollment-overview.md` exists and contains all 6 mandatory H2 sections in this order: `How to Use This Guide`, `Supported Management Surface`, `Out of Scope for Linux via Intune`, `Enrollment Constraints`, `For Admins Familiar with Windows / macOS / Android`, `See Also` (verified via `grep -n "^## " docs/linux-lifecycle/00-enrollment-overview.md`)
2. The 4 LIN-01 / SC#1 mandatory structural elements are all present and verifiable: 3-status capability table with Conditional Access cell reading exactly `Not supported — web-app CA only`; Out of Scope peer H2; Enrollment Constraints H2 containing BYOD H3 with `> ⚠️ Known caveat` blockquote; cross-platform bridge subsection with 3 partial-mapping paragraphs
3. Plan 49-04 validator (V-49-02 through V-49-08) will assert all of the above structural patterns once it ships in Wave 2; Plan 49-01 deliverable is structurally validator-ready
4. Frontmatter `platform: Linux` + 60-day `last_verified` (2026-04-26) + `review_by` (2026-06-25) are all present per C10 blocking-check requirement; Phase 49-04 V-49-18 and v1.5-milestone-audit.mjs C10 will both PASS on this file
</verification>
