---
phase: 49-linux-foundation-taxonomy-glossary-version-matrix
plan: 05
type: execute
wave: 3
depends_on: [49-04]
files_modified:
  - docs/_glossary.md
  - docs/_glossary-android.md
  - docs/_glossary-macos.md
  - scripts/validation/v1.5-audit-allowlist.json
  - .planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VERIFICATION.md
autonomous: true
requirements:
  - LIN-01
  - LIN-02
user_setup: []

must_haves:
  truths:
    - "Each of the 3 existing platform glossaries (`docs/_glossary.md`, `docs/_glossary-android.md`, `docs/_glossary-macos.md`) contains the literal reciprocal sentence 'For Linux terminology, see [Linux Provisioning Glossary](_glossary-linux.md).' appended to its top platform-coverage blockquote per D-19 + D-20"
    - "Atomic commit-2 lands the 3 reciprocal append edits + sidecar pin-coordinate refresh (if any pins shifted) in a SINGLE git commit per D-22 + Phase 48 D-14 atomicity-contract precedent (pin-refresh + pinned-file modification MUST be same commit)"
    - "Post-commit-2 verification: `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0; `node scripts/validation/check-phase-49.mjs` (no --skip-reciprocal) exits 0 (all 22 checks PASS); `node scripts/validation/v1.5-milestone-audit.mjs --verbose` shows C1-C10 blocking PASS, C11-C13 informational PASS or accepted-noise"
    - "VERIFICATION.md authored documenting literal whitelist H2 capability table dump + literal version matrix dump (3 rows × 5 cols) + collision audit results (terms scanned + verdicts) per ROADMAP §Phase 49 line 178; serves as the gate Phase 50 plan author reads to confirm Phase 49 closed"
    - "DPO-01..DPO-05 downstream-phase obligations are summarized in VERIFICATION.md so Phase 50 plan author has explicit handoff scope"
  artifacts:
    - path: "docs/_glossary.md"
      provides: "Reciprocal Linux see-also append in top platform-coverage blockquote"
      contains:
        - "[Linux Provisioning Glossary](_glossary-linux.md)"
    - path: "docs/_glossary-android.md"
      provides: "Reciprocal Linux see-also append in top platform-coverage blockquote (line 12)"
      contains:
        - "[Linux Provisioning Glossary](_glossary-linux.md)"
    - path: "docs/_glossary-macos.md"
      provides: "Reciprocal Linux see-also append in top platform-coverage blockquote (line 10)"
      contains:
        - "[Linux Provisioning Glossary](_glossary-linux.md)"
    - path: ".planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VERIFICATION.md"
      provides: "Phase 50 gate document — literal whitelist text + literal matrix dump + collision audit results + DPO-01..DPO-05 handoff summary"
      contains:
        - "# Phase 49 Verification"
        - "## Whitelist H2 Literal Dump"
        - "## Version Matrix Literal Dump"
        - "## Collision Audit Results"
        - "## Downstream Phase Obligations (DPO-01 through DPO-05)"
  key_links:
    - from: "docs/_glossary.md (top blockquote)"
      to: "docs/_glossary-linux.md"
      via: "Reciprocal sentence appended to existing multi-sentence blockquote per D-20 'within existing blockquote' semantic"
      pattern: "\\[Linux Provisioning Glossary\\]\\(_glossary-linux\\.md\\)"
    - from: "docs/_glossary-android.md (top blockquote line 12)"
      to: "docs/_glossary-linux.md"
      via: "Reciprocal sentence appended to existing single-line blockquote per D-19"
      pattern: "\\[Linux Provisioning Glossary\\]\\(_glossary-linux\\.md\\)"
    - from: "docs/_glossary-macos.md (top blockquote line 10)"
      to: "docs/_glossary-linux.md"
      via: "Reciprocal sentence appended to existing multi-line blockquote per D-19"
      pattern: "\\[Linux Provisioning Glossary\\]\\(_glossary-linux\\.md\\)"
---

<objective>
Wave 3 plan that lands commit-2 of the two-commit Phase 49 atomicity split per D-22 + D-27. Appends the reciprocal one-line sentence "For Linux terminology, see [Linux Provisioning Glossary](_glossary-linux.md)." to the top platform-coverage blockquote in each of the 3 existing glossaries (`_glossary.md`, `_glossary-android.md`, `_glossary-macos.md`) per D-19 + D-20. Refreshes pin coordinates in `scripts/validation/v1.5-audit-allowlist.json` if `regenerate-supervision-pins.mjs --report` shows shifts (PITFALL-12 mitigation per Phase 48 D-14 atomicity contract). Authors `49-VERIFICATION.md` documenting the literal whitelist text + literal matrix dump + collision audit results + DPO-01..DPO-05 handoff summary so Phase 50 plan author has the gate-document Phase 50 reads to confirm Phase 49 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-CONTEXT.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-RESEARCH.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-PATTERNS.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VALIDATION.md
@docs/_glossary.md
@docs/_glossary-android.md
@docs/_glossary-macos.md
</context>

<threat_model>
No new attack surface — documentation phase + file-reads-only validator. No user input, no network calls, no auth/secret handling. Existing CI runs validator in isolated sandbox per .github/workflows/audit-harness-v1.5-integrity.yml per Phase 48 D-18.
</threat_model>

<tasks>
<task>
<task_id>49-05-01</task_id>
<read_first>
- docs/_glossary.md (lines 9-11 — current top platform-coverage blockquote)
- 49-CONTEXT.md (D-19, D-20 — append-only contract, within-existing-blockquote semantic)
- 49-PATTERNS.md (EDIT FILES - Commit-2 -> docs/_glossary.md section, lines 629-641)
</read_first>

<action>
Edit `docs/_glossary.md` to append the reciprocal Linux see-also sentence to the existing top platform-coverage blockquote line 11. Use the Edit tool with this exact replacement:

OLD STRING (current line 11 of `docs/_glossary.md`):
```
> For macOS provisioning terminology (ADE, ABM, Setup Assistant), see the [macOS Provisioning Glossary](_glossary-macos.md).
```

NEW STRING:
```
> For macOS provisioning terminology (ADE, ABM, Setup Assistant), see the [macOS Provisioning Glossary](_glossary-macos.md). For Linux terminology, see [Linux Provisioning Glossary](_glossary-linux.md).
```

The append adds the sentence to the END of the existing `> ` line per D-20 (within existing blockquote, not as a new `>` continuation line). This keeps the line count of `_glossary.md` unchanged. Pin-coordinate risk is LOW per RESEARCH.md (no known supervision_exemptions pinned for `_glossary.md`).
</action>

<acceptance_criteria>
- The corrected line exists exactly (`grep -c "macOS Provisioning Glossary\](_glossary-macos.md). For Linux terminology, see \[Linux Provisioning Glossary\](_glossary-linux.md)" docs/_glossary.md` returns 1)
- The literal reciprocal link `[Linux Provisioning Glossary](_glossary-linux.md)` is present (`grep -c "\[Linux Provisioning Glossary\](_glossary-linux.md)" docs/_glossary.md` returns at least 1)
- The append is on the SAME line as the macOS glossary reference (not a new `>` line) — verify with line context: `grep -n "macOS Provisioning Glossary\](_glossary-macos.md). For Linux" docs/_glossary.md` returns 1 match
- Total line count of `_glossary.md` is unchanged from pre-edit (no new lines inserted)
</acceptance_criteria>
</task>

<task>
<task_id>49-05-02</task_id>
<read_first>
- docs/_glossary-android.md (line 12 — current top platform-coverage blockquote)
- 49-CONTEXT.md (D-19, D-20)
- 49-PATTERNS.md (EDIT FILES - Commit-2 -> docs/_glossary-android.md section, lines 643-651)
</read_first>

<action>
Edit `docs/_glossary-android.md` to append the reciprocal Linux see-also sentence to the existing single-line top platform-coverage blockquote on line 12. Use the Edit tool with this exact replacement:

OLD STRING (current line 12 of `docs/_glossary-android.md`):
```
> **Platform coverage:** This glossary covers Android Enterprise provisioning and management terminology for Intune-managed Android devices. For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md). For Apple-platform terminology (macOS, iOS/iPadOS), see the [Apple Provisioning Glossary](_glossary-macos.md).
```

NEW STRING:
```
> **Platform coverage:** This glossary covers Android Enterprise provisioning and management terminology for Intune-managed Android devices. For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md). For Apple-platform terminology (macOS, iOS/iPadOS), see the [Apple Provisioning Glossary](_glossary-macos.md). For Linux terminology, see [Linux Provisioning Glossary](_glossary-linux.md).
```

The append keeps the blockquote on a single line (`>` line still has same line number 12), preserving line counts for ALL subsequent lines in the file. This is HIGH pin-coordinate risk per RESEARCH.md Section 4 — the `_glossary-android.md` file has supervision_exemptions pinned in `v1.5-audit-allowlist.json`. Pin coordinates only shift if the edit causes the line to wrap into a new `>` continuation line; keeping the append within the existing single line preserves line numbers.
</action>

<acceptance_criteria>
- The corrected line exists exactly (`grep -c "Apple Provisioning Glossary\](_glossary-macos.md). For Linux terminology, see \[Linux Provisioning Glossary\](_glossary-linux.md)" docs/_glossary-android.md` returns 1)
- The literal reciprocal link is present (`grep -c "\[Linux Provisioning Glossary\](_glossary-linux.md)" docs/_glossary-android.md` returns at least 1)
- The append is on the SAME line (line 12) — `sed -n '12p' docs/_glossary-android.md` (or equivalent line-12 read) returns the corrected line in full
- Total line count of `_glossary-android.md` is unchanged from pre-edit
</acceptance_criteria>
</task>

<task>
<task_id>49-05-03</task_id>
<read_first>
- docs/_glossary-macos.md (lines 9-10 — current top platform-coverage blockquote)
- 49-CONTEXT.md (D-19, D-20)
- 49-PATTERNS.md (EDIT FILES - Commit-2 -> docs/_glossary-macos.md section, lines 655-666)
</read_first>

<action>
Edit `docs/_glossary-macos.md` to append the reciprocal Linux see-also sentence to the existing top platform-coverage blockquote line 10. Use the Edit tool with this exact replacement:

OLD STRING (current line 10 of `docs/_glossary-macos.md`):
```
> For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md). For Android Enterprise terminology, see the [Android Enterprise Provisioning Glossary](_glossary-android.md).
```

NEW STRING:
```
> For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md). For Android Enterprise terminology, see the [Android Enterprise Provisioning Glossary](_glossary-android.md). For Linux terminology, see [Linux Provisioning Glossary](_glossary-linux.md).
```

The append adds the sentence to the END of the existing `> ` line per D-20. Pin-coordinate risk: MEDIUM — `_glossary-macos.md` may have supervision_exemptions pinned. Verified by `regenerate-supervision-pins.mjs --report` in next task.
</action>

<acceptance_criteria>
- The corrected line exists exactly (`grep -c "Android Enterprise Provisioning Glossary\](_glossary-android.md). For Linux terminology, see \[Linux Provisioning Glossary\](_glossary-linux.md)" docs/_glossary-macos.md` returns 1)
- The literal reciprocal link is present (`grep -c "\[Linux Provisioning Glossary\](_glossary-linux.md)" docs/_glossary-macos.md` returns at least 1)
- The append is on the SAME line (line 10), not a new `>` continuation
- Total line count of `_glossary-macos.md` is unchanged from pre-edit
</acceptance_criteria>
</task>

<task>
<task_id>49-05-04</task_id>
<read_first>
- scripts/validation/regenerate-supervision-pins.mjs (full file — understand --report mode output)
- scripts/validation/v1.5-audit-allowlist.json (current sidecar; identify supervision_exemptions[] entries for the 3 modified glossaries)
- 49-CONTEXT.md (D-22 commit-2 atomicity; PITFALL-12 pin-coord stability)
- 49-PATTERNS.md (Pin Coordinate Refresh Workflow section, lines 683-720)
</read_first>

<action>
Run the pin-coordinate report after the 3 reciprocal edits (tasks 49-05-01..03) have been made but BEFORE staging the commit. This is the PITFALL-12 mitigation step per D-22 + Phase 48 D-14 atomicity contract.

Execute:
```
node scripts/validation/regenerate-supervision-pins.mjs --report
```

Capture the stdout output. Inspect for messages of the form `pin at line X does not match term at line Y` for any of the 3 glossary files (`_glossary.md`, `_glossary-android.md`, `_glossary-macos.md`).

**Decision logic:**

1. If `--report` shows NO shifted pins for any of the 3 glossaries: no sidecar edit is needed. Proceed to commit task (49-05-05) with the 3 glossary edits only.

2. If `--report` shows SHIFTED pins (any pin coordinate referenced by the sidecar JSON does not match the actual file state): edit `scripts/validation/v1.5-audit-allowlist.json` to update the affected `supervision_exemptions[]` line numbers. The expected shift is +1 per inserted line for any pin that was below the modified line. Per RESEARCH.md Section 4, the at-risk pins for `_glossary-android.md` are at lines 76, 78, 172, 188, 16, 46, 66 — verify these against the actual sidecar file (lazy-load: only update what `--report` flagged).

3. Document the report output (whether shifts were detected or not) in the next task's commit message body so the audit trail is preserved.

If the executor edited the sidecar in step 2, re-run `--report` to verify the shifts are now resolved (pins match file state). Re-run iteratively until `--report` is clean.

**Note:** This task does NOT commit anything. Staging + committing happens in task 49-05-05 atomically with the 3 glossary edits AND any sidecar update.
</action>

<acceptance_criteria>
- Command `node scripts/validation/regenerate-supervision-pins.mjs --report` ran successfully and produced output
- The report output is captured (either shown to the user or saved to a temporary file for the commit-message body)
- If the report flagged shifted pins for `_glossary-android.md` or `_glossary-macos.md`: the corresponding `supervision_exemptions[]` entries in `scripts/validation/v1.5-audit-allowlist.json` were updated, and a re-run of `--report` shows no remaining shifts
- If the report flagged no shifts: the sidecar `v1.5-audit-allowlist.json` is unchanged (verified by `git diff scripts/validation/v1.5-audit-allowlist.json` showing no changes)
- The decision (sidecar edited / sidecar unchanged) is documented for inclusion in the commit-2 message body in task 49-05-05
</acceptance_criteria>
</task>

<task>
<task_id>49-05-05</task_id>
<read_first>
- 49-CONTEXT.md (D-22 commit-2 atomicity contract; D-27 sequence)
- .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-CONTEXT.md (D-14 atomic pin-refresh contract)
</read_first>

<action>
Stage and commit commit-2 atomically per D-22 + Phase 48 D-14. The commit MUST include the 3 reciprocal append edits from tasks 49-05-01/02/03 AND any sidecar update from task 49-05-04 IF pins shifted (otherwise sidecar is unchanged and not staged).

Run these commands in sequence:

1. Stage the 3 glossary edits:
   ```
   git add docs/_glossary.md docs/_glossary-android.md docs/_glossary-macos.md
   ```

2. If task 49-05-04 modified `v1.5-audit-allowlist.json` (pin coordinates shifted), also stage it:
   ```
   git add scripts/validation/v1.5-audit-allowlist.json
   ```

3. Commit with this exact message via heredoc (the commit-message body should reference whether sidecar was modified or not based on task 49-05-04 outcome):

```
git commit -m "$(cat <<'EOF'
docs(49): reciprocal Linux see-also appends + pin refresh (commit-2/2)

Ships Phase 49 commit-2 of 2 per D-22 atomic-split contract:
- docs/_glossary.md: appended 'For Linux terminology, see [Linux Provisioning Glossary](_glossary-linux.md).' to top platform-coverage blockquote per D-19+D-20
- docs/_glossary-android.md: same append on line 12 platform-coverage blockquote
- docs/_glossary-macos.md: same append on line 10 platform-coverage blockquote
- scripts/validation/v1.5-audit-allowlist.json: pin coordinate refresh (if applicable per regenerate-supervision-pins.mjs --report)

Atomicity rationale (Phase 48 D-14 + D-22): pin-coord refresh + pinned-file modifications MUST land in the same commit, otherwise --self-test FAILs between commits. Reciprocal appends use the within-existing-blockquote semantic per D-20 — no new > continuation lines, line counts preserved where possible.

Verification: regenerate-supervision-pins.mjs --self-test exits 0 post-commit; check-phase-49.mjs (no --skip-reciprocal) all 22 checks PASS; v1.5-milestone-audit.mjs C1-C10 PASS, C11-C13 informational PASS or accepted noise.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

4. Verify commit landed: `git log --oneline -1` shows commit subject `docs(49): reciprocal Linux see-also appends + pin refresh (commit-2/2)`.
</action>

<acceptance_criteria>
- Commit landed with subject `docs(49): reciprocal Linux see-also appends + pin refresh (commit-2/2)` (`git log --oneline -1` confirms)
- `git show --stat HEAD` lists at minimum: docs/_glossary.md, docs/_glossary-android.md, docs/_glossary-macos.md (and v1.5-audit-allowlist.json if sidecar edited)
- Working tree clean for the staged files (`git status` shows no remaining changes for the 3 glossaries or sidecar)
- Two commits now exist for Phase 49 (commit-1 from Plan 49-04 + commit-2 from this task): `git log --oneline -2` shows both Phase 49 commits in reverse chronological order
</acceptance_criteria>
</task>

<task>
<task_id>49-05-06</task_id>
<read_first>
- scripts/validation/regenerate-supervision-pins.mjs (verify `--self-test` mode behavior)
- scripts/validation/check-phase-49.mjs (final validator from Plan 49-04)
- 49-CONTEXT.md (D-27 terminal sanity)
- 49-VALIDATION.md (Sampling Rate — after commit-2)
</read_first>

<action>
Run the post-commit-2 verification suite per D-27 step 3. These are READ-ONLY assertions confirming the commit landed correctly and the Phase 49 deliverables are now structurally complete.

Execute these commands in order, asserting expected exit codes:

1. `node scripts/validation/regenerate-supervision-pins.mjs --self-test` → expect exit 0 (pin coordinates match file state; no drift between sidecar and pinned files)

2. `node scripts/validation/check-phase-49.mjs` (no --skip-reciprocal flag) → expect exit 0 (all 22 checks PASS, including V-49-20..V-49-22 reciprocal append checks)

3. `node scripts/validation/v1.5-milestone-audit.mjs --verbose` → expect exit 0 (C1-C10 blocking PASS; C11-C13 informational PASS or within accepted-noise tolerance per Phase 48 D-06 + D-08)

Capture all stdout output (especially V-49-NN PASS/FAIL detail lines from check 2 and C1-C13 status lines from check 3) for inclusion in VERIFICATION.md (next task).

If any of the 3 commands exits non-zero, investigate the failure root cause:
- `--self-test` FAIL → rerun `--report` to identify pin shifts; sidecar may need additional updates (a 3rd commit would violate atomicity contract — coordinate with D-22 and consider the failure as a Phase 49 regression that requires planning decision)
- `check-phase-49.mjs` V-49-20..22 FAIL → reciprocal append string did not land correctly; inspect the relevant glossary file's top blockquote
- `v1.5-milestone-audit.mjs` C10 FAIL → frontmatter regression on a Linux file; inspect the failed file
</action>

<acceptance_criteria>
- `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0
- `node scripts/validation/check-phase-49.mjs` (no flags) exits 0; stdout includes 22 PASS lines and 0 FAIL lines (verify via line-count of "PASS" vs "FAIL" in stdout)
- `node scripts/validation/v1.5-milestone-audit.mjs --verbose` exits 0; stdout includes C10 PASS line
- All 22 V-49-NN checks reported PASS (including V-49-20, V-49-21, V-49-22 reciprocal append assertions which require commit-2 to have landed)
- Output captured for VERIFICATION.md authoring
</acceptance_criteria>
</task>

<task>
<task_id>49-05-07</task_id>
<read_first>
- docs/linux-lifecycle/00-enrollment-overview.md (READ FULL FILE; specifically locate the `## Supported Management Surface` H2 section — copy lines from the H2 header through the end of the `**Status string contract:**` paragraph, which corresponds to the action block authored in 49-01-PLAN.md task 49-01-03 lines ~177-198)
- docs/linux-lifecycle/01-linux-prerequisites.md (READ FULL FILE; specifically locate the `## Supported Ubuntu Versions` H2 section — copy lines from the H2 header through the `**EOS Date column uses pinned-event labels**` paragraph, which corresponds to 49-02-PLAN.md task 49-02-02 lines ~149-164)
- docs/_glossary-linux.md (READ FULL FILE; specifically the Alphabetical Index line authored in 49-03-PLAN.md task 49-03-02 line 188 enumerates all 30 terms; the 21 native terms in topical categories are: APT repository, deb (package format), dm-crypt, dpkg, GA kernel, GNOME desktop, HWE kernel, Identity Broker, intune-agent.timer, intune-portal (package), journalctl, Linux compliance settings, LUKS, microsoft-identity-broker, MS Edge for Linux, packages.microsoft.com, systemd, Ubuntu LTS, /var/log/dpkg.log, /var/log/intune-update.log, Web-app CA)
- 49-CONTEXT.md (D-27 step 4 — VERIFICATION.md authoring; DPO-01 through DPO-05)
- ROADMAP.md §Phase 49 line 178 (VERIFICATION.md required content)
- Captured stdout from task 49-05-06 step 2 (`node scripts/validation/check-phase-49.mjs` full output for the Validator Run Output section)
- Captured stdout from task 49-05-06 step 3 (`node scripts/validation/v1.5-milestone-audit.mjs --verbose` C1-C13 lines for the v1.5 Milestone Audit Output section)
- `git log --oneline -2` output (commit hashes for commit-1 and commit-2 to substitute into the Atomic Commit Verification section)
</read_first>

<action>
Author `.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VERIFICATION.md` per ROADMAP §Phase 49 line 178 mandate ("VERIFICATION.md documents whitelist H2 content, version matrix rows, and collision audit results BEFORE Phase 50 begins"). This is the gate document Phase 50 plan author reads to confirm Phase 49 closed.

Create the file with this exact structure. Substitute the bracketed `<<...>>` placeholders with literal content from the read_first sources (specific line ranges + commands above). Use exact copy-paste from shipped files for the H2 capability table and version matrix dumps — do NOT paraphrase.

```markdown
---
phase: 49-linux-foundation-taxonomy-glossary-version-matrix
status: verified
verified_date: 2026-04-26
verifier: gsd-verifier (Phase 49 commit-2 post-execution)
---

# Phase 49 Verification

This document is the gate Phase 50 plan author reads BEFORE beginning Phase 50 (admin-setup-linux). It captures the literal content of the 3 Phase 49 deliverables in their post-commit state and summarizes the DPO-01 through DPO-05 downstream-phase obligations for the Phase 50 plan author.

## Whitelist H2 Literal Dump

Source: `docs/linux-lifecycle/00-enrollment-overview.md` `## Supported Management Surface` H2

<<COPY-PASTE-LITERAL: the entire `## Supported Management Surface` H2 section from the shipped file, starting with the `## Supported Management Surface` line and ending with the closing line of the `**Status string contract:**` paragraph. This includes the table header `| Capability | Linux Status |`, the table separator row, and all 13 capability rows authored in 49-01-PLAN.md task 49-01-03. Do NOT paraphrase; do NOT abbreviate; preserve exact whitespace.>>

## Version Matrix Literal Dump

Source: `docs/linux-lifecycle/01-linux-prerequisites.md` `## Supported Ubuntu Versions` H2

<<COPY-PASTE-LITERAL: the entire `## Supported Ubuntu Versions` H2 section from the shipped file, starting with the `## Supported Ubuntu Versions` line and ending with the closing line of the `**EOS Date column uses pinned-event labels**` paragraph. This includes the header row `| Version | GA Kernel | HWE Kernel | Support Status | EOS Date |`, the table separator row, the 3 distro rows (Ubuntu 24.04 LTS, Ubuntu 22.04 LTS, Ubuntu 20.04 LTS), and the `[^1]:` footnote definition. Do NOT paraphrase.>>

## Collision Audit Results

Source: `node scripts/validation/check-phase-49.mjs` V-49-19 PITFALL-5 collision audit

**Linux-native terms scanned (21 terms in topical categories — Distro & Lifecycle, Agent & Service, Compliance & Encryption, Operations & Diagnostics):**

For each term below, fill in the verdict by inspecting the shipped `docs/_glossary-linux.md` per V-49-19 logic (term collides with sibling glossary if any of `_glossary.md`, `_glossary-macos.md`, `_glossary-android.md` contain a case-insensitive H3 of the same name; HAS-NOTE if a `> **Cross-platform note:**` blockquote appears within 5 lines after the H3). Replace each `<<verdict>>` with one of: `COLLISION-DETECTED-HAS-NOTE`, `NO-COLLISION`, or `COLLISION-DETECTED-MISSING-NOTE` (the last is a FAIL state and should NOT occur if V-49-19 PASSED).

- APT repository — <<verdict>>
- deb (package format) — <<verdict>>
- dm-crypt — <<verdict>>
- dpkg — <<verdict>>
- GA kernel — <<verdict>>
- GNOME desktop — <<verdict>>
- HWE kernel — <<verdict>>
- Identity Broker — <<verdict>>
- intune-agent.timer — <<verdict>>
- intune-portal (package) — <<verdict>>
- journalctl — <<verdict>>
- Linux compliance settings — <<verdict>>
- LUKS — <<verdict>>
- microsoft-identity-broker — <<verdict>>
- MS Edge for Linux — <<verdict>>
- packages.microsoft.com — <<verdict>>
- systemd — <<verdict>>
- Ubuntu LTS — <<verdict>>
- /var/log/dpkg.log — <<verdict>>
- /var/log/intune-update.log — <<verdict>>
- Web-app CA — <<verdict>>

**Sibling glossaries scanned for collisions:**
- `docs/_glossary.md` (Windows Autopilot Glossary) — <<N>> H3 terms enumerated (count via `grep -c "^### " docs/_glossary.md`)
- `docs/_glossary-macos.md` (Apple Provisioning Glossary) — <<N>> H3 terms enumerated (count via `grep -c "^### " docs/_glossary-macos.md`)
- `docs/_glossary-android.md` (Android Enterprise Provisioning Glossary) — <<N>> H3 terms enumerated (count via `grep -c "^### " docs/_glossary-android.md`)

**Collision audit verdict:** PASS (V-49-19 returned exit 0; all collision-risk Linux-native terms carry a `> **Cross-platform note:**` blockquote within 5 lines of their H3).

## Reciprocal Append Verification

V-49-20, V-49-21, V-49-22 PASS confirmation — each glossary's top platform-coverage blockquote contains the literal reciprocal sentence `For Linux terminology, see [Linux Provisioning Glossary](_glossary-linux.md).`:

- `docs/_glossary.md` — V-49-20: PASS (sentence appended to top blockquote per task 49-05-01)
- `docs/_glossary-android.md` — V-49-21: PASS (sentence appended to line 12 blockquote per task 49-05-02)
- `docs/_glossary-macos.md` — V-49-22: PASS (sentence appended to line 10 blockquote per task 49-05-03)

## Atomic Commit Verification

- Commit-1: `docs(49): foundation gate - whitelist + matrix + glossary + validator (commit-1/2)` — commit hash: <<insert short SHA from `git log --oneline -2` second line>>
- Commit-2: `docs(49): reciprocal Linux see-also appends + pin refresh (commit-2/2)` — commit hash: <<insert short SHA from `git log --oneline -2` first line>>

Both commits landed atomically per D-22 + D-27.

## Validator Run Output

Captured from `node scripts/validation/check-phase-49.mjs` (post-commit-2, no --skip-reciprocal flag). All 22 checks PASS:

```
<<PASTE-LITERAL: full stdout captured in task 49-05-06 step 2; expected 22 PASS lines + final "Summary: 22 passed, 0 failed, 0 skipped" line>>
```

## v1.5 Milestone Audit Output

Captured from `node scripts/validation/v1.5-milestone-audit.mjs --verbose` (post-commit-2). C1-C10 blocking PASS + C11-C13 informational PASS or accepted-noise:

```
<<PASTE-LITERAL: the C1-C13 status lines captured in task 49-05-06 step 3; include the C10 PASS line covering the 3 new Linux files>>
```

## Downstream Phase Obligations (DPO-01 through DPO-05)

The following obligations propagate to downstream Phase 50-59 plan authors. Each obligation references a Phase 49 anchor that downstream content MUST consume rather than re-author.

### DPO-01 — Phase 50 LIN-05 back-link
Phase 50 LIN-05 (Identity Broker pitfall callout in `docs/admin-setup-linux/01-intune-linux-agent.md`) MUST back-link to:
`docs/linux-lifecycle/01-linux-prerequisites.md#non-version-breakpoints` (Identity Broker H4 anchor)
Phase 49 owns the matrix-context anchor; Phase 50 owns the detailed `> ⚠️ Known admin pitfall` callout.

### DPO-02 — Phase 50 LIN-13 inherits 3-status canonical set
Phase 50 LIN-13 (`docs/reference/linux-capability-matrix.md`) MUST use the canonical 3-status string set: `Supported` / `Partial` / `Not supported`. NO 4th status string may be introduced. The Phase 49 validator V-49-07 enforces the closed set; Phase 50 plan author has no freedom to deviate.

### DPO-03 — Phase 50 admin-setup-linux/00-overview.md must NOT duplicate bridge subsection
Phase 50 `docs/admin-setup-linux/00-overview.md` MUST NOT duplicate the cross-platform bridge subsection from Phase 49 `docs/linux-lifecycle/00-enrollment-overview.md#for-admins-familiar-with-windows--macos--android`. Phase 50 overview back-links to that anchor instead. Anti-Pattern 1 (single-canonical-doc) per Phase 34 D-26.

### DPO-04 — Phase 51-52 runbooks reference ~20 Linux-native glossary terms by anchor
Phase 51 (L1 runbooks 30-33) and Phase 52 (L2 runbooks 24-25) reference the Phase 49 ~20 Linux-native glossary terms via anchor (`_glossary-linux.md#term-anchor`). If a runbook needs an additional term not in the Phase 49 set, the runbook author APPENDS to `_glossary-linux.md` per the append-only contract — Phase 49 does not reserve term-coverage exclusivity.

### DPO-05 — Phase 59 CLEAN-08 builds on Phase 49 reciprocal appends
Phase 59 CLEAN-08 (5-platform glossary normalization across Windows + macOS + iOS + Android + Linux) builds on the Phase 49 commit-2 reciprocal appends already present in `docs/_glossary.md`, `docs/_glossary-android.md`, `docs/_glossary-macos.md`. Phase 59 plan author confirms these 3 reciprocal sentences are intact at v1.5 close and adds any additional cross-references identified during normalization.

## Phase 49 Closed

All 5 success criteria from ROADMAP §Phase 49 lines 168-173 satisfied. Both atomic commits landed per D-22 + D-27. Validator + harness exit 0. Phase 50 plan author may begin `/gsd-plan-phase 50`.
```
</action>

<acceptance_criteria>
- File `.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VERIFICATION.md` exists (`test -f .planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VERIFICATION.md` exits 0)
- File contains all 7 mandatory H2 sections: Whitelist H2 Literal Dump, Version Matrix Literal Dump, Collision Audit Results, Reciprocal Append Verification, Atomic Commit Verification, Validator Run Output, v1.5 Milestone Audit Output, Downstream Phase Obligations (DPO-01 through DPO-05) — each `grep -c "^## "` returns at least 7
- The literal whitelist table header `| Capability | Linux Status |` is present in VERIFICATION.md (`grep -c "^| Capability | Linux Status |$" .planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VERIFICATION.md` returns at least 1)
- The literal matrix header `| Version | GA Kernel | HWE Kernel | Support Status | EOS Date |` is present (`grep -c "^| Version | GA Kernel | HWE Kernel | Support Status | EOS Date |$" .planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VERIFICATION.md` returns at least 1)
- All 5 DPO-NN sections present (`grep -c "^### DPO-0[1-5]" .planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VERIFICATION.md` returns 5)
- File ends with the "Phase 49 Closed" sign-off (`grep -c "^## Phase 49 Closed$" .planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VERIFICATION.md` returns 1)
</acceptance_criteria>
</task>
</tasks>

<verification>
End-of-plan verification (each individual task's acceptance criteria above must pass; the plan-level integration check is):

1. All 3 existing glossaries contain the literal reciprocal sentence `For Linux terminology, see [Linux Provisioning Glossary](_glossary-linux.md).` per D-19 + D-20 (verified by V-49-20, V-49-21, V-49-22 PASS)
2. Atomic commit-2 landed with all reciprocal appends + sidecar pin-refresh (if applicable) in single commit per D-22 + Phase 48 D-14 atomicity contract
3. `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0 post-commit-2 (no pin drift between sidecar and pinned files)
4. `node scripts/validation/check-phase-49.mjs` (no flags) exits 0 with all 22 checks PASS
5. `node scripts/validation/v1.5-milestone-audit.mjs --verbose` exits 0 with C1-C10 blocking PASS + C11-C13 informational PASS or accepted-noise per Phase 48 D-06 + D-08
6. `49-VERIFICATION.md` authored with literal whitelist + literal matrix + collision audit results + DPO-01..DPO-05 downstream-phase obligations summary per ROADMAP §Phase 49 line 178; Phase 50 plan author has the gate-document needed to begin Phase 50
7. Phase 49 is closed: 2 atomic commits landed, all 5 ROADMAP SC#1..SC#5 satisfied, Phase 50 unblocked
</verification>
