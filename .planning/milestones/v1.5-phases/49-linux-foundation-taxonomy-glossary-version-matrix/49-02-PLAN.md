---
phase: 49-linux-foundation-taxonomy-glossary-version-matrix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/linux-lifecycle/01-linux-prerequisites.md
autonomous: true
requirements:
  - LIN-01
  - LIN-02
user_setup: []

must_haves:
  truths:
    - "An admin reading docs/linux-lifecycle/01-linux-prerequisites.md sees a Ubuntu version-breakpoint matrix with exactly 3 rows (20.04, 22.04, 24.04) and 5 columns (Version, GA Kernel, HWE Kernel, Support Status, EOS Date) per D-07"
    - "The matrix header row is the literal string '| Version | GA Kernel | HWE Kernel | Support Status | EOS Date |' so the validator V-49-09 can perform an exact-match assertion"
    - "The Ubuntu 20.04 row carries a footnote marker referencing the dedicated '### Ubuntu 20.04 — End-of-Support' H3 below the matrix per D-08"
    - "The '### Non-version Breakpoints' H3 (D-09) anchors the Identity Broker v2.0.2+ re-enrollment behavior so Phase 50 LIN-05 callout in 01-intune-linux-agent.md can back-link to it (DPO-01)"
    - "EOS Date column cells use pinned-event labels ('Intune 2508 - August 2025') not bare dates per D-10 to prevent date drift"
  artifacts:
    - path: "docs/linux-lifecycle/01-linux-prerequisites.md"
      provides: "Canonical Ubuntu version × kernel-track × support-status matrix; downstream Phase 50/52 docs cite back via #non-version-breakpoints and #ubuntu-2004--end-of-support anchors"
      contains:
        - "platform: Linux"
        - "last_verified: 2026-04-26"
        - "review_by: 2026-06-25"
        - "# Linux Intune Client Prerequisites"
        - "> **Platform gate:**"
        - "## Supported Ubuntu Versions"
        - "| Version | GA Kernel | HWE Kernel | Support Status | EOS Date |"
        - "Ubuntu 24.04 LTS"
        - "Ubuntu 22.04 LTS"
        - "Ubuntu 20.04 LTS"
        - "Intune 2508"
        - "### Ubuntu 20.04 — End-of-Support"
        - "### Non-version Breakpoints"
        - "Identity Broker v2.0.2"
        - "## Hardware and Software Prerequisites"
        - "## See Also"
  key_links:
    - from: "docs/linux-lifecycle/01-linux-prerequisites.md#non-version-breakpoints"
      to: "docs/admin-setup-linux/01-intune-linux-agent.md (Phase 50 LIN-05 — DPO-01 back-link target)"
      via: "H3 anchor exposed for Phase 50 to back-link"
      pattern: "### Non-version Breakpoints"
    - from: "docs/linux-lifecycle/01-linux-prerequisites.md"
      to: "docs/_glossary-linux.md"
      via: "Platform gate blockquote and See Also section"
      pattern: "_glossary-linux\\.md"
    - from: "docs/linux-lifecycle/01-linux-prerequisites.md"
      to: "docs/linux-lifecycle/00-enrollment-overview.md"
      via: "See Also back-link"
      pattern: "00-enrollment-overview\\.md"
---

<objective>
Create `docs/linux-lifecycle/01-linux-prerequisites.md` with the canonical 3-row × 5-column Ubuntu version-breakpoint matrix per D-07, the dedicated '### Ubuntu 20.04 — End-of-Support' H3 per D-08 (footnote-marked-row strategy), the '### Non-version Breakpoints' H3 per D-09 (Identity Broker v2.0.2+ re-enrollment anchor for DPO-01), pinned-event EOS labels per D-10, GA/HWE kernel disambiguation per PITFALL-4, and hardware/software prerequisites drawn from STACK.md. Word target: 500-800 (per CD-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-CONTEXT.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-RESEARCH.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-PATTERNS.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VALIDATION.md
@docs/android-lifecycle/03-android-version-matrix.md
@docs/android-lifecycle/01-android-prerequisites.md
</context>

<threat_model>
No new attack surface — documentation phase + file-reads-only validator. No user input, no network calls, no auth/secret handling. Existing CI runs validator in isolated sandbox per .github/workflows/audit-harness-v1.5-integrity.yml per Phase 48 D-18.
</threat_model>

<tasks>
<task>
<task_id>49-02-01</task_id>
<read_first>
- docs/android-lifecycle/03-android-version-matrix.md (lines 1-13 for frontmatter + Platform gate pattern)
- 49-RESEARCH.md (Section 2B frontmatter shape lines 159-168)
- 49-CONTEXT.md (D-07, D-09, D-10)
- 49-PATTERNS.md (NEW FILES - Commit-1 -> 01-linux-prerequisites.md frontmatter excerpt lines 114-134)
</read_first>

<action>
Create `docs/linux-lifecycle/01-linux-prerequisites.md` with frontmatter, H1, and Platform gate blockquote. Exact opening content (the file does not yet exist):

```yaml
---
last_verified: 2026-04-26
review_by: 2026-06-25
applies_to: both
audience: admin
platform: Linux
---
```

Then a blank line, then H1 (line 9):
```markdown
# Linux Intune Client Prerequisites
```

Then a blank line, then Platform gate blockquote (line 11-12):
```markdown
> **Platform gate:** This reference shows the supported Ubuntu LTS versions (22.04 / 24.04), the dropped Ubuntu version (20.04 — Intune 2508 / August 2025 end-of-support), GA vs HWE kernel-track breakpoints, and one non-version breakpoint (Identity Broker v2.0.2+ re-enrollment behavior) that affects all supported Ubuntu versions.
> For enrollment overview and capability whitelist, see [Linux Device Management Enrollment Overview](00-enrollment-overview.md). For Linux terminology, see the [Linux Provisioning Glossary](../_glossary-linux.md).
```

End of task content. The file ends with a single trailing newline after the blockquote.
</action>

<acceptance_criteria>
- File `docs/linux-lifecycle/01-linux-prerequisites.md` exists (`test -f docs/linux-lifecycle/01-linux-prerequisites.md` exits 0)
- Frontmatter `platform: Linux` is present (`grep -c "^platform: Linux$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Frontmatter `audience: admin` is present (`grep -c "^audience: admin$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Frontmatter `last_verified: 2026-04-26` and `review_by: 2026-06-25` both present (each grep returns 1)
- H1 `# Linux Intune Client Prerequisites` is present exactly once (`grep -c "^# Linux Intune Client Prerequisites$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Platform gate blockquote contains literal string `> **Platform gate:**` (`grep -c "^> \*\*Platform gate:\*\*" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Platform gate references Identity Broker v2.0.2+ (`grep -c "Identity Broker v2.0.2" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- Platform gate links to glossary (`grep -c "_glossary-linux.md" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
</acceptance_criteria>
</task>

<task>
<task_id>49-02-02</task_id>
<read_first>
- docs/linux-lifecycle/01-linux-prerequisites.md (file from prior task)
- 49-CONTEXT.md (D-07 matrix shape; D-08 EOS treatment; D-10 pinned-event labels; CD-05 footnote syntax)
- 49-RESEARCH.md (Section 2B Version matrix lines 194-211)
- 49-PATTERNS.md (Adaptation Notes 1-6 for 01-linux-prerequisites.md; Concrete excerpt lines 168-188)
</read_first>

<action>
Append the `## Supported Ubuntu Versions` H2 with the canonical 3-row × 5-column matrix per D-07. The header row MUST be EXACTLY `| Version | GA Kernel | HWE Kernel | Support Status | EOS Date |` (validator V-49-09 performs literal match). Three rows: Ubuntu 24.04 LTS, Ubuntu 22.04 LTS, Ubuntu 20.04 LTS. The 20.04 row carries a footnote marker `[^1]` per D-08 + CD-05; the footnote definition appears below the matrix and points to the `### Ubuntu 20.04 — End-of-Support` H3.

EOS Date column uses pinned-event labels per D-10 (e.g., `Intune 2508 - August 2025`, not bare `August 2025`). GA/HWE kernel cells include inline `[verify-on-current-Ubuntu]` markers per STACK.md research-flags pattern (D-10 MEDIUM-confidence cells).

Append exactly this content (leading blank line required):

```markdown

## Supported Ubuntu Versions

The following matrix locks Ubuntu LTS support scope for Microsoft Intune Linux device management. Three rows are tracked: the two currently-supported versions (22.04 LTS / 24.04 LTS) and the dropped version (20.04 LTS) retained for visibility because admins searching the matrix for "20.04" need to find an explicit dropped-status row, not silence.

| Version | GA Kernel | HWE Kernel | Support Status | EOS Date |
|---|---|---|---|---|
| Ubuntu 24.04 LTS (Noble) | 6.8 [verify-on-current-Ubuntu] | 6.11+ [verify-on-current-Ubuntu] | Supported — x86_64 only | April 2029 (standard) |
| Ubuntu 22.04 LTS (Jammy) | 5.15 [verify-on-current-Ubuntu] | 6.8 [verify-on-current-Ubuntu] | Supported — x86_64 only | April 2027 (standard) |
| Ubuntu 20.04 LTS (Focal) | 5.4 | 5.15 | Dropped — Intune 2508 [^1] | April 2025 (standard) |

[^1]: Ubuntu 20.04 LTS support was removed in the Intune 2508 service release (August 2025). Devices running Ubuntu 20.04 cannot enroll or maintain Intune enrollment. See [Ubuntu 20.04 — End-of-Support](#ubuntu-2004--end-of-support) below for the upgrade path.

**Source confidence:** Ubuntu version supportability HIGH (Microsoft Learn enroll-linux page, last verified 2026-04). GA / HWE kernel-version cells MEDIUM (Ubuntu wiki kernel-version inventory; specific intune-portal kernel minima are not explicitly documented by Microsoft). Cells marked `[verify-on-current-Ubuntu]` carry an inline freshness marker — re-validate on a current Ubuntu install at execution time.

**EOS Date column uses pinned-event labels** ("Intune 2508 — August 2025") rather than bare dates to prevent date-drift surface in admin reading. Bare dates ("August 2025") have lower information density than pinned-event references that name the Intune service release.
```
</action>

<acceptance_criteria>
- H2 `## Supported Ubuntu Versions` exists exactly once (`grep -c "^## Supported Ubuntu Versions$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Matrix header row is exactly `| Version | GA Kernel | HWE Kernel | Support Status | EOS Date |` (`grep -c "^| Version | GA Kernel | HWE Kernel | Support Status | EOS Date |$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Matrix has exactly 3 distro rows (each starting `| Ubuntu`): `grep -c "^| Ubuntu" docs/linux-lifecycle/01-linux-prerequisites.md` returns 3
- Ubuntu 24.04 LTS row present (`grep -c "Ubuntu 24.04 LTS" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- Ubuntu 22.04 LTS row present (`grep -c "Ubuntu 22.04 LTS" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- Ubuntu 20.04 LTS row present and marked Dropped (`grep -c "Dropped — Intune 2508" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Footnote `[^1]` definition exists (`grep -c "^\[^1\]:" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Footnote references "Ubuntu 20.04 — End-of-Support" anchor (`grep -c "(#ubuntu-2004--end-of-support)" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- EOS Date column uses pinned-event label "Intune 2508" (`grep -c "Intune 2508" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- `[verify-on-current-Ubuntu]` markers appear at least once for kernel-version cells (`grep -c "verify-on-current-Ubuntu" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 2)
</acceptance_criteria>
</task>

<task>
<task_id>49-02-03</task_id>
<read_first>
- docs/linux-lifecycle/01-linux-prerequisites.md (file from prior tasks)
- 49-CONTEXT.md (D-08 EOS treatment — H3 below matrix, not strikethrough)
- 49-RESEARCH.md (Section 2B Ubuntu 20.04 EOS H3 lines 213-215)
- 49-PATTERNS.md (Concrete excerpt lines 181-188 — Ubuntu EOS H3 shape)
</read_first>

<action>
Append the `### Ubuntu 20.04 — End-of-Support` H3 immediately below the matrix per D-08. This H3 holds the dropped-version narrative without polluting the supported-version rows. Use exactly this content (leading blank line required):

```markdown

### Ubuntu 20.04 — End-of-Support

Ubuntu 20.04 LTS (Focal) support was dropped in the Intune 2508 service release (August 2025). Devices running Ubuntu 20.04 cannot enroll in Intune nor maintain an existing Intune enrollment after this service release.

**Admin action required:**

- **Audit:** Identify any Ubuntu 20.04 endpoints currently enrolled or attempting enrollment via Intune reporting.
- **Upgrade path:** In-place upgrade is supported from 20.04 → 22.04 via `do-release-upgrade`. Direct upgrade from 20.04 to 24.04 is NOT supported by Ubuntu — go through 22.04 first.
- **Reference:** See [Ubuntu upgrade documentation](https://ubuntu.com/server/docs/upgrade-introduction) for the recommended `do-release-upgrade` procedure.
- **Re-enrollment:** After OS upgrade, re-install `intune-portal` deb from `packages.microsoft.com` and re-enroll via the GUI sign-in flow. The Identity Broker re-enrollment behavior in [Non-version Breakpoints](#non-version-breakpoints) applies post-upgrade if the upgraded `intune-portal` includes Identity Broker v2.0.2+.
```
</action>

<acceptance_criteria>
- H3 `### Ubuntu 20.04 — End-of-Support` exists exactly once (`grep -c "^### Ubuntu 20.04 — End-of-Support$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- H3 references the Intune 2508 service release (`grep -c "Intune 2508 service release" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- Section contains "Admin action required" header (`grep -c "\*\*Admin action required:\*\*" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- Section references the upgrade procedure (`grep -c "do-release-upgrade" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- Section references the upgrade-via-22.04 constraint (`grep -c "go through 22.04 first" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Section back-links to Non-version Breakpoints (`grep -c "(#non-version-breakpoints)" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
</acceptance_criteria>
</task>

<task>
<task_id>49-02-04</task_id>
<read_first>
- docs/linux-lifecycle/01-linux-prerequisites.md (file from prior tasks)
- 49-CONTEXT.md (D-09 Non-version Breakpoints H3 placement; D-15 Identity Broker split-author; DPO-01 Phase 50 LIN-05 back-link target)
- 49-RESEARCH.md (Section 2B Non-version Breakpoints H3 lines 217-220)
- 49-PATTERNS.md (Concrete excerpt lines 156-167 — Non-version Breakpoints H3 shape)
</read_first>

<action>
Append the `### Non-version Breakpoints` H3 per D-09 + D-15. This H3 anchors the Identity Broker v2.0.2+ re-enrollment behavior so Phase 50 LIN-05 callout in `01-intune-linux-agent.md` can back-link to it (DPO-01). Phase 49 owns the matrix-context anchor; Phase 50 owns the detailed `> ⚠️ Known admin pitfall` callout — do NOT duplicate the pitfall callout text here.

Append exactly this content (leading blank line required):

```markdown

### Non-version Breakpoints

The following drift event is NOT gated by Ubuntu LTS version — it is a component-version-gated change in the `intune-portal` package that affects all supported Ubuntu versions identically.

#### Identity Broker v2.0.2+

When the `intune-portal` package updates to include Identity Broker v2.0.2+, the device automatically re-registers with Intune — creating new Intune device IDs and new Microsoft Entra device IDs. Existing device-based Conditional Access assignments, Intune filters, and Entra group memberships that rely on device object IDs continue to point at the *previous* device ID and silently stop applying to the re-registered device.

**Behavior summary:** Single-event re-enrollment that is not visible in the Intune portal as a discrete enrollment action; admins discover it via CA / compliance assignment drift after the fact.

**Source:** Microsoft Learn — Linux deployment guide (verified 2026-04, HIGH confidence).

**Admin action required:** See the [Phase 50 LIN-05 admin pitfall callout](../admin-setup-linux/01-intune-linux-agent.md#identity-broker-v202-re-enrollment) for the step-by-step review checklist (audit CA assignments, filters, Entra group membership; re-target post-re-enrollment device IDs). Phase 49 anchors the breakpoint in this matrix-doc context; Phase 50 owns the detailed admin-action callout.
```
</action>

<acceptance_criteria>
- H3 `### Non-version Breakpoints` exists exactly once (`grep -c "^### Non-version Breakpoints$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- H4 `#### Identity Broker v2.0.2+` exists exactly once (`grep -c "^#### Identity Broker v2.0.2+" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Section explains the re-registration behavior (`grep -c "automatically re-registers" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- Section back-links to Phase 50 LIN-05 callout (`grep -c "../admin-setup-linux/01-intune-linux-agent.md#identity-broker-v202-re-enrollment" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Section explicitly does not duplicate the pitfall callout (no occurrence of `> ⚠️ Known admin pitfall` blockquote — that text belongs to Phase 50). Verify: `grep -c "^> ⚠️ \*\*Known admin pitfall" docs/linux-lifecycle/01-linux-prerequisites.md` returns 0
- Source attribution to Microsoft Learn present (`grep -c "Microsoft Learn — Linux deployment guide" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
</acceptance_criteria>
</task>

<task>
<task_id>49-02-05</task_id>
<read_first>
- docs/linux-lifecycle/01-linux-prerequisites.md (file from prior tasks)
- 49-RESEARCH.md (Section 2B H1->H2->H3 hierarchy lines 170-192; STACK.md hardware requirements references)
- .planning/research/STACK.md (lines 23-35 GNOME / x86_64 / Hyper-V scope)
</read_first>

<action>
Append the `## Hardware and Software Prerequisites` H2 covering GNOME desktop, x86_64 architecture, and supported physical / Azure VM / Hyper-V environments per STACK.md lines 23-35. Append exactly this content (leading blank line required):

```markdown

## Hardware and Software Prerequisites

Beyond the supported Ubuntu LTS versions in the matrix above, the Microsoft Intune Linux client requires the following hardware and software prerequisites. Devices that fail any of these gates cannot enroll.

### Architecture and Hardware

- **CPU architecture:** x86_64 (64-bit Intel / AMD) only. ARM64 / aarch64 is NOT supported.
- **Display environment:** GNOME graphical desktop session. Headless / server-only / IoT installations are NOT supported — `intune-portal` requires interactive GUI sign-in.
- **Supported environments:** Physical hardware, Azure-hosted virtual machines, Hyper-V virtual machines. Other hypervisor hosts are not officially supported (use cases possible but undocumented by Microsoft).

### Required Software Components

- **Microsoft Edge for Linux 102.x or later:** Required for web-app Conditional Access flows. Install via the Microsoft Edge for Linux package from `packages.microsoft.com`.
- **`intune-portal` deb package:** Distributed exclusively via `packages.microsoft.com`. NOT available via snap, NOT available via the default Ubuntu archive. See the [Linux Provisioning Glossary](../_glossary-linux.md#packagesmicrosoftcom) for the package source convention.
- **`microsoft-identity-broker` systemd service:** Installed as a dependency of `intune-portal`. See [Linux Provisioning Glossary — microsoft-identity-broker](../_glossary-linux.md#microsoft-identity-broker).
- **`intune-agent.timer` systemd unit:** User-scoped systemd timer activated post-enrollment. See [Linux Provisioning Glossary — intune-agent.timer](../_glossary-linux.md#intune-agenttimer).

### Networking

- **Outbound HTTPS to Microsoft Intune endpoints:** Required for enrollment, compliance reporting, and Conditional Access evaluation. Specific endpoint list maintained by Microsoft Learn — `last_verified` discipline applies.
- **Outbound HTTPS to `packages.microsoft.com`:** Required for `intune-portal` and Microsoft Edge for Linux installation and updates.
```
</action>

<acceptance_criteria>
- H2 `## Hardware and Software Prerequisites` exists exactly once (`grep -c "^## Hardware and Software Prerequisites$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Architecture / hardware H3 present (`grep -c "^### Architecture and Hardware$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Required Software H3 present (`grep -c "^### Required Software Components$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- Networking H3 present (`grep -c "^### Networking$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- x86_64 constraint stated (`grep -c "x86_64" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- GNOME requirement stated (`grep -c "GNOME graphical desktop session" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- packages.microsoft.com source stated (`grep -c "packages.microsoft.com" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 2)
- Microsoft Edge for Linux 102.x+ requirement stated (`grep -c "Microsoft Edge for Linux 102.x" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
</acceptance_criteria>
</task>

<task>
<task_id>49-02-06</task_id>
<read_first>
- docs/linux-lifecycle/01-linux-prerequisites.md (file from prior tasks)
- 49-RESEARCH.md (Section 2B downstream anchor targets line 222)
</read_first>

<action>
Append the `## See Also` H2 closing section with cross-references back to the enrollment overview, forward to the glossary, and to specific Microsoft Learn source URLs. Append exactly this content (leading blank line required):

```markdown

## See Also

- [Linux Device Management Enrollment Overview](00-enrollment-overview.md) — capability whitelist, Out of Scope exclusions, BYOD vs corporate-owned caveat, cross-platform bridge
- [Linux Provisioning Glossary](../_glossary-linux.md) — terminology including [HWE kernel](../_glossary-linux.md#hwe-kernel), [GA kernel](../_glossary-linux.md#ga-kernel), [Identity Broker](../_glossary-linux.md#identity-broker), [intune-portal (package)](../_glossary-linux.md#intune-portal-package), [packages.microsoft.com](../_glossary-linux.md#packagesmicrosoftcom)
- Microsoft Learn — [Set up enrollment for Linux devices](https://learn.microsoft.com/mem/intune/enrollment/linux-enroll) — authoritative source for enrollment endpoint configuration (apply 60-day `last_verified` discipline)
- Microsoft Learn — [Linux device management overview](https://learn.microsoft.com/mem/intune/fundamentals/deployment-guide-platform-linux) — authoritative source for the Identity Broker v2.0.2+ re-registration behavior cited in [Non-version Breakpoints](#non-version-breakpoints)
```
</action>

<acceptance_criteria>
- H2 `## See Also` exists exactly once (`grep -c "^## See Also$" docs/linux-lifecycle/01-linux-prerequisites.md` returns 1)
- See Also links to enrollment overview (`grep -c "(00-enrollment-overview.md)" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 1)
- See Also links to Linux Provisioning Glossary (`grep -c "Linux Provisioning Glossary" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 2)
- See Also references Microsoft Learn source URLs (`grep -c "learn.microsoft.com" docs/linux-lifecycle/01-linux-prerequisites.md` returns at least 2)
- Final file has exactly 3 H2 sections (`grep -c "^## " docs/linux-lifecycle/01-linux-prerequisites.md` returns 3): Supported Ubuntu Versions, Hardware and Software Prerequisites, See Also (3) — re-count in final file
- File word count is 500-1500 (`wc -w docs/linux-lifecycle/01-linux-prerequisites.md` returns a value between 500 and 1500)
</acceptance_criteria>
</task>
</tasks>

<verification>
End-of-plan verification (each individual task's acceptance criteria above must pass; the plan-level integration check is):

1. File `docs/linux-lifecycle/01-linux-prerequisites.md` exists with frontmatter `platform: Linux` + 60-day `last_verified` (2026-04-26) + `review_by` (2026-06-25)
2. The 5-column matrix header is exactly `| Version | GA Kernel | HWE Kernel | Support Status | EOS Date |` and the matrix has exactly 3 distro rows (20.04, 22.04, 24.04) — V-49-09 + V-49-10 in Plan 49-04 will assert these
3. The 20.04 row is footnote-marked and the `### Ubuntu 20.04 — End-of-Support` H3 appears below the matrix per D-08; V-49-11 will assert H3 presence
4. The `### Non-version Breakpoints` H3 contains the Identity Broker v2.0.2+ #### with back-link to Phase 50 LIN-05 callout per D-09 + DPO-01; V-49-12 will assert H3 presence
5. EOS Date column uses pinned-event labels (`Intune 2508 - August 2025` style) per D-10; the matrix-cell content is checked manually post-execution since validator only checks header literal
6. Hardware/Software Prerequisites H2 contains x86_64 + GNOME + Microsoft Edge for Linux 102.x+ + packages.microsoft.com per STACK.md
</verification>
