---
phase: 51
plan: 05
type: execute
wave: A
depends_on: []
files_modified:
  - docs/l1-runbooks/33-linux-agent-service-failure.md
autonomous: true
requirements: [LIN-11]
requirements_addressed: [LIN-11]

must_haves:
  truths:
    - "docs/l1-runbooks/33-linux-agent-service-failure.md exists with frontmatter (platform: Linux, audience: L1, last_verified 2026-04-27, review_by 2026-06-26)"
    - "Runbook is single-cause (Runbook 22-style) — does NOT contain anchor-indexed Cause A/B/C H2s (D-10 / V-51-15 negative)"
    - "Runbook contains literal H2 `## L1 Triage Steps` (V-51-15 positive)"
    - "Terminal walkthrough across the cause path with `> **Say to the user:**` blockquote (V-51-24 / D-15)"
    - "L1 Triage Steps contain NO sudo prefix on `apt list`, `dpkg -l`, `systemctl --user`, `journalctl --user` (D-13 / V-51-20 read-only — `--user` units NEVER take sudo)"
    - "Sudo prefixes appear only in `## Admin Action Required` H2 — `sudo apt install --reinstall intune-portal` is correct (system-scope)"
    - "Runbook contains at least 1 link to `../_glossary-linux.md#<anchor>` (V-51-23)"
  artifacts:
    - path: "docs/l1-runbooks/33-linux-agent-service-failure.md"
      provides: "L1 single-cause runbook for Linux Intune agent service failure (intune-agent.timer)"
      contains: ["platform: Linux", "audience: L1", "## L1 Triage Steps", "## Admin Action Required", "## Escalation Criteria", "intune-agent.timer", "../_glossary-linux.md#", "> **Say to the user:**"]
  key_links:
    - from: "docs/l1-runbooks/33-linux-agent-service-failure.md"
      to: "docs/l1-runbooks/30-linux-enrollment-failed.md"
      via: "Disambiguation note (route to Runbook 30 if intune-portal deb not installed)"
      pattern: "[Runbook 30: Linux Enrollment Failed](30-linux-enrollment-failed.md)"
    - from: "docs/l1-runbooks/33-linux-agent-service-failure.md"
      to: "docs/admin-setup-linux/01-intune-linux-agent.md"
      via: "Admin reference for canonical service health verification"
      pattern: "[Linux Intune Agent Admin Setup](../admin-setup-linux/01-intune-linux-agent.md)"
---

<objective>
Author `docs/l1-runbooks/33-linux-agent-service-failure.md` — the L1 single-cause runbook for Linux Intune agent service failure (LIN-11). Single-cause Runbook 22-style structure (no anchor-indexed cause decomposition per D-10): single `## L1 Triage Steps` numbered list followed by `## Admin Action Required` and `## Escalation Criteria`. Terminal walkthrough across the cause path (`systemctl --user status intune-agent.timer`, `is-enabled`, `list-timers`, `journalctl --user`) with read-vs-write apt distinction enforced (D-13).

Purpose: Satisfies LIN-11 ("L1 responder can resolve 'Linux Intune agent service not running' symptoms"). Demonstrates the single-cause structural alternative for Phase 52 plan author (DPO-01) — Runbook 33 anchors do NOT exist as `#cause-X-...` per V-51-15 negative assertion.

Output: 1 markdown file at `docs/l1-runbooks/33-linux-agent-service-failure.md` (~120-180 lines) that passes V-51-04 (file existence — counted with the 4 runbooks), V-51-05, V-51-15 (negative cause-H2 + positive L1-Triage-Steps H2), V-51-20, V-51-23, V-51-24, V-51-25 in `check-phase-51.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-linux-l1-triage-runbooks-30-33/51-CONTEXT.md
@.planning/phases/51-linux-l1-triage-runbooks-30-33/51-RESEARCH.md
@.planning/phases/51-linux-l1-triage-runbooks-30-33/51-PATTERNS.md
@docs/l1-runbooks/22-android-enrollment-blocked.md
@docs/admin-setup-linux/01-intune-linux-agent.md
@docs/_glossary-linux.md

<interfaces>
The runbook consumes:
- Phase 50 admin file (cross-reference): `../admin-setup-linux/01-intune-linux-agent.md` for canonical service health verification (referenced at line 89-91 of admin file: `systemctl --user status intune-agent.timer`)
- Phase 49 glossary anchors:
  - `../_glossary-linux.md#intune-agenttimer` (period stripped — verified slug per 51-RESEARCH.md line 412)
  - `../_glossary-linux.md#systemd`
  - `../_glossary-linux.md#journalctl`
  - `../_glossary-linux.md#intune-portal-package`
  - `../_glossary-linux.md#dpkg`

Cross-runbook routing:
- Disambiguation note: if `intune-portal` deb is NOT installed at all, route to Runbook 30 (Cause A package install)

Critical scope rule (D-13 / CDI-Phase51-05):
- `intune-agent.timer` is **user-scope** — `systemctl --user` commands NEVER take sudo. V-51-20 enforces this negatively.
- `apt install --reinstall intune-portal` is system-scope — sudo prefix is correct here, in `## Admin Action Required` H2 ONLY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/l1-runbooks/33-linux-agent-service-failure.md (single-cause Runbook 22-style L1 runbook)</name>
  <files>docs/l1-runbooks/33-linux-agent-service-failure.md</files>
  <read_first>
    - docs/l1-runbooks/22-android-enrollment-blocked.md (PRIMARY analog — single-cause shape; lines 1-49 frontmatter + intro + L1 Triage Steps; lines 51-69 Admin Action Required; lines 71-91 Escalation Criteria)
    - docs/admin-setup-linux/01-intune-linux-agent.md (verify line 89-91 `systemctl --user status intune-agent.timer` — Phase 50 canonical service health command; reference for admin-action remediation)
    - docs/_glossary-linux.md (verify anchor slugs `#intune-agenttimer` line 80, `#systemd` line 96, `#journalctl` line 116, `#intune-portal-package` line 86, `#dpkg` line 70)
    - .planning/phases/51-linux-l1-triage-runbooks-30-33/51-PATTERNS.md (Section E — paste-ready single-cause structure)
    - .planning/phases/51-linux-l1-triage-runbooks-30-33/51-RESEARCH.md (Linux Diagnostic Commands table lines 270-274 — pin Runbook 33 commands; Implementation Risk #7 systemd output format)
    - .planning/phases/51-linux-l1-triage-runbooks-30-33/51-CONTEXT.md (D-10 / D-13 / D-15 / D-16 / CDI-Phase51-05)
  </read_first>
  <behavior>
    - File exists at `docs/l1-runbooks/33-linux-agent-service-failure.md`
    - Frontmatter contains `platform: Linux`, `audience: L1`, `last_verified: 2026-04-27`, `review_by: 2026-06-26`, `applies_to: all`
    - Platform-gate blockquote at top references all 4 sibling L1 platforms
    - Single-cause shape — file does NOT contain `## Cause A:` / `## Cause B:` / `## Cause C:` / `## Cause D:` H2s (V-51-15 negative)
    - File CONTAINS literal H2 `## L1 Triage Steps` (V-51-15 positive)
    - L1 Triage Steps numbered list with at least 1 `> **Say to the user:**` blockquote
    - L1 Triage Steps contain NO `sudo systemctl --user`, `sudo apt list`, `sudo dpkg -l`, `sudo journalctl --user` patterns
    - `## Admin Action Required` H2 contains state-changing remediation (sudo OK in this section for system-scope `sudo apt install --reinstall intune-portal`)
    - `## Escalation Criteria` H2 with 3-part escalation packet
    - At least 1 `../_glossary-linux.md#<anchor>` link
  </behavior>
  <action>
Create `docs/l1-runbooks/33-linux-agent-service-failure.md` with the following structure (single-cause Runbook 22-style).

**1. Frontmatter (paste verbatim):**
```
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: all
audience: L1
platform: Linux
---
```

**2. Platform-gate blockquote (paste verbatim):**
```
> **Platform gate:** This guide covers Linux Intune client troubleshooting (Ubuntu 22.04/24.04 LTS). For Windows Autopilot, see [Windows L1 Runbooks](00-index.md#apv1-runbooks). For macOS ADE, see [macOS ADE Runbooks](00-index.md#macos-ade-runbooks). For iOS/iPadOS, see [iOS L1 Runbooks](00-index.md#ios-l1-runbooks). For Android, see [Android L1 Runbooks](00-index.md#android-l1-runbooks).
```

**3. H1 + Symptom H2 + intro + Disambiguation (paste verbatim — note this is single-cause; NO `## Cause A:` / `## Cause B:` H2s):**
```
# Linux Intune Agent Service Failure

L1 runbook for Linux endpoints (Ubuntu 22.04/24.04 LTS) where the Intune agent service ([`intune-agent.timer`](../_glossary-linux.md#intune-agenttimer)) is not running or not checking in. The [`intune-portal`](../_glossary-linux.md#intune-portal-package) deb package may still be installed; this runbook covers the case where the package is present but the [systemd](../_glossary-linux.md#systemd) timer is inactive, failed, or never firing.

## Symptom

One or more of the following:

- User reports their Linux device "stopped checking in" or "fell off Intune"
- Admin-visible in Intune: device last-check-in timestamp is more than 24 hours old despite the user reporting the device is powered on
- `systemctl --user status intune-agent.timer` shows `Active: inactive (dead)` or `Active: failed` instead of `Active: active (waiting)`
- Common ticket phrasings: "my device hasn't checked in," "Intune says my device is offline but I'm using it now," "the agent is broken."

Routed here from the [Linux Triage Decision Tree](../decision-trees/09-linux-triage.md) LINR33 branch.

> **Disambiguation:** If the device never enrolled (no `intune-portal` deb installed at all), see [Runbook 30: Linux Enrollment Failed](30-linux-enrollment-failed.md) instead. If the timer is running but compliance evaluation is reporting non-compliant, see [Runbook 31: Linux Compliance Non-Compliant](31-linux-compliance-non-compliant.md).
```

**4. L1 Triage Steps H2 (paste verbatim — NO sudo on `systemctl --user` or `journalctl --user` or `apt list`):**
```
## L1 Triage Steps

L1 Triage Steps are read-only checks. L1 does NOT modify any service state — that is an admin action (see [Admin Action Required](#admin-action-required) below).

1. > **Say to the user:** "Let me check whether the Intune agent timer is running on your device. Please open Terminal and type: `systemctl --user status intune-agent.timer`. Read me the output."

2. If the output shows `Active: active (waiting)` and `Loaded: loaded`, the timer is healthy — escalate to a different runbook (the symptom may be misclassified). See [Disambiguation](#symptom) above.

3. If the output shows `Active: inactive (dead)` or `Active: failed`, ask the user to also run `systemctl --user is-enabled intune-agent.timer` and read me the output. Expected: `enabled` (timer set to start at boot) or `static` (timer wired to another service). If `disabled`, the timer was explicitly stopped — admin action required.

4. > **Say to the user:** "Now please type `systemctl --user list-timers intune-agent.timer` and read me the NEXT column."

   If the column shows `-`, the timer has no scheduled next-run — confirms the failure pattern. (Per systemd 245+ output format; Ubuntu 22.04 ships systemd 249, Ubuntu 24.04 ships 255.)

5. Collect [`journalctl --user -u intune-agent.timer --since "1 hour ago"`](../_glossary-linux.md#journalctl) output (or `--since "1 day ago"` for broader scope). Document any failure messages or the absence of expected check-in messages.

6. Confirm the `intune-portal` package is still installed: `apt list --installed | grep intune-portal`. If empty (deb removed), the upstream cause is package removal — route to [Runbook 30](30-linux-enrollment-failed.md) instead.

7. Collect the following observed state for the escalation packet:
   - Output of `systemctl --user status intune-agent.timer`
   - Output of `systemctl --user is-enabled intune-agent.timer`
   - Output of `systemctl --user list-timers intune-agent.timer`
   - Output of `journalctl --user -u intune-agent.timer --since "1 day ago"`
   - User's UPN
   - Device serial number
   - Distro + version (`lsb_release -a`) + kernel (`uname -r`)
```

**5. Admin Action Required H2 (paste verbatim — sudo IS correct here for system-scope `apt install --reinstall`):**
```
## Admin Action Required

L1 documents and hands this packet to the Intune administrator. L1 does not execute any of the following actions.

**Ask the admin to:**

- Restart the user-scope timer: `systemctl --user start intune-agent.timer` (NO sudo — `--user` units do not take sudo). If the timer is `static` or wired to another unit, the admin should restart that parent unit instead.
- If the timer is in a `failed` state, run `systemctl --user reset-failed intune-agent.timer` and then `systemctl --user start intune-agent.timer`.
- If the upstream cause is package corruption: `sudo apt install --reinstall intune-portal` (this DOES use sudo because `apt` is system-scope) and then verify the timer with `systemctl --user is-active intune-agent.timer` — expected `active`.
- For broader troubleshooting, see [Linux Intune Agent Admin Setup](../admin-setup-linux/01-intune-linux-agent.md) for the canonical service health verification procedure.

**Verify:**

- After admin actions: `systemctl --user is-active intune-agent.timer` returns `active`. Within approximately 30 minutes the device should appear back in Intune admin center > Devices > All devices with an updated last-check-in timestamp.

**If the admin confirms none of the above applies:**

- Proceed to [Escalation Criteria](#escalation-criteria).
```

**6. Escalation Criteria H2 + Version History (paste verbatim):**
```
## Escalation Criteria

Escalate to L2 (per Phase 30 D-12 three-part escalation packet). See [Phase 52 L2 Linux Agent Investigation] (forthcoming).

Escalate to L2 if:

- Timer restarts but immediately re-enters `failed` state within minutes
- Timer is `active (waiting)` but no check-in occurs within 60 minutes (the timer is firing but the agent is not communicating with Intune)
- Reinstall succeeds but the timer is still inactive after reinstall
- Multiple users on the same device model show the same failure pattern (likely an OS image / OEM regression — L2 investigation required)

**Before escalating, collect:**

- Device serial number
- Distro + version (`lsb_release -a`) + kernel (`uname -r`)
- User UPN
- Output of `systemctl --user status intune-agent.timer` (immediately after the failed restart attempt)
- Output of `journalctl --user -u intune-agent.timer --since "1 day ago"`
- `dpkg -l intune-portal` output
- Timestamp of the last successful check-in (from Intune admin center)
- Timestamp of the failed restart attempt

---

[Back to Linux Triage](../decision-trees/09-linux-triage.md)

## Version History

| Date | Change | Author |
|------|--------|--------|
| 2026-04-27 | Initial version (Phase 51 — Linux Intune agent service failure single-cause L1 runbook) | -- |
```

CRITICAL — DO NOT include any of the following anchor-indexed cause H2s anywhere in the file: `## Cause A:`, `## Cause B:`, `## Cause C:`, `## Cause D:`. V-51-15 enforces single-cause shape (negative regression-guard).

DO NOT include any literal `sudo systemctl --user`, `sudo apt list`, `sudo dpkg -l`, `sudo journalctl --user` patterns in L1 Triage Steps. The `--user` scope NEVER takes sudo per D-13 / V-51-20.

DO NOT modify any other file in this plan.

DO NOT commit yet — atomic commit owned by 51-08.
  </action>
  <verify>
    <automated>test -f docs/l1-runbooks/33-linux-agent-service-failure.md && grep -cE "^## Cause [A-Z]:" docs/l1-runbooks/33-linux-agent-service-failure.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/l1-runbooks/33-linux-agent-service-failure.md`
    - Frontmatter contains `platform: Linux`, `audience: L1`, `last_verified: 2026-04-27`, `review_by: 2026-06-26` (V-51-05)
    - File does NOT contain any line matching `^## Cause [A-Z]:` (V-51-15 negative — single-cause shape)
    - File contains exactly one line matching `^## L1 Triage Steps\s*$` (V-51-15 positive)
    - File contains `## Admin Action Required` H2
    - File contains `## Escalation Criteria` H2 with literal "Before escalating, collect:" text
    - File contains at least 1 link matching `\.\./_glossary-linux\.md#[a-z0-9-]+` (V-51-23)
    - File contains `> **Say to the user:**` blockquote at least once (V-51-24)
    - L1 Triage Steps section does NOT contain `sudo systemctl --user`, `sudo apt list`, `sudo dpkg -l`, `sudo journalctl --user` patterns (V-51-20)
    - File does NOT contain `TBD`, `TODO`, `FIXME`, `XXX` placeholders (V-51-25)
    - File contains link `[Runbook 30: Linux Enrollment Failed](30-linux-enrollment-failed.md)` (disambiguation)
    - File contains `[Back to Linux Triage](../decision-trees/09-linux-triage.md)` back-link
  </acceptance_criteria>
  <done>33-linux-agent-service-failure.md exists with single-cause Runbook 22-style structure, terminal walkthrough across the cause path with `> **Say to the user:**` blockquotes, NO anchor-indexed cause H2s (V-51-15 negative), NO sudo on user-scope commands (V-51-20), Admin Action Required + Escalation Criteria, glossary cross-links. No commit yet.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-51-13 | Tampering (regression) | Runbook 33 single-cause shape | mitigate | V-51-15 enforces NEGATIVE regex `^## Cause [A-Z]:` (must be absent) AND POSITIVE `^## L1 Triage Steps\s*$` (must be present). Drift toward anchor-indexed multi-cause shape would fail SC#5. |
| T-51-14 | Information disclosure (false elevation) | sudo on `systemctl --user` | mitigate | V-51-20 enforces. `--user` units NEVER take sudo per D-13 + 51-RESEARCH.md command table line 276. |
| T-51-15 | Tampering (PITFALL-12 sidecar pin coords) | Runbook 33 file is NOT in supervision_exemptions[] | accept | Runbook 33 is a brand-new file in this commit — no pin coords exist for it. PITFALL-12 motivation does not apply per CONTEXT D-22. |

No application security threats.
</threat_model>

<verification>
After task completes:

```bash
test -f docs/l1-runbooks/33-linux-agent-service-failure.md
grep -cE "^## Cause [A-Z]:" docs/l1-runbooks/33-linux-agent-service-failure.md  # expect 0
grep -cE "^## L1 Triage Steps\\s*$" docs/l1-runbooks/33-linux-agent-service-failure.md  # expect 1
grep -c "../_glossary-linux.md#" docs/l1-runbooks/33-linux-agent-service-failure.md  # expect >=1
grep -c "> \*\*Say to the user:\*\*" docs/l1-runbooks/33-linux-agent-service-failure.md  # expect >=1
# Verify no sudo on user-scope L1 commands:
grep -nE "sudo (systemctl --user|apt list|dpkg -l|journalctl --user)" docs/l1-runbooks/33-linux-agent-service-failure.md && echo "FAIL" || echo "PASS"
```

If `check-phase-51.mjs` exists, run:
```bash
node scripts/validation/check-phase-51.mjs --verbose
# expect V-51-04 / V-51-05 / V-51-15 / V-51-20 / V-51-23 / V-51-24 / V-51-25 PASS
```

DO NOT commit yet — atomic commit owned by 51-08.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 51 Success Criteria:
- **SC#5 (frontmatter on 60-day cycle, platform: Linux, audience: L1):** ✅ Frontmatter block
- **LIN-11:** ✅ Single-cause runbook with terminal-walkthrough triage steps for `intune-agent.timer` failure

Plan complete when:
- [ ] File exists at `docs/l1-runbooks/33-linux-agent-service-failure.md`
- [ ] All acceptance criteria pass (especially V-51-15 negative + positive)
- [ ] No sudo on user-scope commands
- [ ] No commit yet — 51-08 owns atomicity
</success_criteria>

<output>
After completion, create `.planning/phases/51-linux-l1-triage-runbooks-30-33/51-05-SUMMARY.md` documenting:
- File path + line count
- Single-cause shape verified (no `## Cause [A-Z]:` H2s; one `## L1 Triage Steps` H2)
- Terminal-walkthrough actor-boundary verified (no sudo on user-scope commands)
- Glossary anchor consumption count
- Disambiguation cross-references to Runbook 30/31
- Note: NO COMMIT — atomic commit owned by 51-08
</output>
