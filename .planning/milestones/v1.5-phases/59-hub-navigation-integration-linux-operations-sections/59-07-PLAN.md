---
phase: 59
plan: 59-07
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - docs/quick-ref-l2.md
files_created: []
requirements: [CLEAN-08]
autonomous: true

must_haves:
  truths:
    - "L2 engineer reading `docs/quick-ref-l2.md` sees a `## Linux Quick Reference` H2 with 4 sub-H3 sections matching iOS/Android four-part substructure verbatim per ROADMAP SC#4 (D-21 / D-23)"
    - "Sub-H3 substructure LOCKED: `### Linux Diagnostic Data Collection (3 methods)` (3-row table) / `### Key Intune Portal Paths (Linux L2)` (3-5 row table) / `### Linux Compliance Category Reference` (4-row pointer table) / `### Linux Investigation Runbooks` (2-link list)"
    - "Linux Diagnostic Data Collection 3-method H3 contains 3 verbatim method names: `journalctl (systemd journal)`, `File-based paths`, `Package-state queries` (D-23; pin per V-59-29)"
    - "Linux Compliance Category Reference 4-row pointer table contains 4 category literals: `Allowed Distributions`, `Custom Compliance`, `Device Encryption`, `Password Policy` (D-23 + REQUIREMENTS:27 LIN-04 + admin-setup-linux/03-compliance-policy.md frontmatter line 9 — authoritative count)"
    - "Linux Compliance Category Reference 4 cross-link references to `admin-setup-linux/03-compliance-policy.md` step anchors: `#step-2-configure-allowed-distributions`, `#step-3-configure-custom-compliance-bash-discovery-scripts`, `#step-4-configure-device-encryption-dm-crypt-luks`, `#step-5-configure-password-policy` (V-59-30)"
    - "PITFALL_7_FIREWALL truth satisfied: Linux Compliance Category Reference H3 body contains NO Bash script syntax (no `if`/`then`/`fi`/`exit 0`/`exit 1`/`#!/bin/bash`); NO `compliance-evaluation-cadence` literal; NO per-OS-version distro-version-table content (D-24 + V-59-31 NEGATIVE)"
    - "Linux Investigation Runbooks list contains 2 entries to L2 runbooks 24, 25 with iOS-style ` -- ` disambiguation per row (D-23; thin but unavoidable per finite Linux L2 surface)"
    - "QUICK_REF_LINUX_PRESENT phase-level truth (L2 portion) satisfied"
    - "New `## Linux Quick Reference` H2 appended after the existing `## Android Enterprise Quick Reference` H2 (lines 233-281) — append-only contract per Phase 57 D-31 + Phase 59 D-11 inheritance"
    - "Existing 4 platform Quick Reference H2s in quick-ref-l2.md (Windows / macOS / iOS / Android) byte-identical post-edit (V-59-32 NEGATIVE regression-guard); Android Play Integrity Verdict Reference at lines 261-271 unchanged"

  artifacts:
    - path: "docs/quick-ref-l2.md"
      provides: "L2 quick-reference card with Linux H2 (4-part substructure: Linux Diagnostic Data Collection 3 methods / Key Intune Portal Paths Linux L2 / Linux Compliance Category Reference 4-row pointer table / Linux Investigation Runbooks 2-link list)"
      contains:
        - "## Linux Quick Reference"
        - "### Linux Diagnostic Data Collection (3 methods)"
        - "### Key Intune Portal Paths (Linux L2)"
        - "### Linux Compliance Category Reference"
        - "### Linux Investigation Runbooks"
        - "journalctl (systemd journal)"
        - "File-based paths"
        - "Package-state queries"
        - "Allowed Distributions"
        - "Custom Compliance"
        - "Device Encryption"
        - "Password Policy"
        - "../admin-setup-linux/03-compliance-policy.md#step-2-configure-allowed-distributions"
        - "../admin-setup-linux/03-compliance-policy.md#step-3-configure-custom-compliance-bash-discovery-scripts"
        - "../admin-setup-linux/03-compliance-policy.md#step-4-configure-device-encryption-dm-crypt-luks"
        - "../admin-setup-linux/03-compliance-policy.md#step-5-configure-password-policy"
        - "l2-runbooks/24-linux-log-collection.md"
        - "l2-runbooks/25-linux-agent-investigation.md"

  key_links:
    - from: "docs/quick-ref-l2.md `### Linux Compliance Category Reference` H3"
      to: "docs/admin-setup-linux/03-compliance-policy.md (Phase 50 LIN-04 SSoT — 4 settings-catalog categories)"
      via: "4-row pointer table; one row per category with cross-link to specific step anchor"
      pattern: "admin-setup-linux/03-compliance-policy\\.md#step-[2-5]-"
    - from: "docs/quick-ref-l2.md `### Linux Diagnostic Data Collection (3 methods)` H3"
      to: "docs/l2-runbooks/24-linux-log-collection.md (Phase 52 deliverable — Decision Matrix lines 30-36)"
      via: "3-row table mirroring 24-linux-log-collection.md Decision Matrix"
      pattern: "l2-runbooks/24-linux-log-collection\\.md"
    - from: "docs/quick-ref-l2.md `### Linux Investigation Runbooks` H3"
      to: "docs/l2-runbooks/24 + 25 (Phase 52 deliverables)"
      via: "2-link list with ` -- ` disambiguation"
      pattern: "l2-runbooks/(24-linux-log-collection|25-linux-agent-investigation)\\.md"
---

<objective>
Append a net-new `## Linux Quick Reference` H2 section to `docs/quick-ref-l2.md` after the existing `## Android Enterprise Quick Reference` H2 (currently lines 233-281). The new H2 contains 4 sub-H3 sections (Linux Diagnostic Data Collection 3 methods / Key Intune Portal Paths Linux L2 / Linux Compliance Category Reference / Linux Investigation Runbooks) matching iOS/Android four-part substructure with PITFALL-7-compliant Compliance Category Reference pointer-table mirroring Android Play Integrity Verdict Reference shape per Phase 57 D-22..D-25 + Phase 59 D-23 ENDORSEMENT.

Plan 59-07 is Wave 2; depends on 59-01 (pre-edit anchor inventory must exist); ships D-21 / D-23 / D-24 / D-25 mandates.

Purpose: Implements ROADMAP Phase 59 SC#4 (quick-ref-l2.md contains Linux quick-reference section matching iOS/Android structural depth) — the QUICK_REF_LINUX_PRESENT phase-level truth (L2 portion) + the PITFALL_7_FIREWALL phase-level truth. Implements V-59-28 + V-59-29 + V-59-30 + V-59-31 validator targets.

Output: 1 modified file (`docs/quick-ref-l2.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-ANCHOR-INVENTORY.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
**Files this plan modifies:**
- MODIFY: `docs/quick-ref-l2.md` — append `## Linux Quick Reference` H2 at the END of the file (after Android Quick Reference H2 lines 233-281, before Version History H2 if present); refresh `last_verified` frontmatter; append Version History row

**Direct templates (read for verbatim mirror; DO NOT modify):**
- `docs/quick-ref-l2.md` lines 233-281 (Phase 57 Android Quick Reference H2 — 4-sub-H3 structural template per D-23)
- `docs/quick-ref-l2.md` lines 261-271 (Android Play Integrity Verdict Reference — 3-row pointer table; ENDORSED PITFALL-7-COMPLIANT pattern per D-22..D-25; direct shape template for Linux Compliance Category Reference D-23 4-row pointer table)
- `docs/l2-runbooks/24-linux-log-collection.md` lines 30-36 (Decision Matrix — verbatim source for the 3 method names: `journalctl (systemd journal)` / `File-based paths` / `Package-state queries`)

**Source files referenced by Linux Quick Reference rows (read-only — already exist from Phase 50/52):**
- `docs/admin-setup-linux/03-compliance-policy.md` (Phase 50 LIN-04; SSoT for the 4 settings-catalog categories — Compliance Category Reference cross-link target)
- `docs/l2-runbooks/24-linux-log-collection.md` (Phase 52 LIN-12; 3-method log-collection source + Linux Investigation Runbooks row 1)
- `docs/l2-runbooks/25-linux-agent-investigation.md` (Phase 52 LIN-12; Linux Investigation Runbooks row 2)

**Verified anchor slugs in admin-setup-linux/03-compliance-policy.md (per RESEARCH §Pattern 3):**
- `#step-2-configure-allowed-distributions`
- `#step-3-configure-custom-compliance-bash-discovery-scripts`
- `#step-4-configure-device-encryption-dm-crypt-luks`
- `#step-5-configure-password-policy`

(Plan-author re-verifies these slugs at edit-time by reading the actual H3 headings in 03-compliance-policy.md and applying GFM kebab-case rules; if any heading text has changed since RESEARCH capture, update the slug accordingly.)

**Sibling Phase 59 plans (parallel-safe — disjoint files):**
- 59-02 / 59-04 edit docs/index.md (disjoint)
- 59-03 edits docs/operations/00-index.md (disjoint)
- 59-05 edits 4 glossary files (disjoint)
- 59-06 edits docs/quick-ref-l1.md (disjoint)
</interfaces>
</context>

<threat_model>
N/A — documentation-only phase, no attack surface introduced. Quick-reference card edits with no input validation, network exposure, or sensitive-data handling.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Append `## Linux Quick Reference` H2 with 4 sub-H3 sections to docs/quick-ref-l2.md (D-21 / D-23 / D-24 / D-25)</name>
  <files>docs/quick-ref-l2.md</files>
  <read_first>
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-21..D-25 + D-26 V-59-28..31 validator scope)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md (§Pattern 2 + §Pattern 3 verbatim Android Play Integrity Verdict Reference template at quick-ref-l2.md:261-271; verified anchor slugs for 03-compliance-policy.md)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-ANCHOR-INVENTORY.md (post-edit expected: new `## Linux Quick Reference` H2 in quick-ref-l2.md)
    - docs/quick-ref-l2.md (FULL FILE — locate Android Quick Reference H2 end at line 281 + insertion point; verify Play Integrity table shape at lines 261-271 as direct template)
    - docs/admin-setup-linux/03-compliance-policy.md (verify file exists; read H3 headings to confirm GFM slugs for 4 step anchors; confirm 4 categories per LIN-04 + frontmatter line 9 authoritative count)
    - docs/l2-runbooks/24-linux-log-collection.md lines 30-36 (Decision Matrix — verbatim 3 method names)
    - docs/l2-runbooks/25-linux-agent-investigation.md (verify file exists)
  </read_first>
  <action>
Use the Edit tool to append the new H2 block. The insertion point is immediately AFTER the Android Quick Reference H2 (currently ends around line 281) and BEFORE the next H2 (likely a Version History H2 at line 282+).

Locate the existing Android Quick Reference H2 closing prose (around line 280-281). The file structure is:
```
[end of Android Investigation Runbooks list]

## Version History
```

Append the new Linux H2 immediately before `## Version History`:

```

---

## Linux Quick Reference

**Platform:** Linux (Ubuntu 22.04 / 24.04 LTS) through Microsoft Intune

> **Important:** Linux L2 diagnostic data collection follows a 3-tier methodology (journalctl primary / file-based paths secondary / package-state queries supplemental) per the Phase 52 Decision Matrix. Compliance category configuration content lives at the Phase 50 SSoT — this H2 carries pointer-only references (PITFALL-7 firewall).

### Linux Diagnostic Data Collection (3 methods)

| Method | Primary Tool | Who Triggers | Data Scope | L2 Access Path | Confidence |
|--------|--------------|--------------|------------|----------------|------------|
| journalctl (systemd journal) | `journalctl -u intune-agent` / `journalctl --user -u intune-agent` | L2 (on device via SSH) | systemd journal entries for `intune-agent` service + user-scope timer activations | Direct on-device retrieval | Tier 1 -- primary log mechanism for all Linux Intune agent activity |
| File-based paths | `cat /var/log/intune-update.log` + `cat /var/log/dpkg.log` | L2 (on device via SSH) | Package install/upgrade history (`dpkg.log`) + Intune update transitions (`intune-update.log`) | Direct on-device retrieval | Tier 2 -- supplemental for package-install troubleshooting + post-update re-enrollment |
| Package-state queries | `dpkg -l intune-portal` + `apt-cache policy intune-portal` | L2 (on device via SSH) | Current package version + install state (`ii` flag) + apt source policy | Direct on-device retrieval | Tier 3 -- diagnostic for Identity Broker `intune-portal` 2.0.2+ breaking change re-enrollment loops (Phase 50 LIN-05 known pitfall) |

*(Full method details: [Linux Log Collection Guide](l2-runbooks/24-linux-log-collection.md) is the authoritative source. Per-method scope, evidence-collection prerequisites, and the 3-tier escalation order are documented in the Decision Matrix at lines 30-36 of that file.)*

### Key Intune Portal Paths (Linux L2)

| Path | Purpose |
|------|---------|
| Devices > Linux | Per-device inventory + enrollment state (Linux-scoped) |
| Devices > [device] > Device compliance | Per-device compliance policy evaluation state, timestamps, non-compliant categories (4-category surface: Allowed Distributions / Custom Compliance / Device Encryption / Password Policy) |
| Devices > Device onboarding > Enrollment > Linux (limited surface) | Linux enrollment configuration (no zero-touch — user-initiated only); compliance policy assignment surface |
| Reports > Endpoint analytics (Linux subset) | Per-device telemetry rollup; supplemental for compliance-trend correlation across Linux fleet |

*(Verified {today's date} against Microsoft Learn. Intune admin center reorganizes without deprecation notice — re-verify before content lock-in. Note: Linux has no zero-touch / token portal equivalent; user-initiated enrollment is the only path.)*

### Linux Compliance Category Reference

| Category | One-line role | SSoT |
|----------|---------------|------|
| Allowed Distributions | Restricts compliance to Ubuntu 22.04 / 24.04 LTS; Ubuntu 20.04 dropped from Intune 2508 (August 2025) | [Phase 50 SSoT](../admin-setup-linux/03-compliance-policy.md#step-2-configure-allowed-distributions) |
| Custom Compliance | Bash discovery scripts evaluate device state at scheduled intervals; per-device exit codes drive compliance verdict | [Phase 50 SSoT](../admin-setup-linux/03-compliance-policy.md#step-3-configure-custom-compliance-bash-discovery-scripts) |
| Device Encryption | dm-crypt + LUKS full-disk encryption signal as compliance prerequisite (encryption-compliance triple analog: BitLocker / FileVault / dm-crypt) | [Phase 50 SSoT](../admin-setup-linux/03-compliance-policy.md#step-4-configure-device-encryption-dm-crypt-luks) |
| Password Policy | Minimum length / complexity rules; enforced via PAM `pwquality` configuration on enrolled devices | [Phase 50 SSoT](../admin-setup-linux/03-compliance-policy.md#step-5-configure-password-policy) |

> ⚠️ **Bash discovery script authoring, compliance-evaluation cadence, and per-category remediation playbooks are owned by [Phase 50 SSoT — Linux Compliance Policy](../admin-setup-linux/03-compliance-policy.md).**

Full configuration details and per-category remediation: see the [Linux Compliance Policy admin guide](../admin-setup-linux/03-compliance-policy.md).

### Linux Investigation Runbooks

- [Linux Log Collection Guide](l2-runbooks/24-linux-log-collection.md) -- prerequisite for all Linux L2 investigations (3-method matrix: journalctl primary / file-based paths secondary / package-state queries supplemental)
- [Linux Agent Investigation](l2-runbooks/25-linux-agent-investigation.md) -- service-state diagnosis + Identity Broker `intune-portal` 2.0.2+ re-enrollment loop investigation + systemd timer activation analysis
```

**Critical compliance rules (D-21 / D-23 / D-24 / D-25 / V-59-NN):**
- 4 sub-H3 literals MUST be exactly: `### Linux Diagnostic Data Collection (3 methods)` / `### Key Intune Portal Paths (Linux L2)` / `### Linux Compliance Category Reference` / `### Linux Investigation Runbooks` (V-59-28 anchor pin)
- Linux Diagnostic Data Collection table MUST contain 3 verbatim method names: `journalctl (systemd journal)` AND `File-based paths` AND `Package-state queries` (V-59-29 literals)
- Linux Compliance Category Reference table MUST contain 4 verbatim category literals: `Allowed Distributions`, `Custom Compliance`, `Device Encryption`, `Password Policy` (V-59-30 literals; D-23 + REQUIREMENTS:27 LIN-04 + admin-setup-linux/03-compliance-policy.md frontmatter line 9 authoritative count)
- Linux Compliance Category Reference table MUST contain 4 cross-link references to `../admin-setup-linux/03-compliance-policy.md#step-X-...` anchors (V-59-30); plan-author re-verifies exact anchor slugs at edit-time
- **PITFALL-7 firewall (D-24; V-59-31 NEGATIVE):** Linux Compliance Category Reference H3 body MUST NOT contain:
  - Bash script syntax: NO `if`/`then`/`fi`/`exit 0`/`exit 1`/`#!/bin/bash`/`else` literals
  - `compliance-evaluation-cadence` literal token
  - Per-OS-version distro-version-table content
  - Multi-line example Bash discovery scripts
- Cell content per category MUST be: category name (col-0) + 1-line role description (col-1) + cross-link to SSoT step anchor (col-2). Single-sentence per cell maximum (Pitfall 4).
- NO `[Mode]` bracket tags anywhere in Linux H2 body (V-59-26 contract from Plan 59-06 also applies symmetrically; D-25)
- USE `--` (double-hyphen) for description disambiguation matching Phase 57 Android template, NOT `—` em-dash
- USE `**Platform:** Linux (Ubuntu 22.04 / 24.04 LTS) through Microsoft Intune` intro line (mirrors Android `**Platform:** Android Enterprise through Microsoft Intune` at line 235)
- Linux Investigation Runbooks list MUST contain 2 entries (D-23 — thin but unavoidable per finite Linux L2 surface)
- Existing `## Windows Autopilot Quick Reference` / `## macOS Provisioning Quick Reference` / `## iOS/iPadOS Quick Reference` / `## Android Enterprise Quick Reference` H2s MUST remain byte-identical post-edit (V-59-32 NEGATIVE regression-guard target)
- Existing Android Play Integrity Verdict Reference at lines 261-271 byte-identical (V-59-32; PITFALL-7 endorsed pointer-table pattern is the basis for Linux Compliance Category Reference shape)
- Use `---` separator before the new H2 to match the existing pattern between Android Quick Reference and any subsequent H2

**Frontmatter refresh:**
Update the `last_verified` field in docs/quick-ref-l2.md frontmatter (top of file) to today's date; update `review_by` to +60 days. Use ISO date format `YYYY-MM-DD`.

Replace `{today's date}` placeholders inside the Linux H2 body with the actual ISO date string.

**Version History row:**
Append a new row at the TOP of the Version History table (immediately below the header `| Date | Change | Author |` separator):
```
| {today's date} | Phase 59 (CLEAN-08): added Linux Quick Reference H2 (4-part substructure: Linux Diagnostic Data Collection 3 methods / Key Intune Portal Paths Linux L2 / Linux Compliance Category Reference 4-row pointer table / Linux Investigation Runbooks 2-link list); link-not-copy contract for Phase 50 Compliance Policy SSoT per PITFALL-7 + Phase 57 D-22..D-25 + D-23 ENDORSEMENT inheritance | -- |
```

  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/quick-ref-l2.md','utf8');const errors=[];function sliceH2(content,h2){const re=new RegExp('^'+h2.replace(/[/\\\\^$*+?.()|[\\]{}]/g,'\\\\$&')+'\\\\s*$','m');const m=content.match(re);if(!m)return null;const idx=content.indexOf(m[0]);const after=content.slice(idx+m[0].length);const next=after.search(/^## /m);return next>0?content.slice(idx,idx+m[0].length+next):content.slice(idx);}const linux=sliceH2(c,'## Linux Quick Reference');if(!linux){errors.push('## Linux Quick Reference H2 absent');}else{const subH3s=['### Linux Diagnostic Data Collection (3 methods)','### Key Intune Portal Paths (Linux L2)','### Linux Compliance Category Reference','### Linux Investigation Runbooks'];for(const h3 of subH3s){const re=new RegExp('^'+h3.replace(/[/\\\\^$*+?.()|[\\]{}]/g,'\\\\$&')+'\\\\s*$','m');if(!re.test(linux))errors.push('Sub-H3 missing in Linux Quick Reference H2: '+h3);}function sliceH3(region,h3){const idx=region.indexOf(h3);if(idx<0)return null;const after=region.slice(idx+h3.length);const nextH3=after.search(/^### /m);return nextH3>0?after.slice(0,nextH3):after;}const dataCol=sliceH3(linux,'### Linux Diagnostic Data Collection (3 methods)');if(dataCol){const required=['journalctl (systemd journal)','File-based paths','Package-state queries'];for(const lit of required)if(!dataCol.includes(lit))errors.push('Diagnostic Data Collection missing literal: '+lit+' (V-59-29)');}const compRef=sliceH3(linux,'### Linux Compliance Category Reference');if(compRef){const cats=['Allowed Distributions','Custom Compliance','Device Encryption','Password Policy'];for(const cat of cats)if(!compRef.includes(cat))errors.push('Compliance Category missing: '+cat+' (V-59-30)');const anchors=['#step-2-configure-allowed-distributions','#step-3-configure-custom-compliance-bash-discovery-scripts','#step-4-configure-device-encryption-dm-crypt-luks','#step-5-configure-password-policy'];for(const a of anchors){if(!compRef.includes('admin-setup-linux/03-compliance-policy.md'+a))errors.push('Compliance Category cross-link missing anchor: '+a+' (V-59-30)');}const forbidden=[/\\bif\\b.*\\bthen\\b/,/\\bfi\\b/,/exit 0/,/exit 1/,/#!\\/bin\\/bash/,/compliance-evaluation-cadence/];for(const re of forbidden){if(re.test(compRef))errors.push('V-59-31 NEGATIVE violation: forbidden pattern in Compliance Category Reference: '+re);}}const runbooks=sliceH3(linux,'### Linux Investigation Runbooks');if(runbooks){const expected=['l2-runbooks/24-linux-log-collection.md','l2-runbooks/25-linux-agent-investigation.md'];for(const f of expected)if(!runbooks.includes(f))errors.push('Investigation Runbooks missing link: '+f);}}const preH2s=['## Windows Autopilot Quick Reference','## macOS Provisioning Quick Reference','## iOS/iPadOS Quick Reference','## Android Enterprise Quick Reference'];for(const h of preH2s){if(!c.includes(h))errors.push('Pre-Phase-59 Quick Reference H2 missing: '+h);}if(!c.includes('Play Integrity Verdict Reference'))errors.push('Android Play Integrity Verdict Reference H3 missing (V-59-32 regression-guard violation — should be unchanged)');if(errors.length){console.error('ERRORS:',errors.join('; '));process.exit(1);}console.log('Linux Quick Reference L2 + 4 sub-H3 + 3-method literals + 4 categories + 4 cross-links + PITFALL-7 firewall + anchor stability checks pass');process.exit(0);"</automated>
  </verify>
  <done>docs/quick-ref-l2.md contains `## Linux Quick Reference` H2 with 4 sub-H3 anchors (V-59-28); Linux Diagnostic Data Collection contains 3 verbatim method literals (V-59-29); Linux Compliance Category Reference contains 4 category literals + 4 cross-links to admin-setup-linux/03-compliance-policy.md step anchors (V-59-30); NO Bash syntax / NO compliance-evaluation-cadence literal in Compliance Category Reference (V-59-31 NEGATIVE; D-24 PITFALL-7 firewall); 4 pre-Phase-59 Quick Reference H2s + Android Play Integrity Verdict Reference unchanged (V-59-32); node verification check exits 0.</done>
</task>

</tasks>

<verification>
- `docs/quick-ref-l2.md` contains `## Linux Quick Reference` H2 (V-59-28)
- 4 sub-H3 anchors present: `### Linux Diagnostic Data Collection (3 methods)` / `### Key Intune Portal Paths (Linux L2)` / `### Linux Compliance Category Reference` / `### Linux Investigation Runbooks` (V-59-28)
- Linux Diagnostic Data Collection contains 3 verbatim method names: `journalctl (systemd journal)`, `File-based paths`, `Package-state queries` (V-59-29)
- Linux Compliance Category Reference contains 4 category literals: `Allowed Distributions`, `Custom Compliance`, `Device Encryption`, `Password Policy` (V-59-30)
- 4 cross-links to `admin-setup-linux/03-compliance-policy.md#step-X-...` anchors (V-59-30)
- Linux Compliance Category Reference body does NOT contain Bash syntax / `compliance-evaluation-cadence` literal / per-OS-version content (V-59-31 NEGATIVE; D-24 PITFALL-7 firewall)
- Linux Investigation Runbooks contains 2 entries (D-23)
- 4 pre-Phase-59 platform Quick Reference H2s byte-identical (V-59-32)
- Android Play Integrity Verdict Reference at lines 261-271 byte-identical (V-59-32)
- frontmatter `last_verified` refreshed; Version History row added
</verification>

<success_criteria>
- QUICK_REF_LINUX_PRESENT phase-level truth (L2 portion) satisfied
- PITFALL_7_FIREWALL phase-level truth satisfied (Linux Compliance Category Reference is pointer-only; no SSoT content duplicated)
- D-21 / D-23 / D-24 / D-25 all satisfied
- ROADMAP Phase 59 SC#4 fully satisfied (combined with Plan 59-06 L1 portion)
- V-59-28 + V-59-29 + V-59-30 + V-59-31 validator targets correctly populated for Plan 59-08 verification
</success_criteria>

<output>
After completion, create `.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-07-SUMMARY.md` documenting:
1. The exact line numbers where `## Linux Quick Reference` H2 was inserted (post-edit grep `^## Linux Quick Reference docs/quick-ref-l2.md`)
2. The 4 sub-H3 anchors confirmed
3. The 3 method names in Linux Diagnostic Data Collection table (verbatim per V-59-29)
4. The 4 category literals + 4 cross-link anchors in Linux Compliance Category Reference (V-59-30)
5. Confirmation that V-59-31 NEGATIVE PITFALL-7 firewall satisfied: NO Bash syntax / NO compliance-evaluation-cadence literal / NO per-OS-version content
6. Confirmation that 4 pre-Phase-59 platform Quick Reference H2s + Android Play Integrity Verdict Reference are byte-identical (V-59-32)
7. Note for Plan 59-08 (validator): "All Linux Quick Reference L2 V-59-NN targets are now in place: V-59-28 (H2 + 4 sub-H3), V-59-29 (3 method literals), V-59-30 (4 categories + 4 cross-links), V-59-31 (NEGATIVE PITFALL-7 firewall)."
</output>
