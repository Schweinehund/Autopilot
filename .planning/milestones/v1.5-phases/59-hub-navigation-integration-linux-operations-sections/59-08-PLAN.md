---
phase: 59
plan: 59-08
type: execute
wave: 4
depends_on: [59-02, 59-03, 59-04, 59-05, 59-06, 59-07]
files_modified: []
files_created:
  - scripts/validation/check-phase-59.mjs
requirements: [CLEAN-08]
autonomous: true

must_haves:
  truths:
    - "VALIDATOR_GREEN phase-level truth satisfied: `node scripts/validation/check-phase-59.mjs` exits 0 with all 36 V-59-NN structural assertions PASS"
    - "Validator implementation pattern matches `scripts/validation/check-phase-58.mjs` / `check-phase-57.mjs` / ... / `check-phase-49.mjs` per Phase 48 D-25 + D-27 lineage: file-reads-only / no-shared-module / regex-based; NO AST parsing; NO markdown-link AST library"
    - "Validator covers all 36 V-59-NN assertions per CONTEXT D-26 enumeration + RESEARCH §V-59-NN Assertion Cluster Map: file-existence (V-59-01..06) / Linux H2 (V-59-07..09) / Operations H2 + ops-index (V-59-10..19) / glossary reciprocity (V-59-20..24) / quick-ref Linux H2s (V-59-25..31) / regression-guards + TBD/TODO scan (V-59-32..36)"
    - "Hardcoded H2/H3 anchor strings + literal-token regexes pinned in validator AND CONTEXT.md per D-30; renaming any pinned anchor requires same-commit validator update"
    - "Glossary reciprocity validator (V-59-20..24) is NOVEL for Phase 59 — implements `sliceH3Region()` + `findH3Region()` helpers + `> See also:` line + blockquote-integrity checks per RESEARCH §Pattern 7 Core idiom 6"
    - "Validator includes col-0-exclusion fix per Phase 58 Plan 58-05 inheritance for any 3+-column tables where col-0 is intentionally non-link-bearing (e.g., Linux Compliance Category Reference category-name col-0)"
    - "Validator self-test: 36/36 V-59-NN PASS post-Wave-2-3 content landing"

  artifacts:
    - path: "scripts/validation/check-phase-59.mjs"
      provides: "Phase 59 structural validation harness; 36 V-59-NN assertions covering all 4 GAs + ops-index amendment + glossary reciprocity + regression-guards"
      contains:
        - "#!/usr/bin/env node"
        - "fs.readFileSync"
        - "function sliceH2Region"
        - "function sliceH3Region"
        - "V-59-01"
        - "V-59-07"
        - "V-59-10"
        - "V-59-13"
        - "V-59-20"
        - "V-59-25"
        - "V-59-28"
        - "V-59-31"
        - "V-59-32"
        - "## Linux Provisioning"
        - "## Operations"
        - "## Linux Quick Reference"
        - "Operational Depth"
        - "linux-intune-portal-enrollment.md"
        - "Allowed Distributions"
        - "Custom Compliance"
        - "Device Encryption"
        - "Password Policy"
        - "journalctl"
        - "File-based"
        - "Package-state"

  key_links:
    - from: "scripts/validation/check-phase-59.mjs"
      to: "docs/index.md / docs/operations/00-index.md / docs/quick-ref-l1.md / docs/quick-ref-l2.md / docs/_glossary*.md (8 hub-and-glossary files)"
      via: "fs.readFileSync + regex assertions on H2/H3 literals + cell-shape patterns + literal-token sniffs"
      pattern: "fs\\.readFileSync\\(['\"]docs/"
    - from: "scripts/validation/check-phase-59.mjs"
      to: ".planning/phases/59-*/59-05-COLLISION-MATRIX.md (locked input for V-59-20..23 reciprocity)"
      via: "Optional read of matrix artifact for assertion generation; or hardcoded matrix in validator (D-30 pin)"
      pattern: "59-05-COLLISION-MATRIX\\.md"
---

<objective>
Author `scripts/validation/check-phase-59.mjs` — the Phase 59 validator-as-deliverable per AUDIT-06 mandate + D-26..D-27. The validator is a Node.js ESM script that uses fs.readFileSync + regex only (no AST, no markdown-link library, no shared module per D-27 Phase 48-58 lineage). It implements all 36 V-59-NN structural assertions per CONTEXT D-26 enumeration covering: file-existence, Linux H2 in docs/index.md (D-01..D-06), Operations H2 + ops/00-index.md amendment (D-07..D-13), glossary CLEAN-08 reciprocity (D-14..D-20), Linux quick-ref-l1/l2 H2s (D-21..D-25), and regression-guards + TBD scan.

Plan 59-08 is Wave 4; depends on 59-02 + 59-03 + 59-04 + 59-05 + 59-06 + 59-07 (all content plans must have landed before validator's V-59-NN assertions can verify); ships D-26 / D-27 / D-30 mandates.

Purpose: Implements ROADMAP Phase 59 SC#5 (validator-as-deliverable per AUDIT-06; check-phase-59.mjs ships alongside Phase 59 content). Provides the VALIDATOR_GREEN phase-level truth + the auditable enforcement mechanism for V-59-NN structural assertions. Plan 59-09 pre-commit gate runs this validator as one of 3 required-green gates.

Output: 1 new file (`scripts/validation/check-phase-59.mjs`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-ANCHOR-INVENTORY.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
**Files this plan creates:**
- CREATE: `scripts/validation/check-phase-59.mjs` — net-new validator file; ESM module (`.mjs` extension); shebang `#!/usr/bin/env node`; runnable as `node scripts/validation/check-phase-59.mjs`

**Direct templates (read for verbatim idiom mirror; DO NOT modify):**
- `scripts/validation/check-phase-57.mjs` — Phase 57 validator (26 V-57-NN); source for `sliceH2Region` + H3-slicing + row-count + NEGATIVE regression-guard + TBD/TODO scan idioms (RESEARCH §Pattern 7 Core idioms 1-4)
- `scripts/validation/check-phase-58.mjs` — Phase 58 validator (26 V-58-NN); source for `extractCanonicalDataCells()` col-0 exclusion fix (Plan 58-05 deviation pattern); informational-flag forward-search pattern
- `scripts/validation/check-phase-49.mjs` — Phase 49 validator; reference for regex H3-extraction + line-immediately-following-pattern matching for see-also reciprocity

**Source files validated (read by check-phase-59.mjs at runtime):**
- `docs/index.md` (V-59-01 file-existence + V-59-07..09 Linux H2 + V-59-10..12 Operations H2 + V-59-15..18 PITFALL-7 + V-59-19 NEGATIVE blockquote sniff + V-59-32 regression-guard)
- `docs/operations/00-index.md` (V-59-02 file-existence + V-59-13 4 H2s + V-59-14 row counts)
- `docs/quick-ref-l1.md` (V-59-03 file-existence + V-59-25 Linux H2 + V-59-26 NEGATIVE Mode tags + V-59-27 Decision Tree single link + V-59-32 regression-guard)
- `docs/quick-ref-l2.md` (V-59-04 file-existence + V-59-28 Linux H2 + V-59-29 3 method literals + V-59-30 4 categories + 4 cross-links + V-59-31 NEGATIVE PITFALL-7 firewall + V-59-32 regression-guard)
- `docs/_glossary.md` (V-59-05 file-existence + V-59-20..24 reciprocity)
- `docs/_glossary-macos.md` (V-59-05 file-existence + V-59-20..24 reciprocity)
- `docs/_glossary-android.md` (V-59-05 file-existence + V-59-20..24 reciprocity)
- `docs/_glossary-linux.md` (V-59-06 file-existence + V-59-20..24 reciprocity)

**Locked input (read by check-phase-59.mjs OR hardcoded in validator per D-30):**
- `.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md` (Plan 59-05 deliverable; Reciprocity Pairs section — V-59-20..23 input)

**Don't hand-roll (per RESEARCH §Don't Hand-Roll):**
- DO NOT use markdown AST library (`remark`, `unified`, `markdown-it`)
- DO NOT use shared validator module — Phase 48 D-25 mandate: each `check-phase-NN.mjs` is self-contained
- DO NOT use external dependencies — Node.js built-ins (`fs`, `path`) only

**Sibling Phase 59 plans (Wave 4 dependency on all content plans):**
- 59-01 / 59-02 / 59-03 / 59-04 / 59-05 / 59-06 / 59-07 must all be COMPLETE before this plan's validator can self-test 36/36 PASS
- 59-09 will run this validator as part of pre-commit gate (Wave 5)
</interfaces>
</context>

<threat_model>
N/A — documentation-only phase, no attack surface introduced. Validator is a static-analysis Node.js script with read-only file access; no input validation, network exposure, or sensitive-data handling.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/validation/check-phase-59.mjs with all 36 V-59-NN assertions (D-26 / D-27 / D-30)</name>
  <files>scripts/validation/check-phase-59.mjs</files>
  <read_first>
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-26 V-59-NN enumeration; D-27 implementation pattern; D-30 hardcoded anchor pin discipline)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md (§Pattern 7 Validator Regex Idioms — `sliceH2Region`, H3-slicing, row-count, NEGATIVE regression-guard, TBD/TODO scan, `extractCanonicalDataCells` col-0 exclusion, NOVEL `sliceH3Region` + `> See also:` reciprocity check; §Pattern 8 sliceH3Region; §V-59-NN Assertion Cluster Map for the canonical 36-assertion list)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md (Reciprocity Pairs section — V-59-20..23 input)
    - scripts/validation/check-phase-58.mjs (FULL FILE — verbatim idiom source for sliceH2Region + extractCanonicalDataCells + V-NN-NN runner pattern)
    - scripts/validation/check-phase-57.mjs (FULL FILE — verbatim idiom source for negative regression-guard + TBD scan; line 109 hardcoded `["### Admin Setup", 3]` per-phase row-count assertion pattern; line 408+ PITFALL-7 firewall NEGATIVE check pattern)
    - scripts/validation/check-phase-49.mjs (reference for H3-extraction + see-also-line check)
  </read_first>
  <action>
Create `scripts/validation/check-phase-59.mjs` as a self-contained Node.js ESM script. Follow the Phase 58 validator architecture verbatim with adaptations for Phase 59's 36-assertion scope. Use the file structure below; fill in each assertion's regex / literal check from CONTEXT D-26 + RESEARCH §V-59-NN Assertion Cluster Map.

**File structure (plan-author writes the JavaScript; structural skeleton follows):**

1. Shebang line: `#!/usr/bin/env node`
2. ESM imports: `import fs from 'fs';` + `import path from 'path';`
3. Constants section (D-30 hardcoded pins):
   - `REPO_ROOT = path.resolve(process.argv[1], '..', '..', '..');`
   - File path constants: INDEX_MD / OPS_INDEX_MD / QUICK_REF_L1_MD / QUICK_REF_L2_MD / GLOSSARY_WIN / GLOSSARY_MACOS / GLOSSARY_ANDROID / GLOSSARY_LINUX
4. Helper functions (per RESEARCH §Pattern 7 Core idioms):
   - `readFile(p)` — wrapper for `fs.readFileSync(p, 'utf8')`
   - `fileExists(p)` — wrapper for `fs.existsSync(p)`
   - `escapeRegex(s)` — escape special regex chars
   - `sliceH2Region(content, h2Literal)` — slice from H2 to next `## ` or EOF (RESEARCH idiom 1)
   - `sliceH3Region(content, h3Literal)` — slice from H3 to next `### ` or `## ` or EOF (RESEARCH §Pattern 8)
   - `sliceH3InRegion(region, h3Literal)` — slice H3 within an H2 region (RESEARCH idiom 2)
   - `countDataRows(window)` — count lines starting with `| [` (excludes header + separator rows)
   - `stripCodeAndHistory(content)` — strip `\`\`\`...\`\`\`` blocks + `## Version History` to EOF + `## Changelog` to EOF (RESEARCH idiom 4)
   - `gfmSlug(heading)` — GFM kebab-case slug computation (RESEARCH §Pattern 6)
   - `extractH3Slugs(content)` — return Set of all H3 GFM slugs in a glossary (for V-59-21 anchor-correctness)
5. COLLISION_MATRIX constant — array of `{ term, glossary, anchor }` objects transcribed verbatim from `59-05-COLLISION-MATRIX.md` Anchor Map section
6. RECIPROCITY_PAIRS constant — array of `{ source, sourceAnchor, target, targetAnchor, termCanonical }` objects transcribed verbatim from `59-05-COLLISION-MATRIX.md` Reciprocity Pairs section
7. Assertion runner: `results = []` + `function assert(id, type, description, fn)` that pushes `{id, type, description, pass, details}` and catches exceptions
8. 36 assertion calls (V-59-01..36 per CONTEXT D-26):

   **V-59-01..06: File existence** (6 assertions)
   - V-59-01: docs/index.md exists
   - V-59-02: docs/operations/00-index.md exists
   - V-59-03: docs/quick-ref-l1.md exists
   - V-59-04: docs/quick-ref-l2.md exists
   - V-59-05: docs/_glossary.md + _glossary-macos.md + _glossary-android.md exist
   - V-59-06: docs/_glossary-linux.md exists

   **V-59-07..09: Linux H2 in docs/index.md** (3 assertions)
   - V-59-07 POSITIVE: `## Linux Provisioning` H2 + 3 sub-H3 (`### Service Desk (L1)` + `### Desktop Engineering (L2)` + `### Admin Setup`) + row counts L1=4 / L2=4 / Admin=3
   - V-59-08 POSITIVE: Cross-Platform References has `Linux Provisioning Lifecycle` AND `Linux Capability Matrix` link entries
   - V-59-09 NEGATIVE: docs/index.md does NOT contain `linux-intune-portal-enrollment.md` literal AND does NOT contain `end-user-guides/linux` literal

   **V-59-10..19: Operations H2 + ops/00-index.md** (10 assertions)
   - V-59-10 POSITIVE: `## Operations` H2 present; ordering Linux Provisioning index < Operations index < Cross-Platform References index
   - V-59-11 POSITIVE: 4 sub-H3 anchors within Operations H2 region (`### Co-Management` + `### Patch & Update Management` + `### App Lifecycle Automation` + `### Compliance Drift Detection + Tenant Migration`)
   - V-59-12 NEGATIVE: docs/index.md does NOT contain `Operational Depth` H2/H3 token (use regex `/^##.*Operational Depth/m`)
   - V-59-13 POSITIVE: docs/operations/00-index.md contains 4 H2s (Co-Management + Patch & Update Management + App Lifecycle Automation + Compliance Drift Detection + Tenant Migration)
   - V-59-14 POSITIVE: ops/00-index.md row counts: Co-Management=4 (preserved) / Patch=5 / App=5 / Drift=5
   - V-59-15..18 POSITIVE: Each Operations sub-H3 sub-table has every data row containing a markdown link (PITFALL-7); 4 separate assertions, one per sub-H3
   - V-59-19 NEGATIVE: Operations H2 body does NOT contain `No co-management equivalent` literal

   **V-59-20..24: Glossary reciprocity (CLEAN-08)** (5 assertions)
   - V-59-20 POSITIVE (A1 reciprocity): For each term in COLLISION_MATRIX, every glossary listing the term has `> See also:` line within H3 region
   - V-59-21 POSITIVE (A2 anchor-correctness): For each pair in RECIPROCITY_PAIRS, target anchor exists as H3 in target glossary (use extractH3Slugs to build slug Set per glossary; check `pair.targetAnchor.replace(/^#/, '')` is in target's slug Set)
   - V-59-22 POSITIVE (bidirectional): For each pair in RECIPROCITY_PAIRS, source glossary's H3 region contains a `> See also:` line referencing `path.basename(pair.target) + pair.targetAnchor`
   - V-59-23 POSITIVE (transitivity sanity): RECIPROCITY_PAIRS.length is at least 6 (encryption triple alone produces 6 pairs)
   - V-59-24 NEGATIVE (A3 blockquote integrity): Across all 4 glossaries, every `> See also:` line at line N is preceded by another `>`-prefixed line at line N-1

   **V-59-25..27: quick-ref-l1.md Linux H2** (3 assertions)
   - V-59-25 POSITIVE: `## Linux Quick Reference` H2 + 4 sub-H3 (`### Top Checks` + `### Linux Escalation Triggers` + `### Linux Decision Tree` + `### Linux Runbooks`); Top Checks has exactly 4 numbered items (regex `/^[1-9]\.\s/gm` count = 4)
   - V-59-26 NEGATIVE: Linux H2 body does NOT contain `[BYOD]`/`[ZTE]`/`[AOSP]`/`[Knox]`/`[All GMS]`/`[22.04]`/`[24.04]`/`[Mode]` literals (regex group)
   - V-59-27 POSITIVE: Linux Decision Tree H3 contains exactly 1 `decision-trees/09-linux-triage.md` link

   **V-59-28..31: quick-ref-l2.md Linux H2** (4 assertions)
   - V-59-28 POSITIVE: `## Linux Quick Reference` H2 + 4 sub-H3 (`### Linux Diagnostic Data Collection (3 methods)` + `### Key Intune Portal Paths (Linux L2)` + `### Linux Compliance Category Reference` + `### Linux Investigation Runbooks`)
   - V-59-29 POSITIVE: Linux Diagnostic Data Collection H3 body contains all 3 method literals: `journalctl`, `File-based`, `Package-state`
   - V-59-30 POSITIVE: Linux Compliance Category Reference H3 body contains all 4 category literals (`Allowed Distributions`, `Custom Compliance`, `Device Encryption`, `Password Policy`) AND all 4 cross-link anchors (`admin-setup-linux/03-compliance-policy.md#step-2-configure-allowed-distributions`, `#step-3-configure-custom-compliance-bash-discovery-scripts`, `#step-4-configure-device-encryption-dm-crypt-luks`, `#step-5-configure-password-policy`)
   - V-59-31 NEGATIVE: Linux Compliance Category Reference H3 body does NOT contain Bash syntax tokens (`if/then`, `fi`, `exit 0`, `exit 1`, `#!/bin/bash`) and does NOT contain `compliance-evaluation-cadence` literal

   **V-59-32: H2 anchor stability regression-guard** (1 assertion)
   - V-59-32 POSITIVE: All pre-Phase-59 H2s present in 4 hub files: docs/index.md (Windows Autopilot / macOS Provisioning / iOS/iPadOS Provisioning / Android Enterprise Provisioning / Cross-Platform References) + docs/operations/00-index.md (Co-Management) + docs/quick-ref-l1.md + docs/quick-ref-l2.md (each has 4 platform Quick Reference H2s: Windows Autopilot Quick Reference / macOS Provisioning Quick Reference / iOS/iPadOS Quick Reference / Android Enterprise Quick Reference)

   **V-59-33..36: TBD/TODO/PLACEHOLDER scan** (4 assertions)
   - V-59-33 NEGATIVE: docs/index.md (post strip-code-and-history) contains no banned token (`/\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b/`)
   - V-59-34 NEGATIVE: docs/operations/00-index.md (post strip) contains no banned token
   - V-59-35 NEGATIVE: docs/quick-ref-l1.md (post strip) contains no banned token
   - V-59-36 NEGATIVE: docs/quick-ref-l2.md (post strip) contains no banned token

9. Output runner:
   - Iterate over `results` array
   - Print `[PASS] V-59-NN (TYPE): description` lines (verbose mode prints all; default mode prints only failures)
   - Final line: `${pass}/${pass+fail} V-59-NN assertions PASS (${fail} FAIL)`
   - `process.exit(fail === 0 ? 0 : 1)` — green-gate semantics for Plan 59-09

**Critical implementation rules (D-26 / D-27 / D-30):**
- Total assertion count MUST be 36 V-59-NN per CONTEXT D-26 enumeration
- File-reads-only / regex-based / no-shared-module per D-27
- Every H2/H3 anchor literal + token regex pinned in validator (D-30 brittleness trade-off)
- `process.exit(0)` on all PASS; `process.exit(1)` on any FAIL
- `--verbose` flag for full output (matches Phase 49-58 pattern: check `process.argv.includes('--verbose')`)
- COLLISION_MATRIX + RECIPROCITY_PAIRS hardcoded from `59-05-COLLISION-MATRIX.md` (plan-author transcribes ALL pairs from that artifact)
- Pin Mode-tag regex literally per V-59-26 forbidden list (`[BYOD]`/`[ZTE]`/`[AOSP]`/`[Knox]`/`[All GMS]`/`[22.04]`/`[24.04]`)
- Pin Linux Compliance Category Reference 4 anchors per V-59-30 (`#step-2-configure-allowed-distributions` etc. — re-verify slugs at edit-time per RESEARCH §Pattern 3)
- Pin V-59-31 PITFALL-7 forbidden tokens: `if/then`, `fi`, `exit 0`, `exit 1`, `#!/bin/bash`, `compliance-evaluation-cadence`

**Reference template (read for verbatim sliceH2Region + assert runner pattern):**
The Phase 58 validator at `scripts/validation/check-phase-58.mjs` is the closest structural template. Open it with the Read tool and reuse the helpers + runner pattern. Adapt the assertions to the 36 V-59-NN list above. Phase 49 validator (`check-phase-49.mjs`) has the closest precedent for H3-extraction + see-also-line patterns.

**Self-test:**
After authoring, run the validator script from repo root using the Bash tool. Expected: 36/36 PASS (Plan 59-08 commits validator after Plans 59-02..59-07 have landed Wave 2/3 content). If any FAIL, plan-author investigates: either the validator regex is wrong (fix validator) or the content from prior plan is incorrect (route fix to that plan; plan 59-08 does NOT modify content per scope).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='scripts/validation/check-phase-59.mjs';if(!fs.existsSync(p)){console.error('Validator file missing');process.exit(1)}const c=fs.readFileSync(p,'utf8');const required=['#!/usr/bin/env node','fs.readFileSync','function sliceH2Region','function sliceH3Region','V-59-01','V-59-07','V-59-10','V-59-11','V-59-12','V-59-13','V-59-14','V-59-15','V-59-19','V-59-20','V-59-22','V-59-24','V-59-25','V-59-26','V-59-27','V-59-28','V-59-29','V-59-30','V-59-31','V-59-32','V-59-33','V-59-36','## Linux Provisioning','## Operations','## Linux Quick Reference','## Co-Management','Operational Depth','linux-intune-portal-enrollment.md','Allowed Distributions','Custom Compliance','Device Encryption','Password Policy','journalctl','File-based','Package-state','#step-2-configure-allowed-distributions','#step-5-configure-password-policy','process.exit'];const missing=required.filter(s=>!c.includes(s));if(missing.length){console.error('Missing literals/V-NN-NN ids in validator:',missing);process.exit(1)}console.log('check-phase-59.mjs structural inspection passes (all 36 V-59-NN ids + literal pins present)');process.exit(0);"</automated>
  </verify>
  <done>scripts/validation/check-phase-59.mjs exists; structural inspection finds all 36 V-59-NN ids + locked literal pins (anchors / negative tokens / category names / step anchors); separate manual run of `node scripts/validation/check-phase-59.mjs` exits 0 with 36/36 V-59-NN PASS post-Wave-2/3 content landing.</done>
</task>

<task type="auto">
  <name>Task 2: Self-test the validator (run check-phase-59.mjs and confirm 36/36 PASS)</name>
  <files></files>
  <read_first>
    - scripts/validation/check-phase-59.mjs (the validator created in Task 1)
    - All 8 hub-and-glossary files modified by Plans 59-02 through 59-07 (verify content is in place)
  </read_first>
  <action>
Run the validator from repo root:
```
node scripts/validation/check-phase-59.mjs --verbose
```

Expected output: 36/36 V-59-NN PASS lines + final `36/36 V-59-NN assertions PASS (0 FAIL)` summary.

If any V-59-NN FAILs, diagnose:
1. **Validator regex is wrong:** Fix the regex in check-phase-59.mjs (Task 1 deliverable; this plan owns the validator file).
2. **Content from prior plan is incorrect:** Route the fix to the owning plan (e.g., V-59-29 fail → fix Plan 59-07 quick-ref-l2.md content; V-59-22 fail → fix Plan 59-05 see-also reciprocity content). Plan 59-08 does NOT modify content beyond the validator file itself per scope.

If all 36 PASS: validator self-test green. Plan 59-09 may now run the pre-commit gate (Wave 5).

If any FAIL persists despite fixes: escalate via Plan 59-08 SUMMARY documenting the FAILing assertions for Plan 59-09 to triage at close-gate time.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-59.mjs --verbose</automated>
  </verify>
  <done>`node scripts/validation/check-phase-59.mjs --verbose` exits 0 with 36/36 V-59-NN PASS; if any FAIL persists, the failing assertion IDs are documented in 59-08-SUMMARY.md for Plan 59-09 triage.</done>
</task>

</tasks>

<verification>
- `scripts/validation/check-phase-59.mjs` exists; ESM module with shebang
- Total V-59-NN assertion count = 36 (V-59-01..36 per CONTEXT D-26 enumeration)
- File-reads-only / regex-based / no-shared-module / no-AST per D-27
- All 36 V-59-NN PASS at runtime (post-Wave-2/3 content landing)
- `process.exit(0)` on all PASS; `process.exit(1)` on any FAIL
- Hardcoded literal pins per D-30: H2/H3 anchors, NEGATIVE tokens (Operational Depth, linux-intune-portal-enrollment.md, Bash syntax forbidden list, Mode tags), category literals (Allowed Distributions / Custom Compliance / Device Encryption / Password Policy), 3 method literals (journalctl / File-based / Package-state), 4 step anchors (#step-2..5)
- Validator self-test green (36/36 PASS) post-Wave-2/3 plans
</verification>

<success_criteria>
- VALIDATOR_GREEN phase-level truth satisfied: validator exits 0 with 36/36 V-59-NN PASS
- D-26 / D-27 / D-30 all satisfied
- AUDIT-06 mandate (validator-as-deliverable per phase) satisfied; lineage extended (Phase 49-58 → Phase 59)
- Plan 59-09 pre-commit gate has its primary green-gate target
</success_criteria>

<output>
After completion, create `.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-08-SUMMARY.md` documenting:
1. Total V-59-NN assertion count = 36 (V-59-01..36)
2. Self-test result: PASS count / FAIL count / SKIPPED count (expected 36/0/0 post-Wave-2/3)
3. Confirmation that validator uses fs.readFileSync + regex only (no AST per D-27)
4. Confirmation that COLLISION_MATRIX + RECIPROCITY_PAIRS hardcoded from 59-05-COLLISION-MATRIX.md
5. List of locked literal pins per D-30 (H2/H3 anchors, NEGATIVE tokens, category literals, step anchors)
6. Note for Plan 59-09 (close gate): "check-phase-59.mjs is GREEN with 36/36 V-59-NN PASS; pre-commit gate (3 validators + file-level checks per D-29) may now run."
</output>
