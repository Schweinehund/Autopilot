---
phase: 59
plan: 59-05
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - docs/_glossary.md
  - docs/_glossary-macos.md
  - docs/_glossary-android.md
  - docs/_glossary-linux.md
files_created:
  - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md
requirements: [CLEAN-08]
autonomous: true

must_haves:
  truths:
    - "All 4 platform glossaries (`docs/_glossary.md` Windows, `docs/_glossary-macos.md` Apple/macOS+iOS, `docs/_glossary-android.md` Android, `docs/_glossary-linux.md` Linux) contain reciprocal `> See also: [Term](other-glossary.md#anchor) (Other-Platform); ...` lines as the LAST line of existing per-term cross-platform blockquotes for the locked collision-term matrix (D-14 / D-15)"
    - "Reciprocity contract is BIDIRECTIONAL and TRANSITIVELY COMPLETE: if Term-X exists in glossaries A, B, and C, A's entry references B and C; B references A and C; C references A and B (D-17)"
    - "Pattern alignment normalization: all 4 glossaries use `> **Cross-platform note:**` as the blockquote shape for NEW see-also-bearing blockquotes (currently used by Android-23 + Linux-9; new ones added to Windows _glossary.md follow this shape per D-15)"
    - "Existing macOS `> **Windows equivalent:**` blockquotes (11 instances) are PRESERVED VERBATIM with see-also lines APPENDED inside (do NOT rename to `> **Cross-platform note:**` — preserves Phase 23-24 mature prose per D-15)"
    - "GLOSSARY_RECIPROCITY phase-level truth satisfied: validator V-59-20..23 PASS for all locked-matrix terms"
    - "See-also lines APPENDED inside existing `>` blockquotes (preceded by another `>`-prefixed line within the H3 region — A3 blockquote integrity per D-19); V-59-24 NEGATIVE check passes"
    - "4-File vs 5-File framing — `_glossary-macos.md` IS the iOS glossary per D-18 + REQUIREMENTS.md:144; NO new `_glossary-ios.md` file created"
    - "Phase 49 LIN-02 doc-level banner-link reciprocity (already-shipped at `_glossary.md:11` / `_glossary-macos.md:10` / `_glossary-android.md:12` / `_glossary-linux.md:10`) is PRESERVED byte-identical (D-20 — Phase 59 delta is per-term see-also only, NOT banner re-work)"

  artifacts:
    - path: ".planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md"
      provides: "Locked collision-term matrix authored as Plan 59-05 first action per D-16 plan-time selection mandate; documents which terms exist in which glossaries; basis for V-59-20..23 reciprocity assertions in Plan 59-08 validator"
      contains:
        - "# Phase 59 Collision-Term Matrix"
        - "Captured:"
        - "## Matrix"
        - "| Term"
        - "Windows"
        - "Apple (macOS+iOS)"
        - "Android"
        - "Linux"
        - "## Anchor Map"
        - "## Reciprocity Pairs"
    - path: "docs/_glossary.md"
      provides: "Windows glossary with NEW `> **Cross-platform note:**` blockquotes for collision-matrix terms; each new blockquote contains see-also line per D-15 shape"
      contains:
        - "> **Cross-platform note:**"
        - "> See also:"
    - path: "docs/_glossary-macos.md"
      provides: "Apple glossary with see-also lines APPENDED inside existing `> **Windows equivalent:**` blockquotes (11 preserved verbatim); also inside any pre-existing `> **Cross-platform note:**` if present"
      contains:
        - "> **Windows equivalent:**"
        - "> See also:"
    - path: "docs/_glossary-android.md"
      provides: "Android glossary with see-also lines APPENDED inside existing `> **Cross-platform note:**` blockquotes (23 preserved verbatim)"
      contains:
        - "> **Cross-platform note:**"
        - "> See also:"
    - path: "docs/_glossary-linux.md"
      provides: "Linux glossary with see-also lines APPENDED inside existing `> **Cross-platform note:**` blockquotes (9 preserved verbatim)"
      contains:
        - "> **Cross-platform note:**"
        - "> See also:"

  key_links:
    - from: ".planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md"
      to: "docs/_glossary.md / _glossary-macos.md / _glossary-android.md / _glossary-linux.md (4 glossaries)"
      via: "Term row references each glossary's H3 anchor; reciprocity pair list lock"
      pattern: "_glossary(-(macos|android|linux))?\\.md#"
    - from: "docs/_glossary-{any}.md per-term blockquote"
      to: "docs/_glossary-{other}.md#anchor (cross-platform see-also)"
      via: "`> See also: [Term](other-glossary.md#anchor) (Other-Platform); ...` line APPENDED inside existing `>` blockquote"
      pattern: ">\\s+See also:\\s+\\[[^\\]]+\\]\\(_glossary[^)]+\\)\\s*\\([^)]+\\)"
---

<objective>
Normalize CLEAN-08 see-also reciprocity across all 4 platform glossaries (`_glossary.md` Windows, `_glossary-macos.md` Apple/macOS+iOS, `_glossary-android.md` Android, `_glossary-linux.md` Linux) by appending `> See also: [Term](other-glossary.md#anchor) (Other-Platform); ...` lines as the LAST line of existing per-term cross-platform blockquotes for the locked collision-term matrix. Per D-15 LOCKED shape: existing `> **Windows equivalent:**` macOS blockquotes are PRESERVED VERBATIM (see-also lines APPENDED INSIDE); existing `> **Cross-platform note:**` Android+Linux blockquotes are EXTENDED in place. Windows _glossary.md (currently 0 cross-platform blockquotes) gets NEW `> **Cross-platform note:**` blockquotes for applicable matrix terms.

Plan 59-05 is Wave 2; depends on 59-01 (pre-edit anchor inventory must exist); ships D-14 / D-15 / D-16 / D-17 / D-18 / D-19 / D-20 mandates.

Purpose: Implements ROADMAP Phase 59 SC#2 PRIMARY (5-platform glossary reciprocal see-also entries — extends v1.4 macOS↔Android pattern) — the GLOSSARY_RECIPROCITY phase-level truth + the only requirement-mapped deliverable in Phase 59 (CLEAN-08). Implements V-59-20 / V-59-21 / V-59-22 / V-59-23 / V-59-24 validator targets.

Output: 1 new artifact (`59-05-COLLISION-MATRIX.md`) + 4 modified glossary files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-ANCHOR-INVENTORY.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
**Files this plan creates / modifies:**
- CREATE: `.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md` (locked collision-term matrix per D-16 plan-time mandate)
- MODIFY: `docs/_glossary.md` — ADD new `> **Cross-platform note:**` blockquotes for matrix terms (currently has 0 cross-platform blockquotes)
- MODIFY: `docs/_glossary-macos.md` — APPEND see-also lines INSIDE existing 11 `> **Windows equivalent:**` blockquotes (preserve label verbatim per D-15 anti-rename)
- MODIFY: `docs/_glossary-android.md` — APPEND see-also lines INSIDE existing 23 `> **Cross-platform note:**` blockquotes
- MODIFY: `docs/_glossary-linux.md` — APPEND see-also lines INSIDE existing 9 `> **Cross-platform note:**` blockquotes

**Direct templates (read for verbatim shape mirror; DO NOT modify):**
- `docs/_glossary-android.md` COBO entry blockquote (RESEARCH §Pattern 5 verbatim sample) — `> **Cross-platform note:**` shape
- `docs/_glossary-macos.md` Account-Driven User Enrollment entry — `> **Windows equivalent:**` shape (PRESERVE label verbatim)
- `docs/_glossary-linux.md` dm-crypt entry — example of cross-platform encryption-triple pre-existing prose
- Phase 49 LIN-02 commit-2 banner-link entries at `_glossary.md:11` / `_glossary-macos.md:10` / `_glossary-android.md:12` / `_glossary-linux.md:10` — D-20 already-shipped reference; do NOT re-touch

**Sources for collision-matrix selection (D-16 plan-author seed sources):**
- Phase 49 D-13 9 LIN-02 absent-concept terms: Supervision, DPC, Work Profile, COBO/COPE/WPCO, MGP, ZTE, VPP, Hardware Hash, ABM
- Cross-platform encryption-compliance triple: BitLocker (Windows) / FileVault (Apple) / dm-crypt (Linux)
- Cross-platform CA-channel pair: web-app CA (Linux) ↔ MAM-WE (iOS) — both as "compliance-lite" patterns per D-16
- Enrollment-channel triple: Autopilot (Windows) / ADE (Apple) / ZTE (Android)
- `docs/reference/linux-capability-matrix.md` Cross-Platform Equivalences section (lines 71-93) — additional cross-platform-equivalent term sources

**Sibling Phase 59 plans (parallel-safe — disjoint files):**
- 59-02 edits docs/index.md (disjoint)
- 59-03 edits docs/operations/00-index.md (disjoint)
- 59-06 edits docs/quick-ref-l1.md (disjoint)
- 59-07 edits docs/quick-ref-l2.md (disjoint)
- 59-04 edits docs/index.md in Wave 3 (disjoint files)
</interfaces>
</context>

<threat_model>
N/A — documentation-only phase, no attack surface introduced. Glossary cross-reference edits with no input validation, network exposure, or sensitive-data handling.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author the locked collision-term matrix artifact (D-16 first-action mandate)</name>
  <files>.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md</files>
  <read_first>
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-16 plan-time matrix selection mandate; D-17 bidirectional + transitive completeness; D-18 4-file equivalent)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md (§Pattern 5 glossary blockquote shapes; §specifics see-also line shape regex)
    - docs/_glossary.md (FULL FILE — enumerate all 38 H3 entries; identify which match collision-matrix candidates; verify 0 cross-platform blockquotes)
    - docs/_glossary-macos.md (FULL FILE — enumerate all 11 H3 entries; verify 11 `> **Windows equivalent:**` blockquotes; this is the Apple glossary covering BOTH macOS AND iOS)
    - docs/_glossary-android.md (FULL FILE — enumerate all 24 H3 entries; verify 23 `> **Cross-platform note:**` blockquotes)
    - docs/_glossary-linux.md (FULL FILE — enumerate all 30 H3 entries; verify 9 `> **Cross-platform note:**` blockquotes; check for any "see also" prose)
    - docs/reference/linux-capability-matrix.md lines 71-93 (Cross-Platform Equivalences section — supplemental cross-platform-equivalent term source per D-16)
  </read_first>
  <action>
Create `.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md` with this exact structure. The matrix is the auditable input to Plan 59-08 validator V-59-20..23 reciprocity assertions.

Plan-author selects the matrix (D-16 plan-time gray area). Recommended seed (apply judgment to add/remove based on actual H3 entries discovered in glossaries):

```markdown
# Phase 59 Collision-Term Matrix

**Captured:** {today's date}
**Owner:** Plan 59-05 (per D-16 plan-time matrix selection mandate)
**Purpose:** Locked collision-term matrix for CLEAN-08 see-also reciprocity. This artifact lists which cross-platform-equivalent terms exist in which of the 4 glossaries; basis for V-59-20..23 bidirectional reciprocity assertions in Plan 59-08 validator (`scripts/validation/check-phase-59.mjs`).

## Methodology

Per D-16, matrix sourced from:
1. Phase 49 D-13 9 LIN-02 absent-concept terms (Supervision, DPC, Work Profile, COBO/COPE/WPCO, MGP, ZTE, VPP, Hardware Hash, ABM)
2. Cross-platform encryption-compliance triple (BitLocker / FileVault / dm-crypt)
3. Cross-platform CA-channel pair (web-app CA Linux ↔ MAM-WE iOS as "compliance-lite" analogs)
4. Enrollment-channel triple (Autopilot / ADE / ZTE)
5. `docs/reference/linux-capability-matrix.md` Cross-Platform Equivalences section (lines 71-93)

Per D-17, reciprocity is BIDIRECTIONAL and TRANSITIVELY COMPLETE: every glossary listing a term references EVERY OTHER glossary listing the term.

Per D-18, the 4-file equivalent applies — `_glossary-macos.md` IS the iOS glossary per REQUIREMENTS.md:144.

## Matrix

Columns mark which glossaries list the term (✓ = entry exists; — = not in this glossary).

| Term (canonical) | Windows (`_glossary.md`) | Apple (`_glossary-macos.md`) | Android (`_glossary-android.md`) | Linux (`_glossary-linux.md`) | Cross-platform Equivalent / Notes |
|------------------|--------------------------|------------------------------|----------------------------------|------------------------------|----------------------------------|
| BitLocker / FileVault / dm-crypt (encryption triple) | ✓ (BitLocker) | ✓ (FileVault) | — | ✓ (dm-crypt) | Encryption-compliance signal across 3 platforms; Android Intune does not expose dm-crypt as compliance signal |
| Autopilot / ADE / ZTE (enrollment-channel triple) | ✓ (Autopilot) | ✓ (ADE) | ✓ (ZTE) | — | Linux has no zero-touch enrollment equivalent (user-initiated only) |
| Supervision (Apple) — partial Android analog | — | ✓ (Supervision) | ✓ (Supervision disambiguation stub pointing to macOS) | — | Apple supervision is permanent per-device gating; Android COBO is ownership-mode designation (different semantics) |
| Hardware Hash | ✓ (Hardware Hash) | — | — | — | Windows-only Autopilot concept; no cross-platform equivalent |
| ABM / Apple Business Manager | — | ✓ (ABM) | — | — | Apple-only; no equivalent in Windows/Android/Linux |
| VPP / Volume Purchase Program | — | ✓ (VPP) | — | — | Apple-only app-licensing flow; Android MGP private apps and Windows Microsoft Store for Business are tangential analogs |
| Work Profile / WPCO | — | — | ✓ (Work Profile, WPCO) | — | Android-specific BYOD container; iOS Account-Driven User Enrollment is closest functional analog |
| COBO / COPE / WPCO (ownership modes) | — | — | ✓ (COBO, COPE, WPCO) | — | Android-specific ownership-mode taxonomy; partially analogous to Apple ADE-supervised |
| DPC (Device Policy Controller) | — | — | ✓ (DPC) | — | Android-specific; Apple has analogous Setup Assistant + Configuration Profiles, but DPC is a distinct architectural pattern |
| MGP (Managed Google Play) | — | — | ✓ (MGP) | — | Android-specific app-distribution channel; analogous to Apple VPP and Windows Microsoft Store for Business |
| Web-app CA (Linux) ↔ MAM-WE (iOS) | — | ✓ (MAM-WE) | — | ✓ (web-app CA reference) | Both as "compliance-lite" patterns per CONTEXT D-16 — app-layer enforcement without device-level MDM |

(Plan-author MAY add/remove rows based on actual glossary H3 inventory; aim for ~10-18 terms per CONTEXT D-16 estimate. Each row MUST have at least 2 ✓ marks to qualify as "cross-platform equivalent" — single-platform terms like Hardware Hash are LISTED for reference but do NOT generate see-also lines.)

## Anchor Map

For each term row above with ≥2 ✓ marks, the anchor map records the H3 GFM-derived slug in each listing glossary. Plan-author computes these by reading the actual H3 heading text and applying GFM kebab-case rules.

| Term | Glossary | H3 Heading (verbatim) | GFM Slug |
|------|----------|----------------------|----------|
| BitLocker | `_glossary.md` | `### BitLocker` (or as discovered) | `#bitlocker` |
| FileVault | `_glossary-macos.md` | `### FileVault` (or as discovered) | `#filevault` |
| dm-crypt | `_glossary-linux.md` | `### dm-crypt` (or as discovered) | `#dm-crypt` |
| Autopilot | `_glossary.md` | `### Autopilot` (or as discovered) | `#autopilot` |
| ADE | `_glossary-macos.md` | `### ADE` (Automated Device Enrollment) | `#ade` |
| ZTE | `_glossary-android.md` | `### ZTE` (Zero-Touch Enrollment) | `#zte` |
| Supervision | `_glossary-macos.md` | `### Supervision` | `#supervision` |
| Supervision (Android stub) | `_glossary-android.md` | `### Supervision` (disambiguation pointing to macOS) | `#supervision` |
| ... | ... | ... | ... |

(Plan-author fills in actual GFM slugs from real H3 text in each glossary. If a term in a different glossary uses a different H3 heading text — e.g., Apple's `### ABM (Apple Business Manager)` vs Android's reference `### ABM` — record both verbatim; the see-also link uses the canonical anchor in the target file.)

## Reciprocity Pairs

For each term in the matrix with ≥2 ✓ marks, list ALL bidirectional pairs that must be appended (Cartesian product of listing glossaries). This is the validator V-59-22 bidirectional pair check input.

| Term | Source Glossary | Source Anchor | Target Glossary | Target Anchor |
|------|-----------------|---------------|-----------------|---------------|
| Encryption | `_glossary.md` | `#bitlocker` | `_glossary-macos.md` | `#filevault` |
| Encryption | `_glossary.md` | `#bitlocker` | `_glossary-linux.md` | `#dm-crypt` |
| Encryption | `_glossary-macos.md` | `#filevault` | `_glossary.md` | `#bitlocker` |
| Encryption | `_glossary-macos.md` | `#filevault` | `_glossary-linux.md` | `#dm-crypt` |
| Encryption | `_glossary-linux.md` | `#dm-crypt` | `_glossary.md` | `#bitlocker` |
| Encryption | `_glossary-linux.md` | `#dm-crypt` | `_glossary-macos.md` | `#filevault` |
| Enrollment | `_glossary.md` | `#autopilot` | `_glossary-macos.md` | `#ade` |
| Enrollment | `_glossary.md` | `#autopilot` | `_glossary-android.md` | `#zte` |
| Enrollment | `_glossary-macos.md` | `#ade` | `_glossary.md` | `#autopilot` |
| Enrollment | `_glossary-macos.md` | `#ade` | `_glossary-android.md` | `#zte` |
| Enrollment | `_glossary-android.md` | `#zte` | `_glossary.md` | `#autopilot` |
| Enrollment | `_glossary-android.md` | `#zte` | `_glossary-macos.md` | `#ade` |
| ... | ... | ... | ... | ... |

(Plan-author writes ALL pairs from the matrix; this is the auditable contract Plan 59-08 V-59-22 validates.)

## Estimated see-also line count

| Glossary | Number of see-also lines to append | Notes |
|----------|------------------------------------|-------|
| `_glossary.md` | {plan-author counts; ~5-10 expected} | NEW `> **Cross-platform note:**` blockquotes added for terms not currently in cross-platform blockquote shape |
| `_glossary-macos.md` | {plan-author counts; ~5-10 expected} | APPENDED inside existing 11 `> **Windows equivalent:**` blockquotes (preserve label verbatim per D-15) |
| `_glossary-android.md` | {plan-author counts; ~10-15 expected} | APPENDED inside existing 23 `> **Cross-platform note:**` blockquotes |
| `_glossary-linux.md` | {plan-author counts; ~5-10 expected} | APPENDED inside existing 9 `> **Cross-platform note:**` blockquotes |

**Total reciprocal pairs validated by V-59-22:** {plan-author counts}

This matrix is locked at Plan 59-05 commit-1; Task 2 / 3 / 4 / 5 use it as the authoritative input.
```

Replace every `{...}` placeholder with concrete values derived from actual glossary H3 inventory reads.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md';if(!fs.existsSync(p)){console.error('Collision matrix file missing');process.exit(1)}const c=fs.readFileSync(p,'utf8');const required=['# Phase 59 Collision-Term Matrix','Captured:','## Matrix','## Anchor Map','## Reciprocity Pairs','Windows','Apple','Android','Linux','_glossary.md','_glossary-macos.md','_glossary-android.md','_glossary-linux.md'];const missing=required.filter(s=>!c.includes(s));if(missing.length){console.error('Missing literals:',missing);process.exit(1)}const matrixSection=c.split('## Matrix')[1]||'';const tickCount=(matrixSection.match(/✓/g)||[]).length;if(tickCount<6){console.error('Matrix has fewer than 6 ✓ marks ('+tickCount+'); expected ~20+ across ~10 cross-platform terms');process.exit(1)}console.log('Collision matrix artifact valid; ✓ count='+tickCount);process.exit(0);"</automated>
  </verify>
  <done>59-05-COLLISION-MATRIX.md exists with Matrix + Anchor Map + Reciprocity Pairs sections; matrix lists ≥10 terms with ≥2 ✓ marks each; node verification check exits 0; subsequent tasks (2-5) may now use this matrix as authoritative input.</done>
</task>

<task type="auto">
  <name>Task 2: Append see-also lines inside existing macOS `> **Windows equivalent:**` blockquotes (D-15 preserve-label discipline)</name>
  <files>docs/_glossary-macos.md</files>
  <read_first>
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md (Reciprocity Pairs section — locked input)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-15 PRESERVE `> **Windows equivalent:**` verbatim; D-19 A1+A2+A3 assertions)
    - docs/_glossary-macos.md (FULL FILE — locate each H3 region with `> **Windows equivalent:**` blockquote; identify the LAST `>` line of each blockquote)
    - docs/_glossary.md (target file for see-also link anchor verification)
    - docs/_glossary-android.md (target file for see-also link anchor verification)
    - docs/_glossary-linux.md (target file for see-also link anchor verification)
  </read_first>
  <action>
For each macOS H3 entry that appears in the locked collision matrix (Task 1), append a `> See also: ...` line as the LAST line of its existing `> **Windows equivalent:**` blockquote (or, if no `> **Windows equivalent:**` exists for a matrix-term H3, ADD a new `> **Cross-platform note:**` blockquote with see-also content).

**D-15 LOCKED line shape:**
```
> See also: [Term-Name](_glossary.md#anchor) (Windows); [Term-Name](_glossary-android.md#anchor) (Android); [Term-Name](_glossary-linux.md#anchor) (Linux).
```

(List each glossary the term appears in, except the source glossary itself — i.e., a macOS entry's see-also references Windows/Android/Linux as applicable per the matrix; not self-references.)

**Critical rules (D-15 + D-19 + Pitfalls 7-8):**
- DO NOT rename `> **Windows equivalent:**` to `> **Cross-platform note:**` — preserve verbatim (Pitfall 7)
- DO NOT create a NEW `> **Cross-platform note:**` blockquote when an existing `> **Windows equivalent:**` blockquote is present — APPEND see-also INSIDE the existing blockquote
- The see-also line MUST start with `> ` (space after `>`); MUST be the LAST line of the blockquote (preserves all prior prose)
- The see-also line MUST be PRECEDED by another `>`-prefixed line within the H3 region (A3 blockquote integrity check; Pitfall 8 — see-also line OUTSIDE blockquote breaks V-59-24 NEGATIVE)
- Use `(Apple)` or `(Apple/macOS+iOS)` to label the macOS source where another glossary's see-also references this file; for the see-also line WITHIN macOS, list the OTHER platforms by their natural names: `(Windows)`, `(Android)`, `(Linux)`
- Verify each `_glossary-XXX.md#anchor` target's GFM slug per the Anchor Map in the matrix artifact (A2 anchor-correctness)

**Example for the FileVault H3 entry (assuming it has `> **Windows equivalent:**`):**

Existing (verbatim — DO NOT modify):
```markdown
### FileVault

[existing prose ...]

> **Windows equivalent:** [existing 2-3 sentence prose comparing FileVault to BitLocker, e.g., disk-encryption signal surfaced as compliance...]
```

Edit (append a single `> See also:` line as the LAST `>` line):
```markdown
### FileVault

[existing prose ...]

> **Windows equivalent:** [existing 2-3 sentence prose comparing FileVault to BitLocker, e.g., disk-encryption signal surfaced as compliance...]
> See also: [BitLocker](_glossary.md#bitlocker) (Windows); [dm-crypt](_glossary-linux.md#dm-crypt) (Linux).
```

(Notice no blank line between existing prose and see-also — both lines are part of the SAME `>` blockquote.)

**For matrix terms where the macOS H3 has NO existing cross-platform blockquote:**
Add a NEW `> **Cross-platform note:**` blockquote at an appropriate position (immediately before any `> **Windows equivalent:**` if one exists, or at the END of the H3 body region if none) following this shape:
```markdown
> **Cross-platform note:** [1-2 sentence disambiguation prose authored by plan-author]
> See also: [Term](_glossary.md#anchor) (Windows); [Term](_glossary-android.md#anchor) (Android); [Term](_glossary-linux.md#anchor) (Linux).
```

(Plan-author writes the disambiguation prose; aim for tight 1-2 sentences matching mature Phase 23-24 voice.)

Iterate through every macOS H3 in the locked matrix per the Reciprocity Pairs in Task 1's artifact.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/_glossary-macos.md','utf8');const errors=[];const winEqCount=(c.match(/^> \\*\\*Windows equivalent:/gm)||[]).length;if(winEqCount<11)errors.push('Existing > **Windows equivalent:** blockquotes count='+winEqCount+' (expected ≥11 — preserve verbatim per D-15)');const seeAlsoCount=(c.match(/^> See also:/gm)||[]).length;if(seeAlsoCount===0)errors.push('No > See also: lines found in _glossary-macos.md (Plan 59-05 Task 2 incomplete)');const lines=c.split('\\n');for(let i=0;i<lines.length;i++){if(/^> See also:/.test(lines[i])){if(i===0||!/^>/.test(lines[i-1])){errors.push('Line '+(i+1)+' \"> See also:\" not preceded by another > line (V-59-24 NEGATIVE / Pitfall 8 violation)');}}}if(/^> \\*\\*Cross-platform note:/m.test(c)){console.log('Note: NEW > **Cross-platform note:** blockquotes added for matrix terms without existing > **Windows equivalent:** (acceptable per D-15)');}if(errors.length){console.error('ERRORS:',errors.join('; '));process.exit(1);}console.log('macOS see-also: '+seeAlsoCount+' lines appended; '+winEqCount+' > **Windows equivalent:** preserved verbatim');process.exit(0);"</automated>
  </verify>
  <done>docs/_glossary-macos.md contains see-also lines APPENDED inside existing `> **Windows equivalent:**` blockquotes for each matrix-term H3 in macOS; existing `> **Windows equivalent:**` count preserved (≥11 — verbatim); every see-also line preceded by another `>` line (A3 integrity); node verification check exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Append see-also lines inside existing Android `> **Cross-platform note:**` blockquotes</name>
  <files>docs/_glossary-android.md</files>
  <read_first>
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-15)
    - docs/_glossary-android.md (FULL FILE — locate each H3 region with `> **Cross-platform note:**` blockquote)
    - docs/_glossary.md / _glossary-macos.md / _glossary-linux.md (target anchor verification)
  </read_first>
  <action>
Same pattern as Task 2 but for Android. For each Android H3 entry in the locked collision matrix:

- If existing `> **Cross-platform note:**` blockquote present: APPEND `> See also: ...` line as LAST line of blockquote
- If NO existing cross-platform blockquote for a matrix-term H3: ADD a new `> **Cross-platform note:**` blockquote (1-2 sentence disambiguation prose) + see-also line

**Example for the COBO H3 entry (existing `> **Cross-platform note:**` per RESEARCH §Pattern 5):**

Existing:
```markdown
### COBO (Corporate-Owned, Business-Only)

[existing definition prose ...]

> **Cross-platform note:** No exact Windows/macOS/iOS term; the closest analog is iOS ADE-supervised
> corporate-owned enrollment through [ABM](_glossary-macos.md#abm) (see [iOS Supervision](_glossary-macos.md#supervision))
> or macOS ADE-enrolled supervised. On Windows, Autopilot corporate-owned deployment is the closest analog.
> Mapping is partial — "corporate-owned" has a cross-platform meaning but the per-platform management scopes
> differ. Do not conflate COBO with iOS supervision state; supervision is a permanent per-device gating state
> on iOS, whereas COBO is an ownership-mode designation on Android.
```

Edit (append a single `> See also:` line as the LAST `>` line; preserve all existing prose verbatim):
```markdown
### COBO (Corporate-Owned, Business-Only)

[existing definition prose ...]

> **Cross-platform note:** No exact Windows/macOS/iOS term; the closest analog is iOS ADE-supervised
> corporate-owned enrollment through [ABM](_glossary-macos.md#abm) (see [iOS Supervision](_glossary-macos.md#supervision))
> or macOS ADE-enrolled supervised. On Windows, Autopilot corporate-owned deployment is the closest analog.
> Mapping is partial — "corporate-owned" has a cross-platform meaning but the per-platform management scopes
> differ. Do not conflate COBO with iOS supervision state; supervision is a permanent per-device gating state
> on iOS, whereas COBO is an ownership-mode designation on Android.
> See also: [ADE](_glossary-macos.md#ade) (Apple); [Supervision](_glossary-macos.md#supervision) (Apple); [Autopilot](_glossary.md#autopilot) (Windows).
```

Same critical rules as Task 2:
- DO NOT modify any existing `> **Cross-platform note:**` prose verbatim — APPEND see-also INSIDE
- See-also line starts with `> ` (preserves blockquote integrity); preceded by another `>` line (A3 check)
- Each see-also link target verifies against locked Anchor Map (A2 check)

Iterate through every Android H3 in the locked collision matrix.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/_glossary-android.md','utf8');const errors=[];const cpNoteCount=(c.match(/^> \\*\\*Cross-platform note:/gm)||[]).length;if(cpNoteCount<23)errors.push('Existing > **Cross-platform note:** blockquotes count='+cpNoteCount+' (expected ≥23 — preserve verbatim)');const seeAlsoCount=(c.match(/^> See also:/gm)||[]).length;if(seeAlsoCount===0)errors.push('No > See also: lines found in _glossary-android.md');const lines=c.split('\\n');for(let i=0;i<lines.length;i++){if(/^> See also:/.test(lines[i])){if(i===0||!/^>/.test(lines[i-1])){errors.push('Line '+(i+1)+' \"> See also:\" not preceded by another > line (V-59-24 violation)');}}}if(errors.length){console.error('ERRORS:',errors.join('; '));process.exit(1);}console.log('Android see-also: '+seeAlsoCount+' lines appended; '+cpNoteCount+' > **Cross-platform note:** preserved verbatim');process.exit(0);"</automated>
  </verify>
  <done>docs/_glossary-android.md contains see-also lines APPENDED inside existing `> **Cross-platform note:**` blockquotes for each matrix-term H3; existing 23 blockquotes preserved verbatim; A3 blockquote integrity preserved; node check exits 0.</done>
</task>

<task type="auto">
  <name>Task 4: Append see-also lines inside existing Linux `> **Cross-platform note:**` blockquotes</name>
  <files>docs/_glossary-linux.md</files>
  <read_first>
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-15)
    - docs/_glossary-linux.md (FULL FILE — verify dm-crypt entry has cross-platform-note blockquote per RESEARCH §Pattern 5)
    - docs/_glossary.md / _glossary-macos.md / _glossary-android.md (target anchor verification)
  </read_first>
  <action>
Same pattern as Tasks 2-3 but for Linux. For each Linux H3 entry in the locked collision matrix (likely includes dm-crypt + LUKS + web-app CA + others per matrix):

- Existing 9 `> **Cross-platform note:**` blockquotes: APPEND `> See also: ...` line as LAST line; preserve verbatim
- New matrix terms without cross-platform blockquote: ADD new `> **Cross-platform note:**` blockquote + see-also line

**Example for the dm-crypt entry (existing per RESEARCH §Pattern 5):**

Existing:
```markdown
### dm-crypt

[existing definition prose ...]

> **Cross-platform note:** On Windows, the analog is BitLocker (drive-level encryption surfaced as a
> compliance signal). On macOS, the analog is FileVault (full-disk XTS-AES encryption). Android uses
> dm-crypt internally on some hardware but Intune does not expose Android dm-crypt as a compliance signal
> — see the [Android Enterprise Provisioning Glossary](_glossary-android.md). Do not conflate the
> platform-specific encryption surfaces.
```

Edit (append `> See also:` line):
```markdown
### dm-crypt

[existing definition prose ...]

> **Cross-platform note:** On Windows, the analog is BitLocker (drive-level encryption surfaced as a
> compliance signal). On macOS, the analog is FileVault (full-disk XTS-AES encryption). Android uses
> dm-crypt internally on some hardware but Intune does not expose Android dm-crypt as a compliance signal
> — see the [Android Enterprise Provisioning Glossary](_glossary-android.md). Do not conflate the
> platform-specific encryption surfaces.
> See also: [BitLocker](_glossary.md#bitlocker) (Windows); [FileVault](_glossary-macos.md#filevault) (Apple).
```

Same critical rules as Tasks 2-3.

Iterate through every Linux H3 in the locked collision matrix.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/_glossary-linux.md','utf8');const errors=[];const cpNoteCount=(c.match(/^> \\*\\*Cross-platform note:/gm)||[]).length;if(cpNoteCount<9)errors.push('Existing > **Cross-platform note:** blockquotes count='+cpNoteCount+' (expected ≥9 — preserve verbatim)');const seeAlsoCount=(c.match(/^> See also:/gm)||[]).length;if(seeAlsoCount===0)errors.push('No > See also: lines found in _glossary-linux.md');const lines=c.split('\\n');for(let i=0;i<lines.length;i++){if(/^> See also:/.test(lines[i])){if(i===0||!/^>/.test(lines[i-1])){errors.push('Line '+(i+1)+' \"> See also:\" not preceded by another > line (V-59-24 violation)');}}}if(errors.length){console.error('ERRORS:',errors.join('; '));process.exit(1);}console.log('Linux see-also: '+seeAlsoCount+' lines appended; '+cpNoteCount+' > **Cross-platform note:** preserved verbatim');process.exit(0);"</automated>
  </verify>
  <done>docs/_glossary-linux.md contains see-also lines APPENDED inside existing `> **Cross-platform note:**` blockquotes for each matrix-term H3; existing 9 blockquotes preserved verbatim; A3 integrity preserved; node check exits 0.</done>
</task>

<task type="auto">
  <name>Task 5: Add NEW `> **Cross-platform note:**` blockquotes to Windows _glossary.md (per D-15 pattern alignment normalization)</name>
  <files>docs/_glossary.md</files>
  <read_first>
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-15 — Windows currently has 0 cross-platform blockquotes; ADD new ones for collision-matrix terms)
    - docs/_glossary.md (FULL FILE — verify 0 `> **Cross-platform note:**` blockquotes pre-edit; preserve `> **APv2 only.**`, `> **Both frameworks.**`, `> **APv2 note:**` blockquote shapes)
    - docs/_glossary-macos.md / _glossary-android.md / _glossary-linux.md (target anchor verification)
  </read_first>
  <action>
Windows `_glossary.md` currently has 0 `> **Cross-platform note:**` blockquotes (verified per RESEARCH §Pattern 5: Windows uses `> **APv2 only.**`, `> **Both frameworks.**`, `> **APv2 note:**` shapes only). For each Windows H3 entry that appears in the locked collision matrix:

ADD a new `> **Cross-platform note:**` blockquote with 1-2 sentence disambiguation prose + see-also line. Place the new blockquote AT THE END of the H3 body region (after any existing prose, before the next `### `).

**D-15 LOCKED shape for new Windows blockquotes:**
```markdown
> **Cross-platform note:** [1-2 sentence disambiguation prose authored by plan-author — focus on what the cross-platform analogs ARE and what's distinct about the Windows behavior]
> See also: [Term](_glossary-macos.md#anchor) (Apple/macOS+iOS); [Term](_glossary-android.md#anchor) (Android); [Term](_glossary-linux.md#anchor) (Linux).
```

**Critical rules:**
- Use `> **Cross-platform note:**` shape EXACTLY (matches Android + Linux normalization per D-15 pattern alignment)
- DO NOT modify existing `> **APv2 only.**` / `> **Both frameworks.**` / `> **APv2 note:**` blockquotes (preserve mature v1.0-v1.4 prose)
- The new `> **Cross-platform note:**` blockquote is appended AFTER any existing `>` blockquotes within the H3 region
- The see-also line is the LAST `>` line of the new blockquote (preceded by the `> **Cross-platform note:**` opening line — A3 integrity)
- Use `(Apple/macOS+iOS)` for the macOS see-also label per D-18 + REQUIREMENTS.md:144 (`_glossary-macos.md` covers BOTH macOS AND iOS — single label)

**Example for the BitLocker H3:**

Existing (assume this is the typical Windows glossary entry shape):
```markdown
### BitLocker

[existing definition prose covering Windows BitLocker, drive-level encryption, compliance signal, etc.]

> **APv2 note:** BitLocker compliance enforcement applies to both APv1 and APv2 devices. (or similar pre-existing prose)
```

Edit (append new `> **Cross-platform note:**` blockquote AT END of H3 body):
```markdown
### BitLocker

[existing definition prose covering Windows BitLocker, drive-level encryption, compliance signal, etc.]

> **APv2 note:** BitLocker compliance enforcement applies to both APv1 and APv2 devices. (or similar pre-existing prose)

> **Cross-platform note:** Drive-level encryption analogs across platforms: macOS uses FileVault (XTS-AES full-disk), Linux uses dm-crypt with LUKS for the encrypted-LVM standard Ubuntu install. Android Intune does not expose dm-crypt as a per-device compliance signal (Android-side encryption is implicit).
> See also: [FileVault](_glossary-macos.md#filevault) (Apple/macOS+iOS); [dm-crypt](_glossary-linux.md#dm-crypt) (Linux).
```

Iterate through every Windows H3 in the locked collision matrix that needs cross-platform recognition. Estimated 5-10 entries based on typical Windows ↔ Apple/Android/Linux equivalences.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/_glossary.md','utf8');const errors=[];const cpNoteCount=(c.match(/^> \\*\\*Cross-platform note:/gm)||[]).length;if(cpNoteCount===0)errors.push('No new > **Cross-platform note:** blockquotes added to _glossary.md (Plan 59-05 Task 5 incomplete; per D-15 ADD new blockquotes for matrix terms)');const seeAlsoCount=(c.match(/^> See also:/gm)||[]).length;if(seeAlsoCount===0)errors.push('No > See also: lines found in _glossary.md');const apv2Count=(c.match(/^> \\*\\*APv2 (only|note):/gm)||[]).length;const bothCount=(c.match(/^> \\*\\*Both frameworks:/gm)||[]).length;console.log('Pre-existing blockquote shapes preserved: APv2 (only|note)='+apv2Count+', Both frameworks='+bothCount);const lines=c.split('\\n');for(let i=0;i<lines.length;i++){if(/^> See also:/.test(lines[i])){if(i===0||!/^>/.test(lines[i-1])){errors.push('Line '+(i+1)+' \"> See also:\" not preceded by another > line (V-59-24 violation)');}}}if(errors.length){console.error('ERRORS:',errors.join('; '));process.exit(1);}console.log('Windows _glossary.md: '+cpNoteCount+' new > **Cross-platform note:** blockquotes added; '+seeAlsoCount+' see-also lines');process.exit(0);"</automated>
  </verify>
  <done>docs/_glossary.md contains NEW `> **Cross-platform note:**` blockquotes for each matrix-term H3; each new blockquote contains 1-2 sentence disambiguation + see-also line; existing pre-Phase-59 blockquote shapes (`> **APv2 only.**`, `> **Both frameworks.**`, `> **APv2 note:**`) preserved verbatim; A3 integrity preserved; node check exits 0.</done>
</task>

<task type="auto">
  <name>Task 6: Refresh frontmatter (`last_verified` + Version History) on all 4 modified glossaries</name>
  <files>docs/_glossary.md, docs/_glossary-macos.md, docs/_glossary-android.md, docs/_glossary-linux.md</files>
  <read_first>
    - All 4 glossary files post-Tasks 2-5 (verify see-also lines landed)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-29 step 6 — 60-day `last_verified` refresh)
  </read_first>
  <action>
For each of the 4 modified glossaries, update the frontmatter `last_verified` field to today's date and `review_by` to +60 days (per Phase 34 D-14 universal rule). Use ISO date format `YYYY-MM-DD`.

Append a Version History row at the top of each glossary's Version History table (immediately below the header `| Date | Change | Author |` separator):

For `docs/_glossary.md`:
```
| {today's date} | Phase 59 (CLEAN-08): added NEW `> **Cross-platform note:**` blockquotes with reciprocal see-also lines for collision-matrix terms (encryption / enrollment / etc.) per D-15 pattern alignment normalization | -- |
```

For `docs/_glossary-macos.md`:
```
| {today's date} | Phase 59 (CLEAN-08): appended `> See also:` lines INSIDE existing `> **Windows equivalent:**` blockquotes for collision-matrix terms; existing `> **Windows equivalent:**` labels PRESERVED verbatim per D-15 anti-rename | -- |
```

For `docs/_glossary-android.md`:
```
| {today's date} | Phase 59 (CLEAN-08): appended `> See also:` lines INSIDE existing `> **Cross-platform note:**` blockquotes for collision-matrix terms; existing 23 blockquotes preserved verbatim | -- |
```

For `docs/_glossary-linux.md`:
```
| {today's date} | Phase 59 (CLEAN-08): appended `> See also:` lines INSIDE existing `> **Cross-platform note:**` blockquotes for collision-matrix terms; existing 9 blockquotes preserved verbatim | -- |
```

If any glossary lacks a Version History H2 + table, add one at the END of the file with a single Phase 59 row.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const errors=[];const files=['docs/_glossary.md','docs/_glossary-macos.md','docs/_glossary-android.md','docs/_glossary-linux.md'];const today=new Date().toISOString().slice(0,10);for(const f of files){const c=fs.readFileSync(f,'utf8');const fmMatch=c.match(/^---\\n([\\s\\S]*?)\\n---/);if(fmMatch){const fm=fmMatch[1];if(!/last_verified:/.test(fm))errors.push(f+': frontmatter last_verified field missing');}else{errors.push(f+': frontmatter block not found');}if(!/Phase 59.*CLEAN-08/.test(c))errors.push(f+': Phase 59 Version History row missing');}if(errors.length){console.error('ERRORS:',errors.join('; '));process.exit(1);}console.log('All 4 glossary frontmatter + Version History refresh checks pass');process.exit(0);"</automated>
  </verify>
  <done>All 4 glossaries have refreshed `last_verified` + `review_by` frontmatter; each has a Phase 59 Version History row referencing CLEAN-08; node verification check exits 0.</done>
</task>

</tasks>

<verification>
- `.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-COLLISION-MATRIX.md` exists with Matrix + Anchor Map + Reciprocity Pairs sections (D-16 plan-time matrix lock)
- All 4 glossaries contain `> See also:` lines inside existing or new `>` blockquotes (V-59-20 reciprocity)
- Bidirectional pair completeness: every term in matrix has see-also lines in all listing glossaries (V-59-22)
- A3 blockquote integrity: every `> See also:` line preceded by another `>` line (V-59-24)
- macOS `> **Windows equivalent:**` blockquotes (≥11) preserved verbatim (D-15 anti-rename; Pitfall 7)
- Android `> **Cross-platform note:**` blockquotes (≥23) preserved verbatim
- Linux `> **Cross-platform note:**` blockquotes (≥9) preserved verbatim
- Windows _glossary.md gets NEW `> **Cross-platform note:**` blockquotes for matrix terms (pre-edit count=0; post-edit count ≥5)
- Phase 49 LIN-02 banner-link entries at lines 10-12 of each glossary unchanged (D-20)
- All 4 glossaries have refreshed `last_verified` frontmatter
</verification>

<success_criteria>
- GLOSSARY_RECIPROCITY phase-level truth satisfied: validator V-59-20..23 PASS expected
- D-14 / D-15 / D-16 / D-17 / D-18 / D-19 / D-20 all satisfied
- ROADMAP Phase 59 SC#2 PRIMARY (5-platform glossary reciprocal see-also entries — extends v1.4 macOS↔Android pattern across all 5 platforms) satisfied via 4-file equivalent (Apple glossary covers macOS+iOS per REQUIREMENTS.md:144)
- CLEAN-08 (the only Phase 59 active requirement) covered via this plan's collision-matrix + see-also reciprocity
- V-59-20 / V-59-21 / V-59-22 / V-59-23 / V-59-24 validator targets correctly populated for Plan 59-08 verification
</success_criteria>

<output>
After completion, create `.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-05-SUMMARY.md` documenting:
1. Total terms in locked collision matrix (~10-18 expected)
2. Total reciprocal pairs in matrix (Cartesian product across listing glossaries)
3. Per-glossary see-also line counts post-edit (`grep -c "^> See also:"` for each)
4. Per-glossary preserved blockquote counts (Windows-equivalent: 11; Cross-platform-note Android: 23; Cross-platform-note Linux: 9)
5. Confirmation that Phase 49 LIN-02 banner-link entries at lines 10-12 are byte-identical (D-20)
6. Confirmation that V-59-20 / V-59-22 / V-59-24 reciprocity / bidirectional / blockquote integrity all verified locally
7. Note for Plan 59-08 (validator): "Locked collision matrix is the input to V-59-20..23; matrix file at `.planning/phases/59-*/59-05-COLLISION-MATRIX.md` is auditable; validator may machine-parse the Reciprocity Pairs table for assertion generation."
</output>
