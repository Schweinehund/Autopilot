---
phase: 59
plan: 59-04
type: execute
wave: 3
depends_on: [59-02, 59-03]
files_modified:
  - docs/index.md
files_created: []
requirements: [CLEAN-08]
autonomous: true

must_haves:
  truths:
    - "Admin landing on `docs/index.md` sees a `## Operations` H2 with 4 sub-H3 sections (Co-Management / Patch & Update Management / App Lifecycle Automation / Compliance Drift Detection + Tenant Migration); admin can navigate to any of the 4 ops sub-domains within 1 click (HUB_OPERATIONS_REACHABLE phase-level truth)"
    - "H2 name is EXACTLY `## Operations` — NOT `## Operational Depth` (D-08 verbatim SC#1 test; V-59-12 NEGATIVE regression-guard)"
    - "Operations H2 is inserted AFTER `## Linux Provisioning` H2 (created by Plan 59-02) and BEFORE `## Cross-Platform References` H2 (D-07 + D-11 append-only contract; H2 ordering: ... Android Enterprise Provisioning -> Linux Provisioning -> Operations -> Cross-Platform References)"
    - "Each of 4 sub-H3 sections contains: 1-sentence framing + 1-3 row routing table with `Resource | Description` columns; all data cells contain markdown links (D-09 compact substructure; PITFALL-7 link-not-copy per V-59-15..18)"
    - "Operations H2 body does NOT contain `Operational Depth` literal (V-59-12 NEGATIVE)"
    - "Operations H2 body does NOT contain verbatim-pasted sub-dir blockquote tokens like `No co-management equivalent` (V-59-19 NEGATIVE PITFALL-7 human-review prep)"
    - "No anchor collision between new `## Operations` H2 (slug `#operations`) and existing `### Device Operations` H3 at line 85 (slug `#device-operations`); GFM produces distinct slugs per D-13"
    - "Existing `[Monitoring and Operations](reference/00-index.md#monitoring-and-operations)` Cross-Platform References row at line 214 unchanged (D-12 no-change-required)"

  artifacts:
    - path: "docs/index.md"
      provides: "Hub navigation H2 for cross-platform Operations (4 ops sub-domains); admin lands here, finds ops content in 1 click"
      contains:
        - "## Operations"
        - "### Co-Management"
        - "### Patch & Update Management"
        - "### App Lifecycle Automation"
        - "### Compliance Drift Detection + Tenant Migration"
        - "operations/00-index.md"
        - "operations/co-management/00-overview.md"
        - "operations/patch-management/00-overview.md"
        - "operations/app-lifecycle/00-overview.md"
        - "operations/drift-migration/00-overview.md"

  key_links:
    - from: "docs/index.md `## Operations` `### Co-Management` H3"
      to: "docs/operations/co-management/00-overview.md (Phase 53 deliverable)"
      via: "Sub-table row link"
      pattern: "operations\\/co-management\\/00-overview\\.md"
    - from: "docs/index.md `## Operations` `### Patch & Update Management` H3"
      to: "docs/operations/patch-management/00-overview.md (Phase 54 deliverable)"
      via: "Sub-table row link"
      pattern: "operations\\/patch-management\\/00-overview\\.md"
    - from: "docs/index.md `## Operations` `### App Lifecycle Automation` H3"
      to: "docs/operations/app-lifecycle/00-overview.md (Phase 55 deliverable)"
      via: "Sub-table row link"
      pattern: "operations\\/app-lifecycle\\/00-overview\\.md"
    - from: "docs/index.md `## Operations` `### Compliance Drift Detection + Tenant Migration` H3"
      to: "docs/operations/drift-migration/00-overview.md + drift-migration/04-tenant-migration-runbook.md (Phase 56 deliverables)"
      via: "Sub-table row links"
      pattern: "operations\\/drift-migration\\/(00-overview|04-tenant-migration-runbook)\\.md"
---

<objective>
Append a net-new `## Operations` H2 section to `docs/index.md` between the Linux Provisioning H2 (created by Plan 59-02) and the Cross-Platform References H2 (currently at line 199 + 31 = approximately line 230 post-Plan-59-02). The new H2 contains 4 sub-H3 sections (Co-Management / Patch & Update Management / App Lifecycle Automation / Compliance Drift Detection + Tenant Migration), each with a 1-sentence framing + 1-3 row routing table pointing to `operations/{sub-dir}/00-overview.md` plus major routing endpoints.

Plan 59-04 is Wave 3; depends on 59-02 (Linux H2 in docs/index.md must exist for ordering invariant) AND 59-03 (ops/00-index.md must be fully-populated before Operations H2 links to it). Ships D-07 / D-08 / D-09 / D-11 / D-12 / D-13 mandates.

Purpose: Implements ROADMAP Phase 59 SC#1 PRIMARY (Operations H2 linking to docs/operations/ co-management, patch-management, app-lifecycle, drift-migration sub-directories) — the HUB_OPERATIONS_REACHABLE phase-level truth. Implements V-59-10 + V-59-11 + V-59-12 + V-59-15..18 + V-59-19 validator targets.

Output: 1 modified file (`docs/index.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-ANCHOR-INVENTORY.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-02-SUMMARY.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-03-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
**Files this plan modifies:**
- MODIFY: `docs/index.md` — append `## Operations` H2 immediately after the `## Linux Provisioning` H2 (added by Plan 59-02) and immediately before the `## Cross-Platform References` H2; refresh frontmatter `last_verified`; append Version History row

**Direct dependencies (must exist before this plan executes):**
- Plan 59-02 SUMMARY confirming `## Linux Provisioning` H2 has been appended to docs/index.md
- Plan 59-03 SUMMARY confirming docs/operations/00-index.md has 4 fully-populated H2 sections

**Sub-directory overview files referenced by new sub-tables (read-only — already exist from Phases 53-56):**
- `docs/operations/co-management/00-overview.md` — Phase 53 deliverable; the per-sub-domain hub for Co-Management; 1-sentence framing borrows tone from this file's intro blockquote
- `docs/operations/patch-management/00-overview.md` — Phase 54 deliverable; 1-sentence framing tone source
- `docs/operations/app-lifecycle/00-overview.md` — Phase 55 deliverable; 1-sentence framing tone source
- `docs/operations/drift-migration/00-overview.md` — Phase 56 deliverable; 1-sentence framing tone source
- `docs/operations/00-index.md` — Plan 59-03 deliverable; the ops-domain index linked from sub-table top row

**Optional per-platform deep links (D-09 sub-H3 may include 1-2 rows beyond the sub-dir 00-overview):**
- `docs/operations/co-management/01-windows-tenant-attach.md` (Phase 53)
- `docs/operations/co-management/02-windows-workload-sliders.md` (Phase 53)
- `docs/operations/patch-management/01-windows-wufb-rings.md` (Phase 54)
- `docs/operations/app-lifecycle/01-windows-win32-msix-scale.md` (Phase 55)
- `docs/operations/drift-migration/04-tenant-migration-runbook.md` (Phase 56)

**Sibling Phase 59 plans (Wave 3 file ownership of docs/index.md is exclusive to this plan; does NOT conflict with):**
- 59-02 wrote docs/index.md Linux H2 in Wave 2 (predecessor in same file; this plan APPENDS to it)
- 59-05 / 59-06 / 59-07 edit disjoint files (glossaries / quick-ref-l1 / quick-ref-l2)
</interfaces>
</context>

<threat_model>
N/A — documentation-only phase, no attack surface introduced. Markdown hub-navigation edits with no input validation, network exposure, or sensitive-data handling.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Append `## Operations` H2 with 4 sub-H3 sections to docs/index.md (D-07 / D-08 / D-09 / D-11 / D-13)</name>
  <files>docs/index.md</files>
  <read_first>
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-07..D-13 + D-08 verbatim SC#1 test; D-09 4 sub-H3 + 1-3 row routing table; D-12 line-214 no-change-required; D-13 anchor non-collision)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md (§Pattern 6 GFM Kebab-Case Slug Rules — anchor non-collision verified `#operations` vs `#device-operations`; §specifics recommended sub-H3 framings at CONTEXT line 344)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-ANCHOR-INVENTORY.md (post-edit expected ordering: Linux Provisioning -> Operations -> Cross-Platform References)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-02-SUMMARY.md (verify Plan 59-02 has shipped Linux H2 — depends_on dependency)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-03-SUMMARY.md (verify Plan 59-03 has shipped 4 H2s in ops/00-index.md — depends_on dependency)
    - docs/index.md (post-Plan-59-02 state; locate `## Linux Provisioning` H2 end + `---` separator + `## Cross-Platform References` H2 line)
    - docs/index.md lines 85-92 (existing `### Device Operations` H3 — confirm GFM anchor `#device-operations` is distinct from new `## Operations` slug `#operations`; D-13)
    - docs/operations/co-management/00-overview.md lines 1-25 (intro blockquote tone for Co-Management sub-H3 framing)
    - docs/operations/patch-management/00-overview.md lines 1-25 (intro blockquote tone for Patch sub-H3 framing)
    - docs/operations/app-lifecycle/00-overview.md lines 1-25 (intro blockquote tone for App sub-H3 framing)
    - docs/operations/drift-migration/00-overview.md lines 1-25 (intro blockquote tone for Drift sub-H3 framing)
  </read_first>
  <action>
Use the Edit tool to insert the new H2 block. The insertion point is between the closing `---` separator of the `## Linux Provisioning` H2 (added by Plan 59-02) and the `## Cross-Platform References` H2.

Find the existing snippet (post-Plan-59-02 state):
```
| [Linux Capability Matrix](reference/linux-capability-matrix.md) | Compare Linux feature parity vs Windows, macOS, iOS, Android -- scannable 6-domain table |

---

## Cross-Platform References
```

Replace with:
```
| [Linux Capability Matrix](reference/linux-capability-matrix.md) | Compare Linux feature parity vs Windows, macOS, iOS, Android -- scannable 6-domain table |

---

## Operations

Cross-platform operational guidance for Intune-managed fleets — co-management, patch and update management, app lifecycle automation, and compliance drift detection plus tenant migration. For the full operations index, see [Operations Index](operations/00-index.md).

### Co-Management

Windows ConfigMgr-to-Intune workload management — slider model, migration sequence, tenant attach disambiguation, and Windows Autopatch prerequisites.

| Resource | Description |
|----------|-------------|
| [Co-Management Overview](operations/co-management/00-overview.md) | 7 ConfigMgr-current-branch (CB 2503) workloads, 3 slider states, Pilot Intune disambiguation |
| [Windows Tenant Attach](operations/co-management/01-windows-tenant-attach.md) | Tenant attach (Intune-portal device sync, no workload switching) vs full co-management disambiguation |
| [Workload Slider Migration](operations/co-management/02-windows-workload-sliders.md) | Low-risk-first migration sequence; Endpoint Protection HIGH-RISK callout |

### Patch & Update Management

Cross-platform OS update enforcement — Windows Update for Business rings, macOS managed update commands (DDM), iOS supervised vs unsupervised update lifecycle, and Android per-OEM patch delivery (Play Integrity tier impact).

| Resource | Description |
|----------|-------------|
| [Patch & Update Management Overview](operations/patch-management/00-overview.md) | 4-platform update-enforcement comparison hub (Windows + macOS + iOS + Android) |
| [Windows WUfB Rings](operations/patch-management/01-windows-wufb-rings.md) | Windows Update for Business ring topology; Hotpatch; dual-scan source conflict |
| [Android Patch Delivery](operations/patch-management/04-android-patch-delivery.md) | Per-OEM patch delivery; Play Integrity MEETS_STRONG_INTEGRITY enforcement cascade |

### App Lifecycle Automation

Cross-platform app deployment at scale — Win32/MSIX packaging + supersedence, macOS .pkg/.dmg pipelines, iOS VPP device-vs-user licensing, Android Managed Google Play app lifecycle.

| Resource | Description |
|----------|-------------|
| [App Lifecycle Overview](operations/app-lifecycle/00-overview.md) | 4-platform app-deployment comparison hub (Windows + macOS + iOS + Android) |
| [Windows Win32 / MSIX at Scale](operations/app-lifecycle/01-windows-win32-msix-scale.md) | Win32ContentPrepTool, supersedence chains, dependency graphs, MSIX delivery |
| [Android Managed Google Play Lifecycle](operations/app-lifecycle/04-android-mgp-lifecycle.md) | MGP private track, web app publishing, AMAPI custom-apps API, OEMConfig (Zebra) |

### Compliance Drift Detection + Tenant Migration

Cross-platform configuration drift workflows + tenant-to-tenant migration runbooks — BitLocker re-key, ABM token re-issue, Managed Google Play re-binding, cross-platform encryption-drift surface.

| Resource | Description |
|----------|-------------|
| [Drift Detection + Tenant Migration Overview](operations/drift-migration/00-overview.md) | 4-platform drift-detection + tenant-migration comparison hub |
| [Tenant Migration Runbook](operations/drift-migration/04-tenant-migration-runbook.md) | Windows BitLocker re-key + ABM token re-issue + MGP re-binding; cross-platform encryption drift section |
| [Windows Drift Detection](operations/drift-migration/01-windows-drift-detection.md) | Intune Remediations (Proactive Remediations) detect+remediate Bash/PowerShell pairs |

---

## Cross-Platform References
```

**Critical compliance rules (D-07 / D-08 / D-09 / D-11 / D-12 / D-13 / V-59-NN):**
- H2 literal MUST be `## Operations` EXACTLY — NEVER `## Operational Depth` (D-08 verbatim SC#1 test; V-59-12 NEGATIVE)
- 4 sub-H3 literals MUST be exactly: `### Co-Management` / `### Patch & Update Management` / `### App Lifecycle Automation` / `### Compliance Drift Detection + Tenant Migration` (V-59-11 anchor pin)
- Each sub-H3: 1-sentence framing paragraph + 1-3 row table with `Resource | Description` columns (D-09 bounded budget; preserve hub-nav tier discipline; do NOT exceed 3 rows per sub-H3)
- Every data cell in all 4 sub-tables MUST contain a markdown link `[...](.../...md)` — PITFALL-7 link-not-copy enforced by V-59-15..18
- Sub-tables MUST NOT verbatim-paste sub-dir blockquote prose tokens; specifically MUST NOT contain `No co-management equivalent` literal anywhere in the Operations H2 body (V-59-19 NEGATIVE; that token lives at `co-management/00-overview.md` lines 9-20 — link-only references; PITFALL-7 firewall)
- Existing `### Device Operations` H3 at line 85 (Windows-Autopilot tier-2) MUST remain unchanged (D-13 GFM anchor distinctness — `#operations` vs `#device-operations` are DISTINCT slugs)
- Existing `[Monitoring and Operations](reference/00-index.md#monitoring-and-operations)` row at line 214 (post-Plan-59-02 line shift may have moved it) MUST remain unchanged (D-12 no-change-required — points to `reference/00-index.md`, NOT `operations/`; no doc-tree overlap)
- USE `--` (double-hyphen) for description disambiguation if needed, NOT `—` em-dash, matching Phase 57 / Phase 59-02 conventions
- Optional informational improvement (D-12): plan-author MAY add a Cross-Platform References row pointing to `operations/00-index.md` for symmetry — discretionary, NOT required; if added, append it to the Cross-Platform References table after the 2 Linux entries shipped by Plan 59-02 (do not block this plan on it)

**Frontmatter refresh:**
Update the `last_verified` field in docs/index.md frontmatter to today's date if not already updated by Plan 59-02 in the same commit-day. If both plans run on the same day, the value is already correct; just verify.

**Version History row:**
Append a new row at the TOP of the Version History table (immediately below the header `| Date | Change | Author |` separator), AFTER the row added by Plan 59-02:
```
| {today's date} | Phase 59: appended `## Operations` H2 with 4 sub-H3 sections (Co-Management / Patch & Update Management / App Lifecycle Automation / Compliance Drift Detection + Tenant Migration) routing to operations/ sub-directories (CLEAN-08 SC#1) | -- |
```

  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/index.md','utf8');const errors=[];function sliceH2(content,h2){const re=new RegExp('^'+h2.replace(/[/\\\\^$*+?.()|[\\]{}]/g,'\\\\$&')+'\\\\s*$','m');const m=content.match(re);if(!m)return null;const idx=content.indexOf(m[0]);const after=content.slice(idx+m[0].length);const next=after.search(/^## /m);return next>0?content.slice(idx,idx+m[0].length+next):content.slice(idx);}const ops=sliceH2(c,'## Operations');if(!ops){errors.push('## Operations H2 absent');}else{const subH3s=['### Co-Management','### Patch & Update Management','### App Lifecycle Automation','### Compliance Drift Detection + Tenant Migration'];for(const h3 of subH3s){const re=new RegExp('^'+h3.replace(/[/\\\\^$*+?.()|[\\]{}]/g,'\\\\$&')+'\\\\s*$','m');if(!re.test(ops))errors.push('Sub-H3 missing in Operations H2: '+h3);}const lines=ops.split('\\n');for(let i=0;i<lines.length;i++){const line=lines[i];if(/^\\| [^|]/.test(line)&&!/^\\|---/.test(line)&&!/^\\| (Resource|Guide|Method|Verdict|Category|Path|Date|Author|Change)/.test(line)){if(/^\\| \\[/.test(line))continue;else errors.push('Operations sub-table data row missing markdown link (PITFALL-7 V-59-15..18): '+line.slice(0,80));}}if(/No co-management equivalent/.test(ops))errors.push('V-59-19 NEGATIVE violation: \"No co-management equivalent\" verbatim-pasted from sub-dir blockquote');if(/Operational Depth/.test(c))errors.push('V-59-12 NEGATIVE violation: \"Operational Depth\" token present');}const idxLinux=c.indexOf('## Linux Provisioning');const idxOps=c.indexOf('## Operations');const idxXref=c.indexOf('## Cross-Platform References');if(idxLinux<0||idxOps<0||idxXref<0)errors.push('H2 ordering check skipped (one or more H2s missing)');else if(!(idxLinux<idxOps&&idxOps<idxXref))errors.push('H2 ordering wrong (V-59-10): expected Linux Provisioning < Operations < Cross-Platform References');const expectedAnchorRefs=['operations/co-management/00-overview.md','operations/patch-management/00-overview.md','operations/app-lifecycle/00-overview.md','operations/drift-migration/00-overview.md','operations/00-index.md'];for(const ref of expectedAnchorRefs){if(!c.includes(ref))errors.push('Operations sub-table missing required link: '+ref);}const preAnchors=['## Windows Autopilot','## macOS Provisioning','## iOS/iPadOS Provisioning','## Android Enterprise Provisioning','## Linux Provisioning','## Cross-Platform References'];for(const a of preAnchors){if(!c.includes(a))errors.push('Pre-Phase-59-04 H2 anchor missing: '+a);}if(/^### Device Operations\\s*$/m.test(c)===false)errors.push('Existing ### Device Operations H3 at line 85 missing or modified (D-13 violation)');if(errors.length){console.error('ERRORS:',errors.join('; '));process.exit(1);}console.log('Operations H2 + 4 sub-H3 + ordering + PITFALL-7 + V-59-12 NEGATIVE + V-59-19 NEGATIVE checks pass');process.exit(0);"</automated>
  </verify>
  <done>docs/index.md contains `## Operations` H2 between Linux Provisioning H2 and Cross-Platform References H2 (V-59-10 ordering); 4 sub-H3 anchors present (V-59-11); every sub-table data row contains a markdown link (V-59-15..18 PITFALL-7); `Operational Depth` token absent (V-59-12 NEGATIVE); `No co-management equivalent` blockquote token absent inside Operations H2 (V-59-19 NEGATIVE); `### Device Operations` H3 at line 85 unchanged (D-13); 5 pre-Phase-59 H2 anchors + Linux Provisioning H2 byte-identical (V-59-32 regression-guard); node verification check exits 0.</done>
</task>

</tasks>

<verification>
- `docs/index.md` contains `## Operations` H2 (V-59-10)
- H2 ordering: ... Android Enterprise Provisioning -> Linux Provisioning -> Operations -> Cross-Platform References (V-59-10)
- 4 sub-H3 anchors present (V-59-11)
- `Operational Depth` token absent anywhere in docs/index.md (V-59-12 NEGATIVE)
- Every Operations sub-table data row contains a markdown link (V-59-15..18 PITFALL-7)
- Operations H2 body does NOT contain `No co-management equivalent` literal (V-59-19 NEGATIVE)
- `### Device Operations` H3 (slug `#device-operations`) at line 85 unchanged (D-13)
- `[Monitoring and Operations](reference/00-index.md#monitoring-and-operations)` row in Cross-Platform References unchanged (D-12)
- Frontmatter `last_verified` consistent with Plan 59-02 update
</verification>

<success_criteria>
- HUB_OPERATIONS_REACHABLE phase-level truth satisfied: admin reaches all 4 ops sub-domains in 1 click from docs/index.md Operations H2
- D-07 / D-08 / D-09 / D-11 / D-12 / D-13 all satisfied
- ROADMAP Phase 59 SC#1 PRIMARY (Operations H2 linking to docs/operations/ co-management, patch-management, app-lifecycle, drift-migration sub-directories) satisfied with verbatim SC#1 naming
- V-59-10 + V-59-11 + V-59-12 + V-59-15..18 + V-59-19 validator targets correctly populated for Plan 59-08 verification
- Plan 59-04 progressively-landed atomically; Wave 4 (Plan 59-08 validator) unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-04-SUMMARY.md` documenting:
1. The H2 ordering confirmed: Android Enterprise Provisioning -> Linux Provisioning -> Operations -> Cross-Platform References
2. The exact 4 sub-H3 literals present
3. The row counts per sub-H3 (1-3 each, plan-author choice)
4. Confirmation that `Operational Depth` token is ABSENT anywhere in docs/index.md (V-59-12 NEGATIVE satisfied)
5. Confirmation that `No co-management equivalent` blockquote token is ABSENT inside the new Operations H2 body (V-59-19 NEGATIVE satisfied — PITFALL-7 firewall)
6. Confirmation that `### Device Operations` H3 at line 85 is byte-identical pre/post (D-13 GFM anchor distinctness preserved)
7. Note for Plan 59-08 (validator): "All Operations H2 V-59-NN targets are now in place: V-59-10 (presence + ordering), V-59-11 (4 sub-H3), V-59-12 (NEGATIVE), V-59-15..18 (PITFALL-7), V-59-19 (NEGATIVE blockquote token)."
</output>
