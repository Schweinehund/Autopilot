---
phase: 59
plan: 59-02
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - docs/index.md
files_created: []
requirements: [CLEAN-08]
autonomous: true

must_haves:
  truths:
    - "Admin landing on `docs/index.md` sees a `## Linux Provisioning` H2 with 3 sub-tables (Service Desk L1 / Desktop Engineering L2 / Admin Setup) at row counts L1=4 / L2=4 / Admin=3 matching Phase 57 Android H2 structural depth verbatim per ROADMAP:381 (D-01, D-02, D-03, D-04)"
    - "From the Linux Provisioning H2, an admin can navigate to Linux L1 / L2 / Admin Setup content within 1 click (HUB_LINUX_REACHABLE phase-level truth)"
    - "Cross-Platform References sub-table at lines 199-219 contains 2 new Linux entries: `Linux Provisioning Lifecycle` link + `Linux Capability Matrix` link (D-05)"
    - "docs/index.md does NOT contain link to `linux-intune-portal-enrollment.md` or any `end-user-guides/linux*` literal (D-06; V-59-09 NEGATIVE regression-guard — Phase 57 hub-discipline precedent symmetry)"
    - "Existing pre-Phase-59 H2 anchors (`## Windows Autopilot` / `## macOS Provisioning` / `## iOS/iPadOS Provisioning` / `## Android Enterprise Provisioning` / `## Cross-Platform References`) remain byte-identical (append-only contract per D-11)"
    - "New `## Linux Provisioning` H2 inserted AFTER Android H2 (lines 167-196) and BEFORE Cross-Platform References (line 199); Linux H2 lands FIRST in this Wave-2 ordering, Operations H2 follows in Plan 59-04"

  artifacts:
    - path: "docs/index.md"
      provides: "Hub navigation H2 for Linux Provisioning; admin lands here, finds Linux L1/L2/Admin in 1 click"
      contains:
        - "## Linux Provisioning"
        - "### Service Desk (L1)"
        - "### Desktop Engineering (L2)"
        - "### Admin Setup"
        - "[Linux Provisioning Lifecycle](linux-lifecycle/00-enrollment-overview.md)"
        - "[Linux Triage Decision Tree](decision-trees/09-linux-triage.md)"
        - "[Linux L1 Runbooks](l1-runbooks/00-index.md#linux-l1-runbooks)"
        - "[L1 Quick-Reference Card](quick-ref-l1.md#linux-quick-reference)"
        - "[Linux Log Collection Guide](l2-runbooks/24-linux-log-collection.md)"
        - "[Linux L2 Runbooks](l2-runbooks/00-index.md#linux-l2-runbooks)"
        - "[L2 Quick-Reference Card](quick-ref-l2.md#linux-quick-reference)"
        - "[Linux Admin Setup Overview](admin-setup-linux/00-overview.md)"
        - "[Linux Capability Matrix](reference/linux-capability-matrix.md)"

  key_links:
    - from: "docs/index.md `## Linux Provisioning` H2 L1 sub-table row 4"
      to: "docs/quick-ref-l1.md `## Linux Quick Reference` H2 (created by Plan 59-06)"
      via: "Forward-reference fragment anchor `quick-ref-l1.md#linux-quick-reference`"
      pattern: "quick-ref-l1\\.md#linux-quick-reference"
    - from: "docs/index.md `## Linux Provisioning` H2 L2 sub-table row 4"
      to: "docs/quick-ref-l2.md `## Linux Quick Reference` H2 (created by Plan 59-07)"
      via: "Forward-reference fragment anchor `quick-ref-l2.md#linux-quick-reference`"
      pattern: "quick-ref-l2\\.md#linux-quick-reference"
    - from: "docs/index.md `## Linux Provisioning` H2 Admin sub-table row 3"
      to: "docs/reference/linux-capability-matrix.md (Phase 50 deliverable)"
      via: "Direct link"
      pattern: "reference/linux-capability-matrix\\.md"
---

<objective>
Append a net-new `## Linux Provisioning` H2 section to `docs/index.md` between the existing Android H2 (currently lines 167-196) and the existing Cross-Platform References H2 (currently line 199). The new H2 contains 3 sub-tables (Service Desk L1 / Desktop Engineering L2 / Admin Setup) matching Phase 57 Android H2 structural depth verbatim per ROADMAP:381 mandate. Also append 2 new Linux entries to the Cross-Platform References sub-table per D-05.

Plan 59-02 is Wave 2; depends on 59-01 (pre-edit anchor inventory must exist); ships D-01 / D-02 / D-03 / D-04 / D-05 / D-06 mandates.

Purpose: Implements ROADMAP Phase 59 SC#1 PRIMARY requirement (Linux H2 with sub-tables) — the HUB_LINUX_REACHABLE phase-level truth. Implements V-59-07 + V-59-08 + V-59-09 validator targets (per D-26).

Output: 1 modified file (`docs/index.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-ANCHOR-INVENTORY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
**Files this plan modifies:**
- MODIFY: `docs/index.md` — append `## Linux Provisioning` H2 (after Android H2 lines 167-196, before Cross-Platform References line 199); append 2 entries to Cross-Platform References table at lines 199-219; refresh `last_verified` frontmatter; update Version History row

**Direct template (read for verbatim mirror; DO NOT modify):**
- `docs/index.md` lines 167-196 (Phase 57 Android H2; the structural template per D-01 ROADMAP:381 mandate)

**Source files referenced by new Linux sub-table rows (read-only — already exist from Phases 49-52):**
- `docs/linux-lifecycle/00-enrollment-overview.md` (Phase 49 deliverable; row 1 in L1, L2, Admin sub-tables; row in Cross-Platform References)
- `docs/decision-trees/09-linux-triage.md` (Phase 51 deliverable; row 2 in L1 sub-table)
- `docs/l1-runbooks/00-index.md` (anchor `#linux-l1-runbooks` at line 79; row 3 in L1 sub-table)
- `docs/l2-runbooks/24-linux-log-collection.md` (Phase 52 deliverable; row 2 in L2 sub-table)
- `docs/l2-runbooks/00-index.md` (anchor `#linux-l2-runbooks` at line 165; row 3 in L2 sub-table)
- `docs/admin-setup-linux/00-overview.md` (Phase 50 deliverable; row 1 in Admin sub-table)
- `docs/reference/linux-capability-matrix.md` (Phase 50 deliverable; row 3 in Admin sub-table; row in Cross-Platform References)

**Forward-reference targets (created in later plans within Phase 59):**
- `docs/quick-ref-l1.md#linux-quick-reference` (created by Plan 59-06; L1 sub-table row 4)
- `docs/quick-ref-l2.md#linux-quick-reference` (created by Plan 59-07; L2 sub-table row 4)

These forward references are architecturally acceptable per progressive-landing pattern (RESEARCH §Pattern 1 closing note + Pitfall 6) — links resolve once Phase 59 is complete; Plan 59-08 validator catches broken anchors at close time.

**Sibling Phase 59 plans (do NOT touch their files; this plan is parallel-safe with):**
- 59-03 edits docs/operations/00-index.md (disjoint file)
- 59-05 edits 4 glossary files (disjoint files)
- 59-06 edits docs/quick-ref-l1.md (disjoint file)
- 59-07 edits docs/quick-ref-l2.md (disjoint file)
- 59-04 also edits docs/index.md but in WAVE 3 (depends on 59-03; will run AFTER 59-02)
</interfaces>
</context>

<threat_model>
N/A — documentation-only phase, no attack surface introduced. Markdown hub-navigation edits with no input validation, network exposure, or sensitive-data handling.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Append `## Linux Provisioning` H2 with 3 sub-tables to docs/index.md (D-01 / D-02 / D-03 / D-04 / D-06)</name>
  <files>docs/index.md</files>
  <read_first>
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-01..D-06 + D-11 append-only contract)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md (§Pattern 1 verbatim Android H2 template at docs/index.md:167-196 + Linux mirror derivation block)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-ANCHOR-INVENTORY.md (pre-edit baseline; confirm Android H2 ends at line 196 and Cross-Platform References starts at line 199; the `---` separator at lines 197-198 is the insertion landmark)
    - docs/index.md (full file; specifically lines 167-196 for verbatim Android H2 template + lines 165-166 + 197-198 for `---` separator pattern)
    - docs/l1-runbooks/00-index.md (verify `## Linux L1 Runbooks` H2 exists at line 79; GFM slug `#linux-l1-runbooks`)
    - docs/l2-runbooks/00-index.md (verify `## Linux L2 Runbooks` H2 exists at line 165; GFM slug `#linux-l2-runbooks`)
  </read_first>
  <action>
Use the Edit tool to insert the new H2 block. The insertion point is between the Android H2 closing `---` separator (currently around line 197-198) and the `## Cross-Platform References` H2 (currently line 199). Find the existing snippet:

```
[end of Android H2 Admin Setup table]

---

## Cross-Platform References
```

Replace with:

```
[end of Android H2 Admin Setup table]

---

## Linux Provisioning

Troubleshooting, investigation, and setup guides for Linux (Ubuntu 22.04 / 24.04 LTS) device management through Microsoft Intune. For terminology, see the [Linux Provisioning Glossary](_glossary-linux.md). For enrollment paths, see the [Linux Provisioning Lifecycle](linux-lifecycle/00-enrollment-overview.md).

### Service Desk (L1)

| Resource | When to Use |
|----------|-------------|
| [Linux Provisioning Lifecycle](linux-lifecycle/00-enrollment-overview.md) | Start here -- understand the Linux enrollment paths (Ubuntu 22.04 / 24.04 LTS, intune-portal package install, microsoft-identity-broker) |
| [Linux Triage Decision Tree](decision-trees/09-linux-triage.md) | Identifies the Linux failure scenario from symptoms (enrollment failed / non-compliant / web-app-CA-blocking-Edge / agent service not running) and routes to the correct runbook |
| [Linux L1 Runbooks](l1-runbooks/00-index.md#linux-l1-runbooks) | Scripted procedures for the 4 Linux failure scenarios (runbooks 30-33) |
| [L1 Quick-Reference Card](quick-ref-l1.md#linux-quick-reference) | One-page cheat sheet -- top checks, escalation triggers, decision tree, and runbook list |

### Desktop Engineering (L2)

| Resource | When to Use |
|----------|-------------|
| [Linux Provisioning Lifecycle](linux-lifecycle/00-enrollment-overview.md) | Review the Linux enrollment pipeline before diagnosing |
| [Linux Log Collection Guide](l2-runbooks/24-linux-log-collection.md) | Prerequisite for all Linux L2 investigations (3-method matrix: journalctl / file-based paths / package-state queries) |
| [Linux L2 Runbooks](l2-runbooks/00-index.md#linux-l2-runbooks) | Investigation guides for log collection + agent investigation (runbooks 24-25) |
| [L2 Quick-Reference Card](quick-ref-l2.md#linux-quick-reference) | One-page cheat sheet -- 3-method log collection, Intune portal paths, Linux compliance category reference, investigation runbook list |

### Admin Setup

| Resource | When to Use |
|----------|-------------|
| [Linux Admin Setup Overview](admin-setup-linux/00-overview.md) | Entry point for all Linux admin setup guides; per-file setup sequence (00-05) lives at this overview, not at hub level |
| [Linux Provisioning Lifecycle](linux-lifecycle/00-enrollment-overview.md) | Review the enrollment pipeline before configuring Intune + intune-portal package (admin-context entry) |
| [Linux Capability Matrix](reference/linux-capability-matrix.md) | Compare Linux feature parity vs Windows, macOS, iOS, Android -- scannable 6-domain table |

---

## Cross-Platform References
```

**Critical preservation rules (D-11 append-only contract):**
- DO NOT modify any line above `## Cross-Platform References` other than inserting the new block in the position specified
- DO NOT change `## Windows Autopilot` / `## macOS Provisioning` / `## iOS/iPadOS Provisioning` / `## Android Enterprise Provisioning` H2 literals (V-59-32 regression-guard target)
- DO NOT add a 4th row to L1, L2, or Admin sub-tables (D-02 = 4 rows / D-03 = 4 rows / D-04 = 3 rows; locked counts per V-59-07)
- DO NOT add a row referencing `linux-intune-portal-enrollment.md` (D-06 + V-59-09 NEGATIVE regression-guard — Phase 57 hub-discipline precedent symmetry; LIN-06 end-user guide reaches admin-setup-linux/02-enrollment-profile.md transitively, not at hub level)
- DO NOT add an `end-user-guides/linux*` literal anywhere in the new H2 (V-59-09 NEGATIVE)
- DO NOT add operations cross-references inside the Linux H2 (D-01 explicit — Operations H2 is a SEPARATE H2 added by Plan 59-04)
- USE `--` (double-hyphen) for description disambiguation, NOT `—` em-dash, matching Phase 57 Android template literally (verified at docs/index.md:175 and elsewhere)

**Verbatim row content (D-02 / D-03 / D-04 — locked):**
- L1 row 1: `[Linux Provisioning Lifecycle](linux-lifecycle/00-enrollment-overview.md) | Start here -- understand the Linux enrollment paths (Ubuntu 22.04 / 24.04 LTS, intune-portal package install, microsoft-identity-broker)`
- L1 row 2: `[Linux Triage Decision Tree](decision-trees/09-linux-triage.md) | Identifies the Linux failure scenario from symptoms (enrollment failed / non-compliant / web-app-CA-blocking-Edge / agent service not running) and routes to the correct runbook`
- L1 row 3: `[Linux L1 Runbooks](l1-runbooks/00-index.md#linux-l1-runbooks) | Scripted procedures for the 4 Linux failure scenarios (runbooks 30-33)`
- L1 row 4: `[L1 Quick-Reference Card](quick-ref-l1.md#linux-quick-reference) | One-page cheat sheet -- top checks, escalation triggers, decision tree, and runbook list`
- L2 row 1: `[Linux Provisioning Lifecycle](linux-lifecycle/00-enrollment-overview.md) | Review the Linux enrollment pipeline before diagnosing`
- L2 row 2: `[Linux Log Collection Guide](l2-runbooks/24-linux-log-collection.md) | Prerequisite for all Linux L2 investigations (3-method matrix: journalctl / file-based paths / package-state queries)`
- L2 row 3: `[Linux L2 Runbooks](l2-runbooks/00-index.md#linux-l2-runbooks) | Investigation guides for log collection + agent investigation (runbooks 24-25)`
- L2 row 4: `[L2 Quick-Reference Card](quick-ref-l2.md#linux-quick-reference) | One-page cheat sheet -- 3-method log collection, Intune portal paths, Linux compliance category reference, investigation runbook list`
- Admin row 1: `[Linux Admin Setup Overview](admin-setup-linux/00-overview.md) | Entry point for all Linux admin setup guides; per-file setup sequence (00-05) lives at this overview, not at hub level`
- Admin row 2: `[Linux Provisioning Lifecycle](linux-lifecycle/00-enrollment-overview.md) | Review the enrollment pipeline before configuring Intune + intune-portal package (admin-context entry)`
- Admin row 3: `[Linux Capability Matrix](reference/linux-capability-matrix.md) | Compare Linux feature parity vs Windows, macOS, iOS, Android -- scannable 6-domain table`

  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/index.md','utf8');const errors=[];if(!/^## Linux Provisioning\s*$/m.test(c))errors.push('## Linux Provisioning H2 absent');if(!/^### Service Desk \(L1\)\s*$/m.test(c))errors.push('### Service Desk (L1) H3 absent');if(!/^### Desktop Engineering \(L2\)\s*$/m.test(c))errors.push('### Desktop Engineering (L2) H3 absent');if(!/^### Admin Setup\s*$/m.test(c))errors.push('### Admin Setup H3 absent');function sliceH2(content,h2){const re=new RegExp('^'+h2.replace(/[/\\\\^$*+?.()|[\\]{}]/g,'\\\\$&')+'\\\\s*$','m');const m=content.match(re);if(!m)return null;const idx=content.indexOf(m[0]);const after=content.slice(idx+m[0].length);const next=after.search(/^## /m);return next>0?content.slice(idx,idx+m[0].length+next):content.slice(idx);}const linuxRegion=sliceH2(c,'## Linux Provisioning');if(!linuxRegion){errors.push('Linux H2 region empty');}else{function countH3Rows(region,h3){const idx=region.indexOf(h3);if(idx<0)return 0;const after=region.slice(idx+h3.length);const nextH3=after.search(/^### /m);const win=nextH3>0?after.slice(0,nextH3):after;return (win.match(/^\\| \\[/gm)||[]).length;}const l1=countH3Rows(linuxRegion,'### Service Desk (L1)');const l2=countH3Rows(linuxRegion,'### Desktop Engineering (L2)');const adm=countH3Rows(linuxRegion,'### Admin Setup');if(l1!==4)errors.push('L1 sub-table rows='+l1+' (expected 4)');if(l2!==4)errors.push('L2 sub-table rows='+l2+' (expected 4)');if(adm!==3)errors.push('Admin sub-table rows='+adm+' (expected 3)');if(/linux-intune-portal-enrollment\\.md/.test(linuxRegion))errors.push('LIN-06 end-user guide surfaced in Linux H2 (D-06 violation)');if(/end-user-guides\\/linux/.test(linuxRegion))errors.push('end-user-guides/linux literal in Linux H2 (D-06 violation)');}const expected=['## Windows Autopilot','## macOS Provisioning','## iOS/iPadOS Provisioning','## Android Enterprise Provisioning','## Cross-Platform References'];for(const e of expected){if(!c.includes(e))errors.push('Pre-Phase-59 H2 anchor missing: '+e);}if(errors.length){console.error('ERRORS:',errors.join('; '));process.exit(1);}console.log('All Linux H2 + sub-table + anchor stability checks pass');process.exit(0);"</automated>
  </verify>
  <done>docs/index.md contains `## Linux Provisioning` H2 with 3 sub-H3s and exact row counts (L1=4 / L2=4 / Admin=3); LIN-06 end-user guide is NOT surfaced inside the new H2 (D-06); 5 pre-Phase-59 H2 anchors remain present (V-59-32 regression-guard target); node verification check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Append 2 Linux entries to Cross-Platform References sub-table (D-05) and refresh frontmatter</name>
  <files>docs/index.md</files>
  <read_first>
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-05 Cross-Platform References pattern; D-12 line-214 no-change-required)
    - docs/index.md (lines 199-219 for the existing Cross-Platform References table; line 219 = current last row pointing to `reference/android-capability-matrix.md`)
    - docs/index.md (frontmatter at top of file for `last_verified` field)
  </read_first>
  <action>
Use the Edit tool to append 2 new rows to the Cross-Platform References table. Find the existing last row of the table:

```
| [Android Capability Matrix](reference/android-capability-matrix.md) | Intune feature parity comparison across Windows, macOS, iOS, and Android |

## Version History
```

Replace with:

```
| [Android Capability Matrix](reference/android-capability-matrix.md) | Intune feature parity comparison across Windows, macOS, iOS, and Android |
| [Linux Provisioning Lifecycle](linux-lifecycle/00-enrollment-overview.md) | Ubuntu 22.04 / 24.04 LTS enrollment pipeline summary |
| [Linux Capability Matrix](reference/linux-capability-matrix.md) | Intune feature parity comparison across all 5 platforms |

## Version History
```

**D-05 verbatim row content (locked):**
- New row 1: `[Linux Provisioning Lifecycle](linux-lifecycle/00-enrollment-overview.md) | Ubuntu 22.04 / 24.04 LTS enrollment pipeline summary`
- New row 2: `[Linux Capability Matrix](reference/linux-capability-matrix.md) | Intune feature parity comparison across all 5 platforms`

**Critical preservation rules:**
- DO NOT modify the existing `[Monitoring and Operations](reference/00-index.md#monitoring-and-operations)` row at line 214 (D-12 no-change-required — points to `reference/00-index.md`, NOT `operations/`; no doc-tree overlap with new Operations H2 from Plan 59-04)
- DO NOT add a `_glossary-linux.md` row to Cross-Platform References — the reciprocal-glossary banner-link entry was ALREADY shipped in Phase 49 LIN-02 commit-2 (verified at `docs/_glossary-linux.md:10`); D-05 explicitly notes Phase 59 does NOT re-add the banner
- DO NOT add a row pointing to `linux-intune-portal-enrollment.md` (D-06 + V-59-09 NEGATIVE)

**Frontmatter refresh:**
Update the `last_verified` field in the docs/index.md frontmatter (top of file) to today's date (the Phase 59 commit date). If a `review_by` field exists, refresh it to +60 days from `last_verified` per Phase 34 D-14 universal rule.

**Version History row:**
Append a new row at the TOP of the Version History table (immediately below the header `| Date | Change | Author |` separator):
```
| {today's date} | Phase 59: appended `## Linux Provisioning` H2 with 3 sub-tables (L1=4 rows / L2=4 rows / Admin=3 rows) matching Phase 57 Android H2 structural depth (CLEAN-08 SC#1); appended Linux Provisioning Lifecycle + Linux Capability Matrix Cross-Platform References entries (D-05) | -- |
```

Use ISO date format `YYYY-MM-DD`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/index.md','utf8');const errors=[];function sliceH2(content,h2){const re=new RegExp('^'+h2.replace(/[/\\\\^$*+?.()|[\\]{}]/g,'\\\\$&')+'\\\\s*$','m');const m=content.match(re);if(!m)return null;const idx=content.indexOf(m[0]);const after=content.slice(idx+m[0].length);const next=after.search(/^## /m);return next>0?content.slice(idx,idx+m[0].length+next):content.slice(idx);}const xref=sliceH2(c,'## Cross-Platform References');if(!xref){errors.push('Cross-Platform References H2 region empty');}else{if(!/\\[Linux Provisioning Lifecycle\\]\\(linux-lifecycle\\/00-enrollment-overview\\.md\\)/.test(xref))errors.push('Linux Provisioning Lifecycle entry missing in Cross-Platform References (D-05)');if(!/\\[Linux Capability Matrix\\]\\(reference\\/linux-capability-matrix\\.md\\)/.test(xref))errors.push('Linux Capability Matrix entry missing in Cross-Platform References (D-05)');if(/linux-intune-portal-enrollment\\.md/.test(xref))errors.push('LIN-06 end-user guide surfaced in Cross-Platform References (D-06 violation)');if(/end-user-guides\\/linux/.test(xref))errors.push('end-user-guides/linux literal in Cross-Platform References (D-06 violation)');}const fmMatch=c.match(/^---\\n([\\s\\S]*?)\\n---/);if(fmMatch){const fm=fmMatch[1];if(!/last_verified:/.test(fm))errors.push('frontmatter last_verified field missing');}else{errors.push('frontmatter block not found at top of docs/index.md');}if(!/Phase 59:.*Linux Provisioning/.test(c))errors.push('Version History row for Phase 59 missing');if(errors.length){console.error('ERRORS:',errors.join('; '));process.exit(1);}console.log('Cross-Platform References + frontmatter + Version History checks pass');process.exit(0);"</automated>
  </verify>
  <done>docs/index.md Cross-Platform References table contains 2 new Linux entries (Lifecycle + Capability Matrix); existing Monitoring and Operations row preserved (D-12); LIN-06 NOT surfaced (D-06); frontmatter `last_verified` refreshed; Version History row appended; node verification check exits 0.</done>
</task>

</tasks>

<verification>
- `docs/index.md` contains `## Linux Provisioning` H2 between Android H2 and Cross-Platform References H2
- 3 sub-H3 (Service Desk L1 / Desktop Engineering L2 / Admin Setup) with row counts 4/4/3 (V-59-07)
- Cross-Platform References table contains 2 new Linux entries (V-59-08)
- LIN-06 end-user guide NOT surfaced anywhere in docs/index.md (V-59-09 NEGATIVE)
- 5 pre-Phase-59 H2 anchors remain byte-identical (V-59-32 NEGATIVE regression-guard target satisfied for Plan 59-09)
- frontmatter `last_verified` refreshed to Phase 59 commit date
- Version History row added at top of table
</verification>

<success_criteria>
- HUB_LINUX_REACHABLE phase-level truth satisfied: admin reaches Linux L1 / L2 / Admin Setup in 1 click from docs/index.md
- D-01 / D-02 / D-03 / D-04 / D-05 / D-06 all satisfied with verbatim row content
- D-11 append-only contract honored: Android H2 + iOS H2 + macOS H2 + Windows H2 + Cross-Platform References H2 byte-identical pre/post
- ROADMAP Phase 59 SC#1 PRIMARY (Linux H2 with sub-tables) satisfied
- V-59-07 + V-59-08 + V-59-09 validator targets correctly populated for Plan 59-08 verification
</success_criteria>

<output>
After completion, create `.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-02-SUMMARY.md` documenting:
1. The exact line numbers where `## Linux Provisioning` H2 was inserted (post-edit grep `^## Linux Provisioning`)
2. The 3 sub-H3 row counts confirmed (L1=4 / L2=4 / Admin=3)
3. The 2 new Cross-Platform References rows added
4. Confirmation that LIN-06 (linux-intune-portal-enrollment.md) is NOT linked from docs/index.md (D-06 + V-59-09)
5. Confirmation that 5 pre-Phase-59 H2 anchors remain byte-identical (V-59-32 baseline)
6. New `last_verified` date in frontmatter
7. Note for downstream plans: "Plan 59-02 shipped Linux H2; Plan 59-04 may now append Operations H2 between Linux H2 and Cross-Platform References (Wave 3)."
</output>
