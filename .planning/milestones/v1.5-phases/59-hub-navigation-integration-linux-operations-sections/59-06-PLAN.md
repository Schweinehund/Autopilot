---
phase: 59
plan: 59-06
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - docs/quick-ref-l1.md
files_created: []
requirements: [CLEAN-08]
autonomous: true

must_haves:
  truths:
    - "L1 responder reading `docs/quick-ref-l1.md` sees a `## Linux Quick Reference` H2 with 4 sub-H3 sections matching iOS/Android four-part substructure verbatim per ROADMAP SC#4 (D-21 / D-22)"
    - "Sub-H3 substructure LOCKED: `### Top Checks` (4 items) / `### Linux Escalation Triggers` (4-5 items) / `### Linux Decision Tree` (single link to 09-linux-triage.md) / `### Linux Runbooks` (4-link list to runbooks 30-33)"
    - "Top Checks contains exactly 4 numbered items (parity with macOS/iOS top-checks count, NOT Android-5; per D-22)"
    - "NO `[Mode]` tags anywhere in Linux H2 body (`[BYOD]`/`[ZTE]`/`[AOSP]`/`[Knox]`/`[All GMS]`); NO distro-version tags (`[22.04]`/`[24.04]`); D-25 mode-tag-free contract; V-59-26 NEGATIVE regression-guard"
    - "Linux Decision Tree contains exactly one link to `decision-trees/09-linux-triage.md` (D-22 + V-59-27)"
    - "Linux Runbooks list contains 4 entries pointing to L1 runbooks 30 / 31 / 32 / 33 with iOS-style ` -- ` disambiguation per row (mirrors quick-ref-l1.md:142-147 iOS pattern)"
    - "QUICK_REF_LINUX_PRESENT phase-level truth (L1 portion) satisfied"
    - "New `## Linux Quick Reference` H2 appended after the existing `## Android Enterprise Quick Reference` H2 (lines 149-183) — append-only contract per Phase 57 D-31 / Phase 59 D-11 inheritance"
    - "Existing 4 platform Quick Reference H2s (Windows / macOS / iOS / Android) byte-identical post-edit (V-59-32 NEGATIVE regression-guard)"

  artifacts:
    - path: "docs/quick-ref-l1.md"
      provides: "L1 quick-reference card with Linux H2 (4-part substructure: Top Checks / Linux Escalation Triggers / Linux Decision Tree / Linux Runbooks)"
      contains:
        - "## Linux Quick Reference"
        - "### Top Checks"
        - "### Linux Escalation Triggers"
        - "### Linux Decision Tree"
        - "### Linux Runbooks"
        - "decision-trees/09-linux-triage.md"
        - "l1-runbooks/30-linux-enrollment-failed.md"
        - "l1-runbooks/31-linux-compliance-non-compliant.md"
        - "l1-runbooks/32-linux-ca-blocking-web-access.md"
        - "l1-runbooks/33-linux-agent-service-failure.md"
        - "intune-portal"
        - "intune-agent.timer"
        - "Ubuntu 22.04 / 24.04 LTS"

  key_links:
    - from: "docs/quick-ref-l1.md `## Linux Quick Reference` Top Checks H3"
      to: "Linux device validation surface (Intune admin center > Devices > Linux; dpkg / systemctl commands)"
      via: "4 numbered items — Intune device check / compliance state / package install / agent timer"
      pattern: "intune-portal|intune-agent\\.timer"
    - from: "docs/quick-ref-l1.md `### Linux Decision Tree` H3"
      to: "docs/decision-trees/09-linux-triage.md (Phase 51 deliverable)"
      via: "Single link"
      pattern: "decision-trees/09-linux-triage\\.md"
    - from: "docs/quick-ref-l1.md `### Linux Runbooks` H3"
      to: "docs/l1-runbooks/30-33 (4 Linux L1 runbooks; Phase 51 deliverables)"
      via: "4-link list with ` -- ` disambiguation"
      pattern: "l1-runbooks/(30-linux-enrollment-failed|31-linux-compliance-non-compliant|32-linux-ca-blocking-web-access|33-linux-agent-service-failure)\\.md"
---

<objective>
Append a net-new `## Linux Quick Reference` H2 section to `docs/quick-ref-l1.md` after the existing `## Android Enterprise Quick Reference` H2 (currently lines 149-183). The new H2 contains 4 sub-H3 sections (Top Checks / Linux Escalation Triggers / Linux Decision Tree / Linux Runbooks) matching iOS/Android four-part substructure verbatim per ROADMAP SC#4 + D-22 + D-25 mode-tag-free contract.

Plan 59-06 is Wave 2; depends on 59-01 (pre-edit anchor inventory must exist); ships D-21 / D-22 / D-25 mandates.

Purpose: Implements ROADMAP Phase 59 SC#4 (quick-ref-l1.md and quick-ref-l2.md contain Linux quick-reference sections matching iOS/Android structural depth) — the QUICK_REF_LINUX_PRESENT phase-level truth (L1 portion). Implements V-59-25 + V-59-26 + V-59-27 validator targets.

Output: 1 modified file (`docs/quick-ref-l1.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md
@.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-ANCHOR-INVENTORY.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
**Files this plan modifies:**
- MODIFY: `docs/quick-ref-l1.md` — append `## Linux Quick Reference` H2 at the END of the file (after Android Quick Reference H2 lines 149-183, before Version History H2 if present); refresh `last_verified` frontmatter; append Version History row

**Direct templates (read for verbatim mirror; DO NOT modify):**
- `docs/quick-ref-l1.md` lines 149-183 (Phase 57 Android Quick Reference H2 — structural template per D-22)
- `docs/quick-ref-l1.md` lines 117-148 (iOS Quick Reference H2 — non-mode-tag precedent per D-25; demonstrates 4-sub-H3 substructure without bracket Mode prefixes)

**Source files referenced by Linux Quick Reference rows (read-only — already exist from Phase 51):**
- `docs/decision-trees/09-linux-triage.md` (Phase 51 deliverable; Linux Decision Tree H3 single-link target)
- `docs/l1-runbooks/30-linux-enrollment-failed.md` (Phase 51; Linux Runbooks row 1)
- `docs/l1-runbooks/31-linux-compliance-non-compliant.md` (Phase 51; Linux Runbooks row 2)
- `docs/l1-runbooks/32-linux-ca-blocking-web-access.md` (Phase 51; Linux Runbooks row 3)
- `docs/l1-runbooks/33-linux-agent-service-failure.md` (Phase 51; Linux Runbooks row 4)

**Sibling Phase 59 plans (parallel-safe — disjoint files):**
- 59-02 / 59-04 edit docs/index.md (disjoint)
- 59-03 edits docs/operations/00-index.md (disjoint)
- 59-05 edits 4 glossary files (disjoint)
- 59-07 edits docs/quick-ref-l2.md (disjoint)
</interfaces>
</context>

<threat_model>
N/A — documentation-only phase, no attack surface introduced. Quick-reference card edits with no input validation, network exposure, or sensitive-data handling.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Append `## Linux Quick Reference` H2 with 4 sub-H3 sections to docs/quick-ref-l1.md (D-21 / D-22 / D-25)</name>
  <files>docs/quick-ref-l1.md</files>
  <read_first>
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-CONTEXT.md (D-21..D-22 + D-25 + D-26 V-59-25..27 validator scope)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-RESEARCH.md (§Pattern 2 verbatim Android Quick Reference template at quick-ref-l1.md:149-183 + iOS pattern at lines 117-148)
    - .planning/phases/59-hub-navigation-integration-linux-operations-sections/59-ANCHOR-INVENTORY.md (post-edit expected: new `## Linux Quick Reference` H2 in quick-ref-l1.md)
    - docs/quick-ref-l1.md (FULL FILE — locate Android Quick Reference H2 end + Version History H2 start; insertion point is between them)
    - docs/decision-trees/09-linux-triage.md (verify file exists and Mermaid 4-branch tree per LIN-07; Decision Tree single-link target)
    - docs/l1-runbooks/30-linux-enrollment-failed.md through 33-linux-agent-service-failure.md (verify 4 files exist; gather H1 / frontmatter for accurate description text)
  </read_first>
  <action>
Use the Edit tool to append the new H2 block. The insertion point is immediately AFTER the Android Quick Reference H2 (currently ends around line 183) and BEFORE the next H2 (likely a Version History H2 or end-of-file).

Locate the existing Android Quick Reference H2 closing prose. The file likely has structure:
```
[Android Quick Reference H2 content ending around line 183]

## Version History    (or no Version History — possibly direct EOF)
```

Append the new Linux H2 immediately before any subsequent `## Version History` H2 (or at EOF if no Version History exists yet):

```

---

## Linux Quick Reference

**Platform:** Linux (Ubuntu 22.04 / 24.04 LTS) through Microsoft Intune

### Top Checks

1. Device visible in Intune? -- Intune admin center > Devices > Linux -- search by serial; check enrollment state
2. Compliance state? -- Devices > [device] > Device compliance -- Compliant vs Non-compliant + non-compliant categories (Allowed Distributions / Custom Compliance / Device Encryption / Password Policy)
3. `intune-portal` package installed? -- `dpkg -l intune-portal` returns `ii` status (status indicator `ii` = installed + configured)
4. `intune-agent.timer` running? -- `systemctl --user list-timers intune-agent.timer` shows recent activation timestamp

### Linux Escalation Triggers

- Enrollment timeout > 30 minutes after `intune-portal` sign-in --> **Escalate L2** (collect: `journalctl -u intune-agent`, `/var/log/dpkg.log`, `/var/log/intune-update.log`)
- Compliance category failure persists across 2+ evaluation cycles --> **Escalate L2** (collect: full compliance evaluation output + Bash discovery script logs)
- Conditional access blocking Microsoft Edge sign-in despite device showing as compliant in Intune --> **Escalate L2** (cause-A: stale CA evaluation; cause-B: web-app CA mode mis-scoped; reference [admin-setup-linux/05-conditional-access.md])
- Intune agent service crash-loop on systemd restart --> **Escalate L2** (collect: `systemctl status intune-agent`, `journalctl --user -u intune-agent`)
- Identity Broker `intune-portal` 2.0.2+ Java→broker breaking change re-enrollment loop --> **Escalate L2** (Phase 50 LIN-05 known pitfall; verify post-update CA assignments / filters / Entra group memberships)

### Linux Decision Tree

- [Linux Triage Decision Tree](decision-trees/09-linux-triage.md) -- start here for Linux Intune failures

### Linux Runbooks

- [Linux Enrollment Failed](l1-runbooks/30-linux-enrollment-failed.md) -- package install error / sign-in failure / enrollment timeout
- [Linux Device Non-Compliant](l1-runbooks/31-linux-compliance-non-compliant.md) -- distro/version out of range / disk not encrypted / password policy not met / custom compliance failure
- [Linux Conditional Access Blocking Web Access](l1-runbooks/32-linux-ca-blocking-web-access.md) -- device not enrolled / device not compliant / Edge not signed in
- [Linux Intune Agent Service Failure](l1-runbooks/33-linux-agent-service-failure.md) -- `intune-agent` service not running / timer not firing
```

**Critical compliance rules (D-21 / D-22 / D-25 / V-59-NN):**
- 4 sub-H3 literals MUST be exactly: `### Top Checks` / `### Linux Escalation Triggers` / `### Linux Decision Tree` / `### Linux Runbooks` (V-59-25 anchor pin)
- Top Checks MUST contain EXACTLY 4 numbered items (D-22 — parity with macOS/iOS, NOT Android-5; row count is locked)
- NO `[Mode]` bracket tags anywhere (V-59-26 NEGATIVE — no `[BYOD]`/`[ZTE]`/`[AOSP]`/`[Knox]`/`[All GMS]`/`[22.04]`/`[24.04]` literals; D-25 mode-tag-free contract; Linux has no triage modes per `09-linux-triage.md:15`)
- Linux Decision Tree MUST contain exactly ONE link to `decision-trees/09-linux-triage.md` (V-59-27); single-link list with iOS-style ` -- start here ...` disambiguation
- Linux Runbooks MUST contain exactly 4 entries (one per L1 runbook 30-33) with iOS-style ` -- ` disambiguation per row (mirrors `quick-ref-l1.md:142-147` iOS pattern; NOT Android's bracket-tag-prefix pattern at lines 153-160)
- USE `--` (double-hyphen) for description disambiguation matching iOS/Android conventions, NOT `—` em-dash
- USE `**Platform:** Linux (Ubuntu 22.04 / 24.04 LTS) through Microsoft Intune` intro line (mirrors Android `**Platform:** Android Enterprise through Microsoft Intune` at line 151 + iOS `**Platform:** iOS / iPadOS through Microsoft Intune` at line 119)
- Existing `## Windows Autopilot Quick Reference` / `## macOS Provisioning Quick Reference` / `## iOS/iPadOS Quick Reference` / `## Android Enterprise Quick Reference` H2s MUST remain byte-identical post-edit (V-59-32 NEGATIVE regression-guard target)
- Use `---` separator before the new H2 to match the existing pattern between Android Quick Reference and any subsequent H2

**Frontmatter refresh:**
Update the `last_verified` field in docs/quick-ref-l1.md frontmatter (top of file) to today's date; update `review_by` to +60 days. Use ISO date format `YYYY-MM-DD`.

**Version History row:**
Append a new row at the TOP of the Version History table (immediately below the header `| Date | Change | Author |` separator). If the file lacks a Version History H2, add one immediately after the new Linux Quick Reference H2:
```
| {today's date} | Phase 59 (CLEAN-08): added Linux Quick Reference H2 (4-part substructure: Top Checks 4 items / Linux Escalation Triggers / Linux Decision Tree single link / Linux Runbooks 4-link list) matching iOS quick-ref non-mode-tag pattern; D-25 mode-tag-free contract | -- |
```

  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/quick-ref-l1.md','utf8');const errors=[];function sliceH2(content,h2){const re=new RegExp('^'+h2.replace(/[/\\\\^$*+?.()|[\\]{}]/g,'\\\\$&')+'\\\\s*$','m');const m=content.match(re);if(!m)return null;const idx=content.indexOf(m[0]);const after=content.slice(idx+m[0].length);const next=after.search(/^## /m);return next>0?content.slice(idx,idx+m[0].length+next):content.slice(idx);}const linux=sliceH2(c,'## Linux Quick Reference');if(!linux){errors.push('## Linux Quick Reference H2 absent');}else{const subH3s=['### Top Checks','### Linux Escalation Triggers','### Linux Decision Tree','### Linux Runbooks'];for(const h3 of subH3s){const re=new RegExp('^'+h3.replace(/[/\\\\^$*+?.()|[\\]{}]/g,'\\\\$&')+'\\\\s*$','m');if(!re.test(linux))errors.push('Sub-H3 missing in Linux Quick Reference H2: '+h3);}function sliceH3(region,h3){const idx=region.indexOf(h3);if(idx<0)return null;const after=region.slice(idx+h3.length);const nextH3=after.search(/^### /m);return nextH3>0?after.slice(0,nextH3):after;}const top=sliceH3(linux,'### Top Checks');if(top){const items=(top.match(/^[1-9]\\.\\s/gm)||[]).length;if(items!==4)errors.push('Top Checks numbered items='+items+' (expected 4 per D-22)');}const decisionTree=sliceH3(linux,'### Linux Decision Tree');if(decisionTree){const links=(decisionTree.match(/decision-trees\\/09-linux-triage\\.md/g)||[]).length;if(links!==1)errors.push('Linux Decision Tree links to 09-linux-triage.md count='+links+' (expected exactly 1 per V-59-27)');}const runbooks=sliceH3(linux,'### Linux Runbooks');if(runbooks){const expectedFiles=['l1-runbooks/30-linux-enrollment-failed.md','l1-runbooks/31-linux-compliance-non-compliant.md','l1-runbooks/32-linux-ca-blocking-web-access.md','l1-runbooks/33-linux-agent-service-failure.md'];for(const f of expectedFiles){if(!runbooks.includes(f))errors.push('Linux Runbooks missing required link: '+f);}}const modeTagPatterns=/\\[(BYOD|ZTE|AOSP|Knox|All GMS|22\\.04|24\\.04|Mode|22\\.04 LTS|24\\.04 LTS)\\]/g;const modeTagMatches=linux.match(modeTagPatterns);if(modeTagMatches&&modeTagMatches.length>0)errors.push('V-59-26 NEGATIVE violation: Mode tags present in Linux H2: '+modeTagMatches.join(','));}const preH2s=['## Windows Autopilot Quick Reference','## macOS Provisioning Quick Reference','## iOS/iPadOS Quick Reference','## Android Enterprise Quick Reference'];for(const h of preH2s){if(!c.includes(h))errors.push('Pre-Phase-59 Quick Reference H2 missing: '+h);}if(errors.length){console.error('ERRORS:',errors.join('; '));process.exit(1);}console.log('Linux Quick Reference L1 + 4 sub-H3 + Top Checks=4 items + Decision Tree=1 link + Runbooks=4 entries + Mode-tag-free + anchor stability checks pass');process.exit(0);"</automated>
  </verify>
  <done>docs/quick-ref-l1.md contains `## Linux Quick Reference` H2 with 4 sub-H3 anchors (V-59-25); Top Checks=4 items (D-22); Linux Decision Tree=1 link to 09-linux-triage.md (V-59-27); Linux Runbooks=4 entries to runbooks 30-33; NO mode tags or distro tags (V-59-26 NEGATIVE); 4 pre-Phase-59 platform Quick Reference H2s byte-identical (V-59-32); node verification check exits 0.</done>
</task>

</tasks>

<verification>
- `docs/quick-ref-l1.md` contains `## Linux Quick Reference` H2 (V-59-25)
- 4 sub-H3 anchors present: `### Top Checks` / `### Linux Escalation Triggers` / `### Linux Decision Tree` / `### Linux Runbooks` (V-59-25)
- Top Checks contains exactly 4 numbered items (D-22)
- Linux Decision Tree H3 contains exactly 1 link to `decision-trees/09-linux-triage.md` (V-59-27)
- Linux Runbooks H3 contains 4 links to runbooks 30 / 31 / 32 / 33
- Linux H2 body does NOT contain `[BYOD]`/`[ZTE]`/`[AOSP]`/`[Knox]`/`[All GMS]`/`[22.04]`/`[24.04]` literals (V-59-26 NEGATIVE; D-25 mode-tag-free contract)
- 4 pre-Phase-59 platform Quick Reference H2s byte-identical (V-59-32 regression-guard target)
- frontmatter `last_verified` refreshed; Version History row added
</verification>

<success_criteria>
- QUICK_REF_LINUX_PRESENT phase-level truth (L1 portion) satisfied
- D-21 / D-22 / D-25 all satisfied
- ROADMAP Phase 59 SC#4 partially satisfied (L1 portion; L2 portion satisfied by Plan 59-07)
- V-59-25 + V-59-26 + V-59-27 validator targets correctly populated for Plan 59-08 verification
</success_criteria>

<output>
After completion, create `.planning/phases/59-hub-navigation-integration-linux-operations-sections/59-06-SUMMARY.md` documenting:
1. The exact line numbers where `## Linux Quick Reference` H2 was inserted (post-edit grep `^## Linux Quick Reference docs/quick-ref-l1.md`)
2. The 4 sub-H3 anchors confirmed
3. Top Checks item count (4 — locked per D-22)
4. Linux Decision Tree link count (exactly 1 per V-59-27)
5. Linux Runbooks link list (4 entries with file paths verified)
6. Confirmation that no Mode-tag literals are present anywhere in the new H2 body (V-59-26 satisfied)
7. Note for Plan 59-08 (validator): "All Linux Quick Reference L1 V-59-NN targets are now in place: V-59-25 (H2 + 4 sub-H3), V-59-26 (NEGATIVE no Mode tags), V-59-27 (Decision Tree single link)."
</output>
