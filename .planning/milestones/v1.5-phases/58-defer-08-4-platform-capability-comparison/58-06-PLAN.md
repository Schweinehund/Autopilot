---
phase: 58
plan: 58-06
type: execute
wave: 5
depends_on: [58-03, 58-05]
files_modified:
  - scripts/validation/v1.5-milestone-audit.mjs
files_created: []
requirements: [CLEAN-05]
autonomous: true

must_haves:
  truths:
    - "`scripts/validation/v1.5-milestone-audit.mjs` C12 entry (currently at line 506-542) no longer has the `informational: true` flag at line 508 — promotion from informational → blocking per AUDIT-04"
    - "C12 entry's `id: 12` and `name: 'C12: 4-platform comparison structural validation'` literals UNCHANGED (only the `informational: true` flag is removed; the entire `run()` body and surrounding C12 metadata are preserved verbatim)"
    - "All other check entries in the harness (C1..C11, C13, plus any others) UNCHANGED — Plan 58-06 is a single-line surgical edit at line 508"
    - "`docs/reference/4-platform-capability-comparison.md` MUST exist BEFORE this patch lands (Plan 58-03 gate); without the file, C12 will fail post-promotion (file-existence pre-gate at lines 515-517 returns informational PASS only when file absent — once file exists AND `informational: true` flag is removed, C12 runs structural validation in blocking mode)"
    - "After patch: `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 with C12 PASS in blocking mode (because Plan 58-03's comparison doc satisfies the link-not-copy structural validation that C12 enforces — verified by V-58-07 mirror in Plan 58-05's check-phase-58.mjs)"
    - "After patch: Plan 58-05's V-58-25 assertion now PASSes (it scanned for `informational: true` immediately preceding the C12 name literal; with that flag removed, V-58-25 reports PASS)"
    - "Optional: line 540 detail string `return { pass: true, detail: '(informational)' };` may be updated to `return { pass: true, detail: '5 platform columns + all cells link-bearing' };` for clearer post-promotion semantics — this is optional polish per RESEARCH §C12 promotion mechanics, not required by the promotion contract"

  artifacts:
    - path: "scripts/validation/v1.5-milestone-audit.mjs"
      provides: "C12 promoted from informational → blocking (AUDIT-04 verbatim — promotion gated by file existence, which Plan 58-03 satisfied)"
      contains:
        - "id: 12"
        - "C12: 4-platform comparison structural validation"
        - "docs/reference/4-platform-capability-comparison.md"
        - "platforms = ['Windows', 'macOS', 'iOS', 'Android', 'Linux']"
      forbids:
        - "informational: true (near id 12 only — flag must be absent in C12 region; preserved in other check entries)"

  key_links:
    - from: "scripts/validation/v1.5-milestone-audit.mjs (C12 entry, post-patch)"
      to: "docs/reference/4-platform-capability-comparison.md (Plan 58-03 deliverable)"
      via: "C12 file-existence pre-gate at line 515-517 + structural validation at line 519+ — both enforce post-patch when blocking"
      pattern: "docs/reference/4-platform-capability-comparison\\.md"
    - from: "scripts/validation/v1.5-milestone-audit.mjs (C12 promotion)"
      to: "scripts/validation/check-phase-58.mjs (V-58-25 mirror assertion)"
      via: "V-58-25 verifies the `informational: true` flag absence near C12 name"
      pattern: "informational:\\s*true"
---

<objective>
Promote C12 (4-platform comparison structural validation) in `scripts/validation/v1.5-milestone-audit.mjs` from informational → blocking by removing the `informational: true` flag at line 508. This is the AUDIT-04 promotion gate that lands at Phase 58 close (NOT Phase 60 — CONTEXT D-17 + D-18 explicitly override the prior Phase 60 ROADMAP routing for this single edit; Phase 60 still owns the structural-scope EXPANSION of C12 per ROADMAP:382-383, but Phase 58 owns the informational → blocking flag removal).

Wave 5 — depends on Plan 58-03 (the comparison doc must exist for C12 to PASS post-promotion; without it, C12 would fail because the file-existence pre-gate at lines 515-517 only returns informational PASS, not blocking PASS). Also depends on Plan 58-05 (V-58-25 mirror assertion must exist to verify the promotion).

The patch is a one-line removal — surgical and reversible. The C12 entry's `run()` body, file-existence pre-gate logic, structural validation, and platform-column check are all preserved verbatim. Only the `informational: true` flag at line 508 is removed.

Per CONTEXT D-17: C12 scope is link-presence-only (`/\[.+\]\(.+\)/`); D-17 is the Referee verification that C12 does NOT enforce link-target paths, prose-length, or duplicate-content detection (those are human-review-enforced per SC#2). The promotion only changes the FLAG, not the regex scope.

Per CONTEXT D-16: this single-line patch lands as part of the Phase 58 plan-series; per progressive-landing pattern, it lands AFTER Plan 58-03 (comparison doc exists) AND AFTER Plan 58-05 (validator runnable).

Output: 1 modified file (`scripts/validation/v1.5-milestone-audit.mjs`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-defer-08-4-platform-capability-comparison/58-CONTEXT.md
@.planning/phases/58-defer-08-4-platform-capability-comparison/58-RESEARCH.md
@scripts/validation/v1.5-milestone-audit.mjs
@docs/reference/4-platform-capability-comparison.md

<interfaces>
**File this plan modifies:**
- MODIFY: `scripts/validation/v1.5-milestone-audit.mjs` (single-line edit at line 508 — remove `informational: true,` line)

**Verification dependencies:**
- `docs/reference/4-platform-capability-comparison.md` (Plan 58-03 deliverable — file existence is the C12 pre-gate)
- `scripts/validation/check-phase-58.mjs` (Plan 58-05 deliverable — V-58-25 mirrors this promotion check)

**Sibling Phase 58 plans:**
- 58-03 + 58-05 are gates
- 58-07 will run the harness and validator at Phase 58 close as part of the pre-commit gate; both should exit 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove `informational: true` flag at line 508 of v1.5-milestone-audit.mjs (C12 promotion)</name>
  <files>scripts/validation/v1.5-milestone-audit.mjs</files>
  <read_first>
    - .planning/phases/58-defer-08-4-platform-capability-comparison/58-CONTEXT.md (D-17 + D-18 — C12 promotion lands at Phase 58 close, NOT Phase 60)
    - .planning/phases/58-defer-08-4-platform-capability-comparison/58-RESEARCH.md (§C12 Promotion Mechanics — single-line patch specification)
    - scripts/validation/v1.5-milestone-audit.mjs (lines 505-542 verbatim — confirm C12 entry structure: `id: 12,` line ~506, `name: 'C12: 4-platform comparison structural validation',` line ~507, `informational: true,` line 508, comment block lines 509-511, `run() {` line 512)
    - docs/reference/4-platform-capability-comparison.md (Plan 58-03 deliverable — verify file exists; Plan 58-06 CANNOT land if this file is missing — abort if so)
  </read_first>
  <action>
**Pre-flight gate check:** verify `docs/reference/4-platform-capability-comparison.md` exists (Plan 58-03 deliverable). Abort if missing — C12 promotion without the file would cause C12 to FAIL in blocking mode.

Open `scripts/validation/v1.5-milestone-audit.mjs` and locate the C12 entry. The current relevant block (lines 505-512) is:

```javascript
  {
    id: 12,
    name: 'C12: 4-platform comparison structural validation',
    informational: true,
    // AUDIT-04: INFORMATIONAL-FIRST. File-existence pre-gate. Promotes to blocking once
    // docs/reference/4-platform-capability-comparison.md exists (Phase 58+).
    // Link-not-copy: every non-empty data cell must contain a markdown hyperlink.
    run() {
```

Remove line 508 (`    informational: true,`) entirely. The new block becomes:

```javascript
  {
    id: 12,
    name: 'C12: 4-platform comparison structural validation',
    // AUDIT-04: PROMOTED TO BLOCKING at Phase 58 close (informational flag removed).
    // File-existence pre-gate retained at lines below — informational PASS when file absent (Phase 48-57);
    // BLOCKING when file exists (Phase 58+).
    // Link-not-copy: every non-empty data cell must contain a markdown hyperlink.
    run() {
```

(The comment block is updated to reflect the post-promotion state. The original 3-line comment is replaced with a 4-line comment that documents the promotion. This is optional but recommended for forensic clarity.)

**Strict rules for this edit:**
1. **Remove EXACTLY the `informational: true,` line** — DO NOT remove the trailing comma from the line above (the `name:` line still ends with a comma); DO NOT remove blank lines or alter indentation of surrounding lines
2. **DO NOT touch the `run()` body** at lines 512-541 — the file-existence pre-gate at lines 515-517 + the structural validation at lines 519-540 must be preserved verbatim
3. **DO NOT touch any other check entry** (C1..C11, C13, plus any other ids) — Plan 58-06 is C12-only
4. **DO NOT change the `id: 12,` or `name: 'C12: ...'` literals** — V-58-25 in `check-phase-58.mjs` searches for the literal name and looks BACK 200 chars to detect the `informational: true` flag; if you change the name, V-58-25 won't find C12

**Optional polish (non-required):** the line 540 detail string `return { pass: true, detail: '(informational)' };` may be updated to `return { pass: true, detail: '5 platform columns + all cells link-bearing' };` for clearer post-promotion success-path semantics. If updated, this MUST be the line WITHIN the `run()` body that returns success after structural validation passes (NOT the file-absent informational return at line 517 — that one stays as-is because the file-existence pre-gate is still informational behavior). Plan-author judgment.

**Verification approach:**
1. Run `node scripts/validation/v1.5-milestone-audit.mjs` — expect exit 0 with all blocking checks PASS (C12 should now report blocking PASS because the comparison doc exists from Plan 58-03 and all cells are link-bearing per Plan 58-03's authoring discipline)
2. Run `node scripts/validation/check-phase-58.mjs` — expect V-58-25 PASS (the `informational: true` flag near the C12 name is gone)
3. Grep the harness file: `grep -n "informational" scripts/validation/v1.5-milestone-audit.mjs` — expect remaining `informational` mentions only in OTHER check entries (C11, C13, etc.) but NOT in the C12 entry block (lines ~506-512)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('scripts/validation/v1.5-milestone-audit.mjs','utf8');const c12NameIdx=c.indexOf(\"name: 'C12: 4-platform comparison structural validation'\");if(c12NameIdx<0){console.error('C12 entry not found in harness');process.exit(1)}const before=c.slice(Math.max(0,c12NameIdx-200),c12NameIdx);const inC12Region=c.slice(Math.max(0,c12NameIdx-50),c12NameIdx+800);if(/informational:\\s*true/.test(before)){console.error('FAIL: informational: true flag still preceding C12 name (within 200 chars)');process.exit(1)}if(/^\\s*informational:\\s*true,?\\s*$/m.test(inC12Region)){console.error('FAIL: informational: true literal still present in C12 region');process.exit(1)}console.log('C12 informational flag removed; promotion patch landed.');process.exit(0);" && node scripts/validation/v1.5-milestone-audit.mjs > /tmp/v15-audit.log 2>&1; node -e "const fs=require('fs');const log=fs.readFileSync('/tmp/v15-audit.log','utf8');if(!/C12.*PASS/i.test(log)&&!/C12.*pass/i.test(log)){console.error('C12 not reported PASS in harness output:',log.slice(-500));process.exit(1)}console.log('Harness reports C12 PASS post-promotion.');process.exit(0);"</automated>
  </verify>
  <done>`scripts/validation/v1.5-milestone-audit.mjs` line 508 `informational: true,` removed; C12 name + id + run() body preserved verbatim; harness still parses; harness exits 0 with C12 PASS in blocking mode; node check exits 0.</done>
</task>

</tasks>

<verification>
- `scripts/validation/v1.5-milestone-audit.mjs` C12 entry no longer has `informational: true` flag (line 508 removed)
- C12 entry's `id: 12` and `name: 'C12: 4-platform comparison structural validation'` literals UNCHANGED
- C12 entry's `run()` body UNCHANGED (file-existence pre-gate + structural validation + platform-column check + link-not-copy check all preserved)
- All other check entries in the harness UNCHANGED (C1..C11, C13)
- `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 with C12 PASS in blocking mode
- Plan 58-05's V-58-25 assertion now PASSes when run against the post-patch harness
</verification>

<success_criteria>
- AUDIT-04 satisfied: C12 promoted from informational → blocking at Phase 58 close (NOT Phase 60 — D-17/D-18 routing override)
- Plan 58-03's comparison doc satisfies the C12 link-not-copy structural validation in blocking mode
- C12 promotion mechanics per RESEARCH §C12 promotion: single-line removal at line 508
- ROADMAP requirement traceability: AUDIT-04 row at REQUIREMENTS.md line 85 ("informational-first when scaffolded in Phase 48; promotes to blocking once `4-platform-capability-comparison.md` exists in Phase 58") satisfied
- Phase 60 still owns C12 structural-scope expansion per ROADMAP:382-383 (a separate edit Phase 58 does NOT touch); Phase 58 only owns the flag removal
</success_criteria>

<output>
After completion, create `.planning/phases/58-defer-08-4-platform-capability-comparison/58-06-SUMMARY.md` documenting:
1. Confirmation that Plan 58-03 (comparison doc) and Plan 58-05 (validator) gates were satisfied before patch landing
2. The exact line removed (line 508 `informational: true,`)
3. The optional comment-block update (if applied) showing pre/post state
4. Harness execution output snippet showing C12 PASS in blocking mode
5. V-58-25 in check-phase-58.mjs status post-patch (expected: PASS)
6. Note for Plan 58-07 (close gate): "C12 promotion landed; both v1.5-milestone-audit.mjs and check-phase-58.mjs exit 0; harness blocking checks complete; ready for VERIFICATION.md authoring + STATE.md/ROADMAP.md update."
</output>
