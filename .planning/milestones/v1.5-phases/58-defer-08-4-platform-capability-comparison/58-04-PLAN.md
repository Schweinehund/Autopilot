---
phase: 58
plan: 58-04
type: execute
wave: 4
depends_on: [58-01, 58-02, 58-03]
files_modified:
  - docs/reference/macos-capability-matrix.md
  - docs/reference/ios-capability-matrix.md
  - docs/reference/android-capability-matrix.md
  - docs/reference/linux-capability-matrix.md
requirements: [CLEAN-05]
autonomous: true

must_haves:
  truths:
    - "`docs/reference/macos-capability-matrix.md` intro paragraph (currently line 11) contains a single appended sentence cross-referencing the new comparison doc with the literal text per D-12 recommended literal: 'For a side-by-side comparison of macOS capabilities against Windows, macOS, iOS, Android, and Linux, see [4-Platform Capability Comparison](4-platform-capability-comparison.md).' (or minor wording variant per D-12 plan-author discretion, with stable link target + link text `4-Platform Capability Comparison`)"
    - "`docs/reference/ios-capability-matrix.md` intro paragraph (currently line 11) contains a single appended sentence cross-referencing the new comparison doc per D-12: 'For a side-by-side comparison of iOS capabilities against Windows, macOS, iOS, Android, and Linux, see [4-Platform Capability Comparison](4-platform-capability-comparison.md).'"
    - "`docs/reference/android-capability-matrix.md` intro paragraph (currently line 12) contains a single appended sentence cross-referencing the new comparison doc per D-12: 'For a side-by-side comparison of Android capabilities against Windows, macOS, iOS, Android, and Linux, see [4-Platform Capability Comparison](4-platform-capability-comparison.md).'"
    - "`docs/reference/linux-capability-matrix.md` line 70 prose rewritten to drop the `(when shipped)` hedge per D-13 — current text 'See Phase 58's [4-Platform Capability Comparison](4-platform-capability-comparison.md) (when shipped) for that.' becomes 'See the [4-Platform Capability Comparison](4-platform-capability-comparison.md) for that.' (link target preserved; `(when shipped)` parenthetical removed; `Phase 58's` ownership reference removed since the doc has now shipped)"
    - "`docs/reference/linux-capability-matrix.md` line 112 prose rewritten to drop `(when Phase 58 ships)` hedge per D-13 — current text '- [4-Platform Capability Comparison](4-platform-capability-comparison.md) — when Phase 58 ships' becomes '- [4-Platform Capability Comparison](4-platform-capability-comparison.md)' (em-dash trailing prose removed; link literal preserved)"
    - "After all edits, the literal `(when shipped)` does NOT appear ANYWHERE in `docs/reference/linux-capability-matrix.md` (V-58-20 NEGATIVE assertion target)"
    - "`docs/reference/android-capability-matrix.md` lines 132-139 footer F3 retrofit per D-14: anchor `<a id=\"deferred-4-platform-unified-capability-comparison\"></a>` PRESERVED byte-identical at line 132; H3 heading text `### Deferred: 4-platform unified capability comparison` rewritten to drop `Deferred:` prefix → `### 4-platform unified capability comparison` (Phase 45 AEAOSPFULL-09 precedent: pattern of dropping `Deferred:` prefix once stub body retargeted, per RESEARCH §Android Footer F3 Mechanics); body lines 133-139 (the 7-line block describing the matrix as Android-centric with the AECOMPARE-01 deferral) REMOVED and REPLACED with a single forward-link sentence: 'For a side-by-side comparison of Android Enterprise capabilities against Windows, macOS, iOS, and Linux across all 6 capability domains (Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access), see [4-Platform Capability Comparison](4-platform-capability-comparison.md).'"
    - "After F3 retrofit, the Android matrix footer body does NOT contain pre-Phase-58 deferral wording: the literals `deferred to v1.5` AND `AECOMPARE-01` MUST NOT appear within the H3 block (line 132 anchor through end of section before the closing `---` separator) — V-58-19 NEGATIVE assertion"
    - "Phase 45 anchor `<a id=\"deferred-full-aosp-capability-mapping\"></a>` (line 124) and surrounding heading + body (lines 124-130) UNCHANGED byte-identical pre/post (V-58-22 NEGATIVE regression-guard)"
    - "All 5 H2s in macOS / iOS matrices PLUS the new `## Conditional Access` H2 added by Plan 58-02 PLUS `## Key Gaps Summary` PLUS `## See Also` PLUS `## Version History` UNCHANGED literally pre/post Plan 58-04 (intro 5C edit is paragraph-text-only, not H2-structure)"
    - "All 6 H2s in Android matrix (Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access added by 58-02, Cross-Platform Equivalences) PLUS Key Gaps Summary PLUS See Also PLUS Version History UNCHANGED literally pre/post Plan 58-04 (intro + footer F3 are surface-edit-only)"

  artifacts:
    - path: "docs/reference/macos-capability-matrix.md"
      provides: "macOS matrix intro 5C cross-reference sentence appended (D-12); existing intro paragraph PRESERVED, new sentence inserted at end of paragraph"
      contains:
        - "4-Platform Capability Comparison"
        - "4-platform-capability-comparison.md"
        - "## Conditional Access"
        - "## Enrollment"
        - "## Key Gaps Summary"
    - path: "docs/reference/ios-capability-matrix.md"
      provides: "iOS matrix intro 5C cross-reference sentence appended (D-12)"
      contains:
        - "4-Platform Capability Comparison"
        - "4-platform-capability-comparison.md"
        - "## Conditional Access"
        - "## Enrollment"
        - "## Key Gaps Summary"
    - path: "docs/reference/android-capability-matrix.md"
      provides: "Android matrix intro 5C cross-ref + footer F3 retrofit (D-12 + D-14); anchor preserved; body retargeted to forward-link"
      contains:
        - "4-Platform Capability Comparison"
        - "4-platform-capability-comparison.md"
        - "## Conditional Access"
        - "## Cross-Platform Equivalences"
        - "<a id=\"deferred-4-platform-unified-capability-comparison\"></a>"
        - "<a id=\"deferred-full-aosp-capability-mapping\"></a>"
        - "### 4-platform unified capability comparison"
    - path: "docs/reference/linux-capability-matrix.md"
      provides: "Linux matrix `(when shipped)` hedge removed at line 70 + line 112 (D-13); link literal preserved"
      contains:
        - "4-Platform Capability Comparison"
        - "4-platform-capability-comparison.md"
        - "## Conditional Access"

  key_links:
    - from: "docs/reference/{macos,ios,android}-capability-matrix.md (intro paragraphs)"
      to: "docs/reference/4-platform-capability-comparison.md"
      via: "D-12 5C single-sentence cross-reference inserted at end of existing intro paragraph; link text `4-Platform Capability Comparison` literal"
      pattern: "\\[4-Platform Capability Comparison\\]\\(4-platform-capability-comparison\\.md\\)"
    - from: "docs/reference/android-capability-matrix.md (footer line 132 anchor)"
      to: "docs/reference/4-platform-capability-comparison.md"
      via: "D-14 F3 anchor-preservation + body-retargeting; anchor `<a id=\"deferred-4-platform-unified-capability-comparison\"></a>` preserved verbatim; body replaced with forward-link prose"
      pattern: "<a id=\"deferred-4-platform-unified-capability-comparison\"></a>"
    - from: "docs/reference/linux-capability-matrix.md (lines 70 + 112)"
      to: "docs/reference/4-platform-capability-comparison.md"
      via: "D-13 atomic hedge removal — `(when shipped)` parenthetical and `when Phase 58 ships` em-dash trailing prose dropped; link literal preserved"
      pattern: "\\(when shipped\\)|when Phase 58 ships"
---

<objective>
Execute the surface-edit cluster of Phase 58: D-12 sibling matrix intro 5C cross-reference (macOS / iOS / Android — single sentence appended to existing intro paragraph), D-13 Linux matrix `(when shipped)` hedge removal (line 70 + line 112; atomic with comparison doc landing per D-13 lock), and D-14 Android footer F3 retrofit (anchor preserved at line 132; body lines 133-139 retargeted to single-line forward-link prose; Phase 45 AEAOSPFULL-09 precedent inheritance).

This plan consolidates the 4 surface edits into a single plan because they are all low-risk paragraph-level / line-level edits, all cross-reference the same `4-platform-capability-comparison.md` filename, and all depend on Plan 58-03 having shipped that file. Per RESEARCH §atomic-commit-landing-strategy, Plan 58-04 lands as a single progressive-landing commit covering 4 file edits.

Wave 4 — depends on 58-01 (anchor inventory baseline; Linux+Android edit zones must respect anchor preservation) AND 58-03 (comparison doc must exist for forward-references to resolve at validator runtime).

Purpose: Implements CONTEXT D-12 (5C intro paragraph cross-reference, NOT 5B blockquote / NOT 5D new H2) + D-13 (Linux matrix hedge removal — atomic with comparison doc landing) + D-14 (Android footer F3 — anchor preserved, body retargeted, `Deferred:` prefix dropped per Phase 45 AEAOSPFULL-09 precedent).

Output: 4 modified files (macOS / iOS / Android / Linux capability matrices).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-defer-08-4-platform-capability-comparison/58-CONTEXT.md
@.planning/phases/58-defer-08-4-platform-capability-comparison/58-RESEARCH.md
@.planning/phases/58-defer-08-4-platform-capability-comparison/58-ANCHOR-INVENTORY.md
@docs/reference/4-platform-capability-comparison.md
@CLAUDE.md

<interfaces>
**Files this plan modifies:**
- MODIFY: `docs/reference/macos-capability-matrix.md` (intro paragraph line 11 — append D-12 5C sentence)
- MODIFY: `docs/reference/ios-capability-matrix.md` (intro paragraph line 11 — append D-12 5C sentence)
- MODIFY: `docs/reference/android-capability-matrix.md` (intro paragraph line 12 — append D-12 5C sentence; footer lines 132-139 — D-14 F3 retrofit)
- MODIFY: `docs/reference/linux-capability-matrix.md` (line 70 — D-13 hedge removal; line 112 — D-13 hedge removal)

**Pattern source — Phase 45 AEAOSPFULL-09 footer-stub-retarget precedent (read for D-14 F3 mirror):**
- `docs/reference/android-capability-matrix.md:124-130` — Phase 45 AEAOSPFULL-09 verbatim model: anchor `<a id="deferred-full-aosp-capability-mapping"></a>` preserved at line 124; H3 heading was changed from `### Deferred: full AOSP capability mapping` to `### AOSP per-OEM capability mapping` (Deferred prefix dropped); body retargeted to single forward-link prose. D-14 F3 mirrors this exactly for the `deferred-4-platform-unified-capability-comparison` anchor at line 132.

**Recommended D-12 literal sentence (CONTEXT.md §specifics):**
- "For a side-by-side comparison of {platform} capabilities against Windows, macOS, iOS, Android, and Linux, see [4-Platform Capability Comparison](4-platform-capability-comparison.md)." (substitute `{platform}` per matrix: macOS / iOS / Android)
- Plan-author may use minor wording variation per D-12 discretion as long as link target (`4-platform-capability-comparison.md`) and link text (`4-Platform Capability Comparison`) are stable

**Sibling Phase 58 plans:**
- 58-03 (comparison doc) is a gate; this plan is unblocked when 58-03 ships
- 58-05 will pin V-58-14/15/16 (intro cross-refs present in 3 sibling matrices), V-58-17/18/19 (Android footer anchor preserved + forward-link present + NEGATIVE deferral wording), V-58-20/21 (Linux hedge removal NEGATIVE + link preserved)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append D-12 5C cross-reference sentence to macOS / iOS / Android matrix intros</name>
  <files>docs/reference/macos-capability-matrix.md, docs/reference/ios-capability-matrix.md, docs/reference/android-capability-matrix.md</files>
  <read_first>
    - .planning/phases/58-defer-08-4-platform-capability-comparison/58-CONTEXT.md (D-12 5C lock; recommended literal sentence; reject 5B blockquote / 5D new H2 rationales)
    - docs/reference/macos-capability-matrix.md (full file — confirm intro paragraph at line 11; ensure ` ## Conditional Access` H2 still present from Plan 58-02; verify `4-Platform Capability Comparison` link not yet present)
    - docs/reference/ios-capability-matrix.md (full file — confirm intro paragraph at line 11)
    - docs/reference/android-capability-matrix.md (full file — confirm intro paragraph at line 12; verify line 132 anchor still present pre-Task 2 edit)
    - docs/reference/4-platform-capability-comparison.md (verify Plan 58-03 file exists; this is the gate for forward-references)
  </read_first>
  <action>
**Pre-flight gate check:** verify `docs/reference/4-platform-capability-comparison.md` exists (Plan 58-03 deliverable). Abort if missing.

**Edit Zone A — `docs/reference/macos-capability-matrix.md`** (intro paragraph at line 11):

Current text (line 11 verbatim):
```
This document compares Intune management capabilities between Windows and macOS across five operational domains. This is a feature parity analysis, not a terminology comparison -- for concept mapping between platforms, see [Windows vs macOS Concept Comparison](../windows-vs-macos.md). For macOS admin setup guides, see [macOS Admin Setup Overview](../admin-setup-macos/00-overview.md).
```

UPDATE to APPEND a single new sentence at the end of this paragraph:
```
This document compares Intune management capabilities between Windows and macOS across five operational domains. This is a feature parity analysis, not a terminology comparison -- for concept mapping between platforms, see [Windows vs macOS Concept Comparison](../windows-vs-macos.md). For macOS admin setup guides, see [macOS Admin Setup Overview](../admin-setup-macos/00-overview.md). For a side-by-side comparison of macOS capabilities against Windows, macOS, iOS, Android, and Linux, see [4-Platform Capability Comparison](4-platform-capability-comparison.md).
```

Note (W-8 domain-count): The macOS intro contains the literal `five operational domains`. Post-CA-retrofit (Plan 58-02), the matrix actually has 6 H2s. The macOS intro phrasing is non-enumerated (says "five operational domains" without listing them by name) — this is a token-level edit, not validator-pinned. Plan-author may either (a) leave "five" unchanged (the appended cross-reference sentence carries the 6-domain scope reference via the linked comparison doc) OR (b) update "five operational domains" → "six operational domains" for accuracy. Executor discretion; both are acceptable since this phrase is not pinned by V-58-NN.

**Edit Zone B — `docs/reference/ios-capability-matrix.md`** (intro paragraph at line 11):

Current text (line 11 verbatim):
```
This matrix compares Intune management capabilities across three platforms. Apple-platform readers (admins coming from macOS ADE) will find the iOS↔macOS comparison surfaces the most meaningful differences — DDM maturity, supervision model scope, and VPP licensing behavior. Windows readers should treat iOS and macOS as structurally distinct from Windows despite being managed via the same Intune tenant. For the Windows↔macOS-only view, see the [macOS Capability Matrix](macos-capability-matrix.md). For concept-level terminology comparison, see [Windows vs macOS Concept Comparison](../windows-vs-macos.md).
```

APPEND a single new sentence at the end:
```
This matrix compares Intune management capabilities across three platforms. Apple-platform readers (admins coming from macOS ADE) will find the iOS↔macOS comparison surfaces the most meaningful differences — DDM maturity, supervision model scope, and VPP licensing behavior. Windows readers should treat iOS and macOS as structurally distinct from Windows despite being managed via the same Intune tenant. For the Windows↔macOS-only view, see the [macOS Capability Matrix](macos-capability-matrix.md). For concept-level terminology comparison, see [Windows vs macOS Concept Comparison](../windows-vs-macos.md). For a side-by-side comparison of iOS capabilities against Windows, macOS, iOS, Android, and Linux, see [4-Platform Capability Comparison](4-platform-capability-comparison.md).
```

**Edit Zone C — `docs/reference/android-capability-matrix.md`** (intro paragraph at line 12 — currently a long single paragraph; this task ONLY appends the D-12 sentence; Task 2 owns the footer F3 edit):

Current text (line 12 verbatim — long paragraph):
```
This matrix compares Intune management capabilities across the five Android Enterprise enrollment modes: COBO (Fully Managed), BYOD (Work Profile), Dedicated (COSU), ZTE (Zero-Touch), and AOSP (stub reference). It is organized mode-first (columns = modes, rows = features) across five locked domains — Enrollment, Configuration, App Deployment, Compliance, and Software Updates. For Apple↔Android capability analogs across these modes, see the [Cross-Platform Equivalences section](#cross-platform-equivalences) below, which lays out three paired rows mapping iOS/Apple enrollment concepts to Android enrollment concepts. For the sibling platform matrices, see [iOS Capability Matrix](ios-capability-matrix.md) and [macOS Capability Matrix](macos-capability-matrix.md). For the enrollment narrative that gave rise to these 5 modes (including the two-axes ownership × management-scope model), see [Android Provisioning Lifecycle](../android-lifecycle/00-enrollment-overview.md).
```

APPEND a single new sentence at the end:
```
This matrix compares Intune management capabilities across the five Android Enterprise enrollment modes: COBO (Fully Managed), BYOD (Work Profile), Dedicated (COSU), ZTE (Zero-Touch), and AOSP (stub reference). It is organized mode-first (columns = modes, rows = features) across five locked domains — Enrollment, Configuration, App Deployment, Compliance, and Software Updates. For Apple↔Android capability analogs across these modes, see the [Cross-Platform Equivalences section](#cross-platform-equivalences) below, which lays out three paired rows mapping iOS/Apple enrollment concepts to Android enrollment concepts. For the sibling platform matrices, see [iOS Capability Matrix](ios-capability-matrix.md) and [macOS Capability Matrix](macos-capability-matrix.md). For the enrollment narrative that gave rise to these 5 modes (including the two-axes ownership × management-scope model), see [Android Provisioning Lifecycle](../android-lifecycle/00-enrollment-overview.md). For a side-by-side comparison of Android capabilities against Windows, macOS, iOS, Android, and Linux, see [4-Platform Capability Comparison](4-platform-capability-comparison.md).
```

**MANDATORY domain-count edit (W-8 fix):** The Android intro at line ~12 contains the literal enumerated list `five locked domains — Enrollment, Configuration, App Deployment, Compliance, and Software Updates`. Post-CA-retrofit (Plan 58-02), the matrix has 6 H2s. Because this phrasing is ENUMERATED (lists all domains by name), it MUST be rewritten — leaving "five" while the named list expands would be self-contradictory. REQUIRED edit: replace `five locked domains — Enrollment, Configuration, App Deployment, Compliance, and Software Updates` with `six locked domains — Enrollment, Configuration, App Deployment, Compliance, Software Updates, and Conditional Access`. This update is in addition to the D-12 cross-reference sentence appended at the end of the paragraph; both edits land in this same task. (iOS matrix intro count phrasing is non-enumerated — executor discretion per the macOS note above.)

**Append-only contract — DO NOT modify:**
- Frontmatter
- Existing intro sentences before the new appended one
- Any H2 (including the `## Conditional Access` H2 added by Plan 58-02)
- Any table content
- `## See Also`, `## Version History`
- (Android-specific) Phase 45 anchor at line 124 OR D-14 anchor at line 132 — Task 2 owns the F3 retrofit at line 132
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const macos=fs.readFileSync('docs/reference/macos-capability-matrix.md','utf8');const ios=fs.readFileSync('docs/reference/ios-capability-matrix.md','utf8');const android=fs.readFileSync('docs/reference/android-capability-matrix.md','utf8');const tests=[['macOS intro cross-ref',macos.includes('4-Platform Capability Comparison')&&macos.includes('4-platform-capability-comparison.md')&&macos.includes('side-by-side comparison of macOS capabilities')],['iOS intro cross-ref',ios.includes('4-Platform Capability Comparison')&&ios.includes('4-platform-capability-comparison.md')&&ios.includes('side-by-side comparison of iOS capabilities')],['Android intro cross-ref',android.includes('4-Platform Capability Comparison')&&android.includes('4-platform-capability-comparison.md')&&android.includes('side-by-side comparison of Android capabilities')],['macOS CA H2 still present (Plan 58-02 work intact)',/^## Conditional Access\\s*$/m.test(macos)],['iOS CA H2 still present',/^## Conditional Access\\s*$/m.test(ios)],['Android CA H2 still present',/^## Conditional Access\\s*$/m.test(android)],['macOS first 30 lines contain new sentence',macos.split('\\n').slice(0,30).join('\\n').includes('4-platform-capability-comparison.md')],['iOS first 30 lines contain new sentence',ios.split('\\n').slice(0,30).join('\\n').includes('4-platform-capability-comparison.md')],['Android first 30 lines contain new sentence',android.split('\\n').slice(0,30).join('\\n').includes('4-platform-capability-comparison.md')],['Android Phase 45 anchor still preserved',android.includes('<a id=\"deferred-full-aosp-capability-mapping\"></a>')],['Android D-14 anchor still preserved (Task 2 owns retrofit)',android.includes('<a id=\"deferred-4-platform-unified-capability-comparison\"></a>')]];const fails=tests.filter(([n,v])=>!v).map(([n])=>n);if(fails.length){console.error('Failures:',fails);process.exit(1)}console.log(tests.length+' checks pass');process.exit(0);"</automated>
  </verify>
  <done>All 3 sibling matrix intros (macOS / iOS / Android) contain the D-12 5C cross-reference sentence within the first 30 lines; existing intro prose preserved; CA H2s from Plan 58-02 still present; Android `<a id>` anchors preserved; node check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: D-13 Linux matrix hedge removal (lines 70 + 112) AND D-14 Android footer F3 retrofit (lines 132-139)</name>
  <files>docs/reference/linux-capability-matrix.md, docs/reference/android-capability-matrix.md</files>
  <read_first>
    - .planning/phases/58-defer-08-4-platform-capability-comparison/58-CONTEXT.md (D-13 + D-14)
    - .planning/phases/58-defer-08-4-platform-capability-comparison/58-RESEARCH.md (§Android Footer F3 Mechanics — Phase 45 AEAOSPFULL-09 verbatim model + recommended F3-replacement text)
    - docs/reference/linux-capability-matrix.md (full file — confirm line 70 has `(when shipped)` literal in the Cross-Platform Equivalences intro paragraph; confirm line 112 has `— when Phase 58 ships` em-dash-suffix in the See Also list)
    - docs/reference/android-capability-matrix.md (full file — confirm lines 124-130 = Phase 45 precedent block UNCHANGED; lines 132-139 = D-14 F3 target block)
  </read_first>
  <action>
**Edit Zone D — `docs/reference/linux-capability-matrix.md` line 70 hedge removal (D-13):**

Current line 70 verbatim:
```
This section maps three Linux↔Apple capability pairs called out in ROADMAP SC#4. It is NOT a 4-platform comparison — see Phase 58's [4-Platform Capability Comparison](4-platform-capability-comparison.md) (when shipped) for that. Each paired row attributes the platform explicitly on both sides; mappings are STRUCTURAL, not behavioral (per Phase 49 PITFALL-1 partial-mapping discipline at `_glossary-linux.md` line 84 and 112).
```

REWRITE to drop `Phase 58's` ownership reference and `(when shipped)` parenthetical:
```
This section maps three Linux↔Apple capability pairs called out in ROADMAP SC#4. It is NOT a 4-platform comparison — see the [4-Platform Capability Comparison](4-platform-capability-comparison.md) for that. Each paired row attributes the platform explicitly on both sides; mappings are STRUCTURAL, not behavioral (per Phase 49 PITFALL-1 partial-mapping discipline at `_glossary-linux.md` line 84 and 112).
```

**Edit Zone E — `docs/reference/linux-capability-matrix.md` line 112 hedge removal (D-13):**

Current line 112 verbatim:
```
- [4-Platform Capability Comparison](4-platform-capability-comparison.md) — when Phase 58 ships
```

REWRITE to drop `— when Phase 58 ships` em-dash trailing prose (link literal preserved):
```
- [4-Platform Capability Comparison](4-platform-capability-comparison.md)
```

**Append-only contract for linux-capability-matrix.md:**
- DO NOT modify any of the 6 H2 literals (`## Enrollment` line 15, `## Configuration` 24, `## App Deployment` 33, `## Compliance` 42, `## Software Updates` 51, `## Conditional Access` 59)
- DO NOT modify `## Cross-Platform Equivalences` (line 68 / unchanged structure even if line numbers shift slightly)
- DO NOT modify any table content
- DO NOT modify Version History row
- Only the prose at line 70 + line 112 changes

**Edit Zone F — `docs/reference/android-capability-matrix.md` lines 132-139 footer F3 retrofit (D-14 + Phase 45 AEAOSPFULL-09 precedent):**

Current text (lines 132-139 verbatim):
```
<a id="deferred-4-platform-unified-capability-comparison"></a>
### Deferred: 4-platform unified capability comparison

This matrix is Android-centric with a bounded 3-row Cross-Platform Equivalences
section. A unified Windows|macOS|iOS|Android 4-platform feature comparison doc
is deferred to v1.5 (AECOMPARE-01). The paired rows in this matrix are NOT a
4-platform comparison — they are mode-level feature parity assertions between
Apple and Android, constrained to the 3 SC#1-named pairs.
```

REPLACE with the D-14 F3 retrofit (anchor PRESERVED verbatim at line 132; `Deferred:` prefix dropped from H3 per Phase 45 model; 7-line body block REMOVED and REPLACED with single forward-link sentence):

```markdown
<a id="deferred-4-platform-unified-capability-comparison"></a>
### 4-platform unified capability comparison

For a side-by-side comparison of Android Enterprise capabilities against Windows, macOS, iOS, and Linux across all 6 capability domains (Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access), see [4-Platform Capability Comparison](4-platform-capability-comparison.md).
```

**Critical F3 disciplines (D-14):**
1. **Anchor `<a id="deferred-4-platform-unified-capability-comparison"></a>` MUST appear UNCHANGED on its own line** (validator V-58-17 pin) — DO NOT remove, DO NOT alter the literal string, DO NOT collapse onto another line
2. **`Deferred:` prefix DROPPED from H3 heading** per Phase 45 AEAOSPFULL-09 verbatim model (lines 124-125 — `### AOSP per-OEM capability mapping` not `### Deferred: AOSP per-OEM capability mapping`)
3. **Body REPLACED with single forward-link sentence** containing literal `4-platform-capability-comparison.md` link target (validator V-58-18 pin within 800 chars after the anchor)
4. **Pre-Phase-58 deferral wording REMOVED:** the literals `deferred to v1.5` and `AECOMPARE-01` MUST NOT appear within the H3 block post-edit (validator V-58-19 NEGATIVE assertion)
5. **Phase 45 anchor at line 124 + surrounding heading + body (lines 124-130) UNCHANGED** byte-identical (V-58-22 NEGATIVE regression-guard)

After F3 retrofit, the Android matrix footer block (line 132 anchor + H3 + new single-sentence forward-link prose + closing blank line + `---` separator if present) collapses from 8 lines (132-139) to ~4 lines. This is consistent with the Phase 45 AEAOSPFULL-09 line-count change.

**Append-only contract for android-capability-matrix.md (Task 2 scope):**
- DO NOT modify lines 1-130 (all H2s + tables + Cross-Platform Equivalences + Knox row + Phase 45 AEAOSPFULL-09 block at 124-130)
- DO NOT modify the existing Version History H2 (line 143 currently — Task 2 may APPEND a Phase 58 row to the Version History table; see below)
- ONLY the lines 132-139 block changes per F3 retrofit + optional Version History row append

**Optional Version History row append (consistent with Phase 45 + Phase 57 precedent):**

In the Android matrix Version History table (after the existing 2 rows at lines 147-148), APPEND:
```
| 2026-04-30 | Phase 58 D-14: `#deferred-4-platform-unified-capability-comparison` anchor preserved; H3 `Deferred:` prefix dropped; 7-line body retargeted to single-sentence forward-link prose to `4-platform-capability-comparison.md`. Phase 45 AEAOSPFULL-09 anchor-preservation precedent. | -- |
```

(Substitute commit-day date if the executor commits later than 2026-04-30.)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const linux=fs.readFileSync('docs/reference/linux-capability-matrix.md','utf8');const android=fs.readFileSync('docs/reference/android-capability-matrix.md','utf8');const tests=[['Linux NO `(when shipped)` literal anywhere',!linux.includes('(when shipped)')],['Linux NO `when Phase 58 ships` literal',!linux.includes('when Phase 58 ships')],['Linux still has 4-platform link',linux.includes('[4-Platform Capability Comparison](4-platform-capability-comparison.md)')],['Linux all 6 H2s preserved',['## Enrollment','## Configuration','## App Deployment','## Compliance','## Software Updates','## Conditional Access','## Cross-Platform Equivalences'].every(h=>linux.includes(h))],['Android D-14 anchor preserved verbatim',android.includes('<a id=\"deferred-4-platform-unified-capability-comparison\"></a>')],['Android Phase 45 anchor preserved (V-58-22)',android.includes('<a id=\"deferred-full-aosp-capability-mapping\"></a>')],['Android H3 Deferred prefix dropped',!android.includes('### Deferred: 4-platform unified capability comparison')],['Android H3 has new heading',android.includes('### 4-platform unified capability comparison')],['Android footer body has forward-link',(()=>{const idx=android.indexOf('<a id=\"deferred-4-platform-unified-capability-comparison\"></a>');if(idx<0)return false;const block=android.slice(idx,idx+800);return block.includes('4-platform-capability-comparison.md');})()],['Android footer NEGATIVE: deferred-to-v1.5 wording absent',(()=>{const idx=android.indexOf('<a id=\"deferred-4-platform-unified-capability-comparison\"></a>');if(idx<0)return false;const block=android.slice(idx,idx+800);return!/deferred to v1\\.5|AECOMPARE-01/.test(block);})()],['Android Phase 45 block (lines 124-130 region) unchanged literals',android.includes('<a id=\"deferred-full-aosp-capability-mapping\"></a>')&&android.includes('### AOSP per-OEM capability mapping')&&android.includes('aosp-oem-matrix.md')]];const fails=tests.filter(([n,v])=>!v).map(([n])=>n);if(fails.length){console.error('Failures:',fails);process.exit(1)}console.log(tests.length+' checks pass');process.exit(0);"</automated>
  </verify>
  <done>Linux matrix has zero `(when shipped)` literals AND zero `when Phase 58 ships` literals; the comparison-doc link literal preserved at both line 70 + line 112 sites. Android matrix footer F3 retrofit complete: anchor preserved verbatim, `Deferred:` H3 prefix dropped per Phase 45 model, body retargeted to single forward-link sentence, no pre-Phase-58 deferral wording remains, Phase 45 block at lines 124-130 untouched. Node check exits 0.</done>
</task>

</tasks>

<verification>
- All 3 sibling matrices (macOS / iOS / Android) intros contain the D-12 5C cross-reference sentence with link target `4-platform-capability-comparison.md` and link text `4-Platform Capability Comparison`
- macOS / iOS / Android `## Conditional Access` H2s (added by Plan 58-02) still present and unchanged
- Linux matrix line 70 + line 112 hedge removal: `(when shipped)` and `when Phase 58 ships` literals do NOT appear anywhere in the file
- Linux matrix link to `4-platform-capability-comparison.md` preserved at both former hedge sites
- Linux matrix all 7 H2 literals (Enrollment / Configuration / App Deployment / Compliance / Software Updates / Conditional Access / Cross-Platform Equivalences) UNCHANGED
- Android matrix footer F3 retrofit: anchor `<a id="deferred-4-platform-unified-capability-comparison"></a>` preserved byte-identical; `Deferred:` H3 prefix dropped (Phase 45 AEAOSPFULL-09 precedent); body replaced with single forward-link sentence; pre-Phase-58 deferral wording (`deferred to v1.5` / `AECOMPARE-01`) absent
- Android matrix Phase 45 anchor + heading + body (lines 124-130 region) UNCHANGED byte-identical (V-58-22 regression-guard)
</verification>

<success_criteria>
- D-12 satisfied: 3 sibling matrices cross-reference the new comparison doc via 5C single-sentence-paragraph-edit pattern
- D-13 satisfied: Linux matrix `(when shipped)` hedge removed atomically with comparison-doc landing
- D-14 satisfied: Android footer F3 retrofit — anchor preserved, body retargeted, `Deferred:` prefix dropped per Phase 45 model
- ROADMAP SC#3 satisfied: macOS / iOS / Android intros cross-reference; Android footer body removed and replaced with forward-link
- ROADMAP SC#4 satisfied: Linux matrix cross-references the comparison doc (lines 70 + 112 — link literals preserved post-hedge-removal)
- Phase 45 AEAOSPFULL-09 anchor stability preserved (V-58-22 regression-guard target)
- Plan 58-05 unblocked (validator can now pin V-58-14/15/16 intro cross-refs, V-58-17/18/19 Android footer, V-58-20/21 Linux hedge removal)
</success_criteria>

<output>
After completion, create `.planning/phases/58-defer-08-4-platform-capability-comparison/58-04-SUMMARY.md` documenting:
1. The 3 D-12 5C cross-reference sentences inserted (one per sibling matrix; quote the actual sentences as committed)
2. The 2 Linux matrix hedge sites closed (line 70 prose change + line 112 list-item change)
3. The Android footer F3 retrofit before/after (8-line block → 4-line block; anchor preserved; H3 heading text changed; body content)
4. Confirmation Phase 45 anchor at line 124 region UNCHANGED byte-identical
5. Confirmation `## Conditional Access` H2s from Plan 58-02 still present and unchanged
6. Note for Plan 58-05: "All 3 sibling matrix intro cross-refs present; Android footer F3 retrofit complete with anchor preservation; Linux hedge removed; validator V-58-14..21 assertions are now testable."
</output>
