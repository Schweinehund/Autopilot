---
phase: 58
plan: 58-02
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - docs/reference/macos-capability-matrix.md
  - docs/reference/ios-capability-matrix.md
  - docs/reference/android-capability-matrix.md
requirements: [CLEAN-05]
autonomous: true

must_haves:
  truths:
    - "`docs/reference/macos-capability-matrix.md` contains a new `## Conditional Access` H2 inserted between existing `## Software Updates` body (line 68 H2) and existing `## Key Gaps Summary` H2 (line 78) — D-04 mandatory tier-2 retrofit"
    - "`docs/reference/ios-capability-matrix.md` contains a new `## Conditional Access` H2 inserted between existing `## Software Updates` body (line 70 H2) and existing `## Key Gaps Summary` H2 (line 80) — D-04 mandatory tier-2 retrofit"
    - "`docs/reference/android-capability-matrix.md` contains a new `## Conditional Access` H2 inserted between existing `## Software Updates` body (line 67 H2) and existing `## Cross-Platform Equivalences` H2 (line 76) — D-04 mandatory tier-2 retrofit"
    - "Each new `## Conditional Access` H2 contains a GFM pipe-table whose column count matches its host matrix: macOS = 3 columns (Feature / Windows / macOS); iOS = 4 columns (Feature / Windows / macOS / iOS); Android = 7 columns (Feature / COBO / COPE / BYOD / Dedicated / ZTE / AOSP) per RESEARCH §CA H2 retrofit content scope"
    - "Each new `## Conditional Access` H2 covers at minimum these 5 CA features (mirroring Linux matrix lines 59-66 template): Device-based CA, Web-app CA via Edge/Safari/Chrome, Per-app CA (MAM / MAM-WE / managed app), App-based CA / Approved Client App, Risk-based CA"
    - "Append-only contract honored: 5 existing H2 literals in each of macOS / iOS / Android matrices remain BYTE-IDENTICAL pre/post Plan 58-02 (`## Enrollment`, `## Configuration`, `## App Deployment`, `## Compliance`, `## Software Updates`); Phase 45 anchor `<a id=\"deferred-full-aosp-capability-mapping\"></a>` and D-14 anchor `<a id=\"deferred-4-platform-unified-capability-comparison\"></a>` remain UNCHANGED in Android matrix"
    - "Pre-edit anchor inventory artifact `.planning/phases/58-defer-08-4-platform-capability-comparison/58-ANCHOR-INVENTORY.md` MUST exist (Plan 58-01 gate) before Task execution begins — verifiable via `existsSync` check"
    - "GFM anchor for new H2 is the deterministic slug `#conditional-access` (PITFALL-15-safe; lowercase ASCII; verified in RESEARCH §GFM H2 anchor slug audit) — Plan 58-03 will link comparison doc cells to this anchor in macOS / iOS / Android matrices"

  artifacts:
    - path: "docs/reference/macos-capability-matrix.md"
      provides: "macOS `## Conditional Access` H2 retrofit (D-04 mandatory tier-2); 3-column table; 5 CA features mirroring Linux template"
      contains:
        - "## Conditional Access"
        - "Device-based CA"
        - "Web-app CA"
        - "Risk-based CA"
        - "## Enrollment"
        - "## Configuration"
        - "## App Deployment"
        - "## Compliance"
        - "## Software Updates"
        - "## Key Gaps Summary"
    - path: "docs/reference/ios-capability-matrix.md"
      provides: "iOS `## Conditional Access` H2 retrofit (D-04); 4-column table (Feature / Windows / macOS / iOS); 5 CA features including MAM-WE iOS-primary attribution"
      contains:
        - "## Conditional Access"
        - "Device-based CA"
        - "MAM-WE"
        - "Risk-based CA"
        - "## Enrollment"
        - "## Configuration"
        - "## App Deployment"
        - "## Compliance"
        - "## Software Updates"
        - "## Key Gaps Summary"
    - path: "docs/reference/android-capability-matrix.md"
      provides: "Android `## Conditional Access` H2 retrofit (D-04); 7-column table (Feature / COBO / COPE / BYOD / Dedicated / ZTE / AOSP); 5 CA features mirroring multi-mode existing matrix style"
      contains:
        - "## Conditional Access"
        - "Device-based CA"
        - "Play Integrity"
        - "Risk-based CA"
        - "## Enrollment"
        - "## Configuration"
        - "## App Deployment"
        - "## Compliance"
        - "## Software Updates"
        - "## Cross-Platform Equivalences"

  key_links:
    - from: "docs/reference/macos-capability-matrix.md (new `## Conditional Access` H2 anchor)"
      to: "docs/reference/4-platform-capability-comparison.md (will be created by Plan 58-03)"
      via: "GFM-derived `#conditional-access` slug; comparison doc CA H2 row macOS column will link `[matrix](macos-capability-matrix.md#conditional-access)`"
      pattern: "^## Conditional Access\\s*$"
    - from: "docs/reference/ios-capability-matrix.md (new `## Conditional Access` H2 anchor)"
      to: "docs/reference/4-platform-capability-comparison.md (will be created by Plan 58-03)"
      via: "GFM-derived `#conditional-access` slug; comparison doc CA H2 row iOS column will link `[matrix](ios-capability-matrix.md#conditional-access)`"
      pattern: "^## Conditional Access\\s*$"
    - from: "docs/reference/android-capability-matrix.md (new `## Conditional Access` H2 anchor)"
      to: "docs/reference/4-platform-capability-comparison.md (will be created by Plan 58-03)"
      via: "GFM-derived `#conditional-access` slug; comparison doc CA H2 row Android column will link `[matrix](android-capability-matrix.md#conditional-access)` (Mode-dependent verdict where modes diverge per D-05)"
      pattern: "^## Conditional Access\\s*$"
---

<objective>
Retrofit a `## Conditional Access` H2 into the 3 sibling capability matrices (macOS, iOS, Android) — the D-04 MANDATORY tier-2 deliverable. Each new H2 mirrors the Linux matrix `## Conditional Access` H2 (lines 59-66) structure: a GFM pipe table with the host matrix's existing column count + 5 CA-feature rows (Device-based CA, Web-app CA via Edge/Safari/Chrome, Per-app CA via MAM/MAM-WE, App-based CA / Approved Client App, Risk-based CA).

This plan is the architectural prerequisite for Plan 58-03 (comparison doc authoring): the comparison doc's CA cells targeting `macos-capability-matrix.md#conditional-access` / `ios-capability-matrix.md#conditional-access` / `android-capability-matrix.md#conditional-access` will broken-link until these anchors exist. Plan 58-02 ships those anchors first.

Wave 2 — depends on 58-01 (pre-edit anchor inventory baseline). Disjoint file set within the 3 sibling matrices means tasks within this plan are file-disjoint and could be executed in any internal order. Append-only H2 contract: 5 existing H2s in each sibling matrix are byte-identical pre/post.

Purpose: Implements CONTEXT D-04 (mandatory tier-2 CA H2 retrofit; tier-1 link-CA-cells-to-#compliance was rejected as category-mismatch / misleading-by-construction). Each new H2 satisfies SC#1 ("no column empty or unacknowledged") with real CA-anchor targets for the comparison doc to link to.

Output: 3 modified files (macos / ios / android capability matrices).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-defer-08-4-platform-capability-comparison/58-CONTEXT.md
@.planning/phases/58-defer-08-4-platform-capability-comparison/58-RESEARCH.md
@.planning/phases/58-defer-08-4-platform-capability-comparison/58-ANCHOR-INVENTORY.md
@.planning/ROADMAP.md
@CLAUDE.md

<interfaces>
**Files this plan modifies:**
- MODIFY: `docs/reference/macos-capability-matrix.md` (insert new `## Conditional Access` H2 between line 77 end-of-Software-Updates and line 78 `## Key Gaps Summary`)
- MODIFY: `docs/reference/ios-capability-matrix.md` (insert new `## Conditional Access` H2 between line 79 end-of-Software-Updates and line 80 `## Key Gaps Summary`)
- MODIFY: `docs/reference/android-capability-matrix.md` (insert new `## Conditional Access` H2 between line 75 end-of-Software-Updates and line 76 `## Cross-Platform Equivalences`)

**Linux matrix CA template source (read for content-template mirror; DO NOT modify in this plan — Plan 58-04 owns Linux line 70 + 112 hedge removal):**
- `docs/reference/linux-capability-matrix.md:59-66` — `## Conditional Access` H2 with 4-row table; verbatim template for 5-row CA structure that macOS/iOS/Android retrofits adapt with platform-specific verdicts

**Sibling Phase 58 plans (do NOT touch their files; they depend on this plan):**
- 58-03 will create `docs/reference/4-platform-capability-comparison.md` and link CA cells to the 3 new `#conditional-access` anchors created here
- 58-04 will edit 3 sibling matrix INTROS (different edit zone — line 11/12 area) + Android footer F3
- 58-05 (validator) will pin `## Conditional Access` H2 literal in macOS/iOS/Android matrices via V-58-11/12/13 assertions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert `## Conditional Access` H2 into macOS + iOS matrices (D-04 retrofit, Apple platforms)</name>
  <files>docs/reference/macos-capability-matrix.md, docs/reference/ios-capability-matrix.md</files>
  <read_first>
    - .planning/phases/58-defer-08-4-platform-capability-comparison/58-ANCHOR-INVENTORY.md (verify pre-edit baseline captured; gate enforcement per D-15)
    - .planning/phases/58-defer-08-4-platform-capability-comparison/58-CONTEXT.md (D-04 mandate — tier-2 retrofit; tier-1 rejected as category-mismatch)
    - .planning/phases/58-defer-08-4-platform-capability-comparison/58-RESEARCH.md (§Conditional Access H2 Retrofit Content Scope — macOS section + iOS section; §GFM H2 anchor slug audit confirming `#conditional-access` deterministic slug)
    - docs/reference/linux-capability-matrix.md (read lines 59-66 verbatim — Linux CA template that macOS/iOS retrofits parallel)
    - docs/reference/macos-capability-matrix.md (full file — confirm line 77 = end of Software Updates body; line 78 = `## Key Gaps Summary`; insert between)
    - docs/reference/ios-capability-matrix.md (full file — confirm line 79 = end of Software Updates body; line 80 = `## Key Gaps Summary`; insert between)
  </read_first>
  <action>
**Pre-flight gate check:** verify `.planning/phases/58-defer-08-4-platform-capability-comparison/58-ANCHOR-INVENTORY.md` exists. If missing, abort and instruct executor to run Plan 58-01 first.

**Edit Zone A — `docs/reference/macos-capability-matrix.md`**: insert new `## Conditional Access` H2 block after the existing `## Software Updates` body (last line of that section currently at line 77 `| "Update Only" app deployment | ...`) and BEFORE the existing `## Key Gaps Summary` H2 at line 78. Insert exactly the following content (verbatim — no embellishment, no emoji, no v1/v2 phrasing):

```markdown
## Conditional Access

| Feature | Windows | macOS |
|---------|---------|-------|
| Device-based CA (`Require device to be marked as compliant`) | Supported | Supported (compliance evaluation requires user affinity — see [Compliance](#compliance)) |
| Web-app CA via Edge | Supported | Supported (Microsoft Edge / Safari with Single Sign-On Extension) |
| Per-app CA (MAM) | n/a (Windows uses Intune client) | Limited (MAM-WE is iOS-primary; macOS uses configuration profiles + DeviceID surface) |
| App-based CA / Approved Client App | Supported | Supported (via configuration profile + native MDM enrollment) |
| Risk-based CA | Supported (Entra ID Protection signals) | Supported (Entra ID Protection signals; macOS device state factored when user-affinity is present) |

```

Ensure the H2 line is preceded by exactly one blank line and followed by exactly one blank line before the next `## Key Gaps Summary` H2 (matches existing inter-H2 spacing convention in the file).

**Edit Zone B — `docs/reference/ios-capability-matrix.md`**: insert new `## Conditional Access` H2 block after the existing `## Software Updates` body (last line of that section currently at line 78 `| Update status reporting | ...`) and BEFORE the existing `## Key Gaps Summary` H2 at line 80. Insert exactly the following content (verbatim):

```markdown
## Conditional Access

| Feature | Windows | macOS | iOS |
|---------|---------|-------|-----|
| Device-based CA (`Require device to be marked as compliant`) | Supported | Supported | Supported (supervised attestation strengthens device-state signal; unsupervised devices use jailbreak detection — see [Compliance](#compliance)) |
| Web-app CA via Edge / Safari | Supported | Supported | Supported (Microsoft Edge / Safari with Apple Single Sign-On extension) |
| Per-app CA (MAM-WE) | n/a (Windows uses Intune client) | Limited | Supported (MAM-WE — Managed App Without Enrollment; selective-wipe + per-app PIN; works on devices NOT enrolled in Intune MDM) |
| App-based CA / Approved Client App | Supported | Supported | Supported (Microsoft Authenticator / Outlook / Edge family; per-app token broker) |
| Risk-based CA | Supported (Entra ID Protection signals) | Supported | Supported (Entra ID Protection user-risk signals; iOS device-risk signals via Microsoft Defender for Endpoint — see [Compliance](#compliance)) |

```

Ensure the H2 line is preceded by exactly one blank line and followed by exactly one blank line before `## Key Gaps Summary`.

**Append-only contract — DO NOT modify:**
- Frontmatter (lines 1-7 in both files) — Plan 58-04 owns frontmatter `last_verified` updates if/when needed; this plan is content-only
- 5 existing H2 literals: `## Enrollment`, `## Configuration`, `## App Deployment`, `## Compliance`, `## Software Updates`
- Tables under those 5 H2s
- `## Key Gaps Summary`, `## See Also`, `## Version History` sections
- Intro paragraph (line 11 macOS, line 11 iOS) — Plan 58-04 owns intro 5C cross-reference edits

**Verdict vocabulary note:** the per-platform matrices use richer prose (e.g., `Supported (compliance evaluation requires user affinity)`); the 5-state vocabulary lock (D-02 `Supported | Partial | Not supported | Mode-dependent | n/a`) applies to the comparison doc cells, NOT to source-matrix prose. Use the verbatim content from RESEARCH.md §CA H2 retrofit content scope (macOS / iOS sections) — DO NOT abbreviate to single-word verdicts here.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const macosCheck=()=>{const c=fs.readFileSync('docs/reference/macos-capability-matrix.md','utf8');const tests=[/^## Conditional Access\s*$/m.test(c),/^## Enrollment\s*$/m.test(c),/^## Configuration\s*$/m.test(c),/^## App Deployment\s*$/m.test(c),/^## Compliance\s*$/m.test(c),/^## Software Updates\s*$/m.test(c),/^## Key Gaps Summary\s*$/m.test(c),c.includes('Device-based CA'),c.includes('Web-app CA via Edge'),c.includes('Per-app CA (MAM)'),c.includes('App-based CA / Approved Client App'),c.includes('Risk-based CA'),c.indexOf('## Conditional Access')<c.indexOf('## Key Gaps Summary'),c.indexOf('## Software Updates')<c.indexOf('## Conditional Access')];return tests;};const iosCheck=()=>{const c=fs.readFileSync('docs/reference/ios-capability-matrix.md','utf8');const tests=[/^## Conditional Access\s*$/m.test(c),/^## Enrollment\s*$/m.test(c),/^## Configuration\s*$/m.test(c),/^## App Deployment\s*$/m.test(c),/^## Compliance\s*$/m.test(c),/^## Software Updates\s*$/m.test(c),/^## Key Gaps Summary\s*$/m.test(c),c.includes('Device-based CA'),c.includes('MAM-WE'),c.includes('Risk-based CA'),c.includes('App-based CA / Approved Client App'),c.indexOf('## Conditional Access')<c.indexOf('## Key Gaps Summary'),c.indexOf('## Software Updates')<c.indexOf('## Conditional Access')];return tests;};const m=macosCheck();const i=iosCheck();const allPassM=m.every(Boolean);const allPassI=i.every(Boolean);if(!allPassM){console.error('macOS failures at indices:',m.map((v,idx)=>v?null:idx).filter(x=>x!==null));process.exit(1)}if(!allPassI){console.error('iOS failures at indices:',i.map((v,idx)=>v?null:idx).filter(x=>x!==null));process.exit(1)}console.log('macOS OK ('+m.length+' checks); iOS OK ('+i.length+' checks)');process.exit(0);"</automated>
  </verify>
  <done>macOS and iOS matrices each contain a new `## Conditional Access` H2 between `## Software Updates` body and `## Key Gaps Summary`; tables have correct column counts (3 / 4); 5 existing H2 literals UNCHANGED; node check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Insert `## Conditional Access` H2 into Android matrix (D-04 retrofit, multi-mode)</name>
  <files>docs/reference/android-capability-matrix.md</files>
  <read_first>
    - .planning/phases/58-defer-08-4-platform-capability-comparison/58-CONTEXT.md (D-04 + D-05 Mode-dependent handling for Android multi-mode platform)
    - .planning/phases/58-defer-08-4-platform-capability-comparison/58-RESEARCH.md (§Conditional Access H2 Retrofit Content Scope — Android section; 7-column multi-mode table)
    - docs/reference/android-capability-matrix.md (full file — confirm line 75 = end of Software Updates body; line 76 = `## Cross-Platform Equivalences` H2; line 124 = Phase 45 anchor; line 132 = D-14 anchor — insert new CA H2 between line 75 and line 76)
    - docs/reference/linux-capability-matrix.md (lines 59-66 — Linux CA template as architectural reference; Android adapts to multi-mode)
  </read_first>
  <action>
**Edit Zone — `docs/reference/android-capability-matrix.md`**: insert new `## Conditional Access` H2 block AFTER the existing `## Software Updates` body (last line of that section currently at line 74 `| OEM update enforcement | ...`) and BEFORE the existing `## Cross-Platform Equivalences` H2 at line 76. Insert exactly the following content (verbatim):

```markdown
## Conditional Access

| Feature | COBO (Fully Managed) | COPE (WPCO) | BYOD (Work Profile) | Dedicated (COSU) | ZTE (Zero-Touch) | AOSP |
|---------|----------------------|-------------|---------------------|------------------|------------------|------|
| Device-based CA (`Require device to be marked as compliant`) | Supported (Play Integrity attestation; full device-wide compliance signal) | Supported (Play Integrity on work-profile + device-owner side) | Supported (Play Integrity on work-profile only) | Supported (typically not user-CA-bearing — kiosk fleets) | Supported (post-handoff; matches COBO) | AOSP stub — see [06-aosp-stub.md](../admin-setup-android/06-aosp-stub.md) (Play Integrity not available on non-GMS hardware) |
| Web-app CA via Edge / Chrome | Supported | Supported | Supported | Supported | Supported (post-handoff) | AOSP stub — see [06-aosp-stub.md](../admin-setup-android/06-aosp-stub.md) |
| Per-app CA (MAM) | Supported (Microsoft Intune app DPC + per-app policies) | Supported (work-profile-scoped) | Supported (work-profile-scoped; MAM-WE pattern available for non-enrolled BYOD) | Limited (kiosk fleets typically not MAM-targeted) | Supported (post-handoff) | AOSP stub — see [06-aosp-stub.md](../admin-setup-android/06-aosp-stub.md) |
| App-based CA / Approved Client App | Supported | Supported | Supported | Supported | Supported (post-handoff) | AOSP stub — see [06-aosp-stub.md](../admin-setup-android/06-aosp-stub.md) |
| Risk-based CA | Supported (Entra ID Protection + Microsoft Defender for Endpoint signals) | Supported | Supported | Supported | Supported (post-handoff) | AOSP stub — see [06-aosp-stub.md](../admin-setup-android/06-aosp-stub.md) |

```

Ensure the H2 line is preceded by exactly one blank line and followed by exactly one blank line before `## Cross-Platform Equivalences`.

**Append-only contract — DO NOT modify:**
- Frontmatter (lines 1-8) — Plan 58-04 owns intro/frontmatter edits if needed
- 5 existing H2 literals: `## Enrollment`, `## Configuration`, `## App Deployment`, `## Compliance`, `## Software Updates`
- `## Cross-Platform Equivalences` H2 (line 76) — UNCHANGED literal text and position-relative-to-other-H2s; only the line number shifts because this plan inserts content above it
- `## Key Gaps Summary`, `## See Also`, `## Version History` sections
- Phase 45 anchor `<a id="deferred-full-aosp-capability-mapping"></a>` and surrounding heading + body (lines 124-130) — DO NOT TOUCH (V-58-22 NEGATIVE regression-guard target)
- D-14 anchor `<a id="deferred-4-platform-unified-capability-comparison"></a>` and surrounding body (lines 132-139) — DO NOT TOUCH (Plan 58-04 owns this F3 retrofit)
- Intro paragraph (line 12) — Plan 58-04 owns intro 5C cross-reference edit

**Verdict prose note:** Use the per-mode prose verbatim from RESEARCH §CA H2 retrofit content scope (Android section). DO NOT collapse to single-word verdicts. The 5-state vocabulary lock (D-02) applies to the comparison doc, NOT to per-platform matrix prose. The phrase "AOSP stub — see [06-aosp-stub.md](../admin-setup-android/06-aosp-stub.md)" mirrors the Android matrix's existing AOSP-column convention (e.g., line 18 `Tri-portal admin surface required` row AOSP cell). The "Play Integrity not available on non-GMS hardware" qualifier appears only in the Device-based CA row to call out the architectural constraint; subsequent AOSP cells use the bare stub link.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/reference/android-capability-matrix.md','utf8');const tests=[['CA H2 present',/^## Conditional Access\s*$/m.test(c)],['Existing Enrollment H2',/^## Enrollment\s*$/m.test(c)],['Existing Configuration H2',/^## Configuration\s*$/m.test(c)],['Existing App Deployment H2',/^## App Deployment\s*$/m.test(c)],['Existing Compliance H2',/^## Compliance\s*$/m.test(c)],['Existing Software Updates H2',/^## Software Updates\s*$/m.test(c)],['Existing Cross-Platform Equivalences H2',/^## Cross-Platform Equivalences\s*$/m.test(c)],['Existing Key Gaps Summary H2',/^## Key Gaps Summary\s*$/m.test(c)],['CA between Software Updates and Cross-Platform Equivalences',c.indexOf('## Software Updates')<c.indexOf('## Conditional Access')&&c.indexOf('## Conditional Access')<c.indexOf('## Cross-Platform Equivalences')],['Phase 45 anchor preserved',c.includes('<a id=\"deferred-full-aosp-capability-mapping\"></a>')],['D-14 anchor preserved',c.includes('<a id=\"deferred-4-platform-unified-capability-comparison\"></a>')],['7-column header present','| Feature | COBO (Fully Managed) | COPE (WPCO) | BYOD (Work Profile) | Dedicated (COSU) | ZTE (Zero-Touch) | AOSP |','BYOD','COBO','COPE','Dedicated','ZTE','AOSP'].every(t=>typeof t==='string'?c.includes(t):t)],['Device-based CA row',c.includes('Device-based CA')],['Web-app CA row',c.includes('Web-app CA via Edge / Chrome')],['Per-app CA row',c.includes('Per-app CA (MAM)')],['App-based CA row',c.includes('App-based CA / Approved Client App')],['Risk-based CA row',c.includes('Risk-based CA')],['Play Integrity reference',c.includes('Play Integrity')],['AOSP stub link',c.includes('06-aosp-stub.md')]];const fails=tests.filter(([n,v])=>!v).map(([n])=>n);if(fails.length){console.error('Failures:',fails);process.exit(1)}console.log(tests.length+' checks pass');process.exit(0);"</automated>
  </verify>
  <done>Android matrix contains a new `## Conditional Access` H2 between `## Software Updates` and `## Cross-Platform Equivalences`; 7-column table with all 5 CA features; both deferred anchors (Phase 45 + D-14) preserved byte-identical; 5 existing H2 literals unchanged; node check exits 0.</done>
</task>

</tasks>

<verification>
- All 3 sibling matrices (macOS, iOS, Android) contain a new `## Conditional Access` H2
- Each new H2 is positioned between `## Software Updates` body and the next H2 (Key Gaps for macOS/iOS; Cross-Platform Equivalences for Android)
- Column counts: macOS = 3 (Feature/Win/macOS); iOS = 4 (Feature/Win/macOS/iOS); Android = 7 (Feature + 5 GMS modes + AOSP)
- 5 CA features per matrix: Device-based CA, Web-app CA via Edge/Safari/Chrome, Per-app CA (MAM/MAM-WE), App-based CA / Approved Client App, Risk-based CA
- 5 existing H2 literals in each matrix UNCHANGED (`## Enrollment`, `## Configuration`, `## App Deployment`, `## Compliance`, `## Software Updates`)
- Android Phase 45 anchor (`<a id="deferred-full-aosp-capability-mapping"></a>`) and D-14 anchor (`<a id="deferred-4-platform-unified-capability-comparison"></a>`) PRESERVED byte-identical
- GFM-derived `#conditional-access` anchor now resolvable in all 3 sibling matrices (Plan 58-03 will link comparison-doc cells to these anchors)
</verification>

<success_criteria>
- D-04 satisfied: tier-2 mandatory CA H2 retrofit shipped in macOS / iOS / Android matrices
- SC#1 advanced: 6th domain H2 (`## Conditional Access`) now exists in every sibling matrix; comparison doc CA cells will not broken-link
- Append-only contract honored: no existing H2 literal modified; both Android `<a id>` anchors preserved
- PITFALL-15 sidestepped: GFM-deterministic slug `#conditional-access` (lowercase ASCII space-to-hyphen)
- Plan 58-03 unblocked (comparison doc CA H2 row macOS / iOS / Android cells can link to the new anchors)
</success_criteria>

<output>
After completion, create `.planning/phases/58-defer-08-4-platform-capability-comparison/58-02-SUMMARY.md` documenting:
1. Confirmation that 58-ANCHOR-INVENTORY.md gate was satisfied before edits
2. The 3 new `## Conditional Access` H2 blocks added (macOS/iOS/Android) with column counts and feature row counts
3. Verification that 5 existing H2s and both Android `<a id>` anchors remained byte-identical
4. Note for Plan 58-03: "All 3 new `#conditional-access` anchors resolvable; comparison-doc CA H2 row cells may now link to them; Plan 58-03 may proceed with comparison doc authoring."
5. Note for Plan 58-05 (validator): "V-58-11 / V-58-12 / V-58-13 anchor pin assertions for `## Conditional Access` literal in macOS / iOS / Android matrices are now testable."
</output>
