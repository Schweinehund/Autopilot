---
phase: 53
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/co-management/00-overview.md
autonomous: true
requirements: [COMG-01, COMG-04]
requirements_addressed: [COMG-01, COMG-04]

must_haves:
  truths:
    - "docs/operations/co-management/00-overview.md exists with `platform: Windows` + `audience: admin` + `last_verified: 2026-04-27` + `review_by: 2026-06-26` frontmatter (D-12 + V-53-06)"
    - "File body opens with the verbatim `> **Platform applicability:**` blockquote at TOP (within first 50 lines of body, post-frontmatter) covering macOS/iOS/Android analog migration content with corrected cross-link paths (RESEARCH Area 3 corrections vs CONTEXT D-08): macOS=`02-enrollment-profile.md` (singular), iOS=`09-mam-app-protection.md` (NOT 04-byod-mam-overview.md), Android=`00-overview.md` (D-08 + V-53-18 + V-53-19)"
    - "File contains all 7 literal workload tokens verbatim per REQUIREMENTS.md COMG-01: `Compliance Policies`, `Windows Update Policies`, `Resource Access`, `Endpoint Protection`, `Device Configuration`, `Office Click-to-Run Apps`, `Client Apps` (V-53-07)"
    - "File contains literal H2 `## Three Workload Slider States` followed by 3-row state table with `Configuration Manager` / `Pilot Intune` / `Intune` tokens adjacent (V-53-08)"
    - "File contains POSITIVE assertion: literal `collection-scoped` AND (`not a binary toggle` OR `per-collection`) within ~10 lines of 'Pilot Intune' mention (V-53-09)"
    - "File does NOT contain banned phrasings `partially migrated` / `partially-migrated` / `partially_migrated` / `fully migrated` / `fully-migrated` / `fully_migrated` (case-insensitive) — V-53-10 NEGATIVE regression-guard"
    - "File contains Resource Access deprecation note: literal `deprecated` within 10 lines of `Resource Access` AND reference to `CB 2203` or `CB 2403` (V-53-26 in this file; V-53-26 also enforced in 02 by 53-03)"
    - "File contains Device Configuration → Resource Access + Endpoint Protection implicit-switching note (REQ COMG-01 verbatim) — `Device Configuration` switching also implicitly switches `Resource Access` and `Endpoint Protection`"
    - "File contains soft cross-link to `03-cocmgmt-migration-paths.md` (Autopatch prerequisites surface) — V-53-24"
    - "File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens (V-53-25)"
  artifacts:
    - path: "docs/operations/co-management/00-overview.md"
      provides: "Co-management overview + workload model guide (COMG-01) — 7 workloads + 3 slider states + Pilot Intune disambiguation H2 + Resource Access deprecation + Device Config implicit-switching + cross-platform inline blockquote + soft cross-link to 03"
      contains:
        - "platform: Windows"
        - "audience: admin"
        - "last_verified: 2026-04-27"
        - "review_by: 2026-06-26"
        - "> **Platform applicability:**"
        - "../../admin-setup-macos/02-enrollment-profile.md"
        - "../../admin-setup-ios/00-overview.md"
        - "../../admin-setup-ios/09-mam-app-protection.md"
        - "../../admin-setup-android/00-overview.md"
        - "Jamf"
        - "ABM MDM transfer"
        - "Device Administrator"
        - "Compliance Policies"
        - "Windows Update Policies"
        - "Resource Access"
        - "Endpoint Protection"
        - "Device Configuration"
        - "Office Click-to-Run Apps"
        - "Client Apps"
        - "## Three Workload Slider States"
        - "Configuration Manager"
        - "Pilot Intune"
        - "collection-scoped"
        - "not a binary toggle"
        - "deprecated"
        - "CB 2203"
        - "CB 2403"
        - "03-cocmgmt-migration-paths.md"
  key_links:
    - from: "docs/operations/co-management/00-overview.md"
      to: "docs/operations/co-management/03-cocmgmt-migration-paths.md"
      via: "soft cross-link to Autopatch prereqs surface (D-07 + V-53-24)"
      pattern: "03-cocmgmt-migration-paths\\.md"
    - from: "docs/operations/co-management/00-overview.md"
      to: "docs/admin-setup-macos/02-enrollment-profile.md"
      via: "Platform applicability blockquote macOS analog migration cross-link"
      pattern: "\\.\\./\\.\\./admin-setup-macos/02-enrollment-profile\\.md"
    - from: "docs/operations/co-management/00-overview.md"
      to: "docs/admin-setup-ios/09-mam-app-protection.md"
      via: "Platform applicability blockquote iOS MAM analog migration cross-link (RESEARCH Area 3 path correction)"
      pattern: "\\.\\./\\.\\./admin-setup-ios/09-mam-app-protection\\.md"
    - from: "docs/operations/co-management/00-overview.md"
      to: "docs/admin-setup-android/00-overview.md"
      via: "Platform applicability blockquote Android analog migration cross-link"
      pattern: "\\.\\./\\.\\./admin-setup-android/00-overview\\.md"
---

<objective>
Author `docs/operations/co-management/00-overview.md` (COMG-01) — the co-management overview + workload model guide. Covers all 7 ConfigMgr CB 2503 workloads, three slider states with Pilot Intune disambiguation H2 (PITFALL-8 mandate), Resource Access deprecation (CB 2203/CB 2403), Device Configuration → Resource Access + Endpoint Protection implicit-switching note, soft cross-link to 03 for Autopatch prereqs, and an inline `> **Platform applicability:**` blockquote at the TOP with macOS/iOS/Android analog migration content (REQ COMG-04 inline placement per D-08).

Purpose: Implements CONTEXT D-01 (file naming) + D-04 (Resource Access deprecation row content reused here) + D-06 (Pilot Intune POSITIVE+NEGATIVE dual-defense — content side) + D-07 (soft cross-link to 03 for Autopatch) + D-08 (cross-platform inline blockquote) + D-12 (frontmatter contract). Validator V-53-07/08/09/10/18/19/24/25/26 enforce structural correctness at commit time.

This is the COMG-01 deliverable per REQUIREMENTS.md line 156 traceability mapping. Wave 1 — parallel with 53-02 / 53-03 / 53-04 / 53-05 / 53-06 (no inter-file authoring dependency; soft cross-links resolve at validator runtime).

CRITICAL CORRECTIONS from CONTEXT.md D-08 (per RESEARCH Area 3 verified file system check):
- iOS MAM cross-link: `09-mam-app-protection.md` (NOT `04-byod-mam-overview.md` — that file does NOT exist)
- macOS cross-link: `02-enrollment-profile.md` (singular — NOT `02-enrollment-profiles.md` plural)

Output: 1 new file at `docs/operations/co-management/00-overview.md`. Creates new directory `docs/operations/co-management/` (parent `docs/operations/` will also be created when the operations index file 53-05 ships in the same atomic commit).

DO NOT commit yet — atomic commit owned by 53-07 per CONTEXT D-14 + CDI-Phase53-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-co-management-operational-docs/53-CONTEXT.md
@.planning/phases/53-co-management-operational-docs/53-RESEARCH.md
@.planning/phases/53-co-management-operational-docs/53-PATTERNS.md
@.planning/phases/53-co-management-operational-docs/53-VALIDATION.md
@docs/admin-setup-android/00-overview.md
@docs/admin-setup-ios/00-overview.md
@docs/admin-setup-ios/09-mam-app-protection.md
@docs/admin-setup-macos/02-enrollment-profile.md

<interfaces>
**File this plan creates:** `docs/operations/co-management/00-overview.md`

**Cross-link targets (verified to exist; relative paths from this file):**
- `../../admin-setup-macos/02-enrollment-profile.md` (singular) — VERIFIED EXISTS
- `../../admin-setup-ios/00-overview.md` — VERIFIED EXISTS
- `../../admin-setup-ios/09-mam-app-protection.md` — VERIFIED EXISTS (RESEARCH Area 3 correction; CONTEXT D-08 path was wrong)
- `../../admin-setup-android/00-overview.md` — VERIFIED EXISTS

**Soft cross-link target (created in same atomic commit by 53-04):**
- `03-cocmgmt-migration-paths.md#autopatch-prerequisites` — sibling file in same directory; H2 anchor created by 53-04

**Validator V-53-NN assertions enforced against this file (from CONTEXT.md D-10):**

| V-NN | Assertion |
|------|-----------|
| V-53-01 | File exists |
| V-53-06 | Frontmatter: `platform: Windows`, `audience: admin`, `last_verified: 2026-04-27`, `review_by: 2026-06-26` (60-day cycle) |
| V-53-07 | All 7 workload literal tokens present: `Compliance Policies`, `Windows Update Policies`, `Resource Access`, `Endpoint Protection`, `Device Configuration`, `Office Click-to-Run Apps`, `Client Apps` |
| V-53-08 | Literal H2 `## Three Workload Slider States` AND 3 state tokens (`Configuration Manager`, `Pilot Intune`, `Intune`) within 2000-byte window of H2 |
| V-53-09 | POSITIVE: `collection-scoped` AND (`not a binary toggle` OR `per-collection`) within ~10 lines of "Pilot Intune" |
| V-53-10 | NEGATIVE: NO `partially migrated` / `partially-migrated` / `fully migrated` / `fully-migrated` (case-insensitive) — applies to all 4 co-management/ files; this file is in CO_MGMT_FILES scope |
| V-53-18 | `> **Platform applicability:**` blockquote within first 50 lines of body (post-frontmatter) |
| V-53-19 | Cross-platform analog tokens in blockquote: `Jamf`, `ABM MDM transfer`, `MAM`, `Device Administrator` |
| V-53-24 | Soft cross-link to `03-cocmgmt-migration-paths.md` present |
| V-53-25 | NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens outside Version History |
| V-53-26 | `deprecated` within 10 lines of `Resource Access` AND `CB 2203` or `CB 2403` reference |

**Frontmatter contract (D-12 verbatim):**
```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: all
audience: admin
platform: Windows
---
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/operations/co-management/00-overview.md (frontmatter + Platform applicability blockquote + H1 + 7-workload enumeration + Three Workload Slider States H2 + Pilot Intune POSITIVE assertion + Device Config implicit-switching note + Resource Access deprecation note + soft cross-link to 03)</name>
  <files>docs/operations/co-management/00-overview.md</files>
  <read_first>
    - .planning/phases/53-co-management-operational-docs/53-CONTEXT.md (D-01 file naming + D-04 Resource Access deprecation row + D-06 Pilot Intune dual-defense + D-07 soft cross-link from 00 to 03 Autopatch + D-08 inline blockquote + D-12 frontmatter)
    - .planning/phases/53-co-management-operational-docs/53-RESEARCH.md (Pattern 2 verbatim Platform applicability blockquote with corrected paths; Pattern 4 verbatim Three Workload Slider States table + Pilot Intune note; Research Area 1 7-workload exact tokens + Resource Access deprecation details + Device Config implicit-switching note; Research Area 3 cross-link target verification)
    - .planning/phases/53-co-management-operational-docs/53-PATTERNS.md (Section "00-overview.md" lines 32-216 — paste-ready snippets)
    - .planning/phases/53-co-management-operational-docs/53-VALIDATION.md (Per-Task Verification Map row 53-01-01: V-53-07/08/09/18/19/24/26)
    - docs/admin-setup-android/00-overview.md (analog file lines 1-7 frontmatter shape; lines 14-18 H1 + intro)
    - docs/admin-setup-ios/09-mam-app-protection.md (verify file exists for cross-link)
    - docs/admin-setup-macos/02-enrollment-profile.md (verify file exists for cross-link; note SINGULAR filename)
  </read_first>
  <behavior>
    - File `docs/operations/co-management/00-overview.md` exists
    - Frontmatter (D-12): `last_verified: 2026-04-27`, `review_by: 2026-06-26`, `applies_to: all`, `audience: admin`, `platform: Windows`
    - Platform applicability blockquote at TOP (immediately after frontmatter `---` closing fence, before H1) covering all 3 cross-platform analogs (macOS/iOS/Android) with verified cross-link paths
    - H1 `# Co-Management Overview: ConfigMgr Workload Model` followed by intro paragraph
    - 7-workload enumeration containing all 7 literal tokens verbatim per REQUIREMENTS.md COMG-01
    - `## Three Workload Slider States` H2 with 3-row state table + Pilot Intune POSITIVE assertion ("collection-scoped" + "not a binary toggle")
    - Device Configuration → Resource Access + Endpoint Protection implicit-switching note (REQ COMG-01 verbatim)
    - Resource Access deprecation H3 with `deprecated` + `CB 2203` + `CB 2403` references
    - Soft cross-link to `03-cocmgmt-migration-paths.md` for Autopatch prerequisites surface (Windows Autopatch blockquote with link to `03-cocmgmt-migration-paths.md#autopatch-prerequisites`)
    - NO banned phrasings (`partially migrated`, `fully migrated`, etc.) anywhere in the file
    - NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens
  </behavior>
  <action>
Create the file `docs/operations/co-management/00-overview.md` with the following content sections IN ORDER. The directory `docs/operations/co-management/` does not yet exist — create it implicitly via the file write.

**Section 1 — Frontmatter (D-12 + V-53-06; copy verbatim):**

```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: all
audience: admin
platform: Windows
---
```

**Section 2 — Platform applicability blockquote at TOP (D-08 + V-53-18 + V-53-19; paste verbatim from 53-RESEARCH.md Pattern 2; CRITICAL — use corrected cross-link paths per RESEARCH Area 3):**

```markdown
> **Platform applicability:** This guide is Windows-specific (ConfigMgr + Intune co-management).
> **macOS:** No co-management equivalent — Microsoft Intune does not federate with Jamf at the
> workload-slider level. Migration path: Jamf → Intune via ABM MDM transfer (release device
> assignment in Jamf-bound MDM server in ABM, then re-assign to Intune-bound MDM server; in-use
> devices require user-initiated re-enrollment). See [macOS Enrollment Profile](../../admin-setup-macos/02-enrollment-profile.md).
> **iOS/iPadOS:** No co-management equivalent — Apple MDM is single-server-per-device. Migration
> path: MAM → MDM transition (MAM-WE app protection policies coexist with new MDM enrollment).
> See [iOS/iPadOS Admin Setup](../../admin-setup-ios/00-overview.md) and
> [iOS MAM App Protection](../../admin-setup-ios/09-mam-app-protection.md).
> **Android:** No co-management equivalent. Migration paths: legacy Device Administrator →
> Android Enterprise (DA deprecated August 2022; wipe + re-enrollment required); MAM → MDM for
> BYOD. See [Android Admin Setup](../../admin-setup-android/00-overview.md).
```

CRITICAL — verify cross-link paths character-by-character:
- macOS: `../../admin-setup-macos/02-enrollment-profile.md` (SINGULAR — no trailing `s`)
- iOS MAM: `../../admin-setup-ios/09-mam-app-protection.md` (NOT `04-byod-mam-overview.md`)
- iOS overview: `../../admin-setup-ios/00-overview.md`
- Android: `../../admin-setup-android/00-overview.md`

V-53-19 verifies the analog tokens `Jamf`, `ABM MDM transfer`, `MAM`, `Device Administrator` are present — they are all in the blockquote above.

**Section 3 — H1 + intro paragraph:**

```markdown
# Co-Management Overview: ConfigMgr Workload Model

This guide covers the Windows ConfigMgr-to-Intune co-management workload model for Intune
administrators. Seven workloads can each be migrated independently from ConfigMgr to Intune
management using per-workload slider controls. This overview explains the slider model, the seven
workloads, and how to plan a migration.

Co-management requires a Configuration Manager site (current branch CB 2503 GA April 23 2025 or
later) and Intune licensing. For tenant attach (no workload sliders, portal-sync only), see
[Windows Tenant Attach](01-windows-tenant-attach.md). For the workload migration sequence, see
[Workload Slider Migration](02-windows-workload-sliders.md). For Windows Autopatch prerequisites,
see [Migration Paths and Autopatch](03-cocmgmt-migration-paths.md#autopatch-prerequisites).
```

The cross-link to `03-cocmgmt-migration-paths.md#autopatch-prerequisites` in this paragraph satisfies V-53-24 (soft cross-link from 00 to 03). Additional reinforcement appears in the Windows Autopatch blockquote in Section 8 below.

**Section 4 — 7-workload enumeration (V-53-07; literal tokens locked per REQUIREMENTS.md COMG-01 verbatim):**

```markdown
## The Seven Co-Management Workloads

ConfigMgr CB 2503 (current branch, GA April 23 2025) defines seven co-management workloads. Each
workload's slider state determines whether ConfigMgr or Intune manages that workload's policies.

1. **Compliance Policies** — Device compliance policy enforcement (e.g., minimum OS version,
   BitLocker encryption, antivirus signature freshness). Lowest migration risk; typically the
   first workload moved to Intune.
2. **Windows Update Policies** — Windows Update for Business deferral and ring assignment
   policies. Required for Windows Autopatch enablement (see
   [Migration Paths and Autopatch](03-cocmgmt-migration-paths.md#autopatch-prerequisites)).
3. **Resource Access** — VPN, Wi-Fi, certificate, and email profile policies. **Deprecated since
   CB 2203** and mandated to Intune in CB 2403; the slider is moved automatically when tenants
   upgrade to CB 2403 or later. See [Resource Access Deprecation](#resource-access-deprecation)
   below for migration details.
4. **Endpoint Protection** — Microsoft Defender for Endpoint policy, Windows Defender Firewall,
   Windows Defender Antivirus configuration. **HIGH-RISK** workload — requires Intune Defender
   for Endpoint policy published, targeted, and confirmed reporting healthy before slider move.
   See [Workload Slider Migration > Endpoint Protection HIGH-RISK](02-windows-workload-sliders.md)
   for the full callout.
5. **Device Configuration** — Configuration profiles (settings catalog, ADMX templates, custom
   OMA-URI). **Switching this workload also implicitly switches the Resource Access and Endpoint
   Protection workloads** if those workloads have not already been individually moved (see
   [Implicit Workload Switching](#implicit-workload-switching) below).
6. **Office Click-to-Run Apps** — Microsoft 365 Apps (Office C2R) deployment, channel selection,
   and update policies. Required for Windows Autopatch enablement.
7. **Client Apps** — Win32 LOB apps, MSI deployments, and Microsoft Store app deployments. Has
   the largest pre-migration validation surface (supersedence chains, dependency graphs, app
   detection rules).
```

**Section 5 — `## Three Workload Slider States` H2 + Pilot Intune POSITIVE+NEGATIVE dual-defense (D-06 + V-53-08 + V-53-09; literal H2 string pinned by validator):**

```markdown
## Three Workload Slider States

Each of the seven workloads has an independent slider with three possible states. The slider state
is set per-workload in the ConfigMgr console (Administration > Cloud Services > Co-management).

| State | Scope | Behavior |
|-------|-------|----------|
| Configuration Manager | All co-managed devices | ConfigMgr manages this workload for all devices |
| Pilot Intune | Devices in specified pilot collection only | Intune manages this workload for collection members; remainder of fleet stays on ConfigMgr |
| Intune | All co-managed devices | Intune manages this workload for all enrolled devices |

> **Note:** The Pilot Intune state is **not a binary toggle** — it is collection-scoped Intune
> management. The slider state is per-collection: devices in the pilot collection are managed by
> Intune; devices outside the collection remain on ConfigMgr. A device can be in the Pilot
> Intune collection for some workloads and not others. This per-collection scoping is intentional
> — it allows phased rollouts and per-workload pilot programs.

**Pilot Intune behavioral note for Endpoint Protection and Device Configuration:** When Pilot
Intune is selected for these two workloads, Intune deploys the policies but does NOT perform
policy removal upon unassignment. For policy removal from devices when policies are unassigned,
the workload must be switched to Intune (full migration).
```

CRITICAL — V-53-09 POSITIVE assertion requires the literal token `collection-scoped` AND (`not a binary toggle` OR `per-collection`) within ~10 lines of "Pilot Intune". The blockquote above contains all three tokens.

CRITICAL — V-53-10 NEGATIVE assertion bans `partially migrated`, `partially-migrated`, `fully migrated`, `fully-migrated` (case-insensitive). DO NOT use these phrasings anywhere in the file. Use "collection-scoped" or "Pilot Intune state" or "Intune state" instead.

**Section 6 — Implicit workload switching note (REQ COMG-01 verbatim "Device Configuration → Resource Access + Endpoint Protection implicit-switching"):**

```markdown
## Implicit Workload Switching

Two workloads are implicitly switched when the Device Configuration workload's slider is moved:

- **Resource Access** — Resource Access policy is part of Device Configuration in modern
  ConfigMgr; switching Device Configuration moves Resource Access automatically (if not already
  individually switched).
- **Endpoint Protection** — Endpoint Protection policy is also part of Device Configuration;
  switching Device Configuration moves Endpoint Protection automatically.

> **Implicit workload switching warning:** Before moving the Device Configuration workload,
> verify that Intune Endpoint Protection policy is published and confirmed healthy — moving
> Device Configuration without an Intune EP policy in place creates a Defender coverage gap. See
> [Workload Slider Migration > Endpoint Protection HIGH-RISK](02-windows-workload-sliders.md) for
> the full HIGH-RISK callout.
```

**Section 7 — Resource Access Deprecation H3 (V-53-26 — `deprecated` near `Resource Access` + `CB 2203` or `CB 2403`):**

```markdown
## Resource Access Deprecation {#resource-access-deprecation}

The Resource Access workload is **deprecated since CB 2203** and was mandated to Intune in
**CB 2403**. Starting in CB 2403:

- The Resource Access node is removed from the ConfigMgr console
- The slider is automatically set to Intune
- Tenants upgrading to CB 2403 have this workload moved automatically
- New ConfigMgr deployments cannot configure Resource Access policies in ConfigMgr

For tenants on CB 2403 or later, the Resource Access workload requires no manual migration. For
tenants on CB 2203 — CB 2403 (deprecated but still configurable), Resource Access policies should
be moved to Intune Configuration Profiles (Settings Catalog) in advance of the CB 2403 upgrade.
```

**Section 8 — Windows Autopatch soft cross-link blockquote (D-07 + V-53-24 reinforcement):**

```markdown
## Windows Autopatch and Co-Management

Windows Autopatch is a co-management successor — it requires three workloads to be set to Pilot
Intune or Intune before Autopatch can be enabled:

- Windows Update Policies workload
- Device Configuration workload
- Office Click-to-Run Apps workload

> **Windows Autopatch:** Co-management is a prerequisite for Windows Autopatch. Before enabling
> Autopatch, three workloads (Windows Update Policies, Device Configuration, and Office
> Click-to-Run Apps) must be set to Pilot Intune or Intune. See
> [Migration Paths and Autopatch Prerequisites](03-cocmgmt-migration-paths.md#autopatch-prerequisites)
> for the full prerequisite checklist and configuration steps.
```

V-53-24 verifies the literal cross-link `03-cocmgmt-migration-paths.md` is present in this file. This blockquote and the H1 intro paragraph (Section 3) both contain it.

**Section 9 — Related resources / footer:**

```markdown
## Related Resources

- [Windows Tenant Attach](01-windows-tenant-attach.md) — Tenant attach vs full co-management
  disambiguation
- [Workload Slider Migration](02-windows-workload-sliders.md) — Low-risk-first migration sequence
  with EP HIGH-RISK callout
- [Migration Paths and Autopatch](03-cocmgmt-migration-paths.md) — Windows Autopatch co-management
  prerequisites

## External References

- [ConfigMgr Co-Management Workloads (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/configmgr/comanage/workloads)
- [Co-Management Overview (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/configmgr/comanage/overview)
- [Windows Autopatch Prerequisites (Microsoft Learn)](https://learn.microsoft.com/en-us/windows/deployment/windows-autopatch/prepare/windows-autopatch-prerequisites)
```

After authoring, verify the following grep checks pass against the new file:

```bash
grep -c "platform: Windows" docs/operations/co-management/00-overview.md   # expect ≥1
grep -c "Compliance Policies" docs/operations/co-management/00-overview.md  # expect ≥1
grep -c "Windows Update Policies" docs/operations/co-management/00-overview.md  # expect ≥1
grep -c "Resource Access" docs/operations/co-management/00-overview.md  # expect ≥3
grep -c "Endpoint Protection" docs/operations/co-management/00-overview.md  # expect ≥3
grep -c "Device Configuration" docs/operations/co-management/00-overview.md  # expect ≥3
grep -c "Office Click-to-Run Apps" docs/operations/co-management/00-overview.md  # expect ≥1
grep -c "Client Apps" docs/operations/co-management/00-overview.md  # expect ≥1
grep -c "## Three Workload Slider States" docs/operations/co-management/00-overview.md  # expect 1
grep -c "collection-scoped" docs/operations/co-management/00-overview.md  # expect ≥1
grep -c "> \*\*Platform applicability:\*\*" docs/operations/co-management/00-overview.md  # expect 1
grep -c "03-cocmgmt-migration-paths.md" docs/operations/co-management/00-overview.md  # expect ≥2
grep -c "CB 2203\|CB 2403" docs/operations/co-management/00-overview.md  # expect ≥1
grep -ci "partially migrated\|fully migrated" docs/operations/co-management/00-overview.md  # MUST be 0
grep -c "TBD\|TODO\|FIXME\|XXX\|PLACEHOLDER" docs/operations/co-management/00-overview.md  # MUST be 0
```

DO NOT commit yet — atomic commit owned by 53-07 per CONTEXT D-14 + CDI-Phase53-04.
  </action>
  <verify>
    <automated>test -f docs/operations/co-management/00-overview.md &amp;&amp; grep -q "## Three Workload Slider States" docs/operations/co-management/00-overview.md &amp;&amp; grep -q "collection-scoped" docs/operations/co-management/00-overview.md &amp;&amp; grep -q "platform: Windows" docs/operations/co-management/00-overview.md &amp;&amp; ! grep -qi "partially migrated\|fully migrated" docs/operations/co-management/00-overview.md &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/operations/co-management/00-overview.md` exists
    - File contains frontmatter with `platform: Windows`, `audience: admin`, `last_verified: 2026-04-27`, `review_by: 2026-06-26`, `applies_to: all`
    - File contains literal `> **Platform applicability:**` blockquote within first 50 lines of body (post-frontmatter)
    - File contains all 4 cross-platform analog tokens: `Jamf`, `ABM MDM transfer`, `MAM`, `Device Administrator`
    - File contains all 4 verified cross-link paths verbatim: `../../admin-setup-macos/02-enrollment-profile.md`, `../../admin-setup-ios/00-overview.md`, `../../admin-setup-ios/09-mam-app-protection.md`, `../../admin-setup-android/00-overview.md`
    - File does NOT contain `04-byod-mam-overview.md` (forbidden — file does not exist)
    - File does NOT contain `02-enrollment-profiles.md` (forbidden — plural form is wrong)
    - File contains all 7 literal workload tokens: `Compliance Policies`, `Windows Update Policies`, `Resource Access`, `Endpoint Protection`, `Device Configuration`, `Office Click-to-Run Apps`, `Client Apps`
    - File contains literal H2 `## Three Workload Slider States` (case-sensitive, exact match)
    - Within ~2000 bytes after H2: literal tokens `Configuration Manager`, `Pilot Intune`, `Intune` all present
    - Within ~10 lines of "Pilot Intune": literal `collection-scoped` AND (`not a binary toggle` OR `per-collection`) present
    - File does NOT contain banned phrasings (case-insensitive grep): `partially migrated`, `partially-migrated`, `partially_migrated`, `fully migrated`, `fully-migrated`, `fully_migrated`
    - File contains literal `deprecated` within 10 lines of `Resource Access` AND `CB 2203` or `CB 2403`
    - File contains literal cross-link to `03-cocmgmt-migration-paths.md` (V-53-24 — at least once; this implementation provides ≥2 occurrences for redundancy)
    - File contains the Device Configuration → Resource Access + Endpoint Protection implicit-switching note
    - File does NOT contain any of: `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` (V-53-25)
  </acceptance_criteria>
  <done>00-overview.md authored with all D-04/D-06/D-07/D-08/D-12 surfaces; all V-53-NN assertions for this file will pass; no commit (53-07 owns atomicity).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → reader | Documentation content; reader trust depends on factual correctness + explicit hazard surfacing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01-01 | Information disclosure (operational) | Pilot Intune mental-model drift | mitigate | D-06 dual-defense: POSITIVE assertion (V-53-09 `collection-scoped` proximity) + NEGATIVE assertion (V-53-10 banned phrasings). Author MUST use "collection-scoped" framing exclusively. |
| T-53-01-02 | Information disclosure (cross-platform routing) | macOS/iOS/Android admins reading Windows-only co-management content for cross-platform fleet | mitigate | D-08 inline `> **Platform applicability:**` blockquote at TOP (V-53-18 + V-53-19 enforce). Blockquote with verified cross-link paths routes admins to the correct platform-specific path. |
| T-53-01-03 | Tampering (factual drift) | Workload taxonomy out of sync if CB upgrades (CB 2503 → CB 2603+) | accept | 60-day `review_by: 2026-06-26` cycle is structural mitigation; all factual claims cite Microsoft Learn updated 2025-05-12 + 2026-01-29. |
| T-53-01-04 | Tampering (broken link) | iOS cross-link path is wrong in CONTEXT D-08 (`04-byod-mam-overview.md` does not exist) | mitigate | RESEARCH Area 3 path correction; this plan uses the verified `09-mam-app-protection.md` path. Pre-commit `markdown-link-check` catches any regression. |

Severity: LOW. Documentation phase; no application security surface introduced.
</threat_model>

<verification>
After Task 1 completes:

```bash
# File exists and contains required structural elements
test -f docs/operations/co-management/00-overview.md
echo "file-exists: $?"

# Frontmatter
head -7 docs/operations/co-management/00-overview.md | grep -E "^(platform|audience|last_verified|review_by|applies_to):"

# Platform applicability blockquote in first 50 lines of body
sed -n '/^---$/,/^---$/!p' docs/operations/co-management/00-overview.md | head -50 | grep -c "> \*\*Platform applicability:\*\*"
# expect 1

# All 7 workload tokens present
for tok in "Compliance Policies" "Windows Update Policies" "Resource Access" "Endpoint Protection" "Device Configuration" "Office Click-to-Run Apps" "Client Apps"; do
  count=$(grep -c "$tok" docs/operations/co-management/00-overview.md)
  echo "$tok: $count"
done

# Three Workload Slider States H2 + state tokens
grep -q "^## Three Workload Slider States$" docs/operations/co-management/00-overview.md && echo "H2: PASS" || echo "H2: FAIL"

# POSITIVE Pilot Intune disambiguation (V-53-09)
grep -q "collection-scoped" docs/operations/co-management/00-overview.md && echo "collection-scoped: PASS"
grep -q "not a binary toggle\|per-collection" docs/operations/co-management/00-overview.md && echo "not a binary toggle: PASS"

# NEGATIVE V-53-10 (must be 0)
grep -ic "partially migrated\|partially-migrated\|partially_migrated\|fully migrated\|fully-migrated\|fully_migrated" docs/operations/co-management/00-overview.md
# expect 0

# Resource Access deprecation V-53-26
grep -E "deprecated.*Resource Access|Resource Access.*deprecated" docs/operations/co-management/00-overview.md
grep -E "CB 2203|CB 2403" docs/operations/co-management/00-overview.md

# Soft cross-link to 03 V-53-24
grep -c "03-cocmgmt-migration-paths.md" docs/operations/co-management/00-overview.md
# expect ≥1

# Cross-link path correctness — no broken paths
! grep "04-byod-mam-overview.md" docs/operations/co-management/00-overview.md && echo "no broken iOS path: PASS"
! grep "02-enrollment-profiles.md" docs/operations/co-management/00-overview.md && echo "no plural macOS path: PASS"
grep -q "../../admin-setup-ios/09-mam-app-protection.md" docs/operations/co-management/00-overview.md && echo "iOS MAM path: PASS"
grep -q "../../admin-setup-macos/02-enrollment-profile.md" docs/operations/co-management/00-overview.md && echo "macOS singular path: PASS"

# TBD scan V-53-25
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/co-management/00-overview.md
# expect no matches
```

If 53-06 validator is also authored at this point (parallel Wave 1):

```bash
node scripts/validation/check-phase-53.mjs --verbose 2>&1 | grep "V-53-0[1789]\|V-53-18\|V-53-19\|V-53-24\|V-53-25\|V-53-26"
# All file-specific assertions for 00-overview.md should PASS once all sibling files also land
# (V-53-22 will fail until 53-05 ships ops/00-index.md; V-53-13/14/15/16/17 fail until 53-03 ships)
```

DO NOT commit — atomic commit owned by 53-07.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 53 Success Criteria + REQUIREMENTS.md COMG-01 / COMG-04:

- **SC#1** (3 slider states + Pilot Intune collection-scoped): Satisfied by `## Three Workload Slider States` H2 + 3-row state table + POSITIVE assertion blockquote
- **SC#4** (cross-platform analog callouts): Satisfied by `> **Platform applicability:**` blockquote at TOP with macOS/iOS/Android analog migration content
- **COMG-01** (overview + 7 workloads + 3 states + implicit-switching note): Fully covered
- **COMG-04** (cross-platform inline callouts): Inline blockquote per D-08

Plan complete when:

- [ ] File exists at `docs/operations/co-management/00-overview.md`
- [ ] File frontmatter passes V-53-06 (Windows + admin + 60-day cycle)
- [ ] V-53-07 (7 workloads), V-53-08 (slider states H2), V-53-09 (POSITIVE), V-53-10 (NEGATIVE), V-53-18 (blockquote), V-53-19 (analog tokens), V-53-24 (soft cross-link), V-53-25 (TBD scan), V-53-26 (Resource Access deprecation) all pass against this file
- [ ] All cross-link paths verified to point at existing files
- [ ] No commit — 53-07 owns atomicity per CONTEXT D-14
</success_criteria>

<output>
After completion, create `.planning/phases/53-co-management-operational-docs/53-01-SUMMARY.md` documenting:

- File path + line count
- Sections authored (frontmatter, Platform applicability blockquote, H1 + intro, 7-workload enumeration, Three Workload Slider States H2, Implicit Workload Switching, Resource Access Deprecation, Windows Autopatch soft cross-link blockquote, Related Resources)
- V-53-NN assertions this file satisfies (V-53-01, V-53-06, V-53-07, V-53-08, V-53-09, V-53-10, V-53-18, V-53-19, V-53-24, V-53-25, V-53-26)
- Cross-link paths used (with note: corrected from CONTEXT D-08 per RESEARCH Area 3 — iOS = `09-mam-app-protection.md`, macOS = singular `02-enrollment-profile.md`)
- Note: NO COMMIT — atomic commit owned by 53-07
</output>