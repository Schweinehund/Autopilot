---
phase: 53
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/co-management/01-windows-tenant-attach.md
autonomous: true
requirements: [COMG-03, COMG-04]
requirements_addressed: [COMG-03, COMG-04]

must_haves:
  truths:
    - "docs/operations/co-management/01-windows-tenant-attach.md exists with `platform: Windows` + `audience: admin` + `last_verified: 2026-04-27` + `review_by: 2026-06-26` frontmatter (D-12 + V-53-06)"
    - "File body opens with the verbatim `> **Platform applicability:**` blockquote at TOP (within first 50 lines of body, post-frontmatter) — identical content to 00-overview.md per D-08 + V-53-18 + V-53-19"
    - "File contains a side-by-side comparison table with literal column headers `| Capability | Tenant Attach | Full Co-Management |` (V-53-11) and ≥10 data rows distinguishing tenant attach from full co-management capabilities (D-03)"
    - "File contains literal SC#3 anchor tokens `no workload switching` AND `workload sliders` (V-53-12) — the SC#3-line-242-mandated distinguishing characteristic"
    - "File does NOT contain banned phrasings `partially migrated` / `fully migrated` (case-insensitive variants) — V-53-10 NEGATIVE regression-guard"
    - "File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens (V-53-25)"
    - "Cross-link paths verified to existing files: macOS singular `02-enrollment-profile.md`, iOS `09-mam-app-protection.md` (NOT `04-byod-mam-overview.md`), Android `00-overview.md`"
  artifacts:
    - path: "docs/operations/co-management/01-windows-tenant-attach.md"
      provides: "Tenant attach vs full co-management disambiguation guide (COMG-03) — side-by-side feature comparison table + SC#3 'no workload switching' anchor + cross-platform inline blockquote"
      contains:
        - "platform: Windows"
        - "audience: admin"
        - "last_verified: 2026-04-27"
        - "review_by: 2026-06-26"
        - "> **Platform applicability:**"
        - "../../admin-setup-macos/02-enrollment-profile.md"
        - "../../admin-setup-ios/00-overview.md"
        - "../../admin-setup-ios/09-mam-app-protection.md"
        - "../../admin-setup-android/00-overview.md"
        - "Jamf"
        - "ABM MDM transfer"
        - "Device Administrator"
        - "| Capability | Tenant Attach | Full Co-Management |"
        - "no workload switching"
        - "workload sliders"
        - "Tenant Attach"
        - "Full Co-Management"
        - "CMPivot"
  key_links:
    - from: "docs/operations/co-management/01-windows-tenant-attach.md"
      to: "docs/operations/co-management/00-overview.md"
      via: "intro paragraph cross-reference for slider model context"
      pattern: "00-overview\\.md"
    - from: "docs/operations/co-management/01-windows-tenant-attach.md"
      to: "docs/operations/co-management/03-cocmgmt-migration-paths.md"
      via: "Windows Autopatch eligibility row in comparison table"
      pattern: "03-cocmgmt-migration-paths\\.md"
---

<objective>
Author `docs/operations/co-management/01-windows-tenant-attach.md` (COMG-03) — the tenant attach vs full co-management disambiguation guide. Per CONTEXT D-03, uses a side-by-side feature comparison table (Capability | Tenant Attach | Full Co-Management) to make the distinction explicit. Per CONTEXT D-08 + REQ COMG-04, includes the inline `> **Platform applicability:**` blockquote at the TOP for cross-platform analog migration content.

Purpose: Implements CONTEXT D-03 (side-by-side comparison table — 1C-1 winner) + D-08 (cross-platform inline blockquote) + D-12 (frontmatter contract). Validator V-53-11 enforces the comparison-table-shape; V-53-12 enforces the SC#3 "no workload switching" anchor; V-53-18 + V-53-19 enforce the inline blockquote and analog tokens.

This is the COMG-03 deliverable per REQUIREMENTS.md line 158 traceability mapping. Wave 1 — parallel with 53-01 / 53-03 / 53-04 / 53-05 / 53-06.

CRITICAL CORRECTIONS from CONTEXT.md D-08 (per RESEARCH Area 3 verified file system check):
- iOS MAM cross-link: `09-mam-app-protection.md` (NOT `04-byod-mam-overview.md`)
- macOS cross-link: `02-enrollment-profile.md` (singular)

Output: 1 new file at `docs/operations/co-management/01-windows-tenant-attach.md`.

DO NOT commit yet — atomic commit owned by 53-07 per CONTEXT D-14 + CDI-Phase53-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-co-management-operational-docs/53-CONTEXT.md
@.planning/phases/53-co-management-operational-docs/53-RESEARCH.md
@.planning/phases/53-co-management-operational-docs/53-PATTERNS.md
@.planning/phases/53-co-management-operational-docs/53-VALIDATION.md
@docs/reference/android-capability-matrix.md

<interfaces>
**File this plan creates:** `docs/operations/co-management/01-windows-tenant-attach.md`

**Cross-link targets (verified to exist):**
- `../../admin-setup-macos/02-enrollment-profile.md` (singular) — VERIFIED
- `../../admin-setup-ios/00-overview.md` — VERIFIED
- `../../admin-setup-ios/09-mam-app-protection.md` — VERIFIED (RESEARCH Area 3 correction)
- `../../admin-setup-android/00-overview.md` — VERIFIED

**Sibling files in same directory (created in same atomic commit):**
- `00-overview.md` — created by 53-01
- `02-windows-workload-sliders.md` — created by 53-03
- `03-cocmgmt-migration-paths.md` — created by 53-04

**Validator V-53-NN assertions enforced against this file:**

| V-NN | Assertion |
|------|-----------|
| V-53-02 | File exists |
| V-53-06 | Frontmatter: `platform: Windows`, `audience: admin`, 60-day cycle |
| V-53-10 | NEGATIVE: NO `partially migrated` / `fully migrated` variants (file is in CO_MGMT_FILES scope) |
| V-53-11 | Comparison table with literal `| Capability | Tenant Attach | Full Co-Management |` header |
| V-53-12 | SC#3 anchor literals: `no workload switching` AND `workload sliders` present (case-insensitive match acceptable) |
| V-53-18 | `> **Platform applicability:**` blockquote within first 50 lines of body |
| V-53-19 | Cross-platform analog tokens in blockquote: `Jamf`, `ABM MDM transfer`, `MAM`, `Device Administrator` |
| V-53-25 | NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens |

**Frontmatter contract (D-12 verbatim):**
```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: all
audience: admin
platform: Windows
---
```

**Comparison table baseline (RESEARCH Pattern 5; CD-05 permits adding 4th Notes column):**

```markdown
| Capability | Tenant Attach | Full Co-Management |
|------------|---------------|---------------------|
| Intune admin center device sync | Yes | Yes |
| Remote actions (from Intune portal) | Yes | Yes |
| Endpoint security reports | Yes | Yes |
| CMPivot queries from Intune | Yes | Yes |
| Workload slider control | **No — workloads stay in ConfigMgr** | Yes — per-workload slider |
| Intune policy enforcement | **No — ConfigMgr manages all workloads** | Yes — for switched workloads |
| Conditional Access with Intune compliance | No (compliance workload not moved) | Yes — when Compliance workload moved |
| Windows Autopatch eligibility | No | Yes — requires 3 workloads moved (see [03](03-cocmgmt-migration-paths.md)) |
| Requires Hybrid Entra ID join | No | Typically yes (ConfigMgr enrollment path) |
| Intune license per device | No | Yes |
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/operations/co-management/01-windows-tenant-attach.md (frontmatter + Platform applicability blockquote + H1 + intro + side-by-side comparison table + SC#3 disambiguation prose + Related Resources)</name>
  <files>docs/operations/co-management/01-windows-tenant-attach.md</files>
  <read_first>
    - .planning/phases/53-co-management-operational-docs/53-CONTEXT.md (D-03 side-by-side comparison table + D-08 inline blockquote + D-12 frontmatter)
    - .planning/phases/53-co-management-operational-docs/53-RESEARCH.md (Pattern 2 verbatim Platform applicability blockquote; Pattern 5 verbatim Tenant Attach vs Full Co-Management table; Research Area 3 cross-link target verification)
    - .planning/phases/53-co-management-operational-docs/53-PATTERNS.md (Section "01-windows-tenant-attach.md" lines 218-277 — paste-ready snippets)
    - .planning/phases/53-co-management-operational-docs/53-VALIDATION.md (Per-Task Verification Map row 53-02-01: V-53-11/12/18/19)
    - docs/reference/android-capability-matrix.md (analog Cross-Platform Equivalences section lines 85-93 — paired comparison table shape)
  </read_first>
  <behavior>
    - File `docs/operations/co-management/01-windows-tenant-attach.md` exists
    - Frontmatter (D-12): `last_verified: 2026-04-27`, `review_by: 2026-06-26`, `applies_to: all`, `audience: admin`, `platform: Windows`
    - Platform applicability blockquote at TOP — IDENTICAL content to 00-overview.md (same blockquote per D-08; CD-06 permits identical multi-platform single block)
    - H1 `# Windows Tenant Attach vs Full Co-Management`
    - Side-by-side comparison table with literal columns `| Capability | Tenant Attach | Full Co-Management |` and ≥10 data rows
    - Disambiguation prose surfacing the SC#3 anchors `no workload switching` AND `workload sliders` (literal tokens; case-insensitive grep)
    - NO banned phrasings (`partially migrated`, `fully migrated`, etc.) anywhere in the file
    - NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens
  </behavior>
  <action>
Create the file `docs/operations/co-management/01-windows-tenant-attach.md` with the following content sections IN ORDER.

**Section 1 — Frontmatter (D-12 + V-53-06; copy verbatim):**

```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: all
audience: admin
platform: Windows
---
```

**Section 2 — Platform applicability blockquote at TOP (D-08 + V-53-18 + V-53-19; IDENTICAL to 00-overview.md per CD-06; paste verbatim from 53-RESEARCH.md Pattern 2):**

```markdown
> **Platform applicability:** This guide is Windows-specific (ConfigMgr + Intune co-management).
> **macOS:** No co-management equivalent — Microsoft Intune does not federate with Jamf at the
> workload-slider level. Migration path: Jamf → Intune via ABM MDM transfer (release device
> assignment in Jamf-bound MDM server in ABM, then re-assign to Intune-bound MDM server; in-use
> devices require user-initiated re-enrollment). See [macOS Enrollment Profile](../../admin-setup-macos/02-enrollment-profile.md).
> **iOS/iPadOS:** No co-management equivalent — Apple MDM is single-server-per-device. Migration
> path: MAM → MDM transition (MAM-WE app protection policies coexist with new MDM enrollment).
> See [iOS/iPadOS Admin Setup](../../admin-setup-ios/00-overview.md) and
> [iOS MAM App Protection](../../admin-setup-ios/09-mam-app-protection.md).
> **Android:** No co-management equivalent. Migration paths: legacy Device Administrator →
> Android Enterprise (DA deprecated August 2022; wipe + re-enrollment required); MAM → MDM for
> BYOD. See [Android Admin Setup](../../admin-setup-android/00-overview.md).
```

CRITICAL — verify cross-link paths:
- macOS: `../../admin-setup-macos/02-enrollment-profile.md` (SINGULAR)
- iOS MAM: `../../admin-setup-ios/09-mam-app-protection.md` (NOT 04-byod-mam-overview.md)

**Section 3 — H1 + intro paragraph:**

```markdown
# Windows Tenant Attach vs Full Co-Management

Cloud attach is an umbrella term for three ConfigMgr cloud-integration features: tenant attach,
endpoint analytics, and co-management. This guide focuses on the most commonly conflated pair —
**tenant attach** (cloud-portal device sync, no workload switching) vs **full co-management**
(workload sliders active, per-workload Intune ownership).

For the underlying co-management workload model and slider semantics, see
[Co-Management Overview](00-overview.md). For the workload migration sequence, see
[Workload Slider Migration](02-windows-workload-sliders.md).

## What Tenant Attach Is

Tenant attach is the lightest-weight ConfigMgr cloud-integration feature. ConfigMgr uploads device
records to the Intune admin center; admins can take cloud-side actions (run scripts, install
applications, view CMPivot results, retrieve BitLocker recovery keys) on those devices from the
Intune portal. **Tenant attach does NOT involve workload switching** — every workload's policy
enforcement remains in ConfigMgr. There are no workload sliders to move; there is no per-workload
Intune ownership.

## What Full Co-Management Is

Full co-management adds workload slider control on top of tenant attach capabilities. Each of the
seven co-management workloads has an independent slider (Configuration Manager / Pilot Intune /
Intune); admins can move individual workloads to Intune management while leaving others on
ConfigMgr. Full co-management is a per-workload migration tool — not a switch-everything-at-once
flag.
```

The literal tokens `no workload switching` (in "Tenant attach does NOT involve workload switching" — V-53-12 uses case-insensitive matching of "no workload switching" — adjust to ensure exact phrase appears) and `workload sliders` (in "There are no workload sliders to move" + "Full co-management adds workload slider control") are both present. To make V-53-12 deterministic, the next section adds an explicit anchor-line that uses the exact literal phrase.

**Section 4 — SC#3 anchor section (V-53-12 — exact phrase `no workload switching` AND `workload sliders` deterministically anchored):**

```markdown
## The Distinguishing Characteristic: No Workload Switching in Tenant Attach

The single distinguishing characteristic between tenant attach and full co-management is workload
slider control. Tenant attach has **no workload switching** — workloads stay in ConfigMgr. Full
co-management activates **workload sliders** that allow per-workload migration from ConfigMgr to
Intune.

This is the SC#3 anchor for L2 escalation routing: if an admin is investigating a "device shows
in Intune but Intune policies aren't applying" symptom, the disambiguation question is whether
the tenant has tenant-attach-only or full co-management. With tenant attach, Intune policies
DO NOT apply (no workload switching, no Intune policy enforcement). With full co-management,
Intune policies apply ONLY for workloads whose sliders have been moved.
```

This section deterministically anchors the literal phrases `no workload switching` and `workload sliders` per V-53-12.

**Section 5 — Side-by-side comparison table (D-03 + V-53-11; paste verbatim from RESEARCH Pattern 5):**

```markdown
## Capability Comparison

| Capability | Tenant Attach | Full Co-Management |
|------------|---------------|---------------------|
| Intune admin center device sync | Yes | Yes |
| Remote actions (from Intune portal) | Yes | Yes |
| CMPivot queries from Intune | Yes | Yes |
| Endpoint security reports (Defender) | Yes | Yes |
| BitLocker recovery key retrieval | Yes | Yes |
| Run PowerShell scripts on demand | Yes | Yes |
| Workload slider control | **No — workloads stay in ConfigMgr** | Yes — per-workload slider |
| Intune policy enforcement | **No — ConfigMgr manages all workloads** | Yes — for switched workloads |
| Conditional Access with Intune compliance | No (compliance workload not moved) | Yes — when Compliance workload moved |
| Windows Autopatch eligibility | No | Yes — requires 3 workloads moved (see [03-cocmgmt-migration-paths.md](03-cocmgmt-migration-paths.md)) |
| Requires Hybrid Entra ID join | No | Typically yes (ConfigMgr enrollment path) |
| Intune license per device | No | Yes |
| Endpoint Manager admin center surface | Yes (read-only for non-switched workloads) | Yes (read-write for switched workloads) |
| Co-management slider page in ConfigMgr console | Disabled | Active — per-workload sliders |
```

V-53-11 verifies the literal column header `| Capability | Tenant Attach | Full Co-Management |` is present. V-53-12 verifies `no workload switching` AND `workload sliders` are present in the file (the prose sections above provide the exact-phrase anchors; the table cells reinforce).

The table includes ≥10 data rows (V-53-11 may also assert ≥2 rows; this implementation provides 14 rows for completeness). The cross-link to `03-cocmgmt-migration-paths.md` in the Windows Autopatch row is supplementary (V-53-24 enforces from 00-overview.md, not from 01).

**Section 6 — When to use which:**

```markdown
## When to Use Tenant Attach Only

- Quick win: cloud admin visibility for ConfigMgr-managed devices without an Intune workload
  migration commitment
- Service desk efficiency: enable Intune-portal remote actions (run scripts, retrieve recovery
  keys) for L1/L2 staff already trained on Intune
- Pilot evaluation: prove cloud-portal viability before committing to workload migration

## When to Move to Full Co-Management

- Specific workload requires Intune-only feature (e.g., Conditional Access with Intune compliance,
  Windows Autopatch enablement, Microsoft Defender for Endpoint advanced features)
- Migration sequencing: phased ConfigMgr → Intune migration with per-workload validation before
  full Intune commitment
- Modern device management alignment: cloud-first device management across Windows + macOS + iOS +
  Android — co-management is the Windows-side path

## Migration from Tenant Attach to Full Co-Management

Tenant attach to full co-management is not a "switch on" — it requires:

1. Co-management enrollment in ConfigMgr (Administration > Cloud Services > Co-management)
2. Intune licensing for in-scope devices
3. Workload-slider planning (see [Workload Slider Migration](02-windows-workload-sliders.md) for
   the recommended low-risk-first sequence)
4. Pre-move validation per workload (Compliance evaluator agreement, WUfB ring assignment,
   Defender policy reporting healthy, Configuration Profile parity, etc.)

Tenant attach + full co-management can coexist in a single tenant. Full co-management implicitly
includes tenant attach capabilities (device sync, remote actions, etc.).
```

**Section 7 — Related resources:**

```markdown
## Related Resources

- [Co-Management Overview](00-overview.md) — workload model, three slider states, Pilot Intune
  collection-scoping
- [Workload Slider Migration](02-windows-workload-sliders.md) — low-risk-first migration sequence
  with EP HIGH-RISK callout
- [Migration Paths and Autopatch](03-cocmgmt-migration-paths.md) — Windows Autopatch
  co-management prerequisites

## External References

- [Cloud Attach Overview (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/configmgr/cloud-attach/overview)
- [Tenant Attach Overview (Microsoft Learn)](https://learn.microsoft.com/en-us/mem/intune/fundamentals/intune-add-management-of-windows-pcs)
```

After authoring, verify the following grep checks pass:

```bash
grep -c "platform: Windows" docs/operations/co-management/01-windows-tenant-attach.md   # ≥1
grep -c "> \*\*Platform applicability:\*\*" docs/operations/co-management/01-windows-tenant-attach.md   # 1
grep -c "| Capability | Tenant Attach | Full Co-Management |" docs/operations/co-management/01-windows-tenant-attach.md   # ≥1
grep -ic "no workload switching" docs/operations/co-management/01-windows-tenant-attach.md   # ≥1
grep -ic "workload sliders" docs/operations/co-management/01-windows-tenant-attach.md   # ≥1
grep -c "Jamf\|ABM MDM transfer\|Device Administrator" docs/operations/co-management/01-windows-tenant-attach.md   # ≥3
grep -c "../../admin-setup-ios/09-mam-app-protection.md" docs/operations/co-management/01-windows-tenant-attach.md   # ≥1
grep -ci "partially migrated\|fully migrated" docs/operations/co-management/01-windows-tenant-attach.md   # MUST be 0
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/co-management/01-windows-tenant-attach.md   # MUST be empty
! grep "04-byod-mam-overview.md" docs/operations/co-management/01-windows-tenant-attach.md
! grep "02-enrollment-profiles.md" docs/operations/co-management/01-windows-tenant-attach.md
```

DO NOT commit yet — atomic commit owned by 53-07 per CONTEXT D-14 + CDI-Phase53-04.
  </action>
  <verify>
    <automated>test -f docs/operations/co-management/01-windows-tenant-attach.md &amp;&amp; grep -q "| Capability | Tenant Attach | Full Co-Management |" docs/operations/co-management/01-windows-tenant-attach.md &amp;&amp; grep -qi "no workload switching" docs/operations/co-management/01-windows-tenant-attach.md &amp;&amp; grep -qi "workload sliders" docs/operations/co-management/01-windows-tenant-attach.md &amp;&amp; grep -q "> \*\*Platform applicability:\*\*" docs/operations/co-management/01-windows-tenant-attach.md &amp;&amp; ! grep -qi "partially migrated\|fully migrated" docs/operations/co-management/01-windows-tenant-attach.md &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/operations/co-management/01-windows-tenant-attach.md` exists
    - Frontmatter contains `platform: Windows`, `audience: admin`, `last_verified: 2026-04-27`, `review_by: 2026-06-26`, `applies_to: all`
    - File contains literal `> **Platform applicability:**` blockquote within first 50 lines of body (post-frontmatter)
    - Blockquote contains all 4 cross-platform analog tokens: `Jamf`, `ABM MDM transfer`, `MAM`, `Device Administrator`
    - File contains all 4 verified cross-link paths: `../../admin-setup-macos/02-enrollment-profile.md`, `../../admin-setup-ios/00-overview.md`, `../../admin-setup-ios/09-mam-app-protection.md`, `../../admin-setup-android/00-overview.md`
    - File does NOT contain `04-byod-mam-overview.md` or `02-enrollment-profiles.md` (forbidden — broken paths)
    - File contains literal column-header line `| Capability | Tenant Attach | Full Co-Management |` exactly once
    - File contains ≥10 data rows in the comparison table
    - File contains literal phrase `no workload switching` (case-insensitive grep matches; the SC#3 anchor section uses exact phrase)
    - File contains literal phrase `workload sliders` (case-insensitive grep matches)
    - File does NOT contain banned phrasings (case-insensitive grep): `partially migrated`, `partially-migrated`, `partially_migrated`, `fully migrated`, `fully-migrated`, `fully_migrated`
    - File does NOT contain any of: `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER`
  </acceptance_criteria>
  <done>01-windows-tenant-attach.md authored with all D-03/D-08/D-12 surfaces; V-53-02/06/10/11/12/18/19/25 will pass against this file; no commit (53-07 owns atomicity).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → reader | Documentation content; reader trust depends on factual correctness + SC#3 anchor clarity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02-01 | Information disclosure (operational) | Admin conflates tenant attach with full co-management; expects Intune policy enforcement on tenant-attach-only tenant | mitigate | D-03 side-by-side comparison table + dedicated SC#3 anchor section ("The Distinguishing Characteristic: No Workload Switching"). V-53-11 + V-53-12 enforce structural presence at commit time. |
| T-53-02-02 | Information disclosure (cross-platform routing) | macOS/iOS/Android admins read Windows-only tenant-attach content for cross-platform fleet | mitigate | D-08 inline `> **Platform applicability:**` blockquote at TOP. V-53-18 + V-53-19 enforce. Same pattern as 00-overview.md. |
| T-53-02-03 | Tampering (factual drift) | Tenant attach feature set expands (Microsoft adds new capabilities to cloud attach umbrella) | accept | 60-day `review_by: 2026-06-26` cycle is structural mitigation; comparison table is feature-list-truth-at-2026-04-16-Microsoft-Learn-update. |
| T-53-02-04 | Tampering (broken link) | iOS cross-link path is wrong in CONTEXT D-08 | mitigate | RESEARCH Area 3 path correction; this plan uses the verified `09-mam-app-protection.md` path. |

Severity: LOW. Documentation phase.
</threat_model>

<verification>
After Task 1 completes:

```bash
test -f docs/operations/co-management/01-windows-tenant-attach.md
echo "file-exists: $?"

# Frontmatter
head -7 docs/operations/co-management/01-windows-tenant-attach.md | grep -E "^(platform|audience|last_verified|review_by|applies_to):"

# Platform applicability blockquote in first 50 lines of body
sed -n '/^---$/,/^---$/!p' docs/operations/co-management/01-windows-tenant-attach.md | head -50 | grep -c "> \*\*Platform applicability:\*\*"
# expect 1

# Comparison table column header (V-53-11)
grep -c "| Capability | Tenant Attach | Full Co-Management |" docs/operations/co-management/01-windows-tenant-attach.md
# expect ≥1

# SC#3 anchor literals (V-53-12)
grep -ic "no workload switching" docs/operations/co-management/01-windows-tenant-attach.md   # ≥1
grep -ic "workload sliders" docs/operations/co-management/01-windows-tenant-attach.md   # ≥1

# Cross-platform analog tokens (V-53-19)
for tok in "Jamf" "ABM MDM transfer" "MAM" "Device Administrator"; do
  grep -c "$tok" docs/operations/co-management/01-windows-tenant-attach.md
done

# NEGATIVE V-53-10
grep -ic "partially migrated\|fully migrated" docs/operations/co-management/01-windows-tenant-attach.md   # MUST be 0

# Cross-link path correctness
! grep "04-byod-mam-overview.md" docs/operations/co-management/01-windows-tenant-attach.md && echo "no broken iOS path: PASS"
! grep "02-enrollment-profiles.md" docs/operations/co-management/01-windows-tenant-attach.md && echo "no plural macOS: PASS"

# TBD scan V-53-25
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/co-management/01-windows-tenant-attach.md   # empty
```

DO NOT commit — atomic commit owned by 53-07.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 53 Success Criteria + REQUIREMENTS.md COMG-03 / COMG-04:

- **SC#3** (tenant attach vs full co-management focused disambiguation): Satisfied by side-by-side comparison table + dedicated SC#3 anchor section
- **SC#4** (cross-platform analog callouts): Satisfied by `> **Platform applicability:**` blockquote at TOP
- **COMG-03** (tenant attach disambiguation): Fully covered
- **COMG-04** (cross-platform inline callouts): Inline blockquote per D-08

Plan complete when:

- [ ] File exists at `docs/operations/co-management/01-windows-tenant-attach.md`
- [ ] V-53-02/06/10/11/12/18/19/25 all pass against this file
- [ ] All cross-link paths verified
- [ ] No commit — 53-07 owns atomicity per CONTEXT D-14
</success_criteria>

<output>
After completion, create `.planning/phases/53-co-management-operational-docs/53-02-SUMMARY.md` documenting:

- File path + line count
- Sections authored (frontmatter, Platform applicability blockquote, H1 + intro, What Tenant Attach Is, What Full Co-Management Is, SC#3 Distinguishing Characteristic anchor, Capability Comparison table, When to Use Which, Migration, Related Resources)
- V-53-NN assertions this file satisfies (V-53-02, V-53-06, V-53-10, V-53-11, V-53-12, V-53-18, V-53-19, V-53-25)
- Cross-link paths used (with note: corrected from CONTEXT D-08 per RESEARCH Area 3)
- Comparison table row count
- Note: NO COMMIT — atomic commit owned by 53-07
</output>