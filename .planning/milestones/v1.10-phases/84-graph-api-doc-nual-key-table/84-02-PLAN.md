---
phase: 84-graph-api-doc-nual-key-table
plan: 02
type: execute
wave: 2
depends_on: [84-01]
files_modified:
  - docs/admin-setup-macos/08-auth-methods-deep-dive.md
  - docs/admin-setup-macos/00-overview.md
  - docs/_glossary-macos.md
autonomous: true
requirements: [NUAL-01, GRAPH-01]
must_haves:
  truths:
    - "Guide 08's NUAL section shows one consolidated table with verified MDM plist key literals NewUserAuthorizationMode, UserAuthorizationMode, and EnableCreateUserAtLogin"
    - "The New User Authorization Mode allowed-values cell includes Temporary; User Authorization Mode does NOT include Temporary"
    - "A reader sees the one-time-vs-persistent behavioral asymmetry and the Temporary-not-in-Intune-UI caveat"
    - "The v1.9 deferred-item blockquote in guide 08's NUAL section is gone (PSSO-FUT-01 closed)"
    - "00-overview.md shows guide 11 as a node in the Mermaid diagram and item 11 in the numbered list"
    - "The macOS glossary Platform SSO term links to guide 11 via the reciprocal See also convention"
  artifacts:
    - path: "docs/admin-setup-macos/08-auth-methods-deep-dive.md"
      provides: "Consolidated NUAL table with verified plist key literals; deferred blockquote removed"
      contains: "NewUserAuthorizationMode"
    - path: "docs/admin-setup-macos/00-overview.md"
      provides: "Guide 11 node in Mermaid + numbered-list item 11"
      contains: "11-graph-api-platform-credential.md"
    - path: "docs/_glossary-macos.md"
      provides: "Platform SSO term see-also extended with guide 11 link"
      contains: "11-graph-api-platform-credential.md"
  key_links:
    - from: "docs/admin-setup-macos/00-overview.md"
      to: "docs/admin-setup-macos/11-graph-api-platform-credential.md"
      via: "Mermaid node + numbered-list item"
      pattern: "11-graph-api-platform-credential\\.md"
    - from: "docs/_glossary-macos.md"
      to: "docs/admin-setup-macos/11-graph-api-platform-credential.md"
      via: "reciprocal See also link on Platform SSO term"
      pattern: "11-graph-api-platform-credential\\.md"
---

<objective>
Apply three surgical documentation edits that consolidate guide 08's NUAL table with verified MDM plist key literals and integrate the new guide 11 into the suite's navigation surfaces (overview Mermaid/list + glossary see-also).

Purpose: Satisfies NUAL-01 (verified NUAL key literals; v1.9 deferred blockquote removed → PSSO-FUT-01 closed) and SC #3 / #4 / #5 of Phase 84. Implements D-04 (one consolidated table), D-05 (must-surface literals + asymmetry + Temporary caveat + line-275 correction), D-06 (surgical removal of the deferred blockquote, no prose rewrite), D-07 (extend the existing Platform SSO glossary term with a reciprocal see-also — not a new standalone term). This plan runs in wave 2 because its cross-links resolve against `11-graph-api-platform-credential.md`, created in 84-01.

Output: Three edited Markdown files (guide 08, 00-overview, glossary).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-graph-api-doc-nual-key-table/84-CONTEXT.md
@.planning/phases/84-graph-api-doc-nual-key-table/84-RESEARCH.md
@.planning/phases/84-graph-api-doc-nual-key-table/84-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guide 08 NUAL surgical edit — consolidate table, surface caveats, remove deferred blockquote</name>
  <files>docs/admin-setup-macos/08-auth-methods-deep-dive.md</files>
  <read_first>
    - docs/admin-setup-macos/08-auth-methods-deep-dive.md (the file being edited — read the full `## New User At Login Window (NUAL)` section, lines 258-288, including the existing table at lines 272-276, the deferred blockquote at lines 278-286, and the version-history table at lines 331-334)
    - .planning/phases/84-graph-api-doc-nual-key-table/84-RESEARCH.md (verified NUAL consolidated table ~lines 306-317; Pitfall 4 — Temporary NOT in UserAuthorizationMode ~lines 404-408; Pitfall 5 — no prose rewrite ~lines 410-414)
    - .planning/phases/84-graph-api-doc-nual-key-table/84-PATTERNS.md (exact replacement table + notes block + blast-radius constraint ~lines 118-168)
  </read_first>
  <action>
    Three changes only (D-06 blast-radius discipline — no other edits to the `## New User At Login Window (NUAL)` section, including no change to the H2 title, intro prose at lines 260-269, or the section separator at line 288):
    (1) REPLACE the existing 3-column table (current lines 272-276, headers `Settings Catalog Setting | Type | Purpose`) with one consolidated 5-column table headed `Settings Catalog Display Name | MDM plist key | Type | Allowed Values | Scope`. Rows: `Enable Create User At Login` → `EnableCreateUserAtLogin` (Boolean; `true`/`false` default `false`; Scope = "Prerequisite — requires `UseSharedDeviceKeys: true` in same policy"); `New User Authorization Mode` → `NewUserAuthorizationMode` (String; `Standard`, `Admin`, `Groups`, `Temporary`; Scope = "One-time: applied at account creation only") — this CORRECTS the prior line-275 enum cell which omitted `Temporary` (D-05); `User Authorization Mode` → `UserAuthorizationMode` (String; `Standard`, `Admin`, `Groups` — NO `Temporary`; Scope = "Persistent: re-applied at each subsequent PSSO sign-in").
    (2) Immediately below the table, add a `**Notes:**` block surfacing: `Temporary` is valid in the Apple schema for `NewUserAuthorizationMode` ONLY and is NOT surfaced in the Intune Settings Catalog UI (must use a custom profile or direct plist); `UserAuthorizationMode` does NOT accept `Temporary`; and the behavioral-asymmetry example (`NewUserAuthorizationMode: Admin` + `UserAuthorizationMode: Standard` grants admin at first login then reverts to standard at every subsequent PSSO sign-in — the admin promotion is overwritten).
    (3) DELETE the entire v1.9 deferred-item blockquote currently at lines 278-286 (the `> **Deferred item -- MDM key literals ...` through the `> See \`v1.9-DEFERRED-CLEANUP.md\` ...` line). This closes PSSO-FUT-01.
    Then append one version-history row to the table at lines 331-334: `| 2026-06-23 | Phase 84 (NUAL-01): consolidated NUAL Settings Catalog table with verified MDM plist key literals; removed v1.9 deferred blockquote (PSSO-FUT-01 closed) | -- |`.
  </action>
  <acceptance_criteria>
    - The NUAL table contains the literal plist key `NewUserAuthorizationMode` and the literal plist key `UserAuthorizationMode` and `EnableCreateUserAtLogin`
    - The `New User Authorization Mode` row's allowed-values include the literal `Temporary`
    - The file contains NO occurrence of the deferred-item phrase `unconfirmed from an authoritative Settings Catalog payload schema` (deferred blockquote removed)
    - The file contains NO occurrence of `v1.9-DEFERRED-CLEANUP.md` in the NUAL section (verify: grep returns no match)
    - The file contains a `**Notes:**` block with the literal `not surfaced in the Intune Settings Catalog UI` (or equivalent `NOT surfaced in the Intune Settings Catalog UI`) and the asymmetry phrase `admin promotion is overwritten`
    - The `## New User At Login Window (NUAL)` H2 heading is still present (intro prose unchanged)
    - The version-history table contains a row with literal `Phase 84 (NUAL-01)`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'NewUserAuthorizationMode' docs/admin-setup-macos/08-auth-methods-deep-dive.md && grep -q 'UserAuthorizationMode' docs/admin-setup-macos/08-auth-methods-deep-dive.md && grep -q 'EnableCreateUserAtLogin' docs/admin-setup-macos/08-auth-methods-deep-dive.md && grep -q '## New User At Login Window (NUAL)' docs/admin-setup-macos/08-auth-methods-deep-dive.md && grep -q 'Phase 84 (NUAL-01)' docs/admin-setup-macos/08-auth-methods-deep-dive.md && ! grep -q 'unconfirmed from an authoritative Settings Catalog payload schema' docs/admin-setup-macos/08-auth-methods-deep-dive.md && ! grep -q 'v1.9-DEFERRED-CLEANUP.md' docs/admin-setup-macos/08-auth-methods-deep-dive.md</automated>
  </verify>
  <done>Guide 08's NUAL section has one consolidated 5-column table with verified plist key literals (Temporary added to NewUserAuthorizationMode only), a Notes block surfacing the Temporary-not-in-UI caveat and the one-time-vs-persistent asymmetry, the v1.9 deferred blockquote removed (PSSO-FUT-01 closed), and a Phase 84 version-history row — with no other NUAL prose changes.</done>
</task>

<task type="auto">
  <name>Task 2: 00-overview.md — add guide 11 node to Mermaid and item 11 to numbered list</name>
  <files>docs/admin-setup-macos/00-overview.md</files>
  <read_first>
    - docs/admin-setup-macos/00-overview.md (the file being edited — Mermaid diagram lines 19-32 with guide-10 node at line 31, numbered list with item 10 at line 52, version-history table lines 70-76)
    - .planning/phases/84-graph-api-doc-nual-key-table/84-PATTERNS.md (Mermaid node + numbered-list item + version-history row guidance ~lines 172-218)
  </read_first>
  <action>
    Two additions following the exact Phase 83 guide-10 precedent (do NOT alter any existing node, list item, or prose):
    (1) In the Mermaid `graph LR` block, append a new node after the current last node (`G --> J[10. Kerberos SSO<br/>Extension]` at line 31, before the closing fence): `  J --> K[11. Graph API<br/>Platform Credential]`.
    (2) After numbered-list item 10 (line 52), append item 11: `11. **[Graph API: Platform Credential Management](11-graph-api-platform-credential.md)** -- Programmatic management of macOS Secure Enclave Platform Credentials via Microsoft Graph v1.0 (`platformCredentialMethods`). Covers List / Get / Delete operations (HTTP + PowerShell SDK), permissions matrix (read vs delete; delegated vs application; national cloud), and the leaver/offboarding automation pattern with mandatory dry-run step.`
    Then append a version-history row to the table at lines 70-76: `| 2026-06-23 | Phase 84 (GRAPH-01): added guide 11 node to Mermaid diagram and item 11 to numbered list | -- |`.
  </action>
  <acceptance_criteria>
    - The Mermaid block contains the literal node text `K[11. Graph API<br/>Platform Credential]`
    - The numbered list contains item 11 linking to `11-graph-api-platform-credential.md`
    - The existing guide-10 node `J[10. Kerberos SSO<br/>Extension]` is still present (unchanged)
    - The version-history table contains a row with literal `Phase 84 (GRAPH-01)`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'K\[11. Graph API<br/>Platform Credential\]' docs/admin-setup-macos/00-overview.md && grep -q '11-graph-api-platform-credential.md' docs/admin-setup-macos/00-overview.md && grep -q 'J\[10. Kerberos SSO<br/>Extension\]' docs/admin-setup-macos/00-overview.md && grep -q 'Phase 84 (GRAPH-01)' docs/admin-setup-macos/00-overview.md</automated>
  </verify>
  <done>00-overview.md shows guide 11 as the K node off the J (guide 10) node in the Mermaid diagram and as numbered-list item 11 linking to the new guide, with the guide-10 node preserved and a Phase 84 version-history row appended.</done>
</task>

<task type="auto">
  <name>Task 3: Glossary — extend Platform SSO term see-also with reciprocal guide 11 link</name>
  <files>docs/_glossary-macos.md</files>
  <read_first>
    - docs/_glossary-macos.md (the file being edited — Platform SSO term see-also at line 128 inside the `> **Windows equivalent:**` blockquote; D-15 convention examples at lines 140/146; version-history table lines 152-161)
    - .planning/phases/84-graph-api-doc-nual-key-table/84-PATTERNS.md (D-07 see-also extension target state + convention details ~lines 222-245)
  </read_first>
  <action>
    Single surgical edit per D-07 / D-15 convention: extend the EXISTING `> See also:` line at line 128 (inside the Platform SSO term's `> **Windows equivalent:**` blockquote) by appending one more semicolon-separated entry on the SAME line — `; [Graph API: Platform Credential Management](admin-setup-macos/11-graph-api-platform-credential.md).` (the new final entry ends with `.`, replacing the prior trailing period). Do NOT create a new standalone blockquote, do NOT insert a new line, and do NOT create a new standalone glossary term (D-07 rejected option 4a). Then append a version-history row to the table at lines 152-161: `| 2026-06-23 | Phase 84 (GRAPH-02): extended Platform SSO term see-also with guide 11 cross-link | -- |`.
  </action>
  <acceptance_criteria>
    - The Platform SSO term's `> See also:` line (line ~128) contains the literal link `admin-setup-macos/11-graph-api-platform-credential.md`
    - The same See-also line still contains the prior entries (`07-platform-sso-setup.md` and `Enterprise SSO Plug-in`) — no entries dropped
    - No new standalone `### Platform Credential Graph API` term heading was added (verify: grep returns no match for `### Platform Credential Graph API`)
    - The version-history table contains a row with literal `Phase 84 (GRAPH-02)`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'admin-setup-macos/11-graph-api-platform-credential.md' docs/_glossary-macos.md && grep -q 'admin-setup-macos/07-platform-sso-setup.md' docs/_glossary-macos.md && grep -q 'Phase 84 (GRAPH-02)' docs/_glossary-macos.md && ! grep -q '### Platform Credential Graph API' docs/_glossary-macos.md</automated>
  </verify>
  <done>The macOS glossary Platform SSO term's existing See-also line is extended with a reciprocal link to guide 11 (no new line, no new standalone term), and a Phase 84 version-history row is appended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → macOS shared-device NUAL config | A reader applies a NUAL authorization mode based on the documented table |

This is a documentation-only set of surgical Markdown edits — no executable code, runtime, or input handling. The relevant threat class is documentation-accuracy: a wrong allowed-value or missing caveat leading an admin to misconfigure NUAL authorization on shared Macs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-03 | Tampering / Elevation (privilege misconfig from inaccurate docs) | Guide 08 consolidated NUAL table | mitigate | Document verified Apple-schema literals; `Temporary` shown for `NewUserAuthorizationMode` ONLY (not `UserAuthorizationMode`); surface the one-time-vs-persistent asymmetry so an admin does not accidentally grant persistent admin rights. Enforced by Task 1 acceptance criteria (N-1/D-05). |
| T-84-04 | Repudiation (broken cross-links / stale deferred note) | guide 08 deferred blockquote, 00-overview, glossary | accept | Removing the deferred blockquote and adding accurate cross-links reduces drift; broken-link risk is low and caught by the suite's link conventions. No code attack surface. |
| T-84-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package installs in this phase — documentation-only edits to existing Markdown files. No legitimacy gate applies. |
</threat_model>

<verification>
- Guide 08: one consolidated 5-column NUAL table with verified literals; `Temporary` in `NewUserAuthorizationMode` only; Notes block with the Temporary-not-in-UI caveat and the asymmetry example; deferred blockquote and `v1.9-DEFERRED-CLEANUP.md` reference removed; no other NUAL prose changed.
- 00-overview: guide 11 node (K off J) in Mermaid + numbered-list item 11; guide-10 node preserved.
- Glossary: Platform SSO term see-also extended with guide 11 link on the same line; no new standalone term.
- All three files carry a Phase 84 version-history row.
- No navigation-hub files (`docs/index.md`, `common-issues.md`, `quick-ref-l2.md`) touched (DI-1 navigation-last invariant).
</verification>

<success_criteria>
- SC #3: Guide 08 NUAL table carries verified literals (NewUserAuthorizationMode Standard/Admin/Groups/Temporary; UserAuthorizationMode Standard/Admin/Groups), EnableCreateUserAtLogin prerequisite, one-time-vs-persistent asymmetry, and the Temporary-not-in-Intune-UI note. ✓
- SC #4: v1.9 deferred-item blockquote removed; PSSO-FUT-01 closed. ✓
- SC #5: 00-overview includes a guide 11 node; glossary Platform SSO term has a see-also to guide 11. ✓ (shared with 84-01's guide-11 deliverable.)
- NUAL-01 satisfied; GRAPH-01 integration (overview node + glossary cross-link) completed.
</success_criteria>

<output>
Create `.planning/phases/84-graph-api-doc-nual-key-table/84-02-SUMMARY.md` when done.
</output>
