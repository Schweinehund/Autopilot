---
phase: 84-graph-api-doc-nual-key-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-macos/11-graph-api-platform-credential.md
autonomous: true
requirements: [GRAPH-01, GRAPH-02]
must_haves:
  truths:
    - "An admin can find the GA platformCredentialAuthenticationMethod resource (Graph v1.0) properties documented in guide 11"
    - "An admin can copy verbatim List/Get/Delete HTTP requests that use platformCredentialMethods as the URL navigation property"
    - "An admin can copy the Microsoft.Graph.Identity.SignIns PowerShell SDK cmdlets for each operation"
    - "An admin sees a mandatory [!WARNING] callout on Delete stating it severs the Entra binding and does NOT remote-erase the Secure Enclave key"
    - "An admin can read a permissions matrix distinguishing read vs delete, delegated vs application, and national-cloud availability"
    - "An admin can follow a leaver/offboarding automation pattern that includes a mandatory dry-run step"
    - "An admin understands no Create/Update operation exists (credentials are device-initiated only)"
  artifacts:
    - path: "docs/admin-setup-macos/11-graph-api-platform-credential.md"
      provides: "Hybrid suite-anchored Graph API operations-reference guide for Platform Credential management"
      contains: "platformCredentialMethods"
  key_links:
    - from: "docs/admin-setup-macos/11-graph-api-platform-credential.md"
      to: "docs/admin-setup-macos/07-platform-sso-setup.md"
      via: "Platform-gate header prerequisite link"
      pattern: "07-platform-sso-setup\\.md"
    - from: "docs/admin-setup-macos/11-graph-api-platform-credential.md"
      to: "[!WARNING] Delete callout"
      via: "destructive-operation safety callout in Delete section"
      pattern: "\\[!WARNING\\]"
---

<objective>
Author the new hybrid suite-anchored Graph API operations-reference guide `docs/admin-setup-macos/11-graph-api-platform-credential.md` for programmatic management of macOS Secure Enclave Platform Credentials via the GA `platformCredentialAuthenticationMethod` resource (Graph v1.0). Covers List / Get / Delete operations (HTTP primary + PowerShell SDK co-equal), key properties, a permissions matrix (read vs delete; delegated vs application; national-cloud), the mandatory destructive-Delete `[!WARNING]` callout, and a leaver/offboarding automation pattern with a mandatory dry-run step.

Purpose: Satisfies GRAPH-01 and GRAPH-02, and SC #1 / #2 of Phase 84. This is the phase's primary deliverable. Implements D-01 (hybrid shape), D-02 (middle automation depth), D-03 (HTTP primary, PowerShell SDK co-equal companion), D-08 (`platformCredentialMethods` URL nav-property for ALL operations), D-09 (verified permissions matrix).

Output: One new Markdown guide following the guide-10 hybrid precedent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-graph-api-doc-nual-key-table/84-CONTEXT.md
@.planning/phases/84-graph-api-doc-nual-key-table/84-RESEARCH.md
@.planning/phases/84-graph-api-doc-nual-key-table/84-PATTERNS.md
@docs/admin-setup-macos/10-kerberos-sso-extension.md
@docs/admin-setup-apv1/03-esp-policy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold guide 11 suite anchors + Resource Reference + What This Guide Is NOT</name>
  <files>docs/admin-setup-macos/11-graph-api-platform-credential.md</files>
  <read_first>
    - docs/admin-setup-macos/10-kerberos-sso-extension.md (the hybrid suite-anchor template to mirror — frontmatter lines 1-7, Platform-gate header lines 9-11, `## What This Guide Is NOT` lines 19-36, `## Prerequisites` lines 163-176, `## Verification` lines 249-252, `## See Also` lines 322-331, version-history table lines 335-337)
    - .planning/phases/84-graph-api-doc-nual-key-table/84-RESEARCH.md (Resource Properties table ~lines 363-374; Architecture Patterns Pattern 1 body sequence ~lines 138-150; anti-patterns ~lines 194-201)
    - .planning/phases/84-graph-api-doc-nual-key-table/84-PATTERNS.md (frontmatter/Platform-gate/disambiguation patterns ~lines 26-58; body section sequence ~lines 60-74)
  </read_first>
  <action>
    Create the new file with: frontmatter (`last_verified: 2026-06-23`, `review_by: 2026-09-23`, `applies_to: ADE`, `audience: admin`, `platform: macOS`) per A2 in RESEARCH. Add the Platform-gate header — first sentence: "This guide covers programmatic management of macOS Secure Enclave Platform Credentials via the Microsoft Graph API (`platformCredentialAuthenticationMethod`, Graph v1.0)."; keep the second line (Platform SSO Setup link to `07-platform-sso-setup.md`) and third line (macOS Glossary link to `../_glossary-macos.md`) per the guide-10 verbatim form. Add the H1 title and a one-paragraph intro. Then author `## What This Guide Is NOT` stating that NO Create/Update operation exists (credentials are device-initiated via Company Portal registration only) and this guide covers List/Get/Delete of existing credentials only. Then author `## Resource Reference` as a properties table using the verified properties: `id`, `displayName`, `createdDateTime`, `keyStrength` (values `normal`/`weak`/`unknown`; `normal` = Secure Enclave-backed), `platform` (values `macOS`/`windows`/`iOS`/`android`/`linux`/`unknown`), plus the `device` relationship (returned only on single GET with `?$expand=device`). Use Graph v1.0 throughout — never `/beta/`.
  </action>
  <acceptance_criteria>
    - File `docs/admin-setup-macos/11-graph-api-platform-credential.md` exists
    - File contains `applies_to: ADE` and `platform: macOS` in frontmatter
    - File contains literal string `07-platform-sso-setup.md` (Platform-gate prerequisite link)
    - File contains a `## What This Guide Is NOT` heading and the literal string `device-initiated`
    - File contains a `## Resource Reference` heading
    - File contains literal property names `keyStrength` and `createdDateTime`
    - File contains no occurrence of `/beta/` (verify: grep returns no match)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q 'applies_to: ADE' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q '## What This Guide Is NOT' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q '## Resource Reference' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q 'keyStrength' docs/admin-setup-macos/11-graph-api-platform-credential.md && ! grep -q '/beta/' docs/admin-setup-macos/11-graph-api-platform-credential.md</automated>
  </verify>
  <done>The file exists with valid suite frontmatter, the Platform-gate header, a disambiguation section noting no Create/Update, and a verified resource-properties table; no beta endpoint appears anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Author Operations (List/Get/Delete) with HTTP + PowerShell SDK, the Delete [!WARNING], and the Permissions matrix</name>
  <files>docs/admin-setup-macos/11-graph-api-platform-credential.md</files>
  <read_first>
    - docs/admin-setup-macos/11-graph-api-platform-credential.md (the file being extended — read current state)
    - .planning/phases/84-graph-api-doc-nual-key-table/84-RESEARCH.md (D-08 verified canonical HTTP request lines ~lines 219-241; PowerShell SDK cmdlets ~lines 243-271; D-08/D-09 permissions matrix ~lines 321-355)
    - docs/admin-setup-apv1/03-esp-policy.md (the `> [!WARNING]` blockquote structural pattern ~lines 97-106)
    - .planning/phases/84-graph-api-doc-nual-key-table/84-PATTERNS.md (the exact verified [!WARNING] wording ~lines 273-280)
  </read_first>
  <action>
    Append `## Operations` with three subsections. `### List Platform Credentials`: HTTP first — `GET https://graph.microsoft.com/v1.0/me/authentication/platformCredentialMethods` (self) and `GET https://graph.microsoft.com/v1.0/users/{id | userPrincipalName}/authentication/platformCredentialMethods` (admin); then the co-equal PowerShell companion `Get-MgUserAuthenticationPlatformCredentialMethod -UserId $userId`. `### Get a Platform Credential`: HTTP `GET .../platformCredentialMethods/{platformCredentialAuthenticationMethodId}` plus the `?$expand=device` variant (note: $expand is only on single GET, not List, per A3); then PowerShell `Get-MgUserAuthenticationPlatformCredentialMethod -UserId $userId -PlatformCredentialAuthenticationMethodId $methodId`. `### Delete a Platform Credential`: place the mandatory `> [!WARNING]` callout (verbatim wording from PATTERNS ~line 278) stating Delete severs the Entra binding (forces PSSO re-registration), does NOT remote-erase the device Secure Enclave key, and to test with `-WhatIf` before production; then HTTP `DELETE .../platformCredentialMethods/{platformCredentialAuthenticationMethodId}`; then PowerShell `Remove-MgUserAuthenticationPlatformCredentialMethod` with `-WhatIf` then `-Confirm`. CRITICAL (D-08): every URL uses the `platformCredentialMethods` navigation-property segment — NEVER `platformCredentialAuthenticationMethod` as a URL segment. Then append `## Permissions`: two matrix tables (Read/List+Get and Delete) with the verified least-privileged scopes — Read delegated `UserAuthMethod-PlatformCred.Read` / application `UserAuthMethod-PlatformCred.Read.All`; Delete delegated `UserAuthenticationMethod.ReadWrite` / application `UserAuthenticationMethod.ReadWrite.All`; Entra roles (Global Reader for read; Authentication Administrator / Privileged Authentication Administrator for delete — Global Reader NOT sufficient for delete); the MFA-re-prompt note; and a national-cloud availability row (Global / US Gov L4 / US Gov L5 DOD / China 21Vianet = all Yes). Use the verified matrix in RESEARCH; do NOT copy stale FEATURES-era scopes.
  </action>
  <acceptance_criteria>
    - File contains `## Operations` and the three subsection headings `### List Platform Credentials`, `### Get a Platform Credential`, `### Delete a Platform Credential`
    - File contains the literal URL segment `platformCredentialMethods` at least 3 times (List, Get, Delete)
    - File contains NO occurrence of the wrong URL form `authentication/platformCredentialAuthenticationMethod/` (verify: grep returns no match for that path segment)
    - File contains a `> [!WARNING]` callout, and within it the literal strings `severs the Entra` and `NOT remotely erased` (or `not remotely erased`)
    - File contains the literal cmdlet names `Get-MgUserAuthenticationPlatformCredentialMethod` and `Remove-MgUserAuthenticationPlatformCredentialMethod`
    - File contains `-WhatIf` (dry-run) in the Delete section
    - File contains a `## Permissions` heading and the literal scopes `UserAuthMethod-PlatformCred.Read` (read) and `UserAuthenticationMethod.ReadWrite` (delete)
    - File contains the literal string `Authentication Administrator` and a national-cloud reference (literal `21Vianet`)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '### Delete a Platform Credential' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q '\[!WARNING\]' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q 'Remove-MgUserAuthenticationPlatformCredentialMethod' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q -- '-WhatIf' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q 'UserAuthMethod-PlatformCred.Read' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q 'UserAuthenticationMethod.ReadWrite' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q '21Vianet' docs/admin-setup-macos/11-graph-api-platform-credential.md && ! grep -q 'authentication/platformCredentialAuthenticationMethod/' docs/admin-setup-macos/11-graph-api-platform-credential.md</automated>
  </verify>
  <done>List/Get/Delete are documented with HTTP-primary + PowerShell-companion examples; every URL uses `platformCredentialMethods`; the Delete `[!WARNING]` is present with the severs-binding / does-not-erase-key wording; the permissions matrix carries the verified least-privilege read and delete scopes, Entra roles, MFA note, and national-cloud row.</done>
</task>

<task type="auto">
  <name>Task 3: Author Leaver/Offboarding automation pattern + Prerequisites + Verification + See Also + version-history</name>
  <files>docs/admin-setup-macos/11-graph-api-platform-credential.md</files>
  <read_first>
    - docs/admin-setup-macos/11-graph-api-platform-credential.md (the file being completed — read current state)
    - .planning/phases/84-graph-api-doc-nual-key-table/84-RESEARCH.md (leaver/offboarding pattern ~lines 273-299; D-02 forward-link guidance + Open Question 2 ~lines 456-459; See Also targets in PATTERNS)
    - docs/admin-setup-macos/10-kerberos-sso-extension.md (Prerequisites lines 163-176, Verification lines 249-252, See Also lines 322-331, version-history lines 335-337 — structural templates)
    - .planning/phases/84-graph-api-doc-nual-key-table/84-PATTERNS.md (Prerequisites/Verification/See Also/version-history guidance ~lines 76-114)
  </read_first>
  <action>
    Append `## Leaver / Offboarding Automation Pattern` (D-02 middle depth): the get→review→confirm→delete flow with a MANDATORY dry-run step — enumerate via `Get-MgUserAuthenticationPlatformCredentialMethod`, loop with `Remove-MgUserAuthenticationPlatformCredentialMethod ... -WhatIf` for review, then a confirm step (re-run without `-WhatIf` only after reviewing). Do NOT include bulk-audit / enumerate-users-with-0-registrations examples (Phase 85 RUN-02). Add a one-line forward cross-link to Phase 85 L2 runbook #29 (`29-macos-graph-credential-investigation.md`) here in the automation section per Open Question 2. Then append `## Prerequisites`: (1) Platform SSO deployed (guide 07), (2) Secure Enclave auth method in use (guide 08), (3) appropriate Graph permissions granted to the calling identity, (4) `Install-Module Microsoft.Graph.Identity.SignIns` for the PowerShell examples. Then `## Verification`: confirm a credential exists via the read-only `Get-MgUserAuthenticationPlatformCredentialMethod` before any Delete. Then `## See Also` linking guide 07, guide 08 (Secure Enclave section), `../_glossary-macos.md#platform-sso`, `../_glossary-macos.md#secure-enclave`, the Microsoft Learn resource type page, and the List/Get/Delete operation pages. Finally a bare version-history table (no H2 heading, preceded by `---`): row `| 2026-06-23 | Phase 84 (GRAPH-01/GRAPH-02): initial Graph API Platform Credential guide | -- |`.
  </action>
  <acceptance_criteria>
    - File contains `## Leaver / Offboarding Automation Pattern` and the literal `29-macos-graph-credential-investigation.md` forward cross-link
    - The automation section contains `-WhatIf` (mandatory dry-run)
    - File contains NO bulk-audit example (verify: grep returns no match for `enumerate-users-with-0` and no `0 registrations` reporting example)
    - File contains `## Prerequisites` and the literal `Install-Module Microsoft.Graph.Identity.SignIns`
    - File contains `## Verification` and `## See Also`
    - `## See Also` contains links to `07-platform-sso-setup.md` and `08-auth-methods-deep-dive.md`
    - File ends with a version-history table row containing literal `Phase 84 (GRAPH-01/GRAPH-02)`
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Leaver / Offboarding Automation Pattern' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q '29-macos-graph-credential-investigation.md' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q 'Install-Module Microsoft.Graph.Identity.SignIns' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q '## Verification' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q '## See Also' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q '08-auth-methods-deep-dive.md' docs/admin-setup-macos/11-graph-api-platform-credential.md && grep -q 'Phase 84 (GRAPH-01/GRAPH-02)' docs/admin-setup-macos/11-graph-api-platform-credential.md</automated>
  </verify>
  <done>The guide closes with the dry-run-gated leaver/offboarding pattern (forward-linking runbook #29, no bulk-audit), Prerequisites, read-only Verification, a complete See Also, and the Phase 84 version-history row — all suite anchors present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → production Entra/Graph | A reader copies a documented Delete example and runs it against live production users |

This is a documentation-only deliverable — a new Markdown guide. There is no executable code, runtime, user-input handling, or attack surface in the artifact itself. The only relevant threat class is reader-safety: the destructive Graph Delete operation being copied and run without the dry-run/warning.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Tampering (data destruction by reader) | `### Delete a Platform Credential` examples | mitigate | Mandatory `> [!WARNING]` callout stating Delete severs the Entra binding (forces PSSO re-registration) and does NOT remote-erase the Secure Enclave key; leaver pattern requires a `-WhatIf` dry-run step before live deletion (GRAPH-02 / G-2). Enforced by acceptance criteria in Tasks 2 and 3. |
| T-84-02 | Information disclosure (over-privilege guidance) | `## Permissions` matrix | mitigate | Document least-privilege read (`UserAuthMethod-PlatformCred.Read`) and delete (`UserAuthenticationMethod.ReadWrite`) scopes; note Global Reader is NOT sufficient for delete. Steers readers away from granting excessive Graph permissions (GRAPH-02 / G-3). |
| T-84-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package installs in this phase — documentation-only. The only install referenced is `Install-Module Microsoft.Graph.Identity.SignIns`, a first-party Microsoft Graph PowerShell module named in the official Microsoft Learn reference (not introduced as a project dependency). No legitimacy gate applies. |
</threat_model>

<verification>
- `docs/admin-setup-macos/11-graph-api-platform-credential.md` exists and renders as a valid suite guide (frontmatter, Platform-gate, suite anchors).
- All Graph URLs use `platformCredentialMethods`; none use `authentication/platformCredentialAuthenticationMethod/`; no `/beta/`.
- Delete section carries the `[!WARNING]` callout; automation pattern carries a `-WhatIf` dry-run.
- Permissions matrix carries verified least-privilege read and delete scopes, Entra roles, MFA note, national-cloud row.
- No bulk-audit examples present (deferred to Phase 85 RUN-02); a one-line forward link to runbook #29 exists.
</verification>

<success_criteria>
- SC #1: Guide 11 exists with GA `platformCredentialAuthenticationMethod` resource (v1.0), List/Get/Delete (HTTP + PowerShell SDK), key properties, and a read-vs-delete / delegated-vs-application permissions matrix. ✓
- SC #2: Mandatory `[!WARNING]` callout (severs Entra binding, does NOT remote-erase Secure Enclave key); automation examples include a dry-run step. ✓
- GRAPH-01 and GRAPH-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/84-graph-api-doc-nual-key-table/84-01-SUMMARY.md` when done.
</output>
