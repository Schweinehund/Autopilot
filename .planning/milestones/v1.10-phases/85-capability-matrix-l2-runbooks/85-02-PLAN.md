---
phase: 85-capability-matrix-l2-runbooks
plan: 02
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - docs/reference/4-platform-capability-comparison.md
  - docs/_glossary.md
  - scripts/validation/check-phase-63.mjs
autonomous: true
requirements: [REF-02]
must_haves:
  truths:
    - "The macOS glossary (_glossary-macos.md) is confirmed to already contain the Kerberos SSO Extension term (line 142) and the Platform-SSO→guide-11 see-also (line 128) — verify-not-recreate, no duplicate term authored (D-07)"
    - "_glossary.md Entra ID SSO term see-also line is extended with reciprocal Kerberos SSO Extension + Graph Platform Credential pointers (D-09), the existing macOS-SSO-plugin pointer preserved"
    - "4-platform-capability-comparison.md ## Single Sign-On macOS cell references the new Kerberos capability link-not-copy at #authentication (D-08)"
    - "check-phase-63.mjs V-63-09 BASELINE equals the post-edit git hash-object of 4-platform-capability-comparison.md, updated in the SAME commit as the comparison-doc edit (separate commit from the 85-01 V-63-08 bump)"
    - "node scripts/validation/check-phase-63.mjs reports V-63-09 PASS (not SKIPPED, not FAIL)"
  artifacts:
    - path: "docs/reference/4-platform-capability-comparison.md"
      provides: "macOS Single Sign-On cell updated to reference Kerberos capability"
      contains: "macos-capability-matrix.md#authentication"
    - path: "docs/_glossary.md"
      provides: "Reciprocal Kerberos + Platform Credential see-also on Entra ID SSO term"
      contains: "_glossary-macos.md#kerberos-sso-extension"
    - path: "scripts/validation/check-phase-63.mjs"
      provides: "V-63-09 BASELINE updated to post-edit comparison-doc hash"
      contains: "const BASELINE"
  key_links:
    - from: "docs/_glossary.md"
      to: "docs/_glossary-macos.md#kerberos-sso-extension"
      via: "reciprocal see-also pointer on Entra ID SSO term"
      pattern: "_glossary-macos\\.md#kerberos-sso-extension"
    - from: "docs/reference/4-platform-capability-comparison.md"
      to: "docs/reference/macos-capability-matrix.md#authentication"
      via: "macOS SSO cell link-not-copy"
      pattern: "macos-capability-matrix\\.md#authentication"
    - from: "scripts/validation/check-phase-63.mjs"
      to: "docs/reference/4-platform-capability-comparison.md"
      via: "V-63-09 git hash-object baseline guard"
      pattern: "const BASELINE = '[0-9a-f]{40}'"
---

<objective>
Verify the macOS glossary terms already exist (verify-not-recreate, D-07), add reciprocal Kerberos + Platform-Credential see-also pointers to the Windows `_glossary.md` Entra ID SSO term (D-09), and update the `4-platform-capability-comparison.md` macOS Single Sign-On cell link-not-copy (D-08) — committed atomically with the `check-phase-63.mjs` V-63-09 baseline bump.

Purpose: Satisfies REF-02 and the SC#3 cross-link half. The Phase 85 macOS-glossary work is verification only — both required terms shipped in P83/P84. The reciprocal Windows see-also follows the established `_glossary.md` pattern (the Entra ID SSO term already carries a macOS-Enterprise-SSO-plugin reciprocal). The comparison-doc edit triggers V-63-09 — the **easy-to-miss** harness coupling that, if missed, creates a NEW chain FAIL Phase 86 does NOT cover.

Output: macOS-glossary verification recorded in the SUMMARY; a comparison-doc edit + V-63-09-bump atomic commit; the Windows glossary see-also extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-capability-matrix-l2-runbooks/85-CONTEXT.md
@.planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md
@.planning/phases/85-capability-matrix-l2-runbooks/85-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify macOS glossary terms (verify-not-recreate, D-07) + add reciprocal Windows _glossary.md see-also (D-09)</name>
  <files>docs/_glossary.md</files>
  <read_first>
    - docs/_glossary-macos.md (VERIFY ONLY — confirm Kerberos SSO Extension term at ~line 142, Platform-SSO→guide-11 see-also at ~line 128, Alphabetical Index entry at ~line 17; do NOT edit this file)
    - docs/_glossary.md (Entra ID SSO term at lines 158-162 — the existing `> See also:` line at 162 is the extension host; note it already carries a reciprocal macOS-Enterprise-SSO-plugin pointer)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-PATTERNS.md (§ _glossary.md — extension pattern at lines 457-476; § Semicolon-Separated See-Also Blockquote at lines 516-521)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md (Finding 5 — both macOS terms verified present; suggested reciprocal see-also wording at lines 421-425)
  </read_first>
  <action>
    FIRST verify (do not edit) `docs/_glossary-macos.md`: confirm the `### Kerberos SSO Extension` term exists with its `> See also:` line, the Platform SSO term's see-also already points to `admin-setup-macos/11-graph-api-platform-credential.md`, and the Alphabetical Index lists `[Kerberos SSO Extension](#kerberos-sso-extension)`. Record these confirmations in the SUMMARY. Per D-07 and Pitfall 5, do NOT add, modify, or duplicate any term in `_glossary-macos.md`. If any term is unexpectedly ABSENT, STOP and surface the discrepancy — do not author a duplicate.

    THEN edit `docs/_glossary.md` ONLY at the Entra ID SSO term's existing `> See also:` line (line ~162). EXTEND that single semicolon-separated line — do not replace it and do not add a sibling blockquote. Preserve the existing `[Enterprise SSO Plug-in](_glossary-macos.md#enterprise-sso-plug-in) (...)` entry verbatim, then append two semicolon-separated entries: a `[Kerberos SSO Extension](_glossary-macos.md#kerberos-sso-extension)` pointer with a short parenthetical (e.g. "macOS on-premises Kerberos TGT extension, coexisting with PSSO"), and a `[Graph API: Platform Credential Management](admin-setup-macos/11-graph-api-platform-credential.md)` pointer with a short parenthetical (e.g. "programmatic management of macOS Platform Credentials"). Exact wording is Claude's Discretion. End the line with a period. `_glossary.md` has NO harness guard — it may be committed in the same commit as Task 2's comparison-doc edit or in its own commit.
  </action>
  <verify>
    <automated>grep -q "_glossary-macos.md#kerberos-sso-extension" docs/_glossary.md && grep -q "11-graph-api-platform-credential.md" docs/_glossary.md && grep -q "enterprise-sso-plug-in" docs/_glossary.md && grep -qi "kerberos sso extension" docs/_glossary-macos.md && echo GLOSSARY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/_glossary-macos.md` is unchanged (no new term added; `git diff --quiet docs/_glossary-macos.md` is clean).
    - `docs/_glossary.md` Entra ID SSO see-also line contains all three pointers: the original `_glossary-macos.md#enterprise-sso-plug-in`, the new `_glossary-macos.md#kerberos-sso-extension`, and the new `admin-setup-macos/11-graph-api-platform-credential.md`.
    - The pointers are on the SINGLE existing semicolon-separated `> See also:` line (one blockquote, not two), ending with a period.
    - SUMMARY records the verify-not-recreate confirmation for the two macOS glossary terms.
  </acceptance_criteria>
  <done>macOS glossary verified present (no edit); Windows _glossary.md Entra ID SSO term carries reciprocal Kerberos + Platform-Credential see-also pointers on its existing line.</done>
</task>

<task type="auto">
  <name>Task 2: Update 4-platform-comparison macOS SSO cell link-not-copy (D-08) + atomic V-63-09 BASELINE bump</name>
  <files>docs/reference/4-platform-capability-comparison.md, scripts/validation/check-phase-63.mjs</files>
  <read_first>
    - docs/reference/4-platform-capability-comparison.md (## Single Sign-On section lines 97-103; macOS cell at line 101 already links to `macos-capability-matrix.md#authentication`)
    - scripts/validation/check-phase-63.mjs (V-63-09 block lines 223-242; BASELINE constant at line 230 — confirm line 209 / V-63-08 was set by Plan 85-01 and is NOT re-touched here)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-PATTERNS.md (§ 4-platform-capability-comparison.md — link-not-copy edit at lines 432-453; § check-phase-63.mjs — change-line-230-only rule at lines 195-210; coupling rule at lines 209-210)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md (Finding 6 — V-63-09 is the easy-to-miss coupling; Pitfall 2; pre-edit hash 58b958639294c2bdb4082aecd9967a15d35aff2a; Pitfall 1 — do NOT use the Phase-63 original f25ff51a...)
  </read_first>
  <action>
    Edit the macOS cell in the `## Single Sign-On` table row (line 101) of `docs/reference/4-platform-capability-comparison.md` link-not-copy (D-08): add minimal prose referencing the new Kerberos capability now covered under `#authentication`, keeping the existing `[matrix](macos-capability-matrix.md#authentication)` link target (it already points at the section that now holds the Kerberos rows). Example: change the cell to read `Supported (macOS 14+, incl. Kerberos SSO Extension) — [matrix](macos-capability-matrix.md#authentication)`. Exact wording is Claude's Discretion; do NOT copy/duplicate matrix prose into this cell (link-not-copy). Do not touch the Windows/iOS/Android/Linux cells or the scope-note blockquote.

    Then perform the atomic V-63-09 bump: run `git hash-object docs/reference/4-platform-capability-comparison.md` against the edited file and write the resulting 40-char hash as the `const BASELINE` value at `scripts/validation/check-phase-63.mjs` line 230 (replacing `f25ff51a14b7feac46611c4c0511ed5c074ce03f`). Update the two textual occurrences of the old hash in the V-63-09 comment (line ~223) and `name:` string (line ~225) for consistency. Do NOT touch the V-63-08 block (lines 202-221) — that BASELINE belongs to Plan 85-01's matrix commit (per-file coupling; bumping V-63-08 here would be the prohibited cross-coupling anti-pattern).

    Stage `docs/reference/4-platform-capability-comparison.md` + `scripts/validation/check-phase-63.mjs` together (optionally also `docs/_glossary.md` from Task 1) and commit as one atomic commit, SEPARATE from the Plan 85-01 commit. This is the MANDATORY easy-to-miss V-63-09 bump.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-63.mjs 2>&1 | grep "V-63-09" | grep -qi "PASS" && grep -qi "kerberos" docs/reference/4-platform-capability-comparison.md && grep -q "macos-capability-matrix.md#authentication" docs/reference/4-platform-capability-comparison.md && test "$(git hash-object docs/reference/4-platform-capability-comparison.md)" = "$(grep -A8 'V-63-09' scripts/validation/check-phase-63.mjs | grep -oE "BASELINE = '[0-9a-f]{40}'" | grep -oE '[0-9a-f]{40}')" && echo V0909_MATCH</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reference/4-platform-capability-comparison.md` macOS Single Sign-On cell contains a Kerberos reference AND still contains the link `macos-capability-matrix.md#authentication`.
    - No matrix prose was copied into the cell (link-not-copy honored).
    - `scripts/validation/check-phase-63.mjs` line 230 BASELINE equals `git hash-object docs/reference/4-platform-capability-comparison.md` of the edited file, and is NOT `f25ff51a14b7feac46611c4c0511ed5c074ce03f`.
    - V-63-08 BASELINE (line 209) is unchanged by this plan (it was set by 85-01).
    - The comparison edit and V-63-09 bump are in ONE commit, separate from the 85-01 commit.
  </acceptance_criteria>
  <done>The comparison-doc macOS SSO cell references the Kerberos capability link-not-copy, and V-63-09 BASELINE matches the new comparison-doc hash, committed atomically.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Assert V-63-09 PASS via harness run (and V-63-08 still PASS)</name>
  <files>scripts/validation/check-phase-63.mjs</files>
  <read_first>
    - scripts/validation/check-phase-63.mjs (V-63-09 block lines 223-242 — the catch returns {skipped:true} only when `git hash-object` is unavailable; in this git repo a PASS must be a real PASS)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md (Finding 2 / Pitfall 2 — a missed V-63-09 bump introduces a new chain FAIL Phase 86 does NOT cover; CRITICAL WARNING at Finding 2)
  </read_first>
  <action>
    Run `node scripts/validation/check-phase-63.mjs` and inspect both the V-63-09 and V-63-08 lines. V-63-09 MUST report PASS (detail "4-platform-capability-comparison.md blob hash matches baseline <hash>"), NOT FAIL and NOT skipped. V-63-08 MUST still report PASS (regression check — Plan 85-01's fix must not have been disturbed). If V-63-09 FAILs, the BASELINE at line 230 does not match the post-edit `git hash-object` — recompute and amend Task 2's commit, then re-run. This is a [BLOCKING] gate: the easy-to-miss V-63-09 coupling is only satisfied when the harness confirms it green. Do not consider the plan complete until both V-63-08 and V-63-09 are PASS.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-63.mjs 2>&1 | grep "V-63-09" | grep -qi "PASS" && node scripts/validation/check-phase-63.mjs 2>&1 | grep "V-63-09" | grep -viq "skip" && node scripts/validation/check-phase-63.mjs 2>&1 | grep "V-63-08" | grep -qi "PASS" && echo V0809_BOTH_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-63.mjs` prints a V-63-09 line containing PASS, genuine (not `skipped`, detail does not contain "git hash-object not available").
    - The same run prints V-63-08 PASS (no regression from Plan 85-01).
    - V-63-09 detail reads "blob hash matches baseline" with the new post-edit comparison-doc hash.
  </acceptance_criteria>
  <done>The harness confirms both V-63-09 and V-63-08 PASS; the easy-to-miss comparison-doc atomic bump is validated.</done>
</task>

</tasks>

<threat_model>
No application attack surface — documentation + harness baseline edits only. No executable application code, no user input, no network surface. The only executable change is a 40-char hex string constant in a validation script. No STRIDE register applies (documentation-only phase per RESEARCH.md Security Domain).
</threat_model>

<verification>
- `node scripts/validation/check-phase-63.mjs` reports V-63-09 PASS AND V-63-08 PASS (Task 3).
- `_glossary-macos.md` unchanged (verify-not-recreate); `_glossary.md` Entra ID SSO see-also extended with both new pointers.
- `4-platform-capability-comparison.md` macOS SSO cell references Kerberos link-not-copy.
- `check-phase-63.mjs` line 230 BASELINE == `git hash-object` of edited comparison doc; line 209 (V-63-08) untouched by this plan.
</verification>

<success_criteria>
- SC#3 (cross-link half): macOS glossary terms confirmed present (verify-not-recreate); `4-platform-capability-comparison.md` macOS cell updated link-not-copy for the new Kerberos row; reciprocal Windows `_glossary.md` see-also added.
- V-63-09 blob hash updated in the same atomic commit as the comparison-doc edit; V-63-09 PASS.
- REF-02 fully satisfied.
</success_criteria>

<output>
Create `.planning/phases/85-capability-matrix-l2-runbooks/85-02-SUMMARY.md` when done.
</output>
