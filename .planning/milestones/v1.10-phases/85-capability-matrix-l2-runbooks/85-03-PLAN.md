---
phase: 85-capability-matrix-l2-runbooks
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l2-runbooks/28-macos-kerberos-sso-investigation.md
  - docs/l2-runbooks/29-macos-graph-credential-investigation.md
  - docs/l2-runbooks/00-index.md
autonomous: true
requirements: [RUN-01, RUN-02]
must_haves:
  truths:
    - "docs/l2-runbooks/28-macos-kerberos-sso-investigation.md exists, follows the #27 template (frontmatter, platform gate, ## Context, Tracks with numbered ### Step N, Escalation Packet, Related Resources, Version History), covers ticket-acquisition triage, realm/KDC reachability, TGT verification via `app-sso platform -s` + `klist`, and log collection — with NO 'from L1 escalation' routing block (D-05)"
    - "#28 uses ONLY the locked diagnostic pair `app-sso platform -s` (interpreting tgt_ad vs tgt_cloud, with the cosmetic 'Not signed in' note) + bare `klist`; never `app-sso diagnose` and never `klist -v`"
    - "docs/l2-runbooks/29-macos-graph-credential-investigation.md exists, follows the #27 template as single-track numbered steps, covers enumerate/verify, delete-and-re-register (with mandatory [!WARNING]), and permission/role troubleshooting; bulk-audit is a brief forward/out-of-scope note only (D-06)"
    - "#29 Graph URLs use the `platformCredentialMethods` nav property (v1.0); permissions/endpoints/[!WARNING] semantics are link-not-copy from guide 11 (11-graph-api-platform-credential.md)"
    - "docs/l2-runbooks/00-index.md macOS ADE Runbooks 'When to Use' table gains rows for #28 and #29 (prerequisite = 10-macos-log-collection.md); the macOS L1 Escalation Mapping table is NOT modified (D-05)"
  artifacts:
    - path: "docs/l2-runbooks/28-macos-kerberos-sso-investigation.md"
      provides: "L2 Kerberos SSO investigation runbook (RUN-01, SC#4)"
      contains: "app-sso platform -s"
      min_lines: 60
    - path: "docs/l2-runbooks/29-macos-graph-credential-investigation.md"
      provides: "L2 Graph Platform Credential investigation runbook (RUN-02, SC#5)"
      contains: "platformCredentialMethods"
      min_lines: 60
    - path: "docs/l2-runbooks/00-index.md"
      provides: "macOS ADE Runbooks table rows for #28 and #29"
      contains: "28-macos-kerberos-sso-investigation.md"
  key_links:
    - from: "docs/l2-runbooks/28-macos-kerberos-sso-investigation.md"
      to: "docs/admin-setup-macos/10-kerberos-sso-extension.md"
      via: "Related Resources / Context link to the Kerberos guide"
      pattern: "10-kerberos-sso-extension\\.md"
    - from: "docs/l2-runbooks/29-macos-graph-credential-investigation.md"
      to: "docs/admin-setup-macos/11-graph-api-platform-credential.md"
      via: "link-not-copy reference to guide 11 endpoints/permissions"
      pattern: "11-graph-api-platform-credential\\.md"
    - from: "docs/l2-runbooks/00-index.md"
      to: "docs/l2-runbooks/29-macos-graph-credential-investigation.md"
      via: "When-to-Use table row"
      pattern: "29-macos-graph-credential-investigation\\.md"
---

<objective>
Author the two new L2 runbooks — `28-macos-kerberos-sso-investigation.md` (Kerberos SSO Extension) and `29-macos-graph-credential-investigation.md` (Graph Platform Credential) — using the `27-macos-sso-investigation.md` template, then extend the `00-index.md` macOS ADE Runbooks "When to Use" table with rows for both.

Purpose: Satisfies RUN-01/RUN-02 and SC#4/SC#5. These give L2 engineers structured investigation paths for the Kerberos and Graph-credential failure classes. Both runbooks reuse the locked diagnostics (`app-sso platform -s` + `klist`, never `app-sso diagnose`/`klist -v`) and the locked Graph nav property (`platformCredentialMethods`, v1.0), and both link-not-copy back to guides 10/11 rather than re-deriving content.

Output: two new runbook files + an extended internal index. NO harness guards touch these files; this plan is independent of 85-01/85-02 (no shared files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-capability-matrix-l2-runbooks/85-CONTEXT.md
@.planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md
@.planning/phases/85-capability-matrix-l2-runbooks/85-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author runbook #28 — macOS Kerberos SSO investigation (RUN-01, SC#4, D-04/D-05)</name>
  <files>docs/l2-runbooks/28-macos-kerberos-sso-investigation.md</files>
  <read_first>
    - docs/l2-runbooks/27-macos-sso-investigation.md (structural template — read end-to-end: frontmatter lines 1-7, platform gate line 9, ## Context lines 13-24 including the L1-routing block at 19-24 which is NOT replicated, Track A/B + ### Step N shape, Escalation Packet, Related Resources, Version History; also Step 3 sysdiagnose log-collection procedure to link-not-copy, and the line-106 anti-`app-sso diagnose` note)
    - docs/admin-setup-macos/10-kerberos-sso-extension.md (link target + source of fact: identifier `com.apple.AppSSOKerberos.KerberosExtension`, Type: Credential, `usePlatformSSOTGT`, macOS 14.6+ floor, realm/KDC details)
    - docs/l2-runbooks/10-macos-log-collection.md (prerequisite — reference it; do not copy)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-PATTERNS.md (§ 28-... runbook — frontmatter/platform-gate/Context/Track/Step/Escalation/Related/Version patterns at lines 214-338; § Critical Anti-Patterns)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md (Finding 3 #28 Track layout at lines 321-337; Finding 7 #28 diagnostics at lines 460-473; Pitfalls 3/4/6)
  </read_first>
  <action>
    Create `docs/l2-runbooks/28-macos-kerberos-sso-investigation.md` mirroring the #27 anatomy. Frontmatter: `last_verified: 2026-06-23`, `review_by: 2026-09-23`, `applies_to: ADE`, `audience: L2`, `platform: macOS`. Platform-gate blockquote adapted for Kerberos scope, cross-linking #27 (macOS Platform SSO Investigation) and `00-index.md#macos-ade-runbooks`. Title H1 (e.g. `# macOS Kerberos SSO Extension Investigation`).

    `## Context`: describe the failure classes (TGT not acquired, realm/KDC unreachable, PSSO-TGT integration via `usePlatformSSOTGT`), state the log-collection prerequisite linking `10-macos-log-collection.md`, and include the no-L1-escalation sentinel sentence: "No L1 escalation exists for this failure class — begin at Track A Step 1." DO NOT include a "From L1 escalation?" routing block citing L1 runbook numbers (D-05, Pitfall 4 — no L1 Kerberos runbook exists).

    Author Track A (Ticket Acquisition Failure / TGT not issued) and Track B (PSSO-TGT Integration via `usePlatformSSOTGT`) with numbered `### Step N` headings. Suggested coverage (split boundaries are Claude's Discretion): Track A — confirm registration/TGT state with `app-sso platform -s` (interpret `tgt_ad` = on-prem AD TGT, `tgt_cloud` = Entra TGT) including the cosmetic "Not signed in" menu-bar note so a working PSSO-TGT deployment isn't misread as failed; realm/KDC reachability (`klist` to show cached tickets, realm DNS resolution); log collection (reuse #27 Step 3 sysdiagnose procedure link-not-copy). Track B — verify macOS 14.6+ floor, verify `usePlatformSSOTGT: true` in the Kerberos profile, confirm PSSO is REGISTERED first.

    DIAGNOSTIC COMMAND DISCIPLINE (LOCKED): use ONLY `app-sso platform -s` and bare `klist`. NEVER write `app-sso diagnose` anywhere (Pitfall 3 — unverified subcommand). NEVER write `klist -v` (Pitfall 6 — version-unstable output). 

    Close with `## Microsoft Support Escalation Packet` (full `app-sso platform -s` JSON, sysdiagnose with AppSSO debug logging, Company Portal log incident ID, Intune device config status, macOS + Company Portal versions), `## Related Resources` (link `10-macos-log-collection.md`, guide 10, and #27/#35/#36 for shared-symptom tickets), and `## Version History` with one row: `| 2026-06-23 | Initial version — macOS Kerberos SSO Extension L2 investigation (RUN-01) | -- |`.
  </action>
  <verify>
    <automated>test -f docs/l2-runbooks/28-macos-kerberos-sso-investigation.md && grep -q "app-sso platform -s" docs/l2-runbooks/28-macos-kerberos-sso-investigation.md && grep -q "klist" docs/l2-runbooks/28-macos-kerberos-sso-investigation.md && ! grep -q "app-sso diagnose" docs/l2-runbooks/28-macos-kerberos-sso-investigation.md && ! grep -q "klist -v" docs/l2-runbooks/28-macos-kerberos-sso-investigation.md && ! grep -qi "From L1 escalation" docs/l2-runbooks/28-macos-kerberos-sso-investigation.md && grep -q "10-kerberos-sso-extension.md" docs/l2-runbooks/28-macos-kerberos-sso-investigation.md && echo RUNBOOK28_OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists with #27-style frontmatter (5 keys), platform-gate blockquote, `## Context`, Track A + Track B with `### Step N` headings, `## Microsoft Support Escalation Packet`, `## Related Resources`, `## Version History`.
    - Contains `app-sso platform -s` AND bare `klist`; both `tgt_ad` and `tgt_cloud` are interpreted; the cosmetic "Not signed in" note is present.
    - Does NOT contain the strings `app-sso diagnose` or `klist -v`.
    - Does NOT contain a "From L1 escalation?" routing block; DOES contain the no-L1-escalation sentinel sentence.
    - Links to `10-macos-log-collection.md` (prerequisite) and `../admin-setup-macos/10-kerberos-sso-extension.md` (guide 10).
    - Covers all four SC#4 elements: ticket-acquisition triage, realm/KDC reachability, TGT verification, log collection.
  </acceptance_criteria>
  <done>Runbook #28 exists, follows the #27 template, uses only locked Kerberos diagnostics, omits the L1-routing block, and covers all SC#4 elements.</done>
</task>

<task type="auto">
  <name>Task 2: Author runbook #29 — macOS Graph Platform Credential investigation (RUN-02, SC#5, D-04/D-05/D-06)</name>
  <files>docs/l2-runbooks/29-macos-graph-credential-investigation.md</files>
  <read_first>
    - docs/l2-runbooks/27-macos-sso-investigation.md (structural template — frontmatter, platform gate, ## Context, ### Step N shape, Escalation Packet, Related Resources, Version History; #29 uses single-track numbered steps, not Track A/B)
    - docs/admin-setup-macos/11-graph-api-platform-credential.md (link target + source of truth: endpoint shapes, Permissions section, [!WARNING] delete semantics — these are link-not-copy targets, NOT to be re-derived in the runbook)
    - docs/l2-runbooks/10-macos-log-collection.md (prerequisite — reference, do not copy)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-PATTERNS.md (§ 29-... runbook — single-track numbered-steps structure at lines 341-403; Graph endpoint shapes + mandatory [!WARNING] block + link-not-copy rule)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md (Finding 3 #29 step layout at lines 339-350; Finding 7 #29 Graph endpoints/permissions/[!WARNING] at lines 475-509; D-06 bulk-audit forward-note-only)
  </read_first>
  <action>
    Create `docs/l2-runbooks/29-macos-graph-credential-investigation.md` mirroring the #27 anatomy but as a SINGLE track of numbered `### Step N` headings (no Track A/B — the enumerate→verify→delete→re-register→troubleshoot operations are sequential). Same frontmatter (5 keys, dates 2026-06-23 / 2026-09-23). Platform-gate blockquote adapted for Graph Platform Credential scope, cross-linking #27 and `00-index.md#macos-ade-runbooks`. Title H1 (e.g. `# macOS Graph Platform Credential Investigation`).

    `## Context`: failure classes (Platform Credential not appearing in Graph, delete-and-re-register flow, permission/scope errors), log-collection prerequisite link, and the no-L1-escalation sentinel: "No L1 escalation exists for this failure class — begin at Step 1." NO "From L1 escalation?" block (D-05).

    Numbered steps (ordering is Claude's Discretion): Step 1 Enumerate Platform Credentials — `GET https://graph.microsoft.com/v1.0/users/{id | userPrincipalName}/authentication/platformCredentialMethods`; Step 2 Verify credential properties — `GET .../platformCredentialMethods/{platformCredentialAuthenticationMethodId}?$expand=device`; Step 3 Delete credential — `DELETE .../platformCredentialMethods/{platformCredentialAuthenticationMethodId}` preceded by a mandatory `> [!WARNING]` block stating that deletion severs the Entra ID binding and forces PSSO re-registration but does NOT remotely erase the Secure Enclave key (test with `-WhatIf` / dry-run first); Step 4 Verify re-registration — user sees "Registration Required"; confirm via `app-sso platform -s`; Step 5 Permission/role troubleshooting — distinguish read vs delete scopes, delegated vs application, national-cloud, minimum Entra role — LINK-NOT-COPY to guide 11 §Permissions (do NOT inline the permissions table, Pitfall in Don't-Hand-Roll).

    NAV-PROPERTY DISCIPLINE (LOCKED): every Graph URL uses the `platformCredentialMethods` nav property in the URL path. NEVER use `platformCredentialAuthenticationMethod` as a URL path segment (it is the resource TYPE, not the nav property). 

    DEFER bulk-audit: add a brief one-paragraph forward/out-of-scope note (NOT a full section) stating that bulk-audit / enumerate-users-with-0-registrations reporting is out of scope for this single-user runbook (D-06).

    Close with `## Microsoft Support Escalation Packet`, `## Related Resources` (link `10-macos-log-collection.md`, guide 11, and #27 for shared-symptom tickets), and `## Version History`: `| 2026-06-23 | Initial version — macOS Graph Platform Credential L2 investigation (RUN-02) | -- |`.
  </action>
  <verify>
    <automated>test -f docs/l2-runbooks/29-macos-graph-credential-investigation.md && grep -q "platformCredentialMethods" docs/l2-runbooks/29-macos-graph-credential-investigation.md && grep -q "graph.microsoft.com/v1.0" docs/l2-runbooks/29-macos-graph-credential-investigation.md && grep -q "\[!WARNING\]" docs/l2-runbooks/29-macos-graph-credential-investigation.md && grep -q "11-graph-api-platform-credential.md" docs/l2-runbooks/29-macos-graph-credential-investigation.md && ! grep -qi "From L1 escalation" docs/l2-runbooks/29-macos-graph-credential-investigation.md && ! grep -qE "platformCredentialAuthenticationMethod/" docs/l2-runbooks/29-macos-graph-credential-investigation.md && echo RUNBOOK29_OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists with #27-style frontmatter, platform-gate blockquote, `## Context`, single-track `### Step N` headings, `## Microsoft Support Escalation Packet`, `## Related Resources`, `## Version History`.
    - Every Graph URL uses `platformCredentialMethods` as the nav property; no `platformCredentialAuthenticationMethod/` URL path segment appears.
    - The DELETE step is preceded by a `> [!WARNING]` block with the severs-Entra-binding / does-NOT-erase-SE-key semantics.
    - Permissions and endpoint detail are link-not-copy to `../admin-setup-macos/11-graph-api-platform-credential.md` (no inlined permissions table).
    - Bulk-audit appears ONLY as a brief forward/out-of-scope note, not a full section (D-06).
    - Does NOT contain a "From L1 escalation?" routing block; DOES contain the no-L1-escalation sentinel.
    - Covers all three SC#5 elements: enumerate/verify, delete-and-re-register, permission/role troubleshooting.
  </acceptance_criteria>
  <done>Runbook #29 exists, follows the #27 template as single-track numbered steps, uses the locked `platformCredentialMethods` nav property with the mandatory DELETE [!WARNING], link-not-copies guide 11, defers bulk-audit, and covers all SC#5 elements.</done>
</task>

<task type="auto">
  <name>Task 3: Extend 00-index.md macOS ADE Runbooks table with #28/#29 rows (D-05 — When-to-Use only)</name>
  <files>docs/l2-runbooks/00-index.md</files>
  <read_first>
    - docs/l2-runbooks/00-index.md (macOS ADE Runbooks "When to Use" table at lines 80-86 — match its `| Runbook | When to Use | Prerequisite |` format; the `### macOS L1 Escalation Mapping` table at lines 88-98 which must NOT be touched)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-PATTERNS.md (§ 00-index.md — exact new-row format at lines 422-426; CRITICAL do-not-touch L1 Escalation Mapping at line 428)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md (Finding 4 — exact row text; Pitfall 7 — do NOT add #28/#29 to L1 Escalation Mapping)
  </read_first>
  <action>
    Append two rows to the macOS ADE Runbooks "When to Use" table in `docs/l2-runbooks/00-index.md`, immediately after the existing `[Platform SSO Investigation](27-macos-sso-investigation.md)` row, matching the table's exact `| Runbook | When to Use | Prerequisite |` grammar with prerequisite `[macOS Log Collection](10-macos-log-collection.md)`:
    - Row for `[Kerberos SSO Investigation](28-macos-kerberos-sso-investigation.md)` — When to Use: "Kerberos TGT not acquired, realm/KDC unreachable, or PSSO-TGT integration failure (`usePlatformSSOTGT`)".
    - Row for `[Graph Credential Investigation](29-macos-graph-credential-investigation.md)` — When to Use: "Platform Credential not appearing in Graph, delete-and-re-register flow, or permission/scope errors on `platformCredentialMethods`".

    DO NOT modify the `### macOS L1 Escalation Mapping` table (lines 88-98) — D-05/Pitfall 7: no L1 Kerberos or L1 Graph runbook exists, so there is no L1 source to map. Leave that table byte-unchanged.
  </action>
  <verify>
    <automated>grep -q "28-macos-kerberos-sso-investigation.md" docs/l2-runbooks/00-index.md && grep -q "29-macos-graph-credential-investigation.md" docs/l2-runbooks/00-index.md && grep -c "10-macos-log-collection.md" docs/l2-runbooks/00-index.md && echo INDEX_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/l2-runbooks/00-index.md` macOS ADE Runbooks "When to Use" table contains rows linking `28-macos-kerberos-sso-investigation.md` and `29-macos-graph-credential-investigation.md`, both with prerequisite `[macOS Log Collection](10-macos-log-collection.md)`.
    - The rows are appended after the existing #27 Platform SSO Investigation row.
    - The `### macOS L1 Escalation Mapping` table is unchanged (no #28/#29 rows added there).
  </acceptance_criteria>
  <done>The internal 00-index When-to-Use table lists #28 and #29 with the log-collection prerequisite; the L1 Escalation Mapping table is untouched.</done>
</task>

</tasks>

<threat_model>
No application attack surface — documentation only (two new markdown runbooks + one index-table edit). No executable code, no user input, no network surface. The runbooks document security-relevant procedures (Graph DELETE, Kerberos TGT) but the controls themselves live in guides 10/11 (shipped P83/P84); authoring referencing prose introduces no attack surface. No STRIDE register applies (documentation-only phase per RESEARCH.md Security Domain).
</threat_model>

<verification>
- Both runbook files exist and follow the #27 template; #28 uses only `app-sso platform -s` + bare `klist`; #29 uses `platformCredentialMethods` with the DELETE `[!WARNING]`.
- Neither runbook contains a "From L1 escalation?" routing block; both contain the no-L1-escalation sentinel.
- `00-index.md` When-to-Use table has #28/#29 rows; L1 Escalation Mapping unchanged.
- Anti-pattern greps clean: no `app-sso diagnose`, no `klist -v`, no `platformCredentialAuthenticationMethod/` URL segment.
</verification>

<success_criteria>
- SC#4: `28-macos-kerberos-sso-investigation.md` exists with ticket-acquisition triage, realm/KDC reachability, TGT verification (`app-sso platform -s` / `klist`), and log collection.
- SC#5: `29-macos-graph-credential-investigation.md` exists with enumerate/verify/delete workflow, delete-and-re-register flow, and permission/role troubleshooting.
- RUN-01 and RUN-02 fully satisfied; internal 00-index extended.
</success_criteria>

<output>
Create `.planning/phases/85-capability-matrix-l2-runbooks/85-03-SUMMARY.md` when done.
</output>
