---
phase: 83-kerberos-sso-extension-guide
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md
  - docs/admin-setup-macos/00-overview.md
  - docs/_glossary-macos.md
autonomous: true
requirements: [KRB-04]
must_haves:
  truths:
    - "Guide 09's deferred-item sentence is replaced with a live forward link to guide 10, and no other prose in guide 09 changes"
    - "00-overview.md Mermaid diagram and numbered bullet list both include a node/item for guide 10"
    - "_glossary-macos.md contains a Kerberos SSO Extension entry and an Alphabetical Index link to it"
  artifacts:
    - path: "docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md"
      provides: "Live forward link to guide 10 replacing the PSSO-FUT-04 deferred-note sentence"
      contains: "10-kerberos-sso-extension.md"
    - path: "docs/admin-setup-macos/00-overview.md"
      provides: "Guide 10 Mermaid node (G --> J) and item 10 in the numbered setup sequence"
      contains: "10. **[Kerberos SSO Extension]"
    - path: "docs/_glossary-macos.md"
      provides: "Kerberos SSO Extension glossary term + Alphabetical Index entry"
      contains: "### Kerberos SSO Extension"
  key_links:
    - from: "docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md"
      to: "10-kerberos-sso-extension.md"
      via: "forward link replacing deferred-note sentence"
      pattern: "10-kerberos-sso-extension\\.md"
    - from: "docs/admin-setup-macos/00-overview.md"
      to: "10-kerberos-sso-extension.md"
      via: "Mermaid node J + bullet item 10"
      pattern: "G --> J"
---

<objective>
Perform the three KRB-04 surgical cross-link edits that anchor the new guide 10 into the suite. None of these edits depend on guide 10's CONTENT -- they reference its stable filename (`10-kerberos-sso-extension.md`) and a stable glossary slug -- so this plan runs in wave 1 in parallel with 83-01.

1. Guide 09: replace the one-sentence PSSO-FUT-04 deferred-note (line 148) with a live forward link to guide 10. No other prose in guide 09 changes.
2. 00-overview.md: add a Mermaid node `J[10. Kerberos SSO<br/>Extension]` with edge `G --> J`, plus a numbered bullet item 10.
3. _glossary-macos.md: add a `### Kerberos SSO Extension` entry under `## Authentication` and an Alphabetical Index link.

Each file also receives a version-history row and a frontmatter `last_verified`/`review_by` refresh per the suite surgical-edit convention.

Purpose: Satisfy KRB-04 -- the legacy deferred-item note becomes a live link and the new guide is discoverable from the overview and glossary.
Output: Three surgically edited suite files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-kerberos-sso-extension-guide/83-CONTEXT.md
@.planning/phases/83-kerberos-sso-extension-guide/83-RESEARCH.md
@.planning/phases/83-kerberos-sso-extension-guide/83-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guide 09 forward-link replacement (one sentence, line 148)</name>
  <files>docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md</files>
  <read_first>
    - docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md (lines 138-176: the `### Kerberos SSO Extension (Coexistence)` subsection, line-148 sentence to replace, See Also, and version-history table)
    - .planning/phases/83-kerberos-sso-extension-guide/83-PATTERNS.md (guide 09 surgical-edit section -- verbatim current text + replacement text + surrounding context that must NOT change)
    - .planning/phases/83-kerberos-sso-extension-guide/83-RESEARCH.md (## KRB-04: Exact Surgical Edit Points #1)
  </read_first>
  <action>
    Replace ONLY the line-148 deferred-note sentence (verbatim: "A full Kerberos SSO extension configuration guide (payload walkthrough, Extension Identifier values, profile structure) is deferred to a future documentation phase -- see **PSSO-FUT-04** in the v1.9 deferred-cleanup tracking.") with the forward-link text from PATTERNS: "For the full Kerberos SSO extension configuration guide (payload walkthrough, Extension Identifier values, PSSO TGT integration, and diagnostics), see [Kerberos SSO Extension](10-kerberos-sso-extension.md)."

    Do NOT change the three preceding paragraphs (lines 142-146) of the `### Kerberos SSO Extension (Coexistence)` subsection -- they remain byte-identical. Do NOT change any other prose in guide 09.

    Append a version-history row to the guide 09 table: `| 2026-06-22 | Phase 83 (KRB-04): replaced deferred-note sentence with forward link to guide 10 | -- |`. Update guide 09 frontmatter `last_verified` to 2026-06-22 and `review_by` to 2026-09-22 (suite surgical-edit convention).
  </action>
  <verify>
    <automated>grep -q '\[Kerberos SSO Extension\](10-kerberos-sso-extension.md)' docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md && [ "$(grep -c 'PSSO-FUT-04' docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md)" = "0" ] && grep -q 'Phase 83 (KRB-04): replaced deferred-note sentence with forward link to guide 10' docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md</automated>
  </verify>
  <acceptance_criteria>
    - Forward link `[Kerberos SSO Extension](10-kerberos-sso-extension.md)` present in guide 09.
    - The deferred-note marker `PSSO-FUT-04` appears 0 times in guide 09 (the old sentence is fully removed).
    - The three coexistence paragraphs (lines ~142-146) are unchanged -- `## Configuration-Caused Failures`, `## See Also`, and the Error-10002 prose remain byte-identical (git diff shows ONLY the line-148 sentence + version-history row + frontmatter dates changed).
    - Version-history row `Phase 83 (KRB-04): replaced deferred-note sentence with forward link to guide 10` present with Author `--`.
    - Frontmatter `last_verified: 2026-06-22` and `review_by: 2026-09-22`.
  </acceptance_criteria>
  <done>Guide 09 deferred-note sentence replaced with a live forward link; no other prose changed; version-history + frontmatter refreshed.</done>
</task>

<task type="auto">
  <name>Task 2: 00-overview.md Mermaid node J + bullet item 10</name>
  <files>docs/admin-setup-macos/00-overview.md</files>
  <read_first>
    - docs/admin-setup-macos/00-overview.md (lines 19-31 Mermaid block ending at `G --> I`; line 49 item-9 bullet; lines 67-73 version-history table)
    - .planning/phases/83-kerberos-sso-extension-guide/83-PATTERNS.md (00-overview.md surgical-edits section -- verbatim Mermaid insertion line, bullet insertion text, version-history row)
    - .planning/phases/83-kerberos-sso-extension-guide/83-RESEARCH.md (## KRB-04: Exact Surgical Edit Points #2; A2 -- arrow originates from G not I)
  </read_first>
  <action>
    Add ONE new Mermaid line immediately after the existing `G --> I[9. Enterprise SSO<br/>Migration]` line: `  G --> J[10. Kerberos SSO<br/>Extension]`. The arrow source is `G` (node 7, Platform SSO Setup) -- NOT `I` -- because guide 10 depends on PSSO (guide 07), matching the existing `G --> H` / `G --> I` sibling-descent topology (RESEARCH A2). Use `<br/>` (not `<br>`) per the established node pattern.

    Add a numbered bullet item 10 immediately after the item-9 bullet (line 49), before the `## Cross-Platform References` heading, using the verbatim text from PATTERNS: "10. **[Kerberos SSO Extension](10-kerberos-sso-extension.md)** -- Configure the Apple Kerberos SSO extension (`com.apple.AppSSOKerberos.KerberosExtension`, Type: Credential) via Intune Custom Template (.mobileconfig) for PSSO-integrated on-premises AD Kerberos authentication. Covers realm and Hosts payload, PSSO TGT sharing (`usePlatformSSOTGT`), and `app-sso platform -s` / `klist` diagnostics."

    Append a version-history row: `| 2026-06-22 | Phase 83 (KRB-04): added guide 10 node to Mermaid diagram and item 10 to numbered list | -- |`. Update frontmatter `last_verified`/`review_by` to 2026-06-22 / 2026-09-22. Change nothing else in the file.
  </action>
  <verify>
    <automated>grep -q 'G --> J\[10. Kerberos SSO<br/>Extension\]' docs/admin-setup-macos/00-overview.md && grep -q '10. \*\*\[Kerberos SSO Extension\](10-kerberos-sso-extension.md)\*\*' docs/admin-setup-macos/00-overview.md && grep -q 'Phase 83 (KRB-04): added guide 10 node to Mermaid diagram and item 10 to numbered list' docs/admin-setup-macos/00-overview.md</automated>
  </verify>
  <acceptance_criteria>
    - Mermaid block contains the exact line `G --> J[10. Kerberos SSO<br/>Extension]`; arrow originates from `G` (not `I`); uses `<br/>`.
    - Numbered bullet item `10. **[Kerberos SSO Extension](10-kerberos-sso-extension.md)**` present with the full description (names `com.apple.AppSSOKerberos.KerberosExtension`, `usePlatformSSOTGT`, `app-sso platform -s` / `klist`).
    - Existing items 1-9 and existing Mermaid edges are unchanged (git diff shows only the added node, added bullet, version-history row, frontmatter dates).
    - Version-history row for Phase 83 (KRB-04) present with Author `--`.
  </acceptance_criteria>
  <done>00-overview.md extended with the guide 10 Mermaid node (G --> J) and numbered bullet item 10; version-history + frontmatter refreshed.</done>
</task>

<task type="auto">
  <name>Task 3: Glossary Kerberos SSO Extension entry + Alphabetical Index</name>
  <files>docs/_glossary-macos.md</files>
  <read_first>
    - docs/_glossary-macos.md (line 17 Alphabetical Index; lines 121-141 `## Authentication` section ending with Enterprise SSO Plug-in entry; lines 144-154 version-history)
    - .planning/phases/83-kerberos-sso-extension-guide/83-PATTERNS.md (glossary surgical-insert section -- verbatim entry template, index line, insertion point, version-history row)
    - .planning/phases/83-kerberos-sso-extension-guide/83-RESEARCH.md (## KRB-04: Exact Surgical Edit Points #3; Pattern 5 glossary entry format)
  </read_first>
  <action>
    Insert a new `### Kerberos SSO Extension` entry into the `## Authentication` section AFTER the Enterprise SSO Plug-in entry (after its `> See also:` line ~141) and BEFORE the `---` rule that precedes `## Version History`. Use the verbatim entry-and-see-also text from PATTERNS: one dense paragraph naming the identity `com.apple.AppSSOKerberos.KerberosExtension`, payload Type Credential, TeamIdentifier `apple`, on-prem AD TGT acquisition, Custom Template (.mobileconfig) deployment, PSSO coexistence via `usePlatformSSOTGT: true`, macOS 14.6 floor for PSSO TGT integration / macOS 10.15+ standalone (out of v1.10 scope). Close with `> See also: [Platform SSO](#platform-sso); [Enterprise SSO Plug-in](#enterprise-sso-plug-in); [Kerberos SSO Extension Guide](admin-setup-macos/10-kerberos-sso-extension.md).`

    Insert `[Kerberos SSO Extension](#kerberos-sso-extension)` into the Alphabetical Index line (line 17) between `[Jailbreak Detection](#jailbreak-detection)` and `[MAM-WE](#mam-we)` (K sorts between J and M).

    Append a version-history row: `| 2026-06-22 | Phase 83 (KRB-04): added Kerberos SSO Extension entry to ## Authentication and Alphabetical Index | -- |`. Update frontmatter `last_verified`/`review_by` to 2026-06-22 / 2026-09-22. Use ` -- ` em-dash and backtick code-spans; keep the existing entries byte-identical.
  </action>
  <verify>
    <automated>grep -q '### Kerberos SSO Extension' docs/_glossary-macos.md && grep -q '\[Kerberos SSO Extension\](#kerberos-sso-extension)' docs/_glossary-macos.md && grep -q 'admin-setup-macos/10-kerberos-sso-extension.md' docs/_glossary-macos.md && grep -q 'Phase 83 (KRB-04): added Kerberos SSO Extension entry' docs/_glossary-macos.md</automated>
  </verify>
  <acceptance_criteria>
    - `### Kerberos SSO Extension` entry present under `## Authentication`, containing `com.apple.AppSSOKerberos.KerberosExtension`, `Credential`, `apple`, `usePlatformSSOTGT`, and `14.6`.
    - Entry `> See also:` line links `#platform-sso`, `#enterprise-sso-plug-in`, and `admin-setup-macos/10-kerberos-sso-extension.md`.
    - Alphabetical Index line contains `[Kerberos SSO Extension](#kerberos-sso-extension)` positioned between `Jailbreak Detection` and `MAM-WE`.
    - Existing glossary entries (Platform SSO, Secure Enclave, Enterprise SSO Plug-in) are byte-unchanged.
    - Version-history row for Phase 83 (KRB-04) present with Author `--`; em-dash `—` count is 0.
  </acceptance_criteria>
  <done>Glossary Kerberos SSO Extension term and Alphabetical Index link inserted; existing entries unchanged; version-history + frontmatter refreshed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suite reader -> cross-links | These edits add forward links and a glossary term. The threat surface is documentation accuracy (a broken link or a wrong identifier in the glossary). |

This is a documentation-only plan: no executable code, user input, auth surface, or network/data operations are produced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-05 | Tampering (incorrect guidance) | Glossary identity literal + forward-link target | mitigate | Acceptance criteria enforce the literal `com.apple.AppSSOKerberos.KerberosExtension` / `Type Credential` in the glossary and the exact forward-link target `10-kerberos-sso-extension.md`. The forward link points at guide 10's stable filename (authored by 83-01/02 in the same phase), avoiding a dangling-link state at phase close. |
| T-83-06 | Tampering (scope creep) | Surgical edits touching unintended prose | mitigate | Acceptance criteria require git-diff to show ONLY the targeted line-148 sentence (guide 09), the added Mermaid node + bullet (00-overview), and the added glossary entry + index link -- preventing accidental edits to navigation-hub files (DI-1 navigation-last) or other prose. |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (documentation only). No package legitimacy gate applicable. |

Block-on severity: high. None expected -- no CLI or executable content is published; navigation-hub files (`docs/index.md`, `common-issues.md`, `quick-ref-l2.md`) are explicitly NOT touched (DI-1, Phase 87 owns those).
</threat_model>

<verification>
- Guide 09: forward link to `10-kerberos-sso-extension.md` present; `PSSO-FUT-04` removed (0); only the line-148 sentence + version-history + frontmatter changed.
- 00-overview.md: `G --> J[10. Kerberos SSO<br/>Extension]` present; numbered item 10 present.
- _glossary-macos.md: `### Kerberos SSO Extension` entry + Alphabetical Index link present.
- DI-1 honored: no edits to `docs/index.md`, `docs/common-issues.md`, or `docs/quick-ref-l2.md` (those are Phase 87).
</verification>

<success_criteria>
- KRB-04 delivered: guide 09 deferred-note replaced with live forward link (no other prose change); 00-overview Mermaid + bullet include guide 10; glossary has a Kerberos SSO Extension entry.
- All three edits are surgical (git diff scoped to the targeted lines + version-history rows + frontmatter date refresh).
</success_criteria>

<output>
Create `.planning/phases/83-kerberos-sso-extension-guide/83-03-SUMMARY.md` when done.
</output>
