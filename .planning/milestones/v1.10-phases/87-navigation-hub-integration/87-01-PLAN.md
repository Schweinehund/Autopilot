---
phase: 87-navigation-hub-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.md
  - docs/common-issues.md
autonomous: true
requirements: [REF-03]

must_haves:
  truths:
    - "A reader of docs/index.md sees the macOS Admin Setup Platform SSO row name guide 10 (Kerberos SSO Extension) and guide 11 (Graph API Platform Credential) [SC#1]"
    - "A reader of docs/index.md sees two discrete Desktop Engineering L2 rows for Kerberos SSO Investigation (#28) and Graph Credential Investigation (#29) [SC#1]"
    - "A reader of docs/common-issues.md macOS section finds a Kerberos SSO Extension Failure entry that routes to L2 runbook #28 [SC#2]"
  artifacts:
    - path: "docs/index.md"
      provides: "Enriched macOS Admin Setup row (guides 10/11) + two new L2 rows (#28/#29) + Version History stamp"
      contains: "l2-runbooks/00-index.md#macos-ade-runbooks"
    - path: "docs/common-issues.md"
      provides: "Standalone ### Kerberos SSO Extension Failure entry (L2-only) + Version History stamp"
      contains: "### Kerberos SSO Extension Failure"
  key_links:
    - from: "docs/index.md L2 table"
      to: "docs/l2-runbooks/00-index.md#macos-ade-runbooks"
      via: "anchor link in two new rows"
      pattern: "l2-runbooks/00-index.md#macos-ade-runbooks"
    - from: "docs/common-issues.md Kerberos entry"
      to: "docs/l2-runbooks/28-macos-kerberos-sso-investigation.md"
      via: "L2 bullet link"
      pattern: "l2-runbooks/28-macos-kerberos-sso-investigation.md"
---

<objective>
Wire the already-committed v1.10 content into the two top-level navigation hubs `docs/index.md` and `docs/common-issues.md` via append-only edits, satisfying SC#1 and SC#2 of REF-03 under the navigation-last invariant (all four content targets — guides 10/11, runbooks #28/#29 — are confirmed committed per RESEARCH §1).

This plan makes three locked edits to index.md (enrich the Admin Setup Platform SSO row per D-01; add two discrete L2 rows per D-02; stamp Version History) and one locked entry to common-issues.md (standalone Kerberos SSO Extension Failure entry, L2-only, per D-05; stamp Version History).

Purpose: New Kerberos/Graph content becomes reachable from the master index and the symptom-driven common-issues hub. This is the final discovery layer for v1.10 macOS Platform SSO follow-ons.
Output: Edited docs/index.md and docs/common-issues.md, each with a 2026-06-24 Phase 87 Version History stamp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-navigation-hub-integration/87-CONTEXT.md
@.planning/phases/87-navigation-hub-integration/87-RESEARCH.md
@.planning/phases/87-navigation-hub-integration/87-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.md — enrich Admin Setup row (D-01) + add two L2 rows (D-02) + Version History stamp</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md — READ the full macOS Desktop Engineering L2 table and macOS Admin Setup table (CONTEXT cites lines ~112–132; line numbers drift, so locate by content: the `[macOS Platform SSO Investigation](l2-runbooks/00-index.md#macos-ade-runbooks)` #27 row, and the `[macOS Platform SSO Admin Setup Guides](admin-setup-macos/00-overview.md)` row) AND the `## Version History` table near the bottom (CONTEXT cites ~line 319–335; locate the Phase 81 SSOREF-04 top row).
    - 87-PATTERNS.md §A (line-132 row verbatim analog + grammar), §B (line-123 #27 row verbatim analog + two-row insert grammar), §C (Version History stamp analog + newest-first rule).
    - 87-RESEARCH.md §2.1 (exact insertion points, enriched-row example, two-row example, version stamp text) and §3.2 (confirmed anchor slug `#macos-ade-runbooks`).
    - 87-CONTEXT.md D-01, D-02 (locked link-target constraints) and "Claude's Discretion" (cell wording is editable).
  </read_first>
  <action>
    Make three edits to docs/index.md, all append-only / in-place enrichment — do NOT alter any other rows, link targets, or sections.

    (1) D-01 — Enrich the existing macOS Admin Setup "Platform SSO Admin Setup Guides" row IN PLACE. Keep the link target exactly `admin-setup-macos/00-overview.md` (DO NOT deep-link to guide 10/11 files, DO NOT add separate per-guide rows). Extend the "When to Use" cell prose so it additionally names guide 10 (Kerberos SSO Extension deployment) and guide 11 (Graph API Platform Credential management), following the comma-and-connector list grammar already used for guides 07/08/09. Exact wording is your discretion per D-01 discretion note; see RESEARCH §2.1 for an acceptable example.

    (2) D-02 — Insert two discrete rows into the macOS Desktop Engineering L2 table immediately AFTER the existing `[macOS Platform SSO Investigation]` #27 row, before the blank line preceding the `### Admin Setup` H3. Row one display text names macOS Kerberos SSO Investigation (#28); row two display text names macOS Graph Credential Investigation (#29). BOTH rows MUST use link target `l2-runbooks/00-index.md#macos-ade-runbooks` (the confirmed live anchor — NOT numbered runbook files). "When to Use" cell wording is your discretion following the #27 row's comma-clause symptom style; name the runbook number (#28 / #29) in each cell per the analog grammar.

    (3) Version History — Insert ONE new row as the newest-first top entry of the `## Version History` table body (after the header/separator, before the Phase 81 row). Use date 2026-06-24 and text in the form: `Phase 87 (REF-03): enriched macOS Admin Setup Platform SSO row to name guides 10/11; added macOS L2 rows for Kerberos SSO Investigation (#28) and Graph Credential Investigation (#29)`. Author column is `--`.
  </action>
  <verify>
    <automated>cd /d/claude/Autopilot && grep -c 'l2-runbooks/00-index.md#macos-ade-runbooks' docs/index.md | { read n; [ "$n" -ge 3 ] && echo PASS || echo "FAIL got $n"; }</automated>
    <automated>cd /d/claude/Autopilot && grep -q 'guide 10' docs/index.md && grep -q 'guide 11' docs/index.md && grep -q 'Kerberos SSO Investigation' docs/index.md && grep -q 'Graph Credential Investigation' docs/index.md && grep -q '2026-06-24 | Phase 87 (REF-03)' docs/index.md && echo PASS || echo FAIL</automated>
    <automated>cd /d/claude/Autopilot && grep -c 'admin-setup-macos/10-kerberos-sso-extension.md\|admin-setup-macos/11-graph-api-platform-credential.md' docs/index.md | { read n; [ "$n" -eq 0 ] && echo "PASS no admin deep-link" || echo "FAIL admin deep-link present"; }</automated>
  </verify>
  <acceptance_criteria>
    - docs/index.md macOS Admin Setup Platform SSO row "When to Use" cell text now names guide 10 (Kerberos SSO Extension) AND guide 11 (Graph API Platform Credential); its link target is STILL `admin-setup-macos/00-overview.md` (unchanged, no deep-link to numbered guide files).
    - docs/index.md Desktop Engineering L2 table contains a row whose display text names Kerberos SSO Investigation (#28) and a row whose display text names Graph Credential Investigation (#29); BOTH rows link to `l2-runbooks/00-index.md#macos-ade-runbooks` (count of that anchor string in the file is ≥ 3, accounting for the pre-existing #27 row plus the two new rows).
    - No new discrete per-guide row was added to the Admin Setup table (the enrichment is the single line-132-style row only).
    - docs/index.md `## Version History` has a new newest-first row `| 2026-06-24 | Phase 87 (REF-03): ... | -- |` naming the index.md edits.
    - No other rows, link targets, or sections of docs/index.md were modified.
  </acceptance_criteria>
  <done>Admin Setup row enriched (target unchanged), two L2 rows added (anchor link), Version History stamped; all three verify commands print PASS.</done>
</task>

<task type="auto">
  <name>Task 2: common-issues.md — standalone Kerberos SSO Extension Failure entry (D-05, L2-only) + Version History stamp</name>
  <files>docs/common-issues.md</files>
  <read_first>
    - docs/common-issues.md — READ the macOS ADE Failure Scenarios section, specifically the `### Platform SSO Sign-In Failure` entry (CONTEXT cites :213-218; locate by heading) and the following `## iOS/iPadOS Failure Scenarios` H2 boundary; READ the "no L1 runbook — escalate to L2" L1-bullet analogs (CONTEXT cites the TPM entry ~:64 and Hybrid Join ~:87); READ the `## Version History` table near the bottom (CONTEXT cites ~:379-387; locate the Phase 81 SSOREF-04 top row).
    - 87-PATTERNS.md §D (Platform SSO Sign-In Failure entry verbatim analog + H3 grammar; TPM/Hybrid Join L1-line "no L1 runbook" analogs; entry-to-insert structure; Version History row analog).
    - 87-RESEARCH.md §2.2 (exact insertion point after the PSSO entry / before the iOS H2; entry structure; version stamp text).
    - 87-CONTEXT.md D-05 (standalone `###` entry, L2-only, "no L1 runbook — escalate to L2" prose, link to #28) and "Claude's Discretion" (symptom prose + bullet wording editable). Cross-ref: 85-CONTEXT D-05 (no L1 Kerberos runbook exists — do NOT fabricate one).
  </read_first>
  <action>
    Make two append-only edits to docs/common-issues.md — do NOT alter the existing `### Platform SSO Sign-In Failure` entry or any other content.

    (1) D-05 — Insert a NEW standalone `### Kerberos SSO Extension Failure` symptom entry immediately AFTER the closing `- **L2:**` bullet of the `### Platform SSO Sign-In Failure` entry, before the `## iOS/iPadOS Failure Scenarios` H2. Match the file's one-symptom-per-`###` grammar: heading line, blank line, one symptom-prose sentence (Kerberos TGT not acquired / realm or KDC unreachable / `usePlatformSSOTGT` PSSO-TGT integration failing), blank line, then two bullets. The `- **L1:**` bullet MUST use the "no L1 runbook — escalate to L2" plain-prose style matching the TPM/Hybrid Join precedents (DO NOT fabricate or link to a non-existent L1 Kerberos runbook — 85-CONTEXT D-05 LOCKED). The `- **L2:**` bullet MUST link to `l2-runbooks/28-macos-kerberos-sso-investigation.md`. Exact symptom prose and bullet wording are your discretion within these locked constraints. Do NOT fold this under the PSSO entry — it is a standalone `###` section (#28 ≠ #27).

    (2) Version History — Insert ONE newest-first row at the top of the `## Version History` table body (after the header/separator, before the Phase 81 row). Date 2026-06-24, text form: `Phase 87 (REF-03): added Kerberos SSO Extension Failure entry under macOS ADE Failure Scenarios`. Author `--`.
  </action>
  <verify>
    <automated>cd /d/claude/Autopilot && grep -q '### Kerberos SSO Extension Failure' docs/common-issues.md && grep -q 'l2-runbooks/28-macos-kerberos-sso-investigation.md' docs/common-issues.md && grep -q '2026-06-24 | Phase 87 (REF-03)' docs/common-issues.md && echo PASS || echo FAIL</automated>
    <automated>cd /d/claude/Autopilot && grep -A6 '### Kerberos SSO Extension Failure' docs/common-issues.md | grep -qi 'no L1 runbook' && echo "PASS L2-only" || echo "FAIL missing no-L1-runbook line"</automated>
  </verify>
  <acceptance_criteria>
    - docs/common-issues.md contains a standalone `### Kerberos SSO Extension Failure` heading placed AFTER the `### Platform SSO Sign-In Failure` entry and BEFORE the `## iOS/iPadOS Failure Scenarios` H2.
    - The entry's `- **L1:**` bullet uses the "no L1 runbook — escalate to L2" prose style (no fabricated L1 runbook link).
    - The entry's `- **L2:**` bullet links to `l2-runbooks/28-macos-kerberos-sso-investigation.md`.
    - The existing `### Platform SSO Sign-In Failure` entry is unchanged (the new entry is standalone, not folded into it).
    - docs/common-issues.md `## Version History` has a new newest-first row `| 2026-06-24 | Phase 87 (REF-03): ... | -- |`.
  </acceptance_criteria>
  <done>Standalone L2-only Kerberos entry added with #28 link and no-L1-runbook prose; Version History stamped; both verify commands print PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation-only phase. No executable code, no user input, no auth surface, no network or data handling. The only "surface" is Markdown link integrity within the docs/ tree. There is no client→server or any other runtime trust boundary in scope.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Tampering (Integrity) | Relative link targets in docs/index.md and docs/common-issues.md (#28 runbook, #macos-ade-runbooks anchor, 00-overview.md) | mitigate | Acceptance criteria assert exact link-target strings; navigation-last invariant confirms all 4 content targets are already committed (RESEARCH §1); anchor slug `#macos-ade-runbooks` confirmed live (RESEARCH §3.2). |
| T-87-02 | Information Disclosure / Misrouting | Fabricated L1 escalation source for #28 | accept→mitigate | D-05 / 85-CONTEXT D-05 LOCKED: "no L1 runbook — escalate to L2" prose only; acceptance criterion asserts the no-L1-runbook line and forbids a fabricated L1 link. |
| T-87-03 | Tampering | Accidental edit to unrelated index.md rows / link targets | mitigate | Acceptance criterion asserts no other rows or targets modified; append-only / in-place-enrich constraint; grep gate ensures no admin-setup deep-link was introduced. |

Block-on threshold is `high`. There are NO high-severity threats in this docs-wiring phase: no code execution, no secrets, no untrusted input, no package installs. All identified items are low-severity Markdown-integrity concerns fully mitigated by exact-string acceptance criteria.
</threat_model>

<verification>
- `grep -c 'l2-runbooks/00-index.md#macos-ade-runbooks' docs/index.md` returns ≥ 3 (pre-existing #27 + new #28 + new #29).
- `grep` confirms `guide 10`, `guide 11`, `Kerberos SSO Investigation`, `Graph Credential Investigation` all present in docs/index.md.
- No `admin-setup-macos/10-...` or `admin-setup-macos/11-...` deep-link in docs/index.md.
- `### Kerberos SSO Extension Failure` present in docs/common-issues.md with a #28 L2 link and a "no L1 runbook" line.
- Both edited files carry a `2026-06-24 | Phase 87 (REF-03)` Version History row.
</verification>

<success_criteria>
- SC#1: docs/index.md macOS Admin Setup table names guide 10 and guide 11 (enriched row, target unchanged); Desktop Engineering L2 table has discrete #28 and #29 rows linking to `00-index.md#macos-ade-runbooks`.
- SC#2: docs/common-issues.md macOS section has a Kerberos SSO extension escalation entry routing to L2 #28 (L2-only, no fabricated L1).
- Both files Version-History-stamped (2026-06-24, Phase 87 REF-03).
- No content files edited; no link target points at an uncommitted artifact; no unrelated rows changed.
</success_criteria>

<output>
Create `.planning/phases/87-navigation-hub-integration/87-01-SUMMARY.md` when done.
</output>
