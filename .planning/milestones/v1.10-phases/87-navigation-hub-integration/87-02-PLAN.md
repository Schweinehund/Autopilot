---
phase: 87-navigation-hub-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/quick-ref-l2.md
  - docs/decision-trees/06-macos-triage.md
autonomous: true
requirements: [REF-03]

must_haves:
  truths:
    - "An L2 engineer reading docs/quick-ref-l2.md finds a Kerberos SSO Diagnostics block with the klist command and a pointer to the existing app-sso platform -s block [SC#3]"
    - "docs/l2-runbooks/00-index.md macOS ADE Runbooks table already includes #28 and #29 rows (verified present, no edit) [SC#4]"
    - "docs/decision-trees/06-macos-triage.md routes a Kerberos SSO extension symptom to L2 #28 via a RED escalateL2 leaf under MACSSO [SC#5]"
  artifacts:
    - path: "docs/quick-ref-l2.md"
      provides: "#### Kerberos SSO Diagnostics block (klist only) + #28 runbook bullet + Version History stamp"
      contains: "#### Kerberos SSO Diagnostics"
    - path: "docs/decision-trees/06-macos-triage.md"
      provides: "MACE2 red leaf under MACSSO + click to #28 + Routing Verification row + Version History stamp"
      contains: "MACE2"
  key_links:
    - from: "docs/quick-ref-l2.md Kerberos block"
      to: "existing #### Platform SSO Attestation Command block (app-sso platform -s)"
      via: "in-page pointer link #platform-sso-attestation-command"
      pattern: "#platform-sso-attestation-command"
    - from: "docs/decision-trees/06-macos-triage.md MACE2 node"
      to: "../l2-runbooks/28-macos-kerberos-sso-investigation.md"
      via: "click directive"
      pattern: "click MACE2"
---

<objective>
Complete REF-03 navigation-last wiring across the remaining three nav hubs: append a Kerberos diagnostics block to `docs/quick-ref-l2.md` (D-04), VERIFY (no edit) the #28/#29 rows in `docs/l2-runbooks/00-index.md` (D-03), and extend the Mermaid triage tree in `docs/decision-trees/06-macos-triage.md` with a RED L2-escalation Kerberos leaf (D-06/D-07). Satisfies SC#3, SC#4, SC#5.

quick-ref-l2.md gets a new `#### Kerberos SSO Diagnostics` block containing ONLY `klist` (no `-v`) plus a pointer to the already-present `app-sso platform -s` block (NO re-statement of that fenced block; `app-sso diagnose` PROHIBITED), a #28 runbook bullet, and a Version History stamp. 00-index.md is VERIFY-ONLY: confirm #28/#29 When-to-Use rows exist and confirm NO row is added to the macOS L1 Escalation Mapping table (no L1 source exists). 06-macos-triage.md gets a `MACE2` third arm under the MACSSO diamond, a `click` to `../l2-runbooks/28-...`, an extension of the `class MACE1 escalateL2` assignment (RED), a Routing Verification row, and a Version History stamp.

Purpose: Kerberos diagnostics reach the L2 cheat-sheet and the triage decision tree, completing the discovery surface for v1.10 macOS Kerberos SSO.
Output: Edited quick-ref-l2.md and 06-macos-triage.md (each 2026-06-24 Phase 87 stamped); 00-index.md verified unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-navigation-hub-integration/87-CONTEXT.md
@.planning/phases/87-navigation-hub-integration/87-RESEARCH.md
@.planning/phases/87-navigation-hub-integration/87-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: quick-ref-l2.md — add Kerberos SSO Diagnostics block (klist only, D-04) + #28 runbook bullet + Version History stamp; and VERIFY-ONLY 00-index.md (D-03)</name>
  <files>docs/quick-ref-l2.md, docs/l2-runbooks/00-index.md</files>
  <read_first>
    - docs/quick-ref-l2.md — READ the existing `#### Platform SSO Attestation Command` block (CONTEXT cites :180-188; locate by heading — it contains the `app-sso platform -s` fenced block and the in-page anchor it exposes), the `### macOS Investigation Runbooks` list (CONTEXT cites :190-196; locate the #27 `[Platform SSO Investigation]` bullet), and the `## Version History` table (CONTEXT cites ~:370-382; locate the Phase 81 top row).
    - docs/l2-runbooks/00-index.md — READ the macOS ADE Runbooks When-to-Use table (CONTEXT cites :87-88; locate the existing #28 `[Kerberos SSO Investigation]` and #29 `[Graph Credential Investigation]` rows) AND the macOS L1 Escalation Mapping table (CONTEXT cites :90-100). VERIFY-ONLY — do not edit this file.
    - 87-PATTERNS.md §E (Platform SSO Attestation Command block verbatim analog + grammar; Kerberos block to insert; #27 runbook bullet analog; Version History analog), §F (00-index.md verify-only rows + no-L1-mapping-row constraint).
    - 87-RESEARCH.md §2.3 (exact insertion point, block structure, runbook bullet, version stamp), §2.4 (00-index.md verify-only — #28/#29 rows present, NO escalation-mapping row), §3.3 (locked klist form: plain `klist`, NO `-v`; `app-sso diagnose` PROHIBITED; `tgt_ad`/`tgt_cloud` interpretation), Pitfall 1 (no L1 escalation-mapping row) and Pitfall 2 (no app-sso re-statement).
    - 87-CONTEXT.md D-04 (klist only + pointer, no fenced-block re-statement, app-sso diagnose prohibited), D-03 (00-index.md verify-only, no L1 mapping row). Cross-ref: 83-CONTEXT D-11/D-12/D-13.
  </read_first>
  <action>
    Two append-only edits to docs/quick-ref-l2.md, plus a read-only verification of docs/l2-runbooks/00-index.md. Do NOT edit 00-index.md.

    (1) D-04 — Insert a NEW sibling `#### Kerberos SSO Diagnostics` block in docs/quick-ref-l2.md immediately AFTER the closing prose of the existing `#### Platform SSO Attestation Command` block, before the `### macOS Investigation Runbooks` H3. Structure mirrors the sibling block: `#### Kerberos SSO Diagnostics` heading, blank line, one-sentence lead, blank line, a fenced ```bash``` block containing ONLY the single command `klist` (plain — NO `-v`, NO version-variant flags per D-04 / P83 D-13), blank line, then interpretation prose. The prose MUST end with a pointer sentence to the existing block referencing `tgt_ad` (on-prem AD) vs `tgt_cloud` (Entra) per P83 D-12 and linking to the in-page anchor `#platform-sso-attestation-command`. DO NOT re-state the `app-sso platform -s` fenced block (Pitfall 2 / D-04 LOCKED). `app-sso diagnose` MUST NOT appear anywhere (P83 D-11). Exact note/pointer wording is your discretion within these locks.

    (2) Append a runbook bullet to the `### macOS Investigation Runbooks` list immediately AFTER the #27 `[Platform SSO Investigation]` bullet: `- [Kerberos SSO Investigation](l2-runbooks/28-macos-kerberos-sso-investigation.md) -- <brief symptom description>` following the list grammar. Symptom description is your discretion.

    (3) Insert ONE newest-first Version History row at the top of the `## Version History` table body (after header/separator, before the Phase 81 row). Date 2026-06-24, text form: `Phase 87 (REF-03): added Kerberos SSO Diagnostics block (klist) + Kerberos SSO Investigation runbook bullet to macOS section`. Author `--`.

    (4) D-03 VERIFY-ONLY (docs/l2-runbooks/00-index.md): Confirm the macOS ADE Runbooks When-to-Use table already contains the #28 `[Kerberos SSO Investigation]` row and the #29 `[Graph Credential Investigation]` row (shipped P85). Confirm the macOS L1 Escalation Mapping table contains NO row for #28 or #29, and ADD NONE (no L1 source exists — 85-CONTEXT D-05 LOCKED). Make NO edit to this file and NO Version History stamp for it. Record the verification result in the SUMMARY.
  </action>
  <verify>
    <automated>cd /d/claude/Autopilot && grep -q '#### Kerberos SSO Diagnostics' docs/quick-ref-l2.md && grep -q 'l2-runbooks/28-macos-kerberos-sso-investigation.md' docs/quick-ref-l2.md && grep -q '2026-06-24 | Phase 87 (REF-03)' docs/quick-ref-l2.md && echo PASS || echo FAIL</automated>
    <automated>cd /d/claude/Autopilot && { grep -q 'klist -v' docs/quick-ref-l2.md && echo "FAIL klist -v present"; } ; { grep -q 'app-sso diagnose' docs/quick-ref-l2.md && echo "FAIL app-sso diagnose present"; } ; { ! grep -q 'klist -v' docs/quick-ref-l2.md && ! grep -q 'app-sso diagnose' docs/quick-ref-l2.md && echo PASS; }</automated>
    <automated>cd /d/claude/Autopilot && c=$(grep -c 'app-sso platform -s' docs/quick-ref-l2.md); [ "$c" -eq 1 ] && echo "PASS single app-sso block (count=$c)" || echo "FAIL app-sso restated (count=$c)"</automated>
    <automated>cd /d/claude/Autopilot && grep -q '28-macos-kerberos-sso-investigation.md' docs/l2-runbooks/00-index.md && grep -q '29-macos-graph-credential-investigation.md' docs/l2-runbooks/00-index.md && echo "PASS 00-index rows present" || echo "FAIL 00-index rows missing"</automated>
  </verify>
  <acceptance_criteria>
    - docs/quick-ref-l2.md contains a `#### Kerberos SSO Diagnostics` heading placed AFTER the `#### Platform SSO Attestation Command` block and BEFORE `### macOS Investigation Runbooks`.
    - That block contains a `klist` command inside a fenced bash block; `klist` has NO `-v` flag anywhere in the file.
    - The block does NOT add a second `app-sso platform -s` fenced block (count of `app-sso platform -s` in the file stays exactly 1) and uses an in-page pointer to `#platform-sso-attestation-command` referencing `tgt_ad`/`tgt_cloud`.
    - The string `app-sso diagnose` does NOT appear anywhere in docs/quick-ref-l2.md.
    - docs/quick-ref-l2.md `### macOS Investigation Runbooks` list has a new bullet linking to `l2-runbooks/28-macos-kerberos-sso-investigation.md`.
    - docs/quick-ref-l2.md `## Version History` has a new newest-first row `| 2026-06-24 | Phase 87 (REF-03): ... | -- |`.
    - docs/l2-runbooks/00-index.md: the #28 and #29 When-to-Use rows are present (verify-only); NO new row was added to the macOS L1 Escalation Mapping table; the file is byte-unchanged by this plan (no edit, no Version History stamp).
  </acceptance_criteria>
  <done>Kerberos diagnostics block (klist only) + #28 bullet + stamp added to quick-ref-l2.md; 00-index.md verified (rows present, no mapping row added, no edit); all four verify commands print PASS.</done>
</task>

<task type="auto">
  <name>Task 2: 06-macos-triage.md — MACE2 red leaf under MACSSO (D-06/D-07) + click to #28 + class extension + Routing Verification row + Version History stamp</name>
  <files>docs/decision-trees/06-macos-triage.md</files>
  <read_first>
    - docs/decision-trees/06-macos-triage.md — READ THE FULL FILE (small; RESEARCH read it whole). Locate by content (line numbers drift): the MACSSO diamond and its two existing arms (`MACSSO -->|...| MACR7(...)` and `MACSSO -->|...| MACR8(...)`); the `click MACR7`/`click MACR8` lines; the classDef block — specifically `classDef escalateL2 ...`, `class MACR7,MACR8 resolved`, and `class MACE1 escalateL2`; the MACE1 leaf text (for the "Escalate to L2:" prefix convention); the Routing Verification table (last rows are the Platform SSO #35 / #36 rows); the legend (green=L1, red=L2); and the `## Version History` table (Phase 81 top row).
    - 87-PATTERNS.md §G — G1 (MACSSO arm + leaf node grammar + exact MACE2 line), G2 (click grammar + exact line), G3 (class assignment extension — extend `class MACE1 escalateL2` ONLY, classDef unchanged), G4 (Routing Verification row analog + row to append), G5 (Version History row).
    - 87-RESEARCH.md §3.1 (resolved MACSSO-label solution; exact Mermaid lines 1–5; 3-edge routing budget verification) and Pitfalls 3 (no new MAC3 branch), 4 (leaf MUST be red), 7 (modify the `class` assignment, NOT the `classDef`).
    - 87-CONTEXT.md D-06 (third arm under MACSSO, not a MAC3 branch), D-07 (RED escalateL2, click to #28, Routing Verification row, Version History line). Cross-ref: 85-CONTEXT D-05 (no L1 Kerberos runbook → L2 → red).
  </read_first>
  <action>
    Five append-only / in-place Mermaid edits to docs/decision-trees/06-macos-triage.md. Do NOT add a new MAC3 branch; do NOT modify the `classDef` style lines; do NOT change the `class MACR7,MACR8 resolved` assignment.

    (1) Add a THIRD arm to the MACSSO diamond, immediately AFTER the existing MACR8 arm line, with a Kerberos-TGT edge label and a new node `MACE2`: `MACSSO -->|"Kerberos TGT<br/>not acquired"| MACE2(["Escalate to L2:<br/>Kerberos SSO Investigation"])`. Node ID is `MACE2` (follows the `MACE1` red-escalation naming); leaf text uses the "Escalate to L2:" prefix mirroring MACE1. Edge label wording is your discretion within the Kerberos-TGT symptom (use a `<br/>` line break per the sibling arms' grammar).

    (2) Add a `click` directive immediately AFTER the `click MACR8` line: `click MACE2 "../l2-runbooks/28-macos-kerberos-sso-investigation.md"` (relative path from docs/decision-trees/ to docs/l2-runbooks/ — confirmed correct).

    (3) Extend the `class MACE1 escalateL2` assignment line IN PLACE to `class MACE1,MACE2 escalateL2` (RED). DO NOT modify the `classDef escalateL2 ...` line (Pitfall 7). DO NOT move MACE2 onto the `resolved`/green line (Pitfall 4 / D-07 LOCKED — #28 is an L2 runbook).

    (4) Append a Routing Verification row after the last existing row (the Platform SSO key-error #36 row): `| Kerberos SSO — TGT not acquired | Setup Assistant? Yes | Symptom: Platform SSO → Kerberos TGT | L2 escalation (#28) |` following the table grammar. Wording is your discretion within the symptom→destination structure.

    (5) Insert ONE newest-first Version History row at the top of the `## Version History` table body (after header/separator, before the Phase 81 row). Date 2026-06-24, text form: `Phase 87 (REF-03): added Kerberos SSO third arm under MACSSO (MACE2 → L2 #28) + 1 Routing Verification row`. Author `--`.
  </action>
  <verify>
    <automated>cd /d/claude/Autopilot && grep -q 'MACSSO -->.*MACE2' docs/decision-trees/06-macos-triage.md && grep -q 'click MACE2 "../l2-runbooks/28-macos-kerberos-sso-investigation.md"' docs/decision-trees/06-macos-triage.md && grep -q 'class MACE1,MACE2 escalateL2' docs/decision-trees/06-macos-triage.md && echo PASS || echo FAIL</automated>
    <automated>cd /d/claude/Autopilot && grep -q 'class MACR7,MACR8 resolved' docs/decision-trees/06-macos-triage.md && { ! grep -q 'MACE2 resolved' docs/decision-trees/06-macos-triage.md && echo "PASS MACR7/8 still green, MACE2 not green"; } || echo "FAIL class regression"</automated>
    <automated>cd /d/claude/Autopilot && grep -q 'Kerberos SSO' docs/decision-trees/06-macos-triage.md && grep -qE '\| .*Kerberos.*\|.*Setup Assistant\? Yes.*\|.*\|.*#28.*\|' docs/decision-trees/06-macos-triage.md && grep -q '2026-06-24 | Phase 87 (REF-03)' docs/decision-trees/06-macos-triage.md && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - docs/decision-trees/06-macos-triage.md contains a `MACE2` node, a `MACSSO -->` edge to it (the third MACSSO arm — NOT a new MAC3 branch), and a `click MACE2 "../l2-runbooks/28-macos-kerberos-sso-investigation.md"` line.
    - `MACE2` appears in an `escalateL2` class assignment (`class MACE1,MACE2 escalateL2`) — i.e., RED; it does NOT appear in any `resolved`/green assignment.
    - The `classDef escalateL2 ...` style line is unchanged; the `class MACR7,MACR8 resolved` assignment is unchanged (MACR7/MACR8 remain green).
    - The Routing Verification table has a new row for the Kerberos TGT symptom routing to L2 escalation (#28).
    - docs/decision-trees/06-macos-triage.md `## Version History` has a new newest-first row `| 2026-06-24 | Phase 87 (REF-03): ... | -- |`.
    - Routing stays within the 3-edge budget (MAC1 → MAC3 → MACSSO → MACE2); no new MAC3 branch was added.
  </acceptance_criteria>
  <done>MACE2 red leaf added under MACSSO with click to #28, class extended (MACR7/8 untouched), Routing Verification row + Version History stamped; all three verify commands print PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only phase. No executable code, no user input, no auth surface, no network or data handling. The surfaces in scope are Markdown link integrity and Mermaid diagram syntax within docs/. No runtime trust boundary exists.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-04 | Tampering (Integrity) | Mermaid edge/node/click/class lines in 06-macos-triage.md | mitigate | Acceptance criteria assert the MACE2 node, MACSSO edge, click target string, and class assignment are well-formed; verify greps confirm each line; classDef and MACR7/8 assignment asserted unchanged to prevent a malformed/regressed diagram. |
| T-87-05 | Tampering (Integrity) | Relative link targets (#28 runbook click, #platform-sso-attestation-command in-page anchor) | mitigate | #28 content target confirmed committed (RESEARCH §1, navigation-last); in-page anchor derived from the existing `#### Platform SSO Attestation Command` heading; acceptance criteria assert exact strings. |
| T-87-06 | Information Disclosure / Misrouting | Prohibited diagnostic commands (`klist -v`, `app-sso diagnose`) leaking unstable/banned guidance | mitigate | D-04 / P83 D-11/D-13 LOCKED; dedicated negative-grep verify gate asserts neither string appears; `app-sso platform -s` count asserted to stay at 1 (no re-statement). |
| T-87-07 | Denial (render break) | Mermaid syntax error breaking the triage tree render | mitigate | Edits are append-only and mirror verbatim in-file analog grammar (§G); only the `class MACE1` assignment is changed in place; classDef untouched; acceptance criterion asserts node/edge/click/class lines are present and well-formed. |

Block-on threshold is `high`. There are NO high-severity threats in this docs-wiring phase: no code execution, no secrets, no untrusted input, no package installs. All identified items are low-severity Markdown/Mermaid-integrity concerns fully mitigated by exact-string and negative-string acceptance gates.
</threat_model>

<verification>
- `#### Kerberos SSO Diagnostics` present in quick-ref-l2.md; `klist` present; `klist -v` and `app-sso diagnose` absent; `app-sso platform -s` count stays 1; #28 runbook bullet present.
- 00-index.md #28/#29 rows present (verify-only); no L1 escalation-mapping row added; file unedited.
- 06-macos-triage.md has MACE2 node + MACSSO edge + `click MACE2` to #28 + `class MACE1,MACE2 escalateL2`; MACR7/MACR8 stay `resolved`; classDef unchanged; new Routing Verification row present.
- quick-ref-l2.md and 06-macos-triage.md each carry a `2026-06-24 | Phase 87 (REF-03)` Version History row.
</verification>

<success_criteria>
- SC#3: docs/quick-ref-l2.md macOS section includes `app-sso platform -s` (already present, now pointer-referenced) AND `klist` (new) as Kerberos diagnostic commands.
- SC#4: docs/l2-runbooks/00-index.md macOS ADE Runbooks table includes #28 and #29 rows (verified present; no L1 mapping row fabricated).
- SC#5: docs/decision-trees/06-macos-triage.md includes a Kerberos SSO extension leaf (MACE2, RED) routing to L2 #28.
- Edited files (quick-ref-l2.md, 06-macos-triage.md) Version-History-stamped; 00-index.md unchanged; no banned commands present; no content files edited.
</success_criteria>

<output>
Create `.planning/phases/87-navigation-hub-integration/87-02-SUMMARY.md` when done.
</output>
