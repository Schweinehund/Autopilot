---
phase: 88-harness-lineage-bump-terminal-re-audit-milestone-close
plan: 03
type: execute
wave: 3
depends_on: ["88-02"]
files_modified:
  - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-03-AUDIT-RESULTS.md
autonomous: false
requirements: [HARN-03]
must_haves:
  truths:
    - "Axis 1 fresh `git clone --no-hardlinks` runs check-phase-83..87 standalone (non-apex only, per D-04)"
    - "Axis 2 cross-OS Linux GHA (audit-harness-v1.10-integrity.yml) completes with all 6 validator jobs + harness-run + linux-chain apex green"
    - "Axis 3 fresh zero-context sub-agent independently reproduces the validator results (folded into the Axis-1 fresh-clone dispatch per D-04)"
    - "The apex (check-phase-88) PASS/FAIL/SKIP count is taken from the WARM main tree + Linux GHA, NOT from a cold Windows fresh-clone (D-04 mitigation)"
    - "Cross-OS PASS/FAIL/SKIP counts are EXACT MATCH across the 8-row table"
  artifacts:
    - path: ".planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-03-AUDIT-RESULTS.md"
      provides: "3-axis re-audit evidence + 8-row cross-OS EXACT MATCH table + excluded-surfaces rationale"
      contains: "EXACT MATCH"
  key_links:
    - from: "88-03-AUDIT-RESULTS.md"
      to: "GHA run id (audit-harness-v1.10-integrity.yml)"
      via: "Axis 2 gh workflow run record"
      pattern: "gha_workflow_run|run id"
---

<objective>
Run the 3-axis terminal re-audit (Axis 1 fresh `git clone --no-hardlinks`, Axis 2 cross-OS Linux GHA, Axis 3 fresh zero-context sub-agent) and document a cross-OS EXACT MATCH of PASS/FAIL/SKIP counts. Apply the MANDATORY D-04 mitigation: the apex count comes from the WARM main tree + Linux GHA; the fresh clone runs only the fast non-apex validators (the Windows cold-clone O(n²) subprocess timeout WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 is OPEN and worse at the v1.10 apex depth [48..87]).

Purpose: HARN-03 (part 1) — produce auditor-independent, cross-OS-reproduced evidence that the v1.10 harness chain is deterministically green, recorded in 88-03-AUDIT-RESULTS.md for import into the close-gate MILESTONE-AUDIT.
Output: 88-03-AUDIT-RESULTS.md artifact-only commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-CONTEXT.md
@.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-RESEARCH.md
@.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-02-SUMMARY.md
@.planning/milestones/v1.9-phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Axis 1 + Axis 3 — fresh-clone, zero-context sub-agent runs non-apex validators</name>
  <files>(no repo files — produces console evidence for 88-03-AUDIT-RESULTS.md)</files>
  <read_first>
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-RESEARCH.md (CX-2 / D-04 exact PowerShell recipe; cross-OS validator set table; excluded surfaces; Pitfall 5)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-CONTEXT.md (D-03 axes definition; D-04 mitigation binding)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-02-SUMMARY.md (confirm Atom 2 is on origin/master and the HEAD SHA to clone)
  </read_first>
  <action>Confirm origin/master HEAD via `git -C "D:\claude\Autopilot" rev-parse HEAD`. Spawn ONE fresh gsd-executor sub-agent (zero context-carryover — this single dispatch serves BOTH Axis 1 physical independence AND Axis 3 logical independence per D-04). In that sub-agent, execute the PowerShell recipe from 88-RESEARCH.md CX-2: generate an 8-char `[0-9a-z]` random suffix, `$auditPath = Join-Path $env:TEMP "v1.10-audit-$rand"`, `git clone --no-hardlinks "D:\claude\Autopilot" $auditPath`, assert the clone HEAD equals the main HEAD, then from the clone run `node scripts/validation/check-phase-$n.mjs` for n in 83..87 (NON-APEX only — fast, each <5s) plus `node scripts/validation/v1.10-milestone-audit.mjs --verbose` and `--self-test`. Record each exit code and PASS/FAIL/SKIP line. DO NOT run check-phase-88.mjs (the apex) from the cold clone on Windows — that is the D-04 violation (Pitfall 5: spurious truncated FAILs break EXACT MATCH). After capture, `Remove-Item -Recurse -Force $auditPath` and assert zero orphan `v1.10-audit-*` temp dirs remain. Also capture the WARM main-tree apex result: `node "D:\claude\Autopilot\scripts\validation\check-phase-88.mjs" --verbose` (this is the authoritative apex count per D-04).</action>
  <verify>
    <automated>node D:/claude/Autopilot/scripts/validation/check-phase-88.mjs 2>&1 | tail -1 | grep -E "43 PASS, 0 FAIL, 0 SKIPPED" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Fresh clone HEAD equals main-tree HEAD (clone fidelity asserted).
    - check-phase-83..87 each exit 0 from the fresh clone; v1.10-milestone-audit.mjs --verbose = 15 PASS, --self-test = 9/9 from the clone.
    - check-phase-88.mjs was NOT run from the cold clone (D-04); the apex count (43 PASS / 0 FAIL / 0 SKIPPED) comes from the WARM main tree.
    - Temp clone removed; zero orphan `v1.10-audit-*` dirs remain.
    - The sub-agent was spawned with zero context carryover (Axis 3 independence).
  </acceptance_criteria>
  <done>Axis 1 + Axis 3 evidence captured: non-apex validators reproduced from a fresh clone by a zero-context sub-agent; apex taken from the warm tree per D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Axis 2 — cross-OS Linux GHA dispatch + result capture</name>
  <files>(no repo files — produces GHA run evidence for 88-03-AUDIT-RESULTS.md)</files>
  <read_first>
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-RESEARCH.md (Axis 2 gh dispatch + poll commands; expected GHA result; HARD ordering gate; Don't-Hand-Roll 3-part pre-flight gate)
    - .github/workflows/audit-harness-v1.10-integrity.yml (the workflow to dispatch; confirm job names)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-02-SUMMARY.md (confirm Atom 2 pushed to origin/master)
  </read_first>
  <action>Run the 3-part pre-flight gate first (Don't-Hand-Roll): `git log origin/master --oneline -1` must show the Atom 2 commit (Pitfall 6 — jobs fail `cannot find module` otherwise); `gh auth status` authenticated; `gh workflow list | grep audit-harness-v1.10`. Then dispatch Axis 2: `gh workflow run audit-harness-v1.10-integrity.yml --ref master`. Poll with `gh run list --workflow=audit-harness-v1.10-integrity.yml --limit 1` and `gh run view <RUN_ID>` until complete. Capture: the run id, and per-job conclusions — the 6 `check-phase-83..88` jobs + `harness-run` + `linux-chain-ubuntu-latest` must all be success; `rotting-external-quarterly` SKIPS (expected negative control on workflow_dispatch); `pin-helper-advisory` passes advisory. Record the Linux apex PASS/FAIL/SKIP from the linux-chain job log (the cross-OS authoritative apex count, paired with the warm Windows count from Task 1 per D-04).</action>
  <verify>
    <automated>gh run list --workflow=audit-harness-v1.10-integrity.yml --limit 1 2>&1 | grep -iE "completed|success" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight gate passes: Atom 2 on origin/master, gh authenticated, v1.10 workflow listed.
    - GHA run completes: all 6 check-phase-83..88 jobs + harness-run + linux-chain-ubuntu-latest = success.
    - rotting-external-quarterly SKIPS (negative control confirmed); pin-helper-advisory advisory pass.
    - Linux apex PASS/FAIL/SKIP captured and equals the warm Windows apex count (43 / 0 / 0).
    - Run id recorded for the MILESTONE-AUDIT frontmatter.
  </acceptance_criteria>
  <done>Axis 2 Linux GHA green; cross-OS apex count matches the warm Windows count; run id captured.</done>
</task>

<task type="auto">
  <name>Task 3: Author 88-03-AUDIT-RESULTS.md (8-row EXACT MATCH table) + artifact commit</name>
  <files>.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-03-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/milestones/v1.9-phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-03-PLAN.md (the v1.9 audit-results precedent + commit convention)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-RESEARCH.md (8-row cross-OS table definition; excluded-surfaces list with reasons; D-04 apex provenance)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-PATTERNS.md (Atomic Commit Convention — 88-03 message string)
  </read_first>
  <action>Author 88-03-AUDIT-RESULTS.md documenting: the 3-axis methodology (Axis 1 fresh clone, Axis 2 Linux GHA, Axis 3 zero-context sub-agent) and the D-04 mitigation rationale (apex from warm-tree × GHA; fresh clone for non-apex only because WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 is open and worse at [48..87]). Include the 8-row cross-OS EXACT MATCH table (rows: v1.10-milestone-audit.mjs incl. self-test; check-phase-82 prior-apex continuity; check-phase-83; 84; 85; 86; 87; check-phase-88 apex marked warm-tree×GHA per D-04), each row showing Windows PASS/FAIL/SKIP and Linux PASS/FAIL/SKIP with an EXACT MATCH verdict. List the excluded surfaces with reasons: pin-helper-advisory (CI-only advisory), rotting-external-quarterly (cron-only SKIP negative control), --self-test (folded into row 1), inherited chain 48..82 (covered transitively by apex row 8). Record the GHA run id. Commit ONLY this artifact: `docs(88-03): v1.10 3-axis terminal re-audit results — HARN-03 Axis 1+2+3 EXACT MATCH`.</action>
  <verify>
    <automated>test -f .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-03-AUDIT-RESULTS.md && grep -q "EXACT MATCH" .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-03-AUDIT-RESULTS.md && git show --stat HEAD | grep -c "88-03-AUDIT-RESULTS.md" | grep -qx 1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 88-03-AUDIT-RESULTS.md contains the 8-row cross-OS table; every row's Windows and Linux PASS/FAIL/SKIP match (EXACT MATCH verdict per row).
    - The apex row (check-phase-88) is explicitly annotated as warm-tree × Linux GHA per D-04 (NOT cold Windows clone).
    - Excluded surfaces are listed with reasons.
    - The GHA run id is recorded.
    - `git show --stat HEAD` shows ONLY 88-03-AUDIT-RESULTS.md (artifact-only commit) titled `docs(88-03): v1.10 3-axis terminal re-audit results — HARN-03 Axis 1+2+3 EXACT MATCH`.
  </acceptance_criteria>
  <done>88-03-AUDIT-RESULTS.md committed as the auditor-independent EXACT-MATCH evidence the close-gate (88-04) imports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the 3-axis cross-OS EXACT MATCH before close-gate</name>
  <files>.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-03-AUDIT-RESULTS.md</files>
  <action>Pause for the user to confirm the 8-row cross-OS EXACT MATCH and the D-04 apex provenance recorded in 88-03-AUDIT-RESULTS.md before Plan 88-04 (the close-gate) proceeds. This is a verification-after-automation checkpoint — all three axes were already run autonomously in Tasks 1-3; the human only confirms the recorded evidence is sound and consistent.</action>
  <what-built>3-axis terminal re-audit (Axis 1 fresh clone, Axis 2 Linux GHA, Axis 3 zero-context sub-agent) with the D-04 apex mitigation, documented as an 8-row cross-OS EXACT MATCH table in 88-03-AUDIT-RESULTS.md.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-03-AUDIT-RESULTS.md`.
    2. Confirm all 8 rows show Windows == Linux for PASS/FAIL/SKIP (EXACT MATCH).
    3. Confirm the apex row (check-phase-88) is sourced from warm-tree × Linux GHA, NOT a cold Windows clone (D-04).
    4. Confirm the recorded GHA run id corresponds to a green `audit-harness-v1.10-integrity.yml` run (`gh run view <id>`): 6 validator jobs + harness-run + linux-chain success; rotting-external-quarterly skipped.
    5. Confirm the warm main-tree apex prints `43 PASS, 0 FAIL, 0 SKIPPED`.
  </how-to-verify>
  <verify>
    <human-check>User confirms the 8-row EXACT MATCH table, the D-04 apex provenance, and the green GHA run.</human-check>
  </verify>
  <done>User approval recorded; 88-04 close-gate may proceed.</done>
  <resume-signal>Type "approved" to proceed to the close-gate (88-04), or describe the discrepancy (e.g. a non-matching row) to fix before close.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cold Windows fresh-clone → apex result | The O(n²) cold-Node-subprocess cascade at depth [48..87] can emit spurious truncated FAILs |
| local result claim → cross-OS evidence | A single-OS pass can hide platform-specific nondeterminism; Linux GHA is the independent axis |
| audit evidence → close-gate | The close-gate (88-04) flips 17/17 based on this artifact; fabricated/optimistic counts would be repudiation risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-09 | Spoofing | cold-clone apex spurious FAIL distorts EXACT MATCH | mitigate | D-04 MANDATORY: apex count from warm main tree × Linux GHA only; fresh clone runs check-phase-83..87 (non-apex) exclusively; Pitfall 5 |
| T-88-10 | Repudiation | traceability flip without re-audit evidence | mitigate | 88-03-AUDIT-RESULTS.md (artifact-only commit) is a prerequisite the close-gate imports; human-verify checkpoint gates the 8-row EXACT MATCH before 88-04 |
| T-88-11 | Tampering | orphan temp clones left under $env:TEMP | mitigate | Post-run assert zero `v1.10-audit-*` dirs remain (Remove-Item -Recurse -Force + count check) |
| T-88-12 | Spoofing | Axis 2 dispatched against a ref lacking the validators | mitigate | 3-part pre-flight gate: `git log origin/master -1` shows Atom 2, `gh auth status`, `gh workflow list` (Pitfall 6) |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero installs) |
</threat_model>

<verification>
- Warm main-tree `node check-phase-88.mjs` → 43 PASS / 0 FAIL / 0 SKIPPED.
- Fresh clone check-phase-83..87 → all exit 0; harness 15/15 + self-test 9/9.
- GHA `audit-harness-v1.10-integrity.yml` → all blocking jobs success; rotting-external-quarterly skipped.
- 88-03-AUDIT-RESULTS.md → 8-row EXACT MATCH table, apex annotated warm×GHA, run id recorded.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
- HARN-03 (part 1) satisfied: 3-axis re-audit complete with cross-OS PASS/FAIL/SKIP EXACT MATCH, apex sourced per D-04, evidence committed in 88-03-AUDIT-RESULTS.md.
- No orphan temp clones; no cold-Windows apex run.
- Human verification approved before close-gate.
</success_criteria>

<output>
Create `.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-03-SUMMARY.md` when done. Record: the GHA run id, the 8-row EXACT MATCH confirmation, the apex count (43/0/0), and the 88-03-AUDIT-RESULTS.md commit SHA (for the MILESTONE-AUDIT audit_results_sha field).
</output>
