---
phase: 88-harness-lineage-bump-terminal-re-audit-milestone-close
plan: 02
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - scripts/validation/check-phase-83.mjs
  - scripts/validation/check-phase-84.mjs
  - scripts/validation/check-phase-85.mjs
  - scripts/validation/check-phase-86.mjs
  - scripts/validation/check-phase-87.mjs
  - scripts/validation/check-phase-88.mjs
  - .github/workflows/audit-harness-v1.10-integrity.yml
autonomous: true
requirements: [HARN-02]
must_haves:
  truths:
    - "check-phase-83..87 each exit 0 standalone, asserting V-NN-SELF (dual-invariant) + V-NN-PRESENCE only — structural/self-referential, no v1.10 content coupling (D-01)"
    - "check-phase-88 apex reports 43 PASS / 0 FAIL / 0 SKIPPED with CHAIN_PHASES=[48..87] (40 entries) and CHAIN_SKIP empty"
    - "audit-harness-v1.10-integrity.yml is the 7th coexistence workflow with 6 per-validator jobs (83..88)"
    - "Predecessor v1.4-v1.9 frozen surfaces are byte-unchanged (20-surface diff gate EMPTY)"
    - "Atom 2 lands as ONE indivisible commit (7 files) and is pushed to origin/master (hard gate for 88-03 Axis 2)"
  artifacts:
    - path: "scripts/validation/check-phase-88.mjs"
      provides: "v1.10 chain-apex validator; CHAIN_PHASES=[48..87]"
      contains: "v1.10-milestone-audit.mjs"
    - path: "scripts/validation/check-phase-83.mjs"
      provides: "Phase 83 structural validator (SELF + PRESENCE)"
      contains: "includes(83)"
    - path: ".github/workflows/audit-harness-v1.10-integrity.yml"
      provides: "7th coexistence CI workflow (6 jobs check-phase-83..88 + harness-run + linux-chain apex)"
      contains: "v1.10-milestone-audit.mjs"
  key_links:
    - from: "scripts/validation/check-phase-88.mjs"
      to: "scripts/validation/v1.10-milestone-audit.mjs"
      via: "HARNESS const + V-88-AUDIT-HARNESS check"
      pattern: "v1\\.10-milestone-audit\\.mjs"
    - from: ".github/workflows/audit-harness-v1.10-integrity.yml"
      to: "scripts/validation/check-phase-88.mjs"
      via: "linux-chain-ubuntu-latest job runs the apex"
      pattern: "check-phase-88\\.mjs"
---

<objective>
Author the six per-phase validators (check-phase-83..88.mjs) and the 7th coexistence CI workflow (audit-harness-v1.10-integrity.yml) as Atom 2 of the harness lineage bump, then push to origin/master. check-phase-83..87 are LIGHTWEIGHT structural/self-referential validators (D-01); check-phase-88 is the chain-apex extending CHAIN_PHASES to [48..87].

Purpose: HARN-02 — ship the validator chain + CI surface as ONE indivisible commit, with V19 (pinned in 88-01) read structurally only for PREDECESSOR surfaces, never coupled to v1.10 content (D-01/D-02). Push to origin/master is the hard ordering gate that lets 88-03 dispatch the Axis-2 Linux GHA workflow.
Output: 7-file Atom 2 indivisible commit + `git push origin master`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-CONTEXT.md
@.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-RESEARCH.md
@.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-PATTERNS.md
@.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-CONVENTIONS.md
@.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-01-SUMMARY.md
@.planning/milestones/v1.9-phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author check-phase-83..87.mjs (lightweight structural/self-referential validators)</name>
  <files>scripts/validation/check-phase-83.mjs, scripts/validation/check-phase-84.mjs, scripts/validation/check-phase-85.mjs, scripts/validation/check-phase-86.mjs, scripts/validation/check-phase-87.mjs</files>
  <read_first>
    - scripts/validation/check-phase-75.mjs (the 97-line "AUDIT-HARNESS + SELF only" structural shell to copy; CHAIN_PHASES=[], CHAIN_SKIP=new Set([]), SELF dual-invariant + PRESENCE)
    - .planning/phases/86-chain-health-pass/86-01-SUMMARY.md and 86-02-SUMMARY.md (confirm Phase 86 primary deliverable file path for V-86-PRESENCE)
    - .planning/phases/87-navigation-hub-integration/87-01-SUMMARY.md and 87-02-SUMMARY.md (confirm Phase 87 primary deliverable; docs/index.md per research A3)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-PATTERNS.md (per-phase substitution table + transposed-digit guard)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-CONTEXT.md (D-01 structural-only / D-02 no v1.10 content coupling)
  </read_first>
  <action>For each phase NN in 83,84,85,86,87: copy check-phase-75.mjs verbatim and substitute EVERY `75` with NN (banner string, V-NN-SELF id/name, the `includes(NN)` guard, all comments), and set the DELIVERABLE const per the 88-PATTERNS.md mapping — 83: `docs/admin-setup-macos/10-kerberos-sso-extension.md`; 84: `docs/admin-setup-macos/11-graph-api-platform-credential.md`; 85: `docs/l2-runbooks/28-macos-kerberos-sso-investigation.md`; 86: confirm against 86-SUMMARY (likely `.planning/phases/.../73-RETRO-INVENTORY.md` or a check-phase-58.mjs marker — use the headline deliverable the SUMMARY names); 87: `docs/index.md` (confirm against 87-SUMMARY). Keep these LIGHTWEIGHT per D-01: CHAIN_PHASES=[], CHAIN_SKIP=new Set([]), exactly two checks (V-NN-SELF dual-invariant + V-NN-PRESENCE non-empty file check). They MUST NOT call readAtV19Close() or any git show to assert v1.10 content (D-01/D-02 — PRESENCE confirms file exists and is non-empty, nothing more). After writing each file, run the transposed-digit guard to confirm the SELF `includes(NN)` embeds the CORRECT phase number (copy-paste of a wrong NN is the most common error). Carry the CRLF-normalizing readFile() and the runner loop verbatim from the shell.</action>
  <verify>
    <automated>for n in 83 84 85 86 87; do node scripts/validation/check-phase-$n.mjs || exit 1; m=$(node -e "console.log(require('fs').readFileSync('scripts/validation/check-phase-'+$n+'.mjs','utf8').match(/includes\\((\\d+)\\)/)[1])"); [ "$m" = "$n" ] || { echo "transposed digit in $n: got $m"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 5 validators (check-phase-83..87) exit 0 when run standalone.
    - Each check-phase-NN.mjs SELF check embeds `includes(NN)` with the CORRECT N (transposed-digit guard passes for all 5).
    - Each has CHAIN_PHASES=[] and CHAIN_SKIP=new Set([]) (lightweight; no chain fan-out).
    - None contains `readAtV19Close` or `git show` against a v1.10 content path (D-01: `grep -L "readAtV19Close" scripts/validation/check-phase-8{3,4,5,6,7}.mjs` lists all 5).
    - V-86-PRESENCE and V-87-PRESENCE point to files confirmed present per their phase SUMMARYs.
  </acceptance_criteria>
  <done>Five lightweight structural validators authored, each PASS standalone, each with the correct phase number and a verified-present deliverable, with zero v1.10 content coupling.</done>
</task>

<task type="auto">
  <name>Task 2: Author check-phase-88.mjs chain-apex (CHAIN_PHASES=[48..87])</name>
  <files>scripts/validation/check-phase-88.mjs</files>
  <read_first>
    - scripts/validation/check-phase-82.mjs (the 173-line apex Path-A source; CHAIN_PHASES [48..81], NESTED short-circuit, isPeer 600s threshold, V-82-AUDIT/AUDIT-HARNESS/SELF, resolveArchivedPhasePath)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-PATTERNS.md (check-phase-88 substitution table + verbatim CHAIN_PHASES 40-entry value + V-88-AUDIT SKIP-PASS block)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-RESEARCH.md (Atom 2 apex section; expected 43 PASS; DO NOT add VPP-01a/b analogs)
  </read_first>
  <action>Copy check-phase-82.mjs to scripts/validation/check-phase-88.mjs and apply the substitution table from 88-PATTERNS.md: line 1/4/5 comments `82`→`88` and `v1.9`→`v1.10`; HARNESS const → `scripts/validation/v1.10-milestone-audit.mjs`; CHAIN_PHASES → the verbatim 40-entry array `[48,49,...,87]` (adds 82,83,84,85,86,87); CHAIN_SKIP stays `new Set([])`; V-88-AUDIT points to `88-VERIFICATION.md` / `Phase 88` (SKIP-PASS form until 88-04 lands the verification file — use the resolveArchivedPhasePath block from 88-PATTERNS.md with archive dir `['v1.10-phases']`); V-88-AUDIT-HARNESS wording → `v1.10-milestone-audit.mjs exits 0 (current-milestone harness)`; V-88-SELF dual-invariant → `!CHAIN_PHASES.includes(88)` AND `CHAIN_SKIP.size === 0`; runner banner → `check-phase-88 -- Phase 88 …`. Carry the NESTED short-circuit (`CHECK_PHASE_NESTED === '1'`) and the isPeer 600s timeout threshold verbatim (phases 83-88 are all ≥67 → 600s). DO NOT add VPP-01a/b analogs (no corpus rename in v1.10). Verify CHAIN_PHASES.length === 40 and the SELF guard embeds 88 not 82.</action>
  <verify>
    <automated>node scripts/validation/check-phase-88.mjs 2>&1 | tail -1 | grep -E "43 PASS, 0 FAIL" && node -e "const c=require('fs').readFileSync('scripts/validation/check-phase-88.mjs','utf8'); if(!c.includes('v1.10-milestone-audit.mjs'))process.exit(1); const m=c.match(/CHAIN_PHASES\\s*=\\s*\\[([^\\]]+)\\]/)[1].split(',').length; if(m!==40){console.log('CHAIN len '+m);process.exit(1)}; if(c.match(/includes\\((\\d+)\\)/)[1]!=='88')process.exit(1); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-88.mjs` reports 43 PASS / 0 FAIL / 0 SKIPPED (40 CHAIN + AUDIT + AUDIT-HARNESS + SELF).
    - CHAIN_PHASES has exactly 40 entries [48..87]; CHAIN_SKIP is empty.
    - HARNESS const references `v1.10-milestone-audit.mjs`; V-88-SELF embeds `includes(88)`.
    - check-phase-88.mjs contains NO VPP-01a/b assertions.
    - NESTED short-circuit (`CHECK_PHASE_NESTED`) is present (prevents O(n²) recursive expansion).
  </acceptance_criteria>
  <done>The v1.10 chain-apex passes 43/43 with the correct 40-entry chain and harness wiring, ready for the CI workflow to invoke it.</done>
</task>

<task type="auto">
  <name>Task 3: Author audit-harness-v1.10-integrity.yml + predecessor byte-unchanged gate + Atom 2 indivisible commit + push</name>
  <files>.github/workflows/audit-harness-v1.10-integrity.yml</files>
  <read_first>
    - .github/workflows/audit-harness-v1.9-integrity.yml (the 257-line Path-A source; structural skeleton copies verbatim)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-PATTERNS.md (workflow repoint table; linux-chain + per-validator job templates; predecessor-untouched constraint)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-RESEARCH.md (Atom 2 file set; 20-surface byte-unchanged gate command; push ordering gate; Pitfall 6)
    - .planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-CONVENTIONS.md (captured pre-Phase-88 anchor SHA for the diff gate; 20 frozen surfaces)
  </read_first>
  <action>Copy audit-harness-v1.9-integrity.yml to .github/workflows/audit-harness-v1.10-integrity.yml and apply the 88-PATTERNS.md repoint table: header comment + `name:` → `v1.10` / `Audit Harness v1.10 Integrity` / `7th coexistence workflow` / `chain-83..88`; on.pull_request.paths globs → `v1.10-*` equivalents; parse job → `v1.10-audit-allowlist.json`; path-match job → both `v1.10-audit-allowlist.json` and `v1.10-milestone-audit.mjs`; harness-run job → `v1.10-milestone-audit.mjs`; linux-chain-ubuntu-latest apex → `check-phase-88.mjs` (and `48..87` in the comment) carrying the FETCH-DEPTH-01 + LF-fidelity (`git config --global core.autocrlf false` BEFORE checkout) + `fetch-depth: 0` contracts verbatim; REPLACE the 8 per-validator jobs (check-phase-75..82) with 6 jobs (check-phase-83..88), each `timeout-minutes: 15`, `continue-on-error: false`, `fetch-depth: 0`, node 20; rotting-external-quarterly job carries structurally verbatim with only its `v1.9-audit-allowlist.json` ref repointed (this IS the negative control — its cron-only SKIP on workflow_dispatch is expected); pin-helper-advisory carries verbatim. Both crons carry verbatim. Do NOT loosen any action/runner ref vs v1.9 (no `permissions: write`, no looser action versions). Then run the 20-surface predecessor byte-unchanged HARD gate (`git diff <pre-88-anchor-SHA> HEAD -- <20 surfaces>` from 88-RESEARCH.md / 88-CONVENTIONS.md) using the anchor SHA from 88-01-SUMMARY; output MUST be EMPTY — if non-empty, STOP and identify the mutated surface before committing. Finally stage ALL 7 Atom-2 files (check-phase-83..88 + the workflow) and commit as ONE indivisible commit `feat(88-02): v1.10 validators + CI surface — HARN-02/03 (atomic SC#1 Atom 2)`, then `git push origin master` (HARD ordering gate for 88-03 Axis 2 — the GHA check-phase-83..88 jobs fail with `cannot find module` if dispatched against a ref lacking them; per Pitfall 6).</action>
  <verify>
    <automated>git diff $(node -e "process.stdout.write(require('fs').readFileSync('.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-01-SUMMARY.md','utf8').match(/anchor[^a-f0-9]*([0-9a-f]{7,40})/i)[1])") HEAD -- .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml .github/workflows/audit-harness-v1.7-integrity.yml .github/workflows/audit-harness-v1.8-integrity.yml .github/workflows/audit-harness-v1.9-integrity.yml scripts/validation/v1.4-milestone-audit.mjs scripts/validation/v1.4.1-milestone-audit.mjs scripts/validation/v1.5-milestone-audit.mjs scripts/validation/v1.6-milestone-audit.mjs scripts/validation/v1.7-milestone-audit.mjs scripts/validation/v1.8-milestone-audit.mjs scripts/validation/v1.9-milestone-audit.mjs scripts/validation/v1.4-audit-allowlist.json scripts/validation/v1.4.1-audit-allowlist.json scripts/validation/v1.5-audit-allowlist.json scripts/validation/v1.6-audit-allowlist.json scripts/validation/v1.7-audit-allowlist.json scripts/validation/v1.8-audit-allowlist.json scripts/validation/v1.9-audit-allowlist.json | wc -l | grep -qx 0 && git show --stat HEAD | grep -E "check-phase-8[3-8]|audit-harness-v1.10" | wc -l | grep -qx 7 && git log origin/master --oneline -1 | grep "88-02" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - audit-harness-v1.10-integrity.yml exists with `name: Audit Harness v1.10 Integrity`, 6 per-validator jobs (check-phase-83..88), linux-chain apex running check-phase-88.mjs, and the LF-fidelity + fetch-depth:0 contracts intact.
    - 20-surface predecessor byte-unchanged diff (`git diff <anchor> HEAD -- <20 surfaces>`) produces EMPTY output.
    - `git show --stat HEAD` lists EXACTLY the 7 Atom-2 files (6 validators + 1 workflow) under one commit titled `feat(88-02): v1.10 validators + CI surface — HARN-02/03 (atomic SC#1 Atom 2)`.
    - `git log origin/master --oneline -1` shows the Atom 2 commit (push succeeded — the Axis-2 ordering gate).
    - No predecessor workflow or harness/allowlist file appears in the Atom 2 diff.
  </acceptance_criteria>
  <done>The 7th coexistence workflow + 6 validators ship as ONE indivisible Atom 2 commit, predecessors are byte-unchanged, and the commit is on origin/master — satisfying HARN-02 and unblocking 88-03 Axis 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v1.10 doc content → validators | Validators must assert STRUCTURE/LINEAGE only; coupling to content re-creates chain-health debt (D-01) |
| predecessor frozen surfaces → Atom 2 changes | Any byte change to v1.4-v1.9 harnesses/workflows breaks the coexistence-immutability invariant |
| local Atom 2 commit → origin/master | CI Linux jobs resolve modules from the pushed ref; an un-pushed ref fails with `cannot find module` |
| GitHub Actions workflow → runners | Workflow must not request broader permissions or looser action refs than the v1.9 predecessor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-04 | Tampering | content-coupling regression in check-phase-83..88 | mitigate | D-01 structural/self-referential only: PRESENCE = file-exists-and-non-empty; no blob-hash/anchor-match; `grep -L readAtV19Close` confirms validators contain no frozen-content reads of v1.10 |
| T-88-05 | Tampering | predecessor v1.4-v1.9 frozen surface mutation | mitigate | 20-surface `git diff <anchor> HEAD` HARD gate must be EMPTY before the Atom 2 commit |
| T-88-06 | Tampering | transposed digit in check-phase-NN SELF guard | mitigate | post-authoring guard: extracted `includes(NN)` must equal the file's phase number for all of 83..88 |
| T-88-07 | Elevation of Privilege | CI workflow excessive permissions | mitigate | Path-A copy inherits v1.9 least-privilege defaults (no `permissions: write`, no secrets injected, `continue-on-error: false` on blocking jobs); no action/runner refs loosened |
| T-88-08 | Spoofing | Atom 2 not on origin/master before Axis 2 | mitigate | `git log origin/master --oneline -1` must show the Atom 2 commit before 88-03 dispatches the GHA workflow (Pitfall 6) |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero installs) |
</threat_model>

<verification>
- `for n in 83 84 85 86 87 88; do node scripts/validation/check-phase-$n.mjs || exit 1; done` → all exit 0.
- `node scripts/validation/check-phase-88.mjs` → 43 PASS / 0 FAIL / 0 SKIPPED.
- 20-surface `git diff <anchor> HEAD -- <surfaces>` → EMPTY.
- `git show --stat HEAD` → exactly 7 Atom-2 files in one commit.
- `git log origin/master --oneline -1` → Atom 2 commit present.
</verification>

<success_criteria>
- HARN-02 satisfied: 6 validators + 7th coexistence workflow shipped as ONE indivisible Atom 2 commit; check-phase-88 apex = 43/43; V19 (from 88-01) read structurally for predecessors only (D-01/D-02).
- Predecessor v1.4-v1.9 frozen surfaces byte-unchanged (20-surface gate EMPTY).
- Atom 2 pushed to origin/master (Axis-2 ordering gate for 88-03 met).
</success_criteria>

<output>
Create `.planning/phases/88-harness-lineage-bump-terminal-re-audit-milestone-close/88-02-SUMMARY.md` when done. Record: the Atom 2 commit SHA, confirmation of the EMPTY 20-surface diff, the apex PASS count (43), and confirmation of the origin/master push.
</output>
