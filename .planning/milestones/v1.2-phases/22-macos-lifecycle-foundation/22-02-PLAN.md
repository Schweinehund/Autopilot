---
phase: 22-macos-lifecycle-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reference/macos-commands.md
  - docs/reference/macos-log-paths.md
  - docs/reference/endpoints.md
  - docs/reference/00-index.md
  - docs/index.md
autonomous: true
requirements: [MLIF-02, MLIF-03]
must_haves:
  truths:
    - "Technician can look up macOS Terminal diagnostic commands with synopsis, parameters, expected output, and examples in a dedicated reference file"
    - "Technician can find macOS log paths, Intune agent log locations, unified log subsystems, and configuration profile filesystem locations in a dedicated reference file"
    - "Admin can find all Apple and Microsoft network endpoints required for ADE enrollment in a single reference file with test commands for each endpoint"
    - "Reference index includes macOS entries alongside existing Windows entries"
    - "docs/index.md macOS section links to the lifecycle doc, reference files, and endpoints"
  artifacts:
    - path: "docs/reference/macos-commands.md"
      provides: "macOS Terminal diagnostic commands reference"
      min_lines: 150
      contains: "### profiles"
    - path: "docs/reference/macos-log-paths.md"
      provides: "macOS log paths and configuration profile locations reference"
      min_lines: 40
      contains: "/Library/Logs/Microsoft/Intune"
    - path: "docs/reference/endpoints.md"
      provides: "Extended with macOS ADE Endpoints section"
      contains: "## macOS ADE Endpoints"
    - path: "docs/reference/00-index.md"
      provides: "Updated reference index with macOS entries and platform: all"
      contains: "platform: all"
    - path: "docs/index.md"
      provides: "Updated macOS Provisioning section with lifecycle and reference links"
      contains: "00-ade-lifecycle.md"
  key_links:
    - from: "docs/reference/macos-commands.md"
      to: "docs/reference/macos-log-paths.md"
      via: "See Also cross-reference"
      pattern: "macos-log-paths\\.md"
    - from: "docs/reference/00-index.md"
      to: "docs/reference/macos-commands.md"
      via: "Reference listing link"
      pattern: "macos-commands\\.md"
    - from: "docs/index.md"
      to: "docs/macos-lifecycle/00-ade-lifecycle.md"
      via: "macOS Provisioning section navigation link"
      pattern: "macos-lifecycle/00-ade-lifecycle\\.md"
---

<objective>
Create macOS diagnostic reference files (Terminal commands and log paths), extend the network endpoints reference with macOS ADE endpoints, and update navigation (reference index + docs hub) to link all Phase 22 artifacts.

Purpose: Provides technicians with macOS-equivalent reference materials for log paths, diagnostic commands, and network endpoints (paralleling the existing Windows powershell-ref.md, registry-paths.md, and endpoints.md). Updates navigation so all Phase 22 content is discoverable.
Output: 2 new reference files, 3 updated files (endpoints.md, 00-index.md, index.md)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-macos-lifecycle-foundation/22-CONTEXT.md
@.planning/phases/22-macos-lifecycle-foundation/22-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From docs/reference/powershell-ref.md (pattern for macos-commands.md):
- Frontmatter: last_verified, review_by, applies_to, audience
- Version gate blockquote
- Per-function: ### FunctionName, **Module:**, **Synopsis:**, **Parameters:** table, **Returns:**, **Example:** code block
- 346 lines total, 12 functions documented

From docs/reference/registry-paths.md (pattern for macos-log-paths.md):
- Frontmatter: last_verified, review_by, applies_to, audience
- Version gate blockquote
- Single table: Registry Path | Purpose | Referenced By | Notes
- 24 lines total, 8 rows

From docs/reference/endpoints.md (file to extend):
- Frontmatter: last_verified, review_by, applies_to: both, audience: both
- H1: Autopilot Network Endpoints Reference (rename to platform-neutral per D-19)
- ## Required Endpoints table (13 rows, Windows-specific)
- ## Test Commands with ### PowerShell and ### Curl subsections
- 57 lines total
- Must add ## macOS ADE Endpoints section BELOW existing Windows content per D-15

From docs/reference/00-index.md (file to update):
- Frontmatter: platform: Windows (change to platform: all per D-14)
- H1: Reference Documentation
- ## Existing References section listing endpoints.md, powershell-ref.md, registry-paths.md
- Add macOS References section listing macos-commands.md, macos-log-paths.md

From docs/index.md lines 94-117 (macOS Provisioning section to update):
- ## macOS Provisioning H2 with intro paragraph
- ### Service Desk (L1) - TBD placeholders for Phase 24
- ### Desktop Engineering (L2) - TBD placeholders for Phase 24
- ### Admin Setup - TBD placeholder for Phase 23
- Need to add lifecycle and reference links across all three role sections
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create macOS Terminal commands reference and log paths reference files (MLIF-02)</name>
  <files>docs/reference/macos-commands.md, docs/reference/macos-log-paths.md</files>
  <read_first>
    - docs/reference/powershell-ref.md (full file - structural pattern to mirror for macos-commands.md: Synopsis/Parameters/Returns/Example per function)
    - docs/reference/registry-paths.md (full file - structural pattern to mirror for macos-log-paths.md: table with Path/Purpose/Referenced By/Notes)
    - .planning/phases/22-macos-lifecycle-foundation/22-RESEARCH.md (verified command list lines 397-438, log paths lines 442-458)
    - docs/_glossary-macos.md (glossary terms for cross-references)
  </read_first>
  <action>
**Create `docs/reference/macos-commands.md`:**

Per D-10, D-11, and D-13, this file mirrors the powershell-ref.md pattern but for macOS Terminal diagnostic commands. Include `last_verified` with macOS version.

Frontmatter (YAML between triple-dash delimiters): `last_verified: 2026-04-14`, `review_by: 2026-07-13`, `applies_to: ADE`, `audience: L2`, `platform: macOS`

Version gate: `> **Version gate:** This reference documents macOS Terminal commands for Intune and ADE diagnostics. Commands are OS-shipped unless noted. Last verified against macOS 14 (Sonoma). For Windows PowerShell equivalents, see [PowerShell Reference](powershell-ref.md).`

H1: `# macOS Terminal Commands Reference`

Intro paragraph: "Field guide to macOS Terminal commands for Intune and ADE enrollment diagnostics. These are OS-shipped binaries and Microsoft-provided tools -- not project-owned code. The structure mirrors [PowerShell Reference](powershell-ref.md) for cross-platform consistency."

Then document these 6 commands/tools, each as an H2 with subsections matching the research content (lines 397-438):

**`## profiles`** (OS-shipped binary)
- **Synopsis:** Manages configuration profiles on macOS. Primary tool for viewing installed profiles, checking enrollment status, and triggering re-enrollment.
- **last_verified:** macOS 14 (Sonoma)
- **Subcommands and Flags:** table with columns: Flag/Subcommand | Purpose
  - `show` | Lists all installed profiles with full detail
  - `list` | Lists profile identifiers (shorter output)
  - `status -type enrollment` | Shows MDM enrollment status and user-approved MDM state
  - `-C -v` | Lists all system-domain profiles verbosely
  - `renew -type enrollment` | Triggers ADE re-enrollment on an already-set-up Mac (macOS 10.13+)
- **Expected Output (profiles status -type enrollment):** show sample output block from research
- **Example:** code block with 3 examples (check enrollment, list profiles with details, list identifiers only) from research lines 212-220. Per D-13, include output interpretation guidance explaining each field in the enrollment status output.

**`## log show / log stream`** (OS unified logging)
- **Synopsis:** Queries (log show) or streams (log stream) the macOS unified log. Primary tool for filtering MDM and profile management events by subsystem.
- **last_verified:** macOS 14 (Sonoma)
- **Key Subsystem Identifiers:** table from research lines 425-431:
  - `com.apple.ManagedClient` | Configuration profile installation, MDM command processing
  - `com.apple.ManagedClient.cloudconfigurationd` | ADE/DEP cloud configuration daemon (Setup Assistant enrollment)
  - `com.apple.securityd` | Keychain interactions triggered by MDM profiles
- **Parameters:** table with common flags: `--predicate 'subsystem == "..."'`, `--info`, `--last 1h`, `--debug`
- **Enable Debug Logging:** code block from research lines 433-437 (sudo log config commands)
- **Example:** code blocks for both `log show` and `log stream` from research lines 409-410

**`## system_profiler`** (OS-shipped)
- **Synopsis:** Reports detailed system configuration. The `SPConfigurationProfileDataType` data type outputs all installed configuration profiles in plist format.
- **last_verified:** macOS 14 (Sonoma)
- **Parameters:** table with key data type flag: `SPConfigurationProfileDataType`
- **Example:** `system_profiler SPConfigurationProfileDataType` from research line 411

**`## defaults read`** (OS-shipped)
- **Synopsis:** Reads macOS defaults (preference) values. Used to inspect Intune-managed preference plists.
- **last_verified:** macOS 14 (Sonoma)
- **Parameters:** table: domain path | key (optional)
- **Example:** `defaults read /Library/Managed\ Preferences/com.microsoft.intune` from research line 412

**`## pgrep`** (OS-shipped)
- **Synopsis:** Searches for processes by name. Used to verify Intune management agent processes are running.
- **last_verified:** macOS 14 (Sonoma)
- **Key Processes:** IntuneMdmDaemon, IntuneMdmAgent
- **Example:** `pgrep -il "^IntuneMdm"` from research line 413

**`## IntuneMacODC`** (Microsoft tool)
- **Synopsis:** Microsoft-provided diagnostic collection script. Collects all Intune diagnostic logs and system info into a zip file. NOT OS-shipped -- must be downloaded.
- **last_verified:** macOS 14 (Sonoma), requires macOS 10.15+
- **Acquisition:** code block from research lines 418-421 (curl download, chmod, sudo run)
- **Output:** IntuneMacODC.zip in ODC/ subfolder, opened automatically in Finder
- **Example:** `sudo ./IntuneMacODC.sh` from research line 414

**See Also section:**
- `[macOS Log Paths Reference](macos-log-paths.md)` -- Log file locations referenced by these commands
- `[macOS Provisioning Glossary](../_glossary-macos.md)` -- ADE and ABM terminology
- `[PowerShell Reference](powershell-ref.md)` -- Windows equivalent diagnostic commands
- `[Windows vs macOS Concept Comparison](../windows-vs-macos.md)` -- Cross-platform diagnostic tool mapping

**Create `docs/reference/macos-log-paths.md`:**

Per D-10 and D-12, this file mirrors registry-paths.md as a table with macOS version annotations.

Frontmatter (YAML between triple-dash delimiters): `last_verified: 2026-04-14`, `review_by: 2026-07-13`, `applies_to: ADE`, `audience: L2`, `platform: macOS`

Version gate: `> **Version gate:** This reference documents macOS log paths and configuration profile locations for Intune and ADE diagnostics. Paths verified against macOS 14 (Sonoma). Version-specific differences are noted per row. For Windows registry paths, see [Registry Paths Reference](registry-paths.md).`

H1: `# macOS Log Paths and Configuration Profile Locations`

Intro paragraph: "Canonical reference for macOS log file locations, Intune agent paths, and configuration profile storage. Runbooks and investigation guides link here rather than defining paths inline."

Table with columns: Path | Purpose | Referenced By | Notes

Populate from research lines 448-458 with these 9 rows:
1. `/Library/Logs/Microsoft/Intune/IntuneMDMDaemon*.log` | Intune management daemon -- PKG installs, DMG installs, root-context shell scripts, policy application | IntuneMacODC, L2 runbooks | System-level log. 6 pipe-delimited columns: timestamp | process | level | PID | task | detail
2. `~/Library/Logs/Microsoft/Intune/IntuneMDMAgent*.log` | Intune management agent -- user-context shell scripts, user-level policy | IntuneMacODC, L2 runbooks | May be absent on macOS 15 (known issue as of 2026-04); check system log instead
3. `/Library/Logs/Microsoft/Intune/CompanyPortal*.log` | Company Portal enrollment events, device registration, compliance | IntuneMacODC, L2 runbooks | Generated by Company Portal app during enrollment and compliance checks
4. `/Library/Intune/Microsoft Intune Agent.app` | Intune Management Extension agent binary | pgrep, Activity Monitor | Not visible in /Applications. Check via `pgrep -il "^IntuneMdm"`
5. `/private/var/db/ConfigurationProfiles/` | Binary store of all installed configuration profiles | `profiles` command | Binary format -- do not edit directly. Use `profiles show` or System Settings to inspect
6. `/Library/Managed Preferences/` | MCX-derived managed preference plists applied to device | `defaults read` | Managed plists written here by mdmclient when profiles apply
7. `/Library/Logs/ManagedClient/ManagedClient.log` | Legacy MCX logging (pre-unified log) | Older macOS / legacy L2 reference | Superseded by unified log on macOS 10.12+; still present on some systems
8. Unified log -- subsystem `com.apple.ManagedClient` | MDM command processing, profile installation events | `log show --predicate 'subsystem == "com.apple.ManagedClient"'` | macOS 10.12+ only; use `log` command to query
9. Unified log -- subsystem `com.apple.ManagedClient.cloudconfigurationd` | ADE enrollment discovery during Setup Assistant | `log stream --predicate 'subsystem contains "cloudconfigurationd"'` | Useful for Setup Assistant enrollment failures

**See Also section:**
- `[macOS Terminal Commands Reference](macos-commands.md)` -- Commands that query these log paths
- `[Registry Paths Reference](registry-paths.md)` -- Windows equivalent path reference
  </action>
  <verify>
    <automated>bash -c "test -f docs/reference/macos-commands.md && test -f docs/reference/macos-log-paths.md && echo 'BOTH FILES EXIST' && grep -c '## ' docs/reference/macos-commands.md && grep -c 'profiles' docs/reference/macos-commands.md && grep 'platform: macOS' docs/reference/macos-commands.md && grep 'platform: macOS' docs/reference/macos-log-paths.md && grep -c '/Library/' docs/reference/macos-log-paths.md"</automated>
  </verify>
  <acceptance_criteria>
    - docs/reference/macos-commands.md exists with 150+ lines
    - docs/reference/macos-commands.md contains `platform: macOS` in frontmatter
    - docs/reference/macos-commands.md contains `# macOS Terminal Commands Reference` as H1
    - docs/reference/macos-commands.md contains `## profiles` heading
    - docs/reference/macos-commands.md contains `## log show / log stream` heading
    - docs/reference/macos-commands.md contains `## system_profiler` heading
    - docs/reference/macos-commands.md contains `## defaults read` heading
    - docs/reference/macos-commands.md contains `## pgrep` heading
    - docs/reference/macos-commands.md contains `## IntuneMacODC` heading
    - docs/reference/macos-commands.md contains `profiles status -type enrollment`
    - docs/reference/macos-commands.md contains `com.apple.ManagedClient` (unified log subsystem)
    - docs/reference/macos-commands.md contains `sudo ./IntuneMacODC.sh`
    - docs/reference/macos-commands.md contains `last_verified:` with macOS version text
    - docs/reference/macos-commands.md contains `macos-log-paths.md` in See Also
    - docs/reference/macos-log-paths.md exists with 40+ lines
    - docs/reference/macos-log-paths.md contains `platform: macOS` in frontmatter
    - docs/reference/macos-log-paths.md contains `# macOS Log Paths and Configuration Profile Locations` as H1
    - docs/reference/macos-log-paths.md contains `/Library/Logs/Microsoft/Intune/IntuneMDMDaemon`
    - docs/reference/macos-log-paths.md contains `/private/var/db/ConfigurationProfiles/`
    - docs/reference/macos-log-paths.md contains `macOS 15` (known issue annotation)
    - docs/reference/macos-log-paths.md contains `macOS 10.12` (version annotation for unified log)
    - docs/reference/macos-log-paths.md contains `macos-commands.md` in See Also
  </acceptance_criteria>
  <done>Two macOS reference files exist: macos-commands.md documents 6 tools (profiles, log show/stream, system_profiler, defaults read, pgrep, IntuneMacODC) with Synopsis/Parameters/Output/Example pattern, and macos-log-paths.md documents 9 log paths with version annotations in a table matching registry-paths.md format.</done>
</task>

<task type="auto">
  <name>Task 2: Extend endpoints.md with macOS ADE section and update reference index (MLIF-03)</name>
  <files>docs/reference/endpoints.md, docs/reference/00-index.md</files>
  <read_first>
    - docs/reference/endpoints.md (full file - current 57-line Windows endpoints reference to extend)
    - docs/reference/00-index.md (full file - current reference index to update)
    - .planning/phases/22-macos-lifecycle-foundation/22-RESEARCH.md (macOS endpoint tables lines 462-499, test commands lines 503-526)
  </read_first>
  <action>
**Modify `docs/reference/endpoints.md`:**

Per D-15 through D-20, extend (not replace) this file with macOS ADE endpoints.

1. **Update frontmatter** (per D-20): Change `applies_to: both` to `applies_to: all`. Add `platform: all` field.

2. **Update version gate blockquote** to mention macOS ADE: `> **Version gate:** This endpoint list covers Windows Autopilot (v1 and v2) and macOS ADE (Automated Device Enrollment). Some endpoints are platform-specific; see section headers. Windows TPM attestation endpoints are only required for specific deployment modes or hardware.`

3. **Update H1 title** (per D-19): Change from `# Autopilot Network Endpoints Reference` to `# Network Endpoints Reference`

4. **Rename existing section:** Change `## Required Endpoints` to `## Windows Autopilot Endpoints` to disambiguate now that macOS endpoints are added. The existing table content stays unchanged.

5. **Rename existing section:** Change `## Test Commands` to `## Windows Test Commands` (the content stays unchanged).

6. **Add new sections BELOW the existing Windows Test Commands section** (before the final source attribution line), in this order:

`## macOS ADE Endpoints`

Brief intro: "Network endpoints required for macOS Automated Device Enrollment (ADE) via Apple Business Manager and Intune. Shared Microsoft endpoints are annotated with (shared) -- they appear in the Windows table above and must also be accessible for macOS enrollment."

`### Apple-Specific Endpoints (ADE and MDM)`

Table with columns: URL / Pattern | Service | Purpose | Criticality
Populate from research lines 468-479 with these 9 rows:
- `https://deviceenrollment.apple.com` | Apple DEP | ADE provisional enrollment -- initial device discovery | Critical
- `https://iprofiles.apple.com` | Apple Enrollment Profiles | Enrollment profile download for ADE via ABM/ASM | Critical
- `https://mdmenrollment.apple.com` | Apple MDM Enrollment | Profile uploads and device/account lookups for ADE | Critical
- `https://*.push.apple.com` (port 443, 5223) | Apple Push Notification Service (APNs) | MDM push notifications for all ongoing management commands | Critical
- `https://*.business.apple.com` | Apple Business Manager | Admin portal access for device assignment and token management | Required for admin
- `https://gdmf.apple.com` | Apple Software Updates | MDM server identification of available software updates | Required for updates
- `https://identity.apple.com` | Apple Identity | APNs certificate request portal | Required for cert renewal
- `https://vpp.itunes.apple.com` | VPP / Apps and Books | App license operations via MDM | Required if using VPP apps
- `https://deviceservices-external.apple.com` | MDM Services | MDM servers disable Activation Lock on managed devices | Required for Activation Lock

Add SSL inspection warning after table: `> **Warning:** Apple explicitly requires that SSL/HTTPS inspection be disabled for all Apple service endpoints. SSL inspection causes ADE and APNs to fail silently. Bypass SSL inspection for all *.apple.com and *.push.apple.com domains.`

`### Microsoft Endpoints (shared with Windows)`

Per D-17, these appear with "(shared)" labels, not duplicating the Windows table.

Table with columns: URL / Pattern | Service | Purpose | Criticality
Populate from research lines 485-491 with these 6 rows:
- `https://login.microsoftonline.com` | Entra ID | (shared) User credential validation; Entra device join | Critical
- `https://graph.microsoft.com` / `graph.windows.net` | Microsoft Graph | (shared) Intune device management queries | Critical
- `https://*.manage.microsoft.com` | Intune Core | (shared) MDM enrollment, check-in, policy delivery | Critical
- `https://EnterpriseEnrollment.manage.microsoft.com` | Intune Enrollment | (shared) MDM enrollment initiation | Critical
- `https://enterpriseregistration.windows.net` | Entra Device Registration | (shared) Device Entra registration (Company Portal sign-in) | Critical
- `https://login.live.com` / `account.live.com` | Microsoft Account | (shared) Microsoft account services | Required

`### macOS App and Script Deployment (CDN by Region)`

Table with columns: Region | CDN Endpoint | Port
Populate from research lines 496-499:
- North America | `macsidecar.manage.microsoft.com` | TCP 443
- Europe | `macsidecareu.manage.microsoft.com` | TCP 443
- Asia Pacific | `macsidecarap.manage.microsoft.com` | TCP 443

Add note: "The *.azureedge.net variants of these endpoints are migrating to *.manage.microsoft.com starting March 2025."

`### macOS Test Commands`

Per D-18, use curl and macOS-native tools (not PowerShell). Include these commands in a bash code block:
- `profiles status -type enrollment` (check MDM enrollment status)
- `curl -v --max-time 5 https://deviceenrollment.apple.com` (test Apple ADE discovery)
- `curl -v --max-time 5 https://iprofiles.apple.com` (test enrollment profile endpoint)
- `curl -v --max-time 5 https://api.push.apple.com` (test APNs, note port 5223 may need firewall rules)
- `curl -v --max-time 5 https://manage.microsoft.com` (test Intune MDM)
- `curl -v --max-time 5 https://login.microsoftonline.com` (test Entra ID)
- `curl -v --max-time 5 https://business.apple.com` (test ABM portal)

7. **Update source attribution** at the bottom to add macOS sources: "Windows endpoint list sourced from Microsoft Learn: Windows Autopilot requirements, updated July 2025. Apple endpoints sourced from Apple Support HT101555, updated July 2025. Microsoft Intune endpoints from Microsoft Learn: Intune network endpoints, updated March 2026." Include proper markdown links for each source URL.

**Modify `docs/reference/00-index.md`:**

Per D-14:

1. **Update frontmatter:** Change `platform: Windows` to `platform: all`

2. **Update H1 description paragraph:** Change "Technical reference documents for Windows Autopilot configuration, security, migration, and monitoring." to "Technical reference documents for Windows Autopilot and macOS ADE configuration, security, migration, and monitoring."

3. **Add `## macOS References` section** after the `## Existing References` section (before `## Infrastructure Prerequisites`). Content:
- `[macOS Terminal Commands](macos-commands.md)` -- Terminal diagnostic commands for Intune and ADE (profiles, log show, system_profiler, IntuneMacODC)
- `[macOS Log Paths](macos-log-paths.md)` -- Log file locations, configuration profile paths, and unified log subsystems

4. **Update `## Existing References` section:** Change the endpoints.md line description from "Required URLs for Autopilot connectivity with test commands" to "Required URLs for Windows Autopilot and macOS ADE connectivity with test commands"
  </action>
  <verify>
    <automated>bash -c "grep '## macOS ADE Endpoints' docs/reference/endpoints.md && grep 'deviceenrollment.apple.com' docs/reference/endpoints.md && grep '# Network Endpoints Reference' docs/reference/endpoints.md && grep 'platform: all' docs/reference/endpoints.md && grep 'platform: all' docs/reference/00-index.md && grep 'macos-commands.md' docs/reference/00-index.md && grep 'macos-log-paths.md' docs/reference/00-index.md && grep '## macOS References' docs/reference/00-index.md && echo 'ALL CHECKS PASSED'"</automated>
  </verify>
  <acceptance_criteria>
    - docs/reference/endpoints.md contains `# Network Endpoints Reference` (platform-neutral H1 per D-19)
    - docs/reference/endpoints.md contains `platform: all` in frontmatter (per D-20)
    - docs/reference/endpoints.md contains `## Windows Autopilot Endpoints` (renamed from Required Endpoints)
    - docs/reference/endpoints.md contains `## macOS ADE Endpoints` (new section per D-15)
    - docs/reference/endpoints.md contains `### Apple-Specific Endpoints (ADE and MDM)` subsection
    - docs/reference/endpoints.md contains `### Microsoft Endpoints (shared with Windows)` subsection
    - docs/reference/endpoints.md contains `### macOS App and Script Deployment (CDN by Region)` subsection
    - docs/reference/endpoints.md contains `### macOS Test Commands` subsection
    - docs/reference/endpoints.md contains `deviceenrollment.apple.com`
    - docs/reference/endpoints.md contains `iprofiles.apple.com`
    - docs/reference/endpoints.md contains `*.push.apple.com`
    - docs/reference/endpoints.md contains `macsidecar.manage.microsoft.com` (CDN endpoint)
    - docs/reference/endpoints.md contains `(shared)` annotation on Microsoft endpoints in macOS section (per D-17)
    - docs/reference/endpoints.md contains `SSL` or `ssl` (SSL inspection warning per research)
    - docs/reference/endpoints.md contains `## Windows Test Commands` (renamed from Test Commands)
    - docs/reference/endpoints.md original Windows endpoint table is unchanged (ztd.dds.microsoft.com still present)
    - docs/reference/00-index.md contains `platform: all` (changed from platform: Windows per D-14)
    - docs/reference/00-index.md contains `## macOS References` section
    - docs/reference/00-index.md contains `macos-commands.md` link
    - docs/reference/00-index.md contains `macos-log-paths.md` link
    - docs/reference/00-index.md existing Windows sections (Infrastructure Prerequisites, Security and Compliance, etc.) are unchanged
  </acceptance_criteria>
  <done>endpoints.md extended with macOS ADE section (9 Apple endpoints, 6 shared Microsoft endpoints, 3 CDN endpoints, macOS test commands, SSL inspection warning) below existing Windows content. Reference index updated to platform: all with macOS References section listing both new reference files.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/index.md macOS Provisioning section with Phase 22 content links</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md (full file - current navigation hub with macOS TBD placeholders at lines 94-117)
  </read_first>
  <action>
Update the `## macOS Provisioning` section (lines 94-117) to add lifecycle and reference links to the appropriate role-based subsections. Keep ALL existing TBD placeholder rows for Phase 23/24. Add new linked rows for Phase 22 content. Do NOT modify any content outside the `## macOS Provisioning` section (lines before 94 and after the section's closing `---` separator).

The updated `## macOS Provisioning` section should have this content:

Intro paragraph updated to: "Troubleshooting, investigation, and setup guides for macOS Automated Device Enrollment (ADE) through Apple Business Manager. For terminology, see the [macOS Provisioning Glossary](_glossary-macos.md). For the complete enrollment pipeline, see the [macOS ADE Lifecycle](macos-lifecycle/00-ade-lifecycle.md)."

`### Service Desk (L1)` table gets a new first row:
- `[macOS ADE Lifecycle](macos-lifecycle/00-ade-lifecycle.md)` | Understand the 7-stage macOS enrollment pipeline from ABM registration through desktop
- (keep existing) macOS Triage Decision Tree | Start here -- identifies the macOS failure scenario (TBD - Phase 24)
- (keep existing) macOS L1 Runbooks | Scripted procedures for top macOS enrollment failures (TBD - Phase 24)

`### Desktop Engineering (L2)` table gets 4 new rows before the existing TBD rows:
- `[macOS ADE Lifecycle](macos-lifecycle/00-ade-lifecycle.md)` | End-to-end enrollment stages with behind-the-scenes technical detail
- `[macOS Terminal Commands Reference](reference/macos-commands.md)` | Look up diagnostic commands (profiles, log show, system_profiler, IntuneMacODC)
- `[macOS Log Paths Reference](reference/macos-log-paths.md)` | Find log file locations, unified log subsystems, and configuration profile paths
- `[Network Endpoints Reference](reference/endpoints.md#macos-ade-endpoints)` | Required Apple and Microsoft URLs for ADE enrollment with test commands
- (keep existing) macOS Log Collection Guide | Collect macOS diagnostic logs using IntuneMacODC (TBD - Phase 24)
- (keep existing) macOS L2 Runbooks | Investigation guides for profile, app, and compliance failures (TBD - Phase 24)

`### Admin Setup` table gets 2 new rows before the existing TBD row:
- `[macOS ADE Lifecycle](macos-lifecycle/00-ade-lifecycle.md)` | Review the enrollment pipeline before configuring ABM and Intune
- `[Network Endpoints Reference](reference/endpoints.md#macos-ade-endpoints)` | Verify firewall rules for all required ADE endpoints
- (keep existing) macOS Admin Setup Guides | ABM configuration, enrollment profiles, apps, compliance (TBD - Phase 23)
  </action>
  <verify>
    <automated>bash -c "grep 'macos-lifecycle/00-ade-lifecycle.md' docs/index.md && grep 'reference/macos-commands.md' docs/index.md && grep 'reference/macos-log-paths.md' docs/index.md && grep 'endpoints.md#macos-ade-endpoints' docs/index.md && grep 'TBD - Phase 24' docs/index.md && grep 'TBD - Phase 23' docs/index.md && echo 'ALL INDEX CHECKS PASSED'"</automated>
  </verify>
  <acceptance_criteria>
    - docs/index.md contains `macos-lifecycle/00-ade-lifecycle.md` link (lifecycle doc linked in macOS section)
    - docs/index.md contains `reference/macos-commands.md` link (commands reference linked in L2 section)
    - docs/index.md contains `reference/macos-log-paths.md` link (log paths reference linked in L2 section)
    - docs/index.md contains `endpoints.md#macos-ade-endpoints` link (endpoints linked in L2 and Admin sections)
    - docs/index.md still contains `TBD - Phase 24` (Phase 24 placeholders preserved, at least 4 occurrences)
    - docs/index.md still contains `TBD - Phase 23` (Phase 23 placeholder preserved)
    - docs/index.md `## macOS Provisioning` intro paragraph contains `macOS ADE Lifecycle` link
    - docs/index.md `### Service Desk (L1)` section contains `macos-lifecycle/00-ade-lifecycle.md` link
    - docs/index.md `### Desktop Engineering (L2)` section contains all 4 Phase 22 links (lifecycle, commands, log paths, endpoints)
    - docs/index.md `### Admin Setup` section contains lifecycle and endpoints links
    - docs/index.md content outside `## macOS Provisioning` section is unchanged (Windows Autopilot section, Cross-Platform References section intact)
  </acceptance_criteria>
  <done>docs/index.md macOS Provisioning section updated with linked rows for all Phase 22 artifacts across L1, L2, and Admin subsections while preserving existing TBD placeholders for Phase 23/24.</done>
</task>

</tasks>

<verification>
- All 5 files exist and contain expected content:
  - `test -f docs/reference/macos-commands.md && test -f docs/reference/macos-log-paths.md`
  - `grep '## macOS ADE Endpoints' docs/reference/endpoints.md`
  - `grep '## macOS References' docs/reference/00-index.md`
  - `grep 'macos-lifecycle/00-ade-lifecycle.md' docs/index.md`
- Platform tags correct:
  - `grep 'platform: macOS' docs/reference/macos-commands.md`
  - `grep 'platform: macOS' docs/reference/macos-log-paths.md`
  - `grep 'platform: all' docs/reference/endpoints.md`
  - `grep 'platform: all' docs/reference/00-index.md`
- Existing Windows content preserved:
  - `grep 'ztd.dds.microsoft.com' docs/reference/endpoints.md` (Windows endpoints intact)
  - `grep 'powershell-ref.md' docs/reference/00-index.md` (Windows refs still listed)
- Security note: Pure documentation phase. No code, APIs, or user input handling. No threat models applicable.
</verification>

<success_criteria>
Technician can look up macOS diagnostic commands and log paths in dedicated reference files. Admin can find all Apple and Microsoft ADE endpoints in the extended endpoints reference with macOS test commands. All Phase 22 artifacts are discoverable from the docs hub (index.md) and reference index (00-index.md).
</success_criteria>

<output>
After completion, create `.planning/phases/22-macos-lifecycle-foundation/22-02-SUMMARY.md`
</output>
