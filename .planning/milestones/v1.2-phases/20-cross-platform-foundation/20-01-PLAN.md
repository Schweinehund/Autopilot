---
phase: 20-cross-platform-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_templates/admin-template.md
  - docs/_templates/l1-template.md
  - docs/_templates/l2-template.md
  - docs/windows-vs-macos.md
autonomous: true
requirements: [XPLAT-04, XPLAT-01]

must_haves:
  truths:
    - "Admin can consult a platform-comparison page that maps every Windows Autopilot concept to its macOS ADE equivalent"
    - "Comparison page covers terminology, enrollment mechanisms, provisioning stages, and diagnostic tools per D-03"
    - "All three existing templates include a platform: field in their frontmatter"
    - "New cross-platform docs use platform: all in their frontmatter"
  artifacts:
    - path: "docs/windows-vs-macos.md"
      provides: "Platform concept comparison table and diagnostic tools subsection"
      contains: "platform: all"
    - path: "docs/_templates/admin-template.md"
      provides: "Updated admin template with platform field"
      contains: "platform:"
    - path: "docs/_templates/l1-template.md"
      provides: "Updated L1 template with platform field"
      contains: "platform:"
    - path: "docs/_templates/l2-template.md"
      provides: "Updated L2 template with platform field"
      contains: "platform:"
  key_links:
    - from: "docs/windows-vs-macos.md"
      to: "docs/_glossary.md"
      via: "Markdown cross-references to Windows glossary terms"
      pattern: "_glossary\\.md#"
    - from: "docs/windows-vs-macos.md"
      to: "docs/_glossary-macos.md"
      via: "Markdown cross-references to macOS glossary terms"
      pattern: "_glossary-macos\\.md#"
---

<objective>
Create the platform frontmatter taxonomy (XPLAT-04) and the Windows vs macOS concept comparison page (XPLAT-01). The frontmatter taxonomy adds a `platform:` field to all three existing templates, establishing the convention for all future docs. The comparison page maps Windows Autopilot concepts to macOS ADE equivalents in a feature table format matching the established `apv1-vs-apv2.md` pattern.

Purpose: Provide admins managing both platforms with a single reference page for concept mapping, and establish the frontmatter convention that enables platform-based filtering across the entire documentation suite.
Output: `docs/windows-vs-macos.md` (new), updated `docs/_templates/admin-template.md`, `docs/_templates/l1-template.md`, `docs/_templates/l2-template.md`
</objective>

<threat_model>
This plan creates and modifies Markdown documentation files only. No code execution, no user input processing, no external service integration.
- **Threat:** Broken cross-reference links to glossary files that do not yet exist (_glossary-macos.md created in Plan 02)
  - **Severity:** Low
  - **Mitigation:** Comparison page links to _glossary-macos.md will be valid after Plan 02 executes (same wave). Plan 03 validates all links.
- **Threat:** Template frontmatter change breaks existing docs using the template
  - **Severity:** Low
  - **Mitigation:** Adding a new field with a placeholder value does not affect existing docs. Existing docs are not retroactively edited (per D-19).
No high-severity threats identified.
</threat_model>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cross-platform-foundation/20-CONTEXT.md
@.planning/phases/20-cross-platform-foundation/20-RESEARCH.md

<interfaces>
<!-- Existing template frontmatter pattern (from all 3 templates): -->
```yaml
---
last_verified: YYYY-MM-DD
review_by: YYYY-MM-DD
applies_to: APv1 | APv2 | both
audience: L1 | L2 | admin | both | all
---
```

<!-- Existing comparison page pattern (from docs/apv1-vs-apv2.md): -->
```markdown
---
last_verified: 2026-03-11
review_by: 2026-06-09
applies_to: both
audience: both
---

> **Version gate:** [disambiguation banner]

# APv1 vs APv2: Which Autopilot Are You Troubleshooting?

[Intro paragraph]

## Feature Comparison

| Feature | APv1 (Windows Autopilot) | APv2 (Device Preparation) |
|---------|--------------------------|--------------------------|
| ... | ... | ... |

## Which Guide Do I Use?

[Decision guidance]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add platform frontmatter field to all three existing templates (XPLAT-04)</name>
  <files>docs/_templates/admin-template.md, docs/_templates/l1-template.md, docs/_templates/l2-template.md</files>
  <read_first>docs/_templates/admin-template.md, docs/_templates/l1-template.md, docs/_templates/l2-template.md</read_first>
  <action>
Add a `platform:` field to the YAML frontmatter in each of the three existing templates. The field goes after the `audience:` line in each file. Per D-19, supported values are `Windows`, `macOS`, or `all`. Per D-20, this is orthogonal to the existing `applies_to:` field (which retains its APv1/APv2/both meaning).

**docs/_templates/admin-template.md** (line 14-18 frontmatter block):
- Current frontmatter:
  ```yaml
  last_verified: YYYY-MM-DD
  review_by: YYYY-MM-DD
  applies_to: APv1 | APv2 | both
  audience: admin
  ```
- Change to:
  ```yaml
  last_verified: YYYY-MM-DD
  review_by: YYYY-MM-DD
  applies_to: APv1 | APv2 | both
  audience: admin
  platform: Windows | macOS | all
  ```
- Also add a line to the comment block (line 1-12) after "Include full Intune portal navigation paths":
  `     - Set platform to Windows, macOS, or all`

**docs/_templates/l1-template.md** (line 12-17 frontmatter block):
- Current frontmatter:
  ```yaml
  last_verified: YYYY-MM-DD
  review_by: YYYY-MM-DD
  applies_to: APv1 | APv2 | both
  audience: L1
  ```
- Change to:
  ```yaml
  last_verified: YYYY-MM-DD
  review_by: YYYY-MM-DD
  applies_to: APv1 | APv2 | both
  audience: L1
  platform: Windows | macOS | all
  ```
- Also add a line to the comment block (line 1-11) after "Set applies_to to APv1, APv2, or both":
  `     - Set platform to Windows, macOS, or all`

**docs/_templates/l2-template.md** (line 13-18 frontmatter block):
- Current frontmatter:
  ```yaml
  last_verified: YYYY-MM-DD
  review_by: YYYY-MM-DD
  applies_to: APv1 | APv2 | both
  audience: L2
  ```
- Change to:
  ```yaml
  last_verified: YYYY-MM-DD
  review_by: YYYY-MM-DD
  applies_to: APv1 | APv2 | both
  audience: L2
  platform: Windows | macOS | all
  ```
- Also add a line to the comment block (line 1-12) after "Set applies_to to APv1, APv2, or both":
  `     - Set platform to Windows, macOS, or all`
  </action>
  <verify>
    <automated>grep -c "platform:" docs/_templates/admin-template.md docs/_templates/l1-template.md docs/_templates/l2-template.md</automated>
  </verify>
  <acceptance_criteria>
- `docs/_templates/admin-template.md` contains the line `platform: Windows | macOS | all` in its YAML frontmatter block
- `docs/_templates/l1-template.md` contains the line `platform: Windows | macOS | all` in its YAML frontmatter block
- `docs/_templates/l2-template.md` contains the line `platform: Windows | macOS | all` in its YAML frontmatter block
- Each template's comment block contains the instruction `Set platform to Windows, macOS, or all`
- The `applies_to:` field is unchanged in all three files (still `APv1 | APv2 | both`)
- No other lines in any template are modified
  </acceptance_criteria>
  <done>All three templates have the platform: field in frontmatter and the authoring instruction in the comment block. The applies_to: field is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create Windows vs macOS concept comparison page (XPLAT-01)</name>
  <files>docs/windows-vs-macos.md</files>
  <read_first>docs/apv1-vs-apv2.md, docs/_glossary.md, .planning/phases/20-cross-platform-foundation/20-RESEARCH.md</read_first>
  <action>
Create `docs/windows-vs-macos.md` following the same structural pattern as `docs/apv1-vs-apv2.md` (per D-01). The file maps Windows Autopilot concepts to macOS ADE equivalents.

**Frontmatter** (per D-19, cross-platform doc uses `platform: all`):
```yaml
---
last_verified: 2026-04-13
review_by: 2026-07-12
applies_to: both
audience: all
platform: all
---
```

**Version gate banner** (per D-16, platform disambiguation):
```
> **Platform coverage:** This page maps Windows Autopilot concepts to macOS ADE equivalents.
> It covers terminology and enrollment mechanisms, not Intune feature parity (see [Capability Matrix](TBD - Phase 23)).
```

**H1 title:** `# Windows Autopilot vs macOS ADE: Concept Comparison`

**Introductory paragraph** (Claude's discretion): A brief 2-3 sentence paragraph explaining that Microsoft Intune manages both Windows devices (via Autopilot) and macOS devices (via Apple's Automated Device Enrollment), and that this page maps the key concepts between them to help admins who manage both platforms. Note that this is a terminology and workflow comparison, not a feature parity analysis.

**Section: `## Concept Comparison`** (per D-01, single feature table format):

Create a Markdown table with columns: `| Concept | Windows Autopilot | macOS ADE | Notes |`

Include ALL of the following rows from the RESEARCH.md terminology reference (rows derived from the "Additional Terms for Comparison Page" table in 20-RESEARCH.md). Use the exact terms from research, link first-mention terms to their glossary files using `[term](_glossary.md#anchor)` for Windows terms and `[term](_glossary-macos.md#anchor)` for macOS terms:

1. Device registration portal: Intune admin center (hardware hash upload) | Apple Business Manager (device assignment) | "Windows uses hardware hash; macOS uses serial number via ABM"
2. Enrollment mechanism: Autopilot deployment profile | ADE enrollment profile | "Both configured in Intune admin center"
3. First-run experience: OOBE (Out-of-Box Experience) | Setup Assistant | "Conceptually equivalent -- both run on first power-on"
4. Progress/blocking screen: ESP (Enrollment Status Page) | Await Configuration | "ESP has device-phase and user-phase. Await Configuration is a single hold at end of Setup Assistant."
5. Provisioning stages: OOBE > ESP device phase > ESP user phase > Desktop | Setup Assistant > Await Configuration > Company Portal login > Desktop | "macOS has no formal device/user phase split in the blocking screen"
6. Hardware identity: Hardware hash (4KB device fingerprint) | Serial number (in ABM) | "Different identity mechanisms"
7. Enrollment lock: N/A (ESP blocks by design) | Locked enrollment (prevents management profile removal) | "macOS locked enrollment is a supervision-level setting"
8. Profile delivery mechanism: Windows MDM channel | Apple MDM (APNs) | "Windows uses WinHTTP; macOS uses Apple Push Notification service"
9. User authentication: Entra credentials at OOBE | Setup Assistant with modern authentication (Entra) or legacy | "macOS has two auth methods; modern authentication is recommended"
10. App distribution: Win32 apps, MSI, MSIX, Store apps | DMG, PKG, VPP/Apps and Books | "Completely different packaging formats"
11. Device compliance: Windows security baselines + compliance policies | Compliance policies only (no macOS security baselines in Intune) | "Key gap: macOS has no Intune security baselines"
12. Disk encryption: BitLocker | FileVault | "Both manageable via Intune policies"
13. Firewall: Windows Defender Firewall | macOS Application Firewall | "Different configuration surface areas"
14. Platform-exclusive concepts: Hardware hash, ESP device/user phase, Pre-provisioning, Self-deploying mode, Hybrid Entra join, ODJ | ABM token renewal, Setup Assistant screen customization, Apple ID integration, FileVault escrow, APNs certificate | "Concepts with no direct equivalent on the other platform"

**Section: `## Diagnostic Tools`** (per D-02, subsection below main table):

Create a separate table with columns: `| Task | Windows | macOS |`

Include these rows from RESEARCH.md:
1. Collect diagnostic logs: `mdmdiagnosticstool.exe -out <path>` | `sudo ./IntuneMacODC.sh` (IntuneMacODC tool)
2. View installed profiles: N/A (registry inspection) | `sudo profiles show` or System Settings > Profiles
3. Check enrollment status: `dsregcmd /status` | `profiles status -type enrollment`
4. View MDM agent logs: Event Viewer > DeviceManagement-Enterprise-Diagnostics-Provider | `tail -f /Library/Logs/Microsoft/Intune/*IntuneMDMDaemon*.log`

**Section: `## Which Platform Am I Troubleshooting?`** (mirrors "Which Guide Do I Use?" from apv1-vs-apv2.md):

Provide brief guidance:
- "Use Windows Autopilot docs if:" followed by 3-4 bullet indicators (hardware hash was uploaded, device went through OOBE/ESP, using Windows 10/11)
- "Use macOS ADE docs if:" followed by 3-4 bullet indicators (device assigned in Apple Business Manager, went through Setup Assistant, macOS device)
- Link to the docs hub: "Return to [Documentation Hub](index.md) to find your platform and role."

**Section: `## See Also`**:
- Link to `[Autopilot Glossary](_glossary.md)` -- Windows Autopilot terminology
- Link to `[macOS Provisioning Glossary](_glossary-macos.md)` -- macOS ADE terminology
- Link to `[APv1 vs APv2](apv1-vs-apv2.md)` -- Windows framework selection
- Link to `[Capability Matrix](TBD - Phase 23)` -- Intune feature parity comparison

**Footer:** Version history table with single entry: `| 2026-04-13 | Initial version | -- |`

Per D-03: Do NOT include Intune feature parity details (that is MADM-06, Phase 23). Keep scope to terminology, enrollment mechanisms, provisioning stages, and diagnostic tools.
  </action>
  <verify>
    <automated>grep -c "Concept Comparison\|Diagnostic Tools\|Which Platform\|platform: all" docs/windows-vs-macos.md</automated>
  </verify>
  <acceptance_criteria>
- `docs/windows-vs-macos.md` exists with valid YAML frontmatter containing `platform: all`
- File contains `## Concept Comparison` section with a Markdown table containing at least 14 rows (the 14 concept rows listed above)
- File contains `## Diagnostic Tools` section with a table containing at least 4 rows
- File contains `## Which Platform Am I Troubleshooting?` section
- File contains `## See Also` section with links to `_glossary.md`, `_glossary-macos.md`, `apv1-vs-apv2.md`
- Concept Comparison table has columns: Concept, Windows Autopilot, macOS ADE, Notes
- Diagnostic Tools table has columns: Task, Windows, macOS
- Version gate banner references "Capability Matrix" as Phase 23 TBD
- At least one glossary cross-reference uses the `_glossary.md#` pattern
- At least one glossary cross-reference uses the `_glossary-macos.md#` pattern
- File does NOT contain "feature parity" content (per D-03, that is Phase 23)
  </acceptance_criteria>
  <done>Platform comparison page exists with concept mapping table (14+ rows), diagnostic tools table (4 rows), platform selection guidance, and cross-references to both glossaries. Scoped to terminology and enrollment mechanisms per D-03.</done>
</task>

</tasks>

<verification>
1. `grep -c "platform:" docs/_templates/admin-template.md docs/_templates/l1-template.md docs/_templates/l2-template.md` -- each file returns count >= 1
2. `grep "applies_to:" docs/_templates/admin-template.md docs/_templates/l1-template.md docs/_templates/l2-template.md` -- each still shows original values
3. File `docs/windows-vs-macos.md` exists and contains `platform: all` in frontmatter
4. `grep -c "|" docs/windows-vs-macos.md` returns at least 20 (table rows)
5. `grep "_glossary.md#" docs/windows-vs-macos.md` returns at least 1 match
6. `grep "_glossary-macos.md#" docs/windows-vs-macos.md` returns at least 1 match
</verification>

<success_criteria>
- All three templates have `platform:` in frontmatter and authoring instruction in comment block
- Comparison page has concept mapping table with 14+ rows covering all RESEARCH.md term pairs
- Comparison page has diagnostic tools table with 4 tool comparisons
- Comparison page scoped to terminology/enrollment/diagnostics (no feature parity per D-03)
- All new files use `platform: all` in frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/20-cross-platform-foundation/20-01-SUMMARY.md`
</output>
