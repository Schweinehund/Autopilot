---
phase: 74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md
  - 74-CONVENTIONS.md
autonomous: true
requirements: [VPP-01, HARNESS-09]
must_haves:
  truths:
    - "All 4 'VPP location token' sites (lines 115, 149, 155, 160) in 02-macos-pkg-dmg-pipeline.md read 'content token' after the rename — zero 'VPP location token' remain in docs/operations/app-lifecycle/"
    - "The 3 bare forms (line 142 'VPP token', line 150 'Apple VPP tokens', line 161 'VPP tokens') are UNCHANGED — they are not the rebrand target"
    - "Frontmatter last_verified bumps to 2026-06-08; the VPP rename is one atomic commit revertable in isolation"
    - "74-CONVENTIONS.md exists locking the freshness/SHA matrix, the 4 VPP site lines, the 6-validator cross-OS set, and the resolved-key date prefix"
  artifacts:
    - path: "docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md"
      provides: "VPP-01 4-site rename + last_verified bump"
      contains: "content token"
    - path: ".planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md"
      provides: "Locked-string + freshness/SHA matrix for downstream plans 74-02..74-05"
      contains: "resolved_2026-06-08"
  key_links:
    - from: "74-CONVENTIONS.md PHASE_74_RESOLVED_PREFIX"
      to: "74-02 sidecar resolved keys + 74-03 V-74-VPP-01b assertion"
      via: "locked-constant reference"
      pattern: "resolved_2026-06-08"
---

<objective>
Land the VPP-01 corpus rename and author the Phase 74 conventions/freshness matrix as the Wave-1 scaffold. This plan precedes Atom 1 (74-02) because the VPP-01 sidecar annotations physically live INSIDE `v1.8-audit-allowlist.json` — the file Atom 1 creates — so the corpus must be renamed first and the locked strings established for downstream plans to reference.

Purpose: Close the VPP-01 rotting-reference debt (rename, do NOT delete) and lock the constants (VPP site lines, resolved-key date prefix, 6-validator cross-OS set, BASELINE_12 anchor expectations) that 74-02/03/04/05 consume — preventing re-derivation drift.

Output: Renamed `02-macos-pkg-dmg-pipeline.md` (1 atomic commit) + new `74-CONVENTIONS.md` (1 commit). Total: 2 commits in this plan.

**Authoritative deviation note (bake into commit body):** REQUIREMENTS.md:45 and ROADMAP SC#2 say "3 sites (lines 115, 149, 155)". CONTEXT.md D-02 + user approval (2026-06-08) override this to **4 sites** — adding line 160 — sourced from `v1.7-DEFERRED-CLEANUP.md:213` recording L160 as "the 4th occurrence; total 4". Renaming all 4 leaves zero legacy leak.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONTEXT.md
@.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-RESEARCH.md

# VPP-01 pattern source (READ before editing the doc):
@.claude/tmp/phase74-D02-advisor.md

# Phase 70 conventions doc — Path-A source for 74-CONVENTIONS.md:
@.planning/milestones/v1.7-phases/70-v1-7-audit-harness-lineage-bump-milestone-close-pillar-d-clo/70-CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: VPP-01 4-site rename + frontmatter bump (ATOMIC commit)</name>
  <read_first>
    - docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md (the edit target — read frontmatter lines 1-7 + lines 110-165 covering all 4 sites + the 3 out-of-scope bare forms)
    - .claude/tmp/phase74-D02-advisor.md (SWEEP-02 ANNOTATE-not-remove pattern; current-vs-proposed wording for all 4 sites)
    - .planning/milestones/v1.7-DEFERRED-CLEANUP.md lines 208-223 (VPP-01 source-of-truth; L160 = 4th occurrence)
  </read_first>
  <action>
    In `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md`, replace the literal substring "VPP location token" with "content token" at EXACTLY these 4 sites (verified present at authoring time):
    - Line 115: "the VPP location token, allowing assignment to users (User-Licensed) or devices" → "the content token, ..."
    - Line 149: "2. Sync the ABM / VPP location token to Intune (sync runs at scheduled intervals; manual sync" → "2. Sync the ABM / content token to Intune ..."
    - Line 155: "**ABM token expiry:** The ABM / VPP location token has a 1-year (365-day) expiry; renew via the" → "**ABM token expiry:** The ABM / content token has a 1-year ..."
    - Line 160: "**Token-sharing constraint:** A single VPP location token cannot be shared between Intune" → "**Token-sharing constraint:** A single content token cannot be shared between Intune"

    Use anchored-substring edits (match the full surrounding phrase shown above), NOT line-number pins — lines may drift. Each occurrence in source may be bold-wrapped (`**VPP location token**`); preserve the bold markers, replacing only the inner phrase.

    DO NOT touch the 3 bare forms — line 142 "via a VPP token", line 150 "Apple VPP tokens", line 161 "Up to 3,000 VPP tokens" — these are a different term (not the Apple-Business rebrand target) and are explicitly out of scope per D-02 / Deferred Ideas.

    Bump frontmatter: `last_verified: 2026-04-28` → `last_verified: 2026-06-08`. Leave `review_by` as-is (Claude's Discretion per RESEARCH §VPP-01; do not invent a new horizon unless the existing one is now in the past — it is 2026-06-27, still future, so leave it).

    NO `## Version History` H2 is fabricated — the target file has none and SWEEP-02 never fabricated one (D-02 item 2). In-file provenance = the frontmatter bump only.

    Commit this rename ALONE (one atomic commit, clean `git revert`): `docs(74-01): VPP-01 4-site rename VPP location token → content token (D-02 4-site override of REQ "3 sites")`. Include the deviation note (REQ "3 sites" → D-02 "4 sites" + L160 source) in the commit body.
  </action>
  <acceptance_criteria>
    - `grep -rc "VPP location token" docs/operations/app-lifecycle/` returns 0 (scoped leak-check — NOT whole repo per D-02).
    - `grep -c "content token" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` returns >= 4.
    - `grep -n "VPP token\|Apple VPP tokens" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` still shows the 3 bare-form lines (142, 150, 161) UNCHANGED.
    - `grep -c "last_verified: 2026-06-08" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` returns 1.
    - `git log -1 --format=%s` shows the docs(74-01) VPP rename subject; `git show --stat HEAD` lists ONLY `02-macos-pkg-dmg-pipeline.md`.
    - PREDECESSOR-BYTE-UNCHANGED gate: `git diff ae9e3f4 HEAD -- scripts/validation/v1.4-milestone-audit.mjs scripts/validation/v1.4.1-milestone-audit.mjs scripts/validation/v1.5-milestone-audit.mjs scripts/validation/v1.6-milestone-audit.mjs scripts/validation/v1.7-milestone-audit.mjs scripts/validation/v1.4-audit-allowlist.json scripts/validation/v1.4.1-audit-allowlist.json scripts/validation/v1.5-audit-allowlist.json scripts/validation/v1.6-audit-allowlist.json scripts/validation/v1.7-audit-allowlist.json .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml .github/workflows/audit-harness-v1.7-integrity.yml` returns EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>grep -rc "VPP location token" docs/operations/app-lifecycle/ | grep -q ":0$" && grep -c "content token" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md</automated>
  </verify>
  <done>4 sites renamed, 3 bare forms untouched, frontmatter bumped, scoped leak-check clean, predecessor byte-unchanged, one atomic commit.</done>
</task>

<task type="auto">
  <name>Task 2: Author 74-CONVENTIONS.md (freshness/SHA + locked-string matrix)</name>
  <read_first>
    - .planning/milestones/v1.7-phases/70-v1-7-audit-harness-lineage-bump-milestone-close-pillar-d-clo/70-CONVENTIONS.md (Path-A source for structure)
    - .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONTEXT.md (Claude's Discretion items: which constants to lock)
    - .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-RESEARCH.md (Pattern 4 V-74-VPP, Pattern 5 BASELINE_12, §HARNESS-08 fields)
  </read_first>
  <action>
    Create `.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md` (Path-A from 70-CONVENTIONS.md structure). Lock these constants for downstream plans:

    1. **Authoring date:** 2026-06-08 (frontmatter `last_verified` for VPP file; sidecar resolved-key date).
    2. **PHASE_74_RESOLVED_PREFIX:** `resolved_2026-06-08` — the exact resolved-key string written into the 4 new sidecar entries (74-02) and matched by V-74-VPP-01b (74-03). Note: confirm the v1.7 sidecar key format is `resolved_2026_05_26` (underscores) vs `resolved_2026-06-08` (hyphens) at 74-02 authoring time by reading an existing entry; lock whichever the existing sidecar uses and propagate to BOTH the sidecar write and the assertion. (Existing v1.7 entries use `resolved_2026_05_26` underscore form — lock the underscore form `resolved_2026_06_08` if matching predecessor convention; record the FINAL decision here.)
    3. **VPP-01 4 sites:** lines 115, 149, 155, 160 in `02-macos-pkg-dmg-pipeline.md`; out-of-scope bare forms at 142/150/161.
    4. **6-validator cross-OS-applicable set (74-04 Axis 1 + Axis 2 EXACT MATCH):** `v1.8-milestone-audit.mjs` + `check-phase-70.mjs` + `check-phase-71.mjs` + `check-phase-72.mjs` + `check-phase-73.mjs` + `check-phase-74.mjs`. Excluded: `pin-helper-advisory` (CI-only), `rotting-external-quarterly` (cron-only negative control), harness `--self-test` (local supporting evidence).
    5. **BASELINE_12 anchor:** inserted after BASELINE_11 block (~line 423 of regenerate-supervision-pins.mjs) before `const BASELINE_9 = [`; anchors to the Atom 1 SHA (74-02 commit), a KNOWN-PAST SHA — NOT the future close SHA. BASELINE_11 precedent anchored to `26a1ae9`.
    6. **CHAIN constants for check-phase-74.mjs:** CHAIN_PHASES = [48..73] (26 phases); CHAIN_SKIP = `new Set([])`; V-74-SELF excludes 74.
    7. **Coexistence count:** v1.8 workflow = 5th coexistence file (REQUIREMENTS "Fourth" is stale; ROADMAP "fifth" authoritative).
    8. **Allowlist invariants:** `c13_broken_link_allowlist` stays 15 entries; v1.7 sidecar has NO `quarterly_audit` key (RESEARCH §HARNESS-08 assumed one — there is none; do NOT add one); `phase` field = long-slug form (v1.7 = `70-v1-7-audit-harness-lineage-bump-milestone-close-pillar-d-clo` → v1.8 sets `74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d`); `schema_version` = 1.1 carried forward; `generated` bumps to `2026-06-08T00:00:00Z`.
    9. **Close-gate:** single commit, NO Commit A (D-04); literal `{phase_74_close_SHA}` placeholder recoverable via `git log --all --grep="74-05" --grep="close-gate" --all-match -1 --format=%H`.
    10. **Pre-74 SHA for byte-unchanged gate:** `ae9e3f4` (HEAD before any Phase 74 commit) — use as the `<pre-74-SHA>` baseline in all predecessor-byte-unchanged diffs.

    Commit: `docs(74-01): 74-CONVENTIONS.md freshness/SHA + locked-string matrix`.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md` succeeds.
    - File contains the literal strings: `resolved_2026_06_08` (or the locked form), `check-phase-74.mjs`, `[48..73]` (or the expanded array), `5th coexistence`, `ae9e3f4`, `{phase_74_close_SHA}`.
    - `git show --stat HEAD` lists only `74-CONVENTIONS.md` for this commit.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md && grep -q "check-phase-74.mjs" .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md</automated>
  </verify>
  <done>74-CONVENTIONS.md locks all constants downstream plans reference; committed as its own commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → git history | All edits are local Markdown/doc edits committed to a trusted local repo; no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01-01 | Tampering | VPP rename scope creep (editing wrong sites / bare forms) | mitigate | Anchored-substring edits with full surrounding phrase; scoped leak-check `grep docs/operations/app-lifecycle/`; explicit out-of-scope assertion on lines 142/150/161. |
| T-74-01-02 | Tampering | Accidental predecessor-surface modification | mitigate | Predecessor-byte-unchanged `git diff ae9e3f4 HEAD` gate in acceptance criteria; commit isolation (`git show --stat` lists only the one file). |
| T-74-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit empty); no install surface exists. |

**Honest low-severity assessment:** This is a docs-only Wave-1 plan. No user input, no network, no auth, no data handling. The only real consideration is edit-scope discipline (renaming exactly 4 sites, not the bare forms) — fully covered by the scoped leak-check.
</threat_model>

<verification>
- `grep -rc "VPP location token" docs/operations/app-lifecycle/` → all 0.
- `grep -c "content token" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` → >= 4.
- 74-CONVENTIONS.md exists with all locked constants.
- Predecessor-byte-unchanged diff (`ae9e3f4` → HEAD) over frozen surfaces returns EMPTY.
- 2 commits total (VPP rename atomic + conventions doc).
</verification>

<success_criteria>
VPP-01 closed (4 sites renamed, 3 bare forms preserved, frontmatter bumped, scoped leak-check clean), and 74-CONVENTIONS.md locks every constant 74-02..74-05 reference — both committed in isolation with predecessor surfaces byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-01-SUMMARY.md` when done. Record: the VPP rename commit SHA, the 74-CONVENTIONS.md commit SHA, and the FINAL locked resolved-key prefix (underscore vs hyphen form).
</output>
