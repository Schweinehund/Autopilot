---
phase: 75-glossary-lifecycle-foundation-stub-correction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-macos/03-configuration-profiles.md
autonomous: true
requirements: [PSSO-04]
must_haves:
  truths:
    - "The ## Extensible SSO section no longer claims Platform SSO requires macOS 14+ as a blanket version floor"
    - "The Platform SSO bullet no longer blanket-claims it binds the macOS login password and no longer conflates the three auth methods"
    - "The section closes with an in-suite inline-code pointer to 07-platform-sso-setup.md, NOT an external Microsoft fallback and NOT a markdown link"
    - "The count of #### In Intune admin center headings in the file is unchanged at 9"
  artifacts:
    - path: "docs/admin-setup-macos/03-configuration-profiles.md"
      provides: "Corrected ## Extensible SSO section with DS-5 fixes and deferred forward-link"
      contains: "07-platform-sso-setup.md"
  key_links:
    - from: "docs/admin-setup-macos/03-configuration-profiles.md §Extensible SSO"
      to: "07-platform-sso-setup.md (code span, NOT link)"
      via: "inline-code pointer per D-06"
      pattern: "`07-platform-sso-setup\\.md`"
carry_forward:
  - phase: 76
    task: "Convert the `07-platform-sso-setup.md` inline-code span in docs/admin-setup-macos/03-configuration-profiles.md §Extensible SSO into a live markdown link [Platform SSO setup](07-platform-sso-setup.md) in the SAME commit that creates 07-platform-sso-setup.md (D-06). Link and target must land atomically so the C13 BLOCKING gate never sees a broken link."
---

<objective>
Correct the three documented DS-5 factual errors in the `## Extensible SSO` section of `docs/admin-setup-macos/03-configuration-profiles.md` and replace the external "See official Microsoft documentation" fallback with an in-suite pointer to the (not-yet-written) guide `07-platform-sso-setup.md`. (PSSO-04)

Purpose: The existing stub misstates the macOS version floor (claims 14+, framework is 13+), wrongly claims Platform SSO "binds the macOS login password" as a blanket fact (true only for Password sync), and conflates the three mutually-exclusive auth methods. These errors mislead admins and re-seed exactly the DS-5 mistakes Phase 75 exists to fix.

Output: One edited markdown file. Prose-only changes inside the `## Extensible SSO` section body (lines ~163-168). The closing pointer is an inline-code span (NOT a markdown link) so the BLOCKING C13 link audit stays green until Phase 76 creates the target file.

CRITICAL CARRY-FORWARD: Phase 76 MUST convert this code span to a live markdown link in the same commit that creates `07-platform-sso-setup.md` (recorded in `carry_forward` frontmatter, per D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-CONTEXT.md
@.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-RESEARCH.md
@.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the three DS-5 errors in ## Extensible SSO and replace the external fallback with a deferred inline-code pointer to guide 07</name>
  <files>docs/admin-setup-macos/03-configuration-profiles.md</files>
  <read_first>
    - docs/admin-setup-macos/03-configuration-profiles.md (read the live `## Extensible SSO` section at lines ~157-170; also confirm the count of `#### In Intune admin center` headings before editing so it can be re-confirmed after)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-CONTEXT.md (D-05 targeted-edit mandate items 1-4; D-06 deferred-link mechanism; XC-2 critical finding on the C13 15-entry allowlist)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-RESEARCH.md (Fact Group 1 DS-5 error #1/#2/#3 corrections and the verified version table; Common Pitfalls 2, 4, 5; Code Examples section showing current stub vs corrected elements)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-PATTERNS.md (Exact diff mandate for ## Extensible SSO lines 163-168; FileVault analog for how sections close with in-suite cross-links; Count verification of the 9 `#### In Intune admin center` headings; Deferred-link inline-code form / C13 gate safety)
  </read_first>
  <action>
    Edit ONLY the prose inside the `## Extensible SSO` section body (the three content lines at ~163, ~166, ~168). Do NOT add, remove, rename, or duplicate any `#### In Intune admin center` heading anywhere in the file — the count must stay at 9 (PITFALL-4: changing it shifts the position-numbered duplicate anchors the audit harness references).

    Item 1 — DS-5 #2 (version floor), intro sentence (~line 163): change the version parenthetical from `(macOS 14+)` to the corrected statement `(macOS 13+, macOS 14 recommended)`. The framework is available on macOS 13; macOS 14 is recommended; Smart Card is the only method that requires macOS 14+. Do NOT leave this line stale while editing only line 166 — BOTH `(macOS 14+)` instances must be corrected.

    Item 2 — DS-5 #1 + #2 + #3, Platform SSO bullet (~line 166): rewrite so it (a) corrects the version clause to "macOS 13+; macOS 14 recommended; Smart Card requires macOS 14+", (b) no longer states the blanket claim that it "binds the macOS login password" (that is true ONLY for the Password sync method), and (c) no longer conflates the three methods as one passwordless-Touch-ID-or-smart-card behavior. Instead describe Platform SSO as registering the Mac with Entra ID for device-wide SSO via three mutually-exclusive authentication methods configured in Settings Catalog: Secure Enclave key / Platform Credential (recommended — local password unchanged), Password sync (Entra ID password replaces the local password), and Smart Card (macOS 14+ only). Use RESEARCH Fact Group 1 facts.

    Item 3 — SSO app extension bullet (~line 165): preserve as-is. Touch ONLY if its version clause is wrong; per PATTERNS it currently has no version clause and is factually fine — leave it intact.

    Item 4 — closing sentence (~line 168): DELETE the sentence "See official Microsoft documentation for full Platform SSO configuration, as setup requires specific Entra ID and device registration steps." Replace it with the D-06 inline-code pointer (plain-text sentence with a code span, NOT a markdown link): the filename `07-platform-sso-setup.md` wrapped in backticks within a sentence noting it is added in the next documentation phase. Do NOT write `[Platform SSO setup](07-platform-sso-setup.md)` or any `[...](...07-platform-sso-setup.md...)` markdown-link syntax — that would introduce a broken internal link and fail the BLOCKING C13 gate (the allowlist is hard-coded to exactly 15 entries; guide 07 does not exist until Phase 76).

    Keep TWO distinct bullets (SSO app extension; Platform SSO) — do NOT merge them. Do NOT use fenced code blocks in the edited prose.

    Optional (Claude's discretion per RESEARCH Front Matter): the version-floor correction is fact-bearing — the executor MAY update this file's `last_verified` / `review_by` front matter to 2026-06-20 / 2026-09-20 if it judges the change materially fact-bearing at file level, but this is not required.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && ! grep -qF '(macOS 14+)' docs/admin-setup-macos/03-configuration-profiles.md && ! grep -qF 'See official Microsoft documentation' docs/admin-setup-macos/03-configuration-profiles.md && grep -qF '`07-platform-sso-setup.md`' docs/admin-setup-macos/03-configuration-profiles.md && ! grep -qE '\]\([^)]*07-platform-sso-setup\.md' docs/admin-setup-macos/03-configuration-profiles.md && [ "$(grep -c '^#### In Intune admin center$' docs/admin-setup-macos/03-configuration-profiles.md)" = "9" ] && grep -qF 'Secure Enclave key' docs/admin-setup-macos/03-configuration-profiles.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/admin-setup-macos/03-configuration-profiles.md` no longer contains the string `(macOS 14+)` (BOTH the intro instance at ~line 163 AND the Platform SSO bullet instance at ~line 166 removed)
    - File no longer contains the string `See official Microsoft documentation`
    - File contains the backticked inline-code span `` `07-platform-sso-setup.md` ``
    - File does NOT contain any markdown-link syntax targeting 07 (regex `\]\([^)]*07-platform-sso-setup\.md` matches nothing)
    - The count of `#### In Intune admin center` headings is exactly 9 (unchanged — anchor stability preserved per PITFALL-4)
    - The Platform SSO bullet text names the three auth methods (Secure Enclave key, Password sync, Smart Card) and does not contain the blanket phrase "Binds the macOS login password"
    - Two distinct bullets (SSO app extension; Platform SSO) remain — they are not merged
    - No fenced code block (```) is introduced into the edited section
  </acceptance_criteria>
  <done>The `## Extensible SSO` section states the correct macOS version floor, describes the three distinct auth methods without the blanket password-binding claim, and closes with a deferred inline-code pointer to guide 07 — with the 9 `#### In Intune admin center` heading count preserved and no broken markdown link introduced. All automated verify assertions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → audit harness (C13) | The C13 BLOCKING link-check (`scripts/validation/v1.8-milestone-audit.mjs:670-676`) validates markdown link targets against a hard-coded 15-entry allowlist. A markdown link to the not-yet-existent guide 07 would cross this boundary as a broken internal link. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03 | Tampering | Closing pointer to `07-platform-sso-setup.md` | mitigate | Use an inline-code span, NOT a markdown link (D-06). C13 validates link targets, not code-span text, so the 15-entry allowlist stays untouched and the BLOCKING gate stays green. Acceptance criteria assert the negative (no `](...07-platform-sso-setup.md` syntax) so a regression to a live link is caught at task verification. |
| T-75-04 | Tampering | `#### In Intune admin center` duplicate-heading anchors | mitigate | Acceptance criteria assert the heading count stays exactly 9; changing it would shift position-numbered duplicate anchors the harness references (PITFALL-4). Severity: low (doc-integrity / anchor stability). |
| T-75-05 | Information Disclosure | Corrected stub prose | accept | Prose is generic product/version description; no real tenant IDs, secrets, or PII. Severity: none. |

No threat in this plan rises to "high"; the realistic surface is doc-integrity regression of the C13 gate, fully mitigated by the deferred-link mechanism and grep-able acceptance criteria.
</threat_model>

<verification>
- The C13 BLOCKING link audit remains green: no new markdown link to a non-existent target is introduced (the pointer is an inline-code span).
- Anchor stability: `#### In Intune admin center` count unchanged at 9; no heading added/removed/renamed.
- DS-5 errors #1, #2 (both instances), and #3 are all corrected against RESEARCH Fact Group 1.
- The carry-forward to Phase 76 (convert code span → live link atomically with guide-07 creation) is recorded in this plan's `carry_forward` frontmatter so it is not lost.
</verification>

<success_criteria>
- PSSO-04 satisfied: the `## Extensible SSO` stub is factually correct and closes with an in-suite pointer to guide 07 instead of an external Microsoft fallback.
- All must_haves truths observable by an admin reading the section.
- All automated verify assertions pass.
- Phase-76 link-conversion carry-forward is explicitly recorded.
</success_criteria>

<output>
Create `.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-02-SUMMARY.md` when done. In the SUMMARY, surface the Phase-76 carry-forward (convert the `07-platform-sso-setup.md` code span to a live markdown link in the same commit that creates guide 07) as a prominent, visible note so it is not lost between phases.
</output>
