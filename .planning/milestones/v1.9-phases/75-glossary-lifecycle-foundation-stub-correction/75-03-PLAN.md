---
phase: 75-glossary-lifecycle-foundation-stub-correction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/macos-lifecycle/00-ade-lifecycle.md
autonomous: true
requirements: [SSOREF-03]
must_haves:
  truths:
    - "Stage 4 ### Watch Out For carries a new bullet on SSO extension profile timing before first sign-in"
    - "Stage 6 ### Watch Out For carries a new bullet on Entra device registration via Platform SSO, worded to respect the userless-enrollment skip"
    - "Stage 7 ### Watch Out For carries a new bullet on PSSO PRT renewal and Secure Enclave key loss / re-registration on password reset"
    - "Each of the seven stages still contains exactly four ### subsections (the line-23 invariant is preserved)"
    - "No new ### heading is introduced and no stage-ending --- separator is displaced"
  artifacts:
    - path: "docs/macos-lifecycle/00-ade-lifecycle.md"
      provides: "Append-only SSO-timing bullets at Stages 4/6/7 Watch Out For subsections"
      contains: "Platform SSO"
  key_links:
    - from: "docs/macos-lifecycle/00-ade-lifecycle.md Stage 6 Watch Out For"
      to: "userless-enrollment conditional wording"
      via: "bullet acknowledging skip in userless enrollment"
      pattern: "skipped in userless enrollment"
carry_forward:
  - phase: 76
    task: "Add the `00-overview.md` Mermaid lifecycle nodes AND the bullet-list entries for guides 07/08/09 (Platform SSO setup/troubleshooting/reference) to close the remaining half of SSOREF-03. Phase 75 delivers only the `00-ade-lifecycle.md` Stage 4/6/7 Watch Out For notes; the `00-overview.md` Mermaid + bullet-list portion is logically impossible until guides 07/08/09 exist (Phases 76-78) and is owned by ROADMAP Phase 76 SC5. Land the overview entries in the same phase that creates those guides so SSOREF-03 can be formally closed."
---

<objective>
Add surgical, append-only Platform SSO timing notes to the existing `### Watch Out For` subsection of Stages 4, 6, and 7 in `docs/macos-lifecycle/00-ade-lifecycle.md`. (SSOREF-03)

Purpose: The ADE lifecycle doc currently describes enrollment timing without SSO awareness. Admins need to know WHEN in the lifecycle the SSO extension profile must arrive (Stage 4), WHEN Entra device registration completes (Stage 6, with the userless-enrollment caveat), and the ongoing SSO key-management hazard (Stage 7 password-reset destroys the Secure Enclave key).

Output: One edited markdown file. Three new bullets appended to existing `### Watch Out For` lists — purely additive within an existing subsection. The file documents a "four subsections per stage" invariant at line 23; this plan preserves it by adding NO new `### ` heading and displacing NO stage-ending `---` separator (PITFALL-6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-CONTEXT.md
@.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-RESEARCH.md
@.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append SSO-timing bullets to the Stage 4, 6, and 7 ### Watch Out For subsections</name>
  <files>docs/macos-lifecycle/00-ade-lifecycle.md</files>
  <read_first>
    - docs/macos-lifecycle/00-ade-lifecycle.md (read the live file: confirm the line-23 "four subsections" invariant statement; note the file has 29 total `### ` H3 headings = 1 `### Prerequisites` at ~line 37 OUTSIDE the stages + 28 stage subsections [7 stages × 4]; locate the `### Watch Out For` subsection of Stage 4 [~line 244], Stage 6 [~line 323], Stage 7 [~line 362] and the `---` separator that ends each stage; confirm the existing bullet format `- **Bold label.** Explanation.`)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-CONTEXT.md (D-07 append-to-Watch-Out-For mandate; the Stage 6 userless-enrollment conditional wording; per-stage content list)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-RESEARCH.md (Fact Group 5 Stage 4/6/7 SSO timing facts; Open Question 1 on keeping the Stage 4 note narrowly scoped; Assumptions Log A4 on Stage 7 Secure Enclave key loss; Common Pitfall 6 four-subsections invariant)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-PATTERNS.md (Watch Out For bullet format analogs Stage 2/Stage 5; the exact proposed new-bullet text for Stage 4 / Stage 6 / Stage 7; structural-invariant and stage-separator rules)
  </read_first>
  <action>
    Append exactly ONE new bullet to the END of the existing `### Watch Out For` bullet list in each of Stage 4, Stage 6, and Stage 7 — placed AFTER the current last bullet and BEFORE the stage-ending `---` separator. Match the local format exactly: `- **Bold label ending in period.** Explanation sentence(s).` Use backticks for command/key names. Do NOT introduce a blockquote, callout, fenced code block, or any new `### ` heading inside a stage.

    Stage 4 bullet (SSO extension profile timing) — per RESEARCH Fact Group 5 Stage 4 and Open Question 1 (keep narrowly scoped): flag that when Platform SSO is configured for ADE enrollment-time registration (advanced path, macOS 26+ with `EnableRegistrationDuringSetup`), the SSO extension Settings Catalog profile must be assigned to a static device group and delivered before the user reaches the Entra credential screen in Setup Assistant; if Company Portal has not finished installing, the user sees an "Unable to sign in" registration error and can tap "Try Again". Note that for standard (post-enrollment) Platform SSO this timing constraint does not apply at Stage 4 — registration is triggered later by a "Registration required" notification at the desktop (Stage 7). Forward-compatible with Phase 76 PSSO-12; do NOT link to guide 07 (it does not exist yet).

    Stage 6 bullet (Entra device registration) — per RESEARCH Fact Group 5 Stage 6 and D-07 conditional wording: state that when Company Portal sign-in occurs (skipped in userless enrollment) Platform SSO device registration completes here — the WPJ (Workplace Join) key is written to the Secure Enclave and the device registers with Entra ID, enabling Conditional Access device-compliance evaluation. Note that userless/no-affinity devices never complete this registration and cannot participate in user-based Conditional Access. Note that from August 2025 new registrations use Secure Enclave storage by default — verify with `app-sso platform -s`, not `security find-certificate` (which returns false negatives). The bullet MUST contain the literal phrase "skipped in userless enrollment" so it cannot be read as implying SSO applies to userless devices.

    Stage 7 bullet (SSO key management) — per RESEARCH Fact Group 5 Stage 7 and Assumptions Log A4: state that the macOS Platform SSO PRT renews automatically every 4 hours, but any password reset that bypasses the interactive macOS password-change UI (MDM-forced reset, FileVault recovery key reset) destroys the Secure Enclave-stored PSSO key (unrecoverable); the user receives a new "Registration required" notification and must re-register Platform SSO. Advise warning users/helpdesk that MDM password-reset actions require a follow-up PSSO re-registration.

    Do NOT modify any other stage, any `### ` heading, the line-23 invariant statement, or any `---` separator position. Do NOT update front-matter dates (per RESEARCH, only `_glossary-macos.md` warrants the bump unless judged materially fact-bearing — the Stage 7 SE key note is a candidate but the date bump is optional executor discretion).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -qF 'skipped in userless enrollment' docs/macos-lifecycle/00-ade-lifecycle.md && grep -qF 'app-sso platform -s' docs/macos-lifecycle/00-ade-lifecycle.md && grep -qF 'Registration required' docs/macos-lifecycle/00-ade-lifecycle.md && [ "$(grep -c '^### Watch Out For$' docs/macos-lifecycle/00-ade-lifecycle.md)" = "7" ] && [ "$(grep -c '^### ' docs/macos-lifecycle/00-ade-lifecycle.md)" = "29" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/macos-lifecycle/00-ade-lifecycle.md` contains a new Stage 4 Watch Out For bullet referencing the SSO extension profile delivery before first sign-in (mentions `EnableRegistrationDuringSetup` or "before ... Setup Assistant")
    - The Stage 6 Watch Out For bullet contains the literal phrase `skipped in userless enrollment` and references Secure Enclave / WPJ registration and `app-sso platform -s`
    - The Stage 7 Watch Out For bullet references the 4-hour PRT renewal and Secure Enclave key destruction on password reset plus the "Registration required" re-registration step
    - The count of `### Watch Out For` headings is exactly 7 (one per stage — unchanged; no new Watch Out For subsection created)
    - The total count of `### ` (H3) headings is exactly 29 (UNCHANGED from pre-edit state = 1 `### Prerequisites` outside the stages + 28 stage subsections [7 stages × 4]), proving no new `### ` heading was introduced and the four-subsections-per-stage invariant holds
    - No fenced code block (```) is introduced by the three new bullets (they use inline backticks only)
    - Each new bullet matches the `- **Bold label.** Explanation.` format and sits before its stage-ending `---` separator
  </acceptance_criteria>
  <done>Stages 4, 6, and 7 each carry exactly one new append-only SSO-timing bullet inside their existing `### Watch Out For` list, the Stage 6 wording respects the userless-enrollment skip, and the four-subsections-per-stage invariant (29 total H3 headings = 1 Prerequisites + 28 stage subsections, 7 Watch Out For) is preserved. All automated verify assertions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan appends bullets to a static lifecycle-narrative markdown doc. No auth, no input handling, no runtime code, no network surface, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-06 | Tampering | "four subsections per stage" structural invariant (line 23) and stage-ending `---` separators | mitigate | Acceptance criteria assert exact counts (7 `### Watch Out For`, 29 total `### ` headings) so any accidental new heading or displaced separator is caught at task verification. Severity: low (doc-integrity / anchor stability). |
| T-75-07 | Information Disclosure | New lifecycle bullets | accept | Generic timing/behavior description; no real tenant IDs, secrets, or PII. Severity: none. |

No threat in this plan rises to "high"; the realistic surface is doc-structure regression, fully mitigated by count-based grep acceptance criteria.
</threat_model>

<verification>
- The line-23 "four subsections per stage" invariant holds: total `### ` heading count unchanged at 29 (1 Prerequisites + 28 stage subsections); `### Watch Out For` count unchanged at 7.
- No `### ` heading is added/removed/renamed; no `---` separator is displaced.
- All three new bullets are append-only within existing `### Watch Out For` lists and match the local bullet format.
- Stage 6 wording respects the userless-enrollment conditional (D-07) — does not imply SSO applies to userless devices.
- No markdown link to a non-existent file is introduced (C13 BLOCKING gate unaffected).
</verification>

<success_criteria>
- SSOREF-03 satisfied: Stages 4/6/7 carry surgical, append-only SSO-timing notes.
- All must_haves truths observable by an admin reading the lifecycle doc.
- All automated verify assertions pass.
</success_criteria>

<output>
Create `.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-03-SUMMARY.md` when done.
</output>
