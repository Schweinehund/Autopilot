---
phase: 76-platform-sso-admin-setup-guide
plan: 02
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - docs/admin-setup-macos/00-overview.md
autonomous: true
requirements: [PSSO-01]
must_haves:
  truths:
    - "An admin starting at 00-overview.md can discover all three Platform SSO guides (07/08/09) via the Mermaid diagram and the numbered bullet list"
    - "Guide 07 is a LIVE link in the bullet list; guides 08 and 09 are code-span/plain-text references (not links)"
    - "The v1.8 C13 broken-link gate stays green — no new internal broken link, 15-entry allowlist untouched"
  artifacts:
    - path: "docs/admin-setup-macos/00-overview.md"
      provides: "Overview surfacing of guides 07/08/09 (Mermaid nodes + bullet entries + Version-History row)"
      contains: "[Platform SSO Setup](07-platform-sso-setup.md)"
  key_links:
    - from: "docs/admin-setup-macos/00-overview.md"
      to: "docs/admin-setup-macos/07-platform-sso-setup.md"
      via: "live markdown link in numbered bullet list (entry 7)"
      pattern: "\\[Platform SSO Setup\\]\\(07-platform-sso-setup\\.md\\)"
---

<objective>
Edit `docs/admin-setup-macos/00-overview.md` to surface guides 07, 08, and 09 (SC5 / B1 / D-02). Three body-level, anchor-safe additions: (1) add Mermaid nodes G/H/I for 07/08/09; (2) add bullet entry 7 as a LIVE markdown link to guide 07 (which now exists, from Plan 01), and entries 8/9 as inline code-span references (NOT links) so the frozen v1.8 C13 15-entry allowlist stays untouched; (3) add a Version-History row. No heading changes; front-matter unchanged.

Purpose: Satisfy SC5 — an admin starting at the overview can discover all three Platform SSO guides.
Output: Edited `00-overview.md` (Mermaid body + bullet list + Version-History).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/76-platform-sso-admin-setup-guide/76-CONTEXT.md
@.planning/phases/76-platform-sso-admin-setup-guide/76-RESEARCH.md
@.planning/phases/76-platform-sso-admin-setup-guide/76-PATTERNS.md
@docs/admin-setup-macos/00-overview.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface guides 07/08/09 in 00-overview.md (Mermaid nodes + bullet entries + Version-History row)</name>
  <read_first>
    - docs/admin-setup-macos/00-overview.md (target file — current Mermaid block lines ~19–28, numbered bullet list lines ~30–41, Version-History table lines ~57–60)
    - .planning/phases/76-platform-sso-admin-setup-guide/76-RESEARCH.md §(e) SC5 (B1 edit mechanics, exact Mermaid node additions, exact 08/09 code-span literals, Version-History row, C13 allowlist analysis)
    - .planning/phases/76-platform-sso-admin-setup-guide/76-PATTERNS.md §"00-overview.md (EDIT)" (exact current Mermaid/bullet/Version-History text + the three additions verbatim) and §"Deferred Code-Span Pattern"
  </read_first>
  <action>
    Make three body-level edits to `docs/admin-setup-macos/00-overview.md`. Do NOT change the front-matter, any heading, or the existing six bullet entries / six Mermaid nodes.

    1. Mermaid: inside the existing fenced ```mermaid block, after the last `E --> F` line and before the closing fence, append three nodes connecting from C (Configuration Profiles):
       - `  C --> G[7. Platform SSO<br/>Setup]`
       - `  G --> H[8. Auth Methods<br/>Deep-Dive]`
       - `  G --> I[9. Enterprise SSO<br/>Migration]`
       (Mermaid node labels are not link targets — C13 never validates them; safe regardless of whether guides 08/09 exist.)

    2. Numbered bullet list: after the existing entry 6 and before the `## Cross-Platform References` heading, append:
       - Entry 7 as a LIVE markdown link in the existing bold-link-with-description style: `7. **[Platform SSO Setup](07-platform-sso-setup.md)** -- Configure macOS Platform SSO via the Settings Catalog `com.apple.extensiblesso` payload. Covers Entra prerequisites, the three silent bootstrapping blockers, dual-field mixed-fleet configuration, user-group assignment, and `app-sso platform -s` verification.` (Live link is safe because guide 07 exists after Plan 01.)
       - Entry 8 as a bare code-span (NOT a markdown link): `` 8. `08-auth-methods-deep-dive.md` (added in a later documentation phase) ``
       - Entry 9 as a bare code-span (NOT a markdown link): `` 9. `09-enterprise-sso-plugin-migration.md` (added in a later documentation phase) ``
       Entries 8/9 MUST remain code-spans, never `[text](path)` — converting them to live links would create broken internal links and break the v1.8 C13 gate (PITFALL-5 / D-02).

    3. Version-History: prepend a new row above the existing 2026-04-14 row (newest at top): `| 2026-06-20 | Phase 76: added guides 07/08/09 to Mermaid diagram and numbered list | -- |`.

    Do NOT use fenced code blocks for prose; the only fenced block touched is the existing ```mermaid block.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('docs/admin-setup-macos/00-overview.md','utf8'); let e=[]; ['C --> G[7. Platform SSO<br/>Setup]','G --> H[8. Auth Methods<br/>Deep-Dive]','G --> I[9. Enterprise SSO<br/>Migration]','[Platform SSO Setup](07-platform-sso-setup.md)','`08-auth-methods-deep-dive.md`','`09-enterprise-sso-plugin-migration.md`','Phase 76: added guides 07/08/09'].forEach(n=>{if(!t.includes(n))e.push('missing: '+n)}); if(/\\[08-auth-methods-deep-dive\\.md\\]\\(/.test(t))e.push('08 wrongly made a live link'); if(/\\[09-enterprise-sso-plugin-migration\\.md\\]\\(/.test(t))e.push('09 wrongly made a live link'); const nIntune=(t.match(/^## /gm)||[]).length; if(e.length){console.error(e.join('\\n'));process.exit(1)} console.log('OK')"</automated>
  </verify>
  <done>
    00-overview.md has Mermaid nodes G/H/I for 07/08/09; bullet entry 7 is a live link to guide 07; entries 8/9 are code-spans (not links); a Version-History row for Phase 76 is added. No heading or front-matter changes; the six original bullet entries and Mermaid nodes are unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only edit. No executable code, no user input, no auth surface, no data handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03 | Tampering | C13 broken-link gate in v1.8-milestone-audit.mjs | mitigate | Entry 7 is live-linked only because guide 07 exists (created in Wave-1 Plan 01); entries 8/9 stay code-spans (not links), so no broken internal link is introduced and the 15-entry allowlist is untouched. Mermaid node labels are never validated as links. |
| T-76-04 | Tampering | Anchor stability (inbound links to 00-overview headings) | mitigate | All edits are body-level (Mermaid body, bullet entries, Version-History row). No heading is added, renamed, or reordered; front-matter unchanged (PITFALL-6). |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No package installs — documentation editing only. |
</threat_model>

<verification>
- C13 broken-link gate stays green: `node scripts/validation/v1.8-milestone-audit.mjs` (allowlist.length === 15; 6 transient_external / 9 template_placeholder). Entry 7 live link resolves (guide 07 exists from Plan 01); entries 8/9 are code-spans with no link target.
- 00-overview Mermaid diagram and numbered list include 07/08/09; no heading changes.
</verification>

<success_criteria>
- SC5: 00-overview.md Mermaid diagram and numbered bullet list include nodes/entries for guides 07, 08, and 09; guide 07 is a live link, 08/09 are code-span references; an admin starting at the overview can discover all three Platform SSO guides.
</success_criteria>

<output>
Create `.planning/phases/76-platform-sso-admin-setup-guide/76-02-SUMMARY.md` when done.
</output>
