---
phase: 80-l1-l2-runbooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l1-runbooks/35-macos-sso-sign-in-failure.md
  - docs/l1-runbooks/36-macos-secure-enclave-key.md
autonomous: true
requirements: [SSORUN-01, SSORUN-02]
must_haves:
  truths:
    - "L1 staff can triage 'Registration Required not appearing despite Intune Succeeded' across the four root causes (old Company Portal / Error 10002 legacy conflict / mistyped registration token / dismissed notification) using 35-macos-sso-sign-in-failure.md"
    - "L1 staff can verify Secure Enclave key state and guide a user through the re-registration path after password-reset key loss using 36-macos-secure-enclave-key.md"
    - "Both L1 runbooks instruct the user to run app-sso platform -s as the first triage/verification step via the 'Say to the user' guided Terminal pattern (D-03/C1)"
    - "Both L1 runbooks escalate to L2 #27 via a mandatory in-phase escalation link (D-02a/SC4 edge E5)"
    - "The two runbooks are strictly independent — neither links to nor mentions the other (D-04/D2)"
  artifacts:
    - path: "docs/l1-runbooks/35-macos-sso-sign-in-failure.md"
      provides: "L1 runbook: Platform SSO sign-in failure triage, 4 root causes, app-sso platform -s first, escalation to L2 #27 (SSORUN-01)"
      contains: "app-sso platform -s"
    - path: "docs/l1-runbooks/36-macos-secure-enclave-key.md"
      provides: "L1 runbook: Secure Enclave key verification + loss-after-password-reset recovery, escalation to L2 #27 (SSORUN-02)"
      contains: "app-sso platform -s"
  key_links:
    - from: "docs/l1-runbooks/35-macos-sso-sign-in-failure.md"
      to: "docs/l2-runbooks/27-macos-sso-investigation.md"
      via: "Escalation Criteria 'See' link (in-phase edge E5)"
      pattern: "27-macos-sso-investigation\\.md"
    - from: "docs/l1-runbooks/36-macos-secure-enclave-key.md"
      to: "docs/l2-runbooks/27-macos-sso-investigation.md"
      via: "Escalation Criteria 'See' link (in-phase edge E5)"
      pattern: "27-macos-sso-investigation\\.md"
---

<objective>
Create the two macOS Platform SSO L1 runbooks (#35 and #36) per SSORUN-01 and SSORUN-02, using the L1 macOS house-style template from `docs/l1-runbooks/15-macos-company-portal-sign-in.md`.

Purpose: Give Service Desk (L1) staff dedicated, executable runbooks to triage PSSO "Registration Required not appearing" failures (4 root causes) and Secure Enclave key loss after password reset — each opening with the canonical `app-sso platform -s` Terminal walkthrough and escalating cleanly to L2 #27.

Output:
- `docs/l1-runbooks/35-macos-sso-sign-in-failure.md` (SSORUN-01)
- `docs/l1-runbooks/36-macos-secure-enclave-key.md` (SSORUN-02)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — REQUIRED before authoring (binding decisions + verified facts + copy-ready excerpts)
@.planning/phases/80-l1-l2-runbooks/80-CONTEXT.md
@.planning/phases/80-l1-l2-runbooks/80-RESEARCH.md
@.planning/phases/80-l1-l2-runbooks/80-PATTERNS.md

# House-style analog — the direct template for BOTH runbooks (read in full before writing)
@docs/l1-runbooks/15-macos-company-portal-sign-in.md

# Link-not-copy canonical sources — summarize + link, NEVER duplicate their tables
@docs/admin-setup-macos/07-platform-sso-setup.md
@docs/admin-setup-macos/08-auth-methods-deep-dive.md
@docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create L1 #35 — macOS Platform SSO Sign-In Failure (SSORUN-01)</name>
  <files>docs/l1-runbooks/35-macos-sso-sign-in-failure.md</files>
  <read_first>
    - `docs/l1-runbooks/15-macos-company-portal-sign-in.md` — exact L1 macOS template: frontmatter (lines 1-7), platform-gate blockquote (line 9), "Say to the user" blockquote step pattern (lines 24, 45), Escalation Criteria + "Before escalating, collect:" (lines 47-67), back-to-triage footer (lines 69-71), Version History table (lines 73-77).
    - `.planning/phases/80-l1-l2-runbooks/80-PATTERNS.md` §"docs/l1-runbooks/35-macos-sso-sign-in-failure.md" — copy-ready frontmatter, opening paragraph, prerequisites block, Terminal-step pattern, escalation "See" line targeting 27-macos-sso-investigation.md, footer, Version History row.
    - `.planning/phases/80-l1-l2-runbooks/80-RESEARCH.md` §6 "L1 #35 Root Causes" — the four root causes (old Company Portal <5.2404.0 / Error 10002 legacy conflict / mistyped {{DEVICEREGISTRATION}} token / dismissed notification) and how each is distinguished via Intune portal + app-sso platform -s; §Pitfall 5 (do NOT fabricate failure-state JSON field values).
    - `docs/admin-setup-macos/07-platform-sso-setup.md` — Configuration-Caused Failures table and app-sso platform -s healthy output, to SUMMARIZE + LINK (link-not-copy; never reproduce the table).
    - `docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md` — Error 10002 / legacy-plugin conflict context for root cause #2 (link-not-copy).
  </read_first>
  <action>
    Create `docs/l1-runbooks/35-macos-sso-sign-in-failure.md` following the #15 template structure verbatim where structural, with PSSO content.

    Frontmatter (per D-XX house-style): last_verified: 2026-06-21, review_by: 2026-09-21, applies_to: ADE, audience: L1, platform: macOS.

    Platform-gate blockquote: replicate #15 line 9 verbatim.

    Title + opening paragraph: "# macOS Platform SSO Sign-In Failure" with the opening from PATTERNS.md (use this runbook when the Mac does not display the "Registration Required" notification despite Intune reporting "Succeeded", or PSSO sign-in fails after a registration attempt).

    Prerequisites: replicate the #15 block (Intune admin center, Entra admin center read-only, device serial, user UPN).

    Steps section: Step 1 MUST be the canonical Terminal triage per D-03/C1 — a "Say to the user" guided walkthrough that has the user open Terminal and run `app-sso platform -s` (verbatim, in a fenced ```bash block), then report the full output back. Describe the EXPECTED HEALTHY state as documented in guide 07 ("Device Registration: REGISTERED and User Registration: REGISTERED") and instruct L1 to treat any other output as a failure signal — do NOT fabricate failure-state JSON field names (RESEARCH §Pitfall 5). Subsequent numbered steps walk the four root causes, each with its Intune-portal / Company-Portal-version distinguisher: (1) old Company Portal (<5.2404.0); (2) Error 10002 legacy SSO-extension conflict — summarize + link to guide 09; (3) mistyped {{DEVICEREGISTRATION}} registration token (Intune shows "Succeeded" but no notification) — summarize + link to guide 07 Configuration-Caused Failures; (4) dismissed "Registration Required" notification, with the macOS 14 Repair path (Settings > Users & Groups > Network Account Server > Edit > Repair) and the macOS 13 Company Portal re-registration note.

    Escalation Criteria: bullet list of triggers (admin-only fixes: Error 10002 unassign, wrong token; app-sso shows NOT REGISTERED after user completes registration; etc.). "Before escalating, collect:" checklist MUST request the FULL `app-sso platform -s` output (not one field), device serial, user UPN, macOS version, Company Portal version, Intune device configuration status screenshot. The "See" line is the MANDATORY in-phase escalation edge (D-02a/E5): link to `../l2-runbooks/27-macos-sso-investigation.md`.

    Back-to-triage footer: replicate `[Back to macOS ADE Triage](../decision-trees/06-macos-triage.md)` verbatim (D-02/B1 — kept even though the SSO leaf lands in Phase 81; do NOT edit the tree file).

    Version History table: single row `| 2026-06-21 | Initial version | -- |`.

    PROHIBITED (corpus rules): `security find-certificate` must appear NOWHERE; `app-sso diagnose` must appear NOWHERE; NO link to a decision-tree SSO leaf or any nav-hub (index.md/common-issues/quick-ref); NO cross-link to or mention of 36-macos-secure-enclave-key.md (D-04/D2); do NOT duplicate any guide 07/08/09 table (link-not-copy).
  </action>
  <verify>
    <automated>test -f docs/l1-runbooks/35-macos-sso-sign-in-failure.md && grep -q 'app-sso platform -s' docs/l1-runbooks/35-macos-sso-sign-in-failure.md && grep -q '27-macos-sso-investigation.md' docs/l1-runbooks/35-macos-sso-sign-in-failure.md && grep -q '06-macos-triage.md' docs/l1-runbooks/35-macos-sso-sign-in-failure.md && ! grep -q 'security find-certificate' docs/l1-runbooks/35-macos-sso-sign-in-failure.md && ! grep -q 'app-sso diagnose' docs/l1-runbooks/35-macos-sso-sign-in-failure.md && ! grep -q '36-macos-secure-enclave-key' docs/l1-runbooks/35-macos-sso-sign-in-failure.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/l1-runbooks/35-macos-sso-sign-in-failure.md` exists.
    - Frontmatter has `audience: L1`, `platform: macOS`, `applies_to: ADE`, `last_verified: 2026-06-21`, `review_by: 2026-09-21`.
    - Contains the literal string `app-sso platform -s` in a fenced bash block as Step 1, framed by a "Say to the user" Terminal walkthrough (C1/D-03).
    - All four root causes are present and distinguishable: Company Portal version, Error 10002, mistyped `{{DEVICEREGISTRATION}}` token, dismissed notification.
    - Escalation Criteria "See" line links to `../l2-runbooks/27-macos-sso-investigation.md` (in-phase edge E5).
    - Contains the verbatim footer `[Back to macOS ADE Triage](../decision-trees/06-macos-triage.md)`.
    - Does NOT contain `security find-certificate`.
    - Does NOT contain `app-sso diagnose`.
    - Does NOT contain `36-macos-secure-enclave-key` (no #35↔#36 cross-link, D-04).
    - Does NOT link guide 07/08/09 tables by duplication (summarize + link only).
  </acceptance_criteria>
  <done>The L1 #35 runbook exists, opens with the app-sso platform -s Terminal walkthrough, documents all four root causes, escalates to L2 #27, carries the back-to-triage footer, and contains none of the prohibited strings; the verify command prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Create L1 #36 — macOS Platform SSO Secure Enclave Key Loss (SSORUN-02)</name>
  <files>docs/l1-runbooks/36-macos-secure-enclave-key.md</files>
  <read_first>
    - `docs/l1-runbooks/15-macos-company-portal-sign-in.md` — identical L1 macOS template as Task 1.
    - `.planning/phases/80-l1-l2-runbooks/80-PATTERNS.md` §"docs/l1-runbooks/36-macos-secure-enclave-key.md" — copy-ready opening paragraph and the escalation "See" line for #36; note the explicit "No cross-link to #35" rule (D-04/D2).
    - `.planning/phases/80-l1-l2-runbooks/80-RESEARCH.md` §7 "Secure Enclave Key Loss After Password Reset (L1 #36)" — what destroys the SE key (MDM-driven password reset, FileVault recovery key use), the recovery paths (macOS 14: Settings > Users & Groups > Network Account Server > Edit > Repair; macOS 13: Company Portal Deregister + re-register), the app-sso platform -s state after loss (User Registration not REGISTERED; Device Registration may survive), and the note that FileVault still works (local password) — only the PSSO Entra SSO function is broken.
    - `docs/admin-setup-macos/08-auth-methods-deep-dive.md` — SE Key Destruction Warning, to SUMMARIZE + LINK (link-not-copy).
  </read_first>
  <action>
    Create `docs/l1-runbooks/36-macos-secure-enclave-key.md` using the same #15 template structure as Task 1.

    Frontmatter: last_verified: 2026-06-21, review_by: 2026-09-21, applies_to: ADE, audience: L1, platform: macOS. Platform-gate blockquote verbatim from #15.

    Title + opening: "# macOS Platform SSO — Secure Enclave Key Loss" with the PATTERNS.md opening (use this runbook when PSSO stops working after a password reset, after FileVault recovery-key use, or when app-sso platform -s shows User Registration not REGISTERED despite prior enrollment).

    Prerequisites: replicate the #15 block.

    Steps: Step 1 MUST be the canonical `app-sso platform -s` Terminal verification per D-03/C1 ("Say to the user" walkthrough, verbatim command in a fenced ```bash block, report full output). Describe the expected loss signal in conservative prose (User Registration not in REGISTERED state; Device Registration may still show REGISTERED) — do NOT fabricate JSON field values (RESEARCH §Pitfall 5). Then walk the recovery path: explain that MDM-driven password reset / FileVault recovery-key use destroys the SE key (summarize + link to guide 08 SE Key Destruction Warning), that this is expected behavior, and that FileVault itself still works via the local password — only the PSSO Entra SSO function is broken. Provide the version-branched re-registration: macOS 14 Repair path (Settings > Users & Groups > Network Account Server > Edit > Repair); macOS 13 Company Portal Deregister + re-register. Note that after repair/re-registration the user completes MFA and a new SE key is provisioned.

    Escalation Criteria + "Before escalating, collect:" (full app-sso platform -s output, serial, UPN, macOS version, what triggered the reset, whether Repair/re-registration was attempted). "See" line is the MANDATORY in-phase escalation edge (D-02a/E5): link to `../l2-runbooks/27-macos-sso-investigation.md` (per PATTERNS.md #36 escalation line — scoped to "when re-registration does not resolve the issue").

    Back-to-triage footer verbatim. Version History single row `| 2026-06-21 | Initial version | -- |`.

    PROHIBITED: `security find-certificate` NOWHERE; `app-sso diagnose` NOWHERE; NO nav-hub or SSO-leaf links; NO cross-link to or mention of 35-macos-sso-sign-in-failure.md (D-04/D2); no "start here / which runbook" disambiguation note (deferred to Phase 81); no duplication of guide 08 tables.
  </action>
  <verify>
    <automated>test -f docs/l1-runbooks/36-macos-secure-enclave-key.md && grep -q 'app-sso platform -s' docs/l1-runbooks/36-macos-secure-enclave-key.md && grep -q '27-macos-sso-investigation.md' docs/l1-runbooks/36-macos-secure-enclave-key.md && grep -q '06-macos-triage.md' docs/l1-runbooks/36-macos-secure-enclave-key.md && grep -qi 'Repair' docs/l1-runbooks/36-macos-secure-enclave-key.md && ! grep -q 'security find-certificate' docs/l1-runbooks/36-macos-secure-enclave-key.md && ! grep -q 'app-sso diagnose' docs/l1-runbooks/36-macos-secure-enclave-key.md && ! grep -q '35-macos-sso-sign-in-failure' docs/l1-runbooks/36-macos-secure-enclave-key.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/l1-runbooks/36-macos-secure-enclave-key.md` exists.
    - Frontmatter has `audience: L1`, `platform: macOS`, `applies_to: ADE`, `last_verified: 2026-06-21`, `review_by: 2026-09-21`.
    - Contains the literal string `app-sso platform -s` in a fenced bash block as the verification step, framed by a "Say to the user" Terminal walkthrough (C1/D-03).
    - Documents the SE-key-loss-after-password-reset recovery path including the macOS 14 Repair path (contains "Repair") and the macOS 13 Company Portal deregister + re-register branch.
    - Escalation Criteria "See" line links to `../l2-runbooks/27-macos-sso-investigation.md` (in-phase edge E5).
    - Contains the verbatim footer `[Back to macOS ADE Triage](../decision-trees/06-macos-triage.md)`.
    - Does NOT contain `security find-certificate`.
    - Does NOT contain `app-sso diagnose`.
    - Does NOT contain `35-macos-sso-sign-in-failure` (no #35↔#36 cross-link, D-04).
  </acceptance_criteria>
  <done>The L1 #36 runbook exists, opens with the app-sso platform -s Terminal verification, documents the SE key loss + version-branched re-registration recovery, escalates to L2 #27, carries the back-to-triage footer, contains none of the prohibited strings, and has no cross-link to #35; the verify command prints PASS.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only deliverable, no executable code or new attack surface; the commands documented are read-only device-side diagnostics (`app-sso platform -s`) invoked by the end user in Terminal. No user-input handling, no service, no installed packages. No STRIDE register required for this phase.
</threat_model>

<verification>
- Both L1 runbook files exist with the verified frontmatter (audience: L1, platform: macOS, applies_to: ADE, dates 2026-06-21 / 2026-09-21).
- Both contain `app-sso platform -s` as their first triage/verification step inside a "Say to the user" Terminal walkthrough.
- Both contain the in-phase escalation link to `../l2-runbooks/27-macos-sso-investigation.md` (E5).
- Both carry the verbatim `[Back to macOS ADE Triage](../decision-trees/06-macos-triage.md)` footer.
- Neither contains `security find-certificate` or `app-sso diagnose`.
- Neither file references the other (D-04/D2 strict independence).
- Neither links any Phase-81-owned nav hub or decision-tree SSO leaf.
</verification>

<success_criteria>
- SSORUN-01 satisfied: `35-macos-sso-sign-in-failure.md` triages the four root causes with `app-sso platform -s` first and escalation to L2 #27 (ROADMAP Phase 80 SC1).
- SSORUN-02 satisfied: `36-macos-secure-enclave-key.md` verifies SE key state and guides the password-reset re-registration recovery with `app-sso platform -s` and escalation to L2 #27 (ROADMAP Phase 80 SC2).
- All locked decisions encoded: A1 (n/a here), B1/D-02a (in-phase E5 edge + kept triage footer, no forward SSO-leaf link), C1/D-03 (guided Terminal walkthrough, verbatim command), D2/D-04 (strict #35↔#36 independence).
</success_criteria>

<output>
Create `.planning/phases/80-l1-l2-runbooks/80-01-SUMMARY.md` when done.
</output>
