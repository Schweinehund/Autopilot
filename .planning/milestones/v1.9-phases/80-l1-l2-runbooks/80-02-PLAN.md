---
phase: 80-l1-l2-runbooks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l2-runbooks/27-macos-sso-investigation.md
autonomous: true
requirements: [SSORUN-03]
must_haves:
  truths:
    - "L2 engineers can investigate PSSO registration failures and Password-sync failures using two sequential linear tracks (Registration-failure first, then Password-sync-failure) modeled on 19-android-enrollment-investigation.md (D-01/A1)"
    - "L2 #27 opens with a macOS log-collection prerequisite handoff to 10-macos-log-collection.md (D-01b)"
    - "L2 #27 carries a 'From L1 escalation?' routing block near the top routing #35 and #36 to the matching track (D-01a)"
    - "L2 #27 documents sign-in logs, the sysdiagnose+debug-logging procedure, TLS-inspection exclusion verification, per-user-MFA and AD-bound-account checks (SSORUN-03)"
    - "The macOS 15.0-15.2 re-registration loop is presented as a version-gated callout stating 'fixed in 15.3' (SC3)"
    - "L2 #27 carries reciprocal back-references to both #35 and #36 (D-02a/SC4 edge E6); security find-certificate appears NOWHERE and app-sso platform -s is the sole device-side state command (SC3)"
  artifacts:
    - path: "docs/l2-runbooks/27-macos-sso-investigation.md"
      provides: "L2 runbook: two-track macOS PSSO investigation (Registration-failure + Password-sync-failure) with sysdiagnose, TLS exclusions, MFA/AD-bound checks, 15.0-15.2 version gate (SSORUN-03)"
      contains: "app-sso platform -s"
  key_links:
    - from: "docs/l2-runbooks/27-macos-sso-investigation.md"
      to: "docs/l2-runbooks/10-macos-log-collection.md"
      via: "log-collection prerequisite opener (D-01b)"
      pattern: "10-macos-log-collection\\.md"
    - from: "docs/l2-runbooks/27-macos-sso-investigation.md"
      to: "docs/l1-runbooks/35-macos-sso-sign-in-failure.md"
      via: "Related Resources reciprocal back-reference (E6)"
      pattern: "35-macos-sso-sign-in-failure\\.md"
    - from: "docs/l2-runbooks/27-macos-sso-investigation.md"
      to: "docs/l1-runbooks/36-macos-secure-enclave-key.md"
      via: "Related Resources reciprocal back-reference (E6)"
      pattern: "36-macos-secure-enclave-key\\.md"
---

<objective>
Create the L2 macOS Platform SSO investigation runbook `27-macos-sso-investigation.md` per SSORUN-03, structured as two sequential linear investigation tracks (Registration-failure first, then Password-sync-failure) modeled on `docs/l2-runbooks/19-android-enrollment-investigation.md` — NOT the Mermaid-router model of #26.

Purpose: Give L2 (Desktop Engineer) staff a structured, inline investigation guide covering PSSO registration failures and Password-sync failures, with sign-in logs, the verified sysdiagnose + AppSSO debug-logging procedure, TLS-inspection exclusion verification, per-user-MFA and AD-bound-account checks, and the version-gated macOS 15.0-15.2 re-registration-loop callout ("fixed in 15.3").

Output: `docs/l2-runbooks/27-macos-sso-investigation.md` (SSORUN-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — REQUIRED before authoring (binding decisions + VERIFIED commands + research-flag resolution + copy-ready excerpts)
@.planning/phases/80-l1-l2-runbooks/80-CONTEXT.md
@.planning/phases/80-l1-l2-runbooks/80-RESEARCH.md
@.planning/phases/80-l1-l2-runbooks/80-PATTERNS.md

# Primary house-style analog — the two-track linear L2 investigation template (read in full)
@docs/l2-runbooks/19-android-enrollment-investigation.md

# Secondary analog — the macOS L2 log-collection prerequisite this runbook hands off to
@docs/l2-runbooks/10-macos-log-collection.md

# Link-not-copy canonical sources — summarize + link, NEVER duplicate their tables
@docs/admin-setup-macos/07-platform-sso-setup.md
@docs/admin-setup-macos/08-auth-methods-deep-dive.md
@docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create L2 #27 — macOS Platform SSO Investigation (SSORUN-03)</name>
  <files>docs/l2-runbooks/27-macos-sso-investigation.md</files>
  <read_first>
    - `docs/l2-runbooks/19-android-enrollment-investigation.md` — the two-track linear L2 investigation template: frontmatter (lines 1-7), platform-gate blockquote (line 9), Context with log-collection prerequisite opener (lines 13-17), "From L1 escalation?" routing block (lines 19-24), per-section step structure, Microsoft Support escalation packet pattern (lines 138-143), Related Resources (lines 288-297), Version History (lines 299-303).
    - `docs/l2-runbooks/10-macos-log-collection.md` — the macOS L2 log-collection prerequisite #27 must hand off to (D-01b); confirm the exact link target filename.
    - `.planning/phases/80-l1-l2-runbooks/80-PATTERNS.md` §"docs/l2-runbooks/27-macos-sso-investigation.md" — copy-ready frontmatter (audience: L2, platform: macOS), adapted platform-gate blockquote, Context+prerequisite opener, the adapted "From L1 escalation?" routing block (35→Track A, 36→Track A SE-key path, Password-sync→Track B), the two-track heading skeleton (Track A Steps 1-5, Track B Steps 1-2), the version-gate callout blockquote (verbatim 15.0-15.2 → fixed in 15.3), the Microsoft Support escalation packet labels, the Related Resources block with MANDATORY back-references to #35 and #36, and the Version History row.
    - `.planning/phases/80-l1-l2-runbooks/80-RESEARCH.md` §2 (app-sso diagnose is UNVERIFIED — do NOT use; use the sysdiagnose procedure instead), §3 (verified log predicate `subsystem == "com.apple.AppSSO"`), §4 (verified sysdiagnose + debug-enable/reset procedure), §5 (macOS 15.0-15.2 loop — fixed in 15.3, error signature), §8 (TLS exclusion endpoints + DF-10 link-not-copy), §9 (per-user MFA blocker / DF-3), §10 (AD-bound mobile account / DF-7); §Pitfall 5 (do NOT fabricate app-sso failure-state JSON field values); §Code Examples (verbatim debug-logging + sysdiagnose + log stream commands).
    - `docs/admin-setup-macos/07-platform-sso-setup.md` — Configuration-Caused Failures + Known Silent Blockers DF-10 (TLS exclusions) to SUMMARIZE + LINK.
    - `docs/admin-setup-macos/08-auth-methods-deep-dive.md` — DF-3 (per-user MFA), DF-7 (AD-bound account), SE key behavior to SUMMARIZE + LINK.
    - `docs/l2-runbooks/26-apple-business-permission-denied.md` — READ ONLY to understand the REJECTED Mermaid-router structure (A2). Do NOT replicate it.
  </read_first>
  <action>
    Create `docs/l2-runbooks/27-macos-sso-investigation.md` as a two-track LINEAR investigation runbook (D-01/A1) — do NOT use a Mermaid decision tree as the primary structure (A2 rejected).

    Frontmatter: last_verified: 2026-06-21, review_by: 2026-09-21, applies_to: ADE, audience: L2, platform: macOS.

    Platform-gate blockquote: adapted macOS-PSSO version per PATTERNS.md §27 (covers macOS Platform SSO L2 investigation via Intune and Entra; links to Windows L2 and macOS ADE index sections — link to existing 00-index.md only).

    Context section: scope statement (two failure classes — registration failures and Password-sync failures). MUST open with the log-collection prerequisite handoff (D-01b): "Before starting: collect a diagnostic package per the [macOS Log Collection Guide](10-macos-log-collection.md)." plus the note to enable AppSSO debug logging before reproducing.

    "From L1 escalation?" routing block near the top (D-01a): route L1 35 → Track A (Registration Failure), L1 36 → Track A (SE-key re-registration path), Password-sync symptoms → Track B; "No L1 escalation" → begin at Track A Step 1.

    Track A: Registration Failure Investigation — sequential steps: (1) confirm registration state via `app-sso platform -s` (verbatim, fenced bash; describe healthy REGISTERED/REGISTERED state, conservative prose for failure — no fabricated field values); (2) Intune portal device configuration status (note: "Succeeded" ≠ registration completed); (3) AppSSO debug logging + sysdiagnose capture using the VERIFIED procedure — `sudo log config --mode "level:debug,persist:debug" --subsystem "com.apple.AppSSO"` → reproduce → `sudo sysdiagnose` → `sudo log config --reset --subsystem "com.apple.AppSSO"`; optionally the live `log stream --predicate 'subsystem == "com.apple.AppSSO"' --info` command. Do NOT use `app-sso diagnose` (UNVERIFIED per RESEARCH §2); (4) TLS-inspection exclusion verification — summarize the requirement and LINK to guide 07 DF-10; instruct that the network team must confirm proxy bypass (device-side cannot detect silent TLS interception); (5) macOS 15.0-15.2 re-registration loop check — present the VERSION-GATED callout blockquote stating the affected range 15.0-15.2, the concurrent AppSSOAgent/AppSSODaemon corruption cause, the sysdiagnose error signature, and "Fixed in macOS 15.3" with the action (upgrade; if persists on 15.3+ file Apple Care).

    Track B: Password-Sync Failure Investigation — sequential steps: (1) per-user MFA check (Entra admin center > Users > [user] > Per-user MFA; legacy per-user MFA silently blocks Password sync — summarize + link guide 08 DF-3); (2) AD-bound (mobile) account check (`dscl . -read /Users/<username> OriginalNodeName` or System Settings account type; AD mobile accounts silently fail Password sync — summarize + link guide 08 DF-7).

    Microsoft Support escalation packet: adapt the #19 packet labels to PSSO (full `app-sso platform -s` output, sysdiagnose archive with AppSSO debug enabled, Company Portal log incident ID, Intune device configuration status screenshot, macOS + Company Portal versions, Entra sign-in log screenshot).

    Related Resources: MUST include reciprocal back-references to BOTH `../l1-runbooks/35-macos-sso-sign-in-failure.md` and `../l1-runbooks/36-macos-secure-enclave-key.md` (D-02a/E6 — MANDATORY), the `10-macos-log-collection.md` prerequisite, and link-not-copy references to guides 07/08/09 and the macOS ADE L2 index section.

    Version History: descriptive row per PATTERNS.md — `| 2026-06-21 | Initial version — macOS Platform SSO L2 investigation (SSORUN-03): Registration Failure track + Password-Sync Failure track; macOS 15.0–15.2 version-gate callout | -- |`.

    PROHIBITED (corpus rules / anti-patterns): `security find-certificate` must appear NOWHERE (SC3 absolute — `app-sso platform -s` is the sole device-side state command); `app-sso diagnose` must appear NOWHERE (UNVERIFIED); NO Mermaid decision tree as the primary document structure (A2); NO link to a decision-tree SSO leaf or any nav-hub (index.md/common-issues/quick-ref — Phase 81); do NOT fabricate app-sso failure-state JSON field names; do NOT duplicate any guide 07/08/09 table (link-not-copy).
  </action>
  <verify>
    <automated>test -f docs/l2-runbooks/27-macos-sso-investigation.md && grep -q 'app-sso platform -s' docs/l2-runbooks/27-macos-sso-investigation.md && grep -q '10-macos-log-collection.md' docs/l2-runbooks/27-macos-sso-investigation.md && grep -q '35-macos-sso-sign-in-failure.md' docs/l2-runbooks/27-macos-sso-investigation.md && grep -q '36-macos-secure-enclave-key.md' docs/l2-runbooks/27-macos-sso-investigation.md && grep -q '15.3' docs/l2-runbooks/27-macos-sso-investigation.md && grep -q 'sysdiagnose' docs/l2-runbooks/27-macos-sso-investigation.md && grep -q 'com.apple.AppSSO' docs/l2-runbooks/27-macos-sso-investigation.md && ! grep -q 'security find-certificate' docs/l2-runbooks/27-macos-sso-investigation.md && ! grep -q 'app-sso diagnose' docs/l2-runbooks/27-macos-sso-investigation.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/l2-runbooks/27-macos-sso-investigation.md` exists.
    - Frontmatter has `audience: L2`, `platform: macOS`, `applies_to: ADE`, `last_verified: 2026-06-21`, `review_by: 2026-09-21`.
    - Structured as two linear tracks: a "Track A" Registration Failure section appears before a "Track B" Password-Sync Failure section (D-01/A1 ordering).
    - Context section contains the log-collection prerequisite handoff linking `10-macos-log-collection.md` (D-01b).
    - Contains a "From L1 escalation?" routing block near the top referencing L1 35 and L1 36 (D-01a).
    - Contains `app-sso platform -s` (verbatim, in a bash block) as the Track A registration-state check.
    - Contains the verified sysdiagnose procedure (`sysdiagnose` + `com.apple.AppSSO` debug-logging) — NOT `app-sso diagnose`.
    - Documents TLS-inspection exclusion verification, per-user MFA check, and AD-bound account check.
    - Contains the macOS 15.0-15.2 version-gate callout stating "fixed in 15.3" (contains `15.3`).
    - Related Resources contains reciprocal back-references to BOTH `../l1-runbooks/35-macos-sso-sign-in-failure.md` and `../l1-runbooks/36-macos-secure-enclave-key.md` (E6).
    - Does NOT contain `security find-certificate`.
    - Does NOT contain `app-sso diagnose`.
    - Does NOT use a Mermaid decision tree as the primary structure and does NOT link any Phase-81-owned nav hub or decision-tree SSO leaf.
  </acceptance_criteria>
  <done>The L2 #27 runbook exists as a two-track linear investigation with the log-collection opener, the From-L1-escalation routing block, app-sso platform -s and the verified sysdiagnose procedure, TLS/MFA/AD-bound checks, the 15.0-15.2 "fixed in 15.3" version gate, and reciprocal back-references to #35 and #36; it contains none of the prohibited strings; the verify command prints PASS.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only deliverable, no executable code or new attack surface; the commands documented are read-only diagnostics (`app-sso platform -s`, `sysdiagnose`, `log stream`/`log config` for the AppSSO subsystem) run by an engineer on the affected device. No user-input handling, no service, no installed packages. No STRIDE register required for this phase.
</threat_model>

<verification>
- `27-macos-sso-investigation.md` exists with verified frontmatter (audience: L2, platform: macOS, applies_to: ADE, 2026-06-21 / 2026-09-21).
- Two-track linear structure (Track A Registration before Track B Password-sync); no Mermaid router as primary structure.
- Log-collection prerequisite opener (D-01b) + "From L1 escalation?" routing block (D-01a) present near the top.
- app-sso platform -s + verified sysdiagnose/debug-logging procedure present; `app-sso diagnose` absent.
- TLS exclusion, per-user MFA, AD-bound account checks present.
- macOS 15.0-15.2 version-gate callout with "fixed in 15.3" present.
- Reciprocal back-references to both #35 and #36 present (E6).
- `security find-certificate` absent (SC3).
- No links to Phase-81-owned nav hubs or the decision-tree SSO leaf.
</verification>

<success_criteria>
- SSORUN-03 satisfied: `27-macos-sso-investigation.md` enables L2 to investigate PSSO registration + Password-sync failures via sign-in logs, sysdiagnose, TLS-inspection exclusions, and per-user-MFA/AD-bound checks, with the 15.0-15.2 loop version-gated "fixed in 15.3" and `security find-certificate` absent (ROADMAP Phase 80 SC3).
- All locked decisions encoded: A1/D-01 (two linear tracks modeled on #19), D-01a (From-L1 routing block), D-01b (log-collection opener), B1/D-02a (reciprocal E6 back-refs; no forward SSO-leaf/nav-hub links), research-flag resolution (sysdiagnose instead of UNVERIFIED app-sso diagnose; verified com.apple.AppSSO predicate).
</success_criteria>

<output>
Create `.planning/phases/80-l1-l2-runbooks/80-02-SUMMARY.md` when done.
</output>
