---
phase: 81-nav-hub-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.md
  - docs/common-issues.md
  - docs/quick-ref-l1.md
  - docs/quick-ref-l2.md
autonomous: true
requirements: [SSOREF-04]

must_haves:
  truths:
    - "An admin browsing docs/index.md macOS Provisioning section sees a Platform SSO row in each of the Admin Setup, L1, and L2 tables"
    - "A support tech with an SSO sign-in failure finds a routing entry in common-issues.md pointing to L1 #35 and L2 #27"
    - "An L1 tech finds SSO escalation triggers in quick-ref-l1.md (Secure Enclave key error -> #36; sign-in loop -> #35)"
    - "An L2 engineer finds SSO log paths and the verbatim app-sso platform -s attestation command in quick-ref-l2.md"
    - "All existing rows/sections in these four files are byte-unchanged (append-only)"
  artifacts:
    - path: "docs/index.md"
      provides: "Three new Platform SSO grouped rows (Admin Setup, L1, L2 tables)"
      contains: "macOS Platform SSO Admin Setup Guides"
    - path: "docs/common-issues.md"
      provides: "Platform SSO Sign-In Failure H3 entry routing to #35/#27"
      contains: "Platform SSO Sign-In Failure"
    - path: "docs/quick-ref-l1.md"
      provides: "Two SSO escalation triggers + two SSO runbook links"
      contains: "35-macos-sso-sign-in-failure.md"
    - path: "docs/quick-ref-l2.md"
      provides: "Platform SSO log paths + app-sso platform -s command + L2 #27 link"
      contains: "app-sso platform -s"
  key_links:
    - from: "docs/index.md"
      to: "l1-runbooks/00-index.md#macos-ade-runbooks"
      via: "appended grouped row naming #35 + #36"
      pattern: "macOS Platform SSO Runbooks"
    - from: "docs/quick-ref-l2.md"
      to: "l2-runbooks/27-macos-sso-investigation.md"
      via: "appended investigation runbook bullet"
      pattern: "27-macos-sso-investigation"
---

<objective>
Wire the already-shipped v1.9 Platform SSO content into the four reader-facing nav hubs via append-only edits: the macOS Provisioning tables in `docs/index.md` (SC1, D-04), the macOS failure-scenarios section of `docs/common-issues.md`, and the macOS quick-reference sections of `docs/quick-ref-l1.md` and `docs/quick-ref-l2.md` (SC2).

Purpose: Admins and support staff can discover guides 07/08/09 and runbooks #35/#36/#27 from the navigation hubs, and L1/L2 staff get SSO escalation triggers, log paths, and the canonical attestation command at their fingertips.

Output: Four edited markdown files, each gaining additive nav entries + a Version History row. No existing rows/sections modified (append-only invariant, navigation-last).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-nav-hub-integration/81-CONTEXT.md
@.planning/phases/81-nav-hub-integration/81-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append three Platform SSO grouped rows to docs/index.md macOS Provisioning tables (D-04, SC1)</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md (lines 98-131 — the three macOS Provisioning sub-tables: Service Desk (L1) at ~102-109, Desktop Engineering (L2) at ~111-121, Admin Setup at ~123-129; line 131 is the `---` iOS boundary)
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md §"1. docs/index.md" (verbatim append rows + exact append points)
  </read_first>
  <action>
    Append exactly ONE grouped row to EACH of the three macOS Provisioning sub-tables, matching the established single-grouped-row house style. Do NOT modify any existing row.
    (a) After the last L1 row (`[L1 Quick-Reference Card]...` at ~line 109), append a `[macOS Platform SSO Runbooks](l1-runbooks/00-index.md#macos-ade-runbooks)` row whose "When to Use" cell names BOTH runbook #35 (Platform SSO sign-in failure / "Registration Required" not appearing) AND runbook #36 (Secure Enclave key loss after password reset).
    (b) After the last L2 row (`[L2 Quick-Reference Card]...` at ~line 121), append a `[macOS Platform SSO Investigation](l2-runbooks/00-index.md#macos-ade-runbooks)` row naming runbook #27 (PSSO registration / Password-sync failure investigation).
    (c) After the last Admin Setup row (`[macOS Admin Setup Guides]...` at ~line 129, before the `---` at line 131), append a `[macOS Platform SSO Admin Setup Guides](admin-setup-macos/00-overview.md)` row whose "When to Use" cell — per the D-04 GUARDRAIL — names all three guides by topic: guide 07 (setup), guide 08 (auth-method selection / deep-dive: Secure Enclave key, Password sync, Smart card), and guide 09 (legacy SSO plug-in migration).
    Use the verbatim row strings provided in 81-RESEARCH.md §1a/§1b/§1c as the authoritative copy. Then append one Version History row dated 2026-06-22: `| 2026-06-22 | Phase 81 (SSOREF-04): appended Platform SSO rows to macOS Admin Setup / L1 / L2 nav tables | -- |` (only if index.md has a Version History table; if absent, skip per existing file convention).
  </action>
  <acceptance_criteria>
    - `grep -c "macOS Platform SSO Admin Setup Guides" docs/index.md` returns 1, AND that row's "When to Use" cell contains the substrings "guide 07", "guide 08", and "guide 09" (D-04 guardrail: all three named).
    - `grep -c "macOS Platform SSO Runbooks" docs/index.md` returns 1, and that row references both "#35" and "#36".
    - `grep -c "macOS Platform SSO Investigation" docs/index.md` returns 1, and references "#27".
    - The pre-existing rows are unchanged: `grep -c "macOS Admin Setup Guides\](admin-setup-macos/00-overview.md)" docs/index.md` still returns 2 (the original overview row + the new SSO row), and the original `[macOS L1 Runbooks](l1-runbooks/00-index.md#macos-ade-runbooks)` line is byte-identical to before.
    - `git diff docs/index.md` shows only additions (lines prefixed `+`), zero deletions of existing table rows.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "macOS Platform SSO Admin Setup Guides" docs/index.md | grep -qx 1 && grep -c "macOS Platform SSO Runbooks" docs/index.md | grep -qx 1 && grep -c "macOS Platform SSO Investigation" docs/index.md | grep -qx 1 && echo PASS</automated>
  </verify>
  <done>Three Platform SSO grouped rows appended (one per macOS sub-table); Admin Setup row names guides 07/08/09 by topic; L1 row names #35/#36; L2 row names #27; existing rows byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Append Platform SSO failure entry to docs/common-issues.md macOS section (SC2)</name>
  <files>docs/common-issues.md</files>
  <read_first>
    - docs/common-issues.md (lines 157-213 — `## macOS ADE Failure Scenarios`; the last H3 `### Company Portal Sign-In Failure` at ~206-211; the hard lower boundary `## iOS/iPadOS Failure Scenarios` at line 213; Version History table near line 380)
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md §"2. docs/common-issues.md" (verbatim entry + append point + house-style note)
  </read_first>
  <action>
    After the existing `### Company Portal Sign-In Failure` entry (after its `- **L2:**` bullet, ~line 211) and BEFORE `## iOS/iPadOS Failure Scenarios` (line 213), append ONE new H3 entry matching the existing pattern (H3 heading + one-line description + `- **L1:**` bullet + `- **L2:**` bullet). Heading: `### Platform SSO Sign-In Failure`. The `- **L1:**` bullet links to `l1-runbooks/35-macos-sso-sign-in-failure.md` (and may name the four root causes per 81-RESEARCH.md); the `- **L2:**` bullet links to `l2-runbooks/27-macos-sso-investigation.md`. Do NOT add a `> **macOS:**` banner (house-style note: section-internal H3 entries carry no banner). Use the verbatim entry block in 81-RESEARCH.md §2. Then append one Version History row: `| 2026-06-22 | Phase 81 (SSOREF-04): appended Platform SSO Sign-In Failure entry routing to L1 #35 / L2 #27 | -- |`.
  </action>
  <acceptance_criteria>
    - `grep -c "### Platform SSO Sign-In Failure" docs/common-issues.md` returns 1.
    - The new entry contains a link to `l1-runbooks/35-macos-sso-sign-in-failure.md` AND a link to `l2-runbooks/27-macos-sso-investigation.md` (`grep -c "35-macos-sso-sign-in-failure" docs/common-issues.md` >= 1 and `grep -c "27-macos-sso-investigation" docs/common-issues.md` >= 1).
    - The new H3 appears BEFORE `## iOS/iPadOS Failure Scenarios` (the macOS section boundary is not crossed): in the file, the line number of `### Platform SSO Sign-In Failure` is less than the line number of `## iOS/iPadOS Failure Scenarios`.
    - The prior entry `### Company Portal Sign-In Failure` is byte-unchanged (still exactly 1 occurrence, original text intact).
    - `git diff docs/common-issues.md` shows additions only.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "### Platform SSO Sign-In Failure" docs/common-issues.md | grep -qx 1 && grep -q "27-macos-sso-investigation" docs/common-issues.md && echo PASS</automated>
  </verify>
  <done>One Platform SSO Sign-In Failure H3 appended inside the macOS section (before the iOS boundary), routing to L1 #35 and L2 #27; no banner; Company Portal entry unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Append SSO triggers + log paths + app-sso command to quick-ref-l1.md and quick-ref-l2.md (SC2)</name>
  <files>docs/quick-ref-l1.md, docs/quick-ref-l2.md</files>
  <read_first>
    - docs/quick-ref-l1.md (lines 83-114 — macOS section; `### macOS Escalation Triggers` ~94-101 last bullet "Device non-compliant..."; `### macOS Runbooks` ~105-114 last bullet `[Company Portal Sign-In]`; `---` / iOS boundary at line 116)
    - docs/quick-ref-l2.md (lines 132-179 — macOS section; `### Critical Log Paths` ~161-170; `### macOS Investigation Runbooks` ~173-179 last bullet `[Compliance Evaluation Investigation]`; `---` / iOS boundary at line 181)
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md §"3. quick-ref-l1.md" and §"4. quick-ref-l2.md" (verbatim append blocks; the verbatim `app-sso platform -s` command)
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md §"Common Pitfalls" Pitfall 3 (NO `security find-certificate`; `app-sso platform -s` verbatim)
  </read_first>
  <action>
    quick-ref-l1.md (two appends, both inside the macOS section, before the line-116 boundary):
    (a) Append TWO new bullets to `### macOS Escalation Triggers` after the last existing bullet: one for Secure Enclave key error after password reset / FileVault recovery routing to `l1-runbooks/36-macos-secure-enclave-key.md`; one for Platform SSO sign-in loop / "Registration Required" never appeared routing to `l1-runbooks/35-macos-sso-sign-in-failure.md`. Each may reference `app-sso platform -s` in its collect list.
    (b) Append TWO new bullets to `### macOS Runbooks` after the last existing bullet: `[Platform SSO Sign-In Failure](l1-runbooks/35-macos-sso-sign-in-failure.md)` and `[Platform SSO — Secure Enclave Key Loss](l1-runbooks/36-macos-secure-enclave-key.md)`.

    quick-ref-l2.md (two appends, both inside the macOS section, before the line-181 boundary):
    (c) Append a new `### Platform SSO Log Paths` sub-section (a Path|Purpose table for PSSO/CompanyPortal log paths) PLUS a `#### Platform SSO Attestation Command` block that reproduces the device-side command EXACTLY as `app-sso platform -s` (verbatim — this is the SOLE device-side command; `security find-certificate` must appear NOWHERE). Place it after the existing `### Critical Log Paths` content and before `### macOS Investigation Runbooks`.
    (d) Append ONE bullet to `### macOS Investigation Runbooks`: `[Platform SSO Investigation](l2-runbooks/27-macos-sso-investigation.md)`.

    Use the verbatim blocks in 81-RESEARCH.md §3 and §4 as the authoritative copy. Append one Version History row to EACH file dated 2026-06-22 with a Phase 81 (SSOREF-04) description.
  </action>
  <acceptance_criteria>
    - quick-ref-l1.md: `grep -c "36-macos-secure-enclave-key" docs/quick-ref-l1.md` >= 1 AND `grep -c "35-macos-sso-sign-in-failure" docs/quick-ref-l1.md` >= 1; both new escalation-trigger bullets and both new runbook bullets present.
    - quick-ref-l2.md: `grep -c "app-sso platform -s" docs/quick-ref-l2.md` >= 1 (verbatim command present).
    - quick-ref-l2.md: `grep -c "security find-certificate" docs/quick-ref-l2.md` returns 0 (forbidden command absent — key invariant).
    - quick-ref-l2.md: `grep -c "27-macos-sso-investigation" docs/quick-ref-l2.md` >= 1 (L2 #27 investigation bullet present).
    - Both files: edits land before their respective `## iOS/iPadOS Quick Reference` boundary (macOS section not crossed); existing bullets byte-unchanged.
    - `git diff` on both files shows additions only.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "app-sso platform -s" docs/quick-ref-l2.md && ! grep -q "security find-certificate" docs/quick-ref-l2.md && grep -q "27-macos-sso-investigation" docs/quick-ref-l2.md && grep -q "36-macos-secure-enclave-key" docs/quick-ref-l1.md && grep -q "35-macos-sso-sign-in-failure" docs/quick-ref-l1.md && echo PASS</automated>
  </verify>
  <done>quick-ref-l1.md has two SSO escalation triggers (#36, #35) + two SSO runbook links; quick-ref-l2.md has Platform SSO log paths + verbatim `app-sso platform -s` + L2 #27 bullet; `security find-certificate` absent; existing content unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change surface. No executable code, no user input, no auth surface, no network/data persistence is added or modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01 | Information Disclosure | Appended nav entries / commands in static markdown | accept | Content is operator-facing documentation summarizing already-shipped guides/runbooks; reproduces only the already-public `app-sso platform -s` command. No secrets introduced. |
| T-81-02 | Tampering (integrity) | Broken/incorrect internal links from new nav rows | mitigate | Exact-string acceptance criteria assert link targets verbatim; the frozen v1.8 harness does NOT crawl internal links, so the Plan 04 `81-CROSSLINK-CLOSURE.md` checklist is the cross-corpus safety net. |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — append-only markdown edits, `git` only. No supply-chain surface. |
</threat_model>

<verification>
- All four files edited append-only; `git diff` shows additions only (zero deletions of existing rows/sections).
- `app-sso platform -s` present verbatim in quick-ref-l2.md; `security find-certificate` absent.
- index.md Admin Setup SSO row names guides 07/08/09; L1 row names #35/#36; L2 row names #27.
- common-issues.md SSO entry routes to L1 #35 and L2 #27, inside the macOS section.
</verification>

<success_criteria>
- SC1 satisfied for the three index.md tables (append-only grouped rows).
- SC2 satisfied for common-issues.md (SSO entry), quick-ref-l1.md (triggers), quick-ref-l2.md (log paths + attestation command).
- Navigation-last + append-only invariants honored across all four files.
</success_criteria>

<output>
Create `.planning/phases/81-nav-hub-integration/81-01-SUMMARY.md` when done. Record the exact appended row/entry text and the Version History rows added, so Plan 04's closure checklist can cite this work.
</output>
