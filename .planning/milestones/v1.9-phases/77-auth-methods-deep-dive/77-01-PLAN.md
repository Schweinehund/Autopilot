---
phase: 77-auth-methods-deep-dive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-macos/08-auth-methods-deep-dive.md
  - docs/admin-setup-macos/00-overview.md
  - docs/v1.9-DEFERRED-CLEANUP.md
autonomous: true
requirements: [PSSO-05, PSSO-06, PSSO-07, PSSO-08, PSSO-09, PSSO-10, PSSO-11]

must_haves:
  truths:
    - "An admin reading guide 08 can select between Secure Enclave key, Password sync, and Smart card using a four-dimension comparison table at the top of the doc, with Secure Enclave clearly marked as Microsoft's recommendation (SC1)"
    - "An admin reading the Secure Enclave section learns the key is non-exportable/hardware-bound, drives a device-wide PRT, FileVault still uses the local password at cold boot, and an MDM/recovery password reset destroys the key and forces re-registration (SC2)"
    - "An admin reading the Password sync section learns the ~4-hour sync window, complexity-mismatch failure, macOS 15+ FileVaultPolicy=AttemptAuthentication, the per-user-MFA blocker, and the AD-bound account limitation (SC3)"
    - "An admin reading the Smart card section sees a hard Entra-CBA prerequisite callout first, then sc_auth pairing, the macOS 14+ gate, and the not-available-during-Setup-Assistant constraint; the Touch ID subsection carries the no-password-fallback lockout warning (SC4)"
    - "Passkey/FIDO2 (AAGUID 7FD635B3-2EF9-4542-8D9D-164F2C771EFC with the conditional key-restrictions note) and NUAL (macOS 14+, Shared Device Keys, com.apple.PlatformSSO.AccountShortName) are each documented; the NewUserAuthorizationMode key literal is omitted from guide 08 and tracked in v1.9-DEFERRED-CLEANUP.md (SC5)"
    - "The 00-overview.md line-47 code-span is converted to a live markdown link in the same commit guide 08 is created; the v1.8 C13 broken-link audit passes 15/15"
  artifacts:
    - path: "docs/admin-setup-macos/08-auth-methods-deep-dive.md"
      provides: "Auth-method selection table + three-method deep-dive + advanced sections + corpus tail"
      contains: "Recommended (Microsoft)"
      min_lines: 120
    - path: "docs/admin-setup-macos/00-overview.md"
      provides: "Live link to guide 08 (line-47 conversion) + new Version-History row"
      contains: "[Auth Methods Deep-Dive](08-auth-methods-deep-dive.md)"
    - path: "docs/v1.9-DEFERRED-CLEANUP.md"
      provides: "Tracking record for the omitted NewUserAuthorizationMode MDM key literal"
      contains: "NewUserAuthorizationMode"
  key_links:
    - from: "docs/admin-setup-macos/00-overview.md"
      to: "docs/admin-setup-macos/08-auth-methods-deep-dive.md"
      via: "live markdown link replacing the line-47 code-span"
      pattern: "\\[Auth Methods Deep-Dive\\]\\(08-auth-methods-deep-dive\\.md\\)"
    - from: "docs/admin-setup-macos/08-auth-methods-deep-dive.md"
      to: "docs/admin-setup-macos/07-platform-sso-setup.md"
      via: "platform-gate banner + See Also link (ROADMAP depends-on)"
      pattern: "\\[Platform SSO Setup\\]\\(07-platform-sso-setup\\.md\\)"
    - from: "docs/admin-setup-macos/08-auth-methods-deep-dive.md"
      to: "docs/_glossary-macos.md#secure-enclave"
      via: "See Also glossary anchor"
      pattern: "_glossary-macos\\.md#secure-enclave"
---

<objective>
Author the macOS Platform SSO auth-method deep-dive / selection guide `docs/admin-setup-macos/08-auth-methods-deep-dive.md` and atomically wire it into corpus navigation by converting the `00-overview.md` line-47 code-span to a live markdown link. Also create/append `docs/v1.9-DEFERRED-CLEANUP.md` to track the deliberately-omitted `NewUserAuthorizationMode` MDM key literal (PSSO-11 / PSSO-FUT-01).

Purpose: Give architects and senior admins a single decision-first reference that lets them pick the right Platform SSO authentication method (Secure Enclave key [Microsoft-recommended], Password sync, Smart card) and avoid the three named dangers — FileVault does NOT use the Secure Enclave key, MDM/recovery password reset destroys the SE key, and Touch ID has a no-password-fallback lockout risk.

Output:
- NEW `docs/admin-setup-macos/08-auth-methods-deep-dive.md` (A3 hybrid structure)
- EDIT `docs/admin-setup-macos/00-overview.md` (line-47 code-span → live link + Version-History row)
- NEW/APPEND `docs/v1.9-DEFERRED-CLEANUP.md` (NUAL key tracking)

These three changes land in ONE atomic commit (C13 broken-link gate requires the live link and its target to appear together).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-auth-methods-deep-dive/77-CONTEXT.md
@.planning/phases/77-auth-methods-deep-dive/77-RESEARCH.md
@.planning/phases/77-auth-methods-deep-dive/77-PATTERNS.md
@docs/admin-setup-macos/07-platform-sso-setup.md
@docs/admin-setup-macos/02-enrollment-profile.md
@docs/admin-setup-macos/06-config-failures.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author guide 08 (08-auth-methods-deep-dive.md) in A3 hybrid structure</name>
  <files>docs/admin-setup-macos/08-auth-methods-deep-dive.md</files>
  <read_first>
    - .planning/phases/77-auth-methods-deep-dive/77-RESEARCH.md (all per-method fact sheets; the verbatim FileVault statement; the verbatim Touch-ID no-fallback warning; the distilled 5-step Entra CBA process; the CD-3 misconception rows; the four-dimension selection-table cells; the Confirmed Identifiers table)
    - .planning/phases/77-auth-methods-deep-dive/77-PATTERNS.md (front-matter block, platform-gate banner, hard-bordered callout syntax `> **Title:** body`, leading-empty-cell comparison-table shape, four-column `Misconfiguration | Portal | Symptom | Runbook` failures table, See Also link format, Version-History `| Date | Change | Author |` row with Author `--`, section-local `last_verified` annotation pattern, the duplication-with-cross-reference anchor pattern, the code-span-for-not-yet-authored-files mechanism)
    - .planning/phases/77-auth-methods-deep-dive/77-CONTEXT.md (locked decisions D-01/A3, D-02/C3, D-03/D3, D-04/B1; Claude's Discretion bounds)
    - docs/admin-setup-macos/07-platform-sso-setup.md (primary structural analog: front matter, banner, callouts, Configuration-Caused-Failures table, See Also, Version History — but do NOT clone its `## Steps` task-flow; 08 is a REFERENCE not a setup-TASK guide)
    - docs/admin-setup-macos/02-enrollment-profile.md (secondary skeleton/Version-History exemplar)
    - docs/_glossary-macos.md and docs/_glossary.md (confirm anchors #platform-sso, #secure-enclave, #enterprise-sso-plug-in, #entra-id-sso for See Also)
  </read_first>
  <action>
Create `docs/admin-setup-macos/08-auth-methods-deep-dive.md` as a NEW deep-dive/selection REFERENCE guide using the locked A3 hybrid structure. Internal heading names are at your discretion (new file = no anchor-stability constraint) within the SC1-SC5 factual constraints. Follow 77-PATTERNS.md markup exactly.

Front matter (copy the corpus block, dates per DS-2 90-day cadence):
- `last_verified: 2026-06-21`, `review_by: 2026-09-21`, `applies_to: ADE`, `audience: admin`, `platform: macOS`.

Platform-gate banner (three-line blockquote, adapt the second line per PATTERNS): point line 2 to `[Platform SSO Setup](07-platform-sso-setup.md)` and line 3 to `[macOS Glossary](../_glossary-macos.md)`.

Document body, in this order (D-01/A3):

1. SELECTION TABLE FIRST (D-04/B1 restructure arm). A leading-empty-cell comparison table built to SC1's EXACTLY FOUR dimensions — passwordless / phishing-resistant / hardware / macOS-version — plus the required "Microsoft recommendation" row marking Secure Enclave "Recommended", Password sync "Second choice", Smart card "Third choice". Use the verified cell values from 77-RESEARCH.md Code Examples. The literal string "Recommended (Microsoft)" (or the SC1-required recommendation marking) MUST be present so an admin can see Microsoft's recommendation at a glance. Do NOT lift the ~10-row SUMMARY draft table verbatim (D-04a) — extra dimensions (passkey-capable, local-password-synced, Settings-Catalog-key-value, macOS-13-vs-14 split) go into the per-method bodies, NOT this table.

2. `## Secure Enclave Key Method` (PSSO-06). State the six non-negotiable fact anchors: non-exportable hardware-bound private key; hardware-bound to this Mac + this user; drives a device-wide PRT; FileVault non-relationship; MDM/recovery-reset-destroys-the-key (re-register); WPJ-in-Secure-Enclave-since-Aug-2025 (`security find-certificate` false-negatives → use `app-sso platform -s`). Include the hardware scope (Apple Silicon + T2 Intel list; pre-2018 non-T2 Intel WILL FAIL → fallback to Password sync). Then these subsections per D1/D2:
   - A "what the SE key IS and IS NOT" sub-section (CD-2 mandate).
   - A dedicated `### FileVault and the Secure Enclave Key` sub-section (C3 canonical location, DF-6). State the FileVault fact ONCE here, canonically, using the verified hard-bordered callout from 77-RESEARCH.md (FileVault uses the local macOS password as disk key at cold boot; the SE key is a parallel mechanism, not a replacement; Touch ID/SSO only after disk unlocks). This is the single canonical home — other sections cross-reference this anchor, they do NOT restate it (duplication-with-cross-reference pattern).
   - A point-of-use hard-bordered callout for the MDM/recovery-reset-destroys-the-key danger (canonical home for that danger; the misconception box links here).
   - `### Touch ID Biometric Policy` subsection (PSSO-09): `enable_se_key_biometric_policy` [Company Portal 2504, Extension Data Boolean `true`] + Apple API `UserSecureEnclaveKeyBiometricPolicy` [macOS 14.6+]; the no-password-fallback lockout warning (reproduce the verbatim official Entra wording from 77-RESEARCH.md — do NOT soften it) as a hard-bordered callout; the admin-driven re-registration requirement (users do NOT see a re-registration prompt). Add a section-local `last_verified`/`review_by` provenance annotation (high-drift CP 2504 / macOS 14.6 facts).
   - `### Passkey / FIDO2 from the Platform Credential` subsection (PSSO-10): the Platform Credential as a WebAuthn/FIDO2 passkey; the AAGUID `7FD635B3-2EF9-4542-8D9D-164F2C771EFC` WITH the conditional note (only required when FIDO2 key restrictions apply in the Entra FIDO2 Authentication-Method policy); Entra Authentication-methods enablement; end-user self-enablement in supported browsers; Smart card does NOT support passkey use.

3. `## Password Sync Method` (PSSO-07). What syncs (Entra password becomes/stays equal to the local macOS password; local password is NOT removed); the ~4-hour sync window (add a section-local `last_verified` provenance note — MEDIUM confidence per Assumptions Log A2; phrase as "approximately 4 hours, confirm against current Microsoft Learn"); complexity-mismatch failure; macOS 15+ `FileVaultPolicy = AttemptAuthentication`; the per-user-MFA silent blocker (DF-3); the AD-bound (mobile) account limitation (DF-7). For FileVault cold-boot behavior, CROSS-REFERENCE the SE section's FileVault sub-section anchor (do NOT restate the canonical paragraph) — add the Password-sync nuance that here the local password equals the Entra password, so one password unlocks both FileVault and Entra.

4. `## Smart Card Method` (PSSO-08). OPEN with a hard prerequisite callout (BEFORE any method detail, DF-11): "Entra ID CBA must be configured in the tenant first (separate Entra admin task)." Summarize the distilled 5-step Entra CBA process from 77-RESEARCH.md (PKI container/CA upload → enable CBA for target security groups → authentication-binding policy set to MFA → username-binding policy mapping X.509 attribute → verify via MyApps before deploying the PSSO profile) and link the authoritative source `https://learn.microsoft.com/en-us/entra/identity/authentication/how-to-certificate-based-authentication`. Then: `sc_auth` pairing (smart-card cert paired with the local macOS account) — add a `last_verified` note that the exact `sc_auth` command is MEDIUM confidence per Assumptions Log A3 and point to Apple Platform Deployment documentation for the exact command; the macOS 14+ gate (NOT supported on macOS 13; deprecated field does not accept `SmartCard`); the not-available-during-Setup-Assistant constraint (reproduce the verbatim official wording from 77-RESEARCH.md).

5. `## New User At Login Window (NUAL)` (PSSO-11, conceptual only). macOS 14+; Shared Device Keys (`Platform SSO > Use Shared Device Keys` = Enabled); `com.apple.PlatformSSO.AccountShortName` Account-Name mapping; describe `Enable Create User At Login` behavior and the New-User-Authorization-Mode CONCEPT (Standard/Admin/Groups one-time permissions) by DISPLAY NAME ONLY. The `NewUserAuthorizationMode` MDM key LITERAL is OMITTED (D-03/PSSO-11) — do NOT write the key literal; add a short note that the exact MDM key is deferred pending verification (tracked in `v1.9-DEFERRED-CLEANUP.md`, created in Task 3).

6. `## Common Misconceptions` (D-03/D3 consolidated box). A hard-bordered callout introducing a myth-vs-fact table seeding the three named dangers + the CD-3 rows from 77-RESEARCH.md (the five rows: "No password needed after enrollment" → CONDITIONALLY TRUE; "Password sync removes the local password" → FALSE; "Secure Enclave key method means no password at all" → FALSE; "PSSO satisfies phishing-resistant MFA for CA" → TRUE; "Touch ID means no password is ever needed" → FALSE). Each danger's canonical home is its method section; this box CROSS-REFERENCES those sections via anchor links (FileVault → the FileVault sub-section anchor; key-destruction → the SE section; Touch-ID lockout → the Touch-ID subsection). Do NOT restate the canonical paragraphs.

7. `## Configuration-Caused Failures` (A3 corpus tail). Four-column table `Misconfiguration | Portal | Symptom | Runbook` matching guide 07's shape. Portal = `Intune` for all PSSO entries. Runbook column uses CODE-SPAN filenames (e.g. `` `35-macos-sso-sign-in-failure.md` ``) NOT live links (Phase 80 hasn't created them) or a bare `--` where no runbook applies. Seed rows from the research pitfalls: Smart card without Entra CBA (silent failure); auth-method change on existing fleet policy (fleet-wide re-registration); MDM/recovery password reset (SE key destroyed → re-register); Touch-ID biometric policy enabled on a device lacking Touch ID (no-fallback lockout); AAGUID listed without FIDO2 key restrictions configured.

8. `## See Also` (A3 corpus tail). MUST include a live link `[Platform SSO Setup](07-platform-sso-setup.md)` (ROADMAP depends-on) and the glossary anchors `[Platform SSO](../_glossary-macos.md#platform-sso)`, `[Secure Enclave](../_glossary-macos.md#secure-enclave)`, `[Enterprise SSO Plug-in](../_glossary-macos.md#enterprise-sso-plug-in)`, `[Entra ID SSO](../_glossary.md#entra-id-sso)`. Reference `09-enterprise-sso-plugin-migration.md` as a CODE-SPAN ONLY (`` `09-enterprise-sso-plugin-migration.md` ``), NOT a live link (Phase 78 not authored; C13 mechanism). Do NOT create any live link to a not-yet-existing file.

9. Version History tail: a horizontal rule `---` immediately followed by a `| Date | Change | Author |` table with one row dated `2026-06-21`, change text naming "Phase 77 (PSSO-05..11): initial version", Author `--` (double hyphen, NOT em-dash).

Use double-hyphen `--` (not em-dash) wherever the corpus prose uses an inline dash separator, matching the existing corpus style.
  </action>
  <verify>
    <automated>test -f docs/admin-setup-macos/08-auth-methods-deep-dive.md && grep -q "Recommended" docs/admin-setup-macos/08-auth-methods-deep-dive.md && grep -q "7FD635B3-2EF9-4542-8D9D-164F2C771EFC" docs/admin-setup-macos/08-auth-methods-deep-dive.md && grep -q "enable_se_key_biometric_policy" docs/admin-setup-macos/08-auth-methods-deep-dive.md && grep -q "com.apple.PlatformSSO.AccountShortName" docs/admin-setup-macos/08-auth-methods-deep-dive.md && grep -q "how-to-certificate-based-authentication" docs/admin-setup-macos/08-auth-methods-deep-dive.md && grep -q "07-platform-sso-setup.md" docs/admin-setup-macos/08-auth-methods-deep-dive.md && echo "GUIDE08_OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/admin-setup-macos/08-auth-methods-deep-dive.md` exists with the five-field corpus front matter (`applies_to: ADE`, `audience: admin`, `platform: macOS`, `last_verified: 2026-06-21`, `review_by: 2026-09-21`).
    - Contains the four-dimension selection table at the top with Secure Enclave marked as Microsoft's recommendation (string "Recommended" present; Smart card "Third choice").
    - Contains a dedicated FileVault/Secure-Enclave sub-section stating the cold-boot/local-password fact canonically ONCE.
    - Contains the Touch ID no-password-fallback lockout warning and `enable_se_key_biometric_policy`.
    - Contains the passkey AAGUID `7FD635B3-2EF9-4542-8D9D-164F2C771EFC` WITH the conditional key-restrictions note.
    - Contains the NUAL section with `com.apple.PlatformSSO.AccountShortName` and DOES NOT contain the literal token `NewUserAuthorizationMode` as a configured MDM key (omitted per D-03; a deferral note is acceptable but the key must not be presented as a usable key).
    - Smart card section opens with the Entra CBA prerequisite callout and links `how-to-certificate-based-authentication`.
    - `## Configuration-Caused Failures` four-column table present; runbook filenames are code-spans, not live links.
    - `## See Also` links `07-platform-sso-setup.md` live and the four glossary anchors; references `09-enterprise-sso-plugin-migration.md` as a code-span only (NOT a `[..](09-...)` live link).
    - Verify command prints `GUIDE08_OK`.
  </acceptance_criteria>
  <done>Guide 08 exists with all SC1-SC5 content; the verify command prints GUIDE08_OK; no live link to any not-yet-authored file (09-, runbook filenames) exists in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Atomically convert 00-overview line-47 code-span to a live link + add Version-History row</name>
  <files>docs/admin-setup-macos/00-overview.md</files>
  <read_first>
    - docs/admin-setup-macos/00-overview.md (lines 45-49 numbered list; lines 67-70 Version-History table)
    - .planning/phases/77-auth-methods-deep-dive/77-PATTERNS.md (the exact find/replace for line 47; the double-hyphen `--` style of items 1-7; the Version-History row format)
    - .planning/phases/77-auth-methods-deep-dive/77-CONTEXT.md (anchor-stability constraint: body-level edit only, touch NO headings; C13 atomic-landing rule)
  </read_first>
  <action>
Edit ONLY the body of `docs/admin-setup-macos/00-overview.md` — do NOT add, remove, rename, or reorder any heading (PITFALL-6 anchor stability).

Conversion (line 47): replace the code-span numbered-list item
`8. ` followed by a backtick code-span `08-auth-methods-deep-dive.md` and `(added in a later documentation phase)`
with the live-link form exactly:
`8. **[Auth Methods Deep-Dive](08-auth-methods-deep-dive.md)** -- Selection guide and deep-dive reference for all three Platform SSO authentication methods (Secure Enclave key [recommended], Password sync, Smart card) with FileVault interaction, dangerous misconceptions, Touch ID biometric policy, and Passkey/FIDO2 from the Platform Credential.`

Use the double-hyphen `--` separator (matching items 1-7), NOT an em-dash.

Do NOT touch line 49 (`9. ` + `09-enterprise-sso-plugin-migration.md` code-span) — it stays a code-span until Phase 78 (C13 mechanism). The Mermaid diagram already has the guide-08 node and does NOT change.

Version History: append one new row as the most-recent (bottom) entry to the existing `| Date | Change | Author |` table:
`| 2026-06-21 | Phase 77: converted 08-auth-methods-deep-dive.md code-span to live link with description | -- |`
(Author `--` double hyphen.)
  </action>
  <verify>
    <automated>grep -q "\[Auth Methods Deep-Dive\](08-auth-methods-deep-dive.md)" docs/admin-setup-macos/00-overview.md && ! grep -qE '^8\. `08-auth-methods-deep-dive\.md`' docs/admin-setup-macos/00-overview.md && grep -q "Phase 77: converted" docs/admin-setup-macos/00-overview.md && grep -q '09-enterprise-sso-plugin-migration.md' docs/admin-setup-macos/00-overview.md && echo "OVERVIEW_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `00-overview.md` contains the live link `[Auth Methods Deep-Dive](08-auth-methods-deep-dive.md)`.
    - `00-overview.md` does NOT contain the old line-47 code-span numbered item (the backtick-wrapped `08-auth-methods-deep-dive.md` followed by "added in a later documentation phase").
    - Line 49 `9.` item for `09-enterprise-sso-plugin-migration.md` is unchanged (still a code-span, NOT a live link).
    - A new Version-History row dated `2026-06-21` referencing Phase 77 is present, Author `--`.
    - No heading was added, removed, renamed, or reordered (anchor stability).
    - Verify command prints `OVERVIEW_OK`.
  </acceptance_criteria>
  <done>00-overview line-47 is a live link to guide 08, the old code-span is gone, line 49 is untouched, the Version-History row is added, and the verify command prints OVERVIEW_OK. This task and Task 1 commit together (C13 atomic landing).</done>
</task>

<task type="auto">
  <name>Task 3: Create/append v1.9-DEFERRED-CLEANUP.md tracking the omitted NewUserAuthorizationMode key</name>
  <files>docs/v1.9-DEFERRED-CLEANUP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (PSSO-FUT-01 line 70; the deferred-key Out-of-Scope note line 88; PSSO-11 line 32)
    - .planning/phases/77-auth-methods-deep-dive/77-RESEARCH.md (Assumptions Log A1 + NUAL Facts §5: display name "New User Authorization Mode", values Standard/Admin/Groups, underlying MDM key name unconfirmed)
    - .planning/phases/77-auth-methods-deep-dive/77-CONTEXT.md (Deferred Ideas — mandatory tracking of the key)
  </read_first>
  <action>
If `docs/v1.9-DEFERRED-CLEANUP.md` does not exist, create it with a brief header (title + purpose: "v1.9 deferred-cleanup tracking — items deliberately omitted from v1.9 content pending verification, to be resolved in v1.10+"). If it already exists, append to it (do NOT overwrite existing entries).

Add a tracking entry for the NUAL key, recording at minimum:
- The omitted item: the `NewUserAuthorizationMode` MDM payload key literal (the Settings Catalog DISPLAY name is "New User Authorization Mode" with values Standard / Admin / Groups; the underlying MDM plist key name is unconfirmed).
- Why omitted: LOW research confidence — the MDM key name was not confirmed from an authoritative Settings Catalog payload schema (per PSSO-11 / D-03=B; 77-RESEARCH Assumptions Log A1).
- Where it would land when resolved: `docs/admin-setup-macos/08-auth-methods-deep-dive.md` NUAL section.
- Requirement linkage: PSSO-11 (deferred arm) → PSSO-FUT-01 (REQUIREMENTS lines 70, 88).
- Resolution trigger: verify the key name against the live Settings Catalog payload schema / Microsoft Learn, then document in guide 08.
  </action>
  <verify>
    <automated>test -f docs/v1.9-DEFERRED-CLEANUP.md && grep -q "NewUserAuthorizationMode" docs/v1.9-DEFERRED-CLEANUP.md && grep -q "PSSO-FUT-01" docs/v1.9-DEFERRED-CLEANUP.md && echo "DEFERRED_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/v1.9-DEFERRED-CLEANUP.md` exists.
    - Contains the token `NewUserAuthorizationMode` and the reason it was omitted (LOW confidence / unconfirmed MDM key name).
    - References PSSO-11 and PSSO-FUT-01.
    - If the file pre-existed, prior content is preserved (append, not overwrite).
    - Verify command prints `DEFERRED_OK`.
  </acceptance_criteria>
  <done>v1.9-DEFERRED-CLEANUP.md records the omitted NewUserAuthorizationMode key with rationale and PSSO-11/PSSO-FUT-01 linkage; verify command prints DEFERRED_OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase ships only Markdown documentation: no executable code, no runtime inputs, no auth surface, no network/data flow. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Information disclosure (documentation accuracy) | Security claims in guide 08 (e.g. "passwordless", FileVault relationship, phishing-resistance) | mitigate | All security-bearing claims are sourced from VERIFIED official Microsoft/Apple docs in 77-RESEARCH.md; the D-03 misconception box + C3 FileVault canonical statement + per-method point-of-use callouts prevent the "fully passwordless" mis-claim; 90-day `review_by: 2026-09-21` cadence forces re-verification. |
| T-77-02 | Tampering (silent misconfig from omission) | Smart card section without the Entra CBA prerequisite | mitigate | Hard prerequisite callout opens the Smart card section before any config detail (DF-11); links the authoritative CBA how-to. |
| T-77-03 | Denial of service (admin self-lockout) | Touch ID biometric policy guidance | mitigate | Verbatim no-password-fallback lockout warning + admin-driven re-registration requirement reproduced in the Touch ID subsection (PSSO-09). |

No package installs in this phase (documentation only) → no `T-77-SC` supply-chain checkpoint required. No high-severity runtime threats. Security enforcement (ASVS L1) is satisfied: this is a content phase that describes — and does not implement — authentication mechanisms.
</threat_model>

<verification>
- `node scripts/validation/v1.8-milestone-audit.mjs` PASSES 15/15 after the atomic commit (C13 broken-link gate stays green — the live link to guide 08 has its target present in the same commit; line-49 guide-09 reference and runbook filenames remain code-spans).
- `git diff HEAD --name-only` after the commit shows guide 08, 00-overview.md, and v1.9-DEFERRED-CLEANUP.md changed together (atomic C13 landing).
- All three task verify commands print their OK sentinels (`GUIDE08_OK`, `OVERVIEW_OK`, `DEFERRED_OK`).
</verification>

<success_criteria>
- SC1: Guide 08 opens with a four-dimension (passwordless / phishing-resistant / hardware / macOS-version) selection table marking Secure Enclave as Microsoft's recommendation.
- SC2: Secure Enclave section documents hardware scope, non-exportable key, PRT, FileVault non-relationship (canonical), and password-reset-destroys-the-key.
- SC3: Password sync section documents the ~4-hour window, complexity-mismatch, macOS 15+ FileVaultPolicy, per-user-MFA blocker, AD-bound limitation.
- SC4: Smart card section opens with the Entra CBA prerequisite, covers sc_auth pairing, macOS 14+ gate, Setup-Assistant exclusion; Touch ID subsection carries the no-password-fallback lockout warning.
- SC5: Passkey (AAGUID + conditional note) and NUAL documented; `NewUserAuthorizationMode` key omitted and tracked in `v1.9-DEFERRED-CLEANUP.md`.
- C13: 00-overview line-47 converted to a live link atomically with guide 08; v1.8 audit passes 15/15.
</success_criteria>

<output>
Create `.planning/phases/77-auth-methods-deep-dive/77-01-SUMMARY.md` when done.
</output>
