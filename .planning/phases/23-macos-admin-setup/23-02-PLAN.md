---
phase: 23-macos-admin-setup
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-macos/03-configuration-profiles.md
  - docs/admin-setup-macos/05-compliance-policy.md
autonomous: true
requirements: [MADM-03, MADM-05]

must_haves:
  truths:
    - "Admin can configure macOS configuration profiles for Wi-Fi, VPN, email, restrictions, FileVault, and firewall via Settings Catalog"
    - "Admin can configure macOS compliance policies for SIP, FileVault, firewall, Gatekeeper, and password"
    - "Admin sees explicit callout that no Intune security baselines exist for macOS"
    - "Admin understands the distinction between compliance policies (detect) and configuration profiles (enforce)"
    - "Every configurable setting has a what-breaks callout"
  artifacts:
    - path: "docs/admin-setup-macos/03-configuration-profiles.md"
      provides: "Configuration profile guide covering 6 profile types with Settings Catalog paths and what-breaks callouts"
      contains: "Settings Catalog"
      min_lines: 250
    - path: "docs/admin-setup-macos/05-compliance-policy.md"
      provides: "Compliance policy guide with explicit no-security-baselines callout"
      contains: "no Intune security baselines exist for macOS"
      min_lines: 120
  key_links:
    - from: "docs/admin-setup-macos/03-configuration-profiles.md"
      to: "docs/admin-setup-macos/05-compliance-policy.md"
      via: "compliance vs configuration distinction cross-reference"
      pattern: "05-compliance-policy\\.md"
    - from: "docs/admin-setup-macos/05-compliance-policy.md"
      to: "docs/admin-setup-macos/03-configuration-profiles.md"
      via: "enforcement profile cross-reference"
      pattern: "03-configuration-profiles\\.md"
    - from: "docs/admin-setup-macos/05-compliance-policy.md"
      to: "docs/reference/ca-enrollment-timing.md"
      via: "WSEC-01 cross-reference per D-18"
      pattern: "ca-enrollment-timing"
---

<objective>
Create the macOS configuration profiles guide (MADM-03) and compliance policy guide (MADM-05) for the macOS admin setup suite.

Purpose: These guides cover the core device management configuration: configuration profiles enforce security and management settings via MDM, while compliance policies detect and report non-compliance. The critical insight these guides must communicate is that compliance policies detect but do not enforce -- every compliance check should be paired with a configuration profile. The guides also document the important fact that no Intune security baselines exist for macOS.

Output: Two markdown files in `docs/admin-setup-macos/` with per-setting what-breaks callouts and Settings Catalog navigation paths.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-macos-admin-setup/23-CONTEXT.md
@.planning/phases/23-macos-admin-setup/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create macOS configuration profiles guide (MADM-03)</name>
  <files>docs/admin-setup-macos/03-configuration-profiles.md</files>
  <read_first>
    - docs/_templates/admin-template-macos.md (template structure)
    - docs/macos-lifecycle/00-ade-lifecycle.md (Stage 7 for profile delivery context)
    - docs/_glossary-macos.md (glossary anchors)
    - .planning/phases/23-macos-admin-setup/23-RESEARCH.md (lines 279-304: MADM-03 verified details)
  </read_first>
  <action>
Create `docs/admin-setup-macos/03-configuration-profiles.md` (~250-350 lines) per D-03, D-05, D-13, D-14, D-15, D-16, D-17.

**Frontmatter:**
```yaml
---
last_verified: 2026-04-14
review_by: 2026-07-13
applies_to: ADE
audience: admin
platform: macOS
---
```

**Platform gate blockquote** (same as template).

**H1:** `# macOS Configuration Profiles`

**Important note at top (blockquote):**
```
> **Settings Catalog required:** All new macOS configuration profiles should be created via the Settings Catalog in Intune (Devices > Manage devices > Configuration > Create > New policy > Settings catalog). The older Endpoint protection template is deprecated for new policies -- do not create new profiles via Templates > Endpoint protection.
```

**## Prerequisites:**
- Intune Administrator role
- Devices enrolled via ADE (see [ABM Configuration](01-abm-configuration.md) and [Enrollment Profile](02-enrollment-profile.md))
- Understanding of the difference between configuration profiles (enforce settings) and compliance policies (detect non-compliance) -- see [Compliance Policies](05-compliance-policy.md)

**## Profile Delivery Channel note:**
Configuration profiles are delivered via the **MDM channel (APNs)**, NOT the Intune Management Extension agent. If a profile fails to deliver, troubleshoot APNs connectivity, not the IME agent. Link to [Network Endpoints](../reference/endpoints.md#macos-ade-endpoints) for APNs endpoint list.

**## H2 sections for each of 6 profile types.** Claude's discretion on whether to use H3 per profile type or group related profiles. Use H2 per profile type for clarity.

### Wi-Fi
#### In Intune admin center
Navigation: **Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **Settings catalog** > **Networking** > **Wi-Fi**

Key settings to document with what-breaks callouts:
- SSID: Must match exactly (case-sensitive). What breaks: device cannot find network; symptom appears in device Wi-Fi settings.
- Security type: Must match the network's actual security type. What breaks: connection fails silently; symptom on device.
- Certificate (for enterprise WPA2/WPA3): Must reference a valid SCEP/PKCS certificate profile. What breaks: 802.1X authentication fails; symptom on device and in Intune compliance.

### VPN
Navigation: **Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **Settings catalog** > **Networking** > **VPN**

Key settings with what-breaks: VPN type (IKEv2, SSL, etc.), server address, authentication method. What breaks if wrong VPN type: connection fails; symptom on device.

### Email
Navigation: **Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **Settings catalog** > **Accounts** > **Email**

Key settings: Email server, authentication method, S/MIME. What breaks: email sync fails; symptom in Mail app.

### Restrictions
Navigation: **Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **Settings catalog** > **Restrictions**

Key settings: App Store access, AirDrop, Handoff, Screen capture, Password modification, Content caching. What breaks per setting documented individually.

### FileVault (Disk Encryption)
Navigation: **Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **Settings catalog** > **Full Disk Encryption**

Key settings:
- Enable FileVault: Enforces encryption on next logout.
- Recovery key escrow to Intune: Allows admin recovery key retrieval.
- Recovery key rotation: Configurable rotation after each use.

> **What breaks if misconfigured:** If FileVault enforcement is configured without recovery key escrow, and the user forgets their password, the disk is unrecoverable. Symptom appears in: Intune admin center (no recovery key available when admin searches) and on device (FileVault recovery screen shows no institutional key).
> See: [Troubleshooting Runbook](TBD - Phase 24)

**Critical note:** FileVault has TWO surfaces -- a configuration profile (enforce via Settings Catalog) AND a compliance policy (verify enabled in [Compliance Policies](05-compliance-policy.md)). Always deploy both together.

### Firewall
Navigation: **Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **Settings catalog** > **Networking** > **Firewall** + **Security** > **Security preferences**

Key settings:
- Application firewall: Enable/Disable
- Block all incoming connections: Yes/No
- Stealth mode: Enable/Disable

> **What breaks if misconfigured:** Enabling "Block all incoming connections" without exceptions for management traffic (APNs, IME) can break MDM communication and app deployment. Symptom appears in: Intune admin center (device stops checking in) and on device (apps fail to install).
> See: [Troubleshooting Runbook](TBD - Phase 24)

**Critical note:** Like FileVault, firewall has configuration profile (enforce) AND compliance policy (detect). Deploy both.

**Additional profile types (brief mention with links):**

### Gatekeeper
Navigation: **Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **Settings catalog** > **System Policy** > **System Policy Control** (Allow Identified Developer: True, Enable Assessment: True) + **System Policy Managed** (Disable Override: True)

What breaks: If Gatekeeper is not enforced via configuration profile, users can change the setting to allow apps from "Anywhere." Compliance policy detects the change but does not prevent it.

### Privacy Preferences (PPPC)
Brief mention: Per-app privacy permissions via **Privacy** > **Privacy Preferences Policy Control**. Required for MDM agents and security tools that need Full Disk Access, Screen Recording, etc.

### Extensible SSO
Brief mention: Enterprise SSO plug-in or Platform SSO (macOS 14+) via **Authentication** > **Extensible Single Sign On (SSO)**. Link to official documentation for full configuration.

**## Verification checklist:**
- [ ] Each configuration profile shows "Succeeded" status in Intune admin center > Devices > Configuration > [profile] > Device status
- [ ] Profile delivery confirmed on target devices (System Settings > Profiles on macOS)
- [ ] FileVault escrow key visible in Intune admin center for encrypted devices
- [ ] Firewall and Gatekeeper settings enforced (verify via `defaults read` commands in [macOS Commands Reference](../reference/macos-commands.md))

**## Configuration-Caused Failures table:**
| Misconfiguration | Portal | Symptom | Runbook |
|------------------|--------|---------|---------|
| New profile via Endpoint protection template (deprecated) | Intune | Profile may not contain latest settings; stale template | [TBD - Phase 24] |
| FileVault without recovery key escrow | Intune | Admin cannot retrieve recovery key for locked device | [TBD - Phase 24] |
| Firewall blocks incoming without MDM exceptions | Intune | Device stops checking in; app deployment fails | [TBD - Phase 24] |
| SSID case mismatch in Wi-Fi profile | Intune | Device cannot connect to network | [TBD - Phase 24] |
| Gatekeeper not enforced via config profile | Intune | User overrides Gatekeeper; compliance policy detects but cannot prevent | [TBD - Phase 24] |
| Certificate profile missing for enterprise Wi-Fi | Intune | 802.1X authentication fails on device | [TBD - Phase 24] |

No Renewal / Maintenance section (configuration profiles have no renewable component).

**## See Also:**
- [Compliance Policies](05-compliance-policy.md)
- [App Deployment](04-app-deployment.md)
- [ABM Configuration](01-abm-configuration.md)
- [macOS ADE Lifecycle Overview](../macos-lifecycle/00-ade-lifecycle.md)
- [macOS Terminal Commands Reference](../reference/macos-commands.md)
- [Network Endpoints Reference](../reference/endpoints.md#macos-ade-endpoints)

**Reactive split (D-05):** If the file exceeds 350 lines during writing, split into `03a-config-profiles-network.md` (Wi-Fi, VPN, email) and `03b-config-profiles-security.md` (restrictions, FileVault, firewall, Gatekeeper, PPPC, SSO). Update the `files` field in this task and add cross-links between the split files.
  </action>
  <verify>
    <automated>bash -c "test -f 'D:/claude/Autopilot/docs/admin-setup-macos/03-configuration-profiles.md' && wc -l < 'D:/claude/Autopilot/docs/admin-setup-macos/03-configuration-profiles.md' | xargs test 200 -le && echo PASS || echo FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/admin-setup-macos/03-configuration-profiles.md` exists with >= 200 lines (may be up to 350; if split into 03a/03b per D-05, both files exist)
    - Frontmatter contains `platform: macOS` and `last_verified: 2026-04-14`
    - Contains `Settings Catalog` text (not "Endpoint protection template" as recommended path)
    - Contains H2 or H3 sections for at minimum: Wi-Fi, VPN, Email, Restrictions, FileVault, Firewall
    - Contains `Devices > Manage devices > Configuration > Create > New policy > Settings catalog` navigation path for at least FileVault and Firewall
    - Contains "Endpoint protection template is deprecated" or equivalent deprecation note
    - Contains `## Configuration-Caused Failures` table with `Portal` column
    - Contains at least 5 `What breaks if misconfigured` callouts across all profile types
    - Contains cross-reference to `05-compliance-policy.md` explaining detect vs enforce distinction
    - Contains `[TBD - Phase 24]` for runbook links
    - Contains note about MDM channel (APNs) for profile delivery (not IME agent)
    - File does NOT exceed 350 lines; if it does, it has been split per D-05
  </acceptance_criteria>
  <done>Configuration profiles guide covers all 6+ profile types with Settings Catalog paths, what-breaks callouts, deprecation notice for Endpoint protection template, and compliance vs configuration distinction</done>
</task>

<task type="auto">
  <name>Task 2: Create macOS compliance policy guide (MADM-05)</name>
  <files>docs/admin-setup-macos/05-compliance-policy.md</files>
  <read_first>
    - docs/_templates/admin-template-macos.md (template structure)
    - docs/admin-setup-macos/03-configuration-profiles.md (just created in Task 1 -- cross-reference for detect vs enforce)
    - docs/_glossary-macos.md (glossary anchors)
    - .planning/phases/23-macos-admin-setup/23-RESEARCH.md (lines 356-414: MADM-05 verified details)
  </read_first>
  <action>
Create `docs/admin-setup-macos/05-compliance-policy.md` (~120-160 lines) per D-03, D-13, D-14, D-15, D-16, D-17, D-18.

**Frontmatter:**
```yaml
---
last_verified: 2026-04-14
review_by: 2026-07-13
applies_to: ADE
audience: admin
platform: macOS
---
```

**Platform gate blockquote** (same as template).

**H1:** `# macOS Compliance Policies`

**Critical callout at top (blockquote -- the most important fact in this guide):**
```
> **No Intune security baselines for macOS.** Intune security baselines are available only for Windows (Windows 10/11, Microsoft Defender for Endpoint, Microsoft Edge, Microsoft 365 Apps, HoloLens 2, Windows 365). macOS has zero security baselines. Admins must configure macOS security settings manually via a combination of compliance policies (detect non-compliance) and configuration profiles (enforce settings). See [Configuration Profiles](03-configuration-profiles.md) for enforcement.
```

**## Compliance vs. Configuration: Critical Distinction**

Include this table verbatim from research:
| Action | Compliance Policy | Configuration Profile |
|--------|------------------|----------------------|
| Verify FileVault enabled | Yes | Yes (also enforces it) |
| Verify firewall enabled | Yes | Yes (also configures it) |
| Verify Gatekeeper setting | Yes | Yes (also locks it) |
| Enforce SIP | No (compliance check only) | SIP cannot be enforced via MDM -- read-only |
| Enforce encryption | No | Yes (Settings Catalog > Full Disk Encryption) |

> **What breaks if misconfigured:** If compliance policies are deployed WITHOUT corresponding configuration profiles, devices will be marked non-compliant but nothing enforces the settings. Users can change settings freely, and Conditional Access may block access. Symptom appears in: Intune admin center (devices non-compliant) and on device (Conditional Access blocks user if CA policies are active).
> See: [Troubleshooting Runbook](TBD - Phase 24)

**## Prerequisites:**
- Intune Administrator role
- Devices enrolled via ADE
- Configuration profiles deployed for settings you want to enforce (see [Configuration Profiles](03-configuration-profiles.md))
- Understanding that compliance policies DETECT; configuration profiles ENFORCE

**## Steps:**

### Step 1: Create Compliance Policy
#### In Intune admin center
1. Navigate to **Devices** > **Manage devices** > **Compliance** > **Create policy**.
2. Platform: **macOS**.
3. Enter a policy name and description.

### Step 2: Configure Compliance Settings

Document ALL compliance settings organized by category:

**Device Health:**
- Require System Integrity Protection (SIP): Not configured / Require

> **What breaks if misconfigured:** SIP is a compliance-check-only setting. MDM CANNOT enable or disable SIP -- it is controlled only by booting to Recovery Mode. The compliance policy only detects whether SIP is enabled or disabled. If required and SIP is disabled, device is non-compliant with no automated remediation. User must boot to Recovery Mode and run `csrutil enable`. Symptom appears in: Intune admin center (non-compliant status with "SIP disabled" reason).

**Device Properties:**
- Minimum OS version
- Maximum OS version
- Minimum OS build version (supports Apple Rapid Security Response build strings)
- Maximum OS build version

> **What breaks if misconfigured:** OS version requirements that are ahead of the latest available macOS update will mark ALL devices non-compliant with no remediation path until Apple releases the update. Symptom appears in: Intune admin center (entire fleet non-compliant).

**System Security - Password:**
- Require a password: Not configured / Require
- Simple passwords: Not configured / Block
- Minimum password length
- Password type: Numeric / Alphanumeric
- Number of non-alphanumeric characters
- Maximum minutes of inactivity before password required
- Password expiration (days)
- Number of previous passwords to prevent reuse

> **What breaks if misconfigured:** Password compliance note -- when password requirements change, the new requirement does NOT take effect until the next time the user changes their password. The device remains compliant with the old password until then. This means there is a window where the device technically meets the old policy but not the new one. Symptom appears in: Intune admin center (device may show compliant despite not meeting new password requirements until user changes password).

**System Security - Encryption:**
- Require encryption of data storage on device: Not configured / Require (checks FileVault status)

> Pair with FileVault configuration profile to enforce. See [Configuration Profiles > FileVault](03-configuration-profiles.md).

**System Security - Device Security (Firewall):**
- Firewall: Not configured / Enable
- Incoming connections: Not configured / Block
- Stealth Mode: Not configured / Enable

> Pair with firewall configuration profile to enforce. See [Configuration Profiles > Firewall](03-configuration-profiles.md).

**Gatekeeper:**
- Allow apps downloaded from: Not configured / Mac App Store / Mac App Store and identified developers / Anywhere

> **What breaks if misconfigured:** Compliance policy checks current Gatekeeper setting but does NOT enforce it. A configuration profile (Settings Catalog > System Policy) is required to prevent users from changing the setting. If set to "Mac App Store" and user overrides to "Anywhere" without a config profile enforcing it, device becomes non-compliant immediately. Symptom appears in: Intune admin center (non-compliant for Gatekeeper) and device (Conditional Access blocks access if CA policy active).

### Step 3: Configure Actions for Noncompliance
Document the standard actions:
- Mark device noncompliant (immediately or after grace period in days)
- Send email to end user
- Remotely lock the noncompliant device
- Retire the noncompliant device

### Step 4: Assign Policy
Assign to device groups or user groups.

**## Conditional Access Cross-Reference (per D-18):**
> For macOS-specific Conditional Access enrollment timing considerations (the chicken-and-egg problem where devices are non-compliant during enrollment), see [Conditional Access Enrollment Timing](../reference/ca-enrollment-timing.md). This is a cross-platform issue documented in detail with Windows and macOS resolution patterns.

**## Verification checklist:**
- [ ] Compliance policy shows assigned devices in Intune admin center
- [ ] Device compliance status visible at Devices > [device] > Compliance
- [ ] Non-compliant devices show specific failing settings (not just "non-compliant")
- [ ] Grace period configured matches organizational policy
- [ ] Each compliance setting has a corresponding configuration profile enforcing it

**## Configuration-Caused Failures table:**
| Misconfiguration | Portal | Symptom | Runbook |
|------------------|--------|---------|---------|
| Compliance policy without enforcement config profile | Intune | Devices non-compliant but settings not enforced; users can change freely | [TBD - Phase 24] |
| SIP required but disabled on device | Intune | Non-compliant with no MDM remediation; user must boot to Recovery Mode | [TBD - Phase 24] |
| OS version requirement ahead of available update | Intune | Entire fleet non-compliant until Apple releases update | [TBD - Phase 24] |
| Password change timing gap | Intune | Device appears compliant with old password until user changes it | [TBD - Phase 24] |
| Gatekeeper compliance without config profile | Intune | Users can override Gatekeeper; compliance detects but cannot prevent | [TBD - Phase 24] |

No Renewal / Maintenance section.

**## See Also:**
- [Configuration Profiles](03-configuration-profiles.md) -- enforcement counterpart to compliance detection
- [ABM Configuration](01-abm-configuration.md)
- [Conditional Access Enrollment Timing](../reference/ca-enrollment-timing.md) -- WSEC-01 cross-reference per D-18
- [Capability Matrix](../reference/macos-capability-matrix.md)
- [macOS ADE Lifecycle Overview](../macos-lifecycle/00-ade-lifecycle.md)
  </action>
  <verify>
    <automated>bash -c "test -f 'D:/claude/Autopilot/docs/admin-setup-macos/05-compliance-policy.md' && grep -c 'no Intune security baselines' 'D:/claude/Autopilot/docs/admin-setup-macos/05-compliance-policy.md' | xargs test 0 -lt && echo PASS || echo FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/admin-setup-macos/05-compliance-policy.md` exists with >= 120 lines
    - Frontmatter contains `platform: macOS` and `last_verified: 2026-04-14`
    - Contains the exact phrase "no Intune security baselines" (or close variant "No Intune security baselines") at least once
    - Contains "Compliance vs. Configuration" distinction table with FileVault, firewall, Gatekeeper, SIP, encryption rows
    - Contains all compliance settings: SIP, FileVault (encryption), firewall (3 settings), Gatekeeper, password (8 settings), device properties (4 settings)
    - Contains SIP note stating MDM cannot enable/disable SIP (read-only compliance check)
    - Contains password compliance note about change timing gap
    - Contains `## Configuration-Caused Failures` table with `Portal` column
    - Contains at least 4 `What breaks if misconfigured` callouts
    - Contains cross-reference to `ca-enrollment-timing.md` (per D-18 / WSEC-01)
    - Contains cross-reference to `03-configuration-profiles.md` for enforcement pairing
    - Contains `[TBD - Phase 24]` for runbook links
    - Does NOT contain `## Renewal` section
  </acceptance_criteria>
  <done>Compliance policy guide documents all macOS compliance settings with explicit no-security-baselines callout, compliance vs configuration distinction, per-setting what-breaks callouts, and WSEC-01 cross-reference</done>
</task>

</tasks>

<verification>
Phase-level checks for Plan 02:
1. Both files exist in `docs/admin-setup-macos/`
2. Both follow `admin-template-macos.md` structure
3. Both use `platform: macOS` frontmatter
4. Configuration profiles guide covers >= 6 profile types with Settings Catalog paths
5. Compliance guide contains "no Intune security baselines" callout
6. Bidirectional cross-references between 03 and 05 (detect vs enforce distinction)
7. D-18 cross-reference to ca-enrollment-timing.md present in compliance guide
</verification>

<success_criteria>
- Admin can configure all major macOS configuration profile types using Settings Catalog navigation paths
- Admin can configure all macOS compliance policy settings
- Admin understands compliance policies detect but do not enforce (must pair with config profiles)
- Admin sees explicit "no Intune security baselines for macOS" callout
- All configurable settings have what-breaks callouts
</success_criteria>

<output>
After completion, create `.planning/phases/23-macos-admin-setup/23-02-SUMMARY.md`
</output>
