---
phase: 46-cope-full-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-android/08-cope-full-admin.md
autonomous: true
requirements:
  - AECOPE-01
must_haves:
  truths:
    - "New file `docs/admin-setup-android/08-cope-full-admin.md` exists"
    - "File contains exactly 11 H2s in the COBO sibling order (Key Concepts / Prerequisites / COPE Migration Note / Enrollment profile creation / Enrollment token management / Provisioning method choice / Android 15 FRP and EFRP / What Breaks Summary / Renewal / Maintenance / See Also / Changelog)"
    - "Frontmatter contains `audience: admin`, `platform: Android`, `applies_to: COPE`, `last_verified: <execute-time>`, `review_by: <last_verified + 60d>` per D-26 + D-27"
    - "Intune admin center UI label `Corporate-owned devices with work profile` appears verbatim at least once in `## Enrollment profile creation` H2"
    - "All 7 naming templates appear verbatim: `{{SERIAL}}`, `{{SERIALLAST4DIGITS}}`, `{{DEVICETYPE}}`, `{{ENROLLMENTDATETIME}}`, `{{UPNPREFIX}}`, `{{USERNAME}}`, `{{RAND:x}}`"
    - "Two GA token types appear verbatim: `Corporate-owned with work profile (default)` and `Corporate-owned with work profile, via staging`; staging token expiration up to 65 years"
    - "Android 11+ removal callout enumerates THREE methods: NFC, `afw#setup`, AND token-entry (per RESEARCH NEW FINDING #1)"
    - "Android 15 FRP H2 contains COPE-specific FRP behavior table (NOT copy-pasted from COBO) plus the verbatim Settings-app-reset Google-account re-enrollment requirement (per RESEARCH NEW FINDING #2)"
    - "Android 15 Private Space `> ⚠️` one-line callout present in `## Key Concepts` H2 with forward-links to `_glossary-android.md#private-space` and `03-android-version-matrix.md#android-15-private-space-breakpoint` (per D-08)"
    - "COPE-vs-COBO decision matrix with 5 rows × 3 cols at H2 anchor `#cope-vs-cobo-decision` (placed as sub-H3 inside `## Provisioning method choice` to preserve 11-H2 lock per D-05 / RESEARCH Open Question #1)"
    - "Samsung-admins callout in `### Zero-Touch` H3 with verbatim D-19 text including WPCO↔COPE equivalence sentence and ≥4 forward-links"
    - "Zero instances of banned phrases (`deprecated`, `end of life`, `EOL`, `removed`, `no longer supported`, `obsolete`, `sunset`, `retired`) applied to COPE per D-31"
    - "Audit harness `node scripts/validation/v1.4.1-milestone-audit.mjs` passes 8/8 after file lands"
  artifacts:
    - path: "docs/admin-setup-android/08-cope-full-admin.md"
      provides: "Full Intune admin guide for Corporate-owned devices with work profile (COPE/WPCO)"
      min_lines: 200
      contains:
        - "Corporate-owned devices with work profile"
        - "{{SERIAL}}"
        - "{{SERIALLAST4DIGITS}}"
        - "{{DEVICETYPE}}"
        - "{{ENROLLMENTDATETIME}}"
        - "{{UPNPREFIX}}"
        - "{{USERNAME}}"
        - "{{RAND:x}}"
        - "Private Space"
        - "cope-vs-cobo-decision"
  key_links:
    - from: "docs/admin-setup-android/08-cope-full-admin.md"
      to: "docs/_glossary-android.md#private-space"
      via: "Key Concepts ⚠️ Private Space callout"
      pattern: "_glossary-android.md#private-space"
    - from: "docs/admin-setup-android/08-cope-full-admin.md"
      to: "docs/android-lifecycle/03-android-version-matrix.md#android-15-private-space-breakpoint"
      via: "Key Concepts ⚠️ Private Space callout"
      pattern: "android-15-private-space-breakpoint"
    - from: "docs/admin-setup-android/08-cope-full-admin.md"
      to: "docs/admin-setup-android/07-knox-mobile-enrollment.md"
      via: "Zero-Touch H3 Samsung-admins callout"
      pattern: "07-knox-mobile-enrollment.md"
    - from: "docs/admin-setup-android/08-cope-full-admin.md"
      to: "docs/admin-setup-android/02-zero-touch-portal.md#kme-zt-mutual-exclusion"
      via: "Zero-Touch H3 Samsung-admins callout"
      pattern: "kme-zt-mutual-exclusion"
    - from: "docs/admin-setup-android/08-cope-full-admin.md"
      to: "docs/admin-setup-android/03-fully-managed-cobo.md#cope-migration"
      via: "## COPE Migration Note H2 inverse-direction back-link"
      pattern: "03-fully-managed-cobo.md#cope-migration"
---

<objective>
Author the full Intune admin guide for "Corporate-owned devices with work profile" (COPE / WPCO) at `docs/admin-setup-android/08-cope-full-admin.md`. Mirror COBO sibling 1:1 (11 H2s in fixed order) per CONTEXT D-01, embed the verbatim Android 15 Private Space callout per D-08, embed the COPE-specific FRP behavior table per RESEARCH NEW FINDING #2, surface ALL THREE Android 11+ removed methods (NFC + afw#setup + token-entry) per RESEARCH NEW FINDING #1, embed the Samsung-admins callout under `### Zero-Touch` H3 per D-19, and embed the 5×3 COPE-vs-COBO decision matrix as sub-H3 inside `## Provisioning method choice` per D-05 / RESEARCH Open Question #1.

Purpose: Closes AECOPE-01 (the central Phase 46 deliverable). Establishes the new admin doc that Wave 2 retrofits route to (capability matrix column, glossary back-link, BYOD line 167 retrofit, version-matrix breakpoint H3, COBO migration-note retrofit). Without this file existing, every Wave 2 retrofit becomes a dead link.

Output: One new ~250-line markdown file with exactly 11 H2s in COBO sibling order, frontmatter per D-27, verbatim content from MS Learn `setup-corporate-work-profile.md` + `ref-corporate-methods.md`, zero banned phrases applied to COPE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-cope-full-admin/46-CONTEXT.md
@.planning/phases/46-cope-full-admin/46-RESEARCH.md
@.planning/phases/46-cope-full-admin/46-VALIDATION.md
@docs/admin-setup-android/03-fully-managed-cobo.md
@docs/_templates/admin-template-android.md
@docs/_glossary-android.md

<interfaces>
<!-- Sibling COBO H2 anchors and content patterns. Executor uses these directly — no exploration needed. -->

COBO sibling H2 anchors (`docs/admin-setup-android/03-fully-managed-cobo.md`):
- Line 24: `<a id="key-concepts"></a>` → `## Key Concepts` (line 25)
- Line 37: `<a id="prerequisites"></a>` → `## Prerequisites` (line 38)
- Line 57: `<a id="cope-migration"></a>` → `## COPE Migration Note` (line 58) — REPURPOSED inverse-direction in COPE doc per D-03
- Line 68: `<a id="enrollment-profile"></a>` → `## Enrollment profile creation` (line 69)
- Line 99: `<a id="enrollment-token"></a>` → `## Enrollment token management` (line 100)
- Line 127: `<a id="provisioning-method-choice"></a>` → `## Provisioning method choice` (line 128)
- Line 168: `<a id="android-15-frp"></a>` → `## Android 15 FRP and EFRP` (line 169)
- Line 197: `<a id="what-breaks"></a>` → `## What Breaks Summary` (line 198)
- Line 218: `<a id="renewal-maintenance"></a>` → `## Renewal / Maintenance` (line 219)
- Line 231: `## See Also` (no anchor)
- Line 245: `## Changelog` (no anchor)

Glossary canonical anchors (`docs/_glossary-android.md`):
- `#cope` (line 47) — existing COPE entry
- `#wpco` (line 75) — existing WPCO entry, "WPCO, formerly COPE" equivalence
- `#fully-managed` (line 59)
- `#zero-touch-enrollment` (line 33)
- `#kme` (line 96)
- `#private-space` — NEW; Wave 2 plan creates it; this plan forward-links to it

Capability matrix existing column order (`docs/reference/android-capability-matrix.md`):
- Current: `COBO | BYOD | Dedicated | ZTE | AOSP` (5 cols)
- After Wave 2 retrofit: `COBO | COPE | BYOD | Dedicated | ZTE | AOSP` (6 cols, COPE inserted at index 1 per D-20)

MS Learn primary sources (researcher VERIFIED 2026-04-25):
- `setup-corporate-work-profile.md` — UI label, navigation path, token types, naming templates, Limitations
- `ref-corporate-methods.md` — QR/ZT/KME/NFC/token methods, FRP behavior table, Android 15 COPE re-enrollment requirement, ZT iframe DPC extras JSON
- `factory-reset-protection-emails-not-enforced` — FRP/EFRP nuance
</interfaces>

<verbatim_content>
<!-- Verbatim content blocks the executor MUST embed. Source: 46-RESEARCH.md Code Examples 1-9 + D-08 / D-19 / D-21 LOCKED text. -->

### Block A: Key Concepts ⚠️ Private Space callout (D-08 verbatim, ~50 words single line)

```markdown
> ⚠️ **Android 15 — Private Space (unmanaged):** Android 15+ devices include a user-controlled hidden profile partition that **Intune** cannot manage on COPE, COBO, BYOD, or any other Android Enterprise mode. See [_glossary-android.md#private-space](../_glossary-android.md#private-space) and [version matrix breakpoint](../android-lifecycle/03-android-version-matrix.md#android-15-private-space-breakpoint).
```

NOTE on D-09 wording adjustment: Per RESEARCH Open Question #2 / Pitfall 8 / Option A: drop the absolute "no admin policy lever in COPE or any other mode" phrasing — replace with `**Intune** cannot manage … on COPE, COBO, BYOD, or any other Android Enterprise mode.` This honors Bayton's documented AMAPI-native EMM nuance (AMAPI EMMs DO get allowlist/blocklist policies in Private Space; Intune-via-customDPC does not).

### Block B: Intune admin center COPE navigation (HIGH-confidence verbatim from MS Learn `setup-corporate-work-profile.md`)

```markdown
1. Sign in to the [Microsoft Intune admin center](https://endpoint.microsoft.com).
2. Go to **Devices** → **Enrollment**.
3. Select the **Android** tab.
4. Under **Android Enterprise** → **Enrollment Profiles**, choose **Corporate-owned devices with work profile**.
5. Select **Create profile**.
```

### Block C: Token types verbatim (MS Learn)

```markdown
**Token type**: Choose the type of token you want to use to enroll devices.
- **Corporate-owned with work profile (default)**
- **Corporate-owned with work profile, via staging**

> **Tip:** Enrollment time grouping isn't supported with the staging token. If you're configuring a profile for use with enrollment time grouping, use the corporate-owned with work profile (default) token.

**Token expiration date**: Only available with the staging token. Enter the date you want the token to expire, up to 65 years in the future. Acceptable date format: `MM/DD/YYYY` or `YYYY-MM-DD`. The token expires on the selected date at 12:59:59 PM in the time zone it was created.
```

### Block D: Naming templates (all 7 verbatim, MS Learn)

```markdown
You can use the following strings to create your naming template. Intune replaces the strings with device-specific values.

- `{{SERIAL}}` for the device's serial number.
- `{{SERIALLAST4DIGITS}}` for the last 4 digits of the device's serial number.
- `{{DEVICETYPE}}` for the device type. Example: *AndroidForWork*
- `{{ENROLLMENTDATETIME}}` for the date and time of enrollment.
- `{{UPNPREFIX}}` for the user's first name. Example: *Eric*, when device is user affiliated.
- `{{USERNAME}}` for the user's username when the device is user affiliated. Example: *EricSolomon*
- `{{RAND:x}}` for a random string of numbers, where *x* is between 1 and 9 and indicates the number of digits to add. Intune adds the random digits to the end of the name.

Edits you make to the naming template only apply to new enrollments.
```

### Block E: Android 11+ removed methods (verbatim, includes NEW token-entry finding)

```markdown
For corporate-owned work profile (COPE) devices, the `afw#setup` enrollment method and the Near Field Communication (NFC) enrollment method are only supported on devices running Android 8-10. They are not available on Android 11.
```

PLUS (new finding from MS Learn `ref-corporate-methods.md` "Enroll by using a token" Note section):

```markdown
[Token-entry method] Note: Not supported on Android Enterprise corporate-owned devices with a work profile (COPE) running Android version 11.0.
```

### Block F: COPE FRP behavior table (verbatim from MS Learn `ref-corporate-methods.md` — DIFFERENT FROM COBO)

```markdown
| Enrollment method | Settings > Factory data reset | Settings > Recovery/bootloader | Intune wipe |
|---|---|---|---|
| **Corporate-owned devices with work profile** (COPE) | ✅ factory reset protection | ✅ factory reset protection | ❌ no factory reset protection |
| **Fully managed** (COBO) | ❌ no factory reset protection | ✅ factory reset protection | ❌ no factory reset protection |
| **Dedicated** (COSU) | ❌ no factory reset protection | ✅ factory reset protection | ❌ no factory reset protection |
```

### Block G: Android 15 COPE-specific re-enrollment requirement (verbatim, NOT in COBO)

```markdown
For corporate owned devices with a work profile running Android 15, you will need to re-enter the Google account associated with the configuration after any reset done via the Settings app. It's important to plan your reprovisioning workflow (such as applying an Intune wipe or resetting via the Settings app) accordingly so that you can provide the required credentials if needed. For background and guidance, see [Factory reset protection (FRP) enforcement behavior for Android Enterprise](https://learn.microsoft.com/en-us/troubleshoot/mem/intune/device-configuration/factory-reset-protection-emails-not-enforced).
```

### Block H: Conditional Access exclusion note (verbatim from MS Learn `ref-corporate-methods.md`, mirrors COBO Prerequisites)

```markdown
If you have a Microsoft Entra Conditional Access policy defined that uses the *require a device to be marked as compliant* Grant control or a Block policy and applies to **All Cloud apps**, **Android**, and **Browsers**, you must exclude the **Microsoft Intune** cloud app from this policy. This is because the Android setup process uses a Chrome tab to authenticate your users during enrollment.
```

### Block I: Samsung-admins callout (D-19 verbatim, placed under `### Zero-Touch` H3)

```markdown
> ⚠️ **Samsung admins:** Choose Knox Mobile Enrollment (KME) or Zero-Touch — never both. Configuring both on the same Samsung devices causes out-of-sync enrollment state; KME takes precedence at the device firmware level. KME provisions Samsung corporate-owned-with-work-profile devices into the **WPCO** mode in the Knox EMM profile dropdown — WPCO is Google's modern terminology for the same deployment shape this guide calls COPE (corporate-owned device with a user-separated work profile). See [Knox Mobile Enrollment](07-knox-mobile-enrollment.md) for full KME admin coverage; [02-zero-touch-portal.md#kme-zt-mutual-exclusion](02-zero-touch-portal.md#kme-zt-mutual-exclusion) for the mutual-exclusion record; [_glossary-android.md#wpco](../_glossary-android.md#wpco) for the WPCO↔COPE terminology equivalence; and [_glossary-android.md#zero-touch-enrollment](../_glossary-android.md#zero-touch-enrollment) for the Zero-Touch definition and the iOS ADE cross-platform analog.
```

### Block J: COPE-vs-COBO decision matrix (D-13 LOCKED 5×3 shape; D-15/D-16/D-17 cell rules)

Sub-H3 placement: `### COPE-vs-COBO decision matrix` inside `## Provisioning method choice` H2. Anchor: `<a id="cope-vs-cobo-decision"></a>` placed immediately above the H3.

```markdown
| Decision factor | COBO (Fully Managed) | COPE (WPCO / Corp-Owned Work Profile) |
|---|---|---|
| **Personal apps on the device** | None — corporate-only device | Yes — separated work profile container; user installs personal apps in the personal partition |
| **Profile boundary model** | No profile boundary — entire device is managed scope | Work-profile container isolated from personal-side apps and data |
| **Android version floor for new provisioning** | See [03-android-version-matrix.md#cobo](../android-lifecycle/03-android-version-matrix.md#cobo) | See [03-android-version-matrix.md#cope](../android-lifecycle/03-android-version-matrix.md#cope) |
| **Samsung KME compatibility** | KME provisions COBO directly — see [KME](../_glossary-android.md#kme) and [07-knox-mobile-enrollment.md](07-knox-mobile-enrollment.md) | KME provisions WPCO (the modern COPE shape) — see [KME](../_glossary-android.md#kme), [WPCO glossary](../_glossary-android.md#wpco), and [07-knox-mobile-enrollment.md](07-knox-mobile-enrollment.md) |
| **Recommended for net-new in 2026** | Yes — when no personal-app allowance is needed | Yes — provision as WPCO per current Google guidance |
```
</verbatim_content>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 08-cope-full-admin.md with all 11 H2s, embedded verbatim content blocks A-J, banned-phrase compliance</name>

  <read_first>
    - docs/admin-setup-android/03-fully-managed-cobo.md (PRIMARY SIBLING — 250 lines, 11 H2s at lines 25/38/58/69/100/128/169/198/219/231/245; copy structural patterns; do NOT copy COBO-specific content like Android 10 floor, COBO FRP behavior, COBO Entra-join Chrome-tab Prerequisites wording — adapt for COPE)
    - docs/_templates/admin-template-android.md (Phase 34 D-15 template; ensure frontmatter shape matches)
    - docs/_glossary-android.md (verify glossary anchors `#cope` line 47, `#wpco` line 75, `#fully-managed` line 59, `#zero-touch-enrollment` line 33, `#kme` line 96 exist; `#private-space` does NOT yet exist — Wave 2 creates it; this plan forward-links to it)
    - docs/admin-setup-android/02-zero-touch-portal.md (verify `#kme-zt-mutual-exclusion` and `#dpc-extras-json` anchors exist for forward-link targets)
    - docs/admin-setup-android/07-knox-mobile-enrollment.md (verify line 16 KME/ZT mutex callout pattern; this guide becomes the 3rd Samsung-admin reading-path node per D-24)
    - docs/android-lifecycle/03-android-version-matrix.md (verify line 30 BYOD-row Private Space text; `#android-15-private-space-breakpoint` does NOT yet exist — Wave 2 creates it; this plan forward-links to it)
    - .planning/phases/46-cope-full-admin/46-CONTEXT.md (D-01..D-32 all locked)
    - .planning/phases/46-cope-full-admin/46-RESEARCH.md (Pitfalls 1-8; Code Examples 1-9; Open Questions 1-4 resolved per recommendations: sub-H3 matrix placement / Option A "Intune" scoping / 5 sub-H3s + Android 11+ removal callout / Plan 1+ baseline)
  </read_first>

  <files>docs/admin-setup-android/08-cope-full-admin.md</files>

  <action>
Create the new file `docs/admin-setup-android/08-cope-full-admin.md` with the EXACT structure below. Do not deviate from H2 names, H2 order, or anchor IDs. Embed verbatim content blocks A-J from the `<verbatim_content>` section above at the indicated positions.

**Frontmatter (D-26 + D-27):**
```yaml
---
last_verified: <execute-time-date YYYY-MM-DD>
review_by: <last_verified + 60 days, YYYY-MM-DD>
audience: admin
platform: Android
applies_to: COPE
---
```

**Platform-gate blockquote + H1 + intro paragraphs (mirror COBO lines 9-22 structure; substitute COPE-specific values):**
- Platform-gate `> **Platform gate:** This guide covers Android Enterprise Corporate-Owned Devices with Work Profile (COPE / WPCO) enrollment through Microsoft Intune...` covering the 11-H2 scope. Cross-link to iOS / macOS admin guides + glossary.
- H1: `# Android Enterprise Corporate-Owned Work Profile (COPE / WPCO) Admin Setup`
- "Covered" sentence enumerates the 11 H2s in plain prose
- "Not covered" sentence: MGP binding (link to `01-managed-google-play.md#bind-mgp`), ZT portal mechanics (link to `02-zero-touch-portal.md`), Knox Mobile Enrollment (link to `07-knox-mobile-enrollment.md`), L1/L2 runbooks deferred to v1.5
- "How to use" sentence per COBO line 22 pattern

---

**H2 #1: `## Key Concepts`** (anchor: `<a id="key-concepts"></a>`)

3-4 paragraphs covering:
- **Work profile is the personal-side partition on a corporate device** (COPE-specific: opposite of COBO's "work profile is the entire device" — explain that on COPE the work profile is one of two partitions; the personal-side partition allows user apps; admin manages only the work-profile side device-policy + the device-owner-side OS/restrictions per WPCO model). Link to `[Work Profile](../_glossary-android.md#work-profile)`.
- **Entra join behavior** — Entra-joined at enrollment via Chrome Custom Tab sign-in; CA prereq (cross-link to Prerequisites H2 below). Mirror COBO line 33 wording adapted for COPE.
- **Cross-platform note** mirroring COBO line 35 pattern, explaining COPE has no Windows / iOS / macOS analog (per `_glossary-android.md:79`).
- Embed Block A (D-08 ⚠️ Private Space callout) as the trailing line of this H2, immediately before the next H2.

---

**H2 #2: `## Prerequisites`** (anchor: `<a id="prerequisites"></a>`)

Mirror COBO Prerequisites (lines 38-55) STRUCTURE. Hard prerequisites checklist + tenant-conditional prerequisite + What-breaks blockquote.

Hard prerequisites (substitute COPE specifics):
- Microsoft Intune Plan 1+ (per RESEARCH Open Question #4 recommendation)
- MGP binding complete — link `[01-managed-google-play.md#bind-mgp](01-managed-google-play.md#bind-mgp)`
- Entra join enabled
- Factory-reset or new device — COPE requires out-of-box state
- **Android 8.0+** — hard minimum (HIGHER than COBO's 10.0; per MS Learn `setup-corporate-work-profile.md`). Link `[03-android-version-matrix.md#cope](../android-lifecycle/03-android-version-matrix.md#cope)`.
- GMS connectivity

Tenant-conditional prerequisite (anchor `<a id="ca-exclusion-intune-app"></a>`):
- Embed Block H verbatim. Wrap with HTML comment `<!-- HIGH confidence: MS Learn ref-corporate-methods.md verified 2026-04-25 [HIGH: MS Learn, last_verified: <execute-date>] -->`

What-breaks blockquote: mirror COBO line 55 pattern; describe COPE-specific failures (missing Entra join → COPE token generation fails; missing CA exclusion → Chrome-tab sign-in fails).

---

**H2 #3: `## COPE Migration Note`** (anchor: `<a id="cope-migration"></a>`) — REPURPOSED inverse-direction per D-03

3 paragraphs, COPE-side perspective (NOT COBO-side as in `03-fully-managed-cobo.md:58-66`):
- Para 1: Inverse-direction back-link to COBO: "If you are evaluating COPE versus COBO (Fully Managed) for net-new corporate Android deployments, see [03-fully-managed-cobo.md#cope-migration](03-fully-managed-cobo.md#cope-migration) for the COBO-side framing of this same decision."
- Para 2: COPE-side migration framing for existing COPE fleets — explain that current COPE deployments continue to operate; Google's modern terminology is WPCO; per `[_glossary-android.md#wpco](../_glossary-android.md#wpco)` "WPCO, formerly COPE." Use POSITIVE framing per D-17. NEVER use {deprecated, EOL, removed, no longer supported, obsolete, sunset, retired} attached to COPE.
- Para 3: Forward-link to the in-doc decision matrix: "For a structured COPE-vs-COBO comparison, see [COPE-vs-COBO decision matrix](#cope-vs-cobo-decision) below in the Provisioning method choice section."

---

**H2 #4: `## Enrollment profile creation`** (anchor: `<a id="enrollment-profile"></a>`)

Mirror COBO H2 (lines 69-99) STRUCTURE. Sub-headings:
- `#### In Intune admin center` — Embed Block B (5-step navigation) verbatim.
- Description of profile fields the admin sets next: name, description, token type, naming template.
- Token type explanation — Embed Block C verbatim.
- Naming template explanation — Embed Block D verbatim, including the trailing "Edits you make to the naming template only apply to new enrollments." sentence.
- A `> **What breaks if misconfigured:**` blockquote mirroring COBO pattern (e.g., "Naming template applied retroactively assumption → admins expect existing devices to rename; only new enrollments use updated templates. Recovery: re-enroll affected devices.").

Source-attribution HTML comment: `<!-- HIGH confidence: MS Learn setup-corporate-work-profile.md "Create an enrollment profile" section, last_verified: <execute-date>; updated_at 2026-04-16 per RESEARCH §Sources -->`

---

**H2 #5: `## Enrollment token management`** (anchor: `<a id="enrollment-token"></a>`)

Mirror COBO H2 (lines 100-127) STRUCTURE. Two GA token types as sub-headings:
- `### Default token` — never expires; revoke only on accidental exposure.
- `### Staging token` — configurable up to 65 years (default 65). Cite Block C "up to 65 years" verbatim.

Token rotation + revocation guidance:
- Token rotation cadence per RESEARCH §State-of-the-Art ("rotate staging every 1-2 years regardless of 65-year ceiling").
- `> **What breaks if misconfigured:**` covering: (1) staging token revoked prematurely → in-flight enrollments fail (CRITICAL); (2) exported token shared insecurely → unauthorized device enrollment risk (CRITICAL); (3) token assigned to wrong group → wrong devices enroll (HIGH).

---

**H2 #6: `## Provisioning method choice`** (anchor: `<a id="provisioning-method-choice"></a>`)

Mirror COBO H2 (lines 128-166) STRUCTURE with COPE-specific Android-version-removal callouts. **5 sub-H3s** per RESEARCH Open Question #3 (planner picks: enumerate all 5 methods MS Learn lists for parity-preservation):

Provide a 1-paragraph intro mentioning the four-then-five-method landscape and the Android 11+ removal of three legacy paths (NFC, `afw#setup`, token-entry).

Sub-H3s:

1. `### QR code` — short paragraph; cross-link `[02-provisioning-methods.md#qr-code](../android-lifecycle/02-provisioning-methods.md#qr-code)` for version-availability matrix; `> **What breaks if misconfigured:**` (e.g., stale QR image; revoked token).

2. `### NFC` — short paragraph including verbatim Block E sentence (NFC + afw#setup Android 8-10 only, removed Android 11+). Cross-link to provisioning-methods matrix.

3. `### afw#setup` — short paragraph noting same Android 11+ removal per Block E. Cross-link `[afw#setup glossary](../_glossary-android.md#afw-setup)`.

4. `### Token entry` — **NEW per RESEARCH FINDING #1**. Short paragraph: "Token-entry enrollment is documented in MS Learn `ref-corporate-methods.md` for COPE/WPCO." Embed verbatim Note from Block E ("Not supported on Android Enterprise corporate-owned devices with a work profile (COPE) running Android version 11.0."). Surface this method's Android 11+ removal alongside NFC + afw#setup so the COPE doc enumerates THREE methods removed (not two).

5. `### Zero-Touch` — Mirror COBO line 158-166 structure adapted for COPE. Reference Phase 35 ZT portal binding. Per RESEARCH §Code Examples 9 / D-19 caution: do NOT re-state the ZT iframe DPC extras JSON — instead link `[02-zero-touch-portal.md#dpc-extras-json](02-zero-touch-portal.md#dpc-extras-json)` (canonical-source pattern). Embed Block I (Samsung-admins callout) verbatim immediately AFTER the body paragraph and BEFORE the `> **What breaks**` blockquote. Cross-link `[02-zero-touch-portal.md#link-zt-to-intune](02-zero-touch-portal.md#link-zt-to-intune)` for ZT↔Intune linking.

After the 5 sub-H3s, insert sub-H3 anchor + H3:

```markdown
<a id="cope-vs-cobo-decision"></a>
### COPE-vs-COBO decision matrix
```

Then a 1-2 sentence intro stating purpose ("This matrix helps Intune admins choose between COBO and COPE for net-new corporate Android deployments. Migration story for existing fleets is covered in [COPE Migration Note](#cope-migration) above."). Then embed Block J verbatim.

Source-attribution HTML comment after the matrix: `<!-- HIGH confidence: cell content per CONTEXT D-13/D-15/D-16/D-17 + glossary canonical anchors verified 2026-04-25 -->`

---

**H2 #7: `## Android 15 FRP and EFRP`** (anchor: `<a id="android-15-frp"></a>`)

Mirror COBO H2 (lines 168-195) STRUCTURE — but with COPE-specific FRP behavior per RESEARCH NEW FINDING #2. Do NOT copy-paste COBO body wholesale.

- `> ⚠️` warning callout (mirror COBO line 171 pattern adapted for COPE).
- "**What changed on Android 15.**" paragraph (mirror COBO line 173 wording).
- Embed Block F (FRP behavior table) verbatim, INSIDE this H2. Source-attribution HTML comment: `<!-- HIGH confidence: MS Learn ref-corporate-methods.md Factory reset protection table verified 2026-04-25 [HIGH: MS Learn ref-corporate-methods, last_verified: <execute-date>] -->`
- Embed Block G (Android 15 COPE-specific re-enrollment requirement) verbatim immediately after the table. Note: this requirement applies to COPE specifically and does NOT appear in COBO's H2.
- "**Admin action required.**" paragraph: configure EFRP via Intune policy BEFORE any device reset on Android 15 COPE devices.
- Sub-H3 `### Configure EFRP in Intune` (anchor `<a id="configure-efrp"></a>`): mirror COBO lines 178-189 navigation steps, substitute "COPE devices" for "COBO devices" in step descriptions.
- `> **What breaks if misconfigured:**` blockquote.
- "References:" line: cite `[Google AE Help — Enable EFRP](https://support.google.com/work/android/answer/14549362)`, `[MS Learn — Factory Reset Protection Emails Not Enforced](https://learn.microsoft.com/en-us/troubleshoot/mem/intune/device-configuration/factory-reset-protection-emails-not-enforced)`, `[MS Learn — Corporate enrollment methods reference](https://learn.microsoft.com/en-us/intune/device-enrollment/android/ref-corporate-methods)`.

---

**H2 #8: `## What Breaks Summary`** (anchor: `<a id="what-breaks"></a>`)

Mirror COBO H2 (lines 198-216) STRUCTURE — table with `| Misconfiguration | Section | Severity |` columns. Order rows by severity descending.

Required rows (COPE-specific failure modes):
- Entra join not enabled — `[Prerequisites](#prerequisites)` — CRITICAL
- CA exclusion missing (tenant-conditional) — `[Prerequisites](#prerequisites)` — HIGH (if CA policy applies)
- Staging token revoked prematurely — `[Enrollment token management](#enrollment-token)` — CRITICAL
- Exported token shared insecurely — `[Enrollment token management](#enrollment-token)` — CRITICAL
- KME + ZT dual-configured on Samsung — `[Provisioning method choice — Zero-Touch](#provisioning-method-choice)` — CRITICAL
- EFRP not assigned before Android 15 reset — `[Android 15 FRP and EFRP](#android-15-frp)` — CRITICAL
- Android 15 Settings-app reset without re-supplying Google account — `[Android 15 FRP and EFRP](#android-15-frp)` — HIGH (COPE-specific per Block G)
- NFC / afw#setup / token-entry attempted on Android 11+ COPE — `[Provisioning method choice](#provisioning-method-choice)` — HIGH (COPE-specific Android 11+ removal)
- Naming template assumed retroactive — `[Enrollment profile creation](#enrollment-profile)` — MEDIUM
- Token assigned to wrong group — `[Enrollment token management](#enrollment-token)` — HIGH

Per CONTEXT Deferred §What-Breaks-row-for-Private-Space: do NOT add a Private Space row (no admin lever to break).

---

**H2 #9: `## Renewal / Maintenance`** (anchor: `<a id="renewal-maintenance"></a>`)

Mirror COBO H2 (lines 219-229) STRUCTURE — table with `| Component | Renewal Period | Consequence of Lapse | Renewal Steps |` columns.

Required rows:
- MGP binding — same wording as COBO line 223
- Default enrollment token — same wording as COBO line 224
- Staging enrollment token — same wording as COBO line 225 (mention 65-year ceiling + 1-2-year rotation)
- ZT reseller contract — same wording as COBO line 226
- EFRP policy assignment — same wording as COBO line 227 (substitute "COPE devices" / "COPE fleet")

"Rotation cadence:" sentence mirroring COBO line 229.

---

**H2 #10: `## See Also`** (no anchor)

Bullet list mirroring COBO See Also (lines 231-243). Required items:
- `[Android Enterprise Admin Setup Overview](00-overview.md)`
- `[Managed Google Play Binding](01-managed-google-play.md)` — MGP binding prerequisite
- `[Zero-Touch Portal Configuration](02-zero-touch-portal.md)` — Zero-Touch prerequisite
- `[Fully Managed (COBO) Admin Setup](03-fully-managed-cobo.md)` — sibling mode (COBO/COPE decision matrix above)
- `[BYOD Work Profile Admin Setup](04-byod-work-profile.md)` — sibling personally-owned work-profile path
- `[Knox Mobile Enrollment](07-knox-mobile-enrollment.md)` — Samsung COPE/WPCO provisioning channel
- `[Android Enterprise Prerequisites](../android-lifecycle/01-android-prerequisites.md)`
- `[Android Provisioning Methods](../android-lifecycle/02-provisioning-methods.md)`
- `[Android Version Matrix](../android-lifecycle/03-android-version-matrix.md)`
- `[Android Enterprise Provisioning Glossary — COPE](../_glossary-android.md#cope)`
- `[Android Enterprise Provisioning Glossary — WPCO](../_glossary-android.md#wpco)`
- `[Android Enterprise Provisioning Glossary — Work Profile](../_glossary-android.md#work-profile)`
- `[Android Enterprise Provisioning Glossary — KME](../_glossary-android.md#kme)`
- `[Android Enterprise Provisioning Glossary — Play Integrity](../_glossary-android.md#play-integrity)`

---

**H2 #11: `## Changelog`** (no anchor)

Mirror COBO Changelog (lines 245-250). Initial entry shape:

```markdown
| Date | Change | Author |
|------|--------|--------|
| <execute-date> | Initial version — COPE/WPCO enrollment profile, default + staging-up-to-65-year tokens, all 7 naming templates, 5 provisioning methods (QR / NFC / afw#setup / Token / Zero-Touch) with Android 11+ removal callouts for NFC + afw#setup + token-entry (D-01..D-07 mirror; RESEARCH NEW FINDING #1), Android 15 FRP H2 with COPE-specific FRP behavior table + Settings-app re-enrollment requirement (RESEARCH NEW FINDING #2), Android 15 Private Space ⚠️ callout in Key Concepts (D-08), COPE-vs-COBO decision matrix sub-H3 (D-13), Samsung-admins callout in Zero-Touch H3 (D-19), inverse-direction COPE Migration Note back-link to COBO (D-03). Closes AECOPE-01 / Phase 46. | -- |
```

---

**Banned-phrase compliance (D-31):** Before saving the file, grep your draft for `\b(deprecated|end of life|EOL|removed|no longer supported|obsolete|sunset|retired)\b`. Any hit attached to COPE/WPCO must be reframed using POSITIVE framing per D-17 (e.g., "Google recommends WPCO as the *provisioning name* per current Google guidance" — never "COPE was deprecated"). Note: `removed` may legitimately appear when describing the Android 11+ method removals (NFC/afw#setup/token-entry) — these are method removals, NOT mode/COPE removal — so phrasing like "the NFC enrollment method is not available on Android 11" or "removed as a COPE provisioning path on Android 11+" is acceptable when factual; phrasing like "COPE is removed/deprecated on Android 11+" is NOT acceptable.

**Confidence-attribution markers (D-28):** Every MEDIUM/LOW assertion must carry `[(HIGH|MEDIUM|LOW)(: [A-Za-z ]+)?(, last_verified \d{4}-\d{2}-\d{2})?]` markers. HIGH-confidence content from MS Learn does not need inline markers but should include source-attribution HTML comments at section ends.

**File length envelope:** Target ~250 lines (per COBO sibling shape per D-32 + RESEARCH §Component Responsibilities).
  </action>

  <acceptance_criteria>
    - File `docs/admin-setup-android/08-cope-full-admin.md` exists
    - `grep -c '^## ' docs/admin-setup-android/08-cope-full-admin.md` returns exactly `11`
    - `grep -E '^## (Key Concepts|Prerequisites|COPE Migration Note|Enrollment profile creation|Enrollment token management|Provisioning method choice|Android 15 FRP and EFRP|What Breaks Summary|Renewal / Maintenance|See Also|Changelog)$' docs/admin-setup-android/08-cope-full-admin.md` returns 11 lines (all expected H2 names match COBO sibling)
    - The H2 sequence in the new file MATCHES the COBO sibling order exactly when compared via `diff <(grep -n '^## ' docs/admin-setup-android/08-cope-full-admin.md | sed 's/^[0-9]*://') <(grep -n '^## ' docs/admin-setup-android/03-fully-managed-cobo.md | sed 's/^[0-9]*://')`
    - `grep -F 'Corporate-owned devices with work profile' docs/admin-setup-android/08-cope-full-admin.md` returns ≥1 hit (Block B verbatim UI label)
    - All 7 naming templates literal-string match: `grep -F '{{SERIAL}}' docs/admin-setup-android/08-cope-full-admin.md` returns ≥1; same for `{{SERIALLAST4DIGITS}}`, `{{DEVICETYPE}}`, `{{ENROLLMENTDATETIME}}`, `{{UPNPREFIX}}`, `{{USERNAME}}`, `{{RAND:x}}` (each returns ≥1 hit)
    - Both token types appear: `grep -F 'Corporate-owned with work profile (default)' docs/admin-setup-android/08-cope-full-admin.md` returns ≥1; `grep -F 'Corporate-owned with work profile, via staging' docs/admin-setup-android/08-cope-full-admin.md` returns ≥1
    - 65-year staging token reference: `grep -F 'up to 65 years' docs/admin-setup-android/08-cope-full-admin.md` returns ≥1
    - Android 11+ removal callout enumerates THREE methods: `grep -E '(NFC|afw#setup|token-entry|Token entry)' docs/admin-setup-android/08-cope-full-admin.md` returns ≥3 distinct method references in the Provisioning method choice H2 context
    - COPE-specific FRP table present: `grep -F '| **Corporate-owned devices with work profile** (COPE) | ✅ factory reset protection | ✅ factory reset protection | ❌ no factory reset protection |' docs/admin-setup-android/08-cope-full-admin.md` returns 1 hit
    - Android 15 COPE-specific re-enrollment text present: `grep -F 'For corporate owned devices with a work profile running Android 15, you will need to re-enter the Google account' docs/admin-setup-android/08-cope-full-admin.md` returns 1 hit
    - Private Space ⚠️ callout in Key Concepts: `grep -B0 -A1 'Android 15 — Private Space' docs/admin-setup-android/08-cope-full-admin.md | grep -E '⚠️|_glossary-android.md#private-space|android-15-private-space-breakpoint'` returns ≥2 hits
    - Decision matrix anchor + sub-H3: `grep -F '<a id="cope-vs-cobo-decision"></a>' docs/admin-setup-android/08-cope-full-admin.md` returns 1; `grep -F '### COPE-vs-COBO decision matrix' docs/admin-setup-android/08-cope-full-admin.md` returns 1
    - Decision matrix has 5 data rows × 3 columns: count rows in the matrix table starting after `### COPE-vs-COBO decision matrix` H3 — should have header row + separator + 5 data rows = 7 markdown table lines starting with `|`
    - Samsung-admins callout in Zero-Touch H3 with WPCO equivalence: `grep -F 'WPCO is Google'\''s modern terminology for the same deployment shape this guide calls COPE' docs/admin-setup-android/08-cope-full-admin.md` returns 1 hit
    - Samsung-admins callout has ≥4 forward links: `grep -F '> ⚠️ **Samsung admins:**' docs/admin-setup-android/08-cope-full-admin.md` returns 1 hit, and the same blockquote contains references to `07-knox-mobile-enrollment.md`, `02-zero-touch-portal.md#kme-zt-mutual-exclusion`, `_glossary-android.md#wpco`, `_glossary-android.md#zero-touch-enrollment`
    - Inverse-direction COPE Migration Note back-link: `grep -F '03-fully-managed-cobo.md#cope-migration' docs/admin-setup-android/08-cope-full-admin.md` returns ≥1 hit (back-link to COBO sibling per D-03)
    - Frontmatter validity: `grep -E '^applies_to: COPE$' docs/admin-setup-android/08-cope-full-admin.md` returns 1; `grep -E '^audience: admin$' docs/admin-setup-android/08-cope-full-admin.md` returns 1; `grep -E '^platform: Android$' docs/admin-setup-android/08-cope-full-admin.md` returns 1; `grep -E '^last_verified: \d{4}-\d{2}-\d{2}$' docs/admin-setup-android/08-cope-full-admin.md` returns 1; `grep -E '^review_by: \d{4}-\d{2}-\d{2}$' docs/admin-setup-android/08-cope-full-admin.md` returns 1
    - Banned-phrase compliance (D-31) — zero hits when applied to COPE: `grep -nE '\b(deprecated|end of life|EOL|no longer supported|obsolete|sunset|retired)\b' docs/admin-setup-android/08-cope-full-admin.md` either returns ZERO hits OR every hit is in factual cross-link prose NOT attached to COPE-mode (e.g., a SafetyNet historical reference is acceptable; "COPE is deprecated" is NOT)
    - Audit harness passes: `node scripts/validation/v1.4.1-milestone-audit.mjs` exits 0 with 8/8 PASS
    - File length within envelope: `wc -l docs/admin-setup-android/08-cope-full-admin.md` returns a value between 200 and 320 lines (~250 ± 70 per COBO sibling)
  </acceptance_criteria>

  <verify>
    <automated>node scripts/validation/v1.4.1-milestone-audit.mjs</automated>
  </verify>

  <done>
File `docs/admin-setup-android/08-cope-full-admin.md` exists with exactly 11 H2s in COBO sibling order; embeds all 9 verbatim content blocks (A=Private Space callout / B=Intune nav / C=token types / D=naming templates / E=Android 11+ removed methods × 3 / F=COPE FRP table / G=Android 15 re-enrollment / H=CA exclusion / I=Samsung-admins callout / J=decision matrix); frontmatter per D-26 + D-27; banned-phrase grep clean per D-31; audit harness 8/8 PASS. AECOPE-01 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → markdown corpus | Author writes new content — banned-phrase prose can leak into COPE doc if discipline lapses |
| corpus → audit harness | Audit harness validates content but C9 banned-phrase is INFORMATIONAL-FIRST per D-31 / Phase 43 D-29; not blocking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering (prose injection) | `docs/admin-setup-android/08-cope-full-admin.md` Key Concepts / COPE Migration Note / decision matrix cells | mitigate | (1) Author-time grep `grep -nE '\b(deprecated\|end of life\|EOL\|removed\|no longer supported\|obsolete\|sunset\|retired)\b'` against draft before save; (2) C9 audit-harness check informational-first per D-29 / D-31; (3) plan-task-spec discipline (this plan's `<acceptance_criteria>` makes banned-phrase compliance an explicit gate); (4) positive-framing rule per D-17 (recommend WPCO as *provisioning name* per Google guidance, never as *replacement* for COPE) |
| T-46-05 | Tampering (false-pin reintroduction) | Samsung-admins callout in `### Zero-Touch` H3 | mitigate | Embed Block I VERBATIM including the WPCO-equivalence sentence "WPCO is Google's modern terminology for the same deployment shape this guide calls COPE." Acceptance criterion explicitly greps for this exact sentence to prevent omission. Knox doc `:14`/`:16`/`:61`/`:72` + glossary `:71`/`:75-77`/`:98` precedent verified per RESEARCH §Common Pitfalls 3. |
</threat_model>

<verification>
After file lands, run:
1. `node scripts/validation/v1.4.1-milestone-audit.mjs` — expect 8/8 PASS
2. `grep -c '^## ' docs/admin-setup-android/08-cope-full-admin.md` — expect 11
3. `wc -l docs/admin-setup-android/08-cope-full-admin.md` — expect 200-320
4. `grep -nE '\b(deprecated|end of life|EOL|no longer supported|obsolete|sunset|retired)\b' docs/admin-setup-android/08-cope-full-admin.md` — expect zero hits attached to COPE-mode
5. Visual review of COBO migration note H2 (#3) — confirm INVERSE-DIRECTION wording (forward-link to COBO `:58`, NOT outbound-link from COBO to COPE)
6. Visual review of decision matrix sub-H3 placement — confirm INSIDE `## Provisioning method choice` H2 (preserves 11-H2 lock per D-05)
</verification>

<success_criteria>
- AECOPE-01 ROADMAP SC#1 satisfied: parallel structure to COBO with all enumerated content (UI label / token types / naming templates / Android 8-15 breakpoints / Android 11+ removed methods / decision matrix)
- ROADMAP SC#2 satisfied: Android 15 Private Space `> ⚠️` callout present in `## Key Concepts` H2 with forward-links to glossary + version-matrix anchors (anchors created by Wave 2 plan; this plan creates the link sites)
- ROADMAP SC#5 partially satisfied: zero banned phrases applied to COPE in this file; full SC#5 closure requires Wave 2 file set (BYOD, COBO, glossary, version-matrix, capability matrix all clean)
- D-25 RESEARCH gate PASSED (verified in 46-RESEARCH.md before this plan executes)
- Audit harness 8/8 PASS post-commit
</success_criteria>

<output>
After completion, create `.planning/phases/46-cope-full-admin/46-01-SUMMARY.md` documenting:
- File created with byte-count, line-count, 11 H2 names in order
- Verbatim content blocks A-J landed (cite line numbers in new file for each)
- Banned-phrase grep result (expected: zero hits attached to COPE)
- Audit harness result (expected: 8/8 PASS)
- AECOPE-01 closed; ready for Wave 2 (Plan 02) atomic same-commit retrofits
</output>
