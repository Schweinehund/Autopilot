---
phase: 35
slug: android-prerequisites-mgp-zero-touch-portal
plan: 02
type: execute
wave: 2
depends_on: [phase-34-complete]
files_modified:
  - docs/admin-setup-android/00-overview.md
autonomous: true
requirements: [AEPREQ-02]
must_haves:
  truths:
    - "Admin opening docs/admin-setup-android/00-overview.md sees a mermaid flowchart with 5 mode branches (COBO, BYOD WP, Dedicated, ZTE, AOSP)"
    - "Admin can identify which portals each mode depends on — COBO/BYOD/Dedicated through MGP; ZTE through MGP + ZT portal; AOSP through neither"
    - "Admin sees per-path prerequisites checklists (GMS-path, ZTE-path, AOSP-path, Shared)"
    - "Admin reads a Portal Navigation Note at the bottom clarifying endpoint.microsoft.com vs intune.microsoft.com"
    - "Doc is authored against docs/_templates/admin-template-android.md (Phase 34 structural template) and follows docs/admin-setup-ios/00-overview.md as the primary structural analog"
  artifacts:
    - path: "docs/admin-setup-android/00-overview.md"
      provides: "Admin setup overview for Android Enterprise tri-portal sequence with per-mode routing"
      contains: "# Android Enterprise Admin Setup"
    - path: "docs/admin-setup-android/00-overview.md"
      provides: "Stable anchors for downstream cross-reference: #choose-your-mode, #gms-path-prerequisites, #zte-path-prerequisites, #aosp-path-prerequisites, #portal-navigation-note"
      contains: "GMS-Path Prerequisites"
  key_links:
    - from: "docs/admin-setup-android/00-overview.md"
      to: "docs/admin-setup-android/01-managed-google-play.md"
      via: "mermaid flowchart + numbered path list (Wave 2 sibling; link is dangling at authoring time but resolves by end of Wave 2)"
      pattern: "01-managed-google-play\\.md"
    - from: "docs/admin-setup-android/00-overview.md"
      to: "docs/admin-setup-android/02-zero-touch-portal.md"
      via: "mermaid flowchart + numbered path list"
      pattern: "02-zero-touch-portal\\.md"
    - from: "docs/admin-setup-android/00-overview.md"
      to: "docs/admin-setup-android/02-zero-touch-portal.md#step-0-reseller"
      via: "ZTE-path prerequisites checklist"
      pattern: "02-zero-touch-portal\\.md#step-0-reseller"
---

<objective>
Create `docs/admin-setup-android/00-overview.md` — an admin setup overview (800–1200 words) that routes Intune admins to the correct tri-portal setup sequence for their chosen mode (COBO, BYOD WP, Dedicated, ZTE, AOSP). Satisfies AEPREQ-02 and Phase 35 Success Criterion 2.

Purpose: This is the navigation artifact for `docs/admin-setup-android/`. Downstream Phase 36-39 mode-specific admin guides depend on this overview's mermaid diagram and prerequisites checklists to route admins into the correct path. Creating the `docs/admin-setup-android/` directory (Phase 35 creates it; Phase 34 does not) is a side effect of writing this first admin-setup-android file.

Output: One new markdown file at `docs/admin-setup-android/00-overview.md`, committed to the repository and passing all per-task grep validation checks (35-02-01 through 35-02-06) plus cross-phase guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-android-prerequisites-mgp-zero-touch-portal/35-CONTEXT.md
@.planning/phases/35-android-prerequisites-mgp-zero-touch-portal/35-RESEARCH.md
@.planning/phases/35-android-prerequisites-mgp-zero-touch-portal/35-PATTERNS.md
@.planning/phases/35-android-prerequisites-mgp-zero-touch-portal/35-VALIDATION.md
@.planning/phases/34-android-foundation/34-CONTEXT.md

<interfaces>
<!-- Phase 34 deliverables this doc depends on. -->

From `docs/_templates/admin-template-android.md` (Phase 34 deliverable — MUST exist before this task):
- Frontmatter schema: `last_verified`, `review_by`, `audience`, `platform`, `applies_to`
- Platform gate blockquote pattern
- `#### In Intune admin center` / `#### In Managed Google Play` / `#### In Zero-Touch portal` H4 convention (NOT directly used in overview — this is a navigation doc, not a mode guide)
- HTML-comment subtractive-deletion pattern (not used here; relevant to Plan 03)
- Renewal/Maintenance section (NOT required for overview docs per admin-template-ios.md/admin-template-macos.md precedent — this is a navigation overview, not a guide with configurable settings)

Structural analog (primary, exact role match):
- `docs/admin-setup-ios/00-overview.md` — mermaid flowchart + numbered path list + per-path prerequisites checklists + Portal Navigation Note
- Section heading hierarchy: H1 → `## Setup Sequence` (mermaid + numbered list) → `## Prerequisites` (`### ADE-Path`, `### BYOD-Path`) → `## Portal Navigation Note` → `## See Also`

Secondary analog:
- `docs/admin-setup-macos/00-overview.md` — dual-portal variant of the overview pattern (macOS uses ABM + Intune)

Cross-reference targets:
- `01-managed-google-play.md` (Wave 2 sibling; link resolves within same phase execution)
- `02-zero-touch-portal.md#step-0-reseller` (Wave 2 sibling; specific anchor reserved by Plan 04)
- `01-managed-google-play.md#what-breaks` (Plan 03's reserved anchor)
- `../android-lifecycle/00-enrollment-overview.md` (Phase 34)
- `../_glossary-android.md` (Phase 34)

Reserved anchors (D-11) that this doc MUST establish:
- `#choose-your-mode` → on/near the mermaid section
- `#gms-path-prerequisites` → `### GMS-Path Prerequisites`
- `#zte-path-prerequisites` → `### ZTE-Path Prerequisites`
- `#aosp-path-prerequisites` → `### AOSP-Path Prerequisites`
- `#portal-navigation-note` → `## Portal Navigation Note`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 00-overview.md (admin-setup-android directory entry point)</name>
  <files>docs/admin-setup-android/00-overview.md</files>
  <read_first>
    - `.planning/phases/35-android-prerequisites-mgp-zero-touch-portal/35-CONTEXT.md` (D-07 mermaid spec, D-08 numbered list, D-09 path-split prereqs, D-10 Portal Navigation Note, D-11 anchors)
    - `.planning/phases/35-android-prerequisites-mgp-zero-touch-portal/35-RESEARCH.md` (§Pattern 2 — `00-overview.md` Setup-Sequence Navigation, §Standard Stack → Portal URLs, §Source Confidence by Deliverable → 00-overview.md)
    - `.planning/phases/35-android-prerequisites-mgp-zero-touch-portal/35-PATTERNS.md` (§`docs/admin-setup-android/00-overview.md` pattern assignments — mermaid + path-split + Portal Navigation Note adaptations)
    - `.planning/phases/34-android-foundation/34-CONTEXT.md` (tri-portal shorthand convention, mode labels)
    - `docs/admin-setup-ios/00-overview.md` (primary structural analog — read in full to internalize mermaid + numbered list + prereqs checklist + Portal Navigation Note + See Also footer + changelog table structure)
    - `docs/admin-setup-macos/00-overview.md` (secondary analog — dual-portal variant)
    - `docs/_templates/admin-template-android.md` (Phase 34 — frontmatter schema, platform gate blockquote; note: this is an OVERVIEW not a mode-guide, so Steps / Renewal/Maintenance sections are NOT required)
    - `docs/android-lifecycle/00-enrollment-overview.md` (Phase 34 — verify 5-mode label consistency; cross-reference target)
    - `docs/android-lifecycle/01-android-prerequisites.md` (Plan 01 sibling — Wave 1 output; cross-reference target)
  </read_first>
  <action>
    Create the file `docs/admin-setup-android/00-overview.md` with the exact structure below. First, confirm the directory `docs/admin-setup-android/` does not yet exist (Plan 02 creates it as a side effect of this file). If any other admin-setup-android file already exists from a prior execution, stop and flag — that breaks the plan ordering.

    **Frontmatter** (D-18 convention; 60-day cycle means `review_by` = `last_verified` + 60 days):
    ```yaml
    ---
    last_verified: YYYY-MM-DD
    review_by: YYYY-MM-DD
    applies_to: all
    audience: admin
    platform: Android
    ---
    ```

    **Body structure** (exact headings — D-11 anchors auto-generate from these):
    ```
    # Android Enterprise Admin Setup

    > **Platform gate:** This guide covers Android Enterprise admin setup across all enrollment modes:
    > COBO (Fully Managed), BYOD Work Profile, Dedicated (COSU), Zero-Touch Enrollment (ZTE), and AOSP.
    > For iOS/iPadOS admin setup, see [iOS Admin Setup Guides](../admin-setup-ios/00-overview.md).
    > For macOS ADE setup, see [macOS Admin Setup Guides](../admin-setup-macos/00-overview.md).
    > For Android terminology, see the [Android Enterprise Provisioning Glossary](../_glossary-android.md).

    (1-2 paragraphs: explain the doc's purpose — routes admin to the right path based on mode choice. Cross-reference `../android-lifecycle/00-enrollment-overview.md` for mode-selection help.)

    ## Setup Sequence
    <a id="choose-your-mode"></a>

    (1 paragraph: explain how to read the diagram.)

    ```mermaid
    flowchart TD
        START[Admin lands here] --> CHOOSE{Choose your mode}
        CHOOSE -->|COBO / BYOD WP / Dedicated| MGP[01-managed-google-play.md]
        MGP --> COBO_PATH[Phase 36 — COBO admin guide]
        MGP --> BYOD_PATH[Phase 37 — BYOD Work Profile guide]
        MGP --> DED_PATH[Phase 38 — Dedicated devices guide]
        CHOOSE -->|Zero-Touch Enrollment| MGPZTE[01-managed-google-play.md]
        MGPZTE --> ZT[02-zero-touch-portal.md]
        ZT --> ZTE_PATH[Phase 39 — ZTE admin content]
        CHOOSE -->|AOSP| AOSP_PATH[Phase 39 — AOSP stub]
    ` ` `  (close the fence)

    (Numbered path list — D-08 one-line capsules per path. Each entry is a bolded link + em-dash + one-line capsule. NO duplication of guide content.)

    1. **[Managed Google Play Binding](01-managed-google-play.md)** — Bind the Intune tenant to
       Managed Google Play using an Entra account. Required for all GMS modes (COBO, BYOD WP,
       Dedicated, ZTE). Complete before any GMS enrollment profile.

    2. **[Zero-Touch Portal Configuration](02-zero-touch-portal.md)** — Configure the Zero-Touch
       portal account and DPC extras JSON, and link ZT to Intune. Required for ZTE only.
       Reseller relationship (Step 0) must be in place before this guide.

    ## Prerequisites

    Each admin path has its own prerequisite set. Determine your path from the diagram above, then
    confirm the prerequisites for that path.

    ### GMS-Path Prerequisites

    For COBO, BYOD Work Profile, and Dedicated enrollments:

    - [ ] **Managed Google Play binding** — Complete [01-managed-google-play.md](01-managed-google-play.md) before any GMS enrollment profile.
    - [ ] **Intune Administrator role** — Or custom RBAC role with enrollment management permissions.
    - [ ] **Microsoft Intune Plan 1** (or higher) subscription.
    - [ ] **Entra tenant active** — Required for Entra-preferred MGP binding (since August 2024).

    ### ZTE-Path Prerequisites

    All GMS-path prerequisites PLUS:

    - [ ] **Authorized Zero-Touch reseller relationship** — Devices must have been purchased from an authorized reseller. See [02-zero-touch-portal.md#step-0-reseller](02-zero-touch-portal.md#step-0-reseller).
    - [ ] **Zero-Touch portal Google account** — Corporate email (NOT Gmail). Created at `accounts.google.com/signupwithoutgmail`.
    - [ ] **ZT portal linked to Intune** — See [02-zero-touch-portal.md#link-zt-to-intune](02-zero-touch-portal.md#link-zt-to-intune).

    ### AOSP-Path Prerequisites

    - [ ] **Intune Administrator role**
    - [ ] **Microsoft Intune Plan 1** — Verify if Plan 2 / Intune Suite required per OEM; specialized AR/VR devices may require higher licensing. See [Phase 39 AOSP stub](06-aosp-stub.md) for OEM-specific licensing details.
    - [ ] **Device is on AOSP-supported OEM list** — See [Phase 39 AOSP stub](06-aosp-stub.md).

    ### Shared Prerequisites (All Paths)

    - [ ] **Microsoft Intune Plan 1** (or higher) subscription.
    - [ ] **Intune Administrator role** in Microsoft Intune admin center.
    - [ ] **Active Entra tenant**.

    ## Portal Navigation Note

    **Use `https://endpoint.microsoft.com` as the browser entry point for the Intune admin center.**
    The older `https://intune.microsoft.com` address remains active but may cause browser security
    zone mismatches during the Managed Google Play binding redirect flow (see
    [01-managed-google-play.md#what-breaks](01-managed-google-play.md#what-breaks) for the full
    what-breaks table covering this case).

    Portal paths in these guides reflect the current documented experience. If menu locations differ:

    - Look for equivalent options under **Devices** > **By platform** > **Android** > **Device onboarding** > **Enrollment**.
    - Portal navigation may vary by Intune admin center version and tenant rollout timing.

    ## See Also

    - [Managed Google Play Binding](01-managed-google-play.md)
    - [Zero-Touch Portal Configuration](02-zero-touch-portal.md)
    - [Android Enterprise Prerequisites](../android-lifecycle/01-android-prerequisites.md)
    - [Android Enterprise Enrollment Overview](../android-lifecycle/00-enrollment-overview.md)
    - [Android Provisioning Methods](../android-lifecycle/02-provisioning-methods.md)
    - [Android Version Matrix](../android-lifecycle/03-android-version-matrix.md)
    - [Android Enterprise Provisioning Glossary](../_glossary-android.md)

    ## Changelog

    | Date | Change | Author |
    |------|--------|--------|
    | YYYY-MM-DD | Initial version — tri-portal setup sequence, 5-mode mermaid, per-path prerequisites, Portal Navigation Note | -- |
    ```

    **Decision traceability:**
    - D-07: Single mermaid flowchart with 5 mode branches (COBO / BYOD WP / Dedicated → MGP → Phase 36/37/38; ZTE → MGP + ZT → Phase 39; AOSP → Phase 39 stub). Use the EXACT diagram text from PATTERNS.md §`00-overview.md` — do NOT improvise node IDs or edges.
    - D-08: Numbered list per path. One-line capsules. NO duplication of guide content. Two list items in Phase 35 (MGP and ZT portal); downstream phases may extend but Phase 35 authors exactly these two.
    - D-09: Four prerequisites checklists (GMS-path, ZTE-path, AOSP-path, Shared). Use checkbox syntax `- [ ]`. Use iOS `00-overview.md` as the structural analog.
    - D-10: Portal Navigation Note at the bottom is the single location for the `endpoint.microsoft.com` vs `intune.microsoft.com` redirect reminder at overview level. Full what-breaks callout lives inline in `01-managed-google-play.md#what-breaks` (Plan 03 reserves this anchor).
    - D-11: Four reserved anchors: `#choose-your-mode` (explicit `<a id="choose-your-mode"></a>` because the H2 "Setup Sequence" auto-slug wouldn't match), `#gms-path-prerequisites`, `#zte-path-prerequisites`, `#aosp-path-prerequisites`, `#portal-navigation-note`.

    **Required prohibitions:**
    - NO `#### In Intune admin center` / `#### In Managed Google Play` / `#### In Zero-Touch portal` H4 headings (this is a navigation overview, not a mode-guide).
    - NO `## Steps` heading, no numbered portal steps (same rationale — overview doc).
    - NO "supervision" or "supervised" used as an Android management term (AEAUDIT-04 guard).
    - NO SafetyNet references.
    - NO modifications to v1.0–v1.3 shared files (PITFALL 11 guard).
    - NO `01-android-prerequisites.md` duplication — that's Plan 01's job; this doc cross-references it.

    **Dangling-link notes** (expected at authoring time):
    - `01-managed-google-play.md` and `02-zero-touch-portal.md` are Wave 2 siblings — links are dangling when this task runs if tasks run in parallel. This is permitted because all three Wave 2 files land in the same phase execution. The validation check 35-all-08 (cross-reference integrity) runs at phase end after all Wave 2 plans complete.
    - `06-aosp-stub.md` is a Phase 39 deliverable — permanent forward-reference at Phase 35 time. Callout this as `(Phase 39)` in the text.

    **Mermaid rendering note:** The mermaid block will be verified manually per VALIDATION §Manual-Only Verifications row 1. Grep verification only checks for the presence of the ```` ```mermaid ```` fence and the 5 mode names (COBO, BYOD, Dedicated, ZTE, AOSP) somewhere in the file.

    **Traceability:** This task implements AEPREQ-02 per ROADMAP.md Phase 35 SC2. Decisions applied: D-07, D-08, D-09, D-10, D-11. Per-task validation maps to 35-VALIDATION.md rows 35-02-01 through 35-02-06.
  </action>
  <verify>
    <automated>bash -c 'F=docs/admin-setup-android/00-overview.md && test -f "$F" && grep -q "^\`\`\`mermaid" "$F" && [ "$(grep -cE "COBO|BYOD|Dedicated|ZTE|AOSP" "$F")" -ge 5 ] && grep -qE "gms-path-prerequisites|### GMS-Path Prerequisites" "$F" && grep -qE "zte-path-prerequisites|### ZTE-Path Prerequisites" "$F" && grep -qE "aosp-path-prerequisites|### AOSP-Path Prerequisites" "$F" && grep -qE "portal-navigation-note|## Portal Navigation Note" "$F" && grep -q "endpoint\.microsoft\.com" "$F" && [ "$(grep -icE "supervision|supervised" "$F" || true)" = "0" ] && echo VERIFY_OK'</automated>
  </verify>
  <acceptance_criteria>
    - 35-02-01: `grep -c "^\`\`\`mermaid" docs/admin-setup-android/00-overview.md` returns ≥ 1 (mermaid block present)
    - 35-02-02: `grep -cE "COBO|BYOD|Dedicated|ZTE|AOSP" docs/admin-setup-android/00-overview.md` returns ≥ 5 (all 5 mode names appear)
    - 35-02-03: `grep -c "gms-path-prerequisites\|GMS-Path Prerequisites\|### GMS" docs/admin-setup-android/00-overview.md` returns ≥ 1
    - 35-02-04: `grep -c "zte-path-prerequisites\|ZTE-Path Prerequisites\|### ZTE" docs/admin-setup-android/00-overview.md` returns ≥ 1
    - 35-02-05: `grep -c "aosp-path-prerequisites\|AOSP-Path Prerequisites\|### AOSP" docs/admin-setup-android/00-overview.md` returns ≥ 1
    - 35-02-06: `grep -c "portal-navigation-note\|Portal Navigation Note" docs/admin-setup-android/00-overview.md` returns ≥ 1
    - 35-all-01 (AEAUDIT-04 guard): `grep -in "supervision\|supervised" docs/admin-setup-android/00-overview.md` returns zero matches
    - 35-all-02 (frontmatter): `grep -c "^platform: Android$" docs/admin-setup-android/00-overview.md` returns 1
    - 35-all-03 (frontmatter): `last_verified` and `review_by` both present in frontmatter; `review_by` is exactly 60 days after `last_verified`
    - 35-all-05 (SafetyNet guard): `grep -in "safetynet" docs/admin-setup-android/00-overview.md` returns zero matches
    - 35-all-06 (no deferred-file links): `grep -cE "common-issues|quick-ref-l1|quick-ref-l2" docs/admin-setup-android/00-overview.md` returns 0
    - 35-all-07 (anchor integrity): `#choose-your-mode` resolves via explicit `<a id="choose-your-mode"></a>` tag OR matches a heading auto-slug; `#gms-path-prerequisites`, `#zte-path-prerequisites`, `#aosp-path-prerequisites`, `#portal-navigation-note` all resolve via their H2/H3 heading auto-slugs
    - 35-all-08 (cross-reference integrity): Links to `01-managed-google-play.md`, `02-zero-touch-portal.md`, `02-zero-touch-portal.md#step-0-reseller`, `01-managed-google-play.md#what-breaks` are permitted dangling at this task's run time but must resolve after Plans 03 and 04 complete; links to `../android-lifecycle/*.md` (Phase 34 outputs) must resolve immediately
    - Mermaid diagram contains 5 mode branches wired exactly per D-07 spec (COBO/BYOD/Dedicated → MGP; ZTE → MGP + ZT; AOSP → Phase 39 stub)
    - Four prerequisites checklists present with correct checkbox syntax
    - Portal Navigation Note references `endpoint.microsoft.com` explicitly and links to `01-managed-google-play.md#what-breaks`
    - `See Also` footer lists at minimum: `01-managed-google-play.md`, `02-zero-touch-portal.md`, `../android-lifecycle/01-android-prerequisites.md`, `../android-lifecycle/00-enrollment-overview.md`, `../_glossary-android.md`
    - Word count in body (excluding frontmatter and changelog) is between 800 and 1200 words (admin-guide target per PATTERNS §00-overview.md and v1.3 precedent)
  </acceptance_criteria>
  <done>
    File `docs/admin-setup-android/00-overview.md` exists, passes all 35-02-XX grep checks plus applicable 35-all-XX gates. Mermaid block renders a 5-branch flowchart per D-07. Four prerequisites checklists (GMS/ZTE/AOSP/Shared) present with stable anchors. Portal Navigation Note at bottom cites `endpoint.microsoft.com` and cross-links to `01-managed-google-play.md#what-breaks`. Directory `docs/admin-setup-android/` now exists (created as side effect). Word count 800–1200.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → this doc | Admin uses this as the routing artifact; a broken mermaid or mislabeled path sends them down the wrong mode guide |
| Doc → Phase 34 cross-references | Mode labels (COBO/BYOD WP/Dedicated/ZTE/AOSP) are contract-level from Phase 34 CONTEXT; label drift here causes inconsistency across Phases 36-39 |
| Doc → Wave 2 sibling docs | This overview's mermaid and prereqs lists link to `01-managed-google-play.md` and `02-zero-touch-portal.md` (authored by Plans 03 and 04); anchor drift between plans causes broken links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02-01 | Spoofing (admin follows wrong path) | Mermaid flowchart edge labels | mitigate | D-07 spec from RESEARCH.md locks edge labels: "COBO / BYOD WP / Dedicated" / "Zero-Touch Enrollment" / "AOSP"; acceptance test 35-02-02 verifies all 5 mode names present |
| T-35-02-02 | Tampering (prereqs drift from reality) | Per-path prerequisites checklists | mitigate | D-09 splits into 4 distinct checklists; executor verifies each prerequisite item resolves to a real requirement (Intune Plan 1 verified against STACK.md, MGP binding verified against Plan 03) |
| T-35-02-03 | Information Disclosure (stale URL guidance) | Portal Navigation Note | mitigate | D-10 locks `endpoint.microsoft.com` as the preferred URL; executor verifies this URL at execute time per D-27 |
| T-35-02-04 | Denial of Service (broken anchor breaks downstream links) | `#choose-your-mode`, `#gms-path-prerequisites`, `#zte-path-prerequisites`, `#aosp-path-prerequisites`, `#portal-navigation-note` | mitigate | D-11 contract + acceptance test 35-all-07 |
| T-35-02-05 | Elevation of Privilege (admin misroutes AOSP with Plan 1 when Plan 2/Suite required) | AOSP-Path Prerequisites checklist | mitigate | Checklist item explicitly notes Plan 2/Suite may be required per OEM; links to Phase 39 AOSP stub for specifics |
</threat_model>

<verification>
Overall plan verification:

1. File exists: `test -f docs/admin-setup-android/00-overview.md`
2. Directory created: `docs/admin-setup-android/` is now tracked by git
3. Mermaid + 5-mode branches present (35-02-01, 35-02-02)
4. Four prerequisites checklists present (35-02-03, 35-02-04, 35-02-05 — AOSP and GMS and ZTE checklists verified; Shared checklist not directly grep-checked but required structurally)
5. Portal Navigation Note present (35-02-06)
6. AEAUDIT-04 supervision guard passes (35-all-01)
7. Frontmatter compliant (35-all-02, 35-all-03)
8. SafetyNet guard passes (35-all-05)
9. Deferred-files guard: no links to `common-issues.md`, `quick-ref-l1.md`, `quick-ref-l2.md` (35-all-06)
10. Anchor contract satisfied (35-all-07)
11. PITFALL 11 guard: no shared-file modifications
</verification>

<success_criteria>
Plan 02 is complete when:
- [ ] `docs/admin-setup-android/00-overview.md` exists and passes all 35-02-XX grep checks
- [ ] `docs/admin-setup-android/` directory now exists (side effect)
- [ ] Mermaid flowchart with 5 mode branches wired per D-07 spec
- [ ] Four prerequisites checklists (GMS-Path, ZTE-Path, AOSP-Path, Shared) with D-11 anchors
- [ ] Portal Navigation Note at bottom cites `endpoint.microsoft.com` and cross-links to `01-managed-google-play.md#what-breaks`
- [ ] See Also footer lists the required Phase 34 cross-references plus Phase 35 siblings
- [ ] Zero "supervision" as Android management term; zero SafetyNet references
- [ ] Frontmatter schema valid (platform: Android, 60-day review cycle)
- [ ] Word count 800–1200 in body
- [ ] Change committed to git with conventional message referencing AEPREQ-02

This completes AEPREQ-02 and satisfies Phase 35 Success Criterion 2.
</success_criteria>

<output>
After completion, create `.planning/phases/35-android-prerequisites-mgp-zero-touch-portal/35-02-SUMMARY.md` recording:
- File created and word count
- Directory `docs/admin-setup-android/` created
- Decisions D-07 through D-11 applied (with notes on how)
- Mermaid rendering verification status (manual check per VALIDATION §Manual-Only Verifications row 1)
- Anchor contract confirmed: `#choose-your-mode`, `#gms-path-prerequisites`, `#zte-path-prerequisites`, `#aosp-path-prerequisites`, `#portal-navigation-note`
- Dangling links noted that will resolve when Plans 03 and 04 complete
</output>
