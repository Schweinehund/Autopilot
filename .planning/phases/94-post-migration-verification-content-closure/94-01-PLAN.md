---
phase: 94-post-migration-verification-content-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/macos-lifecycle/02-mdm-migration-psso.md
autonomous: true
requirements: [MIGV-01, MIGV-02, MIGV-03]

must_haves:
  truths:
    - "An operator reading guide 02 Stage 7/Stage 3 'Behind the Scenes' learns — at full confidence, with Microsoft Learn + Microsoft TechCommunity citations — that no Intune configuration beyond the assigned ADE enrollment policy is required for the profile-based enrollment resulting from OS-26 in-place migration (MIGV-01, D-01)"
    - "An operator reading guide 02 Stage 2 sees BOTH support.kandji.io and support.iru.io surfaced (plus docs.iru.com as the authoritative public docs domain), with the standing 'URL unchanged at support.kandji.io' assertion demoted to a verify-which-resolves posture, and the Delete Device Record UI path confirmed against docs.iru.com while preserving the secret-retrieval pre-flight ordering (MIGV-02, D-02)"
    - "An operator reading guide 02 Stage 7 step 4 finds the supervision-status callout framed as MEDIUM confidence, citing the Apple supervision rule (dep1d89f0bff), keeping the single `profiles status -type enrollment | grep Supervised` pilot command, and making no claim that supervision is preserved as fact nor that the PSSO Secure Enclave key survives (MIGV-03, D-03)"
    - "The Stage 7 section-provenance block last_verified/review_by are bumped to authoring day / +90d (D-04), and the out-of-coverage Stage 2 MIGV-02 edit carries a discrete inline freshness signal (D-04)"
    - "Both 'Kandji' and 'Iru' names remain present throughout the edited passages for searchability"
  artifacts:
    - path: "docs/macos-lifecycle/02-mdm-migration-psso.md"
      provides: "All three MIGV verification-content upgrades + D-04 freshness stamps, surgically edited in place"
      contains: "support.iru.io"
  key_links:
    - from: "Stage 7 'Behind the Scenes' (MIGV-01 citation)"
      to: "Stage 7 section-provenance block (line ~428 stamp)"
      via: "in-coverage B1 OS-26-gated content governed by the bumped provenance block"
      pattern: "review_by: 2026-09"
    - from: "Stage 2 step 3 URL language"
      to: "Glossary Kandji/Iru row (line ~548)"
      via: "both-URLs consistency (support.kandji.io + support.iru.io + docs.iru.com)"
      pattern: "support\\.iru\\.io"
---

<objective>
Close the three Phase-90 post-migration verification gaps in the single file
`docs/macos-lifecycle/02-mdm-migration-psso.md` by surgically upgrading existing preliminary
passages to their correct, source-verified confidence levels, and applying the D-04 hybrid
freshness stamps. This is an upgrade-and-verify phase, not net-new authoring — all three gaps
already have preliminary prose; the work is source-verified UPGRADE/CORRECTION of existing
passages.

Faithfully encodes the four locked editorial decisions from 94-CONTEXT.md:
- D-01 (MIGV-01): upgrade the Stage 7 (~line 330) + Stage 3 (~line 204) "Behind the Scenes"
  assertion in place to a stamped, Microsoft-Learn-cited, FULL-confidence statement. No new
  readiness-checklist sidebar. L2 #30 is NOT edited (verified answer does not change triage).
- D-02 (MIGV-02): verified-or-honest-hedge. Surface BOTH support.kandji.io and support.iru.io
  plus docs.iru.com; demote the "URL unchanged" assertion; confirm the delete-path against
  docs.iru.com; preserve the secret-retrieval pre-flight ordering. Keep both Kandji and Iru names.
- D-03 (MIGV-03): refine the existing Stage 7 step-4 MEDIUM supervision callout in place; cite the
  Apple supervision rule; keep the single `profiles status` pilot command; do not upgrade to HIGH;
  no claim the PSSO Secure Enclave key survives.
- D-04: HYBRID stamps. Bump the Stage 7 provenance block for the in-coverage MIGV-01 + MIGV-03
  edits; add a discrete inline freshness signal on the out-of-coverage Stage 2 MIGV-02 edit.

Purpose: An operator finds every post-migration verification claim either source-verified (MIGV-01,
MIGV-02) or honestly confidence-labelled (MIGV-03), with machine-recognizable freshness stamps for
the 90-day review cycle.
Output: A surgically edited `docs/macos-lifecycle/02-mdm-migration-psso.md`. Zero new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/94-post-migration-verification-content-closure/94-CONTEXT.md
@.planning/phases/94-post-migration-verification-content-closure/94-RESEARCH.md
@docs/macos-lifecycle/02-mdm-migration-psso.md
@docs/admin-setup-macos/07-platform-sso-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MIGV-01 citation upgrade + MIGV-03 supervision callout refinement + D-04 Stage 7 provenance-block stamp bump</name>
  <files>docs/macos-lifecycle/02-mdm-migration-psso.md</files>
  <read_first>
    - docs/macos-lifecycle/02-mdm-migration-psso.md (the target file — read the current Stage 3 "Behind the Scenes" at line 204, Stage 7 step 4 + note at lines 324-326, Stage 7 "Behind the Scenes" at line 330, and the Stage 7 section-provenance block at line 428 before editing)
    - .planning/phases/94-post-migration-verification-content-closure/94-RESEARCH.md (Gate 1 findings + the four CITED Microsoft sources for MIGV-01; Gate 3 findings + the Apple dep1d89f0bff citation for MIGV-03; the "Target File Line Map" Edit 1 / Edit 1b / Edit 3 / Edit 4 rows; the authoring-day re-verification steps for Gate 1 and Gate 3; Pitfalls 1, 2, 4)
    - .planning/phases/94-post-migration-verification-content-closure/94-CONTEXT.md (D-01, D-03, D-04 locked framing)
    - docs/admin-setup-macos/07-platform-sso-setup.md (the line-156 section-provenance stamp precedent: `_Section provenance — last_verified: YYYY-MM-DD / review_by: YYYY-MM-DD. ..._`)
  </read_first>
  <action>
    Step A — Authoring-day re-verification (MUST run on the real authoring day before stamping MIGV-01 at full confidence). Re-confirm against current Microsoft Learn that Intune exposes no separate "profile-based enrollment configuration mode" beyond the ADE enrollment policy: fetch learn.microsoft.com/en-us/intune/device-enrollment/apple/setup-automated-macos and search for "profile-based" (confirm no separate configuration object), and confirm the Microsoft TechCommunity blog at techcommunity.microsoft.com/blog/intunecustomersuccess/apple-making-device-migration-to-microsoft-intune-easy-with-upcoming-os-26-relea/4439895 still frames migration as "standard Intune ADE guidance ... enrollment profile ... manually assign it to the devices via device serial number." Also re-confirm the Apple supervision rule at support.apple.com/en-gw/guide/deployment/dep1d89f0bff/web still lists "profile-based Device Enrollment" among the macOS-11+ supervision triggers. If any source materially diverges from 94-RESEARCH.md Gate 1 / Gate 3, do NOT stamp MIGV-01 at full confidence and surface the divergence instead of proceeding.

    Step B — MIGV-01 Stage 7 "Behind the Scenes" (line 330, the first bullet beginning "The Apple-described mechanism for OS-26 migration is..."). Keep the existing factual assertion ("no separate 'profile-based enrollment' configuration mode is required in Intune ... The existing ADE enrollment policy ... automatically handles the migrated device's re-enrollment") and add inline source attribution promoting it to a cited full-confidence statement: cite Microsoft Learn (learn.microsoft.com/en-us/intune/device-enrollment/apple/setup-automated-macos and .../manage-devices-tokens-macos) and the Microsoft TechCommunity Intune blog. Do NOT add a separate readiness-checklist sidebar (D-01 rejects 1B/1C). Keep prose minimal — this is the primary instance.

    Step C — MIGV-01 Stage 3 "Behind the Scenes" (line 204, the third bullet beginning "From Intune's perspective, the migrated device re-enrolls as a fresh enrollment ... No separate 'profile-based enrollment' configuration is required..."). Add a lighter supporting citation matching Stage 7's upgraded form (this is the lighter-form instance per the line map). Keep prose minimal.

    Step D — MIGV-03 Stage 7 step 4 supervision callout (lines 324-326). Refine in place, preserving the locked MEDIUM framing. Keep the literal phrase "MEDIUM confidence" in the step-4 heading/text. Add an explicit citation to the Apple supervision rule (support.apple.com/en-gw/guide/deployment/dep1d89f0bff/web — "Mac computers are also supervised if they: Have macOS 11 or later and enroll ... profile-based Device Enrollment"). Keep the existing single pilot command `profiles status -type enrollment | grep Supervised` in the note UNCHANGED — do NOT add a `profiles list` before/after baseline (D-03). Do NOT upgrade to a flat assertion; the callout must continue to read as "supervision should be re-granted" / "expected", never "supervision is preserved" as established fact (Pitfall 2/4). Make NO claim that the PSSO Secure Enclave key survives migration (that remains correctly handled in Stage 9, untouched).

    Step E — D-04 Stage 7 section-provenance block (line 428). Bump `last_verified` from 2026-06-24 to the authoring day, and `review_by` from 2026-09-24 to authoring day + 90 days (for a 2026-06-26 authoring run: last_verified 2026-06-26 / review_by 2026-09-24 — keep the existing +90d convention the block already uses). Preserve the existing provenance prose including the supervision-status re-confirm instruction. This stamp covers BOTH the MIGV-01 (Stage 3 + Stage 7) and MIGV-03 (Stage 7 step 4) in-coverage edits, which all sit inside the block's OS-26-gated B1 content scope.

    Do NOT edit docs/l2-runbooks/30-macos-mdm-migration-failure.md (research Gate 1: the MIGV-01 answer does not change migration-failure triage). Do NOT touch any nav-hub file, guide 01, or any passage outside the four edit targets above. Cite decisions D-01, D-03, D-04 in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -c "MEDIUM confidence" docs/macos-lifecycle/02-mdm-migration-psso.md` is unchanged or higher (the MIGV-03 step-4 callout still contains the literal string "MEDIUM confidence"); ties to ROADMAP success criterion 3.
    - The MIGV-03 callout contains NO new flat-assertion phrasing such as "supervision is preserved" stated as fact: `grep -ni "supervision is preserved" docs/macos-lifecycle/02-mdm-migration-psso.md` returns no line that asserts it as established fact (any occurrence must be inside a hedge/"not preserved" Watch-Out context).
    - The single pilot command survives and no second baseline command is added: `grep -c "profiles status -type enrollment | grep Supervised" docs/macos-lifecycle/02-mdm-migration-psso.md` returns 1, and no new `profiles list` before/after baseline is added for supervision (`grep -c "profiles list" docs/macos-lifecycle/02-mdm-migration-psso.md` returns 0). Ties to ROADMAP success criterion 3(b).
    - The MIGV-01 Stage 7 "Behind the Scenes" bullet now contains a Microsoft Learn citation: `grep -c "learn.microsoft.com" docs/macos-lifecycle/02-mdm-migration-psso.md` returns >= 1. Ties to ROADMAP success criterion 1.
    - The MIGV-03 callout cites the Apple supervision rule: the file contains the string "dep1d89f0bff". Ties to ROADMAP success criterion 3(a).
    - The Stage 7 section-provenance block is stamped to authoring day: `grep -c "review_by: 2026-09-24" docs/macos-lifecycle/02-mdm-migration-psso.md` returns >= 1 AND `grep -c "last_verified: 2026-06-24" docs/macos-lifecycle/02-mdm-migration-psso.md` reflects the bump (the Stage 7 provenance occurrence updated to the authoring day; verify the authoring-day date is present in the provenance block). Ties to ROADMAP success criteria 1 and 3 (stamp requirement).
    - No claim of PSSO Secure Enclave key survival introduced: the Stage 9 "permanently destroyed when the device unenrolls" language at lines ~392 is byte-unchanged.
    - docs/l2-runbooks/30-macos-mdm-migration-failure.md is byte-unchanged (`git diff --stat` shows no change to that file).
    - No file other than docs/macos-lifecycle/02-mdm-migration-psso.md is modified.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && git diff --name-only | grep -qx "docs/macos-lifecycle/02-mdm-migration-psso.md" && ! git diff --name-only | grep -qx "docs/l2-runbooks/30-macos-mdm-migration-failure.md" && grep -c "profiles status -type enrollment | grep Supervised" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "MEDIUM confidence" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "dep1d89f0bff" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "learn.microsoft.com" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "review_by: 2026-09-24" docs/macos-lifecycle/02-mdm-migration-psso.md && [ "$(grep -c 'profiles list' docs/macos-lifecycle/02-mdm-migration-psso.md)" -eq 0 ]</automated>
  </verify>
  <done>MIGV-01 is documented at full confidence with Microsoft Learn + TechCommunity citations in both Stage 7 and Stage 3 "Behind the Scenes"; MIGV-03 step-4 callout is refined in place at MEDIUM confidence with the Apple dep1d89f0bff citation and the single unchanged pilot command; the Stage 7 provenance block is stamped to authoring day / +90d covering both in-coverage edits; L2 #30 and all out-of-scope passages are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: MIGV-02 Iru/Kandji URL + delete-path verification edits (Stage 2 steps 3/4/5, B2 Stage 3 consistency, glossary) + D-04 Stage 2 inline freshness signal</name>
  <files>docs/macos-lifecycle/02-mdm-migration-psso.md</files>
  <read_first>
    - docs/macos-lifecycle/02-mdm-migration-psso.md (read current Stage 2 step 3 at line 148, step 4 note at line 152, step 5 + conceptual steps at lines 154-161, B2 Stage 3 at lines 477-489, and the Glossary Kandji/Iru row at line 548 before editing)
    - .planning/phases/94-post-migration-verification-content-closure/94-RESEARCH.md (Gate 2 Findings A-E: portal URL resolution results, the confirmed docs.iru.com delete UI path, the four permanently-destroyed secrets, the documentation-gap finding on the secret-retrieval pre-flight, and Finding E on the URL assertions to correct; the "Target File Line Map" Edit 2a/2b/2c/2d/2e rows; the Gate 2 authoring-day re-verification curl steps; Pitfall 3; Security Domain note)
    - .planning/phases/94-post-migration-verification-content-closure/94-CONTEXT.md (D-02 locked framing + D-04 discretionary Stage 2 inline-stamp note)
  </read_first>
  <action>
    Step A — Authoring-day re-verification (MUST run on the real authoring day; record the date + results in the Stage 2 inline freshness signal). Confirm the three portal URLs still resolve and the delete-path source is still public: fetch/confirm docs.iru.com deletion article (docs.iru.com/en/endpoint/devices/device-record-management/deleting-a-device-record-and-uninstalling-iru-endpoint, expect reachable/public), support.kandji.io KB article (support.kandji.io/kb/deleting-a-device-record-and-uninstalling-kandji, expect reachable, Iru-branded), and support.iru.io base (expect resolves but login-gated SPA). If docs.iru.com or support.kandji.io no longer resolves, fall back to the honest-hedge form and record the failure in the inline signal rather than asserting an unverified path (Pitfall 3 — never assert a support.iru.io console path as if login-verified).

    Step B — Edit 2a, Stage 2 step 3 (line 148). Replace the parenthetical "support portal URL unchanged at support.kandji.io" with both-URLs-acknowledged language per D-02: surface support.kandji.io (known-documented anchor, now showing "Kandji is now Iru" branding), support.iru.io (rebrand-expected target, resolves but login-gated on the verification date), and docs.iru.com (the new authoritative, publicly-accessible Iru docs domain). Demote the flat "unchanged" assertion to a "verify which resolves on authoring day" posture. Keep BOTH "Kandji" and "Iru" names present.

    Step C — Edit 2b, Stage 2 step 4 note (line 152). Update the note to reflect the verification-attempt findings: support.kandji.io now displays Iru branding ("Kandji is now Iru"); docs.iru.com is the new authoritative public docs domain where the device-deletion UI path is confirmed; support.iru.io resolves but is a login-gated SPA whose console content is not verifiable without operator login. Preserve the conceptual-action hedge.

    Step D — Edit 2c, Stage 2 step 5 (lines 154-161). Keep the conceptual-steps form but cite docs.iru.com as the verified source for the UI path, and confirm the exact path against it: Navigate to Devices > select the device > Device Action Menu > Delete Device Record > type "DELETE" in the text field > confirm. Add the confirmed "type DELETE in the text field" confirmation step. Preserve and strengthen the existing secret-retrieval pre-flight ordering (retrieve FileVault key + Activation Lock bypass code in steps 3/4 BEFORE step 5's Delete Device Record) and note that the vendor's own docs (docs.iru.com / support.kandji.io) do NOT include a "retrieve secrets first" pre-flight — guide 02 correctly fills that gap (Gate 2 Finding D; Security Domain note). Keep the four permanently-destroyed secrets accurate (Device lock PIN, Recovery password, FileVault Recovery Key, Activation Lock Bypass Code). Add the D-04 discrete inline freshness signal here (discretionary hygiene — planner's call: use a lighter date-of-attempt note recording the authoring-day verification date + what resolved publicly via docs.iru.com vs. what remained login-gated at support.iru.io; this is vendor-sourced and is intentionally separate from the Apple-sourced Stage 7 provenance block).

    Step E — Edit 2e, Glossary Kandji/Iru row (line 548). Replace "support portal remains at support.kandji.io" with both-URLs language consistent with Step B: support.kandji.io (Iru-branded KB) + support.iru.io (login-gated SPA on the verification date) + docs.iru.com (authoritative public docs). Keep both Kandji and Iru names.

    Step F — Edit 2d, B2 Stage 3 (lines 477-489). Consistency check only: confirm B2 Stage 3 has no standalone URL assertion to correct (it cross-references Stage 2 for full Kandji/Iru detail at line 475/B2 Stage 2). Make no substantive URL edit here unless a standalone support.kandji.io assertion is present; if so, align it with the Step B both-URLs language. Keep the edit minimal and byte-disciplined.

    Do NOT fabricate a support.iru.io console navigation path as if login-verified (Pitfall 3). Cite decision D-02 (and D-04 for the inline stamp) in the SUMMARY.
  </action>
  <acceptance_criteria>
    - Both rebrand URLs are surfaced: the file contains "support.iru.io" AND "docs.iru.com" AND retains "support.kandji.io" (`grep -c "support.iru.io"` >= 1, `grep -c "docs.iru.com"` >= 1, `grep -c "support.kandji.io"` >= 1). Ties to ROADMAP success criterion 2.
    - The flat "URL unchanged"/"remains at support.kandji.io" assertions are demoted: `grep -ni "portal URL unchanged at support.kandji.io" docs/macos-lifecycle/02-mdm-migration-psso.md` returns 0, and `grep -ni "support portal remains at support.kandji.io" docs/macos-lifecycle/02-mdm-migration-psso.md` returns 0. Ties to ROADMAP success criterion 2.
    - Both brand names remain present for searchability: the file still contains both "Kandji" and "Iru" (`grep -c "Kandji"` >= 1 and `grep -c "Iru"` >= 1). Ties to ROADMAP success criterion 2 (both-names requirement).
    - The Delete Device Record confirmation step is present: the file contains the confirmed "DELETE" type-to-confirm step near Stage 2 step 5 (the Stage 2 deletion steps reference typing "DELETE" to confirm).
    - The secret-retrieval pre-flight ordering is preserved (retrieval before deletion): the "Retrieve ALL secrets from Kandji/Iru BEFORE performing Delete Device Record" / "permanently destroyed" language at lines ~134 and the Stage 2 step ordering (FileVault + Activation Lock retrieval as steps 3/4 ahead of Delete Device Record as step 5) remains intact. Ties to ROADMAP success criterion 2 (pre-flight still required).
    - A discrete Stage 2 inline freshness signal is present recording the authoring-day verification attempt (the Stage 2 step-5 area contains a date-of-attempt / verification note distinct from the Stage 7 provenance block). Ties to D-04.
    - No support.iru.io login-gated console path is asserted as verified: any reference to support.iru.io is accompanied by login-gated / verify-on-authoring-day hedging (Pitfall 3).
    - No file other than docs/macos-lifecycle/02-mdm-migration-psso.md is modified.
  </acceptance_criteria>
  <verify>
    <automated>cd /d/claude/Autopilot && git diff --name-only | grep -qx "docs/macos-lifecycle/02-mdm-migration-psso.md" && grep -q "support.iru.io" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "docs.iru.com" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "support.kandji.io" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "Kandji" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "Iru" docs/macos-lifecycle/02-mdm-migration-psso.md && [ "$(grep -ci 'portal URL unchanged at support.kandji.io' docs/macos-lifecycle/02-mdm-migration-psso.md)" -eq 0 ] && [ "$(grep -ci 'support portal remains at support.kandji.io' docs/macos-lifecycle/02-mdm-migration-psso.md)" -eq 0 ] && grep -q "BEFORE performing Delete Device Record" docs/macos-lifecycle/02-mdm-migration-psso.md</automated>
  </verify>
  <done>Stage 2 (steps 3/4/5), the glossary Kandji/Iru row, and (consistency-only) B2 Stage 3 surface both support.kandji.io and support.iru.io plus docs.iru.com, demote the flat "URL unchanged" assertions to a verify-which-resolves posture, confirm the docs.iru.com Delete Device Record UI path with the type-"DELETE" confirmation, preserve the secret-retrieval pre-flight ordering, keep both Kandji and Iru names, and carry a discrete Stage 2 inline freshness signal — all without asserting an unverified support.iru.io console path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none applicable) | This phase edits a single Markdown documentation file. It introduces no executable code, no user input, no authentication, no network handling, no data processing, and no new dependencies. There is no trust boundary crossed by this work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | (n/a) | docs/macos-lifecycle/02-mdm-migration-psso.md (prose edits only) | accept | No attack surface: a documentation-only phase that edits one Markdown file introduces no inputs, auth, network, crypto, or data-handling surface. Per 94-RESEARCH.md Security Domain (V2/V4/V5/V6 all "No"), there are no applicable STRIDE threats. The single security-relevant consideration is operator-safety content quality (the Stage 2 surface where operators perform irreversible secret-destruction), which is addressed by the D-02 honest-hedge posture and the preserved secret-retrieval pre-flight ordering in Task 2 — not by a code/security control. |
| T-94-SC | Tampering | package installs (npm/pip/cargo) | accept | No package installs occur in this phase (documentation-only; no build, no dependencies). The supply-chain checkpoint is not applicable. |

> This phase introduces no code and no attack surface. The honest content for a docs phase is to state that there are no applicable threats; no threats are manufactured.
</threat_model>

<verification>
Phase-level checks (all assertions target the single file docs/macos-lifecycle/02-mdm-migration-psso.md unless noted):

1. MIGV-01 (success criterion 1): file contains a Microsoft Learn citation ("learn.microsoft.com") in the Stage 7/Stage 3 "Behind the Scenes" passages; the Stage 7 provenance block carries last_verified = authoring day and review_by = +90d.
2. MIGV-02 (success criterion 2): file surfaces support.kandji.io + support.iru.io + docs.iru.com; flat "URL unchanged" / "remains at support.kandji.io" assertions removed; both Kandji and Iru names present; secret-retrieval pre-flight ordering preserved; Stage 2 inline freshness signal present.
3. MIGV-03 (success criterion 3): Stage 7 step-4 callout retains "MEDIUM confidence"; cites Apple dep1d89f0bff; keeps the single `profiles status -type enrollment | grep Supervised` command (no `profiles list` baseline added); makes no flat "supervision is preserved" assertion and no PSSO Secure-Enclave-key-survival claim.
4. Scope discipline: docs/l2-runbooks/30-macos-mdm-migration-failure.md byte-unchanged; no nav-hub file, guide 01, or out-of-mapped-passage edited; no new files created.
</verification>

<success_criteria>
- ROADMAP success criterion 1 met: MIGV-01 documented at full confidence with current Microsoft Learn citation, stamped via the Stage 7 provenance block; L2 #30 untouched (answer does not change triage).
- ROADMAP success criterion 2 met: Kandji/Iru source-side steps reflect the verified current Iru console delete UI path (docs.iru.com), both URLs surfaced, secret-retrieval pre-flight confirmed still required, both Kandji and Iru names present.
- ROADMAP success criterion 3 met: explicit MEDIUM-confidence supervision callout with Apple-cited reasoning, single pilot command, no flat assertion, no PSSO key-survival claim; carries freshness stamps.
- D-01..D-04 faithfully encoded; no scope creep (no new files, no nav edits, no L2 #30 edit, no `profiles list` baseline, no fabricated support.iru.io console path).
</success_criteria>

<output>
Create `.planning/phases/94-post-migration-verification-content-closure/94-01-SUMMARY.md` when done.
</output>
