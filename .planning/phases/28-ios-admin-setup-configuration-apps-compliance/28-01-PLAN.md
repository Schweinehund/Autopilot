---
phase: 28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-ios/04-configuration-profiles.md
  - docs/admin-setup-ios/00-overview.md
autonomous: true
requirements: [ACFG-01]
requirements_addressed: [ACFG-01]
cross_ai: false

must_haves:
  truths:
    - "An admin reading 04-configuration-profiles.md can identify which of the six covered payload types (Wi-Fi, VPN, Email, Certificates, Device Restrictions, Home Screen Layout) contain supervised-only settings"
    - "Every device-restrictions category section (Camera, iCloud, App Store, Safari, Siri, Kiosk, Password, Keyboard, Cloud and Storage, Connected Devices, Game Center/Doc Viewing, General Block) contains a table with a 'Supervised-only?' column and an immediately-following `> 🔒 **Supervised only:**` blockquote callout"
    - "Every 🔒 callout in the guide links back to ../ios-lifecycle/00-enrollment-overview.md#supervision — no callout uses a different target"
    - "Nine named high-impact supervised-only settings (Block App Store, Block removing apps, Allow activation Lock, Block configuration profile changes, Block modification of account settings, Block iCloud backup, Block AirDrop, Block Camera / Block FaceTime, Block pairing with non-Configurator hosts) receive a full subsection with a 'What breaks if misconfigured' callout"
    - "The Home Screen Layout section includes its own 🔒 supervised-only callout since the entire payload type is supervised-only"
    - "docs/admin-setup-ios/00-overview.md links to the three new guides (04, 05, 06) in the setup sequence list and the Mermaid diagram extends to include them"
    - "The guide contains a Configuration-Caused Failures table using the 'iOS L1 runbooks (Phase 30)' placeholder in the Runbook column"
  artifacts:
    - path: "docs/admin-setup-ios/04-configuration-profiles.md"
      provides: "iOS/iPadOS configuration profiles admin setup guide"
      min_lines: 300
      contains: "🔒 **Supervised only:**"
    - path: "docs/admin-setup-ios/00-overview.md"
      provides: "Admin setup overview with links to guides 04/05/06 and extended Mermaid diagram"
      contains: "04-configuration-profiles.md"
  key_links:
    - from: "docs/admin-setup-ios/04-configuration-profiles.md"
      to: "docs/ios-lifecycle/00-enrollment-overview.md#supervision"
      via: "🔒 supervised-only callout link"
      pattern: "../ios-lifecycle/00-enrollment-overview.md#supervision"
    - from: "docs/admin-setup-ios/00-overview.md"
      to: "docs/admin-setup-ios/04-configuration-profiles.md"
      via: "setup sequence list entry"
      pattern: "04-configuration-profiles.md"
    - from: "docs/admin-setup-ios/00-overview.md"
      to: "docs/admin-setup-ios/05-app-deployment.md"
      via: "setup sequence list entry"
      pattern: "05-app-deployment.md"
    - from: "docs/admin-setup-ios/00-overview.md"
      to: "docs/admin-setup-ios/06-compliance-policy.md"
      via: "setup sequence list entry"
      pattern: "06-compliance-policy.md"
---

<objective>
Deliver the iOS/iPadOS Configuration Profiles admin setup guide (`docs/admin-setup-ios/04-configuration-profiles.md`) covering Wi-Fi, VPN, Email, Certificates, Device Restrictions (with category-level 🔒 supervised-only callouts), and Home Screen Layout. Extend `docs/admin-setup-ios/00-overview.md` with links to the three new Phase 28 guides and an updated Mermaid diagram.

Purpose: Satisfy ACFG-01 — an admin must be able to identify which documented configuration-profile settings require supervised mode without leaving the guide, because every supervised-only setting category carries the 🔒 callout linking to the enrollment overview supervision anchor (per D-01, D-02, D-23, and the user's Q1 resolution adopting category-level granularity).

Output:
- NEW: `docs/admin-setup-ios/04-configuration-profiles.md` (target 300-500 lines per D-04)
- UPDATED: `docs/admin-setup-ios/00-overview.md` (Mermaid extension per D-20 + setup sequence list entries for 04/05/06)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-CONTEXT.md
@.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-RESEARCH.md
@.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-VALIDATION.md

<!-- Structural templates (executor MUST read before writing) -->
@docs/admin-setup-macos/03-configuration-profiles.md
@docs/admin-setup-ios/03-ade-enrollment-profile.md
@docs/admin-setup-ios/00-overview.md
@docs/_templates/admin-template-ios.md

<interfaces>
<!-- Frontmatter schema (from admin-template-ios.md) -->
```yaml
---
last_verified: 2026-04-16
review_by: 2026-07-15
applies_to: ADE
audience: admin
platform: iOS
---
```

<!-- Platform gate (4 lines, placed immediately after frontmatter) -->
```markdown
> **Platform gate:** This guide covers iOS/iPadOS ADE configuration via Apple Business Manager and Intune.
> For macOS ADE setup, see [macOS Configuration Profiles](../admin-setup-macos/03-configuration-profiles.md).
> For iOS/iPadOS enrollment terminology, see the [Apple Provisioning Glossary](../_glossary-macos.md).
> Portal navigation may vary by Intune admin center version. See [Overview](00-overview.md#portal-navigation-note) for details.
```

<!-- 🔒 Supervised-only callout — LOCKED FORMAT (Phase 27 D-01 / Phase 28 D-02) -->
```markdown
> 🔒 **Supervised only:** [setting or feature name] requires supervised mode. [1-2 sentence consequence for unsupervised devices.] See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
```

<!-- "What breaks if misconfigured" callout (from admin-template-ios.md) -->
```markdown
> **What breaks if misconfigured:** [Consequence.] Symptom appears in: [portal or device where the symptom manifests].
```

<!-- Category-level pattern (per user's Q1 resolution — the authoritative interpretation) -->
```markdown
## [Category Name]

| Setting | Supervised-only? | What it does |
|---------|:----------------:|---|
| [Setting A] | 🔒 | [Short description] |
| [Setting B] | — | [Short description] |

> 🔒 **Supervised only:** [Category descriptor] ([named settings in this category]) require supervised mode. On unsupervised devices these settings are ignored silently. See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
```

<!-- Configuration-Caused Failures table placeholder (Phase 27 D-22 inheritance) -->
```markdown
| [Misconfig description] | Intune | [Symptom] | iOS L1 runbooks (Phase 30) |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 04-configuration-profiles.md with frontmatter, platform gate, Prerequisites, Settings Catalog note, Profile Delivery Channel, and the six non-restrictions payload sections (Wi-Fi, VPN, Email, Certificates, Home Screen Layout)</name>
  <files>docs/admin-setup-ios/04-configuration-profiles.md</files>
  <read_first>
    - docs/admin-setup-macos/03-configuration-profiles.md (PRIMARY structural template — mirror section order, headings, callout placement)
    - docs/admin-setup-ios/03-ade-enrollment-profile.md (Phase 27 reference for 🔒 callout format, Key Concepts pattern, iOS-specific platform gate)
    - docs/_templates/admin-template-ios.md (frontmatter schema, callout formats, iOS-no-CLI rule)
    - docs/admin-setup-ios/00-overview.md (to understand the cross-link target expected by downstream tasks)
    - .planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-RESEARCH.md (lines 258-296 for 15 key settings selection and Home Screen Layout callout wording; lines 535-547 for Wi-Fi/VPN/Email "what breaks" content)
  </read_first>
  <action>
Create `docs/admin-setup-ios/04-configuration-profiles.md` with the following exact structure. Use the iOS admin template verbatim for frontmatter and platform gate (no deviations):

**1. Frontmatter** (exactly these fields in this order):
```yaml
---
last_verified: 2026-04-16
review_by: 2026-07-15
applies_to: ADE
audience: admin
platform: iOS
---
```

**2. Platform gate** (4-line blockquote, copy verbatim):
```markdown
> **Platform gate:** This guide covers iOS/iPadOS ADE configuration via Apple Business Manager and Intune.
> For macOS ADE setup, see [macOS Configuration Profiles](../admin-setup-macos/03-configuration-profiles.md).
> For iOS/iPadOS enrollment terminology, see the [Apple Provisioning Glossary](../_glossary-macos.md).
> Portal navigation may vary by Intune admin center version. See [Overview](00-overview.md#portal-navigation-note) for details.
```

**3. H1 title**: `# iOS/iPadOS Configuration Profiles`

**4. Intro paragraph** (1 paragraph, 2-3 sentences): State that configuration profiles are delivered via MDM/APNs channel; distinguish config profiles (enforce) from compliance (detect); note that iOS adds the supervision axis that macOS lacks, which is why many device-restriction settings require supervised mode. Include a sentence: "Settings that require supervised mode are explicitly marked with a 🔒 callout; on unsupervised devices they are ignored silently."

**5. Settings Catalog callout** (mirror the macOS guide's top blockquote, adapted for iOS):
```markdown
> **Settings Catalog recommended:** Create new iOS/iPadOS configuration profiles via the Settings Catalog in Intune (Devices > Manage devices > Configuration > Create > New policy > iOS/iPadOS > Settings catalog). Legacy Templates > Device restrictions remains available for settings not yet in Settings Catalog (notably Home Screen Layout) — use Templates only when a setting is unavailable in Settings Catalog.
```

**6. `## Prerequisites`** (bulleted list, 4 bullets):
- Intune Administrator role
- Devices enrolled via ADE (see [ADE Enrollment Profile](03-ade-enrollment-profile.md))
- Understanding that configuration profiles enforce settings; compliance policies detect non-compliance (see [Compliance Policies](06-compliance-policy.md))
- Understanding of supervised vs unsupervised management scope — see [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision)

**7. `## Profile Delivery Channel`** (1 paragraph, mirror macOS wording):
State that configuration profiles are delivered via MDM channel over APNs (Apple Push Notification service). If a profile fails to deliver, troubleshoot APNs connectivity and the ADE/enrollment state, not an agent (iOS has no Intune Management Extension agent equivalent).

**8. `## Wi-Fi`** section:
- Sub-heading `#### In Intune admin center`
- Navigation: `**Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **iOS/iPadOS** > **Settings catalog** > **Networking** > **Wi-Fi**`
- Key settings bullets: SSID (case-sensitive), Security type (WPA2/WPA3-Enterprise), Authentication (EAP type, identity certificate for 802.1X), Auto-join
- Two `> **What breaks if misconfigured:**` callouts: (a) SSID case mismatch → device cannot find network (symptom on device); (b) Missing SCEP/PKCS certificate profile → 802.1X authentication fails (symptom on device AND Intune admin center shows profile Error status).
- NO 🔒 callout (Wi-Fi is not supervised-only).

**9. `## VPN`** section:
- `#### In Intune admin center` sub-heading
- Navigation: `**Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **iOS/iPadOS** > **Settings catalog** > **Networking** > **VPN**`
- Key settings bullets: Connection type (IKEv2, SSL-VPN, per-app VPN, third-party such as Cisco AnyConnect / Global Protect), Server address (FQDN/IP), Authentication (certificate / username-password / machine cert), Per-app VPN (tie to specific managed apps via bundle ID)
- One `> **What breaks if misconfigured:**` callout: wrong VPN type or server address → connection fails with generic error (symptom on device).
- Add a brief paragraph: "Per-app VPN requires the app to be deployed as a managed app (see [App Deployment](05-app-deployment.md))."
- NO 🔒 callout.

**10. `## Email`** section:
- `#### In Intune admin center` sub-heading
- Navigation: `**Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **iOS/iPadOS** > **Settings catalog** > **Accounts** > **Exchange ActiveSync**` (note iOS uses EAS, not generic IMAP/POP for managed accounts)
- Key settings bullets: Exchange server host, Authentication method (Modern authentication / Basic — recommend Modern), S/MIME signing and encryption certificates (reference certificate profile), Allow Mail Drop, Block move messages to other accounts
- One `> **What breaks if misconfigured:**` callout: Basic auth selected when tenant enforces Modern authentication → Mail app shows "Cannot Get Mail" (symptom on device).
- NO 🔒 callout.

**11. `## Certificates`** section:
- Sub-heading `#### In Intune admin center`
- Navigation: `**Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **iOS/iPadOS** > **Templates** > **[SCEP certificate | PKCS certificate | Trusted certificate]**`
- Explain three profile types briefly: Trusted certificate (root CA .cer), SCEP (dynamic certificate issuance requires NDES + Intune Certificate Connector), PKCS (imported cert from internal CA)
- State explicitly: "Certificate profiles are NOT supervised-only — they deploy to all enrolled iOS/iPadOS devices regardless of supervision state."
- One `> **What breaks if misconfigured:**` callout: SCEP profile without Trusted root deployed first → certificate chain invalid; 802.1X Wi-Fi fails (symptom: device AND Intune admin center profile Error status).
- NO 🔒 callout.

**12. `## Home Screen Layout`** section:
- Sub-heading `#### In Intune admin center`
- Navigation: `**Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **iOS/iPadOS** > **Templates** > **Device features** > **Home screen layout**` (explicit: this is Templates, NOT Settings Catalog — per research line 285)
- Key settings bullets: Dock apps (up to 6), Pages with icon grid, Folders (supervised only; iOS 9.3+), Referenced apps by bundle ID (works for App Store, VPP, and LOB apps)
- Place this 🔒 callout IMMEDIATELY after the key settings (Home Screen Layout is entirely supervised-only):
```markdown
> 🔒 **Supervised only:** Home screen layout requires supervised mode. On unsupervised devices this profile type is unavailable and home screen organization remains fully user-controlled. See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
```
- One `> **What breaks if misconfigured:**` callout: Referenced bundle ID for an uninstalled or mistyped app → blank icons appear or placements silently drop (symptom on device).

Stop before the `## Device Restrictions` section — Task 2 writes that section. Do NOT yet write Verification, Configuration-Caused Failures, or See Also — Task 3 writes those. The file after Task 1 ends immediately after the Home Screen Layout section.
  </action>
  <verify>
    <automated>bash -c 'test -f docs/admin-setup-ios/04-configuration-profiles.md && grep -c "^platform: iOS" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^## Wi-Fi" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^## VPN" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^## Email" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^## Certificates" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^## Home Screen Layout" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^## Prerequisites" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -F "../ios-lifecycle/00-enrollment-overview.md#supervision" docs/admin-setup-ios/04-configuration-profiles.md && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/admin-setup-ios/04-configuration-profiles.md` exits 0
    - `grep -c "^platform: iOS" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "^audience: admin" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "^applies_to: ADE" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "^## Prerequisites$" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "^## Profile Delivery Channel$" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "^## Wi-Fi$" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "^## VPN$" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "^## Email$" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "^## Certificates$" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "^## Home Screen Layout$" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "🔒 \*\*Supervised only:\*\* Home screen layout" docs/admin-setup-ios/04-configuration-profiles.md` returns at least `1`
    - `grep -c "../ios-lifecycle/00-enrollment-overview.md#supervision" docs/admin-setup-ios/04-configuration-profiles.md` returns at least `1` (the Home Screen Layout callout + Prerequisites link)
    - `grep -c "> \*\*What breaks if misconfigured:\*\*" docs/admin-setup-ios/04-configuration-profiles.md` returns at least `5` (Wi-Fi ×2, VPN ×1, Email ×1, Certificates ×1, Home Screen ×1 = 6 minimum)
    - `grep -c "Settings catalog" docs/admin-setup-ios/04-configuration-profiles.md` returns at least `3`
    - File line count >= 80 (check with `wc -l docs/admin-setup-ios/04-configuration-profiles.md`)
  </acceptance_criteria>
  <done>
Frontmatter, platform gate, Prerequisites, Profile Delivery Channel, and five payload sections (Wi-Fi, VPN, Email, Certificates, Home Screen Layout) are present with correct heading levels, navigation paths, key-settings bullets, "What breaks if misconfigured" callouts, and the single 🔒 callout on Home Screen Layout linking to `../ios-lifecycle/00-enrollment-overview.md#supervision`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append the `## Device Restrictions` section with category tables + category-level 🔒 callouts (12 categories) and nine high-impact full-detail subsections</name>
  <files>docs/admin-setup-ios/04-configuration-profiles.md</files>
  <read_first>
    - docs/admin-setup-ios/04-configuration-profiles.md (current state from Task 1 — append, do not overwrite)
    - .planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-RESEARCH.md (lines 128-256 for the complete supervised-only matrix by category; lines 270-281 for the 9 high-impact settings list; lines 531-547 for "what breaks" content sources)
    - .planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-CONTEXT.md (D-01, D-02, D-03, D-22 decisions)
    - docs/admin-setup-ios/03-ade-enrollment-profile.md (for the exact 🔒 callout format — copy verbatim)
  </read_first>
  <action>
Append the Device Restrictions section to `docs/admin-setup-ios/04-configuration-profiles.md`. This is the largest section and implements D-01's category-level pattern (per user's Q1 resolution).

**Start with** `## Device Restrictions` heading, followed by:
- Sub-heading `#### In Intune admin center`
- Navigation: `**Devices** > **Manage devices** > **Configuration** > **Create** > **New policy** > **iOS/iPadOS** > **Settings catalog** > **Restrictions**`
- One-paragraph intro: "Device restrictions expose the largest set of supervised-only settings in iOS/iPadOS management. Settings in this profile type fall into two groups: those available on all enrolled devices (unsupervised and supervised) and those available only on supervised devices. Each category below lists the supervised-only settings present in that category, followed by a 🔒 callout; categories marked entirely supervised-only (Autonomous Single App Mode, Kiosk, Keyboard, Password, Safari Domains) apply to no unsupervised devices."

**Then** a brief `### Software Update Note` paragraph (per D-05 research flag): State that the legacy "Defer software updates" setting in device restrictions remains supervised-only but is being deprecated by Apple. For software update enforcement, use the dedicated DDM-based path at **Devices > Apple updates > iOS/iPadOS update policies**, which works on both supervised and unsupervised devices (iOS 17.0+). Link out-of-scope: "Software update policies are not covered in this guide."

**Then write 12 category subsections**, each using this template (adapted per category):

```markdown
### [Category Name]

| Setting | Supervised-only? | What it does |
|---------|:----------------:|---|
| [Setting 1] | 🔒 | [Short description] |
| [Setting 2] | — | [Short description] |
...

> 🔒 **Supervised only:** [Category summary sentence naming the supervised settings]. On unsupervised devices these settings are ignored silently. See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
```

Use these 12 categories with the settings from RESEARCH.md lines 128-236 (at minimum list the supervised-only settings; include a few common non-supervised settings where the category has them to contrast):

1. **General** (maps to RESEARCH "General (Block)"): Include Block modification of account settings (🔒), Block modification of device name (🔒), Block Screen Time (🔒), Block configuration profile changes (🔒), Allow activation Lock (🔒), Block VPN creation (🔒), Force automatic date and time (🔒), Defer software updates (🔒 — note legacy/deprecating)
2. **App Store, Doc Viewing, Gaming**: Block App Store (🔒, iOS 13+), Block installing apps (🔒), Block automatic app downloads (🔒), Block Game Center (🔒), Block multiplayer gaming in Game Center (🔒), Block in-app purchases (—), Require iTunes Store password for all purchases (—)
3. **Built-in Apps** (Camera/FaceTime/Siri/Safari/News/Books/iMessage): Block Camera (🔒, iOS 13+), Block FaceTime (🔒, iOS 13+), Block Safari (🔒, iOS 13+), Block Safari Autofill (🔒), Block Apple News (🔒), Block Apple Books (🔒), Block iMessage (🔒), Block user-generated content in Siri (🔒), Require Siri profanity filter (🔒), Block Siri (—), Block Find My iPhone (🔒, iOS 13+)
4. **Cloud and Storage**: Block iCloud backup (🔒, iOS 13+), Block iCloud document and data sync (🔒, iOS 13+), Block iCloud Keychain sync (🔒, iOS 13+), Block iCloud Private Relay (🔒, iOS 15+), Block managed apps from storing data in iCloud (—), Force encrypted backup (—)
5. **Connected Devices**: Block AirDrop (🔒), Block pairing with Apple Watch (🔒), Block modifying Bluetooth settings (🔒), Block pairing with non-Configurator hosts (🔒), Block AirPrint (🔒), Disable NFC (🔒, iOS 14.2+), Block access to USB drive in Files app (🔒, iOS 13+), Allow USB accessories while device is locked (🔒, iOS 11.4.1+)
6. **Keyboard and Dictionary** (ENTIRE category supervised-only): Block word definition lookup (🔒), Block predictive keyboards (🔒), Block auto-correction (🔒), Block spell-check (🔒), Block keyboard shortcuts (🔒), Block dictation (🔒), Block QuickPath (🔒, iOS 13+)
7. **Autonomous Single App Mode (ASAM)** (ENTIRE category supervised-only): App name (🔒), App Bundle ID (🔒)
8. **Kiosk / App Lock** (ENTIRE category supervised-only): App to run in kiosk mode (🔒), plus a bullet noting "~20 accessibility/button-lock settings (Assistive touch, VoiceOver, Zoom, Auto lock, Ringer switch, Screen rotation, Touch, Volume buttons, etc.) all supervised-only"
9. **Password** (ENTIRE supervised-only controls): Block passcode modification (🔒), Block modification of Touch ID / Face ID (🔒, iOS 11+), Block password AutoFill (🔒), Block password proximity requests (🔒), Block password sharing (🔒), Require biometric auth for AutoFill of password or credit card (🔒, iOS 11+)
10. **Safari Domains**: Safari password domains (🔒, iOS 9.3+), Allowed Safari web domains (—), Managed Safari web domains (—)
11. **Show or hide apps** (supervised-only bulk): Bullet note: "The entire 'Show or hide apps' allowlist/blocklist capability is supervised-only"
12. **Screen Time / Restrictions additional**: Block Screen Time modification (🔒), Block user erase of content and settings (🔒), Block removing apps (🔒), Block app clips (🔒, iOS 14+)

Each category gets one `> 🔒 **Supervised only:**` blockquote immediately after its table, using the exact pattern shown. Name the category's supervised settings inline in the callout text (e.g., "Camera restrictions (Block Camera, Block FaceTime, Block Safari, Block iMessage, ...) require supervised mode").

**Then write 9 full-detail subsections** under a sub-heading `### Key Supervised-Only Settings — Full Detail` explaining:
"The nine settings below are the most commonly misconfigured supervised-only settings and the ones most likely to generate L1/L2 tickets. Each receives a detailed treatment below." Then one subsection per setting, using heading level `####`:

1. **#### Block App Store** — What it does, use cases (VPP-only fleets, regulated industries), followed by:
   `> **What breaks if misconfigured:** Blocking App Store on a fleet that uses VPP user-licensed apps prevents users from accepting the ABM invitation, which breaks the entire VPP user-licensing flow. Symptom appears in: device (App Store icon missing) and Intune admin center (VPP user licenses stuck in "Invitation sent" state).`

2. **#### Block removing apps** — Protects managed apps from user uninstall. `> **What breaks if misconfigured:** When set to Block, admins can no longer uninstall apps from devices remotely via Intune — the block applies to all removal attempts including MDM-initiated uninstall. Symptom appears in: Intune admin center (uninstall assignment stuck in "Pending" indefinitely).`

3. **#### Allow activation Lock** — Enables supervised activation lock with bypass code escrow to Intune. `> **What breaks if misconfigured:** Enabling activation lock without configuring bypass code escrow (or losing the escrowed code) means a device whose user forgets their iCloud password is effectively bricked — no MDM remediation possible. Symptom appears in: device (activation lock screen at boot; user cannot proceed without Apple ID).`

4. **#### Block configuration profile changes** — Prevents user from removing profiles via Settings. `> **What breaks if misconfigured:** On a device without locked enrollment (see [ADE Enrollment Profile](03-ade-enrollment-profile.md)), this block alone is insufficient — users can still remove the management profile via Settings > General > VPN & Device Management. Pair with locked enrollment for full protection. Symptom appears in: Intune admin center (device unenrolled by user action).`

5. **#### Block modification of account settings** — Prevents users from modifying Entra-joined accounts. `> **What breaks if misconfigured:** If a user needs to re-authenticate Entra (token expiry, password change, MFA reset) and this block is active, they cannot complete re-auth; resolution requires device wipe and re-enrollment. Symptom appears in: device (user cannot sign in or add account) and Intune admin center (device stale, user unable to remediate).`

6. **#### Block iCloud backup** — Data exfiltration control. `> **What breaks if misconfigured:** Fleet-wide blocking of iCloud backup without an alternative corporate backup strategy means users lose all device-local data on device loss, replacement, or crash — no iCloud snapshot available to restore from. Symptom appears in: device (restore flow shows no backup on new device) and help desk (tickets for lost data post-device-replacement).`

7. **#### Block AirDrop** — Data exfiltration control. `> **What breaks if misconfigured:** Fleet-wide AirDrop block without an alternative corporate file-transfer mechanism frustrates legitimate use cases (transferring photos/docs between user's own managed devices). Symptom appears in: help desk (surge in tickets requesting file-transfer alternatives).`

8. **#### Block Camera and Block FaceTime** — Paired; regulated-industry requirement. `> **What breaks if misconfigured:** Overly broad camera block prevents users from participating in video meetings (Microsoft Teams, Zoom). Intended for scoped deployments (hospitals, classified environments) — applying fleet-wide to a general knowledge-worker fleet breaks Teams and Zoom video. Symptom appears in: device (camera unavailable in all apps) and help desk (video meeting complaints).`

9. **#### Block pairing with non-Configurator hosts** — Prevents USB-based unmanagement. `> **What breaks if misconfigured:** Leaving this OFF on supervised corporate devices allows a malicious computer (or a rogue user with a personal Mac) to pair with the device via USB and potentially perform actions including un-supervising via Apple Configurator. Recommended ON for all corporate ADE devices. Symptom appears in: (security audit finding — no user-facing symptom until compromise occurs).`

Word count target for the Device Restrictions section: ~200-300 lines total across category tables, callouts, and the 9 detailed subsections. The complete file (Task 1 + Task 2) should be in the 250-400 line range so Task 3 can bring it to 300-500 total.
  </action>
  <verify>
    <automated>bash -c 'grep -c "^## Device Restrictions" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && CALLOUTS=$(grep -c "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/04-configuration-profiles.md) && test "$CALLOUTS" -ge 13 && LINKS=$(grep -c "../ios-lifecycle/00-enrollment-overview.md#supervision" docs/admin-setup-ios/04-configuration-profiles.md) && test "$LINKS" -ge 13 && grep -c "^#### Block App Store" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^#### Block removing apps" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^#### Allow activation Lock" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^#### Block configuration profile changes" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^#### Block modification of account settings" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^#### Block iCloud backup" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^#### Block AirDrop" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^#### Block Camera" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^#### Block pairing with non-Configurator hosts" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Device Restrictions$" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/04-configuration-profiles.md` returns at least `13` (Home Screen Layout from Task 1 + 12 category-level callouts)
    - `grep -c "../ios-lifecycle/00-enrollment-overview.md#supervision" docs/admin-setup-ios/04-configuration-profiles.md` equals the 🔒 callout count plus the Prerequisites reference (>= 13)
    - Every 🔒 callout ends with `See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).` — verify no 🔒 callout is missing the link: `grep "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/04-configuration-profiles.md | grep -v "ios-lifecycle/00-enrollment-overview.md#supervision" | wc -l` returns `0`
    - All 9 named high-impact setting headings exist: `grep -c "^#### Block App Store$\|^#### Block removing apps$\|^#### Allow activation Lock$\|^#### Block configuration profile changes$\|^#### Block modification of account settings$\|^#### Block iCloud backup$\|^#### Block AirDrop$\|^#### Block Camera\|^#### Block pairing with non-Configurator hosts$" docs/admin-setup-ios/04-configuration-profiles.md` returns at least `9`
    - `grep -c "| Setting | Supervised-only?" docs/admin-setup-ios/04-configuration-profiles.md` returns at least `12` (one per category)
    - `grep -c "^### Software Update Note\|Software Update Note\|software update" docs/admin-setup-ios/04-configuration-profiles.md` returns at least `1` (D-05 note present)
    - `grep -c "Apple updates.*iOS/iPadOS update policies\|iOS/iPadOS update policies" docs/admin-setup-ios/04-configuration-profiles.md` returns at least `1` (DDM cross-reference)
    - File line count >= 250 (`wc -l docs/admin-setup-ios/04-configuration-profiles.md`)
  </acceptance_criteria>
  <done>
The `## Device Restrictions` section is appended with 12 category subsections each containing a Settings/Supervised-only?/What-it-does table and a category-level 🔒 blockquote callout linking to the supervision anchor, followed by 9 `####` full-detail subsections for the high-impact supervised-only settings each with a "What breaks if misconfigured" callout. The Software Update DDM note is present.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close 04-configuration-profiles.md with Verification checklist, Configuration-Caused Failures table, See Also, and revision history; then update 00-overview.md to link the three new guides and extend the Mermaid diagram</name>
  <files>docs/admin-setup-ios/04-configuration-profiles.md, docs/admin-setup-ios/00-overview.md</files>
  <read_first>
    - docs/admin-setup-ios/04-configuration-profiles.md (current state from Tasks 1 and 2)
    - docs/admin-setup-ios/00-overview.md (current Mermaid diagram and setup sequence list — to be extended)
    - docs/admin-setup-macos/00-overview.md (reference for multi-guide Mermaid pattern — lines 17-28)
    - docs/admin-setup-macos/03-configuration-profiles.md (Verification checklist and Config-Caused Failures table format reference — lines 170-196)
    - docs/admin-setup-ios/03-ade-enrollment-profile.md (Phase 27 Configuration-Caused Failures placeholder text "iOS L1 runbooks (Phase 30)" — lines 157-166)
  </read_first>
  <action>

**PART A — Complete `docs/admin-setup-ios/04-configuration-profiles.md`:**

Append these final sections to the file:

1. **`## Verification`** (bulleted checklist with 6 items):
```markdown
## Verification

- [ ] Each configuration profile shows "Succeeded" status in Intune admin center > Devices > Configuration > [profile] > Device status
- [ ] Profile delivery confirmed on target devices (Settings > General > VPN & Device Management > [profile name])
- [ ] Wi-Fi profiles connect devices to enterprise networks without manual SSID entry
- [ ] Device restrictions marked 🔒 show as "Succeeded" on supervised devices and "Not applicable" on any unsupervised devices in the fleet
- [ ] Home screen layout renders correctly on supervised devices (icon placement matches configured layout)
- [ ] Every 🔒 supervised-only setting deployed to a supervised ADE device reports "Succeeded"; the same profile targeted to an unsupervised device reports "Not applicable" for those settings
```

2. **`## Configuration-Caused Failures`** table (with Phase 30 placeholder per D-22):
```markdown
## Configuration-Caused Failures

| Misconfiguration | Portal | Symptom | Runbook |
|------------------|--------|---------|---------|
| Wi-Fi SSID case mismatch | Intune | Device cannot find network; silent auth failure | iOS L1 runbooks (Phase 30) |
| SCEP certificate profile missing for enterprise Wi-Fi | Intune | 802.1X authentication fails on device | iOS L1 runbooks (Phase 30) |
| Basic auth selected for Exchange ActiveSync when tenant requires Modern | Intune | Mail app shows "Cannot Get Mail" | iOS L1 runbooks (Phase 30) |
| Supervised-only restriction targeted to unsupervised device | Intune | Profile shows "Not applicable" for that setting; no user-facing enforcement | iOS L1 runbooks (Phase 30) |
| Block App Store with VPP user-licensed apps | Intune | VPP invitation cannot be accepted; user-licensed apps never install | iOS L1 runbooks (Phase 30) |
| Block modification of account settings active during Entra re-auth requirement | Intune | User cannot re-authenticate; remediation requires device wipe | iOS L1 runbooks (Phase 30) |
| Activation Lock enabled without bypass code escrow | Intune | Device bricked at activation screen if user forgets Apple ID | iOS L1 runbooks (Phase 30) |
| Home screen layout references uninstalled bundle ID | Intune | Blank icons on device home screen | iOS L1 runbooks (Phase 30) |
| Legacy "Defer software updates" used for update enforcement (deprecated path) | Intune | Updates not enforced; Apple is deprecating MDM-based update workloads — use DDM-based "iOS/iPadOS update policies" instead | iOS L1 runbooks (Phase 30) |
```

3. **`## See Also`** (bulleted links):
```markdown
## See Also

- [App Deployment](05-app-deployment.md) — deploying VPP, LOB, and Store apps
- [Compliance Policies](06-compliance-policy.md) — detect vs enforce distinction
- [ADE Enrollment Profile](03-ade-enrollment-profile.md) — supervised mode and locked enrollment
- [APNs Certificate](01-apns-certificate.md) — MDM delivery channel prerequisite
- [iOS/iPadOS Admin Setup Overview](00-overview.md)
- [iOS/iPadOS Enrollment Path Overview](../ios-lifecycle/00-enrollment-overview.md) — supervision concept anchor
- [iOS/iPadOS ADE Lifecycle](../ios-lifecycle/01-ade-lifecycle.md)
- [macOS Configuration Profiles](../admin-setup-macos/03-configuration-profiles.md) — parallel macOS guide
- [Apple Provisioning Glossary](../_glossary-macos.md)
```

4. **Previous/Next footer + revision history**:
```markdown
---
*Previous: [ADE Enrollment Profile](03-ade-enrollment-profile.md) | Next: [App Deployment](05-app-deployment.md) | [Back to Overview](00-overview.md)*

---

| Date | Change | Author |
|------|--------|--------|
| 2026-04-16 | Initial version — iOS/iPadOS configuration profiles guide with Wi-Fi/VPN/Email/Certificates/Home Screen Layout payload sections and Device Restrictions section covering 12 categories with category-level 🔒 supervised-only callouts plus 9 high-impact full-detail subsections | -- |
```

**PART B — Update `docs/admin-setup-ios/00-overview.md`:**

Read the current file; keep the frontmatter, platform gate, H1, and intro paragraph unchanged. Then make these three specific edits:

**Edit 1 — Replace the Mermaid diagram** (currently lines 19-23):
Replace the existing 3-node graph:
```
graph LR
  A[1. APNs<br/>Certificate] --> B[2. ABM/ADE<br/>Token]
  B --> C[3. ADE Enrollment<br/>Profile]
```
with this 6-node graph:
```
graph LR
  A[1. APNs<br/>Certificate] --> B[2. ABM/ADE<br/>Token]
  B --> C[3. ADE Enrollment<br/>Profile]
  C --> D[4. Configuration<br/>Profiles]
  C --> E[5. App<br/>Deployment]
  C --> F[6. Compliance<br/>Policies]
```

**Edit 2 — Extend the setup sequence list** (currently items 1-3): After the existing item 3 (ADE Enrollment Profile), append these three list items (preserve existing 1-3 exactly):

```markdown
4. **[Configuration Profiles](04-configuration-profiles.md)** — Deploy Wi-Fi, VPN, Email, Certificates, Device Restrictions (with supervised-only callouts per category), and Home Screen Layout. Configuration profiles enforce settings; compliance policies detect non-compliance.

5. **[App Deployment](05-app-deployment.md)** — Deploy iOS/iPadOS apps via VPP (device-licensed or user-licensed), LOB (.ipa), or Store apps without VPP. Silent install requires supervised mode AND device licensing.

6. **[Compliance Policies](06-compliance-policy.md)** — Configure OS version gates, jailbreak detection, passcode requirements, and Actions for Noncompliance. Includes dedicated Conditional Access timing section covering the enrollment-to-first-evaluation window.
```

**Edit 3 — Update the H1 and intro**: Change the H1 from `# iOS/iPadOS Admin Setup: Corporate ADE Configuration` to `# iOS/iPadOS Admin Setup: Corporate ADE Configuration and Device Management`. In the intro paragraph, replace "the three prerequisites for iOS/iPadOS Automated Device Enrollment: APNs certificate, ABM/ADE token, and ADE enrollment profile" with "iOS/iPadOS Automated Device Enrollment prerequisites plus the core post-enrollment admin tasks: configuration profiles, app deployment, and compliance policies". Keep the "Complete the guides in order" sentence.

**Edit 4 — Update the revision history table**: Add a new row at the top of the table (immediately after the header row):
```
| 2026-04-16 | Extended overview to include Phase 28 guides 04/05/06 and updated Mermaid diagram to 6-node graph | -- |
```

Do NOT modify any other content in 00-overview.md.
  </action>
  <verify>
    <automated>bash -c 'test -f docs/admin-setup-ios/04-configuration-profiles.md && grep -c "^## Verification$" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^## Configuration-Caused Failures$" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "^## See Also$" docs/admin-setup-ios/04-configuration-profiles.md | grep -q "^1$" && grep -c "iOS L1 runbooks (Phase 30)" docs/admin-setup-ios/04-configuration-profiles.md | awk "{print (\$1 >= 9)}" | grep -q "^1$" && grep -c "04-configuration-profiles.md" docs/admin-setup-ios/00-overview.md | awk "{print (\$1 >= 1)}" | grep -q "^1$" && grep -c "05-app-deployment.md" docs/admin-setup-ios/00-overview.md | awk "{print (\$1 >= 1)}" | grep -q "^1$" && grep -c "06-compliance-policy.md" docs/admin-setup-ios/00-overview.md | awk "{print (\$1 >= 1)}" | grep -q "^1$" && grep -c "4. Configuration<br/>Profiles" docs/admin-setup-ios/00-overview.md | grep -q "^1$" && grep -c "5. App<br/>Deployment" docs/admin-setup-ios/00-overview.md | grep -q "^1$" && grep -c "6. Compliance<br/>Policies" docs/admin-setup-ios/00-overview.md | grep -q "^1$" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Verification$" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "^## Configuration-Caused Failures$" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "^## See Also$" docs/admin-setup-ios/04-configuration-profiles.md` returns `1`
    - `grep -c "iOS L1 runbooks (Phase 30)" docs/admin-setup-ios/04-configuration-profiles.md` returns at least `9` (one per Config-Caused Failures table row)
    - The full file is 300-500 lines per D-04: `wc -l docs/admin-setup-ios/04-configuration-profiles.md` returns a number >= 300 and <= 500 (strongly prefer >= 300; if > 500 that is still acceptable since D-01 expands content)
    - `grep -F "[Configuration Profiles](04-configuration-profiles.md)" docs/admin-setup-ios/00-overview.md` finds at least 1 match
    - `grep -F "[App Deployment](05-app-deployment.md)" docs/admin-setup-ios/00-overview.md` finds at least 1 match
    - `grep -F "[Compliance Policies](06-compliance-policy.md)" docs/admin-setup-ios/00-overview.md` finds at least 1 match
    - `grep -c "4. Configuration<br/>Profiles" docs/admin-setup-ios/00-overview.md` returns `1`
    - `grep -c "5. App<br/>Deployment" docs/admin-setup-ios/00-overview.md` returns `1`
    - `grep -c "6. Compliance<br/>Policies" docs/admin-setup-ios/00-overview.md` returns `1`
    - `grep -c "C --> D" docs/admin-setup-ios/00-overview.md` returns `1` (C edge → new node D)
    - `grep -c "C --> E" docs/admin-setup-ios/00-overview.md` returns `1`
    - `grep -c "C --> F" docs/admin-setup-ios/00-overview.md` returns `1`
    - Every 🔒 callout still links to supervision anchor (regression check): `grep "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/04-configuration-profiles.md | grep -v "ios-lifecycle/00-enrollment-overview.md#supervision" | wc -l` returns `0`
  </acceptance_criteria>
  <done>
`04-configuration-profiles.md` is complete (frontmatter → platform gate → Prerequisites → Profile Delivery Channel → 6 payload sections → Device Restrictions with 12 category tables + 9 detailed subsections → Verification → Configuration-Caused Failures with Phase 30 placeholder → See Also → revision history), total line count 300-500. `00-overview.md` Mermaid diagram extended to 6 nodes with edges C→D, C→E, C→F, and the setup sequence list contains the three new items linking to 04/05/06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 28 is a documentation-only phase. There is no runtime code, no authentication logic, no secrets, and no user input handling.

| Boundary | Description |
|----------|-------------|
| Document → Admin reader | Markdown content describes Intune portal actions an administrator may take. No credentials, tokens, or PII appear in the content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Information Disclosure (misrepresentation) | 04-configuration-profiles.md device restrictions matrix | mitigate | Content sourced from Microsoft Learn device-restrictions-apple reference (RESEARCH.md [VERIFIED]); supervised-only boundaries explicitly listed per category; D-05 software update deprecation verified against current Microsoft Learn update policies reference. |
| T-28-01-02 | Tampering (broken link propagation) | All 🔒 callouts pointing to `../ios-lifecycle/00-enrollment-overview.md#supervision` | mitigate | Acceptance criteria verify the link target string is present on every callout (regression check: zero callouts without the link target string). The supervision anchor existence is verified in the enrollment-overview.md file (line 38). |

ASVS L1 compliance is N/A for markdown documentation. No authentication, session management, access control, or cryptographic claims are made by the guide.
</threat_model>

<verification>
After all three tasks complete, run the combined checks:

```bash
# Structure
test -f docs/admin-setup-ios/04-configuration-profiles.md
wc -l docs/admin-setup-ios/04-configuration-profiles.md    # expect 300-500

# Frontmatter
grep -c "^platform: iOS" docs/admin-setup-ios/04-configuration-profiles.md       # 1
grep -c "^audience: admin" docs/admin-setup-ios/04-configuration-profiles.md     # 1
grep -c "^applies_to: ADE" docs/admin-setup-ios/04-configuration-profiles.md     # 1

# Supervised-only callout integrity
CALLOUT_COUNT=$(grep -c "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/04-configuration-profiles.md)
echo "Callout count: $CALLOUT_COUNT (expect >= 13)"
grep "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/04-configuration-profiles.md | grep -v "ios-lifecycle/00-enrollment-overview.md#supervision" | wc -l   # must be 0

# Required sections
grep -c "^## \(Prerequisites\|Profile Delivery Channel\|Wi-Fi\|VPN\|Email\|Certificates\|Home Screen Layout\|Device Restrictions\|Verification\|Configuration-Caused Failures\|See Also\)$" docs/admin-setup-ios/04-configuration-profiles.md   # 11

# Nine full-detail subsections present
grep -cE "^#### (Block App Store|Block removing apps|Allow activation Lock|Block configuration profile changes|Block modification of account settings|Block iCloud backup|Block AirDrop|Block Camera|Block pairing with non-Configurator hosts)" docs/admin-setup-ios/04-configuration-profiles.md   # >= 9

# Configuration-Caused Failures uses Phase 30 placeholder
grep -c "iOS L1 runbooks (Phase 30)" docs/admin-setup-ios/04-configuration-profiles.md   # >= 9

# Overview update
grep -cE "(04-configuration-profiles|05-app-deployment|06-compliance-policy)\.md" docs/admin-setup-ios/00-overview.md   # >= 3
grep -cE "C --> [DEF]" docs/admin-setup-ios/00-overview.md   # >= 3 (mermaid edges)
```
</verification>

<success_criteria>
Phase SC #1 (per ROADMAP): An admin reading the configuration profiles guide can identify which documented settings (Wi-Fi, VPN, email, device restrictions, certificates, home screen layout) require supervised mode, because each supervised-only setting category has the 🔒 callout linking back to the supervision anchor.

This plan satisfies SC #1 via the category-level 🔒 callout pattern (user's Q1 resolution): 12 device-restriction category callouts + 1 Home Screen Layout callout + 9 full-detail supervised settings, all linking to `../ios-lifecycle/00-enrollment-overview.md#supervision`. A reader scanning the Device Restrictions section sees a Supervised-only? column in every category table and a blockquote callout naming the supervised settings.
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-01-SUMMARY.md`
2. Note the final line count of `04-configuration-profiles.md` and confirm it falls in the 300-500 range
3. Note the number of 🔒 callouts and confirm each links to the supervision anchor
4. Log any deviations from the per-category settings enumeration (which settings were added or omitted vs RESEARCH.md lines 128-236)
</output>
