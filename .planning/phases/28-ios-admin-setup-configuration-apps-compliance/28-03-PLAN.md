---
phase: 28
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-ios/06-compliance-policy.md
autonomous: true
requirements: [ACFG-03]
requirements_addressed: [ACFG-03]
cross_ai: false

must_haves:
  truths:
    - "An admin reading the compliance guide can configure OS version gates, jailbreak detection, and passcode requirements because each has its own subsection with a 'What breaks if misconfigured' callout"
    - "The guide contains exactly one `## Compliance Evaluation Timing and Conditional Access` section that answers SC #4 without requiring the reader to follow cross-references"
    - "The CA timing section contains inline text explaining the Not-Evaluated state, the default compliance posture toggle ('Mark devices with no compliance policy assigned as'), the 0-15 minute gap timing, and the CA behavior during the gap"
    - "The CA timing section cross-references reference/compliance-timing.md and reference/ca-enrollment-timing.md for deep-dive content per D-12"
    - "The CA timing section provides a linkable anchor `#compliance-evaluation-timing-and-conditional-access` for Phase 30/31 runbook use per D-13"
    - "The Actions for Noncompliance section documents iOS-specific behavior: retire is the iOS cleanup action (no full wipe compliance action) and notes that push notification delivery is not guaranteed"
    - "The default compliance posture toggle is documented in TWO places per D-18: the CA timing section AND the compliance policy creation steps"
    - "The guide contains a Configuration-Caused Failures table using 'iOS L1 runbooks (Phase 30)' placeholder"
  artifacts:
    - path: "docs/admin-setup-ios/06-compliance-policy.md"
      provides: "iOS/iPadOS compliance policy admin setup guide with dedicated CA timing section"
      min_lines: 250
      contains: "## Compliance Evaluation Timing and Conditional Access"
  key_links:
    - from: "docs/admin-setup-ios/06-compliance-policy.md"
      to: "docs/reference/compliance-timing.md"
      via: "CA timing section cross-reference"
      pattern: "../reference/compliance-timing.md"
    - from: "docs/admin-setup-ios/06-compliance-policy.md"
      to: "docs/reference/ca-enrollment-timing.md"
      via: "CA timing section cross-reference"
      pattern: "../reference/ca-enrollment-timing.md"
    - from: "docs/admin-setup-ios/06-compliance-policy.md"
      to: "docs/admin-setup-macos/05-compliance-policy.md"
      via: "See Also cross-platform"
      pattern: "admin-setup-macos/05-compliance-policy.md"
---

<objective>
Deliver the iOS/iPadOS Compliance Policy admin setup guide (`docs/admin-setup-ios/06-compliance-policy.md`) covering OS version gates, jailbreak detection, passcode requirements, Actions for Noncompliance (with iOS-specific behaviors), and — most importantly — a dedicated `## Compliance Evaluation Timing and Conditional Access` section that answers SC #4 from the guide alone (not just by cross-reference).

Purpose: Satisfy ACFG-03 — an admin must be able to configure the required compliance settings, understand iOS-specific Actions for Noncompliance behavior, and determine from this guide alone what happens to a device's CA access state in the window between enrollment completion and first compliance evaluation.

Output:
- NEW: `docs/admin-setup-ios/06-compliance-policy.md` (target 300-400 lines — larger than macOS 05 due to D-11 dedicated CA timing section)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-CONTEXT.md
@.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-RESEARCH.md
@.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-VALIDATION.md

<!-- Structural templates (executor MUST read before writing) -->
@docs/admin-setup-macos/05-compliance-policy.md
@docs/admin-setup-ios/03-ade-enrollment-profile.md
@docs/_templates/admin-template-ios.md
@docs/reference/compliance-timing.md
@docs/reference/ca-enrollment-timing.md

<interfaces>
<!-- Frontmatter schema -->
```yaml
---
last_verified: 2026-04-16
review_by: 2026-07-15
applies_to: ADE
audience: admin
platform: iOS
---
```

<!-- Compliance timing sequence (RESEARCH.md lines 498-507) — anchor content for CA timing section -->
```
T+0 min    Enrollment completes; device enters managed state
T+0-15 min Compliance check not yet run. State = "Not evaluated".
           CA behavior determined by "Mark devices with no compliance policy assigned" setting.
T+15-30    First compliance evaluation completes. State = Compliant or Non-compliant.
T+≤8 hr    Full Intune inventory sync (apps, hardware, etc.)
Ongoing    Every 8 hours: automatic Intune check-in and re-evaluation.
           On policy change: propagates on next 8-hour cycle for each device.
           User can force sync immediately via Company Portal > Sync.
```

<!-- Actions for Noncompliance table (from RESEARCH.md lines 459-466) — USE VERBATIM COLUMN CONTENT -->
```markdown
| Action | Schedule unit | iOS-specific notes |
|--------|--------------|--------------------|
| **Mark device non-compliant** | Days (0-365) | Default action; cannot be removed. Setting > 0 days creates a grace period. |
| **Send email to end user** | Days | Uses notification message template; supports variables `{{UserName}}`, `{{DeviceName}}`, `{{DeviceId}}`, `{{OSAndVersion}}`. Delivery within 6 hours. |
| **Remotely lock the noncompliant device** | Days | Supported on iOS/iPadOS; user sees PIN/passcode prompt to unlock. |
| **Add device to retire list** | Days | iOS retire removes company data and unenrolls the device (selective wipe of corporate data). There is no full-device-wipe compliance action — full wipe is a separate device action. |
| **Send push notification to end user** | Days | Delivered via Company Portal or Intune app; **delivery is not guaranteed** and may be delayed hours. |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 06-compliance-policy.md with frontmatter, platform gate, intro, Compliance vs Configuration table, Prerequisites, and the Steps section (policy creation → settings → Actions for Noncompliance → assign) with per-setting What-breaks callouts; include the default compliance posture toggle in Step 1 per D-18</name>
  <files>docs/admin-setup-ios/06-compliance-policy.md</files>
  <read_first>
    - docs/admin-setup-macos/05-compliance-policy.md (PRIMARY structural template — Compliance vs Configuration table, Steps section format, What-breaks callout placement)
    - docs/_templates/admin-template-ios.md (frontmatter, callout formats)
    - .planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-RESEARCH.md (lines 417-494 for iOS compliance settings, actions, and default posture toggle; lines 525-560 for What-breaks content)
    - .planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-CONTEXT.md (D-11 through D-18 decisions)
  </read_first>
  <action>
Create `docs/admin-setup-ios/06-compliance-policy.md`:

**1. Frontmatter**:
```yaml
---
last_verified: 2026-04-16
review_by: 2026-07-15
applies_to: ADE
audience: admin
platform: iOS
---
```

**2. Platform gate** (4-line blockquote):
```markdown
> **Platform gate:** This guide covers iOS/iPadOS ADE configuration via Apple Business Manager and Intune.
> For macOS compliance policy, see [macOS Compliance Policies](../admin-setup-macos/05-compliance-policy.md).
> For iOS/iPadOS enrollment terminology, see the [Apple Provisioning Glossary](../_glossary-macos.md).
> Portal navigation may vary by Intune admin center version. See [Overview](00-overview.md#portal-navigation-note) for details.
```

**3. H1 title**: `# iOS/iPadOS Compliance Policies`

**4. Intro paragraph** (1 paragraph): State that compliance policies DETECT non-compliance; configuration profiles ENFORCE settings. Note that Intune security baselines are Windows-only — iOS/iPadOS admins configure compliance manually. Mention that this guide covers the required compliance settings (OS version, jailbreak detection, passcode) plus the iOS-specific behavior around Conditional Access timing, which is covered in a dedicated section below.

**5. `## Compliance vs. Configuration: Critical Distinction`** (simplified iOS table per user's Q2 resolution — adopt the recommended 4-row simplified version):

```markdown
## Compliance vs. Configuration: Critical Distinction

Compliance policies DETECT non-compliance and report status. Configuration profiles ENFORCE settings and prevent user deviation. Most iOS compliance settings have no paired "enforce via config profile" equivalent — iOS relies on App Store curation, OS integrity, and Apple-signed restrictions rather than MDM-enforced low-level controls. The four most commonly-paired settings are:

| Action | Compliance Policy | Configuration Profile |
|--------|------------------|----------------------|
| Verify device is not jailbroken | Yes (Jailbroken devices = Block) | N/A — iOS has no MDM-enforceable anti-jailbreak primitive; Apple's code signing and Secure Enclave are the enforcement mechanism |
| Verify OS version meets minimum | Yes (Minimum OS version) | Partial — software update policies under **Devices > Apple updates > iOS/iPadOS update policies** can enforce upgrades (DDM-based on iOS 17+) |
| Verify passcode meets requirements | Yes (Password settings) | Yes — passcode requirements in configuration profile (Settings Catalog > Restrictions or Passcode) enforce the rules at passcode-change time |
| Verify restricted apps are not installed | Yes (Restricted apps by Bundle ID) | Partial — "Block App Store" and "Block installing apps" in configuration profile (supervised-only) prevent the install in the first place |

> **What breaks if misconfigured:** If compliance policies are deployed without corresponding configuration profiles (where pairing is possible), devices will be marked non-compliant but nothing prevents the user from changing settings. For passcode requirements specifically, changes to the compliance setting do NOT take effect until the user next changes their passcode — the device may show as compliant with the old passcode until then. Symptom appears in: Intune admin center (devices non-compliant) and on device (Conditional Access blocks access if CA policies are active).
```

**6. `## Prerequisites`** (bulleted list):
- Intune Administrator role
- Devices enrolled in Intune (any path: ADE, Device Enrollment, User Enrollment, or MAM-WE)
- Understanding that compliance detects; configuration enforces (see [Configuration Profiles](04-configuration-profiles.md))
- Conditional Access policies configured (if compliance status should gate access to corporate resources)

**7. `## Steps`** section with four sub-sections:

**`### Step 1: Create Compliance Policy`**:

`#### In Intune admin center`
1. Navigate to **Devices** > **Manage devices** > **Compliance** > **Create policy**.
2. Platform: **iOS/iPadOS**.
3. Enter policy name and description.

**CRITICAL — Default compliance posture toggle (per D-18, must appear here AND in the CA timing section):**

```markdown
Before assigning compliance policies, review the **default compliance posture** toggle at **Endpoint security** > **Device compliance** > **Compliance policy settings** > **Mark devices with no compliance policy assigned as**:

- **Compliant** (Intune default) — Permissive. Devices without an assigned policy and devices in the "Not evaluated" state are treated as compliant. CA "Require compliant device" grants access.
- **Not compliant** — Restrictive. Devices without an assigned policy and devices in "Not evaluated" state are treated as non-compliant. CA "Require compliant device" blocks access.

This toggle is the single most impactful setting for Day 1 device experience and interacts directly with Conditional Access timing behavior described in [Compliance Evaluation Timing and Conditional Access](#compliance-evaluation-timing-and-conditional-access).

> **What breaks if misconfigured:** Leaving the toggle at "Compliant" (the default) in a high-security environment means unmanaged devices and devices without assigned compliance policies are treated as compliant — CA's "Require compliant device" grants them access. Conversely, setting the toggle to "Not compliant" without a configured grace period means users are blocked from CA-protected resources for the 0-30 minutes between enrollment completion and first compliance evaluation — common help-desk complaint pattern during rollouts. Symptom appears in: device (Outlook/SharePoint/Teams shows access denied immediately after enrollment) and Intune admin center (device shows "Not evaluated" state).
```

**`### Step 2: Configure Compliance Settings`**:

Organize as bold sub-category headings (mirror macOS guide's pattern but use iOS categories from RESEARCH.md lines 421-450):

**Email:**

- **Unable to set up email on the device**: Not configured / Require (flags device non-compliant if user has set up a competing email account Intune cannot manage; requires a managed email profile present via configuration profile)

**Device Health:**

- **Jailbroken devices** (iOS 8.0+): Not configured / **Block** (recommended for corporate fleets)

```markdown
> **What breaks if misconfigured:** Leaving jailbroken detection at "Not configured" allows known jailbroken/rooted devices to be treated as compliant — these devices can bypass app sandboxing, extract protected data, and are classified as high risk by Apple Platform Security. Symptom appears in: Intune admin center (compliance reports show known-jailbroken serial numbers as compliant) and risk exposure (data exfiltration paths open on rooted devices).
```

- **Require device to be at or under Device Threat Level** (optional, requires MDE or Mobile Threat Defense connector): Not configured / Secured / Low / Medium / High

**Device Properties (Operating System Version):**

- **Minimum OS version** (iOS 8.0+)
- **Maximum OS version**
- **Minimum OS build version** — supports Apple Rapid Security Response build strings like `20E772520a`
- **Maximum OS build version**

```markdown
> **What breaks if misconfigured:** Setting a Minimum OS version ahead of the latest Apple release marks the entire fleet non-compliant with no remediation path until Apple ships the required version — users cannot manually "update to a release that does not exist yet." Symptom appears in: Intune admin center (entire fleet non-compliant) and device (update available prompts with no eligible update). Mitigation: always set Minimum OS to the version Apple currently ships; update it after testing each point release.
```

**Microsoft Defender for Endpoint (optional):**

- **Require the device to be at or under the machine risk score**: Not configured / Clear / Low / Medium / High

**System Security — Password:**

- Require a password to unlock mobile devices: Not configured / Require
- Simple passwords (blocks 1234, 1111): Not configured / Block
- Minimum password length (digits)
- Required password type: Not configured / Alphanumeric / Numeric
- Number of non-alphanumeric characters
- Maximum minutes after screen lock before password is required
- Maximum minutes of inactivity until screen locks
- Password expiration (days)
- Number of previous passwords to prevent reuse

```markdown
> **What breaks if misconfigured:** When password requirements change, the NEW requirement does not take effect on already-enrolled devices until the user next changes their passcode. The device remains compliant with the old passcode until then. This means there is a window where the device technically meets the old policy but not the new one. Symptom appears in: Intune admin center (device shows compliant despite not meeting new password requirements). Mitigation: pair compliance password settings with a forced passcode change via configuration profile Passcode settings to close the window.
```

**System Security — Device Security:**

- **Restricted apps** — list by Bundle ID; marks device non-compliant if listed unmanaged apps are installed

```markdown
> **What breaks if misconfigured:** Typos in a Bundle ID (e.g., `com.twitter.twitter` vs `com.atebits.Tweetie2` for X/Twitter after rename) cause the compliance check to silently pass regardless of actual installed apps. Symptom appears in: Intune admin center (restricted-apps compliance passes for all devices despite app being present).
```

**`### Step 3: Configure Actions for Noncompliance`**:

Introduce: "When a device is found non-compliant, Intune can trigger one or more actions. The **Mark device non-compliant** action is created automatically on every compliance policy and cannot be removed — it is the action that activates Conditional Access blocking. Setting its schedule greater than 0 days creates a grace period before CA begins blocking."

Then include the actions table (use the `<interfaces>` version verbatim):

```markdown
| Action | Schedule unit | iOS-specific notes |
|--------|--------------|--------------------|
| **Mark device non-compliant** | Days (0-365) | Default action; cannot be removed. Setting > 0 days creates a grace period. |
| **Send email to end user** | Days | Uses notification message template; supports variables `{{UserName}}`, `{{DeviceName}}`, `{{DeviceId}}`, `{{OSAndVersion}}`. Delivery within 6 hours. |
| **Remotely lock the noncompliant device** | Days | Supported on iOS/iPadOS; user sees PIN/passcode prompt to unlock. |
| **Add device to retire list** | Days | iOS retire removes company data and unenrolls the device (selective wipe of corporate data). There is no full-device-wipe compliance action — full wipe is a separate device action. |
| **Send push notification to end user** | Days | Delivered via Company Portal or Intune app; **delivery is not guaranteed** and may be delayed hours. |
```

Follow with a paragraph noting: "Schedule granularity via the admin center UI is whole days only (0, 1, 2, ...). The Graph API supports decimal values (0.25 = 6 hours, 0.5 = 12 hours), but the admin-center minimum grace period is 1 full day."

```markdown
> **What breaks if misconfigured:** Setting Mark-device-non-compliant schedule to 0 days combined with a Retire action at 1 day means a newly-enrolled device hitting the "Not evaluated" compliance state gap (see [Compliance Evaluation Timing and Conditional Access](#compliance-evaluation-timing-and-conditional-access)) can be retired before the admin has time to intervene if the default compliance posture is set to "Not compliant." Mitigation: use a grace period of at least 1 day and verify default compliance posture behavior before deploying Retire actions. Symptom appears in: Intune admin center (devices retired within 24 hours of enrollment with no human review).
```

**`### Step 4: Assign Policy`**:
1. In the policy's **Assignments** page, choose **Included groups** (user groups or device groups).
2. Optionally choose **Excluded groups** for devices that should be exempt.
3. Review and create.

Note: "User-group-assigned policies apply to every device the user signs into. Device-group-assigned policies apply regardless of user. For corporate-owned ADE devices, device-group assignment is typical."

STOP after Step 4. Task 2 writes the dedicated CA timing section. Task 3 writes Verification / Configuration-Caused Failures / See Also.
  </action>
  <verify>
    <automated>bash -c 'test -f docs/admin-setup-ios/06-compliance-policy.md && grep -c "^platform: iOS" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && grep -c "^## Compliance vs. Configuration: Critical Distinction$" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && grep -c "^## Prerequisites$" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && grep -c "^## Steps$" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && grep -c "^### Step 1: Create Compliance Policy$" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && grep -c "^### Step 2: Configure Compliance Settings$" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && grep -c "^### Step 3: Configure Actions for Noncompliance$" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && grep -c "^### Step 4: Assign Policy$" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && grep -F "Mark devices with no compliance policy assigned" docs/admin-setup-ios/06-compliance-policy.md && grep -F "Jailbroken devices" docs/admin-setup-ios/06-compliance-policy.md && grep -F "Minimum OS version" docs/admin-setup-ios/06-compliance-policy.md && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/admin-setup-ios/06-compliance-policy.md` exits 0
    - `grep -c "^platform: iOS" docs/admin-setup-ios/06-compliance-policy.md` returns `1`
    - `grep -c "^applies_to: ADE" docs/admin-setup-ios/06-compliance-policy.md` returns `1`
    - `grep -c "^## Compliance vs. Configuration: Critical Distinction$" docs/admin-setup-ios/06-compliance-policy.md` returns `1`
    - `grep -c "^## Prerequisites$" docs/admin-setup-ios/06-compliance-policy.md` returns `1`
    - `grep -c "^## Steps$" docs/admin-setup-ios/06-compliance-policy.md` returns `1`
    - All four step sub-sections present: `grep -cE "^### Step [1-4]: " docs/admin-setup-ios/06-compliance-policy.md` returns `4`
    - Required compliance topics covered: `grep -cF "Jailbroken devices\|Minimum OS version\|Require a password" docs/admin-setup-ios/06-compliance-policy.md` returns at least `3` (each topic named at least once)
    - Default compliance posture toggle present in Step 1 (D-18 first location): `grep -c "Mark devices with no compliance policy assigned" docs/admin-setup-ios/06-compliance-policy.md` returns at least `1`
    - Actions for Noncompliance table present with iOS-specific retire note: `grep -F "retire removes company data and unenrolls" docs/admin-setup-ios/06-compliance-policy.md` finds at least 1 match
    - Push notification delivery-not-guaranteed note present: `grep -F "delivery is not guaranteed" docs/admin-setup-ios/06-compliance-policy.md` finds at least 1 match
    - Password-change-timing "What breaks" callout present: `grep -F "until the user next changes their passcode" docs/admin-setup-ios/06-compliance-policy.md` finds at least 1 match
    - `grep -c "> \*\*What breaks if misconfigured:\*\*" docs/admin-setup-ios/06-compliance-policy.md` returns at least `5`
    - Line count >= 120 (`wc -l docs/admin-setup-ios/06-compliance-policy.md`)
  </acceptance_criteria>
  <done>
Frontmatter, platform gate, intro, `## Compliance vs. Configuration: Critical Distinction` table (simplified iOS version with 4 rows), Prerequisites, and `## Steps` section with all four sub-sections (Step 1 through Step 4) are written. Step 1 contains the default compliance posture toggle explanation (first of two D-18 locations). Step 2 covers jailbreak detection, OS version gates, passcode, restricted apps with What-breaks callouts. Step 3 contains the Actions for Noncompliance table with iOS-specific retire and push-notification notes. Step 4 covers assignment.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append the dedicated `## Compliance Evaluation Timing and Conditional Access` section — the SC #4-critical content that must answer the CA timing question from this guide alone per D-11/D-12/D-13/D-14</name>
  <files>docs/admin-setup-ios/06-compliance-policy.md</files>
  <read_first>
    - docs/admin-setup-ios/06-compliance-policy.md (current state from Task 1)
    - docs/reference/compliance-timing.md (to understand the depth of reference content so this section complements rather than duplicates)
    - docs/reference/ca-enrollment-timing.md (for chicken-and-egg problem context)
    - .planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-RESEARCH.md (lines 477-523 for CA timing mechanics, default posture toggle behavior, iOS-specific APNs dependency, Setup Assistant CA interaction)
    - .planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-CONTEXT.md (D-11, D-12, D-13, D-14, D-15 decisions and SC #4)
  </read_first>
  <action>
Append the dedicated `## Compliance Evaluation Timing and Conditional Access` section to `docs/admin-setup-ios/06-compliance-policy.md`. **This section must answer SC #4 from this guide alone** — a reader must be able to determine what happens to a device's CA access state in the 0-30 minute window after enrollment WITHOUT following any cross-reference.

Write this exact structure:

```markdown
## Compliance Evaluation Timing and Conditional Access

Compliance evaluation does not happen instantly after enrollment. During the window between enrollment completion and the first compliance evaluation — typically 0-30 minutes — a device's compliance state is "Not evaluated." How Conditional Access treats devices in this state is governed by a single Intune tenant setting: the **default compliance posture toggle**. This section documents the timing sequence, the toggle's behavior in the gap, and iOS-specific considerations.

### Compliance State Timeline Post-Enrollment

| Time after enrollment | Compliance state | What is happening | Conditional Access behavior |
|-----------------------|------------------|-------------------|------------------------------|
| T+0 min | Managed, no compliance record | Device has just completed enrollment; Intune has not yet evaluated it | Determined by the **default compliance posture toggle** (see below) |
| T+0-15 min | Not evaluated | Compliance check scheduled; Intune is collecting device inventory over APNs | Same as T+0 — determined by the default compliance posture toggle |
| T+15-30 min | Compliant or Non-compliant | First compliance evaluation completes | CA "Require compliant device" now evaluates against the actual state |
| T+up to 8 hr | Compliant or Non-compliant (state stable) | Full Intune inventory sync (apps, hardware, restricted apps check) | No change unless inventory reveals a new violation |
| Ongoing | Re-evaluated on 8-hour check-in cycle | Automatic check-in via APNs; policy changes propagate on next cycle | Updated continuously; users can force sync immediately via Company Portal > Sync |

### Default Compliance Posture Toggle

**Location in Intune admin center:** **Endpoint security** > **Device compliance** > **Compliance policy settings** > **Mark devices with no compliance policy assigned as**.

This setting has exactly two values, and its interpretation applies to both (a) devices without any compliance policy assigned and (b) devices in the "Not evaluated" transient state during the 0-30 minute post-enrollment window:

- **Compliant (Intune default)** — Permissive. Devices without assigned policy and devices in Not-evaluated state are treated as **compliant**. Conditional Access "Require compliant device" grants access during the gap. Recommended for rollout environments where enrollment friction is a concern and the fleet-level risk from the 0-30 minute window is acceptable.
- **Not compliant** — Restrictive. Devices without assigned policy and devices in Not-evaluated state are treated as **non-compliant**. Conditional Access "Require compliant device" blocks access during the gap. Recommended for regulated / high-security environments where any unmanaged device contact with corporate resources is unacceptable — but requires user-communication and grace-period planning to avoid help-desk escalations during rollout.

### What Happens in the 0-30 Minute Gap

The answer depends entirely on the toggle:

- **Toggle = Compliant (default):** The user completes enrollment, reaches the home screen, opens Outlook or Teams, and sign-in succeeds. CA sees the device as compliant because no policy has evaluated it yet; access is granted. If a policy later evaluates to Non-compliant, CA blocks access going forward — but the 0-30 minute window provided transparent access.
- **Toggle = Not compliant:** The user completes enrollment, reaches the home screen, opens Outlook or Teams, and sign-in **fails** with a "Your device does not meet your organization's policy" message. The user waits 0-30 minutes for the first compliance evaluation to complete (or triggers **Company Portal > Sync** manually). If the device is compliant, access is then granted; if non-compliant, the user sees a remediation screen with links to the specific failing setting.

### iOS-Specific Timing Considerations

- **APNs dependency:** iOS compliance check-in is initiated by Apple Push Notification service, not by a polling schedule. If APNs is blocked at the network edge (firewall, proxy, captive portal) the 0-15 minute evaluation can stall indefinitely. Verify APNs endpoint reachability if the Not-evaluated state persists beyond 30 minutes. See [APNs Certificate](01-apns-certificate.md).
- **No MDM diagnostic tool on iOS:** Unlike Windows, iOS has no `mdmdiagnosticstool.exe` equivalent. L2 diagnosis of a stuck compliance state requires Company Portal log upload, MDM diagnostic report from Intune admin center, or Mac+cable sysdiagnose (documented in Phase 31 L2 runbooks).
- **Setup Assistant CA interaction:** For ADE enrollments using Setup Assistant with modern authentication, CA evaluation occurs during Setup Assistant sign-in, before enrollment completes. If a CA policy requires a compliant device AND the "Microsoft Intune Enrollment" cloud app is not excluded from that policy, the device cannot complete Setup Assistant. The "Microsoft Intune Enrollment" cloud app is excluded by default across platforms — verify the exclusion remains in place before adjusting CA policies.
- **User can force re-sync:** On the device, **Settings** > **General** > **VPN & Device Management** > [management profile] > **Sync** triggers an immediate MDM check-in. Alternatively, users can use Company Portal > **Settings** > **Sync**. This is the only user-actionable escalation path from a stuck Not-evaluated state.

### Default Compliance Posture Decision Summary

| Organizational profile | Recommended toggle | Rationale |
|------------------------|---------------------|-----------|
| Rolling out ADE to an existing fleet | **Compliant** (default) | Reduces help-desk tickets during rollout; 0-30 min exposure window accepted as transient |
| Regulated industry / high-security | **Not compliant** | Blocks unmanaged devices from CA-protected resources even transiently; plan user communication for the 0-30 minute gap |
| Mixed fleet with sensitive subset | **Not compliant** + per-app CA policies with explicit compliance-bypass groups | Granular control with documented exceptions |

### Cross-References for Deep-Dive Content

The timing sequence and mitigation patterns above are sufficient to determine CA behavior during the 0-30 minute window for any iOS enrollment path. For cross-platform timing mechanics, edge cases (e.g., sync failures, Ongoing-state stuck conditions), and the full Windows/macOS/iOS chicken-and-egg enrollment-vs-CA analysis, see:

- [Compliance Policy Timing](../reference/compliance-timing.md) — cross-platform timing state transitions, state definitions, 8-hour sync cycle, grace period configuration
- [Conditional Access Enrollment Timing](../reference/ca-enrollment-timing.md) — chicken-and-egg problem, "Microsoft Intune Enrollment" cloud app exclusion, platform-specific resolution patterns
```

This section must contain (for SC #4 satisfaction):
- The heading `## Compliance Evaluation Timing and Conditional Access` exactly once
- The anchor `#compliance-evaluation-timing-and-conditional-access` is auto-generated from the heading — verify it is referenced elsewhere in the guide (Step 1 and Step 3 What-breaks callouts from Task 1 already reference it)
- Inline text covering: (a) Not-evaluated state, (b) default compliance posture toggle with exact name "Mark devices with no compliance policy assigned as", (c) the 0-15 minute first-evaluation window, (d) the "Compliant (default)" vs "Not compliant" behaviors in the gap, (e) iOS-specific APNs dependency
- Cross-references to both `../reference/compliance-timing.md` and `../reference/ca-enrollment-timing.md` per D-12
  </action>
  <verify>
    <automated>bash -c 'grep -c "^## Compliance Evaluation Timing and Conditional Access$" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && grep -F "Mark devices with no compliance policy assigned" docs/admin-setup-ios/06-compliance-policy.md && grep -F "Not evaluated" docs/admin-setup-ios/06-compliance-policy.md && grep -F "0-15 min" docs/admin-setup-ios/06-compliance-policy.md && grep -F "first compliance evaluation" docs/admin-setup-ios/06-compliance-policy.md && grep -F "../reference/compliance-timing.md" docs/admin-setup-ios/06-compliance-policy.md && grep -F "../reference/ca-enrollment-timing.md" docs/admin-setup-ios/06-compliance-policy.md && grep -F "APNs" docs/admin-setup-ios/06-compliance-policy.md && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Compliance Evaluation Timing and Conditional Access$" docs/admin-setup-ios/06-compliance-policy.md` returns exactly `1` (SC #3 + anchor generation requirement)
    - The Not-evaluated state, the toggle name, the 0-15 minute window, and "first compliance evaluation" all appear inside the section body (SC #4 proof):
      - `awk "/^## Compliance Evaluation Timing and Conditional Access$/,/^## /" docs/admin-setup-ios/06-compliance-policy.md | grep -c "Not evaluated"` returns at least `2`
      - `awk "/^## Compliance Evaluation Timing and Conditional Access$/,/^## /" docs/admin-setup-ios/06-compliance-policy.md | grep -c "Mark devices with no compliance policy assigned"` returns at least `1`
      - `awk "/^## Compliance Evaluation Timing and Conditional Access$/,/^## /" docs/admin-setup-ios/06-compliance-policy.md | grep -cE "0-15 min|first compliance evaluation"` returns at least `2`
      - `awk "/^## Compliance Evaluation Timing and Conditional Access$/,/^## /" docs/admin-setup-ios/06-compliance-policy.md | grep -c "APNs"` returns at least `1`
    - Both reference cross-links present: `grep -c "\.\./reference/compliance-timing\.md\|\.\./reference/ca-enrollment-timing\.md" docs/admin-setup-ios/06-compliance-policy.md` returns at least `2`
    - Default compliance posture toggle documented in TWO locations per D-18: `grep -c "Mark devices with no compliance policy assigned" docs/admin-setup-ios/06-compliance-policy.md` returns at least `2` (Step 1 from Task 1 + this section)
    - Timeline table present: `awk "/^## Compliance Evaluation Timing and Conditional Access$/,/^## /" docs/admin-setup-ios/06-compliance-policy.md | grep -c "^| T+0"` returns at least `2` (T+0 min and T+0-15 min rows)
    - iOS-specific considerations subsection present: `grep -c "^### iOS-Specific Timing Considerations$" docs/admin-setup-ios/06-compliance-policy.md` returns `1`
    - The Compliant vs Not-compliant behaviors in the gap are both described: `awk "/^## Compliance Evaluation Timing and Conditional Access$/,/^## /" docs/admin-setup-ios/06-compliance-policy.md | grep -cE "Toggle = Compliant|Toggle = Not compliant"` returns at least `2`
  </acceptance_criteria>
  <done>
The `## Compliance Evaluation Timing and Conditional Access` section is written, contains the timing timeline table, the default compliance posture toggle explanation (naming it exactly "Mark devices with no compliance policy assigned as"), the Compliant vs Not-compliant gap behaviors, iOS-specific APNs/Setup Assistant considerations, the decision summary table, and cross-references to both reference docs. The section answers SC #4 inline without requiring the reader to follow cross-references. The default compliance posture toggle now appears in two places in the guide (Step 1 and this section) per D-18.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append Verification checklist, Configuration-Caused Failures table, See Also, and revision history</name>
  <files>docs/admin-setup-ios/06-compliance-policy.md</files>
  <read_first>
    - docs/admin-setup-ios/06-compliance-policy.md (current state from Tasks 1-2)
    - docs/admin-setup-macos/05-compliance-policy.md (lines 118-143 for Verification / Config-Caused Failures / See Also pattern)
    - docs/admin-setup-ios/03-ade-enrollment-profile.md (Phase 27 Config-Caused Failures placeholder)
  </read_first>
  <action>
Append these final sections to `docs/admin-setup-ios/06-compliance-policy.md`:

**1. `## Verification`**:

```markdown
## Verification

- [ ] Compliance policy appears under Devices > Manage devices > Compliance with assigned device count
- [ ] Device compliance status visible at Devices > All devices > [device] > **Device compliance** tab
- [ ] Non-compliant devices show specific failing settings (not just "non-compliant") — click the failing setting for remediation guidance
- [ ] Grace period under Mark device non-compliant matches organizational policy (> 0 days recommended for rollout)
- [ ] Default compliance posture toggle (Mark devices with no compliance policy assigned as) matches organizational security posture — default Compliant for rollout, Not compliant for regulated environments
- [ ] Conditional Access policies using "Require compliant device" grant control enumerate the devices this compliance policy targets
- [ ] Newly-enrolled test device transitions from Not evaluated to Compliant within 30 minutes (APNs reachable)
- [ ] Jailbroken detection is set to Block (not "Not configured") for corporate-fleet compliance policies
```

**2. `## Configuration-Caused Failures`**:

```markdown
## Configuration-Caused Failures

| Misconfiguration | Portal | Symptom | Runbook |
|------------------|--------|---------|---------|
| Minimum OS version set ahead of latest Apple release | Intune | Entire fleet non-compliant until Apple ships the required version | iOS L1 runbooks (Phase 30) |
| Jailbroken detection left at "Not configured" | Intune | Known jailbroken devices treated as compliant; data exfiltration risk open | iOS L1 runbooks (Phase 30) |
| Password compliance changed on already-enrolled fleet | Intune | Device remains compliant with old passcode until user next changes passcode | iOS L1 runbooks (Phase 30) |
| Restricted apps list contains Bundle ID typo | Intune | Compliance check silently passes regardless of installed apps | iOS L1 runbooks (Phase 30) |
| Mark device non-compliant = 0 days with Retire action = 1 day | Intune | Devices retired within 24 hours during Not-evaluated gap; no time for admin intervention | iOS L1 runbooks (Phase 30) |
| Default compliance posture = "Not compliant" without grace period | Intune | Users blocked from CA-protected resources 0-30 min post-enrollment; help desk escalations during rollout | iOS L1 runbooks (Phase 30) |
| Default compliance posture = "Compliant" in high-security environment | Intune | Unmanaged devices and "Not evaluated" devices granted CA access; audit finding | iOS L1 runbooks (Phase 30) |
| APNs blocked at network edge | Intune | Compliance state stuck at "Not evaluated" indefinitely; CA behavior depends on default posture toggle | iOS L1 runbooks (Phase 30) |
| CA "Require compliant device" without "Microsoft Intune Enrollment" cloud app exclusion | Entra | ADE Setup Assistant cannot complete sign-in; chicken-and-egg enrollment block | iOS L1 runbooks (Phase 30) |
| Compliance policy assigned with no corresponding configuration profile where pairing is possible (e.g., passcode) | Intune | Devices marked non-compliant but no enforcement mechanism present | iOS L1 runbooks (Phase 30) |
```

**3. `## See Also`**:

```markdown
## See Also

- [Configuration Profiles](04-configuration-profiles.md) — enforcement counterpart to compliance detection
- [App Deployment](05-app-deployment.md) — managed app status and Restricted apps compliance
- [ADE Enrollment Profile](03-ade-enrollment-profile.md)
- [APNs Certificate](01-apns-certificate.md) — APNs is required for iOS compliance check-in
- [iOS/iPadOS Admin Setup Overview](00-overview.md)
- [iOS/iPadOS Enrollment Path Overview](../ios-lifecycle/00-enrollment-overview.md)
- [iOS/iPadOS ADE Lifecycle](../ios-lifecycle/01-ade-lifecycle.md)
- [Compliance Policy Timing](../reference/compliance-timing.md) — cross-platform timing mechanics deep-dive
- [Conditional Access Enrollment Timing](../reference/ca-enrollment-timing.md) — chicken-and-egg problem deep-dive
- [macOS Compliance Policies](../admin-setup-macos/05-compliance-policy.md) — parallel macOS guide (note: macOS has no dedicated CA timing section; iOS added it per ACFG-03 SC #4)
- [Apple Provisioning Glossary](../_glossary-macos.md)
```

**4. Previous/Next footer + revision history**:

```markdown
---
*Previous: [App Deployment](05-app-deployment.md) | [Back to Overview](00-overview.md)*

---

| Date | Change | Author |
|------|--------|--------|
| 2026-04-16 | Initial version — iOS/iPadOS compliance policy guide with simplified Compliance vs. Configuration table, per-setting What-breaks callouts (jailbreak detection, OS version, passcode, restricted apps), iOS-specific Actions for Noncompliance behaviors, and dedicated Compliance Evaluation Timing and Conditional Access section covering default posture toggle, 0-30 min gap behavior, and iOS APNs considerations (answers SC #4 from the guide alone) | -- |
```
  </action>
  <verify>
    <automated>bash -c 'grep -c "^## Verification$" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && grep -c "^## Configuration-Caused Failures$" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && grep -c "^## See Also$" docs/admin-setup-ios/06-compliance-policy.md | grep -q "^1$" && PLACEHOLDERS=$(grep -c "iOS L1 runbooks (Phase 30)" docs/admin-setup-ios/06-compliance-policy.md) && test "$PLACEHOLDERS" -ge 9 && grep -F "../admin-setup-macos/05-compliance-policy.md" docs/admin-setup-ios/06-compliance-policy.md && grep -F "../reference/compliance-timing.md" docs/admin-setup-ios/06-compliance-policy.md && grep -F "../reference/ca-enrollment-timing.md" docs/admin-setup-ios/06-compliance-policy.md && LINES=$(wc -l < docs/admin-setup-ios/06-compliance-policy.md) && test "$LINES" -ge 250 && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Verification$" docs/admin-setup-ios/06-compliance-policy.md` returns `1`
    - `grep -c "^## Configuration-Caused Failures$" docs/admin-setup-ios/06-compliance-policy.md` returns `1`
    - `grep -c "^## See Also$" docs/admin-setup-ios/06-compliance-policy.md` returns `1`
    - `grep -c "iOS L1 runbooks (Phase 30)" docs/admin-setup-ios/06-compliance-policy.md` returns at least `9` (Config-Caused Failures table rows)
    - See Also cross-platform link present: `grep -F "../admin-setup-macos/05-compliance-policy.md" docs/admin-setup-ios/06-compliance-policy.md` finds at least 1 match
    - See Also reference doc links present: both `grep -F "../reference/compliance-timing.md"` and `grep -F "../reference/ca-enrollment-timing.md"` find at least 1 match each (they also appear in Task 2 section; multiple OK)
    - Final file line count: `wc -l docs/admin-setup-ios/06-compliance-policy.md` returns a number >= 250 (target 300-400 per D-04 "larger than macOS 05" note; minimum 250 acceptable)
    - All required sections in document order exist exactly once:
      - `grep -n "^## " docs/admin-setup-ios/06-compliance-policy.md` shows: Compliance vs. Configuration: Critical Distinction → Prerequisites → Steps → Compliance Evaluation Timing and Conditional Access → Verification → Configuration-Caused Failures → See Also (in this order)
    - SC #3 and SC #4 regression checks pass:
      - SC #3 terms present: `grep -cE "OS version\|Jailbroken\|Passcode" docs/admin-setup-ios/06-compliance-policy.md` returns matches for all three
      - SC #4 terms concentrated in CA section: `awk "/^## Compliance Evaluation Timing and Conditional Access$/,/^## Verification$/" docs/admin-setup-ios/06-compliance-policy.md | grep -cE "grace period|first compliance evaluation|default compliance posture|Not evaluated"` returns at least `4`
  </acceptance_criteria>
  <done>
Verification checklist, Configuration-Caused Failures table (10 rows all using "iOS L1 runbooks (Phase 30)" placeholder), See Also section (with both reference docs and macOS parallel guide cross-linked), and revision history are appended. The complete guide is 250+ lines (ideally 300-400) in this order: frontmatter → platform gate → intro → Compliance vs Configuration → Prerequisites → Steps 1-4 → CA Timing section → Verification → Config-Caused Failures → See Also → revision history.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 28 Plan 03 is a documentation-only phase. No runtime code, no secrets, no authentication logic.

| Boundary | Description |
|----------|-------------|
| Document → Admin reader | Markdown describes Intune compliance portal actions. No credentials appear in the guide. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Information Disclosure (misrepresentation of default posture) | Default compliance posture toggle documentation | mitigate | Toggle name "Mark devices with no compliance policy assigned as" verified verbatim against Microsoft Learn compliance-get-started reference (RESEARCH.md lines 479-492). Behavior in Not-evaluated state verified against both Microsoft Learn and Prelude Security analysis (secondary source). D-14 mandates this setting be documented in CA timing section AND compliance creation steps — plan enforces this in Tasks 1 and 2. |
| T-28-03-02 | Tampering (SC #4 failure — reader can't determine CA state from guide alone) | `## Compliance Evaluation Timing and Conditional Access` section | mitigate | Acceptance criteria in Task 2 enforce specific inline content: Not-evaluated state (≥ 2 mentions in section), toggle name (≥ 1 mention), 0-15 min / first compliance evaluation (≥ 2 mentions), APNs dependency (≥ 1 mention), both "Compliant" and "Not compliant" behaviors explicitly described. Cross-references to reference docs are additive (for deep-dive), not substitutive. |
| T-28-03-03 | Information Disclosure (iOS-specific action behavior) | Actions for Noncompliance table — retire vs wipe, push notification delivery | mitigate | iOS-specific notes sourced from Microsoft Learn actions-for-noncompliance reference. Explicitly documents: (a) retire = selective wipe of corporate data only, (b) no full-wipe compliance action, (c) push notification delivery not guaranteed. |

ASVS L1 compliance is N/A for markdown documentation.
</threat_model>

<verification>
After all three tasks complete:

```bash
test -f docs/admin-setup-ios/06-compliance-policy.md
wc -l docs/admin-setup-ios/06-compliance-policy.md    # expect 250-400 (ideal 300-400 per D-04)

# Required sections (in this order)
grep -nE "^## " docs/admin-setup-ios/06-compliance-policy.md
# Expected: Compliance vs. Configuration / Prerequisites / Steps / Compliance Evaluation Timing and Conditional Access / Verification / Configuration-Caused Failures / See Also

# SC #3 coverage
grep -cE "OS version|Jailbroken|Passcode|Password" docs/admin-setup-ios/06-compliance-policy.md   # all present
grep -c "^### Step [1-4]" docs/admin-setup-ios/06-compliance-policy.md                             # 4

# SC #4 — CA timing content from guide alone
grep -c "^## Compliance Evaluation Timing and Conditional Access$" docs/admin-setup-ios/06-compliance-policy.md   # 1
awk '/^## Compliance Evaluation Timing and Conditional Access$/,/^## Verification$/' docs/admin-setup-ios/06-compliance-policy.md | grep -cE "grace period|first compliance evaluation|default compliance posture|Not evaluated"   # >= 4

# D-14 / D-18 dual-placement of the toggle
grep -c "Mark devices with no compliance policy assigned" docs/admin-setup-ios/06-compliance-policy.md   # >= 2

# D-12 cross-references present
grep -c "\.\./reference/compliance-timing\.md" docs/admin-setup-ios/06-compliance-policy.md   # >= 1
grep -c "\.\./reference/ca-enrollment-timing\.md" docs/admin-setup-ios/06-compliance-policy.md   # >= 1

# Phase 30 placeholder
grep -c "iOS L1 runbooks (Phase 30)" docs/admin-setup-ios/06-compliance-policy.md   # >= 9
```
</verification>

<success_criteria>
Phase SC #3 (per ROADMAP): An admin reading the compliance policy guide can configure OS version gates, jailbreak detection, and passcode requirements, and understands Conditional Access timing behavior and the default compliance posture for newly enrolled devices.

Phase SC #4 (per ROADMAP): A reader can determine from the compliance guide alone what happens to a device's CA access state in the window between enrollment completion and first compliance evaluation.

This plan satisfies SC #3 via the Step 2 Configure Compliance Settings sub-sections (Email / Device Health / Device Properties / Microsoft Defender for Endpoint / System Security — Password / System Security — Device Security), each with What-breaks callouts, plus the Step 1 default compliance posture toggle, plus the dedicated CA timing section.

This plan satisfies SC #4 via the dedicated `## Compliance Evaluation Timing and Conditional Access` section — its inline content (timeline table, default compliance posture toggle with full explanation, explicit "What Happens in the 0-30 Minute Gap" subsection covering both toggle values, iOS-specific APNs dependency) answers the SC #4 question without requiring the reader to follow cross-references. Cross-references are additive for deep-dive content.
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-03-SUMMARY.md`
2. Confirm final line count of `06-compliance-policy.md` (250-400 range)
3. Confirm the CA timing section has a single H2 heading and contains all four SC #4 phrases inline
4. Confirm the default compliance posture toggle appears in exactly two locations (Step 1 and CA timing section) per D-18
</output>
