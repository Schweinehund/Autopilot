---
phase: 28
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-ios/05-app-deployment.md
autonomous: true
requirements: [ACFG-02]
requirements_addressed: [ACFG-02]
cross_ai: false

must_haves:
  truths:
    - "The guide contains a `## Key Concepts Before You Begin` section that explains VPP device-licensed vs user-licensed distinction before the comparison table"
    - "The guide contains a 4-column comparison table with headers VPP Device-Licensed, VPP User-Licensed, LOB (.ipa), Store Apps (without VPP)"
    - "The VPP Device-Licensed section and the VPP User-Licensed section each contain a prominent 🔒 Supervised only callout explicitly referencing silent install"
    - "The guide contains a Verification section that names the three Intune admin center locations to check managed app install status (Apps > All apps > [app] > Monitor > Device install status; Devices > [device] > Managed Apps; Troubleshoot + support > Managed Apps)"
    - "The guide distinguishes managed vs unmanaged apps explicitly in prose"
    - "Every 🔒 callout links to ../ios-lifecycle/00-enrollment-overview.md#supervision"
    - "The guide contains a Configuration-Caused Failures table using 'iOS L1 runbooks (Phase 30)' placeholder"
    - "The guide contains a Renewal/Maintenance table covering VPP token annual renewal and LOB provisioning profile annual renewal"
  artifacts:
    - path: "docs/admin-setup-ios/05-app-deployment.md"
      provides: "iOS/iPadOS app deployment admin setup guide"
      min_lines: 200
      contains: "🔒 **Supervised only:**"
  key_links:
    - from: "docs/admin-setup-ios/05-app-deployment.md"
      to: "docs/ios-lifecycle/00-enrollment-overview.md#supervision"
      via: "🔒 silent install callout link"
      pattern: "../ios-lifecycle/00-enrollment-overview.md#supervision"
    - from: "docs/admin-setup-ios/05-app-deployment.md"
      to: "docs/admin-setup-macos/04-app-deployment.md"
      via: "cross-platform See Also"
      pattern: "admin-setup-macos/04-app-deployment.md"
---

<objective>
Deliver the iOS/iPadOS App Deployment admin setup guide (`docs/admin-setup-ios/05-app-deployment.md`) covering VPP Device-Licensed, VPP User-Licensed, LOB (.ipa), and Store Apps (without VPP) deployment types with a "Key Concepts Before You Begin" section, 4-column comparison table, per-type sections, prominent 🔒 silent install callouts, managed app status verification, and Configuration-Caused Failures table.

Purpose: Satisfy ACFG-02 — an admin must be able to (a) distinguish VPP device-licensed vs user-licensed apps, (b) understand that fully silent install requires supervision AND device licensing, and (c) know where in the Intune admin center to check managed app installation status.

Output:
- NEW: `docs/admin-setup-ios/05-app-deployment.md` (target 250-350 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-CONTEXT.md
@.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-RESEARCH.md
@.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-VALIDATION.md

<!-- Structural templates (executor MUST read before writing) -->
@docs/admin-setup-macos/04-app-deployment.md
@docs/admin-setup-ios/03-ade-enrollment-profile.md
@docs/_templates/admin-template-ios.md

<interfaces>
<!-- Frontmatter schema -->
```yaml
---
last_verified: 2026-04-16
review_by: 2026-07-15
applies_to: ADE
audience: admin
platform: iOS
---
```

<!-- 🔒 Supervised-only callout — LOCKED FORMAT (Phase 27 D-01 / D-02) -->
```markdown
> 🔒 **Supervised only:** [setting or feature name] requires supervised mode. [1-2 sentence consequence for unsupervised devices.] See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
```

<!-- Silent install callout (recommended text per RESEARCH.md lines 329-330) -->
```markdown
> 🔒 **Supervised only:** Fully silent app installation (no user prompt of any kind) requires supervised mode AND device licensing. On unsupervised devices, device-licensed apps still install without an Apple Account prompt, but the user sees a one-time install confirmation. See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
```

<!-- VPP 6-scenario end-user prompt table (verbatim from Microsoft Learn; RESEARCH.md lines 318-325) -->
```markdown
| # | Scenario | Apple Account prompt | Install prompt |
|---|----------|:---------------------:|:----------------:|
| 1 | BYOD — user licensed (not User Enrollment) | Yes | Yes |
| 2 | Corp — user licensed (not supervised) | Yes | Yes |
| 3 | Corp — user licensed (**supervised**) | Yes (for VPP invite) | **No** |
| 4 | BYOD — device licensed | **No** | Yes |
| 5 | Corp — device licensed (not supervised) | **No** | Yes |
| 6 | Corp — device licensed (**supervised**) | **No** | **No** (fully silent) |
```

<!-- Comparison table (12 rows × 4 columns, from RESEARCH.md lines 336-349) — USE THIS VERBATIM -->
```markdown
| Attribute | VPP Device-Licensed | VPP User-Licensed | LOB (.ipa) | Store Apps (without VPP) |
|-----------|---------------------|-------------------|------------|--------------------------|
| Apple Account required | No | Yes (personal Apple Account or Managed Apple Account for User Enrollment) | No | Yes (user installs manually) |
| Silent install (supervised) | **Yes** | No (Apple Account prompt) | Yes (signed + provisioning profile) | No |
| Silent install (unsupervised) | Partial (single install prompt) | No | Yes (signed + provisioning profile) | No |
| User Enrollment compatibility | No | Yes (Managed Apple Account only) | Limited | Yes |
| ADE compatibility | Yes | Yes | Yes | Yes |
| Device Enrollment compatibility | Yes | Yes (personal Apple Account) | Yes | Yes |
| Assignment types | Required, Available for enrolled, Uninstall | Required, Available for enrolled, Uninstall | Required, Available for enrolled, Available w/o enrollment, Uninstall | Required, Available for enrolled, Uninstall |
| Available deployment intent for device groups | No (user groups only) | No (user groups only) | Yes for Required; No for Available | No (user groups only) |
| License model | One license per device | One license / up to 5 devices per user | N/A (owned app) | N/A (user's own licensing) |
| Size limit | N/A (App Store) | N/A (App Store) | **2 GB per app** | N/A (App Store) |
| Update mechanism | Token-level auto-update; Intune sync | Token-level auto-update; Intune sync | CFBundleVersion increment + re-upload | App Store |
| Book support | No | Yes | No | No |
```

<!-- Managed app status verification paths (RESEARCH.md lines 387-399) -->
Three Intune admin center locations:
1. `**Apps** → **All apps** → [app] → **Monitor** → **Device install status** / **User install status**`
2. `**Devices** → **All devices** → [device] → **Managed Apps**`
3. `**Troubleshoot + support** → **Troubleshoot** → enter user → **Managed Apps** pane`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 05-app-deployment.md with frontmatter, platform gate, intro, "Key Concepts Before You Begin", and the 12-row comparison table</name>
  <files>docs/admin-setup-ios/05-app-deployment.md</files>
  <read_first>
    - docs/admin-setup-macos/04-app-deployment.md (PRIMARY structural template — comparison table format, section order)
    - docs/admin-setup-ios/03-ade-enrollment-profile.md (Phase 27 "Key Concepts Before You Begin" pattern at lines 24-44 — copy the structural approach, not the content)
    - docs/_templates/admin-template-ios.md (frontmatter, callout formats)
    - .planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-RESEARCH.md (lines 298-414 for VPP/LOB/Store research; lines 318-349 for the tables to use verbatim; lines 329-330 for silent install callout text)
    - .planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-CONTEXT.md (D-06, D-07, D-08, D-09, D-10 decisions)
  </read_first>
  <action>
Create `docs/admin-setup-ios/05-app-deployment.md` starting from the top:

**1. Frontmatter** (exactly):
```yaml
---
last_verified: 2026-04-16
review_by: 2026-07-15
applies_to: ADE
audience: admin
platform: iOS
---
```

**2. Platform gate** (4-line blockquote verbatim):
```markdown
> **Platform gate:** This guide covers iOS/iPadOS ADE configuration via Apple Business Manager and Intune.
> For macOS app deployment, see [macOS App Deployment](../admin-setup-macos/04-app-deployment.md).
> For iOS/iPadOS enrollment terminology, see the [Apple Provisioning Glossary](../_glossary-macos.md).
> Portal navigation may vary by Intune admin center version. See [Overview](00-overview.md#portal-navigation-note) for details.
```

**3. H1 title**: `# iOS/iPadOS App Deployment`

**4. Intro paragraph** (1 paragraph, 3-4 sentences): State that Intune supports four distinct iOS/iPadOS app deployment types: VPP device-licensed, VPP user-licensed, LOB (.ipa), and Store apps without VPP. Note that the VPP device-licensed vs user-licensed distinction is the single most common source of admin confusion. State that "managed apps" (deployed through Intune) can be verified through the Intune admin center; "unmanaged apps" (user-installed from App Store without Intune intermediation) have no install status tracking.

**5. `## Key Concepts Before You Begin`** section (per D-08). Structure this as three subsections using `###`:

**`### Managed vs Unmanaged Apps`** (1 paragraph):
Define: A managed app is any app deployed through Intune — VPP (device or user-licensed), LOB, or Store apps deployed as Required or Available through Intune. An unmanaged app is an app installed directly by the user from the App Store without Intune intermediation; Intune has no install-status record and cannot remove it via selective wipe. A VPP Available assignment transitions from "Available" tracking to "Managed" only after the user initiates install from Company Portal.

**`### VPP Device-Licensed vs User-Licensed`** (1-2 paragraphs, this is the D-08 focus and SC #2 driver):
State the distinction in one sentence: "Device-licensed VPP assigns one license per device (tied to serial number) and requires no Apple Account on the device; user-licensed VPP assigns one license per user (usable on up to 5 devices per personal Apple Account) and requires the user to sign in with an Apple Account to accept the VPP invitation."

Then state the critical consequence: "Device licensing is strongly recommended for corporate-owned devices because it eliminates the Apple Account dependency. User licensing is required for User Enrollment (BYOD) and for apps that include Books content." 

Add a brief `> **What breaks if misconfigured:**` callout:
```markdown
> **What breaks if misconfigured:** Deploying a VPP app as user-licensed to a device that has "Block App Store" in its device restrictions breaks the VPP invitation flow — the user cannot accept the invitation because the invitation requires App Store access. Symptom appears in: device (VPP invitation prompt appears but user cannot proceed) and Intune admin center (user license shows "Invitation sent" indefinitely).
```

**`### Silent Install Boundary`** (1 paragraph + the 6-row scenario table + the 🔒 callout):
Introduce: "Whether iOS prompts the user during app installation depends on four axes: supervision state (supervised or unsupervised), licensing type (device or user), enrollment type (ADE, Device Enrollment, User Enrollment, BYOD), and Apple Account presence. The authoritative Microsoft-documented behavior is summarized below."

Then include the **6-row end-user prompts table** verbatim (the one listed in interfaces above, rows 1-6).

Immediately below the table, place the prominent 🔒 silent install callout (verbatim from research line 329-330):
```markdown
> 🔒 **Supervised only:** Fully silent app installation (no user prompt of any kind) requires supervised mode AND device licensing. On unsupervised devices, device-licensed apps still install without an Apple Account prompt, but the user sees a one-time install confirmation. See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
```

**6. `## App Type Comparison Table`** section:
Introduce: "The table below summarizes the four deployment types against the attributes that most commonly affect deployment decisions. Use this as the starting point when choosing a deployment method for a specific app."

Then include the **12-row × 4-column comparison table** verbatim (the one listed in interfaces above).

STOP after the comparison table. Task 2 writes the per-type sections and Task 3 writes Verification/Failures/See Also.
  </action>
  <verify>
    <automated>bash -c 'test -f docs/admin-setup-ios/05-app-deployment.md && grep -c "^platform: iOS" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^## Key Concepts Before You Begin$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^### VPP Device-Licensed vs User-Licensed$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^## App Type Comparison Table$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "VPP Device-Licensed.*VPP User-Licensed.*LOB.*Store Apps" docs/admin-setup-ios/05-app-deployment.md | awk "{print (\$1 >= 1)}" | grep -q "^1$" && grep -F "🔒 **Supervised only:** Fully silent app installation" docs/admin-setup-ios/05-app-deployment.md && grep -F "../ios-lifecycle/00-enrollment-overview.md#supervision" docs/admin-setup-ios/05-app-deployment.md && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/admin-setup-ios/05-app-deployment.md` exits 0
    - `grep -c "^platform: iOS" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^applies_to: ADE" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^## Key Concepts Before You Begin$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^### Managed vs Unmanaged Apps$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^### VPP Device-Licensed vs User-Licensed$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^### Silent Install Boundary$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^## App Type Comparison Table$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -F "VPP Device-Licensed | VPP User-Licensed | LOB (.ipa) | Store Apps (without VPP)" docs/admin-setup-ios/05-app-deployment.md` finds at least 1 match
    - `grep -F "🔒 **Supervised only:** Fully silent app installation (no user prompt of any kind) requires supervised mode AND device licensing." docs/admin-setup-ios/05-app-deployment.md` finds at least 1 match
    - `grep -F "../ios-lifecycle/00-enrollment-overview.md#supervision" docs/admin-setup-ios/05-app-deployment.md` finds at least 1 match
    - The 6-scenario prompt table is present: `grep -c "| 6 | Corp — device licensed" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "| 1 | BYOD — user licensed" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - File line count >= 80 (`wc -l docs/admin-setup-ios/05-app-deployment.md`)
  </acceptance_criteria>
  <done>
Frontmatter, platform gate, intro, the three `### ` subsections under Key Concepts Before You Begin (Managed vs Unmanaged Apps; VPP Device-Licensed vs User-Licensed with its "What breaks" callout; Silent Install Boundary with the 6-scenario table and the 🔒 silent install callout), and the 12×4 App Type Comparison Table are all present. The silent install callout uses the exact verbatim text with the supervision anchor link.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Prerequisites and the four per-type deployment sections (VPP Device-Licensed, VPP User-Licensed, LOB, Store Apps) with steps, What-breaks callouts, and a second 🔒 callout on the VPP User-Licensed section</name>
  <files>docs/admin-setup-ios/05-app-deployment.md</files>
  <read_first>
    - docs/admin-setup-ios/05-app-deployment.md (current state from Task 1)
    - docs/admin-setup-macos/04-app-deployment.md (structural template for per-type sections, lines 51-130)
    - .planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-RESEARCH.md (lines 298-414 for per-type content — VPP mechanics lines 298-330; LOB prerequisites lines 353-381; Store apps lines 407-414)
  </read_first>
  <action>
Append the Prerequisites section and the four per-type deployment sections.

**1. `## Prerequisites`** with subdivided per-type sub-sections (`###`):

**`### VPP Prerequisites`** (bulleted list):
- Apple Business Manager account with at least one active Content Manager role
- VPP (Apps and Books) location token uploaded to Intune (Tenant administration > Connectors and tokens > Apple VPP tokens)
- App licenses purchased in ABM (Apps and Books page) — select Device or User licensing at purchase time (Device = recommended default)
- Annual token renewal configured (see Renewal / Maintenance below)

**`### LOB (.ipa) Prerequisites`** (bulleted list, from RESEARCH lines 355-360):
- Apple Developer Enterprise Program membership
- `.ipa` file signed with the organizational Distribution certificate
- `.mobileprovision` provisioning profile uploaded via **Apps** > **iOS/iPadOS** > **iOS app provisioning profiles** (separate object from the app itself)
- Unique `CFBundleVersion` and `CFBundleShortVersionString` in `Info.plist` for each uploaded version
- `.ipa` file size ≤ 2 GB (hard limit)

**`### Store App Prerequisites`** (bulleted list):
- Apple Account signed in on the device (free or paid App Store apps without VPP install under the user's own licensing)
- Device must have App Store access (not blocked by a "Block App Store" device restriction)

**2. `## VPP Device-Licensed`** section:
- Sub-heading `#### In Apple Business Manager`
  1. Sign in to [Apple Business Manager](https://business.apple.com).
  2. Navigate to **Apps and Books**. Search for the app. Purchase licenses with **Assign to: Devices** (device licensing). Confirm location.
- Sub-heading `#### In Intune admin center`
  1. Verify the VPP token is synced: **Tenant administration** > **Connectors and tokens** > **Apple VPP tokens**.
  2. The purchased app appears automatically under **Apps** > **iOS/iPadOS**.
  3. Select the app > **Properties** > **Assignments** > **Add group**. Use **Required** to device groups or user groups.
  4. Under **App information**, confirm **License type: Device**.
- Place this 🔒 callout immediately after the Intune admin center steps:
```markdown
> 🔒 **Supervised only:** Silent install of VPP device-licensed apps — the user sees no prompt of any kind on install — requires supervised mode. On unsupervised devices, device-licensed apps install without an Apple Account prompt but the user sees a one-time install confirmation. See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
```
- One `> **What breaks if misconfigured:**` callout: Assigning VPP device-licensed apps as Available (rather than Required) to device groups does not work — Available for device groups applies only to Required intent. Use Required for device groups; Available requires user groups. Symptom appears in: Intune admin center (assignment succeeds but) device (Company Portal does not list the app).

**3. `## VPP User-Licensed`** section:
- Sub-heading `#### In Apple Business Manager`
  1. Navigate to **Apps and Books**. Purchase licenses with **Assign to: Users**.
- Sub-heading `#### In Intune admin center`
  1. The app appears under **Apps** > **iOS/iPadOS** after VPP sync.
  2. Select the app > **Properties** > **App information** > confirm **License type: User**.
  3. Assign to user groups (Required or Available for enrolled — device group assignment is not available for user-licensed VPP).
- Describe the invitation flow in 1-2 sentences: First-time users receive a VPP invitation requiring App Store sign-in with a personal Apple Account (or Managed Apple Account for User Enrollment). Only after acceptance does the app install.
- Place this 🔒 callout immediately after the invitation flow description (satisfies the "VPP sections each contain a silent install 🔒 callout" truth):
```markdown
> 🔒 **Supervised only:** Silent install is NOT available for VPP user-licensed apps regardless of supervision state — the user always sees an Apple Account prompt for the VPP invitation. For silent install, switch to VPP device licensing, which is the fully silent option on supervised devices. See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
```
- One `> **What breaks if misconfigured:**` callout: Assigning VPP user-licensed apps on devices that have "Block App Store" in device restrictions breaks the VPP invitation flow — the user cannot accept the invitation (invitation requires App Store access). Symptom appears in: device (invitation shows but user cannot proceed) and Intune admin center (user license stuck in "Invitation sent" state).

**4. `## LOB (.ipa)`** section:
- Sub-heading `#### In Intune admin center`
  1. Navigate to **Apps** > **iOS/iPadOS** > **Add** > **Line-of-business app**.
  2. Select the signed `.ipa` file (≤ 2 GB).
  3. Configure app information (name, description, publisher, category, minimum OS version).
  4. Upload the `.mobileprovision` provisioning profile separately via **Apps** > **iOS app provisioning profiles** (mention Distribution certificate expires after 3 years; provisioning profile expires after 1 year).
  5. Assign: Required, Available for enrolled devices, Available with or without enrollment (unique to LOB), or Uninstall.
- State: "LOB apps can install silently on both supervised and unsupervised devices because Apple trusts the enterprise signature — no Apple Account required." No 🔒 callout for LOB silent install (it's signature-gated, not supervision-gated).
- One `> **What breaks if misconfigured:**` callout: Distribution certificate or provisioning profile expiring (Distribution = 3 years; provisioning profile = 1 year) causes all existing LOB installs to fail to launch with no install of new versions. Symptom appears in: device (app crashes on launch with "Unable to Verify App") and Intune admin center (push notification at 30-day expiry warning; stale apps silent-fail thereafter).
- One `> **What breaks if misconfigured:**` callout: Upload .ipa > 2 GB; Intune rejects the upload. Resolution: reduce app size or split into modules. Symptom appears in: Intune admin center (upload error).

**5. `## Store Apps (without VPP)`** section:
- Sub-heading `#### In Intune admin center`
  1. Navigate to **Apps** > **iOS/iPadOS** > **Add** > **iOS store app**.
  2. Search the App Store for the app (by name or App Store URL) — select the bundle ID to add it.
  3. Assign: Required (forces Company Portal download), Available for enrolled devices, or Uninstall.
- State explicitly: "Store apps without VPP require the user to have an Apple Account signed in on the device. Intune cannot assign licenses for these apps — it only tells the device 'install this bundle ID from the App Store.' Paid apps require the user to already own the app via their personal Apple Account; for paid corporate apps, use VPP instead."
- One `> **What breaks if misconfigured:**` callout: Deploying a paid Store app without VPP to corporate devices requires each user to have purchased the app personally — creates licensing and audit problems. Use VPP for all paid corporate apps. Symptom appears in: device (App Store shows "Buy" prompt rather than silent install).

Keep the whole section in the 80-120 line range additional to Task 1's output.
  </action>
  <verify>
    <automated>bash -c 'grep -c "^## Prerequisites$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^### VPP Prerequisites$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^### LOB (.ipa) Prerequisites$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^## VPP Device-Licensed$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^## VPP User-Licensed$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^## LOB (.ipa)$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^## Store Apps (without VPP)$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && CALLOUTS=$(grep -c "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/05-app-deployment.md) && test "$CALLOUTS" -ge 3 && grep "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/05-app-deployment.md | grep -v "ios-lifecycle/00-enrollment-overview.md#supervision" | wc -l | grep -q "^0$" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Prerequisites$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^### VPP Prerequisites$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^### LOB (.ipa) Prerequisites$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^### Store App Prerequisites$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^## VPP Device-Licensed$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^## VPP User-Licensed$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^## LOB (.ipa)$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^## Store Apps (without VPP)$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/05-app-deployment.md` returns at least `3` (Silent Install Boundary from Task 1 + VPP Device-Licensed callout + VPP User-Licensed callout)
    - Both VPP sections contain a 🔒 callout: `awk "/^## VPP Device-Licensed$/,/^## VPP User-Licensed$/" docs/admin-setup-ios/05-app-deployment.md | grep -c "🔒 \*\*Supervised only:\*\* Silent install"` returns at least `1`, AND `awk "/^## VPP User-Licensed$/,/^## LOB/" docs/admin-setup-ios/05-app-deployment.md | grep -c "🔒 \*\*Supervised only:\*\* Silent install"` returns at least `1`
    - Every 🔒 callout links to the supervision anchor: `grep "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/05-app-deployment.md | grep -v "ios-lifecycle/00-enrollment-overview.md#supervision" | wc -l` returns `0`
    - `grep -c "> \*\*What breaks if misconfigured:\*\*" docs/admin-setup-ios/05-app-deployment.md` returns at least `5`
    - `grep -c "2 GB" docs/admin-setup-ios/05-app-deployment.md` returns at least `1`
    - `grep -c "Apple Business Manager" docs/admin-setup-ios/05-app-deployment.md` returns at least `2`
  </acceptance_criteria>
  <done>
Prerequisites (3 subdivided prerequisite lists) and four per-type deployment sections (VPP Device-Licensed, VPP User-Licensed, LOB, Store Apps) are appended. Both VPP sections contain a 🔒 Supervised-only callout referencing silent install. Every 🔒 callout still points to `../ios-lifecycle/00-enrollment-overview.md#supervision`. Each section contains at least one "What breaks if misconfigured" callout.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append Verification section (naming 3 admin-center locations for managed app status), Configuration-Caused Failures table, Renewal/Maintenance table, See Also, and revision history</name>
  <files>docs/admin-setup-ios/05-app-deployment.md</files>
  <read_first>
    - docs/admin-setup-ios/05-app-deployment.md (current state from Tasks 1-2)
    - docs/admin-setup-macos/04-app-deployment.md (lines 124-158 for Verification / Config-Caused Failures / Renewal / See Also pattern)
    - .planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-RESEARCH.md (lines 383-404 for managed app status locations; lines 368-370 for LOB certificate/provisioning profile expiry cadence)
  </read_first>
  <action>
Append these final sections to `docs/admin-setup-ios/05-app-deployment.md`:

**1. `## Verification`** (MUST name the three admin center locations per D-10):

```markdown
## Verification

The Intune admin center exposes managed app installation status in three locations. Use the appropriate one depending on the scope of your check:

### 1. App-centric view (primary)

Navigate to **Apps** > **All apps** > [select app] > **Monitor** > **Device install status** or **User install status**. Shows per-device / per-user:

- Install Status (Installed, Install Pending, Failed, Not Applicable, Uninstalled)
- Timestamp of last status update
- Error code and detail (for Failed entries)

### 2. Device-centric view

Navigate to **Devices** > **All devices** > [select device] > **Managed Apps**. Shows end-to-end app lifecycle for the individual device: targeted, delivered, install status, last check-in.

### 3. Troubleshooting view

Navigate to **Troubleshoot + support** > **Troubleshoot** > enter user > **Managed Apps** pane. User-centric per-app history across all of the user's devices — use when a single user reports an install problem across multiple devices.

**Managed vs unmanaged distinction:** Managed apps = apps deployed via Intune (VPP, LOB, Store-as-Required/Available). Unmanaged apps = apps installed directly by the user from App Store with no Intune assignment record. Unmanaged apps do not appear in any of the three views above.

**Verification checklist:**

- [ ] VPP token shows Active status under Tenant administration > Connectors and tokens > Apple VPP tokens
- [ ] VPP app license count in Intune matches purchased quantity in ABM
- [ ] LOB provisioning profile shows > 30 days until expiry under Apps > iOS app provisioning profiles
- [ ] Test-assigned app shows "Installed" status for at least one target device in the App-centric view
- [ ] For VPP user-licensed apps, at least one user has accepted the VPP invitation (user license status transitions from "Invitation sent" to "Assigned")
```

**2. `## Configuration-Caused Failures`** (table with Phase 30 placeholder):

```markdown
## Configuration-Caused Failures

| Misconfiguration | Portal | Symptom | Runbook |
|------------------|--------|---------|---------|
| VPP user-licensed app assigned to device with "Block App Store" restriction | Intune | Invitation cannot be accepted; user license stuck in "Invitation sent" | iOS L1 runbooks (Phase 30) |
| VPP Available intent assigned to device groups | Intune | App does not appear in Company Portal; assignment silently fails | iOS L1 runbooks (Phase 30) |
| VPP device-licensed app assigned to unsupervised device, admin expected silent install | Intune | User sees one-time install prompt instead of silent install | iOS L1 runbooks (Phase 30) |
| VPP user-licensed app assigned to unsupervised device, admin expected silent install | Intune | User sees Apple Account prompt AND install prompt (silent install NOT available for user licensing) | iOS L1 runbooks (Phase 30) |
| LOB .ipa > 2 GB | Intune | Upload fails with size-limit error | iOS L1 runbooks (Phase 30) |
| LOB Distribution certificate expired (3-year lifecycle) | Intune | All existing LOB apps fail to launch with "Unable to Verify App" | iOS L1 runbooks (Phase 30) |
| LOB provisioning profile expired (1-year lifecycle) | Intune | Existing LOB app installs stop launching; new installs fail to complete | iOS L1 runbooks (Phase 30) |
| Store app deployed without VPP on corporate devices expecting silent install | Intune | User sees Apple Account / Get prompt; paid apps block with "Buy" prompt | iOS L1 runbooks (Phase 30) |
| VPP token not renewed before annual expiry | Intune | VPP apps stop syncing from ABM; existing installs unaffected | iOS L1 runbooks (Phase 30) |
| LOB app CFBundleVersion not incremented on re-upload | Intune | Devices do not detect a new version; existing installed version remains | iOS L1 runbooks (Phase 30) |
```

**3. `## Renewal / Maintenance`** table (VPP token + LOB provisioning profile):

```markdown
## Renewal / Maintenance

| Component | Renewal Period | Consequence of Lapse | Renewal Steps |
|-----------|---------------|---------------------|---------------|
| VPP (Apps and Books) location token | Annual (365 days) | VPP apps stop syncing from ABM; existing installs unaffected but no new assignments, revocations, or license management until renewed | 1. In ABM: **Preferences** > **Payments and Billing** > **Apps and Books** > **Content Tokens** > **Download**. 2. In Intune: **Tenant administration** > **Connectors and tokens** > **Apple VPP tokens** > [token] > **Edit** > **Upload new token** |
| LOB provisioning profile (.mobileprovision) | Annual (1 year from generation) | Existing LOB app installs fail to launch; new installs fail to complete | 1. Generate new `.mobileprovision` via Apple Developer Enterprise portal. 2. In Intune: **Apps** > **iOS app provisioning profiles** > [profile] > **Properties** > **Upload new provisioning profile**. Intune sends 30-day advance expiry notification. |
| LOB Distribution certificate | 3 years (from Apple Developer Enterprise) | All LOB apps signed with the expired certificate fail to launch with "Unable to Verify App" | Generate new Distribution certificate in Apple Developer Enterprise portal; re-sign and re-upload each LOB `.ipa` with the new certificate. |
| APNs certificate | 365 days | iOS MDM communication stops fleet-wide (see [APNs Certificate](01-apns-certificate.md)) | See [APNs Certificate](01-apns-certificate.md) for renewal steps |
```

**4. `## See Also`**:

```markdown
## See Also

- [Configuration Profiles](04-configuration-profiles.md) — Wi-Fi/VPN/Certificates delivery (prerequisite for enterprise app infrastructure)
- [Compliance Policies](06-compliance-policy.md)
- [ADE Enrollment Profile](03-ade-enrollment-profile.md) — supervised mode requirement for silent install
- [ABM/ADE Token](02-abm-token.md) — VPP token is administered in the same ABM account
- [APNs Certificate](01-apns-certificate.md)
- [iOS/iPadOS Admin Setup Overview](00-overview.md)
- [iOS/iPadOS ADE Lifecycle](../ios-lifecycle/01-ade-lifecycle.md)
- [iOS/iPadOS Enrollment Path Overview](../ios-lifecycle/00-enrollment-overview.md) — supervision concept anchor
- [macOS App Deployment](../admin-setup-macos/04-app-deployment.md) — parallel macOS guide
- [Apple Provisioning Glossary](../_glossary-macos.md)
```

**5. Previous/Next footer + revision history**:

```markdown
---
*Previous: [Configuration Profiles](04-configuration-profiles.md) | Next: [Compliance Policies](06-compliance-policy.md) | [Back to Overview](00-overview.md)*

---

| Date | Change | Author |
|------|--------|--------|
| 2026-04-16 | Initial version — iOS/iPadOS app deployment guide with Key Concepts section (managed vs unmanaged, VPP device vs user licensing, silent install boundary table), 4-column deployment type comparison, per-type sections with supervised-only silent install callouts, managed app status verification in three admin-center locations, configuration-caused failures, and VPP/LOB renewal cadences | -- |
```
  </action>
  <verify>
    <automated>bash -c 'grep -c "^## Verification$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^## Configuration-Caused Failures$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^## Renewal / Maintenance$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -c "^## See Also$" docs/admin-setup-ios/05-app-deployment.md | grep -q "^1$" && grep -F "Apps** > **All apps**" docs/admin-setup-ios/05-app-deployment.md && grep -F "Devices** > **All devices**" docs/admin-setup-ios/05-app-deployment.md && grep -F "Troubleshoot + support" docs/admin-setup-ios/05-app-deployment.md && PLACEHOLDERS=$(grep -c "iOS L1 runbooks (Phase 30)" docs/admin-setup-ios/05-app-deployment.md) && test "$PLACEHOLDERS" -ge 9 && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Verification$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^## Configuration-Caused Failures$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^## Renewal / Maintenance$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - `grep -c "^## See Also$" docs/admin-setup-ios/05-app-deployment.md` returns `1`
    - All three admin-center verification paths are present:
      - `grep -F "Apps** > **All apps** > [select app] > **Monitor** > **Device install status**" docs/admin-setup-ios/05-app-deployment.md` finds at least 1 match (App-centric)
      - `grep -F "Devices** > **All devices** > [select device] > **Managed Apps**" docs/admin-setup-ios/05-app-deployment.md` finds at least 1 match (Device-centric)
      - `grep -F "Troubleshoot + support" docs/admin-setup-ios/05-app-deployment.md` finds at least 1 match (Troubleshooting)
    - `grep -F "Managed vs unmanaged distinction" docs/admin-setup-ios/05-app-deployment.md` finds at least 1 match
    - `grep -c "iOS L1 runbooks (Phase 30)" docs/admin-setup-ios/05-app-deployment.md` returns at least `9`
    - Renewal table contains VPP token and LOB provisioning profile rows: `grep -c "VPP (Apps and Books) location token\|LOB provisioning profile" docs/admin-setup-ios/05-app-deployment.md` returns at least `2`
    - See Also contains cross-platform macOS link: `grep -F "../admin-setup-macos/04-app-deployment.md" docs/admin-setup-ios/05-app-deployment.md` finds at least 1 match
    - Final file line count: `wc -l docs/admin-setup-ios/05-app-deployment.md` returns a number >= 200 and ideally 250-350
    - No 🔒 callout regression: `grep "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/05-app-deployment.md | grep -v "ios-lifecycle/00-enrollment-overview.md#supervision" | wc -l` returns `0`
  </acceptance_criteria>
  <done>
Verification section explicitly names the three Intune admin center locations for managed app status (App-centric, Device-centric, Troubleshooting). Managed vs unmanaged distinction is explained inline. Configuration-Caused Failures table has at least 9 rows each using the "iOS L1 runbooks (Phase 30)" placeholder. Renewal table covers VPP token (annual), LOB provisioning profile (annual), LOB Distribution certificate (3 years), and APNs cert cross-reference. See Also links cross-platform to the macOS parallel guide. Final file is 200+ lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 28 Plan 02 is a documentation-only phase. No runtime code, no secrets, no authentication logic.

| Boundary | Description |
|----------|-------------|
| Document → Admin reader | Markdown describes Intune and ABM portal actions for app deployment. No credentials appear in the guide. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Information Disclosure (misrepresentation) | Silent install boundary claim in VPP Device-Licensed section | mitigate | 6-row scenario table from RESEARCH.md lines 318-325 sourced from Microsoft Learn "Manage Apple Volume-Purchased Apps" reference. Explicitly documents that only supervised + device-licensed yields fully silent install (scenario 6); all other combinations show at least one prompt. |
| T-28-02-02 | Information Disclosure (licensing confusion) | VPP user-licensed vs device-licensed distinction | mitigate | D-08 "Key Concepts Before You Begin" section explains the Apple Account requirement difference before the comparison table. What-breaks callout explicitly covers the "Block App Store breaks VPP user licensing" failure mode. |

ASVS L1 compliance is N/A for markdown documentation.
</threat_model>

<verification>
After all three tasks complete:

```bash
test -f docs/admin-setup-ios/05-app-deployment.md
wc -l docs/admin-setup-ios/05-app-deployment.md    # expect 200-350

# Required sections
grep -cE "^## (Key Concepts Before You Begin|App Type Comparison Table|Prerequisites|VPP Device-Licensed|VPP User-Licensed|LOB \(\.ipa\)|Store Apps \(without VPP\)|Verification|Configuration-Caused Failures|Renewal / Maintenance|See Also)$" docs/admin-setup-ios/05-app-deployment.md    # 11

# Silent install callouts
grep -c "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/05-app-deployment.md    # >= 3

# Supervision link target integrity
grep "🔒 \*\*Supervised only:\*\*" docs/admin-setup-ios/05-app-deployment.md | grep -v "ios-lifecycle/00-enrollment-overview.md#supervision" | wc -l    # 0

# Admin-center status verification locations
grep -cF "Device install status" docs/admin-setup-ios/05-app-deployment.md    # >= 1
grep -cF "Managed Apps" docs/admin-setup-ios/05-app-deployment.md              # >= 2 (device-centric + troubleshooting)
grep -cF "Troubleshoot + support" docs/admin-setup-ios/05-app-deployment.md    # >= 1

# Phase 30 placeholder
grep -c "iOS L1 runbooks (Phase 30)" docs/admin-setup-ios/05-app-deployment.md    # >= 9
```
</verification>

<success_criteria>
Phase SC #2 (per ROADMAP): An admin reading the app deployment guide understands the distinction between VPP device-licensed and user-licensed apps, knows that silent install requires supervision, and can check managed app installation status in Intune.

This plan satisfies SC #2 via: (a) the Key Concepts Before You Begin section with its `### VPP Device-Licensed vs User-Licensed` subsection establishing the Apple Account requirement difference; (b) the `### Silent Install Boundary` subsection with the verbatim 6-scenario prompt table and prominent 🔒 silent install callout, plus a second 🔒 callout in the VPP Device-Licensed section; (c) the Verification section naming the three Intune admin center locations for managed app status checking and explicitly distinguishing managed from unmanaged apps.
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/28-ios-admin-setup-configuration-apps-compliance/28-02-SUMMARY.md`
2. Confirm final line count of `05-app-deployment.md` (200-350 range)
3. Confirm all 🔒 callouts link to the supervision anchor (regression grep returns 0)
4. Note the number of rows in the Configuration-Caused Failures table and the 4-column Comparison Table
</output>
