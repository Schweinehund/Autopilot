---
phase: 134-v117-pin-16th-path-a-lineage-bump-terminal-close
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/v1.18-milestone-audit.mjs
  - scripts/validation/v1.18-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
autonomous: true
requirements: [HARN-12]
must_haves:
  truths:
    - "v1.18-milestone-audit.mjs exists as a Path-A copy of v1.17 with C1-C17 inherited verbatim and passes on the live v1.18 corpus"
    - "v1.18-audit-allowlist.json exists; pin count/identity is confirmed-unchanged from v1.17 via regenerate-supervision-pins.mjs --report (zero-drift, or targeted repoint if --report shows drift)"
    - "regenerate-supervision-pins.mjs gains a BASELINE_22 audit-trail comment; the BASELINE_9 line-coordinate array is byte-unchanged"
  artifacts:
    - path: "scripts/validation/v1.18-milestone-audit.mjs"
      provides: "16th Path-A audit harness (C1-C17)"
      contains: "v1.18-audit-allowlist.json"
    - path: "scripts/validation/v1.18-audit-allowlist.json"
      provides: "16th harness sidecar allowlist"
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "BASELINE_22 refresh audit-trail comment"
      contains: "BASELINE_22"
  key_links:
    - from: "scripts/validation/v1.18-milestone-audit.mjs"
      to: "scripts/validation/v1.18-audit-allowlist.json"
      via: "parseAllowlist() reference"
      pattern: "v1\\.18-audit-allowlist\\.json"
---

<objective>
HARN-12 (Atom 1) — author the 16th Path-A audit harness: `v1.18-milestone-audit.mjs` (verbatim Path-A copy of v1.17, C1-C17 inherited) + `v1.18-audit-allowlist.json` (byte-verbatim copy, `--report`-confirmed zero-delta) + the BASELINE_22 refresh comment.

Purpose: Each milestone gets its own `vX.Y-milestone-audit.mjs` + sidecar. The C1-C17 contract is inherited verbatim (no freshness-threshold change this closing cluster). The allowlist pins are line-coordinate-sensitive, so zero-drift must be positively confirmed against the live v1.18 corpus (not assumed) via `regenerate-supervision-pins.mjs --report`.
Output: three files — the harness, the sidecar, and the BASELINE_22 audit-trail comment in the frozen pin generator (array untouched).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md
@.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-CONTEXT.md
@scripts/validation/v1.17-milestone-audit.mjs
@scripts/validation/v1.17-audit-allowlist.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Path-A copy the 16th harness + sidecar; --report-confirm zero pin drift</name>
  <files>scripts/validation/v1.18-milestone-audit.mjs, scripts/validation/v1.18-audit-allowlist.json</files>
  <read_first>
    - scripts/validation/v1.17-milestone-audit.mjs (Path-A template — header comment, C1-C17 checks, the parseAllowlist() allowlist-filename reference near line ~82, the C17 subprocess-spawn of c17-eee-contract.mjs)
    - scripts/validation/v1.17-audit-allowlist.json (sidecar template — the 2 safetynet + 14 supervision + 5 c7_knox + 4 c9 pins, plus the `phase`/`generated` metadata fields)
    - .planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md (Research Target 3 — v1.17→v1.18 relabel points, the zero-line-shift rationale, and the mandatory `--report` confirmation step)
  </read_first>
  <action>
    Copy `v1.17-milestone-audit.mjs` verbatim to `v1.18-milestone-audit.mjs`, relabeling v1.17→v1.18 throughout (header comment, any `HARNESS` const, and the `parseAllowlist()` reference so it reads `v1.18-audit-allowlist.json`). Inherit C1-C17 verbatim — no freshness-threshold change this milestone (matches the source comment). Copy `v1.17-audit-allowlist.json` byte-verbatim to `v1.18-audit-allowlist.json`, updating ONLY the `phase` and `generated` metadata fields. Then run `node scripts/validation/regenerate-supervision-pins.mjs --report` (read-only) against the live v1.18 corpus and confirm ZERO stale pins — do NOT assume zero-delta. If `--report` shows any drift, apply the exact targeted `{file,line}` repoint it reports (same mechanism as v1.17's HYG-02 −1 shift; pin count/identity stays unchanged, only line numbers move) and record the deltas in the SUMMARY. Finally run `node scripts/validation/v1.18-milestone-audit.mjs --verbose` and confirm it exits 0 on the live corpus.
  </action>
  <acceptance_criteria>
    - `scripts/validation/v1.18-milestone-audit.mjs` exists and its allowlist reference is `v1.18-audit-allowlist.json` (grep confirms, zero `v1.17-audit-allowlist` residue).
    - `node scripts/validation/regenerate-supervision-pins.mjs --report` reports zero stale pins against the v1.18 corpus (or SUMMARY records the exact repointed `{file,line}` deltas with count/identity unchanged).
    - `node scripts/validation/v1.18-milestone-audit.mjs --verbose` exits 0.
    - Pin COUNT in v1.18-audit-allowlist.json equals the v1.17 sidecar's pin count (identity unchanged).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/v1.18-milestone-audit.mjs --verbose</automated>
  </verify>
  <done>16th Path-A harness + sidecar exist, C1-C17 inherited, allowlist points at v1.18 sidecar, harness exits 0, and pin drift is positively confirmed zero (or repointed and recorded).</done>
</task>

<task type="auto">
  <name>Task 2: Add the BASELINE_22 refresh audit-trail comment</name>
  <files>scripts/validation/regenerate-supervision-pins.mjs</files>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs (the BASELINE_21 comment block near lines 492-502 — the exact template to replicate; and the BASELINE_9 line-coordinate array that MUST stay untouched)
    - .planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-RESEARCH.md (Research Target 3 — exact BASELINE_22 comment text + the Wave-0-vs-pre-Atom-1 anchor-capture discipline)
  </read_first>
  <action>
    Capture the pre-Atom-1 anchor via `git rev-parse HEAD` immediately before editing (per the Phase-119/125/128 discipline: if a Jira-sync commit landed since Wave-0, the true pre-Atom-1 predecessor is whatever HEAD is at THIS moment, not the Wave-0 anchor from Plan 134-01). Add a BASELINE_22 audit-trail comment mirroring the BASELINE_21 block: record that it closes the BASELINE_21 v1.17 carry-over per the HARN-12 contract, that v1.18 line positions were re-verified against the captured HEAD SHA, that the BASELINE_9 line-coordinate array is NOT altered (comment-only event), and that BASELINE_23 refreshes at the next milestone close per the Path-A inheritance chain. This file is FROZEN except this append-only comment — do NOT convert it to readAtClose (D-02 from Phase 133), do NOT modify the BASELINE_9 array.
  </action>
  <acceptance_criteria>
    - `grep -c "BASELINE_22" scripts/validation/regenerate-supervision-pins.mjs` returns >= 1.
    - The BASELINE_9 line-coordinate array is byte-unchanged (`git diff` shows only added comment lines, zero array-value changes).
    - `node scripts/validation/regenerate-supervision-pins.mjs --report` still runs clean (comment does not break parsing).
    - The captured pre-Atom-1 HEAD SHA is embedded in the comment and recorded in SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "BASELINE_22" scripts/validation/regenerate-supervision-pins.mjs && node scripts/validation/regenerate-supervision-pins.mjs --report >/dev/null && echo OK</automated>
  </verify>
  <done>BASELINE_22 audit-trail comment present with the captured pre-Atom-1 HEAD SHA; BASELINE_9 array untouched; --report still clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live corpus → allowlist pins | The sidecar pins reference exact `{file,line}` positions; a silent line drift would mask a real C2 violation |
| frozen generator → BASELINE array | regenerate-supervision-pins.mjs is a frozen surface; only an append-only comment is sanctioned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-03 | Tampering | v1.18-audit-allowlist.json pins | mitigate | `--report`-confirm zero drift against live corpus; targeted repoint only if reported; count/identity asserted unchanged |
| T-134-04 | Tampering | regenerate-supervision-pins.mjs BASELINE_9 array | mitigate | Append-only comment; `git diff` confirms array byte-unchanged (acceptance criterion) |
| T-134-05 | Repudiation | harness C1-C17 drift | accept | C1-C17 inherited verbatim, no threshold change; harness `--verbose` exit-0 is the objective evidence |

No package-manager installs occur (no `T-134-SC`). Internal validation tooling only — no runtime/auth/network/user-input surface.
</threat_model>

<verification>
- `node scripts/validation/v1.18-milestone-audit.mjs --verbose` exits 0.
- `regenerate-supervision-pins.mjs --report` reports zero drift (or repoints recorded); BASELINE_9 array byte-unchanged.
- `BASELINE_22` present in the generator.
</verification>

<success_criteria>
HARN-12 (Atom 1) satisfied: 16th Path-A harness + sidecar exist and pass, pin drift positively confirmed zero (or repointed with count/identity unchanged), BASELINE_22 audit-trail comment recorded with the pre-Atom-1 anchor.
</success_criteria>

<output>
Create `.planning/phases/134-v117-pin-16th-path-a-lineage-bump-terminal-close/134-02-SUMMARY.md` when done. MUST record the pre-Atom-1 HEAD SHA and the pin-drift result (zero-delta or the exact repoints).
</output>
