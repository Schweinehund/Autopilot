---
phase: 47-integration-re-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_glossary-android.md
  - docs/reference/android-capability-matrix.md
  - docs/admin-setup-android/00-overview.md
  - scripts/validation/v1.4.1-audit-allowlist.json
autonomous: true
requirements:
  - AEINTEG-01
must_haves:
  truths:
    - "Glossary line 16 alphabetical index is alphabetically coherent across the post-Phase-46 24-entry set (afw#setup → Zero-Touch Enrollment)"
    - "Glossary H3 section ordering under each parent H2 is consistent with line 16 index (Knox / KME / KPE sub-order verified, Private Space alphabetical placement verified)"
    - "Capability matrix preserves COPE column at index 1 across all 5 sub-tables (Phase 46 D-20 lock); Knox row populated at #knox-mobile-enrollment-row anchor (Phase 44 lock); AOSP per-OEM matrix link present at lines 121-127 (Phase 45 D-25 lock)"
    - "00-overview.md Mermaid renders 6 branches with KME branch present (Phase 44 D-05 lock); AOSP-Path terminates at single 'Phase 39 — AOSP stub' leaf (D-04 scope-out for per-OEM Mermaid leaves)"
    - "Setup Sequence numbered list aligned with Mermaid branch order"
    - "v1.4.1-audit-allowlist.json supervision_exemptions[] line coordinates re-baselined post any glossary line shift; pin annotations record Plan 47-01 shift origin"
    - "regenerate-supervision-pins.mjs --self-test exits 0 (classifier reproduces hand-authored pin set with shifted coordinates)"
  artifacts:
    - path: "docs/_glossary-android.md"
      provides: "Alphabetically coherent glossary with line 16 index"
      contains: "[afw#setup](#afw-setup)"
    - path: "docs/reference/android-capability-matrix.md"
      provides: "Column-order coherent capability matrix"
      contains: "COPE (WPCO / Corp-Owned Work Profile)"
    - path: "docs/admin-setup-android/00-overview.md"
      provides: "6-branch Mermaid (verified)"
      contains: "Knox - KME Samsung-only"
    - path: "scripts/validation/v1.4.1-audit-allowlist.json"
      provides: "Supervision pin coordinates current post-Plan-47-01"
  key_links:
    - from: "docs/_glossary-android.md line 16"
      to: "H3 sections under each H2"
      via: "anchor links match section ordering"
      pattern: "\\[Supervision\\]\\(#supervision\\)"
    - from: "scripts/validation/v1.4.1-audit-allowlist.json supervision_exemptions[]"
      to: "docs/_glossary-android.md edited line numbers"
      via: "pin coordinate update + annotation string"
      pattern: "re-verified 2026-04-25 post Plan 47-01"
---

<objective>
Surgical re-canonicalization at the three SC#1 hotspot files identified in CONTEXT D-01 and D-05: glossary line 16 alphabetical index, capability matrix column-order coherence, and 00-overview.md Mermaid leaf ordering. Single atomic commit per AEINTEG-01.

Per CONTEXT D-04 the scope is FIXED at these three hotspots — out of scope: re-touching capability matrix lines 121-127 (Phase 45-10 locked), reordering shipped content beyond the named hotspots, reopening Phase 44 D-05 (KME branch ordering) or Phase 46 D-20 (COPE column at index 1).

Per RESEARCH §Open Questions 1+2 and §Current On-Disk State, the on-disk baseline for all three files is already substantially canonical — Plan 47-01 is verification-driven re-canonicalization. Where verification finds drift, surgical edits land. Where verification finds coherence, no-op (zero-edit verification recorded in commit message).

Pin-shift maintenance per CONTEXT D-06 + RESEARCH §Pin-Shift Maintenance Recipe: any glossary line shift propagates to v1.4.1-audit-allowlist.json supervision_exemptions[] coordinates atomically in the same commit; regenerate-supervision-pins.mjs --self-test must exit 0 before commit.

Purpose: Close AEINTEG-01 — the atomic-rebuild verb in ROADMAP SC#1 cannot be satisfied by zero-edit verification alone (CONTEXT D-02), but the on-disk state shipped via Phase 44/45/46 append-only contracts (D-37) means the atomic rebuild collapses to surgical re-canonicalization. The single commit certifies that Phase 47 has surveyed all three hotspots and either left them alone (if coherent) or surgically corrected them (if drift found).

Output: Single atomic commit touching docs/_glossary-android.md + docs/reference/android-capability-matrix.md + docs/admin-setup-android/00-overview.md + scripts/validation/v1.4.1-audit-allowlist.json (the latter only if pin-shift required).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-integration-re-audit/47-CONTEXT.md
@.planning/phases/47-integration-re-audit/47-RESEARCH.md
@.planning/phases/47-integration-re-audit/47-PATTERNS.md
@.planning/phases/47-integration-re-audit/47-VALIDATION.md
@.planning/milestones/v1.4-MILESTONE-AUDIT.md
@docs/_glossary-android.md
@docs/reference/android-capability-matrix.md
@docs/admin-setup-android/00-overview.md
@scripts/validation/v1.4.1-audit-allowlist.json

<interfaces>
<!-- Locked invariants from prior phases — Plan 47-01 MUST preserve these byte-for-byte: -->

Phase 44 D-03 (locked): admin doc 07 line 93 anti-paste blockquote ↔ ZT line 93 byte-identical (Plan 47-01 does not touch these files).

Phase 44 D-05 (locked): 6-branch Mermaid in 00-overview.md with KME branch — order is locked, not re-decided.

Phase 45 D-19 (locked): triage tree single-target ANDR29 preservation (capability matrix retrofit at lines 121-127 already shipped Plan 45-10 commit 3400bff — DO NOT re-touch).

Phase 45 D-25 (locked): append-only contract on shared files; Plan 47-01 surgical edits respect already-shipped content.

Phase 46 D-20 (locked): capability matrix COPE column at index 1 across all 5 sub-tables — verified, not re-decided.

Phase 46 D-22 (locked): Cross-Platform Equivalences section ZERO new paired rows for COPE — verified, not re-authored.

Phase 46 Wave 2 commit ce5ffc0 (precedent): atomic 5-file retrofit + sidecar pin-shift in single commit.

Phase 45-10 commit 3400bff (precedent): atomic 6-file edit + 1 deletion + sidecar pin-shift.

Sidecar pin annotation pattern (verbatim from current sidecar entries):
"re-verified 2026-04-25 post Plan 46-02 Wave 2; line shifted +13 from Plan 45-10 baseline due to Private Space H3 insertion + see-also blockquotes + frontmatter freshness add"

Plan 47-01 follows the same form, substituting "Plan 47-01" and recording the new shift delta + cause description.

Current sidecar supervision_exemptions[] glossary pin coordinates (verified by RESEARCH live read):

| line | reason fragment |
|------|-----------------|
| 16 | Alphabetical index Supervision link |
| 46 | COBO Cross-platform note |
| 66 | Fully Managed Cross-platform note |
| 76 | Supervision H3 heading |
| 78 | Supervision blockquote body |
| 172 | MHS cross-platform note |
| 188 | Version History supervision row |

The capability matrix supervision pins (lines 78-88) are NOT affected by glossary edits.

Pin-shift workflow (RESEARCH §Pin-Shift Maintenance Recipe — copy-paste):
1. Edit docs/_glossary-android.md (line 16 re-sort + any drift-correction H3 reorder).
2. Run: node scripts/validation/regenerate-supervision-pins.mjs --report
3. Update each affected supervision_exemptions[].line value in v1.4.1-audit-allowlist.json.
4. Append to each affected pin's reason string: "re-verified 2026-04-25 post Plan 47-01; line shifted +N from Plan 46-02 Wave 2 baseline due to <cause>"
5. Run: node scripts/validation/regenerate-supervision-pins.mjs --self-test  (must exit 0)
6. Run: node scripts/validation/v1.4.1-milestone-audit.mjs               (must exit 0; C2 supervision PASS)
7. Single atomic commit (docs/ + sidecar).
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-canonicalize docs/_glossary-android.md line 16 alphabetical index + verify H3 section ordering</name>
  <files>docs/_glossary-android.md</files>
  <read_first>
    - .planning/phases/47-integration-re-audit/47-CONTEXT.md (D-01, D-04, D-05, D-06, D-31, D-37 — locked decisions)
    - .planning/phases/47-integration-re-audit/47-RESEARCH.md (Current On-Disk State §1, Pin-Shift Maintenance Recipe, Open Question 1)
    - .planning/phases/47-integration-re-audit/47-PATTERNS.md (docs/_glossary-android.md analog section)
    - docs/_glossary-android.md (full file — read line 16 + every H2/H3 heading to verify alphabetical-section coherence with index)
    - scripts/validation/v1.4.1-audit-allowlist.json (current supervision_exemptions[] pin coordinates baseline)
  </read_first>
  <action>
    Step 1 — Read line 16 verbatim. Current 24-entry index per RESEARCH:

    `[afw#setup](#afw-setup) | [AMAPI](#amapi) | [BYOD](#byod) | [COBO](#cobo) | [COPE](#cope) | [Corporate Identifiers](#corporate-identifiers) | [Dedicated](#dedicated) | [DPC](#dpc) | [EMM](#emm) | [Entra Shared Device Mode](#entra-shared-device-mode) | [Fully Managed](#fully-managed) | [Knox](#knox) | [KME](#kme) | [KPE](#kpe) | [Managed Google Play](#managed-google-play) | [Managed Home Screen](#managed-home-screen) | [OEMConfig](#oemconfig) | [Play Integrity](#play-integrity) | [Private Space](#private-space) | [Supervision](#supervision) | [User Enrollment](#user-enrollment) | [Work Profile](#work-profile) | [WPCO](#wpco) | [Zero-Touch Enrollment](#zero-touch-enrollment)`

    Step 2 — Verify alphabetical order. Per RESEARCH the index is "substantially alphabetical" with K-entries (Knox / KME / KPE) in correct sub-order. If ANY entry is out of order, surgically correct it; otherwise leave the line untouched and record "verification-only no-op" in the task summary.

    Step 3 — Walk every H2/H3 heading in the file and verify each H3 appears under the correct parent H2 in the same alphabetical order as the line 16 index. Per CONTEXT D-05: "verify alphabetical placement of all H3s under their parent H2 sections is consistent with the index ordering". Locked invariants from prior phases:
    - Knox / KME / KPE H3s shipped under Provisioning Methods H2 by Phase 44 Plan 44-06 (do not move)
    - OEMConfig H3 shipped by Phase 45 Plan 45-10 commit 3400bff (do not move)
    - Private Space H3 shipped by Phase 46 Plan 46-02 Wave 2 commit ce5ffc0 under Ownership & Management Scope H2, alphabetical between Play Integrity and Supervision (do not move)
    - All Phase 34/35/36/37/38 entries are in their original Phase 34 D-locked positions

    Step 4 — If H3 placement drift is found anywhere, surgically reorder ONLY the offending H3 block (heading + body); preserve all frontmatter and line 1-15 content unchanged. If no drift: record "verification-only no-op" and leave file untouched.

    Step 5 — If ANY edit was made, capture the BEFORE → AFTER line counts so Task 2 can compute pin shift delta. If no edit: skip Task 2 sidecar updates entirely (sidecar remains unchanged).

    Frontmatter contract: `last_verified` and `review_by` fields STAY at their current values unless this plan ships a content edit (60-day rule per Phase 34 D-14 / CONTEXT D-35) — only update if the file content was actually edited.

    DO NOT touch: any line outside line 16 unless H3 reorder is required; any frontmatter unless content was edited (60-day freshness rule); the version history table; the file header; any of the 18 supervision_exemptions[]-pinned lines unless the edit shifts them organically.
  </action>
  <verify>
    <automated>node scripts/validation/regenerate-supervision-pins.mjs --report</automated>
  </verify>
  <acceptance_criteria>
    - Line 16 of docs/_glossary-android.md is in alphabetical order across all 24 entries (afw#setup / AMAPI / BYOD / COBO / COPE / Corporate Identifiers / Dedicated / DPC / EMM / Entra Shared Device Mode / Fully Managed / Knox / KME / KPE / Managed Google Play / Managed Home Screen / OEMConfig / Play Integrity / Private Space / Supervision / User Enrollment / Work Profile / WPCO / Zero-Touch Enrollment) — verifiable via grep
    - Knox / KME / KPE adjacent in that order on line 16 (Phase 44 D-01 5-SKU sub-order)
    - Private Space appears between Play Integrity and Supervision on line 16 (Phase 46 D-22 anchor placement)
    - OEMConfig appears between Managed Home Screen and Play Integrity on line 16 (Phase 45-10 placement)
    - Every H3 heading in the file appears under the correct parent H2 in alphabetical order consistent with the line 16 index
    - regenerate-supervision-pins.mjs --report exits 0 with no un-pinned Tier-1 occurrences
    - File frontmatter `last_verified` field unchanged unless content edit occurred
  </acceptance_criteria>
  <done>
    Line 16 alphabetical index verified canonical (or surgically corrected to canonical); H3 section ordering consistent with index. Pin-helper --report exits clean. Any line shifts captured for Task 2 sidecar update. If no edits made, file untouched and Task 2 skipped.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-baseline v1.4.1-audit-allowlist.json supervision pin coordinates IF Task 1 made edits</name>
  <files>scripts/validation/v1.4.1-audit-allowlist.json</files>
  <read_first>
    - .planning/phases/47-integration-re-audit/47-CONTEXT.md (D-06, D-31, D-37 — sidecar additive-only contract)
    - .planning/phases/47-integration-re-audit/47-RESEARCH.md (Pin-Shift Maintenance Recipe, Pitfall 3)
    - .planning/phases/47-integration-re-audit/47-PATTERNS.md (Pin-Shift Maintenance Workflow, sidecar §)
    - scripts/validation/v1.4.1-audit-allowlist.json (full — note current schema_version, generated date, supervision_exemptions[] count = 18, current line values for each glossary pin)
    - docs/_glossary-android.md (post-Task-1 state — confirm new line numbers for the 7 glossary supervision pins listed in interfaces)
  </read_first>
  <action>
    GUARD: If Task 1 made no content edits (verification-only no-op) skip this task entirely; the sidecar requires no change. Record in task SUMMARY: "Task 1 was no-op; sidecar unchanged."

    Step 1 — Run `node scripts/validation/regenerate-supervision-pins.mjs --report` and capture output. The --report mode lists every detected supervision occurrence + whether it is currently pinned. Identify shifted pin lines.

    Step 2 — For each glossary supervision_exemptions[] entry whose line shifted (compare to baseline coordinates: 16, 46, 66, 76, 78, 172, 188 — all in `docs/_glossary-android.md`), update the `"line"` field to the new line number.

    Step 3 — Append to each updated pin's `"reason"` string a Plan-47-01 annotation in the verbatim form:
    `"re-verified 2026-04-25 post Plan 47-01; line shifted +N from Plan 46-02 Wave 2 baseline due to <description of glossary change>"`
    Where N is signed integer (positive or negative) and `<description>` is concrete (e.g., "Knox H3 reorder under Provisioning Methods", "OEMConfig alphabetical re-position", or whatever Task 1 changed). The capability matrix supervision pins (file paths matching docs/reference/android-capability-matrix.md, current line values 78-88) are NOT affected — leave them untouched.

    Step 4 — Verify JSON parses post-edit:
    `node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.4.1-audit-allowlist.json','utf8'))"`

    Step 5 — Run `node scripts/validation/regenerate-supervision-pins.mjs --self-test` — MUST exit 0. If non-zero, pin coordinates are wrong; iterate.

    Step 6 — Schema invariants preserved: schema_version unchanged at "1.0", generated unchanged, phase unchanged, safetynet_exemptions[] count unchanged at 4, supervision_exemptions[] count unchanged at 18, cope_banned_phrases[] unchanged at 3 (Plan 47-02 owns C9 extension — DO NOT touch cope_banned_phrases here).

    Additive-only contract per CONTEXT D-31 + Phase 43 D-26: never delete pin entries, never reorder them, only update `line` and append to `reason`.
  </action>
  <verify>
    <automated>node scripts/validation/regenerate-supervision-pins.mjs --self-test &amp;&amp; node scripts/validation/v1.4.1-milestone-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - JSON file parses cleanly (no syntax errors); schema_version = "1.0" unchanged
    - supervision_exemptions[] still has exactly 18 entries (no add, no delete)
    - safetynet_exemptions[] still has exactly 4 entries (untouched)
    - cope_banned_phrases[] still has exactly 3 entries (untouched — Plan 47-02 territory)
    - Each shifted glossary supervision pin has updated `"line"` value matching post-Task-1 docs/_glossary-android.md state
    - Each shifted pin has annotation appended to `"reason"` matching pattern: `re-verified 2026-04-25 post Plan 47-01; line shifted +N from Plan 46-02 Wave 2 baseline due to <description>`
    - regenerate-supervision-pins.mjs --self-test exits 0 (identical-diff to hand-authored set with new coordinates)
    - v1.4.1-milestone-audit.mjs exits 0 (C1-C5 PASS post-edit)
    - Capability matrix supervision pins (lines 78-88) untouched
  </acceptance_criteria>
  <done>
    Sidecar pin coordinates updated atomically with glossary edits (or untouched if Task 1 was no-op). Pin helper --self-test exits 0. Harness blocking checks C1-C5 PASS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify capability matrix column-order coherence + Mermaid leaf ordering (zero-edit verification expected; surgical correction only on drift)</name>
  <files>docs/reference/android-capability-matrix.md, docs/admin-setup-android/00-overview.md</files>
  <read_first>
    - .planning/phases/47-integration-re-audit/47-CONTEXT.md (D-01, D-04, D-05, D-37 — surgical scope; locked invariants from Phase 44 D-05, Phase 45 D-19, Phase 45 D-25, Phase 46 D-20, Phase 46 D-22)
    - .planning/phases/47-integration-re-audit/47-RESEARCH.md (Current On-Disk State §2 + §3, Pitfall 6, Open Question 2)
    - .planning/phases/47-integration-re-audit/47-PATTERNS.md (capability matrix § + 00-overview.md § — copy-paste header pattern + Mermaid pattern)
    - docs/reference/android-capability-matrix.md (full file — read all 5 sub-table headers + Cross-Platform Equivalences section + lines 121-127 anchor region; verify each sub-table column order matches Phase 46 D-20 COPE-at-index-1 invariant)
    - docs/admin-setup-android/00-overview.md (lines 25-43 — read Mermaid block + Setup Sequence numbered list)
  </read_first>
  <action>
    PART A — docs/reference/android-capability-matrix.md verification:

    Step A1 — Read every sub-table header line. Per Phase 46 D-20 + RESEARCH §2, the canonical column order across all 5 sub-tables (Enrollment / Configuration / App Deployment / Compliance / Software Updates) is:
    `| Feature | COBO (Fully Managed) | COPE (WPCO / Corp-Owned Work Profile) | BYOD (Work Profile) | Dedicated (COSU) | ZTE (Zero-Touch) | AOSP |`

    Step A2 — Confirm COPE column index = 1 (immediately after Feature column header) across ALL 5 sub-tables. Confirm AOSP column is rightmost. If drift found in any sub-table, surgically correct ONLY the offending header line + its alignment row (per Phase 46 D-22 ZERO-new-paired-rows in Cross-Platform Equivalences — do NOT add or remove rows).

    Step A3 — Verify Cross-Platform Equivalences section structure preserved (RESEARCH §3 + Phase 46 D-22): section intro present, paired-row header `iOS Supervision (ADE-enrolled)` present at line ~83, paired-row body at line ~84, all 6 supervision pins (lines 78-88) intact. NO new paired rows added; do NOT modify Cross-Platform Equivalences content.

    Step A4 — Verify lines 121-127 still contain the AOSP per-OEM matrix link (Phase 45-10 commit 3400bff lock). DO NOT re-touch this region per CONTEXT D-04.

    Step A5 — Verify #knox-mobile-enrollment-row anchor is present (Phase 44-04 lock). DO NOT rename or relocate.

    If verification finds zero drift, leave file untouched; record "zero-edit verification: cross-table column coherence confirmed" in task SUMMARY.

    PART B — docs/admin-setup-android/00-overview.md Mermaid verification:

    Step B1 — Read the Mermaid block (per RESEARCH §2 it is at lines ~25-37). Confirm 6-branch layout:
    - COBO / BYOD WP / Dedicated → MGP path
    - Zero-Touch Enrollment → MGP → ZT path
    - AOSP → AOSP_PATH single leaf "Phase 39 — AOSP stub"
    - Knox - KME Samsung-only → KNOX path

    Step B2 — Per CONTEXT D-04 + RESEARCH Pitfall 6: AOSP-Path MUST terminate at single leaf — DO NOT add per-OEM AOSP leaves (RealWear / Zebra / Pico / HTC / Meta) to the Mermaid. Phase 45 scoped per-OEM content to admin-setup-android/ docs only. Verify this discipline holds.

    Step B3 — Verify Knox branch ordering (Phase 44 D-05 lock — KME branch at the canonical position locked in Phase 44). DO NOT re-decide branch order.

    Step B4 — Read Setup Sequence numbered list (per RESEARCH §2 at lines ~39-43). Verify items 1/2/3 align with branch order: MGP binding → ZT portal → KME. If misalignment found, surgically correct ONLY the numbered list.

    If verification finds zero drift, leave file untouched; record "zero-edit verification: 6-branch Mermaid + Setup Sequence aligned" in task SUMMARY.

    Frontmatter contract: 60-day freshness rule (Phase 34 D-14 / CONTEXT D-35). `last_verified` and `review_by` fields STAY unchanged unless this task ships a content edit. ONLY update if Step A or Step B made content changes.

    NOTE: The expected outcome based on RESEARCH §Open Question 2 + §Pitfall 6 is "zero-edit verification" — both files are already canonical post-Phase-44/45/46. Surgical correction is the exception, not the rule. The task SUMMARY must explicitly state which files (if any) were touched and why.
  </action>
  <verify>
    <automated>node scripts/validation/v1.4.1-milestone-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - docs/reference/android-capability-matrix.md: COPE column appears at index 1 (immediately after Feature) across all 5 sub-tables — verifiable by grep counting `| COPE` after `| Feature`
    - Cross-Platform Equivalences section (lines 78-88) preserved verbatim — zero new paired rows; supervision pins intact
    - Lines 121-127 AOSP per-OEM matrix link region UNTOUCHED (Phase 45-10 lock)
    - #knox-mobile-enrollment-row anchor present and unrenamed
    - docs/admin-setup-android/00-overview.md: Mermaid block contains exactly 6 branches with KME branch present
    - Mermaid AOSP_PATH terminates at single leaf "Phase 39 — AOSP stub" — NO per-OEM leaves added
    - Setup Sequence numbered list aligned with Mermaid branch order
    - v1.4.1-milestone-audit.mjs exits 0 (C1-C5 PASS; C6 informational PASS-by-design)
    - Frontmatter `last_verified` field unchanged on any file that was not actually edited
  </acceptance_criteria>
  <done>
    Capability matrix column coherence verified canonical across 5 sub-tables (or surgically corrected on drift); Mermaid 6-branch layout + AOSP single-leaf form verified; Setup Sequence aligned. Harness exits 0. Task SUMMARY explicitly records files touched (or zero-edit verification).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem | Plan 47-01 reads/writes static markdown + JSON in repo; no network surface, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01-01 | Tampering | scripts/validation/v1.4.1-audit-allowlist.json | accept | [VERIFIED low risk] Sidecar JSON is committed to git; CI parse job (Phase 43 Plan 08 audit-harness-integrity.yml) enforces JSON validity on every commit; Plan 47-01 changes flow through this gate. No runtime user input. |
| T-47-01-02 | Information Disclosure | docs/_glossary-android.md / docs/reference/android-capability-matrix.md / docs/admin-setup-android/00-overview.md | accept | [ASSUMED low risk per RESEARCH §Security Domain] Public documentation files — no credentials, secrets, or PII. Intended for IT-admin readership; cleared for git tracking by all prior phases. |

ASVS L1 assessment per RESEARCH §Security Domain: V2/V3/V4/V5/V6 do NOT apply — Plan 47-01 modifies documentation and a static sidecar JSON only. No authentication, session, access-control, input-validation, or cryptography surface introduced or modified.
</threat_model>

<verification>
After Task 3, run from repo root:

1. `node scripts/validation/regenerate-supervision-pins.mjs --self-test` → exit 0 mandatory
2. `node scripts/validation/v1.4.1-milestone-audit.mjs --verbose` → exit 0 mandatory; C1 SafetyNet PASS, C2 supervision PASS (18 pins), C3 AOSP wordcount PASS, C4 deferred-file guard PASS, C5 freshness PASS, C6/C7/C9 informational PASS-by-design
3. `grep -nE "^- \[" docs/_glossary-android.md | head -30` → ordered output (alphabetical-section coherent)
4. `grep -nE "^\| (Feature\|Mode\|Setting)" docs/reference/android-capability-matrix.md | head -10` → COPE second column across all 5 sub-tables
5. `grep -A 20 "graph TD\|flowchart TD" docs/admin-setup-android/00-overview.md` → KME branch present, AOSP single leaf
6. JSON parse: `node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.4.1-audit-allowlist.json','utf8'))"` → no errors
</verification>

<success_criteria>
- Line 16 of docs/_glossary-android.md is alphabetically canonical (24 entries from afw#setup → Zero-Touch Enrollment)
- All H3 sections in glossary appear under correct parent H2 in alphabetical order consistent with line 16 index
- Capability matrix COPE column at index 1 across all 5 sub-tables (Phase 46 D-20 invariant preserved)
- Capability matrix lines 121-127 AOSP per-OEM matrix link region UNTOUCHED (Phase 45-10 invariant preserved)
- 00-overview.md Mermaid contains 6 branches with KME branch (Phase 44 D-05 invariant preserved); AOSP-Path = single "Phase 39 — AOSP stub" leaf (D-04 scope-out preserved)
- v1.4.1-audit-allowlist.json supervision_exemptions[] line coordinates current post-edit; pin annotation strings record Plan 47-01 shift origin (only if Task 1 made edits)
- regenerate-supervision-pins.mjs --self-test exits 0
- v1.4.1-milestone-audit.mjs exits 0 (5 blocking + 3 informational checks PASS)
- Atomic single commit touching only docs/_glossary-android.md + docs/reference/android-capability-matrix.md + docs/admin-setup-android/00-overview.md + (conditionally) scripts/validation/v1.4.1-audit-allowlist.json
- AEINTEG-01 closure recorded in 47-01-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/47-integration-re-audit/47-01-SUMMARY.md` with:
- Files actually touched (vs. verified-only)
- Pin-shift delta if any (signed integer per affected pin + cause description)
- Harness exit codes for each verification command
- AEINTEG-01 closure evidence
- Atomic commit SHA
</output>
