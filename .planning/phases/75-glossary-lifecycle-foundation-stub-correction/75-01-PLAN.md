---
phase: 75-glossary-lifecycle-foundation-stub-correction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_glossary-macos.md
  - docs/_glossary.md
autonomous: true
requirements: [SSOREF-01]
must_haves:
  truths:
    - "An admin reading _glossary-macos.md finds Platform SSO, Secure Enclave, and Enterprise SSO Plug-in in a new ## Authentication section"
    - "Platform SSO entry carries a > **Windows equivalent:** blockquote; Secure Enclave and Enterprise SSO Plug-in do NOT"
    - "Secure Enclave see-also links to _glossary.md#tpm with the not-bit-for-bit caveat"
    - "_glossary.md gains a ### Entra ID SSO term with a reciprocal see-also to _glossary-macos.md#enterprise-sso-plug-in (D-03: resolves the XC-1 missing-term finding; the new heading is anchor-safe)"
    - "The existing ### TPM entry body in _glossary.md carries a reciprocal see-also to _glossary-macos.md#secure-enclave"
    - "All three new terms appear in the ## Alphabetical Index of _glossary-macos.md in correct alphabetical position"
  artifacts:
    - path: "docs/_glossary-macos.md"
      provides: "## Authentication section with 3 PSSO terms, updated index, Version History row"
      contains: "## Authentication"
    - path: "docs/_glossary.md"
      provides: "### Entra ID SSO term + reciprocal back-pointer inside ### TPM body"
      contains: "### Entra ID SSO"
  key_links:
    - from: "docs/_glossary-macos.md#secure-enclave"
      to: "docs/_glossary.md#tpm"
      via: "> See also blockquote"
      pattern: "_glossary\\.md#tpm"
    - from: "docs/_glossary.md#tpm"
      to: "docs/_glossary-macos.md#secure-enclave"
      via: "> See also blockquote inside TPM body"
      pattern: "_glossary-macos\\.md#secure-enclave"
    - from: "docs/_glossary.md#entra-id-sso"
      to: "docs/_glossary-macos.md#enterprise-sso-plug-in"
      via: "> See also blockquote"
      pattern: "_glossary-macos\\.md#enterprise-sso-plug-in"
---

<objective>
Establish the macOS Platform SSO authentication vocabulary that all subsequent v1.9 phases (76-81) link to. Add a new `## Authentication` section to `docs/_glossary-macos.md` with three medium-depth entries (Platform SSO, Secure Enclave, Enterprise SSO Plug-in), update its `## Alphabetical Index` and `## Version History`, and create the reciprocal `### Entra ID SSO` term plus a `### TPM` back-pointer in `docs/_glossary.md`. (SSOREF-01)

Purpose: The anchor slugs `#platform-sso`, `#secure-enclave`, `#enterprise-sso-plug-in` (macOS glossary) and `#entra-id-sso` (Windows glossary) are CONTRACTS that Phases 76-81 depend on. They must be named exactly as specified — no downstream phase can link to a term that does not yet exist (XC-1: "Entra ID SSO" had zero occurrences before this phase).

Output: Two edited markdown files. No new files. All edits are additive except the alphabetical-index in-place insertions (the one sanctioned in-place edit per D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-CONTEXT.md
@.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-RESEARCH.md
@.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ## Authentication section, index entries, Version History row, and front-matter dates to _glossary-macos.md</name>
  <files>docs/_glossary-macos.md</files>
  <read_first>
    - docs/_glossary-macos.md (full file — confirm live line positions for the index line, the `## App Protection (MAM)` → `## Version History` boundary, and the `### MAM-WE` analog entry shape)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-CONTEXT.md (D-01 entry depth, D-02 see-also wiring, D-04 index update)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-RESEARCH.md (Fact Group 1 Platform SSO facts and Windows-equivalent text; Fact Group 2 Secure Enclave facts; Fact Group 3 Enterprise SSO Plug-in product hierarchy; Front Matter / Review Cadence section)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-PATTERNS.md (Entry skeleton analog `### MAM-WE`; `> See also:` blockquote placement rules; Alphabetical Index insertion table; Version History row format; `## Authentication` H2 placement with `---` separator cadence; Front matter update)
  </read_first>
  <action>
    Insert a new `## Authentication` H2 section into `docs/_glossary-macos.md` positioned BETWEEN the existing `## App Protection (MAM)` section and the existing `## Version History` section, preceded and followed by `---` separators matching the file's existing blank-line cadence (one blank line, `---`, one blank line, heading).

    The section contains exactly three `### ` entries in this order, each a 3-5 sentence medium-depth definition modeled on the `### MAM-WE` analog:
    - `### Platform SSO` — define per RESEARCH Fact Group 1: macOS-native feature (macOS 13+), powered by the Microsoft Enterprise SSO plug-in, registers the Mac with Entra ID for device-wide SSO via a hardware-bound PRT; mention the three mutually-exclusive auth methods (Secure Enclave key [recommended], Password sync, Smart Card) WITHOUT conflating them and WITHOUT claiming it blanket-"binds the login password". Include a `> **Windows equivalent:**` blockquote (Platform SSO ONLY per D-01) describing the Windows Hello for Business / PRT analogue, and append a `> See also:` line as the LAST line of that SAME blockquote (Phase 59 precedent) linking to `[Enterprise SSO Plug-in](#enterprise-sso-plug-in)` and `[Entra ID SSO](_glossary.md#entra-id-sso)`.
    - `### Secure Enclave` — define per RESEARCH Fact Group 2: Apple SoC secure subsystem, non-exportable hardware-bound keys, hardware scope (Apple Silicon + T2 Intel). NO `> **Windows equivalent:**` block (D-01). Add a STANDALONE `> See also:` blockquote (separated from the definition by a blank line) linking to `[TPM](_glossary.md#tpm)` with the exact D-02 caveat clause: "analogous hardware root of trust; not bit-for-bit equivalent — Secure Enclave performs no TPM-2.0/DICE attestation".
    - `### Enterprise SSO Plug-in` — define per RESEARCH Fact Group 3: the umbrella Microsoft Enterprise SSO plug-in for Apple devices installed via Company Portal, with Platform SSO (modern) and the SSO app extension (legacy) as sub-features; Redirect type. NO `> **Windows equivalent:**` block (D-01). Add a STANDALONE `> See also:` blockquote linking to `[Platform SSO](#platform-sso)` and `[Entra ID SSO](_glossary.md#entra-id-sso)`.

    Update the `## Alphabetical Index` line in place by inserting three `[Display Name](#slug)` entries (separated by ` | `, display name matching heading text exactly) at the correct alphabetical positions: `[Enterprise SSO Plug-in](#enterprise-sso-plug-in)` after `[Await Configuration](#await-configuration)`; `[Platform SSO](#platform-sso)` after `[MAM-WE](#mam-we)`; `[Secure Enclave](#secure-enclave)` after `[Platform SSO](#platform-sso)`. Final merged order per PATTERNS Alphabetical Index table.

    Append ONE new row as the NEWEST (top) row of the `## Version History` table body: date `2026-06-20`, change describing the added `## Authentication` section (three terms), the three new Alphabetical Index entries, and the front-matter date update; author ` -- `.

    Update the YAML front matter dates to `last_verified: 2026-06-20` and `review_by: 2026-09-20` (90-day PSSO review cadence per RESEARCH Front Matter section / D Claude's-discretion ruling).

    Do NOT add any `> **Windows equivalent:**` block to Secure Enclave or Enterprise SSO Plug-in. Do NOT use any fenced code block in entry bodies. Do NOT touch any heading other than adding the three new `### ` entries and the new `## Authentication` H2.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -c '^## Authentication$' docs/_glossary-macos.md | grep -qx 1 && grep -qF '### Platform SSO' docs/_glossary-macos.md && grep -qF '### Secure Enclave' docs/_glossary-macos.md && grep -qF '### Enterprise SSO Plug-in' docs/_glossary-macos.md && grep -qF '[Platform SSO](#platform-sso)' docs/_glossary-macos.md && grep -qF '[Secure Enclave](#secure-enclave)' docs/_glossary-macos.md && grep -qF '[Enterprise SSO Plug-in](#enterprise-sso-plug-in)' docs/_glossary-macos.md && grep -qF 'no TPM-2.0/DICE attestation' docs/_glossary-macos.md && grep -qF 'last_verified: 2026-06-20' docs/_glossary-macos.md && grep -qF 'review_by: 2026-09-20' docs/_glossary-macos.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/_glossary-macos.md` contains exactly one `## Authentication` heading
    - File contains the three headings `### Platform SSO`, `### Secure Enclave`, `### Enterprise SSO Plug-in`
    - Exactly ONE `> **Windows equivalent:**` blockquote exists within the new `## Authentication` section (under Platform SSO only) — grep within the section shows no Windows-equivalent block under Secure Enclave or Enterprise SSO Plug-in
    - The Secure Enclave see-also contains the literal substring `no TPM-2.0/DICE attestation` and links to `_glossary.md#tpm`
    - The Platform SSO see-also links to both `#enterprise-sso-plug-in` and `_glossary.md#entra-id-sso`
    - The Enterprise SSO Plug-in see-also links to both `#platform-sso` and `_glossary.md#entra-id-sso`
    - The `## Alphabetical Index` line contains `[Enterprise SSO Plug-in](#enterprise-sso-plug-in)`, `[Platform SSO](#platform-sso)`, `[Secure Enclave](#secure-enclave)`
    - Front matter contains `last_verified: 2026-06-20` and `review_by: 2026-09-20`
    - The `## Version History` table contains a row dated `2026-06-20` referencing Phase 75 / `## Authentication`
    - No fenced code block (```) appears inside any of the three new entry bodies
  </acceptance_criteria>
  <done>The three PSSO terms exist as `### ` entries in a new `## Authentication` section with correct see-also wiring per D-01/D-02, the index and Version History are updated, and front-matter dates are bumped — all automated grep assertions in verify pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create ### Entra ID SSO term and add reciprocal back-pointer to ### TPM body in _glossary.md</name>
  <files>docs/_glossary.md</files>
  <read_first>
    - docs/_glossary.md (read the `## Security` section live — confirm current order of `### Secure Boot`, `### SCP`, `### Selective wipe`, `### TPM`, `### TPM attestation`; confirm the `### TPM` entry body at ~line 111; confirm the `## Alphabetical Index` line and the position between `[Enrollment Time Grouping (ETG)]` and `[ESP]`)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-CONTEXT.md (D-03 create Entra ID SSO term; XC-1 critical finding; D-02 reciprocal back-pointer placement)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-RESEARCH.md (Fact Group 4 Entra ID SSO Windows-side definition and the recommended 1-2 sentence definition text; Open Question 2 on Security section ordering)
    - .planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-PATTERNS.md (Pattern A Security section structure and insertion point; Pattern B `### TPM` back-pointer; `_glossary.md` Alphabetical Index insertion note)
  </read_first>
  <action>
    Insert a new `### Entra ID SSO` term into the `## Security` section of `docs/_glossary.md`. Place it in correct alphabetical position relative to the actual current Security section content (read live order; "E" precedes "Sc"/"SC"/"Se" so it falls before `### Secure Boot` unless other E-prefixed terms exist). Body: 1-2 sentences per RESEARCH Fact Group 4 recommended definition — Entra ID SSO on Windows brokered by Web Account Manager (WAM); a hardware-bound (TPM when available) Primary Refresh Token (PRT) silently authenticates the user to apps and browsers; PRT carries device-compliance claims for Conditional Access. Add a STANDALONE `> See also:` blockquote (separated from the definition by a blank line) linking to `[Enterprise SSO Plug-in](_glossary-macos.md#enterprise-sso-plug-in)` with a parenthetical noting it is the macOS analogue via the Microsoft Enterprise SSO plug-in and Platform SSO (reciprocal per D-03).

    Append a reciprocal back-pointer inside the existing `### TPM` entry body: leave the existing TPM definition sentence verbatim, add one blank line, then a new `> See also:` blockquote linking to `[Secure Enclave](_glossary-macos.md#secure-enclave)` with the D-02 caveat parenthetical "analogous Apple hardware root of trust; not bit-for-bit equivalent — Secure Enclave performs no TPM-2.0/DICE attestation". This is a body edit (no heading change) and is anchor-safe.

    Add `[Entra ID SSO](#entra-id-sso)` to the `## Alphabetical Index` line in correct alphabetical position (after `[Enrollment Time Grouping (ETG)](#enrollment-time-grouping-etg)`, before `[ESP](#esp)` — "En" < "ES"), so the new term is discoverable via the file's own navigation entry point (consistent with D-04 reasoning).

    Do NOT add a `> **Windows equivalent:**` block to `### Entra ID SSO` (it IS the Windows entry). Do NOT alter the existing `### TPM` definition sentence text. Do NOT add or rename any other heading. Do NOT use fenced code blocks. Do NOT update front-matter dates in this file (per RESEARCH: only `_glossary-macos.md` warrants the date bump unless executor judges this file materially fact-bearing).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -c '^### Entra ID SSO$' docs/_glossary.md | grep -qx 1 && grep -qF '_glossary-macos.md#enterprise-sso-plug-in' docs/_glossary.md && grep -qF '_glossary-macos.md#secure-enclave' docs/_glossary.md && grep -qF 'no TPM-2.0/DICE attestation' docs/_glossary.md && grep -qF '[Entra ID SSO](#entra-id-sso)' docs/_glossary.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/_glossary.md` contains exactly one `### Entra ID SSO` heading
    - The `### Entra ID SSO` entry has a `> See also:` blockquote linking to `_glossary-macos.md#enterprise-sso-plug-in`
    - The `### Entra ID SSO` entry does NOT contain a `> **Windows equivalent:**` blockquote
    - The `### TPM` entry body contains a new `> See also:` blockquote linking to `_glossary-macos.md#secure-enclave` with the literal substring `no TPM-2.0/DICE attestation`
    - The original TPM definition sentence (Trusted Platform Module ... self-deploying and pre-provisioning modes) is unchanged
    - The `## Alphabetical Index` line contains `[Entra ID SSO](#entra-id-sso)`
    - No fenced code block (```) is introduced
    - No `### ` heading other than `### Entra ID SSO` is added, renamed, or removed
  </acceptance_criteria>
  <done>The `### Entra ID SSO` term exists with a reciprocal see-also to the macOS glossary, the `### TPM` body carries the reciprocal back-pointer to Secure Enclave with the D-02 caveat, and the index lists the new term — all automated grep assertions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits static markdown reference docs only. No auth, no input handling, no runtime code, no network surface, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Tampering | Cross-doc anchor links (`_glossary.md#tpm`, `_glossary-macos.md#secure-enclave`, `#entra-id-sso`, `#enterprise-sso-plug-in`) | mitigate | Anchor slugs are deterministic GitHub heading slugs; acceptance criteria assert exact slug strings via grep so a typo that would break the C13 BLOCKING link audit (or downstream Phase 76-81 cross-links) is caught at task verification. Severity: low (doc-integrity only). |
| T-75-02 | Information Disclosure | Glossary example prose | accept | Definitions are generic product descriptions; no real tenant IDs, secrets, GUIDs, or PII are included. Severity: none. |

No threat in this plan rises to "high"; this is a static-documentation edit with a minimal, honestly-scoped threat surface (per phase security note).
</threat_model>

<verification>
- Both files parse as valid markdown (no broken table rows in Version History; no orphaned blockquotes).
- All four contractual anchor references resolve to headings that exist after this plan: `#platform-sso`, `#secure-enclave`, `#enterprise-sso-plug-in` (in `_glossary-macos.md`) and `#entra-id-sso` (in `_glossary.md`).
- No existing heading slug in either file is altered (anchor stability preserved).
- The C13 BLOCKING link audit (`scripts/validation/v1.8-milestone-audit.mjs`) is NOT affected — no new markdown link to a non-existent file is introduced (the only deferred-link concern, guide 07, lives in Plan 02).
</verification>

<success_criteria>
- SSOREF-01 satisfied (in part): `_glossary-macos.md` has a `## Authentication` section with the three named terms and reciprocal see-also links; `_glossary.md` carries the reciprocal back-pointer (TPM → Secure Enclave) and the new `### Entra ID SSO` term.
- All must_haves truths are observable by an admin reading the two glossary files.
- All automated verify assertions pass.
</success_criteria>

<output>
Create `.planning/phases/75-glossary-lifecycle-foundation-stub-correction/75-01-SUMMARY.md` when done.
</output>
