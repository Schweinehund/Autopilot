---
phase: 108-l2-runbooks-31-33-decision-tree-10
plan: 03
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - docs/l2-runbooks/33-8021x-radius-eap-investigation.md
autonomous: true
requirements: [DOT1X-10]

must_haves:
  truths:
    - "An L2 engineer has a RADIUS/NPS-team request checklist that is strictly ask-side (no NPS configuration steps) in #33 (SC3 clause 1, D-04)"
    - "An L2 engineer can diagnose EAP-method mismatch per platform in #33 (SC3 clause 2)"
    - "An L2 engineer can diagnose server-name-validation failures per platform in #33 (SC3 clause 3)"
    - "#33 opens with a Before-starting link to #31 and LINKS EAP + server-name-validation theory to the foundation (link-not-copy; co-equal EAP preserved)"
  artifacts:
    - path: "docs/l2-runbooks/33-8021x-radius-eap-investigation.md"
      provides: "Cross-platform 802.1X L2 RADIUS/EAP investigation runbook (#33)"
      contains: "platform: windows+macos+ios+android+linux"
  key_links:
    - from: "docs/l2-runbooks/33-8021x-radius-eap-investigation.md"
      to: "docs/l2-runbooks/31-8021x-log-collection.md"
      via: "Before-starting prerequisite link"
      pattern: "31-8021x-log-collection\\.md"
    - from: "docs/l2-runbooks/33-8021x-radius-eap-investigation.md"
      to: "docs/admin-setup-8021x/01-eap-method-overview.md + 02-cert-delivery-foundation.md"
      via: "link-not-copy foundation references (EAP overview + server-name validation)"
      pattern: "01-eap-method-overview\\.md"
---

<objective>
Author `docs/l2-runbooks/33-8021x-radius-eap-investigation.md` — the cross-platform L2 RADIUS/EAP investigation runbook (SC3, DOT1X-10).

Framing = D-04 hybrid mapping 1:1 to SC3's three clauses: (1) a RADIUS/NPS-team REQUEST checklist (strictly ask-side — never NPS configuration), (2) per-platform EAP-method-mismatch diagnosis subsections, (3) per-platform server-name-validation-failure diagnosis subsections. Opens with a Before-starting link to #31. EAP-method-comparison and server-name-validation theory are LINKED to `01-eap-method-overview.md` / `02-cert-delivery-foundation.md`, never restated; co-equal-EAP is preserved (diagnosis matches the client profile to the RADIUS policy — it never ranks methods).

Purpose: Enable L2 to determine whether an 802.1X failure is a RADIUS-policy/EAP-negotiation or server-trust problem, what to ask the RADIUS team for, and how to read the per-platform signals.
Output: One new Markdown runbook file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-CONTEXT.md
@.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-RESEARCH.md
@.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author #33 cross-platform 802.1X L2 RADIUS/EAP investigation runbook</name>
  <files>docs/l2-runbooks/33-8021x-radius-eap-investigation.md</files>
  <read_first>
    - Target (to create): docs/l2-runbooks/33-8021x-radius-eap-investigation.md
    - Primary analog: docs/l2-runbooks/27-macos-sso-investigation.md (Track A / Track B dual-section shape → checklist + two per-platform diagnosis blocks; lines 28-134 track structure, 189-207 Related Resources/Version History)
    - Sibling analog (opener): docs/l2-runbooks/31-8021x-log-collection.md (Before-starting prerequisite target)
    - Frontmatter clone source: docs/l1-runbooks/38-8021x-certificate-failure.md lines 1-7 (change audience to L2)
    - Checklist content + per-platform signal tables (verified): 108-RESEARCH.md §"#33 RADIUS Team Request Checklist Structure" (checklist items, EAP-mismatch table, server-name-validation table) and §"Common Pitfalls" 3 (ask-side boundary)
    - macOS confidence NOTE wording: docs/l1-runbooks/38-8021x-certificate-failure.md line 60
    - Section-by-section skeleton: 108-PATTERNS.md §"docs/l2-runbooks/33-8021x-radius-eap-investigation.md" (platform gate + dual foundation callout, Context, RADIUS Team Request Checklist, EAP-Method Mismatch Diagnosis, Server-Name Validation Failure Diagnosis)
    - Link-not-copy targets (do NOT restate): docs/admin-setup-8021x/01-eap-method-overview.md (co-equal EAP, inner-method-must-match-RADIUS-policy), docs/admin-setup-8021x/02-cert-delivery-foundation.md (RADIUS server-name validation field)
    - Out-of-scope boundary: REQUIREMENTS.md line 80 (Intune client-side only; RADIUS/NPS build-out out of scope)
  </read_first>
  <action>
    Create `docs/l2-runbooks/33-8021x-radius-eap-investigation.md`. Per DOT1X-10 and D-04.

    Frontmatter: identical compound token to #31/#32 — `platform: windows+macos+ios+android+linux`, `audience: L2`, `applies_to: both`, `last_verified: 2026-07-01`, `review_by: 2026-09-29`.

    Platform gate + dual foundation callout (PATTERNS §"Platform gate + dual foundation callout"): gate over all five platforms; cross-link #32 and the decision tree; a link-not-copy NOTE pointing the co-equal EAP overview to `../admin-setup-8021x/01-eap-method-overview.md` and the server-name-validation field requirement to `../admin-setup-8021x/02-cert-delivery-foundation.md` (use the plain-GitHub auto-slug anchor for the RADIUS Server-Name Validation section — verify the exact slug against the foundation file heading; no `{#id}`), with an explicit "do not restate EAP comparison or server-name-validation theory here" sentence.

    Context section: describe the two failure classes (EAP method / inner-auth mismatch; server-name validation failure). Add "Before starting: collect 802.1X logs per `[#31](31-8021x-log-collection.md)`." Add "From L1 escalation?" prose naming L1 #39, #40, #41 as escalation sources.

    RADIUS Team Request Checklist (D-04 clause 1, first block after Context). Lead with a NOTE callout: every item is a REQUEST for information from the RADIUS/NPS team — NOT a configuration step (cite REQUIREMENTS.md Out-of-Scope; Pitfall 3). Every item MUST be phrased as "request/confirm from the RADIUS team" with zero imperative NPS-config verbs (no "set", "configure", "create policy", "modify"). Items per RESEARCH: NPS event IDs 6273 (Access-Reject, with Reason Code + Authentication-Type) and 6272 (Access-Accept); configured EAP type (13=EAP-TLS, 25=PEAP, 21=EAP-TTLS); inner auth method for PEAP/TTLS (MS-CHAPv2 or PAP); RADIUS server cert CN/SAN/issuing-CA/expiry; user/device account state (exists, not locked/expired, correct group); recent NPS policy-condition changes; for EAP-TLS, whether KB5014754 SID-in-SAN strong mapping is enabled and whether the client cert passes it.

    EAP-Method Mismatch Diagnosis (D-04 clause 2). Lead with an "EAP type codes and co-equal principle" note linking `01-eap-method-overview.md` and stating the diagnosis matches client profile to RADIUS policy — it does not rank methods. Then five per-platform subsections, H3 with COLON separators (e.g. `### Windows: EAP-Method Mismatch`) to avoid the em-dash double-hyphen slug trap:
    - Windows: EAP type numbers in `Microsoft-Windows-WLAN-AutoConfig/Operational` / `Microsoft-Windows-Wired-AutoConfig/Operational` event data; cross-reference RADIUS-team-confirmed EAP type.
    - macOS: eapolclient EAP-NAK / method rejection before identity exchange (precede with the MEDIUM-confidence macOS NOTE, reusing 38-...md line 60 wording).
    - iOS/iPadOS: symptom-pattern diagnosis — iOS PEAP requires MS-CHAPv2 inner auth; if iOS fails where Windows/macOS succeed, inspect the Intune Wi-Fi profile inner-auth method (PAP → immediate EAP-NAK).
    - Android: `adb logcat -s "wpa_supplicant"` EAP-NAK / method-negotiation lines (USB-debugging WARNING).
    - Linux: `CTRL-EVENT-EAP-PROPOSED-METHOD` / `CTRL-EVENT-EAP-FAILURE` in `journalctl`; compare to `802-1x.eap` from `nmcli connection show <name>`.

    Server-Name Validation Failure Diagnosis (D-04 clause 3). Lead with a link to the foundation server-name-validation section. Five per-platform subsections (COLON-separated H3):
    - Windows: event 8001 / CAPI2 "server certificate chain couldn't be validated" / trust / chain text; check Intune Wi-Fi profile `Certificate server names` field populated.
    - macOS: silent failure (no trust prompt on managed devices); eapolclient TLS handshake failure / "identity rejected by server"; check `Certificate server names`.
    - iOS/iPadOS: silent "Authentication Failed" in portal; check `Certificate server names` field.
    - Android: `adb logcat -s "wpa_supplicant"` SSL certificate verification failure; check `Certificate server names`.
    - Linux: TLS certificate verification error in `journalctl`; check `802-1x.domain-suffix-match` and `802-1x.ca-cert` via `nmcli connection show <name>`.

    Related Resources + Version History (row: "Phase 108 plan 03: initial authoring — 802.1X cross-platform L2 RADIUS/EAP investigation (#33)").

    Callout vocabulary: NOTE / WARNING / DANGER / CRITICAL only — never `IMPORTANT`. No `{#id}` overrides. Co-equal EAP preserved throughout.
  </action>
  <verify>
    <automated>test -f docs/l2-runbooks/33-8021x-radius-eap-investigation.md && grep -q "platform: windows+macos+ios+android+linux" docs/l2-runbooks/33-8021x-radius-eap-investigation.md && grep -q "audience: L2" docs/l2-runbooks/33-8021x-radius-eap-investigation.md && grep -q "31-8021x-log-collection.md" docs/l2-runbooks/33-8021x-radius-eap-investigation.md && grep -q "01-eap-method-overview.md" docs/l2-runbooks/33-8021x-radius-eap-investigation.md && grep -q "02-cert-delivery-foundation.md" docs/l2-runbooks/33-8021x-radius-eap-investigation.md && grep -q "6273" docs/l2-runbooks/33-8021x-radius-eap-investigation.md && grep -q "6272" docs/l2-runbooks/33-8021x-radius-eap-investigation.md && grep -qi "Certificate server names" docs/l2-runbooks/33-8021x-radius-eap-investigation.md && grep -q "CTRL-EVENT-EAP-PROPOSED-METHOD" docs/l2-runbooks/33-8021x-radius-eap-investigation.md && ! grep -q "> \*\*Important:\*\*" docs/l2-runbooks/33-8021x-radius-eap-investigation.md && ! grep -q "{#" docs/l2-runbooks/33-8021x-radius-eap-investigation.md</automated>
  </verify>
  <done>
    File exists with compound frontmatter + `audience: L2` + 90-day stamps; RADIUS Team Request Checklist present with the ask-side NOTE and NPS event IDs 6272/6273; five per-platform EAP-mismatch subsections + five per-platform server-name-validation subsections present; Before-starting link to #31 and link-not-copy references to `01-eap-method-overview.md` + `02-cert-delivery-foundation.md` present; no NPS-config imperative verbs in the checklist; only NOTE/WARNING/DANGER/CRITICAL callouts; no `{#id}` overrides.
  </done>
  <acceptance_criteria>
    - `docs/l2-runbooks/33-8021x-radius-eap-investigation.md` exists.
    - Frontmatter: `platform: windows+macos+ios+android+linux` + `audience: L2` + `applies_to: both` + `last_verified`/`review_by` (90-day pair).
    - Three SC3 blocks present: (1) RADIUS Team Request Checklist, (2) EAP-Method Mismatch Diagnosis with five per-platform subsections, (3) Server-Name Validation Failure Diagnosis with five per-platform subsections.
    - Checklist leads with a NOTE stating items are requests, not config; NPS event IDs `6272` and `6273` present; no NPS-config imperative verb ("set NPS", "configure NPS policy", "create policy", "modify policy") appears inside the checklist (Pitfall 3).
    - Exact strings present: `Certificate server names`, `CTRL-EVENT-EAP-PROPOSED-METHOD`, `adb logcat -s "wpa_supplicant"`, EAP type codes 13/25/21.
    - Before-starting links `31-8021x-log-collection.md`; foundation theory is LINKED (`01-eap-method-overview.md` and `02-cert-delivery-foundation.md`) and NOT restated; no EAP method is ranked/recommended (co-equal preserved).
    - No `> **Important:**` callout; no `{#` anchor overrides.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan authors a single Markdown documentation file. No executable code, no user input handling, no runtime attack surface is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-03 | N/A | docs/l2-runbooks/33-8021x-radius-eap-investigation.md | accept | Documentation-only; no code paths, no new attack surface. The runbook is strictly ask-side (requests information from the RADIUS team) and documents read-only client-side diagnosis. No high-severity threats expected. |
</threat_model>

<verification>
- File created and passes the automated grep gate in Task 1.
- Manual spot-read: confirm the checklist stays ask-side (no NPS configuration), the three SC3 clauses map 1:1, and no EAP method is ranked (co-equal).
</verification>

<success_criteria>
SC3 satisfied: #33 documents what to request from the RADIUS/NPS team (not NPS config) and diagnoses EAP-method mismatch and server-name-validation failures per platform. D-04 ask-side boundary and co-equal-EAP upheld.
</success_criteria>

<output>
Create `.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-03-SUMMARY.md` when done.
</output>
