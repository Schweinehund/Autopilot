---
phase: 108-l2-runbooks-31-33-decision-tree-10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l2-runbooks/31-8021x-log-collection.md
autonomous: true
requirements: [DOT1X-10]

must_haves:
  truths:
    - "An L2 engineer can find the exact per-platform 802.1X log-collection procedure for Windows, macOS, iOS/iPadOS, Android, and Linux in #31 (SC1)"
    - "#31 self-contains the net-new 802.1X signal set and LINKS (does not restate) the general diagnostic package in #01/#10/#14/#18/#24 (D-02, link-not-copy)"
    - "#31 declares itself the prerequisite for #32 and #33 (SC1)"
    - "The Windows wired channel is named Microsoft-Windows-Wired-AutoConfig/Operational, never Dot3Svc (RESEARCH open question 1)"
    - "The macOS EAPOL predicate carries the MEDIUM-confidence NOTE callout with the eapolclient fallback (RESEARCH open question 2)"
  artifacts:
    - path: "docs/l2-runbooks/31-8021x-log-collection.md"
      provides: "Cross-platform 802.1X L2 log-collection runbook (#31)"
      contains: "platform: windows+macos+ios+android+linux"
  key_links:
    - from: "docs/l2-runbooks/31-8021x-log-collection.md"
      to: "docs/l2-runbooks/01-log-collection.md (and 10/14/18/24)"
      via: "Before-starting link-not-copy references"
      pattern: "01-log-collection\\.md"
    - from: "docs/l2-runbooks/31-8021x-log-collection.md"
      to: "docs/l2-runbooks/32-8021x-cert-investigation.md + 33-8021x-radius-eap-investigation.md"
      via: "Related Resources downstream links"
      pattern: "32-8021x-cert-investigation\\.md"
---

<objective>
Author `docs/l2-runbooks/31-8021x-log-collection.md` — the corpus's first cross-platform L2 log-collection runbook and the shared prerequisite for #32 and #33 (SC1, DOT1X-10).

Structure = D-01 hybrid: shared investigation-flow prose + per-platform deep-dive subsections (Windows / macOS / iOS/iPadOS / Android / Linux) at L2 depth (deep collect + INTERPRET). Per D-02, the file LINKS the general diagnostic package to the five existing per-platform log-collection runbooks (#01/#10/#14/#18/#24) and SELF-CONTAINS only the net-new 802.1X-specific signal/filter set (verified absent from all five in RESEARCH.md §"D-02 Verified").

Purpose: Give L2 engineers one authoritative, verified per-platform 802.1X log-collection map that #32 and #33 both depend on.
Output: One new Markdown runbook file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-CONTEXT.md
@.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-RESEARCH.md
@.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author #31 cross-platform 802.1X L2 log-collection runbook</name>
  <files>docs/l2-runbooks/31-8021x-log-collection.md</files>
  <read_first>
    - Target (to create): docs/l2-runbooks/31-8021x-log-collection.md
    - Primary analog: docs/l2-runbooks/24-linux-log-collection.md (numbered per-platform section rhythm + inline confidence tags at lines 52, 69, 77)
    - Secondary analog: docs/l2-runbooks/27-macos-sso-investigation.md lines 13-25 (shared Context + "Before starting" + "From L1 escalation?" routing prose) and lines 189-207 (Related Resources + Version History)
    - Tertiary analog: docs/l2-runbooks/01-log-collection.md lines 1-20 (link-not-copy opener convention)
    - Frontmatter clone source: docs/l1-runbooks/38-8021x-certificate-failure.md lines 1-7 (compound platform token; change audience to L2)
    - macOS confidence callout wording: docs/l1-runbooks/38-8021x-certificate-failure.md line 60 (reuse verbatim tone)
    - Signal strings (verified): 108-RESEARCH.md §"Per-Platform Diagnostic-Signal Map" (Windows/macOS/iOS/Android/Linux subsections) and §"Common Pitfalls" 1, 2, 4
    - Section-by-section skeleton: 108-PATTERNS.md §"docs/l2-runbooks/31-8021x-log-collection.md" (frontmatter, platform gate, Context, Tool Landscape, per-platform Section skeleton, Related Resources, Version History)
    - Link-not-copy targets (do NOT restate their procedures): docs/l2-runbooks/{01,10,14,18,24}-*log-collection.md
  </read_first>
  <action>
    Create `docs/l2-runbooks/31-8021x-log-collection.md`. Per DOT1X-10.

    Frontmatter (compound multi-platform token per PATTERNS §"Shared Patterns — Frontmatter"): `platform: windows+macos+ios+android+linux`, `audience: L2`, `applies_to: both`, `last_verified: 2026-07-01`, `review_by: 2026-09-29` (last_verified + 90 days).

    Platform gate callout (clone 24-linux-log-collection.md lines 3-5): state the guide covers 802.1X L2 log collection across all five platforms; cross-link to `00-index.md` for non-802.1X L2 investigation. Do NOT edit `00-index.md` (deferred to Phase 109).

    Context section (clone 27 lines 13-25): (1) declare #31 the prerequisite for `[#32](32-8021x-cert-investigation.md)` and `[#33](33-8021x-radius-eap-investigation.md)`; (2) "Before starting" link-not-copy references to the five general packages — `01-log-collection.md`, `10-macos-log-collection.md`, `14-ios-log-collection.md`, `18-android-log-collection.md`, `24-linux-log-collection.md` — with an explicit sentence that the net-new 802.1X signals below are NOT covered in those five (D-02); (3) "From L1 escalation?" routing prose naming the four L1 runbooks (#38-#41) and the decision tree `../decision-trees/10-8021x-triage.md`. Do NOT restate mdmdiagnosticstool / IntuneMacODC / iOS sysdiagnose / Android Company Portal procedures — link only.

    Tool Landscape prose (clone 24 lines 22-28): one blockquote summarizing that each platform's supplicant writes to a different surface; reiterate the general-package procedures live in the five linked guides, not here.

    Five per-platform sections, each an H2 using a COLON separator (e.g. `## Windows: 802.1X Log Collection`) to keep single-hyphen anchor slugs (avoid em-dash double-hyphen trap per PATTERNS §"Anchor slug discipline"). Each named signal carries an inline `[HIGH|MEDIUM|LOW, last_verified 2026-07-01]` confidence tag (clone 24 lines 52/69/77). Content per RESEARCH:
    - Windows: Wi-Fi channel `Microsoft-Windows-WLAN-AutoConfig/Operational` [HIGH]; Wired channel `Microsoft-Windows-Wired-AutoConfig/Operational` [HIGH] — MUST use this exact name, never "Dot3Svc" (Pitfall 1); CAPI2 channel `Microsoft-Windows-CAPI2/Operational` [HIGH] (note it is state-changing to enable — required before repro, feeds #32); read-only export via `wevtutil epl` for both AutoConfig channels; key event IDs 8001 (attempt), 8002 (succeeded), 8003 (failed/disconnected), 11006 (explicit EAP failure). Add one interpretation sentence per signal (L2 = collect + INTERPRET).
    - macOS: primary predicate `log show --predicate 'subsystem contains "com.apple.eapol"' --info --last 30m`; fallback `log show --predicate 'process == "eapolclient"' --info --last 2h`; Wireless Diagnostics GUI alternative (Option-click Wi-Fi → Open Wireless Diagnostics → Utilities → Logging → EAPOL → Collect Logs, produces wifi.log). Precede with the MEDIUM-confidence NOTE callout (reuse 38-...md line 60 wording): predicate is community/Jamf-sourced, no official Apple docs as of 2026-06-30, use fallback if empty. Note Sierra+ no longer writes /var/log/eapolclient (legacy).
    - iOS/iPadOS: NOTE callout — no device-side 802.1X log command exists. Intune portal paths: Devices > iOS/iPadOS > [device] > Device configuration; Troubleshooting + Support > Troubleshoot > [user] > Configuration profiles; on-device read-only Settings > General > VPN & Device Management (user-assisted).
    - Android: WARNING callout (reuse PATTERNS §"Android adb prerequisites callout") — developer options + USB debugging + USB cable to a PC with adb in PATH; L2-only step. Primary filter `adb logcat -s "wpa_supplicant"`; note OEM/version variability; treat `ClientModeImpl`/`WifiNative` as LOW-confidence supplemental only. Intune portal path (Devices > Android Enterprise > [device] > Device configuration) for deployment status.
    - Linux: `journalctl -u NetworkManager` [MEDIUM] (primary — NM manages wpa_supplicant subprocess on Ubuntu LTS) and `journalctl -u wpa_supplicant` [MEDIUM] (supplement / standalone per Phase 106 script); interface-scoped `journalctl -u wpa_supplicant@<interface>`; key EAP strings CTRL-EVENT-EAP-STARTED / -PROPOSED-METHOD / -METHOD / -SUCCESS / -FAILURE.

    Related Resources section (clone 27 lines 189-197): link #32, #33, the decision tree, the five general log-collection guides (marked link-not-copy), and `../_glossary-network.md`.

    Version History table (clone 27 lines 202-207): one row — 2026-07-01, "Phase 108 plan 01: initial authoring — 802.1X cross-platform L2 log collection (#31)".

    Callout vocabulary: NOTE / WARNING / DANGER / CRITICAL only — never `IMPORTANT`. No `{#id}` anchor overrides.
  </action>
  <verify>
    <automated>test -f docs/l2-runbooks/31-8021x-log-collection.md && grep -q "platform: windows+macos+ios+android+linux" docs/l2-runbooks/31-8021x-log-collection.md && grep -q "audience: L2" docs/l2-runbooks/31-8021x-log-collection.md && grep -q "Microsoft-Windows-Wired-AutoConfig/Operational" docs/l2-runbooks/31-8021x-log-collection.md && grep -q 'adb logcat -s "wpa_supplicant"' docs/l2-runbooks/31-8021x-log-collection.md && grep -q "com.apple.eapol" docs/l2-runbooks/31-8021x-log-collection.md && grep -q "journalctl -u NetworkManager" docs/l2-runbooks/31-8021x-log-collection.md && grep -q "01-log-collection.md" docs/l2-runbooks/31-8021x-log-collection.md && grep -q "32-8021x-cert-investigation.md" docs/l2-runbooks/31-8021x-log-collection.md && ! grep -qi "Dot3Svc/Operational" docs/l2-runbooks/31-8021x-log-collection.md && ! grep -q "> \*\*Important:\*\*" docs/l2-runbooks/31-8021x-log-collection.md && ! grep -q "{#" docs/l2-runbooks/31-8021x-log-collection.md</automated>
  </verify>
  <done>
    File exists with compound `windows+macos+ios+android+linux` + `audience: L2` frontmatter and 90-day stamps; all five per-platform H2 sections present; exact verified signal strings present (`Microsoft-Windows-WLAN-AutoConfig/Operational`, `Microsoft-Windows-Wired-AutoConfig/Operational`, `Microsoft-Windows-CAPI2/Operational`, `com.apple.eapol` + eapolclient fallback, `adb logcat -s "wpa_supplicant"`, `journalctl -u NetworkManager`/`wpa_supplicant`); links to the five general log-collection guides AND to #32/#33 present; no "Dot3Svc/Operational" string; macOS MEDIUM-confidence NOTE + Android USB-debugging WARNING present; only NOTE/WARNING/DANGER/CRITICAL callouts; no `{#id}` overrides.
  </done>
  <acceptance_criteria>
    - `docs/l2-runbooks/31-8021x-log-collection.md` exists.
    - Frontmatter contains `platform: windows+macos+ios+android+linux`, `audience: L2`, `applies_to: both`, and both freshness stamps (`last_verified` + `review_by` = last_verified + 90 days).
    - Five per-platform subsections present (Windows, macOS, iOS/iPadOS, Android, Linux), each with at least one interpretation sentence (L2 depth, not read-only naming).
    - Exact strings present: `Microsoft-Windows-WLAN-AutoConfig/Operational`, `Microsoft-Windows-Wired-AutoConfig/Operational`, `Microsoft-Windows-CAPI2/Operational`, `adb logcat -s "wpa_supplicant"`, `com.apple.eapol`, `process == "eapolclient"`, `journalctl -u NetworkManager`, `journalctl -u wpa_supplicant`.
    - `grep -qi "Dot3Svc/Operational"` returns NOTHING (channel-name correctness, Pitfall 1).
    - #31 LINKS #01/#10/#14/#18/#24 (grep finds `01-log-collection.md` and the other four filenames) and does NOT restate their collection commands (no `mdmdiagnosticstool`, no `IntuneMacODC` procedure body).
    - Related Resources links `32-8021x-cert-investigation.md` and `33-8021x-radius-eap-investigation.md`.
    - macOS MEDIUM-confidence NOTE callout present with eapolclient fallback; Android USB-debugging WARNING present.
    - No `> **Important:**` callout anywhere; no `{#` anchor overrides.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan authors a single Markdown documentation file. No executable code, no user input handling, no runtime attack surface is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | N/A | docs/l2-runbooks/31-8021x-log-collection.md | accept | Documentation-only; no code paths, no new attack surface. Diagnostic commands documented are read-only collection commands run by engineers on their own managed devices; the runbook explicitly flags the one state-changing step (CAPI2 enable) so operators are aware. No high-severity threats expected. |
</threat_model>

<verification>
- File created and passes the automated grep gate in Task 1.
- Manual spot-read: confirm the D-01 hybrid shape (shared prose then per-platform subsections) and that no foundation theory (cert-ordering, EKU, EAP comparison) is restated.
</verification>

<success_criteria>
SC1 satisfied: #31 documents the per-platform log sources for all five platforms and declares itself the prerequisite for #32 and #33. Link-not-copy (D-02) and channel-name correctness (RESEARCH open question 1) upheld; macOS MEDIUM-confidence preserved (open question 2).
</success_criteria>

<output>
Create `.planning/phases/108-l2-runbooks-31-33-decision-tree-10/108-01-SUMMARY.md` when done.
</output>
