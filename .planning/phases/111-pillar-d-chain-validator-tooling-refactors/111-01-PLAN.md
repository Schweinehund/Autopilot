---
phase: 111-pillar-d-chain-validator-tooling-refactors
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03]
files_modified:
  - scripts/validation/_lib/exec-fail-detail.mjs
  - scripts/validation/check-phase-48.mjs
  - scripts/validation/check-phase-60.mjs
  - scripts/validation/check-phase-61.mjs
  - scripts/validation/check-phase-62.mjs
  - scripts/validation/check-phase-63.mjs
  - scripts/validation/check-phase-64.mjs
  - scripts/validation/check-phase-65.mjs
  - scripts/validation/check-phase-66.mjs
  - scripts/validation/check-phase-67.mjs
  - scripts/validation/check-phase-68.mjs
  - scripts/validation/check-phase-69.mjs
  - scripts/validation/check-phase-70.mjs
  - scripts/validation/check-phase-71.mjs
  - scripts/validation/check-phase-72.mjs
  - scripts/validation/check-phase-73.mjs
  - scripts/validation/check-phase-74.mjs
  - scripts/validation/check-phase-82.mjs
  - scripts/validation/check-phase-88.mjs
  - scripts/validation/check-phase-93.mjs
  - scripts/validation/check-phase-95.mjs
  - scripts/validation/check-phase-100.mjs

must_haves:
  truths:
    - "scripts/validation/_lib/exec-fail-detail.mjs exists, exports execFailDetail + selfTest, and selfTest passes all assertions (TOOL-01)"
    - "All 40 (stdout + stderr).slice(0,N) failure-detail sites across 20 check-phase files consume execFailDetail; no inline slice duplicates remain (TOOL-01)"
    - "All 14 inline frozen-aware readers across check-phase-{61,67,68,70} are replaced by centralized frozen-at-close adoption; check-phase-61 uses a Landmine-C-safe no-stdio local wrapper (TOOL-02)"
    - "The 3 --self-test catch blocks in check-phase-{48,60,61} capture BOTH stdout and stderr (TOOL-03)"
    - "node scripts/validation/check-phase-100.mjs --verbose exits 0 after EVERY atomic commit"
    - "VERBOSE full-chain output is byte-identical before vs after the refactor (green-chain diff empty); forced-failure detail bytes are byte-identical except the intended TOOL-03 stdout addition (D-03)"
    - "An unreadable/corrupted MILESTONE_CLOSE_SHAS entry still yields SKIP (not FAIL) at the affected TOOL-02 site (Landmine B preserved)"
  artifacts:
    - path: "scripts/validation/_lib/exec-fail-detail.mjs"
      provides: "3-axis failure-detail helper execFailDetail(stdout, stderr, {n, trim, prefix}) + selfTest()"
      contains: "export function execFailDetail"
    - path: "scripts/validation/check-phase-61.mjs"
      provides: "TOOL-01 (Variant A+B), TOOL-02 (Landmine-C local wrapper, inline symbols removed), TOOL-03 (--self-test both-stream capture)"
      contains: "execFailDetail"
    - path: "scripts/validation/check-phase-68.mjs"
      provides: "TOOL-01 (Variant A+C), TOOL-02 (centralized adoption), V-68-10 tolerant-OR matcher"
      contains: "hasNewStyle"
    - path: "scripts/validation/check-phase-70.mjs"
      provides: "TOOL-02 adoption of all 8 inline readers (incl. readAtV17CloseGate for the V17_CLOSEGATE site)"
      contains: "readAtV17Close"
  key_links:
    - from: "scripts/validation/check-phase-82.mjs"
      to: "scripts/validation/_lib/exec-fail-detail.mjs"
      via: "import { execFailDetail }"
      pattern: "execFailDetail\\(stdout, stderr"
    - from: "scripts/validation/check-phase-70.mjs"
      to: "scripts/validation/_lib/frozen-at-close.mjs"
      via: "import { readAtV17Close, readAtV17CloseGate }"
      pattern: "readAtV17Close"
    - from: "scripts/validation/check-phase-68.mjs"
      to: "scripts/validation/check-phase-61.mjs"
      via: "V-68-10 tolerant-OR assertion on frozen-aware reader presence"
      pattern: "frozen-at-close"
---

<objective>
Apply three DRY refactors to the chain-validator tooling as ONE combined plan with five
atomic commits (per LOCKED decision D-01), preserving the string/verdict-equivalence
invariant on all predecessor frozen surfaces. The full chain MUST exit 0 after every commit.

- TOOL-01 (EXEC-FAIL-DETAIL-EXTRACTION-01): create `_lib/exec-fail-detail.mjs` and consume it
  at all 40 `(stdout + stderr).slice(0,N)` failure-detail sites across 20 files (per D-02:
  per-call-site N/trim/prefix, byte-preserving; per D-04: `execFailDetail(stdout, stderr, {n,
  trim, prefix})` with separate raw stream args).
- TOOL-02 (FROZEN-AWARE-ADOPTION-SWEEP-01): replace 14 inline frozen-aware readers across
  check-phase-{61,67,68,70} with the centralized `_lib/frozen-at-close.mjs` module, wrapping
  each throwing call catch→null (Landmine B) and preserving check-phase-61's no-stdio
  stderr-leak behavior (Landmine C).
- TOOL-03 (HELPER-SPAWN-STDERR-01): fix the 3 `--self-test` stderr-only catch blocks in
  check-phase-{48,60,61} to capture BOTH stdout and stderr.

Purpose: DRY the three duplicated code-smell patterns without changing any observable
verdict or detail byte, so Phase 112's harness bump inherits a clean chain.
Output: 1 new helper + 21 modified validators, 5 atomic commits, full chain exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-CONTEXT.md
@.planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-RESEARCH.md
@.planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-PATTERNS.md
@scripts/validation/_lib/frozen-at-close.mjs
@scripts/validation/check-phase-82.mjs
</context>

<execution_constraints>
LOCKED (from CONTEXT.md — non-negotiable):
- D-01: ONE combined plan, atomic commits inside, sequential on main tree (worktrees
  unreliable). Order commits so the FULL CHAIN EXITS 0 after EACH commit.
- D-02: N/trim/prefix are per-call-site (NOT per-file). Every call site passes n, trim, and
  prefix EXPLICITLY. Preserve each site's bytes (N=500 vs N=300; trim vs no-trim).
- D-03: verify via forced-failure/VERBOSE diff — green-chain equality alone is NOT evidence.
- D-04: `execFailDetail(stdout, stderr, {n, trim, prefix})` — separate raw stream args,
  all three axes parameterized.
- Landmine A: removing `readRequirementsAtV15Close`/`readRoadmapAtV15Close` from
  check-phase-61 AND making check-phase-68 V-68-10 tolerant MUST be the SAME commit (Task 3).
- Landmine B: `readAtClose` THROWS; every adoption site MUST wrap catch→return null to
  preserve SKIP.
- Landmine C: check-phase-61's inline readers omit `stdio` (stderr leaks to parent, treated
  as significant); adoption in 61 MUST preserve the no-stdio git call — do NOT call raw
  `readAtClose` there.
- Constraint 4: do NOT fold the `isMissing` SKIP-vs-FAIL verdict branch into the detail helper.
- Constraint 5: per-site, not per-file (check-phase-73 and check-phase-100 each hold BOTH a
  Variant A and a Variant C site).
- SCOPE DISCIPLINE: no new checks, no new coverage, no content-file edits. If a refactor
  surfaces a SCOPE-GAP discovery, route forward to v1.15+ — do not expand this phase.

Chain apex = `check-phase-100.mjs` (CHAIN_PHASES=[48..99]). Use
`CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-<N>.mjs --verbose` to run a single
validator without triggering its full sub-chain during development.
</execution_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Capture D-03 baseline + create exec-fail-detail.mjs helper with self-test (TOOL-01 foundation)</name>
  <files>scripts/validation/_lib/exec-fail-detail.mjs</files>
  <read_first>
    - scripts/validation/_lib/frozen-at-close.mjs (primary analog: _lib module header/JSDoc/export style, lines 1-65)
    - scripts/validation/_lib/archive-path.mjs (secondary analog: no-state single-export _lib module)
    - scripts/validation/regenerate-supervision-pins.mjs (self-test invocation convention: argv.includes('--self-test'))
    - .planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-RESEARCH.md §"exec-fail-detail.mjs API (D-04 Locked Design)" and §"Helper Self-Test"
    - .planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-PATTERNS.md §"scripts/validation/_lib/exec-fail-detail.mjs" (exact locked D-04 body + selfTest assertions)
  </read_first>
  <action>
    FIRST, capture the D-03 pre-refactor baseline (before any code change): run
    `node scripts/validation/check-phase-100.mjs --verbose` and save full stdout+stderr to a
    temp file OUTSIDE the repo tree (e.g. under the OS temp dir) named `before-111.txt`. Confirm
    the command exits 0. This baseline is diffed against by every subsequent task.

    THEN create `scripts/validation/_lib/exec-fail-detail.mjs` per D-04. Import `execFileSync`
    is NOT needed (pure string transform). Export `execFailDetail(stdout, stderr, { n, trim =
    false, prefix = '' })` that computes `combined = stdout + stderr`, `sliced =
    combined.slice(0, n)`, and returns `prefix + (trim ? sliced.trim() : sliced)`. Match the
    exact body in PATTERNS.md §exec-fail-detail. `n` has NO default (per D-02: explicit-only);
    `trim` defaults false and `prefix` defaults '' as a documented safety net only.

    Also export `selfTest()` containing the five assertions verbatim from PATTERNS.md lines
    122-127 (labels: A-basic, A-trim, C-basic, C-no-trim, slice-n), returning the success
    string on pass and throwing on mismatch. Add a self-test entry-point using the codebase
    convention: `if (process.argv.includes('--self-test')) { console.log(selfTest()); }` (mirror
    regenerate-supervision-pins.mjs — do NOT use an import.meta.url guard). Include the file
    header comment block naming TOOL-01 / EXEC-FAIL-DETAIL-EXTRACTION-01 and citing D-02 + D-04.

    Do NOT add any consumer imports yet — this commit only creates the helper.
    Commit: `feat(111): add exec-fail-detail.mjs failure-detail helper (TOOL-01, D-04)`.
  </action>
  <verify>
    <automated>node scripts/validation/_lib/exec-fail-detail.mjs --self-test && node scripts/validation/check-phase-100.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - Temp baseline file `before-111.txt` exists (captured before edits) and the baseline
      command exited 0.
    - `scripts/validation/_lib/exec-fail-detail.mjs` contains `export function execFailDetail`
      and `export function selfTest`.
    - `node scripts/validation/_lib/exec-fail-detail.mjs --self-test` prints the success string
      and exits 0 (all 5 assertions pass).
    - `node scripts/validation/check-phase-100.mjs --verbose` exits 0.
    - Diff of the current VERBOSE output vs `before-111.txt` is EMPTY (new file has no consumers;
      chain output unchanged).
    - Exactly one atomic commit created for this task.
  </acceptance_criteria>
  <done>Helper exists, self-test green, chain exits 0, VERBOSE diff empty vs baseline, one commit.</done>
</task>

<task type="auto">
  <name>Task 2: Apply TOOL-01 to all 40 (stdout + stderr).slice sites across 20 files (D-02 per-site N/trim/prefix)</name>
  <files>scripts/validation/check-phase-60.mjs, scripts/validation/check-phase-61.mjs, scripts/validation/check-phase-62.mjs, scripts/validation/check-phase-63.mjs, scripts/validation/check-phase-64.mjs, scripts/validation/check-phase-65.mjs, scripts/validation/check-phase-66.mjs, scripts/validation/check-phase-67.mjs, scripts/validation/check-phase-68.mjs, scripts/validation/check-phase-69.mjs, scripts/validation/check-phase-70.mjs, scripts/validation/check-phase-71.mjs, scripts/validation/check-phase-72.mjs, scripts/validation/check-phase-73.mjs, scripts/validation/check-phase-74.mjs, scripts/validation/check-phase-82.mjs, scripts/validation/check-phase-88.mjs, scripts/validation/check-phase-93.mjs, scripts/validation/check-phase-95.mjs, scripts/validation/check-phase-100.mjs</files>
  <read_first>
    - .planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-RESEARCH.md §"Exact Site Enumeration — 40 Lines Across 20 Files" (the authoritative file:line:variant:N:trim:prefix table)
    - .planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-PATTERNS.md §"Variant-to-Parameter Reference Table" and the Variant A/B/C before/after snippets
    - scripts/validation/check-phase-82.mjs (exact analog: Variant A CHAIN site + Variant C harness site + isMissing branch to leave untouched)
  </read_first>
  <action>
    For each of the 40 sites in the RESEARCH.md enumeration table, add the import
    `import { execFailDetail } from './_lib/exec-fail-detail.mjs';` at the top of each file
    (place alongside existing `./_lib/` imports; add once per file) and replace ONLY the
    `detail:` expression in the `return { pass: false, detail: ... }` line. Do NOT touch the
    `isMissing` branch, the `stderr`/`stdout` local assignments, or the SKIP return
    (constraint 4 — verdict stays at the call site).

    Per-call-site parameter mapping (D-02, constraint 5 — NEVER assume one N per file):
    - Variant A (CHAIN wrapper, 20 sites): replace `'check-phase-' + phaseNum + ' FAIL: ' +
      (stdout + stderr).slice(0, 500).trim()` with `execFailDetail(stdout, stderr, { n: 500,
      trim: true, prefix: 'check-phase-' + phaseNum + ' FAIL: ' })`.
    - Variant B (harness wrapper — ONLY check-phase-60.mjs:254 and check-phase-61.mjs:389):
      replace `'harness FAIL: ' + (stdout + stderr).slice(0, 500).trim()` with
      `execFailDetail(stdout, stderr, { n: 500, trim: true, prefix: 'harness FAIL: ' })`.
      CAUTION: these two are N=500+trim, NOT N=300 (Pitfall 1).
    - Variant C (harness wrapper, 18 sites, files 62-100): replace `'harness FAIL: ' +
      (stdout + stderr).slice(0, 300)` (NO trim) with `execFailDetail(stdout, stderr, { n: 300,
      trim: false, prefix: 'harness FAIL: ' })`.

    check-phase-73 and check-phase-100 each have BOTH a Variant A (N=500) and a Variant C
    (N=300) site — apply per the table, not per-file. Do NOT touch the n=200 `--self-test`
    stderr-only sites (those are TOOL-03, handled in Task 5). Do NOT touch check-phase-61's
    inline frozen readers or check-phase-68 V-68-10 (Tasks 3/4).

    Commit: `refactor(111): consume execFailDetail at 40 failure-detail sites (TOOL-01, D-02)`.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-100.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - Zero remaining inline duplicates: `grep -rE "\(stdout \+ stderr\)\.slice\(0," scripts/validation/check-phase-*.mjs` returns 0 matches (the only surviving raw slice is `combined.slice(0, n)` inside `_lib/exec-fail-detail.mjs`, which the glob excludes).
    - Each of the 20 files imports execFailDetail exactly once: `grep -c "import { execFailDetail }" scripts/validation/check-phase-<file>.mjs` == 1 for every file in the enumeration.
    - check-phase-60.mjs:254 and check-phase-61.mjs:389 use `{ n: 500, trim: true, prefix: 'harness FAIL: ' }` (Variant B preserved, NOT n:300).
    - `node scripts/validation/check-phase-100.mjs --verbose` exits 0.
    - VERBOSE output diff vs `before-111.txt` is EMPTY (byte-identical — execFailDetail reproduces every variant's bytes on the PASS/green path).
    - Exactly one atomic commit created for this task.
  </acceptance_criteria>
  <done>All 40 sites consume execFailDetail with correct per-site params; chain exits 0; VERBOSE diff empty; one commit.</done>
</task>

<task type="auto">
  <name>Task 3: Landmine A atomic commit — remove check-phase-61 inline symbols (Landmine-C-safe) + make check-phase-68 V-68-10 tolerant (TOOL-02 part 1)</name>
  <files>scripts/validation/check-phase-61.mjs, scripts/validation/check-phase-68.mjs</files>
  <read_first>
    - scripts/validation/check-phase-61.mjs lines 39-45 and 58-64 (the two no-stdio inline readers to remove) and line 20 (existing readAtV15Close import)
    - scripts/validation/check-phase-68.mjs lines 210-223 (V-68-10 AND-logic block to replace)
    - scripts/validation/check-phase-73.mjs lines 203-210 (tolerant-OR matcher reference)
    - .planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-RESEARCH.md §"Landmine A — Exact Lines and Tolerant Fix" and §"check-phase-61.mjs — 2 inline readers" (Landmine C detail)
    - .planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-PATTERNS.md §"TOOL-02: check-phase-61.mjs" and §"TOOL-02: check-phase-68.mjs — V-68-10 Landmine A fix"
  </read_first>
  <action>
    In check-phase-61.mjs: DELETE the named functions `readRequirementsAtV15Close()`
    (:39-45) and `readRoadmapAtV15Close()` (:58-64). Replace with ONE local wrapper
    `readAtV15CloseFor61(relPath)` that runs `execFileSync('git', ['show', 'ba2cbc0:' +
    relPath], { encoding: 'utf8', timeout: 10000 }).replace(/\r\n/g, '\n')` inside try, and
    `return null` on catch. Landmine C: deliberately OMIT the `stdio` option so 61's original
    stderr-leak-to-parent behavior is preserved (do NOT call raw `readAtClose`, which sets
    `stdio:['ignore','pipe','pipe']`). Update the two call sites: `readRequirementsAtV15Close()`
    → `readAtV15CloseFor61('.planning/REQUIREMENTS.md')`; `readRoadmapAtV15Close()` →
    `readAtV15CloseFor61('.planning/ROADMAP.md')`. The symbol strings
    `readRequirementsAtV15Close` and `readRoadmapAtV15Close` MUST NOT appear anywhere in
    check-phase-61.mjs afterward. Leave the existing line-20 `readAtV15Close` import in place
    (it satisfies V-68-10's new tolerant check).

    In check-phase-68.mjs: replace the V-68-10 AND-logic block (:215-222) with the OR-tolerant
    logic from PATTERNS.md §V-68-10: `hasOldStyle = c.includes('readRequirementsAtV15Close') &&
    c.includes('readRoadmapAtV15Close')`; `hasNewStyle = c.includes('readAtV15Close') ||
    c.includes('frozen-at-close')`; FAIL only if `!hasOldStyle && !hasNewStyle`; otherwise PASS
    with a `mode` detail string naming centralized vs inline. Do NOT touch check-phase-68's own
    inline readers in this task (Task 4). Do NOT rename the V-68-10 `id`/`name`.

    Both edits land in ONE commit (Landmine A — no intermediate state where 61 lost the symbols
    but 68 still uses AND logic).
    Commit: `refactor(111): landmine-A — drop check-phase-61 inline readers + tolerant V-68-10 (TOOL-02)`.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-100.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "readRequirementsAtV15Close\|readRoadmapAtV15Close" scripts/validation/check-phase-61.mjs` == 0 (both symbols gone from 61).
    - check-phase-61.mjs contains `readAtV15CloseFor61` and the git-show call in it has NO `stdio:` option (Landmine C preserved).
    - check-phase-68.mjs V-68-10 contains `hasNewStyle` and `frozen-at-close` (tolerant-OR fix applied); `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-68.mjs --verbose` reports V-68-10 PASS with a centralized-mode detail string.
    - `node scripts/validation/check-phase-100.mjs --verbose` exits 0 (V-68-10 did not flip FAIL).
    - VERBOSE output diff vs `before-111.txt` differs ONLY in the V-68-10 detail line (the mode-string wording change is an intended PASS-detail change, not a verdict change) — no verdict PASS→FAIL/SKIP transitions anywhere.
    - Exactly one atomic commit created for this task (both files together).
  </acceptance_criteria>
  <done>61 inline symbols removed with no-stdio wrapper; V-68-10 tolerant; chain exits 0; only V-68-10 detail line differs; one commit.</done>
</task>

<task type="auto">
  <name>Task 4: Complete TOOL-02 adoption for check-phase-{67,68,70} inline readers (Landmine B catch→null) + forced-failure SKIP proof</name>
  <files>scripts/validation/check-phase-67.mjs, scripts/validation/check-phase-68.mjs, scripts/validation/check-phase-70.mjs</files>
  <read_first>
    - scripts/validation/_lib/frozen-at-close.mjs (readAtClose THROWS; convenience exports readAtV17Close, readAtV17CloseGate; MILESTONE_CLOSE_SHAS map)
    - scripts/validation/check-phase-67.mjs lines 35-44 (readCorpusFileAtV17Close) and 50-57 (readSidecarAtV17Close JSON variant)
    - scripts/validation/check-phase-68.mjs lines 33-41 (readMilestonesAtV17Close) and 45-51 (readCorpusFileAtV17Close)
    - scripts/validation/check-phase-70.mjs lines 40-112 (8 inline readers incl. readProjectAtV17CloseGate at :106-112)
    - .planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-RESEARCH.md §"TOOL-02 Inline Reader Inventory" and §"D-03 Verification Protocol" (Landmine B SKIP test)
    - .planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-PATTERNS.md §"TOOL-02: check-phase-67/68/70" (replacement tables + wrapper patterns)
  </read_first>
  <action>
    Before removing any inline reader symbol, grep scripts/validation/ for cross-file
    references to that symbol name; if any OTHER validator asserts on it (as V-68-10 did for 61),
    make that assertion tolerant in THIS commit. RESEARCH found V-68-10 as the only such
    landmine (already fixed in Task 3), but verify.

    check-phase-67.mjs: add `import { readAtV17Close } from './_lib/frozen-at-close.mjs';`.
    Add local `tryReadAtV17Close(relPath)` = `try { return readAtV17Close(relPath); } catch {
    return null; }` (Landmine B). Remove `readCorpusFileAtV17Close` (:35-44) → call
    `tryReadAtV17Close(relPath)`. Remove `readSidecarAtV17Close` (:50-57, JSON variant) → add
    `trySidecarAtV17Close()` = `try { const c = readAtV17Close(
    'scripts/validation/v1.7-audit-allowlist.json'); return JSON.parse(c); } catch { return
    null; }` (preserve the JSON.parse-returns-object contract).

    check-phase-68.mjs: add the `readAtV17Close` import + `tryReadAtV17Close` wrapper. Remove
    `readMilestonesAtV17Close` (:33-41) → `tryReadAtV17Close('.planning/MILESTONES.md')` and
    `readCorpusFileAtV17Close` (:45-51) → `tryReadAtV17Close(relPath)`. Leave the Task-3 V-68-10
    fix untouched.

    check-phase-70.mjs: add `import { readAtV17Close, readAtV17CloseGate } from
    './_lib/frozen-at-close.mjs';` + `tryReadAtV17Close` and `tryReadAtV17CloseGate` wrappers.
    Replace all 8 inline readers per the RESEARCH replacement table. CRITICAL:
    `readProjectAtV17CloseGate` (:106-112) maps to `tryReadAtV17CloseGate('.planning/PROJECT.md')`
    (V17_CLOSEGATE SHA 4df3a16) — do NOT route it through readAtV17Close (aa6de68).

    All catch→null wrappers preserve SKIP (Landmine B). The isMissing/verdict logic at each
    consuming check stays put (constraint 4).
    Commit: `refactor(111): adopt centralized frozen-at-close in check-phase-67/68/70 (TOOL-02, Landmine B)`.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-100.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - No inline `execFileSync('git', ['show'` git-show readers remain in check-phase-67/68/70: `grep -rc "execFileSync('git', \['show'" scripts/validation/check-phase-67.mjs scripts/validation/check-phase-70.mjs` == 0 (check-phase-68 likewise 0 for the removed readers). check-phase-61 retains ONE (its Landmine-C wrapper) — expected.
    - check-phase-70.mjs imports both `readAtV17Close` and `readAtV17CloseGate`, and the former readProjectAtV17CloseGate site now routes through `tryReadAtV17CloseGate`.
    - `node scripts/validation/check-phase-100.mjs --verbose` exits 0.
    - VERBOSE output diff vs `before-111.txt` shows NO new differences beyond Task 3's V-68-10 detail line (no verdict transitions; frozen-read content is byte-equivalent).
    - Landmine B forced-failure proof: temporarily set the `V17` entry in `_lib/frozen-at-close.mjs` MILESTONE_CLOSE_SHAS to a nonexistent SHA, run `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-70.mjs --verbose`, confirm the affected frozen-read checks report SKIPPED (not FAILED), then RESTORE the original SHA and confirm `git diff --quiet scripts/validation/_lib/frozen-at-close.mjs` (no residual change committed).
    - Exactly one atomic commit created for this task (frozen-at-close.mjs is NOT part of it — the SHA edit was reverted).
  </acceptance_criteria>
  <done>14th-count inline readers in 67/68/70 replaced with catch→null centralized adoption; CloseGate SHA correct; SKIP proven under bad SHA; chain exits 0; one commit.</done>
</task>

<task type="auto">
  <name>Task 5: Apply TOOL-03 both-stream capture to the 3 --self-test sites in check-phase-{48,60,61}</name>
  <files>scripts/validation/check-phase-48.mjs, scripts/validation/check-phase-60.mjs, scripts/validation/check-phase-61.mjs</files>
  <read_first>
    - scripts/validation/check-phase-48.mjs lines 63-78 (id=4 --self-test catch block)
    - scripts/validation/check-phase-60.mjs lines 183-195 (--self-test catch block)
    - scripts/validation/check-phase-61.mjs lines 397-409 (--self-test catch block)
    - .planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-RESEARCH.md §"TOOL-03 — Exact Sites and Fix" (the n=200 fix pattern + --self-test discriminator note)
    - .planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-PATTERNS.md §"TOOL-03: check-phase-{48,60,61}"
  </read_first>
  <action>
    At each of the 3 catch blocks: after the existing `const stderr = err.stderr ?
    err.stderr.toString() : '';` line, ADD `const stdout = err.stdout ? err.stdout.toString() :
    '';`. Leave the `isMissing` computation (keyed on stderr signals: ENOENT / status 127 /
    'not found' / 'Could not resolve') and the SKIP return UNCHANGED (constraint 4 — those
    signals only appear in stderr; adding stdout does not alter the verdict). Replace the failing
    `return { pass: false, detail: '--self-test FAIL: ' + stderr.slice(0, 200) }` with the
    TOOL-01 helper call `return { pass: false, detail: execFailDetail(stdout, stderr, { n: 200,
    trim: false, prefix: '--self-test FAIL: ' }) }`. Note n=200 here is DISTINCT from Variant A
    (500) and Variant C (300) — do not normalize. Ensure each file already imports execFailDetail
    (check-phase-60 and check-phase-61 got the import in Task 2; check-phase-48 is NOT a TOOL-01
    file, so ADD `import { execFailDetail } from './_lib/exec-fail-detail.mjs';` to
    check-phase-48.mjs).

    Commit: `fix(111): capture stdout+stderr at 3 --self-test catch blocks (TOOL-03)`.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-100.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "stderr.slice(0, 200)" scripts/validation/check-phase-48.mjs scripts/validation/check-phase-60.mjs scripts/validation/check-phase-61.mjs` == 0 (all 3 stderr-only slices replaced).
    - All 3 catch blocks now declare `const stdout = err.stdout` and call `execFailDetail(stdout, stderr, { n: 200, trim: false, prefix: '--self-test FAIL: ' })`.
    - check-phase-48.mjs imports execFailDetail exactly once.
    - `node scripts/validation/check-phase-100.mjs --verbose` exits 0.
    - Green-chain VERBOSE diff vs `before-111.txt` shows NO new differences (self-test PASSES on green, so the added stdout capture does not manifest). Forced-failure proof of the intended change (D-03 exception): run one --self-test site with a deliberately failing `regenerate-supervision-pins.mjs` subprocess (e.g. temporarily point PIN_HELPER at a script that writes to stdout then exits non-zero), confirm the detail now includes the stdout bytes it previously dropped, then restore. This is the ONLY intended byte change and it appears ONLY on the failure path.
    - Exactly one atomic commit created for this task.
  </acceptance_criteria>
  <done>3 self-test sites capture both streams via execFailDetail n=200; chain exits 0; green diff empty; failure-path stdout capture proven; one commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validator → git subprocess | `execFileSync('git', ['show', <frozen-SHA>:<repo-relative-path>])` — argument-array form (NOT shell); SHAs are hardcoded constants in MILESTONE_CLOSE_SHAS; paths are hardcoded repo-relative literals. No external/user input crosses this boundary. |
| validator → node subprocess | `execFileSync('node', [<helper>, '--self-test'])` — argument-array form; helper path is a repo-internal constant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering | git show subprocess (frozen-at-close.mjs / readAtV15CloseFor61) | accept | No user input reaches argv; SHAs and paths are hardcoded literals; execFileSync argument-array form is not shell-interpreted. Refactor preserves the existing (already-safe) call shape byte-for-byte. No new HIGH threat introduced. |
| T-111-02 | Information Disclosure | TOOL-03 adds stdout capture to --self-test catch blocks | accept | Captured stream is from a repo-internal script (regenerate-supervision-pins.mjs) inside the existing validator trust model; output is truncated to 200 chars and only surfaced on the local dev/CI failure path. No PII, no secrets, no external sink. |
| T-111-03 | Denial of Service | git/node subprocess hang | accept (pre-existing) | Existing `timeout: 10000` (git) / `timeout: 30000` (node) options are preserved unchanged by the refactor. Not introduced by this phase. |
| T-111-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase — pure internal ESM refactor, zero new dependencies. Package Legitimacy Gate not triggered. |

**Assessment:** This is an internal validator-tooling refactor with no new external input surface, no network calls, no new dependencies. It introduces NO new HIGH threats. The only subprocess invocations use `execFileSync` argument-array form with hardcoded SHAs/paths (not shell). ASVS V5/V6 not applicable (no external inputs, no cryptography). Security enforcement gate: PASS (no HIGH findings).
</threat_model>

<verification>
Overall phase verification (D-03 protocol):

1. Baseline captured once in Task 1 (`before-111.txt`) BEFORE any edit; command exited 0.
2. After EACH atomic commit: `node scripts/validation/check-phase-100.mjs --verbose` exits 0.
3. After EACH commit: VERBOSE output diffed vs `before-111.txt`. Expected diffs are ONLY:
   - Task 3+: the single V-68-10 PASS-detail mode-string line (intended, not a verdict change).
   - No PASS→FAIL, PASS→SKIP, or SKIP→FAIL verdict transitions anywhere.
4. Forced-failure evidence (green-chain equality is NOT sufficient — constraint 6):
   - Landmine B (Task 4): bad-SHA injection proves affected frozen reads SKIP (not FAIL), then restored.
   - TOOL-03 (Task 5): forced subprocess failure proves stdout is now captured in the detail (the one intended byte change, failure-path only).
5. Scope discipline: `git diff --stat` across all 5 commits touches ONLY the 22 files in
   `files_modified` (1 new helper + 21 validators). No content files, no new checks.
</verification>

<success_criteria>
- ROADMAP SC1: `_lib/exec-fail-detail.mjs` exists; all 40 CHAIN/AUDIT/helper-spawn sites
  consume it; `grep -rE "\(stdout \+ stderr\)\.slice\(0," scripts/validation/check-phase-*.mjs`
  returns 0 (TOOL-01).
- ROADMAP SC2: the 14 inline frozen-aware readers in check-phase-{61,67,68,70} are replaced
  with centralized frozen-at-close adoption; VERBOSE diff proves byte/verdict-equivalence (TOOL-02).
- ROADMAP SC3: the 3 --self-test catch blocks in check-phase-{48,60,61} capture stdout+stderr;
  full chain `node scripts/validation/check-phase-100.mjs --verbose` exits 0 after all three
  refactors (TOOL-03).
- All 5 landmines enforced: A (atomic 61+68 commit), B (catch→null SKIP proven), C (61 no-stdio
  wrapper), verdict/string separation (isMissing untouched), per-site N (73/100 dual-variant).
- 5 atomic commits; chain exits 0 after every one.
</success_criteria>

<output>
Create `.planning/phases/111-pillar-d-chain-validator-tooling-refactors/111-01-SUMMARY.md` when done.
</output>
