---
phase: 123-orphan-nav-hub-retrofit-navigation-last
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/check-nav-hub-links.mjs
autonomous: true
requirements: [RETRO-06]

must_haves:
  truths:
    - "check-nav-hub-links.mjs is a NEW standalone validator (no CHAIN_PHASES) mirroring the c17-eee-contract.mjs standalone precedent — it does NOT extend v1.6-milestone-audit.mjs C13/C16 (no reusable checker exists)"
    - "The GitHub-slug algorithm strips punctuation IN PLACE (does NOT collapse consecutive hyphens), preserving the double-hyphen from space-surrounded punctuation, and honors {#id} overrides FIRST then slug fallback"
    - "Duplicate headings within a file get encounter-order dedup suffixes -1/-2/-3 (Map<slugBase,count>), not a plain Set that loses variants"
    - "Path resolution is relative to the LINKING file's own directory (path.dirname), case-sensitive/Linux-CI posture, http(s)/mailto skipped; a missing target file or absent fragment FAILs with file:line -> target"
    - "The checker scans BOTH directions: outbound (all links FROM the 4 hubs) AND inbound (corpus-wide links whose resolved target is one of the 4 hubs); exit 0 iff both clean, else nonzero with a per-failure report"
  artifacts:
    - path: "scripts/validation/check-nav-hub-links.mjs"
      provides: "Net-new file+anchor link-integrity checker (D-01 Option A) with GitHub-exact slugify+dedup, {#id}-first, fence-masked scan, outbound+inbound directions, --self-test"
      contains: "check-nav-hub-links"
  key_links:
    - from: "scripts/validation/check-nav-hub-links.mjs"
      to: "docs/**/*.md"
      via: "walkMd + fence-masked link scan + resolvable-anchor-set membership"
      pattern: "walkMd"
---

<objective>
Build the net-new automated file+anchor link-checker (`scripts/validation/check-nav-hub-links.mjs`) — the standing SC2 verification gate locked by D-01 (Option A, HIGH, OVERRULED the Proponent's "both/C"). This is where the phase's correctness risk lives: RESEARCH is explicit that an approximated slugifier is *worse than no checker* because it launders false confidence into SC2's "no broken links" claim. Build it to GitHub's EXACT render behavior. This plan writes no doc file and does not fix any link — it produces + self-tests the tool that 123-04 runs against the final corpus.

Purpose: No reusable link-checker exists anywhere in the repo (RESEARCH Pitfall 1: `v1.6-milestone-audit.mjs` C13 only shape-checks an allowlist JSON; C16 is a hardcoded 4-endpoint Apple-Business substring triangle — neither is a generic resolver; C13's "full mlc sweep" was deferred to CI and never implemented in-repo). Must be built from scratch, standalone (no CHAIN_PHASES), mirroring the Phase-115 c17 standalone precedent.
Output: `scripts/validation/check-nav-hub-links.mjs` with a `--self-test` covering the slug edge cases.

## Locked decision honored by this plan
- **D-01** (HIGH): single automated file+anchor checker. Spec is FIXED: (1) path resolution filesystem-relative to the LINKING file's directory, normalize `..`/`.`, empty-path `#frag` -> the linking file itself, case-sensitive/Linux-CI; (2) anchor resolution honors `{#id}` override FIRST then GitHub-slug (87 `{#id}` overrides exist repo-wide outside the hubs); (3) BOTH outbound (from the 4 hubs) AND inbound-to-hub (corpus-wide); (4) exit nonzero with `file:line -> link` per failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-CONTEXT.md
@.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-RESEARCH.md
@scripts/validation/c17-eee-contract.mjs
@scripts/pipeline/retrofit-mermaid-structural.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the GitHub-exact slugify + resolvable-anchor-set builder (with --self-test)</name>
  <read_first>
    - 123-RESEARCH.md §"Pattern 3" (full checker spec) + §"GitHub-slug algorithm (exact)" (the 4 numbered steps, verified against 3 live corpus examples) + §"Resolvable-anchor-set construction"
    - 123-RESEARCH.md §"Don't Hand-Roll" (strip-in-place, don't-collapse, dedup-by-encounter-order) + Pitfall 4 (duplicate-heading dedup landmine: common-issues.md 4x `### 802.1X Network Authentication Failure`; index.md 5x `### Admin Setup`)
    - scripts/validation/c17-eee-contract.mjs (copy verbatim: readFile CRLF-normalizing ~:61-65, walkMd ~:68-86, relNormalize ~:89-94; note the `.replace(/\*\*/g,'')` emphasis-strip precedent ~:191)
    - scripts/pipeline/retrofit-mermaid-structural.mjs (copy verbatim: buildFenceMask ~:262-280 for code-fence exclusion)
  </read_first>
  <behavior>
    The --self-test must assert these exact slug outcomes (all byte-verified in RESEARCH against live headings):
    - slugify("iOS: Compliance / Access Blocked") === "ios-compliance--access-blocked"  (colon has no leading space so removal leaves no gap; " / " is space-surrounded so removing "/" leaves TWO adjacent spaces -> "--")
    - slugify("iOS/iPadOS Quick Reference") === "iosipados-quick-reference"  (no spaces around "/" -> single hyphen, no gap)
    - dedup: four identical "### 802.1X Network Authentication Failure" headings in ONE file -> ["8021x-network-authentication-failure", "...-1", "...-2", "...-3"] in encounter order
    - {#id} override: a heading `### Foo Bar {#custom-anchor}` contributes VERBATIM "custom-anchor" (override wins; the slug of "Foo Bar" is NOT what a link must match)
    - emphasis strip: a heading `### \`code\` Term` slugifies from rendered text (backticks/`**` stripped before slugifying), not raw markdown
    - fence mask: a `## Heading` shown INSIDE a ```markdown code fence does NOT enter the anchor set
  </behavior>
  <action>
    Create `scripts/validation/check-nav-hub-links.mjs` (standalone, NO CHAIN_PHASES array). Copy the reusable helpers verbatim from their sources (readFile, walkMd, relNormalize from c17; buildFenceMask from the retrofit fork). Implement:
    (a) `githubSlug(text)`: lowercase; remove every char NOT in `[a-z0-9 _-]` IN PLACE (do NOT replace with a space); replace each remaining space with a single hyphen; do NOT collapse consecutive hyphens.
    (b) `resolvableAnchorSet(relPath)`: fence-mask the raw content; regex-scan the WHOLE content for `/\{#([a-zA-Z0-9_-]+)\}/g` -> add each VERBATIM; regex-scan non-fenced lines for `/^#{1,6}\s+(.*)$/`, strip any trailing `{#...}` and inline `**`/backtick emphasis, then `githubSlug()` with a `Map<slugBase,count>` encounter-order dedup (2nd occurrence -> `-1`, 3rd -> `-2`, ...); union the override set and the deduped-slug set. Cache per file.
    (c) A `--self-test` mode asserting every case in the behavior block; exit 0 on all-pass, nonzero with the first failing case otherwise.
    This task builds the RESOLVER only (the scan/report wiring is Task 2).
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs --self-test; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-nav-hub-links.mjs --self-test` exits 0 and prints a PASS count for all behavior-block cases
    - The double-hyphen case (`ios-compliance--access-blocked`) and the single-hyphen case (`iosipados-quick-reference`) both pass — proving strip-in-place, no-collapse
    - The 4x-duplicate dedup case yields `-1/-2/-3` suffixes in encounter order (proving Map-count, not plain Set)
    - The `{#id}`-override case resolves the override string verbatim, ignoring the heading-text slug
    - `grep -c 'CHAIN_PHASES' scripts/validation/check-nav-hub-links.mjs` returns 0 (standalone, not a chained validator)
  </acceptance_criteria>
  <done>The slugify + anchor-set resolver matches GitHub's exact render behavior (double-hyphen, no-collapse, encounter-order dedup, {#id}-first, fence-masked, emphasis-stripped), self-tested green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the outbound + inbound scans, path resolution, and the file:line failure report</name>
  <read_first>
    - 123-RESEARCH.md §"Pattern 3" Algorithm-outbound (steps 1-8) + Algorithm-inbound (steps 1-5) + I/O contract
    - 123-RESEARCH.md §"Don't Hand-Roll" row 2 (path.resolve(path.dirname(linkingFileAbs), target) then relNormalize — NOT path.join(cwd,...)) + Pitfall 2 (the cwd-vs-linking-dir bug is the exact class the 11 broken links exhibit)
    - 123-RESEARCH.md §Security Domain (existsSync naturally bounds `../../../etc/passwd`-style targets to "file not found" — no throw, no arbitrary read; keep it that way)
    - scripts/validation/c17-eee-contract.mjs (its full-corpus main() / exit-code + --verbose reporting shape as the pattern to mirror)
    - the resolver from Task 1
  </read_first>
  <action>
    Extend the script with the two-direction scan and CLI (`node scripts/validation/check-nav-hub-links.mjs [--verbose]`):
    OUTBOUND — `HUB_PATHS = [docs/index.md, docs/common-issues.md, docs/quick-ref-l1.md, docs/quick-ref-l2.md]`; for each hub, read (CRLF-normalized), fence-mask, scan non-fenced lines with `/\[([^\]]*)\]\(([^)]+)\)/g` for `(text,target,line)`; skip `/^(https?:|mailto:)/`; split target on the first `#` into filePart/fragPart; resolve filePart via `path.resolve(path.dirname(hubAbs), filePart)` then `relNormalize` (empty filePart = same file); `existsSync` FAIL if missing; if fragPart present, check membership in the target's `resolvableAnchorSet` FAIL if absent.
    INBOUND — `walkMd('docs')`; for every `.md`, scan links resolved relative to THAT file's dir; keep only links whose resolved target relNormalizes to one of HUB_PATHS; check fragPart against the (post-retrofit) hub's anchor set.
    REPORT — collect every failure as `file:line -> [text](target)` with a reason (`target file not found: <resolved>` or `anchor not found: #<frag>`); exit 0 iff zero failures across both directions, else print all failures + a summary count and exit nonzero. Path resolution must never throw on a pathological `../` target — a nonexistent resolved path simply FAILs the existence check.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs --verbose; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The checker runs to completion on the live corpus and prints a report; because the 12 pre-existing breaks are NOT yet fixed at this plan's point in the sequence, it is EXPECTED to exit nonzero and list `quick-ref-l2.md:316` (and the other 10 `../` lines) plus `common-issues.md:360` — proving it actually detects the known-broken links (this is the true-positive proof; 123-04 fixes them and re-runs to 0)
    - Every reported failure line is formatted `file:line -> [text](target)` with a not-found reason
    - Outbound and inbound are BOTH scanned (a corpus-wide `walkMd('docs')` pass runs, filtered to hub targets)
    - A synthetic `../../../../etc/passwd`-style target resolves to a not-found FAIL, not a throw (no unhandled exception)
    - Path resolution uses `path.dirname(linkingFileAbs)`, never `process.cwd()` (grep confirms no `join(process.cwd()` / `resolve(process.cwd()` inside the link-resolution function)
  </acceptance_criteria>
  <done>The checker resolves outbound + inbound links relative to each linking file's directory, reports every broken file/anchor as file:line -> link, and is proven to flag the 12 known pre-existing breaks (true-positive) — ready for 123-04 to gate SC2 after the fixes land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown link target -> filesystem | a relative/`../` target could in principle escape the repo tree |
| checker verdict -> SC2 claim | an approximated slugifier could false-green a genuinely broken link, laundering false confidence into "no broken links" |

No runtime/network/auth/user-input surface — build-time linter over a first-party, code-reviewed markdown tree (RESEARCH §Security Domain).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-03 | Tampering | slugifier disagreeing with GitHub's render (double-hyphen / dedup) and false-greening a broken anchor | mitigate | GitHub-exact algorithm (strip-in-place, no-collapse, encounter-order dedup, {#id}-first); --self-test locks the 3 verified edge cases; Task 2 true-positive proof against the 12 known breaks |
| T-123-04 | Information Disclosure | pathological `../../../etc/passwd` link target reading outside the tree | mitigate | resolution uses existsSync only (no fs read/exec); out-of-tree target simply FAILs "file not found" — no throw, no arbitrary read (RESEARCH §Security Domain) |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | zero external packages — node built-ins only (RESEARCH §Standard Stack: N/A) |
</threat_model>

<verification>
- `node scripts/validation/check-nav-hub-links.mjs --self-test` exits 0 (slug edge cases locked)
- Full run flags the 12 known pre-existing breaks (true-positive) before 123-04's fixes
- No CHAIN_PHASES; standalone validator
- Path resolution is linking-file-relative, existsSync-bounded (no throw on out-of-tree targets)
</verification>

<success_criteria>
A GitHub-exact, standalone, self-tested link-checker exists that scans both directions and reports every broken file/anchor as `file:line -> link`, proven to catch the 12 known breaks. It is the SC2 gate 123-04 runs after the fixes land. No doc is mutated in this plan.
</success_criteria>

<output>
Create `.planning/phases/123-orphan-nav-hub-retrofit-navigation-last/123-02-SUMMARY.md` when done.
</output>
