---
phase: 27-ios-admin-setup-corporate-ade-path
plan: "02"
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - docs/admin-setup-ios/01-apns-certificate.md
  - docs/admin-setup-ios/02-abm-token.md
autonomous: true
requirements: [ACORP-01, ACORP-02]

must_haves:
  truths:
    - "An admin following the APNs certificate guide can create a new certificate and understands the renew-not-replace rule and cross-platform expiry impact"
    - "An admin following the ABM/ADE token guide can configure the iOS enrollment token using cross-references to the macOS ABM guide for shared portal steps, with only iOS-specific differences documented inline"
    - "The supervised-only callout pattern (D-01) is used consistently on any supervised-only setting in both guides"
    - "Neither guide duplicates macOS ABM portal click-paths"
  artifacts:
    - path: "docs/admin-setup-ios/01-apns-certificate.md"
      provides: "APNs certificate creation, renewal, and cross-platform expiry impact guide"
      contains: "Renew"
    - path: "docs/admin-setup-ios/02-abm-token.md"
      provides: "ABM/ADE token guide with macOS cross-references and iOS-specific differences"
      contains: "admin-setup-macos/01-abm-configuration.md"
  key_links:
    - from: "docs/admin-setup-ios/01-apns-certificate.md"
      to: "docs/admin-setup-ios/00-overview.md"
      via: "See Also / navigation link"
      pattern: "00-overview.md"
    - from: "docs/admin-setup-ios/02-abm-token.md"
      to: "docs/admin-setup-macos/01-abm-configuration.md"
      via: "cross-reference links per D-05/D-06/D-07"
      pattern: "admin-setup-macos/01-abm-configuration.md"
    - from: "docs/admin-setup-ios/01-apns-certificate.md"
      to: "docs/admin-setup-ios/02-abm-token.md"
      via: "Next step footer"
      pattern: "02-abm-token.md"
---

<objective>
Create the APNs certificate guide (ACORP-01) and ABM/ADE token guide (ACORP-02) — the two infrastructure prerequisite guides for iOS/iPadOS corporate ADE setup.

Purpose: APNs certificate is shared cross-platform infrastructure (one expired certificate breaks ALL Apple MDM). ABM/ADE token links Apple Business Manager to Intune for iOS device syncing. Both must be configured before an enrollment profile can be created (per D-14 dependency chain).

Output:
- `docs/admin-setup-ios/01-apns-certificate.md` — APNs certificate creation, renewal, and cross-platform expiry warning
- `docs/admin-setup-ios/02-abm-token.md` — ABM/ADE token setup with macOS cross-references
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-ios-admin-setup-corporate-ade-path/27-CONTEXT.md
@.planning/phases/27-ios-admin-setup-corporate-ade-path/27-RESEARCH.md
@.planning/phases/27-ios-admin-setup-corporate-ade-path/27-01-SUMMARY.md

<interfaces>
<!-- Template and structural references from Plan 01 and existing macOS guides -->

From docs/_templates/admin-template-ios.md (created in Plan 01):
- platform: iOS frontmatter
- Platform gate blockquote with portal navigation caveat reference to overview
- Supervised-only callout pattern: > 🔒 **Supervised only:** [feature] requires supervised mode. [...] See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
- "What breaks if misconfigured" callout convention
- Structure: Prerequisites > Steps > Verification > Configuration-Caused Failures > Renewal/Maintenance > See Also

From docs/admin-setup-macos/01-abm-configuration.md (cross-reference target for ACORP-02):
- Step anchors for cross-references:
  - #step-1-download-intune-public-key
  - #step-2-create-mdm-server-and-download-server-token
  - #step-3-upload-server-token-to-intune
  - #step-4-assign-devices-to-mdm-server
  - #step-5-set-default-enrollment-profile
- Token Sync Mechanics table: 24h auto sync (macOS), manual 15min rate limit, full 7-day sync

From docs/admin-setup-ios/00-overview.md (created in Plan 01):
- ## Portal Navigation Note anchor at #portal-navigation-note (D-17 caveat lives here)
- Dependency chain: APNs -> ABM Token -> Enrollment Profile

From 27-RESEARCH.md — Technical Facts Verified:
- APNs navigation: Devices > Device onboarding > Enrollment > Apple tab > Apple MDM Push Certificate
- APNs creation: 4 steps (download CSR, create cert at Apple portal, upload .pem to Intune, enter Apple ID)
- APNs renewal: find EXISTING cert, select Renew (NOT Create), upload new CSR, download new .pem
- APNs lifecycle: 365 days, 30-day grace period, must renew with SAME Apple ID, unique UID preserved on renewal
- iOS ADE token differences from macOS: device type filter (iPhone/iPad), platform select (iOS/iPadOS), 12h delta sync (not 24h), IMEI lookup available
- ADE token limits: 1,000 profiles/token, 200,000 devices/profile, 2,000 tokens/tenant
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create APNs certificate guide (ACORP-01)</name>
  <files>docs/admin-setup-ios/01-apns-certificate.md</files>
  <read_first>
    - docs/_templates/admin-template-ios.md (template structure to follow)
    - docs/admin-setup-ios/00-overview.md (verify portal navigation caveat anchor exists)
    - docs/admin-setup-macos/01-abm-configuration.md (APNs renewal row in Renewal/Maintenance table for cross-platform context)
    - .planning/phases/27-ios-admin-setup-corporate-ade-path/27-RESEARCH.md (APNs technical facts section)
    - .planning/phases/27-ios-admin-setup-corporate-ade-path/27-CONTEXT.md (decisions D-08 through D-11)
  </read_first>
  <action>
Create `docs/admin-setup-ios/01-apns-certificate.md` following the iOS admin template structure.

**Frontmatter:**
```yaml
---
last_verified: 2026-04-16
review_by: 2026-07-15
applies_to: ADE
audience: admin
platform: iOS
---
```

**Platform gate blockquote:**
```markdown
> **Platform gate:** This guide covers iOS/iPadOS APNs certificate configuration for Intune MDM.
> For macOS ADE setup, see [macOS Admin Setup Guides](../admin-setup-macos/00-overview.md).
> For iOS/iPadOS enrollment terminology, see the [Apple Provisioning Glossary](../_glossary-macos.md).
> Portal navigation may vary by Intune admin center version. See [Overview](00-overview.md#portal-navigation-note) for details.
```

**Title:** `# Apple Push Notification (APNs) Certificate`

**Introductory paragraph:** Explain that the APNs certificate enables MDM communication between Intune and ALL Apple devices (iOS, iPadOS, and macOS). State explicitly (per D-11): this is shared cross-platform infrastructure — one certificate covers all Apple platforms. If it expires or is replaced, MDM communication to ALL enrolled Apple devices stops simultaneously.

**Prerequisites section (per D-08):**
```markdown
## Prerequisites

- Intune Administrator role in Microsoft Intune admin center
- A company email address Apple ID (NOT a personal Apple ID) — as a best practice, use a distribution list monitored by more than one person
- Microsoft Intune Plan 1 (or higher) subscription
- Access to the [Apple Push Certificates Portal](https://identity.apple.com)
```

Include "What breaks" callout on the Apple ID prerequisite (per Pitfall 3 from research):

```markdown
> **What breaks if misconfigured:** Using a personal Apple ID means the APNs certificate cannot be renewed if that employee leaves the organization. ALL enrolled iOS, iPadOS, and macOS devices lose MDM communication when the certificate expires. Symptom appears in: Apple Push Certificates Portal (cannot log in to renew) and Intune admin center (all Apple devices show "Not checking in" after expiration).
```

**Steps section — Step 1: Create the Apple Push Notification certificate:**

```markdown
### Step 1: Download the certificate signing request from Intune

#### In Intune admin center

1. Sign in to [Intune admin center](https://intune.microsoft.com).
2. Navigate to **Devices** > **Device onboarding** > **Enrollment** > **Apple** tab > **Apple MDM Push Certificate**.
3. Select **Download your CSR** to save the certificate signing request (.csr) file locally.
4. Grant permission for Microsoft to send user and device information to Apple when prompted.
```

```markdown
### Step 2: Create the certificate in Apple Push Certificates Portal

#### In Apple Push Certificates Portal

1. Navigate to [Apple Push Certificates Portal](https://identity.apple.com).
2. Sign in with the **company email address Apple ID** identified in Prerequisites.
3. Select **Create a Certificate**.
4. Accept the Terms of Use.
5. Upload the .csr file downloaded in Step 1.
6. Select **Download** to save the resulting .pem certificate file locally.
```

> **What breaks if misconfigured:** Uploading the wrong CSR file (e.g., from a different Intune tenant) creates a certificate that cannot communicate with your Intune instance. Symptom appears in: Intune admin center (certificate upload succeeds but devices cannot check in).

```markdown
### Step 3: Upload the certificate to Intune

#### In Intune admin center

1. Return to **Devices** > **Device onboarding** > **Enrollment** > **Apple** tab > **Apple MDM Push Certificate**.
2. Enter the Apple ID used in Step 2. Record this Apple ID — it is required for all future renewals.
3. Upload the .pem certificate file downloaded from Apple Push Certificates Portal.
4. Select **Upload**.
```

**Renewal section (CRITICAL — per D-09, this is the highest-severity callout):**

```markdown
## Renewal

The APNs certificate is valid for **365 days**. A **30-day grace period** allows renewal after expiration, but during the grace period MDM communication may be degraded. Set a calendar reminder 30 days before expiration.

### Renew vs. Create: Critical Distinction

> **What breaks if misconfigured:** Creating a **NEW** APNs certificate instead of **renewing** the existing one immediately breaks MDM communication to **ALL** enrolled iOS, iPadOS, and macOS devices. A new certificate has a different UID. Devices enrolled with the old certificate cannot be managed — they must be wiped and re-enrolled. This affects every Apple device in your organization simultaneously. **Always renew. Never create new.**
```

The renew-not-replace callout MUST be visually prominent — use the blockquote "What breaks" format with bold emphasis on key phrases as shown above.

**Renewal steps:**

```markdown
### Renewal Steps

#### In Intune admin center

1. Navigate to **Devices** > **Device onboarding** > **Enrollment** > **Apple** tab > **Apple MDM Push Certificate**.
2. Select **Download your CSR** to download a new certificate signing request.

#### In Apple Push Certificates Portal

3. Sign in with the **same Apple ID** used to create the original certificate.
4. Find the **existing** certificate in the certificate list.
5. Select **Renew** on the existing certificate. Do NOT select "Create a Certificate."
6. Upload the new CSR from step 2.
7. Download the renewed .pem file.

#### In Intune admin center

8. Upload the renewed .pem file to Intune.
9. Verify the new expiration date (should be ~365 days from renewal).
```

> **What breaks if misconfigured:** Renewing with a DIFFERENT Apple ID than the one used to create the certificate fails. The Apple Push Certificates Portal does not allow a different Apple ID to renew an existing certificate. Symptom appears in: Apple Push Certificates Portal (certificate not visible under wrong Apple ID).

**Verification section:**
```markdown
## Verification

- [ ] APNs certificate status shows "Active" in Intune admin center > Devices > Device onboarding > Enrollment > Apple > Apple MDM Push Certificate
- [ ] Certificate expiration date is visible (should be ~365 days from creation or renewal)
- [ ] Apple ID used is a company email address (not a personal Apple ID)
- [ ] Apple ID is recorded and accessible to multiple IT administrators
```

**Configuration-Caused Failures table:**

| Misconfiguration | Portal | Symptom | Runbook |
|------------------|--------|---------|---------|
| New certificate created instead of renewed | Apple Push Certificates Portal | ALL enrolled iOS, iPadOS, and macOS devices lose MDM communication; devices show "Not checking in" | iOS L1 runbooks (Phase 30) |
| Personal Apple ID used | Apple Push Certificates Portal | Certificate cannot be renewed after employee departure; all Apple devices lose MDM at expiration | iOS L1 runbooks (Phase 30) |
| Certificate expired without renewal | Intune | All Apple device MDM communication stops; 30-day grace period for renewal | iOS L1 runbooks (Phase 30) |
| Wrong CSR uploaded | Apple Push Certificates Portal | Certificate created but cannot communicate with Intune tenant | iOS L1 runbooks (Phase 30) |
| Renewed with different Apple ID | Apple Push Certificates Portal | Existing certificate not visible; renewal fails | iOS L1 runbooks (Phase 30) |

**Renewal / Maintenance table:**

| Component | Renewal Period | Consequence of Lapse | Renewal Steps |
|-----------|---------------|---------------------|---------------|
| APNs certificate (.pem) | Annual (365 days) | ALL MDM communication to ALL enrolled iOS, iPadOS, and macOS devices stops immediately | See [Renewal](#renewal) above |

**See Also:**
```markdown
## See Also

- [ABM/ADE Token Configuration](02-abm-token.md)
- [iOS/iPadOS Admin Setup Overview](00-overview.md)
- [iOS/iPadOS ADE Lifecycle](../ios-lifecycle/01-ade-lifecycle.md)
- [Apple Provisioning Glossary](../_glossary-macos.md)

---
*Next step: [ABM/ADE Token](02-abm-token.md)*
```

**Changelog footer:**
```markdown
---

| Date | Change | Author |
|------|--------|--------|
| 2026-04-16 | Initial version -- APNs certificate creation, renewal, and cross-platform expiry impact | -- |
```

Per D-10: No diagnostic or troubleshooting steps. This guide is preventive, not reactive. Runbook links point to Phase 30 (future) iOS L1 runbooks.
  </action>
  <verify>
    <automated>bash -c "test -f docs/admin-setup-ios/01-apns-certificate.md && grep -c 'platform: iOS' docs/admin-setup-ios/01-apns-certificate.md && grep -c 'Renew' docs/admin-setup-ios/01-apns-certificate.md && grep -c 'Never create new' docs/admin-setup-ios/01-apns-certificate.md && grep -c 'ALL.*iOS.*iPadOS.*macOS' docs/admin-setup-ios/01-apns-certificate.md && grep -c 'identity.apple.com' docs/admin-setup-ios/01-apns-certificate.md"</automated>
  </verify>
  <acceptance_criteria>
    - docs/admin-setup-ios/01-apns-certificate.md exists
    - File contains `platform: iOS` in frontmatter
    - File contains `last_verified: 2026-04-16`
    - File contains `Platform gate:` blockquote
    - File contains `## Prerequisites` with company email address Apple ID requirement
    - File contains `## Renewal` section with renew-vs-create distinction
    - File contains the text "Always renew. Never create new." (or equivalent strong renew-not-replace language)
    - File contains cross-platform expiry warning mentioning iOS, iPadOS, AND macOS (per D-11)
    - File contains `What breaks if misconfigured` callouts (at least 3 instances)
    - File contains `## Verification` checklist
    - File contains `## Configuration-Caused Failures` table with at least 4 rows
    - File contains `## Renewal / Maintenance` table with APNs certificate row
    - File contains `## See Also` with link to 02-abm-token.md
    - File contains `Next step:` footer linking to 02-abm-token.md
    - File does NOT contain troubleshooting procedures (per D-10 — preventive only)
    - File does NOT contain `platform: macOS`
  </acceptance_criteria>
  <done>APNs certificate guide covers creation, renewal with renew-not-replace rule as highest-severity callout, and cross-platform expiry impact warning stating one expired certificate breaks ALL Apple platform MDM communication</done>
</task>

<task type="auto">
  <name>Task 2: Create ABM/ADE token guide (ACORP-02)</name>
  <files>docs/admin-setup-ios/02-abm-token.md</files>
  <read_first>
    - docs/_templates/admin-template-ios.md (template structure to follow)
    - docs/admin-setup-macos/01-abm-configuration.md (PRIMARY cross-reference target — verify all 5 step anchors)
    - docs/admin-setup-ios/01-apns-certificate.md (just created in Task 1 — prerequisite link)
    - docs/admin-setup-ios/00-overview.md (verify portal navigation note anchor)
    - .planning/phases/27-ios-admin-setup-corporate-ade-path/27-RESEARCH.md (ADE token iOS differences section)
    - .planning/phases/27-ios-admin-setup-corporate-ade-path/27-CONTEXT.md (decisions D-05 through D-07)
  </read_first>
  <action>
Create `docs/admin-setup-ios/02-abm-token.md` following the iOS admin template structure with the cross-reference strategy from D-05, D-06, D-07.

**Frontmatter:**
```yaml
---
last_verified: 2026-04-16
review_by: 2026-07-15
applies_to: ADE
audience: admin
platform: iOS
---
```

**Platform gate blockquote:**
```markdown
> **Platform gate:** This guide covers iOS/iPadOS ADE token configuration via Apple Business Manager and Intune.
> For macOS ADE setup, see [macOS Admin Setup Guides](../admin-setup-macos/00-overview.md).
> For iOS/iPadOS enrollment terminology, see the [Apple Provisioning Glossary](../_glossary-macos.md).
> Portal navigation may vary by Intune admin center version. See [Overview](00-overview.md#portal-navigation-note) for details.
```

**Title:** `# ABM/ADE Token Configuration for iOS/iPadOS`

**Introductory paragraph:** Explain that the ADE token links Apple Business Manager to Intune, enabling device syncing and enrollment profile assignment for iOS/iPadOS devices. State that the steps are structurally identical to the macOS ABM token configuration — this guide cross-references the macOS guide for shared portal steps and documents only iOS-specific differences inline. Mention that Apple School Manager (ASM) is functionally identical to ABM for MDM purposes.

**Prerequisites:**
```markdown
## Prerequisites

- APNs certificate configured and active (see [APNs Certificate Guide](01-apns-certificate.md))
- Intune Administrator role in Microsoft Intune admin center
- Device Manager or Administrator role in Apple Business Manager
- Managed Apple ID (NOT personal Apple ID)
- Microsoft Intune Plan 1 (or higher) subscription
- Apple Business Manager account with verified organization
```

> **What breaks if misconfigured:** Using a personal Apple ID means the ADE token cannot be renewed if that employee leaves. New device syncing stops when the token expires (365 days). Existing enrolled devices are unaffected. Symptom appears in: ABM (cannot log in to renew token) and Intune admin center (new devices stop appearing after token expiry).

**"How iOS ADE Token Setup Differs from macOS" section:**
```markdown
## How iOS ADE Token Setup Differs from macOS

The four-step ADE token creation process is the same for iOS/iPadOS and macOS. This guide cross-references the [macOS ABM Configuration Guide](../admin-setup-macos/01-abm-configuration.md) for shared portal steps and documents only iOS-specific differences.

| Aspect | macOS | iOS/iPadOS |
|--------|-------|------------|
| Device type filter in ABM | Mac | iPhone, iPad |
| Platform selection in Intune | macOS | iOS/iPadOS |
| Delta sync interval | Every 24 hours | Every 12 hours |
| Device lookup | Serial number only | Serial number or IMEI |
```

**Steps section — each step uses the cross-reference pattern from D-05:**

```markdown
## Steps

### Step 1: Download the Intune public key certificate

The Intune public key certificate is used to establish trust between Intune and Apple Business Manager. Follow [ABM Configuration: Step 1 — Download Intune Public Key](../admin-setup-macos/01-abm-configuration.md#step-1-download-intune-public-key) in the macOS ABM guide.

**iOS-specific difference:** None. The public key download process is identical for iOS/iPadOS and macOS.
```

```markdown
### Step 2: Create MDM server and download server token in ABM

The MDM server in ABM represents your Intune tenant. Creating the server and downloading the token establishes the enrollment program link. Follow [ABM Configuration: Step 2 — Create MDM Server and Download Server Token](../admin-setup-macos/01-abm-configuration.md#step-2-create-mdm-server-and-download-server-token) in the macOS ABM guide.

**iOS-specific difference:** If your organization uses separate MDM servers for macOS and iOS/iPadOS, use a descriptive name that identifies the platform (e.g., "Intune Production — iOS/iPadOS").
```

```markdown
### Step 3: Upload server token to Intune

Upload the .p7m server token downloaded from ABM to Intune. Follow [ABM Configuration: Step 3 — Upload Server Token to Intune](../admin-setup-macos/01-abm-configuration.md#step-3-upload-server-token-to-intune) in the macOS ABM guide.

**iOS-specific difference:** When creating the token in Intune, select **iOS/iPadOS** as the platform. This determines which device types sync through this token.
```

```markdown
### Step 4: Assign devices to MDM server in ABM

Assign iOS/iPadOS devices to the MDM server so they are synced to Intune. Follow [ABM Configuration: Step 4 — Assign Devices to MDM Server](../admin-setup-macos/01-abm-configuration.md#step-4-assign-devices-to-mdm-server) in the macOS ABM guide.

**iOS-specific difference:** When filtering devices in ABM, filter by device type **iPhone** or **iPad** to locate iOS/iPadOS devices. iOS devices can also be looked up by IMEI in addition to serial number.
```

> **What breaks if misconfigured:** Device must be assigned to the MDM server BEFORE first power-on. If powered on without assignment, the device proceeds through non-managed Setup Assistant and requires a factory reset to fix. Symptom appears in: the device (standard iOS setup, no MDM enrollment).

**Token Sync Mechanics (iOS-specific values):**
```markdown
## Token Sync Mechanics

| Sync Type | Interval | Notes |
|-----------|----------|-------|
| Automatic (delta) | Every 12 hours | Picks up newly assigned devices; faster than macOS (24h) |
| Manual (on-demand) | Rate-limited to once per 15 minutes | Triggered from Intune admin center |
| Full sync | Every 7 days | Re-fetches entire device list from ABM |
```

**Token Limits:**
```markdown
## Token Limits

| Limit | Value |
|-------|-------|
| Maximum enrollment profiles per token | 1,000 |
| Maximum ADE devices per profile | 200,000 |
| Maximum ADE tokens per Intune tenant | 2,000 |
```

**Verification:**
```markdown
## Verification

- [ ] ADE token shows "Active" status in Intune admin center > Devices > Enrollment > Apple > Enrollment program tokens
- [ ] Token expiration date is visible (should be ~365 days from creation)
- [ ] Apple ID matches the Managed Apple ID used in ABM
- [ ] Platform shows iOS/iPadOS (not macOS)
- [ ] At least one iOS/iPadOS device appears after sync (if devices were assigned in ABM)
```

**Configuration-Caused Failures table:**

| Misconfiguration | Portal | Symptom | Runbook |
|------------------|--------|---------|---------|
| Personal Apple ID used for token creation | ABM | Token cannot be renewed after employee departure; new devices stop syncing | iOS L1 runbooks (Phase 30) |
| Device not assigned before first power-on | ABM | Device runs standard iOS setup; does not enroll in MDM | iOS L1 runbooks (Phase 30) |
| Wrong MDM server selected for device | ABM | Device enrolls to wrong Intune tenant or profile | iOS L1 runbooks (Phase 30) |
| Expired token not renewed | Intune | New ABM-assigned devices stop appearing in Intune; existing enrolled devices unaffected | iOS L1 runbooks (Phase 30) |
| Wrong platform selected (macOS instead of iOS) | Intune | iOS/iPadOS devices do not sync through this token | iOS L1 runbooks (Phase 30) |

**Renewal / Maintenance:**

| Component | Renewal Period | Consequence of Lapse | Renewal Steps |
|-----------|---------------|---------------------|---------------|
| ADE token (.p7m) | Annual (365 days) | New devices assigned in ABM stop appearing in Intune; existing enrolled devices unaffected | Download new token from ABM > upload to Intune at Devices > Enrollment > Apple > Enrollment program tokens |
| ABM Terms of Service | On Apple update (ad-hoc) | Apple suspends all syncing until new T&C accepted | Accept in ABM portal; no Intune action needed |

**See Also:**
```markdown
## See Also

- [APNs Certificate](01-apns-certificate.md)
- [ADE Enrollment Profile](03-ade-enrollment-profile.md)
- [macOS ABM Configuration](../admin-setup-macos/01-abm-configuration.md)
- [iOS/iPadOS Admin Setup Overview](00-overview.md)
- [iOS/iPadOS ADE Lifecycle](../ios-lifecycle/01-ade-lifecycle.md)
- [Apple Provisioning Glossary](../_glossary-macos.md)

---
*Next step: [ADE Enrollment Profile](03-ade-enrollment-profile.md)*
```

**Changelog footer:**
```markdown
---

| Date | Change | Author |
|------|--------|--------|
| 2026-04-16 | Initial version -- ABM/ADE token guide with macOS cross-references and iOS-specific differences | -- |
```

**Key constraint (D-06):** This guide does NOT duplicate macOS ABM portal click-paths. Every step links to the macOS guide section and states only the iOS-specific difference. If there is no difference (Step 1), state "None" explicitly (per D-07).
  </action>
  <verify>
    <automated>bash -c "test -f docs/admin-setup-ios/02-abm-token.md && grep -c 'platform: iOS' docs/admin-setup-ios/02-abm-token.md && grep -c 'admin-setup-macos/01-abm-configuration.md' docs/admin-setup-ios/02-abm-token.md && grep -c 'iOS-specific difference' docs/admin-setup-ios/02-abm-token.md && grep -c '12 hours' docs/admin-setup-ios/02-abm-token.md && grep -c '01-apns-certificate.md' docs/admin-setup-ios/02-abm-token.md"</automated>
  </verify>
  <acceptance_criteria>
    - docs/admin-setup-ios/02-abm-token.md exists
    - File contains `platform: iOS` in frontmatter
    - File contains `last_verified: 2026-04-16`
    - File contains at least 4 cross-reference links to `admin-setup-macos/01-abm-configuration.md` (one per step)
    - File contains `iOS-specific difference` text for each of the 4 steps
    - File contains comparison table with macOS vs iOS differences (12h vs 24h sync, device type filter, IMEI lookup)
    - File contains `12 hours` delta sync interval (not 24 hours as macOS)
    - File contains prerequisite link to `01-apns-certificate.md`
    - File contains `What breaks if misconfigured` callouts
    - File contains `## Verification` checklist with Platform shows iOS/iPadOS check
    - File contains `## Configuration-Caused Failures` table with at least 4 rows
    - File contains `## Renewal / Maintenance` table with ADE token and ABM Terms of Service rows
    - File contains `Next step:` footer linking to `03-ade-enrollment-profile.md`
    - File does NOT contain duplicated macOS portal click-paths (per D-06)
    - File does NOT contain `platform: macOS`
    - File mentions Apple School Manager in one sentence
  </acceptance_criteria>
  <done>ABM/ADE token guide uses structured cross-reference pattern (D-05/D-06/D-07) linking to macOS ABM guide for shared portal steps, with iOS-specific differences (device type filter, platform selection, 12h sync, IMEI lookup) documented inline per step</done>
</task>

</tasks>

<verification>
- APNs certificate guide covers creation (3 steps), renewal (renew-not-replace), and cross-platform expiry warning
- ABM token guide cross-references macOS ABM guide for all 4 steps without duplicating click-paths
- Both guides follow iOS admin template structure (Prerequisites, Steps, Verification, Configuration-Caused Failures, Renewal/Maintenance, See Also)
- Both guides have platform: iOS frontmatter and Platform gate blockquotes
- Both guides link to each other and to the overview page
- grep -c "admin-setup-macos/01-abm-configuration.md" docs/admin-setup-ios/02-abm-token.md returns at least 4
</verification>

<success_criteria>
- An admin following 01-apns-certificate.md can create and renew an APNs certificate, understanding the renew-not-replace rule and cross-platform impact
- An admin following 02-abm-token.md can configure the iOS ADE token using macOS guide cross-references with only iOS-specific differences inline
- Neither guide contains troubleshooting procedures (preventive only)
- Neither guide duplicates macOS portal click-paths
</success_criteria>

<output>
After completion, create `.planning/phases/27-ios-admin-setup-corporate-ade-path/27-02-SUMMARY.md`
</output>
