---
phase: 27-ios-admin-setup-corporate-ade-path
plan: "03"
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - docs/admin-setup-ios/03-ade-enrollment-profile.md
autonomous: true
requirements: [ACORP-03]

must_haves:
  truths:
    - "An admin following the ADE enrollment profile guide can configure supervised mode, select an authentication method, customize Setup Assistant panes, and enable locked enrollment"
    - "Every supervised-only setting in the guide uses the exact D-01 callout pattern linking to ios-lifecycle/00-enrollment-overview.md#supervision"
    - "The supervised-only callout is placed immediately after each setting description, before configuration steps (per D-04)"
    - "The guide documents concepts and outcomes rather than exact click-paths for enrollment profile creation (per D-17)"
    - "Authentication methods documented include modern auth (recommended), Company Portal, and legacy (not recommended)"
  artifacts:
    - path: "docs/admin-setup-ios/03-ade-enrollment-profile.md"
      provides: "ADE enrollment profile configuration guide with supervised-only callouts"
      contains: "Supervised only"
  key_links:
    - from: "docs/admin-setup-ios/03-ade-enrollment-profile.md"
      to: "docs/ios-lifecycle/00-enrollment-overview.md"
      via: "supervised-only callout link"
      pattern: "ios-lifecycle/00-enrollment-overview.md#supervision"
    - from: "docs/admin-setup-ios/03-ade-enrollment-profile.md"
      to: "docs/admin-setup-ios/02-abm-token.md"
      via: "prerequisite link"
      pattern: "02-abm-token.md"
    - from: "docs/admin-setup-ios/03-ade-enrollment-profile.md"
      to: "docs/ios-lifecycle/01-ade-lifecycle.md"
      via: "See Also cross-reference"
      pattern: "ios-lifecycle/01-ade-lifecycle.md"
---

<objective>
Create the ADE enrollment profile guide (ACORP-03) — the most complex of the three Phase 27 guides, establishing the supervised-only callout pattern in practice.

Purpose: This guide is where corporate ADE enrollment is fully configured: supervised mode, authentication method, Setup Assistant customization, locked enrollment, user affinity, and await final configuration. Every supervised-only setting MUST use the D-01 callout pattern — this guide is the first real-world application of the pattern that Phase 28-32 will inherit.

Output:
- `docs/admin-setup-ios/03-ade-enrollment-profile.md` — ADE enrollment profile configuration guide
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-ios-admin-setup-corporate-ade-path/27-CONTEXT.md
@.planning/phases/27-ios-admin-setup-corporate-ade-path/27-RESEARCH.md
@.planning/phases/27-ios-admin-setup-corporate-ade-path/27-01-SUMMARY.md

<interfaces>
<!-- Key references for the enrollment profile guide -->

From docs/_templates/admin-template-ios.md (created in Plan 01):
- Supervised-only callout pattern (exact form):
  > 🔒 **Supervised only:** [feature/setting name] requires supervised mode. [1-2 sentence explanation of what this means for unsupervised devices.] See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
- "What breaks if misconfigured" callout convention
- Structure: Prerequisites > Steps > Verification > Configuration-Caused Failures > Renewal/Maintenance > See Also

From docs/admin-setup-macos/02-enrollment-profile.md (structural template):
- Step 1: Create enrollment profile
- Step 2: Configure enrollment settings (table format: Setting | Options | Default | Notes)
- Step 3: Configure Setup Assistant screens (table: Screen | Min Version | Recommended | Notes)
- Step 4: Assign profile to devices
- "What breaks" callouts after each configurable setting

From docs/ios-lifecycle/00-enrollment-overview.md:
- ## Supervision section at #supervision — link target for all supervised-only callouts (D-02)
- Supervision set at enrollment time, cannot be added retroactively without full device erase

From docs/ios-lifecycle/01-ade-lifecycle.md:
- Stage 3 (enrollment profile applied) and Stage 4 (Setup Assistant) context
- Setup Assistant panes enumerated in Stage 4 Behind the Scenes

From 27-RESEARCH.md — ADE Enrollment Profile technical facts:
- Current nav: Devices > Enrollment > Apple > Enrollment program tokens > [token] > Profiles > Create > iOS/iPadOS
- New nav (Q2 CY2026): token > Enrollment policies > Create
- Settings table: User Affinity, Authentication method, Supervised, Locked enrollment, Await final configuration, Device name template
- Authentication methods: Setup Assistant with modern auth (recommended, iOS 13.0+), Company Portal (being removed from new UI), Setup Assistant (legacy, not recommended)
- iOS 13+ ADE auto-supervises but set explicitly to Yes
- Locked enrollment: supervised-only; 30-day removal window for non-ABM-purchased devices
- Await final configuration: device config policies only (not apps); no time limit; iOS 13+ required
- Setup Assistant panes: full iOS-specific list with min versions and deprecated panes
- Deprecated panes: Zoom (iOS 17), Display Tone (iOS 15), Device to Device Migration (iOS 13)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ADE enrollment profile guide (ACORP-03)</name>
  <files>docs/admin-setup-ios/03-ade-enrollment-profile.md</files>
  <read_first>
    - docs/_templates/admin-template-ios.md (template structure and supervised-only callout pattern)
    - docs/admin-setup-macos/02-enrollment-profile.md (macOS enrollment profile — structural precedent)
    - docs/ios-lifecycle/00-enrollment-overview.md (verify #supervision anchor exists)
    - docs/ios-lifecycle/01-ade-lifecycle.md (Stage 4 Setup Assistant pane list for cross-reference)
    - docs/admin-setup-ios/02-abm-token.md (prerequisite link verification)
    - .planning/phases/27-ios-admin-setup-corporate-ade-path/27-RESEARCH.md (enrollment profile technical facts)
    - .planning/phases/27-ios-admin-setup-corporate-ade-path/27-CONTEXT.md (decisions D-01 through D-04, D-17)
  </read_first>
  <action>
Create `docs/admin-setup-ios/03-ade-enrollment-profile.md` following the iOS admin template structure with supervised-only callouts.

**Frontmatter:**
```yaml
---
last_verified: 2026-04-16
review_by: 2026-07-15
applies_to: ADE
audience: admin
platform: iOS
---
```

**Platform gate blockquote:**
```markdown
> **Platform gate:** This guide covers iOS/iPadOS ADE enrollment profile configuration in Intune.
> For macOS enrollment profile setup, see [macOS Enrollment Profile](../admin-setup-macos/02-enrollment-profile.md).
> For iOS/iPadOS enrollment terminology, see the [Apple Provisioning Glossary](../_glossary-macos.md).
> Portal navigation may vary by Intune admin center version. See [Overview](00-overview.md#portal-navigation-note) for details.
```

**Title:** `# iOS/iPadOS ADE Enrollment Profile Configuration`

**Introductory paragraph:** Explain that the enrollment profile configures how iOS/iPadOS devices enroll through ADE, including supervision state, authentication method, Setup Assistant experience, and whether users can remove management. State this is the final step in the ADE prerequisite chain (APNs certificate > ABM token > enrollment profile). State that profile must be assigned to devices BEFORE first power-on.

**Prerequisites:**
```markdown
## Prerequisites

- ADE token configured and active with at least one iOS/iPadOS device synced (see [ABM/ADE Token Guide](02-abm-token.md))
- APNs certificate configured and active (see [APNs Certificate Guide](01-apns-certificate.md))
- Intune Administrator role
```

**Key Concepts Before You Begin:**

```markdown
## Key Concepts Before You Begin

### Supervised Mode

On iOS/iPadOS 13.0 and later, devices enrolled through ADE are **automatically supervised** regardless of this setting. However, you should explicitly set supervised mode to **Yes** in the enrollment profile to:

- Declare supervised intent in the profile configuration
- Ensure consistent behavior across all iOS versions
- Enable supervised-only settings in the enrollment profile (such as locked enrollment)

Supervision cannot be added after enrollment without a full device erase. See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision) for what supervision enables and its consequences.

### Authentication Methods

| Method | iOS Version | Recommendation | Notes |
|--------|-------------|----------------|-------|
| Setup Assistant with modern authentication | iOS 13.0+ | **Recommended** | Supports MFA, Conditional Access, JIT registration (can eliminate Company Portal dependency) |
| Company Portal | iOS 13.0+ | Being phased out | Requires Entra registration via Company Portal app; being removed from new enrollment policies UI experience |
| Setup Assistant (legacy) | Pre-iOS 13.0 | **Not recommended** | Does not support modern Conditional Access; use only for legacy iOS device fleets |

For all new deployments, use **Setup Assistant with modern authentication**.
```

> **What breaks if misconfigured:** Legacy authentication method is incompatible with modern Conditional Access policies. Devices cannot authenticate during Setup Assistant if MFA or CA policies are enforced. Symptom appears in: device Setup Assistant (authentication failure screen).

**Steps section — document concepts and outcomes, not exact click-paths (per D-17):**

```markdown
## Steps

### Step 1: Create the enrollment profile

Navigate to the ADE token in Intune admin center and create a new iOS/iPadOS enrollment profile. Enter a descriptive profile name and optional description.

> **Note:** The Intune admin center is updating enrollment profile navigation. The profile creation location may appear under **Profiles** or **Enrollment policies** depending on your tenant's rollout status. The settings and their effects are the same regardless of navigation path.
```

```markdown
### Step 2: Configure enrollment settings
```

Present enrollment settings as a table (matching macOS guide pattern), followed by per-setting "What breaks" callouts and supervised-only callouts where applicable.

**Enrollment settings table:**

| Setting | Options | Corporate ADE Recommendation | Notes |
|---------|---------|------------------------------|-------|
| User Affinity | Enroll with User Affinity / Enroll without User Affinity / Microsoft Entra shared mode | Enroll with User Affinity | Without = kiosk/shared devices; shared mode = Shared iPad (iPadOS only) |
| Authentication method | Setup Assistant with modern authentication / Company Portal / Setup Assistant (legacy) | Setup Assistant with modern authentication | See [Authentication Methods](#authentication-methods) above |
| Supervised | Yes / No | **Yes** | iOS 13+ auto-supervises via ADE, but set explicitly |
| Locked enrollment | Yes / No | **Yes** | Prevents user from removing management profile |
| Await final configuration | Yes / No | **Yes** | Holds device at Setup Assistant until device configuration policies apply |
| Device name template | Custom pattern (e.g., {{DEVICETYPE}}-{{SERIAL}}) | Organization preference | Applied during enrollment |

After the table, add per-setting detail sections with "What breaks" and supervised-only callouts:

**Supervised mode detail:**

Explain the setting. Then immediately (per D-04) add the supervised-only callout:

> 🔒 **Supervised only:** Supervised mode enables the full range of MDM capabilities including OS update enforcement, silent app installation, and supervised-only configuration profiles. Devices enrolled without supervision receive standard MDM management only and cannot be upgraded to supervised without a full device erase. See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).

> **What breaks if misconfigured:** Setting supervised to No on a corporate ADE deployment means locked enrollment cannot be enforced, supervised-only configuration profiles will not apply, and OS updates cannot be silently enforced. Changing to supervised later requires wiping every affected device. Symptom appears in: Intune admin center (supervised-only policies show "Not applicable" on affected devices).

**Locked enrollment detail:**

Explain that locked enrollment prevents users from removing the management profile via Settings > General > VPN & Device Management. Then immediately (per D-04) add:

> 🔒 **Supervised only:** Locked enrollment requires supervised mode. On unsupervised devices, this setting has no effect and users can remove the management profile from Settings > General > VPN & Device Management. See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).

> **What breaks if misconfigured:** Locked enrollment set to No allows users to navigate to Settings > General > VPN & Device Management and remove the management profile, fully unenrolling the device. This cannot be undone remotely — the device must be factory reset and re-enrolled. Symptom appears in: Intune admin center (device shows as retired/removed).

Add a note about the 30-day window: "For devices not originally purchased through ABM (added via Apple Configurator), users can see the remove management button for the first 30 days after activation even with locked enrollment set to Yes. After 30 days, the button is hidden."

**User affinity detail:**

> **What breaks if misconfigured:** Without user affinity, the device has no primary user — Company Portal will not function, user-targeted Conditional Access policies will not apply, and per-user app assignments will not deliver. Symptom appears in: Intune admin center (device shows no primary user) and Company Portal app (not functional).

**Await final configuration detail:**

Explain: holds the device at Setup Assistant until device configuration policies are installed. Only device configuration policies install during this hold — apps are NOT included. No enforced time limit; typical release within approximately 15 minutes. Supported on iOS/iPadOS 13.0+ with modern auth, userless, or shared mode enrollments. Not available when Shared iPad = Yes combined with Enroll without User Affinity.

> **What breaks if misconfigured:** Await final configuration set to No allows users to reach the home screen before device configuration policies (Wi-Fi, VPN, restrictions, certificates) apply. Devices may be non-compliant immediately after enrollment. Symptom appears in: device (user reaches home screen without management configuration) and Intune admin center (non-compliant status).

```markdown
### Step 3: Configure Setup Assistant panes
```

Introductory text: "Configure which screens users see during iOS/iPadOS Setup Assistant. Hidden screens skip that step during initial device setup. These screens can be shown or hidden per organizational preference."

**Setup Assistant panes table (iOS/iPadOS-specific list from research):**

| Screen | Min iOS/iPadOS Version | Recommended | Notes |
|--------|------------------------|-------------|-------|
| Passcode | 7.0+ | Show | |
| Location Services | 7.0+ | Organization preference | |
| Restore | 7.0+ | Show | |
| Apple ID | 7.0+ | Show | User needs for iCloud |
| Terms and conditions | 7.0+ | Show | |
| Touch ID and Face ID | 8.1+ | Show | |
| Apple Pay | 7.0+ | Hide (configure later) | |
| Siri | 7.0+ | Hide | |
| Diagnostics Data | 7.0+ | Organization preference | |
| Screen Time | 12.0+ | Hide | |
| SIM Setup | 12.0+ | Organization preference | Carrier activation |
| iMessage and FaceTime | 9.0+ | Hide | |
| Appearance | 13.0+ | Show | Light/Dark mode selection |
| Privacy | 11.3+ | Show | |
| Onboarding | 11.0+ | Hide | |
| Watch Migration | 11.0+ | Hide | |
| Emergency SOS | 16.0+ | Organization preference | |
| Action button | 17.0+ | Organization preference | iPhone 15 Pro and later |
| Apple Intelligence | 18.0+ | Organization preference | |
| Camera button | 18.0+ | Organization preference | iPhone 16 and later |
| Web content filtering | 18.2+ | Organization preference | |
| Safety and handling | 18.4+ | Organization preference | |
| Get Started | 15.0+ | Show | |
| App Store | 14.3+ | Organization preference | |
| Software Update | 12.0+ | Show | |
| Terms of Address | 16.0+ | Hide | |
| Android Migration | 9.0+ | Hide | Not applicable to corporate ADE |
| Multitasking | 26.0+ | Organization preference | iPadOS only |
| OS Showcase | 26.0+ | Organization preference | |

Note below the table: "Deprecated panes (do not include in new profiles): Zoom (deprecated iOS 17), Display Tone (deprecated iOS 15), Device to Device Migration (deprecated for iOS 13+)."

> **What breaks if misconfigured:** Setup Assistant pane configuration is cosmetic — hiding or showing panes does not affect device functionality or security. However, hiding the Passcode screen does not prevent passcode enforcement — compliance policies enforce passcode requirements after enrollment.

```markdown
### Step 4: Assign profile to devices
```

Explain: After saving the enrollment profile, assign it to devices synced through the ADE token. This can be done per-device or by setting the profile as the default for the token (recommended — all synced devices automatically receive it).

> **What breaks if misconfigured:** If no enrollment profile is assigned before the device is powered on, the device boots through standard (non-managed) iOS Setup Assistant. The device will not enroll in MDM. Fix requires a factory reset. Symptom appears in: the device (standard iOS setup with no enrollment prompt) and Intune admin center (device does not appear).

**Verification section:**
```markdown
## Verification

- [ ] Enrollment profile appears under the ADE token in Intune admin center
- [ ] Supervised mode is set to Yes
- [ ] Locked enrollment is set to Yes (for corporate-owned devices)
- [ ] Authentication method is "Setup Assistant with modern authentication"
- [ ] Await final configuration is set to Yes
- [ ] User Affinity matches organizational requirement (with affinity for user-assigned, without for kiosk/shared)
- [ ] At least one device shows the profile assignment
- [ ] Setup Assistant pane configuration matches organizational preference
```

**Configuration-Caused Failures table:**

| Misconfiguration | Portal | Symptom | Runbook |
|------------------|--------|---------|---------|
| Supervised mode set to No | Intune | Supervised-only policies show "Not applicable"; locked enrollment ineffective | iOS L1 runbooks (Phase 30) |
| Locked enrollment set to No | Intune | Users can remove management profile via Settings > General > VPN & Device Management | iOS L1 runbooks (Phase 30) |
| No user affinity on user-targeted deployment | Intune | Company Portal non-functional; user CA policies not applied; no primary user | iOS L1 runbooks (Phase 30) |
| Legacy authentication method | Intune | Setup Assistant auth failure when modern CA policies enforced | iOS L1 runbooks (Phase 30) |
| Await final configuration set to No | Intune | User reaches home screen before policies apply; immediate non-compliance | iOS L1 runbooks (Phase 30) |
| No profile assigned before device power-on | Intune | Device boots standard iOS setup; requires factory reset | iOS L1 runbooks (Phase 30) |

**No Renewal / Maintenance section** — enrollment profiles do not have a renewable component. Omit per template instructions.

**See Also:**
```markdown
## See Also

- [ABM/ADE Token Configuration](02-abm-token.md)
- [APNs Certificate](01-apns-certificate.md)
- [iOS/iPadOS Admin Setup Overview](00-overview.md)
- [iOS/iPadOS ADE Lifecycle](../ios-lifecycle/01-ade-lifecycle.md)
- [iOS/iPadOS Enrollment Path Overview](../ios-lifecycle/00-enrollment-overview.md)
- [macOS Enrollment Profile](../admin-setup-macos/02-enrollment-profile.md)
- [Apple Provisioning Glossary](../_glossary-macos.md)

---
*Previous: [ABM/ADE Token](02-abm-token.md) | [Back to Overview](00-overview.md)*
```

**Changelog footer:**
```markdown
---

| Date | Change | Author |
|------|--------|--------|
| 2026-04-16 | Initial version -- ADE enrollment profile with supervised-only callouts, authentication methods, Setup Assistant panes, and locked enrollment | -- |
```

**CRITICAL callout placement rule (D-03, D-04):** The supervised-only callouts (`> 🔒 **Supervised only:**`) MUST appear:
1. On the supervised mode setting itself
2. On the locked enrollment setting
3. Immediately AFTER each setting description, BEFORE any configuration steps
4. Using the EXACT D-01 format with link to `../ios-lifecycle/00-enrollment-overview.md#supervision`

No other settings in the enrollment profile are supervised-only (user affinity, authentication method, await final configuration, device name template are all available regardless of supervision state).
  </action>
  <verify>
    <automated>bash -c "test -f docs/admin-setup-ios/03-ade-enrollment-profile.md && grep -c 'platform: iOS' docs/admin-setup-ios/03-ade-enrollment-profile.md && grep -c 'Supervised only' docs/admin-setup-ios/03-ade-enrollment-profile.md && grep -c 'ios-lifecycle/00-enrollment-overview.md#supervision' docs/admin-setup-ios/03-ade-enrollment-profile.md && grep -c 'What breaks if misconfigured' docs/admin-setup-ios/03-ade-enrollment-profile.md && grep -c 'Setup Assistant with modern authentication' docs/admin-setup-ios/03-ade-enrollment-profile.md && grep -c 'Locked enrollment' docs/admin-setup-ios/03-ade-enrollment-profile.md"</automated>
  </verify>
  <acceptance_criteria>
    - docs/admin-setup-ios/03-ade-enrollment-profile.md exists
    - File contains `platform: iOS` in frontmatter
    - File contains `last_verified: 2026-04-16`
    - File contains exactly 2 supervised-only callouts (`> 🔒 **Supervised only:**`) — one for supervised mode, one for locked enrollment
    - Both supervised-only callouts contain the link `ios-lifecycle/00-enrollment-overview.md#supervision`
    - File contains `## Key Concepts Before You Begin` section with supervised mode explanation and authentication methods table
    - File contains authentication methods table listing modern auth (recommended), Company Portal (being phased out), and legacy (not recommended)
    - File contains enrollment settings table with User Affinity, Authentication method, Supervised, Locked enrollment, Await final configuration, and Device name template
    - File contains `What breaks if misconfigured` callouts (at least 5 instances)
    - File contains Setup Assistant panes table with at least 20 iOS-specific panes
    - File contains deprecated panes note mentioning Zoom (iOS 17), Display Tone (iOS 15)
    - File contains note about 30-day removal window for non-ABM-purchased devices
    - File contains `## Verification` checklist with supervised mode, locked enrollment, and auth method checks
    - File contains `## Configuration-Caused Failures` table with at least 5 rows
    - File does NOT contain `## Renewal / Maintenance` section (enrollment profiles have no renewable component)
    - File does NOT contain exact click-paths for profile creation navigation (per D-17 — concepts and outcomes only)
    - File does NOT contain `platform: macOS`
    - File contains link to `02-abm-token.md` as prerequisite
    - File contains link to `../ios-lifecycle/01-ade-lifecycle.md` in See Also
  </acceptance_criteria>
  <done>ADE enrollment profile guide covers supervised mode, authentication methods (modern auth recommended), Setup Assistant pane customization, locked enrollment, user affinity, and await final configuration — with supervised-only callouts on exactly 2 settings (supervised mode and locked enrollment) using the exact D-01 pattern</done>
</task>

</tasks>

<verification>
- Enrollment profile guide has exactly 2 supervised-only callouts (supervised mode + locked enrollment)
- Both callouts use exact D-01 format with link to ios-lifecycle/00-enrollment-overview.md#supervision
- Callouts placed after setting description, before configuration steps (D-04)
- Authentication methods cover all 3 options with modern auth as recommended
- Setup Assistant panes table is comprehensive (20+ panes) with deprecated panes noted
- Guide documents concepts and outcomes, not exact click-paths (D-17)
- grep -c "Supervised only" docs/admin-setup-ios/03-ade-enrollment-profile.md returns exactly 2
- grep -c "ios-lifecycle/00-enrollment-overview.md#supervision" docs/admin-setup-ios/03-ade-enrollment-profile.md returns at least 2
</verification>

<success_criteria>
- An admin following the guide can configure all enrollment profile settings for a supervised corporate ADE deployment
- Every supervised-only setting has the D-01 callout pattern placed per D-04 rules
- Authentication method guidance clearly recommends modern auth and documents why legacy/Company Portal are not recommended
- Guide is resilient to the Q2 CY2026 UI redesign by documenting settings conceptually, not as click-paths
</success_criteria>

<output>
After completion, create `.planning/phases/27-ios-admin-setup-corporate-ade-path/27-03-SUMMARY.md`
</output>
