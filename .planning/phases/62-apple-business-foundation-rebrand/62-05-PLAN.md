---
phase: 62
plan: 62-05
title: Author `02-ous-architecture.md` (OU hierarchy + 5-row scoping table + Locations→OUs migration framing)
type: execute
wave: 3
status: pending
depends_on: [62-01, 62-02, 62-03]
files_modified:
  - docs/cross-platform/apple-business/02-ous-architecture.md
requirements:
  - AB-04
threats: []
autonomous: true

must_haves:
  truths:
    - "Admin can find the Organizational Units (OU) concept doc explaining hierarchy rules (flat-by-default with optional sub-OUs; no published max-count; depth verification deferred to Phase 63 portal scan)"
    - "Admin can read the 5-row OU scoping table covering devices / content tokens / MDM servers / Managed Apple Accounts / role assignments per D-A7"
    - "Admin can read the Locations→OUs migration framing as conceptual continuity (rebrand event itself lives in the glossary rebrand-mapping table, not here)"
    - "Platform-applicability blockquote states Apple Business OUs are the Apple-side delegation primitive (no direct Intune-side analog)"
  artifacts:
    - path: "docs/cross-platform/apple-business/02-ous-architecture.md"
      provides: "AB-04 OU concept doc; SOT for Phase 64 `15-mdm-server-reassign-runbook.md` + Phase 63 `03-ous-vs-custom-roles.md`"
      contains: "## OU Scope Coverage"
      min_lines: 80
  key_links:
    - from: "docs/cross-platform/apple-business/02-ous-architecture.md OU Scope Coverage table"
      to: "5 resource scoping rows (devices / content tokens / MDM servers / accounts / role assignments)"
      via: "markdown table"
      pattern: "\\| (Devices|Content tokens|MDM servers|Managed Apple Accounts|Role assignments) \\|"
    - from: "docs/cross-platform/apple-business/02-ous-architecture.md"
      to: "docs/_glossary-apple-business.md#organizational-unit"
      via: "relative path link from cross-platform tree"
      pattern: "_glossary-apple-business\\.md"
---

<objective>
Author `02-ous-architecture.md` — the canonical Organizational Units (OU) concept doc. Per AB-04, covers OU hierarchy rules (flat-by-default with optional sub-OUs; no published max nesting depth), the 5-resource OU scoping table per D-A7, and the Locations→OUs migration framing.

Purpose: Establishes the OU primitive that Phase 63 `03-ous-vs-custom-roles.md` references for delegation-topology decisions, and that Phase 64 `15-mdm-server-reassign-runbook.md` cites for OU-scoped MDM server semantics + content-token-per-OU allocation.

Output: 1 new file at `docs/cross-platform/apple-business/02-ous-architecture.md` (~80-150 lines).
</objective>

<execution_context>
@D:/claude/Autopilot/CLAUDE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/62-apple-business-foundation-rebrand/62-CONTEXT.md
@.planning/phases/62-apple-business-foundation-rebrand/62-RESEARCH.md
@.planning/phases/62-apple-business-foundation-rebrand/62-PATTERNS.md
@docs/operations/co-management/00-overview.md

<interfaces>
<!-- Analog: docs/operations/co-management/00-overview.md (PATTERNS.md exact match) -->
<!-- 5-row scoping table per D-A7 + RESEARCH.md OU section -->
<!-- Nesting-depth deferred per CONTEXT.md <deferred> line 234 + ARCHITECTURE.md -->

Frontmatter:
```yaml
---
last_verified: 2026-05-21
review_by: 2026-07-20
applies_to: apple-business
audience: admin
platform: ios+macos
---
```

Platform-applicability blockquote (per PATTERNS.md analog from co-management/00-overview.md lines 14-19):
```markdown
> **Platform applicability:** Apple Business Organizational Units (OUs) are the Apple-side
> delegation primitive for iOS, iPadOS, and macOS Apple-managed devices. There is no direct
> Intune-side analog (Intune scope tags are the closest Microsoft-side concept; see the
> [Apple Business Governance Glossary](../../_glossary-apple-business.md) for terminology canon
> and the [Cross-Org-Boundary Cheat Sheet (Phase 64)](18-cross-org-boundary-cheat-sheet.md)
> for the full Apple-Business-vs-Intune surface disambiguation).
```

5-row OU scoping table (D-A7 mandatory rows + audit log partial-scope row):
| Resource | OU-scoped? | Notes |
| Devices | Yes | Per-OU device pool; reassignment via MDM server transfer (see 15-mdm-server-reassign-runbook.md, Phase 64) |
| Content tokens | Yes | Per-OU token; 1-year validity; OU-scoped allocation per Apple Business Manager axme0f8659ec |
| MDM servers | Yes | One MDM server per OU; assignment cascade through device transfers |
| Managed Apple Accounts | Yes | Per-OU account scope; federation per-OU; SCIM provisioning per-OU |
| Role assignments | Yes | Custom roles scoped to specific OU |
| Audit logs | Partial | Cross-OU visibility deferred to Phase 64 3×3 matrix (`17-audit-log-scoping-runbook.md`) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 62-05-01: Create `02-ous-architecture.md` with frontmatter + platform-applicability blockquote + H1 + intro + Hierarchy Rules section</name>
  <files>docs/cross-platform/apple-business/02-ous-architecture.md</files>
  <action>
Create new file at `docs/cross-platform/apple-business/02-ous-architecture.md`. Structure:

1. Frontmatter (from context).
2. Platform-applicability blockquote (verbatim from context interfaces).
3. H1 "Organizational Units (OUs) Architecture".
4. Intro paragraph: define OU as the Apple-side delegation primitive that replaced legacy ABM "Locations" effective 2026-04-14; reference glossary rebrand-mapping table at `../../_glossary-apple-business.md#rebrand-mapping-table`.
5. H2 `## Hierarchy Rules`:
   - Flat-by-default: Apple new docs describe OU layout as primarily flat (single tier).
   - Optional sub-OUs: Legacy ABM supported one nesting level (depth=2); Apple Business documentation as of 2026-04-14 GA is ambiguous on whether deeper nesting is supported.
   - **Deferred verification:** Definitive max nesting depth requires portal verification deferred to Phase 63 (per CONTEXT.md `<deferred>` line 234). For v1.6 authoring purposes, treat OU hierarchy as flat-by-default with optional one-level sub-OUs.
   - Cross-link forward to Phase 63 `03-ous-vs-custom-roles.md` decision matrix.
6. H2 `## Locations → OUs Migration Framing`:
   - Conceptual continuity: legacy Locations and new OUs share the same delegation primitive role (device pool grouping + admin scoping).
   - Rebrand event itself: see [Apple Business Governance Glossary](../../_glossary-apple-business.md#rebrand-mapping-table) — the H3 Location→OU row in that table is the SOT for the rebrand event.
   - Existing tenant migration: Apple migrated Locations to OUs automatically on 2026-04-14; tenant admins do not need to perform manual migration. Pre-existing per-Location data (devices, tokens, MDM servers) transferred to the corresponding OU without admin intervention.
   - **Untouched-OU trap reference:** Phase 63 `05-vpp-catalog-consolidation` runbook (forthcoming) carries the OP-9 untouched-OU hard-bordered callout for cross-OU content-token migration scenarios; this concept doc does not duplicate that callout.

**Forward-link contract:** Links to Phase 63/64 docs (`03-ous-vs-custom-roles.md`, `15-mdm-server-reassign-runbook.md`, `17-audit-log-scoping-runbook.md`, `18-cross-org-boundary-cheat-sheet.md`) use relative paths from `docs/cross-platform/apple-business/`. At Plan 62-05 execution time, these target files do not yet exist. Plan 62-08's `check-phase-62.mjs` will NOT enforce link existence for these forward refs (they sunset in Phase 64/65 per the same exemption logic as `c16_missing_endpoint_exemptions`).
  </action>
  <verify>
    <automated>test -f docs/cross-platform/apple-business/02-ous-architecture.md && grep -cE "^# Organizational Units" docs/cross-platform/apple-business/02-ous-architecture.md && grep -cE "^## (Hierarchy Rules|Locations.*OUs)" docs/cross-platform/apple-business/02-ous-architecture.md</automated>
    <expected>file exists; H1 present; both H2 sections present</expected>
  </verify>
  <done>File exists with frontmatter + platform-applicability blockquote + H1 + intro + Hierarchy Rules H2 + Locations→OUs Migration Framing H2; nesting-depth deferral noted with forward-ref to Phase 63.</done>
</task>

<task type="auto">
  <name>Task 62-05-02: Append OU Scope Coverage table (5+1 rows) + Version History + commit</name>
  <files>docs/cross-platform/apple-business/02-ous-architecture.md</files>
  <action>
Append to the file:

```markdown
## OU Scope Coverage

The following resources are OU-scoped in Apple Business. Custom roles assigned to a sub-org
admin restrict access to one OU's instance of each resource; cross-OU access requires either
a top-level role (IT Administrator / People Administrator / Content Administrator) or
purpose-built cross-OU custom roles (Phase 63 `04-custom-role-authoring.md` deliverable).

| Resource | OU-scoped? | Notes |
|----------|-----------|-------|
| Devices | Yes | Per-OU device pool; reassignment via MDM server transfer (see `15-mdm-server-reassign-runbook.md` Phase 64) |
| Content tokens | Yes | Per-OU token; 1-year validity; OU-scoped allocation per Apple Business article `axme0f8659ec` |
| MDM servers | Yes | One MDM server per OU; assignment cascade through device transfers |
| Managed Apple Accounts | Yes | Per-OU account scope; federation per-OU; SCIM provisioning per-OU |
| Role assignments | Yes | Custom roles scoped to specific OU |
| Audit logs | Partial | Cross-OU visibility — definitive 3×3 visibility matrix deferred to Phase 64 `17-audit-log-scoping-runbook.md` |

## Cross-References

- Glossary: [Organizational Unit](../../_glossary-apple-business.md#organizational-unit), [OU Scope](../../_glossary-apple-business.md#ou-scope), [Sub-OU](../../_glossary-apple-business.md#sub-ou)
- Role/permission model: [01-role-permission-model.md](01-role-permission-model.md) — Edit OUs / View OUs Edit-without-View pair (OP-3)
- L1 admin-directory lookup convention: [_admin-directory.md](_admin-directory.md) — per-OU admin holder lookup pattern

## Version History

| Date | Change | Author |
|------|--------|--------|
| 2026-05-21 | Phase 62: OUs concept doc — hierarchy rules + 5-row scoping table + Locations→OUs migration framing (AB-04) | -- |
```

**D-A3 invariant check:** This doc cross-links to `ios-capability-matrix.md` and `macos-capability-matrix.md` ONLY as read-only references. Per D-A3 + STATE.md anti-regression invariants, `macos-capability-matrix.md` + `4-platform-capability-comparison.md` MUST remain byte-unchanged in v1.6. Plan 62-05 does NOT modify those files.

Commit message: `docs(62): plan 62-05 — OUs architecture concept doc (AB-04)`
  </action>
  <verify>
    <automated>grep -cE "^## OU Scope Coverage" docs/cross-platform/apple-business/02-ous-architecture.md && grep -cE "^\| (Devices|Content tokens|MDM servers|Managed Apple Accounts|Role assignments|Audit logs) \|" docs/cross-platform/apple-business/02-ous-architecture.md</automated>
    <expected>OU Scope Coverage H2 present (1); 5+1 = 6 scoping table rows present</expected>
  </verify>
  <done>OU Scope Coverage table present with all 5 D-A7 mandatory rows + Audit logs partial-scope row; Cross-References block links glossary + role/permission model + _admin-directory.md; Version History row added; file committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OU scoping concepts ↔ Phase 63/64 dependent docs | This concept doc is the SOT for OU-scoped resource semantics; Phase 64 `15-mdm-server-reassign-runbook.md` cites it for per-OU MDM server scope; integrity of the scoping table directly affects downstream runbook correctness |
| D-A3 invariant ↔ capability matrices | This doc cross-links to `ios-capability-matrix.md` + `macos-capability-matrix.md` as READ-ONLY references; D-A3 forbids modification of those files in v1.6 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| (none-blocking) | I (Information disclosure) | OUs concept doc | accept | All content is public Apple Business documentation; no PII; D-A8 read-only-corpus contract applies |

T-62-A — N/A.
T-62-B — N/A.
T-62-C — Apple article ID `axme0f8659ec` cited in Content tokens row; rot tracked in `c13_rotting_external` (Plan 62-08).
</threat_model>

<verification>
Per VALIDATION.md V-62-05:

| Task ID | V-62-XX | Command |
|---------|---------|---------|
| 62-05-01 | (structural) | `test -f docs/cross-platform/apple-business/02-ous-architecture.md` |
| 62-05-02 | V-62-05 | `grep -cE "^## OU Scope Coverage" docs/cross-platform/apple-business/02-ous-architecture.md && grep -cE "^\| (Devices\|Content tokens\|MDM servers\|Managed Apple Accounts\|Role assignments) \|" $_` -> H2 present + ≥5 mandatory rows |
</verification>

<success_criteria>
1. `docs/cross-platform/apple-business/02-ous-architecture.md` exists with valid frontmatter.
2. Hierarchy Rules H2 documents flat-by-default + optional one-level sub-OUs + Phase 63 portal-verification deferral.
3. Locations → OUs Migration Framing H2 describes conceptual continuity + automatic migration + glossary cross-link.
4. OU Scope Coverage table contains all 5 D-A7 mandatory resources (Devices / Content tokens / MDM servers / Managed Apple Accounts / Role assignments) + Audit logs partial-scope row.
5. Cross-references link to glossary + role/permission model + admin-directory (forward-ref to Phase 64 runbooks accepted).
6. D-A3 invariant honored: zero modifications to capability matrices.
</success_criteria>

<output>
After completion, create `.planning/phases/62-apple-business-foundation-rebrand/62-05-SUMMARY.md` recording the final scoping-table row list + any forward-references documented (for Plan 62-08 sidecar exemption tracking).
</output>
