---
phase: 62
plan: 62-06
title: Author `_admin-directory.md` template (<TENANT_FILL_IN> placeholders + 4 backend types + tenant-warning header)
type: execute
wave: 3
status: pending
depends_on: [62-01, 62-02, 62-03]
files_modified:
  - docs/cross-platform/apple-business/_admin-directory.md
requirements:
  - AB-07
threats: [T-62-A]
autonomous: true

must_haves:
  truths:
    - "L1 admin-directory lookup convention published as a dedicated tenant-fillable template file at `docs/cross-platform/apple-business/_admin-directory.md` per D-01 (NOT embedded inside `02-ous-architecture.md`)"
    - "Template provides a stable file-level C16 anchor target for Phase 65 L1 #34 cross-link (`05-sub-org-admin-onboarding.md#which-admin-owns-this-pool` resolves through this file's path)"
    - "Default upstream copy ships with `<TENANT_FILL_IN>` placeholders ONLY (no tenant-specific data — D-A8 read-only-corpus invariant + T-62-A PII-exposure mitigation)"
    - "4 backend types documented (AD/Entra group convention `apple-business-admins-{ou-slug}`; ServiceNow/Jira CMDB; Confluence/SharePoint admin page; 'no formal directory yet' email-fallback)"
    - "Tenant-warning header (HTML comment block at top of file + tenant-fillable blockquote in body) prevents accidental commit of tenant PII to the upstream corpus (T-62-A mitigation)"
  artifacts:
    - path: "docs/cross-platform/apple-business/_admin-directory.md"
      provides: "AB-07 L1 admin-directory lookup convention template; file-level C16 anchor target for Phase 65 L1 #34"
      contains: "<TENANT_FILL_IN>"
      min_lines: 80
  key_links:
    - from: "docs/cross-platform/apple-business/_admin-directory.md tenant-warning header"
      to: "<TENANT_FILL_IN> placeholders in body"
      via: "explicit instruction that upstream copy preserves placeholders verbatim"
      pattern: "<TENANT_FILL_IN>"
    - from: "Phase 65 L1 #34 future cross-link"
      to: "docs/cross-platform/apple-business/_admin-directory.md file-level path"
      via: "stable file path (C16 anchor target)"
      pattern: "_admin-directory\\.md"
---

<objective>
Author `_admin-directory.md` per AB-07 + D-01 — a dedicated template file at `docs/cross-platform/apple-business/_admin-directory.md` providing a stable file-level cross-link target for Phase 65 L1 #34 (`#which-admin-owns-this-pool` resolves through this file's path).

Per T-62-A (PII-exposure threat): the upstream copy MUST ship with `<TENANT_FILL_IN>` placeholders ONLY. Zero tenant-specific data appears in the upstream corpus. Each tenant clones the file into their own deployment and replaces placeholders locally; tenant PII never enters this repo.

Output: 1 new file at `docs/cross-platform/apple-business/_admin-directory.md` (~80-150 lines).
</objective>

<execution_context>
@D:/claude/Autopilot/CLAUDE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/phases/62-apple-business-foundation-rebrand/62-CONTEXT.md
@.planning/phases/62-apple-business-foundation-rebrand/62-RESEARCH.md
@.planning/phases/62-apple-business-foundation-rebrand/62-PATTERNS.md
@docs/_templates/admin-template-macos.md

<interfaces>
<!-- Analog: docs/_templates/admin-template-macos.md (PATTERNS.md structural-only match) -->
<!-- D-01 decision: dedicated file with <TENANT_FILL_IN> placeholders + 4 backend types -->
<!-- T-62-A PII threat: upstream copy MUST preserve placeholders -->

Frontmatter:
```yaml
---
last_verified: 2026-05-21
review_by: 2026-07-20
applies_to: apple-business
audience: l1
platform: ios+macos
---
```

Header HTML-comment block (T-62-A mitigation; tenant-warning):
```html
<!-- APPLE BUSINESS OU ADMIN DIRECTORY TEMPLATE
     Usage: Tenant copies this file and replaces <TENANT_FILL_IN> placeholders.
     The default upstream copy ships with placeholders ONLY.
     Per D-A8 read-only-corpus contract + T-62-A PII-exposure mitigation:
     - NEVER commit tenant-specific admin contact data to the upstream repo
     - Tenants maintain their populated copy in their own deployment fork or wiki
     - Each <TENANT_FILL_IN> below is a literal placeholder string preserved as-is upstream
-->
```

4 backend types (per D-01 + CONTEXT.md `<specifics>` _admin-directory.md skeleton lines 179-211):
1. AD/Entra group convention: `apple-business-admins-{ou-slug}` security group
2. ServiceNow / Jira CMDB: custom attribute `apple_business_ou_admin`
3. Confluence / SharePoint: tenant-maintained admin contact page (URL link below)
4. No formal directory: `apple-business-admins@<your-tenant>.tld` mailing list fallback
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 62-06-01: Create `_admin-directory.md` with HTML-comment tenant-warning header + frontmatter + tenant-fillable blockquote + Lookup Pattern section (4 backend types)</name>
  <files>docs/cross-platform/apple-business/_admin-directory.md</files>
  <action>
Create new file at `docs/cross-platform/apple-business/_admin-directory.md`. Structure:

1. HTML-comment tenant-warning header (verbatim from context interfaces) — appears BEFORE frontmatter to emphasize tenant-warning intent.
2. Frontmatter: `audience: l1`, `applies_to: apple-business`, `platform: ios+macos`, dates 2026-05-21 / 2026-07-20.
3. H1 `# Apple Business OU Admin Directory (Lookup Convention)`.
4. Tenant-fillable blockquote (T-62-A mitigation reinforced in body):
   ```markdown
   > **Tenant-fillable.** This is a CONVENTION + TEMPLATE. Each tenant populates with their own
   > admin holders and delegation contacts. The default upstream copy contains `<TENANT_FILL_IN>`
   > placeholders ONLY. NEVER commit tenant-specific admin data to the upstream repository per
   > D-A8 read-only-corpus contract and T-62-A PII-exposure mitigation.
   ```
5. H2 `## Lookup Pattern` — L1 staff looking up "which admin owns this device pool" SHOULD resolve via one of 4 backends in tenant preference order. List the 4 backends as a numbered list with description for each:
   1. **AD/Entra group convention** — security group named `apple-business-admins-{ou-slug}` whose members are the Account Holders / IT Administrators delegated to that OU. Resolution method: query AD/Entra for the group + enumerate members.
   2. **ServiceNow / Jira CMDB** — custom CMDB attribute `apple_business_ou_admin` on the OU configuration item record. Resolution method: CMDB lookup by OU slug.
   3. **Confluence / SharePoint** — tenant-maintained admin contact page; link below in Per-OU Admin Holder Lookup table.
   4. **No formal directory** — email `apple-business-admins@<your-tenant>.tld` mailing list as fallback. Resolution method: send-mail-to + wait for callback. Last-resort path; tenant adopts one of options 1-3 as primary.
6. Cross-reference forward to AB-02 Account Holder DO-NOT-DELEGATE callout (link to `01-role-permission-model.md`).
  </action>
  <verify>
    <automated>test -f docs/cross-platform/apple-business/_admin-directory.md && grep -cE "<TENANT_FILL_IN>" docs/cross-platform/apple-business/_admin-directory.md && grep -cE "(AD/Entra group convention|ServiceNow.*CMDB|Confluence.*SharePoint|No formal directory)" docs/cross-platform/apple-business/_admin-directory.md</automated>
    <expected>file exists; placeholders present (≥1); all 4 backend type labels present</expected>
  </verify>
  <done>File exists with HTML-comment tenant-warning header + frontmatter + H1 + tenant-fillable blockquote + Lookup Pattern H2 documenting all 4 backend types; first `<TENANT_FILL_IN>` placeholder usage example in context.</done>
</task>

<task type="auto">
  <name>Task 62-06-02: Append Per-OU Admin Holder Lookup table (with all-placeholder rows) + Tenant Population Instructions + commit</name>
  <files>docs/cross-platform/apple-business/_admin-directory.md</files>
  <action>
Append the per-OU lookup table (placeholders ONLY) + tenant instructions:

```markdown
## Per-OU Admin Holder Lookup

This table is **tenant-fillable**. The default upstream copy ships with one all-placeholder
row demonstrating column structure. Tenants append OU-specific rows in their fork or wiki copy.

| OU | Account Holder (Managed Apple Account) | Delegation Contact | Backend |
|---|---|---|---|
| <TENANT_FILL_IN> | <TENANT_FILL_IN> | <TENANT_FILL_IN> | <TENANT_FILL_IN> |

## Tenant Population Instructions

1. **Fork or clone** this file into your tenant's documentation deployment (do NOT commit
   populated copies back to the upstream repository — T-62-A PII-exposure mitigation).
2. **Choose one primary backend** from the Lookup Pattern section above (AD/Entra group
   convention recommended for most tenants).
3. **For each OU in your Apple Business tenant**, append one row to the Per-OU Admin Holder
   Lookup table with:
   - **OU**: the OU slug or display name
   - **Account Holder**: the Managed Apple Account email (e.g., `account-holder@your-tenant.tld`)
   - **Delegation Contact**: the sub-org admin or delegated IT contact for L1 escalation
   - **Backend**: which backend (1-4) was used to resolve this OU's admin lookup
4. **Periodic verification**: update `last_verified:` frontmatter when reviewing tenant data
   (60-day rule per v1.5 carry-over).

## Cross-References

- Account Holder DO-NOT-DELEGATE invariant: see [Role/Permission Model](01-role-permission-model.md#top-level-roles-4) — OP-2 / DA-2 callout
- OU scoping context: see [OUs Architecture](02-ous-architecture.md)
- L1 runbook integration (Phase 65): `docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` will cross-link to this file's `#per-ou-admin-holder-lookup` anchor — the file-level path is the C16 stable target (Phase 65 deliverable per ABNAV-01)

## Version History

| Date | Change | Author |
|------|--------|--------|
| 2026-05-21 | Phase 62: L1 admin-directory lookup convention template (AB-07) | -- |
```

**T-62-A integrity invariant:** After authoring, confirm `<TENANT_FILL_IN>` literal string appears at least 5 times in the file (header HTML comment + Per-OU table row x4 columns at minimum). If a code reviewer or executor inadvertently replaces any placeholder with tenant-specific data, the upstream copy fails integrity-check V-62-11-PII.

Commit message: `docs(62): plan 62-06 — _admin-directory.md template (AB-07; T-62-A mitigation)`
  </action>
  <verify>
    <automated>grep -c "<TENANT_FILL_IN>" docs/cross-platform/apple-business/_admin-directory.md</automated>
    <expected>≥5 (header comment + table row 4 cols + body references)</expected>
  </verify>
  <done>Per-OU Admin Holder Lookup table present with single all-`<TENANT_FILL_IN>` placeholder row; Tenant Population Instructions present; Cross-References to role/permission model + OUs architecture + Phase 65 L1 #34 forward-ref present; Version History present; ≥5 `<TENANT_FILL_IN>` occurrences (T-62-A integrity check).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream repo ↔ tenant data | T-62-A: tenant admin PII (email addresses, contact names, AD group memberships) MUST NOT enter the upstream repo. Mitigation: upstream copy ships with placeholders only; tenant population happens in tenant-controlled forks/wikis |
| Template file path ↔ Phase 65 cross-link | C16 4-edge triangle (Phase 65 L1 #34 → this file): file-level path stability is load-bearing; renaming or moving this file would break Phase 65 cross-link target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| **T-62-A** | I (Information disclosure / PII exposure) | `_admin-directory.md` upstream copy | **mitigate** | (a) HTML-comment header warns against committing tenant data; (b) tenant-fillable blockquote in body reinforces the warning; (c) all rows use `<TENANT_FILL_IN>` placeholder strings — upstream copy contains zero tenant-specific data; (d) Plan 62-08 `check-phase-62.mjs` V-62-11-PII assertion verifies `<TENANT_FILL_IN>` count ≥5 to detect accidental tenant-data commits |

T-62-B — N/A (no harness work).
T-62-C — N/A (no Apple URL dependencies in this file; the future Phase 64+ cross-references are internal repo paths only).
</threat_model>

<verification>
Per VALIDATION.md V-62-11 (structural) + V-62-11-PII (security):

| Task ID | V-62-XX | Command |
|---------|---------|---------|
| 62-06-01 | V-62-11 | `test -f docs/cross-platform/apple-business/_admin-directory.md && grep -cE "<TENANT_FILL_IN>" $_` -> file exists; placeholders present |
| 62-06-02 | V-62-11-PII | `grep -c "<TENANT_FILL_IN>" docs/cross-platform/apple-business/_admin-directory.md` -> ≥5 (T-62-A integrity gate) |
</verification>

<success_criteria>
1. `docs/cross-platform/apple-business/_admin-directory.md` exists at the correct path (file-level C16 anchor target stable).
2. HTML-comment tenant-warning header appears BEFORE frontmatter (T-62-A reinforcement).
3. Frontmatter: `audience: l1`, `applies_to: apple-business`, `platform: ios+macos`.
4. 4 backend types documented in Lookup Pattern H2 (AD/Entra group convention; ServiceNow/Jira CMDB; Confluence/SharePoint; no formal directory email fallback).
5. Per-OU Admin Holder Lookup table contains 4-column row with all `<TENANT_FILL_IN>` placeholders.
6. Tenant Population Instructions present with 4 numbered steps + 60-day verification rule.
7. `<TENANT_FILL_IN>` literal appears ≥5 times in the file (T-62-A integrity).
8. Cross-references include Phase 65 L1 #34 forward-ref noting C16 anchor target contract.
</success_criteria>

<output>
After completion, create `.planning/phases/62-apple-business-foundation-rebrand/62-06-SUMMARY.md` recording:
- Final `<TENANT_FILL_IN>` count (for V-62-11-PII baseline)
- Confirmation that zero tenant-specific strings (emails, names, AD group names) appear in the file
- File-level C16 anchor target path (verbatim) for Plan 62-08 `check-phase-62.mjs` to assert
</output>
