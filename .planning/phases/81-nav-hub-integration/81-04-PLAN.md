---
phase: 81-nav-hub-integration
plan: 04
type: execute
wave: 2
depends_on: ["81-01", "81-02", "81-03"]
files_modified:
  - .planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md
  - .planning/phases/81-nav-hub-integration/81-VERIFICATION.md
autonomous: true
requirements: [SSOREF-04]

must_haves:
  truths:
    - "81-CROSSLINK-CLOSURE.md enumerates all 8 edges E1-E8 in ROADMAP-line-531 order, each with a source file:line evidence and a resolved checkbox"
    - "After Plans 01-03, all 8 edges are confirmable PRESENT: E1/E5/E6/E7 verified-present with cited evidence; E2/E3/E4/E8 created this phase with cited evidence"
    - "All 8 checkboxes are checked (no deferred-gap rows remain)"
    - "81-VERIFICATION.md references 81-CROSSLINK-CLOSURE.md so the checklist is not author-invisible to the Phase-82 harness author"
    - "The checklist lives in the planning directory, NOT in the docs/ corpus (D-02)"
  artifacts:
    - path: ".planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md"
      provides: "8-edge SSO cross-link closure checklist with file:line evidence + checkboxes"
      contains: "E8"
      min_lines: 20
    - path: ".planning/phases/81-nav-hub-integration/81-VERIFICATION.md"
      provides: "Phase 81 verification note referencing the closure checklist"
      contains: "81-CROSSLINK-CLOSURE.md"
  key_links:
    - from: ".planning/phases/81-nav-hub-integration/81-VERIFICATION.md"
      to: ".planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md"
      via: "explicit reference so the Phase-82 harness author finds it"
      pattern: "81-CROSSLINK-CLOSURE"
---

<objective>
Create the SC4 cross-link closure checklist `81-CROSSLINK-CLOSURE.md` (D-02 — committed, planning-dir, NOT docs/ corpus) enumerating all 8 SSO-E edges E1-E8 verbatim from ROADMAP line 531, each with source `file:line` evidence and a resolved checkbox. After Plans 01-03 land, ALL 8 edges must be confirmable PRESENT. Also create/extend `81-VERIFICATION.md` referencing the checklist so it is not author-invisible to the Phase-82 harness author (D-02 guardrail).

Purpose: The frozen v1.8 harness does NOT crawl internal links — this checklist + the Routing Verification table (Plan 02) are the ONLY safety nets for Phase-81 edges. The checklist is the literal evidence that SC4 is satisfied.

Output: Two new planning-directory files. This plan runs LAST (Wave 2) so it can truthfully confirm all 8 edges after E2/E3/E4/E8 are created and E1/E5/E6/E7 are verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-nav-hub-integration/81-CONTEXT.md
@.planning/phases/81-nav-hub-integration/81-RESEARCH.md
@.planning/phases/81-nav-hub-integration/81-01-SUMMARY.md
@.planning/phases/81-nav-hub-integration/81-02-SUMMARY.md
@.planning/phases/81-nav-hub-integration/81-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 81-CROSSLINK-CLOSURE.md with all 8 edges, file:line evidence, and checkboxes (D-02, SC4)</name>
  <files>.planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md</files>
  <read_first>
    - .planning/ROADMAP.md line 531 (the verbatim SC4 edge enumeration E1-E8 — the authoritative order and definitions)
    - .planning/phases/81-nav-hub-integration/81-03-SUMMARY.md (the exact file:line where E2/E3/E4/E8 cross-links landed, plus the confirmed #authentication slug)
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md §"Edge Audit: E1-E8 Verified State" (the verified file:line evidence for the PRESENT edges E1/E5/E6/E7: E1 at 07-platform-sso-setup.md:15,142; E5 at 35-macos-sso-sign-in-failure.md:98; E6 at 27-macos-sso-investigation.md:191; E7 at 03-configuration-profiles.md:168)
    - .planning/phases/81-nav-hub-integration/81-CONTEXT.md D-02 GUARDRAIL (lines 39-44) and revised D-03 (lines 46-56 — all 8 PRESENT, no deferred-gap rows expected)
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md Pitfall 4 (file MUST NOT go in docs/ corpus)
  </read_first>
  <action>
    Create `.planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md` (planning directory — NOT docs/). Include a header noting it is the SC4 cross-link closure artifact for Phase 81 (SSOREF-04). Build a table enumerating ALL 8 edges in ROADMAP-line-531 order with columns: Edge | Definition | Status | Evidence (file:line) | Resolved?. For each edge cite a concrete `file:line`:
    - E1 `07->glossary` — PRESENT — cite from 81-RESEARCH.md Edge Audit (e.g. 07-platform-sso-setup.md:15 and :142).
    - E2 `glossary->07` — PRESENT (created Plan 03) — cite the exact line from 81-03-SUMMARY.md.
    - E3 `07->capability-matrix#authentication` — PRESENT (created Plan 03) — cite the exact line; note the confirmed `#authentication` slug resolves.
    - E4 `capability-matrix->07` — PRESENT (created Plan 03) — cite the exact line.
    - E5 `35->27 escalation` — PRESENT — cite 35-macos-sso-sign-in-failure.md:98.
    - E6 `27->35 back-link` — PRESENT — cite 27-macos-sso-investigation.md:191.
    - E7 `03-config-profiles->07` — PRESENT — cite 03-configuration-profiles.md:168.
    - E8 `00-ade-lifecycle->07` — PRESENT (created Plan 03) — cite the exact line from 81-03-SUMMARY.md.
    Mark every Resolved? checkbox as checked `[x]`. Add a short note that the v1.8 harness does not crawl internal links (harness-invisible) so this checklist + the 06-macos-triage Routing Verification table are the safety nets, and that Phase 82's C17 adversarial-review decides whether these become a blocking harness check. Use the actual file:line values from the Plan 03 SUMMARY — do NOT invent line numbers; if a cited line cannot be confirmed by reading the target file, re-read the target and correct it before marking `[x]`.
  </action>
  <acceptance_criteria>
    - File exists at `.planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md` and NOT anywhere under `docs/` (`ls docs/81-CROSSLINK-CLOSURE.md` fails / no new docs/ file created).
    - All 8 edge IDs E1-E8 appear exactly once each in the table (`grep -oE "\bE[1-8]\b" .planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md | sort -u | wc -l` returns 8).
    - Every edge row has a checked box: `grep -c "\[x\]" .planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md` >= 8, and `grep -c "\[ \]" ...` returns 0 (no unresolved edges).
    - Each edge row contains a `file:line`-style evidence citation (a path ending `.md` followed by `:` and a digit appears on each edge row).
    - The four created edges (E2/E3/E4/E8) cite file:line values that match those recorded in 81-03-SUMMARY.md.
    - The edge order in the table matches ROADMAP line 531 (E1, E2, E3, E4, E5, E6, E7, E8).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md && [ "$(grep -oE '\bE[1-8]\b' .planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md | sort -u | wc -l)" -eq 8 ] && [ "$(grep -c '\[ \]' .planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md)" -eq 0 ] && [ "$(grep -c '\[x\]' .planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md)" -ge 8 ] && ! test -f docs/81-CROSSLINK-CLOSURE.md && echo PASS</automated>
  </verify>
  <done>81-CROSSLINK-CLOSURE.md created in the planning dir with all 8 edges in ROADMAP order, each with a confirmed file:line and a checked box; zero unresolved edges; file is NOT in docs/.</done>
</task>

<task type="auto">
  <name>Task 2: Create/extend 81-VERIFICATION.md referencing the closure checklist (D-02 guardrail)</name>
  <files>.planning/phases/81-nav-hub-integration/81-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md (the just-created checklist — reference it by relative filename)
    - .planning/phases/81-nav-hub-integration/81-CONTEXT.md D-02 GUARDRAIL (line 44 — "reference the checklist from the phase VERIFICATION.md so it is not author-invisible to the Phase-82 harness author")
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md §"Open Questions" Q2 (recommendation to reference the checklist from VERIFICATION.md)
    - .planning/ROADMAP.md Phase 81 SC1-SC4 (the four success criteria to summarize verification status against)
  </read_first>
  <action>
    Create (or extend, if it already exists) `.planning/phases/81-nav-hub-integration/81-VERIFICATION.md` for Phase 81 (SSOREF-04). Include: a short status summary of SC1-SC4 (each marked satisfied with a one-line pointer to the plan/file that delivered it — SC1 index.md rows via 81-01, SC2 common-issues/quick-ref via 81-01, SC3 triage leaf via 81-02, SC4 cross-link closure via 81-03 + this checklist); and an EXPLICIT reference to `81-CROSSLINK-CLOSURE.md` stating it is the committed 8-edge SSO cross-link closure evidence that the Phase-82 harness author (C17 cross-link check decision) must consult. Note that all Phase-81 edits are append-only and harness-invisible, so this VERIFICATION note + the closure checklist + the 06-macos-triage Routing Verification table are the cross-corpus safety nets. Keep it concise (this is a verification pointer artifact, not a re-audit).
  </action>
  <acceptance_criteria>
    - File exists at `.planning/phases/81-nav-hub-integration/81-VERIFICATION.md`.
    - It references `81-CROSSLINK-CLOSURE.md` by name (`grep -c "81-CROSSLINK-CLOSURE.md" .planning/phases/81-nav-hub-integration/81-VERIFICATION.md` >= 1).
    - It names all four success criteria SC1-SC4 (or equivalently the four deliverables) with a pointer to where each is satisfied.
    - It mentions the Phase-82 C17 / harness-author audience so the checklist is not author-invisible (per D-02 guardrail).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/81-nav-hub-integration/81-VERIFICATION.md && grep -q "81-CROSSLINK-CLOSURE.md" .planning/phases/81-nav-hub-integration/81-VERIFICATION.md && echo PASS</automated>
  </verify>
  <done>81-VERIFICATION.md created referencing 81-CROSSLINK-CLOSURE.md, summarizing SC1-SC4 satisfaction and flagging the checklist for the Phase-82 harness/C17 author so it is not author-invisible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Planning-directory documentation artifacts only. No code, input, auth, or data flow. Not part of the published docs/ corpus or the harness `docs/**` surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-05 | Repudiation / integrity | Closure checklist asserting an edge PRESENT that is not actually present (false attestation) | mitigate | Each `[x]` must cite a `file:line` confirmed by reading the target; the four created edges must match 81-03-SUMMARY.md line numbers; acceptance criteria forbid any `[ ]` unresolved row, forcing real confirmation. |
| T-81-06 | Information Disclosure | Checklist leaking into published docs/ corpus | mitigate | D-02: file lives only in `.planning/`; acceptance criteria assert no `docs/81-CROSSLINK-CLOSURE.md` is created. |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | No package installs — markdown authoring, `git` only. |
</threat_model>

<verification>
- 81-CROSSLINK-CLOSURE.md: 8 edges in ROADMAP order, each PRESENT with confirmed file:line, all boxes checked, zero unresolved, NOT in docs/.
- 81-VERIFICATION.md references the checklist and summarizes SC1-SC4 for the Phase-82 author.
- Created edges' citations match 81-03-SUMMARY.md.
</verification>

<success_criteria>
- SC4 fully satisfied: committed closure checklist confirms all 8 SSO-E edges present across the corpus.
- D-02 honored (planning-dir, harness-author-visible).
</success_criteria>

<output>
Create `.planning/phases/81-nav-hub-integration/81-04-SUMMARY.md` when done. Confirm all 8 edges resolved and note that 81-VERIFICATION.md flags the checklist for Phase 82's C17 decision.
</output>
