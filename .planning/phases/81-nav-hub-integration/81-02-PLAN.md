---
phase: 81-nav-hub-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/decision-trees/06-macos-triage.md
autonomous: true
requirements: [SSOREF-04]

must_haves:
  truths:
    - "An L1 tech in the macOS triage tree can branch off MAC3 to a Platform SSO sub-decision that disambiguates #35 vs #36"
    - "The sub-decision routes to two resolved leaves: L1 #35 (registration notification never appeared) and L1 #36 (Secure Enclave key error)"
    - "The longest path MAC1->MAC3->MACSSO->leaf is exactly 3 edges (3-edge routing invariant preserved, D-01)"
    - "Both new leaves carry classDef resolved styling and click directives"
    - "The Routing Verification table gains matching rows so the 'within 3 edges' completeness claim stays honest"
    - "The #35 leaf preserves onward escalation to L2 #27 (already carried by runbook #35 itself)"
  artifacts:
    - path: "docs/decision-trees/06-macos-triage.md"
      provides: "SSO sub-decision diamond MACSSO + leaves MACR7/MACR8 + 2 Routing Verification rows"
      contains: "MACSSO"
  key_links:
    - from: "docs/decision-trees/06-macos-triage.md"
      to: "../l1-runbooks/35-macos-sso-sign-in-failure.md"
      via: "click MACR7 directive"
      pattern: "click MACR7"
    - from: "docs/decision-trees/06-macos-triage.md"
      to: "../l1-runbooks/36-macos-secure-enclave-key.md"
      via: "click MACR8 directive"
      pattern: "click MACR8"
---

<objective>
Extend the macOS triage decision tree (`docs/decision-trees/06-macos-triage.md`) with a Platform SSO symptom branch off `MAC3` leading to ONE sub-decision diamond (`MACSSO`) that disambiguates and routes to two resolved leaves — L1 #35 ("Registration Required" notification never appeared) and L1 #36 (Secure Enclave key error) — per D-01 / SC3. This is the disambiguation surface Phase 80 D-04 deferred to this leaf.

Purpose: An L1 tech triaging a macOS SSO failure gets a guiding question rather than a bare symptom label, and lands on the correct runbook (#35 vs #36) within the 3-edge routing budget.

Output: One edited Mermaid block + two new Routing Verification rows + new `click` and `classDef resolved` directives, all append-only inside the existing diagram structure. No existing node/edge/row modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-nav-hub-integration/81-CONTEXT.md
@.planning/phases/81-nav-hub-integration/81-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SSO sub-decision diamond + two resolved leaves to the Mermaid block (D-01, SC3)</name>
  <files>docs/decision-trees/06-macos-triage.md</files>
  <read_first>
    - docs/decision-trees/06-macos-triage.md (whole file — Mermaid block lines 29-55; existing MAC3 fan-out edges; `click MACR1..MACR6` directives lines 44-50; `classDef resolved` / `classDef escalateL2` / `class` lines 51-54; frontmatter lines 1-7)
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md §"5. 06-macos-triage.md" (verbatim nodes/edges to add: MACSSO diamond, MACR7 leaf, MACR8 leaf; the new click directives; the `class MACR7,MACR8 resolved` line; the 3-edge invariant verification)
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md §"Common Pitfalls" Pitfall 1 (no second nested diamond — MACSSO is the ONLY diamond between MAC3 and the leaves)
  </read_first>
  <action>
    Inside the existing Mermaid fenced block (before its closing ```), append, after the existing `MAC3 -->|"Other / unclear"| MACE1` edge line and before the existing `click MACR1` block:
    (a) A new edge `MAC3 -->|"Platform SSO<br/>sign-in issue"| MACSSO{...}` where `MACSSO` is a single sub-decision diamond whose question text (Claude's discretion within D-01) distinguishes "the 'Registration Required' notification never appeared" from "a Secure Enclave key error occurred / key lost after reset".
    (b) Two leaf edges off MACSSO: one (`MACR7`, rounded-leaf shape) -> "Platform SSO Sign-In Failure Runbook"; one (`MACR8`) -> "Secure Enclave Key Loss Runbook".
    Use node IDs `MACSSO`, `MACR7`, `MACR8` (next available after MACR6) and the verbatim node/edge text in 81-RESEARCH.md §5.
    (c) After the existing six `click MACR1..MACR6` directives, add `click MACR7 "../l1-runbooks/35-macos-sso-sign-in-failure.md"` and `click MACR8 "../l1-runbooks/36-macos-secure-enclave-key.md"`.
    (d) Apply `classDef resolved` to both new leaves: either add `MACR7,MACR8` to the existing `class MACR1,...,MACR6 resolved` line OR add a new `class MACR7,MACR8 resolved` line. Do NOT add MACR7/MACR8 to the `class MACE1 escalateL2` line (they are resolved leaves, not escalations).
    GUARDRAIL: the path `MAC1->MAC3->MACSSO->MACR7|MACR8` must be EXACTLY 3 edges — do NOT nest any further diamond under MACSSO. The #35 leaf's onward L2 #27 escalation is already carried by runbook #35 itself (no triage-tree action needed for guardrail (e)).
    Also update the frontmatter `last_verified` to `2026-06-22` and `review_by` to `2026-09-20` (90 days out), per house-style convention.
  </action>
  <acceptance_criteria>
    - `grep -c "MACSSO" docs/decision-trees/06-macos-triage.md` >= 2 (the diamond definition + at least one edge referencing it).
    - `grep -q 'click MACR7 "../l1-runbooks/35-macos-sso-sign-in-failure.md"' docs/decision-trees/06-macos-triage.md` is true AND `grep -q 'click MACR8 "../l1-runbooks/36-macos-secure-enclave-key.md"' docs/decision-trees/06-macos-triage.md` is true.
    - A `class` line applying `resolved` includes both `MACR7` and `MACR8` (`grep -E "class .*MACR7.*MACR8.* resolved|class MACR7,MACR8 resolved" docs/decision-trees/06-macos-triage.md` matches).
    - NO line adds MACR7 or MACR8 to `escalateL2` (`grep -E "MACR7|MACR8" docs/decision-trees/06-macos-triage.md | grep -c "escalateL2"` returns 0).
    - 3-edge invariant: there is exactly ONE diamond (`{`-shaped node) introduced by this edit (MACSSO); no second sub-diamond hangs off MACSSO. Manually confirm MACSSO's only outgoing edges go to MACR7 and MACR8 (both leaves), not to another `{...}` node.
    - The six existing `click MACR1..MACR6` directives are byte-unchanged; existing MAC1/MAC2/MAC3 nodes and MACR1-MACR6/MACE1 unchanged. `git diff` shows additions only (plus the two frontmatter date-field edits).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'click MACR7 "../l1-runbooks/35-macos-sso-sign-in-failure.md"' docs/decision-trees/06-macos-triage.md && grep -q 'click MACR8 "../l1-runbooks/36-macos-secure-enclave-key.md"' docs/decision-trees/06-macos-triage.md && [ "$(grep -E 'MACR7|MACR8' docs/decision-trees/06-macos-triage.md | grep -c escalateL2)" -eq 0 ] && echo PASS</automated>
  </verify>
  <done>MACSSO sub-decision diamond + MACR7/MACR8 resolved leaves added with click directives and classDef resolved; exactly one diamond between MAC3 and the leaves (3-edge path); existing nodes/edges/clicks unchanged; frontmatter dates refreshed.</done>
</task>

<task type="auto">
  <name>Task 2: Append two matching rows to the Routing Verification table (D-01 guardrail c, SC3)</name>
  <files>docs/decision-trees/06-macos-triage.md</files>
  <read_first>
    - docs/decision-trees/06-macos-triage.md (Routing Verification table lines 57-69; the line-59 "within 3 edges of the root" claim; the last existing row "Other / unclear ... L2 escalation"; Version History table at file bottom)
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md §"5." "What to ADD to the Routing Verification table" (the two verbatim rows; the 4-column format `| Path | Step 1 | Step 2 | Destination |`)
  </read_first>
  <action>
    Append exactly TWO rows to the end of the Routing Verification table (after the existing "Other / unclear -> L2 escalation" row), keeping the 4-column `| Path | Step 1 | Step 2 | Destination |` format so the line-59 "within 3 edges" completeness claim stays honest after the Mermaid edit. Row 1: Platform SSO — registration not appearing -> Runbook 35. Row 2: Platform SSO — Secure Enclave key error -> Runbook 36. Use the verbatim rows from 81-RESEARCH.md §5. Do NOT modify the line-59 claim text or any existing row. Then append ONE Version History row dated 2026-06-22: `| 2026-06-22 | Phase 81 (SSOREF-04): added Platform SSO sub-decision leaf (MACSSO -> #35/#36) + 2 Routing Verification rows | -- |`.
  </action>
  <acceptance_criteria>
    - The Routing Verification table contains two new rows mentioning "Platform SSO" routing to "Runbook 35" and "Runbook 36" respectively (`grep -c "Platform SSO" docs/decision-trees/06-macos-triage.md` increased; both `Runbook 35` and `Runbook 36` appear in the Routing Verification table region lines >57).
    - Both new rows use the 4-column pipe format (4 `|`-delimited cells) consistent with existing rows.
    - The line-59 "within 3 edges of the root" sentence is byte-unchanged.
    - The existing 7 Routing Verification rows (Device not appearing ... Other / unclear) are byte-unchanged.
    - A Version History row dated 2026-06-22 referencing Phase 81 / SSOREF-04 is present.
    - `git diff` shows additions only.
  </acceptance_criteria>
  <verify>
    <automated>grep -A20 "## Routing Verification" docs/decision-trees/06-macos-triage.md | grep -q "Runbook 35" && grep -A20 "## Routing Verification" docs/decision-trees/06-macos-triage.md | grep -q "Runbook 36" && grep -q "Phase 81 (SSOREF-04)" docs/decision-trees/06-macos-triage.md && echo PASS</automated>
  </verify>
  <done>Two Platform SSO rows appended to the Routing Verification table (->#35, ->#36) keeping the completeness claim honest; line-59 invariant text and existing rows unchanged; Version History row added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change surface — a static Mermaid diagram and a markdown table. No code, input, auth, or data flow added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-02 | Tampering (integrity) | Mermaid click targets / Routing-table claim drift | mitigate | Exact-string acceptance criteria assert `click` targets verbatim; the new Routing Verification rows are added in the SAME plan so the "within 3 edges" completeness claim cannot go stale; 3-edge invariant asserted by single-diamond check. |
| T-81-03 | Denial of Service | Malformed Mermaid breaking diagram render | mitigate | New nodes follow the verified existing MACR{N} pattern; `class`/`click` directives mirror the existing six; no new fence/syntax introduced. |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | No package installs — markdown edit, `git` only. |
</threat_model>

<verification>
- Exactly one new sub-decision diamond (MACSSO) between MAC3 and the two leaves; path MAC1->MAC3->MACSSO->leaf = 3 edges.
- MACR7 -> #35, MACR8 -> #36 via click directives; both styled `resolved`, neither `escalateL2`.
- Routing Verification table gains two matching rows; line-59 claim text unchanged.
- `git diff` additions-only (plus frontmatter date refresh).
</verification>

<success_criteria>
- SC3 satisfied: macOS triage tree SSO leaf routes to L1 #35 and L1 #36 as an append-only extension within the 3-edge budget.
- D-01 guardrails (a)-(e) all honored.
</success_criteria>

<output>
Create `.planning/phases/81-nav-hub-integration/81-02-SUMMARY.md` when done. Record the new node IDs, the two click targets, and the two Routing Verification rows so Plan 04's closure checklist can confirm the SSO leaf is wired.
</output>
