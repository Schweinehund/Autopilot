---
phase: 81-nav-hub-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/macos-lifecycle/00-ade-lifecycle.md
  - docs/_glossary-macos.md
  - docs/admin-setup-macos/07-platform-sso-setup.md
  - docs/reference/macos-capability-matrix.md
autonomous: true
requirements: [SSOREF-04]

must_haves:
  truths:
    - "E8 exists: 00-ade-lifecycle.md Related Guides list links to guide 07"
    - "E2 exists: the Platform SSO glossary term in _glossary-macos.md links to guide 07"
    - "E3 exists: guide 07 links to macos-capability-matrix.md#authentication (anchor resolves)"
    - "E4 exists: macos-capability-matrix.md authentication section links back to guide 07"
    - "Every edit is a single additive cross-link — no prior-phase prose is rewritten; existing definitions/rows/bullets are byte-unchanged"
  artifacts:
    - path: "docs/macos-lifecycle/00-ade-lifecycle.md"
      provides: "E8 Related Guides bullet -> guide 07"
      contains: "07-platform-sso-setup.md"
    - path: "docs/_glossary-macos.md"
      provides: "E2 cross-link from Platform SSO term -> guide 07"
      contains: "07-platform-sso-setup.md"
    - path: "docs/admin-setup-macos/07-platform-sso-setup.md"
      provides: "E3 cross-link -> capability-matrix#authentication"
      contains: "macos-capability-matrix.md#authentication"
    - path: "docs/reference/macos-capability-matrix.md"
      provides: "E4 cross-link -> guide 07"
      contains: "07-platform-sso-setup.md"
  key_links:
    - from: "docs/admin-setup-macos/07-platform-sso-setup.md"
      to: "../reference/macos-capability-matrix.md#authentication"
      via: "appended See Also cross-link"
      pattern: "macos-capability-matrix\\.md#authentication"
    - from: "docs/reference/macos-capability-matrix.md"
      to: "../admin-setup-macos/07-platform-sso-setup.md"
      via: "appended cross-link in authentication section"
      pattern: "07-platform-sso-setup"
---

<objective>
Create the FOUR absent SSO cross-link edges identified by research as one-line additive cross-links, per the REVISED D-03 (2026-06-22, "Create all 4 now"): E8 (`00-ade-lifecycle->07`), E2 (`glossary->07`), E3 (`07->capability-matrix#authentication`), and E4 (`capability-matrix->07`). After this plan, all 8 SSO-E edges are genuinely PRESENT so SC4 is literally satisfied at Phase 81 close.

Purpose: Close the cross-link gaps research found (4 of 8 edges absent) without deferring them to Phase 82. Every missing edge is a single additive cross-link, NOT a content change — so the original D-03 risk (ballooning into prior-phase content rewrites) does not materialize.

Output: Four edited markdown files, each gaining ONE additive cross-link (+ a Version History row where the file has that table). No existing definition/row/bullet rewritten (append-only / cross-link-only).

IMPORTANT — supersedes RESEARCH.md old guardrail: The `<user_constraints>` and Edge-Audit sections of 81-RESEARCH.md were written against the ORIGINAL D-03 ("log E2/E3/E4, do not re-author"). That guardrail is SUPERSEDED by the revised D-03 in 81-CONTEXT.md (lines 46-56) and the phase brief. CREATE E2/E3/E4/E8 here as one-line cross-links. The revised-D-03 STOP rule still applies: if any edge cannot be wired as a clean single additive cross-link without rewriting prose, STOP and surface rather than silently rewriting prior content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-nav-hub-integration/81-CONTEXT.md
@.planning/phases/81-nav-hub-integration/81-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E8 + E2 cross-links (00-ade-lifecycle -> 07; glossary Platform SSO term -> 07)</name>
  <files>docs/macos-lifecycle/00-ade-lifecycle.md, docs/_glossary-macos.md</files>
  <read_first>
    - docs/macos-lifecycle/00-ade-lifecycle.md (lines 378-396 — `## See Also` -> `**Related Guides:**` list at ~391-394; last bullet `[Documentation Hub]` at line 394; `---` boundary at ~395-396; existing bullet style uses ` -- ` double-dash separator)
    - docs/_glossary-macos.md (lines 121-141 — `## Authentication` -> `### Platform SSO` term body at line 125; its `> See also:` blockquote at line 128 currently listing `[Enterprise SSO Plug-in](#enterprise-sso-plug-in); [Entra ID SSO](_glossary.md#entra-id-sso)`; Version History at ~144-153)
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md §"6. 00-ade-lifecycle.md" (verbatim E8 bullet + relative path `../admin-setup-macos/07-platform-sso-setup.md`) and §"Edge Audit E2" (confirms the glossary term has no guide-07 link)
    - .planning/phases/81-nav-hub-integration/81-CONTEXT.md D-03 GUARDRAIL E8 and E2 (lines 50-51)
  </read_first>
  <action>
    E8 — In `docs/macos-lifecycle/00-ade-lifecycle.md`, append ONE bullet to the `**Related Guides:**` list, AFTER the `[Documentation Hub]` bullet (line 394) and BEFORE the `---` at line 395/396: `- [Platform SSO Setup](../admin-setup-macos/07-platform-sso-setup.md) -- Configure macOS Platform SSO authentication via the Settings Catalog \`com.apple.extensiblesso\` payload` (the verbatim D-03 template from 81-RESEARCH.md §6, using the ` -- ` separator). Do NOT modify the two existing bullets.

    E2 — In `docs/_glossary-macos.md`, add an ADDITIVE cross-link from the `### Platform SSO` term to guide 07. Do NOT rewrite the term definition (line 125). Append the link to the term's existing `> See also:` blockquote at line 128 (e.g. add `; [Platform SSO Setup Guide](admin-setup-macos/07-platform-sso-setup.md)` to that existing see-also line), OR add a new `> See:` line immediately under it. The relative path from `docs/_glossary-macos.md` (which lives in `docs/`) to guide 07 is `admin-setup-macos/07-platform-sso-setup.md` (no `../` — same-level subdirectory). Use the exact link text/path from the existing house style.

    Append one Version History row to EACH file dated 2026-06-22: for 00-ade-lifecycle.md `| 2026-06-22 | Phase 81 (SSOREF-04): added E8 Related Guides cross-link to guide 07 | -- |`; for _glossary-macos.md `| 2026-06-22 | Phase 81 (SSOREF-04): added E2 cross-link from Platform SSO term to guide 07 | -- |`.
  </action>
  <acceptance_criteria>
    - E8: `grep -c "07-platform-sso-setup.md" docs/macos-lifecycle/00-ade-lifecycle.md` >= 1, and the match is a bullet inside the `**Related Guides:**` list (appears after `[Documentation Hub]` and before the closing `---`). The bullet uses the ` -- ` separator and path `../admin-setup-macos/07-platform-sso-setup.md`.
    - E2: `grep -c "admin-setup-macos/07-platform-sso-setup.md" docs/_glossary-macos.md` >= 1, located within or immediately adjacent to the `### Platform SSO` term entry (lines ~123-128 region).
    - The `### Platform SSO` definition body (line 125) is byte-unchanged; the existing `> See also:` targets `#enterprise-sso-plug-in` and `_glossary.md#entra-id-sso` are still present (link ADDED, not replaced).
    - The two existing `**Related Guides:**` bullets (`[Autopilot Lifecycle Overview]`, `[Documentation Hub]`) are byte-unchanged.
    - `git diff` on both files shows additions only.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "07-platform-sso-setup.md" docs/macos-lifecycle/00-ade-lifecycle.md && grep -q "admin-setup-macos/07-platform-sso-setup.md" docs/_glossary-macos.md && grep -q "enterprise-sso-plug-in" docs/_glossary-macos.md && echo PASS</automated>
  </verify>
  <done>E8 bullet appended to 00-ade-lifecycle Related Guides (-> guide 07); E2 cross-link added to the Platform SSO glossary term (-> guide 07) without rewriting the definition; existing bullets/see-alsos preserved; Version History rows added.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm #authentication anchor, then create E3 + E4 reciprocal cross-links (07 <-> capability-matrix)</name>
  <files>docs/admin-setup-macos/07-platform-sso-setup.md, docs/reference/macos-capability-matrix.md</files>
  <read_first>
    - docs/reference/macos-capability-matrix.md (line 100 `## Authentication` — the anchor source for `#authentication`; the auth rows 104-112; the `## See Also` list 114-119; Version History at ~123-126) — READ to CONFIRM the heading text is exactly `## Authentication` so the GitHub-flavored slug is `#authentication`.
    - docs/admin-setup-macos/07-platform-sso-setup.md (the `## See Also` block at lines 140-147 — currently bullets to glossary terms, `03-configuration-profiles.md`, `../macos-lifecycle/00-ade-lifecycle.md`; NO capability-matrix link present; Version History at file bottom)
    - .planning/phases/81-nav-hub-integration/81-RESEARCH.md §"Edge Audit E3/E4" (confirms both absent) and §"4 / Edge Audit" anchor-slug convention note
    - .planning/phases/81-nav-hub-integration/81-CONTEXT.md D-03 GUARDRAIL E3 and E4 (lines 52-54) — "Confirm the exact anchor slug of the capability matrix #authentication section before wiring E3/E4 (the anchor must resolve)."
  </read_first>
  <action>
    PRECONDITION (mandatory first step): Read `docs/reference/macos-capability-matrix.md` and confirm the authentication heading is exactly `## Authentication` (GitHub-flavored slug => `#authentication`). If the heading text differs, use the actual auto-generated slug for E3's anchor instead of assuming `#authentication`, and note the deviation in the SUMMARY. Do not wire a non-resolving anchor.

    E3 — In `docs/admin-setup-macos/07-platform-sso-setup.md`, append ONE bullet to the existing `## See Also` list (after the last existing See Also bullet): `- [macOS Capability Matrix — Authentication](../reference/macos-capability-matrix.md#authentication) -- macOS vs Windows auth-method capability comparison and hardware/version gates`. Relative path from `docs/admin-setup-macos/` to `docs/reference/macos-capability-matrix.md` = `../reference/macos-capability-matrix.md`. Do NOT rewrite any existing See Also bullet or guide content.

    E4 — In `docs/reference/macos-capability-matrix.md`, append ONE bullet to the `## See Also` list (lines 114-119) linking back to guide 07: `- [Platform SSO Setup Guide](../admin-setup-macos/07-platform-sso-setup.md) -- step-by-step Platform SSO configuration (Settings Catalog payload, registration flow)`. (The `## See Also` list is the cleanest additive site within the authentication-section area; do NOT alter the auth-section table rows 104-112.) Relative path from `docs/reference/` to guide 07 = `../admin-setup-macos/07-platform-sso-setup.md`.

    Append one Version History row to EACH file dated 2026-06-22: for guide 07 `| 2026-06-22 | Phase 81 (SSOREF-04): added E3 See Also cross-link to macos-capability-matrix.md#authentication | -- |`; for the matrix `| 2026-06-22 | Phase 81 (SSOREF-04): added E4 See Also cross-link to 07-platform-sso-setup.md | -- |`.
  </action>
  <acceptance_criteria>
    - Anchor confirmed: `grep -c "^## Authentication" docs/reference/macos-capability-matrix.md` returns 1 (so `#authentication` resolves). If 0, the executor must have substituted the correct slug and documented it.
    - E3: `grep -c "macos-capability-matrix.md#authentication" docs/admin-setup-macos/07-platform-sso-setup.md` >= 1, located in the `## See Also` block.
    - E4: `grep -c "07-platform-sso-setup.md" docs/reference/macos-capability-matrix.md` >= 1, located in the `## See Also` block (lines >113).
    - The capability-matrix auth-section table rows (104-112, including the Hybrid Entra join anti-feature row) are byte-unchanged; guide 07's existing See Also bullets are byte-unchanged.
    - `git diff` on both files shows additions only.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^## Authentication" docs/reference/macos-capability-matrix.md && grep -q "macos-capability-matrix.md#authentication" docs/admin-setup-macos/07-platform-sso-setup.md && grep -q "07-platform-sso-setup.md" docs/reference/macos-capability-matrix.md && echo PASS</automated>
  </verify>
  <done>`#authentication` anchor confirmed resolvable; E3 added to guide 07 See Also (-> matrix#authentication); E4 added to matrix See Also (-> guide 07); auth-section table rows and existing See Also bullets unchanged; Version History rows added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change surface. Four single-line additive cross-links in static markdown. No code, input, auth, or data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-02 | Tampering (integrity) | Non-resolving internal anchor (`#authentication`) or wrong relative path | mitigate | Task 2 mandates confirming the `## Authentication` heading slug before wiring E3/E4; acceptance criteria assert exact anchor + relative paths; frozen v1.8 harness does not crawl links so Plan 04 checklist is the cross-corpus safety net. |
| T-81-04 | Tampering | Accidental rewrite of prior-phase prose (glossary def / matrix rows) | mitigate | Each edit is a single additive cross-link; acceptance criteria assert existing definitions/rows are byte-unchanged; revised-D-03 STOP rule: surface rather than rewrite if a clean cross-link is not possible. |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | No package installs — markdown edits, `git` only. |
</threat_model>

<verification>
- All four edges (E2, E3, E4, E8) present as single additive cross-links with correct relative paths.
- `#authentication` heading confirmed so E3 anchor resolves.
- No prior-phase definition/row/bullet rewritten; `git diff` additions-only across all four files.
</verification>

<success_criteria>
- SC4 edge-creation portion satisfied: E2/E3/E4/E8 created; combined with verified-present E1/E5/E6/E7, all 8 SSO-E edges are PRESENT (Plan 04 confirms in the closure checklist).
- Revised D-03 honored (create-as-cross-link, no prose rewrites).
</success_criteria>

<output>
Create `.planning/phases/81-nav-hub-integration/81-03-SUMMARY.md` when done. Record, per edge (E2/E3/E4/E8), the exact `file:line` where the new cross-link landed and the confirmed `#authentication` slug — Plan 04's closure checklist will cite these directly.
</output>
