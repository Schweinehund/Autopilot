---
phase: 145-corpus-correction-validator-gate-archival-drift-fix
plan: 03
type: execute
wave: 3
depends_on: ["145-02"]
files_modified:
  - docs/operations/patch-management/01-windows-wufb-rings.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-04, FIX-07, FIX-10]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The Windows guide names the two current Autopatch default rings and no falsified cohort names"
    - "The PITFALL-9 blockquote states the containment position, still carries the PITFALL-9 label, and no longer asserts exclusivity"
    - "The Hotpatch section states default-on, both configuration levels, Arm64 support with CHPE disabled and the VBS requirement, with the corpus's superseded framing attributed rather than deleted"
    - "The Windows 11 Pro exclusion is retained and flagged as unconfirmed in prose"
    - "Every ring token in the file still carries a qualifier: check-phase-54.mjs stays at 32 passed / 0 failed"
    - "review_by minus last_verified is at most 60 days, computed by arithmetic from the commit date"
  artifacts:
    - "docs/operations/patch-management/01-windows-wufb-rings.md — one commit carrying FIX-01, FIX-02, FIX-04, FIX-07 and FIX-10"
  key_links:
    - "V-54-11's ~40-character preceding-qualifier window -> every ring token in the rewritten prose, and every markdown link TARGET (which V-54-11 does not strip)"
    - "V-54-12's whole-file token set -> the preservation list that must survive the FIX-07 attribution rewrite"
    - "the (Disambiguation) parenthetical in the Autopatch H2 -> the C11 keeper for that heading's own plural hit"
---

<objective>
Land every correction `01-windows-wufb-rings.md` owns — FIX-01, FIX-02, FIX-04, FIX-07 and FIX-10 —
as one commit, against the hardest assertion in the repository.

Purpose: D-21 is an owner ruling that Phase 145 **does** edit this file. ROADMAP Phase 146's claim
to be "the only phase in v1.21 that edits `01-windows-wufb-rings.md`" is false as written and is
restated as "the only phase performing **structural surgery** (the driver/firmware stub-and-move) on
this file". This plan performs no structural surgery: it corrects prose in place, leaves the H1, the
anchor-owning H2s, every anchor and the filename untouched, and leaves the entire driver/firmware
section for Phase 146.

This plan carries **one task** deliberately: the commit atom is indivisible, and this file carries
the two most hostile live assertions in the corpus. It gets a plan to itself so a remediation round
here cannot degrade the context of any other correction.

Output: one commit touching exactly one file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-CONTEXT.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-RESEARCH.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-PATTERNS.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-02-SUMMARY.md
</context>

<artifacts>
## Artifacts this phase produces

No new symbols and no new files. This plan modifies one existing document. The phase-level artifacts
(the `V-59-14` assertion renamed with `[v1.5-frozen @ ba2cbc0]`, and `COVERAGE.md`) are listed in
`145-01-PLAN.md`. No SPEC.md exists for this phase, so there is no `## Edge Coverage` /
`## Prohibitions` section to lift.
</artifacts>

<tasks>

<!-- planner-discipline-allow: First -->
<!-- planner-discipline-allow: Fast -->
<!-- planner-discipline-allow: Broad -->
<!-- planner-discipline-allow: Autopatch rings -->
<!-- planner-discipline-allow: mutually exclusive -->
<!-- planner-discipline-allow: mutual exclusion -->
<!-- planner-discipline-allow: mutual-exclusion -->

<task type="auto">
  <name>Task 1: The 01-windows-wufb-rings.md correction atom — FIX-01 + FIX-02 + FIX-04 + FIX-07 + FIX-10, one commit</name>
  <precondition>`node scripts/validation/check-phase-54.mjs` exits 0 with 32 passed BEFORE any edit. ROADMAP Phase 146's blast radius makes this validator a hard gate on every commit touching this file (D-18, D-23); a pre-existing red would be misattributed to this task.</precondition>
  <reversibility rating="one-way">Recorded for the record only — D-21 is an owner ruling already made, and the alternative it rejected (deferring FIX-07 to Phase 146) would land the milestone's own corrections after two phases of new content had already been built on top of the uncorrected text. Do not re-open it and do not gate on it.</reversibility>
  <files>docs/operations/patch-management/01-windows-wufb-rings.md</files>
  <read_first>
    - `docs/operations/patch-management/01-windows-wufb-rings.md` — the whole file (214 lines).
    - `scripts/validation/check-phase-54.mjs` — **mandatory**. Read `V-54-11` in full, including the exact strip list and every qualifier-acceptance branch, `V-54-12` (the whole-file Hotpatch token set), `V-54-13` (the driver/firmware H2 plus the dual-scan literal — this plan must not disturb either), `V-54-07` and `V-54-30`. Locate them with `grep -n "id: 7,\|id: 11,\|id: 12,\|id: 13,\|id: 30," scripts/validation/check-phase-54.mjs`.
    - `scripts/validation/v1.20-milestone-audit.mjs` — the `C11: Ops-domain anti-pattern regex` assertion (pattern list, ±200-character window, `windowKeywords` alternation).
    - `docs/admin-setup-macos/10-kerberos-sso-extension.md` — the `**Source:**` citation shape to copy (D-01).
    - `145-CONTEXT.md` decisions **D-01** and **D-02** and **D-03** (evidence lines and the URL screening rule), **D-04** (what "dated after the event" is read as), **D-06** (dates), **D-08** and **D-09** and **D-10** (correction shape, per-file token obligations, the preservation list), **D-11** (no separate dated correction marker), **D-12** (the Pro-exclusion prose flag), **D-13** (singularise), **D-17** (this is atom class 2), **D-21** and **D-22** and **D-23** (the ownership ruling, the Hotpatch section span, the two ROADMAP directives), **D-39** and **D-40** and **D-41** and **D-42** (rename scope, the disproved justification, headings and filename untouched), **D-54** (the 36-character C11 cliff), **D-55** (the PITFALL-9 literal must survive).
    - `145-RESEARCH.md` — the claim-to-source rows for FIX-01, FIX-02, FIX-04 and FIX-07, including the verbatim first-party hotpatch quotations confirmed by direct fetch, and the per-file WUfB census.
  </read_first>
  <action>
**Re-measure before editing.** Run these and work from the output; do not transcribe a line number
from any planning document — the review that produced them found a systematic off-by-one/two drift:

```
grep -n '^## \|^<a id=' docs/operations/patch-management/01-windows-wufb-rings.md
grep -nio "Autopatch rings" docs/operations/patch-management/01-windows-wufb-rings.md
grep -nE '\b(First|Fast)\b' docs/operations/patch-management/01-windows-wufb-rings.md
grep -nE '\bBroad\b' docs/operations/patch-management/01-windows-wufb-rings.md
grep -niE 'mutually[[:space:]]+exclusive|mutual-exclusion|mutual exclusion' docs/operations/patch-management/01-windows-wufb-rings.md
grep -nE 'May 2026|April 2026|opt-out|VBS|Windows 11 Pro' docs/operations/patch-management/01-windows-wufb-rings.md
```

**Two measured facts that correct the research documents.** Confirm both from your own output before
you rely on them:

1. The C11 pattern runs **case-insensitively**, so the Autopatch disambiguation **H2 itself is a
   third plural hit** in this file — a hit the research document's offset table omits. It is kept
   compliant by the parenthetical word inside that same heading, which is on the keyword allowlist.
   **Never remove that parenthetical from the heading**, and never edit the heading at all (D-42
   leaves headings untouched anyway).
2. Six occurrences of one cohort name in this file are **not** part of the falsified Autopatch
   naming — they belong to the WUfB-side ring table's own ring name, in the compound form
   `Broad WUfB deployment ring`. Leave every one of them alone. The acceptance gate below excludes
   exactly that compound; if your sweep touches those lines, the gate is not what caught it.

Then make all of the following edits and commit them together.

**A. FIX-01 — the falsified Autopatch cohort names (three sites).**
Replace the four-name cohort listing in the Autopatch disambiguation section's opening sentence, the
promotion-sequence sentence that walks the reader through the cohorts, and the numbered
migration-step parenthetical, with the two current default rings `Test` and `Last`. This is
**delete-and-replace, not attribution** — no validator pins those three names (verify with
`grep -rn "First\|Fast\|Broad" scripts/validation/*.mjs`, whose only hits are an explanatory comment
and unrelated identifiers), and ROADMAP Success Criterion 2 requires a reader to find no surviving
instance. Do not write the old names into a "previously called…" clause.

**B. FIX-02 — the exclusivity claim, including the PITFALL-9 blockquote (D-55).**
Replace every exclusivity assertion in this file with the first-party containment position: an
Autopatch group is a container that *includes* an `Update rings policy for Windows 10 and later`
among the policies it creates and assigns, and the documented consequence is **loss of direct
authorship**, not exclusion. The claim appears in the page-intro routing sentence, in the callout
blockquote itself, in the paragraph immediately after that blockquote, and in the migration-step
prose about stale policy objects — measure all of them with the `grep -ni` command above.

Two hard constraints on the blockquote rewrite:
  - The literal `PITFALL-9` **must survive** in it. That label is a corpus-wide cross-reference (the
    overview hub and the co-management migration guide both point at it), and this blockquote is its
    defining occurrence. D-55 requires retaining the literal **and** singularising the plural in the
    same rewrite.
  - Every `ring` token in the replacement text must satisfy `V-54-11`: a qualifier
    (`WUfB deployment`, `Autopatch`, `Update`, or a bare `deployment`) within the ~40 characters
    immediately preceding it. `V-54-11` strips headings, fenced code, inline code and anchors, but
    **it does not strip markdown link targets** — see rule F.

**C. C11 — singularise so the hits die (D-13, D-54).**
After edits A and B, the only surviving plural in this file must be the one inside the disambiguation
H2, which is self-kept. Confirm with `grep -cio "Autopatch rings"` returning exactly `1` and with the
live C11 audit. D-54's 36-character cliff — the slack between the disambiguation heading and the
plural hit below it — is why **no text of any kind may be inserted between that heading and the
paragraph that follows it**, evidence lines included. FIX-01's own rewrite *shortens* that
parenthetical, which is safe; added text is the hazard. Once the plural below the heading is gone,
the cliff is discharged, but the no-insertion rule stands for the length of the edit.

**D. FIX-04 — the product rename (D-39, D-41, D-42).**
Rename the product in **body prose only**, to `Windows Update client policies`:
  - **Retain the coined compound** `WUfB deployment ring` — it is roughly 50 of this file's ~60
    tokens and is the corpus's own coined term.
  - `WUfB` survives in the product name for the reporting surface (Windows Update for Business
    **reports**), FIX-04's one named carve-out.
  - **Do not touch the H1, do not touch the anchor-owning H2, do not rename the file.** All three are
    validator-invisible (`V-54-11` strips headings) and reader-maximal, and an H2 rename silently
    breaks the inbound anchor links that at least eight other documents use. Phase 146 owns
    structural surgery on this file.
  - Table-row cells count as body prose for the rename; External-References link labels and link
    targets do not.
  - **Do not widen the scope on a pin-risk argument.** Per **D-40** the "the other files' pins were
    never surveyed" reasoning is disproved and must not be reused: only one validator file mentions
    the old product name at all. The binding authority for the five-document scope is ROADMAP Success
    Criterion 1's file list plus D-39's owner ruling, nothing else.

**E. FIX-07 — the Hotpatch section, by attribution rather than deletion (D-10, D-12, D-22).**
The section runs from the Hotpatch H2 to the anchor line that precedes the driver/firmware H2 —
derive both boundaries from the `grep -n '^## \|^<a id='` output, do not use a transcribed span.
Correct it to the current first-party position:
  - Hotpatch is **enabled by default for eligible devices**.
  - It is configured at **two** levels: a tenant-wide default that applies only to devices not
    targeted by a quality-update policy, and a per-policy setting that wins where assigned.
  - **Arm64 is supported** with CHPE disabled (`HotPatchRestrictions=1`).
  - **VBS is required.**
  - The corpus's own cutover-month and admin-centre-toggle framing is restated as **what this guide
    asserted**, paraphrased and attributed to the guide — **never attributed to Microsoft**, and
    never simply deleted.
  - The **Windows 11 Pro exclusion is retained and flagged unconfirmed in prose**, in words to the
    effect that this guide's Pro exclusion is not restated by the current article and should be
    treated as unconfirmed. Use **prose, not a bracketed token**: the confidence-marker convention
    the draft proposed importing runs to only four hits across three files corpus-wide — too thin to
    adopt — and `V-54-30`'s placeholder-token regex is case-sensitive, so a bracketed marker is a
    gratuitous risk. Do **not** resolve the Pro question to yes or no: a direct fetch of the current
    article this milestone found it genuinely silent on Pro.

  **Preservation list — every one of these literals must still be present in the file after the
  rewrite** (`V-54-12` tests them against whole-file content, not just the Hotpatch region):
  the cutover month `May 2026`, `VBS`, the word `default`, and at least one of `opt-out` or
  `April 2026`. This is the mechanism that makes the whole phase possible: a pin is a
  literal-presence test, not a claim-endorsement test, so each pinned literal survives inside a
  sentence that is true. A diff that removes any of them is the phase's single most likely
  self-inflicted failure.

**F. FIX-10 — evidence lines, the URL screening rule, and dates (D-01 to D-03, D-06, D-07).**
Add one `**Source:**` line per corrected claim, as a standalone paragraph beneath the paragraph
carrying that claim, in the shape used by the citation precedent you read, with the article titles,
URLs and dates from `145-RESEARCH.md`'s claim-to-source table.

  - **The URL screening rule is specific to this file and is reproduced, not theoretical (D-03).**
    Before adding any link, check its target for a bare ring token: `V-54-11` strips inline code but
    **not** link targets, and a slug containing a hyphenated ring word does not satisfy the
    qualifier test, because the qualifier match requires the qualifier word to sit immediately before
    the token with only whitespace between. Appending the canonical deployment-rings Learn URL to
    this file was measured taking `check-phase-54` from 32/0 to 31/1 with `V-54-11` failing. If a
    chosen URL carries such a token, wrap that URL in backticks (inline code is stripped) or choose a
    different first-party article that does not.
  - Evidence lines go **outside** the leading platform-applicability blockquote (D-07), and **never**
    between the disambiguation heading and the paragraph below it (rule C).
  - **What "dated after the event it describes" means (D-04, owner-ruled).** Read it as *dated after
    the corpus assertion being corrected* — the source's own publication date must post-date the
    `last_verified` stamp under which the falsified claim shipped. The hotpatch correction describes a
    **future** event, so no source can post-date the event itself and the literal reading is
    unsatisfiable. No requirement text is edited to record this.
  - **No separate dated correction marker (D-11).** The re-stamped `last_verified` plus the
    `**Source:**` line carry the date between them. Do not invent a bracketed "corrected on" token:
    this file has no version-history section, so the placeholder-token assertion's version-history
    carve-out does not exist here and there is no anchor a marker could hide behind.
  - Re-stamp the frontmatter: `last_verified` is the **actual date this commit lands**, `review_by`
    is that date plus exactly 60 days, computed with:
    `python -c "import datetime; d=datetime.date.today(); print(d.isoformat(), (d+datetime.timedelta(days=60)).isoformat())"`
  - Leave the frontmatter `platform:` value as it is — `V-54-07` pins it per file.

**G. Standing negatives for this file.**
  - Do not touch the driver/firmware H2, its anchor, the "this is NOT a ring" disambiguation, or any
    part of the dual-scan section. `V-54-13` pins the H2 and the dual-scan literal, and Phase 146
    owns that surgery. A clean deletion takes the chain red.
  - Do not add or remove any `<a id="…"></a>` anchor line.
  - `V-54-30` bars placeholder tokens; do not leave one behind.

**H. Gate, then commit.** Run every command in `<verify>`. `check-phase-54.mjs` is a **hard** gate on
this commit (D-18, D-23, ROADMAP Phase 146 blast radius): if it is not 32/0, do not commit. The
commit touches exactly one file.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs   # HARD GATE. expect: "Summary: 32 passed, 0 failed, 0 skipped", exit 0</automated>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs   # expect: "Summary: 16 passed, 0 failed, 0 skipped", exit 0</automated>
    <automated>grep -cio "Autopatch rings" docs/operations/patch-management/01-windows-wufb-rings.md   # expect: 1 (the self-kept H2 only)</automated>
    <automated>grep -rio "Autopatch rings" docs/ --include='*.md' | wc -l   # expect: 1 corpus-wide</automated>
    <automated>grep -cE '\b(First|Fast)\b' docs/operations/patch-management/01-windows-wufb-rings.md   # expect: 0</automated>
    <automated>grep -nE '\bBroad\b' docs/operations/patch-management/01-windows-wufb-rings.md | grep -vcE 'Broad WUfB deployment ring'   # expect: 0</automated>
    <automated>grep -ciE 'mutually[[:space:]]+exclusive|mutual-exclusion|mutual exclusion' docs/operations/patch-management/01-windows-wufb-rings.md   # expect: 0</automated>
    <automated>grep -c 'PITFALL-9' docs/operations/patch-management/01-windows-wufb-rings.md   # expect: 1 or more</automated>
    <automated>for t in "May 2026" "VBS" "default" "Windows 11 Pro" "dual-scan"; do printf "%s=" "$t"; grep -c "$t" docs/operations/patch-management/01-windows-wufb-rings.md; done   # expect: every count 1 or more</automated>
    <automated>grep -cE 'opt-out|April 2026' docs/operations/patch-management/01-windows-wufb-rings.md   # expect: 1 or more</automated>
    <automated>grep -c 'HotPatchRestrictions=1' docs/operations/patch-management/01-windows-wufb-rings.md   # expect: 1 or more</automated>
    <automated>grep -c '^\*\*Source:\*\* \[' docs/operations/patch-management/01-windows-wufb-rings.md   # expect: 4 or more</automated>
    <automated>python -c "import re,datetime,io; c=io.open('docs/operations/patch-management/01-windows-wufb-rings.md',encoding='utf-8').read(); lv=datetime.date.fromisoformat(re.search(r'^last_verified: (\S+)',c,re.M).group(1)); rb=datetime.date.fromisoformat(re.search(r'^review_by: (\S+)',c,re.M).group(1)); print((rb-lv).days, lv==datetime.date.today())"   # expect: "60 True"</automated>
    <automated>git diff HEAD~1 HEAD -- docs/operations/patch-management/01-windows-wufb-rings.md | grep -c '^[-+]<a id='   # expect: 0 (no anchor line added or removed)</automated>
    <automated>node scripts/validation/check-phase-53.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-57.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-59.mjs   # expect: "Summary: 36 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs   # expect: "234 files checked, 0 with violations, 0 total violations"</automated>
    <automated>node scripts/validation/check-nav-hub-links.mjs   # expect: "0 hub-presence failure(s), 0 corpus-link failure(s), 0 total"</automated>
    <automated>git show --name-only HEAD   # expect: exactly one path, docs/operations/patch-management/01-windows-wufb-rings.md</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-54.mjs` exits 0 and prints `Summary: 32 passed, 0 failed, 0 skipped` — the hard gate.
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0 and prints `Summary: 16 passed, 0 failed, 0 skipped`.
    - `grep -cio "Autopatch rings" docs/operations/patch-management/01-windows-wufb-rings.md` returns `1`, and `grep -rio "Autopatch rings" docs/ --include='*.md' | wc -l` returns `1` corpus-wide — the disambiguation heading, kept compliant by its own parenthetical.
    - `grep -cE '\b(First|Fast)\b' docs/operations/patch-management/01-windows-wufb-rings.md` returns `0`.
    - `grep -nE '\bBroad\b' … | grep -vcE 'Broad WUfB deployment ring'` returns `0` — every surviving occurrence is the legitimate WUfB-side ring name.
    - `grep -ciE 'mutually[[:space:]]+exclusive|mutual-exclusion|mutual exclusion' …` returns `0`.
    - `grep -c 'PITFALL-9' …` returns at least `1` — the label survives the blockquote rewrite (D-55).
    - Each of `May 2026`, `VBS`, `default`, `Windows 11 Pro` and `dual-scan` returns a count of at least 1, and `grep -cE 'opt-out|April 2026'` returns at least 1 — the full `V-54-12` preservation list plus the retained Pro exclusion plus the untouched Phase-146 literal.
    - `grep -c 'HotPatchRestrictions=1' …` returns at least `1`.
    - `grep -c '^\*\*Source:\*\* \[' …` returns at least `4`, every one markdown-linked and dated.
    - The frontmatter arithmetic check prints `60 True`.
    - `git diff HEAD~1 HEAD -- <file> | grep -c '^[-+]<a id='` returns `0` — no anchor added or removed.
    - `git show --name-only HEAD` lists exactly one path.
  </acceptance_criteria>
  <done>The Windows guide names the two current Autopatch default rings, states containment rather than exclusion while keeping its PITFALL-9 label, tells the current hotpatch truth with the corpus's superseded framing attributed and every pinned literal intact, uses the current product name in body prose, and carries dated evidence inside a 60-day window — with check-phase-54 at 32/0 and the driver/firmware section untouched for Phase 146.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external documentation source → corpus prose | First-party Microsoft article content is transcribed into `docs/` as cited claims. No code fetches it; a human reader follows the link. |
| repo working tree → local validator execution | `scripts/validation/*.mjs` execute against the working tree locally and in GitHub Actions. |

This plan edits one markdown document. There is **no network surface, no authentication, no user
input and no secret** in scope. No threat is invented to fill a category.

## STRIDE Threat Register (OWASP ASVS Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-02 | Repudiation | corrected claim provenance | low | mitigate | Each corrected claim carries a dated, markdown-linked `**Source:**` line (D-01), and the superseded position is attributed to this guide rather than silently deleted (D-08), so the correction is auditable in both directions. |
| T-145-06 | Tampering | validator coverage silently weakening | low | mitigate | `V-54-12`'s literals are asserted present by explicit greps in the acceptance criteria as well as by the validator, so an attribution rewrite that quietly drops a pinned literal fails the task rather than the next phase. |
| T-145-05 | Tampering | cited source content drifting after citation | low | accept | Sources are dated at citation time and `review_by` is set 60 days out — the corpus's own re-verification mechanism. RESEARCH.md assumption A1 bounds the residual risk. |
| T-145-04 | Information Disclosure | edited prose | low | accept | The corpus is a published knowledge base; the edits carry no tenant identifiers, credentials, customer data or secrets. |
| T-145-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install occurs in this phase (RESEARCH.md Package Legitimacy Audit: "Not applicable"). |

Spoofing, Denial of Service and Elevation of Privilege have no applicable component in this plan.
</threat_model>

<verification>
- `node scripts/validation/check-phase-54.mjs` → `Summary: 32 passed, 0 failed, 0 skipped` (hard gate)
- `node scripts/validation/v1.20-milestone-audit.mjs` → `Summary: 16 passed, 0 failed, 0 skipped`
- `node scripts/validation/check-phase-53.mjs` → `Summary: 26 passed, 0 failed, 0 skipped`
- `node scripts/validation/check-phase-57.mjs` → `Summary: 26 passed, 0 failed, 0 skipped`
- `node scripts/validation/check-phase-59.mjs` → `Summary: 36 passed, 0 failed, 0 skipped`
- `node scripts/validation/c17-eee-contract.mjs` → `234 files checked, 0 with violations, 0 total violations`
- `node scripts/validation/check-nav-hub-links.mjs` → `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`
</verification>

<success_criteria>
- ROADMAP Success Criterion 1 is satisfied for this file: the current first-party position on Autopatch default rings, WUfB-versus-Autopatch containment, hotpatch's two configuration levels and the product rename, each with a per-claim evidence line and a ≤60-day freshness interval computed by arithmetic.
- ROADMAP Success Criterion 2 is satisfied for this file across all three cohort-naming sites and all exclusivity sites.
- ROADMAP Success Criterion 5's tail clause holds: `check-phase-54.mjs` exits 0 throughout.
- Combined with plans 01 and 02, Success Criterion 2 is now fully discharged: exactly one plural anti-pattern occurrence remains corpus-wide, inside a heading that carries its own allowlisted keeper.
</success_criteria>

<output>
Create `.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-03-SUMMARY.md` when done.
Record in it: the pre-edit and post-edit census for the plural anti-pattern, the cohort names and the
exclusivity wording; the measured presence of every preservation-list literal; the URLs chosen for
each evidence line and whether any needed backticking under the D-03 screening rule; the stamped date
pair; and the commit hash.
</output>
