---
phase: 145-corpus-correction-validator-gate-archival-drift-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/check-phase-59.mjs
  - docs/operations/co-management/03-cocmgmt-migration-paths.md
autonomous: true
requirements: [FIX-12, FIX-01]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "V-59-14 reads docs/operations/00-index.md at the v1.5 close SHA ba2cbc0, not at HEAD"
    - "Adding a sixth data row to the ops index Patch region leaves check-phase-59.mjs at 36 passed / 0 failed"
    - "The Autopatch default-ring parenthetical in co-management/03 names the two current default rings"
    - "check-phase-53.mjs stays at 26 passed / 0 failed after the co-management edit"
  artifacts:
    - "scripts/validation/check-phase-59.mjs — assertion V-59-14 renamed with the [v1.5-frozen @ ba2cbc0] suffix"
    - "docs/operations/co-management/03-cocmgmt-migration-paths.md — one corrected line"
    - ".planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/COVERAGE.md (already written at plan time)"
  key_links:
    - "readAtV15Close (scripts/validation/_lib/frozen-at-close.mjs) -> the immutable commit ba2cbc0 -> docs/operations/00-index.md as it stood at v1.5 close"
    - "audit-harness-v1.5-integrity.yml:270 `if [ -f ... ]` guard -> the check-phase-59.mjs FILENAME (which must not change)"
    - "V-53-20 prerequisite literals -> co-management/03 body prose (the edit must not disturb them)"
---

<objective>
Land the two phase-opening atoms that nothing else depends on and that everything else is safer
after: the `check-phase-59.mjs` frozen-read conversion (FIX-12), which D-17 requires to be the
FIRST commit of the phase and never mixed with document edits, and the five-word Autopatch
default-ring correction in `co-management/03` (FIX-01's fourth site, D-50).

Purpose: FIX-12 disarms a row-count equality that today reads "no patch-management guide may ever be
added", which would take Phase 152 red the moment it grows the operations index from 5 rows to 9.
The `co-management/03` swap is the smallest complete correction in the phase and proves the
prose-edit-plus-validator-gate loop end to end on a file with its own live pins.

Output: two commits. Commit 1 is validator-only. Commit 2 is a single-line prose change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-CONTEXT.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-PATTERNS.md
</context>

<artifacts>
## Artifacts this phase produces

This phase produces **no new symbols and no new source files**. It modifies existing documents plus
one validator assertion. From this plan specifically:

| Artifact | Kind | Note |
|---|---|---|
| `V-59-14` (assertion name string in `scripts/validation/check-phase-59.mjs`) | modified assertion | gains the `[v1.5-frozen @ ba2cbc0]` suffix per D-37, and its read call site moves from the live reader to `readAtV15Close` |
| `.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/COVERAGE.md` | declaration | written at plan time; pre-empts the `verify:pre` api-coverage seal gate. Do not delete it. |

No SPEC.md exists for this phase, so there is no `## Edge Coverage` / `## Prohibitions` section to
lift into these plans.
</artifacts>

<tasks>

<!-- planner-discipline-allow: First -->
<!-- planner-discipline-allow: Fast -->
<!-- planner-discipline-allow: Broad -->

<task type="tracer">
  <name>Task 1: FIX-12 — convert V-59-14's ops-index read to a frozen read and prove row growth is unblocked</name>
  <precondition>The v1.5 close commit is reachable in this clone: `git cat-file -e ba2cbc0^{commit}` exits 0. On a shallow clone it does not, and `readAtV15Close` throws by design (D-36) — halt and report rather than swallowing the error.</precondition>
  <reversibility rating="reversible">One call site and one string in one validator; reverting is a two-line edit with no dependents (no `check-phase-*.mjs` pins check-phase-59's content).</reversibility>
  <files>scripts/validation/check-phase-59.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-59.mjs` — read the file header (lines 1-25, for the `readFile` helper and the existing `readAtV15Close` import), the `V-59-07` block, the `V-59-14` block, and the throw-don't-swallow call site. Locate all four by `grep -n "readAtV15Close\|readAtV116Close\|V-59-14\|V-59-07" scripts/validation/check-phase-59.mjs` — do NOT trust any line number transcribed from CONTEXT.md, RESEARCH.md or PATTERNS.md, all three of which cite different values for the same line.
    - `scripts/validation/_lib/frozen-at-close.mjs` — the `V15` SHA entry and the `readAtV15Close` export.
    - `.github/workflows/audit-harness-v1.5-integrity.yml` — the job step that guards on the presence of this validator file (find it with `grep -n "check-phase-59" .github/workflows/audit-harness-v1.5-integrity.yml`).
    - `145-CONTEXT.md` decisions **D-34** (convert V-59-14 only), **D-35** (helper and assertion wording), **D-36** (let it throw), **D-37** (assertion-name suffix, never the filename), **D-38** (recorded for Phase 152, not this phase).
  </read_first>
  <action>
Re-measure first, then edit. Every coordinate below is to be derived at execution time.

1. **Locate.** `grep -n "V-59-14" scripts/validation/check-phase-59.mjs` gives the assertion-name
   line. The read call site is the first statement inside that assertion's `run()` body; the line
   immediately after it is a null-guard that returns a "File missing" failure.

2. **Record the pre-conversion behaviour (this is the tracer's before-half).** Append one extra
   data row to the `## Patch & Update Management` table in `docs/operations/00-index.md`, copying
   the shape of the rows already there (a scratch row pointing at any existing
   `patch-management/` guide is fine — it is reverted in step 3). Run
   `node scripts/validation/check-phase-59.mjs` and confirm it now reports **35 passed, 1 failed**
   with V-59-14 the failing assertion. Record that output. Then
   `git checkout -- docs/operations/00-index.md`.

3. **Convert.** In the `V-59-14` assertion only:
   - Swap the live read helper call for `readAtV15Close`, passing the same ops-index path constant.
   - Delete the null-guard line that follows it and replace it with the one-line comment used at
     the existing `readAtV15Close` site further down this same file, which records that the helper
     throws on a git error and the throw is allowed to propagate (D-36). Copy that comment's
     wording from the existing site rather than inventing new wording. Do NOT wrap the call in
     `try`/`catch` — that is `readAtV116Close`'s convention in this file, not `readAtV15Close`'s.
   - Rewrite the assertion **name** string so it states that the operations index *carried five
     Patch rows at v1.5 close* (D-35 — not "Phase 59 shipped five rows", because the pinned SHA is
     recorded in `frozen-at-close.mjs` as the Phase 61 / v1.5-close commit), and append the frozen
     suffix `[v1.5-frozen @ ba2cbc0]` to the end of the name, copying the placement used by the
     `V-59-07` assertion name.
   - Change nothing else. `V-59-02` (existence), `V-59-13` (H2 presence) and `V-59-34` (negative
     TBD/TODO hygiene) stay live reads (D-34). Do not rename the file (D-37): the v1.5 harness
     workflow guards on the filename with a file-existence test and a rename becomes a silent skip,
     not a failure.

4. **Prove the conversion (the tracer's after-half).** Repeat step 2's scratch-row append, re-run
   `node scripts/validation/check-phase-59.mjs`, and confirm **36 passed, 0 failed** — the row
   count no longer bars adding a guide. Then `git checkout -- docs/operations/00-index.md` and
   confirm `git status --porcelain docs/` is empty before committing.

5. **Gate.** Run the D-18 validator set (all seven commands are in this task's `<verify>` block)
   and confirm every one is at its recorded baseline.

6. **Commit alone.** This commit contains exactly one changed file. Never stage a document edit
   alongside it (D-17 atom 5: FIX-12 is its own commit, landed first).
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-59.mjs   # expect: "Summary: 36 passed, 0 failed, 0 skipped", exit 0</automated>
    <automated>grep -c 'readAtV15Close(OPS_INDEX_MD)' scripts/validation/check-phase-59.mjs   # expect: 3 (the two pre-existing sites plus V-59-14)</automated>
    <automated>grep -c 'v1.5-frozen @ ba2cbc0' scripts/validation/check-phase-59.mjs   # expect: 2 (V-59-07 and V-59-14)</automated>
    <automated>node scripts/validation/check-phase-53.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-54.mjs   # expect: "Summary: 32 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-57.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs   # expect: "234 files checked, 0 with violations, 0 total violations"</automated>
    <automated>node scripts/validation/check-nav-hub-links.mjs   # expect: "0 hub-presence failure(s), 0 corpus-link failure(s), 0 total"</automated>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs   # expect: "Summary: 16 passed, 0 failed, 0 skipped"</automated>
    <automated>git show --stat HEAD -- scripts/validation/check-phase-59.mjs   # expect: exactly 1 file changed in this commit</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-59.mjs` exits 0 and prints `Summary: 36 passed, 0 failed, 0 skipped`.
    - `grep -c 'readAtV15Close(OPS_INDEX_MD)' scripts/validation/check-phase-59.mjs` returns `3`.
    - `grep -c 'v1.5-frozen @ ba2cbc0' scripts/validation/check-phase-59.mjs` returns `2`.
    - The behavioural probe is recorded in the SUMMARY with both measured outputs: pre-conversion `35 passed, 1 failed` with a sixth Patch row present, post-conversion `36 passed, 0 failed` with the same sixth row present.
    - `git show --name-only HEAD` lists exactly one file, `scripts/validation/check-phase-59.mjs`.
    - `git status --porcelain docs/` is empty (the scratch row was reverted).
    - `ls scripts/validation/check-phase-59.mjs` still resolves — the file was not renamed.
  </acceptance_criteria>
  <done>V-59-14 asserts a historical fact about the operations index at v1.5 close instead of a standing prohibition on growing it, proven by a sixth Patch row passing where it previously failed, and the change is committed on its own.</done>
</task>

<task type="auto">
  <name>Task 2: FIX-01 (site 4 of 4) — correct the Autopatch default-ring names in co-management/03</name>
  <reversibility rating="reversible">A five-word parenthetical in one line of one file.</reversibility>
  <files>docs/operations/co-management/03-cocmgmt-migration-paths.md</files>
  <read_first>
    - `docs/operations/co-management/03-cocmgmt-migration-paths.md` — read the whole file; it is 128 lines. Locate the target line with `grep -nE '\b(First|Fast|Broad)\b' docs/operations/co-management/03-cocmgmt-migration-paths.md` (keying on the individual names, NOT on the comma-and string — REQUIREMENTS FIX-01 records that a comma-and sweep misses this exact site).
    - `scripts/validation/check-phase-53.mjs` — the `V-53-20` required-literal assertion and the `V-53-21` negative that bars a platform-applicability blockquote in this file. Find both with `grep -n "V-53-20\|V-53-21" scripts/validation/check-phase-53.mjs`.
    - `145-CONTEXT.md` decision **D-50** (the full scope ruling for this file) and **D-05** (no evidence line here).
  </read_first>
  <action>
Change the parenthetical listing the falsified four-name Autopatch cohort to name the two current
default rings — `Test` and `Last` — and change nothing else in this file. D-50 is a hard scope
boundary and it was measured, not assumed:

- Do **not** apply FIX-02 here. The surrounding sentence already reads as the corrected containment
  position (Autopatch removing the need for manual ring authoring by managing rings on the tenant's
  behalf), and the file contains no `exclusiv` substring anywhere, so there is no exclusivity claim
  to remove.
- Do **not** apply FIX-04's product rename here — the rename is scoped to the five
  `docs/operations/patch-management/` documents only (D-39, owner-ruled).
- Do **not** add a `**Source:**` evidence line here. FIX-10's own text scopes evidence lines to the
  five patch-management documents (D-05).
- Do **not** add a platform-applicability blockquote — `V-53-21` bars one in this file.
- Do **not** re-stamp this file's frontmatter dates; the ≤60-day interval rule is scoped to the five
  patch-management documents.

The resulting diff must be one line changed. This file carries no `Autopatch rings` plural, so the
C11 ordering constraint that governs `00-overview.md` does not apply here — but run the live C11
audit anyway, because it reads the whole corpus.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-53.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped", exit 0</automated>
    <automated>grep -cE '\b(First|Fast|Broad)\b' docs/operations/co-management/03-cocmgmt-migration-paths.md   # expect: 0</automated>
    <automated>grep -c 'Test and Last' docs/operations/co-management/03-cocmgmt-migration-paths.md   # expect: 1</automated>
    <automated>git show --numstat HEAD -- docs/operations/co-management/03-cocmgmt-migration-paths.md   # expect: 1 insertion, 1 deletion</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs   # expect: "234 files checked, 0 with violations, 0 total violations"</automated>
    <automated>node scripts/validation/check-nav-hub-links.mjs   # expect: "0 hub-presence failure(s), 0 corpus-link failure(s), 0 total"</automated>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs   # expect: "Summary: 16 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-54.mjs   # expect: "Summary: 32 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-57.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-59.mjs   # expect: "Summary: 36 passed, 0 failed, 0 skipped"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-53.mjs` exits 0 and prints `Summary: 26 passed, 0 failed, 0 skipped`.
    - `grep -cE '\b(First|Fast|Broad)\b' docs/operations/co-management/03-cocmgmt-migration-paths.md` returns `0`.
    - `grep -c 'Test and Last' docs/operations/co-management/03-cocmgmt-migration-paths.md` returns `1`.
    - `git show --numstat HEAD -- docs/operations/co-management/03-cocmgmt-migration-paths.md` shows `1	1` — one line added, one removed.
    - `grep -c 'exclusiv' docs/operations/co-management/03-cocmgmt-migration-paths.md` returns `0` (unchanged from the pre-edit measurement — nothing was added).
    - `grep -c 'Platform applicability' docs/operations/co-management/03-cocmgmt-migration-paths.md` returns `0`.
    - All seven D-18 gate validators exit 0 at their baselines.
  </acceptance_criteria>
  <done>The co-management migration guide names the two current Autopatch default rings, the file is otherwise byte-unchanged, and check-phase-53 is still 26/26.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external documentation source → corpus prose | First-party Microsoft/Apple/Google article content is transcribed into `docs/` as cited claims. No code fetches it; a human reader follows the link. |
| repo working tree → local validator execution | `scripts/validation/*.mjs` execute against the working tree on the developer machine and in GitHub Actions. |
| pinned git object → validator input | `readAtV15Close` reads a blob out of commit `ba2cbc0` rather than the working tree. |

This phase edits documentation and one local validator. There is **no network surface, no
authentication, no user input and no secret** in scope. The register below is deliberately short for
that reason; no threat has been invented to fill a category.

## STRIDE Threat Register (OWASP ASVS Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-01 | Tampering | `readAtV15Close(OPS_INDEX_MD)` frozen read | low | mitigate | The read is pinned to the immutable commit `ba2cbc0`; per D-36 the helper's throw is allowed to propagate rather than being caught, so an unreachable or rewritten object fails the assertion loudly instead of silently degrading to a pass. |
| T-145-02 | Spoofing | assertion identity (`V-59-14` name string) | low | mitigate | The name gains the `[v1.5-frozen @ ba2cbc0]` suffix, so the pinned SHA is visible in the validator's own output and a later reader cannot mistake a frozen assertion for a live one. |
| T-145-03 | Denial of Service | v1.5 harness CI job | low | mitigate | D-37 forbids renaming the file; `audit-harness-v1.5-integrity.yml`'s file-existence guard would convert a rename into a silent skip (a green run that validated nothing). The acceptance criteria assert the filename still resolves. |
| T-145-04 | Information Disclosure | edited prose | low | accept | The corpus is a published knowledge base. The edits carry no tenant identifiers, credentials, customer data or secrets. |
| T-145-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install occurs anywhere in this phase (RESEARCH.md Package Legitimacy Audit: "Not applicable"). No dependency enters the tree, so no legitimacy checkpoint is required. |

Repudiation and Elevation of Privilege have no applicable component in this plan: nothing
authenticates, authorises, or acts on behalf of a principal.
</threat_model>

<verification>
- `node scripts/validation/check-phase-59.mjs` → `Summary: 36 passed, 0 failed, 0 skipped`
- `node scripts/validation/check-phase-53.mjs` → `Summary: 26 passed, 0 failed, 0 skipped`
- `node scripts/validation/check-phase-54.mjs` → `Summary: 32 passed, 0 failed, 0 skipped`
- `node scripts/validation/check-phase-57.mjs` → `Summary: 26 passed, 0 failed, 0 skipped`
- `node scripts/validation/c17-eee-contract.mjs` → `234 files checked, 0 with violations, 0 total violations`
- `node scripts/validation/check-nav-hub-links.mjs` → `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`
- `node scripts/validation/v1.20-milestone-audit.mjs` → `Summary: 16 passed, 0 failed, 0 skipped`
- `git log --oneline -2` shows two commits, the validator commit first, each touching exactly one file
</verification>

<success_criteria>
- ROADMAP Success Criterion 4 is fully satisfied: `check-phase-59.mjs`'s ops-index read is a frozen read and a sixth Patch row is demonstrably survivable.
- ROADMAP Success Criterion 2 is partially satisfied: the `co-management/03` site — the one a comma-and sweep misses — no longer names the falsified rings. The remaining sites land in plans 02 and 03.
- All seven D-18 gate validators are at their pre-phase baselines.
</success_criteria>

<output>
Create `.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-01-SUMMARY.md` when done.
Record in it: the two measured probe outputs from Task 1 (pre- and post-conversion), the exact
assertion-name string as landed, and both commit hashes.
</output>
