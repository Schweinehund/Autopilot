---
phase: 145-corpus-correction-validator-gate-archival-drift-fix
plan: 05
type: execute
wave: 2
depends_on: ["145-01"]
files_modified:
  - docs/_glossary-linux.md
  - docs/_glossary-apple-business.md
  - docs/l2-runbooks/25-linux-agent-investigation.md
  - docs/cross-platform/apple-business/10-apple-tv-lifecycle.md
  - docs/decision-trees/09-linux-triage.md
  - docs/diagrams/decision-tree-09-linux-triage.svg
  - scripts/generate-diagrams.py
  - .planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-EVIDENCE.md
autonomous: true
requirements: [FIX-09, FIX-11]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "No file under docs/ references a .planning/ path in any form"
    - "Every identifier whose only defining occurrence was a stripped link still has at least one defining occurrence in docs/"
    - "No file under docs/ and no line of scripts/generate-diagrams.py names the superseded Ubuntu LTS version"
    - "The Linux triage diagram was regenerated by its generator, not text-edited, and the other thirteen diagrams are byte-identical"
    - "The intra-Microsoft RHEL version conflict is stated wherever a supported-version list appears in markdown"
    - "The apex chain validator stays at 101 PASS / 0 FAIL / 0 SKIPPED"
  artifacts:
    - "docs/_glossary-linux.md, docs/_glossary-apple-business.md, docs/l2-runbooks/25-linux-agent-investigation.md, docs/cross-platform/apple-business/10-apple-tv-lifecycle.md — one commit (FIX-11, 11 lines / 4 files)"
    - "26 markdown files + scripts/generate-diagrams.py + docs/diagrams/decision-tree-09-linux-triage.svg — one commit (FIX-09)"
    - ".planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-EVIDENCE.md — the SVG regeneration idempotency proof, which has no automated gate anywhere in scripts/validation/"
  key_links:
    - "check-nav-hub-links.mjs and check-phase-143 and the apex -> the .planning/ links, which re-break at archival unless the substance is inlined now"
    - "scripts/generate-diagrams.py subtitle string -> docs/diagrams/decision-tree-09-linux-triage.svg -> docs/decision-trees/09-linux-triage.md, which must all state the same version list"
    - "check-phase-57's Play Integrity H3 deadline-literal ban -> docs/quick-ref-l2.md, which is in the sweep set and must not be disturbed outside its version strings"
---

<objective>
Land the two corrections whose blast radius is the whole corpus rather than one domain: the
archival-drift fix (FIX-11) and the Linux distribution-version sweep with its diagram regeneration
(FIX-09).

Purpose: FIX-11 cannot be deferred. The `.planning/` links took the nav-hub checker, `check-phase-143`
and the apex red at HEAD when the previous milestone's research was archived; writing this
milestone's own research greened them again, which is a manufactured false green. It re-breaks the
moment v1.21's research is archived at close — *after* the close-gate apex has been measured, and
nobody re-runs the apex after archival. FIX-09 is the corpus's largest single-fact staleness, and its
SVG half is the one deliverable in this phase with **no automated gate anywhere** in
`scripts/validation/`, so the phase carries an explicit verification step and records its output.

Output: two commits, plus an evidence file carrying the regeneration proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-CONTEXT.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-RESEARCH.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-PATTERNS.md
</context>

<artifacts>
## Artifacts this phase produces

No new symbols. This plan modifies existing documents, one Python generator line and one generated
SVG, and creates one new planning artifact:

| Artifact | Kind | Note |
|---|---|---|
| `145-EVIDENCE.md` | new planning file | The SVG regeneration idempotency proof. `grep -rln "docs/diagrams\|\.svg" scripts/validation/` returns nothing, so this half of the success criterion has no automated gate and the recorded output *is* the evidence at close. |

The phase-level artifacts (the `V-59-14` assertion renamed with `[v1.5-frozen @ ba2cbc0]`, and
`COVERAGE.md`) are listed in `145-01-PLAN.md`. No SPEC.md exists for this phase, so there is no
`## Edge Coverage` / `## Prohibitions` section to lift.
</artifacts>

<tasks>

<!-- planner-discipline-allow: .planning/ -->
<!-- planner-discipline-allow: 22.04 -->

<task type="auto">
  <name>Task 1: FIX-11 — remove every planning-path reference from docs/, inlining substance where an identifier would otherwise be orphaned</name>
  <precondition>`node scripts/validation/check-nav-hub-links.mjs` reports 0 hub-presence and 0 corpus-link failures BEFORE any edit. It is green today only because this milestone's own research directory currently exists; that is the false green FIX-11 exists to remove, and it is still the baseline this task is measured against.</precondition>
  <files>docs/_glossary-linux.md, docs/_glossary-apple-business.md, docs/l2-runbooks/25-linux-agent-investigation.md, docs/cross-platform/apple-business/10-apple-tv-lifecycle.md</files>
  <read_first>
    - The four target files. Locate every site first with `grep -rn "\.planning/" docs/ --include='*.md'` — the broad form, not the narrow success-criterion form, which misses two of the sites.
    - `scripts/validation/check-nav-hub-links.mjs` — how it resolves markdown links, so you can tell which of these are link-shaped and which are bare-path prose.
    - `scripts/validation/c17-eee-contract.mjs` — enough to know which assertions govern blockquote length and Summary ordering (assertions 4 and 12), the two known traps in this corpus.
    - `145-CONTEXT.md` decisions **D-28** (all 11 lines across 4 files, not the 9/3 first measured), **D-29** (the success-criterion regex is not amended), **D-30** (drop-the-path-keep-the-label is the default, but three named sites need substance and the executor rule that governs them), **D-31** (the headline site needs only the link unwrapped), **D-32** (the scope boundary — this is bounded to planning paths, not to the roughly 28 other pitfall-document mentions in `docs/`), **D-33** (all three original files are contract-enrolled and the contract validator is at 234/0), **D-19** (run the content-scoped pre-commit advisory), **D-20** (a disproved coupling — do not re-derive it).
  </read_first>
  <action>
**Re-measure first.** `grep -rn "\.planning/" docs/ --include='*.md'` returns the authoritative site
list. Expect 11 lines across 4 files: seven in the Apple Business glossary, two in the Apple TV
lifecycle guide, one in the Linux agent investigation runbook and one in the Linux glossary. The
narrow success-criterion form returns only 9 because two of the sites point at a milestones path
rather than a research or phases path — fix all 11. D-29 rules that fixing the invisible sites is
cheaper than amending a criterion the close-gate measures.

**Default treatment: drop the path, keep the label.** Most sites read as "see IDENTIFIER in
`<planning path>`" and are repaired by deleting the path and its markdown link wrapper while keeping
the identifier and the surrounding sentence intact.

**The executor rule that overrides the default (D-30).** Before stripping any identifier, run
`grep -rn "<identifier>" docs/` and confirm at least one **defining** occurrence survives outside the
site you are editing. If not, inline the substance or drop the identifier — never leave a corpus
identifier with zero occurrences, and never leave a sentence that no longer parses. Three sites are
already known to need this treatment and were measured individually:
  - In the Apple Business glossary, one site's remaining text would dangle as a bare "see … deferred
    items" pointing at a phase directory that **no longer exists**. Inline what the reader needed.
  - In the same glossary, another site defines two continuous-integration identifiers; one of them
    has exactly two corpus occurrences and **both are being stripped**. Inline the substance or drop
    the identifier.
  - In the Linux agent investigation runbook, the pitfall identifier at that site has **exactly one**
    corpus occurrence and it is the site being stripped.
Re-verify each of those three by grep at execution time rather than trusting this list.

**The headline site needs the least work (D-31).** In the Linux glossary, the site referenced by
FIX-11's own requirement text needs only the **link unwrapped**: its own next clause already states
the pitfall's substance (compliance reporting exists but is not consumable by device-level
Conditional Access grants), and `docs/admin-setup-linux/03-compliance-policy.md` carries it too.
Confirm both before deciding, then unwrap.

**Scope boundary (D-32).** The reader-facing-defect argument is bounded to planning paths **only**.
Do not sweep the roughly 28 other mentions of the pitfalls document elsewhere in `docs/` — they are
recorded as deferred with a corpus-hygiene trigger.

**Contract hygiene (D-33).** These are mid-paragraph prose edits. Add no blockquote and no heading,
so the two known contract traps in this corpus — the blockquote-length cap and the Summary-ordering
rule — stay untouched. Re-run the contract validator anyway.

**Gate, then commit.** Stage the four files, run
`bash scripts/validation/pre-commit-advisory.sh` (D-19 — it is content-scoped and its pinned-file
pattern covers the Linux glossary; it is advisory and always exits 0, so read its stderr rather than
its exit code), then run the full gate set below and commit.
  </action>
  <verify>
    <automated>grep -rn "\.planning/" docs/ --include='*.md' | wc -l   # expect: 0</automated>
    <automated>grep -rn "\.planning/research/\|\.planning/phases/" docs/ | wc -l   # expect: 0 (the narrow success-criterion form)</automated>
    <automated>node scripts/validation/check-nav-hub-links.mjs   # expect: "0 hub-presence failure(s), 0 corpus-link failure(s), 0 total", exit 0</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs   # expect: "234 files checked, 0 with violations, 0 total violations"</automated>
    <automated>node scripts/validation/check-phase-59.mjs   # expect: "Summary: 36 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-53.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-54.mjs   # expect: "Summary: 32 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-57.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs   # expect: "Summary: 16 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-144.mjs   # expect: "Result: 101 PASS, 0 FAIL, 0 SKIPPED" (~36s; the apex chain, the broadest available regression gate)</automated>
    <automated>git show --name-only HEAD   # expect: exactly four paths</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\.planning/" docs/ --include='*.md' | wc -l` returns `0` — the broad form, which is strictly stronger than ROADMAP Success Criterion 3's narrow regex.
    - `grep -rn "\.planning/research/\|\.planning/phases/" docs/ | wc -l` returns `0` — Success Criterion 3 as written.
    - For every identifier appearing at a repaired site, `grep -rn "<identifier>" docs/` returns at least one defining occurrence; the summary lists each identifier with its surviving occurrence count.
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`.
    - `node scripts/validation/c17-eee-contract.mjs` reports `234 files checked, 0 with violations, 0 total violations`.
    - `node scripts/validation/check-phase-144.mjs` prints `Result: 101 PASS, 0 FAIL, 0 SKIPPED`.
    - `git diff HEAD~1 HEAD -- docs/ | grep -c '^+>'` returns `0` — no blockquote line was added, so the contract's blockquote-length trap cannot have been triggered.
    - `git show --name-only HEAD` lists exactly four paths.
  </acceptance_criteria>
  <done>No document under docs/ references a planning path in any form, every identifier that a stripped link used to define still has a home in the corpus, and archiving this milestone's research at close can no longer re-redden the nav-hub checker or the apex.</done>
</task>

<task type="auto">
  <name>Task 2: FIX-09 — sweep the superseded Ubuntu LTS version across 26 markdown files and the generator, regenerate the Linux triage diagram, and record the idempotency proof</name>
  <precondition>`python --version` (or `python3 --version`) resolves to a working Python 3 interpreter — `scripts/generate-diagrams.py` must run **unmodified apart from its one edited line**, and a text-edited SVG is an explicit requirement failure, not an acceptable substitute.</precondition>
  <reversibility rating="costly">The version list is asserted across 26 documents plus a generated diagram; undoing it means re-sweeping the same 26 files and re-running the generator.</reversibility>
  <files>the 26 markdown files returned by `grep -rlE '22\.04' docs/ --include='*.md'` (including docs/decision-trees/09-linux-triage.md, docs/quick-ref-l2.md, docs/index.md, docs/reference/linux-capability-matrix.md), scripts/generate-diagrams.py, docs/diagrams/decision-tree-09-linux-triage.svg, .planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-EVIDENCE.md</files>
  <read_first>
    - The authoritative file list, derived at execution time: `grep -rlE '\b22\.04\b' docs/ --include='*.md'` (expect 26 files) and `grep -rnE '\b22\.04\b' docs/ --include='*.md' | wc -l` (expect 77 line matches).
    - `scripts/generate-diagrams.py` — the `title()` helper's signature and the one Linux-triage call whose subtitle argument carries the version list. Locate with `grep -n '22\.04' scripts/generate-diagrams.py` and read the surrounding function, plus one sibling `title(...)` call for shape comparison.
    - `docs/decision-trees/09-linux-triage.md` — all of it (82 lines); its version strings must match the diagram's subtitle exactly.
    - `145-CONTEXT.md` decisions **D-44** (the sweep key and the corrected 26-file set), **D-45** (edit the generator line, then run it unmodified), **D-46** (how idempotency is proved, and why the obvious method cannot work here), **D-47** (there is no automated gate for the SVG half), **D-48** (the version conflict does not go into the diagram, and what the edited element actually is), **D-49** (the markdown page and the subtitle change in the same commit).
    - `145-RESEARCH.md` — the FIX-09 claim-to-source row for the version conflict, and the full 26-file list for cross-checking your own measurement.
  </read_first>
  <action>
**A. Sweep the markdown.** Replace the superseded version with the current supported list — Ubuntu
24.04 and 26.04 LTS plus RHEL 9 and 10 — across all 26 files. Two measured traps:
  - The sweep key is the **bare version number**, not the distribution name adjacent to it. One file
    carries two occurrences with no adjacent distribution token on those lines and is invisible to a
    name-keyed sweep.
  - Some occurrences are slash-compounds inside a version range rather than standalone. Read each
    line before replacing; a blind substitution produces nonsense ranges.

**B. State the version conflict where a supported-version list appears (FIX-09, D-48).** The
intra-Microsoft conflict — one Intune page's prerequisites naming RHEL 8 while the supported-platforms
reference names RHEL 9 and 10 — is reproduced, not flattened. State it wherever a supported-version
list appears in markdown. It does **not** go into the diagram.

**C. Files needing extra care inside the 26.**
  - `docs/quick-ref-l2.md` is in the set. Change its version strings and nothing else — its Play
    Integrity section is governed by a pointer-only mandate that bars three deadline literals from
    that region, and `check-phase-57.mjs` enforces it.
  - `docs/reference/linux-capability-matrix.md` and `docs/reference/4-platform-capability-comparison.md`
    are in the set. Prefer **cell-value edits over structural edits**: line-shifting a capability
    matrix is this repository's known pin hazard.
  - `docs/index.md`, `docs/l1-runbooks/00-index.md` and `docs/l2-runbooks/00-index.md` are navigation
    hubs read by the nav-hub link checker. Do not touch link targets or labels.
  - Five files under `docs/admin-setup-linux/` and the Linux glossary are inside the pre-commit
    advisory's pinned-file pattern — stage, then run the advisory and read its stderr (D-19).

**D. Edit the generator, then run it unmodified (D-45, D-48).** The one line to change is the
**subtitle** argument of the Linux-triage diagram's title call — a centre-anchored subtitle drawn
under the title, not the footer; the footer is a separate call in the same function. Update only the
version-list text inside that subtitle, keeping the trailing separator and document-path suffix
intact. Do **not** put the version conflict in it.

**E. Prove the regeneration is idempotent — and use the right method (D-46).**
```
cp -r docs/diagrams /tmp/diagrams-before        # or the session scratchpad
python scripts/generate-diagrams.py
diff -rq /tmp/diagrams-before docs/diagrams     # expect: exactly one differing file, the Linux triage diagram
md5sum docs/diagrams/*.svg > /tmp/after.md5     # record both before and after digests
```
`git status --porcelain` is **structurally incapable** of proving this: the repository sets
`core.autocrlf=true` and its `.gitattributes` covers only one binary asset, so a pure line-ending
change on an SVG is absorbed by git's normalisation and never appears as a diff. Use digest or
`diff -rq` comparison against a pre-run copy, never porcelain.

**F. Record the proof (D-47).** Create
`.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-EVIDENCE.md` and paste
in: the `diff -rq` output, the before-and-after digests for all fourteen diagrams, the generator
command as run, and one sentence naming the measured fact that `grep -rln "docs/diagrams\|\.svg"
scripts/validation/` returns nothing — so this recorded output *is* the close evidence for the
success criterion's SVG half. Doing the work is free; proving it is not, and that is exactly why the
step is explicit.

**G. Same commit (D-49).** The generator line, the regenerated diagram and the markdown page that
displays it all state the same version list and land together, or the diagram and its page disagree.
Include the evidence file in this commit so the proof travels with the change.
  </action>
  <verify>
    <automated>grep -rnE '\b22\.04\b' docs/ --include='*.md' | wc -l   # expect: 0</automated>
    <automated>grep -cE '\b22\.04\b' scripts/generate-diagrams.py   # expect: 0</automated>
    <automated>grep -rn "22\.04" docs/ scripts/generate-diagrams.py | wc -l   # expect: 0 (the closing form named in D-44)</automated>
    <automated>diff -rq "$SCRATCH/diagrams-before" docs/diagrams   # expect: exactly one line, naming decision-tree-09-linux-triage.svg</automated>
    <automated>grep -c 'RHEL' docs/decision-trees/09-linux-triage.md   # expect: 1 or more</automated>
    <automated>python -c "import io,re; s=io.open('docs/diagrams/decision-tree-09-linux-triage.svg',encoding='utf-8').read(); print('24.04' in s, '26.04' in s, 'RHEL 9' in s, '22.04' in s)"   # expect: "True True True False"</automated>
    <automated>node scripts/validation/check-phase-57.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-53.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-54.mjs   # expect: "Summary: 32 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-59.mjs   # expect: "Summary: 36 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs   # expect: "234 files checked, 0 with violations, 0 total violations"</automated>
    <automated>node scripts/validation/check-nav-hub-links.mjs   # expect: "0 hub-presence failure(s), 0 corpus-link failure(s), 0 total"</automated>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs   # expect: "Summary: 16 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/check-phase-144.mjs   # expect: "Result: 101 PASS, 0 FAIL, 0 SKIPPED" (~36s)</automated>
    <automated>node scripts/validation/regenerate-supervision-pins.mjs --report   # expect: "Stale pins ... 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "22\.04" docs/ scripts/generate-diagrams.py | wc -l` returns `0` — the closing measurement named in D-44.
    - `diff -rq` against the pre-run copy names **exactly one** differing file, the Linux triage diagram; the other thirteen are byte-identical. The full output and the before/after digests are pasted into `145-EVIDENCE.md`.
    - The regenerated SVG contains the current version strings and RHEL 9, and does not contain the superseded version — the Python one-liner prints `True True True False`.
    - The evidence file exists and states, in one sentence, that no script under `scripts/validation/` reads the diagrams directory, so this recorded output is the close evidence for the criterion's SVG half.
    - `node scripts/validation/check-phase-144.mjs` prints `Result: 101 PASS, 0 FAIL, 0 SKIPPED` — the whole chain, which is the only gate broad enough for a 26-file sweep.
    - `node scripts/validation/check-phase-57.mjs` exits 0 — the L2 quick reference's governed region was not disturbed.
    - `node scripts/validation/regenerate-supervision-pins.mjs --report` reports 0 stale pins.
    - `git show --name-only HEAD` lists the swept markdown files, the generator, the regenerated diagram and the evidence file, and nothing else.
  </acceptance_criteria>
  <done>The corpus names only supported distribution versions, the version conflict is reproduced wherever a supported-version list appears, the Linux triage diagram was produced by its generator rather than hand-edited, the other thirteen diagrams are provably byte-identical, and the proof is recorded where the close can read it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external documentation source → corpus prose | First-party Microsoft content is transcribed into `docs/` as cited claims. No code fetches it. |
| repo working tree → local script execution | `scripts/validation/*.mjs` and `scripts/generate-diagrams.py` execute against the working tree locally and in GitHub Actions. |
| planning directory → published corpus | Links from `docs/` into `.planning/` cross a boundary the publish bundle does not carry — the defect FIX-11 removes. |

This plan edits markdown, one Python generator line and one generated SVG. There is **no network
surface, no authentication, no user input and no secret** in scope. No threat is invented to fill a
category.

## STRIDE Threat Register (OWASP ASVS Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-09 | Denial of Service | the apex validator chain after archival | high | mitigate | This is the phase's headline risk and the reason FIX-11 cannot be deferred: the planning-path links go red when this milestone's research is archived at close, *after* the close-gate apex has been measured green, and nothing re-runs the apex afterwards. Task 1 removes all 11 sites now and asserts the broad grep returns 0, which is strictly stronger than the success criterion's own regex. |
| T-145-10 | Tampering | the generated SVG | medium | mitigate | A hand-edited SVG would satisfy a naive reader while diverging from its generator, so the next regeneration silently reverts it. Task 2 requires the generator to produce the file and proves idempotency by digest comparison against a pre-run copy — explicitly not by `git status`, which cannot observe a line-ending-only change under this repository's autocrlf setting. |
| T-145-11 | Repudiation | the ungated SVG deliverable | medium | mitigate | Nothing under `scripts/validation/` reads the diagrams directory, so no automated gate exists for this half of the success criterion. `145-EVIDENCE.md` records the command, the diff and the digests, making the claim auditable at close instead of asserted. |
| T-145-12 | Tampering | orphaned corpus identifiers | low | mitigate | D-30's executor rule requires a grep proving at least one surviving defining occurrence before any identifier is stripped; the acceptance criteria require the per-identifier counts to be recorded. |
| T-145-04 | Information Disclosure | edited prose | low | accept | The corpus is a published knowledge base; the edits carry no tenant identifiers, credentials, customer data or secrets. |
| T-145-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install occurs in this phase (RESEARCH.md Package Legitimacy Audit: "Not applicable"). The generator is an existing in-repo script with no new dependency. |

Spoofing and Elevation of Privilege have no applicable component in this plan.
</threat_model>

<verification>
- `grep -rn "\.planning/" docs/ --include='*.md' | wc -l` → `0`
- `grep -rn "22\.04" docs/ scripts/generate-diagrams.py | wc -l` → `0`
- `node scripts/validation/check-phase-144.mjs` → `Result: 101 PASS, 0 FAIL, 0 SKIPPED`
- `node scripts/validation/check-nav-hub-links.mjs` → `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`
- `node scripts/validation/c17-eee-contract.mjs` → `234 files checked, 0 with violations, 0 total violations`
- `node scripts/validation/check-phase-53/54/57/59.mjs` → all at their recorded baselines
- `node scripts/validation/v1.20-milestone-audit.mjs` → `Summary: 16 passed, 0 failed, 0 skipped`
- `diff -rq` against the pre-run diagram copy → exactly one differing file
</verification>

<success_criteria>
- ROADMAP Success Criterion 3 is satisfied and over-satisfied: both the narrow and the broad planning-path greps return 0, so archiving v1.21's own research at close cannot re-redden the nav-hub checker, `check-phase-143` or the apex.
- ROADMAP Success Criterion 5 is satisfied: the superseded distribution version is gone from every markdown occurrence and from the generator, the diagram was regenerated rather than text-edited, the version conflict is stated wherever a supported-version list appears, and `check-phase-54.mjs` exits 0 throughout.
- The apex chain is green at 101/0/0 after the phase's broadest-radius commits.
</success_criteria>

<output>
Create `.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-05-SUMMARY.md` when done.
Record in it: the per-identifier surviving-occurrence counts from Task 1; the measured file and line
counts before and after the Task 2 sweep; the `diff -rq` result; a pointer to `145-EVIDENCE.md`; and
both commit hashes.
</output>
