---
phase: 145-corpus-correction-validator-gate-archival-drift-fix
plan: 04
type: execute
wave: 2
depends_on: ["145-01"]
files_modified:
  - docs/operations/patch-management/02-macos-update-enforcement.md
  - docs/operations/patch-management/03-ios-update-lifecycle.md
  - docs/operations/patch-management/04-android-patch-delivery.md
  - docs/admin-setup-ios/07-device-enrollment.md
autonomous: true
requirements: [FIX-04, FIX-05, FIX-06, FIX-08, FIX-10]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The macOS guide states the two-stage Apple cutover and records that Intune's deprecation banner carries no date"
    - "The Apple DDM identifiers appear as Intune settings-catalog display names across the iOS surfaces, and OfferPrograms is recategorised rather than deleted at all three remaining sites"
    - "The Android 12-month patch-age requirement is scoped as a condition of the strong-integrity verdict, requiring all partitions"
    - "The announced October enforcement date carries a direct first-party evidence line and does not propagate into the quick-reference Play Integrity region"
    - "All three patch-management files carry a review_by minus last_verified interval of at most 60 days"
    - "check-phase-54.mjs stays at 32 passed / 0 failed after every commit"
  artifacts:
    - "docs/operations/patch-management/02-macos-update-enforcement.md — one commit (FIX-05, FIX-04, FIX-10)"
    - "docs/operations/patch-management/03-ios-update-lifecycle.md + docs/admin-setup-ios/07-device-enrollment.md — ONE coupled commit (FIX-06, FIX-04, FIX-10)"
    - "docs/operations/patch-management/04-android-patch-delivery.md — one commit (FIX-08, FIX-04, FIX-10)"
  key_links:
    - "V-54-32 -> atomic coupling of V-54-19 / V-54-20 / V-54-21, which forces the two iOS files into a single commit"
    - "V-54-15 and V-54-23 verbatim blockquote openers -> byte-identical survival through an attribution rewrite of the blockquote body"
    - "check-phase-57's Play Integrity H3 deadline-literal ban -> the Android date must not propagate into the L2 quick reference"
---

<objective>
Land the three per-platform patch-management corrections: the Apple two-stage cutover (FIX-05), the
Apple DDM settings-catalog display names with the `OfferPrograms` recategorisation (FIX-06), and the
Android strong-integrity patch-age scoping with its now-first-party-sourced enforcement date
(FIX-08) — each with FIX-04's rename and FIX-10's evidence lines and dates.

Purpose: these are D-17's atom classes 3 and 4. Each file is its own commit, except the iOS pair,
which `V-54-32` couples atomically and which must therefore land together or not at all.

Output: three commits — one per atom, four files total.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-CONTEXT.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-RESEARCH.md
@.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-PATTERNS.md
</context>

<artifacts>
## Artifacts this phase produces

No new symbols and no new files. This plan modifies four existing documents. The phase-level
artifacts (the `V-59-14` assertion renamed with `[v1.5-frozen @ ba2cbc0]`, and `COVERAGE.md`) are
listed in `145-01-PLAN.md`. No SPEC.md exists for this phase, so there is no `## Edge Coverage` /
`## Prohibitions` section to lift.
</artifacts>

<tasks>

<!-- planner-discipline-allow: Broad -->
<!-- planner-discipline-allow: Oct 31 2026 -->
<!-- planner-discipline-allow: October 31 2026 -->

<task type="auto">
  <name>Task 1: 02-macos-update-enforcement.md — FIX-05 two-stage Apple cutover + FIX-04 + FIX-10, one commit</name>
  <precondition>`node scripts/validation/check-phase-54.mjs` exits 0 with 32 passed BEFORE any edit — the baseline every acceptance criterion is measured against.</precondition>
  <files>docs/operations/patch-management/02-macos-update-enforcement.md</files>
  <read_first>
    - `docs/operations/patch-management/02-macos-update-enforcement.md` — the whole file (173 lines).
    - `scripts/validation/check-phase-54.mjs` — read `V-54-15` (the byte-identical blockquote opener and its five required tokens), `V-54-16` (the minimum deadline-marker count), `V-54-17` (four DDM-only literals), `V-54-07` and `V-54-30`. Locate with `grep -n "id: 15,\|id: 16,\|id: 17,\|id: 7,\|id: 30," scripts/validation/check-phase-54.mjs`.
    - `docs/admin-setup-macos/10-kerberos-sso-extension.md` — the `**Source:**` citation shape (D-01).
    - `145-CONTEXT.md` decisions **D-01** and **D-02** and **D-05** and **D-07** (evidence lines), **D-04** (what "dated after the event" is read as), **D-06** (dates), **D-08** and **D-09** and **D-10** (correction shape, per-file token obligations, the preservation list), **D-11** (no separate dated correction marker), **D-24** (the Apple banner is not a research question — the absence is stated, not filled), **D-39** (rename scope).
    - `145-RESEARCH.md` — the FIX-05 claim-to-source row (both the Apple deployment-updates page and the Intune deprecated-policies page, with their own dates).
  </read_first>
  <action>
Re-measure first:
```
grep -n 'Hard deadline\|HARD-DEADLINE\|Apple OS 26\|WUfB' docs/operations/patch-management/02-macos-update-enforcement.md
grep -o '\[HARD-DEADLINE' docs/operations/patch-management/02-macos-update-enforcement.md | wc -l
```

**A. FIX-05 — the two-stage truth.** Correct the cutover to: legacy MDM update commands are
**deprecated with the 26 releases** and **non-functional in all 27.0 operating systems**. Author it
in the standard correction shape — corrected statement primary, the superseded corpus framing as a
trailing subordinate clause paraphrased and attributed to **this guide, never to Apple or Microsoft**
— with a `**Source:**` line beneath.

**B. FIX-05's second half — the missing date is the finding.** Intune's own deprecation article
carries a deprecation banner with **no date given**. State that absence explicitly as part of the
correction. This is not an open research question and no further search is authorised: D-24 rules it
a pre-committed statement of absence. Do not invent, infer or approximate a date, and do not leave
the reader with the impression that one exists.

**C. Token obligations — measured, and not the same as the other files (D-09).**
  - `V-54-15` requires a blockquote opener line to survive **byte-identical**, including its warning
    emoji and its bold-label parenthetical. The blockquote **body** below that opener is free text
    and is where the two-stage correction goes. Copy the opener line out of the file before editing
    and diff it back afterwards rather than retyping it.
  - `V-54-15` also requires five whole-file literals; measure them from the file before editing and
    confirm all five survive.
  - `V-54-16` is a **count** gate — an attribution rewrite that consolidates paragraphs can break it
    by dropping inline deadline markers. The file measures 8 today against a floor of 5. Do not let
    the count fall below the floor; record the post-edit count in the summary.
  - `V-54-17` requires four DDM-only literals; confirm each survives.
  - The pinned literal `Apple OS 26` is on D-10's preservation list for this file and must survive
    inside a sentence that is true — a pin is a literal-presence test, not a claim-endorsement test.

**D. FIX-04 — rename the product in body prose only** (this file measures 2 tokens, neither in the
coined compound form). Headings, link labels and link targets are out of scope (D-42).

**E. FIX-10 — evidence lines and dates.** One `**Source:**` line per corrected claim, standalone,
directly beneath the paragraph carrying the claim, markdown-linked URL with a parenthesised update
date, **outside** the leading platform-applicability blockquote (D-07).
**What "dated after the event it describes" means (D-04, owner-ruled):** read it as *dated after the
corpus assertion being corrected* — the source's own publication date must post-date the
`last_verified` stamp under which the falsified claim shipped. The Apple cutover correction describes
a **future** operating-system release, so no source can post-date the event itself and the literal
reading is unsatisfiable. **No separate dated correction marker (D-11):** the re-stamped
`last_verified` plus the `**Source:**` line carry the date between them, and this file has no
version-history section for a bracketed marker to sit outside of. Re-stamp `last_verified` to
the actual commit date and `review_by` to that date plus exactly 60 days:
`python -c "import datetime; d=datetime.date.today(); print(d.isoformat(), (d+datetime.timedelta(days=60)).isoformat())"`.
Leave the frontmatter `platform:` value alone — `V-54-07` pins it per file.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs   # expect: "Summary: 32 passed, 0 failed, 0 skipped", exit 0</automated>
    <automated>grep -o '\[HARD-DEADLINE' docs/operations/patch-management/02-macos-update-enforcement.md | wc -l   # expect: 5 or more</automated>
    <automated>for t in "Apple OS 26" "forceDelayedSoftwareUpdates" "com.apple.SoftwareUpdate" "ScheduleOSUpdate" "DDM" "Software Update Enforce Latest" "Intune Settings Catalog" "forward-compatible"; do printf "%s=" "$t"; grep -c "$t" docs/operations/patch-management/02-macos-update-enforcement.md; done   # expect: every count 1 or more</automated>
    <automated>grep -c '^\*\*Source:\*\* \[' docs/operations/patch-management/02-macos-update-enforcement.md   # expect: 2 or more</automated>
    <automated>python -c "import re,datetime,io; c=io.open('docs/operations/patch-management/02-macos-update-enforcement.md',encoding='utf-8').read(); lv=datetime.date.fromisoformat(re.search(r'^last_verified: (\S+)',c,re.M).group(1)); rb=datetime.date.fromisoformat(re.search(r'^review_by: (\S+)',c,re.M).group(1)); print((rb-lv).days, lv==datetime.date.today())"   # expect: "60 True"</automated>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs   # expect: "Summary: 16 passed, 0 failed, 0 skipped"</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs   # expect: "234 files checked, 0 with violations, 0 total violations"</automated>
    <automated>git show --name-only HEAD   # expect: exactly one path</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-54.mjs` exits 0 and prints `Summary: 32 passed, 0 failed, 0 skipped`.
    - The deadline-marker count is at least `5` and its measured value is recorded in the summary.
    - All eight pinned literals return a count of at least 1.
    - `grep -c '27.0' docs/operations/patch-management/02-macos-update-enforcement.md` returns at least `1` — the second stage of the cutover is stated, not just the first.
    - `grep -c '^\*\*Source:\*\* \[' …` returns at least `2`.
    - The frontmatter arithmetic check prints `60 True`.
    - The summary records, in one sentence, that Intune's deprecation banner carries no date and that this absence is stated in the guide rather than filled.
    - `git show --name-only HEAD` lists exactly one path.
  </acceptance_criteria>
  <done>The macOS guide states both stages of the Apple cutover with dated evidence, records the undated Intune banner as an absence, keeps every pinned literal and the byte-identical blockquote opener, and sits inside a 60-day freshness window.</done>
</task>

<task type="auto">
  <name>Task 2: 03-ios-update-lifecycle.md + admin-setup-ios/07-device-enrollment.md — FIX-06 + FIX-04 + FIX-10, ONE coupled commit</name>
  <precondition>`node scripts/validation/check-phase-54.mjs` exits 0 with 32 passed BEFORE any edit, and `V-54-32` is among the passing assertions — it is the coupling gate this task must not break.</precondition>
  <files>docs/operations/patch-management/03-ios-update-lifecycle.md, docs/admin-setup-ios/07-device-enrollment.md</files>
  <read_first>
    - `docs/operations/patch-management/03-ios-update-lifecycle.md` — the whole file (128 lines).
    - `docs/admin-setup-ios/07-device-enrollment.md` — read at minimum the table region containing the DDM cell; locate it with `grep -n 'OfferPrograms\|unsupervised\|03-ios-update-lifecycle' docs/admin-setup-ios/07-device-enrollment.md`.
    - `scripts/validation/check-phase-54.mjs` — read `V-54-18` (required tokens plus the "at least two of four DDM keys" rule), `V-54-19` (the negative pre-edit cell text, the single-line DDM pattern, and the required cross-link), `V-54-20` (which pins literals in `docs/admin-setup-ios/04-configuration-profiles.md` — a file this task must NOT edit), `V-54-21` (a negative that reads `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`), and `V-54-32` (the atomicity coupling). Locate with `grep -n "id: 18,\|id: 19,\|id: 20,\|id: 21,\|id: 32," scripts/validation/check-phase-54.mjs`.
    - `145-CONTEXT.md` decisions **D-05** (no evidence line and no date re-stamp outside the five patch-management documents), **D-52** (recategorise, never delete; the requirement governs over the requirements-row remark), **D-53** (why these two files are one commit), **D-01** and **D-06** and **D-07** (evidence lines and dates, for the patch-management file only), **D-39** (rename scope).
    - `145-RESEARCH.md` — the FIX-06 claim-to-source row (the Apple declarative software-update page and the Intune settings reference, with their dates) and the measured `OfferPrograms` site table.
  </read_first>
  <action>
Re-measure first:
```
grep -n 'OfferPrograms\|TargetOSVersion\|TargetBuildVersion\|TargetLocalDateTime' docs/operations/patch-management/03-ios-update-lifecycle.md docs/admin-setup-ios/07-device-enrollment.md
grep -n 'unsupervised\|iOS 17\|Aug 2025\|August 2025' docs/operations/patch-management/03-ios-update-lifecycle.md
```

**A. FIX-06 — the settings-catalog display names.** Present the six Intune display names — Delay in
Days, Install Time, Details URL, Target Build Version, Target Date Time, Target OS Version — as what
an admin actually sees in the settings catalog, alongside the underlying declarative payload keys
rather than instead of them. Keeping the payload keys is both truthful and required: `V-54-18` needs
at least two of the four declarative key literals present in the patch-management file.

**B. FIX-06's `OfferPrograms` recategorisation — three sites (D-52).** One site sits in the
enrollment guide's table cell and two sit in the patch-management file. At every one:
**recategorise, never delete.** It is a real first-party Apple key that lives in the `Beta`
dictionary and governs beta-programme enrolment, not update enforcement. FIX-06's requirement text
governs over the requirements-table remark that the key "may be dropped freely" — that remark
describes pin latitude, not a content directive, and is already flagged for a one-line clarification
at milestone close.

**C. The coupling is the reason these two files share a commit (D-53).** The enrollment-guide cell
you are editing is the exact cell `V-54-19` tests. That assertion requires, in that file: the
pre-edit cell text **absent**; a DDM-plus-unsupervised-plus-iOS-17 pattern present **on one line**;
and a cross-link literal to the patch-management iOS filename present. `V-54-32` then requires
`V-54-19`, `V-54-20` and `V-54-21` to pass **together**, so a split commit can leave the coupling
assertion red between two green ones. Land both files in one commit.
  - Do **not** edit `docs/admin-setup-ios/04-configuration-profiles.md`. `V-54-20` pins a
    byte-identical phrase and a forward-link there; the file needs no correction and touching it
    only creates risk.
  - Do **not** introduce the retired filename literal `V-54-21` bars into `.planning/REQUIREMENTS.md`
    or `.planning/ROADMAP.md` — those two planning files are read live by that assertion.

**D. A measured do-not-touch.** The patch-management iOS file contains a rollout-stage ring name in
its rollout-patterns section (a Pilot / production / security-driven three-stage sequence). That
production-stage name is an **iOS rollout convention, not Autopatch cohort naming**, and FIX-01 does
not reach it. Leave it exactly as it is. The corresponding acceptance gate is scoped to the
Autopatch-naming files and deliberately does not fire on this one.

**E. FIX-04 — rename the product in body prose only** in the patch-management file (2 tokens
measured, neither in the coined compound form). The enrollment guide is **outside** FIX-04's
owner-ruled five-document scope (D-39) — do not rename anything there.

**F. FIX-10 — scope is asymmetric across these two files (D-05).**
  - `docs/operations/patch-management/03-ios-update-lifecycle.md`: add `**Source:**` evidence lines
    per corrected claim and re-stamp `last_verified` / `review_by` to a 60-day interval computed with
    `python -c "import datetime; d=datetime.date.today(); print(d.isoformat(), (d+datetime.timedelta(days=60)).isoformat())"`.
  - `docs/admin-setup-ios/07-device-enrollment.md`: **no evidence line and no date re-stamp.**
    FIX-10's own text scopes both granularity and file set to the five patch-management documents.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs   # expect: "Summary: 32 passed, 0 failed, 0 skipped", exit 0</automated>
    <automated>node scripts/validation/check-phase-54.mjs --verbose 2>&1 | grep -E 'V-54-(18|19|20|21|32)'   # expect: all five lines report PASS</automated>
    <automated>grep -c 'OfferPrograms' docs/operations/patch-management/03-ios-update-lifecycle.md docs/admin-setup-ios/07-device-enrollment.md   # expect: 2 and 1 respectively — recategorised at all three sites, deleted at none</automated>
    <automated>grep -c '03-ios-update-lifecycle.md' docs/admin-setup-ios/07-device-enrollment.md   # expect: 1 or more</automated>
    <automated>grep -c '^\*\*Source:\*\* \[' docs/operations/patch-management/03-ios-update-lifecycle.md   # expect: 2 or more</automated>
    <automated>grep -c '^\*\*Source:\*\* \[' docs/admin-setup-ios/07-device-enrollment.md   # expect: 0</automated>
    <automated>python -c "import re,datetime,io; c=io.open('docs/operations/patch-management/03-ios-update-lifecycle.md',encoding='utf-8').read(); lv=datetime.date.fromisoformat(re.search(r'^last_verified: (\S+)',c,re.M).group(1)); rb=datetime.date.fromisoformat(re.search(r'^review_by: (\S+)',c,re.M).group(1)); print((rb-lv).days, lv==datetime.date.today())"   # expect: "60 True"</automated>
    <automated>git show --name-only HEAD   # expect: exactly two paths, the iOS lifecycle guide and the iOS enrollment guide</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs   # expect: "234 files checked, 0 with violations, 0 total violations"</automated>
    <automated>node scripts/validation/check-nav-hub-links.mjs   # expect: "0 hub-presence failure(s), 0 corpus-link failure(s), 0 total"</automated>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs   # expect: "Summary: 16 passed, 0 failed, 0 skipped"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-54.mjs` exits 0 and prints `Summary: 32 passed, 0 failed, 0 skipped`, with `V-54-18`, `V-54-19`, `V-54-20`, `V-54-21` and `V-54-32` all reported PASS in verbose output.
    - `grep -c 'OfferPrograms'` returns `2` for the iOS lifecycle guide and `1` for the iOS enrollment guide.
    - At least two of the four declarative key literals remain present in the iOS lifecycle guide, and all six Intune display names appear in it.
    - `git show --name-only HEAD` lists exactly two paths — the coupling held.
    - `docs/admin-setup-ios/04-configuration-profiles.md` is absent from the commit.
    - `grep -c '^\*\*Source:\*\* \[' docs/admin-setup-ios/07-device-enrollment.md` returns `0`, and its frontmatter dates are unchanged (`git diff HEAD~1 HEAD -- docs/admin-setup-ios/07-device-enrollment.md | grep -c 'last_verified\|review_by'` returns `0`).
    - The frontmatter arithmetic check on the iOS lifecycle guide prints `60 True`.
  </acceptance_criteria>
  <done>The Apple DDM identifiers read as Intune settings-catalog display names alongside their declarative keys, the beta-programme key is recategorised at all three sites and deleted at none, and both coupled files landed in a single commit with V-54-32 green.</done>
</task>

<task type="auto">
  <name>Task 3: 04-android-patch-delivery.md — FIX-08 patch-age scoping and the sourced enforcement date + FIX-04 + FIX-10, one commit</name>
  <precondition>The first-party enforcement-date quote still reproduces: re-fetch `https://learn.microsoft.com/en-us/intune/whats-new/in-development` and confirm the announced-enforcement sentence recorded in `145-RESEARCH.md`'s External Research Finding section is still present, together with that page's own `ms.date`/`updated_at`. If it no longer reproduces, do not fabricate the citation — fall back to the attribution wording named in step C and record the fallback as a deviation.</precondition>
  <files>docs/operations/patch-management/04-android-patch-delivery.md</files>
  <read_first>
    - `docs/operations/patch-management/04-android-patch-delivery.md` — the whole file (215 lines).
    - `scripts/validation/check-phase-54.mjs` — read `V-54-22` (the required H2 plus the deadline marker inside a strong-integrity table row), `V-54-23` (the byte-identical blockquote opener plus six required token families), `V-54-24` (the minimum deadline-marker count), `V-54-25` (OEM coverage literals). Locate with `grep -n "id: 22,\|id: 23,\|id: 24,\|id: 25," scripts/validation/check-phase-54.mjs`.
    - `scripts/validation/check-phase-57.mjs` — read the Play Integrity H3 assertion that bars deadline literals inside `docs/quick-ref-l2.md`. Locate with `grep -n 'Play Integrity Verdict Reference' scripts/validation/check-phase-57.mjs`.
    - `145-CONTEXT.md` decisions **D-24** and **D-25** (the date is now first-party sourced; the attribution/hedge path is superseded, and the literal is retained everywhere and never deleted), **D-26** (three collateral constraints), **D-27** (the bounded research pass is discharged — no second research attempt during execution), **D-08** to **D-10** (correction shape and the preservation list), **D-01** and **D-06** and **D-07** (evidence lines and dates).
    - `145-RESEARCH.md` — the External Research Finding section (the query trail, the page's frontmatter dates and the verbatim announced-enforcement sentence), the FIX-08 claim-to-source rows, and assumption **A2**.
  </read_first>
  <action>
Re-measure first:
```
grep -n 'MEETS_STRONG_INTEGRITY\|Hard deadline\|Android 13+\|12-month\|12 months\|May 2025\|September 30 2025' docs/operations/patch-management/04-android-patch-delivery.md
grep -o '\[HARD-DEADLINE' docs/operations/patch-management/04-android-patch-delivery.md | wc -l
```

**A. FIX-08's substance — the scoping correction.** The 12-month patch-age requirement is a
**condition of the strong-integrity verdict**, requiring **all partitions** (the operating-system
partition *and* the vendor partition) to be within that window — it is **not** a general requirement
of Android 13 and above. Correct the framing accordingly, in the standard shape: corrected statement
primary, the superseded corpus framing as a trailing clause attributed to **this guide**, evidence
line beneath.

**B. The date is sourced — cite it directly (D-25, superseded from the earlier hedge path).** The
announced enforcement date **is** first-party sourced: it appears verbatim on Intune's *In
development* page, whose own `ms.date` post-dates the corpus assertion being corrected, and the
orchestrator re-fetched the page to confirm the quote rather than accepting the researcher's word.
Carry a direct `**Source:**` line per D-01. Two accuracy riders that are part of the requirement,
not stylistic preference:
  1. Attribute it as an **announced** enforcement date on Intune's *In development* page, not as a
     shipped general-availability statement — that page's own note says dates and individual
     features might change. Reflect that hedge in the sentence.
  2. The same section independently corroborates the scoping in step A, so it sources FIX-08's
     substance as well as its date.

**C. Fallback, only if the precondition fails.** If the re-fetch does not reproduce the sentence,
use the superseded attribution wording instead — the corpus asserts the deadline, current
first-party pages do not restate it, it is carried unconfirmed — and record the fallback as a
deviation. Never fabricate a `**Source:**` line: it has no rendering for "we looked and found
nothing".

**D. The date literal is RETAINED everywhere, never deleted.** `V-54-23` pins a blockquote opener
carrying that date **byte-identically**, and the whole-file token check requires it too. Copy the
opener line out before editing and diff it back afterwards rather than retyping it. The blockquote
**body** is free text and is where the correction goes.

**E. Preservation list for this file (D-10) — every one must survive:** the abbreviated and
long-form spellings of the October enforcement date, `Android 13+`, `12-month` (or `12 months`),
`May 2025`, and `September 30 2025`. `V-54-24` additionally requires at least 4 deadline markers;
the file measures 9 today. Do not let an attribution rewrite consolidate them below the floor, and
record the post-edit count.

**F. The date must not propagate (D-26).** `check-phase-57.mjs` bars the deadline literals from the
Play Integrity H3 region of `docs/quick-ref-l2.md` — a pointer-only mandate. The ban is on the
literal itself **regardless of how well sourced it now is**. This task does not edit that file;
confirm the region is still clean after the commit.

**G. Do not import the strong-integrity verdict token into the overview hub.** `V-54-29` bars it
from that file's body prose; the overview's table cell is stripped before the test and is already
safe.

**H. FIX-04 — rename the product in body prose only** (2 tokens measured, neither compound).

**I. FIX-10 — evidence lines and dates**, exactly as in tasks 1 and 2: standalone `**Source:**`
lines outside the leading platform-applicability blockquote, `last_verified` stamped with the actual
commit date and `review_by` at exactly 60 days, computed with
`python -c "import datetime; d=datetime.date.today(); print(d.isoformat(), (d+datetime.timedelta(days=60)).isoformat())"`.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs   # expect: "Summary: 32 passed, 0 failed, 0 skipped", exit 0</automated>
    <automated>node scripts/validation/check-phase-57.mjs   # expect: "Summary: 26 passed, 0 failed, 0 skipped", exit 0</automated>
    <automated>grep -o '\[HARD-DEADLINE' docs/operations/patch-management/04-android-patch-delivery.md | wc -l   # expect: 4 or more</automated>
    <automated>for t in "Android 13+" "May 2025" "September 30 2025" "Zebra LifeGuard"; do printf "%s=" "$t"; grep -c "$t" docs/operations/patch-management/04-android-patch-delivery.md; done   # expect: every count 1 or more</automated>
    <automated>grep -cE '12-month|12 months' docs/operations/patch-management/04-android-patch-delivery.md   # expect: 1 or more</automated>
    <automated>grep -cE 'Oct 31 2026|October 31 2026' docs/operations/patch-management/04-android-patch-delivery.md   # expect: 2 or more</automated>
    <automated>grep -cE 'Oct 31 2026|October 31 2026' docs/quick-ref-l2.md   # expect: 0</automated>
    <automated>grep -c '^\*\*Source:\*\* \[' docs/operations/patch-management/04-android-patch-delivery.md   # expect: 2 or more</automated>
    <automated>grep -c 'whats-new/in-development' docs/operations/patch-management/04-android-patch-delivery.md   # expect: 1 or more (0 only if the precondition fallback was taken and recorded as a deviation)</automated>
    <automated>python -c "import re,datetime,io; c=io.open('docs/operations/patch-management/04-android-patch-delivery.md',encoding='utf-8').read(); lv=datetime.date.fromisoformat(re.search(r'^last_verified: (\S+)',c,re.M).group(1)); rb=datetime.date.fromisoformat(re.search(r'^review_by: (\S+)',c,re.M).group(1)); print((rb-lv).days, lv==datetime.date.today())"   # expect: "60 True"</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs   # expect: "234 files checked, 0 with violations, 0 total violations"</automated>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs   # expect: "Summary: 16 passed, 0 failed, 0 skipped"</automated>
    <automated>git show --name-only HEAD   # expect: exactly one path</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-54.mjs` exits 0 and prints `Summary: 32 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/check-phase-57.mjs` exits 0 and prints `Summary: 26 passed, 0 failed, 0 skipped`, and the deadline-literal count inside `docs/quick-ref-l2.md` is `0`.
    - The deadline-marker count in the Android file is at least `4`, and its measured value is recorded in the summary.
    - Every preservation-list literal returns a count of at least 1.
    - `grep -c 'all partitions' docs/operations/patch-management/04-android-patch-delivery.md` returns at least `1` — the scoping correction is stated, not implied.
    - `grep -c '^\*\*Source:\*\* \[' …` returns at least `2`, and one of them links the *In development* page — unless the precondition fallback was taken, in which case the summary records the fallback as a deviation with the failed re-fetch evidence.
    - The frontmatter arithmetic check prints `60 True`.
    - `git show --name-only HEAD` lists exactly one path.
  </acceptance_criteria>
  <done>The Android guide scopes the patch-age requirement as an all-partitions condition of the strong-integrity verdict, cites the announced enforcement date to a first-party page that post-dates the corpus assertion while retaining every pinned literal, and the date has not leaked into the L2 quick reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external documentation source → corpus prose | First-party Microsoft, Apple and Google content is transcribed into `docs/` as cited claims. Task 3 performs one confirmatory re-fetch of a public documentation page; nothing in the repository calls it at build or run time. |
| repo working tree → local validator execution | `scripts/validation/*.mjs` execute against the working tree locally and in GitHub Actions. |

This plan edits four markdown documents. There is **no network surface in the deliverable, no
authentication, no user input and no secret** in scope. No threat is invented to fill a category.

## STRIDE Threat Register (OWASP ASVS Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-07 | Spoofing | the fetched enforcement-date source | medium | mitigate | The quote was obtained through an AI-mediated fetch tool (RESEARCH.md assumption A2). Task 3's `<precondition>` requires a confirmatory re-fetch immediately before the commit and diffs the returned sentence against the recorded quote; a non-reproducing quote forces the documented attribution fallback instead of a fabricated citation. |
| T-145-02 | Repudiation | corrected claim provenance | low | mitigate | Each corrected claim carries a dated, markdown-linked `**Source:**` line, and the announced date is attributed as announced-and-subject-to-change rather than as a shipped guarantee. |
| T-145-08 | Information Disclosure | deadline literal propagating outside its single source of truth | low | mitigate | `check-phase-57`'s pointer-only mandate keeps the deadline literals out of the L2 quick reference; the acceptance criteria assert the count there is still zero after the commit. |
| T-145-05 | Tampering | cited source content drifting after citation | low | accept | Sources are dated at citation time and `review_by` is set 60 days out. |
| T-145-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install occurs in this phase (RESEARCH.md Package Legitimacy Audit: "Not applicable"). |

Denial of Service and Elevation of Privilege have no applicable component in this plan.
</threat_model>

<verification>
- `node scripts/validation/check-phase-54.mjs` → `Summary: 32 passed, 0 failed, 0 skipped` after each of the three commits
- `node scripts/validation/check-phase-57.mjs` → `Summary: 26 passed, 0 failed, 0 skipped`
- `node scripts/validation/check-phase-53.mjs` → `Summary: 26 passed, 0 failed, 0 skipped`
- `node scripts/validation/check-phase-59.mjs` → `Summary: 36 passed, 0 failed, 0 skipped`
- `node scripts/validation/c17-eee-contract.mjs` → `234 files checked, 0 with violations, 0 total violations`
- `node scripts/validation/check-nav-hub-links.mjs` → `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`
- `node scripts/validation/v1.20-milestone-audit.mjs` → `Summary: 16 passed, 0 failed, 0 skipped`
- `git log --oneline -3` shows three commits — one file, two files, one file
</verification>

<success_criteria>
- ROADMAP Success Criterion 1 is satisfied for the three per-platform files: Apple's two-stage cutover, the Apple settings-catalog display names and the Android patch-age scoping, each with a per-claim evidence line and a ≤60-day interval computed by arithmetic.
- The phase's one research flag is discharged on the record: the announced enforcement date carries a first-party citation whose own date post-dates the corpus assertion, confirmed by a re-fetch immediately before the commit.
- `V-54-32`'s atomic coupling held — the two iOS files landed together.
</success_criteria>

<output>
Create `.planning/phases/145-corpus-correction-validator-gate-archival-drift-fix/145-04-SUMMARY.md` when done.
Record in it: the post-edit deadline-marker counts for the macOS and Android files; the re-fetch
result for the enforcement-date quote (reproduced or fallback taken); the three commit hashes and
their file lists, showing the two-file coupled commit explicitly.
</output>
