---
phase: 32
plan: "07"
type: execute
wave: 3
depends_on: ["32-00", "32-04", "32-05"]
files_modified:
  - docs/quick-ref-l2.md
autonomous: true
requirements: [NAV-02]
tags: [quick-ref, l2, ios, cheat-sheet, sysdiagnose, portal-paths, nav-02]
must_haves:
  truths:
    - "An L2 engineer opens quick-ref-l2.md and finds a new ## iOS/iPadOS Quick Reference section at the bottom"
    - "Opening callout explicitly says 'iOS has NO CLI diagnostic tool' per D-31"
    - "Three tables present: iOS Diagnostic Data Collection (3 methods) / Key Intune Portal Paths (4+ rows) / Sysdiagnose Trigger Reference (with modern unified row + legacy per-device rows)"
    - "Each of the 3 tables has an inline research-flag footnote referencing Phase 30 D-32 or Phase 31 D-30/D-31 per D-32"
    - "Investigation Runbooks list links to all 4 iOS L2 runbooks (14-17)"
    - "Sysdiagnose table does NOT conflate DDM software update with VPP silent install supervision rule (RESEARCH.md Pitfall 4)"
    - "Anchor #iosipados-quick-reference produced (Plan 05's forward reference)"
  artifacts:
    - path: "docs/quick-ref-l2.md"
      provides: "New ## iOS/iPadOS Quick Reference H2 with 3 tables + Investigation Runbooks list + research-flag footnotes"
      contains: "## iOS/iPadOS Quick Reference"
      exports: ["#iosipados-quick-reference"]
  key_links:
    - from: "docs/quick-ref-l2.md iOS Investigation Runbooks"
      to: "docs/l2-runbooks/14-ios-log-collection.md through 17-ios-compliance-ca-timing.md (4 files)"
      via: "bullet list links"
      pattern: "l2-runbooks/14-ios-log-collection.md"
    - from: "docs/quick-ref-l2.md iOS Sysdiagnose table"
      to: "docs/l2-runbooks/14-ios-log-collection.md#section-3-mac-cable-sysdiagnose"
      via: "footnote link"
      pattern: "l2-runbooks/14-ios-log-collection.md#section-3"
---

<objective>
Append `## iOS/iPadOS Quick Reference` H2 section at the bottom of `docs/quick-ref-l2.md` AFTER the existing macOS ADE Quick Reference section (line 178), BEFORE the Version History H2 (line 180). Per D-31, structure is DIFFERENT from macOS section — iOS replaces the Terminal Commands bash block with 3 tables (Diagnostic Data Collection / Intune Portal Paths / Sysdiagnose Trigger Reference). Per D-32 (MANDATORY), each table has an inline research-flag footnote citing Phase 30 D-32 or Phase 31 D-30/D-31. Per RESEARCH.md Pitfall 4, DDM software updates are NOT described as supervised-only. Per RESEARCH.md Open Q4 + Pitfall 5, the sysdiagnose table prepends a "Modern iOS 15+ unified combo" top row above the per-device-type rows (with the per-device rows marked as "Legacy / pre-iOS 15").

Purpose: NAV-02 requirement (partial — quick-ref-l2.md scope only). SC #2 literal. Plan 05's index.md iOS L2 subsection links to `#iosipados-quick-reference` — this plan ships that anchor. D-32 research-flag footnote pattern is novel to Phase 32 — executed here for the first time.

Output: quick-ref-l2.md grows from ~187 lines → ~235-245 lines with: new iOS H2 appended at bottom (~45-55 lines per D-31 target), opening "no CLI" callout, 3 tables each with research-flag footnote, Investigation Runbooks list (4 bullets), platform coverage blockquote updated per D-41, frontmatter bumped, Version History row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-navigation-integration-references/32-CONTEXT.md
@.planning/phases/32-navigation-integration-references/32-RESEARCH.md
@.planning/phases/32-navigation-integration-references/32-PATTERNS.md
@docs/quick-ref-l2.md
@docs/l2-runbooks/14-ios-log-collection.md
@docs/l2-runbooks/15-ios-ade-token-profile.md
@docs/l2-runbooks/17-ios-compliance-ca-timing.md

<interfaces>
<!-- macOS ADE Quick Reference structural reference (docs/quick-ref-l2.md:132-178) -->

macOS section structure (verified):
- Line 130: --- (horizontal rule)
- Line 132: ## macOS ADE Quick Reference
- Line 134: **Platform:** line
- Line 136: ### macOS Log Collection (with bash code block for IntuneMacODC)
- Line ~145: ### Key Terminal Commands (with bash code block)
- Line ~155: ### Critical Log Paths (table)
- Line 171: Full reference note + link
- Line 173: ### macOS Investigation Runbooks
- Line 175-178: 4 runbook bullets
- Line 180: ## Version History

iOS section DIVERGES structurally:
- NO bash code block (iOS has no CLI tool per D-31)
- Opens with "no CLI" callout blockquote
- Has 3 TABLES instead of Log Collection + Terminal Commands + Log Paths layout
- Ends with Investigation Runbooks list (4 bullets: l2-runbooks/14-17)

iOS L2 runbook filenames (Phase 31 deliverables):
- l2-runbooks/14-ios-log-collection.md
- l2-runbooks/15-ios-ade-token-profile.md
- l2-runbooks/16-ios-app-install.md
- l2-runbooks/17-ios-compliance-ca-timing.md

Research flag references (D-32 MANDATORY):
- Phase 30 D-32: Intune admin center UI path research flag (portal paths may have shifted)
- Phase 31 D-30: sysdiagnose trigger verification against Apple Developer docs
- Phase 31 D-31: MDM diagnostic report UI path verification

Target anchor: `#iosipados-quick-reference` (matches Plan 06 anchor — same slug; different file so no collision)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Frontmatter + platform coverage blockquote update</name>
  <files>docs/quick-ref-l2.md</files>
  <read_first>
    - docs/quick-ref-l2.md lines 1-15 (frontmatter + blockquote)
    - .planning/phases/32-navigation-integration-references/32-CONTEXT.md (D-33, D-41)
  </read_first>
  <behavior>
    - Frontmatter last_verified/review_by bumped.
    - Line 9 platform coverage blockquote suffixed per D-41.
  </behavior>
  <action>

**Edit 1 — Frontmatter bump:**
- `last_verified:` → `2026-04-17`
- `review_by:` → `2026-07-16`

**Edit 2 — Platform coverage blockquote (line 9):**

Locate the `> **Platform coverage:**` blockquote near the top. Update to D-41 canonical suffix (preserving existing structure):
```
> **Platform coverage:** This quick-reference covers Windows Autopilot (classic/APv1 and Device Preparation/APv2), macOS ADE, and iOS/iPadOS desktop-engineering investigation references.
```
  </action>
  <verify>
    <automated>grep -c "and iOS/iPadOS" docs/quick-ref-l2.md | grep -qE '^[1-9][0-9]*$' && grep "last_verified: 2026-04-17" docs/quick-ref-l2.md</automated>
  </verify>
  <done>Frontmatter shows 2026-04-17; platform coverage blockquote contains "and iOS/iPadOS".</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append ## iOS/iPadOS Quick Reference with 3 tables + research-flag footnotes + Investigation Runbooks</name>
  <files>docs/quick-ref-l2.md</files>
  <read_first>
    - docs/quick-ref-l2.md lines 170-190 (end of macOS section + Version History)
    - .planning/phases/32-navigation-integration-references/32-CONTEXT.md (D-31, D-32, D-40)
    - .planning/phases/32-navigation-integration-references/32-RESEARCH.md §Pitfall 4 (DDM/silent install distinction), §Pitfall 5 (sysdiagnose modern unified trigger), §Open Q4 (trigger table structure)
    - .planning/phases/32-navigation-integration-references/32-PATTERNS.md §6 (quick-ref-l2.md) structural divergence from macOS
    - docs/l2-runbooks/14-ios-log-collection.md (verify §Section 3 anchor exists for sysdiagnose footnote)
  </read_first>
  <behavior>
    - New `## iOS/iPadOS Quick Reference` H2 appended between macOS section end (line 178) and Version History H2 (line 180).
    - Preceded by `---` horizontal rule separator.
    - Opens with "no CLI diagnostic tool" callout blockquote per D-31.
    - 3 tables, each followed by an italic research-flag footnote per D-32.
    - Sysdiagnose table has 4 rows: 1 "Modern iOS 15+ unified" row + 3 legacy per-device-type rows per RESEARCH.md Open Q4.
    - Portal paths reference DDM enforcement correctly (NOT conflated with supervised-only silent install).
    - Investigation Runbooks list has 4 bullets for L2 runbooks 14-17.
    - Version History row added per D-40.
  </behavior>
  <action>

Locate the line `## Version History` (currently around line 180). IMMEDIATELY BEFORE this H2, INSERT:

```markdown

---

## iOS/iPadOS Quick Reference

**Platform:** iOS/iPadOS through Microsoft Intune

> **Important:** iOS has NO CLI diagnostic tool. No equivalent to `mdmdiagnosticstool.exe` (Windows) or `profiles` / `log show` / `system_profiler` (macOS). Diagnostic data is fragmented across three tiered methods — see below.

### iOS Diagnostic Data Collection (3 methods)

| Method | Who Triggers | L2 Access Path | When to Use |
|--------|--------------|----------------|-------------|
| MDM diagnostic report | L2 (Intune admin center) | Intune > Devices > All devices > [device] > Collect diagnostics | Tier 1 -- always start here; low friction, device-scope state |
| Company Portal log upload | User (on device) | Microsoft Support ticket (indirect — support uploads to case) | Tier 2 -- when Tier 1 is insufficient and no Mac+cable is available |
| Mac+cable sysdiagnose | L2 + user (physical) | Direct `.tar.gz` extraction via macOS Console.app | Tier 3 -- when profile-delivery verbosity is required (ADE token / MDM channel investigation) |

*(Full method details: [iOS Log Collection Guide](l2-runbooks/14-ios-log-collection.md). Verify portal paths per Phase 30 D-32 / Phase 31 D-31 research flags at execution time — Intune admin center UI may have shifted since last plan-time verification 2026-04-17.)*

### Key Intune Portal Paths (iOS L2)

| Path | Purpose |
|------|---------|
| Devices > Enrollment > Apple > Enrollment program tokens > [token] > Profiles | ABM token sync status, enrollment profile assignment, device assignment to profile |
| Devices > Enrollment > Apple > MDM Push Certificate | APNs certificate status, expiry date, renewal Apple ID |
| Devices > Enrollment > Enrollment restrictions | Platform / ownership / count restrictions applied to enrollment |
| Apps > iOS/iPadOS apps | Managed app status, VPP licensing state (device-licensed vs user-licensed), per-device install state |
| Devices > [device] > Device compliance | Per-device compliance policy evaluation state, timestamps, and non-compliant settings |

*(Verify paths per Phase 30 D-32 research flag — Microsoft Learn (verified 2026-04-17) confirms current; re-verify before content lock-in as Intune admin center reorganizes without deprecation notice.)*

### Sysdiagnose Trigger Reference (iOS/iPadOS)

| Device / OS | Trigger Combination |
|-------------|---------------------|
| **Modern iOS / iPadOS 15+** (unified trigger, all current devices) | Press and release both volume buttons + Side/Top button simultaneously (hold for ~250ms) |
| iPhone 8 / SE (1st+2nd gen) / iPad with Touch ID (legacy pre-iOS 15) | Both volume buttons + Sleep/Wake (Side) button |
| iPhone X and later iPhones (legacy pre-iOS 15) | Both volume buttons + Side button |
| iPad with Face ID (legacy pre-iOS 15) | Top button + either volume button |

*(Full procedure: [iOS Log Collection §Section 3](l2-runbooks/14-ios-log-collection.md#section-3-mac-cable-sysdiagnose). Modern unified trigger verified against Apple Developer forums 2026-04-17; legacy per-device-type triggers apply to pre-iOS-15 devices (increasingly rare in managed fleets). Verify triggers per Phase 31 D-30 research flag at execution time.)*

### iOS Investigation Runbooks

- [iOS Log Collection Guide](l2-runbooks/14-ios-log-collection.md) -- prerequisite for all iOS L2 investigations
- [ADE Token & Profile Delivery Investigation](l2-runbooks/15-ios-ade-token-profile.md) -- Pattern A-D failure analysis
- [App Install Failure Diagnosis](l2-runbooks/16-ios-app-install.md) -- three-class [CONFIG]/[TIMING]/[DEFECT] disambiguation
- [Compliance & CA Timing Investigation](l2-runbooks/17-ios-compliance-ca-timing.md) -- compliance axis + CA timing + Not-evaluated terminal state
```

**Critical content constraints:**

1. **Do NOT add "supervised-only" qualifier to any DDM-related row** (RESEARCH.md Pitfall 4). DDM managed software updates work on BOTH supervised and unsupervised iOS 17+ devices.

2. **Do NOT merge silent install and software update into a single rule.** These are distinct:
   - Silent VPP app install = supervised ADE only
   - DDM software update enforcement = works on both supervised and unsupervised

3. **Sysdiagnose table row ordering:** Modern unified first (most readers), then legacy per-device rows with explicit "(legacy pre-iOS 15)" marker.

4. **Research-flag footnotes are mandatory on all 3 tables** per D-32 — this is not discretionary. Each footnote in *italic* styling with Phase XX D-XX flag reference.

**Edit — Version History row:**

Locate the Version History table. INSERT a new row ABOVE the existing most-recent row:
```
| 2026-04-17 | Phase 32: added iOS/iPadOS Quick Reference section with 3-method diagnostic data collection table, Intune portal paths table, sysdiagnose trigger reference table (modern + legacy), and 4-runbook investigation list; research-flag footnotes per D-32 | -- |
```
  </action>
  <verify>
    <automated>grep -c "^## iOS/iPadOS Quick Reference" docs/quick-ref-l2.md | grep -qE '^1$' && grep -c "NO CLI diagnostic tool" docs/quick-ref-l2.md | grep -qE '^1$' && awk '/^## iOS\/iPadOS Quick Reference/,/^## Version History/' docs/quick-ref-l2.md | grep -cE "^### (iOS Diagnostic Data Collection|Key Intune Portal Paths|Sysdiagnose Trigger Reference|iOS Investigation Runbooks)" | grep -qE '^4$' && grep -c "Phase 30 D-32\|Phase 31 D-30\|Phase 31 D-31" docs/quick-ref-l2.md | grep -qE '^[3-9]$|^[1-9][0-9]+$' && grep -cE "l2-runbooks/(14|15|16|17)-ios" docs/quick-ref-l2.md | grep -qE '^[4-9]$|^[1-9][0-9]+$' && test "$(awk '/^### Sysdiagnose Trigger Reference/,/^\*\(/' docs/quick-ref-l2.md | grep -c '^|')" -ge 4 && bash .planning/phases/32-navigation-integration-references/validation/link-check.sh docs/quick-ref-l2.md && bash .planning/phases/32-navigation-integration-references/validation/anchor-collision.sh docs/quick-ref-l2.md</automated>
  </verify>
  <done>`## iOS/iPadOS Quick Reference` exists exactly once; "NO CLI diagnostic tool" callout present; 3 table subsections + Investigation Runbooks subsection present (4 H3s total); ≥3 research-flag footnote references (one per table); all 4 L2 runbook filenames referenced; Sysdiagnose table has ≥4 data rows including modern unified row; link-check.sh + anchor-collision.sh exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portal path string → L2 engineer's navigation | Engineer copies portal path; if stale, time wasted on dead UI |
| sysdiagnose trigger combo → physical device operation | User/L2 attempts trigger; wrong combo on wrong device = no data |
| DDM / silent install capability claim → admin configuration | Admin misapplies supervision rule based on quick-ref → tenant misconfig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | Portal paths + sysdiagnose triggers | mitigate | All portal paths use bracketed placeholders (`[token]`, `[device]`). No tenant IDs. No example serials. Sysdiagnose triggers are Apple-device hardware patterns, not tenant-specific. |
| T-32-02 | Information Disclosure | Stale portal path UI strings | mitigate | D-32 research-flag footnote on EACH of the 3 tables referencing parent flags (Phase 30 D-32 / Phase 31 D-30/D-31). Plan-time verification documented (2026-04-17 Microsoft Learn + Apple Developer). Planner/executor re-verifies at execution time per MANDATORY constraint. |
| T-32-03 | Repudiation (admin-facing) | DDM vs silent install supervision rule | mitigate | Explicitly separated per RESEARCH.md Pitfall 4: DDM software update rows do NOT carry supervised-only qualifier; VPP silent install rows DO. No sweeping "supervised-only for iOS" language. Portal Paths table row for Apps explicitly calls out "VPP licensing state (device-licensed vs user-licensed)" — forces reader to consider licensing + supervision distinctly. |
</threat_model>

<verification>
- `## iOS/iPadOS Quick Reference` H2 exists exactly once
- Opening "no CLI" callout blockquote present
- 3 data tables + 1 Investigation Runbooks subsection (4 H3s total inside the iOS H2)
- Each of the 3 tables has an immediately-following italic research-flag footnote referencing Phase 30/31 D-XX
- Sysdiagnose table contains ≥4 rows (1 modern unified + 3 legacy per-device)
- DDM-related rows do NOT say "supervised-only" (Pitfall 4 compliance)
- All 4 L2 runbook files referenced (14, 15, 16, 17)
- link-check.sh exits 0 (all referenced L2 runbooks exist per Phase 31 Wave completion)
- anchor-collision.sh exits 0 (no duplicate H3s within file)
</verification>

<success_criteria>
NAV-02 partial (quick-ref-l2.md scope):
1. iOS/iPadOS Quick Reference H2 appended
2. "No CLI diagnostic tool" callout per D-31
3. 3 tables: Diagnostic Data Collection / Intune Portal Paths / Sysdiagnose Trigger Reference
4. Research-flag footnote on each table per D-32 (MANDATORY)
5. Modern iOS 15+ unified sysdiagnose row added per RESEARCH.md Open Q4
6. DDM rows NOT conflated with silent install supervision rule per RESEARCH.md Pitfall 4
7. Investigation Runbooks list with 4 iOS L2 runbook bullets (runbooks 14-17)
8. Platform coverage blockquote updated per D-41
9. All pre-existing macOS content unchanged (regression per SC #4)
10. Anchor `#iosipados-quick-reference` produced (Plan 05 forward reference resolves)
</success_criteria>

<output>
After completion, create `.planning/phases/32-navigation-integration-references/32-07-SUMMARY.md` documenting:
- iOS Quick Reference section line count (~45-55 per D-31 target)
- 3 tables with row counts (Diagnostic: 3; Portal Paths: 4-5; Sysdiagnose: 4)
- Research-flag footnote verbatim text for each table (verify Phase XX D-XX references)
- DDM/silent install distinction preserved (Pitfall 4 audit)
- link-check.sh + anchor-collision.sh exit codes
</output>
