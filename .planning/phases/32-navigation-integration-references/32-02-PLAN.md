---
phase: 32
plan: "02"
type: execute
wave: 1
depends_on: ["32-00"]
files_modified:
  - docs/reference/ios-capability-matrix.md
  - docs/reference/00-index.md
  - docs/index.md
autonomous: true
requirements: [NAV-03]
tags: [capability-matrix, ios, reference, trilateral, nav-03]
must_haves:
  truths:
    - "An admin can answer 'can iOS do X' by scanning one trilateral matrix without reading three platform guides"
    - "The matrix has exactly 3 columns (Windows | macOS | iOS) matching SC #3 literal 'across iOS, macOS, and Windows'"
    - "5 domains (Enrollment, Configuration, App Deployment, Compliance, Software Updates) mirror macos-capability-matrix.md structure"
    - "DDM software updates row correctly notes supervised AND unsupervised (not conflated with supervised-only silent install) per RESEARCH.md Pitfall 4"
    - "Key Gaps Summary enumerates 7-10 iOS-specific gaps relative to Windows and macOS"
    - "Apple-parity framing preamble (2-4 sentences) opens the matrix"
    - "Matrix is reachable from docs/index.md Cross-Platform References AND docs/reference/00-index.md iOS References section"
  artifacts:
    - path: "docs/reference/ios-capability-matrix.md"
      provides: "Trilateral Intune capability matrix (Windows | macOS | iOS)"
      contains: "Windows | macOS | iOS"
      min_lines: 100
      exports: ["## Enrollment", "## Configuration", "## App Deployment", "## Compliance", "## Software Updates", "## Key Gaps Summary"]
    - path: "docs/reference/00-index.md"
      provides: "New ## iOS References H2 section listing the capability matrix"
      contains: "ios-capability-matrix.md"
    - path: "docs/index.md"
      provides: "Cross-Platform References table gains 2 new rows (iOS Enrollment Path Overview + iOS Capability Matrix)"
      contains: "ios-capability-matrix.md"
  key_links:
    - from: "docs/index.md Cross-Platform References table"
      to: "docs/reference/ios-capability-matrix.md"
      via: "table row link"
      pattern: "reference/ios-capability-matrix.md"
    - from: "docs/reference/00-index.md iOS References section"
      to: "docs/reference/ios-capability-matrix.md"
      via: "bullet list link"
      pattern: "ios-capability-matrix.md"
    - from: "docs/reference/ios-capability-matrix.md See Also"
      to: "docs/reference/macos-capability-matrix.md, docs/windows-vs-macos.md, docs/admin-setup-ios/00-overview.md"
      via: "See Also footer"
      pattern: "macos-capability-matrix.md"
---

<objective>
Create the NEW file `docs/reference/ios-capability-matrix.md` as a structural clone of `docs/reference/macos-capability-matrix.md` (101 lines, 5 domains + Key Gaps Summary + See Also) adapted to a trilateral matrix (Windows | macOS | iOS) per D-02. Add iOS-specific rows per D-05 across all 5 domains. Open with a 2-4 sentence Apple-parity framing preamble per D-06. Wire the new file into `docs/index.md` Cross-Platform References table (2 new rows per D-27) and `docs/reference/00-index.md` via a NEW `## iOS References` H2 (per D-08).

Purpose: NAV-03 requirement. Phase 32 SC #3 literal: "The iOS capability matrix (`reference/ios-capability-matrix.md`) documents feature parity gaps across iOS, macOS, and Windows in a scannable table format." Filename is LOCKED by the roadmap; cannot rename, cannot merge into macOS matrix. Wired into existing navigation so depth ≤ 2 from `docs/index.md`.

Output: (a) new 100-130 line capability matrix file at `docs/reference/ios-capability-matrix.md`; (b) additive edits to `docs/index.md` (2 Cross-Platform References rows) and `docs/reference/00-index.md` (new iOS References H2 with matrix entry); (c) all pre-existing links preserved (D-38 additive-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-navigation-integration-references/32-CONTEXT.md
@.planning/phases/32-navigation-integration-references/32-RESEARCH.md
@.planning/phases/32-navigation-integration-references/32-PATTERNS.md
@docs/reference/macos-capability-matrix.md
@docs/reference/00-index.md
@docs/index.md
@docs/admin-setup-ios/05-app-deployment.md
@docs/admin-setup-ios/06-compliance-policy.md
@docs/ios-lifecycle/00-enrollment-overview.md
@docs/ios-lifecycle/01-ade-lifecycle.md
@docs/windows-vs-macos.md

<interfaces>
<!-- Structural template: macos-capability-matrix.md (101 lines) -->
<!-- Executor clones this structure and adds third column + iOS rows + Apple-parity preamble -->

From docs/reference/macos-capability-matrix.md (structural template):
- Lines 1-7: frontmatter (last_verified, review_by, applies_to: both, audience: admin, platform: all)
- Line 9: H1 `# Intune: macOS vs Windows Capability Matrix`
- Line 11: single-sentence intro
- Lines 13-26: ## Enrollment (2-column table)
- Lines 28-41: ## Configuration
- Lines 43-56: ## App Deployment
- Lines 58-71: ## Compliance
- Lines 73-76: ## Software Updates
- Lines 78-88: ## Key Gaps Summary (7 numbered gaps)
- Lines 90-94: ## See Also (4 bullet links)
- Lines 96-101: ## Version History (single initial entry)

From docs/index.md Cross-Platform References table (line 131-144):
- `| [macOS Capability Matrix](reference/macos-capability-matrix.md) | Intune feature parity comparison between Windows and macOS ... |`
- iOS rows inserted after this row per D-27 (bottom of table)

From docs/reference/00-index.md:
- Line 19: `## macOS References` H2
- Lines 21-23: 3 bullets (macOS Terminal Commands, macOS Log Paths, macOS Capability Matrix)
- Line 25: `## Infrastructure Prerequisites` H2
- iOS References H2 inserted between lines 23 and 25 per D-08
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/reference/ios-capability-matrix.md (trilateral matrix, 5 domains + Key Gaps)</name>
  <files>docs/reference/ios-capability-matrix.md</files>
  <read_first>
    - docs/reference/macos-capability-matrix.md (ALL 101 lines — structural template)
    - .planning/phases/32-navigation-integration-references/32-CONTEXT.md (D-01 through D-10)
    - .planning/phases/32-navigation-integration-references/32-RESEARCH.md §Code Examples Example 3 (Enrollment table verbatim) and §Pitfall 4 (DDM / silent install distinction)
    - .planning/phases/32-navigation-integration-references/32-PATTERNS.md §1 (ios-capability-matrix.md)
    - docs/_glossary-macos.md (for anchors used in See Also — anchor names stabilized by Plan 01)
  </read_first>
  <behavior>
    - File exists at `docs/reference/ios-capability-matrix.md` with frontmatter `platform: all, audience: admin, applies_to: both, last_verified: 2026-04-17, review_by: 2026-07-16`.
    - H1: `# Intune: iOS/iPadOS Capability Matrix — Windows, macOS, iOS`.
    - 5 H2 domain sections, each with a `| Feature | Windows | macOS | iOS |` trilateral header.
    - Every domain has AT LEAST the rows from macos-capability-matrix.md (with iOS cell added) PLUS iOS-specific rows per D-05.
    - `## Key Gaps Summary` H2 with 7-10 numbered gaps.
    - `## See Also` footer with links to sibling docs.
    - `## Version History` H2 with single initial entry.
    - 100-130 total lines per D-10.
    - DDM software update row does NOT say "supervised only" (RESEARCH.md Pitfall 4 compliance).
    - Silent app install row explicitly says "🔒 supervised ADE only for VPP device-licensed apps".
  </behavior>
  <action>
Create the file `docs/reference/ios-capability-matrix.md` with this exact structure. You may refine prose wording for clarity but you MUST preserve: trilateral column header, all 5 domain H2s, DDM/silent-install distinction, 7+ Key Gaps entries, Apple-parity preamble, See Also links.

```markdown
---
last_verified: 2026-04-17
review_by: 2026-07-16
applies_to: both
audience: admin
platform: all
---

# Intune: iOS/iPadOS Capability Matrix — Windows, macOS, iOS

This matrix compares Intune management capabilities across three platforms. Apple-platform readers (admins coming from macOS ADE) will find the iOS↔macOS comparison surfaces the most meaningful differences — DDM maturity, supervision model scope, and VPP licensing behavior. Windows readers should treat iOS and macOS as structurally distinct from Windows despite being managed via the same Intune tenant. For the Windows↔macOS-only view, see the [macOS Capability Matrix](macos-capability-matrix.md). For concept-level terminology comparison, see [Windows vs macOS Concept Comparison](../windows-vs-macos.md).

## Enrollment

| Feature | Windows | macOS | iOS |
|---------|---------|-------|-----|
| Zero-touch enrollment method | Autopilot (hardware hash to Intune) | ADE via ABM (serial number to ABM) | ADE via ABM (serial number to ABM) |
| Hardware identity | 4KB hardware hash | Serial number | Serial number |
| Enrollment types | User-driven, Pre-provisioning, Self-deploying, Hybrid Entra join | ADE with user affinity, ADE without user affinity | ADE (user affinity / userless), Device Enrollment (Company Portal / web-based), [Account-Driven User Enrollment](../_glossary-macos.md#account-driven-user-enrollment) (BYOD iOS 15+), [MAM-WE](../_glossary-macos.md#mam-we) (app-layer, no device enrollment) |
| Supervision state | N/A | Supervised via ADE | Supervised (ADE) / Unsupervised (Device Enrollment, User Enrollment) |
| Supervised-only capability gates | N/A | Limited (select configuration categories) | Extensive — silent VPP app install, home screen layout, restriction profiles (AirDrop/Camera/etc.), DDM app install |
| Pre-provisioning (White Glove) | Yes (APv1 only) | No | No |
| Self-deploying / kiosk enrollment | Yes (APv1) | Partially (ADE without user affinity) | Partially (ADE without user affinity / Shared iPad — see SIPAD-01 future milestone) |
| Hybrid domain join | Yes (APv1 + Intune Connector) | No | No |
| ESP / Await Configuration equivalent | ESP (device phase + user phase, itemized) | Await Configuration (single lock, generic progress) | Setup Assistant waits (no ESP-equivalent; DDM status channel provides post-enrollment progress on iOS 17+) |
| ABM token shared with macOS | N/A | Yes | Yes (same ABM token covers iOS, iPadOS, macOS, tvOS enrollment) |
| Re-enrollment blocking screen | Yes (every enrollment) | No (first enrollment only) | No (first enrollment only) |
| Dynamic enrollment groups | Yes (ZTDId attribute) | Yes (enrollmentProfileName attribute) | Yes (enrollmentProfileName attribute) |

## Configuration

| Feature | Windows | macOS | iOS |
|---------|---------|-------|-----|
| Declarative Device Management (DDM) | No | Yes (macOS 14+) | Yes (iOS 17+) |
| DDM managed software update enforcement | N/A | Yes (unsupervised OK) | Yes (unsupervised OK — works on both supervised AND unsupervised iOS 17+ devices) |
| Configuration profile mechanism | Intune configuration profiles, Settings Catalog, ADMX templates | Configuration profiles (.mobileconfig) | Configuration profiles (.mobileconfig) |
| Home screen layout control | Limited (Start menu via Windows Shell) | No | Yes (🔒 supervised ADE only) |
| Restriction profiles scope | Policy CSP + MDM restrictions | Limited (select macOS restrictions) | Extensive — AirDrop, Camera, Safari, Siri, App Store, screen recording (🔒 many are supervised-only) |
| Wi-Fi auto-join enforcement | Yes | Yes | Yes (🔒 supervised ADE only for forced auto-join; unsupervised users can disable) |
| Per-app VPN | Yes | Yes | Yes (🔒 supervised ADE only for transparent per-app VPN) |
| Certificate deployment | SCEP, PKCS | SCEP, PKCS, ACME (macOS 14+) | SCEP, PKCS, ACME (iOS 16+) |
| Security baselines | Yes (curated baseline sets) | No | No |

## App Deployment

| Feature | Windows | macOS | iOS |
|---------|---------|-------|-----|
| App packaging formats | Win32 (.intunewin), MSI, MSIX, Microsoft Store | PKG, DMG, Script-based, Microsoft Store | VPP App Store apps, LOB/IPA apps |
| Silent app install | Yes | Yes | Yes (🔒 supervised ADE only for VPP device-licensed; unsupervised requires user confirmation per app) |
| VPP device-licensed vs user-licensed | N/A | Both supported | Both supported — device-licensed preferred for ADE corporate; user-licensed for 1:1 education / personal devices |
| LOB / sideloaded apps | Yes (Win32 wrapper) | Yes (PKG/DMG) | Yes (IPA via Intune, requires enterprise distribution cert OR ABM custom apps) |
| App dependency / install order | Yes | No | No |
| App supersedence (version replace) | Yes | No | No |
| Managed app status reporting | Yes (Intune app deployment status) | Yes | Yes (per-app install state visible in Intune > Apps > iOS/iPadOS apps > [app] > Device install status) |
| Apps removed on retirement | Yes | No (explicit uninstall required) | Yes (managed apps removed on unenrollment / selective wipe) |

## Compliance

| Feature | Windows | macOS | iOS |
|---------|---------|-------|-----|
| OS version gate | Yes | Yes | Yes |
| Passcode / password complexity | Yes | Yes | Yes |
| Disk encryption check | Yes (BitLocker) | Yes (FileVault) | Yes (device-level always-on on iOS — no toggle) |
| Jailbreak / root detection | N/A | Partial (T2 chip attestation) | Yes (iOS-specific check; evaluated at sync intervals, not real-time) |
| TPM attestation | Yes | N/A | N/A |
| Default compliance posture (newly enrolled, not-yet-evaluated) | "Not evaluated" (CA grace period applies) | "Not evaluated" | "Not evaluated" (see [iOS Compliance Policy Guide](../admin-setup-ios/06-compliance-policy.md) for CA timing behavior) |
| Userless device compliance | Yes (Autopilot self-deploying) | No (requires user affinity) | Partial (ADE without user affinity works; Account-Driven UE requires user) |
| Actions for Noncompliance | Yes | Yes | Yes (email, push notification, retire after N days) |

## Software Updates

| Feature | Windows | macOS | iOS |
|---------|---------|-------|-----|
| Update ring management | Yes (Windows Update for Business) | Yes (DDM managed software update on macOS 14+) | Yes (DDM managed software update on iOS 17+ — works supervised AND unsupervised) |
| Update deferral — duration | Up to 365 days (feature), 30 days (quality) | Up to 90 days via DDM | Up to 90 days via DDM (iOS 17+) |
| Legacy update deferral path | N/A | Restrictions profile (pre-DDM) | Restrictions profile (pre-iOS 17 — deprecated, DDM preferred) |
| Emergency / critical update override | Yes (Expedited Windows Quality Updates) | Yes (DDM emergency update) | Yes (DDM emergency update on iOS 17+) |
| Update status reporting | Windows Update report | DDM status channel (macOS 14+) | DDM status channel (iOS 17+); polling on older |

## Key Gaps Summary

The most significant iOS capability gaps relative to Windows and macOS are:

1. **No CLI diagnostic access** — iOS has no equivalent to Windows `mdmdiagnosticstool.exe` or macOS `profiles` / `log show` / `system_profiler`. L2 relies on MDM diagnostic report, Company Portal log upload, or Mac+cable sysdiagnose (see [iOS L2 Log Collection Guide](../l2-runbooks/14-ios-log-collection.md)).
2. **Supervision gates a significant capability subset** — silent app install, home screen control, DDM app install, and most restriction profiles are supervised-only. Supervision is set at ADE enrollment and CANNOT be added retroactively without a full device erase.
3. **No hybrid domain join** — iOS has no concept equivalent to Entra Hybrid Join; authentication is Entra-only.
4. **No registry / plist-equivalent admin inspection** — unlike macOS Terminal (`profiles show`) or Windows regedit, iOS admins cannot directly inspect device state; diagnostics come through Intune admin center reports only.
5. **No pre-provisioning / White Glove equivalent** — iOS has no staging path for IT to pre-configure devices before user delivery; ADE enrollment happens at user's first Setup Assistant.
6. **Jailbreak detection is iOS-only** — represents a unique compliance surface; no Windows or macOS equivalent (TPM attestation addresses a different threat model).
7. **No app dependency or install order control** — like macOS, iOS lacks Intune's Windows Win32 dependency graph feature. Complex deployments must sequence via separate compliance-gated assignments.
8. **Account-Driven User Enrollment privacy limits** — on BYOD devices enrolled via Account-Driven UE, IT cannot see personal apps, photos, iCloud content, or device identifiers beyond the work profile. This is by design (Apple-enforced) but creates support-investigation limitations.

## See Also

- [macOS Capability Matrix](macos-capability-matrix.md) — Windows↔macOS bilateral view (sibling document)
- [Windows vs macOS Concept Comparison](../windows-vs-macos.md) — terminology mapping across platforms
- [iOS Enrollment Path Overview](../ios-lifecycle/00-enrollment-overview.md) — 4-path enrollment comparison (ADE, Device, User, MAM-WE)
- [iOS Admin Setup Overview](../admin-setup-ios/00-overview.md) — entry point for all iOS admin guides
- [APv1 vs APv2 Comparison](../apv1-vs-apv2.md) — Windows framework comparison

---

## Version History

| Date | Change | Author |
|------|--------|--------|
| 2026-04-17 | Initial version — Phase 32: trilateral capability matrix (Windows, macOS, iOS) across 5 domains with Apple-parity framing preamble and 8-gap Key Gaps Summary | -- |
```

Verify line count falls within 100-130 lines (D-10 target).
  </action>
  <verify>
    <automated>test -f docs/reference/ios-capability-matrix.md && grep -cE "^\| Feature \| Windows \| macOS \| iOS \|" docs/reference/ios-capability-matrix.md | grep -qE '^5$' && grep -c "^## " docs/reference/ios-capability-matrix.md | grep -qE '^[7-9]$' && grep -c "supervised AND unsupervised" docs/reference/ios-capability-matrix.md | grep -qE '^[1-9][0-9]*$' && grep -c "supervised ADE only" docs/reference/ios-capability-matrix.md | grep -qE '^[1-9][0-9]*$' && bash .planning/phases/32-navigation-integration-references/validation/link-check.sh docs/reference/ios-capability-matrix.md && wc -l docs/reference/ios-capability-matrix.md | awk '{if ($1 >= 95 && $1 <= 140) exit 0; else exit 1}'</automated>
  </verify>
  <done>File exists; 5 trilateral table headers; ≥7 H2s (5 domains + Key Gaps + See Also + Version History = 8); DDM row says "supervised AND unsupervised"; silent install row says "supervised ADE only"; link-check.sh exits 0; line count 95-140.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire matrix into docs/reference/00-index.md (new ## iOS References H2) + docs/index.md Cross-Platform References</name>
  <files>docs/reference/00-index.md, docs/index.md</files>
  <read_first>
    - docs/reference/00-index.md ALL lines (current state — verified above)
    - docs/index.md lines 127-144 (Cross-Platform References table)
    - docs/index.md lines 1-20 (frontmatter + Choose Your Platform selector)
    - .planning/phases/32-navigation-integration-references/32-CONTEXT.md (D-08, D-27, D-28, D-41)
    - .planning/phases/32-navigation-integration-references/32-PATTERNS.md §4 (index.md) and §7 (reference/00-index.md)
  </read_first>
  <behavior>
    - `docs/reference/00-index.md` has a new `## iOS References` H2 inserted BETWEEN existing `## macOS References` H2 (line 19) and `## Infrastructure Prerequisites` H2 (line 25), containing at minimum the iOS Capability Matrix entry.
    - `docs/index.md` Cross-Platform References table has 2 new rows appended AFTER the macOS Capability Matrix row (line 144): iOS Enrollment Path Overview + iOS Capability Matrix.
    - `docs/index.md` platform coverage blockquote (line 9) suffixed with "and iOS/iPadOS" per D-41.
    - Both files have frontmatter `last_verified` + `review_by` bumped and a new Version History row.
  </behavior>
  <action>

**Edit 1 — `docs/reference/00-index.md`:**

Bump frontmatter lines 2-3:
- `last_verified: 2026-04-13` → `last_verified: 2026-04-17`
- `review_by: 2026-07-12` → `review_by: 2026-07-16`

AFTER line 23 (end of macOS References section — `- [macOS Capability Matrix](macos-capability-matrix.md) — ...`), BEFORE line 25 (`## Infrastructure Prerequisites`), INSERT:

```markdown

## iOS References

- [iOS Capability Matrix](ios-capability-matrix.md) — Intune feature parity comparison across Windows, macOS, and iOS (NAV-03)
```

Append new Version History row ABOVE the existing `2026-04-14 | Added macOS Capability Matrix...` row:
```markdown
| 2026-04-17 | Added iOS References section with iOS Capability Matrix |
```

**Edit 2 — `docs/index.md`:**

Bump frontmatter `last_verified` + `review_by` (matching Phase 32 ship date). Use `2026-04-17` + `2026-07-16`.

Locate the platform coverage blockquote around line 9. Current state:
```
> **Platform coverage:** This index covers Windows Autopilot (classic/APv1 and Device Preparation/APv2) and macOS ADE provisioning.
```
Replace with:
```
> **Platform coverage:** This index covers Windows Autopilot (classic/APv1 and Device Preparation/APv2), macOS ADE, and iOS/iPadOS provisioning.
```

Locate the Cross-Platform References table at lines 129-144. After the line `| [macOS Capability Matrix](reference/macos-capability-matrix.md) | ... |` (line 144), APPEND two new rows (these are the only edits to the Cross-Platform References table from Plan 02 — Plan 05 will add the iOS Provisioning H2 separately; do NOT add that here):

```markdown
| [iOS Enrollment Path Overview](ios-lifecycle/00-enrollment-overview.md) | 4-path comparison with supervision axis (ADE, Device Enrollment, Account-Driven User Enrollment, MAM-WE) |
| [iOS Capability Matrix](reference/ios-capability-matrix.md) | Intune feature parity comparison across Windows, macOS, and iOS (NAV-03) |
```

Add new Version History row ABOVE the existing `2026-04-15` row:
```markdown
| 2026-04-17 | Phase 32: added iOS Capability Matrix + iOS Enrollment Path Overview Cross-Platform References entries; platform coverage updated for trilateral framing | -- |
```

Do NOT add the `## iOS/iPadOS Provisioning` H2 section in this task — that is Plan 05's responsibility. This task ONLY touches the Cross-Platform References table, the platform coverage blockquote, the frontmatter, and the Version History.
  </action>
  <verify>
    <automated>grep -c "^## iOS References" docs/reference/00-index.md | grep -qE '^1$' && grep -c "ios-capability-matrix.md" docs/reference/00-index.md | grep -qE '^1$' && grep -c "ios-capability-matrix.md" docs/index.md | grep -qE '^1$' && grep -c "ios-lifecycle/00-enrollment-overview.md" docs/index.md | grep -qE '^[1-9][0-9]*$' && grep -c "and iOS/iPadOS" docs/index.md | grep -qE '^[1-9][0-9]*$' && bash .planning/phases/32-navigation-integration-references/validation/link-check.sh docs/reference/00-index.md && bash .planning/phases/32-navigation-integration-references/validation/link-check.sh docs/index.md</automated>
  </verify>
  <done>`## iOS References` H2 present in 00-index.md exactly once; ios-capability-matrix.md referenced in both files; iOS Enrollment Path Overview referenced in index.md; platform coverage blockquote updated; link-check.sh exits 0 on both files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capability matrix content → admin's operational decision | Admin reads matrix row and applies a management rule to tenant; incorrect rows can cause tenant misconfiguration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | Example portal paths in matrix rows | mitigate | Matrix contains no portal paths (portal paths are quick-ref-l2 tier content per RESEARCH.md Architectural Responsibility Map). Rows describe capabilities abstractly. No tenant IDs, no example URLs. |
| T-32-03 | Repudiation (admin-facing) | DDM row + silent install row + supervision gate rows | mitigate | RESEARCH.md Pitfall 4 verified — DDM row explicitly says "works supervised AND unsupervised", NOT conflated with silent install. Silent install row explicitly qualifies with "VPP device-licensed apps" + "🔒 supervised ADE only". Each row's supervision dependency is disambiguated per-row per D-04. |
</threat_model>

<verification>
- `test -f docs/reference/ios-capability-matrix.md` passes
- `grep -c "^## " docs/reference/ios-capability-matrix.md` ≥ 7 (5 domains + Key Gaps + See Also + Version History)
- `grep -cE "^\| Feature \| Windows \| macOS \| iOS \|" docs/reference/ios-capability-matrix.md` == 5
- `wc -l docs/reference/ios-capability-matrix.md` in range [95, 140]
- `grep -c "^## iOS References" docs/reference/00-index.md` == 1
- `grep -c "ios-capability-matrix.md" docs/index.md` ≥ 1
- `bash .../link-check.sh docs/reference/ios-capability-matrix.md docs/reference/00-index.md docs/index.md` exits 0
- `grep -c "supervised AND unsupervised" docs/reference/ios-capability-matrix.md` ≥ 1 (DDM distinction per Pitfall 4)
</verification>

<success_criteria>
NAV-03 satisfied per SC #3 literal:
1. File exists at literal path `docs/reference/ios-capability-matrix.md` (not merged into other file)
2. Trilateral structure: `| Feature | Windows | macOS | iOS |` header in all 5 domain tables
3. 5 domains mirror macOS matrix structure (Enrollment, Configuration, App Deployment, Compliance, Software Updates)
4. Apple-parity framing preamble (2-4 sentences) opens the matrix
5. Key Gaps Summary enumerates 7-10 iOS-specific gaps
6. DDM software update row correctly states supervised AND unsupervised (Pitfall 4 compliance)
7. VPP silent install row correctly qualified with "device-licensed" + "supervised ADE only"
8. Reachable from index.md Cross-Platform References (depth 1 from hub)
9. Reachable from reference/00-index.md iOS References (depth 2 from hub via reference index)
10. No pre-existing links broken in index.md or reference/00-index.md
</success_criteria>

<output>
After completion, create `.planning/phases/32-navigation-integration-references/32-02-SUMMARY.md` documenting:
- New file line count (confirm 95-140 range)
- 5 domain tables with row counts
- Key Gaps Summary gap count (7-10 per D-07)
- Wiring edits in 00-index.md and index.md
- link-check.sh exit codes for all 3 files
- Any deviations from the structural template (and reasoning)
</output>
