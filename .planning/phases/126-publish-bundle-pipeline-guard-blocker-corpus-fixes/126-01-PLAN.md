---
phase: 126-publish-bundle-pipeline-guard-blocker-corpus-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_glossary-android.md
  - docs/admin-setup-android/03-fully-managed-cobo.md
  - docs/admin-setup-android/04-byod-work-profile.md
  - docs/reference/android-capability-matrix.md
  - docs/android-lifecycle/03-android-version-matrix.md
  - scripts/pipeline/convert.ps1
autonomous: true
requirements: [HYG-02, HYG-03]

must_haves:
  truths:
    - "None of the 5 Approved docs that carried phase_46_wave2_retrofit still contain that frontmatter key"
    - "Each of the 5 formerly-stale-key docs, once converted, passes guard-docx.mjs CUSTOM-PROPS"
    - "The 9 DEFER-121-07-A files contain no literal YYYY-MM-DD placeholder (verify-only, expected no-op)"
    - "convert.ps1 leaves no orphaned 0-byte .tmp file in TEMP after a conversion"
    - "last_verified is unchanged in every touched file (no freshness-clock reset)"
  artifacts:
    - path: "docs/_glossary-android.md"
      provides: "HYG-02 stale-key removal (RE-179, DEFER-125-06-A)"
      contains: "doc_id: RE-179"
    - path: "docs/admin-setup-android/03-fully-managed-cobo.md"
      provides: "HYG-02 stale-key removal (same defect class)"
    - path: "docs/admin-setup-android/04-byod-work-profile.md"
      provides: "HYG-02 stale-key removal"
    - path: "docs/reference/android-capability-matrix.md"
      provides: "HYG-02 stale-key removal"
    - path: "docs/android-lifecycle/03-android-version-matrix.md"
      provides: "HYG-02 stale-key removal (also an HYG-03 verify target)"
    - path: "scripts/pipeline/convert.ps1"
      provides: ".tmp-leak fix at line 85; PIPE-03 logic untouched"
      contains: "Remove-Item -LiteralPath $rawTempFile"
  key_links:
    - from: "the 5 fixed docs"
      to: "guard-docx.mjs CUSTOM-PROPS"
      via: "phase_46_wave2_retrofit removed from the promoted custom-property set"
      pattern: "phase_46_wave2_retrofit"
---

<objective>
Fold in the two known guard blockers and fix the convert.ps1 temp-file leak so the full Approved
corpus can guard clean end-to-end in Plan 02 rather than tripping the PUB-02 fail-closed gate on
pre-existing debt.

This plan makes three surgical, reformat-only changes:
- HYG-02 (expanded per RESEARCH headline): delete the stale `phase_46_wave2_retrofit` frontmatter
  key from all **5** Approved docs that carry it (the requirement text named only the first file;
  RESEARCH proved 4 siblings carry the identical defect at the identical line 11, and PUB-02's
  fail-closed gate cannot pass until all 5 are fixed). Closes `DEFER-125-06-A`.
- HYG-03 (verify-only per D-08): assert the 9 named `DEFER-121-07-A` files carry no literal
  `YYYY-MM-DD` placeholder (already filled in commit `9031056`). NO corpus-wide date gate (D-09).
- convert.ps1 `.tmp`-leak fix: a minimal edit at line 85 that removes the orphaned 0-byte `.tmp`
  `GetTempFileName()` reserves per call, without disturbing the PIPE-03 temp-copy logic.

Purpose: unblock the batch guard; keep the corpus frozen except these guard-motivated edits (all
`last_verified` untouched — D-10).
Output: 5 cleaned frontmatter blocks, a leak-free convert.ps1, a verified-clean HYG-03 set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-publish-bundle-pipeline-guard-blocker-corpus-fixes/126-CONTEXT.md
@.planning/phases/126-publish-bundle-pipeline-guard-blocker-corpus-fixes/126-RESEARCH.md
@.planning/phases/126-publish-bundle-pipeline-guard-blocker-corpus-fixes/126-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HYG-02 — remove the stale phase_46_wave2_retrofit key from all 5 Approved docs</name>
  <files>docs/_glossary-android.md, docs/admin-setup-android/03-fully-managed-cobo.md, docs/admin-setup-android/04-byod-work-profile.md, docs/reference/android-capability-matrix.md, docs/android-lifecycle/03-android-version-matrix.md</files>
  <read_first>
    - .planning/phases/126-publish-bundle-pipeline-guard-blocker-corpus-fixes/126-PATTERNS.md (section "HYG-02: 5-file stale-key removal" — the exact line-11 shape + full frontmatter context)
    - docs/_glossary-android.md (lines 1-12 — the representative frontmatter block)
    - scripts/pipeline/guard-docx.mjs (lines 32-35 KNOWN_CUSTOM_PROPERTY_KEYS + the CUSTOM-PROPS check, lines 92-105 — proves why an out-of-set key fails)
  </read_first>
  <action>
    In each of the 5 files, delete the single frontmatter line `phase_46_wave2_retrofit: 2026-04-25`
    (line 11 in all 5, byte-identical). This is a reformat-only deletion per HYG-02 / D-10: change
    NOTHING else — do not touch `last_verified`, `review_by`, or any other key; do not reflow the
    frontmatter; do not alter body content. The key `phase_46_wave2_retrofit` is outside the known
    9-key EEE set in guard-docx.mjs, so pandoc promotes it to a Word custom property that trips the
    CUSTOM-PROPS check (the same `DEFER-125-06-A` failure logged for RE-179). All 5 must be fixed or
    Plan 02's fail-closed batch gate never produces a zip. Note in the SUMMARY that HYG-02's
    requirement text named only `docs/_glossary-android.md`; the other 4 are the same defect
    discovered during research (RESEARCH Pitfall 1).
  </action>
  <acceptance_criteria>
    - `grep -rn "phase_46_wave2_retrofit" docs/` returns ZERO matches across the whole corpus.
    - Each of the 5 files still contains its original `last_verified:` value (unchanged) — confirm via git diff that ONLY line 11 was removed in each file (each file's diff is exactly one deleted line, no other hunks).
    - `docs/_glossary-android.md` still contains `doc_id: RE-179`.
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -rc "phase_46_wave2_retrofit" docs/ | grep -v ':0$' | wc -l) -eq 0 && git diff --numstat docs/_glossary-android.md docs/admin-setup-android/03-fully-managed-cobo.md docs/admin-setup-android/04-byod-work-profile.md docs/reference/android-capability-matrix.md docs/android-lifecycle/03-android-version-matrix.md | awk '{if($1!=0||$2!=1) exit 1}'</automated>
  </verify>
  <done>All 5 files carry no phase_46_wave2_retrofit key; each git diff is exactly one deleted line (0 insertions, 1 deletion); last_verified untouched everywhere.</done>
</task>

<task type="auto">
  <name>Task 2: convert.ps1 .tmp-leak fix (surgical, PIPE-03 logic untouched)</name>
  <files>scripts/pipeline/convert.ps1</files>
  <read_first>
    - scripts/pipeline/convert.ps1 (lines 75-135 — the PIPE-03 preprocessing block; the leak is at line 85, the diff-guard/cleanup below it must NOT change)
    - .planning/phases/126-publish-bundle-pipeline-guard-blocker-corpus-fixes/126-PATTERNS.md (section "convert.ps1 (MODIFIED — .tmp-leak fix)" — exact before/after)
    - .planning/phases/126-publish-bundle-pipeline-guard-blocker-corpus-fixes/126-RESEARCH.md (Pattern 4 — root cause: GetTempFileName() reserves the .tmp on disk as a side effect)
  </read_first>
  <action>
    At line 85, replace the single-line `$tempMd = [System.IO.Path]::GetTempFileName() -replace '\.tmp$', '.md'`
    with the three-line form from PATTERNS.md: capture the raw temp file into `$rawTempFile`, derive
    `$tempMd` from it via the same `-replace '\.tmp$', '.md'`, then `Remove-Item -LiteralPath $rawTempFile -Force -ErrorAction SilentlyContinue`
    immediately (before the `Copy-Item`). Do NOT touch anything below line 86: the existing
    `Remove-Item $tempMd -Force` calls (the PIPE-03 fail-closed abort path ~line 130 and the
    post-conversion cleanup ~line 147) clean up the `.md`-suffixed copy and were never the leak
    source. Do not change the pandoc invocation, the version guard, or the diff-guard. SOURCE_DATE_EPOCH
    is deliberately NOT introduced (planner's discretion per CONTEXT — non-load-bearing; nothing
    downstream may key on docx bytes regardless).
  </action>
  <acceptance_criteria>
    - convert.ps1 contains the line `Remove-Item -LiteralPath $rawTempFile -Force -ErrorAction SilentlyContinue`.
    - A single `pwsh -File scripts/pipeline/convert.ps1 -InputMd <sample.md> -OutputDocx <tmp>.docx` run leaves the `*.tmp` count in `$env:TEMP` unchanged before vs after (no new orphan).
    - The sample conversion still succeeds and `node scripts/pipeline/guard-docx.mjs <tmp>.docx` exits 0 (PIPE-03 + conversion behavior intact).
    - git diff of convert.ps1 touches only the line-85 region (no hunks in the PIPE-03 guard/cleanup below).
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$b=(Get-ChildItem $env:TEMP -Filter '*.tmp' -ErrorAction SilentlyContinue).Count; & ./scripts/pipeline/convert.ps1 -InputMd docs/_glossary-android.md -OutputDocx ./.pipeline-output/_leaktest.docx | Out-Null; $a=(Get-ChildItem $env:TEMP -Filter '*.tmp' -ErrorAction SilentlyContinue).Count; if($a -ne $b){exit 1}; exit 0" && node scripts/pipeline/guard-docx.mjs ./.pipeline-output/_leaktest.docx</automated>
  </verify>
  <done>convert.ps1 no longer leaks a .tmp per call; a sample doc still converts and guards clean; PIPE-03 block byte-unchanged below line 86.</done>
</task>

<task type="auto">
  <name>Task 3: HYG-03 — verify the 9 DEFER-121-07-A files carry no YYYY-MM-DD placeholder (no rewrite)</name>
  <files>(verify-only — no files modified; reads docs/_glossary.md, docs/_glossary-linux.md, docs/ios-lifecycle/00-enrollment-overview.md, docs/android-lifecycle/00-enrollment-overview.md, docs/android-lifecycle/01-android-prerequisites.md, docs/android-lifecycle/02-provisioning-methods.md, docs/android-lifecycle/03-android-version-matrix.md, docs/linux-lifecycle/00-enrollment-overview.md, docs/linux-lifecycle/01-linux-prerequisites.md)</files>
  <read_first>
    - .planning/phases/126-publish-bundle-pipeline-guard-blocker-corpus-fixes/126-PATTERNS.md (section "HYG-03: 9-file verify-only pass" — the exact 9 paths + the verify-command pattern + the both-lists note on android-version-matrix)
    - .planning/phases/126-publish-bundle-pipeline-guard-blocker-corpus-fixes/126-CONTEXT.md (D-08 verify-only/idempotent, D-09 NO corpus-wide gate + the 4 legit-content carve-outs, D-10 last_verified untouched)
  </read_first>
  <action>
    Assert each of the 9 named files contains no literal `YYYY-MM-DD` (expected: 0 matches in all 9 —
    already filled in commit `9031056`, HYG-03 is a verify-only no-op per D-08). Do NOT add any
    corpus-wide `YYYY-MM-DD` acceptance gate (D-09) — a naive corpus grep would corrupt 4 legitimately-
    YYYY-MM-DD-bearing Approved docs: `docs/l2-runbooks/01-log-collection.md`,
    `docs/l2-runbooks/06-apv2-log-collection.md`, `docs/admin-setup-android/03-fully-managed-cobo.md`,
    `docs/admin-setup-android/08-cope-full-admin.md`. This check is scoped to the 9 named files ONLY.
    If (and only if) a gap is found in one of the 9, backfill that placeholder from the same file's
    own `last_verified` frontmatter value (D-08 option c) — never touch `last_verified` itself (D-10).
    Note `docs/android-lifecycle/03-android-version-matrix.md` also received the Task-1 HYG-02 edit;
    treat the date-verify here as an independent (expected no-op) check, not a conflict.
  </action>
  <acceptance_criteria>
    - `grep -c "YYYY-MM-DD"` returns 0 for each of the 9 named files.
    - The 4 legit-content carve-out files still CONTAIN their literal `YYYY-MM-DD` occurrences (proof no corpus-wide gate was applied): each returns a non-zero count, unchanged.
    - No file was modified by this task unless a real gap was found and backfilled from last_verified (SUMMARY records whether it was a pure no-op — expected — or which file needed a backfill).
  </acceptance_criteria>
  <verify>
    <automated>for f in docs/_glossary.md docs/_glossary-linux.md docs/ios-lifecycle/00-enrollment-overview.md docs/android-lifecycle/00-enrollment-overview.md docs/android-lifecycle/01-android-prerequisites.md docs/android-lifecycle/02-provisioning-methods.md docs/android-lifecycle/03-android-version-matrix.md docs/linux-lifecycle/00-enrollment-overview.md docs/linux-lifecycle/01-linux-prerequisites.md; do if grep -q "YYYY-MM-DD" "$f"; then echo "GAP: $f"; exit 1; fi; done; grep -q "YYYY-MM-DD" docs/l2-runbooks/01-log-collection.md && grep -q "YYYY-MM-DD" docs/admin-setup-android/08-cope-full-admin.md</automated>
  </verify>
  <done>All 9 HYG-03 files verified clean (0 literal YYYY-MM-DD); the 4 legit-content carve-outs untouched and still bearing their format strings; DEFER-121-07-A confirmed closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git-tracked docs → frontmatter edit | Developer-controlled content; risk is an accidental over-broad edit (touching last_verified / other keys), not adversarial input |
| convert.ps1 → filesystem TEMP | Local temp-file lifecycle; risk is resource leak, not a security exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01-01 | Tampering | HYG-02 frontmatter edits | mitigate | Surgical single-line deletion only; git diff asserts exactly 1 deletion / 0 insertions per file; last_verified explicitly asserted unchanged |
| T-126-01-02 | Tampering | HYG-03 verify pass | mitigate | Verify-only scoped to 9 named files; D-09 corpus-wide gate explicitly forbidden; 4 legit-content carve-outs asserted still present |
| T-126-01-03 | Denial of Service | convert.ps1 TEMP leak | mitigate | Remove-Item of the reserved raw .tmp immediately after GetTempFileName(); before/after count asserted equal |
| T-126-01-SC | Tampering | package installs | accept | Zero new external packages introduced this phase; all tooling pre-existing and pinned — no legitimacy gate applies |
</threat_model>

<verification>
- `grep -rn "phase_46_wave2_retrofit" docs/` → 0 matches.
- convert.ps1 conversion of a sample doc leaves no orphaned `.tmp` and still guards clean.
- All 9 HYG-03 files carry no literal `YYYY-MM-DD`; the 4 carve-out files still do.
- No `last_verified` value changed in any touched file.
</verification>

<success_criteria>
- ROADMAP SC #3 satisfied: `_glossary-android.md` (and its 4 siblings) no longer carry the stale key; the 9 DEFER-121-07-A files have no literal `YYYY-MM-DD`; both reformat-only, `last_verified` untouched.
- The corpus is now guard-clean-ready for Plan 02's full-batch fail-closed run.
</success_criteria>

<output>
Create `.planning/phases/126-publish-bundle-pipeline-guard-blocker-corpus-fixes/126-01-SUMMARY.md` when done
</output>
