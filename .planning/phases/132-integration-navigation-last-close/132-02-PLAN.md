---
phase: 132-integration-navigation-last-close
plan: 02
type: execute
wave: 2
depends_on: [132-01]
files_modified:
  - docs/index.md
autonomous: true
requirements: [CLASS-04]
must_haves:
  truths:
    - "docs/index.md has a dedicated ## Device Configuration Recipes section listing both recipes"
    - "The recipes section sits after ## Linux Provisioning and before ## Operations (D-02)"
    - "Both recipes are reachable via working relative links from index.md"
    - "Troubleshooting hubs (common-issues.md / quick-ref-l1.md / quick-ref-l2.md) remain NOT wired to the recipes"
    - "C17 exits 0 on the full corpus including both recipe files"
    - "The index.md nav commit post-dates the Plan 01 registry+status commit (navigation-last)"
  artifacts:
    - path: "docs/index.md"
      provides: "Device Configuration Recipes discovery section"
      contains: "## Device Configuration Recipes"
  key_links:
    - from: "docs/index.md"
      to: "docs/recipes/01-shared-windows-avd-client.md"
      via: "relative markdown link in recipes section"
      pattern: "recipes/01-shared-windows-avd-client.md"
    - from: "docs/index.md"
      to: "docs/recipes/02-shared-ipad-full-provisioning.md"
      via: "relative markdown link in recipes section"
      pattern: "recipes/02-shared-ipad-full-provisioning.md"
---

<objective>
Make both recipes discoverable from `docs/index.md` via one dedicated recipes section (D-01), confirm the troubleshooting hubs are NOT wired, and re-confirm C17 green on the full corpus. This satisfies CLASS-04 and closes the phase under navigation-last discipline.

Purpose: Navigation is added LAST — only after both recipes are content-complete AND registered (Plan 01). This plan is wave 2 (depends_on 132-01), so its index.md commit structurally post-dates the registry+status commit, satisfying the project's navigation-last convention verified via git history at close.
Output: New index.md recipes section; hub non-wiring confirmed; C17 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-integration-navigation-last-close/132-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Device Configuration Recipes section to index.md</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md (lines ~238-275: the `## Linux Provisioning` section through the start of `## Operations` — the recipes section is inserted between them; study the intro-paragraph + `### subsection` + `| Resource | When to Use |` table style to match convention)
    - docs/recipes/01-shared-windows-avd-client.md (lines 15-25: H1 + Summary for the audience/scope blurb text)
    - docs/recipes/02-shared-ipad-full-provisioning.md (lines 15-25: H1 + Summary for the audience/scope blurb text)
  </read_first>
  <action>
    Insert ONE new top-level section `## Device Configuration Recipes` into `docs/index.md`, positioned immediately after the `## Linux Provisioning` section (after its trailing `---` divider) and immediately before `## Operations` (D-02 placement). The section links BOTH recipes with a short audience/scope blurb each, following the existing index.md entry style (Claude's discretion on exact wording per CONTEXT — a `| Resource | When to Use |` table or short bullet list, consistent with neighboring sections). Use relative links `recipes/01-shared-windows-avd-client.md` and `recipes/02-shared-ipad-full-provisioning.md`.

    D-01 constraint: this is a SINGLE dedicated section listing both recipes. Do NOT distribute or cross-link the recipes into the `## Windows Autopilot` or `## iOS/iPadOS Provisioning` platform sections — recipes stay a single distinct discoverable class. Do NOT touch any other section.
  </action>
  <verify>
    <automated>test $(grep -c '## Device Configuration Recipes' docs/index.md) -eq 1 && grep -q 'recipes/01-shared-windows-avd-client.md' docs/index.md && grep -q 'recipes/02-shared-ipad-full-provisioning.md' docs/index.md && awk '/^## Linux Provisioning/{l=NR} /^## Device Configuration Recipes/{r=NR} /^## Operations/{o=NR} END{exit !(l<r && r<o)}' docs/index.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## Device Configuration Recipes' docs/index.md` returns 1
    - the new heading appears AFTER `## Linux Provisioning` and BEFORE `## Operations` (awk line-order check passes)
    - `docs/index.md` links both `recipes/01-shared-windows-avd-client.md` and `recipes/02-shared-ipad-full-provisioning.md`
    - the recipes are NOT added into `## Windows Autopilot` or `## iOS/iPadOS Provisioning` (those sections' link sets are unchanged from Plan-01 baseline)
  </acceptance_criteria>
  <done>index.md has one dedicated recipes section between Linux Provisioning and Operations, linking both recipes.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm hubs not wired + C17 green on full corpus</name>
  <files></files>
  <read_first>
    - docs/common-issues.md, docs/quick-ref-l1.md, docs/quick-ref-l2.md (confirm no recipe references exist — baseline is 0)
    - scripts/validation/c17-eee-contract.mjs (lines 1-13: `node scripts/validation/c17-eee-contract.mjs` invocation; walks docs/ and enrolls files carrying a doc_id — both recipes are enrolled)
  </read_first>
  <action>
    Confirm the troubleshooting hubs are NOT wired to the recipes: verify `docs/common-issues.md`, `docs/quick-ref-l1.md`, and `docs/quick-ref-l2.md` contain ZERO references to the recipe paths or recipe filenames (CLASS-04 requires this to be a verification/confirmation step, NOT an edit — leave all three hubs untouched). Then run the C17 corpus validator and confirm it exits 0 with both recipe files still enrolled and green after the Plan-01 status flip. Record both results in the SUMMARY.
  </action>
  <verify>
    <automated>test $(grep -lE 'docs/recipes|01-shared-windows-avd|02-shared-ipad' docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md 2>/dev/null | wc -l) -eq 0 && node scripts/validation/c17-eee-contract.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -lE 'docs/recipes|01-shared-windows-avd|02-shared-ipad' docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md` returns 0 files (hubs NOT wired)
    - `git diff --quiet docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md` succeeds (hubs untouched)
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 (0 total violations across the full corpus including docs/recipes/*)
  </acceptance_criteria>
  <done>All three hubs confirmed unwired and untouched; C17 exits 0 on the full corpus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (docs-only) | Change surface is a single Markdown navigation-section edit plus read-only confirmation greps and a validator run. No executable code, network, auth, or user-input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-03 | Information Disclosure | index.md broken/dangling recipe links | mitigate | Relative-link greps assert both recipe paths resolve; C17 corpus gate re-validates the enrolled recipe files |
| T-132-04 | Tampering | accidental hub edit (recipes wired where they must not be) | mitigate | `git diff --quiet` on all three hubs asserts they are byte-unchanged; grep asserts zero recipe references |
| T-132-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no supply-chain surface |
</threat_model>

<verification>
- `grep -c '## Device Configuration Recipes' docs/index.md` == 1, positioned between Linux Provisioning and Operations
- Both recipe relative links present in index.md
- Hubs (common-issues/quick-ref-l1/l2) contain 0 recipe references and are byte-unchanged
- `node scripts/validation/c17-eee-contract.mjs` exits 0
- Navigation-last: this plan is wave 2 / depends_on 132-01, so its commit post-dates the registry+status commit
</verification>

<success_criteria>
Recipes discoverable via one dedicated index.md section (D-01/D-02); hubs confirmed unwired; C17 green on full corpus (CLASS-04 complete). Phase 132 done.
</success_criteria>

<output>
Create `.planning/phases/132-integration-navigation-last-close/132-02-SUMMARY.md` when done.
</output>
