---
phase: 13-apv2-l1-decision-trees-runbooks
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l1-runbooks/06-apv2-deployment-not-launched.md
  - docs/l1-runbooks/07-apv2-apps-not-installed.md
  - docs/l1-runbooks/09-apv2-deployment-timeout.md
autonomous: true
requirements: [TROU-03]
must_haves:
  truths:
    - "L1 agent can follow a scripted runbook for 'deployment experience never launched' using only Intune portal actions with zero PowerShell or registry steps"
    - "L1 agent can follow a scripted runbook for 'apps and scripts not installed' using only Intune portal actions with zero PowerShell or registry steps"
    - "L1 agent can follow a scripted runbook for 'deployment timed out' using only Intune portal actions with zero PowerShell or registry steps"
    - "Each runbook has Prerequisites + numbered Steps + Escalation Criteria with collect list"
    - "'Skipped' app status is explicitly called out as a configuration gap, not an optional skip"
  artifacts:
    - path: "docs/l1-runbooks/06-apv2-deployment-not-launched.md"
      provides: "Deployment not launched L1 runbook"
      contains: "## Steps"
    - path: "docs/l1-runbooks/07-apv2-apps-not-installed.md"
      provides: "Apps not installed L1 runbook"
      contains: "## Steps"
    - path: "docs/l1-runbooks/09-apv2-deployment-timeout.md"
      provides: "Deployment timeout L1 runbook"
      contains: "## Steps"
  key_links:
    - from: "docs/l1-runbooks/06-apv2-deployment-not-launched.md"
      to: "docs/decision-trees/04-apv2-triage.md"
      via: "Back to APv2 Triage Tree footer link"
      pattern: "04-apv2-triage"
    - from: "docs/l1-runbooks/07-apv2-apps-not-installed.md"
      to: "docs/decision-trees/04-apv2-triage.md"
      via: "Back to APv2 Triage Tree footer link"
      pattern: "04-apv2-triage"
    - from: "docs/l1-runbooks/09-apv2-deployment-timeout.md"
      to: "docs/decision-trees/04-apv2-triage.md"
      via: "Back to APv2 Triage Tree footer link"
      pattern: "04-apv2-triage"
    - from: "docs/l1-runbooks/06-apv2-deployment-not-launched.md"
      to: "docs/lifecycle-apv2/01-prerequisites.md"
      via: "See Also link to prerequisites"
      pattern: "01-prerequisites"
---

<objective>
Create the three remaining APv2 L1 runbooks: deployment not launched, apps not installed, and deployment timeout.

Purpose: L1 agents need scripted, portal-only procedures for the three most common APv2 failure scenarios that have L1-resolvable portal checks, completing the full set of APv2 L1 runbooks.

Output: 3 new markdown files in docs/l1-runbooks/ following the established Prerequisites + Steps + Escalation pattern.
</objective>

<threat_model>
This is a documentation-only plan. No code execution, user input handling, or data storage is involved. The attack surface is limited to:
- Markdown content integrity: Portal navigation paths must be accurate (incorrect paths could lead agents to wrong Intune admin center locations)
- Cross-reference correctness: Back links to decision tree and See Also links must use correct relative paths
- No secrets, credentials, or executable code in any deliverable
</threat_model>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-apv2-l1-decision-trees-runbooks/13-CONTEXT.md
@.planning/phases/13-apv2-l1-decision-trees-runbooks/13-RESEARCH.md

<interfaces>
<!-- Key patterns and source material the executor needs. -->

From docs/l1-runbooks/01-device-not-registered.md (L1 runbook master exemplar):
```markdown
---
last_verified: 2026-03-20
review_by: 2026-06-18
applies_to: APv1
audience: L1
---

> **Version gate:** This guide applies to Windows Autopilot (classic).
> For Autopilot Device Preparation, see [APv1 vs APv2 disambiguation](../apv1-vs-apv2.md).

# Device Not Registered in Autopilot
[1-sentence description of when to use this runbook]

## Prerequisites
- Access to Intune admin center (https://intune.microsoft.com) with at minimum read permissions
- Device serial number from the physical device label, BIOS, or user report
- Verified: [prior condition]

## Steps
1. Open the Intune admin center (https://intune.microsoft.com).
2. Navigate to **Devices** > **Windows** > **Enrollment** > **Windows Autopilot devices**.
3. [Check / observe / verify]
4. > **Say to the user:** "[script]"
...

## Escalation Criteria
Escalate to L2 if:
- [Condition]

**Before escalating, collect:**
- [Data item]
- [Data item]

**L2 escalation path:** [link]

---
[Back to Initial Triage Tree](../decision-trees/00-initial-triage.md)

## See Also
## Version History
| Date | Change | Author |
|------|--------|--------|
```

From docs/error-codes/06-apv2-device-preparation.md (Quick Check portal paths -- source content for runbook steps):

Deployment experience never launched (lines 26-40):
- Quick Check: Intune admin center > Devices > Windows > Enrollment > Device preparation policies -- verify policy exists, user group assigned, device group selected. Check user membership in assigned group. Verify OS version (Windows 11 22H2 + KB5035942). Check MDM scope in Entra admin center. Check Entra join permissions. Check corporate identifiers if applicable.

LOB or M365 app install failed (lines 108-121):
- Quick Check: Intune admin center > Devices > Monitor > Windows Autopilot device preparation deployments -- locate deployment record. Apps tab shows per-app status. For Failed apps check app assignment context (System vs User). For Skipped apps check ETG group assignment.

Win32/Store/EAC app install failed (lines 124-138):
- Quick Check: Same deployment report as above. For Skipped status, also check App control for business > Managed Installer policy (known issue, resolved April 2026).

Deployment timed out (lines 143-155):
- Quick Check: Device preparation policies > [policy name] > "Minutes allowed before device preparation fails". Review apps+scripts tab counts vs timeout. Check for Windows 365 deployment (February 2026 fix).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deployment-not-launched and apps-not-installed runbooks</name>
  <files>docs/l1-runbooks/06-apv2-deployment-not-launched.md, docs/l1-runbooks/07-apv2-apps-not-installed.md</files>
  <read_first>
    - docs/l1-runbooks/01-device-not-registered.md (master exemplar -- copy structure exactly: frontmatter fields, version gate format, Prerequisites/Steps/Escalation layout, "Say to the user" callout format)
    - docs/error-codes/06-apv2-device-preparation.md (source Quick Check portal paths for both runbooks -- lines 26-40 for deployment not launched, lines 108-138 for apps not installed)
    - docs/lifecycle-apv2/01-prerequisites.md (prerequisites checklist -- deployment not launched steps trace back to missing prerequisites)
    - docs/_templates/l1-template.md (L1 template confirming no-PowerShell constraint)
  </read_first>
  <action>
**Create `docs/l1-runbooks/06-apv2-deployment-not-launched.md`:**

1. **Frontmatter:** `last_verified: 2026-04-12`, `review_by: 2026-07-11`, `applies_to: APv2`, `audience: L1`

2. **Version gate blockquote:** "This guide covers Autopilot Device Preparation (APv2). For Windows Autopilot (classic), see [Initial Triage Decision Tree](../decision-trees/00-initial-triage.md)."

3. **Title:** `# APv2 Deployment Experience Never Launched`

4. **Description:** "Use this runbook when a device completed OOBE sign-in but the Device Preparation screen never appeared. The user went directly to the Windows desktop without any deployment progress indicator."

5. **Prerequisites:**
    - Access to Intune admin center (https://intune.microsoft.com) with at least read permissions to Device preparation policies and device enrollment
    - Access to Entra admin center (https://entra.microsoft.com) with at least read permissions to device settings and mobility settings
    - Device serial number from the physical device label, BIOS, or user report
    - Confirmed: user signed in during OOBE but no deployment screen appeared (not "deployment started but failed")

6. **Steps** (portal-only, zero PowerShell -- expand each Quick Check path into discrete numbered steps):

    1. Open the Intune admin center (https://intune.microsoft.com).
    2. Navigate to **Devices** > **Windows** > **Enrollment** > **Device preparation policies**.
    3. Verify that at least one Device Preparation policy exists. If no policy exists: this is the cause. Inform the admin team that no Device Preparation policy is configured.
    4. Select the Device Preparation policy. Verify that a **user group** is assigned in the policy's Assignments section. Note the group name.
    5. Verify that a **device group** (Enrollment Time Grouping) is selected in the policy. Note the group name.
    6. > **Say to the user:** "I'm checking your account and device configuration in our management system. This may take a few minutes."
    7. Check whether the signing-in user is a member of the assigned user group: navigate to **Intune admin center** > **Groups** > search for the group name noted in step 4 > **Members**. Search for the user's name or UPN.
    8. If the user is NOT a member of the assigned group: this is the likely cause. Inform the admin team that the user needs to be added to the Device Preparation policy's assigned group.
    9. Verify the device OS version meets minimum requirements: Ask the user to check **Settings** > **System** > **About** and read the Windows version. APv2 requires Windows 11 22H2 or later with update KB5035942 or later installed.
    10. Open the Entra admin center (https://entra.microsoft.com).
    11. Navigate to **Identity** > **Devices** > **Device settings**. Verify that "Users may join devices to Microsoft Entra" is set to **All** or includes the user/group.
    12. Navigate to **Identity** > **Mobility (MDM and WIP)** > **Microsoft Intune**. Verify that **MDM user scope** is set to **All** or includes the user/group. If set to **None**: this is the cause.
    13. If applicable, check corporate identifiers: return to Intune admin center > **Devices** > **Windows** > **Enrollment** > **Corporate device identifiers**. If the organization uses enrollment restrictions that require corporate identifiers, verify the device's serial number or manufacturer/model is listed.
    14. > **Say to the user:** "I've identified [finding from checks above]. I'm going to [action based on finding]. [Provide expected timeline based on your org's SLA.]"

7. **Escalation Criteria:**
    Escalate to L2 if:
    - All portal checks above are clear (policy exists, user in group, OS version correct, MDM scope set, Entra join allowed) but deployment still did not launch
    - OS version appears correct but user is unsure about installed updates
    - Multiple devices from the same user or location are affected (possible tenancy-level configuration issue)

    **Before escalating, collect:**
    - Device serial number
    - OS version and build number (from Settings > System > About)
    - Device Preparation policy name and assigned groups
    - User group membership status (member / not member)
    - MDM user scope setting (All / Some / None)
    - Entra join permission setting
    - Corporate identifier status (if applicable)

    **L2 escalation path:** L2 runbooks index (Phase 14 -- to be created)

8. **Navigation footer:** `[Back to APv2 Triage Tree](../decision-trees/04-apv2-triage.md)`

9. **See Also:** [APv2 Prerequisites](../lifecycle-apv2/01-prerequisites.md), [APv2 Deployment Flow](../lifecycle-apv2/02-deployment-flow.md), [APv1 vs APv2](../apv1-vs-apv2.md)

10. **Version History table:** Single row: 2026-04-12, Initial version.

---

**Create `docs/l1-runbooks/07-apv2-apps-not-installed.md`:**

1. **Frontmatter:** `last_verified: 2026-04-12`, `review_by: 2026-07-11`, `applies_to: APv2`, `audience: L1`

2. **Version gate blockquote:** "This guide covers Autopilot Device Preparation (APv2). For Windows Autopilot (classic), see [ESP Stuck or Failed runbook](02-esp-stuck-or-failed.md)."

3. **Title:** `# APv2 Apps and Scripts Not Installed`

4. **Description:** "Use this runbook when a device went through Device Preparation but expected apps or scripts are missing, failed to install, or were skipped. This covers LOB apps, Microsoft 365 apps, Win32 apps, Microsoft Store apps, and PowerShell scripts assigned through the Device Preparation policy."

5. **Prerequisites:**
    - Access to Intune admin center (https://intune.microsoft.com) with at least read permissions to device deployments and app assignments
    - Device serial number or device name from the user
    - Confirmed: Device Preparation screen appeared and completed (or timed out), but expected apps are missing from the device

6. **Steps** (portal-only, zero PowerShell):

    1. Open the Intune admin center (https://intune.microsoft.com).
    2. Navigate to **Devices** > **Monitor** > **Windows Autopilot device preparation deployments**.
    3. Locate the device's deployment record by searching for the device name or serial number. Select the record.
    4. Select the **Apps** tab. Review the status of each app:
       - **Installed** -- app deployed successfully (no action needed)
       - **In progress** -- app still installing (may resolve on its own)
       - **Failed** -- app attempted to install but encountered an error
       - **Skipped** -- app was expected but was bypassed
    5. > **Important:** A **Skipped** status does not mean the app is optional. It means the app was selected in the Device Preparation policy but was bypassed due to a configuration gap. Treat Skipped with the same urgency as Failed.
    6. Select the **Scripts** tab. Review the status of each script. If any script shows **Failed**: note the script name and skip to step 12 (script failures require L2 investigation).
    7. For each app with **Failed** status: navigate to **Apps** > **All apps** > search for the app name > **Properties** > **Assignments**. Check the **Install context**: it must be **System** (not **User**). If set to User: this is the likely cause -- inform the admin team that the app assignment context must be changed to System for Device Preparation deployments.
    8. > **Say to the user:** "I'm checking the configuration of the apps that didn't install on your device. This may take a few minutes while I review each app."
    9. For each app with **Skipped** status: navigate to **Apps** > **All apps** > search for the app name > **Properties** > **Assignments**. Verify that the Enrollment Time Grouping (ETG) device group is listed in the assignment targets. If the ETG group is NOT in the assignment list: this is the likely cause.
    10. For Skipped Win32, Microsoft Store, or EAC apps where the ETG group IS correctly assigned: navigate to **Endpoint security** > **App control for business**. Check whether a **Managed Installer** policy is active. An active Managed Installer policy is a known issue that can cause apps to be skipped (resolved in April 2026 update). Note this finding.
    11. > **Say to the user:** "I've found [number] app(s) with installation issues. [Describe findings: wrong context / missing group assignment / known issue]. I'm going to [report to admin team / escalate for further investigation]. Expected timeline: [your org's SLA]."
    12. **Script failure fork:** If any script shows Failed status with correct assignment (ETG group is assigned, context is System): script failures require log analysis that is beyond L1 scope. Proceed directly to escalation.

7. **Escalation Criteria:**
    Escalate to L2 if:
    - App shows Failed status with correct install context (System) and correct ETG group assignment
    - Script shows Failed status (all script failures require L2 log analysis)
    - Multiple apps show Skipped despite correct ETG assignment and no Managed Installer policy
    - Deployment report is not available or shows unexpected states

    **Before escalating, collect:**
    - Device serial number and device name
    - Full list of app names with their status (Installed / Failed / Skipped)
    - Full list of script names with their status
    - For each Failed/Skipped app: assignment context (System/User) and ETG group membership status
    - Managed Installer policy status (active / not active / not applicable)
    - Screenshot of the deployment report Apps and Scripts tabs

    **L2 escalation path:** L2 runbooks index (Phase 14 -- to be created)

8. **Navigation footer:** `[Back to APv2 Triage Tree](../decision-trees/04-apv2-triage.md)`

9. **See Also:** [APv2 Deployment Flow](../lifecycle-apv2/02-deployment-flow.md), [APv2 Failure Catalog](../error-codes/06-apv2-device-preparation.md), [APv1 vs APv2](../apv1-vs-apv2.md)

10. **Version History table:** Single row: 2026-04-12, Initial version.
  </action>
  <verify>
    <automated>cd D:/claude/Autopilot &amp;&amp; test -f docs/l1-runbooks/06-apv2-deployment-not-launched.md &amp;&amp; test -f docs/l1-runbooks/07-apv2-apps-not-installed.md &amp;&amp; grep -ci "powershell\|registry\|reg \|HKLM\|HKCU" docs/l1-runbooks/06-apv2-deployment-not-launched.md &amp;&amp; grep -ci "powershell\|registry\|reg \|HKLM\|HKCU" docs/l1-runbooks/07-apv2-apps-not-installed.md || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - docs/l1-runbooks/06-apv2-deployment-not-launched.md exists
    - docs/l1-runbooks/06-apv2-deployment-not-launched.md contains `applies_to: APv2`
    - docs/l1-runbooks/06-apv2-deployment-not-launched.md contains `audience: L1`
    - docs/l1-runbooks/06-apv2-deployment-not-launched.md contains `## Prerequisites`
    - docs/l1-runbooks/06-apv2-deployment-not-launched.md contains `## Steps`
    - docs/l1-runbooks/06-apv2-deployment-not-launched.md contains `## Escalation Criteria`
    - docs/l1-runbooks/06-apv2-deployment-not-launched.md contains `Say to the user`
    - docs/l1-runbooks/06-apv2-deployment-not-launched.md contains `Device preparation policies`
    - docs/l1-runbooks/06-apv2-deployment-not-launched.md contains `MDM user scope`
    - docs/l1-runbooks/06-apv2-deployment-not-launched.md contains `04-apv2-triage.md`
    - grep -ci "powershell\|registry\|reg \|HKLM\|HKCU" docs/l1-runbooks/06-apv2-deployment-not-launched.md returns 0
    - docs/l1-runbooks/07-apv2-apps-not-installed.md exists
    - docs/l1-runbooks/07-apv2-apps-not-installed.md contains `applies_to: APv2`
    - docs/l1-runbooks/07-apv2-apps-not-installed.md contains `audience: L1`
    - docs/l1-runbooks/07-apv2-apps-not-installed.md contains `## Prerequisites`
    - docs/l1-runbooks/07-apv2-apps-not-installed.md contains `## Steps`
    - docs/l1-runbooks/07-apv2-apps-not-installed.md contains `## Escalation Criteria`
    - docs/l1-runbooks/07-apv2-apps-not-installed.md contains `Say to the user`
    - docs/l1-runbooks/07-apv2-apps-not-installed.md contains `Skipped`
    - docs/l1-runbooks/07-apv2-apps-not-installed.md contains `Managed Installer`
    - docs/l1-runbooks/07-apv2-apps-not-installed.md contains `04-apv2-triage.md`
    - grep -ci "powershell\|registry\|reg \|HKLM\|HKCU" docs/l1-runbooks/07-apv2-apps-not-installed.md returns 0
  </acceptance_criteria>
  <done>Deployment-not-launched runbook has 14 portal-only steps covering policy existence, user group membership, OS version, MDM scope, Entra join permissions, and corporate identifiers. Apps-not-installed runbook covers Failed/Skipped/In progress statuses with explicit "Skipped does not mean optional" callout, Managed Installer known issue documentation, and script failure escalation fork. Zero PowerShell/registry content in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Create deployment timeout runbook</name>
  <files>docs/l1-runbooks/09-apv2-deployment-timeout.md</files>
  <read_first>
    - docs/l1-runbooks/01-device-not-registered.md (master exemplar -- copy structure exactly)
    - docs/error-codes/06-apv2-device-preparation.md (source Quick Check portal paths for deployment timeout scenario -- lines 143-155)
    - docs/l1-runbooks/07-apv2-apps-not-installed.md (just created in Task 1 -- verify consistent formatting and cross-linking pattern)
  </read_first>
  <action>
**Create `docs/l1-runbooks/09-apv2-deployment-timeout.md`:**

1. **Frontmatter:** `last_verified: 2026-04-12`, `review_by: 2026-07-11`, `applies_to: APv2`, `audience: L1`

2. **Version gate blockquote:** "This guide covers Autopilot Device Preparation (APv2). For Windows Autopilot (classic), see [ESP Stuck or Failed runbook](02-esp-stuck-or-failed.md)."

3. **Title:** `# APv2 Deployment Timed Out`

4. **Description:** "Use this runbook when a device's Device Preparation deployment exceeded the configured timeout and was marked as failed. The Device Preparation screen may have shown a timeout error or the deployment report shows a timeout status."

5. **Prerequisites:**
    - Access to Intune admin center (https://intune.microsoft.com) with at least read permissions to Device preparation policies and deployment reports
    - Device serial number or device name from the user
    - Confirmed: Device Preparation screen appeared but deployment timed out before completing

6. **Steps** (portal-only, zero PowerShell):

    1. Open the Intune admin center (https://intune.microsoft.com).
    2. Navigate to **Devices** > **Windows** > **Enrollment** > **Device preparation policies**.
    3. Select the Device Preparation policy that applies to this device.
    4. Check the **"Minutes allowed before device preparation fails"** setting. Note the current timeout value (default is 60 minutes).
    5. Navigate to **Devices** > **Monitor** > **Windows Autopilot device preparation deployments**.
    6. Locate the device's deployment record. Select it and review the **Apps** tab and **Scripts** tab.
    7. Count the total number of apps and scripts assigned. Compare this count against the configured timeout value. A large number of apps (especially large Win32 apps or Microsoft 365 Apps) may require more time than the configured timeout allows.
    8. > **Say to the user:** "The deployment on your device timed out before all configuration steps could complete. I'm checking whether the timeout setting needs to be adjusted. This happens occasionally with devices that have many apps to install."
    9. Check whether this is a **Windows 365** (Cloud PC) deployment: navigate to the device record in **Devices** > **All devices** > search for the device > **Properties**. If the device is a Windows 365 Cloud PC, note this -- Windows 365 deployments had a known timeout issue that was resolved in the February 2026 update. If the deployment predates this fix, this may be the root cause.
    10. > **Say to the user:** "I've identified the likely cause of the timeout. [If timeout too low: 'The deployment has more apps than the current timeout setting allows for. I'm reporting this to the admin team to adjust the timeout value.'] [If Windows 365 known issue: 'This appears to be related to a known issue with Cloud PC deployments that has since been fixed. A retry should resolve it.'] [If unclear: 'I'm going to escalate this to our technical team for further investigation.']"

7. **Escalation Criteria:**
    Escalate to L2 if:
    - Timeout value appears adequate for the number of apps/scripts assigned (timeout is not the root cause)
    - Device is not a Windows 365 Cloud PC and app count does not explain the timeout
    - Retry after timeout adjustment still results in timeout
    - Multiple devices timing out with the same policy (possible systemic issue)

    **Before escalating, collect:**
    - Device serial number and device name
    - Configured timeout value (minutes)
    - Number of apps and scripts assigned
    - List of app statuses at time of timeout (Installed / In progress / Failed / Skipped)
    - Whether device is Windows 365 Cloud PC (yes / no)
    - Date of deployment attempt (for Windows 365 known issue timeline)

    **L2 escalation path:** L2 runbooks index (Phase 14 -- to be created)

8. **Navigation footer:** `[Back to APv2 Triage Tree](../decision-trees/04-apv2-triage.md)`

9. **See Also:** [APv2 Apps Not Installed](07-apv2-apps-not-installed.md) (if timeout and app failures co-occur), [APv2 Deployment Flow](../lifecycle-apv2/02-deployment-flow.md), [APv2 Failure Catalog](../error-codes/06-apv2-device-preparation.md), [APv1 vs APv2](../apv1-vs-apv2.md)

10. **Version History table:** Single row: 2026-04-12, Initial version.
  </action>
  <verify>
    <automated>cd D:/claude/Autopilot &amp;&amp; test -f docs/l1-runbooks/09-apv2-deployment-timeout.md &amp;&amp; grep -c "## Steps" docs/l1-runbooks/09-apv2-deployment-timeout.md &amp;&amp; grep -ci "powershell\|registry\|reg \|HKLM\|HKCU" docs/l1-runbooks/09-apv2-deployment-timeout.md || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - docs/l1-runbooks/09-apv2-deployment-timeout.md exists
    - docs/l1-runbooks/09-apv2-deployment-timeout.md contains `applies_to: APv2`
    - docs/l1-runbooks/09-apv2-deployment-timeout.md contains `audience: L1`
    - docs/l1-runbooks/09-apv2-deployment-timeout.md contains `## Prerequisites`
    - docs/l1-runbooks/09-apv2-deployment-timeout.md contains `## Steps`
    - docs/l1-runbooks/09-apv2-deployment-timeout.md contains `## Escalation Criteria`
    - docs/l1-runbooks/09-apv2-deployment-timeout.md contains `Say to the user`
    - docs/l1-runbooks/09-apv2-deployment-timeout.md contains `Minutes allowed before device preparation fails`
    - docs/l1-runbooks/09-apv2-deployment-timeout.md contains `Windows 365`
    - docs/l1-runbooks/09-apv2-deployment-timeout.md contains `04-apv2-triage.md`
    - docs/l1-runbooks/09-apv2-deployment-timeout.md contains `07-apv2-apps-not-installed.md` (cross-link to related runbook)
    - grep -ci "powershell\|registry\|reg \|HKLM\|HKCU" docs/l1-runbooks/09-apv2-deployment-timeout.md returns 0
  </acceptance_criteria>
  <done>Deployment timeout runbook has 10 portal-only steps covering timeout value check, app/script count analysis, and Windows 365 known issue identification. Three distinct portal-verifiable root causes (timeout value too low, app count excessive, Windows 365 pre-fix deployment) documented with appropriate user communication scripts. Zero PowerShell/registry content.</done>
</task>

</tasks>

<verification>
1. `test -f docs/l1-runbooks/06-apv2-deployment-not-launched.md` -- deployment not launched runbook exists
2. `test -f docs/l1-runbooks/07-apv2-apps-not-installed.md` -- apps not installed runbook exists
3. `test -f docs/l1-runbooks/09-apv2-deployment-timeout.md` -- deployment timeout runbook exists
4. `grep -ci "powershell\|registry\|reg \|HKLM\|HKCU" docs/l1-runbooks/06-apv2-deployment-not-launched.md` -- returns 0
5. `grep -ci "powershell\|registry\|reg \|HKLM\|HKCU" docs/l1-runbooks/07-apv2-apps-not-installed.md` -- returns 0
6. `grep -ci "powershell\|registry\|reg \|HKLM\|HKCU" docs/l1-runbooks/09-apv2-deployment-timeout.md` -- returns 0
7. `grep -c "## Steps" docs/l1-runbooks/06-apv2-deployment-not-launched.md` -- returns 1
8. `grep -c "## Steps" docs/l1-runbooks/07-apv2-apps-not-installed.md` -- returns 1
9. `grep -c "## Steps" docs/l1-runbooks/09-apv2-deployment-timeout.md` -- returns 1
10. `grep -c "04-apv2-triage.md" docs/l1-runbooks/06-apv2-deployment-not-launched.md` -- returns at least 1 (back link)
11. `grep -c "04-apv2-triage.md" docs/l1-runbooks/07-apv2-apps-not-installed.md` -- returns at least 1 (back link)
12. `grep -c "04-apv2-triage.md" docs/l1-runbooks/09-apv2-deployment-timeout.md` -- returns at least 1 (back link)
</verification>

<success_criteria>
- All 3 L1 runbooks follow Prerequisites + numbered Steps + Escalation Criteria pattern
- All steps are portal-only (zero PowerShell, zero registry, zero HKLM/HKCU references)
- Each runbook includes "Say to the user" scripts at appropriate points
- Apps-not-installed runbook explicitly addresses "Skipped" status as a configuration gap
- Deployment timeout runbook covers all 3 root causes (timeout value, app count, Windows 365 known issue)
- All runbooks link back to APv2 triage tree
</success_criteria>

<output>
After completion, create `.planning/phases/13-apv2-l1-decision-trees-runbooks/13-02-SUMMARY.md`
</output>
