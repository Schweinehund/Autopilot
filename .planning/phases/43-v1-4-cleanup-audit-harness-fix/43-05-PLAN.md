---
phase: 43
plan: 05
type: execute
wave: 3
depends_on: [02]
files_modified:
  - docs/l2-runbooks/18-android-log-collection.md
  - docs/l2-runbooks/19-android-enrollment-investigation.md
  - docs/l2-runbooks/20-android-app-install-investigation.md
  - docs/l2-runbooks/21-android-compliance-investigation.md
  - docs/_templates/admin-template-android.md
autonomous: true
requirements: [AEAUDIT-03]
must_haves:
  truths:
    - "All 4 L2 Android runbooks (18, 19, 20, 21) have review_by: 2026-06-22 (shifted from 2026-07-22)"
    - "All 4 L2 Android runbooks have UNCHANGED last_verified: 2026-04-23 (metadata-only shift per D-22; no re-verify ritual)"
    - "docs/_templates/admin-template-android.md has last_verified: 1970-01-01 # TEMPLATE-SENTINEL"
    - "docs/_templates/admin-template-android.md HTML-comment rule block documents sentinel semantics"
    - "v1.4.1 harness C5 freshness check flips FAIL → PASS on current tree (was 5 violations; now 0)"
  artifacts:
    - path: "docs/l2-runbooks/18-android-log-collection.md"
      provides: "L2 runbook with 60-day freshness cycle (last_verified 2026-04-23; review_by 2026-06-22)"
      contains: "review_by: 2026-06-22"
    - path: "docs/_templates/admin-template-android.md"
      provides: "Template with 1970-01-01 TEMPLATE-SENTINEL + sentinel-aware HTML-comment rule"
      contains: "TEMPLATE-SENTINEL"
  key_links:
    - from: "docs/_templates/admin-template-android.md:frontmatter"
      to: "scripts/validation/v1.4.1-milestone-audit.mjs C5 sentinel-aware parse"
      via: "last_verified: 1970-01-01 sentinel value"
      pattern: "1970-01-01"
---

<objective>
Normalize `last_verified` 60-day freshness cycle across the 4 L2 Android runbooks (18-21) per D-22 — METADATA-ONLY shift (no re-verify ritual; keep last_verified: 2026-04-23 factually correct; shift review_by: 2026-07-22 → 2026-06-22 to enforce 60-day rule per Phase 34 D-14). Also apply the template sentinel edits per D-24 (belt-and-suspenders with the scope-filter landed in Plan 02): replace template's `last_verified: YYYY-MM-DD` with `last_verified: 1970-01-01 # TEMPLATE-SENTINEL` and extend the HTML-comment rule block with sentinel semantics.

Per D-25: iOS/macOS/Windows admin templates are EXPLICITLY out of scope (Android-scoped for Phase 43; those templates covered defensively by `_*`-prefix scope-filter from Plan 02). Do NOT edit non-Android templates.

Per D-23: Phase 43 PLAN.md must carry explicit "metadata-only shift, no re-verify" clause (this is it — this objective statement IS that clause).

This is Wave 3 — depends on Plan 02 (v1.4.1 harness has sentinel-aware C5 parse to recognize 1970-01-01).

Purpose: Resolves AEAUDIT-03 (60-day freshness rule enforced on L2 runbooks + template handling finalized). After Plan 05, v1.4.1 harness C5 check should flip FAIL → PASS.

Output: 5 file edits (4 L2 runbooks + 1 Android template), one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md
@docs/l2-runbooks/18-android-log-collection.md
@docs/_templates/admin-template-android.md

<interfaces>
<!-- Pattern F: YAML frontmatter for shipped docs (L2 runbook shape) - PATTERNS §Pattern F -->
<!-- Pattern G: Template frontmatter sentinel (new pattern introduced here) - PATTERNS §Pattern G -->

Current L2 runbook frontmatter (verified 2026-04-24, same shape in all 4 files):
  ---
  last_verified: 2026-04-23
  review_by: 2026-07-22
  applies_to: all
  audience: L2
  platform: Android
  ---

Target L2 runbook frontmatter after edit (only line 3 changes):
  ---
  last_verified: 2026-04-23
  review_by: 2026-06-22
  applies_to: all
  audience: L2
  platform: Android
  ---

Current template frontmatter (verified 2026-04-24 — specifically line 28 per PATTERNS §File 11):
  last_verified: YYYY-MM-DD
  review_by: YYYY-MM-DD
  ...

Target template frontmatter after edit:
  last_verified: 1970-01-01 # TEMPLATE-SENTINEL
  review_by: YYYY-MM-DD
  ...

review_by stays YYYY-MM-DD placeholder (author fills in when copying template — belt-and-suspenders: the _*-prefix scope-filter already excludes _templates/ dir per D-24 Plan 02 edit, so the sentinel is fallback-only for any future _-prefix-less template path).

Template HTML-comment rule block edit (PATTERNS §File 11 Edit B):
Current (lines 3-5 of docs/_templates/admin-template-android.md):
     Rules:
     - Fill in all YYYY-MM-DD dates (review_by = last_verified + 60 days, NOT 90)
     - Set platform to Android (this template is Android Enterprise-specific)

Target (lines 3-5 after edit — per D-24 third bullet):
     Rules:
     - Fill in all YYYY-MM-DD dates (review_by = last_verified + 60 days, NOT 90)
     - The `1970-01-01 # TEMPLATE-SENTINEL` value on last_verified is a harness-skip sentinel — REPLACE with actual authoring date when copying this template. The harness (v1.4.1+) treats `1970-01-01` as "template placeholder, skip freshness check." Real docs MUST have a valid authoring date (never keep the sentinel value in a shipped doc).
     - Set platform to Android (this template is Android Enterprise-specific)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 43-05-01: Shift review_by on 4 L2 Android runbooks (metadata-only)</name>
  <files>docs/l2-runbooks/18-android-log-collection.md, docs/l2-runbooks/19-android-enrollment-investigation.md, docs/l2-runbooks/20-android-app-install-investigation.md, docs/l2-runbooks/21-android-compliance-investigation.md</files>
  <read_first>
    - docs/l2-runbooks/18-android-log-collection.md (verify current frontmatter shape; Pattern F reference)
    - docs/l2-runbooks/19-android-enrollment-investigation.md (verify current frontmatter shape)
    - docs/l2-runbooks/20-android-app-install-investigation.md (verify current frontmatter shape; also pinned at line 21 — do NOT edit body, only line 3)
    - docs/l2-runbooks/21-android-compliance-investigation.md (verify current frontmatter shape)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md D-22 / D-23 (metadata-only shift; no re-verify)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §Files 7-10 + §Pattern F (L2 runbook frontmatter shape; 1-line change)
  </read_first>
  <action>
    For each of the 4 L2 Android runbooks, edit ONLY line 3 (the `review_by:` line in frontmatter). Change `2026-07-22` → `2026-06-22`. ALL other content — frontmatter fields, body paragraphs, tables, links, See Also sections — MUST remain byte-identical.

    D-22 scope invariant: `last_verified: 2026-04-23` stays unchanged. Do NOT bump to 2026-04-24. Do NOT re-verify content. The shift is purely metadata normalization to enforce Phase 34 D-14's 60-day rule; content authored 1 day ago is categorically fresh.

    Files:
    1. `docs/l2-runbooks/18-android-log-collection.md`
    2. `docs/l2-runbooks/19-android-enrollment-investigation.md`
    3. `docs/l2-runbooks/20-android-app-install-investigation.md` (CAUTION: this file is also pinned at line 21 — body untouched; only line 3 frontmatter changes)
    4. `docs/l2-runbooks/21-android-compliance-investigation.md`

    Single-line edit per file. Diff shape (all 4 files):
    ```diff
     ---
     last_verified: 2026-04-23
    -review_by: 2026-07-22
    +review_by: 2026-06-22
     applies_to: all
     audience: L2
     platform: Android
     ---
    ```

    Verify after edits:
    ```
    for f in docs/l2-runbooks/18-android-log-collection.md docs/l2-runbooks/19-android-enrollment-investigation.md docs/l2-runbooks/20-android-app-install-investigation.md docs/l2-runbooks/21-android-compliance-investigation.md; do
      grep -q "review_by: 2026-06-22" "$f" || { echo "FAIL: $f review_by not updated"; exit 1; }
      grep -q "last_verified: 2026-04-23" "$f" || { echo "FAIL: $f last_verified changed"; exit 1; }
      ! grep -q "review_by: 2026-07-22" "$f" || { echo "FAIL: $f old review_by still present"; exit 1; }
    done
    echo "All 4 L2 runbooks normalized"
    ```
  </action>
  <verify>
    <automated>for f in docs/l2-runbooks/18-android-log-collection.md docs/l2-runbooks/19-android-enrollment-investigation.md docs/l2-runbooks/20-android-app-install-investigation.md docs/l2-runbooks/21-android-compliance-investigation.md; do grep -q "review_by: 2026-06-22" "$f" && grep -q "last_verified: 2026-04-23" "$f" && ! grep -q "review_by: 2026-07-22" "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "review_by: 2026-06-22" docs/l2-runbooks/18-android-log-collection.md` returns 0
    - `grep -q "review_by: 2026-06-22" docs/l2-runbooks/19-android-enrollment-investigation.md` returns 0
    - `grep -q "review_by: 2026-06-22" docs/l2-runbooks/20-android-app-install-investigation.md` returns 0
    - `grep -q "review_by: 2026-06-22" docs/l2-runbooks/21-android-compliance-investigation.md` returns 0
    - All 4 files still have `last_verified: 2026-04-23` (not bumped)
    - No file contains `review_by: 2026-07-22` (old value removed)
    - `git diff docs/l2-runbooks/*-android-*.md | grep -c "^[-+]review_by"` equals 8 (one "-" and one "+" per file × 4 files)
    - Body content (everything after frontmatter) unchanged: `git diff docs/l2-runbooks/20-android-app-install-investigation.md` shows zero changes beyond the frontmatter line 3
  </acceptance_criteria>
  <done>
    All 4 L2 Android runbooks have review_by shifted from 2026-07-22 to 2026-06-22 (metadata-only; last_verified unchanged; bodies unchanged).
  </done>
</task>

<task type="auto">
  <name>Task 43-05-02: Apply template sentinel + HTML-comment rule edit</name>
  <files>docs/_templates/admin-template-android.md</files>
  <read_first>
    - docs/_templates/admin-template-android.md (verify current frontmatter line 28 + HTML-comment rule block lines 3-5)
    - docs/_templates/admin-template-ios.md (reference ONLY — must stay UNCHANGED in Phase 43 per D-25 Android-scope lock)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md D-24 / D-25 (belt-and-suspenders; Android-scoped)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §File 11 (Edit A frontmatter + Edit B HTML-comment rule block)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §Pattern G (template frontmatter sentinel — newly introduced)
  </read_first>
  <action>
    Two coupled edits to `docs/_templates/admin-template-android.md`.

    **Edit A — frontmatter sentinel** (D-24, PATTERNS §File 11 Edit A):

    Locate the `last_verified:` line in frontmatter (per PATTERNS §File 11: currently line 28, but content-match to be safe — the line reads `last_verified: YYYY-MM-DD`). Replace with:
    ```
    last_verified: 1970-01-01 # TEMPLATE-SENTINEL
    ```

    Keep `review_by: YYYY-MM-DD` placeholder unchanged — author fills in when copying template.

    Diff shape:
    ```diff
     ---
    -last_verified: YYYY-MM-DD
    +last_verified: 1970-01-01 # TEMPLATE-SENTINEL
     review_by: YYYY-MM-DD
     audience: admin
     platform: Android
     ---
    ```

    **Edit B — HTML-comment rule block** (D-24 third bullet, PATTERNS §File 11 Edit B):

    Locate the HTML-comment "Rules:" block near the top of the file (per PATTERNS §File 11: currently lines 3-5 after the opening `<!--`). Add a new bullet between the existing bullets about date-filling and platform-setting. The resulting block should be:

    Before:
    ```
         Rules:
         - Fill in all YYYY-MM-DD dates (review_by = last_verified + 60 days, NOT 90)
         - Set platform to Android (this template is Android Enterprise-specific)
    ```

    After:
    ```
         Rules:
         - Fill in all YYYY-MM-DD dates (review_by = last_verified + 60 days, NOT 90)
         - The `1970-01-01 # TEMPLATE-SENTINEL` value on last_verified is a harness-skip sentinel — REPLACE with actual authoring date when copying this template. The harness (v1.4.1+) treats `1970-01-01` as "template placeholder, skip freshness check." Real docs MUST have a valid authoring date (never keep the sentinel value in a shipped doc).
         - Set platform to Android (this template is Android Enterprise-specific)
    ```

    Use the exact wording above — harness (v1.4.1+) and "never keep the sentinel value in a shipped doc" are both required for future-auditor clarity per D-24.

    DO NOT touch any other template file:
    - `docs/_templates/admin-template-ios.md` — DO NOT EDIT (D-25 v1.5 backlog)
    - `docs/_templates/admin-template-macos.md` — DO NOT EDIT
    - `docs/_templates/admin-template.md` (generic Windows) — DO NOT EDIT

    Verify the iOS template is byte-identical to pre-Plan-05:
    ```
    git diff docs/_templates/admin-template-ios.md  # must be empty
    git diff docs/_templates/admin-template-macos.md 2>/dev/null  # must be empty (may not exist)
    ```
  </action>
  <verify>
    <automated>grep -q "last_verified: 1970-01-01 # TEMPLATE-SENTINEL" docs/_templates/admin-template-android.md && grep -q "harness-skip sentinel" docs/_templates/admin-template-android.md && grep -q "never keep the sentinel value in a shipped doc" docs/_templates/admin-template-android.md && ! grep -q "^last_verified: YYYY-MM-DD" docs/_templates/admin-template-android.md && git diff docs/_templates/admin-template-ios.md 2>/dev/null | wc -l | grep -qE "^(0| *0)$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "last_verified: 1970-01-01 # TEMPLATE-SENTINEL" docs/_templates/admin-template-android.md` returns 0
    - `grep -q "harness-skip sentinel" docs/_templates/admin-template-android.md` returns 0 (rule block bullet added)
    - `grep -q "v1.4.1+" docs/_templates/admin-template-android.md` returns 0 (harness version cited)
    - `grep -q "never keep the sentinel value" docs/_templates/admin-template-android.md` returns 0
    - `! grep -q "^last_verified: YYYY-MM-DD$" docs/_templates/admin-template-android.md` returns 0 (placeholder replaced)
    - `grep -q "review_by: YYYY-MM-DD" docs/_templates/admin-template-android.md` returns 0 (review_by placeholder untouched)
    - `git diff --exit-code docs/_templates/admin-template-ios.md` returns 0 (iOS template unchanged)
    - Template body content (everything after HTML-comment rule block and frontmatter) byte-identical to pre-edit — only the 2 changes listed above landed
  </acceptance_criteria>
  <done>
    Android template has 1970-01-01 TEMPLATE-SENTINEL + sentinel-aware HTML-comment rule block. iOS/macOS/Windows templates untouched (D-25 honored).
  </done>
</task>

<task type="auto">
  <name>Task 43-05-03: Verify v1.4.1 harness C5 flips FAIL → PASS + commit</name>
  <files>(no edits — verification + commit step)</files>
  <read_first>
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VALIDATION.md tasks 43-05-01/02/03
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md D-22/D-23/D-24 (summary of what Plan 05 achieves)
  </read_first>
  <action>
    Run v1.4.1 harness and confirm C5 now PASSES (the 5 freshness violations from Plan 02 should now be zero):
    ```
    node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | tee /tmp/v141-post-plan05.txt
    grep -E "C5.*PASS" /tmp/v141-post-plan05.txt && echo "C5 PASS confirmed" || echo "C5 still FAILing — investigate"
    ```

    Expected: C5 PASS. Any lingering violations indicate:
    - A target file still shows `review_by - last_verified > 60 days` (check each of the 4 runbooks)
    - The template-scope filter or sentinel isn't matching (verify v1.4.1 harness scope-filter includes `hasUnderscoreDirSegment` from Plan 02; verify template has exact `1970-01-01 # TEMPLATE-SENTINEL` string)
    - An unrelated Android doc has its own freshness violation (if so, surface it — not part of Phase 43's scope unless it's a template)

    Also verify the v1.4 harness is unaffected by Plan 05 (v1.4 harness does NOT have sentinel support — template still excluded by default since docs/_templates/admin-template-android.md was already outside v1.4 harness's `androidDocPaths()` enumeration pre-Plan-05. Confirm this is still true):
    ```
    node scripts/validation/v1.4-milestone-audit.mjs --verbose 2>&1 | grep -E "C5"
    ```

    Commit the Plan 05 payload:
    ```
    git add docs/l2-runbooks/1{8,9}-android-*.md docs/l2-runbooks/2{0,1}-android-*.md docs/_templates/admin-template-android.md
    git commit -m "docs(43): normalize 60-day freshness on L2 runbooks 18-21 + template sentinel"
    ```

    Commit message body should cite: D-22 metadata-only shift (no re-verify); D-24 belt-and-suspenders (scope-filter + sentinel); D-25 Android-scoped (iOS/macOS/Windows templates in v1.5 backlog); AEAUDIT-03 closure.
  </action>
  <verify>
    <automated>node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | grep -qE "C5.*PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | grep -qE "C5.*PASS"` returns 0
    - v1.4.1 harness exit code 0 or 1 (not ≥ 2)
    - v1.4 harness still runs end-to-end (unaffected by Plan 05 template edits — v1.4's androidDocPaths doesn't include _templates/)
    - `git log -1 --pretty=%s` shows "docs(43): normalize 60-day freshness" subject
    - `git show --stat HEAD 2>&1 | grep -c "android-"` is 4 OR 5 (4 L2 runbooks + template — Windows-filename-filter varies by `grep` implementation)
    - `git show --stat HEAD 2>&1 | grep -q "admin-template-android.md"` returns 0
    - `git show --stat HEAD 2>&1 | grep -q "18-android-log-collection.md"` returns 0 (sample one runbook)
    - `! git show --stat HEAD 2>&1 | grep -q "admin-template-ios.md"` returns 0 (iOS template NOT in commit)
  </acceptance_criteria>
  <done>
    v1.4.1 harness C5 PASS. AEAUDIT-03 resolved. Commit landed with only Android-scoped files (no iOS/macOS/Windows templates touched).
  </done>
</task>

</tasks>

<verification>
```
# 1. All 4 L2 runbooks updated
for f in docs/l2-runbooks/18-android-log-collection.md docs/l2-runbooks/19-android-enrollment-investigation.md docs/l2-runbooks/20-android-app-install-investigation.md docs/l2-runbooks/21-android-compliance-investigation.md; do
  grep -q "review_by: 2026-06-22" "$f" && grep -q "last_verified: 2026-04-23" "$f" || exit 1
done
# 2. Template has sentinel + rule block
grep -q "last_verified: 1970-01-01 # TEMPLATE-SENTINEL" docs/_templates/admin-template-android.md
grep -q "harness-skip sentinel" docs/_templates/admin-template-android.md
# 3. iOS/macOS templates untouched
git diff --exit-code docs/_templates/admin-template-ios.md 2>/dev/null
# 4. v1.4.1 harness C5 PASS
node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | grep -qE "C5.*PASS"
# 5. Atomic commit
git show --stat HEAD 2>&1 | grep -cE "(1[89]-android|2[01]-android|admin-template-android)"  # expect 5
```
</verification>

<success_criteria>
- 4 L2 Android runbooks at 60-day freshness cycle (last_verified unchanged; review_by shifted)
- Android template at 1970-01-01 TEMPLATE-SENTINEL with sentinel-aware HTML-comment rule
- iOS/macOS/Windows templates UNTOUCHED (D-25 honored)
- v1.4.1 harness C5 PASS (all 5 freshness violations resolved)
- AEAUDIT-03 closed
</success_criteria>

<output>
After completion, create `.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-05-SUMMARY.md` documenting:
- 4 L2 runbooks normalized (cite each file)
- Template sentinel + rule-block edits
- Which templates were deliberately NOT touched (iOS/macOS/Windows — v1.5 backlog)
- v1.4.1 C5 flip (FAIL → PASS)
- Commit SHA
- Unblocks: Plan 06 (C5 integration test), Plan 10 (terminal sanity)
</output>
