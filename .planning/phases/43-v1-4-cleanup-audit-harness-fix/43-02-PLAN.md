---
phase: 43
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/validation/v1.4.1-milestone-audit.mjs
  - scripts/validation/v1.4.1-audit-allowlist.json
autonomous: true
requirements: [AEAUDIT-03, AEAUDIT-05]
must_haves:
  truths:
    - "scripts/validation/v1.4.1-milestone-audit.mjs exists as a copy of v1.4 harness with 6 additive edits applied"
    - "v1.4.1 harness scope-filter excludes any path segment prefixed with underscore (covers _templates/, _drafts/, _archive/)"
    - "v1.4.1 harness C5 parse recognizes 1970-01-01 TEMPLATE-SENTINEL and skips freshness check for that sentinel"
    - "v1.4.1 harness includes C6/C7/C9 informational-first checks (always pass: true; emit findings count)"
    - "scripts/validation/v1.4.1-audit-allowlist.json exists with safetynet/supervision exemptions + cope_banned_phrases array"
    - "v1.4.1 harness C1-C5 baseline PASS on current tree (C2 may still FAIL — fixed by Plan 03; C5 may still FAIL — fixed by Plan 05)"
  artifacts:
    - path: "scripts/validation/v1.4.1-milestone-audit.mjs"
      provides: "Active v1.4.1 audit harness with C6/C7/C9 informational + _* scope-filter + sentinel parse"
      contains: "hasUnderscoreDirSegment"
    - path: "scripts/validation/v1.4.1-audit-allowlist.json"
      provides: "v1.4.1 allow-list sidecar skeleton (Knox/AOSP/COPE phase-44-46 target)"
      contains: "cope_banned_phrases"
  key_links:
    - from: "scripts/validation/v1.4.1-milestone-audit.mjs"
      to: "scripts/validation/v1.4.1-audit-allowlist.json"
      via: "parseAllowlist() readFile call"
      pattern: "scripts/validation/v1.4.1-audit-allowlist.json"
    - from: "scripts/validation/v1.4.1-milestone-audit.mjs"
      to: "docs/_templates/admin-template-android.md"
      via: "C5 sentinel-aware parse recognizing 1970-01-01"
      pattern: "1970-01-01"
---

<objective>
Scaffold the v1.4.1 audit harness. Per D-01: v1.4.1 harness is a `cp` of v1.4 harness plus 6 additive edits — ZERO refactors, zero shared-module extraction (Phase 42 D-25 locks file-reads-only / no-shared-module). Per D-06: adds C6/C7/C9 informational-first checks (always `pass: true`) per Phase 42 D-29 precedent. Per D-24/D-26: adds `_*`-prefix scope-filter to `androidDocPaths()` and sentinel-aware `1970-01-01` C5 parse.

This is Wave 2 — depends on Plan 01 rescue commit landing first. Ships both the v1.4.1 harness file AND the v1.4.1 allow-list sidecar skeleton in one commit (Pattern H atomicity; D-07 "scaffold" commit).

Purpose: Establishes the active harness that Phases 44/45/46 consume. Resolves downstream-dependency: "Phase 44 Knox depends on stable v1.4.1 harness ready for Knox pins" (CONTEXT canonical_refs). Sentinel + scope-filter resolves AEAUDIT-03 template-normalization blocker.

Output: 2 new files (scripts/validation/v1.4.1-milestone-audit.mjs + scripts/validation/v1.4.1-audit-allowlist.json), one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md
@scripts/validation/v1.4-milestone-audit.mjs
@scripts/validation/v1.4-audit-allowlist.json

<interfaces>
<!-- Pattern A: file-reads-only / no-shell-out (PATTERNS §Pattern A) -->
<!-- Pattern B: graceful-degradation JSON parse (PATTERNS §Pattern B) -->
<!-- Pattern C: CRLF normalization (PATTERNS §Pattern C) -->
<!-- Pattern D: YAML frontmatter extraction + multiline-anchored regex (PATTERNS §Pattern D) -->
<!-- Pattern E: runner loop + exit code (PATTERNS §Pattern E) -->
<!-- Pattern I: informational-first (PATTERNS §Pattern I) -->

v1.4 harness sidecar line (already UPDATED by Plan 01):
  const raw = readFile('scripts/validation/v1.4-audit-allowlist.json');

v1.4 harness line 1 (already HAS freeze-marker prepend from Plan 01):
  // FROZEN at commit 3c3a140 — see scripts/validation/v1.4.1-milestone-audit.mjs for active harness

v1.4.1 harness target header (after scrub + replace — per RESEARCH §1 Edit 1):
  #!/usr/bin/env node
  // v1.4.1 Milestone Audit Harness (copy of v1.4 + C6/C7/C9 informational-first per Phase 42 D-29 + _*-prefix scope-filter + TEMPLATE-SENTINEL parse)
  // Source of truth: .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md (D-01..D-08, D-24, D-26)
  // Sidecar allow-list: scripts/validation/v1.4.1-audit-allowlist.json  (Knox / per-OEM AOSP / COPE exemptions)
  // Frozen-predecessor reproducibility anchor: v1.4-milestone-audit.mjs pinned at commit 3c3a140

v1.4.1 sidecar schema (new key added):
  schema_version, generated, phase, safetynet_exemptions[], supervision_exemptions[], cope_banned_phrases[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 43-02-01: Copy v1.4 harness to v1.4.1 + apply 6 additive edits</name>
  <files>scripts/validation/v1.4.1-milestone-audit.mjs</files>
  <read_first>
    - scripts/validation/v1.4-milestone-audit.mjs (source to cp; verify Plan 01 rescue edits landed — line 1 FROZEN marker + sidecar path)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md (D-01: cp + additive; D-06: C6/C7/C9 informational-first; D-24: scope-filter + sentinel; D-26: scope-filter strictly additive)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md §1 (Edits 1-6 — exact diffs; Summary table line 293)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §File 3 (all 6 edits verbatim; "ID 8 is reserved; do NOT renumber")
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §Pattern I (informational-first contract)
  </read_first>
  <action>
    From repo root, copy the file: `cp scripts/validation/v1.4-milestone-audit.mjs scripts/validation/v1.4.1-milestone-audit.mjs`

    Then apply SIX additive edits to `scripts/validation/v1.4.1-milestone-audit.mjs`. Do NOT edit v1.4 harness.

    **Edit 1 — Header comment block replacement** (RESEARCH §1 Edit 1):

    The copied file carries the v1.4 post-rescue header. Remove the FROZEN marker (line 1) and the three v1.4 comment lines (lines 3-5 after shebang). Result — lines 1-5 of v1.4.1 must be EXACTLY:
    ```
    #!/usr/bin/env node
    // v1.4.1 Milestone Audit Harness (copy of v1.4 + C6/C7/C9 informational-first per Phase 42 D-29 + _*-prefix scope-filter + TEMPLATE-SENTINEL parse)
    // Source of truth: .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md (D-01..D-08, D-24, D-26)
    // Sidecar allow-list: scripts/validation/v1.4.1-audit-allowlist.json  (Knox / per-OEM AOSP / COPE exemptions)
    // Frozen-predecessor reproducibility anchor: v1.4-milestone-audit.mjs pinned at commit 3c3a140
    ```

    **Edit 2 — Sidecar path (find-and-replace by content)**:

    Find the single line containing `readFile('scripts/validation/v1.4-audit-allowlist.json')` (the rescued line from Plan 01). Change the filename suffix:

    Before: `const raw = readFile('scripts/validation/v1.4-audit-allowlist.json');`
    After:  `const raw = readFile('scripts/validation/v1.4.1-audit-allowlist.json');`

    **Edit 3 — `androidDocPaths()` scope-filter** (RESEARCH §1 Edit 4, D-24/D-26):

    Locate the final two lines of `androidDocPaths()` (currently `return Array.from(paths).sort();` followed by the closing brace). Replace with the block shown in PATTERNS §File 3 Edit 3 (lines 159-167), verbatim:

    ```
      // D-24 scope-filter: exclude any DIRECTORY segment starting with "_" (e.g., _templates/, _drafts/, _archive/, _partials/).
      // Defensive: covers iOS/macOS/Windows templates without explicit per-template sentinel adoption (v1.5 backlog).
      // Filename segment is INTENTIONALLY skipped so that docs/_glossary-android.md (a shipped doc with "_" prefix filename) remains in scope.
      function hasUnderscoreDirSegment(relPath) {
        const segments = relPath.split('/');
        return segments.slice(0, -1).some(seg => /^_/.test(seg));
      }
      return Array.from(paths).filter(p => !hasUnderscoreDirSegment(p)).sort();
    }
    ```

    Carve-out validation (RESEARCH §1 A7): `docs/_glossary-android.md` dir segments = `["docs"]` → INCLUDED; `docs/_templates/admin-template-android.md` dir segments = `["docs","_templates"]` → EXCLUDED.

    **Edit 4 — Sentinel-aware C5 parse** (RESEARCH §1 Edit 5, D-24):

    Locate the block in the C5 check starting with `const lvMatch = fm.match(/^last_verified:\s*(\d{4}-\d{2}-\d{2})\s*$/m);` through the `if (diffDays > 60)` block. Replace that entire block with the PATTERNS §File 3 Edit 4 code (lines 198-208), verbatim:

    ```
    const lvMatch = fm.match(/^last_verified:\s*(\d{4}-\d{2}-\d{2})\s*(#.*)?$/m);
    const rbMatch = fm.match(/^review_by:\s*(\d{4}-\d{2}-\d{2})\s*(#.*)?$/m);
    if (!lvMatch) { violations.push({ file: relPath, reason: 'last_verified missing or malformed' }); continue; }
    if (lvMatch[1] === '1970-01-01') continue;  // D-24 TEMPLATE-SENTINEL — skip
    if (!rbMatch) { violations.push({ file: relPath, reason: 'review_by missing or malformed' }); continue; }
    const lv = new Date(lvMatch[1]);
    const rb = new Date(rbMatch[1]);
    const diffDays = Math.round((rb - lv) / 86400000);
    if (diffDays > 60) {
      violations.push({ file: relPath, reason: 'review_by-last_verified=' + diffDays + 'd (>60)' });
    }
    ```

    Two changes: (a) regex gains `\s*(#.*)?$` for trailing-comment tolerance; (b) sentinel-skip branch added.

    **Edit 5 — Tag C3 informational + append C6/C7/C9** (RESEARCH §1 Edit 6, Pattern I):

    In the C3 check object definition (object with `id: 3, name: 'C3: AOSP stub word count ...'`), add a new field `informational: true,  // D-06 extension — tag for runner detail-guard` right after the `name:` field, before `run()`.

    THEN append three new check objects to the `checks` array, after the existing C5 object (before the closing bracket). IDs 6, 7, 9 — leave ID 8 reserved per Phase 42 D-29 numbering. Use VERBATIM the code from PATTERNS §File 3 Edit 5 (lines 228-291). That block defines three check objects: C6 "PITFALL-7 preservation in AOSP + per-OEM docs", C7 "bare-'Knox' disambiguation check", C9 "COPE banned-phrase check". Each has `informational: true` and a `run()` body that always returns `{ pass: true, detail: '(informational — N ...)' }` per Pattern I.

    Important: C9's `run()` reads `ALLOWLIST.cope_banned_phrases`. Confirm the harness uses an `ALLOWLIST` variable name for the parsed sidecar (v1.4 harness does; Plan 02 inherits this). If the existing variable is spelled differently, match that spelling — do NOT introduce a new global.

    **Edit 6 — Runner detail-guard generalization** (RESEARCH §1 Edit 6 runner compatibility):

    Locate the runner line `const showDetail = result.detail && (check.id === 3 || VERBOSE);` and replace with:
    ```
    const showDetail = result.detail && (check.informational === true || VERBOSE);
    ```

    This generalizes from literal ID 3 to any check tagged `informational: true` (now C3, C6, C7, C9).
  </action>
  <verify>
    <automated>test -f scripts/validation/v1.4.1-milestone-audit.mjs && node --check scripts/validation/v1.4.1-milestone-audit.mjs && grep -q "v1.4.1 Milestone Audit Harness" scripts/validation/v1.4.1-milestone-audit.mjs && grep -q "readFile('scripts/validation/v1.4.1-audit-allowlist.json')" scripts/validation/v1.4.1-milestone-audit.mjs && grep -q "hasUnderscoreDirSegment" scripts/validation/v1.4.1-milestone-audit.mjs && grep -q "1970-01-01" scripts/validation/v1.4.1-milestone-audit.mjs && grep -q "id: 6" scripts/validation/v1.4.1-milestone-audit.mjs && grep -q "id: 7" scripts/validation/v1.4.1-milestone-audit.mjs && grep -q "id: 9" scripts/validation/v1.4.1-milestone-audit.mjs && ! grep -q "id: 8," scripts/validation/v1.4.1-milestone-audit.mjs && grep -q "check.informational === true" scripts/validation/v1.4.1-milestone-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/validation/v1.4.1-milestone-audit.mjs` returns 0
    - `node --check scripts/validation/v1.4.1-milestone-audit.mjs` exits 0 (syntactically valid)
    - `grep -q "v1.4.1 Milestone Audit Harness" scripts/validation/v1.4.1-milestone-audit.mjs` returns 0 (Edit 1 header applied)
    - `! grep -q "^// FROZEN at commit" scripts/validation/v1.4.1-milestone-audit.mjs` returns 0 (FROZEN marker NOT present on v1.4.1 — that marker applies to v1.4 only)
    - `grep -q "readFile('scripts/validation/v1.4.1-audit-allowlist.json')" scripts/validation/v1.4.1-milestone-audit.mjs` returns 0 (Edit 2)
    - `grep -q "hasUnderscoreDirSegment" scripts/validation/v1.4.1-milestone-audit.mjs` returns 0 (Edit 3)
    - `grep -q "TEMPLATE-SENTINEL" scripts/validation/v1.4.1-milestone-audit.mjs` returns 0 AND `grep -q "'1970-01-01'" scripts/validation/v1.4.1-milestone-audit.mjs` returns 0 (Edit 4)
    - `grep -Eq "id: 6" scripts/validation/v1.4.1-milestone-audit.mjs && grep -Eq "id: 7" scripts/validation/v1.4.1-milestone-audit.mjs && grep -Eq "id: 9" scripts/validation/v1.4.1-milestone-audit.mjs` (Edit 5 C6/C7/C9 added)
    - `! grep -Eq "id: 8," scripts/validation/v1.4.1-milestone-audit.mjs` returns 0 (ID 8 reserved — not in file)
    - `grep -q "check.informational === true" scripts/validation/v1.4.1-milestone-audit.mjs` returns 0 (Edit 6)
    - Count of `informational: true` occurrences ≥ 4 (C3 tagged + C6/C7/C9)
  </acceptance_criteria>
  <done>
    scripts/validation/v1.4.1-milestone-audit.mjs exists with all 6 edits applied: new header, v1.4.1 sidecar path, `_*`-prefix scope-filter, sentinel-aware C5, C3 informational tag + C6/C7/C9 appended, runner detail-guard generalized. Syntactically valid JS.
  </done>
</task>

<task type="auto">
  <name>Task 43-02-02: Author v1.4.1 allow-list sidecar skeleton (JSON)</name>
  <files>scripts/validation/v1.4.1-audit-allowlist.json</files>
  <read_first>
    - scripts/validation/v1.4-audit-allowlist.json (Plan 01 restored baseline — copy safetynet/supervision arrays verbatim)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md (D-04: author net-new v1.4.1 sidecar; D-05: not parameterize — literal sidecar per harness)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md §3 lines 422-434 (final JSON skeleton shape)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §File 4 (full skeleton + validation command)
  </read_first>
  <action>
    Create a new file `scripts/validation/v1.4.1-audit-allowlist.json` with the following content. The `safetynet_exemptions` and `supervision_exemptions` arrays MUST be copied VERBATIM from `scripts/validation/v1.4-audit-allowlist.json` (the 4+9 baseline restored by Plan 01). The `cope_banned_phrases` array is net-new for v1.4.1.

    IMPORTANT: At Plan 02 time the v1.4 sidecar has ONLY 9 supervision pins (Plan 03 expands to 23). The v1.4.1 skeleton ships with the v1.4-baseline 9 pins as-is. Per D-04: v1.4.1 sidecar starts with v1.4 pins copied + (later) any new bridge-prose pins. Knox/per-OEM AOSP/COPE pins are added by Phases 44/45/46 during their content-authoring work — Phase 43 ships an empty-for-those-phases skeleton.

    Write `scripts/validation/v1.4.1-audit-allowlist.json` as valid JSON (standard JSON; no JS comments):

    ```json
    {
      "schema_version": "1.0",
      "generated": "2026-04-24T00:00:00Z",
      "phase": "43-v1-4-cleanup-audit-harness-fix",
      "safetynet_exemptions": [
        <copy all 4 entries VERBATIM from scripts/validation/v1.4-audit-allowlist.json safetynet_exemptions[]>
      ],
      "supervision_exemptions": [
        <copy all 9 entries VERBATIM from scripts/validation/v1.4-audit-allowlist.json supervision_exemptions[]>
      ],
      "cope_banned_phrases": [
        "\\bCOPE\\b[^.]*\\bdeprecated\\b",
        "\\bCOPE\\b[^.]*\\bend of life\\b",
        "\\bCOPE\\b[^.]*\\bremoved\\b"
      ]
    }
    ```

    Verify JSON parses and regex patterns are valid:
    ```
    node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.4.1-audit-allowlist.json','utf8')); for (const p of j.cope_banned_phrases) new RegExp(p, 'i'); console.log('v1.4.1 sidecar OK:', j.safetynet_exemptions.length, 'safetynet +', j.supervision_exemptions.length, 'supervision +', j.cope_banned_phrases.length, 'COPE phrases');"
    ```
    Expected output: `v1.4.1 sidecar OK: 4 safetynet + 9 supervision + 3 COPE phrases`
  </action>
  <verify>
    <automated>test -f scripts/validation/v1.4.1-audit-allowlist.json && node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.4.1-audit-allowlist.json','utf8')); if (j.safetynet_exemptions.length!==4 || j.supervision_exemptions.length!==9 || j.cope_banned_phrases.length!==3) process.exit(1); for (const p of j.cope_banned_phrases) new RegExp(p,'i');"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/validation/v1.4.1-audit-allowlist.json` returns 0
    - `node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.4.1-audit-allowlist.json','utf8'))"` exits 0
    - `node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.4.1-audit-allowlist.json','utf8')); process.exit((j.safetynet_exemptions.length===4 && j.supervision_exemptions.length===9 && j.cope_banned_phrases.length===3) ? 0 : 1)"` exits 0
    - All 3 regex patterns in `cope_banned_phrases` compile via `new RegExp(p, 'i')`
    - `grep -q "\"phase\": \"43-v1-4-cleanup-audit-harness-fix\"" scripts/validation/v1.4.1-audit-allowlist.json` returns 0
  </acceptance_criteria>
  <done>
    scripts/validation/v1.4.1-audit-allowlist.json exists with 4 SafetyNet pins + 9 supervision pins (mirrored from v1.4 baseline) + 3 COPE banned-phrase regex patterns. All regex patterns compile.
  </done>
</task>

<task type="auto">
  <name>Task 43-02-03: Run v1.4.1 harness end-to-end + commit scaffold payload</name>
  <files>(no edits — verify + commit step)</files>
  <read_first>
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md (D-07 commit-2 scaffold; D-27 step 2)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VALIDATION.md tasks 43-02-01/02/03
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §Pattern H (Commit 2 Scaffold payload table)
  </read_first>
  <action>
    Run the v1.4.1 harness end-to-end. Expected:
    - 8 checks run (C1, C2, C3, C4, C5, C6, C7, C9 — no C8)
    - C1 PASS, C3 PASS, C4 PASS, C6 PASS (informational), C7 PASS (informational), C9 PASS (informational)
    - C2 may FAIL (still 27 unexempted supervision findings — Plan 03 fixes) OR may PASS
    - C5 may FAIL (still 5 freshness findings — Plan 05 fixes) OR may pass if scope-filter + sentinel already covered

    Exit code 0 or 1 is acceptable at this point. Exit code ≥ 2 indicates a harness infrastructure error — investigate.

    ```
    node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | tee /tmp/v141-audit-scaffold.txt
    echo "Exit code: $?"
    # Confirm 8 checks ran (look for check labels C1..C9 minus C8)
    grep -E "^(C[0-9]+):" /tmp/v141-audit-scaffold.txt | wc -l
    # Confirm informational details appear for C3/C6/C7/C9
    grep -E "informational" /tmp/v141-audit-scaffold.txt
    ```

    Additionally verify v1.4 harness still works (should be unchanged from Plan 01):
    ```
    node scripts/validation/v1.4-milestone-audit.mjs --verbose > /dev/null 2>&1; test $? -le 1 && echo "v1.4 still OK"
    ```

    If all checks succeed, commit the Plan 02 payload:
    ```
    git add scripts/validation/v1.4.1-milestone-audit.mjs scripts/validation/v1.4.1-audit-allowlist.json
    git commit -m "feat(43): scaffold v1.4.1 audit harness + sidecar skeleton"
    ```

    Commit message body (use heredoc) should enumerate: copy source, 6 edits, sidecar skeleton pins (4+9+3), informational-first contract, Phase 42 D-25 no-shared-module honored, references to D-01/D-04/D-06/D-07/D-24/D-26.
  </action>
  <verify>
    <automated>node scripts/validation/v1.4.1-milestone-audit.mjs --verbose; RC=$?; test $RC -le 1 && node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | grep -q "C6" && node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | grep -q "C7" && node scripts/validation/v1.4.1-milestone-audit.mjs --verbose 2>&1 | grep -q "C9"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/v1.4.1-milestone-audit.mjs --verbose` exits with code 0 or 1
    - Output contains "C6", "C7", and "C9" labels (three new informational checks ran)
    - Output does NOT contain a "C8" label (ID 8 reserved)
    - Output contains "informational" string (at least one informational detail emitted)
    - `node scripts/validation/v1.4-milestone-audit.mjs --verbose; test $? -le 1` (v1.4 harness still functional)
    - `git log -1 --pretty=%s` shows "feat(43): scaffold v1.4.1" commit subject
    - `git show --stat HEAD 2>&1 | grep -q "scripts/validation/v1.4.1-milestone-audit.mjs" && git show --stat HEAD 2>&1 | grep -q "scripts/validation/v1.4.1-audit-allowlist.json"` (both new files in same commit)
  </acceptance_criteria>
  <done>
    Scaffold commit landed — v1.4.1 harness + sidecar skeleton in one commit. Both harnesses (v1.4 and v1.4.1) run end-to-end without infra errors. v1.4.1 emits informational-first details for C3/C6/C7/C9.
  </done>
</task>

</tasks>

<verification>
```
# 1. Files exist
test -f scripts/validation/v1.4.1-milestone-audit.mjs
test -f scripts/validation/v1.4.1-audit-allowlist.json
# 2. JS syntactically valid
node --check scripts/validation/v1.4.1-milestone-audit.mjs
# 3. JSON parses
node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.4.1-audit-allowlist.json','utf8'))"
# 4. All 6 edits landed
grep -q "v1.4.1 Milestone Audit Harness" scripts/validation/v1.4.1-milestone-audit.mjs
grep -q "scripts/validation/v1.4.1-audit-allowlist.json" scripts/validation/v1.4.1-milestone-audit.mjs
grep -q "hasUnderscoreDirSegment" scripts/validation/v1.4.1-milestone-audit.mjs
grep -q "1970-01-01" scripts/validation/v1.4.1-milestone-audit.mjs
grep -Eq "id: 6" scripts/validation/v1.4.1-milestone-audit.mjs
grep -Eq "id: 7" scripts/validation/v1.4.1-milestone-audit.mjs
grep -Eq "id: 9" scripts/validation/v1.4.1-milestone-audit.mjs
grep -q "check.informational === true" scripts/validation/v1.4.1-milestone-audit.mjs
# 5. Harness runs
node scripts/validation/v1.4.1-milestone-audit.mjs --verbose; test $? -le 1
# 6. v1.4 harness unchanged by Plan 02
node scripts/validation/v1.4-milestone-audit.mjs --verbose; test $? -le 1
# 7. Commit atomicity
git show --stat HEAD 2>&1 | grep -cE "v1.4.1-(milestone-audit.mjs|audit-allowlist.json)"  # expect 2
```
</verification>

<success_criteria>
- v1.4.1 harness exists with all 6 additive edits (no refactors to shared v1.4 utilities)
- v1.4.1 sidecar skeleton exists with 4+9 pins mirrored + 3 COPE regex patterns
- Both harnesses run end-to-end (exit code 0 or 1)
- Scaffold commit landed atomically
- Downstream Phases 44/45/46 can now target v1.4.1-audit-allowlist.json for their content pins
</success_criteria>

<output>
After completion, create `.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-02-SUMMARY.md` documenting:
- Files created (v1.4.1 harness + sidecar)
- Which of the 6 edits landed and how verified
- v1.4.1 harness run output (which checks PASS / FAIL expected)
- Scaffold commit SHA
- Unblocks: Plans 03 / 04 / 05 / 06 / 07 / 10
</output>
