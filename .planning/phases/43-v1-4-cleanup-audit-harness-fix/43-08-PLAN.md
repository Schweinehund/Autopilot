---
phase: 43
plan: 08
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - .github/workflows/audit-harness-integrity.yml
  - scripts/hooks/pre-commit.sh
  - scripts/hooks/README.md
autonomous: true
requirements: [AEAUDIT-05]
must_haves:
  truths:
    - ".github/workflows/audit-harness-integrity.yml exists — bootstraps the repo's first CI workflow"
    - "GitHub Action has 4 jobs: parse / path-match / harness-run / pin-helper-advisory (per D-08 + D-15)"
    - "Action triggers on pull_request (path-filtered), schedule (weekly bitrot), and workflow_dispatch"
    - "scripts/hooks/pre-commit.sh exists — native shell + node (no Husky/lefthook/pre-commit-framework per RESEARCH §8)"
    - "Pre-commit asserts BOTH sidecars parse as valid JSON"
    - "pin-helper-advisory job uses continue-on-error: true (D-14 — never fails build)"
  artifacts:
    - path: ".github/workflows/audit-harness-integrity.yml"
      provides: "GitHub Action enforcing sidecar-parse + path-match + harness-run integrity (+ advisory pin helper)"
      contains: "audit-harness-integrity"
    - path: "scripts/hooks/pre-commit.sh"
      provides: "Fast local JSON parse check on both sidecars — minimum-surface native-shell implementation"
      contains: "supervision_exemptions"
    - path: "scripts/hooks/README.md"
      provides: "One-time install instructions for local pre-commit hook"
      contains: "git hooks"
  key_links:
    - from: ".github/workflows/audit-harness-integrity.yml harness-run job"
      to: "scripts/validation/v1.4-milestone-audit.mjs + scripts/validation/v1.4.1-milestone-audit.mjs"
      via: "node harness invocation in CI step"
      pattern: "node scripts/validation/"
---

<objective>
Bootstrap the repo's first CI infrastructure per D-08 (CI enforcement tiers: pre-commit + PR + scheduled weekly) and D-15 (pin-helper-advisory PR comment). Per RESEARCH §8: repo has ZERO existing CI (no `.github/`, no Husky, no lefthook, no pre-commit-framework, no active `.git/hooks/*`). This plan ships native shell + native GitHub Actions YAML — no new dev dependencies.

Per D-07: this is commit-3 of the 3-commit atomicity contract (commit-1 = rescue in Plan 01; commit-2 = scaffold in Plan 02; commit-3 = CI in Plan 08). Although Plans 03/04/05/07 have shipped additional commits between commit-2 and commit-3, those are per-plan discretion commits that layer on top of the scaffold — they don't break the atomicity of the 3 foundational commits.

This is Wave 5 — depends on Plans 01-04 (harness + helper must exist so the CI YAML can reference them).

Purpose: Catches bitrot from deleted paths (the exact class of bug Phase 43 fixes). Establishes reproducible harness-invocation surface for PRs going forward. Resolves AEAUDIT-05 CI bootstrapping requirement.

Output: 3 new files (CI YAML + pre-commit shell + hooks README). One commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md
@.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md

<interfaces>
<!-- No in-repo analog for .github/workflows/ or scripts/hooks/ — Phase 43 bootstraps both -->
<!-- Reference source: RESEARCH §8 YAML verbatim (lines 803-904) + shell verbatim (lines 768-792) -->

Prerequisites (verify present before writing CI):
- scripts/validation/v1.4-milestone-audit.mjs (Plan 01)
- scripts/validation/v1.4-audit-allowlist.json (Plan 01 + Plan 03)
- scripts/validation/v1.4.1-milestone-audit.mjs (Plan 02)
- scripts/validation/v1.4.1-audit-allowlist.json (Plan 02 + Plan 03)
- scripts/validation/regenerate-supervision-pins.mjs (Plan 04)

CI YAML structure (D-08 tiers + D-15 advisory):
- Job 1 "parse": Sidecar JSON parse + non-empty assertion (both v1.4 + v1.4.1)
- Job 2 "path-match" (needs parse): Grep v1.4 + v1.4.1 harness to confirm each references its correct sidecar path
- Job 3 "harness-run" (needs parse + path-match): Run both harnesses via `node`
- Job 4 "pin-helper-advisory" (needs harness-run; continue-on-error: true per D-14): Run `regenerate-supervision-pins.mjs --report`; post PR comment

Triggers:
- pull_request (paths: scripts/validation/, docs/_glossary-android.md, docs/reference/android-capability-matrix.md, docs/android-lifecycle/, docs/admin-setup-android/, docs/l2-runbooks/**-android-*.md, docs/l1-runbooks/**-android-*.md)
- schedule (cron '0 8 * * 1' — weekly Monday 08:00 UTC)
- workflow_dispatch (manual trigger)

Third-party actions (per RESEARCH §8 + PATTERNS §File 14):
- actions/checkout@v4
- actions/setup-node@v4 with node-version '20'
- actions/github-script@v7 (for PR comment posting in advisory job)

Pre-commit shell (RESEARCH §8 lines 768-792 verbatim):
- set -e
- SIDECARS array with both JSON paths
- Loop: if file exists, run `node -e "JSON.parse(...)"` — fail commit on parse error
- Exit 0 with success echo

Install path:
- Script lives at scripts/hooks/pre-commit.sh
- Developer installs manually: cp scripts/hooks/pre-commit.sh .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit
- No Husky, no lefthook, no pre-commit-framework (RESEARCH §8 "minimum-surface" rationale)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 43-08-01: Create .github/workflows/ + audit-harness-integrity.yml</name>
  <files>.github/workflows/audit-harness-integrity.yml</files>
  <read_first>
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md D-08 / D-14 / D-15
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md §8 lines 803-904 (full YAML verbatim)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §File 14 (CI YAML — no in-repo analog; use RESEARCH §8 verbatim)
  </read_first>
  <action>
    Bootstrap the `.github/workflows/` directory (first time in this repo):
    ```
    mkdir -p .github/workflows
    ```

    Write `.github/workflows/audit-harness-integrity.yml` verbatim from RESEARCH §8 lines 805-904 / PATTERNS §File 14 lines 695-788. The YAML must contain:

    ```yaml
    name: Audit Harness Integrity

    on:
      pull_request:
        paths:
          - 'scripts/validation/**'
          - 'docs/_glossary-android.md'
          - 'docs/reference/android-capability-matrix.md'
          - 'docs/android-lifecycle/**'
          - 'docs/admin-setup-android/**'
          - 'docs/l2-runbooks/**-android-*.md'
          - 'docs/l1-runbooks/**-android-*.md'
      schedule:
        - cron: '0 8 * * 1'   # Weekly bitrot catch: 08:00 UTC every Monday
      workflow_dispatch:

    jobs:
      parse:
        name: Sidecar JSON parse
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with: { node-version: '20' }
          - name: Validate v1.4 sidecar
            run: |
              node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.4-audit-allowlist.json','utf8'));
              if (!Array.isArray(j.supervision_exemptions) || j.supervision_exemptions.length === 0) { console.error('supervision_exemptions empty'); process.exit(1); }
              if (!Array.isArray(j.safetynet_exemptions) || j.safetynet_exemptions.length === 0) { console.error('safetynet_exemptions empty'); process.exit(1); }
              console.log('v1.4 OK: '+j.safetynet_exemptions.length+'+'+j.supervision_exemptions.length);"
          - name: Validate v1.4.1 sidecar
            run: |
              node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.4.1-audit-allowlist.json','utf8'));
              if (!Array.isArray(j.supervision_exemptions)) { console.error('supervision_exemptions missing'); process.exit(1); }
              console.log('v1.4.1 OK:', j.safetynet_exemptions.length+'+'+j.supervision_exemptions.length+'+'+(j.cope_banned_phrases||[]).length);"

      path-match:
        name: Sidecar path matches harness
        runs-on: ubuntu-latest
        needs: parse
        steps:
          - uses: actions/checkout@v4
          - name: Assert v1.4 harness sidecar path
            run: |
              if ! grep -q "scripts/validation/v1.4-audit-allowlist.json" scripts/validation/v1.4-milestone-audit.mjs; then
                echo "FAIL: v1.4 harness missing sidecar reference"; exit 1
              fi
          - name: Assert v1.4.1 harness sidecar path
            run: |
              if ! grep -q "scripts/validation/v1.4.1-audit-allowlist.json" scripts/validation/v1.4.1-milestone-audit.mjs; then
                echo "FAIL: v1.4.1 harness missing sidecar reference"; exit 1
              fi

      harness-run:
        name: Harness replay
        runs-on: ubuntu-latest
        needs: [parse, path-match]
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with: { node-version: '20' }
          - name: Run v1.4 harness (historical replay)
            run: node scripts/validation/v1.4-milestone-audit.mjs --verbose
          - name: Run v1.4.1 harness
            run: node scripts/validation/v1.4.1-milestone-audit.mjs --verbose

      pin-helper-advisory:
        name: Supervision pin helper (advisory)
        runs-on: ubuntu-latest
        needs: harness-run
        continue-on-error: true   # D-14 / D-15: advisory only; never fails build
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with: { node-version: '20' }
          - name: Run regenerate-supervision-pins.mjs --report
            run: |
              node scripts/validation/regenerate-supervision-pins.mjs --report > pin-report.txt
              cat pin-report.txt
          - name: Post report as PR comment
            if: github.event_name == 'pull_request'
            uses: actions/github-script@v7
            with:
              script: |
                const fs = require('fs');
                const report = fs.readFileSync('pin-report.txt', 'utf8');
                github.rest.issues.createComment({
                  issue_number: context.issue.number,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  body: '## Supervision Pin Helper (Advisory)\n\n```\n' + report + '\n```\n\n*Advisory only — D-14.*'
                });
    ```

    YAML lint check:
    ```
    # Basic syntax — node can parse YAML via `js-yaml` if available, or use node --check for .yaml files via a loader. Simplest: validate key structure by grepping required elements.
    test -f .github/workflows/audit-harness-integrity.yml
    grep -qE "^name:" .github/workflows/audit-harness-integrity.yml
    grep -qE "^on:" .github/workflows/audit-harness-integrity.yml
    grep -qE "^jobs:" .github/workflows/audit-harness-integrity.yml
    grep -q "parse:" .github/workflows/audit-harness-integrity.yml
    grep -q "path-match:" .github/workflows/audit-harness-integrity.yml
    grep -q "harness-run:" .github/workflows/audit-harness-integrity.yml
    grep -q "pin-helper-advisory:" .github/workflows/audit-harness-integrity.yml
    grep -q "continue-on-error: true" .github/workflows/audit-harness-integrity.yml
    grep -q "cron: '0 8 \* \* 1'" .github/workflows/audit-harness-integrity.yml
    ```
  </action>
  <verify>
    <automated>test -f .github/workflows/audit-harness-integrity.yml && grep -q "^name: Audit Harness Integrity" .github/workflows/audit-harness-integrity.yml && grep -q "supervision_exemptions" .github/workflows/audit-harness-integrity.yml && grep -q "continue-on-error: true" .github/workflows/audit-harness-integrity.yml && grep -q "pin-helper-advisory" .github/workflows/audit-harness-integrity.yml && grep -q "actions/checkout@v4" .github/workflows/audit-harness-integrity.yml && grep -q "actions/setup-node@v4" .github/workflows/audit-harness-integrity.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/audit-harness-integrity.yml` returns 0
    - `test -d .github/workflows` returns 0 (first-ever .github/ dir in repo)
    - YAML contains: `name: Audit Harness Integrity`
    - YAML contains 4 jobs: parse / path-match / harness-run / pin-helper-advisory
    - YAML contains `continue-on-error: true` on pin-helper-advisory (D-14/D-15)
    - YAML contains `cron: '0 8 * * 1'` (weekly schedule)
    - YAML contains `on:` with `pull_request` + `schedule` + `workflow_dispatch` triggers
    - YAML references both harnesses: `grep -q "v1.4-milestone-audit.mjs" && grep -q "v1.4.1-milestone-audit.mjs"`
    - YAML references both sidecars: `grep -q "v1.4-audit-allowlist.json" && grep -q "v1.4.1-audit-allowlist.json"`
    - YAML uses pinned major versions: `actions/checkout@v4`, `actions/setup-node@v4`, `actions/github-script@v7`
    - YAML has `supervision_exemptions` and `JSON.parse` references in parse job (per VALIDATION.md task 43-08-03)
  </acceptance_criteria>
  <done>
    .github/workflows/audit-harness-integrity.yml exists with 4 jobs matching RESEARCH §8 verbatim. Repo's first CI workflow bootstrapped.
  </done>
</task>

<task type="auto">
  <name>Task 43-08-02: Create scripts/hooks/ + pre-commit.sh + README.md</name>
  <files>scripts/hooks/pre-commit.sh, scripts/hooks/README.md</files>
  <read_first>
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md D-08 (pre-commit tier)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-RESEARCH.md §8 lines 768-792 (pre-commit shell verbatim) + RESEARCH §"Recommendation: Native shell + native GitHub Actions YAML. Do NOT introduce Husky/lefthook/pre-commit-framework."
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-PATTERNS.md §File 15 + §File 16 (shell verbatim + README outline)
  </read_first>
  <action>
    Bootstrap scripts/hooks/:
    ```
    mkdir -p scripts/hooks
    ```

    Write `scripts/hooks/pre-commit.sh` verbatim from RESEARCH §8 lines 768-792 / PATTERNS §File 15 lines 807-829:

    ```bash
    #!/usr/bin/env bash
    # Pre-commit hook: fast JSON-parse check on audit allow-list sidecars.
    # Install (manual, per-developer): cp scripts/hooks/pre-commit.sh .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit
    # Rationale: catches typos in sidecar JSON before push (Phase 43 D-08 pre-commit tier).

    set -e

    SIDECARS=(
      "scripts/validation/v1.4-audit-allowlist.json"
      "scripts/validation/v1.4.1-audit-allowlist.json"
    )

    for sidecar in "${SIDECARS[@]}"; do
      if [ -f "$sidecar" ]; then
        # Use node (portable across Windows Git Bash + macOS + Linux) instead of jq (not universally installed).
        if ! node -e "JSON.parse(require('fs').readFileSync('$sidecar', 'utf8'))" 2>/dev/null; then
          echo "FAIL: $sidecar is not valid JSON" >&2
          exit 1
        fi
      fi
    done

    echo "pre-commit: audit allow-list JSON parse OK"
    exit 0
    ```

    Make executable (mode stored in git):
    ```
    chmod +x scripts/hooks/pre-commit.sh
    ```

    Write `scripts/hooks/README.md` per PATTERNS §File 16 outline (no YAML frontmatter — developer-facing):

    ```markdown
    # Git Hooks — Audit Harness Integrity

    ## pre-commit.sh

    Fast JSON-parse check on `scripts/validation/v1.4-audit-allowlist.json` and `scripts/validation/v1.4.1-audit-allowlist.json` before each commit.

    ## Install (one-time per clone)

    ```bash
    cp scripts/hooks/pre-commit.sh .git/hooks/pre-commit
    chmod +x .git/hooks/pre-commit   # Git Bash on Windows honors this
    ```

    ## Rationale

    Phase 43 D-08 tiered enforcement:
    - **Pre-commit tier (this hook):** catches typos locally before push (fast — ~100ms)
    - **CI tier (`.github/workflows/audit-harness-integrity.yml`):** parse + non-empty + path-match + harness-run
    - **Scheduled weekly tier:** catches bitrot from deleted paths (the exact class of bug Phase 43 fixes)

    Both tiers run the same parse check.

    ## Why Not Husky / lefthook / pre-commit-framework?

    Per RESEARCH §8 "minimum-surface principle":
    - Repo is polyglot (PowerShell + Python + Node/TS) with no existing JS dev-tooling dependency on pre-commit frameworks
    - Adding Husky as a dep for a single hook violates "smallest surface that does the job"
    - Pre-commit hook logic is ~10 lines of shell — native + Git-Bash-on-Windows compat
    - Zero new `package.json` dependencies
    ```

    Verify pre-commit works on a dry run (simulate by running against current working-dir state — should exit 0 since Plan 03 landed valid JSON):
    ```
    bash scripts/hooks/pre-commit.sh
    echo "Exit: $?"
    # Expect exit 0 and "pre-commit: audit allow-list JSON parse OK"
    ```

    Verify it fails correctly on bad JSON (temporary corruption — restore immediately after):
    ```
    cp scripts/validation/v1.4-audit-allowlist.json /tmp/v14-backup.json
    echo "BROKEN JSON" > scripts/validation/v1.4-audit-allowlist.json
    bash scripts/hooks/pre-commit.sh 2>&1 || echo "Hook correctly rejected bad JSON"
    cp /tmp/v14-backup.json scripts/validation/v1.4-audit-allowlist.json
    # Re-run to confirm clean after restoration
    bash scripts/hooks/pre-commit.sh && echo "Hook clean after restoration"
    ```
  </action>
  <verify>
    <automated>test -f scripts/hooks/pre-commit.sh && test -f scripts/hooks/README.md && test -x scripts/hooks/pre-commit.sh && bash scripts/hooks/pre-commit.sh && grep -q "JSON.parse" scripts/hooks/pre-commit.sh && grep -q "Husky" scripts/hooks/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/hooks/pre-commit.sh` returns 0
    - `test -f scripts/hooks/README.md` returns 0
    - `test -d scripts/hooks` returns 0
    - pre-commit.sh has shebang `#!/usr/bin/env bash`
    - pre-commit.sh contains `set -e` (fail-fast)
    - pre-commit.sh references both sidecars via `SIDECARS=(...)` array
    - pre-commit.sh uses `node -e "JSON.parse(...)"` for parse validation
    - `bash scripts/hooks/pre-commit.sh` exits 0 on current tree (valid sidecars per Plan 03)
    - pre-commit.sh exits non-zero on malformed JSON (verified via temporary corruption test)
    - README contains install instructions with `cp scripts/hooks/pre-commit.sh .git/hooks/pre-commit`
    - README contains rationale section (Husky / lefthook alternatives explicitly rejected)
    - No changes to `package.json` (no new dev deps per RESEARCH §8 "minimum-surface")
  </acceptance_criteria>
  <done>
    scripts/hooks/pre-commit.sh + scripts/hooks/README.md shipped. Hook executable, validates both sidecars locally, rejects malformed JSON. README documents install + rationale.
  </done>
</task>

<task type="auto">
  <name>Task 43-08-03: Commit CI bootstrap payload</name>
  <files>(no edits — commit step)</files>
  <read_first>
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-CONTEXT.md D-07 (commit-3 CI — third of 3-commit atomicity)
    - .planning/phases/43-v1-4-cleanup-audit-harness-fix/43-VALIDATION.md tasks 43-08-01/02/03
  </read_first>
  <action>
    Commit the Plan 08 payload as one commit (the "CI" commit per D-07 atomicity contract):
    ```
    git add .github/workflows/audit-harness-integrity.yml scripts/hooks/pre-commit.sh scripts/hooks/README.md
    git commit -m "ci(43): audit harness integrity GitHub Action + pre-commit hook"
    ```

    Commit message body should cite:
    - D-08 CI enforcement tiers (pre-commit + PR + scheduled weekly)
    - D-14 pre-commit too aggressive for pin drift — use advisory-only for helper
    - D-15 pin-helper-advisory as a GitHub Action PR comment
    - RESEARCH §8 "minimum-surface" rationale (no Husky/lefthook/pre-commit-framework)
    - AEAUDIT-05 CI closure delta
    - Repo's first CI bootstrap (no prior .github/ directory)

    Verify commit atomicity:
    ```
    git show --stat HEAD 2>&1 | head -20
    # Expect 3 files: audit-harness-integrity.yml, pre-commit.sh, README.md
    ```
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "ci(43)" && git show --stat HEAD 2>&1 | grep -q "audit-harness-integrity.yml" && git show --stat HEAD 2>&1 | grep -q "pre-commit.sh" && git show --stat HEAD 2>&1 | grep -q "scripts/hooks/README.md"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` shows "ci(43)" subject
    - Commit contains `.github/workflows/audit-harness-integrity.yml`
    - Commit contains `scripts/hooks/pre-commit.sh`
    - Commit contains `scripts/hooks/README.md`
    - No other files in commit (atomic CI payload)
  </acceptance_criteria>
  <done>
    CI bootstrap commit (the D-07 "commit 3") landed. Repo has its first GitHub Action + first git-hook artifact. AEAUDIT-05 CI delta resolved.
  </done>
</task>

</tasks>

<verification>
```
# 1. Files exist
test -f .github/workflows/audit-harness-integrity.yml
test -f scripts/hooks/pre-commit.sh
test -f scripts/hooks/README.md

# 2. YAML structural sanity
grep -q "^name: Audit Harness Integrity" .github/workflows/audit-harness-integrity.yml
grep -c "^  [a-z].*:" .github/workflows/audit-harness-integrity.yml  # top-level job keys

# 3. 4 jobs present
for job in parse path-match harness-run pin-helper-advisory; do
  grep -q "$job:" .github/workflows/audit-harness-integrity.yml || exit 1
done

# 4. Advisory contract honored
grep -q "continue-on-error: true" .github/workflows/audit-harness-integrity.yml

# 5. Pre-commit works locally
bash scripts/hooks/pre-commit.sh

# 6. Atomic commit
git show --stat HEAD 2>&1 | grep -cE "(audit-harness-integrity.yml|pre-commit.sh|scripts/hooks/README.md)"  # expect 3
```
</verification>

<success_criteria>
- Repo's first CI workflow exists at `.github/workflows/audit-harness-integrity.yml`
- 4-job structure: parse / path-match / harness-run / pin-helper-advisory
- Pin-helper-advisory is `continue-on-error: true` (never fails build)
- Pre-commit hook exists at `scripts/hooks/pre-commit.sh` (native shell + node — no framework deps)
- README documents install + rationale
- AEAUDIT-05 CI delta closed
- No new package.json dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/43-v1-4-cleanup-audit-harness-fix/43-08-SUMMARY.md` documenting:
- 3 files created + directories bootstrapped
- CI job structure (4 jobs, dependencies)
- Why native-shell + native-YAML over Husky/lefthook
- Commit SHA
- Unblocks: Plan 10 (terminal sanity — runs the same harness invocations CI will run)
</output>
