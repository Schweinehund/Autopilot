---
phase: 152-integration-registry-navigation-last-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_registry/RE-index.md
  - scripts/pipeline/filename-map.md
  - scripts/pipeline/build-filename-map.mjs
  - scripts/pipeline/build-publish-bundle.mjs
autonomous: false
requirements: [INT-01, INT-02, INT-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "docs/_registry/RE-index.md carries exactly 236 rows matching ^| RE- and exactly 236 of them end in | Approved | (D-10, D-19)."
    - "The eleven new rows are RE-226 (firmware-oem-matrix), RE-227 (recipes/05), and RE-228..RE-236 for the nine docs/operations/** documents in path order (D-08)."
    - "Every new row's Title column is its file's H1 verbatim, preserving the 225/225 Title-equals-H1 invariant at 236/236 (D-13, D-14)."
    - "scripts/pipeline/filename-map.md was produced by node scripts/pipeline/build-filename-map.mjs and never hand-edited; the diff against its pre-phase blob is eleven pure additions with zero reordering and zero renames of incumbent rows (D-16, INT-01)."
    - "Both canary literals read 236, each measured by its own instrument over the set that canary actually counts, and both --self-test invocations exit 0 (D-17, D-19, D-20, INT-03)."
    - "Each canary's existing provenance history is intact: the line recording the Phase 137 bump is present verbatim in both scripts, with a new Phase 152 provenance line appended beneath it (D-18)."
    - "Commit A is exactly one commit touching exactly the four files in files_modified (D-01, D-02)."
    - "None of the nine docs/operations/** documents gained a doc_id or a frontmatter status key (D-22, INT-02)."
    - "INT-01 adjacency edge: two Titles that slugify to the same base do not merge — the generator resolves them with its disambiguator and reports 0 unresolved collisions for this change set, and no incumbent .docx name is renamed."
    - "INT-01 empty edge: Commit A is never empty — git log --oneline 56f55307..HEAD -- docs/_registry/ scripts/pipeline/ returns exactly 1 after this plan."
    - "INT-01 ordering edge: new registry rows append at the table tail in strict ascending RE-ID order before ## Review Notes; the generated map sorts numerically so the eleven new entries land last, and a second generator run leaves the map byte-identical."
    - "INT-03 boundary edge: each canary is a strict equality — before the bump both --self-test runs FAIL against 236 rows, after the bump both PASS; 235 or 237 would fail either side."
    - "INT-03 adjacency edge: the two canary values are equal at 236 today but are measured by two different instruments over two different sets, and both measured numbers are recorded separately with neither derived from the other."
    - statement: "If the row-count instrument returns 0, or the generator reports Parsed 0 registry rows, the executor halts rather than bumping a canary to 0."
      verification: backstop
    - "INT-03 ordering edge: canary provenance is append-only and chronological — the prior Phase 137 line is present verbatim beneath the headline comment, with the Phase 152 line beneath it."
    - "INT-03 precision edge: both canary values are exact integers obtained by running the two instruments, never derived from a document count; after the bump grep -c '236' returns 4 lines in build-filename-map.mjs and 5 lines in build-publish-bundle.mjs, and grep -c '225' returns 2 lines in each (the surviving history lines)."
  artifacts:
    - docs/_registry/RE-index.md
    - scripts/pipeline/filename-map.md
    - scripts/pipeline/build-filename-map.mjs
    - scripts/pipeline/build-publish-bundle.mjs
  key_links:
    - "The registry Status:Approved filter in build-publish-bundle.mjs is the publish gate — a non-Approved row silently excludes a document from the bundle and defeats INT-02."
    - "The registry Title column is the sole source of the .docx output filename via build-filename-map.mjs — a tidied Title silently renames a published file and no gate catches it."
    - "The two canaries read different sets (all rows vs Approved-only rows); measuring one and transcribing it to the other is the exact failure that left the bundle canary red for a whole milestone."
  prohibitions:
    - "scripts/pipeline/filename-map.md is never hand-edited — only the generator writes it."
    - "No in-place edit of an existing canary provenance history line; new provenance is appended beneath."
    - "No doc_id and no frontmatter status key is added to any of the nine docs/operations/** documents."
    - "No third commit and no per-task commit: the four files land in exactly one commit."
    - "No edit to any file outside the four in files_modified."
    - "No hand-counted canary target derived from a document count."
    - "No renumbering of an existing registry row."
    - "D-66: no bare-noun platform blockquote token (the form barred by V-54-27 in check-phase-54.mjs) at line start outside a fence, in any file this plan writes, including this plan — the corpus-wide negative scans .planning/ as well as docs/."
---

<objective>
Land the Phase 152 registry atom: the eleven new registry rows at Status: Approved, a
filename-map regenerated by its own generator, and both publish-pipeline canary literals bumped
to a value computed from the registry after the rows land — as one atomic commit of exactly four
files.

Purpose: the registry is the publish gate. Until these rows exist at Status: Approved, none of
the eleven documents Phases 146-151 authored reaches the `.docx` bundle, SharePoint or Copilot
Studio. Splitting this work across commits is what left the second canary red for an entire
milestone (INT-01, INT-03).

Output: Commit A — `docs/_registry/RE-index.md`, `scripts/pipeline/filename-map.md`,
`scripts/pipeline/build-filename-map.mjs`, `scripts/pipeline/build-publish-bundle.mjs`.

Phase shape, per D-05: **four plans, four waves, strictly sequential** — Wave 1 this registry atom;
Wave 2 the publish-bundle proof in isolation; Wave 3 navigation-last; Wave 4 the inbound links, the
terminal gates and the needle-spec handoff. A two-plan shape would put every structural edit, both
hub files, the four inbound links and all terminal gates in one plan with no internal boundary,
which is where this phase's defect density lives.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md
@.planning/phases/152-integration-registry-navigation-last-close/152-PATTERNS.md
</context>

<commit_contract>
**This overrides the executor's default per-task commit behaviour. Read it before Task 1.**

This plan produces **exactly one** content commit ("Commit A"). Tasks 1, 2 and 3 write files and
commit **nothing**. Task 4 makes the single commit.

- Commit A stages exactly these four paths and nothing else:
  `docs/_registry/RE-index.md`, `scripts/pipeline/filename-map.md`,
  `scripts/pipeline/build-filename-map.mjs`, `scripts/pipeline/build-publish-bundle.mjs`.
- Do not run `git commit` in Task 1, 2 or 3. Do not run `git add` in Task 1, 2 or 3.
- If you find yourself about to make a second content commit in this plan, stop and re-read this
  block. INT-01 says "one atomic unit, one commit"; four separate commits is the precise defect
  the requirement exists to prevent, and it was observed in Phase 150 when this override was absent.
- Planning-artifact commits (`.planning/**`) are a separate class and are not counted here.
</commit_contract>

<research_note>
Phase 152 has **no RESEARCH.md by ROADMAP directive**: the Phase 152 ROADMAP entry states
"no research pass — the registry, filename-map, canary and nav-last mechanics are a well-worn
repo pattern with named precedents." The absence is deliberate, not a coverage gap.

The ROADMAP's one named pre-plan verification (a scratch registry row pointing at an unenrolled
operations file, driven end to end through the publish bundle) **was run before planning** and its
results are carried into this plan and Plan 02 as measured facts. Per D-27 that probe is **not**
the full discharge of the research flag — it never exercised the real eleven rows — so Plan 02's
real-row bundle run is the accepted discharge, and that ordering inversion is recorded rather than
hidden.
</research_note>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way identifier allocation RE-228..RE-236</name>
  <files>docs/_registry/RE-index.md (read-only in this task — no edit until Task 2)</files>
  <decision>Whether the nine `docs/operations/**` documents take `RE-228` through `RE-236` in path order, alongside `RE-226` and `RE-227` which their frontmatter already claims.</decision>
  <context>
A shipped registry identifier becomes the provenance of a published `.docx` filename, and this
registry has never renumbered a row. Once Commit A lands and the bundle ships, the allocation
cannot be undone without breaking published filename provenance.

The allocation ceiling is deliberately `max(registry, enrolled corpus) + 1` and not the registry
maximum alone. During this deferral window the registry maximum is `RE-225` while the corpus
already carries `RE-227` in frontmatter, so the registry-only method yields `RE-226` and collides
with a claim that already exists on disk. Frontmatter is authoritative over any planning artifact.

One superseded artifact will contradict this if a reader reaches for it: the milestone architecture
research file states twelve new rows where there are eleven, and assigns `RE-226` and `RE-227` to
the **opposite** documents from their frontmatter. Frontmatter wins.
  </context>
  <options>
    <option id="path-order-228-236">
      <name>Allocate RE-228..RE-236 in path order (firmware-bios 00-04, then patch-management 05-08)</name>
      <pros>Matches the already-claimed frontmatter for RE-226/RE-227; no collision anywhere in RE-226..RE-239; path order is the same order the rows are appended and the same order the generated map sorts into; matches how RE-222..RE-225 landed.</pros>
      <cons>RE-226, a `docs/reference/` path, breaks the historical directory-grouped block that runs RE-142..RE-167 — accepted and recorded rather than worked around.</cons>
    </option>
    <option id="operator-supplied">
      <name>Operator supplies a different allocation</name>
      <pros>Covers the case where a live measurement contradicts the recorded ceiling.</pros>
      <cons>Any deviation must still avoid every identifier claimed in frontmatter, and the eleven rows plus the two canary targets all move together.</cons>
    </option>
  </options>
  <resume-signal>Reply "approved" to take the path-order allocation, or state the corrected identifier-to-path mapping.</resume-signal>
  <reversibility rating="one-way">A shipped registry identifier is the provenance of a published
  `.docx` filename and this registry has never renumbered a row. Once Commit A lands and the
  bundle ships, RE-228..RE-236 cannot be reassigned without breaking published filename
  provenance.</reversibility>
  <read_first>
    - .planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md (D-08, D-09, D-71)
    - docs/_registry/RE-index.md (the last five rows of the table)
    - Per D-71, do NOT trust `.planning/research/ARCHITECTURE.md` on any Phase-152 row: it is superseded on four specific points — it states twelve new registry rows where there are eleven, it assigns `RE-226` and `RE-227` to the **opposite** documents from their frontmatter, it records the ampersand form of the two new headings that D-29 supersedes, and it still describes the operations-index row-count equality as a live read and a blocker, which Phase 145's FIX-12 retired. Read D-71 before reading that file at all.
  </read_first>
  <action>
Present the allocation for confirmation and halt for the operator.

State these four facts, each re-measured live before presenting (do not transcribe them from the
plan — run the commands and report what they returned):

1. The current registry maximum, from the last row matching `^| RE-` in `docs/_registry/RE-index.md`.
2. The current corpus maximum, from `doc_id:` frontmatter across `docs/` — specifically that
   `docs/reference/firmware-oem-matrix.md` already claims `RE-226` and
   `docs/recipes/05-enterprise-update-plan.md` already claims `RE-227`.
3. That the allocation ceiling is `max(registry, enrolled corpus) + 1` per D-09, not the registry
   maximum alone — the registry-only method yields RE-226 and collides with the already-claimed
   frontmatter.
4. That no identifier in the range RE-226 through RE-239 is claimed anywhere else in the
   repository.

Then present the proposed allocation, in path order:

  RE-226 -> docs/reference/firmware-oem-matrix.md (already in frontmatter)
  RE-227 -> docs/recipes/05-enterprise-update-plan.md (already in frontmatter)
  RE-228 -> docs/operations/firmware-bios/00-overview.md
  RE-229 -> docs/operations/firmware-bios/01-windows-dfci.md
  RE-230 -> docs/operations/firmware-bios/02-dell-bios-configuration.md
  RE-231 -> docs/operations/firmware-bios/03-hp-bios-configuration.md
  RE-232 -> docs/operations/firmware-bios/04-lenovo-bios-configuration.md
  RE-233 -> docs/operations/patch-management/05-linux-update-delivery.md
  RE-234 -> docs/operations/patch-management/06-windows-driver-firmware-updates.md
  RE-235 -> docs/operations/patch-management/07-windows-autopatch.md
  RE-236 -> docs/operations/patch-management/08-windows-app-updates.md

Say plainly that this is one-way and why. Then wait for the operator. If any of the four measured
facts differs from what D-08 and D-09 record, do not proceed — report the divergence and halt.
  </action>
  <verify>
    <human-check>Operator confirms the RE-228..RE-236 allocation, or supplies a corrected one.</human-check>
  </verify>
  <acceptance_criteria>
    - All four facts were re-measured live and reported with the actual command output, not transcribed.
    - `grep -c 'RE-22[6-9]\|RE-23[0-9]' docs/_registry/RE-index.md` returns 0 before proceeding (no identifier in the new range is already in the registry).
    - `grep -m1 '^doc_id:' docs/reference/firmware-oem-matrix.md` returns `doc_id: RE-226`.
    - `grep -m1 '^doc_id:' docs/recipes/05-enterprise-update-plan.md` returns `doc_id: RE-227`.
    - The operator has explicitly confirmed before Task 2 begins.
  </acceptance_criteria>
  <done>The allocation is confirmed by the operator and matches the eleven paths above, or the plan is halted with a reported divergence.</done>
</task>

<task type="auto">
  <name>Task 2: Append the eleven registry rows and the Approved-semantic exception note</name>
  <files>docs/_registry/RE-index.md</files>
  <read_first>
    - docs/_registry/RE-index.md — read the header `Status` note in full, the last five table rows, and the entire `## Review Notes` section including the existing `RE-212` entry, whose shape the new note copies
    - .planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md — D-08, D-10, D-11, D-12, D-13, D-14, D-15, D-22
    - .planning/phases/152-integration-registry-navigation-last-close/152-PATTERNS.md — the `RE-index.md` row-append diff from precedent commit `f0b7aa90`
    - docs/_standards/EEE-SOP-standard.md — the §D-08 classification rules cited by D-12 (rule 1 directory precedence, rule 2 executable end-to-end procedure, rule 3 Reference default)
  </read_first>
  <action>
Edit `docs/_registry/RE-index.md` only. Do not stage and do not commit.

**Step A — append eleven rows.** Insert them at the tail of the five-column table, immediately
after the `RE-225` row and before the blank line preceding `## Review Notes` (D-15). The file is
CRLF; use the file's own line terminator so the diff shows eleven added lines, not a whole-file
rewrite.

The table has **five columns and only five**. Per D-72, Phase 151's hand-forward prescribes three
additional registry fields that do not exist in this table; they are silently dropped, and the drop
is recorded in the summary rather than accommodated by widening the table.

Column values, in order `| Doc ID | Path | Title | Doc Type | Status |`:

- Doc ID: the identifier confirmed in Task 1.
- Path: the repository-relative path, exactly as listed in Task 1.
- Title: the file's H1 with the leading `# ` removed, **verbatim** — no re-casing, no tidying, no
  prefix. The eleven verbatim H1s are: `Firmware OEM Capability Matrix`;
  `Enterprise Update Plan: A Governed Update Posture for the Whole Fleet`;
  `Firmware and BIOS Governance`; `Device Firmware Configuration Interface (DFCI)`;
  `Dell BIOS Configuration Through Intune`; `HP BIOS Configuration Through Intune`;
  `Lenovo BIOS Configuration Through Intune`; `Linux Update Delivery`;
  `Windows Driver and Firmware Updates`; `Windows Autopatch`; `Windows Application Updates`.
  Read each H1 from disk and copy it; do not retype it. Per D-14 the matrix keeps its unprefixed
  Title even though five of the six sibling matrix rows carry an `Intune: ` prefix — verbatim-H1
  wins over the sibling pattern.
- Doc Type: `Reference` for RE-226 (the matrix); `Guide` for RE-227 and for all nine operations
  documents. The rule, per D-12: §D-08 rule 1's directory precedence covers only `decision-trees/`
  and `*-lifecycle/`, so it does not fire here; rule 2 governs — an executable end-to-end procedure
  takes `Runbook` or `Guide` — and resolves before rule 3's `Reference` default can fire. The
  operations index's own table header is literally the Guide/Covers pair, and nine sibling rows
  agree.
- Status: `Approved` for all eleven. This is required by SC#1 and forced by mechanism — the
  Approved filter in `build-publish-bundle.mjs` is the publish gate, so any other value silently
  excludes the document and defeats INT-02.

Accept and record that RE-226, a `docs/reference/` path, breaks the historical directory-grouped
block that runs RE-142..RE-167 (D-15). Appending at the tail is how RE-222..RE-225 landed.

**Step B — record the Approved-semantic exception in `## Review Notes`.** The registry header note
defines `Approved` as the EEE retrofit lifecycle state reached in phases 116-118, and the nine
operations rows are deliberately not EEE-enrolled. Add one bullet to `## Review Notes` in the same
shape as the existing `RE-212` entry (bold lead-in naming the identifier range and the file class,
then the reasoning, then a closing sentence saying it is recorded so a future reviewer does not
re-litigate). The bullet must say: that the nine documents carry no `doc_id` and are not C17-gated
by the owner ruling in INT-02; that `Approved` is set because the Approved filter is the publish
gate and any other value silently excludes them; that the header note describes a completed
2026-07 retrofit wave and binds nothing executable, so the dated owner ruling governs; and that the
twenty legacy operations documents are not retrofitted. Match the file's existing dash convention
and its `RE-212` bullet's indentation exactly.

**Step C — do not touch anything else.** Do not add a `doc_id` or a frontmatter `status:` key to
any of the nine operations documents. Do not renumber any existing row. Do not edit the header
`Status` note itself.

Expect `--self-test` to FAIL after this task — both canaries still assert 225 and the registry now
has 236 rows. That failure is the canary doing its job and is the observable that proves the
canaries are live. Task 3 clears it.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && n=$(grep -c "^| RE-" docs/_registry/RE-index.md) && a=$(grep -c "^| RE-.*| Approved |$" docs/_registry/RE-index.md) && echo "rows=$n approved=$a" && [ "$n" = "236" ] && [ "$a" = "236" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| RE-' docs/_registry/RE-index.md` returns exactly `236`.
    - `grep -c '^| RE-.*| Approved |$' docs/_registry/RE-index.md` returns exactly `236`.
    - `grep -c '^| RE-22[6-9] \|^| RE-23[0-6] ' docs/_registry/RE-index.md` returns exactly `11`.
    - For each of the eleven paths, the registry Title cell equals the file's H1 with `# ` stripped, byte for byte. Verify by script over all 236 rows, not by eye: every row's Title must equal its file's first `^# ` line minus the `# ` prefix, and the script must report `236/236`.
    - `grep -n 'RE-226' docs/_registry/RE-index.md` shows Doc Type `Reference`; `grep -n 'RE-227\|RE-228\|RE-229\|RE-23[0-6]' docs/_registry/RE-index.md` shows Doc Type `Guide` on all ten.
    - The eleven new rows appear after the `RE-225` row and before the line `## Review Notes` — assert by line number: the last `^| RE-` line number is less than the `## Review Notes` line number.
    - `## Review Notes` contains a new bullet naming the range RE-228..RE-236, the words `doc_id`, and a statement that the publish gate is the Approved filter.
    - `grep -L '^doc_id:' ` over the nine operations paths lists all nine (none acquired a `doc_id`), and `grep -l '^status:'` over the same nine returns nothing.
    - `git diff --numstat docs/_registry/RE-index.md` shows 12 or 13 added lines (eleven rows plus the Review Notes bullet, which may wrap) and 0 deleted lines.
    - `git status --porcelain` lists `docs/_registry/RE-index.md` as the only modified file; nothing is staged.
    - `node scripts/pipeline/build-filename-map.mjs --self-test` exits non-zero at this point, and the failing assertion names the row count — this is expected and is recorded in the summary as the canary firing.
  </acceptance_criteria>
  <done>The registry carries 236 rows, all Approved, with eleven new rows whose Titles are verbatim H1s and whose Doc Types follow the §D-08 rule; the Review Notes records the Approved-semantic exception; nothing is staged or committed; both self-tests are red on the row count.</done>
</task>

<task type="tracer">
  <name>Task 3: Regenerate the filename map, measure both canary targets separately, bump both canaries to green</name>
  <files>scripts/pipeline/filename-map.md, scripts/pipeline/build-filename-map.mjs, scripts/pipeline/build-publish-bundle.mjs</files>
  <read_first>
    - scripts/pipeline/build-filename-map.mjs — lines 260-290 (the canary (c) comment block, its provenance history, the assertion label and the numeric literal) and the registry parser and slug function above it
    - scripts/pipeline/build-publish-bundle.mjs — line 8 (the module header count), lines 510-525 (the canary (a) comment block, its provenance history, the `stTry` label and the `stAssert` line, which carries the literal twice)
    - scripts/pipeline/filename-map.md — the generated-file warning header and the last four rows
    - .planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md — D-16, D-17, D-18, D-19, D-20, D-21
    - .planning/phases/152-integration-registry-navigation-last-close/152-PATTERNS.md — the two quoted canary blocks and the provenance-line convention from `f0b7aa90`
  </read_first>
  <action>
This is the phase's end-to-end slice: registry row -> generator -> filename map -> both canaries ->
both self-tests green. It touches every layer INT-01, INT-02 and INT-03 name. Do not stage and do
not commit.

**Step 1 — regenerate the map.** Run `node scripts/pipeline/build-filename-map.mjs` from the
repository root. Never hand-edit `scripts/pipeline/filename-map.md`; the generator is its only
writer and the file's own header says so. Capture the generator's console line reporting the parsed
row count, the derived output-filename count and the unresolved-collision count.

**Step 2 — prove the map diff is a pure append.** Run `git diff scripts/pipeline/filename-map.md`
and confirm the unified diff contains only added lines, no removed lines, and no reordering. The
measured expectation is eleven pure additions at the tail with zero renames of incumbent rows. The
hazard this check exists for: a new Title whose base slug collides with an existing one flips that
slug group from single-member to multi-member and renames the **incumbent** `.docx` through the
disambiguator — a silent rename of an already-published file. If the diff shows any removed line,
stop and report; do not proceed to the canary bumps.

**Step 3 — measure canary 1's target with canary 1's own instrument.** Run
`grep -c "^| RE-" docs/_registry/RE-index.md`. Record the number it returns. This counts **all**
registry rows, which is the set `build-filename-map.mjs` asserts over. Do not transcribe a
predicted value.

**Step 4 — measure canary 2's target with canary 2's own instrument, separately.** Count the rows
whose Status cell is `Approved`, which is the set `build-publish-bundle.mjs` asserts over. Run this
as its own command and record its own number. The two are equal today only because every row is
Approved; a single non-Approved row silently diverges them, so the deliverable is the method, not
the number (D-20). Report both numbers separately in the summary, side by side, each with the
command that produced it.

**Step 5 — bump canary 1 at its four sites in `scripts/pipeline/build-filename-map.mjs`.**
The file is CRLF; preserve it. The four edits are:
  (a) the headline comment line stating the row count the registry yields — change its numeric
      literal to the Step-3 value;
  (b) **insert** a new provenance line beneath the existing `bumped 223 -> 225 in Phase 137` line,
      inside the same comment block, matching that block's existing style: parenthetical
      continuation, past tense, naming the previous value, the new value, the phase number, and the
      rows added. Do **not** edit the Phase 137 line or the earlier Phase 132 line — the existing
      history is the drift ledger that let Phase 137 detect the earlier miss, and rewriting it
      destroys the evidence;
  (c) the assertion label string, which repeats the count in prose;
  (d) the numeric literal in the `rows.length ===` comparison.

**Step 6 — bump canary 2 at its four sites in `scripts/pipeline/build-publish-bundle.mjs`.**
This file is LF, not CRLF — do not convert it. The four edits are:
  (a) the module header comment near the top of the file stating the count of Approved documents
      converted — this site sits roughly five hundred lines away from the self-test block, no
      self-test catches a stale comment, and it is the site that would be missed;
  (b) the headline comment above the `stTry` block;
  (c) the `stTry` label string;
  (d) the `stAssert` line, which carries the literal **twice** — once in the label string and once
      in the `rows.length ===` comparison. Both occurrences change.
  Additionally **insert** a new provenance line beneath the existing `bumped 223 -> 225 in Phase 137`
  line in that comment block, in the same style, without editing any prior history line.

**Step 7 — run both self-tests.** `node scripts/pipeline/build-filename-map.mjs --self-test` and
`node scripts/pipeline/build-publish-bundle.mjs --self-test`. Both must exit 0. If either fails on
the row count, the corresponding measurement or bump is wrong — fix the script, never the registry.

**Halt condition.** If Step 3's instrument returns `0`, or the generator reports zero parsed
registry rows, stop and report. Do not bump a canary to zero.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && node scripts/pipeline/build-filename-map.mjs --self-test && node scripts/pipeline/build-publish-bundle.mjs --self-test'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/pipeline/build-filename-map.mjs --self-test` exits 0.
    - `node scripts/pipeline/build-publish-bundle.mjs --self-test` exits 0.
    - `grep -c '^| RE-' scripts/pipeline/filename-map.md` returns exactly `236`.
    - `git diff --numstat scripts/pipeline/filename-map.md` shows exactly `11` added lines and `0` deleted lines.
    - Re-running `node scripts/pipeline/build-filename-map.mjs` a second time leaves `git diff --numstat scripts/pipeline/filename-map.md` unchanged at 11 added / 0 deleted (idempotence).
    - `grep -c '236' scripts/pipeline/build-filename-map.mjs` returns exactly `4`.
    - `grep -c '225' scripts/pipeline/build-filename-map.mjs` returns exactly `2` (the Phase 137 history line plus the new Phase 152 provenance line, which names 225 as its previous value).
    - `grep -c '236' scripts/pipeline/build-publish-bundle.mjs` returns exactly `5`.
    - `grep -c '225' scripts/pipeline/build-publish-bundle.mjs` returns exactly `2`.
    - `grep -c 'bumped 223 -> 225 in Phase 137' scripts/pipeline/build-filename-map.mjs` returns `1` and the same command against `scripts/pipeline/build-publish-bundle.mjs` returns `1` — prior history intact.
    - `grep -c 'Phase 152' scripts/pipeline/build-filename-map.mjs` returns `1` and against `scripts/pipeline/build-publish-bundle.mjs` returns `1` — exactly one new provenance line per file.
    - `file scripts/pipeline/build-filename-map.mjs` still reports CRLF line terminators; `file scripts/pipeline/build-publish-bundle.mjs` still reports no CRLF. `git diff --numstat` on each shows a single-digit line count, not a whole-file rewrite.
    - The summary records the two canary measurements as two separate lines, each naming the command that produced it and the set it counted.
    - Nothing is staged and nothing is committed.
  </acceptance_criteria>
  <done>The map is generator-produced and a pure eleven-row append; both canaries read the value each measured with its own instrument; both self-tests exit 0; both provenance ledgers gained one appended line and lost none.</done>
</task>

<task type="auto">
  <name>Task 4: Land Commit A — the four-file registry atom</name>
  <files>docs/_registry/RE-index.md, scripts/pipeline/filename-map.md, scripts/pipeline/build-filename-map.mjs, scripts/pipeline/build-publish-bundle.mjs</files>
  <read_first>
    - .planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md — D-01, D-02, D-04, D-06
    - The `<commit_contract>` block at the top of this plan
  </read_first>
  <action>
Make the single content commit for this plan.

Before staging, assert that `git status --porcelain` lists exactly four modified paths and nothing
else. If a fifth path appears, stop and report it rather than committing around it.

Stage exactly: `docs/_registry/RE-index.md`, `scripts/pipeline/filename-map.md`,
`scripts/pipeline/build-filename-map.mjs`, `scripts/pipeline/build-publish-bundle.mjs`. Use an
explicit four-path `git add`; never `git add -A` and never `git add .`.

Commit message subject line: a `docs(152)` scope naming the registry atom, the eleven identifiers
as a range, and both canary bumps. The body must record, in plain sentences:
  - the two canary measurements as two separate numbers, each with the command and the set it counted;
  - that the filename map was regenerated by its generator and the diff was eleven pure additions
    with zero renames;
  - that no `status:` flip commit is needed this time, unlike the structural twin Phase 137, which
    required a third commit to flip its two rows from Draft to Approved — both enrolled documents
    here already ship `status: Approved` and the nine operations documents carry no `status:` key at
    all, so the bundle's divergence guard passes without a flip. Record this explicitly: a
    load-bearing premise held only implicitly is one the next document falsifies.

After committing, assert the commit contract held.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && n=$(git log --oneline 56f55307..HEAD -- docs/_registry/ scripts/pipeline/ | wc -l | tr -d " ") && f=$(git show --pretty=format: --name-only HEAD | grep -c . ) && echo "commits=$n files=$f" && [ "$n" = "1" ] && [ "$f" = "4" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline 56f55307..HEAD -- docs/_registry/ scripts/pipeline/` returns exactly `1` line.
    - `git show --pretty=format: --name-only HEAD` lists exactly the four paths in `files_modified` and nothing else.
    - `git status --porcelain -- docs/ scripts/` is empty after the commit.
    - `node scripts/pipeline/build-filename-map.mjs --self-test` and `node scripts/pipeline/build-publish-bundle.mjs --self-test` both still exit 0 at the committed tree.
    - The commit body contains both canary numbers on separate lines with their commands, and a sentence explaining why no flip commit is required.
    - `git diff --quiet 56f55307 -- scripts/validation/` exits 0 — this plan changed no validator.
  </acceptance_criteria>
  <done>Exactly one commit exists between the phase base and HEAD touching `docs/_registry/` or `scripts/pipeline/`, it contains exactly four files, both self-tests are green at that commit, and no validator was touched.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this plan produces

Newly created symbols — these do not exist before this plan and must be excluded from any
drift-verification pass that assumes a symbol predates the phase:

**New registry Doc IDs (11):** `RE-226`, `RE-227`, `RE-228`, `RE-229`, `RE-230`, `RE-231`,
`RE-232`, `RE-233`, `RE-234`, `RE-235`, `RE-236`.

**New registry rows (11)** in `docs/_registry/RE-index.md`, one per identifier above, at
`Status: Approved`.

**New `## Review Notes` bullet** in `docs/_registry/RE-index.md` recording the Approved-semantic
exception for RE-228..RE-236.

**New filename-map rows (11)** in `scripts/pipeline/filename-map.md`, generator-produced, one per
identifier, each carrying a newly derived `.docx` output filename.

**New canary values:** the literal `236` at four sites in `scripts/pipeline/build-filename-map.mjs`
and five sites in `scripts/pipeline/build-publish-bundle.mjs` (the `stAssert` line carries it
twice).

**New provenance lines (2):** one appended Phase 152 line in each canary comment block.

**Modified files (4):** the four paths in `files_modified`. No file is created and no file is
deleted.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry table -> filename generator | Untrusted-shaped text (a Title cell authored by hand) crosses into a filename-derivation function whose output becomes a filesystem path. |
| registry table -> publish filter | The `Status` cell alone decides whether a document is published to an externally indexed library. |
| pipeline scripts -> git history | The canary provenance ledger is the only record that detects a missed bump across milestones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-01 | Tampering | `build-filename-map.mjs` slug/collision path fed by the registry Title column | medium | mitigate | Task 3 Step 2 asserts the regenerated map diff is added-lines-only with zero removed lines, which is the only observable that catches a disambiguator-driven rename of an incumbent `.docx`; the generator's own unresolved-collision count is captured and must be zero. |
| T-152-02 | Tampering | `scripts/pipeline/filename-map.md` | medium | mitigate | Generator-only writes (INT-01, D-16); a second generator run must leave the diff byte-identical, so a hand edit is detectable as non-idempotence. Hand-editing is a listed prohibition. |
| T-152-03 | Information Disclosure | the `Status: Approved` publish filter in `build-publish-bundle.mjs` | high | mitigate | Every published row is enumerated by path in Task 1 and confirmed by the operator at a blocking checkpoint before it is written; the post-task assertion pins the total at exactly 236 Approved rows, so no unenumerated document can ride along into the externally indexed library. |
| T-152-04 | Repudiation | the canary provenance ledgers | medium | mitigate | Append-only provenance (D-18); Task 3's acceptance criteria grep the prior Phase 137 line verbatim in both files and require exactly one new Phase 152 line in each, so an in-place rewrite of history fails the task. |
| T-152-05 | Spoofing | the registry Title column as the sole source of a published filename | medium | mitigate | Task 2 verifies all 236 Titles equal their file's H1 by script over the whole table, not by eye — a tidied Title that silently renames a published file fails the 236/236 assertion. |
| T-152-06 | Denial of Service | a partially applied canary bump leaving one script red | low | mitigate | Both `--self-test` invocations are the task `<verify>` and are re-run at the committed tree in Task 4; a one-sided bump cannot pass. |
| T-152-SC | Tampering | npm/pip/cargo installs | low | accept | This plan performs zero package-manager installs. Every script it runs is Node built-ins only, per the `scripts/validation/` and `scripts/pipeline/` convention. No package-legitimacy audit is required because no package is introduced. |

ASVS level: 1. Blocking threshold: high. T-152-03 is the only high-severity entry and is
mitigated by a blocking human checkpoint plus a total-count assertion.
</threat_model>

<verification>
- `grep -c '^| RE-' docs/_registry/RE-index.md` = 236; `grep -c '^| RE-.*| Approved |$'` = 236.
- All 236 registry Titles equal their file's H1 verbatim (scripted 236/236 check).
- `grep -c '^| RE-' scripts/pipeline/filename-map.md` = 236; `git diff --numstat` against the phase
  base shows 11 added / 0 deleted for that file.
- `node scripts/pipeline/build-filename-map.mjs --self-test` exits 0.
- `node scripts/pipeline/build-publish-bundle.mjs --self-test` exits 0.
- `git log --oneline 56f55307..HEAD -- docs/_registry/ scripts/pipeline/` returns exactly 1.
- `git show --pretty=format: --name-only HEAD` lists exactly 4 paths.
- `git diff --quiet 56f55307 -- scripts/validation/` exits 0.
- None of the nine operations documents has a `doc_id:` or `status:` frontmatter key.
</verification>

<success_criteria>
Commit A exists as one commit of exactly four files. The registry carries 236 Approved rows whose
Titles are verbatim H1s. The filename map is generator-produced, an eleven-row pure append, and
idempotent. Both canaries read a value each measured with its own instrument over its own set, both
`--self-test` runs exit 0, and both provenance ledgers gained exactly one appended line while
retaining every prior line verbatim.
</success_criteria>

<output>
Create `.planning/phases/152-integration-registry-navigation-last-close/152-01-SUMMARY.md` when done.

The summary must carry, verbatim from the run:
- both canary measurements as two separate lines, each with its command and the set it counted;
- the generator's parsed-rows / output-filenames / unresolved-collisions console line;
- the `git diff --numstat` figures for all four files;
- the recorded fact that both self-tests were RED after Task 2 and GREEN after Task 3, naming the
  failing assertion text from the red run.
</output>

