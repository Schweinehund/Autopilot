---
phase: 152-integration-registry-navigation-last-close
plan: 04
type: execute
wave: 4
depends_on: ["152-01", "152-02", "152-03"]
files_modified:
  - docs/operations/patch-management/06-windows-driver-firmware-updates.md
  - docs/operations/app-lifecycle/00-overview.md
  - docs/admin-setup-apv2/02-etg-device-group.md
  - docs/admin-setup-apv1/01-hardware-hash-upload.md
  - docs/index.md
  - docs/operations/00-index.md
  - .planning/STATE.md
autonomous: true
requirements: [INT-04, INT-05, INT-06]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All four inbound links land in this phase, each one deferred by its own predecessor phase specifically because Phase 152 owns it: the reciprocal firmware link from the driver/firmware guide, the app-lifecycle return link, the apv2 WinGet link, and the apv1 hardware-hash link into the DFCI guide's prerequisites (D-51, D-52, D-53, D-54, D-55)."
    - "Phase 148's hand-forward was a class — all inbound navigation-hub wiring — not a two-file enumeration, so INT-04's two-file list is not treated as the boundary (D-55)."
    - "Commit B is exactly one commit containing the two hub files from Plan 03 plus the four inbound-link host files; the phase ends with exactly two content commits (D-01, D-03)."
    - "The phase authors zero validators; the successor validator INT-05 asks for is handed to Phase 153 as a fully pre-specified needle-spec, with every literal transcribed rather than composed at execution time (D-57, D-62)."
    - "SC#5 is discharged as two explicitly named obligations: the content precondition lands here — the three troubleshooting hubs stay unwired to the new recipe, the master hub carries both the table row and the quick-nav fragment, the frozen recipe-hub validator is byte-unchanged, and both corpus gates exit 0 — and the enforcement is handed forward (D-59)."
    - "Byte-unchanged is proved against the phase base with per-path assertions, never against the working tree at one instant, because a validator edited and committed mid-phase would show clean under a bare status check (D-60)."
    - "The needle-spec covers both hub files and the filename map, not just the master hub — a one-file spec would be a straight regression against the structural twin, which carries a filename-map needle (D-61)."
    - "The handoff is recorded on both surfaces — the Plan-Time Research Flags block in STATE.md and this plan's summary — because a spec living only in a summary is not on the surface a Phase 153 planner reads (D-63)."
    - "No accepted-violation baseline is introduced: the audit allowlist is byte-unchanged from the phase base, and the link checker admits no baseline by construction — its own header states it exits 0 only on zero failures with no allowlist, ratchet or expected-failure list (D-64, D-70, INT-06)."
    - "The terminal gate set is three, not two: full-corpus C17, the link checker, and the apex, run after the last commit. The apex is the only gate that executes the milestone audit and therefore the only one that runs the live corpus walk (D-67)."
    - statement: "INT-05 concurrency: the needle-spec handoff is idempotent — if this plan is interrupted after Commit B but before the handoff, re-running the handoff task greps for its own marker in STATE.md before writing and produces no duplicate block."
      verification: backstop
    - "INT-06 unclassified edge (probe row remains unresolved and is surfaced, never dismissed): the deterministic probe could not classify INT-06's edge family. Flagged planner assumption — no-accepted-violation-baseline is operationalized here as byte-equality on the named audit allowlist plus the link checker's structural no-baseline guarantee; there is no test that would catch a baseline introduced elsewhere in the validation tree, so the whole scripts/validation/ tree is additionally asserted byte-unchanged as the closest available proxy. Residual risk accepted and recorded, not dismissed."
  artifacts:
    - docs/operations/patch-management/06-windows-driver-firmware-updates.md
    - docs/operations/app-lifecycle/00-overview.md
    - docs/admin-setup-apv2/02-etg-device-group.md
    - docs/admin-setup-apv1/01-hardware-hash-upload.md
    - .planning/STATE.md
    - .planning/phases/152-integration-registry-navigation-last-close/152-04-SUMMARY.md
  key_links:
    - "The apex is the only gate that runs the milestone audit, so the live corpus walk that polices new hub and link prose is only exercised there — running C17 and the link checker alone is not the terminal gate set."
    - "The frozen recipe-hub validator's generic recipes arm never fires for later recipes, so the new recipe is enforced by nothing today; the fix is a successor validator authored in Phase 153, never an edit to the frozen one."
    - "The apv1 hardware-hash guide is C17-enrolled and carries its own See Also and Version History sections, so its inbound link is not a one-line insertion."
  prohibitions:
    - "scripts/validation/check-phase-132.mjs must be byte-unchanged from the phase base — never edited, worked around additively."
    - "This phase authors zero check-phase validator files."
    - "scripts/validation/v1.20-audit-allowlist.json must be byte-unchanged from the phase base; neither of its two empty buckets is filled."
    - "Guide-level back-links from the operations guides and from recipes 01-04 to the new recipe stay deferred."
    - "docs/operations/patch-management/01-windows-wufb-rings.md is not edited by this phase."
    - "The third WinGet site in docs/admin-setup-apv2/03-device-preparation-policy.md stays on the backlog."
    - "No frontmatter re-stamp on docs/admin-setup-apv1/01-hardware-hash-upload.md or docs/operations/00-index.md."
    - "The three troubleshooting hubs must NOT reference the new recipe path."
    - "No third content commit; Commit B is the last one."
    - "D-66: no bare-noun platform blockquote token (the form barred by V-54-27 in check-phase-54.mjs) at line start outside a fence, in any file this plan writes, including this plan — the corpus-wide negative scans .planning/ as well as docs/."
---

<objective>
Close the phase: land the four inbound links every predecessor deferred here, make the single
Commit B, run the three terminal gates, and hand Phase 153 a needle-spec precise enough that it
does no re-derivation.

Purpose: INT-04's navigation-last mandate is not satisfied by the two hub files alone — Phase 148
handed forward a *class* of inbound wiring, not a pair of files, and three of the four sites were
dropped by an earlier draft. INT-05 is discharged as a content precondition here plus enforcement
in Phase 153. INT-06 is the assertion that none of it was greened with a baseline.

Output: Commit B (six content files), a green terminal gate set, and a needle-spec on two surfaces.

Out of scope by D-75: requirement-row status flips and the ROADMAP checkbox are **not** this phase's
decisions. The milestone close-gate flips requirement rows in one commit, and the execute-phase
machinery advances the roadmap and the state document. Do not flip `INT-01`..`INT-06` rows in
`.planning/REQUIREMENTS.md` and do not tick the Phase 152 roadmap checkbox by hand.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md
@.planning/phases/152-integration-registry-navigation-last-close/152-PATTERNS.md
@.planning/phases/152-integration-registry-navigation-last-close/152-03-SUMMARY.md
@scripts/validation/check-phase-137.mjs
</context>

<commit_contract>
**This overrides the executor's default per-task commit behaviour. Read it before Task 1.**

This plan makes **exactly one** content commit ("Commit B"), in Task 1, and it carries **six** files:
the four inbound-link host files edited in this task **plus** the two hub files Plan 03 left
modified in the working tree.

- Do not commit the four inbound-link edits separately from the two hub files. Commit B is the
  phase's navigation-last commit and INT-04 makes it a single unit.
- Do not make any content commit in Task 2 or Task 3. Task 3 writes `.planning/STATE.md`, which is a
  planning-artifact commit and a separate class — commit it separately from Commit B and never fold
  it in.
- After Commit B, `git log --oneline 56f55307..HEAD -- docs/` must return exactly **2**.
- Commit B's file list is derived from what is actually modified, not fixed in advance at two. An
  earlier draft pinned it to the two index files and would have collided with these four inbound
  links. Enumerate the real list, assert it is exactly six, then commit.
</commit_contract>

<discretion_resolved>
**The `01-windows-wufb-rings.md` companion link is DEFERRED, and here is the ground.**

CONTEXT leaves this to plan time and requires it be recorded either way. Recorded as deferred, for
one measured reason and explicitly *not* for another:

- **The reason it is deferred:** the ROADMAP and STATE both record, as a Phase 146 hard constraint,
  that Phase 146 is the **only phase in v1.21 that edits
  `docs/operations/patch-management/01-windows-wufb-rings.md`** and that its stub-and-move surgery
  must stay isolated. That constraint is milestone-level and still in force.
- **The reason it is NOT deferred:** it is *not* true that the file's validator pins bar a firmware
  link. `[MEASURED]` `check-phase-54.mjs` is the only validator naming that file, and its bare-ring
  negative scans word-boundary ring tokens with a preceding-40-character qualifier window — a
  Related Resources bullet linking `../firmware-bios/00-overview.md` contains no ring token at all
  and could not fire it. Claiming otherwise would be a fabricated premise, and this plan does not
  make one.
- **The routing already exists transitively:** `06-windows-driver-firmware-updates.md` already links
  to `01-windows-wufb-rings.md` in its Related Resources, and Task 1 adds the firmware-bios link to
  `06`, so the path from the rings guide to the firmware domain is one hop.

Record this disposition verbatim in the summary so the next phase inherits the reasoning rather
than the conclusion.
</discretion_resolved>

<tasks>

<task type="auto">
  <name>Task 1: The four inbound links, then Commit B — six files, one commit</name>
  <files>docs/operations/patch-management/06-windows-driver-firmware-updates.md, docs/operations/app-lifecycle/00-overview.md, docs/admin-setup-apv2/02-etg-device-group.md, docs/admin-setup-apv1/01-hardware-hash-upload.md, docs/index.md, docs/operations/00-index.md</files>
  <precondition>Plan 03 left `docs/index.md` and `docs/operations/00-index.md` modified and uncommitted; `git status --porcelain -- docs/` lists exactly those two paths before this task begins.</precondition>
  <read_first>
    - docs/operations/patch-management/06-windows-driver-firmware-updates.md — its `## Related Resources` section in full, including the four existing bullets, their two-line wrapped shape, and the em-dash convention; note the file uses LF line endings
    - docs/operations/app-lifecycle/00-overview.md — its `## Related Resources` section in full; note the file uses CRLF line endings
    - docs/admin-setup-apv2/02-etg-device-group.md — the supported-app-types sentence carrying the unlinked store product name, and the file's existing relative-link style; LF line endings
    - docs/admin-setup-apv1/01-hardware-hash-upload.md — the whole file. It carries a document identifier in its frontmatter and is C17-enrolled, and it has both a `## See Also` section and a `## Version History` table; LF line endings
    - docs/operations/firmware-bios/01-windows-dfci.md — its heading list, to read the prerequisites heading's exact text
    - docs/operations/patch-management/08-windows-app-updates.md — its heading list, to read the routing heading's exact text
    - scripts/validation/check-nav-hub-links.mjs — the slug function (lowercase, delete every character outside the lowercase-alphanumeric-space-underscore-hyphen set **in place**, then map each remaining space to one hyphen, never collapsing consecutive hyphens)
    - .planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md — D-51, D-52, D-53, D-54, D-55, D-56, D-65
  </read_first>
  <action>
Four edits, then one commit. Each host file's line endings differ — two are LF and one is CRLF —
so assert every replacement matched rather than assuming a bare string replace found its target.

**Link 1 — the reciprocal firmware link.** In
`docs/operations/patch-management/06-windows-driver-firmware-updates.md`, append one bullet to
`## Related Resources` linking `../firmware-bios/00-overview.md`. Match the existing bullets' shape
exactly: link text, then the dash separator that file uses, then a short clause naming what the
target covers. This link was added to Phase 149's summary after the phase verifier found it in **no
phase's inheritance**, and its deferral was made conditional on recording all four touch points so
the navigation phase would inherit a checklist rather than a guess. This is that checklist's first
item.

**Link 2 — the app-lifecycle return link.** In `docs/operations/app-lifecycle/00-overview.md`,
append one bullet to `## Related Resources` linking
`../patch-management/08-windows-app-updates.md`. Today that document links out and nothing links in.
Phase 148 declined to edit this directory on the explicit basis that inbound links belong here.

**Link 3 — the apv2 WinGet link.** In `docs/admin-setup-apv2/02-etg-device-group.md`, the
supported-app-types sentence names the Microsoft Store app type with its package manager in
parentheses as bare unlinked text. Turn that phrase into a markdown link whose target is
`../operations/patch-management/08-windows-app-updates.md` plus the fragment for that document's
routing heading — the one that reconciles Enterprise App Management, Store apps and the package
manager. **Derive the fragment, do not retype it:** read the heading text off
`08-windows-app-updates.md`, run it through the checker's slug rules by hand — lowercase, delete
the comma and the em dash in place, then map each remaining space to one hyphen — and note that
because the em dash had a space on each side, the result carries a **double hyphen** before the last
word. The expected value is
`enterprise-app-management-store-apps-and-winget--routing`; if your derivation disagrees with that,
trust your derivation and report the divergence, because the link checker never collapses
consecutive hyphens. Leave the sentence's other app types unlinked. The third site for this link, in
`03-device-preparation-policy.md`, stays on the backlog.

**Link 4 — the apv1 hardware-hash link.** In `docs/admin-setup-apv1/01-hardware-hash-upload.md`, add
a link to the DFCI guide's prerequisites-and-disqualifiers heading — target
`../operations/firmware-bios/01-windows-dfci.md#prerequisites-and-disqualifiers`, derived the same
way and confirmed against the heading on disk.

Two decisions this plan makes for that file, because it is not a one-line edit:
  - **Host section:** the link lands as a new bullet in `## See Also`, matching that section's
    existing bullet shape. This is an *outbound* link from an enrolled file, so the rule that
    governs linking to the file rather than a heading anchor does not apply — that rule governs
    links **into** that file, and the hand-forward describes this one as going to the prerequisites.
  - **Version History:** yes, a row lands. The file has a Version History table and this is a
    content edit; insert one row at the top of it, dated at execution, naming the added See Also
    link and citing `INT-04`, with the Author placeholder that file's existing rows use.
  - **Frontmatter:** not re-stamped. The file is past its review date, and re-stamping it is part of
    the corpus-wide past-due population that is deferred to a hygiene milestone.

**Prose constraint on all four edits.** The live corpus-walk patterns bind every new clause. Never
write the Autopatch product name immediately followed by the ring word; write
`Configuration Manager` rather than either barred legacy form; keep the mobile attestation service
name away from the word compliance.

**Then Commit B.** Enumerate `git status --porcelain -- docs/` and assert it lists exactly six
modified paths: the four host files above plus `docs/index.md` and `docs/operations/00-index.md`.
Stage those six explicitly by path — never `git add -A`, never `git add .`. Make one commit whose
subject names the navigation-last close and whose body enumerates all six files, names the four
inbound links by their source and target, and records the `01-windows-wufb-rings.md` deferral with
the reasoning from this plan's discretion block.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && n=$(git log --oneline 56f55307..HEAD -- docs/ | wc -l | tr -d " ") && f=$(git show --pretty=format: --name-only HEAD | grep -c .) && echo "doc_commits=$n commitB_files=$f" && [ "$n" = "2" ] && [ "$f" = "6" ] && node scripts/validation/check-nav-hub-links.mjs 2>&1 | tail -2'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'firmware-bios/00-overview.md' docs/operations/patch-management/06-windows-driver-firmware-updates.md` returns `1`, and the match is inside the `## Related Resources` section (assert by line number against the section's start and the next H2's start).
    - `grep -c '08-windows-app-updates.md' docs/operations/app-lifecycle/00-overview.md` returns `1`, inside `## Related Resources`.
    - `grep -c 'operations/patch-management/08-windows-app-updates.md#' docs/admin-setup-apv2/02-etg-device-group.md` returns `1`, and the fragment matches the slug derived from the target heading on disk. `grep -c 'Microsoft Store (WinGet)' docs/admin-setup-apv2/02-etg-device-group.md` still returns `1` — the phrase is now the link text, not deleted.
    - `grep -c '01-windows-dfci.md#prerequisites-and-disqualifiers' docs/admin-setup-apv1/01-hardware-hash-upload.md` returns `1`, and the match line number falls between the `## See Also` heading and the `## Version History` heading.
    - The first data row of the Version History table in `docs/admin-setup-apv1/01-hardware-hash-upload.md` is new, dated today, and cites `INT-04`.
    - The frontmatter of `docs/admin-setup-apv1/01-hardware-hash-upload.md` is byte-identical to its `56f55307` version (assert the first 12 lines).
    - `git diff --quiet 56f55307 -- docs/operations/patch-management/01-windows-wufb-rings.md` exits 0 — that file is untouched.
    - `git diff --quiet 56f55307 -- docs/admin-setup-apv2/03-device-preparation-policy.md` exits 0 — the third WinGet site is untouched.
    - `git log --oneline 56f55307..HEAD -- docs/` returns exactly `2` lines.
    - `git show --pretty=format: --name-only HEAD` lists exactly the six expected paths and nothing else.
    - `git status --porcelain -- docs/ scripts/` is empty after the commit.
    - Each of the four host files still reports its original line-ending class under `file`, and `git diff --numstat 56f55307 -- <path>` shows a single-digit changed-line count for each, not a whole-file rewrite.
    - `node scripts/validation/check-nav-hub-links.mjs` reports `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`.
    - `grep -icE 'Autopatch rings|System Center|SafetyNet' ` over the four host files returns `0` for each, and `grep -c 'SCCM'` returns `0` for each.
  </acceptance_criteria>
  <done>All four inbound links resolve, the apv1 guide gained a See Also bullet and a Version History row without a frontmatter re-stamp, Commit B is one commit of exactly six files, the phase stands at exactly two content commits, and the two explicitly deferred sites are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Run the three terminal gates and prove no baseline was introduced</name>
  <files>none written -- assertion-only task over the eleven authored documents, scripts/validation/check-phase-132.mjs, scripts/validation/check-phase-137.mjs, scripts/validation/v1.20-audit-allowlist.json and the whole scripts/validation/ tree</files>
  <read_first>
    - scripts/validation/check-nav-hub-links.mjs — its header, which states it exits 0 only on zero failures with no allowlist, ratchet or expected-failure list of any kind
    - scripts/validation/v1.20-audit-allowlist.json — its structure, and in particular the two empty buckets whose names correspond to the two failure classes this phase can cause
    - .planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md — D-60, D-64, D-67, D-68, D-69, D-70
  </read_first>
  <action>
Run the terminal gate set — **three gates, not two** — after the last commit, and prove the no-
baseline claim.

**Run the gates one at a time, in separate steps, and do not combine the apex with any other
command in a single invocation.** A URL-fragment or line-insertion change can trip line-pinned
allowlist exemptions, and interleaving the apex with another long-running check has previously made
attribution ambiguous.

1. `node scripts/validation/c17-eee-contract.mjs` — must report 236 files checked, 0 with
   violations, 0 total violations. This count is **unchanged from before the phase**, which is the
   cheapest observable proving that no operations document accidentally acquired a document
   identifier — that is, that INT-02 held. When you record it, label it as the C17 *file* count and
   say so explicitly, because the post-phase registry *row* count is also 236 and the collision will
   confuse a reviewer.
2. `node scripts/validation/check-nav-hub-links.mjs` — must report zero hub-presence failures and
   zero corpus-link failures. This checker admits no baseline by construction; every failure it
   reports is fixed in the content, never absorbed.
3. `node scripts/validation/check-phase-144.mjs` — the apex. Must report **101 pass, 0 fail, 0
   skipped**. It was 101/0/0 at the phase base. This is the only gate that executes the milestone
   audit and therefore the only one that runs the live corpus walk policing the new hub and link
   prose, so skipping it would leave that whole class unmeasured. If it reports any skip, treat the
   skip as a gap and investigate it — a skip is not a pass.

**If any gate is red, fix the content.** Never fix a red by adding an entry to
`scripts/validation/v1.20-audit-allowlist.json`. That file's two empty buckets are the reflex fix
for exactly the two failure classes this phase can cause, and filling either is precisely what
INT-06 forbids; the allowlist bump belongs to Phase 153.

**Then prove the byte-unchanged and no-baseline claims, against the phase base and not the working
tree.** Use `git diff --quiet 56f55307 -- <path>` per path, or a blob-SHA pin. A bare
`git diff --stat` reports only the unstaged tree at one instant, so a file edited and committed
mid-phase would show clean.

Assert, each as its own named result:
  - all eleven documents Phases 146-151 authored are byte-unchanged;
  - `scripts/validation/check-phase-132.mjs` is byte-unchanged — this is SC#5's subject, the frozen
    recipe-hub validator, and it is never edited;
  - `scripts/validation/check-phase-137.mjs` is byte-unchanged — record this one as **extra**,
    strictly stronger than SC#5 requires, not as the requirement;
  - `scripts/validation/v1.20-audit-allowlist.json` is byte-unchanged;
  - `scripts/validation/` as a whole is byte-unchanged, and no `check-phase-152.mjs` exists — this
    phase authors zero validators;
  - the three troubleshooting hubs do **not** reference the new recipe path;
  - only the registry, the two hub files, the four inbound-link host files and the two pipeline
    scripts changed under `docs/` and `scripts/` across the whole phase.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && node scripts/validation/c17-eee-contract.mjs 2>&1 | tail -1 && node scripts/validation/check-nav-hub-links.mjs 2>&1 | tail -1 && git diff --quiet 56f55307 -- scripts/validation/ && echo VALIDATORS_UNCHANGED'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations, 0 total violations`, and the record labels this as the C17 *file* count, distinguished by name from the registry's 236 *rows*.
    - `node scripts/validation/check-nav-hub-links.mjs` reports `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`.
    - `node scripts/validation/check-phase-144.mjs` reports 101 pass, 0 fail, 0 skipped, and was run as its own invocation, not combined with another gate.
    - The apex was run **after** Commit B — prove it by recording `git rev-parse HEAD` immediately before the apex invocation and showing it equals the Commit B SHA.
    - `git diff --quiet 56f55307 -- <path>` exits 0 for each of the eleven authored documents, reported as eleven separately named results.
    - `git diff --quiet 56f55307 -- scripts/validation/check-phase-132.mjs` exits 0, recorded as SC#5's subject.
    - `git diff --quiet 56f55307 -- scripts/validation/check-phase-137.mjs` exits 0, recorded as an extra assertion beyond SC#5.
    - `git diff --quiet 56f55307 -- scripts/validation/v1.20-audit-allowlist.json` exits 0.
    - `git diff --quiet 56f55307 -- scripts/validation/` exits 0, and `ls scripts/validation/check-phase-152.mjs` fails.
    - `grep -l 'recipes/05-' docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md` returns nothing.
    - `git diff --name-only 56f55307 HEAD -- docs/ scripts/` lists exactly nine paths: the registry, the two pipeline scripts, the generated filename map, the two hub files and the four inbound-link host files — count them and show the list.
    - No entry was added to any allowlist, ratchet or expected-failure list anywhere under `scripts/validation/` — proven by the tree-level byte-equality assertion above.
  </acceptance_criteria>
  <done>All three terminal gates are green at the Commit B tree, every authored document and every validator and the audit allowlist are byte-unchanged against the phase base, no validator was authored, and the changed-path set across the whole phase is exactly nine files.</done>
</task>

<task type="auto">
  <name>Task 3: Hand Phase 153 a fully pre-specified needle-spec, on both surfaces</name>
  <files>.planning/STATE.md, .planning/phases/152-integration-registry-navigation-last-close/152-04-SUMMARY.md</files>
  <read_first>
    - scripts/validation/check-phase-137.mjs — the complete working template: its header self-documentation block, its line-scoped bullet check and the comment explaining why a whole-file substring test false-matches, and its empty chain constants
    - scripts/validation/check-phase-132.mjs — the filename-map needle it carries, and its recipes arm, which does not cover this recipe and must not be edited
    - .planning/STATE.md — the `### Plan-Time Research Flags` block, which is one of the two required handoff surfaces
    - .planning/phases/152-integration-registry-navigation-last-close/152-CONTEXT.md — D-57, D-58, D-59, D-61, D-62, D-63
  </read_first>
  <action>
Write the needle-spec. This phase authors **zero** validators — the precedent is unanimous across
three milestones and the structural twin committed the handoff rather than the validator, and the
harness requirement assigns the whole leaf range plus the apex to Phase 153 with a chain range
generated by arithmetic.

Record explicitly that a third option existed and was **declined, not overlooked**: two non-leaf
validators in this tree were authored inside a content phase and a navigation phase respectively, so
either would have satisfied the "authored additively" wording with no ratification at all. It was
declined because the Phase 153 leaf would duplicate it.

**Transcribe every literal below. Compose nothing at execution time.** The one exception is a canary
equality, which the requirements demand be computed from the registry — that is a different object
and the two rules do not conflict.

The needle-spec Phase 153 implements as `scripts/validation/check-phase-152.mjs`:

*Shape.* Lightweight leaf. No chain — the chain lives only in the apex. Both chain constants are
empty, matching the twin's own two-line invariant.

*Header.* Must state that the needle is fully pre-specified, copied from this summary and from the
STATE entry, and not re-derived. Must also carry the corrected-literal warning: the frozen
recipe-hub validator's existing not-wired pattern **does not cover** the new recipe path and **must
not be edited** — the coverage is added additively here.

*Needles — master hub, `docs/index.md`:*
  - a recipes-table row whose link target is `recipes/05-enterprise-update-plan.md`;
  - the quick-nav bullet, tested **line-scoped**: extract every line matching the anchor
    `^- \[Device Configuration Recipes\]\(#device-configuration-recipes\)`, assert the match count is
    exactly 1, then test the fragment `enterprise update plan` against **that isolated line only**.
    A whole-file substring test false-matches lines above the bullet, which is why the twin's own
    header documents this;
  - the sub-heading literal `### Firmware and BIOS Governance`.

*Needles — operations index, `docs/operations/00-index.md`:*
  - the heading literal `## Firmware and BIOS Governance`;
  - all four Patch path fragments: `patch-management/05-linux-update-delivery.md`,
    `patch-management/06-windows-driver-firmware-updates.md`,
    `patch-management/07-windows-autopatch.md`, `patch-management/08-windows-app-updates.md`;
  - a Version History row citing `INT-04`.

*Needle — the filename map, `scripts/pipeline/filename-map.md`:* all eleven identifiers `RE-226`
through `RE-236` present. Carrying this is not optional — omitting it would be a straight regression
against the twin, which carries one.

*Negative needle — the three troubleshooting hubs:* `docs/common-issues.md`, `docs/quick-ref-l1.md`
and `docs/quick-ref-l2.md` must **not** contain the fragment `recipes/05-`.

*Self-reference guard:* the same shape the twin uses — the validator asserts its own file is present
at its own path, so a renamed or moved leaf fails loudly rather than silently passing.

**Land it on both surfaces.**

1. `.planning/STATE.md`, in the `### Plan-Time Research Flags` block: add one entry addressed to
   Phase 153, naming `check-phase-152.mjs`, listing every needle above in compressed form, naming
   this summary as the full spec, and stating that the frozen recipe-hub validator is never edited.
   Before writing, grep the block for an existing Phase-153 needle entry; if one is present, replace
   it rather than appending a second — this task must be safe to re-run.
2. This plan's summary, in full, as the authoritative spec.

A spec living only in a summary is not on the surface a Phase 153 planner reads; a spec living only
in STATE is too compressed to implement from. Both are required.

Commit `.planning/STATE.md` as a **planning-artifact** commit, separate from Commit B. Do not fold
it into Commit B and do not create a third content commit.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)" && grep -c "check-phase-152" .planning/STATE.md && n=$(git log --oneline 56f55307..HEAD -- docs/ | wc -l | tr -d " ") && echo "doc_commits=$n" && [ "$n" = "2" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'check-phase-152' .planning/STATE.md` returns exactly `1` — one entry, not two, even if the task is re-run.
    - The STATE entry appears inside the `### Plan-Time Research Flags` block — assert by line number against that heading and the next `###` heading.
    - The STATE entry names all of: the master-hub recipes row, the line-scoped bullet check, the operations-index heading, the four Patch fragments, the filename-map identifiers, and the three-hub negative.
    - The STATE entry states that the frozen recipe-hub validator is never edited.
    - `152-04-SUMMARY.md` carries the full needle-spec with every literal transcribed, including the anchor regex, the bullet fragment, both heading literals, the four Patch fragments, the eleven identifiers, and the three hub paths.
    - The summary records the declined third option — authoring a non-leaf validator in this phase — with the reason it was declined.
    - The summary records the two-obligation discharge of SC#5 by name: content precondition here, enforcement in Phase 153.
    - `git log --oneline 56f55307..HEAD -- docs/` still returns exactly `2` — the STATE commit added no content commit.
    - `git log --oneline -1 -- .planning/STATE.md` shows a commit distinct from the Commit B SHA.
    - `ls scripts/validation/check-phase-152.mjs` fails — the spec was written, the validator was not.
  </acceptance_criteria>
  <done>The needle-spec exists in full in the summary and in compressed form as exactly one entry in STATE.md's Plan-Time Research Flags block, the STATE write is idempotent, the handoff is committed as a planning artifact separate from Commit B, and no validator file was created.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**New inbound links (4):** one Related Resources bullet in
`docs/operations/patch-management/06-windows-driver-firmware-updates.md`; one Related Resources
bullet in `docs/operations/app-lifecycle/00-overview.md`; one inline link on the existing
supported-app-types sentence in `docs/admin-setup-apv2/02-etg-device-group.md`; one See Also bullet
in `docs/admin-setup-apv1/01-hardware-hash-upload.md`.

**New Version History row (1):** in `docs/admin-setup-apv1/01-hardware-hash-upload.md`.

**New anchor references (2, both to pre-existing targets):**
`#enterprise-app-management-store-apps-and-winget--routing` on
`docs/operations/patch-management/08-windows-app-updates.md` and `#prerequisites-and-disqualifiers`
on `docs/operations/firmware-bios/01-windows-dfci.md`. Both headings already exist; only the
references to them are new.

**New handoff surface entry (1):** one entry naming `check-phase-152.mjs` in the
`### Plan-Time Research Flags` block of `.planning/STATE.md`.

**New needle-spec identifiers (declared here, implemented in Phase 153):** the checks this phase
specifies for `check-phase-152.mjs` — the master-hub recipes row, the line-scoped quick-nav bullet,
both Firmware headings, the four Patch fragments, the filename-map identifier set, the three-hub
negative, and the self-reference guard. None of these exists as code in this phase; a drift check
must not treat them as pre-existing symbols.

**No validator file is created.** No registry row, no filename-map row, no `doc_id`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound link -> anchor fragment | Four newly authored references cross into headings owned by other documents; a wrong slug is a dangling anchor that routes a reader nowhere. |
| phase close -> next milestone | The needle-spec is the only artefact carrying this phase's enforcement obligation forward; if it is imprecise, Phase 153 re-derives it and can re-derive it wrong. |
| gate red -> allowlist | The cheapest way to green any of the three terminal gates is to add an accepted-violation entry, which is exactly what the closing requirement forbids. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-18 | Elevation of Privilege | `scripts/validation/v1.20-audit-allowlist.json` and any other allowlist under `scripts/validation/` | high | mitigate | Task 2 asserts byte-equality against the phase base for the named allowlist and for the entire `scripts/validation/` tree, and the plan states that a red gate is fixed in content and never absorbed. The two empty buckets in that file are named explicitly as the reflex fix that is forbidden. |
| T-152-19 | Tampering | `scripts/validation/check-phase-132.mjs`, the frozen recipe-hub validator | high | mitigate | Byte-equality against the phase base is a Task 2 acceptance criterion, proved with a per-path assertion against the base commit rather than a working-tree diff, because a mid-phase edit-and-commit would show clean under the latter. The coverage gap is closed additively by a successor validator in the next phase. |
| T-152-20 | Spoofing | the two anchor fragments authored in Task 1 | medium | mitigate | Fragments are derived from the target headings on disk using the checker's own slug rules rather than retyped, the double-hyphen artefact is called out in advance because the checker never collapses consecutive hyphens, and the link checker is a task `<verify>` that must report zero corpus-link failures. |
| T-152-21 | Repudiation | the needle-spec handoff | medium | mitigate | The spec lands on two surfaces with an idempotent STATE write guarded by a pre-write grep, and every literal is transcribed rather than composed, so the next phase implements rather than re-derives. |
| T-152-22 | Information Disclosure | an unmeasured gate class at phase close | high | mitigate | The terminal gate set is three, not two: the apex is the only gate that executes the milestone audit and therefore the only one that runs the live corpus walk over the newly written hub and link prose. It is run as its own invocation after the last commit, at the Commit B SHA, and any skip is treated as a gap rather than a pass. |
| T-152-23 | Denial of Service | a third content commit breaking the phase's two-commit contract | low | mitigate | The commit contract is stated at the top of the plan, the STATE write is explicitly classed as a planning-artifact commit, and an acceptance criterion asserts the `docs/` commit count is exactly 2 after every task. |
| T-152-SC | Tampering | npm/pip/cargo installs | low | accept | This plan performs zero package-manager installs. It edits markdown and one planning artifact and runs three Node validators that use built-ins only. No package is introduced, so no legitimacy audit applies. |

ASVS level: 1. Blocking threshold: high. All four high-severity entries are mitigated by computed
acceptance criteria measured against the phase base commit, not by review vigilance.
</threat_model>

<verification>
- All four inbound links present, each in its named host section, each resolving under the link checker.
- `git log --oneline 56f55307..HEAD -- docs/` returns exactly 2; Commit B contains exactly 6 files.
- `node scripts/validation/c17-eee-contract.mjs` -> 236 files checked, 0 violations (labelled as the file count).
- `node scripts/validation/check-nav-hub-links.mjs` -> 0 hub-presence, 0 corpus-link failures.
- `node scripts/validation/check-phase-144.mjs` -> 101 pass, 0 fail, 0 skipped, run alone, at the Commit B SHA.
- Eleven authored documents byte-unchanged against `56f55307`, asserted per path.
- `check-phase-132.mjs`, `check-phase-137.mjs`, `v1.20-audit-allowlist.json` and the whole `scripts/validation/` tree byte-unchanged against `56f55307`.
- No `scripts/validation/check-phase-152.mjs` exists.
- The three troubleshooting hubs contain no reference to the new recipe path.
- `docs/operations/patch-management/01-windows-wufb-rings.md` and `docs/admin-setup-apv2/03-device-preparation-policy.md` byte-unchanged against `56f55307`.
- Exactly one `check-phase-152` entry in `.planning/STATE.md`, inside the Plan-Time Research Flags block.
- The changed-path set across the whole phase under `docs/` and `scripts/` is exactly nine files.
</verification>

<success_criteria>
Every inbound link a predecessor deferred to this phase has landed and resolves; the phase closes
with exactly two content commits; all three terminal gates are green at the final tree; no frozen
surface, authored document or allowlist changed; this phase authored zero validators; and Phase 153
inherits a needle-spec on two surfaces that it can implement without re-deriving a single literal.
</success_criteria>

<output>
Create `.planning/phases/152-integration-registry-navigation-last-close/152-04-SUMMARY.md` when done.

The summary must carry:
- the four inbound links, each with its source path, host section, target path and derived fragment,
  and the derivation shown for both fragments;
- the wufb-rings deferral with its recorded ground, including the explicit statement that the
  file's validator pins do **not** bar the link and that the deferral rests on the milestone-level
  isolation constraint instead;
- Commit B's SHA and its six-file list;
- all three terminal gate outputs verbatim, each labelled, with the apex's pass/fail/skip triple and
  the SHA it ran against;
- the eleven per-path byte-unchanged results and the four validator/allowlist results, each named;
- the nine-path phase-wide changed-file list;
- the **full needle-spec**, every literal transcribed, as the authoritative source Phase 153 copies;
- the declined third option and why;
- the two-obligation discharge of SC#5, named;
- the C17 file count and the registry row count named apart from each other.
</output>
