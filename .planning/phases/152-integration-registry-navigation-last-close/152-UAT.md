---
status: testing
phase: 152-integration-registry-navigation-last-close
source: [152-VERIFICATION.md]
started: 2026-08-27T17:30:00Z
updated: 2026-08-27T17:30:00Z
---

## Current Test

number: 1
name: Decide whether docs/index.md:276's recipes section lead-in is amended now or deferred
expected: |
  The lead-in should not universally promise "a concrete, reproducible device configuration
  end-to-end ... from zero to verified end state" when Recipe #5 self-describes at
  docs/recipes/05-enterprise-update-plan.md:30 as "A tenant-wide configuration plan, not a
  per-platform procedure guide."
awaiting: user response

## Tests

### 1. Amend or defer the recipes section lead-in at docs/index.md:276

expected: The lead-in should not universally promise "a concrete, reproducible device configuration end-to-end ... from zero to verified end state" when Recipe #5 self-describes as "A tenant-wide configuration plan, not a per-platform procedure guide."
result: [pending]

Context: this phase amended the sibling quick-nav lead-in at line 38 to "provisioning **and
governance** recipes" (D-46) for exactly this reason, then left line 276 asserting the opposite.
`docs/index.md` is RE-219 and ships as a `.docx`, so the contradiction reaches readers. No
validator can see it — apex 101/0/0, C17 236/0 and the link checker 0/0 are all green with the
contradiction in place. Fixing it costs a third content commit against D-01's two-commit contract,
which is the owner's call.

### 2. Fix or carry forward the `v1.17` VERSION default in build-publish-bundle.mjs:40

expected: Either the default becomes fail-closed (require `--version`) or D-23's "always pass `--version`" is carried forward as an explicit Phase 153 precondition.
result: [pending]

Context: inherited debt, not a Phase 152 regression — `git log -S` dates the default to
`2eae4653` in Phase 127, and this phase touched no line containing `VERSION` or `v1.17`. Phase 152
discharged its own obligation: `dist/docs-library-v1.21.0.zip` exists and
`dist/docs-library-v1.17.zip` is absent. The forward risk is real: Phase 153 SC#4 regenerates the
bundle, and a forgotten flag there silently writes a misnamed archive over a reserved name in a
gitignored directory with no recovery path.

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps
