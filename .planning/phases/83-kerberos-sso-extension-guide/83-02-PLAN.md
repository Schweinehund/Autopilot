---
phase: 83-kerberos-sso-extension-guide
plan: 02
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - docs/admin-setup-macos/10-kerberos-sso-extension.md
autonomous: true
requirements: [KRB-02, KRB-03]
must_haves:
  truths:
    - "Guide 10 documents the macOS 14.6 floor for PSSO + Kerberos TGT integration via usePlatformSSOTGT, the Company Portal version prerequisites, custom_tgt_setting placement, and the cosmetic Not signed in note"
    - "Guide 10 documents Azure Files Cloud-Kerberos as a limited-preview callout (not GA, not the primary path)"
    - "Guide 10 documents app-sso platform -s + klist as the canonical diagnostic pair with tgt_ad/tgt_cloud interpretation, ticket lifecycle, and realm/KDC reachability; app-sso diagnose does not appear"
    - "Guide 10 is a complete suite-conformant document with Verification, See Also, and a version-history table"
  artifacts:
    - path: "docs/admin-setup-macos/10-kerberos-sso-extension.md"
      provides: "PSSO TGT integration, Cloud Kerberos preview callout, diagnostics, Verification, See Also, version-history -- completing guide 10"
      contains: "app-sso platform -s"
      min_lines: 200
  key_links:
    - from: "docs/admin-setup-macos/10-kerberos-sso-extension.md"
      to: "../_glossary-macos.md#kerberos-sso-extension"
      via: "See Also bullet"
      pattern: "_glossary-macos\\.md#kerberos-sso-extension"
    - from: "docs/admin-setup-macos/10-kerberos-sso-extension.md"
      to: "09-enterprise-sso-plugin-migration.md"
      via: "See Also bullet"
      pattern: "09-enterprise-sso-plugin-migration\\.md"
---

<objective>
Complete the new guide `docs/admin-setup-macos/10-kerberos-sso-extension.md` (started by 83-01). Append the PSSO + Kerberos TGT integration section (KRB-02: `usePlatformSSOTGT`, `custom_tgt_setting`, macOS 14.6 floor, cosmetic "Not signed in" note), the Cloud Kerberos limited-preview callout (D-04), the diagnostics section (KRB-03: `app-sso platform -s` + `klist`, `tgt_ad`/`tgt_cloud` interpretation, ticket lifecycle, realm/KDC reachability), the Verification section, the Configuration-Caused Failures table, the `## See Also` section, and the closing version-history table (D-07 anchors).

This plan depends on 83-01 because it appends to the SAME file (shared-file = sequential dependency, wave 2). It delivers KRB-02 and KRB-03.

Purpose: Finish guide 10 as a complete, suite-conformant, accuracy-trap-proof reference covering the PSSO-coexistence TGT-sharing spine (D-01) and the locked diagnostic command set (D-10).
Output: A complete `docs/admin-setup-macos/10-kerberos-sso-extension.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-kerberos-sso-extension-guide/83-CONTEXT.md
@.planning/phases/83-kerberos-sso-extension-guide/83-RESEARCH.md
@.planning/phases/83-kerberos-sso-extension-guide/83-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/83-kerberos-sso-extension-guide/83-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PSSO + Kerberos TGT integration section + Cloud Kerberos preview callout</name>
  <files>docs/admin-setup-macos/10-kerberos-sso-extension.md</files>
  <read_first>
    - docs/admin-setup-macos/10-kerberos-sso-extension.md (the in-progress file from 83-01 -- read current end state before appending)
    - .planning/phases/83-kerberos-sso-extension-guide/83-RESEARCH.md (## Verified Payload Structure: custom_tgt_setting Values table + Cloud Kerberos Profile Keys table; Pitfall 4 custom_tgt_setting placement; Pitfall 5 PSSO-first ordering; ## State of the Art Cloud Kerberos row)
    - .planning/phases/83-kerberos-sso-extension-guide/83-CONTEXT.md (D-02, D-03, D-04, D-12 cosmetic note)
    - .planning/research/PITFALLS.md (§K-2 conflation #4 -- Kerberos requires-PSSO is wrong; standalone exists)
  </read_first>
  <action>
    Append a `## Configuration: PSSO + Kerberos TGT Integration` section (KRB-02, the D-01 spine). Document: `usePlatformSSOTGT: true` enables PSSO-issued TGT sharing (macOS 14.6+ floor, D-03); `performKerberosOnly: true` and `syncLocalPassword: false` guidance for PSSO-combined deployments (from RESEARCH Key-Value table). Include the `custom_tgt_setting` values table (0 both / 1 on-prem only / 2 cloud only / 3 none) from RESEARCH, with the Pitfall-4 placement warning: `custom_tgt_setting` belongs in the PSSO Settings Catalog policy's `ExtensionData` (the `com.microsoft.CompanyPortalMac.ssoextension` profile), NOT in the Kerberos .mobileconfig; tag this `[ASSUMED]` per RESEARCH open question and instruct the executor to confirm against Microsoft Learn at authoring time. Note Company Portal 2508+ gates `custom_tgt_setting`. Add the cosmetic "Not signed in" menu-bar note (D-12): a working PSSO-TGT deployment may show "Not signed in" in the Kerberos menu-bar extra; this is cosmetic and must not be read as failure.

    Then append a `## Configuration: Cloud Kerberos Profile (Limited Preview)` section (D-04). Use a clearly-labeled preview callout (this is NOT GA, NOT the primary path). Include from RESEARCH "Cloud Kerberos Profile Keys": Realm `KERBEROS.MICROSOFTONLINE.COM` (ALL CAPS), Hosts `["windows.net", ".windows.net"]`, `preferredKDCs` kkdcp:// endpoint, `usePlatformSSOTGT` true. State the Azure Files preview status with the azurefiles@microsoft.com onboarding contact. Add an [ASSUMED]/re-verify note (RESEARCH A3): the executor must re-confirm Azure Files Cloud-Kerberos is still in limited preview at authoring time and adjust the callout if it has gone GA.

    Per K-2 #4, briefly note standalone Kerberos-without-PSSO exists but is outside v1.10 scope (D-02 -- a note only, never a co-equal path). Use ` -- ` em-dash and backtick code-spans.
  </action>
  <verify>
    <automated>grep -q '## Configuration: PSSO + Kerberos TGT Integration' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'custom_tgt_setting' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -qi 'Not signed in' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'KERBEROS.MICROSOFTONLINE.COM' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -qi 'limited preview' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'azurefiles@microsoft.com' docs/admin-setup-macos/10-kerberos-sso-extension.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Configuration: PSSO + Kerberos TGT Integration` heading present; `usePlatformSSOTGT` documented with macOS `14.6` floor.
    - `custom_tgt_setting` values table present (values 0/1/2/3); Pitfall-4 placement warning present (key belongs in PSSO Settings Catalog policy, NOT the Kerberos .mobileconfig) and tagged `[ASSUMED]`.
    - Company Portal `2508` gate stated for `custom_tgt_setting`.
    - Cosmetic "Not signed in" note present and explicitly labeled cosmetic / not-a-failure (D-12).
    - `## Configuration: Cloud Kerberos Profile (Limited Preview)` heading present; realm `KERBEROS.MICROSOFTONLINE.COM` (ALL CAPS); `limited preview` wording; `azurefiles@microsoft.com` contact; re-verify-status note present.
    - Standalone-without-PSSO mentioned only as an out-of-scope note (D-02), not a co-equal configuration path.
  </acceptance_criteria>
  <done>PSSO TGT integration section (with custom_tgt_setting placement guard + cosmetic note) and Cloud Kerberos limited-preview callout appended.</done>
</task>

<task type="auto">
  <name>Task 2: Diagnostics + Verification sections (app-sso platform -s + klist)</name>
  <files>docs/admin-setup-macos/10-kerberos-sso-extension.md</files>
  <read_first>
    - docs/admin-setup-macos/10-kerberos-sso-extension.md (current end state after Task 1)
    - .planning/phases/83-kerberos-sso-extension-guide/83-RESEARCH.md (## Code Examples: app-sso platform -s diagnostic output + klist diagnostic; ## D-13 Resolution app-sso kerberos finding; Don't Hand-Roll ticket-lifecycle one-sentence note)
    - .planning/phases/83-kerberos-sso-extension-guide/83-CONTEXT.md (D-10, D-11, D-12, D-13)
    - .planning/research/PITFALLS.md (§K-3 banned app-sso diagnose; klist -v version-variant warning; no kinit/kdestroy)
    - docs/admin-setup-macos/07-platform-sso-setup.md (lines around `## Verification` for the suite Verification heading style)
  </read_first>
  <action>
    Append a `## Verification` section (D-07 anchor) covering the locked diagnostic pair (D-10). Document `app-sso platform -s` as the Platform SSO state command and bind the output interpretation (D-12): `tgt_ad` present = on-prem AD TGT functioning; `tgt_cloud` present = Cloud Kerberos / Azure Files TGT available; neither present = `usePlatformSSOTGT` not set or PSSO registration incomplete. Document `klist` (D-11: pin the version-stable bare `klist` invocation -- do NOT use `klist -v` which varies by macOS version) showing the ticket cache, with the on-prem `krbtgt/CONTOSO.COM@CONTOSO.COM` and cloud `krbtgt/KERBEROS.MICROSOFTONLINE.COM@...` service-principal examples from RESEARCH. Reuse the cosmetic "Not signed in" disambiguation here so L1/L2 do not misread a working deployment as failed (D-12). Add a one-sentence ticket-lifecycle note (from Don't Hand-Roll): the extension proactively renews TGTs on network state change; default TGT lifetime is set by the KDC (typically 10 hours). Add a brief realm/KDC reachability note appropriate to an Intune admin (no AD DC-diagnostic tooling -- K-4).

    Per D-13 / K-3: `app-sso diagnose` is PROHIBITED -- it must NOT appear anywhere. Do NOT include `kinit` or `kdestroy` as admin diagnostic steps. The guide ships WITHOUT `app-sso kerberos -l` by default (D-13); if the executor verifies it against a live macOS 14.6+ binary, it MAY be added as an optional "list configured realms" step noting it is the default kerberos mode (not a new subcommand), else OMIT it.

    Then append a `## Configuration-Caused Failures` table using the guide 07/09 4-column `| Misconfiguration | Portal | Symptom | Runbook |` format. Rows: wrong `Type` (Redirect instead of Credential) -> silent TGT failure; wrong `ExtensionIdentifier` (Microsoft value) -> extension fails to load; missing `usePlatformSSOTGT` -> no PSSO TGT sharing. Use `-- (Phase 85)` as the Runbook placeholder (runbook #28 not yet authored). Use ` -- ` em-dash and backtick code-spans.
  </action>
  <verify>
    <automated>grep -q '## Verification' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'app-sso platform -s' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'tgt_ad' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'tgt_cloud' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -qw 'klist' docs/admin-setup-macos/10-kerberos-sso-extension.md && [ "$(grep -c 'app-sso diagnose' docs/admin-setup-macos/10-kerberos-sso-extension.md)" = "0" ] && [ "$(grep -c 'kdestroy' docs/admin-setup-macos/10-kerberos-sso-extension.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `## Verification` heading present; both `app-sso platform -s` and `klist` documented as the canonical pair (D-10).
    - `tgt_ad` and `tgt_cloud` interpretation bound per D-12 (what each signals; neither = misconfig).
    - `klist` invocation is the bare version-stable form; `klist -v` is NOT presented as a step (D-11).
    - Cosmetic "Not signed in" disambiguation reused in Verification; one-sentence ticket-lifecycle note present; Intune-admin-level realm/KDC reachability note present.
    - PROHIBITED-command guard: `app-sso diagnose` appears 0 times; `kdestroy` 0 times; `kinit` not presented as an admin diagnostic step.
    - `## Configuration-Caused Failures` table present with the three misconfiguration rows and `-- (Phase 85)` runbook placeholder.
  </acceptance_criteria>
  <done>Verification/diagnostics section authored with the locked command pair, bound output interpretation, and the prohibited-command guard satisfied; Configuration-Caused Failures table present.</done>
</task>

<task type="auto">
  <name>Task 3: See Also section + version-history table (guide 10 completion)</name>
  <files>docs/admin-setup-macos/10-kerberos-sso-extension.md</files>
  <read_first>
    - docs/admin-setup-macos/10-kerberos-sso-extension.md (current end state after Task 2)
    - .planning/phases/83-kerberos-sso-extension-guide/83-PATTERNS.md (See Also section format -- the full guide-10 See Also target list; Version-history table format -- the initial-row target)
    - docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md (lines 163-176: See Also + version-history analog)
  </read_first>
  <action>
    Append a `## See Also` section (D-07 anchor) using the guide 09 `- [Label](path.md) -- description` bullet format with the double-dash separator. Include the full target list from PATTERNS "Guide 10 See Also must include": cross-links to 07-platform-sso-setup.md, 08-auth-methods-deep-dive.md, 09-enterprise-sso-plugin-migration.md; bare glossary links to ../_glossary-macos.md#kerberos-sso-extension, #platform-sso, #enterprise-sso-plug-in; and the two external authoritative links (Apple Kerberos SSO Extension deployment reference at developer.apple.com; Microsoft Learn "Enable Kerberos SSO in Platform SSO"). Note: the glossary anchor `#kerberos-sso-extension` is created by 83-03 -- it is a stable slug regardless of authoring order, so linking it now is correct.

    Then append the closing version-history table (D-07 anchor) using the PATTERNS "For guide 10 -- initial row only" format: a `---` rule, then a `| Date | Change | Author |` table with the single initial row `| 2026-06-22 | Phase 83 (KRB-01..04): initial Kerberos SSO Extension guide | -- |`. The Author column is `--` (two hyphens) per suite convention. The version-history table is the final element of the file.
  </action>
  <verify>
    <automated>grep -q '## See Also' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q '_glossary-macos.md#kerberos-sso-extension' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q '09-enterprise-sso-plugin-migration.md' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'Phase 83 (KRB-01..04): initial Kerberos SSO Extension guide' docs/admin-setup-macos/10-kerberos-sso-extension.md && [ "$(grep -c '—' docs/admin-setup-macos/10-kerberos-sso-extension.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `## See Also` heading present with cross-links to guides 07, 08, 09; glossary links `#kerberos-sso-extension`, `#platform-sso`, `#enterprise-sso-plug-in`; and external Apple + Microsoft Learn links.
    - Version-history table is the final element: contains the row `Phase 83 (KRB-01..04): initial Kerberos SSO Extension guide` with Author `--`.
    - Whole-file convention check: em-dash `—` appears 0 times; ` -- ` double-dash used as em-dash substitute.
    - Whole-file accuracy gate (final): `com.apple.AppSSOKerberos.KerberosExtension` present; `app-sso diagnose` absent (0); `UBF8T346G9` absent (0).
  </acceptance_criteria>
  <done>Guide 10 is complete and suite-conformant: See Also and version-history table present; whole-file accuracy and convention gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide reader (Intune / L1 / L2 admin) -> production MDM payload & diagnostic terminal | An admin copies plist values and runs documented diagnostic commands from this doc. The threat surface is the accuracy/safety of the published guidance -- including not publishing a destructive or unverified CLI command. |

This is a documentation-only plan: no executable code, user input, auth surface, or network/data operations are produced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-03 | Tampering (unsafe guidance) | Diagnostics CLI in guide 10 Verification | mitigate | Acceptance criteria PROHIBIT `app-sso diagnose` (unverified, banned since v1.9 Phase 80) and `kdestroy`/`kinit` write-commands in admin steps; `klist` pinned to version-stable form (no `klist -v`). Only read-only diagnostics (`app-sso platform -s`, `klist`) are published (D-10/D-11/D-13). |
| T-83-04 | Tampering (incorrect guidance) | custom_tgt_setting / Cloud Kerberos values | mitigate | `custom_tgt_setting` placement tagged `[ASSUMED]` with executor confirmation step; Cloud Kerberos preview status flagged for re-verification at authoring time (RESEARCH A3/A4). Prevents shipping a stale GA/preview claim or wrong-profile key placement. |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (documentation only). No package legitimacy gate applicable. |

Block-on severity: high. None expected -- the only CLI published is read-only and verified; the prohibited-command guard is an explicit acceptance criterion.
</threat_model>

<verification>
- Guide 10 is complete: sections `## Configuration: PSSO + Kerberos TGT Integration`, `## Configuration: Cloud Kerberos Profile (Limited Preview)`, `## Verification`, `## Configuration-Caused Failures`, `## See Also`, and the closing version-history table are all present.
- KRB-02 literals: `usePlatformSSOTGT`, `custom_tgt_setting`, `14.6`, `2508`, "Not signed in", `KERBEROS.MICROSOFTONLINE.COM`, `azurefiles@microsoft.com`, `limited preview`.
- KRB-03 literals: `app-sso platform -s`, `tgt_ad`, `tgt_cloud`, `klist`.
- Prohibited absent: `app-sso diagnose` (0), `kdestroy` (0), em-dash `—` (0).
</verification>

<success_criteria>
- KRB-02 delivered: macOS 14.6 floor, Company Portal prereqs, `usePlatformSSOTGT`, custom_tgt_setting placement, cosmetic note, Cloud Kerberos limited-preview callout.
- KRB-03 delivered: `app-sso platform -s` + `klist` canonical pair, tgt_ad/tgt_cloud interpretation, ticket lifecycle, realm/KDC reachability.
- Guide 10 is a complete, suite-conformant document.
</success_criteria>

<output>
Create `.planning/phases/83-kerberos-sso-extension-guide/83-02-SUMMARY.md` when done.
</output>
