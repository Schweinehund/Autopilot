---
phase: 83-kerberos-sso-extension-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-macos/10-kerberos-sso-extension.md
autonomous: true
requirements: [KRB-01]
must_haves:
  truths:
    - "docs/admin-setup-macos/10-kerberos-sso-extension.md exists with the suite frontmatter, platform-gate header, and a three-way disambiguation box"
    - "The guide states the Kerberos extension identity literal com.apple.AppSSOKerberos.KerberosExtension, Type Credential (not Redirect), and TeamIdentifier apple"
    - "The guide documents the Intune Custom Template (.mobileconfig) deployment path with realm (ALL CAPS) and Hosts array payload values, and a bounded on-prem AD callout"
  artifacts:
    - path: "docs/admin-setup-macos/10-kerberos-sso-extension.md"
      provides: "New Kerberos SSO Extension admin guide -- identity, payload, Custom Template deployment, disambiguation, prerequisites"
      contains: "com.apple.AppSSOKerberos.KerberosExtension"
      min_lines: 90
  key_links:
    - from: "docs/admin-setup-macos/10-kerberos-sso-extension.md"
      to: "07-platform-sso-setup.md"
      via: "platform-gate header + Prerequisites link"
      pattern: "07-platform-sso-setup\\.md"
    - from: "docs/admin-setup-macos/10-kerberos-sso-extension.md"
      to: "../_glossary-macos.md"
      via: "platform-gate header glossary link"
      pattern: "_glossary-macos\\.md"
---

<objective>
Author the first half of the new guide `docs/admin-setup-macos/10-kerberos-sso-extension.md`: suite frontmatter, platform-gate header, the K-2 three-way disambiguation box ("What This Guide Is NOT"), the extension identity / payload (.mobileconfig Custom Template upload path with the verified Microsoft Learn plist), the bounded on-prem AD realm/KDC callout (D-08/D-09), and the Prerequisites section (D-07 anchor).

This plan delivers KRB-01 (extension identity, payload type, Custom Template deployment, realm/KDC + on-prem AD prerequisites, disambiguation). Plan 83-02 appends the PSSO TGT integration, Cloud Kerberos, diagnostics, Verification, See Also, and version-history sections to the SAME file — which is why 83-02 depends on this plan (sequential, shared file).

Purpose: Give Intune admins a complete, accuracy-trap-proof reference for deploying the Apple Kerberos SSO extension via Intune Custom Template, framed PSSO-coexistence-primary (D-01).
Output: `docs/admin-setup-macos/10-kerberos-sso-extension.md` through the Prerequisites section (the file is completed by 83-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-kerberos-sso-extension-guide/83-CONTEXT.md
@.planning/phases/83-kerberos-sso-extension-guide/83-RESEARCH.md
@.planning/phases/83-kerberos-sso-extension-guide/83-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guide 10 head -- frontmatter, platform-gate header, disambiguation box</name>
  <files>docs/admin-setup-macos/10-kerberos-sso-extension.md</files>
  <read_first>
    - docs/admin-setup-macos/07-platform-sso-setup.md (lines 1-35: frontmatter + platform-gate header + Prerequisites analog)
    - docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md (lines 25-33: `## Product-Name Disambiguation` 4-column table analog; lines 140-146: existing coexistence framing guide 10 must not contradict)
    - .planning/phases/83-kerberos-sso-extension-guide/83-PATTERNS.md (Frontmatter block, Platform-gate header blockquote, Disambiguation table format sections -- verbatim analog excerpts)
    - .planning/research/PITFALLS.md (§K-2 three-way disambiguation table)
  </read_first>
  <action>
    Create the new file. Emit the YAML frontmatter with the five suite keys in order per D-07: last_verified 2026-06-22, review_by 2026-09-22, applies_to ADE, audience admin, platform macOS (copy keys verbatim from guide 07; only dates differ). Add the H1 title "macOS Kerberos SSO Extension". Add the platform-gate blockquote using the three-line structure from PATTERNS: line 1 bold "**Platform gate:**" describing Kerberos SSO extension configuration via Intune Custom Template (.mobileconfig) for PSSO-integrated deployments; line 2 pointing to Platform SSO Setup (07-platform-sso-setup.md) as prerequisite; line 3 verbatim glossary link to ../_glossary-macos.md.

    Add a `## What This Guide Is NOT` disambiguation box (D-07, K-2). Use the guide 09 4-column table style with headers `| Term | What It Is | Configuration Surface |`. Include three rows distinguishing: the Apple Kerberos SSO extension (identifier `com.apple.AppSSOKerberos.KerberosExtension`, Type Credential, Apple-native, on-prem AD TGT only); Platform SSO (identifier `com.microsoft.CompanyPortalMac.ssoextension`, Type Redirect, Entra ID auth); and the Microsoft Enterprise SSO plug-in (Microsoft umbrella product, Redirect). State explicitly that the Kerberos extension does NOT handle Entra ID auth and is NOT shipped by Microsoft (K-2 conflation traps). Use the ` -- ` double-dash em-dash convention throughout; never `—` or ` - `. Use backtick code-spans for all identifiers.
  </action>
  <verify>
    <automated>test -f docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'com.apple.AppSSOKerberos.KerberosExtension' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'com.microsoft.CompanyPortalMac.ssoextension' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'What This Guide Is NOT' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'Platform gate' docs/admin-setup-macos/10-kerberos-sso-extension.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/admin-setup-macos/10-kerberos-sso-extension.md` exists.
    - Frontmatter contains all five keys: `last_verified`, `review_by`, `applies_to`, `audience`, `platform`.
    - Literal `**Platform gate:**` present; line 2 links `07-platform-sso-setup.md`; line 3 links `../_glossary-macos.md`.
    - `## What This Guide Is NOT` heading present with a 3-row table containing BOTH `com.apple.AppSSOKerberos.KerberosExtension` AND `com.microsoft.CompanyPortalMac.ssoextension` as distinct labeled values.
    - No em-dash character `—` anywhere in the file (suite uses ` -- `): `grep -c '—' docs/admin-setup-macos/10-kerberos-sso-extension.md` returns 0.
  </acceptance_criteria>
  <done>Frontmatter, platform-gate header, and three-way disambiguation box authored with both extension identifiers shown distinctly.</done>
</task>

<task type="auto">
  <name>Task 2: Extension identity, payload, Custom Template deployment + side-by-side identifier table</name>
  <files>docs/admin-setup-macos/10-kerberos-sso-extension.md</files>
  <read_first>
    - .planning/phases/83-kerberos-sso-extension-guide/83-RESEARCH.md (## Verified Payload Structure: Key-Value Reference Table; ## Code Examples: On-Premises AD Kerberos Profile .mobileconfig; Intune Custom Template Upload Steps; Common Pitfalls 1-3,6,7)
    - .planning/phases/83-kerberos-sso-extension-guide/83-PATTERNS.md (Anti-Patterns table -- the seven must-not literals)
    - .planning/research/PITFALLS.md (§K-1 wrong identifier, §K-5 Redirect-vs-Credential)
  </read_first>
  <action>
    Append a `## Configuration: On-Premises Active Directory Profile` section (D-06 custom Steps -- NOT the Settings-Catalog picker flow). Include:

    (a) A payload key-value reference table sourced from RESEARCH.md "Key-Value Reference Table": `ExtensionIdentifier` = `com.apple.AppSSOKerberos.KerberosExtension`; `TeamIdentifier` = literal `apple` (Pitfall 1 -- never `UBF8T346G9`); `Type` = `Credential` (K-5 -- never `Redirect`); `PayloadType` = `com.apple.extensiblesso`; `Realm` = `CONTOSO.COM` ALL CAPS (Pitfall 6); `Hosts` = `["contoso.com", ".contoso.com"]` with the required dot-prefixed wildcard (Pitfall 7).

    (b) The full verified on-prem .mobileconfig XML example, reproduced from RESEARCH.md "## Code Examples > On-Premises Active Directory Kerberos Profile (.mobileconfig)" verbatim (this is the authoritative Microsoft Learn 2026-06-15 sample). This XML lives in the GUIDE body as a fenced code block — that is allowed in the authored markdown; the prohibition on fenced code blocks applies only to PLAN `<action>` fields, not to guide content. Include the instruction that admins must replace the placeholder `PayloadUUID`/`PayloadIdentifier` with their own generated values (per RESEARCH Security Domain).

    (c) A side-by-side Extension-Identifier comparison table (K-1 prevention) with two rows: Apple Kerberos extension `com.apple.AppSSOKerberos.KerberosExtension` / Type Credential vs Microsoft PSSO `com.microsoft.CompanyPortalMac.ssoextension` / Type Redirect, clearly labeled "use this one for Kerberos".

    (d) A K-5 callout: "The Kerberos SSO extension uses Type = Credential, NOT Redirect. Using Redirect (the Platform SSO value) is the most common configuration copy-error" and a K-1 callout warning never to paste the `com.microsoft.*` identifier into the Kerberos profile.

    (e) The Intune Custom Template upload steps from RESEARCH.md "Intune Custom Template Upload Steps" (Devices > Configuration > Create > New policy; Platform macOS; Profile type Templates > Custom; Deployment channel Device channel; upload .mobileconfig; assign to user groups NOT device groups).

    Use ` -- ` em-dash convention and backtick code-spans throughout.
  </action>
  <verify>
    <automated>grep -q 'Credential' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'CONTOSO.COM' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q '\.contoso\.com' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'TeamIdentifier' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -qi 'Templates > Custom' docs/admin-setup-macos/10-kerberos-sso-extension.md && [ "$(grep -c 'Type.*Redirect' docs/admin-setup-macos/10-kerberos-sso-extension.md)" != "" ]</automated>
  </verify>
  <acceptance_criteria>
    - `## Configuration: On-Premises Active Directory Profile` heading present.
    - Literal `com.apple.AppSSOKerberos.KerberosExtension` AND literal `<string>Credential</string>` (or `Type = Credential`) present in the .mobileconfig example.
    - Realm value appears as `CONTOSO.COM` (ALL CAPS); Hosts array contains both `contoso.com` and `.contoso.com` (dot-prefixed wildcard present).
    - `TeamIdentifier` shown with value `apple` (literal word); the string `UBF8T346G9` does NOT appear anywhere: `grep -c 'UBF8T346G9' file` returns 0.
    - The Microsoft PSSO identifier `com.microsoft.CompanyPortalMac.ssoextension` appears ONLY in the comparison/disambiguation context, never inside the Kerberos `ExtensionIdentifier` plist value.
    - `Redirect` appears only in disambiguation/callout context (never as the Kerberos profile `Type`); a K-5 callout text mentioning "Redirect" and "Credential" is present.
    - Intune Custom Template navigation present (`Templates > Custom`); assignment to user groups (not device groups) stated.
  </acceptance_criteria>
  <done>On-prem AD configuration section delivers the verified payload, full .mobileconfig, side-by-side identifier comparison, K-1/K-5 callouts, and Custom Template upload steps.</done>
</task>

<task type="auto">
  <name>Task 3: Bounded on-prem AD callout + Prerequisites section</name>
  <files>docs/admin-setup-macos/10-kerberos-sso-extension.md</files>
  <read_first>
    - docs/admin-setup-macos/07-platform-sso-setup.md (lines 17-35: Prerequisites bullet format `**Label:**` + multi-paragraph `>` callout analog)
    - .planning/phases/83-kerberos-sso-extension-guide/83-PATTERNS.md (Prerequisites section format; Known-blockquote callout format for D-08 bounded AD callout -- verbatim target text)
    - .planning/research/PITFALLS.md (§K-4 over-documentation guard -- the AD content ban list)
    - .planning/phases/83-kerberos-sso-extension-guide/83-CONTEXT.md (D-03, D-08, D-09)
  </read_first>
  <action>
    Append a `## Prerequisites` section (D-07 anchor) using the guide 07 `**Label:**` bullet format. Include four bullets per PATTERNS: Platform SSO already deployed (link to 07-platform-sso-setup.md) -- with a Pitfall-5 note that deploying the Kerberos profile with `usePlatformSSOTGT: true` before PSSO is registered has no effect; macOS 14.6 Sonoma or later (D-03 floor for PSSO TGT integration; note macOS 10.15+ standalone is outside v1.10 scope, D-02); Company Portal 5.2408.0 or later for PSSO TGT sharing (and 2508 or later for custom_tgt_setting, forward-referenced to 83-02's integration section); and the on-prem AD / KDC dependency.

    For the on-prem AD / KDC bullet, embed the bounded callout (D-08, K-4) using the `> **Bold label:**` multi-line blockquote format from PATTERNS "Known-blockquote callout format": ONE paragraph stating this guide covers the Intune-admin-facing MDM payload only; configuring the AD Kerberos realm, KDC reachability, and DNS SRV records is an AD-admin responsibility outside this guide's scope; link out to BOTH the Apple Kerberos SSO Extension deployment reference and the Microsoft Learn "Enable Kerberos SSO in Platform SSO" guide. Per D-09 / K-4, do NOT include `nltest`, SRV-record steps, DC diagnostics, OU, or forest content. Keep the realm ALL CAPS convention and the Intune-admin-facing on-prem dependency note in-scope per D-09.

    Use ` -- ` em-dash convention and backtick code-spans throughout.
  </action>
  <verify>
    <automated>grep -q '## Prerequisites' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q '14.6' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -q 'usePlatformSSOTGT' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -qi 'developer.apple.com' docs/admin-setup-macos/10-kerberos-sso-extension.md && grep -qi 'learn.microsoft.com' docs/admin-setup-macos/10-kerberos-sso-extension.md && [ "$(grep -ci 'nltest' docs/admin-setup-macos/10-kerberos-sso-extension.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `## Prerequisites` heading present with four `**Label:**`-prefixed bullets.
    - macOS `14.6` floor stated (D-03); standalone macOS 10.15+ noted as out-of-scope (D-02).
    - Company Portal `5.2408.0` (or `2408`) version stated.
    - Bounded AD callout uses a `> **`-prefixed blockquote, links BOTH `developer.apple.com` (Apple) AND `learn.microsoft.com` (Microsoft), and is ONE paragraph.
    - K-4 ban enforced: `nltest` appears 0 times; no `SRV`-record how-to / OU / forest configuration steps.
    - Pitfall-5 note present (PSSO must be registered before `usePlatformSSOTGT` has effect).
  </acceptance_criteria>
  <done>Prerequisites section authored with macOS 14.6 floor, Company Portal version, Pitfall-5 ordering note, and the bounded one-paragraph AD callout linking to Apple + Microsoft (no AD how-to content).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide reader (Intune admin) -> production MDM payload | An admin copies plist values / CLI from this doc into a live Intune Custom Template profile. The only "untrusted input" surface is the accuracy of the published guidance itself. |

This is a documentation-only plan: no executable code, no user input handling, no auth surface, no network/data operations are produced. The threat surface is limited to documentation accuracy.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering (incorrect guidance) | Extension identifier / payload type in guide 10 | mitigate | Acceptance criteria enforce literal `com.apple.AppSSOKerberos.KerberosExtension`, `Type Credential`, `TeamIdentifier apple`, ALL-CAPS realm, dot-prefixed Hosts (K-1/K-5/Pitfall-1,6,7). Wrong values would cause silent admin misconfiguration. |
| T-83-02 | Information disclosure | Placeholder secrets/UUIDs in .mobileconfig example | accept | Only placeholder values (`CONTOSO.COM`, sample UUIDs) are used; guide instructs admins to substitute their own UUIDs. No real tenant/secret data. |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (documentation only). No package legitimacy gate applicable. |

Block-on severity: high. None expected -- no destructive CLI is published; `app-sso diagnose` and `kinit`/`kdestroy` write-commands are out of scope for this plan (diagnostics are authored in 83-02).
</threat_model>

<verification>
- File `docs/admin-setup-macos/10-kerberos-sso-extension.md` exists and contains: frontmatter (5 keys), platform-gate header, `## What This Guide Is NOT`, `## Configuration: On-Premises Active Directory Profile`, `## Prerequisites`.
- Accuracy literals present: `com.apple.AppSSOKerberos.KerberosExtension`, `Type Credential`, `TeamIdentifier apple`, `CONTOSO.COM`, `.contoso.com`.
- Forbidden literals absent: `UBF8T346G9` (0), em-dash `—` (0), `nltest` (0). `Redirect` and the Microsoft identifier appear ONLY in disambiguation/comparison context.
- Suite conventions honored: ` -- ` double-dash, backtick code-spans for identifiers.
</verification>

<success_criteria>
- KRB-01 delivered through the Prerequisites section: extension identity, payload type, Intune Custom Template deployment, realm/KDC bounded prerequisite, three-way disambiguation.
- File left in a state that 83-02 can append to (no `## See Also` or version-history table yet -- those belong to 83-02).
</success_criteria>

<output>
Create `.planning/phases/83-kerberos-sso-extension-guide/83-01-SUMMARY.md` when done.
</output>
