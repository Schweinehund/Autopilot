---
phase: 128-v116-pin-15th-path-a-lineage-bump-terminal-close
plan: 06
type: execute
wave: 6
depends_on: ["128-05"]
# files_modified is a PLACEHOLDER superset from the 128-01 D-128-C recon; the ACTUAL edited
# set (if any) comes from the 128-05 CASCADE-RED verdict. Expected to fire on NOTHING.
files_modified:
  - scripts/validation/check-phase-49.mjs
  - scripts/validation/check-phase-58.mjs
  - scripts/validation/check-phase-101.mjs
  - scripts/validation/check-phase-118.mjs
  - scripts/validation/check-phase-121.mjs
autonomous: true
requirements: [HARN-09]
must_haves:
  truths:
    - "This slot fires ONLY IF the 128-05 authoritative Axis-2 GHA cascade came back RED on any firing apex AFTER the pre-scoped D-128-C conversions landed; if GREEN it is a documented no-op skip (expected — v1.17 drift is ~nil)"
    - "Remediation admits only in-scope shapes: (1) an additional readAtV116Close frozen-aware conversion of a predecessor content-assertion validator the 128-01 scoping missed; (2) an in-class NESTED/guard fix on a predecessor check-phase-NN validator. NO C15 blockquote-split FP-exemption is expected (v1.17 did no structural retrofit — grounding correction #3)"
    - "NO value-masking (no expected value bumped to the evolved state); NO predecessor FROZEN-surface edit (v1.N-milestone-audit.mjs / sidecar / prior workflow off-limits); CHAIN_SKIP stays EMPTY everywhere (V-SELF hard-asserts size 0)"
    - "Every RED-then-green intermediate state is recorded honestly; the re-pushed GHA cascade concludes green across all firing apexes"
  artifacts:
    - path: ".planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-06-SUMMARY.md"
      provides: "Honest remediation record: fired or no-op; if fired: which shape, root cause, before/after GHA run IDs, the no-value-mask/no-frozen-surface/CHAIN_SKIP-empty statement"
      contains: "CHAIN_SKIP"
  key_links:
    - from: "128-05 CASCADE RED verdict + failing-validator worklist"
      to: "128-06 remediation edits (or documented no-op)"
      via: "recorded failing apex/validator set"
      pattern: "check-phase"
    - from: "128-06 re-pushed green cascade"
      to: "128-07 close-gate precondition"
      via: "before/after GHA run IDs"
      pattern: "check-phase-128"
---

<objective>
The emergent remediation slot: fires ONLY IF the 128-05 authoritative Axis-2 GHA cascade came back RED after the pre-scoped D-128-C conversions. Because v1.17's corpus drift is ~nil (5 inert frontmatter deletions + a no-op, all pre-scoped at plan time), this slot is expected to be a documented no-op skip. If it fires, remediation is strictly in-scope (frozen-aware conversion or in-class guard fix) with NO value-masking, NO frozen-surface edit, and CHAIN_SKIP empty — the honest RED-then-green accounting precedent (v1.15 119-05 / v1.16 125-05).

Purpose: Provide the honest-accounting escape hatch for any surprise the full-chain scoping missed, without smuggling in scope.
Output: 128-06-SUMMARY.md (fired-with-record OR documented no-op skip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-05-AUDIT-RESULTS.md
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-CONTEXT.md
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-RESEARCH.md
@scripts/validation/check-phase-51.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evaluate the 128-05 verdict — skip or remediate</name>
  <read_first>
    - .planning/phases/128-*/128-05-AUDIT-RESULTS.md (the cascade verdict + any failing-apex worklist)
    - .planning/phases/128-*/128-CONTEXT.md D-128-C mandatory constraints (no value-mask, no frozen-surface edit, CHAIN_SKIP empty) + specifics (emergent slot fires on nothing)
    - scripts/validation/check-phase-51.mjs (readAtV115Close conversion precedent) if a frozen-aware conversion is needed
  </read_first>
  <files>.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-06-SUMMARY.md</files>
  <action>
    Read the 128-05 cascade verdict. If GREEN across all firing apexes (the expected outcome), record 128-06 as a documented no-op skip — state explicitly that the pre-scoped D-128-C conversions were sufficient and no emergent remediation was needed — then STOP (do not edit any validator). If any firing apex is RED, diagnose the root cause from the failing-validator worklist and apply ONLY an in-scope fix: either an additional readAtV116Close frozen-aware conversion (for a live-HEAD HYG-doc reader the 128-01 scoping missed) or an in-class NESTED/guard fix. Do NOT value-mask (never bump an expected value to the evolved state), do NOT edit any predecessor frozen surface (v1.N-milestone-audit.mjs / sidecar / prior workflow), and keep CHAIN_SKIP EMPTY on every touched validator. If a fix is applied, commit via direct git (not gsd-sdk), re-push, await the re-run GHA cascade, and record before/after run IDs. Record the honest RED-then-green narrative.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -c "CHAIN_SKIP" .planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-06-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 128-06-SUMMARY.md records either a documented no-op skip (with the reason: pre-scoped conversions sufficient) OR a fired remediation with root cause, shape, and before/after GHA run IDs
    - If fired: an explicit statement that no value-masking occurred, no predecessor frozen surface was edited, and CHAIN_SKIP stayed empty
    - If fired: the re-pushed GHA cascade concludes GREEN across all firing apexes
    - No entry was ever added to any CHAIN_SKIP set
  </acceptance_criteria>
  <done>128-06 recorded as documented no-op OR honest RED-then-green remediation within the D-128-C constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Conditional edits to version-controlled validator scripts (or no-op). No runtime, network, user input, auth, or data surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-11 | Tampering | Green-forcing via value-mask / CHAIN_SKIP entry | mitigate | Acceptance criteria forbid value-masking and any CHAIN_SKIP entry; V-SELF hard-asserts size 0 |
| T-128-12 | Elevation of scope | Remediation exceeding the in-scope shapes (frozen-surface edit / broad sweep) | mitigate | Action restricts fixes to frozen-aware conversion or in-class guard fix; frozen surfaces are off-limits |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Package Legitimacy Audit N/A); attack surface nil |

No high-severity threats: this plan conditionally edits trusted validators or no-ops; no privileged action, no external input.
</threat_model>

<verification>
- 128-06 is either a documented no-op skip or an honest RED-then-green record
- If fired: no value-mask, no frozen-surface edit, CHAIN_SKIP empty, re-pushed cascade GREEN
- Before/after GHA run IDs recorded if fired
</verification>

<success_criteria>
Any surprise the full-chain scoping missed is remediated honestly within the D-128-C constraints, or the slot is a clean documented no-op — leaving the cascade green for the close-gate.
</success_criteria>

<output>
Create `.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-06-SUMMARY.md` when done
</output>
