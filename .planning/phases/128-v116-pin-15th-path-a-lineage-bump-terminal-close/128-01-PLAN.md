---
phase: 128-v116-pin-15th-path-a-lineage-bump-terminal-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-01-SUMMARY.md
autonomous: true
requirements: [HARN-08, HARN-09]
must_haves:
  truths:
    - "The Wave-0 pre-anchor SHA (the predecessor-byte-unchanged HARD-gate base) is captured via `git rev-parse HEAD` and recorded BEFORE any harness/validator file is authored"
    - "V116 is POSITIVELY CONFIRMED as 3dd2512 (full 3dd251249a812e31147cd653a7ad01e6878c091b) — the dual-token grep returns exactly the docs(125-07) close-gate whose subject carries BOTH 'MILESTONE-AUDIT' AND 'MILESTONE CLOSE' (recover-not-assume, per HARN-08 and the V114/V115 precedent)"
    - "The D-128-C conversion set is confirmed to be exactly the 8 files / 14 checks from RESEARCH (check-phase-49/58/59/62/101/109/118/121), and the HYG-02 -1 line-shift diff-scope is verified: `git diff 3dd2512..HEAD` over each of the 5 HYG-02 files shows ONLY the single frontmatter-line deletion (no other drift)"
    - "The 35 stale sidecar line-pins across 4 HYG-02 files (21+8+3+3) are enumerated from v1.16-audit-allowlist.json exactly as RESEARCH lists them, ready for the Atom-1 -1 shift"
  artifacts:
    - path: ".planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-01-SUMMARY.md"
      provides: "Recorded Wave-0 anchor SHA, positively-confirmed V116=3dd2512, the exhaustive D-128-C conversion worklist, the HYG-02 diff-scope confirmation, and the 35-pin -1-shift worklist"
      contains: "3dd2512"
  key_links:
    - from: "128-01 Wave-0 anchor SHA"
      to: "128-05 predecessor-byte-unchanged HARD gate (git diff <anchor>..HEAD over frozen surfaces)"
      via: "recorded anchor SHA consumed at close-audit time"
      pattern: "git diff"
    - from: "128-01 confirmed V116=3dd2512"
      to: "128-03 V116 pin insertion in frozen-at-close.mjs"
      via: "positively-confirmed SHA consumed by the pin"
      pattern: "3dd2512"
    - from: "128-01 HYG-02 diff-scope + 35-pin worklist"
      to: "128-02 v1.17-audit-allowlist.json -1 line-shift"
      via: "recorded per-file pin list consumed by the sidecar repoint"
      pattern: "line"
---

<objective>
Recon + Wave-0 pre-anchor for the v1.17 close. This plan captures the anchor SHA, positively confirms the V116 close-gate SHA (recover-not-assume), and executes the mandatory plan-time full-chain scoping that D-128-C and STATE order — enumerating the exact predecessor-validator conversion set AND confirming the 35-pin sidecar line-shift, so Atom 1 (128-02) and Atom 2 (128-03) author against verified facts rather than assumptions.

Purpose: Eliminate the two-round-remediation failure mode (v1.15 119-05 / v1.16 125-05) by scoping ALL drift at plan time, not late at close.
Output: 128-01-SUMMARY.md recording the anchor SHA, confirmed V116, the D-128-C worklist, the HYG-02 diff-scope proof, and the 35-pin -1-shift worklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-CONTEXT.md
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-RESEARCH.md
@scripts/validation/_lib/frozen-at-close.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Wave-0 anchor + positively confirm V116=3dd2512</name>
  <read_first>
    - .planning/phases/128-*/128-RESEARCH.md §"V116 SHA recovery" + §"Common Pitfalls" (Pitfall 3, 4)
    - .planning/phases/128-*/128-CONTEXT.md D-128-D (SHA is a CANDIDATE — positively confirm)
    - scripts/validation/_lib/frozen-at-close.mjs (V115 block at line 62, export at line 105 — confirm V116 is NOT yet present)
  </read_first>
  <files>.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-01-SUMMARY.md</files>
  <action>
    Run `git rev-parse HEAD` and record the result verbatim as the Wave-0 pre-anchor SHA (the base for the 128-05 predecessor-byte-unchanged HARD gate). Then run the dual-token recovery command `git log --all --grep="MILESTONE-AUDIT" --grep="MILESTONE CLOSE" --all-match -1 --format=%H` and assert the returned full SHA is 3dd251249a812e31147cd653a7ad01e6878c091b (short 3dd2512) with subject beginning "docs(125-07): Phase 125 close-gate". This is recover-not-assume per HARN-08 — do NOT trust the CONTEXT candidate without running the command. Record both the full and short SHA and the confirmed subject line. Confirm frozen-at-close.mjs currently ends at V115 (line 62) / readAtV115Close (line 105) with NO V116 entry present (the pin is this phase's HARN-08 deliverable).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && git log --all --grep="MILESTONE-AUDIT" --grep="MILESTONE CLOSE" --all-match -1 --format=%H | grep -q '^3dd251249a812e31147cd653a7ad01e6878c091b$' && echo CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - The dual-token grep returns exactly `3dd251249a812e31147cd653a7ad01e6878c091b`
    - `grep -c "V116" scripts/validation/_lib/frozen-at-close.mjs` returns 0 (excluding comment references to the deferral) — the pin does not yet exist
    - 128-01-SUMMARY.md records the Wave-0 anchor SHA (output of `git rev-parse HEAD`) and the confirmed V116 full+short SHA + subject
  </acceptance_criteria>
  <done>Wave-0 anchor SHA recorded; V116=3dd2512 positively confirmed via dual-token grep (not assumed).</done>
</task>

<task type="auto">
  <name>Task 2: Full-chain D-128-C scoping + HYG-02 diff-scope + 35-pin worklist</name>
  <read_first>
    - .planning/phases/128-*/128-RESEARCH.md §"D-128-C Conversion Set — DEFINITIVE ENUMERATION" (the 8-file/14-check table) + §"NEW finding … sidecar allowlist line-pins are stale by -1" (the per-file pin tables)
    - .planning/phases/128-*/128-PATTERNS.md §3 (the 4 sample stale-pin entries) + §8 (conversion set)
    - scripts/validation/v1.16-audit-allowlist.json (the 35 pins across 4 files)
  </read_first>
  <files>.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-01-SUMMARY.md</files>
  <action>
    Delegate the FULL predecessor-chain scoping to a fresh sub-agent OR run it directly (do NOT background it — long full-chain runs die at turn boundaries per project memory). Confirm the D-128-C live-HEAD reader set is EXACTLY the 8 files / 14 checks RESEARCH enumerates: check-phase-49 (V-49-19, V-49-21), check-phase-58 (V-58-13/16/17/18/19/22), check-phase-59 (V-59-05, V-59-36), check-phase-62 (V-62-06..09), check-phase-101 (V-101-BANNER), check-phase-109 (V-109-ROW-ANDROID), check-phase-118 (V-118-PRESENCE-MATRIX/ENROLL/REFORMAT/TABLE-REMEDIATION), check-phase-121 (V-121-VHROW). Confirm `grep -r phase_46_wave2_retrofit scripts/validation/` is EMPTY (no validator asserts the removed key — grounding correction #3). Then run `git diff 3dd2512..HEAD -- docs/_glossary-android.md docs/admin-setup-android/03-fully-managed-cobo.md docs/admin-setup-android/04-byod-work-profile.md docs/reference/android-capability-matrix.md docs/android-lifecycle/03-android-version-matrix.md` and confirm the ONLY change per file is the single `phase_46_wave2_retrofit` frontmatter-line deletion (Pitfall 3 insurance). Record the exhaustive 35-pin -1-shift worklist per file (glossary-android 21 pins, android-capability-matrix 8, 03-fully-managed-cobo 3, 03-android-version-matrix 3; 04-byod-work-profile 0) with each pin's old line number → new (old-1), for consumption by 128-02.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && test -z "$(grep -rl phase_46_wave2_retrofit scripts/validation/ 2>/dev/null)" && echo "NO-VALIDATOR-ASSERTS-KEY"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl phase_46_wave2_retrofit scripts/validation/` returns no files (empty)
    - `git diff --stat 3dd2512..HEAD -- docs/_glossary-android.md` shows `1 file changed, 1 deletion(-)` (single-line frontmatter deletion only); same single-deletion shape for all 5 HYG-02 files
    - 128-01-SUMMARY.md lists all 8 conversion-set validators + their 14 check IDs, AND the per-file 35-pin worklist mapping each old line to old-1
  </acceptance_criteria>
  <done>D-128-C conversion set confirmed exhaustive (8/14); HYG-02 diff-scope proven single-line-per-file; 35-pin -1-shift worklist recorded for Atom 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan runs read-only git/grep inspection and writes one planning-doc SUMMARY. No runtime, network, user input, auth, or data surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Tampering | Recovered V116 SHA correctness | mitigate | Positive-confirmation dual-token grep asserts the exact 40-char SHA before it is used downstream; no assumption of the CONTEXT candidate |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | No package installs anywhere in this phase (RESEARCH: Package Legitimacy Audit N/A); attack surface nil |

No high-severity threats: this plan reads version-controlled git history and writes a planning doc — no privileged action, no external input.
</threat_model>

<verification>
- Wave-0 anchor SHA recorded (output of `git rev-parse HEAD`)
- V116 confirmed = 3dd251249a812e31147cd653a7ad01e6878c091b via dual-token grep
- D-128-C conversion set = exactly 8 files / 14 checks
- HYG-02 diff-scope = single frontmatter-line deletion per file
- 35-pin -1-shift worklist recorded
</verification>

<success_criteria>
The Atom-1 and Atom-2 plans can author entirely against verified facts (anchor SHA, V116 SHA, conversion set, pin worklist) with zero remaining discovery — the two-round-remediation failure mode is designed out.
</success_criteria>

<output>
Create `.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-01-SUMMARY.md` when done
</output>
