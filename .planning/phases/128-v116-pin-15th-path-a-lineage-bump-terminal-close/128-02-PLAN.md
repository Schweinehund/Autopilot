---
phase: 128-v116-pin-15th-path-a-lineage-bump-terminal-close
plan: 02
type: execute
wave: 2
depends_on: ["128-01"]
files_modified:
  - scripts/validation/v1.17-milestone-audit.mjs
  - scripts/validation/v1.17-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
autonomous: true
requirements: [HARN-09]
must_haves:
  truths:
    - "v1.17-milestone-audit.mjs is a faithful Path-A byte-copy of v1.16-milestone-audit.mjs (C1-C17 check bodies UNCHANGED) with only the header/lineage/source-of-truth comments and the parseAllowlist sidecar path repointed v1.16 to v1.17"
    - "v1.17-audit-allowlist.json is a Path-A copy with a TARGETED -1 line-shift on exactly 35 pins across 4 HYG-02 files (glossary-android 21, android-capability-matrix 8, 03-fully-managed-cobo 3, 03-android-version-matrix 3); 04-byod-work-profile and all other entries copied byte-verbatim"
    - "After the sidecar repoint, node v1.17-milestone-audit.mjs --verbose exits 0 with C1 (SafetyNet) and C2 (supervision) GREEN — every -1-shifted pin re-verified to still cover its expected banned-term occurrence at the new line (NOT blind-decremented)"
    - "BASELINE_21 freshness comment is appended to regenerate-supervision-pins.mjs, back-anchored to the JIT pre-Atom-1 HEAD (captured via git rev-parse HEAD immediately before this commit), never the Wave-0 SHA and never the close-gate SHA; the BASELINE_9 line-coord array itself is UNTOUCHED"
    - "Atom 1 lands as ONE indivisible commit of exactly these three files; it is never folded into Atom 2"
  artifacts:
    - path: "scripts/validation/v1.17-milestone-audit.mjs"
      provides: "15th Path-A milestone audit harness, C1-C17 inherited verbatim"
      contains: "id: 17"
    - path: "scripts/validation/v1.17-audit-allowlist.json"
      provides: "sidecar allow-list with the 35-pin -1 HYG-02 line-shift; phase string repointed to 128"
      contains: "128-v116-pin"
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "BASELINE_21 freshness audit-trail comment"
      contains: "BASELINE_21"
  key_links:
    - from: "v1.17-milestone-audit.mjs parseAllowlist path constant"
      to: "scripts/validation/v1.17-audit-allowlist.json"
      via: "sidecar load at runtime against live HEAD"
      pattern: "v1.17-audit-allowlist"
    - from: "128-01 35-pin worklist"
      to: "v1.17-audit-allowlist.json -1 shifted pins"
      via: "recorded per-file old to old-1 mapping"
      pattern: "line"
---

<objective>
Atom 1 (ONE commit): author the v1.17 milestone-audit harness + its sidecar allowlist + the BASELINE_21 freshness marker. The harness is a byte-faithful Path-A copy of v1.16's (C1-C17 unchanged); the sidecar copy carries the mandatory 35-pin -1 line-shift that defuses the RESEARCH landmine (HYG-02 deleted one frontmatter line per file, so byte-verbatim pins would trip C1/C2 BLOCKING the first time the harness runs against live HEAD).

Purpose: Produce the current-milestone harness surface that check-phase-128 (Atom 2) targets and that the new CI workflow runs directly.
Output: v1.17-milestone-audit.mjs, v1.17-audit-allowlist.json, BASELINE_21 comment — one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-RESEARCH.md
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-PATTERNS.md
@scripts/validation/v1.16-milestone-audit.mjs
@scripts/validation/regenerate-supervision-pins.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Path-A copy v1.16-milestone-audit.mjs to v1.17-milestone-audit.mjs</name>
  <read_first>
    - scripts/validation/v1.16-milestone-audit.mjs (full — byte-copy source; header lines 1-6, the parseAllowlist sidecar path, C1 lines 266-268 / C2 lines 300-301 exact-match logic)
    - .planning/phases/128-*/128-PATTERNS.md section 2 (exact header-repoint instructions)
  </read_first>
  <files>scripts/validation/v1.17-milestone-audit.mjs</files>
  <action>
    Copy v1.16-milestone-audit.mjs byte-for-byte to v1.17-milestone-audit.mjs. Repoint ONLY the header/provenance region and the sidecar path: change every v1.16 token to v1.17 in the name/lineage line (extend the lineage tail so it ends v1.16 then v1.17); repoint the source-of-truth path to .planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-CONTEXT.md; rewrite the sidecar-provenance comment to describe THIS phase's cause (the 4-file HYG-02 minus-one frontmatter-line-deletion line-shift, commit 7dda1f7) NOT the Phase-121/122 blockquote-split cause; keep the phrase C1-C17 inherited verbatim from v1.16. Change the parseAllowlist sidecar path constant to read scripts/validation/v1.17-audit-allowlist.json. Do NOT touch any C1-C17 check body — the exact-match logic stays byte-identical (the landmine is fixed on the sidecar side only).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && node -c scripts/validation/v1.17-milestone-audit.mjs && grep -c "v1.17-audit-allowlist.json" scripts/validation/v1.17-milestone-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node -c scripts/validation/v1.17-milestone-audit.mjs parses without error
    - grep -c "id: 17" scripts/validation/v1.17-milestone-audit.mjs is 1 or more (C17 inherited)
    - The parseAllowlist path constant references scripts/validation/v1.17-audit-allowlist.json and NOT v1.16-audit-allowlist.json
    - git diff --no-index of v1.16 vs v1.17 milestone-audit shows changes ONLY in header/provenance comments and the sidecar path string (no C1-C17 body diff)
  </acceptance_criteria>
  <done>v1.17-milestone-audit.mjs is a faithful Path-A copy; only header + sidecar path repointed; C1-C17 byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Path-A copy allowlist with the 35-pin -1 line-shift</name>
  <read_first>
    - scripts/validation/v1.16-audit-allowlist.json (full — copy source; the 35 pins across 4 files)
    - .planning/phases/128-*/128-01-SUMMARY.md (the recorded per-file 35-pin old to old-1 worklist + HYG-02 diff-scope proof)
    - .planning/phases/128-*/128-RESEARCH.md section NEW finding (per-file pin line tables) + Pitfall 3 (re-verify, don't blind-decrement)
  </read_first>
  <files>scripts/validation/v1.17-audit-allowlist.json</files>
  <action>
    Copy v1.16-audit-allowlist.json to v1.17-audit-allowlist.json. Repoint generated to the Phase-128 authoring date and phase to 128-v116-pin-15th-path-a-lineage-bump-terminal-close. Apply a uniform -1 line-shift to exactly the 35 pins across 4 HYG-02 files: docs/_glossary-android.md (21 pins at 38,90,94,126,128,130,132,146,148,152,187,202,219,221-twice,225,304,331,333,334,338), docs/reference/android-capability-matrix.md (8 pins at 75,123,125,126,128,130,134,135), docs/admin-setup-android/03-fully-managed-cobo.md (3 pins at 52,54,199), docs/android-lifecycle/03-android-version-matrix.md (3 pins at 58,102,104) — each new line equals old minus 1. For EACH shifted pin, open the live file at the new line and confirm it still contains the expected banned-term occurrence named in that pin's reason (Pitfall 3 — do NOT blind-decrement). Update each shifted pin's reason prefix to note Phase 128 HYG-02 repoint (was line N): -1 from HYG-02 frontmatter-line deletion (commit 7dda1f7). Leave docs/admin-setup-android/04-byod-work-profile.md (count-based tracker, 0 line-pins) and ALL other entries byte-verbatim. Then run the harness against live HEAD to prove C1/C2 GREEN.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.17-audit-allowlist.json','utf8'));console.log('JSON-OK')" && node scripts/validation/v1.17-milestone-audit.mjs --verbose; echo EXIT:$?</automated>
  </verify>
  <acceptance_criteria>
    - The sidecar parses as valid JSON
    - node scripts/validation/v1.17-milestone-audit.mjs --verbose exits 0 (EXIT:0) with C1 (SafetyNet) and C2 (supervision) reported PASS (no FAIL lines)
    - Exactly 35 pins carry the Phase 128 HYG-02 repoint reason prefix; 04-byod-work-profile.md has no line-pin change
    - grep -c "128-v116-pin" scripts/validation/v1.17-audit-allowlist.json is 1 or more (phase string repointed)
  </acceptance_criteria>
  <done>Sidecar copied with the 35-pin -1 shift re-verified against live corpus; harness C1/C2 GREEN on HEAD.</done>
</task>

<task type="auto">
  <name>Task 3: Append BASELINE_21 comment + land Atom 1 as one commit</name>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs (the existing BASELINE_20 comment block at lines 476-491 — the exact shape to mirror)
    - .planning/phases/128-*/128-PATTERNS.md section 7 (BASELINE_21 insertion template)
  </read_first>
  <files>scripts/validation/regenerate-supervision-pins.mjs</files>
  <action>
    Immediately before committing, capture the JIT pre-Atom-1 HEAD via git rev-parse HEAD (this is the back-anchor target; note it may differ from the Wave-0 anchor from 128-01 if a Jira-sync commit landed in between, per the recorded 119/125 distinction). Append ONE new comment block after the existing BASELINE_20 paragraph mirroring its exact shape: BASELINE_21 refreshed on the Phase-128 date, closes BASELINE_20 v1.16 carry-over per HARN-09, v1.17 line positions verified against the captured JIT pre-Atom-1 HEAD, resolution path notes BASELINE_22 refreshes at the next milestone close. Do NOT rename or alter the BASELINE_9 array identifier or any line-coordinate entry — this is a comment-only append. Then stage exactly the three Atom-1 files (v1.17-milestone-audit.mjs, v1.17-audit-allowlist.json, regenerate-supervision-pins.mjs) and commit as ONE indivisible commit using direct git (NOT gsd-sdk query commit — SDK write-verbs hang on this repo per project memory).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -c "BASELINE_21" scripts/validation/regenerate-supervision-pins.mjs && git log -1 --name-only --format="%s" | grep -E "v1.17-milestone-audit|v1.17-audit-allowlist|regenerate-supervision-pins"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "BASELINE_21" scripts/validation/regenerate-supervision-pins.mjs is 1 or more
    - The BASELINE_9 line-coordinate array is byte-unchanged (git diff shows only an appended comment block)
    - The Atom-1 commit contains exactly the three named files and no others
    - Commit was made via direct git (not gsd-sdk)
  </acceptance_criteria>
  <done>BASELINE_21 comment appended (back-anchored to JIT pre-Atom-1 HEAD); Atom 1 landed as one 3-file commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Edits version-controlled Node validation scripts + a declarative JSON sidecar. No runtime, network, user input, auth, or data surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-02 | Tampering | Sidecar line-pin correctness after -1 shift | mitigate | Each shifted pin is re-verified against live corpus at its new line (Pitfall 3), not blind-decremented; harness C1/C2 run proves GREEN before commit |
| T-128-03 | Integrity | Accidental C1-C17 body edit during Path-A copy | mitigate | git diff --no-index vs the v1.16 source asserts no check-body diff |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Package Legitimacy Audit N/A); attack surface nil |

No high-severity threats: this plan copies and repoints trusted validation tooling; no privileged action or external input.
</threat_model>

<verification>
- v1.17-milestone-audit.mjs parses; C1-C17 byte-identical to v1.16 source; sidecar path repointed
- v1.17-audit-allowlist.json valid JSON; 35 pins -1-shifted and re-verified; harness C1/C2 GREEN on HEAD
- BASELINE_21 appended; BASELINE_9 array untouched
- Atom 1 = one 3-file commit via direct git
</verification>

<success_criteria>
The current-milestone harness surface exists and runs GREEN against live HEAD, with the 35-pin landmine defused — ready for check-phase-128 (Atom 2) to target and for the CI workflow to run directly.
</success_criteria>

<output>
Create `.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-02-SUMMARY.md` when done
</output>
