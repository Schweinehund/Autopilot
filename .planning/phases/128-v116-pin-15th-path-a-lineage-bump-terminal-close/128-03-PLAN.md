---
phase: 128-v116-pin-15th-path-a-lineage-bump-terminal-close
plan: 03
type: execute
wave: 3
depends_on: ["128-02"]
files_modified:
  - scripts/validation/_lib/frozen-at-close.mjs
  - scripts/validation/check-phase-49.mjs
  - scripts/validation/check-phase-58.mjs
  - scripts/validation/check-phase-59.mjs
  - scripts/validation/check-phase-62.mjs
  - scripts/validation/check-phase-101.mjs
  - scripts/validation/check-phase-109.mjs
  - scripts/validation/check-phase-118.mjs
  - scripts/validation/check-phase-121.mjs
autonomous: true
requirements: [HARN-08, HARN-09]
must_haves:
  truths:
    - "frozen-at-close.mjs gains V116 set to 3dd2512 in MILESTONE_CLOSE_SHAS (inserted after V115, before the V14-omitted comment) + a readAtV116Close export appended after readAtV115Close — single-entry pattern, back-anchor invariant (V116 references a PAST close SHA; NO V117 pin)"
    - "The V116 pin/export is authored FIRST in this commit so the helper exists before any conversion consumes it (D-128-C dependency)"
    - "Exactly the 8 D-128-C validators (check-phase-49/58/59/62/101/109/118/121) have their live-HEAD reads of the 5 HYG-02 files converted to readAtV116Close across all 14 enumerated checks — needle text UNCHANGED (NO value-masking), only the read SOURCE moves live to frozen"
    - "CHAIN_SKIP stays EMPTY on every touched validator; no CHAIN_PHASES entry is added or removed; no frozen-surface (v1.N-milestone-audit / sidecar / workflow) is edited"
    - "Each of the 8 converted validators exits 0 individually after conversion; Atom 2a lands as ONE commit"
  artifacts:
    - path: "scripts/validation/_lib/frozen-at-close.mjs"
      provides: "V116 back-anchor pin (HARN-08) + readAtV116Close export"
      contains: "readAtV116Close"
    - path: "scripts/validation/check-phase-121.mjs"
      provides: "V-121-VHROW read converted to readAtV116Close (representative of the 8)"
      contains: "readAtV116Close"
  key_links:
    - from: "128-01 confirmed V116 3dd2512"
      to: "frozen-at-close.mjs V116 entry"
      via: "positively-confirmed SHA"
      pattern: "3dd2512"
    - from: "readAtV116Close export"
      to: "the 8 converted check-phase validators"
      via: "import from ./_lib/frozen-at-close.mjs"
      pattern: "readAtV116Close"
---

<objective>
Atom 2a (ONE commit): add the HARN-08 V116 back-anchor pin to frozen-at-close.mjs, then convert the 8 D-128-C predecessor validators' live-HEAD reads of the 5 HYG-02 files to readAtV116Close. The pin MUST be authored first so the helper exists before the conversions consume it. This is the mandatory, plan-time-scoped frozen-aware remediation (HARN-09 mandate) — driven by the latent-hazard directive, not a RED reaction — eliminating the drift that byte-copying live-HEAD reads over the post-HYG-02 corpus would otherwise leave latent.

Purpose: Freeze the v1.16 corpus (HARN-08) and neutralize the latent non-frozen-aware content-assertion hazard proactively (LATENT-NON-FROZEN-AWARE-CONTENT-ASSERTION-01).
Output: frozen-at-close.mjs (V116 pin + export) + 8 converted validators — one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-RESEARCH.md
@.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-PATTERNS.md
@scripts/validation/_lib/frozen-at-close.mjs
@scripts/validation/check-phase-121.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the V116 pin + readAtV116Close export (HARN-08)</name>
  <read_first>
    - scripts/validation/_lib/frozen-at-close.mjs (V115 block lines 62-67, the V14-omitted comment at line 68, the readAtV115Close export at line 105)
    - .planning/phases/128-*/128-PATTERNS.md section 1 (exact insertion text) + 128-RESEARCH.md Pattern 1
    - .planning/phases/128-*/128-01-SUMMARY.md (confirmed V116 SHA)
  </read_first>
  <files>scripts/validation/_lib/frozen-at-close.mjs</files>
  <action>
    Insert a V116 entry set to 3dd2512 into MILESTONE_CLOSE_SHAS directly after the V115 block and before the V14-omitted standalone comment (preserving that comment's position). Include the provenance comment from PATTERNS section 1: Phase 125 Plan 125-07 close-gate, message carries both MILESTONE-AUDIT and MILESTONE CLOSE, confirmed via the dual-token grep returning 3dd251249a812e31147cd653a7ad01e6878c091b, single-entry pattern, back-anchor invariant (V117 deferred to v1.18). Append one export line after readAtV115Close: readAtV116Close resolving to readAtClose of V116 and p. Do this task FIRST (before Task 2) so the helper is defined before the conversions import it.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && node -c scripts/validation/_lib/frozen-at-close.mjs && grep -c "readAtV116Close" scripts/validation/_lib/frozen-at-close.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node -c scripts/validation/_lib/frozen-at-close.mjs parses without error
    - grep finds the V116 3dd2512 entry exactly once in MILESTONE_CLOSE_SHAS
    - grep -c readAtV116Close is 1 or more (export present)
    - No V117 entry exists (back-anchor invariant)
  </acceptance_criteria>
  <done>V116 pin + readAtV116Close export added; helper resolves the frozen v1.16 snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the 8 D-128-C validators to readAtV116Close (14 checks)</name>
  <read_first>
    - .planning/phases/128-*/128-RESEARCH.md section D-128-C DEFINITIVE ENUMERATION (the 8-file/14-check table with file:line + needles)
    - .planning/phases/128-*/128-PATTERNS.md section 8 (conversion mechanics; the check-phase-50 inline and check-phase-51 helper shapes) + Shared Patterns (CHAIN_SKIP-stays-empty invariant)
    - scripts/validation/check-phase-50.mjs (readAtV114Close inline try/catch precedent) + scripts/validation/check-phase-51.mjs (readAtV115Close extracted-helper precedent)
    - Each of the 8 target files' existing import block + the specific line(s) named in the RESEARCH table before editing
  </read_first>
  <files>scripts/validation/check-phase-49.mjs, scripts/validation/check-phase-58.mjs, scripts/validation/check-phase-59.mjs, scripts/validation/check-phase-62.mjs, scripts/validation/check-phase-101.mjs, scripts/validation/check-phase-109.mjs, scripts/validation/check-phase-118.mjs, scripts/validation/check-phase-121.mjs</files>
  <action>
    For each of the 8 files, per the RESEARCH table: (1) add an import of readAtV116Close from ./_lib/frozen-at-close.mjs to the file's import block (verify none currently import from _lib before appending); (2) replace the specific live-HEAD read (readFile or fs.readFileSync) of the HYG-02 file at each enumerated check with a readAtV116Close call wrapped in try/catch (local var + null-check), following whichever of the two established shapes fits the file's existing style; (3) add a one-line comment citing the Phase 128 D-128-C frozen-aware conversion + the honest-accounting SUMMARY path. The exact 14 checks: check-phase-49 (V-49-19 line 293, V-49-21 line 326), check-phase-58 (V-58-13/16/17/18/19/22 lines 241/273/286/297/311/347), check-phase-59 (V-59-05 line 281, V-59-36 line 925), check-phase-62 (V-62-06..09 line 188 loop), check-phase-101 (V-101-BANNER line 76), check-phase-109 (V-109-ROW-ANDROID line 61), check-phase-118 (V-118 PRESENCE-MATRIX/ENROLL/REFORMAT/TABLE-REMEDIATION lines 57/73/98/110), check-phase-121 (V-121-VHROW line 104). Needle strings and all other logic stay byte-identical (NO value-masking). Do NOT touch CHAIN_PHASES or CHAIN_SKIP in any file (both stay as-is; CHAIN_SKIP empty). Do NOT convert any read of a NON-HYG-02 file (that is the out-of-scope FROZEN-AWARE-ADOPTION-SWEEP-01). Convert ONLY reads of the 5 HYG-02 files.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && for f in 49 58 59 62 101 109 118 121; do node scripts/validation/check-phase-$f.mjs >/dev/null 2>&1 && echo "$f:PASS" || echo "$f:FAIL"; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 8 validators prints PASS (exits 0) when run standalone
    - grep -l readAtV116Close matches all 8 files (each imports + uses the helper)
    - grep -c "new Set([])" is unchanged per file — CHAIN_SKIP stays empty; no CHAIN_PHASES line diff in any of the 8 files
    - git diff shows needle/expected strings unchanged in every converted check (only the read source + import + one comment changed)
  </acceptance_criteria>
  <done>All 8 D-128-C validators read the 5 HYG-02 files frozen at V116; needles unchanged; CHAIN_SKIP empty; each exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Land Atom 2a as one commit</name>
  <read_first>
    - .planning/phases/128-*/128-PATTERNS.md Shared Patterns (Atom == one indivisible commit)
    - project memory: gsd-sdk write-verbs hang on this repo — use direct git
  </read_first>
  <files>scripts/validation/_lib/frozen-at-close.mjs, scripts/validation/check-phase-121.mjs</files>
  <action>
    Stage exactly the 9 Atom-2a files (frozen-at-close.mjs + the 8 converted validators) and commit as ONE indivisible commit using direct git (NOT gsd-sdk query commit — SDK write-verbs hang per project memory). The commit message should record HARN-08 (V116 pin) + the D-128-C frozen-aware conversion of 8 validators / 14 checks. Do NOT push yet — the push happens after Atom 2b (128-04) so the two Atom-2 commits fire the CI trigger together.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && git log -1 --name-only --format="%s" | grep -c "check-phase-"</automated>
  </verify>
  <acceptance_criteria>
    - The Atom-2a commit contains exactly the 9 named files (frozen-at-close.mjs + 8 check-phase files) and no others
    - Commit made via direct git (not gsd-sdk)
    - Working tree is clean after the commit
  </acceptance_criteria>
  <done>Atom 2a landed as one 9-file commit; not yet pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Edits version-controlled Node validation scripts only. No runtime, network, user input, auth, or data surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-04 | Tampering | Value-masking risk during conversion | mitigate | Acceptance criteria assert needle/expected strings are byte-unchanged; only the read source moves live to frozen |
| T-128-05 | Integrity | CHAIN_SKIP populated to force green | mitigate | Acceptance criteria assert CHAIN_SKIP stays empty on every touched file; V-SELF hard-asserts size 0 |
| T-128-06 | Tampering | Converting an out-of-scope reader (sweep creep) | mitigate | Action restricts conversion to reads of the 5 HYG-02 files only; the exact 14 checks are enumerated |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Package Legitimacy Audit N/A); attack surface nil |

No high-severity threats: this plan edits trusted validation scripts; no privileged action or external input.
</threat_model>

<verification>
- V116 pin + readAtV116Close export present; no V117 pin
- All 8 D-128-C validators import + use readAtV116Close; each exits 0
- Needle strings unchanged (no value-masking); CHAIN_SKIP empty; CHAIN_PHASES untouched in the 8 files
- Atom 2a = one 9-file commit via direct git; not pushed
</verification>

<success_criteria>
The v1.16 corpus is frozen (HARN-08) and the latent non-frozen-aware content-assertion hazard is eliminated proactively across the exact D-128-C set — with no value-masking, no frozen-surface edit, and CHAIN_SKIP empty.
</success_criteria>

<output>
Create `.planning/phases/128-v116-pin-15th-path-a-lineage-bump-terminal-close/128-03-SUMMARY.md` when done
</output>
