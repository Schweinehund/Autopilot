---
phase: 131-recipe-2-shared-ipad-full-provisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/recipes/02-shared-ipad-full-provisioning.md
autonomous: true
requirements: [IPAD-01, IPAD-02, IPAD-04]
must_haves:
  truths:
    - "docs/recipes/02-shared-ipad-full-provisioning.md exists as doc_type Guide, platform ios+shared-ipad, doc_id RE-223"
    - "An admin reading the top of the recipe sees the all-unsupported anti-feature table BEFORE the Steps (compliance, app-based CA, device-based CA, app protection, email, Company Portal, 'Available' intent, user-licensed VPP), each with a first-party WHY"
    - "The passcode limitation is stated as eight ALPHANUMERIC characters (never '8-digit'), Apple-fixed, grace period in minutes the only knob"
    - "The ADE enrollment step owns the Shared-iPad toggle values inline (Enable Shared iPad / Enroll without user affinity / Supervised=Yes / Shared iPad=Yes) and EXCLUDES 'Await final configuration'"
    - "Maximum cached users is presented as a real settable STD-05 Case-3 field (up to 24 on a 32/64-GB device), NOT prose-only planning guidance (T-1)"
    - "The wipe trigger is stated as two DISTINCT facts (unsupported-device targeting vs. factory-reset-to-apply-a-policy-change), not merged into 'any change wipes' (T-6)"
    - "The Entra shared-device-mode vs Shared iPad distinction is stated correctly inline, not inheriting RE-109's line-83 conflation (T-4)"
  artifacts:
    - path: "docs/recipes/02-shared-ipad-full-provisioning.md"
      provides: "Frontmatter + Summary + specialized Scope banner + Prerequisites + Unsupported/Anti-Feature table + passcode note + ADE-enrollment/sizing/sign-in/VPP happy-path front"
      contains: "## Unsupported and Anti-Feature Callouts"
      min_lines: 90
  key_links:
    - from: "docs/recipes/02-shared-ipad-full-provisioning.md"
      to: "docs/admin-setup-ios/03-ade-enrollment-profile.md"
      via: "cross-link (general ADE fields only)"
      pattern: "03-ade-enrollment-profile"
    - from: "docs/recipes/02-shared-ipad-full-provisioning.md"
      to: "docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md"
      via: "federated sign-in setup link (A2)"
      pattern: "08-managed-apple-account-provisioning"
    - from: "docs/recipes/02-shared-ipad-full-provisioning.md"
      to: "docs/admin-setup-ios/05-app-deployment.md"
      via: "VPP mechanics link (A3)"
      pattern: "05-app-deployment"
---

<objective>
Create `docs/recipes/02-shared-ipad-full-provisioning.md` and author its front half:
frontmatter + EEE line, Summary, the specialized Scope banner, Prerequisites, the
all-unsupported anti-feature table with the passcode note (IPAD-02), and the linear
happy-path front — ADE enrollment profile, the enrollment-policy sizing cluster (IPAD-04),
federated Managed Apple Account sign-in, and device-licensed VPP apps (IPAD-01 front).

This is a DOCUMENTATION-authoring phase in a C17-validated Markdown corpus. The deliverable
is exactly one new Markdown file, instantiating the Phase-129 recipe template and mirroring the
Phase-130 sibling recipe. No application code, no schema, no build artifacts.

Purpose: Deliver the "leads with unsupported" front of the recipe plus the enrollment →
sizing → sign-in → apps happy-path spine, all owning inline only what no existing Approved doc
owns, and cross-linking (never duplicating) the five owner docs.
Output: `docs/recipes/02-shared-ipad-full-provisioning.md` created through the VPP step, C17
self-test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-CONTEXT.md
@.planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-RESEARCH.md
@.planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-PATTERNS.md
@docs/_templates/recipe-template.md
@docs/recipes/01-shared-windows-avd-client.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the recipe file — frontmatter, Summary, Scope banner, Prerequisites, and the all-unsupported anti-feature table + passcode note (IPAD-02)</name>
  <files>docs/recipes/02-shared-ipad-full-provisioning.md</files>
  <read_first>
    - docs/_templates/recipe-template.md (skeleton: fixed H2 order, frontmatter shape, EEE line, Scope banner, anti-feature table header)
    - docs/recipes/01-shared-windows-avd-client.md (sibling exemplar: frontmatter lines 1-25, anti-feature table lines 39-46, scope-banner specialization)
    - .planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-RESEARCH.md (anti-feature WHY-cell source lines 284-295; passcode Pitfall 3 lines 230-234)
    - .planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-PATTERNS.md (Pattern Assignments #1 frontmatter, #2 anti-feature table + passcode note, #7 blockquote splitting)
    - docs/admin-setup-ios/03-ade-enrollment-profile.md (RE-109 — general ADE-fields cross-link target for Prerequisites)
    - docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md (OU-06 — federation-setup link for Prerequisites, A2)
  </read_first>
  <action>
    Create the file. Frontmatter: doc_id RE-223, status Draft, owner Intune Admin Lead, doc_type
    Guide, platform ios+shared-ipad, last_verified 2026-07-17, review_by 2026-10-15, applies_to
    "Shared iPad full provisioning (federated Managed Apple Account, device-licensed VPP, per-role
    layered config)", audience admin. Immediately after the frontmatter close, the EEE line reading
    Platform iOS + Shared iPad · Doc Type Guide · Doc ID RE-223 · Status Draft (this is how
    platform ios+shared-ipad renders per the template comment). H1 title naming the concrete
    end-state (a fully-provisioned Shared iPad). Do NOT insert the template's DELETE-marked worked-
    example HTML comment — that is a template-only artifact.

    Summary: at least 30 words, concrete named end-state — a verified, fully-provisioned Shared iPad
    reached end-to-end from zero through Intune; state the target platform (supervised iPadOS Shared
    iPad) and the admin role (Intune Administrator plus the Entra ID Groups permissions to create
    the enrollment policy, configuration profiles, and app assignments this recipe walks through).

    Scope banner (specialize per the H-LOCK-1 precedent — do not leave the generic template text):
    a `> **Scope:**` blockquote stating this recipe covers named-user Shared iPad provisioning only.
    It EXCLUDES the guest-only / "Require Shared iPad temporary session only" kiosk-style third mode
    (B4 — stated as out-of-scope, a candidate future variant), and it EXCLUDES compliance policy,
    Conditional Access, app protection, and email-profile configuration (all documented below as
    unsupported, never configured). Keep the banner one contiguous run under 200 chars, or split
    across blank-line-separated `>` paragraphs.

    Prerequisites (`## Prerequisites`): bullet list including — eligibility floor iPadOS 13.4+ and at
    least 32 GB storage (A5; do NOT write "64 GB recommended" — first-party has no such floor; the 64
    GB number only appears in the cached-users ceiling); Intune Administrator RBAC; a completed
    Automated Device Enrollment (ADE / ABM) token and Supervised, corporate-owned devices; federated
    identity already set up so users have Managed Apple Accounts — one sentence pointing to OU-06
    (docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md) for that setup,
    never re-authored here (A2). Optionally note the first-party 13.3-vs-13.4 internal inconsistency
    (13.4 is dominant; 13.3 appears only in the wipe-trigger context) per RESEARCH Pitfall 5.

    `## Unsupported and Anti-Feature Callouts` with the frozen 3-column header
    Feature | Why it's unsupported / what breaks | Do this instead. Author the 7-8 rows from
    RESEARCH lines 284-295 verbatim (do NOT re-derive the WHY cells): Compliance policies; App-based
    Conditional Access; Device-based Conditional Access; App protection policies; Email profiles
    (WHY: an error occurs when you assign an email profile — this is T-5, resolved to NOT supported
    despite the applicability table listing Email as applicable); Company Portal (app + website);
    "Available" app intent / user-licensed VPP (WHY: App Store installs disabled on Shared iPad and
    user licensing needs a personal Apple Account App Store sign-in the device can't do — "Do this
    instead" points to the Required + device-licensed VPP step). Do NOT collapse these under a "MAM
    stack" label (misclassifies compliance and device-based CA, which are MDM constructs, per B1).
    Keep the table at 7-8 rows — safely under C17 #11's 25-row threshold; do not grow it.

    Passcode note (B2), rendered as a dedicated note directly adjacent to the anti-feature table (NOT
    a table row, NOT a decision block): Shared iPad passcodes are eight ALPHANUMERIC characters
    (never write "8-digit", "PIN", or "numeric" — Pitfall 3), unchangeable in Apple Business Manager;
    the Intune device-configuration passcode complexity and length settings do not apply; an MDM
    admin can set only the grace period (the minutes a user has to unlock without a passcode). Render
    as plain (non-`>`) prose, or as two-plus blank-line-separated `>` blockquote paragraphs — the
    natural single-paragraph version is ~280 chars and would break C17 #12's 200-char run cap.
  </action>
  <verify>
    <automated>test -f docs/recipes/02-shared-ipad-full-provisioning.md && grep -q "doc_id: RE-223" docs/recipes/02-shared-ipad-full-provisioning.md && grep -q "## Unsupported and Anti-Feature Callouts" docs/recipes/02-shared-ipad-full-provisioning.md && grep -qi "eight alphanumeric" docs/recipes/02-shared-ipad-full-provisioning.md && ! grep -qi "8-digit" docs/recipes/02-shared-ipad-full-provisioning.md && node scripts/validation/c17-eee-contract.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - docs/recipes/02-shared-ipad-full-provisioning.md exists with frontmatter doc_id RE-223, doc_type Guide, platform ios+shared-ipad
    - The file contains '## Unsupported and Anti-Feature Callouts' positioned after Prerequisites and before any '## Steps'
    - The anti-feature table has one row per each of: compliance, app-based CA, device-based CA, app protection, email, Company Portal, "Available" intent, user-licensed VPP (7-8 rows)
    - The Email row documents it as unsupported (T-5), not as an applicable profile
    - The passcode note says "eight alphanumeric characters"; the file contains no "8-digit" anywhere (case-insensitive)
    - The Scope banner is specialized (names the named-user-only scope and the excluded guest-only third mode + unsupported compliance/CA), not the generic template wording
    - Eligibility floor reads iPadOS 13.4+ / ≥32 GB with no "64 GB recommended" claim
    - node scripts/validation/c17-eee-contract.mjs --self-test exits 0
  </acceptance_criteria>
  <done>The recipe file exists through the anti-feature/passcode block; every contiguous top-level blockquote run measures ≤200 chars; C17 self-test exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Author Steps — ADE enrollment profile + enrollment-policy sizing cluster + federated sign-in + device-licensed VPP apps (IPAD-01 front, IPAD-04)</name>
  <files>docs/recipes/02-shared-ipad-full-provisioning.md</files>
  <read_first>
    - docs/recipes/02-shared-ipad-full-provisioning.md (the file created in Task 1 — append the Steps)
    - docs/recipes/01-shared-windows-avd-client.md (sibling: `### Step N:` pattern, STD-05 Case-2 table lines 93-101, split-blockquote what-breaks callouts lines 85-91)
    - .planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-RESEARCH.md (Pattern 2 screen-lock enum lines 166-183; Pattern 3 cached-users lines 185-196; State-of-the-Art Enrollment-policies navigation lines 298-299; T-4/T-6 mechanics)
    - .planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-PATTERNS.md (Pattern Assignments #3 STD-05 cases, #4 what-breaks callouts, #8 link-not-copy anchors lines 165-170)
    - docs/admin-setup-ios/03-ade-enrollment-profile.md (RE-109 — general ADE fields; line 138 Await-final-config unavailability; line-83 conflation to NOT inherit, T-4)
    - docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md (OU-06 — federated sign-in owner, A2)
    - docs/admin-setup-ios/05-app-deployment.md (RE-111 — VPP mechanics owner; device-centric verification view, A3)
    - docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md (OU-07 — session/lifecycle owner; the recipe supersedes its training-sourced values for Intune fields, A4)
  </read_first>
  <action>
    Append `## Steps` and the front steps. Use imperative voice and full Intune portal navigation
    paths. Every configurable step carries a `> **What breaks if misconfigured:**` callout, multi-
    fact callouts split across blank-line-separated `>` paragraphs (each paragraph its own C17 #12
    run, each ≤200 chars — mirror the sibling lines 85-91 and RE-109 lines 100-106 idiom).

    ADE enrollment step: use the newer Intune admin center Enrollment program tokens > Enrollment
    policies navigation (the older Profiles path is de-emphasized per RESEARCH State-of-the-Art), an
    optional soft "navigation may vary by tenant rollout" hedge is fine. Own inline the Shared-iPad
    toggle VALUES as a small table (A1): Enable Shared iPad; User affinity = Enroll without user
    affinity; Supervised = Yes; Shared iPad = Yes. EXCLUDE "Await final configuration" and state WHY
    — it is unavailable in the (no user affinity + Shared iPad = Yes) combination (cite RE-109 line
    138, safe to reference). State the correct Entra distinction inline (T-4): Entra shared device
    mode is a separate User Affinity enum value and is NOT the Shared iPad feature — do not merge
    them the way RE-109's line 83 does; cross-link RE-109 for general ADE fields (supervised / locked
    enrollment / authentication / setup-assistant panes) only, not as the Shared-iPad-settings
    reference. Wipe warning in first-party wording, keeping T-6's TWO facts DISTINCT: (a) a Shared-
    iPad-enabled policy sent to an UNSUPPORTED device (an iPhone, or an iPad on iPadOS 13.3 or
    earlier) triggers a wipe; and, separately, (b) changing an existing enrollment policy requires a
    factory reset for the change to apply. Do not merge these into a punitive "any change wipes."

    Enrollment-policy sizing cluster — co-locate as consecutive blocks at THIS single enrollment-
    policy step (D5; there is no dedicated "Sizing" H2). Attach the per-field iPadOS floors, not a
    blanket "iPadOS 17+":
    - Maximum cached users — STD-05 Case-3 bounded (D1, T-1): one-sentence `> **Ask the admin:**`
      lead-in, then the sizing bounds/consequences as prose OUTSIDE the blockquote (whole number up
      to 24 on a 32-GB or 64-GB device; a low number can delay a new user's data after first sign-in,
      a high number risks running out of on-device storage). This IS a real settable field — do not
      render it as prose-only planning guidance.
    - Maximum seconds after screen lock before password is required — STD-05 Case-2 enum (D2): lead-in
      plus an Option | When to choose | Recorded as table with values 0 / 60 / 300 / 900 / 3600 /
      14400, labeled in SECONDS (not minutes); available for Shared iPad on iPadOS 13.0+. Use RESEARCH
      Pattern 2 as the content source.
    - Maximum seconds of inactivity until user session logs out — STD-05 Case-3 bounded (D3): lead-in
      plus prose bounds (minimum 30 seconds; 0 or blank = never log out; iPadOS 14.5+).
    - QuotaSize and OnlineAuthenticationGracePeriod (D4): a single advanced / plan-time-verify POINTER
      sentence only — these are Apple-MDM SharedDeviceConfiguration keys Intune reads back but does
      not set in the GUI; NEVER give them a "Recorded as" STD-05 block. Note QuotaSize and cached-
      users (ResidentUsers) are alternates for one mechanism (present either/or), and that the online-
      auth grace (days) is a DISTINCT grace period from the screen-lock passcode grace (seconds) above.

    Federated Managed Apple Account sign-in step (A2): one sentence — end users sign in with their
    federated Managed Apple Account, and the Managed Apple ID is just-in-time created at first sign-
    in. Point to OU-06 for the federation/provisioning setup; do NOT re-author OU-06's provisioning
    matrix.

    Device-licensed VPP apps step (A3): own inline the only working Shared-iPad triple — device-
    licensed VPP (or LOB) apps, Required intent, assigned to the DEVICE group. State that user-
    licensed VPP, App Store apps, and the "Available" intent are unsupported on Shared iPad
    (coordinate with the anti-feature table — terse reference, no third restatement, B5). Link RE-111
    for exhaustive VPP mechanics (narrow the verification cross-link to the device-centric / MDM-
    channel path, NOT the Company-Portal view — CP is unsupported here). What-breaks callout:
    assigning Available or user-licensed, or targeting a user group, leaves the app absent because
    App Store installs are disabled and these app types are Not applicable to Shared iPad assignment.
  </action>
  <verify>
    <automated>grep -q "## Steps" docs/recipes/02-shared-ipad-full-provisioning.md && grep -qi "Enroll without user affinity" docs/recipes/02-shared-ipad-full-provisioning.md && grep -qi "Await final configuration" docs/recipes/02-shared-ipad-full-provisioning.md && grep -q "14400" docs/recipes/02-shared-ipad-full-provisioning.md && grep -qi "device-licensed" docs/recipes/02-shared-ipad-full-provisioning.md && node scripts/validation/c17-eee-contract.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - The ADE step shows the four toggle values (Enable Shared iPad / Enroll without user affinity / Supervised=Yes / Shared iPad=Yes) and explicitly EXCLUDES "Await final configuration" with the reason
    - The Entra shared-device-mode vs Shared iPad distinction is stated correctly inline (T-4); the recipe does not merge them
    - The wipe warning states two distinct facts (unsupported-device targeting vs factory-reset-to-apply), not "any change wipes" (T-6)
    - "Maximum cached users" appears as a settable STD-05 Case-3 block bounded to 24 on a 32/64-GB device (T-1), with sizing consequences as prose outside the blockquote
    - The screen-lock timeout is a Case-2 enum table with the values 0/60/300/900/3600/14400 labeled in seconds (D2)
    - Session inactivity appears as a Case-3 block (min 30, 0/blank = never, iPadOS 14.5+, D3)
    - QuotaSize / OnlineAuthenticationGracePeriod appear only as a plan-time-verify pointer, never as a "Recorded as" block (D4)
    - The federated sign-in step is one sentence linking OU-06; the VPP step owns the device-licensed + Required + device-group triple inline and links RE-111
    - node scripts/validation/c17-eee-contract.mjs --self-test exits 0
  </acceptance_criteria>
  <done>Steps through the VPP step are authored with STD-05 sizing blocks and what-breaks callouts; every contiguous blockquote run is ≤200 chars; C17 self-test exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Docs-only phase: no executable code, no auth flow, no data flow, no user-input handling. The only
output is static Markdown rendered to .docx downstream. No trust boundary is crossed by this work.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Tampering (Integrity) | Recipe content accuracy | mitigate | Primary integrity risk is publishing a factually-wrong provisioning instruction that misconfigures a tenant (e.g., regressing an IPAD requirement-inversion trap). Mitigated by the first-party-verified fact base in 131-CONTEXT.md / 131-RESEARCH.md (every load-bearing claim verified 2× against Microsoft Learn) and the explicit T-1..T-6 trap acceptance criteria on each task. |
| T-131-02 | Tampering | C17 corpus gate | accept | The recipe must pass the frozen C17 validator; no validator edit is in scope. Accepting the gate as authoritative — the file is verified against it, the gate itself is not touched. |
| T-131-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (docs-only) — supply-chain surface is nil. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `node scripts/validation/c17-eee-contract.mjs --self-test` exits 0 after each task.
- The file carries frontmatter doc_id RE-223, doc_type Guide, platform ios+shared-ipad.
- The anti-feature table (IPAD-02) precedes Steps and covers all 7-8 unsupported features with the passcode note adjacent.
- The ADE-enrollment/sizing/sign-in/VPP front (IPAD-01 front, IPAD-04) honors traps T-1, T-4, T-6.
- Full-corpus `--verbose` green including the new file is asserted in plan 131-02's closing task (the file is not yet complete after this plan).
</verification>

<success_criteria>
- `docs/recipes/02-shared-ipad-full-provisioning.md` exists through the VPP step.
- IPAD-02 delivered: all-unsupported anti-feature table + passcode note, in the normal skeleton slot.
- IPAD-04 delivered: cached users (settable, T-1), screen-lock enum, session inactivity as STD-05 blocks; QuotaSize/grace as pointers only.
- IPAD-01 front delivered: ADE enrollment (A1, T-4, T-6), federated sign-in (A2), device-licensed VPP (A3).
- C17 self-test exits 0.
</success_criteria>

<output>
Create `.planning/phases/131-recipe-2-shared-ipad-full-provisioning/131-01-SUMMARY.md` when done.
</output>
