---
phase: 25-navigation-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/common-issues.md
  - docs/quick-ref-l1.md
  - docs/quick-ref-l2.md
autonomous: true
requirements:
  - NAVX-02
  - NAVX-03
must_haves:
  truths:
    - "common-issues.md has a 'Choose Your Platform' selector at the top with anchor links to Windows and macOS sections"
    - "common-issues.md has a '## macOS ADE Failure Scenarios' section covering 6 failure scenarios with L1 and L2 runbook links"
    - "common-issues.md has cross-reference banners in shared Windows symptom categories pointing to macOS equivalents"
    - "macOS content in common-issues.md routes exclusively to macOS runbooks; Windows content routes exclusively to Windows runbooks"
    - "quick-ref-l1.md has a '## macOS ADE Quick Reference' section with top checks, escalation triggers, decision tree link, and runbook links"
    - "quick-ref-l2.md has a '## macOS ADE Quick Reference' section with IntuneMacODC command, Terminal commands, log paths, and investigation runbook links"
    - "All three files have platform: all in frontmatter"
  artifacts:
    - path: "docs/common-issues.md"
      provides: "Cross-platform symptom routing index with macOS ADE section and cross-reference banners"
      contains: "## macOS ADE Failure Scenarios"
    - path: "docs/quick-ref-l1.md"
      provides: "L1 quick-reference card with macOS ADE section"
      contains: "## macOS ADE Quick Reference"
    - path: "docs/quick-ref-l2.md"
      provides: "L2 quick-reference card with macOS ADE section"
      contains: "## macOS ADE Quick Reference"
  key_links:
    - from: "docs/common-issues.md"
      to: "docs/l1-runbooks/10-macos-device-not-appearing.md"
      via: "macOS ADE Failure Scenarios section link"
      pattern: "l1-runbooks/10-macos-device-not-appearing"
    - from: "docs/common-issues.md"
      to: "docs/l2-runbooks/10-macos-log-collection.md"
      via: "macOS ADE Failure Scenarios section link"
      pattern: "l2-runbooks/10-macos-log-collection"
    - from: "docs/quick-ref-l1.md"
      to: "docs/decision-trees/06-macos-triage.md"
      via: "macOS Decision Tree link"
      pattern: "decision-trees/06-macos-triage"
    - from: "docs/quick-ref-l2.md"
      to: "docs/l2-runbooks/10-macos-log-collection.md"
      via: "macOS Investigation Runbooks link"
      pattern: "l2-runbooks/10-macos-log-collection"
---

<objective>
Add macOS ADE content to the three core navigation files: common-issues.md (symptom routing with platform selector and cross-reference banners), quick-ref-l1.md (L1 quick-reference card with macOS checks and escalation triggers), and quick-ref-l2.md (L2 quick-reference card with Terminal commands and log paths).

Purpose: Complete NAVX-02 (cross-platform common-issues routing) and NAVX-03 (updated quick-reference cards with macOS sections) so that support staff can find macOS troubleshooting content through the same navigation structures they already use for Windows.

Output: Three modified Markdown files with macOS sections appended, cross-reference banners added, frontmatter updated to platform: all, and all links pointing to existing Phase 24 macOS runbooks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-navigation-integration-polish/25-CONTEXT.md
@.planning/phases/25-navigation-integration-polish/25-RESEARCH.md

@docs/common-issues.md
@docs/quick-ref-l1.md
@docs/quick-ref-l2.md
@docs/decision-trees/00-initial-triage.md
@docs/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add macOS platform selector, cross-reference banners, and macOS ADE section to common-issues.md</name>
  <files>docs/common-issues.md</files>
  <read_first>
    - docs/common-issues.md (current state -- all 139 lines)
    - docs/index.md lines 16-20 (pattern for "Choose Your Platform" selector)
    - docs/decision-trees/00-initial-triage.md lines 8-9 (cross-reference banner pattern)
    - docs/decision-trees/06-macos-triage.md line 9 (reverse cross-reference banner pattern)
    - docs/l1-runbooks/10-macos-device-not-appearing.md (verify file exists and note exact filename)
    - docs/l1-runbooks/11-macos-setup-assistant-failed.md (verify file exists)
    - docs/l2-runbooks/10-macos-log-collection.md (verify file exists)
    - docs/l2-runbooks/11-macos-profile-delivery.md (verify file exists)
    - docs/l2-runbooks/12-macos-app-install.md (verify file exists)
    - docs/l2-runbooks/13-macos-compliance.md (verify file exists)
  </read_first>
  <action>
Modify docs/common-issues.md with these specific changes (per D-01 through D-06, D-14 through D-16):

**1. Update frontmatter** (per D-04):
Set the YAML frontmatter fields to: `last_verified: 2026-04-15`, `review_by: 2026-07-14`, `applies_to: both`, `audience: all`, `platform: all`. Keep the frontmatter delimiters as-is.

**2. Replace the framework banner** (per D-03):
Replace the existing 2-line framework banner (lines 8-9) with:

> **Platform coverage:** This guide covers Windows Autopilot (classic/APv1 and Device Preparation/APv2) and macOS ADE provisioning issues.
> Not sure which platform or framework? See [APv1 vs APv2](apv1-vs-apv2.md) for Windows or [Windows vs macOS](windows-vs-macos.md) for cross-platform.

**3. Change the H1 title** (per D-03):
Replace `# Common Windows Autopilot Issues` with `# Common Provisioning Issues`

**4. Insert "Choose Your Platform" selector** (per D-02):
Insert immediately after the new H1 title, before the existing "Not sure which framework?" blockquote. The selector contains:

`## Choose Your Platform`

Then a bullet list:
- `[Windows Autopilot Issues](#windows-autopilot-issues)` with description "Windows device provisioning failures (APv1 and APv2)"
- `[macOS ADE Failure Scenarios](#macos-ade-failure-scenarios)` with description "macOS enrollment and management failures"

Then a horizontal rule separator.

**5. Wrap existing Windows content under new H2** (per D-02):
Replace the existing `> **Not sure which framework?**` blockquote and the `Symptom-based index routing...` paragraph and `**Framework:** APv1 (classic)` line with:

`## Windows Autopilot Issues`

Followed by a macOS cross-reference banner: `> **macOS:** For macOS ADE troubleshooting, see [macOS ADE Failure Scenarios](#macos-ade-failure-scenarios).`

Then retain the framework guidance blockquote: `> **Not sure which framework?** If you don't know whether the device is using APv1 (classic) or APv2 (Device Preparation), see [APv1 vs APv2](apv1-vs-apv2.md) to identify the framework before selecting a runbook.`

Then `Symptom-based index routing to the appropriate L1 and L2 runbooks.`

Then `**Framework:** APv1 (classic)`

IMPORTANT: All existing Windows section headings (Device Registration Issues, ESP Failures, etc.) become H3 (`###`) under the new `## Windows Autopilot Issues` H2. The APv2 section heading becomes `### APv2 Failure Scenarios`. Similarly, Device Reset, Migration, and Security sections all become H3.

**6. Add cross-reference banners to shared Windows symptom categories** (per D-06, D-15, D-16):
Add a one-line cross-reference banner AFTER the heading and BEFORE the description text in these specific sections only:

In `### Device Registration Issues`, add: `> **macOS:** For macOS device not appearing in Intune, see [macOS: Device Not Appearing](#device-not-appearing-in-intune).`

In `### Profile Assignment Issues`, add: `> **macOS:** For macOS configuration profile issues, see [macOS: Profile Not Applied](#configuration-profile-not-applied).`

In `### Network Connectivity Issues`, add: `> **macOS:** macOS ADE has different endpoint requirements. See [macOS ADE Failure Scenarios](#macos-ade-failure-scenarios) for macOS-specific network issues.`

In `### Security and Enrollment Issues`, add: `> **macOS:** Conditional Access enrollment timing applies cross-platform. See [macOS: Compliance / Access Blocked](#compliance-failure-or-access-blocked) for macOS-specific guidance.`

Do NOT add banners to: ESP Failures, TPM Attestation, Policy Conflicts, Hybrid Join, Device Renamed, APv2 sections, Device Reset, or Migration Issues (these are Windows-only per D-06).

**7. Append macOS ADE Failure Scenarios section** (per D-01, D-05):
After the Version History table, add a horizontal rule then the macOS section:

`## macOS ADE Failure Scenarios`

With a Windows cross-reference banner: `> **Windows:** For Windows Autopilot issues, see [Windows Autopilot Issues](#windows-autopilot-issues).`

Then `**Platform:** macOS ADE (Automated Device Enrollment)`

Then: `Symptom-based index routing to the appropriate macOS L1 and L2 runbooks. Start with the [macOS ADE Triage Decision Tree](decision-trees/06-macos-triage.md) to identify the failure scenario.`

Then 6 subsections (each H3) with symptom description + L1 link + L2 link:

`### Device Not Appearing in Intune` -- Mac serial number not found in Intune admin center after ADE enrollment attempt.
- L1: [Device Not Appearing](l1-runbooks/10-macos-device-not-appearing.md)
- L2: [macOS Log Collection](l2-runbooks/10-macos-log-collection.md) + case-by-case investigation

`### Setup Assistant Stuck or Failed` -- Setup Assistant authentication failure, Await Configuration stuck, or network connectivity issue during enrollment.
- L1: [Setup Assistant Failed](l1-runbooks/11-macos-setup-assistant-failed.md)
- L2: [macOS Log Collection](l2-runbooks/10-macos-log-collection.md) + [Profile Delivery Investigation](l2-runbooks/11-macos-profile-delivery.md)

`### Configuration Profile Not Applied` -- Expected configuration (Wi-Fi, VPN, FileVault, restrictions) missing after enrollment.
- L1: [Profile Not Applied](l1-runbooks/12-macos-profile-not-applied.md)
- L2: [Profile Delivery Investigation](l2-runbooks/11-macos-profile-delivery.md)

`### App Not Installed` -- DMG, PKG, or VPP app not installed or showing failed status after enrollment.
- L1: [App Not Installed](l1-runbooks/13-macos-app-not-installed.md)
- L2: [App Install Failure Diagnosis](l2-runbooks/12-macos-app-install.md)

`### Compliance Failure or Access Blocked` -- Device non-compliant in Intune or user cannot access Microsoft 365 resources despite enrollment.
- L1: [Compliance / Access Blocked](l1-runbooks/14-macos-compliance-access-blocked.md)
- L2: [Compliance Evaluation Investigation](l2-runbooks/13-macos-compliance.md)

`### Company Portal Sign-In Failure` -- Company Portal not available, sign-in failing, or Entra registration incomplete.
- L1: [Company Portal Sign-In](l1-runbooks/15-macos-company-portal-sign-in.md)
- L2: [Compliance Evaluation Investigation](l2-runbooks/13-macos-compliance.md) (for Entra registration issues)

**8. Update Version History** -- add a new row at the top of the Version History table:
`| 2026-04-15 | Added macOS ADE Failure Scenarios section, platform selector, cross-reference banners; updated title and frontmatter for cross-platform coverage | -- |`
  </action>
  <verify>
    <automated>cd D:/claude/Autopilot && grep -c "## Choose Your Platform" docs/common-issues.md && grep -c "## macOS ADE Failure Scenarios" docs/common-issues.md && grep -c "## Windows Autopilot Issues" docs/common-issues.md && grep -c "platform: all" docs/common-issues.md && grep -c "# Common Provisioning Issues" docs/common-issues.md && grep "macOS.*Device Not Appearing" docs/common-issues.md | head -3 && grep "macOS.*Profile Not Applied" docs/common-issues.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - docs/common-issues.md frontmatter contains `platform: all`
    - docs/common-issues.md contains `# Common Provisioning Issues` (not `# Common Windows Autopilot Issues`)
    - docs/common-issues.md contains `## Choose Your Platform` with anchor links to `#windows-autopilot-issues` and `#macos-ade-failure-scenarios`
    - docs/common-issues.md contains `## Windows Autopilot Issues` as an H2
    - docs/common-issues.md contains `## macOS ADE Failure Scenarios` as an H2
    - docs/common-issues.md macOS section contains links to all 6 L1 runbooks: `l1-runbooks/10-macos-device-not-appearing.md` through `l1-runbooks/15-macos-company-portal-sign-in.md`
    - docs/common-issues.md macOS section contains links to all 4 L2 runbooks: `l2-runbooks/10-macos-log-collection.md` through `l2-runbooks/13-macos-compliance.md`
    - docs/common-issues.md contains cross-reference banner `> **macOS:**` in Device Registration Issues section
    - docs/common-issues.md contains cross-reference banner `> **macOS:**` in Profile Assignment Issues section
    - docs/common-issues.md does NOT contain `> **macOS:**` banner in ESP Failures or TPM Attestation sections
    - docs/common-issues.md macOS section contains `> **Windows:**` reverse banner pointing to `#windows-autopilot-issues`
    - No macOS Terminal commands, log paths, or diagnostic procedures appear within any Windows section
    - No Windows PowerShell commands or registry paths appear within the macOS section
  </acceptance_criteria>
  <done>common-issues.md has a cross-platform title, platform selector, Windows section with cross-reference banners in shared categories, and a complete macOS ADE section with 6 failure scenarios routing to the correct macOS L1 and L2 runbooks. Platform frontmatter is `all`.</done>
</task>

<task type="auto">
  <name>Task 2: Append macOS ADE sections to L1 and L2 quick-reference cards</name>
  <files>docs/quick-ref-l1.md, docs/quick-ref-l2.md</files>
  <read_first>
    - docs/quick-ref-l1.md (current state -- all 86 lines, note APv2 section structure at lines 50-79)
    - docs/quick-ref-l2.md (current state -- all 135 lines, note APv2 section structure at lines 77-128)
    - docs/reference/macos-commands.md (source for Terminal commands to include in L2 card)
    - docs/reference/macos-log-paths.md (source for log paths to include in L2 card)
    - docs/decision-trees/06-macos-triage.md (verify exists -- link target for L1 card)
  </read_first>
  <action>
**Part A: Modify docs/quick-ref-l1.md** (per D-07, D-09, D-10):

**1. Update frontmatter** (per D-09):
Set the YAML frontmatter fields to: `last_verified: 2026-04-15`, `review_by: 2026-07-14`, `applies_to: both`, `audience: L1`, `platform: all`.

**2. Update framework banner** -- replace the existing 2-line framework banner (lines 8-9) with:

> **Platform coverage:** This card covers Windows Autopilot (classic/APv1), Autopilot Device Preparation (APv2), and macOS ADE.
> Sections are labeled by platform/framework. See [APv1 vs APv2](apv1-vs-apv2.md) for Windows framework selection or [Windows vs macOS](windows-vs-macos.md) for cross-platform.

**3. Append macOS ADE section** (per D-07, D-10):
After the APv2 Version History table, add a horizontal rule then the macOS section:

`## macOS ADE Quick Reference`

`**Platform:** macOS ADE (Automated Device Enrollment)`

Then `### Top Checks` with 4 numbered items:
1. **Device in ABM?** -- Apple Business Manager > Devices -- search by serial number, verify MDM server assignment shows your Intune MDM server name
2. **Device in Intune?** -- Intune admin center > Devices > macOS -- search by serial number, check enrollment status
3. **Enrollment profile assigned?** -- Intune > Devices > Enrollment > Apple > Enrollment program tokens > [token] > Profiles -- verify profile is assigned to the device
4. **Compliance state?** -- Intune > Devices > macOS > [device] > Device compliance -- check for "Compliant" or review non-compliant settings

Then `### macOS Escalation Triggers` with 5 bullet items:
- Serial in ABM but device not in Intune after 24 hours --> **Escalate L2** (collect: serial number, ABM MDM server assignment screenshot, Intune enrollment status)
- Setup Assistant stuck or authentication failure after one retry --> **Escalate L2** (collect: serial number, screenshot of error, macOS version)
- Configuration profile not applied after 4-hour sync wait and manual sync --> **Escalate L2** (collect: serial number, expected profile name, Intune device compliance screenshot)
- App showing "Failed" in Intune after reinstall attempt --> **Escalate L2** (collect: app name, app type, Intune app install status screenshot)
- Device non-compliant but user believes settings are correct --> **Escalate L2** (collect: non-compliant setting names, device serial)

Then `### macOS Decision Tree` with link:
- [macOS ADE Triage](decision-trees/06-macos-triage.md) -- start here for macOS ADE failures

Then `### macOS Runbooks` with 6 links:
- [Device Not Appearing](l1-runbooks/10-macos-device-not-appearing.md)
- [Setup Assistant Failed](l1-runbooks/11-macos-setup-assistant-failed.md)
- [Profile Not Applied](l1-runbooks/12-macos-profile-not-applied.md)
- [App Not Installed](l1-runbooks/13-macos-app-not-installed.md)
- [Compliance / Access Blocked](l1-runbooks/14-macos-compliance-access-blocked.md)
- [Company Portal Sign-In](l1-runbooks/15-macos-company-portal-sign-in.md)

**4. Update Version History** -- add row: `| 2026-04-15 | Added macOS ADE Quick Reference section with top checks, escalation triggers, and runbook links | -- |`

**Part B: Modify docs/quick-ref-l2.md** (per D-08, D-09, D-10):

**1. Update frontmatter** (per D-09):
Set the YAML frontmatter fields to: `last_verified: 2026-04-15`, `review_by: 2026-07-14`, `applies_to: both`, `audience: L2`, `platform: all`.

**2. Update framework banner** -- replace the existing 2-line framework banner (lines 8-9) with:

> **Platform coverage:** This card covers Windows [Autopilot](_glossary.md#autopilot) (classic/APv1), Autopilot Device Preparation (APv2), and macOS ADE.
> Sections are labeled by platform/framework. See [APv1 vs APv2](apv1-vs-apv2.md) for Windows framework selection or [Windows vs macOS](windows-vs-macos.md) for cross-platform.

**3. Append macOS ADE section** (per D-08, D-10):
After the APv2 Version History table, add a horizontal rule then the macOS section:

`## macOS ADE Quick Reference`

`**Platform:** macOS ADE (Automated Device Enrollment)`

Then `### macOS Log Collection` with text "Download and run IntuneMacODC:" followed by a bash code block:
curl -L https://aka.ms/IntuneMacODC -o IntuneMacODC.sh
chmod u+x ./IntuneMacODC.sh
sudo ./IntuneMacODC.sh

Then `### Key Terminal Commands` with a bash code block containing 5 commands with comments:
# Check MDM enrollment status
profiles status -type enrollment
# List installed configuration profiles
sudo profiles show
# Query MDM events from last hour
log show --predicate 'subsystem == "com.apple.ManagedClient"' --info --last 1h
# Output all profile data
system_profiler SPConfigurationProfileDataType
# Verify Intune agent running
pgrep -il "^IntuneMdm"

Then `### Critical Log Paths` with a table (Path | Purpose):
- `/Library/Logs/Microsoft/Intune/IntuneMDMDaemon*.log` | Intune daemon -- PKG/DMG installs, scripts, policy
- `~/Library/Logs/Microsoft/Intune/IntuneMDMAgent*.log` | Intune agent -- user-context scripts, user policy
- `/Library/Logs/Microsoft/Intune/CompanyPortal*.log` | Company Portal enrollment, registration, compliance

Then: `Unified log subsystems: com.apple.ManagedClient (profile events), com.apple.ManagedClient.cloudconfigurationd (ADE enrollment)`

Then: `Full reference: [macOS Terminal Commands](reference/macos-commands.md) | [macOS Log Paths](reference/macos-log-paths.md)`

Then `### macOS Investigation Runbooks` with 4 links:
- [macOS Log Collection Guide](l2-runbooks/10-macos-log-collection.md) -- prerequisite for all macOS investigations
- [Profile Delivery Investigation](l2-runbooks/11-macos-profile-delivery.md)
- [App Install Failure Diagnosis](l2-runbooks/12-macos-app-install.md)
- [Compliance Evaluation Investigation](l2-runbooks/13-macos-compliance.md)

**4. Update Version History** -- add row: `| 2026-04-15 | Added macOS ADE Quick Reference section with log collection, Terminal commands, log paths, and investigation runbook links | -- |`
  </action>
  <verify>
    <automated>cd D:/claude/Autopilot && grep -c "## macOS ADE Quick Reference" docs/quick-ref-l1.md && grep -c "## macOS ADE Quick Reference" docs/quick-ref-l2.md && grep -c "platform: all" docs/quick-ref-l1.md && grep -c "platform: all" docs/quick-ref-l2.md && grep "decision-trees/06-macos-triage" docs/quick-ref-l1.md && grep "l2-runbooks/10-macos-log-collection" docs/quick-ref-l2.md && grep "IntuneMacODC" docs/quick-ref-l2.md && grep "l1-runbooks/10-macos" docs/quick-ref-l1.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/quick-ref-l1.md frontmatter contains `platform: all`
    - docs/quick-ref-l1.md contains `## macOS ADE Quick Reference` heading
    - docs/quick-ref-l1.md macOS section contains 4 numbered top checks (ABM, Intune, enrollment profile, compliance)
    - docs/quick-ref-l1.md macOS section contains 5 escalation triggers with "Escalate L2" and data collection instructions
    - docs/quick-ref-l1.md macOS section contains link to `decision-trees/06-macos-triage.md`
    - docs/quick-ref-l1.md macOS section contains links to all 6 L1 runbooks: `l1-runbooks/10-macos-device-not-appearing.md` through `l1-runbooks/15-macos-company-portal-sign-in.md`
    - docs/quick-ref-l2.md frontmatter contains `platform: all`
    - docs/quick-ref-l2.md contains `## macOS ADE Quick Reference` heading
    - docs/quick-ref-l2.md macOS section contains IntuneMacODC download command with `curl -L https://aka.ms/IntuneMacODC`
    - docs/quick-ref-l2.md macOS section contains `profiles status -type enrollment` command
    - docs/quick-ref-l2.md macOS section contains `log show --predicate` command
    - docs/quick-ref-l2.md macOS section contains 3 critical log paths in a table: IntuneMDMDaemon, IntuneMDMAgent, CompanyPortal
    - docs/quick-ref-l2.md macOS section contains links to `reference/macos-commands.md` and `reference/macos-log-paths.md`
    - docs/quick-ref-l2.md macOS section contains links to all 4 L2 runbooks: `l2-runbooks/10-macos-log-collection.md` through `l2-runbooks/13-macos-compliance.md`
    - No macOS Terminal commands or log paths appear in the Windows APv1 or APv2 sections of either file
    - No Windows PowerShell commands or registry paths appear in the macOS sections of either file
  </acceptance_criteria>
  <done>Both quick-reference cards have macOS ADE sections appended following the established APv2 pattern, with platform: all frontmatter, macOS-specific content (checks, commands, log paths), and links to all macOS runbooks. No cross-contamination between platform sections.</done>
</task>

</tasks>

<verification>
Run these checks after both tasks complete:

1. Platform separation check -- verify no macOS Terminal commands (profiles, log show, system_profiler, pgrep) appear in Windows sections of any file.

2. All macOS L1 runbook links present in common-issues.md (l1-runbooks/10-macos through l1-runbooks/15-macos -- each should appear once).

3. Anchor consistency check -- verify the "Choose Your Platform" anchor targets exist as headings: `## Windows Autopilot Issues` and `## macOS ADE Failure Scenarios`.

4. Frontmatter check -- `platform: all` appears in common-issues.md, quick-ref-l1.md, and quick-ref-l2.md.
</verification>

<success_criteria>
- common-issues.md is a cross-platform symptom routing index with platform selector, Windows section with cross-reference banners, and macOS section with 6 failure scenarios
- quick-ref-l1.md has macOS ADE section with top checks, escalation triggers, decision tree link, and 6 runbook links
- quick-ref-l2.md has macOS ADE section with IntuneMacODC command, 5 Terminal commands, 3 log paths, and 4 investigation runbook links
- All three files have platform: all frontmatter
- Zero cross-contamination: macOS content only in macOS sections, Windows content only in Windows sections
- Cross-reference banners in shared Windows categories (Device Registration, Profile Assignment, Network, Security) point to macOS equivalents
- All links to macOS runbooks, decision trees, and reference files use correct relative paths
</success_criteria>

<output>
After completion, create `.planning/phases/25-navigation-integration-polish/25-01-SUMMARY.md`
</output>
