---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 07
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - docs/admin-setup-apv1/01-hardware-hash-upload.md
  - docs/admin-setup-8021x/00-overview.md
  - docs/admin-setup-8021x/01-eap-method-overview.md
autonomous: true
requirements: [RETRO-08]

must_haves:
  truths:
    - "apv1/01 is a mermaid-free decision table (2 diamonds OEM/BATCH preserved) with all 8 over-200c blockquote groups split to <=200c"
    - "8021x/00 is a numbered stage list; 8021x/01's sequenceDiagram becomes an ordered numbered step list (10 messages)"
    - "Each carries the EEE header block + doc_id (Guide) + ## Summary-first; C17 exits 0"
  artifacts:
    - path: "docs/admin-setup-apv1/01-hardware-hash-upload.md"
      provides: "Decision table (LOCKED — 10) + 8 blockquote groups remediated"
      contains: "LOCKED — 10"
    - path: "docs/admin-setup-8021x/00-overview.md"
      provides: "Numbered stage list (LOCKED — 3)"
      contains: "LOCKED — 3"
    - path: "docs/admin-setup-8021x/01-eap-method-overview.md"
      provides: "Ordered step list from sequenceDiagram (LOCKED — 10 messages)"
      contains: "LOCKED"
  key_links:
    - from: "docs/admin-setup-apv1/01-hardware-hash-upload.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-077 (Guide) injected by fork"
      pattern: "doc_id:\\s*RE-077"
---

<objective>
Convert 3 admin-setup carve-outs: apv1/01-hardware-hash-upload (FULL, 2 diamonds, the heaviest blockquote burden of the carved class at 8 groups) and the 8021x pair (00 linear, 01 sequenceDiagram). Sequence diagrams convert to ordered numbered step lists per D-02 (NOT tables).

Purpose: RETRO-08 — admin-setup carved-mermaid files retrofitted.
Output: 3 enrolled, C17-green files (RE-077, RE-134, RE-135; Guide per RULING R2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
@docs/_standards/EEE-SOP-standard.md
@docs/decision-trees/10-8021x-triage.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert + enroll apv1/01-hardware-hash-upload.md (RE-077, Guide, LOCKED — 10, 8 blockquote groups)</name>
  <read_first>
    - docs/admin-setup-apv1/01-hardware-hash-upload.md
    - 122-RESEARCH.md Class 2 row for apv1/01 (flowchart TD, 6 nodes / 5 edges (4 labeled) / 2 diamonds OEM, BATCH; 8 existing >200c BQ groups) + Pitfall 5 (blockquote workload)
    - 122-PATTERNS.md §2 (decision-table shape) + §3; docs/decision-trees/10-8021x-triage.md
    - STATE.md [Phase 121-03] (the reusable link/backtick-aware word-preserving greedy blockquote packer — reuse, don't rebuild)
  </read_first>
  <action>
    `git show 71be4ab:docs/admin-setup-apv1/01-hardware-hash-upload.md` to capture the element set (LOCKED-N 10 = 6 nodes + 4 labeled edges; 2 diamonds OEM and BATCH). `{...}` present -> ordinal-column decision table with both OEM and BATCH decisions as rows. Delete fence + stale prose. Annotate `LOCKED — 10 (nodes + labeled edges)` (R1, re-verify). Split ALL 8 existing >200c blockquote groups using the word-preserving Transform A/B discipline + C17's join-then-measure grouping (a genuine blank/non-`>` line breaks a group) — reuse the Phase-121 greedy packer per STATE.md [121-03], word-set multiset diff (strip `>` prefix) to prove zero real-word loss. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/admin-setup-apv1/01-hardware-hash-upload.md` (doc_id RE-077, Guide). Author `## Summary` (>=30 words — hardware hash upload paths: OEM vs manual batch).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/admin-setup-apv1/01-hardware-hash-upload.md; echo "c17=$?"; grep -c '^```mermaid' docs/admin-setup-apv1/01-hardware-hash-upload.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/admin-setup-apv1/01-hardware-hash-upload.md` = 0
    - C17 exits 0 (proves all 8 blockquote groups now ≤200c, assertion #12 green); `grep -c 'LOCKED — 10'` >= 1; `grep -c '^doc_id: RE-077'` = 1; doc_type Guide
    - Both OEM and BATCH diamonds represented as decision rows; stale-prose grep clean; word-set diff shows zero real-word loss on the 8 splits
  </acceptance_criteria>
  <done>apv1/01-hardware-hash-upload.md is mermaid-free, C17-green (incl. 8 blockquote splits), RE-077 (Guide) enrolled.</done>
</task>

<task type="auto">
  <name>Task 2: Convert + enroll admin-setup-8021x/00-overview.md (RE-134, Guide, LOCKED — 3)</name>
  <read_first>
    - docs/admin-setup-8021x/00-overview.md
    - 122-RESEARCH.md Class 2 row for 8021x/00 (linear graph LR, 3 nodes / 2 plain edges / 0 diamonds; 2 BQ)
    - 122-PATTERNS.md §3 (EEE Guide envelope); D-02 (no diamond -> numbered stage list)
  </read_first>
  <action>
    `git show 71be4ab:docs/admin-setup-8021x/00-overview.md` to capture the element set (LOCKED-N 3 = 3 nodes + 0 labeled edges; 2 plain edges). NO diamond -> ordered numbered stage list (D-02 bright-line). Delete fence + stale prose. Annotate `LOCKED — 3 (nodes + labeled edges)` (R1, re-verify). Split both existing >200c blockquote groups. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/admin-setup-8021x/00-overview.md` (doc_id RE-134, Guide). Author `## Summary` (>=30 words — 802.1X admin-setup overview).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/admin-setup-8021x/00-overview.md; echo "c17=$?"; grep -c '^```mermaid' docs/admin-setup-8021x/00-overview.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/admin-setup-8021x/00-overview.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 3'` >= 1; `grep -c '^doc_id: RE-134'` = 1; doc_type Guide
    - Both blockquote groups ≤200c; stale-prose grep clean
  </acceptance_criteria>
  <done>admin-setup-8021x/00-overview.md is mermaid-free, C17-green, RE-134 (Guide) enrolled.</done>
</task>

<task type="auto">
  <name>Task 3: Convert + enroll admin-setup-8021x/01-eap-method-overview.md (RE-135, Guide, sequenceDiagram -> step list)</name>
  <read_first>
    - docs/admin-setup-8021x/01-eap-method-overview.md
    - 122-RESEARCH.md Class 2 row for 8021x/01 (sequenceDiagram, 3 actors / 10 messages; 2 BQ) + D-02 sequence rule
    - 122-PATTERNS.md §3; docs/reference/ca-enrollment-timing.md pattern note (sequence -> step list)
  </read_first>
  <action>
    `git show 71be4ab:docs/admin-setup-8021x/01-eap-method-overview.md` to capture the sequenceDiagram (3 actors, 10 messages). Per D-02, a sequenceDiagram converts to an ORDERED NUMBERED STEP LIST (not a table) — one ordered step per message, preserving actor->actor direction and message label. Delete fence + stale prose. Annotate `LOCKED — 10 (messages)` (ordered-step convention for sequence diagrams; re-verify all 10 messages present). Split both existing >200c blockquote groups. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/admin-setup-8021x/01-eap-method-overview.md` (doc_id RE-135, Guide). Author `## Summary` (>=30 words — EAP method selection sequence).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/admin-setup-8021x/01-eap-method-overview.md; echo "c17=$?"; grep -c '^```mermaid' docs/admin-setup-8021x/01-eap-method-overview.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/admin-setup-8021x/01-eap-method-overview.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 10'` >= 1; `grep -c '^doc_id: RE-135'` = 1; doc_type Guide
    - All 10 messages present as ordered steps; both blockquotes ≤200c; stale-prose grep clean
  </acceptance_criteria>
  <done>admin-setup-8021x/01-eap-method-overview.md is mermaid-free, C17-green, RE-135 (Guide) enrolled as an ordered step list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-conversion -> converted table/list | apv1/01's 2 diamonds and 8021x/01's 10 messages are the loss surface |
| blockquote remediation -> file | word-preserving splits risk real-word drift if not diff-proven |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | dropping an OEM/BATCH decision or a sequence message | mitigate | one-row/one-step per element; LOCKED-N; D-01 re-derivation in 122-13 |
| T-122-08 | Tampering | word-loss during apv1/01's 8 blockquote splits | mitigate | reuse Phase-121 greedy packer; word-set multiset diff proof (strip `>` prefix) |
| T-122-05 | Repudiation | stale diagram-prose left behind | mitigate | delete in-task + stale-prose grep gate |
| T-122-SC | Tampering | package installs | accept | none — content edits + node built-in fork only |
</threat_model>

<verification>
- C17 exits 0 on all 3 files; 0 ```mermaid fences; LOCKED-N 10/3/10 present; doc_id RE-077/134/135 (Guide) injected; apv1/01's 8 blockquotes ≤200c; stale-prose grep clean
</verification>

<success_criteria>
apv1/01 + the 8021x pair are mermaid-free, C17-green, enrolled as Guide; sequence diagram converted to an ordered step list; apv1/01's heavy blockquote burden remediated with zero word loss.
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-07-SUMMARY.md` when done.
</output>
