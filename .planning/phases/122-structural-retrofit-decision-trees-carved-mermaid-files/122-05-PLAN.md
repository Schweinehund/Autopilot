---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 05
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - docs/decision-trees/09-linux-triage.md
  - docs/decision-trees/10-8021x-triage.md
autonomous: true
requirements: [RETRO-05]

must_haves:
  truths:
    - "09-linux-triage is a mermaid-free decision table (LOCKED — 12); C17 exits 0"
    - "10-8021x-triage has its live ```mermaid fence AND its stale Legend section removed, its existing Routing Verification table kept, and its annotation upgraded to the R1 nodes+labeled-edges convention (LOCKED — 11)"
    - "Both carry the EEE header block + doc_id + ## Summary-first"
  artifacts:
    - path: "docs/decision-trees/09-linux-triage.md"
      provides: "Decision table (LOCKED — 12)"
      contains: "LOCKED — 12"
    - path: "docs/decision-trees/10-8021x-triage.md"
      provides: "Fence + Legend removed; Routing Verification table retained; LOCKED — 11 annotation; RE-217 enrolled"
      contains: "LOCKED — 11"
  key_links:
    - from: "docs/decision-trees/10-8021x-triage.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-217 injected by fork"
      pattern: "doc_id:\\s*RE-217"
---

<objective>
Convert the final two decision-trees. 10-8021x-triage is the STD-04-cited exemplar but is NOT yet actually converted — it still carries a live ```mermaid fence (lines 27-47) AND a stale "Legend" section (lines 17-23) describing diamond/rounded-node shapes alongside its already-drafted Routing Verification table. Both the fence and the Legend must be deleted (the Legend becomes stale diagram-prose the instant the fence goes — the exact D-01 RIDER 3 defect class). Its LOCKED-N annotation is upgraded to the R1 convention (nodes + labeled edges = 11, not the older 5-leaf count).

Purpose: RETRO-05 — decision-trees retrofitted; establishes the R1 annotation precedent applied across all 11 decision-trees.
Output: 2 enrolled, C17-green files (RE-216, RE-217).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
@docs/_standards/EEE-SOP-standard.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert + enroll 09-linux-triage.md (RE-216, LOCKED — 12)</name>
  <read_first>
    - docs/decision-trees/09-linux-triage.md (carries platform key)
    - 122-RESEARCH.md Class 1 row for 09 (7 nodes / 5 labeled edges / 2 diamonds; no reconvergence; 1 existing BQ group)
    - 122-PATTERNS.md §2 + §3; docs/decision-trees/10-8021x-triage.md (exemplar shape)
  </read_first>
  <action>
    `git show 71be4ab:docs/decision-trees/09-linux-triage.md` to capture the element set (LOCKED-N 12 = 7 nodes + 5 labeled edges; 2 diamonds). `{...}` present -> ordinal-column decision table. Delete fence + stale prose. Annotate `LOCKED — 12 (nodes + labeled edges)` (R1, re-verify). Split the 1 existing >200c blockquote group. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/decision-trees/09-linux-triage.md` (doc_id RE-216). Author `## Summary` (>=30 words — Linux Intune client triage, Ubuntu LTS).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/decision-trees/09-linux-triage.md; echo "c17=$?"; grep -c '^```mermaid' docs/decision-trees/09-linux-triage.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/decision-trees/09-linux-triage.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 12'` >= 1; `grep -c '^doc_id: RE-216'` = 1
    - Both diamonds represented; pre-existing blockquote ≤200c; stale-prose grep clean
  </acceptance_criteria>
  <done>09-linux-triage.md is mermaid-free, C17-green, RE-216 enrolled.</done>
</task>

<task type="auto">
  <name>Task 2: Convert + enroll 10-8021x-triage.md (RE-217, delete fence + Legend, upgrade to LOCKED — 11)</name>
  <read_first>
    - docs/decision-trees/10-8021x-triage.md (full — the STD-04 exemplar; live fence lines 27-47, stale Legend lines 17-23, existing Routing Verification table lines 53-59)
    - 122-RESEARCH.md Class 1 row for 10 (6 nodes / 5 labeled edges / 1 diamond) + Open Question 1 (the LOCKED-N convention upgrade ruling)
    - 122-CONTEXT.md D-02 Amendment 3 (container-neutral LOCKED-N = nodes + labeled edges) + 122-01 RULING R1
    - 122-PATTERNS.md §2 (this file IS the exemplar shape; and its Legend is the anti-pattern to delete)
  </read_first>
  <action>
    `git show 71be4ab:docs/decision-trees/10-8021x-triage.md` to capture the element set (LOCKED-N 11 = 6 nodes + 5 labeled edges; 1 diamond). DELETE the live ```mermaid fence (lines ~27-47) AND the stale `## Legend` section (lines ~17-23, which describes diamond/rounded-node shape semantics that go stale the instant the fence is removed). KEEP the existing `## Routing Verification` ordinal-column table (Path | Step 1 | Step 2 | Destination) — verify it still preserves all 5 destination rows against the git-show bytes; add any missing node/labeled-edge coverage so the table is complete under the R1 convention. UPGRADE the annotation to `LOCKED — 11 (nodes + labeled edges)` (R1) — do NOT leave the older "5 leaves"-style count. Split the 1 existing >200c blockquote group. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/decision-trees/10-8021x-triage.md` (doc_id RE-217). Author `## Summary` (>=30 words — 802.1X EAP triage).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/decision-trees/10-8021x-triage.md; echo "c17=$?"; grep -c '^```mermaid' docs/decision-trees/10-8021x-triage.md; grep -c '^## Legend' docs/decision-trees/10-8021x-triage.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/decision-trees/10-8021x-triage.md` = 0
    - `grep -c '^## Legend' docs/decision-trees/10-8021x-triage.md` = 0 (stale Legend removed)
    - C17 exits 0; `grep -c 'LOCKED — 11'` >= 1; `grep -c '^doc_id: RE-217'` = 1
    - Routing Verification table retained with all 5 destinations; stale-prose grep clean (`grep -Ei 'click the leaf|node shape|classDef|rounded'` returns nothing)
  </acceptance_criteria>
  <done>10-8021x-triage.md has its fence + stale Legend removed, Routing Verification table retained, annotation upgraded to LOCKED — 11, RE-217 enrolled, C17-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-conversion -> converted table | file 10's Legend is live stale-prose that survives a naive completeness diff |
| fork -> enrolled file | enrolling file 10 while its fence is live trips C17 #1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-05 | Repudiation | file 10's stale `## Legend` + "click the leaf" prose left behind, green under C17 (no diagram parser) | mitigate | explicit Legend deletion + stale-prose grep gate; D-01 re-derivation in 122-12 |
| T-122-01 | Tampering | Routing Verification table missing a node/labeled edge under the new R1 count | mitigate | re-derive N=11 from git-show bytes; complete the table before enroll |
| T-122-02 | Tampering | fork enrolling file 10 with fence still present | mitigate | fork MERMAID-STILL-PRESENT precondition fail-closes |
| T-122-SC | Tampering | package installs | accept | none — content edits + node built-in fork only |
</threat_model>

<verification>
- C17 exits 0 on both files; 0 ```mermaid fences; file 10's `## Legend` removed; LOCKED-N 12/11 present; doc_id RE-216/217 injected; stale-prose grep clean
</verification>

<success_criteria>
Decision-trees 09/10 are mermaid-free, C17-green, enrolled; file 10's fence + stale Legend removed and its annotation upgraded to the R1 nodes+labeled-edges convention, completing all 11 decision-trees.
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-05-SUMMARY.md` when done.
</output>
