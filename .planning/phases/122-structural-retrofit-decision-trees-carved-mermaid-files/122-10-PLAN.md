---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 10
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - docs/lifecycle-apv2/02-deployment-flow.md
  - docs/ios-lifecycle/01-ade-lifecycle.md
  - docs/ios-lifecycle/02-mdm-migration.md
autonomous: true
requirements: [RETRO-07]

must_haves:
  truths:
    - "lifecycle-apv2/02 is classified PER BLOCK: block 1 (linear chain) -> numbered stage list, block 2 (failure-annotation map, 5 dashed F_*->S* edges) -> Stage | Failure Mode table"
    - "ios-lifecycle/01 + /02 are numbered stage lists; ios-lifecycle/02's 10 over-200c blockquote groups are all split to <=200c"
    - "Each carries the EEE header block + doc_id (Guide) + ## Summary-first; C17 exits 0"
  artifacts:
    - path: "docs/lifecycle-apv2/02-deployment-flow.md"
      provides: "Two converted blocks (numbered list + Stage|Failure table with all 5 F_* dashed edges)"
      contains: "Failure Mode"
    - path: "docs/ios-lifecycle/02-mdm-migration.md"
      provides: "Numbered stage list (LOCKED — 7) + 10 blockquote groups remediated"
      contains: "LOCKED — 7"
  key_links:
    - from: "docs/lifecycle-apv2/02-deployment-flow.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-200 (Guide) injected by fork; flips Pending->Approved in 122-15"
      pattern: "doc_id:\\s*RE-200"
---

<objective>
Convert lifecycle-apv2/02 (2 blocks — CONTEXT's cited "second block a triage missed": 5 dashed failure-mapping edges F_REG/F_IME/F_APP1/F_SCRIPT/F_APP2 -> S3/S4/S7/S8/S9) plus the two ios-lifecycle files (linear chains; ios/02 carries the corpus's heaviest blockquote COUNT at 10 groups).

Purpose: RETRO-07 remainder — 3 more of the 9 Mermaid-bearing lifecycle files.
Output: 3 enrolled, C17-green files (RE-200, RE-190, RE-191; Guide).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
@docs/_standards/EEE-SOP-standard.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert + enroll lifecycle-apv2/02-deployment-flow.md (RE-200, Guide, 2 blocks — classify per block)</name>
  <read_first>
    - docs/lifecycle-apv2/02-deployment-flow.md
    - 122-RESEARCH.md Class 3 row for lifecycle-apv2/02 (2 blocks: B1 linear chain 11 nodes/10 plain edges; B2 failure-annotation map 13 nodes, 5 dashed F_REG/F_IME/F_APP1/F_SCRIPT/F_APP2 -> S3/S4/S7/S8/S9; 2 BQ) + Pattern 2 before/after in RESEARCH.md lines 226-247
    - 122-CONTEXT.md D-02 Amendment 1 (per block) + Amendment 2a (failure map -> Stage|Failure table)
  </read_first>
  <action>
    `git show 71be4ab:docs/lifecycle-apv2/02-deployment-flow.md` to capture BOTH ```mermaid blocks (Amendment 1). BLOCK 1 (linear chain, no diamond) -> ordered numbered stage list (LOCKED-N B1 = 11 nodes + 0 labeled). BLOCK 2 (failure-annotation map) -> a `Stage | Failure Mode` 2-column table containing ALL 5 dashed edges (F_REG->S3, F_IME->S4, F_APP1->S7, F_SCRIPT->S8, F_APP2->S9) — this is the exact block a naive triage missed; do NOT drop it. Delete BOTH fences + stale prose. Annotate each block's LOCKED-N (re-verify). Split both existing >200c blockquotes. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/lifecycle-apv2/02-deployment-flow.md` (doc_id RE-200, Guide). Author `## Summary` (>=30 words — APv2 user-driven deployment flow).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/lifecycle-apv2/02-deployment-flow.md; echo "c17=$?"; grep -c '^```mermaid' docs/lifecycle-apv2/02-deployment-flow.md; grep -c 'Failure Mode' docs/lifecycle-apv2/02-deployment-flow.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/lifecycle-apv2/02-deployment-flow.md` = 0 (BOTH blocks removed)
    - C17 exits 0; `grep -c '^doc_id: RE-200'` = 1; doc_type Guide
    - Block 2 is a `Stage | Failure Mode` table with all 5 F_* rows present; both blocks carry LOCKED-N; stale-prose grep clean
  </acceptance_criteria>
  <done>lifecycle-apv2/02-deployment-flow.md's 2 blocks converted per-block (incl. the 5-edge failure map), mermaid-free, C17-green, RE-200 enrolled.</done>
</task>

<task type="auto">
  <name>Task 2: Convert + enroll ios-lifecycle/01-ade-lifecycle.md (RE-190, Guide, LOCKED — 7)</name>
  <read_first>
    - docs/ios-lifecycle/01-ade-lifecycle.md
    - 122-RESEARCH.md Class 3 row for ios-lifecycle/01 (linear chain graph TD, 7 nodes / 6 plain edges / 0 diamonds; 2 BQ)
    - 122-PATTERNS.md §3 (EEE Guide envelope); D-02 (no diamond -> numbered stage list)
  </read_first>
  <action>
    `git show 71be4ab:docs/ios-lifecycle/01-ade-lifecycle.md` to capture the element set (LOCKED-N 7 = 7 nodes + 0 labeled edges). NO diamond -> ordered numbered stage list. Delete fence + stale prose. Annotate `LOCKED — 7 (nodes + labeled edges)` (R1, re-verify). Split both existing >200c blockquote groups. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/ios-lifecycle/01-ade-lifecycle.md` (doc_id RE-190, Guide). Author `## Summary` (>=30 words — iOS/iPadOS ADE lifecycle end-to-end).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/ios-lifecycle/01-ade-lifecycle.md; echo "c17=$?"; grep -c '^```mermaid' docs/ios-lifecycle/01-ade-lifecycle.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/ios-lifecycle/01-ade-lifecycle.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 7'` >= 1; `grep -c '^doc_id: RE-190'` = 1; doc_type Guide
    - Both blockquotes ≤200c; stale-prose grep clean
  </acceptance_criteria>
  <done>ios-lifecycle/01-ade-lifecycle.md is mermaid-free, C17-green, RE-190 enrolled.</done>
</task>

<task type="auto">
  <name>Task 3: Convert + enroll ios-lifecycle/02-mdm-migration.md (RE-191, Guide, LOCKED — 7, 10 blockquote groups)</name>
  <read_first>
    - docs/ios-lifecycle/02-mdm-migration.md
    - 122-RESEARCH.md Class 3 row for ios-lifecycle/02 (linear chain graph TD, 7 nodes / 6 plain edges / 0 diamonds; 10 existing >200c BQ groups incl. one 656-char block — heaviest blockquote COUNT in the corpus) + Pitfall 5
    - STATE.md [Phase 121-03] (reusable greedy blockquote packer)
  </read_first>
  <action>
    `git show 71be4ab:docs/ios-lifecycle/02-mdm-migration.md` to capture the element set (LOCKED-N 7 = 7 nodes + 0 labeled edges — simple linear diagram). NO diamond -> ordered numbered stage list. Delete fence + stale prose. Annotate `LOCKED — 7 (nodes + labeled edges)` (R1, re-verify). The real workload here is BLOCKQUOTE remediation: split ALL 10 existing >200c blockquote groups (incl. the 656c block) using the word-preserving Transform A/B discipline + C17's join-then-measure grouping; reuse the Phase-121 greedy packer; word-set multiset diff (strip `>` prefix) to prove zero real-word loss. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/ios-lifecycle/02-mdm-migration.md` (doc_id RE-191, Guide). Author `## Summary` (>=30 words — iOS MDM migration lifecycle).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/ios-lifecycle/02-mdm-migration.md; echo "c17=$?"; grep -c '^```mermaid' docs/ios-lifecycle/02-mdm-migration.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/ios-lifecycle/02-mdm-migration.md` = 0
    - C17 exits 0 (proves all 10 blockquote groups now ≤200c); `grep -c 'LOCKED — 7'` >= 1; `grep -c '^doc_id: RE-191'` = 1; doc_type Guide
    - Word-set diff shows zero real-word loss across the 10 splits; stale-prose grep clean
  </acceptance_criteria>
  <done>ios-lifecycle/02-mdm-migration.md is mermaid-free, C17-green (10 blockquotes remediated), RE-191 enrolled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-conversion -> converted list/table | lifecycle-apv2/02's second block (5 failure edges) is the CONTEXT-cited miss |
| blockquote remediation -> file | ios/02's 10 groups are the corpus's heaviest split count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | dropping lifecycle-apv2/02 block 2 or one of its 5 F_* dashed edges | mitigate | per-block classification; all-5-rows Stage|Failure table; D-01 re-derivation in 122-14 |
| T-122-08 | Tampering | word-loss across ios/02's 10 blockquote splits | mitigate | reuse Phase-121 greedy packer; word-set multiset diff proof |
| T-122-05 | Repudiation | stale diagram-prose left behind | mitigate | delete in-task + stale-prose grep gate |
| T-122-SC | Tampering | package installs | accept | none — content edits + node built-in fork only |
</threat_model>

<verification>
- C17 exits 0 on all 3 files; 0 ```mermaid fences (incl. apv2/02's 2 blocks); LOCKED-N present; doc_id RE-200/190/191 (Guide) injected; 5-edge failure map + ios/02's 10 blockquotes remediated; stale-prose grep clean
</verification>

<success_criteria>
The 3 files are mermaid-free, C17-green, enrolled; the failure-annotation map (5 edges) and ios/02's blockquote burden fully remediated for D-01 verification.
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-10-SUMMARY.md` when done.
</output>
