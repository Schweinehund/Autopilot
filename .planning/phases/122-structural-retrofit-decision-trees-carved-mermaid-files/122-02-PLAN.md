---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 02
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - docs/decision-trees/08-android-triage.md
  - docs/decision-trees/00-initial-triage.md
  - docs/decision-trees/01-esp-failure.md
autonomous: true
requirements: [RETRO-05]

must_haves:
  truths:
    - "Each of the 3 decision-trees has its ```mermaid fence(s) and all stale diagram-prose removed"
    - "Each diagram is a text-equivalent decision table preserving every node + labeled edge, including all reconvergence/merge edges"
    - "Each file carries the EEE header block + doc_id + ## Summary-first and C17 exits 0"
  artifacts:
    - path: "docs/decision-trees/08-android-triage.md"
      provides: "Decision table (LOCKED — 38) with both 4-way merges preserved as explicit rows"
      contains: "LOCKED — 38"
    - path: "docs/decision-trees/00-initial-triage.md"
      provides: "Decision table (LOCKED — 36) with the 4-way TRD4 reconvergence preserved"
      contains: "LOCKED — 36"
    - path: "docs/decision-trees/01-esp-failure.md"
      provides: "Decision table (LOCKED — 43)"
      contains: "LOCKED — 43"
  key_links:
    - from: "docs/decision-trees/08-android-triage.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-215 injected by fork"
      pattern: "doc_id:\\s*RE-215"
---

<objective>
Convert the 3 highest-edge-count decision-trees (front-loaded per CONTEXT) from Mermaid to C17-compliant decision tables and enroll them via the fork. These carry the corpus's heaviest reconvergence (08's two 4-way merges) — the exact silent-loss hazard Phase 122 exists to fence.

Purpose: RETRO-05 — decision-trees retrofitted, every decision leaf/node/labeled edge preserved.
Output: 3 enrolled, C17-green decision-tree files (RE-215, RE-207, RE-208).

Honor the Plan-01 LOCKED rulings: R1 (`LOCKED — N (nodes + labeled edges)`), decision-trees -> Reference. Front-load 08 (heaviest reconvergence in the 30-file corpus).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
@docs/_standards/EEE-SOP-standard.md
@docs/decision-trees/10-8021x-triage.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert + enroll 08-android-triage.md (RE-215, LOCKED — 38, two 4-way merges)</name>
  <read_first>
    - docs/decision-trees/08-android-triage.md (the file to convert)
    - 122-RESEARCH.md Class 1 row for 08 (14 nodes / 24 labeled edges / 5 diamonds; two 4-way merges into ANDR25 and ANDE3 — "heaviest in corpus")
    - 122-PATTERNS.md §2 (ordinal-column decision-table shape from 10-8021x-triage; the stale-prose anti-pattern grep list)
    - docs/decision-trees/10-8021x-triage.md (ordinal-column exemplar shape — Path | Step 1 | Step 2 | Destination)
    - 122-CONTEXT.md D-01 riders (semantic-paraphrase, multi-block/subgraph enum, stale-prose grep) + D-02 bright-line
  </read_first>
  <action>
    Capture the pre-conversion element set from the base commit: `git show 71be4ab:docs/decision-trees/08-android-triage.md` — enumerate EVERY ```mermaid block, every node, every labeled edge (RESEARCH: 14 nodes + 24 labeled edges = LOCKED-N 38), and BOTH 4-way merges (into ANDR25 and ANDE3). This file has `{...}` decision nodes -> convert to a decision table (D-02 bright-line). Delete the ```mermaid fence and any Legend/`classDef`/node-shape/"click the leaf" stale prose. Author an ordinal-column decision table (10-8021x shape) with ONE ROW PER node/labeled edge so that BOTH 4-way reconvergence merges survive as explicit rows (a flat list would drop them — the D-04 loss this phase prevents). Add the annotation `LOCKED — 38 (nodes + labeled edges)` (RULING R1) — re-verify 38 against the git-show bytes, do not blindly copy. Split every >200c blockquote group (RESEARCH: 1 group) using word-preserving Transform A/B per C17's join-then-measure grouping. Then enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/decision-trees/08-android-triage.md` (injects doc_id RE-215, EEE block, VH row w/ real date, platform). Author `## Summary` (>=30 words, scope statement — Android enrollment/compliance triage) as first H2 replacing the fork [FILL-IN].
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/decision-trees/08-android-triage.md; echo "c17=$?"; grep -c '^```mermaid' docs/decision-trees/08-android-triage.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/decision-trees/08-android-triage.md` = 0
    - C17 exits 0 on the file
    - `grep -c 'LOCKED — 38' docs/decision-trees/08-android-triage.md` >= 1
    - `grep -c '^doc_id: RE-215' docs/decision-trees/08-android-triage.md` = 1
    - Stale-prose grep clean: `grep -Ei 'click the leaf|node shape|classDef|diagram (above|below)|^## Legend' docs/decision-trees/08-android-triage.md` returns nothing
    - Both 4-way merges (ANDR25, ANDE3 targets) are represented as explicit table rows
  </acceptance_criteria>
  <done>08-android-triage.md is a mermaid-free, C17-green decision table with both reconvergence merges preserved and RE-215 enrolled.</done>
</task>

<task type="auto">
  <name>Task 2: Convert + enroll 00-initial-triage.md (RE-207, LOCKED — 36, 4-way TRD4 merge)</name>
  <read_first>
    - docs/decision-trees/00-initial-triage.md
    - 122-RESEARCH.md Class 1 row for 00 (18 nodes / 18 labeled edges / 6 diamonds; 4-way reconvergence into TRD4)
    - 122-PATTERNS.md §2 (decision-table shape) + §3 (EEE Reference envelope)
    - docs/decision-trees/10-8021x-triage.md (exemplar)
  </read_first>
  <action>
    `git show 71be4ab:docs/decision-trees/00-initial-triage.md` to capture the element set (LOCKED-N 36 = 18 nodes + 18 labeled edges; 4-way merge into TRD4). Delete the fence + stale prose. This file has `{...}` nodes -> decision table (ordinal column). Preserve the 4-way TRD4 reconvergence as explicit rows. Annotate `LOCKED — 36 (nodes + labeled edges)` (R1, re-verify). Split the 1 >200c blockquote group. 00 is one of the 5 platform-keyless Windows decision-trees — the fork injects `platform: Windows` via the allowlist (do NOT hand-add a platform key). Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/decision-trees/00-initial-triage.md` (doc_id RE-207). Author `## Summary` (>=30 words — top-level triage entry point, routes to scenario trees).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/decision-trees/00-initial-triage.md; echo "c17=$?"; grep -c '^```mermaid' docs/decision-trees/00-initial-triage.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/decision-trees/00-initial-triage.md` = 0
    - C17 exits 0 on the file
    - `grep -c 'LOCKED — 36' docs/decision-trees/00-initial-triage.md` >= 1
    - `grep -c '^doc_id: RE-207' docs/decision-trees/00-initial-triage.md` = 1 and `grep -c '^platform: Windows' docs/decision-trees/00-initial-triage.md` = 1 (fork-injected via allowlist)
    - Stale-prose grep clean; 4-way TRD4 merge represented as explicit rows
  </acceptance_criteria>
  <done>00-initial-triage.md is mermaid-free, C17-green, RE-207 enrolled with fork-injected Windows platform, TRD4 reconvergence preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Convert + enroll 01-esp-failure.md (RE-208, LOCKED — 43, 8 diamonds)</name>
  <read_first>
    - docs/decision-trees/01-esp-failure.md
    - 122-RESEARCH.md Class 1 row for 01 (24 nodes / 19 labeled edges / 8 diamonds; no reconvergence; 0 existing BQ groups)
    - 122-PATTERNS.md §2 + §3
    - docs/decision-trees/10-8021x-triage.md (exemplar)
  </read_first>
  <action>
    `git show 71be4ab:docs/decision-trees/01-esp-failure.md` to capture the element set (LOCKED-N 43 = 24 nodes + 19 labeled edges; 8 diamonds — the most-diamonds file). Delete the fence + stale prose. `{...}` present -> decision table (ordinal column), one row per node/labeled edge across all 8 decision points. Annotate `LOCKED — 43 (nodes + labeled edges)` (R1, re-verify). 01 is platform-keyless Windows (fork injects platform via allowlist). No existing >200c blockquotes, but re-scan after authoring the table in case new blockquote content was introduced. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/decision-trees/01-esp-failure.md` (doc_id RE-208). Author `## Summary` (>=30 words — ESP failure triage).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/decision-trees/01-esp-failure.md; echo "c17=$?"; grep -c '^```mermaid' docs/decision-trees/01-esp-failure.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/decision-trees/01-esp-failure.md` = 0
    - C17 exits 0 on the file
    - `grep -c 'LOCKED — 43' docs/decision-trees/01-esp-failure.md` >= 1
    - `grep -c '^doc_id: RE-208' docs/decision-trees/01-esp-failure.md` = 1
    - All 8 decision points represented; stale-prose grep clean
  </acceptance_criteria>
  <done>01-esp-failure.md is mermaid-free, C17-green, RE-208 enrolled, all 8 decision points preserved as table rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-conversion -> converted table | a dropped node/labeled edge/merge is silent content loss (C17 has no diagram parser) |
| fork -> enrolled file | enrollment of a still-mermaid file would trip C17 #1 later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | hand-conversion silently dropping a reconvergence/merge edge (08's two 4-way merges, 00's TRD4) | mitigate | one-row-per-node/labeled-edge table shape; LOCKED-N annotation; independent D-01 re-derivation in plan 122-12 |
| T-122-05 | Repudiation | stale diagram-prose (Legend/"click the leaf") left behind, green under C17 | mitigate | delete stale prose in-task + stale-prose grep in acceptance criteria and D-01 verification |
| T-122-02 | Tampering | fork enrolling before the fence is removed | mitigate | fork's MERMAID-STILL-PRESENT precondition (from 122-01) fail-closes |
| T-122-SC | Tampering | package installs | accept | none — content edits + node built-in fork only |
</threat_model>

<verification>
- C17 exits 0 on all 3 files
- 0 ```mermaid fences remain; LOCKED-N annotations present (38/36/43)
- doc_id RE-215/207/208 injected; stale-prose grep clean on all 3
</verification>

<success_criteria>
The 3 heaviest-reconvergence decision-trees are mermaid-free, C17-green, enrolled (RE-207/208/215), with every node + labeled edge (incl. all three 4-way merges) preserved for D-01 verification.
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-02-SUMMARY.md` when done.
</output>
