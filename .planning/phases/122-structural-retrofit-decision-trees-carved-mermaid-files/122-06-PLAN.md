---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 06
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - docs/admin-setup-android/00-overview.md
  - docs/admin-setup-ios/00-overview.md
  - docs/admin-setup-macos/00-overview.md
autonomous: true
requirements: [RETRO-08]

must_haves:
  truths:
    - "Each admin-setup overview is mermaid-free with its diagram converted per the D-02 bright-line, every node + labeled edge preserved"
    - "macos/00's C/D/E->F reconvergence and the G->H->I->J->K chain are preserved in the converted list"
    - "Each carries the EEE header block + doc_id (doc_type Guide) + ## Summary-first; C17 exits 0"
  artifacts:
    - path: "docs/admin-setup-android/00-overview.md"
      provides: "Decision table for the CHOOSE diamond (6 outcomes), LOCKED — 16"
      contains: "LOCKED — 16"
    - path: "docs/admin-setup-ios/00-overview.md"
      provides: "Decision table for the CHOOSE diamond (4 outcomes), LOCKED — 18"
      contains: "LOCKED — 18"
    - path: "docs/admin-setup-macos/00-overview.md"
      provides: "Numbered stage list with reconvergence preserved, LOCKED — 11"
      contains: "LOCKED — 11"
  key_links:
    - from: "docs/admin-setup-android/00-overview.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-092 (Guide) injected by fork"
      pattern: "doc_id:\\s*RE-092"
---

<objective>
Convert the 3 FULL-tier admin-setup overview carve-outs (android/00, ios/00, macos/00) and enroll them Pending->ready-to-flip. android/00 and ios/00 carry `CHOOSE` decision diamonds (-> decision tables); macos/00 has no diamond but a C/D/E->F reconvergence (-> numbered stage list with the merge preserved; FULL tier per Plan-01 RULING R3).

Purpose: RETRO-08 — the 9 admin-setup carved-mermaid files retrofitted, Mermaid resolved per STD-04.
Output: 3 enrolled, C17-green files (RE-092, RE-106, RE-116; doc_type Guide per RULING R2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
@docs/_standards/EEE-SOP-standard.md
@docs/decision-trees/10-8021x-triage.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert + enroll admin-setup-android/00-overview.md (RE-092, Guide, LOCKED — 16)</name>
  <read_first>
    - docs/admin-setup-android/00-overview.md
    - 122-RESEARCH.md Class 2 row for admin-setup-android/00 (flowchart TD, 8 nodes / 10 edges (8 labeled) / 1 diamond CHOOSE with 6 outcomes; 1 BQ)
    - 122-PATTERNS.md §2 (decision-table shape) + §3; docs/decision-trees/10-8021x-triage.md
  </read_first>
  <action>
    `git show 71be4ab:docs/admin-setup-android/00-overview.md` to capture the element set (LOCKED-N 16 = 8 nodes + 8 labeled edges; CHOOSE diamond, 6 outcomes). `{...}` present -> ordinal-column decision table with all 6 CHOOSE outcomes as rows. Delete fence + stale prose. Annotate `LOCKED — 16 (nodes + labeled edges)` (R1, re-verify). Split the 1 existing >200c blockquote group. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/admin-setup-android/00-overview.md` (doc_id RE-092, doc_type Guide via ADMIN_SETUP_CARVEOUT_PATHS). Author `## Summary` (>=30 words — Android admin-setup enrollment-method chooser).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/admin-setup-android/00-overview.md; echo "c17=$?"; grep -c '^```mermaid' docs/admin-setup-android/00-overview.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/admin-setup-android/00-overview.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 16'` >= 1; `grep -c '^doc_id: RE-092'` = 1; `grep -c '^doc_type: Guide' docs/admin-setup-android/00-overview.md` = 1
    - All 6 CHOOSE outcomes represented as rows; stale-prose grep clean
  </acceptance_criteria>
  <done>admin-setup-android/00-overview.md is mermaid-free, C17-green, RE-092 (Guide) enrolled, all 6 outcomes preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Convert + enroll admin-setup-ios/00-overview.md (RE-106, Guide, LOCKED — 18)</name>
  <read_first>
    - docs/admin-setup-ios/00-overview.md
    - 122-RESEARCH.md Class 2 row for admin-setup-ios/00 (flowchart TD, 10 nodes / 9 edges (8 labeled) / 1 diamond CHOOSE with 4 outcomes; 1 BQ)
    - 122-PATTERNS.md §2 + §3; docs/decision-trees/10-8021x-triage.md
  </read_first>
  <action>
    `git show 71be4ab:docs/admin-setup-ios/00-overview.md` to capture the element set (LOCKED-N 18 = 10 nodes + 8 labeled edges; CHOOSE diamond, 4 outcomes). `{...}` present -> ordinal-column decision table with all 4 CHOOSE outcomes as rows. Delete fence + stale prose. Annotate `LOCKED — 18 (nodes + labeled edges)` (R1, re-verify). Split the 1 existing >200c blockquote group. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/admin-setup-ios/00-overview.md` (doc_id RE-106, Guide). Author `## Summary` (>=30 words — iOS/iPadOS admin-setup enrollment-method chooser).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/admin-setup-ios/00-overview.md; echo "c17=$?"; grep -c '^```mermaid' docs/admin-setup-ios/00-overview.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/admin-setup-ios/00-overview.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 18'` >= 1; `grep -c '^doc_id: RE-106'` = 1; doc_type Guide
    - All 4 CHOOSE outcomes represented; stale-prose grep clean
  </acceptance_criteria>
  <done>admin-setup-ios/00-overview.md is mermaid-free, C17-green, RE-106 (Guide) enrolled.</done>
</task>

<task type="auto">
  <name>Task 3: Convert + enroll admin-setup-macos/00-overview.md (RE-116, Guide, LOCKED — 11, reconvergence)</name>
  <read_first>
    - docs/admin-setup-macos/00-overview.md
    - 122-RESEARCH.md Class 2 row for admin-setup-macos/00 (graph LR, 11 nodes / 12 plain edges / 0 diamonds; reconvergence C,D,E->F then chain G->H->I->J->K; 1 BQ) + Open Question 2
    - 122-CONTEXT.md D-02 (no diamond -> numbered stage list) + 122-01 RULING R3 (FULL tier)
    - 122-PATTERNS.md §3 (EEE Guide envelope)
  </read_first>
  <action>
    `git show 71be4ab:docs/admin-setup-macos/00-overview.md` to capture the element set (LOCKED-N 11 = 11 nodes + 0 labeled edges; 12 plain edges; the C/D/E->F fan-in reconvergence + the G->H->I->J->K chain). NO `{...}` diamond -> ordered numbered stage list (D-02 bright-line), NOT a table. BUT the reconvergence is load-bearing and a flat list drops it: add an explicit note capturing "Steps C, D, E all feed into F" and preserve the G->H->I->J->K ordering. Delete fence + stale prose. Annotate `LOCKED — 11 (nodes + labeled edges)` (R1, re-verify — note all edges are plain so N counts nodes only, but the D-01 verifier still checks all 12 plain edges incl. the fan-in). Split the 1 existing >200c blockquote group. Enroll: `node scripts/pipeline/retrofit-mermaid-structural.mjs docs/admin-setup-macos/00-overview.md` (doc_id RE-116, Guide). Author `## Summary` (>=30 words — macOS admin-setup guide roadmap).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs docs/admin-setup-macos/00-overview.md; echo "c17=$?"; grep -c '^```mermaid' docs/admin-setup-macos/00-overview.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^```mermaid' docs/admin-setup-macos/00-overview.md` = 0
    - C17 exits 0; `grep -c 'LOCKED — 11'` >= 1; `grep -c '^doc_id: RE-116'` = 1; doc_type Guide
    - The C/D/E->F reconvergence is explicitly preserved (a merge note or equivalent); stale-prose grep clean
  </acceptance_criteria>
  <done>admin-setup-macos/00-overview.md is mermaid-free, C17-green, RE-116 (Guide) enrolled, reconvergence preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-conversion -> converted list/table | macos/00's fan-in reconvergence is the easiest edge to silently drop into a flat list |
| fork router -> doc_type | admin-setup carve-outs must emit Guide, not be reprocessed as already-enrolled siblings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | dropping macos/00's C/D/E->F merge or a CHOOSE outcome | mitigate | explicit merge note + all-outcomes rows; LOCKED-N; D-01 re-derivation in 122-13 |
| T-122-03 | Tampering | fork reprocessing an already-enrolled admin-setup sibling | mitigate | ADMIN_SETUP_CARVEOUT_PATHS explicit Set (9 entries) + doc_id-idempotency guard |
| T-122-05 | Repudiation | stale diagram-prose left behind | mitigate | delete in-task + stale-prose grep gate |
| T-122-SC | Tampering | package installs | accept | none — content edits + node built-in fork only |
</threat_model>

<verification>
- C17 exits 0 on all 3 files; 0 ```mermaid fences; LOCKED-N 16/18/11 present; doc_id RE-092/106/116 (all Guide) injected; macos/00 reconvergence preserved; stale-prose grep clean
</verification>

<success_criteria>
The 3 FULL admin-setup overview carve-outs are mermaid-free, C17-green, enrolled as Guide; CHOOSE outcomes and the macos/00 reconvergence preserved for D-01 verification.
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-06-SUMMARY.md` when done.
</output>
