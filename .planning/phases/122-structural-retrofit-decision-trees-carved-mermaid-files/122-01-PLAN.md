---
phase: 122-structural-retrofit-decision-trees-carved-mermaid-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pipeline/retrofit-mermaid-structural.mjs
  - docs/_registry/RE-index.md
autonomous: true
requirements: [RETRO-05, RETRO-07, RETRO-08]

must_haves:
  truths:
    - "The Phase-122 fork enrolls a Mermaid-free file but fail-closes (ERROR) on any file that still contains a ```mermaid fence"
    - "The fork ERRORs rather than silently skips when a doc_id is already present, or when a keyless file is outside the confirmed-Windows allowlist"
    - "The fork routes docs/decision-trees/* -> Reference and the 9 admin-setup carve-outs -> Guide via explicit path Sets (not directory prefixes)"
    - "RE-207..RE-217 rows exist (path-keyed, path order 00->10, doc_type Reference) in RE-index.md before buildDocIdMap() runs"
    - "--self-test exits 0 and asserts the NEW mermaid-absence + doc_id-idempotency guards"
  artifacts:
    - path: "scripts/pipeline/retrofit-mermaid-structural.mjs"
      provides: "Phase-122 fork of retrofit-structural.mjs with mermaid-absence + idempotency + keyless-Windows guards, explicit-Set router, auto-filled VH date"
      contains: "MERMAID-STILL-PRESENT"
    - path: "docs/_registry/RE-index.md"
      provides: "11 minted decision-tree rows RE-207..RE-217 (Pending, Reference) + RE-212 rationale note"
      contains: "RE-217"
  key_links:
    - from: "scripts/pipeline/retrofit-mermaid-structural.mjs"
      to: "docs/_registry/RE-index.md"
      via: "buildDocIdMap() join-on-Path"
      pattern: "buildDocIdMap"
---

<objective>
Build the Phase-122 fork of `retrofit-structural.mjs` (the EEE-envelope injector) and mint the 11 net-new decision-tree registry rows, so every downstream conversion plan can enroll its files fail-closed. This plan produces NO document conversions — it produces the tooling + registry prep the 10 conversion plans depend on.

Purpose: The fork is the single shared dependency for enrolling all 30 Mermaid-bearing files (D-03). It must fail CLOSED on unconverted diagrams, double-enrollment, and unknown keyless platforms — the highest-severity Phase-122 hazard is a file with an unconverted diagram enrolling silently (MERMAID-ENROLL-UNGUARDED).
Output: `scripts/pipeline/retrofit-mermaid-structural.mjs` (new fork) + 11 minted `RE-index.md` rows.

## Plan-time rulings LOCKED for all downstream conversion/verification plans

These resolve the two RESEARCH Open Questions and the D-04 router RIDER. Every conversion plan cites these:
- **RULING R1 (LOCKED-N convention, Open Q1):** ALL 30 files use the D-02 Amendment 3 convention `LOCKED — N (nodes + labeled edges)` — count nodes PLUS labeled edges, NOT terminal leaves. This upgrades `10-8021x-triage.md`'s older leaf-count annotation to the new convention (N=11 for file 10, per RESEARCH). No convention-mixing across the 30 files.
- **RULING R2 (admin-setup doc_type, D-04 RIDER):** the fork router emits `doc_type: Guide` for the 9 admin-setup carve-outs (matches existing registry rows, verified live). Decision-trees -> `Reference`; `ca-enrollment-timing.md` -> `Reference`.
- **RULING R3 (admin-setup-macos/00 tier, Open Q2):** FULL verification tier (reconvergence C/D/E->F trips FULL regardless of no-diamond). Max-effort.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-CONTEXT.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-RESEARCH.md
@.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-PATTERNS.md
@scripts/pipeline/retrofit-structural.mjs
@scripts/validation/c17-eee-contract.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fork retrofit-structural.mjs into retrofit-mermaid-structural.mjs with the 4 guard/router changes</name>
  <read_first>
    - scripts/pipeline/retrofit-structural.mjs (766 lines — the fork template; DO NOT edit in place)
    - scripts/validation/c17-eee-contract.mjs (copy its code-fence masking / `^```mermaid` detection method for the new guard)
    - 122-RESEARCH.md "The retrofit-structural.mjs Fork Surface — Exact Line Ranges" + Pitfalls 1-3
    - 122-PATTERNS.md §1 (exact blocks to DELETE, the fail-closed guard idiom, VH-token auto-fill)
    - 122-CONTEXT.md D-03 (all overrules + adds)
  </read_first>
  <behavior>
    Self-test (rewritten sub-test e) and a dry-run must prove:
    - MERMAID-STILL-PRESENT: a synthetic body containing a ```mermaid fence is refused (ok:false)
    - DOC-ID-ALREADY-PRESENT: a synthetic frontmatter already carrying `doc_id:` is refused (ok:false, ERROR not skip)
    - Router: resolveDocType('docs/decision-trees/05-device-lifecycle.md') === 'Reference'; resolveDocType('docs/admin-setup-android/00-overview.md') === 'Guide'; resolveDocType('docs/reference/ca-enrollment-timing.md') === 'Reference'
    - UNKNOWN-KEYLESS-PLATFORM: a keyless synthetic path outside the 00-04 allowlist is refused
    - An already-enrolled admin-setup sibling path (e.g. docs/admin-setup-ios/02-abm-token.md) is NOT in any target Set (Pitfall 2 defense)
  </behavior>
  <action>
    Copy `retrofit-structural.mjs` to a NEW file `scripts/pipeline/retrofit-mermaid-structural.mjs` (fork-don't-refactor). Then apply, per the RESEARCH Fork-Surface line map:
    (1) DELETE `MERMAID_DEFERRED_PATHS` at ALL 4 sites — declaration (~:130-140), Guard 1b (~:310-314), the `--all` `.filter(...)` (~:675-681), and rewrite self-test sub-test (e) (~:554-577). Grep `MERMAID_DEFERRED` across the file first; 4 hits expected (Pitfall 1).
    (2) ADD a fail-closed mermaid-absence precondition right after `content` is read (before frontmatter parse): mask code fences using C17's own `inCodeFence` method, then if any unmasked body line matches `/^```mermaid/` -> `return {ok:false, rel, error:'MERMAID-STILL-PRESENT'}`. This is the highest-severity guard (MERMAID-ENROLL-UNGUARDED).
    (3) ADD a doc_id-idempotency guard immediately after the Guard 2 TEMPLATE-SENTINEL check, before Guard 3: `if (fm.match(/^doc_id:\s*\S+/m)) return {ok:false, rel, error:'DOC-ID-ALREADY-PRESENT: refusing to re-enroll'}` — ERROR, never silent skip (D-03 Specific Overrule 1).
    (4) EXTEND `resolveDocType()` with two NEW explicit `Set`s — `DECISION_TREE_PATHS` (11 exact paths docs/decision-trees/00..10 -> 'Reference') and `ADMIN_SETUP_CARVEOUT_PATHS` (the 9 exact admin-setup carve-out paths -> 'Guide', per RULING R2) — plus a single-path check `'docs/reference/ca-enrollment-timing.md' -> 'Reference'`. Do NOT add directory prefixes (Pitfall 2). Keep GUIDE_DIRS and GLOSSARY_FILES unchanged.
    (5) ADD a keyless-non-Windows guard: extend the platform-injection block (~:349) — `const KNOWN_WINDOWS_KEYLESS_PATHS = new Set([decision-trees 00,01,02,03,04])`; if `platformInjected && !KNOWN_WINDOWS_KEYLESS_PATHS.has(rel)` -> `return {ok:false, rel, error:'UNKNOWN-KEYLESS-PLATFORM'}`.
    (6) AUTO-FILL the VH date: replace the literal `'YYYY-MM-DD'` in NEW_ROW_2COL/NEW_ROW_3COL (~:231-232) with the actual retrofit date via template interpolation (`new Date().toISOString().slice(0,10)`) — closes DEFER-121-07-A root cause.
    (7) KEEP the LF-normalize read (~:155) and utf8 write (~:745) EXACTLY as-is — do NOT reintroduce CRLF handling (D-03 Overrule 2, WR-01 WONTFIX). Rewrite self-test sub-test (e) to assert the NEW guards per the behavior block.
    Do NOT overwrite `retrofit-structural.mjs`.
  </action>
  <verify>
    <automated>node scripts/pipeline/retrofit-mermaid-structural.mjs --self-test; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MERMAID_DEFERRED' scripts/pipeline/retrofit-mermaid-structural.mjs` returns 0 (all 4 sites removed)
    - `grep -c 'MERMAID-STILL-PRESENT\|DOC-ID-ALREADY-PRESENT\|UNKNOWN-KEYLESS-PLATFORM' scripts/pipeline/retrofit-mermaid-structural.mjs` returns 3+
    - `node scripts/pipeline/retrofit-mermaid-structural.mjs --self-test` exits 0 with a PASS count (NOT a ReferenceError stack trace)
    - `scripts/pipeline/retrofit-structural.mjs` is byte-unchanged (fork did not overwrite the template)
  </acceptance_criteria>
  <done>The fork exists, all 4 MERMAID_DEFERRED sites are gone, the 3 new fail-closed guards + explicit-Set router + auto-date are present, and --self-test exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Mint RE-207..RE-217 decision-tree registry rows + RE-212 rationale note</name>
  <read_first>
    - docs/_registry/RE-index.md (append after current max RE-206; table format `| Doc ID | Path | Title | Doc Type | Status |`)
    - 122-PATTERNS.md §4 (net-new mint rows block; the linux-lifecycle RE-202/203 Approved precedent)
    - 122-RESEARCH.md Class 1 table (exact paths + titles per decision-tree file, path order 00->10)
    - 122-CONTEXT.md D-04 (RE-212 = 05-device-lifecycle, record directory-precedence rationale)
  </read_first>
  <action>
    Append 11 rows to `docs/_registry/RE-index.md` after RE-206, in path order 00->10: RE-207=docs/decision-trees/00-initial-triage.md ... RE-212=docs/decision-trees/05-device-lifecycle.md ... RE-217=docs/decision-trees/10-8021x-triage.md. Each row: `Doc Type = Reference`, `Status = Pending` (they flip to Approved in the close plan 122-15 once the fork has run and C17 is green). Title column = the file's H1 title. On RE-212's row (or an adjacent review note), record the D-04(a) rationale: directory precedence (`decision-trees/* -> Reference`, EEE-SOP-standard.md:178-179,186-187) overrides the `-lifecycle` filename — so a future reviewer does not re-litigate it. Verify contiguity: RE-001..217 zero-gap after the append.
  </action>
  <verify>
    <automated>grep -c '^| RE-21[0-7] ' docs/_registry/RE-index.md</automated>
  </verify>
  <acceptance_criteria>
    - Rows RE-207 through RE-217 all present, path-keyed to docs/decision-trees/00..10 in path order (`grep -E '^\| RE-21[0-7] \| docs/decision-trees/' docs/_registry/RE-index.md` returns 8, plus RE-207/208/209 = 11 total)
    - RE-212 row Path = docs/decision-trees/05-device-lifecycle.md, Doc Type = Reference
    - All 11 rows carry Doc Type = Reference and Status = Pending
    - The RE-212 directory-precedence rationale is recorded in the registry
  </acceptance_criteria>
  <done>11 path-keyed Reference/Pending rows RE-207..217 exist so buildDocIdMap() can join decision-tree paths; RE-212 rationale recorded.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end smoke test the fork against a real decision-tree (dry-run join)</name>
  <read_first>
    - scripts/pipeline/retrofit-mermaid-structural.mjs (the fork from Task 1)
    - docs/_registry/RE-index.md (the rows from Task 2)
    - docs/decision-trees/05-device-lifecycle.md (has platform: Windows; RE-212)
  </read_first>
  <action>
    Run the fork in dry-run against `docs/decision-trees/05-device-lifecycle.md` (which still contains its ```mermaid fence at this point). Confirm the mermaid-absence guard fires: the fork returns `MERMAID-STILL-PRESENT` and does NOT enroll — proving the fail-closed precondition works against a genuinely-unconverted file. Then confirm the join layer resolves: temporarily verify (via a dry-run against a synthetic mermaid-free fixture keyed to a real RE row, or by reading the fork's dry-run join output) that `buildDocIdMap()` maps docs/decision-trees/05-device-lifecycle.md -> RE-212 and resolveDocType -> Reference. Record the observed guard behavior in the SUMMARY. Do NOT modify any doc file in this plan.
  </action>
  <verify>
    <automated>node scripts/pipeline/retrofit-mermaid-structural.mjs --dry-run docs/decision-trees/05-device-lifecycle.md 2>&1 | grep -c 'MERMAID-STILL-PRESENT'</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run against the still-mermaid-bearing 05-device-lifecycle.md reports MERMAID-STILL-PRESENT (fail-closed, no enrollment)
    - The fork resolves docs/decision-trees/05-device-lifecycle.md to RE-212 / doc_type Reference in its join layer
    - No doc file is modified by this plan (`git status docs/` shows only RE-index.md changed)
  </acceptance_criteria>
  <done>The fork is proven to fail-closed on an unconverted file and to resolve the newly-minted RE-212 row — ready for conversion plans to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-conversion -> converted doc | untrusted diagram-loss crosses here (fenced by D-01, later plans) |
| fork script -> enrolled file | a script defect could enroll an unconverted or already-enrolled file |
| registry mint -> buildDocIdMap() | a mis-keyed row silently mis-joins doc_id to path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-02 | Tampering | fork processFile() enrolling a file that still has a ```mermaid fence | mitigate | fail-closed `^```mermaid`-absence precondition (MERMAID-STILL-PRESENT), verified by --self-test + Task 3 dry-run |
| T-122-03 | Tampering | fork double-injecting doc_id into an already-Approved sibling | mitigate | doc_id-idempotency ERROR guard + explicit-Set router (not directory prefix) — Pitfalls 2 & 3 |
| T-122-04 | Information Disclosure | mis-minted RE-207..217 row (wrong path) causing a wrong doc_id join | mitigate | path-keyed rows verified contiguous; DOC-ID-UNRESOLVED fail-closed join for any miss |
| T-122-06 | Tampering | UNKNOWN-KEYLESS-PLATFORM defaulting a non-Windows file to Windows | mitigate | keyless allowlist limited to decision-trees 00-04 (verified genuinely Windows); ERROR otherwise |
| T-122-SC | Tampering | npm/pip/cargo installs | accept | zero external packages — fork uses node built-ins only (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node scripts/pipeline/retrofit-mermaid-structural.mjs --self-test` exits 0
- `grep -c 'MERMAID_DEFERRED' scripts/pipeline/retrofit-mermaid-structural.mjs` = 0
- RE-207..217 present, path order, Reference/Pending
- `scripts/pipeline/retrofit-structural.mjs` byte-unchanged
</verification>

<success_criteria>
The fork enables fail-closed enrollment of all 30 files (mermaid-absence, idempotency, keyless-Windows guards, explicit-Set router, auto-date VH); the 11 decision-tree registry rows exist path-keyed for buildDocIdMap(). No document is converted in this plan.
</success_criteria>

<output>
Create `.planning/phases/122-structural-retrofit-decision-trees-carved-mermaid-files/122-01-SUMMARY.md` when done.
</output>
