---
phase: 118-reference-doc-retrofit-table-remediation-26-docs
plan: 06
type: execute
wave: 6
depends_on: [118-01, 118-02, 118-03, 118-04, 118-05]
files_modified: []
autonomous: true
requirements: [RETRO-03]
must_haves:
  truths:
    - "The enrollment-completeness precheck (Phase-115 D-02, scoped to 34) confirms every reference-class file EXCEPT the 1 mermaid-deferred file carries all four EEE keys (doc_id, status, owner, doc_type)"
    - "The 1 mermaid-deferred file (RE-147 docs/reference/ca-enrollment-timing.md) remains keyless (no doc_id key) and its RE-index.md Status stays Pending (D-05)"
    - "C17 exits 0 with zero violations across the full reference enrolled class (all 34 files gated in one full-class scan) — SC4"
    - "RE-index.md shows exactly 34 reference-class rows Approved and RE-147 Pending"
    - "The full-class run reports zero assertion-#1 (mermaid) violations — proving ca-enrollment-timing.md stayed un-enrolled by staying keyless"
  artifacts:
    - path: ".planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-06-SUMMARY.md"
      provides: "Phase-gate proof: enrollment-completeness precheck + full-class C17 exit-0 evidence for the 34 enrolled reference files"
      contains: "C17"
  key_links:
    - from: "full-class C17 run"
      to: "the 34 enrolled reference docs"
      via: "enrollment scan opt-in by doc_id key presence (c17-eee-contract.mjs:519-533)"
      pattern: "exit 0"
---

<objective>
Final phase gate for Phase 118. Run the two-part per-phase success criteria (Phase-115 D-02) across the WHOLE
reference-doc class in one pass: (a) the enrollment-completeness precheck scoped to 34 — every reference-class file
EXCEPT the 1 mermaid-deferred file carries all four EEE keys, and that 1 stays keyless — THEN (b) C17 exits 0 on the 34
enrolled files. This plan writes NO corpus/registry content; it is the verification gate proving RETRO-03 / SC1-SC4
before the phase closes and hands off to Phase 119 (the full Phase-1 corpus close-gate).

Purpose: Individual batch plans (118-02..118-05) gate their own files; this gate proves the reference class is COMPLETE
and internally consistent (no enrolled file missed, the 1 mermaid file not accidentally enrolled, registry
Approved/Pending counts exact) before the Phase-119 audit fold — which requires C17 green across the FULL Phase-1 corpus
(all three retrofit classes) — depends on it (RETRO-03, D-05, Phase-115 D-02). This phase completing the reference class
at 34/34 (+ documented mermaid carve-out) is the last input to that close.
Output: A phase-gate SUMMARY recording the precheck result, the full-class C17 exit-0 evidence, and the 34/1 counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-CONTEXT.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-RESEARCH.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrollment-completeness precheck scoped to 34 (1 mermaid file stays keyless/Pending)</name>
  <files></files>
  <read_first>
    - scripts/validation/c17-eee-contract.mjs (enrollment scan lines 519-533: opt-in by doc_id key presence — a keyless file is NOT scanned)
    - .planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-CONTEXT.md (D-05: the single mermaid-deferred path docs/reference/ca-enrollment-timing.md, RE-147)
    - docs/_registry/RE-index.md (RE-142..RE-167, RE-168..RE-174, RE-177, RE-178 reference-class rows; the 34 must be Approved, RE-147 Pending)
  </read_first>
  <action>
    Enumerate the reference-class corpus: all .md under docs/reference/ (26 files) + docs/error-codes/ (7) + the 2 bare
    comparison docs docs/apv1-vs-apv2.md and docs/windows-vs-macos.md (35 total). Partition into the 34 enrolled and the
    1 mermaid-deferred (docs/reference/ca-enrollment-timing.md). Assert: (1) every one of the 34 enrolled files has all
    four EEE frontmatter keys — grep -q "^doc_id:", "^status:", "^owner:", "^doc_type:" each succeed (the precheck returns
    ZERO missing-key lines for the 34); (2) docs/reference/ca-enrollment-timing.md is KEYLESS — grep -q "^doc_id:" FAILS
    (never enrolled, D-05); (3) in docs/_registry/RE-index.md exactly 34 reference-class rows (RE-142..RE-167 minus
    RE-147 = 25, RE-168..RE-174 = 7, RE-177 + RE-178 = 2) show Status Approved and RE-147 shows Status Pending. This task
    changes NO files — read-only verification. If any assertion fails, STOP and report which file/row is inconsistent
    (a missing enrolled file or an accidentally-enrolled mermaid file is a phase-blocking defect).
  </action>
  <acceptance_criteria>
    - All 34 enrolled reference files carry doc_id + status + owner + doc_type (precheck returns zero missing-key lines).
    - docs/reference/ca-enrollment-timing.md has NO doc_id key (grep -L "^doc_id:" lists it).
    - RE-index.md: exactly 34 reference-class rows Approved (RE-142..167 skip RE-147, RE-168..174, RE-177/178) and RE-147 Pending.
    - `git status --porcelain docs/` shows no changes from this task (read-only).
  </acceptance_criteria>
  <verify>
    <automated>miss=0; for f in $(ls docs/reference/*.md docs/error-codes/*.md docs/apv1-vs-apv2.md docs/windows-vs-macos.md | grep -v '/ca-enrollment-timing\.md$'); do for k in '^doc_id:' '^status:' '^owner:' '^doc_type:'; do grep -q "$k" "$f" || { echo "MISSING $k in $f"; miss=1; }; done; done; grep -q '^doc_id:' docs/reference/ca-enrollment-timing.md &amp;&amp; { echo "ca-enrollment-timing.md unexpectedly enrolled"; miss=1; }; test $miss -eq 0</automated>
  </verify>
  <done>The 34 enrolled files all carry the four EEE keys; ca-enrollment-timing.md is keyless; registry shows 34 Approved / RE-147 Pending.</done>
</task>

<task type="auto">
  <name>Task 2: Full-class C17 exit 0 across all 34 enrolled reference docs</name>
  <files></files>
  <read_first>
    - scripts/validation/c17-eee-contract.mjs (the immutable gate; enrollment scan lines 519-533 — a full run auto-includes every doc_id-bearing docs/ file; #1 mermaid, #11 table-row, #12 blockquote assertions)
    - .planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-RESEARCH.md (all 13 C17 assertions the enrolled files must satisfy; the #12 measurement as belt-and-suspenders; error-codes/00-index 30-row table already #11-compliant)
    - each 118 batch SUMMARY (118-02..118-05 SUMMARY.md — confirm each batch already exited C17 0 for its files)
  </read_first>
  <action>
    Run `node scripts/validation/c17-eee-contract.mjs` (full corpus). Confirm exit 0 with zero violations — this gates
    the 34 newly-enrolled reference docs alongside the already-Approved Phase-113/114/116/117 files. As a
    belt-and-suspenders check, re-run the #12 blockquote measurement (c17-eee-contract.mjs:387-405 logic) across all 34
    enrolled reference files and confirm zero over-limit top-level groups class-wide (including the 00-index double-split
    from 118-05). Confirm the run reports NO assertion-#1 (mermaid) violation — proving ca-enrollment-timing.md stayed
    un-enrolled (a #1 failure would mean it was accidentally given EEE keys). Confirm #11 reports zero violations (the
    only >25-row table, error-codes/00-index's 30-row Quick-Lookup, is already compliant). This task changes NO files.
    Record the full-class exit-0 evidence, the 34/1 counts, the D-118-3 caveat (the 3 Guide-titled files keep doc_type:
    Reference — not an inconsistency), and the D-118-1 note (per-table prose authored on the 8 matrix/comparison files
    for chunk-survival, not gate-forced) in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with zero violations reported.
    - The #12 re-measurement over the 34 enrolled reference files reports zero over-limit top-level blockquote groups.
    - The C17 output reports zero assertion-#1 (mermaid) violations and zero assertion-#11 (table-row) violations.
    - `git status --porcelain docs/` shows no changes from this task (read-only).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs; test $? -eq 0 &amp;&amp; git diff --quiet docs/</automated>
  </verify>
  <done>C17 exits 0 across the full corpus with the 34 enrolled reference docs gated, zero #12 over-limit groups, zero mermaid violations, zero #11 violations, and no file changes; SC1-SC4 proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict → phase close / Phase-119 full-corpus close-gate | A false-green gate would let an incomplete/inconsistent reference class into the frozen-surface re-baseline |

## STRIDE Threat Register

This is a PURE DOCUMENTATION-REFORMAT verification gate — no untrusted input, network, or auth surface. The only surface
worth noting: the gate must not falsely certify an incomplete class — mitigated by three independent proofs (precheck +
full-class C17 exit-0 + exact registry counts).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-06 | Repudiation | phase-gate completeness claim | mitigate | Two-part precheck (34-key presence + 1-keyless) AND full-class C17 exit 0 AND exact 34/1 registry counts — three independent proofs; STOP-and-report on any inconsistency rather than close |
| T-118-05 | Tampering | accidental mermaid enrollment | mitigate | Assertion-#1 (mermaid) violation count == 0 in the full run proves ca-enrollment-timing.md gained no EEE keys; grep -L doc_id confirms it keyless |
| T-118-SC | Tampering | package installs | accept | node-builtins-only C17; zero installs; no supply-chain surface |
</threat_model>

<verification>
- Enrollment-completeness precheck: 34 enrolled files carry all four EEE keys; ca-enrollment-timing.md keyless.
- `node scripts/validation/c17-eee-contract.mjs` exits 0; zero violations class-wide; zero #12 over-limit; zero #1 mermaid; zero #11.
- RE-index.md: exactly 34 reference-class rows Approved, RE-147 Pending.
- No file changes from this plan (read-only gate).
</verification>

<success_criteria>
The full reference-doc class is complete and internally consistent: all 34 enrolled files carry the four EEE keys and
pass C17 (exit 0), ca-enrollment-timing.md remains keyless and Pending, and the registry shows exactly 34 Approved /
RE-147 Pending. RETRO-03 / Phase-118 SC1-SC4 proven; ready for phase close and Phase-119 handoff (last input to the
full Phase-1 corpus close-gate).
</success_criteria>

<output>
Create `.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-06-SUMMARY.md` when done.
</output>
