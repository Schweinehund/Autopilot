---
phase: 118-reference-doc-retrofit-table-remediation-26-docs
plan: 04
type: execute
wave: 4
depends_on: [118-01, 118-03]
files_modified:
  - docs/reference/compliance-timing.md
  - docs/reference/security-baseline-conflicts.md
  - docs/reference/licensing-matrix.md
  - docs/reference/network-infrastructure.md
  - docs/reference/win32-app-packaging.md
  - docs/reference/endpoints.md
  - docs/reference/registry-paths.md
  - docs/reference/powershell-ref.md
  - docs/reference/macos-commands.md
  - docs/reference/macos-log-paths.md
  - docs/_registry/RE-index.md
autonomous: true
requirements: [RETRO-03]
must_haves:
  truths:
    - "All 10 files carry the EEE header block (Platform+Doc Type first, doc_id from registry, doc_type: Reference, Status: Approved) with the D1 label matching frontmatter platform: RE-148/166/157/162/167 = Windows, RE-151 = All Platforms, RE-160/161 = macOS, RE-164/165 (keyless→inject) = Windows (SC1)"
    - "The 2 keyless files (powershell-ref RE-164, registry-paths RE-165) get platform: Windows injected or C17 #10 hard-fails on the absent key; the other 8 already carry a resolvable platform: key left as-is (D-118-4 rider)"
    - "All 10 files exercise the VH CREATE branch (fresh 3-column section — none has a pre-existing ## Version History) (D-118-4)"
    - "Each file has D3-A structure + the v1.15 EEE reformat Version-History row + Last Reviewed = last_verified verbatim; each ## Summary is reference-template-led ≥30 words (SC2, SC3)"
    - "C17 exits 0 on all 10 files with zero #12 over-limit blockquote groups (30 groups fixed word-preserving, incl. win32-app-packaging 9 groups and network-infrastructure 7 groups); RE-148/151/157/160/161/162/164/165/166/167 flipped Pending → Approved (SC4)"
  artifacts:
    - path: "docs/reference/powershell-ref.md"
      provides: "Retrofitted PowerShell reference RE-164 (keyless→Windows injected, CREATE VH branch)"
      contains: "doc_id: RE-164"
    - path: "docs/_registry/RE-index.md"
      provides: "RE-148/151/157/160/161/162/164/165/166/167 Status flipped Pending → Approved"
      contains: "RE-167"
  key_links:
    - from: "docs/reference/registry-paths.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-165 resolved by path join"
      pattern: "RE-165"
    - from: "block line **Platform:** field (powershell-ref, keyless→inject)"
      to: "D1_MAP[Windows]"
      via: "exact D1 label match after platform: Windows injection (C17 #9/#10)"
      pattern: "\\*\\*Platform:\\*\\* Windows"
---

<objective>
Retrofit the 10 security / infrastructure-detail / macOS-CLI / PowerShell reference docs — compliance-timing (RE-148),
security-baseline-conflicts (RE-166), licensing-matrix (RE-157), network-infrastructure (RE-162), win32-app-packaging
(RE-167), endpoints (RE-151), registry-paths (RE-165), powershell-ref (RE-164), macos-commands (RE-160), macos-log-paths
(RE-161) — to the EEE standard so C17 exits 0 on every file. Two files are keyless (powershell-ref, registry-paths) and
need platform: Windows injected; none has an existing Version History (all CREATE). No D-118-1 per-table prose here —
these carry ordinary lookup/reference tables (SKU lists, endpoint lists, command/registry tables), not capability
matrices or comparisons.

Purpose: This is the heaviest-#12-load batch (30 over-limit blockquote groups, incl. win32-app-packaging's corpus-worst
9-group count and network-infrastructure's 7); the 2 keyless files hard-fail C17 #10 without the Windows injection
(RETRO-03, D-118-4).
Output: 10 EEE-conformant reference files + registry Status updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-CONTEXT.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-RESEARCH.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-PATTERNS.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical transform of the 10 files via the helper (2 keyless→Windows)</name>
  <files>docs/reference/compliance-timing.md, docs/reference/security-baseline-conflicts.md, docs/reference/licensing-matrix.md, docs/reference/network-infrastructure.md, docs/reference/win32-app-packaging.md, docs/reference/endpoints.md, docs/reference/registry-paths.md, docs/reference/powershell-ref.md, docs/reference/macos-commands.md, docs/reference/macos-log-paths.md</files>
  <read_first>
    - scripts/pipeline/retrofit-reference.mjs (the forked helper from 118-01)
    - docs/_templates/reference-template.md (target Reference frontmatter key set, EEE block line, D3-A order)
    - docs/_registry/RE-index.md (RE-148 compliance-timing, RE-151 endpoints, RE-157 licensing-matrix, RE-160 macos-commands, RE-161 macos-log-paths, RE-162 network-infrastructure, RE-164 powershell-ref, RE-165 registry-paths, RE-166 security-baseline-conflicts, RE-167 win32-app-packaging)
    - scripts/validation/c17-eee-contract.mjs (assertions #9, #10 the output must satisfy; #10 HARD FAILs on the absent platform key for the 2 keyless files)
    - each of the 10 target files (confirm platform value or its absence for powershell-ref/registry-paths, no existing ## Version History, pre-H1 span)
  </read_first>
  <action>
    Run the helper on the 10 batch files (--dry-run first, review the per-file diff, platform-injection flags, and the
    span-byte-length report, then write). The helper injects doc_id (by path join), status: Approved,
    owner: Intune Admin Lead, doc_type: Reference, and platform: Windows for the 2 keyless files (powershell-ref,
    registry-paths — both are Windows-domain, resolve cleanly in D1_MAP). The other 8 retain their platform: key. Block
    line D1 labels: Windows for RE-148/166/157/162/167/164/165, All Platforms for RE-151 (platform: all), macOS for
    RE-160/161 (platform: macOS). It emits the EEE block line
    `**Platform:** <D1 label> · **Doc Type:** Reference · **Doc ID:** RE-NNN · **Status:** Approved` (middle-dot U+00B7),
    relocates the WHOLE pre-H1 span to after the `## Summary` placeholder, and CREATES a new 3-column `## Version History`
    section with the row `| <commit date> | v1.15 EEE reformat — content not re-reviewed | — |` (em-dash). Do NOT hand-edit
    doc_ids or D1 labels. Confirm last_verified is carried verbatim (not the 1970-01-01 sentinel).
  </action>
  <acceptance_criteria>
    - Enrollment precheck returns zero lines for this batch: each of the 10 files satisfies grep -q "^doc_id:", "^status:", "^owner: Intune Admin Lead", "^doc_type: Reference".
    - docs/reference/powershell-ref.md and docs/reference/registry-paths.md contain `platform: Windows`; the other 8 retain their original platform: value.
    - Every block line lists Platform · Doc Type · Doc ID · Status in order and the Platform field equals D1_MAP[frontmatter platform] (Windows / All Platforms / macOS per file).
    - `grep -c "v1.15 EEE reformat — content not re-reviewed" <file>` == 1 for each of the 10 files (fresh 3-column section, 3 cells).
    - No file contains `last_verified: 1970-01-01`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-reference.mjs docs/reference/compliance-timing.md docs/reference/security-baseline-conflicts.md docs/reference/licensing-matrix.md docs/reference/network-infrastructure.md docs/reference/win32-app-packaging.md docs/reference/endpoints.md docs/reference/registry-paths.md docs/reference/powershell-ref.md docs/reference/macos-commands.md docs/reference/macos-log-paths.md; test $? -eq 0</automated>
  </verify>
  <done>All 10 files carry the four EEE keys, correct block line, relocated pre-H1 span, and a fresh 3-column Version-History row; powershell-ref and registry-paths have platform: Windows injected.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-author the ## Summary prose (reference-template lead, ≥30 words)</name>
  <files>docs/reference/compliance-timing.md, docs/reference/security-baseline-conflicts.md, docs/reference/licensing-matrix.md, docs/reference/network-infrastructure.md, docs/reference/win32-app-packaging.md, docs/reference/endpoints.md, docs/reference/registry-paths.md, docs/reference/powershell-ref.md, docs/reference/macos-commands.md, docs/reference/macos-log-paths.md</files>
  <read_first>
    - docs/_templates/reference-template.md (line 38: the Summary prescription — state what the doc provides, platforms/versions covered, primary audience)
    - scripts/validation/c17-eee-contract.mjs (assertion #5: Summary ≥30 words; #4: Summary is first H2)
    - each target file's existing intro (source to summarize — reformat only, no new claims)
  </read_first>
  <action>
    Replace each `[FILL-IN]` Summary placeholder with ≥30 words of real prose per the reference-template lead
    (reference-template.md:38): state what the doc provides (compliance timing / security-baseline conflicts / licensing
    matrix / network infrastructure / Win32 packaging / network endpoints / registry paths / PowerShell function reference
    / macOS commands / macOS log paths), the platform it covers (Windows for most, All Platforms for endpoints, macOS for
    macos-commands/macos-log-paths), and the primary audience (Intune admins / L2 engineers) — then summarize that file's
    existing scope from its current intro. NO new technical claims (reformat-only envelope). Every Summary ≥30 words and
    the first H2. Do NOT use the L1/L2 read-only diagnostic safety banner.
  </action>
  <acceptance_criteria>
    - Each of the 10 files has exactly one `## Summary` as its first H2 (grep -n "^## " shows `## Summary` first).
    - Each Summary body is ≥30 words (verified by the C17 #5 run in Task 3) and names the reference topic + its platform + an admin/engineer audience.
    - No Summary contains the L1/L2 "read-only diagnostic steps only" banner text.
  </acceptance_criteria>
  <verify>
    <automated>for f in docs/reference/compliance-timing.md docs/reference/security-baseline-conflicts.md docs/reference/licensing-matrix.md docs/reference/network-infrastructure.md docs/reference/win32-app-packaging.md docs/reference/endpoints.md docs/reference/registry-paths.md docs/reference/powershell-ref.md docs/reference/macos-commands.md docs/reference/macos-log-paths.md; do awk '/^## /{print; exit}' "$f" | grep -q "## Summary" || { echo "Summary not first H2: $f"; exit 1; }; done</automated>
  </verify>
  <done>All 10 Summaries are reference-template-led (topic + platform + audience), ≥30 words, first H2; no diagnostic safety banner used.</done>
</task>

<task type="auto">
  <name>Task 3: #12 blockquote compliance (30 groups), C17 exit 0, registry Approved</name>
  <files>docs/reference/compliance-timing.md, docs/reference/security-baseline-conflicts.md, docs/reference/licensing-matrix.md, docs/reference/network-infrastructure.md, docs/reference/win32-app-packaging.md, docs/reference/endpoints.md, docs/reference/registry-paths.md, docs/reference/powershell-ref.md, docs/reference/macos-commands.md, docs/reference/macos-log-paths.md, docs/_registry/RE-index.md</files>
  <read_first>
    - scripts/validation/c17-eee-contract.mjs (assertion #12 lines 387-405: every top-level blockquote group joined-text ≤200 chars; a bare `>` line does NOT split a group; blockquoted code fences are NOT masked)
    - .planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-RESEARCH.md (per-file #12 counts: compliance-timing 3 (371), security-baseline-conflicts 2 (414), licensing-matrix 2 (298), network-infrastructure 7 (283), win32-app-packaging 9 (430, corpus-worst group count), endpoints 2 (272), registry-paths 1 (209), powershell-ref 2 (255), macos-commands 1 (260), macos-log-paths 1 (295) = 30 groups)
    - .planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-PATTERNS.md (Transform A sentence-split / Transform B de-blockquote; word-preserving only)
    - docs/_registry/RE-index.md (Status column for RE-148/151/157/160/161/162/164/165/166/167)
  </read_first>
  <action>
    Run the #12 measurement (the exact c17-eee-contract.mjs:387-405 logic) over the 10 files to list every top-level
    blockquote group exceeding 200 chars — RESEARCH counts 30 over-limit groups in this batch, concentrated in
    win32-app-packaging (9) and network-infrastructure (7). For each apply Transform A (split at a sentence/clause
    boundary by inserting a TRULY empty line — zero characters, not a bare `>` line) or Transform B (de-blockquote a
    non-gate callout with embedded code so the fence becomes a real masked top-level fence). Preserve EVERY word — never
    trim, reword, or drop links; escalate any atomic single-sentence >200c blockquote by splitting at an em-dash/semicolon
    clause boundary rather than trimming (reformat-only envelope). Re-run the measurement to prove zero over-limit groups
    remain. Then run C17 and confirm exit 0 for the 10 files. Finally edit docs/_registry/RE-index.md, flipping Status
    Pending → Approved for rows RE-148, RE-151, RE-157, RE-160, RE-161, RE-162, RE-164, RE-165, RE-166, RE-167 only
    (leave RE-147 Pending — do NOT touch).
  </action>
  <acceptance_criteria>
    - The #12 measurement reports zero over-limit top-level blockquote groups across the 10 batch files.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with zero violations for each of the 10 batch file paths.
    - A word-set diff of each file (pre- vs post-#12-fix) shows no words added or removed (reformat-only proof).
    - RE-index.md rows RE-148/151/157/160/161/162/164/165/166/167 show Status `Approved`; RE-147 remains `Pending`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs; test $? -eq 0</automated>
  </verify>
  <done>Zero over-limit top-level blockquotes remain, C17 exits 0 on all 10 files, and RE-148/151/157/160/161/162/164/165/166/167 are Approved while RE-147 stays Pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper output → indexed body text | The EEE block line and Summary lead are indexed as body text (PIPE-02/OQ4) and recited by Copilot |
| #12 reformat → content envelope | Splitting/de-blockquoting must preserve every word (reformat-only, no new content) |

## STRIDE Threat Register

This is a PURE DOCUMENTATION-REFORMAT phase — no untrusted input, network, or auth surface. The only surface worth
noting: the reformat must not corrupt or lose content — mitigated by per-file C17 exit-0 + word-set diffs (acceptance
criteria).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Tampering | helper write of 10 files (pre-H1 span loss / keyless-injection) | mitigate | Dry-run + span-byte-length + injection-flag report + git diff review; whole-pre-H1-span relocation (118-01 fix); C17 #10 gate confirms the 2 injections before registry flip |
| T-118-02 | Tampering | #12 blockquote splits/de-blockquote (30 groups, corpus-heaviest) | mitigate | Word-preserving Transform A/B only; pre/post word-set diff proves zero content change; #12 re-measurement proves zero over-limit; embedded-code groups de-blockquoted to real masked fences |
| T-118-SC | Tampering | package installs | accept | node-builtins-only; zero npm/pip/cargo installs; no supply-chain surface |
</threat_model>

<verification>
- Enrollment precheck returns zero lines for RE-148/151/157/160/161/162/164/165/166/167.
- `node scripts/validation/c17-eee-contract.mjs` exits 0; zero violations on the 10 batch paths.
- powershell-ref and registry-paths carry platform: Windows (C17 #10 passes on the injected key).
- #12 measurement reports zero over-limit groups; word-set diff shows no content change.
- RE-index.md rows RE-148/151/157/160/161/162/164/165/166/167 = Approved; RE-147 = Pending.
</verification>

<success_criteria>
All 10 security/infra-detail/macOS-CLI/PowerShell reference docs are EEE-conformant, C17-green, reference-template-
Summary-led, with the 2 keyless files Windows-injected, and marked Approved; RE-147 stays Pending. SC1-SC4 satisfied for
RE-148/151/157/160/161/162/164/165/166/167.
</success_criteria>

<output>
Create `.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-04-SUMMARY.md` when done.
</output>
