---
phase: 118-reference-doc-retrofit-table-remediation-26-docs
plan: 03
type: execute
wave: 3
depends_on: [118-01, 118-02]
files_modified:
  - docs/reference/apv1-apv2-migration.md
  - docs/reference/imaging-to-autopilot.md
  - docs/reference/gpo-to-intune.md
  - docs/reference/esp-timeout-tuning.md
  - docs/reference/deployment-reporting.md
  - docs/reference/drift-detection.md
  - docs/reference/new-batch-workflow.md
  - docs/reference/entra-prerequisites.md
  - docs/_registry/RE-index.md
autonomous: true
requirements: [RETRO-03]
must_haves:
  truths:
    - "All 8 files carry the EEE header block (Platform+Doc Type first, doc_id from registry, doc_type: Reference, Status: Approved); all 8 already carry platform: Windows → D1 label Windows, no injection needed (SC1)"
    - "The three Guide-TITLED files (esp-timeout-tuning RE-153, gpo-to-intune RE-154, imaging-to-autopilot RE-155) keep doc_type: Reference exactly per the Phase-114 registry lock (D-118-3) — title ≠ doc_type; C17 #9 compares block↔frontmatter only"
    - "All 8 files exercise the VH CREATE branch (fresh 3-column section — none has a pre-existing ## Version History) (D-118-4)"
    - "Each file has D3-A structure + the v1.15 EEE reformat Version-History row + Last Reviewed = last_verified verbatim; each ## Summary is reference-template-led ≥30 words (SC2, SC3)"
    - "C17 exits 0 on all 8 files with zero #12 over-limit blockquote groups (20 groups fixed word-preserving); RE-146/149/150/152/153/154/155/163 flipped Pending → Approved (SC4)"
  artifacts:
    - path: "docs/reference/apv1-apv2-migration.md"
      provides: "Retrofitted migration reference RE-146 (CREATE VH branch, 4 #12 groups fixed)"
      contains: "doc_id: RE-146"
    - path: "docs/_registry/RE-index.md"
      provides: "RE-146/149/150/152/153/154/155/163 Status flipped Pending → Approved"
      contains: "RE-155"
  key_links:
    - from: "docs/reference/gpo-to-intune.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-154 resolved by path join (doc_type stays Reference despite Guide title)"
      pattern: "RE-154"
    - from: "block line **Platform:** field"
      to: "D1_MAP[Windows]"
      via: "exact D1 label match (C17 #9)"
      pattern: "\\*\\*Platform:\\*\\* Windows"
---

<objective>
Retrofit the 8 migration / monitoring / infrastructure-prerequisite reference docs — apv1-apv2-migration (RE-146),
imaging-to-autopilot (RE-155), gpo-to-intune (RE-154), esp-timeout-tuning (RE-153), deployment-reporting (RE-149),
drift-detection (RE-150), new-batch-workflow (RE-163), entra-prerequisites (RE-152) — to the EEE standard so C17 exits
0 on every file. All 8 already carry platform: Windows (no injection); none has an existing Version History (all CREATE).
No D-118-1 per-table prose here — these carry ordinary lookup/procedure tables, not capability matrices or comparisons.

Purpose: These are the medium-#12-load Windows reference docs (20 over-limit blockquote groups); the three Guide-titled
migration docs must retain doc_type: Reference per the registry lock (RETRO-03, D-118-3, D-118-4).
Output: 8 EEE-conformant reference files + registry Status updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-CONTEXT.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-RESEARCH.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-PATTERNS.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical transform of the 8 Windows reference files via the helper</name>
  <files>docs/reference/apv1-apv2-migration.md, docs/reference/imaging-to-autopilot.md, docs/reference/gpo-to-intune.md, docs/reference/esp-timeout-tuning.md, docs/reference/deployment-reporting.md, docs/reference/drift-detection.md, docs/reference/new-batch-workflow.md, docs/reference/entra-prerequisites.md</files>
  <read_first>
    - scripts/pipeline/retrofit-reference.mjs (the forked helper from 118-01)
    - docs/_templates/reference-template.md (target Reference frontmatter key set, EEE block line, D3-A order)
    - docs/_registry/RE-index.md (RE-146 apv1-apv2-migration, RE-149 deployment-reporting, RE-150 drift-detection, RE-152 entra-prerequisites, RE-153 esp-timeout-tuning, RE-154 gpo-to-intune, RE-155 imaging-to-autopilot, RE-163 new-batch-workflow)
    - scripts/validation/c17-eee-contract.mjs (assertions #9, #10 the output must satisfy)
    - each of the 8 target files (confirm platform: Windows present, no existing ## Version History, pre-H1 span)
  </read_first>
  <action>
    Run the helper on the 8 batch files (--dry-run first, review the per-file diff and the span-byte-length report, then
    write). The helper injects doc_id (RE-146/149/150/152/153/154/155/163 by path join), status: Approved,
    owner: Intune Admin Lead, doc_type: Reference. All 8 already carry platform: Windows — NO injection; the block line
    Platform field is `Windows` (D1_MAP[Windows]). It emits the EEE block line
    `**Platform:** Windows · **Doc Type:** Reference · **Doc ID:** RE-NNN · **Status:** Approved` (middle-dot U+00B7),
    relocates the WHOLE pre-H1 span to after the `## Summary` placeholder, and CREATES a new 3-column `## Version History`
    section (none of these 8 has one) with the row `| <commit date> | v1.15 EEE reformat — content not re-reviewed | — |`
    (em-dash). CRITICAL (D-118-3): the three Guide-titled files (esp-timeout-tuning, gpo-to-intune, imaging-to-autopilot)
    keep doc_type: Reference — do NOT reclassify to Guide. Do NOT hand-edit doc_ids or the Windows label. Confirm
    last_verified is carried verbatim (not the 1970-01-01 sentinel).
  </action>
  <acceptance_criteria>
    - Enrollment precheck returns zero lines for this batch: each of the 8 files satisfies grep -q "^doc_id:", "^status:", "^owner: Intune Admin Lead", "^doc_type: Reference".
    - All 8 files contain `platform: Windows`; every block line begins `**Platform:** Windows` and lists Platform · Doc Type · Doc ID · Status in order.
    - esp-timeout-tuning.md, gpo-to-intune.md, imaging-to-autopilot.md each show `doc_type: Reference` (NOT Guide) in both frontmatter and the block line.
    - `grep -c "v1.15 EEE reformat — content not re-reviewed" <file>` == 1 for each of the 8 files (fresh 3-column section, 3 cells).
    - No file contains `last_verified: 1970-01-01`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-reference.mjs docs/reference/apv1-apv2-migration.md docs/reference/imaging-to-autopilot.md docs/reference/gpo-to-intune.md docs/reference/esp-timeout-tuning.md docs/reference/deployment-reporting.md docs/reference/drift-detection.md docs/reference/new-batch-workflow.md docs/reference/entra-prerequisites.md; test $? -eq 0</automated>
  </verify>
  <done>All 8 Windows reference files carry the four EEE keys, correct Windows block line, relocated pre-H1 span, and a fresh 3-column Version-History row; the 3 Guide-titled files keep doc_type: Reference.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-author the ## Summary prose (reference-template lead, ≥30 words)</name>
  <files>docs/reference/apv1-apv2-migration.md, docs/reference/imaging-to-autopilot.md, docs/reference/gpo-to-intune.md, docs/reference/esp-timeout-tuning.md, docs/reference/deployment-reporting.md, docs/reference/drift-detection.md, docs/reference/new-batch-workflow.md, docs/reference/entra-prerequisites.md</files>
  <read_first>
    - docs/_templates/reference-template.md (line 38: the Summary prescription — state what the doc provides, platforms/versions covered, primary audience)
    - scripts/validation/c17-eee-contract.mjs (assertion #5: Summary ≥30 words; #4: Summary is first H2)
    - each target file's existing intro (source to summarize — reformat only, no new claims)
  </read_first>
  <action>
    Replace each `[FILL-IN]` Summary placeholder with ≥30 words of real prose per the reference-template lead
    (reference-template.md:38): state what the doc provides (migration playbook / timeout tuning / deployment reporting /
    drift detection / batch workflow / Entra prerequisites), that it covers Windows Autopilot, and the primary audience
    (Intune admins / L2 engineers) — then summarize that file's existing scope drawn from its current intro. NO new
    technical claims (reformat-only envelope). Every Summary ≥30 words and the first H2. These are reference docs, not
    diagnostic runbooks — do NOT use the L1/L2 read-only safety banner.
  </action>
  <acceptance_criteria>
    - Each of the 8 files has exactly one `## Summary` as its first H2 (grep -n "^## " shows `## Summary` first).
    - Each Summary body is ≥30 words (verified by the C17 #5 run in Task 3) and names the reference topic + Windows + an admin/engineer audience.
    - No Summary contains the L1/L2 "read-only diagnostic steps only" banner text.
  </acceptance_criteria>
  <verify>
    <automated>for f in docs/reference/apv1-apv2-migration.md docs/reference/imaging-to-autopilot.md docs/reference/gpo-to-intune.md docs/reference/esp-timeout-tuning.md docs/reference/deployment-reporting.md docs/reference/drift-detection.md docs/reference/new-batch-workflow.md docs/reference/entra-prerequisites.md; do awk '/^## /{print; exit}' "$f" | grep -q "## Summary" || { echo "Summary not first H2: $f"; exit 1; }; done</automated>
  </verify>
  <done>All 8 Summaries are reference-template-led (topic + Windows + audience), ≥30 words, first H2; no diagnostic safety banner used.</done>
</task>

<task type="auto">
  <name>Task 3: #12 blockquote compliance, C17 exit 0, registry Approved</name>
  <files>docs/reference/apv1-apv2-migration.md, docs/reference/imaging-to-autopilot.md, docs/reference/gpo-to-intune.md, docs/reference/esp-timeout-tuning.md, docs/reference/deployment-reporting.md, docs/reference/drift-detection.md, docs/reference/new-batch-workflow.md, docs/reference/entra-prerequisites.md, docs/_registry/RE-index.md</files>
  <read_first>
    - scripts/validation/c17-eee-contract.mjs (assertion #12 lines 387-405: every top-level blockquote group joined-text ≤200 chars; a bare `>` line does NOT split a group; blockquoted code fences are NOT masked)
    - .planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-RESEARCH.md (per-file #12 counts: apv1-apv2-migration 4 (max 344), imaging-to-autopilot 5 (max 351), gpo-to-intune 3 (max 439), esp-timeout-tuning 4 (max 368), deployment-reporting 1 (max 295), drift-detection 1 (max 368), new-batch-workflow 1 (max 298), entra-prerequisites 1 (max 264) = 20 groups)
    - .planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-PATTERNS.md (Transform A sentence-split / Transform B de-blockquote; word-preserving only)
    - docs/_registry/RE-index.md (Status column for RE-146/149/150/152/153/154/155/163)
  </read_first>
  <action>
    Run the #12 measurement (the exact c17-eee-contract.mjs:387-405 logic) over the 8 files to list every top-level
    blockquote group exceeding 200 chars — RESEARCH counts 20 over-limit groups in this batch (max 439c in gpo-to-intune).
    For each apply Transform A (split at a sentence/clause boundary by inserting a TRULY empty line — zero characters, not
    a bare `>` line) or Transform B (de-blockquote a non-gate callout with embedded code — for any group in the 600+ char
    range check for an embedded ``` fence and prefer Transform B so the fence becomes a real masked top-level fence).
    Preserve EVERY word — never trim, reword, or drop links; escalate any atomic single-sentence >200c blockquote by
    splitting at an em-dash/semicolon clause boundary rather than trimming (reformat-only envelope). Re-run the
    measurement to prove zero over-limit groups remain. Then run C17 and confirm exit 0 for the 8 files. Finally edit
    docs/_registry/RE-index.md, flipping Status Pending → Approved for rows RE-146, RE-149, RE-150, RE-152, RE-153,
    RE-154, RE-155, RE-163 only (leave RE-147 Pending — do NOT touch).
  </action>
  <acceptance_criteria>
    - The #12 measurement reports zero over-limit top-level blockquote groups across the 8 batch files.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with zero violations for each of the 8 batch file paths.
    - A word-set diff of each file (pre- vs post-#12-fix) shows no words added or removed (reformat-only proof).
    - RE-index.md rows RE-146/149/150/152/153/154/155/163 show Status `Approved`; RE-147 remains `Pending`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs; test $? -eq 0</automated>
  </verify>
  <done>Zero over-limit top-level blockquotes remain, C17 exits 0 on all 8 files, and RE-146/149/150/152/153/154/155/163 are Approved while RE-147 stays Pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper output → indexed body text | The EEE block line and Summary lead are indexed as body text (PIPE-02/OQ4) and recited by Copilot |
| #12 reformat → content envelope | Splitting/de-blockquoting must preserve every word (reformat-only, no new content) |

## STRIDE Threat Register

This is a PURE DOCUMENTATION-REFORMAT phase — no untrusted input, network, or auth surface. The only surface worth
noting: the reformat must not corrupt or lose content — mitigated by per-file C17 exit-0 + word-set diffs (acceptance
criteria).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Tampering | helper write of 8 files (pre-H1 span loss) | mitigate | Dry-run + span-byte-length report + git diff review before commit; whole-pre-H1-span relocation (118-01 fix); C17 exit 0 gate before registry flip |
| T-118-02 | Tampering | #12 blockquote splits/de-blockquote (20 groups) | mitigate | Word-preserving Transform A/B only; pre/post word-set diff proves zero content change; #12 re-measurement proves zero over-limit |
| T-118-SC | Tampering | package installs | accept | node-builtins-only; zero npm/pip/cargo installs; no supply-chain surface |
</threat_model>

<verification>
- Enrollment precheck returns zero lines for RE-146/149/150/152/153/154/155/163.
- `node scripts/validation/c17-eee-contract.mjs` exits 0; zero violations on the 8 batch paths.
- The 3 Guide-titled files show doc_type: Reference in frontmatter and block line.
- #12 measurement reports zero over-limit groups; word-set diff shows no content change.
- RE-index.md rows RE-146/149/150/152/153/154/155/163 = Approved; RE-147 = Pending.
</verification>

<success_criteria>
All 8 migration/monitoring/infra-prereq reference docs are EEE-conformant, C17-green, reference-template-Summary-led,
doc_type: Reference preserved on the 3 Guide-titled files, and marked Approved; RE-147 stays Pending. SC1-SC4 satisfied
for RE-146/149/150/152/153/154/155/163.
</success_criteria>

<output>
Create `.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-03-SUMMARY.md` when done.
</output>
