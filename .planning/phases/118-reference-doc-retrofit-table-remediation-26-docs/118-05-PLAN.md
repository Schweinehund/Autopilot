---
phase: 118-reference-doc-retrofit-table-remediation-26-docs
plan: 05
type: execute
wave: 5
depends_on: [118-01]
files_modified:
  - docs/error-codes/00-index.md
  - docs/error-codes/01-mdm-enrollment.md
  - docs/error-codes/02-tpm-attestation.md
  - docs/error-codes/03-esp-enrollment.md
  - docs/error-codes/04-pre-provisioning.md
  - docs/error-codes/05-hybrid-join.md
  - docs/error-codes/06-apv2-device-preparation.md
  - docs/_registry/RE-index.md
autonomous: true
requirements: [RETRO-03]
must_haves:
  truths:
    - "All 7 error-codes files carry the EEE header block (Platform+Doc Type first, doc_id from registry, doc_type: Reference, Status: Approved); ALL 7 are keyless (applies_to both/APv1/APv2 are NOT platform keys) → platform: Windows injected or C17 #10 hard-fails (D-118-4 rider)"
    - "All 7 files exercise the VH PREPEND-2col branch — each already has a 2-column | Date | Change | Version History table; the inserted v1.15 EEE reformat row has exactly 2 cells (NOT 3) or the table silently misaligns (D-118-4 rider, RESEARCH Pitfall 1)"
    - "docs/error-codes/00-index.md's TWO over-200-char top-level blockquotes (L8 Framework coverage 287c pre-H1; L65 APv2 Note 284c post-H1) are BOTH word-preserving split — #12 scans every top-level group regardless of position (D-118-4 rider)"
    - "The 30-row Quick-Lookup table in 00-index.md is already C17 #11-compliant via existing post-table prose within the 5-line window — do NOT add per-table D-118-1 prose (error-code lookup tables are not capability matrices/comparisons)"
    - "Each file has D3-A structure + the v1.15 EEE reformat Version-History row + Last Reviewed = last_verified verbatim; each ## Summary is reference-template-led ≥30 words; C17 exits 0 on all 7 files; RE-168..RE-174 flipped Pending → Approved (SC2, SC3, SC4)"
  artifacts:
    - path: "docs/error-codes/00-index.md"
      provides: "Retrofitted error-code master index RE-168 (keyless→Windows, PREPEND-2col, double #12 split, #11-compliant 30-row table)"
      contains: "doc_id: RE-168"
    - path: "docs/_registry/RE-index.md"
      provides: "RE-168..RE-174 Status flipped Pending → Approved"
      contains: "RE-174"
  key_links:
    - from: "docs/error-codes/00-index.md"
      to: "docs/_registry/RE-index.md"
      via: "doc_id RE-168 resolved by path join"
      pattern: "RE-168"
    - from: "block line **Platform:** field (keyless→inject)"
      to: "D1_MAP[Windows]"
      via: "exact D1 label match after platform: Windows injection (C17 #9/#10)"
      pattern: "\\*\\*Platform:\\*\\* Windows"
---

<objective>
Retrofit the 7 error-code reference docs — 00-index (RE-168), 01-mdm-enrollment (RE-169), 02-tpm-attestation (RE-170),
03-esp-enrollment (RE-171), 04-pre-provisioning (RE-172), 05-hybrid-join (RE-173), 06-apv2-device-preparation (RE-174) —
to the EEE standard so C17 exits 0 on every file. This is the cohesive error-codes batch (D-02 mandate: one set). ALL 7
are keyless (they carry applies_to: both/APv1/APv2, which are NOT platform keys and NOT in D1_MAP) → platform: Windows
injected. ALL 7 have a pre-existing 2-column Version History → the PREPEND-2col branch. No D-118-1 per-table prose — error
-code lookup tables are not capability matrices/comparisons; the one >25-row table (00-index, 30 rows) is already
#11-compliant.

Purpose: Without platform: Windows injection all 7 hard-fail C17 #10; the 2-column VH shape must be preserved or the row
silently misaligns (RESEARCH Pitfall 1); 00-index has the phase's mandatory double #12 split (RETRO-03, D-118-4).
Output: 7 EEE-conformant error-code reference files + registry Status updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-CONTEXT.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-RESEARCH.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-PATTERNS.md
@.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical transform of the 7 keyless error-codes files via the helper (PREPEND-2col)</name>
  <files>docs/error-codes/00-index.md, docs/error-codes/01-mdm-enrollment.md, docs/error-codes/02-tpm-attestation.md, docs/error-codes/03-esp-enrollment.md, docs/error-codes/04-pre-provisioning.md, docs/error-codes/05-hybrid-join.md, docs/error-codes/06-apv2-device-preparation.md</files>
  <read_first>
    - scripts/pipeline/retrofit-reference.mjs (the forked helper from 118-01, esp. its PREPEND-2col branch)
    - docs/_templates/reference-template.md (target Reference frontmatter key set, EEE block line, D3-A order)
    - docs/_registry/RE-index.md (RE-168 00-index, RE-169 01-mdm-enrollment, RE-170 02-tpm-attestation, RE-171 03-esp-enrollment, RE-172 04-pre-provisioning, RE-173 05-hybrid-join, RE-174 06-apv2-device-preparation)
    - scripts/validation/c17-eee-contract.mjs (assertions #9, #10 — #10 HARD FAILs on the absent platform key for all 7; #11 lines 344-380 confirms 00-index's 30-row table already compliant)
    - each of the 7 target files (confirm keyless frontmatter with applies_to: both/APv1/APv2 but no platform: key; confirm the existing 2-column | Date | Change | Version History table; the pre-H1 span)
  </read_first>
  <action>
    Run the helper on the 7 batch files (--dry-run first, review the per-file diff, the platform-injection flags, the
    VH-branch classification (must be PREPEND-2col for all 7), and the span-byte-length report, then write). The helper
    injects doc_id (RE-168..RE-174 by path join), status: Approved, owner: Intune Admin Lead, doc_type: Reference, and
    platform: Windows for ALL 7 (all keyless — applies_to is NOT a platform key; all 7 are Windows-domain, resolve cleanly
    in D1_MAP). It emits the EEE block line
    `**Platform:** Windows · **Doc Type:** Reference · **Doc ID:** RE-NNN · **Status:** Approved` (middle-dot U+00B7),
    relocates the WHOLE pre-H1 span (for 00-index this is the L8 "Framework coverage" blockquote) to after the `## Summary`
    placeholder, and PREPENDS a 2-column row `| <commit date> | v1.15 EEE reformat — content not re-reviewed |` (2 cells,
    NOT 3 — matching the existing 2-column table) to each file's existing ## Version History. Verify the inserted row has
    exactly 2 pipe-delimited cells per file. Do NOT hand-edit doc_ids or the Windows label. Confirm last_verified is
    carried verbatim (not the 1970-01-01 sentinel). Do NOT add per-table prose to the 30-row Quick-Lookup table — it is
    already #11-compliant and is a lookup table, not a capability matrix.
  </action>
  <acceptance_criteria>
    - Enrollment precheck returns zero lines for this batch: each of the 7 files satisfies grep -q "^doc_id:", "^status:", "^owner: Intune Admin Lead", "^doc_type: Reference".
    - All 7 files contain `platform: Windows` in frontmatter; every block line begins `**Platform:** Windows` and lists Platform · Doc Type · Doc ID · Status in order.
    - For each of the 7 files the newly-inserted `v1.15 EEE reformat` Version-History row has exactly 2 pipe-delimited cells (matches the pre-existing 2-column table — NOT 3 cells); `grep -c "v1.15 EEE reformat — content not re-reviewed" <file>` == 1.
    - No file contains `last_verified: 1970-01-01`; 00-index.md's 30-row Quick-Lookup table has no newly-added `> **Table summary:**` blockquote (already #11-compliant).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-reference.mjs docs/error-codes/00-index.md docs/error-codes/01-mdm-enrollment.md docs/error-codes/02-tpm-attestation.md docs/error-codes/03-esp-enrollment.md docs/error-codes/04-pre-provisioning.md docs/error-codes/05-hybrid-join.md docs/error-codes/06-apv2-device-preparation.md; test $? -eq 0</automated>
  </verify>
  <done>All 7 error-codes files carry the four EEE keys + platform: Windows, correct block line, relocated pre-H1 span, and a 2-cell Version-History row prepended into the existing 2-column table.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-author the ## Summary prose (reference-template lead, ≥30 words)</name>
  <files>docs/error-codes/00-index.md, docs/error-codes/01-mdm-enrollment.md, docs/error-codes/02-tpm-attestation.md, docs/error-codes/03-esp-enrollment.md, docs/error-codes/04-pre-provisioning.md, docs/error-codes/05-hybrid-join.md, docs/error-codes/06-apv2-device-preparation.md</files>
  <read_first>
    - docs/_templates/reference-template.md (line 38: the Summary prescription — state what the doc provides, platforms/versions covered, primary audience)
    - scripts/validation/c17-eee-contract.mjs (assertion #5: Summary ≥30 words; #4: Summary is first H2)
    - each target file's existing intro + error-code table (source to summarize — reformat only, no new claims)
  </read_first>
  <action>
    Replace each `[FILL-IN]` Summary placeholder with ≥30 words of real prose per the reference-template lead
    (reference-template.md:38): state that the doc is an error-code lookup reference (master index / MDM enrollment /
    TPM attestation / ESP enrollment / pre-provisioning / hybrid-join / APv2 device-preparation error codes), that it
    covers Windows Autopilot (classic/APv1) error codes plus APv2 symptom-based failures where applicable, and the
    primary audience (L1 service desk / L2 engineers) — then summarize that file's existing scope from its current intro.
    NO new technical claims (reformat-only envelope). Every Summary ≥30 words and the first H2.
  </action>
  <acceptance_criteria>
    - Each of the 7 files has exactly one `## Summary` as its first H2 (grep -n "^## " shows `## Summary` first).
    - Each Summary body is ≥30 words (verified by the C17 #5 run in Task 3) and names the error-code category + Windows Autopilot + an L1/L2 audience.
    - No Summary contains the L1/L2 "read-only diagnostic steps only" runbook banner text (these are reference lookups, not runbooks).
  </acceptance_criteria>
  <verify>
    <automated>for f in docs/error-codes/00-index.md docs/error-codes/01-mdm-enrollment.md docs/error-codes/02-tpm-attestation.md docs/error-codes/03-esp-enrollment.md docs/error-codes/04-pre-provisioning.md docs/error-codes/05-hybrid-join.md docs/error-codes/06-apv2-device-preparation.md; do awk '/^## /{print; exit}' "$f" | grep -q "## Summary" || { echo "Summary not first H2: $f"; exit 1; }; done</automated>
  </verify>
  <done>All 7 Summaries are reference-template-led (error-code category + Windows Autopilot + L1/L2 audience), ≥30 words, first H2.</done>
</task>

<task type="auto">
  <name>Task 3: #12 blockquote compliance (incl. 00-index double-split), C17 exit 0, registry Approved</name>
  <files>docs/error-codes/00-index.md, docs/error-codes/01-mdm-enrollment.md, docs/error-codes/02-tpm-attestation.md, docs/error-codes/03-esp-enrollment.md, docs/error-codes/04-pre-provisioning.md, docs/error-codes/05-hybrid-join.md, docs/error-codes/06-apv2-device-preparation.md, docs/_registry/RE-index.md</files>
  <read_first>
    - scripts/validation/c17-eee-contract.mjs (assertion #12 lines 387-405: every top-level blockquote group joined-text ≤200 chars regardless of position; a bare `>` line does NOT split a group; blockquoted code fences are NOT masked)
    - .planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-RESEARCH.md (per-file #12 counts: 00-index 2 (287 pre-H1 "Framework coverage" + 284 post-H1 "APv2 Note" — the MANDATORY double-split), 01-mdm-enrollment 1 (689, corpus-worst single group), 02-tpm-attestation 1 (361), 03-esp-enrollment 1 (274), 04-pre-provisioning 1 (270), 05-hybrid-join 1 (248), 06-apv2-device-preparation 2 (328, 210) = 9 groups)
    - .planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-PATTERNS.md (Transform A sentence-split / Transform B de-blockquote; word-preserving only; the 00-index double-split invariant)
    - docs/_registry/RE-index.md (Status column for RE-168..RE-174)
  </read_first>
  <action>
    Run the #12 measurement (the exact c17-eee-contract.mjs:387-405 logic) over the 7 files to list every top-level
    blockquote group exceeding 200 chars — RESEARCH counts 9 over-limit groups. MANDATORY: docs/error-codes/00-index.md
    has TWO independent over-limit groups (the relocated L8 "Framework coverage" 287c and the post-H1 L65 "APv2 Note"
    284c) — BOTH need word-preserving splits, not just the pre-H1 gate relocation. 01-mdm-enrollment carries the
    corpus-worst single group (689c). For each over-limit group apply Transform A (split at a sentence/clause boundary by
    inserting a TRULY empty line — zero characters, not a bare `>` line) or Transform B (de-blockquote a non-gate callout
    with embedded code so the fence becomes a real masked top-level fence — check the 689c group for an embedded ```
    fence). Preserve EVERY word — never trim, reword, or drop links; escalate any atomic single-sentence >200c blockquote
    at an em-dash/semicolon clause boundary rather than trimming (reformat-only envelope). Re-run the measurement to prove
    zero over-limit groups remain. Then run C17 and confirm exit 0 for the 7 files. Finally edit docs/_registry/RE-index.md,
    flipping Status Pending → Approved for rows RE-168, RE-169, RE-170, RE-171, RE-172, RE-173, RE-174 (leave RE-147
    Pending — do NOT touch).
  </action>
  <acceptance_criteria>
    - The #12 measurement reports zero over-limit top-level blockquote groups across the 7 batch files; docs/error-codes/00-index.md's two originally-over-limit groups are BOTH resolved.
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with zero violations for each of the 7 batch file paths.
    - A word-set diff of each file (pre- vs post-#12-fix) shows no words added or removed (reformat-only proof).
    - RE-index.md rows RE-168..RE-174 show Status `Approved`; RE-147 remains `Pending`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs; test $? -eq 0</automated>
  </verify>
  <done>Zero over-limit top-level blockquotes remain (00-index double-split resolved), C17 exits 0 on all 7 files, and RE-168..RE-174 are Approved while RE-147 stays Pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper output → indexed body text | The EEE block line and Summary lead are indexed as body text (PIPE-02/OQ4) and recited by Copilot |
| VH 2-column preservation + #12 reformat → content envelope | The inserted row must match the 2-column table; splitting must preserve every word (reformat-only) |

## STRIDE Threat Register

This is a PURE DOCUMENTATION-REFORMAT phase — no untrusted input, network, or auth surface. The only surface worth
noting: the reformat must not corrupt or lose content — mitigated by per-file C17 exit-0 + word-set diffs + the 2-cell
row check (acceptance criteria).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Tampering | VH 2-column misalignment (3-cell row under 2-col header — RESEARCH Pitfall 1) | mitigate | Helper detectVhColumnCount emits a 2-cell row; Task 1 acceptance criterion asserts exactly 2 cells per file; C17 exit 0 gate before registry flip |
| T-118-02 | Tampering | #12 blockquote splits incl. 00-index double-split + 689c group | mitigate | Word-preserving Transform A/B only; both 00-index groups split; pre/post word-set diff proves zero content change; #12 re-measurement proves zero over-limit |
| T-118-SC | Tampering | package installs | accept | node-builtins-only; zero npm/pip/cargo installs; no supply-chain surface |
</threat_model>

<verification>
- Enrollment precheck returns zero lines for RE-168..RE-174.
- `node scripts/validation/c17-eee-contract.mjs` exits 0; zero violations on the 7 batch paths.
- All 7 carry platform: Windows (C17 #10 passes on the injected key); each VH row has exactly 2 cells.
- 00-index.md's two over-limit blockquotes are both split; #12 measurement reports zero over-limit; word-set diff shows no content change.
- RE-index.md rows RE-168..RE-174 = Approved; RE-147 = Pending.
</verification>

<success_criteria>
All 7 error-code reference docs are EEE-conformant, C17-green, reference-template-Summary-led, keyless-Windows-injected,
2-column-VH-preserved, with the 00-index double #12 split resolved, and marked Approved; RE-147 stays Pending. SC1-SC4
satisfied for RE-168..RE-174.
</success_criteria>

<output>
Create `.planning/phases/118-reference-doc-retrofit-table-remediation-26-docs/118-05-SUMMARY.md` when done.
</output>
