---
phase: 45-per-oem-aosp-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-android/09-aosp-realwear.md
autonomous: true
requirements: [AEAOSPFULL-01]
requirements_addressed: [AEAOSPFULL-01]

must_haves:
  truths:
    - "RealWear admin doc exists at docs/admin-setup-android/09-aosp-realwear.md"
    - "11-H2 sibling-parity skeleton present in fixed order per D-01"
    - "REQUIRED add-on H2 `## Wi-Fi QR Embedding Walkthrough` between H2 #5 and H2 #6 per D-02"
    - "PITFALL-7 framing inline at top per D-04 + D-23 with 'supported under AOSP because no GMS; if GMS is present, use Android Enterprise fully managed instead' wording"
    - "Wi-Fi staging requirement is explicit: WPA-PSK / WPA2-PSK / WPA3 ONLY — NOT EAP-PEAP, NOT EAP-TLS"
    - "Universal banner contract present BEFORE H1 per D-03 (Platform gate + Platform note blockquotes)"
    - "Stable anchors per D-05 on #prerequisites, #provisioning-steps, #verification, #common-failures, #renewal-maintenance, #what-breaks-summary, #wi-fi-qr-embedding"
    - "Frontmatter contract per D-27 with last_verified 2026-04-25 + review_by 2026-06-24 (60-day cycle per D-26)"
  artifacts:
    - path: "docs/admin-setup-android/09-aosp-realwear.md"
      provides: "RealWear (HMT-1 / HMT-1Z1 / Navigator 500) AOSP admin guide"
      min_lines: 180
      contains: "Wi-Fi QR Embedding Walkthrough"
  key_links:
    - from: "docs/admin-setup-android/09-aosp-realwear.md"
      to: "docs/_glossary-android.md"
      via: "AOSP / OEMConfig terminology cross-link in `## See Also`"
      pattern: "_glossary-android.md"
    - from: "docs/admin-setup-android/09-aosp-realwear.md"
      to: "docs/admin-setup-android/06-aosp-stub.md"
      via: "AOSP scope context in `## See Also`"
      pattern: "06-aosp-stub.md"
---

<objective>
Author the RealWear AOSP admin guide at `docs/admin-setup-android/09-aosp-realwear.md` per AEAOSPFULL-01 — covers HMT-1, HMT-1Z1, Navigator 500 with Wi-Fi-credentials-embedded-in-QR REQUIRED scope and PSK-only-NOT-EAP staging-Wi-Fi auth discipline. Honors locked decisions D-01 (11-H2 skeleton), D-02 (REQUIRED `## Wi-Fi QR Embedding Walkthrough` add-on H2), D-03 (universal banner), D-04 + D-23 (PITFALL-7 framing per-claim discipline), D-05 (anchor scaffolding), D-26 (60-day freshness), D-27 (frontmatter), D-29 (shared-file modification guard — single new file only).

Purpose: Close the v1.4 deferred RealWear per-OEM gap; serves as the canonical Intune admin reference for organizations deploying RealWear AR/VR headsets for hands-free frontline work (field service, maintenance, remote expert assistance).
Output: `docs/admin-setup-android/09-aosp-realwear.md` (~1500-2500 words; ~180-280 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-per-oem-aosp-expansion/45-CONTEXT.md
@.planning/phases/45-per-oem-aosp-expansion/45-RESEARCH.md
@.planning/phases/45-per-oem-aosp-expansion/45-PATTERNS.md
@.planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md
@docs/admin-setup-android/07-knox-mobile-enrollment.md
@docs/admin-setup-android/03-fully-managed-cobo.md
@docs/admin-setup-android/06-aosp-stub.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/admin-setup-android/09-aosp-realwear.md</name>
  <files>docs/admin-setup-android/09-aosp-realwear.md</files>
  <read_first>
    - .planning/phases/45-per-oem-aosp-expansion/45-CONTEXT.md (D-01..D-30 locked decisions; D-02 RealWear add-on; D-04/D-23 PITFALL-7; D-05 anchors; D-27 frontmatter; D-29 shared-file guard)
    - .planning/phases/45-per-oem-aosp-expansion/45-RESEARCH.md §1 RealWear (per-OEM data table at line 353-366; Wi-Fi PSK-only authoritative source) and §"Anti-Patterns" (PITFALL-7 erosion + Wi-Fi EAP claims)
    - .planning/phases/45-per-oem-aosp-expansion/45-PATTERNS.md (11-H2 skeleton in `## Pattern Assignments`; Wi-Fi QR Embedding Walkthrough requirements at lines 228-232; sibling H2 line citations)
    - .planning/phases/45-per-oem-aosp-expansion/PHASE-45-AOSP-SOURCE.md (verbatim RealWear deep content extract; suggested 5-item Wi-Fi embed walkthrough outline at lines 39-46; PITFALL-7 invariant at lines 47-51)
    - docs/admin-setup-android/07-knox-mobile-enrollment.md (closest sibling; lines 1-7 frontmatter; lines 11-12 banner; lines 27-156 step pattern; line 193 Renewal/Maintenance; line 202 What Breaks Summary; line 226-230 Changelog)
    - docs/admin-setup-android/03-fully-managed-cobo.md (sibling-parity check; line 197 What Breaks Summary; line 218 Renewal/Maintenance)
    - docs/admin-setup-android/06-aosp-stub.md (current state; lines 11-13 banner; lines 15-19 HTML-comment subtractive deletion; line 23 ⚠️ scope blockquote pattern; lines 41-65 8-OEM enumeration including RealWear)
  </read_first>
  <action>
Create the file `docs/admin-setup-android/09-aosp-realwear.md` with the following exact structure. Do NOT modify any other file. Follow ALL ordering, anchor IDs, frontmatter values, banner blockquotes, source-confidence markers, and PITFALL-7 framing precisely.

**1. Frontmatter (lines 1-7) — verbatim per D-27:**
```yaml
---
last_verified: 2026-04-25
review_by: 2026-06-24
audience: admin
platform: Android
applies_to: AOSP
---
```

**2. Universal banner blockquotes (BEFORE H1) — D-03 LOCKED:**
```markdown
> **Platform gate:** RealWear AOSP device management — HMT-1 / HMT-1Z1 / Navigator 500 frontline AR headsets in Microsoft Intune. For Windows Autopilot, see [Windows Admin](../admin-setup/00-overview.md). For macOS ADE, see [macOS Admin](../admin-setup-macos/00-overview.md). For iOS/iPadOS, see [iOS Admin](../admin-setup-ios/00-overview.md).
> **Platform note:** AOSP management is a distinct surface from Android Enterprise (COBO/BYOD/Dedicated/ZTE) — no GMS, no FCM push, no Managed Google Play. See [Android enrollment overview](../android-lifecycle/00-enrollment-overview.md#aosp).
```

**3. HTML-comment subtractive deletions (between banner and H1) — D-29 inheritance from Phase 34 D-17 + 06-aosp-stub.md:15-19:**
```markdown
<!-- The #### In Managed Google Play subsection is intentionally omitted.
     AOSP does not use Managed Google Play (no Google Mobile Services). -->

<!-- The #### In Zero-Touch portal subsection is intentionally omitted.
     AOSP does not use the Zero-Touch portal; AOSP enrollment is QR-only, one device at a time. -->
```

**4. H1:** `# Configure RealWear AOSP Devices in Intune`

**5. ELEVEN H2s in fixed order per D-01 — emit each H2 with its `<a id="..."></a>` anchor immediately above per D-05:**

H2 #1 — `## Scope and Status` (PITFALL-7 framing inline at top per D-04 + D-23):
- Open with one paragraph stating: "RealWear HMT-1, HMT-1Z1, and Navigator 500 are supported under AOSP in Intune because they have no GMS. If GMS is present on a target device, use Android Enterprise fully managed (COBO) instead — these devices are not supported under AOSP when GMS is available." Append source marker `[HIGH: MS Learn AOSP supported devices, last_verified 2026-04-25]`.
- Add second paragraph noting RealWear devices are AR/VR headsets for hands-free frontline work (field service, maintenance, remote expert assistance).

H2 #2 — `## Hardware Scope` (verbatim table from RESEARCH.md §1 RealWear; PHASE-45-AOSP-SOURCE.md lines 26-32):
```markdown
<a id="hardware-scope"></a>
## Hardware Scope

| Device | Minimum Firmware | Notes |
|---|---|---|
| RealWear HMT-1 | 11.2+ | Primary frontline AR headset; hands-free voice control |
| RealWear HMT-1Z1 | 11.2+ | ATEX/IECEx-certified hazardous-environment variant of HMT-1 |
| RealWear Navigator 500 | 1.1+ | Next-gen head-mounted tablet; high-res camera for remote expert |

All three carry the same AOSP QR enrollment constraints — one device per QR, Wi-Fi credentials MUST be embedded.

`[HIGH: MS Learn AOSP supported devices, last_verified 2026-04-25]`
```

H2 #3 — `<a id="prerequisites"></a>` then `## Prerequisites and Licensing`:
- Bulleted list: Microsoft Intune Plan 2 OR Intune Suite (specialty AR/VR device requirement per MS Learn specialty-devices-with-intune); attach `[HIGH: MS Learn specialty-devices-with-intune, last_verified 2026-04-25]`.
- Entra ID tenant.
- Optional: RealWear Cloud account (coexists with Intune; Microsoft Teams for HMT distributed exclusively through RealWear Cloud) — attach `[HIGH: support.realwear.com/knowledge/faq-intune-aosp, last_verified 2026-04-25]`.
- Staging Wi-Fi network on-site MUST be WPA-PSK / WPA2-PSK / WPA3 (PSK-only) — close the bullet with: "EAP-PEAP and EAP-TLS are NOT supported for the staging Wi-Fi network. Corporate EAP networks must be configured via post-enrollment device-restrictions Wi-Fi profile." Attach `[HIGH: support.realwear.com/knowledge/enrolling-in-microsoft-intune, last_verified 2026-04-25]`.

H2 #4 — `## Enrollment Method`:
- One paragraph: "QR-only AOSP enrollment, one device at a time. Wi-Fi credentials MUST be embedded in the QR enrollment payload because RealWear devices cannot join Wi-Fi interactively during enrollment — the device must come up already network-connected to reach the Intune enrollment server on first boot." Attach `[HIGH: AEAOSPFULL-01 verbatim + RealWear authoritative]`.
- One paragraph noting BOTH user-associated AND userless enrollment modes are supported per RealWear FAQ; attach `[HIGH: support.realwear.com/knowledge/faq-intune-aosp, last_verified 2026-04-25]`.

H2 #5 — `<a id="provisioning-steps"></a>` then `## Provisioning Steps` with step-numbered H3 children per D-01:
- `### Step 1 — Create AOSP enrollment profile in Intune` — numbered list of 4-6 steps with `<!-- verify UI at execute time -->` at each portal navigation step; cite Intune admin center > Devices > Android > Enrollment > Corporate-owned dedicated devices > Token (or Corporate-owned, user-associated as appropriate); per-step `> **What breaks if misconfigured:** {what} Recovery: {action}. Symptom appears in: {surface}.` blockquote indented 4 spaces under preceding numbered list item.
- `### Step 2 — Configure Wi-Fi credentials embedded in the QR payload` — numbered list of 3-5 steps; cross-reference the `## Wi-Fi QR Embedding Walkthrough` H2 below for the full mechanics; emphasize PSK-only auth at the top of this step; include `> **What breaks if misconfigured:** Wi-Fi missing or wrong credentials → device hangs at "Connecting to Intune" with no error surface. Recovery: factory-reset, regenerate QR with correct PSK Wi-Fi, re-scan. Symptom appears in: device-side stall (no Intune-side telemetry).` blockquote.
- `### Step 3 — Generate the AOSP QR code` — numbered list of 2-4 steps; verify-UI HTML comment at portal navigation; `> **What breaks if misconfigured:** ...` blockquote.
- `### Step 4 — Provision the device by scanning the QR (one device per QR)` — numbered list of 3-5 device-side steps (skip first-time-setup; scan QR; observe enrollment progress); call out 15-minute typical enrollment window; `> **What breaks if misconfigured:** ...` blockquote.

H2 (REQUIRED add-on per D-02; positioned between H2 #5 and H2 #6) — `<a id="wi-fi-qr-embedding"></a>` then `## Wi-Fi QR Embedding Walkthrough`:
- Open with one-sentence framing of why Wi-Fi MUST be embedded (no text-input UI on RealWear).
- Numbered 5-item walkthrough following PHASE-45-AOSP-SOURCE.md outline (lines 39-46) but expanded to actual Intune UI mechanics:
  1. **Why Wi-Fi must be embedded (not interactive).** Cite RealWear's lack of text-input UI during enrollment.
  2. **Where in the Intune admin center to configure the QR with Wi-Fi.** Step-by-step UI path: Intune admin center > Devices > Android > Enrollment > Corporate-owned dedicated devices > Token > select profile > Wi-Fi configuration sub-pane. `<!-- verify UI at execute time -->` at the navigation. Note: the Wi-Fi block is part of the AOSP QR generator UI; not a separate Wi-Fi profile.
  3. **Supported Wi-Fi auth types for staging (RealWear-specific constraint).** Verbatim emphasis: "**WPA-PSK, WPA2-PSK, WPA3 ONLY — NOT EAP-PEAP, NOT EAP-TLS.**" Reference RealWear support: "the staging network MUST BE a WPA/WPA2 PSK/WPA3 network type, meaning there is an SSID and Password only." Attach `[HIGH: support.realwear.com/knowledge/enrolling-in-microsoft-intune, last_verified 2026-04-25]`. Note that corporate EAP networks must be deployed via post-enrollment device-restrictions Wi-Fi profile, not the staging QR.
  4. **Failure mode: Wi-Fi absent from QR.** Device hangs at "Connecting to Intune" — no error surface. Recovery: factory-reset, regenerate QR with Wi-Fi, re-scan.
  5. **Failure mode: wrong Wi-Fi credentials in QR (or non-PSK auth attempted).** Silent timeout; no retry UI. Recovery: same as above plus verify SSID + PSK at the AP.
- Two `> **What breaks if misconfigured:** ...` failure-mode blockquotes (one for Wi-Fi-missing, one for wrong-credentials) per the PHASE-45-AOSP-SOURCE.md two-failure-mode contract.

H2 #6 — `<a id="verification"></a>` then `## Verification`:
- Numbered list of 4-6 verification steps (Intune admin center Devices > All devices filter Android, search by serial; device shows "Enrolled" status; compliance state pulled within ~15 minutes of first boot; if hybrid with RealWear Cloud, device also appears in RealWear Cloud admin portal); `<!-- verify UI at execute time -->` at portal navigation steps.

H2 #7 — `<a id="common-failures"></a>` then `## Common Failures` (REQUIRED — Wave 3 runbook 29 Cause A cross-links here per D-21):
- Use a severity-descending table OR sub-H3 per failure mode (author's call per CONTEXT Claude's Discretion). Recommended: 5-7 row table with columns | Symptom | Likely Cause | Recovery | Severity |. Failure modes to include:
  - Device hangs at "Connecting to Intune" (Wi-Fi absent or non-PSK auth attempted) → CRITICAL
  - Device joins network but never appears in Intune (token expired or wrong AOSP enrollment profile) → HIGH
  - Device appears in Intune but not in RealWear Cloud (hybrid coexistence not configured; RealWear Cloud account missing) → MEDIUM
  - Microsoft Teams app missing on HMT (Teams distributed via RealWear Cloud only; admin must enable RealWear Cloud or use vendor app delivery) → MEDIUM
  - Userless token expired at 90 days (per RESEARCH.md §3 token expiry asymmetry; userless = 90d max; user-associated = up to 65y) → HIGH
- Include source markers `[HIGH:...]` or `[MEDIUM: RealWear support forum / community, last_verified 2026-04-25]` per per-row classification.

H2 #8 — `<a id="renewal-maintenance"></a>` then `## Renewal / Maintenance` (sibling parity per 07:193, 02:214, 03:218, 05:249):
```markdown
| Component | Renewal Period | Consequence of Lapse | Renewal Steps |
|---|---|---|---|
| Userless AOSP enrollment token | 90 days max (MS Learn-imposed ceiling for userless mode) | New userless enrollments fail; previously enrolled devices unaffected | Regenerate token via Intune admin center > Devices > Android > Enrollment > Corporate-owned dedicated devices > Token; redistribute QR to staging operators |
| User-associated AOSP enrollment token | Up to 65 years (configured at profile creation) | New user-associated enrollments fail | Regenerate via the same UI path; redistribute QR |
| Intune Plan 2 / Suite licensing | Per Microsoft Customer Agreement renewal cycle | New device enrollments blocked; existing enrolled devices retain compliance until license fully lapses | Renew via Microsoft 365 admin center > Billing |
| RealWear Cloud account (OPTIONAL coexistence) | RealWear-portal-perpetual once provisioned | Microsoft Teams app delivery via RealWear Cloud blocked; Intune-direct enrollment unaffected | Re-provision via support.realwear.com |
| Doc freshness | 60 days (Phase 34 D-14 / D-26) | Stale guidance risk | Re-fetch sources cited in this doc; update `last_verified` and `review_by` frontmatter |
```

H2 #9 — `<a id="what-breaks-summary"></a>` then `## What Breaks Summary` (sibling parity per 07:202, 03:197, 05:229):
```markdown
Inline what-breaks callouts at each decision point. Severity descending within each source section.

| Misconfiguration | Section | Severity |
|------------------|---------|----------|
| Wi-Fi credentials NOT embedded in AOSP QR | [Step 2](#provisioning-steps) | CRITICAL |
| Staging Wi-Fi configured with EAP-PEAP / EAP-TLS instead of PSK | [Wi-Fi QR Embedding Walkthrough](#wi-fi-qr-embedding) | CRITICAL |
| Userless token allowed to lapse beyond 90 days | [Renewal / Maintenance](#renewal-maintenance) | HIGH |
| AOSP enrollment profile assigned wrong device set | [Step 1](#provisioning-steps) | HIGH |
| Intune Plan 1 only (no AR/VR specialty entitlement) | [Prerequisites and Licensing](#prerequisites) | MEDIUM |
| RealWear Cloud account missing for Teams delivery | [Common Failures](#common-failures) | MEDIUM |
```

H2 #10 — `## See Also`:
- `[AOSP Stub](06-aosp-stub.md)` — AOSP scope context
- `[AOSP OEM Matrix](../reference/aosp-oem-matrix.md)` — cross-OEM AOSP capability mapping
- `[Android Capability Matrix](../reference/android-capability-matrix.md)` — overall Android mode capability landscape
- `[Android Glossary](../_glossary-android.md)` — AOSP / OEMConfig / GMS terminology
- `[Android Enrollment Overview](../android-lifecycle/00-enrollment-overview.md#aosp)` — placement of AOSP within the Android enrollment lifecycle

H2 #11 — `## Changelog`:
```markdown
| Date | Change | Author |
|------|--------|--------|
| 2026-04-25 | Initial version (Phase 45 AEAOSPFULL-01) — RealWear AOSP admin guide for HMT-1 / HMT-1Z1 / Navigator 500. PITFALL-7 framing inline per D-04 + D-23. REQUIRED `## Wi-Fi QR Embedding Walkthrough` add-on H2 per D-02 with PSK-only NOT-EAP staging-Wi-Fi discipline (RealWear authoritative). 11-H2 sibling parity per D-01. Anchor scaffolding per D-05. Frontmatter contract per D-27. Sourced from PHASE-45-AOSP-SOURCE.md (Phase 43 → Phase 45 lifecycle; deleted in Plan 10 final commit per Phase 43 D-20). | -- |
```

**6. PITFALL-7 framing per-claim discipline (D-04 + D-23) — apply throughout:**
Every "supported under AOSP" assertion must pair inline with the AOSP baseline caveat. The `## Scope and Status` opening paragraph carries the canonical statement. Repeat the framing at: the top of `## Hardware Scope`, the top of `## Common Failures` (one-line reminder), and the second paragraph of `## Enrollment Method`. Use the verbatim caveat phrase: "supported under AOSP because no GMS; if GMS is present, use Android Enterprise fully managed instead."

**7. Source-confidence markers per D-28:**
Use the exact regex format `[(HIGH|MEDIUM|LOW)(: source description)?(, last_verified YYYY-MM-DD)?]` at every assertion of fact. RESEARCH.md §1 RealWear table at lines 353-366 pre-classifies each fact's confidence — propagate verbatim.

**8. Self-check before commit (planner enforces — executor reads):**
- 11 H2s present in the fixed D-01 order (count `^## ` after H1; expect 11 + 1 add-on = 12).
- All anchor IDs present per D-05 (use grep `<a id="`).
- PITFALL-7 framing present in `## Scope and Status` first paragraph.
- "WPA" + "PSK" appears in `## Wi-Fi QR Embedding Walkthrough`; "EAP" appears with explicit "NOT" qualifier.
- No `Managed Google Play` cross-link anywhere (HTML-comment subtractive deletion preserved).
- Frontmatter values match D-27.
  </action>
  <verify>
    <automated>node scripts/validation/v1.4.1-milestone-audit.mjs &amp;&amp; test -f docs/admin-setup-android/09-aosp-realwear.md &amp;&amp; [ "$(grep -c '^## ' docs/admin-setup-android/09-aosp-realwear.md)" -ge 12 ] &amp;&amp; grep -q 'WPA-PSK\|WPA2-PSK\|WPA3' docs/admin-setup-android/09-aosp-realwear.md &amp;&amp; grep -q 'NOT EAP' docs/admin-setup-android/09-aosp-realwear.md &amp;&amp; grep -q 'supported under AOSP' docs/admin-setup-android/09-aosp-realwear.md &amp;&amp; grep -q '<a id="common-failures"></a>' docs/admin-setup-android/09-aosp-realwear.md &amp;&amp; grep -q '<a id="wi-fi-qr-embedding"></a>' docs/admin-setup-android/09-aosp-realwear.md &amp;&amp; grep -q 'last_verified: 2026-04-25' docs/admin-setup-android/09-aosp-realwear.md &amp;&amp; grep -q 'applies_to: AOSP' docs/admin-setup-android/09-aosp-realwear.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f docs/admin-setup-android/09-aosp-realwear.md`
    - 11-H2 baseline + 1 REQUIRED add-on H2: `grep -c '^## ' docs/admin-setup-android/09-aosp-realwear.md` returns >= 12
    - PSK-only staging-Wi-Fi assertion present: `grep -E 'WPA-PSK|WPA2-PSK|WPA3' docs/admin-setup-android/09-aosp-realwear.md` matches at least once
    - EAP exclusion explicit: `grep 'NOT EAP' docs/admin-setup-android/09-aosp-realwear.md` matches at least once
    - PITFALL-7 framing present: `grep 'supported under AOSP' docs/admin-setup-android/09-aosp-realwear.md` matches at least 4 times (Scope and Status, Hardware Scope, Enrollment Method, Common Failures per D-04 per-claim discipline)
    - Required anchors present: `grep -E '<a id="(prerequisites|provisioning-steps|verification|common-failures|renewal-maintenance|what-breaks-summary|wi-fi-qr-embedding)"></a>' docs/admin-setup-android/09-aosp-realwear.md` matches >= 7 times
    - Frontmatter contract per D-27: `grep 'applies_to: AOSP' docs/admin-setup-android/09-aosp-realwear.md` AND `grep 'audience: admin' docs/admin-setup-android/09-aosp-realwear.md` AND `grep 'last_verified: 2026-04-25' docs/admin-setup-android/09-aosp-realwear.md` AND `grep 'review_by: 2026-06-24' docs/admin-setup-android/09-aosp-realwear.md` all match
    - HTML-comment subtractive deletions preserved per D-29: `grep 'Managed Google Play subsection is intentionally omitted' docs/admin-setup-android/09-aosp-realwear.md` matches
    - Universal banner contract per D-03: `grep '> \*\*Platform gate:\*\*' docs/admin-setup-android/09-aosp-realwear.md` AND `grep '> \*\*Platform note:\*\*' docs/admin-setup-android/09-aosp-realwear.md` both match
    - Source-confidence markers per D-28: `grep -cE '\[HIGH(: [A-Za-z .,/+0-9-]+)?(, last_verified 2026-04-25)?\]' docs/admin-setup-android/09-aosp-realwear.md` returns >= 5
    - No Managed Google Play cross-link: `grep 'managed-google-play\|/managed-apps/' docs/admin-setup-android/09-aosp-realwear.md` returns 0 matches (excluding HTML-comment subtractive deletion line)
    - Audit harness PASS: `node scripts/validation/v1.4.1-milestone-audit.mjs` exits 0 (C1-C5 blocking; C6/C7 informational-first per D-29)
    - Shared-file modification guard per D-29: `git diff --name-only` shows ONLY `docs/admin-setup-android/09-aosp-realwear.md` as a new file (no other writes)
  </acceptance_criteria>
  <done>RealWear admin guide shipped at docs/admin-setup-android/09-aosp-realwear.md per AEAOSPFULL-01 with 11-H2 sibling parity + REQUIRED Wi-Fi QR Embedding Walkthrough add-on H2 + PSK-only-NOT-EAP discipline + PITFALL-7 framing per-claim discipline + anchor scaffolding for Wave 3 runbook 29 Cause A cross-link landing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only phase — no executable code, no user-input handling, no auth/session/crypto surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01-NA | N/A | docs/admin-setup-android/09-aosp-realwear.md | accept | Documentation-only phase. ASVS L1 requirements (V1-V14) are N/A — no attack surface beyond doc-supply-chain integrity (covered by git commit signing + repo access controls). Doc content does not introduce vulnerabilities; it describes existing Intune / MS Learn / RealWear admin paths. |
</threat_model>

<verification>
- `node scripts/validation/v1.4.1-milestone-audit.mjs` exits 0 with C1-C5 PASS (C6/C7 informational-first per D-29)
- `test -f docs/admin-setup-android/09-aosp-realwear.md`
- All `<acceptance_criteria>` for Task 1 pass
- `git diff --name-only` shows ONLY `docs/admin-setup-android/09-aosp-realwear.md` as a new file (D-29 shared-file guard)
</verification>

<success_criteria>
- AEAOSPFULL-01 closed: RealWear AOSP admin guide shipped with all locked decisions honored (D-01 11-H2 + D-02 add-on + D-03 banner + D-04/D-23 PITFALL-7 + D-05 anchors + D-26 freshness + D-27 frontmatter + D-29 file guard)
- File ready for Wave 3 runbook 29 Cause A cross-link landing at `#common-failures` and `#wi-fi-qr-embedding` anchors
- File ready for Wave 2 matrix to consume RealWear column data (Hardware Scope, Wi-Fi REQUIRED + PSK-only, vendor portal OPTIONAL, Intune Plan 2/Suite)
</success_criteria>

<output>
After completion, create `.planning/phases/45-per-oem-aosp-expansion/45-01-SUMMARY.md` per execute-plan template.
</output>
