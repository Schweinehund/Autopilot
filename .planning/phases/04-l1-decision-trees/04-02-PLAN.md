---
phase: 04-l1-decision-trees
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - docs/decision-trees/00-initial-triage.md
autonomous: true
requirements:
  - L1DT-01

must_haves:
  truths:
    - "The triage tree opens with a two-step network reachability gate before any scenario-specific branches"
    - "Every terminal node is one of: Resolved (green), Escalate L2 (red), Escalate Infrastructure (orange)"
    - "The triage tree routes to all 3 scenario trees via Mermaid click links"
    - "The triage tree includes catch-all branches for network failure, device not in portal, and OOBE crash/other"
    - "Deployment mode question appears after network gate as an early branch"
    - "No PowerShell, registry, or log references in any node"
  artifacts:
    - path: "docs/decision-trees/00-initial-triage.md"
      provides: "Hub triage entry point with legend, how-to-use, and scenario tree links"
      contains: "graph TD"
  key_links:
    - from: "docs/decision-trees/00-initial-triage.md"
      to: "docs/decision-trees/01-esp-failure.md"
      via: "click directive and scenario tree list"
      pattern: "01-esp-failure\\.md"
    - from: "docs/decision-trees/00-initial-triage.md"
      to: "docs/decision-trees/02-profile-assignment.md"
      via: "click directive and scenario tree list"
      pattern: "02-profile-assignment\\.md"
    - from: "docs/decision-trees/00-initial-triage.md"
      to: "docs/decision-trees/03-tpm-attestation.md"
      via: "click directive and scenario tree list"
      pattern: "03-tpm-attestation\\.md"
    - from: "docs/decision-trees/00-initial-triage.md"
      to: "docs/error-codes/00-index.md"
      via: "unknown error fallback link"
      pattern: "\\.\\.\/error-codes\/00-index\\.md"
---

<objective>
Create the initial triage decision tree hub document that serves as the single entry point for all L1 Autopilot troubleshooting.

Purpose: This is the L1 agent's starting point. It gates on network reachability, asks about deployment mode, routes to the correct scenario tree (ESP, Profile, TPM), and handles catch-all cases (device not in portal, OOBE crash, unknown errors). The hub-and-spoke model ensures agents always start from a consistent entry point.

Output: One Markdown file `docs/decision-trees/00-initial-triage.md` containing the triage flowchart with click links to all 3 scenario trees, plus the legend, "How to Use" section, and companion tables.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-l1-decision-trees/04-CONTEXT.md
@.planning/phases/04-l1-decision-trees/04-RESEARCH.md
@.planning/phases/04-l1-decision-trees/04-01-SUMMARY.md

@docs/decision-trees/01-esp-failure.md (link target — verify node IDs used)
@docs/decision-trees/02-profile-assignment.md (link target — verify node IDs used)
@docs/decision-trees/03-tpm-attestation.md (link target — verify node IDs used)
@docs/lifecycle/00-overview.md (Mermaid classDef + click pattern reference)
@docs/error-codes/00-index.md (unknown error fallback link target)
@docs/_glossary.md (first-mention glossary linking)
@docs/apv1-vs-apv2.md (APv2 note link target)

<interfaces>
<!-- Node IDs from Plan 01 scenario trees — triage must not reuse these -->
ESP tree uses: ES prefix (ESD1-ESD8, ESA1-ESA4, ESR1-ESR4, ESE1-ESE4)
Profile tree uses: PR prefix (PRD1-PRD7, PRA1-PRA2, PRR1-PRR3, PRE1-PRE6)
TPM tree uses: TP prefix (TPD1-TPD6, TPA1-TPA3, TPR1-TPR3, TPE1-TPE5)
Triage tree MUST use: TR prefix (TRD, TRA, TRR, TRE)

Frontmatter pattern:
```yaml
---
last_verified: 2026-03-20
review_by: 2026-06-18
applies_to: APv1
audience: L1
---
```

Terminal classDef (locked):
```
classDef resolved fill:#28a745,color:#fff
classDef escalateL2 fill:#dc3545,color:#fff
classDef escalateInfra fill:#fd7e14,color:#fff
```

Click syntax pattern (from lifecycle/00-overview.md):
```
click TRA1 "01-esp-failure.md"
```

Triage routing (locked in CONTEXT.md):
1. Network fails general gate -> Escalate: No connectivity
2. Network fails Autopilot endpoint gate -> Escalate: Firewall/proxy
3. Device not in Autopilot portal -> Check hash status, then Escalate L2
4. ESP stuck or error -> ESP tree (01-esp-failure.md)
5. No profile assigned -> Profile tree (02-profile-assignment.md)
6. TPM/provisioning error -> TPM tree (03-tpm-attestation.md)
7. OOBE crash/other -> Basic checks, Escalate L2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create initial triage decision tree hub (00-initial-triage.md)</name>
  <files>docs/decision-trees/00-initial-triage.md</files>
  <read_first>
    - docs/decision-trees/01-esp-failure.md (verify ES node IDs to avoid collision)
    - docs/decision-trees/02-profile-assignment.md (verify PR node IDs to avoid collision)
    - docs/decision-trees/03-tpm-attestation.md (verify TP node IDs to avoid collision)
    - docs/lifecycle/00-overview.md (click syntax and classDef pattern)
    - docs/error-codes/00-index.md (unknown error fallback link target)
    - docs/_glossary.md (terms for first-mention linking)
    - docs/apv1-vs-apv2.md (APv2 note link target)
    - .planning/phases/04-l1-decision-trees/04-CONTEXT.md (all locked decisions)
    - .planning/phases/04-l1-decision-trees/04-RESEARCH.md (patterns and pitfalls)
  </read_first>
  <action>
Create `docs/decision-trees/00-initial-triage.md` with this exact structure. This file has additional sections compared to scenario trees (per CONTEXT.md triage hub specification):

**1. YAML frontmatter:**
```yaml
---
last_verified: 2026-03-20
review_by: 2026-06-18
applies_to: APv1
audience: L1
---
```

**2. Version gate banner:**
```
> **Version gate:** This guide covers Windows Autopilot (classic). For Device Preparation (APv2), see [APv1 vs APv2 disambiguation](../apv1-vs-apv2.md).
```

**3. Title:** `# Initial Triage Decision Tree`

**4. "How to Use These Trees" paragraph** (2-3 sentences): Start here when a user reports an Autopilot deployment issue. Follow each decision point, answering the question shown. The tree will route you to a specific scenario tree or to an escalation point with data collection instructions.

**5. APv2 note:**
```markdown
> **Note:** These decision trees cover Autopilot (classic / APv1). For Device Preparation (APv2) issues, see [APv1 vs APv2 disambiguation](../apv1-vs-apv2.md).
```

**6. Legend table:**
| Symbol | Meaning |
|--------|---------|
| Diamond `{...}` | Decision — answer the question |
| Rectangle `[...]` | Action — perform this step |
| Green rounded `([...])` | Resolved — issue fixed |
| Red rounded `([...])` | Escalate to L2 — collect data and hand off |
| Orange rounded `([...])` | Escalate to Infrastructure/Network — collect data and hand off |

**7. Scenario Trees list** with links:
- [ESP Failure Tree](01-esp-failure.md) — Enrollment Status Page stuck or showing errors
- [Profile Assignment Tree](02-profile-assignment.md) — No profile assigned or wrong profile
- [TPM Attestation Tree](03-tpm-attestation.md) — TPM errors during pre-provisioning or self-deploying

**8. Mermaid `graph TD` decision tree** with TR-prefixed node IDs implementing the locked triage routing:

Network gate (first two nodes):
- TRD1{Can the device reach any website?}
  - No → TRE1([Escalate Infrastructure: No network connectivity])
  - Yes → TRD2{Can the device reach login.microsoftonline.com?}
    - No → TRE2([Escalate Infrastructure: Autopilot endpoints blocked by firewall or proxy])
    - Yes → TRD3

Deployment mode selection (after network gate):
- TRD3{What deployment mode is being used?}
  - User-Driven → TRD4
  - Pre-Provisioning → TRD4
  - Self-Deploying → TRD4
  - Don't know → TRD4 (same path — mode noted for escalation data)

Symptom identification:
- TRD4{Is the device registered in the Autopilot portal?}
  - No → TRA1[Check if hardware hash was imported] → TRE3([Escalate L2: Device not registered in Autopilot])
  - Yes → TRD5{What is the main symptom?}
    - ESP stuck or error → TRA2[Go to ESP Failure Tree] (click link to 01-esp-failure.md)
    - No profile assigned → TRA3[Go to Profile Assignment Tree] (click link to 02-profile-assignment.md)
    - TPM or provisioning error → TRA4[Go to TPM Attestation Tree] (click link to 03-tpm-attestation.md)
    - Error code on screen → TRD6{Can you find the error code in the error tables?}
      - Yes → TRR1([Resolved: Follow L1 Action column in error table]) — link to ../error-codes/00-index.md
      - No → TRE4([Escalate L2: Unknown error code])
    - OOBE crash or other → TRA5[Note symptoms and collect basic info] → TRE5([Escalate L2: OOBE failure — non-standard scenario])
    - Don't know → TRE6([Escalate L2: Unclear symptom — collect all available data])

Add `click` directives for routing nodes:
```
click TRA2 "01-esp-failure.md"
click TRA3 "02-profile-assignment.md"
click TRA4 "03-tpm-attestation.md"
```

classDef declarations at bottom, followed by class assignments. Every terminal node assigned to exactly one class.

Use short keyword edge labels. Use user/symptom language. No PowerShell, registry, or log references.

**9. How to Check table** keyed by decision node IDs:
| Node | Check | Where to Look |
Entries for:
- TRD1: "Open browser on the device; navigate to any public website (e.g., google.com)"
- TRD2: "In the same browser, navigate to https://login.microsoftonline.com — page should load without errors"
- TRD3: "Ask the user or check the deployment documentation for this device — user-driven requires user sign-in at OOBE, pre-provisioning is technician-initiated (Win key 5x), self-deploying starts automatically"
- TRD4: "Open Intune admin center > Devices > Windows > Windows devices > search by device serial number"
- TRD5: "Observe the device screen and ask the user what they see — match to the closest symptom category"
- TRD6: "Check [Master Error Code Index](../error-codes/00-index.md) — search for the hex code shown on screen"

**10. Escalation Data table** keyed by escalation terminal IDs:
| ID | Scenario | Collect | See Also |

Infrastructure escalations (TRE1, TRE2) collect: device IP/subnet, proxy configured (yes/no), which endpoint failed, browser error message, Wi-Fi or ethernet. See Also: Network team / infrastructure support.

L2 escalations (TRE3-TRE6) collect: device serial, error code (if any), deployment mode, timestamp, symptom description, screenshot. See Also: `[L2 Runbooks](../l2-runbooks/) (available after Phase 6)`.

**11. Resolution & Next Steps table** keyed by resolved terminal IDs:
| ID | Resolution | Next Steps |
TRR1 with forward-link to `[L1 Runbooks](../l1-runbooks/) (available after Phase 5)`.

**12. Navigation:**
```markdown
---
**Scenario Trees:**
- [ESP Failure Tree](01-esp-failure.md)
- [Profile Assignment Tree](02-profile-assignment.md)
- [TPM Attestation Tree](03-tpm-attestation.md)
```

**13. Version History table:**
| Date | Change | Author |
| 2026-03-20 | Initial version | — |

First-mention glossary linking in companion text sections for terms like [Autopilot](../_glossary.md#autopilot), [ESP](../_glossary.md#esp), [OOBE](../_glossary.md#oobe), [TPM](../_glossary.md#tpm). Do NOT put glossary links inside Mermaid nodes.
  </action>
  <verify>
    <automated>grep -c "graph TD" docs/decision-trees/00-initial-triage.md && grep "TRD1" docs/decision-trees/00-initial-triage.md | head -1 && grep "TRD2" docs/decision-trees/00-initial-triage.md | head -1 && grep "click TRA2" docs/decision-trees/00-initial-triage.md && grep "click TRA3" docs/decision-trees/00-initial-triage.md && grep "click TRA4" docs/decision-trees/00-initial-triage.md && grep "01-esp-failure.md" docs/decision-trees/00-initial-triage.md | head -1 && grep "02-profile-assignment.md" docs/decision-trees/00-initial-triage.md | head -1 && grep "03-tpm-attestation.md" docs/decision-trees/00-initial-triage.md | head -1 && grep "00-index.md" docs/decision-trees/00-initial-triage.md | head -1 && grep "classDef escalateInfra" docs/decision-trees/00-initial-triage.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/decision-trees/00-initial-triage.md exists
    - File contains `last_verified: 2026-03-20` and `audience: L1` in frontmatter
    - File contains `## How to Use These Trees` or equivalent "How to Use" section
    - File contains APv2 note linking to `../apv1-vs-apv2.md`
    - File contains a legend table with Symbol and Meaning columns
    - File contains scenario tree links list (all 3 scenario files)
    - File contains exactly one `graph TD` Mermaid block
    - File contains TRD1 (general internet check) as first decision diamond
    - File contains TRD2 (login.microsoftonline.com check) as second decision diamond
    - File contains TRD3 (deployment mode) after network gate
    - File contains TRD4 (device in portal check)
    - File contains TRD5 (symptom identification) with multi-option branches
    - File contains `click TRA2 "01-esp-failure.md"` directive
    - File contains `click TRA3 "02-profile-assignment.md"` directive
    - File contains `click TRA4 "03-tpm-attestation.md"` directive
    - File contains `classDef resolved fill:#28a745,color:#fff`
    - File contains `classDef escalateL2 fill:#dc3545,color:#fff`
    - File contains `classDef escalateInfra fill:#fd7e14,color:#fff`
    - File contains `## How to Check` table with entries for TRD1, TRD2
    - File contains `## Escalation Data` table with infrastructure and L2 entries
    - File contains `## Resolution` table
    - File contains `(available after Phase 5)` and `(available after Phase 6)` annotations
    - File contains link to `../error-codes/00-index.md` (unknown error fallback)
    - File contains glossary links `../_glossary.md`
    - File contains no PowerShell commands, no registry paths, no log file paths
    - All node IDs use TR prefix — no ES, PR, or TP prefixed IDs
    - File contains `## Version History` table
  </acceptance_criteria>
  <done>Triage hub document exists with two-step network gate, deployment mode branch, all 7 routing paths from CONTEXT.md, click links to all 3 scenario trees, legend table, How to Use section, APv2 note, and all companion tables. Node IDs are globally unique (TR prefix only, no collision with ES/PR/TP from Plan 01).</done>
</task>

</tasks>

<verification>
After task completes:
1. All 4 decision tree files exist in `docs/decision-trees/`
2. 00-initial-triage.md links to all 3 scenario trees via both click directives and markdown links
3. 00-initial-triage.md opens with network reachability gate (TRD1 → TRD2) before any scenario branches
4. Three and only three terminal categories across all 4 files (check classDef usage)
5. No node ID collisions across all files (TR, ES, PR, TP prefixes only in their respective files)
6. All click links point to files that exist
7. Unknown error fallback links to ../error-codes/00-index.md
</verification>

<success_criteria>
- Triage tree has network gate as first two decision nodes
- Deployment mode appears after network gate
- All 7 triage routing paths from CONTEXT.md are represented
- Click directives link to all 3 scenario tree files
- Legend table explains shapes and colors
- "How to Use" section provides clear instructions
- APv2 note present with link to apv1-vs-apv2.md
- No L1-unsafe content
</success_criteria>

<output>
After completion, create `.planning/phases/04-l1-decision-trees/04-02-SUMMARY.md`
</output>
