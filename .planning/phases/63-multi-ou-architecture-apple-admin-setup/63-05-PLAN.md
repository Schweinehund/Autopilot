---
phase: 63-multi-ou-architecture-apple-admin-setup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md
  - docs/reference/ios-capability-matrix.md
  - docs/cross-platform/apple-business/02-ous-architecture.md
autonomous: true
requirements: [OU-09, OU-10]
must_haves:
  truths:
    - "A pre-edit anchor inventory artifact is captured BEFORE any edit to the two existing files (PITFALL-6 / DA-4)"
    - "Exactly 3 new rows are appended to ios-capability-matrix.md under the existing Enrollment H2 (OU-09)"
    - "macos-capability-matrix.md and 4-platform-capability-comparison.md remain byte-unchanged (OU-10 / D-A3)"
    - "The stale 05-vpp-catalog-consolidation forward-reference in 02-ous-architecture.md is repaired to real Phase 63/64 targets (D-05)"
  artifacts:
    - path: ".planning/phases/63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md"
      provides: "Pre-edit anchor inventory with Pre-edit git SHA entries for the 2 edited files"
      contains: "Pre-edit git SHA"
    - path: "docs/reference/ios-capability-matrix.md"
      provides: "3 new Enrollment rows: Apple TV management / Shared iPad sessions / Apple Business delegation surface"
      contains: "Apple Business delegation surface"
    - path: "docs/cross-platform/apple-business/02-ous-architecture.md"
      provides: "Repaired forward-reference (no longer points to non-existent 05-vpp-catalog-consolidation)"
  key_links:
    - from: "docs/cross-platform/apple-business/02-ous-architecture.md"
      to: "docs/cross-platform/apple-business/11-vpp-catalog-runbook.md"
      via: "D-05 reconciled forward reference"
      pattern: "11-vpp-catalog-runbook.md"
---

<objective>
Perform the two surgical edits to existing files (OU-09 iOS matrix +3 rows; D-05 stale-ref repair in the frozen Phase 62 file `02-ous-architecture.md`), gated by a MANDATORY pre-edit anchor inventory, while enforcing the OU-10 byte-unchanged invariant on the two sibling matrices.

Purpose: Land the iOS capability-matrix delta and repair the dangling reference without shifting any existing anchor and without touching the byte-frozen sibling matrices (preserves C12 240-cell math + D-13/D-18 sibling-anchor-pin contract).
Output: `63-ANCHOR-INVENTORY.md`, edited `ios-capability-matrix.md` (+3 rows), edited `02-ous-architecture.md` (1 reference reconciled).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-CONTEXT.md
@.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-PATTERNS.md
@.planning/phases/62-apple-business-foundation-rebrand/62-ANCHOR-INVENTORY.md

<interfaces>
<!-- Concrete baselines captured at planning time (2026-05-21). Verify still current at execution. -->
BYTE-UNCHANGED GUARD (OU-10 / D-A3) — these git blob hashes MUST be identical post-phase:
  docs/reference/macos-capability-matrix.md          -> blob e91d7f9e001bb7ff4dc56a4ca98c84868fbf0716
  docs/reference/4-platform-capability-comparison.md -> blob f25ff51a14b7feac46611c4c0511ed5c074ce03f
  (recompute via `git hash-object <file>`; must match. Phase 63 must NOT modify either file.)

Files being edited — capture Pre-edit git SHA in the inventory:
  docs/reference/ios-capability-matrix.md            last-touched SHA 610b3bb91393b5175925994e4828f42de36fc601
  docs/cross-platform/apple-business/02-ous-architecture.md  last-touched SHA 74060b7ec665bb0d544236f4ea409988f099aa76

ios-capability-matrix.md Enrollment H2 table format (lines 15-17), 3-column Windows | macOS | iOS:
  | Feature | Windows | macOS | iOS |
  Append rows under this H2 BEFORE the `## Configuration` H2 at line 30.

D-05 stale ref in 02-ous-architecture.md (lines 102-105): currently
  "...consolidation runbook (`05-vpp-catalog-consolidation`, forthcoming) carries the OP-9 untouched-OU hard-bordered callout...This concept doc does not duplicate that callout — refer to Phase 63 for the operational procedure."
  Reconcile to: Phase 64 `11-vpp-catalog-runbook.md` (DELEG-01, owns OP-9 callout) and/or Phase 63 `07-vpp-content-token-consolidation.md` (OU-05). Keep the sentence "This concept doc does not duplicate that callout" intact. ONLY change lines 102-105 — no other line of the frozen file.

Anchor inventory artifact format: mirror 62-ANCHOR-INVENTORY.md (per-file "Pre-edit git SHA (last commit touching file): `<sha>`" + verbatim H2/H3 anchor list). check-phase-62.mjs:249 asserts >= 3 "Pre-edit git SHA" entries for Phase 62; check-phase-63.mjs (Plan 63-06) will assert >= 2 for the 2 edited files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture 63-ANCHOR-INVENTORY.md BEFORE any edit (PITFALL-6 / DA-4)</name>
  <files>.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md</files>
  <read_first>
    - .planning/phases/62-apple-business-foundation-rebrand/62-ANCHOR-INVENTORY.md (artifact format template)
    - docs/reference/ios-capability-matrix.md (inventory all H2/H3 anchors + the Enrollment table)
    - docs/cross-platform/apple-business/02-ous-architecture.md (inventory all H2/H3 anchors)
    - .planning/phases/63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md (target — does not exist yet)
  </read_first>
  <action>
    BEFORE editing any existing file, create `.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md` mirroring the 62-ANCHOR-INVENTORY.md format. Cover the 2 files receiving edits: `docs/reference/ios-capability-matrix.md` and `docs/cross-platform/apple-business/02-ous-architecture.md`. For each, record a `**Pre-edit git SHA (last commit touching file):** \`<sha>\`` line (use `git log -1 --format=%H -- <file>`) and the verbatim H2/H3 anchor list (with line numbers).
    Also record the byte-unchanged BASELINE for the two guard files using `git hash-object`: macos-capability-matrix.md and 4-platform-capability-comparison.md — note these MUST remain byte-identical (OU-10 / D-A3) and are NOT edited by Phase 63.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md','utf8'); const n=(c.match(/Pre-edit git SHA/gi)||[]).length; const ok=n>=2 && c.includes('ios-capability-matrix.md') && c.includes('02-ous-architecture.md') && c.includes('macos-capability-matrix.md') && c.includes('4-platform-capability-comparison.md'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `63-ANCHOR-INVENTORY.md` exists with >= 2 `Pre-edit git SHA` entries.
    - References both edited files (`ios-capability-matrix.md`, `02-ous-architecture.md`) with their pre-edit SHAs and verbatim anchor lists.
    - Records the byte-unchanged baseline (git blob hash) for both guard files (`macos-capability-matrix.md`, `4-platform-capability-comparison.md`).
    - This file is created BEFORE any edit in Tasks 2-3 (artifact-first ordering).
  </acceptance_criteria>
  <done>63-ANCHOR-INVENTORY.md captured with pre-edit SHAs + anchor lists for the 2 edited files and the byte-unchanged baseline for the 2 guard files.</done>
</task>

<task type="auto">
  <name>Task 2: Append exactly 3 rows to ios-capability-matrix.md Enrollment H2 (OU-09); preserve OU-10 guard</name>
  <files>docs/reference/ios-capability-matrix.md</files>
  <read_first>
    - docs/reference/ios-capability-matrix.md (Enrollment H2 table lines 13-29; row format line 15-17; cell value conventions)
    - 63-PATTERNS.md §ios-capability-matrix.md (3 row labels + value conventions)
    - .planning/phases/63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md (just-captured baseline)
  </read_first>
  <action>
    Append EXACTLY 3 new data rows to the Enrollment H2 table in `docs/reference/ios-capability-matrix.md` (insert before the `## Configuration` H2). Keep the existing 3-column `Windows | macOS | iOS` format and the established cell conventions (`🔒 supervised ADE only` / `N/A` / `Yes` / `No`). The 3 rows (Feature column labels):
    1. `Apple TV management` (tvOS via Apple Business / Configurator surface)
    2. `Shared iPad sessions`
    3. `Apple Business delegation surface` (OU-scoped admin)
    Do NOT add, remove, or reorder any existing row. Do NOT touch any other H2. Do NOT modify `docs/reference/macos-capability-matrix.md` or `docs/reference/4-platform-capability-comparison.md` (OU-10 / D-A3 byte-unchanged invariant). Do NOT shift existing anchors.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docs/reference/ios-capability-matrix.md','utf8').replace(/\r\n/g,'\n'); const m=c.split('## Configuration')[0]; const ok=m.includes('Apple TV management')&&m.includes('Shared iPad sessions')&&m.includes('Apple Business delegation surface'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `ios-capability-matrix.md` Enrollment H2 (text before `## Configuration`) contains the 3 new Feature labels: `Apple TV management`, `Shared iPad sessions`, `Apple Business delegation surface`.
    - Exactly 3 rows added (no more, no fewer); each row keeps the 3-column Windows | macOS | iOS shape.
    - `git hash-object docs/reference/macos-capability-matrix.md` == `e91d7f9e001bb7ff4dc56a4ca98c84868fbf0716` (byte-unchanged, OU-10).
    - `git hash-object docs/reference/4-platform-capability-comparison.md` == `f25ff51a14b7feac46611c4c0511ed5c074ce03f` (byte-unchanged, OU-10).
  </acceptance_criteria>
  <done>iOS capability matrix gains exactly 3 Enrollment rows; the two sibling matrices remain byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Repair D-05 stale 05-vpp-catalog-consolidation forward-reference in 02-ous-architecture.md (atomic)</name>
  <files>docs/cross-platform/apple-business/02-ous-architecture.md</files>
  <read_first>
    - docs/cross-platform/apple-business/02-ous-architecture.md (lines 97-105 Untouched-OU Trap section — the ONLY lines to change)
    - 63-PATTERNS.md §02-ous-architecture.md (D-05 replacement guidance)
    - .planning/phases/63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md (pre-edit SHA + anchors for this file)
  </read_first>
  <action>
    Make the SINGLE atomic edit to `docs/cross-platform/apple-business/02-ous-architecture.md` lines 102-105: replace the stale `05-vpp-catalog-consolidation` forward-reference with the reconciled real targets — Phase 64 `11-vpp-catalog-runbook.md` (DELEG-01, owns the operational OP-9 hard-bordered callout) and the Phase 63 admin-setup counterpart `07-vpp-content-token-consolidation.md` (OU-05). Keep the sentence "This concept doc does not duplicate that callout" intact (stable OP-9 deferral-contract anchor text). Change ONLY lines 102-105; do not modify any other line, heading, or anchor in this frozen Phase 62 file.
    Verify post-edit that the file's H2/H3 anchor list matches the 63-ANCHOR-INVENTORY.md baseline (zero anchor shift).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docs/cross-platform/apple-business/02-ous-architecture.md','utf8'); const ok=c.includes('05-vpp-catalog-consolidation')===false && c.includes('11-vpp-catalog-runbook.md') && /does not duplicate/i.test(c); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `02-ous-architecture.md` no longer contains the string `05-vpp-catalog-consolidation`.
    - Contains the reconciled reference `11-vpp-catalog-runbook.md` (and `07-vpp-content-token-consolidation.md`).
    - Retains the verbatim sentence `does not duplicate that callout`.
    - The file's H2/H3 anchor set is unchanged versus the 63-ANCHOR-INVENTORY.md baseline (no anchor shift — PITFALL-6).
    - Only the Untouched-OU Trap reference (lines 102-105) is changed.
  </acceptance_criteria>
  <done>The dangling 05-vpp-catalog-consolidation reference is reconciled to Phase 63/64 targets via a single atomic, anchor-stable edit.</done>
</task>

</tasks>

<threat_model>
Documentation deliverable — no runtime attack surface. Static markdown edits. The critical integrity surfaces are anti-regression invariants, not security: (1) OU-10 byte-unchanged guarantee on two sibling matrices (C12 240-cell math + D-13/D-18 sibling-anchor-pin) — mitigated by git-blob-hash assertions; (2) PITFALL-6 anchor stability on the frozen `02-ous-architecture.md` — mitigated by the mandatory pre-edit anchor inventory and post-edit anchor-set comparison; (3) the atomic single-reference D-05 edit avoids collateral changes. No external input, no executable code introduced.
</threat_model>

<verification>
- `git hash-object docs/reference/macos-capability-matrix.md` and `docs/reference/4-platform-capability-comparison.md` match the recorded baselines (OU-10).
- `node scripts/validation/v1.6-milestone-audit.mjs` exits 0 (no anchor regression in the harness's existing checks).
- All three Task verify commands exit 0.
</verification>

<success_criteria>
- OU-09 satisfied: exactly 3 Enrollment rows added to ios-capability-matrix.md with the pre-edit anchor inventory captured first.
- OU-10 satisfied: the two sibling matrices remain byte-identical to baseline.
- D-05 satisfied: stale forward-reference repaired atomically with zero anchor shift.
</success_criteria>

<output>
Create `.planning/phases/63-multi-ou-architecture-apple-admin-setup/63-05-SUMMARY.md` when done.
</output>
