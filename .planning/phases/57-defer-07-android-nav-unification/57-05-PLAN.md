---
phase: 57
plan: 57-05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l1-runbooks/00-index.md
files_created: []
requirements: [CLEAN-02]
autonomous: true

must_haves:
  truths:
    - "`docs/l1-runbooks/00-index.md` Android L1 Runbooks H2 table (currently lines 64-77) receives ONE additive table row for runbook 28 (Knox; D-21) inserted in numerical order BETWEEN row 27 (currently line 75) and row 29 (currently line 76)"
    - "New Knox row contains literal first-cell link `[28: Android Knox Enrollment Failed](28-android-knox-enrollment-failed.md)` matching existing row format pattern verified at lines 70-76"
    - "New Knox row Mode column literal is `Knox only` (D-21 + RESEARCH §2 verification — runbook 28 frontmatter has `applies_to: KME`; user-facing Mode column literal `Knox only` matches `BYOD only`/`ZTE only`/`AOSP only` display vocabulary at sibling rows; D-14 LOCKED tag vocabulary)"
    - "Intro line 66 staleness fix (RESEARCH §11 fold-into-D-21 recommendation): replace literal `**six most common**` with `**eight most common**` (Phase 44 added Knox runbook 28; Phase 45 added AOSP runbook 29; intro count was not updated). Original intro pattern: `L1 runbooks for the **six most common** Android Enterprise enrollment...` becomes `L1 runbooks for the **eight most common** Android Enterprise enrollment...`"
    - "After insertion, the Android L1 Runbooks table contains EXACTLY 8 rows (excluding header): runbook 22, 23, 24, 25, 26, 27, 28 (NEW), 29 — covers all 8 v1.4.1 scenario categories per CLEAN-02 cross-doc consistency with 57-02 common-issues.md H3 list and 57-03 quick-ref-l1.md Runbooks list"
    - "Existing rows for runbooks 22, 23, 24, 25, 26, 27, 29 UNCHANGED in literal text and Mode-column values (`All GMS modes`, `BYOD only`, `All GMS modes`, `All GMS modes`, `All GMS modes`, `ZTE only`, `AOSP only` per existing file inspection)"
    - "iOS L1 Runbooks H2 + table (lines 1-63) UNCHANGED — append-only contract within Android L1 H2 scope only"
    - "Linux L1 Runbooks H2 + table (lines 78+) UNCHANGED — Phase 51 deliverable preserved"
    - "Frontmatter `last_verified` updated to phase 57 atomic-commit-eligible date (2026-04-30 or commit-day equivalent); `review_by` set to `last_verified + 60 days` (D-32 step 6)"
    - "File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens outside Version History (V-57-26 scope is 4 hub files NOT this file per 57-06 validator scope; sanity check still applied)"
  artifacts:
    - path: "docs/l1-runbooks/00-index.md"
      provides: "Knox row added to Android L1 Runbooks index table (D-21 orthogonal patch supporting CLEAN-02 cross-doc consistency); intro count `six` → `eight` staleness fix per RESEARCH §11"
      contains:
        - "[28: Android Knox Enrollment Failed](28-android-knox-enrollment-failed.md)"
        - "Knox only"
        - "**eight most common**"
        - "## Android L1 Runbooks"
        - "[22: Android Enrollment Blocked](22-android-enrollment-blocked.md)"
        - "[29: Android AOSP Enrollment Failed](29-android-aosp-enrollment-failed.md)"
  key_links:
    - from: "docs/l1-runbooks/00-index.md (Android L1 Runbooks table)"
      to: "docs/l1-runbooks/28-android-knox-enrollment-failed.md"
      via: "New Knox row in numerical order between row 27 and row 29"
      pattern: "28-android-knox-enrollment-failed\\.md"
    - from: "docs/index.md (L1 sub-table row 3, authored by 57-01)"
      to: "docs/l1-runbooks/00-index.md#android-l1-runbooks"
      via: "Anchor link target — Android L1 Runbooks H2 (UNCHANGED literal; only table body modified)"
      pattern: "00-index\\.md#android-l1-runbooks"
---

<objective>
Patch `docs/l1-runbooks/00-index.md` Android L1 Runbooks H2 table to add the missing Knox row (runbook 28) — the CLEAN-02-supportive orthogonal patch surface per CONTEXT D-21 (GA-3 surfacing finding; gap real per RESEARCH §11 verification — runbook 28 file exists on disk but is missing from the index table; table currently lists 22-27 + 29 only).

CLEAN-02 enumerates "Knox enrollment failed" as one of the 8 v1.4.1 scenario categories Phase 57 must surface. For cross-doc consistency between common-issues.md (57-02 D-07 lists 8 H3s incl. Knox), quick-ref-l1.md (57-03 D-17 lists 8 runbooks incl. Knox), and the L1 index, the index table must include the Knox row. Same atomic commit per D-31.

Per CONTEXT D-21 + RESEARCH §2 verification: runbook 28 frontmatter `applies_to:` is literal `KME` (Samsung Knox Mobile Enrollment); user-facing Mode column display literal recommended `Knox only` to match `BYOD only` / `ZTE only` / `AOSP only` display vocabulary at sibling rows + D-14 LOCKED Mode-tag vocabulary. The runbook frontmatter `KME` is the source-of-truth scoping value, but the L1-index Mode column display literal is `Knox only` for cross-doc consistency.

Per RESEARCH §11 fold-into-D-21 recommendation: intro line 66 currently reads `L1 runbooks for the **six most common** Android Enterprise enrollment and compliance failure scenarios.` This is stale — Phase 44 added Knox runbook 28; Phase 45 added AOSP runbook 29; the intro count was not updated. Fold the staleness fix into 57-05 same-commit edit: replace `**six most common**` with `**eight most common**`.

Append-only contract within Android L1 H2 scope only: existing rows for runbooks 22, 23, 24, 25, 26, 27, 29 UNCHANGED. iOS L1 Runbooks H2 + table (lines 1-63) UNCHANGED. Linux L1 Runbooks H2 + table (lines 78+) UNCHANGED.

Purpose: Implements CONTEXT D-21 (Knox row additive insert in numerical order) + RESEARCH §11 fold-in (intro `six` → `eight` staleness fix). Validator V-57-24 will scan for `28-android-knox-enrollment-failed.md` link literal presence in the Android L1 Runbooks H2 region.

Wave 1 — parallel-safe with 57-01 / 57-02 / 57-03 / 57-04 / 57-06 (disjoint file sets; cross-links resolve at validator runtime when atomic commit lands per D-31).

Output: 1 modified file (`docs/l1-runbooks/00-index.md`).

DO NOT commit yet — atomic commit owned by 57-07 per CONTEXT D-31.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-defer-07-android-nav-unification/57-CONTEXT.md
@.planning/phases/57-defer-07-android-nav-unification/57-RESEARCH.md
@.planning/phases/57-defer-07-android-nav-unification/57-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
**Files this plan modifies:**
- MODIFY: `docs/l1-runbooks/00-index.md` — Knox row insertion (D-21) + intro count fix (RESEARCH §11) + frontmatter `last_verified` cycle (D-32 step 6)

**Existing table source (read for row format pattern; modify only Android L1 H2 region):**
- `docs/l1-runbooks/00-index.md:64-77` — Android L1 Runbooks H2 with existing 7-row table (22-27 + 29; Knox 28 MISSING). Existing row format: `| [NN: <Title-Case Name>](NN-android-slug.md) | <one-line scenario description> | <Mode column literal> |`. Existing Mode-column literals observed: `All GMS modes`, `BYOD only`, `ZTE only`, `AOSP only`.

**Knox runbook frontmatter source (read for Mode literal verification; DO NOT modify):**
- `docs/l1-runbooks/28-android-knox-enrollment-failed.md:1-7` — frontmatter has `applies_to: KME`, `audience: L1`, `platform: Android`. Body line 15: `**Applies to KME only (Samsung).**` Display Mode literal for L1-index = `Knox only` per D-21 + D-14 vocabulary.

**Phase 57 sibling plans (do NOT touch their files):**
- 57-01 owns `docs/index.md` + creates `57-ANCHOR-INVENTORY.md`
- 57-02 owns `docs/common-issues.md`
- 57-03 owns `docs/quick-ref-l1.md`
- 57-04 owns `docs/quick-ref-l2.md`
- 57-06 owns `scripts/validation/check-phase-57.mjs`
- 57-07 owns the atomic commit + VERIFICATION.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Knox row + fix intro staleness in Android L1 Runbooks H2 (D-21 + RESEARCH §11)</name>
  <files>docs/l1-runbooks/00-index.md</files>
  <read_first>
    - docs/l1-runbooks/00-index.md (lines 64-77: Android L1 Runbooks H2 — exact row format + Mode column vocabulary; line 66 intro text; lines 70-76 sibling rows for format mirror)
    - docs/l1-runbooks/28-android-knox-enrollment-failed.md (lines 1-7 frontmatter `applies_to: KME` + line 15 body context; verify Mode literal recommendation)
    - .planning/phases/57-defer-07-android-nav-unification/57-CONTEXT.md (D-21 verbatim; D-14 Mode tag vocabulary)
    - .planning/phases/57-defer-07-android-nav-unification/57-RESEARCH.md (§2 Knox runbook 28 frontmatter `applies_to: KME` + display Mode `Knox only`; §11 L1 index gap confirmation + intro `six` → `eight` recommendation)
    - .planning/phases/57-defer-07-android-nav-unification/57-PATTERNS.md (`docs/l1-runbooks/00-index.md` section: Existing table pattern at lines 64-76; Knox row format recommendation; Intro-line "six" → "eight" same-commit edit)
  </read_first>
  <action>
**Edit 1 — Intro line 66 staleness fix (RESEARCH §11 fold-into-D-21):**

Current text at line 66 (verified):
```
L1 runbooks for the **six most common** Android Enterprise enrollment and compliance failure scenarios. Start with the [Android Triage Decision Tree](../decision-trees/08-android-triage.md) to identify the failure mode, then follow the matching runbook below. All runbooks include L1-executable portal-only steps and explicit escalation triggers to L2.
```

Replace `**six most common**` with `**eight most common**`. The rest of the line (Triage Decision Tree link + L1-executable portal-only steps prose) is preserved verbatim.

Result:
```
L1 runbooks for the **eight most common** Android Enterprise enrollment and compliance failure scenarios. Start with the [Android Triage Decision Tree](../decision-trees/08-android-triage.md) to identify the failure mode, then follow the matching runbook below. All runbooks include L1-executable portal-only steps and explicit escalation triggers to L2.
```

**Edit 2 — Insert Knox row in numerical order between row 27 and row 29 (D-21 + PATTERNS recommended Knox row format):**

Current Android L1 Runbooks table (lines 68-76 verified):
```
| Runbook | Scenario | Applies To |
|---------|----------|------------|
| [22: Android Enrollment Blocked](22-android-enrollment-blocked.md) | Enrollment restriction / "device cannot enroll" error | All GMS modes |
| [23: Android Work Profile Not Created](23-android-work-profile-not-created.md) | Work profile container never created after BYOD enrollment | BYOD only |
| [24: Android Device Not Enrolled](24-android-device-not-enrolled.md) | Device never appeared in Intune (no restriction error) | All GMS modes |
| [25: Android Compliance Blocked](25-android-compliance-blocked.md) | Non-compliant / Conditional Access blocking M365 access | All GMS modes |
| [26: Android MGP App Not Installed](26-android-mgp-app-not-installed.md) | Managed Google Play app not delivered to device | All GMS modes |
| [27: Android ZTE Enrollment Failed](27-android-zte-enrollment-failed.md) | Zero-Touch Enrollment did not initiate or stalled | ZTE only |
| [29: Android AOSP Enrollment Failed](29-android-aosp-enrollment-failed.md) | AOSP enrollment did not initiate or stalled across 5 OEMs (RealWear / Zebra / Pico / HTC / Meta Quest) | AOSP only |
```

INSERT a new row BETWEEN the row 27 line and the row 29 line (numerical-order position):

```
| [28: Android Knox Enrollment Failed](28-android-knox-enrollment-failed.md) | Samsung KME provisioning failed (device booted to consumer setup, looped, or never arrived in Intune) | Knox only |
```

Row format MUST mirror existing rows verbatim:
- First cell: `| [NN: Android <Title-Case Name>](NN-android-slug.md) |` — bracketed runbook number + colon + space + Title-Case scenario name; relative-path filename link (no `../` prefix since the index is at same depth as runbook files).
- Second cell: `| <one-line scenario description> |` — past-tense / present-tense scenario; mirror sibling row brevity.
- Third cell: `| <Mode column literal> |` — `Knox only` per D-21 + D-14 + RESEARCH §2.

Resulting Android L1 Runbooks table will have 8 rows (22, 23, 24, 25, 26, 27, 28-NEW, 29) — covers all 8 v1.4.1 scenario categories per CLEAN-02 cross-doc consistency.

**Edit 3 — Frontmatter `last_verified` cycle (D-32 step 6):**

Update top-of-file frontmatter `last_verified:` to commit-day date and `review_by:` to `last_verified + 60 days`.

**Append-only contract enforcement:**
- DO NOT modify lines 1-63 (iOS L1 Runbooks H2 + table — Phase 32 deliverable)
- DO NOT modify line 64 (Android L1 Runbooks H2 literal `## Android L1 Runbooks`)
- DO NOT modify the existing 7 Android L1 Runbooks rows (22, 23, 24, 25, 26, 27, 29) — only insert the new Knox row
- DO NOT modify line 78+ (Linux L1 Runbooks H2 + table — Phase 51 deliverable)
- DO NOT add any other rows to the Android L1 Runbooks table beyond the Knox row

After insertion, append a Version History row at end of the file's `## Version History` table:
```
| 2026-04-30 | Phase 57: added Knox runbook 28 row to Android L1 Runbooks table (orthogonal patch supporting CLEAN-02 cross-doc consistency; runbook 28 file existed on disk since Phase 44 but was missing from the index); fixed stale `six most common` → `eight most common` intro count (Phase 44 + Phase 45 added Knox + AOSP; intro count was never updated) | -- |
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/l1-runbooks/00-index.md','utf8');const checks=[c.includes('[28: Android Knox Enrollment Failed](28-android-knox-enrollment-failed.md)'),c.includes('| Knox only |'),c.includes('**eight most common**'),!c.includes('**six most common**'),c.includes('## Android L1 Runbooks'),c.includes('[22: Android Enrollment Blocked](22-android-enrollment-blocked.md)'),c.includes('[27: Android ZTE Enrollment Failed](27-android-zte-enrollment-failed.md)'),c.includes('[29: Android AOSP Enrollment Failed](29-android-aosp-enrollment-failed.md)')];const failed=checks.map((v,i)=>v?null:i+1).filter(Boolean);if(failed.length){console.error('Failed checks:',failed);process.exit(1)}console.log('All',checks.length,'checks pass');process.exit(0);"</automated>
  </verify>
  <done>docs/l1-runbooks/00-index.md Android L1 Runbooks table has 8 rows including the new Knox row 28 between rows 27 and 29; intro `six most common` literal replaced with `eight most common`; existing 7 Android rows + iOS H2 + Linux H2 UNCHANGED; node check exits 0.</done>
</task>

</tasks>

<verification>
- New Knox row `[28: Android Knox Enrollment Failed](28-android-knox-enrollment-failed.md) | Samsung KME provisioning failed... | Knox only |` present in Android L1 Runbooks table
- Knox row inserted in numerical order BETWEEN row 27 (ZTE) and row 29 (AOSP)
- Intro line 66 `**six most common**` replaced with `**eight most common**`
- Existing 7 Android rows (22, 23, 24, 25, 26, 27, 29) UNCHANGED in text and Mode-column values
- iOS L1 Runbooks H2 + table (lines 1-63) UNCHANGED
- Linux L1 Runbooks H2 + table UNCHANGED
- Frontmatter `last_verified` updated; `review_by` is `last_verified + 60 days`
- V-57-24 validator (authored by 57-06) will detect `28-android-knox-enrollment-failed.md` link literal in Android L1 Runbooks H2 region
</verification>

<success_criteria>
- ROADMAP:326 SC#2 cross-doc consistency closed: Knox runbook 28 surfaced in L1 index AND common-issues.md (57-02) AND quick-ref-l1.md (57-03) — all 8 v1.4.1 scenario categories navigable from a coherent runbook surface
- D-21 orthogonal patch closed (CLEAN-02-supportive)
- RESEARCH §11 staleness fix folded into same atomic commit
- 57-07 atomic commit can include this file's diff
</success_criteria>

<output>
After completion, create `.planning/phases/57-defer-07-android-nav-unification/57-05-SUMMARY.md` documenting:
1. The Knox row added with literal text + Mode column value `Knox only`
2. The intro count fix (`six most common` → `eight most common`)
3. Confirmation that 7 existing Android rows + iOS H2 + Linux H2 UNCHANGED
4. Frontmatter update (last_verified + review_by 60-day cycle)
5. DPO-Phase57-05 propagation note: "Plan 57-05 added Knox row + intro fix to L1 index (D-21 + RESEARCH §11); cross-doc consistency with 57-02/57-03 Knox surfacing achieved; ready for atomic commit per D-31"
</output>
