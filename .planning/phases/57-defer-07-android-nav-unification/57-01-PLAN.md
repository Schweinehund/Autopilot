---
phase: 57
plan: 57-01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.md
files_created:
  - .planning/phases/57-defer-07-android-nav-unification/57-ANCHOR-INVENTORY.md
requirements: [CLEAN-01]
autonomous: true

must_haves:
  truths:
    - "Pre-edit anchor inventory artifact `.planning/phases/57-defer-07-android-nav-unification/57-ANCHOR-INVENTORY.md` exists BEFORE any docs/index.md edit (PITFALL-6 + D-32 step 5) and captures grep output for `index.md#`, `common-issues.md#`, `quick-ref-l1.md#`, `quick-ref-l2.md#` patterns across `docs/`"
    - "`docs/index.md` H2 `## Android Enterprise Provisioning` (line 167 stub) expands to contain THREE sub-H3s: `### Service Desk (L1)` AND `### Desktop Engineering (L2)` AND `### Admin Setup` (D-01)"
    - "Existing intro paragraph at line 169 (`Troubleshooting, investigation, and setup guides for Android Enterprise...`) PRESERVED verbatim — sub-H3 blocks INSERT between line 169 and the `---` separator at line 171"
    - "L1 sub-table contains exactly 4 rows (excluding header) with literal first-cell links: `[Android Provisioning Lifecycle](android-lifecycle/00-enrollment-overview.md)`, `[Android Triage Decision Tree](decision-trees/08-android-triage.md)`, `[Android L1 Runbooks](l1-runbooks/00-index.md#android-l1-runbooks)`, `[L1 Quick-Reference Card](quick-ref-l1.md#android-enterprise-quick-reference)` (D-02)"
    - "L2 sub-table contains exactly 4 rows (excluding header) with literal first-cell links: `[Android Provisioning Lifecycle](android-lifecycle/00-enrollment-overview.md)`, `[Android Log Collection Guide](l2-runbooks/18-android-log-collection.md)`, `[Android L2 Runbooks](l2-runbooks/00-index.md#android-l2-runbooks)`, `[L2 Quick-Reference Card](quick-ref-l2.md#android-enterprise-quick-reference)` (D-03)"
    - "Admin Setup sub-table contains exactly 3 rows (excluding header) with literal first-cell links: `[Android Admin Setup Overview](admin-setup-android/00-overview.md)`, `[Android Provisioning Lifecycle](android-lifecycle/00-enrollment-overview.md)`, `[Android Capability Matrix](reference/android-capability-matrix.md)` (D-04 — single-overview-link discipline; NO per-mode admin-setup-android enumeration; NO ` + ` grouped cells)"
    - "Cross-Platform References table (currently lines 173-191) has TWO new rows added paralleling iOS lines 190-191 pattern: `[Android Provisioning Lifecycle](android-lifecycle/00-enrollment-overview.md)` AND `[Android Capability Matrix](reference/android-capability-matrix.md)`. Existing `_glossary-android.md` row at line 179 UNCHANGED (D-05)"
    - "iOS H2 `## iOS/iPadOS Provisioning` (line 131) UNCHANGED in literal text and ordering — append-only contract (V-57-25 anchor stability NEGATIVE regression-guard)"
    - "Frontmatter `last_verified` updated to phase 57 atomic-commit-eligible date (2026-04-30 or commit-day equivalent); `review_by` set to `last_verified + 60 days` (D-32 step 6)"
    - "NO `## Operations` H2 added (Phase 59 ownership per ROADMAP:354 — D-01 Reject 1D rationale); NO per-mode `admin-setup-android/01..13` enumeration in any sub-table (D-01 Reject 1C rationale)"
    - "Post-edit anchor inventory re-grep produces zero NEW broken anchor references vs pre-edit baseline captured in `57-ANCHOR-INVENTORY.md`"
    - "File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens outside Version History (V-57-26)"
  artifacts:
    - path: ".planning/phases/57-defer-07-android-nav-unification/57-ANCHOR-INVENTORY.md"
      provides: "Pre-edit anchor inventory baseline (PITFALL-6 + D-32 step 5) for VERIFICATION.md cross-check"
      contains:
        - "index.md#"
        - "common-issues.md#"
        - "quick-ref-l1.md#"
        - "quick-ref-l2.md#"
    - path: "docs/index.md"
      provides: "Android Enterprise H2 expansion with 3 sub-tables (L1/L2/Admin) matching iOS Phase 32 structural depth (CLEAN-01 PRIMARY)"
      contains:
        - "## Android Enterprise Provisioning"
        - "### Service Desk (L1)"
        - "### Desktop Engineering (L2)"
        - "### Admin Setup"
        - "android-lifecycle/00-enrollment-overview.md"
        - "decision-trees/08-android-triage.md"
        - "l1-runbooks/00-index.md#android-l1-runbooks"
        - "quick-ref-l1.md#android-enterprise-quick-reference"
        - "l2-runbooks/18-android-log-collection.md"
        - "l2-runbooks/00-index.md#android-l2-runbooks"
        - "quick-ref-l2.md#android-enterprise-quick-reference"
        - "admin-setup-android/00-overview.md"
        - "reference/android-capability-matrix.md"
  key_links:
    - from: "docs/index.md (Service Desk L1 sub-table)"
      to: "docs/quick-ref-l1.md (Android Enterprise Quick Reference H2)"
      via: "L1 sub-table row 4 anchor link"
      pattern: "quick-ref-l1\\.md#android-enterprise-quick-reference"
    - from: "docs/index.md (Desktop Engineering L2 sub-table)"
      to: "docs/quick-ref-l2.md (Android Enterprise Quick Reference H2)"
      via: "L2 sub-table row 4 anchor link"
      pattern: "quick-ref-l2\\.md#android-enterprise-quick-reference"
    - from: "docs/index.md (Cross-Platform References)"
      to: "docs/android-lifecycle/00-enrollment-overview.md AND docs/reference/android-capability-matrix.md"
      via: "Two new Cross-Platform References rows per D-05"
      pattern: "android-lifecycle/00-enrollment-overview\\.md|reference/android-capability-matrix\\.md"
---

<objective>
Author the `docs/index.md` Android Enterprise H2 expansion — the CLEAN-01 PRIMARY surface per CONTEXT D-01..05 (GA-1 winner 1A STRONG; pure iOS Phase 32 mirror; reject 1B/1C/1D per Adversary-Referee adjudication). Expands the current line-167-170 stub into THREE sub-tables (Service Desk L1 / Desktop Engineering L2 / Admin Setup) matching iOS Phase 32 structural depth at lines 131-164 verbatim. Adds two new Cross-Platform References entries (Android Provisioning Lifecycle + Android Capability Matrix) paralleling iOS lines 190-191.

Plan 57-01 also owns the MANDATORY pre-edit anchor inventory (PITFALL-6 + D-32 step 5) — first task creates `57-ANCHOR-INVENTORY.md` BEFORE any edit to `docs/index.md`. This is the auditable phase artifact captured for VERIFICATION.md cross-check.

Append-only H2 contract: Android H2 stays in current ordering position (after iOS, before Cross-Platform References). Existing iOS/macOS/Windows H2s, sub-H3s, and anchors UNCHANGED. Existing intro paragraph at line 169 PRESERVED verbatim — sub-H3 blocks INSERT between line 169 and `---` at line 171.

Per CONTEXT D-01 firewall: NO operations cross-references (Phase 59 owns operations-hub-nav per ROADMAP:354 — Reject 1D); NO admin-setup-android per-mode enumeration at hub level (Reject 1C 2x-penalty); NO novel "Operations" fourth sub-section (Reject 1D zero-precedent).

Purpose: Implements CONTEXT D-01 (3 sub-tables matching iOS Phase 32) + D-02 (L1=4 rows verbatim from D-02 list) + D-03 (L2=4 rows verbatim from D-03 list, deviates from iOS-5-rows because Android has only one lifecycle file `00-enrollment-overview.md`) + D-04 (Admin=3 rows, single-overview-link discipline mirroring macOS:121-127, NOT iOS-7-grouped) + D-05 (Cross-Platform References two new rows paralleling iOS lines 190-191).

Wave 1 — parallel-safe with 57-02 / 57-03 / 57-04 / 57-05 / 57-06 (no inter-file authoring dependency; disjoint file sets; all sibling cross-links resolve at validator runtime when full atomic commit lands per D-31 owned by 57-07).

Output: 1 modified file (`docs/index.md`) + 1 new artifact (`.planning/phases/57-defer-07-android-nav-unification/57-ANCHOR-INVENTORY.md`).

DO NOT commit yet — atomic commit owned by 57-07 per CONTEXT D-31 (single atomic commit covers 4 hub edits + L1 index Knox row + validator).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-defer-07-android-nav-unification/57-CONTEXT.md
@.planning/phases/57-defer-07-android-nav-unification/57-RESEARCH.md
@.planning/phases/57-defer-07-android-nav-unification/57-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
**Files this plan creates / modifies:**
- CREATE: `.planning/phases/57-defer-07-android-nav-unification/57-ANCHOR-INVENTORY.md` (pre-edit baseline; PITFALL-6 + D-32 step 5)
- MODIFY: `docs/index.md` — Android Enterprise H2 expansion (D-01..05) + Cross-Platform References two new rows (D-05) + frontmatter `last_verified` / `review_by` 60-day cycle update (D-32 step 6)

**iOS Phase 32 mirror source (read for analog patterns; DO NOT modify):**
- `docs/index.md:131-164` — iOS Provisioning H2 with 3 sub-tables (L1=4 rows / L2=5 rows / Admin=7 rows) — full mirror source
- `docs/index.md:121-127` — macOS Admin Setup single-overview-link analog (D-04 source for 3-row Android Admin pattern)
- `docs/index.md:173-191` — Cross-Platform References table; iOS pair at lines 190-191 is D-05 mirror

**Phase 57 sibling plans (do NOT touch their files):**
- 57-02 owns `docs/common-issues.md`
- 57-03 owns `docs/quick-ref-l1.md`
- 57-04 owns `docs/quick-ref-l2.md`
- 57-05 owns `docs/l1-runbooks/00-index.md`
- 57-06 owns `scripts/validation/check-phase-57.mjs`
- 57-07 owns the atomic commit + VERIFICATION.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-edit anchor inventory (PITFALL-6 + D-32 step 5)</name>
  <files>.planning/phases/57-defer-07-android-nav-unification/57-ANCHOR-INVENTORY.md</files>
  <read_first>
    - .planning/phases/57-defer-07-android-nav-unification/57-CONTEXT.md (D-32 step 5; PITFALL-6 mandate; "auditable phase output, not in-memory check")
    - .planning/phases/57-defer-07-android-nav-unification/57-PATTERNS.md (Append-only H2 contract; Hardcoded anchor pinning patterns)
  </read_first>
  <action>
Create `.planning/phases/57-defer-07-android-nav-unification/57-ANCHOR-INVENTORY.md` BEFORE editing `docs/index.md`.

Run these four greps from repo root (`D:\claude\Autopilot`) and capture all output verbatim into the inventory artifact:

```
grep -rn "index.md#" docs/
grep -rn "common-issues.md#" docs/
grep -rn "quick-ref-l1.md#" docs/
grep -rn "quick-ref-l2.md#" docs/
```

Inventory file structure:

```markdown
# Phase 57 Pre-Edit Anchor Inventory

**Captured:** {COMMIT-DAY-DATE}
**Purpose:** PITFALL-6 + D-32 step 5 baseline for append-only contract verification across 4 hub files.
**Owner:** Plan 57-01 (per D-34 plan ordering — pre-edit MANDATORY before any docs/index.md edit).

## index.md# references

```
{verbatim grep output for `grep -rn "index.md#" docs/`}
```

## common-issues.md# references

```
{verbatim grep output for `grep -rn "common-issues.md#" docs/`}
```

## quick-ref-l1.md# references

```
{verbatim grep output for `grep -rn "quick-ref-l1.md#" docs/`}
```

## quick-ref-l2.md# references

```
{verbatim grep output for `grep -rn "quick-ref-l2.md#" docs/`}
```

## iOS H2 Anchor Stability Baseline (V-57-25 NEGATIVE regression-guard)

Per RESEARCH §3, the 4 iOS H2 literals locked at Phase 32 close (2026-04-17):

| File | Line | Literal H2 | GFM-derived Anchor |
|------|------|-----------|---------------------|
| docs/index.md | 131 | `## iOS/iPadOS Provisioning` | `#iosipados-provisioning` |
| docs/common-issues.md | 212 | `## iOS/iPadOS Failure Scenarios` | `#iosipados-failure-scenarios` |
| docs/quick-ref-l1.md | 117 | `## iOS/iPadOS Quick Reference` | `#iosipados-quick-reference` |
| docs/quick-ref-l2.md | 182 | `## iOS/iPadOS Quick Reference` | `#iosipados-quick-reference` |

Post-Phase-57 expectation: all 4 literals UNCHANGED; V-57-25 enforces.
```

This artifact persists throughout Phase 57 execution; it is referenced from VERIFICATION.md (per 57-07 close gate).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/57-defer-07-android-nav-unification/57-ANCHOR-INVENTORY.md';if(!fs.existsSync(p))process.exit(1);const c=fs.readFileSync(p,'utf8');const ok=c.includes('index.md#')&&c.includes('common-issues.md#')&&c.includes('quick-ref-l1.md#')&&c.includes('quick-ref-l2.md#')&&c.includes('## iOS/iPadOS Provisioning');process.exit(ok?0:1);"</automated>
  </verify>
  <done>57-ANCHOR-INVENTORY.md exists at the canonical path; contains four grep-output sections (one per hub file pattern) and the iOS H2 stability baseline table; node check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Android Enterprise H2 in docs/index.md (D-01..05)</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md (entire file; specifically lines 131-164 iOS H2 sub-tables for D-02/D-03/D-04 mirror; lines 121-127 macOS Admin single-overview pattern for D-04; lines 173-191 Cross-Platform References for D-05; lines 167-170 current Android stub PRESERVED)
    - .planning/phases/57-defer-07-android-nav-unification/57-CONTEXT.md (D-01..05 verbatim row content)
    - .planning/phases/57-defer-07-android-nav-unification/57-PATTERNS.md (`docs/index.md` section: row format `| [Anchor Text](relative-path[#fragment]) | One-line annotation |`; Cross-Platform References pattern at lines 173-191)
    - .planning/phases/57-defer-07-android-nav-unification/57-ANCHOR-INVENTORY.md (pre-edit baseline; verify no docs/index.md# anchor references will break)
  </read_first>
  <action>
Edit `docs/index.md`. Two edit zones: (A) expand the existing Android H2 stub at line 167; (B) add two new rows to Cross-Platform References. Frontmatter `last_verified` + `review_by` updated to commit-day cycle.

**Edit Zone A — Android Enterprise H2 expansion (between line 169 intro and line 171 `---` separator):**

PRESERVE line 167 H2 verbatim: `## Android Enterprise Provisioning`

PRESERVE line 169 intro paragraph verbatim:
```
Troubleshooting, investigation, and setup guides for Android Enterprise provisioning through Microsoft Intune. For terminology, see the [Android Enterprise Provisioning Glossary](_glossary-android.md). For enrollment paths, see the [Android Provisioning Lifecycle](android-lifecycle/00-enrollment-overview.md).
```

INSERT after line 169 (blank line, then 3 sub-H3 blocks, then blank line before `---` at line 171):

```markdown

### Service Desk (L1)

| Resource | When to Use |
|----------|-------------|
| [Android Provisioning Lifecycle](android-lifecycle/00-enrollment-overview.md) | Start here -- understand the Android enrollment paths (BYOD / COBO / Dedicated / ZTE / AOSP) and DPC modes |
| [Android Triage Decision Tree](decision-trees/08-android-triage.md) | Identifies the Android failure scenario from symptoms and routes to the correct runbook (mode-first per Phase 40 D-01) |
| [Android L1 Runbooks](l1-runbooks/00-index.md#android-l1-runbooks) | Scripted procedures for the 8 Android Enterprise enrollment + compliance failure scenarios (runbooks 22-29) |
| [L1 Quick-Reference Card](quick-ref-l1.md#android-enterprise-quick-reference) | One-page cheat sheet -- top checks with mode tags, escalation triggers, decision tree, and runbook list |

### Desktop Engineering (L2)

| Resource | When to Use |
|----------|-------------|
| [Android Provisioning Lifecycle](android-lifecycle/00-enrollment-overview.md) | Review the Android enrollment paths before diagnosing |
| [Android Log Collection Guide](l2-runbooks/18-android-log-collection.md) | Prerequisite for all Android L2 investigations (3-method: Company Portal / Microsoft Intune App / adb logcat) |
| [Android L2 Runbooks](l2-runbooks/00-index.md#android-l2-runbooks) | Investigation guides for enrollment, app install, compliance, Knox, and AOSP failures (runbooks 18-23) |
| [L2 Quick-Reference Card](quick-ref-l2.md#android-enterprise-quick-reference) | One-page cheat sheet -- 3-method log collection, Intune portal paths, Play Integrity verdict reference, investigation runbook list |

### Admin Setup

| Resource | When to Use |
|----------|-------------|
| [Android Admin Setup Overview](admin-setup-android/00-overview.md) | Entry point for all Android admin setup guides; tri-portal Mermaid diagram + per-mode setup-sequence enumeration lives at this overview, not at hub level |
| [Android Provisioning Lifecycle](android-lifecycle/00-enrollment-overview.md) | Review the enrollment pipeline before configuring Intune + MGP (admin-context entry) |
| [Android Capability Matrix](reference/android-capability-matrix.md) | Compare Android feature parity vs Windows, macOS, iOS -- scannable 5-domain table |
```

Note: row content is VERBATIM from CONTEXT.md D-02 / D-03 / D-04. Use ` -- ` (TWO spaces around two dashes) for annotations consistent with iOS lines 139-163 pattern (PATTERNS.md "iOS-style ` -- ` disambiguation separator" shared pattern).

**Edit Zone B — Cross-Platform References two new rows (D-05; insert after iOS lines 190-191, before `## Version History`):**

After existing line 191 (`| [iOS Capability Matrix](reference/ios-capability-matrix.md) | ... (NAV-03) |`), INSERT TWO new rows in iOS-pair-mirror order:

```markdown
| [Android Provisioning Lifecycle](android-lifecycle/00-enrollment-overview.md) | Mode comparison with DPC ownership axis (BYOD / COBO / Dedicated / ZTE / AOSP) and Managed Google Play binding |
| [Android Capability Matrix](reference/android-capability-matrix.md) | Intune feature parity comparison across Windows, macOS, iOS, and Android |
```

Existing `[Android Enterprise Provisioning Glossary]` row at line 179 UNCHANGED.

**Edit Zone C — Frontmatter (per D-32 step 6, last_verified 60-day cycle):**

Update top-of-file frontmatter `last_verified:` to `2026-04-30` (or commit-day equivalent if executor commits later) and `review_by:` to `last_verified + 60 days`. If frontmatter does not currently exist or fields differ, add them per existing hub-doc convention (see `docs/common-issues.md:1-7` for example shape).

**Append-only contract enforcement:**
- DO NOT modify lines 1-130 (Windows + macOS H2s + sub-H3s)
- DO NOT modify lines 131-164 (iOS H2 + sub-tables) — V-57-25 anchor stability NEGATIVE regression-guard
- DO NOT modify lines 173-191 except to ADD two new rows after line 191 (D-05)
- DO NOT add a `## Operations` H2 anywhere (Phase 59 ownership per ROADMAP:354)
- DO NOT enumerate `admin-setup-android/01..13` per-mode files at hub level (D-01 Reject 1C 2x-penalty rationale)

After edit, append a Version History row at end of `## Version History` table (line 195+):
```
| 2026-04-30 | Phase 57: expanded Android Enterprise H2 to 3 sub-tables (L1=4 rows / L2=4 rows / Admin=3 rows) matching iOS Phase 32 structural depth (CLEAN-01); added Android Provisioning Lifecycle + Android Capability Matrix Cross-Platform References entries (DEFER-07 close) | -- |
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/index.md','utf8');const checks=[c.includes('## Android Enterprise Provisioning'),c.includes('### Service Desk (L1)'),c.includes('### Desktop Engineering (L2)'),c.includes('### Admin Setup'),c.includes('android-lifecycle/00-enrollment-overview.md'),c.includes('decision-trees/08-android-triage.md'),c.includes('l1-runbooks/00-index.md#android-l1-runbooks'),c.includes('quick-ref-l1.md#android-enterprise-quick-reference'),c.includes('l2-runbooks/18-android-log-collection.md'),c.includes('l2-runbooks/00-index.md#android-l2-runbooks'),c.includes('quick-ref-l2.md#android-enterprise-quick-reference'),c.includes('admin-setup-android/00-overview.md'),c.includes('reference/android-capability-matrix.md'),c.includes('## iOS/iPadOS Provisioning'),!c.match(/^## Operations\s*$/m),!/TBD|TODO|FIXME|XXX|PLACEHOLDER/.test(c.replace(/```[\s\S]*?```/g,'').replace(/^## Version History[\s\S]*$/m,''))];const failed=checks.map((v,i)=>v?null:i+1).filter(Boolean);if(failed.length){console.error('Failed checks:',failed);process.exit(1)}console.log('All',checks.length,'checks pass');process.exit(0);"</automated>
  </verify>
  <done>docs/index.md contains the 3 sub-H3s under `## Android Enterprise Provisioning` H2 with row counts L1=4 / L2=4 / Admin=3; iOS H2 literal `## iOS/iPadOS Provisioning` UNCHANGED; Cross-Platform References table has two new Android rows; no `## Operations` H2 added; no TBD/TODO outside Version History; node check exits 0.</done>
</task>

</tasks>

<verification>
- Pre-edit `57-ANCHOR-INVENTORY.md` exists with grep output for all 4 hub-file anchor patterns + iOS H2 stability baseline table
- `docs/index.md` Android Enterprise H2 contains exactly 3 sub-H3s with row counts 4/4/3 per D-02/D-03/D-04
- All 13 link literals from must_haves `contains` block are present in `docs/index.md`
- iOS H2 literal `## iOS/iPadOS Provisioning` UNCHANGED (V-57-25 anchor stability)
- No `## Operations` H2 added (Phase 59 ownership preserved)
- No TBD/TODO/FIXME/XXX/PLACEHOLDER tokens outside Version History (V-57-26)
- Frontmatter `last_verified` updated; `review_by` is `last_verified + 60 days`
</verification>

<success_criteria>
- ROADMAP:325 SC#1 satisfied: Admin landing on docs/index.md Android Enterprise section sees 3 sub-tables (L1 / L2 / Admin Setup) matching the structural depth of Windows, macOS, and iOS sections — not a one-line stub
- CLEAN-01 closed: Admin can navigate Android L1 / L2 / Admin Setup resources at the same depth as iOS, macOS, and Windows
- DEFER-07 / AENAVUNIFY-04 hub-index gap closed for docs/index.md
- Pre-edit anchor inventory captured for VERIFICATION.md cross-check (PITFALL-6 + D-32 step 5)
- 57-07 atomic commit can include this file's diff
</success_criteria>

<output>
After completion, create `.planning/phases/57-defer-07-android-nav-unification/57-01-SUMMARY.md` documenting:
1. The pre-edit anchor inventory commands run + count of anchor references found per hub file
2. The 3 sub-tables added to Android Enterprise H2 (row counts 4/4/3) with all link literals
3. The two new Cross-Platform References rows added (D-05)
4. Confirmation that iOS H2 stability is preserved (V-57-25 baseline match)
5. Frontmatter update (last_verified + review_by 60-day cycle)
6. DPO-Phase57-01 propagation note for 57-07 atomic commit author: "Plan 57-01 expanded docs/index.md Android H2 (CLEAN-01); pre-edit anchor inventory at .planning/phases/57-defer-07-android-nav-unification/57-ANCHOR-INVENTORY.md; ready for atomic commit per D-31"
</output>
