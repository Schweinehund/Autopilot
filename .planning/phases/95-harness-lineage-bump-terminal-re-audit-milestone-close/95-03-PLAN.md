---
phase: 95-harness-lineage-bump-terminal-re-audit-milestone-close
plan: 03
type: execute
wave: 3
depends_on: ["95-02"]
files_modified:
  - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-03-AUDIT-RESULTS.md
autonomous: false
requirements: [HARN-03]
must_haves:
  truths:
    - "Atom 2 is confirmed on origin/master (pre-flight ordering gate) before any axis runs"
    - "Axis 1 (local physical independence): a fresh git clone --no-hardlinks into $env:TEMP runs ONLY the 2 leaf validators (v1.12-milestone-audit.mjs + check-phase-94.mjs) with HEAD==source assertion and zero-orphan cleanup"
    - "Axis 2 (cross-OS): gh workflow run audit-harness-v1.12-integrity.yml --ref master completes; per-job conclusions + Linux chain timing captured"
    - "Axis 3 (logical/context independence): the SAME fresh sub-agent serves zero-context-carryover (one dispatch, two dimensions)"
    - "BOTH chain validators (apex check-phase-95 [48..94] AND continuity check-phase-93 [48..92]) are Linux-GHA SOLE-authoritative — both cascade on cold Windows (corrected D-03 OS split)"
    - "Cross-OS PASS/FAIL/SKIP EXACT MATCH on the 2 leaf rows + Linux-authoritative counts on the 2 chain rows recorded in 95-03-AUDIT-RESULTS.md"
  artifacts:
    - path: ".planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-03-AUDIT-RESULTS.md"
      provides: "3-axis terminal re-audit evidence with 4-row cross-OS table"
      contains: "EXACT MATCH"
      min_lines: 80
  key_links:
    - from: ".planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-03-AUDIT-RESULTS.md"
      to: ".github/workflows/audit-harness-v1.12-integrity.yml"
      via: "Axis 2 gh workflow run --ref master"
      pattern: "audit-harness-v1.12-integrity"
---

<objective>
Ship Plan 95-03: HARN-03 part 1 — the 3-axis terminal re-audit (artifact-only commit, COMMIT 4). Replicate the executed v1.11 (93-03) recipe, v1.12-relabeled, across the **4-row** cross-OS-applicable set (per D-03), and record cross-OS EXACT MATCH evidence in `95-03-AUDIT-RESULTS.md`.

The 4-row cross-OS set (per D-03 — corrected OS split):
| Validator | Type | Windows Axis-1 | Linux Axis-2 | Expected P/F/S | Authority |
|-----------|------|----------------|--------------|----------------|-----------|
| `v1.12-milestone-audit.mjs` (--verbose + --self-test) | leaf | YES | YES | `15/0/0` | cross-OS EXACT MATCH |
| `check-phase-94.mjs` (PRESENCE+SELF+CONTENT) | leaf | YES | YES | `7/0/0` (capture) | cross-OS EXACT MATCH |
| `check-phase-93.mjs` (continuity, CHAIN [48..92]) | chain | NO (cascades) | YES | `~47/0/1` | **Linux sole-authoritative** |
| `check-phase-95.mjs` (apex, CHAIN [48..94]) | chain | NO (cascades) | YES | `~49/0/1` (capture) | **Linux sole-authoritative** |

- **Axis 1 (local physical independence):** ONE fresh sub-agent runs a fresh `git clone --no-hardlinks` into `$env:TEMP\v1.12-audit-<rand8>`, asserts cloned HEAD == source HEAD, runs ONLY the 2 leaf validators, captures exit/PASS/FAIL/SKIP, then `Remove-Item -Recurse -Force` + asserts zero orphans.
- **Axis 2 (cross-OS):** main session `gh workflow run audit-harness-v1.12-integrity.yml --ref master`; capture run URL + per-job conclusions + Linux chain timing. BOTH chain rows come from Linux GHA sole-authoritatively.
- **Axis 3 (logical/context independence):** the SAME sub-agent as Axis 1 — zero-context-carryover. ONE dispatch covering two independence dimensions (do NOT spawn two agents).

Purpose: Prove the v1.12 harness produces identical PASS/FAIL/SKIP counts across a clean Windows clone and the Linux GHA runner on the leaf rows. **The corrected OS split (D-03):** BOTH `check-phase-95` (apex [48..94]) AND continuity `check-phase-93` (CHAIN [48..92]) are chain validators → both cascade on cold Windows (O(n²) deep-nest, empirically fired at [48..87] in v1.10 — [48..94] is depth-monotone-worse) → both counts come EXCLUSIVELY from Linux GHA. The v1.11 plan's "continuity = fast non-apex" label was WRONG.
Output: `95-03-AUDIT-RESULTS.md` (artifact-only commit).

**HARD pre-flight ordering gate (D-03):** Plan 95-02 (Atom 2) MUST already be committed AND on `origin/master`. The v1.12 workflow's check-phase-94/95 jobs FAIL (not skip) if absent from the dispatched ref.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONVENTIONS.md
@.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONTEXT.md
@.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-RESEARCH.md
@.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-PATTERNS.md
@.planning/milestones/v1.11-phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-03-AUDIT-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight ordering gate + Axis 2 cross-OS Linux GHA dispatch</name>
  <files>.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-03-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/milestones/v1.11-phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-03-AUDIT-RESULTS.md (the EXECUTED v1.11 recipe — Axis-2 gh command, pre-flight 3-part gate, cross-OS table format, deep-nest firing evidence)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-CONVENTIONS.md (4-row set + expected counts: harness 15/0/0, check-phase-94 7/0/0, check-phase-93 continuity ~47/0/1, check-phase-95 apex ~49/0/1 Linux-sole-authoritative)
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-RESEARCH.md (Verification Target 5 — Axis-2 command + 3-part pre-flight gate; both-chain-validators-Linux-authoritative correction)
  </read_first>
  <action>
    **Pre-flight 3-part gate (HARD — abort if any fails):**
    1. `git log origin/master --oneline -1` shows the Atom-2 commit (`feat(95-02): … HARN-02 (atomic SC#1 Atom 2)`) on origin/master.
    2. `gh auth status` reports authenticated (Schweinehund).
    3. `gh workflow list` shows `Audit Harness v1.12 Integrity` with `state: active`.
    If the Atom-2 commit is NOT on origin/master, STOP — Plan 95-02 must be pushed first (the workflow's check-phase-94/95 jobs FAIL, not skip, if absent from the dispatched ref).

    **Axis 2 dispatch:** Run `gh workflow run audit-harness-v1.12-integrity.yml --ref master`. Poll the run to completion (`gh run watch` or `gh run list --workflow=audit-harness-v1.12-integrity.yml -L1` then `gh run view <id>`). Capture: run URL, per-job conclusions (parse / path-match / harness-run / linux-chain-ubuntu-latest / check-phase-94 / check-phase-95 / rotting-external-quarterly [expect SKIP — negative control] / pin-helper-advisory), and the Linux chain timing from the `CHAIN_TIMING_LINUX` `::notice`. **BOTH chain counts are Linux-sole-authoritative:** the apex check-phase-95 [48..94] count from `linux-chain-ubuntu-latest`, AND the continuity check-phase-93 [48..92] count (run check-phase-93 on Linux to capture — it cascades on Windows). Record the corrected OS split explicitly: both chain validators are Linux-only; only the 2 leaf rows run on Windows Axis-1.

    Begin authoring `95-03-AUDIT-RESULTS.md` (Path-A from 93-03): frontmatter (clone_path_pattern `v1.12-audit-<rand8>`, gha_workflow_run URL, cross_os_exact_match) + the Axis-2 section with the captured Linux counts for all 4 rows. Leave the Axis-1/Axis-3 Windows leaf columns for Task 2.
  </action>
  <verify>
    <automated>gh run list --workflow=audit-harness-v1.12-integrity.yml -L1 --json conclusion,status,url 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight: `git log origin/master --oneline -1` contains the Atom-2 commit; `gh auth status` authenticated; v1.12 workflow `state: active`.
    - `gh workflow run audit-harness-v1.12-integrity.yml --ref master` dispatched; the run completes with linux-chain-ubuntu-latest + check-phase-94 + check-phase-95 jobs SUCCESS (or documented exact counts).
    - `rotting-external-quarterly` job conclusion = SKIP (negative-control confirmed — cron-only `if:` guard).
    - Both Linux chain counts captured (apex check-phase-95 [48..94] + continuity check-phase-93 [48..92], both sole-authoritative); 95-03-AUDIT-RESULTS.md frontmatter records the run URL + `cross_os_exact_match` field.
  </acceptance_criteria>
  <done>Pre-flight gate passed; Axis-2 Linux GHA run dispatched and completed; per-job conclusions + both Linux chain counts captured into the in-progress 95-03-AUDIT-RESULTS.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Axis 1 + Axis 3 fresh-clone sub-agent (leaf rows only — local physical + logical independence)</name>
  <what-built>
    The v1.12 audit harness (Atom 1 + Atom 2) is committed and on origin/master, and the Axis-2 Linux GHA run has produced cross-OS counts for all 4 rows. This checkpoint runs the Axis-1/Axis-3 fresh-clone independence recipe — a fresh, zero-context PowerShell environment that clones the repo with its own `.git/`, runs ONLY the 2 leaf validators (the 2 chain validators are deliberately NOT run on Windows — both cascade), and proves the local Windows leaf counts match Linux.
  </what-built>
  <how-to-verify>
    Run this fresh-clone recipe in a clean PowerShell session (zero context carryover — this serves BOTH Axis 1 physical independence AND Axis 3 logical independence; ONE session, two dimensions — do NOT spawn two):

    1. Generate `$rand` = 8 chars from charset `[0-9a-z]`. Set `$dest = "$env:TEMP\v1.12-audit-$rand"`.
    2. `git clone --no-hardlinks D:\claude\Autopilot $dest` (auditor independence — own `.git/`, NOT a worktree; permitted under `use_worktrees:false`).
    3. Assert cloned HEAD == source HEAD: compare `git -C $dest rev-parse HEAD` to `git -C D:\claude\Autopilot rev-parse HEAD` — MUST be equal.
    4. From `$dest`, run ONLY the **2 leaf validators**: `node scripts/validation/v1.12-milestone-audit.mjs --verbose` (and `--self-test`), and `node scripts/validation/check-phase-94.mjs`. Capture each exit code + the `Result: N PASS, N FAIL, N SKIPPED` line. **Do NOT run check-phase-93.mjs OR check-phase-95.mjs from the Windows clone** — BOTH are chain validators and cascade to O(n²) deep-nest (3+ min hang, 12+ node procs); their counts are Linux-GHA sole-authoritative (deep-nest guaranteed at [48..92] and [48..94]).
    5. Expected (must match Linux from Axis 2 on the leaf rows): harness `15/0/0`; self-test `9 passed, 0 failed`; check-phase-94 `7/0/0`.
    6. Cleanup: `Remove-Item -Recurse -Force $dest`; assert `(Get-ChildItem $env:TEMP -Filter "v1.12-audit-*" -Directory).Count -eq 0` (zero orphans).
    7. Finalize `95-03-AUDIT-RESULTS.md`: complete the 4-row cross-OS table (Windows Axis 1+3 column populated for the 2 leaf rows ONLY; the 2 chain rows show "Windows: N/A — cascades, Linux-sole-authoritative" with the Linux count from Axis 2), mark each leaf row EXACT MATCH, set `cross_os_exact_match: true`, and record the clone HEAD, `$rand`, and zero-orphan cleanup assertion. Document `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` as the reason BOTH chain rows are Linux-sole-authoritative (carry-forward note for DEFERRED-CLEANUP, depth → [48..94]).

    Confirm: cloned HEAD matched source, both leaf counts matched the Linux Axis-2 counts (EXACT MATCH), and the temp clone was removed with zero orphans.
  </how-to-verify>
  <resume-signal>Type "approved" once the fresh-clone recipe shows cross-OS EXACT MATCH on the 2 leaf rows (harness 15/0/0 + check-phase-94 7/0/0), both chain rows recorded Linux-sole-authoritative, zero orphans, and 95-03-AUDIT-RESULTS.md is finalized — or describe any count divergence.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit the audit-results artifact (COMMIT 4, artifact-only)</name>
  <files>.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-03-AUDIT-RESULTS.md</files>
  <read_first>
    - .planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-03-AUDIT-RESULTS.md (the artifact finalized in Task 2)
    - .planning/milestones/v1.11-phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-03-AUDIT-RESULTS.md (Path-A structure reference)
  </read_first>
  <action>
    Verify `95-03-AUDIT-RESULTS.md` contains the complete 4-row cross-OS table (2 leaf rows marked EXACT MATCH with both columns populated; 2 chain rows marked Linux-sole-authoritative with the Linux count), `cross_os_exact_match: true`, the Axis-2 run URL, the Axis-1 clone HEAD + `$rand` + zero-orphan assertion, and the `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01` both-chains-Linux-authoritative note (depth [48..94]). Then commit ONLY this file (artifact-only commit — no validators, no config changed).
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-03-AUDIT-RESULTS.md','utf8'); ['EXACT MATCH','check-phase-94','check-phase-95','check-phase-93','WINDOWS-CLONE-DEEPNEST-TIMEOUT-01','audit-harness-v1.12-integrity'].forEach(s=>{if(!f.includes(s)){console.error('AUDIT-RESULTS missing '+s);process.exit(1)}}); console.log('audit-results ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `95-03-AUDIT-RESULTS.md` contains a 4-row cross-OS table with EXACT MATCH (leaf rows), `check-phase-93`, `check-phase-94`, `check-phase-95`, `WINDOWS-CLONE-DEEPNEST-TIMEOUT-01`, and `audit-harness-v1.12-integrity`.
    - The table records BOTH chain rows (93 + 95) as Linux-sole-authoritative (Windows N/A — cascades).
    - COMMIT 4 message is exactly: `docs(95-03): HARN-03 3-axis terminal re-audit results (artifact-only)`
    - COMMIT 4 touches ONLY `95-03-AUDIT-RESULTS.md` (artifact-only; no validators/config modified).
  </acceptance_criteria>
  <done>95-03-AUDIT-RESULTS.md finalized with cross-OS EXACT MATCH on the 2 leaf rows + both chain rows Linux-sole-authoritative, committed as COMMIT 4 (artifact-only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Windows ↔ GitHub Actions Linux runner | The re-audit deliberately crosses the OS boundary to prove count-identity on the leaf rows. No untrusted input crosses — both axes run the same in-repo validators against the same `master` ref. |
| source repo → fresh TEMP clone | Axis 1 clones into `$env:TEMP` with `--no-hardlinks` (own `.git/`), runs read-only validators, then removes the clone. No write-back to the source repo. |

## STRIDE Threat Register

ASVS L1 review found **no high-severity items**. This is a read-only verification plan; no secrets, no untrusted input, no auth surface beyond `gh` to the user's own repo.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-03-T | Tampering | Fresh clone left as orphan in TEMP (stale-state leakage) | mitigate | Recipe asserts `Get-ChildItem $env:TEMP -Filter "v1.12-audit-*" -Directory` count == 0 after `Remove-Item -Recurse -Force`. |
| T-95-03-I | Integrity | Axis-2 dispatched against a ref missing the v1.12 jobs (false PASS/SKIP) | mitigate | HARD pre-flight gate: Atom 2 confirmed on origin/master + workflow `state: active` before dispatch; jobs FAIL (not skip) if absent. |
| T-95-03-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | NO installs — `git clone` + `node` (in-tree scripts) + `gh` only. No new deps. No Package Legitimacy Gate required. |
</threat_model>

<verification>
- Pre-flight gate passed (Atom 2 on origin/master + gh auth + workflow active).
- Axis 2 Linux GHA run completed; BOTH chain counts (apex check-phase-95 + continuity check-phase-93) are Linux-sole-authoritative.
- Axis 1/3 fresh clone: HEAD==source, both leaf counts EXACT MATCH Linux, zero orphans.
- 95-03-AUDIT-RESULTS.md records cross_os_exact_match: true; committed artifact-only.
</verification>

<success_criteria>
- ROADMAP SC#3 satisfied: 3-axis terminal re-audit completes (Axis 1 fresh clone + Axis 2 Linux GHA + Axis 3 zero-context sub-agent); cross-OS PASS/FAIL/SKIP counts EXACT MATCH across the 2 leaf rows; both chain rows Linux-authoritative.
- Corrected D-03 OS split documented: BOTH chain validators (93 + 95) Linux-GHA sole-authoritative (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 at [48..94]).
- 95-03-AUDIT-RESULTS.md committed as COMMIT 4 (artifact-only), consumed by the 95-04 close-gate.
</success_criteria>

<output>
Create `.planning/phases/95-harness-lineage-bump-terminal-re-audit-milestone-close/95-03-SUMMARY.md` when done. Record: Axis-2 run URL + both Linux chain counts (apex-95 + continuity-93), Axis-1 clone HEAD + `$rand` + zero-orphan confirmation, the 4-row outcome (2 leaf EXACT MATCH + 2 chain Linux-authoritative), and COMMIT-4 SHA (the `audit_results_sha` for the 95-04 close-gate frontmatter).
</output>
</content>
