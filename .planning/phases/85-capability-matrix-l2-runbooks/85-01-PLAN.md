---
phase: 85-capability-matrix-l2-runbooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/85-capability-matrix-l2-runbooks/85-ANCHOR-INVENTORY.md
  - docs/reference/macos-capability-matrix.md
  - scripts/validation/check-phase-63.mjs
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - "A pre-edit anchor inventory artifact for macos-capability-matrix.md is committed BEFORE the matrix is edited, recording all ## headings + anchor IDs + pre-edit blob hash"
    - "macos-capability-matrix.md contains Kerberos SSO Extension row(s) under the existing ## Authentication H2 (NOT a new ## SSO Extensions H2), Windows cell `n/a — not covered in this matrix`, macOS cell linking to guide 10"
    - "check-phase-63.mjs V-63-08 BASELINE equals the post-edit git hash-object of macos-capability-matrix.md, updated in the SAME commit as the matrix edit"
    - "node scripts/validation/check-phase-63.mjs reports V-63-08 PASS (not SKIPPED, not FAIL)"
  artifacts:
    - path: ".planning/phases/85-capability-matrix-l2-runbooks/85-ANCHOR-INVENTORY.md"
      provides: "Pre-edit anchor inventory (SC#1) mirroring 63-ANCHOR-INVENTORY.md format"
      contains: "e91f28c76441577d4e6176756ba04300ceb555e1"
    - path: "docs/reference/macos-capability-matrix.md"
      provides: "Kerberos SSO Extension row(s) under ## Authentication"
      contains: "AppSSOKerberos.KerberosExtension"
    - path: "scripts/validation/check-phase-63.mjs"
      provides: "V-63-08 BASELINE updated to post-edit matrix hash"
      contains: "const BASELINE"
  key_links:
    - from: "docs/reference/macos-capability-matrix.md"
      to: "docs/admin-setup-macos/10-kerberos-sso-extension.md"
      via: "markdown link in new macOS cell(s)"
      pattern: "10-kerberos-sso-extension\\.md"
    - from: "scripts/validation/check-phase-63.mjs"
      to: "docs/reference/macos-capability-matrix.md"
      via: "V-63-08 git hash-object baseline guard"
      pattern: "const BASELINE = '[0-9a-f]{40}'"
---

<objective>
Establish the pre-edit anchor-inventory artifact for `docs/reference/macos-capability-matrix.md`, then add Kerberos SSO Extension row(s) under the existing `## Authentication` H2, committed atomically with the `check-phase-63.mjs` V-63-08 blob-hash baseline bump.

Purpose: Satisfies REF-01 and SC#1/SC#2. The matrix is a Windows-vs-macOS feature-parity reference doc; Kerberos SSO is an authentication capability that belongs under the live `#authentication` anchor (D-01). The V-63-08 byte-unchanged harness guard breaks the moment the matrix file changes, so the BASELINE must be bumped in the same commit (DI-2, the named atomic-commit discipline).

Output: `85-ANCHOR-INVENTORY.md` (committed first, separately), then an atomic matrix-edit + V-63-08-bump commit that makes V-63-08 PASS for the first time since Phase 81.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-capability-matrix-l2-runbooks/85-CONTEXT.md
@.planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md
@.planning/phases/85-capability-matrix-l2-runbooks/85-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create committed pre-edit anchor inventory artifact (SC#1) — BEFORE any matrix edit</name>
  <files>.planning/phases/85-capability-matrix-l2-runbooks/85-ANCHOR-INVENTORY.md</files>
  <read_first>
    - .planning/milestones/v1.6-phases/63-multi-ou-architecture-apple-admin-setup/63-ANCHOR-INVENTORY.md (canonical format analog — mirror its header block + per-file section + permitted-edits structure)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-PATTERNS.md (§ 85-ANCHOR-INVENTORY.md pattern — verbatim Phase-85 substitution at lines 76-104)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md (Finding 1 — H2 anchor table at lines 104-116; pre-edit SHA + blob hash)
    - docs/reference/macos-capability-matrix.md (the file being inventoried — confirm the ## headings list still matches Finding 1 before recording)
  </read_first>
  <action>
    Create `.planning/phases/85-capability-matrix-l2-runbooks/85-ANCHOR-INVENTORY.md` mirroring the `63-ANCHOR-INVENTORY.md` format. Header block: title `# Phase 85 Pre-Edit Anchor Inventory`, `**Captured:**` dated 2026-06-23 with the "(BEFORE any Phase 85 edits to existing files)" qualifier, `**Files inventoried:** 1 existing file receiving new rows`, `**Purpose:**` referencing SC#1 / PITFALL-6 / DA-4 convention, and `**Mirrors format:**` pointing at the 63-ANCHOR-INVENTORY.md path.

    Add `## File 1: docs/reference/macos-capability-matrix.md` with: `**Pre-edit git SHA (last commit touching file):** 45b4865`; `**Pre-edit blob hash:** e91f28c76441577d4e6176756ba04300ceb555e1`; `**Receiving:** Kerberos SSO Extension row(s) appended inside the ## Authentication table (before ## See Also)`.

    Record the verbatim `### H2/H3 Anchor List (verbatim, pre-edit)` from the live file — H2 anchors with line numbers: `13:## Enrollment`, `29:## Configuration`, `42:## App Deployment`, `55:## Compliance`, `67:## Software Updates`, `79:## Conditional Access`, `88:## Key Gaps Summary`, `100:## Authentication`, `114:## See Also`, `122:## Version History`. State `H3 anchors: (none — H2-only file)`.

    Add a `**Permitted edits per Plan 85-01:**` list: new data row(s) APPENDED inside the Authentication H2 table; zero H2/H3 headings renamed; zero existing prose modified; `check-phase-63.mjs` V-63-08 BASELINE updated to post-edit hash in the same commit.

    Before writing, re-run `git hash-object docs/reference/macos-capability-matrix.md` and confirm it still equals `e91f28c76441577d4e6176756ba04300ceb555e1`. If it differs (file changed since research), record the actual current hash instead — the inventory must reflect the true pre-edit state. Commit this artifact ALONE (no matrix edit staged) so SC#1's "committed before any matrix edits" is literally true.
  </action>
  <verify>
    <automated>git -C "$(pwd)" log --oneline -1 -- .planning/phases/85-capability-matrix-l2-runbooks/85-ANCHOR-INVENTORY.md && grep -q "e91f28c76441577d4e6176756ba04300ceb555e1" .planning/phases/85-capability-matrix-l2-runbooks/85-ANCHOR-INVENTORY.md && grep -q "## Authentication" .planning/phases/85-capability-matrix-l2-runbooks/85-ANCHOR-INVENTORY.md && echo INVENTORY_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/85-capability-matrix-l2-runbooks/85-ANCHOR-INVENTORY.md` exists and is committed in its OWN commit with no edit to `macos-capability-matrix.md` staged in that same commit.
    - File contains the pre-edit blob hash string `e91f28c76441577d4e6176756ba04300ceb555e1` (or the verified-current hash if it drifted).
    - File lists all 10 H2 anchors verbatim including `100:## Authentication` and `114:## See Also`.
    - File records `H3 anchors: (none — H2-only file)`.
    - `git hash-object docs/reference/macos-capability-matrix.md` was run and confirmed BEFORE the matrix was edited.
  </acceptance_criteria>
  <done>The committed anchor-inventory artifact records all matrix H2 anchors, the pre-edit blob hash, and permitted edits — in a standalone commit preceding any matrix change (SC#1 satisfied).</done>
</task>

<task type="auto">
  <name>Task 2: Add Kerberos row(s) under ## Authentication + atomic V-63-08 BASELINE bump (REF-01, D-01/D-02/D-03)</name>
  <files>docs/reference/macos-capability-matrix.md, scripts/validation/check-phase-63.mjs</files>
  <read_first>
    - docs/reference/macos-capability-matrix.md (the ## Authentication table at lines 100-112 — match its `| Feature | Windows | macOS |` column grammar exactly; the ## See Also H2 at line 114 is the insertion boundary; the Version History table at lines 122-128)
    - docs/admin-setup-macos/10-kerberos-sso-extension.md (link target — confirm the file exists and note its anchor/section names so the macOS cell links are valid)
    - scripts/validation/check-phase-63.mjs (V-63-08 block at lines 202-221; BASELINE constant at line 209)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-PATTERNS.md (§ macos-capability-matrix.md — row grammar rules at lines 139-151; § check-phase-63.mjs — V-63-08 change-line-209-only rule at lines 169-193; § Atomic Blob-Hash Baseline Bump at lines 523-532)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md (Finding 2 Pitfall 1 — DO NOT set BASELINE to the Phase-63 original e91d7f9e...; use the post-edit git hash-object result)
  </read_first>
  <action>
    Append Kerberos SSO Extension data row(s) to the END of the `## Authentication` table body (after the `Hybrid Entra join` row, before the `## See Also` H2). Use per-feature rows matching the existing PSSO grammar (D-02) — do NOT create a `## SSO Extensions` H2 (D-01) and do NOT add any Graph-managed Platform Credential row (D-03, Kerberos-only in the matrix). Recommended two rows per RESEARCH Open-Question 2 (exact labels/count are Claude's Discretion): (a) a Kerberos SSO Extension identity/deployment row citing the identifier `com.apple.AppSSOKerberos.KerberosExtension` and `Type: Credential`, deployed via Intune Custom Template; (b) a PSSO-TGT integration row citing `usePlatformSSOTGT` and the macOS 14.6+ floor.

    Each new row's Windows cell MUST be exactly `n/a — not covered in this matrix`. Each macOS cell states the capability in plain text and ends with a `— see [Kerberos SSO Extension Guide](../admin-setup-macos/10-kerberos-sso-extension.md)` (or `— see [guide 10](../admin-setup-macos/10-kerberos-sso-extension.md)`) link. Use bold ONLY for strong negative assertions (none expected here — Kerberos is supported). Do not modify any existing row or prose.

    Add one Version History row at the bottom of the Version History table: `| 2026-06-23 | Phase 85 (REF-01): add Kerberos SSO Extension rows under ## Authentication | -- |`.

    Then perform the atomic V-63-08 bump: run `git hash-object docs/reference/macos-capability-matrix.md` against the edited file, take the resulting 40-char hash, and write it as the `const BASELINE` value at `scripts/validation/check-phase-63.mjs` line 209 (replacing `e91d7f9e001bb7ff4dc56a4ca98c84868fbf0716`). Also update the two textual occurrences of the old hash in the V-63-08 comment (line ~202) and the `name:` string (line ~204) for consistency — the harness mechanically checks only line 209, but keep the labels honest. Do NOT touch the V-63-09 block (lines 223-242); the comparison-doc bump belongs to Plan 85-02 (per-file coupling).

    Stage BOTH files together (`docs/reference/macos-capability-matrix.md` + `scripts/validation/check-phase-63.mjs`) and commit as ONE atomic commit (DI-2). The pre-commit verify in Task 3 must pass before this commit is finalized.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-63.mjs 2>&1 | grep -E "V-63-08" | grep -qi "PASS" && grep -q "AppSSOKerberos.KerberosExtension" docs/reference/macos-capability-matrix.md && test "$(git hash-object docs/reference/macos-capability-matrix.md)" = "$(grep -A8 'V-63-08' scripts/validation/check-phase-63.mjs | grep -oE "BASELINE = '[0-9a-f]{40}'" | grep -oE '[0-9a-f]{40}')" && echo V0808_MATCH</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reference/macos-capability-matrix.md` contains the literal string `AppSSOKerberos.KerberosExtension` and the literal `usePlatformSSOTGT` inside the `## Authentication` table; both new macOS cells link to `../admin-setup-macos/10-kerberos-sso-extension.md`.
    - Each new row's Windows cell is exactly `n/a — not covered in this matrix`.
    - No new `## SSO Extensions` H2 exists; the H2 anchor list is unchanged from the 85-ANCHOR-INVENTORY.md record (10 H2 anchors, same headings).
    - No Graph-managed Platform Credential row was added (D-03).
    - `scripts/validation/check-phase-63.mjs` line 209 BASELINE equals `git hash-object docs/reference/macos-capability-matrix.md` of the edited file, and is NOT `e91d7f9e001bb7ff4dc56a4ca98c84868fbf0716`.
    - The matrix edit and the V-63-08 BASELINE bump are in ONE commit; V-63-09 BASELINE (line 230) is unchanged.
  </acceptance_criteria>
  <done>The matrix carries Kerberos SSO Extension row(s) under ## Authentication and V-63-08 BASELINE matches the new matrix hash, committed atomically.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Assert V-63-08 PASS via harness run</name>
  <files>scripts/validation/check-phase-63.mjs</files>
  <read_first>
    - scripts/validation/check-phase-63.mjs (V-63-08 block lines 202-221 — note the catch returns {skipped:true} only when `git hash-object` is unavailable; in this git repo git IS available, so a PASS must be a real PASS, never a skip)
    - .planning/phases/85-capability-matrix-l2-runbooks/85-RESEARCH.md (Finding 2 — both V-63-08 and V-63-09 were FAILING at HEAD before Phase 85; this task confirms V-63-08 is now fixed)
  </read_first>
  <action>
    Run `node scripts/validation/check-phase-63.mjs` and inspect the V-63-08 line. It MUST report PASS (the detail string should read "macos-capability-matrix.md blob hash matches baseline <hash>"), NOT FAIL and NOT skipped. If V-63-08 FAILs, the BASELINE at line 209 does not match the post-edit `git hash-object` — recompute and fix in Task 2's commit (amend), then re-run. Do not proceed to Plan 85-02 until V-63-08 is green. This is a [BLOCKING] gate: the atomic-commit discipline (DI-2) is only satisfied when the harness confirms it.

    Note V-63-09 may still FAIL at this point — that is expected and correct; the comparison-doc bump is Plan 85-02's responsibility. This task asserts ONLY V-63-08.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-63.mjs 2>&1 | grep "V-63-08" | grep -qi "PASS" && node scripts/validation/check-phase-63.mjs 2>&1 | grep "V-63-08" | grep -viq "skip" && echo V0808_PASS_NOT_SKIPPED</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-63.mjs` prints a V-63-08 line containing PASS.
    - The V-63-08 result is a genuine PASS, not `skipped` (the detail does not contain "git hash-object not available").
    - V-63-08 detail reads "blob hash matches baseline" with the new post-edit hash.
  </acceptance_criteria>
  <done>The harness confirms V-63-08 PASS against the edited matrix; the atomic matrix+baseline commit is validated.</done>
</task>

</tasks>

<threat_model>
No application attack surface — documentation + harness baseline edits only. No executable application code, no user input, no network surface is introduced by this plan. The only executable change is a 40-char hex string constant in a validation script. No STRIDE register applies (security_enforcement acknowledged; this is a documentation-only phase per RESEARCH.md Security Domain).
</threat_model>

<verification>
- `node scripts/validation/check-phase-63.mjs` reports V-63-08 PASS (Task 3).
- `85-ANCHOR-INVENTORY.md` committed before the matrix edit (Task 1 — separate commit precedes Task 2 commit in git log).
- Matrix `## Authentication` table contains Kerberos row(s); no new H2; no Graph row.
- `check-phase-63.mjs` line 209 BASELINE == `git hash-object` of edited matrix; line 230 (V-63-09) untouched.
</verification>

<success_criteria>
- SC#1: Pre-edit anchor inventory artifact committed before any matrix edit, recording all `## ` headings + anchor IDs.
- SC#2 (matrix half): Kerberos SSO Extension row under `## Authentication` with guide-10 link; V-63-08 blob hash updated in the same atomic commit; V-63-08 PASS.
- REF-01 fully satisfied.
</success_criteria>

<output>
Create `.planning/phases/85-capability-matrix-l2-runbooks/85-01-SUMMARY.md` when done.
</output>
