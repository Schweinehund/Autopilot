---
phase: 153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a
plan: 13
type: execute
wave: 10
depends_on: ["153-12"]
files_modified:
  - .planning/milestones/v1.21-MILESTONE-AUDIT.md
  - .planning/milestones/v1.21-DEFERRED-CLEANUP.md
autonomous: true
requirements: [HARN-06]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The deferred-cleanup artifact is FRESHLY AUTHORED — [MEASURED] no file for this milestone exists in the milestones directory — but its carry-forward inventory is SOURCED FROM TWO NAMED PLACES, never invented: the future-requirements block in the requirements document, which carries deferrals this milestone generated and that exist in no predecessor file, and the explicit does-not-pick-up list in the project state document (D-66)."
    - "Every still-open predecessor entry is preserved VERBATIM; nothing is masked by deletion (D-66)."
    - "Two predecessor deferrals DISCHARGE here — the back-anchor pin deferral and the contract-presence residue — and two new ones open: the successor back-anchor deferral, and a successor naming the new harness's own live-HEAD contract leg together with the materialized-set coupling (D-46, D-21, D-67)."
    - "THE DOUBLE-BOOKING ADJUDICATION IS RUN IN WRITING, NOT ASSERTED. The closed residue entry and its successor share an identical root cause and differ only in which harness carries it. Carry re-scoped OR drop and name as new — never both (D-67)."
    - "The documentation style pass is OWNED EXPLICITLY by the milestone audit rather than left as unnarrated work inside the tag's history; its directory is tracked with commits already inside the push set (D-61)."
    - "The second-order asymmetry recorded in the evidence plan is carried into the deferred-cleanup artifact so the next milestone inherits the reasoning rather than re-litigating it (D-56)."
    - "The audit inherits the predecessor artifact's section shape; section ordering within it is a discretionary choice made once and stated (Claude's Discretion)."
    - statement: "HARN-06 empty edge: an inventory row with no still-open predecessor counterpart is a NEW entry and is labelled as such; an empty carry-forward section is only correct if both named sources are empty, and their emptiness is asserted rather than assumed."
      verification: explicit
    - statement: "HARN-06 adjacency edge: a discharged entry and its successor can be textually near-identical. Each pair is written with an explicit relationship line stating whether the successor is a re-scope of the closed entry or a genuinely new item, so the pair is never double-counted."
      verification: explicit
  artifacts:
    - .planning/milestones/v1.21-MILESTONE-AUDIT.md
    - .planning/milestones/v1.21-DEFERRED-CLEANUP.md
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-13-SUMMARY.md
  key_links:
    - "The carry-forward inventory has two sources and only one of them is a predecessor milestone file; the other carries deferrals this milestone itself generated."
    - "The whole-tree planning walker scans both of these new documents, so they are inside a live gate."
    - "The milestone audit is the only place the tag's unnarrated work can be narrated; a tag carrying work no document explains is the failure mode being avoided."
  prohibitions:
    - "No still-open predecessor entry is deleted, shortened or paraphrased away."
    - "No entry is both carried re-scoped and dropped as new; each pair carries an explicit relationship line."
    - "No carry-forward inventory row is invented; every row traces to one of the two named sources."
    - "No `docs/` content file is created, edited or deleted."
    - "No requirement status row is flipped by this plan — the close gate is a single later commit and owns every flip."
    - "Neither glossary frontmatter date field is touched."
    - "No summary or commit message may claim a chain result gathered without the nested environment flag (D-75)."
    - "Commit message bodies must not literally quote governance-carve paths or the paths of amended files in prose (D-76)."
    - "No bare-noun platform blockquote token at line start outside a fence in either new document — the whole-tree walker scans them (D-43)."
---

<objective>
Author the two close artifacts: the milestone audit that narrates everything inside the tag, and the
deferred-cleanup document that carries forward everything still open.

Purpose: a tag carrying work no document explains is the failure this milestone's style-pass scope
ruling exists to prevent. And a deferral inventory that is invented rather than sourced silently drops
obligations — this milestone's carry-forward has two sources, only one of which is a predecessor file.

Output: two new milestone documents, both inside a live planning-tree gate, both green.
</objective>

## Artifacts this phase produces

Created by Phase 153, absent at the phase base; treat as new, not as unacknowledged references.

- **New exports in `scripts/validation/_lib/frozen-at-close.mjs`:** `MILESTONE_CLOSE_SHAS.V120`,
  `readAtV120Close`, `lsTreeAtV120Close`, `materializeDocsAtClose`, `withDocsAtClose`.
- **New validators:** `scripts/validation/check-phase-145.mjs` through `check-phase-152.mjs` and
  `scripts/validation/check-phase-153.mjs`.
- **New check ids:** `V-145-*` through `V-152-*`; on the apex `V-153-AUDIT`, `V-153-CHAIN-<n>`,
  `V-153-AUDIT-HARNESS`, `V-153-SELF`, `V-153-C17CARVEOUT-SUCCESSOR`.
- **New harness lineage:** `scripts/validation/v1.21-milestone-audit.mjs`,
  `scripts/validation/v1.21-audit-allowlist.json`, `BASELINE_25` in
  `scripts/validation/regenerate-supervision-pins.mjs`.
- **New CI workflow:** `.github/workflows/audit-harness-v1.21-integrity.yml`.
- **New planning artifacts:** `.planning/milestones/v1.21-MILESTONE-AUDIT.md`,
  `.planning/milestones/v1.21-DEFERRED-CLEANUP.md`, this phase's summaries, `153-EVIDENCE.md`,
  `153-VERIFICATION.md`, and the gitignored `dist/docs-library-v1.21.zip`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md
@.planning/milestones/v1.20-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `v1.21-MILESTONE-AUDIT.md` — narrate everything inside the tag, including the style pass</name>
  <files>.planning/milestones/v1.21-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v1.20-MILESTONE-AUDIT.md — the whole document. Its section shape is inherited; read it for structure, for how a phase is narrated, for how evidence is quoted, and for how honesty notes are recorded
    - .planning/REQUIREMENTS.md — all fifty-eight requirements across the eight categories, so the traceability section is complete and the category counts are derived rather than recalled
    - .planning/ROADMAP.md — the nine phases and their success criteria
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md — the three-axis results, the six direct harness results, the census and the glossary measurement
    - the eight content phases' verification documents — one per phase, for what each actually shipped and what each measured
    - scripts/docs-style/ — the tracked style-pass directory and its commits inside the push set
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-61, D-73
  </read_first>
  <action>
Author `.planning/milestones/v1.21-MILESTONE-AUDIT.md`, inheriting the predecessor document's section
shape. Choose the section ordering once, state that you did, and keep it.

Content the document must own:

- **Per-phase narration** for all nine phases, each with what it shipped and what it measured, sourced
  from that phase's own verification document rather than from memory.
- **Requirement traceability** for all fifty-eight requirements across the eight categories, with the
  per-category counts derived by counting rather than recalled.
- **The three-axis evidence** at the one shared commit, with the commit quoted, the job-level totals,
  the skip classification and the two reproduction results.
- **The six-harness direct evidence** and the explicit statement that the apex triple is not evidence
  for the conversion criterion.
- **The scope amendment** widening the harness set from five to six, with its owner ruling date and
  the surfaces it touched.
- **The style pass, owned explicitly.** Its directory is tracked and its commits are already inside
  the push set, so it lands on the default branch and appears in this tag's history. Narrate what it
  is, what it changed, what verification it carried, and what remains open about it. Leaving a tag
  carrying unnarrated work is the failure this section exists to prevent, and splitting it out was
  ruled out because it would mean rewriting the whole unpushed history at a close whose evidence is
  keyed to a shared commit.
- **Honesty notes**, in the predecessor's tradition: anything that did not go cleanly, any leg
  recorded incomplete, any remediation round spent, and any correction of record this phase produced
  — the carve-gate reasoning correction, the ordinal distinction, the false twin claim, and the false
  carve-out prose.

Every count in the document is derived at authoring time by a command, and the command is quoted
beside the count.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The document exists and covers all nine phases, each traced to its own verification document.
    - The traceability section covers all fifty-eight requirements; the total and the eight per-category counts are each accompanied by the command that produced them.
    - The three-axis evidence section quotes the one shared commit and reproduces the job-level totals and the skip classification from the evidence file.
    - The document states, in its own words, that the apex triple is not evidence for the conversion criterion, and carries the six direct harness results.
    - The scope amendment is narrated with its ruling date and its affected surfaces.
    - The style pass has its own section naming what it is, its tracked status, its commit count inside the push set, and what remains open.
    - The honesty-notes section names every correction of record this phase produced.
    - `node scripts/validation/check-phase-54.mjs` exits 0 after the document is written — it is inside a live whole-tree planning scan.
  </acceptance_criteria>
  <reversibility rating="reversible">A new planning document; revertible before the close gate.</reversibility>
  <done>The milestone audit narrates all nine phases, all fifty-eight requirements, the three-axis evidence and the style pass, with every count derived by a quoted command.</done>
</task>

<task type="auto">
  <name>Task 2: `v1.21-DEFERRED-CLEANUP.md` — discharges, new entries, and the double-booking adjudication run in writing</name>
  <files>.planning/milestones/v1.21-DEFERRED-CLEANUP.md</files>
  <read_first>
    - .planning/milestones/v1.20-DEFERRED-CLEANUP.md — the whole document: its part structure, every entry, and which entries are still open. Its two entries discharged by this phase are around lines 105 and 138
    - .planning/REQUIREMENTS.md, the future-requirements block — carries deferrals this milestone generated that exist in no predecessor file, including the past-due population and the open research questions
    - .planning/STATE.md, the does-not-pick-up-and-still-carries-forward list — the second named source
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md — the second-order asymmetry paragraph, and the census constraint on the two governance files
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-04-SUMMARY.md — the new harness's born-live-HEAD residue statement
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-01-SUMMARY.md — the materialized-set coupling recorded in the helper header
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-21, D-46, D-56, D-66, D-67, and the deferred-ideas block naming what was reviewed and not ruled in this phase
  </read_first>
  <action>
Author `.planning/milestones/v1.21-DEFERRED-CLEANUP.md` fresh, inheriting the predecessor's part
structure.

**Discharges.** Record both predecessor entries this phase closes — the back-anchor pin deferral and
the contract-presence residue — each with what discharged it, where the evidence is, and the date.

**New entries.** Open at least:

- the **successor back-anchor deferral**: this milestone cannot pin itself, because a pin always
  references a past close; the successor milestone pins it. State the invariant, not just the task.
- a successor naming the **new harness's own live-HEAD contract leg**, which is born unconverted for
  the same structural reason and makes it the successor milestone's sole unconverted harness.
- the **materialized-set coupling**: the set the helper materializes is a silent contract with a
  byte-frozen validator, so a future contract validator reading outside the materialized prefix takes
  six harnesses vacuously green at once.
- the **second-order asymmetry**: the nested guard's rationale no longer holds once harnesses are
  frozen-aware, but the predecessor apex validators carrying that rationale are frozen and cannot be
  edited, so the benefit is permanently invisible to the apex. Carry the reasoning, not only the
  conclusion.

**The double-booking adjudication, run in writing (D-67).** The discharged residue entry and its
successor share an identical root cause and differ only in which harness carries it. Write the
adjudication out: state the shared root cause, state the difference, and then choose ONE of two
dispositions and say which — carry the entry re-scoped to the new harness, OR close it and open the
successor as a genuinely new item. Never both. Every discharged-and-successor pair in the document
carries a relationship line of this kind, so a later reader cannot double-count.

**Carry-forward inventory.** Every row traces to one of the two named sources. Preserve every
still-open predecessor entry verbatim — never mask by deletion. If a row has no still-open predecessor
counterpart, label it new. Assert, rather than assume, that the two sources were read to exhaustion:
record the count of open items each source yielded.

Also record the items reviewed and deliberately **not** ruled in this phase, so they are visibly
parked rather than silently dropped: the untracked skill-file population, the modified configuration
file, the stray root-level capture files, the frozen carve-out check's stale prose as a class, the
stale figure in one requirement's narration, and the stale seed.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The document exists and records both discharges with their evidence locations and dates.
    - At least the four named new entries exist, each stating its invariant or coupling rather than only a task.
    - Every discharged-and-successor pair carries an explicit relationship line stating carry-re-scoped or closed-and-new — never both.
    - Every carry-forward row traces to one of the two named sources, and the open-item count each source yielded is recorded.
    - Every still-open predecessor entry appears verbatim; a diff-style comparison against the predecessor document showing no still-open entry was dropped is recorded.
    - The reviewed-but-not-ruled items are all listed and visibly parked.
    - `node scripts/validation/check-phase-54.mjs` exits 0 after the document is written.
    - `node scripts/validation/check-phase-153.mjs` exits 0 — both new documents are inside the live planning scan that the apex chain covers.
  </acceptance_criteria>
  <reversibility rating="reversible">A new planning document; revertible before the close gate.</reversibility>
  <done>The deferred-cleanup document discharges two entries, opens four with their invariants stated, adjudicates every pair explicitly, and traces every carry-forward row to a named source with nothing dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| carry-forward inventory to future milestones | An omitted row is an obligation that silently disappears. |
| discharged entry to successor entry | A near-identical pair can be double-counted or double-dropped. |
| new planning documents to a live gate | A whole-tree walker scans both documents. |
| tag history to narration | Work inside the tag that no document explains is unaccountable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-62 | Repudiation | an obligation dropped from the carry-forward inventory | high | mitigate | Every row traces to one of two named sources, the open-item count from each source is recorded, and a diff-style comparison against the predecessor document proves no still-open entry was dropped. |
| T-153-63 | Spoofing | a discharged entry and its successor double-counted | medium | mitigate | Every pair carries an explicit relationship line choosing exactly one disposition, and the shared root cause is stated so the pair is legible. |
| T-153-64 | Information Disclosure | a tag carrying unnarrated work | high | mitigate | The style pass has its own audit section naming what it is, its tracked status, its commit count inside the push set and what remains open. |
| T-153-65 | Tampering | a recalled count in the traceability section | medium | mitigate | Every count is derived by a command at authoring time and the command is quoted beside the count. |
| T-153-66 | Denial of Service | a new planning document tripping a live gate | medium | mitigate | Both gates that scan the planning tree are run after each document is written and both must exit zero. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs. Two new planning documents. |

ASVS level: 1. Blocking threshold: high. Both high-severity entries are mitigated by computed
acceptance criteria — a source-traced inventory with per-source counts and a no-dropped-entry
comparison, and a dedicated narration section with derived figures.
</threat_model>

<verification>
- Both documents exist and both live planning gates exit 0 after each is written.
- The traceability section covers fifty-eight requirements with derived counts.
- Both discharges and at least four new entries are recorded.
- Every pair carries a single explicit disposition.
- No still-open predecessor entry was dropped.
</verification>

<success_criteria>
- The milestone audit narrates all nine phases, the three-axis evidence and the style pass
  (HARN-06, SC#5).
- The deferred-cleanup document is freshly authored but sourced from two named places, with nothing
  invented and nothing masked by deletion.
- The double-booking adjudication is run in writing with exactly one disposition per pair.
</success_criteria>

<output>
Create `.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-13-SUMMARY.md` when done.

Record: the chosen section ordering and that it was a discretionary choice; the derived requirement
counts with their commands; the two source open-item counts; the no-dropped-entry comparison; the
per-pair dispositions; and both gate results after each document.
</output>
