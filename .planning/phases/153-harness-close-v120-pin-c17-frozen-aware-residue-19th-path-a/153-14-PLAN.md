---
phase: 153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a
plan: 14
type: execute
wave: 11
depends_on: ["153-13"]
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md
autonomous: false
requirements: [HARN-05, HARN-06]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The publish bundle runs with the version flag PASSED EXPLICITLY. [MEASURED] the builder defaults the version to a literal three milestones old when the flag is absent, and the archive-name derivation accepts that default silently. A forgotten flag writes a misnamed archive over a reserved name in a gitignored directory with no recovery path (D-68)."
    - "BOTH archive filenames are verified after the run — the expected one present and the misnamed one absent. The output directory is gitignored, so the post-run check is the only proof that will ever exist (D-68)."
    - "NEITHER canary is bumped. [MEASURED] both sit at the same value already and a version-only regeneration touches neither (D-68)."
    - "The bundle runs BEFORE the close gate, not after. It performs hundreds of document conversions and exits non-zero on any failure; under the predecessor-but-one ordering that failure lands after an irreversible fifty-eight-requirement flip (D-55)."
    - "The close gate is a SINGLE COMMIT flipping ALL FIFTY-EIGHT requirements to validated across the project, roadmap, state and requirements documents. [MEASURED] fifty-eight is the sum of the eight category counts (D-70)."
    - "EVERY executor prompt touching the close gate carries the COMMIT-CONTRACT OVERRIDE — the default per-task commit destroys exactly this shape, which this repository learned in a recent phase (D-70)."
    - "A later, separate non-gate commit recording the post-gate apex result is permitted and expected (D-70)."
    - "The requirements document is NEVER removed at close — a chain member live-reads it outside the frozen-read mechanism (D-70)."
    - "Archival drift is two classes at two times. Class one was the pre-push static census. CLASS TWO can only manifest after the close gate rewrites the two live-read files, and the precedent proves the measurement is a POST-ARCHIVAL apex re-run — which happens after this phase, not inside the post-gate run (D-71)."
    - "The phase's own lifecycle is explicit: code review of the new validator files, the library helper and the workflow; user-acceptance testing; the milestone audit; then the archive-and-tag step as the owner-run step AFTER the phase. Two recent phases each shipped critical defects that only code review caught with every validator green, and this phase authors more new code than either (D-73)."
    - "The apex chain is run separately from any verification pass (D-77). No accepted-red disposition is available at this close (D-78)."
    - statement: "HARN-05 boundary edge: the archive name is derived from the version flag, so the boundary between correct and misnamed output is a single command-line argument. Both the expected and the misnamed filenames are checked after the run, not just the expected one."
      verification: explicit
    - statement: "HARN-06 empty edge: if the bundle produces zero conversion errors it still must produce a non-empty archive; the archive's existence and non-zero size are both asserted, because an empty archive would satisfy a bare existence check."
      verification: explicit
    - statement: "HARN-06 ordering edge: the close-gate commit is a single commit and the post-gate apex result is recorded in a separate later commit; the ordering between them is fixed and asserted by commit count, not by intent."
      verification: explicit
  artifacts:
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-14-SUMMARY.md
  key_links:
    - "The close gate rewrites the two files a chain member live-reads, which is why the second archival-drift class cannot be measured before it."
    - "The bundle exits non-zero on any conversion failure, so running it after an irreversible flip means discovering the failure with no way back."
    - "The output directory is gitignored, so nothing about the bundle run is recoverable from the repository afterwards."
  prohibitions:
    - "The publish bundle must never be invoked without an explicit version flag."
    - "Neither publish canary literal is edited."
    - "The requirements document is never removed."
    - "The close gate must not be split across more than one commit, and no task other than the gate task may commit any of the four gate files."
    - "No accepted-red disposition is created or invoked."
    - "No `docs/` content file is created, edited or deleted."
    - "Neither glossary frontmatter date field is touched."
    - "No summary, evidence file or commit message may claim a chain result gathered without the nested environment flag (D-75)."
    - "Commit message bodies must not literally quote governance-carve paths or the paths of amended files in prose — the exclusion check greps the message body and the file list together (D-76)."
    - "No bare-noun platform blockquote token at line start outside a fence in any `.planning/` file this plan writes (D-43)."
---

<objective>
Close the milestone: regenerate the publish bundle at this milestone's version, take the single
close-gate commit flipping all fifty-eight requirements, then record the post-gate apex result in a
separate commit and hand off the lifecycle steps that follow the phase.

Purpose: the ordering here is the whole point. The bundle runs first because it can fail and the flip
cannot be undone. The gate is one commit because that is the shape every close in this repository
takes and the shape a default per-task commit destroys.

Output: an archive at the right name, one gate commit, one post-gate record commit, and a named
handoff.
</objective>

<commit_contract>
**THIS OVERRIDES THE EXECUTOR'S DEFAULT PER-TASK COMMIT BEHAVIOUR. READ IT BEFORE TASK 1.**

- **Task 1 makes NO commit.** The publish bundle writes into a gitignored directory. There is nothing
  to commit and nothing may be committed.
- **Task 2 makes NO commit.** It is a decision checkpoint.
- **Task 3 makes EXACTLY TWO commits, in this order:**
  1. **The close gate — ONE commit, four files.** All fifty-eight requirement rows flipped to
     validated across the project, roadmap, state and requirements documents, in a single commit. Do
     not commit any of these four files in any earlier task. Do not split the flip across two commits.
     Do not fold any other file into it.
  2. **The post-gate record — a separate commit**, made after the confirmatory apex run, carrying the
     evidence file and the phase summary. This is explicitly permitted and expected, and it is not
     part of the gate.
- After Task 3, `git log --oneline -2` must show exactly these two commits in that order, and
  `git show --stat HEAD~1` must list exactly four files.
- This repository lost a phase's close shape once to the default per-task commit. The contract above
  is the fix, and it is not advisory.
</commit_contract>

## Artifacts this phase produces

Created by Phase 153, absent at the phase base; treat as new, not as unacknowledged references.

- **New exports in `scripts/validation/_lib/frozen-at-close.mjs`:** `MILESTONE_CLOSE_SHAS.V120`,
  `readAtV120Close`, `lsTreeAtV120Close`, `materializeDocsAtClose`, `withDocsAtClose`.
- **New validators:** `scripts/validation/check-phase-145.mjs` through `check-phase-152.mjs` and
  `scripts/validation/check-phase-153.mjs`.
- **New check ids:** `V-145-*` through `V-152-*`; on the apex `V-153-AUDIT`, `V-153-CHAIN-<n>`,
  `V-153-AUDIT-HARNESS`, `V-153-SELF`, `V-153-C17CARVEOUT-SUCCESSOR`.
- **New harness lineage:** `scripts/validation/v1.21-milestone-audit.mjs`,
  `scripts/validation/v1.21-audit-allowlist.json`, `BASELINE_25` in
  `scripts/validation/regenerate-supervision-pins.mjs`.
- **New CI workflow:** `.github/workflows/audit-harness-v1.21-integrity.yml`.
- **New planning artifacts:** `.planning/milestones/v1.21-MILESTONE-AUDIT.md`,
  `.planning/milestones/v1.21-DEFERRED-CLEANUP.md`, this phase's summaries, `153-EVIDENCE.md`,
  `153-VERIFICATION.md`, and the gitignored `dist/docs-library-v1.21.zip`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md
@.planning/milestones/v1.20-phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-12-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the publish bundle at this milestone's version and verify both filenames</name>
  <files>none tracked — the builder writes into a gitignored directory</files>
  <precondition>`scripts/pipeline/build-publish-bundle.mjs` exists and the document converter it shells out to is available on the path; the registry table it selects from is the committed one at the current head.</precondition>
  <read_first>
    - scripts/pipeline/build-publish-bundle.mjs around lines 36-45 — the version argument parsing and its fallback literal, and the archive-name derivation
    - scripts/pipeline/build-publish-bundle.mjs around lines 515-530 — the two self-test count assertions, and scripts/pipeline/build-filename-map.mjs around line 284 — the third. These are the canaries; read them to know what NOT to edit
    - .planning/STATE.md, the pending-todos block — the carried precondition from the previous phase's user-acceptance testing, which states this exact hazard and its owner ruling
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-55, D-68
  </read_first>
  <action>
Run the publish bundle **with the version flag passed explicitly**. Without it the builder falls back
to a literal three milestones old and the archive-name derivation accepts that silently, writing a
misnamed archive over a reserved name in a gitignored directory with no recovery path. The previous
phase's user-acceptance testing carried this forward as an explicit precondition for this phase
rather than fixing it in the builder, so the flag is the whole mitigation.

Run it now, before the close gate. It performs hundreds of document conversions and exits non-zero on
any failure; running it after the gate means discovering that failure after an irreversible flip.

After the run, verify **both** filenames: the expected archive present and non-empty, and the misnamed
archive absent. The output directory is gitignored, so this post-run check is the only proof that will
ever exist. List the directory and record the listing verbatim.

Do not touch either canary. Both already sit at the same value and a version-only regeneration touches
neither; editing one would be a change this phase has no reason to make.

Make no commit in this task.
  </action>
  <verify>
    <automated>node scripts/pipeline/build-publish-bundle.mjs --version=v1.21 &amp;&amp; test -s dist/docs-library-v1.21.zip &amp;&amp; test ! -e dist/docs-library-v1.17.zip &amp;&amp; ls -la dist/</automated>
  </verify>
  <acceptance_criteria>
    - The builder exits 0 and its conversion error count is zero; the run's summary output is quoted.
    - `dist/docs-library-v1.21.zip` exists and its size is greater than zero; the size is recorded.
    - `dist/docs-library-v1.17.zip` does not exist; the directory listing is recorded verbatim.
    - `git diff scripts/pipeline/build-publish-bundle.mjs scripts/pipeline/build-filename-map.mjs` is empty — neither canary was touched.
    - `git status --porcelain --untracked-files=all` shows no new tracked-directory changes attributable to this task.
    - No commit was made in this task.
  </acceptance_criteria>
  <reversibility rating="reversible">The archive is written into a gitignored directory and can be regenerated.</reversibility>
  <done>The archive exists at this milestone's name with a non-zero size, the misnamed archive is absent, neither canary was edited, and nothing was committed.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Take the close gate — a single commit flipping all fifty-eight requirement rows to validated — or hold, or record a criterion unmet.</decision>
  <context>
This is the one-way door. After the flip the milestone is closed, the archive-and-tag step follows,
and a post-archival apex re-run is the only remaining drift measurement. No accepted-red disposition
exists to absorb a late red: both were discharged and deleted at the predecessor close.

**Present, before asking:**

1. **The evidence in one screen** — the three axes at the one shared commit, the job-level totals with
   the skip classification, the six direct harness results, the apex triple from its own isolated
   invocation, and whether the single remediation round was spent.
2. **The bundle result** — the archive name, its size, and confirmation that the misnamed archive is
   absent. The output directory is gitignored, so this post-run check is the only proof that will ever
   exist.
3. **The two close artifacts** — both exist and both pass the two live planning-tree gates.
4. **What the gate does** — flips all fifty-eight requirement rows to validated in one commit across
   four documents. Derive the fifty-eight by counting the eight categories at the checkpoint and show
   the eight components; do not recall the number.
5. **That it is one-way**, and that the requirements document is never removed at close because a
   chain member live-reads it outside the frozen-read mechanism.
6. **That the bundle already ran** — deliberately before the gate, because it exits non-zero on any
   conversion failure and that failure must not land after an irreversible flip.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed — take the single close-gate commit</name>
      <pros>Closes the milestone on evidence that is complete and captured at one shared commit; unblocks the archive-and-tag step.</pros>
      <cons>Irreversible; the flip cannot be cleanly undone once the milestone is archived and tagged.</cons>
    </option>
    <option id="hold">
      <name>Hold — something in the evidence is not sufficient</name>
      <pros>Nothing irreversible happens while a gap is open; the specific insufficiency is named and can be closed.</pros>
      <cons>The milestone does not close until the named item is resolved and the evidence re-presented.</cons>
    </option>
    <option id="unmet">
      <name>Record a criterion unmet</name>
      <pros>Honest; no red is absorbed and no disposition is invented to hide it.</pros>
      <cons>The milestone does not ship this phase.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, hold, or unmet</resume-signal>
  <read_first>
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md — the three-axis results, the six direct harness results, the census and the glossary measurement
    - .planning/milestones/v1.21-MILESTONE-AUDIT.md and .planning/milestones/v1.21-DEFERRED-CLEANUP.md — the two close artifacts
    - .planning/REQUIREMENTS.md — all fifty-eight rows in their current state, and the eight category headings
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-70, D-78
  </read_first>
  <acceptance_criteria>
    - All six presentation items are put to the owner before the selection is requested.
    - The fifty-eight total and its eight per-category components are derived by a command at the checkpoint and quoted, not recalled.
    - The owner's selection is recorded verbatim in the evidence file.
    - No commit is made in this task.
    - If the selection is hold or unmet, the specific blocker is named and the gate task does not run.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: The SINGLE close-gate commit, then the post-gate confirmatory apex in a separate commit, then the lifecycle handoff</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-EVIDENCE.md</files>
  <precondition>Task 2 recorded a proceed decision from the owner.</precondition>
  <read_first>
    - the commit contract at the top of this plan — read it again before touching any file
    - .planning/REQUIREMENTS.md — all fifty-eight rows and the eight category headings
    - .planning/PROJECT.md, .planning/ROADMAP.md and .planning/STATE.md — the status surfaces the gate updates, including the milestone status line, the phase checkbox and the progress block
    - .planning/milestones/v1.20-phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-12-PLAN.md — the predecessor's close-gate task, for the commit shape and the subject-line form
    - scripts/validation/check-phase-54.mjs lines 28-34 and around line 347 — the two live reads the gate rewrites and the negative literal assertion over one of them
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-70, D-71, D-73, D-76, D-77
  </read_first>
  <action>
**The gate — one commit, four files.**

Flip all fifty-eight requirement rows to validated in the requirements document, and update the
milestone status surfaces in the project, roadmap and state documents so all four are consistent.
Derive the fifty-eight by counting the eight categories, not by recalling it. Stage exactly those four
files and make exactly one commit.

The subject line follows the established close form so the subject-line discriminator that recovers
close commits finds exactly one row for this milestone — the successor milestone will use it to pin
this close. Keep the version token and the close token both in the **subject**, not the body. The
commit message body must not quote a governance-carve path or an amended file path in prose; the
exclusion check greps the message body and the file list together, and the file list is already in the
output.

Never remove the requirements document. A chain member live-reads it outside the frozen-read
mechanism.

**The post-gate confirmatory apex — separate.**

After the gate commit, run the apex once more, in its own invocation, with nothing else in the
session. This proves the harness check is a real pass at the post-gate state. Record the triple.
Describe it accurately: it is a nested-propagating sweep by construction.

Also run the requirements-and-roadmap validator, because the gate just rewrote both files it live-reads
and the negative literal assertion over one of them is exactly the kind of thing a status flip can
trip.

Then commit the evidence file and the phase summary as a **separate** commit. This is permitted and
expected and is not part of the gate.

**The lifecycle handoff.** Record, as the closing section of the summary, what happens after this
phase and in what order: a code review of the new validator files, the library helper and the
workflow — two recent phases each shipped critical defects that only code review caught with every
validator green, and this phase authors more new code than either; then user-acceptance testing; then
the milestone audit; then the archive-and-tag step as the owner-run step **after** the phase. Name
explicitly that the second archival-drift class can only be measured by a **post-archival** apex
re-run, which happens after the archive step and not in the post-gate run above — the post-gate run
happens before archival and cannot see it.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-153.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `git show --stat HEAD~1` lists exactly four files: the project, roadmap, state and requirements documents.
    - `grep -c "^- \[ \]" .planning/REQUIREMENTS.md` returns 0 for the eight in-scope categories, and the count of validated rows equals fifty-eight, derived by command and quoted.
    - `git log --all --format="%H|%s" | awk -F'|' '$2 ~ /v1\.21/ && $2 ~ /MILESTONE CLOSE/'` returns exactly one row, and that row is the gate commit.
    - The gate commit's combined message-and-file-list output is inspected and contains no governance-carve path or amended file path quoted in prose.
    - `.planning/REQUIREMENTS.md` still exists and is tracked.
    - `node scripts/validation/check-phase-153.mjs` exits 0 after the gate commit; its triple is recorded and its harness check is a real pass, not a skip.
    - `node scripts/validation/check-phase-54.mjs` exits 0 after the gate commit — the two files it live-reads were both rewritten by the gate.
    - `git log --oneline -2` shows the post-gate record commit followed by the gate commit, in that order, and the post-gate commit does not contain any of the four gate files.
    - The lifecycle handoff section names all four following steps in order and states that the second archival-drift class is measured only by a post-archival re-run.
  </acceptance_criteria>
  <reversibility rating="one-way">The fifty-eight-requirement flip closes the milestone; the archive-and-tag step follows and there is no clean revert.</reversibility>
  <done>One four-file gate commit flipped all fifty-eight requirements, the post-gate apex and the live-read validator are both green, the record commit is separate, and the lifecycle handoff is written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command-line argument to output name | A single flag decides whether the archive is correctly or destructively named, in a gitignored directory. |
| close gate to live-read validator | The gate rewrites both files a chain member reads live, including one with a negative literal assertion. |
| executor default behaviour to close shape | The default per-task commit destroys the single-commit gate shape. |
| document converter to build process | Hundreds of documents are converted by an external binary during the bundle run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-67 | Tampering | a misnamed archive overwriting a reserved name | high | mitigate | The version flag is passed explicitly, and after the run both filenames are checked — expected present and non-empty, misnamed absent — with the directory listing recorded, because the directory is gitignored and no other proof exists. |
| T-153-68 | Denial of Service | a bundle failure landing after an irreversible flip | high | mitigate | The bundle runs before the gate. It exits non-zero on any conversion failure, and its zero-error result is an acceptance criterion of the task that precedes the checkpoint. |
| T-153-69 | Repudiation | a split or polluted close-gate commit | high | mitigate | The commit contract overrides the executor default, the gate is asserted to be exactly four files by a stat check, and the two-commit ordering is asserted by a log check. |
| T-153-70 | Spoofing | a close commit the successor's discriminator cannot find | high | mitigate | The subject line carries both tokens in the subject rather than the body, and the discriminator is run after the commit and must return exactly one row. |
| T-153-71 | Elevation of Privilege | an external converter invoked over hundreds of documents | medium | mitigate | The builder is unchanged by this phase and is invoked with a single version flag; no new binary, no new version pin, and no user-supplied path enters the invocation. |
| T-153-72 | Information Disclosure | the second archival-drift class going unmeasured | medium | mitigate | The lifecycle handoff states explicitly that the second class is measured only by a post-archival re-run, which happens after the archive step and not in the post-gate run. |
| T-153-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs. The bundle uses an already-installed document converter at an unchanged version. |

ASVS level: 1. Blocking threshold: high. All four high-severity entries are mitigated by computed
acceptance criteria — a two-filename check with a recorded listing, an ordering that puts the failable
step first, a four-file stat assertion, and a post-commit discriminator returning exactly one row.
</threat_model>

<verification>
- The archive exists at this milestone's name with non-zero size and the misnamed archive is absent.
- Neither canary was edited.
- The gate is exactly one commit of exactly four files, and all fifty-eight rows are validated.
- The subject-line discriminator returns exactly one row for this milestone.
- `node scripts/validation/check-phase-153.mjs` and `node scripts/validation/check-phase-54.mjs` both
  exit 0 after the gate.
- The post-gate record commit is separate and carries none of the four gate files.
</verification>

<success_criteria>
- The publish bundle is regenerated at this milestone's version with both filenames verified
  (HARN-05, SC#4).
- The close gate is a single four-file commit flipping all fifty-eight requirements (HARN-06).
- The post-gate apex proves the harness check is a real pass, recorded in a separate commit.
- The lifecycle handoff names code review, user-acceptance testing, the milestone audit and the
  archive-and-tag step, and identifies the post-archival re-run as the only measurement of the second
  archival-drift class.
</success_criteria>

<output>
Create `.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-14-SUMMARY.md` when done, and also produce `153-VERIFICATION.md` for the phase.

Record: the bundle run output, the archive size and the directory listing; the owner's close decision;
the derived fifty-eight count with its eight components; the gate commit's stat output; the
discriminator result; the post-gate apex and live-read validator results; the two-commit log; and the
lifecycle handoff.
</output>
