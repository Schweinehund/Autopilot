---
phase: 153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a
plan: 05
type: execute
wave: 4
depends_on: ["153-04"]
files_modified:
  - .github/workflows/audit-harness-v1.21-integrity.yml
autonomous: true
requirements: [HARN-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The eighteenth workflow is authored by a MECHANICAL PER-JOB DIFF of the predecessor workflow against an explicit literal inventory, never by prose instruction (D-50)."
    - "The harness-run job's display name AND its run command are both repointed — it is the only job that executes the harness, and copied verbatim the workflow reports green having never run the new one (D-50)."
    - "ALL THREE sidecar references are repointed, including the third one inside the quarterly job's inline node script; that job almost never runs, so a verbatim copy there is undetectable by the close evidence (D-50)."
    - "ALL FIVE display-name literals are repointed, because evidence is matched on the display-name field and uncorrected names both mislabel this milestone's evidence and collide with the predecessor's in a blended run listing (D-50, D-58)."
    - "The chain job's display name AND its run command are both repointed, not only its notice line — a notice-only fix leaves the chain job running the predecessor apex while the standalone job runs the new one, silently voiding the dual-apex contract the header asserts (D-50)."
    - "ALL SIX path-filter entries are repointed, including the workflow's own self-reference (D-50)."
    - "The path-match grep literal is repointed; copied verbatim it exits non-zero (D-50)."
    - "BOTH cron literals are carried VERBATIM. The quarterly job's guard is a string equality against its cron literal, so any stagger makes that job permanently unreachable — a green-looking permanent skip that the skips-are-gaps rule cannot see, because the anchor count would still be correct (D-51)."
    - "The dual-apex header contract is restated in full; every checkout carries full fetch depth; chain jobs carry the 30-minute budget and leaf jobs the 15-minute budget. These are ratified constants, not values to re-measure (D-52)."
    - "The workflow carries its OWN apex job and eight standalone leaf jobs, and no job for the predecessor apex — the Path-A rule is carry your own apex, never the predecessor's. Eight separate leaf jobs cost parallel minutes rather than wall-clock, and a grouped job hides which leaf failed (D-54)."
    - "The advisory pin-helper job is carried and explicitly labelled NON-EVIDENCE, never converted into a gate: its continue-on-error setting, its trailing true, and its echo fallback make its conclusion structurally always successful, and the instrument it would gate on is the one this phase bars from being cited as proof (D-54, D-48)."
    - "Two predecessor-validator assertions bind this workflow and appear in no roadmap text: a probe job with no dependency key must exist in EVERY audit-harness workflow, and the checkout-step count must EQUAL the full-fetch-depth count with comment lines stripped — a presence test would pass where an equality fails (D-53)."
    - statement: "HARN-05 concurrency edge (probe row, resolved here): this workflow joins a set of eighteen that share both cron windows, and every job in it except the probe job depends transitively on the harness job. A harness failure yields one visible red plus a fan-out of silent skips. The fan-out size is recorded in the workflow header so the evidence plan can count it."
      verification: explicit
    - statement: "HARN-05 boundary edge: the quarterly job's schedule guard is an exact string equality against its cron literal; a single character of stagger makes it unreachable forever. The literal is asserted byte-identical to the predecessor's."
      verification: explicit
  artifacts:
    - .github/workflows/audit-harness-v1.21-integrity.yml
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-05-SUMMARY.md
  key_links:
    - "Evidence is matched on the display-name field rather than the job key, so display names are an evidence contract and not cosmetic."
    - "The chain job and the standalone apex job both run an apex; if they run different apexes the dual-apex contract in the header becomes false while both jobs stay green."
    - "The predecessor-validator equality between checkout steps and full-fetch-depth declarations means a checkout added without its options block fails a gate rather than merely running shallow."
  prohibitions:
    - "Neither cron literal may be staggered, reworded or re-spaced."
    - "No job for the predecessor milestone's apex appears in this workflow."
    - "The advisory pin-helper job must not be converted into a gate, and its result must not be cited as evidence."
    - "No predecessor workflow file is edited by this plan."
    - "No checkout step may omit its full-fetch-depth option."
    - "No `docs/` content file is created, edited or deleted."
    - "No requirement status row is flipped by this plan."
    - "Neither glossary frontmatter date field is touched."
    - "No summary or commit message may claim a chain result gathered without the nested environment flag (D-75)."
    - "Commit message bodies must not literally quote governance-carve paths or the paths of amended files in prose (D-76)."
    - "No frozen validator line is edited before grepping the validator tree for that exact call-site string (D-79)."
    - "No bare-noun platform blockquote token at line start outside a fence in any `.planning/` file this plan writes (D-43)."
---

<objective>
Author the eighteenth parallel CI workflow as a mechanical per-job diff of the seventeenth, born with
full fetch depth on every checkout, carrying its own apex and eight standalone leaf jobs.

Purpose: this workflow is one third of the terminal evidence. Its display names are the field the
evidence is matched on, its harness job is the only place the new harness runs in CI, and two
predecessor-validator assertions bind it the moment it exists.

Output: one new workflow file with sixteen jobs, both crons verbatim, and zero surviving operational
references to the predecessor milestone.
</objective>

<literal_inventory>
**The workflow is authored against this inventory, job by job. Every row is a literal that must be
repointed or deliberately carried; a prose instruction to "update references" is what let the
predecessor's inventory go under-specified.**

| Region | Predecessor line | Disposition |
|---|---|---|
| Header comment block | 3-28 | Restate for this milestone: the dual-apex contract in full, the fan-out warning with its recomputed dependent-job count, the advisory non-evidence note |
| Workflow display name | 31 | Repoint |
| Path filter entries | 36-40 | Repoint all — including the workflow's own self-reference at 38 |
| Weekly cron | 42 | **Carry verbatim** |
| Quarterly cron | 43 | **Carry verbatim** — the quarterly job guards on a string equality against it |
| Parse job display name | 48 | Repoint |
| Sidecar reference 1 (parse) | 59 | Repoint |
| Path-match job display name | 68 | Repoint |
| Sidecar reference 2 (path-match grep) | 76 | Repoint — copied verbatim the grep exits non-zero |
| Harness-run job display name | 84 | Repoint |
| Harness-run step name and run command | 92-93 | Repoint — the only job that executes the harness |
| Chain job display name | 96 | Repoint |
| Chain job step name and run command | 108-111 | Repoint **both**, plus the notice line inside the run block |
| Leaf jobs | 115-183 | Replace the five predecessor leaf jobs with **eight** leaf jobs for this milestone's phases |
| Standalone apex job | 184-196 | Replace with this milestone's apex job; **no predecessor apex job survives** |
| Sidecar reference 3 (quarterly inline script) | 216 | Repoint — sits in an always-skipping job and is undetectable by close evidence |
| Quarterly job schedule guard | 202 | **Carry verbatim** with its cron literal |
| Advisory pin-helper job | 232-249 | Carry, labelled NON-EVIDENCE |
| Probe job | 250-279 | Carry, and it must keep having **no** dependency key |
</literal_inventory>

## Artifacts this phase produces

Created by Phase 153, absent at the phase base; treat as new, not as unacknowledged references.

- **New exports in `scripts/validation/_lib/frozen-at-close.mjs`:** `MILESTONE_CLOSE_SHAS.V120`,
  `readAtV120Close`, `lsTreeAtV120Close`, `materializeDocsAtClose`, `withDocsAtClose`.
- **New validators:** `scripts/validation/check-phase-145.mjs` through `check-phase-152.mjs` and
  `scripts/validation/check-phase-153.mjs`.
- **New check ids:** `V-145-*` through `V-152-*`; on the apex `V-153-AUDIT`, `V-153-CHAIN-<n>`,
  `V-153-AUDIT-HARNESS`, `V-153-SELF`, `V-153-C17CARVEOUT-SUCCESSOR`.
- **New harness lineage:** `scripts/validation/v1.21-milestone-audit.mjs`,
  `scripts/validation/v1.21-audit-allowlist.json`, `BASELINE_25` in
  `scripts/validation/regenerate-supervision-pins.mjs`.
- **New CI workflow:** `.github/workflows/audit-harness-v1.21-integrity.yml` with jobs `parse`,
  `path-match`, `harness-run`, `linux-chain-ubuntu-latest`, `check-phase-145` through
  `check-phase-152`, `check-phase-153`, `rotting-external-quarterly`, `pin-helper-advisory`,
  `frozen-read-probe`.
- **New planning artifacts:** `.planning/milestones/v1.21-MILESTONE-AUDIT.md`,
  `.planning/milestones/v1.21-DEFERRED-CLEANUP.md`, this phase's summaries, `153-EVIDENCE.md`,
  `153-VERIFICATION.md`, and the gitignored `dist/docs-library-v1.21.zip`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-PATTERNS.md
@.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-04-SUMMARY.md
@.github/workflows/audit-harness-v1.20-integrity.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header, triggers and the three pre-chain jobs — both crons verbatim, both early sidecar references repointed</name>
  <files>.github/workflows/audit-harness-v1.21-integrity.yml</files>
  <read_first>
    - .github/workflows/audit-harness-v1.20-integrity.yml lines 1-95 — the whole header comment block, the trigger block with its six path entries and two crons, and the parse, path-match and harness-run jobs in full including the inline node script in parse and the grep in path-match
    - scripts/validation/v1.21-milestone-audit.mjs — the harness path and its usage flags, and the sidecar path literal it now reads
    - scripts/validation/v1.21-audit-allowlist.json — the sidecar's top-level key names, which the parse job's inline script validates
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-50, D-51, D-52
  </read_first>
  <action>
Create `.github/workflows/audit-harness-v1.21-integrity.yml` and author its first three jobs against
the inventory above.

**Header.** Restate the dual-apex contract in full for this milestone: the chain job and the
standalone apex job both run the same apex recursion, and the span they cover. Restate the fan-out
warning with a **recomputed** dependent-job count for this workflow — every job except the probe job
depends on the harness job directly or transitively, so state how many jobs go silently skipped when
the harness job fails. Restate the advisory non-evidence note. Every count in the header is derived
from this file's own job list, not carried.

**Triggers.** Repoint all six path-filter entries, including the workflow's own self-reference — a
workflow that does not list itself does not re-run when it is edited. Carry **both** cron literals
byte-identically; the quarterly job later guards on a string equality against the second one.

**Parse job.** Repoint its display name and the sidecar path inside its inline node script. Keep the
validation logic; only the path changes.

**Path-match job.** Repoint its display name and the grep literal. The grep asserts the harness
references the sidecar by path; copied verbatim it searches for the predecessor path in the new
harness and exits non-zero.

**Harness-run job.** Repoint its display name, its step name and its run command. This is the only
job in the workflow that executes the harness — a verbatim copy produces a workflow that reports
green having never run the new harness at all.

Every checkout step in all three jobs carries the full-fetch-depth option. This is a ratified
constant, not a value to re-measure.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const y=fs.readFileSync('.github/workflows/audit-harness-v1.21-integrity.yml','utf8');const co=(y.match(/uses: actions\/checkout/g)||[]).length;const fd=(y.split('\n').filter(l=>!l.trim().startsWith('#')).join('\n').match(/fetch-depth: 0/g)||[]).length;if(co===0||co!==fd){console.error('checkout '+co+' != fetch-depth '+fd);process.exit(1)}console.log('checkout/fetch-depth parity: '+co)"</automated>
  </verify>
  <acceptance_criteria>
    - The file parses as YAML: `node -e "require('node:child_process')"` is not sufficient — use a real parse, for example `python -c "import yaml,sys;yaml.safe_load(open('.github/workflows/audit-harness-v1.21-integrity.yml'))"` or an equivalent Node YAML parse, and it must exit 0.
    - The checkout-step count equals the full-fetch-depth count with comment lines stripped, proved by the verify command.
    - `grep -c "0 8 \* \* 1" .github/workflows/audit-harness-v1.21-integrity.yml` returns 1, and a byte-comparison of both cron lines against the predecessor's shows them identical.
    - `grep -c "audit-harness-v1.21-integrity.yml" .github/workflows/audit-harness-v1.21-integrity.yml` returns at least 1 — the self-reference is present in the path filter.
    - `grep -c "v1.20-audit-allowlist.json" .github/workflows/audit-harness-v1.21-integrity.yml` returns 0 for the two jobs authored in this task.
    - The harness-run job's run command names `scripts/validation/v1.21-milestone-audit.mjs`.
    - The path-match grep literal names the new sidecar; run the same grep locally against the new harness and confirm it exits 0.
    - The header states a dependent-job fan-out count derived from this file's job list.
  </acceptance_criteria>
  <reversibility rating="reversible">A new workflow file with no dispatch yet; deletable without consequence.</reversibility>
  <done>The workflow exists with a restated header, six repointed path entries, two verbatim crons, and three pre-chain jobs whose display names, sidecar references and run commands all name this milestone.</done>
</task>

<task type="auto">
  <name>Task 2: The chain job, eight standalone leaf jobs and this milestone's own apex job</name>
  <files>.github/workflows/audit-harness-v1.21-integrity.yml</files>
  <read_first>
    - .github/workflows/audit-harness-v1.20-integrity.yml lines 95-197 — the chain job with its autocrlf-disable step, its checkout, its notice line and its apex run command; the five leaf-job blocks; and the standalone apex job with its display name naming the span it spawns
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-50, D-52, D-54
    - .planning/ROADMAP.md Phase 153 — the eight phases whose leaves this workflow runs, and the apex's span
  </read_first>
  <action>
Author the chain job, eight leaf jobs and the apex job.

**Chain job.** Repoint its display name, its step name, its run command and the notice line inside the
run block — all four, not only the notice. A notice-only fix leaves this job running the predecessor
apex while the standalone apex job runs the new one, which silently makes the dual-apex contract in
the header false while both jobs stay green. Keep the autocrlf-disable step ahead of the checkout,
keep the 30-minute budget, and keep the job non-continue-on-error.

**Leaf jobs.** Replace the five predecessor leaf jobs with **eight**, one per content phase this
milestone shipped, each with its own job key, its own display name, its own checkout with full fetch
depth, the 15-minute budget, and a run command naming that phase's validator. Eight separate jobs
rather than one grouped job: the cost is parallel CI minutes, not wall-clock, and a grouped job hides
which leaf failed.

**Apex job.** Author this milestone's apex job with the 30-minute budget and a display name naming the
span it spawns. **No job for the predecessor apex appears in this workflow** — the Path-A rule is
carry your own apex, never the predecessor's, and the predecessor workflow itself carries its own and
not its predecessor's.

Every checkout in every job added here carries the full-fetch-depth option.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const y=fs.readFileSync('.github/workflows/audit-harness-v1.21-integrity.yml','utf8');const co=(y.match(/uses: actions\/checkout/g)||[]).length;const fd=(y.split('\n').filter(l=>!l.trim().startsWith('#')).join('\n').match(/fetch-depth: 0/g)||[]).length;if(co!==fd){console.error('checkout '+co+' != fetch-depth '+fd);process.exit(1)}const leaves=(y.match(/check-phase-1(4[5-9]|5[0-2])\.mjs/g)||[]).length;if(leaves<8){console.error('leaf run commands: '+leaves);process.exit(1)}console.log('parity '+co+', leaf refs '+leaves)"</automated>
  </verify>
  <acceptance_criteria>
    - The file still parses as YAML and the checkout-to-fetch-depth equality still holds.
    - Exactly eight leaf job keys exist, one per content phase 145 through 152, each with a distinct display name and a run command naming its own validator.
    - `grep -c "check-phase-153.mjs" .github/workflows/audit-harness-v1.21-integrity.yml` returns at least 2 — once in the chain job's run command and once in the standalone apex job's run command.
    - `grep -c "check-phase-144" .github/workflows/audit-harness-v1.21-integrity.yml` returns 0.
    - The chain job's display name, step name, run command and notice line all name the new apex; a grep for the predecessor apex anywhere in the chain job returns nothing.
    - Every leaf job carries the 15-minute budget and both apex-running jobs carry the 30-minute budget.
    - The autocrlf-disable step precedes the checkout in the chain job.
  </acceptance_criteria>
  <reversibility rating="reversible">Job blocks in an undispatched workflow file.</reversibility>
  <done>The chain job and the standalone apex job run the same new apex, eight leaf jobs run this milestone's eight leaves individually, and no predecessor apex job survives.</done>
</task>

<task type="auto">
  <name>Task 3: The tail jobs, the third sidecar reference, and the two predecessor-validator assertions</name>
  <files>.github/workflows/audit-harness-v1.21-integrity.yml</files>
  <read_first>
    - .github/workflows/audit-harness-v1.20-integrity.yml lines 198-279 — the quarterly job with its schedule guard, its pinned link-checker install and its inline node script reading the sidecar; the advisory pin-helper job with its continue-on-error setting, its trailing true and its echo fallback; and the probe job, which has no dependency key
    - scripts/validation/check-phase-139.mjs — the probe-job assertion and the checkout-to-fetch-depth equality assertion in full. Read the equality's comment-stripping behaviour before finalising the file
    - .planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-CONTEXT.md — D-50, D-51, D-53, D-54, D-48
  </read_first>
  <action>
Author the three tail jobs, then run the assertions that bind this file.

**Quarterly job.** Carry its schedule guard with the cron literal byte-identical to the trigger
block's second cron — the guard is a string equality and any stagger makes the job permanently
unreachable, a green-looking permanent skip that the skips-are-gaps rule structurally cannot see
because the legitimate-skip anchor count would still be correct. Repoint the **third sidecar
reference**, which lives inside this job's inline node script. This job almost never runs, so a
verbatim copy here is invisible to the close evidence — that is exactly why it is called out.

**Advisory pin-helper job.** Carry it with its continue-on-error setting, its trailing true and its
echo fallback intact, and label it NON-EVIDENCE in a comment on the job. Do not convert it into a
gate: its conclusion is structurally always successful, and the instrument it would gate on is the
one this phase bars from being cited as pin-drift proof.

**Probe job.** Carry it, and confirm it still has **no** dependency key. A predecessor validator
requires a probe job with no dependency key in every workflow of this family; adding one to make the
graph tidier breaks that assertion.

**Then run the two binding assertions.** Run the predecessor validator that owns them and confirm
both pass against the new file. The fetch-depth assertion is an **equality** between the checkout-step
count and the full-fetch-depth count with comment lines stripped, not a presence test: a checkout
added without its options block fails it. Prove the equality is failable by temporarily removing one
options block, confirming the assertion fails, and restoring.

Finally, audit the whole file for surviving predecessor-milestone literals and enumerate every
remaining hit with a justification. Deliberate lineage-history references in the header are
acceptable; an operational literal is not.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-139.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-139.mjs` exits 0, and its probe-job and fetch-depth lines both report PASS.
    - The failability probe is recorded: with one options block removed the fetch-depth assertion FAILS; restored, it passes. Both outputs are quoted.
    - `grep -c "v1.20-audit-allowlist.json" .github/workflows/audit-harness-v1.21-integrity.yml` returns 0 — all three sidecar references are repointed, including the one inside the quarterly job.
    - The quarterly job's schedule-guard cron string is byte-identical to the trigger block's quarterly cron, proved by extracting both and comparing.
    - The probe job block contains no dependency key.
    - The advisory job retains its continue-on-error setting, its trailing true and its echo fallback, and carries a NON-EVIDENCE comment.
    - A full-file audit of surviving predecessor-milestone literals is recorded, with every remaining hit justified as a deliberate lineage-history reference.
    - The file parses as YAML and the checkout-to-fetch-depth equality holds over the complete file.
    - The workflow declares sixteen jobs; the count is derived from the file and quoted.
  </acceptance_criteria>
  <reversibility rating="reversible">Job blocks in an undispatched workflow file.</reversibility>
  <done>All three tail jobs are correct, all three sidecar references name this milestone, both binding assertions pass and the fetch-depth equality is proved failable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow to repository token | A CI workflow runs with a repository token; its permissions determine what a compromised step can do. |
| workflow to third-party actions | Checkout, node setup and a pinned link checker are external code executed with repository access. |
| schedule guard to job reachability | A string-equality guard silently disables a job when its literal drifts. |
| display name to evidence | Evidence is matched on the display-name field; a wrong name misattributes a result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-21 | Elevation of Privilege | the new workflow's token scope | high | mitigate | The workflow is a per-job diff of an existing one that declares no elevated permissions and writes nothing back to the repository — every job checks out and runs a local Node validator. No `permissions:` escalation, no secrets, and no step that pushes, comments or dispatches is introduced. Confirmed by diffing the new file's job bodies against the predecessor's. |
| T-153-22 | Tampering | third-party actions and the pinned link checker | medium | mitigate | Action references and the link-checker version pin are carried verbatim from the predecessor rather than floated to a newer tag, so this plan introduces no new external dependency and no unpinned version. |
| T-153-23 | Spoofing | display-name literals | high | mitigate | All five display names are repointed and enumerated in the inventory; a full-file audit records every surviving predecessor literal with a justification, so a name collision in a blended run listing cannot pass silently. |
| T-153-24 | Denial of Service | a permanently unreachable quarterly job | medium | mitigate | Both crons are carried byte-identically and the guard literal is compared against the trigger literal by extraction, not by eye. |
| T-153-25 | Repudiation | an advisory job read as evidence | medium | mitigate | The job is labelled NON-EVIDENCE in the file itself, and the plan states that its conclusion is structurally always successful. |
| T-153-26 | Information Disclosure | a workflow that reports green having run nothing | high | mitigate | The harness-run job's run command, the chain job's run command and the standalone apex job's run command are each asserted by grep to name this milestone's artifacts, and the predecessor apex is asserted absent. |
| T-153-SC | Tampering | npm/pip/cargo installs | medium | accept | The quarterly job installs a link checker globally at a pinned version, carried verbatim from the predecessor workflow. No new package is introduced by this plan and no version is floated, so no new legitimacy audit is triggered; the existing pin is inherited. |

ASVS level: 1. Blocking threshold: high. All three high-severity entries are mitigated by computed
acceptance criteria — a permissions-and-body diff against the predecessor, a full-file literal audit,
and grep assertions on the three run commands.
</threat_model>

<verification>
- `node scripts/validation/check-phase-139.mjs` exits 0 with its probe-job and fetch-depth lines PASS.
- The workflow parses as YAML and declares sixteen jobs.
- Checkout-step count equals full-fetch-depth count with comment lines stripped.
- Zero references to the predecessor sidecar or the predecessor apex remain.
- Both cron literals are byte-identical to the predecessor's.
</verification>

<success_criteria>
- The eighteenth workflow is born with full fetch depth on every checkout (HARN-05, SC#4).
- It carries its own apex and eight standalone leaf jobs and no predecessor apex job.
- All three sidecar references and all five display names are repointed.
- Both crons are verbatim and the quarterly guard matches its trigger literal exactly.
</success_criteria>

<output>
Create `.planning/phases/153-harness-close-v120-pin-c17-frozen-aware-residue-19th-path-a/153-05-SUMMARY.md` when done.

Record: the completed literal inventory with each row's disposition; the derived job count and
fan-out count; the cron byte-comparison; the fetch-depth failability probe; the binding-assertion
results; and the full-file audit of surviving predecessor literals with justifications.
</output>
