---
phase: 65-apple-business-l1-l2-hub-navigation-integration
plan: 04
type: execute
wave: 4
depends_on: ["65-01", "65-02", "65-03"]
files_modified:
  - docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md
  - scripts/validation/v1.6-audit-allowlist.json
  - scripts/validation/check-phase-64.mjs
autonomous: true
requirements: [ABNAV-01, ABNAV-02, ABNAV-03, ABNAV-04, ABNAV-05, ABNAV-06, ABNAV-07]

must_haves:
  truths:
    - "C16 4-edge triangle is LIVE with ZERO exemptions: l1_34 → admin_12, admin_12 → l1_34, common_issues → quick_ref_l1, quick_ref_l1 → l1_34 all pass via real substring presence (no allowlist masking)"
    - "12-shared-ipad-passcode-reset.md `## Cross-References` tail contains the new L1 #34 back-link bullet with substring `34-apple-business` — the canonical admin-context doc reciprocally cross-links the L1 entry-point"
    - "v1.6-audit-allowlist.json `c16_missing_endpoint_exemptions` array is EMPTY (length 0) — all 4 sunset-65 entries removed"
    - "check-phase-64.mjs V-64-05 is RECONCILED — assertion flipped from NEGATIVE (must NOT contain) to POSITIVE (MUST contain); old failure-detail string is absent"
    - "All three sub-actions land in ONE indivisible commit (D-04a + 62-08-PLAN §464-465 atomic-reconciliation contract); any partial state leaves the validator chain RED"
    - "Post-commit: check-phase-64.mjs still exits 0 (V-64-05 [RECONCILED] now PASSES via positive assertion); check-phase-65.mjs reports V-65-12 + V-65-13 + V-65-14 ALL PASS; v1.6-milestone-audit.mjs reports C16 PASS with 0 EXEMPTED entries for the 4 sunset-65 paths"
  artifacts:
    - path: "docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md"
      provides: "Canonical admin-context doc with reciprocal L1 #34 back-link in `## Cross-References` tail; ABAUDIT-06/07 line-pair positions unaffected"
      contains: "34-apple-business"
    - path: "scripts/validation/v1.6-audit-allowlist.json"
      provides: "Allowlist with `c16_missing_endpoint_exemptions` array emptied (4 sunset-65 entries removed atomically)"
      contains: "c16_missing_endpoint_exemptions"
    - path: "scripts/validation/check-phase-64.mjs"
      provides: "V-64-05 RECONCILED — positive assertion replacing the negative one; test ID preserved for chain output continuity"
      contains: "RECONCILED"
  key_links:
    - from: "docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md"
      to: "docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md"
      via: "Cross-References bullet append at `:194` (relative path `../../l1-runbooks/`); C16 edge admin_12 → l1_34"
      pattern: "34-apple-business"
    - from: "scripts/validation/v1.6-audit-allowlist.json"
      to: "scripts/validation/v1.6-milestone-audit.mjs"
      via: "Removed 4 endpoint exemptions; C16 substring checks now run against real file content"
      pattern: "c16_missing_endpoint_exemptions"
    - from: "scripts/validation/check-phase-64.mjs"
      to: "docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md"
      via: "V-64-05 [RECONCILED] asserts the back-link landed (positive)"
      pattern: "RECONCILED"
---

<objective>
**ATOMIC TRIO — one task, one commit, INDIVISIBLE.** Land all three sub-actions of the V-64-05 ↔ C16 reconciliation in a SINGLE git commit per D-04a + 62-08-PLAN §464-465 + 65-CONTEXT.md `code_context` ATOMIC RECONCILIATION block. Any partial application leaves the validator chain in a non-zero exit state — Pitfall 1 from 65-RESEARCH.md.

Purpose: Close the C16 4-edge cross-link integrity triangle (l1_34 ↔ admin_12 ↔ common_issues ↔ quick_ref_l1). The l1_34 → admin_12 edge was placed in Wave 2 (L1 #34 contains `12-shared-ipad-passcode-reset`); the common_issues → quick_ref_l1 + quick_ref_l1 → l1_34 edges were placed in Wave 3 (hub appends). What remains is the admin_12 → l1_34 reverse edge, which requires (a) appending `34-apple-business` substring inside `12-`, (b) removing the 4 sunset-65 allowlist exemptions that have been masking the still-pending edges, and (c) flipping the check-phase-64.mjs V-64-05 assertion from "must NOT contain 34-apple-business" (its Phase-64-close form) to "MUST contain 34-apple-business" (its Phase-65-close form). All three sub-actions are mutually-required: any partial state leaves the chain RED (either V-64-05 fails because back-link landed but flip didn't, or C16 fails because exemption removed but back-link didn't land, or chain validator output is misleading because flip happened without the substring being live).
Output: 3 files modified — `docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md` (back-link bullet append + version history row), `scripts/validation/v1.6-audit-allowlist.json` (4 entries removed from `c16_missing_endpoint_exemptions`), `scripts/validation/check-phase-64.mjs` (V-64-05 block replaced at `:135-145`). **ONE commit covering all three.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-CONTEXT.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-RESEARCH.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-PATTERNS.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-VALIDATION.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-CONVENTIONS.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-ANCHOR-INVENTORY.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-01-SUMMARY.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-02-SUMMARY.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-03-SUMMARY.md

<interfaces>
<!-- Sub-action 1 target: 12- canonical admin-context doc. Lines 187-201 contain the existing
     `## Cross-References` tail (3 bullets) + `## Version History`. ABAUDIT-06 line 13 + ABAUDIT-07
     line 116 line-pair exemptions exist; the Wave-4 append at `:194` does not shift them. -->
@docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md

<!-- Sub-action 2 target: 4 entries to remove from c16_missing_endpoint_exemptions array. -->
@scripts/validation/v1.6-audit-allowlist.json

<!-- Sub-action 3 target: V-64-05 block at lines 135-145 to replace with the POSITIVE assertion. -->
@scripts/validation/check-phase-64.mjs

<!-- Harness — C16 edge map at :771-803 reads c16_missing_endpoint_exemptions; substring check
     at :798 must now find `34-apple-business` in 12- once the exemption is removed. -->
@scripts/validation/v1.6-milestone-audit.mjs

<!-- L1 #34 from Wave 2 — the back-link points to this file's relative path `../../l1-runbooks/`. -->
@docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md

EXACT atomic-trio content (from 65-PATTERNS.md "Atomic-Trio Patterns (D-04a — INDIVISIBLE UNIT)"):

Sub-Action 1 — back-link bullet to append at `12-:194` (after last existing bullet, before `## Version History`):

```
- **L1 runbook:** [L1 #34 — Apple Business Shared iPad Passcode Reset](../../l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md) (Path A L1-delegated entry point)
```

Relative path verification (Pitfall 6): from `docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md`, two directories up is `docs/`, then into `l1-runbooks/` — so `../../l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` is correct.

Sub-Action 1 also appends a Version History row to `12-`:

```
| 2026-05-22 | Phase 65 plan 65-04: added L1 #34 back-link to `## Cross-References` tail per D-04a + C16 atomic-reconciliation contract (62-08-PLAN §464-465); removed admin_12 C16 exemption from allowlist | -- |
```

Sub-Action 2 — remove ALL 4 entries from `v1.6-audit-allowlist.json` `c16_missing_endpoint_exemptions` array. Target state: `"c16_missing_endpoint_exemptions": []` (empty array). The 4 entries to remove (per 65-RESEARCH.md §"Current Allowlist Exemptions"; verbatim file paths from 65-PATTERNS.md "Sub-Action 2"):
- `docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` (sunset_phase: "65")
- `docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md` (sunset_phase: "64-65")
- `docs/common-issues.md#apple-business-governance-failure-scenarios` (sunset_phase: "65")
- `docs/quick-ref-l1.md#apple-business-quick-reference` (sunset_phase: "65")

Sub-Action 3 — replace `check-phase-64.mjs:135-145` V-64-05 block. EXACT current code (verified at `:135-145` per 65-RESEARCH.md §"V-64-05 Exact Current Code") to find and replace; EXACT replacement code from 65-PATTERNS.md "Sub-Action 3" / 65-RESEARCH.md §"Recommended Reconciliation Approach". Test ID `V-64-05` is preserved with `[RECONCILED Phase 65]` label so Phase 66 chain output still names the assertion. The assertion BODY flips from `if (has34) return { pass: false ... }; return { pass: true ... };` to `if (!has34) return { pass: false ... }; return { pass: true ... };`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-atomic-trio state ↔ post-atomic-trio state | The triangle is in a transient invalid state DURING execution; the atomic commit collapses pre→post in one step. Mid-state would be RED. |
| 12- ABAUDIT line-pair regions (line 13 + line 116) ↔ tail-append at line 194 | The append occurs after both ABAUDIT comments; their `i`/`i+1` line-pair semantics are unaffected (65-RESEARCH.md:705). |
| Allowlist exemptions ↔ live corpus content | Removing exemptions exposes the substring check to real file content; the substring MUST be present at the same commit boundary. |
| Phase 64 validator semantics ↔ Phase 65 corpus state | V-64-05's NEGATIVE-to-POSITIVE flip is the contract that the Phase 64 close-state and the Phase 65 close-state both validate cleanly; the flip happens IN this commit so neither chain validator goes red. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-16 | Tampering | Partial atomic application (sub-action 1 lands without sub-action 3) | mitigate | All three sub-actions are performed in a single executor session BEFORE the commit; the `git commit` step is the atomic-commit boundary. Pre-commit verification step runs check-phase-64.mjs + check-phase-65.mjs + v1.6-milestone-audit.mjs locally; ALL THREE must exit 0 before the commit lands. |
| T-65-17 | Tampering | Sub-action 1 relative path wrong (`./l1-runbooks/` instead of `../../l1-runbooks/`) | mitigate | Verify relative path: from `docs/cross-platform/apple-business/12-...md`, `../../l1-runbooks/` resolves to `docs/l1-runbooks/`. Per Pitfall 6. The acceptance criteria explicit-string-check guards this. |
| T-65-18 | Tampering | Sub-action 2 leaves a partial exemption (e.g., removes 3 of 4) | mitigate | The target state is `c16_missing_endpoint_exemptions: []` (length 0). The acceptance criteria asserts the array is empty. v1.6-milestone-audit.mjs C16 check reports per-edge EXEMPTED status; if any of the 4 is still exempted, the harness output flags it. |
| T-65-19 | Tampering | Sub-action 3 only changes assertion `name` (not `run()` body) | mitigate | The replacement block changes BOTH the name (`V-64-05 [RECONCILED Phase 65]:`) AND the `run()` body (`if (!has34) return { pass: false ... }`). V-65-14 verifies the OLD failure-detail string is absent (a positive-test for the flip). |
| T-65-20 | Tampering | ABAUDIT-06 (line 13) or ABAUDIT-07 (line 116) line-pair semantics break due to wrong append position | mitigate | The append is at `12-:194` (after the last cross-ref bullet, before `## Version History`) — well below both ABAUDIT lines. Lines 1-194 are unchanged; ABAUDIT line-pair logic uses absolute line indices `i` and `i+1`, both unaffected. Per 65-RESEARCH.md:705. |
| T-65-21 | Tampering | Pre-edit anchor inventory regression on `12-` (PITFALL-6) | mitigate | The append-only edit shifts NO existing headings; the `## Cross-References` H2 stays at line 187 and the new bullet is added inside the existing block. Post-edit re-grep against 65-ANCHOR-INVENTORY.md for `12-` confirms all pre-edit anchors intact. |
| T-65-22 | Repudiation | Atomic-commit message does not record the 3-sub-action contract | mitigate | The commit message MUST explicitly enumerate all three sub-actions and reference D-04a + 62-08-PLAN §464-465 + the C16-triangle-live status. See commit-message template in the action below. |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No installs; markdown + JSON + ESM in-place edits only. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ATOMIC TRIO — append `12-` back-link bullet + empty `c16_missing_endpoint_exemptions` + flip check-phase-64.mjs V-64-05 NEGATIVE→POSITIVE; commit ALL THREE in one git commit</name>
  <files>docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md, scripts/validation/v1.6-audit-allowlist.json, scripts/validation/check-phase-64.mjs</files>
  <read_first>
    - docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md (FULL file but especially lines 187-201 — confirm the existing `## Cross-References` H2 starts at line 187, has 3 bullets ending at line 194, blank line at 195, `## Version History` at 196. ABAUDIT-06 at line 13 and ABAUDIT-07 at line 116 confirmed BELOW the append point.)
    - scripts/validation/v1.6-audit-allowlist.json (FULL file — confirm `c16_missing_endpoint_exemptions` array contents at `:80-85`; verify all 4 entries match the file paths listed in the interfaces block; confirm no other consumer of this array is affected by emptying it)
    - scripts/validation/check-phase-64.mjs lines 135-145 (the EXACT V-64-05 block to replace — see 65-RESEARCH.md §"V-64-05 Exact Current Code" for verbatim source)
    - scripts/validation/v1.6-milestone-audit.mjs lines 771-803 (C16 edge map + exemption check at `:785-790` + substring check at `:798` — confirms that emptying the exemptions array causes the substring check to run against `12-` content; the new back-link satisfies `content.includes('34-apple-business')`)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-PATTERNS.md "Atomic-Trio Patterns (D-04a — INDIVISIBLE UNIT)" (all 3 sub-actions verbatim)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-RESEARCH.md §Research Target 5 "V-64-05 ↔ C16 Atomic Reconciliation" + §Research Target 8 "12- Back-Link — Exact Append Target" + Pitfall 1 + Pitfall 6
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-CONVENTIONS.md Section 5 (Atomic-trio contract)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-ANCHOR-INVENTORY.md (12- pre-edit anchor snapshot — confirm post-edit none shift)
  </read_first>
  <action>
    Perform all THREE sub-actions BEFORE running git commit. Verify each sub-action's acceptance criteria locally with the validator chain BEFORE committing. The commit is the atomic-application boundary.

    **SUB-ACTION 1 — Append L1 #34 back-link bullet to `12-` `## Cross-References` tail + Version History row.**

    Open `docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md`. Locate the existing `## Cross-References` H2 at line 187 and its 3 existing bullets ending at line 194 (the `18-cross-org-boundary-cheat-sheet.md` bullet that wraps lines 192-194 per 65-RESEARCH.md:687). After the last bullet ending at line 194, BEFORE the blank line and `## Version History` H2 at line 196, append the new 4th bullet EXACTLY:

    ```
    - **L1 runbook:** [L1 #34 — Apple Business Shared iPad Passcode Reset](../../l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md) (Path A L1-delegated entry point)
    ```

    Verify the relative path `../../l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` resolves correctly: from `docs/cross-platform/apple-business/`, up two = `docs/`, then `l1-runbooks/34-...` is the L1 #34 from Wave 2. The substring `34-apple-business` is LOAD-BEARING for C16 admin_12 → l1_34 / V-65-12 / V-64-05 [RECONCILED].

    Then locate the `## Version History` H2 (line 196) and its existing version-history table. Append a new table row (the exact Markdown table-row format used in `12-`'s Version History):

    ```
    | 2026-05-22 | Phase 65 plan 65-04: added L1 #34 back-link to `## Cross-References` tail per D-04a + C16 atomic-reconciliation contract (62-08-PLAN §464-465); removed admin_12 C16 exemption from allowlist | -- |
    ```

    Confirm post-edit that ABAUDIT-06 (line 13) and ABAUDIT-07 (line 116) are at their original line indices — they are above line 194 and unaffected.

    **SUB-ACTION 2 — Empty `c16_missing_endpoint_exemptions` in `v1.6-audit-allowlist.json`.**

    Open `scripts/validation/v1.6-audit-allowlist.json`. Locate the `c16_missing_endpoint_exemptions` JSON array at approximately `:80-85`. Remove ALL FOUR objects from the array. The target post-edit state is:

    ```
    "c16_missing_endpoint_exemptions": [],
    ```

    Preserve JSON syntax — the trailing comma after `]` depends on whether this key is the last key in its parent object (preserve current punctuation). Confirm the file remains valid JSON by parsing it: `node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json'))"` MUST not throw. Do NOT modify any other keys in the file (e.g., any C14 / C15 / C13 exemption arrays remain untouched). Do NOT add comments — JSON does not support comments.

    **SUB-ACTION 3 — Replace V-64-05 in `check-phase-64.mjs:135-145` with the [RECONCILED] positive-assertion block.**

    Open `scripts/validation/check-phase-64.mjs`. Locate the V-64-05 block at lines 135-145. The EXACT current block to find-and-replace is from 65-RESEARCH.md §"V-64-05 Exact Current Code" (a 10-line block starting with `// === V-64-05: 12- does NOT contain 34-apple-business reference (C16 Phase 65 gate) ===` and ending with the closing `},` after `return { pass: true, detail: '12- does not contain 34-apple-business (C16 constraint satisfied)' };`).

    Replace with the EXACT block from 65-PATTERNS.md "Sub-Action 3" / 65-RESEARCH.md "Recommended Reconciliation Approach" (the 12-line block starting `// === V-64-05: 12- MUST contain 34-apple-business reference (C16 Phase 65 gate — RECONCILED) ===` and including the `id: 5, name: 'V-64-05 [RECONCILED Phase 65]: ...'` plus the flipped `run()` body where the success path now requires `c.includes('34-apple-business')` to be TRUE).

    Key changes:
    - Comment header: `=== V-64-05: 12- MUST contain 34-apple-business reference (C16 Phase 65 gate — RECONCILED) ===`
    - Assertion name: `'V-64-05 [RECONCILED Phase 65]: 12-shared-ipad-passcode-reset.md MUST contain 34-apple-business (C16 back-link landed)'`
    - Add inline comment `// RECONCILED: was NEGATIVE assertion in Phase 64 (must NOT contain). Phase 65 atomic commit (D-04a + 62-08-PLAN §464-465 contract) added the back-link; assertion flipped to POSITIVE.`
    - Body inverts the polarity: `if (!has34) return { pass: false, detail: '12- missing 34-apple-business back-link -- Phase 65 atomic commit (D-04a) not yet landed' }; return { pass: true, detail: '12- contains 34-apple-business back-link (C16 RECONCILED; Phase 65 atomic commit confirmed)' };`

    The test `id: 5` is PRESERVED so Phase 66 chain validators recognize the assertion by stable ID. The OLD failure-detail string `12- contains 34-apple-business reference (C16 sunset Phase 65; must not appear in Phase 64)` is now ABSENT — V-65-14 asserts this absence.

    **PRE-COMMIT VERIFICATION GATE (run BEFORE `git commit`).** Run all three of these locally and confirm exit codes:

    1. `node scripts/validation/check-phase-64.mjs` — MUST exit 0. V-64-05 [RECONCILED] now PASSES because `12-` contains the new back-link substring. All other V-64-NN assertions unaffected by sub-actions 1-2-3 (the 8 runbooks 11-..18- still exist; their content is unchanged).
    2. `node scripts/validation/check-phase-65.mjs` — V-65-12 (12- contains 34-apple-business), V-65-13 (allowlist empty), V-65-14 (old V-64-05 negative string absent) MUST all PASS. V-65-01..V-65-11 unchanged from Wave 3.
    3. `node scripts/validation/v1.6-milestone-audit.mjs` — MUST exit 0. C16 reports 0 EXEMPTED entries for the 4 sunset-65 paths (the allowlist is empty for that category); C16 reports all 4 edges PASS via real substring presence in their target files.

    If ANY of the three exit non-zero, do NOT commit. Diagnose which sub-action is mis-applied. Common pitfalls (from 65-RESEARCH.md):
    - Pitfall 6: relative path wrong in sub-action 1 → V-65-12 + V-64-05 [RECONCILED] still fail (substring missing because the link path is malformed)
    - Pitfall 1 partial: forgot to remove one of the 4 entries → harness reports the surviving exemption as EXEMPTED; V-65-13 fails (length > 0)
    - Pitfall 3 (not directly relevant but): if the V-64-05 replacement accidentally introduced syntax errors → check-phase-64.mjs throws (exit 1, but with stack-trace output); the find-and-replace MUST preserve the surrounding JS structure (the curly braces around the assertion object, the trailing comma before the next assertion).

    Once ALL three validators exit 0, stage the 3 modified files and create ONE commit with this EXACT message template (D-04a + atomic-trio enumeration):

    ```
    feat(65-04): C16 atomic-trio reconciliation — 12- back-link + allowlist clean + V-64-05 flip

    Lands the V-64-05 ↔ C16 indivisible atomic-trio per D-04a + 62-08-PLAN §464-465:
    1. docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md: appended L1 #34 back-link bullet to existing `## Cross-References` tail (substring `34-apple-business` now present); ABAUDIT-06/07 line-pair positions unaffected
    2. scripts/validation/v1.6-audit-allowlist.json: emptied `c16_missing_endpoint_exemptions` (removed all 4 sunset-65 entries: admin_12, l1_34, common_issues#..., quick_ref_l1#...)
    3. scripts/validation/check-phase-64.mjs:135-145: flipped V-64-05 from NEGATIVE (must NOT contain) to POSITIVE (MUST contain); test ID preserved with [RECONCILED Phase 65] label

    Result: C16 4-edge triangle is LIVE with ZERO exemptions. check-phase-64 + check-phase-65 + v1.6-milestone-audit all exit 0.

    Closes the Phase 62-08 sunset contract.
    ```

    Verify post-commit that `git log -1 --stat` shows exactly 3 files in the commit and no others.
  </action>
  <acceptance_criteria>
    - File `docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md` contains the literal substring `34-apple-business`
    - File contains the literal substring `../../l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md` (correct relative path)
    - The new bullet appears INSIDE the `## Cross-References` H2 block (between line 187 H2 and line ~196 `## Version History` H2)
    - File contains 4 bullets in the `## Cross-References` block (the 3 pre-existing 01-/09-/18- bullets + the new L1 #34 bullet)
    - File `12-` Version History table contains a new row dated `2026-05-22` referencing Phase 65 plan 65-04 + atomic-reconciliation
    - ABAUDIT-06 still at line 13 of `12-` (unchanged); ABAUDIT-07 still at line 116 of `12-` (unchanged) — verified by grep `<!--\s*ABAUDIT-06:` finds the comment at line 13, `<!--\s*ABAUDIT-07:` finds it at line 116
    - File `scripts/validation/v1.6-audit-allowlist.json` parses as valid JSON
    - `c16_missing_endpoint_exemptions` array in `v1.6-audit-allowlist.json` has length 0 (verified: `node -e "console.log(JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json')).c16_missing_endpoint_exemptions.length)"` prints `0`)
    - The 4 specific entries (file paths matching `docs/l1-runbooks/34-apple-business-shared-ipad-passcode-reset.md`, `docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md`, `docs/common-issues.md#apple-business-governance-failure-scenarios`, `docs/quick-ref-l1.md#apple-business-quick-reference`) are NOT present anywhere in the allowlist file
    - File `scripts/validation/check-phase-64.mjs` contains the literal substring `V-64-05 [RECONCILED Phase 65]`
    - File contains the literal substring `12- contains 34-apple-business back-link (C16 RECONCILED`
    - File does NOT contain the literal substring `12- contains 34-apple-business reference (C16 sunset Phase 65; must not appear in Phase 64)` (the OLD failure-detail string — V-65-14 asserts its absence)
    - `node scripts/validation/check-phase-64.mjs` exits 0; output shows V-64-05 [RECONCILED Phase 65] PASS
    - `node scripts/validation/check-phase-65.mjs` reports V-65-12 PASS, V-65-13 PASS, V-65-14 PASS
    - `node scripts/validation/v1.6-milestone-audit.mjs` exits 0; C16 reports all 4 edges PASS with 0 EXEMPTED entries for sunset-65 paths
    - `git log -1 --stat` shows EXACTLY 3 files: `docs/cross-platform/apple-business/12-shared-ipad-passcode-reset.md`, `scripts/validation/v1.6-audit-allowlist.json`, `scripts/validation/check-phase-64.mjs` — atomic commit verified
    - Commit message contains the substring `atomic-trio` AND the substring `D-04a` AND enumerates all three sub-actions
  </acceptance_criteria>
  <done>
    The C16 4-edge triangle is LIVE end-to-end. All 4 edges (l1_34 ↔ admin_12; common_issues → quick_ref_l1; quick_ref_l1 → l1_34) validate via real substring presence. v1.6-audit-allowlist.json has zero sunset-65 endpoint exemptions. check-phase-64.mjs V-64-05 is RECONCILED to a positive assertion that will continue to PASS in all future chain runs. The Phase 62-08 sunset contract is closed. The atomic-trio landed in ONE indivisible commit per D-04a; partial-state risk eliminated.
  </done>
</task>

</tasks>

<verification>
- `node scripts/validation/check-phase-64.mjs` exits 0 (V-64-05 [RECONCILED] passes the positive assertion)
- `node scripts/validation/check-phase-65.mjs` reports V-65-01..V-65-14 ALL PASS (Wave 4 complete; Wave 5 close-gate runs full chain in Plan 65-05)
- `node scripts/validation/v1.6-milestone-audit.mjs` exits 0 — C16 reports 4 PASS edges with 0 EXEMPTED entries for sunset-65 paths; C14/C15 unchanged
- `git log -1 --stat` confirms exactly 3 files in the single atomic commit
- No partial-state failure mode possible — the atomic commit is the all-or-nothing boundary
</verification>

<success_criteria>
- C16 4-edge integrity triangle live end-to-end with NO allowlist masking
- V-64-05 reconciled from NEGATIVE to POSITIVE; test ID preserved for chain output continuity
- Phase 62-08 sunset contract for `12-` admin_12 exemption closed
- Pitfall 1 (partial atomic application) prevented by pre-commit validator gate + acceptance-criteria assertion checking
- Pitfall 6 (`12-` back-link relative path wrong) prevented by explicit `../../l1-runbooks/` substring acceptance check
- PITFALL-6 anchor stability preserved on `12-` (the append is below all anchors)
- Single git commit covers all three sub-actions — D-04a INDIVISIBLE contract honored
</success_criteria>

<output>
Create `.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-04-SUMMARY.md` when the atomic-trio task completes. Record: (a) the atomic commit SHA + the 3 files in the commit; (b) the exit-0 confirmation for check-phase-64.mjs + check-phase-65.mjs + v1.6-milestone-audit.mjs in their POST-atomic-trio state; (c) the V-64-05 transition trace (was NEGATIVE→now POSITIVE; passing in both pre-commit and post-commit by construction because the back-link and the flip land together); (d) the 4 removed allowlist entries (file paths + sunset_phase values); (e) the C16 triangle status: all 4 edges PASS with 0 EXEMPTED entries.
</output>
</content>
</invoke>