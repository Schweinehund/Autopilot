---
phase: 143-link-coverage-fence-mask-unification
plan: 05
type: execute
wave: 6
depends_on: ["143-03", "143-04"]
files_modified:
  - docs/error-codes/02-tpm-attestation.md
  - docs/error-codes/03-esp-enrollment.md
  - docs/error-codes/04-pre-provisioning.md
  - docs/error-codes/05-hybrid-join.md
  - docs/l1-runbooks/02-esp-stuck-or-failed.md
  - docs/l1-runbooks/05-oobe-failure.md
  - docs/l2-runbooks/10-macos-log-collection.md
  - docs/admin-setup-android/04-byod-work-profile.md
  - docs/admin-setup-android/08-cope-full-admin.md
  - docs/admin-setup-apv2/02-etg-device-group.md
  - docs/_glossary-apple-business.md
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
  - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
autonomous: true
requirements: [LINK-01, LINK-03, LINK-04]
estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every Class-B repair is a SOURCE-side fragment rewrite; not one heading line, table row or piece of prose in any target file is modified by this plan."
    - "Each rewritten fragment equals the true GitHub slug of the target heading, re-derived from that heading's text at edit time by the shipped `githubSlug` algorithm — lowercase, strip everything outside `[a-z0-9 _-]` in place, replace each remaining space with one hyphen, never collapse consecutive hyphens."
    - "ZERO Class-B target headings carry a `{#id}` override, because Plan 09's D-38 conversion removed all 87 of them corpus-wide. No rewritten fragment is a doubled slug, and no rewrite bakes a `{#…}` token into a source link. The 22 doubled-slug rewrites the pre-ruling draft planned here no longer exist — those 26 pairs / 65 links were closed target-side by the conversion."
    - "The double-hyphen artifact is still real for a DIFFERENT reason and must be preserved: `## Android 11 — COPE NFC Provisioning Removed` slugs to `android-11--cope-nfc-provisioning-removed` because the stripped em dash was surrounded by spaces on both sides, and `## Enrollment Time Grouping (ETG) -- The Core Mechanism` slugs to `enrollment-time-grouping-etg----the-core-mechanism` for the same reason applied to a literal `--`. Both survive the conversion untouched; neither heading carried an override."
    - "D-06's three verified clean substitutions land as written: `#enrollment-status-page` -> `#esp` (`docs/_glossary.md:53`), `#entra` -> `#entra-id-sso` (`:191`), `#self-deploying` -> `#self-deploying-mode` (`:241`)."
    - "`#intune` is NOT in this plan's set — it is a false friend de-anchored in Plan 03. Do not repoint it to `#intune-management-extension-ime`. Neither is `#aosp`, de-anchored in Plan 03 for the same reason."
    - "The Class-B set is 8 pairs / 13 links: D-06's three substitutions (6 links), `#byod`, `#cope`, `#intunemacODC` (4 links), plus the two pairs Plan 04 Task 1 adjudicated — the ETG fragment (2 links) and `#edit-without-view-dependency-table` (1 link)."
    - "After this plan the dry-run corpus scan reports 274 files, 6252 relative links, 0 broken file targets, 0 broken anchors, 0 total."
    - "LINK-02 empty: a source file whose every link is already valid contributes zero edits and zero failures; the rewrite set is closed over the per-pair ledger, not over a directory sweep."
    - "The corpus holds zero `{#id}` tokens when this plan finishes, as it did when Plan 09 finished — the risk this plan must not create is re-authoring one, not preserving 87 of them."
  artifacts:
    - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
  key_links:
    - "target heading text -> `githubSlug` -> the rewritten source fragment. The heading is the source of truth; the fragment is derived from it, never the reverse."
    - "`143-EVIDENCE.md` per-pair ledger -> this plan's edit set. Every pair Plan 04 Task 1 routed to Class B must be closed here; the 51-pair / 67-link post-conversion invariant is checked at the end, and it must reconcile to the 77-pair / 132-link defect census via D-38's 26-pair / 65-link closure."
  prohibitions:
    - statement: "No target-side edit is made. No heading is reworded and no `<a id>` is added or removed — the Class-B remedy is source-side only (D-04). D-38's target-side work is finished and committed in Plan 09; re-touching a target here would be an unauthorized second pass."
      status: flagged-unverified
      verification: "`git diff -- docs/ | grep '^+' | grep -cE '^\\+#{1,6} |^\\+<a id='` returns 0."
    - statement: "No `{#id}` token is re-authored anywhere in the corpus. D-38 removed all 87; the invariant this plan must not break is ZERO, not 87."
      status: flagged-unverified
      verification: "`grep -rc '{#' docs/ --include=*.md | awk -F: '{s+=$2} END {print s}'` returns 0."
    - statement: "No new documentation content is authored — every edit replaces the fragment substring of an existing link target."
      status: flagged-unverified
      verification: "`git diff -- docs/ --numstat` shows equal added and removed line counts per file."
    - statement: "`#intune` and `#aosp` are not repointed to any glossary heading, and no `### Intune` or `### AOSP` entry is created."
      status: flagged-unverified
      verification: "`grep -rc '#intune-management-extension-ime' docs/` returns 0, `grep -c '### Intune$' docs/_glossary.md` returns 0, and `grep -c '### AOSP' docs/_glossary-android.md` returns 0."
    - statement: "Neither glossary's `last_verified` nor `review_by` is touched."
      status: flagged-unverified
      verification: "`git diff -- docs/_glossary-macos.md docs/_glossary-android.md | grep -cE '^[+-](last_verified|review_by)'` returns 0."
    - statement: "No accepted-violation baseline, allowlist or expected-failure list is introduced to absorb a residual break."
      status: flagged-unverified
      verification: "`git status --porcelain` shows no new file outside `.planning/`, and the final dry-run total is 0 rather than a non-zero count declared acceptable."
---

<objective>
Land D-04's Class-B remedy — source-side link rewrites to the true GitHub slug — for every pair
where a correct target already exists in the target file under a different slug, and drive the
corpus-wide dry-run total to zero.

Purpose: for these 8 pairs the target is correct as authored and only the link's spelling of it is
stale, so the repair is source-side. This is the residue of the class D-01 exposed: 65 of the
original 132 broken links were green in the checker and broken on GitHub because the Pandoc `{#id}`
model registered an anchor GitHub never creates, and D-38's all-87 conversion (Plan 09) closed all
65 of them target-side. What remains here is the honest Class-B tail — stale tokens, a case
mismatch, and two adjudicated pairs — none of it `{#id}`-derived.

Scope shrank sharply under D-38. The pre-ruling draft carried a 22-row "`{#id}`-inflated bulk"
task rewriting ~57 source links to doubled slugs such as
`#cause-a-package-install-failure-cause-a-package-install`. That task is DELETED: the conversion
delivers those links target-side, with no doubled slug baked into any source file and no
dependency on a `{#…}` token that a future phase might remove. The plan drops from 3 tasks and 27
files to 2 tasks and 11 files.

Output: zero broken anchors, zero broken file targets, and a per-pair ledger showing all 51
post-conversion pairs closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
@scripts/validation/check-nav-hub-links.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: All 8 Class-B rewrites — D-06's verified substitutions, the singletons, and the two adjudicated pairs</name>
  <files>docs/error-codes/02-tpm-attestation.md, docs/error-codes/03-esp-enrollment.md, docs/error-codes/04-pre-provisioning.md, docs/error-codes/05-hybrid-join.md, docs/l1-runbooks/02-esp-stuck-or-failed.md, docs/l1-runbooks/05-oobe-failure.md, docs/admin-setup-android/04-byod-work-profile.md, docs/admin-setup-android/08-cope-full-admin.md, docs/l2-runbooks/10-macos-log-collection.md, docs/admin-setup-apv2/02-etg-device-group.md, docs/_glossary-apple-business.md, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `docs/_glossary.md` — heading lines `:43` `## Enrollment`, `:53` `### ESP`, `:95` `### Intune Management Extension (IME)`, `:101` `### Intune Provisioning Client`, `:191` `### Entra ID SSO`, `:241` `### Self-deploying mode`
    - `docs/android-lifecycle/02-provisioning-methods.md:45` — the row carrying `<a id="byod-work-profile"></a>`
    - `docs/android-lifecycle/03-android-version-matrix.md:57` — `### Android 11 — COPE NFC Provisioning Removed`
    - `docs/reference/macos-commands.md:184` — `## IntuneMacODC`
    - `docs/lifecycle-apv2/00-overview.md:40` — `## Enrollment Time Grouping (ETG) -- The Core Mechanism`
    - `docs/cross-platform/apple-business/01-role-permission-model.md:343` — `## Edit-without-View Dependency Table (OP-3 Prevention)`
    - `scripts/validation/check-nav-hub-links.mjs:111-116` — `githubSlug`, the algorithm every rewritten fragment must be derived by
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the per-pair ledger and Plan 04 Task 1's Class-B routings
  </read_first>
  <action>
Append the GOV-02 ledger row for this plan's corpus set BEFORE editing, then apply each rewrite
below. Every edit changes only the `#fragment` portion of a link target on an existing line; the
link text and the file path stay byte-identical.

**D-06's three verified clean substitutions** (targets confirmed present in `docs/_glossary.md`):

| from | to | target heading | source links |
|---|---|---|---|
| `#enrollment-status-page` | `#esp` | `:53` `### ESP` | `docs/l1-runbooks/02-esp-stuck-or-failed.md:24`, `docs/l1-runbooks/05-oobe-failure.md:51` |
| `#entra` | `#entra-id-sso` | `:191` `### Entra ID SSO` | `docs/error-codes/03-esp-enrollment.md:32`, `docs/error-codes/05-hybrid-join.md:23` |
| `#self-deploying` | `#self-deploying-mode` | `:241` `### Self-deploying mode` | `docs/error-codes/02-tpm-attestation.md:23`, `docs/error-codes/04-pre-provisioning.md:23` |

For `#enrollment-status-page` note that the classifier's own prefix match was `## Enrollment` at
`:43`, NOT `### ESP` — these three are human substitutions, and each must be re-confirmed against
the heading text before the edit, not accepted from the table alone.

**`#intune` and `#aosp` are NOT in this set.** Both were de-anchored in Plan 03 as false friends —
`docs/_glossary.md` has no `### Intune` entry and `docs/_glossary-android.md` has no AOSP heading
at any level. Do not repoint either.

**Singleton rewrites** — derive each target slug from the heading text with `githubSlug` and confirm
it matches before editing:

| from | to | target | source link |
|---|---|---|---|
| `#byod` | `#byod-work-profile` | `docs/android-lifecycle/02-provisioning-methods.md:45` (row already anchored) | `docs/admin-setup-android/04-byod-work-profile.md:294` |
| `#cope` | `#android-11--cope-nfc-provisioning-removed` | `docs/android-lifecycle/03-android-version-matrix.md:57` | `docs/admin-setup-android/08-cope-full-admin.md:64`, `:277` |
| `#intunemacODC` | `#intunemacodc` | `docs/reference/macos-commands.md:184` `## IntuneMacODC` | `docs/l2-runbooks/10-macos-log-collection.md:74` |

**The two pairs Plan 04 Task 1 adjudicated to Class B** — apply the routing recorded in
`143-EVIDENCE.md`, confirming each target heading before editing:

| from | to | target | source link |
|---|---|---|---|
| `#enrollment-time-grouping----the-core-mechanism` | `#enrollment-time-grouping-etg----the-core-mechanism` | `docs/lifecycle-apv2/00-overview.md:40` `## Enrollment Time Grouping (ETG) -- The Core Mechanism` | `docs/admin-setup-apv2/02-etg-device-group.md:26`, `:168` |
| `#edit-without-view-dependency-table` | `#edit-without-view-dependency-table-op-3-prevention` | `docs/cross-platform/apple-business/01-role-permission-model.md:343` `## Edit-without-View Dependency Table (OP-3 Prevention)` | `docs/_glossary-apple-business.md:96` |

**Three rewrites the pre-ruling draft listed here are GONE and must not be re-added.**
`#contentpreptool-packaging`, `#resource-access-deprecation` and `#ddm-update-keys` all named
headings that carried `{#id}` overrides, so Plan 09's conversion resolves them target-side.
Writing the doubled forms (`#contentpreptool-packaging-contentpreptool-packaging` and its two
siblings) would now break links that currently work. Confirm each of the three resolves before
finishing.

**Two double-hyphen artifacts are real and must be preserved.** `#cope` carries one because the
heading's em dash is stripped while surrounded by spaces on both sides —
`Android 11 — COPE NFC Provisioning Removed` slugs to `android-11--cope-nfc-provisioning-removed`.
The ETG fragment carries four consecutive hyphens because a literal ` -- ` survives as two stripped
characters between two space-derived hyphens. Preserve both; `githubSlug` never collapses
consecutive hyphens and neither does GitHub.

`#intunemacODC` -> `#intunemacodc` is a pure case fix: the heading is `## IntuneMacODC` and
`githubSlug` lowercases before stripping.

Locate every edit by the link's own text and target string. Several source files were rewritten by
Plan 09's conversion and their line numbering has shifted.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs | tail -2; node scripts/validation/carve-gate.mjs | tail -1; node scripts/validation/check-nav-hub-links.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc '_glossary.md#enrollment-status-page' docs/` returns 0 and `grep -c '_glossary.md#esp' docs/l1-runbooks/02-esp-stuck-or-failed.md` returns 1.
    - `grep -rc '_glossary.md#entra)' docs/error-codes/` returns 0 and `grep -rc '_glossary.md#entra-id-sso' docs/error-codes/` returns 2.
    - `grep -rc '_glossary.md#self-deploying)' docs/error-codes/` returns 0 and `grep -rc '#self-deploying-mode' docs/error-codes/` returns 2.
    - `grep -rc '#intune-management-extension-ime' docs/` returns 0.
    - `grep -c '#android-11--cope-nfc-provisioning-removed' docs/admin-setup-android/08-cope-full-admin.md` returns 2, with the double hyphen intact.
    - `grep -c '#intunemacODC' docs/l2-runbooks/10-macos-log-collection.md` returns 0 and `grep -c '#intunemacodc' docs/l2-runbooks/10-macos-log-collection.md` returns 1.
    - `grep -c '#enrollment-time-grouping-etg----the-core-mechanism' docs/admin-setup-apv2/02-etg-device-group.md` returns 2, with all four hyphens intact, and `grep -c '#enrollment-time-grouping----the-core-mechanism)' docs/admin-setup-apv2/02-etg-device-group.md` returns 0.
    - `grep -c '#edit-without-view-dependency-table-op-3-prevention' docs/_glossary-apple-business.md` returns 1.
    - `grep -rcE '#(contentpreptool-packaging-contentpreptool-packaging|resource-access-deprecation-resource-access-deprecation|ddm-update-keys-ddm-update-keys)' docs/` returns 0 — the three deleted doubled-slug rewrites were not re-added.
    - `git diff -- docs/ | grep '^+' | grep -cE '^\+#{1,6} |^\+<a id=' ` returns 0 — no heading line and no anchor was modified.
    - `grep -rc '{#' docs/ --include=*.md | awk -F: '{s+=$2} END {print s}'` returns 0 — no `{#id}` token was re-authored.
    - `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations, 0 total violations`.
    - `node scripts/validation/carve-gate.mjs` exits 0 with `off-list=0`.
  </acceptance_criteria>
  <done>All 8 Class-B pairs (13 links) are rewritten source-side to heading-derived true GitHub slugs; `#intune` and `#aosp` stay de-anchored; the three conversion-resolved fragments are confirmed untouched; no heading, anchor or `{#id}` token is written; c17 and carve-gate unregressed.</done>
</task>

<task type="auto">
  <name>Task 2: Dry-run to zero + close the 51-pair ledger</name>
  <files>.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md</files>
  <read_first>
    - `scripts/validation/check-nav-hub-links.mjs:253-269` — `checkInboundLinks`, `:259` and `:269`
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the dry-run ladder and the 51-pair post-conversion ledger
  </read_first>
  <action>
Run the established dry-run one final time before the flip: in the working tree only, delete `:259`
(`if (hubSet.has(relPath)) continue;`) and `:269` (`if (!hubSet.has(resolvedRel)) continue;`) from
`checkInboundLinks`, run `node scripts/validation/check-nav-hub-links.mjs --verbose`, capture the
output, then restore with `git checkout -- scripts/validation/check-nav-hub-links.mjs`.

Expected: 274 files, 6252 relative links, **0** broken file targets, **0** broken anchors,
**0** total.

If the total is non-zero, do NOT declare the residue acceptable and do NOT introduce a baseline,
allowlist or expected-failure list — LINK-04 forbids an accepted-violation baseline of any kind.
Instead, reconcile each residual failure against the 51-pair ledger, identify which pair was
mis-classified or mis-rewritten, and repair it in this plan.

Close the ledger in `143-EVIDENCE.md`: assert that all 51 post-conversion (target-file, fragment)
pairs are accounted for, that the per-class counts sum to 51 pairs and 67 links (C 31/38, D 12/16,
B 8/13), and that each pair's recorded remedy matches what actually landed. Assert separately that
the 26 pairs / 65 links D-38's conversion closed are recorded as closed-by-conversion, so the
original 77-pair / 132-link defect census reconciles to zero with no unexplained residue:
51 + 26 = 77 pairs and 67 + 65 = 132 links. Append the final dry-run row to the ladder — the full
measured sequence is 175 -> 173 -> 143 -> 78 -> 49 -> 13 -> 0.

Also record the D-15 rulings in `143-EVIDENCE.md`, since the flip in Plan 06 makes both live:
(a) `docs/_glossary-linux.md:157` links to `../.planning/research/PITFALLS.md` — the single link
that resolves outside `docs/` to an existing target. It carries no `#fragment`, so under the widened
scan it resolves as a file-existence check and passes. **Ruling: in scope, not excluded** — the
checker validates that a relative link resolves to a file that exists, and `.planning/` being
outside `carve-gate`'s `IN_SCOPE_PREFIXES` governs what may be EDITED, not what may be LINKED TO.
Record the reason so a later maintainer does not add an exclusion for it.
(b) `relNormalize:75-80` strips only a `cwd` prefix, so a target resolving ABOVE `cwd` would stay
absolute and produce a bogus "anchor not found". Zero instances exist today — all 14
outside-`docs/` links resolve to paths under `cwd`, 13 of which Plan 03 repaired. Record this as a
known latent class with zero live instances rather than adding speculative handling.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; echo "hub_exit=$?"; git status --porcelain scripts/validation/check-nav-hub-links.mjs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The dry-run summary line reports `0 outbound failure(s), 0 inbound failure(s), 0 total` with the patched scan covering 274 files and 6252 relative links.
    - `git status --porcelain scripts/validation/check-nav-hub-links.mjs | wc -l` returns 0 after the dry-run.
    - `node scripts/validation/check-nav-hub-links.mjs` (unpatched) exits 0.
    - `143-EVIDENCE.md`'s per-pair ledger shows 51 rows, every row with a class and a landed remedy, and the link-count column summing to 67 with per-class subtotals 31/38, 12/16 and 8/13.
    - The ledger's reconciliation line shows 51 + 26 = 77 pairs and 67 + 65 = 132 links against the original defect census, with zero unexplained residue.
    - `143-EVIDENCE.md`'s dry-run ladder has seven measured rows ending at 0 (175, 173, 143, 78, 49, 13, 0).
    - `143-EVIDENCE.md` records both D-15 rulings with their reasons.
    - `grep -rc '{#' docs/ --include=*.md | awk -F: '{s+=$2} END {print s}'` returns 0.
    - `git status --porcelain | grep -vc '^.. \.planning/'` returns 0 — no untracked or modified file outside `.planning/` remains.
  </acceptance_criteria>
  <done>The corpus-wide dry-run reports zero failures with no baseline of any kind; all 51 post-conversion pairs are closed in the ledger and reconcile to the original 77/132 census; both D-15 rulings are recorded; the flip is still uncommitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target heading text -> rewritten source fragment | The heading is the source of truth; a fragment derived from anything else (a prefix match, a table row, a memory of the old slug) silently produces a link that resolves in nobody's renderer. |
| corpus link rewrite -> published `.docx` / SharePoint knowledge base | A wrong repair ships as wrong navigation in the deliverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-18 | Spoofing | prefix-match masquerading as semantic match | high | mitigate | Every rewrite must be derived from the heading text with `githubSlug` and confirmed against the table before writing. The two adjudicated pairs each carry the exact heading line their slug derives from, and `#edit-without-view-dependency-table` carries an explicit warning that the mechanical locator proposes a changelog row at `:395` while the real heading is at `:343`. |
| T-143-19 | Tampering | reviving a rewrite D-38 already resolved | high | mitigate | The three deleted doubled-slug rewrites (`#contentpreptool-packaging`, `#resource-access-deprecation`, `#ddm-update-keys`) are named as GONE with the reason, and an acceptance criterion greps that none of their doubled forms appears anywhere in `docs/`. Writing them would break links that currently work. |
| T-143-20 | Tampering | unauthorized target-side edit | high | mitigate | A prohibition plus greps assert zero modified heading lines, zero added or removed `<a id>` tags, and a corpus `{#id}` population of 0 — the post-D-38 invariant. |
| T-143-21 | Repudiation | residual break absorbed silently | high | mitigate | Task 2 forbids declaring a non-zero residue acceptable and requires reconciliation against the 51-pair ledger AND against the 26-pair conversion closure, so 51 + 26 = 77 must hold; an acceptance criterion asserts the dry-run total is 0 and `git status` is clean outside `.planning/`. |
| T-143-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs; this plan edits Markdown only. `143-RESEARCH.md` Package Legitimacy Audit: "Not applicable". |
</threat_model>

<verification>
- Dry-run: 274 files / 6252 links / **0** broken file targets / **0** broken anchors / **0** total.
- `node scripts/validation/check-nav-hub-links.mjs` -> exit 0.
- `node scripts/validation/c17-eee-contract.mjs` -> `234 files checked, 0 with violations, 0 total violations`.
- `node scripts/validation/carve-gate.mjs` -> exit 0, `off-list=0`.
- `grep -rcE '\{#[a-zA-Z0-9_-]+\}' docs/ --include=*.md` sums to **0** — the post-D-38 invariant, unchanged by this plan.
- `143-EVIDENCE.md`: 51 pairs closed, 67 links, reconciling to 77/132 with the 26/65 conversion closure; seven-row dry-run ladder ending at 0.

</verification>

<success_criteria>
All 8 Class-B pairs (13 links) are rewritten source-side to heading-derived true GitHub slugs; the
two double-hyphen artifacts are preserved; no heading, anchor or `{#id}` token is written; the
three conversion-resolved fragments are confirmed untouched; the corpus-wide dry-run reports zero
with no baseline; the 51-pair ledger is closed and reconciles to the 77-pair defect census.
</success_criteria>

## Artifacts this phase produces

Created or modified by **this plan**:

| Artifact | Kind | Note |
|---|---|---|
| 13 rewritten link fragments across 11 source files | corpus edits | source-side only, heading-derived slugs |
| D-15(a) `.planning/` link scope ruling | decision record in `143-EVIDENCE.md` | in scope, passes as a file-existence check |
| D-15(b) above-`cwd` `relNormalize` latent class | decision record in `143-EVIDENCE.md` | zero live instances, no speculative handling |
| Closed 51-pair remedy ledger | evidence table | every pair with class + landed remedy; reconciles 51+26=77 |
| Seven-row dry-run ladder ending at 0 | evidence table | 175 -> 173 -> 143 -> 78 -> 49 -> 13 -> 0 |
| `v1.20-GOV-02-LEDGER.md` row (Class-B corpus set) | ledger row | row-per-edit, appended |

<output>
Create `.planning/phases/143-link-coverage-fence-mask-unification/143-05-SUMMARY.md` when done.
</output>
