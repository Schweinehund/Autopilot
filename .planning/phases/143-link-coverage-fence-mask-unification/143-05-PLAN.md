---
phase: 143-link-coverage-fence-mask-unification
plan: 05
type: execute
wave: 5
depends_on: ["143-03", "143-04"]
files_modified:
  - docs/error-codes/02-tpm-attestation.md
  - docs/error-codes/03-esp-enrollment.md
  - docs/error-codes/04-pre-provisioning.md
  - docs/error-codes/05-hybrid-join.md
  - docs/l1-runbooks/02-esp-stuck-or-failed.md
  - docs/l1-runbooks/05-oobe-failure.md
  - docs/l1-runbooks/22-android-enrollment-blocked.md
  - docs/l1-runbooks/23-android-work-profile-not-created.md
  - docs/l1-runbooks/27-android-zte-enrollment-failed.md
  - docs/l1-runbooks/30-linux-enrollment-failed.md
  - docs/l1-runbooks/31-linux-compliance-non-compliant.md
  - docs/l1-runbooks/32-linux-ca-blocking-web-access.md
  - docs/l2-runbooks/10-macos-log-collection.md
  - docs/l2-runbooks/20-android-app-install-investigation.md
  - docs/l2-runbooks/24-linux-log-collection.md
  - docs/l2-runbooks/25-linux-agent-investigation.md
  - docs/admin-setup-android/04-byod-work-profile.md
  - docs/admin-setup-android/08-cope-full-admin.md
  - docs/admin-setup-apv2/02-etg-device-group.md
  - docs/operations/app-lifecycle/01-windows-win32-msix-scale.md
  - docs/operations/co-management/00-overview.md
  - docs/operations/co-management/02-windows-workload-sliders.md
  - docs/operations/patch-management/02-macos-update-enforcement.md
  - docs/operations/patch-management/03-ios-update-lifecycle.md
  - docs/quick-ref-l2.md
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
  - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
autonomous: true
requirements: [LINK-01, LINK-03, LINK-04]
estimate:
  tokens: 110000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every Class-B repair is a SOURCE-side fragment rewrite; not one heading line, table row or piece of prose in any target file is modified by this plan."
    - "Each rewritten fragment equals the true GitHub slug of the target heading, re-derived from that heading's text at edit time by the shipped `githubSlug` algorithm — lowercase, strip everything outside `[a-z0-9 _-]` in place, replace each remaining space with one hyphen, never collapse consecutive hyphens."
    - "22 of the Class-B target headings carry a trailing `{#id}` override which under the GitHub model renders as literal visible text AND participates in the slug, so their true slugs are doubled — e.g. `#cause-a-package-install` becomes `#cause-a-package-install-failure-cause-a-package-install`. These are correct, working GitHub anchors."
    - "The double-hyphen artifact is real and must be preserved: `## Deadlines & Cutover Dates {#deadlines-cutover-dates}` slugs to `deadlines--cutover-dates-deadlines-cutover-dates` because the stripped `&` was surrounded by spaces on both sides."
    - "D-06's three verified clean substitutions land as written: `#enrollment-status-page` -> `#esp` (`docs/_glossary.md:53`), `#entra` -> `#entra-id-sso` (`:191`), `#self-deploying` -> `#self-deploying-mode` (`:241`)."
    - "`#intune` is NOT in this plan's set — it is a false friend de-anchored in Plan 03. Do not repoint it to `#intune-management-extension-ime`."
    - "After this plan the dry-run corpus scan reports 274 files, 6252 relative links, 0 broken file targets, 0 broken anchors, 0 total."
    - "LINK-02 empty: a source file whose every link is already valid contributes zero edits and zero failures; the rewrite set is closed over the per-pair ledger, not over a directory sweep."
    - statement: "No link rewritten here is broken again by a future phase removing a `{#id}` token from a target heading."
      verification: backstop
  artifacts:
    - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
  key_links:
    - "target heading text -> `githubSlug` -> the rewritten source fragment. The heading is the source of truth; the fragment is derived from it, never the reverse."
    - "`143-EVIDENCE.md` per-pair ledger -> this plan's edit set. Every pair Plan 04 Task 1 routed to Class B must be closed here; the 77-pair / 132-link invariant is checked at the end."
  prohibitions:
    - statement: "No target-side edit is made. No heading is reworded, no `{#id}` token is deleted, no `<a id>` is added to a heading line — the ratified Class-B remedy is source-side only (D-04, confirmed at Plan 02 Task 1's one-way checkpoint)."
      status: flagged-unverified
      verification: "`git diff -- docs/ | grep '^+' | grep -cE '^\\+#{1,6} '` returns 0, and no diff hunk touches a line matching `\\{#[a-zA-Z0-9_-]+\\}`."
    - statement: "No new documentation content is authored — every edit replaces the fragment substring of an existing link target."
      status: flagged-unverified
      verification: "`git diff -- docs/ --numstat` shows equal added and removed line counts per file."
    - statement: "`#intune` is not repointed to any glossary heading and no `### Intune` entry is created."
      status: flagged-unverified
      verification: "`grep -rc '#intune-management-extension-ime' docs/` returns 0 and `grep -c '### Intune$' docs/_glossary.md` returns 0."
    - statement: "Neither glossary's `last_verified` nor `review_by` is touched."
      status: flagged-unverified
      verification: "`git diff -- docs/_glossary-macos.md docs/_glossary-android.md | grep -cE '^[+-](last_verified|review_by)'` returns 0."
    - statement: "No accepted-violation baseline, allowlist or expected-failure list is introduced to absorb a residual break."
      status: flagged-unverified
      verification: "`git status --porcelain` shows no new file outside `.planning/`, and the final dry-run total is 0 rather than a non-zero count declared acceptable."
---

<objective>
Land D-04's Class-B remedy — source-side link rewrites to the true GitHub slug — for every pair
where a correct target already exists in the target file under a different slug, and drive the
corpus-wide dry-run total to zero.

Purpose: this is the class D-01 exposed. 65 of these links were green in the checker and broken on
GitHub because the Pandoc `{#id}` model registered an anchor GitHub never creates. With the model
corrected, the fragment must be re-derived from the heading GitHub actually renders. The repair is
source-side because the target is correct as authored — only the link's spelling of it is stale.

Output: zero broken anchors, zero broken file targets, and a per-pair ledger showing all 77 pairs
closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
@scripts/validation/check-nav-hub-links.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Glossary and short-tail rewrites — D-06's verified substitutions plus the singletons</name>
  <files>docs/error-codes/02-tpm-attestation.md, docs/error-codes/03-esp-enrollment.md, docs/error-codes/04-pre-provisioning.md, docs/error-codes/05-hybrid-join.md, docs/l1-runbooks/02-esp-stuck-or-failed.md, docs/l1-runbooks/05-oobe-failure.md, docs/admin-setup-android/04-byod-work-profile.md, docs/admin-setup-android/08-cope-full-admin.md, docs/l2-runbooks/10-macos-log-collection.md, docs/operations/app-lifecycle/01-windows-win32-msix-scale.md, docs/operations/co-management/00-overview.md, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `docs/_glossary.md` — heading lines `:43` `## Enrollment`, `:53` `### ESP`, `:95` `### Intune Management Extension (IME)`, `:101` `### Intune Provisioning Client`, `:191` `### Entra ID SSO`, `:241` `### Self-deploying mode`
    - `docs/android-lifecycle/02-provisioning-methods.md:45` — the row carrying `<a id="byod-work-profile"></a>`
    - `docs/android-lifecycle/03-android-version-matrix.md:57` — `### Android 11 — COPE NFC Provisioning Removed`
    - `docs/reference/macos-commands.md:184` — `## IntuneMacODC`
    - `docs/operations/app-lifecycle/01-windows-win32-msix-scale.md:154`, `docs/operations/co-management/00-overview.md:103`, `docs/operations/patch-management/03-ios-update-lifecycle.md:46` — the `{#id}` headings behind three singleton rewrites
    - `scripts/validation/check-nav-hub-links.mjs:111-116` — `githubSlug`, the algorithm every rewritten fragment must be derived by
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the per-pair ledger and Plan 04 Task 1's Class-B routings
  </read_first>
  <action>
Append the GOV-02 ledger row for this plan's corpus set BEFORE editing, then apply each rewrite
below. Every edit changes only the `#fragment` portion of a link target on an existing line; the
link text and the file path stay byte-identical.

**D-06's three verified clean substitutions** (targets confirmed present in `docs/_glossary.md`):

| from | to | target heading | source links |
|---|---|---|---|
| `#enrollment-status-page` | `#esp` | `:53` `### ESP` | `docs/l1-runbooks/02-esp-stuck-or-failed.md:24`, `docs/l1-runbooks/05-oobe-failure.md:51` |
| `#entra` | `#entra-id-sso` | `:191` `### Entra ID SSO` | `docs/error-codes/03-esp-enrollment.md:32`, `docs/error-codes/05-hybrid-join.md:23` |
| `#self-deploying` | `#self-deploying-mode` | `:241` `### Self-deploying mode` | `docs/error-codes/02-tpm-attestation.md:23`, `docs/error-codes/04-pre-provisioning.md:23` |

For `#enrollment-status-page` note that the classifier's own prefix match was `## Enrollment` at
`:43`, NOT `### ESP` — these three are human substitutions, and each must be re-confirmed against
the heading text before the edit, not accepted from the table alone.

**`#intune` is NOT in this set.** It was de-anchored in Plan 03 as a false friend. Do not repoint it.

**Singleton rewrites** — derive each target slug from the heading text with `githubSlug` and confirm
it matches before editing:

| from | to | target | source link |
|---|---|---|---|
| `#byod` | `#byod-work-profile` | `docs/android-lifecycle/02-provisioning-methods.md:45` (row already anchored) | `docs/admin-setup-android/04-byod-work-profile.md:294` |
| `#cope` | `#android-11--cope-nfc-provisioning-removed` | `docs/android-lifecycle/03-android-version-matrix.md:57` | `docs/admin-setup-android/08-cope-full-admin.md:64`, `:277` |
| `#intunemacODC` | `#intunemacodc` | `docs/reference/macos-commands.md:184` `## IntuneMacODC` | `docs/l2-runbooks/10-macos-log-collection.md:74` |
| `#contentpreptool-packaging` | `#contentpreptool-packaging-contentpreptool-packaging` | `docs/operations/app-lifecycle/01-windows-win32-msix-scale.md:154` | same file `:45` (self-link) |
| `#resource-access-deprecation` | `#resource-access-deprecation-resource-access-deprecation` | `docs/operations/co-management/00-overview.md:103` | same file `:48` (self-link) |
| `#ddm-update-keys` | `#ddm-update-keys-ddm-update-keys` | `docs/operations/patch-management/03-ios-update-lifecycle.md:46` | same file `:40` (self-link) |

`#cope` carries a double hyphen because the heading's em dash is stripped while surrounded by
spaces on both sides — `Android 11 — COPE NFC Provisioning Removed` slugs to
`android-11--cope-nfc-provisioning-removed`. Preserve it; `githubSlug` never collapses consecutive
hyphens and neither does GitHub.

`#intunemacODC` -> `#intunemacodc` is a pure case fix: the heading is `## IntuneMacODC` and
`githubSlug` lowercases before stripping.

Also apply any pair Plan 04 Task 1 routed to Class B (`docs/_glossary-android.md#aosp`, and the
`docs/lifecycle-apv2/00-overview.md` ETG pair if Plan 03's re-audit reclassified it), using the
routing recorded in `143-EVIDENCE.md`.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs | tail -2; node scripts/validation/carve-gate.mjs | tail -1; node scripts/validation/check-nav-hub-links.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc '_glossary.md#enrollment-status-page' docs/` returns 0 and `grep -c '_glossary.md#esp' docs/l1-runbooks/02-esp-stuck-or-failed.md` returns 1.
    - `grep -rc '_glossary.md#entra)' docs/error-codes/` returns 0 and `grep -rc '_glossary.md#entra-id-sso' docs/error-codes/` returns 2.
    - `grep -rc '_glossary.md#self-deploying)' docs/error-codes/` returns 0 and `grep -rc '#self-deploying-mode' docs/error-codes/` returns 2.
    - `grep -rc '#intune-management-extension-ime' docs/` returns 0.
    - `grep -c '#android-11--cope-nfc-provisioning-removed' docs/admin-setup-android/08-cope-full-admin.md` returns 2, with the double hyphen intact.
    - `grep -c '#intunemacODC' docs/l2-runbooks/10-macos-log-collection.md` returns 0 and `grep -c '#intunemacodc' docs/l2-runbooks/10-macos-log-collection.md` returns 1.
    - `git diff -- docs/ | grep '^+' | grep -cE '^\+#{1,6} '` returns 0 — no heading line was modified.
    - `git diff -- docs/ | grep -c '{#'` returns 0 — no `{#id}` token was added or removed.
    - `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations, 0 total violations`.
    - `node scripts/validation/carve-gate.mjs` exits 0 with `off-list=0`.
  </acceptance_criteria>
  <done>D-06's three verified substitutions and the six singleton rewrites land source-side; `#intune` stays de-anchored; no heading or `{#id}` token is touched; c17 and carve-gate unregressed.</done>
</task>

<task type="auto">
  <name>Task 2: The `{#id}`-inflated bulk — Linux runbooks, Android patterns, patch-management</name>
  <files>docs/l1-runbooks/22-android-enrollment-blocked.md, docs/l1-runbooks/23-android-work-profile-not-created.md, docs/l1-runbooks/27-android-zte-enrollment-failed.md, docs/l1-runbooks/30-linux-enrollment-failed.md, docs/l1-runbooks/31-linux-compliance-non-compliant.md, docs/l1-runbooks/32-linux-ca-blocking-web-access.md, docs/l2-runbooks/20-android-app-install-investigation.md, docs/l2-runbooks/24-linux-log-collection.md, docs/l2-runbooks/25-linux-agent-investigation.md, docs/admin-setup-apv2/02-etg-device-group.md, docs/operations/co-management/02-windows-workload-sliders.md, docs/operations/patch-management/02-macos-update-enforcement.md, docs/quick-ref-l2.md</files>
  <read_first>
    - `docs/l1-runbooks/30-linux-enrollment-failed.md` — heading lines `:63`, `:104`, `:148`
    - `docs/l1-runbooks/31-linux-compliance-non-compliant.md` — `:66`, `:108`, `:150`, `:191`
    - `docs/l1-runbooks/32-linux-ca-blocking-web-access.md` — `:68`, `:110`, `:151`
    - `docs/l2-runbooks/25-linux-agent-investigation.md` — `:51`, `:121`, `:186`, `:270`
    - `docs/l2-runbooks/19-android-enrollment-investigation.md` — `:128`, `:190`, `:252`
    - `docs/l2-runbooks/00-index.md:185`, `docs/operations/co-management/03-cocmgmt-migration-paths.md:9` and `:33`, `docs/operations/patch-management/04-android-patch-delivery.md:50` and `:76`, `docs/operations/patch-management/02-macos-update-enforcement.md:77`
    - `scripts/validation/check-nav-hub-links.mjs:111-116` — `githubSlug`
  </read_first>
  <action>
Rewrite each source fragment to the true GitHub slug of its target heading. **Derive every slug
from the heading text at edit time** — read the heading line, apply `githubSlug`, and confirm the
result equals the value in the table before writing it. The table below was derived at plan time
from HEAD `599a996b`; treat it as the expected value to confirm, not as text to paste blind.

| target heading (file:line) | from | to | source links |
|---|---|---|---|
| `l1-runbooks/30-linux-enrollment-failed.md:63` | `#cause-a-package-install` | `#cause-a-package-install-failure-cause-a-package-install` | `30-linux-enrollment-failed.md:53`, `:165`; `l2-runbooks/24-linux-log-collection.md:124`, `:206`; `l2-runbooks/25-linux-agent-investigation.md:35`, `:119`, `:184` |
| `…30:104` | `#cause-b-sign-in-failure` | `#cause-b-sign-in-failure-microsoft-identity-broker-cause-b-sign-in-failure` | `…30:54`; `24-linux-log-collection.md:204`; `25-linux-agent-investigation.md:36`, `:344` |
| `…30:148` | `#cause-c-enrollment-timeout` | `#cause-c-enrollment-timeout-intune-agenttimer-cause-c-enrollment-timeout` | `…30:55`; `24:203`; `25:37` |
| `31-linux-compliance-non-compliant.md:66` | `#cause-a-distro-version-out-of-range` | `#cause-a-distro-version-out-of-range-cause-a-distro-version-out-of-range` | `…31:55` |
| `…31:108` | `#cause-b-disk-not-encrypted` | `#cause-b-disk-not-encrypted-cause-b-disk-not-encrypted` | `…31:56` |
| `…31:150` | `#cause-c-password-policy-not-met` | `#cause-c-password-policy-not-met-cause-c-password-policy-not-met` | `…31:57` |
| `…31:191` | `#cause-d-custom-compliance-failure` | `#cause-d-custom-compliance-failure-cause-d-custom-compliance-failure` | `…31:58`; `24:134`, `:207`; `25:39` |
| `32-linux-ca-blocking-web-access.md:68` | `#cause-a-not-enrolled` | `#cause-a-not-enrolled-cause-a-not-enrolled` | `…32:58` |
| `…32:110` | `#cause-b-non-compliant` | `#cause-b-non-compliant-cause-b-non-compliant` | `…32:59` |
| `…32:151` | `#cause-c-edge-not-signed-in` | `#cause-c-edge-not-signed-in-cause-c-edge-not-signed-in` | `…32:60` |
| `l2-runbooks/25-linux-agent-investigation.md:51` | `#trap-a-kernel-track` | `#trap-a-ubuntu-hwe-vs-ga-kernel-mismatch-trap-a-kernel-track` | `24:205`, `:206`, `:208`; `25:35`, `:40`, `:46` |
| `…25:121` | `#trap-b-delivery-path` | `#trap-b-snap-vs-deb-delivery-path-confusion-trap-b-delivery-path` | `24:205`, `:208`, `:209`; `25:35`, `:47` |
| `…25:186` | `#trap-c-service-state` | `#trap-c-service-state-user-scope-confusion-trap-c-service-state` | `24:203`; `25:37`, `:38`, `:48` |
| `…25:270` | `#trap-d-identity-broker` | `#trap-d-identity-broker-v202-re-enrollment-trap-d-identity-broker` | `24:195`, `:204`, `:210`; `25:36`, `:49` |
| `l2-runbooks/00-index.md:185` | `#android-mam-we-investigation-advisory` | `#android-mam-we-investigation-advisory-android-mam-we-investigation-advisory` | `20-android-app-install-investigation.md:43`, `:168` |
| `l2-runbooks/19-android-enrollment-investigation.md:128` | `#pattern-a-work-profile-not-created-byod` | `#pattern-a-work-profile-not-created-byod-pattern-a-work-profile-not-created-byod` | `l1-runbooks/23-android-work-profile-not-created.md:110` |
| `…19:190` | `#pattern-c-zte-device-claim-failure` | `#pattern-c-zte-device-claim-failure-pattern-c-zte-device-claim-failure` | `l1-runbooks/27-android-zte-enrollment-failed.md:232` |
| `…19:252` | `#pattern-e-tenant-config-universal` | `#pattern-e-tenant-config-universal-enrollment-restriction-pattern-e-tenant-config-universal` | `l1-runbooks/22-android-enrollment-blocked.md:87` |
| `operations/co-management/03-cocmgmt-migration-paths.md:33` | `#autopatch-prerequisites` | `#autopatch-prerequisites-autopatch-prerequisites` | `co-management/00-overview.md:33`, `:45`, `:129`; `02-windows-workload-sliders.md:64`, `:104`, `:146` |
| `operations/patch-management/02-macos-update-enforcement.md:77` | `#deadlines-cutover-dates` | `#deadlines--cutover-dates-deadlines-cutover-dates` | same file `:33` (self-link) |
| `operations/patch-management/04-android-patch-delivery.md:76` | `#deadlines-cutover-dates` | `#deadlines--cutover-dates-deadlines-cutover-dates` | `quick-ref-l2.md:345`, `:347` |
| `…04:50` | `#play-integrity-attestation` | `#play-integrity-attestation-play-integrity-attestation` | `quick-ref-l2.md:341`, `:342`, `:343` |

**`#autopatch-prerequisites` needs care.** The mechanical locator's first hit was the H1 at
`03-cocmgmt-migration-paths.md:9` (`# Migration Paths and Autopatch Prerequisites`). The semantic
target is the H2 at `:33`, `## Autopatch Prerequisites {#autopatch-prerequisites}`, whose true slug
is `autopatch-prerequisites-autopatch-prerequisites`. Confirm the H2 before editing; the H1 is not
the section these six links mean.

**`#deadlines-cutover-dates` appears against two different target files** with the same fragment
and the same true slug. Rewrite each source link against its own resolved target, not by a global
find-and-replace on the fragment string — `quick-ref-l2.md` links to the Android file and
`02-macos-update-enforcement.md` self-links to itself.

**The doubled slugs are correct, not a mistake to "clean up".** GitHub renders the `{#…}` token as
literal visible text in the heading and includes it in the slug; RESEARCH.md Open Question 3
records GitHub's own rendering API returning
`href="#cause-a-package-install-failure-cause-a-package-install"` for exactly this heading. Do NOT
delete the `{#id}` token to shorten the slug — that is a target-side edit outside this plan's
ratified remedy and outside the CARVE's authorized scope for this phase.

Per **D-02**, do not reason from the premise that `{#id}` is "barred" — it is not. The corpus holds
**87** `{#id}` occurrences across 29 files, all on heading lines and all heading-trailing. The
plain-GitHub-auto-slug convention is genuinely ratified (`102-RESEARCH.md:360`, `103-RESEARCH.md:408`,
`91-RESEARCH.md:88`) but **ratified and never enforced**. Enforcing it is not this phase's scope;
leaving all 87 byte-unchanged is.

Split the work into at least three commits grouped by directory family (Linux runbooks, Android
patterns, operations/patch-management) so a bisect lands on a small diff.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs | tail -2; node scripts/validation/carve-gate.mjs | tail -1; node scripts/validation/check-nav-hub-links.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc '#cause-a-package-install)' docs/` returns 0 and `grep -rc '#cause-a-package-install-failure-cause-a-package-install' docs/` returns 7.
    - `grep -rc '#trap-a-kernel-track)' docs/` returns 0 and `grep -rc '#trap-a-ubuntu-hwe-vs-ga-kernel-mismatch-trap-a-kernel-track' docs/` returns 6.
    - `grep -rc '#deadlines--cutover-dates-deadlines-cutover-dates' docs/` returns 3, with the double hyphen intact.
    - `grep -rc '#autopatch-prerequisites)' docs/` returns 0 and `grep -rc '#autopatch-prerequisites-autopatch-prerequisites' docs/` returns 6.
    - `grep -rc '#migration-paths-and-autopatch-prerequisites' docs/` returns 0 — the H1 was not used as the target.
    - `git diff -- docs/ | grep '^+' | grep -cE '^\+#{1,6} '` returns 0 — no heading line was modified.
    - `git diff -- docs/ | grep -c '{#'` returns 0 — no `{#id}` token was added or removed anywhere.
    - `grep -rcE '\{#[a-zA-Z0-9_-]+\}' docs/ --include=*.md | awk -F: '{s+=$2} END {print s}'` still returns 87 — the corpus's `{#id}` population is byte-unchanged.
    - `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations, 0 total violations`.
    - `node scripts/validation/carve-gate.mjs` exits 0 with `off-list=0`.
    - `git log --oneline -- docs/ | head -3` shows at least three commits from this task.
  </acceptance_criteria>
  <done>All `{#id}`-inflated and stale-token Class-B fragments are rewritten source-side to their true GitHub slugs, derived from heading text and confirmed; the corpus's 87 `{#id}` tokens are untouched; c17 and carve-gate unregressed.</done>
</task>

<task type="auto">
  <name>Task 3: Dry-run to zero + close the 77-pair ledger</name>
  <files>.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md</files>
  <read_first>
    - `scripts/validation/check-nav-hub-links.mjs:253-269` — `checkInboundLinks`, `:259` and `:269`
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the dry-run ladder and the 77-pair ledger
  </read_first>
  <action>
Run the established dry-run one final time before the flip: in the working tree only, delete `:259`
(`if (hubSet.has(relPath)) continue;`) and `:269` (`if (!hubSet.has(resolvedRel)) continue;`) from
`checkInboundLinks`, run `node scripts/validation/check-nav-hub-links.mjs --verbose`, capture the
output, then restore with `git checkout -- scripts/validation/check-nav-hub-links.mjs`.

Expected: 274 files, 6252 relative links, **0** broken file targets, **0** broken anchors,
**0** total.

If the total is non-zero, do NOT declare the residue acceptable and do NOT introduce a baseline,
allowlist or expected-failure list — LINK-04 forbids an accepted-violation baseline of any kind.
Instead, reconcile each residual failure against the 77-pair ledger, identify which pair was
mis-classified or mis-rewritten, and repair it in this plan.

Close the ledger in `143-EVIDENCE.md`: assert that all 77 (target-file, fragment) pairs are
accounted for, that the per-class counts sum to 77 pairs and 132 links, and that each pair's
recorded remedy matches what actually landed. Append the final dry-run row to the ladder — the
full measured sequence is 175 -> 173 -> 143 -> 127 -> (Plan 04's measured figure) -> 0.

Also record the D-15 rulings in `143-EVIDENCE.md`, since the flip in Plan 06 makes both live:
(a) `docs/_glossary-linux.md:157` links to `../.planning/research/PITFALLS.md` — the single link
that resolves outside `docs/` to an existing target. It carries no `#fragment`, so under the widened
scan it resolves as a file-existence check and passes. **Ruling: in scope, not excluded** — the
checker validates that a relative link resolves to a file that exists, and `.planning/` being
outside `carve-gate`'s `IN_SCOPE_PREFIXES` governs what may be EDITED, not what may be LINKED TO.
Record the reason so a later maintainer does not add an exclusion for it.
(b) `relNormalize:75-80` strips only a `cwd` prefix, so a target resolving ABOVE `cwd` would stay
absolute and produce a bogus "anchor not found". Zero instances exist today — all 14
outside-`docs/` links resolve to paths under `cwd`, 13 of which Plan 03 repaired. Record this as a
known latent class with zero live instances rather than adding speculative handling.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; echo "hub_exit=$?"; git status --porcelain scripts/validation/check-nav-hub-links.mjs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The dry-run summary line reports `0 outbound failure(s), 0 inbound failure(s), 0 total` with the patched scan covering 274 files and 6252 relative links.
    - `git status --porcelain scripts/validation/check-nav-hub-links.mjs | wc -l` returns 0 after the dry-run.
    - `node scripts/validation/check-nav-hub-links.mjs` (unpatched) exits 0.
    - `143-EVIDENCE.md`'s per-pair ledger shows 77 rows, every row with a class and a landed remedy, and the link-count column summing to 132.
    - `143-EVIDENCE.md`'s dry-run ladder has six measured rows ending at 0.
    - `143-EVIDENCE.md` records both D-15 rulings with their reasons.
    - `git status --porcelain | grep -vc '^.. \.planning/'` returns 0 — no untracked or modified file outside `.planning/` remains.
  </acceptance_criteria>
  <done>The corpus-wide dry-run reports zero failures with no baseline of any kind; all 77 pairs are closed in the ledger; both D-15 rulings are recorded; the flip is still uncommitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target heading text -> rewritten source fragment | The heading is the source of truth; a fragment derived from anything else (a prefix match, a table row, a memory of the old slug) silently produces a link that resolves in nobody's renderer. |
| corpus link rewrite -> published `.docx` / SharePoint knowledge base | A wrong repair ships as wrong navigation in the deliverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-18 | Spoofing | prefix-match masquerading as semantic match | high | mitigate | Every rewrite must be derived from the heading text with `githubSlug` and confirmed against the table before writing; `#autopatch-prerequisites` carries an explicit "the H1 is not the section these links mean" instruction with a grep asserting the H1 slug is absent. |
| T-143-19 | Tampering | global find-and-replace across a shared fragment string | high | mitigate | `#deadlines-cutover-dates` resolves against two different target files; the action bars a global replace and requires per-link resolution against its own target. |
| T-143-20 | Tampering | unauthorized target-side edit | high | mitigate | A prohibition plus two greps assert zero modified heading lines and zero `{#id}` tokens added or removed, and a third asserts the corpus `{#id}` population stays at 87. |
| T-143-21 | Repudiation | residual break absorbed silently | high | mitigate | Task 3 forbids declaring a non-zero residue acceptable and requires reconciliation against the 77-pair ledger; an acceptance criterion asserts the dry-run total is 0 and `git status` is clean outside `.planning/`. |
| T-143-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs; this plan edits Markdown only. `143-RESEARCH.md` Package Legitimacy Audit: "Not applicable". |
</threat_model>

<verification>
- Dry-run: 274 files / 6252 links / **0** broken file targets / **0** broken anchors / **0** total.
- `node scripts/validation/check-nav-hub-links.mjs` -> exit 0.
- `node scripts/validation/c17-eee-contract.mjs` -> `234 files checked, 0 with violations, 0 total violations`.
- `node scripts/validation/carve-gate.mjs` -> exit 0, `off-list=0`.
- `grep -rcE '\{#[a-zA-Z0-9_-]+\}' docs/ --include=*.md` sums to 87 — unchanged.
- `143-EVIDENCE.md`: 77 pairs closed, 132 links, six-row dry-run ladder ending at 0.

</verification>

<success_criteria>
Every Class-B fragment is rewritten source-side to a heading-derived true GitHub slug; the doubled
slugs are preserved as correct; no heading, `{#id}` token or target-side line is modified; the
corpus-wide dry-run reports zero with no baseline; the 77-pair ledger is closed.
</success_criteria>

## Artifacts this phase produces

Created or modified by **this plan**:

| Artifact | Kind | Note |
|---|---|---|
| ~31 rewritten link fragments across ~25 source files | corpus edits | source-side only, heading-derived slugs |
| D-15(a) `.planning/` link scope ruling | decision record in `143-EVIDENCE.md` | in scope, passes as a file-existence check |
| D-15(b) above-`cwd` `relNormalize` latent class | decision record in `143-EVIDENCE.md` | zero live instances, no speculative handling |
| Closed 77-pair remedy ledger | evidence table | every pair with class + landed remedy |
| Six-row dry-run ladder ending at 0 | evidence table | 175 -> 173 -> 143 -> 127 -> measured -> 0 |
| `v1.20-GOV-02-LEDGER.md` row (Class-B corpus set) | ledger row | row-per-edit, appended |

<output>
Create `.planning/phases/143-link-coverage-fence-mask-unification/143-05-SUMMARY.md` when done.
</output>
