---
phase: 143-link-coverage-fence-mask-unification
plan: 03
type: execute
wave: 4
depends_on: ["143-01", "143-02", "143-09"]
files_modified:
  - docs/_glossary-macos.md
  - docs/admin-setup-ios/04-configuration-profiles.md
  - docs/admin-setup-ios/07-device-enrollment.md
  - docs/_glossary-apple-business.md
  - docs/linux-lifecycle/01-linux-prerequisites.md
  - docs/admin-setup-macos/10-kerberos-sso-extension.md
  - docs/l2-runbooks/24-linux-log-collection.md
  - docs/l2-runbooks/25-linux-agent-investigation.md
  - docs/admin-setup-android/06-aosp-stub.md
  - docs/l2-runbooks/23-android-aosp-investigation.md
  - docs/error-codes/01-mdm-enrollment.md
  - docs/error-codes/03-esp-enrollment.md
  - docs/error-codes/04-pre-provisioning.md
  - docs/error-codes/05-hybrid-join.md
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: true
requirements: [LINK-03, LINK-04]
estimate:
  tokens: 84000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 13 genuine broken file targets are repaired by removing exactly one `../` level from the link path — 11 in `docs/_glossary-macos.md` (lines 103, 111, 151, 161, 171 twice, 189, 199, 207, 252, 327) and 2 in `docs/admin-setup-ios/` (`04-configuration-profiles.md:165`, `07-device-enrollment.md:46`)."
    - "The four LINK-03 destination files all exist and are unmodified by this plan: `docs/operations/patch-management/03-ios-update-lifecycle.md`, `docs/macos-lifecycle/02-mdm-migration-psso.md`, `docs/l2-runbooks/30-macos-mdm-migration-failure.md`, `docs/macos-lifecycle/01-psso-provisioning-walkthrough.md`."
    - "LINK-03 adds ZERO new anchor failures: the three fragments that become evaluable once the path resolves — `#stage-2-intune-readiness-secret-retrieval-and-source-release`, `#stage-7-post-migration-profile-based-enrollment-b1-path`, `#stage-9-psso-re-registration-b1-path` — all resolve under the GitHub model against `docs/macos-lifecycle/02-mdm-migration-psso.md` headings at `:139`, `:338` and `:396`."
    - "Every Class-D link drops only its `#fragment` and keeps its file target; no link is deleted, no replacement prose is authored, and no missing section is added."
    - "`#intune` is de-anchored, not repointed: `docs/_glossary.md` carries no `### Intune` entry — it carries `### Intune Management Extension (IME)` (`:95`) and `### Intune Provisioning Client`, which are different concepts. A prefix match is not a semantic match (D-06)."
    - "The standing rule 'a corpus edit requires proof the document is wrong' is discharged for every edit in this plan: for LINK-03 by the four destinations existing and the breaks being plain `../` over-escapes; for Class D by the absence of any semantically correct anchor in the target file."
    - "LINK-03 concurrency: the corpus repairs are sequential commits on the main tree (`use_worktrees:false`); the checker is a read-only single-process scan, so no partial-write or interleaving hazard exists."
    - "After this plan the dry-run corpus total falls from 78 to 49 — 0 broken file targets and 49 broken anchors. The starting figure is 78, not 143, because Plan 09's D-38 conversion already closed 65 anchor links target-side."
    - "This plan runs AFTER Plan 09's conversion, so every `file:line` citation into one of the 29 converted files is stale by one line per preceding converted heading in that file. `docs/l2-runbooks/25-linux-agent-investigation.md:352` is now `:356` and `docs/l2-runbooks/23-android-aosp-investigation.md:362` is now `:367`. Locate every edit by its content, never by a pre-conversion line number."
    - "`docs/_glossary-android.md#aosp` is Class D, adjudicated at plan time: `docs/_glossary-android.md` carries NO `AOSP` heading at any level — the only occurrences are prose at `:57`, `:222`, `:245`, `:247`, `:249`, `:277` and a changelog row at `:331`. The mechanical locator's changelog hit is a false friend of exactly the `#intune` kind."
    - "The ETG pair is NOT in this plan. `docs/lifecycle-apv2/00-overview.md:40` carries `## Enrollment Time Grouping (ETG) -- The Core Mechanism`, whose true GitHub slug is `enrollment-time-grouping-etg----the-core-mechanism`; the incoming fragment differs by exactly one omitted token, making it Class B and Plan 05's work."
  artifacts:
    - docs/_glossary-macos.md
    - docs/admin-setup-ios/04-configuration-profiles.md
    - docs/admin-setup-ios/07-device-enrollment.md
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
  key_links:
    - "`docs/_glossary-macos.md` link paths -> `docs/macos-lifecycle/02-mdm-migration-psso.md` Stage 2/7/9 headings — the three fragments that only become evaluable after the path is repaired."
    - "Category 8 (`docs/_glossary-macos.md`, `docs/admin-setup-ios/**`) pre-authorizes the LINK-03 edits; Category 10 from Plan 01 authorizes every Class-D file."
  prohibitions:
    - statement: "Neither `docs/_glossary-macos.md` nor `docs/_glossary-android.md` has its `last_verified` or `review_by` metadata touched — both sit at exactly 90 days against a `>90` freshness test and a one-day edit flips multiple currently-green workflows red (v1.20-CARVE.md:178-182). All 11 LINK-03 fixes land in the macOS glossary, so this bar is live in this plan."
      status: flagged-unverified
      verification: "`git diff -- docs/_glossary-macos.md | grep -cE '^[+-](last_verified|review_by)'` returns 0."
    - statement: "No new documentation content is authored — every edit removes characters (`../` levels or `#fragment` suffixes) from an existing line."
      status: flagged-unverified
      verification: "`git diff -- docs/ --numstat` shows equal added and removed line counts per file (each edit is a same-line replacement), and no added line introduces a sentence absent from the removed line."
    - statement: "No missing section, heading, glossary entry or table row is added to cure a Class-D break (D-04: 'add the missing section' is BARRED)."
      status: flagged-unverified
      verification: "`git diff -- docs/ | grep '^+' | grep -cE '^\\+#{1,6} |^\\+\\|'` returns 0."
    - statement: "No corpus edit lands without recorded proof the document is wrong."
      status: flagged-unverified
      verification: "The plan SUMMARY carries a per-edit proof line; for LINK-03 the four destination `test -f` results, for Class D the target file's anchor-set membership check showing the fragment absent and no semantic equivalent present."
---

<objective>
Clear the two remedy classes that require no new anchor: LINK-03's 13 genuine broken file targets
(plain `../` over-escapes) and the Class-D anchors for which no correct target exists anywhere in
the target file.

Purpose: LINK-03's 13 are the phase's only *file*-target breaks, and 6 of the 11 macOS-glossary
ones carry anchor fragments that are never evaluated today because
`check-nav-hub-links.mjs:236-241` `continue`s when the target file is missing (D-09) — so they must
be repaired before the anchor population can be declared final. Class D is the honest end of D-04's
routing: where no correct target exists, the fragment is dropped and the link points at the file.
`#intune` is the canonical case — four links, a plausible prefix match, and no correct target.

Output: 0 broken file targets, 16 fewer broken anchors (78 -> 49), and a recorded per-edit proof
that each document was wrong.

This plan runs at wave 4, after Plan 09's D-38 conversion. Its Class-D set was re-derived against
the post-conversion 51-pair state: `#aosp` joins as Class D, the ETG pair leaves as Class B, and
the total stays at 12 pairs / 16 links.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md
@.planning/milestones/v1.20-CARVE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LINK-03 — the 13 `../` over-escaped file targets</name>
  <files>docs/_glossary-macos.md, docs/admin-setup-ios/04-configuration-profiles.md, docs/admin-setup-ios/07-device-enrollment.md, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `docs/_glossary-macos.md` — lines 103, 111, 151, 161, 171, 189, 199, 207, 252, 327 and the frontmatter block carrying `last_verified` / `review_by`
    - `docs/admin-setup-ios/04-configuration-profiles.md:165` and `docs/admin-setup-ios/07-device-enrollment.md:46`
    - `docs/macos-lifecycle/02-mdm-migration-psso.md` — heading lines `:139`, `:338`, `:396`
    - `.planning/milestones/v1.20-CARVE.md:178-185` — both Standing bars
  </read_first>
  <action>
Repair all 13 by removing exactly one `../` level. Every one is a plain over-escape: the link
already names the correct destination file, it just escapes one directory too far.

`docs/_glossary-macos.md` sits at `docs/`, so `../macos-lifecycle/…` resolves to repo-root
`macos-lifecycle/…`. Rewrite each of these 11 to drop the leading `../`:

| line | current target | repaired target |
|---|---|---|
| 103 | `../macos-lifecycle/02-mdm-migration-psso.md#stage-7-post-migration-profile-based-enrollment-b1-path` | `macos-lifecycle/02-mdm-migration-psso.md#stage-7-post-migration-profile-based-enrollment-b1-path` |
| 111 | `../macos-lifecycle/02-mdm-migration-psso.md#stage-7-post-migration-profile-based-enrollment-b1-path` | same, `../` dropped |
| 151 | `../macos-lifecycle/02-mdm-migration-psso.md` | `macos-lifecycle/02-mdm-migration-psso.md` |
| 161 | `../macos-lifecycle/02-mdm-migration-psso.md` | `macos-lifecycle/02-mdm-migration-psso.md` |
| 171 | `../macos-lifecycle/02-mdm-migration-psso.md` | `macos-lifecycle/02-mdm-migration-psso.md` |
| 171 | `../l2-runbooks/30-macos-mdm-migration-failure.md` | `l2-runbooks/30-macos-mdm-migration-failure.md` |
| 189 | `../macos-lifecycle/02-mdm-migration-psso.md#stage-2-intune-readiness-secret-retrieval-and-source-release` | same, `../` dropped |
| 199 | `../macos-lifecycle/02-mdm-migration-psso.md#stage-2-intune-readiness-secret-retrieval-and-source-release` | same, `../` dropped |
| 207 | `../macos-lifecycle/02-mdm-migration-psso.md#stage-2-intune-readiness-secret-retrieval-and-source-release` | same, `../` dropped |
| 252 | `../macos-lifecycle/01-psso-provisioning-walkthrough.md` | `macos-lifecycle/01-psso-provisioning-walkthrough.md` |
| 327 | `../macos-lifecycle/02-mdm-migration-psso.md#stage-9-psso-re-registration-b1-path` | same, `../` dropped |

Line 171 carries TWO broken links; repair both.

`docs/admin-setup-ios/` sits one level below `docs/`, so `../../operations/…` resolves to repo-root
`operations/…`. Rewrite both to `../operations/patch-management/03-ios-update-lifecycle.md`:
`04-configuration-profiles.md:165` and `07-device-enrollment.md:46`.

Change nothing else in any of the three files. In `docs/_glossary-macos.md` specifically, do not
touch `last_verified` or `review_by` — the file sits at exactly 90 days against a `>90` freshness
test and a one-day edit flips multiple currently-green workflows red.

Record the discharge of the standing "a corpus edit requires proof the document is wrong" rule:
all four destinations exist on disk, and the three anchor fragments that only become evaluable once
the paths resolve all match real headings — `docs/macos-lifecycle/02-mdm-migration-psso.md:139`
`## Stage 2: Intune Readiness, Secret Retrieval, and Source Release`, `:338`
`## Stage 7: Post-Migration Profile-Based Enrollment (B1 Path)`, `:396`
`## Stage 9: PSSO Re-Registration (B1 Path)` — whose GitHub slugs are byte-identical to the three
fragments. So LINK-03 adds zero new anchor failures.

Append one GOV-02 ledger row covering the three edited corpus files (Category 8 pre-authorized;
row-per-edit, appended at the end, existing rows untouched).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs | tail -2; node scripts/validation/carve-gate.mjs | tail -1; node scripts/validation/check-nav-hub-links.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '](\.\./macos-lifecycle/' docs/_glossary-macos.md` returns 0.
    - `grep -c '](\.\./l2-runbooks/' docs/_glossary-macos.md` returns 0.
    - `grep -c '](macos-lifecycle/02-mdm-migration-psso.md' docs/_glossary-macos.md` returns 7.
    - `grep -c '](\.\./\.\./operations/' docs/admin-setup-ios/04-configuration-profiles.md docs/admin-setup-ios/07-device-enrollment.md` returns 0 for both files.
    - `grep -c '](\.\./operations/patch-management/03-ios-update-lifecycle.md' docs/admin-setup-ios/04-configuration-profiles.md` returns 1, and the same for `07-device-enrollment.md`.
    - All four destinations exist: `test -f docs/operations/patch-management/03-ios-update-lifecycle.md && test -f docs/macos-lifecycle/02-mdm-migration-psso.md && test -f docs/l2-runbooks/30-macos-mdm-migration-failure.md && test -f docs/macos-lifecycle/01-psso-provisioning-walkthrough.md`.
    - `git diff -- docs/_glossary-macos.md | grep -cE '^[+-](last_verified|review_by)'` returns 0.
    - `node scripts/validation/c17-eee-contract.mjs` still prints `234 files checked, 0 with violations, 0 total violations`.
    - `node scripts/validation/carve-gate.mjs` exits 0 with `off-list=0`.
    - The Task-3 dry-run reports **0** broken file targets (down from 13) — proving D-09's stability claim independently, since the three Stage fragments become evaluable only once the paths resolve and none of them adds a failure.
  </acceptance_criteria>
  <done>Zero broken file targets remain; the three newly-evaluable Stage fragments all resolve; the glossary's freshness metadata is byte-unchanged; c17 and carve-gate are unregressed.</done>
</task>

<task type="auto">
  <name>Task 2: Class D — drop the fragment where no correct target exists</name>
  <files>docs/_glossary-apple-business.md, docs/linux-lifecycle/01-linux-prerequisites.md, docs/admin-setup-macos/10-kerberos-sso-extension.md, docs/l2-runbooks/24-linux-log-collection.md, docs/l2-runbooks/25-linux-agent-investigation.md, docs/admin-setup-android/06-aosp-stub.md, docs/l2-runbooks/23-android-aosp-investigation.md, docs/error-codes/01-mdm-enrollment.md, docs/error-codes/03-esp-enrollment.md, docs/error-codes/04-pre-provisioning.md, docs/error-codes/05-hybrid-join.md</files>
  <read_first>
    - `docs/_glossary.md` — the full heading list, specifically `:95` `### Intune Management Extension (IME)` and the absence of any `### Intune` entry
    - `docs/_glossary-android.md` — the full heading list (`## Enrollment`, `### BYOD`, `### COBO`, `### COPE` …), confirming the absence of any `AOSP` heading at any level
    - `docs/lifecycle-apv2/00-overview.md:40` — `## Enrollment Time Grouping (ETG) -- The Core Mechanism`, the heading that makes the ETG pair Class B and therefore NOT this task's work
    - `docs/_glossary-macos.md`, `docs/cross-platform/apple-business/01-role-permission-model.md`, `docs/admin-setup-linux/01-intune-linux-agent.md`, `docs/reference/linux-capability-matrix.md`, `docs/reference/registry-paths.md`, `docs/admin-setup-macos/10-kerberos-sso-extension.md` — the heading lists of every Class-D target file, to confirm the fragment has no semantic equivalent before de-anchoring
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md` — D-04 (de-anchor remedy), D-06 (`#intune` is a false friend)
  </read_first>
  <action>
For each link below, delete the `#fragment` from the link target and keep the file path. Change
nothing else on the line — the link text stays byte-identical. Do NOT add a heading, glossary entry
or table row to cure any of these; "add the missing section" is BARRED (D-04) and the CARVE
Standing bar forbids new documentation content.

**The de-anchor set — 12 (target-file, fragment) pairs / 16 links.** For each, confirm in the
target file that the fragment has no semantic equivalent BEFORE editing, and record that check.
The set was re-derived at plan time against the POST-conversion corpus (Plan 09's 51-pair /
67-link state), so it differs from the pre-ruling draft in exactly two ways: `#aosp` joins as a
newly-adjudicated Class D, and the ETG pair leaves as a newly-adjudicated Class B.

| target file # fragment | links to de-anchor |
|---|---|
| `docs/_glossary.md#intune` | `docs/error-codes/01-mdm-enrollment.md:23`, `docs/error-codes/03-esp-enrollment.md:33`, `docs/error-codes/04-pre-provisioning.md:41`, `docs/error-codes/05-hybrid-join.md:35` |
| `docs/_glossary-android.md#aosp` | `docs/admin-setup-android/06-aosp-stub.md:101`, `docs/l2-runbooks/23-android-aosp-investigation.md:367` (post-conversion line) |
| `docs/_glossary-macos.md#abm-apple-business-manager` | `docs/_glossary-apple-business.md:60` |
| `docs/_glossary-macos.md#managed-apple-id` | `docs/_glossary-apple-business.md:144` |
| `docs/cross-platform/apple-business/01-role-permission-model.md#account-holder-do-not-delegate` | `docs/_glossary-apple-business.md:52` |
| `docs/cross-platform/apple-business/01-role-permission-model.md#intune-side-labels-preserved` | `docs/_glossary-apple-business.md:116` |
| `docs/admin-setup-linux/01-intune-linux-agent.md#identity-broker-v202-re-enrollment` | `docs/linux-lifecycle/01-linux-prerequisites.md:70` |
| `docs/admin-setup-macos/10-kerberos-sso-extension.md#k-1-wrong-extension-identifier` | `docs/admin-setup-macos/10-kerberos-sso-extension.md:45` (a self-link) |
| `docs/admin-setup-macos/10-kerberos-sso-extension.md#k-5-wrong-payload-type` | `docs/admin-setup-macos/10-kerberos-sso-extension.md:46` (a self-link) |
| `docs/reference/linux-capability-matrix.md#monitoring` | `docs/l2-runbooks/24-linux-log-collection.md:218` |
| `docs/reference/linux-capability-matrix.md#supported-management-surface` | `docs/l2-runbooks/25-linux-agent-investigation.md:356` (post-conversion line; was `:352`) |
| `docs/reference/registry-paths.md#winlogon` | `docs/error-codes/03-esp-enrollment.md:45` |

**`#intune` is the canonical case and must not be "helpfully" repaired.** `docs/_glossary.md` has
no `### Intune` entry. Its nearest headings are `### Intune Management Extension (IME)` at `:95`
and `### Intune Provisioning Client`. A prefix match is not a semantic match: neither heading is
the concept "Intune". Under D-04 and the no-new-content bar the remedy is de-anchor — do NOT
repoint to `#intune-management-extension-ime` and do NOT add a `### Intune` glossary entry. D-06
records the false-friend count as **5 of 12**, so treat every prefix-plausible candidate in this
table with the same suspicion.

**Two of these are self-links.** `docs/admin-setup-macos/10-kerberos-sso-extension.md:45` and `:46`
link into their own file at fragments `#k-1-wrong-extension-identifier` and
`#k-5-wrong-payload-type` that do not exist as headings — they name items in a bulleted list.
De-anchoring a self-link leaves a link whose target is the file itself; if the resulting link is a
bare `(#…)`-only form with nothing left after the fragment is removed, drop the link markup and
keep the link TEXT as plain text on the line rather than emitting an empty `()` target.

**`#enrollment-status-page` is NOT in this set.** D-06 rules it a clean Class-B substitution to
`#esp`; it is repaired in Plan 05, not here.

Re-audit every "no target" claim before acting on it (D-03): a stale-by-one-token anchor and a
missing section have opposite remedies. Two pairs were adjudicated at plan time by exactly this
re-audit; confirm each ruling against the file rather than accepting it:

- **`docs/lifecycle-apv2/00-overview.md#enrollment-time-grouping----the-core-mechanism` is Class B,
  and is therefore ABSENT from the table above.** The heading `## Enrollment Time Grouping (ETG)
  -- The Core Mechanism` DOES exist at `:40`; the incoming fragment differs from its true GitHub
  slug `enrollment-time-grouping-etg----the-core-mechanism` by exactly one omitted token (`etg`).
  Plan 05 rewrites it. Do NOT de-anchor it here.
- **`docs/_glossary-android.md#aosp` is Class D, and is therefore PRESENT in the table above.** The
  file carries no `AOSP` heading at any level — `grep -n '^#\{1,6\}.*AOSP' docs/_glossary-android.md`
  returns nothing. AOSP appears only in prose (`:57`, `:222`, `:245`, `:247`, `:249`, `:277`) and
  in a changelog row (`:331`). Confirm that grep returns empty before de-anchoring; if a heading
  has appeared since plan time, this pair is Class B and belongs in Plan 05 instead.

Locate every edit by the link's own text and target string, never by the line numbers above:
`docs/l2-runbooks/23-android-aosp-investigation.md` and
`docs/l2-runbooks/25-linux-agent-investigation.md` were both rewritten by Plan 09's conversion and
their line numbering has shifted.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs | tail -2; node scripts/validation/carve-gate.mjs | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc '_glossary.md#intune)' docs/error-codes/` returns 0 across all five error-code files.
    - `grep -rc '#intune-management-extension-ime' docs/error-codes/` returns 0 — the false friend was not substituted.
    - `grep -c '### Intune$' docs/_glossary.md` returns 0 — no glossary entry was added.
    - `grep -rEc '#(abm-apple-business-manager|managed-apple-id|account-holder-do-not-delegate|intune-side-labels-preserved)' docs/_glossary-apple-business.md` returns 0.
    - `grep -c '#identity-broker-v202-re-enrollment' docs/linux-lifecycle/01-linux-prerequisites.md` returns 0.
    - `grep -Ec '#(k-1-wrong-extension-identifier|k-5-wrong-payload-type)' docs/admin-setup-macos/10-kerberos-sso-extension.md` returns 0.
    - `grep -Ec '#(monitoring|supported-management-surface)' docs/l2-runbooks/24-linux-log-collection.md docs/l2-runbooks/25-linux-agent-investigation.md` returns 0 for the specific `linux-capability-matrix.md` links.
    - `grep -c 'registry-paths.md#winlogon' docs/error-codes/03-esp-enrollment.md` returns 0.
    - `grep -rc '_glossary-android.md#aosp)' docs/` returns 0 across `docs/admin-setup-android/06-aosp-stub.md` and `docs/l2-runbooks/23-android-aosp-investigation.md`, and `grep -c '^### AOSP' docs/_glossary-android.md` returns 0 — de-anchored, not invented.
    - `grep -rc '#enrollment-time-grouping----the-core-mechanism' docs/admin-setup-apv2/02-etg-device-group.md` still returns 2 — the ETG pair was NOT de-anchored here; it is Plan 05's Class-B rewrite.
    - `grep -rc '](\s*)' docs/` returns 0 — no empty link target was produced by a fragment removal.
    - `git diff -- docs/ | grep '^+' | grep -cE '^\+#{1,6} |^\+\|'` returns 0 — no heading and no table row was added.
    - `node scripts/validation/c17-eee-contract.mjs` still prints `234 files checked, 0 with violations, 0 total violations`.
    - `node scripts/validation/carve-gate.mjs` exits 0 with `off-list=0`.
  </acceptance_criteria>
  <done>All 12 Class-D pairs de-anchored (or reclassified to B with a recorded re-audit); `#intune` de-anchored rather than repointed or invented; zero headings, glossary entries or table rows added; c17 and carve-gate unregressed.</done>
</task>

<task type="auto">
  <name>Task 3: Dry-run checkpoint — corpus total falls 78 -> 49</name>
  <files>.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md</files>
  <read_first>
    - `scripts/validation/check-nav-hub-links.mjs:253-269` — `checkInboundLinks`, specifically `:259` and `:269`
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-09-SUMMARY.md` — the recorded dry-run procedure and the measured 78 baseline
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the dry-run ladder and Plan 09's post-conversion class split (31 C / 12 D / 8 B)
  </read_first>
  <action>
Run the same temporary-widening dry-run Plan 02 established, to measure this plan's contribution
and to catch any regression before Plans 04 and 05 build on it. In the working tree only, delete
`:259` (`if (hubSet.has(relPath)) continue;`) and `:269`
(`if (!hubSet.has(resolvedRel)) continue;`) from `checkInboundLinks`, run
`node scripts/validation/check-nav-hub-links.mjs --verbose`, capture the full failure list and the
summary line, then restore with `git checkout -- scripts/validation/check-nav-hub-links.mjs`.

Expected: 274 files, 6252 relative links, **0** broken file targets, **49** broken anchors,
**49** total — down from the 78 Plan 09 measured (13 file targets cleared by Task 1, 16 anchor
links cleared by Task 2). If either adjudication in Task 2 came out the other way, the figure moves
by 2 in the corresponding direction; record whichever holds and reconcile it against Plan 09's
class-split table rather than adjusting the projection.

Append to `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the file
already exists, created by Plan 02 Task 1 and extended by Plan 09 Task 3. Do NOT recreate or
overwrite it. Add the per-class remedy ledger keyed to Plan 09's 51-pair / 67-link split, and
append this plan's row to the existing dry-run ladder: state (which D-14 steps and which remedy
classes have landed), files, links, broken file targets, broken anchors, total. The ladder now
reads 175 (LINK-01 only, templates in, no inline mask) -> 173 (after the tracer repair) -> 143
(after exclusion + masking) -> 78 (after D-38's all-87 conversion) -> **49** (after this plan), and
stays open for Plans 04, 05 and 06. This ledger is the evidence artifact D-22 and the
"Evidence-artifact format for the mask-delta table and the per-class remedy ledger" discretion item
call for; Plan 07 extends it with the LINK-06 mask-state diff.

Do not commit the dry-run patch. Do not delete `:259`/`:269` permanently — that is Plan 06's
commit.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; echo "hub_exit=$?"; git status --porcelain scripts/validation/check-nav-hub-links.mjs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The dry-run summary line reports a total of **49**, with **0** broken file targets, and any divergence is reconciled against Plan 09's class-split table.
    - `grep -c 'target file not found' <dry-run output>` returns 0.
    - `grep -rc '{#' docs/ --include=*.md | awk -F: '{s+=$2} END {print s}'` still returns 0 — this plan re-introduced no `{#id}` token.
    - `git status --porcelain scripts/validation/check-nav-hub-links.mjs | wc -l` returns 0 after the dry-run — the patch was reverted.
    - `node scripts/validation/check-nav-hub-links.mjs` (unpatched) still exits 0 with `0 outbound failure(s), 0 inbound failure(s), 0 total`.
    - `143-EVIDENCE.md` already existed before this task ran and now carries a dry-run ladder with at least 5 measured rows ending at 49.
  </acceptance_criteria>
  <done>Broken file targets are zero, the dry-run total is 49, the evidence ledger gains this plan's row, and the flip is still uncommitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus repair -> published `.docx` / SharePoint knowledge base | Every corpus edit reaches the Copilot Studio knowledge base via the publish bundle; a wrong repair ships as wrong guidance. |
| glossary frontmatter -> six freshness assertions across v1.5-v1.13 harnesses | Both glossaries sit at exactly 90 days against a `>90` test; a one-character metadata edit flips currently-green workflows red. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-09 | Tampering | `docs/_glossary-macos.md` frontmatter | high | mitigate | A dedicated acceptance criterion asserts `git diff` contains zero `+`/`-` lines beginning `last_verified` or `review_by`; the task action names the bar explicitly at the point of edit. |
| T-143-10 | Spoofing | false-friend anchor substitution | high | mitigate | `#intune` is named as the canonical false friend with the specific forbidden substitution (`#intune-management-extension-ime`) asserted absent by grep; D-06's 5-of-12 false-friend rate is stated so the executor treats every prefix-plausible candidate as suspect. |
| T-143-11 | Denial of Service | empty link target from fragment removal | medium | mitigate | Acceptance criterion greps for `](` followed only by whitespace across `docs/`; the self-link case carries an explicit instruction to degrade to plain text rather than emit an empty target. |
| T-143-12 | Repudiation | undocumented corpus edit | medium | mitigate | The standing "proof the document is wrong" rule is discharged per-edit in the SUMMARY: four `test -f` results for LINK-03, an anchor-set membership check per Class-D pair. |
| T-143-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs; this plan edits Markdown only. `143-RESEARCH.md` Package Legitimacy Audit: "Not applicable". |
</threat_model>

<verification>
- Dry-run: 274 files / 6252 links / **0** broken file targets / **49** broken anchors / **49** total.
- `node scripts/validation/check-nav-hub-links.mjs` -> exit 0, `0 outbound / 0 inbound / 0 total`.
- `node scripts/validation/c17-eee-contract.mjs` -> `234 files checked, 0 with violations, 0 total violations`.
- `node scripts/validation/carve-gate.mjs` -> exit 0, `off-list=0`.
- `git diff -- docs/_glossary-macos.md docs/_glossary-android.md | grep -cE '^[+-](last_verified|review_by)'` -> 0.
</verification>

<success_criteria>
Zero broken file targets; 12 Class-D pairs de-anchored with no invented content; the `#intune` and
`#aosp` false friends left de-anchored; the ETG pair correctly routed to Plan 05 as Class B;
glossary freshness metadata byte-unchanged; dry-run total 49; the evidence ladder gains its fifth
measured row.
</success_criteria>

## Artifacts this phase produces

Created or modified by **this plan**:

| Artifact | Kind | Note |
|---|---|---|
| 13 repaired file-target link paths | corpus edit | one `../` level removed each |
| 16 de-anchored links across 12 pairs | corpus edit | `#fragment` dropped, file target kept; includes `#aosp`, excludes ETG |
| `143-EVIDENCE.md` per-class remedy ledger + ladder row 5 | evidence (appended, not created) | 78 -> 49 |
| `v1.20-GOV-02-LEDGER.md` row (Category 8 corpus set) | ledger row | row-per-edit, appended |

<output>
Create `.planning/phases/143-link-coverage-fence-mask-unification/143-03-SUMMARY.md` when done.
</output>
