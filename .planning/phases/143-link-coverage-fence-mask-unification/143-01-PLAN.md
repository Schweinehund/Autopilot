---
phase: 143-link-coverage-fence-mask-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/milestones/v1.20-CARVE.md
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: true
requirements: [LINK-01, LINK-02, LINK-03, LINK-04]
estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md LINK-03 reads '13 genuine broken file targets and 132 genuine broken anchors', carrying a **[SUCCESS-CRITERION AMENDMENT, D-29]** marker, with the superseded original text preserved verbatim (annotate-and-supersede, never overwrite)."
    - "All SEVEN D-29 amendment surfaces carry a marker: REQUIREMENTS LINK-03, REQUIREMENTS:59 sequencing note, ROADMAP Phase 143 SC#1, SC#2, SC#3, the Goal line, and the Discuss-phase flags line."
    - "ROADMAP Phase 143 SC#1 carries BOTH scope-tagged pairs — 271 -> 70 (templates included, no inline mask; the discarded prototype scope) and 268 -> 67 -> 132 (the LINK-02 scope, Pandoc then GitHub model) — because under D-14's ordering no state of the shipped checker ever emits 271 or 70."
    - "v1.20-CARVE.md gains a Category 10 block enumerating the 63 corpus paths outside Category 8's existing roster — the pre-ruling 49-path roster UNION the 29 `{#id}`-override files D-38 converts, of which 15 were already listed, so the delta is exactly 14 — and one 'Recorded scope amendments' bullet per D-29 amendment plus one for D-38."
    - "Every one of the 63 Category 10 paths exists on disk at plan time; an off-list in-scope path is a hard exit 1 at `carve-gate.mjs` with no warn-and-continue path, so an under-listed roster blocks execution mid-phase."
    - "The CARVE amendment commit touches exactly one file: `git show --stat HEAD -- . | grep -c '|'` resolves to a single changed path, `.planning/milestones/v1.20-CARVE.md`."
    - "The SC-amendment commit and the CARVE amendment commit are two SEPARATE commits in that order (D-31 steps 1 and 2), never fused."
    - "`node scripts/validation/carve-gate.mjs` exits 0 after both commits."
    - "v1.20-GOV-02-LEDGER.md gains one appended row covering the two `.planning/` governance documents, following the row-23 batching precedent, with a Result column stating a concrete diff size."
    - "LINK-04 no-baseline: no accepted-violation baseline, allowlist, ratchet file or expected-failure list of any kind is created for the link checker in this plan or any later plan of this phase."
    - "LINK-01 encoding: the checker compares the authored fragment against the authored id, both UN-prefixed; GitHub rewrites authored ids to `user-content-<id>` at render time while heading permalink hrefs stay unprefixed, so modelling the prefix would be wrong."
    - "LINK-02 ordering: failure-report order follows walkMd's readdirSync traversal then line order — deterministic on a given tree, not lexically sorted."
    - statement: "LINK-02 ordering is stable across filesystems and Node versions."
      verification: backstop
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/milestones/v1.20-CARVE.md
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
  key_links:
    - "v1.20-CARVE.md 'Recorded scope amendments' section -> Phase 144's close-gate (Phase 142 D-33: the close-gate reads this section, NOT the in-line markers)."
    - "v1.20-CARVE.md Category 10 globs -> carve-gate.mjs globToRegExp -> every corpus repair in Plans 03/04/05 (an off-list path is a hard exit 1 with no warn-and-continue path)."
    - "REQUIREMENTS.md / ROADMAP.md -> check-phase-54.mjs, the sole LIVE reader of both files (ledger row 23 precedent)."
  prohibitions:
    - statement: "No new documentation content is authored. This plan edits governance documents only; it authors zero prose in `docs/`."
      status: flagged-unverified
      verification: "`git diff --stat` for both commits lists zero paths under `docs/`."
    - statement: "Neither `docs/_glossary-macos.md` nor `docs/_glossary-android.md` has its `last_verified` or `review_by` metadata touched (v1.20-CARVE.md:178-182 zero-margin hazard — both sit at exactly 90 days against a >90 test)."
      status: flagged-unverified
      verification: "`git diff HEAD~2 -- docs/_glossary-macos.md docs/_glossary-android.md` is empty."
    - statement: "The CARVE amendment commit touches no path other than `.planning/milestones/v1.20-CARVE.md` — in-scope or out-of-scope (v1.20-CARVE.md:68-69 rule 1)."
      status: flagged-unverified
      verification: "`git show --name-only --format= HEAD` prints exactly one line."
    - statement: "No existing GOV-02 ledger row is edited or reordered; rows are appended at the end in commit order only."
      status: flagged-unverified
      verification: "`git diff HEAD~1 -- .planning/milestones/v1.20-GOV-02-LEDGER.md` shows additions only, zero deletions."
---

<objective>
Land D-31's first two commits — the seven-surface success-criterion amendment, then the CARVE
Category-10 allowlist amendment — so that every later plan in this phase has pre-authorized scope
and no plan is ever blocked mid-execution waiting on an amendment.

Purpose: `carve-gate.mjs` hard-blocks an off-list in-scope path with exit 1 and no
warn-and-continue path (v1.20-CARVE.md:79-91). The 63 corpus files Plans 09/03/04/05 repair are
outside Category 8's current roster, so the amendment authorizing them must land **before** the
first of those edits (v1.20-CARVE.md:72, rule 3). Separately, D-28 established that the roadmap's
"Discuss-phase flags: None dominant" and the 13-broken-links figure are both false and must be
amended before the phase's own verification can score against them.

Output: two commits (SC amendment, then CARVE amendment) plus one appended GOV-02 ledger row.
No code, no corpus content, no tracer — this plan is the governance gate D-31 requires in front of
every other plan. The phase tracer is Plan 02 Task 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md
@.planning/milestones/v1.20-CARVE.md
@.planning/milestones/v1.20-GOV-02-LEDGER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC-amendment commit — seven statements, annotate-and-supersede, alone and first</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — LINK-01..LINK-06 at `:52-57`, the sequencing note at `:59`, the LINK-05 census at `:61`
    - `.planning/ROADMAP.md:215-232` — Phase 143 Goal, SC#1-#5, Discuss-phase flags, Hard constraints
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md` — D-28, D-29, D-30, D-31
    - `.planning/ROADMAP.md:147-150` — Phase 141 SC#1/#2/#4, the live in-repo example of the `**[SUCCESS-CRITERION AMENDMENT, D-NN]**` and `**[DISCHARGED, D-NN]**` marker pair and of superseded-text preservation
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md:19-23` — the row schema and the row-23 precedent for batching `.planning/` document edits into one row
  </read_first>
  <action>
Amend all SEVEN D-29 surfaces in one commit. Use the ratified instrument (D-30): the in-line
marker `**[SUCCESS-CRITERION AMENDMENT, D-29]**` for a withdrawn/replaced mechanism and
`**[DISCHARGED, D-29]**` for a criterion met as written; annotate-and-supersede, never overwrite —
the original sentence stays byte-unchanged and the amendment is appended after it, exactly as
`.planning/ROADMAP.md:148` does for Phase 141 SC#2.

Surface 1 — `.planning/REQUIREMENTS.md` LINK-03 (`:54`). Current text is
"The 13 genuine broken links are fixed". Amend to name the two populations separately: the 13
genuine broken **file targets** and the **132** genuine broken **anchors**, 145 total.

Surface 2 — `.planning/REQUIREMENTS.md:59`, the "Sequencing is load-bearing" note. Its figures
(6311 links / 40 broken targets / 271 -> 70 anchors / 74%) are the **prototype** scope
(`docs/_templates/` INCLUDED, inline masking OFF). Tag them as such and append the LINK-02
deliverable scope: 274 files / 6252 links / 13 broken file targets / 268 -> 67 (Pandoc model) ->
**132** (GitHub model). Correct "74%" to **75%** (201 of 271 = 74.2%; the ratified figure is 75%
per D-29). State that templates add 58 links and 26 broken targets and that dropping inline
masking adds exactly 1 link and 1 target, so `13 + 26 + 1 = 40` and `6252 + 58 + 1 = 6311`
reconcile the two scopes with zero unexplained residue.

Surface 3 — `.planning/ROADMAP.md:222`, Phase 143 SC#1. Carry **both** scope-tagged pairs:
`271 -> 70` tagged "(templates included, inline masking off — the discarded prototype scope)" and
`268 -> 67 -> 132` tagged "(the LINK-02 scope: templates excluded, inline masking on; Pandoc model
then GitHub model)". State explicitly that under D-14's ordering **no state of the shipped checker
ever emits 271 or 70**, so a verifier re-deriving SC#1 from the deliverable would otherwise read
it as unmet. Also record that LINK-01 is now a net DELETION of the Pandoc `{#id}` override model
plus an ADDITION of `<a id>` recognition, not the addition-only change the original wording implies.

Surface 4 — `.planning/ROADMAP.md:223`, SC#2. Append that the corpus-wide scope is 274 files /
6252 relative links after excluding the 8 files under `docs/_templates/` (282 total `.md` files in
`docs/` at HEAD), and that "no accepted-violation baseline of any kind" is discharged by the
absence of any baseline/allowlist/ratchet artifact, not by a zero count alone.

Surface 5 — `.planning/ROADMAP.md:224`, SC#3. Amend "All 13 genuine broken links" to the two
populations: 13 file targets (11 `../` over-escapes in `docs/_glossary-macos.md`, 2 in
`docs/admin-setup-ios/`) **plus** 132 broken anchors across 77 distinct (target-file, fragment)
pairs. Record the remedy split D-38 (OWNER RULING) produces: converting all 87 `{#id}` overrides
across 29 files to `<a id>` anchors closes **65** of the 132 links / **26** of the 77 pairs
target-side in one mechanical edit, leaving **67** links / **51** pairs for D-04's per-class
routing. State that D-38 SUPERSEDES D-04's source-side routing for those 26 pairs — it does not
weaken D-04, it delivers the same links by the target-side branch D-04 already sanctions — and that
the conversion is measured to introduce **zero** regressions (no link anywhere in the corpus
currently resolves *because* a heading's slug is inflated by its own `{#…}` text).

Surface 6 — `.planning/ROADMAP.md:217`, the Phase 143 **Goal** line. Append that "zero
accepted-violation baseline" now includes the 65 links that were checker-green and GitHub-broken
under the Pandoc anchor model (D-01), which the original goal wording could not have named.

Surface 7 — `.planning/ROADMAP.md:230`, the **Discuss-phase flags** line. Its claim
("None dominant — LINK-01's precedence over LINK-02/LINK-04 and LINK-04's no-baseline rule are
already settled by the requirement text itself") is FALSE per D-28: both ARE settled, but no
requirement, success criterion or CARVE category addressed the 145 genuine breaks LINK-04 requires
be cleared with no baseline, of which 65 were invisible to the checker entirely. Amend and preserve
the superseded text.

Then append ONE row to `.planning/milestones/v1.20-GOV-02-LEDGER.md` covering both `.planning/`
documents, copying row 23's shape and batching precedent. File column names both paths and cites
"Plan 143-01 Task 1, D-29 seven-surface SC amendment". Grep column records the target-scoped
path-literal grep `REQUIREMENTS.md|ROADMAP.md` across `scripts/validation/*.mjs` filtered to
frozen/readAt call sites, exactly as row 23 does. Regression gate column records
`node scripts/validation/check-phase-54.mjs` — the sole LIVE reader of both files — run AFTER the
edit. Result column states the concrete `git diff --numstat` added/removed counts, never a bare
"PASS".

Commit message subject: `docs(143-01): D-29 seven-surface success-criterion amendment`. This commit
touches `.planning/` only and lands ALONE and FIRST — do not stage `v1.20-CARVE.md` with it (D-31
step 1; `carve-gate` cannot detect a fold because `.planning/` is outside `IN_SCOPE_PREFIXES` at
`carve-gate.mjs:36`, so this ordering is deliberate discipline, not gate-enforced).
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SUCCESS-CRITERION AMENDMENT, D-29' .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns a combined total of at least 7 across the two files.
    - `grep -n '132' .planning/REQUIREMENTS.md` matches the amended LINK-03 line.
    - `grep -n '271' .planning/ROADMAP.md` and `grep -n '132' .planning/ROADMAP.md` both match inside the Phase 143 SC#1 block, proving both scope-tagged pairs are present.
    - `grep -c '75%' .planning/REQUIREMENTS.md` is at least 1 and the superseded `74%` string is still present in the same paragraph (annotate-and-supersede, not overwrite).
    - `node scripts/validation/check-phase-54.mjs` exits 0.
    - `git show --name-only --format= HEAD` lists only paths beginning `.planning/` — zero paths under `docs/`, `scripts/`, or `.github/`.
    - `git show --name-only --format= HEAD | grep -c 'v1.20-CARVE.md'` returns 0.
    - `git diff HEAD~1 -- .planning/milestones/v1.20-GOV-02-LEDGER.md | grep -c '^-[^-]'` returns 0 (append-only, zero deleted lines).
  </acceptance_criteria>
  <done>Seven surfaces amended with the ratified marker instrument and superseded text preserved; one GOV-02 ledger row appended; committed alone, first, `.planning/`-only; check-phase-54 exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: CARVE Category-10 amendment commit — 63 corpus paths, touching only v1.20-CARVE.md</name>
  <files>.planning/milestones/v1.20-CARVE.md</files>
  <read_first>
    - `.planning/milestones/v1.20-CARVE.md:64-77` — the amendment procedure (rules 1, 2, 3)
    - `.planning/milestones/v1.20-CARVE.md:114-172` — the "Recorded scope amendments" section and its bullet grammar
    - `.planning/milestones/v1.20-CARVE.md:187-257` — the allowlist block, Category 3 (`:201-210`) and Category 8 (`:249-251`)
    - `scripts/validation/carve-gate.mjs:36-50` — `IN_SCOPE_PREFIXES`, `IN_SCOPE_EXACT`, `globToRegExp`
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md` — D-30, D-32, D-34
  </read_first>
  <action>
Append a Category 10 block inside the existing `carve-allowlist` fenced block in
`.planning/milestones/v1.20-CARVE.md`, after Category 9. Header comment line:
`# Category 10 — LINK-01..06's anchor-remedy corpus set (D-32/D-04, Phase 143)`.

Enumerate these 63 paths, one per line, verbatim. This roster was re-derived at plan time per
D-32's own instruction, by running the ratified model (GitHub anchor slugs, `<a id>` recognition,
`^ {0,3}` fence mask, inline-code masking, `docs/_templates/` excluded, every file scanned as both
source and target) over `docs/` at HEAD `599a996b`. It is the UNION of two sets:

- **Set 1 (49 paths, the pre-D-38 roster).** The 36 distinct source files and 29 distinct target
  files of the 145 genuine breaks, plus the four D-34(c) LINK-03 destination files, minus the 3
  paths already covered by Category 8.
- **Set 2 (29 paths, new under D-38).** Every file carrying a `{#id}` override, since Plan 09
  edits a heading line in each. Measured at HEAD: **87 `{#id}` occurrences across 29 files, all 87
  heading-trailing, zero mid-heading, zero outside a heading.**

**15 of Set 2 were already in Set 1**, so the union is **63** and the honest delta versus the
previously-planned roster is **+14**, not +29. The 14 newly-added paths are
`docs/l1-runbooks/11-macos-setup-assistant-failed.md`, `12-macos-profile-not-applied.md`,
`13-macos-app-not-installed.md`, `14-macos-compliance-access-blocked.md`,
`21-ios-compliance-blocked.md`, `25-android-compliance-blocked.md`,
`28-android-knox-enrollment-failed.md`, `29-android-aosp-enrollment-failed.md`;
`docs/l2-runbooks/21-android-compliance-investigation.md`, `22-android-knox-investigation.md`;
`docs/operations/app-lifecycle/04-android-mgp-lifecycle.md`,
`docs/operations/drift-migration/01-windows-drift-detection.md`,
`docs/operations/patch-management/00-overview.md`, `01-windows-wufb-rings.md`. Every one of them
is an override-carrying file with no other role in this phase.

The roster is deliberately a SUPERSET of the measured post-D-38 edit set: the 65 links the
conversion closes had source files (for example `docs/l2-runbooks/24-linux-log-collection.md`)
that no longer need a source-side rewrite, but they are retained because a hard exit 1 on an
under-listed path blocks execution mid-phase while an over-listed path costs nothing. State that
trade in the header comment rather than silently pruning.

docs/_glossary-android.md
docs/_glossary-apple-business.md
docs/_glossary.md
docs/admin-setup-android/04-byod-work-profile.md
docs/admin-setup-android/06-aosp-stub.md
docs/admin-setup-android/08-cope-full-admin.md
docs/admin-setup-apv2/02-etg-device-group.md
docs/admin-setup-linux/01-intune-linux-agent.md
docs/admin-setup-macos/10-kerberos-sso-extension.md
docs/android-lifecycle/02-provisioning-methods.md
docs/android-lifecycle/03-android-version-matrix.md
docs/cross-platform/apple-business/01-role-permission-model.md
docs/error-codes/00-index.md
docs/error-codes/01-mdm-enrollment.md
docs/error-codes/02-tpm-attestation.md
docs/error-codes/03-esp-enrollment.md
docs/error-codes/04-pre-provisioning.md
docs/error-codes/05-hybrid-join.md
docs/l1-runbooks/02-esp-stuck-or-failed.md
docs/l1-runbooks/05-oobe-failure.md
docs/l1-runbooks/11-macos-setup-assistant-failed.md
docs/l1-runbooks/12-macos-profile-not-applied.md
docs/l1-runbooks/13-macos-app-not-installed.md
docs/l1-runbooks/14-macos-compliance-access-blocked.md
docs/l1-runbooks/21-ios-compliance-blocked.md
docs/l1-runbooks/22-android-enrollment-blocked.md
docs/l1-runbooks/23-android-work-profile-not-created.md
docs/l1-runbooks/25-android-compliance-blocked.md
docs/l1-runbooks/27-android-zte-enrollment-failed.md
docs/l1-runbooks/28-android-knox-enrollment-failed.md
docs/l1-runbooks/29-android-aosp-enrollment-failed.md
docs/l1-runbooks/30-linux-enrollment-failed.md
docs/l1-runbooks/31-linux-compliance-non-compliant.md
docs/l1-runbooks/32-linux-ca-blocking-web-access.md
docs/l2-runbooks/00-index.md
docs/l2-runbooks/10-macos-log-collection.md
docs/l2-runbooks/19-android-enrollment-investigation.md
docs/l2-runbooks/20-android-app-install-investigation.md
docs/l2-runbooks/21-android-compliance-investigation.md
docs/l2-runbooks/22-android-knox-investigation.md
docs/l2-runbooks/23-android-aosp-investigation.md
docs/l2-runbooks/24-linux-log-collection.md
docs/l2-runbooks/25-linux-agent-investigation.md
docs/l2-runbooks/30-macos-mdm-migration-failure.md
docs/lifecycle-apv2/00-overview.md
docs/linux-lifecycle/01-linux-prerequisites.md
docs/macos-lifecycle/01-psso-provisioning-walkthrough.md
docs/macos-lifecycle/02-mdm-migration-psso.md
docs/operations/app-lifecycle/01-windows-win32-msix-scale.md
docs/operations/app-lifecycle/04-android-mgp-lifecycle.md
docs/operations/co-management/00-overview.md
docs/operations/co-management/02-windows-workload-sliders.md
docs/operations/co-management/03-cocmgmt-migration-paths.md
docs/operations/drift-migration/01-windows-drift-detection.md
docs/operations/patch-management/00-overview.md
docs/operations/patch-management/01-windows-wufb-rings.md
docs/operations/patch-management/02-macos-update-enforcement.md
docs/operations/patch-management/03-ios-update-lifecycle.md
docs/operations/patch-management/04-android-patch-delivery.md
docs/quick-ref-l2.md
docs/reference/linux-capability-matrix.md
docs/reference/macos-commands.md
docs/reference/registry-paths.md

Enumerate the paths literally; do NOT substitute directory globs such as `docs/l1-runbooks/**`.
An enumerated roster is what D-32 specifies ("~46-47 need listing") and it keeps the allowlist a
scope bound rather than a blanket corpus authorization.

Record the full delta chain honestly in the header comment; do not silently substitute a new
number for an old one. D-32 projected a 50-file union with 3 already on Category 8 and "~46-47"
needing listing. The first plan-time re-derivation measured a **52**-file union (49 needing
listing) because D-32's union omitted two of the four D-34(c) LINK-03 destination files. D-38's
all-87 conversion then adds the 29 override-carrying files, of which **15 were already in that
49**, giving a **66**-file union — **63** needing listing, **+14** versus the previous roster.
State all three figures (46-47 projected, 49 measured, 63 post-D-38) and their causes.

Assert existence before committing: every one of the 63 paths must resolve to a file on disk. The
plan-time check reports **0 missing**; re-run it rather than trusting this line, because
`carve-gate.mjs` hard-exits 1 on an off-list in-scope path with no warn-and-continue branch, so a
typo in this roster surfaces as a mid-phase execution block, not as a warning.

Then add bullets to the "Recorded scope amendments" section under a new sub-heading
`This phase's own amendments (143-CONTEXT.md's own numbering):`. Follow the exact bullet grammar of
`v1.20-CARVE.md:151-154`: a bold `**D-NN, short label.**` lead-in with at most one colon and the
colon kept OUTSIDE the bold span, then prose naming the defect and why the amendment is authorized.
Author one bullet each for: **D-01** (GitHub anchor model adopted; 65 checker-green/GitHub-broken
links become visible and in scope), **D-04** (per-class remedy routing; "add the missing section"
barred; no prose authored), **D-12** (corpus coverage requires deleting both `:259` and `:269`),
**D-23** (Phase 144 wires `check-phase-143.mjs`; no C18 harness fold; this phase authors no
validator), **D-29** (the seven-surface SC amendment landed in Task 1), **D-32** (this
Category 10 roster and its 46-47 / 49 / 63 re-derivation chain), and **D-38** (below).

The **D-38** bullet is the load-bearing one — it is the only place Phase 144's close-gate learns
that 87 heading lines across 29 files were rewritten. Write it as
`- **D-38, all-87 anchor conversion.**` followed by prose stating: the `{#…}` token renders as
visible literal junk text in the heading on github.com, confirmed first-party against GitHub's own
rendering API (`143-RESEARCH.md` Open Question 3), so it is a rendering defect in all 87 places and
not only in the 22 that happen to be link targets; converting all 87 to the corpus's own `<a id>`
convention ends the class rather than pruning it and leaves exactly one anchor convention
corpus-wide; the conversion closes 65 of the 132 broken anchors target-side with **zero measured
regressions**; and it is the reason Category 10 grew from 49 to 63. Name it as SUPERSEDING D-04's
source-side routing for those 26 pairs, and note that it authors no prose — it removes junk text
and adds an anchor.

Commit message subject: `docs(143-01): CARVE Category 10 — 63-path anchor-remedy corpus roster (D-32/D-38)`.
The commit body carries the one-line rationale required by rule 2. This commit touches **only**
`.planning/milestones/v1.20-CARVE.md` — no other path, in-scope or out-of-scope (rule 1). It lands
after Task 1's commit and before any edit it authorizes (rule 3, D-31 step 2).

Do not add `.gitattributes` or `package.json` to any category. Both are `IN_SCOPE_EXACT`
(`carve-gate.mjs:37`) with no matching allowlist category, so touching either hard-blocks the gate
(D-32); this phase touches neither.
  </action>
  <verify>
    <automated>node scripts/validation/carve-gate.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/carve-gate.mjs` exits 0 and its summary line reports `off-list=0`.
    - `git show --name-only --format= HEAD` prints exactly one line, `.planning/milestones/v1.20-CARVE.md`.
    - `sed -n '/^```carve-allowlist$/,/^```$/p' .planning/milestones/v1.20-CARVE.md | grep -v '^#' | grep -c '^docs/'` returns 65 (Category 8's `docs/_glossary-macos.md` and `docs/admin-setup-ios/**` plus Category 10's 63).
    - `sed -n '/Category 10/,/^```$/p' .planning/milestones/v1.20-CARVE.md | grep -v '^#' | grep -c '^docs/'` returns 63.
    - `sed -n '/Category 10/,/^```$/p' .planning/milestones/v1.20-CARVE.md | grep -v '^#' | grep '^docs/' | sort | uniq -d | wc -l` returns 0 — no duplicated path inflates the count.
    - `grep -c 'Category 10' .planning/milestones/v1.20-CARVE.md` is at least 1.
    - Every one of the 63 paths exists on disk: a loop over the Category 10 block asserting `test -f "$p"` for each reports zero missing.
    - All 29 `{#id}`-override files appear in the Category 10 block — verified by intersecting the block against `grep -rl '{#' docs/ --include=*.md` and asserting the difference is empty.
    - The "Recorded scope amendments" section gains 7 new bullets whose lead-ins match `**D-01,`, `**D-04,`, `**D-12,`, `**D-23,`, `**D-29,`, `**D-32,`, `**D-38,` — verified by `grep -c '^- \*\*D-\(01\|04\|12\|23\|29\|32\|38\), '` returning 7.
    - No bullet lead-in contains a colon inside its bold span: `grep -n '^- \*\*D-[0-9][0-9][^*]*:[^*]*\*\*' .planning/milestones/v1.20-CARVE.md` returns no new-bullet matches (the decision-bullet grammar convention).
    - `git log --format=%s -2` shows the SC-amendment subject BELOW the CARVE-amendment subject, proving the two-commit order.
  </acceptance_criteria>
  <done>Category 10 enumerates 63 verified-existing corpus paths covering all 29 override files; 7 amendment bullets including D-38 are recorded in the section Phase 144's close-gate reads; carve-gate exits 0 with off-list=0; the commit touches exactly one file and follows the SC-amendment commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan author -> frozen-surface allowlist | An amendment widens what any later commit in this milestone may edit without tripping the gate. |
| amendment commit -> Phase 144 close-gate | The "Recorded scope amendments" section is the only surface Phase 144's close-gate reads (Phase 142 D-33); an omission there reads as drift, not as authorized scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-01 | Elevation of Privilege | `v1.20-CARVE.md` allowlist | high | mitigate | Enumerate 63 literal paths instead of directory globs, so the amendment authorizes a bounded superset of the measured repair set and nothing else. Acceptance criteria assert the Category 10 block contains exactly 63 `docs/` lines, zero duplicates, and full coverage of the 29 `{#id}`-override files. |
| T-143-02 | Tampering | CARVE amendment commit isolation | high | mitigate | Rule 1 (single-file commit) asserted by `git show --name-only --format= HEAD` printing exactly one line; rule 3 (lands before the edit it authorizes) enforced by wave ordering — every corpus-editing plan is wave 3 or later. |
| T-143-03 | Repudiation | GOV-02 ledger | medium | mitigate | Append-only asserted by a zero-deleted-lines diff check; Result column must carry a concrete `git diff --numstat` figure, never a bare "PASS". |
| T-143-04 | Information Disclosure | amendment prose | low | accept | Both documents are already in-repo governance artifacts; no credential, tenant identifier or endpoint is introduced. |
| T-143-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs in this phase. `143-RESEARCH.md` Package Legitimacy Audit records "Not applicable — this phase installs no external packages in any ecosystem"; `check-nav-hub-links.mjs:17` pins the "Node built-ins ONLY" convention. No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `node scripts/validation/carve-gate.mjs` exits 0, `off-list=0`.
- `node scripts/validation/check-phase-54.mjs` exits 0.
- `node scripts/validation/c17-eee-contract.mjs` still reports `234 files checked, 0 with violations, 0 total violations` (this plan touches no `docs/` file, so the counts must be unchanged).
- `git log --format=%s -2` shows exactly two commits from this plan, in D-31 order.
</verification>

<success_criteria>
Seven amendment surfaces carry the ratified marker with superseded text preserved; Category 10
enumerates 63 verified-existing paths covering all 29 `{#id}`-override files; seven amendment
bullets including D-38 land in the section Phase 144's close-gate reads; two separate commits in
D-31 order; carve-gate and check-phase-54 both exit 0.
</success_criteria>

## Artifacts this phase produces

Created or modified by **this plan**:

| Artifact | Kind | Note |
|---|---|---|
| `.planning/REQUIREMENTS.md` LINK-03 amendment | governance text | 13 file targets + 132 anchors |
| `.planning/REQUIREMENTS.md:59` sequencing-note amendment | governance text | prototype vs LINK-02 scope, 74% -> 75% |
| `.planning/ROADMAP.md` Phase 143 SC#1/#2/#3 amendments | governance text | both scope-tagged pairs on SC#1 |
| `.planning/ROADMAP.md` Phase 143 Goal-line amendment | governance text | 65 invisible links named |
| `.planning/ROADMAP.md` Phase 143 Discuss-phase-flags amendment | governance text | "None dominant" withdrawn |
| `v1.20-CARVE.md` Category 10 block | allowlist entry | 63 enumerated corpus paths (49 + 14 new under D-38) |
| `v1.20-CARVE.md` "Recorded scope amendments" bullets | governance text | D-01, D-04, D-12, D-23, D-29, D-32, D-38 |
| `v1.20-GOV-02-LEDGER.md` row (`.planning/` docs) | ledger row | row-23 batching precedent |

<output>
Create `.planning/phases/143-link-coverage-fence-mask-unification/143-01-SUMMARY.md` when done.
</output>
