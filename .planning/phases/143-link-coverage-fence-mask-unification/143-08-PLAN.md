---
phase: 143-link-coverage-fence-mask-unification
plan: 08
type: execute
wave: 8
depends_on: ["143-06", "143-07"]
files_modified:
  - .planning/milestones/v1.20-DEFERRED-CLEANUP.md
  - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
  - .planning/phases/143-link-coverage-fence-mask-unification/143-NEEDLE-SPEC.md
autonomous: true
requirements: [LINK-02, LINK-04, LINK-05, LINK-06]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Phase 143 authors NO `check-phase-143.mjs` and NO C18 harness fold; it hands off a needle-spec for Phase 144 to implement (D-23, OWNER-RATIFIED)."
    - "The needle-spec names the exact literals Phase 144 may pin, the spawn idiom to copy (`check-phase-119.mjs:148`'s `execFileSync('node', [TOOL], { stdio: 'pipe', timeout: 300000, cwd: process.cwd() })`), and the measured runtime range that bounds the timeout."
    - "The needle-spec states that the checker must never self-register into a chain array, mirroring the required-ABSENT constraint `check-phase-115.mjs:102` places on the c17 sibling — the same class of constraint D-23 names as binding here."
    - "A row in `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` routes the hand-off through the established instrument rather than through prose (D-26), and states the late-discovery mitigation: at a sub-second runtime the checker can ride HARN-19's 3-axis terminal re-audit as a spot-check REGARDLESS of whether the `check-phase-143.mjs` wiring lands, decoupling the milestone close from the wiring's success."
    - "The `FENCE-AXIS-02` carry-forward is registered with a named token covering both deferred axes: fence LENGTH (`convert.ps1:110` closes on a 3-character prefix while the 14 JS sites require at-least-as-long) and mask SCOPE (two retrofits mask the whole file including front matter while c17 and the six first-H1 scanners run over post-front-matter body lines)."
    - "The out-of-census divergent fence sites are registered under the same or an adjacent token: `check-phase-66.mjs:274`, the 12 unanchored strip sites across `check-phase-54` through `-59`, `carve-gate.mjs:65`, and the column-0-only detectors at `check-phase-135:128`, `-136:216`, `-55:345`, `-122:44` (D-20)."
    - "The enforcement gap is named, not implied: trigger-blindness survives D-23. 10 of 16 workflows carry zero `docs/` path filters, so a `docs/`-only pull request — every link-breaking change — matches no path filter and fires nothing. Real cadence is weekly cron plus manual dispatch. Nobody may read 'enforced' as 'per-change'."
    - "A Stop-hook is rejected as the remedy: `v1.20-CARVE.md:83-85` rules a Stop-hook advisory and not the enforcement mechanism itself, and three hooks already exist so a new one would be the fourth."
    - "The claim 'inert for 20 phases' is FALSE and is not repeated: the checker has been run as a plan-level automated gate and verifier spot-check in Phases 123, 130, 135 and 137, and `137-VERIFICATION.md:27` scores SC5 on it. The real gap is CI wiring only."
    - "The phase's own goal-backward evidence for 'enforced' is a live manual run, which is an accepted evidence standard on this project — `142-VERIFICATION.md` discharges 'check-phase-30 exits 0 standalone' by exactly a live node run."
    - "LINK-06 concurrency: the before/after c17 comparison is valid only when both runs are on the same worktree at the same SHA modulo the edit under test, on a quiesced machine; the recorded evidence declares all three."
    - statement: "Phase 144 actually carries the spawn forward, so enforcement does not evaporate exactly as it did for this checker between Phases 123 and 143."
      verification: backstop
  artifacts:
    - .planning/phases/143-link-coverage-fence-mask-unification/143-NEEDLE-SPEC.md
    - .planning/milestones/v1.20-DEFERRED-CLEANUP.md
    - .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
  key_links:
    - "`143-NEEDLE-SPEC.md` -> Phase 144's `check-phase-143.mjs` -> the apex chain. This is the only artifact carrying the enforcement contract forward."
    - "`v1.20-DEFERRED-CLEANUP.md` row -> HARN-19's 3-axis terminal re-audit spot-check -> the milestone bar, on a path independent of the wiring."
  prohibitions:
    - statement: "No `check-phase-143.mjs` is authored in this phase; validators are the close phase's indivisible atom (D-23, OWNER-RATIFIED)."
      status: flagged-unverified
      verification: "`test ! -f scripts/validation/check-phase-143.mjs`."
    - statement: "No C18 harness fold is performed and no `v1.20-milestone-audit.mjs` is created or edited — Phase 144 SC#2 pins C1-C17 inherited, and this project reads that phrasing as authorization-limiting."
      status: flagged-unverified
      verification: "`git diff --name-only` for this plan lists zero paths under `scripts/` or `.github/`."
    - statement: "No fourth Stop-hook is added."
      status: flagged-unverified
      verification: "`ls .claude/hooks/` returns the same three entries as before this plan."
    - statement: "No new documentation content is authored in `docs/`."
      status: flagged-unverified
      verification: "`git diff --name-only` for this plan contains zero paths under `docs/`."
    - statement: "The struck premises are not restated anywhere in this phase's artifacts — not 'inert for 20 phases', not 'all 16 CI workflows run it', not 'its only reference anywhere is check-phase-123's presence check', not 'no category covers any of them'."
      status: flagged-unverified
      verification: "A grep of all `143-*` artifacts for each struck phrase returns 0, or returns only occurrences explicitly labelled as struck."
---

<objective>
Hand Phase 143's deliverable forward without authoring the validator that enforces it: a needle-spec
for `check-phase-143.mjs`, a routed deferred-cleanup row, two named carry-forward tokens, and a
plain statement of the enforcement gap that survives the hand-off.

Purpose: D-23 (OWNER-RATIFIED) makes Phase 144 the wiring phase — `check-phase-139..NN.mjs` is
booked verbatim by HARN-18, needs no scope amendment, runs inside the apex chain, and adds no second
permanent live-HEAD leg to the harness lineage. But the C17 precedent held only because Phase 115
pre-booked the fold and `check-phase-115.mjs:98-105` actively failed if the contract self-registered.
This plan replicates that booking discipline in the only instrument available to a non-terminal
phase: a written spec plus a routed cleanup row. D-26 adds the late-discovery mitigation Phase 144
needs, since it is terminal and blocked on 139 through 143 — if the wiring turns the checker red
there, no phase remains to absorb it and the milestone bar is what would give.

Output: three `.planning/` artifacts, zero code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md
@.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md
@.planning/milestones/v1.20-DEFERRED-CLEANUP.md
@scripts/validation/check-nav-hub-links.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the `check-phase-143.mjs` needle-spec for Phase 144</name>
  <files>.planning/phases/143-link-coverage-fence-mask-unification/143-NEEDLE-SPEC.md</files>
  <read_first>
    - `scripts/validation/check-nav-hub-links.mjs` — the post-flip file: its summary-line literal, its `--self-test` flag, its exit-code tail, and the absence of any chain-registration identifier
    - `scripts/validation/check-phase-119.mjs:140-160` — the validator-layer spawn idiom to copy
    - `scripts/validation/check-phase-115.mjs:60-115` — the three-pin precedent on the c17 sibling, including the required-ABSENT pin
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the LINK-04 no-baseline discharge and the committed-state runtime range
    - `.planning/ROADMAP.md:234-250` — Phase 144's goal, SC#2 ("C1-C17 inherited") and its Discuss-phase flags line
  </read_first>
  <action>
Create `.planning/phases/143-link-coverage-fence-mask-unification/143-NEEDLE-SPEC.md`. This is the
artifact D-23 hands off. Phase 143 authors no validator; this file tells Phase 144 exactly what to
build so it does not have to re-derive the contract from source.

Include, each with the file:line it was read from:

**1. The tool and its invocation.** `scripts/validation/check-nav-hub-links.mjs`, invoked with no
arguments for the corpus scan and with `--self-test` for the pure-function assertions. Exit 0 iff
zero failures (normal mode) or zero failed assertions (self-test mode); exit 1 otherwise; no third
exit code exists in the file.

**2. The spawn idiom to copy**, quoted from `check-phase-119.mjs:148` — an `execFileSync` of `node`
against the tool path with piped stdio, an explicit timeout, and the process working directory. Give
the measured committed-state runtime range from `143-EVIDENCE.md` (a range across at least three
runs with tree identity, cache state and node version declared) and state that any chosen timeout
must exceed it by a wide margin rather than tracking it.

**3. The literals Phase 144 may pin**, listed exactly, each marked required-PRESENT or
required-ABSENT:
- required-PRESENT: the summary-line prefix `check-nav-hub-links summary: ` — the greppable anchor,
  byte-unchanged across this phase by design.
- required-PRESENT: the flag string `--self-test`, mirroring `check-phase-115.mjs:75`'s bare
  substring pin on the c17 sibling.
- required-PRESENT: the self-test tail `Self-test: ` — the counted-assertions line.
- required-ABSENT: any chain-registration array identifier. The checker must never self-register,
  exactly as `check-phase-115.mjs:102` requires of `c17-eee-contract.mjs`. D-23 names this as the
  same class of constraint binding here.
- required-ABSENT: any baseline, allowlist, ratchet or expected-failure identifier — LINK-04 forbids
  an accepted-violation baseline of any kind, and pinning its absence is what keeps the guarantee
  from silently eroding.

Explicitly warn against pinning the two bucket LABELS inside the summary line. Their wording changed
this phase (they previously implied a four-hub scan) and could reasonably change again; the stable
contract is the prefix, the labelled-counts-plus-total shape, and the exit code.

**4. What the validator should assert**, as a short numbered list: the tool exists at its path; the
corpus run exits 0; the self-test run exits 0; the summary line is present and its total parses to
0; and each required-ABSENT literal is absent from the source.

**5. The rename bar.** `check-phase-123.mjs:40,83` pins this file's PATH STRING via a presence
check. The file must not be renamed by Phase 144 or any later phase; an in-place edit is safe, a
rename breaks a frozen call site.

**6. What is explicitly NOT handed off.** No C18 harness fold. Phase 144 SC#2 pins
`v1.20-milestone-audit.mjs` as Path-A with C1-C17 inherited, and Phase 144's Discuss-phase flags
read "None (closing cluster; consumes prior decisions)" — this project reads that phrasing as
authorization-limiting in its own words, with `93-CONTEXT.md:51` and `95-CONTEXT.md:64` as the
precedent. C17 itself landed only because v1.15 pre-booked it under HARN-01 and D-119, with
`check-phase-115.mjs:98-105` actively failing if the contract self-registered. Phase 143 has no
equivalent booking, so the fold is withdrawn as unauthorized.
  </action>
  <verify>
    <automated>test -f .planning/phases/143-link-coverage-fence-mask-unification/143-NEEDLE-SPEC.md; echo "spec=$?"; test ! -f scripts/validation/check-phase-143.mjs; echo "no_validator=$?"; node scripts/validation/check-nav-hub-links.mjs; echo "cnhl=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `143-NEEDLE-SPEC.md` exists and contains all six numbered sections.
    - Section 3 lists at least 3 required-PRESENT literals and at least 2 required-ABSENT literals, each quoted exactly.
    - Section 3 contains the explicit warning against pinning the two bucket labels.
    - Section 2 quotes the `check-phase-119.mjs:148` spawn idiom and cites a runtime RANGE across n>=3 runs, never a single value.
    - Every claim in the file carries a `file:line` citation.
    - `test ! -f scripts/validation/check-phase-143.mjs` succeeds.
    - `git diff --name-only` for this task lists zero paths under `scripts/`, `.github/` or `docs/`.
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 — the contract the spec describes is live and green.
  </acceptance_criteria>
  <done>The needle-spec exists with six sections, cited literals split into required-PRESENT and required-ABSENT, a spawn idiom, a runtime range, the rename bar, and the withdrawn C18 fold — and no validator was authored.</done>
</task>

<task type="auto">
  <name>Task 2: Route the hand-off and register the carry-forward tokens</name>
  <files>.planning/milestones/v1.20-DEFERRED-CLEANUP.md, .planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md</files>
  <read_first>
    - `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` — the whole file, for its existing row shape and heading structure
    - `.planning/milestones/v1.20-CARVE.md:83-85` — the ruling that a Stop-hook is advisory, not the enforcement mechanism
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-EVIDENCE.md` — the out-of-census divergent-fence register written by Plan 07 Task 3
    - `.planning/phases/143-link-coverage-fence-mask-unification/143-CONTEXT.md` — D-18, D-20, D-24, D-25, D-26, D-27
  </read_first>
  <action>
**Part 1 — the D-26 hand-off row.** Append a row to `.planning/milestones/v1.20-DEFERRED-CLEANUP.md`
following its existing row shape. The row routes the Phase 144 wiring through the established
instrument rather than through prose, and states the late-discovery mitigation: Phase 144 is terminal
and blocked on Phases 139 through 143, so if the wiring turns the checker red there, no phase remains
to absorb it and the milestone bar — both accepted-red dispositions deleted — is what would give. At
its measured sub-second runtime the checker can ride HARN-19's 3-axis terminal re-audit as a
spot-check REGARDLESS of whether the `check-phase-143.mjs` wiring lands, which decouples the close
from the wiring's success. Point the row at `143-NEEDLE-SPEC.md`.

**Part 2 — register `FENCE-AXIS-02`.** Add a row for the two deferred unification axes D-18 names,
under that exact token, because every other carry-forward in this repo has one (`SWEEP-09`,
`DEFER-121-07-B`, `CARVE-1`):
- **Fence length.** `convert.ps1:110` closes a fence via a trimmed-start prefix test against a
  variable holding only the 3-character opener prefix, so a 4-backtick opener is closed by a
  3-backtick line; the 14 JS sites track a fence length and require at-least-as-long. LINK-05's
  subject was indentation only.
- **Mask scope.** `retrofit-mermaid-structural.mjs:422` and `retrofit-nav-hub.mjs:406` mask the whole
  file including front matter, while `c17-eee-contract.mjs:150` and the six first-H1 scanners run
  over post-front-matter body lines.

**Part 3 — register the out-of-census divergent fence sites (D-20)** under the same token or an
adjacent one, carrying forward Plan 07 Task 3's register: `check-phase-66.mjs:274` (a live fence
tracker in a frozen chain validator that runs in every apex, using a whitespace-class anchor and a
bare toggle with no fence-character or fence-length tracking), the 12 unanchored strip sites across
`check-phase-54` through `-59`, `carve-gate.mjs:65` (allowlist parser tolerating an indented opener
but requiring a column-0 closer, with a hard throw), and the column-0-only detectors at
`check-phase-135:128`, `-136:216`, `-55:345` and `-122:44`. Also carry the deferred detection-regex
widening (D-21): the mermaid detectors stay column-0, with zero live instances today.

**Part 4 — name the enforcement gap plainly** in `143-EVIDENCE.md` under a
`## Enforcement: what this phase does and does not deliver` heading. State, with citations:
- Trigger-blindness survives D-23. `audit-harness-v1.19-integrity.yml:22-33` — the template the 17th
  workflow copies — filters pull requests on validator and workflow paths and on
  `.planning/REQUIREMENTS.md`; **10 of 16 workflows carry zero `docs/` path filters**. A
  `docs/`-only pull request, which is every link-breaking change, matches no filter and fires
  nothing. Real cadence is weekly cron plus manual dispatch. Nobody may read "enforced" as
  "per-change".
- A Stop-hook is rejected as the remedy: `v1.20-CARVE.md:83-85` rules a Stop-hook advisory and not
  the enforcement mechanism itself, and three hooks already exist so a new one would be the fourth.
- Correct the record on two struck premises rather than repeating them. "Inert for 20 phases" is
  FALSE: the checker has been run as a plan-level automated gate and verifier spot-check in Phases
  123, 130, 135 and 137, and `137-VERIFICATION.md:27` scores SC5 on it with `:70` listing it as a
  PASS gate. "All 16 CI workflows run c17" is FALSE: only 5 of 16 contain `c17-eee-contract`
  (v1.15 through v1.19), so a harness fold would have bought one workflow, not sixteen. The true
  claim about the checker's reach is zero EXECUTING references outside `.planning/`.
- Record the goal-backward position (D-27): ROADMAP Phase 143's goal says the corpus has "durable,
  **enforced**" coverage, and the verifier scores Goal Achievement and Observable Truths against the
  goal, not only the success-criteria list. This phase's evidence for "enforced" is a live manual
  run plus a written, routed hand-off — and a live manual run IS an accepted evidence standard here,
  since `142-VERIFICATION.md` discharges "check-phase-30 exits 0 standalone" by exactly that. Also
  separate the two requirements: "exits 0" is LINK-04's wording only; LINK-02 carries no exit-0
  clause and is the requirement most exposed by deferral.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; echo "cnhl=$?"; node scripts/validation/c17-eee-contract.mjs | tail -2; node scripts/validation/carve-gate.mjs | tail -1; ls .claude/hooks/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.20-DEFERRED-CLEANUP.md` gains a row pointing at `143-NEEDLE-SPEC.md` and stating the decoupling mitigation.
    - `grep -c 'FENCE-AXIS-02' .planning/milestones/v1.20-DEFERRED-CLEANUP.md` returns at least 1.
    - The `FENCE-AXIS-02` entry names both axes — fence length and mask scope — with their file:line citations.
    - The out-of-census register names all four classes with their file:line citations.
    - `143-EVIDENCE.md` contains the `## Enforcement: what this phase does and does not deliver` section with the 10-of-16 figure, the Stop-hook rejection, both struck-premise corrections, and the D-27 goal-backward position.
    - `grep -c 'inert for 20 phases' .planning/phases/143-link-coverage-fence-mask-unification/*.md` returns 0 except where explicitly labelled as struck.
    - `ls .claude/hooks/ | wc -l` returns the same count as before this plan — no fourth hook.
    - `git diff --name-only` for this plan lists zero paths under `scripts/`, `.github/` or `docs/`.
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0; `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations, 0 total violations`; `node scripts/validation/carve-gate.mjs` exits 0 with `off-list=0`.
  </acceptance_criteria>
  <done>The Phase 144 hand-off is routed through the deferred-cleanup instrument with a decoupling mitigation; `FENCE-AXIS-02` and the out-of-census fence register are named carry-forwards; the enforcement gap and both struck premises are recorded plainly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 143 deliverable -> Phase 144 wiring | The only channel is written: a needle-spec and a routed cleanup row. Anything not written down is lost at the phase boundary, exactly as enforcement was lost for this checker between Phases 123 and 143. |
| "enforced" in the phase goal -> what CI actually fires on | 10 of 16 workflows carry no `docs/` path filter, so the word "enforced" over-promises unless the gap is stated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-33 | Repudiation | silent loss of the enforcement contract at the phase boundary | high | mitigate | The contract is written to `143-NEEDLE-SPEC.md` with per-claim file:line citations and routed through `v1.20-DEFERRED-CLEANUP.md`, the established instrument, rather than through SUMMARY prose. |
| T-143-34 | Spoofing | "enforced" read as "per-change" | high | mitigate | The trigger-blindness gap is stated explicitly with the 10-of-16 figure and the real weekly-cron-plus-dispatch cadence, in the evidence artifact the verifier reads. |
| T-143-35 | Elevation of Privilege | unauthorized C18 harness fold | high | mitigate | Section 6 of the needle-spec records the fold as withdrawn-as-unauthorized with its three-citation basis; a prohibition asserts zero paths under `scripts/` or `.github/` are touched by this plan. |
| T-143-36 | Denial of Service | terminal-phase late discovery | high | mitigate | D-26's decoupling: the checker rides HARN-19's terminal re-audit as a spot-check regardless of whether the wiring lands, so the milestone close does not depend on the wiring's success. |
| T-143-37 | Tampering | struck premises re-entering the record | medium | mitigate | Both struck premises are corrected in writing with their true measured figures, and a grep asserts they do not reappear unlabelled anywhere in this phase's artifacts. |
| T-143-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs; this plan writes Markdown under `.planning/` only. `143-RESEARCH.md` Package Legitimacy Audit: "Not applicable". |
</threat_model>

<verification>
- `test -f .planning/phases/143-link-coverage-fence-mask-unification/143-NEEDLE-SPEC.md` succeeds.
- `test ! -f scripts/validation/check-phase-143.mjs` succeeds.
- `node scripts/validation/check-nav-hub-links.mjs` -> exit 0, total 0.
- `node scripts/validation/check-nav-hub-links.mjs --self-test` -> `Self-test: 10 passed, 0 failed`.
- `node scripts/validation/c17-eee-contract.mjs` -> `234 files checked, 0 with violations, 0 total violations`.
- `node scripts/validation/carve-gate.mjs` -> exit 0, `off-list=0`.
- `ls .claude/hooks/` -> unchanged count.
- `git diff --name-only` across this plan -> `.planning/` paths only.
</verification>

<success_criteria>
The needle-spec exists with six cited sections and a required-PRESENT / required-ABSENT literal
split; the D-26 hand-off row routes through the deferred-cleanup instrument with the decoupling
mitigation; `FENCE-AXIS-02` and the out-of-census fence register are named carry-forwards; the
enforcement gap, the Stop-hook rejection and both struck premises are recorded; no validator, no
harness fold, no fourth hook, no code.
</success_criteria>

## Artifacts this phase produces

Created or modified by **this plan**:

| Artifact | Kind | Note |
|---|---|---|
| `143-NEEDLE-SPEC.md` | new hand-off artifact | 6 cited sections; the only channel to Phase 144 |
| `v1.20-DEFERRED-CLEANUP.md` hand-off row | ledger row | D-26 decoupling mitigation |
| `FENCE-AXIS-02` carry-forward token | ledger row | fence length + mask scope axes |
| Out-of-census divergent-fence register | ledger row | 4 classes, D-20 |
| Enforcement-gap section in `143-EVIDENCE.md` | evidence section | 10-of-16, Stop-hook rejection, 2 struck premises, D-27 position |

<output>
Create `.planning/phases/143-link-coverage-fence-mask-unification/143-08-SUMMARY.md` when done.
</output>
