---
phase: 121-structural-retrofit-glossaries-lifecycle-end-user-guides
plan: 03
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - docs/_glossary-macos.md
  - docs/_glossary-apple-business.md
autonomous: true
requirements: [RETRO-04]
must_haves:
  truths:
    - "docs/_glossary-macos.md and docs/_glossary-apple-business.md each carry the EEE header block (before H1), a net-new ≥30-word scope ## Summary as the first H2, and doc_id/status: Approved/owner/doc_type: Reference frontmatter"
    - "The coverage blockquote is relocated byte-preserved below ## Summary and every blockquote (relocated coverage + all in-body See-also cross-refs) is ≤200 chars — C17 #12 green"
    - "The canonical ### Kandji-Iru term heading (and every other term slug) in _glossary-macos.md is unchanged — plain-GitHub slug, no {#id} override; ## Alphabetical Index links resolve; C17 exits 0 on the full corpus"
  artifacts:
    - path: "docs/_glossary-macos.md"
      provides: "EEE-retrofitted Apple provisioning glossary (Reference, Approved)"
      contains: "## Summary"
    - path: "docs/_glossary-apple-business.md"
      provides: "EEE-retrofitted Apple Business Manager glossary (Reference, Approved)"
      contains: "## Summary"
  key_links:
    - from: "docs/_glossary-macos.md ## Alphabetical Index links"
      to: "term ### anchors incl. #kandji-iru"
      via: "plain-GitHub slug (no {#id} override)"
      pattern: "Kandji-Iru"
---

<objective>
EEE-retrofit docs/_glossary-macos.md (~28 over-200 blockquotes — carries the canonical ### Kandji-Iru
double-hyphen slug hazard) and docs/_glossary-apple-business.md (~6 over-200 blockquotes) to doc_type
Reference, C17-green, term-surface preserved.

Purpose: RETRO-04 (glossaries) with the D-03 strict D3-A layout and R2 word-preserving blockquote splits.
Output: 2 C17-green Reference glossaries; RE-182 (macos), RE-180 (apple-business) injected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the fork and author both net-new Summaries (Reference doc_type)</name>
  <files>docs/_glossary-macos.md, docs/_glossary-apple-business.md</files>
  <read_first>
    - docs/_glossary-macos.md (frontmatter platform: all; the canonical ### Kandji-Iru heading — MUST stay bare, plain-GitHub slug #kandji-iru, no {#id}; coverage blockquote before H1; ## Version History at ~L232)
    - docs/_glossary-apple-business.md (frontmatter platform: ios+macos → "iOS + macOS"; coverage blockquote; ## Version History at ~L196)
    - docs/reference/00-index.md (RE-142 — the net-new-Summary-above-relocated-coverage layout to copy)
    - scripts/pipeline/retrofit-structural.mjs (Reference route for _glossary*.md; [FILL-IN] placeholder; whole-pre-H1-span relocation; VH prepend/create)
    - docs/_standards/EEE-SOP-standard.md (Structure invariant: Summary = first H2, ≥30 words; D-07 glossary→Reference)
  </read_first>
  <action>
    Run node scripts/pipeline/retrofit-structural.mjs docs/_glossary-macos.md docs/_glossary-apple-business.md
    (dry-run first to confirm doc_id resolves to RE-182 / RE-180 and platform maps: all → "All Platforms",
    ios+macos → "iOS + macOS", then write). The fork injects doc_id/status: Approved/owner: Intune Admin
    Lead/doc_type: Reference, emits the block line before H1, relocates the coverage blockquote below a new
    ## Summary, and inserts the [FILL-IN] placeholder + v1.16 VH row. Replace each [FILL-IN] with a NET-NEW
    ≥30-word scope Summary (author's discretion per D-03): for macos, a scope statement covering Apple
    provisioning terminology (ADE/ABM, Platform SSO, Kandji/Iru migration, MDM concepts); for
    apple-business, a scope statement covering Apple Business Manager terminology (tokens, MDM server
    assignment, VPP/apps, device assignment). Summary is NET-NEW scope prose, NOT the coverage see-also
    links (D-03). Leave every term ### heading — especially ### Kandji-Iru — and the ## Alphabetical Index
    byte-unchanged (no {#id} overrides; the double-hyphen bare heading is the canonical slug).
  </action>
  <verify>
    <automated>for f in docs/_glossary-macos.md docs/_glossary-apple-business.md; do grep -q '\[FILL-IN' "$f" && echo "PLACEHOLDER-REMAINS:$f"; done; grep -c '^### Kandji-Iru' docs/_glossary-macos.md</automated>
    <automated>grep -cE '^doc_id: RE-(182|180)' docs/_glossary-macos.md docs/_glossary-apple-business.md</automated>
  </verify>
  <acceptance_criteria>
    - Neither file contains "[FILL-IN"; each has a net-new ## Summary as first H2 with ≥30 words of scope prose
    - Frontmatter carries doc_id (RE-182 macos / RE-180 apple-business), status: Approved, owner: Intune Admin Lead, doc_type: Reference; block line reads "**Doc Type:** Reference"
    - docs/_glossary-macos.md still contains exactly one bare "### Kandji-Iru" heading (no {#id} override); all other term ### headings and both ## Alphabetical Index blocks are byte-unchanged
  </acceptance_criteria>
  <done>Both glossaries enrolled at Reference with net-new ≥30-word Summaries; Kandji-Iru slug preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Word-preserving-split every over-200-char blockquote and prove C17 green + reformat-only</name>
  <files>docs/_glossary-macos.md, docs/_glossary-apple-business.md</files>
  <read_first>
    - docs/_glossary-macos.md, docs/_glossary-apple-business.md (after Task 1 — the relocated coverage blockquote plus every "> See also: ..." blockquote exceeding 200 chars)
    - scripts/validation/c17-eee-contract.mjs (assertion #12 ~L386 — JOINS consecutive > lines; split into SEPARATE blockquotes divided by a blank line)
    - .planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-CONTEXT.md (R2 word-preserving splits; reformat-only discipline)
  </read_first>
  <action>
    Split every blockquote whose joined text exceeds 200 chars into multiple ≤200-char blockquotes,
    preserving every word (Transform A), inserting a blank line between resulting blockquotes so C17 #12
    measures them independently. See-also blockquotes split at " · " separators; the coverage blockquote at
    sentence/clause boundaries. A scripted greedy splitter is acceptable (Phase-117-05 precedent) with a
    word-set reformat-only proof. Reserve de-blockquote (Transform B) only for an unsplittable single
    Markdown-link span. Prove reformat-only per file: multiset of words BEFORE vs AFTER (strip the leading
    ">" prefix token before comparing — Phase-118 lesson) shows zero real words added/removed apart from the
    net-new Summary + fork block line/VH row. Run the full C17 corpus and confirm 0 violations.
  </action>
  <verify>
    <automated>awk '/^>/{if(length($0)>200) c++} END{exit (c>0)}' docs/_glossary-macos.md && awk '/^>/{if(length($0)>200) c++} END{exit (c>0)}' docs/_glossary-apple-business.md && echo NO_LONG_BQ</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -E "checked, 0 with violations, 0 total violations"</automated>
  </verify>
  <acceptance_criteria>
    - No ">"-prefixed line exceeds 200 chars in either file; every blockquote group joined text ≤200 chars (C17 #12 green)
    - node scripts/validation/c17-eee-contract.mjs reports "0 with violations, 0 total violations" and exits 0 (full corpus)
    - Word-set reformat-only proof passes (zero real words added/removed apart from Summary + fork emissions)
    - All term anchor slugs unchanged (incl. #kandji-iru); ## Alphabetical Index links resolve
  </acceptance_criteria>
  <done>Both glossaries C17-green, all blockquotes ≤200 chars, proven reformat-only, slugs intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Reformat-only Markdown edits to two glossary files plus one pipeline invocation. No runtime, network,
auth, session, user-input, or cryptographic surface. The only integrity concern is silent content loss
during blockquote splitting or coverage relocation, and breaking the canonical Kandji-Iru slug.

| Boundary | Description |
|----------|-------------|
| author/fork edit → glossary corpus | Reformat must be byte-faithful except the net-new Summary; anchor slugs must not change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-03-01 | Tampering (silent content loss) | blockquote splitting / coverage relocation | mitigate | Word-set multiset reformat-only proof; C17 full-corpus 0 violations |
| T-121-03-02 | Tampering | ### Kandji-Iru slug (double-hyphen trap) | mitigate | Verify exactly one bare "### Kandji-Iru" heading remains, no {#id} override introduced |
| T-121-03-SC | Tampering | package installs | n/a | No installs; fork uses Node built-ins only |

Spoofing / Repudiation / Information-Disclosure / Denial-of-Service / Elevation-of-Privilege: no new
surface — documentation reformat with no runtime execution path.
</threat_model>

<verification>
Full C17 corpus reports 0 with violations, 0 total violations, exit 0; no ">"-prefixed line exceeds 200
chars; exactly one bare ### Kandji-Iru heading; word-set reformat-only proof passes.
</verification>

<success_criteria>
- ROADMAP Phase 121 SC1 (partial): both files carry the EEE block + Summary-first + normalized Platform
  label; term-surface + plain-GitHub anchor slugs preserved; C17 exits 0
- ROADMAP Phase 121 SC4 (partial): both carry the "v1.16 EEE reformat — content not re-reviewed" VH row
</success_criteria>

<output>
Create `.planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-03-SUMMARY.md` when done
</output>
