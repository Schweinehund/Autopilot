---
phase: 121-structural-retrofit-glossaries-lifecycle-end-user-guides
plan: 06
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - docs/lifecycle/01-hardware-hash.md
  - docs/lifecycle/02-profile-assignment.md
  - docs/lifecycle/05-post-enrollment.md
  - docs/lifecycle-apv2/00-overview.md
  - docs/lifecycle-apv2/01-prerequisites.md
  - docs/lifecycle-apv2/03-automatic-mode.md
  - docs/end-user-guides/android-work-profile-setup.md
  - docs/end-user-guides/linux-intune-portal-enrollment.md
autonomous: true
requirements: [RETRO-07, RETRO-09]
must_haves:
  truths:
    - "The 6 remaining Mermaid-free lifecycle files (lifecycle/{01,02,05}, lifecycle-apv2/{00,01,03}) carry the EEE header block, a net-new ≥30-word ## Summary as first H2, doc_id/status: Approved/owner/doc_type: Guide; the 6 keyless files get platform: Windows injected → normalized 'Windows' label"
    - "The 2 end-user guides (RE-175 android-work-profile-setup, RE-176 linux-intune-portal-enrollment) receive an actual EEE retrofit — doc_id/status: Approved/owner/doc_type: Guide + net-new Summary — flipping them from registry-enrolled-but-unretrofitted to Approved (D-07)"
    - "Every blockquote across the 8 files is ≤200 chars; none contains ```mermaid; C17 exits 0 on the full corpus"
  artifacts:
    - path: "docs/lifecycle/01-hardware-hash.md"
      provides: "EEE-retrofitted Stage-1 lifecycle guide (Guide, Approved, platform: Windows injected)"
      contains: "## Summary"
    - path: "docs/end-user-guides/android-work-profile-setup.md"
      provides: "EEE-retrofitted end-user BYOD guide (Guide, Approved, RE-175)"
      contains: "doc_id: RE-175"
  key_links:
    - from: "docs/end-user-guides/*.md frontmatter"
      to: "RE-175 / RE-176 registry rows (flipped Approved in 121-01)"
      via: "buildDocIdMap join-on-Path (fork injection)"
      pattern: "doc_id: RE-17"
---

<objective>
EEE-retrofit the 6 remaining Mermaid-free lifecycle files (lifecycle/{01,02,05}, lifecycle-apv2/{00,01,03})
and the 2 end-user guides (RE-175/176) to doc_type Guide, C17-green, Status Approved. This plan completes
the 13-file Mermaid-free lifecycle set (RETRO-07 partial, with 121-05) and closes RETRO-09.

Purpose: RETRO-07 (remaining 6) + RETRO-09 (the 2 end-user guides — actual EEE retrofit per D-07, not just
a registry flip). The 6 lifecycle files are keyless Windows Autopilot classic/APv2 docs → platform:
Windows injected.
Output: 8 C17-green Guide files; RE-193/194/197, RE-198/199/201, RE-175/176 injected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the fork on the 8 files and author 8 net-new Summaries (Guide doc_type)</name>
  <files>docs/lifecycle/01-hardware-hash.md, docs/lifecycle/02-profile-assignment.md, docs/lifecycle/05-post-enrollment.md, docs/lifecycle-apv2/00-overview.md, docs/lifecycle-apv2/01-prerequisites.md, docs/lifecycle-apv2/03-automatic-mode.md, docs/end-user-guides/android-work-profile-setup.md, docs/end-user-guides/linux-intune-portal-enrollment.md</files>
  <read_first>
    - docs/lifecycle/01-hardware-hash.md (KEYLESS — no platform: key; Version gate blockquote before H1; first existing H2 is ## Context — the fork inserts ## Summary before it)
    - docs/lifecycle-apv2/00-overview.md (KEYLESS — fork injects platform: Windows)
    - docs/end-user-guides/android-work-profile-setup.md (platform: Android, audience: end-user; currently NO doc_id/status/owner/doc_type — needs actual retrofit per D-07), docs/end-user-guides/linux-intune-portal-enrollment.md (platform: Linux)
    - scripts/pipeline/retrofit-structural.mjs (Guide route for lifecycle dirs + docs/end-user-guides/; keyless → platform: Windows injection; [FILL-IN] placeholder; whole-pre-H1-span relocation; VH prepend/create)
    - docs/_standards/EEE-SOP-standard.md (Structure invariant: Summary = first H2, ≥30 words; D-07 lifecycle/end-user→Guide; D1 map: Windows/Android/Linux)
    - docs/reference/00-index.md (RE-142 — net-new-Summary layout shape)
  </read_first>
  <action>
    Run node scripts/pipeline/retrofit-structural.mjs on the 8 listed files (dry-run first to confirm each
    doc_id resolves — RE-193/194/197 for lifecycle/{01,02,05}, RE-198/199/201 for lifecycle-apv2/{00,01,03},
    RE-175/176 for the two end-user guides — and that the 6 keyless lifecycle files inject platform: Windows
    → "Windows" while the end-user guides map Android → "Android" and Linux → "Linux"; then write). The fork
    injects doc_id/status: Approved/owner: Intune Admin Lead/doc_type: Guide, emits "**Doc Type:** Guide"
    before H1, relocates any pre-H1 span below a new ## Summary, and inserts the [FILL-IN] placeholder +
    v1.16 VH row. Replace each [FILL-IN] with a NET-NEW ≥30-word scope Summary written for the doc's
    audience: the 6 lifecycle Summaries state the Autopilot stage/topic (hardware hash import, profile
    assignment, post-enrollment, APv2 overview/prerequisites/automatic-mode); the 2 end-user Summaries are
    written in end-user voice (setting up a personal Android device for BYOD work profile / enrolling a
    Linux device via the Intune Portal). Write NEW sentences — do NOT promote an existing paragraph to be
    the Summary. Leave all body content, tables, and cross-links untouched.
  </action>
  <verify>
    <automated>for f in docs/lifecycle/01-hardware-hash.md docs/lifecycle/02-profile-assignment.md docs/lifecycle/05-post-enrollment.md docs/lifecycle-apv2/00-overview.md docs/lifecycle-apv2/01-prerequisites.md docs/lifecycle-apv2/03-automatic-mode.md docs/end-user-guides/android-work-profile-setup.md docs/end-user-guides/linux-intune-portal-enrollment.md; do grep -q '\[FILL-IN' "$f" && echo "PLACEHOLDER-REMAINS:$f"; grep -q '^doc_id: RE-' "$f" || echo "NO-DOCID:$f"; grep -q '^doc_type: Guide' "$f" || echo "NO-GUIDE:$f"; done; echo CHECKED</automated>
  </verify>
  <acceptance_criteria>
    - None of the 8 files contains "[FILL-IN"; each has a net-new ## Summary as first H2 with ≥30 words
    - Each frontmatter carries doc_id (as mapped), status: Approved, owner: Intune Admin Lead, doc_type: Guide; block line reads "**Doc Type:** Guide"
    - The 6 keyless lifecycle files now carry platform: Windows (block label "Windows"); the 2 end-user guides now carry doc_id/status/owner/doc_type they previously lacked (D-07)
    - No existing body paragraph was promoted to Summary; all tables and cross-links unchanged
  </acceptance_criteria>
  <done>All 6 remaining Mermaid-free lifecycle files + 2 end-user guides enrolled at Guide with net-new ≥30-word Summaries.</done>
</task>

<task type="auto">
  <name>Task 2: Split over-200-char blockquotes, confirm no ```mermaid, prove C17 green + reformat-only</name>
  <files>docs/lifecycle/01-hardware-hash.md, docs/lifecycle/02-profile-assignment.md, docs/lifecycle/05-post-enrollment.md, docs/lifecycle-apv2/00-overview.md, docs/lifecycle-apv2/01-prerequisites.md, docs/lifecycle-apv2/03-automatic-mode.md, docs/end-user-guides/android-work-profile-setup.md, docs/end-user-guides/linux-intune-portal-enrollment.md</files>
  <read_first>
    - The 8 files after Task 1 — the Version/Platform gate and For-users blockquotes exceeding 200 chars (light: 0-6 per file; lifecycle/02-profile-assignment.md has the most at ~6)
    - scripts/validation/c17-eee-contract.mjs (assertion #12 ~L386 JOINS consecutive > lines; assertion #1 hard-fails on ```mermaid — confirm the 8 have none)
    - .planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-CONTEXT.md (R2 word-preserving splits; reformat-only discipline)
  </read_first>
  <action>
    For each of the 8 files, split every blockquote whose joined text exceeds 200 chars into multiple
    ≤200-char blockquotes, preserving every word (Transform A — sentence/clause boundaries or " · "
    separators, blank line between resulting blockquotes so C17 #12 measures them independently). Reserve
    de-blockquote (Transform B) only for a code-fenced admonition or an unsplittable Markdown-link span.
    Confirm none of the 8 files contains a ```mermaid fence. Prove reformat-only: word-set multiset BEFORE
    vs AFTER (strip leading ">" prefix before comparing) shows zero real words added/removed apart from the
    net-new Summaries + fork block line/VH rows. Run the full C17 corpus and confirm 0 violations.
  </action>
  <verify>
    <automated>for f in docs/lifecycle/01-hardware-hash.md docs/lifecycle/02-profile-assignment.md docs/lifecycle/05-post-enrollment.md docs/lifecycle-apv2/00-overview.md docs/lifecycle-apv2/01-prerequisites.md docs/lifecycle-apv2/03-automatic-mode.md docs/end-user-guides/android-work-profile-setup.md docs/end-user-guides/linux-intune-portal-enrollment.md; do awk '/^>/{if(length($0)>200) c++} END{if(c>0) print "LONG-BQ:'"$f"'"}' "$f"; grep -q '```mermaid' "$f" && echo "MERMAID:$f"; done; echo CHECKED</automated>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -E "checked, 0 with violations, 0 total violations"</automated>
  </verify>
  <acceptance_criteria>
    - No ">"-prefixed line exceeds 200 chars in any of the 8 files; every blockquote group joined text ≤200 chars (C17 #12 green)
    - None of the 8 files contains a ```mermaid fence
    - node scripts/validation/c17-eee-contract.mjs reports "0 with violations, 0 total violations" and exits 0 (full corpus — all 13 Mermaid-free lifecycle + 2 end-user guides now enrolled)
    - Word-set reformat-only proof passes (zero real words added/removed apart from Summaries + fork emissions)
  </acceptance_criteria>
  <done>All 8 files C17-green, blockquotes ≤200 chars, no mermaid, proven reformat-only; RETRO-09 closed and the 13-file Mermaid-free lifecycle set complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Reformat-only Markdown edits to 8 files plus one pipeline invocation. No runtime, network, auth, session,
user-input, or cryptographic surface. Integrity concerns: silent content loss during splitting, and
accidentally enrolling a Mermaid-bearing file.

| Boundary | Description |
|----------|-------------|
| author/fork edit → lifecycle + end-user corpus | Reformat byte-faithful except net-new Summaries |
| enrollment gate → C17 #1 | Only Mermaid-free files may be enrolled this phase (these 8 carry none) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-06-01 | Tampering (silent content loss) | blockquote splitting | mitigate | Word-set multiset reformat-only proof; C17 full-corpus 0 violations |
| T-121-06-02 | Tampering (of enforcement) | enrolling a Mermaid file → C17 #1 | mitigate | Task 2 confirms no ```mermaid in the 8 files; fork MERMAID_DEFERRED_PATHS excludes the 9 |
| T-121-06-03 | Tampering | keyless-file platform injection wrong label | mitigate | Dry-run confirms platform: Windows → "Windows" for the 6 keyless files before write |
| T-121-06-SC | Tampering | package installs | n/a | No installs; fork uses Node built-ins only |

Spoofing / Repudiation / Information-Disclosure / Denial-of-Service / Elevation-of-Privilege: no new
surface — documentation reformat with no runtime execution path.
</threat_model>

<verification>
Full C17 corpus reports 0 with violations, 0 total violations, exit 0; no ">"-prefixed line exceeds 200
chars and no ```mermaid in any of the 8 files; the 6 keyless lifecycle files carry platform: Windows; the 2
end-user guides carry their new EEE frontmatter keys; word-set reformat-only proof passes.
</verification>

<success_criteria>
- ROADMAP Phase 121 SC2 (complete, with 121-05): all 13 Mermaid-free lifecycle docs carry the EEE block +
  Summary-first + normalized Platform label + Status: Approved; C17 exits 0
- ROADMAP Phase 121 SC3 met: RE-175 and RE-176 carry the EEE block + Summary-first + doc_type Guide; C17
  exits 0 on both
- ROADMAP Phase 121 SC4 (partial): all 8 carry the "v1.16 EEE reformat — content not re-reviewed" VH row
</success_criteria>

<output>
Create `.planning/phases/121-structural-retrofit-glossaries-lifecycle-end-user-guides/121-06-SUMMARY.md` when done
</output>
