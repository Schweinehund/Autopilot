---
phase: 29-ios-admin-setup-byod-mam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_templates/admin-template-ios.md
autonomous: true
requirements: [ABYOD-01, ABYOD-02, ABYOD-03]

must_haves:
  truths:
    - "Template has a `PRIVACY-LIMIT CALLOUT PATTERN` comment block mirroring the existing `SUPERVISED-ONLY CALLOUT PATTERN` block"
    - "New pattern specifies plain-blockquote format (no glyph) per D-02"
    - "New pattern specifies `#user-enrollment` as the canonical link target per D-03"
    - "New pattern explicitly scopes usage to `08-user-enrollment.md` only (not 07 or 09) per D-18"
    - "Existing `SUPERVISED-ONLY CALLOUT PATTERN` block is unchanged (Phase 27 lock)"
  artifacts:
    - path: "docs/_templates/admin-template-ios.md"
      provides: "Privacy-limit callout pattern documentation for downstream guides"
      contains: "PRIVACY-LIMIT CALLOUT PATTERN"
  key_links:
    - from: "docs/_templates/admin-template-ios.md"
      to: "docs/ios-lifecycle/00-enrollment-overview.md#user-enrollment"
      via: "pattern example link"
      pattern: "ios-lifecycle/00-enrollment-overview.md#user-enrollment"
---

<plan_grouping_rationale>
Phase 29 is decomposed into 5 plans across 2 waves:

- Wave 1 (Plans 01 + 02): Template extension (01) and overview restructure (02). These two plans touch different files (`_templates/admin-template-ios.md` vs `admin-setup-ios/00-overview.md`) with zero overlap, and have no content dependency on each other — they can run fully in parallel.
- Wave 2 (Plans 03/04/05): Three guide plans run in parallel (zero file overlap between `07-device-enrollment.md`, `08-user-enrollment.md`, `09-mam-app-protection.md`). All three depend on Plans 01 AND 02: the template extension (01) defines the privacy-limit callout format consumed by Plan 04, and the overview restructure (02) provides the shared `#intune-enrollment-restrictions` anchor consumed by Plans 03 and 04.

Plan 01 is a separate plan (rather than merged into the overview plan) because the template is a contract consumed by Plan 04 alone while the overview is consumed by all three Wave 2 plans — keeping them split means the overview plan doesn't need to touch the template and vice versa, which is tidier dependency hygiene and allows Plan 01 and Plan 02 to complete in parallel within Wave 1.
</plan_grouping_rationale>

<objective>
Extend `docs/_templates/admin-template-ios.md` with a new `PRIVACY-LIMIT CALLOUT PATTERN` comment block documenting the format, placement rule, glyph-prohibition rule, link-target rule, and scope rule for the privacy-limit callout introduced by Phase 29. The new block mirrors the existing `SUPERVISED-ONLY CALLOUT PATTERN` convention (lines 30-36) verbatim in style, placement, and comment-block structure. This template extension is the canonical pattern definition consumed by Plan 04 (User Enrollment guide, ABYOD-02) and referenced by Plans 03 and 05 as a negative constraint (those guides must NOT use it).

Purpose: Implements D-05 — the template is the canonical pattern home, mirroring how Phase 27 D-01 introduced the supervised-only callout to the same template. Implementing the pattern in the template first ensures Plan 04 can quote the template when writing the User Enrollment guide rather than reinventing the format.

Output: A single-file diff adding ~12 lines (one new comment block) to `docs/_templates/admin-template-ios.md`, placed immediately below the existing supervised-only block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-ios-admin-setup-byod-mam/29-CONTEXT.md
@.planning/phases/29-ios-admin-setup-byod-mam/29-PATTERNS.md
@.planning/phases/29-ios-admin-setup-byod-mam/29-VALIDATION.md

<interfaces>
<!-- Existing in-file comment block (lines 30-36) — the NEW block must mirror this -->

```markdown
<!-- SUPERVISED-ONLY CALLOUT PATTERN
     Use this exact format for every supervised-only setting. No variations.
     Place immediately AFTER the setting description, BEFORE any configuration steps.
     Link target is ALWAYS the Phase 26 conceptual page, NOT the enrollment profile guide.

     > 🔒 **Supervised only:** [feature/setting name] requires supervised mode. [1-2 sentence explanation of what this means for unsupervised devices.] See [Supervision](../ios-lifecycle/00-enrollment-overview.md#supervision).
-->
```

The new PRIVACY-LIMIT CALLOUT PATTERN block, per 29-PATTERNS.md "Target new comment block to ADD (D-05 + D-01/D-02/D-03)" section:

```markdown
<!-- PRIVACY-LIMIT CALLOUT PATTERN
     Use this exact format for account-driven User Enrollment privacy boundaries. No variations.
     Place immediately AFTER the capability/setting description, BEFORE any configuration steps.
     Plain blockquote only — NO emoji/glyph. Do NOT use 🔒 (reserved for supervised-only) or
     introduce a new glyph (parallel glyph conventions are locked per Phase 27).
     Link target is ALWAYS the Phase 26 conceptual page #user-enrollment anchor,
     NOT the User Enrollment admin guide (08-user-enrollment.md).
     Apply ONLY in account-driven User Enrollment contexts (08-user-enrollment.md).
     Do NOT use in Device Enrollment (07-device-enrollment.md) or MAM-WE (09-mam-app-protection.md).

     > **Privacy limit:** [what IT cannot see/do for this capability]. See [User Enrollment](../ios-lifecycle/00-enrollment-overview.md#user-enrollment).
-->
```

Placement: Insert immediately BELOW the existing supervised-only block (after line 36), leaving a blank line between the two blocks for readability. Do NOT move, edit, or touch the supervised-only block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PRIVACY-LIMIT CALLOUT PATTERN comment block to iOS admin template</name>
  <files>docs/_templates/admin-template-ios.md</files>
  <read_first>
    - docs/_templates/admin-template-ios.md (read fully — the NEW block is inserted after line 36; must not disturb lines 30-36)
    - .planning/phases/29-ios-admin-setup-byod-mam/29-PATTERNS.md section "docs/_templates/admin-template-ios.md — MODIFIED (extend per D-05)" (lines 541-587) — contains the exact block text to insert
    - .planning/phases/29-ios-admin-setup-byod-mam/29-CONTEXT.md D-01 (inline callout format), D-02 (no-glyph rule), D-03 (link target), D-05 (template extension directive), D-18 (ABYOD-01 scope exclusion)
  </read_first>
  <action>
Implements D-05 (add privacy-limit callout pattern to template), D-01 (exact callout format), D-02 (no glyph), D-03 (link target `#user-enrollment`), D-18 (scope excludes 07 and 09).

Read `docs/_templates/admin-template-ios.md` first to confirm the current structure around lines 30-36. The existing SUPERVISED-ONLY CALLOUT PATTERN block lives there and MUST remain byte-identical after this task — do not reformat whitespace, re-wrap lines, change indentation, or touch any character inside or above that block.

Insert the following NEW comment block VERBATIM immediately after the existing supervised-only block (after the closing `-->` on line 36). Leave exactly one blank line between the closing `-->` of the supervised-only block and the opening `<!--` of the new privacy-limit block, mirroring the existing file's single-blank-line separator convention between comment blocks and surrounding content:

```markdown
<!-- PRIVACY-LIMIT CALLOUT PATTERN
     Use this exact format for account-driven User Enrollment privacy boundaries. No variations.
     Place immediately AFTER the capability/setting description, BEFORE any configuration steps.
     Plain blockquote only — NO emoji/glyph. Do NOT use 🔒 (reserved for supervised-only) or
     introduce a new glyph (parallel glyph conventions are locked per Phase 27).
     Link target is ALWAYS the Phase 26 conceptual page #user-enrollment anchor,
     NOT the User Enrollment admin guide (08-user-enrollment.md).
     Apply ONLY in account-driven User Enrollment contexts (08-user-enrollment.md).
     Do NOT use in Device Enrollment (07-device-enrollment.md) or MAM-WE (09-mam-app-protection.md).

     > **Privacy limit:** [what IT cannot see/do for this capability]. See [User Enrollment](../ios-lifecycle/00-enrollment-overview.md#user-enrollment).
-->
```

Copy the text character-for-character including the em-dashes (`—`), the 🔒 glyph inside the prose (referencing the supervised-only lock), the inline-format-example blockquote, and the bold `**Privacy limit:**` marker.

Do NOT:
- Edit, delete, or re-indent the existing SUPERVISED-ONLY CALLOUT PATTERN block (Phase 27 D-01/D-03 locked — "No variations, no abbreviated forms")
- Add a leading glyph to the privacy-limit inline example (D-02 forbids this)
- Change the link target to anything other than `../ios-lifecycle/00-enrollment-overview.md#user-enrollment` (D-03 locked)
- Omit the "Apply ONLY in account-driven User Enrollment contexts" / "Do NOT use in Device Enrollment or MAM-WE" scope rule (this enforces D-18 at template level so future guides don't accidentally adopt the pattern outside ABYOD-02)

No frontmatter changes. No changes anywhere else in the file.
  </action>
  <verify>
    <automated>bash -c "grep -q 'PRIVACY-LIMIT CALLOUT PATTERN' docs/_templates/admin-template-ios.md && grep -q 'SUPERVISED-ONLY CALLOUT PATTERN' docs/_templates/admin-template-ios.md && grep -q '> \*\*Privacy limit:\*\* \[what IT cannot see/do for this capability\]' docs/_templates/admin-template-ios.md && grep -q 'ios-lifecycle/00-enrollment-overview.md#user-enrollment' docs/_templates/admin-template-ios.md && ! grep -q 'Privacy limit.*🔒' docs/_templates/admin-template-ios.md && ! grep -q 'Privacy limit.*🛡️' docs/_templates/admin-template-ios.md && echo OK"</automated>
  </verify>
  <done>
    - `docs/_templates/admin-template-ios.md` contains both the unchanged SUPERVISED-ONLY CALLOUT PATTERN block AND the new PRIVACY-LIMIT CALLOUT PATTERN block
    - New block includes all 6 pattern-rule lines: usage rule, placement rule, glyph rule (no-emoji), link-target rule, scope-rule (ABYOD-02 only), format-literal blockquote example
    - Inline example uses plain `> **Privacy limit:** ...` blockquote with no 🔒, 🛡️, or any other glyph
    - Link target in example is `../ios-lifecycle/00-enrollment-overview.md#user-enrollment`
    - Existing SUPERVISED-ONLY block remains byte-identical (verify via `git diff` — only additions, no deletions or modifications to lines 1-36)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal authoring → published docs | Template extension becomes a pattern reference for future iOS admin guides; incorrect pattern text propagates downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Information Disclosure | Template example text | accept | Template example uses generic placeholder text `[what IT cannot see/do for this capability]` — no real tenant IDs, emails, or internal personas. No PII or confidential org detail in the example block. |
| T-29-01-02 | Tampering (doc-integrity) | Roadmap-disclosure | accept | No Microsoft non-public roadmap referenced in this task; all language is pattern-definition only. No `ms.date` citations needed at template level (those live in individual guide frontmatter). |
</threat_model>

<verification>
Run the automated verify command from Task 1. Additionally, inspect the file with `sed -n '25,55p' docs/_templates/admin-template-ios.md` to confirm:
1. Lines 30-36 still contain the SUPERVISED-ONLY CALLOUT PATTERN block unchanged
2. A new PRIVACY-LIMIT CALLOUT PATTERN block follows after a single blank line
3. Both blocks use the `<!-- ... -->` HTML-comment syntax
4. No stray backticks, code fences, or markdown formatting leaked into the comment content
</verification>

<success_criteria>
- 29-TPL-01 validation row (29-VALIDATION.md) passes: `grep -i "PRIVACY-LIMIT CALLOUT PATTERN" docs/_templates/admin-template-ios.md` exits 0
- `grep -q "SUPERVISED-ONLY CALLOUT PATTERN" docs/_templates/admin-template-ios.md` exits 0 (lock preserved)
- `git diff docs/_templates/admin-template-ios.md` shows only additions (no deletions or character-level changes to lines 1-36)
- Downstream Plan 04 can reference this new comment block as the canonical format when writing privacy-limit callouts in `08-user-enrollment.md`
</success_criteria>

<output>
After completion, create `.planning/phases/29-ios-admin-setup-byod-mam/29-01-SUMMARY.md` documenting:
- What was added (PRIVACY-LIMIT CALLOUT PATTERN comment block)
- Where it was placed (line number range post-edit)
- Confirmation that the SUPERVISED-ONLY block was not modified
- Downstream consumers: Plan 04 (format quoted directly); Plans 03 and 05 (enforce scope exclusion per D-18)
</output>
