---
phase: 141-standalone-red-validator-set-chain-members-green
plan: 05
type: execute
wave: 5
depends_on: ["141-04"]
files_modified:
  - .planning/phases/141-standalone-red-validator-set-chain-members-green/141-sweep.ps1
  - .planning/phases/141-standalone-red-validator-set-chain-members-green/141-EVIDENCE.md
autonomous: true
requirements: [RED-01, RED-02, RED-03]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All nine v1.5 through v1.13 milestone-audit harnesses exit 0 by direct invocation with zero edits to either glossary — SC#1's stated evidence form, re-executed in this phase rather than inherited as an assertion."
    - "Each of the nine harness runs reports a non-zero total assertion count (v1.5 twelve, v1.6 through v1.13 fifteen each). Exit code 0 alone is not evidence: these harnesses exit on a failure count, so a run that executed zero assertions would also exit 0."
    - "Neither glossary's freshness metadata is edited. Both sit at exactly ninety days against a strictly-greater test, so an edge case that is exactly equal must PASS, not collide — a one-day shift in either direction flips multiple currently-green workflows red."
    - "The freshness leg cleared before the self-test greened the cascade, and the eight members went green in that order — RED-01 is the prerequisite, not the self-test. The evidence artifact records the ordering as observed, not as assumed."
    - "Each of check-phase-48, -60, -61, -62, -63, -64, -65 and -66 exits 0 under a BARE invocation with the nesting variable unset, taken in ascending order 48 through 66, one at a time, on a quiesced machine with the cache state declared."
    - "Each of those eight runs reports a non-zero PASS count and a zero FAIL count. A zero-check run exits 0 too, so the PASS count is part of the evidence, not decoration."
    - "Ascending order is load-bearing: a failure at the cheapest member surfaces in under a second instead of eleven minutes deep inside the most expensive member's chain replay, and it prevents a member's own CHAIN guard from being the first place a predecessor's failure is seen."
    - "One check-phase-66 verbose run reports every one of 48 through 65 individually and proves them green in composition — a property eight isolated runs cannot establish."
    - "The evidence artifact states, in its own words, that no nested run and no apex run is evidence for any part of SC#2 or SC#3, because under nesting every chain guard AND all three self-test call sites return a passing skip, so members 62 through 66 exit 0 today even unfixed."
    - "The sweep asserts the count of running validator processes as an executable step and emits that assertion into the artifact — an acknowledgment in prose is not a mechanism."
    - "The anti-contention rule is scoped to same-machine runs. Parallel one-per-job CI execution on isolated runners remains admissible; it is the only form the milestone's all-workflows-green bar can consume. Figures recorded before this phase are grandfathered explicitly."
  artifacts:
    - ".planning/phases/141-standalone-red-validator-set-chain-members-green/141-sweep.ps1 — the quiesce-asserting serial sweep runner"
    - ".planning/phases/141-standalone-red-validator-set-chain-members-green/141-EVIDENCE.md — the RED-01, RED-02 and RED-03 evidence artifact"
  key_links:
    - "The nine harnesses' C5/C10 freshness assertions -> V-60-23 and V-61-33, the two chain-member checks that read them"
    - "The ascending sweep order -> each member's own CHAIN guards; a green run at 48 is a precondition for 60's CHAIN-48 to mean anything"
    - "check-phase-66 --verbose's per-member chain lines -> composition evidence that eight isolated exit codes cannot supply"
    - "The nesting environment variable -> every CHAIN guard and all three self-test call sites -> the false-green trap that would make a Phase 144 apex showing all-green look like proof"
  prohibitions:
    - "Never edit either glossary's last_verified or review_by metadata, or any other corpus content, to satisfy any assertion."
    - "Never run two validators concurrently on this machine, and never record a timing figure without stating the cache state."
    - "Never offer a nested or apex run as evidence for SC#2 or SC#3."
    - "Never present a single un-repeated wall-clock figure to six significant figures as a measurement in a phase whose margin is a couple of percent."
    - "Do not author a new wall-clock pass/fail threshold — the sweep records costs, it does not gate on them (D-19)."
    - "Do not create the sweep script under scripts/ — it is one-shot phase evidence tooling, and a new durable path there would be off-list under the CARVE allowlist and would require its own amendment commit for no benefit."
    - "Do not retroactively invalidate figures recorded before this phase; the grandfather statement is mandatory, because without it the anti-contention rule would inadmit the very apex-adoption figure Phase 142 depends on."
---

<objective>
Produce the phase's evidence: the nine-harness RED-01 discharge, and RED-03's dual-form proof —
eight ascending quiesced standalone invocations plus one verbose composition run — in one
budgeted block at the end, with the false-green trap and the grandfather clause written into the
artifact so no later reader mistakes an apex run for proof.

Purpose: this phase has a documented five-figure record of bad measurements, including one that
was arithmetically impossible against its own series and survived a review. Measurement
discipline is therefore a deliverable, not a habit: quiesce, serialize, declare the cache state,
and make the process-count assertion an executable step whose output lands in the artifact.

Output: a sweep script and an evidence artifact, both in the phase directory.

## Artifacts this phase produces

Contributed by this plan:
- `141-sweep.ps1` — quiesce assertion, serial ascending runner, per-validator wall clock and
  tally capture.
- `141-EVIDENCE.md` — the nine-harness table, the eight-member table, the verbose composition
  transcript excerpt, the false-green trap statement, and the grandfather statement.

The full phase-level artifact roll-up lives in `141-01-PLAN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md
@.planning/phases/141-standalone-red-validator-set-chain-members-green/141-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED-01 evidence — nine harnesses green by direct invocation, zero glossary edits</name>
  <files>.planning/phases/141-standalone-red-validator-set-chain-members-green/141-EVIDENCE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 141" SC#1 as amended by Plan 01 — the stated evidence form.
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md` — the RED-01 measured result (v1.5 twelve of twelve, v1.6 through v1.13 fifteen of fifteen each), D-26, D-29, D-30.
    - `.planning/REQUIREMENTS.md` Out of Scope table — the standing bar on editing either glossary's freshness metadata and its stated reason.
    - `.planning/milestones/v1.20-CARVE.md` §"Standing bars" — the zero-margin hazard.
    - `scripts/validation/check-phase-60.mjs` and `scripts/validation/check-phase-61.mjs` — locate V-60-23 and V-61-33 by identifier so their PASS lines can be quoted rather than paraphrased.
  </read_first>
  <action>
Quiesce and assert it before the first run, using a process query scoped to command lines
matching the validator script name pattern rather than a bare node-process count. Declare the
cache state.

Enumerate the nine milestone-audit harnesses for v1.5 through v1.13 by listing the matching files
under `scripts/validation/` rather than transcribing names from memory. Invoke each one directly
with node, serially, and capture for each: the exit code, the total assertion count, and the
pass/fail split. Do not run them through a chain validator — direct invocation is the stated
evidence form.

Assert that each run reports a non-zero total assertion count. These harnesses exit on their
failure count, so a run that executed zero assertions would also exit 0; a bare exit code is not
sufficient evidence and this distinction must be stated in the artifact.

Confirm zero corpus edits: `git diff --stat` against the phase base for `docs/` overall, and
specifically for both glossary files, must be empty. Record the reason this matters: both
glossaries sit at exactly ninety days against a strictly-greater freshness test, so the
exactly-equal boundary case must resolve as a pass, and a one-day shift in either direction flips
multiple currently-green workflows red.

Record V-60-23 and V-61-33 as individually PASS, quoting the validator output lines.

Start `141-EVIDENCE.md` with a RED-01 section containing: the quiesce assertion output, the cache
state, a nine-row table (harness, exit code, assertion count, pass/fail), the two quoted chain
check lines, the zero-corpus-edit diff output, and one paragraph recording that the freshness leg
cleared before the self-test greened the cascade — the ratified ordering, observed rather than
assumed.

Finally, record explicitly that RED-01 flips Pending to Complete at this phase and reaches
Validated only at Phase 144's single close-gate commit. Do not write Validated anywhere.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-60.mjs</automated>
    <automated>node scripts/validation/check-phase-61.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The quiesce assertion output is captured in `141-EVIDENCE.md` and shows zero matching validator processes.
    - Nine harnesses were enumerated by directory listing, not transcription, and the enumerated list is recorded.
    - Each of the nine direct invocations exits 0, and each recorded row carries a non-zero assertion count.
    - `git diff --stat docs/` produces no output; `git diff --stat docs/_glossary-android.md docs/_glossary-macos.md` produces no output. Both outputs are recorded verbatim.
    - V-60-23 and V-61-33 appear in `141-EVIDENCE.md` as quoted PASS lines from real runs.
    - `141-EVIDENCE.md` contains the exactly-ninety-days boundary statement and the reason a one-day shift is unsafe in either direction.
    - `141-EVIDENCE.md` contains the ordering paragraph and the Pending-to-Complete-now, Validated-at-Phase-144 statement.
    - The string `Validated` does not appear as a status assigned to any requirement in this plan's output.
  </acceptance_criteria>
  <done>Nine harnesses green by direct invocation with non-zero assertion counts, zero corpus edits proven by diff, the two chain checks quoted, RED-01's evidence section written.</done>
</task>

<task type="auto">
  <name>Task 2: RED-03 dual-form evidence — eight ascending standalone invocations plus one verbose composition run</name>
  <files>.planning/phases/141-standalone-red-validator-set-chain-members-green/141-sweep.ps1, .planning/phases/141-standalone-red-validator-set-chain-members-green/141-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md` — the eight-member failure ledger with entry-state tallies and measured wall clocks, D-20, D-21, D-22, D-23, D-31, D-32.
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-04-SUMMARY.md` — the bare check-phase-68 measurement and the quiesce procedure already used once in this phase.
    - `scripts/validation/check-phase-48.mjs` line 72, `scripts/validation/check-phase-60.mjs` line 188, `scripts/validation/check-phase-61.mjs` line 385 — the three self-test call sites that return a passing skip under nesting, and are therefore the mechanism behind the false-green trap.
    - `scripts/validation/check-phase-66.mjs` lines 40-75 (its chain array and its empty skip list) and lines 310-350 (the two spawns, one of which Plan 04 raised).
  </read_first>
  <action>
Write `141-sweep.ps1` in the phase directory. Do not create it under `scripts/` — it is one-shot
phase evidence tooling, and a new durable path there would be off-list under the CARVE allowlist.
The script must do four things, in this order, and emit every result into a machine-readable
block the artifact can quote:

1. Assert the validator-process count. Query running processes and filter on command lines
   matching the validator script name pattern; fail loudly and stop if the count is not zero.
   Do not test a bare node-process count — this machine carries permanent non-validator node
   residents, and that form of the check is also unexecutable on a Linux runner.
2. Record a declared cache state, passed in as a parameter, so the operator states it rather than
   the script guessing.
3. Run the eight members in ascending order — 48, 60, 61, 62, 63, 64, 65, 66 — strictly serially,
   one process at a time, each with the nesting environment variable explicitly cleared for the
   child. Capture for each: wall clock in milliseconds, exit code, and the PASS/FAIL/SKIPPED
   tally line.
4. Re-assert the process count after the sweep and emit the whole record.

Budget roughly 22 minutes for the sweep. Known clean warm costs, ascending: about 0.5, 10, 22,
43, 84, 171, 335 and 665 seconds. If any member's wall clock exceeds twice its listed figure,
record it as an anomaly and note the cache state rather than silently accepting it.

Run the sweep once. Every member must exit 0 with a zero FAIL count AND a non-zero PASS count —
the exit path is a failure-count test, so a run that executed no checks would also exit 0, and
the PASS count is therefore part of the evidence.

Then run form (b): one `node scripts/validation/check-phase-66.mjs --verbose`, bare, on the same
quiesced machine, budget roughly 11 minutes. Capture its per-member chain lines, which report each
of 48 through 65 individually and prove them green in composition — a property eight isolated
runs cannot establish. Quote the full set of those lines into the artifact.

Write the RED-03 section of `141-EVIDENCE.md` containing: the sweep script's emitted record, the
eight-row table, the verbose transcript excerpt, and three mandatory statements in the artifact's
own words:

- The false-green trap. Under the nesting environment variable every chain guard and all three
  self-test call sites return a passing skip, so members 62 through 66 exit 0 today even unfixed.
  No nested run and no apex run is evidence for any part of SC#2 or SC#3, and a Phase 144 apex
  reporting all-green would look exactly like proof while proving nothing about this phase.
- The grandfather clause. The anti-contention rule is scoped to same-machine runs. Parallel
  one-per-job CI execution on isolated runners stays admissible — it is the only form the
  milestone's all-workflows-green bar can consume. Figures recorded before this phase, including
  the apex-adoption cost figure Phase 142 depends on, are grandfathered explicitly and are not
  retroactively inadmissible.
- The handoff. The exponential standalone curve is real and predictive, holding to under one
  percent at its first out-of-sample point; hand the measured law to Phase 142 as a named input,
  and record the gap: the cold-clone apex requirement covers only the apex, so the non-nested
  standalone path and the verbose job's CI cap are currently owned by no requirement.

Commit the script and the artifact together as `docs(141-05): RED-01 and RED-03 evidence — nine
harnesses, eight ascending standalone runs, one verbose composition run`.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-48.mjs</automated>
    <automated>node scripts/validation/check-phase-66.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - `141-sweep.ps1` exists in the phase directory and not under `scripts/`; `git status --porcelain scripts/` is empty for this plan.
    - The script's process-count assertion is an executable step that stops the run on a non-zero count, and its output appears in `141-EVIDENCE.md`. A prose acknowledgment alone does not satisfy this.
    - The script clears the nesting environment variable for each child explicitly, rather than relying on it being unset in the parent.
    - All eight members appear in the artifact with exit code 0, FAIL count 0, and a PASS count greater than 0, in ascending order 48, 60, 61, 62, 63, 64, 65, 66.
    - Every recorded wall clock carries the declared cache state, and the artifact states that the runs were serial, one process at a time.
    - The verbose composition run's per-member chain lines for 48 through 65 are quoted in the artifact, and every one of them reports a green result.
    - `141-EVIDENCE.md` contains all three mandatory statements — the false-green trap, the grandfather clause, and the Phase 142 handoff with its named ownership gap.
    - No new wall-clock pass/fail threshold is authored anywhere in the script or the artifact.
    - `git diff --stat docs/` produces no output.
  </acceptance_criteria>
  <done>Both D-20 evidence forms captured on a quiesced cache-declared machine, the sweep script's process assertion executable and emitted, and the three mandatory statements written into the artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Machine process table -> measurement validity | Orphaned validator subprocesses have already produced four bad figures in this phase's own history |
| Nesting environment variable -> which checks execute | The single mechanism that can turn an unfixed chain into an all-green report |
| Evidence artifact -> Phase 142 and Phase 144 readers | Downstream phases consume these figures as inputs; an unlabelled figure becomes a false premise |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-141-17 | Spoofing | a nested or apex run presented as standalone evidence | critical | mitigate | The sweep clears the nesting variable per child, and the artifact carries a mandatory statement that no nested or apex run is evidence for SC#2 or SC#3 |
| T-141-18 | Spoofing | a zero-check run exiting 0 | high | mitigate | Every member and every harness must report a non-zero PASS or assertion count, not merely exit 0 |
| T-141-19 | Tampering | corpus freshness metadata | critical | mitigate | Zero corpus edits, asserted by an empty diff on `docs/` and on both glossaries; the exactly-ninety-days boundary is recorded as a pass-by-design, not a margin to be adjusted |
| T-141-20 | Repudiation | unlabelled or contaminated measurements | high | mitigate | Executable process-count assertion emitted into the artifact, declared cache state, stated serial execution, and an explicit anomaly rule at twice the listed cost |
| T-141-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager install tasks in this phase; RESEARCH.md's Package Legitimacy Audit records "Not applicable" |
</threat_model>

<verification>
- All eight members exit 0 bare, in ascending order, each with 0 FAIL and a non-zero PASS count.
- All nine harnesses exit 0 by direct invocation, each with a non-zero assertion count.
- `node scripts/validation/check-phase-66.mjs --verbose` exits 0 and its per-member chain lines
  for 48 through 65 are all green.
- `node scripts/validation/carve-gate.mjs` exits 0 — HARD BLOCK (CARVE D-10). This plan modifies
  no in-scope path, so the gate's in-scope diff set is unchanged by it.
- GOV-02 ledger: no row is added, and the SUMMARY states that this is correct rather than a gap —
  this plan modifies no frozen-surface path, and the ledger's own discipline rule says absence is
  the correct outcome for such a plan.
</verification>

<success_criteria>
RED-01 and RED-03 both carry executed, quiesced, cache-declared evidence in both required forms,
with the false-green trap and the grandfather clause written down so no later reader can mistake
an apex run for proof or treat a prior figure as retroactively inadmissible.
</success_criteria>

<output>
Create `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-05-SUMMARY.md` when done
</output>
