---
phase: 141-standalone-red-validator-set-chain-members-green
plan: 06
type: execute
wave: 6
depends_on: ["141-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/141-standalone-red-validator-set-chain-members-green/141-EVIDENCE.md
autonomous: false
requirements: [RED-01, RED-02, RED-03, SWEEP-09]

estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The three audit-harness workflows are dispatched explicitly with an explicit ref, never relied on to fire from a push — no workflow in this repository carries a push trigger, so a push fires nothing, and a dispatch needs the file already on the default branch."
    - "All axes are recorded at ONE shared commit SHA, and CI is never read while the remote is behind the local branch."
    - "Evidence is job-level JSON, never the checks-UI colour: a green run is compatible with a cron-skipped quarterly job and with a continue-on-error advisory job."
    - "The job identity used when reading the JSON is resolved deliberately — the JSON's job name field carries the DISPLAY name, not the YAML job key, and matching on the wrong one silently reads the wrong job."
    - "The 19 previously-never-executed fan-out jobs produce a real result rather than a skip, and every one of them is individually accounted for in the triage table — a job that is still reported as skipped is a finding, not a pass."
    - "Every red job is classified into exactly one of three causes — content failure, wall-clock cap, or environment — and each carries a disposition that is either fixed-in-phase or routed to a named later phase. No red is recorded as accepted."
    - "The two accepted-red dispositions this milestone exists to delete are not reintroduced under any name."
    - "RED-01, RED-02, RED-03 and SWEEP-09 flip from Pending to Complete in the traceability table; none of them is set to Validated, which is the state a single close-gate commit assigns at Phase 144 across all 28 requirements."
  artifacts:
    - "A recorded dispatch record: three workflow run IDs, one shared head SHA, and the job-level JSON for each"
    - "A triage table in 141-EVIDENCE.md covering every fanned-out job with cause and disposition"
    - "Pending-to-Complete status flips for RED-01, RED-02, RED-03 and SWEEP-09 in .planning/REQUIREMENTS.md, plus the matching ROADMAP and STATE updates"
  key_links:
    - "The harness exit code -> the fan-out jobs' gating -> whether 19 jobs execute at all; Plan 02's fix is what makes them runnable and Plan 04's condition change is what makes their result readable"
    - "The four raised job caps -> whether a red is a content failure or a wall-clock artifact; without Plan 04 the fan-out would red on the clock and teach nothing"
    - "This fan-out's triage -> Phase 144's close PR, where discovering 19 never-executed jobs for the first time would be the worst possible moment"
  prohibitions:
    - "Never read CI while the remote is behind the local branch."
    - "Never treat a push as a trigger — dispatch explicitly with an explicit ref."
    - "Never take evidence from the checks UI colour; use job-level JSON."
    - "Never record a red as accepted, under that name or any synonym. Fix it, or route it to a named phase with a stated reason."
    - "Do not set any requirement to Validated in this phase."
    - "Do not force-push, rewrite, or rebase any already-pushed commit to fix a fan-out red."
  flagged_assumptions:
    - "SWEEP-09's edge-coverage probe category came back unclassified from a generic engine and stays UNRESOLVED. It is deliberately not auto-backstopped into a fabricated predicate and not dropped. SWEEP-09's acceptance in this phase rests on Plan 03's shallow-clone fail-loud proof and on Plan 01's amended site-count criterion; no additional edge predicate is asserted for it. The phase verifier must read this as an open item, not a covered one."
    - "The local branch is 76 commits ahead of the remote at plan time. The push in this plan is therefore substantially larger than a single-phase push, and its content includes Phases 139 and 140. If the owner declines the push at the checkpoint, this plan's dispatch tasks cannot execute and the fan-out must be routed to Phase 144 with that decision recorded — which would leave 19 never-executed jobs to be discovered inside the close PR, the exact outcome D-25 exists to prevent."
---

<objective>
Run the first-ever CI fan-out and triage it in-phase: push, dispatch the three audit-harness
workflows at one shared SHA, read job-level JSON, and account for every fanned-out job.

Purpose: 19 of these jobs have reported skipped on every ref all milestone. They become runnable
the moment the harness greens, and readable the moment their gating condition evaluates
regardless of the upstream result. Both landed in earlier plans. Budget one full cycle — 19
never-executed jobs will not all be green first time, and discovering that inside Phase 144's
close PR is the worst possible moment.

Output: a dispatch record, a per-job triage table, and four Pending-to-Complete status flips.

## Artifacts this phase produces

Contributed by this plan:
- A dispatch record (three run IDs at one shared head SHA) appended to `141-EVIDENCE.md`.
- A per-job triage table in `141-EVIDENCE.md`.
- Status flips for RED-01, RED-02, RED-03 and SWEEP-09 in `.planning/REQUIREMENTS.md`, plus the
  matching `.planning/ROADMAP.md` plan checkboxes and `.planning/STATE.md` position update.

The full phase-level artifact roll-up lives in `141-01-PLAN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md
@.planning/phases/141-standalone-red-validator-set-chain-members-green/141-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: authorize the push and the first-ever fan-out dispatch</name>
  <decision>Whether to push the local branch to the remote now and dispatch the three audit-harness workflows, or hold the fan-out.</decision>
  <context>
Publishing commits to the shared remote is effectively one-way here: history rewriting on the
default branch is barred by project convention, so this cannot be undone by the executor. At plan
time the local branch was 76 commits ahead of the remote, so this push carries Phases 139 and 140
as well as this phase's work.

D-25 ratified option A: Phase 141 owns the first CI fan-out run and triages it in-phase. The
prerequisites it named have all landed — Plan 02 greens the harness, Plan 04's condition change
makes the fanned-out jobs report a real result instead of a skip, and Plan 04's cap raises stop
the run from reding on the clock. Everything local is green per Plan 05's evidence.

The cost of option B is that 19 never-executed jobs are discovered for the first time inside
Phase 144's close PR, with no budget to triage them.
  </context>
  <options>
    <option id="option-a">
      <name>Push and dispatch now (D-25 ratified)</name>
      <pros>The 19 never-executed jobs are exercised and triaged with a full phase of budget behind them; Phase 144's close PR inherits a known-good fan-out; the milestone's all-workflows-green bar becomes measurable rather than assumed.</pros>
      <cons>Publishes 76+ commits including two prior phases; the push cannot be undone by the executor; a red fan-out consumes phase budget.</cons>
    </option>
    <option id="option-b">
      <name>Hold the fan-out for Phase 144</name>
      <pros>No publication decision is made in this phase; the local evidence stands on its own.</pros>
      <cons>Reintroduces exactly the failure D-25 was ratified to prevent; leaves the milestone bar unmeasured; the Phase 144 close PR absorbs the discovery.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Push, dispatch all three workflows at one shared SHA, and collect job-level JSON</name>
  <files>.planning/phases/141-standalone-red-validator-set-chain-members-green/141-EVIDENCE.md</files>
  <precondition>The GitHub CLI is installed and authenticated for this repository's remote (verify with an auth-status call before the push); the checkpoint above resolved to option-a.</precondition>
  <reversibility rating="one-way">Publishing commits to the shared default branch cannot be undone by the executor — history rewriting on the default branch is barred by project convention.</reversibility>
  <read_first>
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md` D-25 and D-26.
    - `.planning/REQUIREMENTS.md` — the paragraph recording that these workflows fire on pull-request, schedule and manual dispatch only, and that a green run is compatible with a cron-skipped quarterly job and a continue-on-error advisory job.
    - `.planning/ROADMAP.md` §"Phase 144" hard constraints — the same dispatch rules restated for the close.
    - `.github/workflows/audit-harness-v1.5-integrity.yml`, `-v1.6-integrity.yml`, `-v1.7-integrity.yml` — the trigger blocks and the job keys, so the dispatch targets and the job identities are read from source rather than remembered.
  </read_first>
  <action>
Confirm the working tree is clean and every earlier plan's commits are present. Confirm the
authenticated CLI identity and the remote. Record the local head SHA — this is the one shared SHA
every axis in this plan is recorded at.

Push the branch to the remote. Then confirm the remote is no longer behind before reading
anything from CI: the ahead-count against the upstream must be zero. Reading CI while the remote
is behind produces results for the wrong tree and is barred.

Dispatch each of the three audit-harness workflows explicitly, with an explicit ref naming the
default branch. Do not wait for a push to trigger them — no workflow here carries a push trigger,
and a dispatch requires the workflow file to already exist on the default branch, which the push
just satisfied.

For each dispatched run, wait for completion, then collect the job-level JSON. Do not read the
checks UI. When matching jobs in the JSON, resolve the identity deliberately: the JSON's job name
field carries the DISPLAY name, which is not the YAML job key — decide which one you are matching
on and record that decision, because matching the wrong one silently reads a different job.

Confirm that all three runs report the same head SHA and that it equals the recorded local head.

Append a Dispatch Record section to `141-EVIDENCE.md`: the shared SHA, the three run IDs and
URLs, and for every job in all three runs a row carrying the job key, the display name, the
conclusion, and the duration. Count the jobs and compare against the expected 29 total, of which
23 are per-validator jobs; if the counts differ, record the actual figures as measured and note
the discrepancy rather than restating the expected ones.
  </action>
  <verify>
    <automated>git rev-list --count origin/master..master</automated>
  </verify>
  <acceptance_criteria>
    - `gh auth status` succeeded before the push and its identity is recorded.
    - `git status --porcelain` was empty before the push.
    - After the push, `git rev-list --count origin/master..master` returns 0.
    - The local head SHA is recorded once and every dispatched run's head SHA equals it — recorded per run.
    - Three dispatches were issued explicitly with an explicit ref; the exact commands are recorded.
    - Job-level JSON was collected for all three runs and pasted or summarised per job into `141-EVIDENCE.md`; no conclusion is taken from the checks UI.
    - The Dispatch Record states explicitly whether the JSON job identity was matched on the display name or on the YAML job key.
    - Every job in all three runs appears as a row with a conclusion; the total job count and the per-validator job count are recorded as measured.
    - Any job still reported as skipped is listed separately as a finding, with the reason it was skipped (a schedule guard is a legitimate reason; an upstream gate is not, after Plan 04).
  </acceptance_criteria>
  <done>The branch is pushed, three workflows are dispatched at one shared SHA, and job-level JSON for every job in all three runs is recorded in the evidence artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Triage every fan-out result and flip the four requirement statuses to Complete</name>
  <files>.planning/phases/141-standalone-red-validator-set-chain-members-green/141-EVIDENCE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-EVIDENCE.md` — Task 1's Dispatch Record.
    - `.planning/REQUIREMENTS.md` — the milestone bar sentence naming the two dispositions this milestone deletes, and the traceability table.
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md` D-30 (Pending to Complete now, Validated at Phase 144) and the deferred-items list, which names the phases that legitimately own carried-forward work.
    - `.planning/ROADMAP.md` §"Phase 141" plan list and §"Phase 142"/"Phase 144" scope, so a routed red lands on a phase that actually owns it.
  </read_first>
  <action>
Build a triage table in `141-EVIDENCE.md` covering every job from Task 1's record. Classify each
non-green job into exactly one cause:

- Content failure — an assertion genuinely fails on Linux where it passes locally. Fix in-phase
  if the fix is inside this phase's scope; otherwise route it to the phase that owns the file,
  with the reason stated.
- Wall-clock cap — the job hit its `timeout-minutes`. Raise that job's cap to at least twice the
  observed run duration, re-dispatch that workflow, and record both the before and after
  durations. This is the first real cold-clone Linux measurement for those jobs, and it
  supersedes Plan 04's derived provisional values — say so explicitly and record the new numbers
  as MEASURED.
- Environment — a runner, checkout, or tooling problem rather than a repository-content problem.
  Fix if trivially in scope; otherwise route with a reason.

Every row needs a disposition of fixed-in-phase or routed-to-a-named-phase. No row may be
recorded as an accepted red, under that name or any synonym — the milestone bar requires both
existing accepted-red dispositions to be deleted, not extended.

If a fix and a re-dispatch are needed, re-dispatch only the affected workflow, record the new run
ID, and record that the axes now span two SHAs with the reason — do not silently mix them into
the single-SHA record from Task 1.

Then update the planning documents:
- `.planning/REQUIREMENTS.md` traceability: flip RED-01, RED-02, RED-03 and SWEEP-09 from Pending
  to Complete. Do not write Validated for any of them; a single close-gate commit assigns that at
  Phase 144 across all 28 requirements, and eight prior milestones ran on that invariant.
- `.planning/ROADMAP.md` §"Phase 141": mark the six plan checkboxes complete.
- `.planning/STATE.md`: advance the current position past Phase 141 and record the phase outcome.

Commit as `docs(141-06): CI fan-out triage + RED-01/02/03 and SWEEP-09 to Complete`.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs</automated>
    <automated>node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The triage table covers every job from Task 1's record with no omissions; the row count equals the job count recorded there.
    - Every non-green row carries exactly one of the three causes and a disposition of fixed-in-phase or routed-to-a-named-phase.
    - `grep -ci "accepted-standalone-ci-red\|accepted-scoped-red\|accepted red" .planning/phases/141-standalone-red-validator-set-chain-members-green/141-EVIDENCE.md` returns 0 outside of a sentence explicitly stating that these dispositions are not used.
    - Any wall-clock-capped job has its new cap recorded together with the observed duration, and the new figures are labelled MEASURED and stated to supersede Plan 04's derived provisional values.
    - `grep -n "RED-01\|RED-02\|RED-03\|SWEEP-09" .planning/REQUIREMENTS.md` shows all four traceability rows reading Complete.
    - No traceability row anywhere in `.planning/REQUIREMENTS.md` reads Validated as a result of this task.
    - `node scripts/validation/check-phase-54.mjs` exits 0 after the planning-document edits.
    - `node scripts/validation/carve-gate.mjs` exits 0.
    - If any fix required a second dispatch, the two-SHA situation is stated explicitly with its reason rather than folded into the single-SHA record.
  </acceptance_criteria>
  <done>Every fan-out job is triaged with a cause and a disposition, no red is accepted, and the four requirements read Complete without any reading Validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local default branch -> shared remote | The one-way boundary in this phase; published history cannot be rewritten by convention |
| GitHub Actions runner -> repository code at the dispatched ref | 19 jobs execute repository code for the first time ever |
| CI result surface -> phase evidence | The checks UI colour is a lossy view; job-level JSON is the trustworthy form |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-141-21 | Spoofing | a green run that hides a cron-skipped or advisory job | high | mitigate | Job-level JSON with a per-job conclusion row; any still-skipped job is recorded as a finding with its reason, and a schedule guard is the only legitimate one |
| T-141-22 | Spoofing | reading CI against a stale remote | high | mitigate | The ahead-count against upstream is asserted zero before any CI read, and every run's head SHA is compared to the recorded local head |
| T-141-23 | Tampering | published default-branch history | critical | mitigate | Blocking decision checkpoint before the push; explicit prohibition on force-push, rewrite and rebase of published commits as a fix path |
| T-141-24 | Repudiation | a red silently re-accepted | high | mitigate | Every row needs a cause and a fixed-or-routed disposition, with a negative grep on the two disposition names the milestone bar deletes |
| T-141-25 | Information disclosure | dispatched workflow runs | low | accept | None of the three workflows references any secret; RESEARCH.md verified zero secret references across every workflow this phase touches |
| T-141-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager install tasks in this phase; RESEARCH.md's Package Legitimacy Audit records "Not applicable" |
</threat_model>

<verification>
- `git rev-list --count origin/master..master` returns 0 before any CI read.
- Three workflow runs exist at one shared head SHA equal to the recorded local head, and job-level
  JSON is captured for every job in all three.
- Every job appears in the triage table with a cause and a fixed-or-routed disposition; no red is
  accepted.
- `node scripts/validation/check-phase-54.mjs` exits 0 after the planning-document edits.
- `node scripts/validation/carve-gate.mjs` exits 0 — HARD BLOCK (CARVE D-10).
- GOV-02 ledger: a row is added only if the triage required an edit to a frozen-surface path
  (for example a raised cap in a workflow); if no such edit was needed, no row is added and the
  SUMMARY states that absence is correct per the ledger's own discipline rule.
</verification>

<success_criteria>
The 19 never-executed fan-out jobs have run at a known SHA with job-level evidence, every result
is triaged with a cause and a disposition and none is accepted, and the phase's four requirements
read Complete without any reading Validated.
</success_criteria>

<output>
Create `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-06-SUMMARY.md` when done
</output>
