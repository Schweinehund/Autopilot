---
phase: 141-standalone-red-validator-set-chain-members-green
plan: 02
type: execute
wave: 2
depends_on: ["141-01"]
files_modified:
  - scripts/validation/regenerate-supervision-pins.mjs
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: true
requirements: [RED-02, RED-03]

estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "node scripts/validation/regenerate-supervision-pins.mjs --self-test exits 0 and prints an identical-diff result."
    - "node scripts/validation/check-phase-48.mjs exits 0 standalone (no CHECK_PHASE_NESTED set) with a non-zero PASS count and 0 FAIL — the end-to-end tracer path from corpus content through classify() through the sidecar set-difference through the self-test to a chain member's exit code."
    - "git diff --stat scripts/validation/v1.7-audit-allowlist.json is empty — the v1.7 fixture is byte-unchanged (RED-02 clause 1)."
    - "The diff of regenerate-supervision-pins.mjs contains zero changed lines inside classify() (the function spanning :204-238 pre-edit) — the classifier is not relaxed and not widened (RED-02 clause 2, D-03)."
    - "The self-test's invariant is set equality after subtraction — expectedNewKeys (sidecar Tier-1 minus BASELINE_9) equals classifierNewKeys (live classifier Tier-1 minus BASELINE_9) — so when elements compare equal there is no output ordering to specify; equality is order-independent by construction."
    - "docs/admin-setup-android/03-fully-managed-cobo.md carries TWO Tier-1 occurrences today (51 and 53) where the old array tracked one (36); only 51 is carried forward, and 53 cancels because it is present in both sides of the subtraction. This is a 1-to-2 cardinality change, NOT a clean 9-of-9 bijection, and neither the plan nor the commit message claims one."
    - "A new dated BASELINE_9 audit-trail comment line exists; all four literals pinned live by V-60-09, V-66-03, V-68-06 and V-70-06 are still present verbatim and no pre-existing audit-trail comment line was edited."
    - { statement: "The classifier's set-difference behavior on an empty or single-element occurrence set is not exercised — the live corpus carries 26 occurrence lines across 32 files, and D-07 bars adding a synthetic fixture for the Tier-2 arm. No evidence for the empty/singleton case is produced in this phase.", verification: backstop }
  artifacts:
    - "scripts/validation/regenerate-supervision-pins.mjs — rebased BASELINE_9 array literal (nine coordinates) plus one appended dated audit-trail comment line"
    - ".planning/milestones/v1.20-GOV-02-LEDGER.md — one appended row for the regenerate-supervision-pins.mjs edit"
    - "A recorded pre-SWEEP-09-edit nested tally for check-phase-68 and check-phase-70, handed to Plan 03 and to Phase 142's RED-07 comparison (D-15)"
  key_links:
    - "BASELINE_9 array coordinates -> live doc line content -> v1.7-audit-allowlist.json supervision_exemptions[] — the set-subtraction invariant at regenerate-supervision-pins.mjs:559-582; break any leg and the self-test reports a non-identical diff"
    - "The literal comment `BASELINE_9 refreshed 2026-05-06 (Phase 60 Plan 08)` -> check-phase-60.mjs:177 (V-60-09), a LIVE readFile assertion inside a RED-03 member"
    - "The literals `BASELINE_10 refreshed` and `Phase 66` -> check-phase-66.mjs:127,130 (V-66-03), also a LIVE assertion inside a RED-03 member"
    - "The literals `BASELINE_11 refreshed` and `Phase 70` -> check-phase-70.mjs:179,182 (V-70-06); the literal `v1.7-audit-allowlist.json` -> check-phase-68.mjs:140 (V-68-06)"
    - "--self-test exit code -> check-phase-48.mjs:72, check-phase-60.mjs:188 (V-60-10), check-phase-61.mjs:385 (V-61-34) — the three call sites whose failure drove all eight members red"
  prohibitions:
    - "ZERO edits to docs/_glossary-android.md or any other glossary file. 365 pin coordinates across 16 frozen sidecars depend on _glossary-android.md's exact line positions; both glossaries also sit at exactly 90 days against a strictly-greater freshness test. Read-only."
    - "ZERO edits to classify() in regenerate-supervision-pins.mjs — D-03 withdrew the context-window change outright."
    - "ZERO byte changes to scripts/validation/v1.7-audit-allowlist.json."
    - "Never modify an existing BASELINE_N audit-trail comment line; append a new dated one only (D-04)."
    - "Do not add a tenth coordinate for 03-fully-managed-cobo.md:53 — the array's name and the CONTEXT.md target list both specify nine; adding it is harmless to the self-test but misleading."
    - "Do not add a synthetic negative fixture for the classifier's Tier-2 arm (D-07, dropped and backlogged)."
    - "Do not claim a clean 1:1 nine-coordinate rename in any plan text, comment, or commit message."
    - "A nested (CHECK_PHASE_NESTED=1) or apex run is evidence for NO part of SC#2 or SC#3 — check-phase-48's --self-test call site returns pass:true, skipped:true under nesting (D-21). The check-phase-48 verification in this plan MUST be a bare invocation."
---

<objective>
TRACER. Rebase the nine stale `BASELINE_9` coordinates in
`scripts/validation/regenerate-supervision-pins.mjs` to their live descendants and prove the
whole mechanism end to end on the cheapest possible path: corpus content -> `classify()` ->
sidecar set-difference -> `--self-test` exit 0 -> `check-phase-48` exit 0 standalone.

Purpose: every failing assertion in all eight RED-03 members traces to one defect — the
`--self-test`. All 9 of 9 `BASELINE_9` coordinates are dead (none lands on a line containing
`supervis`; four point at blank lines) because commit `aaf0d2ff` moved the sidecar without
touching the helper. This plan fixes the data, not the classifier, and validates the fix
against the 0.5-second member before the phase spends 22 minutes on the full sweep or touches
twelve reader sites.

Output: a rebased array, an appended dated audit-trail line, a GOV-02 ledger row, and the
pre-edit nested tallies Plan 03 and Phase 142 need.

## Artifacts this phase produces

Contributed by this plan:
- Rebased `BASELINE_9` array literal in `scripts/validation/regenerate-supervision-pins.mjs`.
- One new comment line `// BASELINE_9 refreshed 2026-08-07 (Phase 141 Plan 02): …`.
- One new row in `.planning/milestones/v1.20-GOV-02-LEDGER.md`.
- Recorded pre-SWEEP-09 nested tallies for `check-phase-68` and `check-phase-70` (SUMMARY only,
  no file).

The full phase-level artifact roll-up lives in `141-01-PLAN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md
@.planning/phases/141-standalone-red-validator-set-chain-members-green/141-RESEARCH.md
@.planning/phases/141-standalone-red-validator-set-chain-members-green/141-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — rebase BASELINE_9, prove --self-test exits 0 and check-phase-48 exits 0 standalone</name>
  <files>scripts/validation/regenerate-supervision-pins.mjs</files>
  <read_first>
    - `scripts/validation/regenerate-supervision-pins.mjs` lines 388-440 (the existing audit-trail comment block, the pattern to mirror), lines 520-545 (the last existing entry, the insertion point, and the `BASELINE_9` array at :533-543), and lines 545-628 (`doSelfTest()`, the set-difference at :559-582, the Phase-48-D-14 acceptance record at :597-602, the `unpinnedTier2` assertion at :603-610, the informational `pinnedTier2` branch at :611-615, and the two-branch FAIL string at :624-625).
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-RESEARCH.md` §"Exact BASELINE_9 target coordinates verified against live content" — the current stale array quoted verbatim, and the live content quoted at every one of the nine target coordinates.
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-RESEARCH.md` §"Exact literal strings the four live pins require (verbatim, quoted)" and Pitfall 1 (the cobo cardinality change) and Pitfall 3 (the classifier window is out of scope).
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-PATTERNS.md` §"The append-only audit-trail comment (D-04/D-05)".
    - `scripts/validation/v1.7-audit-allowlist.json` lines 10-24 (`supervision_exemptions[]`, including both `03-fully-managed-cobo.md` entries at 51 and 53) — READ ONLY, never edited.
  </read_first>
  <action>
Before editing, re-verify each of the nine target coordinates against live content rather than
trusting the research file (D-31: this phase's own draft produced four bad measured figures, and
a fifth survived a Finder). For each pair below, print the numbered line and confirm it contains
a `supervis` token: `docs/_glossary-android.md` 145, 147, 303, 333;
`docs/android-lifecycle/00-enrollment-overview.md` 65, 67, 97;
`docs/admin-setup-android/03-fully-managed-cobo.md` 51;
`docs/l2-runbooks/20-android-app-install-investigation.md` 33. Record the nine quoted lines in
the SUMMARY. If any coordinate does not carry the token, STOP and report rather than adjusting
by feel.

Replace the nine entries of the `BASELINE_9` array literal (currently `:533-543`) with exactly
those nine coordinates, preserving the existing array's formatting, entry order by file, and the
`['<path>', <line>]` tuple shape. Change nothing else inside the array declaration and nothing
below it.

Append one new dated audit-trail comment line immediately after the most recent existing entry
(the Phase 138 block ending just above `const BASELINE_9 = [`), mirroring the existing line style
(`// BASELINE_9 refreshed <date> (Phase <N> Plan <NN>): <what and why>`). Never edit an existing
comment line — four live validator assertions read literal substrings out of that block. The new
line must record, in this order: that the array's nine coordinates are rebased from their stale
pre-`aaf0d2ff` positions to their live descendants; that eight of the nine are clean lineage
rebases; that the ninth (`03-fully-managed-cobo.md`) is a content-side cardinality change from
one Tier-1 occurrence to two (51 and 53), of which only 51 is carried forward; and that this is
safe because the self-test's invariant is a set equality after subtraction in which a coordinate
present on both sides cancels — not because it is a lossless rename. Do not describe it as a
one-to-one mapping.

Do not touch `classify()`. Do not touch `scripts/validation/v1.7-audit-allowlist.json`. Do not
touch any file under `docs/`.

Then run the tracer end to end, in this order, on a quiesced machine (see the quiesce rule in
`<verify>`): first `node scripts/validation/regenerate-supervision-pins.mjs --self-test`, then a
BARE `node scripts/validation/check-phase-48.mjs` with `CHECK_PHASE_NESTED` unset. Record both
exit codes and both full result lines. `check-phase-48` was 6 PASS / 1 FAIL at phase entry;
record the actual post-fix tally rather than assuming 7/0.

Commit as `fix(141-02): rebase BASELINE_9 to live descendants — self-test exits 0, check-phase-48
green` with a body naming D-01/D-02 and the cobo cardinality caveat.
  </action>
  <verify>
    <automated>node scripts/validation/regenerate-supervision-pins.mjs --self-test</automated>
    <automated>node scripts/validation/check-phase-48.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Before either timing-sensitive or pass/fail measurement, stray validator processes are killed and the count is asserted: on Windows, `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -like '*check-phase*' }` returns nothing. A bare `Get-Process node` count of 0 is NOT the test — this machine carries permanent non-validator node residents (D-31/D-23).
    - `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0 and its output reports an identical diff. Record the two set sizes it prints.
    - `node scripts/validation/check-phase-48.mjs` (bare, `CHECK_PHASE_NESTED` unset) exits 0 with 0 FAIL and a PASS count greater than 0 — a zero-check run would also exit 0 via `process.exit(failed > 0 ? 1 : 0)`, so a non-zero PASS count is required, not just the exit code.
    - `git diff --stat scripts/validation/v1.7-audit-allowlist.json` produces no output.
    - `git diff --stat docs/` produces no output.
    - `git diff -U0 scripts/validation/regenerate-supervision-pins.mjs` shows changed hunks ONLY inside the `BASELINE_9` array literal and at the single appended comment line; no hunk falls within the pre-edit `classify()` span (lines 204-238). Record the hunk headers in the SUMMARY.
    - `git diff scripts/validation/regenerate-supervision-pins.mjs | grep "^-" | grep -c "BASELINE_9 refreshed"` returns 0 — no existing audit-trail comment line was removed or rewritten.
    - `grep -c "BASELINE_9 refreshed 2026-05-06 (Phase 60 Plan 08)" scripts/validation/regenerate-supervision-pins.mjs` returns 1 (V-60-09's literal).
    - `grep -c "BASELINE_10 refreshed" scripts/validation/regenerate-supervision-pins.mjs` and `grep -c "Phase 66" scripts/validation/regenerate-supervision-pins.mjs` each return at least 1 (V-66-03's two literals).
    - `grep -c "BASELINE_11 refreshed" scripts/validation/regenerate-supervision-pins.mjs` and `grep -c "Phase 70" scripts/validation/regenerate-supervision-pins.mjs` each return at least 1 (V-70-06's two literals).
    - `grep -c "v1.7-audit-allowlist.json" scripts/validation/regenerate-supervision-pins.mjs` returns at least 1 (V-68-06's literal).
    - The `BASELINE_9` array contains exactly 9 tuples: `sed -n '/^const BASELINE_9 = \[/,/^\];/p' scripts/validation/regenerate-supervision-pins.mjs | grep -c "^  \['"` returns 9.
    - Neither the new comment line nor the commit message contains a claim of a one-to-one or bijective nine-coordinate rename.
  </acceptance_criteria>
  <done>BASELINE_9 rebased, --self-test exits 0, bare check-phase-48 exits 0 with 0 FAIL, fixture and classifier and corpus all byte-unchanged, all five pinned literals intact.</done>
</task>

<task type="auto">
  <name>Task 2: Blast-radius verification of the four live pins, chain-member baselines, and the GOV-02 ledger row</name>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` lines 1-24 (discipline, row schema, and rows 2-3 which are the closest analog: a shared-library edit with a pre/post importer baseline).
    - `scripts/validation/check-phase-60.mjs` lines 170-200 (V-60-09's live `readFile` pin at :177 and the NESTED skip at :188).
    - `scripts/validation/check-phase-66.mjs` lines 120-135 (V-66-03's two literal checks).
    - `scripts/validation/check-phase-68.mjs` lines 135-150 (V-68-06) and `scripts/validation/check-phase-70.mjs` lines 175-190 (V-70-06).
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md` D-04, D-15, D-21, D-22, D-23, D-31, D-32.
  </read_first>
  <action>
Run the GOV-02 target-scoped grep set for this edit and record every hit count. Path-literal
grep: `regenerate-supervision-pins.mjs` across `scripts/`, `.github/`, and `.planning/`. Symbol
grep: `BASELINE_9`, `BASELINE_10`, `BASELINE_11`, and `--self-test` across `scripts/validation/`.
CARVE D-12 requires the path-string form, not only the symbol form — a symbol-only grep misses
call sites that pin a path literal.

Then run the blast-radius regression set on a quiesced machine, serially, one validator at a
time, and declare the cache state (warm or cold) for every timing figure recorded:
- BARE `node scripts/validation/check-phase-60.mjs` (V-60-09 and V-60-10 and V-60-12; entry
  state was 23 PASS / 2 FAIL, expected to clear to 0 FAIL — record the actual tally and the wall
  clock, and note the measured cold-versus-warm spread of 63.0 s cold against 9.3-12.4 s warm).
- BARE `node scripts/validation/check-phase-61.mjs` (entry state 31 PASS / 3 FAIL, expected 0
  FAIL — record the actual tally; this is also Plan 03's pre-edit baseline for `check-phase-61`).
- `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-68.mjs` and `CHECK_PHASE_NESTED=1
  node scripts/validation/check-phase-70.mjs` — confirm V-68-06 and V-70-06 PASS and record each
  full PASS/FAIL/SKIPPED tally. These two nested tallies are the declared pre-SWEEP-09-edit
  baseline that Plan 03 compares against and that Phase 142's RED-07 comparison is taken from
  (D-15). Record the mode explicitly as nested in the SUMMARY.
- Do NOT run a bare `check-phase-66` here: it costs about 665 000 ms standalone and belongs to
  Plan 05's single budgeted sweep. Do NOT run a bare `check-phase-68` here for the same reason —
  Plan 04 owns that measurement.
- Record explicitly that no bare `check-phase-68` tally is taken in this plan and why (the
  non-nested chain expansion through 48-66 is exponential per D-32); the phase-entry bare figure
  of 24 PASS / 9 FAIL is already on record in GOV-02 ledger row 4.

Write the SUMMARY's measurement section with the cache state, the quiesce assertion output, and
an explicit statement that every figure was taken from a serial, single-validator-at-a-time run —
never two validators concurrently.

Append one GOV-02 ledger row for the `regenerate-supervision-pins.mjs` edit using the file's
schema (File, Grep command, Hit count, Regression gate run, Result, Plan). Model the Result cell
on row 6's style: explicit hit counts, explicit before-and-after tallies, and an explicit
"no frozen call-site conflict found" sentence if that is what the greps showed.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-60.mjs</automated>
    <automated>node scripts/validation/check-phase-61.mjs</automated>
    <automated>node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The quiesce assertion (validator-process count scoped to `check-phase*` command lines) is captured and recorded as zero before the first timed run.
    - Bare `node scripts/validation/check-phase-60.mjs` exits 0 with 0 FAIL and a non-zero PASS count; V-60-09, V-60-10 and V-60-12 are individually recorded as PASS.
    - Bare `node scripts/validation/check-phase-61.mjs` exits 0 with 0 FAIL and a non-zero PASS count; V-61-21, V-61-32 and V-61-34 are individually recorded as PASS.
    - `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-68.mjs` exits 0 with V-68-06 PASS; the full PASS/FAIL/SKIPPED tally is recorded and labelled as the nested pre-SWEEP-09 baseline.
    - `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-70.mjs` exits 0 with V-70-06 PASS; tally recorded and labelled the same way.
    - Every recorded wall-clock figure carries a stated cache state (warm or cold) and a stated serial-execution note.
    - `node scripts/validation/carve-gate.mjs` exits 0 — `regenerate-supervision-pins.mjs` is CARVE Category 6, already on-list, so no D-09 amendment was needed for it. State this explicitly in the SUMMARY per D-06; the draft never did.
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` gained exactly one appended row; `git diff -- .planning/milestones/v1.20-GOV-02-LEDGER.md | grep -c "^-"` returns 0.
    - The SUMMARY states that no nested or apex run in this plan is offered as evidence for SC#2 or SC#3, and that the two nested tallies above are declared nested precisely so they cannot be mistaken for standalone evidence.
  </acceptance_criteria>
  <done>Four live pins verified intact by direct validator runs, check-phase-60 and check-phase-61 green standalone, nested pre-edit baselines recorded for 68 and 70, ledger row appended, carve-gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/` corpus content -> `classify()` -> `BASELINE_9` coordinates | Untrusted-in-the-sense-of-drifting input: corpus line numbers move under the pin array, which is exactly the defect being repaired |
| `regenerate-supervision-pins.mjs` source text -> four LIVE `readFile` assertions in check-phase-60/66/68/70 | An edit to this file's comment block is read at HEAD by four validators, two of them RED-03 members — the fix's blast radius runs through the chain it is greening |
| Validator subprocess spawn -> parent exit code | `execFileSync` timeouts and swallowed causes can convert a real failure into a false pass or a false red |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-141-04 | Tampering | `scripts/validation/v1.7-audit-allowlist.json` | high | mitigate | RED-02 forbids editing the fixture; acceptance criterion asserts `git diff --stat` on it is empty, and the fix lives in the helper, not the sidecar |
| T-141-05 | Tampering | `docs/_glossary-android.md` and every glossary | critical | mitigate | Read-only; `git diff --stat docs/` asserted empty. 365 pin coordinates across 16 frozen sidecars depend on line positions, and both glossaries sit at exactly 90 days against a strictly-greater freshness test |
| T-141-06 | Repudiation | `BASELINE_N refreshed` audit-trail comment block | medium | mitigate | Append-only; negative-grep asserts no existing `BASELINE_9 refreshed` line was removed. D-05 records that these comments are asserted for existence only, never accuracy — 18 of them sat above a 9-of-9-dead array |
| T-141-07 | Spoofing | a nested or apex validator run presented as standalone evidence | high | mitigate | Every nested run in this plan is explicitly labelled nested; the tracer's `check-phase-48` verification is a bare invocation with `CHECK_PHASE_NESTED` unset |
| T-141-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager install tasks in this phase; RESEARCH.md's Package Legitimacy Audit records "Not applicable" |
</threat_model>

<verification>
- `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0.
- Bare `node scripts/validation/check-phase-48.mjs`, `check-phase-60.mjs`, `check-phase-61.mjs`
  each exit 0 with 0 FAIL and a non-zero PASS count.
- `node scripts/validation/carve-gate.mjs` exits 0 — HARD BLOCK (CARVE D-10).
- GOV-02 ledger: exactly one row appended for `scripts/validation/regenerate-supervision-pins.mjs`,
  carrying the target-scoped path-literal grep AND the symbol grep, with hit counts.
- `git diff --stat` empty for `scripts/validation/v1.7-audit-allowlist.json` and for `docs/`.
</verification>

<success_criteria>
The self-test root cause is gone at the data layer with the classifier and fixture untouched, and
the cheapest chain member proves it end to end standalone in under a second.
</success_criteria>

<output>
Create `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-02-SUMMARY.md` when done
</output>
