---
phase: 148-application-update-management-winget-routing
plan: 02
type: execute
wave: 2
depends_on: ["148-01"]
files_modified:
  - docs/operations/patch-management/08-windows-app-updates.md
  - docs/operations/patch-management/07-windows-autopatch.md
autonomous: true
requirements: [APP-03, APP-04, APP-05, APP-06]

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "docs/operations/patch-management/08-windows-app-updates.md exists and its intro states why an application-update guide lives in patch-management/ rather than app-lifecycle/ — it is the update-governance view of applications, the sibling of 01 through 07, not the deployment view (APP-03, D-36)."
    - "08 states the corrected seam explicitly: app-lifecycle/ owns the object model (app types, packaging, supersedence graphs, dependency chains) and 08 owns update governance (which mechanism, what cadence, what it cannot do); and no file under docs/operations/app-lifecycle/ is edited (D-33, D-35)."
    - "All six Microsoft 365 Apps update channels ship — Current as the default with rollback stated as Not applicable, Current Preview, Monthly Enterprise, Semi-Annual Enterprise, Semi-Annual Enterprise Preview, and Beta as not supported and test-environments-only (SC#3, APP-03, D-26)."
    - "The three structural channel constraints ship: one channel per device, device-scoped rather than user-following, and Teams and OneDrive explicitly outside these channels (SC#3, APP-03, D-26)."
    - "The one-channel-per-device rule is written as the multi-product case it actually is: installing Microsoft 365 Apps plus the subscription Project and Visio desktop apps on one device forces all of them onto the same channel, and a mix on one device is not possible (APP-03, edge probe APP-03/concurrency)."
    - "The channel-change latency is stated rather than implied: the verification registry key updates when the Office automatic-updates scheduled task runs or when a user signs in, and can take at least a day and more, so re-applying a channel setting is not an immediate or self-confirming operation (APP-03, edge probe APP-03/idempotency)."
    - "## Setting the Channel from Intune ships and names the settings catalog as the current surface with the settings Update Channel and Target Version and the Policy and Profile Manager role floor, while recording that the Administrative Templates route remains documented with the distinctly-named Update Channel (2.0) setting (APP-03, D-02 as resolved by RESEARCH R-1 and Open Question 1)."
    - "The post-July-2026 Semi-Annual Enterprise Channel cadence ships as a documented conflict and never as a resolved fact: the announcement is quoted with its own date and its own future tense, the comparison table's surviving twice-yearly description is quoted alongside it, and the guide states that neither reading is supportable from the pages (SC#3, APP-03, D-27, RESEARCH Open Question 2)."
    - "The guide gives the reader the one checkable discriminator the source published — the build-number threshold that indicates the July release installed — plus the dated escape hatch for the prior version, and writes no countdown and no deadline row (APP-03, D-27)."
    - "The Autopatch-enrolled condition is stated in the channels section and links 07-windows-autopatch.md, which owns Autopatch's Edge and Teams coverage; 08 does not re-author it (APP-03, D-37)."
    - "Enterprise App Management's five reachability gates are stated as hard as its licence: an add-on subscription purchasable standalone or as part of the Intune Suite, Windows Win32 exe and msi only, Microsoft-hosted and management-extension-installed, Public plus GCC High plus DoD clouds only, and auto-update requiring a Required assignment (SC#3, APP-04, D-28)."
    - "The unentitled case is stated: the capability requires a subscription in addition to Intune Plan 1 or Plan 2, and the product does not itself perform a licence check on catalog apps — so absence of the entitlement is a procurement fact, not a product-enforced gate (APP-04, edge probe APP-04/empty)."
    - "All eight Enterprise App Management limitations ship as bold sub-labels using the source page's own heading text, in the source page's own order, including malicious version revocation with the statement that the administrator remains responsible for identifying impacted devices, and including version changes during device processing (SC#3, APP-04, D-29, edge probe APP-04/ordering)."
    - "The interrupted and parallel cases are both carried as their own sub-labels: version changes during device processing, and conflicts with other app types targeting the same app (APP-04, edge probe APP-04/concurrency)."
    - "The ESP and device-preparation positive is written as a trade-off under ## Enterprise App Management and not under the callouts H2 — catalog apps can be blocking apps; only the auto-update ones cannot — and it cross-links the two files that already own blocking-app configuration (SC#3, APP-04, D-30, edge probe APP-04/adjacency)."
    - "The published objectives are described as the source describes them — targets and guidelines, explicitly not guarantees — and never as guarantees or as an agreement (APP-04, D-28)."
    - "The name is shown attaching to three genuinely different things — the Intune Microsoft Store app (new) app type, the operating-system App Installer component and its command-line tool, and the developer-environment configuration feature — and the three are never merged (SC#4, APP-05, D-40, edge probe APP-05/adjacency)."
    - "The Microsoft Store app update model ships split, both halves quoted: UWP apps kept current by the Store with or without an Intune assignment, and Win32 Store apps kept current by Intune and therefore requiring an assignment (SC#4, APP-05, D-31, edge probe APP-05/idempotency)."
    - "The suppression asymmetry ships: the Store auto-update block stops the Store's UWP updating, while a Win32 Store app with an active Intune assignment is still automatically updated — so removing the assignment is the only lever for the Win32 half (SC#4, APP-05, D-31, edge probe APP-05/concurrency)."
    - "Win32 Store apps are documented as currently in preview and ARM64 installers as unsupported, both re-verified against the live page during this plan rather than inherited (SC#4, APP-05, RESEARCH Open Question 3 and Assumption A5)."
    - "The developer-environment configuration feature's page facts were re-fetched from that page's own bytes during this plan — canonical URL confirmed, ms.date and rendered update date recorded, each quote pinned to the heading it sits under — before the sentence quoting them was authored, by the same method plan 01's Task 1 used on the six APP-01 items; the sentence carries its own dated Source line, and any carried claim the live page does not confirm ships as an explicit not-found line rather than as silence (APP-05, D-40, D-59, D-61, RESEARCH PREMISE ledger)."
    - "The absence of any published cadence, check interval, schedule, service objective or version pinning for the Store app type is stated as a deliverable and contrasted against the published-but-non-binding Enterprise App Management objectives; no number is invented and none is borrowed from the management extension or from the catalog (SC#4, APP-05, D-32, edge probe APP-05/empty)."
    - "The verbatim FAQ negative ships exactly as written — that Enterprise App Management does not use the package manager, and that catalog apps are directly installed by the Intune management extension — with no extension, no strengthening paraphrase, and no date or rationale the source did not give (SC#4, APP-05, D-39)."
    - "The device-preparation reconciliation ships in the routing section with its device-preparation scope intact: the app-type eligibility filter is a device-preparation filter on the Intune Microsoft Store app (new) app type, not a statement that the command-line tool is an Intune-managed patching surface; and none of the three corpus sites carrying the conflation is edited (SC#4, APP-05, D-43, D-44)."
    - "The system-context root cause is documented first-party: the package manager is a per-user-registered component whose availability depends on an interactive first sign-in and an asynchronous Store registration, so every context where Intune runs code with no signed-in user is a context where it may not be registered and resolvable — and the source's administrator-considerations section covering only elevation is recorded as a documented silence on unattended execution (APP-05, D-41)."
    - "The four-step mechanism hierarchy ships in its source order as a decision list, never inside a code fence, and links app-lifecycle/01#supersedence only in the Win32 fallback tier where supersedence is genuinely the mechanism (APP-03/04/05, D-34, D-48, edge probe APP-05/ordering)."
    - "08 states explicitly that Enterprise App Management auto-update is not supersedence — the source says it uses no new app object and no supersedence relationship — and no auto-update sentence links the supersedence anchor (APP-04, D-34)."
    - "The DesktopAppInstaller policy table ships exactly six rows and is framed as control, not patching, with a link-not-copy pointer to the full reference rather than a transcription of all fifteen settings (SC#4, APP-06, D-45)."
    - "SourceAutoUpdateInterval ships as a table row AND as a bold sub-label expansion beneath it, carrying the disqualifying detail: the package index is not updated in the background and refreshes only when a user invokes a command and the interval has passed, and the setting has no impact on REST-based sources (APP-06, D-42, edge probe APP-06/idempotency)."
    - "The settings-catalog trap closes the hardening section as a consequence: the two policies the Store-app deployment article itself recommends enabling are flagged as not built in and requiring a custom configuration profile, so an administrator hardening Windows through the settings catalog can silently degrade a supported Intune app type using a lever Intune never surfaces (SC#4, APP-06, D-46, edge probe APP-06/concurrency)."
    - "Private package-manager REST sources are named as out of scope in one sentence, noting that the additional-source and allowed-source policies can point at a source Intune cannot see, and the compliance-scripting registry key ships as inline code (APP-06, D-45)."
    - "08 is 10 H2s, 8 own-line <a id> anchors and 0 code fences, and 07's Related Resources gains exactly one entry for 08 so the sibling pair is mutually reachable with zero dangling links at every commit boundary (D-25, D-48, D-65, D-70)."
    - "node scripts/validation/check-nav-hub-links.mjs reports 0 hub-presence failures and 0 corpus-link failures after both files are final."
    - "node scripts/validation/c17-eee-contract.mjs still reports 234 files checked, 0 with violations — the count does not become 235 or 236 (D-70)."
    - "node scripts/validation/check-phase-144.mjs still reports 101 PASS, 0 FAIL, 0 SKIPPED after the commit (D-67)."
    - "The phase verifier re-fetches and diffs, and this plan names its list — the seven pages 08 quotes, including the developer-environment configuration page, with the three volatile ones flagged; no validator parses evidence lines, so the verifier is the only gate that can catch a fabricated or stale quote (D-62)."
    - "A remediation round is inside this plan's budget rather than a plan failure: neither predecessor phase finished in the number of plans it started with (D-66)."
    - statement: "Enterprise App Management auto-update run repeatedly against the same catalog app is not additive to the tenant's app inventory — the source states it detects a newer catalog version and updates targeted devices with no new app object and no supersedence relationship — and 08's prose does not lead a reader to expect a new app object, a version history or a supersedence chain to accumulate."
      verification: backstop
    - statement: "No sentence in 08 lets the three referents of the package-manager name be read as the same thing: every occurrence carries a disambiguating qualifier naming which of the three it means, including inside quoted material where the surrounding prose supplies the qualifier."
      verification: backstop
  prohibitions:
    - requirement_id: APP-05
      category: transparency
      statement: "MUST NOT extend, paraphrase into something stronger, or attach a date or a rationale to the vendor FAQ negative about whether the catalog uses the package manager. Ship it verbatim and stop; this is the strongest anti-fabrication instruction in the evidence base."
    - requirement_id: APP-04
      category: transparency
      statement: "MUST NOT describe the published catalog objectives as guarantees or as a service level agreement; the source itself states they are guidelines, not guarantees, and that no agreement or timeline is offered for adding an app to the catalog."
    - requirement_id: APP-03
      category: transparency
      statement: "MUST NOT convert the Semi-Annual Enterprise Channel announcement's future tense to past tense, MUST NOT write a countdown or a deadline table row, and MUST NOT pick a side between the announcement and the comparison table."
    - requirement_id: APP-05
      category: transparency
      statement: "MUST NOT assert that the Intune Microsoft Store app (new) type is implemented on the package-manager client or on its store source, and MUST NOT assert any repair or re-registration command as a supported unattended or system-context path — the first is unverified and the second is documented for a sandbox scenario only."
    - requirement_id: APP-05
      category: values
      statement: "MUST NOT name any third-party patch-management product, community automation project or OEM vendor, and MUST NOT cite the package-manager client repository's GitHub issue — it is third-party, its unanswered framing is stale, and the first-party sourced absence carries the same conclusion without the dependency."
    - requirement_id: APP-03
      category: transparency
      statement: "MUST NOT invent, estimate or borrow a cadence, check interval, schedule or service objective for the Microsoft Store app type; the total absence of one is itself the deliverable and must be stated as an absence."
  artifacts:
    - docs/operations/patch-management/08-windows-app-updates.md
  key_links:
    - "The eight own-line <a id></a> anchors in 08-windows-app-updates.md — the other half of the 16-anchor cross-link contract Phase 151's Recipe #5 consumes (D-74)."
    - "08-windows-app-updates.md -> 07-windows-autopatch.md — the seam link for the Autopatch-enrolled channel condition; 07 owns the Edge and Teams workload coverage (D-37)."
    - "07-windows-autopatch.md -> 08-windows-app-updates.md — one new Related Resources entry, added by this plan's final task once the target exists, so no commit in this phase lands a dangling link (D-65)."
    - "08-windows-app-updates.md -> ../app-lifecycle/01-windows-win32-msix-scale.md#supersedence — anchor confirmed live at 01:30; linked ONLY from the Win32 fallback tier, never from an auto-update sentence (D-34)."
    - "08-windows-app-updates.md -> ../../reference/win32-app-packaging.md and ../../admin-setup-apv1/03-esp-policy.md — the two files that already own blocking-app configuration (D-30)."
    - "The H1 '# Windows Application Updates' — Phase 152's registry Title input; measured zero colliding Titles across the 225 registry rows at plan time (D-71). No registry row is created in this phase."
---

<objective>
Author `docs/operations/patch-management/08-windows-app-updates.md` and land it as **commit 2 of three**
(D-65). This is four requirements in one file and half of the phase: the Microsoft 365 Apps channel
model, Enterprise App Management with its reachability gates stated as hard as its licence, the
Microsoft Store app update split, and the package-manager routing-and-hardening section that is
deliberately not a patching guide.

Purpose: APP-03, APP-04, APP-05 and APP-06, and ROADMAP Success Criteria 3 and 4.

Output: one new file, plus **one** added Related Resources entry in `07-windows-autopatch.md` so the
sibling pair is mutually reachable. **No** registry row, filename-map row, canary bump, ops-index row
or nav-hub wiring (Phase 152, D-73). **No** `00-overview.md` edit (plan 03). **No** edit to
`docs/operations/app-lifecycle/**`, `docs/admin-setup-apv2/**`, `docs/reference/macos-capability-matrix.md`
or `docs/operations/co-management/**`.

**Two research findings change what this plan builds, and both are already settled — do not
re-litigate them.** (1) The conditional H2 `## Setting the Channel from Intune` **ships**: the fetch
that CONTEXT graded as likely-to-fail succeeded, the article is canonical under the settings-catalog
path, and `08` is therefore 10 H2s and 8 anchors, taking the phase to **16 anchors, not 15**. (2) The
package-manager client repository issue has a collaborator reply, so its unanswered framing is stale;
**do not cite the issue at all**.

`148-RESEARCH.md` carries a byte-verified quote bank for every one of this plan's four requirements.
Copy from it. Do not recall, and do not re-derive a citation it already holds verbatim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-application-update-management-winget-routing/148-CONTEXT.md
@.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md
@.planning/phases/148-application-update-management-winget-routing/148-PATTERNS.md
@.planning/phases/148-application-update-management-winget-routing/148-01-SUMMARY.md
</context>

<phase_wide_rules>

R1 through R12 from `148-01-PLAN.md` bind this plan unchanged. Re-read them there rather than acting
on a paraphrase. The additions and overrides below are specific to `08`.

**R1a — Reuse plan 01's execution date.** `08`'s `last_verified` is the same date `07` shipped with,
and `review_by` is that date plus 60 by arithmetic. All three commits in this phase carry one date
(D-76). The date is recorded in `148-01-SUMMARY.md`.

**R5a — Banned strings in `08`, each with a matching `== 0` acceptance criterion.** The R5 list from
plan 01 applies in full, plus: `winget-cli`; `PatchMyPC`, `Chocolatey`, `Ninite`, `Scappman`,
`Robopack`; `Service Level Agreement` used to describe the catalog's own objectives; `msstore` asserted
as the Store app type's implementation; `Repair-WinGetPackageManager` presented as a supported
unattended path.
<!-- planner-discipline-allow: SCCM, System Center, Autopatch rings, doc_id, Version History, PITFALL-, enrolment, licence, behaviour, TBD, TODO, FIXME, PLACEHOLDER, winget-cli, PatchMyPC, Chocolatey, Ninite, Scappman, Robopack, msstore, Repair-WinGetPackageManager -->

**R13 — The heaviest Source-line load in the milestone.** This file quotes at least seven distinct Learn
pages: the channels overview, the channel-unification article, the settings-catalog article, the
Enterprise App Management article, the Store-apps article, the policy CSP reference, and the
developer-environment configuration page fetched by Task 4 step 2. R7's
one-Source-line-per-contiguous-blockquote-per-page rule is the single most likely thing to break here.
Check the scope of every Source line individually. A Source line covering a list whose items come from
two pages is the exact defect `146-REVIEW.md` WR-01 found.

**R14 — The two never-assert rules.** Neither of these may be stated in any form: that the Intune
Microsoft Store app (new) type is implemented on the package-manager client or its store source; and
that a repair or re-registration command is a supported system-context path (the source documents it
for a sandbox scenario only). Both are recorded as unverified. Where the guide would naturally reach
for either, write the documented silence instead.

**R15 — Two decoy classes measured live on this plan's own pages.** On the Store-apps article, every
mention of the package manager is a **policy carve-out saying the command-line tool escapes Store
lockdown** — a naive term grep on that page returns exactly the hits that invite the conclusion APP-05
bars. Pin every quote by its surrounding heading, never by grep hit. On the channels overview, the
page contradicts itself in four places; quote both sides with their headings and settle nothing.

**R16 — Volatility watch.** Two of this plan's source pages are recorded as time-sensitive: the
channels overview and the channel-unification article (a revision would dissolve the documented
conflict), and the Store-apps article (a preview exit would break a Success Criterion clause). Task 4
re-checks the preview status against the live page before writing it. If the Store-apps article has
left preview, **stop and surface it** — SC#4 and the requirement text both require the preview
wording, and a silent rewrite would put the guide out of contract with the ROADMAP.

**The H2 skeleton (D-25), exact order.** The four package-manager subsection names map one-for-one onto
the research file's prescribed subsections and are **not** discretionary;
`## Unsupported and Anti-Feature Callouts` is the pinned corpus literal, copied verbatim.

```
## Choosing a Windows App Update Mechanism
## Microsoft 365 Apps Update Channels
## Setting the Channel from Intune
## Enterprise App Management
## Microsoft Store Apps
## Enterprise App Management, Store Apps and WinGet — Routing
## Controlling WinGet on Managed Devices
## Unsupported and Anti-Feature Callouts
## Related Resources
## External References
```

**The eight anchor ids — the other half of the 16-anchor Phase-151 contract (D-70, D-74).** One
own-line `<a id="..."></a>` immediately above each of the first eight H2s; none above the two footer
H2s. Short hand-authored ids, matching the sibling convention rather than the literal heading slug:

`choosing-an-app-update-mechanism` · `m365-apps-update-channels` · `setting-the-channel-from-intune` ·
`enterprise-app-management` · `microsoft-store-apps` · `eam-store-winget-routing` ·
`controlling-winget` · `unsupported-callouts`

</phase_wide_rules>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end slice — 08 exists, the seam is stated, the mechanism hierarchy ships, every link and anchor resolves</name>

  <precondition>`docs/operations/patch-management/07-windows-autopatch.md` exists and `check-nav-hub-links` reports 0/0 (plan 148-01 complete).</precondition>

  <files>docs/operations/patch-management/08-windows-app-updates.md</files>

  <read_first>
    - `docs/operations/patch-management/07-windows-autopatch.md` as shipped by plan 01 — the immediate
      sibling; copy its frontmatter field set, its applicability-blockquote shape, its anchor
      convention and its footer shapes rather than re-deriving them from `06`.
    - `docs/operations/patch-management/06-windows-driver-firmware-updates.md` `## Related Resources`
      and `## External References` — the canonical footer shape if `07` diverged.
    - `docs/operations/app-lifecycle/00-overview.md` — the lifecycle-state taxonomy, which already
      publishes `update` as a lifecycle state and already states that all four platforms expose install
      and update. `08`'s seam sentence must not re-assign a concept this file already assigned.
      **Read only; never edited (D-35).**
    - `docs/operations/app-lifecycle/01-windows-win32-msix-scale.md` — confirm by
      `grep -n '<a id='` that the `supersedence` anchor exists, and read the passage documenting
      Available-assigned supersedence auto-updating on the management-extension check-in timer. That
      passage is why the draft seam was falsified. **Read only.**
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md`
      §`Architectural Responsibility Map` — the row-by-row ownership table this file must not
      contradict.
    - `.planning/research/WINGET-GAP.md` §4 — the four-step hierarchy, supplied there inside a code
      fence. It ships here as a decision list.
  </read_first>

  <action>
Create `docs/operations/patch-management/08-windows-app-updates.md` as a complete, structurally final
document with the first body section written at shipping quality.

Author, in order:

1. **Frontmatter**, five fields in `07`'s order, `platform: Windows`, `applies_to: all`,
   `audience: admin`, `last_verified` equal to the date recorded in `148-01-SUMMARY.md`, `review_by`
   equal to that plus 60 by arithmetic. No `doc_id`.
2. **The full-lexicon platform-applicability blockquote**, naming this guide's scope — application
   update governance on Windows: mechanism choice, the Microsoft 365 Apps channel model, the enterprise
   catalog, the Store app type, and the package-manager control surface — and routing to
   `00-overview.md` for the cross-platform overview and to `07-windows-autopatch.md` for the Autopatch
   service.
3. **The H1** `# Windows Application Updates`.
4. **An opening paragraph carrying two seam statements**, each naming the file it defers to.
   First: why this guide lives in `patch-management/` rather than `app-lifecycle/` — it is the
   update-governance view of applications, the sibling of the OS, driver and Autopatch guides, not the
   deployment view. Without that sentence a reader who knows the other directory exists will assume one
   of the two is stale. Second: the ownership split — the other directory owns the object model (app
   types, packaging, supersedence graphs, dependency chains, the content-prep tooling) and this guide
   owns update governance (which mechanism, what cadence, what it cannot do).
5. **All ten H2s from the skeleton, in order, each of the first eight preceded by its own-line anchor**
   from the id list in `<phase_wide_rules>`. Sections 2 through 8 may carry a single holding sentence
   naming what the section will carry — a plain declarative sentence, never a token from the banned
   list.
6. **`## Choosing a Windows App Update Mechanism`, written in full.** Render the four-step hierarchy as
   a decision list in its source order — never a code fence; all seven existing files in this directory
   carry zero fences. Each tier states which mechanism applies, what it requires, and which section of
   this guide covers it. Link `../app-lifecycle/01-windows-win32-msix-scale.md#supersedence` **only**
   from the Win32 fallback tier, where supersedence genuinely is the mechanism. State in that same tier
   that the enterprise catalog's auto-update is **not** supersedence — the source says it uses no new
   app object and no supersedence relationship — so a reader is not sent to a mechanism the vendor
   rules out.
7. **`## Related Resources`** with entries for `00-overview.md`, `07-windows-autopatch.md`,
   `../app-lifecycle/00-overview.md`, `../app-lifecycle/01-windows-win32-msix-scale.md`,
   `../../reference/win32-app-packaging.md` and `../../admin-setup-apv1/03-esp-policy.md`. Keep every
   `[text](url)` construct entirely on one physical line.
8. **`## External References`** with the first-party Learn URLs this file cites.

Write the file LF, with no code fences, and with American spelling throughout.
  </action>

  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs && node scripts/validation/v1.20-milestone-audit.mjs | tail -3 && node scripts/validation/c17-eee-contract.mjs | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - `docs/operations/patch-management/08-windows-app-updates.md` exists.
    - `grep -c '^## ' docs/operations/patch-management/08-windows-app-updates.md` returns exactly `10`.
    - `grep -c '<a id=' docs/operations/patch-management/08-windows-app-updates.md` returns exactly `8`.
    - `grep -c '^```' docs/operations/patch-management/08-windows-app-updates.md` returns exactly `0`.
    - `grep -c 'doc_id' docs/operations/patch-management/08-windows-app-updates.md` returns `0`.
    - The file contains the literals `# Windows Application Updates`, `> **Platform applicability:**`,
      `## Setting the Channel from Intune`,
      `## Enterprise App Management, Store Apps and WinGet — Routing`,
      `## Controlling WinGet on Managed Devices`, `## Unsupported and Anti-Feature Callouts`,
      `07-windows-autopatch.md` and `01-windows-win32-msix-scale.md#supersedence`.
    - All eight anchor ids appear exactly once each:
      `for a in choosing-an-app-update-mechanism m365-apps-update-channels setting-the-channel-from-intune enterprise-app-management microsoft-store-apps eam-store-winget-routing controlling-winget unsupported-callouts; do grep -c "<a id=\"$a\">" docs/operations/patch-management/08-windows-app-updates.md; done`
      prints `1` eight times.
    - The seam is literally present: the intro contains both the string `app-lifecycle` and a sentence
      naming update governance as this guide's scope.
    - Per D-38, the package-manager material is **four** subsections, not two — the file contains all
      four of `## Choosing a Windows App Update Mechanism`, `## Microsoft Store Apps`,
      `## Enterprise App Management, Store Apps and WinGet — Routing` and
      `## Controlling WinGet on Managed Devices`, each mapping one-for-one onto a prescribed research
      subsection. The two requirements they serve are half of this file, not a third of the phase.
    - `awk '/^## Choosing a Windows App Update Mechanism/,/^## Microsoft 365 Apps Update Channels/' docs/operations/patch-management/08-windows-app-updates.md | grep -c 'supersedence'`
      returns >= 1, and the same range contains the literal `not supersedence` or an equivalent
      explicit negation naming the catalog's auto-update.
    - `node scripts/validation/check-nav-hub-links.mjs` prints
      `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `16 passed, 0 failed`.
    - `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations` —
      not 235.
    - `git ls-files --eol docs/operations/patch-management/08-windows-app-updates.md` reports `w/lf`.
    - `grep -cE 'SCCM|System Center|enrolment|licence|behaviour|TBD|TODO|FIXME|PLACEHOLDER|PITFALL-|winget-cli|PatchMyPC|Chocolatey|Ninite|Scappman|Robopack'
      docs/operations/patch-management/08-windows-app-updates.md` returns `0`.
  </acceptance_criteria>

  <done>08 exists with its final structure, both seam statements are present, the mechanism hierarchy
  ships as a decision list with the supersedence link confined to the Win32 tier, and the three live
  gates are at baseline.</done>
</task>

<task type="auto">
  <name>Task 2: APP-03 — the six channels, the three structural constraints, the Intune surface, and the documented cadence conflict</name>

  <precondition>`docs/operations/patch-management/08-windows-app-updates.md` exists with all ten H2s and eight anchors (Task 1 complete).</precondition>

  <files>docs/operations/patch-management/08-windows-app-updates.md</files>

  <read_first>
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md`
      §`Verbatim Quote Bank` → the channels subsection — every channel sentence, both comparison-table
      columns, and the note on the perpetual volume-licence channel.
    - The same file's §`The three ROADMAP-flagged unknowns` → Unknown 1 in full (the settings-catalog
      surface, the redirect decoy, the two differently-named channel settings, the observed-but-not-
      interpreted namespace difference) and Unknown 2 in full (both sides of the cadence conflict, and
      the six-step planner instruction that tells you exactly how to write it).
    - `docs/operations/patch-management/07-windows-autopatch.md` `## Update Workloads and Service
      Objectives` as shipped by plan 01 — so the channel pin is stated here as a condition and linked,
      never re-authored.
  </read_first>

  <action>
Fill two sections.

**`## Microsoft 365 Apps Update Channels`.** Ship all six channels: Current as the default with its
rollback column value stated as not applicable, Current Preview, Monthly Enterprise, Semi-Annual
Enterprise, Semi-Annual Enterprise Preview, and Beta as **not supported** and for test environments
only. A table is the natural shape; keep every row on one physical line.

Ship the three structural constraints as prose, each quoted: channels are device-specific and the
choice does not follow a user from device to device; only one channel can be configured for a device,
so installing the subscription Project and Visio desktop apps alongside Microsoft 365 Apps forces all
of them onto the same channel; and the two collaboration clients named in the source have their own
cadences separate from these channels.

Ship the Autopatch-enrolled condition as a condition and link `07-windows-autopatch.md`: an
Autopatch-enrolled fleet is steered toward the enterprise channel `07` names, which directly constrains
the one-channel-per-device rule. Do not restate `07`'s objective figures here.

Decide deliberately whether to mention the perpetual volume-licence channel; the research file records
it as a real, named seventh channel with its own destination article. If you omit it, say in prose that
the enumeration covers subscription Microsoft 365 Apps, so the reader is not led to believe the list is
exhaustive across all Office product families. Record the choice in the summary either way.

**The cadence conflict — write it exactly as the research file's six-step instruction directs.** State
the announcement with **its own date and its own future tense**, attributed to the channel-unification
article and its message-centre identifier. Then state that the channels overview's comparison table and
its feature-updates section still describe the twice-yearly behaviour, quoting both. Then state plainly
that neither reading is supportable from the pages as of the research date, and that the guide is
describing the documentation's state rather than the service's. Give the reader the one checkable
discriminator the source published — the build-number threshold above which the July release is
installed — and the dated escape hatch for the prior version. Attribute the twice-yearly description to
**the page**, never to the vendor as a present-tense claim about the service. **Two Source lines, one
per page.** No countdown, no deadline row, no "the change has landed" sentence.

**`## Setting the Channel from Intune`.** This H2 ships — the fetch succeeded. Name the settings
catalog as the current surface, with the two settings the article names and the minimum role required
to configure the policy. Cite the **canonical** settings-catalog URL recorded in the research file, not
the older administrative-templates slug that still returns 200 and redirects. Record that the
Administrative Templates route remains documented and that its setting carries a **different** name;
do not collapse the two names into one. Carry the two operationally sharp facts: the scheduled task
that must be enabled on client devices, and the statement that the verification registry key updates
only when that task runs or a user signs in and can take at least a day and more — which is why
re-applying a channel setting is not an immediate or self-confirming operation.

The article uses two different administrative-template namespace strings in two different steps and
does not explain the difference. **Quote at most one, attribute it to the step it came from, and say no
more.** Do not present the difference as meaningful.
  </action>

  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs && node scripts/validation/v1.20-milestone-audit.mjs | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - The `## Microsoft 365 Apps Update Channels` section range contains all six channel names:
      `Current`, `Current Preview` (or `Current (Preview)`), `Monthly Enterprise`,
      `Semi-Annual Enterprise`, a Semi-Annual Enterprise preview form, and `Beta`.
    - That range contains the literals `Not applicable`, `not supported`, `OneDrive` and
      `Microsoft Teams`.
    - That range contains a statement of the one-channel-per-device rule and the device-scoped rule:
      `grep -c 'one update channel for a device'` returns >= 1 and `grep -ci 'device-specific'`
      returns >= 1 over that range.
    - That range contains the literal `07-windows-autopatch.md`.
    - The cadence conflict ships both sides and settles neither:
      `grep -c 'Twice a year'` returns >= 1 over the file, `grep -c 'MC1274325'` returns >= 1,
      `grep -c '20131.20000'` returns >= 1, and `grep -c 'September 8, 2026'` returns >= 1.
    - `grep -ciE 'the change (has )?landed|now receives monthly|unified in July|days remaining|countdown'
      docs/operations/patch-management/08-windows-app-updates.md` returns `0`.
    - The `## Setting the Channel from Intune` section range contains the literals `settings catalog`,
      `Update Channel`, `Target Version`, `Policy and Profile Manager`, `Update Channel (2.0)` and
      `Office Automatic Updates 2.0`.
    - That range cites the canonical article path:
      `grep -c 'device-configuration/settings-catalog/update-office'` returns >= 1, and
      `grep -c 'administrative-templates-update-office'` returns `0`.
    - `grep -c 'office16' docs/operations/patch-management/08-windows-app-updates.md` returns `0` or
      `1` — never both namespace forms.
    - The channels and cadence material carries at least three lines matching `^\*\*Source:\*\*`, and
      no single Source line is followed by quoted material attributed to two different articles.
    - `grep -c '^## ' docs/operations/patch-management/08-windows-app-updates.md` still returns `10`
      and `grep -c '<a id=' ` still returns `8`.
    - `node scripts/validation/check-nav-hub-links.mjs` prints `0 hub-presence failure(s), 0
      corpus-link failure(s), 0 total`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `16 passed, 0 failed`.
  </acceptance_criteria>

  <done>All six channels, the three structural constraints, the Intune settings-catalog surface with
  its distinctly-named administrative-templates counterpart, and the cadence conflict with both sides
  quoted and neither resolved.</done>
</task>

<task type="auto">
  <name>Task 3: APP-04 — Enterprise App Management's gates and objectives, and all eight limitations as bold sub-labels</name>

  <precondition>`docs/operations/patch-management/08-windows-app-updates.md` carries a filled channels section (Task 2 complete).</precondition>

  <files>docs/operations/patch-management/08-windows-app-updates.md</files>

  <read_first>
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md`
      §`Secondary verifications` → the entry enumerating the eight limitations as the structurally
      extracted heading set, with their anchor ids and the two bodies quoted in full. **This table, not
      any research ledger's bullet list, is the definition of "all eight".**
    - The same file's §`Verbatim Quote Bank` → the Enterprise App Management subsection, and the two
      blockquoted framing notes beneath it (the objectives framing, and the trade-off framing).
    - `docs/operations/patch-management/06-windows-driver-firmware-updates.md`
      `## Unsupported and Anti-Feature Callouts` — the bold-sub-label shape this section replicates.
    - `docs/reference/win32-app-packaging.md` and `docs/admin-setup-apv1/03-esp-policy.md` — confirm
      both exist and note the headings the blocking-app cross-links should target. **Read only.**
  </read_first>

  <action>
Fill `## Enterprise App Management` and open `## Unsupported and Anti-Feature Callouts`.

**`## Enterprise App Management`.** State the five reachability gates as hard as the entitlement, each
quoted: an add-on subscription required **in addition to** an Intune Plan 1 or Plan 2 licence,
purchasable standalone or as part of the suite; Windows Win32 applications only, specifically the
installer types the source names; applications hosted by the vendor and installed by the Intune
management extension; the cloud environments the source lists and no others; and auto-update applying
only to applications with a **Required** assignment, with Available-assigned applications continuing to
use the existing update workflow.

State the unentitled case plainly: the capability is a procurement gate rather than a product-enforced
one, because the source also states that no licence check is performed on catalog applications.

Ship the published objectives with the framing the source itself insists on — targets and guidelines,
explicitly **not** guarantees — including the point at which the measurement window starts, the
percentage-within-24-hours target, the manual-validation ceiling, and the expedited goal. Note that
adding an application to the catalog carries an even harder disclaimer, with no timeline offered at all.
These figures exist here because the Store-apps section contrasts against them in Task 4.

Write the ESP and device-preparation positive **here**, as a trade-off, in this section — never under
the callouts H2, whose title asserts the prohibition and would invert the statement. Catalog
applications **can** be selected as blocking applications in both profile types; only the auto-update
ones cannot. Cross-link the two files that already own blocking-app configuration.

State explicitly that this capability's auto-update is **not** supersedence and creates no new app
object, and do not link the supersedence anchor from any sentence in this section.

**`## Unsupported and Anti-Feature Callouts` — the eight limitations.** Use the source page's own eight
heading names as bold sub-labels, in the source page's own order. Do not renumber, rename, merge or
reorder them; the count and the names are properties of the page, and a research ledger's bullet list
is a superset that folds one of the canonical eight into another item — following it ships seven of
eight and fails a Success Criterion silently.

Quote the body of the revocation limitation in full, including the statement that the administrator
remains responsible for identifying impacted devices and taking remediation action; and the body of the
cache-lag limitation, including the exposure window it describes. The remaining six get the source's
heading plus a short faithful summary. One Source line covers this whole contiguous block, because it
all comes from one page.
  </action>

  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs && node scripts/validation/v1.20-milestone-audit.mjs | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - The `## Enterprise App Management` section range contains the literals `Intune Suite`,
      `GCC`, `DoD`, `Required`, `Intune management extension`, and a form of `exe` and `msi`.
    - That range contains the literal `guidelines, not guarantees` (or the source's exact wording of
      it) and contains `24 hours`, `seven days` and `48 hours`.
    - `grep -c 'Service Level Agreement' docs/operations/patch-management/08-windows-app-updates.md`
      is permitted only where the sentence states the objectives are **not** one; there must be no
      sentence describing the catalog's own objectives as an agreement or a guarantee.
    - The ESP and device-preparation positive is in the Enterprise App Management range and **not** in
      the callouts range:
      `awk '/^## Enterprise App Management$/,/^## Microsoft Store Apps/' … | grep -ci 'blocking app'`
      returns >= 1, and
      `awk '/^## Unsupported and Anti-Feature Callouts/,/^## Related Resources/' … | grep -ci 'can be selected as .*blocking'`
      returns `0`.
    - That same Enterprise App Management range contains the literals
      `../../reference/win32-app-packaging.md` and `../../admin-setup-apv1/03-esp-policy.md`.
    - All eight limitation names are present as bold sub-labels. Every one of these greps returns >= 1
      over the callouts range: `rollback`, `Malicious version revocation`, `Catalog cache lag`,
      `rollout rings`, `latest state only`, `Version changes during device processing`,
      `blocking app`, `Conflicts with other app types`.
    - `awk '/^## Unsupported and Anti-Feature Callouts/,/^## Related Resources/' docs/operations/patch-management/08-windows-app-updates.md | grep -c 'PITFALL-'`
      returns `0`.
    - The callouts range contains the literal
      `still responsible for identifying impacted devices` (or the source's exact wording).
    - `awk '/^## Enterprise App Management$/,/^## Microsoft Store Apps/' docs/operations/patch-management/08-windows-app-updates.md | grep -c 'supersedence'`
      returns >= 1 **only** in a negating sentence; there is no `#supersedence` link in this range:
      `grep -c '#supersedence'` over that range returns `0`.
    - `grep -c '^## ' docs/operations/patch-management/08-windows-app-updates.md` still returns `10`
      and `grep -c '<a id=' ` still returns `8`.
    - `node scripts/validation/check-nav-hub-links.mjs` prints `0 hub-presence failure(s), 0
      corpus-link failure(s), 0 total`.
  </acceptance_criteria>

  <done>The five reachability gates, the published-but-non-binding objectives, the ESP and
  device-preparation positive filed as a trade-off in the right section, and all eight limitations as
  bold sub-labels in the source's own order and heading text.</done>
</task>

<task type="auto">
  <name>Task 4: APP-05 and APP-06 — the Store split, the routing reconciliation, the control surface, then commit 2</name>

  <precondition>`docs/operations/patch-management/08-windows-app-updates.md` carries a filled Enterprise App Management section and the eight limitation sub-labels (Task 3 complete).</precondition>

  <files>docs/operations/patch-management/08-windows-app-updates.md, docs/operations/patch-management/07-windows-autopatch.md</files>

  <read_first>
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md` →
      Unknown 3 in full (preview status, the ARM64 single-bullet note, the re-measured cadence absence,
      the split, the suppression asymmetry, the two further constraints), and §`Secondary verifications`
      → the settings-catalog trap entry with both policy rows and the leaf-name table, and the
      system-context entry with the re-verified sentences.
    - The same file's `## Plan-Time Fetch Addendum (Plan 148-01)` item 6 — the two device-preparation
      quotes, fetched by plan 01.
    - The same file's §`MEASURED / PREMISE ledger` → the `PREMISE` subsection, specifically the
      developer-environment configuration row and the row covering the nine unread `DesktopAppInstaller`
      setting descriptions. Both bear on this task and both carry an explicit re-fetch-before-quoting
      instruction; step 2 of this action discharges the first, and the `STACK.md` note below discharges
      the second. Read the ledger's own wording rather than acting on this summary of it.
    - `.planning/research/WINGET-GAP.md` §2.4 and §5 — the fifteen-setting policy table this guide
      ships six rows of, the registry key, and the link-not-copy guardrail that forbids transcribing
      the reference page.
    - `docs/admin-setup-apv2/02-etg-device-group.md` and
      `docs/admin-setup-apv2/03-device-preparation-policy.md` — the two corpus sites carrying the
      conflation, to be reconciled in prose and **never edited**. Also
      `docs/reference/macos-capability-matrix.md` — the third site, which the obvious term grep misses
      because it names the product in full. **All three are read-only.** Per D-12, no validator pins
      any file under `docs/admin-setup-apv2/` — the real constraint on the device-group page is its
      enrollment identifier, and the do-not-edit ruling stands on the blast-radius grounds in D-44,
      not on a validator pin. Do not reason from a validator that does not exist.
    - `.planning/research/STACK.md` §C-1 — note per D-11 that the policy CSP reference page is
      **not** the oldest source in this pillar's set; two other pages in it are older. Drop that
      comparative if you meet it in an inherited artifact; re-fetch the CSP page on its own merits.
      Only the fifteen heading names were verified this research session — re-read the per-setting prose
      for the five rows other than the interval row on that page before quoting any of their text.
    - `docs/operations/patch-management/07-windows-autopatch.md` `## Related Resources` — the entry
      shape the one added line must match.
  </read_first>

  <action>
Fill the remaining three sections, append the APP-05 and APP-06 items to the callouts H2, add one line
to `07`, and land commit 2 of three.

**Two live fetches gate this task's prose. Run both before authoring the sections that quote them.**
Method is the one fixed by D-59 and recorded as zero-fabrication in `147-VERIFICATION.md`: `curl` plus a
local tag-strip plus a literal `grep`, never a rendered-summary fetch tool. Confirm each page's H1 and
its `<link rel="canonical">` before quoting; a Learn slug that returns HTTP 200 is not proof you reached
the intended article. Pin every quote by its surrounding heading, never by grep hit (D-61).

1. **The Store-apps article — preview status.** If Win32 Store applications have left preview, stop and
   surface it rather than rewriting — a Success Criterion and the requirement text both require the
   preview wording.
2. **The developer-environment configuration page — the one PREMISE row this plan would otherwise quote
   unfetched.** `148-RESEARCH.md`'s `## MEASURED / PREMISE ledger` records this page's facts as carried
   from `WINGET-GAP.md` §2.3 and **not** re-verified, with the instruction to re-fetch before quoting;
   D-40's own `[MEASURED]` label is inherited from that ledger, not measured this session. Every other
   claim in this file comes from the byte-verified quote bank or from plan 01's fetch addendum, so this
   is the single exception and it is closed here rather than accepted. Fetch the page and record, in the
   summary, exactly what plan 01's Task 1 recorded per item: the URL actually landed on, the page's
   `ms.date`, its rendered `Last updated on` value, and the heading each quote sits under. Confirm on
   the page's own bytes each of the three claims the routing sentence makes — that the page is scoped to
   development environments, that it does not mention Intune or device management, and that it documents
   partial success as designed behavior. **Any claim the live page does not confirm is written as an
   explicit not-found line naming the search terms used, and the routing sentence drops that clause —
   never keep the clause and stay silent about the failure.** Per D-60, a page that fails to yield its
   content because it renders client-side is recorded as a retrieval failure, never as evidence of
   absence.

**`## Microsoft Store Apps`.** Ship the update model split with both halves quoted: UWP applications
kept current by the Store, with or without an Intune assignment, once installed and unless the Store
policy blocks auto-update; and Store Win32 applications kept current by Intune, therefore requiring an
Intune assignment, and unaffected by the Store's own update policies. Ship the preview status (stated
twice on the page), the ARM64 exclusion — noting that the unsupported list has exactly **one** entry,
so do not write it as if there were several — the paid-application exclusion, and the two-core-processor
floor. Ship the suppression asymmetry with both quotes: the Store auto-update block stops the Store's
UWP updating, while a Store Win32 application with an active Intune assignment is still automatically
updated, so removing the assignment is the only lever on that half.

Then ship the **absence** as a deliverable: the page states *that* these applications are kept up to
date and never says *when* — no cadence, no check interval, no schedule, no service objective and no
version pinning, re-measured this research pass as a zero-hit grep across the whole page — and contrast
that explicitly against the published-but-non-binding objectives written in the previous section. Invent
no number; borrow none from the management extension or from the catalog.

**`## Enterprise App Management, Store Apps and WinGet — Routing`.** Ship the vendor FAQ negative
**verbatim**, question and answer, with its own Source line, and stop there — no extension, no
strengthening paraphrase, no date or rationale the vendor did not give. Then show the name attaching to
three genuinely different things, each named with a qualifier that keeps it distinguishable: the Intune
Microsoft Store app (new) application type, which is a patching surface; the operating-system App
Installer component and its command-line tool, which is not a patching surface and offers only a
policy-shaped on-off surface; and the developer-environment configuration feature, which is out of
scope — one sentence plus its kill-switch row, noting only what step 2's fetch confirmed on the page's
own bytes: that its page is scoped to development environments, never mentions Intune or device
management, and documents partial success as designed behavior. **That sentence carries its own
`**Source:**` line naming the page fetched in step 2 with that page's own `(updated …)` value**, scoped
to that page alone; the kill-switch row's authority is the policy CSP reference, never this page. If
step 2 left any of the three clauses unconfirmed, drop that clause from the sentence and record the
not-found line in the summary.

Then the reconciliation, keeping its **device-preparation scope** intact: where the Autopilot
device-preparation documentation says only Store applications supporting the package manager are
supported, that is a device-preparation eligibility filter on the Intune Microsoft Store app (new)
application type — not a statement that the command-line tool is an Intune-managed patching surface.
Name that this wording appears in the corpus's own device-preparation and group pages, and that a
reference matrix states the same conflation using the product's full name. **Edit none of those three
files.** Record in the summary that the device-preparation page's own wording stands unchanged and
unlinked, so the correction is reachable only by a reader who already found this guide; the inbound
wiring belongs to Phase 152 and the third-site sweep to the backlog.

Also carry, from the Store-apps page, the two carve-outs that seed the conflation in the first place:
that the turn-off-the-Store policy does not affect the command-line tool, and that the private-store-only
policy blocks end-user Store access while still allowing the command-line tool through — which is why
that policy is not the preferred lockdown choice.

**`## Controlling WinGet on Managed Devices` — hardening, not patching.** Ship a **six-row** policy
table naming: the App Installer enable policy, the command-line-interfaces policy, the configuration
feature kill switch, the Store-source policy, the default-source policy, and the source auto-update
interval. Keep every row on one physical line. Add a link-not-copy pointer to the full reference for
the remaining settings; transcribing a vendor reference page is what the research file's own scope
guardrail excludes. Beneath the table, expand the interval setting as a **bold sub-label**: the package
index is cached locally, downloaded only when a user invokes a command and the interval has passed, is
**not** updated in the background, and the setting has no impact on REST-based sources. That is the
clean disqualifier and it is the one row the requirement names by name.

Add one sentence naming private REST sources as out of scope, noting that the additional-source and
allowed-source policies can point at a source Intune cannot see. Ship the compliance-scripting registry
key as inline code.

Close the section with the settings-catalog trap, written as a consequence: the two policies the
vendor's own Store-application deployment article recommends enabling are flagged in that same article
as not built in and requiring a custom configuration profile — so an administrator hardening Windows
through the settings catalog can silently degrade a supported Intune application type using a lever
Intune never surfaces. That sentence is why APP-06 exists. The research file also supplies the
settings-catalog leaf names for the two Store auto-update policies directly; use them rather than
citing only a policy identifier.

**Append to `## Unsupported and Anti-Feature Callouts`**, as bold sub-labels: the Store type's total
cadence absence; the system-context root cause — the package manager is a per-user-registered component
whose availability depends on an interactive first sign-in and an asynchronous Store registration, so
every context in which Intune runs code with no signed-in user is a context where it may not be
registered and resolvable — together with the documented silence that the vendor's own administrator-
considerations section covers only elevation and says nothing about unattended or service-context
execution; and the settings-catalog gap. Name community automation and third-party patch products as a
**category**, one sentence each, with **no project or product named**.

**Then add exactly one line to `07-windows-autopatch.md`'s `## Related Resources`**: an entry for
`08-windows-app-updates.md`, matching the existing entry shape, with the `[text](url)` construct on one
physical line. This is the only edit to `07` in this plan and it closes the sibling pair.

Re-read both files once for prose accuracy against the quote bank, then commit as commit 2 of three
with a `docs(148):`-shaped message naming the new file and the four requirement IDs.
  </action>

  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs && node scripts/validation/check-phase-54.mjs | tail -3 && node scripts/validation/v1.20-milestone-audit.mjs | tail -3 && node scripts/validation/check-phase-144.mjs | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - The `## Microsoft Store Apps` range contains all of: `UWP`, `with or without`, `in preview`,
      `ARM64`, `two core processors` (or the source's wording), and `paid app`.
    - That range states the absence explicitly: `grep -ciE 'no (published )?(cadence|interval|schedule)'`
      returns >= 1, and the range contains a comparative reference to the objectives written in the
      Enterprise App Management section.
    - `grep -cE '[0-9]+ ?(hours?|minutes?|days?)' ` over the `## Microsoft Store Apps` range returns
      `0` **unless** every match sits inside a sentence about the enterprise catalog's objectives — no
      cadence figure may be attributed to the Store app type.
    - The routing range contains the FAQ negative verbatim: `grep -c 'directly installed by the Intune
      management extension'` returns >= 1, and the answer is not followed in the same paragraph by an
      added rationale or a date.
    - The routing range names three distinct referents: it contains `Microsoft Store app (new)`,
      `App Installer`, and a reference to the developer-environment configuration feature, and contains
      the device-preparation reconciliation sentence
      (`grep -ci 'device.preparation eligibility filter'` returns >= 1).
    - The developer-environment clause is sourced to the page step 2 fetched, and not to a neighbor:
      `awk '/^## Enterprise App Management, Store Apps and WinGet — Routing/,/^## Controlling WinGet on Managed Devices/' docs/operations/patch-management/08-windows-app-updates.md | grep -c '^\*\*Source:\*\*'`
      returns >= 2 — the vendor FAQ negative and the configuration page are different articles and R7
      forbids one line covering both — and at least one of those lines names the configuration page's
      own URL and carries an `(updated ` value. A configuration-feature sentence with no dated Source
      line of its own means step 2's fetch did not happen; the task is not done.
    - Every clause in the configuration-feature sentence is one step 2 confirmed on the live page's own
      bytes. A clause step 2 could not confirm is **absent** from the file, and the summary carries its
      not-found line with the search terms used. This is the same contract plan 01's Task 1 ran on the
      six APP-01 items, applied to the one item in this plan that carried the same status.
    - `git status --porcelain docs/admin-setup-apv2/ docs/reference/macos-capability-matrix.md
      docs/operations/app-lifecycle/ docs/operations/co-management/` returns empty — none of the
      read-only reconciliation sites was edited.
    - The `## Controlling WinGet on Managed Devices` range contains exactly six policy names:
      `EnableAppInstaller`, `EnableWindowsPackageManagerCommandLineInterfaces`,
      `EnableWindowsPackageManagerConfiguration`, `EnableMicrosoftStoreSource`, `EnableDefaultSource`,
      `SourceAutoUpdateInterval` — each `grep -c` returns >= 1.
    - That range contains `is not updated in the background`, `REST`,
      `Software\Policies\Microsoft\Windows\AppInstaller`, and
      `Not built in; use a custom configuration profile`.
    - The callouts range contains a system-context sub-label:
      `grep -ciE 'system context|no signed-in user'` returns >= 1 over that range.
    - Per D-47 and D-58, this returns `0`:
      `grep -cE 'winget-cli|PatchMyPC|Chocolatey|Ninite|Scappman|Robopack|Ivanti|TechDirect|Repair-WinGetPackageManager|msstore'
      docs/operations/patch-management/08-windows-app-updates.md` — no project or product is named, the
      client repository issue is not cited, and neither never-assert claim appears in any form.
    - `grep -c '08-windows-app-updates.md' docs/operations/patch-management/07-windows-autopatch.md`
      returns exactly `1`, and that line contains a complete `[text](url)` construct.
    - `grep -c '^## ' docs/operations/patch-management/08-windows-app-updates.md` returns `10`;
      `grep -c '<a id=' ` returns `8`; `grep -c '^```' ` returns `0`.
    - `grep -cE 'SCCM|System Center|enrolment|licence|behaviour|TBD|TODO|FIXME|PLACEHOLDER|PITFALL-'
      docs/operations/patch-management/08-windows-app-updates.md` returns `0`.
    - `node scripts/validation/check-nav-hub-links.mjs` prints `0 hub-presence failure(s), 0
      corpus-link failure(s), 0 total`.
    - `node scripts/validation/check-phase-54.mjs` prints `32 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/check-phase-53.mjs` prints `26 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `16 passed, 0 failed`.
    - `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations`.
    - `node scripts/validation/check-phase-144.mjs` prints `101 PASS, 0 FAIL, 0 SKIPPED`.
    - `git log -1 --name-only` lists exactly two paths, both under
      `docs/operations/patch-management/`.
  </acceptance_criteria>

  <done>The Store split with both halves quoted and the cadence absence contrasted; the FAQ negative
  verbatim and the three referents separated; the reconciliation in prose with all three corpus sites
  untouched; a six-row control table with the interval expansion and the settings-catalog trap as its
  closing consequence; 07 and 08 mutually linked; all nine gates and the apex at baseline; commit 2 of
  three landed.</done>
</task>

</tasks>

<artifacts_this_phase_produces>

Every symbol below is **new in this plan** and does not exist at `a161a43c`. The source-grounding pass
excludes these from drift verification.

**New file paths**
- `docs/operations/patch-management/08-windows-app-updates.md`

**New H2 headings (each becomes a link-checked GitHub anchor slug)**
- `## Choosing a Windows App Update Mechanism`
- `## Microsoft 365 Apps Update Channels`
- `## Setting the Channel from Intune`
- `## Enterprise App Management`
- `## Microsoft Store Apps`
- `## Enterprise App Management, Store Apps and WinGet — Routing`
- `## Controlling WinGet on Managed Devices`
- `## Unsupported and Anti-Feature Callouts`
- `## Related Resources`
- `## External References`

**New `<a id>` anchors (8 — completing the 16-anchor Phase-151 contract)**
`choosing-an-app-update-mechanism` · `m365-apps-update-channels` · `setting-the-channel-from-intune` ·
`enterprise-app-management` · `microsoft-store-apps` · `eam-store-winget-routing` ·
`controlling-winget` · `unsupported-callouts`

**New H1 (Phase 152 registry Title input, not created here)**
- `# Windows Application Updates`

**New row in an existing file**
- One new `## Related Resources` entry in `docs/operations/patch-management/07-windows-autopatch.md`
  pointing at `08-windows-app-updates.md`.

**New nav / overview rows:** none in this plan. All `00-overview.md` insertions belong to plan 03.

</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `learn.microsoft.com` HTML → the shipped corpus | Untrusted external bytes cross into a knowledge base IT administrators act on. This plan quotes six distinct pages, two of them recorded as volatile. |
| research ledgers → corpus prose | A ledger's bullet list is a claim about a page, not the page. One ledger in this repository is already measured to fold a canonical item into another, which would ship seven of eight limitations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-148-05 | Tampering | the eight limitation sub-labels in `08` | high | mitigate | Use the structurally extracted heading set recorded in `148-RESEARCH.md` §Secondary verifications as the definition of "all eight", never a ledger bullet list. Task 3's acceptance criteria assert all eight names individually, so a seven-of-eight ship fails loudly instead of silently. |
| T-148-06 | Tampering | a `**Source:**` line spanning two pages | high | mitigate | R13: this file quotes six pages; check every Source line's scope individually. Task 2's criteria assert no Source line is followed by material from two articles. |
| T-148-07 | Spoofing | the settings-catalog article's redirecting predecessor slug | medium | mitigate | Cite the canonical path; Task 2's criteria assert the canonical path is present and the older slug absent. A guessed sibling slug was measured to serve a different article. |
| T-148-08 | Repudiation | a strengthened or dated paraphrase of the vendor FAQ negative | medium | mitigate | Ship verbatim; the prohibition block bars extension, strengthening and added rationale, and Task 4 asserts the exact answer string. |
| T-148-09 | Denial of service | downstream: an administrator hardens the package manager and silently degrades a supported Intune app type | medium | mitigate | This is the content APP-06 exists to deliver. Documenting the trap as a consequence, with the two policy rows and their custom-profile requirement, **is** the mitigation. |
| T-148-10 | Tampering | a volatile source page revised between research and execution | medium | mitigate | R16 names the three volatile pages. Task 4 re-checks preview status live before writing it and surfaces a change rather than silently rewriting. The phase verifier's re-fetch list must name all three. |
| T-148-11 | Tampering | the developer-environment configuration clause in the routing section | high | mitigate | This is the T-148-01 class inside plan 02: the one claim in this file whose source is a `PREMISE` ledger row rather than a fetched page, carrying an inherited `[MEASURED]` label. Task 4 step 2 re-fetches it by the D-59 method before the sentence is authored, and Task 4's criteria require the sentence to carry its own dated Source line naming that page — a clause that cannot be confirmed is dropped and recorded as not found rather than shipped silently. |
| T-148-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs in any ecosystem; `148-RESEARCH.md` §Package Legitimacy Audit records "Not applicable". |

ASVS level 1; blocking threshold `high`. All three `high` items are dispositioned `mitigate` with a
named, asserted mechanism.
</threat_model>

<assumptions_and_open_items>

**Edge-probe arithmetic (no silent drops).** The deterministic probe returned **19** applicable edges
across APP-01..APP-06, all `unresolved`. Plan 02 owns the **15** rows scoped to APP-03..APP-06. Of
those: **13** are authored into `must_haves.truths` as resolved/explicit and **2** as resolved/backstop
structured markers (APP-04 idempotency and APP-05 encoding). 13 + 2 = 15, with **0** flagged
assumptions in this plan. Across the phase: 16 explicit + 2 backstop + 1 flagged (plan 01's APP-01
`unclassified` row) = **19**.

**The two backstop truths and what abstention means.** Neither "re-running auto-update is not additive
to the app inventory" nor "no sentence lets the three referents be read as the same thing" reduces to a
literal grep. They are authored as structured backstop markers, so if the phase verifier cannot confirm
either with explicit evidence it abstains to `human_needed` rather than passing silently. That is the
intended behavior; do not attempt to convert them into a grep by weakening the statement.

**Carried research assumptions that bear on this plan.**
- The channels overview and the channel-unification article will not be revised between research and
  execution. If either is, the documented conflict evaporates and the framing becomes wrong. Both are
  named in R16 and must be in the verifier's re-fetch list.
- The Store-apps article will not exit preview between research and execution. Task 4 re-checks live
  and surfaces a change rather than absorbing it.
- **The developer-environment configuration page's facts were `PREMISE`, not measured.** The research
  file's ledger carries them from `WINGET-GAP.md` §2.3 with an explicit re-fetch-before-quoting
  instruction, and D-40's `[MEASURED]` label is inherited from that ledger rather than measured this
  session. This is the only claim in plan 02 not backed by the byte-verified quote bank or by plan 01's
  fetch addendum. **Task 4 step 2 is the mitigation**, on the same terms plan 01's Task 1 applied to the
  six APP-01 items: fetch first, record URL, both dates and the heading per quote, and write an explicit
  not-found line rather than keeping an unconfirmed clause. The residual risk — that the page is revised
  between this fetch and the verifier's — is covered by naming it in the verifier's re-fetch list.
- The administrative-template namespace difference on the settings-catalog article is a documentation
  artifact rather than two distinct policy surfaces. Mitigated by quoting at most one namespace and
  attributing it to its step; the guide must not explain the difference.

</assumptions_and_open_items>

<verification>
Run at the end of this plan, all from the repository root:

1. `node scripts/validation/check-phase-54.mjs` → `32 passed, 0 failed, 0 skipped`
2. `node scripts/validation/check-phase-53.mjs` → `26 passed, 0 failed, 0 skipped`
3. `node scripts/validation/check-phase-59.mjs` → `36 passed, 0 failed, 0 skipped`
4. `node scripts/validation/check-nav-hub-links.mjs` → `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`
5. `node scripts/validation/c17-eee-contract.mjs` → `234 files checked, 0 with violations`
6. `node scripts/validation/v1.20-milestone-audit.mjs` → `16 passed, 0 failed`
7. `node scripts/pipeline/build-filename-map.mjs --self-test` → `8 passed, 0 failed`
8. `node scripts/pipeline/build-publish-bundle.mjs --self-test` → `15 passed, 0 failed`
9. `node scripts/validation/check-phase-144.mjs` → `101 PASS, 0 FAIL, 0 SKIPPED`

As in plan 01, a green run proves only that nothing regressed. `08` sits outside `PATCH_FILES`, carries
no `doc_id` and has no registry row, so the only assertions that actually reach it are the corpus-wide
platform-lexicon negative, the link-and-anchor checker, and the ops-domain anti-pattern audit. The
acceptance criteria and the phase verifier carry the weight.
</verification>

<success_criteria>
- `docs/operations/patch-management/08-windows-app-updates.md` exists at 10 H2s, 8 anchors, 0 code
  fences, no `doc_id`, LF line endings, and a 60-day frontmatter interval computed by arithmetic from
  plan 01's execution date.
- ROADMAP SC#3 is satisfied: six channels with Current as the default and its rollback value, Beta as
  not supported, one channel per device, device-scoped rather than user-following, the two
  collaboration clients outside; plus Enterprise App Management's gates stated as hard as its
  entitlement, all eight limitations including revocation, and the blocking-app positive present as a
  trade-off.
- ROADMAP SC#4 is satisfied: the vendor negative verbatim; the Store split with the assignment
  asymmetry, preview status and ARM64 exclusion; the cadence absence stated **and** contrasted; and the
  settings-catalog trap on the two recommended policies.
- `07` and `08` are mutually linked and `check-nav-hub-links` is 0/0 — no commit in this phase lands a
  dangling link.
- Commit 2 of three is landed, touching only the two files in `files_modified`.
</success_criteria>

<output>
Create `.planning/phases/148-application-update-management-winget-routing/148-02-SUMMARY.md` when done.

Record in it: the eight anchor ids as shipped, so the 16-anchor Phase-151 contract is stated in one
place; whether the perpetual volume-licence channel was mentioned or omitted and why; the live
re-check result for the Store-apps preview status; **the full record of Task 4 step 2's fetch of the
developer-environment configuration page — the URL actually landed on, the page's `ms.date`, its
rendered `Last updated on` value, the heading each quote sits under, and any of the three clauses
recorded as not found with the search terms used**; the exact set of Learn pages quoted with the
`(updated …)` value used for each, as the phase verifier's re-fetch list — the configuration page is on
that list; and the nine gate figures measured after the commit.
</output>
