---
phase: 148-application-update-management-winget-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/patch-management/07-windows-autopatch.md
  - .planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md
autonomous: true
requirements: [APP-01, APP-02]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "docs/operations/patch-management/07-windows-autopatch.md exists and documents Windows Autopatch enrollment mechanics, the Test / Last group model, the Autopatch application-update workloads, and the Autopatch-specific reporting surfaces — a delta over the sealed 01 guide, not a re-authoring of ring topology (SC#1, APP-01, D-14)."
    - "07 cross-links co-management/03-cocmgmt-migration-paths.md#autopatch-prerequisites for the workload-slider prerequisites and 01-windows-wufb-rings.md#autopatch-disambiguation plus 01-windows-wufb-rings.md#hotpatch for topology and in-memory patching, and edits neither file (SC#1, APP-01, D-19, D-21, D-49)."
    - "07 states the precedence rule in its own prose: co-management/03 owns the workload-slider prerequisites, 07 owns the Autopatch service prerequisites that 03 has never carried (APP-01, D-21)."
    - "Both entitlement lists ship side by side in ## Autopatch and Hotpatch, and the reader can see them differ: the Autopatch side carries the VDA entry the hotpatch side omits, and the hotpatch side carries the Windows 365 Enterprise entry the Autopatch side omits (SC#2, APP-02, D-17, edge probe APP-02/adjacency)."
    - "The barred conclusion is replaced by an explicit positive statement that an Autopatch entitlement is necessary but not sufficient for hotpatch, and the phrase never appears in an inverted form anywhere in the file (SC#2, APP-02, D-18)."
    - "The two entitlement lists ship in their own source order, each under its own **Source:** line scoped to the single page it came from, so a reader can re-fetch either side independently and neither list borrows the other's authority (APP-02, D-64, edge probe APP-02/ordering)."
    - "The Windows 11 Pro hotpatch eligibility case ships flagged unconfirmed, mirroring the treatment already shipped at 01-windows-wufb-rings.md:144-145 — the page carries zero occurrences of that edition and therefore neither supports nor refutes it, and 07 asserts neither direction (APP-02, D-04, RESEARCH Open Question 8, edge probe APP-02/empty)."
    - "The containment position is written positively — an Autopatch group is a container that includes the standalone update rings policy — and Phase 145's mutual-exclusivity correction is not restated (APP-01, D-20; this section is what Phase 151 SC#2 consumes, D-74)."
    - "The Autopatch prerequisites page's own internal inconsistency is shipped as a documented conflict: two co-management workloads are named, the page's Configuration Manager section refers to three without naming the third, and 07 asserts nothing about the unnamed third (APP-01, D-50, RESEARCH R-3 and Open Question 4)."
    - "Every ring-model, containment, service-objective and reporting-surface claim in 07 was re-fetched from its first-party page during this plan and recorded in 148-RESEARCH.md's plan-time fetch addendum before the section quoting it was authored (RESEARCH PREMISE ledger, Open Question 9, Assumption A3)."
    - "07 is 10 H2s, 8 own-line <a id> anchors and 0 code fences, matching both 05-linux-update-delivery.md (484 lines, 11 Source lines) and 06-windows-driver-firmware-updates.md (792 lines, 64 Source lines) exactly — the corrected sibling measurements, not the smaller figure an earlier draft used (D-08, D-15, D-48, D-70)."
    - "07 is written as a delta over the 166 corpus occurrences of Autopatch across 10 files, not as a greenfield guide; it owns only what nothing else owns (D-09, D-14)."
    - "The phase verifier re-fetches and diffs, and this plan names its list: no validator in this repository parses evidence lines, so the verifier is the only gate that can catch a fabricated quote (D-62)."
    - "A remediation round is inside this plan's budget rather than a plan failure: neither predecessor phase finished in the number of plans it started with (D-66)."
    - "07's frontmatter carries last_verified equal to the execution date of this plan's commit and review_by equal to that date plus 60 days by arithmetic, with no doc_id field (D-70, D-76)."
    - "node scripts/validation/check-nav-hub-links.mjs reports 0 hub-presence failures and 0 corpus-link failures — 07 establishes the corpus's first inbound links to 01's anchors and both resolve (D-06, D-19)."
    - "node scripts/validation/v1.20-milestone-audit.mjs reports 16 passed, 0 failed with 07 inside C11's live walkMd('docs') (D-72, RESEARCH in-repo ledger)."
    - "node scripts/validation/c17-eee-contract.mjs still reports 234 files checked, 0 with violations — the count does not become 235, confirming the no-doc_id rule held (D-70)."
    - "node scripts/validation/check-phase-144.mjs still reports 101 PASS, 0 FAIL, 0 SKIPPED after the commit (D-67)."
  prohibitions:
    - requirement_id: APP-01
      category: transparency
      statement: "MUST NOT attach a **Source:** line to a claim its cited page does not make, and MUST NOT quote any sentence that was not read from that page's own bytes during this phase. No validator in this repository parses evidence lines, so a fabricated quote ships silently."
    - requirement_id: APP-01
      category: transparency
      statement: "MUST NOT present the Autopatch prerequisites page's workload-count inconsistency as resolved, and MUST NOT fill the unnamed third workload from general co-management documentation."
    - requirement_id: APP-02
      category: transparency
      statement: "MUST NOT resolve the Windows 11 Pro hotpatch eligibility question in either direction; the sole first-party page neither supports nor refutes it, and a confident sentence here would be an invention presented with a real citation."
    - requirement_id: APP-01
      category: values
      statement: "MUST NOT name any third-party patch-management product, community automation project, or OEM vendor; the corpus has zero such occurrences and the boundary is a standing scope rule, not a stylistic preference."
  artifacts:
    - docs/operations/patch-management/07-windows-autopatch.md
  key_links:
    - "The eight own-line <a id></a> anchors in 07-windows-autopatch.md — half of the 16-anchor cross-link contract Phase 151's Recipe #5 consumes and cannot author for itself (D-74)."
    - "07-windows-autopatch.md -> 01-windows-wufb-rings.md#autopatch-disambiguation and #hotpatch — the corpus's FIRST inbound links to either anchor; measured 0 such links at plan time, so this is an unproven pattern that the acceptance criteria must verify rather than assume (D-06)."
    - "07-windows-autopatch.md -> ../co-management/03-cocmgmt-migration-paths.md#autopatch-prerequisites — anchor confirmed live at 03:33; 03 is never edited (D-21, D-49)."
    - "07-windows-autopatch.md -> 06-windows-driver-firmware-updates.md#driver-update-reporting — anchor confirmed live at 06:455; the reporting surfaces are disjoint by workload (D-22)."
    - "The H1 '# Windows Autopatch' — Phase 152's registry Title input; measured zero colliding Titles across the 225 registry rows at plan time (D-71). No registry row is created in this phase."
---

<objective>
Author `docs/operations/patch-management/07-windows-autopatch.md` and land it as **commit 1 of three**
(D-65). This is the Autopatch delta the corpus has never had: enrollment mechanics, the `Test` / `Last`
group model, the containment position, the Autopatch application-update workloads and service
objectives, the Autopatch-specific reporting surfaces, and — the deliverable ROADMAP SC#2 names — both
entitlement lists side by side so the reader can see them differ.

Purpose: APP-01 and APP-02, and ROADMAP Success Criteria 1 and 2.

Output: one new file, plus a plan-time fetch addendum appended to `148-RESEARCH.md`. **No** registry
row, filename-map row, canary bump, ops-index row or nav-hub wiring (Phase 152, D-73). **No**
`00-overview.md` edit (plan 03). **No** edit to `01-windows-wufb-rings.md` (sealed by Phase 146) or to
`co-management/**` (D-49, and 146 D-72 is an owner ruling).

**`148-RESEARCH.md` already carries a byte-verified quote bank for APP-02.** Do not re-derive a
citation the research file already carries verbatim. APP-01 is the exception and Task 1 exists because
of it: six of APP-01's load-bearing claims are labelled `PREMISE` in that file and were never fetched.

Five decisions are implemented by this plan's `<phase_wide_rules>` rather than by any one task, and are
named here so the trace is not lost: **D-01** (both pipeline scripts live under `scripts/pipeline/`) in
R1; **D-68** (which gates actually reach `07`, and which do not) in R2; **D-16** (American spelling,
because an H2 becomes a link-checked slug against a checker with no allowlist) in R4; **D-63** (the
evidence-line contract `06` established, one Source line per contiguous quote per page) in R7; and
**D-07** (the hotpatch coordinates were corrected, so locate by quoted text and never by line number) in
R12 and in Task 4's `<read_first>`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-application-update-management-winget-routing/148-CONTEXT.md
@.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md
@.planning/phases/148-application-update-management-winget-routing/148-PATTERNS.md
</context>

<phase_wide_rules>

These bind every task in this plan. They are locked decisions, not suggestions. The banned literals are
collected in R5 rather than scattered through any `<action>` body, so that a negative acceptance
criterion and the instruction that produces it can never collide.

**R1 — Baselines re-measured at HEAD `a161a43c` at plan time, all nine green.** apex
`check-phase-144` **101 PASS / 0 FAIL / 0 SKIPPED** (~27s) · `check-phase-54` **32 passed, 0 failed, 0
skipped** · `check-phase-53` **26/0/0** · `check-phase-59` **36/0/0** · `check-nav-hub-links` **0
hub-presence / 0 corpus-link** · `c17-eee-contract` **234 files checked, 0 with violations** ·
`v1.20-milestone-audit` **16 passed, 0 failed** · `build-filename-map --self-test` **8/0** ·
`build-publish-bundle --self-test` **15/0**. Any deviation is a regression, never a new baseline. Both
pipeline scripts live under `scripts/pipeline/`, not `scripts/` — a wrong path exits
`MODULE_NOT_FOUND`, which reads as a failure and is not one (D-01).

**R2 — The gates assert almost nothing about this deliverable, and that is measured, not assumed.**
`check-phase-54.mjs:35` defines `PATCH_FILES` as the overview plus the four per-platform files; `07` is
outside it, so `V-54-07`, `V-54-08`, `V-54-26`, `V-54-30` and `V-54-31` do not bind this file. `07`
carries no `doc_id`, so C17 does not enroll it. No registry row exists, so neither canary sees it. **The
only live gates on `07` are `V-54-27`, `check-nav-hub-links`' target-and-anchor resolution, and C11 via
`v1.20-milestone-audit.mjs:585`'s live `walkMd('docs')`.** All nine gates read green whether `07` is
correct, wrong, or largely empty. The acceptance criteria carry the weight (D-68).

**R3 — Zero code fences in this file.** Measured at plan time: all seven existing
`docs/operations/patch-management/*.md` files return `0` for `grep -c '^```'`. Entitlement lists,
prerequisites and settings ship as prose, bullet lists, tables and `>` blockquotes (D-48).

**R4 — American spelling in every heading and every line of body prose.** Write `Enrollment`,
`license`, `behavior`, `organization`. Measured corpus-wide at plan time: the American form of
enrollment occurs 5481 times, the British form 5 times, and there are zero British-spelled headings.
The reason is mechanical, not stylistic: an H2 becomes a GitHub slug that `check-nav-hub-links`
resolves byte-for-byte, and that checker has **no baseline, no allowlist and no ratchet** (D-16, 146
D-39). Note that `REQUIREMENTS.md` itself uses the British spelling of the entitlement noun — the
*guide* must not.

**R5 — Banned strings in `07`, each with a matching `== 0` acceptance criterion:**
`SCCM` and `System Center` (write `Configuration Manager` instead); the plural form `Autopatch rings`
(write the singular `Autopatch ring` — the C11 fallback pattern is plural-only and case-insensitive);
`doc_id`; `## Version History`; the `PITFALL-` identifier prefix (use bold sub-labels instead —
precedent `01-windows-wufb-rings.md:184`); `enrolment`, `licence`, `behaviour`; `TBD`, `TODO`, `FIXME`,
`PLACEHOLDER`; `PatchMyPC`, `Chocolatey`, `Ninite`; `winget-cli`; a line-start blockquote using the
bare-noun platform lead-in instead of the full-lexicon `**Platform applicability:**` form.
<!-- planner-discipline-allow: SCCM, System Center, Autopatch rings, doc_id, Version History, PITFALL-, enrolment, licence, behaviour, TBD, TODO, FIXME, PLACEHOLDER, PatchMyPC, Chocolatey, Ninite, winget-cli -->

**R6 — C11 discipline, verified by running the audit and never by reading a keyword list.** Confirmed
by reading the code at plan time: `v1.20-audit-allowlist.json` does **not** define
`c11_ops_patterns`, so `v1.20-milestone-audit.mjs`'s four hardcoded fallback patterns are what actually
run, case-insensitively, over every file under `docs/`. Writing the singular ring form and
`Configuration Manager` means no pattern fires and no plus-or-minus-200-character keyword window needs
maintaining.

**R7 — Evidence-line shape and scope.** `**Source:** [Article title](url) (updated YYYY-MM-DD)` as a
standalone, line-start paragraph immediately **after** the claim it supports — never inside the
blockquote, never as an inline parenthetical (145 D-01, 145 D-07). **One Source line covers one
contiguous blockquote from one page, and may never span claims from two pages** — the exact defect
`146-REVIEW.md` WR-01 found (D-63, D-64, 147 D-21). Measured at plan time: `06` ships 64 such lines,
`05` ships 11. This plan quotes at least four distinct Learn pages; expect many Source lines and do not
consolidate them. Record the page's `ms.date` **and** its rendered `Last updated on` value when they
differ, and use the later one in the `(updated …)` field.

**R8 — Link discipline.** `07` links `00-overview.md`, `01-windows-wufb-rings.md#autopatch-disambiguation`,
`01-windows-wufb-rings.md#hotpatch`, `06-windows-driver-firmware-updates.md#driver-update-reporting`
and `../co-management/03-cocmgmt-migration-paths.md#autopatch-prerequisites`. It must **not** link
`08-windows-app-updates.md` — that file does not exist until plan 02, and
`check-nav-hub-links` has no allowlist, so a forward link would land a red commit. Plan 02's final task
adds the `08` entry to `07`'s Related Resources once the target exists. Anchor fragments are resolved
against the union of every explicit `<a id>` value and every heading's GitHub slug, so every
`file.md#fragment` must hit one of those.

**R9 — Nothing outside the two files in `files_modified` is touched.** No registry row, no
filename-map row, no canary bump, no ops-index row, no nav-hub wiring, no `00-overview.md` edit, no
`01-windows-wufb-rings.md` edit, no `co-management/**` edit, no `docs/operations/app-lifecycle/**`
edit, no glossary edit.

**R10 — `grep` hygiene for this corpus.** These are Markdown files: a line beginning with `#` is a
heading and is content, not a comment. Do **not** filter headings out before counting; count on the raw
file.

**R11 — Line endings.** `07` is a **new** file authored LF, matching its nearest siblings `05` and
`06` (measured `i/lf w/lf`). Do not copy bytes from `00`-`04`, which are `i/lf w/crlf` in the worktree.
On this host, `cat -A` misreports line endings inside Git Bash — measured at plan time, it printed a
bare `$` for a file that `od -c` proves is CRLF. Use `git ls-files --eol` or `od -c` to check EOL,
never `cat -A`.

**R12 — Locating by text, not by line number.** Every line number in `148-CONTEXT.md`,
`148-RESEARCH.md` and `148-PATTERNS.md` is a convenience pointer measured at `a161a43c`, not an
identity. Phase 146's coordinates went stale two commits later, inside Phase 146 itself. Locate by
quoted text, then re-measure.

**The H2 skeleton (D-15), exact order.** All names are Claude's discretion except
`## Unsupported and Anti-Feature Callouts`, which is the pinned corpus literal — copy it verbatim
including capitalization. Measured at plan time: it exists in exactly 7 corpus files, 2 of them under
`docs/operations/` (`05` and `06`), and `grep -rnE "^## .*(Limitation|Absence)" docs/` returns 0.

```
## What Windows Autopatch Is and Is Not
## Enrollment and Prerequisites
## Autopatch Groups and the Test / Last Model
## Autopatch and Windows Update Client Policies
## Update Workloads and Service Objectives
## Autopatch and Hotpatch
## Reporting and Communications
## Unsupported and Anti-Feature Callouts
## Related Resources
## External References
```

**The eight anchor ids — half of the 16-anchor Phase-151 contract (D-70, D-74).** One own-line
`<a id="..."></a>` immediately above each of the first eight H2s; none above the two footer H2s.
Measured at plan time, the sibling convention is a **short hand-authored id, not the literal heading
slug** (`06` uses `what-this-policy-does`, `approval-modes`, `driver-update-reporting`,
`configmgr-coexistence`, `unsupported-callouts`; `05` reuses `unsupported-callouts`). Use exactly:

`what-autopatch-is-and-is-not` · `enrollment-prerequisites` · `autopatch-groups-test-last` ·
`autopatch-and-update-client-policies` · `update-workloads-objectives` ·
`autopatch-hotpatch-licensing` · `autopatch-reporting` · `unsupported-callouts`

</phase_wide_rules>

<tasks>

<task type="auto">
  <name>Task 1: Fetch and record the six APP-01 claims that are PREMISE, before any section quotes them</name>

  <files>.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md</files>

  <read_first>
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md` §`MEASURED /
      PREMISE ledger` — specifically the `PREMISE` subsection, which names the six items and states for
      each one which section of `07` depends on it.
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md` §`Don't
      Hand-Roll` and §`Common Pitfalls` 1-3 — the same-page decoy, the URL that returns 200 without
      being the article, and the page that contradicts itself.
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md`
      §`Environment Availability` — `raw.githubusercontent.com` markdown source is **unavailable** for
      every page this phase needs; the HTML tag-strip is the only path. Budget no time for a
      raw-markdown route.
    - `docs/operations/patch-management/01-windows-wufb-rings.md` — the section carrying the existing
      hotpatch treatment, located by searching for the flagged-unconfirmed wording rather than by line
      number. `07` mirrors that treatment for the Windows 11 Pro case.
  </read_first>

  <action>
Retrieve six outstanding first-party claims and record them verbatim, then stop. This task authors no
corpus content.

Method is fixed by D-59 and by `147-VERIFICATION.md`'s recorded zero-fabrication result: `curl` plus a
local tag-strip plus a literal `grep`. Do **not** use a rendered-summary fetch tool — on this project
the same URL returned contradictory answers twice in one session. Before quoting from any page,
confirm its H1 and its `<link rel="canonical">` match the article you intended; a Learn slug that
returns HTTP 200 is not proof you reached the right article.

The six items, with the URL each comes from. The first two URLs are already cited inside the corpus,
so use them as-is rather than constructing a slug:

1. **Autopatch group ring-model details** — from
   `https://learn.microsoft.com/en-us/windows/deployment/windows-autopatch/deploy/windows-autopatch-groups-overview`.
   Capture verbatim whatever the page states about: the two default deployment rings and whether they
   can be renamed or removed, the minimum number of rings, the maximum rings per group, the maximum
   groups per tenant, and the Dynamic versus Assigned device-distribution options. Record each figure
   with the heading it sits under.
2. **The containment sentence** — same page. Capture the sentence stating that an Autopatch group is a
   container that includes the standalone `Update rings policy for Windows 10 and later` (and whatever
   else it lists as contained). This is the load-bearing citation for the corrected containment
   position and Phase 151 SC#2 consumes it, so it must be exact.
3. **Workload service objectives** — from
   `https://learn.microsoft.com/en-us/windows/deployment/windows-autopatch/overview/windows-autopatch-overview`.
   Capture the quality-update objective, the Microsoft 365 Apps channel objective and the channel it
   names, the Microsoft Edge channel, and the Microsoft Teams channel — each verbatim with its
   percentage where the page gives one.
4. **The Autopatch reporting surface list** — same page (follow to a reporting page if the overview
   only links one, recording the URL you actually landed on). Capture the names of the Autopatch-specific
   reporting surfaces and the communications channel.
5. **The April 2025 feature-activation removal and the tiered entitlement table** — search the Autopatch
   overview, the prerequisites page and the Autopatch what's-new page for the activation-removal
   statement and its date. The tiered entitlement matrix (the Business Premium / A3-plus / E3-plus / F3
   columns against the Releases / Update rings / Quality updates / Feature updates rows) was already
   seen on the prerequisites page this research session; re-confirm it. **If the April 2025 date is not
   found on a first-party page, record it as not found and ship the absence — do not carry the date
   forward on the strength of a research ledger.**
6. **The Autopilot device-preparation quotes** — the two sentences `08` needs in plan 02: the one
   directing that the PowerShell script be configured to run in the System context, and the one stating
   that only Microsoft Store apps supporting the package manager are supported. Reach the tutorial from
   the device-preparation landing page rather than guessing its slug; the corpus already cites
   `https://learn.microsoft.com/en-us/autopilot/device-preparation/known-issues`, which links its
   siblings. Record the URL you actually landed on.

Append the results to `148-RESEARCH.md` as a new final section titled `## Plan-Time Fetch Addendum
(Plan 148-01)`. For every item record: the exact URL fetched, the page's `ms.date`, its rendered
`Last updated on` value, the heading each quote sits under, and the quote itself. Where a claim was
**not** found, record it explicitly as not found with the search terms used — a documented silence is a
result, and a missing item must never be silently inherited from the PREMISE ledger.

Pin every quote by its surrounding heading, never by grep hit. Two decoy classes are already recorded
for this phase: a page whose only mentions of a term are policy carve-outs that invite the opposite
conclusion, and a stale page revision. Two stale-revision tells for the hotpatch page are recorded in
`148-RESEARCH.md`; both were measured absent from the current copy, so if either reappears you have
fetched a cached or stale revision and must refetch.

Do not add a platform-applicability blockquote or any line-start blockquote using the bare-noun
platform lead-in to this addendum — `V-54-27` walks `.planning/` as well as `docs/`.
  </action>

  <verify>
    <automated>node scripts/validation/check-phase-54.mjs | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md` contains the
      literal `## Plan-Time Fetch Addendum (Plan 148-01)`.
    - The addendum contains at least 6 lines matching `^\*\*URL:\*\*` or an equivalent explicit
      per-item URL field — one per fetch item — and each item records both an `ms.date` value and a
      `Last updated on` value, or an explicit statement that the page exposes none.
    - Every item in the addendum is marked either with a verbatim quote **or** with an explicit
      not-found statement naming the search terms used. `grep -c "not found" ` on the addendum is
      permitted to be 0 or more; what is not permitted is an item with neither a quote nor a
      not-found line.
    - `grep -c "windows-autopatch-groups-overview" ` on the addendum returns >= 1 and
      `grep -c "windows-autopatch-overview" ` returns >= 1.
    - Per D-61, every quote in the addendum records the heading it sits under, not just the URL:
      each item carries a heading field. A grep-hit-only citation is not acceptable — one of this
      phase's pages is measured to mention a term only in policy carve-outs that invite the opposite
      conclusion, and another had a stale revision reach two agents.
    - Per D-60, a page that fails to yield its content because it renders client-side is recorded as a
      **retrieval failure**, never as evidence of absence. If any item hits that case, the addendum
      says so in those words.
    - `node scripts/validation/check-phase-54.mjs` exits 0 and prints `32 passed, 0 failed, 0 skipped`
      — `V-54-27` walks `.planning/`, so the addendum must not have introduced a bare platform
      blockquote lead-in.
    - `git status --porcelain` shows exactly one modified file:
      `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md`. No file
      under `docs/` has changed.
  </acceptance_criteria>

  <done>All six PREMISE items are either quoted verbatim with their URL and dates, or explicitly
  recorded as not found; no corpus file has been touched; check-phase-54 is still 32/0/0.</done>
</task>

<task type="tracer">
  <name>Task 2: End-to-end slice — 07 exists, every anchor and cross-link resolves, C11 is clean, and one section ships at final quality</name>

  <precondition>The `## Plan-Time Fetch Addendum (Plan 148-01)` section exists in `148-RESEARCH.md` (Task 1 complete).</precondition>

  <files>docs/operations/patch-management/07-windows-autopatch.md</files>

  <read_first>
    - `docs/operations/patch-management/06-windows-driver-firmware-updates.md`, lines 1 through 50 —
      the five frontmatter fields in order, the full-lexicon applicability blockquote, the H1, and the
      opening paragraph shape. This is the exact structural analog: a patch-management sibling outside
      `PATCH_FILES`, `platform: Windows`, `applies_to: all`, `audience: admin`, no `doc_id`, no Version
      History, LF in the worktree.
    - The same file's `## Related Resources` and `## External References` footer — the entry shape,
      the em-dash description convention, and the link-text conventions. Located near the end of the
      file; locate by heading, not by line number.
    - The same file's first `<a id>`-above-H2 pair and its first blockquote-then-`**Source:**` triplet
      — the two shapes this plan replicates 8 times and dozens of times respectively.
    - `docs/operations/patch-management/01-windows-wufb-rings.md` — confirm by `grep -n '<a id='` that
      the file carries exactly four anchors and that `autopatch-disambiguation` and `hotpatch` are two
      of them. There is no `ring-terminology` anchor in this file; that id lives in `00-overview.md`
      and linking `01#ring-terminology` takes `check-nav-hub-links` from 0/0 to a corpus-link failure.
    - `docs/operations/co-management/03-cocmgmt-migration-paths.md` — confirm the
      `autopatch-prerequisites` anchor exists and read the Pre-Enablement Checklist it introduces, so
      `07`'s precedence sentence describes what `03` actually carries. **Read only. This file is never
      edited by any v1.21 phase.**
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md` §`Verbatim
      Quote Bank` → `For 07-windows-autopatch.md`, plus the `## Plan-Time Fetch Addendum` written by
      Task 1.
  </read_first>

  <action>
Create `docs/operations/patch-management/07-windows-autopatch.md` as a complete, structurally final
document with **one** body section written at shipping quality. Everything the later tasks add is
prose inside sections that already exist — no structural change after this task.

Author, in order:

1. **Frontmatter**, five fields in `06`'s order: `last_verified`, `review_by`, `applies_to: all`,
   `audience: admin`, `platform: Windows`. Set `last_verified` to today's date as reported by the
   system, and compute `review_by` as that date **plus 60 days, by arithmetic** — do not copy `06`'s
   stamps, which are already stale. If execution spans midnight, all three commits in this phase take
   the date of commit 1. Add no `doc_id` field: Phase 152 SC#3 makes non-enrollment permanent for
   `docs/operations/`, and adding one would take C17 from 234 files to 235.
2. **The platform-applicability blockquote**, immediately after the frontmatter, using the full
   lexicon lead-in `> **Platform applicability:**` — the bare-noun variant is a corpus-wide negative
   that walks both `docs/` and `.planning/`. State that this guide is Windows-specific and covers the
   Autopatch service as its own surface: enrollment and service prerequisites, the group model, the
   containment position, the update workloads and their objectives, the entitlement comparison, and
   the reporting surfaces. Route the reader onward: to `00-overview.md` for the cross-platform
   overview, to `01-windows-wufb-rings.md#autopatch-disambiguation` for ring topology and the
   Autopatch-versus-standalone-rings disambiguation, and to
   `01-windows-wufb-rings.md#hotpatch` for the in-memory patching servicing model.
3. **The H1** `# Windows Autopatch`.
4. **An opening paragraph** that states the guide's scope boundary explicitly: ring topology stays in
   `01`, workload-slider prerequisites stay in `co-management/03`, drivers and firmware stay in `06`,
   and this guide owns the Autopatch service itself.
5. **All ten H2s from the skeleton in `<phase_wide_rules>`, in order, each of the first eight preceded
   by its own-line anchor from the id list there.** Sections 2 through 8 may be left as a single
   placeholder-free holding sentence naming what the section will carry — a plain declarative sentence,
   never a token from the banned list in R5.
6. **`## What Windows Autopatch Is and Is Not`, written in full.** This is the tracer's real content.
   It must: define Autopatch as a service rather than a policy; state the corrected containment
   position positively using the containment quote Task 1 fetched, with its own `**Source:**` line;
   state that the disambiguation between an Autopatch group and a standalone update-rings policy is
   authored in `01` and link that anchor; state that this guide does not re-author ring topology; and
   state the precedence rule for prerequisites — `co-management/03` owns the workload-slider chain,
   `07` owns the Autopatch service prerequisites — linking `03`'s anchor. Do **not** restate Phase 145's
   mutual-exclusivity correction; the corpus is already clean of it and the positive statement is what
   Phase 151 SC#2 consumes.
7. **`## Related Resources`** with entries for `00-overview.md`, `01-windows-wufb-rings.md`,
   `06-windows-driver-firmware-updates.md`, and `../co-management/03-cocmgmt-migration-paths.md`.
   Per R8, **no entry for `08-windows-app-updates.md`** — that file does not exist yet. Keep each
   `[text](url)` construct entirely on one physical line; the em-dash description after it may wrap.
8. **`## External References`** with the first-party Learn URLs this file cites, in the `06` footer
   shape.

Write the singular ring form throughout and write `Configuration Manager` in full wherever the
on-premises product is named. Use no code fences anywhere.
  </action>

  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs && node scripts/validation/v1.20-milestone-audit.mjs | tail -3 && node scripts/validation/c17-eee-contract.mjs | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - `docs/operations/patch-management/07-windows-autopatch.md` exists.
    - `grep -c '^## ' docs/operations/patch-management/07-windows-autopatch.md` returns exactly `10`.
    - `grep -c '<a id=' docs/operations/patch-management/07-windows-autopatch.md` returns exactly `8`.
    - `grep -c '^```' docs/operations/patch-management/07-windows-autopatch.md` returns exactly `0`.
    - `grep -c 'doc_id' docs/operations/patch-management/07-windows-autopatch.md` returns `0`.
    - The file contains the literals `## Unsupported and Anti-Feature Callouts` — the pinned corpus
      literal, copied verbatim including capitalization per D-24, whose census was re-measured at plan
      time as 7 files with 2 under `docs/operations/` — `# Windows Autopatch`,
      `> **Platform applicability:**`, `01-windows-wufb-rings.md#autopatch-disambiguation`,
      `01-windows-wufb-rings.md#hotpatch` and
      `03-cocmgmt-migration-paths.md#autopatch-prerequisites`.
    - `grep -c '08-windows-app-updates.md' docs/operations/patch-management/07-windows-autopatch.md`
      returns `0`.
    - `grep -c 'ring-terminology' docs/operations/patch-management/07-windows-autopatch.md` returns `0`
      — per D-05 that id is a phantom on `01`; it lives in `00-overview.md`, and a live probe of the
      wrong form took `check-nav-hub-links` from 0/0 to a corpus-link failure.
    - All eight anchor ids appear exactly once each:
      `for a in what-autopatch-is-and-is-not enrollment-prerequisites autopatch-groups-test-last autopatch-and-update-client-policies update-workloads-objectives autopatch-hotpatch-licensing autopatch-reporting unsupported-callouts; do grep -c "<a id=\"$a\">" docs/operations/patch-management/07-windows-autopatch.md; done`
      prints `1` eight times.
    - The file contains at least one line matching `^\*\*Source:\*\*` and every such line is at the
      start of its own line (`grep -c '^\*\*Source:\*\*'` equals `grep -c '\*\*Source:\*\*'`).
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 and prints
      `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total` — this is the first time anything
      in the corpus links into an anchor of `01`, so it is proven here, not assumed.
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `16 passed, 0 failed`.
    - `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations` —
      not 235.
    - `git ls-files --eol docs/operations/patch-management/07-windows-autopatch.md` reports `w/lf`.
    - `grep -cE 'SCCM|System Center|Autopatch rings|enrolment|licence|behaviour|TBD|TODO|FIXME|PLACEHOLDER|PITFALL-|PatchMyPC|Chocolatey|Ninite|winget-cli'
      docs/operations/patch-management/07-windows-autopatch.md` returns `0`.
    - `grep -c '^## Version History' docs/operations/patch-management/07-windows-autopatch.md` returns `0`.
  </acceptance_criteria>

  <done>07 exists with its final structure — 10 H2s, 8 anchors, 0 fences, no doc_id — one section is
  shipping-quality, every outbound link and anchor fragment resolves, and the three live gates are at
  their plan-time baselines.</done>
</task>

<task type="auto">
  <name>Task 3: Expand the APP-01 enrollment, group-model and client-policies sections from the fetched material</name>

  <precondition>`docs/operations/patch-management/07-windows-autopatch.md` exists with all ten H2s and eight anchors (Task 2 complete).</precondition>

  <files>docs/operations/patch-management/07-windows-autopatch.md</files>

  <read_first>
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md`
      §`Verbatim Quote Bank` → `For 07-windows-autopatch.md`, and the
      §`Secondary verifications` entry answering whether Office Click-to-Run is a documented Autopatch
      prerequisite — it carries the planner instruction for this section verbatim.
    - The `## Plan-Time Fetch Addendum (Plan 148-01)` section of the same file — items 1, 2 and 5.
    - `docs/operations/co-management/03-cocmgmt-migration-paths.md` — read the Pre-Enablement Checklist
      so `07` states what `03` actually carries and does not duplicate it. **Read only.**
    - `docs/operations/patch-management/06-windows-driver-firmware-updates.md` — one full body section,
      to match the density, blockquote cadence and Source-line rhythm of a shipping sibling.
  </read_first>

  <action>
Fill three sections of `07`.

**`## Enrollment and Prerequisites`.** This section owns the Autopatch **service** prerequisites that
`co-management/03` has never carried. Cover, each with its own Source line scoped to the single page it
came from: the Entra ID P1 or P2 requirement; corporate-owned devices only, with Windows personally
owned devices blocked at registration; the 28-day check-in requirement; the required diagnostic-data
levels; the Long-Term Servicing Channel restriction (registration is supported, but only the Windows
quality-updates workload is managed and no feature updates are offered); the app-only authentication
requirement; and the rule that Entra groups used here cannot be user-based. Carry the two verbatim
prerequisites sentences the research file already holds: the one stating that devices must be managed
by Intune or by co-management and that devices managed only by the on-premises product are not
supported, and the one stating that the on-premises product must be cloud-attached. Then state the
precedence rule in prose and link `03`'s anchor for the workload-slider chain.

Ship the entitlement tier matrix (the four subscription columns against the four capability rows) as a
table, and — if and only if Task 1 found it on a first-party page — the feature-activation removal with
its date. If Task 1 recorded it as not found, state the absence in one sentence and do not carry the
date.

**The documented conflict.** The prerequisites page names two co-management workloads in its general
prerequisites table and then, in its own on-premises-product section, refers to "the three workloads
that Windows Autopatch requires" without naming a third; the page's own cross-reference for that
section is a self-bookmark back to itself, so there is no second page to consult. Write both sides,
each quoted under the heading it sits under, and state plainly that the page does not settle the count.
Assert nothing about the identity of the unnamed third workload — measured on that page, the Office
Click-to-Run terms occur zero times, and filling the gap from general co-management documentation is
exactly the failure this instruction exists to prevent. Do **not** edit `co-management/03`, whose own
wording on this point is falsified; plan 03 files that to the backlog with the measurement attached.

**`## Autopatch Groups and the Test / Last Model`.** From Task 1's fetch items 1 and 2: the two default
deployment rings and the fact that they cannot be renamed or removed; the minimum ring count; the
maximum rings per group and the maximum groups per tenant; and the Dynamic versus Assigned distribution
options. Every figure carries the heading it was fetched under. Write the containment position here in
its fullest form — an Autopatch group is a container that includes the standalone update-rings policy —
and link `01`'s disambiguation anchor for the topology itself rather than restating it.

**`## Autopatch and Windows Update Client Policies`.** The corrected containment position as an
operational statement: what an administrator running both surfaces should expect, which surface owns
which setting, and where the topology is authored. Positive framing only; do not restate the
mutual-exclusivity correction Phase 145 already cleared from all five sites.

Write the singular ring form throughout. Keep every table row on one physical line.
  </action>

  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs && node scripts/validation/v1.20-milestone-audit.mjs | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - The file contains the literal `Test` and the literal `Last` within the body of the
      `## Autopatch Groups and the Test / Last Model` section:
      `awk '/^## Autopatch Groups and the Test \/ Last Model/,/^## Autopatch and Windows Update Client Policies/' docs/operations/patch-management/07-windows-autopatch.md | grep -c 'Last'`
      returns >= 1.
    - The same section range contains at least one line matching `^\*\*Source:\*\*`.
    - The `## Enrollment and Prerequisites` section range contains the literal
      `03-cocmgmt-migration-paths.md#autopatch-prerequisites`.
    - The `## Enrollment and Prerequisites` section range contains both sides of the workload-count
      conflict: `grep -c 'Device configuration'` returns >= 1 and `grep -c 'three workloads'` returns
      >= 1 over that range.
    - `grep -ci 'click-to-run' docs/operations/patch-management/07-windows-autopatch.md` — if the count
      is greater than 0, every occurrence must sit in a sentence that states the term does **not**
      appear on the prerequisites page. There must be no sentence asserting it is a prerequisite.
    - `grep -c 'mutually exclusive' docs/operations/patch-management/07-windows-autopatch.md` returns `0`.
    - `grep -cE 'SCCM|System Center|Autopatch rings|enrolment|licence|behaviour|TBD|TODO|FIXME|PLACEHOLDER|PITFALL-|PatchMyPC|Chocolatey|Ninite|winget-cli'
      docs/operations/patch-management/07-windows-autopatch.md` returns `0`.
    - `grep -c '^## ' docs/operations/patch-management/07-windows-autopatch.md` still returns `10` and
      `grep -c '<a id=' ` still returns `8` — no structural drift.
    - `node scripts/validation/check-nav-hub-links.mjs` prints `0 hub-presence failure(s), 0
      corpus-link failure(s), 0 total`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `16 passed, 0 failed`.
  </acceptance_criteria>

  <done>Enrollment and service prerequisites, the group model with its fetched figures, the containment
  position and the documented workload-count conflict all ship, each sourced to the single page it came
  from; co-management/03 is untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Expand the workload, entitlement-comparison, reporting and callouts sections, then commit</name>

  <precondition>`docs/operations/patch-management/07-windows-autopatch.md` carries filled Enrollment, Groups and Client-Policies sections (Task 3 complete).</precondition>

  <files>docs/operations/patch-management/07-windows-autopatch.md</files>

  <read_first>
    - `.planning/phases/148-application-update-management-winget-routing/148-RESEARCH.md`
      §`Secondary verifications` → the entry comparing the two entitlement lists, which carries both
      lists verbatim with their pages and dates and names the three genuine differences.
    - The same file's `## Plan-Time Fetch Addendum (Plan 148-01)` items 3 and 4.
    - `docs/operations/patch-management/01-windows-wufb-rings.md` — the existing flagged-unconfirmed
      treatment of the Windows 11 Pro question, located by searching for the flagged wording. `07`
      mirrors it rather than settling it.
    - `docs/operations/patch-management/06-windows-driver-firmware-updates.md`
      `## Unsupported and Anti-Feature Callouts` — the bold-sub-label shape this section replicates,
      and its `<a id="driver-update-reporting">` anchor, which `07`'s reporting section links.
  </read_first>

  <action>
Fill the remaining four sections and land commit 1 of three.

**`## Update Workloads and Service Objectives`.** From Task 1's fetch item 3: the quality-update
objective and its percentage, the Microsoft 365 Apps objective with the channel it names, the Microsoft
Edge channel, and the Microsoft Teams channel. This section **owns** the Edge and Teams coverage for
the whole phase; the application-update guide states only the Autopatch-enrolled condition and links
back here. Make the channel pin explicit as prose — an Autopatch-enrolled fleet is steered toward the
named enterprise channel, which directly constrains the one-channel-per-device rule the sibling guide
documents. Each objective gets its own Source line scoped to the page it came from.

**`## Autopatch and Hotpatch`.** This is ROADMAP SC#2 and it is the section a reader will be checked
against. Ship **both** entitlement lists side by side, each as its own blockquote in its own source
order, each followed by its own `**Source:**` line pointing at the single page it came from. Never let
one Source line cover both lists. Then state, in the guide's own voice, the three genuine differences:
the entry present only on the Autopatch side, the entry present only on the hotpatch side, and the fact
that one list is Windows 10 and 11 while the other is Windows 11 only.

Follow that with the explicit positive statement the requirement mandates — that an Autopatch
entitlement is **necessary but not sufficient** for hotpatch. Write it as a positive; do not rely on a
bare negative.

Handle the Windows 11 Pro edition question exactly as `01` already handles it: flagged unconfirmed.
Measured on the sole first-party page, that edition name occurs zero times, and the page states the
prerequisite as an entitlement list rather than an edition list, so it neither supports nor refutes
eligibility. Say that, and settle nothing. Two stale-revision tells for that page are recorded in the
research file; if either appears in the copy you read, you have a stale revision and must refetch.

**`## Reporting and Communications`.** From Task 1's fetch item 4: the Autopatch-specific reporting
surfaces and the communications channel, each named as the page names it. Scope this section to
Autopatch surfaces only and link `06-windows-driver-firmware-updates.md#driver-update-reporting` for
driver reporting — measured, the two surfaces are disjoint by workload.

**`## Unsupported and Anti-Feature Callouts`.** Bold sub-labels, no numbered pitfall identifiers. Carry
at minimum: the Long-Term Servicing Channel feature-update absence; the restriction that Entra groups
used here cannot be user-based; the unconfirmed Windows 11 Pro hotpatch case; the documented
workload-count conflict as an unresolved documentation state; and — only if the fetch established it —
the sovereign-cloud availability position. The sovereign-cloud claim currently sits in a planning
ledger as an unquoted, undated summary paragraph filed under gaps to close. **If Task 1 did not find a
first-party source for it, ship it as a documented silence: state that no first-party statement of
Autopatch availability in those clouds was found, and assert neither availability nor exclusion.**

Then replace the seven holding sentences left by Task 2 wherever any survive, re-read the whole file
once for prose accuracy against the quote bank, and commit as commit 1 of three with a
`docs(148):`-shaped message naming the file and the two requirement IDs.
  </action>

  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs && node scripts/validation/v1.20-milestone-audit.mjs | tail -3 && node scripts/validation/check-phase-144.mjs | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - The `## Autopatch and Hotpatch` section range
      (`awk '/^## Autopatch and Hotpatch/,/^## Reporting and Communications/'`) contains **at least two**
      lines matching `^\*\*Source:\*\*` — one per entitlement list, never one covering both.
    - That same range contains the literal `VDA` and the literal `Windows 365 Enterprise`, and contains
      the literal `necessary but not sufficient`.
    - `grep -ci 'if you have autopatch you have hotpatch' docs/operations/patch-management/07-windows-autopatch.md`
      returns `0`.
    - That same range contains the literal `Windows 11 Pro` **only** in a sentence that also contains
      an unconfirmed or not-stated qualifier; there is no sentence asserting Pro eligibility or Pro
      ineligibility.
    - `grep -c '26100.2033' docs/operations/patch-management/07-windows-autopatch.md` returns `0` and
      `grep -c 'x64 (AMD' ` returns `0` — the two stale-revision tells must not have been transcribed.
    - The `## Update Workloads and Service Objectives` range contains the literals `Microsoft Edge` and
      `Microsoft Teams`.
    - The `## Reporting and Communications` range contains the literal
      `06-windows-driver-firmware-updates.md#driver-update-reporting`.
    - `grep -c '^## ' docs/operations/patch-management/07-windows-autopatch.md` returns `10`;
      `grep -c '<a id=' ` returns `8`; `grep -c '^```' ` returns `0`.
    - `grep -cE 'SCCM|System Center|Autopatch rings|enrolment|licence|behaviour|TBD|TODO|FIXME|PLACEHOLDER|PITFALL-|PatchMyPC|Chocolatey|Ninite|winget-cli'
      docs/operations/patch-management/07-windows-autopatch.md` returns `0`.
    - `grep -c '08-windows-app-updates.md' docs/operations/patch-management/07-windows-autopatch.md`
      returns `0`.
    - Per D-23, the sovereign-cloud availability position appears **only** if Task 1 found a
      first-party source for it. If Task 1 recorded it as not found, the callouts range contains a
      sentence stating that no first-party statement was found, and contains no sentence asserting
      either availability or exclusion.
    - Per D-47, no product or project is named:
      `grep -ciE 'patchmypc|chocolatey|ninite|scappman|robopack|ivanti|techdirect'` returns `0`;
      community tooling and third-party patch products appear only as a category.
    - `node scripts/validation/check-nav-hub-links.mjs` prints `0 hub-presence failure(s), 0
      corpus-link failure(s), 0 total`.
    - `node scripts/validation/check-phase-54.mjs` prints `32 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/check-phase-53.mjs` prints `26 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `16 passed, 0 failed`.
    - `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations`.
    - `node scripts/validation/check-phase-144.mjs` prints `101 PASS, 0 FAIL, 0 SKIPPED`.
    - `git log -1 --name-only` lists exactly two paths: the new guide and `148-RESEARCH.md`. No file
      under `docs/operations/co-management/`, `docs/operations/app-lifecycle/`, `docs/_registry/` or
      `docs/admin-setup-apv2/` appears.
  </acceptance_criteria>

  <done>07 ships complete at 10 H2s, 8 anchors and 0 fences; both entitlement lists are shown differing
  with independent Source lines; the necessary-but-not-sufficient statement is present; all nine gates
  and the apex are at their plan-time baselines; commit 1 of three is landed.</done>
</task>

</tasks>

<artifacts_this_phase_produces>

Every symbol below is **new in this plan** and does not exist at `a161a43c`. The source-grounding pass
excludes these from drift verification.

**New file paths**
- `docs/operations/patch-management/07-windows-autopatch.md`

**New H2 headings (each becomes a link-checked GitHub anchor slug)**
- `## What Windows Autopatch Is and Is Not`
- `## Enrollment and Prerequisites`
- `## Autopatch Groups and the Test / Last Model`
- `## Autopatch and Windows Update Client Policies`
- `## Update Workloads and Service Objectives`
- `## Autopatch and Hotpatch`
- `## Reporting and Communications`
- `## Unsupported and Anti-Feature Callouts`
- `## Related Resources`
- `## External References`

**New `<a id>` anchors (8 — half of the 16-anchor Phase-151 contract)**
`what-autopatch-is-and-is-not` · `enrollment-prerequisites` · `autopatch-groups-test-last` ·
`autopatch-and-update-client-policies` · `update-workloads-objectives` ·
`autopatch-hotpatch-licensing` · `autopatch-reporting` · `unsupported-callouts`

**New H1 (Phase 152 registry Title input, not created here)**
- `# Windows Autopatch`

**New planning-artifact section**
- `## Plan-Time Fetch Addendum (Plan 148-01)` in `148-RESEARCH.md`

**New nav / overview rows:** none in this plan. All `00-overview.md` insertions belong to plan 03.

</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `learn.microsoft.com` HTML → the shipped corpus | Untrusted external bytes cross into a knowledge base that IT administrators act on and that is published to SharePoint and Copilot Studio. This is the only real boundary in this plan. |
| planning artifacts → corpus prose | Research ledgers and CONTEXT decisions are ingested text; a ledger row is a claim, not evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-148-01 | Tampering | `07-windows-autopatch.md` prose + `**Source:**` lines | high | mitigate | Write from the byte-verified quote bank and Task 1's fetch addendum only. One Source line per contiguous blockquote per page (R7). Task 1 is a hard precondition for Tasks 3-4 so no `PREMISE` row reaches the page as a quotation. No validator parses evidence lines, so the phase verifier's re-fetch-and-diff is the only catching gate — it is named in the plan. |
| T-148-02 | Spoofing | Learn URL retrieval in Task 1 | medium | mitigate | Verify H1 and `<link rel="canonical">` before quoting; never construct a slug. Reach the device-preparation tutorial from an already-cited sibling page rather than guessing. Record the URL actually landed on. |
| T-148-03 | Tampering | stale page revision served from cache | medium | mitigate | Two recorded stale-revision tells for the hotpatch page are asserted absent in Task 4's acceptance criteria; either appearing means refetch. Record `ms.date` **and** rendered `Last updated on` for every quote. |
| T-148-04 | Information disclosure | credentials / secrets / endpoints | low | accept | No credential, secret or endpoint is added or read by this plan; it authors Markdown and runs local validators. Canon security controls are covered by `/gsd-secure-phase`; not minted here. |
| T-148-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs in any ecosystem. `148-RESEARCH.md` §`Package Legitimacy Audit` records "Not applicable"; no `package.json`, `requirements.txt` or `Cargo.toml` is touched, so the legitimacy gate has no rows to check. |

ASVS level 1; blocking threshold `high`. T-148-01 is the only `high` item and is dispositioned
`mitigate` with a named mechanism.
</threat_model>

<assumptions_and_open_items>

**Edge-probe arithmetic (no silent drops).** The deterministic probe returned **19** applicable edges
across APP-01..APP-06, all `unresolved`. Plan 01 owns the **4** rows scoped to APP-01 and APP-02;
plan 02 owns the remaining 15. Of plan 01's 4: **3** are authored into `must_haves.truths` as
resolved/explicit (APP-02 adjacency, APP-02 empty, APP-02 ordering) and **1** is surfaced below as a
flagged assumption. 3 + 1 = 4. Across the phase: 16 explicit + 2 backstop + 1 flagged = **19**.

**Flagged assumption — the one unresolved edge.**
- **APP-01 / `unclassified`.** The probe could not classify APP-01 into any edge category and its
  disposition rule forbids auto-resolving an unclassified row with a backstop. It is carried here
  explicitly rather than dropped. The substantive risk it stands for is real and named in the research
  file: APP-01's ring-model, containment, service-objective and reporting content rested on `PREMISE`
  rows, not measurements. **Task 1 is the mitigation** — it converts all six to measured or to an
  explicit not-found — but the residual risk (that an APP-01 clause needs a seventh fetch nobody
  enumerated) stays open and is the reason Task 1's acceptance criteria require a not-found line rather
  than silence.

**Carried research assumptions that bear on this plan.**
- The six named `PREMISE` items are the complete outstanding set for APP-01. Medium risk; mitigated by
  Task 1 running before any dependent section is authored.
- The Autopatch sovereign-cloud availability claim has no first-party source at plan time. Task 4 ships
  it as a documented silence unless Task 1 finds one.
- The April 2025 feature-activation removal date is unverified. Task 4 ships the absence unless Task 1
  finds a first-party statement.
- `STACK.md` §C-2's Windows 11 Pro hotpatch question is **not** discharged by this phase and stays in
  the backlog; `07` mirrors `01`'s flagged treatment.

</assumptions_and_open_items>

<assumption_delta_decision>
The deterministic detector fired one `chosen`-family signal on the phase goal ("an admin can **choose**
an app-update channel"): a value the corpus previously derived from the OS-update model has become a
configuration an administrator selects.

- **Primary noun:** **the update-governance surface** — which mechanism keeps a thing current, at what
  cadence, and what it cannot do.
- **Decision:** `promote`. The generalized noun becomes primary and the previously-primary app
  **object model** (app types, packaging, supersedence graphs, dependency chains) is demoted to a
  detail owned by `docs/operations/app-lifecycle/`. This is not new: CONTEXT D-33 already reversed the
  draft's install-versus-currency seam after three corpus measurements falsified it, and the
  Architectural Responsibility Map in `148-RESEARCH.md` records the promotion row by row.
- **Rationale:** `app-lifecycle/00-overview.md` already publishes `update` as a lifecycle state and
  `app-lifecycle/01` already documents unattended auto-update on the extension check-in timer, so an
  install-versus-currency seam would have cut through concepts the corpus had already assigned.
- **Invariant, already required rather than newly added:** every guide in `patch-management/` states
  which mechanism owns currency for its subject and names the file it defers to for the object model
  (CONTEXT D-36). Plan 02's acceptance criteria assert that seam sentence literally.

Advisory only; this records and does not block.
</assumption_delta_decision>

<verification>
Run at the end of this plan, all from the repository root:

1. `node scripts/validation/check-phase-54.mjs` → `32 passed, 0 failed, 0 skipped`
2. `node scripts/validation/check-phase-53.mjs` → `26 passed, 0 failed, 0 skipped`
3. `node scripts/validation/check-phase-59.mjs` → `36 passed, 0 failed, 0 skipped`
4. `node scripts/validation/check-nav-hub-links.mjs` → `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`
5. `node scripts/validation/c17-eee-contract.mjs` → `234 files checked, 0 with violations`
6. `node scripts/validation/v1.20-milestone-audit.mjs` → `16 passed, 0 failed`
7. `node scripts/pipeline/build-filename-map.mjs --self-test` → `8 passed, 0 failed`
8. `node scripts/pipeline/build-publish-bundle.mjs --self-test` → `15 passed, 0 failed`
9. `node scripts/validation/check-phase-144.mjs` → `101 PASS, 0 FAIL, 0 SKIPPED`

A green run of all nine proves only that nothing regressed. It does **not** prove `07` is correct — R2
records exactly which assertions do and do not reach this file. The acceptance criteria on each task,
and the phase verifier's re-fetch-and-diff, are what prove the content.

**Remediation is expected, not a failure.** Neither predecessor phase finished in the number of plans
it started with: Phase 146 shipped 18 commits against 3 plan files, and Phase 147 needed two follow-up
commits after two clean plans. A remediation commit on `07` after code review is inside this plan's
budget.
</verification>

<success_criteria>
- `docs/operations/patch-management/07-windows-autopatch.md` exists at 10 H2s, 8 anchors, 0 code
  fences, no `doc_id`, LF line endings, and frontmatter whose `review_by` minus `last_verified` equals
  exactly 60 days by arithmetic.
- ROADMAP SC#1 is satisfied: enrollment mechanics, the `Test` / `Last` model, the Autopatch application
  workloads and the reporting surfaces are documented; `co-management/03` and the disambiguation anchor
  are cross-linked; neither is re-authored; `check-phase-53.mjs` exits 0.
- ROADMAP SC#2 is satisfied: both entitlement lists ship side by side with independent Source lines,
  the two asymmetric entries are visible, and the necessary-but-not-sufficient statement is present.
- All nine gates and the apex read at their plan-time baselines.
- Commit 1 of three is landed, touching only the two files in `files_modified`.
</success_criteria>

<output>
Create `.planning/phases/148-application-update-management-winget-routing/148-01-SUMMARY.md` when done.

Record in it: the exact `last_verified` / `review_by` dates used (plan 02 and plan 03 must reuse the
same execution date, D-76); the eight anchor ids as shipped; the results of all six Task-1 fetches
including any recorded as not found; whether the sovereign-cloud claim and the April 2025 date shipped
or were stated as absences; and the nine gate figures measured after the commit.
</output>
