---
phase: 104-ios-ipados-802-1x-admin-setup-wi-fi-wired
plan: 02
type: execute
wave: 2
depends_on: ["104-01"]
files_modified:
  - docs/admin-setup-8021x/00-overview.md
autonomous: true
requirements: [DOT1X-06]

must_haves:
  truths:
    - "GOAL discoverability: the local 00-overview.md platform-guide list gains item 5 as a descriptive one-liner linking 05-ios.md, so a reader navigating the 802.1X folder entry point can discover the newly authored iOS/iPadOS guide"
    - "SC1 + SC2 + SC3 surfaced in the entry: the item-5 description names the iOS-distinguishing content (all three EAP methods; MAC-address randomization disabled for NAC, iOS 14+; wired profile targets M-series iPads with USB Ethernet; wired SCEP-only constraint), matching the existing item-3/item-4 descriptive-one-liner style"
    - "Navigation-last (local-only): only the in-folder 00-overview.md platform-list entry + Change History row are edited; no capability-matrix rows and no global nav-hub files (index.md, quick-refs, decision-trees) are touched (those are Phase 109)"
    - "The placeholder line '5–7. Platform guides (Phase 104–106) -- entries added as each guide is authored.' is replaced by item 5 (iOS/iPadOS) plus a '6–7. Platform guides (Phase 105–106) -- entries added as each guide is authored.' continuation, matching the existing item-1/2/3/4 numbered-list link style (bold linked title, double-dash, no trailing period)"
    - "The Mermaid '3–7. Platform Guides' diagram node is left unchanged after inspection — it references a range that stays valid until all five guides are added; the Scope section, wired-availability note, and See Also are unchanged"
    - "Harness allowlisting: 00-overview.md is a pre-existing file; the edit must be recorded for harness allowlisting, the same pattern used for the Phase 103 item-4 edit, the Phase 102 item-3 edit, and the Phase 101 glossary see-also edits"
  artifacts:
    - path: "docs/admin-setup-8021x/00-overview.md"
      provides: "Local 802.1X folder entry point with the iOS/iPadOS platform-guide entry (item 5)"
      contains: "](05-ios.md)"
  key_links:
    - from: "docs/admin-setup-8021x/00-overview.md"
      to: "docs/admin-setup-8021x/05-ios.md"
      via: "platform-guide list item 5"
      pattern: "\\]\\(05-ios\\.md\\)"
---

<objective>
Add the iOS/iPadOS platform-guide entry to the local `docs/admin-setup-8021x/00-overview.md` numbered list (item 5), replacing the reserved "5–7. Platform guides (Phase 104–106)" placeholder line with the authored iOS/iPadOS entry plus a "6–7. Platform guides (Phase 105–106)" continuation for the still-pending platform guides, and appending a Change History row.

Purpose: Make the just-authored `05-ios.md` discoverable from the 802.1X folder entry point. This is the ONLY in-scope navigation edit for Phase 104 — capability matrices and global nav hubs are deferred to Phase 109 (navigation-last).

Output: `docs/admin-setup-8021x/00-overview.md` (EDIT — one numbered-list line replaced with two lines + a Change History row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/admin-setup-8021x/00-overview.md
@.planning/phases/104-ios-ipados-802-1x-admin-setup-wi-fi-wired/104-PATTERNS.md
@.planning/phases/104-ios-ipados-802-1x-admin-setup-wi-fi-wired/104-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the iOS/iPadOS platform-guide entry (item 5) to 00-overview.md</name>
  <files>docs/admin-setup-8021x/00-overview.md</files>
  <read_first>
    - docs/admin-setup-8021x/00-overview.md — the live file (the source of truth): locate the numbered-list line `5–7. Platform guides (Phase 104–106) -- entries added as each guide is authored.` (the single edit target, ~line 32), confirm the item-1/2/3/4 link style above it (item 4 is the Phase-103 precedent, ~line 30), inspect the Mermaid node `C[3–7. Platform<br/>Guides]` (~line 21) to confirm it stays valid as a range, and locate the Change History table (~lines 53-57) whose last row is the Phase-103 item-4 row
    - 104-PATTERNS.md — `00-overview.md (EDIT — add item-5 entry + narrow placeholder)` section: the exact item-5 entry text (l.388-391), the placeholder-line replacement (`5–7` line → `6–7` line, l.393-403), and the Change-History-row pattern + new row to append (l.407-421); the double-hyphen-dash convention note
    - 104-RESEARCH.md — `00-overview.md Edit` (l.474-488): the exact current line-32 target text, the item-5 replacement block, and the Change History entry to append
    - docs/admin-setup-8021x/05-ios.md — confirm the link target filename exists (created in Plan 104-01) before linking to it
  </read_first>
  <action>
    Edit `docs/admin-setup-8021x/00-overview.md`. Replace the single numbered-list line `5–7. Platform guides (Phase 104–106) -- entries added as each guide is authored.` with two lines: (1) item 5, a descriptive one-liner matching the item-1/2/3/4 style — bold linked title `**[iOS/iPadOS 802.1X Admin Setup (Wi-Fi + Wired)](05-ios.md)**`, then `--`, then a single description sentence naming the key content (Wi-Fi and wired profiles for all three EAP methods; MAC-address randomization disabled for NAC environments (iOS 14+); wired profile targets M-series iPads with USB Ethernet; wired SCEP-only constraint), with no trailing period; and (2) a continuation line `6–7. Platform guides (Phase 105–106) -- entries added as each guide is authored.` Keep the title parenthetical free of ` / ` (use `+`) to respect the corpus slug convention and the double-dash em-dash style throughout.

    Inspect the Mermaid `C[3–7. Platform<br/>Guides]` node: leave it unchanged — it references a range that remains valid until all five platform guides are authored. Do NOT modify the Mermaid diagram, the Scope section, the wired-availability note, the See Also section, or any other file; this edit is strictly the numbered-list line plus the Change History row below.

    Append a Change History row dated 2026-06-30 following the existing pattern: `| 2026-06-30 | Added item 5 -- iOS/iPadOS platform-guide entry linking 05-ios.md; narrowed placeholder range from 5--7 to 6--7 | -- |`.

    Record in the SUMMARY that `00-overview.md` is a pre-existing file and this edit must be added to the harness allowlist (same handling as the Phase 103 item-4 edit, the Phase 102 item-3 edit, and the Phase 101 glossary see-also edits).
  </action>
  <verify>
    <automated>grep -q "](05-ios.md)" docs/admin-setup-8021x/00-overview.md && grep -q "6–7. Platform guides (Phase 105–106)" docs/admin-setup-8021x/00-overview.md && ! grep -q "5–7. Platform guides (Phase 104–106)" docs/admin-setup-8021x/00-overview.md && grep -q "](04-macos.md)" docs/admin-setup-8021x/00-overview.md && grep -q "](03-windows.md)" docs/admin-setup-8021x/00-overview.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `00-overview.md` numbered list contains item `5.` with a bold link `](05-ios.md)` and a single description sentence (no trailing period), matching the item-1/2/3/4 format and naming MAC-randomization-for-NAC (iOS 14+), M-series iPad/USB Ethernet wired, and the wired SCEP-only constraint
    - A `6–7. Platform guides (Phase 105–106)` continuation line is present
    - The old `5–7. Platform guides (Phase 104–106)` numbered-list line is gone
    - The pre-existing item 3 (`](03-windows.md)`) and item 4 (`](04-macos.md)`) entries remain intact
    - The Mermaid `3–7. Platform Guides` node is unchanged, and the Scope section, wired-availability note, and See Also are unchanged
    - No other file is modified (no capability-matrix rows, no `docs/index.md` / quick-ref / decision-tree edits — navigation-last, Phase 109)
    - A Change History row dated 2026-06-30 documenting the item-5 addition and the 5–7 → 6–7 narrowing is appended (uses `--` double-hyphen dashes)
    - The SUMMARY records that `00-overview.md` is a pre-existing file requiring harness-allowlist registration (Phase 103 item-4 / Phase 102 item-3 / Phase 101 glossary-edit pattern)
  </acceptance_criteria>
  <done>The iOS/iPadOS guide is discoverable from the local 802.1X folder entry point via item 5; the placeholder range is correctly narrowed to 6–7; the Mermaid node is confirmed unchanged; the pre-existing-file edit is flagged for harness allowlisting. DOT1X-06 navigation is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoring artifact | `00-overview.md` is a static Markdown navigation file; the edit adds one link line, one continuation line, and one Change History row. No executable surface, no input parsing, no runtime. |

This is a documentation navigation edit with no executable attack surface. No high-severity code/runtime threats exist for this docs phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-02-01 | Tampering | Pre-existing file edited outside harness allowlist (integrity-tracking drift) | mitigate | Edit is scoped to the single numbered-list line + one Change History row; SUMMARY flags the file for harness-allowlist registration (Phase 103 item-4 / Phase 102 item-3 / Phase 101 glossary-edit pattern) |
| T-104-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no scripts; documentation-only one-line edit — no supply-chain surface |

Block-on severity: high. No high-severity content-security obligations apply to a navigation link edit; the only tracked concern is allowlist-integrity hygiene (T-104-02-01).
</threat_model>

<verification>
- `00-overview.md` links to `05-ios.md` as numbered item 5 in the platform-guide list.
- The placeholder range is updated from `5–7 (Phase 104–106)` to `6–7 (Phase 105–106)`.
- The existing item 3 (`03-windows.md`), item 4 (`04-macos.md`), and the Mermaid range node are unchanged.
- No global nav-hub or capability-matrix files are touched (navigation-last invariant honored for Phase 104).
- The pre-existing-file edit is recorded for harness allowlisting.
</verification>

<success_criteria>
- The iOS/iPadOS 802.1X guide is reachable from the local 802.1X overview entry point.
- Edit is surgical (one list line replaced + one Change History row); nothing else in `00-overview.md` changes.
- Harness-allowlist registration is flagged in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/104-ios-ipados-802-1x-admin-setup-wi-fi-wired/104-02-SUMMARY.md` when done.
</output>
