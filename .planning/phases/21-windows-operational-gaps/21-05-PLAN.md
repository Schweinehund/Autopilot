---
phase: 21-windows-operational-gaps
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reference/deployment-reporting.md
  - docs/reference/drift-detection.md
  - docs/reference/new-batch-workflow.md
autonomous: true
requirements: [WMON-01, WMON-02, WMON-03]
must_haves:
  truths:
    - "Admin can navigate Intune deployment reports to interpret Autopilot success rates and diagnose failures"
    - "Admin can detect registration and profile assignment drift with proactive monitoring procedures"
    - "Admin can follow a new-batch-of-devices operational workflow from OEM order through first user login with checkpoint verification"
  artifacts:
    - path: "docs/reference/deployment-reporting.md"
      provides: "Intune reporting guide for Autopilot deployments"
      contains: "Windows Autopilot deployments"
    - path: "docs/reference/drift-detection.md"
      provides: "Registration and profile assignment drift detection"
      contains: "Fix pending"
    - path: "docs/reference/new-batch-workflow.md"
      provides: "End-to-end new device batch workflow with checkpoints"
      contains: "OEM Order"
  key_links:
    - from: "docs/reference/new-batch-workflow.md"
      to: "docs/reference/deployment-reporting.md"
      via: "relative markdown link"
      pattern: "deployment-reporting.md"
    - from: "docs/reference/new-batch-workflow.md"
      to: "docs/reference/drift-detection.md"
      via: "relative markdown link"
      pattern: "drift-detection.md"
    - from: "docs/reference/drift-detection.md"
      to: "docs/admin-setup-apv1/04-dynamic-groups.md"
      via: "relative markdown link"
      pattern: "04-dynamic-groups.md"
---

<objective>
Create 3 monitoring and operations reference documents covering deployment reporting, drift detection, and the new-batch-of-devices operational workflow. These complete the Phase 21 content files (plans 01-05 create all 18 requirement files).

Purpose: Admins need to proactively monitor deployment health, detect configuration drift, and have a repeatable playbook for onboarding new device batches. These docs close the gap between "device provisioned" and "fleet is healthy."
Output: 3 new markdown files in `docs/reference/`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-windows-operational-gaps/21-CONTEXT.md
@.planning/phases/21-windows-operational-gaps/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deployment reporting guide (WMON-01)</name>
  <files>
    docs/reference/deployment-reporting.md
  </files>
  <read_first>
    docs/_templates/admin-template.md,
    docs/lifecycle/05-post-enrollment.md,
    .planning/phases/21-windows-operational-gaps/21-RESEARCH.md
  </read_first>
  <action>
Create `docs/reference/deployment-reporting.md`.

- Frontmatter: `last_verified: 2026-04-13`, `review_by: 2026-07-12`, `applies_to: both`, `audience: admin`, `platform: Windows`
- Title: "Intune Deployment Reporting for Windows Autopilot"
- Version gate: "This guide covers deployment reporting for both APv1 and APv2. APv2 reports are in a different location than APv1."

- ## Report Types (table):
  | Report | Portal Path | Framework | History | Key Details |
  |--------|-------------|-----------|---------|-------------|
  | Windows Autopilot deployments | Intune admin center > Devices > Monitor > Windows Autopilot deployments | APv1 | 30 days | Filterable by status; shows per-device deployment outcome |
  | APv2 deployment status | Intune admin center > Devices > Monitor > Windows Autopilot device preparation deployments | APv2 | Near real-time | Download diagnostic logs for failed deployments directly from report |
  | Enrollment time grouping failures | Intune admin center > Devices > Monitor > Enrollment time grouping failures | APv2 | Updated within 20 minutes of failure | Shows devices that failed to join the Enrollment Time Grouping security group |
  | Group policy migration readiness | Intune admin center > Reports > Device management > Group policy analytics | Both | Persistent | Per-GPO MDM support percentage; see [GPO-to-Intune Migration](gpo-to-intune.md) |

- ## Understanding Deployment Status:
  | Status | Meaning | Admin Action |
  |--------|---------|-------------|
  | Successful | Device completed provisioning and passed ESP | None — healthy state |
  | Failed | Error occurred during any provisioning stage | Investigate: check error code, review ESP logs, check profile assignment |
  | In progress | Provisioning ongoing | Wait — check back in 60 minutes. If still "in progress" after 2 hours, likely hung |
  | Abandoned | User powered off or reset device mid-provisioning | Appears in "Failed" bucket. Reattempt provisioning — this is not a configuration issue |

- ## Steps: Accessing and Interpreting Reports
  ### Step 1: Navigate to APv1 deployment report
  1. Navigate to **Intune admin center** > Devices > Monitor > Windows Autopilot deployments
  2. Filter by date range (default: last 30 days)
  3. Filter by status: Failed (to investigate issues)
  4. Click individual device for deployment timeline

  ### Step 2: Navigate to APv2 deployment report
  1. Navigate to **Intune admin center** > Devices > Monitor > Windows Autopilot device preparation deployments
  2. Near real-time status updates (no 30-day limit concept — refreshes continuously)
  3. For failed deployments: click "Download diagnostics" to get full diagnostic bundle
  > **What breaks if misconfigured:** Admin looks at APv1 report location for APv2 devices — sees no data. APv2 reports are in a DIFFERENT portal location. Always confirm which framework the device uses first.

  ### Step 3: Export report data
  1. On any report page, click **Export**
  2. CSV format: device name, serial number, status, error code, timestamps
  3. Use for weekly fleet health reviews or incident tracking

- ## Success Rate Interpretation:
  - Above 95%: Healthy deployment environment
  - 85-95%: Investigate failures — likely a specific device model or configuration issue
  - Below 85%: Systematic problem — check network, Entra configuration, or profile settings
  - "Abandoned" deployments inflate failure rate. Filter these out when calculating true failure rate.

- ## Verification:
  - [ ] Can access APv1 deployment report at Devices > Monitor > Windows Autopilot deployments
  - [ ] Can access APv2 deployment report at Devices > Monitor > Windows Autopilot device preparation deployments
  - [ ] Can export report data as CSV

- ## Configuration-Caused Failures table:
  | Misconfiguration | Symptom | Runbook |
  |------------------|---------|---------|
  | Looking at APv1 reports for APv2 devices | No deployment data visible | Navigate to APv2 report location |
  | 30-day history expired | Cannot find old deployments | Export reports regularly for long-term tracking |

- ## See Also: drift-detection.md, new-batch-workflow.md, ../lifecycle/05-post-enrollment.md, gpo-to-intune.md
  </action>
  <verify>
    <automated>bash -c "test -f 'D:/claude/Autopilot/docs/reference/deployment-reporting.md' && echo 'FILE_EXISTS' || echo 'MISSING_FILE'"</automated>
  </verify>
  <acceptance_criteria>
    - docs/reference/deployment-reporting.md contains `Windows Autopilot deployments`
    - docs/reference/deployment-reporting.md contains `device preparation deployments`
    - docs/reference/deployment-reporting.md contains `30 days` or `30-day`
    - docs/reference/deployment-reporting.md contains `Near real-time` or `near real-time`
    - docs/reference/deployment-reporting.md contains `Enrollment time grouping failures`
    - docs/reference/deployment-reporting.md contains `Successful`
    - docs/reference/deployment-reporting.md contains `Failed`
    - docs/reference/deployment-reporting.md contains `Abandoned`
    - docs/reference/deployment-reporting.md contains `95%`
    - docs/reference/deployment-reporting.md contains `Export` or `CSV`
    - docs/reference/deployment-reporting.md contains `audience: admin` and `platform: Windows`
    - docs/reference/deployment-reporting.md contains `## See Also`
  </acceptance_criteria>
  <done>Deployment reporting guide exists with 4-row report types table (APv1, APv2, ETG failures, GPA), deployment status interpretation (4 states), step-by-step report access for both frameworks, success rate interpretation thresholds, and CSV export instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Create drift detection and new-batch workflow docs (WMON-02, WMON-03)</name>
  <files>
    docs/reference/drift-detection.md,
    docs/reference/new-batch-workflow.md
  </files>
  <read_first>
    docs/_templates/admin-template.md,
    docs/admin-setup-apv1/04-dynamic-groups.md,
    docs/reference/deployment-reporting.md,
    .planning/phases/21-windows-operational-gaps/21-RESEARCH.md
  </read_first>
  <action>
Create 2 markdown files in `docs/reference/`.

**drift-detection.md (WMON-02):**
- Frontmatter: `last_verified: 2026-04-13`, `review_by: 2026-07-12`, `applies_to: both`, `audience: admin`, `platform: Windows`
- Title: "Registration and Profile Assignment Drift Detection"
- Version gate: "This guide covers monitoring Autopilot registration and profile assignment health for both APv1 and APv2."

- ## Profile Assignment States (table):
  | State | Meaning | Expected Duration | Admin Action |
  |-------|---------|-------------------|-------------|
  | Not assigned | Device registered but no profile matched | Investigate immediately | Check group membership; verify dynamic group rule syntax (APv1: ZTDId-based, APv2: corporate identifiers) |
  | Assigning | Profile match found, assignment in progress | Up to 30 minutes | Wait. If still "Assigning" after 30 minutes, investigate group sync delays. |
  | Assigned | Normal healthy state | Permanent (until deregistered) | None — device is correctly configured |
  | Fix pending | Hardware change detected on device; Intune attempting to re-register | Hours to days | If persists >24 hours: check if device hardware hash changed (motherboard/drive replacement). May need to deregister and re-register. |

  > **What breaks if misconfigured:** Dynamic group rule has a typo in the ZTDId attribute name (e.g., `device.physicalIds -any _ -eq "[ZTDid]"` — note wrong casing). Devices never match the group. Profile stays "Not assigned." Devices go through manual OOBE instead of Autopilot.

- ## Proactive Monitoring Procedures:
  ### Weekly Check: Profile Assignment Status
  1. Navigate to **Intune admin center** > Devices > Windows enrollment > Devices
  2. Sort by "Profile status" column
  3. Filter for "Not assigned" and "Fix pending"
  4. Investigate each device: check group membership, verify profile target groups

  ### Weekly Check: Enrollment Anomalies
  1. Navigate to deployment reports (see [Deployment Reporting](deployment-reporting.md))
  2. Filter for failures in last 7 days
  3. Group by error code to identify patterns
  4. Check if failure pattern correlates with a recent policy change

  ### Monthly Check: Stale Registrations
  1. Export device list from Intune > Devices > Windows enrollment > Devices
  2. Compare against active device inventory (serial numbers)
  3. Devices in Autopilot registry but not in active inventory = stale records
  4. Deregister stale devices to prevent ghost profile assignments

- ## Remediation Steps:
  | Issue | Remediation |
  |-------|-------------|
  | Profile not assigned after 30 min | Check group membership; verify ZTDId rule syntax (APv1) or corporate identifiers (APv2); force group sync |
  | Fix pending for >24 hours | Deregister device from Autopilot; re-collect hardware hash; re-register; reassign to group |
  | Assigning stuck >1 hour | Check Intune service health; retry by removing and re-adding device to target group |
  | Multiple devices suddenly "Not assigned" | Check if dynamic group rule was recently modified; check Entra ID group processing health |

- ## Proactive Tools:
  - **Intune Endpoint Analytics:** Device health scores, setup times, common issues across fleet. Path: Intune admin center > Reports > Endpoint analytics
  - **Scheduled CSV exports:** Export device list with profile assignment status weekly for trend tracking
  - **Intune built-in reports:** ESP progress tracking per device (Devices > Monitor)

- ## See Also: deployment-reporting.md, new-batch-workflow.md, ../admin-setup-apv1/04-dynamic-groups.md, ../lifecycle/02-profile-assignment.md

**new-batch-workflow.md (WMON-03):**
- Frontmatter: `last_verified: 2026-04-13`, `review_by: 2026-07-12`, `applies_to: both`, `audience: admin`, `platform: Windows`
- Title: "New Batch of Devices: End-to-End Operational Workflow"
- Version gate: "This workflow applies to both APv1 and APv2 device onboarding. Framework-specific differences noted inline."

- ## End-to-End Checkpoint Workflow (main table):
  | Stage | Action | Verification Checkpoint | Troubleshooting |
  |-------|--------|------------------------|-----------------|
  | 1. OEM Order | Include Autopilot registration in purchase order | Confirm with OEM that hardware hashes will be submitted to your tenant | If OEM doesn't support Autopilot registration, collect hashes manually post-delivery |
  | 2. Hash Collection | OEM submits hashes OR manual collection with `Get-WindowsAutoPilotInfo` | Devices appear in Intune > Devices > Windows enrollment > Devices (allow 24-48 hours for OEM submission) | If devices don't appear: verify OEM submitted to correct tenant ID; check for duplicate serial numbers |
  | 3. Group Assignment | Add devices to target Autopilot group (dynamic or static) | Group membership confirmed in Entra ID > Groups > [group] > Members | If dynamic group: verify rule matches device attributes; allow 15-30 min for processing |
  | 4. Profile Assignment | Wait for Intune to assign Autopilot profile | Profile status = "Assigned" in Intune > Devices > Windows enrollment > Devices (wait 15-30 min post-group-assignment) | If "Not assigned" after 30 min: see [Drift Detection](drift-detection.md) |
  | 5. Staging Area | Prepare deployment location; connect devices to wired network (for pre-provisioning) | Network connectivity test: `Test-NetConnection -ComputerName ztd.dds.microsoft.com -Port 443` passes | If fails: check firewall rules per [Network Infrastructure](network-infrastructure.md) |
  | 6. Provisioning | Power on device; complete Autopilot deployment flow | Device reaches desktop; ESP shows "All apps installed" | If ESP fails: see [ESP Troubleshooting](../l2-runbooks/02-esp-deep-dive.md) |
  | 7. Post-Enrollment Verification | Admin verifies in Intune portal | Compliance = Compliant (allow 15-30 min); Apps = Installed; Profile = Assigned | If non-compliant: see [Compliance Timing](compliance-timing.md) |
  | 8. User Handoff | Deliver device to end user with first-login instructions | User can sign in and access corporate resources (email, Teams, SharePoint) | If access blocked: check Conditional Access per [CA Enrollment Timing](ca-enrollment-timing.md) |

- ## Pre-Batch Checklist:
  Before ordering/deploying a new batch:
  - [ ] Autopilot profile exists and is assigned to the correct group
  - [ ] All required apps are packaged and assigned as Required (see [Win32 App Packaging](win32-app-packaging.md))
  - [ ] ESP timeout configured for expected workload (see [ESP Timeout Tuning](esp-timeout-tuning.md))
  - [ ] Network infrastructure verified for deployment location (see [Network Infrastructure](network-infrastructure.md))
  - [ ] Entra ID prerequisites confirmed (see [Entra Prerequisites](entra-prerequisites.md))
  - [ ] Licensing sufficient for batch size (see [Licensing Matrix](licensing-matrix.md))
  - [ ] Compliance and CA policies tested to not block enrollment (see [CA Enrollment Timing](ca-enrollment-timing.md))
  - [ ] For APv1: dynamic group rule tested with at least one device
  - [ ] For APv2: Enrollment Time Grouping security group configured

- ## Post-Batch Review:
  After all devices in batch are provisioned:
  1. Check deployment report: success rate should be >95% (see [Deployment Reporting](deployment-reporting.md))
  2. Verify all devices show "Assigned" profile status (see [Drift Detection](drift-detection.md))
  3. Verify compliance status for all batch devices (allow 30 min post-deployment)
  4. Document any failures for process improvement
  5. Update device inventory spreadsheet/system

- ## See Also: deployment-reporting.md, drift-detection.md, network-infrastructure.md, entra-prerequisites.md, licensing-matrix.md, ../admin-setup-apv1/00-overview.md
  </action>
  <verify>
    <automated>bash -c "test -f 'D:/claude/Autopilot/docs/reference/drift-detection.md' && test -f 'D:/claude/Autopilot/docs/reference/new-batch-workflow.md' && echo 'ALL_FILES_EXIST' || echo 'MISSING_FILES'"</automated>
  </verify>
  <acceptance_criteria>
    - docs/reference/drift-detection.md contains `Not assigned`
    - docs/reference/drift-detection.md contains `Fix pending`
    - docs/reference/drift-detection.md contains `Assigning`
    - docs/reference/drift-detection.md contains `Assigned`
    - docs/reference/drift-detection.md contains `ZTDId` or `ZTDID`
    - docs/reference/drift-detection.md contains `Endpoint Analytics` or `Endpoint analytics`
    - docs/reference/drift-detection.md contains `deployment-reporting.md`
    - docs/reference/drift-detection.md contains `Weekly Check`
    - docs/reference/new-batch-workflow.md contains `OEM Order` or `OEM`
    - docs/reference/new-batch-workflow.md contains `Hash Collection` or `hash`
    - docs/reference/new-batch-workflow.md contains `User Handoff` or `user handoff`
    - docs/reference/new-batch-workflow.md contains `deployment-reporting.md`
    - docs/reference/new-batch-workflow.md contains `drift-detection.md`
    - docs/reference/new-batch-workflow.md contains `network-infrastructure.md`
    - docs/reference/new-batch-workflow.md contains `Pre-Batch Checklist` or `Pre-batch`
    - docs/reference/new-batch-workflow.md contains `95%`
    - Both files contain `audience: admin` and `platform: Windows`
    - Both files contain `## See Also`
  </acceptance_criteria>
  <done>Drift detection guide exists with 4-state profile assignment table, weekly/monthly monitoring procedures, remediation steps for each issue, and proactive tools list. New-batch workflow exists with 8-stage checkpoint table, pre-batch checklist linking to all infrastructure docs, and post-batch review process.</done>
</task>

</tasks>

<verification>
- All 3 files exist in docs/reference/
- deployment-reporting.md distinguishes APv1 and APv2 report locations
- drift-detection.md has 4 profile assignment states with remediation
- new-batch-workflow.md has 8-stage checkpoint table with cross-references to all Phase 21 infrastructure docs
- All files follow admin-template.md pattern
- new-batch-workflow.md serves as the "capstone" doc linking to WINF and WSEC docs
</verification>

<success_criteria>
Admin can monitor deployment health using correct report locations for each framework. Admin can detect and remediate profile assignment drift proactively. Admin can follow a complete checklist-driven workflow when onboarding a new batch of devices. The monitoring docs reference all relevant infrastructure, security, and migration docs created in Plans 01-04.
</success_criteria>

<output>
After completion, create `.planning/phases/21-windows-operational-gaps/21-05-SUMMARY.md`
</output>
