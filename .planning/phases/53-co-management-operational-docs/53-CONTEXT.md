# Phase 53: Co-Management Operational Docs - Context

**Gathered:** 2026-04-27
**Status:** Ready for planning
**Methodology:** Adversarial review (Finder / Adversary / Referee scored pattern) on 4 gray areas covering 14 candidate sub-options across 84 enumerated defects (484 raw points: 11 CRIT + 32 MED + 41 LOW). Adversary attempted 10 disproves (1 CRIT + 8 MED + 1 LOW) targeting the apparent winners and high-stakes evidence-citable claims. Referee adopted 9 disproves (CRIT F-4C-2-01 + 8 MED including F-1B-1-01 / F-1B-1-02 / F-1B-2-02 / F-2B-3-01 / F-4C-1-01 / F-4C-3-01 partial-downgrade / F-4C-4-01 / F-4C-4-02) and verified all via direct file reads (`scripts/validation/v1.5-milestone-audit.mjs` lines 141-178 confirms C10 hardcoded `linuxDocPaths()` scope; `scripts/validation/v1.5-audit-allowlist.json` direct-counted to 7 top-level keys including lazily-added `c7_knox_allowlist[]`; `.planning/ROADMAP.md` line 448 verbatim; `.planning/REQUIREMENTS.md` lines 156-160 traceability; Phase 52 DPO-Phase52-07 cross-domain transferability of three-layer pattern). Net surviving defect score after rulings: 235 points across 75 confirmed defects, with per-area winners: GA-1/1A = 1A-1 (SUMMARY proposal verbatim with REQ-correct filename `02-windows-workload-sliders.md`); GA-1/1B = 1B-1 (co-management H2 only — 54-56 cross-reference per ROADMAP line 448); GA-1/1C = 1C-1 (side-by-side comparison table; methodology tie-break); GA-2/2A = 2A-1 (single 7-row workload table preceded by separate small slider-state semantics block; methodology tie-break separating PITFALL-8 line 179 obligations (1)+(2)); GA-2/2B = 2B-3 (three-layer EP HIGH-RISK callout per Phase 52 DPO-Phase52-07 cross-domain transferable pattern); GA-2/2C = 2C-3 (H2 framing + NEGATIVE assertion layered per Phase 51 V-51-09 + V-51-19 paired-defense); GA-2/2D = REQ-correct override (Autopatch primary content in `03-cocmgmt-migration-paths.md` per REQ line 160 verbatim, soft cross-link from 00 — all enumerated 2D options conflicted with REQ traceability); GA-3 = 3A (inline `> **Platform applicability:**` blockquote at TOP of each Windows file 00/01/02 with embedded analog migration content — 03 does NOT carry cross-platform content per REQ line 160); GA-4/4A = 4A-1 (full ~22-26 V-53-NN per Phase 52 floor); GA-4/4B = 4B-1 (single atomic commit Goldilocks-rule — V-53-NN append-assertion at ops/00-index.md forces it); GA-4/4C = `platform: Windows` for ALL 4 files (off-ballot resolution: 03 = Windows Autopatch prereqs per REQ line 160, NOT a cross-platform doc; cross-platform ballot premise was false; no sidecar amendment).

<domain>
## Phase Boundary

Phase 53 delivers Windows-led co-management documentation as a 4-file suite under `docs/operations/co-management/` plus creates `docs/operations/00-index.md` (the FIRST top-level ops-tree index file, owned exclusively by Phase 53 per ROADMAP line 448). Phase 51 (Linux L1) is the disjoint parallel-Wave-A peer (ROADMAP line 463 + SUMMARY line 184); Phase 52 (Linux L2) is the directly-precedent methodology source for the three-layer callout pattern + single-atomic-commit Goldilocks-rule + full-validator-scope precedent. Linux is OUT of scope for co-management (no SCCM equivalent on Linux; Linux Intune surface lacks ConfigMgr connector entirely per Phase 49 PITFALL-7 whitelist).

**Six deliverables (5 active requirements: COMG-01, COMG-02, COMG-03, COMG-04, COMG-05):**

1. `docs/operations/co-management/00-overview.md` (COMG-01) — All 7 ConfigMgr CB 2503 (GA April 23 2025) workloads enumerated (Compliance Policies, Windows Update Policies, Resource Access [deprecated since CB 2203, mandated to Intune in CB 2403], Endpoint Protection, Device Configuration, Office Click-to-Run Apps, Client Apps); 3 slider states with Pilot Intune disambiguation H2 ("Three Workload Slider States" — collection-scoped Intune management for Pilot Intune, NOT a binary toggle, per PITFALL-8 line 173); Device Configuration → Resource Access + Endpoint Protection implicit-switching note (REQ COMG-01 verbatim); soft cross-link to `03-cocmgmt-migration-paths.md` for Windows Autopatch prereqs context (per 2D REQ-line-160 override + soft 2D-3-style cross-cutting note); inline cross-platform `> **Platform applicability:**` blockquote at TOP per 3A winner.

2. `docs/operations/co-management/01-windows-tenant-attach.md` (COMG-03) — Tenant attach (Intune-portal device sync + remote actions, NO workload switching) vs full co-management (workload sliders active) disambiguation via side-by-side comparison table per 1C-1 winner; SC#3 verbatim "focused disambiguation section" surface; inline cross-platform `> **Platform applicability:**` blockquote at TOP per 3A winner.

3. `docs/operations/co-management/02-windows-workload-sliders.md` (COMG-02; filename per REQ line 157 verbatim — NOT `02-windows-workload-migration.md`) — Workload-slider migration sequence from SCCM to Intune in low-risk-first order (Compliance → Resource Access → WU → Endpoint Protection → Device Configuration → Apps per REQ COMG-02 verbatim); single 7-row workload table per 2A-1 winner with columns (Workload | Migration Order | Risk | Validate Before Moving Slider) preceded by a separate small slider-state semantics block (PITFALL-8 line 179 (1)+(2) discrete obligations satisfied); per-workload pilot-collection guidance per REQ COMG-02; forward-link to v1.2 Windows migration content (`docs/reference/imaging-to-autopilot.md` + `docs/device-operations/04-tenant-migration.md` + GPO→Intune migration content per REQ COMG-02); Endpoint Protection HIGH-RISK three-layer callout (table-cell + adjacent `> ⚠️` blockquote with full PITFALLS line 179 verbatim mandate "do not move this slider until Intune Defender for Endpoint policy is published, targeted, and confirmed reporting healthy" + per-occurrence inline note where EP referenced) per 2B-3 winner; inline cross-platform `> **Platform applicability:**` blockquote at TOP per 3A winner.

4. `docs/operations/co-management/03-cocmgmt-migration-paths.md` (COMG-05) — Windows Autopatch co-management prerequisites primary content per REQ line 160 verbatim ("Autopatch prerequisites — Device Config + Office C2R workloads"); admin can identify that Windows Autopatch requires Device Configuration AND Office Click-to-Run workloads moved to Intune before enablement (SC#5 + REQ COMG-05); forward-link to Phase 54 patch-management content for Autopatch ring topology + WUfB-vs-Autopatch disambiguation (PITFALL-9 — to be authored at Phase 54 ship per ROADMAP line 256); NOTE: 03 is **Windows-only Autopatch prereqs**, NOT a cross-platform analog migration doc — the filename's plural "paths" reflects "Windows-internal migration paths from co-management" (Path: → Autopatch); cross-platform analog content lives INLINE in 00/01/02 per 3A winner + REQ line 159 verbatim "Inline callout blocks in co-management docs"; `platform: Windows` frontmatter (off-ballot 4C resolution).

5. `docs/operations/00-index.md` (created in this phase per ROADMAP line 249 + line 448) — Top-level operations-tree index. Phase 53 ship-time scope = co-management H2 ONLY per 1B-1 winner (no placeholder rows or scaffold H2s for 54/55/56 since ROADMAP line 448 verbatim "Phase 53 creates; Phases 54–56 cross-reference only" — 54-56 do NOT amend `00-index.md`).

6. `scripts/validation/check-phase-53.mjs` — AUDIT-06 validator-as-deliverable per Phase 48 D-25 file-reads-only contract + Phase 49 D-26 + Phase 51 D-20 + Phase 52 D-11 regex-based pattern. Full validator scope per 4A-1 winner; estimated 22-26 V-53-NN structural assertions (file existence + frontmatter C10 + 7-workload literal-token coverage + 3-slider-state H2 + Pilot Intune POSITIVE H2-anchor + Pilot Intune NEGATIVE regex against "partially migrated" / "fully migrated" with PITFALLS-file path-exclusion per F-2C-3-02 surviving defect + tenant attach comparison-table-presence + cross-platform inline-blockquote presence in 00/01/02 (3A enforcement) + EP HIGH-RISK three-layer assertions (matrix-cell + blockquote + per-occurrence) + Autopatch prereqs literal coverage in 03 (Device Configuration + Office Click-to-Run + Autopatch tokens per SC#5) + append-only-style assertion at `operations/00-index.md` (single H2 only — no future-phase H2s; regression-guard against 1B-3 scaffold pattern) + TBD/TODO scan).

Phase 54 (Patch & Update Management) consumes `operations/00-index.md` per ROADMAP line 253 (cross-reference-only); Phase 55 (App Lifecycle Automation) consumes per ROADMAP line 267; Phase 56 (Drift Detection + Tenant Migration) consumes per ROADMAP line 280 — all three downstream phases CROSS-REFERENCE the index file but do NOT amend it (ownership rule per ROADMAP line 448). Phase 59 (Hub Navigation Integration — Linux + Operations Sections) appends Operations H2 to `docs/index.md` linking to `operations/00-index.md` (DPO-Phase53-03 below). Phase 60 (Audit Harness v1.5 Finalization) registers `check-phase-53.mjs` in CI workflow + runs C13 broken-link automation against Phase 53 cross-link surface (DPO-Phase53-04). Phase 58 (DEFER-08 4-platform capability comparison) MAY consume Phase 53 anchors for its co-management row if applicable (link-not-copy per PITFALL-7).

</domain>

<decisions>
## Implementation Decisions

### File suite split + ops/00-index.md scope (Gray Area 1 — winners: 1A-1 SUMMARY-verbatim + 1B-1 co-management-H2-only + 1C-1 side-by-side-comparison-table)

- **D-01:** **The 4-file suite under `docs/operations/co-management/` follows the SUMMARY.md line 184 + REQUIREMENTS.md lines 156-160 traceability mapping verbatim.** Per 1A-1 winner (1 LOW surviving defect after Adversary disproved Finder's citation errors and "non-standard token" claim — "cocmgmt" appears verbatim in REQUIREMENTS line 160 + SUMMARY line 184 + ARCHITECTURE references):
  - `00-overview.md` (COMG-01) — slider model + 7 workloads + 3 states + Pilot Intune disambiguation
  - `01-windows-tenant-attach.md` (COMG-03) — tenant attach disambiguation
  - `02-windows-workload-sliders.md` (COMG-02; **filename per REQ line 157 verbatim — `sliders` not `migration`**) — migration sequence + EP HIGH-RISK
  - `03-cocmgmt-migration-paths.md` (COMG-05) — Windows Autopatch prereqs primary content
  Reject 1A-2 (autopatch-own-file): F-1A-2-01 CRIT confirmed (combining tenant-attach into 00 violates SC#3 line 242 + REQ line 158). Reject 1A-3 (autopatch-folded-into-02-combined): F-1A-3-01 + F-1A-3-02 confirmed (REQ line 160 traceability conflict + "and"-conjuncted filename violates Phase 51/52 naming hygiene per Phase 51 D-25).
- **D-02:** **`docs/operations/00-index.md` ships at Phase 53 with co-management H2 ONLY** (no placeholder rows, no scaffold H2s for Phases 54/55/56). Per 1B-1 winner (2 LOW surviving after Adversary disproved both MED defects via ROADMAP line 448 verbatim: "Phase 53 creates; Phases 54–56 cross-reference only" — 54/55/56 do NOT amend the index file). Reject 1B-2 (soft placeholders): F-1B-2-01 MED confirmed (placeholder text triggers C13/C11 false-positive surface; PITFALL-13 line 281 mandates allowlist or context window day-one) + F-1B-2-04 LOW (reader-visible noise). Reject 1B-3 (full scaffold with 4 H2s): F-1B-3-01 CRIT confirmed (direct Phase 42 D-03 + ROADMAP line 506 append-only H2-block contract violation; pre-creating future H2s for 54/55/56 forces them to insert content under existing H2s rather than append, breaking the v1.5 contract validators inherit from Phase 51 V-51-21/V-51-22) + F-1B-3-02 + F-1B-3-03 MED confirmed.
- **D-03:** **Tenant attach disambiguation in `01-windows-tenant-attach.md` uses a side-by-side feature comparison table** per 1C-1 winner (tied with 1C-2 at 7 points; methodology tie-break favors 1C-1 because SC#3 line 242 verbatim "no workload switching" is a feature-axis distinction that comparison-table directly anchors). Comparison table columns: Capability | Tenant Attach | Full Co-Management. Rows must include at minimum: workload sliders (no/yes), portal device sync (yes/yes), remote actions (yes/yes), Endpoint Manager admin center surface (yes/yes), workload migration (no/yes), prerequisite Intune license per device (no/yes), Hybrid Entra ID join required (no/yes-typically). Reject 1C-2 (decision-matrix-only): loses "no workload switching" structural anchor + L2 audience (Phase 56) feature-lookup surface. Reject 1C-3 (both layered): F-1C-3-01 + F-1C-3-02 MED confirmed (two adjacent same-shape tables risk visual collision per Phase 52 D-01 differentiated-shapes rationale; same-shape layered untested precedent vs Phase 51 cross-shape matrix+blockquote pattern).

### Workload sequence + EP HIGH-RISK layered defense + Pilot Intune framing (Gray Area 2 — winners: 2A-1 single-7-row + 2B-3 three-layer + 2C-3 H2+NEGATIVE-layered + 2D REQ-line-160-override)

- **D-04:** **`02-windows-workload-sliders.md` carries a single 7-row workload table preceded by a small slider-state semantics block.** Per 2A-1 winner (3-way tie at 12 points; methodology tie-break favors 2A-1 with PITFALL-8 line 179 (1)+(2)+(3) DISTINCT obligations satisfied as parallel structures: small text+state-table block for obligation (1) "explain three slider states" + main 7-row workload sequence table for obligation (2) "table the workloads with migration order and Validate-Before-Moving column" + EP HIGH-RISK three-layer callout for obligation (3)). Workload table columns: `Workload | Migration Order | Risk | Validate Before Moving Slider | Pilot Collection Guidance`. Rows: Compliance Policies (1, LOW, "compliance evaluator agreement; non-compliant device cohort baseline measured", "scope to single AAD security group of test devices"), Resource Access (2 — DEPRECATED, N/A — "this workload's slider is mandated to Intune in CB 2403; document as deprecated / migrated automatically"), Windows Update Policies (3, MEDIUM, "WUfB ring assignment validated; deferral conformance baseline; FORWARD-LINK Phase 54 PATCH-02 ring topology guidance"), Endpoint Protection (4, **HIGH-RISK** — three-layer callout per D-05 below), Device Configuration (5, MEDIUM-HIGH, "configuration profile assignment validated; CSP equivalence verified for in-scope settings; Resource Access + Endpoint Protection implicit-switching impact reviewed"), Office Click-to-Run Apps (6, MEDIUM, "C2R deployment via Intune validated for pilot collection; channel + version stability reviewed"), Client Apps (7, MEDIUM, "Win32 + MSI + Microsoft Store deployments validated; supersedence + dependency graphs reviewed; FORWARD-LINK Phase 55 APP-01 supersedence content"). Reject 2A-2 (two tables): same-shape adjacent reader-flip-confusion risk per Phase 52 D-01. Reject 2A-3 (Mermaid): loses Validate-Before-Moving column structure that PITFALL-8 line 179 (2) explicitly mandates.
- **D-05:** **Endpoint Protection workload carries a three-layer HIGH-RISK callout layered defense surface** per 2B-3 winner (7 points after Adversary disproved F-2B-3-01 transferability concern via Phase 52 DPO-Phase52-07 explicit verbatim cross-domain-transfer endorsement). Three layers required:
  - **Layer 1 (workload table cell):** Risk column entry = `**HIGH-RISK** — see callout` (bold + cross-reference to Layer 2 within same file). Validator V-53-NN asserts this literal pattern in row 4.
  - **Layer 2 (adjacent `> ⚠️` blockquote):** placed immediately below the workload table; literal text quoting PITFALLS line 179 verbatim: `> ⚠️ **Endpoint Protection HIGH-RISK:** do not move this slider until Intune Defender for Endpoint policy is published, targeted, and confirmed reporting healthy. Moving the slider before Defender policy lands and reports healthy creates a fleet-wide Defender coverage gap window. Verify policy reporting via Intune > Endpoint security > Microsoft Defender for Endpoint > Onboarding status before initiating slider move.`
  - **Layer 3 (per-occurrence inline note):** every later text reference to "Endpoint Protection workload" within `02-windows-workload-sliders.md` (e.g., in walk-through narrative, in cross-reference subsections) carries an inline `[HIGH-RISK — see callout above]` reminder. Validator V-53-NN requires ≥2 such inline reminders.
  - Reject 2B-1 (cell only): F-2B-1-01 + F-2B-1-02 CRIT confirmed (PITFALLS line 179 mandate >100 chars exceeds reasonable cell width + Phase 51 V-51-09 layered-defense methodology violation).
  - Reject 2B-2 (cell + blockquote two-layer): F-2B-2-01 + F-2B-2-02 MED confirmed (Phase 52 D-01 three-layer is ops-domain default; per-occurrence inline reminder needed for later text references to EP workload in walk-through/cross-reference subsections).
- **D-06:** **Pilot Intune disambiguation uses H2 framing PLUS validator NEGATIVE assertion against banned phrasings, layered.** Per 2C-3 winner (3 LOW surviving after Adversary review). H2 framing in `00-overview.md`: `## Three Workload Slider States` containing exactly the 3 states (ConfigMgr / Pilot Intune / Intune) with explicit "collection-scoped Intune management; remainder of fleet stays on ConfigMgr" wording for Pilot Intune AND explicit "this is NOT a binary toggle — slider state is per-collection" framing per PITFALLS line 173 + ROADMAP SC#1 line 240 verbatim. Validator V-53-NN NEGATIVE regex asserts the banned phrasings `partially migrated` and `fully migrated` (case-insensitive, with optional hyphen variants `partially-migrated` and `partially_migrated`) do NOT appear in any of the 4 Phase 53 files (PITFALL-8 line 173 regression-guard mirroring Phase 51 V-51-19 inverse-polarity pattern). Validator MUST exclude PITFALLS file path from scan scope per F-2C-3-02 surviving defect (PITFALLS line 173 itself contains the banned phrase as a quoted failure-mode reference); scope-restriction = file-path-prefix filter (only scan files under `docs/operations/co-management/`). PITFALL-13 same-commit-allowlist seeding NOT required at Phase 53 ship (no false-positive surface in the restricted-scope corpus); deferred to first false-positive surface per Phase 48 D-15 YAGNI.
- **D-07:** **Autopatch prereqs (COMG-05) primary content lives in `03-cocmgmt-migration-paths.md` per REQ line 160 verbatim — NOT in `00-overview.md` or `02-windows-workload-sliders.md`.** This is an OFF-BALLOT REFEREE OVERRIDE: all three enumerated 2D options (2D-1/2D-2/2D-3) conflicted with REQ line 160 traceability. Authoritative resolution: 03 carries Autopatch prereqs as primary scope (REQ COMG-05 verbatim "Device Configuration AND Office Click-to-Run workloads moved to Intune before enabling Autopatch"); 00-overview includes a soft cross-cutting note ("Windows Autopatch is a co-management successor — see [03-cocmgmt-migration-paths.md#autopatch-prerequisites]") so admins reading 00-overview encounter the dependency before reaching 02-workload-sliders. The 2D-3-style cross-link from 00 is preserved; the 2D-1-style "Final H2 in 02" is REJECTED (Autopatch belongs in its own file per REQ).

### Cross-platform non-equivalent callouts placement (Gray Area 3 — winner: 3A inline-blockquote-at-top with embedded analog content)

- **D-08:** **Each of `00-overview.md` / `01-windows-tenant-attach.md` / `02-windows-workload-sliders.md` carries an inline `> **Platform applicability:**` blockquote at the TOP of the file with embedded macOS / iOS / Android analog migration content.** Per 3A winner (12 points; tied with 3C; methodology tie-break favors 3A per REQ line 159 verbatim "Inline callout blocks in co-management docs" — "callout block" idiom matches blockquote shape, not Phase 51 D-08 banner-shape). Embedded content per file (~3-5 lines per platform per file):
  - **macOS:** "macOS has no co-management equivalent — Microsoft Intune does not federate with Jamf or other macOS-management agents at the workload-slider level. Migration path: Jamf → Intune via ABM MDM transfer (release device assignment in Jamf-bound MDM server in ABM, then re-assign to Intune-bound MDM server; in-use devices require user-initiated re-enrollment). See [docs/admin-setup-macos/02-enrollment-profiles.md](../../admin-setup-macos/02-enrollment-profiles.md) for ABM MDM transfer mechanics." (per REQ COMG-04 verbatim)
  - **iOS/iPadOS:** "iOS/iPadOS has no co-management equivalent — Apple's MDM model is single-MDM-server-per-device and does not support multi-management-agent slider partitioning. Migration path: MAM → MDM transition (existing MAM-WE app protection policies coexist; new MDM enrollment via ABM MDM transfer or User Enrollment migration). See [docs/admin-setup-ios/00-enrollment-overview.md](../../admin-setup-ios/00-enrollment-overview.md) and [docs/admin-setup-ios/04-byod-mam-overview.md](../../admin-setup-ios/04-byod-mam-overview.md)." (per REQ COMG-04 verbatim)
  - **Android:** "Android has no co-management equivalent — Android Enterprise enrollment modes (Fully Managed / BYOD Work Profile / Dedicated / ZTE / AOSP) are mutually exclusive at the device level. Migration paths: legacy Device Administrator → Android Enterprise (DA deprecated August 2022; existing DA devices require wipe + re-enrollment), and MAM → MDM transition for BYOD scenarios. See [docs/admin-setup-android/00-overview.md](../../admin-setup-android/00-overview.md)." (per REQ COMG-04 verbatim)
  - Reject 3B (centralized-only-in-03): F-3B-01 CRIT confirmed (SC#4 line 243 "macOS / iOS / Android admins reading co-management content see explicit callouts" — admin reading 01/02 with no inline callout fails SC#4) + PITFALLS line 374 "contextual notes must still be present" violation. **Additionally**: 03's REQ-mapped scope is COMG-05 Autopatch prereqs (Windows-internal), not cross-platform analog content.
  - Reject 3C (Phase 51 D-08 platform-gate banner pattern): F-3C-01 + F-3C-02 MED confirmed (banner pattern is L1 audience; co-management is admin audience — transferability needs explicit reasoning + banner read-once at file-entry doesn't reinforce during deep-read).
  - Reject 3D (three-surface layered): F-3D-01 + F-3D-02 MED confirmed (over-engineered for positive requirement vs PITFALL-8 EP HIGH-RISK pitfall-driven layering + same-shape risk).
- **D-09:** **`03-cocmgmt-migration-paths.md` does NOT carry inline cross-platform analog blockquote** because 03's primary scope is Windows Autopatch prereqs per REQ line 160 (NOT cross-platform analog migration paths). The filename's plural "paths" reflects "Windows-internal migration paths from co-management" (i.e., the path from co-management to Autopatch). Cross-platform analog content lives ONLY in 00/01/02 per D-08 + REQ line 159 verbatim "Inline callout blocks in co-management docs" (plural — across files, NOT one centralized doc).

### Validator scope + commit atomicity + frontmatter platform contract (Gray Area 4 — winners: 4A-1 full + 4B-1 single-atomic + off-ballot `platform: Windows` for all 4 files)

- **D-10:** **`check-phase-53.mjs` validator scope = FULL** per 4A-1 winner (12 points; effort cost real but methodology fit with Phase 52 22-assertion floor is direct). File-reads-only / no-shared-module per Phase 48 D-25; regex-based parsing per Phase 49 D-26 + Phase 51 D-20 + Phase 52 D-11. Validator implements the following V-53-NN structural assertions (estimated 22-26 checks):
  - **V-53-01..05 File existence:** `docs/operations/co-management/00-overview.md` + `01-windows-tenant-attach.md` + `02-windows-workload-sliders.md` + `03-cocmgmt-migration-paths.md` + `docs/operations/00-index.md` + `scripts/validation/check-phase-53.mjs`
  - **V-53-06 Frontmatter (C10-equivalent local contract per D-12):** all 4 content files + `00-index.md` carry `platform: Windows` + `audience: admin` + 60-day `last_verified` + `review_by` (validator-local check; not C10 per the harness since C10 scope is `linuxDocPaths()` only — see D-12 below)
  - **V-53-07 7-workload literal coverage (per SC#1 + REQ COMG-01):** `00-overview.md` contains all 7 literal workload names: `Compliance Policies`, `Windows Update Policies`, `Resource Access`, `Endpoint Protection`, `Device Configuration`, `Office Click-to-Run Apps`, `Client Apps`
  - **V-53-08 Three slider states H2 (per D-06 + SC#1):** `00-overview.md` contains literal H2 `## Three Workload Slider States` (or equivalent regex-pinned pattern) AND each of the 3 states (`ConfigMgr`, `Pilot Intune`, `Intune`) appears in body text adjacent to that H2
  - **V-53-09 Pilot Intune POSITIVE assertion (per D-06):** `00-overview.md` contains literal `collection-scoped` AND (`not a binary toggle` OR `not binary toggle` OR `per-collection`) within ~10-line window of "Pilot Intune" mention
  - **V-53-10 Pilot Intune NEGATIVE assertion (per D-06):** none of the 4 Phase 53 content files contain `partially migrated`, `partially-migrated`, `partially_migrated`, `fully migrated`, `fully-migrated`, `fully_migrated` (case-insensitive). Scope-restriction: scan only files under `docs/operations/co-management/` (excludes PITFALLS-file path per F-2C-3-02 surviving defect)
  - **V-53-11 Tenant attach comparison-table presence (per D-03):** `01-windows-tenant-attach.md` contains a table with columns matching `| Capability | Tenant Attach | Full Co-Management |` (literal-token + 2-or-more-row regex)
  - **V-53-12 SC#3 disambiguation literal coverage (per D-03 + SC#3):** `01-windows-tenant-attach.md` contains literal `no workload switching` AND `workload sliders` (the SC#3-line-242-mandated distinguishing characteristic)
  - **V-53-13 Workload-sequence table presence (per D-04 + SC#2):** `02-windows-workload-sliders.md` contains a table with literal column `Validate Before Moving Slider` (or `Validate Before Moving`); ≥6 data rows (Resource Access deprecated row may collapse)
  - **V-53-14 SC#2 migration-order literal coverage (per D-04 + REQ COMG-02):** `02-windows-workload-sliders.md` contains the migration sequence in the documented low-risk-first order — at minimum, `Compliance` precedes `Endpoint Protection` precedes `Device Configuration` precedes `Apps` in document order (text-position regex)
  - **V-53-15 EP HIGH-RISK Layer 1 (per D-05):** `02-windows-workload-sliders.md` Endpoint Protection table row contains literal `HIGH-RISK` token in Risk column
  - **V-53-16 EP HIGH-RISK Layer 2 (per D-05):** `02-windows-workload-sliders.md` contains literal `> ⚠️ **Endpoint Protection HIGH-RISK:**` AND the verbatim PITFALLS-line-179 mandate text "do not move this slider until Intune Defender for Endpoint policy is published, targeted, and confirmed reporting healthy" within the blockquote
  - **V-53-17 EP HIGH-RISK Layer 3 (per D-05):** `02-windows-workload-sliders.md` contains ≥2 per-occurrence `[HIGH-RISK` inline reminders adjacent to "Endpoint Protection" tokens (per-Trap-style per Phase 52 V-52-08 precedent)
  - **V-53-18 Cross-platform inline blockquote at TOP (per D-08):** each of `00-overview.md`, `01-windows-tenant-attach.md`, `02-windows-workload-sliders.md` contains literal `> **Platform applicability:**` blockquote within first 50 lines of file body (post-frontmatter); blockquote contains all 3 platform tokens `macOS`, `iOS`, `Android`
  - **V-53-19 Cross-platform analog literal coverage (per D-08 + REQ COMG-04):** the inline blockquotes contain verbatim REQ-COMG-04 analog tokens — `Jamf`, `ABM MDM transfer`, `MAM`, `MDM`, `Device Administrator` (or `legacy DA`)
  - **V-53-20 03-Autopatch primary content (per D-07 + REQ COMG-05 + SC#5):** `03-cocmgmt-migration-paths.md` contains literal `Device Configuration` AND `Office Click-to-Run` (or `Office C2R`) AND `Autopatch` AND `prerequisite` (or `prerequisites`)
  - **V-53-21 03 NOT cross-platform doc (per D-09):** `03-cocmgmt-migration-paths.md` does NOT contain a `> **Platform applicability:**` blockquote (regression-guard against accidental inclusion of cross-platform content; 03 is Windows-only)
  - **V-53-22 ops/00-index.md scope (per D-02 + 1B-1 winner):** `docs/operations/00-index.md` contains exactly ONE H2 = `## Co-Management` (or equivalent — single H2; no Phase-54/55/56-named H2s; no `TBD` / `Coming in Phase` text). Negative-regex against `Patch Management`, `App Lifecycle`, `Drift`, `Tenant Migration`, `TBD`, `Coming in Phase`, `Phase 54`, `Phase 55`, `Phase 56` as H2-positioned tokens (regression-guard against 1B-2/1B-3 patterns)
  - **V-53-23 Forward-link to v1.2 Windows migration content (per REQ COMG-02):** `02-windows-workload-sliders.md` contains literal cross-link to `imaging-to-autopilot.md` OR `device-operations/04-tenant-migration.md` (REQ COMG-02 verbatim "forward-link to v1.2 Windows migration content")
  - **V-53-24 Soft cross-link from 00 to 03 Autopatch (per D-07 + REQ COMG-05 surface):** `00-overview.md` contains literal cross-link to `03-cocmgmt-migration-paths.md` (anchored to Autopatch prereqs surface)
  - **V-53-25 TBD/TODO/PLACEHOLDER scan (per v1.2 retro carry-forward):** none of the Phase 53 files contain literal `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` outside Version History tables
  - **V-53-26 Resource Access deprecation note (per ROADMAP line 248 + D-04):** `00-overview.md` AND `02-windows-workload-sliders.md` contain literal `deprecated` near `Resource Access` token (≤10-line window) AND reference to `CB 2203` or `CB 2403`
  - Reject 4A-2 (mid-scope ~12-15): F-4A-2-01 + F-4A-2-02 CRIT confirmed (skipping Pilot Intune NEGATIVE assertion silently disarms PITFALL-8 regression-guard; skipping cross-platform layered loses SC#4/COMG-04 verification surface). Reject 4A-3 (minimal ~6-8): F-4A-3-01 + F-4A-3-02 CRIT confirmed (below v1.5 floor of 18-22 V-NN per Phase 52 D-10 precedent; reduces validator to file-existence only — useless for a 5-SC phase).
- **D-11:** **`check-phase-53.mjs` implementation pattern matches `check-phase-52.mjs` / `check-phase-51.mjs` / `check-phase-50.mjs` / `check-phase-49.mjs`** (Phase 48 D-25 + Phase 49 D-26 + Phase 51 D-20 + Phase 52 D-11 lineage). File-reads-only / no-shared-module; markdown parsing is regex-based (heading detection via `^## ` / `^### `; blockquote detection via `^> `; fenced-code-block boundary detection; H2-anchor-suffix detection via `\{#[a-z-]+\}` regex if any anchors present); no markdown-AST dependency.
- **D-12:** **All 4 content files + `operations/00-index.md` use `platform: Windows` frontmatter.** OFF-BALLOT REFEREE RESOLUTION (the 4C ballot premise — that 03 is a cross-platform doc — was false per REQ line 160 mapping COMG-05 to 03 = Windows Autopatch prereqs). All 4 files are Windows-led co-management content. Cross-platform analog content lives INLINE in 00/01/02 per D-08 (REQ line 159 verbatim "Inline callout blocks") — those callouts do NOT change the file's primary platform classification. Ops/00-index.md classified `platform: Windows` AT PHASE 53 SHIP because Phase 53's contribution is the co-management H2 only (Phase 54/55/56 ship-time will not amend per ROADMAP line 448). NOTE on C10: the harness C10 check has hardcoded scope `linuxDocPaths()` per `scripts/validation/v1.5-milestone-audit.mjs` lines 141-178; operations/co-management/ files are NOT in C10 scope, so frontmatter is enforced ONLY by the local V-53-06 validator assertion (D-10), not by C10. NO sidecar amendment required at Phase 53 ship (per Phase 48 D-15 YAGNI + lazy-add forward-extension only on first false-positive surface).
- **D-13:** **Hardcoded H2/anchor strings + literal-token regexes pinned in BOTH the validator (`check-phase-53.mjs`) and CONTEXT.md.** Brittleness trade-off accepted per Phase 49 D-25 + Phase 50 D-25 + Phase 51 D-21 + Phase 52 D-12 precedent. Renaming any pinned H2 or workload token requires same-commit validator update.
- **D-14:** **Commit atomicity = SINGLE atomic commit** per 4B-1 winner (3 LOW surviving; Phase 51 D-22 + Phase 52 D-13 + CDI-Phase52-07 + DPO-Phase52-06 inheritance). The validator's V-53-22 append-only-style assertion at `operations/00-index.md` (single-H2 scope check) FORCES single-commit landing — splitting the commit would FAIL the validator in commit-1 because the append target's H2 wouldn't yet exist. Single atomic commit covers: 4 new content files (00-overview + 01-tenant-attach + 02-workload-sliders + 03-cocmgmt-migration-paths) + 1 new index file (operations/00-index.md) + 1 validator + any same-commit metadata corrections. Reject 4B-2 (two commits): F-4B-2-01 CRIT confirmed (Phase 51 4D rejection precedent CDI-Phase51-06 direct transfer; Phase 52 CDI-Phase52-07 inheritance; validator append-assertion mechanically requires append target in same commit).

### Plan-level ordering and atomicity

- **D-15:** **Phase 53 plan order (single-commit atomicity per D-14):**
  1. **Authoring wave (parallel-safe across files; sequential within hot-spot):** All content + validator can be authored concurrently in worktrees. No upstream phase prerequisites (Phase 53 has no `Depends on` per ROADMAP line 237 — runs Wave A parallel with Phase 51 + 52, but those are now closed). Suggested plan-level decomposition:
     - 53-01: `docs/operations/co-management/00-overview.md` (COMG-01; D-01 scope + D-06 Pilot Intune H2 + soft cross-link to 03 per D-07 + cross-platform inline blockquote per D-08 + Resource Access deprecation note per D-04 V-53-26 + frontmatter `platform: Windows` `audience: admin` 60-day cycle per D-12)
     - 53-02: `docs/operations/co-management/01-windows-tenant-attach.md` (COMG-03; D-03 side-by-side comparison table + SC#3 "no workload switching" anchor per V-53-12 + cross-platform inline blockquote per D-08 + frontmatter)
     - 53-03: `docs/operations/co-management/02-windows-workload-sliders.md` (COMG-02; D-04 7-row workload table + small slider-state semantics block + D-05 EP HIGH-RISK three-layer callout + V-53-13/14/15/16/17 literal coverage + V-53-23 forward-link to v1.2 Windows migration content + cross-platform inline blockquote per D-08 + frontmatter)
     - 53-04: `docs/operations/co-management/03-cocmgmt-migration-paths.md` (COMG-05; D-07 Autopatch prereqs primary content + V-53-20 literal coverage + D-09 NO cross-platform inline blockquote + forward-link to Phase 54 patch-management for Autopatch ring topology + frontmatter)
     - 53-05: `docs/operations/00-index.md` (D-02 co-management H2 only + V-53-22 negative-regex regression-guard + frontmatter `platform: Windows`)
     - 53-06: `scripts/validation/check-phase-53.mjs` (D-10 V-53-01..26 structural assertions; D-11 file-reads-only pattern; estimated 22-26 checks)
  2. **Pre-commit gate:**
     - `node scripts/validation/check-phase-53.mjs` exits 0 (all 22-26 V-53-NN checks pass)
     - `node scripts/validation/v1.5-milestone-audit.mjs --verbose` exits 0 (C1-C12 PASS; C13 informational PASS-or-noise within accepted tolerance per Phase 48 D-08)
     - `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0 (regression-prevention; should be unaffected since Phase 53 files are not pinned in supervision sidecar)
     - `markdown-link-check` against the 5 new content files (informational; not blocking per Phase 48 D-08)
     - Verify all 5 files contain `last_verified` + `review_by` frontmatter on 60-day cycle
  3. **Single atomic commit:** All 4 new co-management files + new ops/00-index.md + validator per D-14 + commit message `docs(53): co-management 4-file suite + operations/00-index.md + check-phase-53 validator` per Phase 50 D-19 + Phase 51 + Phase 52 commit-message-shape precedent.
  4. **Post-commit verification:** Re-run `check-phase-53.mjs` + `v1.5-milestone-audit.mjs` from clean workspace.
  5. **VERIFICATION.md authored** (separate commit per Phase 49/50/51/52 close pattern) documenting validator pass + DPO inheritance verification + 5 SC# satisfaction proof + DPO-Phase53-NN propagation summary for Phase 54+ plan authors.

### Claude's Discretion

- **CD-01:** Exact wording of the 3-line per-platform analog content embedded in the inline blockquotes (D-08 specifies form + minimum content per platform; specific verb choice is author discretion within "macOS/iOS/Android admin sees no co-management equivalent + analog migration path with cross-link" trim rationale).
- **CD-02:** Exact ordering of Resource Access workload row in `02-windows-workload-sliders.md` table (D-04 says "low-risk-first migration order"; Resource Access being deprecated since CB 2203 means its row may be present-but-collapsed or last-row-with-deprecated-callout — author discretion within REQ COMG-02 sequence-preservation contract).
- **CD-03:** Per-workload Pilot Collection Guidance column body length in `02-windows-workload-sliders.md` table — author may scale to coverage need; minimum is REQ COMG-02 verbatim "per-workload pilot-collection guidance" obligation.
- **CD-04:** Whether `00-overview.md` includes a Mermaid diagram (e.g., 3-state slider-position diagram or workload-flow visualization) — Phase 51 trees use `graph TD` + `click`; Phase 52 may include focused Mermaid blocks. Phase 53 author discretion. Validator does NOT assert Mermaid presence (no SC# mandates it).
- **CD-05:** Tenant attach comparison-table column count in `01-windows-tenant-attach.md` — D-03 specifies 3 columns minimum (Capability | Tenant Attach | Full Co-Management); author may add a 4th "Notes" or "When to use" column at discretion.
- **CD-06:** Cross-platform inline blockquote structure — single multi-platform blockquote (3 platforms in one block) vs three separate per-platform blockquotes (one per platform). D-08 mandates content + cross-links; structural shape is author discretion. Validator V-53-18 accepts either via `> **Platform applicability:**` + 3-platform-token coverage.
- **CD-07:** Whether `03-cocmgmt-migration-paths.md` cross-references Phase 54 placeholder content (e.g., "see Phase 54 patch-management content for WUfB-vs-Autopatch ring disambiguation per PITFALL-9") OR defers all Phase 54 references until Phase 54 ships — author discretion. Forward-promise text is acceptable per Phase 30 D-22 placeholder retrofit precedent.
- **CD-08:** `audience` frontmatter value choice per file — `admin` is the default for ops-domain content (Phase 53 is admin audience per ROADMAP line 235 "admins can plan, sequence, and execute a workload migration"). However, `02-windows-workload-sliders.md` carries L2-relevant escalation-investigation surface (workload mismatch + sequence regressions); author may set `audience: admin,L2` if multi-audience labeling helps Phase 56 drift-migration cross-references. Validator V-53-06 accepts any non-empty `audience:` field.

### Cross-decision integration callouts (Referee Part C)

- **CDI-Phase53-01:** **D-08 (cross-platform inline at top of 00/01/02) × D-09 (03 NOT cross-platform doc) × V-53-21 (regression-guard).** Plan author for 53-04 (`03-cocmgmt-migration-paths.md`) MUST NOT include a `> **Platform applicability:**` blockquote — V-53-21 NEGATIVE regression-guard enforces. The cross-platform analog content is exclusively in 00/01/02. This is a load-bearing distinction: 03 is Windows Autopatch prereqs (REQ line 160), not cross-platform analog migration paths.
- **CDI-Phase53-02:** **D-05 (three-layer EP HIGH-RISK) × Phase 52 D-01 + DPO-Phase52-07 inheritance.** Phase 53 EP HIGH-RISK three-layer (table-cell + blockquote + per-occurrence inline) inherits the Phase 52 freshness-caveat layered-defense pattern verbatim, transferred to risk-callout domain per DPO-Phase52-07's explicit cross-domain-transfer endorsement. Plan author for 53-03 MUST implement all three layers; V-53-15/16/17 enforce per-layer.
- **CDI-Phase53-03:** **D-06 (Pilot Intune POSITIVE+NEGATIVE layered) × Phase 51 V-51-09 + V-51-19 dual-defense × PITFALL-13 same-commit-allowlist.** Phase 53 inherits Phase 51's POSITIVE+NEGATIVE pairing pattern (V-53-09 POSITIVE + V-53-10 NEGATIVE). PITFALL-13 same-commit-allowlist seeding NOT required at Phase 53 ship per F-2C-3-02 surviving defect (path-restriction to `docs/operations/co-management/` excludes PITFALLS file as the only known false-positive surface). If new false positives surface during execution, lazy-add per Phase 48 D-15 YAGNI.
- **CDI-Phase53-04:** **D-10 (full validator scope) × D-14 (single atomic commit) coupling.** V-53-22 append-only-style assertion at `operations/00-index.md` (single-H2 scope) FORCES single-commit landing per Phase 51 D-22 + Phase 52 D-13 + CDI-Phase52-07 + DPO-Phase52-06 inheritance. The 1B-1 winner (co-management H2 only at ship time) means the validator's append-target shape is "single H2 must exist" rather than "appended after last existing H2." Plan author for 53-06 must implement V-53-22 as a positive-presence + negative-regression-guard combined assertion (positive: literal `## Co-Management` H2 exists; negative: NO `## Patch Management` / `## App Lifecycle` / `## Drift` / `TBD` tokens at H2 position). This shape is novel for v1.5 (Phase 51/52 used "H2 follows last existing H2" assertions because their append targets had pre-existing H2s).
- **CDI-Phase53-05:** **D-07 (Autopatch primary in 03) × REQUIREMENTS line 160 verbatim.** ROADMAP line 244 SC#5 wording "Admin can identify that Windows Autopatch requires Device Configuration AND Office Click-to-Run workloads moved" is satisfied ONLY when Autopatch primary content is in 03 (REQ line 160's traceability mapping) + soft cross-link from 00 (D-07). Plan author for 53-04 MUST keep Autopatch as primary 03 content; soft cross-link from 00 per D-07 is the methodology-correct supplementary reinforcement (NOT a replacement). Off-ballot 2D-3-style layering (00 cross-link + 03 primary content) is the actual landed shape.
- **CDI-Phase53-06:** **D-12 (`platform: Windows` for all 4 files + index) × C10 scope hardcoded `linuxDocPaths()` × harness C-check expansion at Phase 60.** Phase 53 frontmatter is enforced ONLY by V-53-06 local assertion. C10 doesn't run on operations/ files (verified at `scripts/validation/v1.5-milestone-audit.mjs` lines 141-178). Phase 60 (audit harness finalization) MAY expand a C-check to ops files; if so, Phase 60 plan author handles allowlist seeding per Phase 48 D-15 lazy-add at THAT time, not at Phase 53. Phase 53 is forward-compatible because `platform: Windows` is a token that any future C-check using existing single-platform-string convention will accept.
- **CDI-Phase53-07:** **D-04 (single workload table) × D-05 (three-layer EP HIGH-RISK) coupling.** Layer 1 of EP HIGH-RISK is a TABLE CELL — meaning the workload table cannot be replaced by a non-table representation (this rules out the 2A-3 Mermaid winner if it had been chosen). The single-table 2A-1 winner directly enables Layer 1. If a future phase wants to add a Mermaid sequence diagram for the migration order, it must coexist with the table (CD-04 decision); the table is load-bearing for V-53-15.

### Downstream phase obligations (Referee Part C — for next-phase plan authors)

- **DPO-Phase53-01:** **Phase 54 (Patch & Update Management) inherits Phase 53 ops/00-index.md cross-reference contract.** Phase 54 documentation under `docs/operations/patch-management/` MUST cross-reference `docs/operations/00-index.md` from its own files (e.g., footer "Related" sections); Phase 54 MUST NOT amend `operations/00-index.md` per ROADMAP line 448 verbatim "Phase 53 creates; Phases 54–56 cross-reference only." Phase 54 plan author MUST verify this constraint at plan-author time (cross-reference cleanup as pre-commit gate). If Phase 54 needs `00-index.md` to surface its content, the cross-reference flows FROM Phase 54 files TO `00-index.md` (e.g., "see [Co-Management Index](../00-index.md) for the operations tree" in patch-management files), NOT the inverse.
- **DPO-Phase53-02:** **Phase 54 PATCH-02 inherits Phase 53 D-08 cross-platform inline-blockquote pattern as a candidate shape.** Phase 54's WUfB rings guide (PATCH-02) discusses Windows-specific ring topology but operates in a multi-platform domain (Phase 54 patch-management spans Windows + macOS + iOS + Android per ROADMAP line 251). If PATCH-02 needs platform-applicability callouts, the Phase 53 D-08 inline-blockquote-at-top pattern is the established v1.5 ops-domain shape — Phase 54 plan author SHOULD adopt the same shape for consistency (NOT mandate; per-phase author discretion within methodology family).
- **DPO-Phase53-03:** **Phase 59 (Hub Navigation Integration — Linux + Operations) consumes Phase 53 ops/00-index.md as a link target for `docs/index.md` Operations H2.** Phase 59 plan author MUST treat `docs/operations/00-index.md` as an immutable surface (Phase 53 owns it; Phase 59 does NOT amend `00-index.md`, only `docs/index.md`). The Phase 59 `docs/index.md` Operations H2 entry links TO `operations/00-index.md` per ROADMAP line 463 navigation-files-last principle. Phase 59 also consumes Phase 53 anchors for `docs/quick-ref-l1.md` / `docs/quick-ref-l2.md` Operations sections IF those exist (per Phase 59 ROADMAP line 332-336 hub integration scope).
- **DPO-Phase53-04:** **Phase 60 (Audit Harness v1.5 Finalization) registers `check-phase-53.mjs` in CI workflow + runs C13 broken-link automation against Phase 53 cross-link surface.** Phase 60 plan author adds `check-phase-53.mjs` to `.github/workflows/audit-harness-v1.5-integrity.yml` per AUDIT-06 contract + Phase 48 D-18 + Phase 60 ROADMAP line 343 lazy-skip pattern. C13 informational broken-link automation runs against Phase 53 cross-link surface (~10-12 cross-links per file: forward-link to v1.2 Windows migration content + soft cross-link from 00 to 03 + cross-platform inline blockquotes' 3 platform admin-setup cross-links + Phase 54 forward-promise text in 03). Phase 60 plan author runs C13 sweep against Phase 53 outputs as part of v1.5 close audit. **Optional sidecar amendment**: if Phase 60 expands C10 scope to operations/ files, lazy-add `c10_ops_allowlist[]` sidecar entry per Phase 48 D-15 YAGNI at THAT time (NOT at Phase 53 ship per CDI-Phase53-06).
- **DPO-Phase53-05:** **Phase 58 (DEFER-08 4-platform capability comparison) MAY consume Phase 53 anchors for its Co-Management row IF the comparison doc includes Co-Management as a domain axis.** ROADMAP line 312-316 lists 6 domain H2 sections (Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access) — Co-Management is NOT in the canonical 6-axis list, so Phase 58 likely does NOT consume Phase 53 anchors. If Phase 58 plan author elects to add Co-Management as a 7th axis, link-not-copy contract (PITFALL-7) means cells must hyperlink to Phase 53 H2 anchors (`00-overview.md#three-workload-slider-states`, `01-windows-tenant-attach.md#capability-comparison`, `02-windows-workload-sliders.md#endpoint-protection-high-risk`, `03-cocmgmt-migration-paths.md#autopatch-prerequisites`) — NOT duplicate prose.
- **DPO-Phase53-06:** **Phase 56 (Drift Detection + Tenant Migration) MAY cross-reference Phase 53 tenant attach disambiguation (01-windows-tenant-attach.md) in its tenant-to-tenant migration content.** Phase 56 ROADMAP line 286 covers tenant-to-tenant migration; tenant attach (Phase 53 COMG-03) is a DIFFERENT concept (per-device portal sync vs cross-tenant device migration), but Phase 56 plan author should consider a disambiguation cross-link or "do not confuse with tenant attach (Phase 53)" callout if the terminology collision matters in Phase 56 narrative. NOT mandated; per-phase author discretion.
- **DPO-Phase53-07:** **Phase 53 internal — single-commit atomicity (per D-14 + CDI-Phase53-04).** Phase 53 ships in a single atomic commit because the validator's V-53-22 append-only-style assertion at `operations/00-index.md` forces it (CDI-Phase52-04 + CDI-Phase52-07 inheritance). PITFALL-12 motivation does NOT apply (Phase 53 files not in supervision sidecar). Goldilocks-rule per Phase 51 DPO-07 + Phase 52 DPO-Phase52-06 precedent.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents (researcher, planner, executor, reviewer) MUST read these before authoring or implementing.**

### Phase 53 success criteria + traceability (authoritative scope)

- `.planning/ROADMAP.md` §"Phase 53: Co-Management Operational Docs" (lines 235-249) — Goal, dependencies, requirements (COMG-01..05), 5 success criteria, methodology notes (CB 2503, Resource Access deprecation, ops/00-index.md ownership)
- `.planning/ROADMAP.md` line 240 — SC#1 verbatim: 3 slider states + Pilot Intune collection-scoped framing (PITFALL-8 mandate)
- `.planning/ROADMAP.md` line 241 — SC#2 verbatim: low-risk-first migration sequence (Compliance → Resource Access → WU → Endpoint Protection → Device Configuration → Apps) + EP HIGH-RISK callout full Defender mandate
- `.planning/ROADMAP.md` line 242 — SC#3 verbatim: tenant attach vs full co-management focused disambiguation in `01-windows-tenant-attach.md`
- `.planning/ROADMAP.md` line 243 — SC#4 verbatim: macOS/iOS/Android admins reading co-management content see explicit callouts with analog migration paths (Jamf→Intune via ABM MDM transfer; iOS/Android MAM→MDM transition; Android legacy DA→Enterprise migration)
- `.planning/ROADMAP.md` line 244 — SC#5 verbatim: Windows Autopatch prereqs (Device Configuration AND Office Click-to-Run workloads moved to Intune)
- `.planning/ROADMAP.md` line 248 — Methodology: ConfigMgr CB 2503 (GA April 23 2025) is current branch reference; 7 co-management workloads
- `.planning/ROADMAP.md` line 248 — Methodology: Resource Access workload deprecated since CB 2203, slider mandated to Intune in CB 2403, document as deprecated
- `.planning/ROADMAP.md` line 249 — Methodology: operations/00-index.md created in this phase as top-level operations directory index (cross-referenced by Phases 54, 55, 56)
- `.planning/ROADMAP.md` line 448 — Shared Write Hotspot Ownership row for `docs/operations/00-index.md`: "Phase 53 (creates); Phase 53 creates; Phases 54–56 cross-reference only" (load-bearing for D-02 + DPO-Phase53-01)
- `.planning/ROADMAP.md` line 506 — Append-only H2-block contract attribution (Phase 42 D-03)
- `.planning/REQUIREMENTS.md` §COMG-01..05 (lines 42-46) — full requirement text with all 7 workloads enumerated for COMG-01; low-risk-first sequence for COMG-02; tenant attach disambiguation for COMG-03; macOS/iOS/Android analog paths for COMG-04; Windows Autopatch prereqs for COMG-05
- `.planning/REQUIREMENTS.md` lines 156-160 — Traceability mapping per requirement (load-bearing for D-01 file naming + D-07 Autopatch placement override + D-12 frontmatter resolution):
  - COMG-01 → `operations/co-management/00-overview.md`
  - COMG-02 → `operations/co-management/02-windows-workload-sliders.md` (filename per REQ verbatim — NOT `02-windows-workload-migration.md`)
  - COMG-03 → `operations/co-management/01-windows-tenant-attach.md`
  - COMG-04 → "Inline callout blocks in co-management docs — macOS/iOS/Android non-equivalent analog migration paths" (no specific file; spread across files per D-08)
  - COMG-05 → `operations/co-management/03-cocmgmt-migration-paths.md` (Autopatch prerequisites — load-bearing for D-07 override)
- `.planning/REQUIREMENTS.md` §AUDIT-06 (line 87) — Validator-as-deliverable + CI registration
- `.planning/REQUIREMENTS.md` §"Out of Scope" (lines 104-117) — explicit exclusions

### v1.5 research (authoritative for ConfigMgr scope, pitfalls, and tooling)

- `.planning/research/SUMMARY.md` lines 32-35 — ConfigMgr facts: CB 2503 GA April 23 2025; 7 co-management workloads; Resource Access workload deprecated since CB 2203, slider mandated Intune CB 2403
- `.planning/research/SUMMARY.md` line 78 — Phase 53 must-haves: co-management overview + workload slider migration; all 7 workloads; all 3 slider states including Pilot Intune; EP HIGH-RISK callout
- `.planning/research/SUMMARY.md` lines 93-94 — Phase 53 must-have differentiator: Windows Autopatch co-management prerequisite documentation (Device Config + Office C2R workloads must move before Autopatch)
- `.planning/research/SUMMARY.md` line 175 — Phase 53 parallelism: can run in parallel with Phases 51-52 (disjoint file sets)
- `.planning/research/SUMMARY.md` line 184 — Phase 53 4-file split + ops/00-index.md authoritative deliverables specification
- `.planning/research/SUMMARY.md` line 257 — Phase 53 confidence: CB 2503 + all 7 workloads HIGH confidence
- `.planning/research/SUMMARY.md` line 269 — Confidence table: ConfigMgr CB 2503 co-management workloads HIGH (Microsoft Learn updated 2026-04-15)
- `.planning/research/SUMMARY.md` line 295 — Canonical Microsoft Learn URL: https://learn.microsoft.com/en-us/intune/configmgr/comanage/workloads (updated 2026-04-15)
- `.planning/research/PITFALLS.md` §Pitfall 8 (lines 173-187) — **Co-management slider ambiguity** — load-bearing pitfall for Phase 53. Drives: (1) Pilot Intune disambiguation H2 + NEGATIVE assertion (D-06 + V-53-09/10); (2) workload table with Validate-Before-Moving column (D-04 + V-53-13); (3) Endpoint Protection HIGH-RISK three-layer callout (D-05 + V-53-15/16/17). Mitigation prose at line 179: "(1) explain the three slider states... (2) table the [7] workloads with a recommended migration order and a 'Validate before moving slider' column; (3) call out the Endpoint Protection workload specifically as HIGH-RISK — 'do not move this slider until Intune Defender for Endpoint policy is published, targeted, and confirmed reporting healthy.' Apply the 'what breaks' callout pattern established in v1.1 admin setup guides to each workload row."
- `.planning/research/PITFALLS.md` §Pitfall 9 (lines 191-205) — **WUfB Ring Naming Collision: Autopatch Rings vs WUfB Rings** — RELEVANT to Phase 53's `02-windows-workload-sliders.md` Windows Update Policies workload row (forward-link to Phase 54 PATCH-02 disambiguation). Phase 53 does NOT author the Phase 54 disambiguation; only forward-links.
- `.planning/research/PITFALLS.md` §Pitfall 13 (lines 278-295) — **Ops-domain false-positive risk** — relevant to V-53-10 NEGATIVE assertion path-restriction (CDI-Phase53-03)
- `.planning/research/PITFALLS.md` line 374 — Carry-Forward Methodology Pitfalls table: "Author co-management guide for Windows only, note macOS/iOS/Android 'not applicable' = Acceptable only if explicit scope callout is in the guide's opening; **contextual notes must still be present**" (drives 3B rejection + D-08 inline placement)
- `.planning/research/PITFALLS.md` lines 432-440 — Pitfall-to-mitigation table: P8 → Phase 53 (Workload table with three slider states and EP HIGH-RISK callout); P9 → Phase 54 (forward-link from Phase 53)

### v1.5 audit harness + sidecar (Phase 48 + Phase 51/52 deliverables; Phase 53 consumes)

- `scripts/validation/v1.5-milestone-audit.mjs` — C1-C13 checks. C10 hardcoded scope `linuxDocPaths()` at lines 141-178 (verified during Adversary review; load-bearing for D-12 + CDI-Phase53-06 — operations/ files NOT in C10 scope)
- `scripts/validation/v1.5-audit-allowlist.json` — sidecar with 7 top-level keys (schema_version, generated, phase, safetynet_exemptions, supervision_exemptions, cope_banned_phrases, c7_knox_allowlist). Phase 53 does NOT amend the sidecar at ship per Phase 48 D-15 YAGNI lazy-add (no false-positive surface). Lazy-add deferred to first-occurrence.
- `scripts/validation/regenerate-supervision-pins.mjs` — `--report` mode invoked pre-commit; `--self-test` invoked post-commit; should be unaffected since Phase 53 files are not in supervision sidecar (no Android/COBO/COPE/Knox/AOSP files modified)
- `scripts/validation/check-phase-49.mjs` / `check-phase-50.mjs` / `check-phase-51.mjs` / `check-phase-52.mjs` — direct file-reads-only / regex-based pattern reference for `check-phase-53.mjs` implementation per D-11

### Phase 51 / Phase 52 methodology precedent (Phase 53 inherits patterns)

- `.planning/phases/51-linux-l1-triage-runbooks-30-33/51-CONTEXT.md` D-04 (PITFALL-2 layered defense — tree level + runbook level + validator regex) / D-08 (platform-gate banner pattern; rejected for Phase 53 in favor of inline blockquote per 3A) / D-13 (read-vs-write apt distinction; not applicable to Phase 53 Windows surface) / D-19 (full validator scope precedent; inherited by D-10) / D-22 (single atomic commit Goldilocks-rule; inherited by D-14) / V-51-09 + V-51-19 (POSITIVE+NEGATIVE dual-defense pattern; inherited by D-06 + V-53-09/10) / V-51-21 (append-only assertion at hub file; structural inheritance though Phase 53 differs because its append target is brand-new — see CDI-Phase53-04)
- `.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-CONTEXT.md` D-01 (three-layer freshness-caveat pattern: matrix-cell + adjacent blockquote + per-occurrence inline; **directly inherited by D-05** for EP HIGH-RISK callout per CDI-Phase53-02 + Adversary-disprove-confirmed via DPO-Phase52-07 cross-domain transferability) / D-04 (frontmatter C10 contract; informs D-12 local enforcement) / D-10 (V-52-NN full validator scope precedent ~22 checks; informs D-10 22-26 V-53-NN) / D-11 (file-reads-only / no-shared-module pattern; inherited by D-11) / D-13 (single atomic commit forced by validator append-assertion; inherited by D-14) / DPO-Phase52-07 (three-layer pattern explicitly documented as cross-domain transferable; **load-bearing for 2B-3 winner adoption**)

### v1.4 / v1.3 / v1.2 admin-setup precedents (Phase 53 mirrors structurally for cross-platform analog content)

- `docs/admin-setup-macos/02-enrollment-profiles.md` — Cross-link target for D-08 macOS analog content (ABM MDM transfer mechanics)
- `docs/admin-setup-ios/00-enrollment-overview.md` — Cross-link target for D-08 iOS analog content (4-path enrollment overview)
- `docs/admin-setup-ios/04-byod-mam-overview.md` — Cross-link target for D-08 iOS analog content (MAM-WE → MDM transition)
- `docs/admin-setup-android/00-overview.md` — Cross-link target for D-08 Android analog content (5-mode enrollment overview; legacy DA deprecation context)

### v1.2 Windows migration content (Phase 53 forward-links per REQ COMG-02)

- `docs/reference/imaging-to-autopilot.md` — v1.2 imaging→Autopilot migration guide (forward-link target for V-53-23 + REQ COMG-02 verbatim "forward-link to v1.2 Windows migration content")
- `docs/device-operations/04-tenant-migration.md` — v1.2 tenant migration runbook (alternate forward-link target for V-53-23)
- `docs/lifecycle/04-esp.md` lines 137-138 — Existing co-management ESP differences callout (referenced as cross-link from `02-windows-workload-sliders.md` Endpoint Protection or Device Configuration row context)

### External canonical Microsoft documentation (HIGH confidence per SUMMARY.md line 269)

- https://learn.microsoft.com/en-us/intune/configmgr/comanage/workloads — ConfigMgr CB 2503 co-management workloads (updated 2026-04-15; primary reference for D-04 7-workload enumeration)
- https://learn.microsoft.com/en-us/intune/configmgr/comanage/overview — Co-management overview (slider state semantics)
- https://learn.microsoft.com/en-us/mem/intune/fundamentals/intune-add-management-of-windows-pcs — Tenant attach overview (D-03 disambiguation source)
- https://learn.microsoft.com/en-us/windows/deployment/windows-autopatch/prepare/windows-autopatch-prerequisites — Windows Autopatch prereqs (D-07 + REQ COMG-05 source)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **v1.1 "what breaks" callout pattern** (admin-setup-windows + admin-setup-apv2 guides) — established per-setting "what breaks" inline callout shape; D-04 V-53-13 Validate-Before-Moving column inherits this idiom per PITFALLS line 179 explicit attribution
- **Phase 51/52 V-NN validator-as-deliverable pattern** — `scripts/validation/check-phase-49.mjs` / `check-phase-50.mjs` / `check-phase-51.mjs` / `check-phase-52.mjs` provide direct implementation templates for `check-phase-53.mjs` (file-reads-only / no-shared-module / regex-based parsing per D-11)
- **Phase 51 D-08 platform-gate banner pattern** — REJECTED for Phase 53 cross-platform callouts (3A inline blockquote winner per D-08), but the platform-gate token shape is reusable for the file-top blockquote `> **Platform applicability:**` form
- **Phase 52 D-01 three-layer caveat pattern** — DIRECTLY INHERITED by D-05 EP HIGH-RISK callout (matrix-cell + adjacent blockquote + per-occurrence inline) per CDI-Phase53-02 + DPO-Phase52-07 cross-domain transferability endorsement
- **Phase 51 V-51-09 + V-51-19 POSITIVE+NEGATIVE dual-defense pattern** — DIRECTLY INHERITED by D-06 Pilot Intune disambiguation (V-53-09 POSITIVE H2-anchor + V-53-10 NEGATIVE banned-phrase regression-guard)
- **v1.5 audit harness + sidecar** — `scripts/validation/v1.5-milestone-audit.mjs` C1-C13 checks (Phase 53 satisfies C1-C9 + C12 blocking; C10 hardcoded `linuxDocPaths()` scope means Phase 53 files NOT enforced by C10 — frontmatter is locally enforced by V-53-06 per CDI-Phase53-06); `regenerate-supervision-pins.mjs --self-test` exits 0 unaffected (Phase 53 files not pinned)

### Established Patterns

- **Single atomic commit Goldilocks-rule** — Phase 50 D-19 → Phase 51 D-22 → Phase 52 D-13 + CDI-Phase52-07 + DPO-Phase52-06 → Phase 53 D-14 + CDI-Phase53-04 (validator append-assertion forces single commit; PITFALL-12 motivation does not apply because Phase 53 files not in supervision sidecar)
- **File-reads-only / no-shared-module / regex-based validator pattern** — Phase 48 D-25 + Phase 49 D-26 + Phase 51 D-20 + Phase 52 D-11 → Phase 53 D-11
- **Frontmatter C10-style local contract** — Phase 49 D-04 + Phase 50 + Phase 51 + Phase 52 D-04 establish `platform: <single-string>` + `audience: <L1|L2|admin>` + 60-day `last_verified` + `review_by`; Phase 53 D-12 inherits with `platform: Windows` + `audience: admin` for all 4 files + index
- **Append-only H2-block contract on shared files** (Phase 42 D-03) — Phase 51/52 used "append-after-last-existing-H2"; Phase 53 introduces a NEW shape: "single-H2-only at file creation, append-only contract activates from Phase 54+" (V-53-22 + DPO-Phase53-01)
- **Lazy-add forward-extension on sidecar** — Phase 48 D-15 YAGNI; Phase 53 explicitly does NOT amend sidecar at ship (no false-positive surface; deferred to first-occurrence per CDI-Phase53-03 + Phase 60 DPO-Phase53-04)

### Integration Points

- **`docs/operations/00-index.md` (NEW in Phase 53)** — Cross-referenced by Phase 54 (`operations/patch-management/`), Phase 55 (`operations/app-lifecycle/`), Phase 56 (`operations/drift-migration/`) per ROADMAP line 448. Phase 53 owns; Phases 54-56 cross-reference only.
- **`docs/index.md` Operations H2 (Phase 59 ownership)** — Phase 59 appends Operations H2 linking to `operations/00-index.md`. Phase 53 does NOT touch `docs/index.md` (out of scope; navigation-files-last per Phase 59).
- **v1.2 Windows migration content** — `imaging-to-autopilot.md` + `device-operations/04-tenant-migration.md` cross-linked from `02-windows-workload-sliders.md` per V-53-23
- **Phase 54 PATCH-02 forward-promise** — `02-windows-workload-sliders.md` Windows Update Policies row + `03-cocmgmt-migration-paths.md` Autopatch prereqs forward-link to Phase 54 patch-management (placeholder retrofit per Phase 30 D-22 — author with Phase 54 ROADMAP-line-256 anchor surface)
- **Phase 55 APP-01 forward-promise** — `02-windows-workload-sliders.md` Client Apps row forward-link to Phase 55 app-lifecycle supersedence content (placeholder retrofit similar shape)
- **Phase 56 tenant migration tangential** — Phase 56 may cross-reference Phase 53 `01-windows-tenant-attach.md` for terminology disambiguation (DPO-Phase53-06; not mandated)

</code_context>

<specifics>
## Specific Ideas

- **PITFALL-8 verbatim mandate is the load-bearing PITFALLS-line-179 obligation triple**: (1) explain three slider states; (2) table workloads with migration order + Validate-Before-Moving column; (3) call out Endpoint Protection HIGH-RISK with full Defender mandate. All three obligations have dedicated Phase 53 deliverable surfaces (D-04 + D-05 + D-06).
- **"Apply the 'what breaks' callout pattern established in v1.1 admin setup guides to each workload row"** (PITFALLS line 179 verbatim) — Phase 53 D-04 Validate-Before-Moving column directly inherits this idiom from v1.1 admin-setup-windows + admin-setup-apv2 per-setting "what breaks" callout pattern.
- **"7 co-management workloads" not "8"** — historical drift in some Microsoft Learn copies says "8 workloads" but CB 2503 (current) has 7 (the 8th was an early-CB Office 365 Click-to-Run / O365 split that consolidated). Phase 53 D-04 enumerates the 7 canonical workloads per SUMMARY line 33.
- **Phase 51/52 adversarial-review methodology** — Phase 53 follows the same Finder/Adversary/Referee scored pattern for documentation methodology (per project owner's invocation). Adversary disprove evidence verified via direct file reads (ROADMAP line 448 + C10 source code at `v1.5-milestone-audit.mjs:141-178` + sidecar key count + Phase 52 DPO-Phase52-07 cross-domain transferability + 48-CONTEXT lazy-add endorsement at line 192).
- **Filename `02-windows-workload-sliders.md`** (NOT `02-windows-workload-migration.md`) — REQ line 157 verbatim. The earlier "migration" framing was a Finder analytical placeholder; canonical filename is "sliders" per REQ traceability.
- **03 is Windows Autopatch prereqs, NOT cross-platform analog migration paths** — REQ line 160 verbatim COMG-05 mapping. The filename's plural "paths" reflects Windows-internal migration paths from co-management (e.g., → Autopatch). Cross-platform analog content lives INLINE in 00/01/02 per REQ line 159.
- **Resource Access workload row collapses cleanly to "deprecated"** — CB 2503 still lists Resource Access in the workloads table for migration-path completeness, but the slider was mandated to Intune in CB 2403. Phase 53 D-04 V-53-26 enforces deprecation note; row may be styled with strikethrough or italic per CD-02.

</specifics>

<deferred>
## Deferred Ideas

- **Add `c10_cross_platform_allowlist[]` or `c10_ops_allowlist[]` sidecar entry** — Deferred to Phase 60 audit harness finalization OR first-false-positive surface (lazy-add per Phase 48 D-15 YAGNI). Not authored at Phase 53 ship because C10 doesn't run on operations/ files (verified at `v1.5-milestone-audit.mjs:141-178`).
- **Mermaid slider-state diagram or workload-flow visualization** — Author discretion per CD-04. Validator does NOT assert Mermaid presence. Could be added to `00-overview.md` for visual learners; defer if scope-tight.
- **Co-Management as 7th domain axis in Phase 58 4-platform comparison doc** — Phase 58 ROADMAP line 312-316 lists 6 canonical axes (Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access); Co-Management is NOT canonical. Deferred per DPO-Phase53-05 author discretion.
- **Tenant-attach-vs-tenant-migration disambiguation in Phase 56** — terminology collision between Phase 53 tenant attach (intra-tenant, portal-only) and Phase 56 tenant migration (cross-tenant, full re-enrollment). Per DPO-Phase53-06 Phase 56 author discretion; not mandated cross-link.
- **Phase 54 PATCH-02 forward-promise text retrofit** — Phase 53 `02-windows-workload-sliders.md` Windows Update Policies row + `03-cocmgmt-migration-paths.md` Autopatch prereqs forward-link to Phase 54 PATCH-02 content. Author per Phase 30 D-22 placeholder retrofit precedent (Phase 53 ships with forward-promise; Phase 54 ship surgically retrofits the Phase 53 cross-link anchors). NOT a Phase 53 commit obligation.
- **Phase 55 APP-01 forward-promise text retrofit** — Same shape as above; `02-windows-workload-sliders.md` Client Apps row forward-link. Phase 55 ships retrofit.
- **Phase 59 hub navigation Operations H2** — `docs/index.md` Operations H2 + quick-ref-l1/l2 Operations sections. Phase 59 ownership per ROADMAP line 463; Phase 53 does NOT touch hub navigation.
- **Phase 60 CI registration of `check-phase-53.mjs`** — Add to `.github/workflows/audit-harness-v1.5-integrity.yml`. Phase 60 ownership per AUDIT-06 + DPO-Phase53-04.

### Reviewed Todos (not folded)

None — Phase 53 scope review at discuss-phase identified no pending todos relevant to this phase. The /gsd-todo backlog was checked at workflow init; no co-management or operational-docs items pending.

</deferred>

---

*Phase: 53-co-management-operational-docs*
*Context gathered: 2026-04-27*
