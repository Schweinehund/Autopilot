---
phase: 53
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/co-management/03-cocmgmt-migration-paths.md
autonomous: true
requirements: [COMG-05]
requirements_addressed: [COMG-05]

must_haves:
  truths:
    - "docs/operations/co-management/03-cocmgmt-migration-paths.md exists with `platform: Windows` + `audience: admin` + `last_verified: 2026-04-27` + `review_by: 2026-06-26` frontmatter (D-12 + V-53-06)"
    - "File contains literal H2 anchor `## Autopatch Prerequisites {#autopatch-prerequisites}` so the soft cross-link from `00-overview.md#autopatch-prerequisites` (V-53-24) resolves correctly"
    - "File contains all 4 V-53-20 literal tokens: `Device Configuration`, `Office Click-to-Run`, `Autopatch`, `prerequisite` (or `prerequisites`)"
    - "File documents ALL THREE Autopatch workload prerequisites per RESEARCH Area 2 verified Microsoft Learn (2026-02-26): Windows Update Policies workload + Device Configuration workload + Office Click-to-Run Apps workload — REQUIREMENTS COMG-05 is incomplete (lists only 2); research-flagged correction adds the third (Windows Update Policies)"
    - "File does NOT contain a `> **Platform applicability:**` blockquote — V-53-21 NEGATIVE regression-guard (CDI-Phase53-01) — 03 is Windows Autopatch prereqs ONLY, not cross-platform analog migration paths"
    - "File does NOT contain banned phrasings `partially migrated` / `fully migrated` (case-insensitive) — V-53-10 NEGATIVE regression-guard"
    - "File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens (V-53-25)"
  artifacts:
    - path: "docs/operations/co-management/03-cocmgmt-migration-paths.md"
      provides: "Windows Autopatch co-management prerequisites guide (COMG-05) — three-workload prerequisite checklist + Pilot Intune acceptable note + forward-link to Phase 54 patch-management for WUfB-vs-Autopatch disambiguation"
      contains:
        - "platform: Windows"
        - "audience: admin"
        - "last_verified: 2026-04-27"
        - "review_by: 2026-06-26"
        - "## Autopatch Prerequisites"
        - "{#autopatch-prerequisites}"
        - "Device Configuration"
        - "Office Click-to-Run"
        - "Windows Update"
        - "Autopatch"
        - "prerequisite"
        - "Pilot Intune"
        - "Intune"
  key_links:
    - from: "docs/operations/co-management/03-cocmgmt-migration-paths.md"
      to: "docs/operations/co-management/00-overview.md"
      via: "intro paragraph back-reference for slider model context"
      pattern: "00-overview\\.md"
    - from: "docs/operations/co-management/03-cocmgmt-migration-paths.md"
      to: "docs/operations/co-management/02-windows-workload-sliders.md"
      via: "back-reference to migration sequence"
      pattern: "02-windows-workload-sliders\\.md"
    - from: "docs/operations/co-management/03-cocmgmt-migration-paths.md"
      to: "docs/operations/patch-management/ (Phase 54 forward-promise)"
      via: "CD-07 author discretion — forward-promise to Phase 54 WUfB-vs-Autopatch disambiguation per PITFALL-9"
      pattern: "patch-management"
---

<objective>
Author `docs/operations/co-management/03-cocmgmt-migration-paths.md` (COMG-05) — the Windows Autopatch co-management prerequisites guide. Per CONTEXT D-07 (off-ballot REFEREE OVERRIDE), Autopatch prereqs are the PRIMARY scope of this file; the filename's plural "paths" reflects "Windows-internal migration paths from co-management" (the path TO Autopatch). Per CDI-Phase53-01 + V-53-21, this file does NOT contain a cross-platform `> **Platform applicability:**` blockquote — that pattern is exclusive to 00/01/02.

Purpose: Implements CONTEXT D-07 (Autopatch primary in 03) + D-09 (NO cross-platform blockquote in 03 — V-53-21 regression-guard) + D-12 (frontmatter contract). Validator V-53-20 enforces the four literal Autopatch tokens; V-53-21 enforces the negative regression-guard against accidental inclusion of the cross-platform blockquote.

This is the COMG-05 deliverable per REQUIREMENTS.md line 160 traceability mapping.

CRITICAL CORRECTION from REQUIREMENTS.md COMG-05 wording (per RESEARCH Area 2 verified Microsoft Learn 2026-02-26): Autopatch requires THREE workload prerequisites, not two. REQ COMG-05 lists Device Configuration AND Office Click-to-Run; the verified Microsoft Learn Autopatch prerequisites page also lists Windows Update Policies. This file MUST document all three. V-53-20 still passes (asserts only the two REQ-named tokens) — adding the third workload (Windows Update Policies) is additive and does not break any assertion. The plan author must explicitly include Windows Update Policies in the prerequisite checklist.

Wave 1 — parallel with 53-01 / 53-02 / 53-03 / 53-05 / 53-06.

Output: 1 new file at `docs/operations/co-management/03-cocmgmt-migration-paths.md`.

DO NOT commit yet — atomic commit owned by 53-07 per CONTEXT D-14 + CDI-Phase53-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-co-management-operational-docs/53-CONTEXT.md
@.planning/phases/53-co-management-operational-docs/53-RESEARCH.md
@.planning/phases/53-co-management-operational-docs/53-PATTERNS.md
@.planning/phases/53-co-management-operational-docs/53-VALIDATION.md
@docs/admin-setup-apv2/00-overview.md
@docs/device-operations/04-tenant-migration.md

<interfaces>
**File this plan creates:** `docs/operations/co-management/03-cocmgmt-migration-paths.md`

**Sibling files in same directory (created in same atomic commit):**
- `00-overview.md` — created by 53-01 (links TO this file via soft cross-link `03-cocmgmt-migration-paths.md#autopatch-prerequisites`)
- `01-windows-tenant-attach.md` — created by 53-02 (links TO this file via Windows Autopatch row in comparison table)
- `02-windows-workload-sliders.md` — created by 53-03 (links TO this file via Windows Update Policies row + Office Click-to-Run row)

**Validator V-53-NN assertions enforced against this file:**

| V-NN | Assertion |
|------|-----------|
| V-53-04 | File exists |
| V-53-06 | Frontmatter: `platform: Windows`, `audience: admin`, 60-day cycle |
| V-53-10 | NEGATIVE: NO `partially migrated` / `fully migrated` (case-insensitive); file is in CO_MGMT_FILES scope |
| V-53-20 | All 4 literal tokens: `Device Configuration` AND `Office Click-to-Run` AND `Autopatch` AND `prerequisite` (or `prerequisites`) |
| V-53-21 | NEGATIVE: NO `> **Platform applicability:**` blockquote — regression-guard against accidental cross-platform inclusion |
| V-53-25 | NO TBD/TODO/FIXME/XXX/PLACEHOLDER |

**CRITICAL — V-53-21 regression-guard (CDI-Phase53-01):** This file MUST NOT contain the literal substring `> **Platform applicability:**`. The cross-platform analog content lives ONLY in 00/01/02. Per D-09: "03's primary scope is Windows Autopatch prereqs per REQ line 160 (NOT cross-platform analog migration paths)."

**Frontmatter contract (D-12 verbatim):**
```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: all
audience: admin
platform: Windows
---
```

**Research Area 2 verified Autopatch prereqs (Microsoft Learn updated 2026-02-26) — THREE workloads:**

> Must have the following co-management workloads enabled and set to either Intune or Pilot Intune:
> - Windows Update policies workload
> - Device configuration workload
> - Office Click-to-Run apps workload
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/operations/co-management/03-cocmgmt-migration-paths.md (frontmatter + H1 + intro + Autopatch Prerequisites H2 with anchor + three-workload prerequisite checklist + Pilot Intune acceptable note + Phase 54 WUfB-vs-Autopatch forward-promise + Related Resources). DO NOT include any > **Platform applicability:** blockquote — V-53-21 regression-guard.)</name>
  <files>docs/operations/co-management/03-cocmgmt-migration-paths.md</files>
  <read_first>
    - .planning/phases/53-co-management-operational-docs/53-CONTEXT.md (D-07 Autopatch primary in 03; D-09 NO cross-platform blockquote in 03; CDI-Phase53-01 V-53-21 regression-guard; D-12 frontmatter; CD-07 forward-promise to Phase 54 acceptable)
    - .planning/phases/53-co-management-operational-docs/53-RESEARCH.md (Research Area 2 — CRITICAL: THREE workloads required per Microsoft Learn 2026-02-26, not two as in REQUIREMENTS COMG-05; verbatim Autopatch prereqs text; Pitfall 1 active risk)
    - .planning/phases/53-co-management-operational-docs/53-PATTERNS.md (Section "03-cocmgmt-migration-paths.md" lines 430-538 — paste-ready snippets including the prerequisite checklist + critical-correction blockquote + critical-warning blockquote)
    - .planning/phases/53-co-management-operational-docs/53-VALIDATION.md (Per-Task Verification Map row 53-04-01: V-53-06/20/21)
    - docs/admin-setup-apv2/00-overview.md (analog file lines 18-22 prerequisites checklist shape)
    - docs/device-operations/04-tenant-migration.md (analog file lines 15-19 critical-warning blockquote shape)
  </read_first>
  <behavior>
    - File `docs/operations/co-management/03-cocmgmt-migration-paths.md` exists
    - Frontmatter: `platform: Windows`, `audience: admin`, `last_verified: 2026-04-27`, `review_by: 2026-06-26`, `applies_to: all`
    - H1 + intro paragraph
    - File does NOT contain `> **Platform applicability:**` blockquote anywhere (V-53-21 regression-guard)
    - File contains literal H2 `## Autopatch Prerequisites {#autopatch-prerequisites}` with H2 anchor (so 00-overview.md soft cross-link to `#autopatch-prerequisites` resolves)
    - File documents ALL THREE Autopatch workload prerequisites: Windows Update Policies, Device Configuration, Office Click-to-Run Apps
    - File contains all 4 V-53-20 literal tokens: `Device Configuration`, `Office Click-to-Run`, `Autopatch`, `prerequisite` (or `prerequisites`)
    - File contains a research-flagged correction note about REQUIREMENTS COMG-05 documenting only 2 workloads vs verified Microsoft Learn 3 workloads
    - File contains Phase 54 WUfB-vs-Autopatch forward-promise (CD-07 author discretion)
    - NO banned phrasings; NO TBD/TODO tokens
  </behavior>
  <action>
Create the file `docs/operations/co-management/03-cocmgmt-migration-paths.md` with the following content sections IN ORDER.

**CRITICAL CONSTRAINT — V-53-21 regression-guard:** This file MUST NOT contain the literal substring `> **Platform applicability:**` anywhere. If you find yourself about to copy/adapt the blockquote from 00/01/02, STOP — that pattern is forbidden in 03. The cross-platform analog content lives EXCLUSIVELY in 00/01/02 per D-08 + D-09. Per CDI-Phase53-01: 03 is Windows Autopatch prereqs (REQ line 160), not cross-platform analog migration paths.

**Section 1 — Frontmatter (D-12 + V-53-06; copy verbatim):**

```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: all
audience: admin
platform: Windows
---
```

**Section 2 — H1 + intro paragraph (NO Platform applicability blockquote — V-53-21 regression-guard; this file goes directly from frontmatter to H1):**

```markdown
# Migration Paths and Autopatch Prerequisites

This guide covers Windows-internal migration paths from co-management — specifically, the
Windows Autopatch prerequisite chain. The filename's plural "paths" reflects Windows-side
migration paths (co-management → Autopatch); cross-platform analog migration paths (Jamf → Intune,
MAM → MDM, Android Enterprise modes) are covered inline in
[Co-Management Overview](00-overview.md), [Windows Tenant Attach](01-windows-tenant-attach.md),
and [Workload Slider Migration](02-windows-workload-sliders.md).

For the workload model and slider semantics, see [Co-Management Overview](00-overview.md). For
the workload migration sequence, see [Workload Slider Migration](02-windows-workload-sliders.md).
```

**Section 3 — Autopatch overview:**

```markdown
## Windows Autopatch Overview

Windows Autopatch is a co-management successor managed-update service that automates Windows
quality update (WUfB) ring management for enrolled devices. Autopatch eliminates manual WUfB
ring authoring by managing rings (Test, First, Fast, Broad) automatically based on Microsoft's
quality-signal feedback loops.

Autopatch is a co-management migration path: tenants moving from ConfigMgr-managed Windows Update
to fully cloud-managed updates typically migrate the Windows Update Policies workload to Intune
first, then enable Autopatch to take over ring management. Co-management's Pilot Intune state is
acceptable for Autopatch enablement (devices in pilot collection are eligible).
```

**Section 4 — Autopatch Prerequisites H2 with anchor (V-53-20 literal token coverage; V-53-24 anchor target for soft cross-link from 00-overview.md):**

```markdown
## Autopatch Prerequisites {#autopatch-prerequisites}

Windows Autopatch requires co-management with the following **three workloads** set to
**Pilot Intune** or **Intune** before Autopatch can be enabled:

1. **Windows Update Policies** workload — Slider must be at Pilot Intune or Intune. This
   workload's policies (deferral, ring assignment, deadline enforcement) become Autopatch's
   surface for ring management.
2. **Device Configuration** workload — Slider must be at Pilot Intune or Intune. Configuration
   profile assignments must be deliverable from Intune before Autopatch ring management can apply
   matching configuration.
3. **Office Click-to-Run Apps** workload — Slider must be at Pilot Intune or Intune. Microsoft 365
   Apps update channel and version policies become Intune-managed; Autopatch coordinates Office
   updates alongside Windows quality updates.

If any workload is set to **Pilot Intune** (rather than full Intune), devices must be members of
the corresponding pilot collection — Autopatch only manages devices for which all three
workloads are Intune-managed (either via full Intune state or via Pilot Intune state with the
device in the pilot collection).

> **Critical correction note:** Early documentation referenced only Device Configuration and
> Office Click-to-Run as Autopatch prerequisites (REQUIREMENTS.md COMG-05 wording is incomplete).
> The current Microsoft Learn Autopatch prerequisites page (updated 2026-02-26) specifies all
> three workloads including **Windows Update Policies**. Document all three when planning a
> migration; verifying only Device Configuration and Office Click-to-Run is insufficient.
```

CRITICAL — V-53-20 verifies all 4 literal tokens: `Device Configuration`, `Office Click-to-Run`, `Autopatch`, `prerequisite` (or `prerequisites`). The section above contains all four. The H2 anchor `{#autopatch-prerequisites}` is the V-53-24 cross-link target.

**Section 5 — Prerequisites checklist (analog from `docs/admin-setup-apv2/00-overview.md` lines 18-22 prerequisites shape):**

```markdown
### Pre-Enablement Checklist

Before enabling Windows Autopatch in your tenant, verify the following:

- [ ] **Co-management enabled** — Devices are co-managed (ConfigMgr client + Intune enrollment).
      Verify via ConfigMgr console > Administration > Cloud Services > Co-management; confirm
      enrollment count matches expected device cohort.
- [ ] **Windows Update Policies workload** — Slider set to Pilot Intune or Intune. Verify in
      ConfigMgr Co-management Properties > Workloads tab.
- [ ] **Device Configuration workload** — Slider set to Pilot Intune or Intune. Note that moving
      this workload also implicitly switches Resource Access and Endpoint Protection workloads;
      see [Workload Slider Migration > Endpoint Protection](02-windows-workload-sliders.md) for
      the EP HIGH-RISK callout that applies before this implicit switch.
- [ ] **Office Click-to-Run Apps workload** — Slider set to Pilot Intune or Intune.
- [ ] **Pilot collections configured** — If any of the three workloads is at Pilot Intune,
      target devices must be in the corresponding pilot collection.
- [ ] **Autopatch licensing** — Verify Windows Autopatch licensing prerequisites (Windows 10/11
      Enterprise E3+ with Microsoft 365 E3+ or equivalent); see Microsoft Learn Autopatch
      prerequisites for current license requirements.
- [ ] **Entra ID requirements** — Devices are Entra ID joined or Hybrid Entra ID joined per
      Autopatch device-eligibility rules.

> **Before enabling Autopatch:**
>
> - **All three workloads must be confirmed** — Windows Update Policies AND Device Configuration
>   AND Office Click-to-Run Apps must be at Pilot Intune or Intune BEFORE Autopatch enablement.
> - **Pilot collections must be correctly scoped** — If using Pilot Intune for any workload,
>   devices not in the collection are still managed by ConfigMgr and are NOT Autopatch-eligible.
> - **Endpoint Protection coverage** — If Device Configuration migration is the trigger for
>   Endpoint Protection implicit-switching, verify Intune Defender for Endpoint policy is healthy
>   first (see [02-windows-workload-sliders.md](02-windows-workload-sliders.md) HIGH-RISK callout).
```

**Section 6 — Pilot Intune acceptable for Autopatch (Research Area 2 nuance):**

```markdown
## Pilot Intune State Acceptable for Autopatch

Microsoft Autopatch documentation explicitly accepts either **Intune** or **Pilot Intune** for
all three workloads. Tenants do NOT need to fully migrate (move sliders to Intune for the entire
fleet) before enabling Autopatch — phased rollouts using Pilot Intune are supported.

If using Pilot Intune for one or more of the three workloads, ensure that:

- The pilot collection membership is the same set of devices that will be enrolled in Autopatch
- The pilot collection is regularly reviewed for membership accuracy (devices entering the
  collection become Autopatch-eligible; devices leaving the collection lose Autopatch eligibility)
- The three workloads use coherent pilot collection scoping — split scoping (different pilot
  collections per workload) creates Autopatch-eligibility ambiguity

For full migration (all devices Autopatch-eligible), move all three sliders to **Intune**.
```

**Section 7 — Phase 54 forward-promise (CD-07 author discretion — acceptable per Phase 30 D-22 placeholder retrofit precedent):**

```markdown
## Related Patch Management Content (Phase 54 Forward-Promise)

> **Note:** Windows Update Policies workload migration — including WUfB ring topology and
> WUfB-vs-Autopatch ring naming-collision disambiguation (PITFALL-9) — is covered in Phase 54
> patch-management content. When Phase 54 ships, this section will be retrofitted with a direct
> cross-link to `docs/operations/patch-management/01-windows-wufb-rings.md`. In the interim,
> refer to Microsoft Learn Windows Autopatch documentation for ring topology guidance.
```

**Section 8 — Related resources / footer:**

```markdown
## Related Resources

- [Co-Management Overview](00-overview.md) — workload model + three slider states + Pilot Intune
  collection-scoping
- [Windows Tenant Attach](01-windows-tenant-attach.md) — tenant attach vs full co-management
  disambiguation (Windows Autopatch eligibility row)
- [Workload Slider Migration](02-windows-workload-sliders.md) — low-risk-first migration sequence
  with EP HIGH-RISK callout (relevant for Device Configuration implicit-switching scenario)

## External References

- [Windows Autopatch Prerequisites (Microsoft Learn)](https://learn.microsoft.com/en-us/windows/deployment/windows-autopatch/prepare/windows-autopatch-prerequisites)
- [Co-Management Workloads (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/configmgr/comanage/workloads)
- [Windows Autopatch Overview (Microsoft Learn)](https://learn.microsoft.com/en-us/windows/deployment/windows-autopatch/overview/windows-autopatch-overview)
```

After authoring, run these grep checks:

```bash
# Frontmatter
head -7 docs/operations/co-management/03-cocmgmt-migration-paths.md | grep -E "^(platform|audience|last_verified|review_by|applies_to):"

# H2 anchor for V-53-24 cross-link target
grep -c "## Autopatch Prerequisites {#autopatch-prerequisites}" docs/operations/co-management/03-cocmgmt-migration-paths.md   # ≥1

# V-53-20 literal tokens (all 4 must be present)
for tok in "Device Configuration" "Office Click-to-Run" "Autopatch" "prerequisite"; do
  grep -c "$tok" docs/operations/co-management/03-cocmgmt-migration-paths.md
done

# Three-workload prereqs documentation
grep -c "Windows Update" docs/operations/co-management/03-cocmgmt-migration-paths.md   # ≥3 (research-flagged correction surfaces this)

# CRITICAL — V-53-21 regression-guard (must be 0!)
grep -c "> \*\*Platform applicability:\*\*" docs/operations/co-management/03-cocmgmt-migration-paths.md   # MUST be 0

# NEGATIVE V-53-10
grep -ic "partially migrated\|fully migrated" docs/operations/co-management/03-cocmgmt-migration-paths.md   # 0

# TBD scan
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/co-management/03-cocmgmt-migration-paths.md   # empty
```

DO NOT commit yet — atomic commit owned by 53-07.
  </action>
  <verify>
    <automated>test -f docs/operations/co-management/03-cocmgmt-migration-paths.md &amp;&amp; grep -q "## Autopatch Prerequisites {#autopatch-prerequisites}" docs/operations/co-management/03-cocmgmt-migration-paths.md &amp;&amp; grep -q "Device Configuration" docs/operations/co-management/03-cocmgmt-migration-paths.md &amp;&amp; grep -q "Office Click-to-Run" docs/operations/co-management/03-cocmgmt-migration-paths.md &amp;&amp; grep -q "Autopatch" docs/operations/co-management/03-cocmgmt-migration-paths.md &amp;&amp; grep -q "prerequisite" docs/operations/co-management/03-cocmgmt-migration-paths.md &amp;&amp; ! grep -q "> \*\*Platform applicability:\*\*" docs/operations/co-management/03-cocmgmt-migration-paths.md &amp;&amp; ! grep -qi "partially migrated\|fully migrated" docs/operations/co-management/03-cocmgmt-migration-paths.md &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/operations/co-management/03-cocmgmt-migration-paths.md` exists
    - Frontmatter contains `platform: Windows`, `audience: admin`, `last_verified: 2026-04-27`, `review_by: 2026-06-26`, `applies_to: all`
    - File contains literal H2 line `## Autopatch Prerequisites {#autopatch-prerequisites}` (the H2 anchor target for V-53-24 soft cross-link from 00-overview.md)
    - File contains all 4 V-53-20 literal tokens: `Device Configuration`, `Office Click-to-Run`, `Autopatch`, `prerequisite`
    - File documents all THREE Autopatch workload prerequisites (Windows Update Policies + Device Configuration + Office Click-to-Run Apps) per RESEARCH Area 2 verified Microsoft Learn 2026-02-26
    - File contains a research-flagged correction note explaining the REQUIREMENTS.md COMG-05 incompleteness (only 2 workloads vs verified 3)
    - File does NOT contain literal `> **Platform applicability:**` blockquote anywhere (V-53-21 NEGATIVE regression-guard — CRITICAL)
    - File does NOT contain banned phrasings: `partially migrated`, `partially-migrated`, `partially_migrated`, `fully migrated`, `fully-migrated`, `fully_migrated`
    - File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens
    - File contains pre-enablement checklist with `[ ]` checkbox items per analog shape from `admin-setup-apv2/00-overview.md`
  </acceptance_criteria>
  <done>03-cocmgmt-migration-paths.md authored as Windows-only Autopatch prereqs guide; V-53-04/06/10/20/21/25 will pass; V-53-21 regression-guard satisfied (no Platform applicability blockquote); no commit (53-07 owns atomicity).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → reader | Documentation content; Autopatch prereqs accuracy is operational truth |
| REQUIREMENTS.md → research → reader | REQ COMG-05 is incomplete (2 workloads); research correction (3 workloads) flows through plan to file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04-01 | Information disclosure (operational harm) | Admin documents Autopatch prereqs as 2 workloads (per REQ wording) → enables Autopatch with Windows Update Policies still on ConfigMgr → Autopatch ring management fails or conflicts | mitigate | Research-flagged correction in Section 4 + Section 5 checklist enumerates all 3 workloads. Critical-correction blockquote explicitly calls out the REQ incompleteness. |
| T-53-04-02 | Tampering (regression) | Future edit accidentally adds `> **Platform applicability:**` blockquote to 03 (e.g., during a copy-paste from 00/01/02) | mitigate | V-53-21 NEGATIVE regression-guard at validator level; CDI-Phase53-01 flags this as a load-bearing distinction. Pre-commit gate fails if blockquote appears. |
| T-53-04-03 | Information disclosure (cross-link drift) | 00-overview.md soft cross-link `03-cocmgmt-migration-paths.md#autopatch-prerequisites` does not resolve because anchor missing | mitigate | H2 anchor `{#autopatch-prerequisites}` pinned literal in Section 4; V-53-24 enforces the cross-link from 00 (validator runs after both files exist). Markdown-link-check informational pass at pre-commit. |
| T-53-04-04 | Tampering (factual drift) | Autopatch prereqs page updates (e.g., new 4th workload, removed 1 workload) | accept | 60-day `review_by: 2026-06-26` cycle; cited Microsoft Learn date 2026-02-26. |

Severity: LOW (documentation), but T-53-04-01 has MEDIUM operational severity if the research correction is not applied (admin enables Autopatch with WU Policies on ConfigMgr and gets failed enablement or ring-management conflict).
</threat_model>

<verification>
After Task 1 completes:

```bash
test -f docs/operations/co-management/03-cocmgmt-migration-paths.md
echo "file-exists: $?"

# Frontmatter (V-53-06)
head -7 docs/operations/co-management/03-cocmgmt-migration-paths.md | grep -E "^(platform|audience|last_verified|review_by|applies_to):"

# Autopatch Prerequisites H2 with anchor (V-53-24 target + V-53-20 surface)
grep -n "## Autopatch Prerequisites {#autopatch-prerequisites}" docs/operations/co-management/03-cocmgmt-migration-paths.md   # ≥1 line

# V-53-20 four literal tokens
for tok in "Device Configuration" "Office Click-to-Run" "Autopatch" "prerequisite"; do
  count=$(grep -c "$tok" docs/operations/co-management/03-cocmgmt-migration-paths.md)
  echo "$tok: $count"
done

# Three-workload prereqs surface (research correction)
grep -c "Windows Update Policies\|Windows Update policies" docs/operations/co-management/03-cocmgmt-migration-paths.md   # ≥2

# CRITICAL V-53-21 regression-guard — MUST be 0
echo -n "Platform applicability blockquote count (must be 0): "
grep -c "> \*\*Platform applicability:\*\*" docs/operations/co-management/03-cocmgmt-migration-paths.md

# V-53-10 NEGATIVE
grep -ic "partially migrated\|fully migrated" docs/operations/co-management/03-cocmgmt-migration-paths.md   # 0

# V-53-25 TBD scan
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/co-management/03-cocmgmt-migration-paths.md
# expect empty
```

DO NOT commit — atomic commit owned by 53-07.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 53 Success Criteria + REQUIREMENTS.md COMG-05:

- **SC#5** (Windows Autopatch prereqs): Fully satisfied — Autopatch Prerequisites H2 + three-workload checklist + Pilot Intune acceptable note
- **COMG-05** (Autopatch prereqs primary content per REQ line 160): Fully covered — file is the COMG-05 primary surface

Plan complete when:

- [ ] File exists at `docs/operations/co-management/03-cocmgmt-migration-paths.md`
- [ ] V-53-04/06/10/20/21/25 all pass against this file
- [ ] V-53-21 regression-guard: file does NOT contain `> **Platform applicability:**` (CRITICAL)
- [ ] All three Autopatch workload prereqs documented (research-flagged correction applied)
- [ ] H2 anchor `{#autopatch-prerequisites}` present (cross-link target for V-53-24 from 00-overview.md)
- [ ] No commit — 53-07 owns atomicity
</success_criteria>

<output>
After completion, create `.planning/phases/53-co-management-operational-docs/53-04-SUMMARY.md` documenting:

- File path + line count
- Sections authored (frontmatter, H1 + intro, Windows Autopatch Overview, Autopatch Prerequisites H2 with anchor, Pre-Enablement Checklist with critical-warning blockquote, Pilot Intune State Acceptable, Phase 54 Forward-Promise, Related Resources)
- V-53-NN assertions this file satisfies (V-53-04, V-53-06, V-53-10, V-53-20, V-53-21, V-53-25)
- THREE-workload research correction applied (Windows Update Policies + Device Configuration + Office Click-to-Run Apps; documented critical-correction blockquote)
- V-53-21 regression-guard verification (grep count of `> **Platform applicability:**` is 0)
- H2 anchor `{#autopatch-prerequisites}` present (V-53-24 cross-link target from 00-overview.md)
- Note: NO COMMIT — atomic commit owned by 53-07
</output>