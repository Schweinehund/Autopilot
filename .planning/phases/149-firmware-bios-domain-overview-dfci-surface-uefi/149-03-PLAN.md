---
phase: 149-firmware-bios-domain-overview-dfci-surface-uefi
plan: 03
type: execute
wave: 2
depends_on: ["149-01"]
files_modified:
  - docs/operations/firmware-bios/01-windows-dfci.md
autonomous: false
requirements: [BIOS-02, BIOS-03, BIOS-04]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "01-windows-dfci.md states DFCI's disqualifying prerequisite verbatim from configure-dfci-windows — that DFCI cannot be used with devices manually registered for Windows Autopilot, and that DFCI management requires external attestation of commercial acquisition through an OEM or a Microsoft CSP partner registration. [ROADMAP SC#4, BIOS-04, D-05]"
    - "All FOUR registration channels this corpus teaches are classified against that gate: CSV upload disqualifies, OEM direct registration qualifies, Get-WindowsAutopilotInfo (including -Online) disqualifies, and Partner Center / CSP partner qualifies. [SC#4, BIOS-04, D-05; edge BIOS-04/unclassified]"
    - "01-windows-dfci.md states that an APv2 fleet carries no Autopilot registration at all and therefore no DFCI, attributing that conclusion to this corpus's own APv2 documents rather than to Microsoft. [SC#4, D-05]"
    - "The 24H2 Professional known issue and its workaround (KB5046740 or later, or an Enterprise upgrade before OOBE completes) are carried, quoted from autopilot/dfci-management with its own **Source:** line. [SC#4, BIOS-04]"
    - "01-windows-dfci.md names the nine DFCI OEMs in the source's own order — Acer, Asus, Dynabook, Fujitsu, Microsoft Surface, Panasonic, VAIO, Samsung, NEC — immediately followed by the source's own sentence 'Other OEMs are pending.' under one **Source:** line for autopilot/dfci-management. [ROADMAP SC#3, BIOS-03, D-48; edge BIOS-03/ordering]"
    - "The six-OEM variant on configure-bios-windows and the one-OEM Project Mu variant are each recorded as a documented conflict with the nine-OEM list, each under its OWN **Source:** line, and the six-OEM list is stated as a strict subset of the nine on a page two years staler. [SC#3, BIOS-03, D-47, D-53, D-56; edge BIOS-03/adjacency]"
    - "Dell, HP and Lenovo are stated to appear on none of the three lists, and DFCI is presented as UNAVAILABLE on those fleets rather than as an option declined. [SC#3, BIOS-03; edge BIOS-03/empty]"
    - "01-windows-dfci.md carries a bounded Surface section: the prerequisite floor, the self-registered-devices rule, the disapplied-settings Note quoted verbatim including its leading Disable, and one sentence routing model eligibility to the vendor page. [D-32]"
    - "The Surface section states its own bound on the record — no eligible-model table and no model-gated-settings table ship, because the requirements document bars per-model matrices. [D-32, D-40's precedent for recording a stated bound]"
    - "node scripts/validation/check-nav-hub-links.mjs reports 0 corpus-link failures after every commit in this plan. [D-57]"
  artifacts:
    - docs/operations/firmware-bios/01-windows-dfci.md
  key_links:
    - "01-windows-dfci.md links to ../../lifecycle/01-hardware-hash.md — the corpus source for the four registration channels (D-05)"
    - "01-windows-dfci.md links to ../../admin-setup-apv1/01-hardware-hash-upload.md at FILE level — the prescribed 'if you registered via Path 2 or Path 3, DFCI is not available to you' cross-link (D-44); the inbound direction is Phase 152's"
    - "01-windows-dfci.md links back to 00-overview.md (created in plan 01, so no dangle)"
  prohibitions:
    - "The barred OEM phrasings named in 149-CONTEXT.md D-49 appear nowhere in this file. The hyphenated form six-OEM is the requirement's own wording and is permitted."
    - "No eligible-model table and no model-gated-settings table for Surface ships — the requirements document's Out of Scope section bars per-model matrices literally. [D-32]"
    - "No string ships inside quotation marks that was not captured from a live page — the research ledger's unquoted prose is never rendered as first-party verbatim. [D-37, D-52]"
    - "The string UEFI CSP is never attributed to configure-dfci-windows; that page does not contain it. [D-53, 149-RESEARCH.md Pitfall 4]"
    - "The phrase Enrollment Status Page is used as the product name but never presented as a verbatim quotation of that page's step heading, which uses a different word. [D-53, 149-RESEARCH.md Pitfall 3]"
    - "No single **Source:** line covers claims drawn from two different pages. [D-56]"
    - "No outbound link to any docs/operations/firmware-bios/02-, 03- or 04- file. [D-26]"
    - "No cross-file link carries a heading anchor. [D-27]"
    - "docs/admin-setup-apv1/01-hardware-hash-upload.md, docs/lifecycle/01-hardware-hash.md and docs/admin-setup-apv2/** are cross-linked or read, never edited. [D-44]"
    - "The British spelling forms named in 149-CONTEXT.md D-59 appear nowhere in this file."
    - "The banned-phrase forms named in 149-CONTEXT.md D-58 appear nowhere in this file."
    - "No fenced code block and no version-history section is added. [D-31]"
---

<objective>
Author the DFCI guide's mechanics half: who can use DFCI, which OEMs it reaches, and how far the
Surface treatment goes.

Purpose: D-25 rules that BIOS-03 lives WHOLLY in `01-windows-dfci.md` and that BIOS-04's three-part
gate does too — the overview gets a pointer and nothing more, so a load-bearing first-party quote is
never duplicated across two files. This plan lands ROADMAP Success Criteria 3 and 4.

Output: three new anchored H2s in `docs/operations/firmware-bios/01-windows-dfci.md` —
`## Prerequisites and Disqualifiers`, `## OEM Support` and `## Surface Eligibility`.

Decisions this plan implements or records: D-05 (four registration channels plus the APv2 statement),
D-09 (BIOS-03's own text names Microsoft Surface, so "no BIOS clause mentions Surface" is false — only
the Success-Criterion half is true, and the surviving ground for bounding Surface is the per-model
matrices bar), D-10 (no URL or table this file writes can flip validators 8, 11 or 29 — they are
scoped to other files; the real one-physical-line rule is 147 D-34's), D-12 (anchor ids are
hand-authored short forms, not heading slugs), D-13 (scope the oldest-source superlative to the
**Pillar-A** set and never to the milestone's whole source list — a 2024-05-16 counter-source exists,
and the wider phrasing is negative-grepped by this plan), D-17 (the driver/firmware policy anchor
lives in `01-windows-wufb-rings.md`, not in `06`), D-24 (the H2 skeleton for this file), D-25 (BIOS-03
lives wholly here), D-27 (relative paths; never a bare number), D-32 (Surface is BOUNDED and the bound
is stated on the record; dispose of the two live research instructions by name), D-46 (the bounded
fetch list of seven, every item graded), D-47 (the six-OEM list is BLOCKING for SC#3 — there is no
drop-the-count escape hatch), D-48 (the nine names verbatim), D-51 (both dates beside every quote),
D-53 (pin every quote by its surrounding heading — five decoys), D-56 (the evidence-line contract and
its one-page scope rule), D-58 (banned-phrase discipline), D-59 (American spelling), D-60 (every prose
negative carries a runnable grep), D-70 (read four lines up before calling a citation wrong — the
Referee overturned two agents who reasoned from a decision body without reading its heading).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-PATTERNS.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-01-SUMMARY.md
@docs/operations/patch-management/06-windows-driver-firmware-updates.md
</context>

## Artifacts this phase produces

New symbols created by Phase 149 (excluded from drift verification — none exists at HEAD `d9f3c4c5`):

**Files:** `docs/operations/firmware-bios/00-overview.md` (plan 01),
`docs/operations/firmware-bios/01-windows-dfci.md` (plan 01 seeds it; plans 03/04/05 expand it).

**Hand-authored anchor ids added by THIS plan:** `dfci-prerequisites`, `dfci-oem-support`,
`surface-eligibility`.

**Other new anchor ids in the phase:** `who-holds-the-secret`, `native-bios-surfaces`,
`choosing-a-path`, `domain-boundary`, `updates-vs-configuration`, `before-you-start`,
`unsupported-callouts` (in `00-overview.md`); `what-dfci-is`, `dfci-settings-surface`,
`bricking-irreversible`, `retiring-a-device`, `reusing-a-device`, `recovering-locked-device`,
`unsupported-callouts` (in `01-windows-dfci.md`).

**New glossary terms** (plan 02): `### Device Firmware Configuration Interface (DFCI)`, `### UEFI CSP`,
`### BIOS password`, `### BIOS configuration and other settings`.

**New nav/hub entries:** none — Phase 152 owns all registry, filename-map, canary, ops-index and
`docs/index.md` wiring (D-66).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: the DFCI eligibility gate this file ships is one-way in the field</name>
  <reversibility rating="one-way">A service-desk reader who acts on a wrong eligibility gate specifies or buys hardware that cannot be managed. The CSP-purchase case is common, and the purchase is not undone by a later documentation fix. 149-CONTEXT.md D-05 rates this one-way once shipped.</reversibility>
  <decision>How many Autopilot registration channels does this file classify against DFCI's eligibility gate, and which of them qualify?</decision>
  <options>
    <option id="four-channel">
      <name>Document all four channels, with the gate written as the source states it (D-05's owner ruling)</name>
      <pros>Matches the first-party sentence, which names an OEM or a Microsoft CSP partner registration as what confers the external attestation DFCI requires; matches this corpus's own Import Methods table, which carries four rows; carries the explicit statement that an APv2 fleet has no registration and therefore no DFCI; and rests on the REQUIREMENTS.md amendment plan 02 already filed</pros>
      <cons>Diverges in wording from ROADMAP Success Criterion 4, which says three, so the verifier must grade SC#4 through the amendment rather than literally</cons>
    </option>
    <option id="three-channel">
      <name>Ship the literal three-path reading</name>
      <pros>Matches Success Criterion 4 word for word with no amendment in the loop</pros>
      <cons>Tells a reader who bought through a CSP partner that DFCI is unavailable to them when the source says it is available; the failure mode is hardware bought that cannot be managed, and the CSP-purchase case is common</cons>
    </option>
  </options>
  <recommendation>four-channel. It is already the owner's ruling of 2026-08-24, and the alternative ships a known-false eligibility gate on the one axis whose failure mode is unrecoverable hardware spend.</recommendation>
  <resume-signal>Select: four-channel or three-channel</resume-signal>
  <action>
Present the decision above to the developer and wait for a ruling before task 1 writes any prose.
Do not proceed on the recommendation alone — D-05 rates this one-way once shipped.

Record the ruling verbatim in the SUMMARY, then proceed to task 1. If the ruling is `three-channel`,
stop and report: plan 02's `.planning/REQUIREMENTS.md` amendment and this plan's must-haves both
assume the four-channel reading and would need revision.
  </action>
  <verify>
    <automated>test -f .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-02-SUMMARY.md &amp;&amp; grep -c "AMENDED" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The developer's ruling is recorded verbatim in the SUMMARY before any prose is written
    - `.planning/REQUIREMENTS.md` already carries plan 02's amendment note (the ledger record exists before the content that depends on it)
  </acceptance_criteria>
  <done>The developer has ruled on the four-channel eligibility gate and the ruling is recorded.</done>
</task>

<task type="auto">
  <name>Task 1: Author the prerequisites and disqualifiers section</name>
  <files>docs/operations/firmware-bios/01-windows-dfci.md</files>
  <read_first>
    - docs/operations/firmware-bios/01-windows-dfci.md — the current state after plan 01.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources items (a) and (b). Items (a) and (b) are the ONLY permitted origin for the quoted strings in this task.
    - docs/lifecycle/01-hardware-hash.md lines 53-64 — the `### Import Methods` table with its four rows. Read it; do not edit it.
    - docs/admin-setup-apv1/01-hardware-hash-upload.md — the three Path H2s at `## Path 1: OEM Delivery`, `## Path 2: CSV Bulk Import` and `## Path 3: PowerShell Script (Get-WindowsAutopilotInfo)`. Read it; do not edit it. This file is past due on its review date and sits in the parked past-due population.
    - docs/admin-setup-apv2/00-overview.md lines 29-33 and docs/admin-setup-apv2/04-corporate-identifiers.md line 51 — the APv2 no-registration case. Read them; do not edit them.
    - docs/operations/patch-management/06-windows-driver-firmware-updates.md lines 37-60 — the anchor/H2/claim/`**Source:**`-line rhythm to copy.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-05, D-27, D-44, D-51, D-53, D-56.
  </read_first>
  <action>
Add the anchored H2 `## Prerequisites and Disqualifiers` (anchor id `dfci-prerequisites`, on its own
line immediately above the heading) directly after `## What DFCI Is`.

Open with the three-part gate, stated as prose: the device must be registered with Windows Autopilot;
the registration must confer external attestation of commercial acquisition; and the OS floor applies.
Then ship the eligibility sentence verbatim from `configure-dfci-windows` as one contiguous
blockquote — `You can't use DFCI with devices manually registered for Windows Autopilot, such as
imported from a csv file. By design, DFCI management requires external attestation of the device's
commercial acquisition through an OEM or a Microsoft CSP partner registration to Windows Autopilot.` —
under one standalone `**Source:**` line for that page in the two-date form
`(ms.date 2026-06-23, updated 2026-07-01)`, using the canonical
`intune/device-configuration/templates/configure-dfci-windows` URL path.

Then classify all four registration channels this corpus teaches, as a table with a qualifies/disqualifies
column and a reason column:

| Channel | Qualifies for DFCI | Why |
| CSV upload | No | manual registration; the source names a csv import as the disqualifying example |
| OEM direct registration | Yes | the OEM confers the external attestation of commercial acquisition |
| PowerShell `Get-WindowsAutopilotInfo`, including `-Online` | No | manual registration |
| Partner Center / CSP partner | Yes | the source's gate names a Microsoft CSP partner registration alongside an OEM one |

Attribute the four-channel enumeration to this corpus — link `../../lifecycle/01-hardware-hash.md`,
whose `### Import Methods` table is where the fourth row lives — and attribute the *qualifying* half to
the first-party gate already quoted above. Keep those two attributions in separate sentences; a single
`**Source:**` line may never span a corpus file and a Learn page (D-56). Keep every table row on one
physical line (147 D-34).

Add the prescribed cross-link back to `../../admin-setup-apv1/01-hardware-hash-upload.md` at FILE
level: a reader who registered through that document's CSV or PowerShell paths does not have DFCI
available. Do not use a heading anchor and do not edit that file — the inbound direction is handed to
Phase 152 by name (D-44).

Then add the APv2 statement as its own short paragraph: an APv2 fleet carries no Autopilot
registration at all — `docs/admin-setup-apv2/04-corporate-identifiers.md` registers devices with a
manufacturer/model/serial CSV, and `docs/admin-setup-apv2/00-overview.md` states that any existing
APv1 registration must be removed for APv2 to work — so there is no external attestation and therefore
no DFCI, ever, on those devices. Attribute this conclusion to this corpus's own documents, in the
corpus's own voice, never to Microsoft: 145 D-08 is the rule that a derived claim is attributed to the
corpus, not to the vendor. Do not edit either APv2 file.

Then carry the 24H2 Professional known issue and its workaround, quoted verbatim from
`autopilot/dfci-management` under its own `**Source:**` line in the two-date form
`(ms.date 2025-03-25, updated 2026-04-14)`. Ship both halves — the failure condition and the
workaround (KB5046740 or later, or an Enterprise-edition upgrade before OOBE completes).

Where the enrollment experience is named, write the product name in the standard corpus form and do
not present it as a verbatim quotation of the source page's own step heading — that heading uses a
different word than the product name does, and 149-RESEARCH.md Pitfall 3 records the trap.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^<a id="dfci-prerequisites"></a>' docs/operations/firmware-bios/01-windows-dfci.md` returns 1, and the line immediately below it is `## Prerequisites and Disqualifiers`
    - the file contains the literal `You can't use DFCI with devices manually registered for Windows Autopilot, such as imported from a csv file.`
    - the file contains the literal `through an OEM or a Microsoft CSP partner registration to Windows Autopilot.`
    - the file contains all four channel names: `grep -c 'Partner Center' …` returns at least 1, and the same for `OEM direct registration`, `Get-WindowsAutopilotInfo` and `CSV upload`
    - the classification table has exactly four data rows and each is a single physical line
    - the file contains the literal `KB5046740`
    - `grep -c 'admin-setup-apv2' docs/operations/firmware-bios/01-windows-dfci.md` returns at least 1, and the surrounding sentence states that an APv2 fleet has no Autopilot registration and therefore no DFCI
    - `grep -c '\.\./\.\./lifecycle/01-hardware-hash\.md' docs/operations/firmware-bios/01-windows-dfci.md` returns at least 1
    - `grep -c '\.\./\.\./admin-setup-apv1/01-hardware-hash-upload\.md' docs/operations/firmware-bios/01-windows-dfci.md` returns at least 1, with no `#` fragment on that link
    - `git diff --name-only HEAD~1 HEAD` lists only `docs/operations/firmware-bios/01-windows-dfci.md`
    - every `**Source:**` line added by this task matches `(ms.date [0-9-]*, updated [0-9-]*)` and cites exactly one URL
    - `grep -c 'Enrollment State Page' docs/operations/firmware-bios/01-windows-dfci.md` returns 0
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0 corpus-link failure(s)`
  </acceptance_criteria>
  <done>`## Prerequisites and Disqualifiers` carries the verbatim gate, all four classified channels, the APv2 statement and the 24H2 known issue with its workaround.</done>
</task>

<task type="auto">
  <name>Task 2: Author the OEM support section and record the three-list conflict</name>
  <files>docs/operations/firmware-bios/01-windows-dfci.md</files>
  <read_first>
    - docs/operations/firmware-bios/01-windows-dfci.md — the current state after task 1.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources items (a) and (c), and Pitfall 1, which describes the six-OEM decoy and why a DFCI grep on the wrong page returns exactly the conclusion BIOS-03 bars.
    - .planning/research/STACK.md lines 99-111 — the three-list analysis, including the Project Mu URL and the correction to the framing that Learn calls Project Mu authoritative. Read for coordinates only; nothing here may be quoted (D-37).
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-46, D-47, D-48, D-49, D-51, D-53, D-56.
  </read_first>
  <action>
Add the anchored H2 `## OEM Support` (anchor id `dfci-oem-support`).

**Fetch step first.** 149-RESEARCH.md re-fetched seven sources but did NOT re-fetch the Project Mu
page, which carries the one-OEM variant. D-46 requires it and D-52 bars shipping a quoted string that
was not read from a live page. Before writing, fetch
`https://microsoft.github.io/mu/dyn/mu_feature_dfci/DfciPkg/Docs/Scenarios/DfciScenarios/` as raw bytes
with `curl -A "Mozilla/5.0 ..."` — D-50 records that the browser User-Agent is what turned a 403 into
a 200 on this milestone's other vendor property. Capture the OEM list and note that the page carries
no date at all. If the fetch fails: ship the one-OEM variant as an unquoted characterization with a
`**Source:**` line carrying only the URL and an explicit statement that the page carries no date, and
record the fetch failure in the SUMMARY. Do not drop the variant — SC#3 requires it.

Write the canonical list first: the nine OEMs in the source's own order, verbatim — **Acer, Asus,
Dynabook, Fujitsu, Microsoft Surface, Panasonic, VAIO, Samsung, NEC** — immediately followed by the
source's own closing sentence `Other OEMs are pending.`, quoted, so the list reads as open rather than
exhaustive. One standalone `**Source:**` line for `autopilot/dfci-management` in the two-date form
`(ms.date 2025-03-25, updated 2026-04-14)` covers both, because both come from that one page. Do not
re-sort the names; the order is part of the quotation.

Then record the conflict as a conflict, with THREE separate evidence lines because the three lists
live on three pages (D-56):
- The **six-OEM** variant on `configure-bios-windows`'s DFCI comparison column — `Surface, Acer, Asus,
  Dynabook, Fujitsu, Panasonic` — quoted, under its own `**Source:**` line in the two-date form
  `(ms.date 2024-06-06, updated 2026-07-01)`. State that it is a strict subset of the nine, and that
  its page's `ms.date` is two years older than the canonical list's. This is the oldest source in the
  **Pillar-A** set — scope the superlative to that set explicitly and never to the milestone's whole
  source list, which this plan's acceptance criteria negative-grep for, because a 2024-05-16 source exists
  elsewhere in this milestone's research (D-13).
- The **one-OEM** Project Mu variant, from the fetch step above, under its own `**Source:**` line
  recording that the page carries no date.
- A plain statement that Microsoft does not designate any of the three as authoritative for OEM
  support; the pages cross-reference each other as see-alsos.

Then state the consequence BIOS-03 requires: Dell, HP and Lenovo appear on **none** of the three
lists, so DFCI is **unavailable** on those fleets — not an option an administrator declined, and not a
capability that can be turned on. Route the reader to `00-overview.md` for what those fleets use
instead.

The two barred phrasings named in 149-CONTEXT.md D-49 must not appear. The hyphenated adjective form
`six-OEM` is the requirement's own wording and is fine; the definite-article form is not. There is no
drop-the-count escape hatch: D-47 makes the six-OEM count blocking for SC#3.

Per 149-RESEARCH.md Pitfall 1, cite each page by name for the list it actually carries. A DFCI grep on
`configure-bios-windows` surfaces its comparison table, which is a side-reference and not DFCI's own
enumeration.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^<a id="dfci-oem-support"></a>' docs/operations/firmware-bios/01-windows-dfci.md` returns 1, and the line immediately below it is `## OEM Support`
    - all nine names appear: `for n in Acer Asus Dynabook Fujitsu Panasonic VAIO Samsung NEC "Microsoft Surface"; do grep -c "$n" docs/operations/firmware-bios/01-windows-dfci.md; done` returns a non-zero count for every name
    - the nine names appear in the source's order (Acer before Asus before Dynabook before Fujitsu before Microsoft Surface before Panasonic before VAIO before Samsung before NEC) in their first occurrence block
    - the file contains the literal `Other OEMs are pending.`
    - the file contains the literal `Surface, Acer, Asus, Dynabook, Fujitsu, Panasonic`
    - `grep -c 'microsoft.github.io' docs/operations/firmware-bios/01-windows-dfci.md` returns at least 1
    - `grep -c 'ms.date 2024-06-06' docs/operations/firmware-bios/01-windows-dfci.md` returns at least 1
    - `grep -niE "the six($|[^-])|most business OEMs" docs/operations/firmware-bios/01-windows-dfci.md` returns nothing
    - `grep -niE "in the whole set" docs/operations/firmware-bios/01-windows-dfci.md` returns nothing
    - `grep -niE "unavailable" docs/operations/firmware-bios/01-windows-dfci.md` shows a sentence naming Dell, HP and Lenovo, and no sentence in the section describes DFCI on those fleets as declined, disabled or not chosen
    - three distinct `**Source:**` lines exist in this section, each citing exactly one URL
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0 corpus-link failure(s)`
    - the SUMMARY records whether the Project Mu fetch succeeded, and its outcome
  </acceptance_criteria>
  <done>The nine-OEM list, the pending sentence, both conflicting variants with their own evidence lines, and the unavailable-not-declined statement all ship, with the barred phrasings absent.</done>
</task>

<task type="auto">
  <name>Task 3: Author the bounded Surface eligibility section</name>
  <files>docs/operations/firmware-bios/01-windows-dfci.md</files>
  <read_first>
    - docs/operations/firmware-bios/01-windows-dfci.md — the current state after task 2.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources item (e), and Pitfall 5, which records the display-name divergence between the Surface page and the settings reference.
    - .planning/REQUIREMENTS.md — the `## Out of Scope` section, whose per-model-matrices bar is the ground for this section's bound, including its trailing sentence scoping it to not rewriting vendor manuals.
    - .planning/research/STACK.md lines 113-125 — the two live research instructions this section declines by name. Read for coordinates only; nothing here may be quoted.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-09, D-32, D-40.
  </read_first>
  <action>
Add the anchored H2 `## Surface Eligibility` (anchor id `surface-eligibility`) after `## OEM Support`.

Ship exactly four things, all from `surface-manage-dfci-guide`:
1. The prerequisite floor, quoted verbatim: `Windows 11 or Windows 10 version 1809 or later`;
   Autopilot-registered; and `Unless otherwise specified, listed devices are commercial SKUs only.`
2. The self-registration rule, quoted verbatim: `DFCI won't be applied to self-registered devices.`
   A self-registered Surface is manageable with Intune but never gets DFCI.
3. The disapplied-settings Note, quoted verbatim **including its leading `Disable`**: `DFCI in Intune
   includes settings that don't currently apply to Surface devices: CPU and IO virtualization, Disable
   Boot from network adapters, Windows Platform Binary Table (WPBT), NFC, and SD card.` Then state, in
   the corpus's own voice, which page's display-name convention that quotation uses — the Intune
   settings-reference page names the same underlying setting without the `Disable` prefix, so the two
   first-party pages genuinely differ and this document quotes the Surface page.
4. One sentence that model eligibility carries per-model and per-processor exclusions, routing the
   reader to the vendor page for the current list.

Items 1 through 3 all come from one page, so one contiguous blockquote per claim under one
`**Source:**` line per claim is correct, in the two-date form `(ms.date 2026-07-14, updated 2026-07-14)`.

Then state the bound on the record, in prose, exactly as D-40 requires a deliberate scope choice to be
stated: this section ships no eligible-model table and no model-gated-settings table, because the
requirements document's Out of Scope section bars per-model matrices literally, and the current model
list is high-churn data better reached at the vendor page than copied. Naming this bound in the shipped
document is what stops a verifier reading the phase title's Surface mention as a gap.

Record in the SUMMARY that the two live research instructions at `.planning/research/STACK.md:113` and
`:120` — the enumerable-model-list framing and the model-gated-settings table proposal — are declined
here on that ground, so a later planner does not re-derive them as open work (D-32).

Also record in the SUMMARY the correction D-09 carries: BIOS-03's own requirement text names Microsoft
Surface verbatim, so any claim that no BIOS requirement clause mentions Surface is false; only the
Success-Criterion half of that claim is true.

Do not carry the Surface removal path here. It ships in plan 05, adjacent to the default retire order,
behind its own one-way decision checkpoint, because its own step order performs the destructive
operation first.
  </action>
  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs; node scripts/validation/v1.20-milestone-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^<a id="surface-eligibility"></a>' docs/operations/firmware-bios/01-windows-dfci.md` returns 1, and the line immediately below it is `## Surface Eligibility`
    - the file contains the literal `Windows 11 or Windows 10 version 1809 or later`
    - the file contains the literal `Unless otherwise specified, listed devices are commercial SKUs only.`
    - the file contains the literal `DFCI won't be applied to self-registered devices.`
    - the file contains the literal `Disable Boot from network adapters` (the leading word is present, per 149-RESEARCH.md Pitfall 5)
    - the file contains the literal `Windows Platform Binary Table (WPBT), NFC, and SD card.`
    - a sentence in this section names which page's display-name convention the quotation follows
    - the section contains no markdown table listing Surface device models (`awk '/^## Surface Eligibility$/,/^## /' docs/operations/firmware-bios/01-windows-dfci.md | grep -c '^| '` returns 0)
    - the section states its own bound in prose and names the requirements document's per-model-matrices bar as the ground
    - `awk '/^## Surface Eligibility$/,/^## /' docs/operations/firmware-bios/01-windows-dfci.md | grep -ci 'Refresh from Network'` returns 0 (the removal path is plan 05's)
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 with `0 corpus-link failure(s)`
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0 with `16 passed, 0 failed`
    - the SUMMARY records the declined research instructions and the D-09 correction
  </acceptance_criteria>
  <done>Surface ships bounded — floor, self-registration rule, disapplied-settings Note with its leading word, and one routing sentence — with the bound and the declined research instructions both on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream Microsoft documentation → this corpus | Three OEM lists and a prerequisite gate are quoted across four pages |
| This corpus → hardware purchasing and service-desk decisions | An eligibility statement drives what hardware a reader specifies or buys |

This plan authors static Markdown. There is no application code, no authentication surface, no user
input handling and no data storage — **there is no ASVS/STRIDE attack surface**, the conclusion
149-RESEARCH.md's Security Domain section reaches, stated here rather than omitted. ASVS level 1,
block on `high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-149-13 | Spoofing | The three OEM lists | high | mitigate | Each list carries its own `**Source:**` line naming the page it came from; the six-OEM decoy is quoted only from the page that carries it; the Project Mu variant is re-fetched at execution time or shipped unquoted with its fetch failure recorded (D-46, D-52, D-53) |
| T-149-14 | Tampering | The DFCI eligibility gate | high | mitigate | The checkpoint task above forces an explicit ruling before any prose is written; the gate ships as the first-party sentence states it, quoted, with all four channels classified against it |
| T-149-15 | Repudiation | Claim provenance across four pages | medium | mitigate | Two-date `**Source:**` lines, one page per line, never spanning a corpus file and a vendor page |
| T-149-16 | Information disclosure | — | low | accept | No secret or tenant-specific value is authored |
| T-149-17 | Denial of service | — | low | accept | Static Markdown; no runtime |
| T-149-18 | Elevation of privilege | — | low | accept | No permission model is implemented |
| T-149-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs — 149-RESEARCH.md's Package Legitimacy Audit records "Not applicable" |

T-149-13 and T-149-14 are rated `high` and the configured block threshold is `high`; both are disposed
`mitigate`, and the mitigations are enforced by the checkpoint task and by named acceptance criteria in
tasks 1 and 2 rather than by a validator, because no validator in this repository parses evidence lines.
</threat_model>

<verification>
- `node scripts/validation/check-nav-hub-links.mjs` — 0 corpus-link failures after every commit
- `node scripts/validation/v1.20-milestone-audit.mjs` — 16 passed, 0 failed
- `grep -niE "the six($|[^-])|most business OEMs" docs/operations/firmware-bios/` returns nothing
- Every `**Source:**` line added carries both dates and exactly one URL
- The Project Mu fetch outcome is recorded, success or failure
</verification>

<success_criteria>
- ROADMAP SC#3 is satisfied: nine OEMs in source order, the pending sentence, both variants recorded as a documented conflict, DFCI presented as unavailable on Dell, HP and Lenovo, and the barred phrasings absent
- ROADMAP SC#4 is satisfied: the disqualifying prerequisite quoted first-party, all four registration channels classified, the APv2 case stated, and the 24H2 Professional known issue carried with its workaround
- Surface ships bounded with its bound stated on the record
- `docs/operations/firmware-bios/01-windows-dfci.md` stands at four anchored H2s plus its two tail sections
</success_criteria>

<output>
Create `.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-03-SUMMARY.md` when done
</output>
