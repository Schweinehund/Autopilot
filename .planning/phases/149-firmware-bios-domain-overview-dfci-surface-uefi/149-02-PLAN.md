---
phase: 149-firmware-bios-domain-overview-dfci-surface-uefi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_glossary.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [BIOS-02, BIOS-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/_glossary.md defines four new terms as `### ` H3 entries under one existing topical H2: Device Firmware Configuration Interface (DFCI), UEFI CSP, BIOS password, and BIOS configuration and other settings. [D-06, D-61]"
    - "Each new term has a matching [Term](#anchor) entry inserted in alphabetical position in the single-line `## Alphabetical Index` at docs/_glossary.md:31. [D-63, Phase-75 precedent at :285]"
    - "docs/_glossary.md gains exactly one new `## Version History` row naming Phase 149, the terms added, the section they were added to, and that the Alphabetical Index was updated. [D-63]"
    - "docs/_glossary.md front matter is unchanged — last_verified 2026-06-29 and review_by 2026-09-27 stay as they are, because this file's cycle is 90 days and not the milestone's 60. [D-63]"
    - "Every new glossary anchor is a plain GitHub slug derived from its heading with no {#id} override, and no new heading contains an ampersand or a spaced slash, either of which produces a double hyphen. [D-11, D-62]"
    - ".planning/REQUIREMENTS.md records on the record that BIOS-04's and ROADMAP SC#4's `three registration paths` count was measured against one corpus file only, names the fourth qualifying channel (Partner Center / CSP partner) with its corpus coordinate, and names the APv2 no-registration case. [ROADMAP SC#4, BIOS-04, D-05]"
    - "The amendment states the eligibility gate as the first-party source states it — an OEM or a Microsoft CSP partner registration qualifies; both manual paths disqualify. [BIOS-04, D-05]"
    - "node scripts/validation/check-phase-49.mjs exits 0 after the glossary edit (its H3-collision audit loads this file's term set). [D-63]"
    - "node scripts/validation/c17-eee-contract.mjs exits 0 with zero violations — docs/_glossary.md is doc_id RE-184 and is enrolled, so the new entry inherits all 13 assertions including the 200-character cap on every top-level blockquote. [D-63]"
  artifacts:
    - docs/_glossary.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "The new DFCI glossary entry carries a reciprocal `> See also:` link to the existing [Secure Boot](#secure-boot) entry, reconciling the two per D-61; docs/operations/firmware-bios/00-overview.md links inbound to that same #secure-boot anchor"
    - "No new glossary entry links out to docs/operations/firmware-bios/ — those files are created by a sibling plan in the same wave and an outbound link would dangle in one commit ordering"
  prohibitions:
    - "No new glossary entry links to any file under docs/operations/firmware-bios/. [D-28's no-dangling-commit discipline]"
    - "No {#id} anchor override appears on any new glossary heading. [D-62]"
    - "No new `### ` heading contains an ampersand or a spaced slash. [D-11, D-62]"
    - "The docs/_glossary.md front-matter dates are not changed. [D-63]"
    - "Phase 150's vendor terms — the HP and Lenovo BIOS tool names handed forward by 149-CONTEXT.md D-64 — are not added here."
    - "No existing glossary term body is rewritten; the change is additive plus one reciprocal see-also line."
    - "Every new top-level blockquote in docs/_glossary.md is at most 200 characters. [C17 assertion #12]"
    - ".planning/REQUIREMENTS.md gains no `05-compliance-policy.md` literal — check-phase-54 validator 21 live-reads this file with a negative literal assertion."
    - "The British spelling forms named in 149-CONTEXT.md D-59 appear in no line this plan writes."
---

<objective>
Discharge the roadmap-locked glossary decision and file the SC#4 premise amendment.

Purpose: `STATE.md:327` names "which glossary receives the new terminology" as a **Phase-149**
decision under "Named decisions (LOCKED at roadmap 2026-08-19)", and `PROJECT.md:31` names DFCI first
among the terms this milestone adds. Phases 147 and 148 both deferred the identical flag; a third
deferral would leave a roadmap-locked decision and a milestone scope statement stale (D-06). Separately,
BIOS-04 and ROADMAP SC#4 both assert this corpus teaches *three* registration paths — a count two
corpus files falsify — and D-05 requires that premise be amended on the record rather than shipped
around.

Output: four new terms in `docs/_glossary.md` with their index rows and a version-history row, and an
amendment note in `.planning/REQUIREMENTS.md`.

This plan runs in wave 1 alongside plan 01 and shares no file with it.

Decisions this plan implements or records: D-05 (four registration channels, the APv2 statement, and
the `REQUIREMENTS.md` amendment), D-06 (the owner ruling that `docs/_glossary.md` receives the DFCI
terminology in this phase), D-15 (`FEATURES.md:303` opens "New glossary terminology is confirmed
needed." — the draft quoted only that line's closing warning), D-59 (American spelling), D-60 (every
prose negative carries a runnable grep), D-61 (the four terms, and reconcile against the two existing
entries), D-62 (plain GitHub slugs, no id overrides), D-63 (the three mechanical obligations, and the
90-day cycle that must not be "corrected"), D-64 (Phase 150's vendor terms are NOT added here), D-68
(accepted inconsistencies — this plan is what makes `ROADMAP.md:185`'s blast-radius claim inaccurate),
D-71 (all three of the draft's grounds for deferring the glossary were defective — recorded so no
later phase re-imports them), D-72 (citations use full relative paths).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md
@.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-PATTERNS.md
@docs/_glossary.md
</context>

## Artifacts this phase produces

New symbols created by Phase 149 (excluded from drift verification — none exists at HEAD `d9f3c4c5`):

**Files:** `docs/operations/firmware-bios/00-overview.md`, `docs/operations/firmware-bios/01-windows-dfci.md`
(both authored by plans 01/03/04/05, not this plan).

**New glossary terms authored by THIS plan**, all four confirmed at 0 corpus occurrences at HEAD:
- `### Device Firmware Configuration Interface (DFCI)` → slug `device-firmware-configuration-interface-dfci`
- `### UEFI CSP` → slug `uefi-csp`
- `### BIOS password` → slug `bios-password`
- `### BIOS configuration and other settings` → slug `bios-configuration-and-other-settings`

**New hand-authored anchor ids** (authored by plans 01/03/04/05): `who-holds-the-secret`,
`native-bios-surfaces`, `choosing-a-path`, `domain-boundary`, `updates-vs-configuration`,
`before-you-start`, `unsupported-callouts`, `what-dfci-is`, `dfci-prerequisites`, `dfci-oem-support`,
`surface-eligibility`, `dfci-settings-surface`, `bricking-irreversible`, `retiring-a-device`,
`reusing-a-device`, `recovering-locked-device`.

**New nav/hub entries:** none. Registry rows, filename-map rows, canary bumps, ops-index rows and
`docs/index.md` wiring are Phase 152's single atomic commit (D-66).

<tasks>

<task type="auto">
  <name>Task 1: Add the four firmware terms to the glossary</name>
  <files>docs/_glossary.md</files>
  <read_first>
    - docs/_glossary.md — lines 1-12 (front matter: `doc_id: RE-184`, `status: Approved`, `last_verified: 2026-06-29`, `review_by: 2026-09-27`), line 31 (the single-line Alphabetical Index), lines 109-160 (the `## Hardware` section, including `### TPM` at :137 and `### Firmware TPM (fTPM)` at :153), lines 189-205 (the `## Security` section and the existing `### Secure Boot` entry at :201-203), lines 279-290 (the `## Version History` table and the Phase-75 precedent row at :285).
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-PATTERNS.md — the `docs/_glossary.md` Pattern Assignments block, which gives the term-entry shape, the index-row shape and the version-history row shape verbatim.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources items (b), (c) and (d), for the mechanism facts each definition rests on.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-06, D-61, D-62, D-63, D-64.
  </read_first>
  <action>
Add four `### ` term entries to `docs/_glossary.md`, all under the existing `## Hardware` H2 (line
109), which already holds `### TPM` and `### Firmware TPM (fTPM)`. The alternative home
`## Security` is left to hold `### Secure Boot`; D-61 leaves this choice to discretion, and `## Hardware`
is chosen because all four terms name firmware surfaces and configuration objects rather than security
controls, and because a single insertion point keeps the diff minimal. Record the choice and its ground
in the SUMMARY. Note that H3s inside `## Hardware` are not alphabetically ordered today, so the four
entries may be grouped together.

Headings, exactly (D-62 — plain GitHub slugs, no `{#id}` override; never a spaced slash and never an
ampersand, both of which produce a double hyphen in the slug):
- `### BIOS configuration and other settings`
- `### BIOS password`
- `### Device Firmware Configuration Interface (DFCI)`
- `### UEFI CSP`

Bodies follow the existing `### Secure Boot` shape exactly — a plain H3 followed by one or two
sentences of prose, no template macro, no fenced block:
- **BIOS configuration and other settings** — the Intune Templates profile type that pushes a vendor
  BIOS configuration file to a device. Name the file type `.cctk`, the 2 MB size limit, and that it is
  supported on one vendor (Dell) today. State that it is disjoint from DFCI.
- **BIOS password** — the firmware setup or supervisor password that gates BIOS changes. State that
  *who holds it* is the discriminator the firmware and BIOS governance domain routes on.
- **Device Firmware Configuration Interface (DFCI)** — the Windows firmware management layer Intune
  reaches through a configuration service provider. Name its certificate trust chain, its per-setting
  reporting, its survival across reimage, and its central gate: Autopilot registration through an OEM
  or a Microsoft CSP partner. Write this in the corpus's own voice, not as a quotation.
- **UEFI CSP** — the Windows configuration service provider DFCI settings use.

Add one reciprocal `> See also:` blockquote under the DFCI entry pointing to `[Secure Boot](#secure-boot)`
and `[UEFI CSP](#uefi-csp)`, matching the shape `### TPM` already uses at line 141. This is D-61's
reconciliation against the two existing entries: `### Secure Boot`'s body already reads "A UEFI
firmware feature that verifies the bootloader's digital signature, required for TPM attestation." and
must not be rewritten. Keep the see-also line at 200 characters or fewer — the file is enrolled in the
document contract and assertion #12 caps every top-level blockquote at 200 characters.

Do NOT link any new entry to a file under `docs/operations/firmware-bios/`. Plan 01 creates those
files in the same wave and the commit ordering between the two plans is not guaranteed; an outbound
link written here could dangle in one commit and take the link checker red. The inbound direction from
`00-overview.md` to `#secure-boot` is safe because that anchor already exists at HEAD.

Insert one `[Term](#anchor)` entry per new term into the single-line `## Alphabetical Index` at line
31, in alphabetical position: the two BIOS entries between `[Autopilot Reset]` and `[BootstrapperAgent]`;
the DFCI entry between `[Corporate identifiers]` and `[Device phase]`; the UEFI CSP entry between
`[TPM attestation]` and `[User phase]`. Keep the whole index on ONE physical line — it is a single
pipe-delimited line today and 147 D-34's one-physical-line rule applies.

Add exactly one row to the `## Version History` table at line 279, in the table's existing
`| Date | Change | Author |` shape with author `--`, at the top of the data rows where the newest row
sits. Name Phase 149, the four terms, the section they were added to, and that the Alphabetical Index
was updated, matching the Phase-75 precedent row's level of detail at line 285.

Change nothing in the front matter. This file's review cycle is 90 days
(`last_verified: 2026-06-29` / `review_by: 2026-09-27`) and is not the milestone's 60-day interval;
"correcting" it would be a defect (D-63).

Do not add the two Phase-150 vendor tool terms handed forward by D-64 — the glossary is touched once
per phase, not twice.

The file is LF in the worktree (`git ls-files --eol docs/_glossary.md` reports `w/lf`), so a string
replacement will match; D-18 records that a line-ending mismatch is a silent zero-match.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-49.mjs; node scripts/validation/c17-eee-contract.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### BIOS configuration and other settings$' docs/_glossary.md` returns 1
    - `grep -c '^### BIOS password$' docs/_glossary.md` returns 1
    - `grep -c '^### Device Firmware Configuration Interface (DFCI)$' docs/_glossary.md` returns 1
    - `grep -c '^### UEFI CSP$' docs/_glossary.md` returns 1
    - all four new `### ` headings fall between the `## Hardware` H2 and the next `## ` H2 (verify with `awk '/^## Hardware$/,/^## Network$/' docs/_glossary.md | grep -c '^### '`)
    - `grep -c '{#' docs/_glossary.md` returns 0
    - `grep -nE '^### .*( / |&)' docs/_glossary.md` returns nothing
    - the `## Alphabetical Index` line contains `[BIOS configuration and other settings](#bios-configuration-and-other-settings)`, `[BIOS password](#bios-password)`, `[Device Firmware Configuration Interface (DFCI)](#device-firmware-configuration-interface-dfci)` and `[UEFI CSP](#uefi-csp)`
    - the `## Alphabetical Index` content is still exactly one physical line (`awk '/^## Alphabetical Index$/{f=1;next} f&&NF{print;exit}' docs/_glossary.md | wc -l` returns 1)
    - `git diff docs/_glossary.md | grep -c '^+| 20'` returns 1 (exactly one new version-history row)
    - `git diff docs/_glossary.md | grep -E '^[-+](last_verified|review_by|doc_id|status):'` returns nothing
    - the DFCI entry is followed by a `> See also:` line containing `(#secure-boot)`, and that line is at most 200 characters (`awk 'length>200' docs/_glossary.md` returns nothing)
    - `grep -c 'operations/firmware-bios' docs/_glossary.md` returns 0
    - `node scripts/validation/check-phase-49.mjs` exits 0 with `22 passed, 0 failed`
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with `0 with violations, 0 total violations`
  </acceptance_criteria>
  <done>Four new terms, four index entries and one version-history row are in `docs/_glossary.md`; the collision audit and the document contract are both green.</done>
</task>

<task type="auto">
  <name>Task 2: File the registration-path premise amendment in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the BIOS section (BIOS-01..BIOS-12), and the `## Future Requirements` section for the existing amendment-note conventions such as the bracketed `[SUPERSEDED by ...]` and `[OWNER-RULED ...]` markers already used in that file.
    - .planning/ROADMAP.md — the `### Phase 149:` block, Success Criterion 4, which carries the same count.
    - docs/lifecycle/01-hardware-hash.md lines 53-64 — the `### Import Methods` table, which is the corpus evidence that four channels exist. Read it; do not edit it.
    - docs/admin-setup-apv2/00-overview.md lines 29-33 — the APv1-deregistration statement that establishes the APv2 no-registration case. Read it; do not edit it.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-05 in full, and D-68 for what is deliberately left uncorrected.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-RESEARCH.md — section Sources item (b), for the verbatim eligibility sentence the amendment restates.
  </read_first>
  <action>
Amend `.planning/REQUIREMENTS.md` so BIOS-04's premise is corrected on the record rather than shipped
around. Add a bracketed amendment note to the BIOS-04 bullet, using the same marker style the file
already uses elsewhere — `**[AMENDED <execution date>]**` — recording, concretely:

1. That BIOS-04's and ROADMAP Phase-149 Success Criterion 4's count of **three** registration paths was
   measured against one corpus file only, and is falsified by two others.
2. That `docs/lifecycle/01-hardware-hash.md`'s `### Import Methods` table carries **four** rows — CSV
   upload, OEM direct registration, PowerShell (`Get-WindowsAutopilotInfo`), and Partner Center, whose
   "Who Initiates" cell reads CSP partner. Cite the file by full relative path (D-72).
3. That the first-party gate qualifies the fourth channel: the eligibility sentence on
   `configure-dfci-windows` names an OEM **or a Microsoft CSP partner** registration as what confers
   the external attestation DFCI requires, so two channels qualify and the two manual channels
   disqualify — not one qualifies and two disqualify.
4. That `docs/admin-setup-apv2/**` registers devices with no Autopilot registration at all
   (`04-corporate-identifiers.md` uses a manufacturer/model/serial CSV, and
   `admin-setup-apv2/00-overview.md` states APv1 registration must be removed for APv2 to work), so an
   APv2 fleet has no external attestation and therefore no DFCI, ever.
5. That ROADMAP Phase-149 SC#4 carries the same "three" and is to be read through this amendment —
   Phase 149's content documents four channels and the APv2 case, so a verifier grading SC#4 literally
   would grade against a premise this amendment retires. Do not edit the ROADMAP Success Criterion
   text itself; D-05 scopes the amendment to `.planning/REQUIREMENTS.md`.

Also add the Phase-149 traceability rows for BIOS-01, BIOS-02, BIOS-03, BIOS-04 and BIOS-11 in the
file's existing `## Traceability` section shape, and add to `## Future Requirements` the deferred
entries this phase creates: a fuller treatment of CSP-partner Autopilot registration as a documented
admin-setup path (this phase documents it as a channel only), and the standalone Surface UEFI file
that D-20 folds into `01-windows-dfci.md` (trigger: a requirement that gives Surface UEFI its own
scope). Leave the requirement checkboxes alone — execution and verification flip those, not this task.

Do not touch `.planning/REQUIREMENTS.md:152`'s known self-referential grep claim; it is on the
deferred list and correcting it is out of scope. Do not introduce the purged off-by-one filename
literal this plan's acceptance criteria grep for — `check-phase-54.mjs` validator 21 live-reads this
file and `ROADMAP.md` with a negative assertion on exactly that string, and this is the
still-unremediated live-reading exposure `PROJECT.md` names.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AMENDED' .planning/REQUIREMENTS.md` returns at least 1, and the amendment note is inside or immediately below the BIOS-04 bullet
    - the amendment names all four channels: `grep -c 'Partner Center' .planning/REQUIREMENTS.md` returns at least 1, and the same for `OEM direct registration` and `Get-WindowsAutopilotInfo`
    - the amendment cites `docs/lifecycle/01-hardware-hash.md` by full relative path
    - the amendment names the APv2 case and cites `docs/admin-setup-apv2/00-overview.md` by full relative path
    - the amendment states that an OEM **or** a Microsoft CSP partner registration qualifies
    - the amendment explicitly names ROADMAP Phase-149 Success Criterion 4 as carrying the same count
    - `git diff .planning/ROADMAP.md` shows no change to any Success Criterion line
    - `grep -c '05-compliance-policy.md' .planning/REQUIREMENTS.md` returns 0
    - `.planning/REQUIREMENTS.md` gains traceability rows naming BIOS-01, BIOS-02, BIOS-03, BIOS-04 and BIOS-11 against Phase 149
    - `node scripts/validation/check-phase-54.mjs` exits 0 with `32 passed, 0 failed` (validators 21 and 27 both read this file)
  </acceptance_criteria>
  <done>BIOS-04's falsified count is amended on the record with all four channels, the qualifying gate and the APv2 case, and the live-reading validator is still green.</done>
</task>

<task type="auto">
  <name>Task 3: Gate sweep for the glossary and planning-ledger edits</name>
  <files>docs/_glossary.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/_glossary.md and .planning/REQUIREMENTS.md — the current committed state after tasks 1 and 2.
    - .planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-CONTEXT.md — D-57 for which three gates genuinely bind this phase, D-59 for the spelling grep, D-60 for why the greps are the gate, D-63 for the collision audit, D-71 for the reversed deferral grounds this plan records.
  </read_first>
  <action>
Run the prose negatives over the two edited files and the full gate set, and fix any hit in place
before committing. `check-phase-54`'s validator 27 walks `.planning/` as well as `docs/`, so it binds
`.planning/REQUIREMENTS.md` and every plan file in this phase directory; `check-phase-49` loads the
glossary's H3 term set for the Linux-glossary collision audit; and the document contract binds
`docs/_glossary.md` because it carries a document identifier and lives under `docs/`.

Then run the apex, `check-phase-144.mjs`, and record its measured pass/fail/skip counts in the SUMMARY
rather than transcribing any number from CONTEXT.md.

Record in the SUMMARY, so no later phase re-imports them, that all three of the earlier draft's grounds
for deferring the glossary were defective (D-71): the "no v1.21 requirement covers it" test was the
wrong test, because `STATE.md:327` is a locked roadmap decision and `PROJECT.md:31` is a milestone
scope statement; the "most heavily pinned Reference file" claim was false, the measured validator
mentions being 130 / 120 / 63 for three other glossary files against 10 for this one; and the
zero-margin freshness hazard was already measured as falsified by 147 D-47. Also record that this
plan is what makes `ROADMAP.md:185`'s "none in the existing corpus" blast-radius claim inaccurate — an
accepted inconsistency under D-68, not a defect to be surfaced later as new.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-49.mjs; node scripts/validation/check-phase-54.mjs; node scripts/validation/c17-eee-contract.mjs; node scripts/validation/check-phase-144.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `git diff HEAD~2 --name-only` lists only `docs/_glossary.md` and `.planning/REQUIREMENTS.md`
    - `grep -niE "enrolment|enrols|enrolling\b|licence|behaviour|organisation|programme|labelled|honour|catalogue|recognise|analyse" $(git diff HEAD~2 --name-only) | wc -l` returns 0 for lines this plan added
    - `awk 'length>200' docs/_glossary.md | wc -l` returns 0
    - `node scripts/validation/check-phase-49.mjs` exits 0 with `22 passed, 0 failed`
    - `node scripts/validation/check-phase-54.mjs` exits 0 with `32 passed, 0 failed`
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 with `0 total violations`
    - `node scripts/validation/check-phase-144.mjs` exits 0, and its measured counts are recorded verbatim in the SUMMARY
    - the SUMMARY records the three reversed deferral grounds and the accepted blast-radius inconsistency
  </acceptance_criteria>
  <done>Both edited files pass every binding gate, the apex is green, and the SUMMARY carries the reversed grounds and the accepted inconsistency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream vendor documentation → this corpus | Mechanism facts underlying the four definitions are drawn from first-party pages |
| Planning ledger → downstream phases | The amendment changes a premise later phases and the verifier grade against |

This plan edits two Markdown files. There is no application code, no authentication surface, no user
input handling and no data storage — **there is no ASVS/STRIDE attack surface**, which is the
conclusion 149-RESEARCH.md's Security Domain section reaches and which is stated here rather than
omitted. ASVS level 1, block on `high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-149-07 | Spoofing | Glossary definition prose | low | mitigate | Definitions are written in the corpus's own voice, not as quotations, so no attribution can be fabricated (D-37, D-52) |
| T-149-08 | Tampering | `.planning/REQUIREMENTS.md` live-read surface | medium | mitigate | Task 2's acceptance criteria assert the negative literal validator 21 reads, and task 3 runs `check-phase-54` before the plan closes |
| T-149-09 | Repudiation | Change provenance in an Approved reference document | low | mitigate | Exactly one `## Version History` row naming the phase, the terms and the index update |
| T-149-10 | Information disclosure | — | low | accept | No secret or tenant-specific value is authored |
| T-149-11 | Denial of service | — | low | accept | Static Markdown; no runtime |
| T-149-12 | Elevation of privilege | — | low | accept | No permission model is implemented |
| T-149-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs — 149-RESEARCH.md's Package Legitimacy Audit records "Not applicable" |

No threat is rated `high` or `critical`, so no blocking security checkpoint is inserted.
</threat_model>

<verification>
- `node scripts/validation/check-phase-49.mjs` — 22 passed, 0 failed (the glossary H3-collision audit)
- `node scripts/validation/c17-eee-contract.mjs` — 234 files, 0 violations, all 13 assertions at 0
- `node scripts/validation/check-phase-54.mjs` — 32 passed, 0 failed (validators 21 and 27 both bind this plan's files)
- `node scripts/validation/check-phase-144.mjs` — the apex, green; counts recorded, not transcribed
- `docs/_glossary.md` front matter is byte-unchanged
</verification>

<success_criteria>
- Four new terms, four Alphabetical Index entries and one version-history row exist in `docs/_glossary.md`
- The roadmap-locked glossary decision named at `STATE.md:327` is discharged and the disposition is recorded
- ROADMAP SC#4's registration-path premise is amended on the record in `.planning/REQUIREMENTS.md`, naming four channels, the qualifying gate and the APv2 case
- Every binding gate is green
</success_criteria>

<output>
Create `.planning/phases/149-firmware-bios-domain-overview-dfci-surface-uefi/149-02-SUMMARY.md` when done
</output>
