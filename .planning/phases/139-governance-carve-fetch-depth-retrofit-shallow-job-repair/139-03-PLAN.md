---
phase: 139-governance-carve-fetch-depth-retrofit-shallow-job-repair
plan: 03
type: execute
wave: 3
depends_on: ["139-02"]
files_modified:
  - scripts/validation/check-phase-49.mjs
  - scripts/validation/check-phase-51.mjs
  - scripts/validation/frozen-read-negative-test.mjs
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: true
requirements: [SWEEP-03, GOV-02]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "FOUR silent-swallow frozen-read sites fail loud, not three: `check-phase-49.mjs:264`, `check-phase-49.mjs:297`, `check-phase-49.mjs:334`, and `check-phase-51.mjs:31` (D-30)."
    - "Fail-loud means the inline try/catch is DELETED so the throw propagates to the runner's existing outer catch, which already converts it to one FAIL row — not converted to an explicit false-return (D-27)."
    - "The wrong-diagnosis detail strings that reported a reachable file as missing are gone, so a shallow-clone run reports the real git cause instead."
    - "A frozen read that succeeds with empty content returns an empty string with no throw; only a non-zero git exit throws — an empty-but-valid file is not conflated with a failed read."
    - "The negative test builds its shallow clone with the `file://` URL form and hard-guards on `.git/shallow` existing before the test body runs (D-31)."
    - "The negative test covers THREE cases: an unreachable SHA, a path absent from both disk and the SHA, and a path present on disk but absent at the frozen SHA (D-31)."
    - "Inside the shallow clone, `check-phase-49.mjs` and `check-phase-51.mjs` exit non-zero with the typed frozen cause visible in their output — the fail-loud behaviour is proven at the validator level, not only at the library level."
    - "`check-phase-51.mjs`'s `readFile` function still carries its CRLF normalization after the edit, verified by a REGION-SCOPED gate over that function's body — not a whole-file grep, which the second match inside `V-51-05` would satisfy vacuously (D-42)."
    - "The D-42 gate's regex is read from `check-phase-68.mjs` at runtime rather than transcribed, because a hand-transcription of its escaping never matches (D-42)."
    - "The remaining silent-swallow sites are OUT of scope here and are recorded as SWEEP-09 against Phase 141 (D-33)."
  prohibitions:
    - requirement_id: SWEEP-03
      category: transparency
      statement: "MUST NOT report a reachable file as missing when the real fault is an unreachable SHA — a wrong diagnosis is worse than a loud failure because it sends the reader to the corpus instead of to the clone depth."
  artifacts:
    - path: "scripts/validation/frozen-read-negative-test.mjs"
      provides: "file:// shallow-clone negative harness proving fail-loud at library and validator level"
      contains: ".git/shallow"
    - path: "scripts/validation/check-phase-49.mjs"
      provides: "Three fail-loud frozen-read call sites"
    - path: "scripts/validation/check-phase-51.mjs"
      provides: "One fail-loud frozen-read call site in readTreeFrozen"
  key_links:
    - from: "scripts/validation/frozen-read-negative-test.mjs"
      to: "scripts/validation/_lib/frozen-at-close.mjs"
      via: "asserts err.frozenCause across the three D-31 cases"
      pattern: "frozenCause"
    - from: "scripts/validation/check-phase-49.mjs"
      to: "the runner's existing outer catch"
      via: "the readAt*Close throw now propagates instead of being swallowed"
      pattern: "Unexpected error: "
---

<objective>
Atom 3 of D-41: make the four silent-swallow frozen-read sites fail loud (D-27/D-30) and prove it
with a real `file://` shallow-clone negative harness covering three cases (D-31), guarded by the
D-42 region-scoped `V-68-01` gate.

D-32 places this BEFORE the workflow retrofit. The original justification is void under D-23; the
honest reason is that shipping the retrofit first would leave known silent-swallow sites in the
tree at the end of the phase that names them.

Purpose: SWEEP-03 — a shallow CI job currently reports "File missing" when the real fault is an
unreachable SHA, and one site silently drops roughly 25% of its sibling-term coverage.
Output: two validators edited, one new negative harness, GOV-02 ledger rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-CONTEXT.md
@.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-PATTERNS.md
@.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-02-SUMMARY.md
</context>

## Artifacts this phase produces

See the table in `139-01-PLAN.md` → "Artifacts this phase produces". This plan creates
`scripts/validation/frozen-read-negative-test.mjs`.

<tasks>

<task type="auto">
  <name>Task 1: Grep-before-edit, region-scoped V-68-01 gate, and the four fail-loud sites</name>
  <precondition>`node scripts/validation/_lib/frozen-at-close.mjs --self-test` exits 0 — the typed frozenCause from Plan 02 is in place, so the propagated throw carries a cause.</precondition>
  <read_first>
    - `scripts/validation/check-phase-49.mjs` — the whole file; the three sites in their surrounding `run()` bodies (V-49-18's per-file loop, V-49-19's sibling-term scan, V-49-21's reciprocal-link check) and the runner's own outer catch near the bottom
    - `scripts/validation/check-phase-51.mjs` — `readFile` near the top (the D-42 gate target), `readTreeFrozen` and its frozen-aware header comment, the `V-51-05` block that contains the SECOND match of the D-42 regex, `V-51-06` through `V-51-11` which consume `readTreeFrozen`, and the runner's outer catch
    - `scripts/validation/check-phase-68.mjs` lines 45-65 — `V-68-01`, whose regex literal the gate must read at runtime rather than transcribe, and lines 196-225 for `V-68-10`/`V-68-11`
    - `scripts/validation/check-phase-111.mjs` lines 40-95 — `V-111-TOOL03`, the literal-needle pinning mechanism to grep for
    - `.planning/milestones/v1.20-CARVE.md` — the GOV-02 grep procedure and the allowlist categories covering these two validators
  </read_first>
  <files>scripts/validation/check-phase-49.mjs, scripts/validation/check-phase-51.mjs, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <action>
**(a) GOV-02 target-scoped grep, before any edit (D-12).** Search `scripts/validation/`,
`scripts/pipeline/` and `.github/workflows/` for the path literals
`scripts/validation/check-phase-49.mjs` and `scripts/validation/check-phase-51.mjs`, for the
symbols `readTreeFrozen`, `readAtV15Close`, `readAtV116Close`, `readAtV115Close`, and for each
detail-string literal about to be removed. Record every hit with file and line. If any validator
pins one of the lines about to be removed as a literal `String.includes()` needle, DO NOT remove
that line — keep it verbatim, record the frozen call-site conflict in the ledger, and adapt the
edit around it. That is the v1.18 Phase 133 precedent, where a regression gate caught exactly this
class mid-execution.

**(b) Establish the D-42 region-scoped gate as a runnable script step, before and after.** Read
`V-68-01`'s regex source directly out of `scripts/validation/check-phase-68.mjs` at runtime — do
not transcribe it, because its escaping is written for a string-embedded regex and a hand copy
never matches. Apply that regex ONLY to the body of `check-phase-51.mjs`'s `readFile` function,
extracted by its own function boundaries, and assert a match there. A whole-file application is
useless: the same pattern also occurs inside `V-51-05`, so deleting `readFile`'s normalization
would still pass a file-wide check.

**(c) The four fail-loud edits (D-27/D-30).** In `check-phase-49.mjs`, at each of the three sites,
delete the inline try/catch wrapper so the frozen read's throw propagates to the runner's existing
outer catch, which already formats it as one FAIL row and already drives the process exit code —
no per-site message formatting is needed and none should be added. Where the deleted wrapper's
fallback value made a subsequent null-branch reachable, delete that branch too: the branch at the
per-file loop in `V-49-18` whose failure text reports the document as absent, and the branch in
`V-49-21` whose detail text attributes the absence to the document rather than to the read. Both
are the wrong-diagnosis strings SWEEP-03 exists to remove; both become unreachable once the throw
propagates. Leave the five unrelated live-read null-branches in the same file untouched — they
guard `readFile`, which genuinely returns null for a missing path, and they are correct.

In `check-phase-51.mjs`, delete the try/catch inside `readTreeFrozen` so the helper either returns
content or throws. Deliberately LEAVE the six null guards in `V-51-06` through `V-51-11` in place:
they become unreachable, they mislead nobody once unreachable, and touching six additional frozen
call sites multiplies the GOV-02 grep surface for zero behavioural gain. Record that choice in the
ledger row so it reads as deliberate rather than as an oversight.

Preserve every existing frozen-aware header comment in both files verbatim — they carry the
honest-accounting citations to the authorizing SUMMARYs, and rewriting them would break the
audit trail without changing behaviour.

**(d) Re-run the gate and append ledger rows.** One row per edited file, each carrying the grep
command, the hit count, the region-scoped `V-68-01` gate result, and the standalone exit codes of
`check-phase-49.mjs`, `check-phase-51.mjs` and `check-phase-68.mjs` after the edit.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-68.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-68.mjs` exits 0 — `V-68-01`, `V-68-10` and `V-68-11` all still pass
    - The region-scoped gate passes: `awk '/^function readFile/,/^}/' scripts/validation/check-phase-51.mjs | grep -c 'replace(/' ` is at least 1, and the regex used was read from `check-phase-68.mjs` at runtime rather than transcribed
    - `node scripts/validation/check-phase-49.mjs` exits 0 on the normal deep worktree, with the same PASS/FAIL/SKIPPED tally it had before the edit
    - `node scripts/validation/check-phase-51.mjs` exits 0 on the normal deep worktree, with the same tally it had before the edit
    - `grep -cE 'catch *\{ *(content|androidContent) *= *(null|"")' scripts/validation/check-phase-49.mjs` equals 0
    - `grep -cE 'catch *\{ *return null' scripts/validation/check-phase-51.mjs` equals 0
    - `grep -c 'frozen V116 read failed' scripts/validation/check-phase-49.mjs` equals 0 <!-- planner-discipline-allow: frozen V116 read failed -->
    - `grep -c ': file missing' scripts/validation/check-phase-49.mjs` equals 0 <!-- planner-discipline-allow: : file missing -->
    - `grep -c 'File does not exist: ' scripts/validation/check-phase-49.mjs` is still 5 — the live-read guards were not collateral damage
    - `grep -c 'Honest-accounting' scripts/validation/check-phase-49.mjs` and the same in `check-phase-51.mjs` are unchanged from their pre-edit values
    - `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-138.mjs` reports 0 FAIL
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` gained one row per edited file, each with a non-empty grep command, hit count, and regression-gate result
  </acceptance_criteria>
  <done>All four sites propagate their throw, the wrong-diagnosis strings are gone, `readFile`'s CRLF normalization survives a region-scoped check, and check-phase-68 plus the apex are still green.</done>
</task>

<task type="auto">
  <name>Task 2: file:// shallow-clone negative harness — three cases plus validator-level proof</name>
  <read_first>
    - `scripts/validation/_lib/frozen-at-close.mjs` — `frozenCause`, `readAtClose`, `lsTreeAtClose` as built in Plan 02
    - `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-RESEARCH.md` — the three stderr strings reproduced live this session, and the verified finding that a plain local clone path silently ignores `--depth`
    - `scripts/validation/check-phase-48.mjs` lines 60-100 — the repo's self-test CLI output shape to mirror
    - `scripts/validation/check-phase-49.mjs` and `check-phase-51.mjs` as edited by Task 1
  </read_first>
  <files>scripts/validation/frozen-read-negative-test.mjs</files>
  <action>
Create `scripts/validation/frozen-read-negative-test.mjs` — a standalone Node ESM script, not a
chain validator and not added to any `CHAIN_PHASES` array. State that in its header comment along
with the reason: it performs a real clone, so it belongs on the CLI, not in a per-run apex.

Setup, per D-31: create a temp directory via the platform temp API and run
`git clone --depth 1 file://<absolute repo path>` into it. The `file://` URL form is MANDATORY and
goes verbatim into the success criterion — a plain local filesystem path makes git print one
warning, ignore `--depth`, retain the full history, and let the frozen read SUCCEED, which would
make this entire harness a silent green. Derive the URL from the repository root with backslashes
normalised to forward slashes so it works on Windows. Then HARD-GUARD: assert `.git/shallow`
exists inside the clone and abort loudly if it does not. Run every assertion below with the clone
as `cwd`. Remove the temp directory at the end, including on failure.

Three library-level cases, each asserting both `err.frozenCause` and that the cause appears at the
FRONT of `err.message`:
  1. **Unreachable SHA** — read a corpus file at the v1.5 close tag from inside the shallow clone.
     Expected cause `unreachable-sha`.
  2. **Path absent from both disk and the SHA** — read a path that exists nowhere, at a SHA that
     IS reachable in the clone. Expected cause `absent-path`.
  3. **Path present on disk but absent at the frozen SHA** — the realistic production case of a
     file added after the frozen close. Read a recently-added path at an older reachable SHA.
     Expected cause `absent-path`, and specifically the second sub-pattern, which a two-pattern
     classifier would have mis-routed to `other`.
Also assert the non-throwing boundary: a frozen read of a file that is valid but empty at its SHA
returns an empty string rather than throwing, so an empty file is never conflated with a failed
read. If no such file exists at a pinned SHA, create the case by writing an empty file, committing
it in the clone, and reading it at that commit.

One `lsTreeAtClose` case inside the same clone: an enumeration at an unreachable SHA throws with
cause `unreachable-sha` and does NOT return an empty array — this is the assertion that would have
caught the "green while auditing nothing" failure mode.

Validator-level proof, which is the real point of SWEEP-03: from inside the shallow clone, run
`node scripts/validation/check-phase-49.mjs` and `node scripts/validation/check-phase-51.mjs`.
Assert both exit non-zero AND that their combined output contains the typed cause token
`unreachable-sha`, proving the validators now surface the real fault instead of a missing-file
diagnosis. Record the pre-edit contrast in the plan SUMMARY: before this plan, the same run
reported the documents as absent.

Emit numbered assertion lines and a trailing `N/N PASS` summary; exit non-zero on any failure.
Append the results to the GOV-02 ledger as the regression-gate evidence for both edited files.
  </action>
  <verify>
    <automated>node scripts/validation/frozen-read-negative-test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/frozen-read-negative-test.mjs` exits 0 and prints an `N/N PASS` summary with at least 6 assertions
    - `grep -c 'file://' scripts/validation/frozen-read-negative-test.mjs` is at least 1
    - `grep -c "'.git/shallow'\|\".git/shallow\"" scripts/validation/frozen-read-negative-test.mjs` is at least 1 — the guard is present, not assumed
    - The harness output names all three D-31 cases and shows `unreachable-sha` for case 1 and `absent-path` for cases 2 and 3
    - The harness output shows both `check-phase-49.mjs` and `check-phase-51.mjs` exiting non-zero inside the clone with `unreachable-sha` in their output
    - `grep -c 'CHAIN_PHASES' scripts/validation/frozen-read-negative-test.mjs` equals 0 — the harness is never a chain member <!-- planner-discipline-allow: CHAIN_PHASES -->
    - `grep -c 'execSync(' scripts/validation/frozen-read-negative-test.mjs` equals 0
    - After the run, no temp clone directory remains under the OS temp directory
    - `node scripts/validation/carve-gate.mjs` exits 0 — the new script is on-list, or an amendment commit preceded it per the D-09 procedure
  </acceptance_criteria>
  <done>A real depth-1 `file://` clone with a verified `.git/shallow` proves all three frozen-read causes and proves both edited validators now fail loud with the real git cause.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git stderr -> validator FAIL detail -> CI log | The propagated error message now reaches CI output verbatim instead of being swallowed |
| Repository root path -> `file://` clone URL | A filesystem path is interpolated into a URL passed to `git clone` |
| Temp clone directory -> host filesystem | The harness creates, writes to, and removes a real directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-10 | Information disclosure | Propagated git stderr in CI logs | low | accept | git's frozen-read stderr contains only a SHA and a repo-relative path; the repository has no `secrets.` reference in any workflow, so no credential can appear |
| T-139-02 | Tampering | `git clone` URL construction | medium | mitigate | The URL is derived from the resolved repository root, never from user input, and passed via `execFileSync` argv; `execSync` asserted absent |
| T-139-11 | Denial of service | Temp clone not cleaned up | low | mitigate | The temp directory is removed in a `finally` path so a failed assertion still cleans up; the acceptance criteria assert no residue |
| T-139-12 | Tampering | Removing a line another validator pins verbatim | high | mitigate | The GOV-02 target-scoped grep runs before the edit; any literal-needle hit means the line is kept verbatim and the conflict is recorded, per the v1.18 Phase 133 precedent |
| T-139-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs zero packages |
</threat_model>

<verification>
- `node scripts/validation/check-phase-49.mjs` and `check-phase-51.mjs` each exit 0 on the deep worktree with unchanged tallies
- `node scripts/validation/check-phase-68.mjs` exits 0
- `node scripts/validation/frozen-read-negative-test.mjs` exits 0
- `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-138.mjs` reports 0 FAIL
- `node scripts/validation/carve-gate.mjs` exits 0
</verification>

<success_criteria>
- Four sites, not three, propagate their throw
- The two wrong-diagnosis detail strings are gone; the five unrelated live-read guards survive
- `readFile`'s CRLF normalization is proven present by a region-scoped gate whose regex was read at runtime
- A real `file://` depth-1 clone with a verified `.git/shallow` proves three causes plus the empty-file boundary
- Both edited validators fail loud inside that clone with the typed cause in their output
</success_criteria>

<flagged_assumptions>
- Roughly 34 further silent-swallow frozen-read sites across the remaining validators are
  deliberately OUT of scope here and are recorded as requirement SWEEP-09 against Phase 141 by
  Plan 01 Task 3 (D-33). `check-phase-61.mjs:39-45`'s `readAtV15CloseFor61` is specifically
  unfixable at the library root and is noted as such in REQUIREMENTS.md.
</flagged_assumptions>

<output>
Create `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-03-SUMMARY.md` when done
</output>
