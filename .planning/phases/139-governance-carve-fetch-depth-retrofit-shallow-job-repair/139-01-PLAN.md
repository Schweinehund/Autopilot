---
phase: 139-governance-carve-fetch-depth-retrofit-shallow-job-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.20-CARVE.md
  - scripts/validation/carve-gate.mjs
  - .claude/hooks/v1.20-carve-gate.cjs
  - .claude/settings.local.json
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GOV-01, GOV-02]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single artifact at .planning/milestones/v1.20-CARVE.md carries both a narrative rationale and a machine-parseable fenced allowlist block, and the allowlist spans Phases 139-144 by CATEGORY (D-01/D-03/D-04)."
    - "The artifact is named v1.20-CARVE and the token CARVE-1 does not appear as this milestone's own artifact name anywhere in it (D-02)."
    - "The CARVE records that Phase 133's D-04 remains in force — no workflow may set a ref: to a close SHA — and that adding fetch-depth is orthogonal to ref: (D-05)."
    - "node scripts/validation/carve-gate.mjs exits 0 on a tree whose in-scope changes are all on-list, and exits non-zero when any in-scope path is off-list (D-06/D-10)."
    - "Allowlist matching is deterministic and total: a path matched by at least one category is on-list, a path matching zero categories is off-list, there is no rule ordering or precedence, so on-list and off-list partition the in-scope change set."
    - "carve-gate.mjs exits 0 with an explicit zero-changed-paths message on a clean tree, and exits non-zero when the CARVE's fenced allowlist block is absent or parses to zero patterns — an empty allowlist is a hard error, never permissive."
    - "carve-gate.mjs emits off-list paths sorted lexicographically, so two runs against identical tree state produce byte-identical stdout."
    - "carve-gate.mjs performs only read-only git invocations and writes no file and mutates no index, so a concurrent or interrupted run leaves the repository unchanged."
    - "A commit in the gate's range that touches the CARVE and also touches any in-scope path fails the gate — the amendment must land in its own commit, before the edit it authorizes (D-09)."
    - "A .claude/hooks/ Stop-hook registered in .claude/settings.local.json nudges on first fire and warns on second, is read-only, and fails open on any parse or IO error (D-08)."
    - "An append-only ledger exists at .planning/milestones/v1.20-GOV-02-LEDGER.md with a declared row schema covering file, grep command, hit count, regression gate run, and result (D-11)."
    - "Every frozen-surface path modified in this phase has at least one ledger row; two edits to one path may be two rows, and the Phase-144 row-per-path assertion is satisfied by path presence, not by row count (D-11)."
    - "A plan that modifies no frozen-surface path adds no ledger row, and the ledger header states that this absence is correct rather than missing evidence."
    - "The ledger is append-only: new rows are appended at the end in commit order, and no existing row is edited or reordered by a later plan."
    - "The GOV-02 grep procedure recorded in the CARVE is target-scoped — every grep enumerates the file PATH string as well as the symbol name (D-12)."
    - "ROADMAP.md Phase 139 SC#2 and REQUIREMENTS.md SWEEP-01 state the 97-checkout / 16-workflow scope as an explicit success-criterion amendment (D-13/D-14)."
    - "ROADMAP.md Phase 139 SC#3 and REQUIREMENTS.md SWEEP-02 are re-worded to the dedicated no-needs probe-job form (D-24), so the phase is markable Validated on evidence that is obtainable in Phase 139."
    - "ROADMAP.md Phase 139 SC#4 and REQUIREMENTS.md SWEEP-03 name four fail-loud sites, not three (D-30)."
    - "REQUIREMENTS.md carries a new SWEEP-09 requirement scoped to Phase 141, with a traceability row, and every requirement count that referenced 27 is updated consistently (D-33)."
  prohibitions:
    - requirement_id: GOV-01
      category: transparency
      statement: "MUST NOT let the allowlist self-authorize — a blocked edit must never be cleared by appending its own category to the CARVE in the same commit as the edit."
    - requirement_id: GOV-01
      category: safety
      statement: "MUST NOT degrade a non-zero gate result to a warning-and-continue; the only resolution path for an off-list hit is a separate, prior amendment commit."
    - requirement_id: GOV-02
      category: values
      statement: "MUST NOT edit either glossary's last_verified or review_by metadata to satisfy any assertion anywhere in this milestone."
  artifacts:
    - path: ".planning/milestones/v1.20-CARVE.md"
      provides: "Milestone-scoped authorization of record for every frozen-surface edit in Phases 139-144"
      contains: "carve-allowlist"
    - path: "scripts/validation/carve-gate.mjs"
      provides: "Diff-based byte-unchanged gate over scripts/ .github/ docs/ .gitattributes package.json"
      contains: "--self-test"
    - path: ".claude/hooks/v1.20-carve-gate.cjs"
      provides: "Read-only Stop-hook, nudge-then-warn enforcement of the gate"
      contains: "computeDecision"
    - path: ".planning/milestones/v1.20-GOV-02-LEDGER.md"
      provides: "Append-only row-per-edit GOV-02 evidence ledger"
      contains: "| File | Grep command |"
  key_links:
    - from: "scripts/validation/carve-gate.mjs"
      to: ".planning/milestones/v1.20-CARVE.md"
      via: "parses the first fenced carve-allowlist block out of the CARVE narrative"
      pattern: "v1\\.20-CARVE\\.md"
    - from: ".claude/hooks/v1.20-carve-gate.cjs"
      to: "scripts/validation/carve-gate.mjs"
      via: "spawns the gate read-only and maps its exit code to nudge/warn"
      pattern: "carve-gate\\.mjs"
    - from: ".claude/settings.local.json"
      to: ".claude/hooks/v1.20-carve-gate.cjs"
      via: "Stop hook command registration, 15s timeout"
      pattern: "v1\\.20-carve-gate\\.cjs"
---

<objective>
Deliver the governance layer that authorizes and bounds every frozen-surface edit in v1.20:
the `v1.20-CARVE` artifact (D-01/D-02/D-03/D-04/D-05), the diff-based `carve-gate.mjs`
(D-06/D-07/D-09/D-10), the `.claude/hooks/` Stop-hook (D-08), the GOV-02 evidence ledger
(D-11/D-12), and the scope-of-record amendments to ROADMAP.md / REQUIREMENTS.md
(D-13/D-14/D-24/D-30/D-33).

This is atom 1 of D-41's five-atom order — **nothing in Phases 139-144 may land before it**.

**Tracer reconciliation (TRACER_MODE=true vs D-41):** D-41 is a locked user decision and takes
precedence over the default decomposition shape. It does not conflict: atom 1 is itself a
complete end-to-end governance slice (authorization -> enforcement -> evidence), so Task 1 is
authored as the `type="tracer"` task and is proven end-to-end before any expansion. Atoms 2-5
(Plans 02-06) are the expansion tasks, sequenced by D-41 rather than by wave parallelism.

Purpose: GOV-01's allowlist and byte-unchanged gate govern every edit landed across Phases
139-144, not just this phase's. Without it, every subsequent frozen-surface edit is unauthorized.
Output: 5 new artifacts + 2 amended planning documents.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-CONTEXT.md
@.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created by Phase 139 (all six plans). The plan-review source-grounding pass must
treat every entry below as newly-created, not as drift against existing code.

| Kind | Symbol / path | Plan |
|---|---|---|
| file | `.planning/milestones/v1.20-CARVE.md` | 01 |
| file | `scripts/validation/carve-gate.mjs` | 01 |
| file | `.claude/hooks/v1.20-carve-gate.cjs` | 01 |
| file | `.planning/milestones/v1.20-GOV-02-LEDGER.md` | 01 |
| file | `scripts/validation/frozen-read-negative-test.mjs` | 03 |
| export | `lsTreeAtClose(milestoneTag, dirPrefix, { ext } = {})` in `_lib/frozen-at-close.mjs` | 02 |
| export | `lsTreeAtV141Close` … `lsTreeAtV118Close` (16 per-milestone convenience exports) | 02 |
| export | `frozenCause(err)` classifier in `_lib/frozen-at-close.mjs` | 02 |
| property | `err.frozenCause` (`unreachable-sha` \| `absent-path` \| `other`) on throws from `readAtClose` / `lsTreeAtClose` | 02 |
| CLI flag | `--self-test` on `_lib/frozen-at-close.mjs` | 02 |
| CLI flag | `--self-test`, `--json`, `--base <ref>` on `carve-gate.mjs` | 01 |
| CI job | `frozen-read-probe` (one per workflow, 16 total) | 05 |
| requirement | `SWEEP-09` (recorded here, scoped to Phase 141) | 01 |
| fence tag | ` ```carve-allowlist ` block language tag | 01 |

No new `check-phase-NN.mjs` validator and no new validator ID is created in this phase — D-07
explicitly bars adopting the gate as a chain assertion; `check-phase-139.mjs` is Phase 144's work.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end governance slice — CARVE authorizes, gate enforces, ledger records</name>
  <reversibility rating="one-way">The allowlist is the authorization of record for five phases of frozen-surface edits (D-03); narrowing it retroactively invalidates edits already landed under it. No checkpoint is inserted — CONTEXT.md D-03 records the owner choosing this exact option via adversarial review, so the door is already walked through; the rating is kept for the record per the reversibility taxonomy.</reversibility>
  <read_first>
    - `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-CONTEXT.md` — D-01 through D-12 verbatim; the allowlist's eight categories are enumerated in D-03
    - `.planning/REQUIREMENTS.md` — GOV-01/GOV-02 text and the Out of Scope table (the glossary-metadata bar and the "no new content documentation" bar)
    - `.planning/milestones/v1.18-phases/133-chain-validator-tooling-debt-closure/133-ONE-N-ATTESTATION.md` — the narrative+contract artifact shape precedent; note it is a human-only artifact
    - `scripts/validation/check-phase-63.mjs` lines 195-260 — the byte-unchanged gate CLASS this gate deliberately inverts (frozen-to-frozen vs live-diff); read it to see what `carve-gate.mjs` is NOT
    - `scripts/validation/check-phase-48.mjs` lines 1-100 — the repo's `--self-test` + `execFailDetail` CLI idiom to mirror
  </read_first>
  <files>.planning/milestones/v1.20-CARVE.md, scripts/validation/carve-gate.mjs, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <action>
Author the three governance artifacts and wire them end-to-end.

**(a) `.planning/milestones/v1.20-CARVE.md`** — one file, narrative plus a fenced
machine-parseable allowlist, per D-01. Narrative sections, in order: Purpose and span (Phases
139 through 144 inclusive, per D-04 — this is an owner-ratified edit to the ROADMAP hard
constraint that currently reads 139-143); Naming (the artifact is `v1.20-CARVE`; per D-02 do not
reuse the `CARVE-1` token, which is a live v1.18 Phase 133 token this milestone discharges);
Relationship to Phase 133's D-04 (per D-05, record that D-04 REMAINS IN FORCE — no workflow may
set a `ref:` to a close SHA — and that adding `fetch-depth` is orthogonal to `ref:`; do NOT
record any reversal); Scope exclusion (per D-06, `.planning/` is deliberately outside the gate's
diff scope, and state why: it makes `allowlist subset-of narrative` true by construction and
keeps the gate from policing its own authorization artifact); Amendment procedure (D-09 verbatim
as a numbered procedure); Gate failure disposition (D-10: hard-block on non-zero exit in a plan's
verification step, Stop-hook nudges then warns, the ONLY resolution path is a prior amendment
commit, and note that gate failure is the EXPECTED steady state for Phases 140-143);
GOV-02 grep procedure (D-12: every grep enumerates the file PATH string as well as the symbol
name, modelled on `check-phase-111.mjs`'s `V-111-TOOL03` literal `String.includes()` needle match,
with `check-phase-66.mjs:42`'s `CI_WORKFLOW` path literal cited as the concrete example a
symbol-only grep misses); Recorded scope amendments (D-13/D-14 the 97-checkout / 16-workflow
extension, D-24 the SWEEP-02 probe-job re-scope, D-30 the fourth fail-loud site, D-33 the
SWEEP-09 deferral to Phase 141 — each named as an explicit success-criterion amendment, not a
clarification); and Standing bars (the glossary `last_verified`/`review_by` zero-margin hazard,
and REQUIREMENTS.md Out of Scope line 116's bar on new content documentation).

Then the allowlist: exactly one fenced block whose language tag is `carve-allowlist`, containing
one glob per line, `#` comment lines and blank lines ignored. Author it BY CATEGORY per D-03 —
the eight categories are: all `.github/workflows/audit-harness-*.yml` (16 today plus the 17th
born in Phase 144); all `scripts/validation/v*.*-milestone-audit.mjs`; the nine Pillar-C files
(`scripts/validation/c17-eee-contract.mjs`, `scripts/pipeline/convert.ps1`,
`scripts/validation/check-nav-hub-links.mjs`, and the six `scripts/pipeline/retrofit-*.mjs`);
`scripts/validation/_lib/frozen-at-close.mjs` and `scripts/validation/_lib/archive-path.mjs`;
`scripts/validation/check-phase-{30,31,48,49,51,60,61,62,63,64,65,66,68,69,70}.mjs`;
`scripts/validation/regenerate-supervision-pins.mjs`; the gate's own artifacts self-listed
(`scripts/validation/carve-gate.mjs`, and note the hook and both `.planning/milestones/v1.20-*`
artifacts are out of diff scope by the `.planning/` exclusion); and `docs/_glossary-macos.md`
plus `docs/admin-setup-ios/**` for Phase 143's LINK-03 fixes. Above each glob group put a `#`
comment naming the category and the requirement it serves.

**(b) `scripts/validation/carve-gate.mjs`** — a milestone-lifetime Node ESM script. Per D-07 it
is explicitly NOT a chain validator and MUST NOT be added to any `CHAIN_PHASES` array; put that
statement in its header comment with the reason (a live-HEAD diff assertion frozen into a
permanent apex member goes red at the first v1.21 content commit and stays red forever,
manufacturing the accepted-red class this milestone's bar requires deleting).

Behaviour, per D-06 with all three empirically-corrected mechanics:
  - Default base ref is the v1.19 milestone-close SHA `a7bda73e23efc5e3f9607c3fef37abf8ec4030aa`
    (subject `docs(138-06): v1.19 MILESTONE CLOSE`), overridable with `--base <ref>`.
  - Collect changed paths from `git diff --name-status <base>` — `--name-status`, NOT
    `--name-only`, because after a rename `--name-only` emits only the destination path, so an
    off-list file renamed onto an on-list path would pass silently. For an `R`/`C` status record
    BOTH the source and destination paths.
  - Diff against the base WITHOUT `..HEAD`, so a dirty working tree is visible (D-08 invokes the
    gate in verification steps where trees routinely carry uncommitted work).
  - Additionally collect `git status --porcelain --untracked-files=all`, because a brand-new
    off-list `scripts/*.mjs` is invisible to every `git diff` form. Parse the rename arrow form
    and record both sides.
  - Filter the union to the in-scope prefixes `scripts/`, `.github/`, `docs/`, and the two exact
    paths `.gitattributes` and `package.json`. Everything else, including all of `.planning/`,
    is out of scope and is neither on-list nor off-list.
  - Partition the in-scope set by glob match against the parsed allowlist. Matching is total and
    unordered: a path matching at least one pattern is on-list, a path matching zero patterns is
    off-list, and there is no precedence between patterns. Translate each glob to a RegExp by
    escaping regex metacharacters, then mapping `**` to `.*` and a lone `*` to `[^/]*`; anchor
    both ends. Do not add a glob dependency.
  - Amendment check per D-09: for each commit returned by `git log --format=%H <base>..HEAD`,
    run `git show --name-only --format= <sha>`; if that commit's path set contains the CARVE path
    AND at least one in-scope path, fail with a D-09 violation naming the commit. Apply the same
    rule to the working tree: CARVE dirty together with any in-scope path dirty is a violation.
  - Exit codes: 0 when the in-scope set is empty (print an explicit zero-changed-paths line) or
    entirely on-list; non-zero when any in-scope path is off-list, when a D-09 violation is found,
    or when the CARVE is missing / has no `carve-allowlist` fenced block / that block parses to
    zero patterns. An empty allowlist is a HARD ERROR, never permissive.
  - Off-list paths are printed sorted lexicographically so two runs on identical tree state emit
    byte-identical stdout; each on-list path may optionally report which category matched.
  - The script is strictly read-only: only `git diff`, `git status`, `git log`, `git show
    --name-only`; it writes no file, stages nothing, and never runs `git add` or `git stash`.
  - All git invocations use `execFileSync('git', [...argv])` with an argument array, never
    `execSync` with string interpolation — the repo-wide convention.
  - Flags: `--base <ref>`, `--json` (emit the partition as JSON), `--self-test`.
  - `--self-test` asserts at minimum: the glob translator classifies an adjacency pair correctly
    (a path covered by a category glob is on-list while its sibling under the same directory that
    no glob covers is off-list); a CARVE whose fenced block parses to zero patterns produces a
    non-zero exit rather than a permissive pass; and off-list output is lexicographically sorted.
    Print a `N/N PASS` summary line.

**(c) `.planning/milestones/v1.20-GOV-02-LEDGER.md`** — append-only, per D-11. Header states:
milestone-level placement (not phase-dir) so there is no `resolveArchivedPhasePath` call site and
so Phases 140-143 never mutate a completed Phase 139 artifact; append-only discipline (rows are
appended at the end in commit order, existing rows are never edited or reordered); row-per-edit
semantics (every frozen-surface path modified gets at least one row; two edits to one path may be
two rows; Phase 144 asserts row-per-path presence, never a bare row count); and that a plan which
modifies no frozen-surface path correctly adds NO row — absence is not missing evidence. Then a
markdown table with the columns `File`, `Grep command`, `Hit count`, `Regression gate run`,
`Result`, `Plan`, and no data rows yet (Plan 01 modifies no frozen-surface path).

Wire the three together: the gate reads the CARVE; the ledger's header cites the CARVE as its
authorization. Commit all three in one commit.
  </action>
  <verify>
    <automated>node scripts/validation/carve-gate.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/carve-gate.mjs --self-test` exits 0 and prints an `N/N PASS` summary
    - `node scripts/validation/carve-gate.mjs` exits 0 on the tree as committed by this task
    - Creating an empty file at `scripts/validation/__carve_gate_offlist_probe.mjs` then running `node scripts/validation/carve-gate.mjs` exits non-zero and names that path in the output; deleting the probe file restores exit 0
    - `grep -c 'carve-allowlist' .planning/milestones/v1.20-CARVE.md` is at least 1
    - `node -e "const c=require('fs').readFileSync('.planning/milestones/v1.20-CARVE.md','utf8');const m=c.match(/```carve-allowlist\n([\s\S]*?)\n```/);if(!m)process.exit(1);const pats=m[1].split('\n').map(s=>s.trim()).filter(s=>s&&!s.startsWith('#'));if(pats.length<8)process.exit(1);console.log(pats.length+' patterns')"` exits 0
    - `grep -v '^#' .planning/milestones/v1.20-CARVE.md | grep -c 'audit-harness-\*' ` is at least 1 (the workflow category is present as a glob, not a file enumeration)
    - `grep -c 'ref:' .planning/milestones/v1.20-CARVE.md` is at least 1 and the surrounding text states Phase 133 D-04 remains in force (D-05)
    - `grep -cE '\.planning/' scripts/validation/carve-gate.mjs` shows the exclusion is expressed in the scope filter, and `grep -c 'CHAIN_PHASES' scripts/validation/carve-gate.mjs == 0` (D-07 — the gate is never a chain member) <!-- planner-discipline-allow: CHAIN_PHASES -->
    - `grep -c 'execSync(' scripts/validation/carve-gate.mjs == 0` — every git call uses `execFileSync` with an argv array
    - `grep -c '| File | Grep command |' .planning/milestones/v1.20-GOV-02-LEDGER.md` is at least 1
    - `git log --oneline -1 --name-only` shows all three artifacts landed in a single commit
  </acceptance_criteria>
  <done>The CARVE authorizes, the gate enforces on a real off-list probe and clears when it is removed, and the ledger exists with its schema — the full governance path works end-to-end and is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Stop-hook enforcement — nudge then warn, read-only, fail-open</name>
  <read_first>
    - `.claude/hooks/publish-bundle-gate.cjs` — the full v1.17 HOOK-01 skeleton D-08 names: stdin parse, `stop_hook_active` early-allow, pure `computeDecision()`, fail-open wrapper, anchored `VERSION_RE`, `--self-test` gated behind `require.main === module`
    - `.claude/hooks/jira-milestone-gate.cjs` — the second registered Stop-hook, for the registration and timeout convention
    - `.claude/settings.local.json` — the existing `Stop` hooks array this task appends to
    - `scripts/validation/carve-gate.mjs` — the script the hook spawns (created in Task 1)
  </read_first>
  <files>.claude/hooks/v1.20-carve-gate.cjs, .claude/settings.local.json</files>
  <action>
Create `.claude/hooks/v1.20-carve-gate.cjs` as a CommonJS Stop-hook cloning
`publish-bundle-gate.cjs`'s exact skeleton per D-08. Reuse its `readStdin()`, `allow()` and
`block(reason)` helpers verbatim, its `stop_hook_active` early-allow guard, its pure
`computeDecision()` seam so the logic is testable without stdin, and its fail-open wrapper — any
parse failure, missing file, or IO error resolves to `allow()`.

Behaviour: spawn `node scripts/validation/carve-gate.mjs --json` read-only with a short timeout
(well inside the 15s hook ceiling). On exit 0, `allow()`. On non-zero, implement D-08's
nudge-then-warn: maintain a small counter in a gitignored scratch file under `.claude/` keyed by
the current gate output hash; the first fire emits a NUDGE reason naming the off-list paths and
pointing at the D-09 amendment procedure, the second and subsequent fires for the same off-list
set emit a WARN reason that additionally states the gate is hard-blocking in the plan's
verification step. Do not invent a third exit path beyond `allow` and `block(reason)`.

Reuse `publish-bundle-gate.cjs`'s anchored version regex form (`^v?\d+\.\d+(\.\d+)?$`) for any
milestone-tag-shaped string the hook derives a path from — this is the recorded path-traversal
mitigation and the only reason a tag string is safe to interpolate into a path.

The hook must be strictly read-only with respect to the repository: it never edits a tracked
file, never stages, and its only write is the scratch counter under `.claude/`. Add a `--self-test`
behind `require.main === module` asserting that `computeDecision()` returns allow on gate-exit-0,
nudge on first non-zero, and warn on repeat non-zero with an unchanged off-list set.

Register the hook in `.claude/settings.local.json` by appending a third entry to the existing
`Stop` array, matching the two existing entries byte-for-byte in shape: `type: "command"`,
`command: "node \"$CLAUDE_PROJECT_DIR/.claude/hooks/v1.20-carve-gate.cjs\""`, `timeout: 15`.
Note in the hook's header comment that `.claude/settings.local.json` is gitignored, so hook
activation is machine-local and does not travel with `git clone` — this is the accepted repo
convention (both existing Stop-hooks are registered the same way), not a defect to fix here.
  </action>
  <verify>
    <automated>node .claude/hooks/v1.20-carve-gate.cjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `node .claude/hooks/v1.20-carve-gate.cjs --self-test` exits 0
    - `echo '{}' | node .claude/hooks/v1.20-carve-gate.cjs` exits 0 (fail-open on an unrecognised payload)
    - `printf '{"stop_hook_active":true}' | node .claude/hooks/v1.20-carve-gate.cjs` exits 0 and emits no decision (early-allow guard)
    - `grep -c 'computeDecision' .claude/hooks/v1.20-carve-gate.cjs` is at least 2 (defined and called)
    - `grep -c 'stop_hook_active' .claude/hooks/v1.20-carve-gate.cjs` is at least 1
    - `grep -cE "\\^v\\?\\\\d\\+" .claude/hooks/v1.20-carve-gate.cjs` is at least 1 (anchored tag regex present)
    - `node -e "const j=require('./.claude/settings.local.json');const n=j.hooks.Stop.filter(e=>JSON.stringify(e).includes('v1.20-carve-gate.cjs')).length;if(n!==1)process.exit(1);console.log('registered')"` exits 0
    - `node -e "const j=require('./.claude/settings.local.json');const e=j.hooks.Stop.find(e=>JSON.stringify(e).includes('v1.20-carve-gate.cjs'));if(e.hooks[0].timeout!==15)process.exit(1)"` exits 0
    - `git status --porcelain .claude/settings.local.json` is empty (the file is gitignored — the registration is machine-local by design)
  </acceptance_criteria>
  <done>The Stop-hook is registered and self-tests green; it allows on a clean gate, nudges on the first off-list fire and warns on repeat, and fails open on malformed input.</done>
</task>

<task type="auto">
  <name>Task 3: Record the four scope amendments in ROADMAP.md and REQUIREMENTS.md</name>
  <read_first>
    - `.planning/ROADMAP.md` — Phase 139 §Success Criteria (SC#2, SC#3, SC#4), Phase 139 §Hard constraints, Phase 141 §Requirements, Phase 144 SC#4, and the Progress tables
    - `.planning/REQUIREMENTS.md` — SWEEP-01/02/03 bullets (lines 16-18), the §Traceability table, and the §Coverage block
    - `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-CONTEXT.md` — D-13, D-14, D-24, D-30, D-33, D-04 (the wording each amendment must land)
    - `scripts/validation/check-phase-54.mjs` lines 25-35 and 340-360 — the ONLY validator that reads `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` LIVE (`check-phase-61` and `check-phase-70` read them frozen and are immune); `V-54-21` is a negative literal grep, so confirm what it forbids before editing
  </read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Run the GOV-02 target-scoped grep first (D-12), then land the amendments. Both documents are
live-read by `check-phase-54.mjs`, so grep for both the file-path strings and the assertion
symbols before editing, and record the result as a ledger row in
`.planning/milestones/v1.20-GOV-02-LEDGER.md`.

**ROADMAP.md, §Phase 139:**
  - SC#2 — replace the three-file / 4+18+10 enumeration with the D-13/D-14 scope: every
    `actions/checkout@v4` step across all 16 `.github/workflows/audit-harness-*.yml` files carries
    `fetch-depth: 0` — 97 previously-shallow checkouts of 182 total, 85 already deep. State
    explicitly that this is an owner-ratified SUCCESS-CRITERION AMENDMENT, not a clarification,
    so Phase 144's close-gate reads the extension as authorized rather than as drift.
  - SC#3 — replace the "11 validators complete their frozen reads in a dispatched CI run" wording
    with D-24's form: a dedicated `frozen-read-probe` job with no `needs:`, one per retrofitted
    workflow, performing a frozen `git show` read plus one real `readAtClose` call, evidenced by
    job-level JSON from a single dispatch. Record D-23's reason inline: all 14 `check-phase-48..61`
    jobs in the v1.5 workflow and all 7 in the v1.6 workflow are `needs: harness-run`, both
    harnesses exit 1 at HEAD, so those validators report `skipped` on any ref and the original
    criterion is structurally unobtainable in Phase 139 — its greening depends on Phase 141.
  - SC#4 — name FOUR fail-loud sites per D-30: `check-phase-49.mjs:264`, `check-phase-49.mjs:297`,
    `check-phase-49.mjs:334`, and `check-phase-51.mjs:31`.
  - §Hard constraints — change the GOV-01 span from "Phases 139-143" to "Phases 139-144" per D-04,
    with the reason: Phase 144 makes the milestone's largest frozen-surface edits (V119 pin, new
    harness, apex regeneration, 17th workflow) and is otherwise ungoverned.

**ROADMAP.md, §Phase 141 Requirements** — add `SWEEP-09` to the requirement list and add a
success criterion for it stating the remaining silent-swallow frozen-read sites fail loud.

**ROADMAP.md, §Phase 144 SC#4** — the requirement count changes from 27 to 28.

**REQUIREMENTS.md:**
  - SWEEP-01 — re-word to the 97-checkout / 16-workflow scope with the same amendment framing.
  - SWEEP-02 — re-word to the `frozen-read-probe` form per D-24.
  - SWEEP-03 — name four sites per D-30.
  - Add a new `SWEEP-09` bullet after SWEEP-08 in the SWEEP section: the remaining silent-swallow
    frozen-read sites (measured at roughly 38 `catch`-to-null/empty frozen-read sites across 20
    validators, of which this phase fixes 4) fail loud, with an explicit note per D-33 that
    `check-phase-61.mjs:39-45`'s `readAtV15CloseFor61` CANNOT be fixed at the library root — it
    carries its own inline reader, does not import the library for those reads, and is pinned in
    place by `check-phase-68.mjs:202` `V-68-10`.
  - §Traceability — add the row `| SWEEP-09 | Phase 141 | Pending |`.
  - §Coverage — update `27 total` / `Mapped to phases: 27 (100%)` to 28 / 28, and update the
    milestone-bar line and any other count that referenced 27 v1.20 requirements.
  - Update the `Last updated` footer line to record this amendment.

Do NOT introduce the literal that `V-54-21` forbids into either document.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-54.mjs` exits 0 (the only live reader of both documents)
    - `grep -c '97' .planning/ROADMAP.md` is at least 1 and the Phase 139 SC#2 line mentions all 16 workflows (D-13/D-14)
    - `grep -c 'frozen-read-probe' .planning/ROADMAP.md` is at least 1 and `grep -c 'frozen-read-probe' .planning/REQUIREMENTS.md` is at least 1 (D-24)
    - `grep -c 'check-phase-49.mjs:334' .planning/ROADMAP.md` is at least 1 and the same literal appears in `.planning/REQUIREMENTS.md` (D-30, four sites)
    - `grep -c 'Phases 139–144\|Phases 139-144' .planning/ROADMAP.md` is at least 1 and `grep -c 'Phases 139–143\|Phases 139-143' .planning/ROADMAP.md == 0` within the Phase 139 hard-constraints block (D-04)
    - `grep -c 'SWEEP-09' .planning/REQUIREMENTS.md` is at least 3 (bullet, traceability row, and the Phase-141 scoping note) and at least 1 in `.planning/ROADMAP.md` (D-33)
    - `grep -c 'readAtV15CloseFor61' .planning/REQUIREMENTS.md` is at least 1 (the D-33 unfixable-at-root note is recorded)
    - `node -e "const c=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8');if(/v1\.20 requirements: 27\b/.test(c))process.exit(1);if(!/28/.test(c))process.exit(1)"` exits 0
    - `grep -vE '^\s*#' .planning/REQUIREMENTS.md | grep -c '05-compliance-policy.md' == 0` and the same for `.planning/ROADMAP.md` (V-54-21 stays green)
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` gained a row whose `File` column names both `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` and whose `Regression gate run` column names `check-phase-54.mjs`
  </acceptance_criteria>
  <done>Phase 139's success criteria and the SWEEP requirement texts match the ratified scope, SWEEP-09 is recorded against Phase 141, requirement counts are consistent at 28, and check-phase-54 still exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code session -> Stop-hook stdin | Hook payload JSON is parsed by `v1.20-carve-gate.cjs`; malformed or hostile payloads must fail open, never throw past the hook boundary |
| Working tree / git object store -> `carve-gate.mjs` | Arbitrary repository paths cross into glob matching and into `git` argv |
| Milestone-tag-shaped strings -> filesystem paths in the hook | A tag string interpolated into a path is a traversal surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-01 | Tampering | `carve-gate.mjs` allowlist parsing | high | mitigate | D-09 amendment check: a commit touching the CARVE and any in-scope path in the same commit fails the gate — the allowlist cannot self-authorize |
| T-139-02 | Tampering | `carve-gate.mjs` git invocations | medium | mitigate | Every git call is `execFileSync('git', [argv…])` with an argument array; `execSync` with string interpolation is asserted absent |
| T-139-03 | Information disclosure | Hook stdout in the session transcript | low | accept | Only repository-relative paths and the gate's own summary are emitted; no secrets exist on these paths, and the repo has zero `secrets.` references |
| T-139-07 | Tampering | Hook path derived from a milestone-tag string | low | mitigate | Reuse `publish-bundle-gate.cjs`'s anchored `^v?\d+\.\d+(\.\d+)?$` regex before any tag string reaches a path |
| T-139-06 | Repudiation | GOV-02 ledger | high | mitigate | Row-per-edit at milestone level, append-only; Phase 144 asserts row-per-path presence against the diff-vs-v1.19-close |
| T-139-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs zero packages; RESEARCH.md records the Package Legitimacy Audit as not applicable, so no install-time legitimacy checkpoint is required |
</threat_model>

<verification>
- `node scripts/validation/carve-gate.mjs --self-test` exits 0
- `node scripts/validation/carve-gate.mjs` exits 0 on the plan's final tree
- `node .claude/hooks/v1.20-carve-gate.cjs --self-test` exits 0
- `node scripts/validation/check-phase-54.mjs` exits 0
- `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-138.mjs` reports 0 FAIL (this plan touches no frozen surface, so the apex must be unchanged)
</verification>

<success_criteria>
- The CARVE exists, is named `v1.20-CARVE`, spans Phases 139-144, and carries a category-based fenced allowlist with at least 8 patterns
- The gate hard-blocks a synthetic off-list path and clears when it is removed
- The Stop-hook is registered with a 15s timeout and self-tests green
- The ledger exists with its declared schema and one row recording the ROADMAP/REQUIREMENTS edit
- ROADMAP SC#2/SC#3/SC#4, the Phase-139 hard-constraint span, and REQUIREMENTS SWEEP-01/02/03 + new SWEEP-09 all match the ratified scope
</success_criteria>

<flagged_assumptions>
- **SWEEP-02 / unclassified edge — UNRESOLVED, carried forward, not dismissed.** The edge probe
  could not classify SWEEP-02, and it stays `unresolved` by rule. The substantive reason: the
  residual question — *do the 11 already-frozen-aware validators actually complete their frozen
  reads in CI?* — cannot be observed in Phase 139 at all. All 11 sit behind `needs: harness-run`,
  both harnesses exit 1 at HEAD, so they report `skipped` on any ref until Phase 141's RED-01
  greens them (D-23). The `frozen-read-probe` job (D-24, Plan 05/06) is an accepted PROXY for that
  evidence, not the thing itself. Recorded here so the Phase 144 close audit reads the proxy as
  authorized rather than as a gap. Surfaced, never silently dropped.
</flagged_assumptions>

<output>
Create `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-01-SUMMARY.md` when done
</output>
