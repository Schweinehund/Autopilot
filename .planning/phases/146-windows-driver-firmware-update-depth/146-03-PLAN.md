---
phase: 146-windows-driver-firmware-update-depth
plan: 03
type: execute
wave: 3
depends_on: ["146-01", "146-02"]
files_modified:
  - docs/operations/patch-management/00-overview.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DRV-01, DRV-07]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reader routed by 00-overview.md for driver and firmware approval workflows reaches 06, not the stub (D-55 site 2)."
    - "00-overview.md no longer describes 01-windows-wufb-rings.md as carrying 'the driver/firmware update policy surface' — a claim the move falsifies (D-55 site 3)."
    - "The Ring Terminology driver bullet cross-links 06 without orphaning the Phase-145 **Source:** line that follows it (D-55 site 1)."
    - "REQUIREMENTS.md:128's C11 row no longer asserts the two phantom coordinates 00-overview.md:78 and 01-windows-wufb-rings.md:77, and records the real hit map including the :186/:191 SCCM-Intune keepers (D-20, D-21)."
    - "node scripts/validation/check-phase-144.mjs prints 101 PASS, 0 FAIL, 0 SKIPPED after the final commit — the D-63 post-phase apex run, matching the pre-phase baseline exactly."
    - "check-phase-54 is 32/0/0, v1.20-milestone-audit is 16/0 and check-nav-hub-links is 0/0 after commit 3 (D-61 commit-3 gate: all three)."
    - "No new row is added to 00-overview.md's 4-platform comparison table (D-57) and the leading applicability blockquote is unchanged (D-56)."
    - statement: "The appended cross-link sentence lands at the end of the driver bullet's last prose line, not after the blank line that separates the bullet from its **Source:** citations (D-55 site 1)."
      verification: backstop
  artifacts:
    - docs/operations/patch-management/00-overview.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "00-overview.md -> 06-windows-driver-firmware-updates.md at three sites, all resolved by the corpus-wide link checker."
    - "REQUIREMENTS.md:128 -> the corrected C11 hit map, the document the planner of every later v1.21 phase is contractually bound to."
---

<objective>
Re-route `00-overview.md`'s three now-false driver and firmware references at `06`, correct
`REQUIREMENTS.md:128`'s stale C11 map, and close the phase with D-63's post-phase apex run. This is
commit 3 of D-61's three-commit sequence and the last commit of Phase 146.

Purpose: the move performed in plans 01 and 02 falsifies two live claims in the cross-platform
overview. `00-overview.md` routes readers wanting *"driver and firmware approval workflows"* to what
is now a stub, and its Related Resources entry describes `01` as carrying *"the driver/firmware
update policy surface"*. Leaving either would ship exactly the corpus self-contradiction this
milestone's Pillar E exists to prevent. Separately, D-20 corrects the C11 row that every later v1.21
planner reads and that Phase 145's CONTEXT-only correction never reached.

Output: two modified files, one commit, and a green apex.

**Where `REQUIREMENTS.md` rides — the choice this plan is required to state.** D-20 leaves the commit
assignment to the planner. It rides **commit 3**, for three reasons. First, commits 1 and 2 are the
gate-heavy ones — D-62 records commit 1 as the riskiest in the phase and commit 2 edits the frozen
zone — and widening either diff with a `.planning/` file buys nothing and costs bisect clarity.
Second, D-21 requires the corrected row to record the `:186`/`:191` keeper map, which plan 02 has by
then re-verified by running the audit against a live edit of that exact zone rather than by reading
a decision log. Third, the row is already wrong at HEAD, so deferring it to the last commit risks no
regression: if the phase aborts after commit 1 or 2, the row is exactly as wrong as it was before,
and no completion was ever claimed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-windows-driver-firmware-update-depth/146-CONTEXT.md
@.planning/phases/146-windows-driver-firmware-update-depth/146-RESEARCH.md
@.planning/phases/146-windows-driver-firmware-update-depth/146-PATTERNS.md
@.planning/phases/146-windows-driver-firmware-update-depth/146-01-SUMMARY.md
@.planning/phases/146-windows-driver-firmware-update-depth/146-02-SUMMARY.md
</context>

<precondition>
Commits 1 and 2 have landed and `docs/operations/patch-management/06-windows-driver-firmware-updates.md`
is tracked at HEAD, because all three of this plan's `00-overview.md` edits link to it and
`check-nav-hub-links.mjs` fails a relative link whose target file does not exist. Assert with
`git ls-files --error-unmatch docs/operations/patch-management/06-windows-driver-firmware-updates.md`
exiting 0, and halt if it does not.
</precondition>

<measured_coordinates>
## Re-measured at plan time against the live files

Every number below was read from the live file at plan time, not transcribed. The executor re-reads
before editing, and works **bottom-up** — site 3 first, then site 2, then site 1 — so that no edit
shifts a coordinate not yet used.

**`docs/operations/patch-management/00-overview.md` is 229 lines.**

| Site | Lines | What is there now | Edit |
|---|---|---|---|
| 1 | `:84-88` | The Ring Terminology driver bullet. `:88` is its last prose line and reads `  period do apply to drivers.` | APPEND a cross-link sentence at the **end of `:88`** |
| — | `:89` | blank | do not touch |
| — | `:90`, `:92`, `:94` | three existing `**Source:**` lines | **do not append here** — this is the D-55 trap |
| 2 | `:153-154` | the Windows routing bullet, currently routing *"driver and firmware approval workflows"* to `01` | AMEND `:153-154` and ADD a new bullet after it |
| 3 | `:211-213` | the Related Resources entry for `01`, whose `:212-213` claims it carries *"the driver/firmware update policy surface"* | AMEND the description |

Site 1's trap is exact and is why D-55 corrected the draft: the bullet is `:84-88`, **not** `:85-90`.
Appending at `:90` would place new prose on top of a Phase-145 citation line, orphaning it and
breaking the D-51 evidence-line shape.

**`.planning/REQUIREMENTS.md` — the C11 row is at `:128`, not `:135`.** `:114` is the
`## Validator Constraints on Requirement Text` heading; `:135` is `## Future Requirements`. Confirmed
by `grep -n "C11 ops regex" .planning/REQUIREMENTS.md` returning `128`.
</measured_coordinates>

<constraints>

**C1 — the leading applicability blockquote at `00-overview.md:9-19` needs no change (D-56).** Its
Windows line names only *"ring topology and Autopatch disambiguation"*, both of which are in the
retained set. Do not touch it. It is also inside `V-54-27`'s corpus walk.

**C2 — no new row in the 4-platform comparison table at `:49-55` (D-57).** The table is concept-keyed
across four platforms and a driver/firmware row would be Windows-only. Phase 147's blast radius
permits appending a **column** only, so a new row here would silently become 147's problem, and
`V-54-08` tolerates either — meaning the validator will not stop you. The new routing bullet at site
2 carries the reader instead.

**C3 — `V-54-09`'s 1500-byte window does not reach the edit site, and the draft's reason was wrong
(D-58).** Measured: the `## Ring Terminology` H2 begins at byte 4667 (`:64`); the 1500-byte window
therefore ends at byte 6167, around `:83`; `WUfB deployment ring` sits at byte 4757 (`:66`) and
`Autopatch ring` at 4786 (`:67`). The driver bullet begins at byte **6233 — 66 bytes past the window
boundary**. The correct reason appending is safe is that **the edit site is outside the window
entirely, so appending cannot evict either token**. The draft's line numbers and its eviction
mechanism were both wrong and its conclusion was right by accident. Re-measure rather than trusting
either account.

**C4 — `V-54-11` does not reach `00-overview.md`.** The bare-`ring` negative is scoped to
`01-windows-wufb-rings.md` alone (`check-phase-54.mjs:174`, `readFile(WIN)`), so D-16's
added-byte rule does **not** bind this file. `V-54-29` does bind it, as a NEGATIVE barring
`Hotpatch`, `VBS` and `MEETS_STRONG_INTEGRITY` from its body prose — none of which this plan writes.

**C5 — `V-54-21` and `V-54-32` bind `.planning/REQUIREMENTS.md`.** Both assert that the literal
`05-compliance-policy.md` appears **zero** times in `REQUIREMENTS.md` and `ROADMAP.md`. Never write
that string into either file, in any edit, in any plan or summary that lands in `.planning/`.
`V-54-32` couples it to two other assertions and reports the whole cluster as a single
atomicity-coupling failure, so breaking it produces a confusing diagnostic pointing at iOS files.

**C6 — `V-54-27` walks `.planning/` as well as `docs/` (D-05, D-22).** Editing `REQUIREMENTS.md` is
safe — no line-start `> **Platform:**` is involved — but the edit runs the same corpus-wide gate as
everything else.

**Baselines, all re-run and confirmed at plan time at HEAD `9b913cba`:** apex `check-phase-144`
**101 PASS / 0 FAIL / 0 SKIPPED** (18.9s) · `v1.20-milestone-audit` **16/0** · `c17-eee-contract`
**234 files / 0 violations** · `check-nav-hub-links` **0/0** · `check-phase-54` **32/0/0** ·
`build-publish-bundle --self-test` **15/0** · `build-filename-map --self-test` **8/0**. The two
pipeline scripts live at `scripts/pipeline/`.

</constraints>

<tasks>

<task type="auto">
  <name>Task 1: The three 00-overview.md routing edits</name>

  <files>docs/operations/patch-management/00-overview.md</files>

  <read_first>
    - `docs/operations/patch-management/00-overview.md:82-96`, `:148-160` and `:205-220` — read all
      three ranges immediately before editing and re-measure. Work bottom-up so an earlier edit does
      not shift a later coordinate.
    - `docs/operations/patch-management/00-overview.md:9-19` and `:49-55` — read to confirm you are
      NOT editing them (C1, C2).
    - `scripts/validation/check-phase-54.mjs:127` (`V-54-08`, the 4-platform table) and `:145-159`
      (`V-54-09`, the 1500-byte window) and `:494` (`V-54-29`, the overview's NEGATIVE).
    - `146-CONTEXT.md` D-55, D-56, D-57, D-58.
    - `146-RESEARCH.md` §5.1's final paragraph — the confirmation that `:211-213` does currently make
      the claim the move falsifies.
    - `146-01-SUMMARY.md` — `06`'s final anchor ids, so any fragment link here targets a real anchor.
  </read_first>

  <behavior>
    - A reader scanning the Ring Terminology driver bullet learns `06` exists.
    - A reader using the routing matrix for driver and firmware approval workflows is sent to `06`,
      and the Windows bullet no longer promises that content from `01`.
    - The Related Resources description of `01` is true again.
    - The comparison table, the leading blockquote and the three `**Source:**` lines are untouched.
  </behavior>

  <action>
Three edits, applied bottom-up.

**Site 3 first — `:211-213`, the Related Resources description of `01`.** It currently reads that
`01` covers WUfB deployment ring topology, Autopatch disambiguation, the in-memory kernel patching
servicing model, *"and the driver/firmware update policy surface"*. That last clause is false after
plan 02: `01` now carries a stub, and the surface is documented in `06`. Amend the description so it
no longer claims `01` carries the surface — the honest replacement is that `01` retains the
driver/firmware disambiguation and the dual-scan pitfall. Then **add a new sibling bullet** for `06`
in the same list, matching the existing bullet shape (link, spaced em dash, short description, with
continuation lines indented two spaces as the neighbouring entries do). The draft's "do not touch
what is green" rule would have preserved this falsehood, which is exactly why D-55 raised the site
count from two to three.

**Site 2 — `:153-154`, the Windows routing bullet.** It currently routes readers planning *"WUfB
deployment ring topology, Autopatch enablement, or driver and firmware approval workflows"* to
`01-windows-wufb-rings.md`. Amend it so it stops routing driver and firmware readers to the stub —
drop the driver-and-firmware clause from the `01` bullet, leaving ring topology and Autopatch
enablement. Then **add a new routing bullet immediately after it** sending readers planning driver
and firmware approval workflows to `06-windows-driver-firmware-updates.md`. Keep the list's existing
sentence pattern: "For Windows tenants planning ..., start with [link]." Place the new bullet
adjacent to the Windows one so the platform grouping of the matrix is preserved.

Per C2, this new bullet is what carries the reader — **do not** add a driver/firmware row to the
4-platform comparison table at `:49-55`. `V-54-08` tolerates the addition, so the validator will not
protect you from making Phase 147's blast radius your problem.

**Site 1 last — `:84-88`, the Ring Terminology driver bullet.** Append a cross-link sentence to the
**end of `:88`**, the bullet's final prose line, which currently reads `  period do apply to
drivers.` The sentence points the reader at `06` for the full approval-mode, workflow and deferral
treatment.

**Do not append at `:90`.** `:89` is blank and `:90`, `:92` and `:94` are three existing
`**Source:**` lines from Phase 145. Appending after the blank line would place new prose on top of a
citation, orphaning it from the claim it supports and breaking the D-51 evidence-line shape. This is
the specific coordinate error D-55 corrected in the draft (`:85-90` was wrong; the bullet is
`:84-88`).

Appending here is safe with respect to `V-54-09` for the reason recorded in C3 — the site is 66 bytes
past the 1500-byte window boundary, so it cannot evict `WUfB deployment ring` or `Autopatch ring`.
Confirm by running the validator, not by re-deriving the byte arithmetic.

Leave `:9-19` and `:49-55` untouched (C1, C2). This file's frontmatter `platform` value stays
`cross-platform`; do not change it and do not add Linux to it — that is Phase 147's constraint and
`V-54-07` reads this field.

Do not write `Hotpatch`, `VBS` or `MEETS_STRONG_INTEGRITY` into this file's body prose (`V-54-29`,
C4). Nothing this task writes should need them.
  </action>

  <acceptance_criteria>
    - `node scripts/validation/check-phase-54.mjs` prints `Summary: 32 passed, 0 failed, 0 skipped` —
      covering `V-54-08` (the 4-platform table), `V-54-09` (the 1500-byte window) and `V-54-29`.
    - `node scripts/validation/check-nav-hub-links.mjs` prints `0 hub-presence failure(s),
      0 corpus-link failure(s), 0 total` — every new `06` link resolves.
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `Summary: 16 passed, 0 failed`.
    - `grep -c '06-windows-driver-firmware-updates.md' <file>` returns `3` — one link per D-55 site.
    - `grep -c 'and the driver/firmware update policy' <file>` returns `0` — site 3's false claim is
      gone (D-55).
    - `grep -c 'or driver and firmware approval workflows, start with \[Windows WUfB Rings\]' <file>`
      returns `0` — site 2 no longer routes driver readers to the stub (D-55).
    - `grep -n 'period do apply to drivers' <file>` shows the appended sentence on the **same line**,
      and the line immediately after it is blank, with the following line still beginning
      `**Source:**` (D-55 site 1 — the citation was not orphaned).
    - `grep -c '^\*\*Source:\*\*' <file>` is unchanged from its pre-edit value — no citation line was
      consumed, displaced or duplicated.
    - `git diff <file>` shows **no** hunk inside `:9-19` (C1/D-56) and **no** added table row in
      `:49-55` (C2/D-57).
    - `grep -c '^platform: cross-platform' <file>` returns `1` — frontmatter untouched.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/validation/check-phase-54.mjs &amp;&amp; node scripts/validation/check-nav-hub-links.mjs &amp;&amp; node scripts/validation/v1.20-milestone-audit.mjs &amp;&amp; test "$(grep -c '06-windows-driver-firmware-updates.md' docs/operations/patch-management/00-overview.md)" = "3"</automated>
  </verify>

  <done>
    All three D-55 sites are corrected: the Ring Terminology driver bullet cross-links `06` without
    orphaning its citations, the routing matrix sends driver and firmware readers to `06` instead of
    the stub, and the Related Resources description of `01` is true again. The comparison table, the
    leading blockquote and the frontmatter are untouched. Not yet committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct REQUIREMENTS.md:128's C11 row</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md:114-133` — the `## Validator Constraints on Requirement Text` table,
      to see the row's current text and the surrounding rows' column shape.
    - `scripts/validation/v1.20-milestone-audit.mjs:570-600` — C11's four patterns, the ±200-char
      window and the `windowKeywords` regex, so the corrected row describes the mechanism that is
      actually implemented.
    - `scripts/validation/check-phase-54.mjs:345-358` (`V-54-21`) and `:560-591` (`V-54-32`) — the
      two assertions that bind this file.
    - `146-CONTEXT.md` D-02, D-06, D-19, D-20, D-21, D-22.
    - `146-02-SUMMARY.md` — plan 02's measured confirmation that the `:186`/`:191` keepers survived a
      live edit of the frozen zone.
  </read_first>

  <behavior>
    - A later v1.21 planner reading this row gets the real hit map, not two phantom coordinates.
    - The row records all four C11 patterns, not only the plural-Autopatch one.
    - The row records that a keyword-window keeper can live in a link path, which nothing else in the
      corpus signals.
    - `V-54-21` and `V-54-32` stay green.
  </behavior>

  <action>
Replace the third column of the `C11 ops regex` row at `:128`. The row currently asserts that
*"`00-overview.md:78` is kept green **solely** by the `mutually`/`exclusive` pair ...
`01-windows-wufb-rings.md:77` is the safe one, independently green via its own `PITFALL-9` literal."*

**Both coordinates are phantoms.** There is exactly **one** case-insensitive `\bAutopatch rings\b`
hit corpus-wide — `01-windows-wufb-rings.md:65`, the H2
`## Windows Autopatch Rings (Disambiguation)` — and `00-overview.md` has **zero**. This is the
document the planner of every later v1.21 phase is contractually bound to, and Phase 145's
CONTEXT-only correction never reached it. Correct it here per D-20's owner ruling, and re-measure
the line number of the `\bAutopatch rings\b` hit against the live file before writing it down rather
than copying `:65` from this plan — plan 02 edited that file.

The corrected cell must record, per D-20 and D-21:

1. **The four-pattern reality**, not just the plural-Autopatch one: `\bSystem Center\b`,
   `\bSCCM\b[^.]*\bIntune\b`, `\bAutopatch rings\b` and `\bSafetyNet\b[^.]*\bcompliance\b`, all
   `gi`, scoped to every `docs/**/*.md` via a **live** `walkMd('docs')` at
   `v1.20-milestone-audit.mjs:585`. Note that `v1.20-audit-allowlist.json` carries no
   `c11_ops_patterns` key, so the hardcoded fallback is in force, and `c11_ops_exemptions` is empty.
2. **The real `\bAutopatch rings\b` map** — one hit corpus-wide, in `01`'s Autopatch H2, kept green
   by an **intra-line** `Disambiguation` keyword at hit-offset +16. Because the keeper is intra-line,
   no insertion anywhere in that file can evict it, which is the actual reason the old
   36-character-cliff concern is dead. State plainly that `00-overview.md` carries zero hits, so the
   deleted claim about `:78` and the `mutually`/`exclusive` pair described a hit that does not exist.
3. **The `\bSCCM\b[^.]*\bIntune\b` keeper map (D-21)** — `01` carries two live hits, at the
   `**Dual-scan source conflict pitfall:**` paragraph and at the first bullet of the list beneath it.
   The first is kept by the prose word `co-management` a few characters away. The second is kept
   **only by the link path** `../co-management/02-windows-workload-sliders.md` sitting inside its
   ±200-character window. Record that a keyword-window keeper can live in a **link path rather than
   in prose**, and that nothing in the file signals it — this is the phase's highest-value finding
   and the row is the only place a later planner will look.
4. **The authoring guidance that replaces the deleted ordering rule.** The cheapest defence is not
   to fire the pattern at all: every first-party source says *Configuration Manager*, never `SCCM` or
   `System Center`, so new prose that uses the current product name matches no pattern and needs no
   window maintained across future edits. Where a hit is unavoidable, ride an allowlisted keyword and
   **verify by running the audit, never by reading the keyword list**.

Re-measure every line number you cite against the live files at execution time and mark the row's
figures as measured. Keep the row inside the existing three-column table shape — one table row, same
pipe count as its neighbours — so the surrounding markdown table still parses.

**Never write the literal `05-compliance-policy.md` into this file** (C5, `V-54-21`, `V-54-32`). It
must remain at zero occurrences in both `REQUIREMENTS.md` and `ROADMAP.md`, and `V-54-32` reports a
breach as a confusing atomicity-coupling failure that names iOS files.

Per D-20 this correction also joins the milestone-close cleanup list beside Phase 145's deferred
`V-54-18` row clarification. Record that in the plan summary; it is a deferred-items note, not a file
edit in this phase.

**Record one further ledger item in the summary, also not a file edit (D-48).** The
`update-enterprise-supersedence` re-fetch is **not** Phase 148's, contrary to the draft: measured,
`grep -in "supersed" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns **zero hits**, and Phase
148's research flag names three re-fetches, none of them supersedence. It is out of scope for Phase
146 with **no forward assignment**, so it is an unowned research-ledger item whose trigger is
whichever phase first quotes app-supersedence content. Note it in the summary so a later planner does
not assume it was already assigned.
  </action>

  <acceptance_criteria>
    - `node scripts/validation/check-phase-54.mjs` prints `Summary: 32 passed, 0 failed, 0 skipped`,
      confirming `V-54-21` and `V-54-32` survived the edit.
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `Summary: 16 passed, 0 failed`.
    - `grep -c '00-overview.md:78' .planning/REQUIREMENTS.md` returns `0` — the first phantom
      coordinate is gone (D-20).
    - `grep -c '01-windows-wufb-rings.md:77' .planning/REQUIREMENTS.md` returns `0` — the second
      phantom coordinate is gone (D-20).
    - `grep -c '05-compliance-policy.md' .planning/REQUIREMENTS.md` returns `0` (C5, `V-54-21`).
    - The corrected row names all four C11 patterns: `grep -c 'SafetyNet' .planning/REQUIREMENTS.md`
      is at least `1` and `grep -c 'System Center' .planning/REQUIREMENTS.md` is at least `1` (D-02,
      D-20).
    - The corrected row records the link-path keeper: `grep -c '02-windows-workload-sliders'
      .planning/REQUIREMENTS.md` is at least `1` (D-21, D-06).
    - `grep -n 'C11 ops regex' .planning/REQUIREMENTS.md` returns exactly one line, and that line has
      the same leading and trailing pipe structure as the rows above and below it — the table still
      parses.
    - `grep -cE '^> \*\*Platform:\*\*' .planning/REQUIREMENTS.md` returns `0` (C6, `V-54-27`).
  </acceptance_criteria>

  <verify>
    <automated>node scripts/validation/check-phase-54.mjs &amp;&amp; node scripts/validation/v1.20-milestone-audit.mjs &amp;&amp; test "$(grep -c '00-overview.md:78' .planning/REQUIREMENTS.md)" = "0" &amp;&amp; test "$(grep -c '05-compliance-policy.md' .planning/REQUIREMENTS.md)" = "0"</automated>
  </verify>

  <done>
    `REQUIREMENTS.md:128`'s C11 row records the four-pattern reality, the single real
    `\bAutopatch rings\b` hit with its intra-line keeper, the two `SCCM`-plus-`Intune` hits with the
    link-path keeper that holds one of them green, and the avoid-the-pattern authoring guidance. Both
    phantom coordinates are deleted. `V-54-21` and `V-54-32` are green. Not yet committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Conditional re-stamp, land commit 3, run the post-phase apex</name>

  <files>docs/operations/patch-management/00-overview.md</files>

  <read_first>
    - `docs/operations/patch-management/00-overview.md:1-7` — the two date stamps.
    - `scripts/validation/check-phase-54.mjs:113-117` — `V-54-07`'s one-sided freshness test.
    - `146-CONTEXT.md` D-59, D-60, D-61, D-63, D-64, D-70.
    - `146-01-SUMMARY.md` — the date commit 1 landed on, which all three files take if execution
      spans midnight.
  </read_first>

  <behavior>
    - Both date stamps move together or neither moves.
    - Commit 3 carries exactly two files.
    - The apex is 101/0/0 after the phase, matching the pre-phase baseline captured in plan 01.
  </behavior>

  <action>
**The re-stamp is conditional and is probably a no-op (D-59).** `00-overview.md` currently reads
`last_verified: 2026-08-19` / `review_by: 2026-10-18` — exactly 60 days, the ceiling of the `> 60`
test, with zero slack. If the execution date differs from `2026-08-19`, re-stamp `last_verified` to
the actual execution date and recompute `review_by = last_verified + 60` by arithmetic in the same
commit. If it does not differ, the instruction is a deliberate no-op — say so and do not manufacture
a diff. Per D-70, if execution spans midnight all three of this phase's files take commit 1's date.

**Never advance one stamp without the other (D-60).** `check-phase-54.mjs:113-117` tests
`if (days > 60)` only, so advancing `last_verified` while leaving `review_by` behind drives `days`
negative and **PASSES**. No validator anywhere compares either stamp to today. The gate will not
catch this for you.

**Land commit 3.** D-61's commit-3 gate is **all three** validators — `check-phase-54.mjs` at 32/0/0,
`v1.20-milestone-audit.mjs` at 16/0 and `check-nav-hub-links.mjs` at 0/0. Commit exactly two files:
`docs/operations/patch-management/00-overview.md` and `.planning/REQUIREMENTS.md`.

**Then run D-63's post-phase apex.** Run `node scripts/validation/check-phase-144.mjs` and confirm it
prints `Result: 101 PASS, 0 FAIL, 0 SKIPPED (total checks: 101)` in roughly 18-22 seconds. This is
the "once after the last commit" half of D-63 and its whole purpose is to be compared against the
pre-phase run plan 01 captured. **A drop from 101 is a regression this phase caused, not a new
baseline** — report it rather than accepting it. The apex sets `CHECK_PHASE_NESTED=1` on each child,
so all children genuinely run.

Finally confirm D-64 held across the whole phase by running both pipeline self-tests. Phase 146 lands
**no registry row, no filename-map row, no canary bump and no ops-index row** — that is Phase 152's
single atomic unit. Both canaries parse `docs/_registry/RE-index.md` rather than walking the
filesystem (`build-filename-map.mjs:277-286`, `build-publish-bundle.mjs:517-524`), so an unregistered
`06` moves neither; a planner obeying INT-02 literally would have taken both self-tests to
225-versus-226 RED.

Do not add `06` to `docs/operations/00-index.md`. `V-59-14` at `check-phase-59.mjs:449` is a frozen
read since Phase 145's FIX-12, so ops-index growth is safe — but it is Phase 152's to land, not this
phase's.
  </action>

  <acceptance_criteria>
    - `node scripts/validation/check-phase-54.mjs` prints `Summary: 32 passed, 0 failed, 0 skipped`
      (D-61 commit-3 gate 1 of 3).
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `Summary: 16 passed, 0 failed`
      (D-61 commit-3 gate 2 of 3).
    - `node scripts/validation/check-nav-hub-links.mjs` prints `0 hub-presence failure(s),
      0 corpus-link failure(s), 0 total` (D-61 commit-3 gate 3 of 3).
    - `node scripts/validation/check-phase-144.mjs` prints
      `Result: 101 PASS, 0 FAIL, 0 SKIPPED (total checks: 101)` — D-63's post-phase apex run,
      identical to the pre-phase baseline captured in plan 01 Task 1.
    - `node scripts/validation/c17-eee-contract.mjs` prints `234 files checked, 0 with violations` —
      still 234, not 235 (D-33 held across the phase).
    - `node scripts/pipeline/build-filename-map.mjs --self-test` prints `8 passed, 0 failed` and
      `node scripts/pipeline/build-publish-bundle.mjs --self-test` prints `15 passed, 0 failed`
      (D-64 held — no registry row, no filename-map row, no canary bump).
    - `git show --stat HEAD` lists exactly two changed files:
      `docs/operations/patch-management/00-overview.md` and `.planning/REQUIREMENTS.md` (D-61
      commit 3).
    - `git log --oneline -3` shows exactly three commits for this phase, in D-61's order: `06`
      created, then `01` stub surgery, then `00-overview.md` plus `REQUIREMENTS.md`.
    - The difference in days between `review_by` and `last_verified` in `00-overview.md`'s
      frontmatter is exactly `60` (D-59, D-60).
    - `git diff HEAD~3 --stat -- docs/_registry/ docs/operations/00-index.md scripts/pipeline/`
      returns empty — D-64 and the ops-index deferral held across all three commits.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/validation/check-phase-54.mjs &amp;&amp; node scripts/validation/v1.20-milestone-audit.mjs &amp;&amp; node scripts/validation/check-nav-hub-links.mjs &amp;&amp; node scripts/validation/c17-eee-contract.mjs &amp;&amp; node scripts/pipeline/build-filename-map.mjs --self-test &amp;&amp; node scripts/pipeline/build-publish-bundle.mjs --self-test &amp;&amp; node scripts/validation/check-phase-144.mjs</automated>
  </verify>

  <done>
    Commit 3 has landed with exactly two files. All three D-61 commit-3 gates are green, the
    post-phase apex is 101/0/0 matching the pre-phase baseline, C17 is still 234/0, and both pipeline
    self-tests are unchanged — confirming D-64 held. Phase 146 is complete: three commits, in D-61's
    order, with every baseline in D-63 restored to its HEAD value.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none at runtime) | This plan edits two Markdown documents — one rendered by GitHub for human readers, one a planning artifact read by later planning agents. Neither defines an endpoint, parses untrusted input, executes code or holds a credential. |

## STRIDE Threat Register

Deliberately short and honest for a prose-only plan. ASVS level 1; nothing rises to `high`, so
nothing blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146-05 | Tampering | `.planning/REQUIREMENTS.md:128` as an instruction surface consumed by later planning agents | medium | mitigate | This row is the document later v1.21 planners are contractually bound to, and its current text contains two phantom coordinates that a planner could act on — the failure mode the phase is correcting. The corrected row is asserted by grep in Task 2's acceptance criteria (both phantoms at zero occurrences, all four patterns and the link-path keeper present), and `V-54-21` plus `V-54-32` gate the file against the one literal that would break the harness. |
| T-146-06 | Tampering | The three new `06` link targets in `00-overview.md` | low | mitigate | `check-nav-hub-links.mjs` runs corpus-wide with no allowlist, ratchet or expected-failure list, and resolves both file targets and anchor fragments. Task 1 asserts the exact link count of 3 and gates on 0 corpus-link failures. |

**Supply chain (`T-{phase}-SC`): not applicable.** No `npm`, `pip` or `cargo` install occurs and no
dependency is added. The Package Legitimacy Gate has nothing to audit; RESEARCH records that this
phase has no external packages.

**Schema:** none touched — no migration, model, table or API contract.

**Spoofing / Repudiation / Information Disclosure / Denial of Service / Elevation of Privilege:** no
applicable component.
</threat_model>

<verification>
Run from the repository root after Task 3, in this order:

1. `node scripts/validation/check-phase-54.mjs` -> `Summary: 32 passed, 0 failed, 0 skipped`.
2. `node scripts/validation/v1.20-milestone-audit.mjs` -> `Summary: 16 passed, 0 failed`.
3. `node scripts/validation/check-nav-hub-links.mjs` -> `0 hub-presence failure(s), 0 corpus-link
   failure(s), 0 total`.
4. `node scripts/validation/c17-eee-contract.mjs` -> `234 files checked, 0 with violations`.
5. `node scripts/pipeline/build-filename-map.mjs --self-test` -> `8 passed, 0 failed`.
6. `node scripts/pipeline/build-publish-bundle.mjs --self-test` -> `15 passed, 0 failed`.
7. `node scripts/validation/check-phase-144.mjs` -> `Result: 101 PASS, 0 FAIL, 0 SKIPPED (total
   checks: 101)` — D-63's post-phase half.

Every number must equal the pre-phase baseline. A deviation is a regression this phase caused.
</verification>

<success_criteria>
- `00-overview.md`'s three D-55 sites are corrected and it links to `06` exactly three times.
- The 4-platform comparison table gained no row (D-57) and the leading applicability blockquote is
  unchanged (D-56).
- `REQUIREMENTS.md:128` carries the corrected C11 row with both phantom coordinates deleted, all
  four patterns recorded, and the `:186`/`:191` link-path keeper map documented.
- Commit 3 carries exactly two files, and the phase totals exactly three commits in D-61's order.
- The post-phase apex is 101/0/0, matching plan 01's pre-phase baseline.
- Both pipeline self-tests are unchanged at 8/0 and 15/0, confirming D-64 held.
</success_criteria>

<output>
Create `.planning/phases/146-windows-driver-firmware-update-depth/146-03-SUMMARY.md` when done.
</output>
