---
phase: 78-legacy-sso-plug-in-migration-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md
  - docs/admin-setup-macos/08-auth-methods-deep-dive.md
  - docs/admin-setup-macos/00-overview.md
autonomous: true
requirements: [SSOMIG-01, SSOMIG-02, SSOMIG-03, SSOMIG-04]

must_haves:
  truths:
    - "An admin opening guide 09 immediately sees the four-term product-name hierarchy (Microsoft Enterprise SSO plug-in [umbrella] vs Platform SSO [Settings Catalog modern] vs SSO app extension [Device Features legacy] vs Kerberos SSO extension [separate Apple-native]) made explicit, plus a when-to-use-which migrate/keep/coexist decision matrix that links (not copies) guide 08 for auth-method selection (SC1 / SSOMIG-01)"
    - "An admin reading guide 09 can follow the staged migration sequence (assign PSSO to pilot -> validate app-sso platform -s + confirm Error 10001/10002 absent -> THEN unassign legacy profile) and is warned that assigning both the legacy Device Features SSO app extension and the Platform SSO Settings Catalog policy to the SAME device triggers Error 10002 and stops both (SC2 / SSOMIG-02)"
    - "An admin reading guide 09 sees, BEFORE the migration sequence, an up-front pre-migration compliance-script prerequisite callout stating the security find-certificate -> app-sso platform -s swap once canonically (with a MEDIUM-confidence VR-3 provenance annotation), and finds a dedicated ## Rollback section whose hard-bordered destructive-action callout carries all three SC3 elements: destructive WPJ-key removal from the Secure Enclave, the CA-blocked-until-re-registered impact window, and a cross-reference (not a restatement) to the compliance-script swap (SC3 / SSOMIG-03)"
    - "An admin reading guide 09 finds a bounded Kerberos SSO Extension (Coexistence) subsection delivering exactly the SC4 trio (distinct Apple-native extension, separate Extension Identifiers, coexists with PSSO) plus one cross-reference to the deferred PSSO-FUT-04 deep-dive, and NO payload/configuration detail (SC4 / SSOMIG-04)"
    - "The 00-overview.md line-49 code-span and the 08-auth-methods-deep-dive.md line-327 code-span are both converted to live markdown links in the SAME commit that creates guide 09; the v1.8 C13 broken-link audit passes 15/15"
  artifacts:
    - path: "docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md"
      provides: "A3 hybrid migration/decision reference: opening disambiguation table, migrate/keep/coexist decision matrix, up-front compliance-script prerequisite callout, staged migration sequence + Error 10002 warning, what-breaks section, dedicated ## Rollback section + destructive callout, bounded Kerberos coexistence subsection, corpus tail"
      contains: "Error 10002"
      min_lines: 90
    - path: "docs/admin-setup-macos/08-auth-methods-deep-dive.md"
      provides: "Live link to guide 09 (line-327 code-span conversion in ## See Also) + new Version-History row"
      contains: "[Legacy Enterprise SSO Plug-in"
    - path: "docs/admin-setup-macos/00-overview.md"
      provides: "Live link to guide 09 (line-49 code-span conversion in numbered list) + new Version-History row"
      contains: "[Enterprise SSO Plug-in"
  key_links:
    - from: "docs/admin-setup-macos/00-overview.md"
      to: "docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md"
      via: "live markdown link replacing the line-49 code-span"
      pattern: "\\[.*\\]\\(09-enterprise-sso-plugin-migration\\.md\\)"
    - from: "docs/admin-setup-macos/08-auth-methods-deep-dive.md"
      to: "docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md"
      via: "live markdown link replacing the line-327 code-span in ## See Also"
      pattern: "\\[.*\\]\\(09-enterprise-sso-plugin-migration\\.md\\)"
    - from: "docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md"
      to: "docs/admin-setup-macos/07-platform-sso-setup.md"
      via: "platform-gate banner + reciprocal ## See Also link (ROADMAP depends-on)"
      pattern: "\\[.*\\]\\(07-platform-sso-setup\\.md\\)"
    - from: "docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md"
      to: "docs/_glossary-macos.md#enterprise-sso-plug-in"
      via: "## See Also glossary anchor (Phase 75)"
      pattern: "_glossary-macos\\.md#enterprise-sso-plug-in"
---

<objective>
Author the macOS legacy Enterprise SSO plug-in & migration reference `docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md` (A3 hybrid structure, locked D-01 body order) and atomically wire it into corpus navigation by converting BOTH existing guide-09 code-spans to live markdown links: `08-auth-methods-deep-dive.md:327` (inside `## See Also`) and `00-overview.md:49` (numbered list item 9).

Purpose: Give the mixed-fleet admin (admins with existing Microsoft Enterprise SSO plug-in / SSO app extension deployments) a single decision-first reference that (1) resolves the #1 documentation risk -- the four-term product-name confusion (SC1); (2) routes the migrate/keep-legacy/coexist decision (SC1); (3) describes a staged migration that never triggers Error 10002 (SC2); (4) makes the mandatory destructive rollback discoverable with its CA-blocked impact and the pre-migration compliance-script swap (SC3); and (5) bounds the Kerberos SSO extension coexistence note to the SC4 trio without re-documenting the deferred PSSO-FUT-04 deep-dive (SC4).

Output:
- NEW `docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md` (A3 hybrid; locked D-01 body order)
- EDIT `docs/admin-setup-macos/08-auth-methods-deep-dive.md` (line-327 code-span -> live link + Version-History row)
- EDIT `docs/admin-setup-macos/00-overview.md` (line-49 code-span -> live link + Version-History row)

These three changes land in ONE atomic commit. The C13 broken-link gate (frozen v1.8 harness, 15-entry allowlist with no "not-yet-authored internal doc" category) requires each live link to `09-...md` to have its target present in the same commit. Splitting the nav edits into a separate commit/wave WOULD FAIL C13 (RESEARCH Pitfall 4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-legacy-sso-plug-in-migration-guide/78-CONTEXT.md
@.planning/phases/78-legacy-sso-plug-in-migration-guide/78-RESEARCH.md
@.planning/phases/78-legacy-sso-plug-in-migration-guide/78-PATTERNS.md
@docs/admin-setup-macos/07-platform-sso-setup.md
@docs/admin-setup-macos/08-auth-methods-deep-dive.md
@docs/admin-setup-macos/02-enrollment-profile.md
@docs/admin-setup-macos/06-config-failures.md
@docs/_glossary-macos.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author guide 09 (09-enterprise-sso-plugin-migration.md) in A3 hybrid structure with the locked D-01 body order</name>
  <files>docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md</files>
  <read_first>
    - .planning/phases/78-legacy-sso-plug-in-migration-guide/78-RESEARCH.md (Content Facts by Section -- the verified four-term disambiguation table cells [Section 1], the migrate/keep/coexist routing-facet rows [Section 2], the 7-step staged migration sequence + exact Error 10002 behavior [Section 3], the four what-breaks items [Section 4], the three rollback SC3 elements + VR-3 MEDIUM-confidence caveat [Section 5], the Kerberos SC4 trio [Section 6], the seeded Config-Caused-Failures rows + See Also targets [Section 7])
    - .planning/phases/78-legacy-sso-plug-in-migration-guide/78-PATTERNS.md (front-matter block, platform-gate blockquote, the D-06 "Before You Migrate -- Update Compliance Scripts First" prerequisite callout pattern, hard-bordered single-paragraph and multi-sub-item callout syntax, the D-05 rollback destructive-action callout shape with its three bullets, DS-2 section-provenance annotation, the four-column Config-Caused-Failures table with `--` no-runbook cells, the See Also section with verified glossary anchor line numbers, the Version-History `| Date | Change | Author |` row with `--` Author)
    - .planning/phases/78-legacy-sso-plug-in-migration-guide/78-CONTEXT.md (locked decisions A1/D-01 body order, B4/D-03 matrix scope, C1/D-05+D-06 rollback+prerequisite, D1/D-08 Kerberos bound; D-03a/D-06a link-not-copy boundaries; Claude's Discretion bounds on prose/heading names)
    - docs/admin-setup-macos/08-auth-methods-deep-dive.md (PRIMARY analog: A3 corpus tail at lines 318-333 -- the `## Configuration-Caused Failures` four-column table with `--` cells, the `## See Also` block, the Version-History table; hard-bordered callout formatting; front-matter)
    - docs/admin-setup-macos/07-platform-sso-setup.md (SECONDARY analog: the `### Known Silent Blockers -- Resolve Before Deployment` callout placed BEFORE `## Steps` [lines ~25-37] -- the structural precedent for the D-06 up-front prerequisite-before-sequence pattern; the section-local `_Section provenance_` annotation at line ~154)
    - docs/admin-setup-macos/02-enrollment-profile.md (corpus skeleton + Version-History exemplar)
    - docs/admin-setup-macos/06-config-failures.md (reverse-lookup hub shape -- guide 09's `## Configuration-Caused Failures` tail must stay hub-compatible for Phase 81)
    - docs/_glossary-macos.md (confirm anchors #enterprise-sso-plug-in [line ~136], #platform-sso [line ~123], #secure-enclave [line ~130] for See Also)
    - scripts/validation/v1.8-milestone-audit.mjs (the C13 gate this guide must not break -- no live link to any not-yet-authored file)
  </read_first>
  <action>
Create `docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md` as a NEW A3 hybrid reference for the mixed-fleet admin. Internal heading names are at your discretion (new file = no anchor-stability constraint) within the SC1-SC4 factual constraints. Follow 78-PATTERNS.md markup exactly. Use double-hyphen `--` (NOT em-dash) for inline dash separators and for empty table cells, matching guide 08.

Front matter (five keys exactly, DS-2 90-day cadence): `last_verified: 2026-06-21`, `review_by: 2026-09-21`, `applies_to: ADE`, `audience: admin`, `platform: macOS`. Immediately after the closing `---`, add the three-line platform-gate blockquote: line 1 states this guide covers Enterprise SSO plug-in migration to Platform SSO; line 2 links `[Platform SSO Setup](07-platform-sso-setup.md)`; line 3 links `[macOS Glossary](../_glossary-macos.md)`. Then the H1 title (its wording is your discretion but MUST clearly identify "Enterprise SSO Plug-in" + "Migration"; Task 3 reads this H1 to keep 00-overview consistent).

Author the body in this LOCKED order (D-01):

1. OPENING DISAMBIGUATION (SC1 / SSOMIG-01) -- surfaced FIRST because terminology confusion is the #1 documentation risk. State the four-term hierarchy explicitly (table or labeled definition list -- format is your discretion): "Microsoft Enterprise SSO plug-in for Apple devices" (umbrella, Company Portal-delivered, includes BOTH Platform SSO and the legacy SSO app extension) · "Platform SSO" (modern Settings Catalog path, macOS 13+/recommended 14+) · "SSO app extension" (legacy Device Features template path) · "Kerberos SSO extension" (separate Apple-native, on-prem AD Kerberos only). MUST surface the named trap (writing "configure the Enterprise SSO plug-in" when meaning "configure Platform SSO" -> admin configures the legacy Device Features template -> Error 10002) and the key functional difference (Settings Catalog PSSO auto-provides BOTH PSSO and the SSO app extension; the legacy Device Features template provides ONLY the SSO app extension -- no OS-level login binding, no Entra device join, no hardware-bound PRT). Use the verified cells from RESEARCH Section 1.

2. WHEN-TO-USE-WHICH DECISION MATRIX (B4 / D-03 / SSOMIG-01) -- a scenario-row matrix scoped STRICTLY to the migrate / keep-legacy / coexist axis (rows = fleet scenarios, columns = recommended path). Use the routing-facet rows from RESEARCH Section 2 (all macOS 13+ -> migrate; mixed fleet with macOS 10.15-12 -> keep legacy for that segment + PSSO for 13+; on-prem AD/Kerberos -> PSSO + separate Kerberos extension, point-reference the Kerberos subsection; same-device legacy+PSSO -> FORBIDDEN/Error 10002; hybrid Entra join -> NOT SUPPORTED). D-03a link-not-copy: the recommended-path column ROUTES to guide 08 for auth-method selection via an inline `[Auth Methods Deep-Dive](08-auth-methods-deep-dive.md)` link -- do NOT add passwordless/phishing-resistant/hardware/macOS-version capability columns (those are guide 08's canonical four-dimension table). D-03c: make "coexist" explicit as CROSS-SEGMENT (separate device groups for legacy-OS and PSSO segments -- supported), NOT same-device (legacy profile + PSSO profile on one device -> Error 10002, forbidden) so the matrix does not contradict the SC2 warning.

3. UP-FRONT PRE-MIGRATION COMPLIANCE-SCRIPT PREREQUISITE CALLOUT (D-06 / C1 interlock) -- placed BEFORE the staged migration sequence, following the guide-07 `### Known Silent Blockers` precedent. Use a hard-bordered callout titled to signal the danger (e.g. "Before You Migrate -- Update Compliance Scripts First"). State ONCE CANONICALLY that from approximately August 2025 new Entra device registrations store the WPJ key in the Secure Enclave rather than the Login Keychain, so `security find-certificate` returns false negatives for PSSO-enrolled devices; admins MUST swap compliance checks to `app-sso platform -s` (e.g. grep "Device Registration") BEFORE starting migration, because false negatives during a SUCCESSFUL migration look identical to a failed migration and can trigger an unnecessary -- destructive -- rollback. Attach a DS-2 `_Section provenance_` annotation marking VR-3 as MEDIUM confidence (`last_verified: 2026-06-21` / `review_by: 2026-09-21`; WPJ-storage date sourced from Jamf Community + Microsoft apple-sso-plugin doc; re-verify at each 90-day review). This callout is the SINGLE canonical home for the swap command -- the `## Rollback` section cross-references it, does NOT restate it (D-06a).

4. STAGED MIGRATION SEQUENCE + ERROR 10002 WARNING (SC2 / SSOMIG-02) -- the staged sequence from RESEARCH Section 3: assign PSSO Settings Catalog to a PILOT group (separate from devices on the legacy Device Features profile) -> validate (`app-sso platform -s` shows Device + User Registration REGISTERED; confirm Error 10001 and 10002 absent; confirm browser/app SSO) -> THEN unassign the legacy Device Features SSO app extension profile -> monitor (the ~48-hour window; flag it as a practitioner-recommended minimum, not an official SLA, per RESEARCH A1) -> expand to full fleet assigning PSSO and unassigning legacy per group without overlap -> do NOT delete the legacy profile until confirmed unassigned everywhere. State the Error 10002 behavior CANONICALLY here: `Error 10002: multiple SSOe payloads configured`; when the new Settings Catalog PSSO policy AND the old Device Features SSO app extension profile are both assigned to the SAME device, BOTH STOP WORKING and PSSO registration is blocked. The explicit warning: never assign both to the same device simultaneously.

5. WHAT BREAKS DURING MIGRATION -- the four items from RESEARCH Section 4: Chrome loses SSO until the `com.microsoft.browsercore.json` native-messaging host is present (fix: fresh Company Portal PKG install or an MDM script to place the file); Edge requires the user be signed in to their Edge profile for browser SSO; macOS 12-and-earlier silent registration failure (no error displayed -- retain the legacy profile or upgrade to 13+ first); the transient profile-conflict window (even brief same-device coexistence during policy sync -> Error 10002 -> SSO stops temporarily; minimize via staged pilot rollout). Attach a DS-2 `_Section provenance_` note to this section (Company Portal version floors / Chrome native-messaging host behavior are version-drift candidates).

6. MANDATORY ROLLBACK (C1 / D-05 / SSOMIG-03) -- a DEDICATED `## Rollback` section (first-class H2 heading, NOT a sub-section of migration, NOT a conditional "if migration fails" appendix; it is mandatory and must be discoverable). Inside it, a single hard-bordered destructive-action callout (title signals destructiveness, e.g. "Destructive Action -- Rollback Removes the Secure Enclave WPJ Key") carrying all THREE SC3 elements as bullets: (a) WPJ-key removal from the Secure Enclave is DESTRUCTIVE -- removing the PSSO policy removes the Secure Enclave WPJ key created at enrollment; cannot be undone without re-enrolling in PSSO; (b) CA-blocked-until-re-registered impact window -- after rollback affected users have NO Entra device registration and cannot satisfy device-based Conditional Access until they re-open Company Portal and complete a fresh legacy WPJ registration manually (an active service outage for users under device-based CA); (c) compliance-script swap -- CROSS-REFERENCE the up-front "Before You Migrate" prerequisite callout by its anchor (do NOT restate the `security find-certificate` -> `app-sso platform -s` command, D-06a) and note scripts must be updated before rollback too.

7. KERBEROS SSO EXTENSION COEXISTENCE NOTE (D1 / D-08 / SSOMIG-04) -- a BOUNDED `### Kerberos SSO Extension (Coexistence)` subsection delivering EXACTLY the SC4 trio and nothing more: (1) distinct Apple-NATIVE extension (Kerberos payload type, not the Microsoft Redirect payload; on-prem AD Kerberos only, not Entra auth); (2) separate Extension Identifiers -- the Apple Kerberos extension uses a DIFFERENT identifier than the Microsoft PSSO extension `com.microsoft.CompanyPortalMac.ssoextension`; sharing an identifier causes override (MG-4); (3) coexists with PSSO -- both can deploy simultaneously as separate profile entries with distinct identifiers (PSSO handles Entra cloud auth, Kerberos handles on-prem). Include EXACTLY one explicit cross-reference to the deferred PSSO-FUT-04 full Kerberos deep-dive (out of v1.9 scope). NO payload walkthrough, NO configuration detail (D-08 / RESEARCH Don't-Hand-Roll).

8. CORPUS TAIL -- `## Configuration-Caused Failures` four-column table `| Misconfiguration | Portal | Symptom | Runbook |` matching guide 08's shape; use the four seeded rows from RESEARCH Section 7 (legacy+PSSO same-device -> Error 10002; PSSO on macOS 12 without legacy retained -> silent failure; Chrome native-messaging host missing; PSSO rolled back without legacy WPJ re-registration -> CA-blocked). Phase-80 runbook filenames are CODE-SPANS (`` `35-macos-sso-sign-in-failure.md` ``), NOT live links (not yet authored). No-runbook cells use `--` (double hyphen). The Error 10002 row references the migration-sequence section, does NOT re-explain the dual-profile mechanism (D-06a link-not-copy).

9. `## See Also` (D-07a) -- live links to `[Platform SSO Setup](07-platform-sso-setup.md)` and `[Auth Methods Deep-Dive](08-auth-methods-deep-dive.md)` (both files exist), plus the three glossary anchors `[Enterprise SSO Plug-in](../_glossary-macos.md#enterprise-sso-plug-in)`, `[Platform SSO](../_glossary-macos.md#platform-sso)`, `[Secure Enclave](../_glossary-macos.md#secure-enclave)` (all verified present). NO live links to Phase-80 runbooks (code-spans only). A `---` horizontal rule precedes the Version-History table.

10. VERSION HISTORY -- `| Date | Change | Author |` table with one row: `| 2026-06-21 | Phase 78 (SSOMIG-01..04): initial Enterprise SSO plug-in migration guide | -- |` (Author `--` double hyphen).

CRITICAL constraint: this file MUST NOT contain any live markdown link to a not-yet-authored file (Phase-80 runbook filenames stay code-spans). The only live internal links are to existing files (07, 08, glossary).
  </action>
  <verify>
    <automated>test -f docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md && grep -q "Error 10002" docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md && grep -q "## Rollback" docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md && grep -q "app-sso platform -s" docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md && grep -q "Secure Enclave" docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md && grep -q "Kerberos SSO" docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md && grep -q "PSSO-FUT-04" docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md && grep -q "07-platform-sso-setup.md" docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md && grep -q "_glossary-macos.md#enterprise-sso-plug-in" docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md && echo "GUIDE09_OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md` exists with the five-field corpus front matter (`last_verified: 2026-06-21`, `review_by: 2026-09-21`, `applies_to: ADE`, `audience: admin`, `platform: macOS`) and the three-line platform-gate blockquote linking guide 07 and the glossary.
    - Opening disambiguation (table or definition list) makes the four terms explicit: "Microsoft Enterprise SSO plug-in", "Platform SSO", "SSO app extension", "Kerberos SSO extension" (SC1).
    - A migrate/keep/coexist decision matrix is present that LINKS to guide 08 for auth-method selection and has NO passwordless/phishing-resistant/hardware capability columns; "coexist" is explicitly disambiguated as cross-segment (NOT same-device) (B4 / D-03 / D-03a / D-03c).
    - The compliance-script swap (`security find-certificate` -> `app-sso platform -s`) appears CANONICALLY in an up-front prerequisite callout placed BEFORE the migration sequence, with a MEDIUM-confidence VR-3 `_Section provenance_` annotation (D-06 / D-06b).
    - The staged migration section contains "Error 10002" and the pilot -> validate -> THEN unassign sequence with the same-device dual-profile warning (SC2).
    - A dedicated `## Rollback` H2 section exists with a hard-bordered destructive-action callout carrying all three SC3 elements (destructive Secure Enclave WPJ-key removal · CA-blocked window · compliance-script-swap cross-reference, NOT a restatement) (SC3 / D-05 / D-06a).
    - A bounded `### Kerberos SSO Extension (Coexistence)` subsection delivers the SC4 trio + exactly one PSSO-FUT-04 cross-reference, with NO payload/config detail (SC4 / D-08).
    - `## Configuration-Caused Failures` four-column table present with `--` no-runbook cells; Phase-80 runbook filenames are code-spans, not live links.
    - `## See Also` links guide 07 + guide 08 live and the three glossary anchors; references no not-yet-authored file as a live link.
    - Verify command prints `GUIDE09_OK`.
  </acceptance_criteria>
  <done>Guide 09 exists with all SC1-SC4 content in the locked D-01 body order; the verify command prints GUIDE09_OK; no live link to any not-yet-authored file exists in the file. This task commits together with Tasks 2 and 3 (C13 atomic landing).</done>
</task>

<task type="auto">
  <name>Task 2: Atomically convert 08-auth-methods-deep-dive line-327 code-span to a live link + add Version-History row</name>
  <files>docs/admin-setup-macos/08-auth-methods-deep-dive.md</files>
  <read_first>
    - docs/admin-setup-macos/08-auth-methods-deep-dive.md (line 327 inside `## See Also` -- current code-span; the Version-History table at lines 331-333)
    - .planning/phases/78-legacy-sso-plug-in-migration-guide/78-PATTERNS.md (the exact line-327 find/replace; the Version-History `--` Author convention; the anchor-stability rule for edited files)
    - .planning/phases/78-legacy-sso-plug-in-migration-guide/78-CONTEXT.md (D-07 atomic dual-conversion; anchor-stability constraint -- body-level edit only, touch NO headings; C13 atomic-landing rule)
  </read_first>
  <action>
Edit ONLY the body of `docs/admin-setup-macos/08-auth-methods-deep-dive.md` -- do NOT add, remove, rename, or reorder any heading (PITFALL-6 anchor stability).

Conversion (line 327, inside `## See Also`): replace the current code-span bullet
`- Legacy Enterprise SSO plug-in and migration guide: ` + backtick code-span `09-enterprise-sso-plugin-migration.md` + ` (Phase 78 -- not yet authored)`
with a live-markdown-link bullet, e.g.:
`- [Legacy Enterprise SSO Plug-in & Migration Guide](09-enterprise-sso-plugin-migration.md) -- when to migrate from the legacy SSO app extension to Platform SSO, the staged sequence, and rollback`
Remove the `(Phase 78 -- not yet authored)` annotation -- the link is now live. Link text is your discretion but must clearly identify guide 09's migration purpose. Use the double-hyphen `--` separator, NOT em-dash. After this edit, NO backtick-wrapped `09-enterprise-sso-plugin-migration.md` code-span remains anywhere in the file.

Version History: append one new row as the most-recent (bottom) entry to the existing `| Date | Change | Author |` table:
`| 2026-06-21 | Phase 78: converted guide-09 code-span to live link in ## See Also | -- |`
(Author `--` double hyphen.)
  </action>
  <verify>
    <automated>grep -q "\[.*09-enterprise-sso-plugin-migration\.md\]" docs/admin-setup-macos/08-auth-methods-deep-dive.md && ! grep -q '`09-enterprise-sso-plugin-migration\.md`' docs/admin-setup-macos/08-auth-methods-deep-dive.md && grep -q "Phase 78: converted guide-09 code-span" docs/admin-setup-macos/08-auth-methods-deep-dive.md && echo "GUIDE08NAV_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `08-auth-methods-deep-dive.md` line ~327 contains a live markdown link matching `[...](09-enterprise-sso-plugin-migration.md)`.
    - The file no longer contains the backtick-wrapped code-span `` `09-enterprise-sso-plugin-migration.md` `` nor the "(Phase 78 -- not yet authored)" annotation.
    - A new Version-History row dated `2026-06-21` referencing Phase 78 is present, Author `--`.
    - No heading was added, removed, renamed, or reordered (anchor stability).
    - Verify command prints `GUIDE08NAV_OK`.
  </acceptance_criteria>
  <done>Guide-08 line-327 is a live link to guide 09, the old code-span is gone, the Version-History row is added, and the verify command prints GUIDE08NAV_OK. This task commits together with Tasks 1 and 3 (C13 atomic landing).</done>
</task>

<task type="auto">
  <name>Task 3: Atomically convert 00-overview line-49 code-span to a live link with description + add Version-History row</name>
  <files>docs/admin-setup-macos/00-overview.md</files>
  <read_first>
    - docs/admin-setup-macos/00-overview.md (lines 45-49 numbered list -- item 9 current code-span; lines 19-32 Mermaid diagram [node I already correct -- do NOT edit]; lines 67-71 Version-History table; line 48 item-8 guide-08 entry as the structural model)
    - docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md (read guide 09's H1 title -- authored in Task 1 -- so the description here is consistent, D-08a)
    - .planning/phases/78-legacy-sso-plug-in-migration-guide/78-PATTERNS.md (the exact line-49 find/replace; the item-8 model; the `--` separator style; the Version-History row format)
    - .planning/phases/78-legacy-sso-plug-in-migration-guide/78-CONTEXT.md (D-07 + D-08a: keep Mermaid node I and prose consistent with guide 09's title; anchor-stability -- body-level edit only, touch NO headings; C13 atomic-landing)
  </read_first>
  <action>
Edit ONLY the body of `docs/admin-setup-macos/00-overview.md` -- do NOT add, remove, rename, or reorder any heading (PITFALL-6 anchor stability). Do NOT edit the Mermaid diagram (lines 19-32): node `I` already reads `[9. Enterprise SSO<br/>Migration]` and is consistent (D-08a -- no Mermaid edit needed).

Conversion (line 49, numbered list item 9): replace
`9. ` + backtick code-span `09-enterprise-sso-plugin-migration.md` + ` (added in a later documentation phase)`
with a live-link form modeled on item 8 (line 48), e.g.:
`9. **[Enterprise SSO Plug-in & Migration Guide](09-enterprise-sso-plugin-migration.md)** -- [one-sentence description consistent with guide 09's actual H1: what the legacy plug-in is vs Platform SSO, the migrate/keep/coexist decision, the staged migration that avoids Error 10002, and the mandatory rollback]`
Read guide 09's H1 (from Task 1) and make the description text consistent with it. Remove the `(added in a later documentation phase)` annotation. Use the double-hyphen `--` separator, NOT em-dash. Do NOT touch any other numbered-list item.

Version History: append one new row as the most-recent (bottom) entry to the existing `| Date | Change | Author |` table:
`| 2026-06-21 | Phase 78: converted guide-09 code-span to live link with description | -- |`
(Author `--` double hyphen.)
  </action>
  <verify>
    <automated>grep -q "\[.*09-enterprise-sso-plugin-migration\.md\]" docs/admin-setup-macos/00-overview.md && ! grep -qE '^9\. `09-enterprise-sso-plugin-migration\.md`' docs/admin-setup-macos/00-overview.md && grep -q "Phase 78: converted guide-09 code-span to live link with description" docs/admin-setup-macos/00-overview.md && echo "OVERVIEW09_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `00-overview.md` item 9 contains a live markdown link matching `[...](09-enterprise-sso-plugin-migration.md)` with a description consistent with guide 09's H1.
    - The old line-49 code-span (backtick-wrapped `09-enterprise-sso-plugin-migration.md` followed by "added in a later documentation phase") is gone.
    - The Mermaid diagram is unchanged (node `I` still `[9. Enterprise SSO<br/>Migration]`).
    - A new Version-History row dated `2026-06-21` referencing Phase 78 is present, Author `--`.
    - No heading was added, removed, renamed, or reordered (anchor stability).
    - Verify command prints `OVERVIEW09_OK`.
  </acceptance_criteria>
  <done>00-overview item 9 is a live link to guide 09 with a consistent description, the old code-span is gone, the Mermaid is untouched, the Version-History row is added, and the verify command prints OVERVIEW09_OK. This task commits together with Tasks 1 and 2 in ONE atomic commit (C13 landing); then run `node scripts/validation/v1.8-milestone-audit.mjs` and confirm 15/15.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation accuracy boundary | This phase ships only Markdown documentation: no executable code, no runtime inputs, no auth surface, no network/data flow. The only "boundary" is the published-guidance boundary -- a destructive or stale command, an omitted warning, or a broken internal link crossing from the guide into an admin's production tenant. The dominant threat class is documentation-induced operational harm. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Denial of service (admin self-inflicted outage from incomplete rollback guidance) | The `## Rollback` section | mitigate | The mandatory dedicated `## Rollback` section + hard-bordered destructive-action callout carries all three SC3 elements -- destructive Secure Enclave WPJ-key removal, the CA-blocked-until-re-registered impact window, and the compliance-script swap cross-reference -- so an admin cannot roll back without seeing the CA-blocked warning (D-05 / SSOMIG-03). |
| T-78-02 | Tampering (silent misconfiguration -> both SSO profiles stop working) | Staged migration sequence | mitigate | The migration section states the Error 10002 dual-profile behavior canonically with an explicit "never assign both to the same device" warning and the pilot -> validate -> THEN-unassign ordering; the decision matrix disambiguates "coexist" as cross-segment (NOT same-device) so it cannot contradict the SC2 warning (D-03c / SSOMIG-02). |
| T-78-03 | Information disclosure / false-negative compliance reporting (stale check causes unnecessary destructive rollback) | Compliance-script guidance (VR-3) | mitigate | The `security find-certificate` -> `app-sso platform -s` swap is surfaced as an UP-FRONT pre-migration prerequisite callout (before the migration sequence), so a successful-migration admin sees the false-negative warning before mistaking a healthy migration for a failure; the MEDIUM-confidence VR-3 fact carries a DS-2 `last_verified`/`review_by` provenance annotation forcing 90-day re-verification (D-06 / D-06b / SSOMIG-03). |
| T-78-04 | Tampering (broken internal link shipped to corpus) | Nav-wiring edits (00-overview:49, 08:327) | mitigate | Both code-span -> live-link conversions land in the SAME atomic commit as guide 09's creation, so the C13 broken-link gate (frozen v1.8 harness, 15-entry allowlist) sees a valid target at check time; verified by `node scripts/validation/v1.8-milestone-audit.mjs` passing 15/15 and `git diff HEAD --name-only` showing all three files together (D-07 / C13). |

No package installs in this phase (documentation only) -> no `T-78-SC` supply-chain checkpoint required. No high-severity unmitigated threats remain. Security enforcement (ASVS L1) is satisfied: this is a content phase that describes -- and does not implement -- authentication and migration mechanisms.
</threat_model>

<verification>
- `node scripts/validation/v1.8-milestone-audit.mjs` PASSES 15/15 after the atomic commit (C13 broken-link gate stays green -- the two live links to guide 09 have their target present in the same commit; Phase-80 runbook filenames remain code-spans). Expected C13 line: `{ pass: true, detail: '15-entry broken-link allowlist (6 transient_external + 9 template_placeholder) honored; full mlc sweep deferred to CI' }`.
- `git diff HEAD --name-only` after the commit shows all three files changed together (atomic C13 landing): `docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md`, `docs/admin-setup-macos/08-auth-methods-deep-dive.md`, `docs/admin-setup-macos/00-overview.md`.
- All three task verify commands print their OK sentinels (`GUIDE09_OK`, `GUIDE08NAV_OK`, `OVERVIEW09_OK`).
</verification>

<success_criteria>
- SC1 (SSOMIG-01): Guide 09 opens with the explicit four-term product-name hierarchy (Microsoft Enterprise SSO plug-in / Platform SSO / SSO app extension / Kerberos SSO extension) and a migrate/keep/coexist decision matrix that links (not copies) guide 08 for auth-method selection.
- SC2 (SSOMIG-02): Guide 09 documents the staged migration sequence (assign PSSO to pilot -> validate -> THEN unassign legacy) with the explicit same-device-dual-profile Error 10002 warning, plus what breaks during migration.
- SC3 (SSOMIG-03): Guide 09 has a dedicated mandatory `## Rollback` section covering destructive Secure Enclave WPJ-key removal, the CA-blocked-until-re-registered window, and the pre-migration `security find-certificate` -> `app-sso platform -s` compliance-script swap (stated canonically up front, cross-referenced from rollback).
- SC4 (SSOMIG-04): Guide 09 includes a bounded Kerberos SSO extension coexistence note (distinct Apple-native extension, separate Extension Identifiers, coexists with PSSO) with one PSSO-FUT-04 out-of-scope cross-reference and no config detail.
- C13: Both code-spans (00-overview:49, 08:327) converted to live links atomically with guide 09's creation; v1.8 audit passes 15/15; all three files in one commit.
</success_criteria>

<output>
Create `.planning/phases/78-legacy-sso-plug-in-migration-guide/78-01-SUMMARY.md` when done.
</output>
