---
phase: 142-archival-path-fix-chain-adoption-cold-clone-threshold
plan: 02
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - .planning/milestones/v1.20-CARVE.md
autonomous: true
requirements: [RED-06]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`scripts/validation/check-phase-138.mjs` appears exactly once in the `carve-allowlist` fenced block of `.planning/milestones/v1.20-CARVE.md`"
    - "The amendment commit touches exactly ONE file — `git show --name-only --format= HEAD` prints exactly one line and that line is `.planning/milestones/v1.20-CARVE.md` (CARVE D-09 rule 1)"
    - "`node scripts/validation/carve-gate.mjs` exits 0 both before and after the amendment commit"
    - "`node scripts/validation/carve-gate.mjs --self-test` exits 0 with its full assertion count passing (the CRLF fence-parse regression closed in Phase 141 stays closed)"
    - "`.planning/milestones/v1.20-CARVE.md`'s 'Recorded scope amendments' list contains at least one `D-28` reference — it contained zero before this plan, which made the list already stale for Phase 141"
    - statement: "The Category-5 header comment's validator count is corrected only if no source file pins its literal text; a grep for the header literal across `scripts/` and `.github/` returned zero hits at plan time, so the correction is safe"
      verification: backstop
  prohibitions:
    - statement: "This commit touches no other path, in-scope or out-of-scope — not the GOV-02 ledger, not a phase artifact, not a source file (CARVE D-09 rule 1, D-19)"
      status: flagged-unverified
    - statement: "No allowlist glob is removed, reordered, or narrowed; the amendment is strictly additive"
      status: flagged-unverified
    - statement: "The gate is never invoked by importing its exported helpers — it calls its entry point at module scope and that entry point exits the process; use the CLI only"
      status: flagged-unverified
    - statement: "A gate failure is never resolved by degrading the gate to a warning; the only resolution path for an off-list hit is a prior amendment commit (CARVE D-10 — there is no warn-and-continue path)"
      status: flagged-unverified
  artifacts:
    - "`.planning/milestones/v1.20-CARVE.md` — Category 5 gains `scripts/validation/check-phase-138.mjs` with a rationale comment"
    - "`.planning/milestones/v1.20-CARVE.md` — 'Recorded scope amendments' gains this phase's bullets plus the back-filled Phase-141 D-27/D-28 bullets"
  key_links:
    - "CARVE Category 5 allowlist entry -> Plan 05's edit to `check-phase-138.mjs` (without this entry that edit makes `carve-gate.mjs` exit 1, which CARVE D-10 makes a hard block)"
    - "'Recorded scope amendments' list -> Phase 144's close-gate (it reads that list, not the in-line markers)"
    - "`carve-gate.mjs`'s D-09 working-tree arm -> a dirty `scripts/`/`.github/`/`docs/` tree during this commit would fire a violation"
---

<objective>
Land **Commit 2 of 3**: the CARVE amendment that authorizes Plan 05's edit to the apex, alone and
before it.

Purpose: `scripts/validation/check-phase-138.mjs` is absent from the v1.20 CARVE allowlist. Editing
it first makes `carve-gate.mjs` report one off-list path and exit 1, which CARVE D-10 defines as a
**hard block inside a plan's verification step, with no warn-and-continue path**. This is a verbatim
repeat of Phase 141's D-12 (`check-phase-67` absent from the same list). The same commit closes a
second defect: the CARVE's "Recorded scope amendments" list — the list Phase 144's close-gate
actually reads — has zero references to Phase 141's D-28 and is therefore already stale.

Output: one single-file commit whose entire diff is `.planning/milestones/v1.20-CARVE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md
@.planning/milestones/v1.20-CARVE.md
@.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the apex to Category 5 and record this phase's scope amendments</name>
  <precondition>`git status --porcelain scripts/ .github/ docs/` prints nothing — `carve-gate.mjs`'s D-09 working-tree arm fires a violation when this file and any in-scope path are dirty together, so the tree must be clean of in-scope changes before this edit.</precondition>
  <reversibility rating="costly">The allowlist entry is the standing authorization Phase 144's apex work also inherits; removing it later would retroactively invalidate every edit made under it and require a re-amendment plus a re-run of the gate across the milestone's whole diff.</reversibility>
  <files>.planning/milestones/v1.20-CARVE.md</files>
  <read_first>
    - `.planning/milestones/v1.20-CARVE.md` in full — especially the amendment procedure, the gate-failure disposition, the GOV-02 grep procedure, the "Recorded scope amendments" list, and the `carve-allowlist` fenced block
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` (D-10, D-11, D-19, D-20, D-25, D-27, D-28, D-29, D-33, D-35)
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-CONTEXT.md` D-27 and D-28 (the two bullets being back-filled)
    - `.planning/milestones/v1.20-CARVE.md` Category 5's existing `check-phase-67.mjs` entry and the multi-line `#` comment above it — the exact precedent for the shape of this addition
  </read_first>
  <action>
    Two additive edits to `.planning/milestones/v1.20-CARVE.md`, and nothing else anywhere.

    **(a) The allowlist entry.** Inside the `carve-allowlist` fenced block, in Category 5, append
    `scripts/validation/check-phase-138.mjs` preceded by a `#` rationale comment (procedure rule 2
    requires a one-line rationale either in the commit message or as a comment directly above the
    added glob — do both). The rationale names RED-06 and Phase 142, and states that the apex gains
    a hand-authored `CHAIN_EXTRA` sidecar carrying phases 30 and 31 into the executed chain while
    `CHAIN_PHASES` itself stays pure arithmetic and its three module-load span invariants stay
    untouched. Follow the multi-line comment shape already used above the `check-phase-67.mjs`
    entry. Do not remove, reorder, or narrow any existing glob.

    Category 5's header comment counts "the sixteen chain validators this milestone touches" and
    that count becomes stale. A grep for that header literal across `scripts/` and `.github/`
    returned zero hits at plan time, so no source file pins it — re-run the grep to confirm, then
    correct the count. If the re-run finds any hit, leave the header byte-unchanged and record the
    staleness in the new rationale comment instead.

    **(b) The "Recorded scope amendments" list.** Its lead sentence says "These four amendments" and
    is about to stop being true — update the count to match the final bullet count. Append, in this
    order:

    Back-fill for Phase 141 (currently missing entirely — a grep for `D-28` in this file returns
    zero, so Phase 144's close-gate reads a list that omits Phase 141's amendments):
      - **D-27, amendment instrument.** Phase 141 ruled that the CARVE's own D-09 procedure is the
        wrong instrument for a success-criterion amendment, because the gate's in-scope prefixes
        exclude `.planning/` entirely so the hazard it was cited against cannot fire; the
        established in-line marker pair is the instrument, and the amendment commit lands alone and
        first for process discipline rather than because of the gate.
      - **D-28, six statements across three documents.** Phase 141 amended ROADMAP Phase 141 SC#2
        (which hard-coded a classifier-context-window mechanism that foreclosed the correct fix),
        ROADMAP Phase 141 SC#4's validator census, REQUIREMENTS' SWEEP-09 census on the reader-site
        unit, and the remaining statements in the same set.

    This phase's own amendments:
      - **D-35, RED-04 extended.** RED-04 as worded is already satisfied vacuously by V-30-02, whose
        regular expression is built from a double-quoted string and has never inspected a Mermaid
        block; the amendment brings that defect into RED-04's scope so the fix is authorized scope,
        not drift.
      - **D-20/D-21/D-22, SC#2 additive.** The archived-path resolver alone is insufficient because
        the target prose relocated; the amended mechanism is the resolver for the fixture plus a
        presence-only content anchor for the target.
      - **D-24, V-31-29 instrument.** Metric corrected to the line-count-utility semantics the
        archived spec mandates, plus a re-derived documented target band for runbook 14 from its
        measured retrofit delta; bound-widening alone and per-runbook exemption both rejected.
      - **D-11, RED-06 membership mechanism.** Chain membership is satisfied by the apex's executed
        chain set — the arithmetic span plus a hand-authored sidecar — not by mutating the span.
        Record the explicit ruling that this satisfies HARN-18's arithmetic-not-transcribed mandate,
        with the non-contiguous hand-authored arrays in `check-phase-60.mjs` and `check-phase-62.mjs`
        as precedent, and that leg (d), giving 30 and 31 CI jobs of their own, is DROPPED.
      - **D-25, span correction.** The `[48..138]` span at the three ratified sites is a drafting
        error corrected to `[48..143]`; Phase 144's apex is `check-phase-144` with 96 entries.
      - **D-27/D-28/D-29, NEST-01 method and threshold.** Four method elements restored plus tree
        identity and a verbosity declaration; threshold is a ratio at 8x or greater against a
        declared same-tree same-session warm median; the absolute ceiling and the timeout-raise legs
        are both dropped; mechanism X is two-tier with its structural tier attributed to CARVE-1 and
        SWEEP-01 rather than double-booked into this phase.

    Marker grammar: keep any `**...**` bold span free of colons; put file:line citations in the
    plain text after the span.
  </action>
  <acceptance_criteria>
    - `grep -c "check-phase-138\.mjs" .planning/milestones/v1.20-CARVE.md` returns `2` (one glob line, one rationale-comment mention) and `grep -c "^scripts/validation/check-phase-138\.mjs$" .planning/milestones/v1.20-CARVE.md` returns `1`
    - `grep -c "D-28" .planning/milestones/v1.20-CARVE.md` returns at least `1` (it returned `0` before this task)
    - `grep -c "D-27" .planning/milestones/v1.20-CARVE.md` returns at least `1`
    - `grep -c "^- \*\*D-" .planning/milestones/v1.20-CARVE.md` returns at least `10`, and the list's lead sentence names that same count rather than "four"
    - Every glob line present before the edit is still present: `git diff -- .planning/milestones/v1.20-CARVE.md | grep -c '^-[^-]'` returns `0` except for the intentionally-reworded header/lead-sentence lines, which the diff must show as the only removals
    - `node scripts/validation/carve-gate.mjs` exits 0
    - `node scripts/validation/carve-gate.mjs --self-test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/carve-gate.mjs --self-test && node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>The apex path is on the allowlist, the amendments list names Phase 141's D-27/D-28 and this phase's six, and both gate invocations exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Land Commit 2 and prove its single-file shape</name>
  <files>.planning/milestones/v1.20-CARVE.md</files>
  <read_first>
    - `.planning/milestones/v1.20-CARVE.md` amendment procedure rules 1 and 3
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` D-19
  </read_first>
  <action>
    Stage and commit ONLY `.planning/milestones/v1.20-CARVE.md`. Rule 1 forbids any other path in
    this commit, in-scope or out-of-scope — including the GOV-02 ledger, this phase's plan or
    summary files, and any source file. `carve-gate.mjs` cannot mechanically detect a fold that
    involves only other `.planning/` paths, because `.planning/` sits outside its diff scope, so
    the single-file shape is proven by the commit's own name list rather than by the gate.

    The commit message carries the one-line rationale procedure rule 2 requires: the apex is added
    to Category 5 to authorize Phase 142 Plan 05's `CHAIN_EXTRA` adoption (RED-06), and the
    "Recorded scope amendments" list is back-filled with Phase 141's D-27/D-28 and extended with
    this phase's six.

    Do not proceed to Plan 03 until this commit exists — rule 3 requires the amendment to land
    BEFORE the edit it authorizes, never in the same commit and never after.
  </action>
  <acceptance_criteria>
    - `git show --name-only --format= HEAD | grep -c .` returns `1`
    - `git show --name-only --format= HEAD` prints exactly `.planning/milestones/v1.20-CARVE.md`
    - `git log -1 --format=%s` mentions both `check-phase-138` and `CARVE`
    - `node scripts/validation/carve-gate.mjs` exits 0 at HEAD
    - `git status --porcelain scripts/ .github/ docs/` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>test "$(git show --name-only --format= HEAD | grep -c .)" -eq 1 && git show --name-only --format= HEAD | grep -q '^\.planning/milestones/v1\.20-CARVE\.md$' && node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>Commit 2 exists with a one-file diff, and the gate is green at HEAD.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol / artifact | Kind |
|---|---|
| `scripts/validation/check-phase-138.mjs` glob line in CARVE Category 5 | allowlist entry (new) |
| Category 5 rationale comment naming RED-06 / Phase 142 / `CHAIN_EXTRA` | allowlist comment (new) |
| Eight "Recorded scope amendments" bullets — Phase-141 D-27, D-28 back-fill plus D-35, D-20/D-21/D-22, D-24, D-11, D-25, D-27/D-28/D-29 | doc bullets (new) |
| Commit 2 of D-19's sequence | single-file commit |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| amendment commit -> `carve-gate.mjs` allowlist parse | The gate parses one fenced block from this file; a malformed fence makes the gate inert rather than loud |
| amendment commit -> Phase 144 close-gate | The "Recorded scope amendments" list is the close-gate's authorized-scope evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-142-04-E | Elevation of privilege | The allowlist itself | high | mitigate | The amendment is strictly additive and lands as its own commit before the edit it authorizes; a self-authorizing edit (allowlist entry folded into the same commit as the edit) is exactly what CARVE D-09 rules 1 and 3 exist to prevent, and Task 2's name-list assertion is the proof |
| T-142-05-T | Tampering | `carve-allowlist` fenced block parse | medium | mitigate | Run `carve-gate.mjs --self-test` after the edit; Phase 141 shipped a real defect where a CRLF fence broke the parser and made the gate silently inert on exactly the machines it governs, so a plain exit-0 from the gate alone is insufficient evidence |
| T-142-06-R | Repudiation | The "Recorded scope amendments" list | medium | mitigate | Each new bullet names its ratifying decision id; the back-fill closes a measured gap where Phase 141's amendments were absent from the list the close-gate reads |
| T-142-SC | Tampering | npm/pip/cargo installs | low | accept | No package manager runs in this plan; the phase installs nothing |
</threat_model>

<verification>
- `node scripts/validation/carve-gate.mjs --self-test` exits 0
- `node scripts/validation/carve-gate.mjs` exits 0
- `git show --name-only --format= HEAD` prints exactly one path, the CARVE file
- `grep -c "^scripts/validation/check-phase-138\.mjs$" .planning/milestones/v1.20-CARVE.md` returns 1
</verification>

<success_criteria>
- The apex is on the CARVE allowlist before any plan edits it
- The amendment commit satisfies procedure rules 1, 2 and 3 (single file, rationale, lands first)
- Phase 141's D-27/D-28 amendments are no longer missing from the list Phase 144 reads
- The gate and its self-test are both green at HEAD
</success_criteria>

<output>
Create `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-02-SUMMARY.md` when done
</output>
