---
phase: 142-archival-path-fix-chain-adoption-cold-clone-threshold
plan: 03
type: execute
wave: 3
depends_on: ["142-01", "142-02"]
files_modified:
  - scripts/validation/check-phase-30.mjs
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: true
requirements: [RED-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node scripts/validation/check-phase-30.mjs` exits 0 and prints `Summary: 12 passed, 0 failed, 1 skipped` (from the recorded pre-fix baseline of 10 passed, 2 failed, 1 skipped)"
    - "`node scripts/validation/check-phase-30.mjs --verbose` prints a V-30-01 detail carrying a NONZERO Routing-Verification row count — a zero would mean the successor passed on its other legs alone"
    - "`grep -c 'new RegExp' scripts/validation/check-phase-30.mjs` returns `1` — the V-30-02 construction is a regex literal, and the one remaining occurrence is the deferred filename regex at the top of the file, deliberately untouched"
    - "`node scripts/validation/check-phase-68.mjs` still prints `Result: 33 PASS, 0 FAIL, 0 SKIPPED` and exits 0"
    - "`node scripts/validation/carve-gate.mjs` exits 0 with `check-phase-30.mjs` on-list"
    - "`git status --porcelain docs/` prints nothing — zero corpus edits"
    - "`.planning/milestones/v1.20-GOV-02-LEDGER.md` gains one appended row for `check-phase-30.mjs` whose Hit count column reports the target-scoped path grep's real count and classifies every hit"
    - statement: "The V-30-01 successor's markdown row-count expression is scoped to the section between the Routing-Verification heading and the next heading; the live table contains no embedded pipe characters in cell content, so the naive row match is safe today but is not proven robust against a future table that does"
      verification: backstop
    - statement: "The RED-04 edge-probe row is `unclassified` in the deterministic probe; it is resolved here to the standalone exit-0 criterion plus the nonzero-row-count non-vacuity detail, which is the strongest defensible criterion available for a validator-repair requirement"
      verification: backstop
  prohibitions:
    - statement: "No file under `docs/` is edited — every failure resolves in the validator, because a later ratified decision changed the corpus deliberately in each case (D-01)"
      status: flagged-unverified
    - statement: "V-30-01's successor never asserts a distinct token count as its only structural leg — a count of the iOS-node tokens passes with the entire decision table deleted, because all four occurrences sit on two prose lines (D-04)"
      status: flagged-unverified
    - statement: "V-30-01 never asserts the generic leaf-count pattern the standard mandates; the corpus carries six incompatible locked-count shapes across 36 files and this file's actual literal is the one to assert (D-05)"
      status: flagged-unverified
    - statement: "V-30-10 gains exactly ONE leg. No second leg asserting that the platform value resolves in the enrolment contract's platform map is added — that contract runs its assertion unconditionally on all 234 enrolled files and could never fail, which would recreate the defect class this requirement exists to delete (D-08)"
      status: flagged-unverified
    - statement: "No frozen-read pin against a milestone close SHA is introduced for either assertion; a pin against an immutable corpus is permanently true by construction (D-02/D-03)"
      status: flagged-unverified
    - statement: "The filename regex near the top of this file is NOT repaired here — it is a routed deferral, outside RED-04's amended scope"
      status: flagged-unverified
  artifacts:
    - "`scripts/validation/check-phase-30.mjs` — rewritten V-30-01 and V-30-10 bodies and `name` strings, repaired V-30-02 construction, one new classifier arm at two call sites"
    - "`.planning/milestones/v1.20-GOV-02-LEDGER.md` — one appended row (Plan 03)"
  key_links:
    - "`check-phase-30.mjs` -> the 19 provenance comments in sibling validators that cite its line ranges (comments only, no `String.includes()` assertion — the structural fact that no validator pins its lines stands under the project's ratified definition of a pin)"
    - "V-30-10's line-anchored author-guidance regex -> the byte-identical regex Plan 04 applies to the L2 template, so the two validators stop diverging (D-07)"
    - "The new classifier arm -> the same arm Plan 04 adds to `check-phase-31.mjs`, identical wording at all three call sites"
---

<objective>
Make `check-phase-30.mjs` exit 0 standalone, with every fix landing in the validator and none in the
corpus.

Purpose: three assertions in this file are superseded — each names a corpus shape a later ratified
decision deliberately removed — and a fourth passes vacuously. V-30-01 counts Mermaid decision
diamonds in a file Phase 122 converted to a text table under STD-04. V-30-10 asserts a platform enum
literal Phase 40-01 broke when it added Android, one whole milestone before the decision usually
blamed for it. V-30-02's regular expression is constructed from a double-quoted string, so its
intended any-character class collapses and the captured block text has always been empty — it has
never inspected a Mermaid block since Phase 30, and it sits in the passing column where no
run-and-count method can see it.

Output: a validator that exits 0 for reasons that could actually fail.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md
@.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-RESEARCH.md
@.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-PATTERNS.md
@.planning/milestones/v1.20-CARVE.md
@scripts/validation/check-phase-30.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: GOV-02 target-scoped grep and ledger row, before any edit</name>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `.planning/milestones/v1.20-CARVE.md` GOV-02 grep procedure — target-scoped means the file PATH string, not only the symbol name, and the modelled failure is a later validator pinning a path literal
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` Discipline section and the most recent rows (the Result column is a narrative sentence naming the exact command re-run and its tally, never a bare boolean)
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` D-17, D-18
  </read_first>
  <action>
    Run BOTH grep forms and record them before touching the file.

    Target-scoped: grep recursively for the literal path string `check-phase-30.mjs` across
    `scripts/`, `.github/` and `.planning/milestones/`. Exclude `.claude/` from every grep in this
    plan — seven stale agent-worktree copies of the sibling validator live there and will
    contaminate any repo-wide count (routed deferral 12).

    Symbol-scoped: grep across `scripts/validation/` for the three check ids being rewritten and for
    the two check `name` strings about to change.

    The target-scoped grep legitimately returns roughly 19 hits — provenance comments in the
    milestone-audit harnesses citing this file's line ranges, plus a line-range citation in the
    v1.19 audit harness and one in the sibling validator. That count is what the Hit count column
    exists to hold, not a blocker. For each hit, classify it: comment, or `String.includes()`
    assertion. Under the project's ratified definition of a pin — a plain substring assertion, not a
    comment — none of these is a pin, and the structural fact that no validator pins this file's
    line ranges stands. Record that classification explicitly in the row.

    Capture the pre-edit baselines the Regression gate column needs: `node scripts/validation/
    check-phase-30.mjs` (expected `Summary: 10 passed, 2 failed, 1 skipped`, exit 1) and
    `node scripts/validation/check-phase-68.mjs` (expected `Result: 33 PASS, 0 FAIL, 0 SKIPPED`,
    exit 0).

    Append ONE row to the ledger, following the most recent rows' shape exactly: File, both grep
    commands verbatim, Hit count with the classification, Regression gate run naming the commands,
    a narrative Result sentence, and Plan `03`. Append at the end; never edit or reorder an existing
    row.
  </action>
  <acceptance_criteria>
    - `tail -1 .planning/milestones/v1.20-GOV-02-LEDGER.md` is a table row whose first cell names `check-phase-30.mjs` and whose last cell is `03`
    - That row's Grep command cell contains both a path-string grep and a symbol grep, and its Hit count cell states a number and classifies the hits as comments rather than assertions
    - The row's Regression gate cell names `check-phase-30.mjs` and `check-phase-68.mjs` with their pre-edit tallies
    - `git diff --stat scripts/` prints nothing at the end of this task — the grep precedes the edit
  </acceptance_criteria>
  <verify>
    <automated>tail -1 .planning/milestones/v1.20-GOV-02-LEDGER.md | grep -q 'check-phase-30\.mjs' && git diff --quiet scripts/ && echo "grep-before-edit discipline held"</automated>
  </verify>
  <done>One ledger row exists for `check-phase-30.mjs` with both grep forms, a classified hit count, and pre-edit baselines; no source file is modified yet.</done>
</task>

<task type="auto">
  <name>Task 2: Repair V-30-02's regular-expression construction — the thin code-layer slice</name>
  <files>scripts/validation/check-phase-30.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-30.mjs` — the V-30-02 check body (find it by its check id and its `file-absent-match` type, not by line number; line numbers in this repo drift between sessions)
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` D-35
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-PATTERNS.md` — the verbatim current V-30-02 body and its one-line fix
  </read_first>
  <action>
    Replace V-30-02's string-constructed regular expression with an equivalent regex literal carrying
    the same global flag. Constructing it from a double-quoted JS string drops the backslashes before
    the whitespace and non-whitespace escapes, so the intended any-character class compiles as a
    two-letter class and never matches; a regex literal sidesteps the double-escaping trap entirely.
    Change nothing else in the check — same id, same type, same `name`, same iOS-token match, same
    pass/fail detail strings.

    Add a short comment directly above the replaced line recording the supersession-and-defect cause
    per D-09: the construction has never captured a block, the target file has carried no Mermaid
    fence since Phase 122's conversion under STD-04, and the fix is authorized by the RED-04
    extension landed in Commit 1 (D-35).

    Note honestly, in the comment and in the plan record, what this fix does and does not buy: the
    assertion is a NEGATIVE one over the file's Mermaid blocks, and the target file currently has
    none, so it still passes trivially on today's corpus. What changes is that it can now FAIL —
    before this fix it could not fail under any corpus, which is the vacuity being deleted.

    This is the phase's thinnest code-layer slice: one line, exercised end to end through the
    standalone run, the regression guard and the governance gate before any larger rewrite lands.
  </action>
  <acceptance_criteria>
    - `node --check scripts/validation/check-phase-30.mjs` exits 0
    - `grep -c "new RegExp" scripts/validation/check-phase-30.mjs` returns `1`
    - `node scripts/validation/check-phase-30.mjs` still prints `Summary: 10 passed, 2 failed, 1 skipped` — V-30-02 was and remains a pass, so this fix must not change the tally
    - `node scripts/validation/check-phase-68.mjs` prints `Result: 33 PASS, 0 FAIL, 0 SKIPPED` and exits 0
    - `node scripts/validation/carve-gate.mjs` exits 0
    - `git status --porcelain docs/` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/validation/check-phase-30.mjs && node scripts/validation/check-phase-68.mjs && node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>V-30-02 uses a regex literal, the file still parses, the tally is unchanged, and both gates are green.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite V-30-01 and V-30-10, extend the external-tool classifier, commit</name>
  <files>scripts/validation/check-phase-30.mjs, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `scripts/validation/check-phase-30.mjs` — the V-30-01 body, the V-30-10 body, and BOTH external-tool error classifiers near the bottom of the file
    - `docs/decision-trees/07-ios-triage.md` — the locked-count line, the following prose line, the Routing Verification heading and the table beneath it (READ ONLY)
    - `docs/_templates/l1-template.md` — the author-guidance comment lines carrying the platform enum (READ ONLY)
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-CONTEXT.md` D-04, D-05, D-06, D-07, D-08, D-09, D-13
    - `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-PATTERNS.md` — the verbatim current bodies and the illustrative successor shapes
  </read_first>
  <action>
    **V-30-01.** Replace the body and the `name` string. The current check counts Mermaid decision
    diamonds; the file has been a text decision table since Phase 122. Assert three things together:
    the Routing Verification table has at least 3 data rows; each of the three iOS decision-node
    tokens is present in the file; and the file's ACTUAL locked-count literal is present — assert
    what the file says, not the generic leaf-count pattern the standard mandates, because the corpus
    carries six incompatible locked-count shapes across 36 files and this file's is the one that
    must hold (D-05). Scope the row count to the section between the Routing Verification heading
    and the next heading. Verify the row-count expression against the live table before committing
    to it — the illustrative form in PATTERNS.md is a starting point, not a locked shape, and
    markdown row counting is fragile to embedded pipe characters in cell content (none present
    today). Emit a detail string that reports all three legs with real values, so a `--verbose` run
    shows whether the row count is genuinely nonzero. Treat RESEARCH.md's illustrative successor as
    a starting point to be verified against the live file, not as a finding to be transcribed —
    per D-37, two confident adversarial findings in this phase's own review were strawmen and the
    referee overruled the adversary once on a measurement; refutations get verified as hard as
    claims.

    Record in a comment above the check, per D-09, that this supersedes the pre-conversion diamond
    count and cite the Phase-122 conversion and the version-history row in the target file that
    records it. Record explicitly, per D-04, that the rejected alternative — a distinct iOS-node
    token count — passes with the entire decision table deleted, because all four token occurrences
    sit on two prose lines and the table cells contain none. The token leg here is a supporting
    assertion; the ROW COUNT is the leg that carries the anti-vacuity weight.

    **V-30-10.** Replace the body and the `name` string. The current substring test is a bare
    whole-file `includes` of a pre-Android enum literal that Phase 40-01 (`600eabd6`) removed when
    it extended the template's platform enum with Android — record THAT as the cause (D-06), not the
    later decision usually blamed, which is the correct cause for the sibling assertion only. The
    successor is a single line-anchored regular expression over the template's author-guidance
    comment line asserting the enum including its iOS member. ONE leg only — do not add a second leg
    asserting the platform value resolves in the enrolment contract's platform map (D-08).

    Record in the same comment, honestly, that asserting on a Pandoc-stripped author-guidance comment
    is WEAKER than the pre-conversion live frontmatter affordance; do not present it as equivalent
    coverage. Use the identical regular-expression text Plan 04 will apply to the L2 template, so the
    two validators stop diverging (D-07) — a bare whole-file substring test and a line-anchored one
    have different bypass surfaces, which is how they drifted apart in the first place.

    **The external-tool classifier.** Both classifiers in this file test the same predicate. Add ONE
    new arm to EACH, matching the exact stderr text npm emits on the Linux runner class when both
    the auto-confirm and the no-install flags are passed together and the no-install flag wins. On
    this Windows tree the tool spawn fails with a file-not-found error that the existing arms already
    catch, so a green local run is NOT evidence the arm is right — that is the platform divergence
    that let RED-04 look green locally while failing on the enforcing runner class. Do not remove
    either existing flag from the spawn: dropping the no-install flag would turn a skip into a live
    registry fetch on every CI run. Do not add a nested-execution skip (D-14, withdrawn).

    Commit the source edit and the ledger row together — the ledger lives under `.planning/`, outside
    the gate's diff scope, so co-committing it does not trip the gate.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-30.mjs` exits 0 and prints `Summary: 12 passed, 0 failed, 1 skipped`
    - `node scripts/validation/check-phase-30.mjs --verbose` prints a V-30-01 detail line whose reported Routing-Verification row count is greater than or equal to 3 and is not `0`
    - `grep -c "specific platform" scripts/validation/check-phase-30.mjs` returns `1` — the line-anchored author-guidance assertion is present
    - `grep -c "600eabd6" scripts/validation/check-phase-30.mjs` returns at least `1` — the supersession cause is recorded in source (D-09)
    - `grep -c "no YES option" scripts/validation/check-phase-30.mjs` returns `2` — one new classifier arm per external-tool call site
    - `grep -c "no-install" scripts/validation/check-phase-30.mjs` returns `2` — both spawns keep the no-install flag
    - `grep -c "CHECK_PHASE_NESTED" scripts/validation/check-phase-30.mjs` returns `0`
    - `node scripts/validation/check-phase-68.mjs` prints `Result: 33 PASS, 0 FAIL, 0 SKIPPED` and exits 0
    - `node scripts/validation/carve-gate.mjs` exits 0 and reports zero off-list paths
    - `git status --porcelain docs/` prints nothing, and `git show --name-only --format= HEAD` lists only `scripts/validation/check-phase-30.mjs` and `.planning/milestones/v1.20-GOV-02-LEDGER.md`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-30.mjs && node scripts/validation/check-phase-30.mjs --verbose && node scripts/validation/check-phase-68.mjs && node scripts/validation/carve-gate.mjs && git status --porcelain docs/ | wc -l</automated>
  </verify>
  <done>`check-phase-30.mjs` exits 0 with 12 passed / 0 failed / 1 skipped, the regression guard and the gate are green, and no corpus file changed.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol / artifact | Kind |
|---|---|
| V-30-01 successor body + new `name` string ("Routing Verification table" shape) | rewritten check |
| V-30-02 regex-literal construction | repaired check |
| V-30-10 successor body + new `name` string (line-anchored author-guidance enum) | rewritten check |
| Supersession-cause comments citing `600eabd6` and the Phase-122 conversion | source comments |
| New external-tool classifier arm, 2 call sites in this file | classifier arm |
| GOV-02 ledger row, Plan `03` | ledger row |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> npm registry | The two pre-existing external-tool spawns cross this boundary; the no-install flag is what keeps them from resolving a package over the network |
| repository working tree -> frozen-surface allowlist gate | This file is a frozen surface; the gate and the GOV-02 ledger are the authorization record for editing it |
| `docs/` corpus -> validator | Read-only in this plan; the corpus is input, never output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-142-07-T | Tampering | The two external-tool spawns in this file | high | mitigate | The classifier repair adds a stderr string arm ONLY. Neither the auto-confirm flag nor the no-install flag is removed and the argv array is unchanged — removing the no-install flag would turn a skipped check into a live registry resolution on every CI run of the apex, converting a local classifier bug into a supply-chain fetch. Asserted by a grep for two surviving no-install occurrences |
| T-142-08-T | Tampering | Frozen-surface edit to `check-phase-30.mjs` | medium | mitigate | GOV-02 target-scoped path grep plus symbol grep before the edit, both recorded as a ledger row; `check-phase-68.mjs` re-run as the regression gate; `carve-gate.mjs` re-run to prove the path is on-list |
| T-142-09-D | Denial of service | The apex chain | medium | mitigate | This file is not yet a chain member, so a red here cannot red the apex; adoption is deferred to Plan 05 strictly after this plan is green (D-12) |
| T-142-10-S | Spoofing | Provenance comments citing commit SHAs | low | accept | Audit-trail comments in this repo are asserted for existence, never accuracy, and at least one existing provenance comment is provably false; the cited SHAs here were verified by `git show` during discuss-phase and are recorded as data, not as a machine-checked claim |
| T-142-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. RESEARCH.md's Package Legitimacy Audit records "not applicable" — the two external-tool call sites are pre-existing, and the only change is a string literal in an error-classification predicate, not a change to what is executed or with what arguments |
</threat_model>

<verification>
- `node scripts/validation/check-phase-30.mjs` exits 0, `Summary: 12 passed, 0 failed, 1 skipped`
- `node scripts/validation/check-phase-30.mjs --verbose` shows a nonzero Routing-Verification row count in V-30-01's detail
- `node scripts/validation/check-phase-68.mjs` exits 0, `Result: 33 PASS, 0 FAIL, 0 SKIPPED`
- `node scripts/validation/carve-gate.mjs` exits 0, zero off-list
- `git status --porcelain docs/` prints nothing
- `node scripts/validation/check-phase-138.mjs` still prints `Result: 93 PASS, 0 FAIL, 0 SKIPPED (total checks: 93)` — the apex is unchanged until Plan 05
</verification>

<success_criteria>
- RED-04 is satisfied: `check-phase-30` exits 0 standalone
- All four repaired assertions can fail on a plausible future corpus — none is permanently true by construction
- The GOV-02 grep preceded the edit and its result is a ledger row
- Zero corpus edits
</success_criteria>

<output>
Create `.planning/phases/142-archival-path-fix-chain-adoption-cold-clone-threshold/142-03-SUMMARY.md` when done
</output>
