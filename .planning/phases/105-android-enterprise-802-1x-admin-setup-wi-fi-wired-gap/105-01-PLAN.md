---
phase: 105-android-enterprise-802-1x-admin-setup-wi-fi-wired-gap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-8021x/06-android.md
autonomous: true
requirements: [DOT1X-07]

must_haves:
  truths:
    - "GOAL + SC1 + DOT1X-07: 06-android.md is created following the cloned-and-gap-degraded A3 structure (front-matter -> Prerequisites -> H1 -> Scope banner -> Common Profile Mechanics -> Wi-Fi -> Wired -> See Also -> Change History) and lets an Intune admin configure Android Enterprise Wi-Fi 802.1X across COBO/COPE/COSU/BYOD-WP for all three co-equal EAP methods (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS), no method ranked or recommended as a default"
    - "D-05: a SINGLE Enterprise Wi-Fi configuration path is documented (all AE modes share Templates > Wi-Fi) PLUS a compact mode-applicability matrix (COBO / COPE / COSU / BYOD-WP) that captures the live cross-tab field-name delta -- corporate-owned modes use field 'Radius server name'; personally-owned (BYOD-WP) uses field 'Certificate server names'; NO per-mode subsections are created"
    - "D-06: AOSP is covered by exactly ONE inline out-of-scope/stub note (plain prose, not a section, not a full mode treatment) stating AOSP is a distinct no-GMS platform sharing the same Wi-Fi path but out of scope for this guide"
    - "D-07 (matrix half): the mode-applicability matrix carries factual rows for BOTH per-mode deltas -- the BYOD-WP row cross-references the UPN-in-SAN requirement and the Device-Owner (COBO/COSU/COPE) rows reference cert-access-approval; the matrix itself carries no callout"
    - "Carried from iOS/macOS (no Android equivalent stripped): an explicit 'no authentication-mode selector on Android Enterprise' plain-prose note tells Windows-trained admins the User/Machine/User-or-machine selector is absent; Android authenticates as the current context only"
    - "Server validation is homed once in Common Profile Mechanics as a SECURITY requirement (always populate the server-name field + reference a Root certificate for server validation profile) and LINKS the rogue-RADIUS rationale to 01-eap-method-overview.md#peap-mschapv2 and ../_glossary-network.md#server-name-validation and the Android-11+/14+ server-name notes in 02-cert-delivery-foundation.md, never restating the theory; no example shows validation disabled"
    - "Identity privacy (outer identity) guidance is present for all three EAP methods (anonymous / anonymous@domain), linking ../_glossary-network.md#inner-outer-identity, to prevent cleartext identity leakage before the tunnel"
    - "Co-equal-EAP per-Wi-Fi config matrix has three columns (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS) with an Inner-method row: EAP-TLS = n/a (cert-only); PEAP = None or Microsoft CHAP Version 2 (MS-CHAP v2); EAP-TTLS = Unencrypted password (PAP) / Microsoft CHAP (MS-CHAP) / Microsoft CHAP Version 2 (MS-CHAP v2) -- NO plain CHAP for Android (the macOS/iOS 'PAP / CHAP / MS-CHAP / MS-CHAP v2' row is NOT cloned)"
    - "EAP-TLS client authentication row reads 'Certificates: SCEP, PKCS, or Derived credential'; PEAP/EAP-TTLS client auth = 'Username and Password' (EAP-TTLS may also use certificates); SCEP/PKCS cert delivery + ordering rule are LINKED to 02-cert-delivery-foundation.md, never restated (link-not-copy)"
    - "Link-not-copy: shared concepts (cert-delivery ordering rule, EKU, server-name-validation theory, rogue-RADIUS rationale, EAP-method comparison, identity-privacy theory, per-platform cert matrix) are linked to 01-/02-/_glossary-network.md and never restated"
    - "Strip-list honored: NO iOS 'Disable MAC address randomization: Yes' label, NO M-series iPad wired matrix, NO macOS deployment-channel/keychain prose, NO Windows dot3svc/enforcement-staging DANGER/TEAP/KB5014754, NO Mermaid -- none of these have an Android equivalent"
    - "Freshness: file front-matter carries the 90-day stamp (last_verified 2026-06-30, review_by 2026-09-28, platform: android, applies_to: both, audience: admin)"
  artifacts:
    - path: "docs/admin-setup-8021x/06-android.md"
      provides: "Android Enterprise Wi-Fi 802.1X admin-setup guide (all three co-equal EAP methods across COBO/COPE/COSU/BYOD-WP) plus a wired gap stub, satisfying DOT1X-07"
      contains: "## Wired"
      min_lines: 100
  key_links:
    - from: "docs/admin-setup-8021x/06-android.md"
      to: "docs/admin-setup-8021x/02-cert-delivery-foundation.md#canonical-scope-callout"
      via: "one-line scope banner link (Phase 101 D-06)"
      pattern: "02-cert-delivery-foundation\\.md#canonical-scope-callout"
    - from: "docs/admin-setup-8021x/06-android.md"
      to: "docs/admin-setup-8021x/01-eap-method-overview.md#peap-mschapv2"
      via: "server-validation rogue-RADIUS link-not-copy reference"
      pattern: "01-eap-method-overview\\.md#peap-mschapv2"
    - from: "docs/admin-setup-8021x/06-android.md"
      to: "docs/_glossary-network.md#server-name-validation"
      via: "server-validation rationale link"
      pattern: "_glossary-network\\.md#server-name-validation"
    - from: "docs/admin-setup-8021x/06-android.md"
      to: "docs/_glossary-network.md#inner-outer-identity"
      via: "identity-privacy concept link"
      pattern: "_glossary-network\\.md#inner-outer-identity"
    - from: "docs/admin-setup-8021x/06-android.md"
      to: "docs/admin-setup-8021x/02-cert-delivery-foundation.md#per-platform-cert-delivery-support-matrix"
      via: "SCEP/PKCS cert-delivery link"
      pattern: "per-platform-cert-delivery-support-matrix"
---

<objective>
Author `docs/admin-setup-8021x/06-android.md` — the Android Enterprise per-platform 802.1X admin-setup guide, the FIRST gap platform, covering Wi-Fi 802.1X Intune profiles for all three co-equal EAP methods (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS) across the Android Enterprise enrollment modes (COBO / COPE / COSU / BYOD personally-owned work profile), plus a wired gap stub — satisfying DOT1X-07 and ROADMAP Phase 105 success criteria SC1–SC3.

This plan delivers the SCAFFOLD + COMMON PROFILE MECHANICS + the WI-FI BACKBONE (navigation path, single Wi-Fi path + mode-applicability matrix, AOSP one-line stub, per-EAP config matrix). The two Wi-Fi WARNINGs (UPN-in-SAN, version-gated RADIUS), the cert-access B-08 inline block, the MAC-randomization note, the Wired gap stub, and the closeout are delivered in Task 2 of this same plan.

Purpose: Give an Intune admin a single Android Enterprise guide that walks Wi-Fi 802.1X profile creation, surfaces the Android-specific facts (the enrollment-mode field-name delta, the UPN-in-SAN BYOD deployment-failure risk, the Android 11+/14+ version gates, the "Use device MAC" NAC control, no native wired profile) and LINKS — never restates — the shared 802.1X foundation in `01-`/`02-`/`_glossary-network.md`. This guide clones the Phase-104 iOS A3 deliverable (`05-ios.md`) as its scaffold and applies the Android deltas, stripping all iOS-only (MAC label, M-series wired matrix), macOS-only (deployment channel/keychain), and Windows-only (dot3svc, DANGER, TEAP, KB5014754) mechanics entirely.

Output: `docs/admin-setup-8021x/06-android.md` (CREATE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-android-enterprise-802-1x-admin-setup-wi-fi-wired-gap/105-CONTEXT.md
@.planning/phases/105-android-enterprise-802-1x-admin-setup-wi-fi-wired-gap/105-RESEARCH.md
@.planning/phases/105-android-enterprise-802-1x-admin-setup-wi-fi-wired-gap/105-PATTERNS.md
@docs/admin-setup-8021x/05-ios.md
@docs/admin-setup-8021x/04-macos.md
@docs/admin-setup-8021x/01-eap-method-overview.md
@docs/admin-setup-8021x/02-cert-delivery-foundation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold + Common Profile Mechanics + Wi-Fi backbone (nav path, mode-applicability matrix, AOSP stub, per-EAP matrix)</name>
  <files>docs/admin-setup-8021x/06-android.md</files>
  <read_first>
    - docs/admin-setup-8021x/06-android.md — the file being created (does not exist yet)
    - docs/admin-setup-8021x/05-ios.md — the NEAREST clone scaffold (Phase 104 deliverable): front-matter (lines 1-7), Prerequisites blockquote (9-10), H1 + Scope banner (12-14), `## Common Profile Mechanics` opener + three-profiles prose (16-24), no-auth-mode note (26), `### Server Validation` (28-34), `### Anonymous Outer Identity` (36-44), Wi-Fi `### In Intune admin center` + nav path + co-equal preamble (48-54), Wi-Fi per-EAP matrix (64-73). Do NOT clone the iOS MAC-randomization note (56-62) here — that is Task 2 and uses a different Android control name. Do NOT clone the iOS Wired matrix (99-136)
    - docs/admin-setup-8021x/04-macos.md — secondary analog for the per-EAP Wi-Fi matrix with Inner-method row (lines 66-76); and the compact table-within-Wi-Fi-section format (Network Interface table lines 106-119) whose FORMAT (not content) the mode-applicability matrix borrows
    - 105-RESEARCH.md — Pattern 1 (mode-applicability matrix, lines 204-217), Pattern 2 (per-EAP Wi-Fi config matrix + EAP-TTLS/PEAP inner-method precision, lines 219-237), Pattern 8 (AOSP one-line stub, lines 334-345), Live Verification Summary (lines 441-459: field-name delta, EAP-TTLS no-plain-CHAP, PEAP inner None/MS-CHAPv2)
    - 105-PATTERNS.md — Element 1 front-matter (platform: android), Element 2 Prerequisites verbatim, Element 3 H1 title delta (drop "and Wired"), Element 4 Scope banner verbatim, Element 5 Common Profile Mechanics adaptation (remove iOS wired parenthetical + .mobileconfig; no-auth-mode note; Server Validation link-not-copy; Anonymous Outer Identity), Element 6 Wi-Fi nav-path delta, Element 7 mode-applicability matrix (lines 166-194), Element 8 AOSP stub (lines 198-212), Element 9 per-EAP matrix + Android delta table (lines 216-243), Shared Patterns (anchor-slug double-hyphen trap, link-not-copy, co-equal-EAP boilerplate), Critical Android-Specific Deltas table (lines 592-603)
    - docs/admin-setup-8021x/02-cert-delivery-foundation.md — confirm the live `#canonical-scope-callout` and `#per-platform-cert-delivery-support-matrix` anchor slugs before linking; lines 100-106 carry the Android-11+/14+ server-name notes (link target for Server Validation)
    - docs/admin-setup-8021x/01-eap-method-overview.md — confirm the live `#peap-mschapv2` anchor slug; link target for the rogue-RADIUS rationale
    - docs/_glossary-network.md — confirm the live `#server-name-validation` and `#inner-outer-identity` anchor slugs before linking
  </read_first>
  <action>
    Create `docs/admin-setup-8021x/06-android.md`. Start with the 6-key YAML front matter cloned from `05-ios.md` lines 1-7 with `platform: android` (all other keys identical: `last_verified: 2026-06-30`, `review_by: 2026-09-28`, `applies_to: both`, `audience: admin`). Follow with the Prerequisites blockquote (copy verbatim from `05-ios.md` lines 9-10, linking `01-eap-method-overview.md` and `02-cert-delivery-foundation.md`), then the H1 `# Android Enterprise 802.1X Admin Setup: Wi-Fi` (D-01/Element 3: deliberately drop "and Wired" — the guide is Wi-Fi-primary; Wired is a gap stub), then the one-line Scope banner copied verbatim from `05-ios.md` line 14 linking `02-cert-delivery-foundation.md#canonical-scope-callout`.

    Author a `## Common Profile Mechanics` section, ordered:
    1. Three-separate-profiles STRUCTURAL PROSE (no `> **Label:**` callout, no Mermaid): Android Enterprise 802.1X requires three distinct Intune profiles — one Trusted Certificate profile (RADIUS CA root, for server validation), one SCEP or PKCS client-certificate profile (device identity), and one Wi-Fi network profile referencing the two. Remove the iOS "wired requires SCEP only" parenthetical and the `.mobileconfig` sentence — Android has no wired profile at all and Step 3 references only the Wi-Fi profile type. LINK the cert-delivery ordering rule / EKU / per-platform cert options to `02-cert-delivery-foundation.md` (never restate inline — link-not-copy).
    2. A "No authentication-mode selector on Android Enterprise" plain-prose note (NOT a callout): Android Enterprise does not expose a User / Machine / User-or-machine authentication mode selector in Intune profiles; Windows-trained admins will not find it; Android authenticates as the current context only.
    3. A `### Server Validation` subsection homed once for all three EAP methods: frame populating the server-name field + referencing a **Root certificate for server validation** profile as a SECURITY requirement; LINK the rogue-RADIUS / credential-harvest rationale to `01-eap-method-overview.md#peap-mschapv2` and `../_glossary-network.md#server-name-validation`, and LINK the Android-11+/14+ server-name behavior to `02-cert-delivery-foundation.md` (lines 100-106) rather than restating per-OS behavior; end with "No example in this guide shows server validation disabled." (The combined Android 11+/14+ version-gate WARNING itself is authored in Task 2 — here only link the foundation overview.)
    4. A `### Anonymous Outer Identity (Identity Privacy)` subsection cloned from `05-ios.md` lines 36-44 with "iOS/iPadOS" changed to "Android Enterprise" in the opener; field label **Identity privacy (outer identity)**; the three-EAP-method breakdown (EAP-TLS cert subject, PEAP username, EAP-TTLS username) and realm-routing suffix note apply verbatim; link `../_glossary-network.md#inner-outer-identity`.

    Author a `## Wi-Fi` section opening with a `### In Intune admin center` header whose bold navigation path is **Devices** > **Configuration** > **New policy** > **Android Enterprise** > **Templates** > **Wi-Fi**, noting selection of **Wi-Fi type: Enterprise** to access EAP settings, with the co-equal-EAP preamble (clone verbatim from `05-ios.md` lines 54-55: "All three EAP methods are co-equal -- no method is ranked or recommended as a default. For when to choose each method, see [01-eap-method-overview.md](01-eap-method-overview.md).").

    Add the enrollment-mode note + mode-applicability matrix (D-05/D-07-matrix-half/Element 7): a bold lead-in sentence stating all Android Enterprise enrollment modes use the SAME Templates > Wi-Fi path and the matrix documents the per-tab UI field-name delta. The four-row matrix (columns: Enrollment mode | Tab in Intune | Server name field | Key delta) per RESEARCH Pattern 1: Fully managed (COBO) = Corporate-owned / **Radius server name** / Android 11+/14+ version gates apply, cert-access approval needed; Corporate-owned Work Profile (COPE) = Corporate-owned / **Radius server name** / same; Dedicated (COSU) = Corporate-owned / **Radius server name** / same; Personally owned Work Profile (BYOD-WP) = Personally owned / **Certificate server names** / UPN-in-SAN required (profile deployment fails if absent). In the BYOD-WP row's delta cell, cross-reference the UPN-in-SAN WARNING via a `[link](#wi-fi)`-style pointer (the WARNING is homed later in Task 2 within this same Wi-Fi section — cross-ref TO it, do not duplicate it).

    Add the AOSP one-line out-of-scope stub note (D-06/Element 8) as a single inline sentence of plain prose (NOT a `> **NOTE:**` callout, NOT a section) after the mode matrix: AOSP (Android Open Source Project) devices are a distinct no-GMS platform that share the same Intune Wi-Fi profile path but are out of scope for this guide; cert-delivery options differ (no PKCS Imported). Do not give AOSP a full mode treatment.

    Add the per-EAP-method Wi-Fi config matrix (settings as rows; three columns EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS; minimal `|---|---|---|---|` separator) per RESEARCH Pattern 2, with the MANDATORY Android deltas (Element 9 delta table): EAP type field value (EAP-TLS / PEAP / EAP-TTLS); Server name field row noting the per-tab split (corporate-owned = `Radius server name`; BYOD-WP = `Certificate server names`); Root certificate for server validation = Trusted Certificate profile reference (all three); Client authentication (EAP-TLS = `Certificates: SCEP, PKCS, or Derived credential`; PEAP = `Username and Password`; EAP-TTLS = `Username and Password, or Certificates (SCEP / PKCS)`); Inner method (EAP-TLS = `-- (cert-only; no inner method)`; PEAP = `None, or Microsoft CHAP Version 2 (MS-CHAP v2)`; EAP-TTLS = `Unencrypted password (PAP) / Microsoft CHAP (MS-CHAP) / Microsoft CHAP Version 2 (MS-CHAP v2)` — NO plain CHAP); Identity privacy (outer identity) = `anonymous` or `anonymous@domain` (all three). Keep all three methods co-equal — no "recommended"/"default" language. After the matrix, LINK SCEP/PKCS cert-profile configuration + the deployment ordering rule to `02-cert-delivery-foundation.md` and the `02-cert-delivery-foundation.md#per-platform-cert-delivery-support-matrix`, never restating.

    Use `--` (double hyphen) for em-dashes throughout. Do NOT introduce the MAC note, the UPN-in-SAN WARNING, the version-gate WARNING, the B-08 cert-access block, or the `## Wired` section yet (Task 2). Do NOT introduce any iOS MAC label, M-series wired content, macOS deployment-channel content, Windows mechanics (dot3svc/DANGER/TEAP/KB5014754), or any Mermaid diagram.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q "platform: android" docs/admin-setup-8021x/06-android.md && grep -q "review_by: 2026-09-28" docs/admin-setup-8021x/06-android.md && grep -q "# Android Enterprise 802.1X Admin Setup: Wi-Fi" docs/admin-setup-8021x/06-android.md && grep -q "## Common Profile Mechanics" docs/admin-setup-8021x/06-android.md && grep -q "## Wi-Fi" docs/admin-setup-8021x/06-android.md && grep -q "canonical-scope-callout" docs/admin-setup-8021x/06-android.md && grep -q "01-eap-method-overview.md#peap-mschapv2" docs/admin-setup-8021x/06-android.md && grep -q "_glossary-network.md#server-name-validation" docs/admin-setup-8021x/06-android.md && grep -q "_glossary-network.md#inner-outer-identity" docs/admin-setup-8021x/06-android.md && grep -q "Radius server name" docs/admin-setup-8021x/06-android.md && grep -q "Certificate server names" docs/admin-setup-8021x/06-android.md && grep -qi "AOSP" docs/admin-setup-8021x/06-android.md && grep -q "Unencrypted password (PAP)" docs/admin-setup-8021x/06-android.md && ! grep -qi "Disable MAC address randomization\|M-series\|deployment channel\|dot3svc\|KB5014754\|mermaid" docs/admin-setup-8021x/06-android.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `06-android.md` exists with front-matter `platform: android`, `applies_to: both`, `audience: admin`, `last_verified: 2026-06-30`, `review_by: 2026-09-28`
    - A Prerequisites blockquote links `01-eap-method-overview.md` and `02-cert-delivery-foundation.md`; the one-line Scope banner links `02-cert-delivery-foundation.md#canonical-scope-callout`
    - The H1 is exactly `# Android Enterprise 802.1X Admin Setup: Wi-Fi` (no "and Wired" — D-01)
    - `## Common Profile Mechanics` contains three-separate-profiles prose (Trusted Cert + SCEP/PKCS client cert + Wi-Fi network profile) with NO callout and NO Mermaid; the iOS "wired requires SCEP only" parenthetical and `.mobileconfig` sentence are absent; the cert-ordering rule is linked to `02-cert-delivery-foundation.md`, not restated
    - A "no authentication-mode selector on Android Enterprise" plain-prose note is present (User/Machine/User-or-machine selector absent), not a callout
    - A `### Server Validation` subsection frames validation as a security requirement, links `01-eap-method-overview.md#peap-mschapv2` and `../_glossary-network.md#server-name-validation`, links the Android 11+/14+ overview in `02-`, and states no example shows validation disabled
    - A `### Anonymous Outer Identity (Identity Privacy)` subsection uses the label `Identity privacy (outer identity)`, covers all three EAP methods, and links `../_glossary-network.md#inner-outer-identity`
    - `## Wi-Fi` has a `### In Intune admin center` nav path containing `Android Enterprise`, `Templates`, and `Wi-Fi`, plus the co-equal-EAP preamble
    - A mode-applicability matrix has four rows (COBO, COPE, COSU, BYOD-WP) showing the field-name delta (`Radius server name` for corporate-owned; `Certificate server names` for BYOD-WP) and a BYOD-WP-row cross-reference pointer to the (Task-2) UPN-in-SAN WARNING; NO per-mode subsections exist
    - An AOSP one-line out-of-scope stub note is present as inline plain prose (not a callout, not a section)
    - The per-EAP Wi-Fi matrix has three columns (`EAP-TLS`, `PEAP-MSCHAPv2`, `EAP-TTLS`); the EAP-TTLS Inner-method cell reads `Unencrypted password (PAP) / Microsoft CHAP (MS-CHAP) / Microsoft CHAP Version 2 (MS-CHAP v2)` and contains NO standalone plain `CHAP` option; the PEAP Inner-method cell reads `None, or Microsoft CHAP Version 2 (MS-CHAP v2)`; the EAP-TLS client-auth cell includes `SCEP, PKCS, or Derived credential`; no "recommended"/"default" ranking
    - The forbidden tokens `Disable MAC address randomization`, `M-series`, `deployment channel`, `dot3svc`, `KB5014754`, and any Mermaid block do NOT appear
    - Cross-references to `01-`/`02-`/`_glossary-network.md` are present; shared concepts are linked, not restated
  </acceptance_criteria>
  <done>The scaffold, Common Profile Mechanics, and the Wi-Fi backbone (nav path, single-path mode-applicability matrix, AOSP stub, per-EAP matrix) are authored; the SC1 co-equal-EAP Wi-Fi configuration surface and the D-05/D-06/D-07-matrix-half decisions are in place, ready for the WARNINGs and Wired stub in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: UPN-in-SAN WARNING + cert-access B-08 inline + combined version-gate WARNING + MAC note + Wired gap stub + closeout</name>
  <files>docs/admin-setup-8021x/06-android.md</files>
  <read_first>
    - docs/admin-setup-8021x/06-android.md — the file created in Task 1 (append the remaining Wi-Fi elements, the Wired stub, and the closeout; keep front matter, Common Mechanics, and the Wi-Fi backbone intact)
    - docs/admin-setup-8021x/05-ios.md — analogs for FORM: B-05 "What breaks" WARNING blockquote (lines 75-88) is the form template for the B-06 UPN-in-SAN WARNING; the single-callout-plus-cross-ref pattern (WARNING homed at 75, cross-ref'd from 134); the MAC-randomization plain-prose note FORM (56-62, including the inline freshness stamp at 62); `## Wired` H2 (99); `## See Also` (140-144); `## Change History` (148-152). Do NOT clone iOS content values (MAC label, M-series wired, SCEP-only NOTE)
    - docs/admin-setup-8021x/04-macos.md — analog for the WARNING-with-embedded-table form: the deployment-channel WARNING (lines 20-35) shows how to embed a mini-table inside a `> **WARNING --` blockquote — the version-gate WARNING uses this form (content is entirely Android)
    - 105-RESEARCH.md — Pattern 3 (B-06 UPN-in-SAN WARNING required facts + live-verified text, lines 239-256), Pattern 4 (combined version-gate WARNING + scope note, lines 258-275), Pattern 5 (MAC randomization plain-prose note, lines 277-295), Pattern 6 (Wired gap stub draft + constraints, lines 297-312), Pattern 7 (B-08 cert-access inline, lines 314-332); Common Pitfalls 1-7 (lines 396-437)
    - 105-PATTERNS.md — Element 10 B-06 WARNING + cross-ref discipline (lines 247-287), Element 11 B-08 structural/inline (lines 291-306), Element 12 combined version-gate WARNING + freshness stamp + scope constraint (lines 310-340), Element 13 MAC note Android delta table (lines 344-375), Element 14 Wired gap stub + hard constraints (lines 379-412), Element 15 See Also delta (lines 416-428), Element 16 Change History delta (lines 432-444); Shared Patterns (callout convention, freshness stamp format, anchor-slug trap)
    - 105-CONTEXT.md — locked decisions D-07 (cert-access inline), D-08/D-09/D-10 (UPN-in-SAN tier/home/BYOD-scope), D-11/D-12/D-14 (combined version-gate), D-13 (MAC), D-01/D-02/D-03/D-04 (Wired stub); the live-verified BYOD-only UPN-in-SAN ground truth (lines 119-120)
  </read_first>
  <action>
    Continue `docs/admin-setup-8021x/06-android.md` within the `## Wi-Fi` section (after the Task-1 per-EAP matrix). Author, in this order:

    1. The B-06 UPN-in-SAN "What breaks" WARNING (D-08/D-09/D-10/Element 10), homed HERE in the Wi-Fi/BYOD-WP certificate-authentication context as a single `> **WARNING -- <label>**` blockquote (WARNING tier, NOT DANGER). It MUST convey, per RESEARCH Pattern 3: applies to the BYOD personally-owned work profile ONLY (explicitly NOT COBO/COPE/COSU); covers both user AND device certificates within that work-profile context; symptom = the Wi-Fi profile shows "Error" in Intune and profile DEPLOYMENT fails (not merely authentication); cause = the UPN is absent from the certificate Subject Alternative Name (SAN); fix = add the User Principal Name (UPN) as a SAN attribute in the Android Enterprise SCEP profile. Do NOT mention Fully Managed / COBO / COPE / COSU as also requiring UPN-in-SAN (Pitfall 1 — PITFALLS B-06's "all enrollment types" is the inaccurate outlier; the live MS Learn personally-owned tab is dispositive). This is the SINGLE home; the mode-matrix BYOD-WP row (Task 1) and any SCEP-cert pointer cross-reference TO this WARNING with a link — they do not duplicate it (Pitfall 2). The UPN-in-SAN WARNING does not need its own inline freshness stamp (it inherits the file-level front-matter stamp).

    2. The B-08 Device-Owner cert-access block (D-07/Element 11) as bold-lead PLAIN PROSE (NOT a `> **NOTE:**`/`> **WARNING:**` callout — Pitfall): for Android Enterprise Device Owner enrollment types (COBO, COPE, COSU), set the SCEP profile **Certificate access** to **Grant silently for specific apps** and include the Wi-Fi supplicant in the allowed-apps list; the default ("Require user approval for all apps") is silently rejected on fully managed devices where no user interaction is available, causing 802.1X auth to fail even when the cert profile shows "Succeeded". Note that for Device Owner profiles, Intune certificate reporting is unavailable and Intune cannot revoke certificates delivered via this profile type.

    3. The combined version-gated RADIUS WARNING (D-11/D-12/D-14/Element 12) as ONE `> **WARNING --` blockquote with an embedded mini version-matrix (two rows: Android 11+ and Android 14+) per RESEARCH Pattern 4. Android 11+ row: new Wi-Fi profiles might require the Radius server name field to be configured, or devices may not connect. Android 14+ row: total combined length of all configured RADIUS server names must be 256 characters or fewer, special characters not permitted, profiles that worked on Android 11–13 may silently fail on Android 14 — and the DNS-suffix-not-FQDN-list mitigation lives INSIDE the Android-14+ line (D-14: use the DNS suffix, e.g. `contoso.com`, instead of a list of full FQDNs to stay within 256 chars). Close the blockquote with the inline freshness stamp `> *last_verified: 2026-06-30 · review_by: 2026-09-28*`. Scope the WARNING to the `Radius server name` field in the corporate-owned/AOSP tabs (Pitfall 3: the personally-owned `Certificate server names` field does not carry these sub-bullets); note that general server-name validation applies to all modes via the link to `02-`.

    4. The MAC randomization plain-prose note (D-13/Element 13) as bold-lead PLAIN PROSE (NOT a callout — Pitfall 5) per RESEARCH Pattern 5: bold lead `**MAC address randomization (Android 13+):**`; for NAC environments keyed to device MAC, select **Use device MAC** in the Wi-Fi profile; state the full three-option selector verbatim — **Use device default / Use randomized MAC / Use device MAC**; explain that without "Use device MAC", NAC sees a different randomized MAC per network and RADIUS rejects devices not in the allow list. Use Android's real control name "Use device MAC" — do NOT use iOS's "Disable MAC address randomization: Yes" (Pitfall 7). Carry an inline freshness stamp `*last_verified: 2026-06-30 · review_by: 2026-09-28*`. Omit any "wired connections are unaffected" sentence (Android wired is a gap stub).

    5. The `## Wired` gap stub (D-01/D-02/D-03/D-04/Element 14) as a top-level H2 followed by plain prose ONLY, per RESEARCH Pattern 6. Lead with a bold-lead-in first sentence: **Android Enterprise has no native Intune wired-network profile type.** State exactly the four D-04 facts: (1) no native Intune wired profile type exists for Android Enterprise; (2) no documented OMA-URI workaround; (3) organizations needing wired 802.1X should consult their network/infrastructure team for switch-side configuration outside Intune; (4) Android Enterprise Wi-Fi 802.1X IS fully supported — link back to the [Wi-Fi section](#wi-fi). HARD CAP: one paragraph (two short paragraphs maximum, ~3–6 sentences). NO `> **Label:**` callout (Pitfall 4), NO `###` sub-heading, NO table/matrix, NO per-EAP rows. Bold lead-in on the first sentence only.

    6. Close with `## See Also` (Element 15) linking `01-eap-method-overview.md`, `02-cert-delivery-foundation.md`, and `../_glossary-network.md` (clone the `05-ios.md` 140-144 structure; replace the iOS-wired-PKCS parenthetical with an Android qualifier, e.g. "incl. Android Enterprise BYOD UPN-in-SAN requirement"); and a `## Change History` table (Element 16) with one row dated 2026-06-30 summarizing: "Initial version -- Android Enterprise 802.1X admin setup: Wi-Fi profiles for EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS across COBO/COPE/COSU/BYOD-WP modes; UPN-in-SAN deployment-failure WARNING (BYOD-WP); version-gated RADIUS server-name WARNING (Android 11+/14+); MAC randomization note (Android 13+); wired gap stub (no native Intune wired profile)", Author `--`.

    Confirm the file contains exactly TWO blockquote callouts — the B-06 UPN-in-SAN WARNING and the combined version-gate WARNING (the B-08 cert-access block, the MAC note, and the Wired stub are plain prose, NOT callouts). No DANGER callout anywhere. Use `--` (double hyphen) for em-dashes.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q "## Wired" docs/admin-setup-8021x/06-android.md && grep -q "Android Enterprise has no native Intune wired-network profile type" docs/admin-setup-8021x/06-android.md && grep -qi "no.*OMA-URI" docs/admin-setup-8021x/06-android.md && grep -q "Use device MAC" docs/admin-setup-8021x/06-android.md && grep -q "Use device default" docs/admin-setup-8021x/06-android.md && grep -q "Grant silently for specific apps" docs/admin-setup-8021x/06-android.md && grep -qi "Android 11" docs/admin-setup-8021x/06-android.md && grep -qi "Android 14" docs/admin-setup-8021x/06-android.md && grep -qi "256 characters" docs/admin-setup-8021x/06-android.md && grep -qi "DNS suffix" docs/admin-setup-8021x/06-android.md && grep -qi "Subject Alternative Name" docs/admin-setup-8021x/06-android.md && grep -qi "deployment fails" docs/admin-setup-8021x/06-android.md && grep -c "review_by: 2026-09-28" docs/admin-setup-8021x/06-android.md | grep -qv '^0$' && [ "$(grep -c '^> \*\*WARNING' docs/admin-setup-8021x/06-android.md)" = "2" ] && ! grep -qi "Disable MAC address randomization\|DANGER\|> \*\*NOTE" docs/admin-setup-8021x/06-android.md && grep -q "## See Also" docs/admin-setup-8021x/06-android.md && grep -q "## Change History" docs/admin-setup-8021x/06-android.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - A single `> **WARNING` "What breaks" blockquote (WARNING tier, not DANGER) is homed in the Wi-Fi section conveying: BYOD personally-owned work profile ONLY; user AND device certificates; symptom = Wi-Fi profile shows "Error" / deployment fails (not just auth); cause = UPN absent from cert SAN; fix = add UPN to the SCEP SAN attribute (D-08/D-09/D-10)
    - The UPN-in-SAN WARNING does NOT mention Fully Managed / COBO / COPE / COSU as also requiring UPN-in-SAN (Pitfall 1); it appears exactly ONCE (no second UPN-in-SAN WARNING block — Pitfall 2); the BYOD-WP mode-matrix row cross-references it via a link
    - A Device-Owner cert-access (B-08) block is present as bold-lead PLAIN PROSE (not a callout), naming `Grant silently for specific apps`, the Wi-Fi supplicant, the silent-rejection failure mode, and the Device-Owner cert reporting/revocation limitation (D-07)
    - A single combined version-gate `> **WARNING` blockquote with an embedded Android 11+ / Android 14+ mini-matrix is present, stating Android 11+ may require the Radius server name field, Android 14+ caps total server-name length at 256 characters / no special characters / silent failure on 14, with the DNS-suffix mitigation INSIDE the Android-14+ line, scoped to the `Radius server name` field, carrying an inline `last_verified: 2026-06-30 · review_by: 2026-09-28` stamp (D-11/D-12/D-14)
    - A MAC-randomization PLAIN-PROSE note (not a callout) states `Use device MAC` for NAC, the full `Use device default / Use randomized MAC / Use device MAC` selector, the Android 13+ gate, and carries an inline freshness stamp; the string `Disable MAC address randomization` does NOT appear (D-13)
    - A `## Wired` section is plain prose only (no callout, no sub-heading, no table), leads with the bold sentence `**Android Enterprise has no native Intune wired-network profile type.**`, states all four D-04 facts (no native profile, no OMA-URI workaround, consult network team, Wi-Fi IS supported with a link back to `#wi-fi`), and is at most two short paragraphs (D-01/D-02/D-03/D-04)
    - `## See Also` links `01-`, `02-`, and `../_glossary-network.md`; a `## Change History` table dated 2026-06-30 with the Android initial-version row is present
    - The file contains exactly TWO `> **WARNING` blockquote callouts and ZERO `> **NOTE` callouts and ZERO DANGER callouts; the B-08, MAC, and Wired content remain plain prose
  </acceptance_criteria>
  <done>The two Wi-Fi WARNINGs (UPN-in-SAN, combined version-gate), the B-08 cert-access inline block, the MAC-randomization note, the Wired gap stub, and the closeout are authored; SC1 (UPN-in-SAN prominently documented), SC2 (Android 11+/14+ freshness-stamped), and SC3 (no native wired profile + gap explanation) are satisfied; D-01 through D-14 are fully in place. DOT1X-07 is fully delivered in `06-android.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supplicant (managed Android Enterprise device) → authenticator (switch/AP) → RADIUS/NPS | 802.1X credential exchange; the guide's configuration guidance determines whether this boundary is hardened (server validation, UPN-in-SAN cert correctness, MS-CHAPv2 inner auth, "Use device MAC" for NAC) |
| authoring artifact | `06-android.md` is a static Markdown documentation file — no executable surface, no input parsing, no runtime, no scripts, no code |

This is a pure-documentation phase. There are NO high-severity code/runtime threats — no code, no runtime, no user input, no data handling. The only realistic threat surface is documentation-integrity: publishing inaccurate or stale guidance. Version-gated Android facts (11+/14+, MAC 13+) drift; mitigated by 90-day freshness stamps and plan-time live-MS-Learn re-verification (RESEARCH confirms ZERO drift as of 2026-06-30). Mis-scoping the UPN-in-SAN failure to corporate-owned modes (the PITFALLS B-06 outlier) would mislead admins; mitigated by the locked D-10 BYOD-only scope encoded in Task 2 acceptance.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | Spoofing | Rogue RADIUS / credential harvest if server-validation guidance were wrong | mitigate | Common Mechanics homes server validation as a security requirement, shows NO disabled-validation example, links rationale to 01-/02-/glossary (Task 1 acceptance) |
| T-105-02 | Information Disclosure | Cleartext identity leakage before tunnel | mitigate | Identity privacy (outer identity) guidance for all three EAP methods (Task 1 acceptance) |
| T-105-03 | Denial of Service | Wi-Fi profile DEPLOYMENT fails for BYOD-WP if UPN absent from cert SAN | mitigate | B-06 "What breaks" WARNING homed in the Wi-Fi/BYOD-WP cert-auth context with the fix (add UPN to SCEP SAN), BYOD-scoped per D-10 (Task 2 acceptance) |
| T-105-04 | Denial of Service | Silent connection failure on Android 14+ when RADIUS server names exceed 256 chars / use special chars | mitigate | Combined version-gate WARNING with the DNS-suffix mitigation, freshness-stamped (Task 2 acceptance) |
| T-105-05 | Elevation of Privilege | Randomized MAC bypasses MAC-keyed NAC allow-list | mitigate | MAC-randomization prose note: "Use device MAC" for NAC (Android 13+) (Task 2 acceptance) |
| T-105-06 | Tampering (doc staleness) | Android version-gated facts drift between releases | mitigate | 90-day front-matter + inline freshness stamps; RESEARCH live-verified ZERO drift 2026-06-30 |
| T-105-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no shipped scripts in this phase; documentation-only file creation — no supply-chain surface |

Block-on severity: high. No high-severity code/runtime threats exist for this docs phase. The content-integrity obligations T-105-01..06 are encoded as task acceptance criteria; absence of T-105-01 (server-validation) or T-105-03 (UPN-in-SAN BYOD scope) is a blocking content gap.
</threat_model>

<verification>
- `06-android.md` exists and follows the gap-degraded A3 structure: Common Profile Mechanics → Wi-Fi → Wired → See Also → Change History.
- Single Wi-Fi path + compact mode-applicability matrix (COBO/COPE/COSU/BYOD-WP) capturing the `Radius server name` vs `Certificate server names` delta; no per-mode subsections (D-05).
- AOSP is one inline out-of-scope stub note, not a section (D-06).
- The per-EAP Wi-Fi matrix has three co-equal columns with an Inner-method row; EAP-TTLS lists PAP / MS-CHAP / MS-CHAPv2 with NO plain CHAP; PEAP = None / MS-CHAPv2 (no recommended-default ranking).
- The UPN-in-SAN "What breaks" WARNING is homed once in the Wi-Fi/BYOD-WP context, BYOD-scoped (not generalized to corporate-owned), cross-referenced from the mode matrix (D-08/D-09/D-10).
- B-08 Device-Owner cert-access is structural/inline plain prose, NOT a callout (D-07).
- The combined version-gate WARNING covers Android 11+/14+ in one callout with the DNS-suffix mitigation inside the 14+ line and a 90-day freshness stamp (D-11/D-12/D-14).
- The MAC-randomization note is plain prose using "Use device MAC" (Android 13+), freshness-stamped, NOT a callout, and never uses the iOS label (D-13).
- The `## Wired` section is a plain-prose gap stub (max two short paragraphs, no callout/table/sub-heading) stating the four facts (D-01/D-02/D-03/D-04).
- iOS-only (MAC label, M-series wired), macOS-only (deployment channel/keychain), and Windows-only (dot3svc/DANGER/TEAP/KB5014754) mechanics are entirely absent; exactly two blockquote callouts (both WARNING).
- File-level 90-day freshness stamp present; the version-gate WARNING and the MAC note each carry an additional inline stamp.
</verification>

<success_criteria>
- DOT1X-07 fully delivered by `docs/admin-setup-8021x/06-android.md`.
- SC1: Android Enterprise Wi-Fi 802.1X across COBO/COPE/COSU/BYOD-WP for all three co-equal EAP methods, with the UPN-in-SAN requirement prominently documented (BYOD-WP, deployment fails if absent) — present.
- SC2: Android 11+ RADIUS server-name requirement and Android 14+ 256-char / no-special-chars limit documented with `last_verified`/`review_by` freshness stamps — present.
- SC3: the wired section explicitly states no native Intune wired-network profile type exists for Android Enterprise and documents the gap with a brief explanation of alternatives — present.
- The content-integrity obligations (T-105-01 server validation; T-105-03 UPN-in-SAN BYOD scope) are encoded and verifiable.
</success_criteria>

<output>
Create `.planning/phases/105-android-enterprise-802-1x-admin-setup-wi-fi-wired-gap/105-01-SUMMARY.md` when done.
</output>
