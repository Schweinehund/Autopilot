---
phase: 90-mdm-migration-walkthrough-l2-runbook-30
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l2-runbooks/30-macos-mdm-migration-failure.md
autonomous: true
requirements: [RUN-01]
must_haves:
  truths:
    - "An L2 engineer can open 30-macos-mdm-migration-failure.md and route from a Context preamble (no router table) to one of three parallel tracks"
    - "An L2 engineer can investigate a deadline-lockout device via Track A including conceptual ABM admin recovery"
    - "An L2 engineer can investigate profile-not-delivered / enrollment-failed via Track B including the leftover Kandji/Iru agent diagnostic"
    - "An L2 engineer with a stuck PSSO re-registration is routed by Track C link-not-copy to L2 #27 without inline duplication"
    - "An L2 engineer sees the macOS log-collection prerequisite cross-linked to L2 #10 before investigation"
  artifacts:
    - path: "docs/l2-runbooks/30-macos-mdm-migration-failure.md"
      provides: "L2 migration-failure investigation runbook (three tracks A/B/C)"
      contains: "## Track A"
      min_lines: 150
  key_links:
    - from: "docs/l2-runbooks/30-macos-mdm-migration-failure.md"
      to: "docs/l2-runbooks/10-macos-log-collection.md"
      via: "log-collection prerequisite cross-link in Context preamble"
      pattern: "10-macos-log-collection\\.md"
    - from: "docs/l2-runbooks/30-macos-mdm-migration-failure.md"
      to: "docs/l2-runbooks/27-macos-sso-investigation.md"
      via: "Track C link-not-copy to PSSO registration investigation"
      pattern: "27-macos-sso-investigation\\.md"
---

<objective>
Author the new L2 investigation runbook `docs/l2-runbooks/30-macos-mdm-migration-failure.md` as a true sibling of `27-macos-sso-investigation.md`: a `## Context` preamble with in-preamble routing prose (no router table), three parallel tracks (Track A deadline lockout + ABM admin recovery; Track B profile-not-delivered / enrollment-failed with leftover source-MDM agent diagnostic; Track C PSSO re-registration stuck, link-not-copy to L2 #27), the macOS log-collection prerequisite cross-link to L2 #10, a Microsoft Support Escalation Packet, Related Resources, and a three-column Version History.

Purpose: An L2 engineer has a structured investigation runbook for migration-specific failures (RUN-01).
Output: `docs/l2-runbooks/30-macos-mdm-migration-failure.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-CONTEXT.md
@.planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-RESEARCH.md
@.planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 30 + Context preamble (in-preamble routing) + Track A and Track B</name>
  <files>docs/l2-runbooks/30-macos-mdm-migration-failure.md</files>
  <read_first>
    - docs/l2-runbooks/27-macos-sso-investigation.md (PRIMARY ANALOG — clone frontmatter, Platform gate callout, H1 style, `## Context` preamble with `**From L1 escalation?**` in-preamble routing, `## Track X:` H2 headers, `### Step N:` numbering, bash code-block + `> **Important:**`/`> **Note:**` callout patterns, `> **Version gate — macOS X:**` format)
    - docs/l2-runbooks/10-macos-log-collection.md (the log-collection prerequisite target — confirm filename + heading for the cross-link)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-RESEARCH.md (section "30-macos-mdm-migration-failure.md — L2 Runbook" -> Track A, Track B, and "Context Preamble Routing" tables)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-PATTERNS.md (Pattern Assignments for 30 — every "Rule for 30" block)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-CONTEXT.md (D-03 execution rules; MEDIUM-confidence callout convention)
  </read_first>
  <action>
    Create `docs/l2-runbooks/30-macos-mdm-migration-failure.md`. Mirror `27`'s structure exactly per the PATTERNS.md "Rule for 30" blocks.

    Frontmatter: `last_verified: 2026-06-24`, `review_by: 2026-09-24`, `applies_to: ADE`, `audience: L2`, `platform: macOS`.

    Platform gate callout (`> **Platform gate:**` before H1): scope = macOS MDM migration failure investigation via Intune and ABM; cross-link `00-index.md#macos-ade-runbooks`.

    H1 (no runbook number/"L2" in title): `# macOS MDM Migration Failure Investigation`.

    `## Context` preamble (NO router table — routing is prose+bullets inside Context, D-03):
    - Paragraph 1: describe the three failure classes (Track A deadline lockout; Track B profile-not-delivered / enrollment-failed; Track C PSSO re-registration stuck).
    - Paragraph 2 (mandatory, listed first): `Before starting: collect a diagnostic package per the [macOS Log Collection Guide](10-macos-log-collection.md).` (the L2 #10 cross-link — em-dash separators per l2-runbooks convention).
    - Paragraph 3 `**From L1 escalation?**` style routing bullets mapping symptom/source -> Track: device stuck on non-dismissible migration screen -> Track A; migration appeared to complete but Intune shows not enrolled / profiles not delivered -> Track B; migration complete and enrolled but PSSO "Registration Required" not appearing or stuck -> Track C; PSSO registration initiated but stuck -> Track C -> link to L2 #27.

    `## Track A: Deadline Lockout` — `### Step N:` numbered steps. Symptom (non-dismissible full-screen migration prompt). Diagnostic Step: verify Intune enrollment policy assigned + enrollment endpoint reachable (network/firewall) and ADE enrollment policy assigned to device serial (Intune admin center -> Enrollment -> Apple -> Enrollment program tokens -> Devices). Recovery option A (preferred): fix enrollment config and let the device complete enrollment from the locked screen (lock clears on success). Recovery option B (ABM admin, MEDIUM confidence): wrap in a `> **Note:**` callout stating ABM admin (Administrator or Device Enrollment Manager) can change/cancel the migration; pre-lockout cancel = ABM Devices -> select device -> Change Deadline -> remove deadline -> Save; for a device already mid-lockout, state conceptually + "verify exact ABM UI in current portal on authoring day" + cross-link Apple Business Support. Add a `> **Important:**` caution NOT to assert the exact mid-lockout release click-path as verified fact. Use a `> **Version gate — macOS 26:**` callout for the macOS-26 hard-gate behavior where relevant.

    `## Track B: Profile-Not-Delivered / Enrollment-Failed` — `### Step N:` numbered steps covering the RESEARCH.md root causes: leftover Kandji/Iru agent (diagnostic `ls /Library/Kandji/` and `profiles status -type enrollment` for stale MDM certs; fix = Delete Device Record before migration + allow 15+ min, or push uninstall via Intune; note B2 full-wipe removes all agent artifacts) — state agent-residue root cause at MEDIUM confidence in a callout; ADE enrollment policy not assigned to serial before migration (HIGH); ABM/Intune sync lag (HIGH — manual sync, 24h wait); mixed-fleet macOS <26 device received deadline but cannot complete in-place. All diagnostic commands in fenced `bash` blocks with `#` comments. Cross-reference the L2 #10 IntuneMacODC log-collection prerequisite.

    Surface both source-MDM names where the agent is named ("Kandji (rebranded Iru, Oct 2025)") but DO NOT re-document the walkthrough's source-side Delete-Device-Record procedure — cross-link `02-mdm-migration-psso.md` for the normal-path source-side steps (D-04: L2 #30 cross-links rather than re-documents). Stop after Track B; Task 2 authors Track C + footers.
  </action>
  <verify>
    <automated>grep -q "## Context" docs/l2-runbooks/30-macos-mdm-migration-failure.md && grep -q "## Track A: Deadline Lockout" docs/l2-runbooks/30-macos-mdm-migration-failure.md && grep -q "## Track B:" docs/l2-runbooks/30-macos-mdm-migration-failure.md && grep -q "10-macos-log-collection.md" docs/l2-runbooks/30-macos-mdm-migration-failure.md && ! grep -qiE "^\| *(symptom|source).*track *\|" docs/l2-runbooks/30-macos-mdm-migration-failure.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 30-macos-mdm-migration-failure.md exists; frontmatter contains `audience: L2`, `last_verified: 2026-06-24`, `review_by: 2026-09-24`
    - Contains a `> **Platform gate:**` blockquote and H1 `# macOS MDM Migration Failure Investigation` (no runbook number in H1)
    - Contains a `## Context` preamble with the L2 #10 cross-link (`10-macos-log-collection.md`) listed and `**From L1 escalation?**`-style routing bullets; NO separate H2 router table and NO `| symptom | ... | track |` router table
    - Contains `## Track A: Deadline Lockout` and `## Track B:` H2 sections with `### Step N:` numbered steps
    - Track A ABM admin recovery is inside a MEDIUM-confidence callout with a "verify exact ABM UI / authoring day" note and does NOT assert the mid-lockout release click-path as fact
    - Track B contains the `ls /Library/Kandji/` (or equivalent) leftover-agent diagnostic in a `bash` fence and cross-links `02-mdm-migration-psso.md` rather than re-documenting the source-side Delete-Device-Record steps
  </acceptance_criteria>
  <done>30-macos-mdm-migration-failure.md exists with frontmatter, platform gate, H1, the `## Context` preamble (in-preamble routing, L2 #10 prerequisite first), Track A (deadline lockout + conceptual ABM admin recovery in MEDIUM-confidence callouts), and Track B (profile-not-delivered/enrollment-failed with leftover-agent diagnostic and cross-link to the walkthrough).</done>
</task>

<task type="auto">
  <name>Task 2: Track C (PSSO re-registration stuck, link-not-copy to L2 #27) + Escalation Packet + Related Resources + Version History</name>
  <files>docs/l2-runbooks/30-macos-mdm-migration-failure.md</files>
  <read_first>
    - docs/l2-runbooks/30-macos-mdm-migration-failure.md (the file being modified — Task 1 output; do NOT re-author Track A/B)
    - docs/l2-runbooks/27-macos-sso-investigation.md (PRIMARY ANALOG — `## Microsoft Support Escalation Packet` lines 177-186, `## Related Resources` lines 189-197 with em-dash separators + prerequisite-first ordering, `## Version History` three-column `| Date | Change | Author |` lines 201-204)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-RESEARCH.md (section "Track C: PSSO Re-Registration Stuck" + the Escalation Packet artifact list)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-PATTERNS.md ("Microsoft Support Escalation Packet" + "Related Resources" + "Version History" Rule-for-30 blocks)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-CONTEXT.md (D-03: Track C link-not-copy to L2 #27, must NOT inline #27 investigation steps; must NOT document same-tenant key survival)
  </read_first>
  <action>
    Append to `docs/l2-runbooks/30-macos-mdm-migration-failure.md`.

    `## Track C: PSSO Re-Registration Stuck` — `### Step N:` numbered steps. Core statement: PSSO re-registration is ALWAYS required after MDM migration (Apple authoritative: unenrollment = IdP unregistration); a post-migration "Registration Required" notification is expected, not an error. First check: confirm device is user-affinity enrolled (userless devices never register) and migration enrollment completed (Intune shows Enrolled) before investigating PSSO. Quick verification: a `bash` fence with `app-sso platform -s` and interpretation guidance in prose (device REGISTERED but user not yet = user has not completed MFA re-registration; both not registered = escalate). Routing: if registration is stuck, link-not-copy to `27-macos-sso-investigation.md` (Track A handles registration-failure investigation). Add a `> **Important:**` caution: do NOT inline #27's investigation steps and do NOT document same-tenant PSSO key survival as a possible outcome.

    `## Microsoft Support Escalation Packet` — bulleted artifact list for a migration-failure case: `app-sso platform -s` full output; IntuneMacODC diagnostic package (from L2 #10); Intune device enrollment status screenshot (admin center -> Enrollment -> Apple -> Enrollment program tokens -> [token] -> Devices); ABM migration status screenshot; macOS version + Company Portal version; Company Portal log incident ID; Entra sign-in log screenshot.

    `## Related Resources` — bulleted list using `— ` (em-dash + space) separators (l2-runbooks convention), prerequisite FIRST: `[macOS Log Collection Guide (runbook 10)](10-macos-log-collection.md) — prerequisite for this runbook`; then `[macOS Platform SSO Investigation (runbook 27)](27-macos-sso-investigation.md)` (Track C link-not-copy target); `[macOS MDM Migration Walkthrough](../macos-lifecycle/02-mdm-migration-psso.md)` (walkthrough companion / normal-path source-side steps); `[Platform SSO Setup](../admin-setup-macos/07-platform-sso-setup.md)`; `[macOS ADE L2 Runbook Index](00-index.md#macos-ade-runbooks)`.

    `## Version History` — THREE columns `| Date | Change | Author |` (Author column IS present in l2-runbooks; use `| -- |` for Author), one row: `| 2026-06-24 | Phase 90 (RUN-01): initial macOS MDM migration failure runbook — Track A deadline lockout, Track B profile-not-delivered/enrollment-failed, Track C PSSO re-registration stuck | -- |`.
  </action>
  <verify>
    <automated>grep -q "## Track C:" docs/l2-runbooks/30-macos-mdm-migration-failure.md && grep -q "## Microsoft Support Escalation Packet" docs/l2-runbooks/30-macos-mdm-migration-failure.md && grep -q "27-macos-sso-investigation.md" docs/l2-runbooks/30-macos-mdm-migration-failure.md && grep -q "02-mdm-migration-psso.md" docs/l2-runbooks/30-macos-mdm-migration-failure.md && grep -q "^| Date | Change | Author |" docs/l2-runbooks/30-macos-mdm-migration-failure.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Contains `## Track C: PSSO Re-Registration Stuck` stating PSSO re-registration is ALWAYS required and that "Registration Required" post-migration is expected
    - Track C routes link-not-copy to `27-macos-sso-investigation.md` and does NOT inline #27 investigation steps; does NOT contain any same-tenant key-survival claim
    - Track C `app-sso platform -s` usage does not fabricate a partial output block presented as the healthy end-state (interpretation is in prose)
    - Contains `## Microsoft Support Escalation Packet` listing the IntuneMacODC package + Intune enrollment status + ABM migration status artifacts
    - Contains `## Related Resources` with `10-macos-log-collection.md` listed FIRST as prerequisite, plus links to `27-macos-sso-investigation.md` and `../macos-lifecycle/02-mdm-migration-psso.md`, using `—` em-dash separators
    - Contains `## Version History` with EXACTLY three columns `| Date | Change | Author |` (Author column present per l2-runbooks convention)
  </acceptance_criteria>
  <done>30-macos-mdm-migration-failure.md is complete: Track C (PSSO re-registration stuck, link-not-copy to L2 #27, no same-tenant-key-survival claim), the Microsoft Support Escalation Packet, Related Resources (L2 #10 prerequisite first, em-dash separators), and a three-column Version History with Author column.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan authors a single static markdown L2 runbook (`docs/l2-runbooks/30-macos-mdm-migration-failure.md`). No executable code, no user input, no runtime data flow, no auth/authorization surface, and no secret handling are introduced by the deliverable. No trust boundary is crossed by the artifact.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-03 | Tampering (documentation accuracy / integrity of recovery guidance) | `30-macos-mdm-migration-failure.md` Track A ABM lockout-recovery + Track B agent-residue + Track C PSSO statements | mitigate | State HIGH-confidence facts directly; gate MEDIUM-confidence facts (ABM mid-lockout release UI, leftover-agent root cause) behind `> **Note:**` confidence callouts + verify-on-authoring-day; link-not-copy Track C to L2 #27 to avoid drift; never document same-tenant PSSO key survival; apply freshness stamps via frontmatter `review_by`. |
| T-90-04 | Repudiation / Information Disclosure | n/a — runbook cites only generic diagnostic command names and portal navigation, no live secrets | accept | Diagnostic commands (`app-sso platform -s`, `ls /Library/Kandji/`, `profiles status -type enrollment`) and escalation-packet artifact names contain no credentials or tenant-specific secrets. |
| T-90-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs occur in this phase; deliverable is markdown only. No supply-chain surface introduced. |
</threat_model>

<verification>
- `30-macos-mdm-migration-failure.md` exists with Context preamble (in-preamble routing, no router table), three tracks A/B/C, Escalation Packet, Related Resources, three-column Version History.
- L2 #10 log-collection prerequisite cross-linked and listed first in Related Resources.
- Track C link-not-copies to L2 #27 (no inline #27 steps); no same-tenant key-survival claim anywhere.
- Author column present in Version History (l2-runbooks convention); em-dash separators used.
</verification>

<success_criteria>
- ROADMAP Phase 90 Success Criteria #4 is satisfied by this file.
- RUN-01 is addressed in `30-macos-mdm-migration-failure.md`.
- The file reads as a true sibling of `27-macos-sso-investigation.md`.
</success_criteria>

<output>
Create `.planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-02-SUMMARY.md` when done.
</output>
