---
phase: 90-mdm-migration-walkthrough-l2-runbook-30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/macos-lifecycle/02-mdm-migration-psso.md
autonomous: true
requirements: [MIG-01, MIG-02, MIG-03, MIG-04]
must_haves:
  truths:
    - "An operator can open 02-mdm-migration-psso.md and select B1 (macOS 26+ in-place) or B2 (pre-26 wipe) from a path selector at the top"
    - "An operator can read the shared pre-flight (OS gate + Intune readiness + Kandji/Iru secret-retrieval-before-deletion) authored once, then follow a hard-forked B1 or B2 track"
    - "An operator sees that profiles renew is explicitly NOT a no-wipe shortcut for ADE devices in the B2 fallback"
    - "An operator sees both 'Kandji' and 'Iru' names surfaced with the Oct-2025 rebrand note and a verify-on-authoring-day caveat"
    - "An operator sees mandatory post-migration PSSO re-registration documented as ALWAYS required, with the app-sso platform -s full-end-state verification gate and a link-not-copy handoff to guide 01"
  artifacts:
    - path: "docs/macos-lifecycle/02-mdm-migration-psso.md"
      provides: "Consolidated Kandji/Iru -> Intune MDM migration walkthrough (B1 in-place + B2 wipe fallback)"
      contains: "## Which Path Is Right for You?"
      min_lines: 300
  key_links:
    - from: "docs/macos-lifecycle/02-mdm-migration-psso.md"
      to: "docs/macos-lifecycle/01-psso-provisioning-walkthrough.md"
      via: "link-not-copy handoff at PSSO re-registration junction (D-02) and B2 terminus (D-01)"
      pattern: "01-psso-provisioning-walkthrough\\.md"
    - from: "docs/macos-lifecycle/02-mdm-migration-psso.md"
      to: "docs/admin-setup-macos/07-platform-sso-setup.md"
      via: "link-not-copy PSSO policy reference"
      pattern: "07-platform-sso-setup\\.md"
---

<objective>
Author the new consolidated Kandji/Iru -> Intune MDM migration scenario walkthrough `docs/macos-lifecycle/02-mdm-migration-psso.md`, covering both migration paths as a true sibling of `01-psso-provisioning-walkthrough.md`: a selector-first opening, a shared pre-flight (OS gate + Intune readiness + Kandji/Iru secret-retrieval-before-deletion) authored once, then a hard-fork into B1 (9-stage macOS-26 in-place track) and B2 (a top-level `## B2 Path:` wipe-and-re-enroll section). Document mandatory post-migration PSSO re-registration with the `app-sso platform -s` final gate and link-not-copy handoffs to guide `01`.

Purpose: An operator can migrate Macs from Kandji/Iru into Intune with PSSO following a single doc, without hopping between guides 00/01/02/07 (MIG-01..04).
Output: `docs/macos-lifecycle/02-mdm-migration-psso.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-CONTEXT.md
@.planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-RESEARCH.md
@.planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 02 + shared pre-flight + B1 macOS-26 in-place track (Stages 1-9)</name>
  <files>docs/macos-lifecycle/02-mdm-migration-psso.md</files>
  <read_first>
    - docs/macos-lifecycle/01-psso-provisioning-walkthrough.md (PRIMARY ANALOG — clone frontmatter shape, Platform gate callout, H1 pattern, three-role paragraph, Which-Path selector table, Userless-devices callout, Prerequisites checklist, Mermaid pipeline, Stage Summary Table with Path column, 4-block stage anatomy, app-sso platform -s gate two-fence pattern, OS-26 freshness stamp)
    - docs/macos-lifecycle/00-ade-lifecycle.md (SECONDARY ANALOG — 4-block stage anatomy reference)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-RESEARCH.md (section "Facts the Docs MUST State" -> "Shared Pre-flight" + "B1 — macOS 26 In-Place Path (9 stages)"; the Authoring Postures table for MEDIUM-confidence facts)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-PATTERNS.md (Pattern Assignments for 02 — every "Rule for 02" block)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-CONTEXT.md (D-01, D-02, D-04 execution rules)
  </read_first>
  <action>
    Create `docs/macos-lifecycle/02-mdm-migration-psso.md`. Mirror `01`'s structure exactly per the PATTERNS.md "Rule for 02" blocks.

    Frontmatter: keys `last_verified: 2026-06-24`, `review_by: 2026-09-24`, `applies_to: ADE`, `audience: all`, `platform: macOS`.

    Platform gate callout (blockquote `> **Platform gate:**` immediately before H1, no blank line): scope = macOS MDM migration from Kandji/Iru to Intune with PSSO, covering B1 (macOS 26+ in-place, wipe-free) and B2 (pre-macOS-26 wipe-and-re-enroll); cross-link `00-ade-lifecycle.md` and `01-psso-provisioning-walkthrough.md`.

    H1: `# macOS MDM Migration Walkthrough: B1 In-Place (macOS 26+) and B2 Wipe-and-Re-Enroll`.

    Three-role paragraph (L1 Service Desk / L2 Desktop Engineering / Intune Admins), then `## Which Path Is Right for You?` selector table with columns `| Path | macOS Requirement | Migration Type | PSSO Re-registration | Use When |`; Path values `**B1 — macOS 26 in-place**` (macOS 26+, wipe-free in-place, re-registration always required) and `**B2 — Pre-macOS-26 wipe**` (macOS 25 or earlier, retire/wipe/re-enroll, fresh PSSO from scratch). Selector/divergence table is the first content after the role paragraph (D-01).

    `> **Userless devices:**` callout immediately after the selector (userless devices never reach PSSO registration; this walkthrough covers user-affinity enrollments only).

    `### Prerequisites` with `- [ ]` checklist, split `**Common prerequisites (B1 and B2):**` and `**B1-only prerequisites (macOS 26+ in-place path):**`; cross-link `00-ade-lifecycle.md` for base ADE prerequisites and `07-platform-sso-setup.md` for PSSO policy (link-not-copy).

    `## The MDM Migration Pipeline` with a `graph TD` Mermaid block: shared pre-flight stages feed a fork node at the macOS-26 gate; B1 9-stage arm and B2 5-stage arm diverge. Add a `>` prose summary below the fence noting B1 and B2 share only the OS-gate + secret-retrieval pre-flight (2 stages) then hard-fork.

    `## Stage Summary Table` with EXACT columns `| Stage | Actor | Location | What Happens | Key Pitfall | Path |`. Path column values ONLY `Both`, `B1`, `B2`. Shared pre-flight rows = `Both`; B1 in-place stages = `B1`.

    Author the SHARED PRE-FLIGHT stages once (Path: Both) using the 4-block anatomy (`### What the Admin Sees`, `### What Happens` numbered, `### Behind the Scenes`, `### Watch Out For`): OS-gate fleet assessment (macOS 26+ for B1; macOS 25 or earlier routes to B2) AND the Kandji/Iru secret-retrieval/source-release stage. In the secret-retrieval stage (D-04, MIG-03): state vendor-neutral conceptual steps — retrieve FileVault recovery keys AND Activation Lock bypass codes BEFORE deletion (both permanently destroyed on Delete Device Record; bypass codes only retrievable within 30 days of supervision), then Delete Device Record, then agent self-removes at next ~15-min check-in. Surface BOTH names on first mention as "Kandji (rebranded **Iru**, October 2025; support portal URL unchanged at support.kandji.io)". Add a MEDIUM-confidence `> **Note:**` callout: "Verify current Iru console labels on authoring day — Oct-2025 rebrand; steps may differ from support.kandji.io documentation." Include the VPP/location-token sequencing fact (revoke licenses + remove token in Kandji/Iru FIRST, then upload to Intune) and the sync-timing fact (up to 24h auto-sync; 15-min manual rate limit; 7-day full cooldown) in the Intune readiness shared stage.

    Author the B1 macOS-26 in-place track (Path: B1), 9 stages per RESEARCH.md "B1 — macOS 26 In-Place Path", each with the 4-block anatomy: Fleet Assessment (OS gate) with the MEDIUM-confidence one-time-wipe pitfall callout for pre-26-ABM-enrolled devices; Intune Readiness (verify ADE enrollment policy assigned to device serials OR token default + PSSO Settings Catalog policy assigned to target user groups BEFORE triggering ABM — state as fact that the existing ADE enrollment policy handles re-enrollment, no separate profile-based-enrollment config); ABM "Assign Device Management" (primary label, with a verify-label-on-authoring-day note that it may read "Re-assign Device Management" depending on assignment state); Set Deadline (1-90 day range with a recommendation to pilot one device and set the deadline only after Intune readiness is confirmed — discretion on phrasing); User Notification Window (cadence: daily until 24h, hourly in last 24h, then 60/30/10/1 min); Deadline Enforcement (non-dismissible full-screen lock; pre-lockout recovery via ABM "Change Deadline -> remove deadline"); Post-migration enrollment (genuine unenroll + reenroll via profile-based enrollment; ACME cert reissued; supervision expected-preserved at MEDIUM confidence with a `profiles status -type enrollment | grep Supervised` pilot-check callout); and the Stage 9 PSSO re-registration block.

    Stage 9 PSSO re-registration (D-02, MIG-04) — author ONLY migration-delta facts inline: genuine unenroll -> IdP unregistration; new-tenant Secure Enclave key re-created; ACME reissued; why "Registration Required" appears post-migration; PSSO re-registration is ALWAYS required. Then the `### How to Verify` gate using the exact two-fence pattern from `01` (a `bash` fence with `app-sso platform -s`, then a plain no-language fence showing ONLY the full end-state `Device Registration: REGISTERED` / `User Registration: REGISTERED`). Add the link-not-copy handoff sentence pointing to `01-psso-provisioning-walkthrough.md` for the user-facing MFA/registration UX (bidirectional junction anchor, D-02). Escalation bullets must include L1 #35, L1 #36, L2 #27. Do NOT fabricate partial/intermediate app-sso output. Do NOT mention same-tenant key survival anywhere.

    Place the `_Section provenance — \`last_verified: 2026-06-24\` / \`review_by: 2026-09-24\`. ..._` italic stamp at the end of the B1 in-place track (and on the ABM "Assign Device Management" + Deadline mechanics if authored as a delimited subsection), specifying re-confirm targets: ABM button label, non-dismissible lock behavior, supervision-status preservation.

    Use `--` (double hyphen) as the description separator in any cross-link bullets (macos-lifecycle directory convention). Do NOT inline guide 00/01/02/07 reference content (link-not-copy). Stop after Stage 9 + the B1 provenance stamp; Task 2 authors the B2 fork and footers.
  </action>
  <verify>
    <automated>grep -q "## Which Path Is Right for You?" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "Device Registration: REGISTERED" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "rebranded \*\*Iru\*\*" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -Eq "Stage Summary Table" docs/macos-lifecycle/02-mdm-migration-psso.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 02-mdm-migration-psso.md exists and frontmatter contains `last_verified: 2026-06-24` and `review_by: 2026-09-24` and `audience: all`
    - Contains a `> **Platform gate:**` blockquote and an H1 matching `# macOS MDM Migration Walkthrough: B1 ... and B2 ...`
    - Contains `## Which Path Is Right for You?` with a table whose Path column values are only `**B1 — macOS 26 in-place**` and `**B2 — Pre-macOS-26 wipe**`
    - Contains a `## Stage Summary Table` whose Path column values are only `Both`, `B1`, `B2`
    - Contains a `## The MDM Migration Pipeline` section with a ```` ```mermaid ```` `graph TD` block
    - Surfaces both names: contains the literal `Kandji` and `Iru` with the October 2025 rebrand note and `support.kandji.io`
    - Secret-retrieval stage states retrieval of FileVault keys AND Activation Lock bypass codes BEFORE deletion (MIG-03 ordering explicit)
    - Stage 9 contains the two-fence `app-sso platform -s` gate showing ONLY `Device Registration: REGISTERED` / `User Registration: REGISTERED` (no `NOT REGISTERED` string anywhere in any app-sso output fence)
    - Stage 9 contains a link-not-copy handoff to `01-psso-provisioning-walkthrough.md` for registration UX and states PSSO re-registration is ALWAYS required
    - Does NOT contain any statement that the PSSO/Secure Enclave key survives migration or is preserved same-tenant
    - Contains the `_Section provenance — ` freshness stamp on the B1/OS-26-gated content
  </acceptance_criteria>
  <done>02-mdm-migration-psso.md exists with frontmatter, platform gate, H1, selector table, userless callout, prerequisites, Mermaid pipeline, Stage Summary Table (Path column Both/B1/B2), the shared pre-flight stages (OS gate + Kandji/Iru secret-retrieval with both names and before-deletion ordering), and the full B1 9-stage in-place track ending with the Stage 9 PSSO re-registration delta + app-sso gate + link-not-copy handoff + provenance stamp.</done>
</task>

<task type="auto">
  <name>Task 2: B2 wipe-and-re-enroll fork section + See Also / Glossary / Version History footers</name>
  <files>docs/macos-lifecycle/02-mdm-migration-psso.md</files>
  <read_first>
    - docs/macos-lifecycle/02-mdm-migration-psso.md (the file being modified — Task 1 output; do NOT re-author Task 1 content)
    - docs/macos-lifecycle/01-psso-provisioning-walkthrough.md (PRIMARY ANALOG — the `## A2 Path:` top-level divergence section pattern lines 361-442, the See Also three-subsection block lines 445-466, Glossary Quick Reference lines 469-483, Version History lines 486-491)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-RESEARCH.md (section "B2 — Pre-macOS-26 Fallback (5 stages)" + the `profiles renew` prohibition wording)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-PATTERNS.md ("A2-style divergence/fork callout block pattern" + "See Also" + "Glossary Quick Reference" + "Version History" Rule-for-02 blocks; the `profiles renew` prohibition shared pattern)
    - .planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-CONTEXT.md (D-01 B2 terminus = link-not-copy handoff to guide 01; MIG-02)
  </read_first>
  <action>
    Append to `docs/macos-lifecycle/02-mdm-migration-psso.md`.

    Add a TOP-LEVEL `## B2 Path: Pre-macOS-26 Wipe-and-Re-Enroll` section (mirroring `01`'s `## A2 Path:` top-level divergence block, NOT an H3). Inside, use `>` blockquote callouts with `---` horizontal-rule separators for the hard requirements/risks, and include a `**B2 Requirements Summary:**` table with columns `| B2 Requirement | Value |`. Author the 5 B2 stages per RESEARCH.md "B2 — Pre-macOS-26 Fallback": OS gate (confirm macOS 25 or earlier; B1 in-place not available; this path requires a wipe); secret retrieval (same mandatory before-deletion ordering as the shared stage — reference it, do not re-document the full Kandji/Iru steps); retire/wipe in Kandji/Iru (Erase Mac) WITH the `profiles renew` prohibition; ADE re-enroll via Intune (device contacts ABM during Setup Assistant; standard ADE flow; ACME issued); and a terminal stage that is a link-not-copy handoff to `01-psso-provisioning-walkthrough.md`'s A1 standard provisioning path for fresh PSSO (D-01 — do NOT re-document PSSO provisioning here).

    The `profiles renew` prohibition (MIG-02) must appear as a bold-lead `> **Important:**` callout or a bold-lead "Watch Out For" bullet, stating: `sudo profiles renew -type enrollment` is NOT a supported migration path for ADE-enrolled macOS devices — macOS only queries ABM for MDM assignment during Setup Assistant after a device wipe; using it on an ADE-enrolled device results in enrollment failure (the SSO extension profile cannot install because MDM enrollment is its prerequisite — a circular dependency).

    Then author the footer sections in `01`'s order:
    - `## See Also` with three subsections `**Terminology and Concepts:**`, `**Technical References:**`, `**Related Guides:**`, using `--` (double hyphen) separators. Related Guides MUST include link-not-copy entries to: `00-ade-lifecycle.md`, `01-psso-provisioning-walkthrough.md` (bidirectional anchor for D-02/D-01), `07-platform-sso-setup.md`, `02-enrollment-profile.md` (at `../admin-setup-macos/`), `27-macos-sso-investigation.md` (at `../l2-runbooks/`), and `30-macos-mdm-migration-failure.md` (at `../l2-runbooks/`, the L2 #30 reciprocal link).
    - `## Glossary Quick Reference` table with columns `| Term | Definition | First Appears |`, bracketed term links into `../_glossary-macos.md#anchor`; include migration terms: ABM "Assign Device Management", Deadline, FileVault recovery key, Activation Lock bypass code, ACME, profile-based enrollment, Kandji/Iru.
    - `## Version History` with TWO columns only `| Date | Change |` (NO Author column — macos-lifecycle convention), one row: `| 2026-06-24 | Phase 90 (MIG-01..04): initial MDM migration walkthrough (B1 in-place + B2 wipe paths) |`.
  </action>
  <verify>
    <automated>grep -q "## B2 Path: Pre-macOS-26 Wipe-and-Re-Enroll" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "profiles renew" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "## Glossary Quick Reference" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -q "30-macos-mdm-migration-failure.md" docs/macos-lifecycle/02-mdm-migration-psso.md && ! grep -q "^| Date | Change | Author |" docs/macos-lifecycle/02-mdm-migration-psso.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Contains a top-level `## B2 Path: Pre-macOS-26 Wipe-and-Re-Enroll` section with a `**B2 Requirements Summary:**` table (columns `| B2 Requirement | Value |`)
    - Contains the literal string `profiles renew` inside a statement that it is NOT a supported shortcut for ADE-enrolled devices (MIG-02)
    - B2 terminal stage is a link-not-copy handoff to `01-psso-provisioning-walkthrough.md` (does NOT re-document PSSO provisioning steps)
    - Contains `## See Also` with `**Related Guides:**` linking to `01-psso-provisioning-walkthrough.md` AND `30-macos-mdm-migration-failure.md` AND `27-macos-sso-investigation.md`, using `--` separators
    - Contains `## Glossary Quick Reference` with columns `| Term | Definition | First Appears |`
    - Contains `## Version History` with EXACTLY two columns `| Date | Change |` and NO `Author` column (assert no `| Date | Change | Author |` header present)
  </acceptance_criteria>
  <done>02-mdm-migration-psso.md is complete: B2 top-level fork section with Requirements Summary table, the `profiles renew`-is-not-a-shortcut prohibition, the link-not-copy B2 terminus to guide 01, and the See Also / Glossary Quick Reference / two-column Version History footers matching guide 01's sibling conventions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan authors a single static markdown documentation file (`docs/macos-lifecycle/02-mdm-migration-psso.md`). There is no executable code, no user input, no runtime data flow, no authentication or authorization surface, and no secret handling introduced by the deliverable. No trust boundary is crossed by the artifact.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Tampering (documentation accuracy / integrity of guidance) | `02-mdm-migration-psso.md` migration + security-sensitive facts (PSSO key behavior, ACME reissue, FileVault/Activation-Lock secret retrieval ordering) | mitigate | Bake HIGH-confidence facts as stated; gate MEDIUM-confidence facts (supervision, ABM lockout recovery, Iru labels) behind explicit confidence callouts + verify-on-authoring-day notes; apply `last_verified`/`review_by` freshness stamps on OS-26-gated content; never state PSSO/Secure-Enclave key survival; mandate secret-retrieval-BEFORE-deletion ordering (MIG-03). |
| T-90-02 | Information Disclosure | n/a — static doc cites no live secrets, tokens, or tenant data | accept | Doc contains only generic procedure descriptions and public command names (`app-sso platform -s`, `profiles renew`); no credentials or environment-specific secrets embedded. |
| T-90-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs occur in this phase; deliverable is markdown only. No supply-chain surface introduced. |
</threat_model>

<verification>
- `02-mdm-migration-psso.md` exists with selector-first opening, shared pre-flight (Both), B1 9-stage track, B2 top-level fork, and all footers.
- Path columns constrained to Both/B1/B2 (Stage Summary Table) and the two named paths (selector table).
- MIG-02 (`profiles renew` not-a-shortcut), MIG-03 (both names + before-deletion ordering), MIG-04 (PSSO re-registration + app-sso gate + bidirectional 01 link) all present.
- No fabricated partial `app-sso platform -s` output; no same-tenant key-survival claim.
- Version History is two-column (no Author) per macos-lifecycle convention.
</verification>

<success_criteria>
- ROADMAP Phase 90 Success Criteria #1, #2, #3 are satisfied by this file.
- MIG-01, MIG-02, MIG-03, MIG-04 are addressed in `02-mdm-migration-psso.md`.
- The file reads as a true sibling of `01-psso-provisioning-walkthrough.md` (frontmatter shape, footers, separators).
</success_criteria>

<output>
Create `.planning/phases/90-mdm-migration-walkthrough-l2-runbook-30/90-01-SUMMARY.md` when done.
</output>
