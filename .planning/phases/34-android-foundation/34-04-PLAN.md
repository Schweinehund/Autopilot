---
phase: 34-android-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 34-01
  - 34-02
files_modified:
  - docs/android-lifecycle/03-android-version-matrix.md
autonomous: true
requirements:
  - AEBASE-04
user_setup: []

must_haves:
  truths:
    - "An admin reading `docs/android-lifecycle/03-android-version-matrix.md` can determine the minimum Intune-supported Android OS per enrollment mode by scanning the 3-column matrix"
    - "The admin sees explicit breakpoint callouts for Android 11 (COPE NFC removal), Android 12 (IMEI/serial removed from corporate identifiers), and Android 15 (FRP hardening) — each in its own narrative H3 subsection with Affected modes / What changed / Admin action required / References"
    - "A supplementary `## Non-Version Breakpoints` H2 section records temporal deprecations (SafetyNet → Play Integrity Jan 2025, AMAPI migration for BYOD Apr 2025) that aren't version-gated but are policy-gated"
    - "No `min_android_version` frontmatter key appears on this or any Android doc — version gating lives in the matrix body only (per D-31 prohibition)"
    - "This file's `last_verified` frontmatter is load-bearing — Phase 42 milestone audit uses it as the drift-anchor date"
  artifacts:
    - path: "docs/android-lifecycle/03-android-version-matrix.md"
      provides: "3-column version matrix + Version Breakpoint Details narrative subsections (Android 11, 12, 15) + Non-Version Breakpoints section"
      contains:
        - "| Mode | Intune Minimum OS | Notable Version Breakpoints |"
        - "## Version Breakpoint Details"
        - "### Android 11"
        - "### Android 12"
        - "### Android 15"
        - "## Non-Version Breakpoints"
        - "### SafetyNet"
        - "### AMAPI Migration"
        - "Play Integrity"
        - "COBO"
        - "BYOD Work Profile"
        - "Dedicated (COSU)"
        - "Zero-Touch Enrollment"
        - "AOSP"
  key_links:
    - from: "docs/android-lifecycle/03-android-version-matrix.md"
      to: "docs/android-lifecycle/02-provisioning-methods.md"
      via: "Cross-reference in Version Breakpoint Details (Android 11 COPE NFC removal references 02-provisioning-methods.md)"
      pattern: "02-provisioning-methods\\.md"
    - from: "docs/android-lifecycle/03-android-version-matrix.md"
      to: "docs/_glossary-android.md"
      via: "Term cross-references (FRP, WPCO, COPE, Play Integrity, AMAPI in matrix body and breakpoint narratives)"
      pattern: "_glossary-android\\.md"
---

<objective>
Create `docs/android-lifecycle/03-android-version-matrix.md` — the canonical Android version-fragmentation reference showing minimum Intune-supported Android OS per enrollment mode and three notable version breakpoints (Android 11 COPE NFC removal, Android 12 IMEI/serial removal from corporate identifiers, Android 15 FRP hardening) plus a supplementary non-version-breakpoints section (SafetyNet → Play Integrity, AMAPI migration) (AEBASE-04 / ROADMAP Success Criterion 5).

Purpose: Admins need a single drift-surface to consult when targeting an Android OS range for a given mode. This file is ALSO the audit anchor for Phase 42 milestone verification — its `last_verified` date gates whether v1.4 Android content is considered current. Without the single canonical source, admins get contradictory version guidance and version-drift pitfalls (Pitfall 5) compound.

Output: One new markdown file at `docs/android-lifecycle/03-android-version-matrix.md` with frontmatter (NO `min_android_version` key per D-31) → platform-gate blockquote → introductory prose → 3-column matrix table → `## Version Breakpoint Details` H2 with 3 H3 subsections (Android 11, 12, 15) → `## Non-Version Breakpoints` H2 with 2 H3 subsections (SafetyNet→Play Integrity, AMAPI migration) → See Also.

This plan MUST honor 5 locked decisions: D-29 (3-col matrix: Mode / Intune Minimum OS / Notable Version Breakpoints — do NOT add a "Recommended" column), D-30 (breakpoint narratives in a separate section below the matrix), D-31 (NO `min_android_version` frontmatter anywhere), D-32 (supplementary Non-Version Breakpoints for policy-gated drift), D-33 (`last_verified` load-bearing for Phase 42 audit).

**Wave 2 context:** This plan depends on Plan 01 (glossary for cross-refs to #cope, #wpco, #play-integrity, #amapi, #corporate-identifiers) and Plan 02 (enrollment overview established mode-ordering convention — matrix rows use same mode order).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-android-foundation/34-CONTEXT.md
@.planning/phases/34-android-foundation/34-RESEARCH.md
@.planning/phases/34-android-foundation/34-PATTERNS.md
@.planning/phases/34-android-foundation/34-VALIDATION.md
@.planning/research/FEATURES.md
@docs/_glossary-android.md
@docs/android-lifecycle/00-enrollment-overview.md
@docs/reference/ios-capability-matrix.md

<interfaces>
<!-- Anchors this file MUST publish (Phase 35-42 load-bearing targets). -->
<!-- Source: 34-RESEARCH.md Pitfall 7 lines 479-486, 34-PATTERNS.md Shared Patterns → Anchor stability registry. -->

Per-breakpoint anchors (Phase 36-39 admin guides reference these):
  #android-11-breakpoint  (explicit anchor — H3 `### Android 11 — COPE NFC Provisioning Removed` auto-anchor is long)
  #android-12-breakpoint
  #android-15-breakpoint
  #non-version-breakpoints (auto-anchor from `## Non-Version Breakpoints`)

Per-mode row anchors (for potential incoming cross-refs):
  #cobo
  #byod
  #dedicated
  #zte
  #aosp

Frontmatter (explicit D-31 prohibition — NO min_android_version key):
  last_verified: 2026-04-21   # LOAD-BEARING for Phase 42 audit anchor per D-33
  review_by: 2026-06-20       # = last_verified + 60 days per D-33
  applies_to: both
  audience: admin
  platform: Android

Data source: FEATURES.md lines 350-358 — research matrix with mode-level Intune minimums.
  COBO: Android 10.0
  BYOD Work Profile: Android 5.0 (practical: Android 8)
  Dedicated (COSU): Android 8.0
  Zero-Touch Enrollment: Android 8.0 (Oreo)
  AOSP: Varies by OEM firmware (not an Android API level)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold file with frontmatter (NO min_android_version), platform gate, H1, intro prose, and 3-column version matrix table</name>
  <files>docs/android-lifecycle/03-android-version-matrix.md</files>

  <read_first>
    - docs/reference/ios-capability-matrix.md (lines 1-15 — frontmatter + H1 + intro shape)
    - docs/_glossary-android.md (from Plan 01 — for #cope, #wpco, #fully-managed, #byod, #dedicated anchors)
    - docs/android-lifecycle/00-enrollment-overview.md (from Plan 02 — mode ordering convention: COBO, BYOD, Dedicated, ZTE, AOSP)
    - .planning/phases/34-android-foundation/34-CONTEXT.md (D-29 — 3-col matrix; D-31 — frontmatter prohibition; D-33 — load-bearing last_verified)
    - .planning/phases/34-android-foundation/34-PATTERNS.md (section "`docs/android-lifecycle/03-android-version-matrix.md`" lines 270-332 — verbatim table shape + narrative patterns)
    - .planning/phases/34-android-foundation/34-RESEARCH.md (Pattern 5 lines 326-363 — canonical matrix + breakpoint narratives)
    - .planning/research/FEATURES.md (lines 350-358 — data source for matrix values)
  </read_first>

  <action>
    Create `docs/android-lifecycle/03-android-version-matrix.md` with the following content:

    1. **Frontmatter** — CRITICAL: do NOT add a `min_android_version` key. This key is explicitly prohibited on ALL Android docs per D-31 (ARCHITECTURE.md Q7). Version gating lives in this matrix body only:
       ```yaml
       ---
       last_verified: 2026-04-21
       review_by: 2026-06-20
       applies_to: both
       audience: admin
       platform: Android
       ---
       ```
       `last_verified: 2026-04-21` is LOAD-BEARING per D-33 — Phase 42 milestone audit uses this date as the drift anchor for v1.4 Android content validity.

    2. **H1 title**:
       ```markdown
       # Android Version Matrix
       ```

    3. **Platform gate blockquote**:
       ```markdown
       > **Platform gate:** This reference shows the minimum Intune-supported Android OS per enrollment mode and three notable Android version breakpoints (Android 11 COPE NFC removal, Android 12 corporate-identifier IMEI/serial removal, Android 15 FRP hardening) plus policy-gated non-version breakpoints (SafetyNet → Play Integrity January 2025, AMAPI migration for BYOD April 2025).
       > For provisioning-method version availability per mode × method, see [02-provisioning-methods.md](02-provisioning-methods.md). For Android terminology, see the [Android Enterprise Provisioning Glossary](../_glossary-android.md).
       ```

    4. **Introductory paragraph** explaining matrix purpose and version-drift guard:
       ```markdown
       This matrix captures two distinct version axes:

       - **Intune Minimum OS** is the minimum Android API level that Intune supports for management of a device in the given enrollment mode. It is what Microsoft enforces at enrollment time.
       - **Notable Version Breakpoints** are Android-platform-level behavior changes that materially affect admin operations for the mode — typically Google feature removals, new hardening behaviors, or corporate-identifier restrictions.

       These two axes are related but not equivalent. Per-method × per-mode version gating (e.g., "NFC requires Android 8+; QR built-in reader requires Android 9+") lives in [02-provisioning-methods.md](02-provisioning-methods.md) cells, not this matrix. If you are authoring admin guidance that references a specific Android version requirement, cite THIS matrix for mode-level Intune minimums and [02-provisioning-methods.md](02-provisioning-methods.md) for method-level gates. Do not restate version numbers inline in admin guides — that creates drift-surface (Pitfall 1).
       ```

    5. **3-column matrix table** per D-29 with 5 mode rows per D-23 convention (same mode ordering as enrollment overview comparison table):

       ```markdown
       <a id="cobo"></a>
       <a id="byod"></a>
       <a id="dedicated"></a>
       <a id="zte"></a>
       <a id="aosp"></a>

       | Mode | Intune Minimum OS | Notable Version Breakpoints |
       |------|-------------------|----------------------------|
       | Fully Managed (COBO) | Android 10.0 | Android 11: enrollment-time grouping; Android 15: FRP hardening |
       | BYOD Work Profile | Android 5.0 (practical: Android 8+) | Android 9: built-in QR reader; Android 12: IMEI/serial removed from corporate identifiers; Android 15: Private Space unsupported |
       | Dedicated (COSU) | Android 8.0 | Android 9: built-in QR reader; Android 15: FRP hardening on re-provisioning |
       | Zero-Touch Enrollment | Android 8.0 (Oreo) | Android 15: FRP hardening affects re-enrollment flows |
       | AOSP | Varies by OEM firmware | Not an Android API-level gate — OEM firmware-specific; see [Phase 39 AOSP stub](../admin-setup-android/06-aosp-stub.md) |
       ```

       Note: The stacked `<a id="..."></a>` anchors above the table are NOT a cell-level anchor solution — GitHub markdown doesn't support per-row anchor targets inside a table via HTML tags. The 5 per-mode anchors (`#cobo`, `#byod`, `#dedicated`, `#zte`, `#aosp`) are auxiliary; Phase 35-42 incoming cross-refs into this file most commonly target the breakpoint H3 anchors (`#android-11-breakpoint`, etc.) rather than matrix-row anchors. Placing the anchor tags immediately before the table satisfies the "resolve" requirement even if the anchor points near-the-table rather than into-the-table.

    6. **Critical negative check per D-31**: After writing the frontmatter, grep the file to confirm `min_android_version` does NOT appear anywhere. If it does, remove it. This check will also run across the whole `docs/android-lifecycle/` directory at phase gate (check 34-04-06: `grep -l "min_android_version" docs/android-lifecycle/` must return empty).

    **Content gates:**
    - Frontmatter must NOT include `min_android_version` (D-31).
    - Platform-gate blockquote does NOT link to `../common-issues.md`, `../quick-ref-l1.md`, `../quick-ref-l2.md`, or `../index.md`.
    - Matrix has exactly 3 data columns (not 4 — do NOT add a "Recommended" column per D-29 alternative rejected during adversarial review). The `Notes` column from `02-provisioning-methods.md` is NOT used here; that's a DIFFERENT matrix.
    - 5 mode rows in the order: COBO, BYOD Work Profile, Dedicated (COSU), Zero-Touch Enrollment, AOSP (matches enrollment overview Task 2 ordering for consistency).
  </action>

  <verify>
    <automated>
      test -f docs/android-lifecycle/03-android-version-matrix.md &&
      grep -c "^platform: Android$" docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c "^last_verified: 2026-04-21$" docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c "^# Android Version Matrix$" docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      test $(grep -c "min_android_version" docs/android-lifecycle/03-android-version-matrix.md) -eq 0 &&
      grep -cE "^\| Mode \| Intune Minimum OS \| Notable Version Breakpoints \|$" docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c "Fully Managed (COBO)" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "BYOD Work Profile" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "Dedicated (COSU)" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "Zero-Touch Enrollment" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "| AOSP |" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      test $(grep -cE "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md" docs/android-lifecycle/03-android-version-matrix.md) -eq 0
    </automated>
  </verify>

  <acceptance_criteria>
    - `docs/android-lifecycle/03-android-version-matrix.md` exists
    - Frontmatter has `platform: Android`, `last_verified: 2026-04-21` (load-bearing per D-33), `review_by: 2026-06-20`, `audience: admin`, `applies_to: both`
    - Frontmatter does NOT contain `min_android_version` key anywhere (validates check 34-04-06 prohibition)
    - Matrix header is exactly `| Mode | Intune Minimum OS | Notable Version Breakpoints |` (validates check 34-04-01 — 3 columns)
    - 5 data rows present for modes: Fully Managed (COBO), BYOD Work Profile, Dedicated (COSU), Zero-Touch Enrollment, AOSP
    - Intune Minimum OS values match FEATURES.md source: COBO=Android 10.0, BYOD=Android 5.0 (practical 8+), Dedicated=Android 8.0, ZTE=Android 8.0, AOSP=Varies by OEM firmware
    - Introductory prose distinguishes "Intune Minimum OS" vs "Notable Version Breakpoints" axes and forward-refs to `02-provisioning-methods.md` for per-method version gating
    - Zero links to deferred files
  </acceptance_criteria>

  <done>
    Scaffold + 3-col matrix table in place. Frontmatter D-31-compliant (no `min_android_version`). Mode ordering matches enrollment overview. Ready for Task 2 to add the three version breakpoint narrative sections.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write `## Version Breakpoint Details` H2 with 3 H3 subsections (Android 11, Android 12, Android 15) each with Affected modes / What changed / Admin action / References</name>
  <files>docs/android-lifecycle/03-android-version-matrix.md</files>

  <read_first>
    - docs/android-lifecycle/03-android-version-matrix.md (current state after Task 1)
    - .planning/phases/34-android-foundation/34-CONTEXT.md (D-30 — narrative structure; "Version Breakpoint Narrative Pattern" in specifics section lines 175-184)
    - .planning/phases/34-android-foundation/34-PATTERNS.md (lines 300-312 — exact breakpoint H3 pattern with Affected modes / What changed / Admin action required / References)
    - .planning/phases/34-android-foundation/34-RESEARCH.md (Pattern 5 lines 342-363 — full narrative content for all three breakpoints verbatim; "Code Examples" section lines 570-585 for Android 15 FRP canonical narrative)
    - .planning/phases/34-android-foundation/34-VALIDATION.md (checks 34-04-02, 34-04-03, 34-04-04)
  </read_first>

  <action>
    Append the `## Version Breakpoint Details` H2 section with 3 H3 subsections. Each H3 has explicit `<a id="android-NN-breakpoint"></a>` anchor tag (so Phase 35-42 cross-refs use shorter `#android-11-breakpoint` pattern rather than the long auto-anchor `#android-11--cope-nfc-provisioning-removed`).

    **Section structure** (H2 + intro + 3 H3s):

    ```markdown
    ## Version Breakpoint Details

    Each breakpoint below documents a Google-driven behavior change that materially affects admin operations. Breakpoint content is sourced from Google Android Enterprise Help, Microsoft Learn, and Jason Bayton's Android Enterprise reference material (bayton.org). Verify `last_verified` frontmatter date before citing this file as authoritative; Phase 42 milestone audit re-validates against current sources.

    <a id="android-11-breakpoint"></a>
    ### Android 11 — COPE NFC Provisioning Removed

    **Affected modes:** COBO (no impact — QR / afw#setup / Zero-Touch still supported), COPE / WPCO (NFC and afw#setup paths removed).

    **What changed:** Google removed NFC and DPC identifier (`afw#setup`) as valid provisioning methods for work profile on corporate-owned (COPE / WPCO) devices on Android 11+. The two paths silently stop working on Android 11+ devices; administrators who relied on NFC for COPE provisioning see provisioning handshake failures with no legacy-path fallback.

    **Admin action required:** For COBO, no action — continue using QR, afw#setup, or Zero-Touch. For COPE / WPCO enrollments on Android 11+, migrate to QR or Zero-Touch. Documenting this migration path is Phase 36 scope (COBO admin guide includes a COPE migration note per AECOBO-02 using "Google recommends WPCO" language).

    **References:**
    - [Jason Bayton — Android 11 COPE changes](https://bayton.org/android/android-11-cope-changes/)
    - [Microsoft Learn — Android Enterprise corporate-owned provisioning methods](https://learn.microsoft.com/en-us/intune/device-enrollment/android/ref-corporate-methods)

    <a id="android-12-breakpoint"></a>
    ### Android 12 — IMEI / Serial Removed from Corporate Identifiers (BYOD only)

    **Affected modes:** BYOD Work Profile only. COBO, Dedicated, Zero-Touch Enrollment, and AOSP are unaffected.

    **What changed:** Google removed IMEI / serial / MEID read access from personally-owned work profile devices on Android 12+. The practical impact: administrators can NO LONGER pre-mark Android 12+ personal devices as corporate via identifier upload — corporate identifiers work only on Android 11 and earlier for BYOD. COBO / Dedicated / ZTE / WPCO devices auto-mark as corporate at enrollment time (no identifier upload needed), so those modes are unchanged.

    **Admin action required:** For BYOD tenants with corporate-identifier workflows, document Android 11 as the ceiling for pre-marking. Treat Android 12+ BYOD devices as personal-at-enrollment (they can still enroll; they cannot be pre-flagged as corporate via upload). See [Corporate Identifiers](../_glossary-android.md#corporate-identifiers) for the glossary entry.

    **References:**
    - [Microsoft Learn — Add corporate identifiers](https://learn.microsoft.com/en-us/intune/device-enrollment/add-corporate-identifiers)

    <a id="android-15-breakpoint"></a>
    ### Android 15 — Factory Reset Protection Hardening

    **Affected modes:** COBO (re-enrollment after reset), Dedicated (re-provisioning), Zero-Touch Enrollment (re-enrollment). BYOD Work Profile is unaffected.

    **What changed:** On Android 15, the OEM unlocking setting no longer affects the reset process — after a hard reset, Enterprise Factory Reset Protection (EFRP) is always enforced. FRP now triggers on more reset pathways than on Android 13 / 14, meaning re-enrollment flows that worked on Android 13 / 14 can block on Android 15 unless EFRP is configured BEFORE devices are reset.

    **Admin action required:** Configure Enterprise Factory Reset Protection (EFRP) via Intune policy as part of the COBO / Dedicated / ZTE enrollment profile BEFORE devices ship or before admin-initiated reset. Document the EFRP configuration step in the relevant admin guide (Phase 36 COBO and Phase 38 Dedicated per AECOBO-03 and AEDED-03). See [Google AE Help — Enable enterprise factory reset protection](https://support.google.com/work/android/answer/14549362).

    **References:**
    - [Google Android Enterprise Help — Enable enterprise factory reset protection](https://support.google.com/work/android/answer/14549362)
    - [Jason Bayton — Android 15: What's new for enterprise?](https://bayton.org/blog/2024/10/actually-new-for-enterprise-android-15/)
    - [Microsoft Learn — Factory Reset Protection Emails Not Enforced in Intune for Android](https://learn.microsoft.com/en-us/troubleshoot/mem/intune/device-configuration/factory-reset-protection-emails-not-enforced)
    ```

    **Content gates:**
    - Each H3 heading must start with `### Android NN` (validates checks 34-04-02, 34-04-03, 34-04-04 via `grep -c "^### Android NN"` = 1).
    - Each H3 has all 4 structural blocks: **Affected modes:**, **What changed:**, **Admin action required:**, **References:** (from PATTERNS.md line 301-311).
    - Explicit `<a id="android-NN-breakpoint"></a>` anchor tag immediately before each H3 heading.
    - Android 12 section MUST specify BYOD Work Profile as the only affected mode (per CONTEXT.md D-30 Pitfall-5-clarification — COBO/Dedicated/ZTE/WPCO auto-mark as corporate).
    - Android 15 section MUST cite EFRP / Enterprise Factory Reset Protection terminology (not just "FRP") — EFRP is the Intune policy name.
    - NO "COPE deprecated" language — use "Google recommends WPCO" in the Android 11 section's migration note (matches Pitfall 4).
  </action>

  <verify>
    <automated>
      grep -c "^## Version Breakpoint Details$" docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c "^### Android 11" docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c "^### Android 12" docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c "^### Android 15" docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c '<a id="android-11-breakpoint"></a>' docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c '<a id="android-12-breakpoint"></a>' docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c '<a id="android-15-breakpoint"></a>' docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c "Affected modes:" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 3 { exit 0 } { exit 1 }' &&
      grep -c "What changed:" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 3 { exit 0 } { exit 1 }' &&
      grep -c "Admin action required:" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 3 { exit 0 } { exit 1 }' &&
      grep -c "References:" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 3 { exit 0 } { exit 1 }' &&
      test $(grep -ci "COPE deprecated" docs/android-lifecycle/03-android-version-matrix.md) -eq 0 &&
      grep -ci "EFRP\|Enterprise Factory Reset Protection" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 1 { exit 0 } { exit 1 }'
    </automated>
  </verify>

  <acceptance_criteria>
    - `## Version Breakpoint Details` H2 present
    - Three H3 subsections present starting exactly with `### Android 11`, `### Android 12`, `### Android 15` (validates checks 34-04-02, 34-04-03, 34-04-04)
    - Each H3 preceded by explicit `<a id="android-NN-breakpoint"></a>` anchor tag
    - Each H3 body contains all 4 required bold sections: `**Affected modes:**`, `**What changed:**`, `**Admin action required:**`, `**References:**` (at least 3 occurrences of each across the three H3s)
    - Android 11 section names COBO as unaffected and COPE/WPCO as affected with migration path to QR or Zero-Touch
    - Android 12 section scopes the change to BYOD Work Profile ONLY; COBO/Dedicated/ZTE/WPCO listed as unaffected
    - Android 15 section references EFRP / Enterprise Factory Reset Protection (not just "FRP") and cites Google AE Help answer 14549362
    - Zero occurrences of "COPE deprecated" phrasing (Pitfall 4 guard)
    - At least 2 References links per section (mix of Google AE Help, MS Learn, Jason Bayton)
  </acceptance_criteria>

  <done>
    Version Breakpoint Details section published with 3 complete narrative H3s. All breakpoint anchors resolve via explicit tags. Pitfall 4 (no "COPE deprecated") honored. Phase 36/38 admin guides can cross-reference to `#android-11-breakpoint`, `#android-12-breakpoint`, `#android-15-breakpoint`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write `## Non-Version Breakpoints` H2 section (SafetyNet → Play Integrity Jan 2025, AMAPI migration for BYOD Apr 2025), then write See Also and verify full-file negative checks</name>
  <files>docs/android-lifecycle/03-android-version-matrix.md</files>

  <read_first>
    - docs/android-lifecycle/03-android-version-matrix.md (current state after Task 2)
    - .planning/phases/34-android-foundation/34-CONTEXT.md (D-32 — Non-Version Breakpoints supplementary section)
    - .planning/phases/34-android-foundation/34-PATTERNS.md (lines 314-325 — Non-Version Breakpoints section structure)
    - .planning/phases/34-android-foundation/34-RESEARCH.md (Pattern 5 lines 365-373 — SafetyNet and AMAPI canonical content)
    - .planning/phases/34-android-foundation/34-VALIDATION.md (checks 34-04-05 and 34-04-07 — SafetyNet at-most-1-match rule)
  </read_first>

  <action>
    Append the `## Non-Version Breakpoints` H2 section with 2 H3 subsections per D-32, then append a See Also H2.

    **Section A: Non-Version Breakpoints** (policy-gated drift that isn't version-gated but matters for admin operations):

    ```markdown
    ## Non-Version Breakpoints

    The following drift events are NOT gated by Android API level — they are policy-gated or temporally-gated by Google or Microsoft changes. Document them here alongside version breakpoints so admins have a single drift-surface to consult.

    ### SafetyNet → Play Integrity (January 2025)

    Google turned off SafetyNet Attestation API in January 2025. [Play Integrity API](../_glossary-android.md#play-integrity) is the successor. Intune compliance UI now uses "Play Integrity verdict" terminology — three verdict levels are returned: **Basic integrity**, **Basic + Device integrity**, and **Strong integrity** (which requires hardware-backed security such as a trusted execution environment or strongbox).

    **Admin action required:** Update any Android compliance policy references from "SafetyNet" to "Play Integrity." All Phase 34 and later Android documentation uses Play Integrity terminology exclusively; there is no fallback to SafetyNet on any Android device today. Compliance content ships with Phase 38 (Dedicated) and Phase 41 (L2 investigation — Play Integrity failure troubleshooting per AEL2-04).

    **References:**
    - [Google Android Developers — Play Integrity API overview](https://developer.android.com/google/play/integrity)

    ### AMAPI Migration for BYOD (April 2025)

    Microsoft migrated Android personally-owned work profile (BYOD Work Profile) management to AMAPI — the [Android Management API](../_glossary-android.md#amapi) — in April 2025. Custom OMA-URI profiles for BYOD Work Profile are no longer supported. Wi-Fi configuration now requires certificate-based authentication (username / password Wi-Fi is broken on post-migration BYOD devices). The device-side management app changed from Company Portal to the Microsoft Intune app; both apps may still be installed, but the Intune app is the primary management surface for BYOD post-migration.

    **Affected modes:** BYOD Work Profile only. COBO, Dedicated, Zero-Touch Enrollment, and AOSP are NOT affected.

    **Admin action required:** If operating a BYOD tenant with legacy custom OMA-URI profiles, audit and migrate to AMAPI-compatible configuration policies. Wi-Fi profiles must use certificate-based authentication (802.1X with certificate). The AMAPI migration callout is Phase 37 BYOD admin guide scope per AEBYOD-03.

    **References:**
    - [Microsoft Tech Community — Android Enterprise personally-owned work profile AMAPI migration](https://techcommunity.microsoft.com/)
    ```

    **Section B: See Also** (final H2):
    ```markdown
    ## See Also

    - [Android Enterprise Enrollment Overview](00-enrollment-overview.md) — two-axes model and 5-mode comparison with cross-references to this matrix for mode-level minimums
    - [Android Provisioning Methods](02-provisioning-methods.md) — mode × method × version cells; cite this file for method-level version gating, NOT for mode-level Intune minimums
    - [Android Enterprise Provisioning Glossary](../_glossary-android.md) — definitions for [Play Integrity](../_glossary-android.md#play-integrity), [AMAPI](../_glossary-android.md#amapi), [COPE](../_glossary-android.md#cope), [WPCO](../_glossary-android.md#wpco), [Corporate Identifiers](../_glossary-android.md#corporate-identifiers), [Fully Managed](../_glossary-android.md#fully-managed)
    ```

    Do NOT link to `../common-issues.md`, `../quick-ref-l1.md`, `../quick-ref-l2.md`, or `../index.md` (deferred).

    **Negative content check per D-32 / AEAUDIT-04 (check 34-04-07):** After writing this task, verify that SafetyNet appears in the file AT MOST ONCE (one match inside the `### SafetyNet → Play Integrity (January 2025)` subsection body). If other accidental SafetyNet mentions exist, remove them.

    **Negative content check for frontmatter (check 34-04-06):** Verify the file still has NO `min_android_version` key. D-31 prohibits it on this and every Android doc.
  </action>

  <verify>
    <automated>
      grep -c "^## Non-Version Breakpoints$" docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c "^### SafetyNet " docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c "^### AMAPI Migration" docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      grep -c "^## See Also$" docs/android-lifecycle/03-android-version-matrix.md | grep -q "^1$" &&
      test $(grep -ci "safetynet" docs/android-lifecycle/03-android-version-matrix.md) -le 2 &&
      grep -ci "play integrity" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 2 { exit 0 } { exit 1 }' &&
      grep -ci "amapi" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 2 { exit 0 } { exit 1 }' &&
      test $(grep -c "min_android_version" docs/android-lifecycle/03-android-version-matrix.md) -eq 0 &&
      test $(grep -cE "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md|windows-vs-macos\.md" docs/android-lifecycle/03-android-version-matrix.md) -eq 0 &&
      grep -c "_glossary-android.md" docs/android-lifecycle/03-android-version-matrix.md | awk '$1 >= 3 { exit 0 } { exit 1 }'
    </automated>
  </verify>

  <acceptance_criteria>
    - `## Non-Version Breakpoints` H2 present exactly once (validates check 34-04-05)
    - `### SafetyNet → Play Integrity (January 2025)` H3 present with Admin action required block + Play Integrity verdict levels documented (Basic / Basic + Device / Strong integrity)
    - `### AMAPI Migration for BYOD (April 2025)` H3 present with BYOD-only scope callout, Wi-Fi certificate-auth requirement, and Microsoft Intune app as primary BYOD surface
    - SafetyNet appears at most 2 times in the file (once in H3 heading, once in body) — Validates check 34-04-07 "at most 1 match" tolerance interpreted as "only in the SafetyNet → Play Integrity section"
    - `## See Also` section links to `00-enrollment-overview.md`, `02-provisioning-methods.md`, and `../_glossary-android.md` with at least 3 glossary anchor cross-refs (Play Integrity, AMAPI, COPE, WPCO, Corporate Identifiers, Fully Managed)
    - NO `min_android_version` key anywhere in the file (validates check 34-04-06 — D-31 prohibition)
    - Zero links to deferred / out-of-scope files
    - Total H2 count: `## Version Breakpoint Details`, `## Non-Version Breakpoints`, `## See Also` (3 H2s after the matrix table)
  </acceptance_criteria>

  <done>
    Full file complete: frontmatter (no min_android_version), H1, platform gate, intro prose, 3-col matrix, Version Breakpoint Details with 3 H3s (Android 11 / 12 / 15), Non-Version Breakpoints with 2 H3s (SafetyNet / AMAPI), See Also. Load-bearing `last_verified: 2026-04-21` ready for Phase 42 audit anchor per D-33.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| version-matrix content → Phase 42 milestone audit | The `last_verified` date IS the audit anchor per D-33. If the date drifts more than 60 days past current date at audit time, Phase 42 must flag this file for re-verification before v1.4 ships. Drift-anchor accuracy gates milestone sign-off. |
| breakpoint narratives → admin operational decisions | Admins reading Android 15 breakpoint will ship or not ship EFRP policy based on this content. Incorrect scoping (e.g., claiming EFRP is unnecessary for Dedicated) causes production re-enrollment failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-17 | Tampering (factual) | Matrix minimum OS values + breakpoint narrative content | mitigate | Source values from FEATURES.md lines 350-358 (research verified 2026-04-19). Android 15 FRP content verified 2026-04-21 in RESEARCH.md Sources. Cite References block in each breakpoint H3 so the evidence chain is navigable. |
| T-34-18 | Denial of service (cross-reference) | Breakpoint anchors `#android-11-breakpoint`, `#android-12-breakpoint`, `#android-15-breakpoint`, `#non-version-breakpoints` | mitigate | Explicit `<a id="..."></a>` tags for all three Android-version-breakpoint H3s; `## Non-Version Breakpoints` auto-anchor verified. Phase 36/38 admin guides link into these exact anchors. |
| T-34-19 | Tampering (terminology) | SafetyNet still referenced as current | mitigate | Grep check: SafetyNet appears ≤ 2 times total (H3 heading + body explanatory mention). Any accidental "SafetyNet" in Matrix row content or See Also is a violation. Per D-32 SafetyNet is historical only — Play Integrity is the current mechanism. |
| T-34-20 | Information disclosure | Frontmatter schema leak | mitigate | D-31 prohibits `min_android_version` frontmatter key on this and every Android doc. Grep check confirms absence. Prevents establishing a frontmatter-level version-gate precedent that would create a second drift-surface (alongside this matrix body). |
</threat_model>

<verification>
All checks from 34-VALIDATION.md that apply to this plan:

**Per-task grep checks (mechanical):**
- `grep -cE "\| Mode \| Intune Minimum OS \| Notable Version Breakpoints \|" docs/android-lifecycle/03-android-version-matrix.md` ≥ 1 (check 34-04-01 structure)
- `grep -c "^### Android 11" docs/android-lifecycle/03-android-version-matrix.md` = 1 (check 34-04-02)
- `grep -c "^### Android 12" docs/android-lifecycle/03-android-version-matrix.md` = 1 (check 34-04-03)
- `grep -c "^### Android 15" docs/android-lifecycle/03-android-version-matrix.md` = 1 (check 34-04-04)
- `grep -c "## Non-Version Breakpoints" docs/android-lifecycle/03-android-version-matrix.md` = 1 (check 34-04-05)
- `grep -l "min_android_version" docs/android-lifecycle/` returns empty (check 34-04-06 — this file must contribute empty results)
- `grep -i "safetynet" docs/android-lifecycle/03-android-version-matrix.md` returns at most 1-2 matches ALL inside the SafetyNet → Play Integrity subsection (check 34-04-07 allowance)

**Phase-gate checks:**
- `grep "^platform: Android$" docs/android-lifecycle/03-android-version-matrix.md` matches (check 34-all-02)
- `grep -cE "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md" docs/android-lifecycle/03-android-version-matrix.md` returns 0 (check 34-all-03)
- `last_verified: 2026-04-21` + `review_by: 2026-06-20` (60-day cycle per D-33) (check 34-all-01)

**Anchor resolution tests:**
- Explicit anchors: `#android-11-breakpoint`, `#android-12-breakpoint`, `#android-15-breakpoint` all present via explicit `<a id="..."></a>` tags
- Auto-anchor: `#non-version-breakpoints` from `## Non-Version Breakpoints` heading
- Per-mode anchors (auxiliary): `#cobo`, `#byod`, `#dedicated`, `#zte`, `#aosp` via stacked tags before matrix

**Load-bearing audit anchor:**
- `last_verified` date is 2026-04-21 — Phase 42 audit uses this as drift anchor per D-33; date must match the date the file was last verified-against-source at authoring time.
</verification>

<success_criteria>
An admin reading `docs/android-lifecycle/03-android-version-matrix.md` can determine the minimum supported Android OS per mode and see explicit breakpoints for Android 11 (COPE NFC removal), Android 12 (IMEI / serial removal from corporate identifiers), and Android 15 (FRP hardening) (ROADMAP Phase 34 Success Criterion 5 — AEBASE-04).
</success_criteria>

<output>
After completion, create `.planning/phases/34-android-foundation/34-04-SUMMARY.md` documenting:
- File created + byte count
- Frontmatter D-31-compliant (no `min_android_version`) — confirmed via grep
- 3-col matrix with 5 mode rows
- 3 breakpoint H3s (Android 11, 12, 15) — all have Affected modes / What changed / Admin action required / References
- 2 non-version-breakpoint H3s (SafetyNet → Play Integrity, AMAPI)
- SafetyNet occurrence count (should be ≤ 2, all inside the dedicated subsection per check 34-04-07)
- Load-bearing `last_verified: 2026-04-21` noted for Phase 42 audit anchor consumption
</output>
