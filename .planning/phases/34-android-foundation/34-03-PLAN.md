---
phase: 34-android-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 34-01
  - 34-02
files_modified:
  - docs/android-lifecycle/02-provisioning-methods.md
autonomous: true
requirements:
  - AEBASE-03
user_setup: []

must_haves:
  truths:
    - "An admin reading `docs/android-lifecycle/02-provisioning-methods.md` can identify for any given enrollment mode which of NFC / QR / afw#setup / Zero-Touch provisioning methods are supported — by locating the mode row and scanning across"
    - "Each supported cell shows the min Android version where the method works for that mode (e.g., '✓ Android 9+ built-in; Android 7-8 needs external reader')"
    - "A pre-table Samsung Knox Mobile Enrollment (KME) mutual-exclusion blockquote warns admins that KME and Zero-Touch are mutually exclusive on the same Samsung device, with a forward-ref that KME is deferred to v1.4.1"
    - "A Notes column captures mode-level constraints that don't fit per-cell (e.g., COBO: Dual-SIM register IMEI 1; Dedicated: MHS exit-PIN sync; AOSP: OEM firmware-specific)"
    - "This is THE SINGLE canonical provisioning-method matrix in v1.4 — no mode guides in Phases 36-39 duplicate this grid (Anti-Pattern 1 guard)"
  artifacts:
    - path: "docs/android-lifecycle/02-provisioning-methods.md"
      provides: "Canonical mode × method matrix with version-annotated cells, Samsung KME mutual-exclusion callout, Notes column, KME deferral footer"
      contains:
        - "| Mode | NFC | QR | afw#setup | Zero-Touch | Notes |"
        - "Samsung"
        - "Knox Mobile Enrollment"
        - "v1.4.1"
        - "Fully Managed (COBO)"
        - "BYOD Work Profile"
        - "Dedicated (COSU)"
        - "Zero-Touch Enrollment"
        - "AOSP"
  key_links:
    - from: "docs/android-lifecycle/02-provisioning-methods.md"
      to: "docs/android-lifecycle/00-enrollment-overview.md"
      via: "Back-reference in See Also + in-body links"
      pattern: "00-enrollment-overview\\.md"
    - from: "docs/android-lifecycle/02-provisioning-methods.md"
      to: "docs/_glossary-android.md"
      via: "Term cross-references (afw#setup, Zero-Touch Enrollment, Fully Managed, Managed Home Screen)"
      pattern: "_glossary-android\\.md"
---

<objective>
Create `docs/android-lifecycle/02-provisioning-methods.md` — the canonical provisioning-method matrix comparing NFC, QR, afw#setup, and Zero-Touch across all five Android enrollment modes with Android version availability embedded in cells (AEBASE-03 / ROADMAP Success Criterion 4).

Purpose: This is the SINGLE authoritative reference for which provisioning methods work with which mode at which Android version. Mode guides in Phase 36-39 reference filtered rows from this matrix via anchor links rather than duplicating the grid (Anti-Pattern 1 guard). If two copies drift, admins get contradictory guidance.

Output: One new markdown file at `docs/android-lifecycle/02-provisioning-methods.md` with frontmatter → platform-gate blockquote → introductory prose explaining the mode-first reader model → Samsung KME mutual-exclusion blockquote (D-27) → 5×5 matrix table (5 mode rows × 4 method cols + Notes) → per-method H2 subsections with anchor stability → KME deferral H2 footer → See Also.

This plan MUST honor 6 locked decisions: D-23 (mode-rows × method-cols orientation), D-24 (version annotations embedded in cells — NOT a separate column), D-25 (Notes supplementary column), D-26 (single canonical matrix — Anti-Pattern 1 guard), D-27 (Samsung KME mutual-exclusion callout placed adjacent-to-ZT-column, interpreted as pre-table blockquote per Open Question 3 Recommendation A), D-28 (no KME row — footer mention only).

**Wave 2 context:** This plan depends on Plans 01 (glossary — term anchors for afw#setup, Fully Managed, etc.) and 02 (enrollment overview — mode H3 anchors like `#fully-managed-cobo`, `#byod-work-profile`, `#dedicated-cosu`, `#zero-touch-enrollment-zte`, `#aosp`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-android-foundation/34-CONTEXT.md
@.planning/phases/34-android-foundation/34-RESEARCH.md
@.planning/phases/34-android-foundation/34-PATTERNS.md
@.planning/phases/34-android-foundation/34-VALIDATION.md
@.planning/research/FEATURES.md
@docs/_glossary-android.md
@docs/android-lifecycle/00-enrollment-overview.md
@docs/reference/ios-capability-matrix.md

<interfaces>
<!-- Anchors this file MUST publish (Phase 35-42 load-bearing cross-reference targets). -->
<!-- Source: 34-RESEARCH.md Pitfall 7 lines 479-486, 34-PATTERNS.md Shared Patterns → Anchor stability registry. -->

Per-method anchors (Phase 36-39 mode guides filter-link into these):
  #nfc
  #qr
  #afw-setup               (requires explicit <a id="afw-setup"></a> tag due to GitHub kebab-case anomaly with #)
  #zero-touch
  #samsung-kme-mutual-exclusion
  #knox-mobile-enrollment-kme---deferred-to-v141   (KME deferral H2)

Per-mode row-target anchors (for enrollment-overview Provisioning Surface column links):
  The enrollment overview Task 2 links targets:
    02-provisioning-methods.md#cobo
    02-provisioning-methods.md#byod-work-profile
    02-provisioning-methods.md#dedicated-cosu
    02-provisioning-methods.md#zero-touch
    02-provisioning-methods.md#aosp
  These anchors must resolve — use explicit <a id="cobo"></a>, <a id="byod-work-profile"></a>,
  <a id="dedicated-cosu"></a>, <a id="aosp"></a> tags at appropriate sections if the default
  H2 kebab-case doesn't produce them directly.

Frontmatter:
  last_verified: 2026-04-21
  review_by: 2026-06-20
  applies_to: both
  audience: admin
  platform: Android

Incoming references (already authored in Wave 1):
  From docs/android-lifecycle/00-enrollment-overview.md Provisioning Surface column cells:
    "see [02-provisioning-methods.md#cobo](02-provisioning-methods.md#cobo)" etc. for each mode.

Term cross-references this file makes:
  ../_glossary-android.md#afw-setup
  ../_glossary-android.md#zero-touch-enrollment
  ../_glossary-android.md#fully-managed
  ../_glossary-android.md#managed-home-screen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold file with frontmatter, platform gate, H1, intro paragraph explaining mode-first reader model, and pre-table Samsung KME mutual-exclusion blockquote</name>
  <files>docs/android-lifecycle/02-provisioning-methods.md</files>

  <read_first>
    - docs/reference/ios-capability-matrix.md (lines 1-15 — frontmatter + H1 + intro prose precedent for reference-matrix docs)
    - docs/_glossary-android.md (from Plan 01 — for term cross-reference targets)
    - docs/android-lifecycle/00-enrollment-overview.md (from Plan 02 — verify incoming links `#cobo`, `#byod-work-profile`, etc. match anchors this file publishes)
    - .planning/phases/34-android-foundation/34-CONTEXT.md (D-23 through D-28 — matrix decisions)
    - .planning/phases/34-android-foundation/34-PATTERNS.md (section "`docs/android-lifecycle/02-provisioning-methods.md`" lines 204-267 — full pattern guidance with verbatim table shape)
    - .planning/phases/34-android-foundation/34-RESEARCH.md (Pattern 4 lines 303-324 — canonical matrix with full 5-row example + KME callout; Open Question 3 Recommendation A for pre-table blockquote placement)
    - .planning/research/FEATURES.md (lines 329-336 — source matrix data; orientation in source is method-rows, published is mode-rows per D-23)
  </read_first>

  <action>
    Create `docs/android-lifecycle/02-provisioning-methods.md` with the following content:

    1. **Frontmatter** (YAML between `---` fences):
       ```yaml
       ---
       last_verified: 2026-04-21
       review_by: 2026-06-20
       applies_to: both
       audience: admin
       platform: Android
       ---
       ```

    2. **H1 title**:
       ```markdown
       # Android Provisioning Methods
       ```

    3. **Platform gate blockquote**:
       ```markdown
       > **Platform gate:** This reference matrix covers the four Android Enterprise provisioning methods supported in Microsoft Intune — NFC, QR code, DPC identifier (`afw#setup`), and Zero-Touch — across all five enrollment modes (Fully Managed COBO, BYOD Work Profile, Dedicated COSU, Zero-Touch Enrollment, AOSP).
       > For iOS/iPadOS enrollment methods, see [iOS/iPadOS Enrollment Overview](../ios-lifecycle/00-enrollment-overview.md). For Android terminology, see the [Android Enterprise Provisioning Glossary](../_glossary-android.md).
       ```

    4. **Introductory paragraph** explaining the mode-first reader model per D-23:
       ```markdown
       This matrix is organized mode-first — readers locate their target enrollment mode (COBO, BYOD Work Profile, Dedicated, Zero-Touch Enrollment, AOSP) in the row list, then scan across to see which provisioning methods are supported and at which Android version. Cells carry both the support indicator and the version gate; the Notes column captures mode-level constraints that apply regardless of method (e.g., dual-SIM IMEI registration for COBO, Managed Home Screen exit-PIN sync for Dedicated, OEM firmware dependency for AOSP).

       This is the SINGLE canonical provisioning-method reference for v1.4. Downstream admin guides in [admin-setup-android/](../admin-setup-android/) (Phase 35-39) reference filtered rows from this matrix via anchor links rather than duplicating the grid. If you are authoring a mode-specific admin guide, link to the appropriate row in this file rather than copying the matrix.
       ```

    5. **Samsung KME mutual-exclusion blockquote** per D-27 (placed IMMEDIATELY before the matrix table — "adjacent to Zero-Touch column header" interpreted per Open Question 3 Recommendation A as pre-table visually-contiguous blockquote):
       ```markdown
       > **Samsung devices:** Knox Mobile Enrollment (KME) is mutually exclusive with Zero-Touch on the same Samsung device. Configure only one. KME is deferred to v1.4.1; see the [KME deferral note](#knox-mobile-enrollment-kme---deferred-to-v141) at the bottom of this page.
       ```

    At this point the file has: frontmatter + H1 + platform-gate + intro prose + KME mutual-exclusion pre-table blockquote. Task 2 adds the 5×5 matrix table immediately below this blockquote.

    **Content gates:**
    - Platform-gate blockquote does NOT link to `../common-issues.md`, `../quick-ref-l1.md`, `../quick-ref-l2.md`, or `../index.md` (Pitfall 8).
    - The KME callout is a blockquote (starts with `>`), not a markdown comment or an H2 section.
  </action>

  <verify>
    <automated>
      test -f docs/android-lifecycle/02-provisioning-methods.md &&
      grep -c "^platform: Android$" docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c "^# Android Provisioning Methods$" docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -ci "Samsung" docs/android-lifecycle/02-provisioning-methods.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -ci "Knox Mobile Enrollment" docs/android-lifecycle/02-provisioning-methods.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "v1.4.1" docs/android-lifecycle/02-provisioning-methods.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "^> \*\*Samsung devices:" docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      test $(grep -cE "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md" docs/android-lifecycle/02-provisioning-methods.md) -eq 0
    </automated>
  </verify>

  <acceptance_criteria>
    - `docs/android-lifecycle/02-provisioning-methods.md` exists
    - Frontmatter has `platform: Android`, `last_verified: 2026-04-21`, `review_by: 2026-06-20`, `audience: admin`, `applies_to: both`
    - H1 is `# Android Provisioning Methods`
    - Platform-gate blockquote present and does NOT link to deferred files
    - Introductory prose explicitly states "mode-first" organization and references downstream mode guides using filtered-row link pattern
    - Samsung KME blockquote starts with `> **Samsung devices:**`, mentions "Knox Mobile Enrollment", "mutually exclusive with Zero-Touch", and "deferred to v1.4.1" (validates checks 34-03-02 and 34-03-04)
    - No deferred-file links
  </acceptance_criteria>

  <done>
    Scaffold committed through the pre-table Samsung KME mutual-exclusion blockquote. Ready for Task 2 to add the 5×5 matrix immediately below.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the 5-row × 5-column provisioning matrix (mode rows × method cols + Notes) with embedded version annotations in cells</name>
  <files>docs/android-lifecycle/02-provisioning-methods.md</files>

  <read_first>
    - docs/android-lifecycle/02-provisioning-methods.md (current state after Task 1)
    - .planning/phases/34-android-foundation/34-CONTEXT.md (D-23 through D-26 — orientation/cells/notes)
    - .planning/phases/34-android-foundation/34-PATTERNS.md (lines 238-254 — verbatim matrix code with cell-value rules)
    - .planning/phases/34-android-foundation/34-RESEARCH.md (Pattern 4 lines 309-322 — canonical matrix content + lines 562-568 — canonical COBO row cell examples)
    - .planning/research/FEATURES.md (lines 329-336 — data source; flipped orientation per D-23)
    - .planning/phases/34-android-foundation/34-VALIDATION.md (checks 34-03-01 and 34-03-03)
  </read_first>

  <action>
    Immediately after the Samsung KME pre-table blockquote, write the 5-row × 5-column matrix with this EXACT header and 5 data rows (matches PATTERNS.md lines 240-247 + RESEARCH.md lines 315-322):

    ```markdown
    | Mode | NFC | QR | afw#setup | Zero-Touch | Notes |
    |------|-----|-----|-----------|------------|-------|
    | <a id="cobo"></a>Fully Managed (COBO) | ✓ (Android 8+; NFC tag with ≥ 888 bytes) | ✓ (Android 9+ built-in reader; Android 7-8 needs external QR reader app) | ✓ (all supported Android) | ✓ (Android 8+, via [Zero-Touch portal](https://enterprise.google.com/android/zero-touch/customers)) | Dual-SIM devices: register IMEI 1 only — see Phase 35 prerequisites. See also `../_glossary-android.md#cobo`. |
    | <a id="byod-work-profile"></a>BYOD Work Profile | ✗ | ✗ | ✗ | ✗ | Company Portal user-initiated enrollment only; no corporate provisioning methods apply. AMAPI migration April 2025 — Microsoft Intune app is the primary management surface post-migration. See `../_glossary-android.md#byod`. |
    | <a id="dedicated-cosu"></a>Dedicated (COSU) | ✓ (Android 8+) | ✓ (Android 9+ built-in reader) | ✓ (all supported Android) | ✓ (Android 8+) | Managed Home Screen exit-PIN must sync between device-restrictions profile and MHS app config — see Phase 38 and `../_glossary-android.md#managed-home-screen`. |
    | <a id="zero-touch"></a>Zero-Touch Enrollment | — | — | — | ✓ (Android 8+, via portal) | ZT is itself a provisioning method — reseller relationship is Step 0; see Phase 35 `02-zero-touch-portal.md`. |
    | <a id="aosp"></a>AOSP | ✗ | ✓ (Android 10+, one device at a time, Wi-Fi credentials embedded in QR for RealWear-class OEMs) | ✗ | ✗ | OEM firmware-specific; full OEM matrix deferred to v1.4.1 — see Phase 39 AOSP stub. |
    ```

    **Cell-value rules per D-24 and D-25:**
    - `✓` = supported (use the Unicode check mark U+2713 consistent with the rest of the codebase; inspect existing docs with Grep if uncertain).
    - `✗` = not supported (Unicode U+2717).
    - `—` = not applicable (use an em dash for the Zero-Touch Enrollment row's non-ZT method cells because ZT cannot be "not supported" for its own methods — it simply doesn't apply to compare with itself).
    - Version-gating embedded in `✓` cells: `✓ (Android N+ ...)` format. At minimum one per mode row supporting a method.
    - Notes column: mode-level constraints + glossary cross-reference at the end of each Notes cell.

    **Anchor tags per D-26 Integration Points requirement:** Add explicit `<a id="..."></a>` tags in the Mode column cells so incoming links from the enrollment overview (`02-provisioning-methods.md#cobo`, `#byod-work-profile`, `#dedicated-cosu`, `#zero-touch`, `#aosp`) resolve. Without explicit anchors, GitHub's default anchor generation from table-cell text is unreliable — and Phase 35-42 references depend on these exact anchors.

    **Verification content gates:**
    - Table must have exactly 5 data rows (one per mode).
    - Table must have 6 columns (Mode + 4 methods + Notes).
    - At least 4 cells must contain `Android N+` version annotations per check 34-03-03 (`grep -cE "Android [0-9]+" docs/android-lifecycle/02-provisioning-methods.md` ≥ 4).
    - BYOD row has ✗ in all 4 method columns (corporate provisioning methods don't apply to BYOD Work Profile per the reader-model rationale in CONTEXT.md).
    - Zero-Touch Enrollment row has `—` in the NFC/QR/afw#setup columns (not applicable — ZT IS the method).
    - AOSP row shows QR-only support with Android 10+ gate.
  </action>

  <verify>
    <automated>
      grep -cE "^\| Mode \| NFC \| QR \| afw#setup \| Zero-Touch \| Notes \|$" docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c "Fully Managed (COBO)" docs/android-lifecycle/02-provisioning-methods.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "BYOD Work Profile" docs/android-lifecycle/02-provisioning-methods.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "Dedicated (COSU)" docs/android-lifecycle/02-provisioning-methods.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "Zero-Touch Enrollment" docs/android-lifecycle/02-provisioning-methods.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c "^| <a id=\"aosp\"></a>AOSP\|^| AOSP" docs/android-lifecycle/02-provisioning-methods.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -cE "Android [0-9]+" docs/android-lifecycle/02-provisioning-methods.md | awk '$1 >= 4 { exit 0 } { exit 1 }' &&
      grep -c '<a id="cobo"></a>' docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c '<a id="byod-work-profile"></a>' docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c '<a id="dedicated-cosu"></a>' docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c '<a id="zero-touch"></a>' docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c '<a id="aosp"></a>' docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$"
    </automated>
  </verify>

  <acceptance_criteria>
    - Table header row `| Mode | NFC | QR | afw#setup | Zero-Touch | Notes |` appears exactly once (validates check 34-03-01 structure)
    - Exactly 5 data rows, one per mode: Fully Managed (COBO), BYOD Work Profile, Dedicated (COSU), Zero-Touch Enrollment, AOSP
    - Version annotations appear in ≥ 4 cells using `Android N+` format (validates check 34-03-03: `grep -cE "Android [0-9]+" docs/android-lifecycle/02-provisioning-methods.md` ≥ 4)
    - All 5 explicit `<a id="..."></a>` anchor tags present in Mode column cells: `cobo`, `byod-work-profile`, `dedicated-cosu`, `zero-touch`, `aosp` (resolves incoming links from `00-enrollment-overview.md`)
    - BYOD row has `✗` in all 4 method columns (NFC / QR / afw#setup / Zero-Touch)
    - Zero-Touch Enrollment row has `—` in the NFC / QR / afw#setup columns
    - AOSP row shows QR support only, with Android 10+ version gate
    - Notes column cells link back to `../_glossary-android.md` anchors or forward-reference Phase 35/38/39
  </acceptance_criteria>

  <done>
    Canonical 5×5 matrix with all 5 mode rows and 6 columns in place. Version annotations embedded in cells per D-24. Notes column captures mode-level constraints per D-25. Explicit anchor tags resolve incoming enrollment-overview links.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write per-method H2 subsections (with method anchors), KME deferral H2 footer, and See Also section</name>
  <files>docs/android-lifecycle/02-provisioning-methods.md</files>

  <read_first>
    - docs/android-lifecycle/02-provisioning-methods.md (current state after Task 2)
    - docs/_glossary-android.md (for term cross-reference targets: afw#setup, Zero-Touch Enrollment)
    - .planning/phases/34-android-foundation/34-CONTEXT.md (D-28 — KME deferral footer; Claude's Discretion — exact cell content author discretion)
    - .planning/phases/34-android-foundation/34-PATTERNS.md (lines 255-267 — KME deferral footer pattern; Shared Patterns → Anchor stability for per-method anchors #nfc, #qr, #afw-setup, #zero-touch)
    - .planning/phases/34-android-foundation/34-RESEARCH.md (Pitfall 7 lines 479-486 — mandatory per-method anchors)
  </read_first>

  <action>
    Append the following sections after the matrix table.

    **Section A: Per-Method Details** — 4 H2 subsections, one per method. These sections establish the canonical anchors `#nfc`, `#qr`, `#afw-setup`, `#zero-touch` that Phase 36-39 admin guides cross-reference (per Pitfall 7 anchor registry). Each H2 is ~60-100 words of method-level context.

    ```markdown
    ## Per-Method Details

    ### NFC

    Near-Field Communication provisioning uses an NFC tag (≥ 888 bytes) encoded with an Android Enterprise provisioning payload. An admin initially configures one "source" NFC tag; unenrolled (freshly reset) target devices receive the configuration by bumping against the tag. Android 8+ required. Not supported for BYOD Work Profile (personally-owned devices do not use corporate NFC flows) or AOSP (OEM-dependent).

    <a id="qr"></a>
    ### QR

    QR code provisioning uses a QR payload encoded with the enrollment configuration. Android 9+ has a built-in QR reader in the setup wizard; Android 7-8 requires an external QR reader app installed before setup. Works for Fully Managed COBO, Dedicated (COSU), and AOSP (Android 10+, one device at a time, Wi-Fi credentials required in the QR payload for RealWear-class OEMs). Not supported for BYOD Work Profile.

    ### afw#setup

    The DPC identifier string (`afw#setup`) — literally the text "afw#setup" entered in place of a Google account during device setup — triggers the Android Enterprise DPC download on freshly-reset fully-managed-eligible devices. Supported on all fully-managed-eligible Android versions. See [Android Glossary — afw#setup](../_glossary-android.md#afw-setup). Applies to COBO and Dedicated; does not apply to BYOD Work Profile or AOSP.

    <a id="zero-touch"></a>
    ### Zero-Touch

    Zero-Touch provisioning is corporate-scale: Android OEM resellers upload device IMEI and serial lists to the [Zero-Touch portal](https://enterprise.google.com/android/zero-touch/customers), and those devices enroll automatically on first boot. Reseller relationship is Step 0 — the portal requires a reseller identity. Android 8+. Applies to COBO, Dedicated, and Zero-Touch Enrollment (ZTE is a mode that uses Zero-Touch provisioning). See [Android Glossary — Zero-Touch Enrollment](../_glossary-android.md#zero-touch-enrollment). Samsung devices: see the [Samsung KME mutual-exclusion note](#samsung-kme-mutual-exclusion) above.
    ```

    **Critical: Anchor tag placement for #zero-touch.** The matrix table already has `<a id="zero-touch"></a>` on the ZTE row per Task 2. A second anchor with the same id is technically a duplicate and GitHub tolerates it but style-wise confusing. Use a different explicit anchor `<a id="zero-touch-method"></a>` before the `### Zero-Touch` method H3 OR accept duplicate and rely on the first-declared-wins rule.

    **Simpler interpretation:** Phase 35-42 cross-references into `02-provisioning-methods.md#zero-touch` primarily target the METHOD section (not the matrix row). The matrix row's `<a id="zero-touch"></a>` is there to resolve `#zero-touch` incoming from the enrollment overview's Provisioning Surface column. Both in one file is acceptable — the H3 heading `### Zero-Touch` will also produce an auto-anchor `#zero-touch` duplicating. Keep both; consistent behavior.

    **For `#afw-setup`** — same as the glossary (Plan 01 Task 2): since the H3 heading `### afw#setup` produces an unreliable anchor due to `#` in heading text, add an explicit anchor tag. The `<a id="afw-setup"></a>` placement is NOT included above — add it immediately before `### afw#setup`:

    ```markdown
    <a id="afw-setup"></a>
    ### afw#setup
    ```

    Update the action above to include this anchor tag.

    **Section A** (also add `<a id="samsung-kme-mutual-exclusion"></a>` BEFORE the Samsung KME pre-table blockquote in Task 1's output — edit the file to insert this anchor tag right above the blockquote so `#samsung-kme-mutual-exclusion` link target resolves).

    Since the pre-table blockquote itself doesn't generate an anchor, the executor must go back and insert:
    ```markdown
    <a id="samsung-kme-mutual-exclusion"></a>
    > **Samsung devices:** Knox Mobile Enrollment (KME)...
    ```

    **Section B: KME Deferral Footer H2** per D-28:
    ```markdown
    ## Knox Mobile Enrollment (KME) — Deferred to v1.4.1

    Knox Mobile Enrollment is Samsung's Zero-Touch-equivalent for Samsung hardware. A KME row will be added to the matrix above in v1.4.1 per [PROJECT.md Key Decisions](../../.planning/PROJECT.md). For v1.4, treat Samsung devices as Zero-Touch-eligible per the [Samsung KME mutual-exclusion note](#samsung-kme-mutual-exclusion) at the top of this page. Do NOT configure both KME and Zero-Touch on the same Samsung device — KME takes precedence at the device firmware level.

    Full KME admin documentation (binding Knox Admin Portal to Intune, Samsung reseller workflow, KME-specific failure modes) is tracked in the v1.4 deferred-items list and will ship in v1.4.1.
    ```

    The H2 heading `## Knox Mobile Enrollment (KME) — Deferred to v1.4.1` generates an anchor approximately `#knox-mobile-enrollment-kme---deferred-to-v141` (GitHub's kebab-case treatment of ` — ` is `---`). The `#knox-mobile-enrollment-kme---deferred-to-v141` target already exists as the link target from the pre-table Samsung blockquote (Task 1). Verify the anchor resolves; if GitHub produces a different kebab-case, add explicit `<a id="knox-mobile-enrollment-kme---deferred-to-v141"></a>` tag.

    **Section C: See Also** (final H2):
    ```markdown
    ## See Also

    - [Android Enterprise Enrollment Overview](00-enrollment-overview.md) — two-axes model and 5-mode comparison; references filtered rows from this matrix
    - [Android Version Matrix](03-android-version-matrix.md) — minimum Intune-supported Android OS per mode; version breakpoint narratives (Android 11, 12, 15)
    - [Android Enterprise Provisioning Glossary](../_glossary-android.md) — definitions for [afw#setup](../_glossary-android.md#afw-setup), [Zero-Touch Enrollment](../_glossary-android.md#zero-touch-enrollment), [Managed Home Screen](../_glossary-android.md#managed-home-screen)
    ```

    Do NOT link to `../common-issues.md`, `../quick-ref-l1.md`, `../quick-ref-l2.md`, `../index.md`, or `../windows-vs-macos.md` (all deferred).
  </action>

  <verify>
    <automated>
      grep -c "^## Per-Method Details$" docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c "^### NFC$" docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c "^### QR$" docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c "^### afw#setup$" docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c "^### Zero-Touch$" docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c '<a id="afw-setup"></a>' docs/android-lifecycle/02-provisioning-methods.md | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
      grep -c '<a id="samsung-kme-mutual-exclusion"></a>' docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c "^## Knox Mobile Enrollment (KME) — Deferred to v1.4.1$" docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      grep -c "^## See Also$" docs/android-lifecycle/02-provisioning-methods.md | grep -q "^1$" &&
      test $(grep -cE "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md|windows-vs-macos\.md" docs/android-lifecycle/02-provisioning-methods.md) -eq 0
    </automated>
  </verify>

  <acceptance_criteria>
    - `## Per-Method Details` H2 present with 4 H3 subsections: `### NFC`, `### QR`, `### afw#setup`, `### Zero-Touch`
    - Explicit `<a id="afw-setup"></a>` anchor tag immediately before `### afw#setup` (resolves `#afw-setup` incoming links from glossary and Phase 35-42)
    - Explicit `<a id="samsung-kme-mutual-exclusion"></a>` anchor tag immediately before the pre-table Samsung KME blockquote (resolves `#samsung-kme-mutual-exclusion` link target used in QR/Zero-Touch H3 body and in the KME deferral footer)
    - `## Knox Mobile Enrollment (KME) — Deferred to v1.4.1` H2 exists with content noting v1.4.1 deferral, Samsung mutual-exclusion reminder, and forward-ref to full KME docs in v1.4.1
    - `## See Also` section links to `00-enrollment-overview.md`, `03-android-version-matrix.md`, `../_glossary-android.md`
    - Zero links to deferred / out-of-scope files: `grep -cE "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md|windows-vs-macos\.md" docs/android-lifecycle/02-provisioning-methods.md` returns 0
    - All 4 method H3 sections contain at least one cross-reference to `../_glossary-android.md#<anchor>`
  </acceptance_criteria>

  <done>
    Per-method H2 subsections establish canonical anchors for Phase 36-39. KME deferral footer documents why KME is absent from the matrix with forward-pointer to v1.4.1. See Also links the 4 Phase 34 deliverables. All anchors (method + Samsung KME) resolve via explicit tags.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| matrix content → admin operational decisions | Admins use this matrix to decide which provisioning method to attempt for a given mode. A missing `✓` (or a `✓` where `✗` should be) misdirects enrollment attempts; incorrect version gating causes unexplained failures on older devices. |
| canonical matrix → Phase 36-39 filtered-row consumers | Phase 36-39 mode guides link to anchors (`#cobo`, `#byod-work-profile`, etc.) in this file. Anchor text changes silently break downstream links. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-13 | Tampering (factual) | Matrix cell version annotations | mitigate | Version values sourced from FEATURES.md lines 329-336 (verified 2026-04-19) + RESEARCH.md Sources (2026-04-21 web verification). Any cell claiming Android version N+ must match the research source; manual review at verification time. |
| T-34-14 | Denial of service (cross-reference) | Anchor stability for `#cobo`, `#byod-work-profile`, `#dedicated-cosu`, `#zero-touch`, `#aosp`, `#afw-setup`, `#samsung-kme-mutual-exclusion` | mitigate | Explicit `<a id="..."></a>` tags added at each target location. Anchors tested via markdown-preview inspection at plan execution time. |
| T-34-15 | Tampering (scope) | Knox Mobile Enrollment inclusion | mitigate | D-28 explicitly defers KME to v1.4.1. KME appears only in (a) pre-table Samsung mutual-exclusion blockquote and (b) `## Knox Mobile Enrollment (KME) — Deferred to v1.4.1` footer. No KME row in the data grid. Grep check: matrix header row pattern must not include "Knox" or "KME" in any column. |
| T-34-16 | Information disclosure | QR code content | mitigate | QR codes are sensitive artifacts (embed enrollment tokens); document GENERATION process only, never embed actual QR code images. RESEARCH.md Anti-Patterns line 390 explicitly prohibits QR images. |
</threat_model>

<verification>
All checks from 34-VALIDATION.md that apply to this plan:

**Per-task grep checks:**
- `grep -cE "\| Mode \| NFC \| QR \| afw#setup \| Zero-Touch \| Notes \|" docs/android-lifecycle/02-provisioning-methods.md` returns 1, and table has 5 mode rows (check 34-03-01 structure)
- `grep -c -i "knox mobile enrollment" docs/android-lifecycle/02-provisioning-methods.md` ≥ 1 (check 34-03-02)
- `grep -cE "Android [0-9]+" docs/android-lifecycle/02-provisioning-methods.md` ≥ 4 (check 34-03-03)
- `grep -c "v1.4.1" docs/android-lifecycle/02-provisioning-methods.md` ≥ 1 (check 34-03-04)

**Phase-gate checks:**
- `grep "^platform: Android$" docs/android-lifecycle/02-provisioning-methods.md` matches (check 34-all-02)
- `grep -cE "common-issues\.md|quick-ref-l1\.md|quick-ref-l2\.md" docs/android-lifecycle/02-provisioning-methods.md` returns 0 (check 34-all-03)

**Anchor resolution tests:**
- Incoming from enrollment-overview.md: `#cobo`, `#byod-work-profile`, `#dedicated-cosu`, `#zero-touch`, `#aosp` — all explicit anchor tags in Mode column
- Internal cross-refs: `#samsung-kme-mutual-exclusion` (explicit tag), `#afw-setup` (explicit tag), `#knox-mobile-enrollment-kme---deferred-to-v141` (from H2 heading — verify)

**Manual semantic checks:**
- AOSP row Notes cell mentions "OEM firmware-specific" and forward-refs to Phase 39 AOSP stub
- BYOD row Notes cell mentions AMAPI migration April 2025 and Microsoft Intune app as primary BYOD surface
- Dedicated row Notes cell mentions Managed Home Screen exit-PIN sync and Phase 38 reference
</verification>

<success_criteria>
An admin reading `docs/android-lifecycle/02-provisioning-methods.md` can identify for any given enrollment mode which of the four provisioning methods (NFC, QR, DPC identifier afw#setup, Zero-Touch) are supported and the Android version availability per method (ROADMAP Phase 34 Success Criterion 4 — AEBASE-03).
</success_criteria>

<output>
After completion, create `.planning/phases/34-android-foundation/34-03-SUMMARY.md` documenting:
- File created + byte count
- Matrix structure confirmed: 5 mode rows × 4 method cols + Notes = 5 columns-of-cells per row + Notes
- KME mutual-exclusion and deferral footers both present
- All explicit anchor tags inserted (5 mode anchors + afw-setup + samsung-kme-mutual-exclusion)
- Incoming enrollment-overview links resolved (transient state from Plan 02 Wave 1 now resolves)
- Version annotation count (should be ≥ 4 per check 34-03-03)
</output>
