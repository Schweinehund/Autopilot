---
phase: 125-v115-pin-14th-path-a-lineage-bump-terminal-close
plan: 02
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - scripts/validation/v1.16-milestone-audit.mjs
  - scripts/validation/v1.16-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
autonomous: true
requirements: [HARN-06]
must_haves:
  truths:
    - "v1.16-milestone-audit.mjs is a faithful Path-A copy of v1.15 (C1-C17 inherited verbatim, C17 already folded as check id 17) that exits 0 --self-test on the post-Phase-124 tree"
    - "v1.16-audit-allowlist.json is a VERBATIM copy of the v1.15 sidecar (phase field + generated timestamp repointed only) — NO line-pin repoint, per the 125-01 byte-unchanged evidence"
    - "The single functional string (the parseAllowlist sidecar path) reads scripts/validation/v1.16-audit-allowlist.json"
    - "BASELINE_20 freshness comment is back-anchored to the JIT pre-Atom-1 HEAD SHA (captured immediately before the commit), never the Wave-0 SHA and never the close-gate SHA"
    - "Atom 1 lands as ONE indivisible commit of exactly three files; it is never folded into Atom 2"
  artifacts:
    - path: "scripts/validation/v1.16-milestone-audit.mjs"
      provides: "14th Path-A milestone audit harness, C1-C17"
      contains: "id: 17"
    - path: "scripts/validation/v1.16-audit-allowlist.json"
      provides: "verbatim-copied sidecar allow-list (phase repointed only)"
      contains: "125-v115-pin"
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "BASELINE_20 freshness audit-trail comment"
      contains: "BASELINE_20"
  key_links:
    - from: "v1.16-milestone-audit.mjs parseAllowlist"
      to: "scripts/validation/v1.16-audit-allowlist.json"
      via: "readFile of the sidecar path"
      pattern: "v1.16-audit-allowlist.json"
    - from: "v1.16-milestone-audit.mjs check id 17"
      to: "scripts/validation/c17-eee-contract.mjs"
      via: "execFileSync subprocess-spawn (inherited from v1.15, byte-unchanged)"
      pattern: "execFileSync.*c17-eee-contract"
---

<objective>
Atom 1 (SC2 / HARN-06) — ONE indivisible commit shipping the harness core of the 14th Path-A lineage bump: `v1.16-milestone-audit.mjs` (Path-A copy of v1.15, C1-C17 inherited verbatim), a VERBATIM copy of the `v1.16-audit-allowlist.json` sidecar (phase-string repoint only — the five pinned files are byte-unchanged), and the BASELINE_20 freshness comment in `regenerate-supervision-pins.mjs`.

Purpose: This is the first of the two locked atoms (D-125-3). It carries the corpus-wide lint surface. Unlike v1.15's 119-02 (which had to repoint C2/C7/C9 pins after Phases 116-118 retouched the pinned files), v1.16's retrofit touched glossaries/decision-trees/lifecycle/nav-hubs — NOT the pinned files — so the sidecar copies VERBATIM (Pitfall 3: blindly "repointing" correct pins would corrupt them).

Output: three files, ONE commit `docs(125-02): Atom 1 — v1.16 milestone audit harness (C1-C17) + sidecar (verbatim) + BASELINE_20`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author v1.16-milestone-audit.mjs — copy v1.15 Path-A, repoint strings (C17 already folded, do NOT re-fold)</name>
  <files>scripts/validation/v1.16-milestone-audit.mjs</files>
  <read_first>
    - scripts/validation/v1.15-milestone-audit.mjs (in full — the Path-A copy source; C1-C16 bodies AND the check id 17 C17 subprocess-spawn block are copied VERBATIM, do not re-derive or re-fold)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §2a (the exact line-by-line string-substitution table: line 2 lineage banner append "→ v1.16" / "inherited verbatim from v1.15"; line 3 source-of-truth → 125-CONTEXT.md D-125-1..4; line 4 sidecar-ref → v1.16-audit-allowlist.json + DROP the v1.15 "line-pins repointed" note; line 5 frozen anchor BYTE-UNCHANGED; line 36 usage; line 82 parseAllowlist body) + "The one functional string that MUST change is line 82"
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §code_context "Reusable Assets" (copy v1.15 scaffold → repoint v1.15→v1.16 + advance range) + §Don't Hand-Roll (only the sidecar-ref string is functional; C1-C17 inherited)
  </read_first>
  <action>
    Copy scripts/validation/v1.15-milestone-audit.mjs → scripts/validation/v1.16-milestone-audit.mjs. Apply ONLY the string substitutions in RESEARCH §2a: advance the lineage banner (line ~2) to end "→ v1.15 → v1.16" and state "C1-C16 inherited verbatim from v1.15" (v1.16 does NOT touch any freshness-threshold/C5/C10 logic — a diff touching those is a regression); repoint the Source-of-truth line (~3) to `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md (D-125-1..D-125-4)`; repoint the Sidecar allow-list line (~4) to `v1.16-audit-allowlist.json` and DROP the v1.15 "line-pins repointed per Phase-118 EEE-retrofit drift" note (§2b: no repoint expected this milestone); leave the frozen-anchor line (~5, the v1.6/Phase-66 predecessor anchor) BYTE-UNCHANGED; repoint the usage line (~36) to `node scripts/validation/v1.16-milestone-audit.mjs`.

    The one FUNCTIONAL change: repoint the single sidecar-path literal at the parseAllowlist() call site (line ~82) from 'scripts/validation/v1.15-audit-allowlist.json' to 'scripts/validation/v1.16-audit-allowlist.json' (confirmed single call-site; this is the literal the workflow path-match job greps for in §2d).

    Do NOT re-fold C17 — it is already present in v1.15 as check id 17 via execFileSync subprocess-spawn of c17-eee-contract.mjs; copy that block byte-unchanged (only the surrounding v1.15→v1.16 comment tokens change, never the argument-array invocation). Leave C1-C16 bodies, the runner loop, and the imports byte-unchanged. Confirm ZERO occurrences of `v1.15-audit-allowlist.json` remain (grep-assert) and that `node scripts/validation/v1.16-milestone-audit.mjs --self-test` exits 0.
  </action>
  <verify>
    <automated>bash -c 'node scripts/validation/v1.16-milestone-audit.mjs --self-test >/dev/null 2>&1 && grep -q "id: 17" scripts/validation/v1.16-milestone-audit.mjs && grep -q "scripts/validation/v1.16-audit-allowlist.json" scripts/validation/v1.16-milestone-audit.mjs && ! grep -q "v1.15-audit-allowlist.json" scripts/validation/v1.16-milestone-audit.mjs && echo ATOM1_HARNESS_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validation/v1.16-milestone-audit.mjs` exists and contains the literal `id: 17` and the `execFileSync('node', ['scripts/validation/c17-eee-contract.mjs'` argument-array invocation (copied, not re-folded)
    - The file references `scripts/validation/v1.16-audit-allowlist.json` and contains ZERO occurrences of `v1.15-audit-allowlist.json`
    - `node scripts/validation/v1.16-milestone-audit.mjs --self-test` exits 0
    - The lineage banner ends "→ v1.15 → v1.16"; the Source-of-truth line points at 125-CONTEXT.md; the frozen-anchor (v1.6/Phase-66) line is byte-unchanged
  </acceptance_criteria>
  <done>v1.16-milestone-audit.mjs exists as a faithful Path-A copy of v1.15 with the sidecar path repointed (single functional change) and C17 carried byte-unchanged; --self-test exits 0; C1-C16 unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Author v1.16-audit-allowlist.json — VERBATIM copy (phase + timestamp repoint only, no pin changes)</name>
  <files>scripts/validation/v1.16-audit-allowlist.json</files>
  <read_first>
    - scripts/validation/v1.15-audit-allowlist.json (in full — the copy source; all {file,line} pins are carried VERBATIM this milestone)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md §"sidecar byte-unchanged" (the five pinned files each 0 commits since 29a3599 — the verbatim-copy authorization)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §2b (KEY DIFFERENCE FROM v1.15 — the retrofit did NOT touch the pinned files; copy verbatim, no line-pin repoint) + §Common Pitfall 3 (blindly repointing correct pins would corrupt them)
  </read_first>
  <action>
    Copy scripts/validation/v1.15-audit-allowlist.json → scripts/validation/v1.16-audit-allowlist.json. Repoint ONLY: the "phase" field from "119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal" to "125-v115-pin-14th-path-a-lineage-bump-terminal-close", and the "generated" timestamp to the current date. Do NOT change any {file,line} pin value — the 125-01 reconnaissance confirmed all five C2/C7/C9-pinned files are byte-unchanged since 29a3599, so the v1.15 line numbers are still correct (Pitfall 3: repointing correct pins corrupts them). Preserve every allowlist array and its entry count verbatim (C13 count-asserts its broken-link allowlist).

    If — contrary to the 125-01 evidence — the v1.15 harness-on-HEAD baseline (from 125-01 Task 2) reported a FAIL on one of these pins, repoint ONLY that specific {file,line} to its actual current line verified against the live file, and record the deviation explicitly. Otherwise the expected outcome is a clean phase-string-only repoint. Do NOT refresh the c13 quarterly next_review unless 125-01 flagged it stale.
  </action>
  <verify>
    <automated>bash -c 'node -e "JSON.parse(require(\"fs\").readFileSync(\"scripts/validation/v1.16-audit-allowlist.json\",\"utf8\"))" && grep -q "125-v115-pin" scripts/validation/v1.16-audit-allowlist.json && ! grep -q "119-frozen-surface-re-baseline" scripts/validation/v1.16-audit-allowlist.json && echo SIDECAR_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validation/v1.16-audit-allowlist.json` is valid JSON (parses without error)
    - The "phase" field equals "125-v115-pin-14th-path-a-lineage-bump-terminal-close"; ZERO occurrences of "119-frozen-surface-re-baseline"
    - Every {file,line} pin is byte-identical to the v1.15 sidecar (verbatim copy) UNLESS 125-01 explicitly flagged a specific pin as shifted — in which case only that one is repointed and the deviation is recorded
    - All allowlist array entry counts are preserved verbatim (C13 broken-link allowlist count unchanged)
  </acceptance_criteria>
  <done>The sidecar is a verbatim copy of v1.15 with only the phase string + generated timestamp repointed; no {file,line} pin changed (byte-unchanged pinned files); valid JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Append BASELINE_20 comment to regenerate-supervision-pins.mjs (JIT-anchored), commit Atom 1 as ONE indivisible commit</name>
  <files>scripts/validation/regenerate-supervision-pins.mjs</files>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs lines 462-483 (the BASELINE_18/BASELINE_19 comment blocks ending ~line 482 + the `const BASELINE_9 = [` declaration at ~line 483 — the insertion point; do NOT read/alter beyond this)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §2c (the exact 8-line BASELINE_20 comment template + the <DATE>/<WAVE-0-SHA> fill points; mirror BASELINE_19's shape) + §Common Pitfall 2 (back-anchor to the JIT pre-Atom-1 HEAD, NOT the Wave-0 SHA — v1.15 saw c6ea8d2 vs a323332 diverge via an intervening Jira-sync)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md (the Wave-0 anchor — recorded for reference, but the BASELINE_20 comment uses the JIT SHA captured in THIS task)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §D-125-3 (Atom 1 = harness + allowlist + BASELINE_20 back-anchored to the Wave-0 SHA; ONE indivisible commit; do NOT fold Atom 1 into Atom 2)
  </read_first>
  <action>
    IMMEDIATELY before authoring this commit, capture the current HEAD via `git rev-parse HEAD` — this is the JIT pre-Atom-1 anchor for the BASELINE_20 freshness comment. It may DIFFER from the Wave-0 anchor recorded in 125-01 if an automated Jira-sync commit landed between (Pitfall 2); use THIS JIT-captured SHA for the BASELINE_20 comment. (The Wave-0 SHA remains the base for 125-04's byte-unchanged gate — they are different invariants.)

    Append a new BASELINE_20 comment block AFTER the BASELINE_19 block and BEFORE the `const BASELINE_9 = [` line, following the exact 8-line template in RESEARCH §2c: reference Phase 125 Plan 125-02 + HARN-06 + REQUIREMENTS.md + ROADMAP.md Phase 125 SC; state "v1.16 line positions verified against HEAD <JIT pre-Atom-1 SHA>"; note "BASELINE_9 entries above remain unchanged — Phase 125 does NOT alter the line-coord array"; and the resolution path "... → v1.15 → BASELINE_19 → v1.16 → BASELINE_20; BASELINE_21 refreshes at the next milestone close". Do NOT touch the BASELINE_9 array itself and do NOT touch the v1.7-audit-allowlist.json self-test references (they deliberately dogfood the Phase-43 fixture — repointing them is the forbidden anti-pattern) UNLESS a live `--self-test` FAILs.

    Then commit all THREE Atom-1 files (v1.16-milestone-audit.mjs, v1.16-audit-allowlist.json, regenerate-supervision-pins.mjs) as ONE indivisible commit: `docs(125-02): Atom 1 — v1.16 milestone audit harness (C1-C17) + sidecar (verbatim) + BASELINE_20`. Do NOT include any check-phase validator, frozen-at-close, or CI-workflow file (those are Atom 2 / SC2 — folding Atom 1 into Atom 2 is the rejected D2). Verify via `git show --stat HEAD` that the commit contains exactly three files.
  </action>
  <verify>
    <automated>bash -c 'node scripts/validation/regenerate-supervision-pins.mjs --self-test >/dev/null 2>&1 && grep -q "BASELINE_20" scripts/validation/regenerate-supervision-pins.mjs && echo BASELINE20_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `regenerate-supervision-pins.mjs` contains a "BASELINE_20" comment block referencing HARN-06/Phase 125 and a JIT pre-Atom-1 HEAD SHA (captured in this task, may differ from the Wave-0 SHA)
    - The BASELINE_9 array and the three v1.7-audit-allowlist.json self-test references are unchanged
    - `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0
    - The Atom-1 commit (`git show --stat HEAD`) contains EXACTLY the three files (v1.16-milestone-audit.mjs, v1.16-audit-allowlist.json, regenerate-supervision-pins.mjs) and NO check-phase/frozen-at-close/workflow file
  </acceptance_criteria>
  <done>BASELINE_20 back-anchored to the JIT pre-Atom-1 SHA; self-test green; Atom 1 committed as one indivisible commit of exactly three files (no Atom-2 leak).</done>
</task>

</tasks>

<threat_model>
This phase installs no external packages; all tooling is in-repo `.mjs` + git + node built-ins. Atom 1 touches only three predecessor-analog harness files (copy-and-repoint); no CI config, no runtime, no external service. Surfaces are minimal and stated honestly.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v1.15 sidecar → v1.16 sidecar copy | A blind line-pin "repoint" (when the pinned files are byte-unchanged) would corrupt correct pins and false-pass/fail a genuine violation |
| commit boundary (Atom 1 vs Atom 2) | Folding atoms violates the SC/D-125-3 indivisibility contract the frozen-surface audit depends on |
| BASELINE_20 anchor | Anchoring to the Wave-0 SHA instead of the JIT pre-Atom-1 HEAD mis-records the audit-trail (Pitfall 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-02-PINCORRUPT | Tampering | v1.16-audit-allowlist.json pins | mitigate | Verbatim copy authorized by 125-01 byte-unchanged evidence; JSON-parse + count-assert; repoint ONLY a pin 125-01 explicitly flagged |
| T-125-02-ATOMFOLD | Tampering | Atom-1 commit contents | mitigate | `git show --stat` asserts exactly the 3 Atom-1 files; no Atom-2 file (check-phase/frozen-at-close/workflow) leaked in |
| T-125-02-ANCHOR | Repudiation | BASELINE_20 back-anchor | mitigate | Capture JIT pre-Atom-1 HEAD with `git rev-parse HEAD` immediately before commit; record the SHA in the comment |
| T-125-02-SC | Tampering | package installs | accept | No package-manager installs this phase (node built-ins only) |
</threat_model>

<verification>
- `node scripts/validation/v1.16-milestone-audit.mjs --self-test` exits 0; sidecar path repointed; zero v1.15-audit-allowlist.json references
- v1.16-audit-allowlist.json parses as JSON; phase repointed; pins byte-unchanged
- BASELINE_20 present (JIT-anchored); regenerate-supervision-pins.mjs --self-test exits 0
- Atom-1 commit stat shows exactly 3 files
</verification>

<success_criteria>
Atom 1 ships as ONE indivisible commit: v1.16-milestone-audit.mjs (Path-A copy, C17 inherited) + verbatim v1.16-audit-allowlist.json (phase repoint only) + BASELINE_20 (JIT-anchored). Self-tests green; no Atom-2 file included.
</success_criteria>

<output>
Create `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-02-SUMMARY.md` when done, recording the Atom-1 commit SHA and the JIT pre-Atom-1 anchor SHA used for BASELINE_20.
</output>
</content>
