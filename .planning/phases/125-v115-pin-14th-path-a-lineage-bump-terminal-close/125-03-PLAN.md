---
phase: 125-v115-pin-14th-path-a-lineage-bump-terminal-close
plan: 03
type: execute
wave: 3
depends_on: ["125-02"]
files_modified:
  - scripts/validation/check-phase-120.mjs
  - scripts/validation/check-phase-121.mjs
  - scripts/validation/check-phase-122.mjs
  - scripts/validation/check-phase-123.mjs
  - scripts/validation/check-phase-124.mjs
  - scripts/validation/check-phase-125.mjs
  - scripts/validation/_lib/frozen-at-close.mjs
  - .github/workflows/audit-harness-v1.16-integrity.yml
autonomous: true
requirements: [HARN-05, HARN-06]
must_haves:
  truths:
    - "check-phase-120..124 are FIVE lightweight leaf validators (CHAIN_PHASES=[], CHAIN_SKIP=new Set([])) whose needles derive from each phase's VERIFICATION.md — including check-phase-120, deliberately deferred from Phase 120 (validator-atom deferral)"
    - "check-phase-125 is the apex: CHAIN_PHASES=[48..124] (77 entries ending 124, NOT the transcription-error [48..119]), CHAIN_SKIP=new Set([]), HARNESS=v1.16-milestone-audit.mjs, with hard length!==77 / terminus!==124 module-load throws"
    - "frozen-at-close.mjs gains V115='29a3599' + readAtV115Close, back-anchored to the v1.15 close-gate (the HARN-05 pin) — inserted AFTER V114 before the // V14 omitted comment"
    - "audit-harness-v1.16-integrity.yml is the 13th coexistence workflow with paths: repointed to v1.16-* (MANDATORY else it never fires), the check-phase-*.mjs glob byte-unchanged, dual-apex + LF-fidelity flags preserved verbatim"
    - "Atom 2 lands as ONE indivisible commit of exactly eight files, then is pushed to a branch/PR to trigger the authoritative Axis-2 GHA re-audit + the 9-workflow predecessor cascade"
  artifacts:
    - path: "scripts/validation/check-phase-125.mjs"
      provides: "chain apex, 77-entry chain 48..124"
      contains: "check-phase-124"
    - path: "scripts/validation/_lib/frozen-at-close.mjs"
      provides: "V115 frozen-close pin (HARN-05)"
      contains: "V115"
    - path: ".github/workflows/audit-harness-v1.16-integrity.yml"
      provides: "13th CI coexistence workflow, Linux-authoritative Axis 2"
      contains: "v1.16-"
  key_links:
    - from: "check-phase-125.mjs HARNESS"
      to: "scripts/validation/v1.16-milestone-audit.mjs"
      via: "AUDIT-HARNESS subprocess-spawn"
      pattern: "v1.16-milestone-audit.mjs"
    - from: "audit-harness-v1.16-integrity.yml linux-chain-ubuntu-latest"
      to: "check-phase-125.mjs (recurses 48..124)"
      via: "node scripts/validation/check-phase-125.mjs --verbose"
      pattern: "check-phase-125.mjs"
    - from: "frozen-at-close.mjs V115"
      to: "v1.15 close-gate commit 29a3599"
      via: "MILESTONE_CLOSE_SHAS lookup"
      pattern: "29a3599"
---

<objective>
Atom 2 (SC1+SC2 / HARN-05 + HARN-06) — ONE indivisible commit authoring the six new chain validators (five leaves 120-124 + the apex 125), the V115 frozen-close pin, and the 13th CI coexistence workflow; then PUSH to trigger the authoritative Axis-2 GHA re-audit + the 9-workflow predecessor cascade.

Purpose: This atom carries the per-phase regression guards (120-124 leaves) + the apex (125, CHAIN_PHASES=[48..124]) that together prove the retrofitted-corpus chain atomically; the V115='29a3599' pin (HARN-05, back-anchored to v1.15's close-gate — the mandatory back-anchor the v1.15 close deferred); and the CI workflow whose push is the load-bearing Axis-2 trigger (D-125-4 ordering gate — Atom 2 must be pushed FIRST for Axis 2 to run). Note: check-phase-120.mjs was DELIBERATELY not authored in Phase 120 (validator-atom deferral), so this atom authors SIX validators, not five (Pitfall 5). The apex range is the RESEARCH-corrected [48..124]/77, NOT the [48..119] transcription error printed in ROADMAP SC2 / REQUIREMENTS HARN-06 / STATE (that text is corrected at the close-gate, Plan 125-07).

Output: eight files, ONE commit `docs(125-03): Atom 2 — check-phase-120..125 + frozen-at-close V115 + v1.16 CI workflow`, then a push to a branch/PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author check-phase-120..124 (5 lightweight leaf validators, needles from each VERIFICATION.md)</name>
  <files>scripts/validation/check-phase-120.mjs, scripts/validation/check-phase-121.mjs, scripts/validation/check-phase-122.mjs, scripts/validation/check-phase-123.mjs, scripts/validation/check-phase-124.mjs</files>
  <read_first>
    - scripts/validation/check-phase-118.mjs (or another v1.15 leaf 113..118, in full — the lightweight leaf template: presence() helper, needle checks, V-NN-SELF dual-invariant asserting CHAIN_PHASES excludes NN AND CHAIN_SKIP.size===0, runner loop; CHAIN_PHASES=[] and CHAIN_SKIP=new Set([]))
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §3 "Validator-atom deferral note" (check-phase-120 was NOT authored in Phase 120 — author it here) + §Common Pitfall 5
    - EACH of: .planning/phases/120-*/120-VERIFICATION.md, 121-*/121-VERIFICATION.md, 122-*/122-VERIFICATION.md, 123-*/123-VERIFICATION.md, 124-*/124-VERIFICATION.md — the "Required Artifacts" / "Observable Truths" tables are the authoritative needle source (file paths, verbatim landing strings, registry-flip events, C17-exit-0 confirmations). READ ALL FIVE before authoring their needles.
    - STATE.md "Accumulated Context" [120]/[121]/[122]/[123]/[124] execution decisions (the shipped needles: STD-04 Mermaid policy + c17 assertion #1; RETRO-04/07/09 glossary+lifecycle+guide enrollment; RETRO-05/07/08 decision-tree+carved-mermaid; RETRO-06 nav-hub navigation-last; PIPE-03/04/05 pandoc-alias + filename-map + Draft-probe)
  </read_first>
  <action>
    Author five leaf validators following the v1.15 leaf template (check-phase-118.mjs) verbatim in structure. For EACH phase substitute: the phase number in every string literal (118→120..124), the DELIVERABLE_* path constants + one presence() call per deliverable file, the landed-content needle strings sourced from that phase's own *-VERIFICATION.md, the header one-line phase-name summary, and a V-NN-SELF check asserting CHAIN_PHASES does NOT include NN AND CHAIN_SKIP.size===0. In EVERY leaf file keep `const CHAIN_PHASES = []` and `const CHAIN_SKIP = new Set([])` — chain logic lives ONLY in the apex (125).

    Needle guidance per phase (derive exact strings from each VERIFICATION.md): 120 — docs/_standards/EEE-SOP-standard.md Mermaid-policy resolution (STD-04) + c17-eee-contract.mjs assertion #1 updated + the frozen-at-close.mjs:5-9 corrected HYG-01 comment + the 5-class Doc Type taxonomy extension. 121 — glossary + 13 Mermaid-free lifecycle + RE-175/176 end-user-guide registry-Approved flips + C17-green (RETRO-04/07-partial/09). 122 — decision-trees + 9 Mermaid-bearing lifecycle + carved-mermaid files at Status:Approved, LOCKED-N leaf-parity annotations, RETRO-05/07-close/08. 123 — the 4 nav-hubs (index/common-issues/quick-ref-l1/l2) EEE-enrolled + check-nav-hub-links.mjs 0/0 + navigation-last git-attestation (RETRO-06). 124 — PIPE-03 pandoc-alias fix (custom-props check) + PIPE-04 filename-map.md + PIPE-05 Draft-label probe PASS.

    check-phase-120 is the validator-atom-deferred file — it does NOT exist yet; author it fresh like the others. Do NOT add the NESTED guard to leaf files (that guard only matters for the apex). Copy the pass/fail/skip runner loop verbatim; only the console.log banner phase number changes.
  </action>
  <verify>
    <automated>bash -c 'for n in 120 121 122 123 124; do node scripts/validation/check-phase-$n.mjs >/dev/null 2>&1 || { echo "check-phase-$n FAIL"; exit 1; }; done; echo LEAVES_OK'</automated>
  </verify>
  <acceptance_criteria>
    - All five files `scripts/validation/check-phase-120.mjs`..`check-phase-124.mjs` exist (including the previously-deferred check-phase-120)
    - Each contains `const CHAIN_PHASES = []` and `const CHAIN_SKIP = new Set([])` and a V-NN-SELF check
    - `node scripts/validation/check-phase-120.mjs` (and 121..124) each exit 0 on the current tree
    - Needle strings in each file trace to that phase's *-VERIFICATION.md (not invented)
  </acceptance_criteria>
  <done>Five leaf validators exist (incl. the deferred check-phase-120), each exits 0, each with empty CHAIN_PHASES/CHAIN_SKIP and phase-sourced needles.</done>
</task>

<task type="auto">
  <name>Task 2: Author check-phase-125 apex (CHAIN_PHASES=[48..124], 77 entries) + add V115 pin to frozen-at-close.mjs</name>
  <files>scripts/validation/check-phase-125.mjs, scripts/validation/_lib/frozen-at-close.mjs</files>
  <read_first>
    - scripts/validation/check-phase-119.mjs (in full — the apex fork-base: HARNESS const, CHAIN_PHASES=[48..118]/71-entry array, the hard length!==71 / terminus!==118 module-load throws, NESTED-aware CHAIN-NN loop, AUDIT-HARNESS check, V-119-SELF dual-invariant, V-119-AUDIT archive-path check)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §3 (the LOAD-BEARING apex-range convention: [48..(closephase−1)]; the correct v1.16 values table — apex CHAIN_PHASES=[48..124] 77 entries, length!==77, terminus [0]!==48||last!==124, V-125-SELF asserts !includes(125) AND size===0, HARNESS='scripts/validation/v1.16-milestone-audit.mjs') + the "77 = 124−48+1" arithmetic + §Common Pitfall 1
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §1 (the exact V115 map-entry text + the readAtV115Close export line; insert AFTER the V114:'7d922a7' block, BEFORE the // V14 omitted comment; append the export AFTER readAtV114Close)
    - scripts/validation/_lib/frozen-at-close.mjs (in full — the V114 entry ~line 56 + readAtV114Close export ~line 98, the single-entry pin pattern)
    - scripts/validation/_lib/archive-path.mjs (resolveArchivedPhasePath; milestoneRoots stays the current default — v1.16-phases won't be an archive target until /gsd-complete-milestone runs AFTER this phase)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-01-SUMMARY.md (the confirmed V115=29a3599)
  </read_first>
  <action>
    Fork scripts/validation/check-phase-119.mjs → scripts/validation/check-phase-125.mjs with these substitutions: HARNESS → 'scripts/validation/v1.16-milestone-audit.mjs'; CHAIN_PHASES → the full 77-entry integer array 48 through 124 inclusive (append 119,120,121,122,123,124 to the v1.15 [48..118] array) — the [48..(closephase−1)] invariant means closephase=125 so the apex EXCLUDES 125 and INCLUDES v1.16's own chained phases 120..124; the hard module-load throws become `length !== 77` and `[0] !== 48 || last !== 124` (author [48..124]/77, NOT the transcription-error [48..119] — Pitfall 1: [48..119] fails the length/terminus throw AND excludes 120-124); CHAIN_SKIP stays `new Set([])` (hard — V-125-SELF asserts size 0; adding entries to force green is the forbidden self-disqualifier). Repoint every "119" token to "125" in the header, the V-125-SELF check (assert CHAIN_PHASES does NOT include 125), the range comment [48..118]→[48..124], and the V-125-AUDIT check whose resolveArchivedPhasePath targets "125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-VERIFICATION.md" with the current milestoneRoots default. Preserve the NESTED guard on BOTH the CHAIN-NN loop and the AUDIT-HARNESS check verbatim, and the isPeer subTimeout + execFailDetail formatting.

    Note: V-125-AUDIT will PASS-via-skip until 125-VERIFICATION.md is authored at close-gate time (Plan 125-07) — this is expected, mirrors 119's "not yet authored" skip.

    In scripts/validation/_lib/frozen-at-close.mjs: insert the V115 map entry per RESEARCH §1 (`V115: '29a3599',` with the single-entry comment noting it is the Phase-119 close-gate, message contains both "MILESTONE-AUDIT" and "MILESTONE CLOSE", do NOT pin a SUMMARY follow-up, back-anchor invariant — V115 references a PAST close SHA, V116 deferred to v1.17) immediately AFTER the V114:'7d922a7' block and BEFORE the `// V14 omitted` comment; append `export const readAtV115Close = (p) => readAtClose('V115', p);` immediately after the readAtV114Close export. Before finalizing, re-confirm 29a3599 resolves to the substantive close-gate via `git log -1 --format=%s 29a3599` (message contains "MILESTONE CLOSE"). This is the HARN-05 pin; it must exist before any Wave-5 readAtV115Close conversion (D-125-1 dependency), which is satisfied because Atom 2 is pushed before the slot fires.
  </action>
  <verify>
    <automated>bash -c 'node -e "const m=require(\"fs\").readFileSync(\"scripts/validation/check-phase-125.mjs\",\"utf8\"); const arr=m.match(/CHAIN_PHASES\s*=\s*\[([^\]]*)\]/s)[1].split(\",\").filter(x=>x.trim()).length; if(arr!==77){console.error(\"CHAIN_PHASES has \"+arr+\" entries, expected 77\");process.exit(1)}" && grep -q "48,49,50" scripts/validation/check-phase-125.mjs && grep -q ",124]" scripts/validation/check-phase-125.mjs && grep -q "v1.16-milestone-audit.mjs" scripts/validation/check-phase-125.mjs && grep -q "V115: .29a3599." scripts/validation/_lib/frozen-at-close.mjs && grep -q "readAtV115Close" scripts/validation/_lib/frozen-at-close.mjs && echo APEX_PIN_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `check-phase-125.mjs` CHAIN_PHASES has exactly 77 numeric entries, starting 48 and ending 124; the module-load throws assert length===77 and terminus===124; CHAIN_SKIP is `new Set([])`
    - `check-phase-125.mjs` HARNESS === 'scripts/validation/v1.16-milestone-audit.mjs'; V-125-SELF asserts 125 is absent from CHAIN_PHASES AND CHAIN_SKIP.size===0
    - The NESTED guard is present on both the CHAIN-NN loop and AUDIT-HARNESS check
    - `frozen-at-close.mjs` contains `V115: '29a3599'` (after V114, before // V14 omitted) and `readAtV115Close`; NO `V116` entry exists
    - `git log -1 --format=%s 29a3599` contains "MILESTONE CLOSE"
  </acceptance_criteria>
  <done>Apex check-phase-125 forks 119 with 77-entry chain 48..124 (length/terminus throws), empty CHAIN_SKIP, v1.16 HARNESS, NESTED guards preserved; V115='29a3599' pin + readAtV115Close export added and re-verified; no V116.</done>
</task>

<task type="auto">
  <name>Task 3: Author audit-harness-v1.16-integrity.yml (13th workflow), commit Atom 2 as ONE commit, push to trigger Axis 2 + cascade</name>
  <files>.github/workflows/audit-harness-v1.16-integrity.yml</files>
  <read_first>
    - .github/workflows/audit-harness-v1.15-integrity.yml (in full, ~246 lines — the copy source: parse/path-match/harness-run jobs, linux-chain-ubuntu-latest, per-phase check jobs, rotting-external-quarterly, pin-helper-advisory)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §2d (the exact element-by-element repoint table: name line 15; header 1-13 "12th"→"13th"/"check-phase-113..119"→"120..125"/"spawns 48..118"→"48..124"; paths: filter 19-25 all v1.15→v1.16 MANDATORY; the check-phase-*.mjs glob line 21 BYTE-UNCHANGED; parse job v1.16-audit-allowlist.json; path-match job line 59; harness-run job; linux-chain-ubuntu-latest 90-95 check-phase-125 recurses 48..124 + [48..124] notice; per-phase jobs 97-193 replace 113..119 with 120..125; DUAL-APEX note) + the PRESERVE-VERBATIM list (autocrlf false, fetch-depth 0, continue-on-error false, timeout-minutes 30, CHAIN_TIMING_LINUX notice, 2 crons, rotting-external-quarterly, pin-helper-advisory; do NOT add CHECK_PHASE_NESTED=1 to either top-level GHA apex)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §5b (the cascade is 9 firing workflows v1.7-v1.15 + the new v1.16, NOT 11; v1.5/v1.6 do NOT fire; 48..66 covered by apex recursion) + §Common Pitfall 6
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §D-125-4 (paths: filter MUST repoint or the workflow never fires; preserve linux-chain-ubuntu-latest; B↔C↔D interlock — Atom-2 push FIRST)
  </read_first>
  <action>
    Copy .github/workflows/audit-harness-v1.15-integrity.yml → .github/workflows/audit-harness-v1.16-integrity.yml. Apply the RESEARCH §2d repoints: `name:` v1.15→v1.16; ALL `paths:` entries (`scripts/validation/v1.15-*`→`v1.16-*`; the workflow filename; the two milestone-audit doc paths `.planning/milestones/v1.15-MILESTONE-AUDIT.md`/`v1.15-DEFERRED-CLEANUP.md`→`v1.16-*`) — this is MANDATORY, else the workflow never fires (D-125-4); the `scripts/validation/check-phase-*.mjs` glob line stays BYTE-UNCHANGED (this is what fires the cascade). Update header comments "12th coexistence workflow"→"13th", "check-phase-113..119"→"check-phase-120..125", "spawns 48..118"→"spawns 48..124", DUAL-APEX note "check-phase-119 ... 48..118"→"check-phase-125 ... 48..124", and add "+ v1.15" to the frozen-harness list. Repoint every `v1.15-audit-allowlist.json`/`v1.15-milestone-audit.mjs` string literal in the parse/path-match/harness-run job steps to v1.16. In linux-chain-ubuntu-latest, change the apex command from `check-phase-119.mjs` to `check-phase-125.mjs` and the notice/comment `[48..118]`→`[48..124]`. Replace the per-phase job block (check-phase-113..119) with a 6-job block (check-phase-120..125), same per-job template, substituting the phase number in the 4 places per job (job key, name:, step name:, run:).

    PRESERVE VERBATIM (D-125-4 MANDATORY rider): linux-chain-ubuntu-latest's `git config --global core.autocrlf false` set BEFORE checkout, `fetch-depth: 0`, `continue-on-error: false`, `timeout-minutes: 30`, the CHAIN_TIMING_LINUX ::notice emission, the two crons, rotting-external-quarterly (markdown-link-check@3.14.2 pin unchanged), pin-helper-advisory (continue-on-error true). PRESERVE the two apparently-redundant full-apex invocations (standalone check-phase-125 job + linux-chain job) — do NOT deduplicate and do NOT add CHECK_PHASE_NESTED=1 to either top-level GHA invocation (Pitfall 6).

    Then commit ALL EIGHT Atom-2 files (check-phase-120..125 [6], frozen-at-close.mjs, audit-harness-v1.16-integrity.yml) as ONE indivisible commit: `docs(125-03): Atom 2 — check-phase-120..125 + frozen-at-close V115 + v1.16 CI workflow`. Do NOT include any Atom-1 file. Verify via `git show --stat HEAD` that the commit contains exactly eight files. Then push to a branch/PR (the load-bearing D-125-4 ordering gate — the push triggers the authoritative Axis-2 GHA re-audit AND the 9-workflow predecessor cascade against retrofitted HEAD; the close-gate necessarily post-dates this push). Record the branch/PR and the resulting workflow run IDs (v1.16 + the firing predecessor workflows) in the SUMMARY for Plan 125-04. Do NOT run the full apex chain locally on Windows (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 now at depth [48..124], one deeper than v1.15 — killed at turn boundaries); authoritative apex execution is the Linux GHA run.
  </action>
  <verify>
    <automated>bash -c 'grep -q "v1.16-milestone-audit.mjs" .github/workflows/audit-harness-v1.16-integrity.yml && grep -q "check-phase-125.mjs" .github/workflows/audit-harness-v1.16-integrity.yml && grep -q "core.autocrlf false" .github/workflows/audit-harness-v1.16-integrity.yml && grep -q "fetch-depth: 0" .github/workflows/audit-harness-v1.16-integrity.yml && grep -q "check-phase-\*.mjs" .github/workflows/audit-harness-v1.16-integrity.yml && ! grep -q "v1.15-milestone-audit.mjs" .github/workflows/audit-harness-v1.16-integrity.yml && echo WORKFLOW_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/audit-harness-v1.16-integrity.yml` exists; `paths:` contains `scripts/validation/v1.16-*` and the v1.16 milestone-audit doc paths AND the byte-unchanged `scripts/validation/check-phase-*.mjs` glob; ZERO `v1.15-milestone-audit.mjs`/`v1.15-audit-allowlist.json` references
    - The linux-chain job runs `check-phase-125.mjs` and preserves `core.autocrlf false` (before checkout), `fetch-depth: 0`, `continue-on-error: false`, `timeout-minutes: 30`
    - A standalone `check-phase-125` job AND the `linux-chain-ubuntu-latest` job both exist (dual-apex preserved, not deduplicated); no `CHECK_PHASE_NESTED=1` on either top-level invocation
    - The Atom-2 commit (`git show --stat HEAD`) contains exactly the eight Atom-2 files and NO Atom-1 file
    - The commit is pushed to a branch/PR; the v1.16 + firing predecessor workflow run IDs are recorded in the SUMMARY
  </acceptance_criteria>
  <done>The 13th CI workflow is authored (v1.16-repointed, check-phase-*.mjs glob + LF-fidelity + dual-apex preserved); Atom 2 committed as one indivisible eight-file commit and pushed to trigger the authoritative Axis-2 GHA re-audit + the 9-workflow cascade.</done>
</task>

</tasks>

<threat_model>
This phase installs no external packages; all tooling is in-repo `.mjs` + git + node built-ins. The one live-service surface is CI-workflow config (GitHub Actions) — copy-and-repoint of a predecessor workflow, with the LF-fidelity/fetch-depth/PR-blocking contracts preserved verbatim. No new runtime, no external installs. Surfaces stated honestly.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| apex validator → predecessor chain (48..124) | A wrong CHAIN_PHASES bound (e.g. the [48..119] transcription error) or a non-empty CHAIN_SKIP silently passes an unproven chain / fails the length-terminus throw |
| frozen-at-close V115 pin → git history | A wrong SHA reads the wrong frozen corpus for every future readAtV115Close consumer |
| CI paths: filter → Axis-2 + cascade trigger | An un-repointed v1.16-* paths: filter means the authoritative re-audit never fires (silent no-gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-03-CHAINBOUND | Tampering | check-phase-125 CHAIN_PHASES / CHAIN_SKIP | mitigate | Author [48..124]/77 (NOT the [48..119] transcription error); length!==77 + terminus!==124 module-load throws; V-125-SELF hard-asserts 125 absent + size 0 |
| T-125-03-V115 | Tampering/Info-disclosure | frozen-at-close V115 pin | mitigate | Re-confirm 29a3599 message contains "MILESTONE CLOSE"; back-anchor invariant (no V116); reject SUMMARY follow-ups |
| T-125-03-PATHS | Tampering | CI paths: filter | mitigate | Repoint all v1.15 path entries to v1.16; keep the check-phase-*.mjs glob byte-unchanged (fires the cascade); grep-assert zero v1.15 harness references |
| T-125-03-DEDUP | Tampering | dual-apex CI jobs | mitigate | Preserve both full-apex invocations verbatim; no CHECK_PHASE_NESTED=1 on top-level jobs (Pitfall 6) |
| T-125-03-ATOMFOLD | Tampering | Atom-2 commit contents | mitigate | `git show --stat` asserts exactly the 8 Atom-2 files, no Atom-1 leak |
| T-125-03-SC | Tampering | package installs | accept | No package-manager installs (node built-ins + actions pinned byte-unchanged) |
</threat_model>

<verification>
- All five leaves + apex exit 0 locally (single-validator runs; NOT the full apex chain on Windows)
- CHAIN_PHASES has 77 entries ending 124; CHAIN_SKIP empty; length/terminus throws present
- frozen-at-close contains V115='29a3599' + readAtV115Close; no V116
- Workflow repointed to v1.16-*, check-phase-*.mjs glob + LF-fidelity + dual-apex preserved; zero v1.15 harness references
- Atom-2 commit is exactly 8 files; pushed; Axis-2 + cascade run IDs recorded
</verification>

<success_criteria>
Atom 2 ships as ONE indivisible commit (6 chain validators incl. the deferred check-phase-120 + V115 pin + 13th CI workflow) and is pushed to trigger the authoritative Axis-2 GHA re-audit + the 9-workflow cascade. Apex chain 48..124 (77 entries), CHAIN_SKIP empty, V115='29a3599' back-anchored.
</success_criteria>

<output>
Create `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-03-SUMMARY.md` when done, recording the Atom-2 commit SHA, the push branch/PR, and the triggered Axis-2 + predecessor-cascade workflow run IDs.
</output>
</content>
