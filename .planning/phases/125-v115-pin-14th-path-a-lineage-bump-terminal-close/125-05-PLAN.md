---
phase: 125-v115-pin-14th-path-a-lineage-bump-terminal-close
plan: 05
type: execute
wave: 5
depends_on: ["125-04"]
files_modified:
  - docs/_glossary-apple-business.md
  - scripts/validation/check-phase-62.mjs
  - scripts/validation/check-phase-67.mjs
autonomous: true
requirements: [HARN-06]
must_haves:
  truths:
    - "This slot fires ONLY IF the Plan 125-04 authoritative Axis-2 GHA cascade came back RED on any firing apex; if GREEN it is a documented no-op skip"
    - "Remediation admits exactly THREE shapes: (1) readAtV115Close frozen-aware conversion of a predecessor content-assertion validator; (2) a NEW <!-- ABAUDIT-NN --> C15 FP-exemption on the tripping line of a v1.16-retrofitted structural doc (the DOMINANT shape); (3) an in-class NESTED/guard fix on a predecessor check-phase-NN validator"
    - "NO value-masking (no expected value bumped to the evolved state); NO predecessor FROZEN-surface edit (v1.N-milestone-audit.mjs / sidecar / workflow off-limits); CHAIN_SKIP stays EMPTY everywhere (V-SELF hard-asserts size 0)"
    - "Every RED-then-green intermediate state is recorded honestly; the re-pushed GHA cascade concludes green across all 9 firing apexes"
  artifacts:
    - path: ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-05-SUMMARY.md"
      provides: "Honest remediation record: which shape(s) fired, root cause, before/after GHA run IDs, the explicit no-value-mask/no-frozen-surface/CHAIN_SKIP-empty statement"
      contains: "no value-mask"
  key_links:
    - from: "125-04 CASCADE RED verdict + failing-validator worklist"
      to: "ABAUDIT-NN C15 exemption / readAtV115Close conversion / NESTED-guard fix"
      via: "triggered-only in-class remediation"
      pattern: "ABAUDIT"
---

<objective>
The pre-authorized EMERGENT chain-health remediation slot (HARN-06, D-125-1/C1). Budgeted as a REAL plan because the close PR is the FIRST time the predecessor apexes see the retrofitted v1.16 corpus, and the DOMINANT drift vector — a C15/ABAUDIT anti-pattern trip on `docs/_glossary-apple-business.md` (RE-180) caused by a Phase-121/122/123 `#12` blockquote split — is un-pre-convertible (it only surfaces on the assembled-corpus GHA scan). v1.15's emergent RED (commit ad583fd, run 28823233887) was exactly this shape.

Purpose: If the Plan 125-04 authoritative Axis-2 GHA cascade is RED, restore chain health using ONLY the three sanctioned shapes — never masking values, never editing a frozen harness surface, never adding a CHAIN_SKIP entry. Then re-push and re-audit the cascade to green. If 125-04 reported CASCADE GREEN, this plan is a documented no-op skip.

Output: 1-or-more remediation commits (using the three admitted shapes) + 125-05-SUMMARY.md honest-accounting record + a re-pushed green Axis-2 GHA cascade. The close-gate remains a SEPARATE single commit (125-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md
@.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate check — fire only if CASCADE RED; else record no-op skip</name>
  <files>.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md (the CASCADE RED|GREEN verdict + the failing-validator/workflow worklist if RED, with the C15/ABAUDIT identification)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §D-125-1 (remediation fires ONLY if the authoritative Axis-2 GHA is RED; the three shapes; honest-atomicity) + §specifics (the C15/ABAUDIT shape is the DOMINANT, un-pre-convertible risk)
  </read_first>
  <action>
    Read the Plan 125-04 verdict. If it is "CASCADE GREEN", record in 125-05-SUMMARY.md: "No-op skip — Plan 125-04 Axis-2 GHA cascade concluded GREEN on first push across all 9 firing apexes; no chain-health remediation required; close-gate precondition (Axis 2 + cascade) already cleared." Do NOT edit any file. This plan then completes as a skip.

    If the verdict is "CASCADE RED", proceed to Task 2 using the failing-validator worklist (which specific check-phase-NN FAILed, in which firing workflow, and the failing assertion class). Classify the failure into one of the three sanctioned shapes: (1) a predecessor content-assertion reading a retrofitted structural doc at live HEAD → readAtV115Close conversion; (2) a C15/ABAUDIT anti-pattern trip on a v1.16-retrofitted structural doc (e.g. _glossary-apple-business.md, RE-180 — the DOMINANT expected shape) → a NEW `<!-- ABAUDIT-NN -->` exemption comment on the tripping line INSIDE that this-milestone doc; (3) an AUDIT-HARNESS/self-test step lacking the CHECK_PHASE_NESTED guard → in-class NESTED-guard fix. Record the pre-remediation RED run ID + tally verbatim as the honest-accounting baseline. (The files_modified list in frontmatter is a PLACEHOLDER candidate set — the ACTUAL edited files come from the 125-04 worklist; update the SUMMARY to the real set.)
  </action>
  <verify>
    <automated>bash -c 'test -f ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-05-SUMMARY.md" && grep -Eq "No-op skip|CASCADE RED" ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-05-SUMMARY.md" && echo GATE_RECORDED'</automated>
  </verify>
  <acceptance_criteria>
    - 125-05-SUMMARY.md records either "No-op skip" (with the 125-04 GREEN verdict cited) or "CASCADE RED" (with the pre-remediation run ID + failing-validator worklist + the shape classification)
    - In the no-op case, git status shows no docs/ or scripts/validation edits from this plan
  </acceptance_criteria>
  <done>The fire/skip gate is evaluated against the 125-04 verdict and recorded; remediation proceeds only on CASCADE RED, with the failure classified into one of the three sanctioned shapes.</done>
</task>

<task type="auto">
  <name>Task 2 (conditional): Apply the sanctioned remediation shape(s), re-push, re-audit cascade to green — honest record</name>
  <files>docs/_glossary-apple-business.md, scripts/validation/check-phase-62.mjs, scripts/validation/check-phase-67.mjs</files>
  <read_first>
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-04-AUDIT-RESULTS.md (the exact failing check-phase-NN identities + the tripping doc/line — the files listed in files_modified above are PLACEHOLDER candidates; the ACTUAL files to edit are whichever 125-04 reported RED)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-RESEARCH.md §4b (the C15 mechanism: check id 15, appleBusinessDocPaths, regex-7 negative-lookahead 160-char same-line window, the `<!-- ABAUDIT-\d+ -->` exemption exempting the comment line AND the next line; structural retrofit docs carry ZERO ABAUDIT today; a NEW ABAUDIT comment on the retrofitted _glossary-apple-business.md is ALLOWED — this-milestone surface, not a predecessor frozen doc) + §4a (the readAtV115Close conversion shape for {path,line} pins 67/101 + literal scans 109/110)
    - scripts/validation/v1.15-milestone-audit.mjs check id 15 lines ~713-758 (the C15 mechanism + the ABAUDIT exemption format — read to author a correctly-formatted NEW exemption comment)
    - scripts/validation/_lib/frozen-at-close.mjs (readAtClose / readAtV115Close — the frozen-aware-read helper a content assertion must use; already added in Atom 2 / 125-03, satisfying the D-125-1 dependency)
    - .planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-CONTEXT.md §D-125-1 MANDATORY RIDER remediation constraints (NO value-masking, NO predecessor frozen-surface edit, CHAIN_SKIP empty; an ABAUDIT comment on a v1.16 Phase-121/122/123 structural doc IS allowed)
  </read_first>
  <action>
    ONLY IF Task 1 recorded CASCADE RED. Apply the sanctioned shape matching the classified failure:

    Shape 2 (DOMINANT — C15/ABAUDIT): if a C15 regex-7 trip fired on a v1.16-retrofitted structural doc (most likely docs/_glossary-apple-business.md / RE-180, where a `#12` blockquote split isolated a "Managed Apple ID" sentence outside its 160-char exempting window), add a NEW `<!-- ABAUDIT-NN: <reason> -->` comment on the tripping line INSIDE that this-milestone doc, formatted exactly per the C15 mechanism (it exempts the comment line AND the next line). This edits a v1.16 structural surface (allowed), NOT a predecessor frozen doc.

    Shape 1 (readAtV115Close conversion): if a predecessor content-assertion validator (e.g. check-phase-67 {path,line:64} pin into _glossary-macos.md, or check-phase-101/109/110 literal scans) trips because it reads the retrofitted corpus at live HEAD, convert THAT assertion to a frozen-aware read via readAtV115Close() from _lib/frozen-at-close.mjs. Edit ONLY the predecessor check-phase-NN.mjs validator (in-class), never a frozen harness surface.

    Shape 3 (NESTED-guard): if a predecessor AUDIT-HARNESS/self-test step re-validates the evolved corpus under nesting, add the CHECK_PHASE_NESTED guard to that step.

    HARD CONSTRAINTS (all shapes): NEVER value-mask (do not bump an expected value to match the evolved corpus — the T1 self-disqualifier). NEVER edit a frozen harness surface (any v1.N-milestone-audit.mjs / sidecar / workflow). CHAIN_SKIP stays `new Set([])` everywhere — never add an entry to force green. A shared conversion/exemption greens every recursing apex at once (idempotent across the 9 firing workflows). Commit the remediation as 1-or-more commits `fix(125-05): chain-health remediation — <shape/target> (no value-mask, CHAIN_SKIP ∅)`. Re-push to the branch/PR and re-run the cascade; await the FRESH Axis-2 runs and confirm every firing apex concludes green (FAIL 0). Record in 125-05-SUMMARY.md the honest-accounting narrative: the discarded pre-remediation RED run ID + tally, the root cause, the exact fix per shape (with an explicit "NO value-masking; NO frozen surface edited; CHAIN_SKIP left empty" statement), and the re-run GREEN run IDs. Signal Plan 125-04's verdict is now re-evaluated to GREEN for the close-gate.
  </action>
  <verify>
    <automated>bash -c 'grep -qi "no value-mask" ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-05-SUMMARY.md" && grep -Eq "GREEN|No-op skip" ".planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-05-SUMMARY.md" && echo REMEDIATION_RECORDED'</automated>
  </verify>
  <acceptance_criteria>
    - (RED path) Only sanctioned surfaces are edited: a v1.16 retrofitted structural doc (ABAUDIT comment) and/or a predecessor check-phase-NN.mjs validator (readAtV115Close / NESTED-guard); git diff shows NO v1.N-milestone-audit.mjs / sidecar / workflow edit and NO expected-value bump
    - No CHAIN_SKIP entry was added anywhere (every touched validator keeps `new Set([])`)
    - The re-pushed Axis-2 GHA cascade concludes green (FAIL 0) across all 9 firing apexes; the fresh run IDs are recorded
    - 125-05-SUMMARY.md contains the honest-accounting narrative (discarded RED run ID + tally, root cause, per-shape fix, re-run GREEN run IDs) with the explicit "NO value-masking / NO frozen surface / CHAIN_SKIP empty" statement
    - (GREEN path) This task is a documented no-op; no files edited
  </acceptance_criteria>
  <done>If fired: the sanctioned shape(s) applied in-class (ABAUDIT on the retrofitted doc / readAtV115Close conversion / NESTED-guard), re-pushed, cascade green, honest record complete. If not fired: documented no-op skip.</done>
</task>

</tasks>

<threat_model>
This phase installs no external packages; all tooling is in-repo `.mjs` + git + gh + node built-ins. The remediation edits are the single largest integrity surface of the phase: they must stay within the three sanctioned shapes and never touch a frozen harness surface or mask a value. No external installs, no runtime. Surfaces stated honestly.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RED apex → remediation edit surface | The largest integrity risk of the phase: an executor could "fix" the RED by value-masking, editing a frozen harness surface, or adding CHAIN_SKIP — all of which silently defeat the chain-of-trust |
| v1.16 structural doc vs predecessor frozen doc | An ABAUDIT comment on a Phase-121/122/123 doc is allowed (this-milestone surface); the same on a predecessor frozen doc is forbidden |
| honest-accounting record → audit trail | Hiding the RED-then-green intermediate state produces a dishonest milestone-audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-05-VALUEMASK | Tampering | validator expected values | mitigate | Hard prohibition on value-masking (T1); git diff review asserts no expected-value bump; fix is ABAUDIT/readAtV115Close/NESTED-guard only |
| T-125-05-FROZEN | Tampering | v1.N frozen harness surfaces | mitigate | Remediation edits ONLY a v1.16 retrofitted doc or a predecessor check-phase-NN.mjs; frozen harness surfaces off-limits; byte-unchanged gate (125-04) re-runs after remediation |
| T-125-05-SKIP | Tampering | CHAIN_SKIP | mitigate | CHAIN_SKIP stays `new Set([])`; V-SELF hard-asserts size 0; adding an entry is the forbidden shortcut |
| T-125-05-REPUDIATE | Repudiation | honest-accounting record | mitigate | Discarded RED run ID + tally recorded; re-run GREEN run IDs recorded; no clean-first-pass fabrication |
| T-125-05-SC | Tampering | package installs | accept | No package-manager installs this phase |
</threat_model>

<verification>
- Fire/skip gate evaluated against 125-04 verdict
- (If fired) only sanctioned surfaces edited (v1.16 doc ABAUDIT / predecessor check-phase-NN); no value-mask; CHAIN_SKIP empty; re-pushed cascade green across all 9 firing apexes
- Honest-accounting narrative present with before/after run IDs
</verification>

<success_criteria>
Either a documented no-op skip (125-04 was GREEN) OR an honest, constraint-compliant remediation using the three sanctioned shapes (ABAUDIT on a retrofitted doc / readAtV115Close conversion / NESTED-guard, no value-mask, CHAIN_SKIP empty) that re-pushes the Axis-2 cascade to green. The close-gate remains a separate single commit.
</success_criteria>

<output>
Create `.planning/phases/125-v115-pin-14th-path-a-lineage-bump-terminal-close/125-05-SUMMARY.md` when done, recording fire-vs-skip, the real edited-file set + shape(s) used (if fired), and before/after Axis-2 cascade run IDs.
</output>
</content>
