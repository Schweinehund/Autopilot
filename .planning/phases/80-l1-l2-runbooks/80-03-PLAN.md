---
phase: 80-l1-l2-runbooks
plan: 03
type: execute
wave: 2
depends_on: ["80-01", "80-02"]
files_modified:
  - docs/l1-runbooks/00-index.md
  - docs/l2-runbooks/00-index.md
autonomous: true
requirements: [SSORUN-01, SSORUN-02, SSORUN-03]
must_haves:
  truths:
    - "docs/l1-runbooks/00-index.md lists #35 and #36 as rows in the macOS ADE Runbooks table (SC4)"
    - "docs/l2-runbooks/00-index.md lists #27 in the macOS ADE When-to-Use table (SC4)"
    - "docs/l2-runbooks/00-index.md macOS L1 Escalation Mapping table maps L1 #35 and L1 #36 → L2 #27 (SC4)"
    - "All edits are append-only additions to existing macOS sections; no existing rows are modified and no Phase-81-owned nav-hub file is touched"
  artifacts:
    - path: "docs/l1-runbooks/00-index.md"
      provides: "L1 index rows registering runbooks #35 and #36 under the macOS ADE Runbooks table"
      contains: "35-macos-sso-sign-in-failure.md"
    - path: "docs/l2-runbooks/00-index.md"
      provides: "L2 index row for #27 + escalation-mapping rows L1 #35/#36 → L2 #27"
      contains: "27-macos-sso-investigation.md"
  key_links:
    - from: "docs/l2-runbooks/00-index.md"
      to: "docs/l2-runbooks/27-macos-sso-investigation.md"
      via: "macOS L1 Escalation Mapping rows (SC4)"
      pattern: "27-macos-sso-investigation\\.md"
---

<objective>
Register the three Phase-80 runbooks in the L1 and L2 index files per SC4, using append-only edits to the existing macOS sections. The L1 index gains #35/#36 rows; the L2 index gains a #27 When-to-Use row AND two escalation-mapping rows (L1 #35 → L2 #27, L1 #36 → L2 #27).

Purpose: Make the new runbooks discoverable from the existing runbook indexes and wire the L1→L2 escalation mapping required by SC4. This is the in-phase index wiring only — all nav-hub and decision-tree-SSO-leaf wiring is Phase 81 (navigation-last invariant).

Output (edits):
- `docs/l1-runbooks/00-index.md`
- `docs/l2-runbooks/00-index.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — REQUIRED (binding decisions + copy-ready index-row excerpts)
@.planning/phases/80-l1-l2-runbooks/80-CONTEXT.md
@.planning/phases/80-l1-l2-runbooks/80-PATTERNS.md

# Files to edit (read in full to locate exact insertion points)
@docs/l1-runbooks/00-index.md
@docs/l2-runbooks/00-index.md

# SUMMARYs of the runbooks being registered (titles + filenames must match the row links exactly)
@.planning/phases/80-l1-l2-runbooks/80-01-SUMMARY.md
@.planning/phases/80-l1-l2-runbooks/80-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add #35/#36 rows to the L1 index macOS ADE Runbooks table</name>
  <files>docs/l1-runbooks/00-index.md</files>
  <read_first>
    - `docs/l1-runbooks/00-index.md` — locate the `## macOS ADE Runbooks` section and its `| # | Runbook | When to Use |` table (currently ending at the row for #15 `15-macos-company-portal-sign-in.md`, immediately before the `## iOS L1 Runbooks` section). Also locate the `## Version History` table near the end.
    - `.planning/phases/80-l1-l2-runbooks/80-PATTERNS.md` §"docs/l1-runbooks/00-index.md" — copy-ready new rows for #35/#36 and the Version History row; note the affordance at line 81 ("portal-first or terminal walkthrough as appropriate per cause") that legitimizes the Terminal step (the macOS section intro does NOT need to change).
  </read_first>
  <action>
    Append two rows to the existing macOS ADE Runbooks `| # | Runbook | When to Use |` table, AFTER the #15 row and BEFORE the `## iOS L1 Runbooks` section header (per PATTERNS.md copy-ready rows):
    - `| 35 | [Platform SSO Sign-In Failure](35-macos-sso-sign-in-failure.md) | "Registration Required" notification not appearing despite Intune Succeeded, or Platform SSO sign-in not working after registration attempt |`
    - `| 36 | [Platform SSO — Secure Enclave Key Loss](36-macos-secure-enclave-key.md) | Platform SSO stopped working after a password reset or FileVault recovery key use |`

    Prepend a new row at the TOP of the `## Version History` table (below the header row):
    - `| 2026-06-21 | Phase 80 SSORUN-01/02: added macOS Platform SSO runbooks #35 and #36 to macOS ADE Runbooks table | -- |`

    Do NOT modify the macOS section intro, any existing row, or any other section. Append-only. Do NOT touch any nav-hub file or the decision-tree (Phase 81).
  </action>
  <verify>
    <automated>grep -q '35-macos-sso-sign-in-failure.md' docs/l1-runbooks/00-index.md && grep -q '36-macos-secure-enclave-key.md' docs/l1-runbooks/00-index.md && grep -q 'Phase 80 SSORUN-01/02' docs/l1-runbooks/00-index.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/l1-runbooks/00-index.md` macOS ADE Runbooks table contains a row linking `35-macos-sso-sign-in-failure.md` (numbered 35).
    - The same table contains a row linking `36-macos-secure-enclave-key.md` (numbered 36).
    - Both rows appear after the #15 row and before the `## iOS L1 Runbooks` header.
    - A new Version History row dated 2026-06-21 referencing Phase 80 SSORUN-01/02 is present at the top of the Version History table.
    - No existing row or section intro was modified.
  </acceptance_criteria>
  <done>The L1 index registers #35 and #36 in the macOS ADE Runbooks table with the correct link filenames, plus a Phase-80 Version History row; the verify command prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Add #27 row + L1 #35/#36 → L2 #27 escalation mapping to the L2 index</name>
  <files>docs/l2-runbooks/00-index.md</files>
  <read_first>
    - `docs/l2-runbooks/00-index.md` — locate the `## macOS ADE Runbooks` section, its `### When to Use` table (currently ending at the Compliance Evaluation Investigation `13-macos-compliance.md` row), and the `### macOS L1 Escalation Mapping` table (currently ending at the "Company Portal sign-in failure" row). Do NOT modify the version-gate blockquote at the top of the macOS section. Also locate the `## Version History` table near the end.
    - `.planning/phases/80-l1-l2-runbooks/80-PATTERNS.md` §"docs/l2-runbooks/00-index.md" — copy-ready new When-to-Use row, the two escalation-mapping rows (use the linked `[L1 NN: Title](../l1-runbooks/NN-file.md)` format, matching the iOS/Android tables), and the Version History row.
  </read_first>
  <action>
    Append one row to the macOS ADE `### When to Use` table, AFTER the Compliance Evaluation Investigation row:
    - `| [Platform SSO Investigation](27-macos-sso-investigation.md) | PSSO registration not completing, "Registration Required" loop, or Password-sync sign-in failure | [macOS Log Collection](10-macos-log-collection.md) |`

    Append two rows to the `### macOS L1 Escalation Mapping` table, AFTER the "Company Portal sign-in failure" row (linked format per PATTERNS.md):
    - `| [L1 35: Platform SSO Sign-In Failure](../l1-runbooks/35-macos-sso-sign-in-failure.md) | [Platform SSO Investigation](27-macos-sso-investigation.md) — Registration Failure track |`
    - `| [L1 36: Platform SSO — Secure Enclave Key Loss](../l1-runbooks/36-macos-secure-enclave-key.md) | [Platform SSO Investigation](27-macos-sso-investigation.md) — Registration Failure track (SE key re-registration path) |`

    Prepend a new row at the TOP of the `## Version History` table (below the header row):
    - `| 2026-06-21 | Phase 80 SSORUN-03/SC4: added macOS Platform SSO Investigation #27 to When-to-Use table; added L1 #35 and L1 #36 escalation mapping rows to macOS L1 Escalation Mapping table | -- |`

    Do NOT modify the macOS version-gate blockquote, any existing row, or any other platform's section. Append-only. Do NOT touch any nav-hub file or the decision-tree (Phase 81).
  </action>
  <verify>
    <automated>grep -q '27-macos-sso-investigation.md' docs/l2-runbooks/00-index.md && grep -q '35-macos-sso-sign-in-failure.md' docs/l2-runbooks/00-index.md && grep -q '36-macos-secure-enclave-key.md' docs/l2-runbooks/00-index.md && grep -q 'Phase 80 SSORUN-03/SC4' docs/l2-runbooks/00-index.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/l2-runbooks/00-index.md` macOS ADE `### When to Use` table contains a row linking `27-macos-sso-investigation.md` with `10-macos-log-collection.md` as prerequisite.
    - The `### macOS L1 Escalation Mapping` table contains a row mapping L1 #35 (`35-macos-sso-sign-in-failure.md`) → `27-macos-sso-investigation.md`.
    - The same table contains a row mapping L1 #36 (`36-macos-secure-enclave-key.md`) → `27-macos-sso-investigation.md`.
    - A new Version History row dated 2026-06-21 referencing Phase 80 SSORUN-03/SC4 is present at the top of the Version History table.
    - The macOS version-gate blockquote and all existing rows are unmodified.
  </acceptance_criteria>
  <done>The L2 index registers #27 in the When-to-Use table and maps both L1 #35 and L1 #36 to L2 #27 in the escalation-mapping table, plus a Phase-80 Version History row; the verify command prints PASS.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only deliverable, no executable code or new attack surface; these are append-only Markdown table-row edits to existing index files. No user-input handling, no service, no installed packages. No STRIDE register required for this phase.
</threat_model>

<verification>
- `docs/l1-runbooks/00-index.md` macOS ADE Runbooks table contains #35 and #36 rows with correct link filenames.
- `docs/l2-runbooks/00-index.md` macOS When-to-Use table contains the #27 row, and the macOS L1 Escalation Mapping table maps both L1 #35 and L1 #36 to L2 #27 (SC4).
- Both files gain an append-only Phase-80 Version History row; no existing rows or section intros modified.
- No Phase-81-owned nav-hub file (docs/index.md, common-issues.md, quick-ref-l1/l2.md) or the decision-tree (06-macos-triage.md) is touched.
</verification>

<success_criteria>
- SSORUN-01/02/03 index registration satisfied: ROADMAP Phase 80 SC4 — both index files have new rows for the Phase-80 runbooks and the L2 escalation-mapping table maps L1 #35 and L1 #36 → L2 #27.
- Navigation-last invariant honored: only the two runbook index files are edited; all nav-hub + decision-tree SSO-leaf wiring remains deferred to Phase 81.
</success_criteria>

<output>
Create `.planning/phases/80-l1-l2-runbooks/80-03-SUMMARY.md` when done.
</output>
