---
phase: 67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Branch A (expected case — URLs alive):
  - scripts/validation/v1.6-audit-allowlist.json
  # Branch B (conditional — only if any of the 4 URLs drifted):
  # - docs/admin-setup-ios/05-app-deployment.md
  # - docs/admin-setup-macos/01-abm-configuration.md
  # - docs/admin-setup-macos/04-app-deployment.md
  # - docs/_glossary-macos.md
autonomous: true
requirements:
  - SWEEP-01
tags:
  - validation
  - rotting-references
  - apple-business
  - branchable
  - phase-67

must_haves:
  truths:
    - "All 4 c13_rotting_external.ci_1_abm_urls entries have been verified against live Apple URL state on 2026-05-26 using markdown-link-check@3.14.2 (the exact pin used by audit-harness-v1.6-integrity.yml:167 rotting-external-quarterly job)"
    - "Tool stdout (JSON results) AND corroborating curl HEAD output are captured into a draft 67-VERIFICATION.md SWEEP-01 H2 subsection (4 sub-blocks: Mechanism / Tool Output / Corroborating Evidence / Outcome) for Plan 67-03 to finalize"
    - "Branch decision is recorded explicitly in the draft 67-VERIFICATION.md: Branch A if all 4 URLs returned status='alive'; Branch B if ANY URL returned status='dead' or non-200 statusCode"
    - "Sidecar scripts/validation/v1.6-audit-allowlist.json c13_rotting_external.ci_1_abm_urls retains its 4-entry array shape (V-66-02 shape stability per check-phase-66.mjs:85-112) AND each of the 4 entries gains a new field \"last_revalidated\": \"2026-05-26\" (ANNOTATE-not-remove per CONTEXT.md D-04)"
    - "If Branch A: NO corpus edits land; commit touches only scripts/validation/v1.6-audit-allowlist.json"
    - "If Branch B: only the affected lines on the affected file(s) are surgically updated; each affected file gains a tail-table row dated 2026-05-26 AND a frontmatter last_verified: 2026-05-26 bump; _glossary-macos.md gains one coordinating row in its ## Version History H2 table at line 121"
    - "Pre-commit dry-run triple (v1.6-milestone-audit.mjs + check-phase-62.mjs + check-phase-66.mjs) all exit 0 BEFORE git add"
    - "JSON parse guard runs immediately after sidecar edit: node -e \"JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); console.log('OK')\" prints OK"
    - "Single git revert <Plan 67-01 SHA> cleanly restores pre-SWEEP-01 baseline (per-plan boundary preserved per CONTEXT.md D-04 Option E)"
  artifacts:
    - path: scripts/validation/v1.6-audit-allowlist.json
      provides: "c13_rotting_external.ci_1_abm_urls: 4 entries annotated with last_revalidated:2026-05-26 (Branch A) OR updated + annotated (Branch B); 4-entry shape preserved either way"
      contains:
        - "\"c13_rotting_external\":"
        - "\"ci_1_abm_urls\""
        - "\"last_revalidated\": \"2026-05-26\""
        - "\"quarterly_audit\":"
        - "\"0 8 1 1,4,7,10 *\""
    - path: .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-VERIFICATION.md
      provides: "Draft created (or appended-to) with SWEEP-01 H2 subsection capturing mechanism + tool output + corroborating evidence + branch outcome — Plan 67-03 close-gate finalizes the rest of the artifact"
      contains:
        - "## SWEEP-01: ABM URL Live-State Verification (2026-05-26)"
        - "markdown-link-check@3.14.2"
        - "### Mechanism"
        - "### Tool Output"
        - "### Corroborating Evidence"
        - "### Outcome"
  key_links:
    - from: "scripts/validation/v1.6-audit-allowlist.json c13_rotting_external.ci_1_abm_urls[*]"
      to: "live Apple endpoint https://business.apple.com"
      via: "markdown-link-check@3.14.2 alive/dead probe"
      pattern: "https://business\\.apple\\.com"
    - from: "scripts/validation/v1.6-audit-allowlist.json c13_rotting_external.ci_1_abm_urls[*].last_revalidated"
      to: "audit-harness-v1.6-integrity.yml:160 rotting-external-quarterly cron schedule"
      via: "per-entry timestamp annotation traceable to cron next-fire 2026-07-01"
      pattern: "\"last_revalidated\":\\s*\"2026-05-26\""
---

<objective>
Verify the 4 `business.apple.com` URL references catalogued in `scripts/validation/v1.6-audit-allowlist.json` `c13_rotting_external.ci_1_abm_urls` against live Apple URL state using the same tool + pin (`markdown-link-check@3.14.2`) that the `audit-harness-v1.6-integrity.yml:167` `rotting-external-quarterly` job uses, capture the evidence into a draft `67-VERIFICATION.md` SWEEP-01 H2 subsection, then land EITHER:

- **Branch A (expected case — all 4 URLs return `status: 'alive'`):** annotate each of the 4 sidecar entries with `"last_revalidated": "2026-05-26"`. NO corpus edits. Single sidecar-only commit. Quarterly cron continues monitoring (next fire 2026-07-01).
- **Branch B (contingent — any URL returned `dead` or non-200 statusCode):** surgically update the affected line(s) on the affected file(s); append a tail-table Version History row dated 2026-05-26 to each affected file; bump `last_verified: 2026-05-26` frontmatter on each; append a coordinating row to `_glossary-macos.md` `## Version History` H2 table at line 121; annotate the sidecar entries with `"last_revalidated": "2026-05-26"` AND update the `url` / `line` / `reason` fields as the surgical updates require.

**Purpose:** SWEEP-01 contract per REQUIREMENTS.md:12 + ROADMAP.md:280 — produce fresh evidence of live Apple URL state at Phase 67 close-gate (not "trust prior calibration"). Uses the cron-pinned tool locally for IS-equivalent evidence to a hypothetical 2026-07-01 first-fire (D-02 locked). Branch A is the most-likely outcome per Phase 66 calibration on 2026-05-25 + advisor's corroborating `curl -I https://business.apple.com` returning `HTTP/1.1 200 OK / Server: Apple / no redirect` on 2026-05-26 14:29:40 UTC; the branchable plan structure absorbs either outcome without replanning.

**Output:**
1. Draft `.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-VERIFICATION.md` containing the SWEEP-01 H2 subsection with mechanism / tool output / corroborating evidence / outcome blocks (Plan 67-03 close-gate appends SC#1-4 checklist + validator exit table later)
2. `scripts/validation/v1.6-audit-allowlist.json` `c13_rotting_external.ci_1_abm_urls` 4 entries annotated with `"last_revalidated": "2026-05-26"`; entry shape preserved (V-66-02 shape stable)
3. Branch B only: surgical line updates on affected files + tail-table VH rows + frontmatter `last_verified:` bumps + glossary `## Version History` H2 coordinating row
4. ONE atomic commit (single SHA) per Branch A or Branch B commit-message variant per CONTEXT.md D-04 lines 125-128

**Branchable plan grammar:** Wave 1 produces evidence and a decision; Wave 2 forks; Wave 3 commits and post-verifies. Per CONTEXT.md D-04 line 152: "SWEEP-01 and SWEEP-02 are logically independent — a clean revert per requirement is the desired property." Plan 67-02 (atomic SWEEP-02 commit) runs sequentially AFTER this plan completes per the Wave 2 dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-CONTEXT.md
@.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-RESEARCH.md
@.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-PATTERNS.md
@.planning/milestones/v1.6-DEFERRED-CLEANUP.md
@.planning/research/PITFALLS.md
@scripts/validation/v1.6-audit-allowlist.json
@scripts/validation/check-phase-66.mjs
@scripts/validation/check-phase-62.mjs
@scripts/validation/v1.6-milestone-audit.mjs
@.github/workflows/audit-harness-v1.6-integrity.yml

<interfaces>
<!-- Exact line refs + before/after snippets. Executor MUST read these source files to verify pre-edit state. -->

**`scripts/validation/v1.6-audit-allowlist.json` lines 79-85 — `c13_rotting_external.ci_1_abm_urls` (4 entries, current state at HEAD):**

```json
"c13_rotting_external": {
  "ci_1_abm_urls": [
    { "url": "https://business.apple.com", "file": "docs/admin-setup-ios/05-app-deployment.md", "line": 92, "reason": "ABM live portal URL; retained per Apple newsroom post-rebrand; included for completeness — quarterly check confirms reachability", "category": "live_url_quarterly_check" },
    { "url": "https://business.apple.com", "file": "docs/admin-setup-macos/01-abm-configuration.md", "line": 52, "reason": "ABM live portal URL; retained per Apple newsroom post-rebrand", "category": "live_url_quarterly_check" },
    { "url": "https://business.apple.com", "file": "docs/admin-setup-macos/04-app-deployment.md", "line": 105, "reason": "ABM live portal URL; retained per Apple newsroom post-rebrand", "category": "live_url_quarterly_check" },
    { "url": "https://business.apple.com", "file": "docs/_glossary-macos.md", "line": 64, "reason": "ABM live portal URL in definitional prose; retained per Apple newsroom post-rebrand", "category": "live_url_quarterly_check" }
  ],
```

**Branch A annotation pattern (4 entries; ANNOTATE-not-remove per CONTEXT.md D-04):**

For each entry, APPEND ONE new field `"last_revalidated": "2026-05-26"` immediately before the closing `}`. The new field is the LAST property in the entry object (matching the precedent where `category` is the closing field). Net diff: exactly 4 lines changed; each line gains `, "last_revalidated": "2026-05-26"` immediately before the `}` close brace. Single-line compact object shape preserved (no pretty-print expansion).

Example AFTER for entry #1:
```json
{ "url": "https://business.apple.com", "file": "docs/admin-setup-ios/05-app-deployment.md", "line": 92, "reason": "ABM live portal URL; retained per Apple newsroom post-rebrand; included for completeness — quarterly check confirms reachability", "category": "live_url_quarterly_check", "last_revalidated": "2026-05-26" }
```

**Branch B contingency:** For any URL that returned `status: 'dead'` OR a non-200 statusCode, the executor MUST:
1. Identify the affected file(s) via the entry's `file` + `line` fields
2. Surgically update the URL on each affected file (read the file, locate the URL via grep, replace with the new URL from the markdown-link-check output if Apple returned a 301 with Location header; if a true sunset/404, replace with the closest Apple replacement URL per the markdown-link-check error message OR pause execution to request human input)
3. Update the sidecar entry: change `"url"` to the new URL; update `"line"` if the new URL is on a different line; update `"reason"` to append `"; v1.7 Phase 67 SWEEP-01 surgical update 2026-05-26 (Apple sunset/redirect)"`; AND add `"last_revalidated": "2026-05-26"`
4. Append tail-table row to each affected file (text per "Branch B tail-table row text" below)
5. Bump `last_verified: 2026-05-26` frontmatter on each affected file
6. Append ONE coordinating row to `docs/_glossary-macos.md` `## Version History` H2 table at line 124 (newest-first; immediately after the `|------|` separator)

**Branch B tail-table row text (per CONTEXT.md Claude's Discretion line 159 — date + phase + SWEEP-NN + closure marker preserved):**

```
| 2026-05-26 | Phase 67 (SWEEP-01): updated business.apple.com reference at L<line> per live-state verification (PITFALLS.md CI-1 closure; Branch B surgical update) | -- |
```

**Branch B glossary `## Version History` H2 coordinating row text (per 67-PATTERNS.md:154-157):**

```
| 2026-05-26 | Phase 67 (SWEEP-01): coordinating row for business.apple.com surgical update at L64 (PITFALLS.md CI-1 closure; Branch B) | -- |
```

(Note: this row only lands if `_glossary-macos.md` line 64 itself was affected; if a DIFFERENT file's URL drifted, the glossary still gets a coordinating row referencing that file rather than L64.)

**Sidecar JSON parse guard (run immediately after every sidecar edit per 67-PATTERNS.md:206-208 / 67-RESEARCH.md Pitfall 1):**

```
node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); console.log('OK')"
```

Expected output: `OK`. Any other output (parse error message) means a brace/comma is broken — fix BEFORE `git add`.

**Pre-commit dry-run triple (per CONTEXT.md D-04 Wave 7 pattern, scoped to 3 validators sufficient for the sidecar-only edit set):**

```
node scripts/validation/v1.6-milestone-audit.mjs   # expect exit 0 (C11/C14/C15/C16 inert on Phase 67 surface per CONTEXT.md D-03 line 99)
node scripts/validation/check-phase-62.mjs         # expect exit 0 (V-62-SIDECAR only asserts c16==0; c13 annotations are free-text)
node scripts/validation/check-phase-66.mjs         # expect exit 0 modulo CHAIN_SKIP {48,51,58,60,61}; V-66-02 verifies c13_rotting_external is object + quarterly_audit.cadence
```

**markdown-link-check@3.14.2 invocation (per 67-RESEARCH.md Example 1 + CONTEXT.md D-02 lines 59-65):**

```
npm install --no-save markdown-link-check@3.14.2

node -e "require('markdown-link-check')('[ABM](https://business.apple.com)', { timeout: '10s' }, (err, results) => { if (err) { console.error('markdown-link-check error:', err.message); process.exit(2); } console.log(JSON.stringify(results, null, 2)); process.exit(results[0].status === 'alive' ? 0 : 1); });"
```

The 4 `ci_1_abm_urls` entries are all the same domain (`https://business.apple.com`) — per CONTEXT.md Claude's Discretion line 158, ONE probe is sufficient for the 4 entries; log all 4 entries in 67-VERIFICATION.md for traceability. (Per 67-RESEARCH.md Pitfall 2: probing once also naturally avoids Apple's bot-mitigation rate-limiting on 4-in-rapid-succession requests.)

**Corroborating curl HEAD probe (per CONTEXT.md D-02 line 48 + 67-RESEARCH.md Example 3 — SECONDARY evidence):**

```
curl.exe -I -L --max-time 10 https://business.apple.com
```

Expected: `HTTP/1.1 200 OK`, `Server: Apple`, no rebrand-banner header. Use `curl.exe` explicitly (not the PowerShell `curl` alias which is `Invoke-WebRequest`).

**67-VERIFICATION.md SWEEP-01 H2 subsection template (per 67-RESEARCH.md Example 10 + 67-PATTERNS.md:312-335 — author this as a NEW file or, if Plan 67-03 has already started a draft, append):**

```markdown
# Phase 67 — Verification & Close-Gate Report

**Closed:** TBD (Plan 67-03)
**Status:** in-progress

## SWEEP-01: ABM URL Live-State Verification (2026-05-26)

### Mechanism
`markdown-link-check@3.14.2` invoked locally via `node -e "require('markdown-link-check')(...)"` — same pin as `audit-harness-v1.6-integrity.yml:167` `rotting-external-quarterly` job; IS-equivalent evidence per CONTEXT.md D-02. Pinned via `npm install --no-save markdown-link-check@3.14.2` to avoid `package.json` mutation (no top-level `package.json` exists at repo root — verified per 67-RESEARCH.md A4).

### Tool Output (primary evidence)
[Paste verbatim JSON output from markdown-link-check invocation here, inside a ```json fenced block]
- Invocation: `node -e "require('markdown-link-check')('[ABM](https://business.apple.com)', { timeout: '10s' }, ...)"`
- Captured: 2026-05-26 HH:MM:SS UTC (substitute actual timestamp at execution time)
- Exit code: <0 if alive | 1 if dead | 2 if tool error>

### Corroborating Evidence (secondary — curl HEAD)
[Paste verbatim curl.exe -I output here, inside a ``` fenced block]
- Captured: 2026-05-26 HH:MM:SS UTC

### Outcome
**Branch A** — All 4 ci_1_abm_urls entries (admin-setup-ios/05-app-deployment.md:92, admin-setup-macos/01-abm-configuration.md:52, admin-setup-macos/04-app-deployment.md:105, _glossary-macos.md:64) point to the same alive URL; sidecar annotated with `"last_revalidated": "2026-05-26"`; no corpus edits required. Quarterly cron continues monitoring (next fire 2026-07-01).

OR (substitute if Branch B):

**Branch B** — N of 4 URLs returned non-alive status; surgical updates applied to <list affected files>; tail-table Version History rows appended; sidecar entries updated.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 67-01-01: SWEEP-01 ABM URL verification + branchable corpus/sidecar edit + draft 67-VERIFICATION.md SWEEP-01 H2 subsection + pre-commit dry-run + atomic commit</name>
  <files>scripts/validation/v1.6-audit-allowlist.json, .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-VERIFICATION.md, docs/admin-setup-ios/05-app-deployment.md, docs/admin-setup-macos/01-abm-configuration.md, docs/admin-setup-macos/04-app-deployment.md, docs/_glossary-macos.md</files>
  <read_first>
    - scripts/validation/v1.6-audit-allowlist.json (read lines 75-122 — verify the current 4-entry `ci_1_abm_urls` array shape at lines 80-85, the 6-entry `ci_2_vpp_location_token` array at lines 86-93, and the `quarterly_audit` metadata at lines 113-118; these MUST remain shape-stable across this plan)
    - scripts/validation/check-phase-66.mjs (read lines 40-112 — verify CHAIN_PHASES at line 43, CHAIN_SKIP at line 64, and V-66-02 c13_rotting_external object-shape + quarterly_audit.cadence assertion at lines 85-112)
    - scripts/validation/check-phase-62.mjs (read lines 270-295 — V-62-SIDECAR currently asserts c16_missing_endpoint_exemptions.length == 0; c13 annotations are NOT shape-enforced)
    - scripts/validation/v1.6-milestone-audit.mjs (read lines 90-130 to confirm appleBusinessDocPaths() C15 scope EXCLUDES both admin-setup-ios/05-app-deployment.md and admin-setup-macos/04-app-deployment.md — confirms C15 cannot fire on Phase 67 edited surface)
    - .github/workflows/audit-harness-v1.6-integrity.yml (read lines 150-189 — verify markdown-link-check@3.14.2 pin at line 167, cron `0 8 1 1,4,7,10 *` guard at line 160; this file is NOT modified by Plan 67-01 per REQUIREMENTS.md:64)
    - docs/admin-setup-ios/05-app-deployment.md (read lines 1-7 frontmatter + line 92 for SWEEP-01 URL site context — for potential Branch B)
    - docs/admin-setup-macos/01-abm-configuration.md (read lines 1-7 frontmatter + line 52 for SWEEP-01 URL site context)
    - docs/admin-setup-macos/04-app-deployment.md (read lines 1-7 frontmatter + line 105 for SWEEP-01 URL site context)
    - docs/_glossary-macos.md (read lines 1-7 frontmatter + line 64 for SWEEP-01 URL site context + lines 121-128 for existing `## Version History` H2 table — needed for Branch B coordinating row)
    - .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-CONTEXT.md (read D-02 mechanism at lines 46-71 + D-04 commit topology at lines 101-152 — branchable plan grammar + ANNOTATE-not-remove sidecar mode)
    - .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-RESEARCH.md (read §SWEEP-01 Execution Mechanics + §Code Examples #1-3 — invocation + capture + corroborating; §Common Pitfalls 1 + 2 + 5)
    - .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-PATTERNS.md (read sidecar per-entry annotation pattern at lines 168-209)
  </read_first>
  <action>
**WAVE 1 — VERIFICATION (probe + capture, no edits yet)**

1. Install the cron-pinned tool one-shot (no `package.json` mutation since no top-level `package.json` exists per 67-RESEARCH.md A4):

```
npm install --no-save markdown-link-check@3.14.2
```

Confirm install succeeded; the directory `node_modules/markdown-link-check/` should exist post-install.

2. Verify pin matches the cron:

```
npm view markdown-link-check@3.14.2 version
```

Expected output: `3.14.2`. If anything else, STOP and investigate — pin drift breaks D-02 IS-equivalence.

3. Probe `https://business.apple.com` via `markdown-link-check` ONCE (the 4 sidecar entries are all the same URL; one probe is sufficient per CONTEXT.md Claude's Discretion line 158 + 67-RESEARCH.md Pitfall 2 rate-limiting avoidance). Capture stdout to a temp file:

```
node -e "require('markdown-link-check')('[ABM](https://business.apple.com)', { timeout: '10s' }, (err, results) => { if (err) { console.error('markdown-link-check error:', err.message); process.exit(2); } console.log(JSON.stringify(results, null, 2)); process.exit(results[0].status === 'alive' ? 0 : 1); });" 2>&1 | Tee-Object -Variable mlcOutput -FilePath .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-mlc-output.txt
$mlcExit = $LASTEXITCODE
Write-Host "markdown-link-check exit code: $mlcExit"
```

4. Run the corroborating `curl.exe` HEAD probe (use `curl.exe` explicitly, NOT the PowerShell `curl` alias — `curl` resolves to `Invoke-WebRequest` in PowerShell 5+ and has different output shape):

```
curl.exe -I -L --max-time 10 https://business.apple.com 2>&1 | Tee-Object -FilePath .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/_tmp-curl-head.txt
```

Expected: `HTTP/1.1 200 OK` first line; `Server: Apple` header present; no rebrand-banner header. If the curl returns 200 BUT markdown-link-check returned `dead`, that's the 67-RESEARCH.md Pitfall 2 rate-limiting scenario — document under "tool divergence" in the 67-VERIFICATION.md subsection but treat the CURL 200 as the authoritative live-state signal (Branch A still applies).

5. **Decision point — Branch selection:**
   - If markdown-link-check exit was 0 AND `results[0].status === 'alive'` AND statusCode 200 → **Branch A** (expected case)
   - If markdown-link-check exit was 1 AND result was 429/503/transient AND curl returned 200 OK → **Branch A** (Pitfall 2 rate-limit; document divergence but treat as alive)
   - If markdown-link-check exit was 1 AND result was 301/302 with a Location header → **Branch B** (Apple issued a redirect; update URL to the Location target)
   - If markdown-link-check exit was 1 AND result was 404/410/sunset AND curl also returns non-200 → **Branch B** (Apple sunset; surgical update required)
   - If markdown-link-check exit was 2 (tool error) → STOP, pause execution, capture the tool stderr into _tmp-mlc-output.txt and investigate (not a Branch A or Branch B condition)

**WAVE 2 — DRAFT 67-VERIFICATION.md SWEEP-01 H2 SUBSECTION**

Create `.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-VERIFICATION.md` (NEW file). Use the template in `<interfaces>` "67-VERIFICATION.md SWEEP-01 H2 subsection template". Populate:

- Document header: `# Phase 67 — Verification & Close-Gate Report` + `**Closed:** TBD (Plan 67-03)` + `**Status:** in-progress`
- `## SWEEP-01: ABM URL Live-State Verification (2026-05-26)` H2 + 4 H3 subsections (Mechanism / Tool Output / Corroborating Evidence / Outcome)
- Paste verbatim contents of `_tmp-mlc-output.txt` inside a ```json fenced block under `### Tool Output`
- Paste verbatim contents of `_tmp-curl-head.txt` inside a ``` fenced block under `### Corroborating Evidence`
- Under `### Outcome`: select the Branch A OR Branch B paragraph per the Wave 1 decision; substitute the actual N count if Branch B

(Plan 67-03 close-gate later appends the SWEEP-02 H2, validator exit code table, SC#1-4 satisfaction checklist, and traceability blocks — Plan 67-01 stops at the SWEEP-01 H2 subsection.)

**WAVE 2A — BRANCH A (expected case; URLs unchanged)**

If Wave 1 selected Branch A:

EDIT 1 — `scripts/validation/v1.6-audit-allowlist.json` lines 81-84 (the 4 `ci_1_abm_urls` entries):

For each of the 4 entries, append the new field `, "last_revalidated": "2026-05-26"` immediately before the closing `}` of the entry. The new field is the LAST property in the entry object (matching the precedent where `category` was the closing field). After this edit, each of lines 81-84 gains ~30 characters; the 4-entry array shape is preserved.

AFTER state (verbatim, for verification):

```json
"c13_rotting_external": {
  "ci_1_abm_urls": [
    { "url": "https://business.apple.com", "file": "docs/admin-setup-ios/05-app-deployment.md", "line": 92, "reason": "ABM live portal URL; retained per Apple newsroom post-rebrand; included for completeness — quarterly check confirms reachability", "category": "live_url_quarterly_check", "last_revalidated": "2026-05-26" },
    { "url": "https://business.apple.com", "file": "docs/admin-setup-macos/01-abm-configuration.md", "line": 52, "reason": "ABM live portal URL; retained per Apple newsroom post-rebrand", "category": "live_url_quarterly_check", "last_revalidated": "2026-05-26" },
    { "url": "https://business.apple.com", "file": "docs/admin-setup-macos/04-app-deployment.md", "line": 105, "reason": "ABM live portal URL; retained per Apple newsroom post-rebrand", "category": "live_url_quarterly_check", "last_revalidated": "2026-05-26" },
    { "url": "https://business.apple.com", "file": "docs/_glossary-macos.md", "line": 64, "reason": "ABM live portal URL in definitional prose; retained per Apple newsroom post-rebrand", "category": "live_url_quarterly_check", "last_revalidated": "2026-05-26" }
  ],
```

JSON parse guard (MANDATORY immediately after edit):

```
node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); console.log('OK')"
```

Expected: prints `OK`. If anything else, the edit broke JSON syntax — fix BEFORE proceeding.

Line-ending sanity check (per 67-RESEARCH.md Pitfall 5):

```
git diff scripts/validation/v1.6-audit-allowlist.json
```

Expected: ~4-6 lines of diff (4 entries gained one field each; possibly one or two whitespace lines). If the entire file shows as changed, CRLF/LF drift occurred — revert with `git checkout scripts/validation/v1.6-audit-allowlist.json` and re-edit using LF preservation.

NO other files are touched in Branch A (NO corpus edits per CONTEXT.md D-04 line 116 + REQUIREMENTS.md:12 "Either confirmed-current (no edits required ...)").

**WAVE 2B — BRANCH B (contingent; one or more URLs drifted)**

If Wave 1 selected Branch B:

For EACH affected URL (where markdown-link-check returned non-alive or non-200):

1. Identify the affected file + line from the sidecar entry (file/line pair). The 4 candidate sites are:
   - `docs/admin-setup-ios/05-app-deployment.md:92`
   - `docs/admin-setup-macos/01-abm-configuration.md:52`
   - `docs/admin-setup-macos/04-app-deployment.md:105`
   - `docs/_glossary-macos.md:64`

2. Determine the replacement URL:
   - If markdown-link-check output included a Location header (301/302), use that as the replacement
   - If markdown-link-check output is 404/410/sunset, use the closest Apple replacement (Apple's standard practice is to redirect business.apple.com to apps.apple.com/business or similar; if no obvious replacement, pause and request human input)

3. Surgically update the URL on the affected file (use `Edit` tool with exact line precision — read the file at the affected line first to confirm pre-edit state matches the sidecar's `line` field).

4. Append a tail-table Version History row to the affected file (immediately after the last existing row; do NOT promote to H2 — D-01 forbids new H2 in admin-setup-* files):

```
| 2026-05-26 | Phase 67 (SWEEP-01): updated business.apple.com reference at L<line> per live-state verification (PITFALLS.md CI-1 closure; Branch B surgical update) | -- |
```

Substitute `<line>` with the actual affected line number.

5. Bump frontmatter `last_verified:` to `2026-05-26` on the affected file. Existing frontmatter values:
   - admin-setup-ios/05-app-deployment.md:2 → currently `last_verified: 2026-04-18`
   - admin-setup-macos/01-abm-configuration.md:2 → currently `last_verified: 2026-05-21`
   - admin-setup-macos/04-app-deployment.md:2 → currently `last_verified: 2026-04-14`
   - _glossary-macos.md:2 → currently `last_verified: 2026-05-05`

   Replace the existing date with `2026-05-26`. Leave `review_by:` and other fields byte-identical.

6. Append ONE coordinating row to `docs/_glossary-macos.md` `## Version History` H2 table at line 124 (newest-first; insert immediately after the `|------|------|--------|` separator at line 124 so the new row becomes the new top row at line 125; existing top row at current line 125 becomes line 126):

```
| 2026-05-26 | Phase 67 (SWEEP-01): coordinating row for business.apple.com surgical update at <affected file>:L<line> (PITFALLS.md CI-1 closure; Branch B) | -- |
```

Substitute `<affected file>` + `<line>` per the actual Wave 1 finding. If MULTIPLE URLs drifted across MULTIPLE files, condense to a single row that references all affected files.

   PITFALL-6 anti-regression note: the H2 `## Version History` at line 121 MUST remain at line 121 post-edit. The row insertion happens at line 124-125 INSIDE the existing table; H2 line position is unaffected by intra-table row inserts. No new blank lines should be added between rows.

   (Plan 67-01 does NOT capture an anchor inventory for `_glossary-macos.md` — that's Plan 67-02's responsibility for SWEEP-02. If Plan 67-01 lands a Branch B edit to `_glossary-macos.md`, Plan 67-02 Wave 1 anchor inventory captures the post-Branch-B state as its pre-SWEEP-02 baseline.)

7. Update each affected sidecar entry in `c13_rotting_external.ci_1_abm_urls`:
   - Update `"url"` to the new URL (if changed)
   - Update `"line"` to the new line number (if surgical update shifted the URL position)
   - Append `; v1.7 Phase 67 SWEEP-01 surgical update 2026-05-26 (Apple <redirect|sunset>)` to the `"reason"` field
   - Add `"last_revalidated": "2026-05-26"`

   Non-affected entries (URLs that returned alive) still get `"last_revalidated": "2026-05-26"` annotation per D-04 ANNOTATE-not-remove.

   JSON parse guard (MANDATORY after each entry edit, or once after the batch):

```
node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); console.log('OK')"
```

**WAVE 3 — PRE-COMMIT DRY-RUN + ATOMIC COMMIT + POST-COMMIT VERIFY**

1. Pre-commit dry-run triple (MANDATORY before `git add` per CONTEXT.md D-04 + 67-PATTERNS.md:556-564):

```
node scripts/validation/v1.6-milestone-audit.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 1 FAILED: harness exit $LASTEXITCODE — Phase 67 should be inert per CONTEXT.md D-03 line 99" }
node scripts/validation/check-phase-62.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 2 FAILED: V-62-SIDECAR canary — c16_missing_endpoint_exemptions check broke?" }
node scripts/validation/check-phase-66.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 3 FAILED: V-66-02 sidecar shape (c13_rotting_external object + quarterly_audit.cadence) — Phase 67 edits broke V-66-02?" }
```

ALL 3 MUST exit 0 (check-phase-66.mjs may report 5 SKIPPED entries per CHAIN_SKIP {48,51,58,60,61}; SKIPPED is NOT failure). If any fail, fix the underlying issue and re-run; do NOT `git add` until green.

2. Atomic commit (Branch A — sidecar-only OR Branch B — corpus + sidecar + glossary):

**Branch A commit** (touches only `scripts/validation/v1.6-audit-allowlist.json` and `.planning/phases/67-.../67-VERIFICATION.md`):

```
git add scripts/validation/v1.6-audit-allowlist.json .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-VERIFICATION.md

git commit -m "$(cat <<'EOF'
docs(67-01): SWEEP-01 — 4 ABM URLs verified live + sidecar last_revalidated annotation (no corpus edits)

Verified all 4 c13_rotting_external.ci_1_abm_urls entries against live Apple URL state on 2026-05-26 using markdown-link-check@3.14.2 (the exact pin used by audit-harness-v1.6-integrity.yml:167 rotting-external-quarterly job — IS-equivalent evidence per CONTEXT.md D-02).

All 4 entries (admin-setup-ios/05-app-deployment.md:92, admin-setup-macos/01-abm-configuration.md:52, admin-setup-macos/04-app-deployment.md:105, _glossary-macos.md:64) point to the same URL https://business.apple.com which returned status='alive' / statusCode=200. Corroborating curl.exe -I HEAD probe returned HTTP/1.1 200 OK / Server: Apple / no redirect. Branch A taken per CONTEXT.md D-04 line 113-117 expected case.

Sidecar annotated with per-entry "last_revalidated": "2026-05-26" (ANNOTATE-not-remove per D-04). 4-entry shape preserved (V-66-02 shape stable per check-phase-66.mjs:85-112). NO corpus edits required per REQUIREMENTS.md:12 "Either confirmed-current (no edits required ...)".

Quarterly rotting-external-quarterly cron job continues monitoring at cron '0 8 1 1,4,7,10 *'; next fire 2026-07-01.

Pre-commit dry-run protocol passed: v1.6-milestone-audit.mjs + check-phase-62.mjs + check-phase-66.mjs all exit 0 modulo CHAIN_SKIP {48,51,58,60,61}.

Tool stdout + corroborating curl HEAD captured in .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-VERIFICATION.md SWEEP-01 H2 subsection (Plan 67-03 close-gate appends SC#1-4 checklist + validator exit table).

Rollback: git revert <THIS_SHA> cleanly restores pre-SWEEP-01 sidecar baseline (per-plan boundary per CONTEXT.md D-04 Option E).

SWEEP-01
EOF
)"
```

**Branch B commit** (touches sidecar + 67-VERIFICATION.md draft + N affected docs files + possibly _glossary-macos.md coordinating row):

```
git add scripts/validation/v1.6-audit-allowlist.json .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-VERIFICATION.md <list affected docs files explicitly>

git commit -m "$(cat <<'EOF'
docs(67-01): SWEEP-01 — surgical update <N> of 4 ABM URLs + Version History rows + sidecar refresh

Verified the 4 c13_rotting_external.ci_1_abm_urls entries against live Apple URL state on 2026-05-26 using markdown-link-check@3.14.2; <N> of 4 URLs returned non-alive status / Apple <redirect|sunset>. Branch B surgical update taken per CONTEXT.md D-04 line 118-127.

Affected file(s) per Wave 1 markdown-link-check output:
- <file:line> — <new URL> (was https://business.apple.com)
- ...

Each affected file:
- URL surgically updated on the affected line
- Tail-table Version History row appended (Phase 67 SWEEP-01 Branch B; PITFALLS.md CI-1 closure)
- Frontmatter last_verified bumped to 2026-05-26

_glossary-macos.md ## Version History H2 at line 121 gains one coordinating row at line 125 (newest-first; PITFALL-6 anchor at line 121 unshifted — intra-table insert).

Sidecar c13_rotting_external.ci_1_abm_urls: affected entries gained url/line/reason updates + "last_revalidated":"2026-05-26"; unaffected entries gained "last_revalidated":"2026-05-26" only. 4-entry shape preserved.

Pre-commit dry-run protocol passed: v1.6-milestone-audit.mjs + check-phase-62.mjs + check-phase-66.mjs all exit 0 modulo CHAIN_SKIP {48,51,58,60,61}.

Rollback: git revert <THIS_SHA> cleanly restores pre-SWEEP-01 baseline.

SWEEP-01
EOF
)"
```

3. Post-commit verify:

```
git log --name-only -1 HEAD
```

Branch A expected output: `scripts/validation/v1.6-audit-allowlist.json` + `.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-VERIFICATION.md` (exactly 2 files).

Branch B expected output: the above 2 files PLUS 1-4 affected docs files PLUS possibly `_glossary-macos.md` for the coordinating row.

```
node scripts/validation/v1.6-milestone-audit.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT harness exit $LASTEXITCODE" }
node scripts/validation/check-phase-62.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT check-phase-62 exit $LASTEXITCODE" }
node scripts/validation/check-phase-66.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT check-phase-66 exit $LASTEXITCODE" }
```

ALL 3 MUST exit 0 modulo CHAIN_SKIP. If anything regresses post-commit (despite green dry-run), investigate immediately — chain green-gate has regressed.

(`Grep` for `"last_revalidated": "2026-05-26"` in the sidecar should return exactly 4 matches — one per `ci_1_abm_urls` entry. If Branch B, additional non-alive URLs may also have updated `"reason"` text — verify diff size matches expectations.)
  </action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); const arr=j.c13_rotting_external.ci_1_abm_urls; console.log('entries:'+arr.length); console.log('annotated:'+arr.filter(e=>e.last_revalidated==='2026-05-26').length); if(arr.length!==4){process.exit(1)} if(arr.filter(e=>e.last_revalidated==='2026-05-26').length!==4){process.exit(2)}" ; if ($LASTEXITCODE -ne 0) { Write-Error "sidecar shape/annotation FAIL" } ; node scripts/validation/v1.6-milestone-audit.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "harness FAIL" } ; node scripts/validation/check-phase-62.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "check-62 FAIL" } ; node scripts/validation/check-phase-66.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "check-66 FAIL" } ; Test-Path .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-VERIFICATION.md</automated>
    <expected>sidecar entries=4 AND annotated=4 (all 4 carry last_revalidated 2026-05-26); harness + check-phase-62 + check-phase-66 all exit 0 (check-phase-66 may report 5 SKIPPED per CHAIN_SKIP); 67-VERIFICATION.md draft exists.</expected>
  </verify>
  <done>SWEEP-01 verification artifact (markdown-link-check stdout + corroborating curl HEAD) captured into 67-VERIFICATION.md SWEEP-01 H2 subsection; sidecar 4 `ci_1_abm_urls` entries annotated with `last_revalidated:2026-05-26` (4-entry shape preserved); Branch A: no corpus edits, single sidecar+verification commit; Branch B: surgical updates + tail-table rows + frontmatter bumps + glossary coordinating row + sidecar updates, single multi-file commit; pre-commit dry-run triple green (harness + check-phase-62 + check-phase-66 modulo CHAIN_SKIP); post-commit verify identical exit codes; single git revert restores pre-SWEEP-01 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev environment ↔ `https://business.apple.com` (Apple public endpoint) | One-shot HTTPS probe via markdown-link-check + curl; no credentials, no auth. Apple-controlled response is the verification subject (not trusted input). |
| `markdown-link-check@3.14.2` npm package ↔ local Node.js runtime | Pinned exact version matches `audit-harness-v1.6-integrity.yml:167` cron pin; package legitimacy audited at 67-RESEARCH.md "Package Legitimacy Audit"; no postinstall script per `npm view markdown-link-check@3.14.2 scripts.postinstall` (empty). |
| Working-tree sidecar edit ↔ JSON parser of check-phase-66.mjs V-66-02 | V-66-02 enforces object shape + `quarterly_audit.cadence` literal only; per-entry annotations are free-text and NOT shape-enforced per check-phase-66.mjs:85-112. |
| Pre-commit dry-run ↔ chain green-gate at HEAD | 3-validator triple (harness + check-phase-62 + check-phase-66) must exit 0 BEFORE git add; identical exit codes post-commit prove the dry-run was a faithful preview. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01-MLC | Tampering / Spoofing | markdown-link-check@3.14.2 package supply chain | mitigate | Pinned exact version `@3.14.2`; package legitimacy audited (npm registry verified + tcort/markdown-link-check upstream + empty postinstall per `npm view ...scripts.postinstall`); IS-equivalent pin to the production cron job that has been running this exact pin against the same Apple URL since Phase 66 commit `3a9a671` with no incidents. |
| T-67-01-RL | Information Disclosure (Pitfall 2 — false-positive from Apple rate-limiting) | markdown-link-check probe against `business.apple.com` | mitigate | (a) Probe ONCE (4 sidecar entries are same URL); (b) corroborating `curl.exe -I` HEAD probe with `--max-time 10` independently verifies; (c) Branch decision logic accepts "curl 200 OK + markdown-link-check 429/503" as Branch A with "tool divergence" notation in 67-VERIFICATION.md. |
| T-67-01-JS | Tampering (JSON syntax corruption from sidecar edit) | scripts/validation/v1.6-audit-allowlist.json | mitigate | (a) JSON parse guard runs immediately after every sidecar edit: `node -e "JSON.parse(...)"` must print OK; (b) `git diff` size check — Branch A expects ~4-6 lines changed, Branch B expects ~10-15 lines changed; entire-file diff means CRLF/LF drift and triggers revert+redo; (c) post-edit `check-phase-66.mjs` V-66-02 fails fast on parse errors with exact error message. |
| T-67-01-SC | Tampering | npm install (one new package install via `npm install --no-save markdown-link-check@3.14.2`) | mitigate | `--no-save` flag prevents `package.json` mutation (no top-level `package.json` exists at repo root per 67-RESEARCH.md A4); pin is IS-equivalent to the production cron; package legitimacy audited and APPROVED in 67-RESEARCH.md "Package Legitimacy Audit". Per CONTEXT.md D-04: no [ASSUMED]/[SUS] packages, no blocking checkpoint required. |
| T-67-01-AR | Tampering (anti-regression — accidental hub edit) | Phase 67 must NOT touch `docs/common-issues.md`, `docs/quick-ref-l1.md`, `docs/quick-ref-l2.md`, `docs/operations/00-index.md`, `docs/index.md`, capability matrices, or older workflow files | mitigate | `<files>` block explicitly enumerates the 6 candidate files (only the sidecar + 67-VERIFICATION.md guaranteed; 4 docs files conditional on Branch B); STATE.md:122-130 anti-regression invariants explicitly cited; commit hook `git log --name-only -1 HEAD` post-verify confirms file set. |
</threat_model>

<verification>
**Plan 67-01 → SWEEP-01 verification targets:**

| Target | Command |
|--------|---------|
| Sidecar 4-entry shape preserved | `node -e "const j=JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); console.log(j.c13_rotting_external.ci_1_abm_urls.length)"` → expect `4` |
| All 4 entries annotated | `(Select-String -Path scripts/validation/v1.6-audit-allowlist.json -Pattern '"last_revalidated":\s*"2026-05-26"' \| Measure-Object).Count` → expect `>= 4` |
| V-66-02 sidecar shape stable | `node scripts/validation/check-phase-66.mjs --verbose 2>&1 \| Select-String -Pattern 'V-66-02' \| Select-String -Pattern 'PASS'` → expect 1 match |
| Harness inertia confirmed | `node scripts/validation/v1.6-milestone-audit.mjs ; $LASTEXITCODE` → expect `0` |
| V-62-SIDECAR not broken by c13 annotation | `node scripts/validation/check-phase-62.mjs ; $LASTEXITCODE` → expect `0` |
| 67-VERIFICATION.md draft exists with SWEEP-01 subsection | `Test-Path .planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-VERIFICATION.md` → `True`; `(Select-String -Path ... -Pattern 'SWEEP-01: ABM URL Live-State Verification \(2026-05-26\)' \| Measure-Object).Count` → expect `>= 1` |
| Atomic commit boundary | `git log --name-only -1 HEAD` → Branch A: exactly 2 files (sidecar + 67-VERIFICATION.md); Branch B: 2 + N affected docs files (1 to 5) |
</verification>

<success_criteria>
1. `markdown-link-check@3.14.2` was installed one-shot (`npm install --no-save`); `npm view markdown-link-check@3.14.2 version` returned `3.14.2`.
2. The probe `node -e "require('markdown-link-check')('[ABM](https://business.apple.com)', { timeout: '10s' }, ...)"` returned JSON results captured into `_tmp-mlc-output.txt` (kept as transient artifact OR may be cleaned up post-commit per Claude's Discretion).
3. The corroborating `curl.exe -I -L --max-time 10 https://business.apple.com` HEAD probe was captured into `_tmp-curl-head.txt`.
4. `.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-VERIFICATION.md` exists and contains the `## SWEEP-01: ABM URL Live-State Verification (2026-05-26)` H2 subsection with 4 H3 sub-blocks (Mechanism / Tool Output / Corroborating Evidence / Outcome).
5. Each of the 4 `c13_rotting_external.ci_1_abm_urls` entries in `scripts/validation/v1.6-audit-allowlist.json` carries the field `"last_revalidated": "2026-05-26"`; 4-entry array shape preserved.
6. `node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); console.log('OK')"` prints `OK` (no JSON syntax error).
7. Branch A: ONE git commit at HEAD touches exactly 2 files (sidecar + 67-VERIFICATION.md). Branch B: ONE git commit touches 2 + N affected docs files (1 to 5).
8. Pre-commit dry-run triple (v1.6-milestone-audit.mjs + check-phase-62.mjs + check-phase-66.mjs) all exit 0 modulo CHAIN_SKIP {48,51,58,60,61}; identical exit codes post-commit.
9. Single `git revert <Plan 67-01 SHA>` cleanly restores pre-SWEEP-01 baseline byte-identically (per-plan boundary per CONTEXT.md D-04 Option E; SWEEP-01 and SWEEP-02 are logically independent).
10. NO hub files (`docs/common-issues.md`, `docs/quick-ref-l1.md`, `docs/quick-ref-l2.md`, `docs/operations/00-index.md`, `docs/index.md`) touched. NO capability matrices touched. NO older workflow files touched. (Anti-regression invariants per STATE.md:122-130 + REQUIREMENTS.md:64.)
</success_criteria>

<output>
Create `.planning/phases/67-corpus-surgical-sweeps-pillar-a-low-risk-warm-up/67-01-SUMMARY.md` with:
- Branch taken (A or B) + Wave 1 decision rationale (markdown-link-check exit code + curl HEAD result)
- Commit SHA + `git log --name-only -1 HEAD` output verbatim
- Sidecar diff summary: 4 entries annotated; entry count preserved at 4
- Pre-commit dry-run + post-commit verification exit codes (3 validators)
- Pointer to draft 67-VERIFICATION.md SWEEP-01 H2 subsection (Plan 67-03 close-gate finalizes the rest)
- Wave 2 handoff: Plan 67-02 SWEEP-02 ready to execute (no inter-plan state carry beyond the sidecar — Plan 67-02 operates on `ci_2_vpp_location_token` block independently)
</output>
</content>
