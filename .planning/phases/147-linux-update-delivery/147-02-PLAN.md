---
phase: 147-linux-update-delivery
plan: 02
type: execute
wave: 2
depends_on: [147-01]
files_modified:
  - docs/operations/patch-management/00-overview.md
  - .planning/REQUIREMENTS.md
  - .planning/research/PITFALLS.md
autonomous: true
requirements: [LNX-01]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/operations/patch-management/00-overview.md carries a fifth Linux column in its Cross-Platform Comparison table, appended last, with every table row still on one physical line (D-33, D-34, D-35)."
    - "The overview's prose reads as five-platform everywhere: all seven `four` tokens, the column-order sentence, the applicability blockquote enumeration, the H1 intro enumeration, the Ring Terminology 'other three platforms' sentence, and the platform enumerations inside the Cross-Platform Planning Considerations bullets (D-37, D-38)."
    - "The overview's frontmatter `platform:` value is still exactly `cross-platform` — Linux is never appended to it (D-40, ROADMAP.md:126)."
    - "The overview routes a Linux reader to the new guide from four places: the platform-applicability blockquote, the routing list, Related Resources, and the Attestation bullet's Examples clause (D-39)."
    - "The Phase-146 D-73 defect at the overview's driver/firmware bullet is corrected with a Microsoft Learn source: the 0-30 day deferral belongs to the driver update policy's own setting, not to a WUfB deployment ring (D-31)."
    - "Three requirement-text corrections carry `**[OWNER-RULED IN SCOPE 2026-08-20]**` markers in `.planning/REQUIREMENTS.md`, following `REQUIREMENTS.md:46` (DRV-06) as the repo's existing instrument for exactly this (D-04, D-30, D-31, D-32)."
    - "`.planning/research/PITFALLS.md`'s misattributed Canonical citation is corrected in-phase, so the misattribution stops propagating to every future phase that reads the artifact (D-32)."
    - "node scripts/validation/check-phase-54.mjs reports 32 passed, 0 failed, 0 skipped after every overview edit — V-54-08, V-54-26, V-54-29, V-54-30 and V-54-31 all still green (D-34, D-36, D-40)."
    - "node scripts/validation/check-phase-144.mjs reports 101 PASS, 0 FAIL, 0 SKIPPED after commit 2 — the post-phase half of D-59's apex pair."
    - "Every one of the eleven `00-overview.md` string replacements is confirmed to have matched; the file is CRLF in the worktree while its structural analog is LF, and a zero-match is silent (D-63)."
    - statement: "Each inserted literal appears in `00-overview.md` exactly once — no Linux column, routing bullet, Related Resources entry or blockquote line is double-inserted by a re-run edit (edge probe LNX-01/idempotency)."
      verification: backstop
  prohibitions:
    - requirement_id: LNX-01
      category: transparency
      statement: "MUST NOT let the overview's Linux cells imply Intune orchestrates, schedules, defers or enforces Linux updates; every absent capability is written as an explicit parenthetical absence, never left blank or softened."
    - requirement_id: LNX-01
      category: values
      statement: "MUST NOT degrade, truncate or drop any of the four existing platforms' cells, routing sentences or capability statements to make room for Linux; the five-platform rewrite is additive for every existing reader."
  artifacts:
    - docs/operations/patch-management/00-overview.md
    - .planning/REQUIREMENTS.md
    - .planning/research/PITFALLS.md
  key_links:
    - "00-overview.md -> 05-linux-update-delivery.md, in four places; the target must already exist from plan 01 or check-nav-hub-links reports 'target file not found'."
    - "The overview's Cross-Platform Comparison header row — V-54-08 matches it with [^\\n]* gaps and V-54-29 strips it per physical line, so the row is safe only while it stays unwrapped."
    - ".planning/REQUIREMENTS.md is live-read by check-phase-54's V-54-21 negative and is coupled into V-54-32's atomicity check."
---

<objective>
Make `docs/operations/patch-management/00-overview.md` genuinely five-platform, land the three
owner-ruled requirement-text corrections, correct the misattributed Canonical citation in the pitfalls
research artifact, and close the phase with the post-phase apex run. This is commit 2 of D-59's
two-commit sequence.

Purpose: the phase Goal — *the corpus becomes genuinely five-platform for updates*. Plan 01 authored
the guide; without this plan the guide is unreachable from the hub and the overview still tells every
reader there are four platforms.

Output: three modified files, one commit. No new file, no registry row, no filename-map row, no canary
bump, no ops-index row, no nav-hub wiring (D-64), and no edit to any of the four sibling routing
blockquotes (D-49).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-linux-update-delivery/147-CONTEXT.md
@.planning/phases/147-linux-update-delivery/147-RESEARCH.md
@.planning/phases/147-linux-update-delivery/147-PATTERNS.md
@.planning/phases/147-linux-update-delivery/147-01-SUMMARY.md
</context>

<phase_wide_rules>

**R1 — Baselines at HEAD `ee300b50`, re-measured at plan time.** `check-phase-54` **32/0/0** ·
`check-phase-50` **26/0/0** · `check-nav-hub-links` **0/0** · `c17-eee-contract` **234 files / 0
violations** · `v1.20-milestone-audit` **16/0** · `build-filename-map --self-test` **8/0** ·
`build-publish-bundle --self-test` **15/0** · apex `check-phase-144` **101/0/0**. Any deviation is a
regression, never a new baseline.

**R2 — The CRLF trap, inverted polarity.** `git ls-files --eol docs/operations/patch-management/`
reports index LF throughout, worktree **CRLF** for `00-overview.md` through
`04-android-patch-delivery.md`, and worktree **LF** for `06-windows-driver-firmware-updates.md`. The
edit target is the CRLF one. A literal string-replace authored against an LF mental model **silently
no-ops** against CRLF content: the tool reports success, `git diff` shows nothing, and the failure is
invisible until a validator or a reader finds it. **After every single replacement, confirm it
matched** — re-read the changed region or check `git diff --stat` — and never batch replacements
without checking each. This is the same trap class Phase 146 recorded with the opposite file as the
outlier.

**R3 — Every table row stays on one physical line.** `V-54-29`'s strip is `/^\|.*\|.*$/gm`, applied
per **physical** line, and `V-54-08` matches the header with `[^\n]*` gaps. `00-overview.md` wraps
prose at about 95 columns, and a sixth column pushes the Enforcement primitive row past **380
characters** — so re-wrapping is the natural author move and the failure is **silent until the
validator runs**. Measured probes: re-wrapping the Hotpatch/EOL row fails `V-54-29` (31/1);
re-wrapping the header row fails `V-54-08` (31/1). Do not wrap.

**R4 — Never touch the applicability blockquote's lead-in.** `V-54-26` requires the literal
`> **Platform applicability:**` inside the first 50 body lines of every `PATCH_FILES` entry, and
`00-overview.md` is one of the five. A measured probe rewording the lead-in to add a parenthetical
failed `V-54-26` (31/1). **Add a Linux line; never edit the lead-in substring.** The prose that
follows the lead-in on the same physical line may change.

**R5 — Never advance one date stamp without the other.** `check-phase-54.mjs` tests only
`if (days > 60)`, so advancing `last_verified` alone drives the interval negative and **silently
passes** while leaving the file wrong. Both stamps move together or neither does.

**R6 — Banned strings introduced by this plan's edits.** `V-54-30` is a live negative on `TBD`,
`TODO`, `FIXME`, `XXX` and `PLACEHOLDER` across the five `PATCH_FILES`, and `00-overview.md` is one
of them. `V-54-29` is a live negative on `Hotpatch`, `VBS` and `MEETS_STRONG_INTEGRITY` in the
overview's **body prose** — it strips table rows, fenced code, `[text](url)` link syntax and inline
code before testing, so those three tokens remain legal inside a table cell and illegal in a
sentence. `V-54-21` is a live negative on the literal `05-compliance-policy.md` across
`REQUIREMENTS.md` **and** `ROADMAP.md`, coupled into `V-54-32`'s atomicity check; the file this phase
creates is `05-linux-update-delivery.md` and the Linux compliance file is `03-compliance-policy.md`,
so the banned literal has no legitimate reason to be typed — but a task editing `REQUIREMENTS.md`
about a Linux compliance policy is exactly where it would slip in. Also do not write `SCCM` or
`System Center` in the overview's new prose; write `Configuration Manager`.
<!-- planner-discipline-allow: TBD, TODO, FIXME, XXX, PLACEHOLDER, Hotpatch, VBS, MEETS_STRONG_INTEGRITY, 05-compliance-policy.md, SCCM, System Center -->

**R7 — Coordinates are pointers, not identities.** Every line number below was re-measured at plan
time against HEAD `ee300b50` and matched CONTEXT's map 7-for-7 with zero drift. They will still shift
the moment an earlier edit in this same plan adds or removes a line. **Locate each site by its quoted
text, then edit.** Phase 146's coordinates went stale inside Phase 146 itself, shifted by two lines by
a commit two later.

**R8 — Nothing outside the three named files is touched.** No new file, no registry row, no
filename-map row, no canary bump, no ops-index row, no nav-hub wiring, no glossary edit, no edit to
`docs/reference/`, `docs/admin-setup-linux/`, `06-windows-driver-firmware-updates.md`, or the four
sibling routing blockquotes in `01` through `04`.

**The eleven `00-overview.md` edit sites, measured at plan time.** Six are D-39's routing and content
sites; the remaining five are the D-37/D-38 prose sweep. Three of the largest four-platform
assertions carry no `four` token at all, which is why the `four` grep alone is an incomplete map.

| # | Locate by | Line at plan time | Kind |
|---|---|---|---|
| S1 | `> Windows, macOS, iOS/iPadOS, and Android — covering concept terminology` | 10 | prose sweep, inside the applicability blockquote |
| S2 | `> **Android:** See [Android Patch Delivery](04-android-patch-delivery.md)` (append after) | 19 | D-39 site 1 — the Linux blockquote line |
| S3 | `This guide is the cross-platform overview for patch and update management across Windows, macOS,` / `iOS/iPadOS, and Android.` | 23-24 | prose sweep, H1 intro |
| S4 | `across all four platforms. Use` and `live in the four sibling guides.` | 39-40 | prose sweep |
| S5 | `The columns are ordered Windows, macOS, iOS/iPadOS, Android to match the navigation order used by` / `the four sibling per-platform files (file numbers 01 through 04).` | 45-46 | prose sweep, column-order sentence |
| S6 | the header row `\| Concept \| Windows \| macOS \| iOS/iPadOS \| Android \|`, its delimiter row, and all five body rows | 49-55 | D-39 site 2 — the Linux column |
| S7 | `all four platforms; both are detailed` | 59 | prose sweep |
| S8 | `three platforms (macOS, iOS, Android) do not use ring terminology` | 100 | prose sweep — carries no `four` token |
| S9 | `set per deployment ring, and the quality-update deadline and grace period do apply to drivers.` | 88 | D-39 site 6 — the D-73 correction |
| S10 | `encryption and OS version checks. Attestation does not install updates` (the Attestation bullet's `Examples:` list, immediately before) | 130-132 | D-39 site 5 |
| S11 | `[Android Patch Delivery](04-android-patch-delivery.md).` in the routing list; `applicable), and Related Resources / External References footer for that platform. The four guides`; `across all four platforms and benefit from being addressed`; `- **Compliance signal harmonization** — The four platforms expose different compliance signals`; and the platform enumerations inside the Cadence alignment, Communications cadence and Reporting parity bullets | 165-166, 168, 175, 184, 177-207 | D-39 sites 3 and 4 plus the remaining prose sweep |

</phase_wide_rules>

<tasks>

<task type="auto">
  <name>Task 1: The Linux column and the five-platform prose sweep — the validator-hazard cluster</name>

  <files>docs/operations/patch-management/00-overview.md</files>

  <read_first>
    - `docs/operations/patch-management/00-overview.md:1-62` — the frontmatter, the applicability
      blockquote, the H1 and its intro, and the Cross-Platform Comparison section including the table
      and both prose paragraphs that surround it. Read the current bytes rather than trusting the
      coordinate table in R7.
    - `docs/operations/patch-management/00-overview.md:96-101` and `:168-207` — the Ring Terminology
      sentence that says "three platforms", and the Cross-Platform Planning Considerations bullets
      whose platform enumerations carry no `four` token.
    - `docs/reference/4-platform-capability-comparison.md` — the corpus's own 4-to-5 transition
      precedent: filename kept, H1 retitled, Linux appended last (D-38). Read for the pattern; this
      file is **not** edited here.
    - `scripts/validation/check-phase-54.mjs:127-143` (`V-54-08`'s regex and its four acceptance
      paths), `:419-435` (`V-54-26`'s literal), `:494-512` (`V-54-29`'s per-physical-line strip and
      its banned set), `:533-548` (`V-54-31`'s unanchored frontmatter regex).
    - `147-PATTERNS.md` `### docs/operations/patch-management/00-overview.md (six edit sites)` — the
      absent-capability parenthetical convention and the Site 2 table shape.
    - `147-CONTEXT.md` D-33, D-34, D-35, D-36, D-37, D-38, D-40, D-66.
  </read_first>

  <behavior>
    The three highest-risk edits in this plan, done together because they share one validator surface.
    - Appending a fifth column keeps `V-54-08` green because its regex uses `[^\n]*` gaps and the four
      existing literals stay in order; five live probes — Linux appended last, before Android, first,
      after Windows, and between macOS and iOS — all returned 32/0/0. Order is an editorial choice,
      not a validator constraint.
    - The same append breaks `V-54-29` the instant a row is wrapped, because that strip is
      per-physical-line. This is the failure that is invisible on inspection.
    - The prose sweep has to reach five sites the `four` grep does not find.
  </behavior>

  <action>
**Site S6 — the Linux column.** Append `Linux` as the **last** column of the Cross-Platform
Comparison table: the header row, the delimiter row and all five body rows. Appending last is chosen
on editorial grounds and because `docs/reference/4-platform-capability-comparison.md` does exactly
that; per D-33 it is **not** a validator constraint, and the earlier claim that `V-54-08` is
order-sensitive was measured false against five live probes. What `V-54-08` does require is that the
four existing literals stay in their existing order and that the header stays on one physical line.

The five Linux cells (D-35), using the table's own parenthetical convention for absent capabilities —
the Android Deferral cell already reads `(No tenant-side deferral; Google Play handles)`:

| Row | Linux cell |
|---|---|
| Cadence model | distro-native `apt` + `unattended-upgrades`, no Intune orchestration |
| Deferral mechanism | `(No Intune-side deferral; distro-native only)` |
| Enforcement primitive | `(None — compliance policy + web-app CA is attestation only)` |
| Hotpatch / EOL surface | Livepatch via Ubuntu Pro (Canonical-side entitlement) |
| Hard deadline | `(no hard deadline)` |

Use the U+2014 em dash in the Enforcement cell, matching the corpus's existing usage. `V-54-29` strips
table rows before testing its banned set, and `Livepatch` neither is nor contains the banned token, so
that cell is safe **inside the table** — it must not migrate into a sentence. The same parenthetical
form would be fatal in `docs/reference/linux-capability-matrix.md`, whose `VALID_STATUS` is a prefix
match; that file is not edited (D-41).

**R3 is the whole risk here.** After adding the column, the Enforcement primitive row passes 380
characters. Leave it on one physical line. Do not reflow, do not wrap, do not let an editor soft-wrap
become a hard wrap. Verify with a per-line length check before running any validator.

**The prose sweep — eleven-site map in R7, five of which carry no `four` token.**

- S1: the applicability blockquote's enumeration becomes five-platform. **Do not touch the
  `> **Platform applicability:**` lead-in substring** (R4) — only the prose after it.
- S3: the H1 intro paragraph's enumeration becomes five-platform.
- S4: `across all four platforms` and `the four sibling guides` become five.
- S5: the column-order sentence gains Linux in last position and its parenthetical
  `(file numbers 01 through 04)` becomes `01 through 05`. Note for the record that
  `06-windows-driver-firmware-updates.md` sits outside that range by design and is not a per-platform
  guide; if a later phase adds another non-platform sibling the parenthetical needs rethinking rather
  than incrementing.
- S7: `all four platforms` becomes five. The sentence is about the two hard cutovers, and Linux adds
  none — keep the count of hard cutovers at two while the platform count becomes five.
- S8: `The other three platforms (macOS, iOS, Android) do not use ring terminology` becomes four
  platforms including Linux. This site carries **no** `four` token and a `four` grep misses it
  entirely. `V-54-09` requires the Ring Terminology H2 plus `WUfB deployment ring` plus
  `Autopatch ring` within roughly ten lines and has about 1,380 bytes of slack in its 1,500-byte
  window, so this edit cannot reach it.
- S11's sweep half: `The four guides` becomes five; `across all four platforms` becomes five;
  `The four platforms expose different compliance signals` becomes five, and that bullet's
  parenthesised per-platform list gains a Linux clause. Add a Linux clause to the Cadence alignment,
  Communications cadence and Reporting parity bullets' enumerations too — per D-38 these bullets are
  among the three largest four-platform assertions in the file and none of them carries a `four`
  token.

**The frontmatter `platform:` value stays exactly `cross-platform`** (D-40). Linux is never appended
to it — that is locked by `ROADMAP.md:126` rather than being a decision. A value like
`cross-platform, Linux` would pass `V-54-31`'s unanchored regex and fail `V-54-07`'s anchored one,
producing a split-failure shape that is confusing to diagnose.

Per R2, confirm every one of these replacements actually matched. Do not proceed to Task 2 with an
unverified edit.
  </action>

  <acceptance_criteria>
    - `node scripts/validation/check-phase-54.mjs` prints `Summary: 32 passed, 0 failed, 0 skipped`
      and exits 0 — in particular `V-54-08`, `V-54-26`, `V-54-29`, `V-54-30` and `V-54-31` all PASS
      (D-34, D-36, D-40).
    - `awk 'length > 300 && /^\|/ {n++} END {print n+0}' docs/operations/patch-management/00-overview.md`
      returns at least `1` and every such line is a single unwrapped table row — verified by
      `grep -c '^|' docs/operations/patch-management/00-overview.md` returning exactly `7` (one
      header, one delimiter, five body rows), the same count as before the edit (R3, D-34).
    - `grep -cE '^\| Concept \|.*\| Windows \|.*\| macOS \|.*\| iOS/iPadOS \|.*\| Android \|.*\| Linux \|' docs/operations/patch-management/00-overview.md`
      returns `1` — the header row carries all five platforms, in order, on one physical line (D-33).
    - `grep -c 'No Intune-side deferral; distro-native only' docs/operations/patch-management/00-overview.md`
      returns `1`; `grep -c 'compliance policy + web-app CA is attestation only' ...` returns `1`;
      `grep -c 'Livepatch via Ubuntu Pro' ...` returns `1` (D-35).
    - `grep -c '\bfour\b' docs/operations/patch-management/00-overview.md` returns `0` — all seven
      tokens swept (D-37).
    - `grep -c 'three platforms (macOS, iOS, Android)' docs/operations/patch-management/00-overview.md`
      returns `0` — the site that carries no `four` token was found and swept (D-38).
    - `grep -c 'file numbers 01 through 04' docs/operations/patch-management/00-overview.md` returns
      `0` and `grep -c 'file numbers 01 through 05' ...` returns `1`.
    - `grep -c 'and Android — covering concept terminology' docs/operations/patch-management/00-overview.md`
      returns `0` — the blockquote enumeration was swept — while
      `grep -c '> \*\*Platform applicability:\*\*' ...` returns `1`, byte-identical to before the
      edit (R4, D-36).
    - `grep -c '^platform: cross-platform$' docs/operations/patch-management/00-overview.md` returns
      `1` and `grep -c '^platform: cross-platform, ' ...` returns `0` (D-40).
    - `grep -cE '\bTBD\b|\bTODO\b|\bFIXME\b|\bXXX\b|\bPLACEHOLDER\b' docs/operations/patch-management/00-overview.md`
      returns `0` (R6, `V-54-30`).
    - `git diff --stat docs/operations/patch-management/00-overview.md` shows a non-zero change count
      — proof that no replacement silently no-opped against CRLF content (R2, D-63).
    - `git status --porcelain` lists only `docs/operations/patch-management/00-overview.md` as
      modified (R8).
  </acceptance_criteria>

  <verify>
    <automated>node scripts/validation/check-phase-54.mjs &amp;&amp; node scripts/validation/v1.20-milestone-audit.mjs &amp;&amp; node scripts/validation/c17-eee-contract.mjs</automated>
  </verify>

  <done>
    The Cross-Platform Comparison table has five columns with Linux last, all seven rows on one
    physical line each, and the five D-35 cells verbatim. The overview's prose reads five-platform at
    all eleven swept sites, including the three that carry no `four` token. The frontmatter
    `platform:` value is untouched. `check-phase-54` is 32/0/0, the audit 16/0 and C17 234/0. Nothing
    is committed yet.
  </done>

  <reversibility rating="reversible">
    Every edit is a text replacement inside one tracked file and is revertible with `git checkout --`.
    The caveat D-34 records is that the `V-54-29` and `V-54-08` failure modes are invisible on
    inspection, so it is reversible **only if caught** — which is what the per-line length and header
    regex criteria are for.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: The four Linux routing sites, the D-73 correction, and the conditional date re-stamp</name>

  <files>docs/operations/patch-management/00-overview.md</files>

  <read_first>
    - `docs/operations/patch-management/00-overview.md` — the current state after Task 1. Every line
      number in R7 has shifted by whatever Task 1 added; locate each site by its quoted text (R7).
    - `docs/operations/patch-management/00-overview.md:8-19` — the platform-applicability blockquote's
      four per-platform bullet lines, whose shape the Linux line must match.
    - `docs/operations/patch-management/00-overview.md:151-170` — the routing list's per-platform
      sentence shape and the paragraph that follows it.
    - `docs/operations/patch-management/00-overview.md:213-226` — the Related Resources list, ordered
      `01, 06, 02, 03, 04`. The order is **non-numeric**, so the insertion point for the new entry is
      a judgement call rather than an obvious append (D-39).
    - `docs/operations/patch-management/00-overview.md:127-133` — the Attestation bullet and its
      `Examples:` list, which currently names Android Play Integrity verdicts and iOS device-level
      checks.
    - `docs/operations/patch-management/00-overview.md:84-92` — the driver/firmware bullet carrying
      the D-73 defect, and the two `**Source:**` lines that follow it.
    - `docs/operations/patch-management/06-windows-driver-firmware-updates.md:292-340` — the already
      correct treatment of the same fact: the WUfB deployment ring quality-update deferral does not
      reach drivers, the driver policy has its own deferral instead, and the driver policy's own
      deferral is 0 to 30 days via the `Make updates available after (days)` setting.
    - `docs/operations/patch-management/05-linux-update-delivery.md` — the file plan 01 created, so
      the four new links point at real content and the summary clauses describe what is actually
      there.
    - `147-CONTEXT.md` D-31, D-39, D-49, D-61, D-63, D-73.
  </read_first>

  <behavior>
    Five content edits plus one conditional metadata edit. Four of them create the inbound links that
    make the new guide reachable — this is why plan 02 depends on plan 01: the link checker resolves
    target existence with no allowlist, ratchet or expected-failure list, and a link to a file that
    does not exist is an immediate `target file not found`.
    - The Linux reader is routed from four places.
    - The Phase-146 D-73 defect is corrected with a source, not with a drive-by edit.
    - The date stamps move together or not at all.
  </behavior>

  <action>
**Site S2 — the platform-applicability blockquote's Linux line (D-39 site 1).** Append a `**Linux:**`
bullet line after the `**Android:**` line, in the same `> **Platform:** See [Title](file.md) for ...`
bold-label shape the four existing lines use, routing to `05-linux-update-delivery.md`. Summarize what
the guide actually covers: no native Intune update policy, Bash platform-script delivery, the
root-context hazard, and `unattended-upgrades`. Do not touch the lead-in line (R4).

**Site S11's routing half — the routing bullet (D-39 site 3).** Add a Linux sentence to
`## Routing to Per-Platform Guides`, matching the existing per-platform shape: for Linux tenants
planning apt-family update delivery on Intune-managed Ubuntu, start with the new guide. Place it after
the Android sentence so the list reads in the same order as the table's columns.

**Site S11's Related Resources half (D-39 site 4).** Add a `- [Linux Update Delivery](05-linux-update-delivery.md) — <summary clause>`
entry in the same shape as every existing entry. The list's existing order is `01, 06, 02, 03, 04` —
**non-numeric** — so the insertion point is Claude's discretion (D-39). Choose one and state the
reasoning in the SUMMARY rather than leaving it unexplained.

**Site S10 — the Attestation bullet's `Examples:` clause (D-39 site 5).** Append a Linux clause to
the existing `Examples:` list in the `## Deferral vs Enforcement` section's third bullet. Per D-30 the
Linux lever is **web-app** Conditional Access — it gates web access to Entra-protected applications
through Microsoft Edge for Linux, and device-level Conditional Access grants are not available for
Linux at all. Keep it to one clause in the existing list; the full treatment lives in the new guide.

**Site S9 — the D-73 correction (D-39 site 6).** This is the Phase-146 owner-ruled deferral into this
phase; no other phase owns it. The defect is live: the driver/firmware bullet currently reads that
`the 0–30 day deferral are set per deployment ring`, which attributes the driver policy's own setting
to a WUfB deployment ring. It also carries a grammatical error in the same clause.

Correct it to state what `06-windows-driver-firmware-updates.md` already states correctly: the
quality-update deferral set in the WUfB deployment ring policy **does not** reach drivers approved by
the driver update policy; the driver policy has **its own** 0-to-30-day deferral, configured by its
`Make updates available after (days)` setting and counted from the day the update entered the policy;
and the quality-update deadline and grace period **do** apply to drivers. Keep the existing sentence's
other two facts intact — approval mode is fixed at creation, and the cross-link to the driver guide.

This earns a **sourced** correction rather than a drive-by edit (D-31). Add or extend a `**Source:**`
line citing
`https://learn.microsoft.com/en-us/intune/device-updates/windows/configure-driver-update-policy` with
`(updated 2026-04-24)`. That page is already the in-repo citation for the same fact at
`06-windows-driver-firmware-updates.md`, so it does not need re-fetching; RESEARCH source #8 records
it as discharging D-06(d). Follow R7 from plan 01: one `**Source:**` line covers one page's claim, on
its own paragraph.

**The conditional date re-stamp (D-61, D-72).** `00-overview.md` currently carries
`last_verified: 2026-08-20` and `review_by: 2026-10-19` — an interval of exactly 60, which is the
ceiling of `V-54-07`'s `> 60` test with zero slack. All six files in the directory sit at exactly 60;
`02`, `03` and `04` carry `2026-08-19 / 2026-10-18` and are not edited by this phase.

If the execution date of commit 1 differs from `2026-08-20`, re-stamp `last_verified` to that date and
recompute `review_by` as `last_verified + 60` by arithmetic, in this same commit. If it does not
differ, **this instruction is a deliberate no-op** — do not manufacture a diff. Per R5, never advance
one stamp without the other: `check-phase-54.mjs` tests only the upper bound, so advancing
`last_verified` alone drives the interval negative and silently passes. Worked example: a commit-1
date of `2026-08-21` gives `last_verified: 2026-08-21` and `review_by: 2026-10-20`.

Per R2, confirm every replacement matched.

Record in the SUMMARY, not in the file, that the four sibling routing blockquotes in `01`, `02`, `03`
and `04` each enumerate the other three platform siblings and will now silently omit Linux (D-49).
`06` carries no platform enumeration and is unaffected. This is a **knowingly accepted
inconsistency**, not a new defect — the roadmap's blast radius locks edits to `00-overview.md` only —
and recording it is what stops the verifier and code review surfacing it as a regression. Also record
the three Phase-151 hand-offs the new guide carries (D-73).
  </action>

  <acceptance_criteria>
    - `grep -c '05-linux-update-delivery.md' docs/operations/patch-management/00-overview.md` returns
      exactly `3` — one blockquote line, one routing sentence, one Related Resources entry — and
      `grep -n '05-linux-update-delivery.md' ...` shows them in three distinct sections (D-39, edge
      probe LNX-01/idempotency: exactly three, never six).
    - `grep -cE '^> \*\*Linux:\*\* See' docs/operations/patch-management/00-overview.md` returns `1`
      (D-39 site 1).
    - `grep -c 'Microsoft Edge' docs/operations/patch-management/00-overview.md` returns at least `1`,
      inside the Attestation bullet's `Examples:` list, and the clause scopes the lever to web access
      to Entra-protected applications rather than to device access (D-30, D-39 site 5).
    - `grep -c 'the 0–30 day deferral are' docs/operations/patch-management/00-overview.md` returns
      `0` — the defective clause is gone (D-31).
    - `grep -c 'Make updates available after' docs/operations/patch-management/00-overview.md`
      returns at least `1` — the correction names the driver policy's own setting (D-31).
    - `grep -c 'configure-driver-update-policy' docs/operations/patch-management/00-overview.md`
      returns at least `1` — the correction is sourced rather than asserted (D-31).
    - `grep -cE '^last_verified: [0-9]{4}-[0-9]{2}-[0-9]{2}$' docs/operations/patch-management/00-overview.md`
      returns `1` and `grep -cE '^review_by: [0-9]{4}-[0-9]{2}-[0-9]{2}$' ...` returns `1`, and the
      difference between the two dates is exactly 60 days by arithmetic (R5, D-61).
    - `node scripts/validation/check-nav-hub-links.mjs` prints
      `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total` — every new link resolves,
      including the target file plan 01 created (D-48).
    - `node scripts/validation/check-phase-54.mjs` prints `Summary: 32 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` prints `Summary: 16 passed, 0 failed`.
    - `git diff --stat docs/operations/patch-management/00-overview.md` shows a change count strictly
      greater than Task 1's, proving none of this task's replacements silently no-opped (R2, D-63).
    - `git status --porcelain` lists only `docs/operations/patch-management/00-overview.md` (R8).
  </acceptance_criteria>

  <verify>
    <automated>node scripts/validation/check-nav-hub-links.mjs &amp;&amp; node scripts/validation/check-phase-54.mjs &amp;&amp; node scripts/validation/v1.20-milestone-audit.mjs</automated>
  </verify>

  <done>
    All six D-39 edit sites are landed. A Linux reader is routed from the applicability blockquote, the
    routing list, Related Resources and the Attestation examples. The Phase-146 D-73 defect is
    corrected with a Microsoft Learn source. The date stamps are either both advanced with a 60-day
    interval or both untouched. `check-nav-hub-links` is 0/0 with three new inbound links to the new
    guide, and `check-phase-54` is 32/0/0. Nothing is committed yet.
  </done>
</task>

<task type="auto">
  <name>Task 3: The three owner-ruled requirement-text corrections, the pitfalls citation fix, commit 2, and the post-phase apex</name>

  <files>.planning/REQUIREMENTS.md, .planning/research/PITFALLS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md:46` — the DRV-06 entry, which is the repo's existing instrument for
      exactly this: an `**[OWNER-RULED IN SCOPE 2026-08-19]**` marker appended inline to the
      requirement bullet, followed by the correction sentence and what is withdrawn (D-04).
    - `.planning/REQUIREMENTS.md:42` (DRV-02), `:51` (LNX-01) and `:53` (LNX-03) — the three bullets
      the markers attach to.
    - `.planning/REQUIREMENTS.md:56` — the existing scoping note that the five-minute cap does not
      belong to the platform-script surface, which stays as is.
    - `.planning/research/PITFALLS.md:541` — the sentence crediting the Canonical automatic-updates
      page for `update-notifier-common`, and its two named sources.
    - `scripts/validation/check-phase-54.mjs:345-359` — `V-54-21`, a live negative on an off-by-one
      filename literal (named in R6) across `REQUIREMENTS.md` and `ROADMAP.md`, coupled into
      `V-54-32`'s atomicity check. This is the one live pin on the file this task edits, and CONTEXT
      never named it.
    - `147-CONTEXT.md` D-04, D-30, D-31, D-32, D-42, D-59, D-60, D-64, D-65, D-67, D-68.
  </read_first>

  <behavior>
    The last three edits and the close. The requirement-text corrections are owner-ruled to land as
    formal markers in the requirements file rather than as CONTEXT notes, because a CONTEXT note dies
    with the phase and a requirements marker is read by every future phase.
    - Three `**[OWNER-RULED IN SCOPE 2026-08-20]**` markers land, following the DRV-06 shape.
    - The misattributed Canonical citation is corrected at source so it stops propagating.
    - Commit 2 lands with exactly three files.
    - The apex runs once, after the last commit.
  </behavior>

  <action>
**The three requirement-text corrections (D-04).** All three carry the literal marker
`**[OWNER-RULED IN SCOPE 2026-08-20]**`, appended inline to the requirement bullet in the DRV-06
shape at `REQUIREMENTS.md:46`: marker, then the correction in one or two sentences, then what is
narrowed or withdrawn.

1. **On `LNX-01` (D-30).** Narrow the Conditional Access lever to the corpus's ratified position: the
   lever gates **web access to Entra-protected applications** through Microsoft Edge for Linux 102.x
   and later, not device access; device-level Conditional Access grants are not available for Linux,
   per `docs/_glossary-linux.md` and the byte-exact literal in
   `docs/reference/linux-capability-matrix.md`. Record that this makes LNX-01's "attestation, not
   enforcement" strictly **stronger**. Record also that the companion rule an earlier draft proposed —
   coining no device-level Conditional Access framing anywhere — is **withdrawn**: it has no validator
   behind it, and the corpus already uses the phrase in prose on eleven lines across six files. What
   is barred is creating a device-level Conditional Access **heading** in
   `docs/admin-setup-linux/05-conditional-access.md`, a file this phase does not edit (D-42).
2. **On `DRV-02` (D-31).** Record the Phase-146 D-73 correction: the 0-to-30-day deferral belongs to
   the driver update policy's own `Make updates available after (days)` setting, not to a WUfB
   deployment ring, and the defective sentence in `docs/operations/patch-management/00-overview.md`
   was corrected in Phase 147 with a Microsoft Learn source. `DRV-02` is a completed requirement;
   appending a marker records the correction without reopening it, which is the same thing DRV-06's
   own marker does.
3. **On `LNX-03` (D-32).** Record that the Canonical automatic-updates page does **not** document
   `update-notifier-common` — zero occurrences — and that the page carries a same-prefix decoy which
   is the graphical postpone-prompt client, an unrelated feature. Record that the misattributed
   citation in `.planning/research/PITFALLS.md` was corrected in this phase, and that no first-party
   page fetched in Phase 147 documents a `update-notifier-common` dependency, so the guide ships that
   as a documented silence rather than a claim.

**R6 applies with force here.** `V-54-21` is a live negative on one specific off-by-one filename
literal in this exact file, coupled into `V-54-32`'s atomicity check; R6 names the literal, and this
paragraph deliberately does not repeat it. The new guide is `05-linux-update-delivery.md` and the
Linux compliance file is `03-compliance-policy.md`; a task writing about a Linux compliance policy and
a `05-` file in the same paragraph is precisely where the banned literal slips in. Read R6, then write
the correct filenames.

**The pitfalls citation correction (D-32).** In `.planning/research/PITFALLS.md`, the sentence that
credits the Canonical automatic-updates page for the `update-notifier-common` claim names two sources
and one of them does not carry the claim. Correct the attribution: drop the automatic-updates page as
the source for the `update-notifier-common` half, keep it as the source for the `Automatic-Reboot`
half, and mark the `update-notifier-common` half as carrying no first-party source found in Phase
147's fetch pass. Do not delete the claim — record its sourcing status honestly, which is the same
instrument Phase 145 used when it corrected the FEATURES D-2 defect. Correcting it here stops the
misattribution propagating to every future phase that reads the artifact.

Do not touch the `Automatic-Reboot` rewording guidance, the `(a)`-through-`(f)` list at `:554`, the
distro-list block, or the five-minute-cap scoping paragraphs — all of them are correct and are cited
by plan 01.

**Land commit 2.** Verify `git status --porcelain` shows exactly three changed paths —
`docs/operations/patch-management/00-overview.md`, `.planning/REQUIREMENTS.md` and
`.planning/research/PITFALLS.md` — and that no registry file, filename-map, canary, ops-index or
nav-hub file appears (R8, D-64). D-59 specifies two commits for this phase; the two requirement-text
markers and the pitfalls fix ride in commit 2 rather than opening a third, which follows Phase 146's
own precedent of landing its `REQUIREMENTS.md` correction inside its final content commit. Commit with
a message in the repo's existing `docs(147): ...` form.

Note for the record that `docs/operations/00-index.md` is **not** touched by this phase (D-65). Row
growth there would be safe — `V-59-13` is presence-only and `V-54-28` bars only the literal
`## Patch Management` while the index carries `## Patch & Update Management` — and only `V-59-14` is a
frozen read while `V-59-02`, `V-59-13` and `V-59-34` still live-read it. The ops-index row belongs to
Phase 152 either way.

**Then run the post-phase apex.** `node scripts/validation/check-phase-144.mjs` must print
`Result: 101 PASS, 0 FAIL, 0 SKIPPED (total checks: 101)`. This is the "once after commit 2" half of
D-59's pair; Task 1 of plan 01 ran the "once before commit 1" half. `check-phase-144` **is** the apex —
`ls scripts/validation/ | grep -E "14[5-9]"` returns nothing, because Phases 145 and 146 authored no
validator, so do not look for a later one (D-05). Do not run the apex between the two commits; it is
redundant with the per-commit gates and costs about nineteen seconds each time.

Verify C11 by **running the audit**, never by reading the keyword list (D-67). The live source is
`ALLOWLIST.c11_ops_patterns`, which the sidecar does not define today, so the four-pattern set is a
fallback rather than the authority; `c11_ops_exemptions` is empty and no line pin touches
`docs/operations/`.
  </action>

  <acceptance_criteria>
    - `grep -c '\*\*\[OWNER-RULED IN SCOPE 2026-08-20\]\*\*' .planning/REQUIREMENTS.md` returns `3`
      (D-04).
    - `grep -n '\*\*\[OWNER-RULED IN SCOPE 2026-08-20\]\*\*' .planning/REQUIREMENTS.md` shows the
      three markers on the `DRV-02`, `LNX-01` and `LNX-03` bullets respectively (D-30, D-31, D-32).
    - `grep -c '05-compliance-policy.md' .planning/REQUIREMENTS.md` returns `0` and
      `grep -c '05-compliance-policy.md' .planning/ROADMAP.md` returns `0` (R6, `V-54-21`).
    - `node scripts/validation/check-phase-54.mjs` prints `Summary: 32 passed, 0 failed, 0 skipped` —
      `V-54-21` and its `V-54-32` atomicity coupling both still PASS after the requirements edit.
    - `grep -c 'update-notifier-common' .planning/research/PITFALLS.md` returns at least `1`, and the
      sentence at the corrected site no longer names the Canonical automatic-updates page as the
      source for that half of the claim — verified by reading the corrected sentence (D-32).
    - `git show --stat HEAD` lists exactly three changed paths:
      `docs/operations/patch-management/00-overview.md`, `.planning/REQUIREMENTS.md` and
      `.planning/research/PITFALLS.md` (R8, D-64).
    - `git log --oneline -2` shows exactly two commits for this phase, matching D-59's two-commit
      shape.
    - `node scripts/validation/check-phase-144.mjs` prints
      `Result: 101 PASS, 0 FAIL, 0 SKIPPED (total checks: 101)` after commit 2 (D-59's apex pair).
    - All seven per-commit gates at their exact R1 baselines after commit 2:
      `check-phase-54` 32/0/0 · `check-phase-50` 26/0/0 · `check-nav-hub-links` 0/0 ·
      `c17-eee-contract` 234 files / 0 violations · `v1.20-milestone-audit` 16/0 ·
      `build-filename-map --self-test` 8/0 · `build-publish-bundle --self-test` 15/0 (D-60).
    - `git status --porcelain docs/_registry/ docs/operations/00-index.md` returns empty — no registry
      row and no ops-index row landed, which is why both canaries are still at their baselines
      (D-64, D-65).
  </acceptance_criteria>

  <verify>
    <automated>node scripts/validation/check-phase-54.mjs &amp;&amp; node scripts/validation/check-phase-50.mjs &amp;&amp; node scripts/validation/check-nav-hub-links.mjs &amp;&amp; node scripts/validation/c17-eee-contract.mjs &amp;&amp; node scripts/validation/v1.20-milestone-audit.mjs &amp;&amp; node scripts/pipeline/build-filename-map.mjs --self-test &amp;&amp; node scripts/pipeline/build-publish-bundle.mjs --self-test &amp;&amp; node scripts/validation/check-phase-144.mjs</automated>
  </verify>

  <done>
    Three `**[OWNER-RULED IN SCOPE 2026-08-20]**` markers are in `.planning/REQUIREMENTS.md` on the
    `DRV-02`, `LNX-01` and `LNX-03` bullets. The misattributed Canonical citation in
    `.planning/research/PITFALLS.md` is corrected. Commit 2 has landed with exactly three files, the
    phase stands at exactly two commits, all seven per-commit gates are at their exact HEAD baselines,
    and the post-phase apex is 101 PASS / 0 FAIL / 0 SKIPPED.
  </done>

  <reversibility rating="reversible">
    Three text edits in tracked files, revertible with `git revert`. The requirements markers are
    additive annotations that reopen no completed requirement, and the pitfalls correction restores an
    attribution rather than deleting a claim.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author to reader | The deliverables are one Markdown hub document and two planning artifacts. Nothing crosses at runtime: no endpoint, no untrusted input, no executed code, no credential. The editorial boundary is the only real one, and this plan's edits are routing and comparison text rather than instructions to act. |

## STRIDE Threat Register

ASVS level 1, blocking threshold `high`. Nothing here rises to `high`. This plan ships no shell
snippet and no command at all, so the elevation-of-privilege surface plan 01 carries is absent here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-147-06 | Tampering | The five new Linux cells in the Cross-Platform Comparison table, read by an admin comparing platforms before choosing a posture | medium | mitigate | Every absent capability ships as an explicit parenthetical absence using the table's own existing convention, never blank and never softened — enforced by three exact-string acceptance criteria on the cell literals. The cells are transposed from `docs/reference/linux-capability-matrix.md`, which is cited and unedited, so the hub and the reference layer cannot drift apart in this phase. |
| T-147-07 | Tampering | A silent CRLF zero-match leaving a "successful" edit that never landed, so the overview keeps telling readers there are four platforms while the phase reports done | medium | mitigate | R2 requires confirming each of the eleven replacements individually, and both content tasks carry a `git diff --stat` criterion whose whole purpose is to detect a no-op. `git ls-files --eol` was re-measured at plan time: the edit target is the CRLF file and its structural analog is the LF one. |
| T-147-08 | Repudiation | The three `**[OWNER-RULED IN SCOPE 2026-08-20]**` markers assert an owner ruling inside the requirements record | low | accept | The markers reproduce rulings already recorded verbatim in `147-CONTEXT.md` with their own `[OWNER-RULED 2026-08-20]` stamps, and they follow `REQUIREMENTS.md:46` (DRV-06) as the repo's existing instrument. The audit trail is the CONTEXT file plus the commit; no marker asserts anything the owner did not rule. |
| T-147-09 | Tampering | The one outbound `https://learn.microsoft.com` target added by the D-73 correction | low | accept | `check-nav-hub-links.mjs`'s `resolveLinkTarget` returns null for `http(s)`, so no tool here verifies it. Compensating control: the URL is copied from `06-windows-driver-firmware-updates.md`'s existing in-repo citation for the same fact, already verified in Phase 146, rather than composed fresh. |

**Supply chain (`T-147-SC`): not applicable.** No `npm`, `pip` or `cargo` install and no dependency
added; `147-RESEARCH.md` records no external packages in this phase. The only tool dependency is
`node`, already present and green at HEAD.

**Schema:** no schema is touched — no migration, no model, no table, no API contract.

**Spoofing / Information Disclosure / Denial of Service / Elevation of Privilege:** no applicable
component. No tenant id, device name, serial, account or token appears in any edit, and this plan
ships no command of any kind.
</threat_model>

<probe_accounting>
## Spec-less probe accounting (this plan's share)

The phase-level arithmetic and the full eleven-row disposition table live in `147-01-PLAN.md`
`<probe_accounting>`: **11 probe-surfaced items == 11 authored into `must_haves` + 0 surfaced as
flagged assumptions** — eight resolving `explicit` and three `backstop`. The two flagged assumptions
recorded in plan 01 are residual sourcing gaps rather than probe rows and sit outside this
arithmetic.

This plan carries **1** of the eleven authored rows:

| # | Requirement | Category | Disposition | Where |
|---|---|---|---|---|
| 1 | LNX-01 | idempotency | resolved, **backstop** | `must_haves.truths` above — each inserted literal appears exactly once, backed by the `grep -c '05-linux-update-delivery.md'` returns exactly `3` criterion in Task 2 and by R2's per-replacement confirmation rule |

The remaining ten authored rows are carried by `147-01-PLAN.md`, as are both residual-gap flagged
assumptions. No row is dropped, silently or otherwise, and none is dismissed.

**§B prohibitions.** Two survive the recall-then-precision pass for this plan's scope and are authored
descriptor-less into `must_haves.prohibitions` above: the overview's Linux cells must not overstate
Intune's capability, and the five-platform rewrite must not degrade any existing platform's content to
make room for Linux. Dropped as routine engineering: CRLF handling, table-row line length, date-stamp
arithmetic, validator baselines — all already owned by explicit acceptance criteria. Dropped as canon
with a breadcrumb: no security or compliance canon item applies to a routing-text edit, so none is
minted here.
</probe_accounting>

<assumption_delta_decision>
`gsd_run query assumption-delta scan 147 --json` returns `detected: true` with the same single
`chosen` signal on the token `custom` that plan 01 ruled on — the scan is phase-scoped, so both plans
see one result. Plan 01 ruled it against guide-authoring work. This ruling is against plan 02's own
surface: the five-platform rewrite, the three requirement-text markers, the D-30 Conditional Access
narrowing, the D-31/D-73 driver-deferral correction and the D-32 citation fix. Three nouns were
re-asked. Two hold; one does not.

- **Noun now primary:** `deferral mechanism`, qualified by the policy that owns it.
- **Decision: `add-alongside`** — accepted debt, one table row deep.
- **Rationale:** the D-31/D-73 correction makes the overview's driver/firmware bullet state that
  Windows has *two* deferral settings — the WUfB deployment ring's quality-update deferral, which does
  not reach drivers, and the driver update policy's own `Make updates available after (days)` — while
  the Cross-Platform Comparison table's single `Deferral mechanism` row keeps its Windows cell
  `WUfB deployment rings (quality/feature)` untouched (R8, D-64, and the no-degradation prohibition).
  The prose is corrected; the row above it still asserts the singular, which is the same conflation
  D-73 exists to remove. The corpus already carries the promoted model in
  `06-windows-driver-firmware-updates.md`, so this plan aligns a drifted copy rather than inventing an
  entity — but a second copy stays drifted, and calling that `no-change` would be false.
- **What would force a later promote:** any phase that has to answer *which* deferral applies to a
  given update type **from the table** — a second Windows deferral surface, or the `Deferral mechanism`
  row becoming load-bearing for routing rather than orientation. The promote is then to key that row
  by owning policy instead of by platform, which is a table-shape change and sits outside this phase's
  blast radius. Until then the stale cell is recorded in the SUMMARY next to D-49's sibling-blockquote
  staleness, so verification and code review read it as accepted rather than as a missed edit.

Two further nouns were re-asked and ruled `no-change`, each for its own reason:

- **`platform`** — plan 02 adds a fifth instance to a noun that is already general: four table
  columns, four sibling guides, and a frontmatter `platform:` value that D-40 and `ROADMAP.md:126`
  hold at exactly `cross-platform` rather than enumerating members. Linux populates the existing
  identity; it does not rename it. The one place cardinality is baked into an identity is the
  `(file numbers 01 through 04)` parenthetical, which Task 1 increments and whose rethink trigger
  Task 1 already states rather than leaving implicit.
- **`Conditional Access`** — D-30 does split the lever into web-app CA and device-level CA, but that
  split is already the corpus's ratified primary: `docs/reference/linux-capability-matrix.md` carries
  the byte-exact literal pinned by `V-50-15`, and `docs/_glossary-linux.md` states the device-level
  exclusion. D-30 narrows the *requirement text* toward a model promoted before this phase, and it
  withdraws the companion rule that would have widened the modeling. The movement is toward less
  modeling, not more.

The `custom` signal itself stays a lexical false positive for the reason plan 01 recorded: it belongs
to Intune's product noun *custom compliance script*, not to a knob this plan parameterises. The three
`**[OWNER-RULED IN SCOPE 2026-08-20]**` markers reuse an instrument that already exists at
`REQUIREMENTS.md:46`, so they add an instance, not a kind. This checkpoint is advisory and blocks
nothing.
</assumption_delta_decision>

<verification>
Run the seven per-commit gates from the repository root after Task 3, then the apex:

1. `node scripts/validation/check-phase-54.mjs` -> `Summary: 32 passed, 0 failed, 0 skipped`
2. `node scripts/validation/check-phase-50.mjs` -> `Summary: 26 passed, 0 failed, 0 skipped`
3. `node scripts/validation/check-nav-hub-links.mjs` -> `check-nav-hub-links summary: 0 hub-presence
   failure(s), 0 corpus-link failure(s), 0 total`
4. `node scripts/validation/c17-eee-contract.mjs` -> `C17 summary: 234 files checked, 0 with
   violations, 0 total violations`
5. `node scripts/validation/v1.20-milestone-audit.mjs` -> `Summary: 16 passed, 0 failed`
6. `node scripts/pipeline/build-filename-map.mjs --self-test` -> `8 passed, 0 failed`
7. `node scripts/pipeline/build-publish-bundle.mjs --self-test` -> `15 passed, 0 failed`

Then, once, after commit 2: `node scripts/validation/check-phase-144.mjs` ->
`Result: 101 PASS, 0 FAIL, 0 SKIPPED (total checks: 101)`.

Any deviation from these exact numbers is a regression, not a new baseline. Both canaries run because
their passing at 225-versus-225 is the mechanical proof that no registry row landed anywhere in this
phase.
</verification>

<success_criteria>
- `docs/operations/patch-management/00-overview.md` reads as a five-platform hub: five table columns
  with Linux last, five-platform prose at all eleven swept sites, and three inbound links to the new
  guide.
- The overview's frontmatter `platform:` value is still exactly `cross-platform`.
- The Phase-146 D-73 defect is corrected with a Microsoft Learn source.
- Three `**[OWNER-RULED IN SCOPE 2026-08-20]**` markers exist in `.planning/REQUIREMENTS.md`.
- The misattributed Canonical citation in `.planning/research/PITFALLS.md` is corrected.
- Exactly three files are in commit 2's diff, and the phase stands at exactly two commits.
- All seven per-commit gates are at their exact HEAD baselines and the post-phase apex is 101/0/0.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols this plan introduces, for the source-grounding pass to exclude from drift verification.

### Modified files

| Path | Produced by | Consumed by |
|---|---|---|
| `docs/operations/patch-management/00-overview.md` | plan 02 | every reader routing to a per-platform update guide; Phase 148, which is the fourth and last content phase to touch it |
| `.planning/REQUIREMENTS.md` | plan 02 | every future phase reading the requirement record; `check-phase-54`'s `V-54-21` live-reads it |
| `.planning/research/PITFALLS.md` | plan 02 | every future phase reading the pitfalls artifact — which is exactly why the misattribution is corrected at source |

### New table column

A fifth `Linux` column in the overview's Cross-Platform Comparison table, appended last, with five new
cells whose literals are pinned by acceptance criteria:
`(No Intune-side deferral; distro-native only)` · `(None — compliance policy + web-app CA is
attestation only)` · `Livepatch via Ubuntu Pro (Canonical-side entitlement)` · `(no hard deadline)` ·
the distro-native cadence cell.

### New requirement-table states

Three `**[OWNER-RULED IN SCOPE 2026-08-20]**` markers, on the `DRV-02`, `LNX-01` and `LNX-03` bullets
of `.planning/REQUIREMENTS.md`. The marker literal itself already exists in the file at `:46` with a
2026-08-19 date; the 2026-08-20 variant is new.

### New inbound links

Three references to `05-linux-update-delivery.md` inside `00-overview.md` — one blockquote line, one
routing sentence, one Related Resources entry — plus a Linux clause in the Attestation bullet's
`Examples:` list that names no file.

### Explicitly NOT produced

No new file, no registry row, no filename-map row, no canary bump, no `docs/operations/00-index.md`
row, no nav-hub wiring, no glossary entry, no edit to `docs/reference/`, `docs/admin-setup-linux/`,
`06-windows-driver-firmware-updates.md`, or the four sibling routing blockquotes. All Phase 152's or
ruled out (D-41, D-42, D-47, D-49, D-64, D-65).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/147-linux-update-delivery/147-02-SUMMARY.md` when done.

The SUMMARY must additionally record: the chosen insertion point for the Related Resources entry and
why (D-39); whether the conditional date re-stamp fired or was a deliberate no-op, with the dates
(D-61); the four sibling routing blockquotes' knowingly accepted staleness (D-49); the three
Phase-151 hand-offs (D-73); the untouched Windows `Deferral mechanism` table cell's knowingly
accepted staleness, per the `add-alongside` ruling in `<assumption_delta_decision>`; and the exact
`git show --stat` output for commit 2.
</output>
