---
phase: 71-archive-automation-root-cause-fix-pillar-a
plan: 02
type: execute
wave: 2
depends_on:
  - 71-01
files_modified:
  - .planning/MILESTONES.md
autonomous: true
requirements:
  - ARCHIVE-02
tags:
  - milestones-re-authoring
  - archive-residue-sweep
  - replacement-not-deletion
  - milestone-audit-source-of-truth

must_haves:
  truths:
    - "v1.1 H2 entry in `.planning/MILESTONES.md` is re-authored from canonical `v1.1-MILESTONE-AUDIT.md` source-of-truth; the `- Edit 1 -- docs/error-codes/00-index.md:` debris line is REPLACED with restored semantic content (the truncated bullet's actual subject — error-code consolidation work)"
    - "v1.2 H2 entry in `.planning/MILESTONES.md` is re-authored from canonical `v1.2-MILESTONE-AUDIT.md` source-of-truth; the 4 debris lines (3× `- One-liner:` + 1× `- Commit:` at lines 145-148) are REPLACED with canonical bullets covering Phase 20-25 deliverables"
    - "V-71-MILESTONES-01 + V-71-ARCHIVE02-01 flip from transient FAIL (Plan 71-01 SHA) → PASS (this plan's SHA), closing the chicken-and-egg gap"
    - "v1.0/v1.3/v1.4/v1.4.1/v1.5/v1.6/v1.7 H2 ranges remain byte-unchanged (predecessor-byte-unchanged invariant) — `git diff` against this SHA touches ONLY the v1.1 + v1.2 H2 hunks"
    - "All v1.4/v1.5/v1.6/v1.7 workflow YAMLs + audit harnesses + sidecar JSONs remain byte-unchanged since Plan 71-01 SHA (anti-regression invariant per REQUIREMENTS.md:69)"
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "Re-authored v1.1 H2 + v1.2 H2 entries from MILESTONE-AUDIT canonical source-of-truth; zero placeholder-label debris"
      contains: "## v1.1"
  key_links:
    - from: ".planning/MILESTONES.md (v1.1 H2)"
      to: ".planning/milestones/v1.1-MILESTONE-AUDIT.md"
      via: "Bullet content derived from Requirements Coverage table + Tech Debt + Conclusion sections; **Note:** row cites audit-doc path + retroactive-authoring date 2026-06-03"
      pattern: "v1.1-MILESTONE-AUDIT\\.md"
    - from: ".planning/MILESTONES.md (v1.2 H2)"
      to: ".planning/milestones/v1.2-MILESTONE-AUDIT.md"
      via: "Bullet content derived from Executive Summary + Phase Verification Summary + Requirements Coverage tables; **Audit status:** row cites `gaps_found` per audit-doc frontmatter"
      pattern: "v1.2-MILESTONE-AUDIT\\.md"
    - from: "scripts/validation/check-phase-71.mjs (V-71-MILESTONES-01)"
      to: ".planning/MILESTONES.md HEAD"
      via: "Anchored-bullet regex scan — flips FAIL→PASS at this plan's commit"
      pattern: "PLACEHOLDER_TOKENS"
---

<objective>
Re-author the v1.1 + v1.2 H2 entries in `.planning/MILESTONES.md` from canonical source-of-truth `.planning/milestones/v1.1-MILESTONE-AUDIT.md` + `.planning/milestones/v1.2-MILESTONE-AUDIT.md`, sweeping the 5 placeholder-label debris lines (v1.2:145-148 + v1.1:164) via REPLACEMENT FROM CANONICAL SOURCE (NOT deletion). Symmetric with v1.6 + v1.7 retroactive-authoring at v1.7 close 2026-05-29. Honors "DO NOT mask via deletion — investigate the script" doctrine per `v1.7-DEFERRED-CLEANUP.md:30,32`.

Purpose: ARCHIVE-02 closure. Plan 71-01 shipped the V-71-MILESTONES-01 + V-71-ARCHIVE02-01 tripwire assertions; this plan REMOVES the debris those assertions detect. The 5th debris line (v1.1 line 164 `- Edit 1 -- docs/error-codes/00-index.md:`) was a NEW DISCOVERY by the D-03 advisor's pre-sweep grep enumeration — NOT in `v1.7-DEFERRED-CLEANUP.md:38-48` known-sites list; NOT tracked in Plan 68-04 Garbage Debris Elimination Confirmation. Per CONTEXT D-03 LOCKED Option D, REPLACEMENT (not raw deletion) restores the semantic content (the truncated bullet's actual subject was error-code consolidation work per `v1.1-MILESTONE-AUDIT.md` Requirements Coverage rows 70-96).

Output:
- `.planning/MILESTONES.md` — v1.1 H2 re-authored (~15 lines) + v1.2 H2 re-authored (~20 lines); both with `**Audit status:**` row + `**Note:** retroactively authored 2026-06-03 during Phase 71 ARCHIVE-02 sweep` row + canonical bullet list

This plan ships ONE commit. Plan 71-02 is separately revertable from Plan 71-01 per Phase 68 Plan 68-04 `d142c7a` MILESTONES.md-separation precedent (different epistemic class: root-cause-fix vs. symptom-residue sweep).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-CONTEXT.md
@.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-RESEARCH.md
@.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-PATTERNS.md
@.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-01-SUMMARY.md
@.planning/milestones/v1.7-DEFERRED-CLEANUP.md
@.planning/milestones/v1.1-MILESTONE-AUDIT.md
@.planning/milestones/v1.2-MILESTONE-AUDIT.md
@.planning/MILESTONES.md

<interfaces>
<!-- Re-authoring source-of-truth + retroactive-authoring exemplar -->

From `.planning/MILESTONES.md` lines 25-32 (v1.6 H2 — canonical retroactive-authoring style; Phase 71 v1.1 + v1.2 H2 entries adopt this pattern):

```markdown
## v1.6 Apple Business Delegated Governance & Multi-Org Operations (Shipped: 2026-05-25)

**Phases completed:** 5 phases (62-66), 30 plans
**Audit status:** `passed` (.planning/milestones/v1.6-MILESTONE-AUDIT.md; terminal re-audit at Phase 66-04 from fresh `git clone --no-hardlinks` ...; 23 PASS / 0 FAIL / 5 SKIPPED in check-phase-66.mjs; 15/15 harness PASS in fully-blocking mode; 39/39 requirements closed)

**Note:** v1.6 entry retroactively authored during v1.7 milestone close (2026-05-29) after `/gsd-complete-milestone v1.7` invocation surfaced the ARCHIVE-01 cdcce23-class defect ...

**Key accomplishments (summary; see v1.6-MILESTONE-AUDIT.md for full detail):** [...paragraph form with -- separators...]
```

KEY pattern elements (apply to v1.1 + v1.2 re-authored H2 entries):
1. `## vX.Y <Title> (Shipped: YYYY-MM-DD)` H2 heading
2. `**Phases completed:** N phases (XX-YY), N plans, [N tasks]` summary row
3. `**Audit status:** \`<status>\` (.planning/milestones/<v>-MILESTONE-AUDIT.md; <key metrics>)` row
4. `**Note:**` row citing retroactive-authoring date 2026-06-03 + Phase 71 ARCHIVE-02 sweep context
5. `**Key accomplishments:**` — bullet-list form per v1.7 H2 (preferred for Phase 71) OR paragraph-summary form per v1.6 H2

From `.planning/milestones/v1.1-MILESTONE-AUDIT.md` (165 lines) — lines 70-96 Requirements Coverage table + lines 114-136 Tech Debt section + line 165 Conclusion ("All 24 v1.1 requirements are satisfied. No critical blockers."):
Source-of-truth for v1.1 H2 re-authoring. Frontmatter `status: tech_debt`. 7 phases (11-17), 18 plans, 42 tasks.

From `.planning/milestones/v1.2-MILESTONE-AUDIT.md` (345 lines) — lines 5-187 frontmatter + Executive Summary + lines 206-261 Requirements Coverage + lines 268-275 Phase Verification Summary:
Source-of-truth for v1.2 H2 re-authoring. Frontmatter `status: gaps_found` (16/38 requirements formally verified; Phases 21 + 24 lacked VERIFICATION.md at close — process gaps not content gaps). 6 phases (20-25), 20 plans, 23 tasks.

From `.planning/MILESTONES.md` pre-sweep state — empirical residue inventory per D-03 advisor:

| Line | Content | Token class |
|---|---|---|
| v1.2:145 | `- One-liner:` | placeholder-label |
| v1.2:146 | `- One-liner:` | placeholder-label |
| v1.2:147 | `- Commit:` | placeholder-label |
| v1.2:148 | `- One-liner:` | placeholder-label |
| v1.1:164 | `- Edit 1 -- docs/error-codes/00-index.md:` | NEW DISCOVERY (different token-class — not in known-sites list) |

Edit-tool surgical-edit mechanism (per Plan 68-04 T-68-04-LE precedent):
- Use `Edit` tool with VERBATIM `old_string` + `new_string` blocks (NOT `Write`, NOT PowerShell `Set-Content` — CRLF/LF drift risk)
- Use SMALL `old_string` ranges (4-debris-line block + 2 surrounding lines for uniqueness) per RESEARCH Pitfall 3
- Read MILESTONES.md FIRST to confirm exact line ranges (line numbers may shift slightly from CONTEXT estimates as the file evolves; locate H2 boundaries by `^## v1\.X ` regex scan)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read source-of-truth audit docs + locate v1.1 H2 + v1.2 H2 ranges in MILESTONES.md</name>
  <files>(no writes — reconnaissance task)</files>
  <read_first>
    - `.planning/milestones/v1.1-MILESTONE-AUDIT.md` (entire 165 lines — frontmatter + Executive Summary + Requirements Coverage table lines 70-96 + Tech Debt lines 114-136 + Conclusion line 165; identify the 5 bullets-worth of accomplishment content from Phases 11-17)
    - `.planning/milestones/v1.2-MILESTONE-AUDIT.md` (entire 345 lines — frontmatter lines 5-187 + Executive Summary + Requirements Coverage tables lines 206-261 + Phase Verification Summary lines 268-275; identify the 5 bullets-worth of accomplishment content from Phases 20-25 + the `status: gaps_found` rationale)
    - `.planning/MILESTONES.md` (entire file — locate v1.0/v1.1/v1.2/v1.3/v1.4/v1.4.1/v1.5/v1.6/v1.7 H2 boundaries via `^## v1\.X ` scan; confirm v1.2 debris lines at ~145-148 + v1.1 debris line at ~164 — EXACT LINE NUMBERS may have shifted)
    - `.planning/MILESTONES.md` lines 3-32 (v1.6 + v1.7 H2 retroactive-authoring exemplars — Phase 71 v1.1 + v1.2 inherit this style)
    - `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-RESEARCH.md` §"Example 8: Plan 71-02 v1.1 H2 Re-Authoring (Edit Tool)" (lines 846-897 — concrete 5-bullet skeleton derived from v1.1-MILESTONE-AUDIT.md; Note: rows cite retroactive-authoring 2026-06-03 + ARCHIVE-02 + v1.7-DEFERRED-CLEANUP.md:38-48 NEW DISCOVERY)
    - `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-RESEARCH.md` §"Example 9: Plan 71-02 v1.2 H2 Re-Authoring (Edit Tool)" (lines 899-960 — concrete 5-bullet skeleton derived from v1.2-MILESTONE-AUDIT.md; `Audit status: gaps_found` row reflects audit-doc frontmatter line 5)
    - `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-PATTERNS.md` §"`.planning/MILESTONES.md`" (lines 451-484 — Edit-tool surgical-edit mechanism + anti-patterns: NO Set-Content/Out-File, NO large old_string, NO touching v1.0/v1.3/v1.4/v1.4.1/v1.5/v1.6/v1.7 H2 ranges)
  </read_first>
  <action>
    Read all three source files (`v1.1-MILESTONE-AUDIT.md`, `v1.2-MILESTONE-AUDIT.md`, `MILESTONES.md`) once in full. Extract:
    (a) v1.1 accomplishment narrative — 5 bullets minimum covering Phases 11 (LIFE-01..04), 12-14 (TROU-01..05), 15 (ASET-01..05), 16 (ADMN-01..07), 17 (NAVG-01..04) — per audit-doc Requirements Coverage table; the 5th bullet semantically RESTORES the truncated `- Edit 1 -- docs/error-codes/00-index.md:` debris by covering the error-code consolidation work in Phases 16-17.
    (b) v1.2 accomplishment narrative — 5 bullets minimum covering Phase 20 (XPLAT-01..04 / NAVX-01), Phase 21 (WDLC/WINF/WSEC/WMIG/WMON 18 reqs), Phase 22 (MLIF-01..03), Phase 23 (MADM-01..06), Phases 24-25 (MTRO-01..04 + NAVX-02..03) — per audit-doc Requirements Coverage tables; reflect `status: gaps_found` honestly (Phases 21 + 24 lacked VERIFICATION.md at close — process gaps not content gaps; tech-debt routed to v1.3).
    (c) Exact line ranges in `.planning/MILESTONES.md` for: v1.0 H2 start, v1.1 H2 start, v1.2 H2 start, v1.3 H2 start (the H2 boundaries that frame the v1.1 + v1.2 edit ranges). RESEARCH lines 569 estimates v1.2 H2 at lines 138-153 and v1.1 H2 at lines 155-166 — verify these are still accurate at execution time (file may have shifted slightly since the research date 2026-06-03).

    Produce a reconnaissance memo at `.claude/tmp/71-02-reconnaissance.md` containing:
    - Final v1.1 H2 line range (start line + end line, inclusive)
    - Final v1.2 H2 line range (start line + end line, inclusive)
    - The 5 v1.1 bullets in draft form (1-2 sentences each, REQ-ID-anchored)
    - The 5 v1.2 bullets in draft form (1-2 sentences each, REQ-ID-anchored)
    - Audit-status one-liner for v1.1 (cite audit-doc path + key metric: 24/24 requirements + 7/7 phases + 5/6 integration + 5/6 flows)
    - Audit-status one-liner for v1.2 (cite audit-doc path + key metric: 16/38 requirements formally verified; Phases 21 + 24 lacked VERIFICATION.md; tech-debt routed to v1.3 closure)

    Per CONTEXT line 194 Claude's Discretion: bullet count of 5 per H2 matches v1.6 + v1.7 manually-authored scale; the planner MAY tune bullet count per H2 (4-6 acceptable). Discretion ALSO permits switching between bullet-list form (v1.7 style) and paragraph-summary form (v1.6 style); RESEARCH recommends bullet-list per Phase 71 dominant pattern.

    Do NOT write to MILESTONES.md in this task. The actual Edit-tool invocation is Task 2 (v1.1 H2) + Task 3 (v1.2 H2).
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path .claude/tmp/71-02-reconnaissance.md) { Get-Content .claude/tmp/71-02-reconnaissance.md | Select-String -Pattern 'v1\.1 H2.*range|v1\.2 H2.*range' | Measure-Object | Select-Object -ExpandProperty Count }"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/tmp/71-02-reconnaissance.md` exists
    - Memo cites concrete line range for v1.1 H2 (start line + end line)
    - Memo cites concrete line range for v1.2 H2 (start line + end line)
    - Memo lists 5 draft bullets for v1.1 H2 (REQ-ID-anchored per audit doc)
    - Memo lists 5 draft bullets for v1.2 H2 (REQ-ID-anchored per audit doc)
    - Memo cites `status: tech_debt` for v1.1 (from `.planning/milestones/v1.1-MILESTONE-AUDIT.md` frontmatter)
    - Memo cites `status: gaps_found` for v1.2 (from `.planning/milestones/v1.2-MILESTONE-AUDIT.md` frontmatter line 5)
    - Memo identifies the 5th v1.1 bullet as the semantic restoration of the truncated `- Edit 1 -- docs/error-codes/00-index.md:` debris (covering error-code consolidation work)
  </acceptance_criteria>
  <done>
    Reconnaissance memo at `.claude/tmp/71-02-reconnaissance.md` provides concrete line ranges + draft bullet content + audit-status one-liners ready for Edit-tool invocations in Tasks 2-3. Nothing committed; planner has executed the one judgment-bearing step (bullet distillation) before any file is touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit-tool surgical edit — v1.1 H2 re-authoring (REPLACE `- Edit 1 --` debris)</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - `.planning/MILESTONES.md` (the current state — Read tool returns content with line numbers; confirms existing line-ending convention via the Read output)
    - `.claude/tmp/71-02-reconnaissance.md` (Task 1 output — final v1.1 H2 line range + 5 draft bullets + audit-status one-liner)
    - `.planning/milestones/v1.1-MILESTONE-AUDIT.md` (re-confirm bullet content matches audit-doc Requirements Coverage)
    - `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-RESEARCH.md` §"Pitfall 3: Edit-Tool old_string Trailing-Whitespace Drift" (lines 562-573 — use SMALL old_string ranges, NOT whole H2 block)
    - `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-PATTERNS.md` §"Anti-patterns to avoid" (lines 480-484 — NO Set-Content/Out-File; NO large old_string ranges; NO touching v1.0/v1.3+ H2 ranges)
  </read_first>
  <behavior>
    - Edit tool invocation produces a single hunk on the v1.1 H2 range
    - `old_string` captures the existing v1.1 H2 block including the `- Edit 1 -- docs/error-codes/00-index.md:` debris line (and enough surrounding context for uniqueness, but NOT the entire H2 block — small range preferred per Pitfall 3)
    - `new_string` is the re-authored v1.1 H2 block with: H2 heading row + `**Phases completed:**` row + `**Audit status:**` row citing `tech_debt` + `**Note:**` row citing retroactive-authoring 2026-06-03 + ARCHIVE-02 sweep + `v1.7-DEFERRED-CLEANUP.md:38-48 NEW DISCOVERY` provenance + `**Key accomplishments:**` row + 5 bullet items
    - Bullet 5 (the semantic restoration of the truncated `- Edit 1 --` debris) covers Intune Connector guide + 30-entry consolidated error-code reverse-lookup table per Phases 16-17 ADMN-06..07 + NAVG-01..04 audit-doc rows
    - EOL convention preserved (whatever MILESTONES.md uses — CRLF or LF; Edit tool inherits the file's line endings)
    - Post-edit: `grep -c "^- Edit 1 --" .planning/MILESTONES.md` returns 0
    - Post-edit: v1.1 H2 contains the literal substring `v1.1-MILESTONE-AUDIT.md` (Note: row cites audit-doc path)
    - v1.0 H2 + v1.2 H2 + v1.3 H2 + v1.4 H2 + v1.4.1 H2 + v1.5 H2 + v1.6 H2 + v1.7 H2 ranges are byte-unchanged (other H2 boundaries untouched)
  </behavior>
  <action>
    Use the `Edit` tool (NOT `Write`, NOT PowerShell `Set-Content` per Plan 68-04 T-68-04-LE precedent — CRLF/LF drift risk).

    Step 1: Re-read `.planning/MILESTONES.md` (via Read tool) to capture the CURRENT exact v1.1 H2 content with trailing whitespace, exact line endings, exact bullet markers. Markdown editors hide trailing whitespace; the Read tool returns lines verbatim.

    Step 2: Construct `old_string` as a SMALL contextual block — the 4-6 lines immediately around the `- Edit 1 --` debris line for uniqueness (the H2 heading + a preceding bullet + the debris bullet + a following line if present). Do NOT include the entire H2 block — large old_strings increase whitespace-drift mismatch risk per Pitfall 3.

    Step 3: Construct `new_string` as the re-authored v1.1 H2 content per Task 1 reconnaissance memo. Structure:
    ```
    ## v1.1 APv2 Documentation & Admin Setup Guides (Shipped: 2026-04-13)

    **Phases completed:** 7 phases (11-17), 18 plans, 42 tasks
    **Audit status:** `tech_debt` (.planning/milestones/v1.1-MILESTONE-AUDIT.md; <key metrics from reconnaissance memo>)

    **Note:** v1.1 entry retroactively re-authored 2026-06-03 during Phase 71 ARCHIVE-02 sweep from canonical source-of-truth `v1.1-MILESTONE-AUDIT.md` per Plan 71-02. Symmetric with v1.6+v1.7 retroactive-authoring 2026-05-29. The pre-sweep entry carried 1 debris line at line 164 (`- Edit 1 -- docs/error-codes/00-index.md:`) — discovered by D-03 advisor pre-sweep grep enumeration; NOT in `v1.7-DEFERRED-CLEANUP.md:38-48` known-sites list.

    **Key accomplishments:**

    - <bullet 1 from reconnaissance memo — Phase 16 ADMN-01..03>
    - <bullet 2 — Phase 16 ADMN-04..07>
    - <bullet 3 — Phases 11-14 LIFE-01..04 + TROU-01..05>
    - <bullet 4 — Phase 15 ASET-01..05>
    - <bullet 5 — Phases 16-17 ADMN-06..07 + NAVG-01..04 SEMANTIC RESTORATION OF TRUNCATED DEBRIS LINE>
    ```

    Step 4: Invoke `Edit` tool with `old_string` + `new_string`. If Edit reports `old_string not found`, REVERT mentally (no file change yet), re-Read the target lines, and reconstruct `old_string` with exact whitespace + line-endings.

    Step 5: Post-edit, run `grep -c "^- Edit 1 --" .planning/MILESTONES.md` — MUST return 0.

    Step 6: Visually inspect `git diff HEAD -- .planning/MILESTONES.md` to confirm changes are limited to the v1.1 H2 range (no spurious whitespace edits elsewhere).
  </action>
  <verify>
    <automated>powershell -Command "$diff = git diff HEAD -- .planning/MILESTONES.md; if ((Select-String -InputObject $diff -Pattern '^- Edit 1 --' -SimpleMatch).Count -gt 0) { exit 1 }; if ((Get-Content .planning/MILESTONES.md | Select-String -Pattern '^- Edit 1 --' -SimpleMatch).Count -ne 0) { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- Edit 1 --" .planning/MILESTONES.md` returns 0 (debris line removed)
    - `grep -c "## v1.1 APv2 Documentation" .planning/MILESTONES.md` returns 1 (H2 heading preserved)
    - `grep -c "v1.1-MILESTONE-AUDIT.md" .planning/MILESTONES.md` ≥ 1 (audit-doc cited in Note row)
    - `grep -c "retroactively re-authored 2026-06-03" .planning/MILESTONES.md` ≥ 1 (Note row present)
    - `grep -c "tech_debt" .planning/MILESTONES.md` ≥ 1 (Audit status row present)
    - `git diff HEAD -- .planning/MILESTONES.md` shows changes EXCLUSIVELY within the v1.1 H2 range — no hunks in v1.0/v1.2/v1.3/v1.4/v1.4.1/v1.5/v1.6/v1.7 H2 ranges
  </acceptance_criteria>
  <done>
    v1.1 H2 entry re-authored from `v1.1-MILESTONE-AUDIT.md` canonical source-of-truth. The `- Edit 1 -- docs/error-codes/00-index.md:` debris is REPLACED (not deleted) with semantic restoration. `**Audit status:** tech_debt` + `**Note:** retroactively re-authored 2026-06-03` rows present. Other H2 entries byte-unchanged. Not yet committed (Task 3 adds the v1.2 H2 edit; Task 4 commits both atomically within Plan 71-02).
  </done>
</task>

<task type="auto">
  <name>Task 3: Edit-tool surgical edit — v1.2 H2 re-authoring (REPLACE 4 debris lines)</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - `.planning/MILESTONES.md` (current state after Task 2 — re-Read; line numbers may have shifted from Task 2's v1.1 H2 edit)
    - `.claude/tmp/71-02-reconnaissance.md` (Task 1 output — final v1.2 H2 line range + 5 draft bullets + audit-status one-liner with `gaps_found` rationale)
    - `.planning/milestones/v1.2-MILESTONE-AUDIT.md` (re-confirm bullet content matches audit-doc Requirements Coverage; cite the `gaps_found` rationale honestly: Phases 21 + 24 lacked VERIFICATION.md at close — process gaps not content gaps; tech-debt routed to v1.3)
    - `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-RESEARCH.md` §"Pitfall 3 + Pitfall 4" (lines 562-586 — small old_string ranges + check v1.0/v1.3+ ranges untouched after edit)
  </read_first>
  <behavior>
    - Edit tool invocation produces a single hunk on the v1.2 H2 range
    - `old_string` captures the existing v1.2 H2 block including 4 debris lines (3× `- One-liner:` + 1× `- Commit:` at the known sites) with enough surrounding context for uniqueness
    - `new_string` is the re-authored v1.2 H2 block with: H2 heading + Phases completed row + `**Audit status:** gaps_found` row + Note row + 5 bullet items covering Phases 20-25
    - Post-edit: `grep -cE "^- (One-liner:|Commit:)" .planning/MILESTONES.md` returns 0 (all v1.2 debris removed)
    - Post-edit: v1.2 H2 contains literal substring `v1.2-MILESTONE-AUDIT.md` (Note: row cites audit-doc path) + `gaps_found` (Audit status row)
    - v1.0/v1.1/v1.3/v1.4/v1.4.1/v1.5/v1.6/v1.7 H2 ranges remain byte-unchanged (other H2 boundaries untouched relative to Task 2's post-edit state)
  </behavior>
  <action>
    Same mechanism as Task 2 with v1.2-specific content.

    Step 1: Re-read `.planning/MILESTONES.md` (Task 2 may have shifted line numbers for the v1.2 region if v1.1 re-author changed line count). Confirm v1.2 H2 boundaries by locating `^## v1\.2 ` heading.

    Step 2: Construct `old_string` as SMALL contextual block — the 4 debris lines + 1-2 surrounding lines for uniqueness. The debris cluster is contiguous (3× One-liner + 1× Commit) per pre-sweep enumeration.

    Step 3: Construct `new_string` per Task 1 reconnaissance memo. Structure:
    ```
    ## v1.2 Cross-Platform Provisioning & Operational Gaps (Shipped: 2026-04-16)

    **Phases completed:** 6 phases (20-25), 20 plans, 23 tasks
    **Audit status:** `gaps_found` (.planning/milestones/v1.2-MILESTONE-AUDIT.md; 16/38 requirements formally verified; Phases 21 + 24 lacked VERIFICATION.md at close — process gaps not content gaps; tech-debt routed to v1.3 closure)

    **Note:** v1.2 entry retroactively re-authored 2026-06-03 during Phase 71 ARCHIVE-02 sweep from canonical source-of-truth `v1.2-MILESTONE-AUDIT.md` per Plan 71-02. Symmetric with v1.6+v1.7 retroactive-authoring 2026-05-29. The pre-sweep entry carried 4 debris lines at the v1.2 H2's bullet section (3× `- One-liner:` + 1× `- Commit:`) per `v1.7-DEFERRED-CLEANUP.md:38-48` known-sites enumeration.

    **Key accomplishments:**

    - <bullet 1 — Phase 20 XPLAT-01..04 + NAVX-01 cross-platform nav hub>
    - <bullet 2 — Phase 21 WDLC/WINF/WSEC/WMIG/WMON 18 reqs — content shipped; VERIFICATION.md tech-debt routed to v1.3>
    - <bullet 3 — Phase 22 MLIF-01..03 macOS lifecycle foundation>
    - <bullet 4 — Phase 23 MADM-01..06 macOS admin setup + commands + log paths>
    - <bullet 5 — Phases 24-25 MTRO-01..04 + NAVX-02..03 cross-platform symptom routing>
    ```

    Step 4: Invoke `Edit` tool with `old_string` + `new_string`. If Edit reports `old_string not found`, REVERT mentally (no file change yet — the v1.1 edit from Task 2 stays in place), re-Read the target lines, and reconstruct `old_string`.

    Step 5: Post-edit, run `grep -cE "^- (One-liner:|Commit:)" .planning/MILESTONES.md` — MUST return 0.

    Step 6: Visually inspect `git diff HEAD -- .planning/MILESTONES.md` — should show EXACTLY 2 hunks (one for v1.1 H2 from Task 2, one for v1.2 H2 from this task). Any third hunk indicates accidental drift; revert and redo per Pitfall 4.
  </action>
  <verify>
    <automated>powershell -Command "if ((Get-Content .planning/MILESTONES.md | Select-String -Pattern '^- (One-liner:|Commit:)' -CaseSensitive).Count -ne 0) { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^- (One-liner:|Commit:)" .planning/MILESTONES.md` returns 0 (v1.2 debris removed)
    - `grep -c "## v1.2 Cross-Platform" .planning/MILESTONES.md` returns 1 (H2 heading preserved)
    - `grep -c "v1.2-MILESTONE-AUDIT.md" .planning/MILESTONES.md` ≥ 1 (audit-doc cited in Note row)
    - `grep -c "gaps_found" .planning/MILESTONES.md` ≥ 1 (Audit status row reflects audit-doc frontmatter honestly)
    - `git diff HEAD -- .planning/MILESTONES.md` shows EXACTLY 2 hunks (v1.1 H2 from Task 2 + v1.2 H2 from this task) — no spurious 3rd+ hunks
    - All 5 v1.2 bullets cite Phase 20 / 21 / 22 / 23 / 24-25 REQ-IDs per audit-doc Requirements Coverage
  </acceptance_criteria>
  <done>
    v1.2 H2 entry re-authored from `v1.2-MILESTONE-AUDIT.md` canonical source-of-truth. 4 debris lines REPLACED with 5 canonical accomplishment bullets covering Phase 20-25 deliverables + `**Audit status:** gaps_found` row + `**Note:** retroactively re-authored 2026-06-03` row. Other H2 ranges byte-unchanged. Both v1.1 + v1.2 H2 edits are staged in the working tree (not yet committed); Task 4 stages + commits both atomically.
  </done>
</task>

<task type="auto">
  <name>Task 4: Pre-commit dry-run + commit (V-71-MILESTONES-01 + V-71-ARCHIVE02-01 flip FAIL→PASS)</name>
  <files>(no new files — git operation only)</files>
  <read_first>
    - `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-RESEARCH.md` §"Example 10: Plan 71-02 Post-Edit Boundary Verification" (lines 962-1000 — PowerShell pre-commit dry-run + predecessor-byte-unchanged check + MILESTONES.md diff hunk-count check)
    - `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-PATTERNS.md` §"Shared Pattern 5: Predecessor-byte-unchanged invariant" (lines 673-697 — PowerShell predecessor-surface check)
    - Phase 68 Plan 68-04 commit `d142c7a` git log — MILESTONES.md surgical edit as SEPARATE plan from validator surgery (Phase 71 Plan 71-02 inherits this precedent)
    - Plan 71-01 SHA (captured by Plan 71-01 Task 4 closing operation; available via `git log -1 --format=%H --grep='ARCHIVE-01.*atomic SC#4'`)
  </read_first>
  <behavior>
    - Pre-commit dry-run executes:
      (a) `node scripts/validation/check-phase-71.mjs` → MUST exit 0 (V-71-MILESTONES-01 + V-71-ARCHIVE02-01 flip from transient FAIL at Plan 71-01 SHA → PASS at the staged state)
      (b) Predecessor-byte-unchanged check: `git diff <Plan-71-01-SHA> HEAD -- .github/workflows/audit-harness-v1.{4,5,6,7}-integrity.yml scripts/validation/v1.{4,5,6,7}*.{mjs,json}` returns EMPTY (zero modification to v1.4/v1.5/v1.6/v1.7 frozen surfaces)
      (c) MILESTONES.md hunk count: `git diff HEAD -- .planning/MILESTONES.md` produces EXACTLY 2 hunks (v1.1 H2 + v1.2 H2)
    - `git add` stages exactly 1 file: `.planning/MILESTONES.md`
    - `git commit` creates ONE SHA covering the v1.1 + v1.2 H2 re-authoring (atomic-within-plan; both H2 edits in ONE commit per RESEARCH §"Open Questions" #4 LOCKED resolution lines 1063-1067)
    - Commit message subject: `docs(archive): ARCHIVE-02 re-author v1.1+v1.2 MILESTONES.md entries from MILESTONE-AUDIT source (replacement-not-deletion per ARCHIVE-01 doctrine)`
    - Commit message body explicitly mentions:
      - REPLACEMENT FROM CANONICAL SOURCE (not deletion) per `v1.7-DEFERRED-CLEANUP.md:30,32` retroactive-authoring doctrine
      - v1.1 line 164 NEW DISCOVERY by D-03 advisor pre-sweep grep (`- Edit 1 -- docs/error-codes/00-index.md:` token-class not in known-sites list)
      - V-71-MILESTONES-01 + V-71-ARCHIVE02-01 flip FAIL→PASS at this SHA (closes the chicken-and-egg gap from Plan 71-01)
      - Phase 68 Plan 68-04 `d142c7a` MILESTONES.md-separation precedent (per-plan-separable commit, not bundled with Plan 71-01)
  </behavior>
  <action>
    Run the three pre-commit dry-run commands per RESEARCH Example 10:
    1. `node scripts/validation/check-phase-71.mjs` — expect exit 0; capture stdout showing V-71-MILESTONES-01 + V-71-ARCHIVE02-01 PASS (the green-baseline witness).
    2. PowerShell predecessor-surface loop per PATTERNS.md Shared Pattern 5 — iterate the 11 frozen surfaces (4 workflows + 5 audit-MJS + 1 sidecar JSON + 1 v1.7-audit-allowlist.json) and confirm `git diff <Plan-71-01-SHA> HEAD -- <surface>` returns EMPTY for each. If any surface has non-empty diff → ABORT (manual review required; do NOT commit).
    3. `git diff HEAD -- .planning/MILESTONES.md` — confirm exactly 2 hunks (one per `@@ ` header line).

    If all three pre-commit checks pass, stage and commit:
    `git add .planning/MILESTONES.md`
    `git commit -m "docs(archive): ARCHIVE-02 re-author v1.1+v1.2 MILESTONES.md entries from MILESTONE-AUDIT source (replacement-not-deletion per ARCHIVE-01 doctrine)" -m "<body>"`

    Body content (HEREDOC for multi-line):
    - "v1.1 H2 re-authored from `.planning/milestones/v1.1-MILESTONE-AUDIT.md` (165 lines)"
    - "v1.2 H2 re-authored from `.planning/milestones/v1.2-MILESTONE-AUDIT.md` (345 lines)"
    - "v1.1 H2 carried 1 debris line at line 164 (`- Edit 1 -- docs/error-codes/00-index.md:`) — NEW DISCOVERY by D-03 advisor pre-sweep grep enumeration; NOT in `v1.7-DEFERRED-CLEANUP.md:38-48` known-sites list. Semantic content restored from audit-doc Phases 16-17 (ADMN-06..07 + NAVG-01..04) error-code consolidation rows."
    - "v1.2 H2 carried 4 debris lines at lines 145-148 (3× `- One-liner:` + 1× `- Commit:`) per `v1.7-DEFERRED-CLEANUP.md:38-48` known-sites enumeration."
    - "REPLACEMENT FROM CANONICAL SOURCE (NOT deletion) per `v1.7-DEFERRED-CLEANUP.md:30,32` doctrine — symmetric with v1.6+v1.7 retroactive-authoring at v1.7 close 2026-05-29."
    - "V-71-MILESTONES-01 + V-71-ARCHIVE02-01 flip from transient FAIL at Plan 71-01 SHA → PASS at this SHA (closes the chicken-and-egg gap per Plan 70-05 Commit A `14683de` precedent)."
    - "Plan 71-02 is separately revertable from Plan 71-01 per Phase 68 Plan 68-04 `d142c7a` MILESTONES.md-separation precedent (different epistemic class: root-cause-fix vs. symptom-residue sweep)."

    Capture the resulting SHA via `git log -1 --format=%H` for Plan 71-03's 71-VERIFICATION.md Section D record.

    Do NOT push. Do NOT amend. Do NOT skip hooks.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-71.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Pre-commit `node scripts/validation/check-phase-71.mjs` exits 0 (V-71-MILESTONES-01 + V-71-ARCHIVE02-01 PASS — chicken-and-egg gap closed)
    - Pre-commit predecessor-surface loop: zero modifications to v1.4/v1.4.1/v1.5/v1.6/v1.7 workflow YAMLs + audit-MJS + sidecar JSONs since Plan 71-01 SHA
    - Pre-commit MILESTONES.md `git diff` hunk count = 2 (v1.1 H2 + v1.2 H2 only)
    - `git log --name-only -1 HEAD` returns exactly 1 file: `.planning/MILESTONES.md`
    - `git log -1 --format=%s HEAD` returns the literal subject `docs(archive): ARCHIVE-02 re-author v1.1+v1.2 MILESTONES.md entries from MILESTONE-AUDIT source (replacement-not-deletion per ARCHIVE-01 doctrine)`
    - `git log -1 --format=%b HEAD | grep -c "REPLACEMENT FROM CANONICAL SOURCE"` ≥ 1
    - `git log -1 --format=%b HEAD | grep -c "Plan 68-04"` ≥ 1 (MILESTONES.md-separation precedent cited)
    - `git log -1 --format=%b HEAD | grep -c "Plan 70-05 Commit A"` ≥ 1 (chicken-and-egg resolution cited)
    - `git log -1 --format=%b HEAD | grep -c "v1\.1 line 164"` ≥ 1 (NEW DISCOVERY documented)
    - SHA captured (`$plan7102sha = git log -1 --format=%H`) is a valid 40-char hex string
  </acceptance_criteria>
  <done>
    Plan 71-02 commit lands. V-71-MILESTONES-01 + V-71-ARCHIVE02-01 now PASS post-sweep. v1.4/v1.5/v1.6/v1.7 frozen surfaces byte-unchanged. MILESTONES.md diff is exactly 2 hunks (v1.1 + v1.2). The Plan 71-02 SHA is the green-baseline witness Plan 71-03 records in 71-VERIFICATION.md as the chicken-and-egg-resolution witness for the Plan 71-01 transient.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit-doc content → MILESTONES.md re-authored bullets | Hand-translated narrative; planner-judgment crosses here |
| Edit-tool old_string → MILESTONES.md filesystem | Verbatim-match contract; whitespace drift causes silent failure or wrong target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02-01 | Tampering | Edit-tool old_string captures wrong lines (off-by-one error → edits unintended H2) | mitigate | Pre-edit Read tool returns line numbers verbatim; Task 1 reconnaissance memo captures exact line ranges before Tasks 2-3 invoke Edit. Pitfall 4 explicitly guards: Task 3 post-edit verification asserts `git diff HEAD -- .planning/MILESTONES.md` produces exactly 2 hunks (v1.1 + v1.2 only — no v1.0/v1.3/v1.4+ drift). |
| T-71-02-02 | Tampering | Trailing-whitespace drift in old_string causes Edit silently to fail or partially match | mitigate | Pitfall 3 mitigation: SMALL old_string ranges (4-6 lines, not whole H2 block). If Edit reports `old_string not found`, Tasks 2-3 explicitly REVERT mentally and re-Read the target lines to capture exact whitespace before retrying. |
| T-71-02-03 | Tampering | CRLF/LF drift via PowerShell Set-Content / Out-File | mitigate | Tasks 2-3 EXPLICITLY use the `Edit` tool (NOT `Write`, NOT `Set-Content`, NOT `Out-File`) per Plan 68-04 T-68-04-LE precedent. Edit tool preserves existing line-endings. Anti-pattern called out explicitly in Action sections. |
| T-71-02-04 | Information Disclosure | v1.1 line 164 NEW DISCOVERY surfaces in commit message + Note row (visible in git log + MILESTONES.md HEAD) | accept | This IS the doctrinal documentation contract per "DO NOT mask via deletion" — the discovery and the replacement are deliberately surfaced for future-author awareness. v1.7-DEFERRED-CLEANUP.md:38-48 known-sites list extension is the audit-trail mechanism. |
| T-71-02-05 | Repudiation | Future author cannot verify the re-authored bullets accurately reflect audit-doc content | mitigate | `**Note:** retroactively re-authored 2026-06-03 from canonical source-of-truth v1.X-MILESTONE-AUDIT.md` row in each H2 provides explicit provenance. `git log` on `.planning/milestones/v1.1-MILESTONE-AUDIT.md` + `.planning/milestones/v1.2-MILESTONE-AUDIT.md` provides the canonical source's own audit-trail. Manual-only verification (per VALIDATION.md line 74) covers the subjective "is this bullet faithful to the audit doc?" check. |
| T-71-02-SC | Tampering | npm/pip/cargo installs in this plan | mitigate | **ZERO external packages installed.** This plan modifies one markdown file. No `npm install`, no `package.json` mutation. Package Legitimacy Audit row count = 0. |

**ASVS Level:** L1 (project-internal documentation edits; no auth surface; no PII; no external network).
**Block-on-severity:** high. All listed threats dispositioned mitigate (5) or accept (1); zero high-severity unmitigated.
</threat_model>

<verification>
## Plan 71-02 Overall Verification

After Task 4 commit lands:

1. **MILESTONES.md is debris-free:**
   - `grep -cE "^- (One-liner:|SUBSUMED BY PLAN|Hash:|Pre-edit:|Total file size:|File:|Insertion position:|Single deliverable:|Plan goal:|Found during:|Edit [0-9]+ --)" .planning/MILESTONES.md` returns 0
2. **V-71-MILESTONES-01 PASSES** — `node scripts/validation/check-phase-71.mjs` exits 0 (the validator from Plan 71-01 no longer fails on MILESTONES.md scan).
3. **V-71-ARCHIVE02-01 PASSES** — scoped scan of v1.1 H2 + v1.2 H2 ranges shows zero placeholder bullets.
4. **Predecessor-byte-unchanged invariant preserved:**
   - `git diff <Plan-71-01-SHA> HEAD -- .github/workflows/audit-harness-v1.4-integrity.yml .github/workflows/audit-harness-v1.4.1-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml .github/workflows/audit-harness-v1.7-integrity.yml scripts/validation/v1.4-milestone-audit.mjs scripts/validation/v1.4.1-milestone-audit.mjs scripts/validation/v1.5-milestone-audit.mjs scripts/validation/v1.6-milestone-audit.mjs scripts/validation/v1.7-milestone-audit.mjs scripts/validation/v1.7-audit-allowlist.json` returns EMPTY
5. **Commit isolation:**
   - `git log --name-only -1 HEAD` returns exactly 1 file (`.planning/MILESTONES.md`)
   - `git diff HEAD~1 HEAD -- .planning/MILESTONES.md` shows exactly 2 hunks (v1.1 H2 + v1.2 H2)
6. **No external packages:** `git diff HEAD~1 HEAD -- package.json package-lock.json` returns EMPTY.
</verification>

<success_criteria>
- [ ] `.planning/MILESTONES.md` v1.1 H2 re-authored from `v1.1-MILESTONE-AUDIT.md` (5 bullets covering Phases 11-17; `**Audit status:** tech_debt` + `**Note:** retroactively re-authored 2026-06-03`)
- [ ] `.planning/MILESTONES.md` v1.2 H2 re-authored from `v1.2-MILESTONE-AUDIT.md` (5 bullets covering Phases 20-25; `**Audit status:** gaps_found` + `**Note:** retroactively re-authored 2026-06-03`)
- [ ] `grep -cE "^- (One-liner:|Commit:|Edit [0-9]+ --)" .planning/MILESTONES.md` returns 0 (SC#3 — zero placeholder tokens remain)
- [ ] V-71-MILESTONES-01 PASSES (post-sweep)
- [ ] V-71-ARCHIVE02-01 PASSES (v1.1 + v1.2 H2 scoped scan clean)
- [ ] Predecessor-byte-unchanged invariant preserved (v1.4/v1.5/v1.6/v1.7 surfaces byte-unchanged since Plan 71-01 SHA)
- [ ] `git diff HEAD~1 HEAD -- .planning/MILESTONES.md` shows exactly 2 hunks (no spurious drift)
- [ ] Commit subject contains `replacement-not-deletion per ARCHIVE-01 doctrine`
- [ ] Commit body cites Plan 68-04 `d142c7a` MILESTONES.md-separation precedent + Plan 70-05 Commit A chicken-and-egg resolution + v1.1 line 164 NEW DISCOVERY
</success_criteria>

<output>
Create `.planning/phases/71-archive-automation-root-cause-fix-pillar-a/71-02-SUMMARY.md` when done.

**Critical content for 71-02-SUMMARY.md:**
- `**One-liner:** Re-authored v1.1 + v1.2 H2 entries in .planning/MILESTONES.md from canonical MILESTONE-AUDIT source-of-truth; 5 debris lines (v1.2:145-148 + v1.1:164) REPLACED with restored bullets; V-71-MILESTONES-01 + V-71-ARCHIVE02-01 flipped FAIL→PASS at SHA <SHA>.`
- **H2 "Pre-commit Dry-Run Output"** capturing the `check-phase-71.mjs` PASS output verbatim (the green-baseline witness for the chicken-and-egg resolution).
- **H2 "Predecessor-Byte-Unchanged Verification"** capturing the empty diff output for all 11 frozen surfaces.
- **Closing commit SHA** recorded for Plan 71-03's 71-VERIFICATION.md Section D Atomic-Commit SHA Record table.
</output>
