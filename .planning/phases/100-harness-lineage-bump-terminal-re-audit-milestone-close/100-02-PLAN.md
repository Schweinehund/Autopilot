---
phase: 100-harness-lineage-bump-terminal-re-audit-milestone-close
plan: 02
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - scripts/validation/check-phase-96.mjs
  - scripts/validation/check-phase-97.mjs
  - scripts/validation/check-phase-98.mjs
  - scripts/validation/check-phase-99.mjs
  - scripts/validation/check-phase-100.mjs
  - scripts/validation/_lib/frozen-at-close.mjs
  - .github/workflows/audit-harness-v1.13-integrity.yml
autonomous: true
requirements: [HARN-02]
must_haves:
  truths:
    - "check-phase-96 is a leaf validator (CHAIN_PHASES=[], CHAIN_SKIP=new Set([])) deriving needles INLINE per D-01 (no 96-NEEDLE-SPEC.md) → 13 PASS / 0 FAIL / 0 SKIP, needling the ACC-01 309/319 landing strings (NOT pre-existing line-326), ACC-02, a unique ACC-04 token, the 3 GLOS-01 URLs, the `### Kandji-Iru` slug guard, and the `| VPP |`-removed negative assertion"
    - "check-phase-97 (16 checks), check-phase-98 (14 checks; N4 negative-lookahead `com\\.microsoft\\.CompanyPortalMac(?!\\.ssoextension)`), check-phase-99 (23 checks) are leaf validators hard-asserting the tokens from their NEEDLE-SPEC.md files"
    - "check-phase-100 is chain-apex: CHAIN_PHASES=[48..99] (52 entries, apex EXCLUDES own phase 100), CHAIN_SKIP=new Set([]), V-100-SELF asserts 100 absent + CHAIN_SKIP empty; HARNESS repointed to v1.13-milestone-audit.mjs"
    - "_lib/frozen-at-close.mjs gains V112:'12f2c7b' entry + readAtV112Close export (single entry, no V112_CLOSEGATE)"
    - "audit-harness-v1.13-integrity.yml ships as the 10th CI coexistence workflow with check-phase-96/97/98/99/100 jobs; predecessors v1.4-v1.12 byte-unchanged; the 4 linux-chain contracts intact"
    - "Atom 2 lands as ONE indivisible commit touching EXACTLY 7 files; V112 rides Atom 2; committed AND pushed to origin/master (hard ordering gate for Plan 100-03)"
  artifacts:
    - path: "scripts/validation/check-phase-100.mjs"
      provides: "chain-apex validator CHAIN_PHASES=[48..99]"
      contains: "CHAIN_SKIP = new Set([])"
    - path: "scripts/validation/check-phase-96.mjs"
      provides: "Phase 96 inline-derived needle leaf validator (13 checks)"
      contains: "Kandji-Iru"
    - path: "scripts/validation/_lib/frozen-at-close.mjs"
      provides: "V112 frozen-close pin + readAtV112Close"
      contains: "readAtV112Close"
    - path: ".github/workflows/audit-harness-v1.13-integrity.yml"
      provides: "10th CI coexistence workflow"
      contains: "check-phase-100"
  key_links:
    - from: ".github/workflows/audit-harness-v1.13-integrity.yml"
      to: "scripts/validation/check-phase-100.mjs"
      via: "linux-chain-ubuntu-latest job runs the apex"
      pattern: "check-phase-100"
    - from: "scripts/validation/_lib/frozen-at-close.mjs"
      to: "MILESTONE_CLOSE_SHAS.V112"
      via: "readAtClose('V112', p)"
      pattern: "V112.*12f2c7b"
---

<objective>
Ship Plan 100-02: HARN-02 Atom 2 — the v1.13 per-phase validators (check-phase-96..100) + V112 frozen-close pin + 10th CI coexistence workflow. ONE indivisible commit, EXACTLY 7 files.

Files (all Path-A copies / inline-derived per locked D-01/D-03):
- `check-phase-96.mjs` — leaf validator, needles DERIVED INLINE per D-01 (Phase 96 shipped before the needle-spec convention; NO retroactive 96-NEEDLE-SPEC.md). 13 checks.
- `check-phase-97.mjs` — leaf validator, tokens from `97-NEEDLE-SPEC.md`. 16 checks.
- `check-phase-98.mjs` — leaf validator, tokens from `98-NEEDLE-SPEC.md`; N4 uses negative-lookahead. 14 checks.
- `check-phase-99.mjs` — leaf validator, tokens from `99-NEEDLE-SPEC.md`. 23 checks.
- `check-phase-100.mjs` — chain-apex from `check-phase-95.mjs`, range **[48..99] (52 entries, NOT [48..100])** per D-03, CHAIN_SKIP empty Set, V-100-SELF dual-invariant, HARNESS → v1.13-milestone-audit.mjs.
- `_lib/frozen-at-close.mjs` (EDIT) — add `V112: '12f2c7b'` entry + `readAtV112Close` export. **V112 rides ATOM 2 here, NOT Atom 1** (per ROADMAP SC#2 + HARN-02).
- `audit-harness-v1.13-integrity.yml` — 10th coexistence workflow from `audit-harness-v1.12-integrity.yml`, jobs repointed to check-phase-96/97/98/99/100.

Purpose: Land the complete v1.13 validator surface + frozen pin + CI as one revertable atom. **Ordering-safe:** `12f2c7b` is a known-PAST SHA and check-phase-100 reads only PRIOR-milestone closes via `_lib/frozen-at-close.mjs` → no Commit A.
Output: 5 net-new validators + frozen-at-close edit + CI workflow; ONE commit; pushed to origin/master.

**HARD ordering gate (D-03):** This atom MUST be committed AND pushed to `origin/master` before Plan 100-03 runs — the v1.13 workflow's check-phase-96..100 jobs FAIL (not skip) if absent from the dispatched ref.
**CRITICAL pitfalls (100-RESEARCH §"Common Pitfalls"):** (1) NEVER author CHAIN_PHASES=[48..100] — that self-references phase 100 → V-100-SELF FAIL. (2) NEVER add entries to CHAIN_SKIP — V-SELF hard-asserts size 0 (this was the disqualified GA3-C CRITICAL). (3) NEVER needle the pre-existing line-326 phrase. (4) check-phase-98 N4 needs the negative-lookahead. (5) NO 96-NEEDLE-SPEC.md. (6) V112 must ride the SAME commit as check-phase-100.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-CONTEXT.md
@.planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: check-phase-96 (inline-derived, 13 checks) + check-phase-97 (16 checks)</name>
  <files>scripts/validation/check-phase-96.mjs, scripts/validation/check-phase-97.mjs</files>
  <read_first>
    - scripts/validation/check-phase-94.mjs (PATH-A SOURCE for the leaf shape — CHAIN_PHASES=[], CHAIN_SKIP=new Set([]), {id,file,needle} array + loop, CRLF-normalizing readFile, V-NN-PRESENCE, V-NN-SELF dual-invariant, runner loop)
    - scripts/validation/check-phase-92.mjs (FORM reference for the {id,file,needle} content-needle array)
    - .planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-RESEARCH.md (§"check-phase-96.mjs — Derived Inline per D-01" full assertion table with exact needle strings; §"check-phase-97.mjs" token list)
    - .planning/phases/96-surgical-conflict-fixes/96-CONTEXT.md (D-01 source for the check-phase-96 needles — landed 309/319; line-326 pre-existing; removed VPP row; bare `### Kandji-Iru` slug guard)
    - .planning/phases/97-enrollment-filevault-depth-formalization/97-NEEDLE-SPEC.md (the 16-check token spec for check-phase-97)
    - docs/macos-lifecycle/00-ade-lifecycle.md, docs/l1-runbooks/15-macos-company-portal-sign-in.md, docs/_glossary-macos.md (the check-phase-96 targets — confirm needle strings present before asserting)
    - docs/admin-setup-macos/02-enrollment-profile.md, docs/admin-setup-macos/03-configuration-profiles.md (the check-phase-97 targets)
  </read_first>
  <action>
    Create two leaf validators from the `check-phase-94.mjs` base shape (substitute phase number throughout: id strings, V-NN-PRESENCE / V-NN-SELF names, SELF `.includes(NN)`, header, console label). Both are LEAF: `CHAIN_PHASES = []`, `CHAIN_SKIP = new Set([])` (chain lives ONLY in the apex check-phase-100). CRLF-normalize (`.replace(/\r\n/g, '\n')`) before all needle tests. Hard-assert all checks (NO allowlist/sidecar). Never assert `last_verified`/`review_by`/date strings (time-bombs).

    **check-phase-96.mjs (13 checks; needles DERIVED INLINE per D-01 — NO 96-NEEDLE-SPEC.md):** Build the assertion set EXACTLY per 100-RESEARCH §"check-phase-96.mjs" assertion table:
    - DELIVERABLES: `docs/macos-lifecycle/00-ade-lifecycle.md` (00), `docs/l1-runbooks/15-macos-company-portal-sign-in.md` (15), `docs/_glossary-macos.md` (GLOS).
    - 3 PRESENCE checks (file exists + non-empty for 00 / 15 / GLOS).
    - ACC-01 N1 needle the corrected ~line-309 landing string and N2 the ~line-319 string (use the exact strings in the RESEARCH table). DO NOT needle the pre-existing line-326 "never distributed via Apple VPP" phrase (false-green risk against pre-96 bytes).
    - ACC-02 needle (the "static user group" correction at guide 00 ~line 252 — use the discriminating phrase from the RESEARCH table, not a bare "static").
    - ACC-04 needle: a UNIQUE token from the corrected remediation sentence at runbook 15:30 (per RESEARCH table — not the non-discriminating generic "user group").
    - GLOS-01: three CONTENT needles `support.iru.io`, `support.kandji.io`, `docs.iru.com`.
    - SLUG-GUARD: assert `### Kandji-Iru` heading present (the `#kandji-iru` double-hyphen slug landmine).
    - VPP-ROW-GONE: NEGATIVE assertion — `| VPP |` is ABSENT from guide 00 (assert `!content.includes('| VPP |')`).
    - V-96-SELF dual-invariant.
    Do NOT needle the glossary `#vpp` definition (~15 inbound links). Add a header note explaining WHY content needles are load-bearing (guide 00/15/glossary pre-exist Phase 96 as patches — bare PRESENCE is trivially green on old bytes) and WHY 326 is excluded. Total = 3 PRESENCE + 8 CONTENT + 1 NEGATIVE + 1 SELF = 13 checks.

    **check-phase-97.mjs (16 checks):** Build per `97-NEEDLE-SPEC.md` + 100-RESEARCH §"check-phase-97.mjs": 2 PRESENCE (guide 02 + guide 03) + 13 CONTENT + 1 SELF. Key tokens — guide 02: `Non Platform SSO Accounts`, `Restrict editing`, `Prefill account info`, `{{partialUPN}}`, `{{username}}`; guide 03: `FileVault Options`, `Recovery Key Escrow`, `| Defer |` (pipe-anchored, NOT bare `Defer`), `dontAllowFDEDisable`, `DestroyFVKeyOnStandby`, `Recovery Key Rotation In Months`, `Local Password Policy`, `-2016341107`. Use the COMPLETE token list from 97-NEEDLE-SPEC.md (13 content needles).
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-96.mjs | tail -1 && node scripts/validation/check-phase-97.mjs | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-96.mjs` exits 0 with `13 PASS, 0 FAIL, 0 SKIPPED`; `node scripts/validation/check-phase-97.mjs` exits 0 with `16 PASS, 0 FAIL, 0 SKIPPED`.
    - check-phase-96 contains `### Kandji-Iru`, `support.iru.io`, `support.kandji.io`, `docs.iru.com`, a `| VPP |` negative assertion, `V-96-PRESENCE`, `V-96-SELF` with `.includes(96)`, `CHAIN_PHASES = []`, `CHAIN_SKIP = new Set([])`; it does NOT contain the string `never distributed via Apple VPP` as a needle.
    - check-phase-96 needles the 309/319 ACC-01 landing strings (verbatim from the RESEARCH table) and a unique ACC-04 token; no retroactive `96-NEEDLE-SPEC.md` file was created (`test ! -f .planning/phases/96-surgical-conflict-fixes/96-NEEDLE-SPEC.md`).
    - check-phase-97 contains `| Defer |` (pipe-anchored), `-2016341107`, `Non Platform SSO Accounts`, `FileVault Options`, and `V-97-SELF` with `.includes(97)`; `CHAIN_PHASES = []`.
  </acceptance_criteria>
  <done>check-phase-96 (13/0/0, inline-derived per D-01, 326 excluded, slug + VPP-row guards present) and check-phase-97 (16/0/0) created as leaf validators; no 96-NEEDLE-SPEC.md authored.</done>
</task>

<task type="auto">
  <name>Task 2: check-phase-98 (14 checks, N4 negative-lookahead) + check-phase-99 (23 checks)</name>
  <files>scripts/validation/check-phase-98.mjs, scripts/validation/check-phase-99.mjs</files>
  <read_first>
    - scripts/validation/check-phase-94.mjs (PATH-A SOURCE leaf shape — same as Task 1)
    - .planning/phases/98-guide-07-comprehensive-pass/98-NEEDLE-SPEC.md (the 14-check token spec incl. the N4 negative-lookahead rigor exemplar)
    - .planning/phases/99-new-runbook-navigation-wiring/99-NEEDLE-SPEC.md (the 23-check token spec; N17 count===3, quick-ref-l1 link count>=2)
    - .planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-RESEARCH.md (§"check-phase-98.mjs" + §"check-phase-99.mjs" — token lists, Pitfall 4 N4 substring caution, N9/N10 split guidance from Open Questions)
    - docs/admin-setup-macos/07-platform-sso-setup.md (check-phase-98 target)
    - docs/l1-runbooks/37-macos-local-password-reset.md, docs/l1-runbooks/36-macos-secure-enclave-key.md, docs/l1-runbooks/00-index.md, docs/index.md, docs/common-issues.md, docs/quick-ref-l1.md, docs/decision-trees/06-macos-triage.md (check-phase-99 targets — WRAP committed content; do NOT edit)
  </read_first>
  <action>
    Create two more leaf validators from the `check-phase-94.mjs` base shape (LEAF: `CHAIN_PHASES = []`, `CHAIN_SKIP = new Set([])`; CRLF-normalize; hard-assert; no freshness/date assertions).

    **check-phase-98.mjs (14 checks):** Build per `98-NEEDLE-SPEC.md` + 100-RESEARCH §"check-phase-98.mjs": 1 PRESENCE (guide 07) + 12 CONTENT + 1 SELF. Deliverable `docs/admin-setup-macos/07-platform-sso-setup.md`. Key tokens: `com.microsoft.CompanyPortalMac.ssoextension`, `Enable Registration During Setup`, `5.2604.0`, `Non Platform SSO Accounts`, `AccountShortName`, `Configuration-Caused Failures`, `ADE Path Prerequisites`, `Registration Approach: Decision and Alternatives`, `End-User Sign-In Experience (Secure Enclave)`, `Setup-Assistant SSO-Extension Diagnostic Tree`, `Extension Identifier typo`. **N4 CRITICAL (Pitfall 4):** the bundle-ID-only check uses the negative-lookahead regex `/com\.microsoft\.CompanyPortalMac(?!\.ssoextension)/` to avoid false-matching the `.ssoextension` lines. Use the COMPLETE token list from 98-NEEDLE-SPEC.md.

    **check-phase-99.mjs (23 checks):** Build per `99-NEEDLE-SPEC.md` + 100-RESEARCH §"check-phase-99.mjs": 7 PRESENCE (the 7 deliverable files) + 15 CONTENT + 1 SELF. Key tokens: `audience: L1`, `platform: macOS`, `applies_to: ADE`, `does NOT reset the independent local macOS password`, `](36-macos-secure-enclave-key.md) for the mandatory PSSO re-registration` (count === 3 in runbook 37), `MACR9`, `click MACR9`, `../l1-runbooks/37-macos-local-password-reset.md`, `Runbook 37`, plus the hub-file link tokens. **Count assertions:** N17 must appear exactly 3 times in runbook 37; `](l1-runbooks/37-macos-local-password-reset.md)` must appear >= 2 times in quick-ref-l1.md (use count>=2 per the Open-Questions guidance — simpler). Use the COMPLETE token list from 99-NEEDLE-SPEC.md. Do NOT edit `docs/decision-trees/06-macos-triage.md` (WR-02 already fixed in `5d6ee80`; this validator only WRAPS/asserts committed content).
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-98.mjs | tail -1 && node scripts/validation/check-phase-99.mjs | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-98.mjs` exits 0 with `14 PASS, 0 FAIL, 0 SKIPPED`; `node scripts/validation/check-phase-99.mjs` exits 0 with `23 PASS, 0 FAIL, 0 SKIPPED` (24 acceptable if N9/N10 were structurally split — capture the exact count).
    - check-phase-98 contains the literal negative-lookahead `com\.microsoft\.CompanyPortalMac(?!\.ssoextension)` and `com.microsoft.CompanyPortalMac.ssoextension` and `5.2604.0`; `CHAIN_PHASES = []`; `V-98-SELF` with `.includes(98)`.
    - check-phase-99 contains a count===3 assertion for the runbook-37 re-registration link and a count>=2 assertion for the quick-ref-l1 runbook-37 link; 7 PRESENCE targets; `V-99-SELF` with `.includes(99)`; it does NOT modify `docs/decision-trees/06-macos-triage.md` (`git status` shows that file unmodified).
    - Both validators are LEAF (`CHAIN_PHASES = []`, `CHAIN_SKIP = new Set([])`).
  </acceptance_criteria>
  <done>check-phase-98 (14/0/0, N4 negative-lookahead) and check-phase-99 (23/0/0, count assertions) created as leaf validators; 06-macos-triage.md untouched.</done>
</task>

<task type="auto">
  <name>Task 3: check-phase-100 apex [48..99] + frozen-at-close V112 + 10th CI workflow; commit Atom 2 (7 files) and push</name>
  <files>scripts/validation/check-phase-100.mjs, scripts/validation/_lib/frozen-at-close.mjs, .github/workflows/audit-harness-v1.13-integrity.yml</files>
  <read_first>
    - scripts/validation/check-phase-95.mjs (PATH-A SOURCE for check-phase-100.mjs — chain-apex: HARNESS const, CHAIN_PHASES [48..94], CHAIN_SKIP, V-NN-AUDIT, NESTED child-spawn block + peer-timeout logic, V-NN-AUDIT-HARNESS, V-NN-SELF dual-invariant ~lines 130-143, runner loop)
    - scripts/validation/_lib/frozen-at-close.mjs (the file being MODIFIED — the V111 entry + readAtV111Close export are the mirror templates; last entry currently V111='919b23b')
    - .github/workflows/audit-harness-v1.12-integrity.yml (PATH-A SOURCE for the 10th workflow; job structure + the 4 linux-chain contract values fetch-depth:0 / core.autocrlf false / continue-on-error:false / timeout-minutes:30 + CHAIN_TIMING_LINUX ::notice + path filters + crons)
    - .planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-RESEARCH.md (§"check-phase-95.mjs Template Analysis" CHAIN_PHASES 52-entry array + V-100-SELF + V-100-AUDIT resolveArchivedPhasePath; §"Frozen-at-Close V-Pin Ladder — V112 Entry" exact entry + export; §"CI Workflow — 10th Coexistence File" substitution table + path filters + new jobs + inherited invariants)
  </read_first>
  <action>
    Create the apex validator + frozen pin + CI workflow, then commit all 7 Atom-2 files together and push.

    **check-phase-100.mjs (copy from check-phase-95.mjs):** all `95`→`100` in id/name/header/SELF strings and console label. `HARNESS = 'scripts/validation/v1.13-milestone-audit.mjs'`. `CHAIN_PHASES` = explicit **52-entry** integer array `[48,49,…,99]` (every integer 48 through 99 inclusive — this is `[48..99]`, NOT `[48..100]`; the apex EXCLUDES its own phase per the `[48..N-1]` invariant and the disqualified self-reference pitfall). `CHAIN_SKIP = new Set([])` (verbatim empty-Set invariant — NEVER add entries). V-100-AUDIT → `resolveArchivedPhasePath('100-harness-lineage-bump-terminal-re-audit-milestone-close/100-VERIFICATION.md', ['v1.13-phases'])`, regex `/Phase 100/i` (SKIP-PASS until Plan 100-04 authors 100-VERIFICATION.md). Copy the NESTED child-spawn block (`CHECK_PHASE_NESTED=1`, child env, execFileSync, peer-timeout `phaseNum >= 67` logic) VERBATIM. V-100-AUDIT-HARNESS runs `v1.13-milestone-audit.mjs`. V-100-SELF dual-invariant: `if (CHAIN_PHASES.includes(100))` FAIL + `if (CHAIN_SKIP.size !== 0)` FAIL. Runner loop verbatim. Total = 1 AUDIT + 52 CHAIN + 1 AUDIT-HARNESS + 1 SELF = 55 checks.

    **_lib/frozen-at-close.mjs (EDIT — 2 appends, NO second entry):** After the V111 entry, inside `MILESTONE_CLOSE_SHAS`, add `V112: '12f2c7b',` with a comment mirroring the V111 comment shape (Phase 95 Plan 95-04 — v1.12 milestone close-gate `docs(95-04)`; single entry; no separate V112_CLOSEGATE — full SHA `12f2c7bab511dffb5b3c9dc3e642a4360ef06065`). After the `readAtV111Close` export, add `export const readAtV112Close = (p) => readAtClose('V112', p);` (mirror the V111 export spacing). NO `V112_CLOSEGATE` (v1.12 closed in ONE commit → atom == close-gate). This is the locked divergence — V112 rides Atom 2.

    **audit-harness-v1.13-integrity.yml (copy from audit-harness-v1.12-integrity.yml):** apply the substitution table in 100-RESEARCH §"CI Workflow". All `v1.12`→`v1.13` repoints. Header "9th coexistence" → "10th"; `name:` → `Audit Harness v1.13 Integrity`. `on.pull_request.paths` → `scripts/validation/v1.13-*`, `scripts/validation/check-phase-*.mjs`, `.github/workflows/audit-harness-v1.13-integrity.yml`, `.planning/REQUIREMENTS.md`, `.planning/milestones/v1.13-MILESTONE-AUDIT.md`, `.planning/milestones/v1.13-DEFERRED-CLEANUP.md`. Crons verbatim. `parse` job → validate `v1.13-audit-allowlist.json`. `path-match` job → grep `v1.13-audit-allowlist.json` in `v1.13-milestone-audit.mjs`. `harness-run` → `node scripts/validation/v1.13-milestone-audit.mjs --verbose`. `linux-chain-ubuntu-latest` → run apex `check-phase-100.mjs --verbose`, KEEP all 4 contract values (fetch-depth:0, core.autocrlf false, continue-on-error:false, timeout-minutes:30) + CHAIN_TIMING_LINUX `::notice`. Per-validator standalone jobs: REPLACE the v1.12 check-phase-94/95 jobs with exactly 5 jobs `check-phase-96`, `check-phase-97`, `check-phase-98`, `check-phase-99`, `check-phase-100` (fetch-depth:0, timeout-minutes:15, continue-on-error:false, needs: harness-run, node 20). `rotting-external-quarterly` → carry verbatim (cron-only `if:` guard SKIPs on workflow_dispatch — negative control); repoint sidecar to `v1.13-audit-allowlist.json`. `pin-helper-advisory` → carry verbatim (continue-on-error:true). Keep `markdown-link-check@3.14.2` pinned and `actions/checkout@v4` + `actions/setup-node@v4` pinned.

    Commit all 7 files (check-phase-96/97/98/99 from Tasks 1-2 + check-phase-100 + _lib/frozen-at-close.mjs + audit-harness-v1.13-integrity.yml) as ONE atom with message `feat(100-02): v1.13 validators + V112 pin + CI surface — HARN-02 (atomic SC#1 Atom 2)`, then `git push origin master`. Verify the push reached origin (hard ordering gate for 100-03).
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('scripts/validation/check-phase-100.mjs','utf8'); if(!/CHAIN_PHASES\s*=\s*\[48,/.test(f)||!f.includes(',99]')||!f.includes('new Set([])')||!f.includes('v1.13-milestone-audit.mjs')){console.error('apex shape wrong');process.exit(1)} if(/,100\]/.test(f)){console.error('apex must NOT include 100');process.exit(1)} console.log('apex ok')" && node -e "const f=require('fs').readFileSync('scripts/validation/_lib/frozen-at-close.mjs','utf8'); if(!f.includes('V112:')||!f.includes('12f2c7b')||!f.includes('readAtV112Close')){console.error('frozen V112 missing');process.exit(1)} if(f.includes('V112_CLOSEGATE')){console.error('forbidden V112_CLOSEGATE present');process.exit(1)} console.log('frozen V112 ok')" && node -e "const y=require('fs').readFileSync('.github/workflows/audit-harness-v1.13-integrity.yml','utf8'); ['check-phase-96','check-phase-97','check-phase-98','check-phase-99','check-phase-100','fetch-depth: 0','continue-on-error: false','timeout-minutes: 30','Audit Harness v1.13'].forEach(s=>{if(!y.includes(s)){console.error('workflow missing '+s);process.exit(1)}}); console.log('workflow ok')"</automated>
  </verify>
  <acceptance_criteria>
    - check-phase-100 contains a `CHAIN_PHASES` array covering every integer 48..99 (52 entries) and `CHAIN_SKIP = new Set([])`; it does NOT include 100 (`grep -c ",100]" scripts/validation/check-phase-100.mjs` == 0 for the CHAIN_PHASES literal); V-100-SELF asserts `CHAIN_PHASES.includes(100)` is false AND `CHAIN_SKIP.size === 0`; HARNESS const = `scripts/validation/v1.13-milestone-audit.mjs`. (Running the apex on warm-tree Windows may show deep-nest cascade noise — expected; authoritative count comes from Linux GHA in Plan 100-03.)
    - `_lib/frozen-at-close.mjs` contains `V112:`, `'12f2c7b'`, and `readAtV112Close`; contains NO `V112_CLOSEGATE`; the V111 entry is byte-unchanged.
    - `audit-harness-v1.13-integrity.yml` contains `check-phase-96`..`check-phase-100`, `Audit Harness v1.13`, `fetch-depth: 0`, `continue-on-error: false`, `timeout-minutes: 30`; the standalone-job section references exactly check-phase-96/97/98/99/100 (no check-phase-94/95 jobs).
    - Predecessor byte-unchanged so far: `git diff HEAD~1 HEAD -- .github/workflows/audit-harness-v1.12-integrity.yml scripts/validation/v1.12-milestone-audit.mjs scripts/validation/v1.12-audit-allowlist.json` is EMPTY (no v1.12 surface modified by Atom 2).
    - COMMIT message is exactly: `feat(100-02): v1.13 validators + V112 pin + CI surface — HARN-02 (atomic SC#1 Atom 2)`.
    - The commit touches EXACTLY 7 files: check-phase-96/97/98/99/100.mjs + _lib/frozen-at-close.mjs + audit-harness-v1.13-integrity.yml (`git show --stat HEAD --format=` lists only these 7).
    - Atom 2 is pushed: `git log origin/master --oneline -1` shows the Atom-2 commit (HARD ordering gate for Plan 100-03 satisfied).
  </acceptance_criteria>
  <done>Atom 2 (exactly 7 files) committed and pushed to origin/master; apex CHAIN=[48..99] (52 entries, 100 absent, CHAIN_SKIP empty); V112='12f2c7b' pin present (no V112_CLOSEGATE); 10th workflow with 5 validator jobs + 4 contracts intact; v1.12 surfaces byte-unchanged; ordering gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → GitHub Actions runner | The only boundary: `audit-harness-v1.13-integrity.yml` runs in GitHub-hosted Actions, executing in-repo `.mjs` validators against a pinned ref. No external input, no secrets, no network calls beyond checkout. |

## STRIDE Threat Register

ASVS L1 review found **no high-severity items**. The realistic threat surface is harness-integrity: a validator that passes without proving the deliverable landed, or a CHAIN_SKIP entry masking a regression.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-02-T | Tampering | A false-green validator needling pre-existing bytes (e.g., the line-326 ACC-01 phrase) instead of the Phase-96 landing strings | mitigate | check-phase-96 needles the 309/319 landing strings; acceptance gate asserts the line-326 phrase is NOT used as a needle; needles derived from committed Phase-96 content per D-01. |
| T-100-02-E | Elevation/Integrity | CHAIN_SKIP non-empty masking a real chain regression (the disqualified GA3-C) | mitigate | check-phase-100 V-100-SELF hard-asserts `CHAIN_SKIP.size === 0`; acceptance gate asserts `new Set([])` and apex does not include 100. |
| T-100-02-I | Integrity | A v1.4-v1.12 frozen surface mutated while authoring the 10th workflow | mitigate | Acceptance gate asserts `git diff` of the v1.12 workflow/harness/sidecar is EMPTY; full 29-surface gate runs at close-gate (100-04). |
| T-100-02-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | NO installs — Path-A copies of in-tree scripts; workflow uses pinned `actions/checkout@v4` + `actions/setup-node@v4` + node only. No Package Legitimacy Gate required. |
</threat_model>

<verification>
- check-phase-96 → 13/0/0 (inline-derived, 326 excluded, slug + VPP-row guards); check-phase-97 → 16/0/0; check-phase-98 → 14/0/0 (N4 negative-lookahead); check-phase-99 → 23/0/0 (count assertions).
- check-phase-100 CHAIN_PHASES=[48..99] (52 entries) + CHAIN_SKIP empty + HARNESS→v1.13; 100 absent.
- frozen-at-close: V112='12f2c7b' + readAtV112Close, single entry, no V112_CLOSEGATE.
- 10th workflow: check-phase-96..100 jobs + 4 contract values intact; v1.12 surfaces byte-unchanged.
- Atom 2 = exactly 7 files, one commit, pushed to origin/master.
</verification>

<success_criteria>
- ROADMAP SC#2 satisfied: Atom 2 ships as ONE indivisible commit — check-phase-96..100 + frozen-at-close V112 (`12f2c7b`) + audit-harness-v1.13-integrity.yml (10th); predecessors v1.4-v1.12 byte-unchanged.
- D-03 honored: apex CHAIN_PHASES=[48..99] (52 entries, NOT [48..100]); CHAIN_SKIP empty Set.
- V112 rides Atom 2 (locked divergence); no Commit A; ordering-safe.
- Atom 2 pushed to origin/master — hard precondition for Plan 100-03 met.
</success_criteria>

<output>
Create `.planning/phases/100-harness-lineage-bump-terminal-re-audit-milestone-close/100-02-SUMMARY.md` when done. Record: the Atom-2 COMMIT SHA, origin/master push confirmation, the 4 leaf-validator local counts (96:13/0/0, 97:16/0/0, 98:14/0/0, 99:23-or-24/0/0), the apex CHAIN_PHASES=[48..99] confirmation, and a note that the apex-100 + continuity-95 chain counts are deferred to Linux GHA (Plan 100-03, sole-authoritative — both cascade on cold Windows).
</output>
</content>
