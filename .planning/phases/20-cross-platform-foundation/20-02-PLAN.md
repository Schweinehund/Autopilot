---
phase: 20-cross-platform-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_glossary-macos.md
  - docs/_glossary.md
  - docs/_templates/admin-template-macos.md
autonomous: true
requirements: [XPLAT-02, XPLAT-03]

must_haves:
  truths:
    - "Admin can look up any macOS-specific term (ADE, ABM, Setup Assistant, Await Configuration, VPP, ABM token) in a dedicated macOS glossary"
    - "Each macOS glossary entry has a bidirectional cross-reference to its Windows equivalent in _glossary.md"
    - "Windows glossary has a header banner directing users to the macOS glossary"
    - "macOS admin template has dual-portal step sections (ABM + Intune admin center) per D-12"
    - "macOS admin template has a Portal column in its Configuration-Caused Failures table per D-14"
  artifacts:
    - path: "docs/_glossary-macos.md"
      provides: "macOS provisioning glossary with 6+ terms and bidirectional cross-references"
      contains: "Windows equivalent"
    - path: "docs/_glossary.md"
      provides: "Updated Windows glossary with banner linking to macOS glossary"
      contains: "_glossary-macos.md"
    - path: "docs/_templates/admin-template-macos.md"
      provides: "macOS admin guide template with dual-portal steps and Portal column in failures table"
      contains: "In Apple Business Manager"
  key_links:
    - from: "docs/_glossary-macos.md"
      to: "docs/_glossary.md"
      via: "Bidirectional cross-reference links"
      pattern: "_glossary\\.md#"
    - from: "docs/_glossary.md"
      to: "docs/_glossary-macos.md"
      via: "Header banner link"
      pattern: "_glossary-macos\\.md"
    - from: "docs/_templates/admin-template-macos.md"
      to: "docs/_glossary-macos.md"
      via: "Template references macOS glossary for terminology links"
      pattern: "_glossary-macos\\.md"
---

<objective>
Create the macOS provisioning glossary (XPLAT-02) and the macOS admin guide template (XPLAT-03). The glossary mirrors the existing Windows glossary structure with macOS-specific terms and bidirectional cross-references. The admin template adapts the Windows admin template for macOS dual-portal workflows (ABM + Intune admin center).

Purpose: Provide a terminology reference for macOS provisioning that cross-links with existing Windows terminology, and a template that ensures consistent macOS admin guides with portal-scoped steps and cross-portal failure documentation.
Output: `docs/_glossary-macos.md` (new), updated `docs/_glossary.md` (banner only), `docs/_templates/admin-template-macos.md` (new)
</objective>

<threat_model>
This plan creates and modifies Markdown documentation files only. No code execution, no user input processing, no external service integration.
- **Threat:** Broken cross-reference links between glossaries if anchor names don't match
  - **Severity:** Low
  - **Mitigation:** Anchor names derived from heading text following standard Markdown conventions. Both files in same `docs/` directory, so relative paths are simple filenames.
- **Threat:** macOS terminology inaccuracy
  - **Severity:** Low
  - **Mitigation:** All terms verified against Microsoft Learn and Apple documentation in 20-RESEARCH.md with HIGH confidence ratings.
No high-severity threats identified.
</threat_model>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cross-platform-foundation/20-CONTEXT.md
@.planning/phases/20-cross-platform-foundation/20-RESEARCH.md

<interfaces>
<!-- Existing Windows glossary structure (from docs/_glossary.md): -->
```markdown
---
last_verified: 2026-04-13
review_by: 2026-07-12
applies_to: both
audience: both
---

> **Framework coverage:** This glossary covers terminology for both Windows Autopilot (classic/APv1) and Autopilot Device Preparation (APv2).
> Terms specific to one framework are labeled. See [APv1 vs APv2](apv1-vs-apv2.md) for framework selection.

# Autopilot Glossary

## Alphabetical Index

[APv1](#apv1) | [APv2](#apv2) | [Autopilot Reset](#autopilot-reset) | ...

---

## Enrollment

### OOBE
Out-of-Box Experience -- ...

### ESP
Enrollment Status Page -- ...
> **APv2 note:** APv2 does not use ESP. See [Enrollment Time Grouping (ETG)](#enrollment-time-grouping-etg)...

---
## Hardware
### Hardware hash
...
---
## Network
...
---
## Security
...
---
## Deployment Modes
...
```

<!-- Existing admin template structure (from docs/_templates/admin-template.md): -->
```markdown
<!-- ADMIN SETUP GUIDE TEMPLATE ...
     Rules:
     - Every configurable setting MUST have a "What breaks if misconfigured" callout
     - Use imperative voice for steps
     - Include full Intune portal navigation paths
     Reviewer: Intune Admin Lead
-->
---
last_verified: YYYY-MM-DD
review_by: YYYY-MM-DD
applies_to: APv1 | APv2 | both
audience: admin
---

> **Version gate:** [This guide covers Windows Autopilot (classic) | ...]

# [Admin Task Title]

## Prerequisites
## Steps
### Step 1: [Configuration action]
  1. Navigate to **Intune admin center** > [full portal path].
  > **What breaks if misconfigured:** [Consequence]

## Verification
## Configuration-Caused Failures
| Misconfiguration | Symptom | Runbook |
## See Also
```

<!-- Windows glossary cross-reference callout pattern: -->
```markdown
> **APv2 note:** APv2 does not use ESP. See [Enrollment Time Grouping (ETG)](#enrollment-time-grouping-etg) for the APv2 equivalent mechanism.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create macOS provisioning glossary and add banner to Windows glossary (XPLAT-02)</name>
  <files>docs/_glossary-macos.md, docs/_glossary.md</files>
  <read_first>docs/_glossary.md, .planning/phases/20-cross-platform-foundation/20-RESEARCH.md</read_first>
  <action>
**Part A: Create `docs/_glossary-macos.md`**

Create a new file mirroring the exact structural pattern of `docs/_glossary.md` (per D-06). The file contains macOS-specific provisioning terms with bidirectional cross-references to Windows equivalents.

**Frontmatter** (per D-19):
```yaml
---
last_verified: 2026-04-13
review_by: 2026-07-12
applies_to: both
audience: all
platform: all
---
```

**Banner** (per D-05, directs to Windows glossary):
```markdown
> **Platform coverage:** This glossary covers macOS-specific provisioning and management terminology.
> For Windows Autopilot terminology, see the [Windows Autopilot Glossary](_glossary.md).
```

**H1 title:** `# macOS Provisioning Glossary`

**Alphabetical Index** (pipe-delimited, same pattern as Windows glossary line 15):
```markdown
## Alphabetical Index

[ABM](#abm) | [ABM Token](#abm-token) | [ADE](#ade) | [Await Configuration](#await-configuration) | [Setup Assistant](#setup-assistant) | [VPP](#vpp)
```

Followed by a `---` horizontal rule.

**Semantic Categories** with term entries. Organize into categories that map to the macOS domain. Use these categories:

**`## Enrollment`** (contains: ADE, Await Configuration, Setup Assistant)

**`## Device Management`** (contains: ABM, ABM Token)

**`## App Distribution`** (contains: VPP)

Each term entry follows this exact pattern (mirroring the Windows glossary's structure):
```markdown
### [Term Name]

[Definition -- 2-3 sentences from RESEARCH.md terminology table. Use the exact definitions verified in the research.]

> **Windows equivalent:** [Windows term](_glossary.md#anchor) -- [brief explanation of the equivalence or difference].
```

**Exact term definitions to use** (from RESEARCH.md "Core macOS Terms" table):

1. **ADE** (under Enrollment):
   ```
   ### ADE

   Automated Device Enrollment -- Apple's zero-touch enrollment mechanism for organization-owned macOS (and iOS/iPadOS) devices through Apple Business Manager. Devices assigned to an MDM server in ABM enroll automatically during Setup Assistant. Formerly known as DEP (Device Enrollment Program).

   > **Windows equivalent:** [Windows Autopilot](_glossary.md#apv1) -- APv1 uses hardware hash registration and profile assignment; APv2 uses [Enrollment Time Grouping](_glossary.md#enrollment-time-grouping-etg). Both achieve zero-touch enrollment but use different identity mechanisms (serial number via ABM vs hardware hash upload to Intune).
   ```

2. **Await Configuration** (under Enrollment):
   ```
   ### Await Configuration

   A setting in the macOS ADE enrollment profile (officially "Await final configuration") that locks the device at the end of Setup Assistant until Intune confirms critical configuration policies are installed. The user sees an "Awaiting final configuration" screen. Default for new enrollment profiles since late 2024.

   > **Windows equivalent:** [ESP](_glossary.md#esp) (Enrollment Status Page) -- both block the user from accessing the desktop until policies and apps are applied. Key difference: ESP has explicit device-phase and user-phase stages, while Await Configuration is a single hold point at the end of Setup Assistant.
   ```

3. **Setup Assistant** (under Enrollment):
   ```
   ### Setup Assistant

   The macOS first-run configuration experience presented after power-on or device wipe. Screens are customizable (show or hide) via the ADE enrollment profile in Intune. Includes Apple-specific screens such as Apple ID, FileVault, Siri, and Privacy.

   > **Windows equivalent:** [OOBE](_glossary.md#oobe) (Out-of-Box Experience) -- both are the first-run setup flow on their respective platforms. OOBE has Autopilot-specific branding and Entra credential entry; Setup Assistant has Apple-specific screens and supports both modern (Entra) and legacy authentication.
   ```

4. **ABM** (under Device Management):
   ```
   ### ABM

   Apple Business Manager -- Apple's web portal for managing device enrollment, app distribution (Apps and Books), and Managed Apple IDs for organizations. ABM is the single Apple-side portal for all device management administration. Accessed at [business.apple.com](https://business.apple.com).

   > **Windows equivalent:** No direct single equivalent. Device enrollment is managed in the [Intune admin center](https://intune.microsoft.com) under Devices > Windows > Enrollment. ABM is Apple-side while Intune is Microsoft-side; macOS admins work across both portals.
   ```

5. **ABM Token** (under Device Management):
   ```
   ### ABM Token

   The server token (.p7m file) downloaded from Apple Business Manager that enables communication between Intune and ABM for device sync and enrollment profile deployment. Also called "enrollment program token" or "ADE token" in Microsoft documentation. Must be renewed annually -- a lapsed token silently stops new device syncing.

   > **Windows equivalent:** No equivalent. Windows Autopilot uses Graph API and direct Intune integration without a separate token file. The annual renewal lifecycle of the ABM token is a macOS-unique operational concern.
   ```

6. **VPP** (under App Distribution):
   ```
   ### VPP

   Volume Purchase Program -- Apple's bulk app licensing mechanism, now branded as "Apps and Books" within Apple Business Manager. Licenses are purchased in ABM and synced to Intune via a location token. Enables silent app installation on supervised devices without Apple ID sign-in on the device.

   > **Windows equivalent:** Microsoft Store for Business (deprecated) and Intune app deployment. Windows uses Win32 app packaging (.intunewin), MSI, MSIX, and Microsoft Store apps. The VPP/Apps and Books licensing model has no direct Windows equivalent.
   ```

End the file with a `---` horizontal rule and a Version History table:
```markdown
## Version History

| Date | Change | Author |
|------|--------|--------|
| 2026-04-13 | Initial version -- 6 macOS terms with Windows cross-references | -- |
```

**Part B: Add banner to `docs/_glossary.md`**

In the existing `docs/_glossary.md`, add a single line to the existing banner blockquote (currently lines 8-9). Insert a new line after line 9 (the "See [APv1 vs APv2]..." line):

Current banner (lines 8-9):
```markdown
> **Framework coverage:** This glossary covers terminology for both Windows Autopilot (classic/APv1) and Autopilot Device Preparation (APv2).
> Terms specific to one framework are labeled. See [APv1 vs APv2](apv1-vs-apv2.md) for framework selection.
```

Change to (add third line):
```markdown
> **Framework coverage:** This glossary covers terminology for both Windows Autopilot (classic/APv1) and Autopilot Device Preparation (APv2).
> Terms specific to one framework are labeled. See [APv1 vs APv2](apv1-vs-apv2.md) for framework selection.
> For macOS provisioning terminology (ADE, ABM, Setup Assistant), see the [macOS Provisioning Glossary](_glossary-macos.md).
```

No other changes to `_glossary.md`. Do NOT modify any term entries, frontmatter, or other content.
  </action>
  <verify>
    <automated>grep -c "Windows equivalent" docs/_glossary-macos.md &amp;&amp; grep -c "_glossary-macos.md" docs/_glossary.md</automated>
  </verify>
  <acceptance_criteria>
- `docs/_glossary-macos.md` exists with valid YAML frontmatter containing `platform: all`
- File contains `# macOS Provisioning Glossary` as H1
- File contains `## Alphabetical Index` with pipe-delimited links to all 6 terms
- File contains exactly 6 term headings: `### ADE`, `### ABM`, `### ABM Token`, `### Await Configuration`, `### Setup Assistant`, `### VPP`
- Each term entry contains a `> **Windows equivalent:**` callout with a link to `_glossary.md#` (6 callouts total)
- ADE entry contains "Formerly known as DEP" (per Pitfall 4 in research)
- ABM Token entry contains "renewed annually" or "annual renewal" (per Pitfall 5 in research)
- Await Configuration entry explicitly states "single hold point" to differentiate from ESP's two phases (per Pitfall 3 in research)
- File contains `## Enrollment`, `## Device Management`, `## App Distribution` category headings
- Banner blockquote links to `_glossary.md` (the Windows glossary)
- `docs/_glossary.md` line 10 (new third banner line) contains `_glossary-macos.md`
- `docs/_glossary.md` has no changes other than the single added banner line
  </acceptance_criteria>
  <done>macOS glossary exists with 6 terms, bidirectional cross-references to Windows glossary, and correct terminology (ADE not DEP, ABM token renewal noted, Await Configuration vs ESP difference explicit). Windows glossary has a banner line linking to macOS glossary.</done>
</task>

<task type="auto">
  <name>Task 2: Create macOS admin guide template (XPLAT-03)</name>
  <files>docs/_templates/admin-template-macos.md</files>
  <read_first>docs/_templates/admin-template.md, .planning/phases/20-cross-platform-foundation/20-CONTEXT.md</read_first>
  <action>
Create `docs/_templates/admin-template-macos.md` as a separate file adapted from the Windows admin template (per D-11). This template is used by all macOS admin setup guides in Phase 23+.

**Comment block** (adapted from Windows template per D-15):
```markdown
<!-- MACOS ADMIN SETUP GUIDE TEMPLATE
     Usage: Copy this file as your starting point for any macOS admin configuration guide.
     Rules:
     - Fill in all YYYY-MM-DD dates (review_by = last_verified + 90 days)
     - Set platform to macOS (this template is macOS-specific)
     - Replace all [bracketed placeholders] with actual content
     - Every configurable setting MUST have a "What breaks if misconfigured" callout
       specifying which portal the misconfiguration occurs in AND where the symptom manifests
     - Use imperative voice for steps ("Navigate to...", "Select...", "Enter...")
     - Include full navigation paths for both ABM and Intune admin center portals
     - Steps that span both portals MUST use #### In Apple Business Manager and
       #### In Intune admin center sub-sections
     - Runbook links use [TBD - Phase 24] until macOS troubleshooting runbooks exist
     - Include Renewal/Maintenance section ONLY when the guide's subject has a
       renewable component (e.g., ADE token, APNs certificate). Omit otherwise.
     Reviewer: macOS Platform Lead (role, not person name)
-->
```

**Frontmatter:**
```yaml
---
last_verified: YYYY-MM-DD
review_by: YYYY-MM-DD
audience: admin
platform: macOS
---
```

Note: No `applies_to:` field. Per D-20, `applies_to` is for Windows framework disambiguation (APv1/APv2/both) which does not apply to macOS docs. Omitting it avoids confusion.

**Version gate** (per D-16):
```markdown
> **Platform gate:** This guide covers macOS ADE configuration via Apple Business Manager and Intune.
> For Windows Autopilot setup, see [Windows Admin Setup Guides](../admin-setup-apv1/00-overview.md).
> For macOS provisioning terminology, see the [macOS Glossary](../_glossary-macos.md).
```

**H1:** `# [Admin Task Title]`

**Prerequisites section:**
```markdown
## Prerequisites

- [Required admin role -- e.g., Intune Administrator, ABM Device Manager]
- [Required license or subscription -- e.g., Microsoft Intune Plan 1, Apple Business Manager account]
- [Prior configuration that must be completed first -- link to that guide]
- [Required portal access -- specify both ABM and Intune admin center if applicable]
```

**Steps section** (per D-12, dual-portal sub-sections):
```markdown
## Steps

### Step 1: [Configuration action]

#### In Apple Business Manager

1. Sign in to [Apple Business Manager](https://business.apple.com).
2. Navigate to **[section]** > **[sub-section]**.
3. [Action].

   > **What breaks if misconfigured:** [Consequence. Symptom appears in: {specify portal where symptom manifests, which may differ from the portal where the misconfiguration occurs}.]
   > See: [Troubleshooting Runbook Title](TBD - Phase 24)

#### In Intune admin center

1. Sign in to [Intune admin center](https://intune.microsoft.com).
2. Navigate to **Devices** > **Enrollment** > **Apple** > **[path]**.
3. [Action].

   > **What breaks if misconfigured:** [Consequence. Symptom appears in: {specify portal}.]
   > See: [Troubleshooting Runbook Title](TBD - Phase 24)

### Step 2: [Next configuration action]

[Repeat portal-scoped sub-sections as needed. Not every step requires both portals -- use only the sub-sections that apply to the step's actions.]
```

**Verification section:**
```markdown
## Verification

- [ ] [How to confirm the configuration is correct in ABM -- specific ABM page to check, expected state]
- [ ] [How to confirm the configuration is correct in Intune -- specific Intune portal location, expected state]
```

**Configuration-Caused Failures table** (per D-14, includes Portal column):
```markdown
## Configuration-Caused Failures

| Misconfiguration | Portal | Symptom | Runbook |
|------------------|--------|---------|---------|
| [Setting X set to wrong value] | [ABM or Intune] | [What admin or user sees, and in which portal the symptom manifests] | [TBD - Phase 24] |
| [Setting Y missing] | [ABM or Intune] | [What happens] | [TBD - Phase 24] |
```

**Renewal/Maintenance section** (per D-18, conditional -- include template with authoring note):
```markdown
<!-- Include this section ONLY if the guide's subject has a renewable component
     (e.g., ADE token, APNs certificate). Delete this entire section if not applicable. -->

## Renewal / Maintenance

| Component | Renewal Period | Consequence of Lapse | Renewal Steps |
|-----------|---------------|---------------------|---------------|
| [e.g., ADE token] | [e.g., 365 days] | [What stops working] | [Brief steps or link to renewal procedure] |
```

**See Also section:**
```markdown
## See Also

- [Related macOS admin guide](link)
- [macOS ADE Lifecycle Overview](link)
- [Windows vs macOS Concept Comparison](../windows-vs-macos.md)
- [macOS Provisioning Glossary](../_glossary-macos.md)
```
  </action>
  <verify>
    <automated>grep -c "In Apple Business Manager\|In Intune admin center\|Portal" docs/_templates/admin-template-macos.md</automated>
  </verify>
  <acceptance_criteria>
- `docs/_templates/admin-template-macos.md` exists
- File starts with a comment block containing `MACOS ADMIN SETUP GUIDE TEMPLATE`
- Comment block contains "Include full navigation paths for both ABM and Intune admin center portals" (per D-15)
- Comment block contains "Reviewer: macOS Platform Lead" (per D-15)
- Comment block contains "Runbook links use [TBD - Phase 24]" (per D-17)
- Comment block contains "Include Renewal/Maintenance section ONLY when" (per D-18)
- YAML frontmatter contains `platform: macOS` and `audience: admin`
- YAML frontmatter does NOT contain `applies_to:` (per D-20, inapplicable for macOS)
- Version gate blockquote contains "macOS ADE" and links to Windows admin setup guides (per D-16)
- Steps section contains `#### In Apple Business Manager` sub-heading (per D-12)
- Steps section contains `#### In Intune admin center` sub-heading (per D-12)
- "What breaks" callouts mention "Symptom appears in:" for cross-portal visibility (per D-13)
- Configuration-Caused Failures table has 4 columns: Misconfiguration, Portal, Symptom, Runbook (per D-14)
- Renewal/Maintenance section exists (conditionally, with HTML comment authoring instruction per D-18)
- See Also section links to `../windows-vs-macos.md` and `../_glossary-macos.md`
  </acceptance_criteria>
  <done>macOS admin template exists with dual-portal step sub-sections, cross-portal "what breaks" callouts, Portal column in failures table, conditional Renewal/Maintenance section, and all D-11 through D-18 decisions implemented.</done>
</task>

</tasks>

<verification>
1. `grep -c "Windows equivalent" docs/_glossary-macos.md` returns 6 (one per term)
2. `grep "### ADE\|### ABM\b\|### ABM Token\|### Await Configuration\|### Setup Assistant\|### VPP" docs/_glossary-macos.md` returns 6 lines
3. `grep "_glossary-macos.md" docs/_glossary.md` returns at least 1 match (the banner line)
4. `grep -c "In Apple Business Manager" docs/_templates/admin-template-macos.md` returns at least 1
5. `grep -c "In Intune admin center" docs/_templates/admin-template-macos.md` returns at least 1
6. `grep "| Portal |" docs/_templates/admin-template-macos.md` returns at least 1 (the 4-column table header)
7. `grep "platform: macOS" docs/_templates/admin-template-macos.md` returns 1 match
8. `grep "platform: all" docs/_glossary-macos.md` returns 1 match
</verification>

<success_criteria>
- macOS glossary has 6 terms with definitions matching RESEARCH.md and bidirectional Windows cross-references
- Windows glossary has a single new banner line linking to macOS glossary (no other changes)
- macOS admin template has dual-portal step structure, cross-portal failure callouts, and Portal column in failures table
- All decisions D-04 through D-18 are implemented as specified
</success_criteria>

<output>
After completion, create `.planning/phases/20-cross-platform-foundation/20-02-SUMMARY.md`
</output>
