---
phase: 114-eee-standard-templates-doc-id-registry-metadata-rules
plan: 03
type: execute
wave: 3
depends_on: ["114-02"]
files_modified:
  - docs/_templates/admin-template.md
  - docs/_templates/l1-template.md
  - docs/_templates/l2-template.md
  - docs/_templates/admin-template-android.md
  - docs/_templates/admin-template-ios.md
  - docs/_templates/admin-template-macos.md
  - docs/_templates/reference-template.md
autonomous: true
requirements: [STD-02]
must_haves:
  truths:
    - "Every docs/_templates/*.md produces an EEE-conformant new doc: 4 new frontmatter keys, single-line header block, ## Summary first, Status: Draft default"
    - "No template carries an unmappable pipe-list platform: value — all resolve in the D1 map (so C17 exits 0 on all templates at Phase 115 SC3)"
    - "A new reference-template.md exists with the Phase-118 table-remediation convention"
  artifacts:
    - path: "docs/_templates/reference-template.md"
      provides: "Reference-class template with table-remediation (>25-row prose-summary) guidance"
      contains: "doc_type: Reference"
      min_lines: 25
    - path: "docs/_templates/admin-template.md"
      provides: "Admin template with D-07 platform fix + 4 EEE keys + block + Summary"
      contains: "platform: all"
  key_links:
    - from: "docs/_templates/*.md platform: value"
      to: "EEE-SOP-standard.md D1 map"
      via: "mappable platform value (all / Android / iOS / macOS)"
      pattern: "platform:\\s*(all|Android|iOS|macOS)"
    - from: "docs/_templates/*.md header block"
      to: "EEE-SOP-standard.md D-05 block spec"
      via: "Platform · Doc Type · Doc ID · Status"
      pattern: "Platform.*Doc Type.*Doc ID.*Status"
---

<objective>
Make every template in `docs/_templates/` born-EEE-conformant so all new docs are correct out of the box AND so C17 exits 0 on all templates at Phase 115 SC3. For all 6 existing templates: add the 4 new frontmatter keys (`doc_id`, `status`, `owner`, `doc_type`), add the single-line EEE header block, add `## Summary` as the first H2, normalize `last_verified` to the TEMPLATE-SENTINEL, and default `status: Draft`. For the 3 pipe-list templates (admin, l1, l2): apply the D-07 fix (`platform: all` + HTML comment) so their platform value resolves in the D1 map. Add one NEW `reference-template.md` (D-06) carrying the Phase-118 table-remediation convention. The RCA template stays deferred (D-06).

Purpose: New docs must be born conformant; templates are the second-order enforcement of the standard, and Phase 115 SC3 requires C17 green on every template.
Output: 6 modified templates + 1 new `reference-template.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-PATTERNS.md
@.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-RESEARCH.md
@docs/_standards/EEE-SOP-standard.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix + conform the 3 pipe-list templates (admin, l1, l2) — D-07 + 4 keys + block + Summary</name>
  <read_first>
    - docs/_standards/EEE-SOP-standard.md (§Visible Header Block Format, §D1 map, §Required Frontmatter Schema — the authoritative block/key spec)
    - docs/_templates/admin-template.md (current pipe-list frontmatter + version-gate blockquote position)
    - docs/_templates/l1-template.md (current pipe-list frontmatter)
    - docs/_templates/l2-template.md (current pipe-list frontmatter)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-PATTERNS.md (§admin-template.md, §l1-template.md, §l2-template.md — exact target frontmatter and block; §D-07 Pipe-List Fix; §TEMPLATE-SENTINEL Pattern)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-RESEARCH.md (§Template Changes — the per-template delta and version-gate relocation)
  </read_first>
  <action>
    For each of `admin-template.md`, `l1-template.md`, `l2-template.md`: (1) Insert the 4 new frontmatter keys in order immediately before the existing `platform:` line — `doc_id: RE-[FILL-IN]`, `status: Draft`, `owner: [FILL-IN]`, `doc_type: [Guide|Runbook per template — admin=Guide, l1=Runbook, l2=Runbook]`. (2) D-07: replace the pipe-list `platform: Windows | macOS | ... | all` with `platform: all` followed by the HTML comment `<!-- choose: Windows|macOS|iOS|Android|Linux|all -->`. (3) Normalize `last_verified:` to `1970-01-01 # TEMPLATE-SENTINEL` (so freshness validators skip templates). (4) Add the EEE block line immediately after the frontmatter `---` close: `**Platform:** All Platforms · **Doc Type:** [Guide|Runbook] · **Doc ID:** RE-[NNN] · **Status:** Draft` (Platform label is `All Platforms` because `platform: all` resolves there via the D1 map — C17 assertion #9). (5) Add `## Summary` as the FIRST H2 immediately after the H1 with a placeholder instructing ≥30 words of scope/audience/safety (for l1/l2 runbooks: lead with the one-line read-only-scope / escalation-guardrail banner). (6) Relocate the existing `> **Version gate:**` blockquote from between frontmatter and H1 to AFTER `## Summary` (D3-A structure: frontmatter → block → H1 → ## Summary → gate blockquote → sections). Keep all other existing template body content intact. Do NOT fill in real Doc IDs (those come from the registry at doc-creation time).
  </action>
  <verify>
    <automated>grep -L "platform: all" docs/_templates/admin-template.md docs/_templates/l1-template.md docs/_templates/l2-template.md; grep -c "|" docs/_templates/admin-template.md docs/_templates/l1-template.md docs/_templates/l2-template.md | grep "platform" ; for f in admin l1 l2; do grep -q "^doc_id:" docs/_templates/${f}-template.md && grep -q "^status: Draft" docs/_templates/${f}-template.md && grep -Fq "**Doc ID:**" docs/_templates/${f}-template.md && echo "${f} OK"; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 templates has `platform: all` and NO pipe-list `|` in its `platform:` frontmatter line (`grep "^platform:" ...` shows no `|`).
    - Each has all 4 new keys: `doc_id`, `status: Draft`, `owner`, `doc_type` (admin→Guide, l1→Runbook, l2→Runbook).
    - Each has the single-line EEE block `**Platform:** All Platforms · **Doc Type:** ... · **Doc ID:** RE-[NNN] · **Status:** Draft` after the frontmatter close, and `## Summary` as the first H2.
    - `last_verified` is `1970-01-01 # TEMPLATE-SENTINEL` in all 3.
    - The `> **Version gate:**` blockquote is positioned AFTER `## Summary`, not before the H1.
  </acceptance_criteria>
  <done>The 3 pipe-list templates are EEE-conformant and their platform value resolves in the D1 map; C17 assertion #10 will pass on them at Phase 115.</done>
</task>

<task type="auto">
  <name>Task 2: Conform the 3 concrete-platform templates + author the new reference-template.md</name>
  <read_first>
    - docs/_standards/EEE-SOP-standard.md (block spec, D1 map, taxonomy — reference-template uses doc_type: Reference)
    - docs/_templates/admin-template-android.md (analog: the most complete template — HTML comment header, TEMPLATE-SENTINEL, concrete platform value)
    - docs/_templates/admin-template-ios.md (current frontmatter — platform: iOS)
    - docs/_templates/admin-template-macos.md (current frontmatter — platform: macOS)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-PATTERNS.md (§admin-template-android/ios/macos deltas; §reference-template.md NEW — HTML comment block, frontmatter, table convention)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-RESEARCH.md (§New Reference Template — table-remediation rule for Phase 118)
  </read_first>
  <action>
    Part A — for each of `admin-template-android.md`, `admin-template-ios.md`, `admin-template-macos.md`: insert the 4 new keys in order before the existing `platform:` line — `doc_id: RE-[FILL-IN]`, `status: Draft`, `owner: [FILL-IN]`, `doc_type: Guide`; leave the concrete `platform:` value UNCHANGED (`Android`/`iOS`/`macOS` — already mappable, NO D-07 fix); normalize `last_verified` to `1970-01-01 # TEMPLATE-SENTINEL` if not already; add the EEE block line after the frontmatter close showing the concrete label (`**Platform:** Android · **Doc Type:** Guide · **Doc ID:** RE-[NNN] · **Status:** Draft`, and iOS/macOS respectively); add `## Summary` as the first H2 (≥30 words placeholder). Part B — create `docs/_templates/reference-template.md` (D-06, mirroring the admin-template-android.md HTML-comment-guidance shape): an HTML comment header stating usage + the Phase-118 rule "any table >25 rows requires a prose summary within 5 lines of the closing delimiter" + "no Mermaid fences"; frontmatter `last_verified: 1970-01-01 # TEMPLATE-SENTINEL`, `review_by: YYYY-MM-DD`, `doc_id: RE-[FILL-IN]`, `status: Draft`, `owner: [FILL-IN]`, `doc_type: Reference`, `platform: all` + HTML comment `<!-- choose from D1 map: Windows|macOS|iOS|Android|Linux|all -->` (use a mappable value so C17 passes the template); the EEE block `**Platform:** All Platforms · **Doc Type:** Reference · **Doc ID:** RE-[NNN] · **Status:** Draft`; H1 `# [Reference Doc Title — descriptive, not a bare RE-NNN]`; `## Summary` (first H2, ≥30 words placeholder); a sample table section with a `> **Table summary:**` blockquote demonstrating the >25-row prose-summary convention; and a `## See Also` section. Do NOT author an RCA template (D-06 defers it).
  </action>
  <verify>
    <automated>for f in android ios macos; do grep -q "^doc_id:" docs/_templates/admin-template-${f}.md && grep -q "^doc_type: Guide" docs/_templates/admin-template-${f}.md && echo "${f} OK"; done; test -f docs/_templates/reference-template.md && grep -q "^doc_type: Reference" docs/_templates/reference-template.md && grep -qi "25 rows\|table summary" docs/_templates/reference-template.md && ! test -f docs/_templates/rca-template.md && echo "reference OK, no rca"</automated>
  </verify>
  <acceptance_criteria>
    - `admin-template-android/ios/macos.md` each have the 4 new keys, `doc_type: Guide`, an unchanged concrete `platform:` value, a matching EEE block line, and `## Summary` as first H2.
    - `docs/_templates/reference-template.md` exists with `doc_type: Reference`, `status: Draft`, a mappable `platform:` value (resolves in D1 map), the EEE block, `## Summary` first, and the Phase-118 table-remediation guidance (">25 rows → prose summary within 5 lines").
    - No `docs/_templates/rca-template.md` is created (RCA deferred per D-06).
    - All 7 templates (6 existing + reference) carry `status: Draft` and the single-line EEE block; none carries `owner` inside the block.
  </acceptance_criteria>
  <done>All 7 templates are born-EEE-conformant with mappable platform values, and the reference template carries the Phase-118 table-remediation convention; STD-02 is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template → every future new doc | A non-conformant template propagates the defect to every doc created from it |
| template platform value → C17 (Phase 115) | An unmappable template platform value FAILS C17 on all templates at Phase 115 SC3 |
| template block → grounding index | The block a template emits reaches the index for every future doc; owner must never be in it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-06 | Tampering | template platform value unmappable | mitigate (high) | D-07 fix all 3 pipe-list templates to `platform: all`; reference-template uses a mappable value; acceptance greps confirm no `|` remains in any template `platform:` line |
| T-114-07 | Information Disclosure | template block includes owner or wrong field order | mitigate | Block spec sourced from the standard's D-05 section; acceptance asserts block field set = {Platform, Doc Type, Doc ID, Status} and owner absent from the block |
| T-114-11 | Tampering | template freshness date not sentinel | mitigate | Normalize `last_verified` to `1970-01-01 # TEMPLATE-SENTINEL` so freshness validators skip templates (avoids a malformed-date C10 failure if a template ever enters scope) |
| T-114-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase |
</threat_model>

<verification>
- SC3 (STD-02): all `docs/_templates/*` produce EEE-conformant docs; `grep "^platform:" docs/_templates/*.md` shows no pipe-list values; the new `reference-template.md` exists with table-remediation guidance.
- Every template carries the 4 new keys, the single-line block, `## Summary`-first, and `Status: Draft`.
</verification>

<success_criteria>
All 7 templates are born-EEE-conformant, every platform value resolves in the D1 map, and the reference template scaffolds Phase-118 table remediation — Phase 115 C17 will exit 0 on all templates.
</success_criteria>

<output>
Create `.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-03-SUMMARY.md` when done.
</output>
