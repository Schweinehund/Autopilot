---
phase: 114-eee-standard-templates-doc-id-registry-metadata-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [META-01]
must_haves:
  truths:
    - "Running the v1.14 milestone audit with a probe file carrying doc_id/status/owner/doc_type in linuxDocPaths() scope reports C10 (id:10) = PASS"
    - "The probe file is deleted and never committed — git working tree is clean of it after the task"
    - "SC1 precondition (META-01) is proven empirically before any corpus file gains the four new frontmatter keys"
  artifacts: []
  key_links:
    - from: "docs/admin-setup-linux/99-c10-probe.md (temporary)"
      to: "scripts/validation/v1.14-milestone-audit.mjs C10 (id:10)"
      via: "linuxDocPaths() directory walk of docs/admin-setup-linux"
      pattern: "id:\\s*10"
---

<objective>
Prove the META-01 precondition: C10 (the Linux-frontmatter validator, id:10, BLOCKING since Phase 48) is lenient on the four new EEE frontmatter keys (`doc_id`, `status`, `owner`, `doc_type`) BEFORE any authoring or corpus edit begins. Research already proved this by code inspection (v1.14-milestone-audit.mjs C10 is an additive line-anchored validator with no key whitelist — it only asserts `platform: Linux`, an ISO `last_verified`, and `review_by` within 90 days). This plan confirms it empirically with a throwaway probe.

Purpose: A strict C10 would fail the entire Linux corpus the moment the retrofit adds the new keys. This is the phase-opening gate — nothing else in Phase 114 may proceed until C10 leniency is confirmed.
Output: An empirical PASS record in the SUMMARY. No committed file artifacts (the probe is created, tested, and deleted within this task).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-RESEARCH.md
@.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove C10 leniency with a throwaway probe, then delete it</name>
  <read_first>
    - scripts/validation/v1.14-milestone-audit.mjs (lines 199-239 linuxDocPaths() scope; lines 517-555 the C10 check — confirm no key whitelist, only platform/last_verified/review_by)
    - docs/admin-setup-linux/00-overview.md (the existing Linux frontmatter shape the probe mirrors)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-PATTERNS.md (§SC1 C10-Leniency Probe — exact probe shape, placement, invocation, pass criterion, cleanup)
    - .planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-RESEARCH.md (§C10 Leniency Research; Common Pitfall 1 — probe MUST be inside linuxDocPaths())
  </read_first>
  <action>
    Establish a C10 baseline, then run the leniency probe. (1) From repo root run `node scripts/validation/v1.14-milestone-audit.mjs` and record the C10 (id:10) result line as the baseline. (2) Create `docs/admin-setup-linux/99-c10-probe.md` — this exact path is inside linuxDocPaths() via the `docs/admin-setup-linux` directory walk (verified L216), so C10 is guaranteed to scan it (avoids Pitfall 1 false-positive). Frontmatter carries the four NEW keys plus the three C10-relevant keys: `doc_id: RE-T10`, `status: Draft`, `owner: test-owner`, `doc_type: Reference`, `platform: Linux`, `last_verified: 2026-07-03`, `review_by: 2026-09-30` (delta = 89 days, within C10's 90-day rule; not the 1970-01-01 sentinel), then an H1 `# C10 Leniency Probe` and one body line noting it is a deletable META-01 fixture. (3) Re-run `node scripts/validation/v1.14-milestone-audit.mjs`; confirm C10 (id:10) STILL reports PASS with the four unknown keys present. (4) Delete `docs/admin-setup-linux/99-c10-probe.md`. (5) Confirm `git status` shows the probe path absent (untracked file removed) — the probe is NEVER staged or committed. Record the before/after C10 result and the four-keys-present PASS in the SUMMARY as the META-01 proof.
  </action>
  <verify>
    <automated>node scripts/validation/v1.14-milestone-audit.mjs 2>&1 | grep -iE "id:\s*10|C10" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - With `docs/admin-setup-linux/99-c10-probe.md` present, `node scripts/validation/v1.14-milestone-audit.mjs` reports the C10 (id:10) check as PASS (the four new keys do not cause a C10 violation).
    - The probe frontmatter contains all four new keys (`doc_id`, `status`, `owner`, `doc_type`) AND `platform: Linux`, `last_verified: 2026-07-03`, `review_by: 2026-09-30`.
    - After deletion, `git status --porcelain docs/admin-setup-linux/99-c10-probe.md` returns empty (file gone, never tracked); no probe file remains anywhere under `docs/`.
    - SUMMARY records the empirical C10 PASS-with-new-keys result as the META-01 precondition proof.
  </acceptance_criteria>
  <done>C10 is empirically confirmed lenient on the four new EEE frontmatter keys; the probe is deleted and uncommitted; META-01 precondition is satisfied and the rest of Phase 114 may proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoring repo → validator harness | The C10 validator is the integrity gate; a false PASS here would let a strict-C10 corpus break silently on retrofit |
| repo working tree → git history | A leftover probe file could be committed and later retrofitted/indexed as if it were a real corpus doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Tampering | probe file left in corpus | mitigate | Create + run + delete within one task; acceptance asserts `git status` clean of the probe path; probe never staged/committed |
| T-114-02 | Repudiation | false C10 PASS (probe out of scope) | mitigate | Place probe at `docs/admin-setup-linux/99-c10-probe.md` — verified inside linuxDocPaths() (L216); re-run confirms C10 actually scanned the probe (Pitfall 1 avoided) |
| T-114-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase — no supply-chain surface |
</threat_model>

<verification>
- SC1 (META-01): `node scripts/validation/v1.14-milestone-audit.mjs` reports C10 (id:10) = PASS with a probe carrying the four new keys in linuxDocPaths() scope.
- Probe deleted; working tree clean of the probe; no corpus file was edited.
</verification>

<success_criteria>
C10 is confirmed lenient on `doc_id`, `status`, `owner`, `doc_type`; the throwaway probe is deleted and never committed; Phase 114 authoring is unblocked.
</success_criteria>

<output>
Create `.planning/phases/114-eee-standard-templates-doc-id-registry-metadata-rules/114-01-SUMMARY.md` when done, recording the empirical C10 PASS-with-new-keys result.
</output>
