---
phase: 103-macos-802-1x-admin-setup-wi-fi-wired
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-8021x/04-macos.md
autonomous: true
requirements: [DOT1X-05]

must_haves:
  truths:
    - "GOAL + SC1 + D-01..D-03: 04-macos.md follows the cloned A3 structure (Common Profile Mechanics -> Wi-Fi -> Wired -> See Also -> Change History) and lets an Intune admin create a macOS Wi-Fi 802.1X profile covering EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS at co-equal depth, with the immutable deployment-channel decision documented before any configuration steps"
    - "SC1 + D-01 + D-02 + D-03: a standalone WARNING callout + decision table at the top of Common Profile Mechanics maps cert type -> channel (user cert -> User channel/user keychain; device cert -> Device channel/system keychain), states the channel cannot be changed after assignment, and gives the delete/recreate/reassign remediation path; the callout is WARNING severity, not DANGER (no DANGER callout exists in the macOS guide)"
    - "D-04: an explicit 'No authentication-mode selector on macOS' note in Common Mechanics tells Windows-trained admins the User/Machine/User-or-machine selector is absent and the Deployment channel is the macOS analog"
    - "SC3 + D-05 + D-11 + D-12: server validation is homed once in Common Mechanics framed as a SECURITY requirement (always populate Certificate server names + reference a Root certificate for server validation profile), surfaces the macOS dynamic-trust-dialog symptom and the A-05 'disabling server validation is flagged as a security violation' fact, and LINKS the rogue-RADIUS rationale to 01-/02-/_glossary-network.md#server-name-validation without restating it; no example ever shows validation disabled"
    - "D-01: each connection subsection carries a compact per-EAP-method config matrix with three method columns (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS) and no recommended-default ranking (co-equal EAP)"
    - "D-10: the per-EAP matrix carries an 'Inner method' row populated per macOS option set: EAP-TLS = -- (n/a); PEAP = MS-CHAPv2 (inner not separately selectable); EAP-TTLS = PAP / CHAP / MS-CHAP / MS-CHAP v2"
    - "SC2 + D-06: the Wired subsection carries a prominent standalone NOTE callout stating wired client certificates are SCEP only (PKCS not supported) and pointing PKCS-only shops to 02-cert-delivery-foundation.md before attempting wired config"
    - "SC2 + D-07: the Wired subsection documents the Network Interface selector (seven options, First active Ethernet default) and carries a complete per-EAP-method config matrix at full peer depth equal to Wi-Fi"
    - "D-08 + D-09: macOS Wi-Fi vs wired profile-type confusion (B-04) is prevented structurally by distinct ## Wi-Fi and ## Wired sections with their own Intune nav paths plus at most one inline separateness sentence at the top of the Wired subsection -- NOT a blockquote callout"
    - "Link-not-copy: shared concepts (deployment ordering rule, EKU, server-name-validation theory, rogue-RADIUS rationale, EAP-method comparison, per-platform cert matrix) are linked to 01-/02-/_glossary-network.md, never restated"
    - "No Windows-only mechanics: dot3svc, enforcement-staging DANGER, TEAP, KB5014754/SID-in-SAN, PFX Import, and the Settings Catalog sentence are entirely absent (no macOS equivalent)"
    - "Identity privacy (outer identity) guidance is present for all three EAP methods on both connections (anonymous / anonymous@domain) to prevent cleartext identity leakage before the tunnel"
    - "Freshness: file front-matter carries the 90-day stamp (last_verified 2026-06-30, review_by 2026-09-28, platform: macos, applies_to: both); no inline callout freshness stamp is mandated"
  artifacts:
    - path: "docs/admin-setup-8021x/04-macos.md"
      provides: "macOS Wi-Fi + wired 802.1X admin-setup guide, all three co-equal EAP methods"
      contains: "## Wired"
      min_lines: 90
  key_links:
    - from: "docs/admin-setup-8021x/04-macos.md"
      to: "docs/admin-setup-8021x/02-cert-delivery-foundation.md#canonical-scope-callout"
      via: "scope banner link"
      pattern: "02-cert-delivery-foundation\\.md#canonical-scope-callout"
    - from: "docs/admin-setup-8021x/04-macos.md"
      to: "docs/admin-setup-8021x/01-eap-method-overview.md"
      via: "prerequisites + server-validation rogue-RADIUS link-not-copy reference"
      pattern: "01-eap-method-overview\\.md"
    - from: "docs/admin-setup-8021x/04-macos.md"
      to: "docs/_glossary-network.md#server-name-validation"
      via: "server-validation rationale link (D-12)"
      pattern: "_glossary-network\\.md#server-name-validation"
---

<objective>
Author `docs/admin-setup-8021x/04-macos.md` — the macOS per-platform 802.1X admin-setup guide covering Wi-Fi and wired Intune profiles for all three co-equal EAP methods (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS), satisfying DOT1X-05 and ROADMAP Phase 103 success criteria SC1–SC3.

Purpose: Give an Intune admin a single macOS guide that walks through Wi-Fi and wired 802.1X profile creation, surfaces the high-consequence macOS-specific facts (immutable deployment-channel decision, no auth-mode selector, server-name/dynamic-trust-dialog suppression, wired SCEP-only constraint, EAP-TTLS inner-auth option set) and links — never restates — the shared 802.1X foundation in `01-`/`02-`/`_glossary-network.md`. This guide clones the locked Phase-102 A3 Hybrid template and applies the macOS deltas (no Windows-only mechanics: dot3svc, enforcement-staging DANGER, TEAP, KB5014754 are entirely absent).

Output: `docs/admin-setup-8021x/04-macos.md` (CREATE), following the A3 structure (Common Profile Mechanics [deployment-channel WARNING + decision table → no-auth-mode note → server validation → identity privacy] → Wi-Fi → Wired [separateness sentence → SCEP-only NOTE → Network Interface selector → dynamic-trust delta → matrix] → See Also → Change History).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-macos-802-1x-admin-setup-wi-fi-wired/103-RESEARCH.md
@.planning/phases/103-macos-802-1x-admin-setup-wi-fi-wired/103-PATTERNS.md
@.planning/phases/103-macos-802-1x-admin-setup-wi-fi-wired/103-CONTEXT.md
@docs/admin-setup-8021x/03-windows.md
@docs/admin-setup-8021x/01-eap-method-overview.md
@docs/admin-setup-8021x/02-cert-delivery-foundation.md
@docs/admin-setup-macos/03-configuration-profiles.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold + Common Profile Mechanics + Wi-Fi subsection</name>
  <files>docs/admin-setup-8021x/04-macos.md</files>
  <read_first>
    - docs/admin-setup-8021x/04-macos.md — the file being created (does not exist yet)
    - docs/admin-setup-8021x/03-windows.md — the locked A3 template being cloned: front-matter block (lines 1-7), Prerequisites blockquote (9-10), Scope banner (14), `## Common Profile Mechanics` opener (16-18), `### Server Validation` home (33-43), `### Anonymous Outer Identity` (44-52), Wi-Fi `### In Intune admin center` + matrix (58-77). Do NOT clone the `### Authentication Mode` table (20-31), the wired-only validation bullets (39-42), or the Settings Catalog sentence (64)
    - 103-RESEARCH.md — Guide Content Facts: Front Matter (l.106-118), Scope Banner (l.120-128), Common Profile Mechanics section (deployment-channel exact UI labels + immutability verbatim note + decision table + WARNING callout draft l.141-186, no-auth-mode note l.188-192, server validation D-05/D-11/D-12 l.194-203, identity privacy l.205-216), Wi-Fi Subsection (nav path l.222-226, EAP types l.230, per-EAP matrix l.235-257); Plan-Time Verification Flags V1 + V4 (l.635-645)
    - 103-PATTERNS.md — YAML front-matter (change platform:windows->macos), Prerequisites/Scope copy-verbatim rules, deployment-channel WARNING blockquote format, no-auth-mode note placement, Server Validation macOS adaptation, Anonymous Outer Identity field-label change, Wi-Fi nav-path delta + Settings-Catalog omission, Wi-Fi matrix delta map (l.229-243), shared patterns (double-dash em-dash, double-hyphen slug trap, `### In Intune admin center` header, matrix orientation)
    - 103-CONTEXT.md — locked decisions D-01 through D-12; co-equal EAP, link-not-copy, navigation-last, no-Windows-mechanics hard constraints
    - docs/admin-setup-8021x/02-cert-delivery-foundation.md — confirm the live `#canonical-scope-callout` and `#per-platform-cert-delivery-support-matrix` anchor slugs before linking
    - docs/admin-setup-8021x/01-eap-method-overview.md — confirm the live `#peap-mschapv2` anchor slug; link target for the rogue-RADIUS rationale
  </read_first>
  <action>
    Create `docs/admin-setup-8021x/04-macos.md`. Start with the 6-key YAML front matter cloned from `03-windows.md` lines 1-7 with `platform: macos` (all other keys identical: `last_verified: 2026-06-30`, `review_by: 2026-09-28`, `applies_to: both`, `audience: admin`). Follow with the Prerequisites blockquote (copy verbatim from `03-windows.md` lines 9-10, linking `01-eap-method-overview.md` and `02-cert-delivery-foundation.md`), the H1 `# macOS 802.1X Admin Setup: Wi-Fi and Wired` (use ` and `, not ` / `, to avoid the double-hyphen anchor-slug trap), then the one-line Scope banner copied verbatim from `03-windows.md` line 14 linking `02-cert-delivery-foundation.md#canonical-scope-callout`.

    Author a `## Common Profile Mechanics` section (substitute "macOS" for "Windows" in the opener prose; link the ordering rule / EKU / per-platform cert options to `02-cert-delivery-foundation.md`, never restate). Order the content per D-01/D-04/D-05:
    1. Deployment-channel WARNING callout + decision table as the FIRST content (D-01/D-02/D-03), before any configuration steps. Use the `> **WARNING -- Deployment channel: choose before creating the profile**` blockquote format (double-hyphen label separator, blank `>` line between paragraphs, inner table prefixed `> |`). The callout MUST convey: the Deployment channel field determines where authentication certificates are stored and cannot be changed after the profile is assigned; the inner decision table mapping User certificate -> User channel -> User keychain and Device certificate -> Device channel -> System keychain; and the remediation path (delete the profile, create a new one with the correct channel, reassign). Severity is WARNING, NOT DANGER (D-03) — the macOS guide has no DANGER callout.
    2. A short "No authentication-mode selector on macOS" note (D-04, plain prose, NOT a callout) stating macOS does not expose a User / Machine / User-or-machine authentication mode selector in Intune profiles, that Windows-trained admins will not find it, and that the Deployment channel (User vs Device keychain) is the macOS analog to the credential-context decision.
    3. A `### Server Validation` subsection (D-05/D-11/D-12) homed once for both connections and all three EAP methods: frame populating **Certificate server names** + referencing a **Root certificate for server validation** profile + enabling validation as a SECURITY requirement; surface the macOS dynamic-trust-dialog symptom (without Certificate server names populated, macOS presents an interactive trust dialog users click through on every connection; populating the field bypasses it) and the A-05 fact verbatim ("On iOS/macOS, disabling server validation in a managed profile is flagged as a security violation"); LINK the rogue-RADIUS / credential-harvest rationale to `01-eap-method-overview.md#peap-mschapv2` and `../_glossary-network.md#server-name-validation` (never restate the theory); end with "No example in this guide shows server validation disabled." Do NOT clone the Windows wired-only hardening bullets (Disable user prompts, Require cryptographic binding).
    4. A `### Anonymous Outer Identity (Identity Privacy)` subsection cloned from `03-windows.md` lines 44-52 with the field label updated to **Identity privacy (outer identity)** and the three-EAP-method breakdown (EAP-TLS cert subject, PEAP username, EAP-TTLS username) retained; copy the realm-routing suffix note.

    Author a `## Wi-Fi` section opening with a `### In Intune admin center` header whose bold navigation path is **Devices** > **Configuration** > **New policy** > **macOS** > **Templates** > **Wi-Fi**, noting selection of **Wi-Fi type: Enterprise** to access EAP settings. Do NOT add the Windows Settings Catalog sentence. Follow with the per-EAP-method config matrix (settings as rows; three columns EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS; minimal `|---|---|---|---|` separator) per RESEARCH.md l.235-257: EAP type field value, Certificate server names, Root certificate for server validation, Client authentication (SCEP or PKCS for EAP-TLS — no Derived credential; Username and Password for PEAP/TTLS), Inner method per D-10 (EAP-TLS = -- (cert-only); PEAP = -- (PEAP tunnels MSCHAPv2; inner not separately selectable); EAP-TTLS = PAP / CHAP / MS-CHAP / MS-CHAP v2), Identity privacy (outer identity). OMIT the Windows `Perform server validation` and `Authentication mode` rows (D-04). Add the Wi-Fi EAP-TLS client-cert note (both SCEP and PKCS supported for Wi-Fi) and link cert-profile config + the per-platform cert matrix to `02-`. Keep all three EAP methods co-equal — no "recommended"/"default" language.

    Spot-check before writing the matrix rows (RESEARCH.md V1/V4): verify the macOS Wi-Fi PEAP inner-method UI (sub-dropdown vs implicit MSCHAPv2) and the current Intune Wi-Fi navigation path against the live portal or Microsoft Learn ref-wifi-settings-apple; the matrix PEAP Inner-method cell stays "-- (PEAP tunnels MSCHAPv2; inner not separately selectable)" regardless. Use `--` (double hyphen) for em-dashes throughout.
  </action>
  <verify>
    <automated>grep -q "platform: macos" docs/admin-setup-8021x/04-macos.md && grep -q "review_by: 2026-09-28" docs/admin-setup-8021x/04-macos.md && grep -q "## Common Profile Mechanics" docs/admin-setup-8021x/04-macos.md && grep -qi "Deployment channel" docs/admin-setup-8021x/04-macos.md && grep -q "## Wi-Fi" docs/admin-setup-8021x/04-macos.md && grep -q "canonical-scope-callout" docs/admin-setup-8021x/04-macos.md && grep -q "_glossary-network.md#server-name-validation" docs/admin-setup-8021x/04-macos.md && ! grep -qi "dot3svc\|DANGER\|KB5014754\|TEAP" docs/admin-setup-8021x/04-macos.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `04-macos.md` exists with front-matter `platform: macos`, `applies_to: both`, `last_verified: 2026-06-30`, `review_by: 2026-09-28`
    - A Prerequisites blockquote linking `01-eap-method-overview.md` and `02-cert-delivery-foundation.md` and a one-line Scope banner linking `02-cert-delivery-foundation.md#canonical-scope-callout` are present
    - The H1 is `# macOS 802.1X Admin Setup: Wi-Fi and Wired` (uses ` and `, no ` / `)
    - `## Common Profile Mechanics` opens with a `> **WARNING` deployment-channel callout containing an inner table mapping `User channel`/`User keychain` and `Device channel`/`System keychain`, the "cannot be changed after" immutability statement, and the delete/recreate/reassign remediation
    - No `DANGER` callout exists anywhere in the file (D-03)
    - A "No authentication-mode selector" note is present (mentions the User/Machine/User-or-machine selector is absent on macOS) and is plain prose, not a blockquote callout
    - A `### Server Validation` subsection frames validation as a security requirement, contains the verbatim "flagged as a security violation" fact, mentions the dynamic trust dialog, links `01-eap-method-overview.md#peap-mschapv2` and `../_glossary-network.md#server-name-validation`, and contains no example showing validation disabled
    - The Windows wired-only bullets (`Disable user prompts`, `Require cryptographic binding`) are absent
    - Anonymous outer identity guidance uses the label `Identity privacy (outer identity)` and covers all three EAP methods
    - A `## Wi-Fi` section has a `### In Intune admin center` nav path containing `macOS`, `Templates`, and `Wi-Fi`, with no Settings Catalog sentence
    - The Wi-Fi matrix has three EAP-method columns (`EAP-TLS`, `PEAP-MSCHAPv2`, `EAP-TTLS`), an `Inner method` row showing `PAP / CHAP / MS-CHAP / MS-CHAP v2` for EAP-TTLS, and no `Perform server validation` or `Authentication mode` row; no "recommended"/"default" ranking in prose
    - The Windows-only tokens `dot3svc`, `DANGER`, `KB5014754`, `TEAP` do not appear in the file
    - Cross-references to `01-`/`02-`/`_glossary-network.md` are present; shared concepts are linked, not restated
  </acceptance_criteria>
  <done>The scaffold, Common Profile Mechanics (deployment-channel WARNING + no-auth-mode note + server validation + identity privacy), and the Wi-Fi half of the guide are authored; SC1 and the SC3 server-validation/identity-privacy portions are satisfied; D-01, D-02, D-03, D-04, D-05, D-10 (Wi-Fi matrix), D-11, D-12 are in place.</done>
</task>

<task type="auto">
  <name>Task 2: Wired subsection + SCEP-only callout + Network Interface selector + closeout</name>
  <files>docs/admin-setup-8021x/04-macos.md</files>
  <read_first>
    - docs/admin-setup-8021x/04-macos.md — the file created in Task 1 (append the Wired subsection and closeout; keep front matter, Common Mechanics, and Wi-Fi intact)
    - docs/admin-setup-8021x/03-windows.md — Wired structural slots being cloned/replaced: `## Wired` + `### In Intune admin center` (95-99), `## See Also` (185-189), `## Change History` (193-197). Do NOT clone the dot3svc WARNING (103-121), the enforcement-staging DANGER (122-138), the TEAP note (165-167), the KB5014754 section (171-181), or the PFX-Import client-cert option
    - 103-RESEARCH.md — Wired Subsection: nav path (l.263-265), one-line B-04 separateness sentence (l.267), SCEP-only callout exact wording + draft (l.287-309), EAP types (l.311), Server Trust UI grouping (l.316), Network Interface field + seven options (l.271-285), per-EAP Wired matrix (l.318-333), dynamic-trust wired delta (l.335-337); SCEP-Only Wired Callout full draft (l.512-528); See Also / Change History macOS variants; Plan-Time Verification Flag V2 (l.642)
    - 103-PATTERNS.md — Wired section opener + B-04 sentence (l.246-262), Wired SCEP-only NOTE callout format (l.265-278), Network Interface selector explanation (l.281-285), Wired matrix delta map (l.308-323), See Also delta (remove PFX-Import mention), Change History macOS row (l.357-367), callout/em-dash/slug conventions
    - 103-CONTEXT.md — D-06, D-07, D-08, D-09, D-10, D-11, D-12 exact scope boundaries; no-DANGER and no-Windows-mechanics constraints
  </read_first>
  <action>
    Append a `## Wired` section to `04-macos.md`. Immediately after the heading, add the single D-09 one-line B-04 separateness sentence as plain prose (NOT a callout): "macOS Wi-Fi and wired 802.1X use separate Intune profile types and are configured independently." Then open a `### In Intune admin center` block with the bold navigation path **Devices** > **Configuration** > **New policy** > **macOS** > **Templates** > **Wired network**. Do NOT clone the Windows "WiredNetwork CSP" / "two high-consequence wired-only prerequisites" opener.

    Add the prominent standalone SCEP-only NOTE callout (D-06) using `> **NOTE -- Wired client certificates: SCEP only (PKCS not supported)**` blockquote format per RESEARCH.md l.512-528: macOS wired network profiles support only SCEP certificate profiles for client authentication; PKCS certificate profiles are not supported for the wired profile type; Wi-Fi supports both SCEP and PKCS; PKCS-only shops must configure SCEP infrastructure before deploying wired 802.1X on macOS; link `02-cert-delivery-foundation.md#per-platform-cert-delivery-support-matrix`. Severity is NOTE (informational constraint) — no DANGER callout.

    Add a Network Interface selector explanation (D-07) as plain prose + a two-column (Option / Behavior) markdown table matching the matrix style (NOT a callout), placed after the SCEP-only NOTE and before the per-EAP matrix. List all seven options with **First active Ethernet** as the default, and note that "active" variants use interfaces actively working on the device (falling to the next in service-order priority if none active) per RESEARCH.md l.271-285.

    Add the one-line dynamic-trust-dialog wired delta (D-05) as prose after the Network Interface selector: populating **Certificate server names** also bypasses the dynamic trust dialog that would otherwise appear on the user's device at each wired connection attempt (the wired-specific expression of the behavior already homed in Common Mechanics). Do NOT restate the full server-validation theory — this is a one-line delta.

    Add the per-EAP-method Wired config matrix (settings as rows; three columns EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS; minimal `|---|---|---|---|` separator) per RESEARCH.md l.318-333 at full peer depth (D-07): EAP type field value; `Server Trust -- Certificate server names` (macOS wired UI groups this under a "Server Trust" section); Root certificate for server validation; Client Authentication method (`Certificates (SCEP only; PKCS not supported)` for EAP-TLS; `Username and Password` for PEAP/EAP-TTLS); Inner method per D-10 (EAP-TLS = -- (cert-only); PEAP = -- (PEAP tunnels MSCHAPv2; inner not separately selectable); EAP-TTLS = PAP / CHAP / MS-CHAP / MS-CHAP v2); Identity privacy (outer identity). OMIT the Windows `Perform server validation`, `Disable user prompts for server validation`, `Require cryptographic binding`, `PFX Import`, and `Authentication mode` rows. Keep all three methods co-equal.

    Spot-check before writing the Wired PEAP matrix row (RESEARCH.md V2): verify the macOS wired PEAP inner-method UI against the live portal or Microsoft Learn ref-wired-network-settings-macos; the matrix cell stays "-- (PEAP tunnels MSCHAPv2; inner not separately selectable)" regardless.

    Close the file with a `## See Also` section linking `01-eap-method-overview.md`, `02-cert-delivery-foundation.md` (update the `02-` description to remove the PFX-Import mention — e.g., "SCEP/PKCS cert delivery, per-platform cert matrix"), and `../_glossary-network.md`; and a `## Change History` table whose initial-version row is dated 2026-06-30 and summarizes the macOS guide content (immutable deployment-channel WARNING; wired SCEP-only callout; dynamic trust dialog suppression). Do NOT add an inline callout freshness stamp (no macOS high-drift callout is mandated). Confirm no Windows-only section (dot3svc, enforcement-staging DANGER, TEAP, KB5014754, PFX Import) was introduced anywhere in the file.
  </action>
  <verify>
    <automated>grep -q "## Wired" docs/admin-setup-8021x/04-macos.md && grep -q "Wired network" docs/admin-setup-8021x/04-macos.md && grep -qi "separate Intune profile types" docs/admin-setup-8021x/04-macos.md && grep -qi "SCEP only" docs/admin-setup-8021x/04-macos.md && grep -qi "PKCS" docs/admin-setup-8021x/04-macos.md && grep -qi "First active Ethernet" docs/admin-setup-8021x/04-macos.md && grep -qi "dynamic trust" docs/admin-setup-8021x/04-macos.md && grep -q "## Change History" docs/admin-setup-8021x/04-macos.md && grep -q "## See Also" docs/admin-setup-8021x/04-macos.md && ! grep -qi "dot3svc\|DANGER\|KB5014754\|TEAP\|PFX Import" docs/admin-setup-8021x/04-macos.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - A `## Wired` section exists, opening with the single inline separateness sentence (`separate Intune profile types`) as plain prose, not a blockquote callout (D-08/D-09)
    - The Wired `### In Intune admin center` nav path contains `macOS`, `Templates`, and `Wired network`
    - A `> **NOTE` SCEP-only callout states `SCEP only` / `PKCS` not supported for wired and links `02-cert-delivery-foundation.md#per-platform-cert-delivery-support-matrix`
    - A Network Interface selector table lists `First active Ethernet` as default and all seven options; it is a plain table, not a blockquote callout
    - A one-line dynamic-trust-dialog wired delta is present after the Network Interface selector
    - The Wired matrix has three EAP-method columns, a `Server Trust -- Certificate server names` row label, a `Certificates (SCEP only; PKCS not supported)` EAP-TLS client-auth cell, an `Inner method` row showing `PAP / CHAP / MS-CHAP / MS-CHAP v2` for EAP-TTLS, and no `Perform server validation`, `Disable user prompts`, `Require cryptographic binding`, `PFX Import`, or `Authentication mode` rows
    - `## See Also` links `01-`, `02-`, and `../_glossary-network.md` with the `02-` description free of any PFX-Import mention; a `## Change History` table dated 2026-06-30 is present
    - The Windows-only tokens `dot3svc`, `DANGER`, `KB5014754`, `TEAP`, `PFX Import` do not appear anywhere in the file
    - No inline callout freshness stamp is present (only the file-level front-matter stamp)
  </acceptance_criteria>
  <done>The Wired subsection (separateness sentence, SCEP-only NOTE, Network Interface selector, dynamic-trust delta, full per-EAP matrix) and the closeout are authored; SC2 and the SC3 outer-identity-per-EAP portion are satisfied; D-06, D-07, D-08, D-09, D-10 (Wired matrix) are in place. DOT1X-05 is fully delivered in `04-macos.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supplicant (managed macOS device) → authenticator (switch/AP) → RADIUS/NPS | 802.1X credential exchange; the guide's configuration guidance determines whether this boundary is hardened (server validation, deployment channel) |
| authoring artifact | `04-macos.md` is a static Markdown documentation file with no executable surface, no input parsing, no runtime — it ships no scripts and no code |

This is a pure-documentation phase. There are NO high-severity code/runtime threats — no code, no runtime, no user input, no data handling. The only realistic threat surface is documentation-integrity: publishing an insecure configuration as guidance. The single content-integrity obligation is that the server-validation guidance must never instruct disabling validation (the A-05/C-02 rogue-RADIUS risk), which D-11/D-12 mitigate by mandating security-violation framing and never showing disabled-validation examples.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Spoofing | Rogue RADIUS / credential harvest if server-validation guidance were wrong (A-05/C-02) | mitigate | Common Mechanics homes server validation as a security requirement, surfaces the "disabling server validation is flagged as a security violation" fact, and shows NO disabled-validation example; rationale linked to 01-/02-/glossary (Task 1 acceptance) |
| T-103-02 | Tampering | Wrong deployment channel → cert in wrong keychain → auth failure + security risk (macOS immutable-channel pitfall) | mitigate | Deployment-channel WARNING callout + decision table as first Common Mechanics content; cert-type→channel mapping + delete/recreate remediation (Task 1 acceptance) |
| T-103-03 | Information Disclosure | Cleartext identity leakage before tunnel | mitigate | Identity privacy (outer identity) guidance for all three EAP methods on both connections (Task 1 acceptance) |
| T-103-04 | Denial of Service | PKCS cert silently unusable on wired → failed/blocked wired auth | mitigate | Prominent SCEP-only NOTE callout in the Wired subsection before the config matrix (Task 2 acceptance) |
| T-103-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no shipped scripts in this phase; documentation-only file creation — no supply-chain surface |

Block-on severity: high. No high-severity code/runtime threats exist for this docs phase. The content obligations T-103-01..04 are encoded as task acceptance criteria; absence of T-103-01 (the one genuine content-integrity risk) is a blocking gap.
</threat_model>

<verification>
- `04-macos.md` exists and follows the cloned A3 structure: Common Profile Mechanics → Wi-Fi → Wired → See Also → Change History.
- The deployment-channel callout is WARNING severity with a cert-type→channel decision table and the immutability + delete/recreate/reassign remediation; no DANGER callout exists.
- Both per-EAP-method matrices present three co-equal columns (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS) with an Inner-method row populated per D-10; no recommended-default ranking.
- Server validation is homed once in Common Mechanics with the security-violation framing and links to 01-/02-/glossary; no example shows validation disabled.
- The Wired subsection carries the SCEP-only NOTE, the Network Interface selector table, and the one-line dynamic-trust delta; macOS wired matrix enforces SCEP-only client auth.
- Windows-only mechanics (dot3svc, enforcement-staging DANGER, TEAP, KB5014754, PFX Import, Settings Catalog sentence) are entirely absent.
- File-level 90-day freshness stamp present; no inline callout stamp.
- Plan-time verification flags V1/V2 (PEAP inner-method UI) and V4 (Wi-Fi nav path) were spot-checked at authoring time.
</verification>

<success_criteria>
- DOT1X-05 fully delivered by `docs/admin-setup-8021x/04-macos.md`.
- SC1: macOS Wi-Fi 802.1X profile, three co-equal EAP methods, immutable deployment-channel decision before profile creation — present.
- SC2: macOS wired profile with Network Interface selector + SCEP-only / PKCS-not-supported callout — present.
- SC3: server-name field populated to suppress the dynamic trust dialog + outer-identity/identity-privacy per EAP method — present.
- The content-integrity obligation (T-103-01: never instruct disabling server validation) is encoded and verifiable.
</success_criteria>

<output>
Create `.planning/phases/103-macos-802-1x-admin-setup-wi-fi-wired/103-01-SUMMARY.md` when done.
</output>
