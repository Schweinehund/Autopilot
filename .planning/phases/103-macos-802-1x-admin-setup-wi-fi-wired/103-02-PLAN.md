---
phase: 103-macos-802-1x-admin-setup-wi-fi-wired
plan: 02
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - docs/admin-setup-8021x/00-overview.md
autonomous: true
requirements: [DOT1X-05]

must_haves:
  truths:
    - "The local 00-overview.md platform-guide list gains item 4 as a descriptive one-liner linking 04-macos.md, so a reader navigating the 802.1X folder entry point can discover the newly authored macOS guide"
    - "Navigation-last (local-only): only the in-folder 00-overview.md platform-list entry is edited; no capability-matrix rows and no global nav-hub files (index.md, quick-refs, decision-trees) are touched (those are Phase 109)"
    - "The placeholder '4-7. Platform guides (Phase 103-106)' line is replaced by item 4 (macOS) plus a '5-7. Platform guides (Phase 104-106)' continuation, matching the existing item-1/2/3 numbered-list link style (bold linked title, double-dash, no trailing period)"
    - "V3: the Mermaid '3-7. Platform Guides' diagram node is left unchanged after inspection — it references a range that stays valid until all five guides are added"
    - "Harness allowlisting: 00-overview.md is a pre-existing file; the edit must be recorded for harness allowlisting, the same pattern used for the Phase 102 item-3 edit and the Phase 101 glossary see-also edits"
  artifacts:
    - path: "docs/admin-setup-8021x/00-overview.md"
      provides: "Local 802.1X folder entry point with the macOS platform-guide entry (item 4)"
      contains: "](04-macos.md)"
  key_links:
    - from: "docs/admin-setup-8021x/00-overview.md"
      to: "docs/admin-setup-8021x/04-macos.md"
      via: "platform-guide list item 4"
      pattern: "\\]\\(04-macos\\.md\\)"
---

<objective>
Add the macOS platform-guide entry to the local `docs/admin-setup-8021x/00-overview.md` numbered list (item 4), replacing the reserved "4–7. Platform guides (Phase 103–106)" placeholder line with the authored macOS entry plus a "5–7. Platform guides (Phase 104–106)" continuation for the still-pending platform guides, and appending a Change History row.

Purpose: Make the just-authored `04-macos.md` discoverable from the 802.1X folder entry point. This is the ONLY in-scope navigation edit for Phase 103 — capability matrices and global nav hubs are deferred to Phase 109 (navigation-last).

Output: `docs/admin-setup-8021x/00-overview.md` (EDIT — one numbered-list line replaced with two lines + a Change History row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/admin-setup-8021x/00-overview.md
@.planning/phases/103-macos-802-1x-admin-setup-wi-fi-wired/103-PATTERNS.md
@.planning/phases/103-macos-802-1x-admin-setup-wi-fi-wired/103-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the macOS platform-guide entry (item 4) to 00-overview.md</name>
  <files>docs/admin-setup-8021x/00-overview.md</files>
  <read_first>
    - docs/admin-setup-8021x/00-overview.md — the live file (the source of truth): locate the numbered-list line `4–7. Platform guides (Phase 103–106) -- entries added as each guide is authored.` (the single edit target, ~line 30), confirm the item-1/2/3 link style above it (item 3 is the Phase-102 precedent, ~line 28), and inspect the Mermaid node `C[3–7. Platform<br/>Guides]` (~line 21) to confirm it stays valid as a range (V3)
    - 103-PATTERNS.md — `00-overview.md (EDIT — add item 4)` section: exact region to edit (line 30), the descriptive-one-liner link format (bold linked title, `--`, single sentence, no trailing period), the rule that the `4–7.` line becomes item 4 + a new `5–7.` continuation, the Mermaid-node-unchanged note (V3/assumption A1), and the Change-History row to append
    - 103-RESEARCH.md — `00-overview.md Platform Entry (edit, item 4)` (l.341-358) and `00-overview.md Item-4 Edit (Surgical Replacement)` (l.551-565): the exact target text and replacement text; Plan-Time Verification Flag V3 (l.643)
    - docs/admin-setup-8021x/04-macos.md — confirm the link target filename exists (created in Plan 103-01) before linking to it
  </read_first>
  <action>
    Edit `docs/admin-setup-8021x/00-overview.md`. Replace the single numbered-list line `4–7. Platform guides (Phase 103–106) -- entries added as each guide is authored.` with two lines: (1) item 4, a descriptive one-liner matching the item-1/2/3 style — bold linked title `**[macOS 802.1X Admin Setup (Wi-Fi + Wired)](04-macos.md)**`, then `--`, then a single description sentence naming the key content (Wi-Fi and wired profiles for all three EAP methods; immutable deployment-channel decision (User vs Device keychain) before profile creation; wired SCEP-only constraint; server name required to suppress dynamic trust dialog), with no trailing period; and (2) a continuation line `5–7. Platform guides (Phase 104–106) -- entries added as each guide is authored.` Keep the title parenthetical free of ` / ` (use `+`) to respect the corpus slug convention and the double-dash em-dash style throughout.

    Inspect the Mermaid `C[3–7. Platform<br/>Guides]` node (V3): leave it unchanged — it references a range that remains valid until all five platform guides are authored. Do NOT modify the Mermaid diagram, the Scope section, the wired-availability note, the See Also section, or any other file; this edit is strictly the numbered-list line plus the Change History row below.

    Append a Change History row dated 2026-06-30 following the existing pattern: `| 2026-06-30 | Added item 4 -- macOS platform-guide entry linking 04-macos.md; narrowed placeholder range from 4--7 to 5--7 | -- |`.

    Record in the SUMMARY that `00-overview.md` is a pre-existing file and this edit must be added to the harness allowlist (same handling as the Phase 102 item-3 edit and the Phase 101 glossary see-also edits).
  </action>
  <verify>
    <automated>grep -q "](04-macos.md)" docs/admin-setup-8021x/00-overview.md && grep -q "5–7. Platform guides (Phase 104–106)" docs/admin-setup-8021x/00-overview.md && ! grep -q "4–7. Platform guides (Phase 103–106)" docs/admin-setup-8021x/00-overview.md && grep -q "](03-windows.md)" docs/admin-setup-8021x/00-overview.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `00-overview.md` numbered list contains item `4.` with a bold link `](04-macos.md)` and a single description sentence (no trailing period), matching the item-1/2/3 format
    - A `5–7. Platform guides (Phase 104–106)` continuation line is present
    - The old `4–7. Platform guides (Phase 103–106)` numbered-list line is gone
    - The pre-existing item 3 (`](03-windows.md)`) entry remains intact
    - The Mermaid `3–7. Platform Guides` node is unchanged (V3) and the Scope section, wired-availability note, and See Also are unchanged
    - No other file is modified (no capability-matrix rows, no `docs/index.md` / quick-ref / decision-tree edits — navigation-last, Phase 109)
    - A Change History row dated 2026-06-30 documenting the item-4 addition and the 4–7 → 5–7 narrowing is appended
    - The SUMMARY records that `00-overview.md` is a pre-existing file requiring harness-allowlist registration (Phase 102 item-3 / Phase 101 glossary-edit pattern)
  </acceptance_criteria>
  <done>The macOS guide is discoverable from the local 802.1X folder entry point via item 4; the placeholder range is correctly narrowed to 5–7; the Mermaid node is confirmed unchanged; the pre-existing-file edit is flagged for harness allowlisting. DOT1X-05 navigation is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoring artifact | `00-overview.md` is a static Markdown navigation file; the edit adds one link line, one continuation line, and one Change History row. No executable surface, no input parsing, no runtime. |

This is a documentation navigation edit with no executable attack surface. No high-severity code/runtime threats exist for this docs phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-02-01 | Tampering | Pre-existing file edited outside harness allowlist (integrity-tracking drift) | mitigate | Edit is scoped to the single numbered-list line + one Change History row; SUMMARY flags the file for harness-allowlist registration (Phase 102 item-3 / Phase 101 glossary-edit pattern) |
| T-103-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no scripts; documentation-only one-line edit — no supply-chain surface |

Block-on severity: high. No high-severity content-security obligations apply to a navigation link edit; the only tracked concern is allowlist-integrity hygiene (T-103-02-01).
</threat_model>

<verification>
- `00-overview.md` links to `04-macos.md` as numbered item 4 in the platform-guide list.
- The placeholder range is updated from `4–7 (Phase 103–106)` to `5–7 (Phase 104–106)`.
- The existing item 3 (`03-windows.md`) and the Mermaid range node are unchanged.
- No global nav-hub or capability-matrix files are touched (navigation-last invariant honored for Phase 103).
- The pre-existing-file edit is recorded for harness allowlisting.
</verification>

<success_criteria>
- The macOS 802.1X guide is reachable from the local 802.1X overview entry point.
- Edit is surgical (one list line replaced + one Change History row); nothing else in `00-overview.md` changes.
- Harness-allowlist registration is flagged in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/103-macos-802-1x-admin-setup-wi-fi-wired/103-02-SUMMARY.md` when done.
</output>
