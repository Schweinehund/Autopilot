---
phase: 113-conversion-pipeline-lock-representative-set-grounding-valida
plan: 04
type: execute
wave: 4
depends_on: ["113-03"]
files_modified:
  - .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md
  - .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-FINDINGS.md
autonomous: false
requirements: [PIPE-02]
user_setup:
  - service: sharepoint-copilot-studio
    why: "Live grounding validation of the converted representative set (D-01 owner-run leg)"
    dashboard_config:
      - task: "Upload the 5 guard-passed .docx files to the test SharePoint document library and wait for the Copilot Studio connector to sync"
        location: "Test SharePoint document library + connected Copilot Studio agent (owner-provisioned; out of agent scope per REQUIREMENTS L77)"

must_haves:
  truths:
    - "An owner-run PIPE-02 runbook exists with prerequisites, upload procedure, and the exact Q1-Q6 query sequence (SC4)"
    - "A findings template exists capturing all four empirical open questions: citation-title source, Status:Draft retrieval, chunk-boundary behavior, custom-property promotion (SC4)"
    - "The owner executes the runbook and records answers to all four open questions in the findings artifact (SC4)"
    - "Findings confirm document-level citations resolve and the single-line header block appears as body text, not a Word custom property (SC4)"
  artifacts:
    - path: ".planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md"
      provides: "Owner-run grounding-validation procedure (prereqs, upload, Q1-Q6, completion condition)"
      contains: "Copilot Studio"
    - path: ".planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-FINDINGS.md"
      provides: "Findings template for OQ1-OQ4; owner-filled at the checkpoint"
      contains: "OQ1"
  key_links:
    - from: "PIPE-02-RUNBOOK.md query sequence"
      to: "PIPE-02-FINDINGS.md OQ1-OQ4"
      via: "each query records into a specific open-question slot"
      pattern: "OQ[1-4]"
    - from: "PIPE-02-FINDINGS.md"
      to: "Phase 114 standard authoring"
      via: "Status:Draft + citation-title + custom-property answers gate Phase 114 decisions"
      pattern: "Phase 114"
---

<objective>
Author the owner-run grounding-validation artifacts and complete PIPE-02 at the single owner
checkpoint (D-01, D-06). The agent has NO live Copilot Studio / SharePoint access (REQUIREMENTS L77);
its deliverable is the step-by-step runbook + the findings scaffold, NOT the empirical answers. The
owner uploads the guard-passed representative set, runs the query sequence, and records the results.

This resolves the four empirical open questions that gate Phase 114 standard authoring (SC4):
citation-title source (may flag a v1.16 file-rename candidate), Status:Draft retrieval gate-vs-label
(gates whether Phase 114 needs content-approval instructions), chunk-boundary behavior (P-02), and
custom-property promotion of non-standard YAML keys with pinned pandoc 3.7.0.2.

Purpose: Empirically confirm the locked pipeline actually grounds correctly in the real platform
before the ~150-doc retrofit begins — the one leg of this milestone that requires live platform
testing.
Output: `PIPE-02-RUNBOOK.md`, `PIPE-02-FINDINGS.md` (authored by agent; findings filled by owner at
the checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/113-CONTEXT.md
@.planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/113-RESEARCH.md
@scripts/pipeline/test-fixtures/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author PIPE-02-RUNBOOK.md — owner-run grounding procedure (SC4)</name>
  <files>.planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/113-RESEARCH.md (§Owner-Run PIPE-02 Procedure lines 489-557 — required sections, upload procedure, the Q1-Q6 query table, completion condition)
    - scripts/pipeline/test-fixtures/README.md (the 5-doc manifest + guard results to reference as prerequisites)
  </read_first>
  <action>
    Author `PIPE-02-RUNBOOK.md` in the phase directory as an owner-facing, step-by-step procedure, per
    RESEARCH §Owner-Run PIPE-02 Procedure. Include four required sections: (1) Prerequisites — the test
    SharePoint document library URL and the connected Copilot Studio agent name/URL (owner fills in),
    plus the agent-confirmed precondition that all 5 representative `.docx` files converted and passed
    the guard (reference the Plan-03 manifest). (2) Upload Procedure — copy the 5 `.docx` files to the
    test SharePoint library via the SharePoint UI, verify they appear without indexing errors, wait
    15-30 min for the Copilot Studio connector to sync. (3) Query Sequence — reproduce the exact Q1-Q6
    query table from RESEARCH lines 507-514 (Q1 citation-title probe on the Windows doc; Q2 doc-ID
    body-text retrieval `What does document RE-T01 cover?`; Q3 compound-platform 802.1X retrieval; Q4
    Android capability-table chunk retrieval; Q5 Status:Draft retrieval; Q6 optional chunk-boundary
    probe), each with the exact query text and what to record. (4) Phase Completion Condition — the
    owner pastes findings into PIPE-02-FINDINGS.md; Phase 113 closes when that file is committed with
    all four open questions answered. State plainly that the live upload + queries are owner-run because
    the agent has no SharePoint/Copilot Studio access (D-01; REQUIREMENTS L77). No fenced code blocks
    are needed — this is a procedure doc, use a Markdown table for the query sequence.
  </action>
  <verify>
    <automated>grep -qi "Copilot Studio" .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md && grep -qiE "Q1|Q5" .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md && grep -qi "RE-T01" .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `PIPE-02-RUNBOOK.md` contains the four sections: Prerequisites, Upload Procedure, Query Sequence, Phase Completion Condition
    - The Query Sequence includes at least Q1 through Q5 with exact query text (grep matches `Q1` and `Q5`, and the `RE-T01` doc-ID probe)
    - The runbook states the live legs are owner-run and references the Plan-03 guard-passed manifest as a precondition
  </acceptance_criteria>
  <done>PIPE-02-RUNBOOK.md is a complete owner-facing procedure with prerequisites, upload steps, the Q1-Q6 query sequence, and the completion condition.</done>
</task>

<task type="auto">
  <name>Task 2: Author PIPE-02-FINDINGS.md — findings template for OQ1-OQ4 (SC4)</name>
  <files>.planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-FINDINGS.md</files>
  <read_first>
    - .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/113-RESEARCH.md (§Owner-Run PIPE-02 Procedure — Recording Template lines 516-557: the exact OQ1-OQ4 structure)
    - .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md (Task 1 output — the query sequence each OQ records against)
  </read_first>
  <action>
    Author `PIPE-02-FINDINGS.md` as the owner-fillable recording template, reproducing the structure
    from RESEARCH lines 521-554 verbatim in intent: a header (test date, tester, test SharePoint
    library URL, Copilot Studio agent) and four Open-Questions-Resolved slots — OQ1 Citation Title
    Source (filename vs SharePoint Title column vs Word title property vs H1; plus the v1.16
    file-rename candidate flag), OQ2 Status:Draft Retrieval Gate vs Label (retrieved-or-not; the Phase
    114 owner decision on whether SharePoint content-approval is needed), OQ3 Chunk Boundary Behavior
    (whether the Android capability-matrix header row + data rows are retrieved together or split —
    P-02), and OQ4 Custom YAML Key Promotion (open a converted `.docx` in Word → File > Properties >
    Custom; record whether `doc_id`, `status`, `owner`, `doc_type` appear as custom properties with
    pinned pandoc 3.7.0.2). Include a checkbox for the core SC4 confirmations: document-level citations
    resolve, and the single-line header block appears as body text (not only as a Word custom
    property). Add an "Additional Observations" free-text section. Leave all answer slots blank for the
    owner. This is a Phase-114 handoff artifact.
  </action>
  <verify>
    <automated>grep -qE "OQ1" .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-FINDINGS.md && grep -qE "OQ4" .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-FINDINGS.md && grep -qi "Phase 114" .planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `PIPE-02-FINDINGS.md` contains all four open-question slots OQ1, OQ2, OQ3, OQ4 (grep matches `OQ1` and `OQ4`)
    - OQ2 records the Phase-114 owner decision on Status:Draft (label vs retrieval gate); the doc references `Phase 114` handoff
    - The template includes the SC4 confirmations (document-level citations resolve; header block indexed as body text, not only a custom property)
    - Answer slots are left blank/unchecked for the owner to fill
  </acceptance_criteria>
  <done>PIPE-02-FINDINGS.md is a blank owner-fillable template covering OQ1-OQ4 plus the SC4 citation/body-text confirmations, ready for the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: OWNER CHECKPOINT — run the live grounding validation and record findings (SC4)</name>
  <what-built>
    The agent has locked the pipeline (pandoc 3.7.0.2 pinned + reference.docx committed), authored the
    standalone guard, converted the 5-doc representative set, and confirmed each doc passes the guard
    (no YAML leak, Heading styles preserved). The agent has authored PIPE-02-RUNBOOK.md (procedure) and
    PIPE-02-FINDINGS.md (blank recording template). The agent CANNOT run this leg — it has no
    SharePoint / Copilot Studio access (D-01; REQUIREMENTS L77).
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md` and follow it end to end.
    2. Upload the 5 guard-passed `.docx` files (from `.pipeline-output/`) to your test SharePoint document library; wait 15-30 min for the Copilot Studio connector to sync.
    3. Run the Q1-Q6 query sequence in the Copilot Studio chat exactly as written.
    4. For OQ4, open one converted `.docx` in Word → File > Properties > Custom and record whether `doc_id`/`status`/`owner`/`doc_type` appear.
    5. Record all observations in `PIPE-02-FINDINGS.md`, answering OQ1 (citation-title source), OQ2 (Status:Draft retrieval gate vs label), OQ3 (chunk-boundary behavior), and OQ4 (custom-property promotion), and confirm document-level citations resolve and the header block appears as body text.
    Expected outcome: all four open questions answered; the SC4 confirmations checked (or issues described).
  </how-to-verify>
  <resume-signal>Commit the filled-in PIPE-02-FINDINGS.md, then type "findings recorded" or describe any grounding issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local .docx → test SharePoint library | owner uploads converted docs into a live indexed surface |
| Copilot Studio index → owner findings | empirical results cross from the live platform into the Phase-114 record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-08 | Information Disclosure | Status:Draft fixture uploaded to a library | mitigate | The runbook confines uploads to the OWNER's TEST SharePoint library (not production); the deployment policy (Plan 02 README) documents Draft exclusion from the production path |
| T-113-09 | Repudiation | unrecorded / partial grounding findings | mitigate | The findings template forces explicit answers to all four OQs plus SC4 confirmations; the phase-completion condition requires a committed PIPE-02-FINDINGS.md before close |
| T-113-10 | Elevation of Privilege | agent attempting live upload/query without credentials | accept | Out of scope by decision D-01 / REQUIREMENTS L77; the live legs are owner-run by necessity — the checkpoint is autonomous:false and never agent-executed |
</threat_model>

<verification>
- PIPE-02-RUNBOOK.md contains Copilot Studio prereqs, the Q1-Q6 sequence, and the RE-T01 doc-ID probe.
- PIPE-02-FINDINGS.md contains OQ1-OQ4 slots and the Phase-114 handoff reference.
- Owner checkpoint: PIPE-02-FINDINGS.md committed with all four open questions answered (phase completion condition).
</verification>

<success_criteria>
- The owner-run runbook + findings scaffold are authored (SC4 — agent deliverable).
- The owner records answers to all four empirical open questions and the SC4 confirmations at the blocking checkpoint (SC4 — owner-supplied).
- The live legs remain owner-run (autonomous:false), honoring D-01 / REQUIREMENTS L77.
</success_criteria>

<output>
Create `.planning/phases/113-conversion-pipeline-lock-representative-set-grounding-valida/113-04-SUMMARY.md` when done.
</output>
