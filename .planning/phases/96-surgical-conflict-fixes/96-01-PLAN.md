---
phase: 96-surgical-conflict-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/macos-lifecycle/00-ade-lifecycle.md
autonomous: true
requirements: [ACC-01, ACC-02]
must_haves:
  truths:
    - "Stage 6 of guide 00 states macOS Company Portal is deployed as a required PKG/LOB app via Intune, never via VPP / Apps and Books"
    - "The 'silent installation' rationale at Stage 6 is attributed to the Intune Management Extension, not VPP licensing"
    - "The orphaned VPP row in the Glossary Quick Reference table is gone"
    - "The Stage-4 SSO-extension Watch-Out bullet (line ~250) assigns the policy to a static user group, not a static device group"
    - "Frontmatter freshness stamps reflect the Phase 96 edit date with the +3-month invariant preserved"
  artifacts:
    - path: "docs/macos-lifecycle/00-ade-lifecycle.md"
      provides: "Corrected Stage-6 PKG/LOB deployment prose, removed VPP quick-ref row, corrected Stage-4 user-group assignment, bumped stamps, version-history row"
      contains: "static user group"
  key_links:
    - from: "docs/macos-lifecycle/00-ade-lifecycle.md Stage 6 prose"
      to: "docs/macos-lifecycle/00-ade-lifecycle.md line 326 exemplar"
      via: "consistent PKG/LOB-never-VPP framing"
      pattern: "PKG"
---

<objective>
Correct the two verified factual errors in `docs/macos-lifecycle/00-ade-lifecycle.md`: the false "Company Portal deployed via VPP" claim at Stage 6 (ACC-01 + D-01 + D-02) and the wrong "static device group" assignment for the Stage-4 SSO-extension policy (ACC-02 + D-05). Bump freshness stamps and add a version-history row (D-03).

Purpose: Readers must no longer encounter the VPP-on-macOS-Company-Portal error or the device-vs-user-group contradiction in guide 00.
Output: One edited Markdown file — Stage-6 prose rewritten to PKG/LOB, orphaned VPP quick-ref row removed, Stage-4 bullet corrected to user group, stamps bumped, version-history row added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-surgical-conflict-fixes/96-CONTEXT.md
@.planning/phases/96-surgical-conflict-fixes/96-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Stage-6 VPP claims to PKG/LOB and remove the orphaned VPP glossary row (ACC-01, D-01, D-02)</name>
  <files>docs/macos-lifecycle/00-ade-lifecycle.md</files>
  <read_first>
    - docs/macos-lifecycle/00-ade-lifecycle.md (the file being edited — read lines 299-330 for Stage-6 prose and lines 370-421 for the See-Also + Glossary Quick Reference table)
    - .planning/phases/96-surgical-conflict-fixes/96-RESEARCH.md lines 90-146 (verbatim current text of lines 309, 319, 326 exemplar, 382 See-Also, and 411 VPP row)
  </read_first>
  <action>
    Three edits, all within `docs/macos-lifecycle/00-ade-lifecycle.md`, all aligned to the already-correct exemplar at line 326 (which states the macOS Company Portal "is never distributed via Apple VPP / Apps and Books (that channel is iOS/iPadOS only)" — DO NOT modify line 326).

    Edit A (line ~309): Rewrite the third sentence of the "Company Portal launch" item. Currently it reads "The app can be deployed via [VPP](../_glossary-macos.md#vpp) (Apps and Books in [ABM](../_glossary-macos.md#abm)) for silent installation." Replace it so the macOS method is a required PKG (line-of-business / unmanaged macOS PKG) app deployed via Intune, and the silent-installation rationale is correctly attributed to the Intune Management Extension (IME) — NOT VPP licensing. Remove the `[VPP](../_glossary-macos.md#vpp)` inline link entirely (per D-02). You MAY keep an `[ABM](../_glossary-macos.md#abm)` reference only if the rewritten sentence still legitimately mentions ABM; otherwise drop it. Do NOT add a new "never via VPP" callout — line 326 already carries that explanation (avoid triplication per RESEARCH Pitfall 1).

    Edit B (line ~319): Rewrite the full bullet sentence. Currently it reads "Company Portal must be deployed as a required app. The recommended deployment method is through VPP (Apps and Books) for silent, license-managed installation. Alternatively, it can be deployed as a DMG or PKG app through Intune." Rewrite so PKG/LOB is stated as THE macOS deployment method (added to Intune as a line-of-business app or as an unmanaged macOS PKG app), matching line 326's phrasing. Strip the false "recommended method is through VPP" premise entirely. Do not leave a dangling "Alternatively" clause that implies VPP was the primary option.

    Edit C (line ~411): Delete the entire Glossary Quick Reference table row "| [VPP](../_glossary-macos.md#vpp) | Volume Purchase Program (Apps and Books) for app deployment | Stage 6 |". Remove the whole line including its trailing newline so the table remains well-formed (the preceding "Await Configuration" row becomes the last row).

    Do NOT touch: line 326 (exemplar), line ~382 See-Also prose ("ADE, ABM, Setup Assistant, VPP terminology with Windows equivalents" — keep verbatim per D-02), and do NOT touch the `#vpp` definition in `_glossary-macos.md` (different file, out of this plan's scope).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -c "recommended deployment method is through VPP" docs/macos-lifecycle/00-ade-lifecycle.md | grep -qx 0 && grep -c "can be deployed via \[VPP\]" docs/macos-lifecycle/00-ade-lifecycle.md | grep -qx 0 && grep -cE "^\| \[VPP\].*Stage 6 \|$" docs/macos-lifecycle/00-ade-lifecycle.md | grep -qx 0 && grep -qF "iOS/iPadOS only" docs/macos-lifecycle/00-ade-lifecycle.md && grep -qF "VPP terminology with Windows equivalents" docs/macos-lifecycle/00-ade-lifecycle.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `00-ade-lifecycle.md` no longer contains the string `recommended deployment method is through VPP`
    - `00-ade-lifecycle.md` no longer contains the string `can be deployed via [VPP]`
    - `00-ade-lifecycle.md` no longer contains a table row matching `^| [VPP].*Stage 6 |$`
    - `00-ade-lifecycle.md` still contains `iOS/iPadOS only` (line 326 exemplar untouched)
    - `00-ade-lifecycle.md` still contains `VPP terminology with Windows equivalents` (line 382 See-Also preserved per D-02)
    - Stage-6 prose at lines ~309 and ~319 now names PKG / line-of-business (LOB) as the macOS deployment method and attributes silent install to the Intune Management Extension
  </acceptance_criteria>
  <done>Stage-6 VPP claims rewritten to PKG/LOB, silent-install correctly attributed to IME, orphaned VPP quick-ref row removed, line 326 exemplar and line 382 See-Also untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Correct Stage-4 SSO-extension group type to user group, bump stamps, add version-history row (ACC-02, D-05, D-03)</name>
  <files>docs/macos-lifecycle/00-ade-lifecycle.md</files>
  <read_first>
    - docs/macos-lifecycle/00-ade-lifecycle.md (read lines 1-7 frontmatter, lines 200-252 Stage-4 Watch-Out bullets, lines 415-420 Version History table)
    - .planning/phases/96-surgical-conflict-fixes/96-RESEARCH.md lines 148-168 (verbatim line 250 bullet + version-history row text) and lines 77-88 (target stamps)
  </read_first>
  <action>
    Three edits within `docs/macos-lifecycle/00-ade-lifecycle.md`.

    Edit A (line ~250, Stage-4 "Watch Out For" bullet about pre-Entra-screen SSO-extension delivery): Change the substring "must be assigned to a static device group" to "must be assigned to a static user group". This is the single group-type correction (D-05) — the SSO extension Settings Catalog profile targets users via user affinity, not devices. Do NOT alter the rest of the bullet (the EnableRegistrationDuringSetup wording, the "Try Again" guidance, and the Stage-7 cross-reference all stay as-is). Leave the Stage-3 enrollment-profile device/serial targeting elsewhere in the file unchanged — that is a distinct, correct device-group reference.

    Edit B (frontmatter, lines ~1-7): Set `last_verified: 2026-06-28` and `review_by: 2026-09-28` (was 2026-06-20 / 2026-09-20). Preserve the +3-month / same-day-of-month invariant.

    Edit C (Version History table, lines ~415-420): Add a new row immediately below the table header/separator (most-recent-first), documenting the Phase 96 change. Use date `2026-06-28`, change text describing ACC-01 and ACC-02 work: corrected Stage-6 Company Portal VPP/LOB claims (lines 309, 319), removed orphaned VPP glossary quick-ref row (line 411), and corrected Stage-4 SSO-extension policy group type from device to user (line 250); author column `--`.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -qF "static user group" docs/macos-lifecycle/00-ade-lifecycle.md && grep -c "static device group" docs/macos-lifecycle/00-ade-lifecycle.md | grep -qx 0 && grep -qF "last_verified: 2026-06-28" docs/macos-lifecycle/00-ade-lifecycle.md && grep -qF "review_by: 2026-09-28" docs/macos-lifecycle/00-ade-lifecycle.md && grep -qE "\| 2026-06-28 \| Phase 96" docs/macos-lifecycle/00-ade-lifecycle.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `00-ade-lifecycle.md` contains `static user group`
    - `00-ade-lifecycle.md` no longer contains `static device group`
    - `00-ade-lifecycle.md` frontmatter contains `last_verified: 2026-06-28`
    - `00-ade-lifecycle.md` frontmatter contains `review_by: 2026-09-28`
    - `00-ade-lifecycle.md` contains a Version History row beginning `| 2026-06-28 | Phase 96`
  </acceptance_criteria>
  <done>Stage-4 bullet corrected to user group, frontmatter stamps bumped to 2026-06-28 / 2026-09-28, Phase 96 version-history row added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only edit. No code, no auth, no data handling, no user input, no trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Information accuracy/integrity | Edited Stage-6 / Stage-4 prose | mitigate | Acceptance criteria assert exact grep-able strings against RESEARCH.md verbatim quotes, so a wrong-direction edit (e.g., leaving a VPP claim or mis-correcting the group type) fails verification before ship |

No high-severity application-security threats apply: this phase introduces no authentication, access control, cryptography, network surface, or user-input handling. ASVS L1 has no applicable controls for prose-only edits.
</threat_model>

<verification>
- `bash -c 'grep -c "static device group" docs/macos-lifecycle/00-ade-lifecycle.md'` returns 0
- VPP quick-ref row absent; line 326 exemplar and line 382 See-Also present
- Frontmatter stamps read 2026-06-28 / 2026-09-28; Phase 96 version-history row present
</verification>

<success_criteria>
ROADMAP Success Criteria 1 and 2 satisfied: guide 00 no longer describes Company Portal as VPP/Apps-and-Books deployable at Stage-4/Stage-6, the self-contradiction is removed, and line ~250 states a static user group.
</success_criteria>

<output>
Create `.planning/phases/96-surgical-conflict-fixes/96-01-SUMMARY.md` when done
</output>
