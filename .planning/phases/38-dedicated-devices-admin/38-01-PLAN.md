---
phase: 38-dedicated-devices-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-android/05-dedicated-devices.md
autonomous: true
requirements: [AEDED-01, AEDED-02, AEDED-03]
tags: [android, dedicated, cosu, kiosk, mhs, exit-pin, frp, intune, docs]

must_haves:
  truths:
    - "An admin following docs/admin-setup-android/05-dedicated-devices.md can configure a Dedicated (kiosk/COSU) enrollment profile for any of the four scenarios (single-app kiosk, multi-app kiosk MHS, digital signage, Entra shared device mode), reads a persona callout naming Intune Admin + LOB Operations Owner BEFORE any Intune steps, and reaches enrollment configuration via a hybrid orientation paragraph that cross-links Phase 36 #enrollment-profile rather than restating COBO mechanics (AEDED-01 SC1 + PITFALL 7)."
    - "An admin reading the Exit-kiosk PIN synchronization H2 (D-02) sees the verbatim SC2 lock phrase 'configured identically in both' — at minimum once in the H2 opening blockquote and once in each of the two inline reminders at the device-restrictions and MHS app-config settings — and sees the verbatim error string 'A PIN to exit kiosk mode has not been set by your IT admin' tagged with a [MEDIUM: MS Q&A community, last_verified YYYY-MM-DD] source-confidence marker (AEDED-02 SC2)."
    - "An admin reading the Android 15 FRP and re-provisioning H2 (D-04) sees the 3-pathway behavior matrix (Settings reset = no FRP for Dedicated; Recovery/bootloader = FRP applies; Intune wipe = no FRP) tagged with a [HIGH: MS Learn ref-corporate-methods, last_verified YYYY-MM-DD] source-confidence marker, with EFRP configuration cross-linked to Phase 36 #configure-efrp rather than restated (AEDED-03 SC3 + Phase 36 D-05 'no pre-empting' guard)."
    - "The Provisioning method choice section references Phase 34 02-provisioning-methods.md#dedicated-cosu (filtered Dedicated row) without restating the 5x4 matrix, and the Zero-Touch subsection carries a verbatim KME/ZT Samsung mutual-exclusion callout cross-linking Phase 35 02-zero-touch-portal.md#kme-zt-mutual-exclusion plus a D-15 Phase 39 boundary note (AEDED-01 SC4 + D-11 + D-15 + Anti-Pattern 1)."
    - "All 11 mandatory <a id='...'></a> HTML-id anchors per D-08 are present (#audience-and-stakeholders, #scenarios, #prerequisites, #enrollment-profile, #enrollment-token, #provisioning-method-choice, #exit-kiosk-pin-synchronization, #exit-kiosk-pin, #android-15-frp-reprovisioning, #what-breaks, #renewal-maintenance), with #exit-kiosk-pin-synchronization and #exit-kiosk-pin co-located on the same <a> line per D-08 dual-anchor syntax."
    - "The Entra shared device mode vs multi-app kiosk SC5 disambiguation appears in BOTH the Scenarios table 'User identity model' column AND the '### How to choose' routing paragraph using D-13 locked phrasing 'Multi-app kiosk = curated app set on a device with no per-user identity. Entra shared device mode = curated app set on a device where multiple workers sign in/out with their own Entra credentials.' (AEDED-01 SC5 + D-13)."
    - "Every behavioral assertion carries an inline version tag (Android 8.0+ for Dedicated minimum at Prerequisites; section-level 'Applies to Android 15+' on D-04 H2 covering all 3 pathways per F-4A-02 adjudication), and the doc has correct frontmatter per D-18 (platform: Android, audience: admin, applies_to: Dedicated, last_verified: 2026-04-22, review_by: 2026-06-21) with zero occurrences of 'SafetyNet' and zero occurrences of 'supervision' as an Android management term."
  artifacts:
    - path: "docs/admin-setup-android/05-dedicated-devices.md"
      provides: "Dedicated (kiosk/COSU) admin guide: D-06 14-section H2 structure (frontmatter -> platform gate -> Platform note banner -> Key Concepts (with #audience-and-stakeholders persona) -> Scenarios -> Prerequisites -> Enrollment profile (D-03 hybrid) -> Enrollment token -> Provisioning method choice (filtered-row Phase 34 + KME/ZT + Phase 39 boundary) -> Exit-kiosk PIN synchronization (D-02 SC2 H2) -> Android 15 FRP and re-provisioning (D-04 SC3 H2) -> What-breaks inline + #what-breaks umbrella -> Renewal/Maintenance -> optional For L1 helpdesk agents); 4-row D-01 Scenarios comparison table; D-04 3-pathway FRP behavior breakdown; D-12 source-confidence markers at 4+ MEDIUM/HIGH locations; D-19 glossary first-use cross-links to _glossary-android.md anchors"
      min_words: 3200
      max_words: 4200
      contains_anchors: ["audience-and-stakeholders", "scenarios", "prerequisites", "enrollment-profile", "enrollment-token", "provisioning-method-choice", "exit-kiosk-pin-synchronization", "exit-kiosk-pin", "android-15-frp-reprovisioning", "what-breaks", "renewal-maintenance"]
      contains_phrases_min: ["configured identically in both", "A PIN to exit kiosk mode has not been set by your IT admin", "Multi-app kiosk = curated app set on a device with no per-user identity", "Entra shared device mode = curated app set on a device where multiple workers sign in/out with their own Entra credentials", "Applies to Android 15+", "Android 8.0+", "com.microsoft.launcher.enterprise", "Knox Mobile Enrollment (KME) or Zero-Touch — never both", "describing FRP behavior during factory-reset re-provisioning"]
      contains_phrases_zero: ["SafetyNet", "supervision mode", "supervised Android"]
      contains_marker_regex_min_4: '\[(HIGH|MEDIUM|LOW)(: [A-Za-z ]+)?(, last_verified \d{4}-\d{2}-\d{2})?\]'
  key_links:
    - from: "docs/admin-setup-android/05-dedicated-devices.md#prerequisites"
      to: "docs/admin-setup-android/01-managed-google-play.md#bind-mgp"
      via: "markdown cross-link (Phase 35 MGP binding prereq, never restated)"
      pattern: "01-managed-google-play\\.md#bind-mgp"
    - from: "docs/admin-setup-android/05-dedicated-devices.md#enrollment-profile"
      to: "docs/admin-setup-android/03-fully-managed-cobo.md#enrollment-profile"
      via: "markdown cross-link (D-03 hybrid orientation: Phase 36 canonical step-by-step)"
      pattern: "03-fully-managed-cobo\\.md#enrollment-profile"
    - from: "docs/admin-setup-android/05-dedicated-devices.md#enrollment-token"
      to: "docs/admin-setup-android/03-fully-managed-cobo.md#enrollment-token"
      via: "markdown cross-link (Phase 36 token mechanics canonical)"
      pattern: "03-fully-managed-cobo\\.md#enrollment-token"
    - from: "docs/admin-setup-android/05-dedicated-devices.md#android-15-frp-reprovisioning"
      to: "docs/admin-setup-android/03-fully-managed-cobo.md#configure-efrp"
      via: "markdown cross-link (D-04 closing cross-link; Phase 36 D-05 no-pre-empting guard)"
      pattern: "03-fully-managed-cobo\\.md#configure-efrp"
    - from: "docs/admin-setup-android/05-dedicated-devices.md#android-15-frp-reprovisioning"
      to: "docs/android-lifecycle/03-android-version-matrix.md#android-15-breakpoint"
      via: "markdown cross-link (Phase 34 version matrix narrative)"
      pattern: "03-android-version-matrix\\.md#android-15-breakpoint"
    - from: "docs/admin-setup-android/05-dedicated-devices.md#provisioning-method-choice"
      to: "docs/android-lifecycle/02-provisioning-methods.md#dedicated-cosu"
      via: "filtered-row reference (Anti-Pattern 1 guard; matrix lives in canonical doc)"
      pattern: "02-provisioning-methods\\.md#dedicated-cosu"
    - from: "docs/admin-setup-android/05-dedicated-devices.md#provisioning-method-choice (Zero-Touch subsection)"
      to: "docs/admin-setup-android/02-zero-touch-portal.md#kme-zt-mutual-exclusion"
      via: "D-11 verbatim KME/ZT Samsung callout"
      pattern: "02-zero-touch-portal\\.md#kme-zt-mutual-exclusion"
    - from: "docs/admin-setup-android/05-dedicated-devices.md (Platform note banner + Key Concepts terminology)"
      to: "docs/_glossary-android.md#dedicated"
      via: "D-19 glossary first-use cross-link (note: glossary heading is 'Dedicated', auto-anchor #dedicated; no separate #cosu heading exists)"
      pattern: "_glossary-android\\.md#dedicated"
    - from: "docs/admin-setup-android/05-dedicated-devices.md (Key Concepts terminology)"
      to: "docs/_glossary-android.md#managed-home-screen"
      via: "D-19 glossary first-use cross-link"
      pattern: "_glossary-android\\.md#managed-home-screen"
    - from: "docs/admin-setup-android/05-dedicated-devices.md (Key Concepts terminology)"
      to: "docs/_glossary-android.md#entra-shared-device-mode"
      via: "D-19 glossary first-use cross-link (SC5 disambiguation target)"
      pattern: "_glossary-android\\.md#entra-shared-device-mode"
---

<objective>
Author `docs/admin-setup-android/05-dedicated-devices.md` — the third mode-specific corporate-ownership admin guide in the v1.4 Android Enterprise milestone. The guide covers Dedicated (kiosk/COSU) device enrollment with the dual-stakeholder persona callout (Intune Admin + LOB Operations Owner per PITFALL 7), the 4-scenario overview (single-app / multi-app / digital signage / Entra shared device mode), enrollment-profile deltas from Phase 36 COBO via the D-03 hybrid orientation pattern, the MHS exit-PIN synchronization H2 (AEDED-02 SC2 — top repeated-escalation pattern for Dedicated devices), and the Android 15 FRP re-provisioning H2 (AEDED-03 SC3 — 3-pathway behavior matrix distinct from COBO re-enrollment).

Purpose: Satisfy AEDED-01 / AEDED-02 / AEDED-03. Establish 11 stable HTML-id anchors per D-08 that Phase 40 (L1 routing) and Phase 41 (L2 investigation) will consume without renaming. Honor Anti-Pattern 1 by cross-linking Phase 34 provisioning-method matrix and Phase 36 enrollment-profile / EFRP / token mechanics rather than restating them.

Output: Exactly ONE NEW file — `docs/admin-setup-android/05-dedicated-devices.md`. ZERO modifications to any other file (D-17 shared-file guard).

Scope discipline (what this plan does NOT do):
- Does NOT modify Phase 34/35/36/37 docs (D-17 hard guard)
- Does NOT restate Phase 36 enrollment-profile step-by-step or EFRP config steps (Anti-Pattern 1; Phase 36 D-05 no-pre-empting)
- Does NOT restate the 5x4 provisioning-method matrix (Anti-Pattern 1; filtered-row link only)
- Does NOT carry corporate-scale ZTE content (Phase 39 owns; D-15 boundary note only)
- Does NOT carry a Dedicated L1 runbook (PITFALL 7 line 197; Phase 40 triage-tree routes to L2 or LOB ops owner)
- Does NOT define Android terms inline (D-19 glossary first-use cross-link pattern)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-dedicated-devices-admin/38-CONTEXT.md
@.planning/phases/38-dedicated-devices-admin/38-RESEARCH.md
@.planning/phases/38-dedicated-devices-admin/38-PATTERNS.md
@.planning/phases/38-dedicated-devices-admin/38-VALIDATION.md
@docs/admin-setup-android/03-fully-managed-cobo.md
@docs/admin-setup-android/04-byod-work-profile.md
@docs/admin-setup-android/02-zero-touch-portal.md
@docs/admin-setup-android/01-managed-google-play.md
@docs/admin-setup-android/00-overview.md
@docs/_glossary-android.md
@docs/android-lifecycle/02-provisioning-methods.md
@docs/android-lifecycle/03-android-version-matrix.md
@docs/_templates/admin-template-android.md
@CLAUDE.md

<interfaces>
<!-- Verified anchor inventory available for Phase 38 cross-links. Executor MUST use these EXACT anchor IDs. Do NOT invent new anchors or rename. -->

Phase 36 `docs/admin-setup-android/03-fully-managed-cobo.md` (shipped 2026-04-21):
- L24  `<a id="key-concepts">`
- L37  `<a id="prerequisites">`
- L44  `<a id="entra-join-prerequisite">` (inline)
- L51  `<a id="ca-exclusion-intune-app">`
- L57  `<a id="cope-migration">`
- L68  `<a id="enrollment-profile">`
- L99  `<a id="enrollment-token">`
- L127 `<a id="provisioning-method-choice">`
- L168 `<a id="android-15-frp">`
- L177 `<a id="configure-efrp">`
- L197 `<a id="what-breaks">`
- L218 `<a id="renewal-maintenance">`

Phase 36 KME/ZT verbatim callout (lines 162-163, REUSE EXACT WORDING per D-11):
```
> ⚠️ **Samsung admins:** Choose Knox Mobile Enrollment (KME) or Zero-Touch — never both. Configuring both on the same devices causes out-of-sync enrollment state on Samsung hardware. Full KME coverage is deferred to v1.4.1. See [02-zero-touch-portal.md#kme-zt-mutual-exclusion](02-zero-touch-portal.md#kme-zt-mutual-exclusion) for the mutual-exclusion record and [_glossary-android.md#zero-touch-enrollment](../_glossary-android.md#zero-touch-enrollment) for the Zero-Touch definition and the iOS ADE cross-platform analog.
```

Phase 35 `docs/admin-setup-android/02-zero-touch-portal.md` (shipped 2026-04-22):
- L23  `<a id="prerequisites">`
- L32  `<a id="step-0-reseller">`
- L43  `<a id="create-zt-account">`
- L56  `<a id="link-zt-to-intune">`
- L88  `<a id="dpc-extras-json">`
- L119 `<a id="kme-zt-mutual-exclusion">`
- L137 `<a id="renewal-maintenance">`

Phase 35 `docs/admin-setup-android/01-managed-google-play.md` (shipped 2026-04-22):
- L80  `<a id="bind-mgp">`

Phase 34 `docs/android-lifecycle/03-android-version-matrix.md` (shipped 2026-04-21):
- L23  `<a id="dedicated">` (Dedicated row anchor in version matrix)
- L67  `<a id="android-15-breakpoint">` (Android 15 FRP breakpoint heading anchor)

Phase 34 `docs/android-lifecycle/02-provisioning-methods.md` (shipped 2026-04-21):
- L27  `<a id="dedicated-cosu">` (filtered Dedicated row in 5x4 matrix — USE THIS, NOT `#dedicated`)

Phase 34 `docs/_glossary-android.md` (shipped 2026-04-21) — heading auto-anchors (NO `<a id>` tags):
- `#byod` (### BYOD, L21)
- `#zero-touch-enrollment` (### Zero-Touch Enrollment, L33)
- `#dedicated` (### Dedicated, L53) — NOTE: there is NO separate `#cosu` heading; use `#dedicated` for both COSU and Dedicated first-use links
- `#fully-managed` (### Fully Managed, L59)
- `#work-profile` (### Work Profile, L69)
- `#dpc` (### DPC, L96)
- `#entra-shared-device-mode` (### Entra Shared Device Mode, L110)
- `#managed-google-play` (### Managed Google Play, L116)
- `#managed-home-screen` (### Managed Home Screen, L130)
- `#play-integrity` (### Play Integrity, L136)
- `#afw-setup` (`<a id="afw-setup">` at L83)

Important — anchor name corrections vs CONTEXT.md / PATTERNS.md drafting language:
- CONTEXT D-19 says "_glossary-android.md anchors `#dedicated-device`, `#cosu`, ..." — `#dedicated-device` and `#cosu` DO NOT EXIST. Use `#dedicated` for both Dedicated-device and COSU first-use cross-links. Reason: glossary uses heading slugs, headings are `### Dedicated` and there is no separate `### COSU` heading.
- CONTEXT D-19 says "_glossary-android.md anchors `#lock-task-mode`" — there is NO `### Lock Task Mode` heading in glossary. Either omit the Lock Task Mode glossary cross-link or define it inline (planner discretion: omit cross-link; describe inline at first use). Confirmed via `grep "^#" docs/_glossary-android.md`.
- PATTERNS line 153 says "Cross-link: `docs/android-lifecycle/03-android-version-matrix.md#dedicated`" — this anchor `#dedicated` IS at version-matrix L23 (verified). Use it for Prerequisites Android version cross-link.
- For the Provisioning method choice filtered-row link, USE `#dedicated-cosu` (verified at provisioning-methods L27), not `#dedicated`.

Frontmatter target values for this execution (D-18):
```yaml
---
last_verified: 2026-04-22
review_by: 2026-06-21
audience: admin
platform: Android
applies_to: Dedicated
---
```
(review_by = last_verified + 60 days per D-07 / Phase 36 D-13 / Phase 37 D-15 precedent)

Source-confidence marker regex (D-12 / Phase 37 D-11):
```
\[(HIGH|MEDIUM|LOW)(: [A-Za-z ]+)?(, last_verified \d{4}-\d{2}-\d{2})?\]
```

4 mandatory marker locations in Phase 38:
1. MHS exit-PIN H2 opening blockquote: `[MEDIUM: MS Q&A community, last_verified 2026-04-22]`
2. Verbatim error string "A PIN to exit kiosk mode has not been set by your IT admin": `[MEDIUM: MS Q&A community, last_verified 2026-04-22]`
3. Android 15 FRP 3-pathway opening blockquote: `[HIGH: MS Learn ref-corporate-methods, last_verified 2026-04-22]`
4. Dedicated default token expiry assertion in D-03 deltas: `[MEDIUM: no MS Learn statement; behavior inferred, last_verified 2026-04-22]`

(Optional 5th: each individual FRP pathway line MAY repeat the HIGH marker per D-12; canonical home is the H2 opening blockquote.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Verify upstream anchor stability prereqs</name>
  <files>
    (read-only verification; no files modified)
  </files>
  <read_first>
    - docs/admin-setup-android/03-fully-managed-cobo.md (full file — verify 8 anchors present per Phase 36 anchor stability contract)
    - docs/admin-setup-android/02-zero-touch-portal.md (full file — verify 3 anchors present per Phase 35 anchor stability contract)
    - docs/admin-setup-android/01-managed-google-play.md (lines 78-90 — verify #bind-mgp present)
    - docs/android-lifecycle/02-provisioning-methods.md (lines 25-30 — verify #dedicated-cosu present)
    - docs/android-lifecycle/03-android-version-matrix.md (lines 20-30 and 60-72 — verify #dedicated and #android-15-breakpoint present)
    - docs/_glossary-android.md (full file — confirm heading inventory: #dedicated, #managed-home-screen, #entra-shared-device-mode, #play-integrity, #dpc, #afw-setup, #fully-managed; confirm absence of #cosu and #lock-task-mode)
  </read_first>
  <action>
    Run anchor inventory verification for Phase 38 cross-link targets. This is a read-only sanity check before authoring; no docs are modified. Execute each grep below and confirm the expected output count. If any expected anchor is MISSING, STOP and report the gap to the user before proceeding to Task 2 (a missing upstream anchor invalidates the cross-link plan and must be resolved first).

    Run these grep commands verbatim and confirm each minimum count. Note: this project runs on Windows bash — these are bash/ripgrep-syntax commands runnable via the `Grep` tool or `Bash` tool.

    1. `grep -cE "<a id=\"(enrollment-profile|enrollment-token|provisioning-method-choice|android-15-frp|configure-efrp|cope-migration|what-breaks|renewal-maintenance)\"" docs/admin-setup-android/03-fully-managed-cobo.md` — MUST return >= 8 (one count per matching anchor; expected exactly 8).
    2. `grep -cE "<a id=\"(kme-zt-mutual-exclusion|link-zt-to-intune|dpc-extras-json)\"" docs/admin-setup-android/02-zero-touch-portal.md` — MUST return >= 3.
    3. `grep -c "<a id=\"bind-mgp\"" docs/admin-setup-android/01-managed-google-play.md` — MUST return == 1.
    4. `grep -c "<a id=\"dedicated-cosu\"" docs/android-lifecycle/02-provisioning-methods.md` — MUST return == 1 (this is the filtered-row anchor used by D-03 enrollment-profile delta cross-link AND Provisioning method choice section).
    5. `grep -c "<a id=\"android-15-breakpoint\"" docs/android-lifecycle/03-android-version-matrix.md` — MUST return == 1.
    6. `grep -c "<a id=\"dedicated\"" docs/android-lifecycle/03-android-version-matrix.md` — MUST return == 1 (Dedicated row in version matrix; used by Prerequisites Android-version cross-link).
    7. `grep -nE "^### (Dedicated|Managed Home Screen|Entra Shared Device Mode|Play Integrity|DPC|Fully Managed)$" docs/_glossary-android.md` — MUST return >= 6 lines (one per heading).
    8. `grep -cE "^### (COSU|Lock Task Mode)$" docs/_glossary-android.md` — MUST return == 0 (CONFIRMS: do NOT cross-link `#cosu` or `#lock-task-mode`; describe inline instead).

    Record the output of each command in a one-paragraph audit summary in your task output. Do NOT proceed to Task 2 if any check fails — instead, raise the discrepancy as a blocker.

    Per D-16 executor re-verification: at the start of Task 2, you will ALSO need to re-verify the Intune admin center Dedicated enrollment-profile blade path (RESEARCH Flag 4 indicated MEDIUM confidence, navigation may have shifted). For this Wave 0 task, simply NOTE that re-verification is queued; do not perform it here.

    No files are modified in this task. The output is a verbal audit report.
  </action>
  <verify>
    <automated>grep -cE "&lt;a id=\"(enrollment-profile|enrollment-token|provisioning-method-choice|android-15-frp|configure-efrp|what-breaks|renewal-maintenance|kme-zt-mutual-exclusion|bind-mgp|dedicated-cosu|android-15-breakpoint)\"" docs/admin-setup-android/03-fully-managed-cobo.md docs/admin-setup-android/02-zero-touch-portal.md docs/admin-setup-android/01-managed-google-play.md docs/android-lifecycle/02-provisioning-methods.md docs/android-lifecycle/03-android-version-matrix.md</automated>
  </verify>
  <acceptance_criteria>
    - All 7 grep commands above produce the expected counts (8, 3, 1, 1, 1, 1, >=6).
    - Grep #8 returns 0 (confirms `#cosu` and `#lock-task-mode` glossary headings DO NOT exist; executor must use `#dedicated` for first-use cross-link of both Dedicated-device and COSU terminology, and define Lock Task Mode inline rather than cross-link).
    - The audit report summary explicitly states: "All upstream anchors verified shipped; safe to author Phase 38 doc with the cross-link targets listed in <interfaces>."
    - If ANY check fails, no further task in this plan executes; the discrepancy is reported as a blocker requiring user direction (e.g., "anchor X missing in Phase 36 — plan revision needed").
  </acceptance_criteria>
  <done>
    Audit report posted with all 8 grep results; either green-light to proceed to Task 2 or explicit blocker raised.
  </done>
</task>

<task type="auto">
  <name>Task 2 (Wave 1): Author docs/admin-setup-android/05-dedicated-devices.md (Sections 1-7: frontmatter through Provisioning method choice)</name>
  <files>
    docs/admin-setup-android/05-dedicated-devices.md (CREATE NEW FILE)
  </files>
  <read_first>
    - .planning/phases/38-dedicated-devices-admin/38-CONTEXT.md (full file — locked decisions D-01 through D-20; the source of truth for shape, phrasing, and constraints)
    - .planning/phases/38-dedicated-devices-admin/38-RESEARCH.md (full file — research flag results; verbatim text patterns in `## Code Examples` section starting line 449; STACK and FEATURES references)
    - .planning/phases/38-dedicated-devices-admin/38-PATTERNS.md (full file — section-by-section pattern map with concrete excerpts from Phase 36)
    - docs/admin-setup-android/03-fully-managed-cobo.md (full file — primary structural analog; copy frontmatter pattern, platform-gate pattern, Key Concepts pattern, Prerequisites pattern, Provisioning method choice pattern; do NOT copy COBO-specific content)
    - docs/admin-setup-android/04-byod-work-profile.md (lines 1-90 — secondary analog for inline source-confidence marker placement in shipped form)
    - docs/_templates/admin-template-android.md (full file — tri-portal H4 scaffold)
    - docs/android-lifecycle/02-provisioning-methods.md (verify #dedicated-cosu row content for filtered-row link)
    - docs/_glossary-android.md (verify exact heading slugs for first-use cross-links per <interfaces> block)
  </read_first>
  <action>
    Create the NEW file `docs/admin-setup-android/05-dedicated-devices.md` and write Sections 1 through 7 of the D-06 14-section structure (frontmatter through `## Provisioning method choice`). Save the file at the end of this task with these sections complete; Sections 8-14 will be added in Task 3 with an Edit on the same file.

    **WRITE THESE SECTIONS IN ORDER:**

    **Section 1 — Frontmatter (D-18, D-06 step 1)**

    Use this exact YAML block (last_verified = today's date 2026-04-22; review_by = +60 days = 2026-06-21):
    ```yaml
    ---
    last_verified: 2026-04-22
    review_by: 2026-06-21
    audience: admin
    platform: Android
    applies_to: Dedicated
    ---
    ```

    **Section 2 — Platform gate blockquote (D-06 step 2)**

    Adapt Phase 36 lines 9-13 pattern. Required content:
    - Line 1 of blockquote: scope summary mentioning Dedicated (kiosk/COSU), the 4 scenarios (single-app / multi-app / digital signage / Entra shared device mode), enrollment profile, MHS exit-PIN sync, Android 15 FRP re-provisioning behavior. (Applies to Android 8.0+).
    - Cross-link line: `For Android Enterprise Fully Managed (COBO) enrollment, see [03-fully-managed-cobo.md](03-fully-managed-cobo.md#key-concepts).`
    - Cross-link line: `For BYOD Work Profile, see [04-byod-work-profile.md](04-byod-work-profile.md#key-concepts).`
    - Cross-link line: `For iOS/iPadOS admin setup, see [iOS Admin Guides](../admin-setup-ios/00-overview.md).`
    - Cross-link line: `For macOS admin setup, see [macOS Admin Guides](../admin-setup-macos/00-overview.md).`
    - Cross-link line: `For Android provisioning terminology, see the [Android Enterprise Provisioning Glossary](../_glossary-android.md).`

    Then a `# Android Enterprise Dedicated (kiosk/COSU) Admin Setup` H1 followed by a 1-2 sentence intro paragraph naming the dual-stakeholder audience and the four scenarios.

    Then a "How to use" line: `**How to use:** Intune administrators read linearly. L1 Service Desk and L2 Desktop Engineering use the Phase 40 and Phase 41 runbooks (not this guide). LOB Operations Owners read [Audience and stakeholders](#audience-and-stakeholders) for the persona-specific responsibilities.`

    **Section 3 — Platform note banner (D-10, D-06 step 3, F-XCUT-08 closure)**

    Place AFTER platform gate, BEFORE `## Key Concepts` H2. Use this exact wording (D-10 verbatim):
    ```markdown
    > **Platform note:** "Dedicated device" in Android Enterprise (COSU — Corporate-Owned, Single-Use) is single-purpose kiosk hardware with no per-user identity. This is structurally distinct from iOS Shared iPad (multi-user shared identity) and Windows Shared PC (multi-user fast-switch). For cross-platform comparison, see [Android dedicated device disambiguation](../_glossary-android.md#dedicated).
    ```
    NOTE: glossary anchor is `#dedicated`, NOT `#dedicated-device` (the `#dedicated-device` form referenced in CONTEXT D-10 is incorrect — see <interfaces> block; verified glossary headings list).

    **Section 4 — `## Key Concepts` H2 (D-06 step 4)**

    Open with: `<a id="key-concepts"></a>` then `## Key Concepts`.

    Inside Key Concepts, add TWO H3 subsections:

    Subsection 4a — `### Audience and stakeholders` (D-01 persona, ~150 words):
    - MUST be prefixed by: `<a id="audience-and-stakeholders"></a>` (D-08 mandatory anchor #1)
    - First sentence MUST be the PITFALL 7 line 194 verbatim core wording: "Audience: Intune Admin + Line-of-Business Operations Owner. The operations owner defines the locked app(s); the Intune admin configures the enrollment and kiosk policy."
    - Then ~120 additional words naming concrete responsibilities for both roles. Examples (use at least 2 of these or equivalents): warehouse-floor manager picking the inventory-scanner app while the Intune admin configures the enrollment profile and Lock-Task-Mode allowlist; retail operations lead picking the customer-self-service app set while the Intune admin configures multi-app kiosk + MHS app config; signage operations lead picking the signage app while the Intune admin configures Dedicated enrollment + screensaver mode.
    - End with the D-05 disclaimer: "The LOB Operations Owner is a business stakeholder, not an Intune RBAC role; they do not need Intune admin permissions."

    Subsection 4b — `### Terminology` (~100-150 words):
    - First-use glossary cross-links per D-19 (in this subsection, introduce each term with a cross-link to the glossary):
      - `[Dedicated](../_glossary-android.md#dedicated)` — combined Dedicated/COSU mode definition
      - `[Managed Home Screen (MHS)](../_glossary-android.md#managed-home-screen)`
      - `[Entra Shared Device Mode](../_glossary-android.md#entra-shared-device-mode)`
      - `[Play Integrity](../_glossary-android.md#play-integrity)` (Phase 38 MUST use Play Integrity, never SafetyNet)
      - `[DPC (Device Policy Controller)](../_glossary-android.md#dpc)`
      - `[afw#setup](../_glossary-android.md#afw-setup)`
    - Lock Task Mode: Define INLINE in 1 sentence (no glossary cross-link — `#lock-task-mode` does NOT exist in glossary). Suggested wording: "Lock Task Mode is the Android OS-level mechanism that pins one app to the foreground and prevents user navigation away; Dedicated single-app kiosks use this directly without MHS."

    Cross-platform note (Phase 36 D-08 precedent reapplied): one short blockquote disambiguating Dedicated from iOS Shared iPad and Windows Shared PC, cross-linking the same `_glossary-android.md#dedicated` target. (May reuse the D-10 banner content concept but not duplicate verbatim.)

    **Section 5 — `## Scenarios` H2 (D-01 + D-13 + D-14, D-06 step 5)**

    Open with: `<a id="scenarios"></a>` then `## Scenarios` (D-08 mandatory anchor #2).

    Then a brief intro sentence naming the 4 Dedicated scenarios.

    Then this exact 4-row table (column order locked per D-01: Scenario | Locking style | Token type | User identity model | Example deployment). Use the wording from PATTERNS lines 109-113:

    ```markdown
    | Scenario | Locking style | Token type | User identity model | Example deployment |
    |---|---|---|---|---|
    | Single-app kiosk | Android Lock Task Mode (one app pinned) | Standard | No user identity | Warehouse barcode scanner |
    | Multi-app kiosk (MHS) | Managed Home Screen launcher (`com.microsoft.launcher.enterprise`) | Standard | No user identity | Retail employee device with curated app set |
    | Digital signage (userless) | MHS or Lock Task Mode; screen-saver mode | Standard | No user identity | Lobby display or menu board |
    | Entra shared device mode | MHS with sign-in enabled | Corporate-owned dedicated device with Microsoft Entra ID shared mode | Per-user Entra sign-in/sign-out | Multi-shift frontline worker device |
    ```

    Then `### How to choose` H3 (~100 words routing paragraph) which MUST contain BOTH locked sentences verbatim per D-13:
    - "Multi-app kiosk = curated app set on a device with no per-user identity."
    - "Entra shared device mode = curated app set on a device where multiple workers sign in/out with their own Entra credentials."
    Plus 1-2 additional sentences guiding readers to single-app vs multi-app vs signage decision (e.g., "Single-app kiosks suit one-task workflows like barcode scanning; digital signage typically uses MHS in screensaver mode for unattended displays.").

    Add a brief D-14 scope reminder sentence: "Only multi-app kiosk and digital signage scenarios use Managed Home Screen (MHS); single-app kiosk uses Lock Task Mode directly. The MHS exit-PIN synchronization requirement (see [Exit-kiosk PIN synchronization](#exit-kiosk-pin-synchronization)) applies to MHS scenarios only."

    **Section 6 — `## Prerequisites` H2 (D-06 step 6)**

    Open with: `<a id="prerequisites"></a>` then `## Prerequisites` (D-08 mandatory anchor #3).

    Adapt Phase 36 lines 37-55 pattern but with these Dedicated-specific changes:

    Hard prerequisites (checklist):
    - Microsoft Intune Plan 1+ with Intune Administrator role.
    - Managed Google Play (MGP) binding complete — cross-link `[01-managed-google-play.md#bind-mgp](01-managed-google-play.md#bind-mgp)`.
    - Factory-reset or new device — Dedicated requires out-of-box state.
    - **Android 8.0+** (Dedicated minimum, NOT Android 10.0+ which is COBO's minimum). Cross-link `[03-android-version-matrix.md#dedicated](../android-lifecycle/03-android-version-matrix.md#dedicated)`.
    - Google Mobile Services (GMS) connectivity on the device and the out-of-box network.
    - Static Entra device group available for enrollment-time grouping (PITFALL 7 requirement; dynamic groups lag during burst provisioning per D-03 delta 3).

    Tenant-conditional prerequisites:
    - Zero-Touch portal binding (only required for Dedicated-via-ZTE provisioning) — cross-link `[02-zero-touch-portal.md#link-zt-to-intune](02-zero-touch-portal.md#link-zt-to-intune)`.
    - For Entra shared device mode scenario: users must have Entra licenses and sign-in apps must be MSAL-integrated (per glossary `#entra-shared-device-mode`).

    Note (compare-with-COBO): "Dedicated mode does NOT require the Conditional Access Microsoft-Intune-cloud-app exclusion that COBO's Chrome-tab Entra sign-in requires; Dedicated standard tokens have no per-user Entra join at the device level. Entra shared device mode token requires Entra at the per-user app sign-in layer, not at device enrollment."

    What-breaks blockquote at section end:
    > **What breaks if misconfigured:** MGP not bound → MHS app cannot be assigned, multi-app and digital signage scenarios fail at the "Installing required apps" step. Recovery: re-bind MGP per [01-managed-google-play.md#bind-mgp](01-managed-google-play.md#bind-mgp). Symptom appears in: Intune admin center (Managed Google Play status disconnected) and on the device during enrollment (apps fail to install).

    **Section 7 — `## Provisioning method choice` H2 (D-06 step 9, D-11, D-15)**

    NOTE: Although D-06 lists this as step 9, AUTHORING ORDER places it as Section 7 here because Sections 7 (Enrollment profile, D-06 step 7) and 8 (Enrollment token, D-06 step 8) belong logically to Task 3 (Enrollment-profile + token + landmarks). This task closes with the standalone Provisioning method choice section that has no Phase 36 EFRP entanglement. Renumbering: Task 2 writes frontmatter→Platform gate→banner→Key Concepts→Scenarios→Prerequisites→Provisioning method choice. Task 3 writes Enrollment profile + Enrollment token + Exit-kiosk PIN sync + Android 15 FRP re-provisioning + What-breaks + Renewal/Maintenance + optional L1 helpdesk H2 and re-orders the body so D-06 step 7 (Enrollment profile) appears before D-06 step 9 (Provisioning method choice) per CONTEXT.md D-06 final layout.

    ACTUALLY — to keep the file in D-06 order at end-of-plan, write the file in correct D-06 order. So Section 7 in this task is `## Enrollment profile` per D-06 step 7. CORRECTING:

    **Section 7 — `## Enrollment profile` H2 (D-03 hybrid, D-06 step 7)**

    Open with: `<a id="enrollment-profile"></a>` then `## Enrollment profile` (D-08 mandatory anchor #4).

    Use the D-03 hybrid orientation pattern from PATTERNS lines 164-197 / RESEARCH lines 463-475. Required content:

    Orientation paragraph (~80 words) — MUST cross-link Phase 36 canonical:
    ```markdown
    Dedicated profile creation follows the same Intune flow as Fully Managed (COBO) — see [Phase 36 enrollment profile section](03-fully-managed-cobo.md#enrollment-profile) for the canonical step-by-step (Devices → Device onboarding → Enrollment → Android tab → Enrollment Profiles → Corporate-owned dedicated devices). The Dedicated-specific deltas below explain what differs.
    ```

    NOTE: "Devices → Device onboarding → Enrollment" reflects current MS Learn navigation per RESEARCH Flag 4. Per D-16, executor MUST re-verify this navigation path at the actual Intune admin center URL `https://go.microsoft.com/fwlink/?linkid=2109431` (the URL is stable; the breadcrumb may shift). If you find the navigation has shifted, ADJUST THE PARENTHETICAL TEXT AND ADD AN INLINE MEDIUM CONFIDENCE MARKER:
    `[MEDIUM: MS Learn setup-dedicated, last_verified 2026-04-22; Intune unified admin center may rename breadcrumbs]` immediately after the parenthetical, then continue.

    Then 4 inline deltas (~280 words total — ~70 words each), each ending with a what-breaks blockquote per PITFALL 2:

    **Delta 1: Token type selection** — Standard token vs "Corporate-owned dedicated device with Microsoft Entra ID shared mode" token. Decision criterion: per-user Entra sign-in needed → Entra shared device mode token; otherwise Standard. Cross-link to [Scenarios](#scenarios) routing paragraph.
    > **What breaks if misconfigured:** Wrong token type means the chosen scenario is structurally unavailable. Example: choosing Standard for a multi-shift sign-in workflow eliminates per-user identity capability; all users share the same device account. Recovery: revoke the token, recreate the enrollment profile with the correct token type, redistribute the new QR/enrollment artifact. Symptom appears in: device runtime (sign-in flow missing or Entra prompts absent).

    **Delta 2: Static Entra device group assignment** — REQUIRED. Dynamic groups lag during burst provisioning when device group membership is computed AFTER enrollment but the token-check fires AT enrollment.
    > **What breaks if misconfigured:** Dynamic device group used → token-check enrollment failures during burst provisioning when device group membership lags behind enrollment timing. Recovery: change the enrollment profile assignment to a static Entra device group; re-issue tokens if needed. Symptom appears in: Intune admin center (devices stuck at enrollment with token-check failures); on device (enrollment flow blocked at token-validation step).

    **Delta 3: Managed Home Screen (MHS) app config requirement (multi-app + digital signage scenarios only)** — The Managed Home Screen app (`com.microsoft.launcher.enterprise`) MUST be assigned as **Required** to the device group. (D-14 scope: this delta does NOT apply to single-app kiosk scenarios.) Cross-link to multi-app row in [Scenarios](#scenarios) and forward-reference [Exit-kiosk PIN synchronization](#exit-kiosk-pin-synchronization) for the related two-policy PIN sync requirement: "the exit-kiosk PIN MUST be configured identically in both this MHS app config policy AND the device restrictions profile."
    > **What breaks if misconfigured:** MHS app not assigned (or assigned as Available rather than Required) → device boots to standard Android launcher instead of MHS; no kiosk lockdown applied. Recovery: in Intune admin center, set the MHS app assignment to Required for the Dedicated device group; force a policy sync. Symptom appears in: device home screen (standard Android launcher visible instead of MHS).

    **Delta 4: Token expiry and QR rotation discipline (D-09)** — Dedicated tokens require an expiry date (configurable up to 65 years; no "never expires" option exists, unlike COBO's Default token type). Append the source-confidence marker AFTER the assertion:
    `[MEDIUM: no MS Learn statement on default; behavior inferred from UI field description, last_verified 2026-04-22]`
    Add the QR rotation discipline note: "Token expiry rotation breaks any printed/laminated/posted QR enrollment artifacts. When rotating tokens, generate new QR + redistribute to all field locations using the old token. Plan rotation cadence to align with operational refresh windows." Cross-link to Phase 36 `[Phase 36 enrollment token section](03-fully-managed-cobo.md#enrollment-token)` for general token mechanics.
    > **What breaks if misconfigured:** Printed/laminated QR artifacts expire silently when their underlying token expires; field provisioning succeeds at scan but fails at token-exchange. Recovery: revoke old token, generate new token with longer expiry, regenerate QR image, redistribute to field locations. Prevention: track token expiry dates centrally; rotate before expiry rather than after artifacts fail.

    Add a brief closing sentence: "Per [D-16 verification protocol], re-verify the Intune admin center navigation path at execute time — the URL `https://go.microsoft.com/fwlink/?linkid=2109431` is stable but breadcrumbs occasionally shift in unified admin center rollouts."

    File save point — Task 2 completes with the file containing Sections 1-7 (frontmatter, platform gate, banner, Key Concepts, Scenarios, Prerequisites, Enrollment profile). Approximate cumulative word count target: ~1700-2200 words (~50% of total D-07 envelope).
  </action>
  <verify>
    <automated>test -f docs/admin-setup-android/05-dedicated-devices.md && grep -cE "&lt;a id=\"(audience-and-stakeholders|scenarios|prerequisites|enrollment-profile)\"" docs/admin-setup-android/05-dedicated-devices.md && grep -c "Multi-app kiosk = curated app set on a device with no per-user identity" docs/admin-setup-android/05-dedicated-devices.md && grep -c "Audience: Intune Admin + Line-of-Business Operations Owner" docs/admin-setup-android/05-dedicated-devices.md && grep -c "03-fully-managed-cobo.md#enrollment-profile" docs/admin-setup-android/05-dedicated-devices.md && wc -w docs/admin-setup-android/05-dedicated-devices.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/admin-setup-android/05-dedicated-devices.md` exists.
    - `grep -cE "&lt;a id=\"(audience-and-stakeholders|scenarios|prerequisites|enrollment-profile)\"" docs/admin-setup-android/05-dedicated-devices.md` returns 4 (4 of 11 mandatory anchors present in this task; remaining 7 added in Task 3).
    - `grep -c "Multi-app kiosk = curated app set on a device with no per-user identity" docs/admin-setup-android/05-dedicated-devices.md` returns 1 (D-13 SC5 lock phrase #1).
    - `grep -c "Entra shared device mode = curated app set on a device where multiple workers sign in/out with their own Entra credentials" docs/admin-setup-android/05-dedicated-devices.md` returns 1 (D-13 SC5 lock phrase #2).
    - `grep -c "Audience: Intune Admin + Line-of-Business Operations Owner" docs/admin-setup-android/05-dedicated-devices.md` returns 1 (PITFALL 7 line 194 verbatim).
    - `grep -c "03-fully-managed-cobo.md#enrollment-profile" docs/admin-setup-android/05-dedicated-devices.md` returns >= 1 (D-03 hybrid cross-link).
    - `grep -c "01-managed-google-play.md#bind-mgp" docs/admin-setup-android/05-dedicated-devices.md` returns >= 1 (Prerequisites MGP cross-link).
    - `grep -c "../_glossary-android.md#dedicated" docs/admin-setup-android/05-dedicated-devices.md` returns >= 2 (Platform note banner + Terminology first-use cross-link).
    - `grep -c "../_glossary-android.md#managed-home-screen" docs/admin-setup-android/05-dedicated-devices.md` returns >= 1 (Terminology first-use).
    - `grep -c "../_glossary-android.md#entra-shared-device-mode" docs/admin-setup-android/05-dedicated-devices.md` returns >= 1 (Terminology first-use).
    - `grep -c "Android 8.0+" docs/admin-setup-android/05-dedicated-devices.md` returns >= 1 (D-19 version tag at Prerequisites).
    - `grep -c "com.microsoft.launcher.enterprise" docs/admin-setup-android/05-dedicated-devices.md` returns >= 1 (MHS package ID, Delta 3).
    - `grep -cE "\[MEDIUM:" docs/admin-setup-android/05-dedicated-devices.md` returns >= 1 (Delta 4 token expiry MEDIUM marker; D-12 marker #4).
    - `wc -w docs/admin-setup-android/05-dedicated-devices.md` returns a count between 1500 and 2400 (sections 1-7 should be ~50% of the 3200-4200 D-07 envelope; some flex allowed pending Task 3).
    - File contains H2 sections in this order: `## Key Concepts`, `## Scenarios`, `## Prerequisites`, `## Enrollment profile` (verified by `grep -nE "^## " docs/admin-setup-android/05-dedicated-devices.md` showing these in correct order).
    - Frontmatter contains all five D-18 keys (`platform: Android`, `audience: admin`, `applies_to: Dedicated`, `last_verified: 2026-04-22`, `review_by: 2026-06-21`) — verified by `grep -cE "^(platform|audience|applies_to|last_verified|review_by):" docs/admin-setup-android/05-dedicated-devices.md` returning 5.
    - Frontmatter includes `applies_to: Dedicated` (single string per D-18) — verified by `grep -c "^applies_to: Dedicated$" docs/admin-setup-android/05-dedicated-devices.md` returning 1.
  </acceptance_criteria>
  <done>
    File created. Sections 1-7 (frontmatter through Enrollment profile with D-03 hybrid + 4 inline deltas) committed to the file. All grep acceptance checks pass. Source-confidence marker for D-09 token expiry MEDIUM present. Two SC5 lock-phrase sentences present verbatim. PITFALL 7 line 194 verbatim wording present in persona section.
  </done>
</task>

<task type="auto">
  <name>Task 3 (Wave 1): Author Sections 8-14 of docs/admin-setup-android/05-dedicated-devices.md (Enrollment token through Renewal/Maintenance + optional L1 helpdesk H2)</name>
  <files>
    docs/admin-setup-android/05-dedicated-devices.md (EDIT — append Sections 8-14 after Task 2 saved Sections 1-7)
  </files>
  <read_first>
    - docs/admin-setup-android/05-dedicated-devices.md (READ — see what Task 2 produced; verify Section 7 (Enrollment profile) ends correctly so Section 8 (Enrollment token) appends cleanly)
    - .planning/phases/38-dedicated-devices-admin/38-CONTEXT.md (re-read D-02, D-04, D-06 steps 8-14, D-09, D-11, D-12, D-15 for landmark sections — these are the source of truth for the Task 3 sections)
    - .planning/phases/38-dedicated-devices-admin/38-RESEARCH.md (re-read `## Code Examples` section starting line 449 for D-02 and D-04 verbatim patterns; re-read Flag 3 FRP table at lines 175-193 for HIGH-confidence 3-pathway content; re-read Flag 6 verbatim error string at lines 240-251)
    - .planning/phases/38-dedicated-devices-admin/38-PATTERNS.md (re-read sections "#### Enrollment token H2 pattern" L203-235, "#### Provisioning method choice H2 pattern" L239-275, "#### Exit-kiosk PIN synchronization H2" L279-312, "#### Android 15 FRP and re-provisioning H2" L316-381, "#### What-breaks anchor pattern" L385-403, "#### Renewal / Maintenance H2 pattern" L407-427)
    - docs/admin-setup-android/03-fully-managed-cobo.md (lines 99-125 Enrollment token; lines 127-166 Provisioning method choice; lines 162-163 KME/ZT verbatim callout for D-11 reuse; lines 197-216 What-breaks; lines 218-229 Renewal/Maintenance — STRUCTURAL templates only, do NOT copy COBO-specific content)
    - docs/admin-setup-android/02-zero-touch-portal.md (lines 117-122 — verify the kme-zt-mutual-exclusion anchor location for cross-link target)
  </read_first>
  <action>
    Append Sections 8-14 to `docs/admin-setup-android/05-dedicated-devices.md` after the Task 2 content. Use Edit on the existing file (do NOT recreate). Final file should have all 14 D-06 sections in order.

    **Section 8 — `## Enrollment token` H2 (D-06 step 8)**

    Open with: `<a id="enrollment-token"></a>` then `## Enrollment token` (D-08 mandatory anchor #5).

    Adapt Phase 36 lines 99-125 pattern but with Dedicated-specific token semantics. Required content:

    Intro sentence: "After the Dedicated enrollment profile is created, Intune generates the enrollment token associated with the profile. The token is the secret artifact admins distribute through the chosen provisioning method (embedded in the QR payload, baked into the NFC tag, or substituted into the Zero-Touch DPC extras JSON)."

    Token types table (REPLACE COBO's table — Dedicated has different semantics):

    ```markdown
    | Token type | Expiry | Enrollment-time grouping | Use case |
    |------------|--------|--------------------------|----------|
    | Standard ("Corporate-owned dedicated device (default)") | Mandatory expiry date, configurable up to 65 years; no "never expires" option | Supported (static device group only) | Standard Dedicated scenarios: single-app, multi-app MHS, digital signage |
    | Corporate-owned dedicated device with Microsoft Entra ID shared mode | Mandatory expiry date, configurable up to 65 years | Supported (static device group only) | Multi-shift Entra shared device mode scenarios |
    ```
    Append: `[MEDIUM: no MS Learn statement on default; behavior inferred from UI field description, last_verified 2026-04-22]` (D-12 marker for token expiry assertion).

    Cross-link sentence: "For token lifecycle operations (Replace, Revoke, Export) — these are identical to COBO mechanics — see [Phase 36 enrollment token section](03-fully-managed-cobo.md#enrollment-token)."

    Brief contrast note: "**Contrast with COBO:** COBO offers a 'Default (corporate-owned, fully managed)' token type that does NOT expire. Dedicated has no equivalent — every Dedicated token requires an expiry date. This makes the QR rotation discipline operationally important for Dedicated deployments using printed or laminated QR artifacts (see [Enrollment profile § Delta 4](#enrollment-profile))."

    Two what-breaks callouts (severity-descending):
    > **What breaks if misconfigured:** Token assigned to a group that excludes the target devices → token-check fails during enrollment with an opaque error. Recovery: reassign to an inclusive static group; re-run device enrollment. Symptom appears in: Intune admin center (enrollment failures with token-check error) and device setup flow.
    > **What breaks if misconfigured:** Exported token shared insecurely (email, public SharePoint, group chat) grants unauthorized enrollment capability. Recovery: revoke token immediately (already-enrolled devices unaffected); regenerate and redistribute via secure channel.

    **Section 9 — `## Provisioning method choice` H2 (D-06 step 9, D-11 KME/ZT, D-15 Phase 39 boundary)**

    Open with: `<a id="provisioning-method-choice"></a>` then `## Provisioning method choice` (D-08 mandatory anchor #6).

    Adapt Phase 36 lines 127-166 pattern. Opening paragraph (filtered-row reference per Anti-Pattern 1):
    ```markdown
    Dedicated supports all four Android Enterprise provisioning methods: QR code, NFC, `afw#setup` (DPC identifier), and Zero-Touch. The full method-availability and cross-mode support matrix lives in [02-provisioning-methods.md](../android-lifecycle/02-provisioning-methods.md); this section carries Dedicated-specific constraint callouts that the matrix does not cover. The Dedicated matrix row is the filtered-row target: see [02-provisioning-methods.md#dedicated-cosu](../android-lifecycle/02-provisioning-methods.md#dedicated-cosu).
    ```

    Then 4 H3 method subsections (~80 words each + 1 what-breaks each):

    **`### QR code`** — Most common Dedicated provisioning method. QR contains enrollment token + Wi-Fi credentials in plaintext; treat as secret artifact. Per D-09, plan token rotation BEFORE printing QR artifacts.
    > **What breaks if misconfigured:** Stale QR (regenerated after print/lamination but old artifact still in field) → scan succeeds but token-exchange fails. Recovery: regenerate QR from current enrollment profile and redistribute. Cross-link [Enrollment profile § Delta 4](#enrollment-profile) for QR rotation discipline.

    **`### NFC`** — Available for Dedicated; tag payload limited to 888 bytes (NFC Forum Type 2 Tags); shorten SSID and drop optional DPC extras to fit.
    > **What breaks if misconfigured:** NFC tag payload exceeds 888 bytes → tap step fails with no clear error. Recovery: shorten SSID, drop optional extras, rewrite tag.

    **`### afw#setup` (DPC identifier)** — On the initial Google sign-in prompt during factory-reset setup, type `afw#setup` instead of a Gmail address; triggers DPC download and Dedicated provisioning. System apps disabled by default — Intune device configuration profile must explicitly re-enable required system apps via the system-app allow list. Cross-link `[afw#setup](../_glossary-android.md#afw-setup)` for the historical context.
    > **What breaks if misconfigured:** Required system apps not re-enabled → users report broken device state post-provisioning. Recovery: add required system-app package names to the allow list and redeploy.

    **`### Zero-Touch`** — Dedicated supports ZTE provisioning. Cross-link `[02-zero-touch-portal.md#link-zt-to-intune](02-zero-touch-portal.md#link-zt-to-intune)` for ZT↔Intune linking and `[02-zero-touch-portal.md#dpc-extras-json](02-zero-touch-portal.md#dpc-extras-json)` for DPC extras JSON.

    D-15 Phase 39 boundary note (immediately after the ZT subsection intro):
    > Corporate-scale ZTE content (reseller-upload handoff, device claim workflow, profile assignment at scale, dual-SIM IMEI 1 registration) is delivered separately in **Phase 39** — see [Phase 35 ZT portal doc](02-zero-touch-portal.md) as the current canonical entry point for ZT setup.

    D-11 KME/ZT verbatim callout (REUSE EXACT WORDING from Phase 36 lines 162-163):
    ```markdown
    > ⚠️ **Samsung admins:** Choose Knox Mobile Enrollment (KME) or Zero-Touch — never both. Configuring both on the same devices causes out-of-sync enrollment state on Samsung hardware. Full KME coverage is deferred to v1.4.1. See [02-zero-touch-portal.md#kme-zt-mutual-exclusion](02-zero-touch-portal.md#kme-zt-mutual-exclusion) for the mutual-exclusion record and [_glossary-android.md#zero-touch-enrollment](../_glossary-android.md#zero-touch-enrollment) for the Zero-Touch definition and the iOS ADE cross-platform analog.
    ```

    > **What breaks if misconfigured:** ZT device connects to captive-portal Wi-Fi at first boot → Zero-Touch configuration download fails → device falls through to consumer Android OOBE; must factory-reset to retry. Recovery: ship devices with a SIM for cellular fallback, or provide an open Wi-Fi network without captive-portal interception at the provisioning site.

    **Section 10 — `## Exit-kiosk PIN synchronization` H2 (D-02 SC2 — LANDMARK SECTION; D-06 step 10)**

    Open with the dual-anchor (D-08 mandatory anchors #7 and #8 on the same line per D-08 dual-anchor syntax):
    ```markdown
    <a id="exit-kiosk-pin-synchronization"></a><a id="exit-kiosk-pin"></a>
    ## Exit-kiosk PIN synchronization
    ```

    Opening blockquote — MUST contain the SC2 lock phrase "configured identically in both" verbatim AND the [MEDIUM: MS Q&A community, last_verified 2026-04-22] marker (D-12 marker #1):
    ```markdown
    > ⚠️ **Multi-app kiosks and digital signage:** the exit-kiosk PIN MUST be configured identically in both the device restrictions profile AND the Managed Home Screen app configuration. Mismatch causes a visible error at kiosk exit attempt — the top repeated-escalation pattern for Dedicated devices. `[MEDIUM: MS Q&A community, last_verified 2026-04-22]`
    ```

    Body (~120 words):

    "The exit-kiosk PIN requires configuration in two separate Intune policies:

    1. **Device Restrictions profile** (Devices → Configuration → Create profile → Android Enterprise → Fully managed, dedicated, and corporate-owned work profile — Device restrictions): setting **Leave kiosk mode** (Enable) + **Leave kiosk mode code** (4-6 digit numeric PIN). Inline reminder: the exit-kiosk PIN MUST be configured identically in both this device restrictions profile AND the [MHS app configuration policy](#exit-kiosk-pin-synchronization); mismatch causes the runtime error documented below.

    2. **MHS App Configuration policy** (Apps → Configuration → Managed devices → Android → Managed Home Screen, app `com.microsoft.launcher.enterprise`): setting **Exit lock task mode password** (4-6 digit code). Inline reminder: the exit-kiosk PIN MUST be configured identically in both this MHS app configuration AND the [device restrictions profile](#exit-kiosk-pin-synchronization); mismatch causes the runtime error documented below.

    Both must be set to the same value. If the exit-kiosk PIN is set in only one policy (or set to different values across the two), users see 'A PIN to exit kiosk mode has not been set by your IT admin' at exit attempt. `[MEDIUM: MS Q&A community, last_verified 2026-04-22]`

    **Remediation:** Verify both policies carry the same 4-6 digit code. Redeploy both policies; the device picks up changes on next policy sync (typically within 8 hours; force a sync from the Intune admin center for faster verification).

    **Scope (per D-14):** Multi-app kiosk and digital signage scenarios only. Single-app kiosk does NOT use Managed Home Screen — it uses Lock Task Mode directly — so this synchronization requirement does not apply. See [Scenarios](#scenarios)."

    NOTE: The verbatim error string MUST appear in the body exactly as: `'A PIN to exit kiosk mode has not been set by your IT admin'` (with single quotes); the second `[MEDIUM: MS Q&A community, last_verified 2026-04-22]` immediately follows the quote per D-12 marker #2.

    NOTE: The phrase "configured identically in both" MUST appear verbatim (a) in the opening blockquote, (b) in the inline reminder under list item 1, AND (c) in the inline reminder under list item 2. Total occurrences: 3 (per SC2 lock-phrase enforcement).

    **Section 11 — `## Android 15 FRP and re-provisioning` H2 (D-04 SC3 — LANDMARK SECTION; D-06 step 11)**

    Open with: `<a id="android-15-frp-reprovisioning"></a>` then `## Android 15 FRP and re-provisioning` (D-08 mandatory anchor #9).

    Section-level version tag and opening blockquote — MUST contain the [HIGH: MS Learn ref-corporate-methods, last_verified 2026-04-22] marker (D-12 marker #3, upgraded from MEDIUM per Flag 3):
    ```markdown
    > **Applies to Android 15+**
    >
    > ⚠️ Dedicated devices are typically re-provisioned (factory reset + re-enroll), not re-enrolled in place — describing FRP behavior during factory-reset re-provisioning is therefore distinct from COBO's re-enrollment-in-place context. On Android 15, FRP behavior depends on which reset pathway you use. `[HIGH: MS Learn ref-corporate-methods, last_verified 2026-04-22]`
    ```

    NOTE: The phrase "describing FRP behavior during factory-reset re-provisioning" MUST appear verbatim per AEDED-03 SC3 lock-phrase requirement.

    3-pathway breakdown (~280 words total). Each pathway gets a bold heading + behavior + what-breaks blockquote. Use this exact structure:

    **Pathway 1 — Settings > Factory data reset:** No FRP triggered on Dedicated (COSU) devices. Safe for routine re-provisioning. (Materially different from COPE behavior, which has FRP on Settings reset.) `[HIGH: MS Learn ref-corporate-methods, last_verified 2026-04-22]`
    > **What breaks:** Relying on Settings reset for offline/abandoned devices is safe for FRP, but token expiry must still be valid at re-enrollment time. If the token has expired since the QR was printed, re-enrollment fails at token-exchange — see [Enrollment token § QR rotation discipline](#enrollment-token).

    **Pathway 2 — Recovery/bootloader reset:** FRP applies. Enterprise FRP (EFRP) pre-configuration required to recover; otherwise Google account credential intervention with the pre-reset device-owner account is needed (HIGH recovery cost). `[HIGH: MS Learn ref-corporate-methods, last_verified 2026-04-22]`
    > **What breaks:** Field-replacement workflow that uses recovery reset on Dedicated devices without EFRP pre-configured = device lockout requiring Google-account credential intervention. Recovery: configure EFRP per [Phase 36 EFRP configuration](03-fully-managed-cobo.md#configure-efrp) BEFORE provisioning the Dedicated fleet, not after.

    **Pathway 3 — Intune wipe:** No FRP. Cleanest re-provisioning pathway when the device is online and Intune-managed. `[HIGH: MS Learn ref-corporate-methods, last_verified 2026-04-22]`
    > **What breaks:** Requires the device to be online and Intune-managed at the moment of wipe; offline devices need Pathway 1 or 2.

    Closing cross-links (these are MANDATORY per D-04 — Phase 36 D-05 "no pre-empting" guard means Phase 38 NEVER restates EFRP config steps):
    ```markdown
    - **Configure EFRP via Intune policy** — see [Phase 36 EFRP configuration](03-fully-managed-cobo.md#configure-efrp) for the canonical Device restrictions profile setup (Factory reset protection emails allowlist, assignment scoping). This guide does NOT restate those steps.
    - **For the Android 15 FRP behavioral breakpoint narrative** — see [Android version matrix § Android 15](../android-lifecycle/03-android-version-matrix.md#android-15-breakpoint).
    ```

    Closing note: "**Section-level version tag (per F-4A-02 adjudication):** the 'Applies to Android 15+' tag at the top of this H2 covers all 3 pathways as sub-claims of one Android 15 assertion; per-pathway version tags are not required."

    **Section 12 — What-breaks umbrella anchor (D-06 step 12)**

    Per D-06 step 12 + Phase 36 D-11 precedent, what-breaks is woven inline through enrollment profile + provisioning method sections (already done in earlier sections via blockquotes). This section is a MINIMAL umbrella anchor + brief summary table to satisfy D-08 mandatory anchor #10 (`#what-breaks`) without restating the inline content.

    Add this brief section:
    ```markdown
    <a id="what-breaks"></a>
    ## What-breaks summary

    Inline what-breaks callouts appear at each decision point throughout this guide. Severity-descending umbrella reference (use this as a quick navigation index):

    | Misconfiguration | Section | Severity |
    |---|---|---|
    | Exit-kiosk PIN configured in only one of the two policies | [Exit-kiosk PIN synchronization](#exit-kiosk-pin-synchronization) | CRITICAL |
    | EFRP not assigned before Android 15 recovery/bootloader reset | [Android 15 FRP and re-provisioning](#android-15-frp-reprovisioning) | CRITICAL |
    | KME + ZT dual-configured on Samsung | [Provisioning method choice — Zero-Touch](#provisioning-method-choice) | CRITICAL |
    | Wrong token type for Entra shared device mode scenario | [Enrollment profile — Delta 1](#enrollment-profile) | HIGH |
    | Dynamic device group used with Dedicated standard token | [Enrollment profile — Delta 2](#enrollment-profile) | HIGH |
    | MHS app not assigned as Required (multi-app/signage) | [Enrollment profile — Delta 3](#enrollment-profile) | HIGH |
    | QR artifact stale after token rotation | [Enrollment profile — Delta 4](#enrollment-profile) | HIGH |
    | MGP binding lapsed | [Prerequisites](#prerequisites) | HIGH |
    | Token assigned to wrong group | [Enrollment token](#enrollment-token) | HIGH |
    | Required system apps not re-enabled (afw#setup) | [Provisioning method choice — afw#setup](#provisioning-method-choice) | MEDIUM |
    | NFC tag payload exceeds 888 bytes | [Provisioning method choice — NFC](#provisioning-method-choice) | MEDIUM |
    | ZT device connects to captive-portal Wi-Fi | [Provisioning method choice — Zero-Touch](#provisioning-method-choice) | MEDIUM |
    ```

    **Section 13 — `## Renewal / Maintenance` H2 (D-06 step 13)**

    Open with: `<a id="renewal-maintenance"></a>` then `## Renewal / Maintenance` (D-08 mandatory anchor #11).

    Adapt Phase 36 lines 218-229 pattern with Dedicated-specific rows:

    ```markdown
    | Component | Renewal Period | Consequence of Lapse | Renewal Steps |
    |-----------|----------------|----------------------|---------------|
    | MGP binding | No expiry while the bound Entra account remains active | New app approvals (including MHS updates) and app distribution stop for enrolled devices | Re-bind via Intune admin center — see [01-managed-google-play.md#bind-mgp](01-managed-google-play.md#bind-mgp) |
    | Dedicated enrollment token (Standard) | Mandatory expiry date set at creation; configurable up to 65 years | New enrollments using the token fail at token-exchange; already-enrolled devices unaffected | Generate new token; redistribute via current provisioning channel (regenerate QR / rewrite NFC / update DPC extras JSON). Plan rotation BEFORE printing/laminating QR artifacts |
    | Dedicated enrollment token (Entra shared device mode) | Mandatory expiry date set at creation; configurable up to 65 years | Same as Standard; plus user sign-in workflow blocked for Entra shared device mode scenarios | Same as Standard |
    | MHS app config policy (multi-app + signage) | Review quarterly or after Microsoft Intune app updates that may shift `com.microsoft.launcher.enterprise` defaults | Configuration drift — exit-kiosk PIN may un-sync if device restrictions PIN is rotated without updating MHS PIN | Audit both policies; redeploy if drift detected |
    | EFRP policy assignment (Android 15 fleets) | Review every 60 days (matches this guide's review_by cadence) | EFRP drift → recovery-reset devices may lock | Audit Device Restrictions profile assignment scoped to Dedicated devices; verify allowlist still covers fleet's Google accounts. See [Phase 36 EFRP configuration](03-fully-managed-cobo.md#configure-efrp) |
    | ZT reseller contract (if using Zero-Touch) | Reseller-specific, typically annual | New device IMEI/serial uploads stop appearing in ZT portal | Contact reseller; see [02-zero-touch-portal.md#step-0-reseller](02-zero-touch-portal.md#step-0-reseller) |
    ```

    Add a "Rotation cadence" paragraph: "Dedicated enrollment tokens — there is NO 'never expires' option (unlike COBO's Default token); plan rotation cadence to align with operational refresh windows (typically annually or with QR-print refresh batches). MHS app config policy — re-validate exit-kiosk PIN sync after every device-restrictions PIN change. EFRP — audit assignment and allowlist coverage every 60-day cycle. MGP binding — verify the bound Entra account each 60-day cycle."

    **Section 14 — Optional `## For L1 helpdesk agents` H2 (D-06 step 14, OPTIONAL — planner discretion)**

    DECISION (executor takes planner's call here): INCLUDE this section because PITFALL 7 explicitly calls out the audience-mismatch failure mode and including this section reinforces the "no Dedicated L1 runbook" boundary at point of need. Keep it short (~80-120 words) and EXPLICITLY guard against L1-executable-steps drift.

    Content:
    ```markdown
    ## For L1 helpdesk agents

    Per [PITFALL 7](../../.planning/research/PITFALLS.md) and the v1.4 Phase 40 triage tree boundary: **this section does NOT contain L1-executable steps for Dedicated devices.** Dedicated-device failures route to L2 (policy investigation in [Phase 41 log-collection runbook](../l2-runbooks/18-android-log-collection.md) when shipped) or to the LOB Operations Owner (app-side / device-side issues that L1 cannot resolve without Intune admin or business-owner escalation).

    L1 agents triaging an Android symptom: confirm the device is Dedicated mode (not BYOD or COBO) using the [Phase 40 Android triage tree](../decision-trees/08-android-triage.md) (when shipped). If Dedicated, route to L2 or to the LOB Operations Owner (see [Audience and stakeholders](#audience-and-stakeholders)).
    ```

    **Final sections — See Also + Changelog (Phase 36 / Phase 37 precedent; not numbered in D-06 but standard at end of file):**

    Add `## See Also` with cross-links: 00-overview.md, 01-managed-google-play.md, 02-zero-touch-portal.md, 03-fully-managed-cobo.md, 04-byod-work-profile.md, 02-provisioning-methods.md, 03-android-version-matrix.md, _glossary-android.md anchors (#dedicated, #managed-home-screen, #entra-shared-device-mode, #play-integrity).

    Add `## Changelog` table:
    ```markdown
    | Date | Change | Author |
    |------|--------|--------|
    | 2026-04-22 | Initial version — Dedicated (kiosk/COSU) admin guide: persona callout (D-01), 4-scenario overview (D-01 + D-13 + D-14), enrollment-profile deltas via D-03 hybrid, MHS exit-PIN synchronization H2 (D-02 / AEDED-02), Android 15 FRP and re-provisioning H2 (D-04 / AEDED-03), tri-portal provisioning methods with KME/ZT callout (D-11) and Phase 39 boundary note (D-15) | -- |
    ```

    **File save point — Task 3 closes the file.** All 14 D-06 sections present. Total word count target: 3200-4200 per D-07.

    Per D-16: in addition to writing the file, this task must perform a brief executor-time re-verification of:
    - The Intune admin center Dedicated enrollment-profile blade path (verify against the live MS Learn `setup-dedicated` page or stable URL https://go.microsoft.com/fwlink/?linkid=2109431) — if the breadcrumb has shifted from "Devices → Device onboarding → Enrollment → Android tab → Enrollment Profiles → Corporate-owned dedicated devices", update the parenthetical in Section 7 (Enrollment profile) accordingly.
    - The MHS exit-PIN verbatim error string (verify community Q&A 2182159 still cites this string; if the MHS app has shipped a new error string in a recent update, update the verbatim quote AND update the marker date).

    Record any re-verification findings in your task summary. If neither has changed, simply record "Re-verified 2026-04-22; navigation path and error string unchanged from RESEARCH.md."
  </action>
  <verify>
    <automated>grep -cE "&lt;a id=\"(audience-and-stakeholders|scenarios|prerequisites|enrollment-profile|enrollment-token|provisioning-method-choice|exit-kiosk-pin-synchronization|exit-kiosk-pin|android-15-frp-reprovisioning|what-breaks|renewal-maintenance)\"" docs/admin-setup-android/05-dedicated-devices.md && grep -c "configured identically in both" docs/admin-setup-android/05-dedicated-devices.md && grep -c "A PIN to exit kiosk mode has not been set by your IT admin" docs/admin-setup-android/05-dedicated-devices.md && grep -c "describing FRP behavior during factory-reset re-provisioning" docs/admin-setup-android/05-dedicated-devices.md && wc -w docs/admin-setup-android/05-dedicated-devices.md</automated>
  </verify>
  <acceptance_criteria>
    - All 11 mandatory anchors present: `grep -cE "&lt;a id=\"(audience-and-stakeholders|scenarios|prerequisites|enrollment-profile|enrollment-token|provisioning-method-choice|exit-kiosk-pin-synchronization|exit-kiosk-pin|android-15-frp-reprovisioning|what-breaks|renewal-maintenance)\"" docs/admin-setup-android/05-dedicated-devices.md` returns 11.
    - SC2 lock phrase present at minimum 3 times: `grep -c "configured identically in both" docs/admin-setup-android/05-dedicated-devices.md` returns >= 3 (1 in opening blockquote + 1 in inline reminder under device restrictions list item + 1 in inline reminder under MHS app config list item).
    - Verbatim error string present exactly once: `grep -c "A PIN to exit kiosk mode has not been set by your IT admin" docs/admin-setup-android/05-dedicated-devices.md` returns 1.
    - SC3 verbatim phrase present: `grep -c "describing FRP behavior during factory-reset re-provisioning" docs/admin-setup-android/05-dedicated-devices.md` returns 1.
    - Word count within D-07 envelope: `wc -w docs/admin-setup-android/05-dedicated-devices.md` returns a count between 3200 and 4200.
    - All 14 D-06 sections present in correct order: `grep -nE "^## " docs/admin-setup-android/05-dedicated-devices.md` returns sections in this order: Key Concepts, Scenarios, Prerequisites, Enrollment profile, Enrollment token, Provisioning method choice, Exit-kiosk PIN synchronization, Android 15 FRP and re-provisioning, What-breaks summary, Renewal / Maintenance, For L1 helpdesk agents, See Also, Changelog.
    - D-04 section-level version tag present: `grep -c "Applies to Android 15+" docs/admin-setup-android/05-dedicated-devices.md` returns >= 1.
    - All 4 source-confidence markers present: `grep -cE "\[(HIGH|MEDIUM):" docs/admin-setup-android/05-dedicated-devices.md` returns >= 4 (1 token expiry MEDIUM in Section 7 + 1 token expiry MEDIUM repeated in Section 8 + 2 MHS exit-PIN MEDIUM markers in Section 10 + 1 FRP HIGH marker in Section 11 opening + optional 3 per-pathway HIGH repeats).
    - D-11 KME/ZT verbatim callout present: `grep -c "Knox Mobile Enrollment (KME) or Zero-Touch — never both" docs/admin-setup-android/05-dedicated-devices.md` returns 1.
    - D-15 Phase 39 boundary note present: `grep -c "Phase 39" docs/admin-setup-android/05-dedicated-devices.md` returns >= 1.
    - Phase 36 EFRP cross-link present: `grep -c "03-fully-managed-cobo.md#configure-efrp" docs/admin-setup-android/05-dedicated-devices.md` returns >= 1 (D-04 closing cross-link mandatory per Phase 36 D-05 no-pre-empting guard).
    - Phase 36 enrollment-token cross-link present: `grep -c "03-fully-managed-cobo.md#enrollment-token" docs/admin-setup-android/05-dedicated-devices.md` returns >= 1.
    - Phase 35 KME/ZT mutual-exclusion cross-link present: `grep -c "02-zero-touch-portal.md#kme-zt-mutual-exclusion" docs/admin-setup-android/05-dedicated-devices.md` returns 1 (D-11).
    - Phase 34 version matrix Android 15 cross-link present: `grep -c "03-android-version-matrix.md#android-15-breakpoint" docs/admin-setup-android/05-dedicated-devices.md` returns 1 (D-04 closing cross-link).
    - Phase 34 provisioning-methods filtered-row link present: `grep -c "02-provisioning-methods.md#dedicated-cosu" docs/admin-setup-android/05-dedicated-devices.md` returns 1 (Anti-Pattern 1 + D-15 + Section 9 opening).
    - L1 helpdesk H2 includes the explicit "does NOT contain L1-executable steps" guard: `grep -c "does NOT contain L1-executable steps" docs/admin-setup-android/05-dedicated-devices.md` returns 1.
    - Re-verification note from D-16 re-check is recorded in the task output (either confirming no change or specifying what was updated).
  </acceptance_criteria>
  <done>
    File complete with all 14 D-06 sections in correct order. All 11 mandatory anchors present. SC2 lock phrase present 3+ times. SC3 lock phrase present once. Verbatim MHS error string present once. 4+ source-confidence markers present. Word count within 3200-4200 envelope. D-11 verbatim KME/ZT callout present. D-15 Phase 39 boundary note present. All Phase 34/35/36 cross-link targets present. D-16 executor re-verification recorded.
  </done>
</task>

<task type="auto">
  <name>Task 4 (Wave 2): Run AEAUDIT-04 audit + full structural compliance check on docs/admin-setup-android/05-dedicated-devices.md</name>
  <files>
    (read-only audit; no files modified)
  </files>
  <read_first>
    - docs/admin-setup-android/05-dedicated-devices.md (full file — the audit subject)
    - .planning/phases/38-dedicated-devices-admin/38-VALIDATION.md (full file — the per-task verification map and audit command specifications)
    - .planning/phases/38-dedicated-devices-admin/38-CONTEXT.md (D-20 audit guards inheritance specification)
  </read_first>
  <action>
    Run the full AEAUDIT-04 milestone-audit-equivalent grep audit on `docs/admin-setup-android/05-dedicated-devices.md` and produce a single audit report block. This task does NOT modify the doc; if any check fails, raise the failure as a blocker and propose specific corrections in the audit report (the executor may then return to Task 3 to apply the corrections via a follow-up Edit before this audit re-runs).

    Run each grep below verbatim and record actual vs expected output. Use the `Grep` tool or `Bash` tool. Compose a Markdown audit report with one row per check.

    **AEAUDIT-04 guards (per D-20):**
    1. `grep -c "SafetyNet" docs/admin-setup-android/05-dedicated-devices.md` — MUST return 0 (Play Integrity only, never SafetyNet).
    2. `grep -nE "supervision" docs/admin-setup-android/05-dedicated-devices.md` — must return 0 lines OUTSIDE of cross-platform contrast notes (allowed if the term appears only in a cross-platform note explicitly contrasting Android with iOS Supervision; if the bare word "supervision" appears as an Android management term anywhere in the doc, FAIL). The grep returns ALL occurrences; manual review classifies each.
    3. `grep -c "^last_verified:" docs/admin-setup-android/05-dedicated-devices.md` — MUST return 1 (frontmatter present per D-18).
    4. `grep -c "^review_by:" docs/admin-setup-android/05-dedicated-devices.md` — MUST return 1.
    5. `grep -c "^applies_to: Dedicated$" docs/admin-setup-android/05-dedicated-devices.md` — MUST return 1.
    6. `grep -c "^platform: Android$" docs/admin-setup-android/05-dedicated-devices.md` — MUST return 1.
    7. `grep -c "^audience: admin$" docs/admin-setup-android/05-dedicated-devices.md` — MUST return 1.

    **D-08 anchor contract (all 11 must be present):**
    8. For each of these 11 anchors, run `grep -c "<a id=\"$ANCHOR\"" docs/admin-setup-android/05-dedicated-devices.md` and confirm == 1: audience-and-stakeholders, scenarios, prerequisites, enrollment-profile, enrollment-token, provisioning-method-choice, exit-kiosk-pin-synchronization, exit-kiosk-pin, android-15-frp-reprovisioning, what-breaks, renewal-maintenance. Total: 11/11.
    9. `grep -c "<a id=" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 11 (mandatory minimum; plus one combined line for the dual-anchor exit-kiosk-pin synchronization line where both `#exit-kiosk-pin-synchronization` and `#exit-kiosk-pin` are on the same line — that single line registers as 2 grep matches).
    10. Confirm dual-anchor syntax for D-02 H2: `grep -c "<a id=\"exit-kiosk-pin-synchronization\"></a><a id=\"exit-kiosk-pin\"></a>" docs/admin-setup-android/05-dedicated-devices.md` — MUST return 1.

    **D-07 length envelope:**
    11. `wc -w docs/admin-setup-android/05-dedicated-devices.md` — MUST return a count between 3200 and 4200.

    **D-12 source-confidence marker contract:**
    12. `grep -cE "\[(HIGH|MEDIUM|LOW)(: [A-Za-z &]+)?(, last_verified \d{4}-\d{2}-\d{2})?\]" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 4 (D-12 mandatory locations: 1 token expiry MEDIUM + 2 MHS exit-PIN MEDIUM markers + 1 FRP HIGH opening; up to 4 additional repeats allowed for per-pathway FRP markers and Section 8 token expiry repeat).
    13. `grep -c "\[MEDIUM: MS Q&A community" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 2 (D-02 H2 opening blockquote + verbatim error string body, both MEDIUM-confidence per Flag 6).
    14. `grep -c "\[HIGH: MS Learn ref-corporate-methods" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 1 (D-04 H2 opening blockquote per Flag 3 HIGH-confidence upgrade).
    15. `grep -c "\[MEDIUM: no MS Learn statement" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 1 (D-09 token expiry default per Flag 2 MEDIUM).

    **SC verbatim lock-phrase audit (AEDED-01 SC2 + SC3 + SC5):**
    16. `grep -c "configured identically in both" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 3 (SC2 lock phrase: D-02 H2 opening + 2 inline reminders).
    17. `grep -c "A PIN to exit kiosk mode has not been set by your IT admin" docs/admin-setup-android/05-dedicated-devices.md` — MUST return == 1 (verbatim error string from RESEARCH Flag 6).
    18. `grep -c "describing FRP behavior during factory-reset re-provisioning" docs/admin-setup-android/05-dedicated-devices.md` — MUST return == 1 (AEDED-03 SC3 verbatim semantic compliance).
    19. `grep -c "Multi-app kiosk = curated app set on a device with no per-user identity" docs/admin-setup-android/05-dedicated-devices.md` — MUST return == 1 (D-13 SC5 disambiguation phrase #1).
    20. `grep -c "Entra shared device mode = curated app set on a device where multiple workers sign in/out with their own Entra credentials" docs/admin-setup-android/05-dedicated-devices.md` — MUST return == 1 (D-13 SC5 disambiguation phrase #2).
    21. `grep -c "Audience: Intune Admin + Line-of-Business Operations Owner" docs/admin-setup-android/05-dedicated-devices.md` — MUST return == 1 (PITFALL 7 line 194 verbatim core wording in persona callout).
    22. `grep -c "Knox Mobile Enrollment (KME) or Zero-Touch — never both" docs/admin-setup-android/05-dedicated-devices.md` — MUST return == 1 (D-11 verbatim KME/ZT callout core wording).

    **Cross-link target audit (Anti-Pattern 1 + D-04 + D-11 + Phase 36 D-05 cross-link mandates):**
    23. `grep -c "03-fully-managed-cobo.md#enrollment-profile" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 1 (D-03 hybrid cross-link).
    24. `grep -c "03-fully-managed-cobo.md#enrollment-token" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 1.
    25. `grep -c "03-fully-managed-cobo.md#configure-efrp" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 1 (D-04 closing cross-link; Phase 36 D-05 no-pre-empting MANDATE).
    26. `grep -c "03-android-version-matrix.md#android-15-breakpoint" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 1 (D-04 closing cross-link).
    27. `grep -c "02-provisioning-methods.md#dedicated-cosu" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 1 (Anti-Pattern 1 filtered-row link).
    28. `grep -c "02-zero-touch-portal.md#kme-zt-mutual-exclusion" docs/admin-setup-android/05-dedicated-devices.md` — MUST return == 1 (D-11).
    29. `grep -c "01-managed-google-play.md#bind-mgp" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 1 (Prerequisites MGP cross-link).

    **D-17 shared-file modification guard:**
    30. `git status --porcelain docs/ .planning/` — MUST show `?? docs/admin-setup-android/05-dedicated-devices.md` (or `A` if staged) as the ONLY change under `docs/`. If any other doc shows `M` (modified), FAIL — D-17 violation. (Any modifications under `.planning/phases/38-dedicated-devices-admin/` are allowed since SUMMARY/RETROSPECTIVE may be added by execute-plan.)

    **D-19 glossary first-use cross-link audit:**
    31. `grep -c "../_glossary-android.md#dedicated" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 1 (Platform note banner OR Terminology — D-19 first-use of "Dedicated"; serves both Dedicated and COSU first-use per <interfaces> note that `#cosu` does not exist in glossary).
    32. `grep -c "../_glossary-android.md#managed-home-screen" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 1.
    33. `grep -c "../_glossary-android.md#entra-shared-device-mode" docs/admin-setup-android/05-dedicated-devices.md` — MUST return >= 1.

    **D-06 14-section structural audit:**
    34. `grep -nE "^## " docs/admin-setup-android/05-dedicated-devices.md` — MUST return H2 headings in this exact relative order: Key Concepts → Scenarios → Prerequisites → Enrollment profile → Enrollment token → Provisioning method choice → Exit-kiosk PIN synchronization → Android 15 FRP and re-provisioning → What-breaks summary → Renewal / Maintenance → For L1 helpdesk agents → See Also → Changelog. (Frontmatter, Platform gate, and Platform note banner sections precede the first H2 and do not appear in this grep.)

    Compile all 34 results into a single Markdown audit report with PASS/FAIL per check. If 100% PASS, set `nyquist_compliant: true` in `.planning/phases/38-dedicated-devices-admin/38-VALIDATION.md` frontmatter (this is the only allowed file modification for this audit task; modifying VALIDATION.md is permitted as a planning-artifact update, not a docs/ modification, so does NOT violate D-17).

    If ANY check fails, do NOT modify VALIDATION.md; instead emit a clear "BLOCKER: Task 4 audit found N failures — see Task 3 corrections needed" report listing each failure with the specific text/anchor that needs to be added or fixed.
  </action>
  <verify>
    <automated>echo "=== Anchor contract ===" &amp;&amp; grep -cE "&lt;a id=\"(audience-and-stakeholders|scenarios|prerequisites|enrollment-profile|enrollment-token|provisioning-method-choice|exit-kiosk-pin-synchronization|exit-kiosk-pin|android-15-frp-reprovisioning|what-breaks|renewal-maintenance)\"" docs/admin-setup-android/05-dedicated-devices.md &amp;&amp; echo "=== AEAUDIT-04 SafetyNet ===" &amp;&amp; grep -c "SafetyNet" docs/admin-setup-android/05-dedicated-devices.md &amp;&amp; echo "=== Word count ===" &amp;&amp; wc -w docs/admin-setup-android/05-dedicated-devices.md &amp;&amp; echo "=== SC lock phrases ===" &amp;&amp; grep -c "configured identically in both" docs/admin-setup-android/05-dedicated-devices.md &amp;&amp; grep -c "describing FRP behavior during factory-reset re-provisioning" docs/admin-setup-android/05-dedicated-devices.md &amp;&amp; grep -c "A PIN to exit kiosk mode has not been set by your IT admin" docs/admin-setup-android/05-dedicated-devices.md &amp;&amp; echo "=== Source-confidence marker count ===" &amp;&amp; grep -cE "\[(HIGH|MEDIUM|LOW)(: [A-Za-z &amp;]+)?(, last_verified \d{4}-\d{2}-\d{2})?\]" docs/admin-setup-android/05-dedicated-devices.md &amp;&amp; echo "=== D-17 shared-file guard ===" &amp;&amp; git status --porcelain docs/</automated>
  </verify>
  <acceptance_criteria>
    - All 34 audit checks PASS (or explicit failure report listing each failed check with the specific correction needed for Task 3 follow-up).
    - Audit report posted in task output as a single Markdown table with columns Check# | Description | Expected | Actual | PASS/FAIL.
    - Total file word count within 3200-4200 envelope (D-07).
    - All 11 mandatory anchors present per D-08.
    - Zero "SafetyNet" occurrences.
    - All 4 source-confidence markers present at correct minimums (>= 2 MS Q&A MEDIUM + >= 1 MS Learn HIGH + >= 1 inferred MEDIUM).
    - All 6 mandatory cross-links present (Phase 36 #enrollment-profile + #enrollment-token + #configure-efrp + Phase 35 #kme-zt-mutual-exclusion + Phase 34 #android-15-breakpoint + Phase 34 #dedicated-cosu).
    - All 3 SC verbatim lock phrases present at correct counts (SC2 "configured identically in both" >= 3; SC3 "describing FRP behavior during factory-reset re-provisioning" == 1; verbatim error string == 1).
    - D-13 SC5 disambiguation phrases both present (each == 1).
    - PITFALL 7 line 194 verbatim core wording present (== 1).
    - D-11 KME/ZT verbatim core wording present (== 1).
    - D-17 shared-file guard satisfied (git status shows ONLY `05-dedicated-devices.md` as new under `docs/`).
    - D-06 14-section H2 order is correct (verified by manual review of the H2 grep output).
    - On 100% PASS: `nyquist_compliant: true` set in `.planning/phases/38-dedicated-devices-admin/38-VALIDATION.md` frontmatter.
  </acceptance_criteria>
  <done>
    Audit report posted with 34/34 PASS (or explicit blocker report with corrections needed). On full PASS, VALIDATION.md frontmatter updated to `nyquist_compliant: true`. Phase 38 ready for `/gsd-verify-work` and downstream Phase 40/41 anchor consumption.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs reader → docs author | Reader trusts the doc's claims about Intune behavior, MHS error strings, FRP pathways, navigation paths. Misleading or stale claims cause real-world misconfiguration. |
| Phase 38 → Phase 36/35/34 anchors | Cross-link integrity assumes upstream anchors remain stable. Renames upstream break Phase 38 navigation. |
| Phase 38 → Phase 40/41 anchors | Phase 38 publishes 11 stable anchors that downstream phases consume. Renaming any of the 11 breaks downstream cross-references. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information Disclosure / Elevation of Privilege | Exit-kiosk PIN configuration (two-policy sync) | mitigate | D-02 H2 + 2 inline reminders enforce dual-policy PIN sync requirement at point of admin decision. SC2 lock phrase "configured identically in both" appears 3+ times to maximize reader awareness. Verbatim error string + MEDIUM-confidence marker tells admins exactly what failure looks like and that the source is community-confirmed (not authoritative MS Learn). |
| T-38-02 | Denial of Service / Spoofing | Android 15 FRP lockout post recovery/bootloader reset | mitigate | D-04 H2 documents 3-pathway FRP behavior; cross-links Phase 36 #configure-efrp for EFRP pre-config (the mitigation). HIGH-confidence marker on the 3-pathway claims confirms MS Learn authoritative source. What-breaks per pathway tells admins which pathway = lockout risk and how to avoid (Pathway 2 requires EFRP). |
| T-38-03 | Information Disclosure | QR enrollment artifact contains plaintext token + Wi-Fi credentials | mitigate | Provisioning method choice § QR code section explicitly states "QR contains enrollment token + Wi-Fi credentials in plaintext; treat as secret artifact." D-09 token rotation discipline in D-03 enrollment-profile delta 4 prevents stale-QR exploitation. |
| T-38-04 | Tampering / Spoofing | Wrong token type for Entra shared device mode → users share single device account when they should sign in/out individually | mitigate | D-13 SC5 disambiguation phrasing in BOTH Scenarios table User identity model column AND How to choose routing paragraph. D-03 enrollment-profile delta 1 has what-breaks callout for wrong token type. |
| T-38-05 | Denial of Service | Dynamic device group used → token-check enrollment fails during burst provisioning | mitigate | D-03 enrollment-profile delta 2 documents static-group requirement with what-breaks callout. Prerequisites checklist also includes static Entra device group requirement. |
| T-38-06 | Repudiation / Information Disclosure | Stale doc claims (MHS error string changed; navigation breadcrumbs shifted; default token expiry behavior changed) mislead admins | mitigate | D-12 source-confidence markers (HIGH/MEDIUM/LOW) tell readers the freshness and authority of each MEDIUM/LOW claim. D-16 executor re-verification protocol re-checks navigation path and error string at execute time. 60-day review_by frontmatter forces re-validation cycle. |
| T-38-07 | Spoofing | KME + ZT dual-configured on Samsung → out-of-sync enrollment state | mitigate | D-11 verbatim KME/ZT mutual-exclusion callout in ZT subsection cross-links Phase 35 anchor. Reused verbatim from Phase 36 to maintain consistency. |
| T-38-08 | Audience misalignment (PITFALL 7) | LOB Operations Owner cannot use the doc independently → app-side decisions miscommunicated to Intune admin → wrong scenario configured | mitigate | D-01 persona callout in Key Concepts (BEFORE any Intune steps) names both stakeholders with concrete responsibilities. Optional Section 14 (For L1 helpdesk agents) reinforces "no Dedicated L1 runbook" routing boundary. |
</threat_model>

<verification>
Phase-level verification (cross-task):

1. **Anchor contract (D-08):** All 11 mandatory `<a id="...">` anchors present in shipped doc; verified by Task 4 check #8-#10.
2. **D-06 14-section structure:** All 14 sections (frontmatter + platform gate + banner + 11 H2s + optional L1 H2) present in correct order; verified by Task 4 check #34.
3. **D-07 length envelope:** Word count between 3200 and 4200; verified by Task 4 check #11.
4. **D-12 source-confidence markers:** Minimum 4 markers at the 4 mandatory locations (token expiry MEDIUM, exit-PIN MEDIUM x2, FRP HIGH); verified by Task 4 checks #12-#15.
5. **SC verbatim lock-phrase compliance:** SC2 phrase "configured identically in both" >= 3 occurrences; SC3 phrase "describing FRP behavior during factory-reset re-provisioning" == 1; verbatim error string == 1; SC5 D-13 disambiguation phrases both present; verified by Task 4 checks #16-#22.
6. **AEAUDIT-04 inheritance:** Zero SafetyNet, zero "supervision" as Android management term, frontmatter `last_verified` present; verified by Task 4 checks #1-#7.
7. **D-17 shared-file guard:** ONLY `docs/admin-setup-android/05-dedicated-devices.md` is a new/modified file under `docs/`; verified by Task 4 check #30.
8. **Cross-link mandates:** All 6 mandatory cross-links present (D-03 hybrid, D-04 closing pair, D-11 KME/ZT, Anti-Pattern 1 filtered-row, MGP prereq); verified by Task 4 checks #23-#29.
9. **Anti-Pattern 1 compliance:** Manual review (no clean grep): the doc must NOT restate the 5x4 provisioning-method matrix or Phase 36 EFRP config steps or Phase 36 enrollment-profile step-by-step. Manual check: ensure D-03 orientation is ~80 words and deltas focus on the 4 Dedicated-specific items, NOT a restated step-by-step.
10. **D-16 executor re-verification:** Task 3 must record either "navigation and error string unchanged" or specific updates applied. Failure to record = blocker.

Pass criteria: Task 1 grep audit passes (all upstream anchors present); Tasks 2 + 3 produce a 3200-4200-word doc with all 14 sections, all 11 anchors, all 6 cross-links, all 3 SC lock phrases (with correct counts), and 4+ source-confidence markers; Task 4 audit returns 34/34 PASS.
</verification>

<success_criteria>
- All 5 success criteria from ROADMAP.md Phase 38 entry (lines 161-166) are demonstrably satisfied by the shipped doc:
  1. SC1: Persona callout (Intune Admin + LOB Operations Owner) at top of Key Concepts; Scenarios H2 with 4-row comparison table; reachable enrollment-profile configuration via D-03 hybrid orientation. Verified by Task 4 grep checks for the audience anchor, scenarios anchor, and enrollment-profile anchor + Phase 36 cross-link.
  2. SC2: MHS exit-PIN H2 with verbatim "configured identically in both" lock phrase at minimum 3 times. Verified by Task 4 check #16.
  3. SC3: Android 15 FRP H2 with "describing FRP behavior during factory-reset re-provisioning" verbatim phrase + 3-pathway breakdown + Phase 36 EFRP cross-link. Verified by Task 4 checks #18 and #25.
  4. SC4: Provisioning-method section uses filtered-row reference to `02-provisioning-methods.md#dedicated-cosu` and does NOT restate the 5x4 grid. Verified by Task 4 check #27 + manual Anti-Pattern 1 review.
  5. SC5: Entra shared device mode vs multi-app kiosk disambiguation in BOTH Scenarios table (User identity model column) AND How to choose routing paragraph; D-13 verbatim phrasing both present. Verified by Task 4 checks #19 and #20.
- All 3 phase requirement IDs (AEDED-01, AEDED-02, AEDED-03) traceable to doc sections through `requirements:` frontmatter and the must_haves truths.
- AEAUDIT-04 milestone audit guards satisfied (D-20): zero SafetyNet, zero "supervision" as Android management term, `last_verified` present, source-confidence regex satisfied where MEDIUM/LOW assertions appear, anchor contract per D-08 satisfied.
- D-17 shared-file modification guard satisfied: zero modifications to existing files under `docs/` (the only new file is `05-dedicated-devices.md`); `.planning/` modifications limited to phase-internal artifacts (VALIDATION.md frontmatter update on PASS).
- Phase 40 and Phase 41 can consume the 11 stable anchors without renaming (anchor stability contract per D-08).
</success_criteria>

<output>
After completion, create `.planning/phases/38-dedicated-devices-admin/38-01-SUMMARY.md` per the standard summary template, including:
- Header: phase, plan, completed date, file produced
- What was built: 1 file (`docs/admin-setup-android/05-dedicated-devices.md`); word count; section count; anchor count
- Decisions traced: D-01 through D-20 with one-line implementation note per decision
- Cross-links published (downstream consumption inventory): the 11 anchors + their intended Phase 40/41 consumers
- Audit report: Task 4 results (34/34 PASS or specific failure inventory if any)
- D-16 re-verification findings: whether navigation path / MHS error string changed since RESEARCH.md
- Patterns followed (extract for future phases): D-03 hybrid orientation pattern, D-12 source-confidence marker pattern, D-08 anchor stability pattern
- Patterns avoided: Anti-Pattern 1 (matrix duplication), Phase 36 D-05 no-pre-empting (EFRP config), PITFALL 7 (audience mismatch), PITFALL 9 (shared file mods)
</output>
