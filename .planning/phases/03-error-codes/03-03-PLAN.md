---
phase: 03-error-codes
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - docs/error-codes/00-index.md
autonomous: false
requirements:
  - ERRC-01
must_haves:
  truths:
    - "A technician can Ctrl+F any error code on the index page and immediately see which category file to open"
    - "The index lists every error from all 5 category files in a single condensed table"
    - "Each row links to the primary category file for that error"
    - "The index has a Categories section linking to each category file with brief descriptions"
  artifacts:
    - path: "docs/error-codes/00-index.md"
      provides: "Master error code lookup table as single entry point"
      contains: "0x80180014"
  key_links:
    - from: "docs/error-codes/00-index.md"
      to: "docs/error-codes/01-mdm-enrollment.md"
      via: "Category column links to category files"
      pattern: "01-mdm-enrollment"
    - from: "docs/error-codes/00-index.md"
      to: "docs/error-codes/05-hybrid-join.md"
      via: "Category column links to category files"
      pattern: "05-hybrid-join"
---

<objective>
Create the master error code index (00-index.md) as the single entry point for all error code lookups.

Purpose: The index is the "Ctrl+F page" — a technician sees a hex code on screen, opens this page, finds it instantly, and follows the link to the detailed category file. Without this, technicians must guess which category file their error belongs to.

Output: One condensed lookup table in `docs/error-codes/00-index.md` with every error code from all 5 category files, linking to primary category for each.
</objective>

<execution_context>
@C:/Users/joanderson/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joanderson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-error-codes/03-CONTEXT.md
@.planning/phases/03-error-codes/03-RESEARCH.md

Files created by Plan 01 and Plan 02 (read these to build the index):
@docs/error-codes/01-mdm-enrollment.md
@docs/error-codes/02-tpm-attestation.md
@docs/error-codes/03-esp-enrollment.md
@docs/error-codes/04-pre-provisioning.md
@docs/error-codes/05-hybrid-join.md

@docs/_glossary.md
@docs/apv1-vs-apv2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create master error code index (00-index.md)</name>
  <files>docs/error-codes/00-index.md</files>
  <action>
Read all 5 category files (01 through 05) created by Plans 01 and 02 to extract the complete error code inventory. Then create `docs/error-codes/00-index.md`.

**Structure (in order):**
1. YAML frontmatter: `last_verified: 2026-03-14`, `review_by: 2026-06-12`, `applies_to: APv1`, `audience: both`
2. Version gate banner (same as all other error-codes files)
3. H1: `# Error Code Index`
4. Intro (2-3 sentences): "This is the master lookup table for all Windows [Autopilot](../_glossary.md#autopilot) error codes. Find your error code below and follow the link to the detailed category page for causes, L1 actions, and L2 fixes."

5. H2: `## Categories`
   Bulleted list of all 5 category files with brief description:
   - [MDM Enrollment Errors (0x8018xxxx)](01-mdm-enrollment.md) — MDM enrollment failures during OOBE
   - [TPM Attestation Errors](02-tpm-attestation.md) — TPM hardware and firmware attestation failures
   - [ESP and Enrollment Errors](03-esp-enrollment.md) — Enrollment Status Page failures and policy conflicts
   - [Pre-Provisioning and Self-Deploying Errors](04-pre-provisioning.md) — Technician flow and kiosk deployment failures
   - [Hybrid Join and Device Registration Errors](05-hybrid-join.md) — Hybrid Entra join failures and event ID mapping

6. H2: `## Quick Lookup`
   Mode legend: `**Mode:** UD=User-Driven · PP=Pre-Provisioning · SD=Self-Deploying · All=All modes`

   Condensed 4-column table per CONTEXT.md:
   ```
   | Code | Name | Mode | Category |
   |------|------|------|----------|
   ```

   List EVERY error code from all 5 category files. Sort by hex code ascending, with Event IDs at the end (sorted by event number ascending). For each row:
   - Code: the hex code or `Event NNN`
   - Name: CamelCase name from the category file
   - Mode: abbreviated mode tags
   - Category: linked to the primary category file with anchor (e.g., `[MDM Enrollment](01-mdm-enrollment.md#0x80180014)`)

   Do NOT include multi-cause row numbers or L1/L2 columns — this is a condensed lookup. The technician follows the Category link for full detail.

   Do NOT include cross-reference entries (those only exist to redirect within category files). Include each code exactly ONCE, pointing to its PRIMARY category file.

   Expected approximate row count: ~25-30 unique error codes/event IDs total across all categories.

7. H2: `## No-Code ESP Failures`
   Brief note: "Some ESP failures show 'Something went wrong' without a specific error code. See [ESP and Enrollment Errors — Policy Conflicts](03-esp-enrollment.md#policy-conflicts-no-discrete-error-code) for these scenarios."

8. APv2 Notes blockquote: "For APv2 (Device Preparation) error coverage, check the APv2 Notes section at the bottom of each category page. For framework differences, see [APv1 vs APv2 disambiguation](../apv1-vs-apv2.md)."

9. No prev/next navigation (index is the entry point — no "prev" page). Instead: `---` then a note: "Select a category above or use Ctrl+F to find your error code in the Quick Lookup table."

10. Version History table

**CRITICAL:** The index must be COMPLETE. Every error code that has a primary entry in any category file must appear in the Quick Lookup table. Read all 5 category files before writing the index. If a code only appears as a cross-reference row in a category file (e.g., TPM codes in 04-pre-provisioning.md), it should appear in the index pointing to the PRIMARY file (02-tpm-attestation.md), not the cross-reference file.
  </action>
  <verify>
    <automated>test -f docs/error-codes/00-index.md && grep -q "01-mdm-enrollment" docs/error-codes/00-index.md && grep -q "02-tpm-attestation" docs/error-codes/00-index.md && grep -q "03-esp-enrollment" docs/error-codes/00-index.md && grep -q "04-pre-provisioning" docs/error-codes/00-index.md && grep -q "05-hybrid-join" docs/error-codes/00-index.md && grep -q "Event 807" docs/error-codes/00-index.md && grep -q "0x80180014" docs/error-codes/00-index.md</automated>
  </verify>
  <done>00-index.md exists with condensed 4-column Quick Lookup table containing every error code from all 5 category files. Each row links to its primary category file. Categories section lists and describes all 5 files. No-code ESP failures section links to policy conflicts. Index is complete — no error code from any category file is missing from the lookup table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete error code reference (all 6 files)</name>
  <files>docs/error-codes/00-index.md</files>
  <what-built>Complete error code reference with 6 files: master index, MDM enrollment, TPM attestation, ESP/enrollment, pre-provisioning/self-deploying, and hybrid join error tables</what-built>
  <how-to-verify>
    1. Open docs/error-codes/00-index.md — verify the Quick Lookup table lists all error codes and each links to the correct category file
    2. Open docs/error-codes/01-mdm-enrollment.md — verify 0x80180014 has two cause rows with different L1 actions
    3. Open docs/error-codes/02-tpm-attestation.md — verify hardware-specific notes for Infineon, ST Micro/Nuvoton, AMD, Intel Tiger Lake
    4. Open docs/error-codes/03-esp-enrollment.md — verify policy conflict table has all 6 policies; AppWorkload.log (not legacy name)
    5. Open docs/error-codes/04-pre-provisioning.md — verify cross-reference rows point to 02-tpm-attestation.md
    6. Open docs/error-codes/05-hybrid-join.md — verify all 6 event IDs present; 0x80070774 has 3 cause rows; ODJ Connector notes reference new log path
    7. Spot-check: pick any error code from the index, follow the link, confirm it reaches the right row in the category file
    8. Verify all files have consistent frontmatter, version gate banner, prev/next navigation, and APv2 Notes
  </how-to-verify>
  <action>Present all 6 error code files for user review</action>
  <verify>User confirms all files are correct</verify>
  <done>User approves complete error code reference</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- 00-index.md exists and lists every error code from all 5 category files
- Categories section links to all 5 files with descriptions
- Quick Lookup table is sorted by hex ascending, event IDs at end
- No duplicate entries in index (each code appears once, pointing to primary file)
- No-code ESP failures section references policy conflicts in 03-esp-enrollment.md
- All links between error-codes files resolve correctly
- APv2 Notes present
- Version gate banner present
</verification>

<success_criteria>
- Index contains every error code from all 5 category files (estimated 25-30 rows)
- Every Category link resolves to the correct file and anchor
- No error code is missing from the index
- A technician can Ctrl+F any hex code or Event ID on the index page and find it
</success_criteria>

<output>
After completion, create `.planning/phases/03-error-codes/03-03-SUMMARY.md`
</output>
