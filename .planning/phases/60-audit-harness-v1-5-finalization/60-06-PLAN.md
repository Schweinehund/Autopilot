---
phase: 60
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_glossary-android.md
  - docs/_templates/admin-template-ios.md
  - docs/admin-setup-android/10-aosp-zebra.md
autonomous: false
requirements: [AUDIT-05]
must_haves:
  truths:
    - "All 4 broken anchor refs in _glossary-android.md:16 (#kme + #kpe), _templates/admin-template-ios.md:28 (00-overview.md#portal-navigation-note), and admin-setup-android/10-aosp-zebra.md:10 (`#`) resolve after fix"
    - "PITFALL-12 pin-coord watch executed: regenerate-supervision-pins.mjs --report run BEFORE the _glossary-android.md edit; if shifts occur, sidecar pins refreshed in SAME commit per Phase 48 D-14 atomicity-contract (precedent: commit a01ab1d Phase 59)"
    - "Each fix follows D-08 HTML-shim cluster-edit pattern (3 commits = 1 per file)"
    - "No Phase 49-59 V-NN-NN structural invariant breaks"
    - "regenerate-supervision-pins.mjs --self-test exit code unchanged pre/post (still FAIL with current BASELINE_9 staleness; AUDIT-07 close happens in Plan 09 atomic harness commit, not this plan)"
  artifacts:
    - path: "docs/_glossary-android.md"
      provides: "Android glossary with valid #kme and #kpe alphabetical-index anchors"
      contains: "<a id=\"kme\"></a>"
    - path: "docs/_templates/admin-template-ios.md"
      provides: "iOS admin template with valid #portal-navigation-note anchor in 00-overview.md OR rewritten to existing target"
      contains: "00-overview.md"
    - path: "docs/admin-setup-android/10-aosp-zebra.md"
      provides: "Zebra AOSP doc with line 10 bare #-anchor removed or pointed at valid target"
      contains: "10-aosp-zebra.md"
  key_links:
    - from: "_glossary-android.md:16 alphabetical index entries [KME](#kme) and [KPE](#kpe)"
      to: "### KME (Knox Mobile Enrollment) and ### KPE (Knox Platform for Enterprise) H3 sections"
      via: "<a id=\"kme\"></a> and <a id=\"kpe\"></a> shims placed immediately above each H3"
      pattern: "<a id=\"(kme|kpe)\"></a>\\n\\n### (KME|KPE)"
---

<objective>
Fix 4 Category A broken anchor references across 3 files, with mandatory PITFALL-12 pin-coord watch on the `docs/_glossary-android.md:16` edit. This is the ONLY plan in Phase 60 that touches a file appearing in supervision_exemptions[] / safetynet_exemptions[] / c7_knox_allowlist[] sidecar pin lists per RESEARCH PITFALL-12 surface watched.

Purpose: Closes 4 of the 51 Category A baseline findings; honors Phase 48 D-14 atomicity-contract by bundling any pin-coord shifts with the anchor-fix commit. Without this guard, the _glossary-android.md edit could shift pin coordinates and break supervision_exemptions[] without atomic refresh.
Output: 3 modified .md files (1 commit each, with pin-coord refresh bundled into the _glossary-android.md commit if needed)
Autonomous: false — Task 1 includes a `checkpoint:human-verify` step to confirm pin-shift inspection before landing the _glossary commit, per Phase 48 D-14 atomicity-contract precedent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-CONTEXT.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-RESEARCH.md
@.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md
@docs/_glossary-android.md
@scripts/validation/v1.5-audit-allowlist.json
@scripts/validation/regenerate-supervision-pins.mjs

<interfaces>
PITFALL-12 surface watched (per RESEARCH lines 203-208):
- _glossary-android.md:16 is the ONLY Category A anchor in pinned territory
- The file appears in supervision_exemptions[] (lines 12-15, 21, 24-25, 31), safetynet_exemptions[] (lines 6-7), c7_knox_allowlist[] (lines 49-53), AND BASELINE_9 (regenerate-supervision-pins.mjs:393-397)
- ANY line shift in this file requires sidecar pin refresh in same commit per Phase 48 D-14
- Precedent: Phase 59 commit a01ab1d "refresh supervision_exemptions + c7_knox_allowlist + safetynet_exemptions line numbers after Phase 59 line shifts"

48-VERIFICATION-broken-links.md baseline rows for this plan:
  - _glossary-android.md:16 -> #kme (line 34 of 48-VERIFICATION)
  - _glossary-android.md:16 -> #kpe (line 35 of 48-VERIFICATION)
  - _templates/admin-template-ios.md:28 -> 00-overview.md#portal-navigation-note (line 36 of 48-VERIFICATION)
  - admin-setup-android/10-aosp-zebra.md:10 -> `#` bare-fragment (line 37 of 48-VERIFICATION)

Verified file structure for _glossary-android.md (per Read this session):
  - Line 16: `[afw#setup](#afw-setup) | [AMAPI](#amapi) | ... | [KME](#kme) | [KPE](#kpe) | ...`
  - Line 127: `### KME (Knox Mobile Enrollment)`
  - Line 133: `### KPE (Knox Platform for Enterprise)`
  - The {#kme} explicit-id is NOT present on these H3s (the headings carry only the human-readable name)
  - Fix mechanism: D-08(b) HTML-shim — insert `<a id="kme"></a>` and `<a id="kpe"></a>` immediately before lines 127 and 133

Sidecar pin coordinates currently relevant to _glossary-android.md (from v1.5-audit-allowlist.json):
  - safetynet_exemptions: lines 183, 198
  - supervision_exemptions: lines 16, 49, 69, 79, 81, 82, 179, 193, 196
  - c7_knox_allowlist: lines 121, 123 (x2), 125, 195

If shims at lines 126 and 132 (immediately above lines 127 and 133) insert as 2-line blocks (shim + blank line), the file gains +4 lines TOTAL between line 126 and line 134. This shifts:
  - safetynet_exemptions:183 -> 187 (+4)
  - safetynet_exemptions:198 -> 202 (+4)
  - supervision_exemptions:179 -> 183 (+4)
  - supervision_exemptions:193 -> 197 (+4)
  - supervision_exemptions:196 -> 200 (+4)
  - c7_knox_allowlist:121,123,125 -> 125,127,129 (+4 each, since they sit AFTER line 121-125)
  - c7_knox_allowlist:195 -> 199 (+4)
  - BASELINE_9 entries 172,188 -> 176,192 (+4)
  - Lines 16, 49, 69, 79, 81, 82 are BEFORE the inserted shims and DO NOT shift.

Final pin-refresh count: ~11 pin entries need line-coord update if 2-line shims (shim + blank line) inserted at line 126 and line 132. If single-line shims (no blank line below), the shift is +2 instead of +4.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run pre-edit pin-report; capture baseline pin coords</name>
  <files>
    .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt
  </files>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs (lines 1-50; confirm --report mode usage)
    - scripts/validation/v1.5-audit-allowlist.json (current pin coordinates pre-edit)
  </read_first>
  <action>
    Capture current pin-coordinate state BEFORE _glossary-android.md edit. This is the PITFALL-12 pre-edit baseline per RESEARCH "Pattern 5: PITFALL-12 Pin-Coord Watch" + Phase 48 D-14.

    Step 1: Run the report mode and save output:
    ```
    node scripts/validation/regenerate-supervision-pins.mjs --report > .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt 2>&1
    ```

    Step 2: Capture sidecar pin coordinates verbatim (separate snapshot for diff comparison post-edit):
    ```bash
    grep '"docs/_glossary-android.md"' scripts/validation/v1.5-audit-allowlist.json >> .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt
    echo "---BASELINE_9---" >> .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt
    grep -A 12 'const BASELINE_9' scripts/validation/regenerate-supervision-pins.mjs >> .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt
    ```

    DO NOT commit this artifact yet; it is a transient pin-baseline reference for diff comparison. The file will be deleted at end of plan after Task 4 verifies pin coords match expectations.

    No git commit for this task; pure reconnaissance.
  </action>
  <verify>
    <automated>test -f .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt && grep -c '_glossary-android.md' .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt | awk '{ if ($1 >= 8) print "PASS pin-baseline-captured"; else print "FAIL pin-baseline="$1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File `60-06-PIN-PRE-EDIT.txt` exists in `.planning/phases/60-audit-harness-v1-5-finalization/`
    - File contains at least 8 lines mentioning `_glossary-android.md` (multiple sidecar pin entries + BASELINE_9 entries)
    - File contains `---BASELINE_9---` separator line
    - No git commit; this file is NOT staged
    - No edits to any production file
  </acceptance_criteria>
  <done>Pre-edit pin baseline captured for diff comparison post-edit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix _glossary-android.md (2 anchor shims for #kme + #kpe) with mandatory pin-coord refresh in same commit</name>
  <files>
    docs/_glossary-android.md,
    scripts/validation/v1.5-audit-allowlist.json,
    scripts/validation/regenerate-supervision-pins.mjs
  </files>
  <read_first>
    - docs/_glossary-android.md (lines 1-200; locate ### KME and ### KPE H3s; verified at lines 127 and 133)
    - scripts/validation/v1.5-audit-allowlist.json (full file; current pin coordinates referenced in interfaces block)
    - scripts/validation/regenerate-supervision-pins.mjs lines 386-403 (BASELINE_9 array; entries 3 and 4 are _glossary-android.md:172 and :188)
    - .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt (Task 1 baseline)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-CONTEXT.md (D-14 atomicity-contract reference)
  </read_first>
  <action>
    This is the ATOMIC PITFALL-12 commit. ONE git commit must bundle:
    1. _glossary-android.md edit (2 HTML shims)
    2. v1.5-audit-allowlist.json pin-coord refresh (if shifts detected)
    3. regenerate-supervision-pins.mjs BASELINE_9 line-coord refresh (if shifts detected)

    Step 1: Edit _glossary-android.md. Insert shims at the verified H3 locations:

    Above line 127 (`### KME (Knox Mobile Enrollment)`):
    ```
    <a id="kme"></a>
    ### KME (Knox Mobile Enrollment)
    ```
    (Use single-line insertion — shim + heading on consecutive lines, no blank line between. This keeps the line shift to +1 per shim, total +2 across the 2 shims, minimizing the pin-refresh surface.)

    Above line 133 (`### KPE (Knox Platform for Enterprise)`):
    ```
    <a id="kpe"></a>
    ### KPE (Knox Platform for Enterprise)
    ```

    DO NOT modify line 16 (the alphabetical index — the [KME](#kme) and [KPE](#kpe) link refs already exist there). DO NOT modify the H3 heading text. DO NOT modify any other content.

    Step 2: Re-run pin report:
    ```
    node scripts/validation/regenerate-supervision-pins.mjs --report > /tmp/post-edit.txt 2>&1
    ```

    Step 3: Diff against Task 1 baseline:
    ```
    diff .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt /tmp/post-edit.txt
    ```

    Step 4: Compute expected line-shift impact. Single-line shim insertion at lines 127 and 133:
    - Lines 1-126: NO shift (all pins at lines 16, 49, 69, 79, 81, 82, 121, 123, 125 are unchanged)
    - Line 127 onwards: +1 shift (line 127 H3 moves to line 128 etc.)
    - Lines after second shim insertion (originally line 134+): +2 shift (cumulative)

    Pin coordinates that need refresh in v1.5-audit-allowlist.json:
    - safetynet_exemptions:183 -> 185 (+2)
    - safetynet_exemptions:198 -> 200 (+2)
    - supervision_exemptions:179 -> 181 (+2)
    - supervision_exemptions:193 -> 195 (+2)
    - supervision_exemptions:196 -> 198 (+2)
    - c7_knox_allowlist:195 -> 197 (+2)
    - Lines 121, 123, 125 (pre-line-127): NO shift
    - Lines 16, 49, 69, 79, 81, 82 (pre-line-127): NO shift

    BASELINE_9 entries that need refresh in regenerate-supervision-pins.mjs:
    - ['docs/_glossary-android.md', 172] -> 174 (+2)
    - ['docs/_glossary-android.md', 188] -> 190 (+2)
    - Lines 76, 78 (pre-line-127): NO shift

    Step 5: Apply the pin-coord updates. For each shifted entry in v1.5-audit-allowlist.json, replace the integer `"line": N` with `"line": N+2`. Update the reason text if it cites a Phase number to add a note like `(Phase 60 carry-over: line N shifted +2 from #kme/#kpe shim insert)`.

    For BASELINE_9, update the array entries and add a comment block above the array:
    ```javascript
    // BASELINE_9 refreshed 2026-05-06 (Phase 60 Plan 06): Phase 59 line positions
    // updated to current after Phase 60 _glossary-android.md #kme/#kpe HTML-shim
    // insertion at lines 127 and 133 (net +2 lines after line 126).
    ```
    (Preserve the existing `// BASELINE_9 refreshed 2026-04-26` comment as the historical record above the new comment.)

    Step 6: Verify all 3 file changes parse + harness still GREEN:
    ```
    node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8'))" && echo "JSON OK"
    node scripts/validation/v1.5-milestone-audit.mjs > /dev/null 2>&1 && echo "harness GREEN"
    ```

    Step 7: Single ATOMIC commit bundling all 3 files:
    ```
    git add docs/_glossary-android.md scripts/validation/v1.5-audit-allowlist.json scripts/validation/regenerate-supervision-pins.mjs
    git commit -m "fix(60-06): add #kme + #kpe HTML shims to _glossary-android.md + refresh sidecar+BASELINE_9 pin coords (PITFALL-12 atomic per Phase 48 D-14)"
    ```

    Critical: do NOT split this into multiple commits. The atomicity-contract requires one commit per Phase 48 D-14 / Phase 59 commit a01ab1d precedent.
  </action>
  <verify>
    <automated>grep -c '<a id="kme"></a>\|<a id="kpe"></a>' docs/_glossary-android.md | awk '{ if ($1 >= 2) print "PASS shims="$1; else print "FAIL shims="$1 }' && node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8')); console.log('PASS json-parse')" && node scripts/validation/v1.5-milestone-audit.mjs > /dev/null 2>&1 && echo "PASS harness-green" || echo "FAIL harness" && git log --oneline -1 | grep -c 'fix(60-06):' | awk '{ if ($1 == 1) print "PASS commit-landed"; else print "FAIL no-commit" }'</automated>
  </verify>
  <acceptance_criteria>
    - docs/_glossary-android.md contains exactly 2 net new HTML shim lines: `<a id="kme"></a>` and `<a id="kpe"></a>` (each `grep -c "id=\"<name>\"" file == 1`)
    - Each shim sits immediately before the corresponding `### KME` or `### KPE` H3 (verified: line offset between shim and H3 is 1)
    - scripts/validation/v1.5-audit-allowlist.json parses as valid JSON (`node -e "JSON.parse(...)"` exits 0)
    - scripts/validation/v1.5-audit-allowlist.json pin coordinates refreshed for entries originally at lines 179/183/188/193/195/196/198 (now shifted by +2 to lines 181/185/190/195/197/198/200 respectively — exact mapping per action step 4)
    - scripts/validation/regenerate-supervision-pins.mjs BASELINE_9 entries 3-4 updated: `['docs/_glossary-android.md', 174]` and `['docs/_glossary-android.md', 190]`
    - regenerate-supervision-pins.mjs has new comment line containing `BASELINE_9 refreshed 2026-05-06 (Phase 60 Plan 06)`
    - `node scripts/validation/v1.5-milestone-audit.mjs` exits 0
    - SINGLE git commit (`git log --oneline -1` shows 1 entry with `fix(60-06):` prefix touching all 3 files)
    - Phase 49-59 V-NN-NN check-phase chain validators all still PASS: `for n in 48 49 51 52 53 54 55 56 57 58 59; do node scripts/validation/check-phase-$n.mjs > /dev/null 2>&1 || echo "FAIL phase-$n"; done` produces no FAIL lines
  </acceptance_criteria>
  <done>Atomic 3-file commit lands; shims + pin-refresh + BASELINE_9 refresh all in one commit; harness GREEN; chain validators GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PITFALL-12 atomic-commit verification gate (human inspect pin-shift correctness)</name>
  <what-built>
    Task 2 atomically updated _glossary-android.md with 2 anchor shims AND refreshed sidecar pin coordinates AND refreshed regenerate-supervision-pins.mjs BASELINE_9 in a single commit, per Phase 48 D-14 atomicity-contract (Phase 59 commit a01ab1d precedent).
  </what-built>
  <how-to-verify>
    1. Inspect commit: `git show HEAD --stat` — confirm exactly 3 files changed (docs/_glossary-android.md, scripts/validation/v1.5-audit-allowlist.json, scripts/validation/regenerate-supervision-pins.mjs)
    2. Visual diff sanity check on sidecar pin shifts:
       ```
       git show HEAD -- scripts/validation/v1.5-audit-allowlist.json | grep -E '^[+-]\s+\{"file":\s*"docs/_glossary-android.md"'
       ```
       Expect 6-8 paired +/- lines showing line-coord increments by +2 for entries previously at lines 179/183/188/193/195/196/198.
    3. Visual diff sanity check on BASELINE_9:
       ```
       git show HEAD -- scripts/validation/regenerate-supervision-pins.mjs | grep -E '^[+-]\s+\['
       ```
       Expect 2 paired +/- lines showing 172 -> 174 and 188 -> 190.
    4. Run full validator chain: `node scripts/validation/v1.5-milestone-audit.mjs --verbose && for n in 48 49 51 52 53 54 55 56 57 58 59; do node scripts/validation/check-phase-$n.mjs; done` — every command must exit 0.
    5. Confirm regenerate-supervision-pins.mjs --self-test STILL fails with the SAME failure message as pre-Plan-06 (the AUDIT-07 close happens in Plan 09, not here):
       ```
       node scripts/validation/regenerate-supervision-pins.mjs --self-test
       ```
       Expected exit code: 1 (still). Expected failure message: still references stale BASELINE_9 baseline mapping. THIS IS EXPECTED — Plan 09 closes AUDIT-07.
  </how-to-verify>
  <resume-signal>Type "approved" if pin-shift diffs and validator chain look correct, OR describe any anomaly (e.g., "shift is +4 instead of +2 — shims may have inserted blank lines")</resume-signal>

  <files>(checkpoint-task — no files modified by this task; previous tasks staged the files)</files>
  <action>Pause for human verification per <how-to-verify> instructions. No automated action; human runs the commands listed and inspects output. If all PASS, type the resume-signal to continue.</action>
  <verify>Manual: human confirms each step in <how-to-verify> exits 0 / produces expected output before approving.</verify>
  <done>Human types "approved" (or describes failure for retry).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix _templates/admin-template-ios.md:28 + admin-setup-android/10-aosp-zebra.md:10 (2 path/anchor fixes); cleanup pin-baseline artifact</name>
  <files>
    docs/_templates/admin-template-ios.md,
    docs/admin-setup-android/10-aosp-zebra.md,
    .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt
  </files>
  <read_first>
    - docs/_templates/admin-template-ios.md (lines 1-100; locate line 28 link target `00-overview.md#portal-navigation-note`; check if `../admin-setup-ios/00-overview.md` exists with that anchor)
    - docs/admin-setup-android/10-aosp-zebra.md (lines 1-30; line 10 has bare `#` anchor link)
    - docs/admin-setup-ios/00-overview.md (verify presence + scan for any existing #portal-navigation-note anchor or near-equivalent like #portal or #navigation)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md lines 36-37
  </read_first>
  <action>
    File 1: docs/_templates/admin-template-ios.md line 28 (1 fix):

    The broken target is `00-overview.md#portal-navigation-note`. Inspect `docs/admin-setup-ios/00-overview.md` for an existing H2/H3 with text matching "Portal Navigation" or similar. If found, the GFM-slug should be `#portal-navigation` or `#portal-navigation-note`. Apply D-08 mechanism per finding:
      - If a heading with text "Portal Navigation Note" exists, add `<a id="portal-navigation-note"></a>` shim above it in `docs/admin-setup-ios/00-overview.md` (preferred since 00-overview.md is the link target file)
      - If no matching heading exists, rewrite the link at `docs/_templates/admin-template-ios.md:28` to point at the closest existing H2/H3 in 00-overview.md (use D-08(a) rewrite-ref).
      - If 00-overview.md does not contain any portal-navigation content at all, REMOVE the broken link reference at line 28 (delete the bullet/sentence containing `00-overview.md#portal-navigation-note`).

    Document the choice in commit message: `fix(60-06): repair admin-template-ios.md:28 broken anchor (chose: <shim|rewrite|delete>)`. Touch ONLY the file containing the fix mechanism (either admin-template-ios.md OR admin-setup-ios/00-overview.md OR both depending on chosen mechanism).

    File 2: docs/admin-setup-android/10-aosp-zebra.md line 10 (1 fix):

    Line 10 contains a bare `#` fragment in a markdown link (e.g., `[text](#)`). This is a placeholder that points nowhere. Apply D-08(a) rewrite-ref OR D-08 delete-ref:
      - If line 10 is a meaningful section reference whose target exists elsewhere in the file (e.g., should point at "## Prerequisites" -> #prerequisites), rewrite the link to that GFM-slug
      - If line 10 is a placeholder bullet with no clear intended target, REMOVE the link wrapper (e.g., `[Setup steps](#)` -> `Setup steps`) preserving the prose text

    Inspect line 10 context to determine which fix applies. Document choice in commit message: `fix(60-06): repair 10-aosp-zebra.md:10 bare-fragment anchor (chose: <rewrite|delete>)`.

    Cleanup: After both fixes land, remove the transient pin-baseline file:
    ```
    git rm .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt
    git commit -m "chore(60-06): cleanup pin-baseline artifact post-Plan-06 close"
    ```
    (The PIN-PRE-EDIT.txt file is NOT a long-term artifact — it served only to support Task 3 manual verification.)

    Total commits in Plan 06: 3 (Task 2 atomic harness commit + Task 4 fix-1 commit + Task 4 fix-2 commit + Task 4 cleanup commit = 4 commits actually). Acceptable per D-25 progressive-landing.

    Regression-guard after each commit: `node scripts/validation/v1.5-milestone-audit.mjs` exits 0.
  </action>
  <verify>
    <automated>grep -c '00-overview.md#portal-navigation-note' docs/_templates/admin-template-ios.md | awk '{ if ($1 == 0) print "PASS admin-template-fixed"; else print "INFO still-references-broken-target ($1)" }' && grep -E '^\[.*\]\(#\)' docs/admin-setup-android/10-aosp-zebra.md | wc -l | awk '{ if ($1 == 0) print "PASS zebra-fixed"; else print "FAIL zebra-bare-anchor-remains" }' && test ! -f .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt && echo "PASS pin-baseline-cleaned" || echo "FAIL pin-baseline-still-present" && node scripts/validation/v1.5-milestone-audit.mjs > /dev/null 2>&1 && echo "PASS harness-green" || echo "FAIL harness"</automated>
  </verify>
  <acceptance_criteria>
    - docs/_templates/admin-template-ios.md no longer contains the broken target string `00-overview.md#portal-navigation-note` (verified: `grep -c "portal-navigation-note" docs/_templates/admin-template-ios.md == 0` UNLESS the chosen mechanism added a shim in 00-overview.md and kept the link, in which case the link is now resolvable — both outcomes acceptable)
    - docs/admin-setup-android/10-aosp-zebra.md line 10 no longer contains `](#)` bare-fragment pattern (verified: `grep -E '\\]\\(#\\)' docs/admin-setup-android/10-aosp-zebra.md` returns 0 matches at line 10)
    - PIN-PRE-EDIT.txt artifact deleted (`test ! -f .planning/phases/60-audit-harness-v1-5-finalization/60-06-PIN-PRE-EDIT.txt` returns true)
    - Commit messages document the chosen fix mechanism (`<shim|rewrite|delete>` keyword present)
    - `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 after each commit
    - Phase 49-59 chain validators all still GREEN
  </acceptance_criteria>
  <done>2 broken targets resolved + transient artifact cleaned; harness regression-guard PASSES; plan completes 4/4 anchor/path fixes for the PITFALL-12-vulnerable cluster.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown content edit -> sidecar pin-coord state | _glossary-android.md edit can shift pin coordinates pinned in sidecar; uncoordinated edit breaks atomicity-contract. |
| sidecar JSON parse -> harness load | Pin-coord update must preserve valid JSON; parse failure cascades to harness FAIL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-06-01 | T (Tampering) | sidecar pin coordinates | mitigate | Atomic 3-file commit per Phase 48 D-14; pre-edit pin-baseline captured + post-edit diff verified at Task 3 checkpoint. |
| T-60-06-02 | T (Tampering) | regenerate-supervision-pins.mjs BASELINE_9 array | mitigate | Refresh in same commit as content edit; comment block updated to document refresh date + rationale. |
| T-60-06-03 | I (Information Disclosure) | PIN-PRE-EDIT.txt transient file | accept | Cleanup at Task 4 ensures no pin baseline leaks into long-term `.planning/` history. |
| T-60-06-04 | D (DoS via parse failure) | v1.5-audit-allowlist.json | mitigate | `node -e "JSON.parse(...)"` validation gate at Task 2 step 6 before commit. |
</threat_model>

<verification>
- 4 commits land in this plan (Task 2 atomic harness + Task 4 admin-template-ios + Task 4 10-aosp-zebra + Task 4 cleanup)
- Atomic commit (Task 2) touches exactly 3 files: docs/_glossary-android.md, scripts/validation/v1.5-audit-allowlist.json, scripts/validation/regenerate-supervision-pins.mjs
- Sidecar pin coords for entries originally at lines 179/183/188/193/195/196/198 shifted by +2
- BASELINE_9 entries 3-4 shifted by +2 (172 -> 174, 188 -> 190)
- regenerate-supervision-pins.mjs --self-test STILL FAILS pre-Plan-09 (AUDIT-07 close is Plan 09's responsibility per CONTEXT D-19)
- `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 after every commit
- Phase 49-59 chain validators all GREEN
- PIN-PRE-EDIT.txt transient artifact deleted
</verification>

<success_criteria>
- 4 broken anchor/path refs in 48-VERIFICATION baseline rows 34-37 resolved
- PITFALL-12 atomic-commit pattern honored (Phase 48 D-14 contract)
- Pin-coord shifts reconciled in same commit as content edit
- Phase 49-59 V-NN-NN structural assertions preserved
- Plan 09 atomic harness commit unblocked (no further pin-coord refresh dependencies)
</success_criteria>

<output>
After completion, create `.planning/phases/60-audit-harness-v1-5-finalization/60-06-SUMMARY.md`
</output>
