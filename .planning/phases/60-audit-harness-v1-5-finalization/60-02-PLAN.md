---
phase: 60
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/l1-runbooks/11-macos-setup-assistant-failed.md
  - docs/l1-runbooks/12-macos-profile-not-applied.md
  - docs/l1-runbooks/13-macos-app-not-installed.md
  - docs/l1-runbooks/14-macos-compliance-access-blocked.md
autonomous: true
requirements: [AUDIT-05]
must_haves:
  truths:
    - "All 8 broken anchor refs in docs/l1-runbooks/{11,12,13,14}-macos-*.md (per 48-VERIFICATION-broken-links.md lines 41-50) resolve to existing in-document anchors after fix"
    - "Each fix follows D-08 cluster-edit pattern (rewrite ref OR add <a id> shim) with per-anchor rationale committed"
    - "Pre-Plan-09 second-pass markdown-link-check sweep finds 0 NEW findings against 48-VERIFICATION baseline (per D-12)"
    - "No Phase 49-59 V-NN-NN structural invariant breaks (regression-guard satisfied per D-26)"
  artifacts:
    - path: "docs/l1-runbooks/11-macos-setup-assistant-failed.md"
      provides: "ESP/setup-assistant runbook with valid anchors at lines 25-27"
      contains: "<a id=\"authentication-failure\"></a>"
    - path: "docs/l1-runbooks/12-macos-profile-not-applied.md"
      provides: "Profile-not-applied runbook with valid anchors"
      contains: "<a id=\"profile-not-showing\"></a>"
    - path: "docs/l1-runbooks/13-macos-app-not-installed.md"
      provides: "App-not-installed runbook with valid anchors"
      contains: "<a id=\"dmg-pkg-missing\"></a>"
    - path: "docs/l1-runbooks/14-macos-compliance-access-blocked.md"
      provides: "Compliance-access-blocked runbook with valid anchors"
      contains: "<a id=\"device-non-compliant\"></a>"
  key_links:
    - from: "Lines 25-27 of each macOS runbook (quick-nav TOC bullet list)"
      to: "Per-cause H2/H3 sections in same file"
      via: "<a id=\"...\"></a> shim placed at start of target section"
      pattern: "<a id=\"(authentication-failure|await-configuration-stuck|network-connectivity|profile-not-showing|profile-showing-not-working|dmg-pkg-missing|vpp-missing|app-install-failed|device-non-compliant|compliant-access-blocked)\"></a>"
---

<objective>
Fix 8 Category A broken anchor references in 4 macOS l1-runbooks (lines 25-29 each, quick-nav TOC blocks). Apply D-08 cluster-edit pattern: add explicit `<a id="..."></a>` shim at the section each anchor was intended to address. Per-file commit per D-25 progressive-landing.

Purpose: Closes 8 of the 51 Category A baseline findings before Plan 09's atomic harness commit promotes C13 to blocking. Without this fix, C13 promotion FAILs on these refs.
Output: 4 modified .md files (1 commit each = 4 commits)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-CONTEXT.md
@.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md
@docs/l1-runbooks/02-esp-stuck-or-failed.md

<interfaces>
<!-- Anchor-shim pattern reference: docs/l1-runbooks/02-esp-stuck-or-failed.md line 35 already uses {#device-phase-steps} -->
<!-- However mlc-tool 3.14.2 doesn't honor {#explicit-id} extended-attribute; only honors <a id="..."></a> HTML shims and GFM auto-slugs. -->

D-08 fix mechanism (per 60-CONTEXT.md decisions):
  (a) Rewrite anchor ref to existing GFM-slug -- preferred for typos/casing
  (b) Add <a id="..."></a> HTML shim immediately before the heading -- preferred when broken anchor encodes meaningful intent

For ALL fixes in this plan, use (b) HTML-shim approach because:
  1. The {#explicit-id} attribute already present at section H2/H3 lines but mlc-tool 3.14.2 doesn't recognize it
  2. <a id="..."></a> is GFM-honored and mlc-tool-honored
  3. Preserves the intentional anchor name; no link-call-site rewrites needed
  4. Cluster-edit per file: 2-3 shim insertions per file is small surface

48-VERIFICATION-broken-links.md baseline rows (lines 41-50) for this plan:
  | docs/l1-runbooks/11-macos-setup-assistant-failed.md | 25 | `#authentication-failure` |
  | docs/l1-runbooks/11-macos-setup-assistant-failed.md | 26 | `#await-configuration-stuck` |
  | docs/l1-runbooks/11-macos-setup-assistant-failed.md | 27 | `#network-connectivity` |
  | docs/l1-runbooks/12-macos-profile-not-applied.md | 25 | `#profile-not-showing` |
  | docs/l1-runbooks/12-macos-profile-not-applied.md | 26 | `#profile-showing-not-working` |
  | docs/l1-runbooks/13-macos-app-not-installed.md | 25 | `#dmg-pkg-missing` |
  | docs/l1-runbooks/13-macos-app-not-installed.md | 26 | `#vpp-missing` |
  | docs/l1-runbooks/13-macos-app-not-installed.md | 27 | `#app-install-failed` |
  | docs/l1-runbooks/14-macos-compliance-access-blocked.md | 28 | `#device-non-compliant` |
  | docs/l1-runbooks/14-macos-compliance-access-blocked.md | 29 | `#compliant-access-blocked` |

(That is 10 anchor refs across 4 files. Plan 02 closes all 10. Note: 48-VERIFICATION lists 10 entries for these 4 files; CONTEXT D-08 cluster-edit pattern correctly describes this as ~10-anchor cluster across this 4-file plan.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix 11-macos-setup-assistant-failed.md (3 anchor shims)</name>
  <files>docs/l1-runbooks/11-macos-setup-assistant-failed.md</files>
  <read_first>
    - docs/l1-runbooks/11-macos-setup-assistant-failed.md (full file — locate H2/H3 targets for each broken anchor)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md lines 41-43 (3 broken anchors at lines 25-27)
  </read_first>
  <action>
    The file has 3 broken anchor references at lines 25, 26, 27 in the quick-nav TOC block:
      - Line 25: `[Authentication Failure](#authentication-failure)` -> target heading text "Authentication Failure" or similar
      - Line 26: `[Await Configuration Stuck](#await-configuration-stuck)` -> target heading "Await Configuration Stuck"
      - Line 27: `[Network Connectivity](#network-connectivity)` -> target heading "Network Connectivity"

    Step 1: Read the full file. For each broken anchor, locate the corresponding H2 or H3 section by searching for the heading text. The section already exists; only the explicit id-shim is missing.

    Step 2: For EACH of the 3 sections, insert `<a id="<anchor-name>"></a>` on its own line IMMEDIATELY ABOVE the H2/H3 heading line. Concrete edits:

    Edit 1 (anchor `authentication-failure`):
    ```
    BEFORE:
    ## Authentication Failure {#authentication-failure}
    or
    ## Authentication Failure

    AFTER (insert one line above the heading):
    <a id="authentication-failure"></a>

    ## Authentication Failure {#authentication-failure}
    ```

    Edit 2 (anchor `await-configuration-stuck`): same pattern; insert `<a id="await-configuration-stuck"></a>` immediately above the matching H2/H3.

    Edit 3 (anchor `network-connectivity`): same pattern; insert `<a id="network-connectivity"></a>` immediately above the matching H2/H3.

    DO NOT modify the heading text or {#explicit-id} attribute. DO NOT modify the quick-nav TOC bullets at lines 25-27. ONLY add the 3 HTML-shim lines.

    If a heading does NOT exist in the file for one of the 3 anchors, the broken link points at intended-but-never-authored content. In that case, fall back to D-08(a): rewrite the broken ref at line 25/26/27 to the closest existing H2/H3 GFM-slug. Document choice in commit message.

    Per D-25, commit this single file change as one commit. Commit message format:
    `fix(60-02): repair 3 broken anchors in 11-macos-setup-assistant-failed.md (cluster-edit per D-08)`
  </action>
  <verify>
    <automated>grep -c '<a id="authentication-failure"></a>\|<a id="await-configuration-stuck"></a>\|<a id="network-connectivity"></a>' docs/l1-runbooks/11-macos-setup-assistant-failed.md | awk '{ if ($1 >= 3) print "PASS shims="$1; else print "FAIL shims="$1 }' && node scripts/validation/v1.5-milestone-audit.mjs > /dev/null 2>&1 && echo "PASS harness-still-green" || echo "FAIL harness-broken"</automated>
  </verify>
  <acceptance_criteria>
    - File contains 3 new shim lines: `<a id="authentication-failure"></a>`, `<a id="await-configuration-stuck"></a>`, `<a id="network-connectivity"></a>` (verified: `grep -c '<a id=' docs/l1-runbooks/11-macos-setup-assistant-failed.md` >= 3 net new shims)
    - Each shim sits immediately above the corresponding H2 or H3 heading line (verified: `grep -B 0 -A 1 '<a id=' file | grep -c '^##'` matches shim count)
    - File frontmatter unchanged (`grep -c 'last_verified:' file == 1`; same value as pre-edit)
    - `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 (regression-guard — harness still GREEN in current informational mode)
    - Quick-nav TOC bullet text at lines 25-27 unchanged (only shims added; not link-text rewrites)
  </acceptance_criteria>
  <done>3 HTML shims added; harness regression-guard PASSES; commit landed with message `fix(60-02): repair 3 broken anchors ...`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix 12-macos-profile-not-applied.md + 13-macos-app-not-installed.md + 14-macos-compliance-access-blocked.md (7 anchor shims across 3 files)</name>
  <files>
    docs/l1-runbooks/12-macos-profile-not-applied.md,
    docs/l1-runbooks/13-macos-app-not-installed.md,
    docs/l1-runbooks/14-macos-compliance-access-blocked.md
  </files>
  <read_first>
    - docs/l1-runbooks/12-macos-profile-not-applied.md (lines 1-150; locate H2/H3 for each broken anchor)
    - docs/l1-runbooks/13-macos-app-not-installed.md (lines 1-150; locate H2/H3 for each broken anchor)
    - docs/l1-runbooks/14-macos-compliance-access-blocked.md (lines 1-150; locate H2/H3 for each broken anchor)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md lines 44-50 (7 broken anchors at lines 25-29 of these 3 files)
  </read_first>
  <action>
    Apply D-08 HTML-shim pattern to 7 broken anchors across 3 files. Land as 3 separate commits (one per file) per D-25 progressive-landing.

    File 1: docs/l1-runbooks/12-macos-profile-not-applied.md (2 shims):
      - Insert `<a id="profile-not-showing"></a>` immediately above the matching H2/H3 (target text: "Profile Not Showing" or similar)
      - Insert `<a id="profile-showing-not-working"></a>` immediately above the matching H2/H3 (target text: "Profile Showing But Not Working" or similar)
      - Commit: `fix(60-02): repair 2 broken anchors in 12-macos-profile-not-applied.md`

    File 2: docs/l1-runbooks/13-macos-app-not-installed.md (3 shims):
      - Insert `<a id="dmg-pkg-missing"></a>` immediately above matching H2/H3 (target: "DMG/PKG Missing" or similar)
      - Insert `<a id="vpp-missing"></a>` immediately above matching H2/H3 (target: "VPP Missing" or similar)
      - Insert `<a id="app-install-failed"></a>` immediately above matching H2/H3 (target: "App Install Failed" or similar)
      - Commit: `fix(60-02): repair 3 broken anchors in 13-macos-app-not-installed.md`

    File 3: docs/l1-runbooks/14-macos-compliance-access-blocked.md (2 shims):
      - Insert `<a id="device-non-compliant"></a>` immediately above matching H2/H3 (target: "Device Non-Compliant" or similar)
      - Insert `<a id="compliant-access-blocked"></a>` immediately above matching H2/H3 (target: "Compliant Access Blocked" or similar)
      - Commit: `fix(60-02): repair 2 broken anchors in 14-macos-compliance-access-blocked.md`

    For each file: DO NOT modify quick-nav TOC bullets, DO NOT modify heading text, DO NOT modify frontmatter. Only insert HTML shims.

    If a target heading is missing for any anchor (rare; same fall-back as Task 1), apply D-08(a) rewrite-ref instead and document choice in commit message.

    Run regression-guard after EACH commit:
    ```
    node scripts/validation/v1.5-milestone-audit.mjs && echo "harness GREEN"
    ```
  </action>
  <verify>
    <automated>grep -c '<a id=' docs/l1-runbooks/12-macos-profile-not-applied.md | awk '{ if ($1 >= 2) print "PASS file12-shims="$1; else print "FAIL file12-shims="$1 }' && grep -c '<a id=' docs/l1-runbooks/13-macos-app-not-installed.md | awk '{ if ($1 >= 3) print "PASS file13-shims="$1; else print "FAIL file13-shims="$1 }' && grep -c '<a id=' docs/l1-runbooks/14-macos-compliance-access-blocked.md | awk '{ if ($1 >= 2) print "PASS file14-shims="$1; else print "FAIL file14-shims="$1 }' && node scripts/validation/v1.5-milestone-audit.mjs > /dev/null 2>&1 && echo "PASS harness-still-green" || echo "FAIL harness-broken"</automated>
  </verify>
  <acceptance_criteria>
    - File 12 contains 2 net new shim lines: `<a id="profile-not-showing"></a>` and `<a id="profile-showing-not-working"></a>`
    - File 13 contains 3 net new shim lines: `<a id="dmg-pkg-missing"></a>`, `<a id="vpp-missing"></a>`, `<a id="app-install-failed"></a>`
    - File 14 contains 2 net new shim lines: `<a id="device-non-compliant"></a>` and `<a id="compliant-access-blocked"></a>`
    - Frontmatter unchanged in all 3 files (`grep -c 'last_verified:' file == 1` each)
    - 3 separate commits in git log: `git log --oneline -10 | grep -c 'fix(60-02): repair' == 3` (Task 1 already added 1; Task 2 adds 3 -> total 4 commits at end)
    - `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 after each commit
  </acceptance_criteria>
  <done>7 HTML shims added across 3 files in 3 commits; harness regression-guard PASSES after each commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown content -> harness scope | Modified docs are within harness scan scope (walkMd('docs')); fixes must not introduce C1-C13 violations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02-01 | T (Tampering) | docs/l1-runbooks/{11,12,13,14}-macos-*.md content | mitigate | Each task's regression-guard runs harness exit-0 check after every commit; harness FAIL aborts plan landing. |
| T-60-02-02 | I (Information Disclosure) | runbook content visible to L1 ops | accept | Shims are zero-content HTML elements; no information added. |
</threat_model>

<verification>
- `grep -c '<a id=' docs/l1-runbooks/{11,12,13,14}-macos-*.md` totals >= 10 (3+2+3+2)
- `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 after each commit
- Pre-Plan-09 second-pass markdown-link-check for these 4 files reports 0 broken anchors (post-fix verification)
- 4 commits land in `gsd/phase-60-audit-harness-v1-5-finalization` (or current branch) with `fix(60-02):` prefix
</verification>

<success_criteria>
- 10 broken anchor refs in 48-VERIFICATION-broken-links.md baseline rows 41-50 are now resolvable in their host files
- Each fix follows D-08 HTML-shim pattern (no link-text rewrites, no heading edits)
- 4 separate commits land per D-25 progressive-landing
- Harness regression-guard PASSES after each commit
- No PITFALL-12 surface — none of these files appear in supervision_exemptions[] / safetynet_exemptions[] / c7_knox_allowlist[] sidecar pin lists (confirmed by RESEARCH PITFALL-12 surface watched line 207)
</success_criteria>

<output>
After completion, create `.planning/phases/60-audit-harness-v1-5-finalization/60-02-SUMMARY.md`
</output>
