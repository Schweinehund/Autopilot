---
phase: 60
plan: 08
type: execute
wave: 2
depends_on: ["60-01", "60-02", "60-03", "60-04", "60-05", "60-06", "60-07"]
files_modified:
  - scripts/validation/v1.5-milestone-audit.mjs
  - scripts/validation/v1.5-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
  - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md
autonomous: false
requirements: [AUDIT-03, AUDIT-04, AUDIT-05, AUDIT-07]
must_haves:
  truths:
    - "C9 (cope_banned_phrases) promoted from informational to blocking via informational:true line removal AND new exemption-mechanism wiring (D-17 + D-18)"
    - "C11 (ops-domain anti-patterns) promoted from informational to blocking via informational:true line removal AND new proximity-window negation logic mirroring C1 SafetyNet pattern (D-01)"
    - "C12 (4-platform comparison) scope expanded to verify 6 named H2 anchors (Enrollment / Configuration / App Deployment / Compliance / Software Updates / Conditional Access) per D-13 + D-16; informational:true already absent (Plan 58-06 commit bc9cee6)"
    - "C13 (broken-link automation) promoted from informational to blocking via informational:true line removal AND c13_broken_link_allowlist[] application (D-06)"
    - "v1.5-audit-allowlist.json grows by 3 NEW arrays: c9_exemptions[] (per Plan 01 60-CALIBRATION.md seed), c11_ops_exemptions[] (reserved-empty per D-02), c13_broken_link_allowlist[] (15 entries: 6 transient_external + 9 template_placeholder per D-10)"
    - "regenerate-supervision-pins.mjs BASELINE_9 array refreshed; --self-test exits 0 (closes AUDIT-07 carry-over per D-19)"
    - "48-VERIFICATION-broken-links.md Triage Decision column populated for all 75 entries; Total findings: 75 line preserved byte-identical (D-11)"
    - "Sidecar phase field updated to 60-audit-harness-v1-5-finalization; generated timestamp updated"
    - "ALL changes (harness file + sidecar JSON + regenerate-supervision-pins.mjs + 48-VERIFICATION-broken-links.md) land in ONE atomic git commit per Phase 43 D-07 atomicity-contract"
    - "Post-commit: node scripts/validation/v1.5-milestone-audit.mjs exits 0 with 12/12 PASS, 0 informational EXCEPT C3 (Phase 39 self-cert)"
    - "Post-commit: regenerate-supervision-pins.mjs --self-test exits 0"
    - "Post-commit: ALL Phase 49-59 V-NN-NN structural assertions still PASS (regression-guard per D-26)"
  artifacts:
    - path: "scripts/validation/v1.5-milestone-audit.mjs"
      provides: "12-check harness in fully-blocking mode for v1.5"
      contains: "c11_ops_exemptions"
    - path: "scripts/validation/v1.5-audit-allowlist.json"
      provides: "Complete v1.5 sidecar with 3 new arrays"
      contains: "c13_broken_link_allowlist"
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "Self-test PASS via refreshed BASELINE_9"
      contains: "BASELINE_9 refreshed 2026-05-06"
    - path: ".planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md"
      provides: "75-finding inventory with Triage Decision column populated"
      contains: "FIXED-PHASE-60"
  key_links:
    - from: "v1.5-milestone-audit.mjs C9/C11/C13 run() bodies"
      to: "v1.5-audit-allowlist.json c9_exemptions[] / c11_ops_exemptions[] / c13_broken_link_allowlist[] arrays"
      via: "ALLOWLIST.cN_NAME || [] defensive default + new Set(allowlist.map(...)) allowKey lookup"
      pattern: "ALLOWLIST\\.c(9_exemptions|11_ops_exemptions|13_broken_link_allowlist)"
    - from: "regenerate-supervision-pins.mjs BASELINE_9 array"
      to: "v1.5-audit-allowlist.json supervision_exemptions[] current line coords"
      via: "Set-diff (sidecar - baseline) yields Phase 43 hand-authored NEW pins; classifier must reproduce identically"
      pattern: "BASELINE_9 = \\[\\s*\\['docs/_glossary-android.md', (76|78|17[0-9]|18[0-9])\\]"
    - from: "48-VERIFICATION-broken-links.md Triage Decision column"
      to: "Plan 09 check-phase-60.mjs V-60-08 assertion"
      via: "regex match on populated Triage Decision cells"
      pattern: "FIXED-PHASE-60|ALLOWLISTED-c13_broken_link_allowlist"
---

<objective>
Land THE atomic harness commit for Phase 60. Single commit bundles 4 promotions (C9 + C11 + C13 informational removal + C12 H2-anchor expansion) + 3 new sidecar arrays + BASELINE_9 refresh + 48-VERIFICATION-broken-links.md Triage Decision column population. Closes AUDIT-03, AUDIT-04, AUDIT-05, AUDIT-07 simultaneously.

Purpose: Per Phase 43 D-07 + Phase 48 D-14 atomicity-contract, all 4 file changes land in ONE commit. Splitting them creates partial-state failures: e.g., promoting C13 without c13_broken_link_allowlist[] populated FAILs on transient external URLs; promoting C9 without c9_exemptions[] populated FAILs on legitimate disambiguation prose; refreshing BASELINE_9 separately leaves harness in mixed state. Atomic landing is the only safe pattern.

Output: ONE git commit touching exactly 4 files; harness in 12/12 PASS fully-blocking mode post-commit.
Autonomous: false — Task 4 has a `checkpoint:human-verify` gate before the atomic commit lands, given the high blast radius (4 promotion changes + 4 sidecar adds + BASELINE_9 + close-out artifact).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-CONTEXT.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-RESEARCH.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-PATTERNS.md
@.planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md
@.planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md
@scripts/validation/v1.5-milestone-audit.mjs
@scripts/validation/v1.5-audit-allowlist.json
@scripts/validation/regenerate-supervision-pins.mjs
@scripts/validation/check-phase-58.mjs

<interfaces>
=== CRITICAL: All four file changes MUST land in ONE git commit. ===

Per Phase 43 D-07 atomicity-contract + Phase 48 D-14 + Phase 59 commit a01ab1d precedent.

**1. v1.5-milestone-audit.mjs changes:**

C9 promotion (lines 417-437):
- DELETE line 420: `informational: true,`
- REPLACE C9 run() body (lines 422-435) with C7-pattern allowKey-set + bare-count-with-pin-exempt loop. Verbatim template from harness:392-415; adaptation in 60-PATTERNS.md "C9 adaptation pattern (D-18 mechanism)".
- Source the cope_banned_phrases from sidecar; compile each as `new RegExp(p, 'gi')` (note `gi` flag — current `i`-only must change to `gi` for line iteration).
- Add `const allowlist = ALLOWLIST.c9_exemptions || []; const allowKey = new Set(allowlist.map(e => e.file + ':' + e.line));`
- Replace boolean `pat.test(c)` increment with iterating `while ((m = pat.exec(c)) !== null) { lineNum = ...; if (!allowKey.has(t + ':' + lineNum)) violations.push({...}); }`
- Replace `return { pass: true, detail: '(informational)' }` with two-arm `if (violations.length === 0) return { pass: true }; return { pass: false, detail: violations.length + ' un-exempted COPE banned-phrase hit(s): ' + violations.slice(0, 3).map(v => v.file + ':' + v.line).join(', ') };`

C11 promotion (lines 477-504):
- DELETE line 480: `informational: true,`
- REPLACE C11 run() body with C1-symmetric proximity-window negation. Verbatim template from harness:200-219; adaptation in 60-PATTERNS.md "C11 adaptation pattern (D-01 mechanism)".
- Compile patterns with `new RegExp(p, 'gi')` (g-flag added for line iteration).
- Add `const opsAllowlist = ALLOWLIST.c11_ops_exemptions || []; const opsAllowKey = new Set(opsAllowlist.map(e => e.file + ':' + e.line));`
- For each match in each docs/**/*.md file: get line number; check pin (continue if pinned); check ±200-char window for D-01 keyword regex (continue if matches); else push to violations.
- D-01 keyword regex (final form, refined per Plan 01 60-CALIBRATION.md Section B output):
  ```
  /successor|deprecated|historical|disambiguation|mutual-exclusion|PITFALL-9|first-occurrence|callout/i
  ```
  (ADD any keyword discovered as needed in 60-CALIBRATION.md; this set was the D-01 starting point and is confirmed by RESEARCH HIGH-confidence.)
- Two-arm response: `if (violations.length === 0) return { pass: true }; return { pass: false, detail: violations.length + ' un-exempted ops-domain anti-pattern hit(s): ' + violations.slice(0, 3).map(v => v.file + ':' + v.line + ' (' + v.pattern + ')').join(', ') };`

C12 expansion (lines 514-549):
- LINE 480 already has informational removed (Plan 58-06 commit bc9cee6).
- INSERT 6-H2 anchor sub-check between line 547 (`if (emptyCells.length > 0) return ...`) and line 548 (success return). Verbatim template from check-phase-58.mjs:117-126:
  ```javascript
  // 6 H2-anchor sub-check (D-13 + D-16 — Phase 60 expansion)
  const h2s = ["## Enrollment", "## Configuration", "## App Deployment", "## Compliance", "## Software Updates", "## Conditional Access"];
  const missing = h2s.filter(h => !new RegExp("^" + h.replace(/[/\\^$*+?.()|[\]{}]/g, "\\$&") + "\\s*$", "m").test(content));
  if (missing.length) return { pass: false, detail: 'Missing H2 anchors: ' + missing.join(', ') };
  ```
- UPDATE line 548 success-return detail string from `'5 platform columns + all data cells link-bearing'` to `'5 platform columns + all data cells link-bearing + 6 named H2 anchors present'`.

C13 promotion (lines 551-567):
- DELETE line 554: `informational: true,`
- REPLACE C13 run() body to apply c13_broken_link_allowlist[] count + category check. New shape:
  ```javascript
  run() {
    const allowlist = ALLOWLIST.c13_broken_link_allowlist || [];
    if (allowlist.length !== 15) {
      return { pass: false, detail: 'c13_broken_link_allowlist[] expected 15 entries (6 transient_external + 9 template_placeholder), got ' + allowlist.length };
    }
    const transient = allowlist.filter(e => e.category === 'transient_external').length;
    const template = allowlist.filter(e => e.category === 'template_placeholder').length;
    if (transient !== 6 || template !== 9) {
      return { pass: false, detail: 'c13 category counts: ' + transient + ' transient_external (expect 6), ' + template + ' template_placeholder (expect 9)' };
    }
    return { pass: true, detail: '15-entry broken-link allowlist (6 transient_external + 9 template_placeholder) honored; full mlc sweep deferred to CI' };
  }
  ```
  (Note: full markdown-link-check tool sweep is invoked at CI-level via .github/workflows/audit-harness-v1.5-integrity.yml + Phase 61 fresh-clone re-audit; harness check verifies the allowlist sidecar shape + count matches D-10 mandate.)

**2. v1.5-audit-allowlist.json changes:**

Update top-level fields:
- `"phase": "60-audit-harness-v1-5-finalization"`
- `"generated": "2026-05-06T00:00:00Z"`

Append 3 NEW arrays after existing `c7_knox_allowlist[]`:

  a. `c9_exemptions[]` — populated from Plan 01 60-CALIBRATION.md Section A "legitimate-disambiguation" rows. Shape per Phase 42 D-26:
  ```json
  "c9_exemptions": [
    {"file": "docs/_glossary-android.md", "line": 196, "reason": "Phase 60 graduation baseline -- COPE Version History sunset row (historical changelog; PITFALL-13 disambiguation)"}
  ]
  ```
  Expected count per RESEARCH HIGH-confidence claim: 4 entries minimum (`_glossary-android.md:196`, `admin-setup-android/03-fully-managed-cobo.md:149`, `android-lifecycle/03-android-version-matrix.md:37`, `reference/android-capability-matrix.md:50`). Refine per actual 60-CALIBRATION.md output.

  b. `c11_ops_exemptions[]` — RESERVED-EMPTY per D-02:
  ```json
  "c11_ops_exemptions": []
  ```

  c. `c13_broken_link_allowlist[]` — 15 entries per D-10. Shape: `{file, line, target, reason, category}`. Source from 48-VERIFICATION-broken-links.md Category B:
  ```json
  "c13_broken_link_allowlist": [
    {"file": "docs/admin-setup-android/02-zero-touch-portal.md", "line": 160, "target": "https://support.google.com/work/android/topic/9158960", "reason": "PITFALL-14 transient external; Google support URL non-MS domain; REQUIREMENTS Out-of-Scope spirit", "category": "transient_external"},
    {"file": "docs/admin-setup-android/07-knox-mobile-enrollment.md", "line": 51, "target": "https://knox.samsung.com", "reason": "PITFALL-14 transient external; Samsung Knox redirect chain; REQUIREMENTS Out-of-Scope spirit", "category": "transient_external"},
    {"file": "docs/admin-setup-android/09-aosp-realwear.md", "line": 125, "target": "https://portal.realwear.com", "reason": "PITFALL-14 transient external; RealWear portal non-MS domain; REQUIREMENTS Out-of-Scope spirit", "category": "transient_external"},
    {"file": "docs/l1-runbooks/04-network-connectivity.md", "line": 35, "target": "https://ztd.dds.microsoft.com", "reason": "PITFALL-14 transient external; ZTD endpoint reachability transient; REQUIREMENTS Out-of-Scope spirit (MS endpoint URL not docs.microsoft.com)", "category": "transient_external"},
    {"file": "docs/l1-runbooks/28-android-knox-enrollment-failed.md", "line": 69, "target": "https://knox.samsung.com", "reason": "PITFALL-14 transient external; Samsung Knox redirect chain; REQUIREMENTS Out-of-Scope spirit", "category": "transient_external"},
    {"file": "docs/reference/endpoints.md", "line": 125, "target": "https://support.apple.com/en-us/HT101555", "reason": "PITFALL-14 transient external; Apple support URL redirect chain; REQUIREMENTS Out-of-Scope spirit", "category": "transient_external"},
    {"file": "docs/_templates/admin-template.md", "line": 23, "target": "link", "reason": "intentional template stub by design (placeholder)", "category": "template_placeholder"},
    {"file": "docs/_templates/admin-template.md", "line": 43, "target": "../runbooks-l1/relevant-runbook.md", "reason": "intentional template stub by design (placeholder for runbook ref)", "category": "template_placeholder"},
    {"file": "docs/_templates/admin-template-android.md", "line": 51, "target": "../l1-runbooks/[filename].md", "reason": "intentional template stub by design (placeholder)", "category": "template_placeholder"},
    {"file": "docs/_templates/admin-template-android.md", "line": 111, "target": "../l1-runbooks/[runbook-filename].md", "reason": "intentional template stub by design (placeholder)", "category": "template_placeholder"},
    {"file": "docs/_templates/admin-template-android.md", "line": 113, "target": "../l1-runbooks/26-mgp-app-not-installed.md", "reason": "intentional template stub by design (placeholder for not-yet-authored runbook)", "category": "template_placeholder"},
    {"file": "docs/_templates/admin-template-ios.md", "line": 71, "target": "../l1-runbooks/[runbook-filename].md", "reason": "intentional template stub by design (placeholder)", "category": "template_placeholder"},
    {"file": "docs/_templates/admin-template-ios.md", "line": 57, "target": "link", "reason": "intentional template stub by design (placeholder)", "category": "template_placeholder"},
    {"file": "docs/_templates/admin-template-macos.md", "line": 49, "target": "../l1-runbooks/[runbook-filename].md", "reason": "intentional template stub by design (placeholder)", "category": "template_placeholder"},
    {"file": "docs/_templates/admin-template-macos.md", "line": 13, "target": "link", "reason": "intentional template stub by design (placeholder)", "category": "template_placeholder"}
  ]
  ```
  Total: 6 transient_external + 9 template_placeholder = 15 entries.

**3. regenerate-supervision-pins.mjs changes:**

BASELINE_9 array refresh (lines 386-403). Refresh approach: hand-edit each of the 9 entries to current sidecar pin coordinates so `--self-test` exits 0.

NEW BASELINE_9 array (per interfaces analysis above):
```javascript
const BASELINE_9 = [
  ['docs/_glossary-android.md', 79],   // ### Supervision heading (was 65/76; 79 at Phase 59; stable post-Plan-06 — H3 sits before line 127)
  ['docs/_glossary-android.md', 81],   // Supervision disambiguation blockquote (was 67/78; 81 at Phase 59; stable post-Plan-06)
  ['docs/_glossary-android.md', 181],  // MHS cross-platform note (was 134/172/179; 181 post-Plan-06 +2 from #kme/#kpe shims)
  ['docs/_glossary-android.md', 198],  // Version History row (was 148/188/196; 198 post-Plan-06 +2)
  ['docs/android-lifecycle/00-enrollment-overview.md', 51],
  ['docs/android-lifecycle/00-enrollment-overview.md', 53],
  ['docs/android-lifecycle/00-enrollment-overview.md', 83],
  ['docs/admin-setup-android/03-fully-managed-cobo.md', 36],
  ['docs/l2-runbooks/20-android-app-install-investigation.md', 21]
];
```

Update comment block above BASELINE_9 (lines 386-392). Preserve previous comments + add Phase 60 entry:
```javascript
// BASELINE_9 refreshed 2026-04-26 (Phase 48 Plan 01): pre-Phase-43 pin coordinates updated to current line positions after Phase 44-46 content additions.
// BASELINE_9 refreshed 2026-05-06 (Phase 60 Plan 08): _glossary-android.md entries refreshed to post-Plan-06 line coords (#kme/#kpe shims caused +2 shift after line 127); aligns with current sidecar supervision_exemptions[] AT v1.5 close. Closes AUDIT-07 carry-over per CONTEXT D-19.
```

**4. 48-VERIFICATION-broken-links.md changes:**

Populate the Triage Decision column for ALL 75 entries. Schema per CONTEXT D-11:
- 51 Category A anchor-fixes (Plans 02-06 closed) -> `FIXED-PHASE-60`
- 9 Category B path-fixes from Plan 07 -> `FIXED-PHASE-60`
- 6 Category B transient external URLs -> `ALLOWLISTED-c13_broken_link_allowlist`
- 9 Category B template placeholders -> `ALLOWLISTED-c13_broken_link_allowlist`

Triage tally: 51 (Cat A) + 9 (Cat B file-paths) = 60 FIXED-PHASE-60. 6+9 = 15 ALLOWLISTED. Sum 75.

Specific cell value per Category B row (mapping by target string):
- `../runbooks-l1/relevant-runbook.md` -> `ALLOWLISTED-c13_broken_link_allowlist`
- `link` (3 occurrences) -> `ALLOWLISTED-c13_broken_link_allowlist`
- `../l1-runbooks/[filename].md` -> `ALLOWLISTED-c13_broken_link_allowlist`
- `../l1-runbooks/[runbook-filename].md` (3 occurrences) -> `ALLOWLISTED-c13_broken_link_allowlist`
- `../l1-runbooks/26-mgp-app-not-installed.md` -> `ALLOWLISTED-c13_broken_link_allowlist`
- `https://support.google.com/work/android/topic/9158960` -> `ALLOWLISTED-c13_broken_link_allowlist`
- `https://knox.samsung.com` (2 occurrences) -> `ALLOWLISTED-c13_broken_link_allowlist`
- `https://portal.realwear.com` -> `ALLOWLISTED-c13_broken_link_allowlist`
- `https://ztd.dds.microsoft.com` -> `ALLOWLISTED-c13_broken_link_allowlist`
- `https://support.apple.com/en-us/HT101555` -> `ALLOWLISTED-c13_broken_link_allowlist`
- `../admin-setup/00-overview.md` (5 occurrences) -> `FIXED-PHASE-60`
- `../reference/conditional-access-enrollment.md` -> `FIXED-PHASE-60`
- `02-device-registration.md` -> `FIXED-PHASE-60`
- `05-policy-conflict.md` -> `FIXED-PHASE-60`
- `../l2-runbooks/01-network-connectivity.md` -> `FIXED-PHASE-60`

DO NOT change `**Total findings:** 75` in frontmatter line 7. DO NOT change Summary table at line 146 (`| **Total** | **75** | **0** | **75** |`). DO NOT change any structural prose. ONLY populate empty Triage Decision cells.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Edit v1.5-milestone-audit.mjs — promote C9 + C11 + C13 + expand C12 (4 in-place check-body rewrites)</name>
  <files>scripts/validation/v1.5-milestone-audit.mjs</files>
  <read_first>
    - scripts/validation/v1.5-milestone-audit.mjs lines 192-219 (C1 SafetyNet — verbatim template for C11 D-01 mechanism)
    - scripts/validation/v1.5-milestone-audit.mjs lines 392-415 (C7 Knox — verbatim template for C9 D-18 mechanism)
    - scripts/validation/v1.5-milestone-audit.mjs lines 417-437 (C9 current — informational; rewrite per D-17)
    - scripts/validation/v1.5-milestone-audit.mjs lines 477-504 (C11 current — informational; rewrite per D-01)
    - scripts/validation/v1.5-milestone-audit.mjs lines 514-549 (C12 current — already-promoted; expand per D-13/D-16)
    - scripts/validation/v1.5-milestone-audit.mjs lines 551-567 (C13 current — informational; rewrite per D-06)
    - scripts/validation/check-phase-58.mjs lines 117-126 (V-58-05 — verbatim H2-literal regex template for C12 expansion)
    - .planning/phases/60-audit-harness-v1-5-finalization/60-PATTERNS.md (full file — exact code-shape templates for all 4 rewrites)
    - .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md (Plan 01 output — c9_exemptions[] seed list + C11 final keyword regex)
  </read_first>
  <action>
    Apply 4 in-place edits to v1.5-milestone-audit.mjs per the interfaces block exactly. DO NOT commit yet (atomic commit happens at Task 4 with all 4 files).

    Edit 1 — C9 promotion (lines 417-437):
    - DELETE line 420 `informational: true,`
    - REPLACE lines 422-435 (run() body) with C7-pattern allowKey + bare-count-with-pin-exempt loop. Use the full code block from 60-PATTERNS.md "C9 adaptation pattern (D-18 mechanism)" verbatim. Critical points:
      * Compile patterns with `'gi'` flag (was `'i'`)
      * Add `const allowlist = ALLOWLIST.c9_exemptions || []; const allowKey = new Set(allowlist.map(e => e.file + ':' + e.line));`
      * Iterate via `while ((m = pat.exec(c)) !== null)` (replacing the boolean `pat.test(c)` increment)
      * Two-arm response: pass true if violations.length === 0; else fail with violation count + first 3.

    Edit 2 — C11 promotion (lines 477-504):
    - DELETE line 480 `informational: true,`
    - REPLACE lines 483-503 (run() body) with C1-symmetric proximity-window negation. Use the full code block from 60-PATTERNS.md "C11 adaptation pattern (D-01 mechanism)" verbatim. Critical points:
      * Compile patterns with `'gi'` flag (was `'i'`)
      * Iterate via `while ((m = re.exec(content)) !== null)` (replacing the boolean check)
      * Pre-window allowlist pin check via `opsAllowKey.has(...)` Set lookup
      * ±200-char window keyword regex check: `/successor|deprecated|historical|disambiguation|mutual-exclusion|PITFALL-9|first-occurrence|callout/i`
      * Two-arm response with detail showing first 3 violations + pattern that hit

    Edit 3 — C12 expansion (insertion BEFORE line 548):
    - INSERT 5-line block immediately BEFORE line 548 (the success return), AFTER the existing line 545-547 emptyCells check:
      ```javascript
      // 6 H2-anchor sub-check (D-13 + D-16 — Phase 60 expansion per AUDIT-04 SC#2)
      const h2s = ["## Enrollment", "## Configuration", "## App Deployment", "## Compliance", "## Software Updates", "## Conditional Access"];
      const missing = h2s.filter(h => !new RegExp("^" + h.replace(/[/\\^$*+?.()|[\]{}]/g, "\\$&") + "\\s*$", "m").test(content));
      if (missing.length) return { pass: false, detail: 'Missing H2 anchors: ' + missing.join(', ') };
      ```
    - UPDATE line 548 (existing success return) detail string from `'5 platform columns + all data cells link-bearing'` to `'5 platform columns + all data cells link-bearing + 6 named H2 anchors present'`.
    - DO NOT delete or modify any of the existing lines 514-547 (platform columns check + link-not-copy check are PRESERVED).

    Edit 4 — C13 promotion (lines 551-567):
    - DELETE line 554 `informational: true,`
    - REPLACE lines 559-566 (run() body) with allowlist-shape verification + count assertion. Use the full code block from interfaces (lines starting `const allowlist = ALLOWLIST.c13_broken_link_allowlist || [];`).

    DO NOT modify lines 71-79 (parseAllowlist), C1 (192-219), C7 (392-415), C10 (445-475), runner (575-606), or any other check.

    Verify file compiles without runtime crash:
    ```
    node --check scripts/validation/v1.5-milestone-audit.mjs && echo "PARSE OK"
    ```
  </action>
  <verify>
    <automated>node --check scripts/validation/v1.5-milestone-audit.mjs && echo "PASS parse" || echo "FAIL parse" && grep -c '^\s*informational: true,' scripts/validation/v1.5-milestone-audit.mjs | awk '{ if ($1 <= 1) print "PASS informational-flags="$1; else print "FAIL "$1"-informational-flags-still-present" }' && grep -c 'c11_ops_exemptions\|c9_exemptions\|c13_broken_link_allowlist' scripts/validation/v1.5-milestone-audit.mjs | awk '{ if ($1 >= 3) print "PASS sidecar-arrays-referenced="$1; else print "FAIL only-"$1"-arrays-wired" }' && grep -c '## Enrollment\|## Configuration\|## App Deployment\|## Compliance\|## Software Updates\|## Conditional Access' scripts/validation/v1.5-milestone-audit.mjs | awk '{ if ($1 >= 6) print "PASS C12-h2-anchors="$1; else print "FAIL C12-only-"$1"-h2s" }'</automated>
  </verify>
  <acceptance_criteria>
    - File parses successfully (`node --check` exits 0)
    - File contains AT MOST 1 `informational: true,` line (only C3 Phase 39 self-cert may retain it; C9/C11/C13 must have it removed; C12 already had it removed)
    - Sidecar array references present: `c9_exemptions`, `c11_ops_exemptions`, `c13_broken_link_allowlist` each appear at least once
    - C12 contains all 6 H2 literals: `## Enrollment`, `## Configuration`, `## App Deployment`, `## Compliance`, `## Software Updates`, `## Conditional Access`
    - File line count delta within reasonable bound (~+20 to +50 lines added; net additions over deletions for the 4 rewrites). Verifiable: `wc -l file` increases by 20-50 lines vs pre-edit baseline (pre = 606, expected post = ~625-650).
    - NO commit yet (this task only edits; commit lands at Task 4)
  </acceptance_criteria>
  <done>4 in-place edits applied; file parses; informational flags removed for C9/C11/C13; C12 6-H2 expansion present; staged but NOT committed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Edit v1.5-audit-allowlist.json — append 3 new arrays + update phase + generated</name>
  <files>scripts/validation/v1.5-audit-allowlist.json</files>
  <read_first>
    - scripts/validation/v1.5-audit-allowlist.json (full file; current 55 lines)
    - .planning/phases/60-audit-harness-v1-5-finalization/60-CALIBRATION.md (Plan 01 output — c9_exemptions[] seed entries + reasons)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md lines 96-119 (Category B inventory)
  </read_first>
  <action>
    Apply 3 edits to v1.5-audit-allowlist.json. DO NOT commit yet.

    Edit 1: Top-level field updates:
    - Replace `"phase": "48-audit-harness-bootstrap-broken-link-sweep-first-pass"` with `"phase": "60-audit-harness-v1-5-finalization"`
    - Replace `"generated": "2026-04-26T00:00:00Z"` with `"generated": "2026-05-06T00:00:00Z"`

    Edit 2: After existing `c7_knox_allowlist[]` array (line 53 closing `]`), append a comma + 3 new arrays. Final structure (post-edit):
    ```json
      ],
      "c9_exemptions": [
        ... <entries from 60-CALIBRATION.md Section A "legitimate-disambiguation" rows; minimum 4 expected per RESEARCH HIGH-confidence>
      ],
      "c11_ops_exemptions": [],
      "c13_broken_link_allowlist": [
        ... <15 entries: 6 transient_external + 9 template_placeholder per interfaces block>
      ]
    }
    ```

    For c9_exemptions[] entries: source from 60-CALIBRATION.md Section A. Each entry shape `{file, line, reason}`. Reason text follows lineage convention (cite Phase + structural justification + PITFALL-13 disambiguation reference).

    For c13_broken_link_allowlist[] entries: 15 verbatim entries from interfaces block. Each entry shape `{file, line, target, reason, category}`.

    Verify JSON parses:
    ```
    node -e "const j = JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8')); console.log('PASS keys:', Object.keys(j).sort().join(','))"
    ```

    Expected output keys (sorted): `c11_ops_exemptions,c13_broken_link_allowlist,c7_knox_allowlist,c9_exemptions,cope_banned_phrases,generated,phase,safetynet_exemptions,schema_version,supervision_exemptions`

    DO NOT modify safetynet_exemptions[], supervision_exemptions[] (Plan 06 already refreshed those), cope_banned_phrases[], or c7_knox_allowlist[] (lines 5-53). Only top-level field updates + array appends.
  </action>
  <verify>
    <automated>node -e "const j = JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8')); console.log(j.phase === '60-audit-harness-v1-5-finalization' ? 'PASS phase' : 'FAIL phase='+j.phase); console.log(Array.isArray(j.c9_exemptions) ? 'PASS c9='+j.c9_exemptions.length : 'FAIL c9-not-array'); console.log(Array.isArray(j.c11_ops_exemptions) && j.c11_ops_exemptions.length === 0 ? 'PASS c11-reserved-empty' : 'FAIL c11='+j.c11_ops_exemptions); console.log(Array.isArray(j.c13_broken_link_allowlist) && j.c13_broken_link_allowlist.length === 15 ? 'PASS c13-15-entries' : 'FAIL c13='+j.c13_broken_link_allowlist.length); const t = j.c13_broken_link_allowlist.filter(e => e.category === 'transient_external').length; const p = j.c13_broken_link_allowlist.filter(e => e.category === 'template_placeholder').length; console.log(t === 6 && p === 9 ? 'PASS c13-categories t='+t+' p='+p : 'FAIL c13-categories t='+t+' p='+p)"</automated>
  </verify>
  <acceptance_criteria>
    - JSON parses successfully
    - `phase` field = `"60-audit-harness-v1-5-finalization"`
    - `generated` field = `"2026-05-06T00:00:00Z"`
    - `c9_exemptions` is an array (length >= 0; may be 0 entries if Plan 01 calibration found zero legitimate hits)
    - `c11_ops_exemptions` is an array of length EXACTLY 0 (reserved-empty per D-02)
    - `c13_broken_link_allowlist` is an array of length EXACTLY 15
    - Within c13_broken_link_allowlist: 6 entries have `category: "transient_external"` and 9 have `category: "template_placeholder"`
    - Existing arrays (safetynet_exemptions, supervision_exemptions, cope_banned_phrases, c7_knox_allowlist) untouched in count and content
    - NO commit yet
  </acceptance_criteria>
  <done>Sidecar JSON updated with 3 new arrays + phase/generated fields; JSON valid; all existing arrays untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Edit regenerate-supervision-pins.mjs BASELINE_9 + populate 48-VERIFICATION-broken-links.md Triage Decision column</name>
  <files>
    scripts/validation/regenerate-supervision-pins.mjs,
    .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md
  </files>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs lines 380-403 (BASELINE_9 array + comment block)
    - scripts/validation/v1.5-audit-allowlist.json supervision_exemptions[] (post-Task-2 state — Plan 06 refreshed line coords)
    - .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md (full file; 75 entries needing Triage Decision column population)
  </read_first>
  <action>
    Edit 1: regenerate-supervision-pins.mjs BASELINE_9 refresh.

    Update BASELINE_9 array contents (lines 393-403) to the new array shown in interfaces block:
    ```javascript
    const BASELINE_9 = [
      ['docs/_glossary-android.md', 79],   // ### Supervision heading
      ['docs/_glossary-android.md', 81],   // Supervision disambiguation blockquote
      ['docs/_glossary-android.md', 181],  // MHS cross-platform note (post-Plan-06 +2)
      ['docs/_glossary-android.md', 198],  // Version History row (post-Plan-06 +2)
      ['docs/android-lifecycle/00-enrollment-overview.md', 51],
      ['docs/android-lifecycle/00-enrollment-overview.md', 53],
      ['docs/android-lifecycle/00-enrollment-overview.md', 83],
      ['docs/admin-setup-android/03-fully-managed-cobo.md', 36],
      ['docs/l2-runbooks/20-android-app-install-investigation.md', 21]
    ];
    ```

    Update comment block above BASELINE_9 (lines 386-392). Preserve previous comments + add Phase 60 entry per interfaces block.

    Verify file parses: `node --check scripts/validation/regenerate-supervision-pins.mjs && echo "PARSE OK"`.

    Edit 2: 48-VERIFICATION-broken-links.md Triage Decision column population.

    For each row in Category A table (rows 34-84 of the file, 51 entries), set the empty Triage Decision cell `| |` to `| FIXED-PHASE-60 |`.

    For each row in Category B table (rows 96-119, 24 entries), set the Triage Decision cell per the target-string mapping in interfaces block:
    - 5 rows with target `../admin-setup/00-overview.md` -> `FIXED-PHASE-60`
    - 1 row `../reference/conditional-access-enrollment.md` -> `FIXED-PHASE-60`
    - 1 row `02-device-registration.md` -> `FIXED-PHASE-60`
    - 1 row `05-policy-conflict.md` -> `FIXED-PHASE-60`
    - 1 row `../l2-runbooks/01-network-connectivity.md` -> `FIXED-PHASE-60`
    - 6 external URL rows (Google, Knox×2, RealWear, ztd.dds, Apple) -> `ALLOWLISTED-c13_broken_link_allowlist`
    - 9 template-placeholder rows (link×3, [filename].md, [runbook-filename].md×3, 26-mgp-app-not-installed.md, ../runbooks-l1/relevant-runbook.md) -> `ALLOWLISTED-c13_broken_link_allowlist`

    Triage tally: 51 + 9 = 60 FIXED-PHASE-60. 6+9 = 15 ALLOWLISTED. Sum 75.

    DO NOT change frontmatter `total_findings: 75` (line 7). DO NOT change Summary table at line 146. DO NOT change any structural prose. ONLY populate empty Triage Decision cells.

    Verify population is complete:
    ```bash
    grep -c '| FIXED-PHASE-60 \|| ALLOWLISTED-c13_broken_link_allowlist ' .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md
    ```
    Expected: 75
  </action>
  <verify>
    <automated>node --check scripts/validation/regenerate-supervision-pins.mjs && echo "PASS pin-helper-parse" || echo "FAIL pin-helper-parse" && grep -c "BASELINE_9 refreshed 2026-05-06" scripts/validation/regenerate-supervision-pins.mjs | awk '{ if ($1 == 1) print "PASS baseline-9-refresh-comment"; else print "FAIL baseline-9-comment="$1 }' && grep -c "'docs/_glossary-android.md', 79\\]" scripts/validation/regenerate-supervision-pins.mjs | awk '{ if ($1 == 1) print "PASS baseline-9-entry-79"; else print "FAIL no-79-entry" }' && grep -c "'docs/_glossary-android.md', 198\\]" scripts/validation/regenerate-supervision-pins.mjs | awk '{ if ($1 == 1) print "PASS baseline-9-entry-198"; else print "FAIL no-198-entry" }' && grep -c '| FIXED-PHASE-60 \\|| ALLOWLISTED-c13_broken_link_allowlist ' .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md | awk '{ if ($1 == 75) print "PASS triage-75-populated"; else print "FAIL triage-only-"$1"-populated" }' && grep -c 'total_findings: 75' .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md | awk '{ if ($1 >= 1) print "PASS total-frontmatter-preserved"; else print "FAIL total-baseline-edited" }'</automated>
  </verify>
  <acceptance_criteria>
    - regenerate-supervision-pins.mjs parses (`node --check` exits 0)
    - BASELINE_9 array contains 4 _glossary-android.md entries with line numbers 79, 81, 181, 198 (each via per-line grep)
    - Comment block contains `BASELINE_9 refreshed 2026-05-06 (Phase 60 Plan 08)` (1 occurrence)
    - 48-VERIFICATION-broken-links.md has exactly 75 populated Triage Decision cells (60 FIXED-PHASE-60 + 15 ALLOWLISTED-c13_broken_link_allowlist)
    - 48-VERIFICATION-broken-links.md frontmatter `total_findings: 75` byte-identical pre/post
    - 48-VERIFICATION-broken-links.md Summary table `| **Total** | **75** | **0** | **75** |` byte-identical pre/post
    - NO commit yet (atomic commit at Task 4)
  </acceptance_criteria>
  <done>BASELINE_9 refreshed; Triage Decision column 75/75 populated; baseline byte-identity preserved; staged but not committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Pre-commit gate — atomic harness commit verification + landing</name>
  <what-built>
    Tasks 1-3 staged 4 file edits in working tree:
    1. v1.5-milestone-audit.mjs — C9/C11/C13 promotion + C12 H2 expansion
    2. v1.5-audit-allowlist.json — 3 new arrays + phase/generated update
    3. regenerate-supervision-pins.mjs — BASELINE_9 refresh
    4. 48-VERIFICATION-broken-links.md — Triage Decision column populated

    No commit yet — atomic commit lands here per Phase 43 D-07 atomicity-contract.
  </what-built>
  <how-to-verify>
    Run the FULL pre-commit gate suite. EVERY command MUST exit 0 before staging:

    1. Sidecar JSON parse:
       ```
       node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8'))"
       ```
       Expect: silent success.

    2. Harness exit-0 in fully-blocking mode:
       ```
       node scripts/validation/v1.5-milestone-audit.mjs --verbose
       ```
       Expect: 12/12 PASS, 0 informational EXCEPT C3 (Phase 39 self-cert). Output line `Summary: 12 passed, 0 failed, 0 skipped` (or similar — count C4 retired status).

    3. AUDIT-07 close — self-test exit-0:
       ```
       node scripts/validation/regenerate-supervision-pins.mjs --self-test
       ```
       Expect: `Self-test: PASS` and exit 0.

    4. Phase 49-59 chain validators all GREEN (regression-guard per D-26):
       ```
       for n in 48 49 51 52 53 54 55 56 57 58 59; do
         echo "--- check-phase-$n ---"
         node scripts/validation/check-phase-$n.mjs || echo "FAIL phase-$n"
       done
       ```
       Expect: NO `FAIL phase-N` lines printed.

    5. Sidecar shape sanity:
       ```
       node -e "const j = require('fs').readFileSync('scripts/validation/v1.5-audit-allowlist.json','utf8'); const o = JSON.parse(j); console.log('phase=', o.phase, 'c9=', o.c9_exemptions.length, 'c11=', o.c11_ops_exemptions.length, 'c13=', o.c13_broken_link_allowlist.length)"
       ```
       Expect: `phase= 60-audit-harness-v1-5-finalization c9= <N> c11= 0 c13= 15`

    6. Triage population sanity:
       ```
       grep -c '| FIXED-PHASE-60 \|| ALLOWLISTED-c13_broken_link_allowlist' .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md
       ```
       Expect: 75

    If ALL 6 verification commands pass, stage all 4 files and create the atomic commit:
    ```
    git add scripts/validation/v1.5-milestone-audit.mjs scripts/validation/v1.5-audit-allowlist.json scripts/validation/regenerate-supervision-pins.mjs .planning/phases/48-audit-harness-bootstrap-broken-link-sweep-first-pass/48-VERIFICATION-broken-links.md

    git commit -m "$(cat <<'EOF'
    feat(60-08): atomic harness commit -- promote C9+C11+C13 to blocking, expand C12 to 6 H2 anchors, seed 3 sidecar arrays, refresh BASELINE_9 (closes AUDIT-03/04/05/07)

    - C9 (cope_banned_phrases): informational -> blocking; new c9_exemptions[] sidecar array (per Plan 01 60-CALIBRATION.md); allowKey-set + line-locality loop (mirrors C7 Knox pattern). Honors Phase 48 D-06 promotion-target.
    - C11 (ops-domain anti-patterns): informational -> blocking; C1-symmetric proximity-window negation against /successor|deprecated|historical|disambiguation|mutual-exclusion|PITFALL-9|first-occurrence|callout/i ±200-char window; c11_ops_exemptions[] reserved-empty per D-02. Closes AUDIT-03.
    - C12 (4-platform comparison): scope expanded to verify 6 named H2 anchors (Enrollment / Configuration / App Deployment / Compliance / Software Updates / Conditional Access) per D-13 + D-16. Closes AUDIT-04.
    - C13 (broken-link automation): informational -> blocking; new c13_broken_link_allowlist[] sidecar array with 15 entries (6 transient_external + 9 template_placeholder per D-10); 48-VERIFICATION-broken-links.md Triage Decision column populated for all 75 entries. Closes AUDIT-05.
    - regenerate-supervision-pins.mjs: BASELINE_9 array refreshed to current sidecar coordinates (post-Phase-59 + post-Plan-06 line shifts); --self-test exits 0. Closes AUDIT-07 carry-over.
    - Sidecar phase field updated: 48-audit-harness-bootstrap-broken-link-sweep-first-pass -> 60-audit-harness-v1-5-finalization

    Atomic per Phase 43 D-07 + Phase 48 D-14 contract: harness changes + sidecar additions + BASELINE_9 refresh + close-out artifact land in ONE commit. Splitting causes partial-state failures.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    After commit, run post-commit verification (same 6 checks above). All must STILL pass.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 pre-commit checks PASS and atomic commit landed cleanly, OR describe any FAIL output</resume-signal>

  <files>(checkpoint-task — no files modified by this task; previous tasks staged the files)</files>
  <action>Pause for human verification per <how-to-verify> instructions. No automated action; human runs the commands listed and inspects output. If all PASS, type the resume-signal to continue.</action>
  <verify>Manual: human confirms each step in <how-to-verify> exits 0 / produces expected output before approving.</verify>
  <done>Human types "approved" (or describes failure for retry).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness JS code edit -> CI runtime | Changed regex `g`-flag introduces line-iteration; runtime cost grows from O(file-count) to O(file-count * occurrence-count); must remain bounded. |
| sidecar JSON shape edit -> harness load | parseAllowlist() degrades-to-empty on parse failure; partial JSON writes break harness silently. |
| BASELINE_9 array edit -> --self-test pass-criteria | Hand-edit must align with current sidecar coords; mismatch causes false FAIL or false PASS. |
| 48-VERIFICATION-broken-links.md Triage edit -> Phase 48 audit-trail | Frontmatter total_findings + Summary table MUST be byte-identical (Phase 48 D-11); only Triage Decision column cells populated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-08-01 | T (Tampering) | sidecar JSON shape | mitigate | Pre-commit JSON parse gate (Task 4 step 1); JSON validation runs before any staged file is committed. |
| T-60-08-02 | T (Tampering) | harness JS regex flag | mitigate | Pre-commit `node --check` parse gate (Task 1); harness exit-0 gate (Task 4 step 2) catches runtime errors. |
| T-60-08-03 | D (DoS via regex) | C11 4-pattern proximity-window scan with `g`-flag iteration | mitigate | Pattern set is fixed at 4 entries (D-03); proximity window is fixed at ±200 chars. Worst-case: 4 patterns × ~200 docs × ~5 occurrences/doc = 4000 iterations; bounded. |
| T-60-08-04 | D (DoS via regex) | C9 8-pattern cope_banned_phrases scan | mitigate | Greedy `[^.]*` regex bounded by `.` terminator. Per RESEARCH HIGH-confidence, hits are ~4 per Android scope; bounded. Future v1.6+ may tighten to `[^.\n]*` if false-positive rate excessive. |
| T-60-08-05 | T (Tampering) | BASELINE_9 array | mitigate | Pre-commit --self-test gate (Task 4 step 3); FAIL aborts commit; refresh values cross-checked against current sidecar supervision_exemptions[] line coords. |
| T-60-08-06 | I (Information Disclosure) | 48-VERIFICATION close-out audit-trail | accept | Triage Decision column population is intentional disclosure; baseline marker preserved per CONTEXT D-11. |
| T-60-08-07 | E (Elevation of Privilege) | none in scope | accept | All edits are file-content / config; no privilege boundaries crossed. |
</threat_model>

<verification>
- ONE git commit landing with `feat(60-08): atomic harness commit` prefix
- Commit touches exactly 4 files (verified: `git show HEAD --stat | grep -c '^ scripts/\| .planning/' == 4`)
- Post-commit `node scripts/validation/v1.5-milestone-audit.mjs --verbose` exits 0 with 12/12 PASS in fully-blocking mode
- Post-commit `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0
- Post-commit Phase 49-59 chain validators all PASS
- Post-commit sidecar phase field = `60-audit-harness-v1-5-finalization`
- Post-commit 48-VERIFICATION-broken-links.md has 75 Triage Decision cells populated; frontmatter + Summary preserved byte-identical
</verification>

<success_criteria>
- AUDIT-03 closed (C11 promoted blocking; sidecar reserved-empty c11_ops_exemptions[])
- AUDIT-04 closed (C12 expanded to 6 H2 anchors)
- AUDIT-05 closed (C13 promoted blocking; 15-entry allowlist; 48-VERIFICATION 75-finding inventory closed via Triage Decision column)
- AUDIT-07 closed (regenerate-supervision-pins.mjs --self-test exits 0; BASELINE_9 refreshed)
- Phase 48 D-06 C9 promotion-target honored (C9 promoted blocking)
- Atomic-commit contract honored per Phase 43 D-07 / Phase 48 D-14 / Phase 59 a01ab1d precedent
- Phase 49-59 V-NN-NN structural assertions all preserved (regression-guard GREEN)
- Plan 09 (check-phase-60.mjs validator) unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/60-audit-harness-v1-5-finalization/60-08-SUMMARY.md`
</output>
