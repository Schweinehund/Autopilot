---
phase: 124-pipeline-fix-descriptive-filename-pass-draft-label-grounding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pipeline/convert.ps1
  - scripts/pipeline/lib/ooxml.mjs
  - scripts/pipeline/guard-docx.mjs
  - scripts/pipeline/README.md
autonomous: true
requirements: [PIPE-03]

must_haves:
  truths:
    - "The 12 previously-failing admin-setup nav-footer files convert exit 0 through convert.ps1 (per D-04 positive)"
    - "A converted nav file's docProps/custom.xml still carries the 9 promoted custom properties (per D-04 OQ4 non-regression)"
    - "word/document.xml is byte-identical pre-fix vs post-fix for the 14 previously-passing nav files (per D-04 byte-equivalence)"
    - "The source .md file on disk is never mutated — preprocessing operates on an ephemeral temp copy only (per D-01)"
  artifacts:
    - path: "scripts/pipeline/convert.ps1"
      provides: "Pre-pandoc ephemeral-temp-copy preprocessing block (D-01) + D-03(a) fence-tracking + D-03(b) fail-closed diff guard; line-74 invocation stays flag-identical ($InputMd -> $tempMd only)"
      contains: "Previous|Next step"
    - path: "scripts/pipeline/lib/ooxml.mjs"
      provides: "extractCustomProperties(docxPath) export following the findHeadingStyleIds() convention"
      exports: ["extractCustomProperties"]
    - path: "scripts/pipeline/guard-docx.mjs"
      provides: "CUSTOM-PROPS check registered in the checks array (D-04 OQ4 guard)"
      contains: "CUSTOM-PROPS"
    - path: "scripts/pipeline/README.md"
      provides: "SC1 prose documenting the pre-pandoc source-normalization step (not a pandoc flag)"
  key_links:
    - from: "scripts/pipeline/convert.ps1 preprocessing block"
      to: "pandoc line-74 invocation"
      via: "ephemeral $tempMd fed to & $pandocBin in place of $InputMd"
      pattern: "tempMd"
    - from: "scripts/pipeline/guard-docx.mjs CUSTOM-PROPS check"
      to: "docProps/custom.xml"
      via: "extractCustomProperties() from lib/ooxml.mjs"
      pattern: "extractCustomProperties"
---

<objective>
Fix the pandoc nav-footer YAML-alias conversion defect (`DEFER-119-C`, exit-64 `Unknown alias 'Previous'`/`'Next'`) at the pipeline surface without regressing the OQ4 frontmatter → Word custom-property promotion path. This is the gating plan of Phase 124 (Wave 1) and delivers SC1 + SC2.

Purpose: The 12 admin-setup guides whose nav-footer is the italic `*Previous:`/`*Next step:` `---`…`---` bracket currently fail conversion, blocking their upload to the grounded library. The fix is surgical pipeline-side preprocessing on an ephemeral temp copy (D-01) — the frozen source `.md` is never mutated, and pandoc's line-74 invocation stays flag-identical.

Output: Extended `convert.ps1` (preprocessing block + fail-closed guard), a new `extractCustomProperties()` OOXML helper, a new permanent `CUSTOM-PROPS` guard check, and updated `README.md` SC1 prose — all proven by the D-04 three-part regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/124-CONTEXT.md
@.planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/124-RESEARCH.md
@.planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/124-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the D-01/D-03 pre-pandoc preprocessing block into convert.ps1</name>
  <read_first>
    - scripts/pipeline/convert.ps1 (the file being modified — the param block lines 13-18 and the "Canonical conversion (SC1)" block lines 69-79)
    - 124-RESEARCH.md Pattern 1 (lines 255-330 — the copy-paste-ready PowerShell block, empirically verified this session)
    - 124-PATTERNS.md §convert.ps1 (insertion anchor + the Write-Error/exit 1 idiom to mirror from lines 41-49 of convert.ps1)
    - 124-CONTEXT.md decisions D-01, D-02 (rejected alternatives — do NOT re-open), D-03(a)/(b)/(c)
  </read_first>
  <action>
    Insert a PIPE-03 preprocessing block into `scripts/pipeline/convert.ps1` immediately BEFORE the `# ─── Canonical conversion (SC1) ───` comment block (before current line 69). Do NOT change the param signature (`$InputMd`, `$OutputDocx`, `$ReferenceDoc` stay exactly as-is — `-OutputDocx` is the locked PIPE-04 hook per D-10). The block must: (1) copy `$InputMd` to an ephemeral temp file (`.md` extension) — the source `.md` is NEVER written; (2) read the temp file lines and track fenced-code state (triple-backtick or tilde `~~~` fences), never rewriting any `---` inside a fence (D-03a); (3) for each standalone `^---\s*$` line that is blank-preceded (previous line trims to empty) and NOT inside a fence, peek forward past blank lines and, iff the next non-blank line matches the anchor regex `^\s*\*(Previous|Next step)\b`, rewrite that `---` line to `* * *` (opening delimiter only — the trailing `---` degrades to a harmless thematic break); (4) implement the D-03(b) fail-closed guard: diff the source lines against the temp lines and abort with `Write-Error` + `exit 1` (mirror the version-guard idiom at convert.ps1:41-49, NOT `throw`) if ANY diff exists other than an expected `---`→`* * *` rewrite on an anchor-matched line. Change ONLY the invocation line (line 74) to feed `$tempMd` to `& $pandocBin` in place of `$InputMd`; every flag stays identical per D-01. Clean up `$tempMd` after conversion. Do NOT broaden the anchor to catch the 5 `docs/lifecycle/*.md` `## Navigation` lines (they already convert exit 0 — RESEARCH anti-pattern; D-02 rejects blanket over-matching).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && pwsh -NoProfile -File scripts/pipeline/convert.ps1 -InputMd docs/admin-setup-ios/03-ade-enrollment-profile.md -OutputDocx .pipeline-output/probe-ios03.docx; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `convert.ps1` exit 0 on `docs/admin-setup-ios/03-ade-enrollment-profile.md` (was exit 64 before the fix)
    - convert.ps1 stdout shows the `PIPE-03 preprocessing: N nav-footer rewrite(s), guard PASS` line
    - `git diff --stat docs/admin-setup-ios/03-ade-enrollment-profile.md` shows ZERO changes to the source .md after a conversion run (source never mutated)
    - The invocation block's flag set is unchanged — only `$InputMd` → `$tempMd` on the `& $pandocBin` line (verify by reading the block: `--reference-doc=$ReferenceDoc` present, no new flags)
    - The anchor regex is literally `^\s*\*(Previous|Next step)\b` (no broadening)
  </acceptance_criteria>
  <done>The 12-file failing class converts exit 0 through convert.ps1; source .md untouched; D-03(b) guard is fail-closed; anchor is the exact D-01 pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Add extractCustomProperties() to ooxml.mjs and a permanent CUSTOM-PROPS check to guard-docx.mjs</name>
  <read_first>
    - scripts/pipeline/lib/ooxml.mjs (the file being modified — extractEntry() lines 38-81, findHeadingStyleIds() lines 115-122, imports lines 18-19)
    - scripts/pipeline/guard-docx.mjs (the file being modified — imports line 25, runHeadingStyleCheck() lines 66-79, checks.push blocks lines 131-141, self-test stAssert lines 159-282, runner exit line ~314)
    - 124-RESEARCH.md Pattern 2 (lines 332-366 — exact extractCustomProperties body + the confirmed 9-key set)
    - 124-PATTERNS.md §ooxml.mjs and §guard-docx.mjs (verbatim function bodies + checks-array registration convention)
    - 124-CONTEXT.md D-04 (OQ4 regression: 9-key non-regression + no-body-leak) and RESEARCH Open Question 1 (permanent-check recommendation)
  </read_first>
  <action>
    Add `extractCustomProperties(docxPath)` to `scripts/pipeline/lib/ooxml.mjs` following the `findHeadingStyleIds()` convention exactly (JSDoc block, `extractEntry(docxPath, 'docProps/custom.xml')` call, `/<property[^>]*\bname="([^"]+)"/g` regex scan, string-array return). No new import — it reuses the in-file `extractEntry`. Then in `scripts/pipeline/guard-docx.mjs`: add `extractCustomProperties` to the internal-lib import line (line 25), write a `runCustomPropsCheck(path)` mirroring `runHeadingStyleCheck()`'s try/catch + `{ pass, detail }` shape, and register it via a new `checks.push({ id: 'CUSTOM-PROPS', name: 'V-GUARD-CUSTOM-PROPS: ...', run() { return runCustomPropsCheck(docxPath); } })` appended after the existing HEADING-STYLE push (the generic runner loop and self-test need zero further changes). Make the PERMANENT check LENIENT so it passes on any legitimately-promoted doc (D-04's "9-keys-present" full assertion is proven as a one-time regression in Task 3, NOT baked as a per-file gate that would false-fail the 4-key or reduced-key fixtures): the permanent check PASSES iff (a) `docProps/custom.xml` is present and every property name it returns is within the known set `{applies_to, audience, doc_id, doc_type, last_verified, owner, platform, review_by, status}` (i.e. no rogue/unexpected custom property — a promotion-regression signal), and FAILS on any name outside that set. The no-body-leak half is already covered by the existing YAML-LEAK check — do not duplicate it. This is the permanent-guard choice per RESEARCH OQ1 (A2); it extends the standalone guard surface only and touches NO chain-registered validator (Phase-125 firewall intact).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && node scripts/pipeline/guard-docx.mjs --self-test; echo "selftest=$?" && node scripts/pipeline/guard-docx.mjs .pipeline-output/probe-ios03.docx; echo "guard=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/pipeline/guard-docx.mjs --self-test` exits 0
    - `node -e "import('./scripts/pipeline/lib/ooxml.mjs').then(m=>console.log(m.extractCustomProperties('.pipeline-output/probe-ios03.docx').sort().join(',')))"` prints the 9 keys `applies_to,audience,doc_id,doc_type,last_verified,owner,platform,review_by,status`
    - `guard-docx.mjs` on the Task-1 probe .docx exits 0 and its output lists a `CUSTOM-PROPS` PASS line
    - The CUSTOM-PROPS check is registered via `checks.push` (append, not restructure); no chain-registered validator file is touched
  </acceptance_criteria>
  <done>extractCustomProperties() exported; permanent lenient CUSTOM-PROPS guard check registered; self-test green; guard green on a converted nav file.</done>
</task>

<task type="auto">
  <name>Task 3: Run the D-04 three-part regression + document the SC1 preprocessing step in README.md</name>
  <read_first>
    - scripts/pipeline/README.md (the file being modified — SC1 "Canonical Invocation / No other flags" section lines 9-37)
    - 124-RESEARCH.md Code Examples §"The verified 12-file failing set" (lines 502-517 — the exact 12 paths) and Pitfall 4 (byte-equivalence must be a runnable check, not a paper claim)
    - 124-CONTEXT.md D-04 (all three regression legs) and the "verified defect surface" note (12 fail, 14 pass, README SC1 must document the pre-pandoc step)
    - scripts/pipeline/lib/ooxml.mjs extractEntry() (used to byte-diff word/document.xml)
  </read_first>
  <action>
    Update `scripts/pipeline/README.md`'s SC1 section: keep the canonical invocation as the single source of truth but add prose DOCUMENTING the pre-pandoc source-normalization step — state that before the pandoc call, `convert.ps1` copies the input to an ephemeral temp file and rewrites a blank-preceded `---` opening a nav-footer bracket (anchored on `*Previous:`/`*Next step:`) to `* * *` on that copy only, that the source `.md` is never mutated, and that the pandoc invocation itself (line 74) stays flag-identical (this is NOT a pandoc flag). Then execute and record the full D-04 three-part regression in the plan SUMMARY: (POSITIVE) convert all 12 files in RESEARCH's verified failing set through `convert.ps1` and confirm each exits 0; (OQ4 NON-REGRESSION) run `guard-docx.mjs` on a converted nav file and confirm `docProps/custom.xml` still carries all 9 promoted properties AND no frontmatter-key leaks into `word/document.xml` body; (BYTE-EQUIVALENCE) for each of the 14 previously-passing nav files, convert once with the fix and confirm `word/document.xml` is byte-identical to a baseline conversion (use `extractEntry()` for both; per Pitfall 4 this MUST be a per-file diff across all 14, not a single spot-check). Record the exact 12-file exit-0 list, the 9-key confirmation, and the 14-file byte-diff result in the SUMMARY.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && for f in docs/admin-setup-8021x/00-overview.md docs/admin-setup-ios/00-overview.md docs/admin-setup-ios/01-apns-certificate.md docs/admin-setup-ios/02-abm-token.md docs/admin-setup-ios/03-ade-enrollment-profile.md docs/admin-setup-ios/04-configuration-profiles.md docs/admin-setup-ios/05-app-deployment.md docs/admin-setup-ios/06-compliance-policy.md docs/admin-setup-ios/07-device-enrollment.md docs/admin-setup-ios/08-user-enrollment.md docs/admin-setup-ios/09-mam-app-protection.md docs/admin-setup-macos/00-overview.md; do pwsh -NoProfile -File scripts/pipeline/convert.ps1 -InputMd "$f" -OutputDocx ".pipeline-output/$(basename $f .md).docx" >/dev/null 2>&1 || echo "FAIL: $f"; done; echo "all-12-converted"</automated>
  </verify>
  <acceptance_criteria>
    - All 12 files in RESEARCH's verified failing set convert exit 0 (the loop prints no `FAIL:` line)
    - README.md SC1 prose documents the pre-pandoc source-normalization step and states line 74 stays flag-identical
    - SUMMARY records the OQ4 9-key confirmation on a converted nav file (custom.xml has all 9; no body leak)
    - SUMMARY records a per-file byte-diff of word/document.xml for all 14 previously-passing nav files, each byte-identical pre/post fix
    - No source .md in docs/ shows any git diff after the regression run
  </acceptance_criteria>
  <done>SC1 documented; D-04 positive (12 exit-0) + OQ4 non-regression (9 keys, no leak) + byte-equivalence (14 identical) all proven and recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source `.md` → pandoc | The preprocessing step transforms content between the frozen source and the pandoc consumer; the source's byte-unchanged guarantee is the trust invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01-A | Tampering | convert.ps1 preprocessing regex | mitigate | D-03(b) fail-closed diff guard: `Write-Error` + `exit 1` on any temp-vs-source delta other than `---`→`* * *` on an anchor-matched line — never silently proceeds |
| T-124-01-B | Tampering | source `.md` on disk | mitigate | Preprocessing writes only an ephemeral temp copy (D-01); source is read-only; Task-1 acceptance asserts zero git diff on the source after a run |
| T-124-01-C | Tampering | fenced-code `---` inside a doc | mitigate | D-03(a) fence-tracking skips any `---` inside a ``` ``` ```/`~~~` fence (0 instances today; durable-tool latent defense) |
| T-124-SC | Tampering | npm/pip/cargo installs | accept | Phase installs ZERO packages (RESEARCH Package Legitimacy Audit = N/A — Node built-ins + pinned pandoc only); no install surface |
</threat_model>

<verification>
- SC1: a representative previously-failing admin-setup file converts exit 0 through convert.ps1 (Task 1 + Task 3)
- SC2 / D-04: all 12 failing files exit 0; custom.xml retains 9 props with no body leak; word/document.xml byte-identical on the 14 passers (Task 3)
- guard-docx.mjs --self-test exits 0 (Task 2)
- Phase-125 firewall: no chain validator, no V115 entry, no close-gate, no requirement flip
</verification>

<success_criteria>
- convert.ps1 carries the D-01 preprocessing block + D-03(a) fence-tracking + D-03(b) fail-closed guard; line-74 invocation flag-identical
- lib/ooxml.mjs exports extractCustomProperties(); guard-docx.mjs has a permanent CUSTOM-PROPS check; --self-test green
- The D-04 three-part regression is executed and recorded (12 exit-0, 9-key OQ4 non-regression, 14 byte-identical)
- README.md SC1 documents the pre-pandoc source-normalization step
- No docs/ source .md mutated
</success_criteria>

<output>
Create `.planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/124-01-SUMMARY.md` when done
</output>
