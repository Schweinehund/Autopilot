---
phase: 124-pipeline-fix-descriptive-filename-pass-draft-label-grounding
plan: 03
type: execute
wave: 3
depends_on: ["124-01", "124-02"]
files_modified:
  - scripts/pipeline/test-fixtures/draft-test-doc.md
  - scripts/pipeline/test-fixtures/README.md
  - .planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/PIPE-05-RUNBOOK.md
  - .planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/PIPE-05-FINDINGS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [PIPE-05]

must_haves:
  truths:
    - "draft-test-doc.md carries the shipped EEE single-line block (·-separated, Platform-first) placed BEFORE the H1, with frontmatter status: draft (per D-14)"
    - "The reformatted fixture converts exit 0 through convert.ps1 and passes guard-docx.mjs (agent-runnable local legs)"
    - "PIPE-05-RUNBOOK.md (reusable, parameterized, two fixed queries, binary rubric) and a blank owner-fillable PIPE-05-FINDINGS.md exist in the phase dir (per D-15)"
    - "REQUIREMENTS.md:35 inversion corrected (D-17) and ROADMAP SC4 reworded outcome-neutral with FAIL-escalation (D-18)"
    - "The owner-run live Copilot Studio confirmation is a blocking checkpoint sequenced LAST; SC4 checkbox is NOT auto-flipped (per D-16)"
  artifacts:
    - path: "scripts/pipeline/test-fixtures/draft-test-doc.md"
      provides: "Reformatted PIPE-05 fixture: shipped EEE block before H1, status: draft"
      contains: "Status:** Draft"
    - path: ".planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/PIPE-05-RUNBOOK.md"
      provides: "Reusable owner-run procedure: parameterized fill-ins, the fixture, two fixed queries, binary rubric, SC3 test-library-only warning"
    - path: ".planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/PIPE-05-FINDINGS.md"
      provides: "Blank owner-fillable evidence template with D-16 tenant-unavailable stub path + D-18 FAIL-escalation reminder + D-19 note"
  key_links:
    - from: "scripts/pipeline/test-fixtures/draft-test-doc.md"
      to: "convert.ps1 + guard-docx.mjs"
      via: "agent-runnable local conversion/guard proof before owner upload"
      pattern: "draft-test-doc"
    - from: "PIPE-05-FINDINGS.md"
      to: "Phase 125 HARN-07"
      via: "necessary-but-not-sufficient Draft-label slice (D-19)"
      pattern: "HARN-07"
---

<objective>
Prepare and gate the owner-run Draft-label grounding probe (PIPE-05, D-13 owner-run / agent-prepared / blocking-checkpoint-last). Terminal plan of Phase 124 (Wave 3), delivering SC4. Confirms empirically that the shipped EEE header-block format renders the literal "Draft" label and makes it queryable in live Copilot Studio.

Purpose: v1.15's Phase-113 probe DID exercise the visible `**Status:**` body-text leg (the leg that surfaces); PIPE-05 is a cosmetic FORMAT re-confirmation of the shipped `·`-separated Platform-first single-line block vs. the v1.15 `.`-separated Doc-ID-first stub — NOT a new-leg discovery (D-17). The agent prepares the fixture, runbook, and blank findings, proves the local convert+guard legs, then HALTS; the owner (Josh) runs the irreducible live-tenant upload + query leg.

Output: reformatted `draft-test-doc.md` fixture, `PIPE-05-RUNBOOK.md` + blank `PIPE-05-FINDINGS.md`, the D-17 REQUIREMENTS.md:35 correction, the D-18 ROADMAP SC4 reword, and a blocking owner-verify checkpoint that flips SC4 only on the owner's in-thread confirmation + committed FINDINGS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/124-CONTEXT.md
@.planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/124-RESEARCH.md
@.planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/124-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reformat draft-test-doc.md to shipped EEE format + prove local convert/guard legs</name>
  <read_first>
    - scripts/pipeline/test-fixtures/draft-test-doc.md (the fixture being modified — currently `.`-separated, Doc-ID-first, block placed AFTER the H1)
    - scripts/pipeline/test-fixtures/README.md (the "Stub EEE Header Format" section lines 25-35 — superseded convention; add a one-line note)
    - docs/l1-runbooks/01-device-not-registered.md (real shipped-format corpus analog: frontmatter close → header block → blank → H1 → blank → `## Summary`)
    - docs/_standards/EEE-SOP-standard.md §Format specification (lines 80-119 — `·`-separated, Platform-first, before the H1)
    - 124-RESEARCH.md Pitfall 3 (position-relative-to-H1 must move, not just separator) + "Draft-test-doc.md reformat target" (lines 527-563) + Open Question 3 / Assumption A4 (frontmatter key count)
    - 124-PATTERNS.md §draft-test-doc.md (target shape, 4 key structural facts)
    - 124-CONTEXT.md D-14 (single artifact, shipped block + status: draft, no A/B twin)
  </read_first>
  <action>
    Reformat `scripts/pipeline/test-fixtures/draft-test-doc.md` to the shipped EEE format: (1) replace the visible header line with the single-line block `**Platform:** macOS · **Doc Type:** Runbook · **Doc ID:** RE-T05 · **Status:** Draft` — exactly four fields in Platform → Doc Type → Doc ID → Status order, `·` (U+00B7 middle-dot) separator, NOT `.`; (2) RELOCATE that block to sit immediately after the frontmatter close and BEFORE the `# Draft Test Document — macOS Synthetic` H1 (its current position after the H1 is wrong per Pitfall 3 — position, not just wording, must match); (3) ensure `## Summary` is the first H2 immediately after the H1 with nothing between the block and it (add a `## Summary` section if absent, keeping the existing synthetic body). PLANNER DECISION (resolving RESEARCH OQ3/A4 explicitly): EXPAND the frontmatter to the full 9-key set (`doc_id, status: draft, owner, doc_type, platform, last_verified, review_by, applies_to, audience`) so the fixture faithfully mirrors a real retrofitted Draft doc per D-14's own framing and promotes cleanly through the 124-01 CUSTOM-PROPS guard — keep `status: draft` (the mutated Draft leg). Keep the existing deployment-policy blockquote (test-library-only warning). Then add a one-line note to `test-fixtures/README.md` that this one fixture now follows the SHIPPED format/position (the other 5 fixtures legitimately keep the old stub shape). Prove the local legs: convert the reformatted fixture through `convert.ps1` and run `guard-docx.mjs` — both exit 0.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && pwsh -NoProfile -File scripts/pipeline/convert.ps1 -InputMd scripts/pipeline/test-fixtures/draft-test-doc.md -OutputDocx .pipeline-output/draft-test-doc.docx && node scripts/pipeline/guard-docx.mjs .pipeline-output/draft-test-doc.docx; echo "guard=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The visible block is `**Platform:** macOS · **Doc Type:** Runbook · **Doc ID:** RE-T05 · **Status:** Draft` (·-separated, Platform-first) and sits BEFORE the H1 (the block line number is less than the `# Draft Test Document` line number)
    - Frontmatter carries `status: draft` and the full 9-key set
    - `convert.ps1` exits 0 on the fixture; `guard-docx.mjs` exits 0 (including a CUSTOM-PROPS PASS)
    - test-fixtures/README.md carries the one-line shipped-format note; the other 5 fixtures are untouched
  </acceptance_criteria>
  <done>Fixture reformatted to shipped EEE format + position, 9-key frontmatter with status: draft; local convert + guard legs both exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Author PIPE-05-RUNBOOK.md + blank PIPE-05-FINDINGS.md + apply the D-17/D-18 corrections</name>
  <read_first>
    - .planning/milestones/v1.15-phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-RUNBOOK.md (172 lines — structural template: Prerequisites fill-in table, Upload Procedure, Query Sequence table, Completion condition + commit/confirm-in-thread closing)
    - .planning/milestones/v1.15-phases/113-conversion-pipeline-lock-representative-set-grounding-valida/PIPE-02-FINDINGS.md (213 lines — Run Metadata table, SC4 Confirmations checkbox pair, binary `- [x]`/`- [ ]` convention)
    - .planning/REQUIREMENTS.md (line 35 — PIPE-05 wording to correct per D-17)
    - .planning/ROADMAP.md — Phase 124 SC4 is the line beginning "The owner confirms via a live Copilot Studio session…" (currently line 189; SC3 is line 188 — do NOT reword SC3). Match on the SC4 TEXT, not a hardcoded line number.
    - scripts/pipeline/README.md §SC3 (test-library-only upload rule to echo in the runbook)
    - 124-CONTEXT.md D-15 (evidence artifacts, two fixed queries, binary rubric), D-16 (checkpoint discipline + tenant-unavailable stub), D-17 (REQUIREMENTS.md:35 correction wording), D-18 (SC4 reword — outcome-neutral + FAIL-escalation), D-19 (PIPE-05 ≠ HARN-07)
    - 124-PATTERNS.md §PIPE-05-RUNBOOK.md and §PIPE-05-FINDINGS.md (sections to reuse; D-16 fallback + D-18 escalation are NEW prose)
  </read_first>
  <action>
    Author `.planning/phases/124-.../PIPE-05-RUNBOOK.md` off the v1.15 PIPE-02-RUNBOOK.md shape, narrowed per D-15: a Prerequisites owner-fill-in table (test SharePoint library URL, Copilot Studio agent name + chat URL as `_(owner fills in)_` placeholders — reusable/parameterized); an Upload Procedure for the ONE `draft-test-doc.docx` file with an explicit repeated SC3 warning ("upload to the TEST library ONLY, never the production/indexed library"); a Query Sequence with exactly TWO fixed queries — a RENDER query ("Tell me about the draft macOS test document") and a QUERYABLE query ("What is the status of the RE-T05 document?"); and a binary rubric: PASS = the literal "Draft" appears in the response/citation, attributable to the visible body-text block. Author `.planning/phases/124-.../PIPE-05-FINDINGS.md` as a BLANK owner-fillable template: Run Metadata table, an SC4 Confirmations checkbox pair for the two queries, PLUS two NEW sections not in the v1.15 template — (a) a D-16 tenant-unavailable stub path ("prepared; live confirmation deferred to deployment — never a fabricated pass") and (b) a D-18 FAIL-escalation reminder (a recorded surfacing FAIL does NOT auto-close SC4 — it escalates as a defect requiring triage before Phase 124 close). Add the D-19 note that PIPE-05-FINDINGS.md is a NECESSARY-BUT-NOT-SUFFICIENT input to Phase 125's HARN-07 (narrow Draft-label slice, not the whole-corpus grounding confirmation). Apply the D-17 correction to REQUIREMENTS.md:35 — reword so it states accurately that the v1.15 Phase-113 probe DID exercise the visible `**Status:**` body-text leg (the surfacing leg), that the frontmatter `status:` leg is the non-surfacing one (promotes to an invisible Word custom property), and that PIPE-05 is a cosmetic format re-confirmation (per EEE-SOP-standard.md §L284-296), not a new-leg discovery. Reword ROADMAP SC4 (D-18) by MATCHING ON THE SC4 TEXT — find the line beginning "The owner confirms via a live Copilot Studio session…" (do NOT match SC3 above it, and do NOT rely on a hardcoded line number) — and replace it outcome-neutral with the FAIL-escalation clause: "PIPE-05 probe executed against the shipped EEE header-block format; the Draft label's render + queryability recorded in PIPE-05-FINDINGS.md" — a PASS or a tenant-unavailable deferred result closes SC4 cleanly; a recorded surfacing FAIL escalates as a genuine defect. Do NOT flip the SC4 checkbox in this task. Do NOT touch the REQUIREMENTS.md Traceability table Status column (that flip is Phase 125's job — firewall).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && test -f ".planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/PIPE-05-RUNBOOK.md" && test -f ".planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/PIPE-05-FINDINGS.md" && grep -c "HARN-07" ".planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/PIPE-05-FINDINGS.md"</automated>
  </verify>
  <acceptance_criteria>
    - PIPE-05-RUNBOOK.md exists with the Prerequisites fill-in table, ONE-file Upload Procedure, an explicit SC3 test-library-only warning, exactly two fixed queries (render + queryable), and the binary PASS rubric
    - PIPE-05-FINDINGS.md exists as a BLANK template with the Run Metadata table, SC4 checkbox pair, the D-16 tenant-unavailable stub section, the D-18 FAIL-escalation reminder, and the D-19 HARN-07 note (grep finds `HARN-07`)
    - REQUIREMENTS.md:35 now states the v1.15 probe exercised the VISIBLE `**Status:**` leg and frames PIPE-05 as a format re-confirmation (D-17 inversion corrected)
    - ROADMAP SC4 (the "The owner confirms via a live Copilot Studio session…" line) reworded outcome-neutral with the FAIL-escalation clause (D-18); SC3 above it left intact; no SC4 checkbox flipped
    - No requirement flipped to Validated; REQUIREMENTS.md Traceability Status column untouched (Phase-125 firewall)
  </acceptance_criteria>
  <done>RUNBOOK + blank FINDINGS authored per D-15; D-16 stub + D-18 escalation + D-19 note present; REQUIREMENTS.md:35 (D-17) and ROADMAP SC4 (D-18, matched by text) corrected; no checkbox/requirement flip.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: OWNER-RUN — live Copilot Studio Draft-label confirmation (blocking, sequenced last)</name>
  <what-built>
    The agent has prepared everything the owner needs and HALTED: (1) `scripts/pipeline/test-fixtures/draft-test-doc.md` reformatted to the shipped EEE block + `status: draft`, proven to convert exit 0 and pass guard-docx.mjs locally; (2) `.pipeline-output/draft-test-doc.docx` produced; (3) `PIPE-05-RUNBOOK.md` (parameterized, two fixed queries, binary rubric, SC3 test-library-only warning) and blank `PIPE-05-FINDINGS.md`. The live Copilot Studio / SharePoint upload + query leg is genuinely owner-only — no connector or credential exists in this environment (D-13).
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/PIPE-05-RUNBOOK.md` and fill in the Prerequisites table (test library URL, Copilot Studio agent name + chat URL).
    2. Upload `.pipeline-output/draft-test-doc.docx` to the TEST SharePoint library ONLY (never the production/indexed library — README §SC3). Wait for Copilot Studio to sync/index it.
    3. Run the TWO fixed queries from the runbook: the RENDER query ("Tell me about the draft macOS test document") and the QUERYABLE query ("What is the status of the RE-T05 document?").
    4. Record PASS/FAIL/DEFERRED in `PIPE-05-FINDINGS.md` per the binary rubric (PASS = the literal "Draft" appears in the response/citation, attributable to the visible body-text block). Commit the filled FINDINGS.
    5. If the tenant is unavailable: per D-16, commit the FINDINGS stub recording "prepared; live confirmation deferred to deployment" — never a fabricated pass. This is a clean deferred close of SC4.
    6. If the Draft label does NOT surface (a real surfacing FAIL): per D-18 this does NOT auto-close SC4 — it escalates as a genuine defect (shipped block regressed vs. the v1.15 stub, contradicting EEE-SOP-standard.md §L284-296) requiring triage before Phase 124 close.

    Checkpoint discipline (D-16): hold the active Jira Story In Progress across this checkpoint (do not let the Stop-hook flip it Done mid-probe). The SC4 checkbox flips ONLY on the owner's in-thread confirmation + committed FINDINGS — the agent must not auto-flip it. A continuation commits the result after the owner confirms.
  </how-to-verify>
  <resume-signal>Owner types "approved" (PASS recorded) OR "deferred" (tenant-unavailable stub committed) OR "fail" (surfacing FAIL — escalate to triage). Then a continuation commits the FINDINGS and closes SC4 per the recorded outcome.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo fixture → live SharePoint/Copilot Studio tenant | The test artifact crosses into an external SaaS control plane the repo does not own; the "test library only" scoping is the trust invariant |
| owner probe result → committed FINDINGS | The evidence record must reflect the actual live outcome, not an optimistic assumption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-03-A | Information Disclosure | draft-test-doc.docx uploaded to the wrong library | mitigate | Explicit SC3 "TEST library ONLY, never production/indexed" warning repeated in the RUNBOOK Upload Procedure and echoed in the checkpoint how-to-verify |
| T-124-03-B | Repudiation | fabricated/optimistic PIPE-05 PASS if tenant unavailable | mitigate | D-16: commit an honest "prepared; live confirmation deferred" stub rather than a fabricated confirmation; agent never auto-flips SC4 |
| T-124-SC | Tampering | npm/pip/cargo installs | accept | Phase installs ZERO packages (Node built-ins + pinned pandoc only); no install surface |
</threat_model>

<verification>
- SC4 / D-14: fixture reformatted to shipped EEE block + status: draft; converts + guards clean locally (Task 1)
- D-15: reusable RUNBOOK + blank FINDINGS authored, two fixed queries, binary rubric (Task 2)
- D-17: REQUIREMENTS.md:35 inversion corrected; D-18: ROADMAP SC4 (matched by text) reworded outcome-neutral + FAIL-escalation (Task 2)
- D-13/D-16: owner-run blocking checkpoint sequenced last; no SC4 auto-flip; honest tenant-unavailable path (Task 3)
- D-19: PIPE-05-FINDINGS.md flagged as necessary-but-not-sufficient input to HARN-07
- Phase-125 firewall: no chain validator, no V115 entry, no close-gate, no requirement flip to Validated
</verification>

<success_criteria>
- draft-test-doc.md carries the shipped EEE block (·-separated, Platform-first, before H1) + 9-key frontmatter with status: draft; convert + guard exit 0
- PIPE-05-RUNBOOK.md + blank PIPE-05-FINDINGS.md exist with the D-15 shape + D-16/D-18/D-19 additions
- REQUIREMENTS.md:35 (D-17) and ROADMAP SC4 (D-18, matched by SC4 text) corrected
- Owner-run confirmation is a blocking checkpoint sequenced last; SC4 flips only on owner confirm + committed FINDINGS
</success_criteria>

<output>
Create `.planning/phases/124-pipeline-fix-descriptive-filename-pass-draft-label-grounding/124-03-SUMMARY.md` when done
</output>
