---
phase: 79-reference-integration-capability-matrix-5-platform-compariso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-ANCHOR-INVENTORY.md
  - docs/reference/macos-capability-matrix.md
  - docs/reference/4-platform-capability-comparison.md
autonomous: true
requirements: [SSOREF-02]

must_haves:
  truths:
    - "An admin opening docs/reference/macos-capability-matrix.md finds a new ## Authentication section (inserted before ## See Also, after ## Key Gaps Summary) with seven Feature|Windows|macOS prose rows covering: Auth methods (Secure Enclave key [Microsoft-recommended] / Password sync / Smart card), Hardware gate (T2 / Apple Silicon for Secure Enclave), macOS version floor (macOS 14.0 recommended floor), Entra ID licensing (no P1/P2 for PSSO itself), NUAL (macOS 14+), Passkey/FIDO2 (Secure Enclave only), and Hybrid Entra join = Not supported (explicit anti-feature, its own row) -- macOS cells summarize and link guide 08/09 (link-not-copy; guide 08's four-dimension table is NOT cloned); Windows cells are terse scope markers 'n/a -- not covered in this matrix' (D-01 / A1 / B2 / SC1)"
    - "The existing ## Configuration 'Platform SSO' row at macos-capability-matrix.md line 38 now reads '| Platform SSO | No | Yes (macOS 14+ via Settings Catalog) -- see [Authentication](#authentication) |' -- cell text changed only; the ## Configuration heading and its #configuration anchor are untouched (X1)"
    - "docs/reference/4-platform-capability-comparison.md has a new ## Single Sign-On H2 section (the 7th section, inserted after ## Conditional Access and before ## See Also) with one feature row 'OS-integrated SSO (Platform SSO)'; the macOS cell is the SC2-verbatim string 'Supported (macOS 14+) — [matrix](macos-capability-matrix.md#authentication)'; the Windows/iOS/Android/Linux cells are bare uppercase N/A; a one-line scope footnote follows the table (C1 / D-03 / X2 / X3 / SC2)"
    - "The pre-edit anchor inventory 79-ANCHOR-INVENTORY.md exists, lists both reference files' pre-edit H2 slugs, and was committed in its OWN commit BEFORE the content-edit commit (git log order: inventory commit precedes content commit) (D-04 / D1)"
    - "node scripts/validation/v1.8-milestone-audit.mjs passes (C12 + C13 green): C12 sees the six named H2s still present in the comparison doc (a 7th H2 is allowed), the macOS SSO cell carries a valid [text](link), and the non-macOS cells are exempt uppercase N/A; C13's 15-entry allowlist shape is unchanged; the matrix #authentication anchor target lands in the SAME commit as the comparison cell that links to it"
  artifacts:
    - path: ".planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-ANCHOR-INVENTORY.md"
      provides: "Pre-edit H2-slug inventory of both reference files + superset assertion; committed before any content edit (SC2 hard prerequisite)"
      contains: "configuration"
      min_lines: 20
    - path: "docs/reference/macos-capability-matrix.md"
      provides: "New ## Authentication section (7 rows) before ## See Also; X1 cell edit on the ## Configuration Platform SSO row; updated front-matter dates + Version-History row"
      contains: "## Authentication"
    - path: "docs/reference/4-platform-capability-comparison.md"
      provides: "New ## Single Sign-On section after ## Conditional Access (X3 macOS cell, X2 non-macOS N/A cells, scope footnote); updated front-matter dates + Version-History row"
      contains: "## Single Sign-On"
  key_links:
    - from: "docs/reference/4-platform-capability-comparison.md"
      to: "docs/reference/macos-capability-matrix.md#authentication"
      via: "X3 SC2-verbatim macOS SSO cell link"
      pattern: "\\[matrix\\]\\(macos-capability-matrix\\.md#authentication\\)"
    - from: "docs/reference/macos-capability-matrix.md"
      to: "docs/reference/macos-capability-matrix.md#authentication"
      via: "X1 ## Configuration Platform SSO row back-reference"
      pattern: "see \\[Authentication\\]\\(#authentication\\)"
    - from: "docs/reference/macos-capability-matrix.md#authentication"
      to: "docs/admin-setup-macos/08-auth-methods-deep-dive.md"
      via: "macOS Authentication cells summarize + link guide 08 (link-not-copy)"
      pattern: "08-auth-methods-deep-dive\\.md"
---

<objective>
Integrate the v1.9 macOS Platform SSO authentication facts (authored in guides 08/09, Phases 77-78) into the two cross-platform reference documents using the established link-not-copy architecture, behind a mandatory pre-edit anchor inventory. Three deliverables, two commits:

1. CREATE `.planning/phases/79-.../79-ANCHOR-INVENTORY.md` -- the pre-edit H2-slug inventory of both reference files + superset assertion. Committed in its OWN commit (commit 1) BEFORE any content edit (ROADMAP SC2 hard prerequisite, D-04 / D1).
2. EDIT `docs/reference/macos-capability-matrix.md` -- append a new `## Authentication` section (7 rows, before `## See Also`) AND surgically update the existing `## Configuration` "Platform SSO" row at line 38 to back-reference `#authentication` (D-01 / A1 / B2 / X1 / SC1).
3. EDIT `docs/reference/4-platform-capability-comparison.md` -- add a new `## Single Sign-On` H2 (the 7th section, after `## Conditional Access`) with the macOS PSSO cell (X3 SC2-verbatim) + non-macOS bare `N/A` cells (X2) + a scope footnote (C1 / D-03 / SC2). The matrix and comparison edits land TOGETHER in ONE atomic commit (commit 2).

Purpose: An admin navigating the reference docs can reach the complete macOS PSSO auth-method picture (three methods, hardware/version gates, Entra licensing, NUAL, passkey, and the hybrid-join anti-feature) from the matrix's own Authentication section, and sees PSSO surfaced as a cross-platform SSO dimension in the comparison doc -- all without anchor drift breaking the frozen v1.8 C12/C13 harness.

Output:
- NEW `.planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-ANCHOR-INVENTORY.md` (commit 1)
- EDIT `docs/reference/macos-capability-matrix.md` (commit 2)
- EDIT `docs/reference/4-platform-capability-comparison.md` (commit 2)

CRITICAL commit ordering: the anchor-inventory commit MUST precede the content commit (ROADMAP hard prerequisite, RESEARCH Pitfall 1). The two content files MUST land in the SAME commit -- splitting them risks C13 failure because the comparison doc's `macos-capability-matrix.md#authentication` link target is created in the matrix file in that same commit (RESEARCH Pitfall, Pattern 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-CONTEXT.md
@.planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-RESEARCH.md
@.planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-PATTERNS.md
@docs/reference/macos-capability-matrix.md
@docs/reference/4-platform-capability-comparison.md
@docs/admin-setup-macos/08-auth-methods-deep-dive.md
@docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit 79-ANCHOR-INVENTORY.md alone (commit 1 -- the hard prerequisite)</name>
  <files>.planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-ANCHOR-INVENTORY.md</files>
  <read_first>
    - docs/reference/macos-capability-matrix.md (enumerate every `^## ` H2 to confirm the 8 pre-edit headings: Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access, Key Gaps Summary, See Also)
    - docs/reference/4-platform-capability-comparison.md (enumerate every `^## ` H2 to confirm the 8 pre-edit headings: Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access, See Also, Version History)
    - .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-PATTERNS.md (the "Proposed 79-ANCHOR-INVENTORY.md layout" block -- the exact two-table + superset-assertion shape to reproduce; the deterministic grep `^## ` -> GitHub slug rule)
    - .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-RESEARCH.md (Pattern 1 anchor sets; Pitfall 1 -- this file MUST be committed before any content edit)
    - .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-CONTEXT.md (D-04 / D1 -- own-commit-first rule; superset additions are only #authentication in the matrix and #single-sign-on in the comparison; #configuration is preserved)
    - .planning/phases/78-legacy-sso-plug-in-migration-guide/78-01-PLAN.md (analogous docs-phase plan: atomic-commit discipline, anchor-stability framing)
  </read_first>
  <action>
Create `.planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-ANCHOR-INVENTORY.md` using the Write tool (do NOT use bash redirection). Reproduce the layout in 79-PATTERNS.md "Proposed 79-ANCHOR-INVENTORY.md layout": an H1 title "Phase 79 Pre-Edit Anchor Inventory", a Generated: 2026-06-21 line and a one-line Purpose statement (committed before content edits per SC2 hard prerequisite; proves post-edit anchor sets are strict supersets of these pre-edit sets).

Two H2 tables, each `| H2 heading (exact) | Slug | Post-edit status |`:

For `macos-capability-matrix.md` -- 8 pre-edit rows, slugified by the GitHub rule (lowercase, spaces to hyphens, strip non-alphanumeric-non-hyphen): `## Enrollment` -> #enrollment (preserved); `## Configuration` -> #configuration (preserved -- X1 edits cell text only, heading NOT touched); `## App Deployment` -> #app-deployment (preserved); `## Compliance` -> #compliance (preserved); `## Software Updates` -> #software-updates (preserved); `## Conditional Access` -> #conditional-access (preserved); `## Key Gaps Summary` -> #key-gaps-summary (preserved); `## See Also` -> #see-also (preserved). Then one ADDED row: `## Authentication` -> #authentication (ADDED, before `## See Also`).

For `4-platform-capability-comparison.md` -- 8 pre-edit rows: `## Enrollment` -> #enrollment; `## Configuration` -> #configuration; `## App Deployment` -> #app-deployment; `## Compliance` -> #compliance; `## Software Updates` -> #software-updates; `## Conditional Access` -> #conditional-access; `## See Also` -> #see-also; `## Version History` -> #version-history (all preserved). Then one ADDED row: `## Single Sign-On` -> #single-sign-on (ADDED, before `## See Also`).

Close with a "## Superset Assertion" section stating both post-edit sets are strict supersets of the pre-edit sets (matrix adds only #authentication; comparison adds only #single-sign-on; no pre-edit anchor removed or renamed; the comparison doc's six C12-named H2s -- Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access -- all remain present).

Commit this file ALONE as commit 1 (e.g. `docs(79): pre-edit anchor inventory for reference integration`). Do NOT stage or commit either content file in this commit. This is the ROADMAP hard prerequisite -- the inventory commit must exist in git history BEFORE the content commit (RESEARCH Pitfall 1).
  </action>
  <verify>
    <automated>test -f .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-ANCHOR-INVENTORY.md && grep -qi "configuration" .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-ANCHOR-INVENTORY.md && grep -qi "authentication" .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-ANCHOR-INVENTORY.md && grep -qi "single-sign-on" .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-ANCHOR-INVENTORY.md && git log --oneline -1 -- .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-ANCHOR-INVENTORY.md && echo "INVENTORY_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `79-ANCHOR-INVENTORY.md` exists and lists BOTH reference files' pre-edit H2 slugs (8 each) with a Post-edit status column.
    - The matrix table includes a `#configuration` row explicitly marked preserved (X1 edits cell text only).
    - The two post-edit additions are exactly `#authentication` (matrix) and `#single-sign-on` (comparison); a Superset Assertion section states no pre-edit anchor is removed/renamed and the six C12-named comparison H2s remain.
    - The file is committed in its OWN commit (commit 1); `git log --oneline -- <inventory path>` shows that commit, and it precedes any commit touching the two content files (verified in Task 3 git-history check).
    - Neither `macos-capability-matrix.md` nor `4-platform-capability-comparison.md` is modified in this commit.
    - Verify command prints `INVENTORY_OK`.
  </acceptance_criteria>
  <done>79-ANCHOR-INVENTORY.md exists with both pre-edit anchor tables + superset assertion and is committed alone as commit 1 before any content edit; the verify command prints INVENTORY_OK.</done>
</task>

<task type="auto">
  <name>Task 2: Edit macos-capability-matrix.md -- append ## Authentication (7 rows) + X1 line-38 Configuration cell edit + front-matter/Version-History (part of commit 2; do NOT commit yet)</name>
  <files>docs/reference/macos-capability-matrix.md</files>
  <read_first>
    - docs/reference/macos-capability-matrix.md (front-matter lines 1-7; the `## Configuration` heading at line 28 + the Platform SSO row at line 38 [X1 target]; the existing 3-col `| Feature | Windows | macOS |` section shape; the terse Windows scope-marker precedent at line 84 `n/a (Windows uses Intune client)`; `## Key Gaps Summary` ending ~line 98; `## See Also` at line 100; the Version-History table at lines 109-111)
    - .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-PATTERNS.md (the full `## Authentication` section shape with the 7 exact rows + link targets; the X1 find/replace; the front-matter DS-2 date update; the Version-History append row; the insertion point between line 98 and line 100; the `--` vs ` — ` dash conventions)
    - .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-RESEARCH.md (SC1 Fact Set rows 1-7 with confirmed values + confidence; D-01a matrix is NOT C12-bound [prose cells OK]; Pitfall 3 -- do NOT touch the ## Configuration heading; Pitfall 4 -- do NOT clone guide 08's four-dimension table; Pitfall 7 -- insert before ## See Also; Open Questions -- update front-matter to 2026-06-21 / 2026-09-21)
    - .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-CONTEXT.md (A1 seven rows; B2 terse Windows scope markers + section-top scope line; D-02a/P1 placement before ## See Also; X1 cell-text-only edit; do NOT leak "v1.9" into reader-facing cells; CA-needs-P1 nuance LINKS to guide 08, do NOT restate)
    - docs/admin-setup-macos/08-auth-methods-deep-dive.md (canonical auth-method facts -- the link target for the macOS Authentication cells; confirm the file path/name for the relative link `../admin-setup-macos/08-auth-methods-deep-dive.md`)
    - docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md (canonical migration facts -- secondary link target where relevant)
    - scripts/validation/v1.8-milestone-audit.mjs (confirm the matrix file is NOT in the C12 canonical-6-col scope -- prose cells are legal here; matrix edits must not break any other check)
  </read_first>
  <action>
Edit `docs/reference/macos-capability-matrix.md`. Three changes; do NOT commit in this task (the commit happens in Task 3 atomically with the comparison-doc edit).

(a) FRONT-MATTER (lines 2-3): update `last_verified: 2026-04-14` -> `last_verified: 2026-06-21` and `review_by: 2026-07-13` -> `review_by: 2026-09-21` (DS-2 90-day cadence; the `## Authentication` addition is a material content change). Touch no other front-matter key.

(b) X1 CELL EDIT (line 38, inside `## Configuration` -- cell text ONLY, the heading at line 28 is NOT touched): replace the exact line
`| Platform SSO | No | Yes (macOS 14+ via Settings Catalog) |`
with
`| Platform SSO | No | Yes (macOS 14+ via Settings Catalog) — see [Authentication](#authentication) |`
Use a spaced em-dash ` — ` before "see" (matching the comparison doc's link grammar and the X1 locked string). Do not alter any other row in the Configuration table, and do not rename/reorder the `## Configuration` heading (Pitfall 3 -- the `#configuration` anchor must be preserved).

(c) APPEND `## Authentication` SECTION between the end of `## Key Gaps Summary` (~line 98) and the `## See Also` heading (line 100). Start with the H2 `## Authentication`, then a one-line section-top scope statement that this section documents macOS Platform SSO (PSSO) authentication and that Windows SSO configuration is not covered in this matrix (phrase as scope, NOT "out of v1.9 scope" -- do NOT leak the milestone label). Then a 3-column `| Feature | Windows | macOS |` table with EXACTLY these 7 rows in order (macOS cells = concise prose summary + link to guide 08/09, link-not-copy -- do NOT clone guide 08's four-dimension selection table; Windows cells = terse scope marker `n/a — not covered in this matrix`):

  1. Auth methods -- macOS cell: three methods -- Secure Enclave key (Microsoft-recommended), Password sync, Smart card; link to `[Auth Methods Deep Dive](../admin-setup-macos/08-auth-methods-deep-dive.md)`.
  2. Hardware gate -- macOS cell: Secure Enclave method requires T2 chip (Intel 2018-2020) or Apple Silicon (M1+, 2020+); Password sync and Smart card have no hardware gate.
  3. macOS version floor -- macOS cell: macOS 14.0 Sonoma (recommended floor -- all three methods, non-deprecated Settings Catalog key, NUAL, Repair flow); link guide 08 for the macOS 13 absolute-minimum detail. Phrase the floor as "macOS 14.0 (recommended floor)" per D-01 (reconciles with the Configuration row's "macOS 14+").
  4. Entra ID licensing -- macOS cell: No Entra ID P1 or P2 required for Platform SSO itself; link guide 08 for CA-integration licensing detail. Do NOT restate the CA-needs-P1 nuance (it lives canonically in guide 08 -- link-not-copy).
  5. NUAL (New User at Login) -- macOS cell: Supported, macOS 14+; on-demand account creation at the login window using Shared Device Keys; link guide 08. Document HIGH-confidence facts only; do NOT include the LOW-confidence `NewUserAuthorizationMode` key.
  6. Passkey / FIDO2 -- macOS cell: Supported via the Platform Credential -- Secure Enclave method ONLY; requires Entra Authentication-methods enablement; link guide 08.
  7. Hybrid Entra join -- macOS cell: **Not supported** -- macOS PSSO requires Entra ID (cloud-only) join; Microsoft has no plans to support hybrid Entra join on macOS. This is its OWN explicit anti-feature row, never silently omitted (REQUIREMENTS:83 anti-feature discipline).

Use the spaced em-dash ` — ` for inline separators in the macOS cells and Windows scope markers (matching line 84's style). Exact macOS-cell prose wording and row-label text are at your discretion within the SC1 facts above and the link-not-copy constraint.

(d) VERSION-HISTORY (table at lines 109-111): append ONE new row below the existing initial-version row (do NOT edit the existing row): `| 2026-06-21 | Add ## Authentication section (7 rows: auth methods, hardware gate, macOS version floor, Entra licensing, NUAL, passkey/FIDO2, hybrid Entra join anti-feature); update ## Configuration Platform SSO row to link #authentication (X1); refresh DS-2 dates | -- |`. The Author column uses `--` (two hyphens), matching the existing row.

Do NOT run the audit yet and do NOT commit -- Task 3 commits both content files atomically and runs the harness.
  </action>
  <verify>
    <automated>grep -q "^## Authentication" docs/reference/macos-capability-matrix.md && grep -q "Yes (macOS 14+ via Settings Catalog) — see \[Authentication\](#authentication)" docs/reference/macos-capability-matrix.md && grep -q "Hybrid Entra join" docs/reference/macos-capability-matrix.md && grep -q "Not supported" docs/reference/macos-capability-matrix.md && grep -qn "^## Configuration" docs/reference/macos-capability-matrix.md && grep -q "last_verified: 2026-06-21" docs/reference/macos-capability-matrix.md && grep -q "review_by: 2026-09-21" docs/reference/macos-capability-matrix.md && grep -c "not covered in this matrix" docs/reference/macos-capability-matrix.md && echo "MATRIX_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `macos-capability-matrix.md` contains a new `## Authentication` H2 inserted AFTER `## Key Gaps Summary` and BEFORE `## See Also` (Pitfall 7).
    - The Authentication table has the 7 dimension rows in order, including a dedicated "Hybrid Entra join" row whose macOS cell contains "Not supported"; each macOS cell summarizes + links guide 08/09 (link-not-copy -- guide 08's four-dimension table is NOT cloned, Pitfall 4); each Windows cell is a terse `n/a — not covered in this matrix` scope marker (grep count for "not covered in this matrix" is >= 7).
    - Line 38 Configuration cell now reads `| Platform SSO | No | Yes (macOS 14+ via Settings Catalog) — see [Authentication](#authentication) |` and the `## Configuration` heading is unchanged (`#configuration` anchor preserved, Pitfall 3).
    - No reader-facing cell or prose contains the string "v1.9".
    - Front-matter `last_verified: 2026-06-21` / `review_by: 2026-09-21`; one new Version-History row dated 2026-06-21 with Author `--` is appended (existing row unchanged).
    - The file is NOT committed in this task.
    - Verify command prints `MATRIX_OK`.
  </acceptance_criteria>
  <done>macos-capability-matrix.md has the new 7-row ## Authentication section before ## See Also, the X1 line-38 cell edit, updated front-matter dates, and an appended Version-History row, with the ## Configuration heading untouched; the verify command prints MATRIX_OK. File staged but NOT committed -- Task 3 commits both content files together.</done>
</task>

<task type="auto">
  <name>Task 3: Edit 4-platform-capability-comparison.md -- add ## Single Sign-On (X3 macOS cell, X2 N/A cells, footnote) + front-matter/Version-History; commit 2 atomically with Task 2 + run harness</name>
  <files>docs/reference/4-platform-capability-comparison.md</files>
  <read_first>
    - docs/reference/4-platform-capability-comparison.md (front-matter lines 1-7 [`last_verified: 2026-05-01`, the already-stale `review_by: 2026-06-15`]; the verdict-vocabulary lock at line 15; the 6-col `| Feature | Windows | macOS | iOS | Android | Linux |` header; the lowercase-linked `n/a` convention at lines 24, 93; `## Conditional Access` ending ~line 95; `## See Also` at line 97; the Version-History table at lines 108-110 -- note the em-dash `—` style in the change text)
    - .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-PATTERNS.md (the full `## Single Sign-On` section shape; the exact 6 cell values [Feature label, N/A x4, X3 macOS cell]; the footnote model; the insertion point between line 95 and line 97; the front-matter DS-2 update; the Version-History append row)
    - .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-RESEARCH.md (Pattern 3-5 -- C12 cell rule, X2 bare uppercase N/A exemption [audit.mjs:643 case-sensitive], X3 SC2-verbatim string passing the `/\[.+\]\(.+\)/` regex, 6-named-H2 sub-check allows a 7th; Pitfall 2 -- do NOT use lowercase n/a for non-macOS SSO cells; Pitfall 5 -- do NOT adapt the SC2 string; Pitfall 6 -- there is NO existing stub; the section is ADDED; Open Questions -- update front-matter to 2026-06-21 / 2026-09-21)
    - .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-CONTEXT.md (C1/D-03 new ## Single Sign-On 7th section after ## Conditional Access; X2 bare N/A + scope footnote; X3 SC2 verbatim, ADDED not updated; do NOT leak "v1.9")
    - scripts/validation/v1.8-milestone-audit.mjs (C12 ~lines 610-655: 6-col canonical table cell rule, `—` and uppercase `N/A` exemptions [case-sensitive], the six named-H2 presence check; C13 ~lines 660-679: 15-entry allowlist shape only -- the new internal link cannot trip it once the matrix #authentication target lands in the same commit)
    - .planning/phases/78-legacy-sso-plug-in-migration-guide/78-01-PLAN.md (analogous docs-phase plan: the atomic-commit + harness-gate verification shape this task mirrors)
  </read_first>
  <action>
Edit `docs/reference/4-platform-capability-comparison.md`. Then commit BOTH content files atomically (this file + the Task-2 matrix edits) and run the harness.

(a) FRONT-MATTER (lines 2-3): update `last_verified: 2026-05-01` -> `last_verified: 2026-06-21` and `review_by: 2026-06-15` -> `review_by: 2026-09-21` (the existing `review_by` is already past as of 2026-06-21; the SSO addition is a substantive review). Touch no other key.

(b) ADD `## Single Sign-On` SECTION between the end of `## Conditional Access` (~line 95) and the `## See Also` heading (line 97). This is the 7th content H2 (C1/D-03; the six C12-named H2s remain present, a 7th is allowed). Section body: the H2 `## Single Sign-On`, then the standard 6-col header `| Feature | Windows | macOS | iOS | Android | Linux |` with the separator row, then EXACTLY ONE data row with these exact cell values:
  - Feature (col 0): `OS-integrated SSO (Platform SSO)`
  - Windows (col 1): `N/A`  (bare uppercase -- X2; C12-exempt per audit.mjs:643 case-sensitive; do NOT use lowercase `n/a`, do NOT add a link -- Pitfall 2)
  - macOS (col 2): `Supported (macOS 14+) — [matrix](macos-capability-matrix.md#authentication)`  (X3 SC2-VERBATIM -- copy this string EXACTLY, including the `(macOS 14+)` parenthetical and the spaced em-dash ` — `; do NOT normalize to verdict-grammar -- Pitfall 5)
  - iOS (col 3): `N/A`
  - Android (col 4): `N/A`
  - Linux (col 5): `N/A`

After the table, add a one-line scope footnote (blockquote or italic line, your discretion) noting that non-macOS platform SSO authentication is not covered in this documentation set and that `N/A` reflects deliberate scope rather than capability absence. Do NOT use the string "v1.9" in the footnote (reader-facing scope phrasing).

This section is ADDED, not updated -- there is NO pre-existing SSO/Authentication stub in this file (Pitfall 6). Do NOT hunt for one.

(c) VERSION-HISTORY (table at lines 108-110): append ONE new row below the existing initial-version row: `| 2026-06-21 | Add ## Single Sign-On section (7th H2) with one feature row -- macOS Platform SSO cell (SC2 verbatim, links matrix #authentication); non-macOS cells bare N/A (X2); refresh DS-2 dates | -- |`. Author column uses `--` (two hyphens). Match the existing row's dash style for the change-text body.

(d) ATOMIC COMMIT + HARNESS: stage BOTH `docs/reference/macos-capability-matrix.md` (from Task 2) and `docs/reference/4-platform-capability-comparison.md` and commit them TOGETHER in ONE commit (commit 2; e.g. `docs(79): integrate macOS PSSO auth into matrix + comparison (SSOREF-02)`). This is mandatory: the comparison doc's `macos-capability-matrix.md#authentication` link target is the `## Authentication` H2 created in the matrix in this same commit -- splitting the two files into separate commits risks C13 broken-link failure (RESEARCH Pattern 5 / Pitfall). Then run `node scripts/validation/v1.8-milestone-audit.mjs` and confirm it passes (C12 + C13 green). Confirm `git diff HEAD~1 --name-only` shows BOTH content files, and that this commit comes AFTER the Task-1 anchor-inventory commit in git history.
  </action>
  <verify>
    <automated>grep -q "^## Single Sign-On" docs/reference/4-platform-capability-comparison.md && grep -q "OS-integrated SSO (Platform SSO)" docs/reference/4-platform-capability-comparison.md && grep -q "Supported (macOS 14+) — \[matrix\](macos-capability-matrix.md#authentication)" docs/reference/4-platform-capability-comparison.md && grep -q "last_verified: 2026-06-21" docs/reference/4-platform-capability-comparison.md && grep -q "review_by: 2026-09-21" docs/reference/4-platform-capability-comparison.md && for h2 in "## Enrollment" "## Configuration" "## App Deployment" "## Compliance" "## Software Updates" "## Conditional Access"; do grep -qF "$h2" docs/reference/4-platform-capability-comparison.md || { echo "MISSING $h2"; exit 1; }; done && node scripts/validation/v1.8-milestone-audit.mjs && git diff HEAD~1 --name-only | grep -q "docs/reference/macos-capability-matrix.md" && git diff HEAD~1 --name-only | grep -q "docs/reference/4-platform-capability-comparison.md" && echo "COMPARISON_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `4-platform-capability-comparison.md` contains a new `## Single Sign-On` H2 inserted AFTER `## Conditional Access` and BEFORE `## See Also`.
    - The SSO table has exactly one data row "OS-integrated SSO (Platform SSO)"; the macOS cell is the EXACT SC2 string `Supported (macOS 14+) — [matrix](macos-capability-matrix.md#authentication)` (not normalized -- Pitfall 5); the Windows/iOS/Android/Linux cells are bare uppercase `N/A` (not lowercase, not linked -- Pitfall 2); a one-line scope footnote follows the table and contains no "v1.9".
    - The six C12-named H2s (Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access) all remain present.
    - Front-matter `last_verified: 2026-06-21` / `review_by: 2026-09-21`; one new Version-History row dated 2026-06-21 with Author `--` appended (existing row unchanged).
    - `node scripts/validation/v1.8-milestone-audit.mjs` PASSES (C12 + C13 green).
    - The matrix + comparison edits are in ONE commit (`git diff HEAD~1 --name-only` shows BOTH content files); the anchor-inventory commit (Task 1) precedes this content commit in git history (`git log --oneline` order).
    - Verify command prints `COMPARISON_OK`.
  </acceptance_criteria>
  <done>4-platform-capability-comparison.md has the new ## Single Sign-On section with the X3 SC2-verbatim macOS cell and X2 bare-N/A non-macOS cells + scope footnote, updated front-matter dates, and an appended Version-History row; both content files are committed together as commit 2 (after the Task-1 inventory commit); the v1.8 audit passes; the verify command prints COMPARISON_OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published-guidance boundary | This phase ships only Markdown: two edited reference docs + one planning-artifact inventory. No executable code, no runtime inputs, no auth surface, no network/data flow. The only boundary is published guidance -- a wrong cell value, anchor drift, a stale/duplicate PSSO statement, or a broken internal link crossing from the reference doc into an admin's mental model and the frozen v1.8 audit harness. The dominant threat class is documentation-induced harness breakage / reader-misleading content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Tampering (C12 BLOCKING harness failure from a wrong comparison cell value) | The `## Single Sign-On` row in 4-platform-capability-comparison.md | mitigate | Non-macOS cells use bare uppercase `N/A` (the only C12-exempt + link-honest value per audit.mjs:643 case-sensitive check); the macOS cell uses the SC2-verbatim string which contains a valid `[text](link)` matching the C12 `/\[.+\]\(.+\)/` regex; the six C12-named H2s are preserved (a 7th is allowed). Verified by `node scripts/validation/v1.8-milestone-audit.mjs` passing C12 (Task 3). |
| T-79-02 | Tampering (C13 / broken-link failure -- macOS link lands without its `#authentication` target) | Cross-file link comparison -> matrix#authentication | mitigate | The matrix `## Authentication` H2 (the link target) and the comparison cell that links to it land in the SAME atomic commit (commit 2), so the target exists when the harness/readers resolve the link; verified by `git diff HEAD~1 --name-only` showing both files and the audit passing C13 (Task 3). |
| T-79-03 | Tampering (anchor drift -- an existing slug renamed/removed breaks inbound comparison-doc links) | Existing H2 headings in both reference files, esp. matrix `## Configuration` | mitigate | The pre-edit `79-ANCHOR-INVENTORY.md` (committed first as commit 1) records all 16 pre-edit slugs; X1 is a cell-text-only edit (the `## Configuration` heading at line 28 is untouched -> `#configuration` preserved); only `#authentication` and `#single-sign-on` are added; the post-edit superset assertion and the audit confirm no slug is removed/renamed (Pitfall 3). |
| T-79-04 | Information disclosure (stale / duplicate PSSO statement misleads the reader) | The existing matrix `## Configuration` "Platform SSO" row | mitigate | X1 edits the line-38 cell to back-reference `#authentication`, so the higher-in-file PSSO statement is no longer a stale link-less duplicate but routes the reader to the full Authentication section; the hybrid-join anti-feature gets its own explicit row (never silently omitted, REQUIREMENTS:83) so readers are not misled by omission. |
| T-79-05 | Tampering (process-order violation -- content committed before the mandatory anchor inventory) | Git commit ordering | mitigate | The anchor inventory is committed ALONE as commit 1 before any content edit (ROADMAP hard prerequisite, RESEARCH Pitfall 1); Task 1 commits it alone, Task 2 stages-but-does-not-commit the matrix, Task 3 commits both content files as commit 2; `git log --oneline` order confirms the inventory commit precedes the content commit. |

No package installs in this phase (documentation only) -> no `T-79-SC` supply-chain checkpoint required. No high-severity unmitigated threats remain. Security enforcement (ASVS L1) is satisfied: this is a content phase that summarizes -- and does not implement -- authentication mechanisms; the live security domain (RESEARCH "Security Domain") confirms no V2-V6 ASVS categories apply.
</threat_model>

<verification>
- `node scripts/validation/v1.8-milestone-audit.mjs` PASSES after commit 2 (C12 + C13 green): C12 sees the six named H2s present in the comparison doc, the macOS SSO cell carrying a valid `[text](link)`, and the non-macOS cells exempt as uppercase `N/A`; C13's 15-entry allowlist shape is unchanged and the `#authentication` target is present in the same commit.
- Anchor inventory committed FIRST: `git log --oneline -- .planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-ANCHOR-INVENTORY.md` shows commit 1, and `git log --oneline` confirms it precedes the content commit (commit 2).
- Atomic content commit: `git diff HEAD~1 --name-only` after commit 2 shows BOTH `docs/reference/macos-capability-matrix.md` and `docs/reference/4-platform-capability-comparison.md` changed together (not split).
- Anchor superset / stability: `grep -n "^## Configuration" docs/reference/macos-capability-matrix.md` still resolves (heading untouched); `grep "^## " docs/reference/macos-capability-matrix.md` shows the 8 pre-edit H2s plus `## Authentication`; `grep "^## " docs/reference/4-platform-capability-comparison.md` shows the 8 pre-edit H2s plus `## Single Sign-On`.
- X1 / X2 / X3 grep assertions:
  - X1: `grep "Yes (macOS 14+ via Settings Catalog) — see \[Authentication\](#authentication)" docs/reference/macos-capability-matrix.md`
  - X3: `grep "Supported (macOS 14+) — \[matrix\](macos-capability-matrix.md#authentication)" docs/reference/4-platform-capability-comparison.md`
  - X2: the four non-macOS SSO cells are bare uppercase `N/A` (visual/parse on the `OS-integrated SSO` row).
- SC1 7-row coverage: matrix `## Authentication` table has 7 rows incl. a "Hybrid Entra join" row whose macOS cell contains "Not supported"; each Windows cell is `n/a — not covered in this matrix`.
- All three task verify commands print their OK sentinels (`INVENTORY_OK`, `MATRIX_OK`, `COMPARISON_OK`).
</verification>

<success_criteria>
- SSOREF-02 / SC1: `docs/reference/macos-capability-matrix.md` has a new `## Authentication` section (inserted before `## See Also`, preserving all existing anchor offsets) with 7 rows -- auth methods (Secure Enclave key / Password sync / Smart card), hardware gate (T2 / Apple Silicon for Secure Enclave), macOS version floor (14.0 recommended), Entra ID licensing (no P1/P2 for PSSO itself), NUAL (macOS 14+), Passkey/FIDO2 (Secure Enclave only), and hybrid Entra join (NOT SUPPORTED -- explicit anti-feature) -- macOS cells link guide 08/09 (link-not-copy), Windows cells are terse scope markers; the `## Configuration` Platform SSO row (line 38) back-references `#authentication` (X1).
- SSOREF-02 / SC2: `docs/reference/4-platform-capability-comparison.md` macOS Platform SSO cell is `Supported (macOS 14+) — [matrix](macos-capability-matrix.md#authentication)` (X3 SC2-verbatim) within a new `## Single Sign-On` section; non-macOS cells are bare `N/A` (X2); a pre-edit anchor-inventory artifact (`79-ANCHOR-INVENTORY.md`) is committed BEFORE any matrix edit (D-04).
- Harness: `node scripts/validation/v1.8-milestone-audit.mjs` passes (C12 + C13 green); the matrix + comparison edits are in ONE commit; the anchor-inventory is in a prior separate commit; no existing H2 slug renamed/removed (post-edit superset holds).
</success_criteria>

<output>
Create `.planning/phases/79-reference-integration-capability-matrix-5-platform-compariso/79-01-SUMMARY.md` when done.
</output>
