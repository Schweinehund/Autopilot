---
phase: 86-chain-health-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validation/check-phase-58.mjs
  - scripts/validation/check-phase-59.mjs
  - scripts/validation/check-phase-72.mjs
  - scripts/validation/check-phase-73.mjs
  - scripts/validation/check-phase-74.mjs
  - scripts/validation/check-phase-82.mjs
autonomous: true
requirements: [CHAIN-01, CHAIN-02]

must_haves:
  truths:
    - "check-phase-58.mjs standalone exits 0 with 0 FAIL / 0 SKIPPED (V-58-10 frozen-aware at ba2cbc0) — D-01"
    - "check-phase-59.mjs standalone exits 0 with 0 FAIL / 0 SKIPPED (V-59-24 frozen-aware at ba2cbc0) — D-01"
    - "check-phase-73.mjs standalone exits 0 with 0 FAIL / 0 SKIPPED (V-73-INVENTORY + V-73-AUDIT archive-aware, v1.8-phases) — D-02, D-03"
    - "check-phase-72.mjs standalone reports 0 SKIPPED for V-72-AUDIT-VERIFY (archive-aware, v1.8-phases) — D-03"
    - "check-phase-74.mjs standalone reports 0 SKIPPED for V-74-AUDIT (archive-aware, v1.8-phases) — D-03 (4th read found in RESEARCH)"
    - "check-phase-82.mjs standalone V-82-AUDIT no longer SKIPPED (archive-aware, v1.9-phases) — D-03"
    - "cascade validators check-phase-{60,61,62,63,64,65,66}.mjs are byte-unchanged (git diff empty) — D-04"
    - "CHAIN_SKIP remains new Set([]) in all edited files; CHECK_PHASE_NESTED guard intact — D-04, SC#1"
  artifacts:
    - path: "scripts/validation/check-phase-58.mjs"
      provides: "V-58-10 reads comparison doc at frozen SHA ba2cbc0"
      contains: "readAtV15Close"
    - path: "scripts/validation/check-phase-59.mjs"
      provides: "V-59-24 reads glossaries at frozen SHA ba2cbc0"
      contains: "readAtV15Close"
    - path: "scripts/validation/check-phase-72.mjs"
      provides: "V-72-AUDIT-VERIFY archive-aware read"
      contains: "resolveArchivedPhasePath"
    - path: "scripts/validation/check-phase-73.mjs"
      provides: "V-73-INVENTORY + V-73-AUDIT archive-aware reads"
      contains: "resolveArchivedPhasePath"
    - path: "scripts/validation/check-phase-74.mjs"
      provides: "V-74-AUDIT archive-aware read"
      contains: "resolveArchivedPhasePath"
    - path: "scripts/validation/check-phase-82.mjs"
      provides: "V-82-AUDIT archive-aware read (apex)"
      contains: "resolveArchivedPhasePath"
  key_links:
    - from: "scripts/validation/check-phase-58.mjs"
      to: "scripts/validation/_lib/frozen-at-close.mjs"
      via: "import { readAtV15Close }"
      pattern: "import.*readAtV15Close.*frozen-at-close"
    - from: "scripts/validation/check-phase-73.mjs"
      to: "scripts/validation/_lib/archive-path.mjs"
      via: "import { resolveArchivedPhasePath }"
      pattern: "import.*resolveArchivedPhasePath.*archive-path"
    - from: "scripts/validation/check-phase-82.mjs"
      to: "scripts/validation/_lib/archive-path.mjs"
      via: "import { resolveArchivedPhasePath } with ['v1.9-phases']"
      pattern: "resolveArchivedPhasePath\\([^)]*v1\\.9-phases"
---

<objective>
Convert the legacy HEAD-coupled validator assertions in six chain validators to deterministic frozen-aware / archive-aware reads, so each edited validator passes standalone with 0 FAIL / 0 SKIPPED and the 7 cascade validators (60-66) auto-clear with zero edits.

This is a pure tooling pass. All four design decisions (D-01..D-05) are LOCKED via adversarial-review; RESEARCH.md and PATTERNS.md supply exact edit shapes, line numbers, helper names, and analogs. Do NOT re-decide — translate the locked decisions into surgical edits.

Two conversion patterns, each applied to a disjoint file set:
- **D-01 frozen-aware** (`readAtV15Close` from `_lib/frozen-at-close.mjs`): check-phase-58 (V-58-10 only), check-phase-59 (V-59-24 only). SHA `ba2cbc0` (V15) is already published — no new SHA entry.
- **D-02 / D-03 archive-aware** (`resolveArchivedPhasePath` from `_lib/archive-path.mjs`): check-phase-72 (V-72-AUDIT-VERIFY), check-phase-73 (V-73-INVENTORY + V-73-AUDIT), check-phase-74 (V-74-AUDIT), check-phase-82 (V-82-AUDIT). For each archive-aware read, REPLACE the `{ pass: true, skipped: true }` SKIP-PASS sentinel with `{ pass: false }` FAIL-if-not-found.

Purpose: resolve CHAIN-01 (10 legacy FAILs cleared, no CHAIN_SKIP masking) and lay the source-edit foundation CHAIN-02 verifies in Plan 86-02.
Output: six edited validator files; cascade 60-66 untouched; CHAIN_SKIP still `new Set([])`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-chain-health-pass/86-CONTEXT.md
@.planning/phases/86-chain-health-pass/86-RESEARCH.md
@.planning/phases/86-chain-health-pass/86-PATTERNS.md

# Helpers consumed (read signatures before editing)
@scripts/validation/_lib/frozen-at-close.mjs
@scripts/validation/_lib/archive-path.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-01 frozen-aware conversion of V-58-10 and V-59-24</name>
  <files>scripts/validation/check-phase-58.mjs, scripts/validation/check-phase-59.mjs</files>
  <read_first>
    - scripts/validation/check-phase-58.mjs (target: imports lines 11-13; V-58-10 at line 197-200, readFile(COMPARISON_DOC) at line 199)
    - scripts/validation/check-phase-59.mjs (target: imports lines 11-13; V-59-24 at line 644, readFile(f) loop at line 647)
    - scripts/validation/check-phase-61.mjs (ANALOG: import at line 20; readAtV15Close call + name suffix at lines 288-295)
    - scripts/validation/_lib/frozen-at-close.mjs (HELPER: readAtV15Close = readAtClose('V15', p); V15 = 'ba2cbc0'; throws on git error)
    - 86-PATTERNS.md §"check-phase-58.mjs" and §"check-phase-59.mjs" (exact before/after shapes)
  </read_first>
  <action>
    In check-phase-58.mjs: add `import { readAtV15Close } from './_lib/frozen-at-close.mjs';` after the existing `node:process` import. In the V-58-10 check (id: 10, line 197) ONLY, replace `const c = readFile(COMPARISON_DOC);` with `const c = readAtV15Close(COMPARISON_DOC);` and delete the immediately-following `if (c === null) return { pass: false, ... }` null-guard line (readAtV15Close throws on git error — let it propagate). Append the exact suffix ` [v1.5-frozen @ ba2cbc0]` to the V-58-10 `name` string. Do NOT touch V-58-01..09 or V-58-11..26 — those keep `readFile()` against live files.

    In check-phase-59.mjs: add the same `import { readAtV15Close } from './_lib/frozen-at-close.mjs';` import. In the V-59-24 check (id: 24, line 644) ONLY, inside the `for (const f of ALL_GLOSSARIES)` loop replace `const c = readFile(f);` with `const c = readAtV15Close(f);` and remove the `if (c === null) continue;` guard line that follows it. Append ` [v1.5-frozen @ ba2cbc0]` to the V-59-24 `name` string. Do NOT touch V-59-20/21/22/23 — they assert current-state structure and must stay live (Pitfall 6).

    Do NOT add or expand any CHAIN_SKIP entry. Do NOT edit any other validator file in this task.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-58.mjs && node scripts/validation/check-phase-59.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-58.mjs` exits 0 and stdout shows "0 FAIL" / "0 SKIPPED" (RESEARCH target ~26 PASS / 0 / 0)
    - `node scripts/validation/check-phase-59.mjs` exits 0 and stdout shows "0 FAIL" / "0 SKIPPED" (RESEARCH target ~36 PASS / 0 / 0)
    - `grep -c "readAtV15Close" scripts/validation/check-phase-58.mjs` >= 2 (import + call); same for check-phase-59.mjs
    - both files contain the literal suffix `[v1.5-frozen @ ba2cbc0]` exactly once each (on V-58-10 / V-59-24 name)
    - `grep -c "new Set(\[\])" scripts/validation/check-phase-58.mjs` unchanged from pre-edit (CHAIN_SKIP empty)
    - no `readAtV15Close` appears on any V-58 or V-59 check other than V-58-10 / V-59-24
  </acceptance_criteria>
  <done>V-58-10 and V-59-24 read at frozen SHA ba2cbc0 with the [v1.5-frozen @ ba2cbc0] name suffix; both validators pass standalone 0 FAIL / 0 SKIPPED; no other assertions altered; CHAIN_SKIP untouched.</done>
</task>

<task type="auto">
  <name>Task 2: D-02 + D-03 archive-aware conversion of V-72/73/74/82 VERIFICATION + V-73-INVENTORY reads</name>
  <files>scripts/validation/check-phase-72.mjs, scripts/validation/check-phase-73.mjs, scripts/validation/check-phase-74.mjs, scripts/validation/check-phase-82.mjs</files>
  <read_first>
    - scripts/validation/check-phase-72.mjs (target: V-72-AUDIT-VERIFY id 'AUDIT-VERIFY' at line 109-117; readFile path at line 112; SKIP-PASS sentinel at line 113)
    - scripts/validation/check-phase-73.mjs (target: V-73-INVENTORY id 'INVENTORY' at line 73-82, path at line 76; V-73-AUDIT id 'AUDIT' at line 166, SKIP-PASS sentinel below it)
    - scripts/validation/check-phase-74.mjs (target: V-74-AUDIT id 'AUDIT' at line 107-115; readFile path at line 110; SKIP-PASS sentinel at line 111)
    - scripts/validation/check-phase-82.mjs (target: V-82-AUDIT id 'AUDIT' at line 49-57; readFile path at line 52; SKIP-PASS sentinel at line 53)
    - scripts/validation/check-phase-62.mjs (ANALOG: resolveArchivedPhasePath import at line 22, explicit milestoneRoots call at line 42)
    - scripts/validation/check-phase-63.mjs (ANALOG: same pattern at lines 25/49)
    - scripts/validation/_lib/archive-path.mjs (HELPER: resolveArchivedPhasePath(phaseSuffix, milestoneRoots) returns string or null; non-throwing; caller owns FAIL)
    - 86-PATTERNS.md §"check-phase-72/73/74/82" (exact before/after) and §"milestoneRoots Reference Table"
  </read_first>
  <action>
    Add `import { resolveArchivedPhasePath } from './_lib/archive-path.mjs';` to the import block of EACH of the four files (after the existing node-builtin imports). Apply the canonical null-guard pattern at each target call-site: resolve the path with `resolveArchivedPhasePath(phaseSuffix, milestoneRoots)`, then `const verif = verifPath ? readFile(verifPath) : null;`, then REPLACE the existing `if (!verif) return { pass: true, skipped: true, ... }` SKIP-PASS sentinel with `if (!verif) return { pass: false, detail: '... not found in live tree or <milestone> archive' };`. Keep every downstream heading/row assertion unchanged.

    Exact phaseSuffix + milestoneRoots per call-site:
    - check-phase-72 V-72-AUDIT-VERIFY (line 112): suffix `72-chain-wrapper-hardening-pillar-b/72-VERIFICATION.md`, roots `['v1.8-phases']`. Keep the `Per-Validator Audit Inventory` heading check.
    - check-phase-73 V-73-INVENTORY (line 76): suffix `73-retrospective-forward-port-pillar-c/73-RETRO-INVENTORY.md`, roots `['v1.8-phases']`. Keep the `>= 19` row-count check; on null return `{ pass: false, detail: '73-RETRO-INVENTORY.md missing (not in live tree or v1.8-phases archive)' }` (this is already a FAIL path, not SKIP-PASS — just route it through the resolver).
    - check-phase-73 V-73-AUDIT (line ~169): suffix `73-retrospective-forward-port-pillar-c/73-VERIFICATION.md`, roots `['v1.8-phases']`. REPLACE SKIP-PASS with FAIL. Keep the `/Phase 73/i` heading check.
    - check-phase-74 V-74-AUDIT (line 110): suffix `74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-VERIFICATION.md`, roots `['v1.8-phases']`. REPLACE SKIP-PASS with FAIL. Keep the `/Phase 74/i` check.
    - check-phase-82 V-82-AUDIT (line 52): suffix `82-harness-lineage-bump-terminal-re-audit-milestone-close/82-VERIFICATION.md`, roots `['v1.9-phases']` — CRITICAL, NOT v1.8-phases (Pitfall 1; phase 82 closed in v1.9). REPLACE SKIP-PASS with FAIL. Keep the `/Phase 82/i` check.

    Do NOT import `readAtV15Close` into any of these four files (Pitfall 2 — frozen-aware is only 58/59). Do NOT add or expand CHAIN_SKIP. Do NOT touch cascade validators 60-66.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-72.mjs; node scripts/validation/check-phase-73.mjs; node scripts/validation/check-phase-74.mjs; node scripts/validation/check-phase-82.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-73.mjs` exits 0 with "0 FAIL" and "0 SKIPPED" (V-73-INVENTORY PASS via archive, V-73-AUDIT PASS via archive)
    - `node scripts/validation/check-phase-72.mjs` stdout shows "0 SKIPPED" (V-72-AUDIT-VERIFY now PASS, not SKIPPED)
    - `node scripts/validation/check-phase-74.mjs` stdout shows "0 SKIPPED" (V-74-AUDIT now PASS, not SKIPPED)
    - `node scripts/validation/check-phase-82.mjs` standalone shows "0 SKIPPED" (V-82-AUDIT now PASS — the only apex-level SKIPPED is cleared)
    - each of the four files contains `resolveArchivedPhasePath` (grep matches in 72, 73, 74, 82)
    - check-phase-82.mjs contains `['v1.9-phases']` AND does NOT pass `['v1.8-phases']` to the V-82-AUDIT resolver
    - zero `skipped: true` sentinels remain on V-72-AUDIT-VERIFY / V-73-AUDIT / V-74-AUDIT / V-82-AUDIT (grep the four assertion bodies — SKIP-PASS replaced with FAIL)
    - none of 72/73/74/82 import `readAtV15Close`
  </acceptance_criteria>
  <done>All four archive-aware reads resolve via resolveArchivedPhasePath with correct milestoneRoots (v1.8 for 72/73/74, v1.9 for 82); every SKIP-PASS sentinel on these VERIFICATION reads is replaced with FAIL; V-73-INVENTORY resolves the archived inventory; all four validators report 0 SKIPPED; check-phase-73 passes 0 FAIL / 0 SKIPPED standalone.</done>
</task>

<task type="auto">
  <name>Task 3: Verify cascade-untouched + CHAIN_SKIP invariants, then commit all six edits atomically</name>
  <files>scripts/validation/check-phase-58.mjs, scripts/validation/check-phase-59.mjs, scripts/validation/check-phase-72.mjs, scripts/validation/check-phase-73.mjs, scripts/validation/check-phase-74.mjs, scripts/validation/check-phase-82.mjs</files>
  <read_first>
    - 86-CONTEXT.md §"D-04 (cascade)" (cascade 60-66 ZERO edits; CHAIN_SKIP stays Set([]); keep NESTED guard)
    - 86-RESEARCH.md §"Cascade Zero-Edit Confirmation" and §"Sequencing: Wave/Plan Structure" (one atomic commit recommended)
    - scripts/validation/check-phase-82.mjs (V-82-SELF at line 128 already asserts CHAIN_SKIP.size === 0 — built-in guardrail)
  </read_first>
  <action>
    Verify the cascade-untouched and CHAIN_SKIP invariants BEFORE committing. Confirm via `git diff --stat` that ONLY the six target files (check-phase-{58,59,72,73,74,82}.mjs) appear and that check-phase-{60,61,62,63,64,65,66}.mjs are absent from the diff. Confirm `new Set([])` (empty CHAIN_SKIP) is still present in every file that declares CHAIN_SKIP and that no numeric entries were added. Confirm the `CHECK_PHASE_NESTED` recursion guard line is intact (unchanged) in check-phase-82.mjs.

    Commit Discretion (D-decision, per CONTEXT.md "Claude's Discretion" + RESEARCH §Sequencing): land all six edits as ONE atomic commit. Rationale: the six files are mutually independent but the apex only reaches the target tally when ALL are in place; a single commit prevents misleading partial-state apex runs (Pitfall 7) and keeps the cascade-untouched invariant auditable in one diff. Commit message: `fix(86-01): convert HEAD-coupled chain assertions to frozen/archive-aware reads (CHAIN-01)`.

    Do NOT run the full apex here (that is Plan 86-02's job and takes ~7-10 min). Standalone per-file verification from Tasks 1-2 is sufficient for this plan's gate.
  </action>
  <verify>
    <automated>git diff --stat | grep -E "check-phase-(60|61|62|63|64|65|66)\.mjs" && echo "CASCADE_TOUCHED_FAIL" || echo "CASCADE_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat` (pre-commit) lists exactly the six files check-phase-{58,59,72,73,74,82}.mjs and NO others under scripts/validation/
    - the cascade-clean verify command prints `CASCADE_CLEAN` (no 60-66 file in the diff)
    - `grep -rn "new Set(\[" scripts/validation/check-phase-{58,59,72,73,74,82}.mjs` shows only `new Set([])` (no entries added)
    - `grep -c "CHECK_PHASE_NESTED" scripts/validation/check-phase-82.mjs` unchanged from pre-edit
    - after commit, `git log -1 --name-only` shows the six files and the message contains `CHAIN-01`
  </acceptance_criteria>
  <done>git diff confirms cascade 60-66 untouched and CHAIN_SKIP empty across all edited files; NESTED guard intact; all six edits committed in one atomic commit referencing CHAIN-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none at runtime) | Tooling-only change. No network, no auth, no user input, no data handling. Edits are to local Node validator scripts executed by the developer/CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering | Validator correctness (false-PASS masking real drift) | mitigate | Frozen-SHA determinism (readAtV15Close @ ba2cbc0 is a fixed historical constant) + archive-aware reads resolve to known-existing files; SKIP-PASS replaced with FAIL so a missing archive surfaces as red, not green. Cross-OS EXACT-MATCH proof in Plan 86-02 catches OS-divergent behavior. |
| T-86-02 | Tampering | Cascade integrity (accidental edit to 60-66 breaks byte-invariant) | mitigate | Task 3 git diff --stat gate asserts cascade 60-66 byte-unchanged before commit. |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH §Package Legitimacy Audit omitted (no external packages). No supply-chain surface. |

Runtime attack surface is essentially nil; the only real risk is a false-PASS validator masking real drift, mitigated by frozen-SHA determinism + archive resolution + SKIP→FAIL conversion + cross-OS exact-match (Plan 86-02). Block on: high severity (none present).
</threat_model>

<verification>
After all three tasks, each edited validator must pass standalone:
- `node scripts/validation/check-phase-58.mjs` → 0 FAIL / 0 SKIPPED
- `node scripts/validation/check-phase-59.mjs` → 0 FAIL / 0 SKIPPED
- `node scripts/validation/check-phase-72.mjs` → 0 SKIPPED
- `node scripts/validation/check-phase-73.mjs` → 0 FAIL / 0 SKIPPED
- `node scripts/validation/check-phase-74.mjs` → 0 SKIPPED
- `node scripts/validation/check-phase-82.mjs` (standalone, not --verbose) → 0 SKIPPED for V-82-AUDIT

Invariants (Task 3 gate):
- `git diff --stat` shows only the six target files under scripts/validation/; cascade 60-66 absent
- CHAIN_SKIP is `new Set([])` everywhere; CHECK_PHASE_NESTED guard intact
</verification>

<success_criteria>
- CHAIN-01 source edits complete: V-58-10 + V-59-24 frozen-aware (ba2cbc0); V-72-AUDIT-VERIFY, V-73-INVENTORY, V-73-AUDIT, V-74-AUDIT, V-82-AUDIT archive-aware with SKIP-PASS→FAIL
- All six edited validators pass standalone with 0 FAIL and 0 SKIPPED (74/72 specifically 0 SKIPPED on their AUDIT reads)
- Cascade validators 60-66 byte-unchanged (auto-clear deferred to apex run in 86-02)
- CHAIN_SKIP remains `new Set([])` (SC#1); NESTED recursion guard intact
- All edits committed in one atomic commit referencing CHAIN-01
</success_criteria>

<output>
Create `.planning/phases/86-chain-health-pass/86-01-SUMMARY.md` when done.
</output>
