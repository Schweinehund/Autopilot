---
phase: 86-chain-health-pass
plan: 02
type: execute
wave: 2
depends_on: ["86-01"]
files_modified: []
autonomous: true
requirements: [CHAIN-01, CHAIN-02]

must_haves:
  truths:
    - "Apex check-phase-82.mjs --verbose exits 0 with 0 FAIL / 0 SKIPPED on Windows local (target 37 PASS / 0 / 0) — SC#3"
    - "Cascade validators 60-66 PASS inside the apex run with zero edits (auto-cleared once 58/59 exit 0) — D-04, CHAIN-01"
    - "Linux GHA linux-chain-ubuntu-latest job exits 0 (run conclusion = success) — SC#4"
    - "Windows and Linux apex PASS/FAIL/SKIP tallies are an EXACT MATCH — SC#4 cross-OS"
  artifacts:
    - path: ".planning/phases/86-chain-health-pass/86-02-SUMMARY.md"
      provides: "Recorded Windows apex tally + GHA run id/URL/conclusion + cross-OS match evidence"
      min_lines: 20
  key_links:
    - from: "scripts/validation/check-phase-82.mjs"
      to: "cascade + archive/frozen reads from 86-01"
      via: "recursive subprocess chain-guards (CHAIN_PHASES 48..81)"
      pattern: "check-phase-82"
    - from: ".github/workflows/audit-harness-v1.9-integrity.yml"
      to: "linux-chain-ubuntu-latest job (node check-phase-82.mjs --verbose)"
      via: "workflow_dispatch"
      pattern: "linux-chain-ubuntu-latest"
---

<objective>
Prove the full validator chain exits 0 FAIL / 0 SKIPPED on BOTH Windows local and Linux GHA, with cross-OS EXACT MATCH — the CHAIN-02 deliverable. This plan adds no source edits; it runs the apex locally, dispatches the existing v1.9 GHA workflow, and records cross-OS evidence.

D-05 (LOCKED): obtain the Linux proof by `workflow_dispatch` on the EXISTING `.github/workflows/audit-harness-v1.9-integrity.yml` — its `linux-chain-ubuntu-latest` job already runs apex `check-phase-82.mjs --verbose` with `core.autocrlf false`, `fetch-depth: 0`, `continue-on-error: false`. Do NOT author a throwaway workflow. Do NOT defer cross-OS proof to Phase 88.

Depends on 86-01: all six source edits must be committed and pushed before the apex run and GHA dispatch — partial state makes the tally meaningless (Pitfall 7).

Purpose: close CHAIN-02 (full chain 0 FAIL / 0 SKIPPED on Windows + Linux EXACT MATCH) and confirm the cascade 60-66 auto-clear empirically.
Output: 86-02-SUMMARY.md recording the Windows apex tally, the GHA run id/URL/conclusion, and the cross-OS exact-match comparison.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-chain-health-pass/86-CONTEXT.md
@.planning/phases/86-chain-health-pass/86-RESEARCH.md

# Cross-OS proof vehicle
@.github/workflows/audit-harness-v1.9-integrity.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full apex on Windows local and confirm 0 FAIL / 0 SKIPPED with cascade auto-clear</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - 86-RESEARCH.md §"Definitive SKIPPED Set at Apex" (target 37 PASS / 0 FAIL / 0 SKIPPED; current pre-edit 26/10/1) and §"Specific Ideas" open verification item (enumerate the EXACT final SKIPPED set — do not assume the three known reads were the complete set)
    - scripts/validation/check-phase-82.mjs (apex; CHAIN_PHASES 48..81; spawns each child via execFileSync; --verbose enumerates per-child results)
    - .planning/phases/86-chain-health-pass/86-01-SUMMARY.md (confirm all six edits committed before running apex)
  </read_first>
  <action>
    Confirm 86-01's six edits are committed (check `git log` / 86-01-SUMMARY.md) before running. Run the apex in full mode: `node scripts/validation/check-phase-82.mjs --verbose` (≈7-10 min — it recursively spawns the 48..81 chain). Capture the final tally line and the full per-validator breakdown.

    Enumerate the EXACT final SKIPPED set from the --verbose output (the Referee flagged apex-completeness as MEDIUM confidence — RESEARCH §Specific Ideas). If ANY SKIPPED remains beyond the four AUDIT reads already converted in 86-01, STOP and report it as a gap (do not mask with CHAIN_SKIP) — it likely needs the same archive-aware treatment and must route back to a gap-closure plan rather than be suppressed.

    Confirm the cascade auto-clear: in the --verbose breakdown, the V-82-CHAIN-{60,61,62,63,64,65,66} chain-guards must each report PASS even though 60-66 were never edited (D-04). Record the Windows tally verbatim for the cross-OS comparison in Task 2.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-82.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-82.mjs --verbose` exits 0 (echo $? / $LASTEXITCODE = 0)
    - final tally shows "0 FAIL" AND "0 SKIPPED" (RESEARCH target: 37 PASS / 0 FAIL / 0 SKIPPED)
    - --verbose breakdown shows V-82-CHAIN-60 through V-82-CHAIN-66 all PASS (cascade auto-cleared, zero edits)
    - the enumerated SKIPPED set is empty; if non-empty, the run is treated as FAILED and the residual SKIPPED is reported as a gap (not suppressed)
    - Windows tally string is captured verbatim into 86-02-SUMMARY.md for cross-OS comparison
  </acceptance_criteria>
  <done>Apex check-phase-82.mjs --verbose exits 0 with 0 FAIL / 0 SKIPPED on Windows; the complete SKIPPED set is enumerated and empty; cascade 60-66 confirmed PASS with zero edits; Windows tally recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch v1.9 GHA workflow, watch linux-chain job, confirm Linux exit 0 + cross-OS EXACT MATCH</name>
  <files>.planning/phases/86-chain-health-pass/86-02-SUMMARY.md</files>
  <read_first>
    - .github/workflows/audit-harness-v1.9-integrity.yml (workflow_dispatch at line 26; linux-chain-ubuntu-latest at line 74; runs `node scripts/validation/check-phase-82.mjs --verbose` at line 87-90; needs: harness-run; continue-on-error:false; fetch-depth:0; autocrlf false)
    - 86-CONTEXT.md §"D-05" (dispatch existing workflow; do NOT author a throwaway)
    - 86-RESEARCH.md §"Assumptions Log" A2 (if path-filter gating blocks dispatch, use GitHub web UI — workflow_dispatch has no required inputs) and §"Environment Availability" (gh CLI available)
    - Task 1's recorded Windows tally (the EXACT MATCH reference)
  </read_first>
  <action>
    Ensure 86-01's commit is pushed to the remote default branch (the linux-chain job checks out the pushed ref). Dispatch the existing workflow with the gh CLI: `gh workflow run audit-harness-v1.9-integrity.yml`. Capture the run id (e.g. `gh run list --workflow=audit-harness-v1.9-integrity.yml --limit 1`), then watch it: `gh run watch <run-id> --exit-status` (or `gh run view <run-id> --log` after completion). If the gh dispatch fails on path-filter gating, fall back to the GitHub web UI Actions tab → "Audit Harness v1.9 Integrity" → Run workflow (A2 fallback).

    Read the `linux-chain-ubuntu-latest` job log: extract the apex tally printed by `node scripts/validation/check-phase-82.mjs --verbose` and the `CHAIN_TIMING_LINUX` notice. Compare the Linux PASS/FAIL/SKIP tally to the Windows tally from Task 1 — they MUST be an EXACT MATCH (same PASS count, 0 FAIL, 0 SKIPPED on both).

    Write 86-02-SUMMARY.md recording: the GHA run id + URL, the run conclusion, the linux-chain job conclusion, the Linux apex tally, the Windows apex tally from Task 1, and an explicit EXACT-MATCH confirmation. Do NOT author, edit, or add any workflow file.
  </action>
  <verify>
    <automated>gh run list --workflow=audit-harness-v1.9-integrity.yml --limit 1 --json conclusion,databaseId,url</automated>
  </verify>
  <acceptance_criteria>
    - `gh workflow run audit-harness-v1.9-integrity.yml` dispatches a run (or web-UI fallback used and documented)
    - `gh run watch <run-id> --exit-status` returns 0 (or `gh run list ... --json conclusion` shows `"conclusion":"success"`)
    - the `linux-chain-ubuntu-latest` job conclusion is `success` (continue-on-error:false means a red job fails the run)
    - Linux apex tally extracted from the job log shows 0 FAIL / 0 SKIPPED matching RESEARCH target (37 PASS)
    - Windows tally (Task 1) and Linux tally are byte-identical in PASS/FAIL/SKIP counts — recorded as EXACT MATCH in 86-02-SUMMARY.md
    - no workflow file appears in `git status` (existing workflow dispatched, none authored/edited)
    - 86-02-SUMMARY.md contains the run id, URL, conclusion, both tallies, and the EXACT-MATCH statement
  </acceptance_criteria>
  <done>The existing v1.9 integrity workflow was dispatched (not authored); the linux-chain-ubuntu-latest job concluded success with 0 FAIL / 0 SKIPPED; Windows and Linux apex tallies are an exact match; evidence (run id, URL, conclusion, both tallies) recorded in 86-02-SUMMARY.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none at runtime) | Verification-only plan. No source edits, no network endpoints authored, no auth/data handling. GHA dispatch runs an existing, already-reviewed workflow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03 | Tampering | False cross-OS PASS (OS-divergent validator behavior masked) | mitigate | linux-chain job uses fetch-depth:0 + autocrlf:false + continue-on-error:false; explicit EXACT-MATCH of PASS/FAIL/SKIP tallies between Windows and Linux catches CRLF / path-separator divergence. |
| T-86-04 | Repudiation | Unverifiable claim of green Linux run | mitigate | 86-02-SUMMARY.md records the concrete gh run id, URL, and conclusion — auditable, not a bare assertion. |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. No supply-chain surface. |

Verification-only; no runtime attack surface. Block on: high severity (none present).
</threat_model>

<verification>
- Windows: `node scripts/validation/check-phase-82.mjs --verbose` → exit 0, 0 FAIL / 0 SKIPPED, cascade 60-66 PASS
- Linux GHA: `gh run watch <id> --exit-status` → 0; `linux-chain-ubuntu-latest` conclusion `success`; apex log 0 FAIL / 0 SKIPPED
- Cross-OS: Windows tally == Linux tally (PASS/FAIL/SKIP exact match), recorded in 86-02-SUMMARY.md
</verification>

<success_criteria>
- CHAIN-02 satisfied: full validator chain (48..82) exits 0 FAIL / 0 SKIPPED on Windows local (SC#3) and Linux GHA (SC#4)
- Cross-OS EXACT MATCH confirmed and evidenced (run id, URL, conclusion, both tallies)
- Cascade validators 60-66 confirmed auto-cleared with zero edits inside the apex run
- GHA proof obtained via dispatch of the existing v1.9 workflow (no throwaway workflow authored)
- 86-02-SUMMARY.md records all cross-OS evidence
</success_criteria>

<output>
Create `.planning/phases/86-chain-health-pass/86-02-SUMMARY.md` when done (must include GHA run id, URL, conclusion, Windows + Linux apex tallies, and the EXACT-MATCH confirmation).
</output>
