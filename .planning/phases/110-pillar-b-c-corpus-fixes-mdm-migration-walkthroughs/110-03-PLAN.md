---
phase: 110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [docs/macos-lifecycle/02-mdm-migration-psso.md]
autonomous: true
requirements: [MIGF-02]

must_haves:
  truths:
    - "ONE combined appendix is appended to docs/macos-lifecycle/02-mdm-migration-psso.md (after the Version History), with two H3 subsections `### Jamf Pro` and `### Mosyle` (per D-03)"
    - "Each vendor H3 covers all three sub-steps: FileVault key retrieval, Activation Lock bypass, device-record deletion (per MIGF-02)"
    - "Depth is conceptual-action + 'verify current console labels on your authoring day' hedge — no invented literal click-paths (per D-03)"
    - "Headings slug clean: bare `### Jamf Pro` / `### Mosyle` and an H2 with no ` / ` or parentheses in the slug-generating part (double-hyphen trap avoided, per D-03)"
    - "Callouts reuse the file's own > **Important:** / > **Note:** inline style, NOT Phase-109 NOTE/WARNING/DANGER/CRITICAL boxes (per D-03)"
  artifacts:
    - path: "docs/macos-lifecycle/02-mdm-migration-psso.md"
      provides: "Jamf Pro + Mosyle source-MDM release addendum (MIGF-02)"
      contains: "## Appendix: Source-MDM Release Steps for Jamf Pro and Mosyle"
  key_links:
    - from: "appendix preamble"
      to: "Stage 2 (Kandji/Iru source release) within the same file"
      via: "reference-not-re-author heading link (link-not-copy)"
      pattern: "Appendix: Source-MDM Release Steps"
---

<objective>
Append one combined appendix to the existing macOS migration walkthrough documenting Jamf Pro and Mosyle source-MDM release steps (MIGF-02), at conceptual-action depth with the file's established authoring-day hedge discipline.

Purpose: Let admins migrating from Jamf Pro or Mosyle (not just Kandji/Iru) follow the three source-release sub-steps — FileVault key retrieval, Activation Lock bypass, device-record deletion (SC5).
Output: One appended `## Appendix …` section with `### Jamf Pro` and `### Mosyle` H3s; no other part of the file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-CONTEXT.md
@.planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-RESEARCH.md
@.planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-PATTERNS.md
@docs/macos-lifecycle/02-mdm-migration-psso.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append appendix H2 + preamble + `### Jamf Pro` H3</name>
  <files>docs/macos-lifecycle/02-mdm-migration-psso.md</files>
  <read_first>
    - docs/macos-lifecycle/02-mdm-migration-psso.md — the whole file (append target is AFTER the Version History table, currently the last content ~line 564); Stage 2 (~:126-181) is the self-analog for the three-sub-step release structure + the `> **Important:**` pre-deletion warning (~:134) and `> **Note:**` login-gated-console hedge (~:152-153)
    - .planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-PATTERNS.md §2 (appendix heading slug-safety, structure, callout style, preamble text)
    - .planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-RESEARCH.md §"MIGF-02: Jamf Pro" (three sub-steps + hedge guidance) and Pitfall 6
  </read_first>
  <action>
    After the Version History table (end of file), append the appendix H2 heading exactly: "## Appendix: Source-MDM Release Steps for Jamf Pro and Mosyle" (no ` / `, no parentheses — slug must be clean). Add a short preamble paragraph connecting the appendix to Stage 2's Kandji/Iru steps: organizations migrating from Jamf Pro or Mosyle follow the same three-step sequencing (FileVault recovery key retrieval → Activation Lock bypass code retrieval → device-record deletion) at conceptual-action level; exact console paths differ; verify current console labels on your authoring day. Then add "### Jamf Pro" (bare heading) covering all three sub-steps: (1) FileVault recovery key retrieval, (2) Activation Lock bypass code retrieval, (3) device-record deletion/unmanage (~15-min MDM profile removal at next check-in). Include a `> **Important:**` pre-deletion warning stating FileVault recovery keys AND Activation Lock bypass codes are permanently destroyed on device-record deletion — retrieve BOTH first. Include a `> **Note:**` hedge that Jamf Pro console navigation is not live-verifiable without login; the conceptual action is the same; verify current labels on your authoring day. Use conceptual-action framing only — do NOT invent literal click-paths. Use the file's `> **Bold label:**` inline callout style, NOT NOTE/WARNING/DANGER/CRITICAL boxes.
  </action>
  <acceptance_criteria>
    - `grep -c "^## Appendix: Source-MDM Release Steps for Jamf Pro and Mosyle$" docs/macos-lifecycle/02-mdm-migration-psso.md` == 1 (H2 present, slug-clean — no ` / `, no parentheses).
    - `grep -c "^### Jamf Pro$" docs/macos-lifecycle/02-mdm-migration-psso.md` == 1.
    - The Jamf Pro subsection mentions FileVault recovery key retrieval, Activation Lock bypass code retrieval, and device-record deletion (all three sub-steps).
    - The appendix contains a `> **Important:**` and a `> **Note:**` callout; contains no "NOTE:" / "WARNING:" / "DANGER:" / "CRITICAL:" box syntax.
    - Contains an authoring-day hedge phrase (e.g. "on your authoring day").
    - Content above the appendix (Stages, See Also, Glossary QR, Version History) is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -qx "## Appendix: Source-MDM Release Steps for Jamf Pro and Mosyle" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -qx "### Jamf Pro" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -qi "on your authoring day" docs/macos-lifecycle/02-mdm-migration-psso.md && ! grep -qE "^(NOTE|WARNING|DANGER|CRITICAL):" docs/macos-lifecycle/02-mdm-migration-psso.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Append `### Mosyle` H3</name>
  <files>docs/macos-lifecycle/02-mdm-migration-psso.md</files>
  <read_first>
    - docs/macos-lifecycle/02-mdm-migration-psso.md — the appendix + `### Jamf Pro` H3 from Task 1 (append `### Mosyle` after it, still inside the same appendix H2)
    - .planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-RESEARCH.md §"MIGF-02: Mosyle" (three sub-steps; note two Activation Lock bypass codes per device — user-initiated + MDM-initiated; hedge guidance)
    - .planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-PATTERNS.md §2 (callout + hedge patterns)
  </read_first>
  <action>
    After the `### Jamf Pro` subsection, append "### Mosyle" (bare heading) under the same appendix H2, covering all three sub-steps for Mosyle: (1) FileVault recovery key retrieval (Mosyle escrows keys via its Security profile), (2) Activation Lock bypass code retrieval (note two codes may exist per device — user-initiated and MDM-initiated — retrieve both), (3) device-record deletion (~15-min MDM profile removal at next check-in). Mirror the same `> **Important:**` pre-deletion warning (retrieve FileVault key AND Activation Lock bypass codes BEFORE deletion — permanently destroyed after) and the same `> **Note:**` authoring-day hedge for Mosyle console navigation. Conceptual-action depth only; no invented literal click-paths. Same inline `> **Bold label:**` callout style.
  </action>
  <acceptance_criteria>
    - `grep -c "^### Mosyle$" docs/macos-lifecycle/02-mdm-migration-psso.md` == 1 (clean slug `mosyle`).
    - The Mosyle subsection mentions FileVault recovery key retrieval, Activation Lock bypass code retrieval (with the two-codes note), and device-record deletion.
    - `grep -c "^### Jamf Pro / Mosyle" docs/macos-lifecycle/02-mdm-migration-psso.md` == 0 (double-hyphen-trap heading NOT used).
    - Both `### Jamf Pro` and `### Mosyle` sit under the single `## Appendix: Source-MDM Release Steps for Jamf Pro and Mosyle` H2.
    - The Mosyle subsection includes a `> **Important:**` pre-deletion warning and an authoring-day hedge.
  </acceptance_criteria>
  <verify>
    <automated>grep -qx "### Mosyle" docs/macos-lifecycle/02-mdm-migration-psso.md && ! grep -q "### Jamf Pro / Mosyle" docs/macos-lifecycle/02-mdm-migration-psso.md && grep -qi "MDM-initiated" docs/macos-lifecycle/02-mdm-migration-psso.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → docs corpus | Only Markdown text is appended; no executable code, no runtime input, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-06 | Information (over-precise unverifiable steps) | Jamf Pro / Mosyle console paths | mitigate | Conceptual-action + authoring-day hedge required by acceptance criteria; no invented literal click-paths (RESEARCH MEDIUM confidence). |
| T-110-07 | Tampering (broken anchors) | Appendix + H3 headings | mitigate | Acceptance criteria assert slug-clean headings and reject the ` / ` double-hyphen-trap heading (Pitfall 6). |
</threat_model>

<verification>
- One appendix H2 with slug-clean title; two bare H3s `### Jamf Pro` / `### Mosyle`.
- Each vendor covers all three sub-steps (FileVault key / Activation Lock bypass / device-record deletion).
- Conceptual-action depth + authoring-day hedge; inline > **Important:** / > **Note:** callouts only.
- No content above the appendix changed.
</verification>

<success_criteria>
SC5 satisfied: an admin can follow source-MDM-specific release steps for Jamf Pro and Mosyle in the 02-mdm-migration-psso.md addendum.
</success_criteria>

<output>
Create `.planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-03-SUMMARY.md` when done.
</output>
