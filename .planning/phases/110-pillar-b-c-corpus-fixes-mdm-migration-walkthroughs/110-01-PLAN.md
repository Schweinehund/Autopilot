---
phase: 110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/index.md, docs/quick-ref-l1.md, docs/common-issues.md]
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

must_haves:
  truths:
    - "docs/index.md macOS L1 Runbooks row states the count 9 (not 6), reworded to avoid duplicating the :112 PSSO row (per D-04 FIX-01)"
    - "docs/quick-ref-l1.md #36 Secure-Enclave trigger uses the L1 'Use ... runbook' pattern, not 'Escalate L2 via' (per D-04 FIX-02)"
    - "docs/common-issues.md 'macOS Local Password: User Locked Out' block has an L1 #36 bullet between the L1 #37 bullet and the L2 #27 bullet (per D-04 FIX-03)"
    - "The adjacent 'Platform SSO Re-Registration Failure (Post-Migration)' block in common-issues.md is untouched"
  artifacts:
    - path: "docs/index.md"
      provides: "Corrected macOS L1 runbook count (FIX-01)"
      contains: "9 macOS L1 runbooks"
    - path: "docs/quick-ref-l1.md"
      provides: "L1 'try this first' framing for #36 (FIX-02)"
      contains: "Use [Platform SSO — Secure Enclave Key Loss](l1-runbooks/36-macos-secure-enclave-key.md) runbook"
    - path: "docs/common-issues.md"
      provides: "Inserted L1 #36 bullet in User-Locked-Out block (FIX-03)"
      contains: "36-macos-secure-enclave-key.md) — mandatory PSSO re-registration"
  key_links:
    - from: "docs/index.md macOS L1 Runbooks row"
      to: "l1-runbooks/00-index.md#macos-ade-runbooks"
      via: "existing anchor — MUST remain unchanged"
      pattern: "l1-runbooks/00-index.md#macos-ade-runbooks"
---

<objective>
Apply the three v1.13-deferred corpus-accuracy fixes (Pillar B) at their REAL live anchors, honoring the D-04 stale-anchor drift table. Each fix is a single-line reword or single-bullet insertion in a pre-existing file.

Purpose: Correct three factual/navigation nits so L1 technicians get accurate runbook counts and correct "try-this-first" routing for macOS Platform SSO scenarios (SC1, SC2, SC3).
Output: Three edited Markdown files — no new files, no cross-file wiring beyond text already in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-CONTEXT.md
@.planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-RESEARCH.md
@.planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-01 — correct macOS L1 runbook count in docs/index.md</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md (the macOS L1 Runbooks table row containing "(6 runbooks: device, Setup Assistant, profiles, apps, compliance, Company Portal)" — live anchor ~:110, NOT the stale :108)
    - docs/index.md sibling row ~:112 (macOS Platform SSO Runbooks — already references #35/#36/#37; used for the DRY "see row below" pointer)
    - .planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-PATTERNS.md §3 (FIX-01 exact reword + DRY / anchor-preservation rules)
    - .planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-RESEARCH.md §"FIX-01" (count source of truth: 9 = 6 ADE + 3 PSSO; exclude 802.1X #38-41)
  </read_first>
  <action>
    Locate the "macOS L1 Runbooks" table row by its content signal "(6 runbooks: device, Setup Assistant, profiles, apps, compliance, Company Portal)" (per D-04, this is the live anchor near line 110 — do NOT target the stale REQUIREMENTS/ROADMAP :108). Replace ONLY the parenthetical enumeration so the cell reads: Scripted procedures for top macOS ADE enrollment failures (9 macOS L1 runbooks — 6 ADE plus 3 Platform SSO; see row below). The count is 9 = the six ADE runbooks (#10-15) plus the three Platform SSO runbooks (#35/#36/#37) under the `## macOS ADE Runbooks` H2. EXCLUDE 802.1X runbooks #38-41 (they live under a separate H2 and are cross-platform — 6→9, NOT 6→13). Do NOT enumerate #35/#36/#37 again (the :112 PSSO row already does — DRY / link-not-copy). Do NOT modify the :112 row and do NOT add a new row. The link anchor `l1-runbooks/00-index.md#macos-ade-runbooks` MUST remain byte-identical.
  </action>
  <acceptance_criteria>
    - `grep -F "9 macOS L1 runbooks — 6 ADE plus 3 Platform SSO; see row below" docs/index.md` returns exactly one match.
    - `grep -F "(6 runbooks: device, Setup Assistant, profiles, apps, compliance, Company Portal)" docs/index.md` returns zero matches.
    - `grep -c "l1-runbooks/00-index.md#macos-ade-runbooks" docs/index.md` is unchanged (still 2 — the L1 Runbooks row and the PSSO row).
    - The macOS Platform SSO Runbooks row (referencing runbook #35/#36/#37) is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -F "9 macOS L1 runbooks — 6 ADE plus 3 Platform SSO; see row below" docs/index.md && ! grep -qF "(6 runbooks: device, Setup Assistant" docs/index.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: FIX-02 — reword #36 trigger to L1 "try this first" in docs/quick-ref-l1.md</name>
  <files>docs/quick-ref-l1.md</files>
  <read_first>
    - docs/quick-ref-l1.md (the #36 trigger line containing "**Escalate L2** via [Platform SSO — Secure Enclave Key Loss]" — live anchor ~:106, NOT the stale :101; plus sibling lines ~:107 (#35) and ~:108 (#37) for the pattern to match)
    - .planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-PATTERNS.md §4 (FIX-02 exact reword; collect-list preserved verbatim)
    - .planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-RESEARCH.md §"FIX-02"
  </read_first>
  <action>
    Locate the trigger line by content signal "Secure Enclave key error after password reset or FileVault recovery --> **Escalate L2** via [Platform SSO — Secure Enclave Key Loss](l1-runbooks/36-macos-secure-enclave-key.md) first". Change ONLY the routing verb-phrase from "**Escalate L2** via [Platform SSO — Secure Enclave Key Loss](l1-runbooks/36-macos-secure-enclave-key.md) first" to "**Use [Platform SSO — Secure Enclave Key Loss](l1-runbooks/36-macos-secure-enclave-key.md) runbook** first" so the line reads: Secure Enclave key error after password reset or FileVault recovery --> **Use [Platform SSO — Secure Enclave Key Loss](l1-runbooks/36-macos-secure-enclave-key.md) runbook** first; escalate to L2 if re-registration fails (collect: serial number, macOS version, `app-sso platform -s` output). This matches the sibling #35 (:107) and #37 (:108) "Use ... runbook" pattern. Preserve the trailing collect-list verbatim. Do NOT change the #35 or #37 sibling lines (already correct).
  </action>
  <acceptance_criteria>
    - `grep -F "**Use [Platform SSO — Secure Enclave Key Loss](l1-runbooks/36-macos-secure-enclave-key.md) runbook** first" docs/quick-ref-l1.md` returns exactly one match.
    - `grep -F "**Escalate L2** via [Platform SSO — Secure Enclave Key Loss]" docs/quick-ref-l1.md` returns zero matches.
    - The collect-list "(collect: serial number, macOS version, `app-sso platform -s` output)" is still present on that line.
    - Sibling lines for #35 and #37 are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -qF "Use [Platform SSO — Secure Enclave Key Loss](l1-runbooks/36-macos-secure-enclave-key.md) runbook** first" docs/quick-ref-l1.md && ! grep -qF "Escalate L2** via [Platform SSO — Secure Enclave Key Loss]" docs/quick-ref-l1.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: FIX-03 — insert L1 #36 bullet in User-Locked-Out block of docs/common-issues.md</name>
  <files>docs/common-issues.md</files>
  <read_first>
    - docs/common-issues.md (the "### macOS Local Password: User Locked Out" block ~:249-254 — target by heading text, NOT the stale line range :242-247; the L1 #37 bullet and L2 #27 bullet are the insertion boundaries)
    - docs/common-issues.md the adjacent "### Platform SSO Re-Registration Failure (Post-Migration)" block ~:241-247 (the WRONG block — must NOT be touched)
    - .planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-PATTERNS.md §5 (FIX-03 exact insertion text + wrong-block warning)
    - .planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-RESEARCH.md §"FIX-03" and Pitfall 4
  </read_first>
  <action>
    Under the heading "### macOS Local Password: User Locked Out" (identify by heading text per D-04; do NOT use the stale :242-247 range, which points at the wrong Post-Migration block), insert exactly ONE new bullet BETWEEN the existing L1 #37 bullet ("- **L1:** [macOS Local Password Recovery](l1-runbooks/37-macos-local-password-reset.md)") and the L2 #27 bullet ("- **L2:** [macOS Platform SSO Investigation](l2-runbooks/27-macos-sso-investigation.md) — if PSSO re-registration fails after recovering access"). The inserted bullet is: - **L1:** [Platform SSO — Secure Enclave Key Loss](l1-runbooks/36-macos-secure-enclave-key.md) — mandatory PSSO re-registration after password recovery (the reset invalidates the Secure Enclave key). Resulting order: L1 #37 → L1 #36 → L2 #27. Do NOT edit the adjacent "### Platform SSO Re-Registration Failure (Post-Migration)" block — #36 (key loss after password reset) does not fit that block's trigger; touching it is scope creep.
  </action>
  <acceptance_criteria>
    - `grep -F "36-macos-secure-enclave-key.md) — mandatory PSSO re-registration after password recovery (the reset invalidates the Secure Enclave key)" docs/common-issues.md` returns exactly one match.
    - Within the "macOS Local Password: User Locked Out" block the bullet order is L1 #37 (37-macos-local-password-reset.md) then the new L1 #36 (36-macos-secure-enclave-key.md) then L2 #27 (27-macos-sso-investigation.md).
    - The "Platform SSO Re-Registration Failure (Post-Migration)" block still reads "- **L1:** No L1 runbook — escalate to L2" (i.e. it was NOT modified).
  </acceptance_criteria>
  <verify>
    <automated>grep -qF "36-macos-secure-enclave-key.md) — mandatory PSSO re-registration after password recovery" docs/common-issues.md && grep -qF "**L1:** No L1 runbook — escalate to L2" docs/common-issues.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → docs corpus | Only Markdown text is authored; no executable code, no runtime input, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Information (misleading docs) | Edited count/routing text (FIX-01/02/03) | mitigate | Exact-string acceptance criteria + drift-table anchor targeting prevent wrong-anchor / wrong-count edits (RESEARCH Pitfalls 3-5). |
| T-110-02 | Tampering (unintended edit) | Adjacent Post-Migration block in common-issues.md | mitigate | Acceptance criterion asserts the adjacent block is unchanged (FIX-03 wrong-block guard). |
</threat_model>

<verification>
- FIX-01: docs/index.md shows "9 macOS L1 runbooks" and no longer shows the "(6 runbooks: device …" enumeration; `#macos-ade-runbooks` anchor unchanged; :112 row unchanged.
- FIX-02: docs/quick-ref-l1.md #36 line uses "Use ... runbook" and no longer "Escalate L2 via"; collect-list preserved; siblings unchanged.
- FIX-03: docs/common-issues.md User-Locked-Out block order is #37 → #36 → #27; Post-Migration block untouched.
</verification>

<success_criteria>
SC1 (FIX-01), SC2 (FIX-02), SC3 (FIX-03) satisfied: all three live anchors corrected per the D-04 drift table with no collateral edits.
</success_criteria>

<output>
Create `.planning/phases/110-pillar-b-c-corpus-fixes-mdm-migration-walkthroughs/110-01-SUMMARY.md` when done.
</output>
