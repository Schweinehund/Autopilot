---
phase: 107-l1-runbooks-38-41-802-1x-triage
plan: 03
type: execute
wave: 2
depends_on: ["107-01", "107-02"]
files_modified:
  - docs/decision-trees/10-8021x-triage.md
autonomous: true
requirements: [DOT1X-09]

must_haves:
  truths:
    - "Decision tree docs/decision-trees/10-8021x-triage.md routes L1 by symptom to the correct runbook, with per-platform leaves living INSIDE the runbooks (not the tree) per D-03 (SC3)"
    - "The tree axis is symptom-primary (2A per D-03): one root question -> 4 symptom branches -> the 4 runbooks (#38/#39/#40/#41); it stays within the ~5-node / 2-step budget (1 root + 4 symptom terminals + 1 escalation terminal = 6 nodes)"
    - "The tree IS a Phase-107 deliverable per D-04 (the Phase 108 title mention is a stale label); it is authored here, not in Phase 108"
    - "Mermaid house style is cloned from 09-linux-triage.md: graph TD, Legend, classDef resolved/escalateL2, click directives to the four runbooks, and a Routing-Verification table"
    - "Per D-06 the escalation ('Don't know / Other') node has NO click directive (L2 #31 does not exist yet); L2 #31 is named in prose only, no live link"
    - "The tree does not edit docs/l1-runbooks/00-index.md or any nav hub (D-08 / navigation-last — Phase 109)"
  artifacts:
    - path: "docs/decision-trees/10-8021x-triage.md"
      provides: "802.1X symptom-primary L1 triage decision tree routing to runbooks #38-41"
      contains: "graph TD"
      min_lines: 50
  key_links:
    - from: "docs/decision-trees/10-8021x-triage.md"
      to: "docs/l1-runbooks/38-8021x-certificate-failure.md"
      via: "click directive on the cert-failure branch"
      pattern: "38-8021x-certificate-failure\\.md"
    - from: "docs/decision-trees/10-8021x-triage.md"
      to: "docs/l1-runbooks/39-8021x-radius-reject.md"
      via: "click directive on the RADIUS-reject branch"
      pattern: "39-8021x-radius-reject\\.md"
    - from: "docs/decision-trees/10-8021x-triage.md"
      to: "docs/l1-runbooks/40-8021x-server-trust-failure.md"
      via: "click directive on the server-trust branch"
      pattern: "40-8021x-server-trust-failure\\.md"
    - from: "docs/decision-trees/10-8021x-triage.md"
      to: "docs/l1-runbooks/41-8021x-eap-negotiation-failure.md"
      via: "click directive on the EAP-negotiation branch"
      pattern: "41-8021x-eap-negotiation-failure\\.md"
---

<objective>
Author the new 802.1X symptom-primary L1 triage decision tree
`docs/decision-trees/10-8021x-triage.md`, routing L1 by failure symptom to the four
runbooks authored in Plans 01-02.

Purpose: Provide the single symptom-primary entry point (SC3) that a technician uses to
land on the correct 802.1X runbook, with per-platform detail deferred to the runbooks
(D-03) and the tree held within the ~5-node/2-step budget. The tree is a Phase-107
deliverable (D-04).

Output: One new Mermaid decision-tree Markdown file under `docs/decision-trees/`, cloning
the 09-linux-triage.md scaffold. Wave 2 — authored after the four runbook filenames and
titles are fixed by Plans 01-02. Documentation only — no code, no runtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-CONTEXT.md
@.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-RESEARCH.md
@.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 802.1X symptom-primary triage decision tree #10 (SC3)</name>
  <files>docs/decision-trees/10-8021x-triage.md</files>
  <read_first>
    - docs/decision-trees/10-8021x-triage.md (the file being created — confirm it does not yet exist)
    - docs/decision-trees/09-linux-triage.md (EXACT Mermaid scaffold analog: frontmatter, platform gate, How-to-Use + node-budget declaration, Legend, graph TD block, click directives, classDef, Routing-Verification, How-to-Check, Escalation Data, Related Resources, Version History)
    - docs/l1-runbooks/38-8021x-certificate-failure.md (runbook titles/paths for click targets + branch discriminators — authored in Plan 01)
    - docs/l1-runbooks/39-8021x-radius-reject.md (Plan 01)
    - docs/l1-runbooks/40-8021x-server-trust-failure.md (Plan 02)
    - docs/l1-runbooks/41-8021x-eap-negotiation-failure.md (Plan 02)
    - docs/_glossary-network.md, docs/admin-setup-8021x/01-eap-method-overview.md, docs/admin-setup-8021x/02-cert-delivery-foundation.md (Related Resources link targets)
    - .planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-RESEARCH.md §"Pattern 2: Decision Tree Structure" + §"Per-Symptom Mapping" (branch discriminators)
    - .planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-PATTERNS.md §"docs/decision-trees/10-8021x-triage.md" (verbatim scaffold extraction)
  </read_first>
  <action>
    Create `docs/decision-trees/10-8021x-triage.md` by cloning the `09-linux-triage.md` scaffold.
    Frontmatter: `last_verified: 2026-06-30`, `review_by: 2026-09-28`, `applies_to: both`, `audience: L1`,
    `platform: windows+macos+ios+android+linux`. Add a `> **Platform gate:**` blockquote adapted to
    cross-platform 802.1X triage (reference the initial triage tree and the four runbooks). Add
    `## How to Use This Tree` stating "All terminal nodes are within 2 decision steps of the root (well under
    the 5-node budget)" and noting the flat symptom-primary shape per D-03 (per-platform leaves live inside the
    runbooks, not this tree) and that the tree is a Phase-107 deliverable per D-04.
    `## Legend`: keep Diamond / Green resolved / Red escalate rows; DROP the orange pitfallCallout row (no
    architectural callout node in this tree).
    Mermaid `graph TD` block (D-03, 2A): root diamond `EAP1{"What is the user's<br/>802.1X failure symptom?"}`
    with exactly four symptom branches to rounded terminals — cert profile Error/Pending -> `EAP38` (Runbook 38);
    trust prompt / untrusted server / RADIUS root CA missing -> `EAP40` (Runbook 40); EAP method or inner-auth
    mismatch (e.g., iOS fails, others succeed) -> `EAP41` (Runbook 41); cert profiles Succeeded, RADIUS rejects
    auth -> `EAP39` (Runbook 39) — plus a fifth "Don't know / Other" branch to an escalation terminal `EAPE`.
    Add `click` directives ONLY for the four runbook terminals, using relative paths
    `../l1-runbooks/38-8021x-certificate-failure.md`, `39-8021x-radius-reject.md`,
    `40-8021x-server-trust-failure.md`, `41-8021x-eap-negotiation-failure.md`. The `EAPE` escalation node has
    NO click directive (D-06 — L2 #31 does not exist yet; name it in prose only). classDefs: `resolved fill:#28a745,color:#fff`
    and `escalateL2 fill:#dc3545,color:#fff`; class the four runbook nodes resolved and `EAPE` escalateL2; OMIT
    the `pitfallCallout` classDef. Node budget = 6 total.
    Add `## Routing Verification` (Path / Step 1 / Step 2 / Destination; Step 2 = "(terminal)" for all rows;
    reference node `EAP1`), `## How to Check` (802.1X symptom discriminators from RESEARCH Per-Symptom Mapping),
    `## Escalation Data` (EAPE collect list: device serial, UPN, platform, Intune cert-profile status, the named
    event-log channel — naming L2 Log Collection (#31) in PROSE only, no link), `## Related Resources` (bullets
    to the four new runbooks, `_glossary-network.md`, `01-eap-method-overview.md`, `02-cert-delivery-foundation.md`,
    and the initial triage tree), and `## Version History` (row `2026-06-30 | Phase 107 plan 03: initial authoring
    — 802.1X triage decision tree (symptom-primary, D-03/D-04) | --`).
    Callouts NOTE/WARNING/DANGER/CRITICAL only. Plain GitHub auto-slug anchors, no `{#id}`, avoid the
    double-hyphen trap. Do NOT edit `docs/l1-runbooks/00-index.md` or any nav hub (D-08 / navigation-last —
    Phase 109). Placeholder identifiers only.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && test -f docs/decision-trees/10-8021x-triage.md && grep -q "platform: windows+macos+ios+android+linux" docs/decision-trees/10-8021x-triage.md && grep -q "graph TD" docs/decision-trees/10-8021x-triage.md && grep -q "38-8021x-certificate-failure.md" docs/decision-trees/10-8021x-triage.md && grep -q "39-8021x-radius-reject.md" docs/decision-trees/10-8021x-triage.md && grep -q "40-8021x-server-trust-failure.md" docs/decision-trees/10-8021x-triage.md && grep -q "41-8021x-eap-negotiation-failure.md" docs/decision-trees/10-8021x-triage.md && [ "$(grep -c 'click ' docs/decision-trees/10-8021x-triage.md)" = "4" ] && ! grep -q "pitfallCallout" docs/decision-trees/10-8021x-triage.md && ! grep -q "l2-runbooks/3" docs/decision-trees/10-8021x-triage.md && ! grep -qi "IMPORTANT:" docs/decision-trees/10-8021x-triage.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/decision-trees/10-8021x-triage.md` exists with the exact frontmatter line `platform: windows+macos+ios+android+linux` and `audience: L1`.
    - Contains a `graph TD` Mermaid block with a single symptom-primary root question and four symptom branches routing to runbooks #38/#39/#40/#41 (SC3, D-03).
    - Contains exactly four `click ` directives, one per runbook, referencing `../l1-runbooks/38-...`, `39-...`, `40-...`, `41-...`; the escalation node has NO click directive.
    - NO `pitfallCallout` classDef or class; classDefs limited to `resolved` and `escalateL2`.
    - Node budget respected: 1 root + 4 runbook terminals + 1 escalation terminal = 6 nodes; `## Routing Verification` states all terminals within 2 decision steps.
    - Escalation node names L2 Log Collection (#31) in prose with NO markdown link to `../l2-runbooks/` (no substring `l2-runbooks/3`).
    - `## How to Use This Tree` notes the symptom-primary shape (D-03) and that per-platform leaves live inside the runbooks; no `IMPORTANT:` callout; no edit to `00-index.md`.
    - Placeholder identifiers only.
  </acceptance_criteria>
  <done>Decision tree #10 exists, satisfies SC3 (routes by symptom to the correct runbook with per-platform leaves in the runbooks), honors D-03/D-04/D-06/D-08, stays within the node budget, and passes the automated string checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Markdown/Mermaid documentation only. No code, no runtime, no auth, no user input, no network surface. Zero attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Information Disclosure | Tree example identifiers | mitigate | Tree MUST use placeholder identifiers only; never embed real credentials, keys, or serials — consistent with the suite "never commit credentials" rule. |
| T-107-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — pure documentation phase, no package installs (RESEARCH §Package Legitimacy Audit: NOT APPLICABLE). |
</threat_model>

<verification>
- File exists under `docs/decision-trees/`.
- Symptom-primary `graph TD` with 4 runbook branches + 1 escalation; exactly 4 click directives; no `pitfallCallout`; no live `../l2-runbooks/` links; no `IMPORTANT:` callout; no nav-hub edit.
- Routing-Verification table present; node budget = 6.
</verification>

<success_criteria>
- SC3 satisfied: `10-8021x-triage.md` routes L1 by symptom to the correct runbook with per-platform leaves (in the runbooks).
- D-03, D-04, D-06, D-08 honored and cited; DOT1X-09 advanced.
</success_criteria>

<output>
Create `.planning/phases/107-l1-runbooks-38-41-802-1x-triage/107-03-SUMMARY.md` when done.
</output>
