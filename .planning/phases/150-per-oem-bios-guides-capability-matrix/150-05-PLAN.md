---
phase: 150-per-oem-bios-guides-capability-matrix
plan: 05
type: execute
wave: 4
depends_on: ["150-04"]
files_modified:
  - docs/operations/firmware-bios/00-overview.md
autonomous: true
requirements: [BIOS-05, BIOS-06, BIOS-07, BIOS-12]

estimate:
  tokens: 55000
  raw_tokens: 37000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/operations/firmware-bios/00-overview.md no longer states that vendor-specific procedures for Dell, HP and Lenovo are not yet written in this corpus — that sentence became false the moment Plan 04 committed, and it is replaced rather than left dangling (D-37)."
    - "The overview carries outbound links to all three new guides and to the capability matrix, so the domain router points at both its procedures and its own comparison artifact (D-37, D-38)."
    - "The inverted-prerequisite pair's canonical one-line formulation — Dell wants a virgin BIOS; Lenovo needs a provisioned one — appears in 00-overview.md, which is the placement Success Criterion 2 actually grades (BIOS-06, D-32, D-33)."
    - "The HP and Lenovo custody blockquotes are reduced to a custody claim plus a link to the guide that now carries the quote with its evidence line, because Success Criterion 3 requires those quotes in the HP guide quoted against Dell's statement, and pointing does not satisfy quoted against (D-36)."
    - "The sentence asserting HP Connect is administered at admin.hp.com rather than in the Intune console is qualified, not deleted: HP Connect has appeared in the Intune admin center's Partner portals tab since April 2023, the same discovery surface Dell has (BIOS-07, D-40, D-50, U-6)."
    - "The overview's last_verified takes the execution date and review_by is that date plus 60 by arithmetic (D-39)."
    - "The overview keeps exactly 9 H2 headings and exactly 7 anchors — the edit adds no heading and removes none (D-78)."
    - statement: "After the overview commit, check-nav-hub-links reports 0 hub-presence failures and 0 corpus-link failures, and check-phase-144.mjs reports 101 PASS, 0 FAIL, 0 SKIPPED — both at the phase-start baseline."
      verification: backstop
    - statement: "The Dell custody quote and its Learn evidence line at 00-overview.md lines 42-46 are byte-unchanged, because the 2026-08-25 falsification re-fetch found the Learn page byte-identical with ms.date 2024-06-06 and updated_at 2026-07-01 unchanged."
      verification: backstop
  artifacts:
    - docs/operations/firmware-bios/00-overview.md (edited at seven sites, committed alone)
  key_links:
    - "00-overview.md -> 02-dell-bios-configuration.md"
    - "00-overview.md -> 03-hp-bios-configuration.md"
    - "00-overview.md -> 04-lenovo-bios-configuration.md"
    - "00-overview.md -> ../../reference/firmware-oem-matrix.md"
  prohibitions:
    - statement: "docs/operations/firmware-bios/01-windows-dfci.md is not edited. Linking into it is not editing it."
      status: flagged-unverified
      verification: manual
    - statement: "docs/operations/patch-management/06-windows-driver-firmware-updates.md is not edited."
      status: flagged-unverified
      verification: manual
    - statement: "The three guides and the matrix committed by Plan 04 are not edited in this plan. If a defect in one of them surfaces here, it is fixed in its own follow-up commit and reported, not folded silently into the overview commit."
      status: flagged-unverified
      verification: manual
    - statement: "No row for RE-226 is added to docs/_registry/RE-index.md — Phase 152 owns every registry row."
      status: flagged-unverified
      verification: manual
    - statement: "Neither 225-row publish-bundle canary is bumped."
      status: flagged-unverified
      verification: manual
    - statement: "docs/index.md and docs/operations/00-index.md are not edited — navigation-last wiring is Phase 152."
      status: flagged-unverified
      verification: manual
    - statement: "No H2 heading is added to or removed from 00-overview.md, and no anchor is added or removed. The file's 9 H2 / 7 anchor shape is unchanged."
      status: flagged-unverified
      verification: manual
    - statement: "No code fence is introduced into 00-overview.md."
      status: flagged-unverified
      verification: manual
    - statement: "The Dell custody blockquote and its Learn evidence line are not touched — the falsification re-fetch found no correction needed."
      status: flagged-unverified
      verification: manual
---

<objective>
Close the phase by editing the file Phase 149 shipped, so the domain router tells the truth about what
now exists and carries the one sentence Success Criterion 2 is actually graded on.

Purpose: three things break the moment Plan 04's commit lands. The overview asserts the vendor guides
do not exist, which becomes false. The overview holds the HP and Lenovo custody quotes that Success
Criterion 3 requires to live in the HP guide, quoted against Dell's statement. And the overview
asserts an HP-versus-Dell asymmetry on Intune UI discoverability that the 2026-08-25 research
falsified. Phase 150 owns all three corrections by explicit hand-forward from 149's falsification
clause.

This is the phase's **second and final commit** (D-80). It lands alone, after the content commit, and
the apex runs again after it.

Output: `docs/operations/firmware-bios/00-overview.md`, edited at seven sites, committed alone.

**Decisions this plan implements:** D-32 (the cross-reference sentence, and its overview placement is
the one SC#2 grades), D-33 (the draft's ground for keeping the pair out of the overview is deleted —
the overview already carries BIOS-01 and parts of BIOS-02 and BIOS-11), D-36 (the HP and Lenovo quotes
are reduced to a custody claim plus a link, owner-ruled), D-37 (the false sentence spans lines 128-130,
and the edit count is four to six sites, not three), D-38 (the overview gains an outbound link to the
matrix), D-39 (frontmatter authority is D-30, not D-69: `last_verified` takes the execution date and
`review_by` is +60 by arithmetic), D-40 (the falsification checklist, now run — two claims unchanged,
one needing a qualifier), D-41 (`01-windows-dfci.md` is not edited; linking is not editing), D-42
(editing this file is mechanically safe, and here is the measurement), D-50 (U-6's closure rule, now
closed with a positive answer), D-65 (zero code fences), D-71 (retired-branding convention), D-78
(the anchor contract is unchanged by this edit), D-79 (relative paths computed from the source file),
D-80 (the second commit), D-81 (four binding gate classes), D-82 (the prose-negative grep set), D-83
(the apex, run again after this commit), D-88 (accepted inconsistencies).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-per-oem-bios-guides-capability-matrix/150-CONTEXT.md
@.planning/phases/150-per-oem-bios-guides-capability-matrix/150-RESEARCH.md
@.planning/phases/150-per-oem-bios-guides-capability-matrix/150-PATTERNS.md
@.planning/phases/150-per-oem-bios-guides-capability-matrix/150-01-PLAN.md
</context>

## Read the phase-wide rules first

`150-01-PLAN.md` carries the phase-wide rules governing this plan: the two-commit contract, the
measured baselines, the nine measured corrections, the evidence-line form and the barred-prose list.
Read that section before executing.

## Precondition

`git log -1 --name-only` shows Plan 04's content commit containing the three guides, the matrix and
`docs/_glossary.md`, and `git status --porcelain` shows a clean tree. If the content commit has not
landed, stop: every link this plan adds would dangle, and the whole point of the two-commit contract
is that the overview edit lands last.

## Editing a file Phase 149 shipped is mechanically safe — the measurement

D-42's proof, which the grill draft never supplied and which is worth carrying because "measured safe"
and "never checked" look identical from the outside:

- `[MEASURED]` `grep -rn "firmware-bios" scripts/` returns **0**. No validator, frozen-blob gate or
  hash pin in this repository touches this directory. Editing `00-overview.md` cannot break a frozen
  surface, because no frozen surface reads it.
- `[MEASURED]` `V-54-29`'s `OV` constant is `docs/operations/patch-management/00-overview.md` only —
  a different file with a similar name. The two silent leak modes it guards against, the em-dash
  description following a link and a link wrapped across a newline defeating the link regex, do **not**
  bind here.
- `[MEASURED]` `V-54-07`'s 60-day interval check is `PATCH_FILES`-scoped at `check-phase-54.mjs:35`,
  and all five of those files live in `patch-management/`. Nothing enforces the interval on this file;
  D-39 sets +60 as a house convention, not to satisfy a gate.
- `[MEASURED]` the file is LF-terminated. Confirm with `file docs/operations/firmware-bios/00-overview.md`
  before any string replace — a replace against the wrong terminator matches zero times, silently.

## The seven edit sites, with their measured coordinates

Line numbers were verified at plan time against HEAD `b08edfd1`. **Re-locate every site by content
before editing** — run `sed -n '124,134p'` and `sed -n '42,70p'` and match the text, because a
preceding edit shifts every number below it. Apply the sites in the order given, or work bottom-up, so
earlier edits do not invalidate later coordinates.

| # | Site | Measured location | What changes |
|---|---|---|---|
| 1 | The now-false sentence | lines **128-130**; line 128 ends `…Vendor-specific procedures for Dell, HP and`, line 129 begins `Lenovo are not yet written in this corpus`, line 130 ends `…before the procedure exists.`, and **line 131 is blank** | Replaced. The blank line at 131 is preserved. |
| 2 | Outbound links to the three guides | same physical site as #1, inside `## Choosing a Path` | Added. |
| 3 | The cross-reference sentence (D-32) | same physical site as #1 | Added. |
| 4 | The outbound link to the matrix (D-38) | `## Choosing a Path` and `## Related Resources` | Added. |
| 5 | The HP custody quote and evidence line | blockquote at **line 52**, evidence line at **line 54** | Reduced to a claim plus a link. |
| 6 | The Lenovo custody quote and evidence line | blockquote at **line 66**, evidence line at **line 68** | Reduced to a claim plus a link. |
| 7 | Frontmatter dates | `last_verified` and `review_by` in the frontmatter block | `last_verified` = execution date; `review_by` = +60. |

Plus one correction folded into site 5's paragraph: the `admin.hp.com rather than in the Intune console`
clause at **lines 48-49** is **qualified, not deleted** (D-40's checklist item 1, U-6).

D-37 counted this as four to six sites; the measured count with D-40's correction folded in is seven.
Recorded so a reviewer does not read the difference as scope creep.

## The falsification checklist (D-40) — run, with its results

D-40 required a named checklist with one owner, run at plan time. It was run on 2026-08-25 and is
recorded in `150-RESEARCH.md`. Its three results, which this plan encodes:

1. **HP cloud-vault custody** — the source PDF was re-fetched; the cloud-vault quote is **unchanged**.
   No correction to the quote itself. But the adjacent claim in the same paragraph, that HP Connect is
   administered at `admin.hp.com` **rather than in the Intune console**, now needs a qualifier: HP
   Connect joined the Intune admin center's Partner portals tab in April 2023, so the two vendors are
   symmetric on UI discoverability and differ only on secret custody. **Qualify, do not delete** — the
   console genuinely does live at `admin.hp.com`.
2. **Lenovo Azure Key Vault** — not re-fetched, deliberately: Phase 149 already shipped it flagged as
   this corpus's own inference rather than a vendor claim, so it carries no vendor-quote obligation.
   No new information contradicts it. **No correction.**
3. **Dell "Intune stores the BIOS passwords"** — the Learn page was re-fetched and is **byte-identical**,
   with `ms.date 2024-06-06` and `updated_at 2026-07-01` unchanged. **No correction.** The Dell
   blockquote at lines 42-44 and its evidence line at 46 are therefore left byte-unchanged, and an
   acceptance criterion checks that.

<tasks>

<task type="auto">
  <name>Task 1: Edit 00-overview.md at all seven sites</name>
  <files>docs/operations/firmware-bios/00-overview.md</files>
  <precondition>Plan 04's content commit has landed and the working tree is clean.</precondition>
  <read_first>
    - `docs/operations/firmware-bios/00-overview.md` — read the whole file. In particular: the
      frontmatter at lines 1-7; the custody table and the three custody paragraphs at lines 27-79,
      including the Dell blockquote at 42-44 with its evidence line at 46, the HP paragraph at 48-50
      with its blockquote at 52 and evidence line at 54, the currency caveat at 56-60, and the Lenovo
      paragraph at 62-64 with its blockquote at 66 and evidence line at 68; `## Choosing a Path` at
      109-130; and `## Related Resources` at 226-239. **Copy this file's own conventions** — its
      evidence-line form, its blockquote form, its bullet form in `## Choosing a Path` — rather than
      importing any other file's style. This is the file's own precedent.
    - `docs/operations/firmware-bios/02-dell-bios-configuration.md`,
      `03-hp-bios-configuration.md`, `04-lenovo-bios-configuration.md` — their H1 titles, so the link
      text in this file matches the documents it points at.
    - `docs/reference/firmware-oem-matrix.md` — its H1 title, for the same reason.
    - `.planning/phases/150-per-oem-bios-guides-capability-matrix/150-RESEARCH.md` — the U-6 closure
      quote and its source metadata, for the qualifier at site 5.
  </read_first>
  <action>
Edit `docs/operations/firmware-bios/00-overview.md` in place at the seven sites tabulated above. Do not
use a different template; this file's own conventions are the reference. Confirm LF line endings
before any string replace, and re-locate each site by matching its text rather than trusting a cached
line number.

**Site 1, 2, 3 and part of 4 — the `## Choosing a Path` tail, lines 128-130.**

Replace the three-line sentence beginning `does not, who is willing to hold your BIOS password.
Vendor-specific procedures for Dell, HP and` through `the choice can be made before the procedure
exists.` Take care: line 128 begins mid-sentence with `does not, who is willing to hold your BIOS
password.` and that clause belongs to the **preceding** sentence, which stays. Only the
`Vendor-specific procedures…` sentence is replaced. Preserve the blank line at 131. A replacement
written to the wrong range leaves a dangling sentence fragment, and no validator in this repository
reads prose, so it would ship invisibly.

The replacement states that the procedures now exist and routes to them, using relative same-directory
links (D-79):

- `[Dell BIOS Configuration Through Intune](02-dell-bios-configuration.md)`
- `[HP BIOS Configuration Through Intune](03-hp-bios-configuration.md)`
- `[Lenovo BIOS Configuration Through Intune](04-lenovo-bios-configuration.md)`
- `[Firmware OEM Capability Matrix](../../reference/firmware-oem-matrix.md)` — note the two-level
  relative path, computed from `docs/operations/firmware-bios/`, per D-79.

Format the three guide links as prose or as bullets, at the executor's discretion, matching the
surrounding section's existing bullet style. Say what the matrix is for, in one clause: it reads the
same six capability dimensions across all three manufacturers rather than down one.

**Site 3 in full — the cross-reference sentence (D-32).** Add the canonical one-line formulation of
the inverted-prerequisite pair to `## Choosing a Path`, in this exact form, because Success Criterion
2 is graded on the pair reading as a cross-vendor decision point and two halves sitting in two
separate files do not produce that reading:

`Dell wants a virgin BIOS; Lenovo needs a provisioned one.`

Give it one sentence of surrounding context naming what each vendor requires — Dell's native template
requires no pre-existing BIOS password and refuses when one exists; Lenovo cannot bootstrap an initial
supervisor password remotely and requires one already set at imaging or during OOBE — and route to
both guides' Prerequisites sections for the sourced detail. The overview is the correct home for this
sentence: it already carries BIOS-01 and parts of BIOS-02 and BIOS-11, so the notion that a Phase 150
requirement cannot land in a Phase 149 file is not a rule this corpus holds (D-33).

**Site 4 in full — the matrix link in `## Related Resources`.** Add
`[Firmware OEM Capability Matrix](../../reference/firmware-oem-matrix.md)` as a bullet, with an
em-dash description, in the same form as the existing bullets at lines 227-239. Also add the three
guide links there if the executor judges the section reads better with the domain's full inventory,
matching how `01-windows-dfci.md` is already listed. The domain's router must point at its own
capability matrix; the original edit list enumerated the guides and omitted the matrix entirely.

**Site 5 — the HP custody paragraph, blockquote at 52 and evidence line at 54.**

Two changes in one paragraph.

First, **qualify** the clause at lines 48-49. The current text reads that HP Connect is `administered
at admin.hp.com rather than in the Intune console`. That is not wrong about where the console lives,
but it now implies an asymmetry with Dell that no longer exists. Rewrite so both facts stand: the
console itself is administered at `admin.hp.com`, and HP Connect is also reachable from the Intune
admin center's Partner portals tab, alongside Dell's, so the two vendors are symmetric on discovery
and differ on secret custody — which is the distinction this domain actually routes on. Do not delete
the `admin.hp.com` fact.

Second, per D-36 (owner-ruled), **reduce the blockquote at line 52 and its evidence line at 54** to a
custody claim plus a link. Success Criterion 3 requires that quote to sit in the HP guide, quoted
against Dell's no-customer-data statement, and a pointer does not satisfy "quoted against" — so the
quote's home is now `03-hp-bios-configuration.md`, which carries it with its own evidence line beside
Dell's. Here, state the custody position as a claim in this corpus's own words — the BIOS password is
held in HP's cloud vault and never enters your tenant — and link
`[HP BIOS Configuration Through Intune](03-hp-bios-configuration.md)` for the quote and its source.
Remove the now-orphaned `**Source:**` line at 54 along with the blockquote it evidenced; leaving an
evidence line with nothing to evidence is worse than removing both. The URL stays available in
`## External References`, which is not edited.

Leave the currency caveat at lines 56-60 in place — it still applies, and `150-RESEARCH.md` re-confirmed
on 2026-08-25 that no HP Connect guide newer than Version 1.2.0 / 2022-09-27 exists.

**Site 6 — the Lenovo custody paragraph, blockquote at 66 and evidence line at 68.** Same treatment
per D-36's `Same treatment for the Lenovo quote`: reduce to a custody claim plus a link to
`[Lenovo BIOS Configuration Through Intune](04-lenovo-bios-configuration.md)`, removing the blockquote
and its evidence line together. Keep the paragraph at lines 70-79 that labels the comparative
superlative as this corpus's own inference — that labeling is doing real work and the Lenovo guide
repeats it rather than replacing it.

**Site 7 — frontmatter dates (D-39).** Set `last_verified` to the execution date and `review_by` to
that date plus 60, by arithmetic. Frontmatter authority here is D-30, not D-69: D-69 is a
midnight-spanning tiebreaker, not a general rule. Nothing enforces the interval on this file — `V-54-07`
is `PATCH_FILES`-scoped — so this is a house convention applied deliberately.

**Do not touch** the Dell blockquote at lines 42-44 or its evidence line at 46. The falsification
re-fetch on 2026-08-25 found the Learn page byte-identical with both dates unchanged, so there is
nothing to correct, and an unnecessary edit to a verified quote is its own risk.

**Do not add or remove any H2 or any anchor.** The file's 9 H2 / 7 anchor contract is unchanged by
this edit, and 146 D-39 and 148 D-16 make every H2 a slug Phase 151 must match byte-for-byte.

**Do not introduce a code fence** (D-65).

Then commit **this file alone** — the phase's second and final commit (D-80). Message:
`docs(150): route the firmware domain overview to the new per-OEM guides and matrix (D-32, D-36, D-37, D-38, D-40)`.
  </action>
  <verify>
    <automated>cd /d/claude/Autopilot &amp;&amp; O=docs/operations/firmware-bios/00-overview.md &amp;&amp; echo "H2=$(grep -c '^## ' $O) ANCHORS=$(grep -c '&lt;a id=' $O) FENCES=$(grep -c '^```' $O)" &amp;&amp; grep -c 'are not yet written in this corpus' $O; grep -c 'Dell wants a virgin BIOS; Lenovo needs a provisioned one' $O &amp;&amp; grep -c '02-dell-bios-configuration.md' $O &amp;&amp; grep -c '03-hp-bios-configuration.md' $O &amp;&amp; grep -c '04-lenovo-bios-configuration.md' $O &amp;&amp; grep -c 'firmware-oem-matrix.md' $O &amp;&amp; grep -c 'Intune stores the BIOS passwords for each device' $O</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'are not yet written in this corpus' docs/operations/firmware-bios/00-overview.md`
      returns **0** — the now-false sentence is gone (D-37).
    - No dangling fragment remains: the sentence beginning `Two questions settle almost every case`
      still ends with a full stop and reads as a complete sentence, and the blank line preceding
      `<a id="domain-boundary"></a>` is still present. Checkable by reading the replaced region.
    - `grep -c 'Dell wants a virgin BIOS; Lenovo needs a provisioned one' docs/operations/firmware-bios/00-overview.md`
      returns **1** — the canonical formulation, in its fixed byte-form, in the placement SC#2 grades
      (BIOS-06, D-32, D-34).
    - `grep -c '02-dell-bios-configuration.md' …`, `grep -c '03-hp-bios-configuration.md' …` and
      `grep -c '04-lenovo-bios-configuration.md' …` each return at least **1** (D-37).
    - `grep -c 'firmware-oem-matrix.md' docs/operations/firmware-bios/00-overview.md` returns at least
      **1**, and every occurrence uses the two-level relative path `../../reference/` (D-38, D-79).
    - `grep -c 'Passwords are managed by HP Connect and stored in a cloud vault' docs/operations/firmware-bios/00-overview.md`
      returns **0**, and the same string returns **1** in
      `docs/operations/firmware-bios/03-hp-bios-configuration.md` — the quote moved, it was not lost
      (D-36).
    - `grep -c 'Lenovo.BIOS.Certificates module has been updated' docs/operations/firmware-bios/00-overview.md`
      returns **0** — the Lenovo blockquote was reduced too (D-36).
    - No orphaned evidence line remains: every `^\*\*Source:\*\*` line in the file is immediately
      preceded, within three lines, by a `^> ` blockquote. Checkable by inspecting each
      `grep -n '^\*\*Source:\*\*'` hit.
    - `grep -c 'Intune stores the BIOS passwords for each device' docs/operations/firmware-bios/00-overview.md`
      returns **1** and `git diff HEAD~1 -- docs/operations/firmware-bios/00-overview.md | grep -c '^-> Intune stores'`
      returns **0** — the Dell quote is byte-unchanged (D-40 item 3).
    - `grep -ci 'Partner portals' docs/operations/firmware-bios/00-overview.md` returns at least **1**
      — the U-6 qualifier landed (D-40 item 1, D-50).
    - `grep -c 'admin.hp.com' docs/operations/firmware-bios/00-overview.md` returns at least **1** —
      the fact was qualified, not deleted.
    - `grep -c '^## ' docs/operations/firmware-bios/00-overview.md` returns exactly **9** and
      `grep -c '<a id=' …` returns exactly **7** — unchanged from before the edit (D-78).
    - `grep -c '^```' docs/operations/firmware-bios/00-overview.md` returns **0** (D-65).
    - `review_by` minus `last_verified` in the frontmatter is exactly **60** days, and `last_verified`
      equals the execution date (D-39).
    - `grep -vn '^>' docs/operations/firmware-bios/00-overview.md | grep -cE 'Proactive Remediation|Azure Active Directory'`
      returns **0** (D-71).
    - `grep -nEi '\b(behaviour|colour|organis[ae]|licence|enrolment|honour|labelled|centre)\b' docs/operations/firmware-bios/00-overview.md`
      returns no output (D-82).
    - `git log -1 --name-only --format='%s'` shows exactly **one** file,
      `docs/operations/firmware-bios/00-overview.md` (D-80).
  </acceptance_criteria>
  <done>
    All seven sites are edited, the false sentence is gone without a dangling fragment, the canonical
    inversion sentence is in the overview, both reduced quotes now live in their guides, the U-6
    qualifier landed without deleting the `admin.hp.com` fact, the Dell quote is untouched, and the
    file is committed alone as the phase's second commit.
  </done>
  <reversibility rating="costly">
    This edits a file that Phase 149 shipped and verified. Reverting means re-opening the false
    sentence and re-orphaning the two quotes now living in the guides. The edit is nonetheless
    mandatory: 149 D-65's falsification clause hands this correction forward to Phase 150 explicitly,
    and D-36 is an owner ruling.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Take every gate back to baseline and close the phase</name>
  <files>docs/operations/firmware-bios/00-overview.md, docs/operations/firmware-bios/02-dell-bios-configuration.md, docs/operations/firmware-bios/03-hp-bios-configuration.md, docs/operations/firmware-bios/04-lenovo-bios-configuration.md, docs/reference/firmware-oem-matrix.md (all verified, none modified)</files>
  <read_first>
    - `docs/operations/firmware-bios/00-overview.md` — the file as just committed.
    - `.planning/phases/150-per-oem-bios-guides-capability-matrix/150-04-SUMMARY.md` — the post-content-commit
      gate verdicts, which this task must match exactly.
    - `.planning/phases/150-per-oem-bios-guides-capability-matrix/150-01-PLAN.md` — the measured
      phase-start baselines table.
  </read_first>
  <action>
Run every gate class after the overview commit (D-81), run the apex **separately from the verifier**
(D-83), and close the phase's evidence record.

1. `node scripts/validation/c17-eee-contract.mjs` — expect `235 files checked, 0 with violations, 0
   total violations`. The overview is unenrolled, so this file count is unchanged from Plan 04's.
2. `node scripts/validation/check-nav-hub-links.mjs` — expect
   `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`. Every new outbound link from the
   overview resolves, and every fragment resolves against a real anchor.
3. `node scripts/validation/check-phase-54.mjs` — expect 0 failures. This is the run that would catch
   a prose regression introduced by the edit, and it walks `.planning/` as well as `docs/`.
4. `node scripts/validation/v1.20-milestone-audit.mjs` — expect exit 0. C11 strips nothing, so the URL
   text in the four new links is scanned too. A link change is a prose change in this repository, and
   that has tripped a prose guard after many green plans before.
5. `node scripts/validation/check-phase-49.mjs` — expect `22 passed, 0 failed, 0 skipped`.
6. `node scripts/validation/check-phase-144.mjs` — expect `101 PASS, 0 FAIL, 0 SKIPPED`, the exact
   phase-start baseline. About 60 seconds.

Then run the three-way plus matrix structural check one final time, because this is the phase's last
opportunity to catch drift before verification: the `^## ` line sets of the three guides must be
identical to each other, and the matrix's six capability H2s must be byte-identical to the guides'
six. That equality is the mechanical fact Success Criterion 1 rests on.

Also confirm the blast-radius fence held across the whole phase, not just this commit: over the two
content commits plus the amendment commit,
`git diff --name-only <phase-start SHA>..HEAD` must list exactly seven paths — the three guides, the
matrix, `docs/_glossary.md`, `docs/operations/firmware-bios/00-overview.md` and
`.planning/REQUIREMENTS.md` — plus this phase's own planning artifacts. Nothing under `scripts/`, no
`docs/_registry/RE-index.md`, no index file.

Finally, record the phase's closing evidence in the SUMMARY: every gate verdict, the two content
commit SHAs plus the amendment SHA, and the Phase 152 hand-forward restated as a rule — the registry
row for `RE-226`, registry and filename-map rows for the three unenrolled operations guides, the
filename map regenerated by its generator, and both canary targets recomputed from the registry after
the rows land rather than hard-coded from a document count.

Also record the five accepted inconsistencies (D-88) explicitly, so the verifier and the code review
do not surface them as defects:

(a) Success Criterion 3's phrase about both paths carrying the fleet-first de-provisioning order is
HP-only in the sources — Dell documents no order at all, re-confirmed 2026-08-25. Owner-ruled D-30.
(b) The matrix ships `status: Approved` and is absent from the publish bundle until Phase 152 lands
its registry row, and nothing fails. Measured D-22.
(c) U-6 is now **closed with a positive answer** rather than left open — the research addendum updates
D-88's original wording, which anticipated it closing as open.
(d) `06-windows-driver-firmware-updates.md` keeps vendor update clients generic while this phase names
Dell Command | Update, HP Image Assistant and Lenovo System Update. Deliberate, D-68.
(e) Success Criterion 1 and BIOS-05 say "five-section"; Plan 01 filed the six-section amendment in
`.planning/REQUIREMENTS.md` naming D-01 as its ground, so both are to be read through it.

And record the one measured correction to D-01's own descriptive text: the guides ship **three** tail
H2s, not two, because both `00-overview.md` and `01-windows-dfci.md` measurably do. D-78's anchor rule
— anchors equal H2 minus the two **unanchored** tail H2s — is unaffected and holds exactly.
  </action>
  <verify>
    <automated>cd /d/claude/Autopilot &amp;&amp; for f in 02-dell 03-hp 04-lenovo; do grep '^## ' docs/operations/firmware-bios/$f-bios-configuration.md &gt; /tmp/$f.h2; done &amp;&amp; diff /tmp/02-dell.h2 /tmp/03-hp.h2 &amp;&amp; diff /tmp/02-dell.h2 /tmp/04-lenovo.h2 &amp;&amp; node scripts/validation/c17-eee-contract.mjs 2&gt;&amp;1 | tail -2 &amp;&amp; node scripts/validation/check-nav-hub-links.mjs 2&gt;&amp;1 | tail -3 &amp;&amp; node scripts/validation/check-phase-54.mjs 2&gt;&amp;1 | tail -2 &amp;&amp; node scripts/validation/check-phase-49.mjs 2&gt;&amp;1 | tail -2 &amp;&amp; node scripts/validation/check-phase-144.mjs 2&gt;&amp;1 | tail -3 &amp;&amp; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - Both H2 diffs across the three guides produce **no output**.
    - The matrix's six capability H2s match the guides' six byte-for-byte — checkable by extracting the
      matrix's `^## ` lines, dropping `## Summary`, `## Key Gaps Summary`, `## Source Attribution`,
      `## See Also` and `## Version History`, and diffing the remainder against the first six `^## `
      lines of `02-dell-bios-configuration.md`.
    - `node scripts/validation/c17-eee-contract.mjs` prints `235 files checked, 0 with violations, 0
      total violations`.
    - `node scripts/validation/check-nav-hub-links.mjs` prints
      `0 hub-presence failure(s), 0 corpus-link failure(s), 0 total`.
    - `node scripts/validation/check-phase-54.mjs` prints `0 failed`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0.
    - `node scripts/validation/check-phase-49.mjs` prints `22 passed, 0 failed, 0 skipped`.
    - `node scripts/validation/check-phase-144.mjs` prints `101 PASS, 0 FAIL, 0 SKIPPED` — the exact
      phase-start baseline.
    - `git status --porcelain` shows no modified or untracked corpus file left behind.
    - `git diff --name-only` across the phase's commits lists exactly the seven expected paths plus
      this phase's own planning artifacts; nothing under `scripts/`, no `docs/_registry/RE-index.md`,
      no `docs/index.md`, no `docs/operations/00-index.md`.
    - `grep -c '^| RE-' docs/_registry/RE-index.md` still returns **225**.
    - `node scripts/pipeline/build-filename-map.mjs --self-test` and
      `node scripts/pipeline/build-publish-bundle.mjs --self-test` both pass at their 225-row
      assertions.
  </acceptance_criteria>
  <done>
    Every gate is back at its phase-start baseline with C17 up exactly one file; the three guides and
    the matrix are structurally identical where they must be; the blast-radius fence held across the
    whole phase; and the closing evidence, the accepted inconsistencies and the Phase 152 hand-forward
    are recorded.
  </done>
</task>

</tasks>

<threat_model>
Documentation altitude, ASVS level 1, block on `high`. This plan edits a file that a previous phase
shipped and verified, so the dominant risks are regression risks rather than new-content risks.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The domain router to the reader | `00-overview.md` is the entry point for every reader of this domain. A wrong routing claim here misdirects a reader before they reach any guide. |
| A previous phase's verified content to this phase's edit | Phase 149 shipped and verified this file. Every byte changed here is a byte that passed a verification this phase is now re-opening. |
| A moved quote to its evidence line | Removing a blockquote without removing its evidence line leaves a citation pointing at nothing. |
| Prose text to the prose gates | A link or URL change is a prose change in this repository, and prose guards have gone red after many green plans on exactly that. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-150-26 | Tampering | The lines-128-130 replacement leaving a dangling sentence fragment | high | mitigate | The measured coordinates are given with the exact text that begins and ends each line, the action warns that line 128 opens mid-sentence with a clause that must survive, and an acceptance criterion requires the preceding sentence to still read complete. No validator in this repository reads prose, so a broken sentence would ship invisibly — this is the single highest-probability defect in the plan. |
| T-150-27 | Repudiation | An evidence line orphaned by removing the blockquote it evidenced | high | mitigate | D-36's reduction removes each blockquote **and** its `**Source:**` line together, and an acceptance criterion requires every remaining evidence line to sit within three lines of a blockquote. |
| T-150-28 | Information disclosure | A custody quote lost rather than moved | high | mitigate | The acceptance criteria check both sides: the HP cloud-vault string must return 0 in the overview and 1 in the HP guide. A quote that vanishes from both is the failure mode; a quote in exactly one place is the intent. |
| T-150-29 | Spoofing | Shipping the stale HP-versus-Dell UI asymmetry | medium | mitigate | The U-6 qualifier is required by an acceptance criterion, and the `admin.hp.com` fact is required to survive — the correction qualifies rather than deletes, so neither half is lost. |
| T-150-30 | Tampering | Editing the Dell quote that the falsification re-fetch found unchanged | medium | mitigate | An explicit prohibition plus a diff-based acceptance criterion: the Dell blockquote must not appear as a removed line in the commit diff. An unnecessary edit to a verified quote is its own risk. |
| T-150-31 | Denial of service | A prose or link gate going red after the edit | medium | mitigate | All four gate classes plus the apex are re-run after this commit, separately from the verifier, and the apex must return to exactly 101/0/0. Running them only at the end of the phase is what allows a regression to be attributed to the wrong change. |
| T-150-32 | Elevation of privilege | An H2 or anchor added or removed, breaking a slug Phase 151 must match | high | mitigate | An explicit prohibition plus acceptance criteria pinning the file at exactly 9 H2s and 7 anchors, unchanged. |
| T-150-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase; `150-RESEARCH.md`'s Package Legitimacy Audit records it as not applicable. No legitimacy checkpoint is required. |
</threat_model>

<verification>
- `grep -c 'are not yet written in this corpus' docs/operations/firmware-bios/00-overview.md` = 0.
- `grep -c 'Dell wants a virgin BIOS; Lenovo needs a provisioned one' docs/operations/firmware-bios/00-overview.md` = 1.
- The HP cloud-vault string returns 0 in the overview and 1 in the HP guide.
- `grep -c '^## '` = 9 and `grep -c '<a id='` = 7 in the overview, unchanged.
- `node scripts/validation/c17-eee-contract.mjs` yields `235 files checked, 0 with violations`.
- `node scripts/validation/check-nav-hub-links.mjs` yields `0 hub-presence failure(s), 0 corpus-link
  failure(s), 0 total`.
- `node scripts/validation/check-phase-54.mjs` yields 0 failed.
- `node scripts/validation/v1.20-milestone-audit.mjs` exits 0.
- `node scripts/validation/check-phase-49.mjs` yields `22 passed, 0 failed`.
- `node scripts/validation/check-phase-144.mjs` yields `101 PASS, 0 FAIL, 0 SKIPPED`.
- `git log -1 --name-only` shows exactly one file.
</verification>

<success_criteria>
- `docs/operations/firmware-bios/00-overview.md` no longer claims the vendor guides do not exist, and
  the replacement leaves no dangling sentence fragment.
- The overview links to all three guides and to the matrix, with correct relative depths.
- `Dell wants a virgin BIOS; Lenovo needs a provisioned one.` is present in the overview, which is the
  placement Success Criterion 2 grades.
- The HP and Lenovo custody quotes and their evidence lines are gone from the overview and present in
  their guides; the Dell quote and its evidence line are byte-unchanged.
- The `admin.hp.com` claim is qualified with HP Connect's Partner-portals-tab presence rather than
  deleted or left stale.
- Frontmatter dates advanced with a 60-day interval; the 9 H2 / 7 anchor contract is unchanged.
- The file is committed alone as the phase's second commit, and every gate is back at its phase-start
  baseline with C17 up exactly one file.
</success_criteria>

<output>
Create `.planning/phases/150-per-oem-bios-guides-capability-matrix/150-05-SUMMARY.md` when done.
Record: all seven edit sites with their final line numbers, the commit SHA, every gate verdict, the
three-way and matrix H2 equality results, the whole-phase blast-radius diff, the five accepted
inconsistencies with their grounds, the measured three-tail-H2 correction to D-01's descriptive text,
and the Phase 152 hand-forward stated as a rule rather than a number.
</output>
