---
phase: 119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal
plan: 03
type: execute
wave: 3
depends_on: ["119-02"]
files_modified:
  - scripts/validation/check-phase-113.mjs
  - scripts/validation/check-phase-114.mjs
  - scripts/validation/check-phase-115.mjs
  - scripts/validation/check-phase-116.mjs
  - scripts/validation/check-phase-117.mjs
  - scripts/validation/check-phase-118.mjs
  - scripts/validation/check-phase-119.mjs
  - scripts/validation/_lib/frozen-at-close.mjs
  - .github/workflows/audit-harness-v1.15-integrity.yml
autonomous: true
requirements: [HARN-02, HARN-03]
must_haves:
  truths:
    - "check-phase-113..118 are six lightweight leaf validators (CHAIN_PHASES=[], CHAIN_SKIP=new Set([])) whose needles derive from each phase's VERIFICATION.md"
    - "check-phase-119 is the apex: CHAIN_PHASES=[48..118] (71 entries ending 118), CHAIN_SKIP=new Set([]), HARNESS=v1.15-milestone-audit.mjs"
    - "frozen-at-close.mjs gains V114='7d922a7' + readAtV114Close, back-anchored to the v1.14 close-gate"
    - "audit-harness-v1.15-integrity.yml is the 12th coexistence workflow with paths: repointed to v1.15-*, dual-apex structure and LF-fidelity flags preserved verbatim"
    - "Atom 2 lands as ONE indivisible commit, then is pushed to a branch/PR to trigger the authoritative Axis-2 GHA re-audit"
  artifacts:
    - path: "scripts/validation/check-phase-119.mjs"
      provides: "chain apex, 71-entry chain 48..118"
      contains: "check-phase-118"
    - path: "scripts/validation/_lib/frozen-at-close.mjs"
      provides: "V114 frozen-close pin"
      contains: "V114"
    - path: ".github/workflows/audit-harness-v1.15-integrity.yml"
      provides: "12th CI coexistence workflow, Linux-authoritative Axis 2"
      contains: "v1.15-"
  key_links:
    - from: "check-phase-119.mjs HARNESS"
      to: "scripts/validation/v1.15-milestone-audit.mjs"
      via: "AUDIT-HARNESS subprocess-spawn"
      pattern: "v1.15-milestone-audit.mjs"
    - from: "audit-harness-v1.15-integrity.yml linux-chain-ubuntu-latest"
      to: "check-phase-119.mjs (recurses 48..118)"
      via: "node scripts/validation/check-phase-119.mjs --verbose"
      pattern: "check-phase-119.mjs"
    - from: "frozen-at-close.mjs V114"
      to: "v1.14 close-gate commit 7d922a7"
      via: "MILESTONE_CLOSE_SHAS lookup"
      pattern: "7d922a7"
---

<objective>
Atom 2 (SC2, HARN-02 + HARN-03) — ONE indivisible commit authoring the seven new chain validators, the V114 frozen-close pin, and the 12th CI coexistence workflow; then PUSH to trigger the authoritative Axis-2 GHA re-audit.

Purpose: This atom carries the per-phase regression guards (113-118 leaves) + the apex (119, CHAIN_PHASES=[48..118]) that together prove the frozen-surface re-baseline atomically; the V114='7d922a7' pin (back-anchored to v1.14's close-gate); and the CI workflow whose push is the load-bearing Axis-2 trigger (D-03 ordering gate — Atom 2 must be pushed FIRST for Axis 2 to run).

Output: nine files, ONE commit `docs(119-03): Atom 2 — check-phase-113..119 + frozen-at-close V114 + v1.15 CI workflow`, then a push to a branch/PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-CONTEXT.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-RESEARCH.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-PATTERNS.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-01-SUMMARY.md
@.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author check-phase-113..118 (6 lightweight leaf validators, needles from each VERIFICATION.md)</name>
  <files>scripts/validation/check-phase-113.mjs, scripts/validation/check-phase-114.mjs, scripts/validation/check-phase-115.mjs, scripts/validation/check-phase-116.mjs, scripts/validation/check-phase-117.mjs, scripts/validation/check-phase-118.mjs</files>
  <read_first>
    - scripts/validation/check-phase-101.mjs (in full, 153 lines — the lightweight leaf template: presence() helper, needle checks, V-NN-SELF dual-invariant, runner loop)
    - .planning/phases/119-.../119-PATTERNS.md §Pattern 5 (full structural template + per-file substitutions + the check-phase-115 special note)
    - .planning/phases/119-.../119-RESEARCH.md §Code Examples #5 (per-phase needle sourcing) + Assumption A5 (116/117/118 VERIFICATION.md MUST be read before authoring their needles)
    - EACH of: .planning/phases/113-*/113-VERIFICATION.md, 114-*/114-VERIFICATION.md, 115-*/115-VERIFICATION.md, 116-*/116-VERIFICATION.md, 117-*/117-VERIFICATION.md, 118-*/118-VERIFICATION.md — the "Required Artifacts" / "Observable Truths" tables are the authoritative needle source (file paths, byte-counts, verbatim landing strings). 116/117/118 were NOT read in full during research — READ THEM NOW.
  </read_first>
  <action>
    Author six leaf validators following check-phase-101.mjs verbatim in structure. For EACH phase substitute: the phase number in every string literal (101→113..118), the DELIVERABLE_* path constants + one presence() call per deliverable file, the landed-content needle strings sourced from that phase's own *-VERIFICATION.md, the header one-line phase-name summary, and a V-NN-SELF check asserting CHAIN_PHASES does NOT include NN AND CHAIN_SKIP.size===0. In EVERY leaf file keep `const CHAIN_PHASES = []` and `const CHAIN_SKIP = new Set([])` — chain logic lives ONLY in the apex (119).

    Needle guidance per phase: 113 — pipeline presence (scripts/pipeline/lib/ooxml.mjs, guard-docx.mjs, convert.ps1) + version-pin "3.7.0.2" + README SC3 deployment-policy heading. 114 — docs/_standards/EEE-SOP-standard.md presence + D1-map coverage (the ~20 platform variants) + docs/_registry/RE-index.md presence + templates. 115 — c17-eee-contract.mjs presence + fixture presence + assert the standalone script self-tests; do NOT assert chain registration (115-VERIFICATION.md's Deferred Items table explicitly assigns "C17 registered in the harness chain" to Phase 119 — that lives in Atom 1's C17 fold, not here). 116/117/118 — each phase's registry-flip-to-Approved event + C17-exit-0 confirmation (the "SC-[batch]-ENROLL / SC-[batch]-C17" needle convention from 118-PATTERNS.md); derive exact needle strings from the three VERIFICATION.md files you read.

    Do NOT add the NESTED guard to leaf files — they have no nested re-invocation of a frozen audit (that guard only matters for the apex). Copy the pass/fail/skip runner loop verbatim; only the console.log banner phase number changes.
  </action>
  <verify>
    <automated>bash -c 'for n in 113 114 115 116 117 118; do node scripts/validation/check-phase-$n.mjs >/dev/null 2>&1 || { echo "check-phase-$n FAIL"; exit 1; }; done; echo LEAVES_OK'</automated>
  </verify>
  <acceptance_criteria>
    - All six files `scripts/validation/check-phase-113.mjs`..`check-phase-118.mjs` exist
    - Each contains `const CHAIN_PHASES = []` and `const CHAIN_SKIP = new Set([])` and a V-NN-SELF check
    - `node scripts/validation/check-phase-113.mjs` (and 114..118) each exit 0 on the current tree
    - check-phase-115.mjs asserts standalone c17-eee-contract.mjs presence/self-test and does NOT assert chain registration
    - Needle strings in each file trace to that phase's *-VERIFICATION.md (not invented)
  </acceptance_criteria>
  <done>Six leaf validators exist, each exits 0, each with empty CHAIN_PHASES/CHAIN_SKIP and phase-sourced needles; 115 asserts standalone-not-chain.</done>
</task>

<task type="auto">
  <name>Task 2: Author check-phase-119 apex (CHAIN_PHASES=[48..118]) + add V114 pin to frozen-at-close.mjs</name>
  <files>scripts/validation/check-phase-119.mjs, scripts/validation/_lib/frozen-at-close.mjs</files>
  <read_first>
    - scripts/validation/check-phase-112.mjs (in full, 197 lines — the apex fork-base: HARNESS const, CHAIN_PHASES [48..111], NESTED-aware CHAIN-NN loop, AUDIT-HARNESS check, V-NN-SELF, V-NN-AUDIT archive-path check)
    - .planning/phases/119-.../119-PATTERNS.md §Pattern 6 (the exact apex substitutions + the [48..N-1] invariant + CHAIN_SKIP-empty invariant) + §Pattern 4 (frozen-at-close V114 insertion point + export + verification gate) + §Shared Patterns "NESTED-guard doctrine" + "Back-anchor invariant"
    - scripts/validation/_lib/frozen-at-close.mjs (in full, 81 lines — the V113 entry + readAtV113Close export, the single-entry pin pattern)
    - scripts/validation/_lib/archive-path.mjs (30 lines — resolveArchivedPhasePath; milestoneRoots stays ['v1.14-phases'])
    - .planning/phases/119-.../119-01-SUMMARY.md (the confirmed V114=7d922a7 disambiguation)
  </read_first>
  <action>
    Fork scripts/validation/check-phase-112.mjs → scripts/validation/check-phase-119.mjs with these substitutions: HARNESS → 'scripts/validation/v1.15-milestone-audit.mjs'; CHAIN_PHASES → the full 71-entry integer array 48 through 118 inclusive (append 112,113,114,115,116,117,118 to the v1.14 [48..111] array) — the [48..N-1] invariant means N=119 so the apex EXCLUDES 119; CHAIN_SKIP stays `new Set([])` (hard, non-negotiable — V-119-SELF asserts size 0; adding entries to force green is the forbidden GA3-C self-disqualifier). Repoint every "112" token to "119" in the header, the V-119-SELF check (assert CHAIN_PHASES does NOT include 119), the range comment [48..111]→[48..118], and the V-119-AUDIT check whose resolveArchivedPhasePath targets "119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-VERIFICATION.md" with milestoneRoots ['v1.14-phases'] (v1.15-phases will not exist as an archive target until /gsd-complete-milestone runs, which is AFTER this phase — so keep the current default). Preserve the NESTED guard on BOTH the CHAIN-NN loop and the AUDIT-HARNESS check verbatim (its incompleteness caused v1.14's first-run RED). Preserve the isPeer>=67 600s subTimeout logic and the execFailDetail failure formatting.

    Note: V-119-AUDIT will PASS-via-skip until 119-VERIFICATION.md is authored at close-gate time (mirrors 112's "not yet authored" skip) — this is expected, not a failure.

    In scripts/validation/_lib/frozen-at-close.mjs: insert the map entry `V114: '7d922a7',` (with the single-entry comment: "Phase 112 Plan 112-05 close-gate — v1.14 milestone close-gate; atom == close-gate; same single-entry pattern as V18..V113") immediately AFTER the V113 entry and BEFORE the `// V14 omitted` comment block; append `export const readAtV114Close = (p) => readAtClose('V114', p);` immediately after the readAtV113Close export. Before finalizing, re-confirm 7d922a7 resolves to the substantive close-gate via `git log -1 --format=%s 7d922a7` (message contains "MILESTONE CLOSE") — do NOT pin f3959c8 (the ambiguous follow-up). Back-anchor invariant: V114 references a PAST close SHA; there is NO V115 pin this phase (that is v1.16's job).
  </action>
  <verify>
    <automated>bash -c 'node -e "const m=require(\"fs\").readFileSync(\"scripts/validation/check-phase-119.mjs\",\"utf8\"); const arr=m.match(/CHAIN_PHASES\s*=\s*\[([^\]]*)\]/s)[1].split(\",\").filter(x=>x.trim()).length; if(arr!==71){console.error(\"CHAIN_PHASES has \"+arr+\" entries, expected 71\");process.exit(1)}" && grep -q "48,49,50" scripts/validation/check-phase-119.mjs && grep -q ",118]" scripts/validation/check-phase-119.mjs && grep -q "V114: .7d922a7." scripts/validation/_lib/frozen-at-close.mjs && grep -q "readAtV114Close" scripts/validation/_lib/frozen-at-close.mjs && echo APEX_PIN_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `check-phase-119.mjs` CHAIN_PHASES has exactly 71 numeric entries, starting 48 and ending 118; CHAIN_SKIP is `new Set([])`
    - `check-phase-119.mjs` HARNESS === 'scripts/validation/v1.15-milestone-audit.mjs'; V-119-SELF asserts 119 is absent from CHAIN_PHASES
    - The NESTED guard is present on both the CHAIN-NN loop and AUDIT-HARNESS check
    - `frozen-at-close.mjs` contains `V114: '7d922a7'` and `readAtV114Close`; NO `V115` entry exists
    - `git log -1 --format=%s 7d922a7` contains "MILESTONE CLOSE"
  </acceptance_criteria>
  <done>Apex check-phase-119 forks 112 with 71-entry chain 48..118, empty CHAIN_SKIP, v1.15 HARNESS, NESTED guards preserved; V114='7d922a7' pin + export added and re-verified; no V115.</done>
</task>

<task type="auto">
  <name>Task 3: Author audit-harness-v1.15-integrity.yml (12th workflow), commit Atom 2 as ONE commit, push to trigger Axis 2</name>
  <files>.github/workflows/audit-harness-v1.15-integrity.yml</files>
  <read_first>
    - .github/workflows/audit-harness-v1.14-integrity.yml (in full, ~313 lines — the copy source: parse/path-match/harness-run jobs, linux-chain-ubuntu-latest, per-phase check jobs, rotting-external-quarterly, pin-helper-advisory)
    - .planning/phases/119-.../119-PATTERNS.md §Pattern 7 (exact repoints; the linux-chain flags to preserve verbatim; the 12-job→7-job block replacement; the dual-apex anti-pattern) + §RESEARCH #7 (paths: filter repoint; header 11th→12th)
    - .planning/phases/119-.../119-RESEARCH.md §Common Pitfall 6 (do NOT deduplicate the standalone check-phase-119 job vs linux-chain-ubuntu-latest — both run the full apex; this is intentional and audited) + §D-119-2 (paths: filter MUST repoint or the workflow never fires)
  </read_first>
  <action>
    Copy .github/workflows/audit-harness-v1.14-integrity.yml → .github/workflows/audit-harness-v1.15-integrity.yml. Repoint: `name:` v1.14→v1.15; ALL four `paths:` entries (`scripts/validation/v1.14-*`→`v1.15-*`; the workflow filename; and the two milestone-audit doc paths `.planning/milestones/v1.14-MILESTONE-AUDIT.md`/`v1.14-DEFERRED-CLEANUP.md`→`v1.15-*`) — `scripts/validation/check-phase-*.mjs` is already a wildcard, leave it. Update header comments "11th coexistence workflow"→"12th", "check-phase-101..112"→"check-phase-113..119", and the frozen-harness list to add "+ v1.14". Repoint every `v1.14-audit-allowlist.json`/`v1.14-milestone-audit.mjs` string literal inside the parse/path-match/harness-run job steps to v1.15. In the linux-chain-ubuntu-latest job, change the apex command from `check-phase-112.mjs` to `check-phase-119.mjs` and the notice/comment `[48..111]`→`[48..118]`. Replace the 12-job block (check-phase-101..112) with a 7-job block (check-phase-113..119), same per-job template, substituting the phase number in the 4 places per job (job key, name:, step name:, run:). In the trailing jobs repoint only the sidecar JSON path string to v1.15-audit-allowlist.json; leave the markdown-link-check@3.14.2 pin and regenerate-supervision-pins.mjs invocation unchanged; leave the cron schedule lines unchanged.

    PRESERVE VERBATIM (D-119-2 MANDATORY rider): linux-chain-ubuntu-latest's `git config --global core.autocrlf false` set BEFORE checkout, `fetch-depth: 0`, `continue-on-error: false`, `timeout-minutes: 30`. PRESERVE the two apparently-redundant full-apex invocations (standalone check-phase-119 job + linux-chain job) — do NOT deduplicate and do NOT add CHECK_PHASE_NESTED=1 to either top-level GHA invocation (Pitfall 6).

    Then commit ALL NINE Atom-2 files (check-phase-113..119 [7], frozen-at-close.mjs, audit-harness-v1.15-integrity.yml) as ONE indivisible commit: `docs(119-03): Atom 2 — check-phase-113..119 + frozen-at-close V114 + v1.15 CI workflow`. Do NOT include any Atom-1 file. Then push to a branch/PR (this is the load-bearing D-03 ordering gate — the push triggers the authoritative Axis-2 GHA re-audit; the close-gate necessarily post-dates this push). Record the branch/PR and the resulting workflow run in the SUMMARY for Plan 119-04 to consume. Do NOT run the full apex chain locally on Windows (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01 at depth [48..118] — it gets killed at turn boundaries); authoritative apex execution is the Linux GHA run.
  </action>
  <verify>
    <automated>bash -c 'grep -q "v1.15-milestone-audit.mjs" .github/workflows/audit-harness-v1.15-integrity.yml && grep -q "check-phase-119.mjs" .github/workflows/audit-harness-v1.15-integrity.yml && grep -q "core.autocrlf false" .github/workflows/audit-harness-v1.15-integrity.yml && grep -q "fetch-depth: 0" .github/workflows/audit-harness-v1.15-integrity.yml && ! grep -q "v1.14-milestone-audit.mjs" .github/workflows/audit-harness-v1.15-integrity.yml && echo WORKFLOW_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/audit-harness-v1.15-integrity.yml` exists; `paths:` contains `scripts/validation/v1.15-*` and the v1.15 milestone-audit doc paths; ZERO `v1.14-milestone-audit.mjs`/`v1.14-audit-allowlist.json` references
    - The linux-chain job runs `check-phase-119.mjs` and preserves `core.autocrlf false` (before checkout), `fetch-depth: 0`, `continue-on-error: false`, `timeout-minutes: 30`
    - A standalone `check-phase-119` job AND the `linux-chain-ubuntu-latest` job both exist (dual-apex preserved, not deduplicated); no `CHECK_PHASE_NESTED=1` on either top-level invocation
    - The Atom-2 commit (`git show --stat HEAD`) contains exactly the nine Atom-2 files and NO Atom-1 file
    - The commit is pushed to a branch/PR and the triggered workflow run ID is recorded in the SUMMARY
  </acceptance_criteria>
  <done>The 12th CI workflow is authored (v1.15-repointed, LF-fidelity + dual-apex preserved); Atom 2 committed as one indivisible nine-file commit and pushed to trigger the authoritative Axis-2 GHA re-audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| apex validator → predecessor chain (48..118) | A wrong CHAIN_PHASES bound or a non-empty CHAIN_SKIP could silently pass an unproven chain (frozen-surface trust) |
| frozen-at-close V114 pin → git history | A wrong SHA reads the wrong frozen corpus for every future readAtV114Close consumer |
| CI paths: filter → Axis-2 trigger | An un-repointed paths: filter means the authoritative re-audit never fires (silent no-gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-03-CHAINBOUND | Tampering | check-phase-119 CHAIN_PHASES / CHAIN_SKIP | mitigate | Assert exactly 71 entries 48..118; CHAIN_SKIP empty; V-119-SELF hard-asserts 119 absent + size 0 |
| T-119-03-V114 | Tampering/Info-disclosure | frozen-at-close V114 pin | mitigate | Re-confirm 7d922a7 message contains "MILESTONE CLOSE"; reject f3959c8; back-anchor invariant (no V115) |
| T-119-03-PATHS | Tampering | CI paths: filter | mitigate | Repoint all four v1.14 path entries to v1.15; grep-assert zero v1.14 harness references remain; verify workflow fires on push |
| T-119-03-DEDUP | Tampering | dual-apex CI jobs | mitigate | Preserve both full-apex invocations verbatim; no CHECK_PHASE_NESTED=1 on top-level jobs (Pitfall 6) |
| T-119-03-ATOMFOLD | Tampering | Atom-2 commit contents | mitigate | `git show --stat` asserts exactly the 9 Atom-2 files, no Atom-1 leak |
| T-119-03-SC | Tampering | package installs | accept | No package-manager installs this phase (node built-ins + actions pinned unchanged) |
</threat_model>

<verification>
- All six leaves + apex exit 0 locally (single-validator runs; NOT the full apex chain on Windows)
- CHAIN_PHASES has 71 entries ending 118; CHAIN_SKIP empty
- frozen-at-close contains V114='7d922a7' + readAtV114Close; no V115
- Workflow repointed to v1.15-*, LF-fidelity + dual-apex preserved; zero v1.14 harness references
- Atom-2 commit is exactly 9 files; pushed; Axis-2 run ID recorded
</verification>

<success_criteria>
Atom 2 ships as ONE indivisible commit (7 chain validators + V114 pin + 12th CI workflow) and is pushed to trigger the authoritative Axis-2 GHA re-audit. Apex chain 48..118 (71 entries), CHAIN_SKIP empty, V114='7d922a7' back-anchored.
</success_criteria>

<output>
Create `.planning/phases/119-frozen-surface-re-baseline-13th-path-a-lineage-bump-terminal/119-03-SUMMARY.md` when done, recording the Atom-2 commit SHA, the push branch/PR, and the triggered Axis-2 workflow run ID.
</output>
