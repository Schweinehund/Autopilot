---
phase: 52
plan: 02
type: execute
wave: A
depends_on: []
files_modified:
  - docs/l2-runbooks/25-linux-agent-investigation.md
autonomous: true
requirements: [LIN-12]
requirements_addressed: [LIN-12]

must_haves:
  truths:
    - "docs/l2-runbooks/25-linux-agent-investigation.md exists with frontmatter (platform: Linux, audience: L2, applies_to: all, last_verified: 2026-04-27, review_by: 2026-06-26) per CONTEXT D-04 + V-52-05 + ROADMAP:225 SC#4"
    - "Runbook 25 uses anchor-indexed H2-per-trap shape mirroring docs/l1-runbooks/25-android-compliance-blocked.md per CONTEXT D-05 + V-52-11 — 4 H2 headings with literal anchor strings: `## Trap A: Ubuntu HWE vs GA Kernel Mismatch {#trap-a-kernel-track}`, `## Trap B: Snap-vs-Deb Delivery Path Confusion {#trap-b-delivery-path}`, `## Trap C: Service-State User-Scope Confusion {#trap-c-service-state}`, `## Trap D: Identity Broker v2.0.2+ Re-enrollment {#trap-d-identity-broker}`"
    - "Each Trap H2 body contains in order: `**Entry condition:**` paragraph, `### Symptom`, `### Investigation Steps` (numbered with fenced code blocks; NO `> **Say to the user:**` per D-08), `### Resolution`, `### Verification` per CONTEXT D-05"
    - "Trap A body contains literals `Ubuntu HWE` AND `GA kernel` AND `uname -r` per ROADMAP:223 SC#2 + V-52-12"
    - "Trap B body contains literals `snap` AND `deb` per ROADMAP:223 SC#2 + V-52-14 (snap-vs-deb detection)"
    - "Trap C body contains literals `systemctl status intune-agent` AND `systemctl enable --user --now intune-agent.timer` per ROADMAP:223 SC#2 + V-52-13"
    - "Runbook 25 contains at least 3 literal cross-links to Phase 51 L1 cause anchors per CONTEXT D-09 + V-52-15 + Phase 51 DPO-01 inheritance — minimum viable set: `30-linux-enrollment-failed.md#cause-a-package-install`, `30-linux-enrollment-failed.md#cause-b-sign-in-failure`, `33-linux-agent-service-failure.md`"
    - "Runbook 25 does NOT contain `> **Say to the user:**` blockquotes per CONTEXT D-08 + V-52-16 + CDI-Phase52-05 (L2-direct framing; L1-pattern forbidden — 0/23 occurrences across L2 corpus)"
    - "Runbook 25 does NOT contain literal `Require device to be marked as compliant` per V-52-21 (PITFALL-2 negative regression-guard inherited from Phase 51 V-51-19)"
    - "Runbook 25 honors read-vs-write apt distinction per V-52-17: NO `sudo apt list`, NO `sudo dpkg -l`, NO `sudo systemctl --user`, NO `sudo journalctl --user`; `sudo journalctl -u microsoft-identity-device-broker` is permitted (system-scope read); state-changing sudo commands appear ONLY in `### Resolution` H3 sections"
    - "Runbook 25 contains NO mode-axis tokens per V-52-20 (no BYOD/COBO/COPE/Dedicated/ZTE/AOSP/COSU)"
    - "Runbook 25 contains at least 1 link to `../_glossary-linux.md#<anchor>` per V-52-18"
    - "Runbook 25 contains NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens per V-52-22"
    - "Runbook 25 references Runbook 24 in Context H2 as prerequisite per Pattern Map integration-points line 254"
  artifacts:
    - path: "docs/l2-runbooks/25-linux-agent-investigation.md"
      provides: "Linux L2 agent investigation guide — anchor-indexed 4-trap structure"
      contains: ["platform: Linux", "audience: L2", "last_verified: 2026-04-27", "review_by: 2026-06-26", "> **Platform gate:**", "# Linux Agent Investigation", "**From L1 escalation?**", "## Trap A: Ubuntu HWE vs GA Kernel Mismatch {#trap-a-kernel-track}", "## Trap B: Snap-vs-Deb Delivery Path Confusion {#trap-b-delivery-path}", "## Trap C: Service-State User-Scope Confusion {#trap-c-service-state}", "## Trap D: Identity Broker v2.0.2+ Re-enrollment {#trap-d-identity-broker}", "Ubuntu HWE", "GA kernel", "uname -r", "systemctl status intune-agent", "systemctl enable --user --now intune-agent.timer", "30-linux-enrollment-failed.md#cause-a-package-install", "30-linux-enrollment-failed.md#cause-b-sign-in-failure", "33-linux-agent-service-failure.md", "../_glossary-linux.md#"]
  key_links:
    - from: "docs/l2-runbooks/25-linux-agent-investigation.md"
      to: "docs/l1-runbooks/30-linux-enrollment-failed.md#cause-a-package-install"
      via: "From L1 escalation? cross-link block per CONTEXT D-09 + DPO-01 inheritance"
      pattern: "30-linux-enrollment-failed.md#cause-a-package-install"
    - from: "docs/l2-runbooks/25-linux-agent-investigation.md"
      to: "docs/l1-runbooks/33-linux-agent-service-failure.md"
      via: "Trap C primary L2 escalation target from L1 Runbook 33"
      pattern: "33-linux-agent-service-failure.md"
    - from: "docs/l2-runbooks/25-linux-agent-investigation.md"
      to: "docs/l2-runbooks/24-linux-log-collection.md"
      via: "## Context H2 prerequisite reference per Pattern Map integration-points"
      pattern: "24-linux-log-collection.md"
    - from: "docs/l2-runbooks/25-linux-agent-investigation.md"
      to: "docs/_glossary-linux.md#microsoft-identity-broker"
      via: "Trap D glossary back-link per CD-07"
      pattern: "../_glossary-linux.md#microsoft-identity-broker"
---

<objective>
Author `docs/l2-runbooks/25-linux-agent-investigation.md` — the Linux L2 agent investigation guide (LIN-12 part 2; SC#2). Anchor-indexed H2-per-trap shape mirroring `docs/l1-runbooks/25-android-compliance-blocked.md` per CONTEXT D-05. Four traps (locked anchors per D-06):

- Trap A: Ubuntu HWE vs GA Kernel Mismatch `{#trap-a-kernel-track}`
- Trap B: Snap-vs-Deb Delivery Path Confusion `{#trap-b-delivery-path}`
- Trap C: Service-State User-Scope Confusion `{#trap-c-service-state}`
- Trap D: Identity Broker v2.0.2+ Re-enrollment `{#trap-d-identity-broker}`

Purpose: Satisfies SC#2 (covers HWE/GA + `uname -r` + snap-vs-deb + `systemctl status intune-agent` + `systemctl enable --user --now intune-agent.timer` per ROADMAP:223). Consumes Phase 51 L1 cause anchors as locked surfaces per DPO-01 (≥3 cross-links per V-52-15). L2-direct framing per D-08 — NO L1 narration pattern (V-52-16; CDI-Phase52-05 GA-4-COUP-2 retained coupling defect). Trap content is cross-tier-distinct from L1 cause topology per CDI-Phase52-03 (Anti-Pattern 1).

Output: 1 markdown file at `docs/l2-runbooks/25-linux-agent-investigation.md` that passes V-52-02, V-52-05 (RB25), V-52-11, V-52-12, V-52-13, V-52-14, V-52-15, V-52-16 (RB25), V-52-17 (RB25), V-52-18 (RB25), V-52-20 (RB25), V-52-21, V-52-22 (RB25) in `check-phase-52.mjs`.

DO NOT commit yet — atomic commit owned by 52-05 per CONTEXT D-13 + DPO-Phase52-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-CONTEXT.md
@.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-RESEARCH.md
@.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-PATTERNS.md
@docs/l1-runbooks/25-android-compliance-blocked.md
@docs/l2-runbooks/19-android-enrollment-investigation.md
@docs/l1-runbooks/30-linux-enrollment-failed.md
@docs/l1-runbooks/33-linux-agent-service-failure.md
@docs/_glossary-linux.md
@docs/linux-lifecycle/01-linux-prerequisites.md

<interfaces>
The runbook consumes:

- 25-android-compliance-blocked.md analog (CONTEXT D-05; verified read):
  - Lines 1-7: frontmatter shape (audience: L1 - INVERT to L2; platform: Android - INVERT to Linux)
  - Lines 33-44: anchor-indexed H2 list (How to Use This Runbook framing)
  - Lines 48-93: Cause A body shape (Entry condition paragraph, Symptom H3, L1 Triage Steps -> Investigation Steps for L2, Admin Action Required -> Resolution, Escalation -> Verification)
  - Line 35 verbatim "Causes are independently diagnosable" framing -> "Traps are independently diagnosable" for L2
- 19-android-enrollment-investigation.md analog (CD-04; verified read):
  - Lines 19-25: "From L1 escalation?" block pattern (top-of-runbook reference block)
- Phase 51 L1 cause anchors (DPO-01 LOCKED surfaces — full enumeration from CONTEXT canonical_refs lines 150-153):
  - `../l1-runbooks/30-linux-enrollment-failed.md#cause-a-package-install` (Trap A and Trap B return-route)
  - `../l1-runbooks/30-linux-enrollment-failed.md#cause-b-sign-in-failure` (Trap D return-route)
  - `../l1-runbooks/30-linux-enrollment-failed.md#cause-c-enrollment-timeout` (Trap C return-route)
  - `../l1-runbooks/31-linux-compliance-non-compliant.md#cause-d-custom-compliance-failure` (optional eligible — RESEARCH Area 8 line 466)
  - `../l1-runbooks/33-linux-agent-service-failure.md` (Trap C primary L2 escalation target — single-cause file, no anchor)
- Phase 49 anchors: `docs/_glossary-linux.md#microsoft-identity-broker`, `#hwe-kernel`, `#ga-kernel`, `#deb-repository`, `#snap`, `#systemd`, `#intune-agent.timer`, `#dpkg`, `#apt` (V-52-18 — at least 1 link required)
- Phase 49 version matrix: `docs/linux-lifecycle/01-linux-prerequisites.md` (Ubuntu 22.04 GA = 5.15 / HWE = 6.8; Ubuntu 24.04 GA = 6.8 / HWE = 6.11+; Trap A version-band reference)

The runbook provides (consumed downstream):

- 4 trap anchors `#trap-a-kernel-track`, `#trap-b-delivery-path`, `#trap-c-service-state`, `#trap-d-identity-broker` for Phase 58 4-platform comparison Linux row + Phase 59 hub navigation (DPO-Phase52-02 / DPO-Phase52-03)

Author discretion (CD per CONTEXT):

- CD-02: Exact ordering of Trap A/B/C/D — author may reorder if telemetry suggests different real-world frequency; validator pins LITERAL anchors, not ordering
- CD-04: Whether Runbook 25 includes a top-of-runbook "From L1 escalation?" block OR per-Trap embedded references — recommend top-of-runbook block (mirror 19-android-enrollment-investigation.md:19-25) for parity with anchor-indexed structure; validator V-52-15 accepts ≥3 cross-link tokens regardless of placement
- CD-05: Per-Trap body length — proportionate to L2-actionable surface depth (Phase 51 Cause A ~50 lines / Cause C ~40 / Cause B+D ~25-30)
- CD-06: Mermaid usage — optional (e.g., a kernel-track-decision Mermaid in Trap A); validator does NOT assert Mermaid presence
- CD-07: Trap D cross-link to Phase 50 LIN-05 admin callout in `01-intune-linux-agent.md` OR via `_glossary-linux.md#microsoft-identity-broker` — recommend glossary anchor
- CD-08: L2 callout label uniformity (`> **Note:**` vs `> **Source confidence:**`) — author discretion

Forbidden patterns (validator-enforced negative regression-guards):

- `> **Say to the user:**` (V-52-16 — L2 audience contract; 0 occurrences across 23 existing L2 runbooks)
- `Require device to be marked as compliant` (V-52-21 — PITFALL-2 inheritance from Phase 51 V-51-19; Trap D references CA assignment drift WITHOUT this exact phrase)
- `sudo apt list`, `sudo dpkg -l`, `sudo systemctl --user`, `sudo journalctl --user` (V-52-17 — DPO-Phase52-01)
- `BYOD`, `COBO`, `COPE`, `Dedicated`, `ZTE`, `AOSP`, `COSU` (V-52-20 — PITFALL-1 mode-axis regression-guard)
- `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` (V-52-22)

Cross-tier distinctness obligation (CDI-Phase52-03 — human verifiable):

- Trap A/B/C/D content documents L2 INVESTIGATION TOOLS AND TECHNIQUES (kernel commands, package-state queries, service-scope mechanics, dsreg invocation), NOT re-stated L1 user-facing symptoms (package-install symptom, sign-in failure symptom, agent-service-failure symptom)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author Runbook 25 Sections 1-4 (frontmatter + Platform gate + H1 + Context with From L1 escalation? block + How to Use This Runbook H2)</name>
  <files>docs/l2-runbooks/25-linux-agent-investigation.md</files>
  <read_first>
    - docs/l1-runbooks/25-android-compliance-blocked.md (PRIMARY analog — frontmatter lines 1-7; H1 line 11; Prerequisites line 24-31; How to Use This Runbook lines 33-44; Cause A H2 line 48 with anchor `{#cause-a-play-integrity-verdict-failure}`)
    - docs/l2-runbooks/19-android-enrollment-investigation.md (SECONDARY analog — From L1 escalation? block at lines 19-25)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-PATTERNS.md (Section "docs/l2-runbooks/25-linux-agent-investigation.md" lines 236-330)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-CONTEXT.md (D-05 anchor-indexed shape; D-06 anchor naming; D-07 ordering by likelihood; D-08 L2-direct framing; D-09 L1 cross-link reference-only; CD-04 placement choice)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-RESEARCH.md (Research Area 8 lines 452-476 — Trap-to-L1-Cause mapping)
  </read_first>
  <behavior>
    - File `docs/l2-runbooks/25-linux-agent-investigation.md` exists
    - Frontmatter contains `platform: Linux`, `audience: L2`, `applies_to: all`, `last_verified: 2026-04-27`, `review_by: 2026-06-26`
    - `> **Platform gate:**` blockquote appears immediately after frontmatter close
    - H1 `# Linux Agent Investigation` appears once
    - `**From L1 escalation?**` reference block contains at least 3 literal cross-links to Phase 51 L1 anchors
    - `## How to Use This Runbook` H2 contains 4 in-page anchor links to `#trap-a-kernel-track`, `#trap-b-delivery-path`, `#trap-c-service-state`, `#trap-d-identity-broker`
    - `## Context` H2 references `24-linux-log-collection.md` as prerequisite
  </behavior>
  <action>
Create `docs/l2-runbooks/25-linux-agent-investigation.md` containing the following sections IN ORDER. Sections 5-9 (the 4 Trap H2s + Related Resources/Version History) are added in Tasks 2 and 3.

**Section 1: Frontmatter — paste this exact YAML block as the first 7 lines of the file (CONTEXT D-04 + V-52-05):**

```
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: all
audience: L2
platform: Linux
---
```

**Section 2: Platform gate blockquote — paste verbatim immediately after the frontmatter close (mirrors Runbook 24 + 18-android-log-collection.md line 9):**

```
> **Platform gate:** This guide covers Linux Intune client (`intune-portal`) L2 investigation via Microsoft Intune. For Windows Autopilot, see [Windows L2 Runbooks](00-index.md). For macOS ADE, see [macOS ADE Runbooks](00-index.md#macos-ade-runbooks). For iOS/iPadOS, see [iOS L2 Runbooks](00-index.md#ios-l2-runbooks). For Android Enterprise, see [Android L2 Runbooks](00-index.md#android-l2-runbooks).
```

**Section 3: H1 + Context H2 + From L1 escalation? block — paste verbatim (PATTERNS.md lines 286-296 adapted for Linux + per CD-04 top-of-runbook block placement):**

```
# Linux Agent Investigation

## Context

This runbook is the L2 trap-by-trap investigation guide for Linux Intune client (`intune-portal`) failures on Ubuntu 22.04/24.04 LTS. It is consumed AFTER log collection — start with [Linux Log Collection Guide (Runbook 24)](24-linux-log-collection.md) to gather `journalctl` output, file-based log snapshots, and `dpkg`/`apt` package-state queries before opening this runbook.

The four trap families documented below are cross-tier-distinct from Phase 51 L1 cause topology (Anti-Pattern 1 mitigation per CDI-Phase52-03). L1 causes describe user-facing symptoms (package install / sign-in / enrollment timeout / agent service failure); L2 traps describe investigation tools and techniques (kernel-track verification, snap-vs-deb delivery path detection, user-scope vs system-scope service mechanics, Identity Broker re-enrollment signals).

**From L1 escalation?** One of the Phase 51 Linux L1 runbooks has routed here. L1 collected the escalation packet: serial number, user UPN, distro+kernel version (`lsb_release -a` and `uname -r` output), `dpkg -l intune-portal` output, and device-side symptom description.

- L1 [30-linux-enrollment-failed.md#cause-a-package-install](../l1-runbooks/30-linux-enrollment-failed.md#cause-a-package-install) → [Trap A](#trap-a-kernel-track) (kernel-track mismatch causes deb install failure) or [Trap B](#trap-b-delivery-path) (snap-vs-deb delivery confusion)
- L1 [30-linux-enrollment-failed.md#cause-b-sign-in-failure](../l1-runbooks/30-linux-enrollment-failed.md#cause-b-sign-in-failure) → [Trap D](#trap-d-identity-broker) (Identity Broker v2.0.2+ re-enrollment)
- L1 [30-linux-enrollment-failed.md#cause-c-enrollment-timeout](../l1-runbooks/30-linux-enrollment-failed.md#cause-c-enrollment-timeout) → [Trap C](#trap-c-service-state) (service-state user-scope confusion)
- L1 [33-linux-agent-service-failure.md](../l1-runbooks/33-linux-agent-service-failure.md) → [Trap C](#trap-c-service-state) (primary L2 escalation target from Runbook 33)
- L1 [31-linux-compliance-non-compliant.md#cause-d-custom-compliance-failure](../l1-runbooks/31-linux-compliance-non-compliant.md#cause-d-custom-compliance-failure) → return to [Runbook 24 Section 2.2](24-linux-log-collection.md#section-2-file-based-paths) for `/var/log/intune-update.log` Bash exit-code reading
- No L1 escalation: begin at [Trap A](#trap-a-kernel-track) and work through independently
```

**Section 4: How to Use This Runbook H2 — paste verbatim (PATTERNS.md lines 309-316 adapted from 25-android-compliance-blocked.md:35):**

```
## How to Use This Runbook

Check the trap that matches your observation. Traps are independently diagnosable — you do not need to rule out prior traps. Each trap has its own Entry condition, Symptom, Investigation Steps, Resolution, and Verification subsections; choose the trap matching the L1 escalation packet (or the symptom you observed) and follow that trap's Investigation Steps.

- [Trap A: Ubuntu HWE vs GA Kernel Mismatch](#trap-a-kernel-track)
- [Trap B: Snap-vs-Deb Delivery Path Confusion](#trap-b-delivery-path)
- [Trap C: Service-State User-Scope Confusion](#trap-c-service-state)
- [Trap D: Identity Broker v2.0.2+ Re-enrollment](#trap-d-identity-broker)
```

DO NOT use the L1 narration blockquote pattern starting with `> **Say to the user:**` anywhere (V-52-16 — L2-direct framing only).

DO NOT prefix with sudo: `apt list`, `dpkg -l`, `systemctl --user`, `journalctl --user` (V-52-17 / DPO-Phase52-01).

DO NOT include any of: BYOD, COBO, COPE, Dedicated, ZTE, AOSP, COSU (V-52-20 — Linux has no enrollment-mode axis per Phase 49 whitelist-first).

DO NOT commit yet — atomic commit owned by 52-05.
  </action>
  <verify>
    <automated>test -f docs/l2-runbooks/25-linux-agent-investigation.md &amp;&amp; grep -c "30-linux-enrollment-failed.md#cause-" docs/l2-runbooks/25-linux-agent-investigation.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/l2-runbooks/25-linux-agent-investigation.md`
    - Frontmatter matches `/^platform: Linux\s*$/m` AND `/^audience: L2\s*$/m` AND `/^last_verified: 2026-04-27\s*$/m` AND `/^review_by: 2026-06-26\s*$/m` (V-52-05)
    - File contains H1 `# Linux Agent Investigation`
    - File contains H2 `## Context` AND H2 `## How to Use This Runbook`
    - File contains literal `**From L1 escalation?**` text
    - File contains at least 3 of these literal cross-link strings: `30-linux-enrollment-failed.md#cause-a-package-install`, `30-linux-enrollment-failed.md#cause-b-sign-in-failure`, `30-linux-enrollment-failed.md#cause-c-enrollment-timeout`, `33-linux-agent-service-failure.md` (V-52-15 floor)
    - File contains literal `24-linux-log-collection.md` (Context H2 prerequisite cross-reference)
    - File contains 4 in-page anchor links: `#trap-a-kernel-track`, `#trap-b-delivery-path`, `#trap-c-service-state`, `#trap-d-identity-broker` (How to Use This Runbook H2 list)
    - File does NOT contain literal L1 narration blockquote pattern (V-52-16)
    - File does NOT match regex `/\bsudo\s+apt\s+list\b/` AND does NOT match `/\bsudo\s+dpkg\s+-l\b/` AND does NOT match `/\bsudo\s+systemctl\s+--user\b/` AND does NOT match `/\bsudo\s+journalctl\s+--user\b/` (V-52-17 — partial scope; finalized after Task 2/3)
    - File does NOT contain any of: BYOD, COBO, COPE, Dedicated, ZTE, AOSP, COSU (V-52-20)
    - File does NOT contain any of: TBD, TODO, FIXME, XXX, PLACEHOLDER (V-52-22)
  </acceptance_criteria>
  <done>Sections 1-4 (frontmatter + Platform gate + H1 + Context with From L1 escalation? block + How to Use This Runbook) authored; ready for Task 2 (Trap A + Trap B); no commit (52-05 owns atomicity).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append Trap A H2 (kernel HWE/GA — V-52-12 SC#2 content) + Trap B H2 (snap-vs-deb — V-52-14 SC#2 content)</name>
  <files>docs/l2-runbooks/25-linux-agent-investigation.md</files>
  <read_first>
    - docs/l2-runbooks/25-linux-agent-investigation.md (verify Sections 1-4 from Task 1 are present)
    - docs/l1-runbooks/25-android-compliance-blocked.md (lines 48-93 — Cause A body shape: Entry condition + Symptom + Investigation/Triage Steps + Resolution/Admin Action + Verification/Escalation)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-PATTERNS.md (Trap A body lines 379-405; Trap B body lines 410-433)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-RESEARCH.md (Research Area 3 lines 264-298 — HWE/GA kernel matrix; Research Area 4 lines 308-343 — snap-vs-deb detection commands)
    - docs/linux-lifecycle/01-linux-prerequisites.md (Phase 49 version matrix — confirm GA/HWE bands for Trap A reference)
    - docs/_glossary-linux.md (verify anchors `#hwe-kernel`, `#ga-kernel`, `#deb-repository`, `#snap` exist for cross-references)
  </read_first>
  <behavior>
    - Trap A H2 has literal `## Trap A: Ubuntu HWE vs GA Kernel Mismatch {#trap-a-kernel-track}` (V-52-11)
    - Trap A body contains literal `Ubuntu HWE` AND `GA kernel` AND `uname -r` (V-52-12)
    - Trap A body has subsections: `**Entry condition:**`, `### Symptom`, `### Investigation Steps`, `### Resolution`, `### Verification`
    - Trap B H2 has literal `## Trap B: Snap-vs-Deb Delivery Path Confusion {#trap-b-delivery-path}` (V-52-11)
    - Trap B body contains literal `snap` AND `deb` (V-52-14)
    - Trap B body has the same 5 subsections
  </behavior>
  <action>
APPEND the following two H2 blocks to `docs/l2-runbooks/25-linux-agent-investigation.md` (immediately after the `## How to Use This Runbook` H2 from Task 1).

**Trap A — paste verbatim (RESEARCH Area 3 + PATTERNS.md lines 379-405; V-52-11 anchor + V-52-12 SC#2 literals):**

````
## Trap A: Ubuntu HWE vs GA Kernel Mismatch {#trap-a-kernel-track}

**Entry condition:** L1 escalation packet shows `uname -r` output with a kernel version that does not match the Ubuntu release's GA kernel band, AND the user reports `intune-portal` deb install failure or `intune-agent.timer` activation failure.

See [HWE kernel](../_glossary-linux.md#hwe-kernel) and [GA kernel](../_glossary-linux.md#ga-kernel) for the kernel-channel distinction. Phase 49 version matrix: Ubuntu 22.04 GA = 5.15 / HWE = 6.8; Ubuntu 24.04 GA = 6.8 / HWE = 6.11+ (24.04.3 HWE may be 6.14+).

### Symptom

- `intune-portal` deb install fails at `dpkg --configure` stage with PAM-module errors
- `intune-agent.timer` activation fails after a successful enrollment with no clear journalctl error in user-scope timer journal
- `lsb_release -rs` shows 22.04 but `uname -r` shows a kernel starting with `6.` (HWE backport from 24.04 GA)

### Investigation Steps

1. Verify kernel track on the device:

   ```bash
   uname -r
   lsb_release -rs
   ```

2. Cross-reference against the Linux version-track matrix (Phase 49 [01-linux-prerequisites.md](../linux-lifecycle/01-linux-prerequisites.md)):
   - Ubuntu 22.04 + GA kernel → `uname -r` returns `5.15.x-xxx-generic`
   - Ubuntu 22.04 + HWE kernel → `uname -r` returns `6.8.x-xxx-generic`
   - Ubuntu 24.04 + GA kernel → `uname -r` returns `6.8.x-xxx-generic`
   - Ubuntu 24.04 + HWE kernel → `uname -r` returns `6.11.x-xxx-generic` (or 6.14+ for 24.04.3)

3. Confirm package version compatibility against installed kernel:

   ```bash
   dpkg -l intune-portal | grep -A1 intune-portal
   ```

4. Pull recent dpkg.log entries for the install attempt timeline:

   ```bash
   grep -E "intune-portal|microsoft-identity-broker" /var/log/dpkg.log | tail -50
   ```

> **Note:** Microsoft Learn does not document specific HWE-kernel-related failure signatures for `intune-portal`. PITFALL-4 in `.planning/research/PITFALLS.md` establishes HWE divergence as a known risk; the L2-actionable signal is the `uname -r` output cross-referenced against the version-track matrix above.

### Resolution

If the user is on HWE kernel for Ubuntu 22.04 and the `intune-portal` deb install is failing, pin to GA kernel via:

```bash
sudo apt install linux-image-generic
```

Or, if HWE compatibility is required for other reasons, install the `intune-portal` deb against the HWE kernel:

```bash
sudo apt update
sudo apt install --reinstall intune-portal
```

Then reboot and re-attempt enrollment.

### Verification

```bash
uname -r
dpkg -l intune-portal | grep ^ii
systemctl --user status intune-agent.timer
```

Expected: `uname -r` matches GA or HWE band per the version-track matrix; `dpkg -l intune-portal` shows `ii` (installed+configured); `intune-agent.timer` is `active (waiting)`. If still failing, escalate to L1 [30-linux-enrollment-failed.md#cause-a-package-install](../l1-runbooks/30-linux-enrollment-failed.md#cause-a-package-install) for return-route triage.

````

**Trap B — paste verbatim (RESEARCH Area 4 + PATTERNS.md lines 410-433; V-52-11 anchor + V-52-14 snap/deb literals):**

````
## Trap B: Snap-vs-Deb Delivery Path Confusion {#trap-b-delivery-path}

**Entry condition:** L1 escalation packet shows enrollment failed at package-install stage AND `dpkg -l intune-portal` returns no rows OR shows an unexpected version. The user may have followed deprecated preview-era instructions referencing a snap installation path.

See [deb-repository](../_glossary-linux.md#deb-repository) and [snap](../_glossary-linux.md#snap) for delivery-path context. `intune-portal` is delivered EXCLUSIVELY as a deb from `packages.microsoft.com` per Phase 49 D-04 — there is no GA snap distribution.

### Symptom

- `dpkg -l intune-portal` returns no rows even though the user believes the agent is installed
- `which intune-portal` returns a path under `/snap/` instead of `/opt/microsoft/`
- `apt list --installed | grep intune-portal` returns empty but `snap list` shows an `intune-portal` entry
- Enrollment workflow fails because the snap-based binary is not the same as the GA deb-based agent

### Investigation Steps

1. Confirm whether deb is installed (read-only):

   ```bash
   apt list --installed | grep intune-portal
   dpkg -l intune-portal
   ```

   Expected GA output: `intune-portal/jammy,now 2.0.X amd64 [installed]` and `ii  intune-portal  2.0.X  amd64  ...` (`ii` = installed + configured).

2. Detect erroneous snap installation (negative-space check):

   ```bash
   snap list 2>/dev/null | grep -i intune
   ```

   Expected: no output. Unexpected: any snap entry indicates the user is on the deprecated preview path.

3. Verify binary path (deb vs snap):

   ```bash
   which intune-portal 2>/dev/null
   readlink -f $(which intune-portal 2>/dev/null) 2>/dev/null
   ```

   - GA deb path: `/opt/microsoft/intune-portal/bin/intune-portal` or similar system path
   - Snap path: contains `/snap/intune-portal/` (deprecated)

### Resolution

If the user is on a snap-based installation, remove the snap and install the GA deb:

```bash
sudo snap remove intune-portal
curl -sSL https://packages.microsoft.com/keys/microsoft.asc | sudo gpg --dearmor -o /usr/share/keyrings/microsoft.gpg
sudo apt update
sudo apt install intune-portal
```

If both snap and deb are present (theoretically possible if user added the Microsoft APT repo after installing the snap), remove the snap first then re-confirm the deb is the primary binary.

### Verification

```bash
which intune-portal
dpkg -l intune-portal | grep ^ii
snap list 2>/dev/null | grep -i intune
```

Expected: `which intune-portal` resolves to the deb system path; `dpkg -l intune-portal` shows `ii`; snap detection returns empty. If still failing, escalate to L1 [30-linux-enrollment-failed.md#cause-a-package-install](../l1-runbooks/30-linux-enrollment-failed.md#cause-a-package-install).

````

DO NOT use the L1 narration blockquote pattern (V-52-16).

DO NOT prefix with sudo: `apt list`, `dpkg -l`, `systemctl --user`, `journalctl --user` (V-52-17). State-changing sudo commands (`sudo apt install`, `sudo snap remove`) appear ONLY inside `### Resolution` H3 sections.

DO NOT commit yet.
  </action>
  <verify>
    <automated>grep -c "## Trap A:" docs/l2-runbooks/25-linux-agent-investigation.md &amp;&amp; grep -c "## Trap B:" docs/l2-runbooks/25-linux-agent-investigation.md &amp;&amp; grep -c "Ubuntu HWE" docs/l2-runbooks/25-linux-agent-investigation.md &amp;&amp; grep -c "GA kernel" docs/l2-runbooks/25-linux-agent-investigation.md &amp;&amp; grep -c "uname -r" docs/l2-runbooks/25-linux-agent-investigation.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains H2 matching regex `/^## Trap A: [^\n]*\{#trap-a-kernel-track\}\s*$/m` (V-52-11)
    - File contains H2 matching regex `/^## Trap B: [^\n]*\{#trap-b-delivery-path\}\s*$/m` (V-52-11)
    - Within the Trap A body block (from `## Trap A:` to next `## ` H2): contains literal `Ubuntu HWE` AND `GA kernel` AND `uname -r` (V-52-12)
    - Within the Trap B body block: contains literal `snap` AND literal `deb` (V-52-14)
    - Trap A and Trap B both contain the subsections `**Entry condition:**`, `### Symptom`, `### Investigation Steps`, `### Resolution`, `### Verification`
    - Trap A body contains a fenced code block with `uname -r` AND `lsb_release -rs`
    - Trap B body contains a fenced code block with `apt list --installed | grep intune-portal` AND `dpkg -l intune-portal`
    - File does NOT contain L1 narration blockquote pattern (V-52-16)
    - File does NOT contain `Require device to be marked as compliant` (V-52-21)
    - Sudo-prefixed read-only commands absent in Investigation Steps (V-52-17)
  </acceptance_criteria>
  <done>Trap A + Trap B authored with V-52-11 anchors + V-52-12 + V-52-14 SC#2 literal coverage; no commit; ready for Task 3 (Trap C + Trap D + Related Resources + Version History).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append Trap C H2 (service-state — V-52-13 SC#2 content) + Trap D H2 (Identity Broker — V-52-21 negative regression-guard) + Related Resources + Version History</name>
  <files>docs/l2-runbooks/25-linux-agent-investigation.md</files>
  <read_first>
    - docs/l2-runbooks/25-linux-agent-investigation.md (verify Trap A + Trap B from Task 2 are present)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-PATTERNS.md (Trap C body lines 437-468; Trap D body lines 472-498)
    - .planning/phases/52-linux-l2-investigation-runbooks-24-25/52-RESEARCH.md (Research Area 6 lines 393-426 — systemctl --user mechanics + canonical L2 verification sequence; Research Area 5 lines 348-386 — Identity Broker re-enrollment signals + dual-scope service)
    - docs/l1-runbooks/33-linux-agent-service-failure.md (Phase 51 single-cause file; Trap C primary L2 escalation source)
    - docs/_glossary-linux.md (verify anchors `#systemd`, `#intune-agent.timer`, `#microsoft-identity-broker` exist)
  </read_first>
  <behavior>
    - Trap C H2 has literal `## Trap C: Service-State User-Scope Confusion {#trap-c-service-state}` (V-52-11)
    - Trap C body contains literal `systemctl status intune-agent` AND `systemctl enable --user --now intune-agent.timer` (V-52-13)
    - Trap D H2 has literal `## Trap D: Identity Broker v2.0.2+ Re-enrollment {#trap-d-identity-broker}` (V-52-11)
    - Trap D body documents the dual-scope service split (`microsoft-identity-broker` user-scope + `microsoft-identity-device-broker` system-scope)
    - File does NOT contain literal `Require device to be marked as compliant` anywhere (V-52-21)
    - Related Resources H2 lists Phase 49 glossary + Phase 50 capability matrix + Phase 51 L1 runbooks + Runbook 24
    - Version History H2 has 1 row dated 2026-04-27
    - File contains at least 1 link matching `../_glossary-linux.md#<anchor>` (V-52-18)
  </behavior>
  <action>
APPEND the following blocks to `docs/l2-runbooks/25-linux-agent-investigation.md` (immediately after the Trap B H2 block from Task 2).

**Trap C — paste verbatim (RESEARCH Area 6 + PATTERNS.md lines 437-468; V-52-11 anchor + V-52-13 SC#2 literals):**

````
## Trap C: Service-State User-Scope Confusion {#trap-c-service-state}

**Entry condition:** L1 escalation packet (typically from L1 [33-linux-agent-service-failure.md](../l1-runbooks/33-linux-agent-service-failure.md)) shows that the user reports `intune-agent` is "not running," but the L1 responder may have queried the wrong systemd scope (system vs user). `intune-agent.timer` is a USER-scope unit; querying it with `systemctl status intune-agent` (no `--user`) returns "Unit not found" or wrong information.

See [systemd](../_glossary-linux.md#systemd) and [intune-agent.timer](../_glossary-linux.md#intune-agent.timer) for service-management context.

### Symptom

- `systemctl status intune-agent` (without `--user`) returns "Unit intune-agent.service could not be found" or a stale system-scope record
- `systemctl --user status intune-agent.timer` returns `inactive (dead)` or `failed`
- L1 attempted `sudo systemctl restart intune-agent` and reported no improvement (because the unit they restarted was wrong scope)

### Investigation Steps

1. Confirm the system-scope query L1 may have used (this is the SC#1 ROADMAP literal):

   ```bash
   systemctl status intune-agent
   ```

   This may report "Unit not found" — that's expected because the canonical user-scope unit is `intune-agent.timer`.

2. Query the correct user-scope timer (no sudo per DPO-Phase52-01):

   ```bash
   systemctl --user status intune-agent.timer
   systemctl --user is-active intune-agent.timer
   systemctl --user is-enabled intune-agent.timer
   ```

   Expected healthy state: `Active: active (waiting)`, `is-active` returns `active`, `is-enabled` returns `enabled`.

3. List all user-scope timers to confirm `intune-agent.timer` is registered:

   ```bash
   systemctl --user list-timers --all
   ```

   Expected: `intune-agent.timer` appears with a non-empty NEXT column.

4. Collect user-scope timer journal for recent activations:

   ```bash
   journalctl --user -u intune-agent.timer --since "1 hour ago" --no-pager
   ```

5. If the timer is failed/inactive, also inspect related user-scope services (broker, agent process):

   ```bash
   systemctl --user status microsoft-identity-broker
   journalctl --user -u microsoft-identity-broker --since "1 hour ago" --no-pager
   ```

### Resolution

If the timer is inactive and not enabled, re-enable it (no sudo for `--user` scope — this is the SC#2 ROADMAP literal):

```bash
systemctl enable --user --now intune-agent.timer
```

If the user-scope systemd instance is in a degraded state, reload the user-scope daemon (no sudo):

```bash
systemctl --user daemon-reload
systemctl --user restart intune-agent.timer
```

If the system-scope agent is genuinely failed (rare — it is typically a wrapper service), restart it explicitly (sudo permitted only for system-scope state-changing operations):

```bash
sudo systemctl restart intune-agent
```

### Verification

```bash
systemctl --user is-active intune-agent.timer
systemctl --user list-timers intune-agent.timer
journalctl --user -u intune-agent.timer --since "5 minutes ago" --no-pager
```

Expected: `is-active` returns `active`; `list-timers` shows a non-empty NEXT column; recent journal entries show timer firing successfully.

````

**Trap D — paste verbatim (RESEARCH Area 5 + PATTERNS.md lines 472-498; V-52-11 anchor + V-52-21 negative regression-guard — DO NOT use literal phrase `Require device to be marked as compliant`):**

````
## Trap D: Identity Broker v2.0.2+ Re-enrollment {#trap-d-identity-broker}

**Entry condition:** Admin reports CA assignment drift, Intune filter regression, or Entra group membership not applying for a previously-working Linux endpoint, AND `dpkg -l microsoft-identity-broker` shows version 2.0.2 or later. The Identity Broker v2.0.2+ architecture automatically re-registers and re-enrolls devices, creating new Intune device IDs and new Microsoft Entra device IDs — silently — when the broker package crosses the v2.0.2 threshold.

See [microsoft-identity-broker](../_glossary-linux.md#microsoft-identity-broker) for broker context. This trap consumes Phase 50 LIN-05 admin-side pitfall callout (`docs/admin-setup-linux/01-intune-linux-agent.md`) by reference per CD-07.

### Symptom

- A Linux endpoint that was previously compliant suddenly shows up as non-compliant or unassigned in the Intune admin center
- The endpoint's Intune device ID has changed since the last L1 escalation packet
- Existing CA assignments scoped to the old device ID stop applying after a broker package update — affected users see policy not enforced or filters not matching the new device IDs

### Investigation Steps

1. Confirm Identity Broker version crossed the v2.0.2 threshold:

   ```bash
   dpkg -l microsoft-identity-broker
   dpkg -l microsoft-identity-device-broker
   ```

   Look for version `2.0.2` or later in either package.

2. Check current device state via the dsreg tool:

   ```bash
   dsreg --status
   ```

   Capture the output `Device ID` field. Compare against the L1 escalation packet's recorded device ID — if they differ, re-enrollment occurred.

3. Check user-scope broker for re-enrollment events:

   ```bash
   journalctl --user -u microsoft-identity-broker --since "1 week ago" --no-pager | grep -iE "register|enroll|device.id|re-register"
   ```

4. Check system-scope broker for device registration events (system journal may require sudo):

   ```bash
   sudo journalctl -u microsoft-identity-device-broker --since "1 week ago" --no-pager | grep -iE "register|enroll|device.id"
   ```

5. Cross-reference the new device ID against Intune admin center (Devices > All devices > filter by Linux platform) to confirm the new device ID is assigned the correct CA filters / Entra groups.

> **Note:** Microsoft Learn does not document specific journal log message text for re-enrollment events. The `grep -iE "register|enroll|device.id"` pattern above is an inferred broad signal; the definitive check is `dsreg --status` device ID comparison.

### Resolution

This trap does NOT have a device-side fix — it is an after-the-fact admin investigation. The remediation is admin-side:

- Update CA assignments / Intune filters / Entra group memberships to scope to the NEW device ID (or to a dynamic membership rule that survives device ID changes)
- Document the re-enrollment event in the ticket history
- Cross-link to Phase 50 LIN-05 admin-side pitfall callout for re-enrollment-aware admin workflows

If the device-side cleanup is required (e.g., to force a full re-enrollment to a clean state), the dsreg cleanup workflow is:

```bash
sudo dsreg --tenant-id <tenant-id> --unregister
dsreg --cleanup
```

(Then the user re-signs in via the Intune Portal app to trigger fresh enrollment.)

### Verification

```bash
dsreg --status
dpkg -l microsoft-identity-broker | grep ^ii
journalctl --user -u microsoft-identity-broker --since "1 hour ago" --no-pager | tail -20
```

Expected: `dsreg --status` reports a stable device ID; broker package shows `ii` state; recent broker journal entries show normal sign-in activity (no fresh re-register events). For return-route to L1, see [30-linux-enrollment-failed.md#cause-b-sign-in-failure](../l1-runbooks/30-linux-enrollment-failed.md#cause-b-sign-in-failure).

````

**Related Resources + Version History — paste verbatim:**

```
## Related Resources

- [L2 Runbook Index — Linux L2 Runbooks](00-index.md#linux-l2-runbooks) — Linux L2 runbook set
- [Linux Log Collection Guide (Runbook 24)](24-linux-log-collection.md) — prerequisite for this runbook; Section 1 journalctl output is consumed by all 4 traps
- [Linux Provisioning Glossary](../_glossary-linux.md) — `microsoft-identity-broker`, `intune-agent.timer`, `systemd`, `journalctl`, `hwe-kernel`, `ga-kernel`, `deb-repository`, `snap`
- [Linux L1 Triage Decision Tree](../decision-trees/09-linux-triage.md) — Phase 51 L1 routing surface
- [Linux Capability Matrix — Supported Management Surface](../reference/linux-capability-matrix.md#supported-management-surface) — Ubuntu 22.04/24.04 LTS scope and version-track context for Trap A
- [L1 30: Linux Enrollment Failed](../l1-runbooks/30-linux-enrollment-failed.md) — L1 escalation source; Cause A (package install) → Trap A or Trap B; Cause B (sign-in) → Trap D; Cause C (timeout) → Trap C
- [L1 31: Linux Compliance Non-Compliant](../l1-runbooks/31-linux-compliance-non-compliant.md) — L1 escalation source (custom-compliance Bash via Runbook 24 Section 2)
- [L1 33: Linux Agent Service Failure](../l1-runbooks/33-linux-agent-service-failure.md) — L1 escalation source (primary route to Trap C)
- [Phase 50 LIN-05 Identity Broker callout](../admin-setup-linux/01-intune-linux-agent.md) — admin-side pitfall context for Trap D

## Version History

| Date | Change | Author |
|------|--------|--------|
| 2026-04-27 | Initial version (Phase 52 — Linux L2 agent investigation; 4 anchor-indexed Traps; CONTEXT D-05/D-06/D-08/D-09; SC#2 literal coverage per V-52-12/13/14; PITFALL-2 negative regression-guard per V-52-21) | -- |
```

DO NOT use the literal phrase `Require device to be marked as compliant` anywhere in Trap D body (V-52-21). Trap D documents the CA assignment drift consequence (re-enrollment creates new device IDs which causes existing CA assignments / Intune filters / Entra group memberships to stop applying silently) without using this exact PITFALL-2 phrasing.

DO NOT use the L1 narration blockquote pattern (V-52-16).

DO NOT prefix with sudo: `apt list`, `dpkg -l`, `systemctl --user`, `journalctl --user` (V-52-17). State-changing sudo commands (`sudo apt install`, `sudo systemctl restart`) appear ONLY inside `### Resolution` H3 sections per DPO-Phase52-01. `sudo journalctl -u microsoft-identity-device-broker` is permitted (system-scope read).

DO NOT commit — atomic commit owned by 52-05.
  </action>
  <verify>
    <automated>grep -c "## Trap C:" docs/l2-runbooks/25-linux-agent-investigation.md &amp;&amp; grep -c "## Trap D:" docs/l2-runbooks/25-linux-agent-investigation.md &amp;&amp; grep -c "systemctl status intune-agent" docs/l2-runbooks/25-linux-agent-investigation.md &amp;&amp; grep -c "systemctl enable --user --now intune-agent.timer" docs/l2-runbooks/25-linux-agent-investigation.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains H2 matching `/^## Trap C: [^\n]*\{#trap-c-service-state\}\s*$/m` (V-52-11)
    - File contains H2 matching `/^## Trap D: [^\n]*\{#trap-d-identity-broker\}\s*$/m` (V-52-11)
    - Within Trap C body: contains literal `systemctl status intune-agent` AND literal `systemctl enable --user --now intune-agent.timer` (V-52-13)
    - File does NOT contain literal `Require device to be marked as compliant` anywhere (V-52-21)
    - File contains at least 1 link to `../_glossary-linux.md#<anchor>` (V-52-18)
    - File contains H2 `## Related Resources` AND H2 `## Version History`
    - Related Resources contains literal `24-linux-log-collection.md` AND `09-linux-triage.md` AND `_glossary-linux.md`
    - Version History table has 1 row with date `2026-04-27`
    - Final cross-link audit: file contains at least 3 of the V-52-15 locked-surface literals
    - File contains 4 H2 trap headers with locked anchors (V-52-11 — full enumeration: `#trap-a-kernel-track`, `#trap-b-delivery-path`, `#trap-c-service-state`, `#trap-d-identity-broker`)
    - File does NOT contain L1 narration blockquote pattern (V-52-16)
    - File does NOT match `/\bsudo\s+apt\s+list\b/`, `/\bsudo\s+dpkg\s+-l\b/`, `/\bsudo\s+systemctl\s+--user\b/`, or `/\bsudo\s+journalctl\s+--user\b/` (V-52-17)
    - File does NOT contain BYOD/COBO/COPE/Dedicated/ZTE/AOSP/COSU (V-52-20)
    - File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER (V-52-22)
    - Cross-tier distinctness audit (CDI-Phase52-03 human verifiable): each Trap body documents L2 INVESTIGATION TOOLS (kernel commands, package-state queries, service-scope mechanics, dsreg invocation), not re-stated L1 user-facing symptoms
  </acceptance_criteria>
  <done>Runbook 25 complete with 4 anchor-indexed Traps + Related Resources + Version History; satisfies V-52-02/05/11/12/13/14/15/16/17/18/20/21/22; no commit (52-05 owns atomicity).</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-52-06 | Tampering (regression) | 4 Trap H2 anchors | mitigate | V-52-11 string-pinned regex for each anchor. CDI-Phase52-02 Phase 56/58/59 brittleness contract — anchor renames require same-commit Phase 52 validator update per CONTEXT D-12. |
| T-52-07 | Tampering (regression) | SC#2 literal coverage | mitigate | V-52-12 (Trap A: Ubuntu HWE / GA kernel / uname -r) + V-52-13 (Trap C: systemctl literals) + V-52-14 (Trap B: snap / deb) — all string-pinned. |
| T-52-08 | Tampering (regression) | L2 audience contract | mitigate | V-52-16 negative regex enforces L1 narration blockquote pattern absent. CDI-Phase52-05 GA-4-COUP-2 retained coupling defect; 0/23 occurrences across L2 corpus. |
| T-52-09 | Tampering (regression) | PITFALL-2 reintroduction in Trap D | mitigate | V-52-21 negative regex enforces `Require device to be marked as compliant` absent. Phase 51 V-51-19 inheritance. |
| T-52-10 | Information disclosure | Anti-Pattern 1 cross-tier topology import | accept | Not validator-enforced (cross-tier-distinctness is human-verifiable per CDI-Phase52-03). Author MUST verify each Trap body documents L2 investigation tools, not re-stated L1 symptoms. |
| T-52-11 | Tampering (regression) | L1 cross-link surface | accept | Phase 51 DPO-01 locks anchor surface; CDI-Phase52-02 same-commit brittleness contract. V-52-15 enforces ≥3 cross-link presence at commit time. |

No application security threats — runbook is documentation-only. Trap D `dsreg --cleanup` and `sudo dsreg --tenant-id <id> --unregister` are device-management commands documented in Resolution context; per Phase 30 D-10, these appear ONLY in `### Resolution` H3 sections.
</threat_model>

<verification>
After all 3 tasks complete:

```bash
# File existence + frontmatter
test -f docs/l2-runbooks/25-linux-agent-investigation.md
grep -E "^(platform|audience|applies_to|last_verified|review_by):" docs/l2-runbooks/25-linux-agent-investigation.md

# 4 Trap anchor presence (V-52-11)
grep -E "^## Trap A: [^\\n]*\\{#trap-a-kernel-track\\}" docs/l2-runbooks/25-linux-agent-investigation.md
grep -E "^## Trap B: [^\\n]*\\{#trap-b-delivery-path\\}" docs/l2-runbooks/25-linux-agent-investigation.md
grep -E "^## Trap C: [^\\n]*\\{#trap-c-service-state\\}" docs/l2-runbooks/25-linux-agent-investigation.md
grep -E "^## Trap D: [^\\n]*\\{#trap-d-identity-broker\\}" docs/l2-runbooks/25-linux-agent-investigation.md

# SC#2 literal coverage (V-52-12, V-52-13, V-52-14)
grep -c "Ubuntu HWE" docs/l2-runbooks/25-linux-agent-investigation.md
grep -c "GA kernel" docs/l2-runbooks/25-linux-agent-investigation.md
grep -c "uname -r" docs/l2-runbooks/25-linux-agent-investigation.md
grep -c "systemctl status intune-agent" docs/l2-runbooks/25-linux-agent-investigation.md
grep -c "systemctl enable --user --now intune-agent.timer" docs/l2-runbooks/25-linux-agent-investigation.md

# L1 cross-link surface (V-52-15)
grep -E "30-linux-enrollment-failed.md#cause-(a|b|c)" docs/l2-runbooks/25-linux-agent-investigation.md | wc -l
grep -c "33-linux-agent-service-failure.md" docs/l2-runbooks/25-linux-agent-investigation.md

# Negative regression-guards (V-52-16, V-52-17, V-52-20, V-52-21, V-52-22)
grep "Require device to be marked as compliant" docs/l2-runbooks/25-linux-agent-investigation.md && echo FAIL_V-52-21 || echo PASS_V-52-21
grep -E "sudo apt list|sudo dpkg -l|sudo systemctl --user|sudo journalctl --user" docs/l2-runbooks/25-linux-agent-investigation.md && echo FAIL_V-52-17 || echo PASS_V-52-17
grep -E "\\bBYOD\\b|\\bCOBO\\b|\\bCOPE\\b|\\bDedicated\\b|\\bZTE\\b|\\bAOSP\\b|\\bCOSU\\b" docs/l2-runbooks/25-linux-agent-investigation.md && echo FAIL_V-52-20 || echo PASS_V-52-20
grep -E "\\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\\b" docs/l2-runbooks/25-linux-agent-investigation.md && echo FAIL_V-52-22 || echo PASS_V-52-22

# Glossary anchor consumption (V-52-18)
grep -c "../_glossary-linux.md#" docs/l2-runbooks/25-linux-agent-investigation.md
```

If `check-phase-52.mjs` exists (52-03 authored in parallel), run:

```bash
node scripts/validation/check-phase-52.mjs --verbose
```

Expect V-52-02, V-52-05 (RB25), V-52-11, V-52-12, V-52-13, V-52-14, V-52-15, V-52-16 (RB25), V-52-17 (RB25), V-52-18 (RB25), V-52-20 (RB25), V-52-21, V-52-22 (RB25) PASS.

DO NOT commit — atomic commit owned by 52-05 per CONTEXT D-13.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 52 Success Criteria:

- **SC#2** (Runbook 25 covers Ubuntu HWE vs GA kernel + `uname -r` + snap-vs-deb detection + `systemctl status intune-agent` + `systemctl enable --user --now intune-agent.timer`): Trap A covers HWE/GA + `uname -r`; Trap B covers snap-vs-deb; Trap C covers the systemctl literals (V-52-12/13/14)
- **SC#4** (frontmatter on 60-day cycle, platform: Linux, audience: L2): Frontmatter satisfies V-52-05

Plan complete when:

- [ ] File exists at `docs/l2-runbooks/25-linux-agent-investigation.md`
- [ ] All Task 1 + Task 2 + Task 3 acceptance criteria pass (V-52-02/05/11/12/13/14/15/16/17/18/20/21/22 once 52-03 validator authored)
- [ ] No forbidden patterns
- [ ] No commit — 52-05 owns atomicity
</success_criteria>

<output>
After completion, create `.planning/phases/52-linux-l2-investigation-runbooks-24-25/52-02-SUMMARY.md` documenting:

- File path + line count
- 4 Trap H2 anchors verified (literal strings)
- SC#2 literal coverage (V-52-12, V-52-13, V-52-14)
- L1 cross-link count + literal anchors used (V-52-15)
- V-52-16 / V-52-17 / V-52-20 / V-52-21 / V-52-22 negative regression-guards verified
- Glossary anchor links used (V-52-18 — count + which anchors)
- Cross-tier distinctness audit notes (CDI-Phase52-03 — confirm Trap content is L2 investigation tools, not L1 symptom restatement)
- Note: NO COMMIT — atomic commit owned by 52-05
</output>
