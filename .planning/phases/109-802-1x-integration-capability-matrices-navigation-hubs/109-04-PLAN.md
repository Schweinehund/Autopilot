---
phase: 109-802-1x-integration-capability-matrices-navigation-hubs
plan: 04
type: execute
wave: 2
depends_on: ["109-01", "109-02", "109-03"]
files_modified: []
autonomous: true
requirements: [DOT1X-11]
must_haves:
  truths:
    - "All four existing platform glossaries carry the [Network Authentication Glossary](_glossary-network.md) see-also banner (SC3 — verify-only, D-01)"
    - "All 11 edited files (5 matrices + 6 hubs) contain their 802.1X rows/entries with resolvable link targets (SC1 + SC2)"
    - "Every 802.1X nav target was committed before its referencing nav edit (navigation-last invariant, SC4)"
  artifacts:
    - path: ".planning/phases/109-802-1x-integration-capability-matrices-navigation-hubs/109-04-SUMMARY.md"
      provides: "Goal-backward SC1-SC4 verification record"
      contains: "SC4"
  key_links:
    - from: "docs/_glossary-macos.md"
      to: "docs/_glossary-network.md"
      via: "existing see-also banner (satisfies iOS via macOS per D-01)"
      pattern: "_glossary-network.md"
---

<objective>
Close out Phase 109: verify **SC3** (glossary see-also banners — verify-only per D-01, no edits) and perform the **navigation-last (SC4)** + goal-backward verification that SC1 and SC2 landed correctly across all 11 edited files. This plan authors NO content edits — it is a verification and gate.

**SC3 is verify-only (D-01):** All four existing SC3-named platform glossaries already carry the `[Network Authentication Glossary](_glossary-network.md)` banner (`_glossary.md:13`, `_glossary-macos.md:12`, `_glossary-android.md:14`, `_glossary-linux.md:12`). The fifth ROADMAP-named file `_glossary-ios.md` does not exist — iOS terminology folds into `_glossary-macos.md`, which carries the banner (satisfied-by mapping). `_glossary-apple-business.md` is deliberately NOT touched. No banner is added or edited.

Purpose: SC3 + SC4 confirmation; final goal-backward gate for the phase.
Output: verification record in 109-04-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-802-1x-integration-capability-matrices-navigation-hubs/109-CONTEXT.md
@.planning/phases/109-802-1x-integration-capability-matrices-navigation-hubs/109-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC3 — verify glossary see-also banners (verify-only, no edits)</name>
  <files>docs/_glossary.md, docs/_glossary-macos.md, docs/_glossary-android.md, docs/_glossary-linux.md</files>
  <read_first>
    - docs/_glossary.md, docs/_glossary-macos.md, docs/_glossary-android.md, docs/_glossary-linux.md (confirm the network-glossary see-also banner near the stated lines; lines may have shifted — find by content, not line number)
    - 109-RESEARCH.md § "Deliverable 6 › See-Also Banners (SC3)" and CONTEXT.md D-01
  </read_first>
  <action>
    Read each of the four platform glossaries and confirm each carries a see-also banner referencing `_glossary-network.md`. Do NOT edit any glossary. Do NOT add a banner to `_glossary-apple-business.md` (not SC3-named; leave untouched). Record in the SUMMARY: for each of the four files, the confirmed banner line text and line number; and the `_glossary-ios.md`→`_glossary-macos.md` satisfied-by mapping. If any banner is genuinely absent (unexpected — D-01 says all present), STOP and flag as a gap rather than silently editing.
  </action>
  <verify>
    <automated>for f in _glossary _glossary-macos _glossary-android _glossary-linux; do grep -F "_glossary-network.md" "docs/${f}.md" >/dev/null && echo "OK ${f}" || echo "MISSING ${f}"; done</automated>
  </verify>
  <acceptance_criteria>
    - All four glossaries return `OK` (banner referencing `_glossary-network.md` present): `_glossary.md`, `_glossary-macos.md`, `_glossary-android.md`, `_glossary-linux.md`.
    - `docs/_glossary-network.md` exists (banner target) and was NOT modified.
    - `docs/_glossary-apple-business.md` was NOT modified (`git diff --name-only` does not list it).
    - `git diff --name-only` lists ZERO glossary files (verify-only — no edits made).
    - SUMMARY records banner line text + line number per file and the ios→macos satisfied-by mapping.
  </acceptance_criteria>
  <done>SC3 confirmed satisfied by pre-existing banners; no glossary edited.</done>
</task>

<task type="auto">
  <name>Task 2: SC1/SC2/SC4 goal-backward verification across all 11 files</name>
  <files>.planning/phases/109-802-1x-integration-capability-matrices-navigation-hubs/109-04-SUMMARY.md</files>
  <read_first>
    - 109-01-SUMMARY.md, 109-02-SUMMARY.md, 109-03-SUMMARY.md (confirm each plan's edits landed)
    - The 11 edited files (5 matrices + 6 hubs) at their 802.1X insertion points
    - 109-RESEARCH.md § "Deliverable 5" (nav-last target list — all 15 content targets confirmed committed)
  </read_first>
  <action>
    Run the goal-backward checks and record results in the SUMMARY:
    1. SC1 — grep `Network Authentication (802.1X)` across the 5 matrices; confirm exactly one row per file (5 total) with correct verdict words and resolvable link targets.
    2. SC2 — confirm each of the 6 hubs carries its 802.1X entries: index.md (foundation guides + glossary + per-platform admin/L1), common-issues.md (4 failure H3s), quick-ref-l1.md (triage tree + L1), quick-ref-l2.md (L2 #31-33), l1-runbooks/00-index.md (## 802.1X L1 Runbooks), l2-runbooks/00-index.md (## 802.1X L2 Runbooks).
    3. Link resolution — for every inserted link, confirm the target file exists on disk (all 15 content targets from RESEARCH Deliverable 5 are committed).
    4. SC4 (navigation-last) — confirm via `git log --oneline` that every referenced content file (admin-setup-8021x/*, l1/l2 runbooks, decision tree, _glossary-network.md) was committed BEFORE the Phase 109 nav commits. Since all content landed in Phases 101-108, this is expected to pass; record the ordering evidence.
    5. D-03 compliance — confirm no dedicated 802.1X H2 exists in the four prose hubs (index.md, common-issues.md, quick-ref-l1.md, quick-ref-l2.md).
    Write pass/fail per SC into 109-04-SUMMARY.md. If any check fails, flag it as a gap for gap-closure rather than marking the phase complete.
  </action>
  <verify>
    <automated>for f in 4-platform-capability-comparison macos-capability-matrix ios-capability-matrix android-capability-matrix linux-capability-matrix; do grep -c -F "Network Authentication (802.1X)" "docs/reference/${f}.md"; done; grep -lF "8021x" docs/index.md docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md docs/l1-runbooks/00-index.md docs/l2-runbooks/00-index.md</automated>
  </verify>
  <acceptance_criteria>
    - SC1: each of the 5 matrices returns grep count `1` for `Network Authentication (802.1X)`.
    - SC2: all 6 hub files are returned by the `grep -lF 8021x` list (each carries at least one 802.1X reference).
    - Every inserted link target resolves to an existing committed file (0 broken links).
    - SC4: git-log ordering confirms all content targets committed before Phase 109 nav commits (records commit SHAs/dates as evidence).
    - D-03: `grep -n '^## ' ` on the four prose hubs shows no dedicated 802.1X topic H2.
    - 109-04-SUMMARY.md records pass/fail per SC1-SC4; any failure is flagged as a gap (not silently passed).
  </acceptance_criteria>
  <done>SC1, SC2, SC4 confirmed across all 11 files with evidence recorded; phase ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
N/A — verification-only, no executable code, no user input, no data flow, no file edits (Task 1 is read-only; Task 2 writes only the SUMMARY). No package-manager installs. RESEARCH.md § Security Domain confirms security review is N/A for a nav-wiring phase.
</threat_model>

<verification>
- SC3 confirmed by pre-existing banners (no edits).
- SC1 (5 matrix rows) + SC2 (6 hub entry sets) present and link-resolvable.
- SC4 navigation-last ordering confirmed via git log.
- D-03 (no dedicated 802.1X H2 in the four prose hubs) confirmed.
</verification>

<success_criteria>
SC3 + SC4 satisfied and recorded; SC1 + SC2 goal-backward verification passes across all 11 edited files, with any gap surfaced for closure.
</success_criteria>

<output>
Create `.planning/phases/109-802-1x-integration-capability-matrices-navigation-hubs/109-04-SUMMARY.md` when done.
</output>
