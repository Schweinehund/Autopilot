---
phase: 109-802-1x-integration-capability-matrices-navigation-hubs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.md
  - docs/common-issues.md
  - docs/quick-ref-l1.md
  - docs/quick-ref-l2.md
autonomous: true
requirements: [DOT1X-11]
must_haves:
  truths:
    - "The four prose nav hubs carry 802.1X entries folded into existing groupings — no dedicated 802.1X H2 is created in any of the four (D-03, SC2)"
    - "index.md ## Cross-Platform References lists the 802.1X overview + EAP-method + cert-delivery foundation guides + network glossary"
    - "quick-ref-l1.md ## Decision Trees carries the 802.1X triage tree; quick-ref-l2.md ## Investigation Runbooks carries L2 #31-33"
    - "common-issues.md gains a 802.1X H3 under each of the 4 existing platform H2s (no new Linux H2 created)"
  artifacts:
    - path: "docs/index.md"
      provides: "802.1X foundation guides + glossary in Cross-Platform References; per-platform admin/L1/L2 entries folded into existing platform sections"
      contains: "admin-setup-8021x/00-overview.md"
    - path: "docs/common-issues.md"
      provides: "802.1X Network Authentication Failure H3 under Windows/macOS/iOS/Android platform H2s"
      contains: "decision-trees/10-8021x-triage.md"
    - path: "docs/quick-ref-l1.md"
      provides: "802.1X triage tree in ## Decision Trees + L1 #38-41 folded into runbook groupings"
      contains: "decision-trees/10-8021x-triage.md"
    - path: "docs/quick-ref-l2.md"
      provides: "L2 #31-33 in ## Investigation Runbooks"
      contains: "l2-runbooks/31-8021x-log-collection.md"
  key_links:
    - from: "docs/index.md"
      to: "docs/admin-setup-8021x/00-overview.md"
      via: "Cross-Platform References table entry"
      pattern: "admin-setup-8021x/00-overview.md"
    - from: "docs/common-issues.md"
      to: "docs/l1-runbooks/38-8021x-certificate-failure.md"
      via: "802.1X failure H3 L1 link"
      pattern: "l1-runbooks/38-8021x-certificate-failure.md"
---

<objective>
Wire the four **prose** navigation hubs (`docs/index.md`, `docs/common-issues.md`, `docs/quick-ref-l1.md`, `docs/quick-ref-l2.md`) with entries for the already-committed 802.1X guides, runbooks, and decision tree (part of SC2). Per **D-03**, fold every entry into a pre-existing grouping by artifact type or platform — **do NOT create a dedicated "802.1X / Network Authentication" H2 in any of these four hubs.** Link-not-copy: entries point to committed content, never restate theory.

**common-issues.md Linux note (RESEARCH A6):** `common-issues.md` has only 4 platform H2s (Windows/macOS/iOS/Android) — no Linux H2 exists. Consistent with D-03 (fold-in, no restructuring), do NOT create a `## Linux Failure Scenarios` H2. Linux 802.1X is discoverable through the same cross-platform L1 #38-41 / L2 #31-33 runbooks reachable from index.md, quick-ref-l1/l2, and the runbook-catalog indexes (Plan 03).

Purpose: SC2 (prose-hub portion) — 802.1X reachable from every prose entry point.
Output: 4 modified prose hubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-802-1x-integration-capability-matrices-navigation-hubs/109-CONTEXT.md
@.planning/phases/109-802-1x-integration-capability-matrices-navigation-hubs/109-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold 802.1X entries into docs/index.md existing groupings</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md (## Cross-Platform References ~:299; the per-platform H2s Windows/macOS/iOS/Android/Linux, each with ### Admin Setup, ### Service Desk (L1), and L2 sub-tables — confirm current line positions before inserting)
    - 109-RESEARCH.md § "Deliverable 4 › Hub 1: docs/index.md" (full entry text for Cross-Platform References + per-platform routing)
    - Targets: docs/admin-setup-8021x/{00-overview,01-eap-method-overview,02-cert-delivery-foundation,04-macos,05-ios,06-android,07-linux}.md, docs/_glossary-network.md, docs/decision-trees/10-8021x-triage.md
  </read_first>
  <action>
    Fold entries into existing sections only (no new H2). Concretely:
    1. In `## Cross-Platform References`, append four table rows (after the last matrix row, before `## Version History`): `[802.1X Admin Setup Overview](admin-setup-8021x/00-overview.md)`, `[EAP Method Overview](admin-setup-8021x/01-eap-method-overview.md)`, `[Certificate Delivery Foundation](admin-setup-8021x/02-cert-delivery-foundation.md)`, `[Network Authentication Glossary](_glossary-network.md)` — each with a one-line description column matching the existing table's column shape (see RESEARCH Hub 1 for exact text).
    2. In each per-platform `### Admin Setup` table (macOS/iOS/Android/Linux), append one row linking the platform 802.1X admin guide: macOS→`admin-setup-8021x/04-macos.md`, iOS→`admin-setup-8021x/05-ios.md`, Android→`admin-setup-8021x/06-android.md`, Linux→`admin-setup-8021x/07-linux.md`.
    3. In each per-platform `### Service Desk (L1)` grouping (Windows APv1, macOS, iOS, Android, Linux), append a row pointing to `decision-trees/10-8021x-triage.md` (802.1X Triage) — the tree + L1 #38-41 are cross-platform.
    Match each table's existing column shape. No new H2. No freshness stamps. Append-only.
  </action>
  <verify>
    <automated>grep -F "admin-setup-8021x/00-overview.md" docs/index.md && grep -F "_glossary-network.md" docs/index.md && grep -c -F "decision-trees/10-8021x-triage.md" docs/index.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Cross-Platform References` contains links to `admin-setup-8021x/00-overview.md`, `01-eap-method-overview.md`, `02-cert-delivery-foundation.md`, and `_glossary-network.md` (4 grep hits).
    - Each per-platform Admin Setup grouping links its 802.1X guide: `04-macos.md`, `05-ios.md`, `06-android.md`, `07-linux.md` each appear at least once.
    - `decision-trees/10-8021x-triage.md` appears at least once (folded into L1 groupings).
    - No new heading matching `^## .*802` was added (grep -n '^## ' shows no dedicated 802.1X H2) — D-03 compliance.
    - All link targets exist on disk. `git diff` additive only.
  </acceptance_criteria>
  <done>index.md carries 802.1X foundation/admin/L1 entries folded into existing groupings, no dedicated H2.</done>
</task>

<task type="auto">
  <name>Task 2: Add 802.1X failure H3 to common-issues.md platform sections</name>
  <files>docs/common-issues.md</files>
  <read_first>
    - docs/common-issues.md (platform H2s: ## Windows Autopilot Issues ~:23, ## macOS ... ~:157, ## iOS/iPadOS Failure Scenarios ~:249, ## Android Enterprise Failure Scenarios ~:304 — confirm exact H2 titles/positions; note NO Linux H2 exists)
    - 109-RESEARCH.md § "Deliverable 4 › Hub 2: docs/common-issues.md" (H3 template) and § "Common Pitfalls › Pitfall 4"
    - Targets: docs/l1-runbooks/{38,39,40,41}-*.md, docs/l2-runbooks/{31,32,33}-*.md, docs/decision-trees/10-8021x-triage.md
  </read_first>
  <action>
    Under each of the FOUR existing platform H2s (Windows, macOS, iOS, Android), append one `### 802.1X Network Authentication Failure` H3 at the end of that platform's section (before the next platform H2). Each H3 contains: a one-line symptom sentence; an **L1:** line linking L1 #38-41 and the `decision-trees/10-8021x-triage.md` triage tree; an **L2:** line linking L2 #31-33 (all cross-platform — same links reused under each platform). For the Android H3, phrase the symptom to acknowledge Wi-Fi-only 802.1X (no native Intune wired profile) — PITFALL-4; do NOT imply Android wired support. Use the exact link paths from RESEARCH Hub 2. Do NOT create a Linux H2 (see objective note). No freshness stamps; append-only.
  </action>
  <verify>
    <automated>grep -c "### 802.1X Network Authentication Failure" docs/common-issues.md; grep -F "decision-trees/10-8021x-triage.md" docs/common-issues.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 4 `### 802.1X Network Authentication Failure` H3 headings (one per existing platform H2).
    - Each H3 links all four L1 runbooks (`38-`, `39-`, `40-`, `41-8021x-*.md`) and the triage tree.
    - Each H3 links all three L2 runbooks (`31-`, `32-`, `33-8021x-*.md`).
    - No `## Linux` H2 was added (grep -n '^## ' shows the platform-H2 set unchanged aside from none added).
    - Android H3 does not assert wired 802.1X support (PITFALL-4).
    - All link targets exist. `git diff` additive only.
  </acceptance_criteria>
  <done>common-issues.md carries a 802.1X failure H3 under each of the 4 existing platform H2s, no Linux H2 minted.</done>
</task>

<task type="auto">
  <name>Task 3: Fold 802.1X entries into quick-ref-l1.md and quick-ref-l2.md</name>
  <files>docs/quick-ref-l1.md, docs/quick-ref-l2.md</files>
  <read_first>
    - docs/quick-ref-l1.md (## Decision Trees ~:35, ## Runbooks ~:42, and per-platform ### Runbooks sub-sections)
    - docs/quick-ref-l2.md (## Investigation Runbooks ~:65, and per-platform ### Investigation Runbooks sub-sections)
    - 109-RESEARCH.md § "Deliverable 4 › Hub 3" and "Hub 4" (exact entry lines)
    - Targets: docs/decision-trees/10-8021x-triage.md, docs/l1-runbooks/{38,39,40,41}-*.md, docs/l2-runbooks/{31,32,33}-*.md
  </read_first>
  <action>
    quick-ref-l1.md: (a) in `## Decision Trees`, append `- [802.1X Triage](decision-trees/10-8021x-triage.md) — 802.1X authentication failure symptom routing (cross-platform)`. (b) Fold the four cross-platform L1 runbooks (#38-41) into the artifact-type `## Runbooks` grouping (append the four `l1-runbooks/38..41-8021x-*.md` entries with a "cross-platform" note). No new H2.
    quick-ref-l2.md: in `## Investigation Runbooks`, append the three L2 entries: `l2-runbooks/31-8021x-log-collection.md` (marked prerequisite), `32-8021x-cert-investigation.md`, `33-8021x-radius-eap-investigation.md`, each with the one-line description from RESEARCH Hub 4. No new H2.
    Match each hub's existing list/table shape. No freshness stamps; append-only.
  </action>
  <verify>
    <automated>grep -F "decision-trees/10-8021x-triage.md" docs/quick-ref-l1.md && grep -c -F "8021x" docs/quick-ref-l1.md; grep -F "l2-runbooks/31-8021x-log-collection.md" docs/quick-ref-l2.md</automated>
  </verify>
  <acceptance_criteria>
    - quick-ref-l1.md `## Decision Trees` links `decision-trees/10-8021x-triage.md`; the four L1 runbooks `38..41-8021x-*.md` each appear.
    - quick-ref-l2.md `## Investigation Runbooks` links all three `l2-runbooks/3{1,2,3}-8021x-*.md`; #31 is marked as prerequisite.
    - No dedicated 802.1X H2 added to either file (grep -n '^## ' unchanged aside from none added) — D-03.
    - All link targets exist. `git diff` additive only across both files.
  </acceptance_criteria>
  <done>quick-ref-l1/l2 carry 802.1X tree + L1/L2 entries folded into existing artifact-type groupings.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only navigation edits, no executable code, no user input, no data flow. No package-manager installs. RESEARCH.md § Security Domain confirms security review is N/A for a nav-wiring phase.
</threat_model>

<verification>
- 4 prose hubs each carry 802.1X entries folded into pre-existing groupings (SC2 portion).
- No dedicated 802.1X H2 created in any of the four prose hubs (D-03).
- No Linux H2 minted in common-issues.md.
- All link targets committed and resolvable.
</verification>

<success_criteria>
SC2 (prose-hub portion): index.md, common-issues.md, quick-ref-l1.md, quick-ref-l2.md all carry 802.1X guide/runbook/decision-tree entries, folded into existing structure per D-03.
</success_criteria>

<output>
Create `.planning/phases/109-802-1x-integration-capability-matrices-navigation-hubs/109-02-SUMMARY.md` when done.
</output>
