---
phase: 101-802-1x-foundation-glossary-eap-methods-cert-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_glossary-network.md
autonomous: true
requirements: [DOT1X-01]
must_haves:
  truths:
    - "A reader can learn the 802.1X 3-actor model (supplicant / authenticator / authentication server), EAPOL, and the RADIUS exchange from docs/_glossary-network.md"
    - "All 13 platform-neutral network-auth terms are defined with the exact GitHub auto-slug anchors from RESEARCH (incl. #inner-outer-identity, #8021x, #eku-client-authentication)"
    - "No platform-specific term (dynamic trust dialog, dot3svc, NAC, wpa_supplicant, nmcli) appears in the neutral glossary"
  artifacts:
    - path: "docs/_glossary-network.md"
      provides: "Platform-neutral 802.1X network-authentication glossary, 13 terms grouped under 3 H2 sections, alphabetical index, freshness front-matter"
      contains: "### inner-outer identity"
  key_links:
    - from: "docs/_glossary-network.md (Domain coverage banner)"
      to: "_glossary.md, _glossary-macos.md, _glossary-android.md, _glossary-linux.md"
      via: "one-way navigation aid links in the top blockquote (NO back-links into platform glossaries)"
      pattern: "Domain coverage"
---

<objective>
Author `docs/_glossary-network.md` — the new platform-neutral 802.1X network-authentication glossary that all foundation files and per-platform guides (Phases 102–106) link into. This is the build-order root (DOT1X-01): the overview, EAP overview, cert-delivery foundation, and the see-also banners all reference its term anchors, so it must exist and be anchor-correct first.

Purpose: Establish the single source of truth for the 3-actor model, EAPOL, RADIUS exchange, EAP framework, and certificate-delivery vocabulary so downstream guides link-not-copy.
Output: One new file, `docs/_glossary-network.md`, with 13 terms (the DOT1X-01 floor of 9 plus authenticator, authentication server, EKU/Client Authentication, inner-outer identity).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-CONTEXT.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold glossary + author Protocol Actors and Authentication Methods term groups</name>
  <files>docs/_glossary-network.md</files>
  <read_first>
    - `docs/_glossary-macos.md` (lines 1–28) — exact front-matter field order, top-blockquote banner format, H1, single-line pipe-separated alphabetical index, H2-group + H3-term entry structure, `---` rule between H2 sections. This is the EXACT analog.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-CONTEXT.md` — decision D-08 (13-term floor), D-09 (platform-specific terms OUT), D-10 (one-way links, no back-links), D-11 (no `_glossary-ios.md`).
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md` §Per-File Authoring Spec File 1, §Term Definitions Table (verbatim definitions + exact slugs), §House-Style Conventions (front-matter schema, index format), and the `### inner-outer identity` TRAP WARNING.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-PATTERNS.md` §`docs/_glossary-network.md` (front-matter substitution values, Domain-coverage banner adaptation, H2-group names, term-entry pattern).
  </read_first>
  <action>
    Create `docs/_glossary-network.md`. Front-matter (exact field order, no custom fields): `last_verified: 2026-06-29`, `review_by: 2026-09-27`, `applies_to: both`, `audience: all`, `platform: all`.
    Top blockquote labeled `> **Domain coverage:**` describing platform-neutral 802.1X terminology, with one-way navigation-aid links to `_glossary.md`, `_glossary-macos.md`, `_glossary-android.md`, `_glossary-linux.md` (these are reader-misroute aids, NOT D-10 back-links; omit the Apple Business governance line). H1 `# Network Authentication Glossary`. Then a `## Alphabetical Index` as a SINGLE unbroken pipe-separated line in A-Z order exactly: `[802.1X](#8021x) | [authentication server](#authentication-server) | [authenticator](#authenticator) | [EAP](#eap) | [EAPOL](#eapol) | [EKU (Client Authentication)](#eku-client-authentication) | [inner-outer identity](#inner-outer-identity) | [PKCS](#pkcs) | [RADIUS](#radius) | [SCEP](#scep) | [server-name validation](#server-name-validation) | [supplicant](#supplicant) | [trusted root](#trusted-root)`. Insert a `---` rule after the index.
    Author H2 group `## 802.1X Protocol Actors` containing H3 terms `### 802.1X`, `### supplicant`, `### authenticator`, `### authentication server`, `### EAPOL` (use verbatim definitions from RESEARCH §Term Definitions Table). Insert a `---` rule, then H2 group `## Authentication Methods` containing `### EAP`, `### RADIUS`, `### inner-outer identity`. CRITICAL slug discipline: write the heading literally as `### inner-outer identity` (hyphen, NOT `inner/outer`) to produce `#inner-outer-identity`; note `inner/outer` as the shorthand in the body. No `{#id}` overrides anywhere. Do NOT include any platform-specific term (dynamic trust dialog, NAC/MAC randomization, dot3svc, wpa_supplicant, nmcli) — D-09.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q '^last_verified: 2026-06-29' docs/_glossary-network.md && grep -q '^review_by: 2026-09-27' docs/_glossary-network.md && grep -q '> \*\*Domain coverage:\*\*' docs/_glossary-network.md && grep -q '^# Network Authentication Glossary' docs/_glossary-network.md && grep -q '^### inner-outer identity' docs/_glossary-network.md && grep -q '^### authentication server' docs/_glossary-network.md && grep -q '\[802.1X\](#8021x)' docs/_glossary-network.md && ! grep -qi 'dynamic trust dialog\|wpa_supplicant\|nmcli\|dot3svc' docs/_glossary-network.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/_glossary-network.md` exists with the 5-field front-matter in exact order and `last_verified: 2026-06-29` / `review_by: 2026-09-27`.
    - Top blockquote begins `> **Domain coverage:**` and links to all four platform glossaries; no back-link target points into a platform glossary as a network-term reference (navigation aid only).
    - H1 is exactly `# Network Authentication Glossary`.
    - Alphabetical index is a single line, A-Z, containing `[802.1X](#8021x)` and `[inner-outer identity](#inner-outer-identity)`.
    - H2 `## 802.1X Protocol Actors` contains `### 802.1X`, `### supplicant`, `### authenticator`, `### authentication server`, `### EAPOL`; H2 `## Authentication Methods` contains `### EAP`, `### RADIUS`, `### inner-outer identity`.
    - Heading is `### inner-outer identity` (NOT `### inner/outer identity`).
    - No platform-specific term (dynamic trust dialog, dot3svc, NAC, wpa_supplicant, nmcli) appears.
  </acceptance_criteria>
  <done>The glossary scaffold, index, and 8 of 13 terms (Protocol Actors + Authentication Methods groups) are present with correct anchors and no platform-specific leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Author Certificate Delivery term group + change history</name>
  <files>docs/_glossary-network.md</files>
  <read_first>
    - `docs/_glossary-network.md` (the file created in Task 1) — to append the third H2 group after the existing `## Authentication Methods` section.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md` §Term Definitions Table rows for SCEP, PKCS, trusted root, EKU (Client Authentication), server-name validation (verbatim definitions + exact slugs `#scep`, `#pkcs`, `#trusted-root`, `#eku-client-authentication`, `#server-name-validation`).
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-PATTERNS.md` §Change History Table (the `| Date | Change | Author |` pattern, Author column `--`, `--` not em dash).
  </read_first>
  <action>
    Append a `---` rule then H2 group `## Certificate Delivery` to `docs/_glossary-network.md`, with H3 terms `### SCEP`, `### PKCS`, `### trusted root`, `### EKU (Client Authentication)`, `### server-name validation` using the verbatim definitions from RESEARCH §Term Definitions Table. Confirm the auto-slug for `### EKU (Client Authentication)` is `#eku-client-authentication` (parentheses stripped, single hyphen — no double-hyphen trap). Keep definitions protocol-level / platform-neutral (D-09): the SCEP/PKCS/trusted-root entries may name supported platforms as facts but must not contain per-platform Intune UI steps. After the last term, add a `---` rule and a `## Change History` table with one row: `| 2026-06-29 | Initial version -- new platform-neutral 802.1X network-authentication glossary (13 terms) | -- |`. Use ` -- ` (double hyphen), never em dash.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q '^## Certificate Delivery' docs/_glossary-network.md && grep -q '^### SCEP' docs/_glossary-network.md && grep -q '^### PKCS' docs/_glossary-network.md && grep -q '^### trusted root' docs/_glossary-network.md && grep -q '^### EKU (Client Authentication)' docs/_glossary-network.md && grep -q '^### server-name validation' docs/_glossary-network.md && grep -q '2026-06-29 | Initial version' docs/_glossary-network.md && [ "$(grep -c '^### ' docs/_glossary-network.md)" -eq 13 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - H2 `## Certificate Delivery` present with all five H3 terms (`### SCEP`, `### PKCS`, `### trusted root`, `### EKU (Client Authentication)`, `### server-name validation`).
    - Total H3 term count across the file is exactly 13.
    - A `## Change History` table exists with a single `2026-06-29 | Initial version` row and Author column `--`.
    - Definitions are protocol-level; no per-platform Intune UI step appears.
  </acceptance_criteria>
  <done>All 13 terms are authored, the file has the certificate-delivery group, and the change-history table is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| reader → documentation | No executable surface. Risk is documentation correctness only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Information (misguidance) | term definitions | mitigate | Use only HIGH-confidence verbatim definitions from RESEARCH §Term Definitions Table; keep platform-neutral (D-09) |
| T-101-02 | Tampering (broken anchors) | GitHub auto-slug anchors | mitigate | Follow exact slug table; write `### inner-outer identity` to avoid the `#innerouter-identity` double-hyphen trap |
No HIGH-severity application threats: this phase ships static Markdown only — no code, input handling, or auth surface.
</threat_model>

<verification>
- `docs/_glossary-network.md` exists with valid 5-field freshness front-matter.
- Exactly 13 `### ` term headings; index links resolve to those exact slugs.
- No platform-specific terms present (D-09); no `{#id}` overrides.
</verification>

<success_criteria>
Maps to Phase 101 SC1 (3-actor model + EAPOL + RADIUS learnable from `_glossary-network.md`). The see-also banner wiring half of SC1 is delivered by Plan 101-05.
</success_criteria>

<output>
Create `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-01-SUMMARY.md` when done.
</output>
