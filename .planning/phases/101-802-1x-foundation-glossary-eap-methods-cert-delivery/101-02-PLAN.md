---
phase: 101-802-1x-foundation-glossary-eap-methods-cert-delivery
plan: 02
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - docs/admin-setup-8021x/01-eap-method-overview.md
autonomous: true
requirements: [DOT1X-02]
must_haves:
  truths:
    - "An admin can choose among EAP-TLS, PEAP-MSCHAPv2, and EAP-TTLS as co-equal paths, each with what authenticates / what the client requires / trust requirements / when chosen"
    - "A comparison table summarizes the three methods after the individual sections; TEAP appears as one awareness paragraph only (no H2, not co-equal)"
    - "No example or callout shows server validation disabled (pitfall A-05 / C-02)"
  artifacts:
    - path: "docs/admin-setup-8021x/01-eap-method-overview.md"
      provides: "Co-equal EAP-method overview with 3-actor model Mermaid sequence diagram, three H2 method sections, comparison table, single-paragraph TEAP note"
      contains: "## EAP-TLS"
  key_links:
    - from: "docs/admin-setup-8021x/01-eap-method-overview.md"
      to: "docs/_glossary-network.md term anchors"
      via: "prerequisites banner + inline term links (link-not-copy)"
      pattern: "_glossary-network.md"
---

<objective>
Author `docs/admin-setup-8021x/01-eap-method-overview.md` (DOT1X-02): the co-equal EAP-method overview. This is the first file inside the new `docs/admin-setup-8021x/` folder, so this plan also establishes the folder. It links into the Plan 101-01 glossary anchors (link-not-copy), which is why it runs after the glossary exists.

Purpose: Give admins a co-equal, no-recommended-default comparison of EAP-TLS, PEAP-MSCHAPv2, and EAP-TTLS, plus the 3-actor/EAPOL conceptual model, so per-platform guides reference this single source.
Output: One new file with a Mermaid sequence diagram, three co-equal H2 method sections, a comparison table, and a one-paragraph TEAP awareness note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-CONTEXT.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-PATTERNS.md
@docs/_glossary-network.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 01-eap-method-overview + author 3-actor model and the three co-equal EAP method sections</name>
  <files>docs/admin-setup-8021x/01-eap-method-overview.md</files>
  <read_first>
    - `docs/_glossary-network.md` (created in Plan 101-01) — to confirm the exact anchor slugs for the prerequisites banner and inline term links (#8021x, #eap, #radius, #supplicant, #authentication-server, #inner-outer-identity).
    - `docs/admin-setup-macos/08-auth-methods-deep-dive.md` (lines 1–70) — EXACT analog for front-matter, prerequisites blockquote, H1, co-equal H2 method-section structure, `> **Label:**` callout style, `---` separators between method sections.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md` §Per-File Authoring Spec File 3 (per-method sub-topics + the Mermaid sequenceDiagram source + TEAP exclusion) and §Common Pitfalls A-05/C-02 (server validation), E-06 (co-equal).
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-PATTERNS.md` §`01-eap-method-overview.md` (front-matter values, prerequisites banner adaptation, sequenceDiagram four-space indent format, co-equal H2 rule, `> **Security note:**` for PEAP).
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-CONTEXT.md` §Specifics (co-equal, no recommended default; TEAP not co-equal).
  </read_first>
  <action>
    Create `docs/admin-setup-8021x/01-eap-method-overview.md` (creating the `docs/admin-setup-8021x/` folder). Front-matter: `last_verified: 2026-06-29`, `review_by: 2026-09-27`, `applies_to: both`, `audience: admin`, `platform: all`. Prerequisites blockquote `> **Prerequisites:**` linking to `../_glossary-network.md` and naming the terms to read first (802.1X, EAP, RADIUS, supplicant, authentication server). H1 `# 802.1X EAP Method Overview`. Section `## The 802.1X Three-Actor Model` explaining supplicant → authenticator → authentication server with EAPOL flow, linking to glossary anchors (link-not-copy, do not redefine terms), and a Mermaid `sequenceDiagram` (four-space indent, `participant S/A/R as ...`, `->>` requests / `-->>` responses, `Note over S,R:` for the method exchange) per the RESEARCH source block.
    Author three CO-EQUAL `## EAP-TLS`, `## PEAP-MSCHAPv2`, `## EAP-TTLS` H2 sections (E-06: all three at H2, never H3), each covering the four sub-topics: what authenticates, what the client requires, trust requirements, when chosen. In the PEAP-MSCHAPv2 section add a `> **Security note:**` stating server validation is REQUIRED and that the default Windows EAP XML ships it disabled and must be overridden — but NEVER show an example with server validation disabled (A-05/C-02). Do NOT use the words "recommended default" or rank the methods. Separate method sections with `---` rules. No fenced configuration that disables `PerformServerValidation`.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q '^last_verified: 2026-06-29' docs/admin-setup-8021x/01-eap-method-overview.md && grep -q '^# 802.1X EAP Method Overview' docs/admin-setup-8021x/01-eap-method-overview.md && grep -q 'sequenceDiagram' docs/admin-setup-8021x/01-eap-method-overview.md && grep -q '^## EAP-TLS' docs/admin-setup-8021x/01-eap-method-overview.md && grep -q '^## PEAP-MSCHAPv2' docs/admin-setup-8021x/01-eap-method-overview.md && grep -q '^## EAP-TTLS' docs/admin-setup-8021x/01-eap-method-overview.md && grep -q '_glossary-network.md' docs/admin-setup-8021x/01-eap-method-overview.md && ! grep -qi 'PerformServerValidation=false\|PerformServerValidation = false\|disable server validation\|recommended default' docs/admin-setup-8021x/01-eap-method-overview.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists with 5-field front-matter (`last_verified: 2026-06-29`, `review_by: 2026-09-27`, `applies_to: both`, `audience: admin`, `platform: all`).
    - `> **Prerequisites:**` banner links to `../_glossary-network.md`.
    - `## The 802.1X Three-Actor Model` present with a Mermaid `sequenceDiagram` and links to glossary anchors (terms not redefined inline).
    - Exactly three method sections at H2 level: `## EAP-TLS`, `## PEAP-MSCHAPv2`, `## EAP-TTLS`; each covers what authenticates / client requires / trust requirements / when chosen.
    - PEAP section carries a `> **Security note:**` requiring server validation; no example/callout disables server validation; the string "recommended default" does not appear.
  </acceptance_criteria>
  <done>The file exists with the conceptual model, sequence diagram, and three co-equal method sections, with the server-validation pitfall mitigated.</done>
</task>

<task type="auto">
  <name>Task 2: Author EAP comparison table, TEAP awareness paragraph, and change history</name>
  <files>docs/admin-setup-8021x/01-eap-method-overview.md</files>
  <read_first>
    - `docs/admin-setup-8021x/01-eap-method-overview.md` (created in Task 1) — to append after the three method sections.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md` §Per-File Authoring Spec File 3 — the exact `## EAP Method Comparison` table (Client cert / Server cert / Inner credential / Identity privacy / Intune support / Wired support rows) and the TEAP one-paragraph rule.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-PATTERNS.md` §Change History Table.
  </read_first>
  <action>
    Append `## EAP Method Comparison` with the summary table from RESEARCH (columns: Property | EAP-TLS | PEAP-MSCHAPv2 | EAP-TTLS; rows for client-cert required, server-cert required, inner credential, identity privacy, Intune support, wired support; include the Linux script-based-only footnote). Then add `## TEAP` as EXACTLY ONE paragraph (no H3 sub-sections, not co-equal) noting TEAP is Windows-wired-only awareness, surfaced in the Windows guide (Phase 102), and is not a co-equal path here. Add a brief forward-reference sentence pointing to `02-cert-delivery-foundation.md` for certificate delivery (link-not-copy; do not duplicate cert content — E-02). End with a `---` rule and a `## Change History` table row: `| 2026-06-29 | Initial version -- co-equal EAP method overview (EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS) | -- |`.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q '^## EAP Method Comparison' docs/admin-setup-8021x/01-eap-method-overview.md && grep -q '^## TEAP' docs/admin-setup-8021x/01-eap-method-overview.md && ! grep -q '^### TEAP' docs/admin-setup-8021x/01-eap-method-overview.md && grep -q '02-cert-delivery-foundation.md' docs/admin-setup-8021x/01-eap-method-overview.md && grep -q '2026-06-29 | Initial version' docs/admin-setup-8021x/01-eap-method-overview.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `## EAP Method Comparison` table present with one column per method and equal treatment (E-06); includes the Linux script-based-only footnote.
    - `## TEAP` exists as a single paragraph at H2 with no H3 sub-sections; it is explicitly not co-equal.
    - A forward-reference link to `02-cert-delivery-foundation.md` exists (no cert content duplicated — E-02).
    - `## Change History` table has the `2026-06-29 | Initial version` row.
  </acceptance_criteria>
  <done>The comparison table, single-paragraph TEAP note, forward reference, and change history are present; the file is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| reader → documentation | Static Markdown; no executable surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-03 | Spoofing (rogue RADIUS) | PEAP-MSCHAPv2 section | mitigate | Mandate server validation; never show an example disabling it (A-05/C-02 pitfall) |
| T-101-04 | Information (mis-selection) | co-equal EAP presentation | mitigate | Present three methods co-equally with explicit "when chosen" guidance; no recommended default (E-06) |
No HIGH-severity application threats: static documentation only.
</threat_model>

<verification>
- File exists with valid freshness front-matter and three co-equal H2 method sections.
- No server-validation-disabled example; "recommended default" absent.
- TEAP is one H2 paragraph; comparison table present; links into `_glossary-network.md` and forward to `02-cert-delivery-foundation.md`.
</verification>

<success_criteria>
Maps to Phase 101 SC2 (admin can choose among EAP-TLS / PEAP-MSCHAPv2 / EAP-TTLS as co-equal paths, with client requirements and when-to-choose).
</success_criteria>

<output>
Create `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-02-SUMMARY.md` when done.
</output>
