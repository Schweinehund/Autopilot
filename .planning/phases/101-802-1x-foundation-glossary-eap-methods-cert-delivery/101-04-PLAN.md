---
phase: 101-802-1x-foundation-glossary-eap-methods-cert-delivery
plan: 04
type: execute
wave: 4
depends_on: [101-02, 101-03]
files_modified:
  - docs/admin-setup-8021x/00-overview.md
autonomous: true
requirements: [DOT1X-01]
must_haves:
  truths:
    - "A reader has an entry point for the 802.1X folder listing the two foundation guides (01-eap, 02-cert) with a descriptive one-liner per guide (house-style, NOT a capability table) and a Mermaid setup-sequence (A2 / D-01)"
    - "The only capability fact at the entry point is the wired-availability gap flag for Android and Linux (A2/D-02) — no EAP-method cells, no cert-type cells, no capability table"
    - "All links resolve to files that exist at commit time (01-eap, 02-cert, _glossary-network.md); the platform-guides Mermaid node is an unlinked placeholder"
  artifacts:
    - path: "docs/admin-setup-8021x/00-overview.md"
      provides: "Thin navigation overview: guide scope blockquote, Mermaid graph LR, two-entry numbered list, wired-gap flag, scope sentence linking to canonical callout, See Also, change history"
      contains: "## Setup Sequence"
  key_links:
    - from: "docs/admin-setup-8021x/00-overview.md"
      to: "01-eap-method-overview.md, 02-cert-delivery-foundation.md, ../_glossary-network.md, 02-cert-delivery-foundation.md#canonical-scope-callout"
      via: "numbered list + scope sentence (links must resolve — files exist by Wave 4)"
      pattern: "01-eap-method-overview.md"
---

<objective>
Author `docs/admin-setup-8021x/00-overview.md` (DOT1X-01 entry point) — the thin navigation summary for the new 802.1X folder. Runs LAST (Wave 4) because it links to `01-eap-method-overview.md` and `02-cert-delivery-foundation.md`, which must exist for the links to resolve (broken-link C13 hygiene; per link-not-copy build order).

Purpose: Give readers an orientation entry point that lists only the files that exist at Phase 101 commit time, plus a scope-orientation wired-gap flag — without becoming the Phase-109 capability matrix (navigation-last).
Output: One new file matching the `admin-setup-macos/00-overview.md` house style (A2 thin nav).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-CONTEXT.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md
@.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-PATTERNS.md
@docs/admin-setup-8021x/01-eap-method-overview.md
@docs/admin-setup-8021x/02-cert-delivery-foundation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the thin navigation overview (A2)</name>
  <files>docs/admin-setup-8021x/00-overview.md</files>
  <read_first>
    - `docs/admin-setup-macos/00-overview.md` (whole file) — EXACT analog: front-matter, `> **Platform gate:**`-style top blockquote, H1, intro paragraph, `## Setup Sequence` + Mermaid `graph LR`, numbered descriptive one-liner list (` -- ` separator), `## See Also` bare-bullet list, `*Next step:*` italic line, change history table.
    - `docs/admin-setup-8021x/01-eap-method-overview.md` and `docs/admin-setup-8021x/02-cert-delivery-foundation.md` — to confirm exact link filenames and the `#canonical-scope-callout` anchor for the scope sentence.
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-RESEARCH.md` §Per-File Authoring Spec File 2 (Phase-101 Mermaid with placeholder `C` node; two numbered entries; wired-availability flag; scope sentence linking to canonical callout) and §Open Questions Q1 (only existing-file entries; placeholder node, no link).
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-CONTEXT.md` decisions A2, D-02 (wired-gap flag is the ONLY capability fact permitted; NO capability table).
    - `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-PATTERNS.md` §`00-overview.md` (Mermaid node format, one-liner `--` separator, See Also, change history).
  </read_first>
  <action>
    Create `docs/admin-setup-8021x/00-overview.md`. Front-matter: `last_verified: 2026-06-29`, `review_by: 2026-09-27`, `applies_to: both`, `audience: admin`, `platform: all`. Top blockquote `> **Guide scope:**` describing 802.1X enterprise network auth for Intune-managed devices (client-side only), linking to `../_glossary-network.md`. H1 `# 802.1X Network Authentication: Admin Setup Guides`. One intro sentence (use ` -- ` not em dash). `## Setup Sequence` with a Mermaid `graph LR` of three nodes — `A[1. EAP Method<br/>Overview] --> B[2. Cert Delivery<br/>Foundation]` and `B --> C[3–7. Platform<br/>Guides]` where node `C` is an UNLINKED placeholder label (no file link — avoids C13 broken-link failure). Numbered list with exactly two entries at Phase 101: `1. **[EAP Method Overview](01-eap-method-overview.md)** -- ...` and `2. **[Certificate Delivery Foundation](02-cert-delivery-foundation.md)** -- ...`, plus a `3–7. Platform guides (Phase 102–106) -- entries added as each guide is authored.` non-linked line. After the list, a `> **Wired 802.1X availability note:**` blockquote (D-02 — the ONLY capability fact): Android Enterprise = Wi-Fi only, no native Intune wired profile; Linux = no native Intune Wi-Fi/wired profile, script-based EAP-TLS only. Add a `## Scope` one-liner: "Intune client-side configuration only — RADIUS/NPS assumed to exist" linking to `02-cert-delivery-foundation.md#canonical-scope-callout`. Add `## See Also` (bare bullets to `01-eap-method-overview.md`, `02-cert-delivery-foundation.md`, `../_glossary-network.md`), a `*Next step: [EAP Method Overview](01-eap-method-overview.md)*` italic line, a `---` rule, and a `## Change History` row `| 2026-06-29 | Initial version -- 802.1X admin-setup folder overview (two foundation guides) | -- |`. Do NOT add any platform×{Wi-Fi,wired,EAP,cert} capability table (that is Phase 109).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q '^last_verified: 2026-06-29' docs/admin-setup-8021x/00-overview.md && grep -q '^# 802.1X Network Authentication: Admin Setup Guides' docs/admin-setup-8021x/00-overview.md && grep -q '^## Setup Sequence' docs/admin-setup-8021x/00-overview.md && grep -q 'graph LR' docs/admin-setup-8021x/00-overview.md && grep -q '(01-eap-method-overview.md)' docs/admin-setup-8021x/00-overview.md && grep -q '(02-cert-delivery-foundation.md)' docs/admin-setup-8021x/00-overview.md && grep -q '> \*\*Wired 802.1X availability note:\*\*' docs/admin-setup-8021x/00-overview.md && grep -q '#canonical-scope-callout' docs/admin-setup-8021x/00-overview.md && grep -q '2026-06-29 | Initial version' docs/admin-setup-8021x/00-overview.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists with 5-field front-matter (`last_verified: 2026-06-29` / `review_by: 2026-09-27`, `audience: admin`).
    - `> **Guide scope:**` top blockquote links to `../_glossary-network.md`; H1 exact.
    - `## Setup Sequence` Mermaid `graph LR` has an unlinked `C[3–7. Platform Guides]` placeholder node (no broken link).
    - Numbered list has exactly two linked entries: `01-eap-method-overview.md` and `02-cert-delivery-foundation.md` (plus a non-linked 3–7 placeholder line).
    - `> **Wired 802.1X availability note:**` present for Android + Linux; NO capability table / no EAP-method or cert-type cells.
    - `## Scope` sentence links to `02-cert-delivery-foundation.md#canonical-scope-callout`; `## See Also`, `*Next step:*` line, and `2026-06-29 | Initial version` change-history row present.
  </acceptance_criteria>
  <done>The thin A2 navigation overview exists with resolving links, the placeholder Mermaid node, the wired-gap flag, and no capability table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| reader → documentation | Static Markdown; no executable surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-08 | Information (broken nav) | numbered list + Mermaid links | mitigate | Link only to files that exist at Wave 4 (01-eap, 02-cert); platform-guides node is an unlinked placeholder (C13 hygiene) |
| T-101-09 | Information (drift/dual source) | entry-point capability facts | mitigate | A2: only the Android/Linux wired-gap flag permitted; no capability table (Phase-109 navigation-last) |
No HIGH-severity application threats: static documentation only.
</threat_model>

<verification>
- File exists with valid freshness front-matter.
- All links resolve (01-eap, 02-cert, _glossary-network.md, #canonical-scope-callout); placeholder node unlinked.
- Wired-gap flag present; no capability table.
</verification>

<success_criteria>
Supports Phase 101 SC1 (entry point into the foundation) and reuses SC4 scope callout via the scope sentence. Adheres to A2/navigation-last (no Phase-109 capability matrix).
</success_criteria>

<output>
Create `.planning/phases/101-802-1x-foundation-glossary-eap-methods-cert-delivery/101-04-SUMMARY.md` when done.
</output>
