---
phase: 137-integration-navigation-last-close
plan: 02
type: execute
wave: 2
depends_on: ["137-01"]
files_modified:
  - docs/index.md
  - .planning/STATE.md
autonomous: true
requirements: [CLASS-06]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`docs/index.md` carries a table row whose link text is RE-224's H1 verbatim and whose target is `recipes/03-windows-11-multi-app-kiosk.md`, and a second row for RE-225 targeting `recipes/04-android-dedicated-mhs-multi-app.md`, in filename order after the RE-223 row"
    - "The single line matching `^- \\[Device Configuration Recipes\\]\\(#device-configuration-recipes\\)` names all four recipes, including the fixed prose fragments `Windows 11 multi-app kiosk` and `Android Dedicated multi-app kiosk`"
    - "Both index.md surfaces land in ONE commit: `git show --stat` on Commit C lists `docs/index.md` as its only content file (SC3, D-22)"
    - "Navigation-last holds structurally: Commit C's author timestamp strictly post-dates the plan-137-01 registry/regeneration commit's author timestamp, both named by SHA (D-16)"
    - "The three troubleshooting hubs stay NOT wired: the D-03 grep over `docs/common-issues.md`, `docs/quick-ref-l1.md`, `docs/quick-ref-l2.md` matches no file, and `git diff --quiet` over all three succeeds across the whole phase (SC4)"
    - "`node scripts/validation/c17-eee-contract.mjs` exits 0 with `234 files checked, 0 with violations` after Commit C (SC5)"
    - "`node scripts/validation/check-nav-hub-links.mjs` exits 0 reporting `0 outbound failure(s), 0 inbound failure(s), 0 total` after Commit C (SC5)"
    - "All 42 `../` outbound links shipped by recipes 03 and 04 resolve to files that exist — a surface no validator in the repo covers (D-17)"
    - "`.planning/STATE.md`'s Plan-Time Research Flags block carries a Phase-138 entry holding the D-19/D-20 needle-spec with the MEASURED literals as shipped, and `137-02-SUMMARY.md` carries the verbatim `Needle-spec handoff` section plus the D-02 correction of record and the D-06 deferred-cleanup candidate"
  artifacts:
    - path: "docs/index.md"
      provides: "Both recipes discoverable on both navigation surfaces"
      contains: "recipes/04-android-dedicated-mhs-multi-app.md"
    - path: ".planning/STATE.md"
      provides: "Phase-138 needle-spec handoff appended to the existing Plan-Time Research Flags block"
      contains: "check-phase-137.mjs"
  key_links:
    - from: "docs/index.md Device Configuration Recipes table"
      to: "docs/recipes/03-windows-11-multi-app-kiosk.md"
      via: "relative markdown link, link text == recipe H1 == RE-index Title"
      pattern: "recipes/03-windows-11-multi-app-kiosk.md"
    - from: "docs/index.md Device Configuration Recipes table"
      to: "docs/recipes/04-android-dedicated-mhs-multi-app.md"
      via: "relative markdown link, link text == recipe H1 == RE-index Title"
      pattern: "recipes/04-android-dedicated-mhs-multi-app.md"
    - from: "docs/index.md line-38 quick-nav bullet"
      to: "the ## Device Configuration Recipes section"
      via: "anchor link #device-configuration-recipes, with per-recipe prose names that the Phase-138 needle asserts line-scoped"
      pattern: "Android Dedicated multi-app kiosk"
---

<objective>
Make both recipes discoverable from `docs/index.md` on BOTH surfaces — the Device Configuration Recipes table and the line-38 prose quick-nav bullet — landed in a single commit, then close the phase's non-code obligations: the hubs-not-wired ruling with its correction of record, the uncovered outbound-link sweep, and the needle-spec handoff to Phase 138. This satisfies CLASS-06.

Purpose: Navigation lands LAST. Per D-13 this plan is wave 2 (`depends_on: ["137-01"]`), so its index.md commit structurally post-dates the registry+regeneration commit — navigation-last is a wave dependency (D-16), not a discipline note a plan author could forget. The same-commit both-surfaces rule closes the WR-01 defect class, which recurred once already at Phase 132 and had nothing watching for it.

Output: Two index.md table rows, one replaced quick-nav bullet, a green nav-hub link-checker, a one-off outbound-link sweep, and a needle-spec Phase 138 can build a validator from.

<must_honor priority="ABSOLUTE">
1. **BOTH index.md surfaces in ONE commit.** Splitting the table rows from the bullet reproduces the exact WR-01 defect this requirement exists to close.
2. **The line-38 bullet's replacement string is FIXED in this plan, verbatim.** It is not composed at execution time. `index.md:38` carries no path, filename, or doc ID, so leaving its wording to discretion makes the Phase-138 needle unbuildable.
3. **This is an APPEND to an existing table plus a REPLACEMENT of one bullet.** Phase 132's analog commit `71ad89a3` created the whole `## Device Configuration Recipes` H2 + intro + table because none existed. That section exists now. Do NOT re-create the H2, the intro sentence, or the table header.
4. **`Dedicated` stays capitalized** in `Android Dedicated multi-app kiosk` — it is an enrollment-mode proper noun (`index.md:36`, `quick-ref-l2.md:352`, `docs/recipes/04-*.md:9` all capitalize it). The existing bullet's capitalization of the first two recipes is NOT a defect; leave both existing fragments byte-unchanged.
5. **The three troubleshooting hubs are NOT edited.** Not one character. The ruling is that they stay unwired; enforcement is a grep plus `git diff --quiet`, not a code change.
6. **`scripts/validation/check-phase-132.mjs` is a v1.18 frozen surface — do NOT open it to edit and do NOT "fix" its regex** (D-04). Its `V-132-HUBSNOTWIRED` pattern provably does not match `recipes/03-` or `recipes/04-`; the corrected coverage is additive and belongs in the Phase-138 needle-spec Task 3 hands off.
7. **Phase 137 does NOT author `check-phase-137.mjs`.** Leaf validators are the harness-close phase's deliverable (HARN-15 already names `check-phase-135..138.mjs` as Phase 138's). Task 3 writes a spec, not a validator.
8. **Do NOT add platform-section `### Admin Setup` cross-reference rows.** CLASS-06's text is exhaustive at "the table and the prose quick-nav bullet", and Phase 132 added none either.
</must_honor>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/137-integration-navigation-last-close/137-CONTEXT.md
@.planning/phases/137-integration-navigation-last-close/137-RESEARCH.md
@.planning/phases/137-integration-navigation-last-close/137-PATTERNS.md
@.planning/phases/137-integration-navigation-last-close/137-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit C — both docs/index.md navigation surfaces, in one commit</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md (lines 33-42: the quick-nav bullet list; line 38 is the ONLY bullet on the page that names individual documents — note that :34 macOS enumerates nothing and :40 Cross-Platform References is generic, so "every bullet enumerates its members" is false and is not the reason for this edit)
    - docs/index.md (lines 274-285: the `## Device Configuration Recipes` intro sentence, the `| Resource | When to Use |` header, the RE-222 row, the RE-223 row at :281, the blank line at :282, and the `---` divider at :283 before `## Operations` at :285)
    - docs/recipes/03-windows-11-multi-app-kiosk.md (line 15 H1 and the `## Summary` paragraph — the link text is the H1 verbatim; the Summary is the blurb's source material)
    - docs/recipes/04-android-dedicated-mhs-multi-app.md (line 15 H1 and the `## Summary` paragraph — same)
    - .planning/phases/137-integration-navigation-last-close/137-PATTERNS.md (the note that analog commit `71ad89a3` built a whole new section and this edit must NOT copy that shape)
  </read_first>
  <action>
    Two edits to `docs/index.md`, staged and committed TOGETHER as one commit — this is Commit C of the three-commit topology (D-14), and it implements D-07, D-08, D-09, D-10, D-11 and D-12.

    EDIT 1 — replace the line-38 bullet in full, per D-07. Find this exact existing line:

    `- [Device Configuration Recipes](#device-configuration-recipes) -- End-to-end provisioning recipes with embedded admin decision points (shared Windows AVD-client device, Shared iPad full provisioning)`

    Replace it with this exact line, copied verbatim — do not compose, re-case, or re-order:

    `- [Device Configuration Recipes](#device-configuration-recipes) -- End-to-end provisioning recipes with embedded admin decision points (shared Windows AVD-client device, Shared iPad full provisioning, Windows 11 multi-app kiosk, Android Dedicated multi-app kiosk)`

    The two new fragments extend the existing enumeration in filename order (D-08), matching the table order below. The parenthetical order needs no separate ruling; it follows the table. Per D-11 the existing capitalization is not a defect and `Dedicated` stays capitalized. Note the deliberate divergence from RE-225's own `applies_to` frontmatter, which reads "Android Enterprise Dedicated multi-app kiosk" — the bullet drops "Enterprise". That is the ruled string, not a defect to correct.

    EDIT 2 — append exactly two rows to the existing `| Resource | When to Use |` table, immediately after the RE-223 row at line 281 and before the blank line at :282. Filename order: 03 then 04 (D-08). Link text is each recipe's H1 verbatim per D-09, character-for-character identical to the registry Title plan 137-01 wrote — never re-derived. Use the `--` double-hyphen separator the two existing rows use, not an em-dash. The rows are:

    `| [Windows 11 Multi-App Kiosk: Assigned Access Provisioning](recipes/03-windows-11-multi-app-kiosk.md) | Lock a Windows 11 device to a restricted user experience -- a multi-app allow-list, Start layout, and Taskbar -- delivered through an AssignedAccess CSP custom OMA-URI profile, with no Templates GUI path available |`

    `| [Android Dedicated Multi-App Kiosk: Managed Home Screen Provisioning](recipes/04-android-dedicated-mhs-multi-app.md) | Lock an Android Enterprise Dedicated device to a curated Managed Home Screen app grid -- MHS assigned as Required, an App Configuration policy carrying the worked JSON payload, and exit-PIN hardening |`

    Those two blurbs are 31 and 30 words, inside D-10's measured 20-33 word precedent budget, and follow the two conformant shapes the existing rows use. If you reword either, keep it inside that budget and record the actual word count in the SUMMARY, because Phase 138 derives needles from measured actuals.

    Touch nothing else in `docs/index.md`. Per D-12, do not add the recipes into `## Windows Autopilot`, `## Android Enterprise Provisioning`, or any platform section. The `## Operations` section must keep mentioning Apple Business inside its own body — an existing predecessor assertion reads that window.

    Then run C17 and the nav-hub link-checker, and commit. Subject: `docs(137-02): register RE-224/RE-225 in index.md navigation (table + quick-nav bullet)`. Body: one bullet for the table append, one for the bullet replacement, and a line stating both surfaces landed together per the WR-01 defect class.
  </action>
  <verify>
    <automated>test $(grep -c '^| \[Windows 11 Multi-App Kiosk: Assigned Access Provisioning\](recipes/03-windows-11-multi-app-kiosk.md) |' docs/index.md) -eq 1 && test $(grep -c '^| \[Android Dedicated Multi-App Kiosk: Managed Home Screen Provisioning\](recipes/04-android-dedicated-mhs-multi-app.md) |' docs/index.md) -eq 1 && test $(grep -c '^- \[Device Configuration Recipes\](#device-configuration-recipes)' docs/index.md) -eq 1 && grep '^- \[Device Configuration Recipes\](#device-configuration-recipes)' docs/index.md | grep -q 'Windows 11 multi-app kiosk, Android Dedicated multi-app kiosk)$' && node scripts/validation/c17-eee-contract.mjs && node scripts/validation/check-nav-hub-links.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| \[Windows 11 Multi-App Kiosk: Assigned Access Provisioning\](recipes/03-windows-11-multi-app-kiosk.md) |' docs/index.md` returns 1
    - `grep -c '^| \[Android Dedicated Multi-App Kiosk: Managed Home Screen Provisioning\](recipes/04-android-dedicated-mhs-multi-app.md) |' docs/index.md` returns 1
    - both new rows sit AFTER the `recipes/02-shared-ipad-full-provisioning.md` row and BEFORE the `## Operations` heading (awk line-order check passes), with 03 before 04
    - `grep -c '^- \[Device Configuration Recipes\](#device-configuration-recipes)' docs/index.md` returns exactly 1, and that single extracted line ends with `Windows 11 multi-app kiosk, Android Dedicated multi-app kiosk)` — tested on the extracted line in isolation, never as a whole-file match (a whole-file search for `Dedicated` hits `index.md:36` and three other sites)
    - the link text of each new row is byte-identical to the corresponding `RE-index.md` Title written by plan 137-01: `awk -F'|' '/^\| RE-224 \|/{print $4}' docs/_registry/RE-index.md` trimmed equals the row's bracketed link text; same for RE-225
    - `grep -c '## Device Configuration Recipes' docs/index.md` still returns 1 and `grep -c '| Resource | When to Use |' docs/index.md` is unchanged from HEAD — the section and header were appended to, not re-created
    - `git show --stat` for this commit lists `docs/index.md` as its only content file
    - `git diff --numstat HEAD~1 -- docs/index.md` shows exactly 3 insertions and 1 deletion
    - `node scripts/validation/c17-eee-contract.mjs` exits 0 reporting `234 files checked, 0 with violations`
    - `node scripts/validation/check-nav-hub-links.mjs` exits 0 reporting `0 outbound failure(s), 0 inbound failure(s), 0 total`
    - `node scripts/validation/check-phase-125.mjs` and `node scripts/validation/check-phase-123.mjs` still exit 0 (both read `docs/index.md`)
    - the `## Operations` window still contains the string `Apple Business` (the `V-65-11` invariant, asserted directly because its parent chain runner is red at HEAD for unrelated reasons)
  </acceptance_criteria>
  <done>Commit C exists: both index.md surfaces landed together, C17 and the nav-hub link-checker green, no other file touched.</done>
</task>

<task type="auto">
  <name>Task 2: Hubs-not-wired evidence + the uncovered outbound-link sweep</name>
  <files>(read-only — this task edits nothing)</files>
  <read_first>
    - docs/common-issues.md, docs/quick-ref-l1.md, docs/quick-ref-l2.md (confirm zero recipe references; these are symptom-to-runbook routers, and a recipe is a `doc_type: Guide` zero-to-verified provisioning procedure — routing a symptom to one is a category error)
    - docs/quick-ref-l2.md (line 352 — the existing route to `docs/l2-runbooks/19-android-enrollment-investigation.md`, whose pattern at :226-242 already covers the MHS-unassigned symptom; this is the hub-to-runbook-to-recipe layering that already serves L1 adjacency)
    - docs/recipes/04-android-dedicated-mhs-multi-app.md (lines 107 and 292 — the outbound links to `../l2-runbooks/20-android-app-install-investigation.md`, proving the recipe already links OUT to the runbook layer)
    - scripts/validation/check-nav-hub-links.mjs (lines 210-255: it scans links FROM the 4 hubs and links TARGETING the 4 hubs — nothing else; the recipes' own `../` outbound links are covered by no tool in this repo)
  </read_first>
  <action>
    No file is modified by this task. It implements D-01 (hubs stay not-wired), D-02 (the correction of record), D-03 layer 1 (the mechanical grep), D-05 (the ruling is scoped, no standing rule) and D-17's third gate (the outbound sweep). Produce three pieces of evidence and record all of them verbatim in `137-02-SUMMARY.md`.

    (1) HUBS-NOT-WIRED (D-01, D-03). Run the phase's own hub-enforcement grep and confirm it matches no file, then confirm all three hubs are byte-unchanged across the entire phase via `git diff --quiet` against the phase's base commit. This mirrors how Phase 132 enforced the same ruling — with a raw grep, never with a validator, because the validator that was believed to cover this does not.

    (2) THE CORRECTION OF RECORD (D-02, D-05). State plainly in the SUMMARY, so Phase 138 and 139 cannot re-import the false premise: `check-phase-132.mjs:97` tests a pattern against the three hub files whose arms are `docs/recipes`, `01-shared-windows-avd`, and `02-shared-ipad`. The hub files live IN `docs/`, so a real link from a hub reads `recipes/03-...` — the `docs/recipes` arm never fires, and the other two arms are hardcoded to the previous two recipes. `REQUIREMENTS.md:31`'s claim that it bars `docs/recipes` generically is factually wrong, so the cost of wiring the hubs is zero, not prohibitive. The hubs stay unwired on the doc-class-taxonomy ground and the existing-layering ground alone — NOT on a frozen-validator-cost ground, which is deleted. Record the ruling as SCOPED to RE-224 and RE-225, with the sentence: "The taxonomy ground generalizes, but no standing rule is enacted here — the phase adding recipe 05 re-rules."

    (3) OUTBOUND-LINK SWEEP (D-17, third gate). Recipes 03 and 04 ship 42 `../`-relative outbound links across 10 unique file targets, and `check-nav-hub-links.mjs` covers none of them. Resolve every one against the filesystem, relative to `docs/recipes/`, and confirm each target file exists. There are also 10 anchored targets among them; spot-check that each named anchor exists as a heading in its target file, including `../recipes/01-shared-windows-avd-client.md#step-5a-kiosk-configuration` — recipe 01's Step 5a/5b headings are pinned as literal strings by a frozen predecessor validator, so a drift there would be a corpus-wide problem, not just a broken link. Record the resolved-target list and the sweep result in the SUMMARY. This is a one-off manual sweep; a durable checker is out of scope because v1.19 carries NO-TOOLING-PILLAR.
  </action>
<!-- planner-discipline-allow: recipes/0[34]-|03-windows-11-multi-app-kiosk|04-android-dedicated-mhs -->
<!-- planner-discipline-allow: recipes/0[34]- -->
<!-- planner-discipline-allow: 03-windows-11-multi-app-kiosk -->
<!-- planner-discipline-allow: 04-android-dedicated-mhs -->
  <verify>
    <automated>test -z "$(grep -lE 'recipes/0[34]-|03-windows-11-multi-app-kiosk|04-android-dedicated-mhs' docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md 2>/dev/null)" && git diff --quiet docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md && test -z "$(grep -hoE '\]\(\.\./[^)]+\)' docs/recipes/03-windows-11-multi-app-kiosk.md docs/recipes/04-android-dedicated-mhs-multi-app.md | sed -E 's/^\]\(//; s/\)$//; s/#.*$//' | sort -u | while read -r t; do [ -f "docs/recipes/$t" ] || echo "MISSING: $t"; done)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -lE 'recipes/0[34]-|03-windows-11-multi-app-kiosk|04-android-dedicated-mhs' docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md` prints nothing (no file matches)
    - `git diff --quiet docs/common-issues.md docs/quick-ref-l1.md docs/quick-ref-l2.md` succeeds against the phase base commit — all three hubs byte-unchanged
    - the outbound sweep prints no `MISSING:` line; the resolved unique-target list has 10 entries and is quoted in the SUMMARY
    - all 10 anchored outbound targets are spot-checked and their anchors exist as headings, explicitly including `Step 5a` in `docs/recipes/01-shared-windows-avd-client.md`
    - `git diff --quiet docs/recipes/01-shared-windows-avd-client.md` succeeds — the zero-edit guard on recipe 01 holds
    - `137-02-SUMMARY.md` contains the correction of record naming `check-phase-132.mjs:97` and `REQUIREMENTS.md:31`, and the scoped-ruling sentence verbatim
    - `git diff --quiet scripts/validation/check-phase-132.mjs` succeeds — the frozen surface was not touched
  </acceptance_criteria>
  <done>Hubs proven unwired and untouched, the false-coverage premise corrected on the record, and all 42 uncovered outbound links proven to resolve.</done>
</task>

<task type="auto">
  <name>Task 3: Needle-spec handoff to Phase 138 + deferred-cleanup candidate</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (lines 322-328: the existing `### Plan-Time Research Flags (not blockers — resolve at each phase's plan time)` block, which already carries two Phase-138 entries at :326 and :327 — append there, do NOT open a parallel entry alongside them)
    - docs/index.md (the two rows and the bullet line as SHIPPED by Task 1 — the spec must carry MEASURED literals, not planned ones)
    - .planning/phases/137-integration-navigation-last-close/137-CONTEXT.md (D-19, D-20, D-21 — the needle shape and its exact literals; D-06 — the deferred-cleanup candidate text)
    - .planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-01-SUMMARY.md (line 113 area) and .planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-01-PLAN.md (line 25 area) — the milestone's carrier convention for drafting a deferred-cleanup entry inside a phase SUMMARY, since the `v1.19-DEFERRED-CLEANUP.md` file itself is created by Phase 138
  </read_first>
  <action>
    Write the specification a Phase-138 author will build `check-phase-137.mjs` from. Per D-18, do NOT author the validator — leaf validators are HARN-15's deliverable and `check-phase-135..138.mjs` is already named as a Phase-138 output. This task implements D-18, D-19, D-20, D-21, D-22 and D-06.

    First re-read the two shipped table rows and the shipped bullet line out of `docs/index.md` and use those measured strings, not the planned ones, everywhere below.

    STATE.md — append ONE bullet to the existing Plan-Time Research Flags block, immediately after the two Phase-138 entries already there, in their style. It must say that Phase 138's `check-phase-137.mjs` carries a per-recipe, LINE-SCOPED co-presence invariant (extract the one bullet line and test it in isolation — never a whole-file `c.includes()`, because a whole-file search for `Dedicated` matches `index.md:36` two lines above the recipes bullet plus three other sites), and must name the four literals: table targets `recipes/03-windows-11-multi-app-kiosk.md` and `recipes/04-android-dedicated-mhs-multi-app.md`; bullet fragments `Windows 11 multi-app kiosk` and `Android Dedicated multi-app kiosk`. It must also require a hubs-not-wired assertion using the CORRECTED literals `recipes/03-` and `recipes/04-` — that assertion belongs in the new leaf validator precisely because the v1.18 predecessor's own pattern does not cover them and must not be edited. Add that the needle is only buildable because the bullet-side literals were fixed at ruling time.

    `137-02-SUMMARY.md` — carry a section headed exactly `Needle-spec handoff`. This is the surface the harness already reads: the prior analogous leaf validator's header states its needles were derived inline from the phase VERIFICATION document, so this section is what the phase verifier lifts into `137-VERIFICATION.md`. It must carry, as measured actuals: the full shipped bullet line text, both shipped table link texts, both shipped table target paths, and the measured word count of each blurb.

    `137-02-SUMMARY.md` — also carry a flagged `v1.19-DEFERRED-CLEANUP.md` candidate contribution with this entry text verbatim: "If a kiosk-lockout or MHS-exit-PIN-lockout L1/L2 runbook is ever authored, the hub routes to THAT runbook and the runbook may cross-link the recipe; the three hubs never link `docs/recipes` directly." Flag it as a contribution — Phase 138 creates the file.

    `137-02-SUMMARY.md` — state plainly the tension this phase is closing by hand: CLASS-06 asks for a validator needle rather than reliance on code review; the needle lands in Phase 138; Phase 138 is hard-blocked on the owner's push. The in-phase mechanical grep pair from Task 1 is what covers that window.

    Commit STATE.md alone. Subject: `docs(137-02): hand the check-phase-137 needle-spec to Phase 138`.
  </action>
  <verify>
    <automated>grep -q 'check-phase-137.mjs' .planning/STATE.md && grep -q 'Android Dedicated multi-app kiosk' .planning/STATE.md && grep -q 'recipes/04-android-dedicated-mhs-multi-app.md' .planning/STATE.md && awk '/^### Plan-Time Research Flags/{s=NR} /^### Pending Todos/{e=NR} /check-phase-137/{n=NR} END{exit !(s<n && n<e)}' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - the new STATE.md bullet's line number falls between the `### Plan-Time Research Flags` heading and the `### Pending Todos` heading (awk containment check passes) — appended to the existing block, not a parallel entry
    - `grep -c 'check-phase-137.mjs' .planning/STATE.md` returns at least 1
    - STATE.md contains all four needle literals: `recipes/03-windows-11-multi-app-kiosk.md`, `recipes/04-android-dedicated-mhs-multi-app.md`, `Windows 11 multi-app kiosk`, `Android Dedicated multi-app kiosk`
    - STATE.md contains the corrected hubs literals `recipes/03-` and `recipes/04-`
    - `137-02-SUMMARY.md` contains a heading matching `^#+ .*Needle-spec handoff`
    - the bullet line text recorded under that heading is byte-identical to `grep '^- \[Device Configuration Recipes\](#device-configuration-recipes)' docs/index.md`
    - both table link texts and both target paths recorded under that heading are byte-identical to the shipped `docs/index.md` rows
    - `137-02-SUMMARY.md` contains the deferred-cleanup candidate entry text verbatim, marked as a flagged contribution
    - `ls scripts/validation/check-phase-137.mjs` fails — no validator was authored in this phase
    - `git show --stat` for this commit lists `.planning/STATE.md` as its only file
  </acceptance_criteria>
  <done>Phase 138 has a buildable, line-scoped needle-spec with measured literals in both STATE.md and the SUMMARY; the deferred-cleanup candidate is drafted; no validator was authored.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 137-02's share)

| Artifact | Exact value | Where |
|----------|-------------|-------|
| index.md table row RE-224 | `\| [Windows 11 Multi-App Kiosk: Assigned Access Provisioning](recipes/03-windows-11-multi-app-kiosk.md) \| Lock a Windows 11 device to a restricted user experience -- ... \|` | `docs/index.md`, after the RE-223 row |
| index.md table row RE-225 | `\| [Android Dedicated Multi-App Kiosk: Managed Home Screen Provisioning](recipes/04-android-dedicated-mhs-multi-app.md) \| Lock an Android Enterprise Dedicated device to a curated Managed Home Screen app grid -- ... \|` | `docs/index.md`, after the RE-224 row |
| Line-38 quick-nav bullet (replaced) | `- [Device Configuration Recipes](#device-configuration-recipes) -- End-to-end provisioning recipes with embedded admin decision points (shared Windows AVD-client device, Shared iPad full provisioning, Windows 11 multi-app kiosk, Android Dedicated multi-app kiosk)` | `docs/index.md` line 38 |
| Needle-spec handoff | Line-scoped per-recipe co-presence invariant + corrected hubs literals, with measured actuals | `.planning/STATE.md` Plan-Time Research Flags block + a `Needle-spec handoff` section in `137-02-SUMMARY.md` |
| Correction of record (D-02) | The v1.18 hubs regex does not cover recipes 03/04; `REQUIREMENTS.md:31` is wrong on this point | `137-02-SUMMARY.md`, for lift into `137-VERIFICATION.md` |
| Deferred-cleanup candidate (D-06) | Non-barred-surface hub wiring, routed to whatever phase authors a kiosk-lockout or MHS-exit-PIN-lockout runbook | `137-02-SUMMARY.md`, flagged contribution to the Phase-138-created file |

Produced by plan 137-01 (listed for phase completeness): the two `RE-index.md` rows, the two `filename-map.md` entries, the two `.docx` output stems, and the two canary bumps.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (docs-only) | The change surface is one Markdown navigation edit plus one planning-document append, backed by read-only greps and validator runs. No executable code, network, auth, session, or user-input path. Follows the `132-02-PLAN.md:108` precedent for this same job. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-137-06 | Tampering | The WR-01 defect class — the table row lands without the quick-nav bullet, or vice versa | high | mitigate | Both edits are one task and one commit; `git show --stat` asserts a single content file and `git diff --numstat` asserts 3 insertions / 1 deletion; the line-38 assertion is line-scoped, not whole-file |
| T-137-07 | Information Disclosure | Broken or dangling recipe links in `docs/index.md` | low | mitigate | Anchored `grep -c` on each exact `(recipes/0N-....md)` target returns 1; `check-nav-hub-links.mjs` 0/0; C17 corpus gate re-validates both enrolled recipe files |
| T-137-08 | Tampering | Accidental hub edit — recipes wired where the ruling says they must not be | medium | mitigate | `git diff --quiet` on all three hubs asserts byte-unchanged; the D-03 grep asserts zero references; both are recorded evidence, not a code change |
| T-137-09 | Tampering | The v1.18 frozen validator surface edited to "fix" its miscovering regex | high | mitigate | Explicitly prohibited in `must_honor`; `git diff --quiet scripts/validation/check-phase-132.mjs` asserted; the corrected coverage is additive, in the Phase-138 needle-spec |
| T-137-10 | Repudiation | Navigation-last claimed but not provable — plan-artifact commits make "the SHA per plan" ambiguous | medium | mitigate | Commits A/B/C are named explicitly by SHA with author timestamps in the SUMMARY; the ordering assertion is `Commit C author timestamp > Commit B author timestamp`, with plan-artifact commits excluded by name |
| T-137-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs in this phase; no supply-chain surface |
</threat_model>

<verification>
- Both index.md table rows present with H1-verbatim link text and the exact relative targets, in filename order, after RE-223 and before `## Operations`
- The single line-38 bullet, extracted in isolation, ends with `Windows 11 multi-app kiosk, Android Dedicated multi-app kiosk)`
- Commit C is a single-content-file commit with 3 insertions / 1 deletion on `docs/index.md`
- Commit C's author timestamp strictly post-dates plan 137-01's registry/regeneration commit's author timestamp — both named by SHA, plan-artifact commits excluded by name
- The D-03 hub grep matches nothing; all three hubs byte-unchanged; `check-phase-132.mjs` byte-unchanged
- All 42 `../` outbound links in recipes 03/04 resolve; 10 anchored targets spot-checked
- `node scripts/validation/c17-eee-contract.mjs` exits 0 at `234 files checked, 0 with violations`
- `node scripts/validation/check-nav-hub-links.mjs` exits 0 at `0 outbound failure(s), 0 inbound failure(s), 0 total`
- `check-phase-123` and `check-phase-125` still exit 0; the `## Operations` window still contains `Apple Business`
- STATE.md's Plan-Time Research Flags block carries the Phase-138 needle-spec with all six literals; `scripts/validation/check-phase-137.mjs` does NOT exist
</verification>

<success_criteria>
CLASS-06 complete: both recipes reachable from `docs/index.md` on both surfaces in one commit; navigation-last provable by commit timestamp against a wave-1 SHA; the troubleshooting-hub disposition recorded as an explicit ruling with its correction of record; full-corpus C17 green and link-checker 0/0. ROADMAP SC3, SC4, and SC5 satisfied. Phase 137 done.
</success_criteria>

<output>
Create `.planning/phases/137-integration-navigation-last-close/137-02-SUMMARY.md` when done.

It MUST carry: (a) the `Needle-spec handoff` section with measured actuals; (b) the D-02 correction of record verbatim; (c) the scoped-ruling sentence declining a standing rule; (d) the flagged `v1.19-DEFERRED-CLEANUP.md` candidate entry; (e) the outbound-link sweep's resolved-target list and result; (f) Commit C's SHA and author timestamp alongside plan 137-01's Commit B SHA and author timestamp, so the navigation-last ordering is checkable from the SUMMARY alone; (g) the measured word count of each new table blurb.

Do NOT copy Phase 132's "zero pipeline code changes" observable truth or its `git diff --quiet scripts/pipeline/build-filename-map.mjs` evidence into anything this phase produces — that check is expected to fail here, and its replacement is plan 137-01's two `--self-test` runs.
</output>
