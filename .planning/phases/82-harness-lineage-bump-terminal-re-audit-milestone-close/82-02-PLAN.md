---
phase: 82-harness-lineage-bump-terminal-re-audit-milestone-close
plan: 02
type: execute
wave: 2
depends_on:
  - 82-01
files_modified:
  - scripts/validation/check-phase-75.mjs
  - scripts/validation/check-phase-76.mjs
  - scripts/validation/check-phase-77.mjs
  - scripts/validation/check-phase-78.mjs
  - scripts/validation/check-phase-79.mjs
  - scripts/validation/check-phase-80.mjs
  - scripts/validation/check-phase-81.mjs
  - scripts/validation/check-phase-82.mjs
  - .github/workflows/audit-harness-v1.9-integrity.yml
autonomous: true
requirements:
  - SSOHARN-02
  - SSOHARN-03
must_haves:
  truths:
    - "check-phase-75..82.mjs all exist and exit 0 standalone (8 net-new validators) per SSOHARN-02"
    - "check-phase-82 chain-apex declares CHAIN_PHASES=[48..81] and CHAIN_SKIP=new Set([]) (empty); V-82-SELF asserts 82 not in chain AND CHAIN_SKIP.size===0"
    - "check-phase-81 hard-asserts all 8 V-81-CROSSLINK SSO-E edges (E1-E8) per D-01"
    - "audit-harness-v1.9-integrity.yml is the 6th coexistence workflow with 8 jobs check-phase-75..82, inheriting fetch-depth:0 / autocrlf false / continue-on-error:false / timeout-minutes:30 per SSOHARN-03"
    - "Atom 2 (8 validators + workflow) lands as ONE indivisible commit per ROADMAP SC#2 and is pushed to origin/master"
  artifacts:
    - path: "scripts/validation/check-phase-82.mjs"
      provides: "chain-apex validator; CHAIN_PHASES=[48..81]; drops V-74-VPP-01a/b; richer dual-invariant SELF"
      contains: "CHAIN_SKIP = new Set([])"
    - path: "scripts/validation/check-phase-81.mjs"
      provides: "non-apex validator carrying V-81-CROSSLINK-01..08 (8 SSO-E hard-asserts)"
      contains: "CROSSLINK"
    - path: "scripts/validation/check-phase-75.mjs"
      provides: "lightweight net-new validator (SELF + phase-presence assertion)"
      min_lines: 40
    - path: ".github/workflows/audit-harness-v1.9-integrity.yml"
      provides: "6th coexistence CI workflow; 8 jobs check-phase-75..82; Linux apex; negative-control quarterly job"
      contains: "check-phase-82.mjs"
  key_links:
    - from: "scripts/validation/check-phase-82.mjs"
      to: "scripts/validation/check-phase-{48..81}.mjs"
      via: "CHAIN subprocess fan-out (execFileSync, CHECK_PHASE_NESTED=1)"
      pattern: "CHECK_PHASE_NESTED"
    - from: "scripts/validation/check-phase-81.mjs"
      to: "docs/admin-setup-macos/07-platform-sso-setup.md (+ 7 more SSO-E source files)"
      via: "CRLF-normalized readFile substring hard-assert"
      pattern: "platform-sso-setup\\.md"
    - from: ".github/workflows/audit-harness-v1.9-integrity.yml"
      to: "scripts/validation/check-phase-82.mjs"
      via: "linux-chain-ubuntu-latest apex job"
      pattern: "check-phase-82\\.mjs"
---

<objective>
Ship Atom 2 (SSOHARN-02 + SSOHARN-03) as ONE indivisible commit: 8 net-new per-phase validators (`check-phase-75.mjs` through `check-phase-82.mjs`) plus the 6th-coexistence CI workflow `audit-harness-v1.9-integrity.yml`. Then push to `origin/master` (HARD ordering gate for Plan 82-03 Axis-2 dispatch).

Purpose: Author the per-phase validator chain for v1.9 (phases 75-81 never shipped per-phase validators during execution → all 8 are net-new, unlike v1.8 where 71/72/73 pre-existed). The chain-apex `check-phase-82.mjs` replays the full chain [48..81]; `check-phase-81.mjs` carries the 8 SSO-E cross-link assertions (D-01); 75-80 are lightweight (DIVERGENCE-6 resolved: lightweight, not full-chain). The CI workflow gives the cross-OS Linux surface for Plan 82-03's Axis 2.

Output: 8 validator files + 1 workflow file in ONE Atom-2 commit, pushed to origin/master.

This is a Path-A phase. All 6 RESEARCH divergences are pre-resolved in the task actions: apex DROPS V-74-VPP-01a/b (DIVERGENCE-2); 75-80 are LIGHTWEIGHT (DIVERGENCE-6); the negative control is the existing `rotting-external-quarterly` cron-gated SKIP, NOT a new job (DIVERGENCE-5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-CONTEXT.md
@.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-RESEARCH.md
@.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-PATTERNS.md
@.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-CONVENTIONS.md
@.planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 6 lightweight net-new validators check-phase-75..80.mjs</name>
  <files>scripts/validation/check-phase-75.mjs, scripts/validation/check-phase-76.mjs, scripts/validation/check-phase-77.mjs, scripts/validation/check-phase-78.mjs, scripts/validation/check-phase-79.mjs, scripts/validation/check-phase-80.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-71.mjs` (Path-A structural shell — imports, CRLF `readFile` lines 35-39, `checks[]`, richer dual-invariant SELF lines 246-260, runner loop lines 262-289)
    - `scripts/validation/check-phase-75.mjs` (one of the files you are creating — read after first write to confirm shape)
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-RESEARCH.md` §Q2 (non-apex template) + DIVERGENCE-6 (lightweight decision) + Open Question 2
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-PATTERNS.md` "check-phase-75..80.mjs" + "Shared Patterns" (CRLF read, dual-invariant SELF, runner loop)
  </read_first>
  <action>
    Author `check-phase-75.mjs` through `check-phase-80.mjs` as LIGHTWEIGHT validators (DIVERGENCE-6 / Open Question 2 — RESEARCH + PATTERNS both lock lightweight over the heavy 71/72/73 full-chain shape, because 75-80 shipped docs/corpus already covered by the milestone-audit C-checks, and lightweight keeps cross-OS counts deterministic and avoids the O(n²) cold-clone cost). Each validator:
    - Copies the check-phase-71 structural shell: imports (`readFileSync, existsSync` from node:fs, `join` from node:path, `process`), the CRLF-normalizing `readFile` helper (`.replace(/\r\n/g,'\n')`), the `LABEL_WIDTH=60` padLabel runner loop, and the `[id/checks.length]` prefix + `Result: N PASS, M FAIL, K SKIPPED` + `process.exit(failed>0?1:0)` contract.
    - Declares `const CHAIN_PHASES = []` (lightweight — NO chain; the chain lives ONLY in apex 82) and `const CHAIN_SKIP = new Set([])`.
    - Carries TWO checks only: (1) a `V-NN-SELF` richer dual-invariant guard asserting `!CHAIN_PHASES.includes(NN)` AND `CHAIN_SKIP.size === 0` (per check-phase-71:247-260); and (2) one `V-NN-PRESENCE` phase-presence assertion confirming the phase's primary deliverable file exists and is non-empty (use the phase's headline doc, e.g. check-phase-76 → `docs/admin-setup-macos/07-platform-sso-setup.md`, check-phase-77 → `docs/admin-setup-macos/08-auth-methods-deep-dive.md`, check-phase-78 → `docs/admin-setup-macos/09-enterprise-sso-plugin-migration.md`, check-phase-79 → `docs/reference/macos-capability-matrix.md`, check-phase-80 → `docs/l1-runbooks/35-macos-sso-sign-in-failure.md`, check-phase-75 → `docs/_glossary-macos.md`; confirm the exact deliverable filenames against the phase SUMMARYs / ROADMAP before pinning). Relabel `71`→`NN` and the banner `check-phase-NN -- Phase NN …` throughout.
    Do NOT use frozen reads (no `_lib/frozen-at-close.mjs` import) — these are local HEAD presence checks. Deterministic PASS counts are load-bearing for the Plan 82-03 cross-OS EXACT MATCH diff.
    DO NOT commit yet — Atom 2 commits as one unit after Task 4.
  </action>
  <verify>
    <automated>for n in 75 76 77 78 79 80; do node scripts/validation/check-phase-$n.mjs || exit 1; done; echo "75-80 all exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - check-phase-75.mjs..80.mjs all exist and `node check-phase-NN.mjs` exits 0 standalone.
    - Each declares `CHAIN_PHASES = []` (lightweight) and `CHAIN_SKIP = new Set([])`.
    - Each carries exactly a `V-NN-SELF` dual-invariant guard + one `V-NN-PRESENCE` deliverable-existence assertion.
    - Each prints a deterministic `Result: N PASS, M FAIL, K SKIPPED` line with FAIL=0.
  </acceptance_criteria>
  <done>6 lightweight net-new validators check-phase-75..80.mjs authored, each exits 0 standalone; not yet committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author check-phase-81.mjs with V-81-CROSSLINK 8-edge hard-asserts</name>
  <files>scripts/validation/check-phase-81.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-71.mjs` (Path-A structural shell)
    - `scripts/validation/check-phase-67.mjs` (V-67-03/04 assertion form, lines 117-159 — read N files, CRLF-normalized, assert substring presence)
    - `scripts/validation/check-phase-81.mjs` (the file you are creating)
    - `.planning/phases/81-nav-hub-integration/81-CROSSLINK-CLOSURE.md` (the 8 SSO-E edges with file:line evidence — confirm the 8 needles before authoring)
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-RESEARCH.md` §Q3 (SSO_EDGES drop-in array with E1-E8) + DIVERGENCE / D-01
  </read_first>
  <behavior>
    - `node scripts/validation/check-phase-81.mjs` exits 0 today (all 8 edges currently present in the corpus).
    - The validator declares 8 checks `CROSSLINK-E1` … `CROSSLINK-E8`, each asserting one forward-slash needle is present in its source file (CRLF-normalized read).
    - If any SSO-E source file is missing or its needle is absent, the corresponding check FAILs (blocking — no allowlist).
    - V-81-SELF asserts `!CHAIN_PHASES.includes(81)` AND `CHAIN_SKIP.size === 0`.
  </behavior>
  <action>
    Author `check-phase-81.mjs` from the check-phase-71 structural shell (lightweight base like 75-80: `CHAIN_PHASES = []`, `CHAIN_SKIP = new Set([])`, CRLF `readFile`, dual-invariant `V-81-SELF`, standard runner loop) PLUS the V-81-CROSSLINK assertions (the 81 exception per D-01). Build the `SSO_EDGES` array verbatim from RESEARCH §Q3 — the 8 (id, file, needle) triples (CONFIRM each against `81-CROSSLINK-CLOSURE.md` first):
    E1 `docs/admin-setup-macos/07-platform-sso-setup.md` → `../_glossary-macos.md#platform-sso`;
    E2 `docs/_glossary-macos.md` → `admin-setup-macos/07-platform-sso-setup.md`;
    E3 `docs/admin-setup-macos/07-platform-sso-setup.md` → `../reference/macos-capability-matrix.md#authentication`;
    E4 `docs/reference/macos-capability-matrix.md` → `../admin-setup-macos/07-platform-sso-setup.md`;
    E5 `docs/l1-runbooks/35-macos-sso-sign-in-failure.md` → `../l2-runbooks/27-macos-sso-investigation.md`;
    E6 `docs/l2-runbooks/27-macos-sso-investigation.md` → `../l1-runbooks/35-macos-sso-sign-in-failure.md`;
    E7 `docs/admin-setup-macos/03-configuration-profiles.md` → `07-platform-sso-setup.md`;
    E8 `docs/macos-lifecycle/00-ade-lifecycle.md` → `../admin-setup-macos/07-platform-sso-setup.md`.
    For each edge push a check `CROSSLINK-${e.id}` named `V-81-CROSSLINK-${e.id}: …` whose `run()` does: `const c = readFile(e.file)` (CRLF-normalized, Windows-safe forward-slash needle per D-01); if `c === null` return fail `e.file + ' missing'`; if `!c.includes(e.needle)` return fail `${e.id} needle absent: ${e.needle}`; else return pass. NO allowlist / NO sidecar entries (mirrors C16 empty `c16_missing_endpoint_exemptions: []`; hard-assert all 8). Mirror the V-67-03 read pattern but assert presence (not count). Relabel banner `check-phase-81 -- Phase 81 …`.
    DO NOT commit yet — Atom 2 commits as one unit after Task 4.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-81.mjs && node -e "const c=require('fs').readFileSync('scripts/validation/check-phase-81.mjs','utf8');const needles=['../_glossary-macos.md#platform-sso','../reference/macos-capability-matrix.md#authentication','../l2-runbooks/27-macos-sso-investigation.md','../l1-runbooks/35-macos-sso-sign-in-failure.md','../admin-setup-macos/07-platform-sso-setup.md'];const miss=needles.filter(n=>!c.includes(n));if(miss.length){console.error('missing needles in source:',miss);process.exit(1)}const n=(c.match(/CROSSLINK-E[1-8]/g)||[]).length;if(n<8){console.error('CROSSLINK count',n);process.exit(1)}console.log('V-81-CROSSLINK 8 edges OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-81.mjs` exits 0 (8/8 CROSSLINK pass + SELF pass).
    - The validator references all 8 SSO-E needles (E1-E8) verbatim and declares `CROSSLINK-E1`..`CROSSLINK-E8` checks.
    - Uses the CRLF-normalizing `readFile` (forward-slash needles, no allowlist).
    - V-81-SELF dual-invariant guard present.
  </acceptance_criteria>
  <done>check-phase-81.mjs authored with 8 V-81-CROSSLINK hard-asserts (D-01); exits 0; not yet committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the chain-apex check-phase-82.mjs (CHAIN_PHASES=[48..81], drop VPP)</name>
  <files>scripts/validation/check-phase-82.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-74.mjs` (Path-A source — 222 lines; constants at 41/47/50, NESTED loop 124-159, AUDIT-HARNESS 161-181, runner 194-221; carries V-74-VPP-01a/b at 58-103 → DROP)
    - `scripts/validation/check-phase-71.mjs` (richer dual-invariant SELF lines 247-260 — use THIS form for V-82-SELF, not check-phase-74's single-invariant SELF)
    - `scripts/validation/check-phase-82.mjs` (the file you are creating)
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-RESEARCH.md` §Q2 (apex template) + DIVERGENCE-2 (drop VPP) + Open Question 1
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-PATTERNS.md` "check-phase-82.mjs (chain-apex)"
  </read_first>
  <behavior>
    - `node scripts/validation/check-phase-82.mjs --verbose` exits 0; replays CHAIN-48 through CHAIN-81 (34 subprocesses) plus AUDIT, AUDIT-HARNESS, SELF.
    - `CHAIN_PHASES` lists every integer 48..81; `CHAIN_SKIP` is `new Set([])` (empty).
    - V-82-SELF asserts `!CHAIN_PHASES.includes(82)` AND `CHAIN_SKIP.size === 0`.
    - HARNESS points at `v1.9-milestone-audit.mjs`.
    - V-74-VPP-01a/b have NO v1.9 equivalent and are absent.
  </behavior>
  <action>
    Copy `check-phase-74.mjs` → `check-phase-82.mjs` and apply (per §Q2 / DIVERGENCE-2):
    - Line 41: `const HARNESS = 'scripts/validation/v1.8-milestone-audit.mjs';` → `v1.9-milestone-audit.mjs`.
    - Line 47: `const CHAIN_PHASES = [48,…,73];` → list every integer `[48,49,50,…,80,81]` (add 74-81).
    - Line 50: `const CHAIN_SKIP = new Set([]);` → UNCHANGED, stays empty (D-03 / SC#2).
    - DROP both `V-74-VPP-01a` (lines 58-75) and `V-74-VPP-01b` (lines 83-103) — v1.9 has NO corpus rename, so these have no analog (DIVERGENCE-2 / Open Question 1 recommendation). The apex = AUDIT + CHAIN(48..81) + AUDIT-HARNESS + SELF only.
    - Copy the NESTED CHAIN loop verbatim (becomes V-82-CHAIN-{48..81}); the `CHECK_PHASE_NESTED=1` short-circuit + `isPeer = phaseNum >= 67 ? 600000 : 300000` timeout logic carries unchanged (phases 75-81 are all peers → 600s).
    - AUDIT assertion: repoint heading-presence from `74-VERIFICATION.md` + `/Phase 74/i` → `82-VERIFICATION.md` + `/Phase 82/i` (SKIP-PASS until 82-VERIFICATION.md authored in Plan 82-04).
    - AUDIT-HARNESS: repoint HARNESS const to `v1.9-milestone-audit.mjs`; the v1.8 label says "predecessor-byte-unchanged" but for v1.9 the harness is NEW → adjust the wording (e.g. "v1.9-milestone-audit.mjs exits 0 (current-milestone harness)").
    - V-82-SELF: use the RICHER check-phase-71 dual-invariant form — assert `!CHAIN_PHASES.includes(82)` AND `CHAIN_SKIP.size === 0` (NOT check-phase-74's single-invariant SELF).
    - Runner banner: `check-phase-82 -- Phase 82 …`.
    DO NOT commit yet — Atom 2 commits as one unit after Task 4.
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-82.mjs --verbose && node -e "const c=require('fs').readFileSync('scripts/validation/check-phase-82.mjs','utf8');if(!c.includes('v1.9-milestone-audit.mjs')){console.error('HARNESS not repointed');process.exit(1)}if(!/CHAIN_SKIP\s*=\s*new Set\(\[\]\)/.test(c)){console.error('CHAIN_SKIP not empty');process.exit(1)}if(!c.includes('81')||!c.includes('48')){console.error('CHAIN_PHASES range');process.exit(1)}if(/VPP-01/.test(c)){console.error('VPP assertions not dropped');process.exit(1)}if(/CHAIN_PHASES.includes(82)/.test(c)===false&&!c.includes('82')){console.error('SELF guard');process.exit(1)}console.log('apex OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-82.mjs --verbose` exits 0; replays CHAIN-48..81.
    - `CHAIN_PHASES` lists every int 48..81; `CHAIN_SKIP = new Set([])`.
    - V-82-SELF asserts both `!CHAIN_PHASES.includes(82)` and `CHAIN_SKIP.size === 0`.
    - HARNESS = `v1.9-milestone-audit.mjs`; no `VPP-01` assertions present.
    - AUDIT points at `82-VERIFICATION.md` / `Phase 82`.
  </acceptance_criteria>
  <done>chain-apex check-phase-82.mjs authored (chain [48..81], empty CHAIN_SKIP, VPP dropped, richer SELF, harness repointed); exits 0; not yet committed.</done>
</task>

<task type="auto">
  <name>Task 4: Author audit-harness-v1.9-integrity.yml (6th coexistence), commit Atom 2 indivisibly, push to origin/master</name>
  <files>.github/workflows/audit-harness-v1.9-integrity.yml</files>
  <read_first>
    - `.github/workflows/audit-harness-v1.8-integrity.yml` (Path-A source — 201 lines; header/triggers 1-26, parse/path-match 29-61, harness-run 63-72, linux-chain apex 74-92, per-validator jobs 94-148, rotting-external-quarterly 150-182, pin-helper-advisory 184-200)
    - `.github/workflows/audit-harness-v1.9-integrity.yml` (the file you are creating)
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-RESEARCH.md` §Q7 (YAML job structure) + DIVERGENCE-5 (negative control) + §Q10 (predecessor list)
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-PATTERNS.md` "audit-harness-v1.9-integrity.yml"
  </read_first>
  <action>
    Copy `audit-harness-v1.8-integrity.yml` → `audit-harness-v1.9-integrity.yml` (the 6th coexistence file; coexists with base v1.4 `audit-harness-integrity.yml` + v1.5/v1.6/v1.7/v1.8 — ZERO edits to those 5 predecessors) and apply (per §Q7):
    - `name: Audit Harness v1.8 Integrity` → `v1.9`.
    - `on.pull_request.paths`: repoint all `v1.8` → `v1.9` (the `v1.8-*` glob, the self-path yml, `v1.8-MILESTONE-AUDIT.md`, `v1.8-DEFERRED-CLEANUP.md`); keep `check-phase-*.mjs` and `REQUIREMENTS.md`.
    - `on.schedule`: CARRY BOTH crons (`0 8 * * 1` weekly; `0 8 1 1,4,7,10 *` quarterly). Keep `workflow_dispatch`.
    - `parse` / `path-match` / `harness-run` jobs: repoint inline `v1.8-audit-allowlist.json` / `v1.8-milestone-audit.mjs` references → `v1.9`.
    - `linux-chain-ubuntu-latest` apex job: keep `timeout-minutes: 30`, `continue-on-error: false`, `git config --global core.autocrlf false` BEFORE checkout, `actions/checkout@v4 with fetch-depth: 0`, `CHAIN_TIMING_LINUX ::notice`; repoint apex run to `check-phase-82.mjs --verbose`.
    - Per-validator jobs: REPLACE the 71/72/73/74 set with 8 jobs `check-phase-75` through `check-phase-82`, each `timeout-minutes: 15`, `continue-on-error: false`, `fetch-depth: 0`, node 20.
    - `rotting-external-quarterly`: keep AS-IS structurally — this IS the negative control (its `if: schedule && '0 8 1 1,4,7,10 *'` SKIPs on workflow_dispatch). Repoint its `v1.8-audit-allowlist.json` → `v1.9`. Keep `markdown-link-check@3.14.2` pinned. DO NOT author a separate job literally named "negative-control" (DIVERGENCE-5).
    - `pin-helper-advisory`: `continue-on-error: true`, no version string → carry unchanged.
    Validate YAML parses. THEN COMMIT ATOM 2 AS ONE INDIVISIBLE COMMIT (ROADMAP SC#2) — stage ALL 9 Atom-2 files together: `check-phase-75.mjs`..`check-phase-82.mjs` (8) + `audit-harness-v1.9-integrity.yml`. Commit message: `feat(82-02): v1.9 validators + CI surface — SSOHARN-02/03 (atomic SC#1 Atom 2)`. NEVER split Atom 2 across commits.
    THEN PUSH to origin/master: `git push origin master`. This is the HARD ordering gate for Plan 82-03 — the workflow's check-phase-75..82 jobs FAIL (`cannot find module`) if dispatched against a ref that lacks them.
  </action>
  <verify>
    <automated>node -e "const yaml=require('fs').readFileSync('.github/workflows/audit-harness-v1.9-integrity.yml','utf8');const need=['Audit Harness v1.9','check-phase-82.mjs','check-phase-75','fetch-depth: 0','core.autocrlf false','continue-on-error: false','timeout-minutes: 30'];const miss=need.filter(n=>!yaml.includes(n));if(miss.length){console.error('MISSING:',miss);process.exit(1)}for(let p=75;p<=82;p++){if(!yaml.includes('check-phase-'+p)){console.error('missing job check-phase-'+p);process.exit(1)}}if(yaml.includes('v1.8-milestone-audit')||yaml.includes('v1.8-audit-allowlist')){console.error('stale v1.8 ref');process.exit(1)}console.log('v1.9 workflow OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `audit-harness-v1.9-integrity.yml` exists, parses, named `Audit Harness v1.9 Integrity`, references `check-phase-75`..`check-phase-82` (8 jobs), inherits `fetch-depth: 0` / `core.autocrlf false` / `continue-on-error: false` / `timeout-minutes: 30`; no stale `v1.8-milestone-audit`/`v1.8-audit-allowlist` references; quarterly negative-control job preserved (no new "negative-control" job).
    - Atom 2 is ONE commit (`feat(82-02): … atomic SC#1 Atom 2`) containing exactly the 9 Atom-2 files; `git show --stat HEAD` lists all 9 and no others.
    - `git log origin/master --oneline -1` shows the Atom-2 commit (pushed to origin/master).
    - The 5 predecessor workflows (base + v1.5/v1.6/v1.7/v1.8) are byte-unchanged.
  </acceptance_criteria>
  <done>audit-harness-v1.9-integrity.yml authored (6th coexistence); Atom 2 committed as one indivisible commit and pushed to origin/master (D-03 ordering gate satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Tooling-only phase: Node ESM validators + a GitHub Actions workflow. The realistic boundary is CI-supply-chain / harness-integrity — a validator that silently passes, a workflow with excessive permissions, or the apex chain that fails to short-circuit and exhausts CI time.

| Boundary | Description |
|----------|-------------|
| Per-phase validator → corpus | A mis-authored CROSSLINK assert could pass with a missing edge (silent pass) |
| GHA workflow → repo checkout | Workflow permissions / fetch-depth determine cross-OS fidelity |
| Apex chain subprocess fan-out | NESTED short-circuit must prevent O(n²) recursive expansion / timeout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-02-01 | Tampering | check-phase-81.mjs V-81-CROSSLINK (could pass with a needle absent) | mitigate | Hard-assert (no allowlist); verify exits 0 today AND grep all 8 needles present in source; `c === null` and `!includes(needle)` both FAIL |
| T-82-02-02 | Denial of Service | check-phase-82 apex cold-chain O(n²) (WINDOWS-CLONE-DEEPNEST-TIMEOUT-01, worse at [48..81]) | mitigate | NESTED short-circuit carried verbatim; isPeer 600s timeout; Plan 82-03 takes apex counts from warm-tree × Linux GHA, not cold-clone |
| T-82-02-03 | Elevation of Privilege | audit-harness-v1.9-integrity.yml job permissions | mitigate | Path-A copy inherits least-privilege defaults from v1.8 (no `permissions: write`, no secrets); `continue-on-error: false` on blocking jobs |
| T-82-02-04 | Tampering | predecessor 5 workflows / harnesses unchanged | mitigate | 6th-coexistence copy only; acceptance asserts the 5 predecessors byte-unchanged; Plan 82-04 runs the hard byte-unchanged diff |
| T-82-02-SC | Tampering | npm/pip/cargo installs | accept | NO package installs in this plan; `markdown-link-check@3.14.2` is pre-existing/pinned in the inherited workflow, not newly added (RESEARCH §Security: Package Legitimacy Audit N/A) |

No HIGH-severity threats (tooling-only; no auth/crypto/input surface per RESEARCH Security Domain).
</threat_model>

<verification>
- `node scripts/validation/check-phase-NN.mjs` for NN in 75..82 → all exit 0 standalone.
- `node scripts/validation/check-phase-81.mjs` → 8/8 CROSSLINK pass.
- `node scripts/validation/check-phase-82.mjs --verbose` → CHAIN-48..81 green, exit 0.
- YAML check: 8 jobs check-phase-75..82, inherited invariants present, no stale v1.8 refs.
- `git show --stat HEAD` → exactly the 9 Atom-2 files in one commit; `git log origin/master --oneline -1` shows it pushed.
</verification>

<success_criteria>
- 8 net-new validators check-phase-75..82.mjs authored; all exit 0 standalone (SSOHARN-02).
- check-phase-82 apex: CHAIN_PHASES=[48..81], CHAIN_SKIP empty, VPP dropped, richer dual-invariant SELF.
- check-phase-81 carries all 8 V-81-CROSSLINK hard-asserts (D-01); 75-80 lightweight.
- audit-harness-v1.9-integrity.yml is the 6th coexistence workflow with 8 jobs, inherited invariants (SSOHARN-03).
- Atom 2 lands as ONE indivisible commit per ROADMAP SC#2 and is pushed to origin/master (D-03 ordering gate).
- The 5 predecessor workflows remain byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-02-SUMMARY.md` when done.
</output>
