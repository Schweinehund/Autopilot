---
phase: 82-harness-lineage-bump-terminal-re-audit-milestone-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-CONVENTIONS.md
  - scripts/validation/v1.9-milestone-audit.mjs
  - scripts/validation/v1.9-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
  - scripts/validation/_lib/frozen-at-close.mjs
autonomous: true
requirements:
  - SSOHARN-01
must_haves:
  truths:
    - "v1.9-milestone-audit.mjs runs 15/15 PASS and --self-test 9/9 (C1-C16 verbatim, NO C17) per SSOHARN-01"
    - "v1.9-milestone-audit.mjs loads v1.9-audit-allowlist.json (functional sidecar repoint, not v1.8)"
    - "_lib/frozen-at-close.mjs exports readAtV18Close and MILESTONE_CLOSE_SHAS.V18 === '2bd79d8' (single entry, no V18_CLOSEGATE) per D-04"
    - "regenerate-supervision-pins.mjs carries a BASELINE_13 audit-trail comment anchored to a real past SHA per SSOHARN-01"
    - "Atom 1 (audit harness + allowlist + BASELINE_13 + V18 entry) lands as ONE indivisible commit per ROADMAP SC#1"
  artifacts:
    - path: ".planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-CONVENTIONS.md"
      provides: "Freshness/SHA matrix + locked strings for the whole phase (10-validator set, V18=2bd79d8, BASELINE_13 anchor, 8 SSO-E needles)"
      min_lines: 40
    - path: "scripts/validation/v1.9-milestone-audit.mjs"
      provides: "7th milestone-audit in the lineage; verbatim Path-A copy of v1.8 (C1-C16, self-test 9/9)"
      contains: "v1.9-audit-allowlist.json"
    - path: "scripts/validation/v1.9-audit-allowlist.json"
      provides: "v1.9 sidecar allowlist; c13 carried forward, generated/phase relabeled"
      contains: "82-harness-lineage-bump-terminal-re-audit-milestone-close"
    - path: "scripts/validation/_lib/frozen-at-close.mjs"
      provides: "V18 frozen-close SHA registry entry + readAtV18Close export"
      contains: "readAtV18Close"
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "BASELINE_13 freshness audit-trail comment (closes BASELINE_12 v1.8 carry-over)"
      contains: "BASELINE_13"
  key_links:
    - from: "scripts/validation/v1.9-milestone-audit.mjs"
      to: "scripts/validation/v1.9-audit-allowlist.json"
      via: "readFile sidecar load at line ~79"
      pattern: "v1\\.9-audit-allowlist\\.json"
    - from: "scripts/validation/_lib/frozen-at-close.mjs"
      to: "readAtClose('V18')"
      via: "convenience export readAtV18Close"
      pattern: "readAtV18Close"
---

<objective>
Author the Phase-82 conventions reference (Wave 1, its own commit) and then ship Atom 1 (SSOHARN-01) as ONE indivisible commit: the v1.9 milestone-audit harness Path-A copy + its sidecar allowlist + the BASELINE_13 freshness comment + the V18 frozen-close entry in `_lib/frozen-at-close.mjs`.

Purpose: Bump the v1.8 audit-harness lineage to v1.9 (7th in the v1.4→…→v1.8→v1.9 chain) and pin the v1.8 close-gate SHA (V18 = `2bd79d8`) BEFORE any per-phase validator is authored (AP-5 prevention; satisfied structurally because Atom 1 commits before Atom 2 in Plan 82-02).

Output: `82-CONVENTIONS.md` (Wave 1 commit) + Atom 1 (Wave 2 indivisible commit) touching 4 files.

This is a HIGHLY MECHANICAL Path-A phase. There is no design freedom — only precise transcription of named analog files with the exact relabels documented in 82-RESEARCH.md §Q1/Q4/Q5/Q6 and 82-PATTERNS.md. All 6 RESEARCH divergences are pre-resolved in the task actions below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-CONTEXT.md
@.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-RESEARCH.md
@.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 82-CONVENTIONS.md (Wave 1 — its own commit)</name>
  <files>.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-CONVENTIONS.md</files>
  <read_first>
    - `.planning/milestones/v1.8-phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md` (Path-A source for the conventions doc shape — freshness/SHA matrix + locked-strings table)
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-CONVENTIONS.md` (the file you are creating)
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-RESEARCH.md` §Q1–Q10 (the verified constants to lock)
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-CONTEXT.md` D-01..D-04 + "Claude's Discretion" (which constants to lock)
  </read_first>
  <action>
    Create `82-CONVENTIONS.md` mirroring `74-CONVENTIONS.md`'s shape (freshness/SHA matrix + locked-strings table). Lock these phase-wide constants so 82-02/82-03/82-04 reference one source of truth:
    (1) The 10-validator cross-OS-applicable set: `v1.9-milestone-audit.mjs` (row 1, --self-test folded in), `check-phase-74.mjs` (row 2, prior-apex continuity), and `check-phase-75/76/77/78/79/80/81/82.mjs` (rows 3-10, net-new). Note exclusions with reasons: `pin-helper-advisory` (CI-only advisory), `rotting-external-quarterly` (cron-only SKIP = negative control), harness `--self-test` (folded into row 1), inherited chain 48..73 (covered transitively by apex).
    (2) V18 SHA = `2bd79d8` (full `2bd79d89440c6bd7b1f749dafcb29ed309e58295`; `docs(74-05)` v1.8 close-gate). Single entry per D-04 (no V18_CLOSEGATE).
    (3) BASELINE_13 anchor SHA = `3007960` (pre-Phase-82 HEAD / Phase-81 close baseline) — a REAL known-PAST SHA per DIVERGENCE-4, NOT the `{atom_1_sha}` placeholder precedent and NOT the future close SHA.
    (4) The 8 SSO-E edge needles for V-81-CROSSLINK (E1-E8), copied verbatim from 82-RESEARCH.md §Q3 table (edge → source file → forward-slash needle substring). State they are CRLF-normalized substring hard-asserts (no allowlist, mirrors C16 empty array).
    (5) The milestone-audit 4-line load-bearing edit set (lines 2, 4, 35, 79) and the explicit "do NOT bump lines 5/90" note per DIVERGENCE-1.
    (6) Pre-Phase-82 anchor SHA = `3007960` for the predecessor-byte-unchanged HARD gate at close.
    Also record the 5 commit / 4 plan layout (D-02) and the HARD ordering gate (Atom 2 pushed to origin/master before Plan 82-03 Axis-2 dispatch).
    Commit this file ALONE as the Wave-1 commit: `docs(82-01): 82-CONVENTIONS freshness/SHA matrix + locked phase strings`. This commit is SEPARATE from Atom 1 (Task 2-4 below).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-CONVENTIONS.md','utf8');const need=['2bd79d8','3007960','check-phase-82','../_glossary-macos.md#platform-sso','../l2-runbooks/27-macos-sso-investigation.md'];const miss=need.filter(n=>!c.includes(n));if(miss.length){console.error('MISSING:',miss);process.exit(1)}console.log('82-CONVENTIONS locked strings present')"</automated>
  </verify>
  <acceptance_criteria>
    - `82-CONVENTIONS.md` exists, ≥40 lines, mirrors `74-CONVENTIONS.md` structure.
    - Contains the 10-validator set (with the named exclusions + reasons), V18=`2bd79d8`, BASELINE_13 anchor=`3007960`, all 8 SSO-E needles (E1-E8 verbatim from §Q3), the 4-line milestone-audit edit set, the pre-Phase-82 anchor `3007960`, and the D-02 5-commit layout + ordering gate.
    - Committed ALONE (Wave-1 commit), not bundled with Atom 1.
  </acceptance_criteria>
  <done>82-CONVENTIONS.md authored with all locked constants and committed as its own Wave-1 commit.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-flight green-corpus check + author v1.9-milestone-audit.mjs and v1.9-audit-allowlist.json (Atom 1 part 1)</name>
  <files>scripts/validation/v1.9-milestone-audit.mjs, scripts/validation/v1.9-audit-allowlist.json</files>
  <read_first>
    - `scripts/validation/v1.8-milestone-audit.mjs` (Path-A source — 979 lines, C1-C16, self-test 9/9; the 4 load-bearing lines are 2, 4, 35, 79)
    - `scripts/validation/v1.8-audit-allowlist.json` (Path-A source — 531 lines; c13_broken_link_allowlist is exactly 6+9=15)
    - `scripts/validation/v1.9-milestone-audit.mjs` (the file you are creating)
    - `scripts/validation/v1.9-audit-allowlist.json` (the file you are creating)
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-RESEARCH.md` §Q1 (4-line edit set) + §Q4 (allowlist edits) + DIVERGENCE-1 + DIVERGENCE-3
  </read_first>
  <action>
    FIRST (Wave-0 green-corpus precondition per RESEARCH §Q4 / Assumption A2): run `node scripts/validation/v1.8-milestone-audit.mjs --verbose` at current HEAD and confirm 15/15 PASS. If it does NOT pass 15/15, STOP and surface the failing check — the v1.9 corpus must be green before the Path-A copy inherits it. Do not proceed to the copy until 15/15 confirmed.
    THEN copy `v1.8-milestone-audit.mjs` → `v1.9-milestone-audit.mjs` and change EXACTLY 4 lines (per §Q1; the v1.7→v1.8 diff proved these are the only load-bearing lines, 979→979 line count):
    - Line 2: `v1.8 … Path A copy of v1.7; lineage … → v1.8` → `v1.9 … Path A copy of v1.8; lineage … → v1.8 → v1.9; C1-C16 inherited verbatim`.
    - Line 4: sidecar comment `v1.8-audit-allowlist.json (v1.8 Path-A from v1.7 … per Phase 74 …)` → `v1.9-audit-allowlist.json (v1.9 Path-A from v1.8 with c13_rotting_external reset for v1.9 per Phase 82 close-state)`.
    - Line 35: `// Usage: node scripts/validation/v1.8-milestone-audit.mjs …` → `v1.9-milestone-audit.mjs`.
    - Line 79: `const raw = readFile('scripts/validation/v1.8-audit-allowlist.json');` → `v1.9-audit-allowlist.json` (FUNCTIONAL sidecar repoint — load-bearing).
    DO NOT touch line 5 (`Frozen-predecessor … v1.6 … Phase 66`) or line 90 (`Apple Business v1.6 docs`) — DIVERGENCE-1: never bumped in any prior hop; bumping them is a Path-A violation. DO NOT add a C17 / 16th check (D-01; checks array stays exactly 15 entries, ids 1-7,9-16, no C8). DO NOT edit the self-test block (lines 813-940; content-agnostic, carried verbatim → 9/9 preserved).
    THEN copy `v1.8-audit-allowlist.json` → `v1.9-audit-allowlist.json` and change ONLY (per §Q4 / DIVERGENCE-3): `generated` → a 2026-06 v1.9 timestamp; `phase` → `"82-harness-lineage-bump-terminal-re-audit-milestone-close"`; optionally `quarterly_audit.next_review` `2026-07-01` → `2026-10-01` (cosmetic, no validator asserts it). CARRY FORWARD `c13_rotting_external` verbatim (ci_1: 4 entries; ci_2_vpp_location_token: 10 entries; ci_3; quarterly_audit) — "c13 reset for v1.9" means label/timestamp refresh + carry-forward, NOT a content wipe. Keep `c13_broken_link_allowlist` at EXACTLY 6+9=15 entries (C13 hard-asserts this count). Keep `c16_missing_endpoint_exemptions: []` empty.
    DO NOT commit yet — Atom 1 commits as one unit after Task 3.
  </action>
  <verify>
    <automated>node scripts/validation/v1.9-milestone-audit.mjs --verbose && node scripts/validation/v1.9-milestone-audit.mjs --self-test && node -e "const c=require('fs').readFileSync('scripts/validation/v1.9-milestone-audit.mjs','utf8');if(!c.includes('v1.9-audit-allowlist.json'))process.exit(1);const a=JSON.parse(require('fs').readFileSync('scripts/validation/v1.9-audit-allowlist.json','utf8'));if(a.c13_broken_link_allowlist.length!==15){console.error('c13 count',a.c13_broken_link_allowlist.length);process.exit(1)}if(a.phase!=='82-harness-lineage-bump-terminal-re-audit-milestone-close'){console.error('phase',a.phase);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight: `node scripts/validation/v1.8-milestone-audit.mjs --verbose` reported 15/15 PASS at HEAD before the copy.
    - `node scripts/validation/v1.9-milestone-audit.mjs --verbose` exits 0 with 15/15 PASS.
    - `node scripts/validation/v1.9-milestone-audit.mjs --self-test` reports `9 passed, 0 failed`.
    - `git diff --no-index scripts/validation/v1.8-milestone-audit.mjs scripts/validation/v1.9-milestone-audit.mjs` shows exactly 4 changed lines (2, 4, 35, 79); line count 979→979; no C17/16th check; self-test block byte-identical.
    - `v1.9-audit-allowlist.json` has `phase` = `82-harness-lineage-bump-terminal-re-audit-milestone-close`, `c13_broken_link_allowlist` length 15, `c16_missing_endpoint_exemptions` empty, `c13_rotting_external` carried forward verbatim.
  </acceptance_criteria>
  <done>v1.9-milestone-audit.mjs (4-line relabel, 15/15, self-test 9/9) and v1.9-audit-allowlist.json (relabel + c13 carry-forward) authored; not yet committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add V18 frozen-close entry + readAtV18Close, and BASELINE_13 comment (Atom 1 part 2) — then commit Atom 1 indivisibly</name>
  <files>scripts/validation/_lib/frozen-at-close.mjs, scripts/validation/regenerate-supervision-pins.mjs</files>
  <read_first>
    - `scripts/validation/_lib/frozen-at-close.mjs` (the file you modify — `MILESTONE_CLOSE_SHAS` at lines 17-28, convenience exports at 50-54; confirmed NO V18 today; `readAtClose` at line 39)
    - `scripts/validation/regenerate-supervision-pins.mjs` (the file you modify — BASELINE_12 comment region lines 424-431; `const BASELINE_9` array at 432 is NOT touched)
    - `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-RESEARCH.md` §Q5 (V18 drop-in) + §Q6 (BASELINE_13 drop-in) + DIVERGENCE-4
  </read_first>
  <behavior>
    - After edit: `import('./scripts/validation/_lib/frozen-at-close.mjs')` exposes `readAtV18Close` as a function.
    - After edit: `MILESTONE_CLOSE_SHAS.V18 === '2bd79d8'` and there is NO `V18_CLOSEGATE` key (single entry per D-04).
    - After edit: `readAtV18Close('scripts/validation/v1.8-milestone-audit.mjs')` returns the v1.8 file content at SHA `2bd79d8` (CRLF-normalized via `git show`), non-null.
    - After edit: `regenerate-supervision-pins.mjs` contains a `BASELINE_13` comment anchored to `3007960`; `const BASELINE_9` array is byte-unchanged.
  </behavior>
  <action>
    In `_lib/frozen-at-close.mjs`: append ONE entry to `MILESTONE_CLOSE_SHAS` after `V17_CLOSEGATE` — `V18: '2bd79d8',` with a comment noting `Phase 74 Plan 74-05 — v1.8 milestone close-gate (docs(74-05); single entry per D-04, v1.8 closed in ONE commit so no V18_CLOSEGATE)`. Append ONE convenience export after `readAtV17CloseGate` — `export const readAtV18Close = (p) => readAtClose('V18', p);`. SINGLE V18 entry only (D-04 / Pitfall 4) — do NOT add a second `V18_CLOSEGATE`. The 7-char short SHA `2bd79d8` matches the V15/V16/V17 abbreviation style.
    In `regenerate-supervision-pins.mjs`: append a BASELINE_13 audit-trail comment block AFTER the BASELINE_12 region (after line 431), mirroring the BASELINE_12 prose with relabels: `BASELINE_12`→`BASELINE_13`, `Phase 74 Plan 74-02`→`Phase 82 Plan 82-01`, `closes BASELINE_11 v1.7`→`closes BASELINE_12 v1.8`, `HARNESS-09`→`SSOHARN-01`, `Phase 74 SC#1`→`Phase 82 SC#1`, and anchor `against HEAD 3007960 (Phase 81 close baseline + Phase 82 chain green)` — use the REAL past SHA `3007960` per DIVERGENCE-4, NOT the `{atom_1_sha}` placeholder precedent. Note resolution path `BASELINE_14 will refresh at the next milestone close`. Do NOT touch the `const BASELINE_9` array (comment-only change; v1.9 ships no Android-glossary line shifts).
    THEN COMMIT ATOM 1 AS ONE INDIVISIBLE COMMIT (ROADMAP SC#1) — stage ALL FOUR Atom-1 files together: `scripts/validation/v1.9-milestone-audit.mjs`, `scripts/validation/v1.9-audit-allowlist.json`, `scripts/validation/_lib/frozen-at-close.mjs`, `scripts/validation/regenerate-supervision-pins.mjs`. Commit message: `feat(82-01): v1.9 harness-core Path-A — SSOHARN-01 + V18 pin (atomic SC#1 Atom 1)`. NEVER split Atom 1 across commits. The `82-CONVENTIONS.md` from Task 1 is already its own separate commit and is NOT part of this commit.
  </action>
  <verify>
    <automated>node -e "import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>{if(typeof m.readAtV18Close!=='function'){console.error('no readAtV18Close');process.exit(1)}if(m.MILESTONE_CLOSE_SHAS.V18!=='2bd79d8'){console.error('V18',m.MILESTONE_CLOSE_SHAS.V18);process.exit(1)}if('V18_CLOSEGATE'in m.MILESTONE_CLOSE_SHAS){console.error('unexpected V18_CLOSEGATE');process.exit(1)}const r=m.readAtV18Close('scripts/validation/v1.8-milestone-audit.mjs');if(!r){console.error('readAtV18Close returned null');process.exit(1)}console.log('V18 OK')})" && grep -v '^#' scripts/validation/regenerate-supervision-pins.mjs | grep -c 'BASELINE_13'</automated>
  </verify>
  <acceptance_criteria>
    - `import('./scripts/validation/_lib/frozen-at-close.mjs')` exposes `readAtV18Close` (function) and `MILESTONE_CLOSE_SHAS.V18 === '2bd79d8'`; NO `V18_CLOSEGATE` key present.
    - `readAtV18Close('scripts/validation/v1.8-milestone-audit.mjs')` returns non-null content (git show at 2bd79d8 succeeds).
    - `regenerate-supervision-pins.mjs` contains a `BASELINE_13` comment anchored to `3007960`; the `const BASELINE_9` array is byte-unchanged vs HEAD.
    - Atom 1 is ONE commit (`feat(82-01): … atomic SC#1 Atom 1`) containing exactly the 4 Atom-1 files; `82-CONVENTIONS.md` is in a separate earlier commit; `git show --stat HEAD` lists all 4 Atom-1 files and no others.
  </acceptance_criteria>
  <done>V18 entry + readAtV18Close added (single entry), BASELINE_13 comment added (real SHA anchor), and Atom 1 committed as one indivisible commit per SC#1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a tooling-only phase (Node ESM validators + a sidecar JSON + audit-trail comments). No runtime services, no network surface, no user input. The relevant boundary is harness-integrity / supply-chain: a validator that silently passes when it should fail, or a frozen-read that resolves the wrong SHA.

| Boundary | Description |
|----------|-------------|
| Path-A copy → green corpus | A mis-transcribed harness could report a false 15/15 (silent pass) |
| `git show <SHA>:<path>` frozen read | Wrong/typo'd V18 SHA would read the wrong historical state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01-01 | Tampering | v1.9-milestone-audit.mjs (Path-A copy could silently weaken a check) | mitigate | Verify exactly 4 lines changed (git diff --no-index vs v1.8); --self-test 9/9 + 15/15 PASS gate; no C17 added |
| T-82-01-02 | Spoofing | V18 frozen-close SHA (wrong SHA reads wrong milestone state) | mitigate | V18 pinned to verified known-past `2bd79d8`; verify task calls readAtV18Close and asserts non-null git-show result |
| T-82-01-03 | Information Disclosure | execFileSync('git', ['show', …]) in frozen reader | accept | arg-array form (no shell interpolation); inherited RETRO-02 hardened helper; no new code path |
| T-82-01-SC | Tampering | npm/pip/cargo installs | accept | NO package installs in this plan (RESEARCH §Security: Package Legitimacy Audit N/A; markdown-link-check@3.14.2 is pre-existing in inherited CI, not installed here) |

No HIGH-severity threats (tooling-only; no auth/crypto/input surface per RESEARCH Security Domain).
</threat_model>

<verification>
- `node scripts/validation/v1.9-milestone-audit.mjs --verbose` → 15/15 PASS, exit 0.
- `node scripts/validation/v1.9-milestone-audit.mjs --self-test` → 9 passed, 0 failed.
- `git diff --no-index scripts/validation/v1.8-milestone-audit.mjs scripts/validation/v1.9-milestone-audit.mjs` → exactly 4 changed lines (2, 4, 35, 79).
- `import('./scripts/validation/_lib/frozen-at-close.mjs')` → readAtV18Close is a function, MILESTONE_CLOSE_SHAS.V18 === '2bd79d8', no V18_CLOSEGATE.
- `git log --oneline -2` → two commits: the `82-CONVENTIONS` Wave-1 commit, then the `feat(82-01): … Atom 1` indivisible commit with exactly 4 files.
</verification>

<success_criteria>
- 82-CONVENTIONS.md authored with all phase-wide locked constants (committed Wave 1, alone).
- v1.9-milestone-audit.mjs is a verbatim Path-A copy (4-line relabel, 15/15, self-test 9/9, NO C17).
- v1.9-audit-allowlist.json relabeled (generated/phase) with c13 carried forward and exactly 15 broken-link entries.
- _lib/frozen-at-close.mjs has the single V18 entry (`2bd79d8`) + readAtV18Close export.
- regenerate-supervision-pins.mjs has the BASELINE_13 comment anchored to real SHA 3007960.
- Atom 1 lands as ONE indivisible commit per ROADMAP SC#1 (4 files); 82-CONVENTIONS is a separate commit.
</success_criteria>

<output>
Create `.planning/phases/82-harness-lineage-bump-terminal-re-audit-milestone-close/82-01-SUMMARY.md` when done.
</output>
