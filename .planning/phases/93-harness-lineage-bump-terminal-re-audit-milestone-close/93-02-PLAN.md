---
phase: 93-harness-lineage-bump-terminal-re-audit-milestone-close
plan: 02
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - scripts/validation/check-phase-89.mjs
  - scripts/validation/check-phase-90.mjs
  - scripts/validation/check-phase-91.mjs
  - scripts/validation/check-phase-92.mjs
  - scripts/validation/check-phase-93.mjs
  - scripts/validation/_lib/frozen-at-close.mjs
  - .github/workflows/audit-harness-v1.11-integrity.yml
autonomous: true
requirements: [HARN-02]
must_haves:
  truths:
    - "check-phase-89/90/91 are lightweight (V-NN-PRESENCE + V-NN-SELF only) → 2 PASS / 0 FAIL / 0 SKIP each; PRESENCE targets are the 3 content deliverables"
    - "check-phase-92 hard-asserts V-92-CROSSLINK-E1..E8 (8 nav-edge needles) + V-92-SELF → 9 PASS / 0 FAIL / 0 SKIP"
    - "check-phase-93 is chain-apex: CHAIN_PHASES covers 48..92 (45 entries), CHAIN_SKIP = new Set([]), V-93-SELF asserts 93 absent; HARNESS repointed to v1.11"
    - "_lib/frozen-at-close.mjs gains V110:'a3617e9' entry + readAtV110Close export (single entry, no V110_CLOSEGATE)"
    - "audit-harness-v1.11-integrity.yml ships as the 8th coexistence workflow with check-phase-89..93 jobs; predecessors v1.4-v1.10 byte-unchanged"
    - "Atom 2 lands as ONE indivisible commit touching exactly 7 files; V110 rides Atom 2 (locked divergence)"
    - "Atom 2 is committed AND pushed to origin/master (hard ordering gate for Plan 93-03)"
  artifacts:
    - path: "scripts/validation/check-phase-93.mjs"
      provides: "chain-apex validator CHAIN_PHASES=[48..92]"
      contains: "CHAIN_SKIP = new Set([])"
    - path: "scripts/validation/check-phase-92.mjs"
      provides: "V-92-CROSSLINK-E1..E8 nav-edge assertions"
      contains: "CROSSLINK"
    - path: "scripts/validation/_lib/frozen-at-close.mjs"
      provides: "V110 frozen-close pin + readAtV110Close"
      contains: "readAtV110Close"
    - path: ".github/workflows/audit-harness-v1.11-integrity.yml"
      provides: "8th CI coexistence workflow"
      contains: "check-phase-93"
  key_links:
    - from: ".github/workflows/audit-harness-v1.11-integrity.yml"
      to: "scripts/validation/check-phase-93.mjs"
      via: "linux-chain-ubuntu-latest job runs the apex"
      pattern: "check-phase-93"
    - from: "scripts/validation/_lib/frozen-at-close.mjs"
      to: "MILESTONE_CLOSE_SHAS.V110"
      via: "readAtClose('V110', p)"
      pattern: "V110.*a3617e9"
---

<objective>
Ship Plan 93-02: HARN-02 Atom 2 — the v1.11 per-phase validators + V110 frozen-close pin + 8th CI coexistence workflow. ONE indivisible commit, exactly 7 files (COMMIT 3).

Files (all Path-A copies / minimal appends per locked D-01/D-02/D-04):
- `check-phase-89/90/91.mjs` — lightweight (V-NN-PRESENCE + V-NN-SELF) from `check-phase-87.mjs`.
- `check-phase-92.mjs` — V-92-CROSSLINK-E1..E8 (FORM from `check-phase-81.mjs`) + V-92-SELF.
- `check-phase-93.mjs` — chain-apex from `check-phase-88.mjs`, range bumped [48..87] → [48..92], CHAIN_SKIP empty Set, V-93-SELF, HARNESS → v1.11.
- `_lib/frozen-at-close.mjs` (EDIT) — add `V110: 'a3617e9'` entry + `readAtV110Close` export. **LOCKED DIVERGENCE: V110 rides ATOM 2 here, NOT Atom 1** (per ROADMAP SC#2 + HARN-02).
- `audit-harness-v1.11-integrity.yml` — 8th coexistence workflow from `audit-harness-v1.10-integrity.yml`, jobs repointed to check-phase-89..93.

Purpose: Land the complete v1.11 validator surface + frozen pin + CI as one revertable atom. **Ordering-safe:** `a3617e9` is a known-PAST SHA and check-phase-93 reads only PRIOR-milestone closes → no Commit A.
Output: 5 net-new validators + frozen-at-close edit + CI workflow; ONE commit; pushed to origin/master.

**HARD ordering gate (D-03):** This atom MUST be committed AND pushed to `origin/master` before Plan 93-03 runs — the CI workflow's check-phase-89..93 jobs FAIL (not skip) if absent from the dispatched ref.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-CONVENTIONS.md
@.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-CONTEXT.md
@.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-RESEARCH.md
@.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: check-phase-89/90/91 (lightweight) + check-phase-92 (V-92-CROSSLINK-E1..E8)</name>
  <files>scripts/validation/check-phase-89.mjs, scripts/validation/check-phase-90.mjs, scripts/validation/check-phase-91.mjs, scripts/validation/check-phase-92.mjs</files>
  <read_first>
    - scripts/validation/check-phase-87.mjs (PATH-A SOURCE for 89/90/91 — 98 lines, canonical lightweight shape: CHAIN_PHASES=[], CHAIN_SKIP=new Set([]), DELIVERABLE, V-NN-PRESENCE, V-NN-SELF, readFile CRLF-normalize, runner loop)
    - scripts/validation/check-phase-81.mjs (PATH-A SOURCE FORM for check-phase-92 — {id,file,needle} SSO_EDGES array, CRLF-normalized read, forward-slash includes() substring, NO allowlist/sidecar, 8 edges + 1 SELF)
    - .planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-CONVENTIONS.md (the 3 PRESENCE targets + the 8 V-92-CROSSLINK needles — locked in Plan 93-01)
    - .planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-PATTERNS.md (lightweight pattern + cross-link FORM + needle table)
    - .planning/phases/92-navigation-hub-integration/92-VERIFICATION.md (L52-63 Key Link table — confirms the 8 needles are live; harness currently BLIND to them)
  </read_first>
  <action>
    Create the 4 per-phase content/cross-link validators.

    **check-phase-89/90/91.mjs (lightweight, copy from check-phase-87.mjs):** swap `87`→target throughout (id strings, V-NN-PRESENCE / V-NN-SELF names, SELF guard `.includes(NN)`, header). Set `CHAIN_PHASES = []` and `CHAIN_SKIP = new Set([])` (chain lives only in apex 93). Set `DELIVERABLE`:
    - check-phase-89 → `docs/macos-lifecycle/01-psso-provisioning-walkthrough.md`
    - check-phase-90 → `docs/l2-runbooks/30-macos-mdm-migration-failure.md`
    - check-phase-91 → `docs/_glossary-macos.md`
    Header relabel: cite "LIGHTWEIGHT per Phase 93 D-01" (re-applies Phase 82 D-01). **check-phase-91 explicit DEFER:** add a header comment noting it does NOT re-assert V-63-08/09 — `check-phase-63` owns the BASELINE blob-hash pins (updated by Phase 91 commit `7039630`, replayed every apex run); re-asserting would create a two-place-update hazard (D-01). Each file = exactly 2 checks (PRESENCE + SELF) → 2 PASS / 0 FAIL / 0 SKIP.

    **check-phase-92.mjs (cross-link FORM from check-phase-81.mjs + SELF from check-phase-87.mjs):** rename the `{id,file,needle}` array → nav-edge set; IDs `V-81-CROSSLINK-*` → `V-92-CROSSLINK-E1..E8`; SELF guard `.includes(81)` → `.includes(92)`; `CHAIN_PHASES = []`, `CHAIN_SKIP = new Set([])`. Populate the 8 rows from 93-CONVENTIONS.md (E1 index→01-psso; E2 index→02-mdm-migration; E3 index→30-runbook; E4 common-issues→30-runbook; E5 common-issues→27-sso; E6 quick-ref-l2→30-runbook; E7 quick-ref-l2→`#platform-sso-attestation-command`; E8 06-macos-triage→`../l2-runbooks/30-macos-mdm-migration-failure.md`). Keep substring `includes()` (forward-slash, CRLF-normalized read) — NO allowlist/sidecar (mirrors C16 empty exemptions). Total = 9 checks (8 CROSSLINK + 1 SELF) → 9 PASS / 0 FAIL / 0 SKIP. Copy the shared readFile CRLF-normalizer and runner loop verbatim.
  </action>
  <verify>
    <automated>for n in 89 90 91 92; do echo "=== check-phase-$n ==="; node scripts/validation/check-phase-$n.mjs | tail -1; done</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-89.mjs` / `90` / `91` each exit 0 with `Result: 2 PASS, 0 FAIL, 0 SKIPPED`.
    - `node scripts/validation/check-phase-92.mjs` exits 0 with `Result: 9 PASS, 0 FAIL, 0 SKIPPED`.
    - check-phase-92 contains all 8 needles: `grep -c "01-psso-provisioning-walkthrough.md\|02-mdm-migration-psso.md\|30-macos-mdm-migration-failure.md\|27-macos-sso-investigation.md\|platform-sso-attestation-command" scripts/validation/check-phase-92.mjs` shows all five distinct needle strings present (E1-E8 collectively).
    - check-phase-92 contains `V-92-CROSSLINK-E1` … `V-92-CROSSLINK-E8` ids and a `V-92-SELF` with `.includes(92)`.
    - check-phase-91 header contains a DEFER note referencing check-phase-63 (no V-63-08/09 re-assertion: `grep -c "V-63-08\|BASELINE" scripts/validation/check-phase-91.mjs` == 0 for assertion code).
    - Each of 89/90/91 contains `CHAIN_PHASES = []` and `CHAIN_SKIP = new Set([])`.
  </acceptance_criteria>
  <done>All 4 per-phase validators created; 89/90/91 report 2/0/0, 92 reports 9/0/0; 8 needles asserted; check-phase-91 defers to check-phase-63.</done>
</task>

<task type="auto">
  <name>Task 2: check-phase-93 apex + frozen-at-close V110 + 8th CI workflow; commit Atom 2 (7 files) and push</name>
  <files>scripts/validation/check-phase-93.mjs, scripts/validation/_lib/frozen-at-close.mjs, .github/workflows/audit-harness-v1.11-integrity.yml</files>
  <read_first>
    - scripts/validation/check-phase-88.mjs (PATH-A SOURCE for check-phase-93.mjs — 174 lines, chain-apex: HARNESS const :38, CHAIN_PHASES [48..87] :41, CHAIN_SKIP :44, V-NN-AUDIT :48-58, NESTED child-spawn :71-106, V-NN-AUDIT-HARNESS :108-128, V-NN-SELF :130-144, runner :146-173)
    - scripts/validation/_lib/frozen-at-close.mjs (the file being MODIFIED — V19 entry :28-30 + readAtV19Close export :62 are the mirror templates; last entry is currently V19, no V110)
    - .github/workflows/audit-harness-v1.10-integrity.yml (PATH-A SOURCE for the 8th workflow — 229 lines; job structure + the 4 linux-chain contract values fetch-depth:0 / core.autocrlf false / continue-on-error:false / timeout-minutes:30)
    - .planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-PATTERNS.md (apex constants + NESTED block + frozen-at-close append + workflow relabel table)
    - .planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-CONVENTIONS.md (V110=a3617e9, CHAIN [48..92], lineage label, 8th-coexistence string)
  </read_first>
  <action>
    Create the apex validator + frozen pin + CI workflow, then commit all 7 Atom-2 files together and push.

    **check-phase-93.mjs (copy from check-phase-88.mjs):** all `88`→`93` in id/name/header/SELF strings. `HARNESS = 'scripts/validation/v1.11-milestone-audit.mjs'`. `CHAIN_PHASES` = explicit 45-entry integer array `[48,49,…,92]` (every integer 48 through 92 inclusive). `CHAIN_SKIP = new Set([])` (verbatim, Phase 68 `7b635ca` empty-Set invariant). V-NN-AUDIT → target `93-…/93-VERIFICATION.md` with archive-list `['v1.11-phases']`, regex `/Phase 93/i` (SKIP-PASS until close-gate authors 93-VERIFICATION.md). Copy the NESTED child-spawn block (`CHECK_PHASE_NESTED=1`, isPeer `phaseNum>=67` → 600s, child env, execFileSync) VERBATIM. V-93-SELF dual-invariant: `if (CHAIN_PHASES.includes(93))` FAIL + `if (CHAIN_SKIP.size !== 0)` FAIL. Runner loop verbatim (LABEL_WIDTH=60, `Result: N PASS, N FAIL, N SKIPPED`, `process.exit(failed>0?1:0)`).

    **_lib/frozen-at-close.mjs (EDIT — 2 appends, NO second entry):** After the V19 entry (:30), inside `MILESTONE_CLOSE_SHAS`, add `V110: 'a3617e9',` with a comment mirroring the V19 comment shape (Phase 88 Plan 88-04 — v1.10 milestone close-gate; single entry; no separate V110_CLOSEGATE). After the `readAtV19Close` export (:62), add `export const readAtV110Close = (p) => readAtClose('V110', p);`. NO `V110_CLOSEGATE` (v1.10 closed in ONE commit → atom == close-gate). This is the LOCKED DIVERGENCE — V110 rides Atom 2.

    **audit-harness-v1.11-integrity.yml (copy from audit-harness-v1.10-integrity.yml):** all `v1.10`→`v1.11` repoints. Header "7th coexistence" → "8th"; `name:` → `Audit Harness v1.11 Integrity`. `on.pull_request.paths` repoint v1.10-* → v1.11-* (harness, allowlist, MILESTONE-AUDIT, DEFERRED-CLEANUP). Crons verbatim. `parse` job → validate `v1.11-audit-allowlist.json`. `path-match` job → grep `v1.11-audit-allowlist.json` in `v1.11-milestone-audit.mjs`. `harness-run` → `node scripts/validation/v1.11-milestone-audit.mjs --verbose`. `linux-chain-ubuntu-latest` → run apex `check-phase-93.mjs`, KEEP all 4 contract values (fetch-depth:0, core.autocrlf false, continue-on-error:false, timeout-minutes:30) + CHAIN_TIMING_LINUX `::notice`. Per-validator jobs: replace 83-88 jobs with check-phase-89/90/91/92/93 (fetch-depth:0, timeout-minutes:15, continue-on-error:false). `rotting-external-quarterly` → carry (cron-only `if:` guard SKIPs on workflow_dispatch — D-03 negative control); repoint sidecar to v1.11. `pin-helper-advisory` → carry verbatim (continue-on-error:true).

    Commit all 7 files as ONE atom with the locked message, then `git push origin master`. Verify the push reached origin (hard ordering gate for 93-03).
  </action>
  <verify>
    <automated>node scripts/validation/check-phase-93.mjs | tail -1 && node -e "const f=require('fs').readFileSync('scripts/validation/_lib/frozen-at-close.mjs','utf8'); if(!f.includes(\"V110:\")||!f.includes(\"a3617e9\")||!f.includes('readAtV110Close')){console.error('frozen V110 missing');process.exit(1)} console.log('frozen V110 ok')" && node -e "const y=require('fs').readFileSync('.github/workflows/audit-harness-v1.11-integrity.yml','utf8'); ['check-phase-93','fetch-depth: 0','continue-on-error: false','timeout-minutes: 30','Audit Harness v1.11'].forEach(s=>{if(!y.includes(s)){console.error('workflow missing '+s);process.exit(1)}}); console.log('workflow ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-93.mjs` runs the full chain (exits 0; warm-tree Windows may show deep-nest noise — that is expected, the authoritative count comes from Linux GHA in Plan 93-03). check-phase-93 contains `CHAIN_PHASES` covering every integer 48..92 (45 entries) and `CHAIN_SKIP = new Set([])`.
    - check-phase-93 `V-93-SELF` asserts `CHAIN_PHASES.includes(93)` is false; HARNESS const = `scripts/validation/v1.11-milestone-audit.mjs`.
    - `_lib/frozen-at-close.mjs` contains `V110:`, `'a3617e9'`, and `readAtV110Close`; contains NO `V110_CLOSEGATE`.
    - `audit-harness-v1.11-integrity.yml` contains `check-phase-93`, `Audit Harness v1.11`, `fetch-depth: 0`, `continue-on-error: false`, `timeout-minutes: 30`; jobs reference check-phase-89/90/91/92/93.
    - Predecessor byte-unchanged so far: `git diff HEAD~1 HEAD -- .github/workflows/audit-harness-v1.10-integrity.yml scripts/validation/v1.10-milestone-audit.mjs scripts/validation/v1.10-audit-allowlist.json` is EMPTY (no v1.10 surface modified by Atom 2).
    - COMMIT 3 message is exactly: `feat(93-02): v1.11 validators + V110 pin + CI surface — HARN-02 (atomic SC#1 Atom 2)`
    - COMMIT 3 touches EXACTLY 7 files: the 5 check-phase-89..93.mjs + `_lib/frozen-at-close.mjs` + `audit-harness-v1.11-integrity.yml`.
    - Atom 2 is pushed: `git log origin/master --oneline -1` shows the Atom-2 commit (HARD ordering gate for Plan 93-03 satisfied).
  </acceptance_criteria>
  <done>Atom 2 (7 files) committed as COMMIT 3 and pushed to origin/master; apex CHAIN=[48..92]; V110 pin present; 8th workflow in place; v1.10 surfaces byte-unchanged; ordering gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → GitHub Actions runner | The only boundary: `audit-harness-v1.11-integrity.yml` runs in GitHub-hosted Actions. It executes in-repo `.mjs` validators against a pinned ref; no external input, no secrets, no network calls beyond the checkout. |

## STRIDE Threat Register

ASVS L1 review found **no high-severity items**. The integrity controls are predecessor-byte-unchanged + `continue-on-error: false`.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-02-T | Tampering | A v1.4-v1.10 frozen surface mutated while authoring the 8th workflow | mitigate | Acceptance gate asserts `git diff` of the v1.10 workflow/harness/sidecar is EMPTY; full 23-surface gate runs at close-gate (93-04). |
| T-93-02-E | Elevation/Integrity | CI workflow with `continue-on-error: true` masking a real validator failure | mitigate | linux-chain + per-validator jobs keep `continue-on-error: false` (verified in acceptance); only `pin-helper-advisory` is advisory (true) by design. |
| T-93-02-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | NO package installs (Path-A copies of in-tree scripts; workflow uses `actions/checkout` + node only, no new npm deps). No Package Legitimacy Gate required. |
</threat_model>

<verification>
- check-phase-89/90/91 → 2/0/0; check-phase-92 → 9/0/0 (verified in Task 1).
- check-phase-93 CHAIN_PHASES=[48..92] + CHAIN_SKIP empty + HARNESS→v1.11.
- frozen-at-close: V110='a3617e9' + readAtV110Close, single entry.
- 8th workflow: check-phase-89..93 jobs + 4 contract values intact; v1.10 surfaces byte-unchanged.
- Atom 2 = exactly 7 files, one commit, pushed to origin/master.
</verification>

<success_criteria>
- ROADMAP SC#2 satisfied: Atom 2 ships as ONE indivisible commit — check-phase-89..93 + frozen-at-close V110 (`a3617e9`) + audit-harness-v1.11-integrity.yml (8th); predecessors v1.4-v1.10 byte-unchanged.
- V110 rides Atom 2 (locked divergence); no Commit A; ordering-safe.
- Atom 2 pushed to origin/master — hard precondition for Plan 93-03 met.
</success_criteria>

<output>
Create `.planning/phases/93-harness-lineage-bump-terminal-re-audit-milestone-close/93-02-SUMMARY.md` when done. Record: COMMIT-3 (Atom-2) SHA, origin/master push confirmation, per-validator local counts (89/90/91=2/0/0, 92=9/0/0), and a note that apex-93 count is deferred to Linux GHA (Plan 93-03, sole-authoritative).
</output>
