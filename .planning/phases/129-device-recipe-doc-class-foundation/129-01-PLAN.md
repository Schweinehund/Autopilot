---
phase: 129-device-recipe-doc-class-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_standards/EEE-SOP-standard.md
  - .planning/research/ARCHITECTURE.md
autonomous: true
requirements: [CLASS-01]
must_haves:
  truths:
    - "EEE-SOP-standard.md carries a D-02 edge-case ruling bullet classifying docs/recipes/* as doc_type: Guide, referencing STD-05 (D-09)"
    - "EEE-SOP-standard.md carries a new top-level '## Admin Decision-Point Block Format (STD-05)' section with the full 3-case composite spec, mandatory-blank-line rule, case-boundary rule, 3-rule branch floor, RECOMMENDED PSSO idiom, ONE fenced worked branching example, and the fenced-sample carve-out sentence (D-01..D-05, D-11)"
    - "STD-05 states the normative rule that every recipe Summary OPENS with a one-line concrete end-state statement, phrased 'REQUIRED; enforced by registry review, not by the harness' (D-13)"
    - "Version-History table gains one 2026-07-17 row naming both STD-05 and the D-02 ruling row; frontmatter last_verified stays 2026-07-04 (D-12)"
    - "C17 stays green: --self-test 4/4 PASS and --verbose 229 files / 0 violations (standard remains enrolled and compliant; no C17 script edit per D-10)"
    - "ARCHITECTURE.md no longer claims 'every doc-class directory has an 00-* index'; the recipes/ structure tree shows only the two recipe files, no 00-overview.md line (D-14, D-15)"
  artifacts:
    - path: "docs/_standards/EEE-SOP-standard.md"
      provides: "STD-05 decision-point block spec + D-02 recipe ruling + Version-History row"
      contains: "## Admin Decision-Point Block Format (STD-05)"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Corrected recipes/ structure tree (no false 00-index universality claim)"
  key_links:
    - from: "docs/_standards/EEE-SOP-standard.md (D-02 ruling bullet)"
      to: "STD-05 section"
      via: "bullet references STD-05 by name for the full decision-point spec"
      pattern: "docs/recipes/.*Guide.*STD-05"
    - from: "STD-05 fenced worked example"
      to: "D-01 composite shape"
      via: "> **Ask the admin:** lead-in + blank line + 4-column branching table inside one fenced block"
      pattern: "Ask the admin"
---

<objective>
Define the Device Recipe doc class in the governance standard: add the slim D-02 edge-case ruling row (`docs/recipes/*` → `doc_type: Guide`), author the full top-level STD-05 decision-point block spec, add the Version-History row, and correct the one false claim in `.planning/research/ARCHITECTURE.md:61`. This is the contract Plan 02's template instantiates and Phases 130/131 author against.

Purpose: Lock the doc-class ruling and the admin decision-point block format ONCE, in the standard, so both recipes inherit a consistent, C17-safe decision-point convention without retrofitting (FOUNDATION-FIRST; RESEARCH Pitfalls #3/#4). Deciding here is cheaper than fixing two content files later.
Output: Edited `docs/_standards/EEE-SOP-standard.md` (3 additive edits) and a one-line-family correction to `.planning/research/ARCHITECTURE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-device-recipe-doc-class-foundation/129-CONTEXT.md
@.planning/phases/129-device-recipe-doc-class-foundation/129-RESEARCH.md
@.planning/phases/129-device-recipe-doc-class-foundation/129-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add D-02 recipe ruling + STD-05 decision-point spec + Version-History row to EEE-SOP-standard.md</name>
  <files>docs/_standards/EEE-SOP-standard.md</files>
  <read_first>
    - docs/_standards/EEE-SOP-standard.md — READ the whole file, but the load-bearing ranges are: lines 134–167 (the `### D-02 Edge-case rulings` bullet list; the last "Lifecycle documents" bullet ends at line 156), lines 398–452 (the STD-04 section and the `---` divider at 450 + `## C17 Enforcement Reference` header at 452 — the insertion point), lines 82–84 (D-05 visible-header house style), lines 429–434 (STD-04 D-03's fenced-sample-in-prose house style that D-11 mirrors), lines 510–516 (Version-History table; STD-04 row at 515, file ends 516), frontmatter line 7 (last_verified: 2026-07-04 — MUST stay unchanged).
    - .planning/phases/129-device-recipe-doc-class-foundation/129-CONTEXT.md — decisions D-01..D-05 (the 3-case composite spec + branch floor), D-09 (placement: STD-05 section + slim D-02 row), D-11 (STD-05 content split + fenced example + carve-out sentence), D-12 (Version-History, no last_verified bump), D-13 (normative Summary end-state rule).
    - .planning/phases/129-device-recipe-doc-class-foundation/129-PATTERNS.md — "EEE-SOP-standard.md" section: the exact sibling-bullet shape, the divider+header insertion pattern, the fenced worked-example block, and the exact Version-History row wording to model.
    - docs/macos-lifecycle/01-psso-provisioning-walkthrough.md (lines ~60–100) — the RECOMMENDED branch idiom source (decision lead-in + path table + sibling branch sections, non-converging terminals at ~line 65) that STD-05's RECOMMENDED-idiom description cites.
    - docs/admin-setup-apv1/04-dynamic-groups.md (lines ~50–65) — live blank-line-chunked blockquote idiom proving D-02's mandatory-blank-line rule is already production-safe.
  </read_first>
  <action>
    Make THREE additive edits to docs/_standards/EEE-SOP-standard.md. Add only; never edit or delete existing STD-04 header text or the `#### Non-MECE precedence rule (D-08)` subsection (check-phase-120.mjs substring-asserts on those — RESEARCH Frozen-Surface Check).

    EDIT 1 — D-02 ruling bullet. Insert a new 8th bullet as the last item of the `### D-02 Edge-case rulings` list (immediately after the "Lifecycle documents" bullet ending at line 156, before the blank line at 157). Match the sibling bullet Markdown shape exactly (bold lead phrase + backtick path glob + `→` + backtick `Guide` + citation + one-sentence rationale). Content: `**Device Recipe documents** (\`docs/recipes/*\`) → \`Guide\` (v1.18 STD-05).` followed by a one-sentence directory-precedence rationale stating recipes are end-to-end procedural provisioning walkthroughs and pointing to STD-05 for the full admin decision-point block spec. KEEP IT SLIM — one bullet, one sentence of rationale (D-09); the multi-paragraph spec lives in STD-05, NOT here.

    EDIT 2 — new top-level STD-05 section. Insert between the `---` divider at line 450 and the `## C17 Enforcement Reference` header at line 452, bracketed by `---` dividers exactly as STD-04 is (copy the `\n---\n\n## Title\n\n[content]\n\n---\n\n` surrounding pattern). Title: `## Admin Decision-Point Block Format (STD-05)` (descriptive-phrase-then-parenthetical-tag, matching STD-04's `## Mermaid-in-Enrolled-Classes Policy (STD-04)` convention). Use `### D-NN: Title` internal subsections like STD-04. STD-05 MUST carry, in prose:
      - The 3-case composite block rule (D-01): every decision point opens with a ONE-SENTENCE `> **Ask the admin:**` blockquote lead-in carrying the prompt ONLY (options/consequences NEVER in the blockquote — a faithful options+consequence blockquote measures ~228 chars and hard-fails C17 #12's 200-char contiguous-top-level-blockquote cap). Case 1 branching decision: lead-in + `| Option | When to choose | Consequence if wrong | Branch |` table with Branch cells linking to branch sections. Case 2 enumerable-value decision: lead-in + `| Option | When to choose | Recorded as |` table. Case 3 free-value prompt: lead-in alone, NO table.
      - The MANDATORY blank line rule (D-02): a blank line between the lead-in blockquote and any following table is required, or GFM lazy continuation absorbs the pipe row into the blockquote (destroys the table and inflates the #12 char count). State this explicitly.
      - The case-boundary rule (D-03): when a table is used vs. not; frame the composite as a documented combination of already-shipped corpus Patterns 2+3 (decision table + short blockquote callout), not a novel construct; state that decision content is NEVER placed in a code fence in live recipes.
      - The 3-rule NORMATIVE branch floor (D-04), exactly three rules: (1) decision blocks use the D-01 composite shape, never code fences for decision content; (2) branch bodies are prose step sequences — not tables-as-procedure, not fenced pseudo-config; (3) branch headings live at H2/H3 only, never deeper, and boolean decisions (e.g. guest-sessions on/off) may skip branch headings entirely via an if/then prose pair. Reserve MUST/NORMATIVE language EXCLUSIVELY for these three rules.
      - The RECOMMENDED idiom (D-05), labeled RECOMMENDED (not normative): the PSSO-walkthrough shape (shared spine → decision block → sibling H3 `### Branch A —`/`### Branch B —` sections, bold pseudo-headings inside branches, plain routing sentence). The spec MUST NOT mandate a rejoin point — non-converging forks are explicitly permitted (cite the PSSO "no reconvergence" precedent).
      - The normative Summary end-state rule (D-13): every recipe's `## Summary` OPENS with a one-line concrete end-state statement; phrase it per the standard's established D-08 pattern — "REQUIRED; enforced by registry review, not by the harness."
      - ONE compact FENCED worked branching mini-example (D-11): show the composite shape (an `> **Ask the admin:**` lead-in + blank line + a small 4-column branching table) inside a ```markdown fenced block (the inCodeFence mask exempts it from C17 #11/#12 on this enrolled non-sentinel standard). Model the Kiosk-vs-Shared-PC subject from PATTERNS. Include the one-line carve-out sentence: a fenced spec sample in this index-excluded standard is NOT the same as fenced decision content in a live indexed recipe (which remains banned).
    Prose wording of subsections and internal numbering is Claude's discretion within D-09..D-13; keep the standard's existing voice.

    EDIT 3 — Version-History row. Append ONE row after line 515 (the STD-04 row), matching that row's `| YYYY-MM-DD | v1.NN ... |` format. Date 2026-07-17. Name BOTH the new STD-05 section (D-01..D-05: 3-case composite block, mandatory blank line, case-boundary rule, 3-rule branch floor + RECOMMENDED PSSO idiom) AND the new Device Recipe D-02 edge-case ruling row (`docs/recipes/*` → `Guide`) (D-12). Do NOT touch the frontmatter `last_verified` at line 7 — it stays 2026-07-04 (D-12).

    DO NOT edit scripts/validation/c17-eee-contract.mjs or add any allowlist entry (D-10).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs --self-test && node scripts/validation/c17-eee-contract.mjs --verbose</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validation/c17-eee-contract.mjs --self-test` exits 0 with 4/4 sub-tests PASS.
    - `node scripts/validation/c17-eee-contract.mjs --verbose` exits 0 reporting 229 files checked, 0 files with violations, 0 total violations (unchanged from the pre-phase baseline — the standard is edited in place, no new file enrolls in this plan).
    - `grep -n "## Admin Decision-Point Block Format (STD-05)" docs/_standards/EEE-SOP-standard.md` returns exactly one line, positioned after the STD-04 section and before `## C17 Enforcement Reference`.
    - `grep -n "Device Recipe documents" docs/_standards/EEE-SOP-standard.md` returns a bullet line inside the `### D-02 Edge-case rulings` list containing both `docs/recipes/*` and the backtick token `Guide` and a reference to `STD-05`.
    - STD-05 body contains, as source assertions: the exact 3-column enumerable header `| Option | When to choose | Recorded as |`, the exact 4-column branching header `| Option | When to choose | Consequence if wrong | Branch |`, the phrase `Ask the admin`, the phrase `enforced by registry review, not by the harness`, an explicit statement of the mandatory blank-line rule, exactly three NORMATIVE branch-floor rules, and the RECOMMENDED-not-mandated no-reconvergence statement.
    - STD-05 contains exactly one ```markdown fenced worked example and a carve-out sentence distinguishing a fenced spec sample here from banned fenced decision content in live recipes.
    - `grep -c "^| 2026-07-17 |" docs/_standards/EEE-SOP-standard.md` returns 1; that row names both `STD-05` and the D-02 recipe ruling.
    - `grep -n "^last_verified: 2026-07-04" docs/_standards/EEE-SOP-standard.md` still matches (frontmatter last_verified unchanged).
    - The existing strings `## Mermaid-in-Enrolled-Classes Policy (STD-04)` and `#### Non-MECE precedence rule (D-08)` are both still present and unmodified (check-phase-120.mjs substring targets).
    - `scripts/validation/c17-eee-contract.mjs` is byte-unchanged (D-10): `git diff --quiet scripts/validation/c17-eee-contract.mjs` exits 0.
  </acceptance_criteria>
  <done>STD-05 spec, D-02 ruling bullet, and Version-History row are present and additive-only; C17 self-test 4/4 and verbose 229/0; check-phase-120 substring targets intact; C17 script and last_verified untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the false 00-index universality claim in ARCHITECTURE.md (D-15)</name>
  <files>.planning/research/ARCHITECTURE.md</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md — lines 56–69, the "Recommended Project Structure" fenced code block; specifically lines 58–61, the `00-overview.md` structure-tree entry whose trailing comment claims `convention: every doc-class directory has an 00-* index`.
    - .planning/phases/129-device-recipe-doc-class-foundation/129-CONTEXT.md — D-14 (NO docs/recipes/00-overview.md index doc in this milestone) and D-15 (fix line 61 at source) plus the Assumptions Log A1 rationale.
    - .planning/phases/129-device-recipe-doc-class-foundation/129-PATTERNS.md — the ARCHITECTURE.md section: exact before/after of the structure-tree collapse.
  </read_first>
  <action>
    In the "Recommended Project Structure" fenced block, DELETE the entire `00-overview.md` structure-tree entry (the tree line plus its two wrapped `#`-continuation comment lines that together carry the false "convention: every doc-class directory has an 00-* index" claim). Collapse the `recipes/` subtree to exactly two entries: `01-windows-avd-shared-device.md` (Recipe #1) and `02-shared-ipad-full-provisioning.md` (Recipe #2), keeping the `├──`/`└──` tree glyphs correct (the last remaining entry uses `└──`). This is the cleanest fix per A1: it removes the false claim (D-15) AND aligns the recommended structure with D-14 (no index doc exists in this phase). Do not edit anything else in the file.
  </action>
  <verify>
    <automated>grep -c "every doc-class directory has an 00-\* index" .planning/research/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "every doc-class directory has an 00-\* index" .planning/research/ARCHITECTURE.md` returns 0 (the false claim is gone).
    - `grep -c "recipes/00-overview.md" .planning/research/ARCHITECTURE.md` returns 0 (no index-doc line remains in the recommended structure).
    - The `recipes/` subtree still lists both `01-windows-avd-shared-device.md` and `02-shared-ipad-full-provisioning.md`, and the last entry uses the `└──` glyph.
    - No other line in the file changed: `git diff --stat .planning/research/ARCHITECTURE.md` shows only the structure-tree region touched.
  </acceptance_criteria>
  <done>The false 00-index universality claim is removed, the recipes/ tree shows only the two recipe files with correct glyphs, and no other content changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan authors Markdown/planning text only. No runtime, network, auth, or untrusted-input surface exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Tampering | docs/_standards/EEE-SOP-standard.md predecessor-frozen surfaces (STD-04 header, D-08 subsection) | mitigate | Edits are additive-only; acceptance criteria assert STD-04/D-08 substring targets remain present so check-phase-120.mjs stays green |
| T-129-02 | Tampering | scripts/validation/c17-eee-contract.mjs (shared subprocess-spawned C17 script) | mitigate | D-10: never edit this file; acceptance criterion `git diff --quiet` proves byte-unchanged, preventing retroactive mutation of every predecessor milestone audit |
| T-129-03 | Denial (regression) | Full corpus C17 greenness | mitigate | Verify --self-test 4/4 and --verbose 229/0 after edits; a violation blocks the plan |
| T-129-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A) — no supply-chain surface |
</threat_model>

<verification>
- `node scripts/validation/c17-eee-contract.mjs --self-test` → 4/4 PASS, exit 0.
- `node scripts/validation/c17-eee-contract.mjs --verbose` → 229 files checked, 0 violations, exit 0.
- `git diff --quiet scripts/validation/c17-eee-contract.mjs` → exit 0 (C17 script untouched).
- STD-05 section, D-02 ruling bullet, and 2026-07-17 Version-History row all present; frontmatter `last_verified` still 2026-07-04.
- ARCHITECTURE.md no longer asserts the 00-index universality claim.
</verification>

<success_criteria>
- CLASS-01 satisfied: the standard carries the slim D-02 recipe ruling + the full STD-05 decision-point block spec (3-case composite, blank-line rule, case-boundary rule, 3-rule branch floor, RECOMMENDED idiom, fenced example, carve-out sentence, normative Summary end-state rule).
- C17 remains green (self-test 4/4, verbose 229/0); no C17 script edit; predecessor substring targets intact.
- ARCHITECTURE.md:61 false claim corrected at source.
</success_criteria>

<output>
Create `.planning/phases/129-device-recipe-doc-class-foundation/129-01-SUMMARY.md` when done.
</output>
