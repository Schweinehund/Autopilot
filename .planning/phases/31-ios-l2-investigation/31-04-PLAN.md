---
phase: 31-ios-l2-investigation
plan: 04
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - docs/l2-runbooks/16-ios-app-install.md
autonomous: true
requirements: [L2TS-02]
tags: [ios, l2, app-install, vpp, supervision, sc4]

must_haves:
  truths:
    - "An L2 engineer investigating an app install failure can distinguish configuration errors [CONFIG] from timing issues [TIMING] from genuine defects [DEFECT] — SC #4 literal satisfied"
    - "An L2 engineer sees a named failure pattern for each VPP device / VPP user / LOB (IPA) / supervision-boundary-blocked scenario"
    - "An L2 engineer reads a Microsoft Support escalation checklist (≥3 data items) attached to the [DEFECT] class"
    - "An L2 engineer who hits MAM-WE issues is routed to the 00-index.md#mam-we-investigation-advisory (not a stub runbook) per D-13"
  artifacts:
    - path: "docs/l2-runbooks/16-ios-app-install.md"
      provides: "iOS App Install Failure Diagnosis runbook"
      contains: "[CONFIG]"
      min_lines: 161
  key_links:
    - from: "docs/l2-runbooks/16-ios-app-install.md"
      to: "docs/l2-runbooks/00-index.md#mam-we-investigation-advisory"
      via: "D-13 MAM-WE cross-reference"
      pattern: "mam-we-investigation-advisory"
---

<objective>
Author `docs/l2-runbooks/16-ios-app-install.md` — the iOS App Install Failure Diagnosis runbook satisfying SC #4 three-class disambiguation (configuration error / timing issue / genuine defect). Uses **text markers `[CONFIG]` / `[TIMING]` / `[DEFECT]`** (NOT emoji — per Wave 0 emoji audit decision recorded in `placeholder-inventory.json` _note: 0 emoji matches in existing L1/L2 runbooks).

Covers VPP device-licensed, VPP user-licensed, LOB (IPA), and supervision-boundary failure modes. Mirrors Phase 24 macOS `12-macos-app-install.md` (194 lines) channel-table structure with iOS-specific adaptations: no IME channel; MDM channel only; three-class markers; MAM-WE deferral cross-link per D-13.

Purpose: File 16 is the L2 target for L1 triage-tree "app not working" routing (decision-trees/07-ios-triage.md lines 72/82/89 per D-22) and for any L1-escalated app install ticket.

Output: One new file at `docs/l2-runbooks/16-ios-app-install.md` (~190-210 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-ios-l2-investigation/31-CONTEXT.md
@.planning/phases/31-ios-l2-investigation/31-RESEARCH.md
@.planning/phases/31-ios-l2-investigation/31-PATTERNS.md
@.planning/phases/31-ios-l2-investigation/placeholder-inventory.json
@docs/l2-runbooks/12-macos-app-install.md
@docs/l2-runbooks/14-ios-log-collection.md
@docs/admin-setup-ios/05-app-deployment.md

<interfaces>
<!-- D-29 platform gate banner -->
```markdown
> **Platform gate:** This guide covers iOS/iPadOS L2 investigation via Intune. For Windows Autopilot, see [Windows L2 Runbooks](00-index.md). For macOS ADE, see [macOS ADE Runbooks](00-index.md#macos-ade-runbooks).
```

<!-- D-28 frontmatter for file 16 -->
```yaml
---
last_verified: 2026-04-17
review_by: 2026-07-16
applies_to: all
audience: L2
platform: iOS
---
```

<!-- Text-marker policy locked Wave 0 — runbook 16 uses these, NOT emoji -->
[CONFIG] — configuration error class (admin fix in Intune/ABM)
[TIMING] — timing issue class (wait/sync, no admin fix needed)
[DEFECT] — genuine defect class (Microsoft Support escalation required)

<!-- D-13 MAM-WE cross-reference — verbatim -->
```markdown
> **MAM-WE scope:** MAM-WE app protection policy failures (selective wipe failures, PIN loop, app protection not applying) are **out of Phase 31 scope** — see [MAM-WE Investigation Advisory](00-index.md#mam-we-investigation-advisory) for deferred ADDTS-01 milestone routing.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author runbook 16 — frontmatter + Context with channel table + Investigation + per-class failure pattern tables ([CONFIG] / [TIMING])</name>

  <read_first>
    - .planning/phases/31-ios-l2-investigation/placeholder-inventory.json (confirm _note recorded TEXT markers, NOT emoji — DO NOT use ⚙️ / ⏱️ / 🐛)
    - docs/l2-runbooks/12-macos-app-install.md (full file — structural analog; inherit Context channel table + Investigation shape)
    - .planning/phases/31-ios-l2-investigation/31-RESEARCH.md §Runbook 16 Failure Class Mapping (lines 399-442 — exhaustive VPP/LOB failure tables)
    - .planning/phases/31-ios-l2-investigation/31-CONTEXT.md D-11 (single runbook), D-12 (three-class disambiguation), D-13 (MAM cross-ref), D-34 research (supervision boundary)
    - docs/admin-setup-ios/05-app-deployment.md (cross-reference target for VPP/LOB configuration)
  </read_first>

  <files>docs/l2-runbooks/16-ios-app-install.md</files>

  <behavior>
    - Test A: NO emoji present: `grep -cE "(⚙️|⏱️|🐛)" docs/l2-runbooks/16-ios-app-install.md` returns 0.
    - Test B: Text markers present in sufficient count: `grep -c "\[CONFIG\]"` >= 2, `grep -c "\[TIMING\]"` >= 1 (V-31-11 after Task 2 adds [DEFECT]).
    - Test C: Frontmatter valid with applies_to: all.
    - Test D: MAM-WE cross-reference link present (V-31-13).
  </behavior>

  <action>
    Create `docs/l2-runbooks/16-ios-app-install.md` with ~100-120 lines for this task.

    **1. Frontmatter** (verbatim from `<interfaces>`).

    **2. Platform gate banner** (verbatim D-29).

    **3. Title**: `# iOS App Install Failure Diagnosis`

    **4. `## Triage`** — inherit shape from 11/12/13 macOS analogs:
       ```
       **From L1 escalation?** L1 collected: device serial, iOS version, supervision state, app name + type (VPP device / VPP user / LOB IPA / App Store), Intune install status screenshot. Skip to Step 2 (failure classification) if the app type is known.

       **Starting fresh?** Begin at Step 1.
       ```

    **5. `## Context`** — ~15 lines covering:
       - Scope: all iOS app deployment channels (VPP device-licensed, VPP user-licensed, LOB IPA, App Store redirect).
       - Distinction (mirroring macOS 12's channel-disambiguation approach but iOS-adapted): "iOS has **only the MDM channel** for managed app install — there is no IME-daemon equivalent as on macOS. Apps are delivered via Apple MDM commands (`InstallApplication` legacy OR declarative app management from iOS 17.2+). **The app LICENSING MODEL and the device SUPERVISION STATE are the axes that determine failure mode**, not the delivery channel."
       - Include an Apple-deployment-axes table:
         | Axis | Options | Affects |
         | Licensing | VPP device / VPP user / LOB (IPA) / App Store | Who the license binds to; whether Managed Apple Account required |
         | Supervision | Supervised / Unsupervised | Whether silent install is possible (D-34: supervised only) |
         | Assignment | Required / Available / Available for enrolled devices | Whether install is auto vs user-initiated |
       - D-13 MAM-WE cross-reference blockquote (verbatim from `<interfaces>`). This MUST appear in the Context section so L2 engineers reading for MAM-WE issues early-exit to the index advisory.
       - Close with: "Before starting: collect a diagnostic package per [iOS Log Collection Guide](14-ios-log-collection.md)."

    **6. `## Three-Class Disambiguation`** — Explain the marker convention:
       ```
       This runbook tags every failure pattern with one of three classes:

       - **[CONFIG]** — Configuration error. Fix in Intune admin center, ABM, or on the device.
       - **[TIMING]** — Timing issue. Wait for next check-in or trigger a Company Portal Sync. No admin change required.
       - **[DEFECT]** — Genuine defect. All [CONFIG] conditions verified; all [TIMING] windows elapsed; still failing. Escalate to Microsoft Support with the data-collection checklist below.
       ```
       Follow with a one-liner: "When investigating, rule out [CONFIG] first, then [TIMING], then declare [DEFECT]."

    **7. `## Investigation — Step 1: Identify the app type`** — Breadcrumb: Intune admin center > Apps > iOS/iPadOS > [app] > **Properties** > App information panel. Note: type shown as "iOS store app", "iOS line-of-business app", or "iOS volume purchase program app" (+ device-vs-user license toggle on the latter).

    **8. `## Investigation — Step 2: Check supervision state`** — On device: Settings > General > VPN & Device Management > Management Profile — look for "Supervised" banner. On Intune: Devices > [device] > Overview > Management name typically indicates supervision. Supervision is set at enrollment time and cannot be added retroactively without wipe (referenced from iOS Lifecycle foundation).

    **9. `## Investigation — Step 3: Managed app state verification` (from RESEARCH)** — Three-step procedure:
       1. Intune admin center > Apps > iOS/iPadOS > [app] > **Device install status** — per-device state (Installed / Pending / Failed / Not applicable).
       2. On device (user-coordinated): Settings > General > VPN & Device Management > Management Profile > More Details — scan "Managed Apps" section.
       3. Company Portal app > My apps — does app appear? Does "Install" tap succeed?

       Decision fork:
       - Step 1 Installed + Step 2 confirms + Step 3 works → app healthy; symptom is app-level config or MAM.
       - Step 1 Failed + Step 2 missing → proceed to failure-class tables in Step 4.
       - Step 1 Installed + Step 2 missing → device-side stale cache; trigger Company Portal Sync or profile re-delivery.

    **10. `## Investigation — Step 4: Match against failure patterns`** — Include TWO failure pattern tables for this task (Task 2 adds [DEFECT]):

       `### VPP Device-Licensed failures` (4 rows — pull from RESEARCH §VPP Device-Licensed):
       | Failure | Class | Indicator | Resolution |
       |---------|-------|-----------|------------|
       | VPP token expired | [CONFIG] | Tenant admin > Connectors and tokens > Apple VPP tokens shows Expired | Renew in ABM → upload renewed `.vpptoken` to Intune |
       | License count exhausted | [CONFIG] | Apps > iOS/iPadOS > [app] > Properties shows total==consumed | Purchase additional licenses in ABM; sync VPP token |
       | Device-licensed app assigned to unsupervised device (silent install expected) | [CONFIG] | Device shows no "Supervised" banner; app Pending or user prompted | Change assignment to "Available" OR re-enroll via ADE with supervision |
       | Device not synced recently | [TIMING] | Last check-in > 8h; `Pending install` | Company Portal Sync OR Intune Sync; wait 15 min; recheck |

       `### VPP User-Licensed failures` (3 rows — from RESEARCH):
       | Failure | Class | Indicator | Resolution |
       |---------|-------|-----------|------------|
       | User has no Managed Apple Account | [CONFIG] | Assignment in Intune shows user group; user not a Managed Apple Account in ABM | Switch to device licensing OR onboard Managed Apple Account |
       | User-licensed app assigned to Device Enrollment / ADE device | [CONFIG] | Device is supervised ADE or Device Enrollment; user-licensed requires User Enrollment | Switch assignment to device licensing |
       | Apple ID mismatch on device | [CONFIG] | Device has personal Apple ID, not Managed Apple Account | Sign out personal; sign in Managed Apple Account |

       `### LOB (IPA) failures` (4 rows — from RESEARCH):
       | Failure | Class | Indicator | Resolution |
       |---------|-------|-----------|------------|
       | IPA unsigned or expired enterprise cert | [CONFIG] | Device shows "Cannot Install [App]" OR silent failure; sysdiagnose `appleagent.log` code-signing rejection | Re-sign with current Apple Developer Enterprise Program cert; re-upload |
       | IPA > 2 GB | [CONFIG] | Intune upload fails OR device download fails consistently | Reduce IPA size OR split into LOB + resources |
       | Unsupervised device, silent-install expectation | [CONFIG] | Unsupervised; IPA requires user prompt | Change assignment OR supervise device |
       | IPA signed with dev provisioning profile (not distribution) | [CONFIG] | Install succeeds but app crashes at launch; sysdiagnose shows provisioning-profile mismatch | Re-sign with distribution provisioning profile |

       Add one inline note per D-34 research finding (supervised silent install boundary):
       > **Supervision boundary (D-34 research verified 2026-04-17):** Silent install requires supervision on iOS 17+. Declarative Device Management (DDM, iOS 17.2+) changed HOW apps install (autonomous execution + activation predicates) but did NOT change the supervision-vs-prompt boundary. DDM can also "take over" management of user-installed unmanaged apps — silent on supervised, user-accept on unsupervised (a new [CONFIG] failure mode).

    **11. `## Version History`** (placeholder; Task 2 finalizes):
       ```
       ## Version History

       | Date | Change | Author |
       |------|--------|--------|
       | 2026-04-17 | Initial version — iOS app install failure diagnosis | -- |
       ```

    Commit: `docs(31): scaffold 16-ios-app-install.md — Context + three-class convention + VPP/LOB failure tables`.

    Run `node scripts/validation/check-phase-31.mjs --quick` and confirm V-31-13 passes and [CONFIG]/[TIMING] markers are being counted (V-31-11 will fully pass in Task 2).
  </action>

  <verify>
    <automated>node scripts/validation/check-phase-31.mjs --quick 2>&1 | grep -E "V-31-(13|26|27|28)" && test $(grep -cE "(⚙️|⏱️|🐛)" docs/l2-runbooks/16-ios-app-install.md) -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `test -f docs/l2-runbooks/16-ios-app-install.md`
    - NO emoji: `test $(grep -cE "(⚙️|⏱️|🐛)" docs/l2-runbooks/16-ios-app-install.md) -eq 0`
    - `grep -c "\[CONFIG\]" docs/l2-runbooks/16-ios-app-install.md` >= 8 (many [CONFIG] failures in VPP/LOB tables)
    - `grep -c "\[TIMING\]" docs/l2-runbooks/16-ios-app-install.md` >= 1
    - V-31-13 passes: `grep -q "00-index\\.md#mam-we-investigation-advisory" docs/l2-runbooks/16-ios-app-install.md`
    - V-31-26/27/28 pass (frontmatter + applies_to:all + banner)
  </acceptance_criteria>

  <done>
    File exists; three-class convention + text markers + VPP/LOB tables + MAM advisory cross-ref inline; NO emoji; V-31-13/26/27/28 green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete runbook 16 — [DEFECT] class + Microsoft Support escalation checklist + Resolution Scenarios + Related Resources</name>

  <read_first>
    - docs/l2-runbooks/16-ios-app-install.md (current state from Task 1)
    - .planning/phases/31-ios-l2-investigation/31-RESEARCH.md §VPP Device-Licensed failures — specifically the last "Silent install supervised + token active + license available + still Failed" [DEFECT] row and its escalation data list
    - docs/l2-runbooks/12-macos-app-install.md lines 137-157 (Resolution Scenarios block — inherit shape)
    - .planning/phases/31-ios-l2-investigation/31-VALIDATION.md V-31-12 (escalation checklist structural rule: Microsoft Support within 20 lines + ≥3 bullets)
  </read_first>

  <files>docs/l2-runbooks/16-ios-app-install.md</files>

  <behavior>
    - Test A: `grep -c "\[DEFECT\]" docs/l2-runbooks/16-ios-app-install.md` >= 1 (V-31-11).
    - Test B: After `[DEFECT]` first mention, "Microsoft Support" appears within the next 4000 chars AND there are ≥3 markdown bullets (V-31-12).
    - Test C: File length 161-241 lines (V-31-29 ±15% of 190-210 band).
    - Test D: V-31-11 fully passes (all three markers present at required counts).
  </behavior>

  <action>
    Append to `docs/l2-runbooks/16-ios-app-install.md`.

    **1. `### Genuine Defects (Declared [DEFECT])`** — Explain: "A failure is [DEFECT] when ALL [CONFIG] conditions have been verified AND all [TIMING] windows elapsed AND the install still fails with no actionable Intune error. This is rare. Misclassifying a [CONFIG] or [TIMING] as a [DEFECT] wastes Microsoft Support engineering time; verify every [CONFIG] row above + at least one full 8h check-in cycle before declaring [DEFECT]."

       Then list 2-3 observed defect patterns (from RESEARCH):
       - **[DEFECT] VPP device-licensed silent install stalled** — Supervised + token active + license available + app exists in ABM → still shows Failed with no Intune error. No admin resolution.
       - **[DEFECT] DDM-managed app stuck on activation predicate** — iOS 17.2+ Required app install never completes despite device meeting declared predicate. iOS version + app bundle ID + predicate JSON required for support.
       - **[DEFECT] App Store binary broken post-iOS-upgrade** — VPP app stopped launching after iOS major version upgrade; install shows Installed but launch crashes.

    **2. `### Microsoft Support Escalation Checklist (for [DEFECT] class)`** — Structural requirement: within 20 lines of [DEFECT] mention, have "Microsoft Support" heading + ≥3 bullet data items (V-31-12). Use these bullets (adapt from RESEARCH):

       Open a Microsoft Support case with the following **data collection package**:
       - Device serial number + iOS version + supervision state (from Step 2)
       - App ID (Intune GUID) + Bundle ID (e.g., `com.microsoft.Office.Outlook`) + app type (VPP device / VPP user / LOB / Store)
       - Intune install status screenshot (Apps > [app] > Device install status row for the target device)
       - VPP token `lastSyncSuccessDateTime` (from Tenant admin > Connectors and tokens > VPP tokens pane; numeric error code if Graph-pulled per runbook 15 Step 1 Supplement)
       - ABM app assignment state screenshot (Apps & Books > [app] > Assignments)
       - Company Portal log upload incident ID (from runbook 14 Tier 2) OR sysdiagnose `.tar.gz` (runbook 14 Tier 3, PII-redacted)
       - Timeline: when assignment was made, when device was last synced, when failure was first observed

    **3. `## Resolution Scenarios`** — Inherit macOS 12 shape with 5 named scenarios (iOS-adapted):

       `### Scenario A: Unsupervised device + Required silent-install expectation`
       Fix: Change assignment to "Available" in Intune Apps > [app] > Assignments. User self-installs from Company Portal. OR re-enroll via ADE with supervision enabled — requires device wipe.

       `### Scenario B: VPP token expired`
       Fix: Renew VPP token in ABM (Apps & Books > your name > Content tokens > [country] > Download token). In Intune: Tenant admin > Connectors and tokens > Apple VPP tokens > [token] > Edit > Upload `.vpptoken`. Sync token. Retry assignment within 30 min.

       `### Scenario C: User-licensed to non-User-Enrollment device`
       Fix: Either reassign the app as device-licensed (Apps > [app] > Properties > License type: Device) OR re-enroll the device under User Enrollment. Device-licensed is simpler for managed devices.

       `### Scenario D: LOB IPA code-signing issue`
       Fix: Verify enterprise cert expiry in Apple Developer Enterprise account. Re-sign IPA with current cert + distribution provisioning profile. Re-upload to Intune. Delete-and-reassign (not just upgrade) to clear device-side stale binary.

       `### Scenario E: Post-check-in stale state`
       Fix: Company Portal > Sync OR Intune Remote Sync. Wait 15-30 min. If still Pending, escalate to timing-window investigation (may be [TIMING] — no action needed) OR to Microsoft Support with [DEFECT] checklist.

    **4. `## Related Resources`** — Bullets:
       - `[iOS Log Collection Guide](14-ios-log-collection.md)` — prerequisite
       - `[ADE Token & Profile Delivery Investigation](15-ios-ade-token-profile.md)` — if failure is enrollment-related, not app-specific
       - `[iOS App Deployment Admin Guide](../admin-setup-ios/05-app-deployment.md)` — VPP/LOB configuration reference
       - `[MAM-WE Investigation Advisory](00-index.md#mam-we-investigation-advisory)` — deferred ADDTS-01 scope for MAM-WE app protection failures

    Run `node scripts/validation/check-phase-31.mjs --quick` and confirm V-31-11, V-31-12, V-31-13 all PASS. File length should be 161-241 lines.

    Commit: `docs(31): complete 16-ios-app-install.md — [DEFECT] class + MS Support escalation checklist + Resolution Scenarios`.
  </action>

  <verify>
    <automated>node scripts/validation/check-phase-31.mjs --quick 2>&1 | grep -E "V-31-(11|12|13|29)" && test $(wc -l < docs/l2-runbooks/16-ios-app-install.md) -ge 161</automated>
  </verify>

  <acceptance_criteria>
    - V-31-11 passes: [CONFIG] >= 2, [TIMING] >= 1, [DEFECT] >= 1 (actual counts will be much higher)
    - V-31-12 passes: Microsoft Support + 3+ bullets within 4000 chars of first [DEFECT]
    - V-31-13 passes: MAM advisory cross-ref link present (from Task 1)
    - V-31-29 passes: line count 161-241
    - NO emoji: `test $(grep -cE "(⚙️|⏱️|🐛)" docs/l2-runbooks/16-ios-app-install.md) -eq 0`
    - `grep -q "DDM" docs/l2-runbooks/16-ios-app-install.md` AND `grep -q "Declarative Device Management" docs/l2-runbooks/16-ios-app-install.md` (D-34 research findings cited)
  </acceptance_criteria>

  <done>
    Runbook 16 complete; V-31-11/12/13 + V-31-26/27/28/29 green; NO emoji; SC #4 three-class disambiguation literal satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| L2 engineer → ABM VPP token operations | Read-only guidance; write actions (token renewal) require ABM admin |
| Device → LOB IPA installer | Code-signing trust boundary; expired enterprise cert = failed install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | Tampering | LOB IPA code-signing | mitigate | LOB failure table documents code-signing verification as a [CONFIG] rule; Resolution Scenario D prescribes re-sign with distribution provisioning profile before redeploy |
| T-31-07 | Elevation of privilege | MAM-WE investigation scope creep | mitigate | D-13 MAM cross-reference blockquote in Context routes MAM issues OUT of file 16 and to the 00-index.md advisory (V-31-13 grep gate); prevents L2 from treating file 16 as a MAM runbook |

Phase-wide T-31-01/02/03 not directly triggered in file 16 (no sysdiagnose triggers or Graph API writes in app-install diagnosis path). File 16 references runbook 14 for data collection — inherits T-31-01/T-31-02 mitigations via cross-reference.
</threat_model>

<verification>
- V-31-11/12/13 + V-31-26/27/28/29 all green
- NO emoji in file 16 (text markers locked per Wave 0 emoji audit)
- SC #4 three-class disambiguation literal satisfied
</verification>

<success_criteria>
Runbook 16 satisfies SC #4 (three-class config/timing/defect distinction) for app install. Failure pattern tables cover VPP device, VPP user, LOB IPA, and supervision-boundary failures. Microsoft Support escalation checklist attached to [DEFECT] class. MAM-WE deferred to ADDTS-01 via cross-reference (not stub runbook).
</success_criteria>

<output>
After completion, create `.planning/phases/31-ios-l2-investigation/31-04-SUMMARY.md`.
</output>
