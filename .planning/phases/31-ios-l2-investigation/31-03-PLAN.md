---
phase: 31-ios-l2-investigation
plan: 03
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - docs/l2-runbooks/15-ios-ade-token-profile.md
autonomous: true
requirements: [L2TS-02]
tags: [ios, l2, ade, token, profile-delivery, sc3, graph-api]

must_haves:
  truths:
    - "An L2 engineer sees specific indicators to check (token sync status, profile assignment state, enrollment profile GUID) — SC #3 literal satisfied"
    - "An L2 engineer reads 4 known failure patterns (Pattern A token/sync; Pattern B profile-never-assigned; Pattern C APNs/network; Pattern D wrong-MDM-server) each with indicators + resolution — SC #3 literal satisfied"
    - "An L2 engineer is warned (T-31-03) that the Graph API scope is READ-ONLY, tenant-wide, and that write operations are deferred to ADDTS-02"
    - "Prerequisites section enforces triple-portal access (ABM + Intune + Entra) per D-10 — V4 Access Control"
  artifacts:
    - path: "docs/l2-runbooks/15-ios-ade-token-profile.md"
      provides: "iOS ADE Token & Profile Delivery Investigation runbook"
      contains: "### Pattern A:"
      min_lines: 187
  key_links:
    - from: "docs/l2-runbooks/15-ios-ade-token-profile.md"
      to: "docs/l2-runbooks/14-ios-log-collection.md"
      via: "Before starting / Prerequisites / cross-references"
      pattern: "14-ios-log-collection"
---

<objective>
Author `docs/l2-runbooks/15-ios-ade-token-profile.md` — the iOS ADE Token & Profile Delivery Investigation runbook satisfying SC #3 (indicators to check AND known failure patterns with resolution steps) via the D-07 hybrid three-layer H2 structure: Investigation - Data Collection / Analysis - Match Against Known Failure Patterns / Resolution. Mirrors `11-macos-profile-delivery.md` (191 lines) with iOS-specific delta: Pattern A-D substance from RESEARCH + Graph API READ-ONLY preamble + triple-portal prerequisite.

Purpose: File 15 is the primary L2 target for iOS L1 runbooks 16 (APNs expired), 17 (ADE not starting), 18 (enrollment restriction blocking), 19 (license invalid; ADE-path), and 20 (device cap reached). Its existence + content is what makes Wave 5 D-22 retrofits meaningful.

Output: One new file at `docs/l2-runbooks/15-ios-ade-token-profile.md` (~220-280 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-ios-l2-investigation/31-CONTEXT.md
@.planning/phases/31-ios-l2-investigation/31-RESEARCH.md
@.planning/phases/31-ios-l2-investigation/31-PATTERNS.md
@docs/l2-runbooks/11-macos-profile-delivery.md
@docs/l2-runbooks/14-ios-log-collection.md

<interfaces>
<!-- D-29 platform gate banner — verbatim -->
```markdown
> **Platform gate:** This guide covers iOS/iPadOS L2 investigation via Intune. For Windows Autopilot, see [Windows L2 Runbooks](00-index.md). For macOS ADE, see [macOS ADE Runbooks](00-index.md#macos-ade-runbooks).
```

<!-- D-09 Graph API READ-ONLY preamble — verbatim from CONTEXT.md -->
```markdown
> **Graph API scope:** Where this runbook uses the Microsoft Graph API, usage is strictly READ-ONLY (GET requests). No modifications. No token regeneration. For deep token GUID extraction, write operations, or advanced enumeration, see **ADDTS-02** (future milestone — `L2 runbook for ADE token delivery deep-dive`).
```

<!-- D-28 frontmatter shape for file 15 — applies_to: ADE -->
```yaml
---
last_verified: 2026-04-17
review_by: 2026-07-16
applies_to: ADE
audience: L2
platform: iOS
---
```

<!-- Graph API endpoints + permission scope verified in RESEARCH D-33 -->
GET https://graph.microsoft.com/beta/deviceManagement/depOnboardingSettings
GET https://graph.microsoft.com/beta/deviceManagement/depOnboardingSettings/{depOnboardingSettingId}
Scope required: DeviceManagementServiceConfig.Read.All
Key response fields: tokenExpirationDateTime, lastModifiedDateTime, lastSuccessfulSyncDateTime, lastSyncTriggeredDateTime, lastSyncErrorCode, syncedDeviceCount, appleIdentifier
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author runbook 15 — frontmatter + Triage + Context + Graph preamble + Prerequisites + Investigation (Steps 1-4)</name>

  <read_first>
    - docs/l2-runbooks/11-macos-profile-delivery.md (full file — structural analog for steps 1-4 shape)
    - .planning/phases/31-ios-l2-investigation/31-RESEARCH.md §D-33 (Graph API endpoint + response shape)
    - .planning/phases/31-ios-l2-investigation/31-CONTEXT.md D-07 (hybrid three-layer structure), D-08 (portal-first + Graph supplement), D-09 (Graph warning preamble), D-10 (triple-portal prereq)
    - docs/l2-runbooks/14-ios-log-collection.md (prereq cross-reference target)
    - docs/l1-runbooks/16-ios-apns-expired.md through 20-ios-device-cap-reached.md (L1 runbooks escalating INTO file 15 — read to understand the handoff)
  </read_first>

  <files>docs/l2-runbooks/15-ios-ade-token-profile.md</files>

  <behavior>
    - Test A: Frontmatter has `applies_to: ADE` (per D-28) — NOT `all`.
    - Test B: `grep -q "depOnboardingSettings" docs/l2-runbooks/15-ios-ade-token-profile.md` AND `grep -q "DeviceManagementServiceConfig\\.Read\\.All" docs/l2-runbooks/15-ios-ade-token-profile.md` AND `grep -q "READ-ONLY" docs/l2-runbooks/15-ios-ade-token-profile.md` AND `grep -q "ADDTS-02" docs/l2-runbooks/15-ios-ade-token-profile.md` — all 4 sub-checks pass (V-31-09).
    - Test C: `grep -qiE "ABM.*Intune.*Entra|ABM.*Entra.*Intune|Intune.*ABM.*Entra" docs/l2-runbooks/15-ios-ade-token-profile.md` (V-31-10 triple-portal).
    - Test D: H2 `## Investigation — Data Collection` present (V-31-08 partial).
  </behavior>

  <action>
    Create `docs/l2-runbooks/15-ios-ade-token-profile.md` with the following structure. Target ~110-140 lines for this task (patterns + resolution come in Task 2).

    **1. Frontmatter** (verbatim from `<interfaces>` — note `applies_to: ADE`).

    **2. Platform gate banner** (verbatim D-29).

    **3. Title**: `# ADE Token & Profile Delivery Investigation`

    **4. `## Triage`** — Inherit shape from 11-macos-profile-delivery.md lines 13-17:
       ```
       **From L1 escalation?** L1 collected: device serial number, iOS version, Intune enrollment status, token name, and profile name. Skip to Step 2 if Pattern is unknown; otherwise jump directly to the matching Pattern in Analysis.

       **Starting fresh?** Begin at Step 1 in Investigation — Data Collection.
       ```

    **5. `## Context`** — ~10 lines. Scope: iOS/iPadOS ADE enrollment investigation covering both token-side issues (ABM-to-Intune sync) AND profile-delivery issues (Intune-to-device). Route-in sources: this is the target of L1 runbooks 16, 17, 18, 19 (ADE path), 20. Do NOT cover non-ADE enrollment paths (those route to runbook 14 directly). End with: "Before starting: collect a diagnostic package per [iOS Log Collection Guide](14-ios-log-collection.md)."

    **6. Graph API READ-ONLY preamble** — Insert the verbatim D-09 blockquote from `<interfaces>`.

    **7. `## Prerequisites` (D-10 triple-portal)** — Bullets:
       - **Apple Business Manager (ABM)** read access — device assignment state, MDM server association verification (Pattern B/D). Recommended role: *People > Admin* or *Device > Reader* within ABM.
       - **Intune admin center** read access — token state, profile assignment, compliance. Minimum: *Endpoint Security Manager* or custom role with `Organization/Read` + `Enrollment programs/Read`.
       - **Microsoft Entra** read access — verifying user license + group assignment when ADE is license-gated. Minimum: *Directory Readers*.
       - **(Optional) Microsoft Graph API** scope `DeviceManagementServiceConfig.Read.All` (read-only) — for Pattern A sync-state numeric error code extraction (step 1 supplement). Acquire via app-registration or Graph Explorer with delegated permissions.

       Then add a V4 Access Control reminder:
       ```
       > **Access-control note:** The Graph `DeviceManagementServiceConfig.Read.All` scope grants **tenant-wide read** across all DEP tokens. Prefer delegated (user-context) over application (service-principal) credentials for L2 ad-hoc investigation — principle of least privilege. If L2 lacks all three portals, hand off to a colleague with combined read access rather than requesting elevated write scope.
       ```

    **8. `## Investigation — Data Collection`** — H2 heading MUST match exactly (V-31-08).

       `### Step 1: Token sync status (Intune admin center)` — Breadcrumb: Intune admin center > Devices > iOS/iPadOS > iOS/iPadOS enrollment > **Enrollment program tokens** > [token]. Observables to collect: token expiration date, last sync date, `syncedDeviceCount`, status (Active / Expired / Sync error). If numeric `lastSyncErrorCode` needed → supplement with Graph GET in Step 1 Supplement below.

       `### Step 1 Supplement: Graph API token snapshot (READ-ONLY)` — Include endpoint + response-shape example. Use this fenced code block:
       ```http
       GET https://graph.microsoft.com/beta/deviceManagement/depOnboardingSettings
       GET https://graph.microsoft.com/beta/deviceManagement/depOnboardingSettings/{id}
       ```
       Document the key fields: `tokenExpirationDateTime`, `lastModifiedDateTime`, `lastSuccessfulSyncDateTime`, `lastSyncTriggeredDateTime`, `lastSyncErrorCode`, `syncedDeviceCount`, `appleIdentifier`. Each field maps to a Pattern A or D indicator (inline a small mapping list).

       `### Step 2: Profile assignment state (Intune admin center)` — Breadcrumb: Enrollment program tokens > [token] > **Profiles** > [profile] > **Assign devices**. Observables: is the failing device's serial listed? "Assigned" vs "Not assigned" column. Cross-check against ABM device list.

       `### Step 3: Enrollment profile GUID (Intune admin center URL extraction)` — Document the method: with the profile open, inspect the browser URL; the GUID is the segment after `.../enrollmentProfileId/`. Capture this GUID for Pattern C/D debugging (compare against device's MDM payload).

       `### Step 4: Device-side enrollment state` — Collect per runbook 14: Tier 1 on-device screenshot (Settings > General > VPN & Device Management > Management Profile > More Details) — this surfaces the device's view of the enrollment target URL, profile identifiers, and the issuing organization name. If mismatch suspected (Pattern D), also Tier 3 sysdiagnose for `kernel.log` APNs session records.

    **9. `## Version History`** (placeholder; Task 2 finalizes):
       ```
       ## Version History

       | Date | Change | Author |
       |------|--------|--------|
       | 2026-04-17 | Initial version — iOS ADE token & profile delivery investigation | -- |
       ```

    Commit: `docs(31): scaffold 15-ios-ade-token-profile.md — Triage + Context + Graph preamble + Prerequisites + Investigation steps 1-4`.

    Run `node scripts/validation/check-phase-31.mjs --quick` and confirm V-31-09, V-31-10, V-31-26, V-31-27, V-31-28 all PASS. V-31-06/07/08 will fully pass only after Task 2.
  </action>

  <verify>
    <automated>node scripts/validation/check-phase-31.mjs --quick 2>&1 | grep -E "V-31-(08|09|10|26|27|28)"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f docs/l2-runbooks/15-ios-ade-token-profile.md`
    - V-31-09 passes (4 sub-checks): `grep -q "depOnboardingSettings"` AND `grep -q "DeviceManagementServiceConfig\\.Read\\.All"` AND `grep -qi "READ-ONLY"` AND `grep -q "ADDTS-02"`
    - V-31-10 passes: `grep -qiE "(ABM|Apple Business Manager)" docs/l2-runbooks/15-ios-ade-token-profile.md` AND `grep -q "Intune"` AND `grep -qiE "(Entra|Azure AD)"`
    - V-31-26 passes: frontmatter fields all present
    - V-31-27 passes: `grep -q "^applies_to: ADE$" docs/l2-runbooks/15-ios-ade-token-profile.md`
    - V-31-28 passes: D-29 banner in first 30 lines
    - `grep -q "^## Investigation — Data Collection$" docs/l2-runbooks/15-ios-ade-token-profile.md`
    - `grep -q "tenant-wide read" docs/l2-runbooks/15-ios-ade-token-profile.md` (T-31-03 mitigation present)
  </acceptance_criteria>

  <done>
    File exists; Triage/Context/Graph preamble/Prerequisites/Investigation-steps-1-4 written; V-31-09/10/26/27/28 green; T-31-03 mitigation inline.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete runbook 15 — Analysis (Pattern A/B/C/D) + Resolution + Escalation Ceiling + Related Resources</name>

  <read_first>
    - docs/l2-runbooks/15-ios-ade-token-profile.md (current state from Task 1)
    - .planning/phases/31-ios-l2-investigation/31-RESEARCH.md §Pattern A-D Substance for Runbook 15 (lines 333-395 — full pattern content with indicators + resolutions)
    - .planning/phases/31-ios-l2-investigation/31-CONTEXT.md D-07 pattern list
    - docs/l2-runbooks/11-macos-profile-delivery.md lines 134-175 (Resolution Scenarios + Escalation Ceiling shape)
  </read_first>

  <files>docs/l2-runbooks/15-ios-ade-token-profile.md</files>

  <behavior>
    - Test A: `grep -cE "^### Pattern [ABCD]:" docs/l2-runbooks/15-ios-ade-token-profile.md` returns exactly 4 (V-31-06).
    - Test B: Each Pattern's body contains both "Indicator" and "Resolution" substrings (V-31-07).
    - Test C: Three top H2s present: `## Investigation — Data Collection`, `## Analysis — Match Against Known Failure Patterns`, `## Resolution` (V-31-08).
    - Test D: File length 187-322 lines (V-31-29 ±15% of 220-280 band).
  </behavior>

  <action>
    Append the following sections to `docs/l2-runbooks/15-ios-ade-token-profile.md` between Step 4 and Version History. Use Edit tool.

    **1. `## Analysis — Match Against Known Failure Patterns`** — H2 exact (V-31-08).

       For each Pattern, use this sub-template (MUST include both "Indicator" and "Resolution" words somewhere within the section body — V-31-07):

       `### Pattern A: Token expired or sync stuck`
       - **Indicators:** From RESEARCH §Pattern A — Intune shows token `Expired` OR last sync > 24h OR Graph `lastSyncErrorCode` != 0; new ABM-added devices not appearing in Intune after 24h; existing devices fail enrollment with "This device belongs to another organization" or "no enrollment profile found".
       - **Matches Data Collection:** Step 1 (sync timestamps), Step 1 Supplement (numeric error code).
       - **Resolution:** See `## Resolution` §Pattern A below.

       `### Pattern B: Profile never assigned to device in ABM`
       - **Indicators:** Device appears in Intune (ABM sync succeeded) but profile column shows "not assigned"; Setup Assistant skips MDM profile step.
       - **Matches Data Collection:** Step 2 (profile assignment state), ABM Devices view cross-check.
       - **Resolution:** See §Pattern B below.

       `### Pattern C: Profile assigned but device never received it (APNs / network path)`
       - **Indicators:** Profile IS assigned per Intune but device's Setup Assistant never shows the "Remote Management" pane; device can reach apple.com / icloud.com but not deviceenrollment.apple.com / mdmenrollment.apple.com / *.push.apple.com; sysdiagnose kernel.log shows connection failures to MDM enrollment endpoints.
       - **Matches Data Collection:** Step 4 (device-side state + Tier 3 sysdiagnose kernel.log).
       - **Resolution:** See §Pattern C below.

       `### Pattern D: Wrong MDM server on device (token rotation / tenant migration)`
       - **Indicators:** Device enrolled but receives no MDM commands; Settings > General > VPN & Device Management shows Management Profile from a DIFFERENT tenant URL OR a previous MDM provider; Graph `lastModifiedDateTime` < 30 days ago AND `appleIdentifier` differs from ticket-recorded prior state; ABM device record shows "Previously assigned to" with a different MDM server.
       - **Pattern A vs D disambiguation note** (from CONTEXT specifics): Pattern A = Intune-side token state issue (fix in Intune); Pattern D = device-side enrollment target mismatch (**requires device wipe** — cannot be fixed in Intune alone).
       - **Matches Data Collection:** Step 3 (GUID compare), Step 4 (on-device profile origin).
       - **Resolution:** See §Pattern D below.

    **2. `## Resolution`** — H2 exact (V-31-08). For each Pattern, give numbered steps derived from RESEARCH:

       `### Pattern A Resolution`
       1. If expired: re-download token from ABM (Settings > [your name] > Preferences > Your MDM Server > Download Token) and re-upload to Intune (Enrollment program tokens > [token] > Edit > upload `.p7m`).
       2. If sync stuck: click **Sync** on the token. Wait 30 min; re-check.
       3. If `syncedDeviceCount` drastically below ABM device count: the token may be orphaned (ABM reassigned to a different MDM server). Verify in ABM > Devices > [serial] > MDM server assignment.

       `### Pattern B Resolution`
       1. In ABM: Devices > [serial] > **Assign to MDM server** → confirm Intune MDM server. (Requires L2 ABM write access OR escalate to ABM admin.)
       2. In Intune: Enrollment Program Tokens > [token] > Profiles > [profile] > **Assign devices** → select serial.
       3. On device: user boots → reaches network → `iprofiles` triggers ADE check → downloads profile → continues Setup Assistant.

       `### Pattern C Resolution`
       1. Verify network path from device's enrollment location to Apple endpoints. Provide network/firewall team the Apple reference: `https://support.apple.com/HT210060` (Apple's MDM/ADE network requirements).
       2. Verify APNs cert state in Intune > Tenant administration > Connectors and tokens > Apple push notification certificate — not expired, not revoked, APNs Apple ID matches expected.
       3. Try enrollment from a known-good network (hotspot / home WiFi) to isolate corporate network.

       `### Pattern D Resolution` (requires device wipe — INLINE this warning)
       > **Destructive action:** Pattern D resolution requires a device wipe. Confirm with the device owner and org policy before proceeding.
       1. Confirm tenant migration history with admin team.
       2. Wipe device in old tenant (if accessible) OR physical factory reset.
       3. In ABM: confirm device is assigned to CURRENT Intune MDM server.
       4. Re-enroll via Setup Assistant.

    **3. `## Escalation Ceiling`** — Open a Microsoft Support case if:
       - Pattern A: `lastSyncErrorCode` != 0 after token renewal + resync; collect Graph GET response body + ABM token download timestamp.
       - Pattern B: step 1 completes but step 2 still shows "not assigned" after 30 min of sync.
       - Pattern C: network path confirmed open end-to-end AND APNs cert active AND device still never sees profile; collect sysdiagnose kernel.log excerpt + network-path evidence + APNs tenant ID.
       - Pattern D: device CANNOT be factory reset (user data constraint); escalate for "release device from MDM" ABM procedure.

       **Data to include in every support case:**
       - Device serial number + iOS version
       - Intune device status screenshot
       - Graph GET response body (if Pattern A)
       - ABM device record screenshot
       - Company Portal log incident ID (if collected per runbook 14 Tier 2)
       - Sysdiagnose `.tar.gz` (if Pattern C/D — PII-redacted per runbook 14 Tier 3 callout)

    **4. `## Related Resources`** — Bullets:
       - `[iOS Log Collection Guide](14-ios-log-collection.md)` — prerequisite
       - `[iOS ADE Lifecycle](../ios-lifecycle/01-ade-lifecycle.md)` — Stage 1-2 token-sync context
       - `[APNs Certificate Guide](../admin-setup-ios/01-apns-certificate.md)` — Pattern C reference
       - `[ABM/ADE Token Guide](../admin-setup-ios/02-abm-token.md)` — Pattern A reference
       - `[ADE Enrollment Profile Guide](../admin-setup-ios/03-ade-enrollment-profile.md)` — profile configuration reference
       - `[iOS L1 Runbooks 16-20](../l1-runbooks/)` — L1 escalation sources routing into this runbook

    After writing, run `node scripts/validation/check-phase-31.mjs --quick` and confirm V-31-06/07/08/09/10 all PASS. File length should be 187-322 lines.

    Commit: `docs(31): complete 15-ios-ade-token-profile.md — Pattern A/B/C/D analysis + Resolution + Escalation Ceiling`.
  </action>

  <verify>
    <automated>node scripts/validation/check-phase-31.mjs --quick 2>&1 | grep -E "V-31-(06|07|08|09|10|29)" && test $(wc -l < docs/l2-runbooks/15-ios-ade-token-profile.md) -ge 187</automated>
  </verify>

  <acceptance_criteria>
    - V-31-06 passes: exactly 4 `### Pattern [ABCD]:` headings
    - V-31-07 passes: each Pattern body contains both "Indicator" and "Resolution"
    - V-31-08 passes: 3 top-level H2s (Investigation — Data Collection / Analysis — Match... / Resolution) all present
    - V-31-29 passes: line count 187-322
    - `grep -q "Destructive action" docs/l2-runbooks/15-ios-ade-token-profile.md` (Pattern D wipe warning inline)
    - `grep -q "HT210060" docs/l2-runbooks/15-ios-ade-token-profile.md` (Apple ADE/MDM network reference cited)
  </acceptance_criteria>

  <done>
    Runbook 15 complete; V-31-06..V-31-10 + V-31-26/27/28/29 green; Pattern D wipe-warning inline; SC #3 literal satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| L2 engineer → Microsoft Graph API (depOnboardingSettings) | Tenant-wide read scope crosses trust boundary (V14 Config / T-31-03) |
| L2 engineer → Apple Business Manager | ABM admin actions (device reassign) cross org-portal boundary |
| Device → Apple MDM endpoints | Pattern C investigation confirms network-path boundary open/blocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Elevation of privilege | Graph `DeviceManagementServiceConfig.Read.All` scope | mitigate | Two inline callouts: (1) D-09 verbatim preamble names READ-ONLY + ADDTS-02 write-op boundary; (2) Prerequisites V4 Access-control note warns tenant-wide read + delegated-over-application preference (V-31-09 + V-31-10 grep gates) |
| T-31-D | Tampering | Pattern D wipe resolution | mitigate | Inline "Destructive action" blockquote warning before wipe steps |
| T-31-05 | Information disclosure | Sysdiagnose attached to MS Support ticket (Pattern C/D escalation) | transfer | References runbook 14's T-31-01 PII callout; executor must trust that runbook 14 mitigation is sufficient |
</threat_model>

<verification>
- V-31-06/07/08/09/10 + V-31-26/27/28/29 all green
- SC #3 literal satisfied: indicators (Steps 1-4) AND known failure patterns (A/B/C/D) with resolution steps
- Triple-portal prerequisite (D-10) enforced + V4 Access Control reminder inline
</verification>

<success_criteria>
Runbook 15 satisfies SC #3. L2 engineer investigating ADE token/profile failure has indicators to check + 4 known patterns + resolution steps + escalation criteria. T-31-03 mitigated via Graph preamble + Prerequisites access-control note.
</success_criteria>

<output>
After completion, create `.planning/phases/31-ios-l2-investigation/31-03-SUMMARY.md`.
</output>
