---
phase: 31-ios-l2-investigation
plan: 02
type: execute
wave: 1
depends_on: [31-01]
files_modified:
  - docs/l2-runbooks/14-ios-log-collection.md
autonomous: true
requirements: [L2TS-01]
tags: [ios, l2, log-collection, runbook, sc1, sc2]

must_haves:
  truths:
    - "An L2 engineer can locate the tool-landscape preamble and immediately understand no mdmdiagnosticstool.exe equivalent exists on iOS"
    - "An L2 engineer reads a 3-method decision matrix as the first structural element and can pick a method based on who triggers, data scope, L2 access path, and latency"
    - "An L2 engineer can trigger sysdiagnose on any modern iPhone or iPad using the unified trigger combo documented per D-30 research"
    - "An L2 engineer is warned (T-31-01) that sysdiagnose contains PII and must follow data-handling before ticket attachment"
    - "An L2 engineer understands (T-31-02) that Company Portal log uploads leave tenant control and route through Microsoft support infrastructure"
  artifacts:
    - path: "docs/l2-runbooks/14-ios-log-collection.md"
      provides: "iOS L2 log collection runbook (Tier 1 MDM + Tier 2 Company Portal + Tier 3 Mac+cable sysdiagnose)"
      contains: "## Section 3: Mac+Cable Sysdiagnose"
      min_lines: 136
  key_links:
    - from: "docs/l2-runbooks/14-ios-log-collection.md"
      to: "docs/ios-lifecycle/01-ade-lifecycle.md#section-3-mac-cable-sysdiagnose"
      via: "anchor link from Wave 5 retrofit of ios-lifecycle line 364"
      pattern: "Section 3: Mac\\+Cable Sysdiagnose"
---

<objective>
Author `docs/l2-runbooks/14-ios-log-collection.md` — the iOS L2 log collection runbook satisfying SC #1 (three methods with explicit data-type mapping) and SC #2 (explicit "no iOS equivalent to mdmdiagnosticstool.exe" preamble), following the Phase 24 macOS log-collection structural analog with the iOS-specific Tier 1/2/3 restructure per D-01/D-04.

Purpose: This runbook is the **prerequisite** for runbooks 15/16/17 (each opens with "Before starting: collect a diagnostic package per iOS Log Collection Guide"). Its section 3 heading `## Section 3: Mac+Cable Sysdiagnose` is the anchor target of Wave 5's `ios-lifecycle/01-ade-lifecycle.md` line 364 retrofit.

Output: One new file at `docs/l2-runbooks/14-ios-log-collection.md` (~160-180 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ios-l2-investigation/31-CONTEXT.md
@.planning/phases/31-ios-l2-investigation/31-RESEARCH.md
@.planning/phases/31-ios-l2-investigation/31-PATTERNS.md
@docs/l2-runbooks/10-macos-log-collection.md
@docs/l2-runbooks/00-index.md
@docs/ios-lifecycle/01-ade-lifecycle.md

<interfaces>
<!-- Platform gate banner (D-29, verbatim — appears in all 4 iOS L2 runbooks) -->
```markdown
> **Platform gate:** This guide covers iOS/iPadOS L2 investigation via Intune. For Windows Autopilot, see [Windows L2 Runbooks](00-index.md). For macOS ADE, see [macOS ADE Runbooks](00-index.md#macos-ade-runbooks).
```

<!-- D-02 Tool Landscape preamble (verbatim from CONTEXT.md) -->
```markdown
> **Tool landscape:** There is **no iOS equivalent to `mdmdiagnosticstool.exe`**. iOS diagnostic data is fragmented across three methods — MDM diagnostic report, Company Portal upload, and Mac+cable sysdiagnose. Each yields different data types; the decision matrix below guides method selection.
```

<!-- D-28 frontmatter shape for file 14 -->
```yaml
---
last_verified: 2026-04-17
review_by: 2026-07-16
applies_to: all
audience: L2
platform: iOS
---
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author runbook 14 — frontmatter + Context + Tool Landscape + Decision Matrix + Tier 1 (MDM diagnostic report)</name>

  <read_first>
    - docs/l2-runbooks/10-macos-log-collection.md (full file — structural analog; inherit frontmatter/banner shape)
    - .planning/phases/31-ios-l2-investigation/31-RESEARCH.md §D-31 (MDM diagnostic report correction — Intune "Collect diagnostics" is MAM-scoped; on-device pull is via Settings > General > VPN & Device Management)
    - .planning/phases/31-ios-l2-investigation/31-CONTEXT.md D-01, D-02, D-03, D-04 (tier flow, preamble, matrix, section order)
    - docs/ios-lifecycle/01-ade-lifecycle.md lines 355-373 (authoritative project text for on-device MDM diagnostic retrieval path)
  </read_first>

  <files>docs/l2-runbooks/14-ios-log-collection.md</files>

  <behavior>
    - Test A: File exists with valid frontmatter (platform: iOS, audience: L2, applies_to: all, last_verified/review_by ISO dates).
    - Test B: D-29 platform gate banner appears within first 30 lines.
    - Test C: Verbatim SC #2 preamble grep `grep -q "no iOS equivalent to .*mdmdiagnosticstool\.exe" docs/l2-runbooks/14-ios-log-collection.md` passes.
    - Test D: Decision matrix has `| Method |` header AND contains "MDM diagnostic report" AND "Company Portal" AND "sysdiagnose" substrings.
    - Test E: Tier 1 section exists and addresses the D-31 correction — distinguishes on-device MDM diagnostic (Settings > General > VPN...) from the MAM-scoped Intune "Collect diagnostics" remote action with its 30-min latency + 4MB/50-diagnostic cap.
  </behavior>

  <action>
    Create `docs/l2-runbooks/14-ios-log-collection.md` with the following structure (use exact headings — executor authors the prose within each heading per research). Target length ~75-90 lines for this task's content (Tier 2/3 come in Task 2).

    **1. Frontmatter (lines 1-7)** — verbatim:
    ```
    ---
    last_verified: 2026-04-17
    review_by: 2026-07-16
    applies_to: all
    audience: L2
    platform: iOS
    ---
    ```

    **2. Platform gate banner (line 9)** — verbatim D-29 text from `<interfaces>` above.

    **3. Title** (line 11): `# iOS Log Collection Guide`

    **4. `## Context` (line 13)** — explain scope: L2 investigation prerequisite; covers three collection methods; routes to specific investigation runbooks.

    **5. `## Tool Landscape`** — insert the verbatim D-02 preamble blockquote from `<interfaces>` above.

    **6. `## Decision Matrix`** — create a markdown table with header `| Method | Who Triggers | Data Scope | L2 Access Path | Physical Requirements | Typical Latency |` and 3 data rows (per D-03 from CONTEXT; latencies from RESEARCH D-31/D-32):
       - Row 1: MDM diagnostic report / User (on-device) OR L2 (MAM-scoped admin action) / on-device profile state (per 01-ade-lifecycle.md line 363) OR MAM-scoped Intune collection / Intune admin center > Troubleshooting + support > Troubleshoot > Diagnostics tab (MAM only) OR user screenshots device surface / User's iOS device (no Mac) / ~30 min (MAM) or immediate (on-device screenshot)
       - Row 2: Company Portal log upload / User / Company Portal + Authenticator logs / **Microsoft Support ticket (not self-service)** / iOS device only / **1-3 business days (ticket roundtrip)**
       - Row 3: Mac+cable sysdiagnose / User triggers on device; L2 receives via AirDrop / Full unified log + kernel log + `.tar.gz` bundle / Mac receives bundle over AirDrop; opens in Console.app for live streaming / iOS device + Mac + USB-C/Lightning cable / ~10 min (trigger) + transfer time

    **7. `## Section 1: MDM Diagnostic Report` (Tier 1)** — MUST reflect the D-31 research correction. Use two sub-headings:
       - `### 1a. On-Device Profile State (user-coordinated; always available)` — path: Settings > General > VPN & Device Management > Management Profile > More Details. User screenshots or narrates. No admin role needed on L2 side. Cite cross-reference: `[as documented in iOS ADE Lifecycle §Post-enrollment diagnostics](../ios-lifecycle/01-ade-lifecycle.md#stage-7-post-enrollment)`.
       - `### 1b. MAM App Protection Diagnostics (Intune admin center; MAM scope only)` — explicit note: "The Intune 'Collect diagnostics' remote action for iOS/iPadOS is scoped to App Protection (MAM) logs ONLY — it does NOT produce a general MDM enrollment/config bundle. Supported apps: Outlook, Teams, OneDrive, Edge, Word, Excel, PowerPoint, OneNote, Microsoft 365." Then document the breadcrumb: `Intune admin center > Troubleshooting + support > Troubleshoot > [user] > App Protection > Checked-in > [app] > "..." > Collect diagnostics`. Download path: `Troubleshoot > Summary > Diagnostics tab`. State the **4 MB / 50-diagnostic cap** — uploads exceeding this cap require Microsoft Intune Support. State the ~30-min delivery latency. State the required role: `Help Desk Operator` OR `School Administrator` OR custom role with `Remote tasks/Collect diagnostics` permission.

    **8. `## Version History`** (placeholder block; Task 2 will finalize):
    ```
    ## Version History

    | Date | Change | Author |
    |------|--------|--------|
    | 2026-04-17 | Initial version — iOS L2 log collection runbook | -- |
    ```

    Commit message: `docs(31): create iOS L2 log collection runbook — frontmatter + Context + Tool Landscape + Decision Matrix + Tier 1`.

    Run the harness quick check: `node scripts/validation/check-phase-31.mjs --quick`. Expect V-31-01 (SC #2 preamble), V-31-02 (3 methods named in matrix), V-31-26 (frontmatter), V-31-27 (applies_to: all), V-31-28 (banner) to PASS after this commit. V-31-03/04/05 and V-31-29 may still FAIL (Task 2 delivers those).
  </action>

  <verify>
    <automated>node scripts/validation/check-phase-31.mjs --quick 2>&1 | grep -E "V-31-(01|02|26|27|28)"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f docs/l2-runbooks/14-ios-log-collection.md`
    - V-31-01 passes: `grep -q "no iOS equivalent to .*mdmdiagnosticstool\\.exe" docs/l2-runbooks/14-ios-log-collection.md`
    - V-31-02 passes: decision matrix with Method header + 3 method names
    - V-31-26 passes: frontmatter fields all present
    - V-31-27 passes: `applies_to: all`
    - V-31-28 passes: platform gate banner in first 30 lines
    - `grep -q "Settings > General > VPN & Device Management" docs/l2-runbooks/14-ios-log-collection.md` (on-device path documented per D-31 correction)
    - `grep -q "4 MB" docs/l2-runbooks/14-ios-log-collection.md` AND `grep -q "50 diagnostics" docs/l2-runbooks/14-ios-log-collection.md` (cap documented)
    - `grep -q "Help Desk Operator" docs/l2-runbooks/14-ios-log-collection.md` (RBAC documented — V4 Access Control per T-31)
  </acceptance_criteria>

  <done>
    File exists; frontmatter + banner + preamble + decision matrix + Tier 1 sections written; V-31-01/02/26/27/28 green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete runbook 14 — Tier 2 Company Portal (with T-31-02 data-egress callout) + Tier 3 Sysdiagnose (with T-31-01 PII callout) + Common Artifacts + Related Resources</name>

  <read_first>
    - docs/l2-runbooks/14-ios-log-collection.md (current state from Task 1)
    - .planning/phases/31-ios-l2-investigation/31-RESEARCH.md §D-30 (sysdiagnose unified trigger + per-device details + AirDrop-to-Mac flow)
    - .planning/phases/31-ios-l2-investigation/31-RESEARCH.md §D-32 (Company Portal send logs UI + ticket-based retrieval)
    - .planning/phases/31-ios-l2-investigation/31-VALIDATION.md (V-31-03, V-31-04, V-31-05 requirements)
    - docs/l2-runbooks/10-macos-log-collection.md lines 128-155 (Section 3 structural analog — inherit shape)
  </read_first>

  <files>docs/l2-runbooks/14-ios-log-collection.md</files>

  <behavior>
    - Test A: `grep -qE "(both volume|volume.*(side|top) button)" docs/l2-runbooks/14-ios-log-collection.md` (sysdiagnose trigger present — V-31-03).
    - Test B: `grep -qiE "(PII|private data|redact|data-handling)" docs/l2-runbooks/14-ios-log-collection.md` (T-31-01 PII callout — V-31-04).
    - Test C: `grep -qE "(Microsoft (backend|support infrastructure)|leaves tenant)" docs/l2-runbooks/14-ios-log-collection.md` (T-31-02 data-egress callout — V-31-05).
    - Test D: File has heading `## Section 3: Mac+Cable Sysdiagnose` exactly (anchor target for Wave 5 lifecycle retrofit).
    - Test E: File length is 136-207 lines (V-31-29 ±15% of 160-180 target band).
  </behavior>

  <action>
    Extend `docs/l2-runbooks/14-ios-log-collection.md` by appending the following sections between Tier 1 and Version History. Use Edit tool; keep Task 1's content intact.

    **1. `## Section 2: Company Portal Log Upload` (Tier 2)** — sub-headings:
       - `### How to Trigger` — three paths per RESEARCH D-32: (1) error-driven (tap **Report** on any error dialog), (2) menu-driven (**More** tab > **Send Logs**), (3) shake gesture (Shake device > **Send Diagnostic Report**; enable via Settings > Apps > Company Portal > Shake Gesture if not prompted).
       - `### Full Upload Flow` — numbered 6-step list: (1) Open Company Portal. (2) Reproduce the event. (3) Initiate upload. (4) Tap Open Authenticator for combined upload. (5) Save the **incident ID**. (6) Tap Email Logs.
       - `### Verbose Logging Preflight` — tell L2 to enable verbose logging at Company Portal > More > Settings BEFORE reproducing to capture extra detail.
       - `### L2 Retrieval (ticket-based)` — INLINE THIS VERBATIM BLOCKQUOTE (T-31-02 mitigation):
         ```
         > **Data egress:** The iOS Company Portal log upload leaves tenant control. Logs are routed to Microsoft support infrastructure and are NOT directly downloadable from the Intune admin center. An L2 engineer with an incident ID MUST open a Microsoft Intune Support ticket and provide the incident ID — Microsoft support retrieves and returns the logs (or analysis) on the L2's behalf. Typical roundtrip: 1-3 business days. This is the signal to escalate to Tier 3 (Mac+cable sysdiagnose) when time is critical.
         ```

    **2. `## Section 3: Mac+Cable Sysdiagnose` (Tier 3)** — **heading text MUST match exactly** (anchor `#section-3-mac-cable-sysdiagnose`; Wave 5 retrofit of `ios-lifecycle/01-ade-lifecycle.md` line 364 depends on this). Sub-headings:
       - `### Prerequisites` — Mac + cable (USB-C for iPhone 15+ / iPad Pro 3+; Lightning for iPhone 5-14 and earlier iPads — cite RESEARCH D-30 table) + Console.app on the Mac.
       - `### Trigger on Device (Unified Modern iOS)` — INLINE: "Press and release **both volume buttons + side button** (iPhone) or **both volume buttons + top button** (iPad) simultaneously for **1 to 1.5 seconds**. Device vibrates briefly (iPhone only; iPad is silent). Collection completes in approximately 10 minutes and runs in the background." Add the critical warning: "Holding too long triggers the Power Off / Emergency SOS screen instead — dismiss and retry with shorter hold."
       - `### Per-Device Trigger Reference (iOS 15+)` — markdown table with 4 rows:
         | Device class | Trigger |
         | iPhone 8 / iPhone SE (Touch ID, Home button) | Both volume + side/sleep-wake |
         | iPhone X and later (Face ID) | Both volume + side button |
         | iPad with Home button (Touch ID) | Both volume + top/sleep-wake |
         | iPad with Face ID | Both volume + top button |
       - `### Artifact Location and Retrieval` — on-device path: `Settings > Privacy & Security > Analytics & Improvements > Analytics Data`; filename pattern `sysdiagnose_YYYY.MM.DD_HH-MM-SS±ZZZZ_iPhoneOS_iPhone_XX.X.X_XXXXXX.tar.gz`. Retrieval: AirDrop to the L2's Mac from the Analytics Data share sheet. Fallback if AirDrop unavailable: Apple Configurator 2 on the Mac browses device filesystem.
       - `### Console.app Live Streaming (during failure reproduction)` — Connect device; Trust the Mac prompt; Open Console.app > Devices sidebar > select iOS device; click to stream unified log during reproduction. Sysdiagnose capture itself is triggered ON THE DEVICE via the button combo above — Console.app is for streaming, not remote trigger.
       - `### Cable Type by Device` — inline 2 bullets: Lightning for iPhone 5-14 and iPad 7-9 / Air 1-4 / mini 1-5; USB-C for iPhone 15+ and iPad Pro 3+ / Air 4+ / 10th gen+ / mini 6+.
       - INLINE THIS VERBATIM BLOCKQUOTE (T-31-01 mitigation):
         ```
         > **PII warning:** Sysdiagnose bundles contain private data — device identifiers, installed app inventory, network history, location hints, user account identifiers. Before attaching to a Microsoft Support ticket, L2 MUST follow the organization's data-handling policy: redact or segregate PII per tenant policy. Sysdiagnose is NOT safe to upload to public issue trackers.
         ```

    **3. `## Common Artifacts Cross-Reference`** — 1 row per artifact showing "where collected / which investigation runbook uses it":
       | Artifact | Collection Method | Used by |
       | On-device MDM profile state (screenshot) | Tier 1a | 15-ios-ade-token-profile.md Step 4, 17-ios-compliance-ca-timing.md "Per-Cause Deep-Dive" |
       | Company Portal + Authenticator logs | Tier 2 | Escalation to Microsoft Support tickets |
       | MAM App Protection diagnostics zip | Tier 1b | MAM advisory (see `00-index.md#mam-we-investigation-advisory`) — out of Phase 31 scope |
       | Sysdiagnose `.tar.gz` (kernel.log, unified log) | Tier 3 | 15 Pattern C (APNs network path), 17 "Not evaluated" terminal state |

    **4. `## Related Resources`** — bullets:
       - `[ADE Token & Profile Delivery Investigation](15-ios-ade-token-profile.md)` — next step after Tier 1/2/3 collection for enrollment failures
       - `[App Install Failure Diagnosis](16-ios-app-install.md)` — next step for VPP/LOB/supervision issues
       - `[Compliance & CA Timing Investigation](17-ios-compliance-ca-timing.md)` — next step for compliance state issues
       - `[iOS ADE Lifecycle §Post-enrollment diagnostics](../ios-lifecycle/01-ade-lifecycle.md)` — on-device MDM diagnostic retrieval authoritative reference
       - `[iOS L2 Runbooks index](00-index.md#ios-l2-runbooks)` — hub

    **5. Ensure `## Version History` ends the file** (Task 1 placeholder; keep as-is).

    After writing, run `node scripts/validation/check-phase-31.mjs --quick` and confirm V-31-01 through V-31-05 + V-31-26/27/28 all PASS. File length should be 136-207 lines (V-31-29).

    Commit: `docs(31): complete iOS L2 log collection runbook — Tier 2 + Tier 3 + artifacts + PII/egress callouts`.
  </action>

  <verify>
    <automated>node scripts/validation/check-phase-31.mjs --quick 2>&1 | grep -E "V-31-(01|02|03|04|05|26|27|28|29)" && test $(wc -l < docs/l2-runbooks/14-ios-log-collection.md) -ge 136</automated>
  </verify>

  <acceptance_criteria>
    - V-31-03 passes: `grep -qE "(both volume|volume.*(side|top)|Sleep/Wake)" docs/l2-runbooks/14-ios-log-collection.md`
    - V-31-04 passes: `grep -qiE "(PII|private data|redact|data-handling)" docs/l2-runbooks/14-ios-log-collection.md`
    - V-31-05 passes: `grep -qE "(Microsoft (backend|support infrastructure)|leaves tenant)" docs/l2-runbooks/14-ios-log-collection.md`
    - `grep -q "^## Section 3: Mac+Cable Sysdiagnose$" docs/l2-runbooks/14-ios-log-collection.md` (exact heading for anchor target)
    - V-31-29 passes: `wc -l` returns a value in the range 136-207
    - All V-31-01..V-31-05 + V-31-26/27/28 show PASS in harness output
  </acceptance_criteria>

  <done>
    Runbook 14 is complete; all V-31-01..V-31-05 + V-31-26/27/28 green; Section 3 heading matches anchor expected by Wave 5 retrofit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS device → Microsoft backend | Company Portal uploads cross tenant boundary (T-31-02) |
| Sysdiagnose file → Microsoft Support ticket | PII leaves the organization via ticket attachment (T-31-01) |
| L2 engineer → Intune admin center | MAM Collect Diagnostics action requires specific RBAC (V4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information disclosure | Sysdiagnose attachment to MS Support ticket | mitigate | Section 3 PII callout blockquote (V-31-04 grep gate) reminding L2 to redact per org data-handling before attachment |
| T-31-02 | Information disclosure | Company Portal log upload routing | mitigate | Tier 2 L2 Retrieval blockquote (V-31-05 grep gate) naming Microsoft support infrastructure as destination and "leaves tenant control" framing |
| T-31-04 | Elevation of privilege | MAM Collect diagnostics RBAC | mitigate | Tier 1b documents required role (Help Desk Operator / School Administrator / custom role with `Remote tasks/Collect diagnostics`) — prevents L2 requesting scope they don't need |
</threat_model>

<verification>
- `node scripts/validation/check-phase-31.mjs --quick` passes V-31-01..V-31-05 + V-31-26/27/28 + V-31-29
- Section 3 heading is exact match for Wave 5 lifecycle retrofit anchor
- File within line-count bounds (136-207)
</verification>

<success_criteria>
SC #1 satisfied (3-method decision matrix + method-to-data mapping). SC #2 satisfied (no-mdmdiagnosticstool.exe preamble present and prominent). T-31-01 + T-31-02 mitigations inline in Sections 3 and 2 respectively. All harness checks for file 14 green.
</success_criteria>

<output>
After completion, create `.planning/phases/31-ios-l2-investigation/31-02-SUMMARY.md`.
</output>
