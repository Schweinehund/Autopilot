---
phase: 151-recipe-5-the-enterprise-update-plan
plan: 02
type: execute
wave: 2
depends_on: ["151-01"]
files_modified:
  - docs/recipes/05-enterprise-update-plan.md
autonomous: true
requirements: [RCP-01, RCP-02, RCP-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Step 2 (hotpatch posture) exists as a Case 2 block with the anchor decision-hotpatch-posture, a single-line Ask-the-admin lead-in, the marker line '**Applies to:** Windows-only · **Reversibility:** Reversible', a blank line, and a three-column Option/When to choose/Recorded as table carrying exactly three option rows (D-09, D-10, D-16, D-17, D-27)."
    - "Step 2 is written on the CORRECTED hotpatch position: hotpatch security updates are ENABLED BY DEFAULT for eligible devices, Arm64 is supported once CHPE is disabled rather than the feature being x64-only, VBS is required, the edition constraint is Windows 11 Enterprise, and ineligible devices silently receive the LCU instead (RCP-02)."
    - "Step 2 states the two-level precedence rule explicitly: the tenant-level toggle applies only to devices NOT in a Windows quality update policy, which is why per-cohort override is a real third branch and not a restatement of the second (D-27)."
    - "Step 2 has NO branch bodies — it is Case 2, not Case 1, and STD-05 D-04 rule 3's Boolean exemption does not apply to it because it has three branches plus a precedence rule (D-27, D-28)."
    - "Step 3 (driver approval mode) exists as a Case 1 block with the anchor decision-driver-approval-mode, the marker line '**Applies to:** Windows-only · **Reversibility:** Destructive', and a four-column Option/When to choose/Consequence if wrong/Branch table carrying exactly two option rows (D-09, D-14, D-27)."
    - "Step 3's Consequence-if-wrong content states in the recipe itself, not only in a linked guide, that switching approval mode destroys all existing approvals, pauses and declines (RCP-02, D-14, D-57)."
    - "Step 3 carries two sibling H3 branch bodies '### Step 3a:' and '### Step 3b:', each a prose step sequence with a full Intune click-path and a one-line routing sentence forward to Step 4 (D-28, D-29, D-31, D-32)."
    - "'Destructive' appears exactly once in a Reversibility field across the whole recipe after this plan, and no second occurrence is introduced (D-12, D-13, D-14)."
    - "Each of Steps 2 and 3 carries exactly one What-breaks-if-misconfigured blockquote, and no contiguous top-level blockquote run in the file exceeds 200 characters (D-18, D-19, D-20)."
    - "Every substantive claim in Steps 2 and 3 carries a per-claim Source evidence line covering one page only, in the milestone's FIX-10 convention (D-58)."
    - "Every outbound link added by this plan resolves to an id on the verified anchor roster in 151-01-PLAN.md, using a full relative path with no numeric-prefix shorthand (D-62, D-65)."
    - "docs/recipes/05-enterprise-update-plan.md is still UNCOMMITTED when this plan finishes; no commit is made by this plan (D-52)."
    - statement: "After this plan the file's H2 count is still exactly 8 in the locked order, and its Step-heading count has grown from 3 to 8 (Steps 1, 1a, 1b, 2, 3, 3a, 3b plus no others)."
      verification: backstop
  artifacts:
    - docs/recipes/05-enterprise-update-plan.md (expanded — Steps 2 and 3 plus Step 3's two branch bodies)
  key_links:
    - "Step 2 -> ../operations/patch-management/01-windows-wufb-rings.md#hotpatch (hotpatch enabled by default and its absent automatic rollback, D-62)"
    - "Step 2 -> ../operations/patch-management/07-windows-autopatch.md#autopatch-hotpatch-licensing (the entitlement that is necessary but not sufficient)"
    - "Step 3 -> ../operations/patch-management/06-windows-driver-firmware-updates.md#approval-modes"
    - "Step 3a/3b -> ../operations/patch-management/06-windows-driver-firmware-updates.md#approval-workflow and #deferral-deadline-behavior"
  prohibitions:
    - statement: "No commit is made by this plan; the recipe stays in the working tree for Plan 05's Commit B."
      status: flagged-unverified
      verification: manual
    - statement: "No file other than docs/recipes/05-enterprise-update-plan.md is modified."
      status: flagged-unverified
      verification: manual
    - statement: "Step 2 is not written as a Boolean decision and does not use the STD-05 D-04 rule 3 if/then prose-pair exemption."
      status: flagged-unverified
      verification: manual
    - statement: "The falsified hotpatch framing — opt-in, x64-only, or gated on 24H2 plus x64 plus VBS as a three-part test — is not written anywhere."
      status: flagged-unverified
      verification: manual
    - statement: "None of the four C11 ops anti-pattern literals is written."
      status: flagged-unverified
      verification: manual
    - statement: "No visible D-NN label appears in any Step heading."
      status: flagged-unverified
      verification: manual
    - statement: "No code fence is added to the recipe."
      status: flagged-unverified
      verification: manual
    - statement: "No operations guide is edited; linking into one is not editing it."
      status: flagged-unverified
      verification: manual
---

<objective>
Author the two Windows-quality-update decisions that sit between topology and firmware: hotpatch
posture, and driver approval mode — the latter carrying RCP-02's single **Destructive** rating.

Purpose: these two are where the milestone's two most consequential corrections land next to its
most dangerous switch. RCP-02 grades the hotpatch decision on the corrected position (enabled by
default, not opt-in; not x64-only; VBS required), and D-14 assigns `Destructive` to driver approval
mode because switching it destroys every approval, pause and decline already recorded. Both must be
restated in the recipe itself, not deferred to a link, because a reader who follows only the link
gets the plan wrong.

Output: `docs/recipes/05-enterprise-update-plan.md` expanded with Steps 2 and 3 and Step 3's two
sibling branch bodies. **Left uncommitted.**

**Decisions this plan implements:** D-09 (no new columns), D-10 (the marker line), D-11
(recommended-branch ratings), D-12 (nine ratings total), D-13 (the closed enum), D-14 (`Destructive`
is Step 3), D-16 (marker vocabulary), D-17 (both Steps are Windows-only), D-18 (single-line lead-ins),
D-19 (one measured breaks-callout per Step), D-23 (anchor-only internal references), D-27 (Step 2 is
Case 2 with three branches and a precedence rule; Step 3 is Case 1), D-28 (sibling H3 branches),
D-29 (Step 3's two branch bodies), D-31 (reconvergence sentences), D-32 (full click-paths), D-52 (no
commit), D-57 (restate-and-link), D-58 (evidence lines), D-62 (the wufb-rings guide sources hotpatch),
D-65 (full relative paths), D-76 (C11 defense), D-77 (run the gates by hand).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-01-PLAN.md
@.planning/phases/151-recipe-5-the-enterprise-update-plan/151-01-SUMMARY.md
@.planning/research/FEATURES.md
</context>

## Phase-wide rules

**Read the `## Phase-wide rules` section of `151-01-PLAN.md` before executing this plan.** It carries
the commit contract, the measured gate baselines, the fourteen verified gate facts, the locked H2
skeleton, the ten-Step property table, the decision-block shapes, the verified anchor roster and the
barred-prose list. Nothing in it is restated here.

<!-- planner-discipline-allow: x64-only -->
<!-- planner-discipline-allow: opt-in -->
<!-- planner-discipline-allow: mutually exclusive -->
<!-- planner-discipline-allow: Autopatch rings -->
<!-- planner-discipline-allow: SCCM -->
<!-- planner-discipline-allow: System Center -->
<!-- planner-discipline-allow: Reversibility: -->

The two rules that bite hardest in this plan:

- **No commit (D-52).** This plan appends to an uncommitted file and leaves it uncommitted. Do not
  fall back to a per-task commit default.
- **The intermediate red state is expected and enumerated.** `check-nav-hub-links.mjs` may fail only
  on fragments inside `docs/recipes/05-enterprise-update-plan.md` that Plans 03, 04 and 05 still
  create. Any failure naming a target outside that file is a real defect introduced by this plan.

## Artifacts this phase produces

See the `## Artifacts this phase produces` section of `151-01-PLAN.md`. The symbols this plan adds
from that list are the anchors `decision-hotpatch-posture` and `decision-driver-approval-mode`, and
the H3 headings `### Step 2:`, `### Step 3:`, `### Step 3a:` and `### Step 3b:`.

<tasks>

<task type="auto">
  <name>Task 1: Author Step 2 — hotpatch posture, on the corrected position</name>
  <files>docs/recipes/05-enterprise-update-plan.md</files>
  <read_first>
    - `docs/recipes/05-enterprise-update-plan.md` — read the whole file as Plan 01 left it. Step 2
      appends after Step 1b and must match Step 1's block anatomy exactly: anchor, single-line
      lead-in, marker line, blank line, table, click-path, breaks-callout, evidence lines.
    - `docs/operations/patch-management/01-windows-wufb-rings.md` — read the `hotpatch` anchored
      section in full. This is the guide that sources hotpatch being enabled by default and hotpatch's
      absent automatic rollback, and it was omitted from the draft's roster entirely (D-62).
      **Read-only.**
    - `docs/operations/patch-management/07-windows-autopatch.md` — read the
      `autopatch-hotpatch-licensing` anchored section in full: the Autopatch entitlement is necessary
      but **not sufficient** for hotpatch, because the two licence lists differ. **Read-only.**
    - `.planning/research/FEATURES.md` section 5.2 row **D-2** — the rewritten block, including the
      verbatim first-party statement that hotpatch security updates are enabled by default for all
      eligible devices in Microsoft Intune, the CHPE / `HotPatchRestrictions=1` Arm64 correction, the
      VBS requirement, the Windows 11 Enterprise edition constraint (Windows 365 Enterprise
      qualifies), the silent-LCU fallback for ineligible devices, and the two-level precedence rule.
    - `.planning/research/FEATURES.md` section 5.3 the **Hotpatch update** row — automatic rollback is
      not supported but hotpatches can be uninstalled, and uninstalling requires a device restart.
      Step 2's breaks-callout and consequence text reference this; Plan 05 authors its
      `## Rollback/Recovery` entry.
    - `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md` — D-27 (why Step 2 is
      not Boolean and not Case 1) and D-57 (restate-and-link).
  </read_first>
  <action>
Append `### Step 2:` to `## Steps`, immediately after `### Step 1b:`, at the Case 2 shape locked in
`151-01-PLAN.md`'s Phase-wide rules.

Structure, in order:

- The hand-authored anchor `decision-hotpatch-posture`, bare and self-closing, with **no visible D-NN
  label** in the heading (D-21, D-23).
- A one-sentence `> **Ask the admin:**` blockquote lead-in carrying the prompt only, blank-line
  separated from any other blockquote (D-18).
- The marker line, on its own non-blockquote bold line, byte-exact:
  `**Applies to:** Windows-only · **Reversibility:** Reversible`
  using U+00B7 as the separator (D-10, D-16, D-17).
- A mandatory blank line (STD-05 D-02).
- The Case 2 table `| Option | When to choose | Recorded as |` with **exactly three** option rows —
  leave the tenant default in place; block hotpatch at the tenant level; override per-cohort in a
  Windows quality update policy. Do **not** add a fourth column and do **not** use the Case 1
  four-column shape: STD-05 D-01 enumerates both column sets and D-04 rule 1 makes using the D-01
  composite shape one of only three normative rules (D-09).

Content requirements — this Step is graded by RCP-02 on the **corrected** position, so every one of
these must be present in the recipe's own prose, not only behind a link (D-57):

- Hotpatch security updates are **enabled by default** for all eligible devices. The decision is
  therefore whether to **leave it on**, not whether to turn it on. Do not write the falsified opt-in
  framing anywhere.
- Arm64 **is** supported once CHPE is disabled — a one-time change requiring a restart. The feature is
  **not** x64-only. Do not write the falsified x64-only framing anywhere.
- What remains true and must be stated: **VBS is required**; the edition constraint is Windows 11
  **Enterprise**, with Windows 365 Enterprise qualifying; and **ineligible devices silently receive
  the LCU instead**, which does require a restart — that silent fallback is the reason an admin who
  believes the fleet is hotpatching may be wrong about part of it.
- The **two-level precedence rule**: the tenant-level toggle applies only to devices **not** in a
  Windows quality update policy. This is what makes the third branch (per-cohort override) a genuinely
  different choice rather than a restatement of the second, and it is why D-27 rules this decision is
  **not** Boolean and may not use STD-05 D-04 rule 3's if/then prose-pair exemption.
- The Autopatch entitlement is **necessary but not sufficient** for hotpatch, because the two licence
  lists differ — link `../operations/patch-management/07-windows-autopatch.md#autopatch-hotpatch-licensing`
  for the full treatment rather than re-authoring it.

Then the full Intune click-path for setting the tenant-level posture and for the per-cohort override,
from the admin center down to the blade (D-32) — the guides carry blade locations, not numbered
procedures, so this recipe is the only place the navigation path exists.

Close Step 2 with exactly one `> **What breaks if misconfigured:**` blockquote. **Measure it**: the
contiguous top-level run must be under 200 characters. Name what the admin will see and what the end
user will see, and add a `See:` line linking the guide that covers the failure — this domain has no L1
or L2 runbook to link (D-19, D-38).

Every substantive claim carries a per-claim `**Source:**` evidence line immediately after the material
it evidences, covering one page only (D-58).

Step 2 has **no branch bodies** — it is Case 2. Do not add `### Step 2a` or `### Step 2b`.

**Do not commit.**
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&amp;1 | tail -2; node scripts/validation/v1.20-milestone-audit.mjs 2>&amp;1 | tail -2; node scripts/validation/check-nav-hub-links.mjs 2>&amp;1 | tail -4; grep -c '^### Step ' docs/recipes/05-enterprise-update-plan.md; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<a id="decision-hotpatch-posture"' docs/recipes/05-enterprise-update-plan.md` returns
      `1`.
    - `grep -c '^\*\*Applies to:\*\* Windows-only · \*\*Reversibility:\*\* Reversible$' docs/recipes/05-enterprise-update-plan.md`
      returns `1` — anchored to end-of-line so it cannot match the longer `Reversible — disruptive`
      value on Step 1.
    - `grep -c '^### Step 2[ab]:' docs/recipes/05-enterprise-update-plan.md` returns `0` — Case 2 has
      no branch bodies.
    - The Step 2 table is the three-column Case 2 shape with exactly three option rows:
      `sed -n '/<a id="decision-hotpatch-posture"/,/^### Step 3/p' docs/recipes/05-enterprise-update-plan.md | grep -c '^| '`
      returns `5` (one header row, one separator row, three option rows).
    - `sed -n '/<a id="decision-hotpatch-posture"/,/^### Step 3/p' docs/recipes/05-enterprise-update-plan.md | grep -c 'Consequence if wrong'`
      returns `0` — the Case 1 column set was not used (D-09).
    - `grep -ci 'enabled by default' docs/recipes/05-enterprise-update-plan.md` returns at least `1`.
    - `grep -ci 'x64-only' docs/recipes/05-enterprise-update-plan.md` returns `0` and
      `grep -ci 'opt-in' docs/recipes/05-enterprise-update-plan.md` returns `0` — neither falsified
      framing is present.
    - `grep -c 'CHPE' docs/recipes/05-enterprise-update-plan.md` returns at least `1` and
      `grep -c 'VBS' docs/recipes/05-enterprise-update-plan.md` returns at least `1`.
    - `sed -n '/<a id="decision-hotpatch-posture"/,/^### Step 3/p' docs/recipes/05-enterprise-update-plan.md | grep -c 'quality update policy'`
      returns at least `1` — the two-level precedence rule is stated.
    - `grep -c 'autopatch-hotpatch-licensing' docs/recipes/05-enterprise-update-plan.md` returns at
      least `1` and `grep -c '01-windows-wufb-rings.md#hotpatch' docs/recipes/05-enterprise-update-plan.md`
      returns at least `1` (D-62).
    - `grep -c '> \*\*Ask the admin:\*\*' docs/recipes/05-enterprise-update-plan.md` returns `2` and
      `grep -c '> \*\*What breaks if misconfigured:\*\*' docs/recipes/05-enterprise-update-plan.md`
      returns `2`.
    - No contiguous top-level blockquote run exceeds 200 characters:
      `node -e "const fs=require('fs');const L=fs.readFileSync('docs/recipes/05-enterprise-update-plan.md','utf8').replace(/\r\n/g,'\n').split('\n');let i=0,max=0;while(i<L.length){if(/^>/.test(L[i])){const b=[];while(i<L.length&&/^>/.test(L[i])){b.push(L[i].replace(/^>\s?/,''));i++;}max=Math.max(max,b.join(' ').length);}else i++;}console.log(max)"`
      prints a number `<= 200`.
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0.
    - `git status --porcelain` shows `docs/recipes/05-enterprise-update-plan.md` untracked and no other
      modified file.
  </acceptance_criteria>
  <done>
    Step 2 exists as a three-row Case 2 block with the correct anchor, the byte-exact Windows-only /
    Reversible marker line, the corrected enabled-by-default and Arm64-supported positions, the VBS
    and Enterprise-edition constraints, the silent-LCU fallback, the two-level precedence rule, both
    guide links, a full click-path, one measured breaks-callout and per-claim evidence lines — and the
    file is still uncommitted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Step 3 — driver approval mode, the Destructive decision</name>
  <files>docs/recipes/05-enterprise-update-plan.md</files>
  <read_first>
    - `docs/recipes/05-enterprise-update-plan.md` — read the whole file as Task 1 left it, in
      particular Step 1's Case 1 block and its two sibling H3 branch bodies, which Step 3 mirrors
      exactly in shape.
    - `docs/operations/patch-management/06-windows-driver-firmware-updates.md` — read the
      `approval-modes`, `approval-workflow` and `deferral-deadline-behavior` anchored sections in
      full. These carry the two modes, the approval workflow the Manual branch walks, and the
      deferral and deadline behavior the Automatic branch depends on. **Read-only.**
    - `docs/operations/patch-management/07-windows-autopatch.md` — read the
      `autopatch-and-update-client-policies` anchored section: the Autopatch driver-policy relationship
      that makes the mode switch destructive in a service-managed topology. **Read-only.**
    - `.planning/research/FEATURES.md` section 5.2 row **D-3** — the two branches and the
      `[SOURCED]` `Destructive` rating, whose stated consequence is that switching modes in Autopatch
      destroys all approvals, pauses and declines.
    - `.planning/research/FEATURES.md` section 5.3 the **Driver update** and **Autopatch driver mode
      switch** rows — no policy rollback, and no recovery for the mode switch. Step 3's consequence
      text references these; Plan 05 authors their `## Rollback/Recovery` entries.
    - `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-CONTEXT.md` — D-14 (this is where
      `Destructive` lands and SC#2 rides on it) and D-11 (the rating is assigned at the recommended
      branch).
  </read_first>
  <action>
Append `### Step 3:` to `## Steps`, immediately after Step 2, at the Case 1 shape locked in
`151-01-PLAN.md`'s Phase-wide rules — the same anatomy as Step 1.

Structure, in order:

- The hand-authored anchor `decision-driver-approval-mode`, bare and self-closing, with no visible
  D-NN label in the heading.
- A one-sentence `> **Ask the admin:**` blockquote lead-in carrying the prompt only.
- The marker line, on its own non-blockquote bold line, byte-exact:
  `**Applies to:** Windows-only · **Reversibility:** Destructive`
  This is the **only** occurrence of `Destructive` in a Reversibility field in the whole recipe.
  SC#2 and RCP-02 are graded by counting it, so it must land exactly here and nowhere else (D-12,
  D-14).
- A mandatory blank line.
- The Case 1 table `| Option | When to choose | Consequence if wrong | Branch |` with **exactly two**
  option rows — Automatic approval with per-ring deferral, and Manual approval with approval dates —
  each Branch cell linking by heading slug to its sibling H3.

Content requirements:

- The Consequence-if-wrong content must state, **in the recipe's own prose**, that switching approval
  mode destroys all existing approvals, pauses and declines — they are lost with the replaced
  policies, and there is no recovery. Do not defer this to a link: a reader who follows only the link
  gets the plan wrong, and this is the sentence the `Destructive` rating exists to justify (D-14,
  D-57).
- State that the driver update policy carries its own deferral, separate from the Windows Update
  client policy's — the ring deferral does not reach drivers approved by the driver update policy.
  Link `../operations/patch-management/06-windows-driver-firmware-updates.md#deferral-deadline-behavior`
  for the full treatment.
- Preserve dash fidelity on any deferral range you restate: the corpus writes these with an en-dash
  (U+2013), and a range lifted from inside a verbatim quote keeps its own dash and its quotation marks
  (D-61).

Then `### Step 3a:` and `### Step 3b:` as sibling H3 branch bodies — never H4, because a Step is
already H3 and STD-05 D-04 rule 3 caps branch headings at H3 (D-28). Each is a **prose step
sequence**, never a table-as-procedure and never fenced pseudo-config (STD-05 D-04 rule 2). Each
carries its full Intune click-path from the admin center down to the blade (D-32), and each closes
with a one-line routing sentence forward to Step 4 (D-31). Branch headings carry a colon and no other
punctuation so their slugs are clean and the Branch-cell links resolve.

Close Step 3 with exactly one `> **What breaks if misconfigured:**` blockquote, measured under 200
characters, naming what the admin will see and what the end user will see, with a `See:` line linking
the driver guide.

Every substantive claim carries a per-claim `**Source:**` evidence line covering one page only.

**Do not commit.** Then run every gate in the `<verify>` block and read the output; record the exact
`check-nav-hub-links` failure list in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&amp;1 | tail -2; node scripts/validation/v1.20-milestone-audit.mjs 2>&amp;1 | tail -2; node scripts/validation/check-nav-hub-links.mjs 2>&amp;1 | tail -6; grep -c '^### Step ' docs/recipes/05-enterprise-update-plan.md; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<a id="decision-driver-approval-mode"' docs/recipes/05-enterprise-update-plan.md`
      returns `1`.
    - `grep -c 'Reversibility:\*\* Destructive$' docs/recipes/05-enterprise-update-plan.md` returns
      exactly `1` — RCP-02's single Destructive rating, anchored to end-of-line (D-12, D-14).
    - `grep -c 'Reversibility:\*\* Effectively irreversible$' docs/recipes/05-enterprise-update-plan.md`
      returns `0` — Step 4 has not been authored yet; Plan 03 lands it.
    - `grep -c '^### Step 3[ab]:' docs/recipes/05-enterprise-update-plan.md` returns `2`.
    - `grep -c '^### Step ' docs/recipes/05-enterprise-update-plan.md` returns `7` — Steps 1, 1a, 1b,
      2, 3, 3a, 3b.
    - The Step 3 table is the four-column Case 1 shape with exactly two option rows:
      `sed -n '/<a id="decision-driver-approval-mode"/,/^### Step 3a/p' docs/recipes/05-enterprise-update-plan.md | grep -c '^| '`
      returns `4` (header, separator, two option rows), and the same range contains
      `Consequence if wrong`.
    - `sed -n '/<a id="decision-driver-approval-mode"/,/^### Step 4/p' docs/recipes/05-enterprise-update-plan.md | grep -ciE 'approvals[,;].*(pause|decline)'`
      returns at least `1` — the destroy-all-approvals consequence is stated in the recipe's own prose.
    - Both Branch-cell link targets resolve: `node scripts/validation/check-nav-hub-links.mjs` reports
      no failure naming `#step-3a-` or `#step-3b-`.
    - `grep -c '> \*\*Ask the admin:\*\*' docs/recipes/05-enterprise-update-plan.md` returns `3` and
      `grep -c '> \*\*What breaks if misconfigured:\*\*' docs/recipes/05-enterprise-update-plan.md`
      returns `3`.
    - `grep -c '```' docs/recipes/05-enterprise-update-plan.md` returns `0`.
    - `grep -c 'Autopatch rings' docs/recipes/05-enterprise-update-plan.md` returns `0`,
      `grep -c 'SCCM' docs/recipes/05-enterprise-update-plan.md` returns `0`,
      `grep -c 'System Center' docs/recipes/05-enterprise-update-plan.md` returns `0`.
    - `grep -cE '\]\((0[0-9]|[0-9])#' docs/recipes/05-enterprise-update-plan.md` returns `0` — no
      numeric-prefix link shorthand (D-65).
    - No contiguous top-level blockquote run exceeds 200 characters (same node one-liner as Task 1).
    - `node scripts/validation/c17-eee-contract.mjs` reports `236 files checked, 0 with violations`.
    - `node scripts/validation/v1.20-milestone-audit.mjs` exits 0.
    - `node scripts/validation/check-nav-hub-links.mjs` reports 0 hub-presence failures, and every
      corpus-link failure names a fragment inside `docs/recipes/05-enterprise-update-plan.md` that
      Plan 03, 04 or 05 still creates.
    - `git status --porcelain` shows `docs/recipes/05-enterprise-update-plan.md` untracked and no other
      modified file — this plan makes no commit (D-52).
  </acceptance_criteria>
  <done>
    Step 3 exists as a two-row Case 1 block with the correct anchor, the byte-exact Windows-only /
    Destructive marker line landing exactly once in the file, the destroy-all-approvals consequence
    stated in the recipe's own prose, two sibling H3 branch bodies with full click-paths and routing
    sentences, one measured breaks-callout and per-claim evidence lines — and the file is still
    uncommitted.
  </done>
  <reversibility rating="costly">
    D-14's assignment of `Destructive` to this Step is what SC#2 is counted against. Moving it to a
    different Step later means re-rating the set under D-11's recommended-branch rule and re-grading
    RCP-02. The assignment is settled in `151-CONTEXT.md`; no checkpoint is inserted.
  </reversibility>
</task>

</tasks>

<threat_model>
See the `<threat_model>` block in `151-01-PLAN.md` for the phase's trust boundaries and full STRIDE
register. The threats this plan directly mitigates:

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151-03 | Denial of service | Step 3 — switching driver approval mode destroys all approvals, pauses and declines | high | mitigate | The consequence is stated in the recipe's own Consequence-if-wrong prose rather than only behind a link (D-57), the rating is `Destructive` (D-14), and the acceptance criteria grep for the sentence, not just for the rating. |
| T-151-04 | Tampering | Step 2 restating hotpatch on a framing the corpus has already falsified | high | mitigate | RCP-02's three corrections (enabled by default, not x64-only, VBS required) are each an acceptance-criterion grep, and the two falsified framings are negative-grepped to zero. |
| T-151-05 | Repudiation | A driver deferral range restated without its dash marking | medium | mitigate | D-61 — restate an en-dash range with an en-dash, and keep a quoted range inside its quotation marks. |
| T-151-07 | Spoofing | A Branch-cell link resolving to nothing because a branch heading slug drifted | medium | mitigate | Branch headings carry a colon and no other punctuation, and the link checker is run at the end of the task with the Step 3a and 3b fragments explicitly named in the acceptance criteria. |
| T-151-SC | Tampering | npm, pip or cargo installs | low | accept | No package-manager install task exists in this plan. |
</threat_model>

<verification>
- `node scripts/validation/c17-eee-contract.mjs` → `236 files checked, 0 with violations`.
- `node scripts/validation/v1.20-milestone-audit.mjs` → exit 0.
- `node scripts/validation/check-nav-hub-links.mjs` → 0 hub-presence failures; every corpus-link
  failure confined to fragments Plans 03-05 create.
- `grep -c '^### Step ' docs/recipes/05-enterprise-update-plan.md` → `7`.
- `grep -c 'Reversibility:\*\* Destructive$' docs/recipes/05-enterprise-update-plan.md` → `1`.
- `git status --porcelain` → the recipe untracked, nothing else modified, no new commit.
</verification>

<success_criteria>
- Steps 2 and 3 exist at the locked block anatomy, with correct anchors, byte-exact marker lines and
  the correct STD-05 case shape for each (three-column Case 2 for Step 2, four-column Case 1 for
  Step 3).
- The hotpatch decision ships on all three corrected positions and states the two-level precedence
  rule; neither falsified framing appears anywhere.
- `Destructive` occurs exactly once in a Reversibility field, on Step 3, and its justifying
  consequence is in the recipe's own prose.
- Step 3's two branch bodies resolve from their Branch cells, carry full click-paths and reconverge to
  Step 4.
- C17 is 236/0, the milestone audit exits 0, and the file is still uncommitted.
</success_criteria>

<output>
Create `.planning/phases/151-recipe-5-the-enterprise-update-plan/151-02-SUMMARY.md` when done.

Record in it: the exact `check-nav-hub-links` failure list; the measured longest blockquote run after
each task; the running Step-heading count; the running counts of the four Reversibility enum values;
and confirmation that no commit was made.
</output>
