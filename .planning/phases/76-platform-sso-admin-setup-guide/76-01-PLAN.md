---
phase: 76-platform-sso-admin-setup-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-macos/07-platform-sso-setup.md
  - docs/admin-setup-macos/03-configuration-profiles.md
autonomous: true
requirements: [PSSO-01, PSSO-02, PSSO-03, PSSO-12]
must_haves:
  truths:
    - "An admin can follow guide 07 to build a Platform SSO Settings Catalog policy with the correct identifiers (extension id, Team ID, registration token)"
    - "An admin deploying to a mixed macOS 13/14+ fleet finds the dual-field side-by-side table that prevents Error 10001"
    - "An admin can identify and resolve all three bootstrapping blockers (per-user MFA, CA exclusion, TLS exemption) BEFORE the Settings Catalog step"
    - "An admin can locate the advanced ADE-during-Setup path with macOS 26 + CP 5.2604.0 prerequisites, static-groups-only, Smart-card-excluded, and wipe-to-fix recovery — clearly marked optional, post-enrollment is the default"
    - "An admin can verify registration with app-sso platform -s in the Verification section"
    - "The 03-configuration-profiles.md stub now links live to guide 07, and guide 07 exists in the same commit (C13 stays green)"
  artifacts:
    - path: "docs/admin-setup-macos/07-platform-sso-setup.md"
      provides: "End-to-end macOS Platform SSO admin setup guide (corpus skeleton)"
      contains: "com.microsoft.CompanyPortalMac.ssoextension"
      min_lines: 90
    - path: "docs/admin-setup-macos/03-configuration-profiles.md"
      provides: "Live markdown link to guide 07 (converted from code-span)"
      contains: "[07-platform-sso-setup.md](07-platform-sso-setup.md)"
  key_links:
    - from: "docs/admin-setup-macos/03-configuration-profiles.md"
      to: "docs/admin-setup-macos/07-platform-sso-setup.md"
      via: "live markdown link in §Extensible SSO (line ~168)"
      pattern: "\\[07-platform-sso-setup\\.md\\]\\(07-platform-sso-setup\\.md\\)"
    - from: "docs/admin-setup-macos/07-platform-sso-setup.md"
      to: "docs/_glossary-macos.md#platform-sso"
      via: "See Also glossary anchor links created in Phase 75"
      pattern: "_glossary-macos\\.md#platform-sso"
---

<objective>
Author `docs/admin-setup-macos/07-platform-sso-setup.md` — the end-to-end macOS Platform SSO admin setup guide — using the corpus outer skeleton (per D-01/A3), and in the SAME plan/commit convert the deferred `07-platform-sso-setup.md` code-span in `03-configuration-profiles.md` (line ~168) to a live markdown link (D-06 carry-over). The atomic landing of link-and-target keeps the frozen v1.8 C13 broken-link gate green.

Guide 07 covers: the `com.apple.extensiblesso` Settings Catalog payload and all field values (PSSO-01); the mixed-fleet dual-field side-by-side table that prevents Error 10001 (PSSO-02); the three silent bootstrapping blockers in an upfront callout placed BEFORE any Settings Catalog step plus point-of-use cross-references (PSSO-03, per C4/D-01a); and the advanced/optional ADE-during-Setup-Assistant subsection at the guide end with post-enrollment as the documented default (PSSO-12, per D1).

Purpose: Satisfy SC1–SC4 of Phase 76 and discharge the Phase-75 D-06 mandatory carry-over atomically.
Output: New file `07-platform-sso-setup.md`; one-line edit + Version-History row in `03-configuration-profiles.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/76-platform-sso-admin-setup-guide/76-CONTEXT.md
@.planning/phases/76-platform-sso-admin-setup-guide/76-RESEARCH.md
@.planning/phases/76-platform-sso-admin-setup-guide/76-PATTERNS.md
@docs/admin-setup-macos/02-enrollment-profile.md
@docs/admin-setup-macos/03-configuration-profiles.md
@docs/_glossary-macos.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author guide 07 — front-matter, platform gate, Prerequisites with upfront bootstrapping blockers callout, and the Settings Catalog Steps including the dual-field table</name>
  <read_first>
    - docs/admin-setup-macos/07-platform-sso-setup.md (target file being created)
    - docs/admin-setup-macos/02-enrollment-profile.md (corpus skeleton exemplar — front-matter, platform gate, Step/`#### In Intune admin center` pattern, Verification/Config-Failures/See-Also/Version-History shapes)
    - .planning/phases/76-platform-sso-admin-setup-guide/76-RESEARCH.md §(a) SC1 (all identifiers, Settings Catalog navigation path, field/value table, URLs, version gates, assignment rule, Entra prerequisites), §(b) SC2 (dual-field table prescription + Error 10001 root cause), §(c) SC3 (the three blockers DF-3/DF-9/DF-10 with placement rule and point-of-use cross-refs)
    - .planning/phases/76-platform-sso-admin-setup-guide/76-PATTERNS.md §"07-platform-sso-setup.md (CREATE)" (front-matter values, platform-gate verbatim lines, section skeleton, Navigation Path Format) and §"Hard-Bordered Blockquote Callout" (upfront blockers callout format)
    - docs/_glossary-macos.md (confirm #platform-sso / #secure-enclave / #enterprise-sso-plug-in anchors for inline term links)
  </read_first>
  <action>
    Create `docs/admin-setup-macos/07-platform-sso-setup.md` and write the document head through the Steps section.

    Front-matter (5 keys, exact order per PATTERNS): last_verified: 2026-06-20; review_by: 2026-09-20; applies_to: ADE; audience: admin; platform: macOS. The 2026-06-20 / 2026-09-20 values are MANDATORY (DS-2 90-day PSSO cadence carry-over).

    Platform-gate blockquote: first line "This guide covers macOS Platform SSO configuration via Microsoft Intune."; the two following lines (Windows Autopilot link to ../admin-setup-apv1/00-overview.md and macOS Glossary link to ../_glossary-macos.md) copied verbatim from the analog.

    H1 title (e.g. "macOS Platform SSO Setup") followed by an intro sentence linking the inline term [Platform SSO](../_glossary-macos.md#platform-sso).

    ## Prerequisites: bullet list covering — Entra "Users may join devices to Microsoft Entra ID" enabled; Intune Device Configuration Read/Create/Update/Assign permission; tenant MFA enabled for the registration step; Company Portal install prerequisite (minimum 5.2404.0 for standard post-enrollment PSSO); macOS 13 minimum (macOS 14 recommended; Smart Card requires macOS 14+); for Sequoia fleets note "macOS 15.0–15.2: re-registration loop — fixed in 15.3; upgrade to 15.3+" (NEVER write "awaiting fix"). DS-1 DISCIPLINE: this main PSSO section carries NO supervised-only callout.

    Under Prerequisites add a subsection `### Known Silent Blockers — Resolve Before Deployment` containing ONE hard-bordered blockquote callout (use the PATTERNS hard-bordered format: `> **Before You Deploy — Resolve These First:**`, blank `>` separators, `> - ` bullets) naming ALL THREE blockers together (per C4/D-03): (1) remove legacy per-user MFA — silently blocks Password sync registration, no error shown (DF-3); (2) exclude newly-enrolled devices from strict CA "require compliant device" gating during the bootstrap window — circular dependency, user registration fails with "compliant device required" (DF-9); (3) exempt PSSO / Microsoft login endpoints (login.microsoftonline.com, login.microsoft.com, sts.windows.net) from TLS break-and-inspect — certificate-pinned flows break behind inspecting proxies (DF-10). This callout MUST appear BEFORE any Settings Catalog step (DF-3 / D-01a ordering invariant). Do NOT place these three in the Configuration-Caused Failures table (D-03).

    ## Steps with `### Step N:` subheadings (count/naming at your discretion; new file → no anchor constraint). Required step content, in this order:
    - A step for Entra prerequisites / assignment / network: cover device-join setting, USER-group assignment ("assign to user groups, NOT device groups and NOT filters on device groups" — VERIFIED), and include the point-of-use cross-references for the CA-exclusion blocker (Entra/assignment step) and the TLS-exemption blocker (network step) back to the upfront callout.
    - A step "Install / verify Company Portal" (minimum 5.2404.0 floor for standard path).
    - A step "Create the Settings Catalog policy" using the exact Navigation path from PATTERNS Navigation Path Format (bold > chains). State the payload `com.apple.extensiblesso`. Document the required picker fields and values from RESEARCH §(a): Extension Identifier `com.microsoft.CompanyPortalMac.ssoextension`; Team Identifier `UBF8T346G9`; Registration Token `{{DEVICEREGISTRATION}}` (literal, with braces); Type `Redirect`; Platform SSO > Authentication Method (macOS 14+: Password / UserSecureEnclaveKey / SmartCard); Token To User Mapping Account Name `com.apple.PlatformSSO.AccountShortName`; Full Name `name`; Use Shared Device Keys (Enabled, macOS 14+); Screen Locked Behavior `Do Not Handle`; URLs (login.microsoftonline.com, login.microsoft.com, sts.windows.net, plus the sovereign-cloud additions noted as conditional). Place the per-user-MFA point-of-use cross-ref at the auth-method portion of this step. Do NOT include the `NewUserAuthorizationMode` key (deferred per D3=B).
    - Inside the Settings Catalog step, add the mixed-fleet dual-field side-by-side TABLE (PSSO-02 / VR-4 / D-04): columns for macOS 13 vs macOS 14+ showing Field = `Authentication Method (Deprecated)` (macOS 13 only; Password or UserSecureEnclaveKey — NO SmartCard) vs `Platform SSO > Authentication Method` (macOS 14+; all three methods), Supports SmartCard (No / Yes), and Failure-if-missing (Error 10001 on macOS 13 devices / missed Platform SSO sub-settings). Call out the Error 10001 consequence adjacent to or in the table. Both fields MUST be in ONE policy.
    - An assignment step (or fold into the Entra/assignment step) confirming USER-group targeting.

    Do NOT paste full file contents from analogs verbatim; replicate STRUCTURE and use the exact identifier/field strings from RESEARCH. Do NOT use fenced code blocks for prose; tables and `>` callouts are markdown, not fenced code.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('docs/admin-setup-macos/07-platform-sso-setup.md','utf8'); const needles=['last_verified: 2026-06-20','review_by: 2026-09-20','com.apple.extensiblesso','com.microsoft.CompanyPortalMac.ssoextension','UBF8T346G9','{{DEVICEREGISTRATION}}','## Prerequisites','## Steps','Authentication Method (Deprecated)','Platform SSO > Authentication Method','Error 10001','app-sso platform -s'===t.includes('app-sso platform -s')?'app-sso platform -s':'__VERIFY_SKIP__']; const missing=['last_verified: 2026-06-20','review_by: 2026-09-20','com.apple.extensiblesso','com.microsoft.CompanyPortalMac.ssoextension','UBF8T346G9','{{DEVICEREGISTRATION}}','## Prerequisites','## Steps','Authentication Method (Deprecated)','Platform SSO > Authentication Method','Error 10001'].filter(n=>!t.includes(n)); if(missing.length){console.error('MISSING:',missing);process.exit(1)} const bIdx=t.indexOf('Before You Deploy'); const scIdx=t.search(/Create the Settings Catalog|Settings Catalog policy/); if(bIdx===-1){console.error('blockers callout missing');process.exit(1)} if(scIdx!==-1 && bIdx>scIdx){console.error('blockers callout NOT before Settings Catalog step');process.exit(1)} if(/supervised-only|supervised only/i.test(t.split(/## Advanced|ADE-during-Setup|Advanced \/ Optional/)[0])){console.error('supervised-only callout found in main section (DS-1 violation)');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <done>
    Guide 07 exists with front-matter (last_verified: 2026-06-20 / review_by: 2026-09-20), platform gate, ## Prerequisites, the upfront `### Known Silent Blockers` callout naming all three blockers and appearing BEFORE the Settings Catalog step, and ## Steps containing the Settings Catalog payload with all verified identifiers and the dual-field side-by-side table referencing Error 10001. No supervised-only callout in the main section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete guide 07 — Verification, Configuration-Caused Failures, ADE Advanced/Optional subsection, See Also, Version-History — and atomically convert the 03-stub code-span to a live link</name>
  <read_first>
    - docs/admin-setup-macos/07-platform-sso-setup.md (target file — appended in this task)
    - docs/admin-setup-macos/02-enrollment-profile.md (Verification checkbox list, Configuration-Caused Failures 4-col table, See Also, Version-History shapes — analog lines 108–140)
    - docs/admin-setup-macos/03-configuration-profiles.md (target edit — §Extensible SSO line ~168 code-span; Version-History table lines ~199–203; the nine `#### In Intune admin center` subheadings that must NOT change)
    - .planning/phases/76-platform-sso-admin-setup-guide/76-RESEARCH.md §(a) `app-sso platform -s` verification, §(d) SC4 (ADE prerequisites table, Enable Registration During Setup field path, Smart-card exclusion, wipe-to-fix, update-to-macOS-26-first branch, ADE front-matter note), §(f) 03-stub conversion (exact current/target line text)
    - .planning/phases/76-platform-sso-admin-setup-guide/76-PATTERNS.md §"Configuration-Caused Failures table" (code-span runbook placeholders), §"Hard-Bordered Blockquote Callout" (ADE Advanced/Optional border + ADE-only callout), §"See Also section" (glossary anchor links), §"Version-History Row Format", §"03-configuration-profiles.md (EDIT)" (exact line-168 target + Version-History row to prepend)
  </read_first>
  <action>
    Append to `docs/admin-setup-macos/07-platform-sso-setup.md`:

    ## Verification — `- [ ]` checkbox list mixing device-side and portal checks (D-01b): include `app-sso platform -s` device-side check (note expected `Device Registration: REGISTERED` and `User Registration: REGISTERED`); Intune portal profile status "Succeeded" under Devices > Configuration > [profile] > Device status; and System Settings > Privacy and security > Profiles shows `com.apple.extensiblesso Profile`.

    ## Configuration-Caused Failures — 4-column table `Misconfiguration | Portal | Symptom | Runbook`. Rows from RESEARCH: only macOS 14+ auth method on mixed fleet → Error 10001; legacy SSO extension profile still assigned alongside PSSO → Error 10002; `{{DEVICEREGISTRATION}}` mistyped/missing braces → profile succeeds, no registration prompt; auth method changed on existing policy → fleet-wide re-registration. For Runbook cells use the D-06 code-span placeholder pattern (e.g. `` `35-macos-sso-sign-in-failure.md` (Phase 80) ``) — NOT live links (those files do not exist yet). Do NOT add the three bootstrapping blockers to this table (D-03).

    ADE Advanced/Optional subsection at the guide END (D1/PSSO-12): bordered block labeled "Advanced / Optional" (use the hard-bordered callout pattern). It MUST appear AFTER the standard post-enrollment flow and the standard sections. Content: (1) an ADE-ONLY callout — distinct string from any supervised-only callout (DS-1); (2) prerequisites table — macOS 26 (hard gate), Company Portal 5.2604.0 (LOB app, NOT VPP), Assigned (static) USER groups only (NOT dynamic, NOT device), three-policy same-group rule (Settings Catalog PSSO policy + Company Portal LOB app + ADE enrollment profile on the SAME static user groups); (3) an achievable "update to macOS 26 first" prerequisite branch (if fleet < macOS 26, update before enrolling via this path); (4) the key Settings Catalog field path "Authentication > Extensible single sign-on > Platform SSO > Enable Registration During Setup" = Enabled (and Enable Create First User During Setup = Enabled, Password method only); (5) Smart Card excluded note; (6) wipe-to-fix recovery statement (no in-place fix; wipe and re-enroll). State that post-enrollment is the documented default and this ADE path is optional/advanced.

    ## See Also — bare `- [text](path)` links including the Phase-75 glossary anchors: [Platform SSO](../_glossary-macos.md#platform-sso), [Secure Enclave](../_glossary-macos.md#secure-enclave), [Enterprise SSO Plug-in](../_glossary-macos.md#enterprise-sso-plug-in), [Configuration Profiles](03-configuration-profiles.md) (back-link to the stub now pointing here), [macOS ADE Lifecycle Overview](../macos-lifecycle/00-ade-lifecycle.md).

    Version-History: `---` rule then `| Date | Change | Author |` table with the initial row `| 2026-06-20 | Phase 76 (PSSO-01/02/03/12): initial Platform SSO admin setup guide | -- |`.

    THEN edit `docs/admin-setup-macos/03-configuration-profiles.md` (same plan = same commit, atomic with guide 07's existence per D-06/C13):
    - On line ~168, replace `Continue with Platform SSO setup in `07-platform-sso-setup.md` (added in the next documentation phase).` with `Continue with Platform SSO setup in [07-platform-sso-setup.md](07-platform-sso-setup.md).` (drop the parenthetical; backtick code-span → live markdown link). Change ONLY that line.
    - Do NOT touch the `## Extensible SSO` heading (line 157) or any of the nine `#### In Intune admin center` subheadings (anchor stability, PITFALL-6).
    - Prepend a Version-History row (newest at top): `| 2026-06-20 | Phase 76 (PSSO-01 / D-06): converted `07-platform-sso-setup.md` code-span to live markdown link | -- |`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const g=fs.readFileSync('docs/admin-setup-macos/07-platform-sso-setup.md','utf8'); const c=fs.readFileSync('docs/admin-setup-macos/03-configuration-profiles.md','utf8'); let e=[]; ['## Verification','app-sso platform -s','## Configuration-Caused Failures','## See Also','_glossary-macos.md#platform-sso','_glossary-macos.md#secure-enclave','_glossary-macos.md#enterprise-sso-plug-in','macOS 26','5.2604.0','Enable Registration During Setup','| Date | Change | Author |','Phase 76 (PSSO-01/02/03/12)'].forEach(n=>{if(!g.includes(n))e.push('guide07 missing: '+n)}); if(!/ade[- ]only/i.test(g))e.push('ADE-only callout missing'); if(!/wipe/i.test(g))e.push('wipe-to-fix missing'); const seeAlsoIdx=g.indexOf('## See Also'); const adeIdx=Math.max(g.lastIndexOf('Advanced / Optional'),g.search(/ADE[- ]during[- ]Setup/i)); const scIdx=g.search(/Create the Settings Catalog|Settings Catalog policy/); if(adeIdx!==-1 && scIdx!==-1 && adeIdx<scIdx)e.push('ADE subsection not at end (before Settings Catalog step)'); if(c.includes('`07-platform-sso-setup.md` (added in the next documentation phase)'))e.push('03-stub still code-span'); if(!c.includes('[07-platform-sso-setup.md](07-platform-sso-setup.md)'))e.push('03-stub not a live link'); if(!c.includes('Phase 76 (PSSO-01 / D-06)'))e.push('03 version-history row missing'); const nIntune=(c.match(/#### In Intune admin center/g)||[]).length; if(nIntune!==9)e.push('03 #### In Intune admin center count != 9 (got '+nIntune+')'); if(e.length){console.error(e.join('\\n'));process.exit(1)} console.log('OK')"</automated>
  </verify>
  <done>
    Guide 07 is complete with ## Verification (incl. app-sso platform -s), ## Configuration-Caused Failures (code-span runbook placeholders, no bootstrapping blockers), the ADE Advanced/Optional subsection at the END (ADE-only callout, macOS 26 + CP 5.2604.0, static-groups-only, Smart-card-excluded, wipe-to-fix, update-to-macOS-26-first branch), ## See Also with the three glossary anchors, and a Version-History row. 03-configuration-profiles.md line 168 is now a live link to guide 07 with a Version-History row added, the nine `#### In Intune admin center` subheadings unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only phase. No executable code, no user input, no auth surface, no data handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Tampering | C13 broken-link gate in v1.8-milestone-audit.mjs | mitigate | Live link in 03-config-profiles.md lands in the SAME plan/commit that creates guide 07; 00-overview 08/09 stay code-spans (Plan 02). No new internal broken link at any commit boundary; 15-entry allowlist untouched. |
| T-76-02 | Information Disclosure | Factual accuracy of security guidance (TLS exemption, CA policy, per-user MFA) | accept | Content is faithful representation of Microsoft's official published guidance (verified 2026-05-18 / 2026-06-01); no new security mechanism is invented. Risk is factual drift, mitigated by last_verified/review_by 90-day cadence. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — documentation authoring only. |
</threat_model>

<verification>
- C13 broken-link gate stays green: `node scripts/validation/v1.8-milestone-audit.mjs` (allowlist.length === 15; 6 transient_external / 9 template_placeholder; no new internal broken links). Guide 07 exists in the same commit as the 03-stub live link.
- Guide 07 carries all verified identifiers, the dual-field table, the upfront blockers callout before any Settings Catalog step, and the ADE subsection at the end.
</verification>

<success_criteria>
- SC1 (PSSO-01): guide 07 covers the `com.apple.extensiblesso` payload, extension id `com.microsoft.CompanyPortalMac.ssoextension`, Team ID `UBF8T346G9`, registration token `{{DEVICEREGISTRATION}}`, Entra prerequisites, USER-group assignment, Company Portal prerequisite, and `app-sso platform -s` verification.
- SC2 (PSSO-02): dual-field side-by-side table (macOS 13 deprecated field AND macOS 14+ field in one policy) preventing Error 10001 is present in the Settings Catalog step.
- SC3 (PSSO-03): all three bootstrapping blockers appear in an upfront callout BEFORE any Settings Catalog step plus point-of-use cross-refs.
- SC4 (PSSO-12): ADE Advanced/Optional subsection at the guide end with macOS 26 + CP 5.2604.0, static-groups-only, Smart-card-excluded, wipe-to-fix, post-enrollment-default framing, and last_verified/review_by carried.
- D-06 carry-over: 03-stub code-span is now a live markdown link, landed atomically with guide 07.
</success_criteria>

<output>
Create `.planning/phases/76-platform-sso-admin-setup-guide/76-01-SUMMARY.md` when done.
</output>
