---
phase: 116-l1-l2-runbook-retrofit-75-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pipeline/retrofit-runbook.mjs
autonomous: true
requirements: [RETRO-01]
must_haves:
  truths:
    - "The helper resolves a doc_id for all 75 L1/L2 runbook paths by joining RE-index.md on the Path column (never hand-transcribed)"
    - "The helper resolves every present platform value through the D1_MAP and injects platform: Windows for the 17 keyless files (L1 01-09, L2 01-08)"
    - "A --dry-run across all 75 files writes nothing to the corpus or registry and reports zero unresolved doc_ids and zero unmapped platforms"
    - "The helper refuses to write any path outside docs/l1-runbooks/ or docs/l2-runbooks/ (path allowlist)"
    - "The helper refuses to process a file whose last_verified is the TEMPLATE-SENTINEL 1970-01-01 (false-pass guard)"
  artifacts:
    - path: "scripts/pipeline/retrofit-runbook.mjs"
      provides: "Mechanical EEE retrofit helper (D-03): frontmatter key injection, EEE block line, gate-blockquote relocation, Version-History row, platform injection"
      contains: "const D1_MAP"
  key_links:
    - from: "scripts/pipeline/retrofit-runbook.mjs"
      to: "docs/_registry/RE-index.md"
      via: "path-column join to resolve doc_id"
      pattern: "RE-\\d+"
    - from: "scripts/pipeline/retrofit-runbook.mjs"
      to: "scripts/validation/c17-eee-contract.mjs"
      via: "D1_MAP copied verbatim (lines 26-47)"
      pattern: "D1_MAP"
---

<objective>
Build the mechanical EEE retrofit helper `scripts/pipeline/retrofit-runbook.mjs` (D-03) that the seven
batch plans (116-02..116-08) invoke to perform the deterministic half of every runbook retrofit. This
plan builds and proves the script only — it writes NOTHING to the corpus or registry (dry-run + self-test).

Purpose: The block↔frontmatter exact-match (C17 #9) and the doc_id join are unforgiving of hand
transcription across 75 files; the mechanical shape is identical per file and belongs in a node-builtins
script that derives every value programmatically (per D-03, RETRO-01).
Output: One node-builtins-only script mirroring the `scripts/pipeline/` idiom, validated by --self-test
and a full-corpus --dry-run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-l1-l2-runbook-retrofit-75-docs/116-CONTEXT.md
@.planning/phases/116-l1-l2-runbook-retrofit-75-docs/116-RESEARCH.md
@.planning/phases/116-l1-l2-runbook-retrofit-75-docs/116-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/pipeline/retrofit-runbook.mjs</name>
  <files>scripts/pipeline/retrofit-runbook.mjs</files>
  <read_first>
    - scripts/pipeline/guard-docx.mjs (node-builtins idiom: imports, argv handling, checks array, runner loop, padLabel PASS/FAIL output, exit-code convention)
    - scripts/validation/c17-eee-contract.mjs (COPY VERBATIM: D1_MAP lines 26-47; readFile+CRLF-normalize lines 61-65; walkMd lines 68-86 using lstatSync; relNormalize lines 88-94; frontmatter regex /^---\n([\s\S]*?)\n---/m lines 122-133)
    - docs/_registry/RE-index.md (Path-column table format the helper parses to build Map<relativePath, docId>)
    - docs/_templates/l1-template.md and docs/_templates/l2-template.md (target frontmatter key set, EEE block line, D3-A body order)
    - docs/_standards/EEE-SOP-standard.md (single-line block format; D2 Last Reviewed = last_verified verbatim; Version-History row exact string)
  </read_first>
  <action>
    Create `scripts/pipeline/retrofit-runbook.mjs`, node built-ins ONLY (node:fs, node:path, node:process —
    zero npm packages, matching the scripts/pipeline/ invariant). Support argv flags `--dry-run` (compute
    and report, write nothing) and `--verbose`. Accept one or more file paths as positional args, or `--all`
    to process every `.md` under docs/l1-runbooks/ and docs/l2-runbooks/.

    Copy VERBATIM from c17-eee-contract.mjs: the 20-entry D1_MAP (lines 26-47), readFile (CRLF-normalize with
    .replace(/\r\n/g,'\n')), walkMd (lstatSync, skip symlinks), relNormalize (backslash→forward-slash), and the
    frontmatter regex /^---\n([\s\S]*?)\n---/m (m flag mandatory — files may have an HTML-comment preamble;
    NEVER use content.startsWith('---')). Any divergence from D1_MAP causes C17 #9/#10 failures.

    Build buildDocIdMap(registryPath): parse RE-index.md, match data rows with /^\|\s*(RE-\d+)\s*\|\s*(docs\/[^\|]+?)\s*\|/,
    return Map keyed on the Path column (column 2) → RE-NNN (column 1). Join on Path ONLY, never title or order.

    Per target file, in order:
    1. Parse frontmatter. Look up doc_id = map.get(relNormalize(path)); ERROR and skip if not found.
    2. TEMPLATE-SENTINEL guard: read last_verified; if it equals 1970-01-01, ERROR and refuse to write
       (prevents C17 silently skipping #9/#12). Carry the original last_verified value verbatim into output.
    3. Path allowlist guard: refuse to write any path not under docs/l1-runbooks/ or docs/l2-runbooks/.
    4. Platform: if the frontmatter has no `platform:` key, inject `platform: Windows` (keyless set = L1 01-09,
       L2 01-08); else carry the existing value verbatim (no frontmatter normalization). Compute D1 label =
       D1_MAP[platform]; ERROR if unmapped (no fallback).
    5. Tier from path: `docs/l1-runbooks/` → owner string `L1 Team Lead`; `docs/l2-runbooks/` → `L2 Desktop Lead`.
    6. Frontmatter output: prepend keys `doc_id: RE-NNN`, `status: Approved`, `owner: <tier owner>`,
       `doc_type: Runbook`; inject `platform: Windows` when needed; RETAIN existing last_verified, review_by,
       applies_to, audience verbatim.
    7. Detect the pre-H1 gate blockquote: the FIRST `/^>/` run appearing before the first `/^# [^#]/` H1 line
       (structural position — NEVER match on the "Version gate"/"Platform gate" literal). Strip it from that
       pre-H1 position.
    8. Emit the EEE block line as the first non-blank body line, exact field order Platform · Doc Type · Doc ID ·
       Status using the middle-dot U+00B7 and bold labels: `**Platform:** <D1 label> · **Doc Type:** Runbook ·
       **Doc ID:** RE-NNN · **Status:** Approved`. owner is NEVER in the block.
    9. Body order out: block line → blank → `# <existing H1 title>` → blank → `## Summary` → blank →
       `[FILL-IN: >=30 words, opens with the tier scope/safety banner]` placeholder → blank → [relocated gate
       blockquote] → [remaining original sections unchanged]. Do NOT rename or delete any existing H2 (e.g. an
       existing `## Context` stays, positioned after `## Summary`).
    10. Version-History: prepend the row `| YYYY-MM-DD | v1.15 EEE reformat — content not re-reviewed | — |`
        (em-dash `—`; YYYY-MM-DD placeholder for the executor to fill at commit time) as the first data row under
        `## Version History`. If the section is ABSENT (only docs/l2-runbooks/01-log-collection.md), create a
        `## Version History` section at end of file with the header row and this data row.

    In --dry-run, print a per-file report line (doc_id resolved, platform injected Y/N, D1 label, gate relocated
    Y/N, Version-History created-or-prepended) and write nothing. Use the guard-docx.mjs padLabel PASS/FAIL output
    convention; exit 0 when all files resolve cleanly, exit 1 on any ERROR (unresolved doc_id, unmapped platform,
    sentinel date, path outside allowlist).

    Do NOT author Summary prose, do NOT fix D-05 blockquotes, do NOT edit RE-index.md — those are batch-plan
    hand-work.
  </action>
  <acceptance_criteria>
    - `node scripts/pipeline/retrofit-runbook.mjs --all --dry-run` exits 0 and reports a resolved doc_id for
      all 75 files (grep the output for 75 `RE-0` lines).
    - The script source contains the D1_MAP with all 20 entries copied verbatim from c17-eee-contract.mjs
      (diff the D1_MAP block against lines 26-47 — byte-identical entries).
    - The script imports only from `node:fs`, `node:path`, `node:process` (no `require(`/`import` of any non-node
      module; grep confirms zero external package names).
    - `git status --porcelain docs/` shows ZERO modified files after the dry-run (dry-run writes nothing).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-runbook.mjs --all --dry-run; test $? -eq 0 &amp;&amp; git diff --quiet docs/</automated>
  </verify>
  <done>The helper exists, is node-builtins-only, resolves all 75 doc_ids and platforms via the registry join and D1_MAP, and its dry-run writes nothing to the corpus or registry.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the helper with --self-test and a full-corpus --dry-run</name>
  <files>scripts/pipeline/retrofit-runbook.mjs</files>
  <read_first>
    - scripts/pipeline/retrofit-runbook.mjs (the script authored in Task 1)
    - scripts/validation/c17-eee-contract.mjs (D1_MAP + platform-resolution logic the dry-run must agree with)
    - .planning/phases/116-l1-l2-runbook-retrofit-75-docs/116-RESEARCH.md (Q3 platform table — the 17 keyless files and every D1 label; Q6 enrollment precheck)
    - docs/_registry/RE-index.md (75 Runbook rows RE-001..RE-075)
  </read_first>
  <action>
    Add a `--self-test` mode to retrofit-runbook.mjs (mirroring guard-docx.mjs / c17-eee-contract.mjs) with
    small in-memory fixtures asserting: (a) buildDocIdMap parses a sample registry row to the correct RE-NNN;
    (b) a keyless-Windows fixture triggers `platform: Windows` injection; (c) an `ios+macos+shared-ipad` fixture
    resolves the D1 label `iOS + macOS + Shared iPad`; (d) a `last_verified: 1970-01-01` fixture is REFUSED;
    (e) a path outside docs/l1-runbooks//l2-runbooks/ is REFUSED. Then run the full-corpus dry-run and confirm
    it agrees with RESEARCH Q3: exactly 17 files flagged for platform injection (L1 01-09, L2 01-08), and every
    other file's platform resolves to a D1 label with zero "unmapped" errors.
  </action>
  <acceptance_criteria>
    - `node scripts/pipeline/retrofit-runbook.mjs --self-test` exits 0 (all sub-tests pass).
    - `node scripts/pipeline/retrofit-runbook.mjs --all --dry-run --verbose` reports exactly 17 files flagged
      for `platform: Windows` injection and zero unmapped-platform errors.
    - The dry-run output lists the sentinel-refusal and path-allowlist guards as active (grep output for the
      guard labels).
    - `git diff --quiet docs/ docs/_registry/RE-index.md` (no corpus or registry writes from self-test/dry-run).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/retrofit-runbook.mjs --self-test &amp;&amp; node scripts/pipeline/retrofit-runbook.mjs --all --dry-run &gt;/dev/null; test $? -eq 0</automated>
  </verify>
  <done>--self-test passes all five guard sub-tests, and the full-corpus dry-run confirms 17 injection files, zero unmapped platforms, and zero writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper script → corpus files | The script rewrites Markdown; a bug could corrupt content or write outside the runbook dirs |
| registry table → doc_id output | An incorrect path-join would stamp the wrong Doc ID into a runbook (C17 #9 catches, but late) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Tampering | retrofit-runbook.mjs write path | mitigate | Path allowlist (refuse any path outside docs/l1-runbooks//l2-runbooks/); --dry-run default in this plan writes nothing; batch plans git-diff-review before commit; C17 gate downstream |
| T-116-03 | Tampering | frontmatter / block generation | mitigate | doc_id and D1 label derived programmatically (never hand-transcribed); TEMPLATE-SENTINEL guard refuses 1970-01-01; last_verified carried verbatim (no content change) |
| T-116-SC | Tampering | package installs | accept | node-builtins-only; zero npm/pip/cargo installs; no third-party packages introduced, so no supply-chain surface (honest: this is a doc-reshape script) |
</threat_model>

<verification>
- `node scripts/pipeline/retrofit-runbook.mjs --self-test` exits 0.
- `node scripts/pipeline/retrofit-runbook.mjs --all --dry-run` exits 0, resolves all 75 doc_ids, flags exactly
  17 platform-injection files, and writes nothing.
- Script is node-builtins-only; D1_MAP byte-identical to c17-eee-contract.mjs lines 26-47.
</verification>

<success_criteria>
The mechanical retrofit helper exists, is node-builtins-only, and is proven by self-test + full-corpus dry-run
without touching the corpus or registry. Batch plans 116-02..116-08 can now invoke it.
</success_criteria>

<output>
Create `.planning/phases/116-l1-l2-runbook-retrofit-75-docs/116-01-SUMMARY.md` when done.
</output>
