---
phase: 55
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md
autonomous: true
requirements:
  - APP-04
  - APP-05
requirements_addressed:
  - APP-04
  - APP-05

must_haves:
  truths:
    - "docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md exists with `platform: macOS` (D-19) + `audience: admin` + `last_verified: 2026-04-28` + `review_by: 2026-06-27` frontmatter (V-55-07)"
    - "File body opens with verbatim `> **Platform applicability:**` blockquote at TOP within first 50 lines of body routing back to 00-overview + 3 sibling per-platform files (D-13 + V-55-26)"
    - "File covers all 6 macOS app-type variants per APP-04 + V-55-16: literals `LOB PKG`, `unmanaged PKG`, `DMG`, `Apple Developer ID Installer`, `VPP`, `Mac App Store`, `ABM` (or `Apple Business Manager`) all present"
    - "File contains a verbatim `> 📋 Community pattern — MEDIUM confidence` (or `- MEDIUM confidence`) blockquote callout per D-16 + APP-05 + V-55-17 with both `Installomator` AND `Intuneomator` literals within the blockquote scope"
    - "File body does NOT contain bare `> 📋` blockquotes without the `Community pattern` qualifier (V-55-17 NEGATIVE regression-guard for lexicon-family preservation per D-16)"
    - "File explicitly labels Installomator and Intuneomator as community OSS / non-Microsoft-supported tooling (NOT presented as supported delivery paths)"
    - "File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens (V-55-30)"
    - "File does NOT contain bare `> **Platform:**` token (V-55-27)"
  artifacts:
    - path: "docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md"
      provides: "macOS PKG / DMG / VPP / Mac App Store delivery pipeline guide with Installomator/Intuneomator MEDIUM-confidence community-pattern adjacency callout (APP-04 + APP-05)"
      contains:
        - "platform: macOS"
        - "audience: admin"
        - "last_verified: 2026-04-28"
        - "review_by: 2026-06-27"
        - "> **Platform applicability:**"
        - "LOB PKG"
        - "unmanaged PKG"
        - "DMG"
        - "Apple Developer ID Installer"
        - "VPP"
        - "Mac App Store"
        - "ABM"
        - "> 📋 Community pattern"
        - "MEDIUM confidence"
        - "Installomator"
        - "Intuneomator"
  key_links:
    - from: "docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md"
      to: "docs/operations/app-lifecycle/00-overview.md"
      via: "Platform applicability blockquote routing back to overview hub"
      pattern: "00-overview\\.md"
    - from: "docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md"
      to: "Microsoft Learn lob-apps-macos / macos-unmanaged-pkg / vpp-apps-ios"
      via: "External References footer"
      pattern: "learn\\.microsoft\\.com.*macos|vpp-apps"
---

<objective>
Author `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` — the macOS-specific app delivery pipeline guide covering APP-04 (all supported macOS app types: LOB PKG with Apple Developer ID Installer cert + signed; unmanaged PKG via Intune agent; DMG via Intune agent → /Applications; Microsoft 365 native VPP/managed; Mac App Store via ABM) and APP-05 (Installomator + Intuneomator as a `> 📋 Community pattern — MEDIUM confidence` adjacency callout with explicit non-Microsoft-supported labeling).

Purpose: Implements CONTEXT D-13 (cross-platform inline blockquote) + D-16 (Installomator/Intuneomator MEDIUM-confidence callout token form) + D-19 (`platform: macOS` frontmatter) + D-20 (pinned anchor strings) + CDI-Phase55-03 (no three-layer treatment — Phase 55 has zero hard-deadline items; APP-05 is a single-callout community attribution, not three-layer). Validator V-55-16/17/26/27/30/31 enforce structural correctness at commit time.

This file maps to APP-04 + APP-05 per REQ traceability lines 172-173. Wave 1 — parallel with 55-01 / 55-02 / 55-04 / 55-05 (no inter-file authoring dependency; cross-links resolve at validator runtime when full atomic commit lands per D-21).

Output: 1 new file at `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md`.

DO NOT commit yet — atomic commit owned by 55-07 author per CONTEXT D-21 + CDI-Phase55-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-app-lifecycle-automation/55-CONTEXT.md
@.planning/phases/55-app-lifecycle-automation/55-RESEARCH.md
@.planning/phases/55-app-lifecycle-automation/55-VALIDATION.md
@docs/operations/patch-management/02-macos-update-enforcement.md

<interfaces>
**File this plan creates:** `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md`

**Frontmatter contract (D-19 — single-string `platform: macOS`):**
```yaml
---
last_verified: 2026-04-28
review_by: 2026-06-27
applies_to: all
audience: admin
platform: macOS
---
```

**Validator V-55-NN assertions enforced against this file:**

| V-NN | Assertion |
|------|-----------|
| V-55-03 | File exists |
| V-55-07 | Frontmatter: `platform: macOS`, `audience:` non-empty, 60-day cycle |
| V-55-16 | macOS app-types coverage: literals `LOB PKG` AND `unmanaged PKG` AND `DMG` AND `Apple Developer ID Installer` AND `VPP` AND `Mac App Store` AND `ABM` (or `Apple Business Manager`) |
| V-55-17 | Literal `> 📋 Community pattern — MEDIUM confidence` (or `> 📋 Community pattern - MEDIUM confidence` — em-dash AND hyphen accepted) blockquote with `Installomator` AND `Intuneomator` literals within blockquote scope. NEGATIVE regression-guard: zero bare `> 📋` without `Community pattern` qualifier in body content |
| V-55-26 | `> **Platform applicability:**` blockquote within first 50 lines of body |
| V-55-27 | NEGATIVE: NO bare `> **Platform:**` token |
| V-55-30 | NEGATIVE: NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens |
| V-55-31 | SC#5 multi-platform frontmatter |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md (frontmatter + Platform applicability blockquote at TOP + H1 + intro + macOS app-type variants H2s for LOB PKG / unmanaged PKG / DMG / VPP / Mac App Store / ABM + Installomator/Intuneomator MEDIUM-confidence community-pattern callout + Related Resources/External References)</name>
  <files>docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md</files>
  <read_first>
    - .planning/phases/55-app-lifecycle-automation/55-CONTEXT.md (D-13 inline blockquote token; D-16 Installomator/Intuneomator MEDIUM-confidence callout literal token; D-19 platform: macOS; D-20 pinned anchor strings; CDI-Phase55-03 no three-layer treatment for any Phase 55 content)
    - .planning/phases/55-app-lifecycle-automation/55-RESEARCH.md (§4 macOS app deployment pipeline facts — LOB PKG Developer ID Installer cert + signed; unmanaged PKG via Intune agent + pre/post-install scripts; DMG via Intune agent → /Applications; PKG/DMG assignment limitation: Required only; Microsoft 365 native VPP/managed; Mac App Store via ABM; LOB PKG uninstall available only when "Install as managed" = Yes; §5 Installomator/Intuneomator community-pattern facts — MIT-licensed Bash command-line tool; Swift-based wrapper bridging 900+ Installomator labels)
    - .planning/phases/55-app-lifecycle-automation/55-VALIDATION.md (Per-Task Verification Map rows 55-03-01 through 55-03-04)
    - docs/operations/patch-management/02-macos-update-enforcement.md (Phase 54 macOS sibling — frontmatter shape; Platform applicability blockquote token at line 9; H2 structure pattern; Related Resources/External References footer)
  </read_first>
  <behavior>
    - File `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` exists
    - Frontmatter (D-19): `last_verified: 2026-04-28`, `review_by: 2026-06-27`, `applies_to: all`, `audience: admin`, `platform: macOS`
    - Platform applicability blockquote at TOP routing back to 00-overview + 3 sibling per-platform files using verbatim `> **Platform applicability:**` token (D-13 + V-55-26)
    - H1 followed by intro paragraph identifying scope (5 Microsoft-supported macOS app types + 1 community-pattern adjacency callout)
    - Body covers all 6 macOS app-type variants required by V-55-16:
      - **LOB PKG** (managed; Apple Developer ID Installer cert + signed)
      - **Unmanaged PKG** (via Intune agent; pre/post-install scripts available)
      - **DMG** (via Intune agent; mounts and copies .app to /Applications)
      - **VPP** (Microsoft 365 native; volume-purchased managed apps)
      - **Mac App Store** (via ABM / Apple Business Manager; silent install)
      - **ABM** / Apple Business Manager mentioned by name as the source for VPP and Mac App Store delivery
    - Dedicated `> 📋 Community pattern — MEDIUM confidence` blockquote callout (D-16; V-55-17) with both `Installomator` AND `Intuneomator` literal tokens within blockquote scope; explicit non-Microsoft-supported labeling
    - File body does NOT contain bare `> 📋` blockquotes without the `Community pattern` qualifier (V-55-17 NEGATIVE)
    - NO bare `> **Platform:**` token (V-55-27)
    - NO TBD/TODO/FIXME/XXX/PLACEHOLDER tokens (V-55-30)
  </behavior>
  <action>
Create the file `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` with the following content sections IN ORDER.

**Section 1 — Frontmatter (D-19; copy verbatim):**

```yaml
---
last_verified: 2026-04-28
review_by: 2026-06-27
applies_to: all
audience: admin
platform: macOS
---
```

**Section 2 — Platform applicability blockquote at TOP (D-13 + V-55-26; verbatim `> **Platform applicability:**` token):**

```markdown
> **Platform applicability:** This guide is macOS-specific (LOB PKG / unmanaged PKG / DMG / VPP /
> Mac App Store via ABM; community-pattern adjacency callout for Installomator and Intuneomator
> as non-Microsoft-supported tooling). For the cross-platform overview, see
> [App Lifecycle Overview](00-overview.md).
> **Windows:** See [Win32 / MSIX at Scale](01-windows-win32-msix-scale.md).
> **iOS/iPadOS:** See [iOS VPP Licensing](03-ios-vpp-licensing.md).
> **Android:** See [Android MGP Lifecycle](04-android-mgp-lifecycle.md).
```

**Section 3 — H1 + intro paragraph:**

```markdown
# macOS PKG / DMG Pipeline: Microsoft-Supported App Delivery + Community Adjacency

This guide is the macOS-specific app delivery reference covering all Microsoft-supported app
types for Intune-managed Macs: LOB PKG (managed; Apple Developer ID Installer-signed),
unmanaged PKG (via the Intune management agent), DMG (via the Intune management agent), VPP /
Microsoft 365 native (volume-purchased managed apps), and Mac App Store apps via ABM
(Apple Business Manager).

A separate community-pattern adjacency callout describes Installomator and Intuneomator as
**community-OSS automation tooling** that admins commonly run alongside Intune. These tools are
**NOT Microsoft-supported** and are documented here only as adjacent context — not as a
supported delivery path.

For the cross-platform overview, see [App Lifecycle Overview](00-overview.md).
```

**Section 4 — `## LOB PKG (Managed)` H2 (V-55-16 literals — `LOB PKG` + `Apple Developer ID Installer`):**

```markdown
## LOB PKG (Managed)

LOB PKG is the Microsoft-supported delivery method for Apple Developer ID Installer-signed PKG
files distributed as managed apps via Intune. Per Microsoft Learn, the .pkg file must:

- Be signed using an **Apple Developer ID Installer** certificate, obtained from an Apple
  Developer account.
- Be a component package or a package containing multiple packages.
- Not contain a bundle, disk image, or .app file.
- Contain a payload.

**Assignment limitation:** When using the LOB PKG (managed) deployment method, the only
assignment intent available is **Required**. Available-for-enrolled-devices and Uninstall
intents are NOT available.

**Uninstall behavior:** The Uninstall intent is only displayed for LOB apps created with
**Install as managed** set to **Yes**.

**Plan-author note:** Code-signing the .pkg file with the Developer ID Installer cert is the
prerequisite that distinguishes LOB PKG from unmanaged PKG. If the source publisher does not
provide a signed PKG, the unmanaged PKG path applies.
```

**Section 5 — `## Unmanaged PKG (via Intune Agent)` H2 (V-55-16 literal — `unmanaged PKG`):**

```markdown
## Unmanaged PKG (via Intune Agent)

Unmanaged PKG delivery covers the Microsoft-supported workflow for PKG files that the LOB PKG
workflow cannot install or that are not signed by an Apple Developer ID Installer certificate.
The Intune management agent for macOS is the delivery mechanism. Per Microsoft Learn:

- Pre-install and post-install scripts are available for unmanaged PKG apps installed via the
  Intune agent.
- The Intune management agent must be installed on the device (typically delivered via the
  enrollment workflow).
- Assignment intent: **Required** only (matches LOB PKG limitation).

Use unmanaged PKG when the source publisher does not provide a Developer ID Installer-signed
PKG, or when the LOB PKG workflow rejects the package due to bundle/disk-image/.app inclusion.
```

**Section 6 — `## DMG (via Intune Agent)` H2 (V-55-16 literal — `DMG`):**

```markdown
## DMG (via Intune Agent)

DMG delivery covers Microsoft-supported deployment of macOS disk-image apps via the Intune
management agent. The agent mounts the DMG and copies the .app bundle inside to the
Applications folder, supporting apps packaged as disk images containing one or more .app files.

Per Microsoft Learn:
- The Intune management agent for macOS must be installed on the device.
- Intune instructs the agent to mount the DMG and copy the .app to /Applications.
- Assignment intent: **Required** only (PKG/DMG share the Required-only constraint).

DMG is the preferred path when the source publisher distributes the app as a .dmg containing
a .app bundle (common for Mac developer tooling). DMG vs unmanaged PKG choice usually follows
the source publisher's distribution format.
```

**Section 7 — `## VPP (Microsoft 365 Native + Volume Purchased)` H2 (V-55-16 literals — `VPP`):**

```markdown
## VPP (Microsoft 365 Native + Volume Purchased)

VPP (Volume Purchase Program) covers volume-purchased apps delivered via the Apple Business
Manager (ABM) location-token integration with Intune. For macOS, VPP includes:

- **Microsoft 365 native VPP/managed apps** — Microsoft 365 macOS apps available via Intune as
  managed apps, syncing per the ABM token sync schedule.
- **Volume-purchased commercial apps** — Apps purchased through ABM are synced to Intune via
  the VPP token, allowing assignment to users or devices.

**Default license type:** When you create a new VPP assignment, the default license type is now
**device** (per Microsoft Learn, applies to both iOS and macOS contexts; existing assignments
unchanged).

**License migration:** Per Microsoft Learn, apps can migrate silently from user to device
licenses only when using **Required** assignment type. Apps cannot migrate from device to user
licenses for any assignment type.

For the broader iOS/iPadOS VPP device-vs-user licensing comparison and reclamation behavior,
see [iOS VPP Licensing](03-ios-vpp-licensing.md). Although this is the macOS file, the iOS guide
covers the licensing-model decision support that applies cross-platform; macOS-specific
reclamation has a 30-day grace period (where iOS does not).
```

**Section 8 — `## Mac App Store (via ABM)` H2 (V-55-16 literals — `Mac App Store`, `ABM`, `Apple Business Manager`):**

```markdown
## Mac App Store (via ABM)

Mac App Store apps are delivered to Intune-managed Macs via **Apple Business Manager (ABM)**.
ABM allows admins to obtain app licenses and synchronize them to Intune via a VPP token,
allowing silent installation of App Store apps on Macs without requiring users to sign in with
an Apple ID.

**Workflow:**
1. Purchase the Mac App Store app via Apple Business Manager.
2. Sync the ABM/VPP token to Intune (sync runs at scheduled intervals).
3. Assign the app to users or devices in Intune.
4. App installs silently on supervised Macs without Apple ID prompts.

**ABM token expiry:** The ABM/VPP location token has a 1-year expiry; renew via the ABM portal
before expiry to avoid sync failures.
```

**Section 9 — `## Community Pattern: Installomator / Intuneomator` H2 (D-16 + APP-05 + V-55-17 — verbatim `> 📋 Community pattern — MEDIUM confidence` blockquote):**

```markdown
## Community Pattern: Installomator / Intuneomator

> 📋 Community pattern — MEDIUM confidence
>
> **Installomator** is a community-OSS, MIT-licensed Bash command-line tool for macOS app
> install/update automation. **Intuneomator** is a Swift-based wrapper that bridges
> Installomator's 900+ application labels into Intune deployment workflows. Both projects are
> community-maintained on GitHub and are **NOT Microsoft-supported**.
>
> Many macOS administrators run these tools alongside Intune to automate app patching for the
> long tail of commercial macOS apps that lack VPP/ABM presence and that the source publisher
> does not redistribute as Developer ID Installer-signed PKGs. The community-pattern attribution
> here acknowledges their wide deployment but **does not endorse them as a supported delivery
> path**. Admins choosing this path should treat the tools as community OSS — version-pin the
> tool releases, audit the application labels for trust, and assume Microsoft Support will not
> assist with troubleshooting Installomator-driven app failures.
>
> - **Installomator** (GitHub: Installomator/Installomator; project page: installomator.com) —
>   Bash command-line tool; runs locally as part of an admin's automation script.
> - **Intuneomator** (GitHub: gilburns/Intuneomator) — Swift-based wrapper; bridges
>   Installomator labels into Intune `.intunemac` packaging workflows.

This callout is a **community-pattern adjacency** acknowledgment only. The Microsoft-supported
delivery paths for macOS apps are LOB PKG, unmanaged PKG, DMG, VPP, and Mac App Store via ABM
(documented in the H2 sections above). Admins should default to the Microsoft-supported paths
when feasible and treat Installomator/Intuneomator as a supplementary automation tool for apps
that cannot be packaged for the supported paths.
```

The literal `> 📋 Community pattern — MEDIUM confidence` is enforced by V-55-17 (em-dash and
hyphen both accepted by validator). Both `Installomator` AND `Intuneomator` literals MUST be
within the blockquote scope.

**V-55-17 NEGATIVE regression-guard:** No bare `> 📋` blockquote (without the `Community pattern`
qualifier) anywhere in the body. If you use additional informational callouts elsewhere in the
file, use a different blockquote prefix (e.g., `> ℹ️` or `> 📌` or unprefixed `> ` text);
ANY `> 📋` use must include the `Community pattern` qualifier.

**Section 10 — Related Resources + External References footer:**

```markdown
## Related Resources

- [App Lifecycle Overview](00-overview.md) — Cross-platform app lifecycle hub
- [Win32 / MSIX at Scale](01-windows-win32-msix-scale.md) — Windows sibling
- [iOS VPP Licensing](03-ios-vpp-licensing.md) — iOS sibling (VPP device-vs-user reclamation)
- [Android MGP Lifecycle](04-android-mgp-lifecycle.md) — Android sibling
- [macOS App Deployment](../../admin-setup-macos/04-app-deployment.md) — v1.2 macOS app deployment admin guide
- [macOS Update Enforcement](../patch-management/02-macos-update-enforcement.md) — Phase 54 macOS sibling

## External References

- [Add macOS LOB apps (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/intune-service/apps/lob-apps-macos)
- [Add unmanaged PKG apps (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/intune-service/apps/macos-unmanaged-pkg)
- [Apple VPP via Intune (Microsoft Learn)](https://learn.microsoft.com/en-us/intune/intune-service/apps/vpp-apps-ios)
- [Installomator (GitHub)](https://github.com/Installomator/Installomator) — Community OSS; not Microsoft-supported
- [Intuneomator (GitHub)](https://github.com/gilburns/Intuneomator) — Community OSS; not Microsoft-supported
- [Operations Documentation Index](../00-index.md) — Cross-reference only; Phase 55 does NOT amend the operations index per ROADMAP line 457
```

After authoring, verify the following grep checks pass:

```bash
grep -c "platform: macOS" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md           # expect 1
grep -c "^> \*\*Platform applicability:\*\*" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md  # expect 1
grep -c "LOB PKG" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md                   # expect >=1
grep -c "unmanaged PKG" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md             # expect >=1
grep -c "DMG" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md                       # expect >=1
grep -c "Apple Developer ID Installer" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md  # expect >=1
grep -c "VPP" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md                       # expect >=1
grep -c "Mac App Store" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md             # expect >=1
grep -E "ABM|Apple Business Manager" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md  # expect match
grep -c "Installomator" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md             # expect >=1
grep -c "Intuneomator" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md              # expect >=1
grep -E "> 📋 Community pattern.{0,5}MEDIUM confidence" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md  # expect match
grep -E "^> 📋 [^C]" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md                # MUST be empty (no bare > 📋 without "Community pattern")
grep -c "^> \*\*Platform:\*\*" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md      # MUST be 0 (V-55-27)
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md  # MUST be empty (V-55-30)
```

DO NOT commit yet — atomic commit owned by 55-07 author per CONTEXT D-21 + CDI-Phase55-05.
  </action>
  <verify>
    <automated>test -f docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -q "LOB PKG" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -q "unmanaged PKG" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -q "DMG" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -q "Apple Developer ID Installer" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -q "VPP" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -q "Mac App Store" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -qE "ABM|Apple Business Manager" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -q "Installomator" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -q "Intuneomator" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -qE "Community pattern.{0,5}MEDIUM confidence" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -q "platform: macOS" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; grep -q "^> \*\*Platform applicability:\*\*" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; ! grep -q "^> \*\*Platform:\*\*$" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` exists
    - Frontmatter contains `platform: macOS`, `audience: admin`, `last_verified: 2026-04-28`, `review_by: 2026-06-27`, `applies_to: all`
    - File contains literal `> **Platform applicability:**` blockquote within first 50 lines (V-55-26)
    - File contains all 6 macOS app-type variant literals required by V-55-16: `LOB PKG` AND `unmanaged PKG` AND `DMG` AND `Apple Developer ID Installer` AND `VPP` AND `Mac App Store` AND `ABM` (or `Apple Business Manager`)
    - File contains `> 📋 Community pattern — MEDIUM confidence` blockquote (em-dash OR hyphen accepted) (V-55-17)
    - Within the `> 📋 Community pattern — MEDIUM confidence` blockquote scope: `Installomator` AND `Intuneomator` literal tokens present
    - File body does NOT contain bare `> 📋` blockquotes without `Community pattern` qualifier (V-55-17 NEGATIVE)
    - Installomator and Intuneomator are explicitly labeled as community-OSS / non-Microsoft-supported
    - File does NOT contain bare `> **Platform:**` token (V-55-27)
    - File does NOT contain TBD/TODO/FIXME/XXX/PLACEHOLDER tokens (V-55-30)
    - `grep -c "platform: macOS" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` returns 1
    - `grep -c "^> \*\*Platform applicability:\*\*" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` returns 1
  </acceptance_criteria>
  <done>02-macos-pkg-dmg-pipeline.md authored covering all 6 macOS app-type variants per APP-04 + Installomator/Intuneomator MEDIUM-confidence community-pattern callout per APP-05 + D-16; V-55-03/07/16/17/26/27/30/31 assertions pass; no commit (55-07 owns atomicity).</done>
</task>

</tasks>

<threat_model>
None — Phase 55 is documentation-only. No code paths modified, no API surface added, no authentication/authorization changes, no data handling, no third-party dependency additions. Documentation accuracy is the only safety surface and is enforced by the literal-token regex validator (check-phase-55.mjs) + Microsoft Learn citation discipline + 60-day last_verified frontmatter cycle.
</threat_model>

<verification>
After Task 1 completes:

```bash
# File exists and structural elements
test -f docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md && echo "exists: PASS"

# Frontmatter
head -7 docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md | grep -E "^(platform|audience|last_verified|review_by|applies_to):"

# Platform applicability blockquote in first 50 lines (V-55-26)
sed -n '/^---$/,/^---$/!p' docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md | head -50 | grep -c "^> \*\*Platform applicability:\*\*"
# expect 1

# All 6 macOS app-type variants (V-55-16)
for token in "LOB PKG" "unmanaged PKG" "DMG" "Apple Developer ID Installer" "VPP" "Mac App Store"; do
  count=$(grep -c "$token" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md)
  echo "$token: $count (expect >=1)"
done
grep -E "ABM|Apple Business Manager" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md && echo "ABM/Apple Business Manager: PASS"

# Community pattern callout (V-55-17 POSITIVE)
grep -E "> 📋 Community pattern.{0,5}MEDIUM confidence" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md && echo "MEDIUM confidence callout: PASS"

# Installomator + Intuneomator within blockquote (V-55-17)
awk '/> 📋 Community pattern/,/^[^>]/' docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md | grep -E "Installomator" && echo "Installomator in callout: PASS"
awk '/> 📋 Community pattern/,/^[^>]/' docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md | grep -E "Intuneomator" && echo "Intuneomator in callout: PASS"

# V-55-17 NEGATIVE — no bare > 📋 without Community pattern qualifier
grep -E "^> 📋 [^C]" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md
# expect empty

# V-55-27 NEGATIVE bare-Platform
grep -c "^> \*\*Platform:\*\*" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md
# expect 0

# V-55-30 TBD scan
grep -E "\b(TBD|TODO|FIXME|XXX|PLACEHOLDER)\b" docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md
# expect empty
```

If 55-06 validator is also authored (Wave 2):

```bash
node scripts/validation/check-phase-55.mjs --verbose 2>&1 | grep -E "V-55-(03|07|16|17|26|27|30|31)"
```

DO NOT commit — atomic commit owned by 55-07 per CONTEXT D-21.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 55 Success Criteria #3 + REQUIREMENTS.md APP-04/05:

- **SC#3** satisfied: Admin reading 02-macos-pkg-dmg-pipeline.md sees Installomator and Intuneomator referenced as a `> 📋 Community pattern — MEDIUM confidence` adjacency callout with explicit non-Microsoft-supported labeling — not presented as a supported delivery path
- **APP-04** satisfied: All supported macOS app types covered (LOB PKG with Developer ID Installer cert; unmanaged PKG via Intune agent; DMG via Intune agent → /Applications; Microsoft 365 native VPP/managed; Mac App Store via ABM)
- **APP-05** satisfied: Confidence-attributed adjacency callout for Installomator + Intuneomator with non-Microsoft-supported labeling per D-16

Plan complete when:

- [ ] File exists at `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md`
- [ ] V-55-03 (file exists), V-55-07 (frontmatter), V-55-16 (macOS app-types), V-55-17 (Community pattern callout + Installomator + Intuneomator), V-55-26 (blockquote at TOP), V-55-27 (no bare-Platform), V-55-30 (TBD scan), V-55-31 (multi-platform frontmatter) all pass
- [ ] No commit — 55-07 owns atomicity per CONTEXT D-21
</success_criteria>

<output>
After completion, create `.planning/phases/55-app-lifecycle-automation/55-03-SUMMARY.md` documenting:

- File path + line count
- Sections authored (frontmatter, Platform applicability blockquote, H1 + intro, LOB PKG, Unmanaged PKG, DMG, VPP, Mac App Store, Community Pattern callout, Related Resources, External References)
- 6 macOS app-type variants covered (V-55-16 literal-token coverage)
- Community-pattern callout exact wording (CD-06 plan-author discretion exercised)
- V-55-NN assertions this file satisfies (V-55-03, V-55-07, V-55-16, V-55-17, V-55-26, V-55-27, V-55-30, V-55-31)
- Note: NO COMMIT — atomic commit owned by 55-07
</output>
</content>
