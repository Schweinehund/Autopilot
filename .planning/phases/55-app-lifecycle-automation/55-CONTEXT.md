# Phase 55: App Lifecycle Automation - Context

**Gathered:** 2026-04-28
**Status:** Ready for planning
**Methodology:** Adversarial review (Finder / Adversary / Referee scored pattern, Wave-of-3) on 4 gray areas covering 16 candidate sub-options across 95 enumerated defects (533 raw points: 11 CRIT × 10 + 24 MED × 5 + 60 LOW × 1 — distributed 135 / 155 / 126 / 57 across GA-1/2/3/4). Adversary attempted 25 disproves earning +109 points (32 / 69 / 2 / 6); 8 disproves were factually decisive (file-system verification — notably `01-windows-wufb-rings.md` actual H2-count refutation and `_glossary-ios.md` non-existence per LIN-02). Referee adopted 19 disproves and rejected 6, with severity adjustments on 4 defects (3 downgrades + 1 upgrade for SSoT and "operate" verb concerns). Net surviving defect score after rulings: **4/37/31/38 for GA-1**, **25/10/35/25 for GA-2**, **17/27/36/36 for GA-3**, **12/25/12 (4C)/11 (4D) for GA-4** — per-area winners: GA-1 = **1A** (three discrete top-level H2s; lowest at 4 surviving LOW-severity authoring-contract gaps); GA-2 = **2B** (comparison table + terminology disambiguation H2; 10 surviving MED defects all content-shape guidance not blockers); GA-3 = **3A** (2-column VPP attribute table with reclamation rows added); GA-4 = **4C-prime hybrid** (filename `04-android-mgp-lifecycle.md` retained for naming-convention parity + peer H2s for MGP-substantive + Zebra-OEMConfig-operational-summary-with-Phase-45-cross-link, mirroring Phase 54 sibling peer-H2 + cross-link pattern at lines 134 + 184).

<domain>
## Phase Boundary

Phase 55 delivers Multi-Platform App Lifecycle Automation documentation as a 5-file suite under `docs/operations/app-lifecycle/` plus a `check-phase-55.mjs` validator. **Phase 55 is the Wave B sibling of Phase 54** (concurrent execution; disjoint file sets within `operations/`); Phase 53 (Co-Management) is the methodology source for cross-platform inline-blockquote shape (D-08 inheritance), single-atomic-commit Goldilocks-rule (D-14 inheritance), and the operations/00-index.md cross-reference contract (DPO-Phase53-01 inheritance carried through DPO-Phase54-02). Phase 54 (Patch & Update Management) is the most-recent precedent for ops-domain phase shape: 1D Hybrid 00-overview + per-platform files + same-commit atomic landing + FULL-scope validator-as-deliverable. **Linux is OUT of scope for Phase 55** (no Linux app-lifecycle REQ — Linux app delivery is script-based per Phase 50 PITFALL-1 whitelist); Phase 55 covers 4 platforms only (Windows + macOS + iOS + Android).

**Six deliverables (8 active requirements: APP-01..08):**

1. `docs/operations/app-lifecycle/00-overview.md` (no specific REQ; ROADMAP line 290 deliverable framing) — **2B winner** (cross-platform comparison table + cross-cutting `## App-lifecycle terminology` concept H2 disambiguating supersedence/replacement/lifecycle/license-reclamation cross-platform). Size budget 200-350 lines (1D Hybrid inheritance from Phase 54 D-01). NO APP-01..08 substantive content duplicated (REQ traceability firewall enforced by V-55-NN NEGATIVE assertions paralleling V-54-29 — explicit grep-against tokens like `Win32ContentPrepTool`, `\.intunewin`, `Required assignment`, `Replace vs Update`, `Installomator`, `Intuneomator`, `OEMConfig`, `MGP private track`, `AMAPI`, `reclamation`). Cross-platform inline `> **Platform applicability:**` blockquote at TOP per Phase 54 D-04 token inheritance. Frontmatter `platform: cross-platform` per Phase 54 D-19 + CD-01 inheritance.

2. `docs/operations/app-lifecycle/01-windows-win32-msix-scale.md` (APP-01 + APP-02 fold + APP-03 fold) — **1A winner** (three discrete top-level H2s in document order: `## Supersedence` → `## Dependency Graphs` → `## ContentPrepTool Packaging`). PRIMARY: APP-01 supersedence chains (max 10 superseding apps; Replace vs Update semantics; targeting required on superseding app) AS `## Supersedence` H2 with **supersedence behavior matrix** (assignment type × supersedence action: Available vs Required × Uninstall vs Replace) + **PITFALL-10 Required-assignment exception as dedicated `> ⚠️` callout adjacent to behavior matrix** (per PITFALLS line 221 "dedicated callout, not buried in prose"). FOLD: APP-02 dependency graphs (max 100 dependencies; recursive; circular-detection) AS `## Dependency Graphs` H2 with **combined supersedence+dependency 10-node subgraph** illustration (REQ APP-02 verbatim "alongside supersedence content"). FOLD: APP-03 ContentPrepTool packaging (.intunewin format) AS `## ContentPrepTool Packaging` H2 covering 4 detection rule types (MSI product code / file / registry / custom PowerShell). MSIX = brief routing-only sub-section under packaging H2 (~5-10 lines pointing to Microsoft Learn for MSIX-specific guidance + scope-disclaimer note that MSIX does NOT support supersedence per STACK.md line 234). Cross-platform inline `> **Platform applicability:**` blockquote at TOP. `platform: Windows` frontmatter.

3. `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` (APP-04 + APP-05 fold) — PRIMARY: APP-04 macOS app delivery across all supported types (LOB PKG with Apple Developer ID Installer cert + signed; unmanaged PKG via Intune agent; DMG via Intune agent → /Applications; Microsoft 365 native VPP/managed; Mac App Store via ABM). FOLD: APP-05 Installomator/Intuneomator community-pattern adjacency callout — **`> 📋 Community pattern — MEDIUM confidence`** blockquote token (verbatim per REQUIREMENTS line 65 + ROADMAP line 291) with explicit non-Microsoft-supported labeling. NOT presented as a supported delivery path. Cross-platform inline `> **Platform applicability:**` blockquote at TOP. `platform: macOS` frontmatter.

4. `docs/operations/app-lifecycle/03-ios-vpp-licensing.md` (APP-06) — **3A winner** (2-column VPP-only attribute comparison table with reclamation rows added). Table columns: `VPP Device-Licensed | VPP User-Licensed`. Table rows: license source / Apple ID requirement / supervised silent install / **reclamation on retire-wipe** / **reclamation on remove-app** / BYOD applicability + 1-2 additional rows for reclamation grace period if applicable. SC#4 "distinguish AND reclamation" conjunction satisfied via column-by-column scan + dedicated reclamation rows. Reclamation behavior is **verified net-new** in Phase 55 (no "reclaim/reclamation" appears anywhere in v1.3 admin-setup-ios per file-system verification). Cross-link to v1.3 `docs/admin-setup-ios/05-app-deployment.md` for the broader 4-column app-type matrix (LOB / Store Apps cells) — link-not-copy contract. **NO Mermaid** (Phase 54 iOS sibling `03-ios-update-lifecycle.md` zero-Mermaid pattern parity preserved). Cross-platform inline `> **Platform applicability:**` blockquote at TOP. `platform: iOS` frontmatter.

5. `docs/operations/app-lifecycle/04-android-mgp-lifecycle.md` (APP-07 + APP-08 fold) — **4C-prime hybrid winner** (filename retained for naming-convention parity; two peer H2s in document order). PRIMARY: APP-07 MGP private-app publishing AS `## Managed Google Play Private App Publishing` H2 (substantive — direct APK upload to MGP private track + MGP web app for web-clip shortcuts + AMAPI custom-apps API change applicable since 2024 framing). FOLD: APP-08 Zebra OEMConfig AS `## Zebra OEMConfig APK Side-Load (NOT via MGP) {#zebra-oemconfig}` peer H2 with **operational-summary paragraph + 3-bullet operate-the-lifecycle list (update / revoke / troubleshoot) + explicit cross-link to `../../admin-setup-android/10-aosp-zebra.md`** for substantive Zebra OEMConfig content (Hardware Scope / Prerequisites / Provisioning / two-OEMConfig-app disambiguation table — Phase 45 SSoT preserved at 24,518 bytes; verified line 56 disclaimer "OEMConfig profile delivery is via Intune APK push (NOT Managed Google Play...)"). Pattern mirrors Phase 54 sibling `04-android-patch-delivery.md` peer H2 + cross-link shape (verified at lines 134 + 184). SC#5 conjunction (MGP AND OEMConfig AND "NOT via MGP") satisfied across two peer anchors. Cross-platform inline `> **Platform applicability:**` blockquote at TOP. `platform: Android` frontmatter.

6. `scripts/validation/check-phase-55.mjs` — AUDIT-06 validator-as-deliverable per Phase 48 D-25 file-reads-only contract + Phase 49 D-26 + Phase 50 D-25 + Phase 51 D-20 + Phase 52 D-11 + Phase 53 D-10 + Phase 54 D-17 regex-based pattern lineage. Full validator scope per Phase 54 D-17 precedent; estimated **30-34 V-55-NN structural assertions** (file existence × 5 + frontmatter local contract × 5 + per-platform literal-token coverage + supersedence behavior matrix + PITFALL-10 callout + dependency H2 + 10-node subgraph + ContentPrepTool .intunewin literal × 4 detection rule types + Installomator + Intuneomator + MEDIUM-confidence community-pattern callout literal + VPP device-vs-user reclamation literals × 2 + MGP private-app publishing + AMAPI 2024 + Zebra OEMConfig peer-H2 + "NOT via Managed Google Play" disclaimer + cross-link to 10-aosp-zebra.md + cross-platform inline blockquote × 5 files + bare-`> **Platform:**` NEGATIVE regression-guard + Mermaid-NEGATIVE regression-guard for 03-ios-vpp-licensing.md + 00-overview anti-scope-creep NEGATIVE regression-guard + ops/00-index.md NOT-amended NEGATIVE regression-guard + TBD/TODO scan).

Phase 56 (Drift Detection + Tenant Migration) consumes `operations/00-index.md` per ROADMAP line 457 + 290 (cross-reference-only); Phase 59 (Hub Navigation Integration) consumes Phase 55 anchors for `docs/index.md` Operations H2 expansion. Phase 55 MUST cross-reference `operations/00-index.md` from its own files, MUST NOT amend it (DPO-Phase53-01 + DPO-Phase54-02 + ROADMAP line 457 hotspot ownership rule). Phase 60 (Audit Harness v1.5 Finalization) registers `check-phase-55.mjs` in CI workflow + runs C13 broken-link automation against Phase 55 cross-link surface. Phase 58 (DEFER-08 4-platform capability comparison) WILL consume Phase 55 anchors for its App Deployment domain row (one of the canonical 6 axes per ROADMAP line 322; link-not-copy per PITFALL-7).

</domain>

<decisions>
## Implementation Decisions

### 00-overview.md scope shape (Gray Area 2 — winner: 2B)

- **D-01:** **`docs/operations/app-lifecycle/00-overview.md` follows 2B shape: cross-platform comparison table + `## App-lifecycle terminology` concept H2 + per-platform routing.** Per Adversary-Referee adjudication (10 surviving defect points; 2A=25, 2C=35, 2D=25). Reject 2A: F-2A-01 CRIT (Phase 54 D-01 1D Hybrid inheritance violation — no concept H2) + F-2A-02 CRIT (size-budget undershoot to ~80-100 lines, below 200-350 cap). Reject 2C: F-2C-01 CRIT (PITFALL-10 cross-platform mis-attribution — PITFALLS line 228-229 verbatim "authored as part of the Win32 section") + F-2C-02 CRIT (no cross-platform analog for Required-vs-Available — Win32-only concept) + F-2C-03 CRIT (direct collision with SC#1 callout location at `01-windows-win32-msix-scale.md`). Reject 2D: F-2D-02 CRIT (detection-method row imports APP-03 substantive content into 00-overview cross-platform space) + F-2D-03 CRIT (OEM-specific exception row imports APP-08 Zebra content — directly parallels Phase 54 D-01 line 44 firewall NEGATIVE assertion forbidding Zebra LifeGuard at 00-overview).
  - **Content scope:** (a) cross-platform comparison table with 5+ rows (packaging format / deployment model / supersedence support / dependency support / licensing model — concrete cross-platform concepts; columns = Windows / macOS / iOS / Android — 4 platforms × 5+ concept rows minimum); (b) `## App-lifecycle terminology` concept H2 disambiguating cross-platform terms (e.g., "lifecycle states: install / supersede / retire" or "assignment intents: required / available / uninstall" — picking a genuinely cross-platform triad analogous to Phase 54's deferral/enforcement/attestation; must NOT use single-platform tokens like "sideload" Android-only or "Replace vs Update" Win32-only); (c) per-platform routing cross-links to all 4 per-platform files.
  - **Size budget:** 200-350 lines hard cap (Phase 54 D-01 inheritance — 1.4-2.4× Phase 53 145-line baseline).
  - **Anti-scope-creep regression-guard:** V-55-NN NEGATIVE assertion: 00-overview MUST NOT contain `Win32ContentPrepTool`, `\.intunewin`, `Required assignment`, `Replace vs Update`, `Installomator`, `Intuneomator`, `OEMConfig`, `MGP private track`, `AMAPI`, `reclamation` literal tokens (all per-platform-file territory). REQ traceability table at REQUIREMENTS lines 169-176 maps zero APP requirements to 00-overview — structural firewall paralleling V-54-29.
- **D-02:** **Concept H2 must be genuinely cross-platform.** Per F-2B-03 + F-2B-04 surviving guidance: terminology H2 restricted to cross-platform concepts with active mechanisms on at least 3 of 4 platforms. Forbidden anchor topics: "supersedence" (Win32-only — APP-01 territory), "Replace vs Update" (Win32-only), "sideload" (Android-only — APP-08 territory), "reclamation" (iOS-VPP-specific — APP-06 territory). Permitted anchor topics: lifecycle-state taxonomy, assignment-intent taxonomy, packaging-vs-distribution-vs-lifecycle layered concepts (if scoped to NOT import per-platform substantive content).
- **D-03:** **Frontmatter `platform: cross-platform`** for 00-overview per Phase 54 D-19 + CD-01 inheritance recommendation (single new token; cleaner than comma-string; forward-compatible with future Phase 60 C-check expansion). Validator V-55-07 accepts either single-token `cross-platform` or comma-string `Windows,macOS,iOS,Android`.

### Win32 file H2 architecture (Gray Area 1 — winner: 1A)

- **D-04:** **`docs/operations/app-lifecycle/01-windows-win32-msix-scale.md` follows 1A shape: three discrete top-level H2s in document order — `## Supersedence` → `## Dependency Graphs` → `## ContentPrepTool Packaging`.** Per Adversary-Referee adjudication (4 surviving defect points; 1B=37, 1C=31, 1D=38). Reject 1B: F-1B-01 CRIT (demoting APP-02 to H3 violates REQ APP-02 + SC#2 verbatim "discrete H2 section") + F-1B-02 CRIT (MSIX coequal H2 fabricates scope — STACK.md line 234 verbatim "MSIX apps do NOT support supersedence"). Reject 1C: F-1C-01 CRIT (combined `## App Versioning` H2 violates "discrete H2 alongside" — alongside requires two adjacent peer H2s, not one merged H2). Reject 1D: F-1D-01 CRIT (same combining violation) + F-1D-02 CRIT (dedicated MSIX H2 fabricates scope, no APP REQ basis).
  - **`## Supersedence` H2 content (APP-01 PRIMARY):** supersedence behavior matrix (rows = `Available assignment` / `Required assignment`; columns = `Uninstall` / `Replace`; cells = behavior outcomes including PITFALL-10 Required-assignment exception); max 10 superseding apps per app limit; Replace vs Update option semantics (Replace = "uninstall previous version" enabled; Update = disabled); targeting required on superseding app contract; **PITFALL-10 Required-assignment exception as dedicated `> ⚠️` callout placed immediately adjacent to behavior matrix** (PITFALLS line 221 "dedicated callout, not buried in prose"). Validator V-55-NN-A asserts callout literal-pin within ~30-line window of `## Supersedence` H2.
  - **`## Dependency Graphs` H2 content (APP-02 FOLD):** max 100 dependencies per app; recursive application; circular-dependency detection (Intune blocks at config time but admin can misconfigure); **combined supersedence+dependency 10-node subgraph** illustration showing both relationship types in single visual artifact (REQ APP-02 verbatim "combined supersedence+dependency 10-node subgraph"). Subgraph format: ASCII art OR Mermaid OR static image — plan-author discretion (CD-04 inheritance from Phase 54 CD-04). Validator V-55-NN-B asserts max-100 literal token + circular-detection literal token + 10-node subgraph artifact presence (regex check: at least 10 nodes in some text/artifact within H2 scope).
  - **`## ContentPrepTool Packaging` H2 content (APP-03):** Win32ContentPrepTool current GA version (v1.8.7 per SUMMARY line 47 — verify at plan-time); `.intunewin` format conversion workflow; **4 detection rule types covered: MSI product code / file / registry / custom PowerShell script** (REQ APP-03 verbatim). MSIX scope = brief routing-only sub-section (~5-10 lines pointing to Microsoft Learn for MSIX-specific guidance + scope-disclaimer note "MSIX does NOT support supersedence; use version replacement in the same app entry instead" verbatim from STACK.md line 234). Validator V-55-NN-C asserts ContentPrepTool literal + .intunewin literal + 4 detection rule type literals + MSIX-no-supersedence disclaimer literal.
- **D-05:** **PITFALL-10 callout placement = inside `## Supersedence` H2, immediately adjacent to behavior matrix.** Per F-1A-03 surviving authoring-contract gap resolution. Plan author MUST place the dedicated `> ⚠️ **Required-assignment exception:**` callout within ~10 lines of the supersedence behavior matrix (NOT at file top, NOT in `## Dependency Graphs` H2, NOT in `## ContentPrepTool Packaging` H2). Validator V-55-NN-A enforces literal-token pin "Required-assignment exception" within line range of supersedence behavior matrix anchor.
- **D-06:** **Anti-duplication contract with `docs/reference/win32-app-packaging.md`.** Per F-1C-08 / F-1D-08 surviving concern. Existing reference doc already covers detection rule priority order + install order and dependencies + anti-pattern warning. Phase 55 `## ContentPrepTool Packaging` H2 MUST cross-reference (NOT duplicate) the reference doc. Cross-reference contract: "For detection rule priority and conflict resolution, see [Win32 App Packaging Reference](../../reference/win32-app-packaging.md)." Validator V-55-NN asserts cross-link exists. NOTE: `win32-app-packaging.md` line 95 "Maximum dependency depth: 10 levels" is **chain depth** (orthogonal to APP-02's "max 100 dependencies per app" which is total count) — both facts coexist; do NOT conflate (Adversary-confirmed file-system finding).

### iOS VPP comparison shape (Gray Area 3 — winner: 3A)

- **D-07:** **`docs/operations/app-lifecycle/03-ios-vpp-licensing.md` follows 3A shape: 2-column VPP attribute comparison table.** Per Adversary-Referee adjudication (17 surviving defect points; 3B=27, 3C=36, 3D=36). Reject 3B: F-3B-01 CRIT (Phase 54 iOS sibling `03-ios-update-lifecycle.md` zero-Mermaid pattern parity break — verified file-system; entire `docs/operations/patch-management/` has zero Mermaid). Reject 3C: F-3C-01 CRIT (4×5 paragraph-blob cells fail L2 audience scan-time) + F-3C-02 CRIT ("distinguish" conjunct buried at cell-text level rather than column-header level — direct SC#4 fail). Reject 3D: F-3D-01 CRIT (scan-time distinguish failure with prose) + F-3D-02 CRIT (multiline-regex prose pin violates Phase 54 LOCKED line-anchored pattern).
  - **Table shape:** 2 columns (`VPP Device-Licensed` | `VPP User-Licensed`); 6+ rows: license source / Apple ID requirement / supervised silent install / **reclamation on retire-wipe** / **reclamation on remove-app** / BYOD applicability + 1-2 optional rows for reclamation grace period or ABM revocation behavior at plan-author discretion.
  - **Reclamation conjunct (SC#4 second conjunct):** **VERIFIED NET-NEW** in Phase 55. File-system scan confirms zero "reclaim/reclamation" tokens anywhere in `docs/admin-setup-ios/`. APP-06 reclamation behavior (retire/wipe returns device license; remove app returns user license) is genuinely Phase 55 territory. Reclamation rows are the dominant net-new content and MUST be present. Validator V-55-NN-D asserts literal-pin coverage: `retire/wipe` AND `device license` (or `device-license`) within reclamation row context; `remove app` (or `remove-app`) AND `user license` within reclamation row context.
  - **Anti-duplication mitigation:** Cross-link to v1.3 `docs/admin-setup-ios/05-app-deployment.md` lines 51-64 (4-column app-type comparison table covering VPP Device-Licensed + VPP User-Licensed + LOB + Store Apps × 12 attributes) for the broader app-type matrix. Phase 55 file is **deepening** the VPP slice, NOT duplicating — distinct contribution: reclamation behavior + decision-support framing. Cross-link contract: "For the full 4-column app-type comparison covering LOB and Store Apps alongside VPP Device-Licensed and VPP User-Licensed, see [iOS App Deployment](../../admin-setup-ios/05-app-deployment.md)."
  - **NO Mermaid:** Phase 54 iOS sibling pattern parity preserved (verified zero Mermaid in 03-ios-update-lifecycle.md AND entire docs/operations/patch-management/ tree). Validator V-55-NN-E NEGATIVE regression-guard: zero Mermaid blocks (` ``` `mermaid` ` ``` `) in 03-ios-vpp-licensing.md.
- **D-08:** **BYOD/Corporate × Supervised/Unsupervised dimensions reused from v1.3 cross-reference, NOT re-encoded.** Per F-3C-05 surviving concern + Plan-Author Open Question. The existing v1.3 silent-install boundary table at `05-app-deployment.md:36-43` already encodes 6 BYOD/Corp × user/device-licensed × supervised/unsupervised scenarios. Phase 55 03-ios-vpp-licensing.md table embeds these as value-cell qualifiers (e.g., "Yes (supervised) / Partial (unsupervised)") rather than authoring a separate sub-matrix. User Enrollment as third state (per `09-mam-app-protection.md` + `08-user-enrollment.md`) covered in BYOD-applicability row prose with cross-link.
- **D-09:** **Mermaid forbidden in Phase 55 iOS file.** Per D-07 + F-3B-01 CRIT inheritance. Plan author has structural-discretion on Mermaid in OTHER Phase 55 files (00-overview can include Mermaid per Phase 54 CD-04 author-discretion inheritance; 01-windows-win32-msix-scale.md can include Mermaid for combined 10-node subgraph artifact per D-04 CD-04 inheritance), but `03-ios-vpp-licensing.md` MUST be Mermaid-free for iOS-doc-pattern parity.

### Android MGP + Zebra OEMConfig file architecture (Gray Area 4 — winner: 4C-prime hybrid)

- **D-10:** **`docs/operations/app-lifecycle/04-android-mgp-lifecycle.md` follows 4C-prime hybrid shape: filename retained for naming-convention parity + two peer top-level H2s + Phase 45 cross-link for substantive Zebra OEMConfig content.** Per Adversary-Referee adjudication (4C=8 + filename-brevity hybrid; 4A=12, 4B=25, 4D=11). Reject 4B: F-4B-01 CRIT (sub-H3 demotion under MGP H2 fails SC#5 conjunction at H2-anchor level + Phase 56 cross-link target stability) + F-4B-02 MED (Phase 54 sibling uses peer H2s at lines 134 + 162). Reject 4D-pure-stub: F-4D-01 HIGH-escalated (pure cross-link stub fails SC#5 "operate" verb — APP-08 verbatim "operate the OEMConfig app lifecycle (update, revoke, troubleshoot)"). Reject 4A-substantive-Zebra: F-4A-02 HIGH-escalated (substantive Zebra duplicates Phase 45 admin guide 24,518 bytes — SSoT violation).
  - **`## Managed Google Play Private App Publishing` H2 content (APP-07 PRIMARY):** direct APK upload to MGP private track; MGP web app for web-clip shortcuts; **AMAPI custom-apps-via-Google-Play API change applicable since 2024** framing (NOT a hard-deadline; soft historical context). Validator V-55-NN-F asserts literal-pin: `Managed Google Play` + `private track` (or `private app track`) + `web app` (or `web clip`) + `AMAPI` + `2024` literals.
  - **`## Zebra OEMConfig APK Side-Load (NOT via MGP) {#zebra-oemconfig}` H2 content (APP-08 FOLD — operational summary):** **operational-summary paragraph** (~3-5 lines describing the Zebra OEMConfig surface and the load-bearing exception that OEMConfig delivery does NOT use MGP) + **3-bullet operate-the-lifecycle list** (update / revoke / troubleshoot — discharges APP-08 SC#5 "operate" verb) + **explicit cross-link to `../../admin-setup-android/10-aosp-zebra.md`** for substantive Zebra OEMConfig content (Hardware Scope L29 / Prerequisites L42 / Provisioning L59 / OEMConfig APK Push L110 / two-OEMConfig-app disambiguation L116-119 — Phase 45 SSoT preserved at 24,518 bytes). Pattern explicitly mirrors Phase 54 sibling `04-android-patch-delivery.md` peer-H2-plus-cross-link shape (verified at lines 134 substantive Zebra LifeGuard + 184 cross-link to Phase 45 Knox guide). SC#5 conjunction (MGP AND OEMConfig AND "NOT via MGP" disclaimer) satisfied across two peer anchors. Validator V-55-NN-G asserts literal-pin: `OEMConfig` + `APK side-load` (or `APK push`) + `NOT via Managed Google Play` (or `NOT Managed Google Play`) + cross-link to `10-aosp-zebra.md` literal.
- **D-11:** **AMAPI 2024 framing placement = inside `## Managed Google Play Private App Publishing` H2 body content, NOT at file top.** Per F-4A-04 surviving concern. AMAPI is a soft historical context (already-past 2024 API change), not a hard deadline. Single inline mention with year reference is sufficient — no `> ⚠️` blockquote, no three-layer treatment.
- **D-12:** **Filename `04-android-mgp-lifecycle.md` retained verbatim** per REQ traceability table line 175 + ROADMAP line 293. Filename advertises MGP-primary scope; the OEMConfig peer H2 inside the file is the load-bearing exception that admins discover via TOC scan. Filename brevity preferred over filename-as-conjunction-disambiguator (rejected 4C-pure with verbose `04-android-mgp-and-oemconfig-lifecycle.md`).

### Cross-platform routing pattern across per-platform files (inheritance from Phase 54 D-04)

- **D-13:** **Each of `00-overview.md` / `01-windows-win32-msix-scale.md` / `02-macos-pkg-dmg-pipeline.md` / `03-ios-vpp-licensing.md` / `04-android-mgp-lifecycle.md` carries an inline `> **Platform applicability:**` blockquote at the TOP of the file** (line 9, after frontmatter) per Phase 54 D-04 + DPO-Phase54-01 inheritance. Content per file: brief one-line platform identifier + back-link to `00-overview.md` for full cross-platform comparison + ≤3 sub-bullets pointing to the other three sibling per-platform files. The blockquote is pointer-only (link-not-copy contract — full comparison content lives in 00-overview per D-01). Verified Phase 54 implementation at `docs/operations/patch-management/00-overview.md:9` + `01-windows-wufb-rings.md:9` + `02-macos-update-enforcement.md:9` + `03-ios-update-lifecycle.md:9` + `04-android-patch-delivery.md:9` — all 5 files use the established `> **Platform applicability:**` token verbatim.
  - **Critical token discipline:** Use established `> **Platform applicability:**` token verbatim. Bare `> **Platform:**` is **forbidden** (zero corpus precedent; Phase 54 D-04 lexicon-family preservation rule inherited). Lexicon-family preservation enforced by V-55-NN NEGATIVE regression-guard (paralleling V-54-27).
- **D-14:** **Cross-platform routing surface centralization at `00-overview.md`** per D-01 + D-13 coupling. The full 4-platform comparison table + cross-platform terminology concept H2 + per-platform routing live in 00-overview only; per-platform files carry only pointer-shape inline blockquote. NOT a new decision; direct Phase 54 D-05 inheritance.

### No hard-deadline items in Phase 55

- **D-15:** **Phase 55 has ZERO hard-deadline items** — no three-layer (table-cell + adjacent blockquote + per-occurrence inline reminder) treatment needed in any Phase 55 file. Confirmed across all 8 active REQs:
  - APP-01/02/03 Win32 (supersedence/dependency/ContentPrepTool): no time-bounded deadline; max-10 / max-100 / detection-rule semantics are policy-stable
  - APP-04 macOS app delivery: no deadline; PKG/DMG semantics stable
  - APP-05 Installomator/Intuneomator: community-pattern attribution, not deadline
  - APP-06 iOS VPP licensing: no deadline; reclamation behavior is steady-state semantics
  - APP-07 AMAPI 2024 API change: already-past soft cutover, not a hard deadline (April 2024 effective; documented as historical context per D-11)
  - APP-08 Zebra OEMConfig: stable mechanism; no deadline
  - This contrasts with Phase 54 which had 2 hard-deadline items (Apple OS 26 macOS legacy MDM removal + Android MEETS_STRONG_INTEGRITY Oct 2026). Phase 55 inherits Phase 54 D-13 three-layer pattern by NOT applying it.
- **D-16:** **Confidence-attribution callout pattern (APP-05 Installomator/Intuneomator).** Per REQUIREMENTS line 65 + ROADMAP line 291 verbatim: `> 📋 Community pattern — MEDIUM confidence` blockquote token. New token-family entry in the corpus (Phase 52 D-01 freshness-caveat blockquote `> LOW-MEDIUM confidence` is the closest precedent). Validator V-55-NN-H asserts literal-pin: `> 📋 Community pattern — MEDIUM confidence` (or `> 📋 Community pattern - MEDIUM confidence` — both em-dash and hyphen accepted) within `02-macos-pkg-dmg-pipeline.md` body. NEGATIVE regression-guard: bare `> 📋` without `Community pattern` qualifier forbidden (lexicon-family preservation paralleling D-13 platform-applicability pattern).

### Validator scope + commit atomicity + frontmatter platform contract

- **D-17:** **`check-phase-55.mjs` validator scope = FULL** per Phase 54 D-17 + Phase 53 D-10 22-26 V-NN floor precedent. File-reads-only / no-shared-module per Phase 48 D-25; regex-based parsing per Phase 49 D-26 + Phase 51 D-20 + Phase 52 D-11 + Phase 53 D-11 + Phase 54 D-18. Estimated **30-34 V-55-NN structural assertions:**
  - **V-55-01..06 File existence:** 5 app-lifecycle content files + validator self-reference + cross-reference targets (10-aosp-zebra.md + 05-app-deployment.md + win32-app-packaging.md)
  - **V-55-07 Frontmatter local contract (per D-13 Phase 54 inheritance):** 5 app-lifecycle files carry single-string `platform: <Windows|macOS|iOS|Android|cross-platform>` + `audience: admin` (or `admin,L2` per CD-11 inheritance) + 60-day `last_verified` + `review_by`. NOTE: 00-overview platform-tag = `cross-platform` per D-03 (or comma-string accepted per V-54-31 inheritance).
  - **V-55-08 Cross-platform comparison table presence (per D-01 2B):** `00-overview.md` contains a table with columns matching `Windows | macOS | iOS | Android` (or transpose with platforms as rows; validator accepts either orientation via literal-token regex).
  - **V-55-09 App-lifecycle terminology concept H2 (per D-01 2B):** `00-overview.md` contains literal H2 `## App-lifecycle terminology` (or equivalent regex-pinned pattern with at least 3 cross-platform terminology tokens within ~30-line window).
  - **V-55-10 Anti-scope-creep regression-guard at 00-overview (per D-02 firewall):** `00-overview.md` does NOT contain literal tokens `Win32ContentPrepTool`, `\.intunewin`, `Required assignment` (full literal — single occurrences in cross-platform-comparison-table context excluded), `Replace vs Update`, `Installomator`, `Intuneomator`, `OEMConfig`, `MGP private track`, `AMAPI`, `reclamation`. Anti-duplication firewall paralleling V-54-29.
  - **V-55-11 Supersedence H2 + behavior matrix presence (per D-04 + SC#1):** `01-windows-win32-msix-scale.md` contains literal H2 `## Supersedence` AND a markdown table within ~30-line window with cells covering `Available` / `Required` × `Uninstall` / `Replace` (or equivalent literal-token coverage).
  - **V-55-12 PITFALL-10 Required-assignment callout (per D-04 + D-05 + SC#1):** `01-windows-win32-msix-scale.md` contains literal `> ⚠️` blockquote with `Required-assignment exception` (or `Required assignment exception`) within ~10 lines of supersedence behavior matrix anchor.
  - **V-55-13 Dependency Graphs H2 + max-100 + 10-node subgraph (per D-04 + SC#2):** `01-windows-win32-msix-scale.md` contains literal H2 `## Dependency Graphs` AND `100 dependencies` (or `max 100`) literal AND `circular` literal AND ≥10-node graph artifact (Mermaid OR ASCII OR image-link).
  - **V-55-14 ContentPrepTool H2 + 4 detection rule types (per D-04 + APP-03):** `01-windows-win32-msix-scale.md` contains literal H2 `## ContentPrepTool Packaging` AND `\.intunewin` AND all 4 detection rule type literals: `MSI product code` AND `file` AND `registry` AND `PowerShell` (case-insensitive partial matches on `MSI`, `file`, `registry`, `PowerShell` in detection-rule context).
  - **V-55-15 MSIX-no-supersedence disclaimer (per D-04 + STACK.md:234):** `01-windows-win32-msix-scale.md` contains literal `MSIX` AND `does NOT support supersedence` (or `do NOT support supersedence`) within `## ContentPrepTool Packaging` H2 scope.
  - **V-55-16 macOS app-types coverage (per APP-04):** `02-macos-pkg-dmg-pipeline.md` contains literal `LOB PKG` AND `unmanaged PKG` AND `DMG` AND `Apple Developer ID Installer` AND `VPP` AND `Mac App Store` AND `ABM` (or `Apple Business Manager`) literal coverage.
  - **V-55-17 Installomator/Intuneomator MEDIUM-confidence callout (per D-16 + APP-05):** `02-macos-pkg-dmg-pipeline.md` contains literal `> 📋 Community pattern — MEDIUM confidence` (or `- MEDIUM confidence`) blockquote with `Installomator` AND `Intuneomator` literals within blockquote scope. NEGATIVE regression-guard: zero bare `> 📋` without `Community pattern` qualifier in body content.
  - **V-55-18 iOS VPP comparison table presence (per D-07 + SC#4):** `03-ios-vpp-licensing.md` contains a 2-column comparison table (or 2-column-equivalent dl-list) with `VPP Device-Licensed` (or `Device-Licensed`) AND `VPP User-Licensed` (or `User-Licensed`) column headers.
  - **V-55-19 iOS VPP reclamation literals (per D-07 + APP-06):** `03-ios-vpp-licensing.md` contains `retire/wipe` (or `retire and wipe`) AND `device license` (or `device-license`) within reclamation context AND `remove app` (or `remove-app`) AND `user license` (or `user-license`) within reclamation context.
  - **V-55-20 iOS no-Mermaid NEGATIVE regression-guard (per D-09):** `03-ios-vpp-licensing.md` contains zero Mermaid code blocks (` ``` `mermaid ... ``` ` ` patterns).
  - **V-55-21 iOS cross-link to v1.3 app deployment (per D-07 anti-duplication mitigation):** `03-ios-vpp-licensing.md` contains literal cross-link to `05-app-deployment.md` (relative path: `../../admin-setup-ios/05-app-deployment.md`).
  - **V-55-22 Android MGP H2 + AMAPI 2024 (per D-10 + APP-07):** `04-android-mgp-lifecycle.md` contains literal H2 `## Managed Google Play Private App Publishing` (or equivalent regex-pinned pattern) AND `private track` (or `private app track`) AND `web app` (or `web clip`) AND `AMAPI` AND `2024` literals.
  - **V-55-23 Android Zebra OEMConfig peer H2 + NOT-via-MGP disclaimer (per D-10 + APP-08 + SC#5):** `04-android-mgp-lifecycle.md` contains literal H2 `## Zebra OEMConfig APK Side-Load` (with optional `(NOT via MGP)` parenthetical) AND `OEMConfig` AND `APK side-load` (or `APK push`) AND `NOT via Managed Google Play` (or `NOT Managed Google Play`) literals.
  - **V-55-24 Android Zebra cross-link to Phase 45 admin guide (per D-10 SSoT):** `04-android-mgp-lifecycle.md` contains literal cross-link to `10-aosp-zebra.md` (relative path: `../../admin-setup-android/10-aosp-zebra.md`).
  - **V-55-25 Android Zebra operate-the-lifecycle 3-bullet list (per D-10 + SC#5 "operate" verb):** `04-android-mgp-lifecycle.md` contains literal `update` AND `revoke` AND `troubleshoot` within Zebra OEMConfig H2 scope (verbs satisfying APP-08 "operate the OEMConfig app lifecycle (update, revoke, troubleshoot)").
  - **V-55-26 Cross-platform inline blockquote at TOP (per D-13 + Phase 54 D-04 inheritance):** each of 5 app-lifecycle files contains literal `> **Platform applicability:**` blockquote within first 50 lines of file body (post-frontmatter). Paralleling V-54-26.
  - **V-55-27 Bare-`> **Platform:**` NEGATIVE regression-guard (per D-13):** zero files (across the entire `docs/` + `.planning/` tree) match literal `> **Platform:**` (with no qualifier word). Paralleling V-54-27.
  - **V-55-28 ops/00-index.md NOT amended NEGATIVE regression-guard (per DPO-Phase53-01 + DPO-Phase54-02):** `docs/operations/00-index.md` does NOT contain `## App Lifecycle` H2 (Phase 53 owns the file; Phase 55 cross-references only). Phase 59 will add this H2 in its own commit per ROADMAP line 463.
  - **V-55-29 win32-app-packaging.md cross-link presence (per D-06 anti-duplication mitigation):** `01-windows-win32-msix-scale.md` contains literal cross-link to `win32-app-packaging.md` (relative path: `../../reference/win32-app-packaging.md`).
  - **V-55-30 TBD/TODO/PLACEHOLDER scan (per V-53-25 + V-54-30 inheritance):** none of Phase 55 files contain literal `TBD`, `TODO`, `FIXME`, `XXX`, `PLACEHOLDER` outside Version History tables.
  - **V-55-31 SC#5 multi-platform frontmatter (per ROADMAP line 295):** all 5 app-lifecycle ops files carry valid `platform:` frontmatter; 00-overview's value is whatever D-03 + CD-01 resolves.
  - **V-55-32 v1.4.1 atomicity discipline cross-link (per D-19 + RETROSPECTIVE.md line 166):** plan-time check that all Phase 55 deliverables (5 content files + validator + cross-link consistency) are staged in same commit (verified at pre-commit gate; not a runtime literal-pin assertion).
- **D-18:** **`check-phase-55.mjs` implementation pattern matches `check-phase-54.mjs` / `check-phase-53.mjs` / `check-phase-52.mjs` / `check-phase-51.mjs` / `check-phase-50.mjs` / `check-phase-49.mjs`** (Phase 48 D-25 + lineage). File-reads-only / no-shared-module; markdown parsing is regex-based.
- **D-19:** **All 5 app-lifecycle content files use single-string `platform:` frontmatter (per Phase 54 D-19 inheritance lineage).** 01 = `Windows`, 02 = `macOS`, 03 = `iOS`, 04 = `Android`. 00-overview.md = `cross-platform` per D-03 + CD-01 inheritance. NOTE on C10: harness C10 check has hardcoded scope `linuxDocPaths()` per `scripts/validation/v1.5-milestone-audit.mjs` lines 141-178; operations/app-lifecycle/ files are NOT in C10 scope, so frontmatter is enforced ONLY by the local V-55-07 validator assertion, not by C10 (CDI-Phase55-04 inheritance from CDI-Phase54-04).
- **D-20:** **Hardcoded H2/anchor strings + literal-token regexes pinned in BOTH the validator (`check-phase-55.mjs`) and CONTEXT.md.** Brittleness trade-off accepted per Phase 49 D-25 + Phase 50 D-25 + Phase 51 D-21 + Phase 52 D-12 + Phase 53 D-13 + Phase 54 D-20 precedent. Renaming any pinned H2, comparison-table column, supersedence behavior matrix cell, deadline date, or PITFALL-10 callout token requires same-commit validator update.
- **D-21:** **Commit atomicity = SINGLE atomic commit** per Phase 51 D-22 + Phase 52 D-13 + Phase 53 D-14 + Phase 54 D-21 + CDI-Phase55-05 inheritance + ROADMAP line 271 v1.4.1 atomicity lesson. Phase 55 has NO retrofit obligations (unlike Phase 54's PATCH-06 same-commit-atomic at admin-setup-ios/07-device-enrollment.md), so single-commit landing is mechanical: 5 new content files + validator + VERIFICATION (separate commit per Phase 49/50/51/52/53/54 close pattern). Single atomic commit covers: 5 new content files (app-lifecycle/00-overview + 01-windows-win32-msix-scale + 02-macos-pkg-dmg-pipeline + 03-ios-vpp-licensing + 04-android-mgp-lifecycle) + 1 validator (check-phase-55.mjs).

### Plan-level ordering and atomicity

- **D-22:** **Phase 55 plan order (single-commit atomicity per D-21):**
  1. **Authoring wave (parallel-safe across files; sequential within hot-spot):** All content + validator can be authored concurrently in worktrees. Phase 55 has `Depends on: Phase 53` per ROADMAP line 286 (operations/00-index.md must exist for cross-reference). Phase 53 closed on 2026-04-28. Suggested plan-level decomposition (7 plans — Phase 55 has fewer plans than Phase 54's 9 because Phase 55 has zero retrofit + zero REQ errata obligations):
     - **55-01:** `docs/operations/app-lifecycle/00-overview.md` (D-01 2B winner; cross-platform comparison table with 5+ rows + `## App-lifecycle terminology` concept H2 with cross-platform-only triad + routing cross-links to 01-04 + cross-platform inline blockquote per D-13 + frontmatter `platform: cross-platform` per D-03)
     - **55-02:** `docs/operations/app-lifecycle/01-windows-win32-msix-scale.md` (APP-01/02/03 fold; D-04 1A winner — three discrete top-level H2s; D-05 PITFALL-10 callout adjacent to behavior matrix; D-06 cross-link to win32-app-packaging.md; combined 10-node subgraph artifact; cross-platform inline blockquote per D-13 + frontmatter `platform: Windows`)
     - **55-03:** `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` (APP-04 + APP-05 fold; all 6 macOS app-type variants per V-55-16 + D-16 Installomator/Intuneomator MEDIUM-confidence callout literal; cross-platform inline blockquote + frontmatter `platform: macOS`)
     - **55-04:** `docs/operations/app-lifecycle/03-ios-vpp-licensing.md` (APP-06; D-07 3A winner — 2-column VPP attribute table with reclamation rows; D-08 BYOD/supervision dimensions reused from v1.3 cross-reference; D-09 zero-Mermaid; V-55-19 reclamation literals; cross-link to v1.3 05-app-deployment.md per D-07; cross-platform inline blockquote + frontmatter `platform: iOS`)
     - **55-05:** `docs/operations/app-lifecycle/04-android-mgp-lifecycle.md` (APP-07 + APP-08 fold; D-10 4C-prime hybrid winner — peer H2s for MGP-substantive + Zebra-OEMConfig-operational-summary + cross-link to 10-aosp-zebra.md; D-11 AMAPI 2024 framing inline; D-12 filename retained; V-55-22..25 literal coverage; cross-platform inline blockquote + frontmatter `platform: Android`)
     - **55-06:** `scripts/validation/check-phase-55.mjs` (D-17 V-55-01..32 structural assertions; D-18 file-reads-only pattern; estimated 30-34 checks)
     - **55-07:** pre-commit gate (3 validators) + single atomic commit per D-21 + VERIFICATION.md authored (separate commit per close pattern)
  2. **Pre-commit gate:**
     - `node scripts/validation/check-phase-55.mjs` exits 0 (all 30-34 V-55-NN checks pass)
     - `node scripts/validation/v1.5-milestone-audit.mjs --verbose` exits 0 (C1-C12 PASS; C13 informational PASS-or-noise within accepted tolerance per Phase 48 D-08; C11 informational — no false positives expected at Phase 55 ship per CDI-Phase55-03)
     - `node scripts/validation/regenerate-supervision-pins.mjs --self-test` exits 0 (regression-prevention; should be unaffected since Phase 55 files are not in supervision sidecar)
     - `markdown-link-check` against the 5 new content files + cross-link targets (informational; not blocking per Phase 48 D-08)
     - Verify all 5 new files contain `last_verified` + `review_by` frontmatter on 60-day cycle
     - Verify all 5 new files contain `> **Platform applicability:**` blockquote at TOP (per D-13 + V-55-26)
     - Verify `02-macos-pkg-dmg-pipeline.md` contains `> 📋 Community pattern — MEDIUM confidence` callout (per D-16 + V-55-17)
  3. **Single atomic commit:** All 5 new app-lifecycle files + validator per D-21 + commit message (suggested):
     ```
     docs(55): app-lifecycle 5-file suite + check-phase-55 validator

     - 5 new ops files: operations/app-lifecycle/00-overview through 04-android-mgp-lifecycle
     - APP-01/02/03 Win32 supersedence + dependency + ContentPrepTool packaging fold
       (three discrete top-level H2s; PITFALL-10 Required-assignment callout adjacent to behavior matrix)
     - APP-04/05 macOS PKG/DMG pipeline + Installomator/Intuneomator MEDIUM-confidence community callout
     - APP-06 iOS VPP device-vs-user licensing with reclamation rows (net-new vs v1.3)
     - APP-07/08 Android MGP private-app publishing + Zebra OEMConfig peer-H2 with cross-link to Phase 45 SSoT
     - 30-34 V-55-NN structural assertions in check-phase-55.mjs
     - Per Phase 54 1D Hybrid + 2B + 1A + 3A + 4C-prime adversarial-review winners
     ```
  4. **Post-commit verification:** Re-run `check-phase-55.mjs` + `v1.5-milestone-audit.mjs` from clean workspace.
  5. **VERIFICATION.md authored** (separate commit per Phase 49/50/51/52/53/54 close pattern) documenting validator pass + DPO inheritance verification + 5 SC# satisfaction proof + DPO-Phase55-NN propagation summary for Phase 56+ plan authors.

### Claude's Discretion

- **CD-01:** Frontmatter `platform:` value for `00-overview.md` — `cross-platform` (single new token per Phase 54 CD-01 inheritance recommendation) OR `Windows,macOS,iOS,Android` (comma-string aligning with SC#5 ROADMAP line 295 "correct multi-platform frontmatter"). Validator V-55-07 + V-55-31 accept either. Recommendation: `platform: cross-platform`.
- **CD-02:** Exact wording of the 4 per-platform sub-bullets inside the `> **Platform applicability:**` blockquote on each per-platform file (D-13 specifies form + back-link to 00-overview; specific verb choice and brevity is plan-author discretion).
- **CD-03:** Exact column count of the cross-platform comparison table in `00-overview.md` (D-01 mandates 5+ rows × 4 platforms; plan-author may add 6th row at discretion). Validator V-55-08 accepts any orientation.
- **CD-04:** Whether the combined 10-node supersedence+dependency subgraph in `01-windows-win32-msix-scale.md` is rendered as Mermaid, ASCII art, or static image — plan-author discretion. Validator V-55-13 accepts any artifact form satisfying ≥10-node count regex.
- **CD-05:** Whether `00-overview.md` includes a Mermaid diagram (e.g., 4-platform-app-lifecycle-comparison) — Phase 54 CD-04 author discretion inheritance. Validator does NOT assert Mermaid presence at 00-overview. NOTE: 03-ios-vpp-licensing.md is Mermaid-FORBIDDEN per D-09 (different from 00-overview which is permissive).
- **CD-06:** Exact wording of the `> 📋 Community pattern — MEDIUM confidence` Installomator/Intuneomator callout (D-16 specifies token form + token literal; specific phrasing within community-pattern attribution is plan-author discretion).
- **CD-07:** Exact column choice for the iOS VPP comparison table (D-07 specifies 6+ rows; plan-author may add or rename rows — e.g., "Reclamation grace period" vs "Reclamation latency" — at discretion). Validator V-55-19 accepts either via literal-token coverage of `retire/wipe` + `device license` + `remove app` + `user license` regardless of row labels.
- **CD-08:** Exact text of the operate-the-lifecycle 3-bullet list at `04-android-mgp-lifecycle.md` `## Zebra OEMConfig` H2 (D-10 specifies update / revoke / troubleshoot tokens; specific phrasing within operational-summary contract is plan-author discretion). Validator V-55-25 accepts either via literal-token coverage of `update` + `revoke` + `troubleshoot`.
- **CD-09:** Whether the cross-platform terminology concept H2 in 00-overview uses "lifecycle states: install / supersede / retire" OR "assignment intents: required / available / uninstall" OR "packaging-vs-distribution-vs-lifecycle layered concepts" framing — plan-author discretion within D-02 anti-scope-creep constraint (forbidden: "supersedence" Win32-only / "Replace vs Update" Win32-only / "sideload" Android-only / "reclamation" iOS-only as anchor topics; permitted: cross-platform concepts with active mechanisms on ≥3 of 4 platforms).
- **CD-10:** Whether `00-overview.md` carries `audience: admin` (default per Phase 54 CD-11 lineage) OR `audience: admin,L2` (cross-platform overview may serve L2 escalation cross-references) — plan-author discretion. Validator V-55-07 accepts any non-empty `audience:` field.
- **CD-11:** Whether the AMAPI 2024 framing in 04-android-mgp-lifecycle.md uses "applicable since 2024" vs "as of April 2024" vs "since 2024" exact phrasing — plan-author discretion. Validator V-55-22 accepts any via `AMAPI` + `2024` literal coverage.

### Cross-decision integration callouts (Referee Part C)

- **CDI-Phase55-01:** **D-13 (cross-platform inline at top of all 5 app-lifecycle files) × D-21 (single atomic commit) × V-55-26/27.** Plan author for 55-01..05 MUST include `> **Platform applicability:**` blockquote at TOP of every file (per Phase 54 D-04 token inheritance). V-55-26 enforces presence; V-55-27 enforces NEGATIVE regression-guard against bare `> **Platform:**` token (lexicon-family preservation paralleling Phase 54 V-54-27). The bare-noun token is a load-bearing FORBIDDEN pattern inherited from Phase 54.
- **CDI-Phase55-02:** **D-04 (1A three discrete top-level H2s) × D-05 (PITFALL-10 callout placement) × V-55-11/12.** Plan author for 55-02 MUST place the dedicated `> ⚠️ Required-assignment exception:` callout within ~10 lines of the supersedence behavior matrix INSIDE `## Supersedence` H2 (NOT at file top, NOT in `## Dependency Graphs` H2). V-55-11 + V-55-12 jointly enforce H2 + matrix + adjacent callout. PITFALL-10's "dedicated callout, not buried in prose" mandate (PITFALLS line 221) is satisfied by H2-adjacent placement.
- **CDI-Phase55-03:** **D-15 (zero hard-deadline items in Phase 55) × Phase 54 D-13 inheritance.** Phase 55 inherits Phase 54 HARD-DEADLINE three-layer pattern by NOT applying it. AMAPI 2024 (D-11) + Installomator MEDIUM-confidence (D-16) + reclamation behavior semantics (D-07) are all single-callout items, not three-layer. Plan authors for 55-01..05 MUST NOT introduce three-layer treatment for any Phase 55 content; that pattern is reserved for genuine HARD-DEADLINE items per Phase 54 D-13 + Phase 52 D-01 lineage.
- **CDI-Phase55-04:** **D-19 (`platform:` frontmatter for all 5 app-lifecycle files) × C10 hardcoded `linuxDocPaths()` × harness C-check expansion at Phase 60.** Phase 55 frontmatter is enforced ONLY by V-55-07 local assertion. C10 doesn't run on operations/app-lifecycle/ files (verified at `scripts/validation/v1.5-milestone-audit.mjs` lines 141-178). Phase 60 (audit harness finalization) MAY expand a C-check to ops files; if so, Phase 60 plan author handles allowlist seeding per Phase 48 D-15 lazy-add at THAT time, not at Phase 55. Phase 55 is forward-compatible. Direct CDI-Phase54-04 inheritance.
- **CDI-Phase55-05:** **D-21 (single atomic commit) × no retrofit obligations × commit-decomposition simplicity.** Phase 55 has NO same-commit-atomic forced couplings (unlike Phase 54's PATCH-06 retrofit at admin-setup-ios/07-device-enrollment.md:35). Plan author for 55-07 MUST stage 5 new content files + validator in single atomic commit; pre-commit hook enforces. Decomposition is mechanical: 7 plans (vs Phase 54's 9) because Phase 55 has zero retrofit + zero REQ errata + zero forward-link insertions.
- **CDI-Phase55-06:** **D-07 (3A iOS shape) × D-09 (Mermaid forbidden in iOS file) × Phase 54 iOS sibling pattern parity.** Phase 54 iOS file `03-ios-update-lifecycle.md` uses zero Mermaid; entire docs/operations/patch-management/ tree uses zero Mermaid; v1.3 admin-setup-ios uses zero Mermaid. Phase 55 03-ios-vpp-licensing.md inherits this iOS-doc-pattern. Plan author for 55-04 MUST NOT introduce Mermaid; V-55-20 NEGATIVE regression-guard enforces. Other Phase 55 files (00-overview + 01-windows + 02-macos + 04-android) have Mermaid-discretion per CD-05 + CD-04 inheritance.
- **CDI-Phase55-07:** **D-10 (4C-prime hybrid for Android file) × Phase 45 SSoT preservation × SC#5 "operate" verb satisfaction.** Plan author for 55-05 MUST split Zebra OEMConfig content into operational-summary in Phase 55 file (~3-5 lines + 3-bullet operate-the-lifecycle list) + cross-link to Phase 45 admin guide for substantive content. V-55-23 + V-55-24 + V-55-25 jointly enforce: peer H2 with NOT-via-MGP disclaimer + cross-link to 10-aosp-zebra.md + 3 operational verbs (update / revoke / troubleshoot). Pure stub fails SC#5 "operate" verb (rejected 4D); substantive Zebra duplicates Phase 45 SSoT (rejected 4A); sub-H3 demotion fails Phase 54 sibling peer-H2 precedent (rejected 4B).
- **CDI-Phase55-08:** **D-01 (00-overview 2B scope) × D-02 (cross-platform-only terminology constraint) × V-55-10 anti-scope-creep regression-guard × REQ traceability firewall.** REQ traceability table (REQUIREMENTS lines 169-176) maps every APP-01..08 to a specific per-platform file; ZERO APP requirements are mapped to 00-overview. Plan author for 55-01 MUST NOT include `Win32ContentPrepTool`, `\.intunewin`, `Required assignment`, `Replace vs Update`, `Installomator`, `Intuneomator`, `OEMConfig`, `MGP private track`, `AMAPI`, `reclamation` in 00-overview body content beyond cross-platform comparison table cells (and even there, prefer concept-level descriptors over per-platform-tool-name literals). V-55-10 NEGATIVE regression-guard enforces. Direct parallel of Phase 54 V-54-29.

### Downstream phase obligations (Referee Part C — for next-phase plan authors)

- **DPO-Phase55-01:** **Phase 56 (Drift Detection + Tenant Migration) inherits Phase 55 cross-platform inline blockquote pattern.** Phase 56 documentation under `docs/operations/drift-migration/` SHOULD adopt the same `> **Platform applicability:**` token reuse + 5-file-per-platform shape with 00-overview hub. NOT mandated; per Phase 53 DPO-Phase53-02 + Phase 54 DPO-Phase54-01 inheritance (per-phase author discretion within methodology family). Phase 56 plan author considers adopting the same shape for v1.5 ops-domain consistency.
- **DPO-Phase55-02:** **Phase 56 inherits Phase 55 ops/00-index.md cross-reference contract.** Phase 56 documentation under `docs/operations/drift-migration/` MUST cross-reference `docs/operations/00-index.md` from its own files; MUST NOT amend `00-index.md` per ROADMAP line 457 verbatim "Phase 53 creates; Phases 54–56 cross-reference only." Phase 59 (Hub Navigation Integration) appends Operations H2 to `docs/index.md` linking to `00-index.md` AND adds Phase 54/55/56 sub-references in `00-index.md` body content.
- **DPO-Phase55-03:** **Phase 56 (Drift Detection) MAY cross-reference Phase 55 anchors for app-install-regression drift signals.** Phase 56 ROADMAP line 302 covers cross-platform compliance drift signals (Windows app install regression among others). Phase 56 plan author should consider cross-referencing Phase 55 `01-windows-win32-msix-scale.md#supersedence-matrix` + `04-android-mgp-lifecycle.md#zebra-oemconfig` as drift-signal sources. NOT mandated; per-phase author discretion.
- **DPO-Phase55-04:** **Phase 60 (Audit Harness v1.5 Finalization) registers `check-phase-55.mjs` in CI workflow + runs C13 broken-link automation against Phase 55 cross-link surface.** Phase 60 plan author adds `check-phase-55.mjs` to `.github/workflows/audit-harness-v1.5-integrity.yml` per AUDIT-06 contract. C13 informational broken-link automation runs against Phase 55 cross-link surface (~12-18 cross-links per file: forward-links to per-platform files + back-links to 00-overview + cross-platform inline blockquote sub-bullets + Phase 45 admin-setup-android/10-aosp-zebra.md cross-link + v1.3 admin-setup-ios/05-app-deployment.md cross-link + win32-app-packaging.md cross-link + Phase 53 cross-management cross-references + Microsoft Learn external URLs). Phase 60 plan author runs C13 sweep against Phase 55 outputs as part of v1.5 close audit. **Optional sidecar amendment**: if Phase 60 expands C10 scope to operations/app-lifecycle/ files, lazy-add `c10_ops_allowlist[]` sidecar entry per Phase 48 D-15 YAGNI at THAT time (NOT at Phase 55 ship per CDI-Phase55-04). Also: if Phase 60 expands C11 ops-domain regex to flag confidence-attribution token clusters (`Community pattern`, `MEDIUM confidence`, etc.), sidecar `c11_app_lifecycle_allowlist[]` lazy-add per CDI-Phase55-03.
- **DPO-Phase55-05:** **Phase 58 (DEFER-08 4-platform capability comparison) consumes Phase 55 anchors for App Deployment domain row.** ROADMAP line 322 lists 6 canonical domain axes (Enrollment, Configuration, App Deployment, Compliance, Software Updates, Conditional Access) — App Deployment IS in the canonical 6-axis list, so Phase 58 WILL consume Phase 55 anchors. Link-not-copy contract (PITFALL-7) means cells must hyperlink to Phase 55 H2 anchors (`00-overview.md#cross-platform-comparison`, `00-overview.md#app-lifecycle-terminology`, `01-windows-win32-msix-scale.md#supersedence`, `01-windows-win32-msix-scale.md#dependency-graphs`, `01-windows-win32-msix-scale.md#contentpreptool-packaging`, `02-macos-pkg-dmg-pipeline.md` (full file or H2 anchors), `03-ios-vpp-licensing.md` (full file or VPP-licensing-table anchor), `04-android-mgp-lifecycle.md#managed-google-play-private-app-publishing`, `04-android-mgp-lifecycle.md#zebra-oemconfig`) — NOT duplicate prose. Phase 58 plan author MUST verify these anchors stable post-Phase-55 close.
- **DPO-Phase55-06:** **Phase 59 (Hub Navigation Integration) appends operations App Lifecycle H2 to `docs/index.md` linking to `00-overview.md` + `00-index.md`.** Phase 59 ownership per ROADMAP line 457 navigation-files-last principle. Phase 55 does NOT touch `docs/index.md`. Phase 59 also adds app-lifecycle quick-ref entries in `docs/quick-ref-l2.md` Operations sections IF those exist (per Phase 59 ROADMAP line 332-336 hub integration scope).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase 55 source documents
- `.planning/REQUIREMENTS.md` lines 60-68 — APP-01..08 verbatim definitions
- `.planning/REQUIREMENTS.md` lines 169-176 — APP-01..08 traceability mapping per file
- `.planning/ROADMAP.md` lines 284-295 — Phase 55 goal + SC#1-5 verbatim + dependencies
- `.planning/research/SUMMARY.md` lines 47, 80-81 — Win32 ContentPrepTool v1.8.7, Hotpatch May 2026 (cross-Phase-54 reference)
- `.planning/research/FEATURES.md` lines 108, 150-153, 161-162, 168 — App lifecycle research per platform
- `.planning/research/PITFALLS.md` lines 212-230 — PITFALL-10 Win32 supersedence Required-assignment exception (canonical framing for D-04 + D-05)
- `.planning/research/STACK.md` line 234 — MSIX does NOT support supersedence (canonical framing for D-04 + V-55-15)

### Phase 54 inheritance (most recent ops-domain precedent)
- `.planning/phases/54-patch-update-management/54-CONTEXT.md` — full Phase 54 decisions (D-01..22, CD-01..11, CDI-Phase54-01..08, DPO-Phase54-01..06)
- `docs/operations/patch-management/00-overview.md` — Phase 54 1D Hybrid 00-overview implementation reference (218 lines; comparison table + 3 concept H2s + routing)
- `docs/operations/patch-management/01-windows-wufb-rings.md` — Phase 54 Windows-platform file reference (multi-H2 structure + PITFALL-9 ring-disambiguation)
- `docs/operations/patch-management/03-ios-update-lifecycle.md` — Phase 54 iOS sibling reference (zero Mermaid; D-09 pattern parity source)
- `docs/operations/patch-management/04-android-patch-delivery.md` — Phase 54 Android sibling reference (peer H2s for OEM exceptions at lines 134 + 162; cross-link pattern at line 184; D-10 4C-prime template source)
- `scripts/validation/check-phase-54.mjs` — validator pattern reference for D-17 V-55-NN structural assertions

### Phase 55 cross-link targets (link-not-copy)
- `docs/admin-setup-android/10-aosp-zebra.md` — Phase 45 v1.4.1 Zebra OEMConfig admin guide (24,518 bytes; substantive OEMConfig content; line 56 disclaimer "OEMConfig profile delivery is via Intune APK push (NOT Managed Google Play...)" — D-10 SSoT source for V-55-24 cross-link target)
- `docs/admin-setup-ios/05-app-deployment.md` — v1.3 iOS app deployment guide (4-column app-type comparison at lines 51-64; silent-install scenarios at lines 36-43; device-vs-user prose at lines 24-28 — D-07 anti-duplication mitigation source for V-55-21 cross-link target)
- `docs/admin-setup-ios/04-configuration-profiles.md` — v1.3 iOS configuration profiles (referenced by Phase 54 D-08 PITFALL-13 forward-link; orthogonal to Phase 55)
- `docs/admin-setup-macos/04-app-deployment.md` — v1.2 macOS app deployment (APP-04 cross-reference target)
- `docs/admin-setup-android/01-managed-google-play.md` — v1.4 MGP binding admin guide (anti-duplication awareness for 04-android-mgp-lifecycle.md MGP H2 — Phase 55 covers private-app *publishing* lifecycle, NOT MGP tenant binding)
- `docs/reference/win32-app-packaging.md` — existing Win32 packaging reference (detection rule priority + dependency depth — D-06 anti-duplication mitigation source for V-55-29 cross-link target)
- `docs/operations/00-index.md` — operations directory index (Phase 53 owns; Phase 55 cross-references but does NOT amend per DPO-Phase53-01 + DPO-Phase54-02 + V-55-28 NEGATIVE regression-guard)

### v1.5 audit harness
- `scripts/validation/v1.5-milestone-audit.mjs` — milestone audit harness (C10 hardcoded `linuxDocPaths()` scope at lines 141-178; CDI-Phase55-04 source)
- `scripts/validation/v1.5-audit-allowlist.json` — sidecar allowlist (Phase 60 lazy-add target per DPO-Phase55-04)
- `.github/workflows/audit-harness-v1.5-integrity.yml` — CI workflow (Phase 60 registers check-phase-55.mjs per DPO-Phase55-04)

### Glossaries (verified file-system findings)
- `docs/_glossary.md` (Windows) — exists
- `docs/_glossary-macos.md` (macOS + iOS terminology folded per LIN-02) — exists
- `docs/_glossary-android.md` (Android) — exists
- `docs/_glossary-linux.md` (Linux) — exists
- **NO `_glossary-ios.md` exists** — iOS terminology lives inside `_glossary-macos.md` per REQUIREMENTS line 144 LIN-02 (load-bearing fact for F-2B-02 disprove)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **Phase 54 validator pattern (`check-phase-54.mjs`)**: 32 V-54-NN regex-based file-reads-only assertions. Phase 55 inherits structure verbatim (file existence + frontmatter + per-platform literal-token coverage + cross-platform inline blockquote × 5 + bare-noun NEGATIVE regression-guard + ops/00-index.md NOT-amended NEGATIVE regression-guard + TBD/TODO scan).
- **Phase 54 1D Hybrid 00-overview shape (`docs/operations/patch-management/00-overview.md`)**: 218 lines, 5-row × 4-platform comparison table + 3 concept H2s (Ring Terminology, Deferral vs Enforcement, Cross-Platform Planning Considerations) + routing. Phase 55 00-overview inherits structure verbatim (target ~200-260 lines: comparison table + 1-2 concept H2s + routing).
- **Phase 54 Android sibling peer-H2-plus-cross-link pattern (`docs/operations/patch-management/04-android-patch-delivery.md` lines 134 + 184)**: substantive Zebra LifeGuard H2 + cross-link to admin-setup. Phase 55 04-android-mgp-lifecycle.md inherits this pattern verbatim (substantive MGP H2 + Zebra OEMConfig peer H2 with operational summary + cross-link to Phase 45 SSoT).
- **Phase 54 cross-platform inline blockquote (`> **Platform applicability:**` token at line 9)**: verified across all 5 Phase 54 files. Phase 55 inherits token verbatim.

### Established Patterns
- **Single atomic commit per ops-domain phase** (Phase 51 D-22 + 52 D-13 + 53 D-14 + 54 D-21): 5 content files + validator land in one commit; VERIFICATION.md in separate commit per close pattern. Phase 55 has no retrofit obligations so decomposition is mechanical (7 plans).
- **File-reads-only regex-based validator** (Phase 48 D-25 + 49 D-26 + 50 D-25 + 51 D-20 + 52 D-11 + 53 D-11 + 54 D-18): no shared modules, regex-pinned literal tokens, hardcoded H2/anchor strings. Phase 55 V-55-NN follows.
- **60-day `last_verified` cycle** (Phase 49 D-04 + cascading): all Phase 55 files frontmatter-locked.
- **REQ traceability firewall at 00-overview** (Phase 54 D-01 line 44): NEGATIVE-assertion regex-guard against per-platform substantive content. Phase 55 V-55-10 inherits + extends with APP-specific token list.
- **Cross-platform inline blockquote at line 9 every operations/ file** (Phase 54 D-04 + V-54-26/27): `> **Platform applicability:**` verbatim; bare `> **Platform:**` forbidden (lexicon-family preservation).

### Integration Points
- `docs/operations/00-index.md` — Phase 55 cross-references (DPO-Phase53-01 + DPO-Phase54-02 + V-55-28 NEGATIVE regression-guard ensures NO `## App Lifecycle` H2 added; Phase 59 owns hub integration)
- `docs/admin-setup-android/10-aosp-zebra.md` — Phase 55 04-android-mgp-lifecycle.md cross-links (V-55-24 enforces literal cross-link target stability)
- `docs/admin-setup-ios/05-app-deployment.md` — Phase 55 03-ios-vpp-licensing.md cross-links (V-55-21 enforces literal cross-link target stability)
- `docs/reference/win32-app-packaging.md` — Phase 55 01-windows-win32-msix-scale.md cross-links (V-55-29 enforces literal cross-link target stability)
- `scripts/validation/v1.5-milestone-audit.mjs` — Phase 60 will register check-phase-55.mjs in CI workflow (DPO-Phase55-04)
- Phase 56 will consume Phase 55 anchors for app-install-regression drift signals (DPO-Phase55-03)
- Phase 58 will consume Phase 55 anchors for App Deployment domain row in 4-platform comparison (DPO-Phase55-05)
- Phase 59 will append operations App Lifecycle H2 to `docs/index.md` linking to `00-overview.md` (DPO-Phase55-06)

</code_context>

<specifics>
## Specific Ideas

- **Methodology endorsement (user direction during discussion):** "for each choice in each of the areas, use /adversarial-review to recommend the best one and provide your reasoning" — invoked the adversarial-review skill (Finder / Adversary / Referee scored pattern) per Phase 54 precedent. 12 agents across 3 sequential waves (4 parallel Finders + 4 parallel Adversaries + 4 parallel Referees). Per-area winners selected by surviving-defect-point arithmetic.
- **Verified file-system findings that shaped decisions:**
  - `01-windows-wufb-rings.md` actual H2-count is 6-7 (depending on boilerplate inclusion), NOT 4 as Finder claimed — refuted "four-H2 precedent" claims in F-1A-01/F-1B-04/F-1C-04 (Adversary disprove sustained on all three by Referee).
  - Only 4 platform glossaries exist (no `_glossary-ios.md`); none define supersedence/dependency/sideload as glossary terms — refuted F-2B-02 glossary-collision CRIT (Adversary disprove sustained by Referee).
  - `docs/admin-setup-android/10-aosp-zebra.md` exists at 24,518 bytes with substantive OEMConfig content and line 56 "NOT Managed Google Play" disclaimer verbatim — confirms 4D cross-link target viability and 4A SSoT-violation severity.
  - `docs/operations/patch-management/04-android-patch-delivery.md` line 134 `## Zebra LifeGuard OTA` + line 162 `## Samsung Knox Service Plugin` are PEER H2s — confirms Phase 54 sibling shape that Phase 55 4C-prime hybrid inherits.
  - License reclamation behavior (APP-06 conjunct) is **verified net-new** — no "reclaim/reclamation" anywhere in v1.3 admin-setup-ios/. Reclamation rows are the dominant Phase 55 contribution to iOS file.
  - Phase 54 iOS sibling `03-ios-update-lifecycle.md` has zero Mermaid blocks — confirmed F-3B-01 CRIT pattern parity defect; D-09 forbids Mermaid in 03-ios-vpp-licensing.md.
  - `docs/operations/patch-management/00-overview.md` is 218 lines with table + 3 concept H2s — confirms 1D Hybrid baseline; Phase 55 2B target ~200-260 lines.

</specifics>

<deferred>
## Deferred Ideas

- **MSIX substantive content** (CD-04 inheritance): MSIX as a coequal scope was rejected across 1B/1D candidates because APP-01/02/03 are Win32-only and STACK.md line 234 verbatim "MSIX apps do NOT support supersedence." Phase 55 ships MSIX as routing-only sub-section. If future v1.5.1 or v1.6+ identifies MSIX-specific lifecycle requirements, that's a new phase — not Phase 55 scope.
- **Linux app-lifecycle coverage**: Phase 49 PITFALL-1 whitelist locks Linux app delivery as script-based only (no Win32/MSI/MSIX/.pkg analog). Phase 55 explicitly excludes Linux; if future v1.5.1+ adds Linux app-lifecycle requirements, that's a new phase.
- **OEMConfig substantive content in Phase 55 file**: 4D-pure rejected because pure stub fails SC#5 "operate" verb; 4A substantive rejected because duplicates Phase 45 SSoT. Phase 55 ships operational-summary + cross-link (4C-prime). If future v1.5.1+ identifies need for substantive OEMConfig content in operations/ context, that's a new phase.
- **Win32ContentPrepTool version-pin verification at plan-time**: SUMMARY.md line 47 cites v1.8.7 (August 13 2024); plan author for 55-02 must verify current GA version at execution time per "current GA" REQ APP-03 wording. If GA has shifted to a newer version, update accordingly.
- **AMAPI 2024 historical-context scope**: Phase 55 ships AMAPI as soft historical context inline. If future v1.5.1+ identifies need for AMAPI substantive lifecycle content (vs MGP private-app publishing), that's a new phase.

### Reviewed Todos (not folded)
None — `gsd-tools todo match-phase 55` returned 0 matches.

</deferred>

---

*Phase: 55-app-lifecycle-automation*
*Context gathered: 2026-04-28*
