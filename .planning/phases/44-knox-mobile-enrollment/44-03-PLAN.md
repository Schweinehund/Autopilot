---
phase: 44-knox-mobile-enrollment
plan: 03
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - docs/l2-runbooks/22-android-knox-investigation.md
autonomous: true
requirements:
  - AEKNOX-03
must_haves:
  truths:
    - "docs/l2-runbooks/22-android-knox-investigation.md exists with Phase 34 admin template frontmatter (audience: L2, applies_to: KME)"
    - "Runbook contains 4-step Investigation Data Collection (Device registration / Knox Admin Portal state / Knox profile + Intune token sync / Device-side enrollment state)"
    - "Runbook contains 5 Patterns A-E (KME profile misconfig / Knox tripped / KME-ZT collision / Knox license edge / DPC custom JSON malformation)"
    - "Each Pattern follows D-09 structure: Typical class / Symptom / Known Indicators / Resolution Steps / Microsoft Support escalation packet"
    - "Microsoft Support escalation packet has 3 sub-bullets per Pattern (Token sync status / Profile assignment state / Enrollment profile GUID)"
    - "Play Integrity 3-tier verdicts only (Basic / Basic+Device / Strong); zero SafetyNet references"
    - "Graph API READ-ONLY scope blockquote present"
    - "Pattern B (Knox tripped) explicitly references Strong-integrity verdict failure"
    - "Cross-link from L1 runbook 28 routing table present"
  artifacts:
    - path: docs/l2-runbooks/22-android-knox-investigation.md
      provides: "L2 runbook for Samsung KME investigation; Pattern-based analysis; Microsoft Support escalation framing"
      min_lines: 250
      contains:
        - "applies_to: KME"
        - "audience: L2"
        - "Pattern A:"
        - "Pattern B:"
        - "Pattern C:"
        - "Pattern D:"
        - "Pattern E:"
        - "Microsoft Support escalation packet"
        - "Strong integrity"
  key_links:
    - from: docs/l2-runbooks/22-android-knox-investigation.md
      to: docs/l1-runbooks/28-android-knox-enrollment-failed.md
      via: From-L1-escalation router (L1 28 Cause → Pattern mapping)
      pattern: "28-android-knox-enrollment-failed.md"
    - from: docs/l2-runbooks/22-android-knox-investigation.md
      to: docs/admin-setup-android/07-knox-mobile-enrollment.md
      via: Resolution Steps cross-links into admin doc anchors
      pattern: "07-knox-mobile-enrollment.md"
    - from: docs/l2-runbooks/22-android-knox-investigation.md
      to: docs/l2-runbooks/18-android-log-collection.md
      via: Investigation Data Collection Step 4 (device-side log collection)
      pattern: "18-android-log-collection.md"
---

<objective>
Author the new L2 runbook `docs/l2-runbooks/22-android-knox-investigation.md` for Samsung KME failure investigation. Mirrors Phase 41 D-09 Pattern A-E + per-Pattern Microsoft Support escalation packet from sibling L2 runbook 19 (enrollment-investigation).

Purpose: Provide L2 with Pattern-based investigation framework matched to L1 runbook 28's Causes A-D + E. Surface Knox-specific signals: Knox tripped status (Strong-integrity verdict failure), KME profile misconfiguration, KME→ZT collision (KME wins), Knox license edge cases, DPC Custom JSON malformation (D-03 anti-paste violation symptom). Use ONLY Play Integrity 3-tier verdicts (Basic / Basic+Device / Strong); zero SafetyNet tokens. Graph API usage strictly READ-ONLY GET.

Output: One new file (`22-android-knox-investigation.md`); zero modifications to other files in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md
@.planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md
@.planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md
@.planning/phases/44-knox-mobile-enrollment/44-VALIDATION.md
@docs/l2-runbooks/19-android-enrollment-investigation.md
@docs/admin-setup-android/07-knox-mobile-enrollment.md

KEY VERBATIM PATTERNS (sourced from PATTERNS.md File 3 + RESEARCH §3):

**Frontmatter (Phase 34 L2 template):**
- last_verified: execute date YYYY-MM-DD
- review_by: execute date + 60d YYYY-MM-DD
- applies_to: KME
- audience: L2
- platform: Android

**Platform gate (verbatim from runbook 19:9):**
"> **Platform gate:** This guide covers Android Enterprise L2 investigation via Intune. For Windows Autopilot, see [Windows L2 Runbooks](00-index.md). For macOS ADE, see [macOS ADE Runbooks](00-index.md#macos-ade-runbooks). For iOS/iPadOS, see [iOS L2 Runbooks](00-index.md#ios-l2-runbooks)."

**H1 + Context H2:**
"# Android Knox Mobile Enrollment Investigation

## Context

This runbook covers Knox Mobile Enrollment failure investigation across Samsung KME-provisioned devices (provisions into COBO / Dedicated / WPCO modes — KME does NOT enroll devices into a separate KME mode; it provisions Samsung devices into one of the three existing Android Enterprise device-owner modes via the Knox portal handoff). AOSP enrollment is out of scope. Samsung Galaxy XR / wearable / tablet KME variants are routed to Phase 45 (per-OEM AOSP)."

**From-L1-escalation router (per RESEARCH §3 — runbook 22 Pattern A-E mapping):**
"**From L1 escalation?** L1 runbook 28 (Knox enrollment failed) has escalated. L1 collected: serial number, KME profile name, device manufacturer = Samsung, observed Cause. Skip to the Pattern matching L1's observation:
- L1 28 Cause A (B2B account pending) → [Pattern A](#pattern-a-kme-profile-misconfiguration) (KME profile misconfiguration / B2B account state)
- L1 28 Cause B (device not in KAP) → [Pattern B](#pattern-b-knox-tripped) (Knox tripped status investigation) OR reseller-upload audit
- L1 28 Cause C (KME profile not assigned) → [Pattern A](#pattern-a-kme-profile-misconfiguration) (KME profile misconfiguration)
- L1 28 Cause D (KME/ZT collision) → [Pattern C](#pattern-c-kme-zt-collision) (KME→ZT collision)
- L1 28 Cause E (DPC JSON malformed / Knox license / Knox tripped) → [Pattern D](#pattern-d-knox-license-edge) (Knox license edge cases) OR [Pattern E](#pattern-e-dpc-json-malformation) (DPC custom JSON malformation)
- No L1 escalation: begin at Investigation Data Collection Step 1"

**Graph API READ-ONLY scope blockquote (verbatim from runbook 19:26 — IDENTICAL):**
"> **Graph API scope:** Where this runbook references the Microsoft Graph API, usage is strictly READ-ONLY (GET requests). No modifications. No token regeneration. No DPC extras JSON mutation. For deep Android Enterprise Graph operations, see ADDTS-ANDROID-02 (future milestone — Android Graph API deep-dive)."

**Investigation Data Collection — 4 steps (mirror runbook 19:28-110 shape; KME-specific values per RESEARCH §3):**
- Step 1: Device registration state (Intune admin center > Devices > All devices; verify Manufacturer = Samsung)
- Step 2: Knox Admin Portal device state (admin-side check; serial in Devices view; profile assignment column populated)
- Step 3: Knox profile + Intune enrollment token sync state (KME profile EMM = Microsoft Intune; Custom JSON Data parses correctly; Intune-side enrollment-profile staging-token Active)
- Step 4: Device-side enrollment state (per [runbook 18](18-android-log-collection.md) Section 2 — Microsoft Intune app logs preferred; adb logcat last-resort if USB-debug available)

**Pattern A skeleton (verbatim D-09 shape from runbook 19:114-150; replicate for B/C/D/E):**

```
### Pattern A: KME Profile Misconfiguration {#pattern-a-kme-profile-misconfiguration}

**Typical class:** ⚙️ Config Error (KME profile EMM choice / Custom JSON Data field-name) — occasionally ⏱️ Timing (B2B approval lag)

**Symptom:** Samsung device boots to Setup Wizard or wrong-mode Intune enrollment; Knox Admin Portal shows device with profile assigned, but device never reaches the Intune DPC OR enrolls into Fully Managed when Dedicated was intended.

**Known Indicators:**
- Knox Admin Portal Devices view shows Samsung device with empty Profile column despite admin assignment attempt (KAP cache lag)
- Knox Admin Portal Profile shows EMM = something other than "Microsoft Intune" (most-common: stale demo profile)
- Custom JSON Data field is empty OR contains placeholder "enter Intune enrollment token string" not yet substituted
- Intune admin center > Devices > Android shows device enrolled in unexpected mode (Fully Managed when Dedicated intended → KME profile EMM template chose wrong mode)
- B2B account approval >2 business days pending despite re-submission

**Resolution Steps:**
1. Verify the Knox profile EMM is set to "Microsoft Intune" (admin doc Step 2 cross-link: [Step 2 — Create KME EMM profile](../admin-setup-android/07-knox-mobile-enrollment.md#step-2-emm-profile)).
2. Verify Custom JSON Data has the Intune enrollment token substituted (no literal "enter Intune enrollment token string" remains) per [admin doc Step 5](../admin-setup-android/07-knox-mobile-enrollment.md#dpc-custom-json).
3. Verify the Intune-side enrollment profile token is Active (not expired/revoked) via Intune admin center > Devices > Android > Android Enterprise enrollment > Corporate-owned device enrollment.
4. If profile assigned AFTER first boot: factory-reset device and retry (KME does not retro-trigger).
5. For B2B approval lag: contact Samsung Knox B2B support per [admin doc Step 0](../admin-setup-android/07-knox-mobile-enrollment.md#step-0-b2b-approval).

**Microsoft Support escalation packet (D-09):**
- **Token sync status:** Intune-side enrollment-profile staging-token Active state + last token-rotation timestamp (Intune admin center screenshot)
- **Profile assignment state:** Knox Admin Portal Profile-to-Devices assignment view (admin-provided screenshot)
- **Enrollment profile GUID:** Intune admin center URL fragment for the enrollment profile (Graph API READ-ONLY GET if URL not directly visible)
```

**Pattern B (Knox tripped) — same shape; KEY DIFFERENCE: explicitly references Strong-integrity verdict failure:**
- Anchor: `{#pattern-b-knox-tripped}`
- Typical class: ⚙️ Config Error (firmware-state non-recoverable)
- Symptom: Samsung device fails KME enrollment with cryptic error; bootloader was previously unlocked OR custom recovery flashed; Knox eFuse flag set ("tripped").
- Known Indicators include: "Play Integrity verdict fails Strong-integrity check (Knox attestation contributes to Strong on Samsung devices; tripped efuse prevents Strong attestation regardless of OS state)"; "Knox Admin Portal Devices view 'Knox eFuse status' column shows 'Tripped' (if KAP exposes the field — see Open Question 4)"; "Microsoft Q&A reports show admins discovering tripped status after enrollment failures (citation: learn.microsoft.com/answers/questions/2282160)"
- Resolution Steps: "Knox-tripped is non-recoverable for KME — direct admin to alternate enrollment path (Company Portal BYOD OR non-KME COBO via afw#setup/QR)"
- Microsoft Support escalation packet: same 3 sub-bullets

**Pattern C (KME→ZT collision) — same shape; mirror of runbook 19 Pattern C from inverse direction:**
- Anchor: `{#pattern-c-kme-zt-collision}`
- Typical class: ⚙️ Config Error (dual-portal misconfiguration)
- Symptom: Samsung device dual-configured in BOTH Knox Mobile Enrollment AND Zero-Touch portal; KME takes precedence at firmware level; ZT enrollment never initiates.
- Known Indicators: "Device serial appears in BOTH KAP Devices view AND ZT portal Devices tab"; "Intune shows enrollment via KME pathway (KME profile applied) when ZT was the desired path"
- Resolution Steps: "Choose ONE portal; remove from the other; factory-reset device; retry first-boot enrollment"
- Cross-link to [admin doc KME/ZT Mutual Exclusion](../admin-setup-android/07-knox-mobile-enrollment.md#kme-zt-mutual-exclusion) and [02-zero-touch-portal.md#kme-zt-mutual-exclusion](../admin-setup-android/02-zero-touch-portal.md#kme-zt-mutual-exclusion)

**Pattern D (Knox license edge cases) — same shape:**
- Anchor: `{#pattern-d-knox-license-edge}`
- Typical class: ⚙️ Config Error (license activation drift)
- Symptom: KME profile applies but advanced KPE Premium features (kiosk customization, advanced restriction policies) do not activate; OR Knox Admin Portal device row shows licensing column anomaly.
- Known Indicators: "KPE Premium activation column in KAP Devices view empty despite Plan 1+ tenant; admin attempted to license Knox Suite/Manage thinking it was required for KME (D-01 5-SKU disambiguation reinforced)"
- Resolution Steps: "Verify Plan 1+ supplies KPE Premium transparently per [admin doc 5-SKU disambiguation](../admin-setup-android/07-knox-mobile-enrollment.md#sku-disambiguation); KPE Premium has been free since Samsung 2024-03-21 update; KME does NOT require Knox Suite/Manage."

**Pattern E (DPC custom JSON malformation) — same shape:**
- Anchor: `{#pattern-e-dpc-json-malformation}`
- Typical class: ⚙️ Config Error (D-03 anti-paste violation)
- Symptom: Samsung device hits "Applying configuration..." screen and never advances; Knox Admin Portal shows enrollment as "Started" but Intune admin center never sees the device.
- Known Indicators: "Custom JSON Data contains the ZT-style nested PROVISIONING_ADMIN_EXTRAS_BUNDLE wrapper (admin pasted ZT JSON into KME profile by mistake — D-03 anti-paste warning was missed)"; "Custom JSON Data is structurally invalid (extra wrapper keys, missing curly brace, etc.)"
- Resolution Steps: "Replace Custom JSON Data with the FLAT KME JSON: `{\"com.google.android.apps.work.clouddpc.EXTRA_ENROLLMENT_TOKEN\": \"<token>\"}` per [admin doc Step 5](../admin-setup-android/07-knox-mobile-enrollment.md#dpc-custom-json). Save profile in KAP. Factory-reset device. Re-attempt first-boot enrollment."

**Resolution H2 + Microsoft Support escalation criteria (verbatim shape from runbook 19:267-285):**
"## Resolution

Each Pattern sub-section above includes Resolution Steps and a Microsoft Support escalation packet (token sync status, profile assignment state, enrollment profile GUID). This section aggregates the common Microsoft Support case framing.

### Microsoft Support escalation criteria

Escalate to Microsoft Support when:
- All ⚙️ Config Error paths ruled out across the identified Pattern
- ⏱️ Timing windows have elapsed (24-48 hours for first-sync patterns; 1-2 business days for B2B approval per Pattern A)
- Pattern indicators match but Resolution Steps do not move the device to the Active state
- Data collection per [runbook 18](18-android-log-collection.md) is complete

### Case data to attach (all patterns)
1. Device serial number + Android OS version + Samsung manufacturer (must be Samsung)
2. User UPN + tenant domain
3. Pattern (A/B/C/D/E)
4. Intune admin center screenshots per Investigation Data Collection Step 1-3
5. Knox Admin Portal screenshot of device row showing assignment state + Knox eFuse status (if KAP exposes it)
6. Knox B2B account email + KME profile name
7. Pattern-specific escalation packet (token sync status, profile assignment state, enrollment profile GUID — per D-09)
8. Runbook 18 log artifact IDs (Microsoft Intune app log incident OR adb logcat excerpt)"

**Play Integrity 3-tier verdict structure (LOCKED — Audit C1 enforces zero SafetyNet):**
"## Play Integrity Verdict Reference (Knox-relevant)

Knox L2 investigation references Play Integrity 3-tier verdicts ONLY:
- **Basic integrity** — device hardware/OS not detected as compromised
- **Basic + Device integrity** — adds basic device-recognition signal
- **Strong integrity** — requires hardware-backed boot attestation from device TEE; on Samsung devices, Knox attestation contributes to Strong integrity. **Pattern B (Knox tripped) fails Strong integrity** because the Knox eFuse flag at firmware level prevents hardware-backed attestation regardless of OS state."

**Related Resources (mirror runbook 19:287-297):**
"## Related Resources
- [Android Log Collection Guide (runbook 18)](18-android-log-collection.md) — prerequisite for this runbook
- [L2 Runbook Index](00-index.md#android-l2-runbooks)
- [L1 Runbook 28: Android Knox Enrollment Failed](../l1-runbooks/28-android-knox-enrollment-failed.md) — sibling
- [Knox Mobile Enrollment Admin Guide](../admin-setup-android/07-knox-mobile-enrollment.md)
- [Glossary — Knox](../_glossary-android.md#knox)
- [Glossary — KME](../_glossary-android.md#kme)
- [Glossary — KPE](../_glossary-android.md#kpe)
- [Glossary — Play Integrity](../_glossary-android.md#play-integrity)"
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create L2 runbook 22 skeleton (frontmatter through Investigation Data Collection 4-step section)</name>
  <files>docs/l2-runbooks/22-android-knox-investigation.md</files>
  <read_first>
    - .planning/phases/44-knox-mobile-enrollment/44-CONTEXT.md (D-09 Pattern-based investigation; Play Integrity 3-tier; zero SafetyNet)
    - .planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md (File 3 — verbatim copy-from-source for runbook 22)
    - docs/l2-runbooks/19-android-enrollment-investigation.md (sibling D-09 model — full file is the analog)
    - docs/admin-setup-android/07-knox-mobile-enrollment.md (Plan 01 output — Resolution Steps cross-link target anchors must exist)
  </read_first>
  <behavior>
    - File MUST exist with Phase 34 L2 runbook template frontmatter (last_verified, review_by 60d apart, applies_to: KME, audience: L2, platform: Android)
    - Platform gate at line ~9 (verbatim from runbook 19:9 — same Android L2 platform-gate boilerplate)
    - H1 = `# Android Knox Mobile Enrollment Investigation`
    - `## Context` H2 with KME-specific scope paragraph (provisions into COBO/Dedicated/WPCO modes; not a separate "KME mode")
    - From-L1-escalation router with 5-row Cause→Pattern mapping
    - Graph API READ-ONLY scope blockquote (verbatim from runbook 19:26)
    - `## Investigation — Data Collection (mode-agnostic)` H2 with 4 sub-H3 Steps (Device registration / Knox Admin Portal state / Knox profile + Intune token sync / Device-side enrollment state)
    - Each Step has `**Breadcrumb:**` and `**Observables to collect:**` per runbook 19 shape
  </behavior>
  <action>
    Use the Write tool to create `docs/l2-runbooks/22-android-knox-investigation.md` with these sections in order, using the verbatim markdown blocks from `<context>`:

    1. **Frontmatter** (Phase 34 L2 template). Compute dates: `node -e "const d=new Date(); console.log('last_verified: '+d.toISOString().slice(0,10)); d.setDate(d.getDate()+60); console.log('review_by: '+d.toISOString().slice(0,10))"`. Set `applies_to: KME`, `audience: L2`, `platform: Android`.
    2. **Platform gate blockquote** at line ~9 — verbatim from `<context>`.
    3. **H1** = `# Android Knox Mobile Enrollment Investigation`.
    4. **`## Context`** H2 — verbatim KME-specific scope paragraph from `<context>`.
    5. **From-L1-escalation router** — verbatim 5-row Cause→Pattern mapping from `<context>`.
    6. **Graph API READ-ONLY blockquote** at line ~26 — verbatim from `<context>`.
    7. **`## Investigation — Data Collection (mode-agnostic)`** H2 with 4 sub-H3 Steps:
       - `### Step 1: Device registration state (Intune admin center > Devices > All devices)` — Breadcrumb to Intune admin center; Observables: device visible? Manufacturer = Samsung? Enrollment state? Mode (FM / Dedicated / WPCO)?
       - `### Step 2: Knox Admin Portal device state` — Breadcrumb to KAP Devices view (admin-only); Observables: serial in Devices view? Profile column populated? Knox eFuse status if exposed?
       - `### Step 3: Knox profile + Intune enrollment token sync state` — Breadcrumb to KAP Profiles + Intune enrollment-profile blade; Observables: KME profile EMM = Microsoft Intune? Custom JSON Data parses with `EXTRA_ENROLLMENT_TOKEN` substituted? Intune-side staging-token Active?
       - `### Step 4: Device-side enrollment state (collect per runbook 18 based on mode)` — Cross-link to [runbook 18](18-android-log-collection.md); Observables: Microsoft Intune app log incident ID; adb logcat excerpt if USB-debug available.

    Stop here. Pattern A-E and Resolution H2 are deferred to Task 2.
  </action>
  <verify>
    <automated>test -f docs/l2-runbooks/22-android-knox-investigation.md && grep -q "applies_to: KME" docs/l2-runbooks/22-android-knox-investigation.md && grep -q "audience: L2" docs/l2-runbooks/22-android-knox-investigation.md && grep -q '^# Android Knox Mobile Enrollment Investigation$' docs/l2-runbooks/22-android-knox-investigation.md && grep -q 'Graph API scope' docs/l2-runbooks/22-android-knox-investigation.md && grep -q 'Investigation — Data Collection' docs/l2-runbooks/22-android-knox-investigation.md && grep -q '28-android-knox-enrollment-failed.md' docs/l2-runbooks/22-android-knox-investigation.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/l2-runbooks/22-android-knox-investigation.md` returns 0
    - `grep -q "applies_to: KME"` matches
    - `grep -q "audience: L2"` matches
    - `grep -q "platform: Android"` matches
    - `grep -qE "last_verified: 20[0-9]{2}-[0-9]{2}-[0-9]{2}"` matches
    - `grep -qE "review_by: 20[0-9]{2}-[0-9]{2}-[0-9]{2}"` matches
    - `grep -q '^# Android Knox Mobile Enrollment Investigation$'` matches (exact H1)
    - `grep -q '## Context'` matches
    - `grep -q 'Platform gate'` matches
    - `grep -q 'From L1 escalation'` matches
    - `grep -q '28-android-knox-enrollment-failed.md'` matches (cross-link to L1 runbook 28 in router)
    - `grep -q 'Graph API scope'` matches (READ-ONLY scope blockquote)
    - `grep -q 'READ-ONLY (GET requests)'` matches
    - `grep -q 'Investigation — Data Collection'` matches
    - `grep -c '^### Step [1-4]:' docs/l2-runbooks/22-android-knox-investigation.md` returns 4
    - `! grep -q "SafetyNet"` (audit C1)
    - `! grep -qE "supervis(ed|ion|or)"` (audit C2)
  </acceptance_criteria>
  <done>L2 runbook skeleton exists through Investigation Data Collection (4 Steps). All 17 acceptance criteria pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append Pattern A-E (D-09 structure with Microsoft Support escalation packets), Play Integrity 3-tier reference, Resolution H2 + Microsoft Support escalation criteria, Related Resources</name>
  <files>docs/l2-runbooks/22-android-knox-investigation.md</files>
  <read_first>
    - docs/l2-runbooks/22-android-knox-investigation.md (file from Task 1 — append below Investigation Data Collection)
    - docs/l2-runbooks/19-android-enrollment-investigation.md (Pattern A-E + Microsoft Support escalation pattern source; lines 112-297)
    - .planning/phases/44-knox-mobile-enrollment/44-PATTERNS.md (File 3 — Pattern A-E specifications)
    - .planning/phases/44-knox-mobile-enrollment/44-RESEARCH.md (§3 — Pattern A-E for runbook 22; §Common Pitfalls 1-6 for symptoms)
    - docs/admin-setup-android/07-knox-mobile-enrollment.md (cross-link target anchors must exist for Resolution Steps)
  </read_first>
  <behavior>
    - `## Analysis — Match Against Known Patterns` H2 (mirror runbook 19:112)
    - 5 Pattern sub-sections H3, each with `{#pattern-x-anchor}` ID and the D-09 sub-section structure: `**Typical class:**` / `**Symptom:**` / `**Known Indicators:**` (4-7 bullets) / `**Resolution Steps:**` (numbered with cross-links) / `**Microsoft Support escalation packet (D-09):**` (3 sub-bullets — Token sync status / Profile assignment state / Enrollment profile GUID)
    - Pattern B (Knox tripped) explicitly mentions "Strong integrity" verdict failure (Audit C1 — Play Integrity only)
    - Pattern C (KME→ZT collision) cross-links to admin doc KME/ZT Mutual Exclusion + ZT doc anchor
    - Pattern E (DPC JSON malformation) shows the FLAT JSON `{"com.google.android.apps.work.clouddpc.EXTRA_ENROLLMENT_TOKEN": ...}` as the resolution
    - `## Play Integrity Verdict Reference (Knox-relevant)` H2 listing 3 tiers (Basic / Basic+Device / Strong) with explicit Knox/tripped attribution
    - `## Resolution` H2 with Microsoft Support escalation criteria + Case data to attach list
    - `## Related Resources` list with cross-links to runbook 18, L1 runbook 28, admin doc 07, glossary anchors
  </behavior>
  <action>
    Use the Edit tool to append the following sections AFTER the `## Investigation — Data Collection (mode-agnostic)` H2 section created in Task 1, in this order:

    1. **`## Analysis — Match Against Known Patterns`** H2 introductory paragraph: "Match observed indicators to one of five Patterns below. Patterns are independently diagnosable. Each Pattern includes Resolution Steps and a Microsoft Support escalation packet."

    2. **Pattern A** (`{#pattern-a-kme-profile-misconfiguration}`) — VERBATIM the Pattern A skeleton block from `<context>` (Typical class / Symptom / Known Indicators / Resolution Steps / Microsoft Support escalation packet).

    3. **Pattern B** (`{#pattern-b-knox-tripped}`) — same D-09 shape; Knox-tripped specifics from `<context>`. CRITICAL: Known Indicators MUST include the verbatim line: "Play Integrity verdict fails Strong-integrity check (Knox attestation contributes to Strong on Samsung devices; tripped efuse prevents Strong attestation regardless of OS state)". Resolution Steps direct admin to alternate enrollment path (Knox-tripped is non-recoverable for KME).

    4. **Pattern C** (`{#pattern-c-kme-zt-collision}`) — same shape; KME→ZT collision per `<context>`. Cross-link to [admin doc KME/ZT Mutual Exclusion](../admin-setup-android/07-knox-mobile-enrollment.md#kme-zt-mutual-exclusion) and [02-zero-touch-portal.md#kme-zt-mutual-exclusion](../admin-setup-android/02-zero-touch-portal.md#kme-zt-mutual-exclusion).

    5. **Pattern D** (`{#pattern-d-knox-license-edge}`) — same shape; KPE Premium activation drift per `<context>`. Cross-link to [admin doc 5-SKU disambiguation](../admin-setup-android/07-knox-mobile-enrollment.md#sku-disambiguation).

    6. **Pattern E** (`{#pattern-e-dpc-json-malformation}`) — same shape; D-03 anti-paste violation per `<context>`. Resolution Steps contain the verbatim FLAT JSON `{"com.google.android.apps.work.clouddpc.EXTRA_ENROLLMENT_TOKEN": "<token>"}` and cross-link to [admin doc Step 5](../admin-setup-android/07-knox-mobile-enrollment.md#dpc-custom-json).

    7. **`## Play Integrity Verdict Reference (Knox-relevant)`** H2 — verbatim from `<context>` (3-tier list; explicit Pattern B / Knox tripped attribution to Strong-integrity).

    8. **`## Resolution`** H2 — verbatim from `<context>` (Microsoft Support escalation criteria + Case data to attach 8-item list).

    9. **`## Related Resources`** list — verbatim from `<context>` (cross-links to runbook 18, L1 runbook 28, admin doc 07, glossary anchors `#knox`, `#kme`, `#kpe`, `#play-integrity`).

    CRITICAL audit constraints:
    - Zero `SafetyNet` references (audit C1) — even when discussing Play Integrity history, do NOT mention SafetyNet (Phase 41 Knox L2 lock; Pitfall avoided).
    - Zero `supervision` / `supervised` references (audit C2 — KME is Samsung-only; no iOS bridge prose).
    - Zero Graph API mutating commands — READ-ONLY GET only.
    - Do NOT include device-side `adb shell setprop` or `pm enable/disable` commands.
    - Do NOT cite Samsung firmware-level efuse-clearing procedures (Knox tripped is non-recoverable; runbook directs to alternate enrollment paths).
    - Each Pattern's Microsoft Support escalation packet MUST contain exactly 3 sub-bullets labeled "Token sync status:", "Profile assignment state:", "Enrollment profile GUID:".
  </action>
  <verify>
    <automated>grep -q '## Analysis — Match Against Known Patterns' docs/l2-runbooks/22-android-knox-investigation.md && grep -q '### Pattern A: KME Profile Misconfiguration {#pattern-a-kme-profile-misconfiguration}' docs/l2-runbooks/22-android-knox-investigation.md && grep -q '### Pattern B:.*Knox.*{#pattern-b-knox-tripped}' docs/l2-runbooks/22-android-knox-investigation.md && grep -q '### Pattern C:.*{#pattern-c-kme-zt-collision}' docs/l2-runbooks/22-android-knox-investigation.md && grep -q '### Pattern D:.*{#pattern-d-knox-license-edge}' docs/l2-runbooks/22-android-knox-investigation.md && grep -q '### Pattern E:.*{#pattern-e-dpc-json-malformation}' docs/l2-runbooks/22-android-knox-investigation.md && grep -q 'Strong integrity' docs/l2-runbooks/22-android-knox-investigation.md && grep -q 'Microsoft Support escalation packet' docs/l2-runbooks/22-android-knox-investigation.md && ! grep -q 'SafetyNet' docs/l2-runbooks/22-android-knox-investigation.md && ! grep -qE 'supervis(ed|ion|or)' docs/l2-runbooks/22-android-knox-investigation.md</automated>
  </verify>
  <acceptance_criteria>
    - 5 Pattern H3 headings: `grep -c '^### Pattern [A-E]:' docs/l2-runbooks/22-android-knox-investigation.md` returns 5
    - All 5 anchor IDs present: `grep -cE '\{#pattern-[a-e]-[a-z-]+\}' docs/l2-runbooks/22-android-knox-investigation.md` returns 5
    - Each Pattern has D-09 sub-sections present:
      - `grep -c '\*\*Typical class:\*\*' docs/l2-runbooks/22-android-knox-investigation.md` returns at least 5
      - `grep -c '\*\*Symptom:\*\*' docs/l2-runbooks/22-android-knox-investigation.md` returns at least 5
      - `grep -c '\*\*Known Indicators:\*\*' docs/l2-runbooks/22-android-knox-investigation.md` returns at least 5
      - `grep -c '\*\*Resolution Steps:\*\*' docs/l2-runbooks/22-android-knox-investigation.md` returns at least 5
      - `grep -c '\*\*Microsoft Support escalation packet (D-09):\*\*' docs/l2-runbooks/22-android-knox-investigation.md` returns at least 5
    - D-09 escalation packet 3 sub-bullets per Pattern (Token sync / Profile assignment / Enrollment profile GUID):
      - `grep -c '\*\*Token sync status:\*\*' docs/l2-runbooks/22-android-knox-investigation.md` returns at least 5
      - `grep -c '\*\*Profile assignment state:\*\*' docs/l2-runbooks/22-android-knox-investigation.md` returns at least 5
      - `grep -c '\*\*Enrollment profile GUID:\*\*' docs/l2-runbooks/22-android-knox-investigation.md` returns at least 5
    - Pattern B explicitly references Strong integrity: `grep -A 8 '#pattern-b-knox-tripped' docs/l2-runbooks/22-android-knox-investigation.md | grep -q 'Strong integrity'`
    - Pattern E contains FLAT JSON: `grep -q '"com.google.android.apps.work.clouddpc.EXTRA_ENROLLMENT_TOKEN"' docs/l2-runbooks/22-android-knox-investigation.md`
    - Play Integrity 3-tier reference H2 present: `grep -q '## Play Integrity Verdict Reference (Knox-relevant)' docs/l2-runbooks/22-android-knox-investigation.md`
    - All 3 PI tiers mentioned: `grep -qE 'Basic integrity' && grep -qE 'Basic \+ Device integrity' && grep -qE 'Strong integrity'` (combined; counted in Pattern B + reference H2)
    - `## Resolution` H2 present
    - `## Related Resources` list present with cross-links to runbook 18, L1 runbook 28, admin doc 07, glossary anchors
    - Cross-links to admin doc 07: `grep -c '07-knox-mobile-enrollment.md' docs/l2-runbooks/22-android-knox-investigation.md` returns at least 5
    - Audit C1: `! grep -q 'SafetyNet'`
    - Audit C2: `! grep -qE 'supervis(ed|ion|or)'`
    - File total line count `wc -l` returns at least 250 (substantial L2 runbook)
    - VALIDATION.md AEKNOX-03 unit-grep passes:
      ```
      test -f docs/l2-runbooks/22-android-knox-investigation.md && grep -qE "Pattern A|Pattern B|Pattern C|Pattern D|Pattern E" docs/l2-runbooks/22-android-knox-investigation.md && grep -q "Microsoft Support escalation packet" docs/l2-runbooks/22-android-knox-investigation.md && grep -qE "Basic|Strong integrity" docs/l2-runbooks/22-android-knox-investigation.md && ! grep -q "SafetyNet" docs/l2-runbooks/22-android-knox-investigation.md
      ```
  </acceptance_criteria>
  <done>L2 runbook 22 is complete with 5 Patterns (D-09 structure with 3-bullet Microsoft Support escalation packets), Pattern B Strong-integrity attribution, Play Integrity reference H2, Resolution H2, Related Resources. All 19 acceptance criteria pass. AEKNOX-03 VALIDATION unit-grep exits 0. Audit harness 8/8 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| L2 worker → Microsoft Support escalation | L2 hands a structured escalation packet to MS Support; the runbook gates what fields must be collected per Pattern |
| Markdown source → published doc | Pattern symptoms and resolution steps must reflect Samsung KME 2026 reality (firmware behavior, KAP UI, Play Integrity verdict tier) |
| Cross-links → admin doc 07 anchors | Cross-references to admin doc anchors (#step-0-b2b-approval, #step-2-emm-profile, #step-4-assign-profile, #dpc-custom-json, #sku-disambiguation, #kme-zt-mutual-exclusion) must resolve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Information Disclosure (incorrect) | Pattern D Knox license edge — KPE Premium "free since 2024-03-21" claim | mitigate | Cite Samsung 2024-03-21 update; 60-day freshness re-verifies; researcher OQ-1 (does Plan 1+ supply Premium transparently in 2026?) re-verification at execute time |
| T-44-02 | Information Disclosure (incorrect) | Pattern B Knox tripped → Strong-integrity verdict claim | mitigate | Cite Microsoft Q&A `learn.microsoft.com/answers/questions/2282160`; researcher OQ-4 (does KAP expose tripped status?) re-verification; Pattern B Resolution Steps gate on KAP exposing the field — fallback path "ask Samsung support" |
| T-44-03 | Tampering (link-rot in cross-references) | Cross-links to admin doc 07 anchors | mitigate | Plan 01 (Wave 1) ships admin doc with these exact anchor names; verify all anchors before this plan executes; audit-harness link-resolution re-verifies on freshness cycle |
| T-44-04 | Information Disclosure | Pattern E DPC JSON resolution — FLAT JSON form | mitigate | Cite Microsoft Learn `setup-samsung-knox-mobile` (updated 2026-04-14) verbatim; D-03 anti-paste discoverability via grep marker |
</threat_model>

<verification>
After both tasks complete:

1. Audit harness: `node scripts/validation/v1.4.1-milestone-audit.mjs` — expect 8/8 PASS; no new C1/C2/C5 failures.
2. AEKNOX-03 unit-grep predicate (from VALIDATION.md):
   ```bash
   test -f docs/l2-runbooks/22-android-knox-investigation.md && \
     grep -qE "Pattern A|Pattern B|Pattern C|Pattern D|Pattern E" docs/l2-runbooks/22-android-knox-investigation.md && \
     grep -q "Microsoft Support escalation packet" docs/l2-runbooks/22-android-knox-investigation.md && \
     grep -qE "Basic|Strong integrity" docs/l2-runbooks/22-android-knox-investigation.md && \
     ! grep -q "SafetyNet" docs/l2-runbooks/22-android-knox-investigation.md
   ```
   Expected: exit 0.
3. Pattern B Strong-integrity attribution check: `grep -A 10 'pattern-b-knox-tripped' docs/l2-runbooks/22-android-knox-investigation.md | grep -q 'Strong integrity'` exits 0.
</verification>

<success_criteria>
- File `docs/l2-runbooks/22-android-knox-investigation.md` exists with all sections (frontmatter, Platform gate, H1, Context, From-L1-router, Graph API READ-ONLY blockquote, 4-step Investigation Data Collection, 5 Patterns A-E with D-09 structure, Play Integrity reference H2, Resolution H2, Related Resources)
- 5 Patterns follow D-09 structure (Typical class / Symptom / Known Indicators / Resolution Steps / Microsoft Support escalation packet)
- Each Pattern's Microsoft Support escalation packet has 3 sub-bullets (Token sync status / Profile assignment state / Enrollment profile GUID)
- Pattern B (Knox tripped) explicitly references Strong-integrity verdict failure
- Pattern E (DPC JSON malformation) contains FLAT JSON form
- Play Integrity 3-tier reference H2 lists Basic / Basic+Device / Strong
- Zero SafetyNet (C1), zero supervision (C2)
- Audit harness 8/8 PASS
- AEKNOX-03 VALIDATION unit-grep exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/44-knox-mobile-enrollment/44-03-SUMMARY.md` documenting:
- File created (path + line count)
- 5 Patterns with anchor IDs
- D-09 sub-section pattern verified (5×5 = 25 sub-sections)
- Microsoft Support escalation packet 3-bullet count (5×3 = 15 bullets)
- Pattern B Strong-integrity attribution verified
- Audit harness 8/8 PASS state
</output>
