---
phase: 120-eee-standard-extension-mermaid-c17-policy-hygiene-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_standards/EEE-SOP-standard.md
  - scripts/validation/c17-eee-contract.mjs
autonomous: true
requirements: [STD-04]
must_haves:
  truths:
    - "The EEE standard states the Mermaid-in-enrolled-classes policy: text-equivalent conversion (D-01), with the D-03 conversion shapes and the D-04 honesty caveat that green-C17 does not attest leaf-completeness"
    - "The standard's D-02 Edge-case rulings carry 4 new class rulings (glossary to Reference, decision-trees to Reference, nav-hub to Reference, lifecycle to Guide) plus the D-08 non-MECE precedence rule"
    - "The Doc Type Taxonomy controlled-vocabulary table still lists exactly 4 values (Runbook | Guide | RCA | Reference) — no new doc_type"
    - "C17 assertion #1 regex behavior is byte-unchanged; --self-test exits 0; full corpus run reports 0 violations; the file contains no 'CHAIN_PHASES' substring"
  artifacts:
    - path: "docs/_standards/EEE-SOP-standard.md"
      provides: "Mermaid policy section (STD-04) + 4 new D-02 rulings + D-08 precedence rule + C17 ref row 1 cross-reference + Version History row"
      contains: "Mermaid-in-Enrolled-Classes Policy"
    - path: "scripts/validation/c17-eee-contract.mjs"
      provides: "Assertion #1 unchanged; optional comment-only pointer to the new standard section"
      contains: "hasMermaid"
  key_links:
    - from: "docs/_standards/EEE-SOP-standard.md C17 Enforcement Reference row 1"
      to: "docs/_standards/EEE-SOP-standard.md Mermaid-in-Enrolled-Classes Policy"
      via: "Source-in-this-standard cross-reference"
      pattern: "Mermaid-in-Enrolled-Classes Policy"
---

<objective>
Extend the canonical EEE SOP standard (docs/_standards/EEE-SOP-standard.md) with the LOCKED
Mermaid-in-enrolled-classes policy (STD-04, decisions D-01..D-04), the 4 new D-02 Doc Type
edge rulings (D-07) and the D-08 non-MECE precedence rule — and add an optional comment-only
cross-reference in c17-eee-contract.mjs WITHOUT changing assertion #1 behavior (per D-02).

Purpose: Codify the Mermaid text-equivalent-conversion rule and the extended Doc Type mapping
so the two Mermaid-dependent retrofits (Phase 122, RETRO-05/RETRO-08) and the structural
retrofits (Phase 121, 123) have a locked, cited policy to consume. This phase writes the
STANDARD + CONTRACT only — it retrofits NO content doc.
Output: Edited standard doc; optionally-annotated (behavior-unchanged) C17 contract; all four
baseline validation commands still green and byte-identical in result to the pre-edit baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-eee-standard-extension-mermaid-c17-policy-hygiene-fix/120-CONTEXT.md
@.planning/phases/120-eee-standard-extension-mermaid-c17-policy-hygiene-fix/120-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend EEE-SOP-standard.md with the Mermaid policy, 4 new D-02 rulings, and the D-08 precedence rule</name>
  <files>docs/_standards/EEE-SOP-standard.md</files>
  <read_first>
    - docs/_standards/EEE-SOP-standard.md (whole file — anchor sections: Doc Type Taxonomy ~L123, D-02 Edge-case rulings ~L134, RCA forward-compat note ~L145, D1 Platform Normalization Map ~L290, C17 Enforcement Reference ~L353, Version History ~L411)
    - .planning/phases/120-eee-standard-extension-mermaid-c17-policy-hygiene-fix/120-CONTEXT.md (LOCKED decisions D-01..D-08; the 5-class mapping table under D-05; the D-07 rulings; the D-08 precedence steps)
    - .planning/phases/120-eee-standard-extension-mermaid-c17-policy-hygiene-fix/120-RESEARCH.md (Recommended Section Placement, Pitfalls 2 and 3, Anti-Patterns to Avoid)
  </read_first>
  <action>
    Make four additive edits to docs/_standards/EEE-SOP-standard.md. (a) Insert a NEW top-level
    section titled "## Mermaid-in-Enrolled-Classes Policy (STD-04)" immediately BEFORE the
    "## C17 Enforcement Reference (Needle-Spec for Phase 115)" section. State the LOCKED rule per
    D-01: every enrolled Mermaid diagram is converted to a C17-compliant text equivalent (NOT a
    carve-out), with the Copilot-grounding rationale (pandoc-to-.docx body text is the sole
    indexed surface; raw mermaid graph-TD / classDef code and edge-only branch logic land as
    garbage in the citation body). Codify the D-03 conversion shapes: decision graphs/flowcharts
    become a decision table (Scenario, Leaf, Resolution) or a nested decision list where EVERY
    node/leaf and every labeled edge appears as a row/item, the routing target is a relative
    Markdown link, and the leaf count is annotated as a LOCKED invariant (the RE-068
    "LOCKED — N leaves" pattern; name docs/decision-trees/10-8021x-triage.md "Routing
    Verification" table as the in-corpus exemplar); sequence diagrams become an ordered numbered
    step list of the actor exchange; and any legend / node-shape glossary / classDef color
    explanation / "click the leaf" prose is removed or rewritten to describe the table. Include
    the D-04 HONESTY CAVEAT: C17 assertion #1 is an opener-regex with NO diagram parser — it
    guarantees Mermaid ABSENCE but CANNOT verify the replacement table preserved every leaf;
    leaf-completeness is a human authoring-and-review obligation, not something a green C17 run
    attests. Per RESEARCH Pitfall 3, write the caveat as plain prose OR as a blockquote kept
    under 200 characters (do NOT emit a blockquote over 200 chars). (b) APPEND to the existing
    "### D-02 Edge-case rulings" bullet list (keep it BULLETS, not a table — RESEARCH
    Anti-Pattern) the 4 new D-07 rulings: glossary docs (docs/_glossary*.md) map to Reference
    (paradigm lookup); decision-tree docs (docs/decision-trees/*) map to Reference, extending
    the existing "decision-support material" language to name decision trees explicitly and
    discharge the "no procedural walkthrough" tension in text; navigation/index hubs (index.md,
    common-issues.md, quick-ref-l1/l2.md) map to Reference (non-procedural default); lifecycle
    dirs (*-lifecycle/*) map to Guide (end-to-end procedural setup). (c) Add a NEW
    "#### Non-MECE precedence rule (D-08)" subsection right after the D-02 bullets stating the
    3-step authoring-time tie-breaker: (1) directory precedence — a doc in a class-dedicated
    directory takes that directory type regardless of topical overlap (decision-trees/* to
    Reference, *-lifecycle/* to Guide); (2) else classify by dominant body structure (executable
    end-to-end procedure to Runbook/Guide, else Reference); (3) tie to Reference; note the worked
    resolutions (decision-trees/05-device-lifecycle.md to Reference, quick-ref-l1 to Reference)
    and that this is enforced by registry review, not the harness (C17 #9 cannot catch a wrong
    value). (d) The 5-class mapping table from CONTEXT D-05 may be included as an ILLUSTRATIVE
    5-row / 3-column table inside the new prose (per RESEARCH: keep it exactly 5 rows, do NOT
    merge extra columns — Pitfall 2). CRITICAL LOCKED CONSTRAINT (D-05): do NOT modify the
    "## Doc Type Taxonomy" controlled-vocabulary table — it stays at exactly the 4 rows Runbook,
    Guide, RCA, Reference. Do NOT add a VALID_DOC_TYPES concept (deferred). Prose wording is
    Claude's discretion; the substance is fixed by D-01..D-08.
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs 2>&1 | grep -E "174 files checked, 0 with violations"</automated>
  </verify>
  <acceptance_criteria>
    - EEE-SOP-standard.md contains a top-level header "## Mermaid-in-Enrolled-Classes Policy (STD-04)" located before "## C17 Enforcement Reference"
    - That section states text-equivalent conversion is the rule (not a carve-out) AND contains the D-04 caveat conveying that a green C17 run does not attest leaf-completeness / that assertion #1 has no diagram parser
    - "### D-02 Edge-case rulings" now contains bullet rulings naming glossary to Reference, decision-trees to Reference, nav/index hubs to Reference, and lifecycle to Guide (4 new bullets, still a bullet list — no new table replacing the bullets)
    - A "#### Non-MECE precedence rule (D-08)" (or equivalently-named) subsection exists stating the 3-step directory-precedence then dominant-structure then tie-to-Reference ordering
    - The "## Doc Type Taxonomy" controlled-vocabulary table still lists exactly 4 values (Runbook, Guide, RCA, Reference) and no additional doc_type row was added
    - "node scripts/validation/c17-eee-contract.mjs" reports "174 files checked, 0 with violations, 0 total violations" and exits 0 (STD-001 itself still passes its own contract; no assertion #11 over-25-row or #12 over-200-char blockquote violation was introduced)
  </acceptance_criteria>
  <done>The standard doc carries the Mermaid policy (D-01..D-04), the 4 D-07 rulings, and the D-08 precedence rule; the 4-value taxonomy table is unchanged; full C17 corpus run is 174 files / 0 violations / exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-reference C17 assertion #1 to the new standard section (comment-only) and prove zero regression</name>
  <files>docs/_standards/EEE-SOP-standard.md, scripts/validation/c17-eee-contract.mjs</files>
  <read_first>
    - scripts/validation/c17-eee-contract.mjs (assertion #1 region ~L201-210 with the "const hasMermaid = bodyLines.some(...)" line; the Assertions doc-comment list ~L101-114; the --self-test harness ~L418+; confirm the file currently contains no CHAIN_PHASES substring)
    - docs/_standards/EEE-SOP-standard.md, C17 Enforcement Reference assertion row 1 (~L363) and Version History table (~L411)
    - .planning/phases/120-eee-standard-extension-mermaid-c17-policy-hygiene-fix/120-RESEARCH.md (Pattern 1: Comment-only C17 cross-reference; Pitfall 1 — the forbidden CHAIN_PHASES substring; Verifying after edits command block)
    - .planning/phases/120-eee-standard-extension-mermaid-c17-policy-hygiene-fix/120-CONTEXT.md (D-02: assertion #1 stays byte-unchanged; comment is optional and must not change behavior)
  </read_first>
  <action>
    Two edits, both non-behavioral. (a) In docs/_standards/EEE-SOP-standard.md, edit ONLY the
    "Source in this standard" column of assertion row 1 in the "## C17 Enforcement Reference"
    table so it cross-references the new "## Mermaid-in-Enrolled-Classes Policy (STD-04)" section
    (leave the assertion text and every other row untouched), and add ONE new row to the
    "## Version History" table dated 2026-07-07 describing the STD-04 Mermaid policy + D-02
    rulings + D-08 precedence-rule additions. (b) In scripts/validation/c17-eee-contract.mjs, add
    a single comment-only pointer near assertion #1 (either in the Assertions doc-comment list or
    immediately above the "const hasMermaid = ..." line) that names the new standard section as
    the policy source. HARD CONSTRAINTS (LOCKED D-02 + RESEARCH Pitfall 1): do NOT alter, move, or
    remove the "const hasMermaid = bodyLines.some(...)" logic or the inCodeFence mask; the added
    comment MUST NOT contain the literal substring CHAIN_PHASES (it would falsely trip
    V-115-STANDALONE in check-phase-115.mjs) and MUST NOT alter or remove the strings
    "--self-test" or "C17 assertion-violation-counts:". If phrasing near chain concepts is needed,
    use wording like "harness registration" instead of the forbidden token. The comment is
    optional per CONTEXT — if any doubt, prefer the standard-doc cross-reference alone; but if
    added, it must satisfy every constraint above. Then run the full baseline verification suite
    and confirm each result is identical to the pre-edit baseline recorded in RESEARCH (self-test
    4/4, corpus 174/0, check-phase-115 7 PASS, v1.15 harness 16/16).
  </action>
  <verify>
    <automated>node scripts/validation/c17-eee-contract.mjs --self-test 2>&1 | grep -E "Self-test: 4 passed, 0 failed" && node scripts/validation/check-phase-115.mjs 2>&1 | grep -E "7 PASS, 0 FAIL" && node scripts/validation/v1.15-milestone-audit.mjs 2>&1 | grep -E "16 passed, 0 failed"</automated>
    <automated>test "$(grep -c 'CHAIN_PHASES' scripts/validation/c17-eee-contract.mjs)" = "0" && echo NO_FORBIDDEN_TOKEN</automated>
  </verify>
  <acceptance_criteria>
    - C17 Enforcement Reference assertion row 1 "Source in this standard" column now references the "Mermaid-in-Enrolled-Classes Policy" section; the assertion text ("No Mermaid code fences...") is unchanged
    - "## Version History" table has one new dated row (2026-07-07) describing the STD-04 additions
    - "node scripts/validation/c17-eee-contract.mjs --self-test" prints "Self-test: 4 passed, 0 failed" and exits 0
    - "node scripts/validation/check-phase-115.mjs" prints "7 PASS, 0 FAIL, 0 SKIPPED" and exits 0
    - "grep -c 'CHAIN_PHASES' scripts/validation/c17-eee-contract.mjs" returns 0
    - "node scripts/validation/v1.15-milestone-audit.mjs" prints "16 passed, 0 failed" and exits 0
    - The line "const hasMermaid = bodyLines.some(...)" and the inCodeFence mask are byte-for-byte unchanged in behavior (git diff of that region shows only added comment lines, no code-token change)
  </acceptance_criteria>
  <done>Standard-doc cross-reference + Version History row added; optional C17 comment-only pointer added without altering assertion #1 logic or introducing CHAIN_PHASES; self-test 4/4, check-phase-115 7 PASS, corpus 174/0, v1.15 harness 16/16 all still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase edits one Markdown standard doc and adds comment-only text to one Node validator
script. It introduces NO runtime, network, authentication, session, user-input, or cryptographic
surface. There is no new trust boundary. The only integrity concern is not weakening the C17
enforcement contract and not corrupting the frozen-harness gate.

| Boundary | Description |
|----------|-------------|
| author edit -> C17 contract file | The only place a mistaken edit could weaken machine enforcement (mitigated by D-02: assertion #1 byte-unchanged) |
| author edit -> check-phase-115 needle gate | A stray comment substring could trip a naive string-presence check (mitigated by the CHAIN_PHASES prohibition) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Tampering (of enforcement, not data) | c17-eee-contract.mjs assertion #1 regex | mitigate | D-02 LOCKED: assertion #1 logic + inCodeFence mask stay byte-unchanged; only comments may be added; --self-test must exit 0 (verified in Task 2) |
| T-120-02 | Tampering | check-phase-115.mjs V-115-STANDALONE needle check | mitigate | Added comment MUST NOT contain the literal string CHAIN_PHASES and MUST preserve "--self-test" / "C17 assertion-violation-counts:"; verified by grep -c returning 0 and check-phase-115 7 PASS |
| T-120-03 | Tampering | EEE-SOP-standard.md (enrolled, self-checked by C17) | accept-with-verify | An edit that introduces a over-25-row table or over-200-char blockquote would fail STD-001's own C17 contract; caught by the full-corpus 174/0 run in Task 1 |
| T-120-SC | Tampering | package installs | n/a | No npm/pip/cargo installs occur in this phase; supply-chain checkpoint not applicable |

Spoofing / Repudiation / Information-Disclosure / Denial-of-Service / Elevation-of-Privilege:
no applicable new surface — this is a documentation-standard + comment-only validator edit with
no runtime execution path introduced.
</threat_model>

<verification>
Run the exact baseline command set from 120-RESEARCH.md before and after the edits; every result
MUST match the pre-edit baseline (self-test 4/4 exit 0; corpus 174 files / 0 violations exit 0;
check-phase-115 7 PASS exit 0; v1.15-milestone-audit 16/16 exit 0). The standard doc must still
list exactly 4 doc_type values, and c17-eee-contract.mjs must contain zero CHAIN_PHASES
substrings.
</verification>

<success_criteria>
- ROADMAP Phase 120 SC1 met: EEE-SOP-standard.md states the resolved Mermaid policy with rationale
- ROADMAP Phase 120 SC2 met: C17 assertion #1 updated to match the policy (retained hard-fail, behavior byte-unchanged) and --self-test still exits 0
- ROADMAP Phase 120 SC3 (partial — taxonomy) met: the 5 structural classes are mapped to existing types via the 4 new D-02 rulings + D-08 precedence rule; taxonomy table stays at 4 values
- All four baseline validation commands green and unchanged vs. pre-edit baseline
</success_criteria>

<output>
Create `.planning/phases/120-eee-standard-extension-mermaid-c17-policy-hygiene-fix/120-01-SUMMARY.md` when done
</output>
