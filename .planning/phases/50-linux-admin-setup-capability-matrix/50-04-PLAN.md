---
phase: 50
plan: 04
type: execute
wave: 2
depends_on: [50-01, 50-02]
files_modified:
  - docs/reference/linux-capability-matrix.md
autonomous: true
requirements: [LIN-13]
requirements_addressed: [LIN-13]

must_haves:
  truths:
    - "docs/reference/linux-capability-matrix.md is a Win|Linux BILATERAL matrix (D-01) with explicit Not supported cells for CA device-level, app binary delivery, zero-touch enrollment, configuration profiles, and Hybrid Entra Join"
    - "Matrix has 6 domain H2s with Conditional Access ELEVATED to its own H2 (D-02 / CDI-Phase50-04 anchor stability)"
    - "Cross-Platform Equivalences H2 contains exactly 3 attributed paired rows per D-13/D-14/D-15 (corrected SC#4 floor)"
    - "Linux column cells use the 3-status closed set (Supported / Partial / Not supported [— qualifier]) inherited from Phase 49 DPO-02 / CDI-01"
    - "CA cell literal reads 'Not supported — web-app CA only' per PITFALL-2 + Phase 49 V-49-08 inheritance"
  artifacts:
    - path: "docs/reference/linux-capability-matrix.md"
      provides: "Win|Linux bilateral capability matrix + 3-pair Cross-Platform Equivalences (LIN-13)"
      contains: ["platform: Linux", "applies_to: both", "audience: admin", "## Enrollment", "## Configuration", "## App Deployment", "## Compliance", "## Software Updates", "## Conditional Access", "## Cross-Platform Equivalences", "## Key Gaps Summary", "## See Also", "## Version History", "Not supported \u2014 web-app CA only"]
  key_links:
    - from: "docs/reference/linux-capability-matrix.md"
      to: "docs/_glossary-linux.md (4 anchors: intune-portal-package, microsoft-identity-broker, intune-agenttimer, web-app-ca)"
      via: "Cross-Platform Equivalences paired-row anchors (D-13/D-14/D-15)"
      pattern: "_glossary-linux.md#"
    - from: "docs/reference/linux-capability-matrix.md"
      to: "docs/_glossary-macos.md (2 anchors: mam-we, apns)"
      via: "Cross-Platform Equivalences paired-row Apple-side anchors"
      pattern: "_glossary-macos.md#"
---

<objective>
Author the Linux capability matrix at `docs/reference/linux-capability-matrix.md` — a Win|Linux BILATERAL comparison (D-01) with 6 domain H2s, Cross-Platform Equivalences H2 with 3 attributed paired rows (D-13/D-14/D-15), Key Gaps Summary, See Also, Version History.

Purpose: LIN-13 deliverable. The bilateral mandate is locked in REQUIREMENTS.md line 155 ("bilateral") + ROADMAP.md line 119. Conditional Access is ELEVATED to its own H2 (D-02) — distinct from the macOS analog where CA folds into Compliance — providing CDI-Phase50-04 anchor stability for Phase 51 runbook 32 + Phase 58 4-platform comparison.
Output: 1 reference markdown file with bilateral 2-column tables across 6 domains + 3-pair Cross-Platform Equivalences.

This plan is Wave 2 because it consumes admin-setup-linux files (plans 01-02) for cross-link verification. Cross-links are bidirectional: admin guides link to matrix anchors (`#conditional-access`, `#app-deployment`, `#compliance`); the matrix's See Also links back.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-linux-admin-setup-capability-matrix/50-CONTEXT.md
@.planning/phases/50-linux-admin-setup-capability-matrix/50-PATTERNS.md
@.planning/phases/50-linux-admin-setup-capability-matrix/50-VALIDATION.md
@.planning/phases/49-linux-foundation-taxonomy-glossary-version-matrix/49-VERIFICATION.md
@docs/reference/macos-capability-matrix.md
@docs/reference/android-capability-matrix.md
@docs/_glossary-linux.md
@docs/_glossary-macos.md
@docs/linux-lifecycle/00-enrollment-overview.md

<interfaces>
<!-- Phase 49 anchor sources for D-13/D-14/D-15 paired rows. ALL VERIFIED EXIST per CONTEXT.md D-16. -->

From docs/_glossary-linux.md (Phase 49 — verified):
- `#intune-portal-package` (line 80) — D-13 Pair 1 Linux side
- `#microsoft-identity-broker` (line 86) — D-13 Pair 1 Linux side
- `#intune-agenttimer` (line 92) — D-13 Pair 1 + D-14 Pair 2 Linux side
- `#web-app-ca` (line 108) — D-15 Pair 3 Linux side

From docs/_glossary-macos.md (Phase 49 verified):
- `#mam-we` (line 68) — D-14 Pair 2 + D-15 Pair 3 Apple side
- `#apns` (line 101) — D-14 Pair 2 Apple side

Plan 01-02 produce admin-setup-linux files referencing matrix anchors:
- `linux-capability-matrix.md#conditional-access` (referenced from 03 + 05)
- `linux-capability-matrix.md#compliance` (referenced from 03)
- `linux-capability-matrix.md#app-deployment` (referenced from 04)

These anchors MUST exist in this plan's matrix output (created by the 6 domain H2s).

Frontmatter contract (D-03):
```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: both              # bilateral matrix
audience: admin
platform: Linux               # NOT 'all' — D-03 file-name-focal-platform pattern; macOS predates v1.5 C10 convention
---
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/reference/linux-capability-matrix.md (LIN-13 + D-01..D-15)</name>
  <files>docs/reference/linux-capability-matrix.md</files>
  <read_first>
    - docs/reference/macos-capability-matrix.md (PRIMARY structural template — Win|macOS bilateral; lines 22-87 cells; 5 H2 domains + Cross-Platform Equivalences)
    - docs/reference/android-capability-matrix.md (secondary template — Cross-Platform Equivalences H2 placement at lines 76-92; paired-row format with attribution)
    - docs/_glossary-linux.md (verify anchor existence: lines 80, 86, 92, 108)
    - docs/_glossary-macos.md (verify anchor existence: lines 68, 101)
    - docs/linux-lifecycle/00-enrollment-overview.md (Phase 49 V-49-08 — CA cell literal `Not supported — web-app CA only` precedent)
    - .planning/phases/50-linux-admin-setup-capability-matrix/50-PATTERNS.md (lines 323-410 — capability matrix pattern map)
    - .planning/phases/50-linux-admin-setup-capability-matrix/50-CONTEXT.md (D-01 bilateral; D-02 6 domains + CA elevated; D-03 frontmatter; D-04 3-status closed set; D-05/D-06 Equivalences placement; D-13/D-14/D-15 paired-row content)
    - docs/admin-setup-linux/03-compliance-policy.md (plan 02 task 1 output — verify cross-references resolve to matrix anchors)
    - docs/admin-setup-linux/05-conditional-access.md (plan 02 task 3 output — verify cross-references resolve to #conditional-access anchor)
  </read_first>
  <behavior>
    - File exists with C10-compliant frontmatter (`platform: Linux`, `applies_to: both`)
    - Title: `# Intune: Linux vs Windows Capability Matrix`
    - 6 PINNED domain H2s in order: Enrollment / Configuration / App Deployment / Compliance / Software Updates / Conditional Access (D-02)
    - Each domain H2 has a `| Feature | Windows | Linux |` table; Linux cells use 3-status closed set
    - CA H2 row contains LITERAL: `Not supported \u2014 web-app CA only` (em-dash; PITFALL-2 + Phase 49 V-49-08 inheritance)
    - `## Cross-Platform Equivalences` H2 contains 3 paired rows (D-13/D-14/D-15) with attribution + glossary anchor links
    - `## Key Gaps Summary`, `## See Also`, `## Version History` H2s present (10 H2s total per D-06)
    - Negative assertion: file does NOT duplicate Phase 49 whitelist content (DPO-07 — back-link only)
  </behavior>
  <action>
Create `docs/reference/linux-capability-matrix.md` mirroring `docs/reference/macos-capability-matrix.md` shape with the following EXACT requirements:

**Frontmatter (D-03 verbatim — note `platform: Linux`, NOT `all`):**
```yaml
---
last_verified: 2026-04-27
review_by: 2026-06-26
applies_to: both
audience: admin
platform: Linux
---
```

**H1:** `# Intune: Linux vs Windows Capability Matrix`

**Intro paragraph:**
```
This document compares Intune management capabilities between Windows (Autopilot v1, Autopilot v2, manual enrollment) and Linux (Ubuntu 22.04 LTS, Ubuntu 24.04 LTS — `intune-portal` deb client). Linux Intune management is structurally narrower than Windows; this matrix quantifies the gaps and provides a Cross-Platform Equivalences section attributing the closest Apple/Android analogs for each Linux capability.

For Linux admin setup guides, see [Linux Admin Setup Overview](../admin-setup-linux/00-overview.md). For the locked Linux management surface (whitelist) and out-of-scope callouts, see [Linux Enrollment Overview](../linux-lifecycle/00-enrollment-overview.md#supported-management-surface) — this matrix EXTENDS the whitelist with bilateral comparison; it does not duplicate the whitelist.
```

**6 PINNED domain H2s (D-02; ORDER matters; each followed by a 2-column `| Feature | Windows | Linux |` table):**

```markdown
## Enrollment

| Feature | Windows | Linux |
|---------|---------|-------|
| Zero-touch / autopilot enrollment | Windows Autopilot v1/v2 (Pre-Provisioning, USB profiles, OOBE flows) | Not supported |
| Hybrid Entra Join | Supported (AD + Entra Connect; AAD-joined dual-state) | Not supported |
| User-initiated MDM enrollment | Supported (Settings > Accounts > Access work or school) | Supported (intune-portal app sign-in; user-initiated only) |
| BYOD enrollment | Supported (per ABM/Entra-bound flows) | Partial — see [BYOD vs Corporate-Owned Caveat](../linux-lifecycle/00-enrollment-overview.md#enrollment-constraints) |

## Configuration

| Feature | Windows | Linux |
|---------|---------|-------|
| MDM configuration profiles (Settings Catalog) | Supported (extensive — thousands of CSP-mapped settings) | Not supported — Linux has no MDM configuration profile concept in Intune |
| Custom configuration via scripts | Supported (Win32 / PowerShell / Intune Remediations) | Partial — Bash scripts run via app-delivery surface; settings-catalog Custom Compliance for compliance-detect only |
| OEMConfig | n/a (Windows uses ADMX) | Not supported |
| Hardware/firmware policies | Supported (driver/firmware updates via WUfB) | Not supported |

## App Deployment

| Feature | Windows | Linux |
|---------|---------|-------|
| Binary package delivery | Supported (Win32 .intunewin, MSIX, MSI, Microsoft Store) | Not supported — script-based only (no Win32/MSIX/.pkg analog; no .deb direct delivery) |
| Script-based delivery | Supported (PowerShell scripts, Intune Remediations) | Supported (Bash scripts; org-authored install logic; Intune handles delivery + exit-code reporting) |
| Microsoft Store apps | Supported (Microsoft Store for Business retired; current Microsoft Store integration) | Not supported |
| App supersedence + dependency graphs | Supported (Win32 supersedence, max 10 superseding; max 100 dependencies) | Not supported |

## Compliance

| Feature | Windows | Linux |
|---------|---------|-------|
| Built-in compliance settings | Supported (extensive — TPM, BitLocker, OS version, AV, Firewall, etc.) | Partial — 4 settings-catalog categories (Allowed Distributions / Custom Compliance / Device Encryption / Password Policy) |
| Custom compliance scripts | Supported (PowerShell discovery scripts) | Supported (Bash discovery scripts) |
| Compliance enforcement (CA grant) | Supported via `Require device to be marked as compliant` | Not supported — see [Conditional Access](#conditional-access) |
| Compliance reporting | Supported | Supported (detect-only on Linux) |

## Software Updates

| Feature | Windows | Linux |
|---------|---------|-------|
| Update enforcement | Supported (Windows Update for Business rings; Autopatch; Hotpatch) | Not supported — Intune does not orchestrate apt updates |
| Driver/firmware updates | Supported (separately from quality/feature) | Not supported |
| Update scheduling | Supported (deferral periods, deadlines) | Not supported — apt update timing is unmanaged |

## Conditional Access

| Feature | Windows | Linux |
|---------|---------|-------|
| Device-based CA (`Require device to be marked as compliant`) | Supported | Not supported \u2014 web-app CA only |
| Web-app CA via Edge | Supported | Supported (Microsoft Edge for Linux 102.x+) |
| MAM (Managed App without enrollment) | n/a (Windows uses Intune client) | Not supported (no Intune SDK for Linux apps; closest analog is web-app CA via Edge — see [Cross-Platform Equivalences](#cross-platform-equivalences)) |
| Risk-based CA | Supported (Entra ID Protection signals) | Partial — risk evaluation works for the user/session but does not factor Linux device state |
```

**CRITICAL — CA cell literal (V-50-15 / Phase 49 V-49-08 contract):** The CA H2 row for Device-based CA MUST contain the EXACT em-dash literal `Not supported \u2014 web-app CA only` (UTF-8 em-dash U+2014, not a hyphen). Validator probes the literal byte sequence.

**`## Cross-Platform Equivalences` H2 (D-05/D-06 placement — below 6 domain H2s, above Key Gaps Summary):**

Preamble paragraph (mirroring android-capability-matrix.md lines 83-84):
```
This section maps three Linux↔Apple capability pairs called out in ROADMAP SC#4. It is NOT a 4-platform comparison — see Phase 58's [4-Platform Capability Comparison](4-platform-capability-comparison.md) (when shipped) for that. Each paired row attributes the platform explicitly on both sides; mappings are STRUCTURAL, not behavioral (per Phase 49 PITFALL-1 partial-mapping discipline at `_glossary-linux.md` line 84 and 112).
```

3-pair table (D-13/D-14/D-15 — EXACT attribution required):

```markdown
| Linux | macOS / iOS |
|-------|-------------|
| **Linux `intune-portal` deb + `microsoft-identity-broker` systemd unit** | **macOS Microsoft Intune Company Portal app + IntuneMDMDaemon LaunchAgent** |
| Linux ships the [`intune-portal` package](../_glossary-linux.md#intune-portal-package) (org-installed via apt from packages.microsoft.com) plus the [`microsoft-identity-broker`](../_glossary-linux.md#microsoft-identity-broker) systemd unit (system-scope) for Entra ID broker tokens; user-scope check-in runs via [`intune-agent.timer`](../_glossary-linux.md#intune-agenttimer). Pairing is structural — both deliver client-side enrollment + token broker. Note: the Linux side has DISTINCT system-scope (broker) and user-scope (timer) components; the macOS side combines roles into the user-scope LaunchAgent. | macOS uses the Microsoft Intune Company Portal app (user-installed) plus the IntuneMDMDaemon LaunchAgent (user-scope) for the equivalent role. See also [`mam-we`](../_glossary-macos.md#mam-we) for the related per-app pattern. |

| Linux | iOS |
|-------|-----|
| **Linux `intune-agent.timer` user-scope check-in** | **iOS APNs-triggered MDM check-in cycle** |
| The [`intune-agent.timer`](../_glossary-linux.md#intune-agenttimer) is a user-scope systemd timer that wakes the agent on a schedule to re-evaluate compliance and pull new policies. Pairing is structural — both are "device-side schedule on which compliance is re-evaluated". The transport mechanism diverges: Linux is timer-poll; iOS is [APNs-push](../_glossary-macos.md#apns) (Apple Push Notification service). | iOS receives APNs push notifications that trigger an MDM check-in to fetch new commands and report state. |

| Linux | iOS |
|-------|-----|
| **Linux web-app CA (Microsoft Edge for Linux 102.x+)** | **iOS MAM-WE (Managed App Without Enrollment)** |
| Linux's only CA enforcement vehicle is the [web-app CA pattern](../_glossary-linux.md#web-app-ca) — Edge presents the CA challenge for org web-app sign-ins. Both Linux web-app CA and iOS [MAM-WE](../_glossary-macos.md#mam-we) deliver org-data protection without device-level CA enforceability — that is the LIN-13-named "compliance-lite" pattern. The mapping is STRUCTURAL, not behavioral. Architectural divergence: Linux web-app CA is BROWSER-CHALLENGE (per-session token at sign-in); iOS MAM-WE is APP-LAYER selective-wipe (per-app data containerization without device-level MDM). | iOS MAM-WE delivers managed-app data protection (selective wipe, copy/paste restrictions) on devices NOT enrolled in Intune MDM. |
```

**`## Key Gaps Summary`** (CD-04 recommended; mirror android-capability-matrix.md):

5-7 prose paragraphs distilling the most-significant Linux-vs-Windows deltas:
- App delivery: script-based-only is the largest gap (no Win32 supersedence; no driver/firmware delivery)
- Conditional Access: device-level CA absent; web-app CA via Edge is the only path
- Configuration: no MDM configuration profile concept; compliance-only surface
- Software updates: Intune does not orchestrate apt updates
- BYOD vs corporate-owned: ambiguity per Phase 49 BYOD caveat

**`## See Also`**:
```
- [Linux Admin Setup Overview](../admin-setup-linux/00-overview.md)
- [Linux Enrollment Overview](../linux-lifecycle/00-enrollment-overview.md) — Phase 49 management-surface whitelist + out-of-scope callouts
- [Linux Prerequisites](../linux-lifecycle/01-linux-prerequisites.md) — Ubuntu version matrix
- [Linux Provisioning Glossary](../_glossary-linux.md)
- [macOS Capability Matrix](macos-capability-matrix.md) — Win|macOS bilateral comparison
- [iOS Capability Matrix](ios-capability-matrix.md)
- [Android Capability Matrix](android-capability-matrix.md)
- [4-Platform Capability Comparison](4-platform-capability-comparison.md) — when Phase 58 ships
```

**`## Version History`**:
```
| Date | Change | Author |
|------|--------|--------|
| 2026-04-27 | Initial version — Linux capability matrix Win|Linux bilateral; 6 domain H2s with CA elevated; 3-pair Cross-Platform Equivalences (Phase 50) | -- |
```

**Negative assertion (DPO-07 inheritance):** This file MUST NOT verbatim duplicate the Phase 49 whitelist H2 content. The matrix EXTENDS the whitelist with bilateral comparison + per-domain elaboration but does not re-publish the whitelist verbatim. Refer instead via the See Also link.

Per CONTEXT.md D-01 (bilateral), D-02 (6 H2s + CA elevated), D-03 (frontmatter), D-04 (3-status closed set), D-05/D-06 (Equivalences placement + 10 H2s), D-13/D-14/D-15 (3-pair attribution rephrased per W-CRIT-1/2/3 corrections), D-16 (no glossary append; all 6 anchors verified), CDI-Phase50-04 (CA H2 anchor stability), DPO-07 (no whitelist duplication).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/reference/linux-capability-matrix.md','utf8');const h2=['## Enrollment','## Configuration','## App Deployment','## Compliance','## Software Updates','## Conditional Access','## Cross-Platform Equivalences','## Key Gaps Summary','## See Also','## Version History'];const allH2=h2.every(h=>new RegExp('^'+h.replace(/[.*+?^${}()|[\\]\\\\]/g,'\\\\$&')+'\\\\s*$','m').test(c));const ok=c.includes('platform: Linux')&&c.includes('applies_to: both')&&allH2&&c.includes('Not supported \u2014 web-app CA only')&&c.includes('_glossary-linux.md#intune-portal-package')&&c.includes('_glossary-linux.md#microsoft-identity-broker')&&c.includes('_glossary-linux.md#intune-agenttimer')&&c.includes('_glossary-linux.md#web-app-ca')&&c.includes('_glossary-macos.md#mam-we')&&c.includes('_glossary-macos.md#apns');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/reference/linux-capability-matrix.md`
    - Frontmatter: `platform: Linux` (NOT `all`), `applies_to: both`, `audience: admin`, 60-day cycle
    - File contains all 10 PINNED H2 lines (V-50-14): `## Enrollment`, `## Configuration`, `## App Deployment`, `## Compliance`, `## Software Updates`, `## Conditional Access`, `## Cross-Platform Equivalences`, `## Key Gaps Summary`, `## See Also`, `## Version History`
    - File contains literal byte sequence `Not supported \u2014 web-app CA only` (em-dash U+2014; V-50-15 PITFALL-2 inheritance)
    - File contains all 6 glossary anchor literals: `_glossary-linux.md#intune-portal-package`, `_glossary-linux.md#microsoft-identity-broker`, `_glossary-linux.md#intune-agenttimer`, `_glossary-linux.md#web-app-ca`, `_glossary-macos.md#mam-we`, `_glossary-macos.md#apns` (V-50 paired-row anchor probes)
    - All Linux column cells in the 6 domain tables match regex `^(Supported|Partial|Not supported)` after pipe-splitting (V-50-16 column-aware closed-set check; cell index 2 in `| Feature | Windows | Linux |`)
    - Cross-Platform Equivalences H2 contains ≥3 paired rows (V-50-17 ≥3 attribution count)
    - Cross-Platform Equivalences attribution matches D-13/D-14/D-15 verbatim phrasing for the LEAD row of each pair (`Linux 'intune-portal' deb + 'microsoft-identity-broker' systemd unit`; `Linux 'intune-agent.timer' user-scope check-in`; `Linux web-app CA (Microsoft Edge for Linux 102.x+)`)
    - File does NOT contain a verbatim copy of the Phase 49 whitelist H2 content (DPO-07 negative — uses back-link only)
  </acceptance_criteria>
  <done>linux-capability-matrix.md exists with bilateral Win|Linux 2-column tables across 6 domains, CA elevated to its own H2 with literal `Not supported — web-app CA only`, 3-pair Cross-Platform Equivalences with corrected D-13/D-14/D-15 attribution, all 6 glossary anchor links present, 10 H2s total per D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation-only | Reference material; no code, no runtime. Architectural fidelity prevents downstream misconfigurations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-06 | Information Disclosure (architectural mis-attribution) | Cross-Platform Equivalences pairs | mitigate | Pair attribution MUST disclose the divergence between mapped concepts (timer-poll vs APNs-push; browser-challenge vs app-layer wipe) per PITFALL-1 partial-mapping discipline. Suppression of divergence disclosure could mislead admins into expecting behavioral parity. |
| T-50-07 | Spoofing/Tampering (downstream misuse) | CA H2 anchor `#conditional-access` | mitigate | The CA H2 anchor is a load-bearing reference for Phase 51 runbook 32 + Phase 58 4-platform comparison. Renaming it requires same-commit validator + downstream-phase updates per CDI-Phase50-04. |

Phase 50 is documentation-only.
</threat_model>

<verification>
After task completes:

```bash
# File exists
test -f docs/reference/linux-capability-matrix.md

# 10 H2s probe
grep -c '^## ' docs/reference/linux-capability-matrix.md
# Expect: ≥ 10 (10 H2s + any sub-section H2s; Equivalences H2 + 3 pair tables)

# CA cell literal probe (em-dash)
grep -F "Not supported — web-app CA only" docs/reference/linux-capability-matrix.md

# 3-pair anchor probes
grep -F "_glossary-linux.md#intune-portal-package" docs/reference/linux-capability-matrix.md
grep -F "_glossary-linux.md#microsoft-identity-broker" docs/reference/linux-capability-matrix.md
grep -F "_glossary-linux.md#intune-agenttimer" docs/reference/linux-capability-matrix.md
grep -F "_glossary-linux.md#web-app-ca" docs/reference/linux-capability-matrix.md
grep -F "_glossary-macos.md#mam-we" docs/reference/linux-capability-matrix.md
grep -F "_glossary-macos.md#apns" docs/reference/linux-capability-matrix.md
```

DO NOT commit — atomic D-18 commit owned by plan 06.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 50 Success Criteria:
- **SC#4 (linux-capability-matrix.md "Not supported" cells + ≥3 attributed pairs):** ✅ Task 1 delivers (with corrected D-13/D-14/D-15 attribution; SC#4 itself corrected same-commit per D-22 in plan 06)
- **SC#5 (frontmatter cycle on matrix):** ✅ C10-compliant frontmatter

Plan complete when:
- [ ] File exists at `docs/reference/linux-capability-matrix.md`
- [ ] 10 PINNED H2s present in correct order (D-06)
- [ ] 6 domain tables use Win|Linux 3-status closed set (D-04)
- [ ] CA cell literal byte-perfect with em-dash (V-50-15)
- [ ] 3 paired rows with verbatim D-13/D-14/D-15 attribution
- [ ] All 6 glossary anchors linked (Linux ×4 + macOS ×2)
- [ ] DPO-07 — no whitelist duplication
</success_criteria>

<output>
After completion, create `.planning/phases/50-linux-admin-setup-capability-matrix/50-04-SUMMARY.md` documenting:
- File path + line count
- Frontmatter values
- 10 H2 list verification
- CA cell literal byte-sequence verification
- 6 glossary anchor link verification
- 3-pair attribution verification (verbatim D-13/D-14/D-15 lead lines)
- DPO-07 anti-duplication verification
- Note: NO COMMIT — atomic D-18 commit owned by plan 06
</output>
