---
phase: 51
plan: 07
type: execute
wave: B
depends_on: [51-01, 51-02, 51-03, 51-04, 51-05, 51-06]
files_modified:
  - docs/l1-runbooks/00-index.md
  - docs/decision-trees/00-initial-triage.md
autonomous: true
requirements: [LIN-07, LIN-08, LIN-09, LIN-10, LIN-11]
requirements_addressed: [LIN-07, LIN-08, LIN-09, LIN-10, LIN-11]

must_haves:
  truths:
    - "docs/l1-runbooks/00-index.md has new H2 `## Linux L1 Runbooks` appended after the last existing Android entry (current line 76); 4-row table mirroring Android section pattern (V-51-21)"
    - "docs/decision-trees/00-initial-triage.md has 5 append-only edits at the platform-gate banner area, Scenario Trees list, See Also section, Scenario Trees footer, and Version History (V-51-22 — at least 3 occurrences of `[Linux Triage](09-linux-triage.md)` link)"
    - "All edits are append-only (Phase 42 D-03 contract) — preserve all existing Android/iOS/macOS/APv2/Initial-Triage content untouched; do NOT modify any line that existed before Phase 51"
    - "Edits do NOT introduce any of: TBD, TODO, FIXME, XXX placeholders"
  artifacts:
    - path: "docs/l1-runbooks/00-index.md"
      provides: "Bilateral discoverability surface for Linux L1 Runbooks (append-only Linux H2 + 4-row table)"
      contains: ["## Linux L1 Runbooks", "[30: Linux Enrollment Failed](30-linux-enrollment-failed.md)", "[31: Linux Compliance Non-Compliant](31-linux-compliance-non-compliant.md)", "[32: Linux CA Blocking Web-App Access](32-linux-ca-blocking-web-access.md)", "[33: Linux Agent Service Failure](33-linux-agent-service-failure.md)"]
    - path: "docs/decision-trees/00-initial-triage.md"
      provides: "Cross-platform tree-discovery surface for Linux Triage (append-only Linux entries at 5 positions)"
      contains: ["[Linux Triage](09-linux-triage.md)", "Linux:**", "Ubuntu LTS"]
  key_links:
    - from: "docs/l1-runbooks/00-index.md#linux-l1-runbooks"
      to: "docs/l1-runbooks/30-linux-enrollment-failed.md"
      via: "Append-only Linux L1 Runbooks index entry"
      pattern: "[30: Linux Enrollment Failed](30-linux-enrollment-failed.md)"
    - from: "docs/decision-trees/00-initial-triage.md"
      to: "docs/decision-trees/09-linux-triage.md"
      via: "Append-only Linux platform-gate banner + Scenario Trees + See Also + footer + Version History"
      pattern: "[Linux Triage](09-linux-triage.md)"
---

<objective>
Append Linux navigation entries to two existing hub files: (1) `docs/l1-runbooks/00-index.md` — add `## Linux L1 Runbooks` H2 + 4-row table after the last existing Android entry (current line 76); (2) `docs/decision-trees/00-initial-triage.md` — add Linux entries at 5 positions (platform-gate banner, Scenario Trees list, See Also section, Scenario Trees footer, Version History). All edits append-only per Phase 42 D-03 contract.

Purpose: Satisfies SC#4 (`l1-runbooks/00-index.md` has Linux section appended; `decision-trees/00-initial-triage.md` has Linux branch added; both append-only). Provides bilateral discoverability for Phase 51 deliverables (tree + 4 runbooks).

This plan is sequenced AFTER plans 01-06 in Wave B because the append edits reference filenames that plans 01-05 must have authored; the validator (51-06) reads these append-only assertions in V-51-21 and V-51-22.

Output: 2 modified existing files (no new files); both files retain all pre-existing content + receive Linux additions in append-only positions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-linux-l1-triage-runbooks-30-33/51-CONTEXT.md
@.planning/phases/51-linux-l1-triage-runbooks-30-33/51-PATTERNS.md
@.planning/phases/51-linux-l1-triage-runbooks-30-33/51-RESEARCH.md
@docs/l1-runbooks/00-index.md
@docs/decision-trees/00-initial-triage.md

<interfaces>
**`docs/l1-runbooks/00-index.md`** — append target #1:
- Insertion point: After line 76 (the AOSP Runbook 29 row, last existing Android entry — verify current state before editing)
- Mirror the Android L1 Runbooks H2 + table block (lines 64-76)
- New content: `## Linux L1 Runbooks` H2 + intro paragraph + 4-row table (Runbook | Scenario | Applies To)
- Optionally: a single Version History row indicating the addition (per CD-08 author discretion)

**`docs/decision-trees/00-initial-triage.md`** — append target #2 (5 insertion points):
1. **Platform-gate banner area** — after line 11 (the Android line `> **Android:** ...`)
2. **Scenario Trees list** — after line 40 (the Android entry `- [Android Triage](08-android-triage.md) ...`)
3. **See Also section** — after line 122 (the Android entry `- [Android Triage](08-android-triage.md) ...`)
4. **Scenario Trees footer** — after line 133 (Android entry `- [Android Triage](08-android-triage.md)`)
5. **Version History** — new top row, after line 138 header separator (reverse-chronological — newest first)

V-51-22 requires ≥3 occurrences of `[Linux Triage](09-linux-triage.md)` literal link in `00-initial-triage.md` after edits. The 5 insertion points yield 4 occurrences of the link (banner has descriptive prefix; Scenario Trees + See Also + footer all carry the literal `[Linux Triage](09-linux-triage.md)` form), comfortably exceeding the floor.

Append-only contract (Phase 42 D-03):
- Do NOT modify any existing line in either file
- Do NOT delete any existing entries
- Insert new content immediately AFTER the listed insertion-point line; preserve all content below
- Any line that existed before this plan must be byte-identical after this plan
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append `## Linux L1 Runbooks` H2 + 4-row table to docs/l1-runbooks/00-index.md</name>
  <files>docs/l1-runbooks/00-index.md</files>
  <read_first>
    - docs/l1-runbooks/00-index.md (FULL FILE — verify current end-of-Android section line; verify Android L1 Runbooks H2 pattern at line 64-76; identify the precise line after which Linux H2 should be inserted)
    - .planning/phases/51-linux-l1-triage-runbooks-30-33/51-PATTERNS.md (Section G — paste-ready Linux L1 Runbooks H2 + 4-row table)
    - .planning/phases/51-linux-l1-triage-runbooks-30-33/51-RESEARCH.md (Append-Only Insertion Points lines 451-468 — exact insertion points + H2 + intro paragraph + table content)
    - .planning/phases/51-linux-l1-triage-runbooks-30-33/51-CONTEXT.md (D-25 step 2 — append-only contract semantics; Phase 42 D-03 attribution)
  </read_first>
  <behavior>
    - File `docs/l1-runbooks/00-index.md` retains all pre-existing content (no lines modified or deleted)
    - File contains new H2 `## Linux L1 Runbooks` after the last existing Android section entry
    - The new H2 is followed by an intro paragraph + a 3-column table (Runbook | Scenario | Applies To) with 4 rows for Runbooks 30-33
    - Table cells use literal markdown link syntax `[30: Linux Enrollment Failed](30-linux-enrollment-failed.md)` etc.
    - V-51-21 will pass against the modified file
  </behavior>
  <action>
Read `docs/l1-runbooks/00-index.md` in full to confirm the current end of the Android L1 Runbooks section (51-RESEARCH.md says line 76 is the last AOSP Runbook 29 row, but verify the current state in case Phase 50 or other phases touched the file).

After the line that ends the Android section (immediately after the last Android-platform table row), insert a blank line, then the following block VERBATIM:

```markdown
## Linux L1 Runbooks

L1 runbooks for the four most common Linux Intune client failure scenarios on Ubuntu 22.04/24.04 LTS. Start with the [Linux Triage Decision Tree](../decision-trees/09-linux-triage.md) to identify the failure mode, then follow the matching runbook below. All runbooks include L1-executable steps (portal-first or terminal walkthrough as appropriate per cause) and explicit escalation triggers to L2.

| Runbook | Scenario | Applies To |
|---------|----------|------------|
| [30: Linux Enrollment Failed](30-linux-enrollment-failed.md) | Enrollment failed at package install / sign-in / or timeout | Ubuntu 22.04/24.04 LTS |
| [31: Linux Compliance Non-Compliant](31-linux-compliance-non-compliant.md) | Device shows non-compliant in Intune (distro/encryption/password/custom-compliance) | Ubuntu 22.04/24.04 LTS |
| [32: Linux CA Blocking Web-App Access](32-linux-ca-blocking-web-access.md) | User blocked accessing M365 in Edge (web-app CA only — PITFALL-2) | Ubuntu 22.04/24.04 LTS |
| [33: Linux Agent Service Failure](33-linux-agent-service-failure.md) | intune-agent.timer not running / not checking in | Ubuntu 22.04/24.04 LTS |
```

If the file has a Version History H2 / table at the bottom and that table's most-recent row is below the Android section, also append a new Version History row at the top of the table (reverse-chronological order):

```
| 2026-04-27 | Added Linux L1 Runbooks section (runbooks 30-33) | -- |
```

If the Version History table is in chronological-ascending order in this file (verify by reading first), append at the bottom instead — match the existing direction.

DO NOT modify any other line in the file. DO NOT delete any existing entry. DO NOT touch the Android L1 Runbooks H2 or table.

DO NOT commit yet — atomic commit owned by 51-08.
  </action>
  <verify>
    <automated>grep -c "^## Linux L1 Runbooks$" docs/l1-runbooks/00-index.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly one line matching `^## Linux L1 Runbooks\s*$`
    - File contains all 4 literal table-row link patterns: `[30: Linux Enrollment Failed](30-linux-enrollment-failed.md)`, `[31: Linux Compliance Non-Compliant](31-linux-compliance-non-compliant.md)`, `[32: Linux CA Blocking Web-App Access](32-linux-ca-blocking-web-access.md)`, `[33: Linux Agent Service Failure](33-linux-agent-service-failure.md)` (V-51-21)
    - File contains intro paragraph linking to `[Linux Triage Decision Tree](../decision-trees/09-linux-triage.md)`
    - All pre-existing Android L1 Runbooks H2 + table rows are preserved byte-identical (manual diff review confirms append-only)
    - File does NOT contain `TBD`, `TODO`, `FIXME`, `XXX` introduced by this edit
  </acceptance_criteria>
  <done>00-index.md has Linux L1 Runbooks H2 + 4-row table appended after Android section; all pre-existing content preserved; V-51-21 passes; no commit yet.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append Linux entries to docs/decision-trees/00-initial-triage.md at 5 positions</name>
  <files>docs/decision-trees/00-initial-triage.md</files>
  <read_first>
    - docs/decision-trees/00-initial-triage.md (FULL FILE — verify current state of all 5 insertion points: line 11 Android banner; line 40 Scenario Trees list Android entry; line 122 See Also Android entry; line 133 Scenario Trees footer Android entry; line 138 Version History header separator)
    - .planning/phases/51-linux-l1-triage-runbooks-30-33/51-PATTERNS.md (Section H — paste-ready insertion content for all 5 positions)
    - .planning/phases/51-linux-l1-triage-runbooks-30-33/51-RESEARCH.md (Append-Only Insertion Points lines 469-495 — exact 5 insertion points)
    - .planning/phases/51-linux-l1-triage-runbooks-30-33/51-CONTEXT.md (D-25 step 2 — exact verbatim insertion strings; Phase 42 D-03 attribution)
  </read_first>
  <behavior>
    - File `docs/decision-trees/00-initial-triage.md` retains all pre-existing content (no lines modified or deleted)
    - File contains 5 new lines, each at the listed insertion point, mirroring the existing Android entry format
    - V-51-22 will pass: file contains ≥3 occurrences of `[Linux Triage](09-linux-triage.md)` literal link
  </behavior>
  <action>
Read `docs/decision-trees/00-initial-triage.md` in full to verify the current line numbers of the 5 insertion points (51-RESEARCH.md cites them as 11/40/122/133/138 but the file may have evolved; verify the existing Android entry pattern at each location before editing).

**Insertion #1 — Platform-gate banner area** (after the Android line near line 11):

Verify the existing Android banner reads exactly:
```
> **Android:** For Android enrollment/compliance troubleshooting, see [Android Triage](08-android-triage.md).
```

Append the following line VERBATIM immediately after that line (preserve any blank line that follows the Android banner):
```
> **Linux:** For Linux Intune client troubleshooting (Ubuntu LTS), see [Linux Triage](09-linux-triage.md).
```

**Insertion #2 — Scenario Trees list** (after the Android entry near line 40):

Verify the existing Android Scenario Trees entry reads exactly:
```
- [Android Triage](08-android-triage.md) — Android enrollment/compliance failure routing
```

Append the following line VERBATIM immediately after that line:
```
- [Linux Triage](09-linux-triage.md) — Linux Intune client (Ubuntu 22.04/24.04 LTS) failure routing
```

(Use em-dash `—` separator per existing Scenario Trees list convention.)

**Insertion #3 — See Also section** (after the Android entry near line 122):

Verify the existing Android See Also entry reads exactly:
```
- [Android Triage](08-android-triage.md) -- Android enrollment/compliance triage
```

Append the following line VERBATIM immediately after that line:
```
- [Linux Triage](09-linux-triage.md) -- Linux Intune client (Ubuntu LTS) triage
```

(Use double-hyphen `--` separator per existing See Also convention; the See Also section uses `--` while Scenario Trees uses `—`. Match each section's existing style.)

**Insertion #4 — Scenario Trees footer** (after the Android entry near line 133):

Verify the existing Android footer entry reads exactly:
```
- [Android Triage](08-android-triage.md)
```

Append the following line VERBATIM immediately after that line:
```
- [Linux Triage](09-linux-triage.md)
```

**Insertion #5 — Version History** (new top row, after line 138 header separator):

Verify the existing Version History table header is in reverse-chronological order (newest first). The current top row should be the Android-banner addition row (likely date 2026-04-23 or similar).

Append the following row as a NEW TOP ROW (immediately after the table header separator `|---|---|---|`):
```
| 2026-04-27 | Added Linux banner + triage link (Scenario Trees, See Also, Version History) | -- |
```

If the Version History table is in chronological-ascending order (oldest first), append at the bottom of the table instead — match existing direction.

DO NOT modify any pre-existing line in the file. DO NOT delete any existing entry. DO NOT touch the Windows / macOS / iOS / Android entries.

DO NOT commit yet — atomic commit owned by 51-08.
  </action>
  <verify>
    <automated>grep -c "\[Linux Triage\](09-linux-triage.md)" docs/decision-trees/00-initial-triage.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains the literal `> **Linux:** For Linux Intune client troubleshooting (Ubuntu LTS), see [Linux Triage](09-linux-triage.md).` blockquote (insertion #1)
    - File contains the literal `- [Linux Triage](09-linux-triage.md) — Linux Intune client (Ubuntu 22.04/24.04 LTS) failure routing` Scenario Trees entry (insertion #2; em-dash separator)
    - File contains the literal `- [Linux Triage](09-linux-triage.md) -- Linux Intune client (Ubuntu LTS) triage` See Also entry (insertion #3; double-hyphen separator)
    - File contains the literal `- [Linux Triage](09-linux-triage.md)` Scenario Trees footer entry (insertion #4)
    - File contains a Version History row dated `2026-04-27` mentioning Linux banner addition (insertion #5)
    - File contains AT LEAST 3 occurrences of the literal substring `[Linux Triage](09-linux-triage.md)` (V-51-22 floor; the 5 insertions yield ≥4 occurrences)
    - All pre-existing Windows/macOS/iOS/Android entries are preserved byte-identical (manual diff review confirms append-only)
    - File does NOT contain `TBD`, `TODO`, `FIXME`, `XXX` introduced by this edit
  </acceptance_criteria>
  <done>00-initial-triage.md has 5 Linux entries appended at platform-gate banner, Scenario Trees, See Also, footer, Version History; ≥3 occurrences of `[Linux Triage](09-linux-triage.md)` link; all pre-existing content preserved; V-51-22 passes; no commit yet.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-51-19 | Tampering (append-only contract violation) | accidental modification of existing Android/iOS/macOS lines | mitigate | Manual diff review at 51-08 atomic commit confirms only Linux additions. V-51-21 + V-51-22 are presence checks (positive); they don't enforce that pre-existing content is byte-identical. Plan executor MUST not modify any line that existed before this plan. |
| T-51-20 | Information disclosure (PITFALL-12 sidecar pin coords) | append targets are pinned in v1.5-audit-allowlist.json supervision_exemptions[] | accept | Per 51-RESEARCH.md File-State Verification: `00-index.md` and `00-initial-triage.md` are NOT in supervision_exemptions[] (Android files only). PITFALL-12 motivation does not transfer per CONTEXT D-22 + DPO-07. |

No application security threats.
</threat_model>

<verification>
After both tasks complete:

```bash
# V-51-21 surface
grep -c "^## Linux L1 Runbooks$" docs/l1-runbooks/00-index.md   # expect 1
grep -c "30-linux-enrollment-failed.md" docs/l1-runbooks/00-index.md  # expect >=1
grep -c "31-linux-compliance-non-compliant.md" docs/l1-runbooks/00-index.md  # expect >=1
grep -c "32-linux-ca-blocking-web-access.md" docs/l1-runbooks/00-index.md  # expect >=1
grep -c "33-linux-agent-service-failure.md" docs/l1-runbooks/00-index.md  # expect >=1

# V-51-22 surface
grep -c "\[Linux Triage\](09-linux-triage.md)" docs/decision-trees/00-initial-triage.md  # expect >=3
grep -c "Linux:\*\*" docs/decision-trees/00-initial-triage.md  # expect >=1 (banner)
```

Run validator (after 51-06 completes):
```bash
node scripts/validation/check-phase-51.mjs --verbose
# expect V-51-21 PASS + V-51-22 PASS
```

Manual diff review:
```bash
git diff docs/l1-runbooks/00-index.md docs/decision-trees/00-initial-triage.md
# Confirm only Linux additions; no pre-existing line modifications.
```

DO NOT commit yet — atomic commit owned by 51-08.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 51 Success Criteria:
- **SC#4 (`l1-runbooks/00-index.md` Linux section appended; `decision-trees/00-initial-triage.md` Linux branch added; both append-only):** ✅ Both Tasks 1 and 2 deliver

Plan complete when:
- [ ] `docs/l1-runbooks/00-index.md` has Linux L1 Runbooks H2 + 4-row table appended (V-51-21)
- [ ] `docs/decision-trees/00-initial-triage.md` has 5 Linux entries appended at 5 positions (V-51-22 ≥3 occurrences)
- [ ] All pre-existing content preserved byte-identical
- [ ] No commit yet — 51-08 owns atomicity
</success_criteria>

<output>
After completion, create `.planning/phases/51-linux-l1-triage-runbooks-30-33/51-07-SUMMARY.md` documenting:
- Both files modified + line counts before/after
- Insertion points used (line numbers + verbatim mirror of existing Android entries)
- V-51-21 + V-51-22 verification commands run
- Append-only contract verification (no pre-existing lines modified)
- Note: NO COMMIT — atomic commit owned by 51-08
</output>
