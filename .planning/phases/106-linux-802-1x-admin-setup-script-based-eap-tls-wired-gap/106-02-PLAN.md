---
phase: 106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap
plan: 02
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - docs/admin-setup-8021x/00-overview.md
autonomous: true
requirements: [DOT1X-08]

must_haves:
  truths:
    - "GOAL discoverability: the local 00-overview.md platform-guide list item 7 becomes a descriptive one-liner linking 07-linux.md (replacing the placeholder 'Platform guide (Phase 106) -- entry added when guide is authored' at line 36), so a reader navigating the 802.1X folder entry point can discover the newly authored Linux guide"
    - "SC1 + SC3 surfaced in the entry: the item-7 description names the Linux-distinguishing content (no native Intune Wi-Fi/wired/cert-delivery profiles; EAP-TLS via nmcli NetworkManager 802-1x.* as an OS-level workaround; out-of-band certificate prerequisites; Ubuntu 24.04/26.04 LTS; PEAP-MSCHAPv2 + EAP-TTLS out of scope), matching the existing item-3/4/5/6 descriptive-one-liner style (bold linked title, double-dash, no trailing period)"
    - "Navigation-last (local-only): only the in-folder 00-overview.md item-7 list line + one Change-History row are edited; no capability-matrix rows and no global nav-hub files (index.md, quick-refs, decision-trees, l1/l2 indexes) are touched (those are Phase 109)"
    - "The pre-existing wired-availability note at line 38 (which already pre-frames Linux as 'script-based EAP-TLS only via nmcli; see the Linux guide for details') is NOT modified — the item-7 link now connects that description to the actual guide; the Mermaid setup-sequence node, the Scope section, and See Also are unchanged"
    - "Harness allowlisting: 00-overview.md is a pre-existing file; the edit must be recorded for harness allowlisting, the same pattern used for the Phase 105 item-6 edit, the Phase 104 item-5 edit, the Phase 103 item-4 edit, and the Phase 102 item-3 edit"
  artifacts:
    - path: "docs/admin-setup-8021x/00-overview.md"
      provides: "Local 802.1X folder entry point with the Linux platform-guide entry (item 7) — the final per-platform entry, completing items 3-7"
      contains: "](07-linux.md)"
  key_links:
    - from: "docs/admin-setup-8021x/00-overview.md"
      to: "docs/admin-setup-8021x/07-linux.md"
      via: "platform-guide list item 7"
      pattern: "\\]\\(07-linux\\.md\\)"
---

<objective>
Fill item 7 of the local `docs/admin-setup-8021x/00-overview.md` platform-guide numbered list with the live descriptive link to `07-linux.md`, replacing the item-7 placeholder line ("7. Platform guide (Phase 106) -- entry added when guide is authored." at line 36), and append one Change-History row. This is the final per-platform entry, completing the numbered list (items 3-7).

Purpose: Make the just-authored `07-linux.md` discoverable from the 802.1X folder entry point. This is the ONLY in-scope navigation edit for Phase 106 — capability matrices and global nav hubs are deferred to Phase 109 (navigation-last).

Output: `docs/admin-setup-8021x/00-overview.md` (EDIT — the item-7 placeholder line replaced with the live link + a Change-History row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/admin-setup-8021x/00-overview.md
@.planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-PATTERNS.md
@.planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill item 7 (Linux) in 00-overview.md with the live 07-linux.md link + Change-History row</name>
  <files>docs/admin-setup-8021x/00-overview.md</files>
  <read_first>
    - docs/admin-setup-8021x/00-overview.md — the live file (source of truth): locate the item-7 placeholder line `7. Platform guide (Phase 106) -- entry added when guide is authored.` (~line 36, the single edit target); confirm the item-3/4/5/6 link style above it (item 6 is the Phase-105 Android precedent, ~line 34); confirm the wired-availability note (~line 38) already previews the Linux gap (DO NOT MODIFY it); inspect the Mermaid `C[3–7. Platform<br/>Guides]` node (~line 21) — it stays valid; locate the Change-History table (~lines 57-63) whose last row is the Phase-105 item-6 row
    - .planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-RESEARCH.md — Section 5 (`The Exact 00-overview.md Item-7 Edit`): the exact current line-36 target text, the item-7 replacement block, the Change-History row to append, and the explicit instruction NOT to modify the wired-availability note at line 38
    - .planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-PATTERNS.md — `docs/admin-setup-8021x/00-overview.md (EDIT)` section: the current item-7 placeholder text, the exact item-7 replacement, the Wired-Availability-Note DO-NOT-MODIFY note, and the Change-History-Row format (append after the Android row, `Author` = `--`, double-hyphen dashes)
    - docs/admin-setup-8021x/07-linux.md — confirm the link target filename exists (created in Plan 106-01) before linking to it
  </read_first>
  <action>
    Edit `docs/admin-setup-8021x/00-overview.md`. Replace the single numbered-list line `7. Platform guide (Phase 106) -- entry added when guide is authored.` with the live item-7 descriptive one-liner (from RESEARCH Section 5), matching the item-3/4/5/6 style — bold linked title `**[Linux 802.1X Admin Setup (EAP-TLS via nmcli)](07-linux.md)**`, then `--`, then a single description naming the key content: no native Intune Wi-Fi, wired, or cert-delivery profiles for Linux; the guide documents EAP-TLS via nmcli (NetworkManager `802-1x.*`) as an OS-level workaround, with out-of-band certificate prerequisites; Ubuntu 24.04 LTS and 26.04 LTS; PEAP-MSCHAPv2 and EAP-TTLS out of scope (not in verifiable Microsoft/vendor sources for Intune-managed Linux fleets). No trailing period, matching the sibling entries. Keep the double-dash em-dash style; preserve any en-dash characters used elsewhere in the file.

    Do NOT modify the wired-availability note at line 38 (it already correctly pre-frames Linux as "script-based EAP-TLS only via nmcli; see the Linux guide for details" — the item-7 link now connects that phrase to the guide). Do NOT modify the Mermaid `3–7. Platform Guides` node (it remains valid with all guides now authored), the Scope section, the See Also section, or any other file.

    Append a Change-History row dated 2026-06-30 after the existing Phase-105 Android row, following the established pattern: `| 2026-06-30 | Added item 7 -- Linux platform-guide entry linking 07-linux.md; placeholder removed | -- |`.

    Record in the SUMMARY that `00-overview.md` is a pre-existing file and this edit must be added to the harness allowlist (same handling as the Phase 105 item-6 edit, the Phase 104 item-5 edit, the Phase 103 item-4 edit, and the Phase 102 item-3 edit).
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q "](07-linux.md)" docs/admin-setup-8021x/00-overview.md && ! grep -q "7. Platform guide (Phase 106) -- entry added when guide is authored" docs/admin-setup-8021x/00-overview.md && grep -q "](06-android.md)" docs/admin-setup-8021x/00-overview.md && grep -q "](05-ios.md)" docs/admin-setup-8021x/00-overview.md && grep -q "](04-macos.md)" docs/admin-setup-8021x/00-overview.md && grep -q "](03-windows.md)" docs/admin-setup-8021x/00-overview.md && grep -q "Added item 7 -- Linux" docs/admin-setup-8021x/00-overview.md && grep -q "script-based EAP-TLS only via nmcli; see the Linux guide for details" docs/admin-setup-8021x/00-overview.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `00-overview.md` numbered list item `7.` is a bold link `](07-linux.md)` with a single description (no trailing period), matching the item-3/4/5/6 format and naming the no-native-Intune-profile gap, EAP-TLS via nmcli (NetworkManager `802-1x.*`), out-of-band cert prerequisites, Ubuntu 24.04/26.04 LTS, and PEAP/EAP-TTLS out of scope
    - The old placeholder line `7. Platform guide (Phase 106) -- entry added when guide is authored.` is gone
    - The pre-existing item 3 (`](03-windows.md)`), item 4 (`](04-macos.md)`), item 5 (`](05-ios.md)`), and item 6 (`](06-android.md)`) entries remain intact
    - The wired-availability note at line 38 is unchanged (still reads "Linux has no native Intune Wi-Fi or wired profile -- script-based EAP-TLS only via nmcli; see the Linux guide for details"); the Mermaid `3–7. Platform Guides` node, the Scope section, and See Also are unchanged
    - No other file is modified (no capability-matrix rows, no `docs/index.md` / quick-ref / decision-tree / l1-l2-index edits — navigation-last, Phase 109)
    - A Change-History row dated 2026-06-30 documenting the item-7 addition is appended (uses `--` double-hyphen dashes, `Author` = `--`)
    - The SUMMARY records that `00-overview.md` is a pre-existing file requiring harness-allowlist registration (Phase 105 item-6 / Phase 104 item-5 / Phase 103 item-4 / Phase 102 item-3 pattern)
  </acceptance_criteria>
  <done>The Linux guide is discoverable from the local 802.1X folder entry point via item 7, completing the per-platform numbered list (items 3-7); the wired-availability note and Mermaid node are confirmed unchanged; the pre-existing-file edit is flagged for harness allowlisting. DOT1X-08 navigation is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published-documentation artifact | `00-overview.md` is a static Markdown navigation file; the edit replaces one placeholder line with one link line and appends one Change-History row. No executable surface, no input parsing, no runtime. |

This is a documentation navigation edit with no executable attack surface. No high-severity code/runtime threats exist for this docs phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-02-01 | Tampering | Pre-existing file edited outside harness allowlist (integrity-tracking drift) | mitigate | Edit is scoped to the single item-7 numbered-list line + one Change-History row; SUMMARY flags the file for harness-allowlist registration (Phase 105 item-6 / Phase 104 item-5 / Phase 103 item-4 / Phase 102 item-3 pattern) |
| T-106-02-02 | Tampering | Accidental modification of the pre-framed wired-availability note or Mermaid node | mitigate | Acceptance asserts line 38 wired note and the Mermaid `3–7` node are byte-unchanged; verify greps confirm the wired note string persists |
| T-106-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no scripts; documentation-only one-line edit — no supply-chain surface |

Block-on severity: high. No high-severity content-security obligations apply to a navigation link edit; the tracked concerns are allowlist-integrity hygiene (T-106-02-01) and not disturbing the pre-framed Linux wired note (T-106-02-02).
</threat_model>

<verification>
- `00-overview.md` links to `07-linux.md` as numbered item 7 in the platform-guide list, completing items 3-7.
- The item-7 placeholder line is replaced with the live descriptive link (no trailing period, sibling style).
- The existing item 3 (`03-windows.md`), item 4 (`04-macos.md`), item 5 (`05-ios.md`), item 6 (`06-android.md`), the wired-availability note (line 38), and the Mermaid range node are unchanged.
- No global nav-hub or capability-matrix files are touched (navigation-last invariant honored for Phase 106).
- The pre-existing-file edit is recorded for harness allowlisting.
</verification>

<success_criteria>
- The Linux 802.1X guide is reachable from the local 802.1X overview entry point.
- Edit is surgical (the item-7 placeholder line replaced with the live link, plus one Change-History row); nothing else in `00-overview.md` changes — the wired-availability note at line 38 is untouched.
- Harness-allowlist registration is flagged in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-02-SUMMARY.md` when done.
</output>
