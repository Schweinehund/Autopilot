---
phase: 106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-setup-8021x/07-linux.md
autonomous: true
requirements: [DOT1X-08]

must_haves:
  truths:
    - "GOAL + SC1 + DOT1X-08 + D-03: 07-linux.md is created and OPENS (before any config content) with a prominent `> **WARNING --` lead callout (double-hyphen house form; NOT `IMPORTANT` which is out-of-vocabulary; NOT `CRITICAL`/`DANGER` which are hazard-reserved) stating Intune provides no native Wi-Fi, wired, or cert-delivery profiles for Linux and the documented approach is an OS-level shell-script + nmcli workaround, not an Intune profile"
    - "SC3 + D-04 (TWO separate callouts): the SC1 HIGH-confidence gap WARNING leads the guide, and a SEPARATE MEDIUM-confidence callout stating the Linux Intune surface is actively developing appears AFTER the workaround content, carrying its own inline `*last_verified: 2026-06-30 · review_by: 2026-09-28*` stamp; the two callouts are NOT merged"
    - "SC3 + D-09: PEAP-MSCHAPv2 and EAP-TTLS each receive exactly ONE out-of-scope sentence (possible via nmcli but not in verifiable Microsoft/vendor sources for Intune-managed fleets); NO nmcli config detail is shown for either method"
    - "D-10 (co-equal-EAP protection): EAP-TLS-only is framed as a source-confidence / documentation-scope boundary, NOT a method preference; the framing links back to co-equal 01-eap-method-overview.md and never ranks or recommends EAP-TLS as 'preferred'/'best for Linux'"
    - "D-05: an applies-to / prerequisites note names Ubuntu 24.04 LTS and 26.04 LTS explicitly (the RESEARCH.md live-verified SUPERSESSION of the CONTEXT.md 22.04+24.04 pair; 22.04 support ends Aug 2026 and is off the MS Learn supported-platforms list); the versions are NOT placed in the front-matter `platform:` field (which stays the single token `linux`)"
    - "D-06 + SC2: followable nmcli `802-1x.*` EAP-TLS command steps are present (802-1x.eap tls, .identity, .ca-cert, .client-cert, .private-key, .private-key-password / -flags; Wi-Fi adds wifi-sec.key-mgmt wpa-eap + 802-11-wireless.ssid) PLUS a TWO-column `| nmcli Property | EAP-TLS Value / Placeholder |` reference parameter table (NOT the three-column sibling per-EAP matrix); NOT a full standalone runnable Bash script; the section ends with the LOCKED verification trio `nmcli connection show`, `ip addr show`, `journalctl -u NetworkManager` (NOT wpa_supplicant diagnostic variants — those are Phase 108)"
    - "D-07 + SECURITY: a separate, prominent out-of-band certificate-prerequisites note states the client cert + private key + CA root must be pre-placed on the device out-of-band (Intune delivers zero cert profiles to Linux); the nmcli params reference FILE PATHS (placeholders like /etc/certs/...) only; NO inline private-key material appears anywhere (no `-----BEGIN` PEM block)"
    - "D-08: an 'illustrative / validate before fleet deployment' MEDIUM-confidence disclaimer sits at the lead-in to the workaround section (distinct from the SC3 platform-surface callout)"
    - "D-01 + D-02: a top-level `## Wired` H2 (cross-guide parallelism) is present, collapsed to point at the SAME nmcli EAP-TLS workaround with `connection.type ethernet` (drop wifi-sec.key-mgmt + 802-11-wireless.ssid); it is NOT Android's 'consult your network team' punt — Linux's wired alternative IS the OS-level nmcli path"
    - "Front-matter stamp block copied from 06-android.md :1-7 with only `platform: android` -> `platform: linux` (applies_to: both, audience: admin, last_verified: 2026-06-30, review_by: 2026-09-28); Prerequisites blockquote + H1 + one-line Scope banner -> 02-cert-delivery-foundation.md#canonical-scope-callout replicated verbatim"
    - "Link-not-copy: shared theory (cert-delivery ordering, EKU, server-name-validation rationale, EAP-method comparison, identity-privacy) is LINKED to 01-/02-/_glossary-network.md, never restated; See-Also + Change-History footer mirror 06-android.md; NO sibling-only mechanics cloned (Windows dot3svc/TEAP/KB5014754; macOS keychain channel; iOS MAC label / M-series wired; Android enrollment modes / UPN-in-SAN / 'Use device MAC')"
  artifacts:
    - path: "docs/admin-setup-8021x/07-linux.md"
      provides: "Linux (Ubuntu 24.04/26.04 LTS) 802.1X EAP-TLS admin-setup guide via nmcli 802-1x.* connection parameters, with the no-native-Intune-profile gap leading the guide, out-of-band cert prerequisites, PEAP/EAP-TTLS out of scope, and a Wired H2, satisfying DOT1X-08"
      contains: "## Wired"
      min_lines: 90
  key_links:
    - from: "docs/admin-setup-8021x/07-linux.md"
      to: "docs/admin-setup-8021x/02-cert-delivery-foundation.md#canonical-scope-callout"
      via: "one-line scope banner link (Phase 101 D-06)"
      pattern: "02-cert-delivery-foundation\\.md#canonical-scope-callout"
    - from: "docs/admin-setup-8021x/07-linux.md"
      to: "docs/admin-setup-8021x/01-eap-method-overview.md"
      via: "co-equal-EAP reconciliation link (D-10)"
      pattern: "01-eap-method-overview\\.md"
    - from: "docs/admin-setup-8021x/07-linux.md"
      to: "docs/admin-setup-8021x/02-cert-delivery-foundation.md"
      via: "cert-delivery-foundation link-not-copy reference"
      pattern: "02-cert-delivery-foundation\\.md"
    - from: "docs/admin-setup-8021x/07-linux.md"
      to: "docs/_glossary-network.md"
      via: "network glossary See-Also link"
      pattern: "_glossary-network\\.md"
---

<objective>
Author `docs/admin-setup-8021x/07-linux.md` — the Linux (Ubuntu LTS) per-platform 802.1X admin-setup guide, the FIFTH and FINAL per-platform guide and the DEEPEST GAP PLATFORM: Intune provides ZERO native surface for Linux (no Wi-Fi profile, no wired profile, no cert-delivery profiles), so the entire guide documents an OS-level shell-script + `nmcli` (NetworkManager `802-1x`) EAP-TLS workaround, with the platform gap LEADING the whole guide. This plan delivers the complete `07-linux.md` file in two tasks — satisfying DOT1X-08 and ROADMAP Phase 106 success criteria SC1–SC3, and honoring all 10 locked CONTEXT decisions (D-01..D-10).

Task 1 delivers the SCAFFOLD + LEAD GAP WARNING + SCOPE/PREREQUISITE backbone (front-matter, Prerequisites, H1, Scope banner, the SC1 lead gap WARNING, the EAP method scope note, the Ubuntu applies-to note, and the out-of-band certificate-prerequisites note). Task 2 delivers the WORKAROUND BODY (validate-before-fleet disclaimer, nmcli EAP-TLS Wi-Fi steps + two-column reference parameter table, verification trio, the `## Wired` H2, the SC3 MEDIUM-confidence freshness callout, and the See-Also / Change-History closeout).

Purpose: Give an operator a single Linux guide that opens with the settled no-native-Intune-profile gap fact, walks the nmcli `802-1x.*` EAP-TLS parameter steps for Wi-Fi and wired, handles certificates out-of-band with placeholder file paths (never inline key material), marks PEAP/EAP-TTLS out of scope as a source-confidence boundary (NOT a method preference), and LINKS — never restates — the shared 802.1X foundation in `01-`/`02-`/`_glossary-network.md`. This guide clones the immediately-prior gap deliverable `06-android.md` as its scaffold and applies the deepest-gap degradation, stripping all sibling-only mechanics entirely.

Output: `docs/admin-setup-8021x/07-linux.md` (CREATE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-CONTEXT.md
@.planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-RESEARCH.md
@.planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-PATTERNS.md
@docs/admin-setup-8021x/06-android.md
@docs/admin-setup-8021x/01-eap-method-overview.md
@docs/admin-setup-8021x/02-cert-delivery-foundation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold + lead gap WARNING (SC1) + EAP scope note + Ubuntu applies-to + out-of-band cert-prerequisites note</name>
  <files>docs/admin-setup-8021x/07-linux.md</files>
  <read_first>
    - docs/admin-setup-8021x/07-linux.md — the file being created (does not exist yet)
    - docs/admin-setup-8021x/06-android.md — the NEAREST clone scaffold (immediately-prior gap deliverable): front-matter stamp block (lines 1-7), Prerequisites blockquote (9-10), H1 + Scope banner (12-14), and the lead `> **WARNING --` form (line 79). Clone the front-matter changing ONLY `platform: android` -> `platform: linux`; replicate Prerequisites + Scope banner verbatim. Do NOT clone Android's Common Profile Mechanics, enrollment-mode matrix, UPN-in-SAN WARNING, or 'Use device MAC' note
    - .planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-RESEARCH.md — Section 3.1 front-matter template, 3.2 Prerequisites, 3.3 Scope banner, 3.4 lead WARNING tier (double-hyphen; vocabulary census NOTE/WARNING/DANGER/CRITICAL only, IMPORTANT out-of-vocab); Section 4 proposed section outline (lead WARNING -> EAP Method Scope Note -> Applies To -> Certificate Prerequisites); Flag 3 (Ubuntu 24.04 + 26.04 supersedes 22.04 + 24.04); Section 5 is Task-2/Plan-2 scope
    - .planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-CONTEXT.md — locked decisions D-03 (WARNING tier), D-04 (two callouts — SC1 gap fact leads), D-09 (PEAP/EAP-TTLS one sentence each), D-10 (co-equal source-confidence framing), D-05 (Ubuntu applies-to note, plan-time verify), D-07 (out-of-band cert prerequisites, file paths only, no inline keys)
    - .planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-PATTERNS.md — Front-Matter Stamp Block rules, Prerequisites Blockquote, H1 + Scope Banner, Lead Gap WARNING Callout (D-03/SC1), NOTE Callout Form, Link-Not-Copy Boundary table, Anchor Slug Rules (double-hyphen trap)
    - docs/admin-setup-8021x/02-cert-delivery-foundation.md — confirm the live `#canonical-scope-callout` anchor slug before linking the Scope banner; confirm this is the link target for cert-delivery concepts referenced from the cert-prerequisites note (link-not-copy)
    - docs/admin-setup-8021x/01-eap-method-overview.md — confirm the live filename/anchor for the D-10 co-equal reconciliation link
  </read_first>
  <action>
    Create `docs/admin-setup-8021x/07-linux.md`. Start with the 5-key YAML front-matter stamp block cloned from `06-android.md` lines 1-7 with `platform: linux` (all other keys identical: `last_verified: 2026-06-30`, `review_by: 2026-09-28`, `applies_to: both`, `audience: admin`). Do NOT put Ubuntu version numbers in `platform:` — it stays the single lowercase token `linux` (D-05). Follow with the Prerequisites blockquote copied verbatim from `06-android.md` lines 9-10 (linking `01-eap-method-overview.md` and `02-cert-delivery-foundation.md`), then the H1 `# Linux 802.1X Admin Setup: EAP-TLS via nmcli`, then the one-line Scope banner copied verbatim from `06-android.md` line 14 linking `02-cert-delivery-foundation.md#canonical-scope-callout`.

    Immediately after the Scope banner (before ANY config content), author the SC1 LEAD GAP WARNING as a `> **WARNING -- <short label>**` blockquote with `>` continuation lines (block form, per D-03/D-04 and RESEARCH Section 4). Use the double-hyphen separator `WARNING --` (NOT `WARNING:`, NOT `IMPORTANT`, NOT `CRITICAL`, NOT `DANGER`). The body (3-4 sentences) MUST convey: Intune provides no native Wi-Fi profile, no native wired profile, and no certificate-delivery profiles for Linux; the approach documented in this guide is an OS-level shell-script / nmcli workaround, NOT an Intune profile; certificate delivery is handled out-of-band (point forward to the certificate-prerequisites note below). This is the HIGH-confidence settled gap fact (SC1); do NOT merge the MEDIUM-confidence "actively developing" caveat into it (that is a separate callout authored in Task 2, per D-04).

    Author an `## EAP Method Scope Note` section (D-09/D-10) placed after the gap WARNING / at the head of the EAP-TLS workaround framing. Lead with the co-equal reconciliation sentence framing EAP-TLS-only as a SOURCE-CONFIDENCE / documentation-scope boundary, NOT a method preference: state that all three EAP methods remain co-equal (link `01-eap-method-overview.md`), and this guide documents EAP-TLS only because it is the sole method with verifiable nmcli/vendor documentation for Intune-managed Linux fleets — a source-confidence scope boundary, not a preference. Then add EXACTLY one out-of-scope sentence each for PEAP-MSCHAPv2 and EAP-TTLS (technically possible via nmcli but not documented in verifiable Microsoft/vendor sources for Intune-managed Linux fleets; out of scope for this guide). Do NOT rank or recommend EAP-TLS as "preferred"/"best for Linux" (that violates the locked co-equal constraint), and do NOT show any nmcli config detail for PEAP or EAP-TTLS (one sentence each only).

    Author an `## Applies To` note (D-05, using the RESEARCH Flag-3 live-verified versions): state the guide applies to Ubuntu 24.04 LTS and 26.04 LTS managed by Microsoft Intune (NOT 22.04 + 24.04 — 22.04 support ends Aug 2026 and is off the current MS Learn supported-platforms list); note that the nmcli/NetworkManager `802-1x.*` properties are distro-agnostic while per-version Intune agent capability differences may affect agent behavior, so always test on a representative device before fleet rollout. Keep the version names in this note, NOT in the front-matter.

    Author a `## Certificate Prerequisites (Out-of-Band)` note (D-07) with standalone prominence (a `> **Note --` blockquote is appropriate; calibrate callout-vs-prose per CONTEXT callout-discipline). State that Intune delivers no certificate profiles to Linux devices, so before running the nmcli commands the client certificate + private key + CA root certificate must be pre-placed on the device by a separate out-of-band process (e.g., an Intune Bash script or manual copy); list placeholder FILE PATHS only (e.g., `/etc/certs/ca-root.pem`, `/etc/certs/client-cert.pem`, `/etc/certs/private-key.pem`); state that file paths should be adjusted per deployment and that private-key material must NEVER be embedded in documentation; link cert-delivery concepts to `02-cert-delivery-foundation.md` (link-not-copy). SECURITY: do NOT include any real or example private-key PEM material — placeholder paths only.

    Use `--` (double hyphen) for em-dashes throughout. Use plain GitHub auto-slug headings (no `{#id}` overrides); avoid double-hyphen-in-heading slug traps. Do NOT yet author the nmcli command steps, the reference parameter table, the verification trio, the `## Wired` H2, the SC3 freshness callout, or the See-Also / Change-History footer (Task 2). Do NOT introduce any sibling-only mechanics (Windows dot3svc/TEAP/KB5014754; macOS keychain deployment channel; iOS MAC label / M-series wired; Android enrollment modes / UPN-in-SAN / 'Use device MAC').
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q "platform: linux" docs/admin-setup-8021x/07-linux.md && grep -q "review_by: 2026-09-28" docs/admin-setup-8021x/07-linux.md && grep -q "applies_to: both" docs/admin-setup-8021x/07-linux.md && grep -q "# Linux 802.1X Admin Setup: EAP-TLS via nmcli" docs/admin-setup-8021x/07-linux.md && grep -q "02-cert-delivery-foundation.md#canonical-scope-callout" docs/admin-setup-8021x/07-linux.md && grep -q "> \*\*WARNING --" docs/admin-setup-8021x/07-linux.md && grep -q "01-eap-method-overview.md" docs/admin-setup-8021x/07-linux.md && grep -qi "24.04" docs/admin-setup-8021x/07-linux.md && grep -qi "26.04" docs/admin-setup-8021x/07-linux.md && grep -qi "PEAP" docs/admin-setup-8021x/07-linux.md && grep -qi "EAP-TTLS" docs/admin-setup-8021x/07-linux.md && grep -qi "out-of-band\|out of band" docs/admin-setup-8021x/07-linux.md && ! grep -q "^-----BEGIN" docs/admin-setup-8021x/07-linux.md && ! grep -qi "22.04" docs/admin-setup-8021x/07-linux.md && ! grep -q "> \*\*IMPORTANT\|> \*\*CRITICAL\|> \*\*DANGER" docs/admin-setup-8021x/07-linux.md && ! grep -qi "dot3svc\|KB5014754\|deployment channel\|M-series\|Use device MAC\|UPN-in-SAN" docs/admin-setup-8021x/07-linux.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `07-linux.md` exists with front-matter `platform: linux`, `applies_to: both`, `audience: admin`, `last_verified: 2026-06-30`, `review_by: 2026-09-28`; no Ubuntu version numbers appear in the `platform:` field
    - A Prerequisites blockquote links `01-eap-method-overview.md` and `02-cert-delivery-foundation.md`; the H1 is `# Linux 802.1X Admin Setup: EAP-TLS via nmcli`; the one-line Scope banner links `02-cert-delivery-foundation.md#canonical-scope-callout`
    - The SC1 lead gap callout is a `> **WARNING --` blockquote placed BEFORE any config content, conveying no native Wi-Fi profile, no native wired profile, no cert-delivery profiles for Linux, and that the approach is an OS-level shell-script/nmcli workaround not an Intune profile (D-03); it does NOT use `IMPORTANT`/`CRITICAL`/`DANGER` and does NOT contain the "actively developing" caveat (D-04)
    - An `## EAP Method Scope Note` frames EAP-TLS-only as a source-confidence/documentation-scope boundary (NOT a preference), links co-equal `01-eap-method-overview.md`, and gives exactly one out-of-scope sentence each for PEAP-MSCHAPv2 and EAP-TTLS with NO nmcli config detail for either (D-09/D-10); no "recommended"/"preferred"/"best for Linux" language for EAP-TLS
    - An `## Applies To` note names Ubuntu 24.04 LTS and 26.04 LTS (NOT 22.04), notes the distro-agnostic nmcli mechanics + per-version agent-capability caveat + test-before-rollout (D-05)
    - A `## Certificate Prerequisites (Out-of-Band)` note states Intune delivers no cert profiles to Linux, lists placeholder file paths for CA root + client cert + private key, forbids inline key material, and links `02-cert-delivery-foundation.md` (D-07); no `-----BEGIN` PEM block appears anywhere
    - The forbidden sibling-only tokens `dot3svc`, `KB5014754`, `deployment channel`, `M-series`, `Use device MAC`, `UPN-in-SAN` do NOT appear
  </acceptance_criteria>
  <done>The scaffold, the SC1 lead gap WARNING, the EAP method scope note (D-09/D-10 co-equal framing), the Ubuntu 24.04/26.04 applies-to note (D-05), and the out-of-band cert-prerequisites note (D-07) are authored; the gap leads the guide and the scope/prerequisite backbone is in place, ready for the nmcli workaround body, Wired H2, and SC3 freshness callout in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Validate-before-fleet disclaimer + nmcli EAP-TLS steps + two-column reference table + verification trio (SC2) + Wired H2 + SC3 freshness callout + closeout</name>
  <files>docs/admin-setup-8021x/07-linux.md</files>
  <read_first>
    - docs/admin-setup-8021x/07-linux.md — the file created in Task 1 (append the workaround body, Wired H2, SC3 callout, and closeout; keep front-matter, lead WARNING, EAP scope note, applies-to, and cert-prerequisites note intact)
    - .planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-RESEARCH.md — Section 2 (the CONCRETE nmcli command steps for Wi-Fi and wired, the confirmed 802-1x.* property set, and the private-key-password-flags values), Section 3.5 inline freshness stamp form, Section 3.6 two-column reference table shape, Section 3.7 Wired H2 form, Section 3.8 See-Also/Change-History footer, Section 4 outline (workaround disclaimer -> config steps -> reference table -> verification -> Wired -> SC3 freshness callout -> footer), Open Questions (file:// prefix + ifname placeholder guidance)
    - .planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-CONTEXT.md — locked decisions D-06 (nmcli steps + reference table, no full runnable script, verification trio), D-08 (validate-before-fleet disclaimer at workaround lead-in), D-01/D-02 (Wired H2 collapsed to same nmcli via type ethernet, NOT 'consult network team'), D-04 (separate SC3 MEDIUM-confidence freshness callout)
    - .planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-PATTERNS.md — Per-EAP Config-Matrix Table Shape (two-column adaptation for EAP-TLS-only, with source-attribution trailing line), Wired H2 Form (bare `## Wired`, collapsed stub with concrete `type ethernet` command; Android 'consult network team' is DO-NOT-CLONE), Inline Freshness Stamp Form, See-Also + Change-History Footer, Sibling-Only Mechanics DO-NOT-CLONE table
    - docs/admin-setup-8021x/06-android.md — analog for the `## Wired` H2 form (line 110), the inline freshness stamp `*last_verified: 2026-06-30 · review_by: 2026-09-28*` (line 100), and the See-Also / Change-History footer structure (lines 116-128); do NOT clone Android's "consult your network team" wired body (line 112) or any Android-specific See-Also parentheticals
  </read_first>
  <action>
    Continue `docs/admin-setup-8021x/07-linux.md` after the Task-1 certificate-prerequisites note. Author, in this order:

    1. A `## EAP-TLS via nmcli (Wi-Fi)` section (use the parenthetical form to avoid the double-hyphen slug trap) opening with the D-08 "validate before fleet deployment / illustrative" disclaimer as a short `> **Note --` blockquote at the workaround lead-in: state that the nmcli commands below are illustrative, that parameters (SSID, file paths, identity) are placeholders, and that they must be validated on a test device before fleet deployment (this is MEDIUM-confidence content sourced from NetworkManager docs + vendor references, not an official MS Learn Linux 802.1X guide). Keep this distinct from the SC3 platform-surface callout.

    2. Followable nmcli EAP-TLS Wi-Fi command steps (D-06/SC2), using the exact property set and command form from RESEARCH Section 2: a `nmcli connection add type wifi ...` step with `wifi-sec.key-mgmt wpa-eap`, `802-1x.eap tls`, `802-1x.identity`, `802-1x.ca-cert`, `802-1x.client-cert`, `802-1x.private-key`, and `802-1x.private-key-password-flags 4`, followed by an `nmcli connection up` activation step. Use placeholder file PATHS only (e.g. `/etc/certs/...`) — NEVER inline private-key material. Add the brief flags explanation (`4` = unencrypted key / no passphrase; `0` = system-managed, also add `802-1x.private-key-password`). Optionally add the RESEARCH Open-Questions parenthetical that older NetworkManager versions may require a `file://` prefix on cert paths. Do NOT ship a full sprawling standalone runnable Bash script — the discrete nmcli command steps ARE the documented bash (D-06).

    3. A TWO-column reference parameter table titled to reference the nmcli `802-1x.*` EAP-TLS properties (D-06), with header `| nmcli Property | EAP-TLS Value / Placeholder |` (NOT the three-column `| Setting | EAP-TLS | PEAP-MSCHAPv2 | EAP-TTLS |` sibling matrix — the two-column form avoids implying co-equal PEAP/TTLS coverage). Rows cover at minimum: `802-1x.eap` = `tls`; `802-1x.identity`; `802-1x.ca-cert`; `802-1x.client-cert`; `802-1x.private-key`; `802-1x.private-key-password` (omit if key unencrypted); `802-1x.private-key-password-flags` (`4` unencrypted / `0` system-managed); `802-11-wireless-security.key-mgmt` = `wpa-eap` (Wi-Fi only; not used for wired); `802-11-wireless.ssid` (Wi-Fi only). Add the trailing source-attribution line: `Source: NetworkManager Reference Manual -- settings-802-1x (networkmanager.dev). Property values are illustrative placeholders; validate for your environment.`

    4. A `### Verification` subsection ending the workaround with the LOCKED verification trio (SC2): `nmcli connection show`, `ip addr show`, and `journalctl -u NetworkManager`, with a one-line purpose for each (connection status; IP-address assignment confirming RADIUS granted access; NetworkManager logs for EAP negotiation). Do NOT substitute wpa_supplicant diagnostic variants (those belong to the Phase 108 L2 row).

    5. A bare `## Wired` H2 (D-01/D-02), collapsed to point at the SAME nmcli EAP-TLS workaround via `connection.type ethernet`: state that the same EAP-TLS workaround applies to wired (Ethernet) 802.1X — change `connection.type` from `wifi` to `ethernet`, specify the interface name (`ifname`, e.g. discoverable via `ip link show`), and keep all `802-1x.*` properties identical; remove the `wifi-sec.key-mgmt` and `802-11-wireless.ssid` properties (not applicable to wired). Show the concrete `nmcli connection add type ethernet ...` command. Reinforce that Intune provides no native wired-network profile for Linux either, certificates are pre-placed out-of-band, and the same verification trio applies. This is NOT Android's "consult your network team" punt — do NOT clone that text; the Linux wired alternative IS the OS-level nmcli path (D-02).

    6. The SC3 MEDIUM-confidence freshness callout (D-04), a SEPARATE callout placed AFTER the workaround content, as a `> **Note -- Linux Intune surface is actively developing` blockquote: state that Intune's Linux management capabilities continue to evolve, that as of the guide's last_verified date Intune delivers no native Wi-Fi/wired/certificate profiles for Linux, and that the reader should verify the current feature set (link the MS Learn Linux deployment guide) before each major fleet deployment; close the blockquote with the inline stamp `> *last_verified: 2026-06-30 · review_by: 2026-09-28*`. This is a distinct callout from the SC1 lead WARNING (D-04) and from the D-08 validate-before-fleet disclaimer (D-08).

    7. Close with `## See Also` linking `01-eap-method-overview.md`, `02-cert-delivery-foundation.md`, and `../_glossary-network.md` (clone the `06-android.md` 116-120 structure; use Linux-accurate parentheticals — remove Android-specific UPN-in-SAN / SCEP-PKCS-delivery references that do not apply to Linux), then a `## Change History` table with one row dated 2026-06-30 summarizing the initial version (Linux 802.1X EAP-TLS admin setup via nmcli; platform gap lead WARNING; EAP method scope note with PEAP/TTLS out of scope; Ubuntu 24.04/26.04 LTS; out-of-band cert prerequisites; nmcli Wi-Fi + wired EAP-TLS steps; two-column reference parameter table; verification trio; MEDIUM-confidence freshness callout), Author column `--`.

    Confirm no inline private-key material appears anywhere (placeholder file paths only). Use `--` (double hyphen) for em-dashes. Use plain GitHub auto-slug headings; verify the `## EAP-TLS via nmcli (Wi-Fi)` heading auto-slugs cleanly (avoid a `--`-in-heading four-hyphen slug). Do NOT clone any sibling-only mechanics.
  </action>
  <verify>
    <automated>cd "D:/claude/Autopilot" && grep -q "## Wired" docs/admin-setup-8021x/07-linux.md && grep -q "type ethernet" docs/admin-setup-8021x/07-linux.md && grep -q "802-1x.eap" docs/admin-setup-8021x/07-linux.md && grep -q "802-1x.private-key" docs/admin-setup-8021x/07-linux.md && grep -q "wpa-eap" docs/admin-setup-8021x/07-linux.md && grep -q "| nmcli Property | EAP-TLS Value / Placeholder |" docs/admin-setup-8021x/07-linux.md && ! grep -q "| Setting | EAP-TLS | PEAP-MSCHAPv2 | EAP-TTLS |" docs/admin-setup-8021x/07-linux.md && grep -q "nmcli connection show" docs/admin-setup-8021x/07-linux.md && grep -q "ip addr show" docs/admin-setup-8021x/07-linux.md && grep -q "journalctl -u NetworkManager" docs/admin-setup-8021x/07-linux.md && ! grep -q "journalctl -u wpa_supplicant" docs/admin-setup-8021x/07-linux.md && grep -qi "validate.*before.*fleet\|before fleet deployment" docs/admin-setup-8021x/07-linux.md && grep -qi "actively developing" docs/admin-setup-8021x/07-linux.md && grep -q "review_by: 2026-09-28" docs/admin-setup-8021x/07-linux.md && grep -q "## See Also" docs/admin-setup-8021x/07-linux.md && grep -q "## Change History" docs/admin-setup-8021x/07-linux.md && ! grep -q "^-----BEGIN" docs/admin-setup-8021x/07-linux.md && ! grep -qi "consult your network" docs/admin-setup-8021x/07-linux.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - A `> **Note --` validate-before-fleet / illustrative disclaimer (D-08) sits at the workaround lead-in, distinct from the SC3 callout
    - Followable nmcli EAP-TLS Wi-Fi command steps are present using `802-1x.eap tls`, `802-1x.identity`, `802-1x.ca-cert`, `802-1x.client-cert`, `802-1x.private-key`, `802-1x.private-key-password-flags`, plus `wifi-sec.key-mgmt wpa-eap` and an `nmcli connection up` activation; placeholder file paths only; NOT a full standalone runnable script (D-06/SC2)
    - A two-column reference parameter table has header exactly `| nmcli Property | EAP-TLS Value / Placeholder |` and the three-column `| Setting | EAP-TLS | PEAP-MSCHAPv2 | EAP-TTLS |` header does NOT appear; a source-attribution line references networkmanager.dev
    - A `### Verification` subsection ends the workaround with the exact locked trio `nmcli connection show`, `ip addr show`, `journalctl -u NetworkManager`; `journalctl -u wpa_supplicant` does NOT appear (SC2)
    - A bare `## Wired` H2 shows the concrete `nmcli connection add type ethernet ...` command with `802-1x.*` identical and `wifi-sec.key-mgmt`/`802-11-wireless.ssid` removed; the text "consult your network" does NOT appear (D-01/D-02)
    - A SEPARATE SC3 MEDIUM-confidence `> **Note -- ... actively developing` callout appears AFTER the workaround content, links the MS Learn Linux deployment guide, and carries the inline `*last_verified: 2026-06-30 · review_by: 2026-09-28*` stamp (D-04)
    - `## See Also` links `01-`, `02-`, and `../_glossary-network.md` with Linux-accurate parentheticals; a `## Change History` table dated 2026-06-30 with the initial-version row is present
    - No `-----BEGIN` private-key material appears anywhere in the file
  </acceptance_criteria>
  <done>The workaround body (validate-before-fleet disclaimer, nmcli EAP-TLS Wi-Fi steps, two-column reference parameter table, verification trio), the `## Wired` H2 (collapsed to the same nmcli via type ethernet), the separate SC3 MEDIUM-confidence freshness callout, and the See-Also / Change-History closeout are authored; SC1 (gap leads), SC2 (followable nmcli steps + locked verification trio), and SC3 (PEAP/TTLS out of scope one-sentence-each + freshness-stamped actively-developing callout) are satisfied; D-01 through D-10 are fully in place. DOT1X-08 is fully delivered in `07-linux.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supplicant (managed Linux device) → authenticator (switch/AP) → RADIUS/NPS | 802.1X credential exchange; the guide's nmcli configuration guidance determines whether this boundary is hardened (server validation via `802-1x.ca-cert`, correct EAP-TLS client cert, out-of-band cert placement) |
| published-documentation artifact | `07-linux.md` is a static Markdown documentation file — no executable surface, no input parsing, no runtime. The one load-bearing content-security concern is whether the nmcli examples leak real credential material into the repo/site |

This is a pure-documentation phase. There are NO high-severity code/runtime threats — no code, no runtime, no user input, no data handling. The one genuine content-security mitigation (T-106-CRED) is captured below. Version-gated facts (Ubuntu LTS pair, Linux Intune surface) drift; mitigated by the 90-day front-matter + inline freshness stamps and the plan-time live-MS-Learn re-verification (RESEARCH confirmed the gap unchanged and updated the Ubuntu pair to 24.04 + 26.04 as of 2026-06-30).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-CRED | Information Disclosure | Published admin docs embed real credential material (private key, PFX password, or cert secret) inside the nmcli examples, leaking secrets into the repo/site | mitigate | The guide uses PLACEHOLDER file paths (`802-1x.private-key /etc/certs/private-key.pem`) and placeholder identifiers only; never inline key bytes; the private-key password is shown via `802-1x.private-key-password-flags` (agent/system-managed) rather than a literal password. Directly implements locked D-07/S2. Enforced by both tasks' acceptance (no `-----BEGIN` PEM block anywhere) |
| T-106-01 | Spoofing | Rogue RADIUS / credential harvest if server-validation guidance were wrong | mitigate | The EAP-TLS steps and reference table require `802-1x.ca-cert` (CA root for server verification); the cert-prerequisites note pre-places the CA root out-of-band; server-name-validation theory linked to `02-`/glossary (link-not-copy), never shown disabled |
| T-106-02 | Tampering (doc staleness) | Linux Intune surface / Ubuntu LTS pair drift between releases | mitigate | 90-day front-matter stamp + the SC3 inline-stamped "actively developing" callout; plan-time live verification updated the Ubuntu pair to 24.04 + 26.04 and confirmed the no-native-profile gap |
| T-106-03 | Tampering | Operator applies illustrative nmcli commands unvalidated in production | mitigate | The D-08 "validate before fleet deployment" disclaimer at the workaround lead-in flags the MEDIUM-confidence content |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no shipped scripts in this phase (RESEARCH Package Legitimacy Audit = Not Applicable); documentation-only file creation — no supply-chain surface |

Block-on severity: high. No high-severity code/runtime threats exist for this docs phase. The load-bearing content-security obligation T-106-CRED (no inline credential material — D-07) is encoded as blocking acceptance in BOTH tasks (no `-----BEGIN` PEM block); its absence is a blocking content-security gap.
</threat_model>

<verification>
- `07-linux.md` exists and leads with the SC1 `> **WARNING --` gap callout (no native Wi-Fi/wired/cert profiles; OS-level shell-script/nmcli workaround, not an Intune profile) BEFORE any config content (D-03/D-04).
- A separate MEDIUM-confidence "actively developing" SC3 callout with its own inline freshness stamp appears after the workaround content; the two callouts are not merged (D-04).
- PEAP-MSCHAPv2 and EAP-TTLS each get one out-of-scope sentence; EAP-TLS-only is framed as a source-confidence boundary (not a preference) linking co-equal `01-` (D-09/D-10).
- The Ubuntu applies-to note names 24.04 + 26.04 (not 22.04) and keeps versions out of the front-matter `platform:` field (D-05).
- The out-of-band cert-prerequisites note pre-places CA root + client cert + private key via placeholder file paths; no inline key material anywhere (D-07 / T-106-CRED).
- Followable nmcli `802-1x.*` EAP-TLS steps + a two-column reference parameter table (not the three-column sibling matrix) + the locked verification trio `nmcli connection show` / `ip addr show` / `journalctl -u NetworkManager` (not wpa_supplicant) are present (D-06/SC2).
- A validate-before-fleet disclaimer sits at the workaround lead-in (D-08).
- The `## Wired` H2 is collapsed to the same nmcli workaround via `type ethernet` (concrete command; wifi-sec/ssid removed) and is NOT Android's "consult your network team" punt (D-01/D-02).
- See-Also links `01-`/`02-`/`_glossary-network.md`; Change-History has the initial-version row; no sibling-only mechanics are cloned.
</verification>

<success_criteria>
- DOT1X-08 fully delivered by `docs/admin-setup-8021x/07-linux.md`.
- SC1: the guide opens with a prominent callout that Intune provides no native Wi-Fi/wired/cert-delivery profiles for Linux and the documented approach is a shell-script workaround (not an Intune profile) — present.
- SC2: an operator can follow the nmcli `802-1x.*` EAP-TLS steps and verify via `nmcli connection show`, `ip addr show`, and `journalctl -u NetworkManager` — present.
- SC3: PEAP-MSCHAPv2 and EAP-TTLS are documented as out of scope with a one-sentence explanation, and a MEDIUM-confidence freshness-stamped "surface actively developing" callout is present — present.
- The load-bearing content-security obligation (T-106-CRED: no inline credential material — D-07) is encoded and verifiable (no `-----BEGIN` PEM block).
</success_criteria>

<output>
Create `.planning/phases/106-linux-802-1x-admin-setup-script-based-eap-tls-wired-gap/106-01-SUMMARY.md` when done.
</output>
