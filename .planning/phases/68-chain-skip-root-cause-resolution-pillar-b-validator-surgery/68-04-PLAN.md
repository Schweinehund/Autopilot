---
phase: 68-chain-skip-root-cause-resolution-pillar-b-validator-surgery
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/MILESTONES.md
autonomous: true
requirements: []
tags:
  - planning-doc-cleanup
  - cdcce23-garbage-entry-deletion
  - v-61-19
  - v-61-20
  - phase-68
  - pillar-b
  - validator-surgery

must_haves:
  truths:
    - "`.planning/MILESTONES.md` lines 3-71 inclusive (69 lines total) have been DELETED — this is the cdcce23 garbage v1.5 H2 entry inserted by commit `cdcce23` (`chore: archive v1.5 milestone files`, 2026-05-07) on top of the correct entry; scripted-extraction debris (`One-liner:`, `Hash:`, `Pre-edit:`, `NO COMMIT MADE.` placeholders)"
    - "Post-delete `(Get-Content .planning/MILESTONES.md).Count` returns 138 (was 208 pre-edit; 208-70=138)"
    - "Post-delete `Select-String -Path .planning/MILESTONES.md -Pattern '^## v1.5'` returns exactly 1 match at line 3 (the correct entry, formerly at line 73, with `(Shipped: 2026-05-07)` not `(Shipped: 2026-05-08)`)"
    - "Post-delete `grep -c '^## v1.5' .planning/MILESTONES.md` returns 1 (single v1.5 H2 in the entire file)"
    - "The correct v1.5 entry (formerly lines 73-92, now at lines 3-22) preserves: `## v1.5 Linux Platform, Operational Depth & Cross-Platform Cleanup (Shipped: 2026-05-07)` H2 + `**Phases completed:** 14 phases (48-61), 96+ plans, ~150 tasks` + `**Audit status:** passed` + `**Key accomplishments:**` H3 with 4 Pillar sections + `**v1.4.1 deferred items closed:** DEFER-07 ... DEFER-08` + `**Methodology highlights:** Wave-based parallel execution...`"
    - "`node scripts/validation/check-phase-61.mjs` exits 0 with V-61-19 (Methodology highlights present) + V-61-20 (DEFER-07 + DEFER-08 cited) BOTH PASS (were FAIL pre-edit because indexOf('## v1.5 ') returned the garbage entry at line 3 which lacked Methodology highlights / DEFER-07-08 citations)"
    - "Zero content authoring (per CONTEXT.md D-04: V-61-19/20 are NOT content gaps — corpus defect from cdcce23 archive automation; surgical deletion of debris, NOT content addition)"
    - "Pre-delete boundary verification (per RESEARCH Assumptions A3 + A5): `sed -n '69,75p' .planning/MILESTONES.md` (or PowerShell equivalent) confirms line 71 is the boundary separator before the correct entry's line-73 H2; planner runs this BEFORE the delete and adjusts byte-range if boundary differs from assumed"
    - "File ends with a single trailing newline (markdown convention preserved); other v1.4/v1.4.1/v1.3 H2 entries below remain byte-unchanged"
  artifacts:
    - path: .planning/MILESTONES.md
      provides: "Single correct v1.5 H2 entry; cdcce23 garbage-debris entry removed; line count reduced 208→138"
      contains:
        - "# Milestones"
        - "## v1.5 Linux Platform, Operational Depth & Cross-Platform Cleanup (Shipped: 2026-05-07)"
        - "**Phases completed:** 14 phases (48-61), 96+ plans, ~150 tasks"
        - "**Audit status:** passed"
        - "**Methodology highlights:**"
        - "DEFER-07"
        - "DEFER-08"
  key_links:
    - from: ".planning/MILESTONES.md correct v1.5 H2 entry (post-delete at line 3, formerly line 73)"
      to: "scripts/validation/check-phase-61.mjs V-61-19 (Methodology highlights) + V-61-20 (DEFER-07 + DEFER-08 citations)"
      via: "indexOf('## v1.5 ', startIdx) substring scan starting from MILESTONES_DOC const at line 36"
      pattern: "## v1\\.5 Linux Platform.*Shipped: 2026-05-07"
    - from: ".planning/MILESTONES.md correct v1.5 H2 entry"
      to: "Source-of-truth attribution `965f509` (`docs(61-05): MILESTONES.md v1.5 entry append`)"
      via: "git log archaeology confirms 965f509 is the legitimate authoring commit; cdcce23 (`chore: archive v1.5 milestone files`) is the introducing garbage-debris commit"
      pattern: "965f509|cdcce23"
---

<objective>
Resolve V-61-19 + V-61-20 by surgically DELETING the cdcce23 garbage v1.5 H2 entry from `.planning/MILESTONES.md` lines 3-71 inclusive. This is a planning-doc corpus-defect remediation, NOT a content gap fix — per CONTEXT.md D-04 and RESEARCH.md §MILESTONES.md cdcce23 Defect, commit `cdcce23` (`chore: archive v1.5 milestone files`, 2026-05-07) inserted a SECOND, GARBAGE v1.5 H2 entry at MILESTONES.md lines 3-70 above the correct entry at lines 73-92. The garbage body contains scripted-extraction debris (`One-liner:`, `SUBSUMED BY PLAN 48-01.`, `Hash:`, `Pre-edit:`, `NO COMMIT MADE.` placeholders) rather than real bullet content. The correct entry at lines 73-92 already has full structured content satisfying V-61-19 (Methodology highlights) + V-61-20 (DEFER-07 + DEFER-08 citations) — the validator's `indexOf('## v1.5 ')` simply returns the garbage entry first and FAILs.

**Fix mechanism (per CONTEXT.md D-04 + RESEARCH §Plan 68-04):**
- DELETE `.planning/MILESTONES.md` lines 3-71 inclusive (69 lines total)
- Preserve line 1 `# Milestones` H1 + line 2 blank
- Preserve original lines 73-208 (the correct v1.5 entry + below)
- Post-delete: line count 208→138; single `^## v1.5` H2 at new line 3

**Why separated from Plan 68-03 (atomic CHAIN-03 commit):** Per CONTEXT.md D-04 §"Empirical grounding 1": "Corpus definition resolved — 4 independent sources all use 'corpus' = `docs/*`. `.planning/MILESTONES.md` is planning doc. SC#5 'no out-of-band corpus edits' does NOT block the MILESTONES.md edit. The 4-plan separation is NOT strictly required by SC#5 but is preserved for topological clarity at Phase 70 terminal re-audit (separate plans = separate revertibility per requirement)."

**Why NOT a CHAIN-NN requirement:** V-61-19 + V-61-20 are validator assertions that PASS when MILESTONES.md has the correct content; they are NOT in scope of CHAIN-01/02/03 (which are the CHAIN_SKIP root causes per REQUIREMENTS.md:18-22). Plan 68-04's frontmatter `requirements` field is empty by design — this plan supports V-61-19/V-61-20 PASS which is a precondition for Plan 68-03's CHAIN_SKIP removal (the chain must be cascade-green before CHAIN_SKIP can come off — verified at Plan 68-03 Wave 1).

**Discovery routed to v1.7-DEFERRED-CLEANUP.md (Plan 68-05 authors):** The cdcce23 archive-script garbage-insert ROOT CAUSE itself is out of Phase 68 scope (advisor D-04 §5.2). Plan 70 v1.7 milestone-archival may RE-TRIGGER the same defect — Plan 70 author MUST audit `.planning/MILESTONES.md` post-archival via pre-archive vs. post-archive diff. Plan 68-05 close-gate files this as deferred line item.

**Output:** ONE commit deleting 69 lines of MILESTONES.md (the cdcce23 garbage entry); check-phase-61 V-61-19 + V-61-20 BOTH PASS post-edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-CONTEXT.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-RESEARCH.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-PATTERNS.md
@.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-VALIDATION.md
@.planning/MILESTONES.md
@scripts/validation/check-phase-61.mjs

<interfaces>
<!-- Exact byte-range delete + verification commands. Executor MUST read MILESTONES.md head + check-phase-61.mjs V-61-NN bodies before editing. -->

**Pre-edit empirical state (per 68-RESEARCH.md §MILESTONES.md cdcce23 Defect — captured 2026-05-26):**

```
$ wc -l .planning/MILESTONES.md
208 .planning/MILESTONES.md

$ grep -n "^## v1.5" .planning/MILESTONES.md
3:## v1.5 Linux Platform, Operational Depth & Cross-Platform Cleanup (Shipped: 2026-05-08)   <-- garbage (cdcce23 attribution)
73:## v1.5 Linux Platform, Operational Depth & Cross-Platform Cleanup (Shipped: 2026-05-07)  <-- correct (965f509 attribution)
```

**Lines 3-70 garbage content sample (per 68-RESEARCH.md §"Lines 3-71 content sample"):**

```
## v1.5 Linux Platform, Operational Depth & Cross-Platform Cleanup (Shipped: 2026-05-08)

**Phases completed:** 14 phases, 101 plans, 70 tasks

**Key accomplishments:**

- One-liner:
- SUBSUMED BY PLAN 48-01.
- SUBSUMED BY PLAN 48-01.
- One-liner:
- One-liner:
- One-liner:
- One-liner:
- One-liner:
- One-liner:
- Co-management overview with 7 CB 2503 workloads, 3 slider states...
- One-liner:
...
- File:
- Hash:
- Total file size:
- NO COMMIT MADE.
...
```

This is scripted-extraction debris from archive automation that extracted placeholder labels instead of real bullet content.

**Lines 73-92 correct content (per 68-RESEARCH.md §"Line 73-92 content"):**

The correct v1.5 entry at line 73 has:
- Line 73: `## v1.5 Linux Platform, Operational Depth & Cross-Platform Cleanup (Shipped: 2026-05-07)` H2
- Line 75: `**Phases completed:** 14 phases (48-61), 96+ plans, ~150 tasks`
- Line 77: `**Audit status:** passed (.planning/milestones/v1.5-MILESTONE-AUDIT.md...)`
- Line 80: `**Key accomplishments:**` with 4 bulleted Pillar sections
- Line 87: `**v1.4.1 deferred items closed:** DEFER-07 ... DEFER-08`
- Line 89: `**Known deferred items at close...**`
- Line 91: `**Methodology highlights:** Wave-based parallel execution...`

This entry satisfies V-61-18 (Phases completed line) + V-61-19 (Key accomplishments + Methodology highlights) + V-61-20 (DEFER-07 + DEFER-08 citations).

**Git archaeology (per 68-RESEARCH.md §"Git archaeology"):**

```
$ git log --oneline -5 .planning/MILESTONES.md
cdcce23 chore: archive v1.5 milestone files (Linux Platform, Operational Depth & Cross-Platform Cleanup)   <-- introducing garbage commit
965f509 docs(61-05): MILESTONES.md v1.5 entry append + ROADMAP Phase 61 close + STATE.md milestone-complete   <-- correct authoring commit
6f91d14 chore: archive v1.4.1 milestone (Android Enterprise Completion & v1.4 Cleanup)
13d2c88 chore: archive v1.4 milestone files (MILESTONES.md, PROJECT.md, ROADMAP.md, STATE.md)
9a4cb4f chore: archive v1.3 milestone files
```

`965f509` (docs(61-05)) is the legitimate "MILESTONES.md v1.5 entry append" commit; `cdcce23` is the introducing commit of the garbage at lines 3-70.

**Exact byte-range delete (per 68-RESEARCH.md §"Plan 68-04 exact line range to DELETE"):**

DELETE `.planning/MILESTONES.md` lines 3 through 71 inclusive (69 lines total):
- Line 3: garbage `## v1.5 ... (Shipped: 2026-05-08)` H2
- Lines 4-70: garbage body (One-liner stubs, Hash:, NO COMMIT, etc.)
- Line 71: blank line separator (or `---` separator — planner verifies byte-exact boundary via `sed -n '69,75p'` before delete per Assumption A3+A5)

Post-delete:
- Line 1: `# Milestones: Windows Autopilot Troubleshooter` (or whatever the H1 is — preserved)
- Line 2: blank line (preserved)
- Line 3: `## v1.5 Linux Platform, Operational Depth & Cross-Platform Cleanup (Shipped: 2026-05-07)` (formerly line 73; correct entry)
- ... rest of file (formerly lines 74-208, now 4-138)

**Boundary verification (MANDATORY pre-delete per RESEARCH Assumptions A3 + A5):**

```
# Inspect lines 69-75 to confirm line 71 is the boundary separator (blank line OR --- horizontal rule)
# AND line 73 is the correct H2:

(Get-Content .planning/MILESTONES.md)[68..74] | ForEach-Object -Begin { $i = 69 } -Process { "{0,3}: {1}" -f $i, $_ ; $i++ }

# Expected output:
#  69: (last line of garbage body OR blank)
#  70: (last line of garbage body OR blank)
#  71: (blank OR --- separator)
#  72: (blank OR ---)
#  73: ## v1.5 Linux Platform, Operational Depth & Cross-Platform Cleanup (Shipped: 2026-05-07)
#  74: (blank line after H2)
#  75: **Phases completed:** 14 phases (48-61), 96+ plans, ~150 tasks
```

If line 73 is NOT the correct H2 (Shipped: 2026-05-07), STOP — investigate immediately. The byte-range may have shifted from RESEARCH baseline.

**check-phase-61.mjs V-61-19 + V-61-20 inspection (planner reads before editing):**

```
$ grep -n "V-61-19\|V-61-20" scripts/validation/check-phase-61.mjs
```

V-61-19 + V-61-20 use the `c.indexOf('## v1.5 ', startIdx)` pattern starting from `MILESTONES_DOC` const at line 36 (per RESEARCH §Target Validator Inventory check-phase-61 row); both assertions scan from the FIRST `## v1.5 ` H2 found.

**Post-delete verification commands (per 68-RESEARCH.md §Verification Harness Plan 68-04):**

```
(Get-Content .planning/MILESTONES.md).Count
# REQUIRED: 138 (208 - 70 deleted lines)

Select-String -Path .planning/MILESTONES.md -Pattern "^## v1.5"
# REQUIRED: 1 match at line 3 (the correct entry "Shipped: 2026-05-07")

(Get-Content .planning/MILESTONES.md)[0..4]
# REQUIRED: line 1 = "# Milestones..." H1; line 2 = blank; line 3 = "## v1.5 ... (Shipped: 2026-05-07)"

node scripts/validation/check-phase-61.mjs 2>&1 | Select-String "V-61-19|V-61-20"
# REQUIRED: both PASS

node scripts/validation/check-phase-61.mjs ; $LASTEXITCODE
# REQUIRED: 0 (34/34 PASS — but ONLY after Plan 68-02 ALSO landed; check-phase-61 cascades from Plan 68-02 + Plan 68-04)
```

**Note on cascade ordering:** Plan 68-04 is Wave 1 (file-disjoint from Plan 68-01 + Plan 68-02). However, check-phase-61's FULL pass (34/34) requires BOTH Plan 68-02 (cascade clearance) AND Plan 68-04 (V-61-19/20 clearance) to have landed. Plan 68-04 alone clears V-61-19 + V-61-20 specifically; other V-61-NN may still be in cascade-fail state from V-60 issues that Plan 68-02 fixes. The chain becomes cascade-green only when ALL three Wave 1 plans land.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 68-04-01: Verify pre-delete boundary + DELETE MILESTONES.md lines 3-71 + post-delete verification (V-61-19 + V-61-20 PASS) + atomic commit</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - .planning/MILESTONES.md (read full file — confirm line 3 is garbage `## v1.5 ... (Shipped: 2026-05-08)` H2; confirm line 73 is correct `## v1.5 ... (Shipped: 2026-05-07)` H2; inspect lines 69-75 boundary; verify file ends at line 208 ± tolerance; capture full file for revertibility)
    - scripts/validation/check-phase-61.mjs (read lines 30-36 — confirm MILESTONES_DOC const at line 36; read V-61-19 + V-61-20 check bodies — they use indexOf('## v1.5 ', startIdx) pattern; identify substring assertions for "Methodology highlights" + "DEFER-07" + "DEFER-08")
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-CONTEXT.md (re-read D-04 §"Empirical grounding 2" at lines 137-138 — V-61-19/20 NOT content gaps; corpus-defect cdcce23 archive automation introduced garbage entry; surgical deletion = zero content authoring)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-RESEARCH.md (re-read §MILESTONES.md cdcce23 Defect at lines 646-714 — pre-edit empirical state + git archaeology + line range verification + Phase 70 archive risk)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-PATTERNS.md (re-read §".planning/MILESTONES.md (Plan 68-04 — cdcce23 garbage-entry deletion)" at lines 530-608)
    - .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-VALIDATION.md (re-read lines 54-55 — Per-Task Verification Map entries 68-04-01 + 68-04-02)
  </read_first>
  <action>
**WAVE 1 — PRE-DELETE BOUNDARY VERIFICATION (MANDATORY per RESEARCH Assumptions A3 + A5):**

1. Capture pre-delete state:

```
$preDeleteLineCount = (Get-Content .planning/MILESTONES.md).Count
Write-Host "Pre-delete line count: $preDeleteLineCount"
# Expected: 208 (or close — if drift from 208, investigate WHY before proceeding)

Select-String -Path .planning/MILESTONES.md -Pattern "^## v1.5"
# Expected: 2 matches at lines 3 + 73 (the garbage at 3 + correct at 73)

# Inspect boundary (lines 69-75) to confirm:
#   - line 71 OR 72 is the boundary separator (blank line OR --- horizontal rule)
#   - line 73 is the CORRECT H2 with "(Shipped: 2026-05-07)"
(Get-Content .planning/MILESTONES.md)[68..74] | ForEach-Object -Begin { $i = 69 } -Process { "{0,3}: {1}" -f $i, $_ ; $i++ }
```

If the boundary verification shows line 73 is NOT the correct `## v1.5 ... (Shipped: 2026-05-07)` H2, OR shows a different boundary line count, STOP — investigate before proceeding. The byte-range may have shifted from RESEARCH baseline (captured 2026-05-26); do NOT proceed with assumed line numbers.

2. Inspect line 71 specifically (the last line to be deleted):

```
$line71 = (Get-Content .planning/MILESTONES.md)[70]
Write-Host "Line 71: '$line71'"
# Expected: blank line OR --- separator (NOT content from the correct entry)
```

3. Pre-delete check-phase-61 baseline (V-61-19 + V-61-20 FAIL state):

```
node scripts/validation/check-phase-61.mjs 2>&1 | Out-File -FilePath .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/_tmp-check-61-pre-delete.txt
Write-Host "check-phase-61 pre-delete exit=$LASTEXITCODE"

# Inspect V-61-19 + V-61-20 specifically:
Get-Content .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/_tmp-check-61-pre-delete.txt | Select-String "V-61-19|V-61-20"
# Expected: both report FAIL (with detail "Methodology highlights missing" + "DEFER-07 not cited" OR similar)
```

**WAVE 2 — SURGICAL DELETE OF LINES 3-71 INCLUSIVE (69 lines):**

4. Use the `Edit` tool (NOT `Write` — `Write` would rewrite the entire file and risk byte-level drift on lines 73-208). Use `Edit` with a multi-line `old_string` exactly matching lines 3-71 inclusive (from the file Read in step 1), replaced with empty string `""`.

If the `Edit` tool's `old_string` parameter is too long to be reliable as one match, use the following surgical alternative:

```
# Read the file
$content = Get-Content .planning/MILESTONES.md
# Take lines 1-2 (the H1 + blank) and lines 72-end (the correct entry + below)
$newContent = @() + $content[0..1] + $content[71..($content.Count-1)]
# Write back
$newContent | Set-Content -Path .planning/MILESTONES.md -Encoding utf8
```

**CRITICAL line-ending preservation:** PowerShell `Set-Content` with `-Encoding utf8` may inject CRLF line endings on Windows. Use `-Encoding utf8NoBOM` if available (PowerShell 6+); otherwise verify post-edit with `git diff` that line endings are unchanged. If git diff shows the entire file as changed (CRLF/LF drift), revert with `git checkout .planning/MILESTONES.md` and use the `Edit` tool with `old_string`/`new_string` instead (which preserves line endings byte-perfectly).

**Alternative: use the `Edit` tool with `old_string`** by reading lines 1-3 + lines 71-74 to construct a precise `old_string`/`new_string` pair that removes only the targeted range. The `old_string` could be: lines 1-3 garbage H2 (3 lines: `# Milestones` H1 + blank + garbage H2) → replace with: lines 1-3 correct (3 lines: `# Milestones` H1 + blank + correct H2). Then a second Edit removes the residual garbage body. This two-Edit approach is more reliable if the file is large.

**Recommended approach:**
1. First Read the file at offset=0, limit=80 — capture lines 1-80 exactly
2. Identify the exact line content for the boundary at line 72 (the blank between garbage and correct entry)
3. Use `Edit` with `old_string` = lines 3-71 verbatim (the garbage block) + `new_string` = empty string OR a single blank line

Either approach works — choose based on which is more reliable given the executor's tool environment.

**WAVE 3 — POST-DELETE VERIFICATION:**

5. Verify line count + H2 count:

```
$postDeleteLineCount = (Get-Content .planning/MILESTONES.md).Count
Write-Host "Post-delete line count: $postDeleteLineCount"
# REQUIRED: 138 (was 208; delta = 70 lines deleted; if -1 or +1 off, accept ± tolerance for trailing-newline edge case BUT re-verify content boundaries)

$h2Count = (Select-String -Path .planning/MILESTONES.md -Pattern "^## v1.5" | Measure-Object).Count
Write-Host "v1.5 H2 count: $h2Count"
# REQUIRED: 1 (single correct entry)

Select-String -Path .planning/MILESTONES.md -Pattern "^## v1.5"
# REQUIRED: 1 match at new line 3 with "(Shipped: 2026-05-07)" NOT "(Shipped: 2026-05-08)"
```

6. Verify top-of-file shape:

```
(Get-Content .planning/MILESTONES.md)[0..6] | ForEach-Object -Begin { $i = 1 } -Process { "{0,3}: {1}" -f $i, $_ ; $i++ }
# Expected:
#   1: # Milestones... (H1 preserved)
#   2: (blank)
#   3: ## v1.5 Linux Platform, Operational Depth & Cross-Platform Cleanup (Shipped: 2026-05-07)
#   4: (blank)
#   5: **Phases completed:** 14 phases (48-61), 96+ plans, ~150 tasks
#   6: (blank)
#   7: **Audit status:** passed (.planning/milestones/v1.5-MILESTONE-AUDIT.md...)
```

7. Verify content preservation (Methodology highlights + DEFER citations now in scan range):

```
Select-String -Path .planning/MILESTONES.md -Pattern "Methodology highlights"
# REQUIRED: 1+ match (the correct entry's Methodology highlights line)

Select-String -Path .planning/MILESTONES.md -Pattern "DEFER-07"
# REQUIRED: 1+ match

Select-String -Path .planning/MILESTONES.md -Pattern "DEFER-08"
# REQUIRED: 1+ match
```

8. Verify other H2 entries (v1.4, v1.4.1, v1.3, etc.) are byte-unchanged below the deleted range:

```
Select-String -Path .planning/MILESTONES.md -Pattern "^## "
# Expected: list of all H2 entries; the count should equal the pre-delete count MINUS 1 (one garbage H2 removed)
# Spot-check 2-3 of the older v1.4/v1.3 H2 entries to confirm they appear unchanged (compare against git blame OR the pre-edit baseline)
```

9. Run check-phase-61 to confirm V-61-19 + V-61-20 PASS:

```
node scripts/validation/check-phase-61.mjs 2>&1 | Out-File -FilePath .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/_tmp-check-61-post-delete.txt
Write-Host "check-phase-61 post-delete exit=$LASTEXITCODE"

Get-Content .planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/_tmp-check-61-post-delete.txt | Select-String "V-61-19|V-61-20"
# REQUIRED: both PASS
```

**Note on overall check-phase-61 exit code:** If Plan 68-02 has NOT yet landed (Plan 68-04 is wave-1 file-disjoint and can run before or in parallel with 68-02), check-phase-61 may still have other V-61-NN FAILs from V-60-cascade issues. The Plan 68-04 contract is ONLY V-61-19 + V-61-20 PASS — overall check-phase-61 exit code may still be non-zero if other V-61-NN are pending Plan 68-02. Verify ONLY V-61-19 + V-61-20 PASS as Plan 68-04's contract.

**WAVE 4 — ATOMIC COMMIT:**

10. Verify git status shows ONLY the MILESTONES.md change:

```
git status
# Expected: exactly 1 file modified — .planning/MILESTONES.md
```

If git status shows any other modified files, STOP — investigate (this plan should touch ONLY MILESTONES.md).

11. Atomic single-file commit (per CONTEXT.md D-04 Plan 68-04 = "MILESTONES.md cdcce23 corpus-defect remediation; Single commit; Planning-doc edit only"):

```
git add .planning/MILESTONES.md

git commit -m "$(cat <<'EOF'
fix(planning): remove duplicate v1.5 MILESTONES entry inserted by cdcce23 archive automation (Phase 68-04)

DELETE .planning/MILESTONES.md lines 3-71 inclusive (69 lines) — the cdcce23
garbage v1.5 H2 entry inserted by commit `cdcce23` (`chore: archive v1.5
milestone files`, 2026-05-07) on top of the correct entry at lines 73-92
(commit `965f509` `docs(61-05): MILESTONES.md v1.5 entry append`).

The garbage entry contains scripted-extraction debris (`One-liner:`,
`SUBSUMED BY PLAN 48-01.`, `Hash:`, `Pre-edit:`, `NO COMMIT MADE.`
placeholders) rather than real bullet content. The correct entry (formerly
at lines 73-92, now promoted to lines 3-22 by this deletion) has full
structured content satisfying V-61-18 (Phases completed) + V-61-19 (Key
accomplishments + Methodology highlights) + V-61-20 (DEFER-07 + DEFER-08
citations).

Per CONTEXT.md D-04 §"Empirical grounding 2": V-61-19 + V-61-20 are NOT
content gaps; they are a CORPUS DEFECT. The validator's
`indexOf('## v1.5 ')` was returning the garbage entry first → FAIL. After
deletion, `indexOf('## v1.5 ')` returns the correct entry → V-61-19 + V-61-20
PASS.

Zero content authoring (per CONTEXT.md D-04: surgical deletion of debris,
NOT content addition).

Pre-delete state (empirically verified per RESEARCH §MILESTONES.md cdcce23
Defect, 2026-05-26):
- line count: 208
- ^## v1.5 matches: 2 (lines 3 and 73)
- line 3: garbage H2 "(Shipped: 2026-05-08)"
- line 73: correct H2 "(Shipped: 2026-05-07)"

Post-delete state:
- line count: 138 (delta -70)
- ^## v1.5 matches: 1 (line 3)
- line 3: correct H2 "(Shipped: 2026-05-07)"
- check-phase-61.mjs V-61-19 + V-61-20: PASS

Per CONTEXT.md D-04 §"Empirical grounding 1": `.planning/MILESTONES.md` is
planning doc, NOT corpus (4 independent sources confirm "corpus" = `docs/*`).
SC#5 "no out-of-band corpus edits" does NOT block this edit; topological
separation from CHAIN-03 atomic commit preserved for auditor clarity at
Phase 70 terminal re-audit.

Discovery deferred to v1.7-DEFERRED-CLEANUP.md (Plan 68-05 authors with
SHA citation): cdcce23 archive-script garbage-insert ROOT CAUSE itself is
out of Phase 68 scope per CONTEXT D-04 §"Empirical grounding 5". Phase 70
v1.7 milestone-archival MAY RE-TRIGGER the same defect; Plan 70 author MUST
audit `.planning/MILESTONES.md` post-archival via pre-archive vs.
post-archive diff.

Other H2 entries (v1.4, v1.4.1, v1.3, etc.) below the deleted range remain
byte-unchanged.

Rollback: git revert <THIS_SHA> cleanly re-inserts the cdcce23 garbage
entry at lines 3-71 (per-plan boundary preserved per CONTEXT.md D-04
Option E; independently revertible from CHAIN-NN commits).

Phase-68-04
EOF
)"
```

12. Post-commit verify:

```
git log --name-only -1 HEAD
# Expected: exactly 1 file: .planning/MILESTONES.md

git show --stat HEAD
# Expected: 1 file changed; ~69 lines deleted, ~0 lines added (or 1 line if a trailing newline normalization happened)
```

13. Final post-commit re-run of check-phase-61 (proves the dry-run was faithful):

```
node scripts/validation/check-phase-61.mjs 2>&1 | Select-String "V-61-19|V-61-20"
# REQUIRED: both PASS

node scripts/validation/check-phase-61.mjs ; Write-Host "POST-COMMIT check-phase-61 exit=$LASTEXITCODE"
# Expected: 0 if Plan 68-02 ALSO landed; non-zero with V-61-NN other-cascade FAILs if Plan 68-02 has not yet landed (these resolve once Plan 68-02 lands; Plan 68-04 contract is V-61-19/V-61-20 only)
```

Capture commit SHA for Plan 68-05 close-gate authoring (substituted as `{68_04_SHA}` in Plan 68-03's canonical CHAIN_SKIP comment block).
  </action>
  <verify>
    <automated>$lc = (Get-Content .planning/MILESTONES.md).Count ; if ($lc -ne 138) { Write-Error "line count $lc, expected 138" } ; $h2Count = (Select-String -Path .planning/MILESTONES.md -Pattern "^## v1.5" | Measure-Object).Count ; if ($h2Count -ne 1) { Write-Error "expected 1 ^## v1.5 H2, got $h2Count" } ; $h2Line = (Select-String -Path .planning/MILESTONES.md -Pattern "^## v1.5").Line ; if (-not ($h2Line -like "*Shipped: 2026-05-07*")) { Write-Error "wrong v1.5 H2 preserved: $h2Line" } ; if ((Select-String -Path .planning/MILESTONES.md -Pattern "Methodology highlights" | Measure-Object).Count -lt 1) { Write-Error "Methodology highlights missing" } ; if ((Select-String -Path .planning/MILESTONES.md -Pattern "DEFER-07" | Measure-Object).Count -lt 1) { Write-Error "DEFER-07 citation missing" } ; if ((Select-String -Path .planning/MILESTONES.md -Pattern "DEFER-08" | Measure-Object).Count -lt 1) { Write-Error "DEFER-08 citation missing" } ; if ((Select-String -Path .planning/MILESTONES.md -Pattern "One-liner:" | Measure-Object).Count -gt 0) { Write-Error "garbage 'One-liner:' debris still present" } ; if ((Select-String -Path .planning/MILESTONES.md -Pattern "NO COMMIT MADE\." | Measure-Object).Count -gt 0) { Write-Error "garbage 'NO COMMIT MADE.' debris still present" } ; node scripts/validation/check-phase-61.mjs 2>&1 | Select-String "V-61-19|V-61-20" | ForEach-Object { if ($_ -match "FAIL") { Write-Error "V-61-19/20 still FAIL post-delete: $_" } }</automated>
    <expected>line count = 138; exactly 1 ^## v1.5 H2 at line 3 with "Shipped: 2026-05-07" (NOT 2026-05-08); Methodology highlights + DEFER-07 + DEFER-08 all present (at least 1 match each); zero garbage debris remaining (no "One-liner:" placeholders, no "NO COMMIT MADE."); check-phase-61 V-61-19 + V-61-20 both PASS.</expected>
  </verify>
  <done>.planning/MILESTONES.md lines 3-71 deleted (69 lines; cdcce23 garbage v1.5 H2 entry removed); line count 208→138; single ^## v1.5 H2 at new line 3 with "Shipped: 2026-05-07"; Methodology highlights + DEFER-07 + DEFER-08 citations preserved; zero garbage debris remaining; check-phase-61 V-61-19 + V-61-20 BOTH PASS; ONE single-file atomic commit landed; commit message body cites Phase 68-04 + corpus-defect rationale + Plan 70 archive-risk forward-pointer; git revert cleanly re-inserts the cdcce23 garbage; commit SHA captured for Plan 68-05 close-gate canonical-comment-block SHA substitution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Surgical byte-range deletion ↔ markdown structural integrity | Delete preserves H1 (line 1) + blank (line 2) + correct H2 entry (formerly line 73, now line 3); other H2 entries below remain byte-unchanged; markdown convention (blank-line-before-H2) preserved |
| Pre-delete boundary verification ↔ assumed line 73 H2 location | Planner runs `(Get-Content)[68..74]` boundary inspection BEFORE deleting to confirm byte-range hasn't drifted from RESEARCH 2026-05-26 baseline |
| Line-ending preservation ↔ PowerShell encoding pitfalls | If executor uses `Set-Content` with default encoding, CRLF/LF drift may occur on Windows; mitigation = use `Edit` tool with `old_string`/`new_string` for byte-perfect line-ending preservation, OR verify post-edit `git diff` shows ONLY the targeted line range changes |
| Phase 70 archive-script re-trigger risk ↔ Plan 68-04 symptom fix | This plan deletes the symptom (cdcce23 garbage entry); RESEARCH §"Phase 70 archive risk" + advisor D-04 §5.2 flag the archive-script root cause for v1.7-DEFERRED-CLEANUP.md investigation; Plan 68-05 close-gate authors that deferred item |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04-BD | Tampering (boundary drift — line 73 H2 location may have shifted from RESEARCH 2026-05-26 baseline) | .planning/MILESTONES.md byte-range | mitigate | Pre-delete boundary verification via `(Get-Content)[68..74]` MANDATORY before delete; if line 73 is NOT the correct H2 with "(Shipped: 2026-05-07)", STOP and investigate |
| T-68-04-LE | Tampering (line-ending drift from PowerShell Set-Content encoding) | .planning/MILESTONES.md whole-file rewrite via Set-Content | mitigate | Recommended approach uses `Edit` tool with `old_string`/`new_string` for byte-perfect line-ending preservation; if Set-Content used, post-edit `git diff` MUST show ONLY targeted line range changes (not entire-file diff indicating CRLF/LF drift) |
| T-68-04-OD | Tampering (over-delete — accidentally removing lines 72+ which is the correct entry) | .planning/MILESTONES.md lines 72-208 | mitigate | Strict line-range protocol: DELETE lines 3-71 inclusive ONLY; preserve lines 1-2 (H1+blank) + lines 72-208 (correct entry + below); post-delete line count check (208→138) + ^## v1.5 H2 count (1 match) verifies no over-delete |
| T-68-04-PA | Tampering (Phase 70 archive-script re-trigger of same defect) | Plan 70 v1.7 milestone-archival → .planning/MILESTONES.md re-insert garbage | mitigate | Plan 68-04 fixes the symptom only; Plan 68-05 close-gate authors v1.7-DEFERRED-CLEANUP.md line item flagging cdcce23 ROOT CAUSE for v1.8+ investigation + Plan 70 author audit checklist; commit message body documents the forward-pointer |
| T-68-04-AR | Tampering (anti-regression — accidental hub edit) | docs/* (no edits expected — this plan is .planning/* only) | mitigate | `<files>` block explicitly lists only `.planning/MILESTONES.md`; post-edit `git status` MUST show ONLY 1 file modified; commit `git log --name-only -1 HEAD` confirms exactly 1 file |
</threat_model>

<verification>
**Plan 68-04 → V-61-19 + V-61-20 verification targets (per 68-VALIDATION.md lines 54-55):**

| Target | Command | Expected |
|--------|---------|----------|
| Line count | `(Get-Content .planning/MILESTONES.md).Count` | `138` |
| ^## v1.5 H2 count | `(Select-String -Path .planning/MILESTONES.md -Pattern "^## v1.5" \| Measure-Object).Count` | `1` |
| ^## v1.5 H2 content | `(Select-String -Path .planning/MILESTONES.md -Pattern "^## v1.5").Line` | contains `Shipped: 2026-05-07` |
| Methodology highlights present | `Select-String -Path .planning/MILESTONES.md -Pattern "Methodology highlights"` | 1+ match |
| DEFER-07 + DEFER-08 cited | `Select-String -Path .planning/MILESTONES.md -Pattern "DEFER-0[78]"` | 2+ matches |
| Zero garbage debris | `Select-String -Path .planning/MILESTONES.md -Pattern "One-liner:\|NO COMMIT MADE\."` | 0 matches |
| V-61-19 + V-61-20 PASS | `node scripts/validation/check-phase-61.mjs 2>&1 \| Select-String "V-61-19\|V-61-20"` | both report PASS |
| Atomic commit boundary | `git log --name-only -1 HEAD` | exactly 1 file: `.planning/MILESTONES.md` |
</verification>

<success_criteria>
1. `.planning/MILESTONES.md` lines 3-71 inclusive (69 lines) have been DELETED — the cdcce23 garbage v1.5 H2 entry; lines 1-2 (H1 + blank) preserved; lines 72-208 (correct entry + below) preserved byte-unchanged.
2. Post-delete line count: 138 (was 208; delta -70).
3. `Select-String -Path .planning/MILESTONES.md -Pattern "^## v1.5"` returns exactly 1 match at new line 3 with `(Shipped: 2026-05-07)` (NOT `(Shipped: 2026-05-08)`).
4. Methodology highlights + DEFER-07 + DEFER-08 citations all present in the surviving correct v1.5 entry.
5. Zero garbage debris remaining: no `One-liner:`, no `NO COMMIT MADE.`, no `Hash:`, no `Pre-edit:` placeholder labels.
6. `node scripts/validation/check-phase-61.mjs` reports V-61-19 + V-61-20 BOTH PASS (were FAIL pre-edit).
7. Other H2 entries (v1.4, v1.4.1, v1.3, etc.) below the deleted range remain byte-unchanged.
8. ONE atomic git commit at HEAD touches EXACTLY 1 file: `.planning/MILESTONES.md`; commit message body cites Phase 68-04 + corpus-defect rationale + Plan 70 archive-risk forward-pointer.
9. Single `git revert <Plan 68-04 SHA>` cleanly re-inserts the cdcce23 garbage entry at lines 3-71 byte-identically (per-plan boundary preserved per CONTEXT.md D-04 Option E; independently revertible from CHAIN-NN commits).
10. NO files outside `.planning/MILESTONES.md` touched. NO `docs/*` corpus edits. NO validator source edits.
11. Zero content authoring (per CONTEXT.md D-04 — surgical deletion of debris only).
12. Pre-delete boundary verification documented in summary (line 71 confirmed as garbage-body / blank-separator before correct entry's line 73 H2).
13. Commit SHA captured for Plan 68-05 close-gate authoring (substituted as `{68_04_SHA}` in Plan 68-03's canonical CHAIN_SKIP comment block + v1.7-DEFERRED-CLEANUP.md ARCHIVE-01 line item references the same SHA).
</success_criteria>

<output>
Create `.planning/phases/68-chain-skip-root-cause-resolution-pillar-b-validator-surgery/68-04-SUMMARY.md` with:
- Commit SHA + `git log --name-only -1 HEAD` output verbatim (1 file: .planning/MILESTONES.md)
- Pre-delete vs post-delete line count: 208 → 138 (delta -70)
- Pre-delete vs post-delete ^## v1.5 H2 count: 2 → 1 (the garbage at former line 3 removed; correct entry now at line 3)
- Boundary verification output: lines 69-75 pre-delete + lines 1-7 post-delete
- check-phase-61 V-61-19 + V-61-20 pre/post-delete state: FAIL → PASS
- Garbage debris elimination confirmation: 0 matches for "One-liner:" / "NO COMMIT MADE." / "Hash:" / "Pre-edit:"
- Cascade note: overall check-phase-61 exit code 0 ONLY when Plan 68-02 ALSO landed (Plan 68-04 contract is V-61-19/V-61-20 ONLY; other V-61-NN cascade FAILs resolve via Plan 68-02)
- Phase 70 archive-risk forward-pointer documented (Plan 68-05 will author v1.7-DEFERRED-CLEANUP.md ARCHIVE-01 line item with this SHA)
- Wave 2 handoff to Plan 68-03 (CHAIN-03 atomic): Plan 68-04 was file-disjoint from 68-01 + 68-02; Plan 68-03 depends on ALL three Wave 1 plans landing (chain must be cascade-green before CHAIN_SKIP removal)
</output>
</content>
</invoke>