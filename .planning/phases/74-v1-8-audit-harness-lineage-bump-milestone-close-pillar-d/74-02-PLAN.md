---
phase: 74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d
plan: 02
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - scripts/validation/v1.8-milestone-audit.mjs
  - scripts/validation/v1.8-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
autonomous: true
requirements: [HARNESS-07, HARNESS-08]
must_haves:
  truths:
    - "v1.8-milestone-audit.mjs is a Path-A copy of v1.7-milestone-audit.mjs with ONLY version-label substitutions (header + lineage + sidecar path); all C1-C16 logic + self-test 9/9 verbatim; harness exits 0"
    - "v1.8-audit-allowlist.json is a Path-A copy of v1.7 sidecar with 4 NEW ci_2_vpp_location_token entries (post-VPP, resolved flag) appended to the existing 6 (annotate-not-remove); c13_broken_link_allowlist stays 15; generated + phase fields bumped to v1.8"
    - "BASELINE_12 comment block inserted into regenerate-supervision-pins.mjs after the BASELINE_11 block, anchored to the Atom 1 SHA (this commit, a known-PAST SHA — NOT the future close SHA)"
    - "All 3 files land in ONE indivisible commit per ROADMAP SC#1 Atom 1"
  artifacts:
    - path: "scripts/validation/v1.8-milestone-audit.mjs"
      provides: "HARNESS-07 — 6th milestone-audit harness (Path-A from v1.7, C1-C16 verbatim)"
      contains: "v1.8 Milestone Audit Harness"
    - path: "scripts/validation/v1.8-audit-allowlist.json"
      provides: "HARNESS-08 — v1.8 sidecar with 4 post-VPP resolved entries + 6 preserved"
      contains: "ci_2_vpp_location_token"
    - path: "scripts/validation/regenerate-supervision-pins.mjs"
      provides: "HARNESS-08 — BASELINE_12 freshness comment (closes BASELINE_11 v1.7 carry-over)"
      contains: "BASELINE_12"
  key_links:
    - from: "scripts/validation/v1.8-milestone-audit.mjs parseAllowlist()"
      to: "scripts/validation/v1.8-audit-allowlist.json"
      via: "fs.readFileSync sidecar path (v1.7 → v1.8)"
      pattern: "v1.8-audit-allowlist.json"
    - from: "scripts/validation/v1.8-audit-allowlist.json c13_rotting_external.ci_2_vpp_location_token"
      to: "02-macos-pkg-dmg-pipeline.md 4 renamed sites (74-01)"
      via: "resolved annotation referencing the post-VPP corpus state"
      pattern: "resolved_2026_06_08"
---

<objective>
Atom 1 of ROADMAP SC#1: create the v1.8 harness core. Path-A copy `v1.8-milestone-audit.mjs` and `v1.8-audit-allowlist.json` from their v1.7 counterparts (version-label substitutions only — all 16 checks travel forward verbatim), annotate the sidecar with the 4 post-VPP resolved entries, and insert the BASELINE_12 freshness comment into `regenerate-supervision-pins.mjs`. All 3 files land in ONE indivisible commit (precedent: 60-08 / 62-08 / 66-02 / 70-02).

Purpose: Establish the v1.8 milestone-audit harness + sidecar as the 6th in the v1.4→v1.4.1→v1.5→v1.6→v1.7→v1.8 lineage, and close the BASELINE_11 v1.7 carry-over.

Output: 3 new/modified files in ONE commit (Atom 1 SHA). This SHA is the BASELINE_12 anchor (resolved post-commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONTEXT.md
@.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md
@.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-RESEARCH.md

# Path-A sources (READ before authoring):
@scripts/validation/v1.7-milestone-audit.mjs
@scripts/validation/v1.7-audit-allowlist.json
@scripts/validation/regenerate-supervision-pins.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Path-A copy v1.8-milestone-audit.mjs + v1.8-audit-allowlist.json (with 4 VPP entries)</name>
  <read_first>
    - scripts/validation/v1.7-milestone-audit.mjs (Path-A source — read header lines 1-4, parseAllowlist() ~line 79, C13 count guard ~line 670, self-test ~816-940; the rest copies verbatim)
    - scripts/validation/v1.7-audit-allowlist.json (Path-A source — read top fields schema_version/generated/phase + the c13_rotting_external.ci_2_vpp_location_token array of 6 entries for exact shape)
    - .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md (locked resolved-key prefix, phase-field value, allowlist invariants)
  </read_first>
  <action>
    **v1.8-milestone-audit.mjs** — copy `v1.7-milestone-audit.mjs` verbatim, then apply ONLY these version-label substitutions (do NOT change any C1-C16 check logic, self-test, or runner):
    - Header comment line 2: `v1.7 Milestone Audit Harness` → `v1.8 Milestone Audit Harness`
    - Lineage comment line 3: `Lineage: v1.4 -> v1.4.1 -> v1.5 -> v1.6 -> v1.7` → append ` -> v1.8`
    - Sidecar path comment line 4: `v1.7-audit-allowlist.json` → `v1.8-audit-allowlist.json`
    - parseAllowlist() read path (~line 79): `'scripts/validation/v1.7-audit-allowlist.json'` → `'scripts/validation/v1.8-audit-allowlist.json'`
    - C13 count guard (~line 670): `if (allowlist.length !== 15)` stays UNCHANGED at 15 (no new broken links).
    - Self-test (lines ~816-940) + runner (lines ~947-979): verbatim, no changes.

    **v1.8-audit-allowlist.json** — copy `v1.7-audit-allowlist.json` verbatim, then:
    - Top field `generated`: → `"2026-06-08T00:00:00Z"`
    - Top field `phase`: → `"74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d"` (long-slug form matching v1.7's `70-v1-7-...` format)
    - `schema_version`: leave `1.1` (carried forward).
    - `c13_broken_link_allowlist`: UNCHANGED, exactly 15 entries.
    - **Do NOT add a `quarterly_audit` key** — the v1.7 sidecar has none (RESEARCH §HARNESS-08 incorrectly assumed one; 74-CONVENTIONS.md item 8 confirms its absence).
    - Append 4 NEW entries to `c13_rotting_external.ci_2_vpp_location_token` (keep the existing 6 — ANNOTATE not remove), one per VPP-01 site, each matching the existing entry shape (file/line/term/context/reason/category + resolved flag). Use the locked resolved-key prefix from 74-CONVENTIONS.md (underscore form `resolved_2026_06_08: true` to match the existing `resolved_2026_05_26` convention):
      - Entry 7: file `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md`, line 115, term `VPP location token`, reason `"Phase 74 VPP-01 surgical rename — legacy term replaced with current Apple terminology 'content token'"`, category `legacy_term_surgical_rename_candidate`, `resolved_2026_06_08: true`.
      - Entry 8: same file, line 149, same term/reason/category/resolved flag.
      - Entry 9: same file, line 155, same.
      - Entry 10: same file, line 160, same.

    Smoke-test the harness locally before staging (recommended, per Claude's Discretion in CONTEXT.md): `node scripts/validation/v1.8-milestone-audit.mjs` should exit 0 and parse the new sidecar.

    Do NOT commit yet — Task 2 (BASELINE_12) joins the same indivisible Atom 1 commit.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/v1.8-milestone-audit.mjs; echo "exit=$?"` prints `exit=0`.
    - `node scripts/validation/v1.8-milestone-audit.mjs --self-test 2>&1 | grep -c "PASS"` shows the 9 self-test PASS lines preserved (>= 9).
    - `node -e "const j=require('./scripts/validation/v1.8-audit-allowlist.json'); const e=j.c13_rotting_external.ci_2_vpp_location_token; console.log('total='+e.length); console.log('resolved74='+e.filter(x=>Object.keys(x).some(k=>k.startsWith('resolved_2026_06_'))).length); console.log('blink='+j.c13_broken_link_allowlist.length); console.log('phase='+j.phase);"` prints `total=10`, `resolved74=4`, `blink=15`, and the long-slug phase.
    - `grep -c "v1.7-audit-allowlist.json" scripts/validation/v1.8-milestone-audit.mjs` returns 0 (sidecar path fully repointed).
    - `git diff scripts/validation/v1.7-milestone-audit.mjs scripts/validation/v1.7-audit-allowlist.json` returns EMPTY (predecessor v1.7 untouched).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/v1.8-milestone-audit.mjs; echo "exit=$?"</automated>
  </verify>
  <done>v1.8 harness + sidecar are verbatim Path-A copies + 4 VPP resolved entries; harness exits 0; v1.7 predecessor byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Insert BASELINE_12 comment + ATOMIC Atom 1 commit (3 files)</name>
  <read_first>
    - scripts/validation/regenerate-supervision-pins.mjs (read lines 410-435 — the BASELINE_11 comment block + the `const BASELINE_9 = [` boundary that is the insertion point)
    - .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-RESEARCH.md (Pattern 5 BASELINE_12 wording template)
    - .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md (BASELINE_12 anchor = Atom 1 SHA, known-PAST)
  </read_first>
  <action>
    Insert a BASELINE_12 comment block into `scripts/validation/regenerate-supervision-pins.mjs` immediately after the BASELINE_11 block (the comment ending at `... -> v1.7 -> BASELINE_11).`) and before `const BASELINE_9 = [`. Model the wording on the BASELINE_11 comment (lines 416-423). The block MUST:
    1. Reference `HARNESS-09 contract` (NOT HARNESS-02 which is v1.7) and `REQUIREMENTS.md` + `ROADMAP.md` (Phase 74 SC#1).
    2. Name `Phase 74 Plan 74-02` as the authoring event + date `2026-06-08`.
    3. Anchor to `{atom_1_sha}` — write the literal placeholder now, then resolve it post-commit via `git rev-parse --short HEAD` immediately after the Atom 1 commit, amending the comment with the actual SHA. (This is a KNOWN-PAST anchor — the SHA of THIS commit — NOT a forward-reference; mirrors v1.7 BASELINE_11 anchored to `26a1ae9`.)
    4. State `BASELINE_9 entries above remain unchanged -- Phase 74 does NOT alter the line-coord array`.
    5. State the Path-A inheritance chain ending `... -> v1.7 -> BASELINE_11 -> v1.8 -> BASELINE_12`, with forward-pointer `BASELINE_13 will refresh at v1.9 close`.

    Do NOT alter the BASELINE_9 array or any other line-coord data — comment-only insertion.

    **ATOMIC commit** all 3 Atom-1 files together — `v1.8-milestone-audit.mjs` + `v1.8-audit-allowlist.json` + `regenerate-supervision-pins.mjs`:
    `feat(74-02): v1.8 harness-core Path-A — HARNESS-07/08 + BASELINE_12 (atomic SC#1 Atom 1)`.

    After committing, run `git rev-parse --short HEAD`, then `git commit --amend` (or a tiny follow-up is NOT allowed — keep Atom 1 indivisible; use `--amend` while the SHA placeholder is still acceptable, OR leave `{atom_1_sha}` as a recoverable literal and record the real SHA in SUMMARY). Prefer: leave `{atom_1_sha}` literal in the comment and record the resolved Atom 1 SHA in 74-02-SUMMARY.md + carry it into 74-05 (same recoverable-placeholder idiom as the close SHA). This keeps Atom 1 a single clean commit.
  </action>
  <acceptance_criteria>
    - `grep -n "BASELINE_12" scripts/validation/regenerate-supervision-pins.mjs` shows the comment present AFTER the BASELINE_11 block and BEFORE `const BASELINE_9 = [`.
    - `grep -c "HARNESS-09 contract" scripts/validation/regenerate-supervision-pins.mjs` returns 1.
    - The BASELINE_9 array is byte-identical to its pre-edit state: `git diff ae9e3f4 HEAD -- scripts/validation/regenerate-supervision-pins.mjs` shows ONLY added comment lines (no changes inside `const BASELINE_9 = [ ... ]`).
    - `git show --stat HEAD` lists EXACTLY 3 files: v1.8-milestone-audit.mjs, v1.8-audit-allowlist.json, regenerate-supervision-pins.mjs (Atom 1 indivisibility).
    - `node scripts/validation/regenerate-supervision-pins.mjs --self-test; echo "exit=$?"` prints `exit=0` (comment insertion did not break the script).
    - PREDECESSOR-BYTE-UNCHANGED gate: `git diff ae9e3f4 HEAD -- scripts/validation/v1.4-milestone-audit.mjs scripts/validation/v1.4.1-milestone-audit.mjs scripts/validation/v1.5-milestone-audit.mjs scripts/validation/v1.6-milestone-audit.mjs scripts/validation/v1.7-milestone-audit.mjs scripts/validation/v1.4-audit-allowlist.json scripts/validation/v1.4.1-audit-allowlist.json scripts/validation/v1.5-audit-allowlist.json scripts/validation/v1.6-audit-allowlist.json scripts/validation/v1.7-audit-allowlist.json .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml .github/workflows/audit-harness-v1.7-integrity.yml` returns EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "BASELINE_12" scripts/validation/regenerate-supervision-pins.mjs && node scripts/validation/regenerate-supervision-pins.mjs --self-test; echo "exit=$?"</automated>
  </verify>
  <done>BASELINE_12 comment inserted (comment-only); all 3 Atom-1 files in ONE indivisible commit; predecessor surfaces byte-unchanged; Atom 1 SHA recorded for downstream anchoring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local node runtime → sidecar JSON | Harness reads the v1.8 sidecar JSON (locally authored, trusted); no external/untrusted input. |
| local filesystem → git history | All edits committed to trusted local repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-02-01 | Tampering | C1-C16 logic drift during Path-A copy | mitigate | Version-label substitutions ONLY; `git diff` of v1.7 source must be EMPTY; harness exits 0 + self-test 9/9 PASS confirm logic intact. |
| T-74-02-02 | Tampering | Sidecar entry-count corruption (broken-link guard) | mitigate | Acceptance asserts `c13_broken_link_allowlist.length === 15` and `ci_2 total === 10`; C13 guard unchanged at 15. |
| T-74-02-03 | Tampering | BASELINE_9 array accidental edit during comment insertion | mitigate | `git diff` confirms only added comment lines; regenerate `--self-test` exits 0. |
| T-74-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH audit empty). All scripts use Node built-ins only. |

**Honest low-severity assessment:** Tooling-only plan. No network, no auth, no user input, no data handling. Threat surface is limited to copy-fidelity and commit-isolation discipline, both covered by exit-0 + git-diff gates.
</threat_model>

<verification>
- `node scripts/validation/v1.8-milestone-audit.mjs` exits 0; self-test >= 9 PASS.
- Sidecar: 10 ci_2 entries (6 + 4), 4 with resolved_2026_06_* flag, 15 broken-link entries, long-slug phase.
- BASELINE_12 comment present; BASELINE_9 array unchanged; regenerate `--self-test` exits 0.
- Atom 1 = exactly 3 files in ONE commit.
- Predecessor v1.4..v1.7 frozen surfaces byte-unchanged (`ae9e3f4` → HEAD).
</verification>

<success_criteria>
HARNESS-07 + HARNESS-08 delivered: v1.8 harness/sidecar are verbatim Path-A copies with the 4 post-VPP resolved entries, BASELINE_12 closes the BASELINE_11 carry-over, all 3 files in one indivisible Atom-1 commit, harness exits 0, predecessor surfaces byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-02-SUMMARY.md` when done. Record: the Atom 1 SHA (the BASELINE_12 anchor), the harness exit code + self-test PASS count, and the final ci_2 entry count.
</output>
