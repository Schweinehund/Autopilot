---
phase: 74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d
plan: 03
type: execute
wave: 3
depends_on: ["74-02"]
files_modified:
  - scripts/validation/check-phase-74.mjs
  - .github/workflows/audit-harness-v1.8-integrity.yml
autonomous: true
requirements: [HARNESS-09, HARNESS-10]
must_haves:
  truths:
    - "check-phase-74.mjs is the NEW chain-apex (Path-A from check-phase-73.mjs): CHAIN_PHASES=[48..73] (26 phases), CHAIN_SKIP=new Set([]), HARNESS repointed to v1.8-milestone-audit.mjs, V-74-SELF excludes 74"
    - "check-phase-74.mjs adds V-74-VPP-01a (>=4 'content token' AND 0 'VPP location token' in the target doc) + V-74-VPP-01b (exactly 4 ci_2 entries with the resolved flag) — net-new logic modeled on V-67-03/04"
    - "check-phase-71/72/73.mjs are verified chain-current and need NO re-authoring (already shipped; predecessor-byte-unchanged does NOT cover chain validators, but no edit is needed)"
    - "audit-harness-v1.8-integrity.yml is the 5th coexistence file (Path-A from v1.7): v1.8 path-filter, per-phase jobs check-phase-71/72/73/74, linux-chain runs check-phase-74 --verbose, 2 crons + rotting-external-quarterly + pin-helper-advisory preserved verbatim"
    - "check-phase-74.mjs + audit-harness-v1.8-integrity.yml land in ONE indivisible commit (Atom 2) which is committed AND pushed to origin/master before Plan 74-04 runs"
  artifacts:
    - path: "scripts/validation/check-phase-74.mjs"
      provides: "HARNESS-09 — v1.8 chain-apex validator + V-74-VPP assertions"
      contains: "V-74-VPP-01a"
    - path: ".github/workflows/audit-harness-v1.8-integrity.yml"
      provides: "HARNESS-10 — 5th CI coexistence file"
      contains: "check-phase-74"
  key_links:
    - from: "scripts/validation/check-phase-74.mjs HARNESS const"
      to: "scripts/validation/v1.8-milestone-audit.mjs"
      via: "execFileSync wrapper (V-74-AUDIT-HARNESS)"
      pattern: "v1.8-milestone-audit.mjs"
    - from: ".github/workflows/audit-harness-v1.8-integrity.yml linux-chain job"
      to: "scripts/validation/check-phase-74.mjs"
      via: "node check-phase-74.mjs --verbose on ubuntu-latest"
      pattern: "check-phase-74.mjs"
---

<objective>
Atom 2 of ROADMAP SC#1: ship the v1.8 chain-apex validator + CI surface. Path-A copy `check-phase-74.mjs` from `check-phase-73.mjs` (extend CHAIN_PHASES to [48..73], repoint HARNESS to v1.8, add V-74-SELF + the two net-new V-74-VPP assertions) and Path-A copy `audit-harness-v1.8-integrity.yml` from v1.7 (5th coexistence file). Both files land in ONE indivisible commit (Atom 2). Atom 2 MUST be committed AND pushed to origin/master before Plan 74-04 dispatches the GHA workflow (D-03 ordering gate).

Purpose: Provide the v1.8 chain-apex validator that proves VPP-01 completion (V-74-VPP) and the 5th parallel CI workflow that runs check-phase-71..74 on Linux ubuntu-latest for the cross-OS Axis 2 re-audit.

Output: 2 new files in ONE commit (Atom 2 SHA), pushed to origin/master.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONTEXT.md
@.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md
@.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-RESEARCH.md

# Path-A sources (READ before authoring):
@scripts/validation/check-phase-73.mjs
@scripts/validation/check-phase-67.mjs
@.github/workflows/audit-harness-v1.7-integrity.yml

<interfaces>
<!-- Extracted contracts the executor needs. Use directly — no codebase exploration. -->
From scripts/validation/check-phase-73.mjs (Path-A source):
  const CHAIN_PHASES = [48..72];                 // → [48..73] for 74 (add 73)
  const CHAIN_SKIP = new Set([]);                // INVARIANT: empty per Phase 68 7b635ca
  const HARNESS = 'scripts/validation/v1.7-milestone-audit.mjs';  // → v1.8 for 74
  // NESTED-aware CHAIN loop ~lines 341-376 (copy verbatim; only console.log header changes)
  // V-73-AUDIT-HARNESS ~378-398 (execFileSync stdout+stderr capture, WRAPPER-01 hardened)
  // V-73-SELF ~400-414 (asserts phase not in CHAIN_PHASES + CHAIN_SKIP empty)
  // _lib/frozen-at-close.mjs consumption (Phase 73 a85da77) — reads PRIOR-milestone close SHAs only

From scripts/validation/check-phase-67.mjs (V-74-VPP model — lines 110-189):
  V-67-03: content-token count >= N at frozen SHA   → V-74-VPP-01a (count at HEAD)
  V-67-04: sidecar resolved-entry count             → V-74-VPP-01b (4 resolved entries)

From scripts/validation/v1.8-audit-allowlist.json (created in 74-02):
  c13_rotting_external.ci_2_vpp_location_token = [ ...6 existing, 4 new with resolved_2026_06_08:true ]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify check-phase-71/72/73 chain-current; author check-phase-74.mjs (Path-A + V-74-VPP)</name>
  <read_first>
    - scripts/validation/check-phase-73.mjs (Path-A source — read CHAIN_PHASES/CHAIN_SKIP/HARNESS consts, NESTED loop ~341-376, V-73-AUDIT-HARNESS ~378-398, V-73-SELF ~400-414, runner ~416-443)
    - scripts/validation/check-phase-67.mjs lines 110-189 (V-67-03/04 — V-74-VPP model)
    - scripts/validation/check-phase-74.mjs (the file being created)
    - .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md (locked CHAIN consts + resolved-key prefix)
  </read_first>
  <action>
    First, VERIFY check-phase-71/72/73.mjs are present and chain-current (they already exist — shipped in their own phases). Run each (`node scripts/validation/check-phase-71.mjs`, `-72`, `-73`) and confirm exit 0. Do NOT re-author them (HARNESS-09's 71/72/73 obligation is satisfied by their existing chain-current presence; predecessor-byte-unchanged does not cover chain validators, but no edit is needed).

    Copy `check-phase-73.mjs` verbatim to `check-phase-74.mjs`, then apply:
    - `CHAIN_PHASES` = `[48,49,...,72,73]` (add 73; 26 phases total).
    - `CHAIN_SKIP` = `new Set([])` (INVARIANT — confirm empty).
    - `HARNESS` const → `'scripts/validation/v1.8-milestone-audit.mjs'`.
    - Rename all `V-73-*` ids/names → `V-74-*` (V-74-AUDIT-HARNESS, V-74-SELF, V-74-CHAIN, etc.).
    - V-74-SELF: assert `74` NOT in CHAIN_PHASES (D-22 auditor-independence) + CHAIN_SKIP empty.
    - Runner header console.log → `check-phase-74 -- ...`.
    - Keep the NESTED-aware CHAIN loop (~341-376) and the WRAPPER-01-hardened execFileSync stdout+stderr capture verbatim.
    - Keep `_lib/frozen-at-close.mjs` consumption verbatim — it reads only PRIOR-milestone close SHAs (V141/V15/V16/V17/V17_CLOSEGATE; NO V18 — confirms NO Commit A per D-04).

    ADD the two net-new V-74-VPP assertions (modeled on V-67-03/04; all file reads MUST apply `.replace(/\r\n/g, '\n')` for CRLF safety per Phase 31 ca40eb9):
    - **V-74-VPP-01a** — read `docs/operations/app-lifecycle/02-macos-pkg-dmg-pipeline.md` at HEAD; assert `("content token" count) >= 4` AND `("VPP location token" count) === 0`; FAIL with the offending counts otherwise.
    - **V-74-VPP-01b** — read `scripts/validation/v1.8-audit-allowlist.json`; assert `c13_rotting_external.ci_2_vpp_location_token` has exactly 4 entries whose keys include one starting with the locked PHASE_74_RESOLVED_PREFIX (`resolved_2026_06_`, prefix-match per RESEARCH Pitfall 5 — NOT an exact-key match). Define `const PHASE_74_RESOLVED_PREFIX = 'resolved_2026_06_';` near the top and reference it in both the assertion and its docstring.

    Register both new assertions in the `checks` array. Smoke-test: `node scripts/validation/check-phase-74.mjs` should exit 0 with V-74-VPP-01a/01b PASS.

    Do NOT commit yet — Task 2 (workflow yml) joins the same Atom 2 commit.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-74.mjs; echo "exit=$?"` prints `exit=0`.
    - `node scripts/validation/check-phase-74.mjs --verbose 2>&1 | grep -E "V-74-VPP-01a|V-74-VPP-01b" | grep -c PASS` returns 2.
    - `grep -c "v1.7-milestone-audit.mjs" scripts/validation/check-phase-74.mjs` returns 0 (HARNESS fully repointed).
    - `node -e "const fs=require('fs');const s=fs.readFileSync('scripts/validation/check-phase-74.mjs','utf8');if(/CHAIN_PHASES\s*=\s*\[[^\]]*\b74\b/.test(s)){console.error('FAIL: 74 in CHAIN_PHASES');process.exit(1)}console.log('OK: 74 excluded from CHAIN_PHASES')"` prints OK.
    - `grep -c "new Set(\[\])" scripts/validation/check-phase-74.mjs` returns >= 1 (CHAIN_SKIP empty).
    - `node scripts/validation/check-phase-71.mjs; echo 71=$?; node scripts/validation/check-phase-72.mjs; echo 72=$?; node scripts/validation/check-phase-73.mjs; echo 73=$?` all print `=0` (chain-current; no re-authoring).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-74.mjs; echo "exit=$?"</automated>
  </verify>
  <done>check-phase-74.mjs is a Path-A chain-apex with [48..73], empty CHAIN_SKIP, v1.8 HARNESS, V-74-SELF excluding 74, and V-74-VPP-01a/01b PASS; 71/72/73 verified chain-current.</done>
</task>

<task type="auto">
  <name>Task 2: Path-A audit-harness-v1.8-integrity.yml + ATOMIC Atom 2 commit + push to origin/master</name>
  <read_first>
    - .github/workflows/audit-harness-v1.7-integrity.yml (Path-A source — read path-filter ~16-26, per-phase jobs ~94-148, linux-chain-ubuntu-latest job, rotting-external-quarterly cron-only `if:`, pin-helper-advisory continue-on-error)
    - .github/workflows/audit-harness-v1.8-integrity.yml (the file being created)
    - .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md (5th coexistence count; predecessor byte-unchanged set)
  </read_first>
  <action>
    Copy `audit-harness-v1.7-integrity.yml` verbatim to `audit-harness-v1.8-integrity.yml`, then change ONLY:
    - Path filters: `scripts/validation/v1.7-*` → `scripts/validation/v1.8-*`; `audit-harness-v1.7-integrity.yml` → `audit-harness-v1.8-integrity.yml`; `v1.7-MILESTONE-AUDIT.md` → `v1.8-MILESTONE-AUDIT.md`; `v1.7-DEFERRED-CLEANUP.md` → `v1.8-DEFERRED-CLEANUP.md`. Keep `scripts/validation/check-phase-*.mjs` and `.planning/REQUIREMENTS.md` unchanged.
    - Per-phase jobs: replace check-phase-67/68/69/70 jobs → check-phase-71/72/73/74 (job names + the `node scripts/validation/check-phase-7N.mjs` invocations). All other job properties (`fetch-depth: 0`, `continue-on-error: false`, `timeout-minutes`, `core.autocrlf false`) UNCHANGED.
    - `linux-chain-ubuntu-latest` job: chain-apex `check-phase-66.mjs --verbose` (v1.7) → `check-phase-74.mjs --verbose` (v1.8 apex). Preserve `fetch-depth: 0` (FETCH-DEPTH-01 inheritance).
    - PRESERVE VERBATIM: `rotting-external-quarterly` (`if: github.event_name == 'schedule' && github.event.schedule == '0 8 1 1,4,7,10 *'` — correctly SKIPS on workflow_dispatch, a negative control); `pin-helper-advisory` (only job keeping `continue-on-error: true`); both crons (`0 8 * * 1` weekly + `0 8 1 1,4,7,10 *` quarterly). PR-blocking per D-A9 (all jobs except pin-helper-advisory are blocking).

    Validate YAML syntax locally if a linter is available (`node -e` JSON-of-YAML or `gh workflow view` post-push).

    **ATOMIC Atom 2 commit** — `check-phase-74.mjs` + `audit-harness-v1.8-integrity.yml` together:
    `feat(74-03): v1.8 validators + CI surface — HARNESS-09/10 (atomic SC#1 Atom 2)`.

    **THEN PUSH to origin/master** (mandatory ordering gate — D-03): `git push origin master`. Verify the Atom 2 SHA is on origin/master before this plan is considered done (Plan 74-04 Axis 2 dispatch fails if check-phase-74.mjs is absent from the dispatched ref).
  </action>
  <acceptance_criteria>
    - `test -f .github/workflows/audit-harness-v1.8-integrity.yml` succeeds.
    - `grep -c "check-phase-74" .github/workflows/audit-harness-v1.8-integrity.yml` returns >= 2 (a per-phase job + the linux-chain apex).
    - `grep -c "v1.7-" .github/workflows/audit-harness-v1.8-integrity.yml` returns 0 (all v1.7 labels repointed).
    - `grep -c "0 8 1 1,4,7,10 \*" .github/workflows/audit-harness-v1.8-integrity.yml` returns >= 1 (quarterly cron + rotting-external `if:` preserved).
    - `git show --stat HEAD` lists EXACTLY 2 files: check-phase-74.mjs + audit-harness-v1.8-integrity.yml (Atom 2 indivisibility).
    - `git log origin/master --oneline -1` shows the Atom 2 SHA (pushed).
    - `gh workflow list 2>&1 | grep -i "audit-harness-v1.8"` shows the new workflow registered (after push; GHA auto-registers).
    - PREDECESSOR-BYTE-UNCHANGED gate: `git diff ae9e3f4 HEAD -- .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml .github/workflows/audit-harness-v1.6-integrity.yml .github/workflows/audit-harness-v1.7-integrity.yml scripts/validation/v1.4-milestone-audit.mjs scripts/validation/v1.5-milestone-audit.mjs scripts/validation/v1.6-milestone-audit.mjs scripts/validation/v1.7-milestone-audit.mjs` returns EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/workflows/audit-harness-v1.8-integrity.yml && grep -c "check-phase-74" .github/workflows/audit-harness-v1.8-integrity.yml</automated>
  </verify>
  <done>v1.8 workflow is the 5th coexistence file (v1.8 path-filter, check-phase-71..74 jobs, linux-chain apex = check-phase-74, crons + advisory + quarterly preserved); Atom 2 in ONE commit pushed to origin/master; predecessor surfaces byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git → origin/master (GitHub) | Atom 2 push to authenticated origin/master; GHA auto-registers the new workflow. Push targets a trusted ref via authenticated gh/git. |
| GHA runner → repo checkout | The v1.8 workflow runs check-phase-*.mjs on ubuntu-latest against the dispatched ref (master); inputs are the repo's own tracked files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-03-01 | Tampering | CHAIN_PHASES self-inclusion (74 in its own chain → infinite loop) | mitigate | V-74-SELF assertion + acceptance regex confirming 74 NOT in CHAIN_PHASES; CHAIN_SKIP empty. |
| T-74-03-02 | Tampering | V-74-VPP resolved-key date mismatch (assertion vs sidecar) | mitigate | Prefix-match `resolved_2026_06_` (RESEARCH Pitfall 5) + single PHASE_74_RESOLVED_PREFIX const referenced by both sites. |
| T-74-03-03 | Tampering | Predecessor workflow YAML drift (editor auto-format) | mitigate | Predecessor-byte-unchanged `git diff ae9e3f4 HEAD` gate over the 4 existing workflows; commit isolation. |
| T-74-03-04 | Spoofing/Tampering | Pushing to a wrong/untrusted ref | mitigate | Explicit `git push origin master`; `git log origin/master -1` confirms the intended Atom 2 SHA landed on the trusted ref before 74-04. |
| T-74-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH audit empty); workflow uses Node built-ins + actions/checkout only (Path-A from a trusted predecessor workflow). |

**Honest low-severity assessment:** Tooling/CI plan. The one real consideration is that the push targets origin/master (a trusted, authenticated ref) and the new workflow only executes the repo's own validator scripts — no untrusted input, no secrets handling. Edit-scope + ref-correctness are covered by the byte-unchanged and `git log origin/master` gates.
</threat_model>

<verification>
- check-phase-74.mjs exits 0; V-74-VPP-01a + V-74-VPP-01b PASS; 74 excluded from CHAIN_PHASES; CHAIN_SKIP empty.
- check-phase-71/72/73.mjs exit 0 (chain-current, unmodified).
- audit-harness-v1.8-integrity.yml is the 5th coexistence file with check-phase-71..74 + linux-chain apex check-phase-74; quarterly/advisory preserved.
- Atom 2 = exactly 2 files in ONE commit, pushed to origin/master (`git log origin/master -1` shows the SHA).
- Predecessor v1.4..v1.7 workflows + harnesses byte-unchanged (`ae9e3f4` → HEAD).
</verification>

<success_criteria>
HARNESS-09 + HARNESS-10 delivered: check-phase-74.mjs (Path-A chain-apex + V-74-VPP) and audit-harness-v1.8-integrity.yml (5th coexistence file) in one indivisible Atom-2 commit, committed AND pushed to origin/master, with predecessor surfaces byte-unchanged — satisfying the D-03 ordering gate for Plan 74-04.
</success_criteria>

<output>
Create `.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-03-SUMMARY.md` when done. Record: the Atom 2 SHA, confirmation it is on origin/master, the V-74-VPP-01a/01b PASS status, and the check-phase-74.mjs PASS/FAIL/SKIP counts (for 74-04 cross-OS EXACT MATCH comparison).
</output>
