---
phase: 74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d
plan: 04
type: execute
wave: 4
depends_on: ["74-03"]
files_modified:
  - .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-04-AUDIT-RESULTS.md
autonomous: false
requirements: [HARNESS-11]
user_setup:
  - service: github-actions
    why: "Axis 2 cross-OS Linux GHA workflow_dispatch of audit-harness-v1.8-integrity.yml requires gh CLI authenticated to run workflows on master. The Atom 2 commit (Plan 74-03) MUST already be on origin/master so check-phase-71..74 jobs actively run (not fail-on-missing)."
    env_vars: []
    dashboard_config:
      - task: "Verify gh CLI is authenticated (gh auth status — confirmed account Schweinehund, active at plan time)"
        location: "Local CLI: gh auth login if not already authenticated"
      - task: "Verify Atom 2 commit (Plan 74-03) is on origin/master before Axis 2 dispatch"
        location: "Local CLI: git log origin/master --oneline -1 (must show the 74-03 Atom 2 SHA)"
      - task: "Verify v1.8 workflow is registered + active"
        location: "Local CLI: gh workflow list | grep audit-harness-v1.8"
must_haves:
  truths:
    - "74-04-AUDIT-RESULTS.md exists with Axis 1 (local fresh-clone via fresh gsd-executor sub-agent) + Axis 2 (cross-OS Linux GHA workflow_dispatch) evidence sections + Cross-OS PASS-Count EXACT MATCH table"
    - "Axis 1 = ONE fresh gsd-executor sub-agent (Task tool) running git clone --no-hardlinks into $env:TEMP\\v1.8-audit-<rand> (NOT a worktree — permitted under use_worktrees:false); asserts cloned HEAD == source HEAD; runs the 6 cross-OS validators; captures exit codes + PASS/FAIL/SKIP counts; removes the clone (zero orphans)"
    - "Axis 3 = the SAME sub-agent serving zero-context-carryover (Axis 1 + Axis 3 are ONE dispatch covering physical-clone + logical-context independence — do NOT spawn two agents)"
    - "Axis 2 = gh workflow run audit-harness-v1.8-integrity.yml --ref master post-Atom-2-on-master; run URL + per-job conclusions + Linux chain timing captured; rotting-external-quarterly correctly SKIPS (negative control, not a failure)"
    - "Cross-OS PASS-Count EXACT MATCH across the 6 cross-OS-applicable validators: v1.8-milestone-audit.mjs + check-phase-70/71/72/73/74.mjs"
    - "ORDERING GATE enforced: Atom 2 on origin/master + gh auth OK + v1.8 workflow active BEFORE any Axis 2 dispatch"
  artifacts:
    - path: ".planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-04-AUDIT-RESULTS.md"
      provides: "HARNESS-11 — 3-axis terminal re-audit evidence (Axis 1 local + Axis 2 GHA + cross-OS EXACT MATCH table + Wave 5 handoff)"
      contains: "EXACT MATCH"
  key_links:
    - from: "74-04-AUDIT-RESULTS.md Axis 1"
      to: "fresh gsd-executor sub-agent + git clone --no-hardlinks in $env:TEMP"
      via: "Task tool spawn (D-22 INTENT) + D-03 LOCKED physical isolation"
      pattern: "no-hardlinks"
    - from: "74-04-AUDIT-RESULTS.md Axis 2"
      to: "GHA workflow_dispatch run on master HEAD post-Atom-2"
      via: "gh workflow run audit-harness-v1.8-integrity.yml --ref master"
      pattern: "actions/runs/"
---

<objective>
Execute the HARNESS-11 3-axis terminal re-audit at v1.8 close (D-03 LOCKED — exact Phase 70 HARNESS-05 replication, v1.8-relabeled). Verify the entire v1.8 harness lineage operates correctly in a context-isolated (fresh sub-agent), filesystem-isolated (fresh clone, separate `.git/`), AND OS-isolated (Linux GHA) execution environment.

Purpose: Prove cross-OS parity at the literal close-gate moment by stacking three independence axes — (1) fresh `git clone --no-hardlinks` (D-03), (2) fresh gsd-executor sub-agent with zero context-carryover (D-22), (3) cross-OS Linux GHA (CILINUX-01). Cross-OS PASS-Count must EXACT MATCH across all 6 cross-OS-applicable validators.

Output: ONE artifact-only commit landing `74-04-AUDIT-RESULTS.md`. This artifact is consumed by the HARNESS-12 close-gate (74-05).

**autonomous:false** — this plan spawns a sub-agent + dispatches a GHA workflow + requires gh auth + enforces the D-03 ordering gate (Atom 2 must be on origin/master first). Pause for the auth/ordering checkpoint before Axis 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONTEXT.md
@.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-CONVENTIONS.md
@.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-RESEARCH.md

# 3-axis re-audit recipe + per-axis dossier (READ before executing):
@.claude/tmp/phase74-D03-advisor.md
@.planning/milestones/v1.7-phases/70-v1-7-audit-harness-lineage-bump-milestone-close-pillar-d-clo/70-04-AUDIT-RESULTS.md

<interfaces>
Axis 1 PowerShell fresh-clone recipe — 74-RESEARCH.md "3-Axis Re-Audit — Axis 1 PowerShell Recipe" (lines ~687-711). Key: $rand over [0-9a-z] 8-char; git clone --no-hardlinks D:\claude\Autopilot $env:TEMP\v1.8-audit-$rand; assert cloned HEAD == source HEAD; run 6 validators; Remove-Item -Recurse -Force; assert zero orphans.
Axis 2 gh dispatch — 74-RESEARCH.md "Axis 3 GHA Dispatch Command" (lines ~713-722): gh workflow run audit-harness-v1.8-integrity.yml --ref master; gh run list --workflow=...; gh run view <RUN_ID> --log.
6 cross-OS-applicable validators (74-CONVENTIONS.md): v1.8-milestone-audit.mjs + check-phase-70/71/72/73/74.mjs. Excluded: pin-helper-advisory (CI-only), rotting-external-quarterly (cron-only negative control), harness --self-test (local supporting evidence).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: ORDERING GATE — Atom 2 on origin/master + gh auth + v1.8 workflow active</name>
  <what-built>
    The D-03 ordering gate. Before any Axis 2 GHA dispatch, the Atom 2 commit (Plan 74-03: check-phase-74.mjs + audit-harness-v1.8-integrity.yml) MUST be on origin/master, gh CLI must be authenticated, and the v1.8 workflow must be registered + active. The GHA jobs are active `node` invocations that FAIL (not skip) if check-phase-74.mjs is absent from the dispatched ref.
  </what-built>
  <how-to-verify>
    Run and confirm all three pass (PowerShell):
    1. `git log origin/master --oneline -1` — shows the 74-03 Atom 2 SHA (from 74-03-SUMMARY.md). If not present, push it first (`git push origin master`).
    2. `gh auth status` — shows an authenticated, active account. If a stale env token interferes: `$env:GH_TOKEN=$null; $env:GITHUB_TOKEN=$null` then re-run.
    3. `gh workflow list | Select-String "audit-harness-v1.8"` — shows the workflow with `state: active`.
  </how-to-verify>
  <resume-signal>Type "gate-clear" once all three checks pass, or describe the blocker (e.g., Atom 2 not pushed, gh auth expired).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Axis 1 + Axis 3 — fresh gsd-executor sub-agent, fresh-clone re-audit of 6 validators</name>
  <read_first>
    - .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-RESEARCH.md (Axis 1 PowerShell recipe lines ~687-711)
    - .claude/tmp/phase74-D03-advisor.md (per-axis recipe + 6-validator set + exclusion reasons)
    - .planning/milestones/v1.7-phases/70-v1-7-audit-harness-lineage-bump-milestone-close-pillar-d-clo/70-04-AUDIT-RESULTS.md (Phase 70 Axis 1 evidence format to mirror)
  </read_first>
  <action>
    Spawn ONE fresh `gsd-executor` sub-agent via the Task/Agent tool (D-22 INTENT: zero context carryover from the content-author agents that wrote 74-01/02/03). The sub-agent serves BOTH Axis 1 (physical isolation) AND Axis 3 (logical/context isolation) — these are ONE dispatch covering two independence dimensions. Do NOT spawn two separate agents (D-03 ruling — Option D literalism is wrong).

    The sub-agent runs this PowerShell recipe (adapt from 74-RESEARCH Axis 1):
    1. `$rand = -join ((48..57)+(97..122) | Get-Random -Count 8 | ForEach-Object {[char]$_})` (8-char [0-9a-z]).
    2. `$auditPath = Join-Path $env:TEMP "v1.8-audit-$rand"`.
    3. `$mainHeadSha = git -C 'D:\claude\Autopilot' rev-parse HEAD`.
    4. `git clone --no-hardlinks 'D:\claude\Autopilot' $auditPath` (NOT a worktree — separate `.git/`, permitted under use_worktrees:false).
    5. `$cloneHeadSha = git -C $auditPath rev-parse HEAD`; assert `$mainHeadSha -eq $cloneHeadSha` (else throw).
    6. `Push-Location $auditPath`; run the 6 cross-OS validators, capturing exit code + PASS/FAIL/SKIP counts for each:
       - `node scripts/validation/v1.8-milestone-audit.mjs`
       - `node scripts/validation/check-phase-70.mjs`
       - `node scripts/validation/check-phase-71.mjs`
       - `node scripts/validation/check-phase-72.mjs`
       - `node scripts/validation/check-phase-73.mjs`
       - `node scripts/validation/check-phase-74.mjs` (chain-apex)
    7. `Pop-Location`; `Remove-Item -Recurse -Force $auditPath`.
    8. Assert zero orphans: `@(Get-ChildItem $env:TEMP -Filter 'v1.8-audit-*' -Directory).Count -eq 0`.

    The sub-agent returns: cloned HEAD SHA, per-validator exit codes + PASS/FAIL/SKIP counts, and the orphan-cleanup confirmation. Record these verbatim — they form the Axis 1 column of the cross-OS EXACT MATCH table.

    Do NOT commit yet — Axis 2 (Task 3) evidence joins the same artifact.
  </action>
  <acceptance_criteria>
    - Sub-agent transcript shows `$mainHeadSha -eq $cloneHeadSha` (HEAD match) — the clone is at the same commit as source HEAD.
    - All 6 validators exit 0 in the fresh clone; PASS/FAIL/SKIP counts captured for each (FAIL count = 0 for all 6).
    - Orphan check returns 0 (`v1.8-audit-*` temp dirs cleaned up).
    - The sub-agent is a SINGLE Task spawn (one agent serving Axis 1 + Axis 3), not two.
  </acceptance_criteria>
  <verify>
    <human-check>Sub-agent transcript confirms HEAD match, all 6 validators exit 0 with FAIL=0, and zero orphan temp dirs.</human-check>
  </verify>
  <done>Fresh sub-agent ran the 6 cross-OS validators in a fresh --no-hardlinks clone at source HEAD; all exit 0; clone removed; Axis 1 PASS counts captured.</done>
</task>

<task type="auto">
  <name>Task 3: Axis 2 — cross-OS Linux GHA dispatch + EXACT MATCH table + artifact commit</name>
  <read_first>
    - .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-RESEARCH.md (Axis 3 GHA dispatch lines ~713-722; Pitfall 3 + 4 gh auth/ordering)
    - .planning/milestones/v1.7-phases/70-v1-7-audit-harness-lineage-bump-milestone-close-pillar-d-clo/70-04-AUDIT-RESULTS.md (artifact structure + EXACT MATCH table format)
    - .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-03-SUMMARY.md (Atom 2 SHA + Axis 1-comparable local PASS counts)
  </read_first>
  <action>
    Dispatch the cross-OS Linux re-audit (only after Task 1 gate cleared):
    1. `gh workflow run audit-harness-v1.8-integrity.yml --ref master`.
    2. `gh run list --workflow=audit-harness-v1.8-integrity.yml --limit 1` — capture the run ID + URL.
    3. Wait for completion; `gh run view <RUN_ID>` — capture per-job conclusions + Linux chain wall-clock timing.
    4. Confirm `rotting-external-quarterly` SKIPPED (cron-only `if:` — negative control, NOT a failure). Confirm check-phase-71/72/73/74 jobs + linux-chain (check-phase-74 --verbose) all succeeded.

    Author `.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-04-AUDIT-RESULTS.md` (mirror Phase 70's `70-04-AUDIT-RESULTS.md` structure). Include:
    - YAML frontmatter: audit_method, clone_path pattern, gha_workflow_run_url, source HEAD SHA.
    - Axis 1 section: fresh-clone evidence (HEAD match, per-validator exit codes + PASS/FAIL/SKIP).
    - Axis 2 section: GHA run URL + per-job conclusions + Linux timing + rotting-external-quarterly-SKIP negative-control note.
    - Axis 3 note: same sub-agent served zero-context-carryover (logical independence) alongside Axis 1.
    - **Cross-OS PASS-Count EXACT MATCH table:** rows = the 6 cross-OS-applicable validators; columns = Axis 1 (Windows local) PASS/FAIL/SKIP vs Axis 2 (Linux GHA) PASS/FAIL/SKIP. EXACT MATCH required for all 6. (Excluded from the cross-OS table with reasons: pin-helper-advisory CI-only, rotting-external-quarterly cron-only negative control, harness --self-test local-only.)
    - Wave 5 Handoff section: the Atom 1 SHA, Atom 2 SHA, this audit-results SHA, and the cross-OS PASS counts — for HARNESS-12 (74-05) to import into v1.8-MILESTONE-AUDIT.md.

    Commit artifact-only: `docs(74-04): HARNESS-11 3-axis terminal re-audit results (artifact-only)`.
  </action>
  <acceptance_criteria>
    - `gh run list --workflow=audit-harness-v1.8-integrity.yml --limit 1` shows a completed run with conclusion `success` (modulo the expected rotting-external-quarterly skip).
    - 74-04-AUDIT-RESULTS.md contains a "Cross-OS PASS-Count EXACT MATCH" table with all 6 validators showing identical Axis 1 vs Axis 2 PASS/FAIL/SKIP counts; FAIL=0 across the board.
    - 74-04-AUDIT-RESULTS.md contains the GHA run URL (`grep -c "actions/runs/" 74-04-AUDIT-RESULTS.md` >= 1) and documents the rotting-external-quarterly SKIP as a negative control.
    - `git show --stat HEAD` lists ONLY 74-04-AUDIT-RESULTS.md (artifact-only commit).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "EXACT MATCH" .planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-04-AUDIT-RESULTS.md</automated>
    <human-check>GHA run concluded success (rotting-external-quarterly skip expected); cross-OS PASS counts match Axis 1 vs Axis 2 for all 6 validators.</human-check>
  </verify>
  <done>Axis 2 Linux GHA run succeeded; cross-OS EXACT MATCH verified across 6 validators; 74-04-AUDIT-RESULTS.md committed artifact-only with the Wave-5 handoff data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → GitHub Actions (gh dispatch) | `gh workflow run --ref master` dispatches against origin/master via authenticated gh CLI. The ref is a trusted, authenticated repo ref. |
| GHA ubuntu-latest runner → repo checkout | The dispatched workflow runs the repo's own check-phase-*.mjs against master HEAD; inputs are tracked repo files only. |
| source repo → fresh clone (TEMP) | `git clone --no-hardlinks` from the local trusted repo into $env:TEMP; separate `.git/`, removed post-audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-04-01 | Spoofing/Tampering | gh dispatch against an untrusted/wrong ref | mitigate | Explicit `--ref master`; ordering gate (Task 1) confirms Atom 2 is on origin/master and gh auth is the expected active account (Schweinehund) before dispatch. |
| T-74-04-02 | Tampering | Stale GH_TOKEN/GITHUB_TOKEN env override using wrong identity | mitigate | `gh auth status` in the gate; RESEARCH Pitfall 4 remediation (`$env:GH_TOKEN=$null`) documented if a stale token interferes. |
| T-74-04-03 | Information Disclosure | Orphan temp clones leaking repo contents in $env:TEMP | mitigate | `Remove-Item -Recurse -Force` + zero-orphan assertion in the Axis 1 recipe. |
| T-74-04-04 | Repudiation | Re-audit evidence not anchored to a verifiable run | mitigate | Capture the GHA run URL + run ID + cloned HEAD SHA in the artifact; artifact-only commit provides a bisect-clean audit boundary. |
| T-74-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs; the dispatched workflow uses actions/checkout + Node built-ins from a Path-A-trusted predecessor workflow. |

**Honest low-severity assessment:** The one real consideration flagged in the security model — the Axis 2 `gh workflow run` requires an authenticated gh CLI and dispatches against origin/master. Mitigated by the explicit `--ref master`, the ordering gate's `gh auth status` check, and the stale-token remediation. No secrets are handled by the validators; the only externally-influenced inputs are git history + the GHA dispatch, both trusted/authenticated.
</threat_model>

<verification>
- Ordering gate cleared: Atom 2 on origin/master + gh auth active + v1.8 workflow active.
- Axis 1: fresh sub-agent, fresh --no-hardlinks clone at source HEAD, 6 validators exit 0, zero orphans.
- Axis 2: Linux GHA run success (rotting-external-quarterly skip = negative control).
- Cross-OS PASS-Count EXACT MATCH across the 6 cross-OS-applicable validators (FAIL=0).
- 74-04-AUDIT-RESULTS.md committed artifact-only with the Wave-5 handoff data.
</verification>

<success_criteria>
HARNESS-11 delivered: 3-axis terminal re-audit (fresh-clone D-03 + fresh sub-agent D-22 + cross-OS Linux GHA CILINUX-01) confirms cross-OS PASS-Count EXACT MATCH across all 6 cross-OS-applicable validators, captured in an artifact-only 74-04-AUDIT-RESULTS.md commit for the HARNESS-12 close-gate.
</success_criteria>

<output>
Create `.planning/phases/74-v1-8-audit-harness-lineage-bump-milestone-close-pillar-d/74-04-SUMMARY.md` when done. Record: the audit-results commit SHA, the GHA run URL, the cross-OS PASS counts (Axis 1 vs Axis 2), and confirmation of EXACT MATCH across all 6 validators.
</output>
