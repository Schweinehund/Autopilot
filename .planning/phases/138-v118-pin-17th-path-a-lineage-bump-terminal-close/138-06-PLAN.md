---
phase: 138-v118-pin-17th-path-a-lineage-bump-terminal-close
plan: 06
type: execute
wave: 6
depends_on: [138-05]
files_modified:
  - .planning/milestones/v1.19-DEFERRED-CLEANUP.md
  - .planning/milestones/v1.19-MILESTONE-AUDIT.md
  - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-VERIFICATION.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HARN-16]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "v1.19-DEFERRED-CLEANUP.md exists at the milestones path, is strictly log-only, and carries all six mandatory additions beyond the obvious carried list"
    - "The four entries handed over by phase 136's first summary are transcribed VERBATIM per its binding mandate, including the coordinate correction riding as an append to the existing fence-mask entry rather than as a new entry"
    - "A DROPPED-and-recorded-Closed section exists, and the predecessor's back-anchor pin deferral is dropped from the carried list with a Closed record in the milestone audit"
    - "The double-booking guard runs at ROOT-CAUSE level, not identifier level; the self-test defect has exactly one owner and the scoped-red entry cross-references rather than restates it"
    - "v1.19-MILESTONE-AUDIT.md records the 3-axis re-audit with the single shared SHA, the PASS/FAIL/SKIP exact-match table, the corrected cascade baseline, the byte-unchanged HARD gate at its independently derived surface count, and 17-of-17 requirements traceability"
    - "[EDGE HARN-16/adjacency] The requirement flip lands as exactly ONE commit; no second close-gate commit exists, and that single commit stages all four planning documents plus both milestone documents plus the phase verification document"
    - "[EDGE HARN-16/ordering] The close-gate is authored only AFTER the cross-OS axis is captured, and the confirmatory apex run happens only AFTER the close-gate commit lands"
    - "HARN-15 gate part 4: the post-close-gate confirmatory apex run asserts the apex audit check is PASS, not SKIP — closing the resolver-null hole exactly once, where it matters"
    - "All 17 v1.19 requirements read Validated across the four planning documents with zero remaining pending entries"
  prohibitions:
    - "No open deferred item may be dropped without an explicit Closed record; masking by deletion is barred and the carried list is not silently trimmed"
    - "The predecessor's zero-failure full-chain claim must not be repeated; the correction of record is logged instead"
    - "No historical planning artefact may be retro-edited to carry a correction; corrections ride as appends to the entry that owns them"
    - "The requirement flip must not be split across commits, and no requirement may be flipped that this milestone did not deliver"
  artifacts:
    - path: ".planning/milestones/v1.19-DEFERRED-CLEANUP.md"
      provides: "Terminal deferred-cleanup log with the six mandatory additions, the dropped-and-closed section and the provenance footer"
      contains: "V119-PIN-DEFERRAL"
    - path: ".planning/milestones/v1.19-MILESTONE-AUDIT.md"
      provides: "Terminal milestone audit — 3-axis narrative, exact-match table, byte-unchanged gate, 17-of-17 traceability"
    - path: ".planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-VERIFICATION.md"
      provides: "The phase verification document — also the apex audit check's resolver target"
  key_links:
    - from: "the single close-gate commit"
      to: "17 v1.19 requirement identifiers"
      via: "a Validated flip across four planning documents in one atomic commit"
      pattern: "Validated"
    - from: ".planning/phases/138-.../138-VERIFICATION.md"
      to: "check-phase-138.mjs V-138-AUDIT"
      via: "resolveArchivedPhasePath resolving the phase suffix under this milestone's own root token"
      pattern: "v1\\.19-phases"
---

<objective>
HARN-16 (close-gate half) — author the two terminal milestone documents and the phase verification document, land the single atomic close-gate commit flipping all 17 v1.19 requirements to Validated, and run the post-close-gate confirmatory apex that closes the resolver hole.

Purpose: this is the terminal deliverable of v1.19. The deferred-cleanup document is strictly log-only; the phase around it is not, because the close-gate necessarily rewrites four planning documents. The confirmatory apex run is not optional: one predecessor validator reads the live requirements and roadmap documents with a negative assertion attached, and the close-gate rewrites both — a pre-gate green proves nothing about post-gate state.
Output: two milestone documents, the phase verification document, and one atomic commit flipping 17 requirements, followed by a confirmatory apex run whose audit check must be a real pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-04-SUMMARY.md
@.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-05-SUMMARY.md
@.planning/milestones/v1.18-MILESTONE-AUDIT.md
@.planning/milestones/v1.18-DEFERRED-CLEANUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author v1.19-DEFERRED-CLEANUP.md with the six mandatory additions and the root-cause double-booking guard</name>
  <files>.planning/milestones/v1.19-DEFERRED-CLEANUP.md</files>
  <read_first>
    - .planning/milestones/v1.18-DEFERRED-CLEANUP.md (the whole file — line 7 the drop-when-closed doctrine scoped to open items; line 9 the working-tree cruft already scoped out; line 15 the dropped-and-closed section precedent; line 57 the second carve-out recorded closed; lines 71, 83 and 93 the deployment and infrastructure trio; line 110 the cold-clone versus within-apex separation; line 113 the pending-status wording on the standalone-red entry; line 123 the zero-failure claim that is corrected rather than repeated; lines 125-129 the self-test deferral in full; line 166 the cross-references section; line 177 the provenance footer)
    - .planning/milestones/v1.17-DEFERRED-CLEANUP.md line 162 (the earlier dropped-and-closed precedent)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md, each of these decisions read in full: D-21 (the log-only-document versus not-log-only-phase distinction and the guardrail barring edits to the six historical coordinate sites), D-22 (the file's path and the two guarded alternatives), D-23 (the workflow paths coupling this file participates in), D-24 (the redirected correction of record and why the self-test deferral carries unchanged rather than re-opened), D-25 (the six mandatory additions with the complete new and carried lists and the derivation-command guardrail), D-26 (the verbatim-plus-scope-line rule for the hub entry), D-27 (the root-cause-level double-booking ruling and its sole-owner assignment), D-28 (the scope correction as text-only with its occurrences-only labelling), D-17 (the two lowest-numbered validators booked re-surfaced, all five failing assertions, and the frozen call-site preservation constraint), and D-32 (their protection asymmetry — outside every chain and outside the byte gate, yet inside every workflow's paths glob)
    - .planning/phases/136-recipe-4-android-dedicated-mhs-multi-app/136-01-SUMMARY.md lines 199 and 201-207 (the binding verbatim-transcription mandate and the four entries it hands over)
    - .planning/phases/135-recipe-3-windows-11-multi-app-kiosk/135-01-SUMMARY.md line 113 (the line names TWO candidates, not one — both are carried)
    - .planning/phases/137-integration-navigation-last-close/137-02-SUMMARY.md line 197 and 137-CONTEXT.md lines 50 and 211 (the hub sentence to copy verbatim and the two surfaces that give the entry its name)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-04-SUMMARY.md (the enumerated standalone-red set and its four root-cause classes, which the scoped-red entry cites)
  </read_first>
  <action>
    Author the deferred-cleanup document at the milestones path — the workflow's paths filter pins that location explicitly, and two other candidate locations are guarded against. Inherit the predecessor's part structure, its cross-references section and its provenance footer; the footer is where the double-booking cross-check is recorded by convention.

    The document is strictly log-only: zero corpus edits, zero fixes. The phase around it is not log-only, since the close-gate rewrites four planning documents — say which is which rather than conflating them.

    Guardrail on the coordinate correction: the six planning-artefact sites carrying the superseded coordinate are historical records and are NOT edited. The correction rides as an append to the existing fence-mask entry, exactly as the handing-over summary mandates.

    Apply the six mandatory additions beyond the obvious carried list.

    First, restore the deployment and infrastructure trio to carried — content approval, the structured search index, and the automated upload. All three were re-carried at the two prior closes and are re-stated in the live requirements document. Dropping open items is the masking-by-deletion the document's own header bars.

    Second, add all four entries the recipe-four phase's first summary hands over, transcribed verbatim as its mandate requires and as its plan's traceability row repeats: the shared-versus-dedicated taxonomy reference document; the past-due review date on the Android dedicated-devices guide; the coordinate correction appended to the existing fence-mask entry; and the rollback-and-recovery divergence count.

    Third, add the first of the two candidates named on the recipe-three phase's summary line — the retrieval-quality unfalsified-extrapolation entry. That line names two; the second was already booked while citing the same line as its own precedent, so booking only one leaves half the handover on the floor.

    Fourth, add a dropped-and-recorded-Closed section. It is structurally present at both prior closes. The predecessor's back-anchor pin deferral closes at this milestone through this phase's first plan, so it is dropped from the carried list and gets a Closed record in the milestone audit.

    Fifth, discharge the standalone continuous-integration red entry before extending it. Its status at the prior close was explicitly pending the owner's push and not pre-assumed. Discharge it with the evidence captured in the previous plan, using the corrected pass-and-fail baseline, then extend its span through the predecessor milestone.

    Sixth, note the second carve-out as correctly dropped — closed at the prior milestone, and the do-not-mask doctrine is scoped to open items only. Add a one-line note that it is closed and not re-carried, so the departure from the earlier option-B reasoning reads as deliberate.

    New entries also include: this milestone's own back-anchor pin deferral, since freezing this corpus is the successor's job; the two lowest-numbered pre-chain validators booked as re-surfaced rather than new, with all five failing assertions listed and the preservation constraint stated — a later validator pins one of them as a frozen call site and requires both its import and its discriminator marker to survive any future fix; the scoped-red entry citing the ten-member set and its four root-cause classes; the uncovered outbound-link coverage item; the broken hub-enforcement regex; the non-barred hub-wiring surface; the fence-mask divergence; the scope correction; and the correction of record.

    Carried entries include: the first carve-out; the deep-nest cold-clone timeout with its depth label advanced to the forty-eight-through-one-hundred-thirty-seven span and its cold-clone cost curve kept separate from the within-apex curve a prior remediation already collapsed; the latent frozen-awareness item carrying the correction; the self-test deferral unchanged; the five recipe-future items; the Android app-deployment guide; the multi-tenant single-sign-on and Kerberos families; the rename item; the wired network authentication and cloud certificate items; and all the older part-C items.

    Copy the hub entry's sentence verbatim, and add a scope line naming the two surfaces the entry is named for — the runbook index and the decision-trees directory. The drafted sentence is about future routing while the identifier is named for those two surfaces; without the scope line the item loses the thing it is named for.

    Run the double-booking guard at ROOT-CAUSE level, not identifier level. Exact-identifier matching against the frozen predecessor exclusion list is right in form but cannot catch the real double-book here: the scoped-red entry's content and the self-test deferral are one defect, because one of the enumerated failing checks IS the self-test, failing at the same fixture line. An identifier matcher sees two identifiers and passes. Ruling: the self-test deferral is the SOLE owner of that defect; the scoped-red entry cross-references it and does not restate it. Record the guard's result in the provenance footer.

    Two corrections of record go in rather than being carried. The larger one: the predecessor's claim that a complete non-nested chain found no failures. The predecessor apex sets the nesting guard on every child it spawns, so it structurally cannot produce a non-nested chain result, and eight of the chain members exit non-zero standalone on inputs unchanged since that close. Log the correction; do not repeat the claim. The smaller one: the predecessor audit's push semantics, where two separate lines assert that a push fires the cascade — both are false, since no workflow in this repository carries a push trigger. Log it; the in-place amendment of the predecessor document is a live option a successor may take, not one this phase exercises.

    On the scope correction: it is a text correction here with zero corpus edits, and the live requirements document already carries the corrected occurrence and file figures. Label the percentage increase as occurrences-only; the file-count increase is much smaller. Pin the derivation command with a markdown-only include filter — without it the count is inflated by an untracked generated directory, and a successor reads the corrected figure as stale again.
  </action>
  <acceptance_criteria>
    - The file exists at `.planning/milestones/v1.19-DEFERRED-CLEANUP.md`.
    - `grep -c "V119-PIN-DEFERRAL" .planning/milestones/v1.19-DEFERRED-CLEANUP.md` is at least 1.
    - A dropped-and-recorded-Closed section exists, and `V118-PIN-DEFERRAL` appears there — not in the carried list.
    - All three deployment-and-infrastructure items appear in the carried list; grep each by name.
    - The four entries handed over by the recipe-four phase appear, and a diff of each against its source lines in `136-01-SUMMARY.md` shows verbatim transcription.
    - The coordinate correction appears as an append inside the existing fence-mask entry, not as a standalone new entry; and `git diff --name-only` shows zero modifications to any file under `.planning/phases/135-*`, `.planning/phases/136-*` or `.planning/phases/137-*`.
    - Both candidates from the recipe-three phase's summary line appear.
    - The standalone continuous-integration red entry records a discharged status with the corrected 5-and-10 baseline and an extended span; the word pending does not survive on that entry.
    - The second carve-out has a one-line closed-and-not-re-carried note.
    - The scoped-red entry names ten members and four root-cause classes, contains an explicit statement that fixing two of the classes would not green the set, and cross-references the self-test deferral rather than restating it; the self-test deferral itself is present and unchanged.
    - The provenance footer records the root-cause-level double-booking guard result.
    - `grep -c "0 FAIL across the non-nested" .planning/milestones/v1.19-DEFERRED-CLEANUP.md` is 0, and a correction-of-record entry for that claim is present.
    - The hub entry contains the verbatim sentence plus a scope line naming both surfaces.
    - The scope-correction entry labels its percentage as occurrences-only and pins its derivation command with a markdown-only include filter.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/milestones/v1.19-DEFERRED-CLEANUP.md && grep -q "V119-PIN-DEFERRAL" .planning/milestones/v1.19-DEFERRED-CLEANUP.md && grep -q "V118-PIN-DEFERRAL" .planning/milestones/v1.19-DEFERRED-CLEANUP.md && test "$(grep -c '0 FAIL across the non-nested' .planning/milestones/v1.19-DEFERRED-CLEANUP.md)" = "0" && echo OK</automated>
  </verify>
  <done>The deferred-cleanup log exists at the pinned path with all six mandatory additions, the verbatim handover entries, the dropped-and-closed section, both corrections of record, the root-cause double-booking guard recorded in the provenance footer, and zero historical artefacts retro-edited.</done>
</task>

<task type="auto">
  <name>Task 2: Author v1.19-MILESTONE-AUDIT.md and 138-VERIFICATION.md</name>
  <files>.planning/milestones/v1.19-MILESTONE-AUDIT.md, .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-VERIFICATION.md</files>
  <read_first>
    - .planning/milestones/v1.18-MILESTONE-AUDIT.md (the full-structure template — the frontmatter scores, mechanical-checks and notes blocks, then the executive summary, phase-closure narrative, auditor-independence verification, cross-OS exact-match table, predecessor-workflow cascade scan, predecessor byte-unchanged gate at line 294, requirements traceability, mechanical-checks detail, audit-harness lineage, cross-phase integration, deferred-items summary and the milestone-close checklist; also lines 254, 258 and 290, whose push semantics are corrected rather than copied, and line 240, the single-shared-SHA precedent to follow)
    - .planning/milestones/v1.16-MILESTONE-AUDIT.md line 244 (the second single-shared-SHA precedent) and line 214 (the earlier root-causing of the two lowest-numbered validators)
    - .planning/milestones/v1.17-MILESTONE-AUDIT.md line 22 against lines 99, 151, 250 and 267, and line 25 (the cross-SHA defect and its unsupported exact-match assertion — the shape this document must not reproduce)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-04-SUMMARY.md and 138-05-SUMMARY.md (the entire evidence base this document narrates — the gate parts, the byte-unchanged verdict, both local axes, the cross-OS capture and the cascade disposition)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-01-SUMMARY.md (the Wave-0 anchor and the independently derived surface count to cite in the byte-unchanged section)
    - .planning/REQUIREMENTS.md lines 100-121 (the traceability table with all 17 identifiers)
  </read_first>
  <action>
    Author the milestone audit from the predecessor's structure, keeping its section ordering. Content is not a verbatim copy — several of its recorded facts are corrected here.

    In the auditor-independence section, narrate the three axes honestly. The Windows fresh-clone axis is advisory, and the record states that it was not run at two earlier closes and clean at the two most recent — not-run is not clean, and calling it four clean cycles would be false. The fresh zero-context axis is corroborating in weight but was mandatory in execution because it is a named leg in both the requirement and the roadmap's success criterion; if it fell back to the shallow form, the table entry is a shape match and explicitly not a count match, with the earlier rejection of that form cited and rebutted. The cross-OS axis is the sole authoritative leg for both chain validators.

    The cross-OS table records PASS, FAIL and SKIP triples, not pass counts alone, and every axis in it cites the one shared SHA fixed at dispatch time. Record that single-SHA discipline explicitly and cite the two prior closes that followed it. Do not reproduce the defect two closes back, where the frontmatter and body carried different SHAs under an exact-match assertion. The expected apex result carries at least one skip before the gate; state the reason.

    In the cascade scan, use the corrected baseline of five green and ten red across the fifteen pre-existing workflows, with the arithmetic shown against the actual workflow count. Record the disposition as machine-verified with per-run job evidence, and record that the predecessor workflow was green and is excluded from the disposition by construction because it carries the only standalone job for the predecessor apex.

    In the byte-unchanged section, state the independently derived surface count and its composition, and state that the shared-dependency extension is part of the gate, not an aside. Do not carry the predecessor's smaller count.

    In the harness-lineage section, append this milestone's entry as the newest, seventeenth generation, and record the pin-drift proof instrument actually used along with the note that the previously cited instrument could not have proven what it claimed.

    Record two corrections of record in the audit as well: the push-semantics correction, since two lines of the predecessor audit assert that a push fires the cascade and no workflow in this repository carries a push trigger; and the Closed record for the predecessor's back-anchor pin deferral, which this phase's first plan discharged.

    The requirements traceability table lists all 17 identifiers with their phases and Validated status: the five kiosk identifiers and one hygiene identifier from the first content phase, the five managed-home-screen identifiers and one hygiene identifier from the second, the two integration identifiers from the third, and the three harness identifiers from this phase.

    Then author the phase verification document covering all three harness requirements. This document is not only a record — it is the resolver target the apex audit check looks for under this milestone's own phase-root token, which is why the confirmatory run in the next task can assert a real pass rather than a skip.
  </action>
  <acceptance_criteria>
    - Both files exist.
    - The audit's requirements traceability table lists exactly 17 identifiers, all reading Validated; the count is asserted programmatically, not eyeballed.
    - The cross-OS table records PASS, FAIL and SKIP for every axis, and every row cites the same single SHA; a differing SHA anywhere fails this criterion.
    - The cascade section records five green and ten red with the arithmetic shown, and marks the predecessor workflow as excluded from the disposition with its reason.
    - The byte-unchanged section states the independently derived surface count and names the four shared dependencies covered by the extension; the predecessor's smaller count does not appear as this milestone's figure.
    - The auditor-independence section contains the not-run-is-not-clean sentence, and if the shallow zero-context form was used it is labelled shape-match-not-count-match with the prior rejection cited.
    - The harness-lineage section records this as the seventeenth generation and names the pin-drift instrument actually used.
    - Both corrections of record appear: the push semantics, and the Closed record for the predecessor's pin deferral.
    - `grep -c "0 FAIL across the non-nested" .planning/milestones/v1.19-MILESTONE-AUDIT.md` is 0.
    - The phase verification document covers all three harness requirements by identifier.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/milestones/v1.19-MILESTONE-AUDIT.md && test -f .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-VERIFICATION.md && test "$(grep -c '0 FAIL across the non-nested' .planning/milestones/v1.19-MILESTONE-AUDIT.md)" = "0" && echo OK</automated>
  </verify>
  <done>The milestone audit exists with an honest three-axis narrative, a PASS/FAIL/SKIP table at one shared SHA, the corrected cascade baseline, the independently derived byte-unchanged count, the seventeenth-generation lineage entry, both corrections of record and 17-of-17 traceability; the phase verification document exists and covers all three harness requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Land the single atomic close-gate commit, then run the post-close-gate confirmatory apex</name>
  <precondition>138-05-SUMMARY.md records the cross-OS axis captured with the new apex job passed; the close-gate is not authored before that evidence exists.</precondition>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 38-46 (the three unticked harness checkboxes and the blocking-precondition section that is now discharged) and lines 100-121 (the traceability table with all 17 rows and their status column)
    - .planning/ROADMAP.md lines 29-32 (the phase checklist) and lines 129-145 (this phase's section with its four success criteria) and the progress table below them
    - .planning/STATE.md (the milestone status, progress block, current-position block and the pending-todo entries this milestone closes)
    - .planning/PROJECT.md (the milestone status block to flip to shipped)
    - .planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-CONTEXT.md (D-15 part 4 for why the confirmatory run is mandatory; D-19 for the assertion that the audit check must now be a real pass, not a skip)
    - scripts/validation/check-phase-54.mjs lines 30-31 and 344-355 and 568-586 (the live reads of the requirements and roadmap documents with negative assertions — the concrete reason a pre-gate green proves nothing about post-gate state)
  </read_first>
  <action>
    Land ONE atomic close-gate commit. It stages the two milestone documents and the phase verification document from the previous tasks, plus the four planning documents, and it flips all 17 v1.19 requirement identifiers to Validated: the five kiosk identifiers, the two hygiene identifiers, the five managed-home-screen identifiers, the two integration identifiers and the three harness identifiers. In the requirements document, tick the three unticked harness checkboxes, set the traceability status column to Validated on all 17 rows, and mark the blocking-precondition section discharged. In the roadmap, tick this phase's checklist entry, mark the phase section complete, and flip the milestone row in the progress table to shipped. In the project and state documents, flip the milestone status, the progress figures and the current-position blocks.

    One commit. Not two, not a preparatory commit followed by a flip — the two prior closes both landed this as a single commit and the milestone-close record depends on it being one object. Keep the subject line carrying both this milestone's version token and the milestone-close token, since that pair is the discriminator the successor's back-anchor recovery will search on; a subject missing either token makes the successor's pin unrecoverable by the sanctioned method.

    Then, and only then, run the post-close-gate confirmatory apex — the fourth part of the gate, which is not optional. Run the apex verbose with the nesting-guard environment variable unset. The close-gate just rewrote the live requirements and roadmap documents, and one predecessor validator reads both live with a negative assertion attached, so this is the first run whose result says anything about post-gate state.

    Assert specifically that the apex's own audit check is now a real PASS, not a skip. Before this commit the phase verification document did not exist, so the resolver returned nothing and the check legitimately skip-passed; that skip-pass is the shape in which a wrong root token would hide forever. Now the document exists under this milestone's own phase root, so a correct token resolves it and a wrong one does not. This single assertion is where the resolver hole closes — it fails loudly exactly once, at the only moment it matters.

    Record the confirmatory run's full summary line, the audit check's status explicitly, and the close-gate commit SHA in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%s` shows one close-gate commit whose subject contains both this milestone's version token and the milestone-close token; `git log --all --format="%H|%s"` filtered on that subject pair returns exactly 1 result.
    - `git show --stat HEAD` lists all seven files: both milestone documents, the phase verification document, and the four planning documents.
    - `git rev-list --count HEAD~1..HEAD` is 1, and no second close-gate commit exists in the phase's history.
    - All three harness checkboxes in the requirements document are ticked, and `grep -c "| Pending |" .planning/REQUIREMENTS.md` is 0.
    - The traceability table reads Validated on all 17 rows; the count is asserted programmatically.
    - The roadmap's phase checklist entry is ticked and its progress row shows the milestone shipped.
    - `node scripts/validation/check-phase-138.mjs --verbose` run after the commit, with the nesting-guard variable unset, exits 0.
    - In that post-commit run the apex audit check reports a real pass and NOT a skip; the check's status line is captured verbatim in the SUMMARY, and a skip here is a hard failure of this task.
    - The confirmatory run's summary line and the close-gate commit SHA are recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-138.mjs --verbose && test "$(grep -c '| Pending |' .planning/REQUIREMENTS.md)" = "0" && git show --stat HEAD | grep -q "v1.19-MILESTONE-AUDIT" && echo OK</automated>
  </verify>
  <done>One atomic close-gate commit flips all 17 requirements to Validated across the four planning documents while staging both milestone documents and the phase verification document, its subject carries the recovery discriminator pair, and the post-close-gate confirmatory apex exits 0 with its audit check a real pass rather than a skip.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

| Symbol / artifact | Kind | Owner plan |
|---|---|---|
| `MILESTONE_CLOSE_SHAS.V118` (key `'7af8a147'`) | new object key in `_lib/frozen-at-close.mjs` | 138-01 |
| `readAtV118Close` | new export | 138-01 |
| `scripts/validation/v1.19-milestone-audit.mjs` | new file — 17th Path-A harness (C1–C17) | 138-02 |
| `scripts/validation/v1.19-audit-allowlist.json` | new file — sidecar, 59 line-pins in 5 arrays | 138-02 |
| `BASELINE_23` comment block | append-only comment in `regenerate-supervision-pins.mjs` | 138-02 |
| `scripts/validation/check-phase-135.mjs` — `V-135-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-136.mjs` — `V-136-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-137.mjs` — `V-137-*` checks | new leaf validator | 138-03 |
| `scripts/validation/check-phase-138.mjs` — `V-138-AUDIT`, `V-138-CHAIN-48`…`V-138-CHAIN-137`, `V-138-AUDIT-HARNESS`, `V-138-SELF` (93 checks) | new chain apex | 138-03 |
| `.github/workflows/audit-harness-v1.19-integrity.yml` — jobs `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`, `check-phase-135`, `check-phase-136`, `check-phase-137`, `check-phase-138`, `rotting-external-quarterly`, `pin-helper-advisory` | new file — 16th CI coexistence workflow | 138-03 |
| `dist/docs-library-v1.19.zip` | build artifact (gitignored) | 138-04 |
| `.planning/milestones/v1.19-DEFERRED-CLEANUP.md` | new doc | 138-06 |
| `.planning/milestones/v1.19-MILESTONE-AUDIT.md` | new doc | 138-06 |
| `.planning/phases/138-.../138-VERIFICATION.md` | new doc — the apex AUDIT resolver target | 138-06 |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| close-gate commit → requirement Validated state | The flip is the terminal source of truth for milestone completion; a partial or split flip misreports shipped state |
| deferred-cleanup log → the open-debt record | An item silently dropped is debt erased rather than discharged, and the next milestone inherits a shorter list than reality |
| close-gate rewrite → predecessor validators reading live planning documents | The commit rewrites two documents one predecessor validator reads live with a negative assertion attached |
| resolver-null skip-pass → a permanently false-green audit check | Before the gate the skip-pass is legitimate; after it, a skip means the root token is wrong and nothing else would ever say so |
| commit subject line → the successor's back-anchor recovery | The sanctioned recovery method searches the subject for a token pair; a subject missing either makes this SHA unrecoverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-33 | Repudiation | the 17-requirement Validated flip | high | mitigate | One atomic commit asserted by commit count and by a staged-file check over all seven paths; zero remaining pending entries asserted programmatically |
| T-138-34 | Tampering | the open-debt record | high | mitigate | Dropped items require an explicit Closed record; the carried list is grep-asserted to still contain the deployment and infrastructure trio and every other open item |
| T-138-35 | Spoofing | a permanently skip-passing apex audit check | high | mitigate | The post-close-gate confirmatory run asserts a real pass and treats a skip as a hard failure — the single point where a wrong root token fails loudly |
| T-138-36 | Denial of Service | archival drift breaking predecessor validators post-gate | medium | mitigate | The confirmatory run is performed after the commit, over the rewritten live documents, so the class is caught before the milestone is declared closed |
| T-138-37 | Tampering | historical planning artefacts retro-edited to carry a correction | medium | mitigate | Corrections ride as appends to the owning entry; a diff over the three predecessor phase directories is asserted empty |
| T-138-38 | Spoofing | a double-booked deferral appearing as two independent items | medium | mitigate | The guard runs at root-cause level, one owner is named for the shared defect, and the guard's result is recorded in the provenance footer |
| T-138-39 | Repudiation | an unrecoverable close SHA for the successor's pin | high | mitigate | The commit subject carries the version and milestone-close token pair, and the sanctioned subject-line recovery filter is asserted to return exactly one result |

No package-manager installs occur in this plan, so no supply-chain threat (`T-138-SC`) applies. This is documentation and planning-state authoring plus git operations over the trusted local repository; there is no runtime service, no auth surface and no user-supplied input.
</threat_model>

<verification>
- The deferred-cleanup log exists at the pinned path with the six mandatory additions, the verbatim handover entries, the dropped-and-closed section, both corrections of record and the root-cause guard result in the provenance footer.
- The milestone audit records the three axes honestly at one shared SHA with PASS/FAIL/SKIP triples, the corrected cascade baseline, the independently derived byte-unchanged count and 17-of-17 traceability.
- No historical planning artefact under the three predecessor phase directories is modified.
- One atomic commit stages all seven files, flips all 17 requirements to Validated with zero pending remaining, and carries the recovery discriminator pair in its subject.
- The post-close-gate confirmatory apex exits 0, and its audit check is a real pass rather than a skip.
</verification>

<success_criteria>
HARN-16 (close-gate half) and HARN-15 gate part 4 satisfied: v1.19 closes through two terminal milestone documents and a phase verification document, a single atomic commit flipping all 17 requirements to Validated, and a post-close-gate confirmatory apex whose audit check is a real pass — closing the resolver-null hole at the one moment it matters.
</success_criteria>

<output>
Create `.planning/phases/138-v118-pin-17th-path-a-lineage-bump-terminal-close/138-06-SUMMARY.md` when done. It MUST record the close-gate commit SHA with its subject line, the result of the subject-line recovery filter on that pair, confirmation that all 17 requirements read Validated with zero pending remaining, the post-close-gate confirmatory apex summary line, the apex audit check's status captured verbatim as a pass rather than a skip, and the root-cause double-booking guard result.
</output>
