---
phase: 138-v118-pin-17th-path-a-lineage-bump-terminal-close
plan: 03
subsystem: infra
tags: [validation-harness, chain-apex, path-a-lineage, ci-workflow, hazard-fix, needle-spec]

# Dependency graph
requires:
  - phase: 138-01
    provides: "V118 back-anchor pin + readAtV118Close export; WAVE0_ANCHOR 64ee54dd6a4d7ec7617521a988912e10df781808"
  - phase: 138-02
    provides: "v1.19-milestone-audit.mjs (17th Path-A harness) + v1.19-audit-allowlist.json + BASELINE_23, exits 0 --verbose (16/16 PASS)"
  - phase: 137
    provides: "137-02-SUMMARY.md's pre-specified check-phase-137.mjs needle-spec literals (table targets, bullet fragments, corrected hubs-not-wired literals)"
provides:
  - "scripts/validation/check-phase-135.mjs — leaf validator, 7 checks, needled from 135-VERIFICATION.md Observable Truths, exits 0"
  - "scripts/validation/check-phase-136.mjs — leaf validator, 11 checks, needled from 136-02-SUMMARY.md Closure-Table Actuals, exits 0"
  - "scripts/validation/check-phase-137.mjs — leaf validator, 5 checks, the fully pre-specified LINE-SCOPED docs/index.md co-presence needle, exits 0, mutation-tested"
  - "scripts/validation/check-phase-138.mjs — new v1.19 chain-apex, CHAIN[48..137] (90 entries, generated by arithmetic), 93 total checks, three hazard fixes applied, exits 0 standalone non-nested"
  - ".github/workflows/audit-harness-v1.19-integrity.yml — 16th CI coexistence workflow, 3 leaf jobs + dual apex, no check-phase-134 job, DUAL-APEX header restated byte-identical apart from the renumbered apex citation"
affects: ["138-04", "138-06"]

# Actuals (#2632)
actuals:
  tokens: 13749
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Chain-array generated by arithmetic (Array.from) rather than hand-transcribed/copied-forward, with dedup + length + termini module-load throws proven by reverted mutation"
    - "Non-existent chain child = FAIL (deleted-predecessor-validator regression), not graceful skip — corrects the check-phase-134 template's structural blind spot"
    - "Narrowed missing-child classifier (ENOENT/status-127 only) so a module-load throw can never be laundered into a green skip"
    - "Line-scoped single-line-extraction needle testing (never whole-file c.includes()) for per-recipe co-presence checks"

key-files:
  created:
    - scripts/validation/check-phase-135.mjs
    - scripts/validation/check-phase-136.mjs
    - scripts/validation/check-phase-137.mjs
    - scripts/validation/check-phase-138.mjs
    - .github/workflows/audit-harness-v1.19-integrity.yml
  modified: []

key-decisions:
  - "check-phase-135's anti-feature/line-count needles use the LIVE-MEASURED actuals (328 lines, 12 anti-feature rows) rather than 135-VERIFICATION.md's prose fragments (329 lines, an unreduced '8' sub-count) — the prose itself performs 8+2+2=12 arithmetic; the validator asserts the reduced, live-corpus-matching total (Rule 1 auto-fix, see Deviations)"
  - "CHAIN_PHASES in check-phase-138.mjs is GENERATED via Array.from({length: 90}, ...) rather than hand-transcribed, per D-29's explicit 'do not copy Phase 134's array forward' instruction"
  - "check-phase-138.mjs's three hazard fixes (D-30) are new-file-only per D-00a byte-unchanged doctrine — check-phase-134.mjs itself is untouched and still carries all three"
  - "audit-harness-v1.19-integrity.yml's DUAL-APEX header paragraph is kept byte-identical to the predecessor's apart from the renumbered apex citation, per D-09's explicit 'restate verbatim' instruction — this necessarily reproduces the predecessor's own literal 'CHECK_PHASE_NESTED=1' substring inside prose (grep count 1, matching the predecessor file's own count), which conflicts with one literal acceptance-criteria grep expecting 0; the byte-identical requirement is treated as authoritative since it is independently specified three times (CONTEXT D-09, canonical_refs, PATTERNS.md) and directly diff-verified, while the blanket grep is a plan-authoring artifact that the predecessor file itself would also fail (see Deviations)"

patterns-established:
  - "Leaf validators author their needle checks as content-region regex/table-row counters (countTableDataRows/extractSection helpers) rather than raw line-index pinning — matches the established check-phase-129/130/132/133 lightweight style while still reflecting exact measured counts"

requirements-completed: [HARN-15]

coverage:
  - id: D1
    description: "Three leaf validators (check-phase-135/136/137) authored, needle-checking each phase's shipped measurements; all exit 0 against the live corpus; check-phase-137's LINE-SCOPED invariant proven by a reverted false-match mutation"
    requirement: "HARN-15"
    verification:
      - kind: unit
        ref: "for n in 135 136 137; do node scripts/validation/check-phase-$n.mjs; done — 7/7, 11/11, 5/5 PASS respectively, exit 0 each"
        status: pass
      - kind: other
        ref: "check-phase-137 mutation test: moved 'Android Dedicated multi-app kiosk' from the quick-nav bullet into docs/index.md:36 (an unrelated line) — apex exits 1 (BULLET FAIL); reverted via git checkout, re-run exits 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "check-phase-138 apex authored: CHAIN[48..137] generated by arithmetic (90 entries, not copied), 93 total checks, three module-load guards, three hazard fixes (maxBuffer, narrowed missing-child classifier, non-existent-child FAIL), this milestone's own archive-root token; exits 0 standalone non-nested with no competing load"
    requirement: "HARN-15"
    verification:
      - kind: unit
        ref: "node scripts/validation/check-phase-138.mjs --verbose — 92 PASS, 0 FAIL, 1 SKIP (V-138-AUDIT, legitimate pre-close-gate), 93 total checks, 14.4s wall-clock"
        status: pass
      - kind: other
        ref: "truncation mutation: CHAIN_END 137->136 — import throws 'CHAIN_PHASES length 89 !== 90 (integers 48..137 inclusive)'; reverted"
        status: pass
      - kind: other
        ref: "rename mutation: check-phase-90.mjs renamed away — apex reports 'CHAIN-90 FAIL -- ...is absent -- deleted predecessor validator regression', not SKIPPED; reverted, check-phase-90.mjs confirmed passing again"
        status: pass
    human_judgment: false
  - id: D3
    description: "16th CI coexistence workflow authored: 3 leaf jobs + dual apex (check-phase-138 standalone job + linux-chain-ubuntu-latest), no check-phase-134 job, DUAL-APEX header byte-identical apart from the renumbered citation, all three planning-document paths present, all 15 prior workflows untouched"
    requirement: "HARN-15"
    verification:
      - kind: unit
        ref: "python3 -c \"import yaml; yaml.safe_load(open('.github/workflows/audit-harness-v1.19-integrity.yml'))\" — YAML OK"
        status: pass
      - kind: other
        ref: "diff of the two DUAL-APEX header paragraphs (lines 11-14 both files) — differs only in check-phase-134->138 and 48..133->48..137"
        status: pass
    human_judgment: false

duration: ~20min
completed: 2026-08-04
status: complete
---

# Phase 138 Plan 03: check-phase-135..138 Validators + 16th CI Workflow Summary

**Authored the HARN-15 validator atom (three needle-based leaf validators + the new v1.19 chain-apex with three fixed inherited hazards) plus the 16th CI coexistence workflow, and proved the apex green standalone with 93/93 checks (92 PASS / 1 legitimate SKIP / 0 FAIL) before anything leaves this machine.**

## Performance

- **Duration:** ~20 min
- **Tasks:** 3
- **Files modified:** 5 (all created)

## Accomplishments

- `check-phase-135.mjs` (7 checks) — recipe-03 shape (328 lines, `## Rollback/Recovery` ordering, one column-0 XML fence, 3-row namespace table, 12-row anti-feature table) + the zero-edit tripwire on recipe-01's Step 5a/5b headings, exits 0
- `check-phase-136.mjs` (11 checks) — recipe-04 shape (301 lines, 8-H2 skeleton in fixed order, 6 Step headings, 5 admin-question lead-ins, 6 misconfiguration callouts, 9-row anti-feature table, 9-data-row decomposition table, 7-line Verification checklist, 4-row failures table, one column-0 JSON fence), exits 0
- `check-phase-137.mjs` (5 checks) — the fully pre-specified LINE-SCOPED needle from 137-02-SUMMARY.md: per-recipe table-row co-presence + a single isolated bullet-line extraction (never whole-file `.includes()`) + hubs-not-wired on the corrected `recipes/03-`/`recipes/04-` literals; the line-scoping was **proven, not assumed** — a reverted mutation moving the bullet fragment onto an unrelated line drove the check to FAIL
- `check-phase-138.mjs` (93 checks) — the new v1.19 apex: `CHAIN_PHASES` generated by arithmetic (48..137, 90 entries) rather than copied from `check-phase-134`'s 86-entry array; dedup/length/termini module-load throws proven by a reverted truncation mutation; three latent hazards fixed rather than inherited (explicit 20 MiB `maxBuffer`, narrowed missing-child classifier restricted to `ENOENT`/status-127, a non-existent chain child now FAILs rather than skip-passing — proven by a reverted rename mutation); this milestone's own `['v1.19-phases']` archive-root token used, no predecessor form present; exits 0 standalone non-nested (92 PASS / 1 legitimate pre-close-gate SKIP / 0 FAIL, 14.4s wall-clock, no competing local load)
- `audit-harness-v1.19-integrity.yml` — 16th CI coexistence workflow: 3 new leaf jobs + one apex job, deliberately no `check-phase-134` job (the v1.18 workflow already carries the sole standalone job for the predecessor apex per D-08.iii), DUAL-APEX header paragraph restated byte-identical to the predecessor's apart from the renumbered apex citation, all three planning-document paths present in the trigger filter, all 15 prior workflow files untouched

## Task Commits

Each task was committed atomically:

1. **Task 1: Author the three leaf validators check-phase-135, -136 and -137** - `17d6ad35` (feat)
2. **Task 2: Author the apex check-phase-138 with derived numbers and three hazard fixes; prove it green standalone** - `143450c7` (feat)
3. **Task 3: Author the 16th CI coexistence workflow** - `be78d661` (feat)

**Plan metadata:** committed alongside this SUMMARY (see final commit below)

## Files Created/Modified

- `scripts/validation/check-phase-135.mjs` - leaf validator, recipe-03 shape + recipe-01 zero-edit tripwire, 7 checks
- `scripts/validation/check-phase-136.mjs` - leaf validator, recipe-04 shape, 11 checks
- `scripts/validation/check-phase-137.mjs` - leaf validator, LINE-SCOPED docs/index.md co-presence + hubs-not-wired, 5 checks
- `scripts/validation/check-phase-138.mjs` - new v1.19 chain-apex, CHAIN[48..137], 93 total checks, three hazard fixes
- `.github/workflows/audit-harness-v1.19-integrity.yml` - 16th CI coexistence workflow

## Apex Derived Counts (verbatim from the verbose summary)

```
Result: 92 PASS, 0 FAIL, 1 SKIPPED (total checks: 93)
```

- **Total checks: 93** (90 CHAIN + AUDIT + AUDIT-HARNESS + SELF) — matches D-29's derived figure exactly.
- **Chain portion: 90** (`CHAIN_PHASES[0]=48`, `CHAIN_PHASES[89]=137`, generated via `Array.from({length: 90}, (_, i) => 48 + i)`, never hand-transcribed).
- **1 SKIP** = `V-138-AUDIT` (138-VERIFICATION.md not yet authored — legitimate pre-close-gate skip-pass, the expected and correct state before Plan 138-06's close-gate).
- **0 FAIL**, standalone, non-nested, no competing local load, 14.4s wall-clock.

## Three Reverted Mutation-Test Outcomes

**1. Truncated chain array (check-phase-138.mjs module-load throw):**
```
Mutation: CHAIN_END 137 -> 136 (CHAIN_PHASES becomes 89 entries)
Observed: THREW AS EXPECTED: check-phase-138 CHAIN_PHASES length 89 !== 90 (integers 48..137 inclusive)
```
Reverted via a clean file restore; `git status --short` clean afterward.

**2. Renamed chain child (check-phase-138.mjs FAIL, not SKIP):**
```
Mutation: scripts/validation/check-phase-90.mjs renamed to check-phase-90.mjs.renamed
Observed: [CHAIN-90/93] V-138-CHAIN-90: check-phase-90.mjs exits 0 (CHAIN regression-guard) FAIL --
          scripts/validation/check-phase-90.mjs is absent -- deleted predecessor validator regression
          (a member of the fixed 90-entry CHAIN_PHASES span must exist on disk)
Result: 91 PASS, 1 FAIL, 1 SKIPPED (total checks: 93)
```
Reverted via `mv` back; `check-phase-90.mjs` confirmed passing again standalone.

**3. False-match index line (check-phase-137.mjs BULLET, LINE-SCOPED proof):**
```
Mutation: moved the fragment "Android Dedicated multi-app kiosk" from docs/index.md's quick-nav
          bullet line into docs/index.md:36 (the unrelated "Android Enterprise Provisioning" line)
Observed: [BULLET/5] V-137-BULLET: ... FAIL -- BULLET needle absent on the isolated bullet line:
          "Android Dedicated multi-app kiosk"
Result: 4 PASS, 1 FAIL, 0 SKIPPED
```
This is the exact trap the plan warns against: a whole-file `c.includes()` would have found the
fragment (now on the unrelated line) and passed incorrectly. Because the validator extracts and
tests only the single bullet line in isolation, it correctly failed. Reverted via `git checkout --
docs/index.md`; `git status --short` clean, re-run exits 0 (5/5 PASS).

## Archive-Root Token Confirmation

- `grep -c "v1\.19-phases" scripts/validation/check-phase-138.mjs` → **4** (present, at least 1)
- `grep -Ec "v1\.1[5-8]-phases" scripts/validation/check-phase-138.mjs` → **0** (no predecessor form present)

This milestone's own, objectively correct token is used — never the predecessor-copied wrong token
that `check-phase-119/125/128` carry frozen.

## Decisions Made

- check-phase-135's line-count and anti-feature-row needles use the **live-measured actuals** (328
  lines, 12 anti-feature rows) rather than 135-VERIFICATION.md's stated prose fragments (329 lines,
  an unreduced "8" sub-count that the prose's own text resolves to 8+2+2=12) — see Deviations.
- `CHAIN_PHASES` in `check-phase-138.mjs` is generated via `Array.from`, never hand-transcribed, per
  D-29's explicit instruction not to copy Phase 134's array forward.
- The three hazard fixes (D-30) land only in the new `check-phase-138.mjs` — `check-phase-134.mjs`
  itself is untouched and still carries all three, per the D-00a byte-unchanged doctrine.
- The DUAL-APEX header paragraph in the new workflow is byte-identical to the predecessor's apart
  from the renumbered apex citation, per D-09's "restate verbatim" instruction — see Deviations for
  the resulting acceptance-criteria conflict this creates and how it was resolved.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] check-phase-135's stated needle values (329 lines / 8 anti-feature rows) do not match the live corpus (328 lines / 12 anti-feature rows)**
- **Found during:** Task 1 (measuring recipe-03's actual shape before authoring the validator)
- **Issue:** The plan's action text (transcribing 135-VERIFICATION.md's prose) states "the recipe file exists at 329 lines" and "the anti-feature table has 8 rows." Direct measurement of `docs/recipes/03-windows-11-multi-app-kiosk.md` (both at the commit immediately after Phase 135's content landed, and at current HEAD) shows 328 lines. The anti-feature table has 12 data rows — matching 135-VERIFICATION.md's own row-3 arithmetic ("8 mandated rows plus 2 locked additions plus 2 bonus rows" = 12), which the plan's summary text collapsed to the unreduced "8" fragment.
- **Fix:** `check-phase-135.mjs` asserts the measured actuals (328 lines, 12 anti-feature rows), which is what makes the validator pass against the live corpus — the task's own acceptance criterion ("exits 0" for all three leaves) would otherwise be unsatisfiable.
- **Files modified:** `scripts/validation/check-phase-135.mjs`
- **Verification:** `node scripts/validation/check-phase-135.mjs` exits 0, 7/7 PASS.
- **Committed in:** `17d6ad35` (Task 1 commit)

**2. [Rule 1 - Bug] Task 3's acceptance criteria contain a direct, unresolvable-both-ways contradiction: `grep -c "CHECK_PHASE_NESTED"` expected 0 vs. the DUAL-APEX header paragraph required to be byte-identical to the predecessor's**
- **Found during:** Task 3 (running the full acceptance-criteria grep sweep)
- **Issue:** The predecessor's own DUAL-APEX header paragraph (`.github/workflows/audit-harness-v1.18-integrity.yml:11-14`) contains the literal substring `CHECK_PHASE_NESTED=1` inside its own prose ("do NOT add CHECK_PHASE_NESTED=1 to either top-level GHA invocation") — confirmed: `grep -c "CHECK_PHASE_NESTED" audit-harness-v1.18-integrity.yml` = 1 on the existing, already-shipped v1.18 file. The plan mandates this paragraph be restated "byte-identical... apart from the renumbered apex citation" (both in the acceptance criteria and independently in CONTEXT D-09, canonical_refs, and PATTERNS.md — the heavier, three-times-repeated instruction), which necessarily reproduces that same substring. A separate acceptance-criteria line simultaneously requires `grep -c "CHECK_PHASE_NESTED"` to equal 0. Both cannot hold at once for any correct implementation.
- **Resolution:** Prioritized the byte-identical DUAL-APEX paragraph requirement (the more heavily and independently specified invariant, directly diff-verified) over the blanket grep-count-0 criterion, which the predecessor file itself would also fail under an identical test. The resulting file's `CHECK_PHASE_NESTED` grep count is **1**, matching the predecessor's own count exactly, and the string appears only inside the restated prose comment — no job's `env:` block sets `CHECK_PHASE_NESTED` (confirmed: no `env:` key anywhere in the new workflow file).
- **Files modified:** `.github/workflows/audit-harness-v1.19-integrity.yml` (as authored; no rework needed once the conflict was identified)
- **Verification:** `diff` of the two DUAL-APEX header paragraphs shows the only differences are the renumbered apex citation; `grep -c "CHECK_PHASE_NESTED"` = 1 (matches predecessor); no job sets the env var.
- **Committed in:** `be78d661` (Task 3 commit)

---

**Total deviations:** 2 auto-fixed (2 Rule 1 bugs — one a stale-prose-vs-live-corpus mismatch, one a self-contradictory acceptance-criteria pair inherited from the plan's own literal grep expectations)
**Impact on plan:** Both resolutions were necessary to satisfy the task's own overriding acceptance criterion ("exits 0" / byte-identical dual-apex paragraph, confirmed by diff). No scope creep, no architectural change, no frozen surface touched.

## Issues Encountered

None beyond the two self-caught deviations above, both resolved within the same task before its commit.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All four new `check-phase-135..138.mjs` validators exist, needle-checked, and green; the apex proves
  the full 48..137 recursion standalone non-nested before anything is pushed
- `audit-harness-v1.19-integrity.yml` is ready on the local working tree for the owner's Plan 138-05
  `git push` + `workflow_dispatch` checkpoint (D-01/D-02) — this plan performed no push, no
  `gh workflow run`, per the plan's hard constraint
- Plan 138-04's byte-unchanged HARD gate can now diff `WAVE0_ANCHOR` (`64ee54dd6a4d7ec7617521a988912e10df781808`) against current HEAD; this plan's five new files are all NEW additions (not modifications to any frozen surface) and should appear cleanly in that diff
- The `check-phase-30/31` standalone-RED set and the `regenerate-supervision-pins.mjs --self-test` red condition are unchanged by this plan (out of scope, per D-16/D-24) — not re-verified here
- No blockers or concerns carried forward

---
*Phase: 138-v118-pin-17th-path-a-lineage-bump-terminal-close*
*Completed: 2026-08-04*

## Self-Check: PASSED

- FOUND: `scripts/validation/check-phase-135.mjs`
- FOUND: `scripts/validation/check-phase-136.mjs`
- FOUND: `scripts/validation/check-phase-137.mjs`
- FOUND: `scripts/validation/check-phase-138.mjs`
- FOUND: `.github/workflows/audit-harness-v1.19-integrity.yml`
- FOUND: commit `17d6ad35`
- FOUND: commit `143450c7`
- FOUND: commit `be78d661`
