---
phase: 64-apple-business-delegation-runbooks
plan: 04
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - docs/cross-platform/apple-business/16-managed-apple-account-runbook.md
  - docs/cross-platform/apple-business/17-audit-log-scoping-runbook.md
autonomous: true
requirements: [DELEG-06, DELEG-07]

must_haves:
  truths:
    - "Admin can provision Managed Apple Accounts via 16- covering manual + SCIM + OIDC+JIT including a 60-day federation collision resolution sub-section (OP-7) and SCIM token renewal cadence"
    - "Admin can scope audit log access per OU via 17- including author-scope vs target-scope semantics (OP-14), SIEM export pattern (OP-13), and the 'no public REST API' anti-feature"
  artifacts:
    - path: "docs/cross-platform/apple-business/16-managed-apple-account-runbook.md"
      provides: "DELEG-06 account provisioning runbook (manual/SCIM/OIDC+JIT) + 60-day federation collision sub-H2"
      contains: "60-day"
    - path: "docs/cross-platform/apple-business/17-audit-log-scoping-runbook.md"
      provides: "DELEG-07 audit log scoping runbook with author/target-scope + SIEM export + no-REST-API anti-feature"
      contains: "no public REST API"
  key_links:
    - from: "docs/cross-platform/apple-business/16-managed-apple-account-runbook.md"
      to: "docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md"
      via: "back-reference to admin-setup counterpart for federation setup prerequisites"
      pattern: "08-managed-apple-account-provisioning"
    - from: "docs/cross-platform/apple-business/17-audit-log-scoping-runbook.md"
      to: "docs/cross-platform/apple-business/01-role-permission-model.md"
      via: "D-03 Required Role & Permission citation"
      pattern: "01-role-permission-model"
---

<objective>
Author the two identity-and-observability runbooks: `16-` (Managed Apple Account provisioning across manual / SCIM / OIDC+JIT with the OP-7 60-day federation collision sub-section) and `17-` (audit log scoping with author/target-scope semantics, SIEM export, and the "no public REST API" anti-feature).

Purpose: `16-` is the operational counterpart to the `08-` admin-setup federation doc; `17-` documents an anti-feature (no public REST API) so admins seeking automation are correctly redirected.
Output: Two runbook markdown files honoring the D-01 envelope, D-02/D-03 blocks; both use the light `> **Note:**` tier (no destructive `⛔` hard callout — these are not single destructive actions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/64-apple-business-delegation-runbooks/64-CONTEXT.md
@.planning/phases/64-apple-business-delegation-runbooks/64-RESEARCH.md
@.planning/phases/64-apple-business-delegation-runbooks/64-PATTERNS.md
@.planning/phases/64-apple-business-delegation-runbooks/64-CONVENTIONS.md

<interfaces>
<!-- 08- is the admin-setup counterpart to 16-; envelope model + forward-link relationship -->
@docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md
<!-- Envelope analog + light Note tier -->
@docs/cross-platform/apple-business/06-mdm-server-assignment.md
<!-- D-03 Required Role & Permission SOT -->
@docs/cross-platform/apple-business/01-role-permission-model.md

Neither 16- nor 17- carries a destructive `⛔` hard callout. Use the light `> **Note:**` tier (analog 09- line 98) for informational warnings (60-day collision, no-REST-API, retention SLA).

Envelope frontmatter (both): last_verified: 2026-05-22 / review_by: 2026-07-21 / applies_to: apple-business / audience: admin
platform values: 16- = ios+macos (does NOT span tvOS, same as 08-) ; 17- = ios+ipados+macos+tvos

D-03: neither 16- nor 17- cites 04- (people-management and logs permissions are not in the Sub-Org Admin bundle); both cite 01- directly (16- People/Organization Access subgroup; 17- Logs/Activity permission group).

Open-question flags (do NOT fabricate; tag `[verify in portal]` / `[ASSUMED]`): SCIM renewal 60-vs-90-day window (16-); audit log retention SLA — Apple does not publish, do NOT state a number (17-); 60-day post-collision recovery path (16-).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 16-managed-apple-account-runbook.md (DELEG-06)</name>
  <files>docs/cross-platform/apple-business/16-managed-apple-account-runbook.md</files>
  <read_first>
    - docs/cross-platform/apple-business/08-managed-apple-account-provisioning.md (admin-setup counterpart; envelope model lines 1-34; forward-link relationship; differential 04- omission)
    - docs/cross-platform/apple-business/01-role-permission-model.md (People / Organization Access subgroup permissions for account creation + federation config)
    - .planning/phases/64-apple-business-delegation-runbooks/64-RESEARCH.md (DELEG-06: Path A manual / Path B SCIM / Path C OIDC+JIT; SCIM renewal cadence; OP-7 60-day federation collision; SCIM-vs-OIDC decision criteria; SCIM-manual-roaming conflict anti-pattern)
    - .planning/phases/64-apple-business-delegation-runbooks/64-PATTERNS.md (16- section: 08- analog; platform ios+macos; D-03 block citing 01- only)
    - .planning/phases/64-apple-business-delegation-runbooks/64-CONVENTIONS.md (envelope spec; light-Note tier)
    - docs/cross-platform/apple-business/16-managed-apple-account-runbook.md (the file being created)
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/16-managed-apple-account-runbook.md` with the D-01 envelope (platform: ios+macos; Platform-applicability block; D-02 scope-boundary callout + forward link to `18-`; Training-data notice citing FEATURES §6 + STACK §5 + PITFALLS OP-7, 60-day target). Add `## Required Role & Permission` per D-03 citing `01-role-permission-model.md` People / Organization Access subgroup ("Create/Edit Managed Apple Accounts" OU-scoped; "Configure SCIM provisioning" / "Configure federated authentication" tenant-wide DENY-by-default) — do NOT cite `04-`; include OP-2. Body documents the three provisioning paths: Path A manual account creation (Users > Add > Add Account); Path B SCIM from Entra (gallery app, SCIM endpoint, token, attribute mapping, group scope) including the SCIM token renewal cadence and the 4-day transfer window; Path C OIDC + JIT (Federated Authentication > OIDC; auto-create on first sign-in; ssf.manage/ssf.read scopes). Include the SCIM-vs-OIDC decision-criteria table from RESEARCH. Add a dedicated `## 60-Day Federation Collision Resolution` sub-section (OP-7) covering: pre-federation preflight conflicting-Apple-ID count; user-comms 60-day notice template; resolution path (user renames personal Apple ID OR admin force-federates); and the recovery-if-window-missed path tagged `[ASSUMED — Apple Enterprise Support; needs live verification]`. Add the SCIM-manual-roaming conflict anti-pattern as a `> **Note:**`. Back-reference `08-managed-apple-account-provisioning.md` for federation setup prerequisites. Tag the SCIM renewal window as `[ASSUMED]` noting 60-vs-90-day uncertainty and that axm526a05814 is authoritative. Add `## Verification`. Use light `> **Note:**` tier; no `⛔` hard callout.
  </action>
  <acceptance_criteria>
    - File exists with `platform: ios+macos`
    - Documents all three paths: manual, SCIM, OIDC+JIT
    - Contains a `60-Day Federation Collision` sub-section (OP-7) and references SCIM token renewal cadence
    - SCIM renewal window is tagged `[ASSUMED]` (not fabricated as a hard number)
    - Back-references `08-managed-apple-account-provisioning.md`
    - Contains `## Required Role & Permission` + `## Verification` + forward link to `18-` + citation to `01-`; does NOT cite `04-`
    - Passes C15 framing guard
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-64.mjs --verbose 2>&1 | grep -E "V-64-(01|06|07|08|09|10)" ; node -e "const fs=require('fs');const c=fs.readFileSync('docs/cross-platform/apple-business/16-managed-apple-account-runbook.md','utf8');const need=['platform: ios+macos','SCIM','OIDC','60-Day Federation Collision','## Required Role & Permission','## Verification','18-cross-org-boundary-cheat-sheet','01-role-permission-model','08-managed-apple-account-provisioning','[ASSUMED]'];const miss=need.filter(s=>!c.includes(s));if(miss.length){console.error('MISSING: '+miss.join(' | '));process.exit(1)}if(c.includes('04-custom-role-authoring')){console.error('16- must NOT cite 04- (D-03)');process.exit(1)}console.log('16- OK')"</automated>
  </verify>
  <done>16- ships with all three provisioning paths, the OP-7 60-day collision sub-section, SCIM renewal cadence tagged [ASSUMED], 08- back-reference, and full envelope.</done>
</task>

<task type="auto">
  <name>Task 2: Author 17-audit-log-scoping-runbook.md (DELEG-07)</name>
  <files>docs/cross-platform/apple-business/17-audit-log-scoping-runbook.md</files>
  <read_first>
    - docs/cross-platform/apple-business/06-mdm-server-assignment.md (envelope template)
    - docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md (light `> **Note:**` tier analog lines 98-101)
    - docs/cross-platform/apple-business/01-role-permission-model.md ("Read log files" — Logs / Activity permission group)
    - .planning/phases/64-apple-business-delegation-runbooks/64-RESEARCH.md (DELEG-07: activity log access path; author-scope vs target-scope semantics OP-14; SIEM export pattern OP-13; no-public-REST-API anti-feature; Device API distinction; retention SLA caveat)
    - .planning/phases/64-apple-business-delegation-runbooks/64-PATTERNS.md (17- section: no hard callout, light Note tier for anti-feature; retention-SLA Note)
    - .planning/phases/64-apple-business-delegation-runbooks/64-CONVENTIONS.md (envelope spec; light-Note tier)
    - docs/cross-platform/apple-business/17-audit-log-scoping-runbook.md (the file being created)
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/17-audit-log-scoping-runbook.md` with the D-01 envelope (platform: ios+ipados+macos+tvos; Platform-applicability block; D-02 scope-boundary callout + forward link to `18-`; Training-data notice citing FEATURES §7 + PITFALLS OP-13/OP-14, 60-day target). Add `## Required Role & Permission` per D-03 citing `01-role-permission-model.md` Logs / Activity permission group ("Read log files", OU-scoped, delegatable) — do NOT cite `04-`; include OP-2. Body covers: the Activity sidebar log-access path (filter by Category / Event / Status / Date range; Download Logs) tagging filter dimensions `[CITED: training; needs live verification]`; a required `## Author-Scope vs Target-Scope` H2 (OP-14) explaining audit entries are scoped to the AUTHOR's OU not the TARGET resource's OU, with the OU-A→OU-B "ghost device" example and the tenant-wide-query resolution; a SIEM export pattern (OP-13) recommending at-least-monthly export to external SIEM, with a `> **Note:**` stating Apple does NOT publish a definitive retention SLA (do NOT state a number); and a `## No Public REST API` anti-feature section stating audit log access is UI-only, distinguishing the separate Apple Business Device API (Device API Manager preset) which does NOT expose audit logs, and redirecting automation requests to Apple Feedback Assistant. Add `## Verification`. Use light `> **Note:**` tier; no `⛔` hard callout.
  </action>
  <acceptance_criteria>
    - File exists with `platform: ios+ipados+macos+tvos`
    - Contains an author-scope vs target-scope section (OP-14) with the ghost-device example
    - Contains a SIEM export pattern (OP-13) and a Note that Apple does NOT publish a retention SLA (no fabricated number)
    - Contains the string `no public REST API` (or `No Public REST API` heading) anti-feature documentation
    - Distinguishes the Apple Business Device API from audit log access
    - Contains `## Required Role & Permission` + `## Verification` + forward link to `18-` + citation to `01-`; does NOT cite `04-`
    - Passes C15 framing guard
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-64.mjs --verbose 2>&1 | grep -E "V-64-(01|06|07|08|09|10)" ; node -e "const fs=require('fs');const c=fs.readFileSync('docs/cross-platform/apple-business/17-audit-log-scoping-runbook.md','utf8');const lc=c.toLowerCase();const need=['platform: ios+ipados+macos+tvos','author-scope','target-scope','siem','no public rest api','device api','## required role & permission','## verification','18-cross-org-boundary-cheat-sheet','01-role-permission-model'];const miss=need.filter(s=>!lc.includes(s));if(miss.length){console.error('MISSING: '+miss.join(' | '));process.exit(1)}if(c.includes('04-custom-role-authoring')){console.error('17- must NOT cite 04- (D-03)');process.exit(1)}console.log('17- OK')"</automated>
  </verify>
  <done>17- ships with author/target-scope semantics, SIEM export pattern, no-public-REST-API anti-feature, Device API distinction, retention-SLA caveat (no fabricated number), and full envelope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook author → C15 banned-phrase guard | 16- references Entra SCIM/OIDC federation; 17- references SIEM/Device API — Entra/Intune-adjacent phrasing risk |
| runbook content → operator action | 16- missing the OP-7 collision warning could lock users out of federation; 17- overstating a retention SLA could cause compliance gaps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-11 | Information Disclosure | 16- Entra SCIM/OIDC federation prose | mitigate | Frame as Apple-Business-side federation config (identity creation in Apple Business + Entra); ABAUDIT comment on any line that trips a C15 regex |
| T-64-12 | Repudiation | 17- fabricated retention SLA | mitigate | Explicit Note that Apple does NOT publish a retention SLA; no number stated; SIEM export recommended (OP-13) |
| T-64-13 | Tampering | 16- fabricated SCIM renewal window | mitigate | SCIM renewal window tagged `[ASSUMED]` with axm526a05814 as authoritative source (open question 3) |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs — documentation only |
</threat_model>

<verification>
- `node scripts/validation/check-phase-64.mjs` — V-64-01..V-64-10 pass for 16-/17-
- `node scripts/validation/v1.6-milestone-audit.mjs` — C15 framing guard passes
- 16- has the OP-7 60-day sub-section; 17- has author/target-scope + no-public-REST-API
</verification>

<success_criteria>
- 16- ships with manual/SCIM/OIDC+JIT, OP-7 60-day collision sub-section, SCIM cadence ([ASSUMED]-tagged), 08- back-reference
- 17- ships with author/target-scope (OP-14), SIEM export (OP-13), no-public-REST-API anti-feature, Device API distinction, no fabricated retention number
- C15 gate passes for both files
</success_criteria>

<output>
Create `.planning/phases/64-apple-business-delegation-runbooks/64-04-SUMMARY.md` when done
</output>
