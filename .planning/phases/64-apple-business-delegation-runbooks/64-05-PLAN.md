---
phase: 64-apple-business-delegation-runbooks
plan: 05
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - docs/cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md
autonomous: true
requirements: [DELEG-08]

must_haves:
  truths:
    - "18- ships as the canonical Apple-Business-vs-Intune-vs-integration disambiguation TABLE (the D-02 SOT) that 11-..17- forward-link to"
    - "18- harbors the C15 anti-pattern allowlist exemptions in HTML comments — one ABAUDIT-NN per C15-triggering table line (line-pair-scoped)"
    - "18- carries the full D-01 envelope but does NOT carry the scope-boundary forward-link to itself (it IS the SOT)"
  artifacts:
    - path: "docs/cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md"
      provides: "DELEG-08 disambiguation table + C15 ABAUDIT exemption host"
      contains: "ABAUDIT-"
  key_links:
    - from: "docs/cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md"
      to: "scripts/validation/v1.6-milestone-audit.mjs"
      via: "C15 ABAUDIT exemptions consumed by the line-pair-scoped allowlist scanner"
      pattern: "ABAUDIT-\\d+"
---

<objective>
Author `18-cross-org-boundary-cheat-sheet.md` — the canonical Apple-Business-vs-Intune-vs-integration disambiguation table (DELEG-08). This is the D-02 source-of-truth that runbooks `11-`–`17-` forward-link to, and the designated host for the C15 anti-pattern allowlist exemptions (AUDIT-11 contract).

Purpose: `18-` is the highest C15-risk surface in Phase 64 because the disambiguation table necessarily references both Apple Business permissions AND Intune RBAC in the same rows. Each C15-triggering line needs its own line-pair-scoped ABAUDIT comment. Isolating it in its own plan keeps the ABAUDIT-numbering discipline auditable.
Output: One cheat-sheet markdown file with the full D-01 envelope, the disambiguation table, and the sequential ABAUDIT exemptions starting at the allocation defined in 64-CONVENTIONS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/64-apple-business-delegation-runbooks/64-CONTEXT.md
@.planning/phases/64-apple-business-delegation-runbooks/64-RESEARCH.md
@.planning/phases/64-apple-business-delegation-runbooks/64-PATTERNS.md
@.planning/phases/64-apple-business-delegation-runbooks/64-CONVENTIONS.md

<interfaces>
<!-- Envelope analog + D-02 SOT-destination role -->
@docs/cross-platform/apple-business/07-vpp-content-token-consolidation.md
<!-- ABAUDIT style-guide convention (lines 60-83) + live ABAUDIT-01/02/03 examples -->
@docs/cross-platform/apple-business/00-overview.md
<!-- Live ABAUDIT-04 line-pair example -->
@docs/cross-platform/apple-business/06-mdm-server-assignment.md
<!-- The C15 regex set + line-pair-scoped ABAUDIT allowlist scanner (~lines 718-739) -->
@scripts/validation/v1.6-milestone-audit.mjs

C15 line-pair rule: `<!-- ABAUDIT-NN: rationale -->` on line i exempts ONLY line i+1. One comment per banned line. A table row referencing "Intune RBAC" / "Intune role" / "Intune scope tag" / "Intune admin role" needs its own ABAUDIT comment on the immediately preceding line.

ABAUDIT numbering: starts at the allocation in 64-CONVENTIONS.md (ABAUDIT-05 reserved for 12- Path B; 18- table rows continue sequentially after that). Use the next available numbers in sequence, no gaps, no duplicates with shipped ABAUDIT-01..04 or with 12-'s allocation.

D-02 status: 18- IS the scope-boundary SOT — it does NOT carry a scope-boundary callout pointing to itself. It DOES carry the rest of the envelope (frontmatter + Platform applicability + Training-data notice + `## Verification`).

Envelope frontmatter: last_verified: 2026-05-22 / review_by: 2026-07-21 / applies_to: apple-business / audience: admin / platform: ios+ipados+macos+tvos
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 18-cross-org-boundary-cheat-sheet.md (DELEG-08)</name>
  <files>docs/cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md</files>
  <read_first>
    - docs/cross-platform/apple-business/07-vpp-content-token-consolidation.md (envelope template; D-02 SOT-destination role)
    - docs/cross-platform/apple-business/00-overview.md (lines 60-83 ABAUDIT style guide; ABAUDIT-01/02/03 live examples + comment form)
    - docs/cross-platform/apple-business/06-mdm-server-assignment.md (lines 12-13 ABAUDIT-04 line-pair live example)
    - scripts/validation/v1.6-milestone-audit.mjs (C15 regex set ~lines 718-727; line-pair-scoped allowlist scanner ~lines 733-739)
    - .planning/phases/64-apple-business-delegation-runbooks/64-RESEARCH.md (DELEG-08: disambiguation table columns + minimum rows; ABAUDIT concentration; Pitfall 1 line-pair rule)
    - .planning/phases/64-apple-business-delegation-runbooks/64-PATTERNS.md (18- section: ABAUDIT comment form, sequencing; D-02 SOT no-self-link rule)
    - .planning/phases/64-apple-business-delegation-runbooks/64-CONVENTIONS.md (ABAUDIT registry — exact numbers available to 18- after 12-'s allocation)
    - docs/cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md (the file being created)
  </read_first>
  <action>
    Create `docs/cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md` with the D-01 envelope MINUS the scope-boundary forward-link (18- is the SOT and does NOT point to itself): 5-field frontmatter (platform: ios+ipados+macos+tvos); `> **Platform applicability:**` block; `> **Training-data notice:**` block citing STACK §6 + the C15 guard semantics, 60-day target. Body is the disambiguation TABLE with columns `Capability | Apple Business owns | Intune owns | Integration layer`, with rows covering at minimum: VPP catalog management, Shared iPad passcode reset, Device release, Cross-OU device transfer, MDM server reassignment, Managed Apple Account provisioning, Audit log access, Content token download, and Enrollment profile assignment (Intune-side, out of Apple Business scope). For EVERY table row whose cell text contains a C15-triggering phrase (e.g. "Intune RBAC", "Intune role", "Intune scope tag", "Intune admin role" per the milestone-audit regex set), place an `<!-- ABAUDIT-NN: intentional Apple-Business-vs-Intune disambiguation row; C15 false-positive allowlisted -->` comment on the line IMMEDIATELY before that table-row line (line-pair-scoped — the comment exempts only the next line). Number the ABAUDIT comments sequentially per 64-CONVENTIONS.md, continuing after the ABAUDIT-05 allocation reserved for 12- Path B, with no gaps and no collision with shipped ABAUDIT-01..04. If a single row spans multiple markdown lines each containing a banned phrase, each such line gets its own comment. Add a `## Verification` H2. Phrase Intune-side cells as factual ownership statements (Intune OWNS X), not as Apple-Business-maps-to-Intune-delegation framing, to minimize the regex surface. Tag any uncertain ownership boundary `[CITED: training; needs live verification]`.
  </action>
  <acceptance_criteria>
    - File exists with `platform: ios+ipados+macos+tvos`
    - Contains the disambiguation table with the four columns (Capability / Apple Business owns / Intune owns / Integration layer)
    - Table covers at least the 9 required capability rows (VPP, passcode reset, release, transfer, MDM reassign, account provisioning, audit, content token, enrollment profile)
    - Every C15-triggering table line is preceded by an `<!-- ABAUDIT-NN:` comment on the immediately prior line (line-pair-scoped)
    - ABAUDIT numbers are sequential, gap-free, and do NOT collide with ABAUDIT-01..04 or 12-'s ABAUDIT-05
    - Does NOT contain a scope-boundary forward-link to itself (D-02 SOT)
    - Contains `## Verification` H2
    - Passes C15 framing guard (V-64-10 + milestone-audit C15) with all exemptions correctly placed
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-64.mjs --verbose 2>&1 | grep -E "V-64-(01|06|07|09|10)" ; node scripts/validation/v1.6-milestone-audit.mjs 2>&1 | grep -iE "C15|18-cross-org" ; node -e "const fs=require('fs');const c=fs.readFileSync('docs/cross-platform/apple-business/18-cross-org-boundary-cheat-sheet.md','utf8');const need=['platform: ios+ipados+macos+tvos','Apple Business owns','Intune owns','Integration layer','## Verification','ABAUDIT-'];const miss=need.filter(s=>!c.includes(s));if(miss.length){console.error('MISSING: '+miss.join(' | '));process.exit(1)}const nums=[...c.matchAll(/ABAUDIT-(\d+)/g)].map(m=>+m[1]);const sorted=[...new Set(nums)].sort((a,b)=>a-b);for(let i=1;i<sorted.length;i++){if(sorted[i]-sorted[i-1]>1){console.error('ABAUDIT gap between '+sorted[i-1]+' and '+sorted[i]);process.exit(1)}}if(sorted.some(n=>n<5)){console.error('18- ABAUDIT number collides with shipped 01..04');process.exit(1)}console.log('18- OK; ABAUDIT='+sorted.join(','))"</automated>
  </verify>
  <done>18- ships as the disambiguation-table SOT with the full envelope (minus self-link), one line-pair-scoped ABAUDIT comment per C15-triggering row, sequential gap-free numbering, and a passing C15 gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook author → C15 banned-phrase guard | 18- is the highest-density C15 surface — every disambiguation row references both Apple Business and Intune in the same cells |
| 18- ABAUDIT host → milestone-audit allowlist scanner | Misplaced or mis-numbered ABAUDIT comments either leak a banned phrase (C15 FAIL) or over-exempt content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-14 | Information Disclosure | 18- disambiguation table Intune-RBAC rows | mitigate | One line-pair-scoped ABAUDIT comment per C15-triggering line; framing as factual ownership statements minimizes regex surface |
| T-64-15 | Tampering | ABAUDIT numbering collision / gaps | mitigate | Sequential gap-free numbering per 64-CONVENTIONS.md registry; acceptance criteria assert no collision with ABAUDIT-01..04 and no gaps |
| T-64-16 | Tampering | 18- self-referential scope-boundary link | mitigate | D-02 SOT rule — 18- omits the forward-link to itself; acceptance criteria assert absence |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs — documentation only |
</threat_model>

<verification>
- `node scripts/validation/check-phase-64.mjs` — V-64-01/06/07/09/10 pass for 18-
- `node scripts/validation/v1.6-milestone-audit.mjs` — C15 passes with all 18- ABAUDIT exemptions correctly placed (line-pair-scoped)
- ABAUDIT numbering sequential, gap-free, no collision with shipped 01..04
</verification>

<success_criteria>
- 18- is the canonical disambiguation-table SOT with the four-column table and the 9+ required capability rows
- Every C15-triggering line carries its own line-pair-scoped ABAUDIT exemption with sequential numbering
- C15 gate passes; 18- omits the self-referential scope-boundary link
</success_criteria>

<output>
Create `.planning/phases/64-apple-business-delegation-runbooks/64-05-SUMMARY.md` when done
</output>
