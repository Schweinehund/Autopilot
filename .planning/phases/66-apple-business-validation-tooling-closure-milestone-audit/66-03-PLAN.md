---
phase: 66-apple-business-validation-tooling-closure-milestone-audit
plan: 03
type: execute
wave: 3
depends_on: [66-02]
files_modified:
  - .github/workflows/audit-harness-v1.6-integrity.yml
  - .planning/milestones/v1.6-DEFERRED-CLEANUP.md
autonomous: true
requirements:
  - AUDIT-15
tags:
  - validation
  - ci
  - apple-business
  - phase-66
  - deferred-cleanup

must_haves:
  truths:
    - ".github/workflows/audit-harness-v1.6-integrity.yml exists as Path-A copy from audit-harness-v1.5-integrity.yml; coexists with v1.4 + v1.5 workflows (v1.4 + v1.5 workflows NOT modified per D-04 LOCKED)"
    - "CI workflow has TWO schedule crons: weekly bitrot '0 8 * * 1' (Path-A from v1.5) + new quarterly '0 8 1 1,4,7,10 *' (Phase 66 c13_rotting_external check)"
    - "CI workflow has the exact 15-entry pull_request.paths list per CONTEXT.md D-04 (14 source paths + 1 workflow self-reference)"
    - "CI workflow has new job `rotting-external-quarterly` gated by `if: github.event_name == 'schedule' && github.event.schedule == '0 8 1 1,4,7,10 *'`; default continue-on-error: false (D-A9 fully-blocking); positioned BEFORE pin-helper-advisory in the jobs block"
    - "CI workflow preserves pin-helper-advisory as the LAST job with continue-on-error: true VERBATIM from v1.5 line 297 (Phase 43 D-14 / Phase 48 D-22 precedent — the ONLY advisory job in v1.6)"
    - "CI workflow has 5 per-phase check-phase-NN jobs (62/63/64/65/66) replacing v1.5's 14 jobs (48-61), each preserving the graceful-degradation `if [ -f ... ]; then ... else echo skipping ... fi` shape"
    - ".planning/milestones/v1.6-DEFERRED-CLEANUP.md exists with CI-1 + CI-2 + CI-3 sections (each with file paths + line refs + trigger-to-sweep criteria) + CHAIN_SKIP-CRLF deferred-CI-Linux section + Other Deferrals section (carried from Phases 63-65)"
    - "DEFERRED-CLEANUP.md documents the calibration finding: CI-1 measured at 4 business.apple.com URL refs (NOT ~30 historical estimate) — positive surprise per RESEARCH.md Pitfall 3"
    - "check-phase-66.mjs V-66-05 and V-66-07 transition from FAIL to PASS after this wave commits (V-66-06 still RED until Wave 5 authors MILESTONE-AUDIT.md)"
  artifacts:
    - path: .github/workflows/audit-harness-v1.6-integrity.yml
      provides: "PR-blocking CI gate for v1.6 audit harness + 5 chain validators + new quarterly rotting-external job"
      min_lines: 280
      contains:
        - "name: Audit Harness v1.6 Integrity"
        - "scripts/validation/v1.6-*"
        - "docs/cross-platform/apple-business/**"
        - "cron: '0 8 * * 1'"
        - "cron: '0 8 1 1,4,7,10 *'"
        - "rotting-external-quarterly"
        - "node-version: '20'"
        - "v1.6-milestone-audit.mjs"
        - "v1.6-audit-allowlist.json"
        - "check-phase-62"
        - "check-phase-66"
        - "pin-helper-advisory"
        - "continue-on-error: true"
    - path: .planning/milestones/v1.6-DEFERRED-CLEANUP.md
      provides: "v1.7+ backlog handoff for CI-1/CI-2/CI-3 rotting-reference candidates + CHAIN_SKIP CRLF resolution"
      min_lines: 60
      contains:
        - "# v1.6 Deferred Cleanup"
        - "## CI-1: ABM URL Corpus Sweep"
        - "## CI-2: VPP Location Token Surgical Rename"
        - "## CI-3: Managed Apple ID Corpus-Wide Rename"
        - "CHAIN_SKIP"
        - "0 8 1 1,4,7,10 *"
        - "calibration finding"
  key_links:
    - from: ".github/workflows/audit-harness-v1.6-integrity.yml rotting-external-quarterly job"
      to: "scripts/validation/v1.6-audit-allowlist.json c13_rotting_external.ci_1_abm_urls"
      via: "node -e 'JSON.parse(fs.readFileSync(...))' URL extraction at quarterly cron schedule"
      pattern: "ci_1_abm_urls"
    - from: ".github/workflows/audit-harness-v1.6-integrity.yml path-match job"
      to: "scripts/validation/v1.6-milestone-audit.mjs"
      via: "grep -q 'scripts/validation/v1.6-audit-allowlist.json'"
      pattern: "v1\\.6-audit-allowlist\\.json"
    - from: ".planning/milestones/v1.6-DEFERRED-CLEANUP.md CI-1 section"
      to: ".planning/research/PITFALLS.md:617-636"
      via: "documentation cross-reference"
      pattern: "PITFALLS\\.md:617"
---

<objective>
Wave 3 lands the CI workflow + deferred-cleanup backlog artifact in two parallel-safe files (no overlap, both authored from scratch in one commit). This satisfies AUDIT-15 partial (CI workflow + DEFERRED-CLEANUP authoring; the terminal re-audit + MILESTONE-AUDIT are Waves 4 + 5).

**Purpose:**
1. **CI workflow** — `.github/workflows/audit-harness-v1.6-integrity.yml` Path-A copy from v1.5 with v1.6-scoped 15-entry path-filter + 2 crons (weekly + new quarterly) + new `rotting-external-quarterly` job per D-04 LOCKED. Coexists with v1.4 and v1.5 workflows (do NOT modify older files).
2. **DEFERRED-CLEANUP.md** — NEW artifact (no v1.5 standalone equivalent) authoring the v1.7+ handoff: CI-1 / CI-2 / CI-3 rotting-reference candidates + CHAIN_SKIP-on-Windows-CRLF deferred-CI-Linux item + carried-from-63/65 deferrals.

**Output:**
1. `.github/workflows/audit-harness-v1.6-integrity.yml` (NEW; Path-A from v1.5 audit workflow; ~280 lines)
2. `.planning/milestones/v1.6-DEFERRED-CLEANUP.md` (NEW; ~80-100 lines)
3. Post-commit: check-phase-66.mjs V-66-05 + V-66-07 PASS (CI workflow + DEFERRED-CLEANUP exist with expected content); V-66-06 still RED until Wave 5 authors MILESTONE-AUDIT.md
4. Coexistence verified: v1.4 + v1.5 workflows untouched per D-04 LOCKED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-CONTEXT.md
@.planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-RESEARCH.md
@.planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-PATTERNS.md
@.planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-VALIDATION.md
@.github/workflows/audit-harness-v1.5-integrity.yml
@.github/workflows/audit-harness-integrity.yml
@.planning/research/PITFALLS.md
@scripts/validation/v1.6-audit-allowlist.json
@scripts/validation/check-phase-66.mjs

<interfaces>
<!-- Wave 3 reads the v1.5 CI workflow VERBATIM as the Path-A source. Wave 2 already populated c13_rotting_external — Wave 3 reads it to write the quarterly job. -->

**Path-A source for CI workflow:** `.github/workflows/audit-harness-v1.5-integrity.yml` (310 lines at HEAD ad5c9c9)
- Lines 1-5: header comment block + `name:`
- Lines 7-21: `on:` triggers (`pull_request:` with paths list, `schedule:` with single weekly cron, `workflow_dispatch:`)
- Lines 23-41: `parse:` job — Parse v1.5 sidecar JSON via `node -e ...`
- Lines 43-56: `path-match:` job — `grep -q "scripts/validation/v1.5-audit-allowlist.json" scripts/validation/v1.5-milestone-audit.mjs`
- Lines 58-67: `harness-run:` job — `node scripts/validation/v1.5-milestone-audit.mjs --verbose`
- Lines 69-292: 14 per-phase `check-phase-NN:` jobs (48-61) each with graceful-degradation `if [ -f ... ]; then ... fi`
- Lines 293-309: `pin-helper-advisory:` job (line 297 `continue-on-error: true` — the ONLY advisory job in v1.5)

**Wave 3 transformation (Path-A copy with v1.6 deltas):**
- Replace `name:` -> `Audit Harness v1.6 Integrity`
- Replace `pull_request.paths` list with the 15-entry LOCKED list per CONTEXT.md D-04 (verbatim above in interfaces)
- ADD second cron `'0 8 1 1,4,7,10 *'` to `schedule:` (preserve weekly `'0 8 * * 1'`)
- Update `parse:` job label/path to v1.6 sidecar
- Update `path-match:` job grep target to v1.6 file names
- Update `harness-run:` job script path to v1.6-milestone-audit.mjs
- REPLACE 14 v1.5 per-phase jobs (48-61) with 5 v1.6 per-phase jobs (62/63/64/65/66) — same graceful-degradation shape
- ADD NEW `rotting-external-quarterly` job BEFORE pin-helper-advisory; gated by `if: github.event_name == 'schedule' && github.event.schedule == '0 8 1 1,4,7,10 *'`
- PRESERVE `pin-helper-advisory:` as LAST job, VERBATIM (continue-on-error: true stays)

**Sidecar URL extraction for rotting-external-quarterly job** (reads from scripts/validation/v1.6-audit-allowlist.json — already populated in Wave 2):
- `node -e "const sidecar = JSON.parse(fs.readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8')); const c13 = sidecar.c13_rotting_external || {}; const urls = Array.isArray(c13.ci_1_abm_urls) ? c13.ci_1_abm_urls.map(e => e.url).filter(Boolean) : []; fs.writeFileSync('quarterly-urls.txt', urls.join('\\n'));"`
- Then `markdown-link-check` is invoked against each extracted URL (CI-2 + CI-3 entries do not carry URLs — they are line-ref tracking only per RESEARCH.md Example 6 line 661)

**markdown-link-check version pinning:** RESEARCH.md tags as `[ASSUMED]`. Wave 3 executor MUST run `npm view markdown-link-check version` to discover the current published version and pin it in the workflow YAML (replace `<PINNED_VERSION>` placeholder). If npm view is unavailable (offline), use a known-good version such as `3.12.2` and tag as `[ASSUMED]` in a comment.

**DEFERRED-CLEANUP.md structure source:** 66-PATTERNS.md lines 443-504 contains the full template body — copy verbatim with the calibrated finding (4 business.apple.com URL refs, not ~30).

**v1.6-audit-allowlist.json c13_rotting_external state (Wave 2 output):** Already populated per 66-02 atomic commit; ci_1_abm_urls has 4 entries; ci_2_vpp_location_token has 6 entries; ci_3_managed_apple_id has 16 file entries (45 total occurrences). DEFERRED-CLEANUP.md mirrors this enumeration in human-readable form.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 66-03-01: Author audit-harness-v1.6-integrity.yml (Path-A from v1.5 + 15-entry path-filter + quarterly cron + rotting-external-quarterly job)</name>
  <files>.github/workflows/audit-harness-v1.6-integrity.yml</files>
  <read_first>
    - .github/workflows/audit-harness-v1.5-integrity.yml (Path-A source — read ALL 310 lines; extract: header 1-5, triggers 7-21, parse job 23-41, path-match job 43-56, harness-run job 58-67, per-phase job template 69-83, advisory job 293-309)
    - .github/workflows/audit-harness-integrity.yml (v1.4 — read first 30 lines to confirm broad-filter `scripts/validation/**` coexistence pattern; do NOT modify)
    - .planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-CONTEXT.md (D-04 LOCKED — 15-entry path-filter list at lines 146-163; cron + job specifications at lines 169-184)
    - .planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-RESEARCH.md (Code Examples 6 lines 613-663 — quarterly cron + new CI job full template with `if:` gate + markdown-link-check invocation pattern)
    - .planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-PATTERNS.md (`.github/workflows/audit-harness-v1.6-integrity.yml` section lines 225-360 — all job-shape excerpts)
    - scripts/validation/v1.6-audit-allowlist.json (read lines 1-90 to confirm c13_rotting_external is populated per Wave 2 — the rotting-external-quarterly job depends on this state)
    - scripts/validation/check-phase-66.mjs (read V-66-05 assertion at the assertion array to confirm exact substring checks the workflow YAML must satisfy: `"cron: '0 8 * * 1'"`, `"cron: '0 8 1 1,4,7,10 *'"`, `'rotting-external-quarterly'`, `"scripts/validation/v1.6-*"`, `'docs/cross-platform/apple-business/**'`)
  </read_first>
  <action>
**PHASE 1: VERSION PIN DISCOVERY**

Run `npm view markdown-link-check version 2>&1` to discover current published version. Record output. If the command succeeds, use that exact version string (e.g., `3.12.2`) for the `<PINNED_VERSION>` placeholder. If npm view fails (offline/network restricted), use `3.12.2` and add a YAML comment `# [ASSUMED] npm view unavailable at authoring time; verify before next quarterly run` immediately above the install step.

**PHASE 2: AUTHOR THE WORKFLOW (Path-A copy + transformations)**

Create `.github/workflows/audit-harness-v1.6-integrity.yml` by:

1. **Header (Path-A lines 1-5 with v1.5 -> v1.6 substitution):**
```
# v1.6 Audit Harness Integrity
# v1.6 integration surface. v1.4 + v1.4.1 + v1.5 harnesses frozen in their respective workflow files.
# New v1.6 checks (C14-C16) and per-phase validators (62-66) registered here.

name: Audit Harness v1.6 Integrity
```

2. **Triggers (Path-A lines 7-21 with 15-entry path-filter REPLACEMENT + second cron ADDITION):**
```
on:
  pull_request:
    paths:
      - 'scripts/validation/v1.6-*'
      - 'docs/cross-platform/apple-business/**'
      - 'docs/l1-runbooks/34-apple-business-*.md'
      - 'docs/l2-runbooks/26-apple-business-*.md'
      - 'docs/admin-setup-ios/01-abm-configuration.md'
      - 'docs/admin-setup-ios/02-abm-token.md'
      - 'docs/admin-setup-macos/01-abm-configuration.md'
      - 'docs/admin-setup-macos/02-abm-token.md'
      - 'docs/reference/ios-capability-matrix.md'
      - 'docs/operations/00-index.md'
      - 'docs/common-issues.md'
      - 'docs/quick-ref-l1.md'
      - 'docs/quick-ref-l2.md'
      - 'docs/index.md'
      - '.github/workflows/audit-harness-v1.6-integrity.yml'
  schedule:
    - cron: '0 8 * * 1'         # Weekly bitrot catch: 08:00 UTC every Monday (Path-A from v1.5)
    - cron: '0 8 1 1,4,7,10 *'  # Quarterly c13_rotting_external check: 08:00 UTC on 1st of Jan/Apr/Jul/Oct (Phase 66 AUDIT-14)
  workflow_dispatch:
```

3. **`parse:` job (Path-A lines 23-41 with v1.5 -> v1.6 substitution):**
```
jobs:
  parse:
    name: Parse v1.6 sidecar JSON
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - name: Validate v1.6-audit-allowlist.json
        run: |
          node -e "
            const fs = require('fs');
            const j = JSON.parse(fs.readFileSync('scripts/validation/v1.6-audit-allowlist.json', 'utf8'));
            if (!Array.isArray(j.supervision_exemptions) || j.supervision_exemptions.length === 0) {
              console.error('supervision_exemptions empty');
              process.exit(1);
            }
            console.log('v1.6 sidecar OK: ' + j.supervision_exemptions.length + ' supervision pins');
          "
```

4. **`path-match:` job (Path-A lines 43-56 with grep target swap):**
```
  path-match:
    name: Harness references v1.6 sidecar
    runs-on: ubuntu-latest
    needs: parse
    steps:
      - uses: actions/checkout@v4
      - name: Verify harness sidecar reference
        run: |
          if grep -q "scripts/validation/v1.6-audit-allowlist.json" scripts/validation/v1.6-milestone-audit.mjs; then
            echo "OK: harness references v1.6 sidecar"
          else
            echo "FAIL: harness does not reference v1.6-audit-allowlist.json"
            exit 1
          fi
```

5. **`harness-run:` job (Path-A lines 58-67 with script swap):**
```
  harness-run:
    name: Run v1.6 milestone audit harness
    runs-on: ubuntu-latest
    needs: path-match
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - name: Run v1.6-milestone-audit.mjs
        run: node scripts/validation/v1.6-milestone-audit.mjs --verbose
```

6. **5 per-phase `check-phase-NN:` jobs (replace v1.5's 14 jobs 48-61 with v1.6's 5 jobs 62/63/64/65/66):**
For each NN in {62, 63, 64, 65, 66}, replicate the v1.5 graceful-degradation pattern at lines 69-83:
```
  check-phase-{NN}:
    name: check-phase-{NN} validator
    runs-on: ubuntu-latest
    needs: harness-run
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - name: Run check-phase-{NN}.mjs
        run: |
          if [ -f scripts/validation/check-phase-{NN}.mjs ]; then
            node scripts/validation/check-phase-{NN}.mjs
          else
            echo "check-phase-{NN}.mjs not present -- skipping (graceful degradation per Phase 42 D-31)"
          fi
```
(Replace `{NN}` literally; emit 5 blocks total.)

7. **NEW `rotting-external-quarterly:` job (positioned BEFORE pin-helper-advisory; full template from 66-RESEARCH.md Example 6 lines 627-663):**
```
  rotting-external-quarterly:
    name: Quarterly c13_rotting_external link-check
    runs-on: ubuntu-latest
    needs: harness-run
    if: github.event_name == 'schedule' && github.event.schedule == '0 8 1 1,4,7,10 *'
    # DEFAULT continue-on-error: false -- quarterly drift FAILS the scheduled run per D-A9 fully-blocking
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - name: Install markdown-link-check
        run: npm install -g markdown-link-check@<PINNED_VERSION>  # Replace placeholder with version discovered in Phase 1
      - name: Extract URLs from c13_rotting_external
        run: |
          node -e "
            const fs = require('fs');
            const sidecar = JSON.parse(fs.readFileSync('scripts/validation/v1.6-audit-allowlist.json','utf8'));
            const c13 = sidecar.c13_rotting_external || {};
            const urls = [];
            if (Array.isArray(c13.ci_1_abm_urls)) urls.push(...c13.ci_1_abm_urls.map(e => e.url).filter(Boolean));
            // CI-2 + CI-3 entries do not carry URLs -- they are line-ref tracking only
            fs.writeFileSync('quarterly-urls.txt', urls.join('\\n'));
            console.log('Quarterly URL check candidates: ' + urls.length);
          "
      - name: Run markdown-link-check
        run: |
          if [ -s quarterly-urls.txt ]; then
            while IFS= read -r url; do
              markdown-link-check <(echo "[link]($url)") || exit 1
            done < quarterly-urls.txt
          else
            echo "No URLs to check in c13_rotting_external (empty population -- DEFERRED-CLEANUP.md notes calibration finding)"
          fi
```

8. **`pin-helper-advisory:` job (preserve VERBATIM from Path-A lines 293-309 — the ONLY `continue-on-error: true` in the file):**
```
  pin-helper-advisory:
    name: Supervision-pin drift advisory (CI)
    runs-on: ubuntu-latest
    needs: harness-run
    continue-on-error: true   # Phase 43 D-14 / Phase 48 D-22: advisory only; never fails build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - name: Run regenerate-supervision-pins.mjs --report
        run: |
          node scripts/validation/regenerate-supervision-pins.mjs --report || true
          echo "[advisory] CI advisory job; never blocks merge per Phase 43 D-14 + Phase 48 D-22"
      - name: Run --self-test (advisory only)
        run: |
          node scripts/validation/regenerate-supervision-pins.mjs --self-test || \
            echo "[advisory] --self-test failed; advisory only"
```

**PHASE 3: STRUCTURAL VERIFICATION (read-only post-write)**

Confirm the authored file:
- Contains exactly 2 cron lines (`Select-String -Path .github/workflows/audit-harness-v1.6-integrity.yml -Pattern "cron:"` returns 2 matches)
- Contains 15 path-filter entries inside the `paths:` block
- Contains exactly 1 `continue-on-error: true` (in pin-helper-advisory; NO other job carries it)
- Contains 5 `check-phase-NN:` job blocks (NN in {62,63,64,65,66})
- Contains exactly 1 `rotting-external-quarterly:` block with `if: github.event_name == 'schedule'` gate
- `<PINNED_VERSION>` placeholder is REPLACED with actual version string (or carries `[ASSUMED]` comment if npm view failed)

**PHASE 4: DO NOT TOUCH v1.4 or v1.5 WORKFLOWS**

Coexistence per D-04 LOCKED — `.github/workflows/audit-harness-integrity.yml` (v1.4) and `.github/workflows/audit-harness-v1.5-integrity.yml` (v1.5) MUST remain byte-identical pre/post Wave 3. Confirm via `git status --short .github/workflows/` — only `audit-harness-v1.6-integrity.yml` should appear as untracked/new.
  </action>
  <verify>
    <automated>Test-Path .github/workflows/audit-harness-v1.6-integrity.yml ; (Select-String -Path .github/workflows/audit-harness-v1.6-integrity.yml -Pattern "cron:" | Measure-Object).Count ; (Select-String -Path .github/workflows/audit-harness-v1.6-integrity.yml -Pattern "rotting-external-quarterly:" | Measure-Object).Count ; (Select-String -Path .github/workflows/audit-harness-v1.6-integrity.yml -Pattern "continue-on-error: true" | Measure-Object).Count ; (Select-String -Path .github/workflows/audit-harness-v1.6-integrity.yml -Pattern "check-phase-(62|63|64|65|66):" | Measure-Object).Count ; git status --short .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml</automated>
    <expected>Test-Path: True; cron count: 2; rotting-external-quarterly count: 1; continue-on-error: true count: 1; check-phase-NN count: 5; git status on v1.4 + v1.5 workflows shows EMPTY (untouched per D-04 coexistence).</expected>
  </verify>
  <done>audit-harness-v1.6-integrity.yml authored with 2 crons + 15-entry path-filter + 5 check-phase jobs + 1 rotting-external-quarterly job + pin-helper-advisory preserved; v1.4 + v1.5 workflows byte-identical (untouched); check-phase-66.mjs V-66-05 PASSes when this file is on disk.</done>
</task>

<task type="auto">
  <name>Task 66-03-02: Author v1.6-DEFERRED-CLEANUP.md (CI-1/CI-2/CI-3 + CHAIN_SKIP-CRLF + Other Deferrals)</name>
  <files>.planning/milestones/v1.6-DEFERRED-CLEANUP.md</files>
  <read_first>
    - .planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-CONTEXT.md (canonical_refs section: PITFALLS.md line refs for CI-1/CI-2/CI-3 source-of-truth; deferred section listing CHAIN_SKIP + worktree + multi-tenant carry-overs)
    - .planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-RESEARCH.md (c13_rotting_external Populated Payload at lines 697-740 — exact enumeration to mirror in human-readable form; calibration finding at line 742)
    - .planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-PATTERNS.md (lines 443-504 — full DEFERRED-CLEANUP.md template body)
    - .planning/research/PITFALLS.md (sections CI-1 at lines 617-636 + CI-2 at 640-660 + CI-3 at 663-680 — source-of-truth definitions cross-linked from the new artifact)
    - .planning/milestones/v1.5-MILESTONE-AUDIT.md (read deferred_items inline block at lines 41-89 — structural inspiration only; Phase 66 LIFTS to standalone file per CONTEXT D-04)
    - scripts/validation/v1.6-audit-allowlist.json (read c13_rotting_external object populated in Wave 2 — DEFERRED-CLEANUP enumeration MUST match this verbatim)
    - scripts/validation/check-phase-64.mjs (read lines 65-73 — CHAIN_SKIP root-cause documentation block referenced in the CHAIN_SKIP-CRLF section)
  </read_first>
  <action>
Create `.planning/milestones/v1.6-DEFERRED-CLEANUP.md` with the following structure (template from 66-PATTERNS.md lines 443-504, populated with Wave 2 sidecar enumeration):

```
# v1.6 Deferred Cleanup — v1.7+ Backlog

**Authored:** 2026-05-24
**Milestone:** v1.6 (Apple Business Delegated Governance)
**Cross-link from:** `.planning/milestones/v1.6-MILESTONE-AUDIT.md` deferred_items
**Trigger-to-sweep:** Each section below carries explicit criteria for when v1.7+ should pick up the work.

## CI-1: ABM URL Corpus Sweep

**Scope:** Audit all `business.apple.com` and `support.apple.com/guide/apple-business-manager/...` URL refs across the corpus.
**Source-of-truth:** `.planning/research/PITFALLS.md:617-636`
**Calibration finding (Phase 66 measurement at HEAD ad5c9c9):** 4 `business.apple.com` URL refs (NOT ~30 historical estimate); 0 `support.apple.com/guide/apple-business-manager/` refs. The historical estimate likely included Apple Business Manager text occurrences (term references), not URL hyperlinks — positive surprise; rot risk is lower than originally feared.
**Enumerated entries** (matches `scripts/validation/v1.6-audit-allowlist.json` `c13_rotting_external.ci_1_abm_urls`):
- `docs/admin-setup-ios/05-app-deployment.md:92` -> https://business.apple.com (live; quarterly check confirms reachability)
- `docs/admin-setup-macos/01-abm-configuration.md:52` -> https://business.apple.com (live; quarterly check)
- `docs/admin-setup-macos/04-app-deployment.md:105` -> https://business.apple.com (live; quarterly check)
- `docs/_glossary-macos.md:64` -> https://business.apple.com (live; quarterly check)
**Quarterly audit:** `0 8 1 1,4,7,10 *` cron in `.github/workflows/audit-harness-v1.6-integrity.yml` `rotting-external-quarterly` job runs `markdown-link-check` against these URLs on Jan/Apr/Jul/Oct 1st at 08:00 UTC.
**Trigger-to-sweep:** Apple drops legacy URL support OR v1.7 scope explicitly includes corpus sweep OR quarterly audit detects drift (URL returns non-2xx).

## CI-2: VPP Location Token Surgical Rename

**Scope:** Surgical rename of "VPP (Apps and Books) location token" / "VPP location token" to a new canonical term contingent on Apple's evolving label.
**Source-of-truth:** `.planning/research/PITFALLS.md:640-660`
**Enumerated entries** (6 measured at HEAD ad5c9c9; canonical pair per PITFALLS:642 is entries 2 + 6 below; matches `c13_rotting_external.ci_2_vpp_location_token`):
- `docs/admin-setup-ios/05-app-deployment.md:71` (prerequisites bullet)
- `docs/admin-setup-ios/05-app-deployment.md:201` (Renewal/Maintenance table; CI-2 canonical site #1 per PITFALLS:642)
- `docs/admin-setup-macos/04-app-deployment.md:45` (prerequisites bullet)
- `docs/admin-setup-macos/04-app-deployment.md:46` (prerequisites bullet)
- `docs/admin-setup-macos/04-app-deployment.md:113` (verification step)
- `docs/admin-setup-macos/04-app-deployment.md:148` (Renewal/Maintenance table; CI-2 canonical site #2 per PITFALLS:642)
**Trigger-to-sweep:** v1.7 scope includes corpus sweep (lower-effort surgical edit deferred to maintain Q5(b) no-corpus-sweep contract for v1.6).

## CI-3: Managed Apple ID Corpus-Wide Rename

**Scope:** Replace "Managed Apple ID" -> new canonical term (Managed Apple Account) across v1.0-v1.5 corpus.
**Source-of-truth:** `.planning/research/PITFALLS.md:663-680`
**Enumerated entries** (45 occurrences across 16 files at HEAD ad5c9c9; matches `c13_rotting_external.ci_3_managed_apple_id`):
- `docs/admin-setup-android/04-byod-work-profile.md` (1 occurrence)
- `docs/admin-setup-ios/00-overview.md` (2 occurrences)
- `docs/admin-setup-ios/02-abm-token.md` (2 occurrences)
- `docs/admin-setup-ios/08-user-enrollment.md` (17 occurrences — PITFALLS:665 canonical CI-3 pervasive site; highest density)
- `docs/admin-setup-ios/09-mam-app-protection.md` (1 occurrence)
- `docs/admin-setup-macos/01-abm-configuration.md` (5 occurrences)
- `docs/cross-platform/apple-business/02-ous-architecture.md` (1 occurrence — Phase 62 deliverable; legacy term in cross-reference context only)
- `docs/cross-platform/apple-business/09-shared-ipad-lifecycle.md` (1 occurrence — Phase 63 deliverable)
- `docs/ios-lifecycle/01-ade-lifecycle.md` (2 occurrences)
- `docs/l1-runbooks/17-ios-ade-not-starting.md` (2 occurrences)
- `docs/macos-lifecycle/00-ade-lifecycle.md` (2 occurrences)
- `docs/reference/android-capability-matrix.md` (1 occurrence)
- `docs/_glossary-android.md` (1 occurrence)
- `docs/_glossary-apple-business.md` (3 occurrences — Phase 62 glossary; intentional legacy<->new term reciprocity per AB-01)
- `docs/_glossary-linux.md` (1 occurrence)
- `docs/_glossary-macos.md` (3 occurrences)
**Total:** 45 occurrences across 16 files.
**Trigger-to-sweep:** Microsoft Intune adopts the rebrand per REQUIREMENTS.md:74 (Intune-side label "Apple VPP tokens" UNCHANGED in portal as of 2026-04-30 tutorial refresh per STATE.md:141).

## CHAIN_SKIP {48, 51, 58, 60, 61} Resolution via CI-Linux Job

**Scope:** Pre-existing CRLF + archived-path failures documented at `scripts/validation/check-phase-64.mjs:65-73`.
**Root causes:**
- (a) `.planning/phases/` partially archived to `.planning/milestones/v1.5-phases/` — phases 48-61 chain validators reference paths that may not exist in the current tree depending on archival state
- (b) CRLF-LF mismatch on Windows host for Mermaid regex (`\\n` literal in pattern but Windows line-ending is `\\r\\n`)
- (c) cascading failures into check-phase-60/61 from C7/C9 line-number mismatch
**Current state at v1.6 close:** All 5 entries remain in CHAIN_SKIP across `check-phase-62.mjs` through `check-phase-66.mjs` -- suppressed-as-justified on Windows host per `check-phase-64.mjs:65-73`.
**v1.6 Phase 66 advisor finding (new):** The Phase 66 terminal re-audit per D-03 runs from a fresh git clone in `$env:TEMP\v1.6-audit-<rand>` -- BUT it remains on the same Windows host, so CRLF behavior is unchanged. CHAIN_SKIP {48,51,58,60,61} continues to suppress as expected.
**Trigger-to-sweep:** v1.7 introduces CI-Linux job (matches `65-VERIFICATION.md:439` Section K aspirational expectation: "Phase 66's fresh Linux worktree is expected to resolve" -- amended Phase 66 to "v1.7 CI-Linux job" given the `use_worktrees:false` constraint discovered during D-03 analysis).

## Other Deferrals (Carried from Phases 63-65)

- **Multi-tenant Apple Business surfaces** -- Q2 scope-out (v1.6 covers ONE Apple Business account with Locations and/or custom roles)
- **Apple Business Device API documentation** -- Apple has not yet published developer.apple.com landing; acknowledge-but-do-not-document-deeply in `01-role-permission-model.md` per Phase 62 deferral
- **Per-OU Conference Room Display deep-dive** -- Phase 63 deferral (Apple deployment guides thin on per-OU partitioning)
- **Account Holder lockout dedicated recovery runbook** -- Phase 65 deferral (handled as a 7-leaf branch in L2 #26 decision tree per D-02 hybrid)
- **Worktree-lifecycle remediation on Windows** -- out of v1.6 scope; user's standing constraint `use_worktrees:false` (per `.planning/config.json:7`) is durable per memory file `project_execphase_sequential.md`; not a v1.7 deliverable
- **Apple School Manager (ASM) education-specific surfaces** -- outside enterprise scope per REQUIREMENTS.md:81; not a v1.7 deliverable unless education-tenant scope opens

## v1.7+ Routing Summary

| Item | Routing | Required by |
|------|---------|-------------|
| CI-1 ABM URL corpus sweep | v1.7+ | Quarterly audit cron at Jan/Apr/Jul/Oct 2026 |
| CI-2 VPP location token rename | v1.7+ | Lower-effort surgical sweep |
| CI-3 Managed Apple ID rename | v1.7+ | Contingent on Microsoft Intune adopting rebrand |
| CHAIN_SKIP CRLF resolution via CI-Linux job | v1.7+ | v1.7 entry-phase planning |
| Multi-tenant Apple Business + Apple Business Device API + per-OU CRD + Account Holder runbook + ASM | v1.7+ (or later) | Future scope decisions |
```

Substring requirements for V-66-07 to PASS (per check-phase-66.mjs assertion authored in Wave 1):
- `# v1.6 Deferred Cleanup` (title line)
- `## CI-1` (exact heading)
- `## CI-2` (exact heading)
- `## CI-3` (exact heading)
- `CHAIN_SKIP` (substring appears in the CHAIN_SKIP section heading)
  </action>
  <verify>
    <automated>Test-Path .planning/milestones/v1.6-DEFERRED-CLEANUP.md ; (Select-String -Path .planning/milestones/v1.6-DEFERRED-CLEANUP.md -Pattern "^## CI-1" | Measure-Object).Count ; (Select-String -Path .planning/milestones/v1.6-DEFERRED-CLEANUP.md -Pattern "^## CI-2" | Measure-Object).Count ; (Select-String -Path .planning/milestones/v1.6-DEFERRED-CLEANUP.md -Pattern "^## CI-3" | Measure-Object).Count ; (Select-String -Path .planning/milestones/v1.6-DEFERRED-CLEANUP.md -Pattern "CHAIN_SKIP" | Measure-Object).Count ; node scripts/validation/check-phase-66.mjs --verbose 2>&1 | Select-String -Pattern "V-66-07" | Select-String -Pattern "PASS"</automated>
    <expected>Test-Path: True; CI-1/CI-2/CI-3 each appears as a level-2 heading (1 each); CHAIN_SKIP substring count >= 1; V-66-07 reports PASS.</expected>
  </verify>
  <done>v1.6-DEFERRED-CLEANUP.md authored with CI-1 + CI-2 + CI-3 sections (each with file paths + line refs + trigger-to-sweep) + CHAIN_SKIP-CRLF section + Other Deferrals carried from Phases 63-65; check-phase-66.mjs V-66-07 reports PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-harness-v1.6-integrity.yml ↔ GitHub Actions runner | CI YAML is executed in GitHub-hosted runner (ubuntu-latest); no secrets passed; all inputs are version-controlled repo state |
| rotting-external-quarterly job ↔ external URL probes | `markdown-link-check` performs outbound HTTP(S) to URLs listed in `c13_rotting_external.ci_1_abm_urls`; URLs are author-controlled (no user input flows into URL list) |
| Coexistence ↔ v1.4 + v1.5 workflows | v1.4 broad-filter `scripts/validation/**` may double-trigger on v1.6 validator edits — intentional cross-version safety net per D-04 LOCKED note |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-03-V14 | Tampering (config drift) | CI workflow YAML — preserve `continue-on-error: false` on all primary jobs | mitigate | D-A9 fully-blocking + D-04 LOCKED: only `pin-helper-advisory` carries `continue-on-error: true` (verbatim from v1.5 line 297; Phase 43 D-14 / Phase 48 D-22 precedent). Task 66-03-01 PHASE 3 verification asserts `(Select-String continue-on-error: true).Count == 1` — exactly one occurrence. |
| T-66-03-YI | Tampering (YAML injection via path-filter expansion) | pull_request.paths list | mitigate | Path-filter list is human-authored from D-04 LOCKED 15-entry list; no user-controlled wildcards; standard `**` glob is filesystem-safe per GitHub Actions spec. |
| T-66-03-CC | Tampering (subprocess hijacking via PATH) | rotting-external-quarterly job `markdown-link-check` invocation | accept | Job runs in ephemeral ubuntu-latest runner with isolated PATH; `npm install -g markdown-link-check@<PINNED_VERSION>` pins the version (defense-in-depth against typosquat). The `<PINNED_VERSION>` placeholder MUST be replaced with a `npm view`-verified version OR carry an `[ASSUMED]` comment if npm view unavailable. |
| T-66-03-LE | Information disclosure (URL list leakage) | quarterly-urls.txt artifact | accept | URLs are public (e.g., https://business.apple.com); no internal endpoints; tmp file written inside ephemeral runner sandbox and never persisted beyond job. |
| T-66-03-SC | Tampering | npm install markdown-link-check@<PINNED_VERSION> | mitigate | Package is a long-maintained Node ecosystem tool used by v1.4 + v1.5 CI lineage (Path-A inheritance contract per STATE.md:103); not a slopcheck candidate. Wave 3 executor MUST run `npm view markdown-link-check version` and pin the discovered version; if unavailable, fall back to `3.12.2` and tag `[ASSUMED]` in a YAML comment. No blocking checkpoint required (legitimacy verified via v1.5 lineage usage; not a new dependency). |
</threat_model>

<verification>
**Per VALIDATION.md 66-03-01 + 66-03-02 verification targets:**

| Task ID | V-66-XX | Command |
|---------|---------|---------|
| 66-03-01 | V-66-05 | `node scripts/validation/check-phase-66.mjs --verbose 2>&1 \| Select-String -Pattern 'V-66-05' \| Select-String -Pattern 'PASS'` |
| 66-03-01 | YAML shape | `(Select-String -Path .github/workflows/audit-harness-v1.6-integrity.yml -Pattern "^  - cron:" \| Measure-Object).Count -eq 2` (2 cron lines) |
| 66-03-01 | YAML shape | `(Select-String -Path .github/workflows/audit-harness-v1.6-integrity.yml -Pattern "rotting-external-quarterly:" \| Measure-Object).Count -eq 1` |
| 66-03-01 | YAML shape | `(Select-String -Path .github/workflows/audit-harness-v1.6-integrity.yml -Pattern "continue-on-error: true" \| Measure-Object).Count -eq 1` (ONLY pin-helper-advisory) |
| 66-03-01 | YAML shape | `(Select-String -Path .github/workflows/audit-harness-v1.6-integrity.yml -Pattern "check-phase-(62\|63\|64\|65\|66):" \| Measure-Object).Count -eq 5` |
| 66-03-01 | coexistence | `git status --short .github/workflows/audit-harness-integrity.yml .github/workflows/audit-harness-v1.5-integrity.yml` returns EMPTY (v1.4 + v1.5 untouched) |
| 66-03-02 | V-66-07 | `node scripts/validation/check-phase-66.mjs --verbose 2>&1 \| Select-String -Pattern 'V-66-07' \| Select-String -Pattern 'PASS'` |
| 66-03-02 | structure | `(Select-String -Path .planning/milestones/v1.6-DEFERRED-CLEANUP.md -Pattern "^## CI-(1\|2\|3)" \| Measure-Object).Count -eq 3` |
| 66-03 close-gate | chain | `node scripts/validation/check-phase-66.mjs` exits 1 with EXACTLY 1 RED (V-66-06 only); V-66-01..05 + V-66-07 + V-66-ABAUDIT-STALENESS + V-66-CHAIN + V-66-AUDIT + V-66-SELF all PASS or SKIPPED. |
</verification>

<success_criteria>
1. `.github/workflows/audit-harness-v1.6-integrity.yml` exists; passes structural greps (2 crons, 5 check-phase jobs, 1 rotting-external-quarterly job, 1 pin-helper-advisory, exactly 1 continue-on-error: true).
2. `.github/workflows/audit-harness-v1.5-integrity.yml` and `.github/workflows/audit-harness-integrity.yml` are byte-identical pre/post Wave 3 (coexistence per D-04 LOCKED).
3. `markdown-link-check@<PINNED_VERSION>` placeholder replaced with `npm view`-discovered version OR tagged `[ASSUMED]` with comment.
4. `.planning/milestones/v1.6-DEFERRED-CLEANUP.md` exists with CI-1 + CI-2 + CI-3 + CHAIN_SKIP-CRLF + Other Deferrals sections; mirrors `c13_rotting_external` enumeration verbatim in human-readable form.
5. DEFERRED-CLEANUP documents the calibration finding (4 business.apple.com URL refs, not ~30 — positive surprise per RESEARCH.md Pitfall 3).
6. check-phase-66.mjs post-Wave-3: V-66-05 PASS + V-66-07 PASS + V-66-01..04 PASS + V-66-ABAUDIT-STALENESS PASS + V-66-CHAIN-* PASS/SKIPPED + V-66-AUDIT PASS + V-66-SELF PASS; ONLY V-66-06 RED (Wave 5 deliverable).
</success_criteria>

<output>
Create `.planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-03-SUMMARY.md` with:
- CI workflow path + line count + cron + job inventory (parse, path-match, harness-run, check-phase-62..66, rotting-external-quarterly, pin-helper-advisory)
- `markdown-link-check` version discovered (or `[ASSUMED] 3.12.2`)
- DEFERRED-CLEANUP.md section count + key calibration finding (CI-1 = 4 URLs measured, not ~30 historical estimate)
- check-phase-66.mjs post-Wave-3 state: 8 PASS + 5 SKIPPED + 1 FAIL (V-66-06); exit 1
- Coexistence verification: v1.4 + v1.5 workflow files unchanged (git status empty)
- Wave 4 handoff: fresh-clone re-audit will exercise this new CI workflow via the chain validator subprocess pattern
</output>
</content>
</invoke>