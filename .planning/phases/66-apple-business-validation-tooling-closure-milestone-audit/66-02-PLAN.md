---
phase: 66-apple-business-validation-tooling-closure-milestone-audit
plan: 02
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - scripts/validation/v1.6-milestone-audit.mjs
  - scripts/validation/v1.6-audit-allowlist.json
  - scripts/validation/regenerate-supervision-pins.mjs
  - scripts/validation/check-phase-66.mjs
  - scripts/validation/check-phase-62.mjs  # PROBABLE 5th file — discovered during dry-run per D-01 cascade contract; may be no-op if c13 shape change doesn't conflict with V-62-SIDECAR
autonomous: true
requirements:
  - AUDIT-14
tags:
  - validation
  - apple-business
  - atomic-commit
  - harness
  - phase-66

must_haves:
  truths:
    - "v1.6-milestone-audit.mjs windowKeywords regex contains all 6 LOCKED C11 tokens (apple-business-side, intune-side, integration-handshake, owned-by-apple-business, owned-by-intune, scope-boundary) per ROADMAP.md:239"
    - "v1.6-milestone-audit.mjs synthetic regex 7 at line 854 matches production regex 7 at line 725 byte-for-byte (negative-lookahead window = 160 chars; 6 added exclusion terms: renamed, personal, Apple\\s+Business, scopes, ABM, account)"
    - "v1.6-audit-allowlist.json c13_rotting_external is a populated OBJECT (not empty array) with ci_1_abm_urls + ci_2_vpp_location_token + ci_3_managed_apple_id + quarterly_audit sub-fields; quarterly_audit.cadence = '0 8 1 1,4,7,10 *'"
    - "regenerate-supervision-pins.mjs contains BASELINE_10 freshness comment immediately following the BASELINE_9 comment block at lines ~390-397 (Phase 66 audit-trail closing BASELINE_9 v1.5 carry-over per AUDIT-14)"
    - "regenerate-supervision-pins.mjs BASELINE_9 array literal at lines 399-409 is byte-identical to pre-Phase-66 state (Phase 66 does NOT mutate the coordinate array per RESEARCH.md Example 1 rationale)"
    - "ALL 4 (or 5 if check-phase-62.mjs is the discovered reconciliation file) files land in ONE atomic commit verified via `git log --name-only -1 HEAD`"
    - "Pre-commit dry-run (D-01 LOCKED protocol) passed green BEFORE git add: harness + --self-test + check-phase-62.mjs + check-phase-63.mjs + check-phase-64.mjs + check-phase-65.mjs + check-phase-66.mjs all exit 0 modulo CHAIN_SKIP"
    - "Post-commit: v1.6-milestone-audit.mjs exits 0 with C14 PASS + C15 PASS + C16 PASS; v1.6-milestone-audit.mjs --self-test exits 0; check-phase-66.mjs reports V-66-01..04 + V-66-ABAUDIT-STALENESS all PASS (V-66-05..07 still RED until Wave 3 + Wave 5)"
    - "Single git revert <SHA> restores Phase 65 close-gate baseline byte-identically (rollback semantics per D-01)"
  artifacts:
    - path: scripts/validation/v1.6-milestone-audit.mjs
      provides: "C11 windowKeywords +6 tokens; synthetic regex 7 back-port"
      contains:
        - "apple-business-side|intune-side|integration-handshake|owned-by-apple-business|owned-by-intune|scope-boundary"
        - ".{0,160}(Microsoft Intune|Intune documentation|continues to use|formerly|legacy|predates|rebrand|renamed|personal|Apple\\s+Business|scopes|ABM|account)"
    - path: scripts/validation/v1.6-audit-allowlist.json
      provides: "c13_rotting_external populated object + quarterly_audit metadata"
      contains:
        - "\"c13_rotting_external\":"
        - "\"quarterly_audit\":"
        - "\"0 8 1 1,4,7,10 *\""
        - "\"ci_1_abm_urls\""
        - "\"ci_2_vpp_location_token\""
        - "\"ci_3_managed_apple_id\""
    - path: scripts/validation/regenerate-supervision-pins.mjs
      provides: "BASELINE_10 freshness comment closing BASELINE_9 v1.5 carry-over"
      contains:
        - "BASELINE_10 refreshed"
        - "Phase 66"
        - "closes BASELINE_9 v1.5 carry-over"
    - path: scripts/validation/check-phase-66.mjs
      provides: "(authored Wave 1) — touched in Wave 2 atomic commit if dry-run requires assertion tuning"
      contains:
        - "V-66-01"
  key_links:
    - from: "scripts/validation/v1.6-milestone-audit.mjs windowKeywords"
      to: "C11 keyword exemption logic (existing harness behavior at line 577)"
      via: "regex alternation"
      pattern: "apple-business-side\\|intune-side"
    - from: "scripts/validation/v1.6-audit-allowlist.json c13_rotting_external"
      to: ".github/workflows/audit-harness-v1.6-integrity.yml rotting-external-quarterly job (Wave 3)"
      via: "JSON.parse + URL extraction at cron 0 8 1 1,4,7,10 *"
      pattern: "ci_1_abm_urls"
    - from: "scripts/validation/check-phase-62.mjs V-62-SIDECAR (if extended in Wave 2 cascade)"
      to: "scripts/validation/v1.6-audit-allowlist.json c13_rotting_external"
      via: "JSON shape assertion (Array.isArray || (object && quarterly_audit))"
      pattern: "c13_rotting_external"
---

<objective>
Land the AUDIT-14 atomic harness commit per D-01 LOCKED: ONE indivisible commit containing all 4 mandatory files (v1.6-milestone-audit.mjs C11+regex-7 / v1.6-audit-allowlist.json c13_rotting_external populated / regenerate-supervision-pins.mjs BASELINE_10 comment / check-phase-66.mjs V-66 assertions) PLUS a probable 5th reconciling file (check-phase-62.mjs V-62-SIDECAR extension) discovered during the mandatory pre-commit dry-run.

**Purpose:** AUDIT-14 contract per REQUIREMENTS.md:63 and ROADMAP.md:239 says "BASELINE_10 refreshes in an atomic harness commit (closes BASELINE_9 v1.5 carry-over)" — atomic is contractual language, not stylistic. Per CONTEXT.md D-01 (score A=1 / B=5 / C=3) and the V-62-SIDECAR cascade precedent at 65-VERIFICATION.md:255-261, splitting across multiple commits reds the chain in intermediate states. The pre-commit dry-run is the protocol that discovers cross-validator dependencies (the 3->4-file growth at Phase 65) BEFORE staging.

**Output:**
1. ONE git commit (single SHA) modifying 4 or 5 files (4 mandatory + 1 probable check-phase-62.mjs reconciliation)
2. Post-commit verification: `git log --name-only -1 HEAD` shows exactly the 4-5 files; full chain (harness + --self-test + check-phase-{62..66}.mjs) exits 0 modulo CHAIN_SKIP
3. v1.6 audit harness state at HEAD: C14/C15/C16 PASS; --self-test PASS; check-phase-66.mjs reports V-66-01..04 + V-66-ABAUDIT-STALENESS + V-66-CHAIN + V-66-AUDIT + V-66-SELF all PASS (V-66-05..07 still RED until Wave 3 + Wave 5 deliver CI YAML + MILESTONE-AUDIT.md + DEFERRED-CLEANUP.md)

**Atomicity is the work product.** This plan has ONE task — splitting the atomic commit into per-file tasks defeats the indivisibility contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-CONTEXT.md
@.planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-RESEARCH.md
@.planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-PATTERNS.md
@.planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-VALIDATION.md
@.planning/phases/62-apple-business-foundation-rebrand/62-08-PLAN.md
@.planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-VERIFICATION.md
@scripts/validation/v1.6-milestone-audit.mjs
@scripts/validation/v1.6-audit-allowlist.json
@scripts/validation/regenerate-supervision-pins.mjs
@scripts/validation/check-phase-62.mjs
@scripts/validation/check-phase-66.mjs

<interfaces>
<!-- Exact line refs + before/after diffs. Executor must read these source files to verify pre-edit state -->

**v1.6-milestone-audit.mjs:577 — C11 windowKeywords (BEFORE / AFTER):**
- BEFORE (Phase 65 close state at HEAD ad5c9c9):
  `const windowKeywords = /successor|deprecated|historical|disambiguation|mutual-exclusion|mutually\\s+exclusive|co-management|migration|transition|replacement|PITFALL-9|first-occurrence|callout/i;`
- AFTER: append `|apple-business-side|intune-side|integration-handshake|owned-by-apple-business|owned-by-intune|scope-boundary` immediately before `/i` (preserve `/i` flag exactly)
- Net delta: +6 alternation terms; line count unchanged (single-line regex)

**v1.6-milestone-audit.mjs:854 — synthetic regex 7 back-port (BEFORE / AFTER):**
- BEFORE: `/\\bManaged\\s+Apple\\s+ID\\b(?!.{0,80}(Microsoft Intune|Intune documentation|continues to use|formerly|legacy|predates|rebrand))/i,`
- AFTER (copy verbatim from production line 725): `/\\bManaged\\s+Apple\\s+ID\\b(?!.{0,160}(Microsoft Intune|Intune documentation|continues to use|formerly|legacy|predates|rebrand|renamed|personal|Apple\\s+Business|scopes|ABM|account))/i,`
- Net delta: window `.{0,80}` -> `.{0,160}`; +6 exclusion terms in alternation

**v1.6-audit-allowlist.json:79 — c13_rotting_external populated payload (BEFORE / AFTER):**
- BEFORE: `  "c13_rotting_external": [],` (single line at line 79)
- AFTER: multi-line populated object per RESEARCH.md "c13_rotting_external Populated Payload" section (lines 697-740). MUST contain:
  - `ci_1_abm_urls`: array with 4 verified entries (business.apple.com refs at admin-setup-ios/05-app-deployment.md:92, admin-setup-macos/01-abm-configuration.md:52, admin-setup-macos/04-app-deployment.md:105, _glossary-macos.md:64)
  - `ci_2_vpp_location_token`: array with 6 verified entries (per RESEARCH.md lines 705-711)
  - `ci_3_managed_apple_id`: array with 16 file entries + 1 metadata totals object (per RESEARCH.md lines 713-731; 45 total occurrences across 16 files)
  - `quarterly_audit`: { cadence: '0 8 1 1,4,7,10 *', scope: 'external-url-link-check (ci_1_abm_urls only — ci_2 + ci_3 are line-ref tracking, no URL probe)', tool: 'markdown-link-check', next_review: '2026-07-01' }
- Preserve EVERYTHING ELSE at lines 5-78 (8 sibling arrays: safetynet_exemptions / supervision_exemptions / cope_banned_phrases / c7_knox_allowlist / c9_exemptions / c11_ops_exemptions / c13_broken_link_allowlist / c16_missing_endpoint_exemptions) and at line 80 (`c16_missing_endpoint_exemptions: []`) BYTE-IDENTICAL.

**regenerate-supervision-pins.mjs ~lines 386-398 — BASELINE_10 comment insertion:**
- INSERT immediately after the existing BASELINE_9 freshness comment block (the third BASELINE_9 refresh comment ends around line 397) and BEFORE the `const BASELINE_9 = [` array literal on line 399.
- Comment shape (multi-line `//` block, mirroring BASELINE_9 freshness comments at lines 390/393/396):
  ```
  // BASELINE_10 refreshed 2026-05-24 (Phase 66 Plan 66-02): closes BASELINE_9 v1.5 carry-over
  // per AUDIT-14 contract (REQUIREMENTS.md:63 + ROADMAP.md:239); v1.6 line positions verified
  // against HEAD ad5c9c9 (Phase 65 close-gate baseline). BASELINE_9 entries above remain unchanged
  // -- Phase 66 does NOT alter the line-coord array; this comment records the audit-trail event
  // that line-positions were re-verified at Phase 66 close and remain valid for the v1.6 corpus.
  // Resolution path: BASELINE_11 will refresh at v1.7 close per the Path-A inheritance pattern
  // (v1.4.1 -> BASELINE_8 -> v1.5 -> BASELINE_9 -> v1.6 -> BASELINE_10).
  ```
- DO NOT touch the `const BASELINE_9 = [ ... ]` array literal at lines 399-409 (the 9 entries stay byte-identical).

**check-phase-62.mjs:277-291 — V-62-SIDECAR (PROBABLE 5th-file extension; discovered during dry-run):**
- Current shape: asserts `parsed.c16_missing_endpoint_exemptions || [];` and `c16.length === 0` (already [RECONCILED Phase 65])
- IF dry-run shows V-62-SIDECAR FAIL after staging the c13_rotting_external populated payload: EXTEND the assertion to ALSO validate c13_rotting_external shape, accepting BOTH `Array.isArray(c13r) && c13r.length === 0` (legacy phase 62-65 baseline) AND `c13r && typeof c13r === 'object' && c13r.quarterly_audit` (post-AUDIT-14 shape). Update the assertion `name` to include `[EXTENDED Phase 66]` marker. Pattern in 66-PATTERNS.md lines 600-635 (file `scripts/validation/check-phase-62.mjs` section).
- IF dry-run shows V-62-SIDECAR still PASS: this file is NOT modified; atomic commit is 4 files.

**check-phase-66.mjs:** Touched in Wave 2 commit ONLY IF the Wave 1 V-66 assertion bodies need tuning to match the actual c13_rotting_external shape that Wave 2 lands (e.g., if RESEARCH.md JSON shape was wrong and the executor adjusts during dry-run). Default: NO TOUCH in Wave 2 commit — Wave 1 authored the validator correctly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 66-02-01: AUDIT-14 ATOMIC HARNESS COMMIT — pre-commit dry-run + indivisible commit of 4-5 files (v1.5 Plan 60-08 / Phase 62-08 atomic-commit precedent)</name>
  <files>scripts/validation/v1.6-milestone-audit.mjs, scripts/validation/v1.6-audit-allowlist.json, scripts/validation/regenerate-supervision-pins.mjs, scripts/validation/check-phase-66.mjs, scripts/validation/check-phase-62.mjs</files>
  <read_first>
    - scripts/validation/v1.6-milestone-audit.mjs (read lines 570-585 to verify pre-edit state of windowKeywords at line 577; read lines 720-730 to verify production regex 7 at line 725; read lines 850-860 to verify synthetic regex 7 at line 854; read lines 810-825 to verify the --self-test framework entry point)
    - scripts/validation/v1.6-audit-allowlist.json (read lines 75-85 to verify pre-edit state of c13_rotting_external: [] at line 79 and c16_missing_endpoint_exemptions at line 80)
    - scripts/validation/regenerate-supervision-pins.mjs (read lines 383-410 to verify BASELINE_9 freshness comment lines at 390/393/396 and the BASELINE_9 array literal starting at line 399)
    - scripts/validation/check-phase-62.mjs (read lines 275-295 to verify V-62-SIDECAR current shape — currently asserts c16.length === 0 only; needs extension IF dry-run cascades)
    - scripts/validation/check-phase-66.mjs (Wave 1 authored — verify V-66-01..04 + V-66-ABAUDIT-STALENESS assertion shape; this file is the green-gate canary for Wave 2)
    - .planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-RESEARCH.md (sections: "c13_rotting_external Populated Payload" at lines 697-740 — the exact JSON shape to write into v1.6-audit-allowlist.json; "Pattern 1: Atomic Harness Commit with 5th-File Budget" lines 165-180 for the cascade rationale; "Pitfall 1: Cross-validator dependency cascade" lines 261-277 for the dry-run loop steps)
    - .planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-PATTERNS.md (sections: "v1.6-milestone-audit.mjs (MODIFIED)" lines 508-540 — modification deltas; "v1.6-audit-allowlist.json (MODIFIED)" lines 543-575 — c13 payload shape; "regenerate-supervision-pins.mjs (MODIFIED)" lines 579-596 — BASELINE_10 comment shape; "check-phase-62.mjs (PROBABLE MODIFICATION)" lines 600-635 — V-62-SIDECAR extension pattern)
    - .planning/phases/65-apple-business-l1-l2-hub-navigation-integration/65-VERIFICATION.md (read "Atomic-Trio Reconciliation Record" lines 255-261 — direct precedent for the 3->4 file cascade Phase 66 inherits as 4->5 budget)
    - .planning/phases/62-apple-business-foundation-rebrand/62-08-PLAN.md (read Task 62-08-04 at lines 430-490 — atomic-commit recipe verbatim precedent; pre-commit dry-run + post-commit verification structure)
  </read_first>
  <action>
**PHASE 1: PRE-EDIT VERIFICATION (read-only)**

Confirm pre-edit state by inspecting each target line:
1. `v1.6-milestone-audit.mjs:577` matches the BEFORE regex in `<interfaces>` (no `apple-business-side` substring present)
2. `v1.6-milestone-audit.mjs:854` matches the BEFORE regex (`.{0,80}` window, 6 exclusions only)
3. `v1.6-audit-allowlist.json:79` is exactly `  "c13_rotting_external": [],`
4. `regenerate-supervision-pins.mjs` has BASELINE_9 freshness comments at lines 390/393/396 and `const BASELINE_9 = [` at line 399 (NO BASELINE_10 comment present yet)
5. `check-phase-62.mjs:277-291` V-62-SIDECAR currently asserts `c16.length === 0` only (does NOT assert c13_rotting_external shape)
6. `check-phase-66.mjs` exists from Wave 1; V-66-01..04 + V-66-ABAUDIT-STALENESS assertions present

**PHASE 2: APPLY THE 4 MANDATORY EDITS (working tree, NOT yet staged)**

EDIT 1 — `scripts/validation/v1.6-milestone-audit.mjs` line 577:
Replace the entire regex line with: `const windowKeywords = /successor|deprecated|historical|disambiguation|mutual-exclusion|mutually\\s+exclusive|co-management|migration|transition|replacement|PITFALL-9|first-occurrence|callout|apple-business-side|intune-side|integration-handshake|owned-by-apple-business|owned-by-intune|scope-boundary/i;`

EDIT 2 — `scripts/validation/v1.6-milestone-audit.mjs` line 854:
Replace the synthetic regex 7 line with the production line 725 verbatim: `  /\\bManaged\\s+Apple\\s+ID\\b(?!.{0,160}(Microsoft Intune|Intune documentation|continues to use|formerly|legacy|predates|rebrand|renamed|personal|Apple\\s+Business|scopes|ABM|account))/i,` (preserve the trailing comma and the 2-space indent at column 1-2 to match the surrounding `C15_BANNED_SYNTH` array shape).

EDIT 3 — `scripts/validation/v1.6-audit-allowlist.json` line 79:
Replace `  "c13_rotting_external": [],` with the multi-line populated object from RESEARCH.md "c13_rotting_external Populated Payload" section (lines 697-740) — VERBATIM JSON. Preserve trailing comma to keep the JSON valid (the next key on line 80 is `"c16_missing_endpoint_exemptions": []`).

EDIT 4 — `scripts/validation/regenerate-supervision-pins.mjs` ~line 398:
Insert the BASELINE_10 multi-line `//` comment block per the shape in `<interfaces>` IMMEDIATELY after the existing BASELINE_9 freshness comment block (after line 397) and BEFORE the `const BASELINE_9 = [` array literal (line 399). DO NOT modify the BASELINE_9 array contents.

**PHASE 3: PRE-COMMIT DRY-RUN LOOP (MANDATORY — D-01 LOCKED protocol)**

Run the following in sequence — ALL must exit 0 BEFORE git add:

```
node scripts/validation/v1.6-milestone-audit.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 1 FAILED: harness exit $LASTEXITCODE" }
node scripts/validation/v1.6-milestone-audit.mjs --self-test ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 2 FAILED: --self-test exit $LASTEXITCODE" }
node scripts/validation/check-phase-62.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 3 FAILED: check-phase-62 exit $LASTEXITCODE (V-62-SIDECAR canary)" }
node scripts/validation/check-phase-63.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 4 FAILED: check-phase-63 exit $LASTEXITCODE" }
node scripts/validation/check-phase-64.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 5 FAILED: check-phase-64 exit $LASTEXITCODE" }
node scripts/validation/check-phase-65.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "DRY-RUN STEP 6 FAILED: check-phase-65 exit $LASTEXITCODE" }
node scripts/validation/check-phase-66.mjs ; if ($LASTEXITCODE -ne 0) { Write-Host "DRY-RUN STEP 7 (check-phase-66): exit $LASTEXITCODE — verify failures are V-66-05/06/07 ONLY (CI YAML + MILESTONE-AUDIT + DEFERRED-CLEANUP not yet authored)" }
```

**Expected dry-run state:**
- Steps 1, 2, 3, 4, 5, 6 MUST exit 0
- Step 7 (check-phase-66.mjs) MUST exit 1 with EXACTLY 3 failures (V-66-05, V-66-06, V-66-07 — all reference Wave 3/5 deliverables not yet authored); V-66-01..04 + V-66-ABAUDIT-STALENESS + V-66-CHAIN + V-66-AUDIT + V-66-SELF MUST all PASS

**If Step 3 (check-phase-62.mjs V-62-SIDECAR) FAILS** with "c13_rotting_external is unexpected shape" or "Array.isArray check FAIL": this is the predicted cascade per Pitfall 1. APPLY THE PROBABLE 5TH-FILE EDIT:

EDIT 5 (CONDITIONAL — only if Step 3 cascaded) — `scripts/validation/check-phase-62.mjs:277-291`:
Extend the V-62-SIDECAR assertion per 66-PATTERNS.md lines 600-635 pattern. Update the assertion `name` to `'V-62-SIDECAR [RECONCILED Phase 65, EXTENDED Phase 66]: ... + c13_rotting_external is object-shaped (Phase 66 AUDIT-14 population)'`. Add logic:
```
const c13r = parsed.c13_rotting_external;
if (Array.isArray(c13r) && c13r.length === 0) {
  return { pass: true, detail: 'valid JSON; c16=0; c13_rotting_external=[] (pre-AUDIT-14 baseline)' };
}
if (c13r && typeof c13r === 'object' && c13r.quarterly_audit) {
  return { pass: true, detail: 'valid JSON; c16=0; c13_rotting_external=object (Phase 66 AUDIT-14 shape)' };
}
return { pass: false, detail: 'c13_rotting_external is unexpected shape: ' + JSON.stringify(c13r).slice(0, 120) };
```
Re-run the dry-run loop from STEP 1. ALL must exit 0 (with check-phase-66.mjs still RED on V-66-05/06/07 only).

**If any OTHER validator (check-phase-63/64/65) FAILS:** investigate the specific failure. The dry-run is the discovery mechanism. Add the reconciling file with `[RECONCILED Phase 66]` marker to the atomic set; re-loop from STEP 1.

**PHASE 4: ATOMIC COMMIT (one commit; ALL files in single SHA)**

After fully green dry-run (modulo expected V-66-05/06/07):

```
git add scripts/validation/v1.6-milestone-audit.mjs scripts/validation/v1.6-audit-allowlist.json scripts/validation/regenerate-supervision-pins.mjs scripts/validation/check-phase-66.mjs
# IF Edit 5 was applied:
git add scripts/validation/check-phase-62.mjs

git commit -m "$(cat <<'EOF'
feat(validation): AUDIT-14 atomic harness commit — C11 +6 LOCKED tokens / c13_rotting_external populated / BASELINE_10 freshness / regex-7 synthetic back-port

Lands AUDIT-14 contract per REQUIREMENTS.md:63 + ROADMAP.md:239 in ONE indivisible commit per v1.5 Plan 60-08 / Phase 62-08 atomic-commit precedent (STATE.md:111-112).

Components (all in one commit; full chain exits 0 modulo Wave-3/5 V-66-05/06/07):
- v1.6-milestone-audit.mjs: windowKeywords +6 LOCKED C11 tokens (apple-business-side / intune-side / integration-handshake / owned-by-apple-business / owned-by-intune / scope-boundary) at line 577 per ROADMAP:239; synthetic regex 7 back-port at line 854 (.{0,80} -> .{0,160} window; +6 exclusion terms: renamed|personal|Apple\\s+Business|scopes|ABM|account) matches production line 725
- v1.6-audit-allowlist.json: c13_rotting_external populated as object with ci_1_abm_urls (4 calibrated URL refs — smaller than ~30 historical estimate per RESEARCH.md calibration finding) + ci_2_vpp_location_token (6 entries) + ci_3_managed_apple_id (16 file entries, 45 total occurrences) + quarterly_audit metadata (cadence '0 8 1 1,4,7,10 *')
- regenerate-supervision-pins.mjs: BASELINE_10 freshness comment block inserted at ~line 398 (closes BASELINE_9 v1.5 carry-over per STATE.md:138; BASELINE_9 array literal unchanged — Phase 66 records audit-trail event, not coordinate mutation)
- check-phase-66.mjs: V-66-01..04 + V-66-ABAUDIT-STALENESS now all PASS (V-66-05/06/07 still RED until Wave 3 + Wave 5)
# IF EDIT 5 applied, append:
- check-phase-62.mjs: V-62-SIDECAR [EXTENDED Phase 66] accepts both legacy c13_rotting_external=[] (pre-AUDIT-14) and post-AUDIT-14 object shape — pre-commit dry-run cascade discovery per Phase 65's 3->4 file growth precedent at 65-VERIFICATION.md:255-261

Co-location confirmed: scripts/validation/v1.6-audit-allowlist.json already at scripts/validation/ — no migration required per CONTEXT D-01.

Pre-commit dry-run protocol passed: harness exit 0, harness --self-test exit 0, check-phase-{62..66} exit 0 modulo expected V-66-05/06/07 reds (Wave 3 + Wave 5 deliverables).

Rollback: git revert <THIS_SHA> restores Phase 65 close-gate baseline byte-identically.

AUDIT-14
EOF
)"
```

**PHASE 5: POST-COMMIT VERIFICATION**

```
git log --name-only -1 HEAD
```
Output MUST list exactly 4 OR 5 files: v1.6-milestone-audit.mjs, v1.6-audit-allowlist.json, regenerate-supervision-pins.mjs, check-phase-66.mjs (+ check-phase-62.mjs IF Edit 5 was applied). If any other file appears, revert + redo with correct file selection.

```
node scripts/validation/v1.6-milestone-audit.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT harness exit $LASTEXITCODE" }
node scripts/validation/v1.6-milestone-audit.mjs --self-test ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT --self-test exit $LASTEXITCODE" }
node scripts/validation/check-phase-62.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT check-phase-62 exit $LASTEXITCODE" }
node scripts/validation/check-phase-63.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT check-phase-63 exit $LASTEXITCODE" }
node scripts/validation/check-phase-64.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT check-phase-64 exit $LASTEXITCODE" }
node scripts/validation/check-phase-65.mjs ; if ($LASTEXITCODE -ne 0) { Write-Error "POST-COMMIT check-phase-65 exit $LASTEXITCODE" }
```
ALL 6 MUST exit 0. If any fail post-commit (despite dry-run green), investigate immediately — chain green-gate has regressed.

`node scripts/validation/check-phase-66.mjs` MUST exit 1 with EXACTLY 3 failures (V-66-05, V-66-06, V-66-07). V-66-01, V-66-02, V-66-03, V-66-04, V-66-ABAUDIT-STALENESS, V-66-CHAIN-*, V-66-AUDIT, V-66-SELF all PASS or SKIPPED.
  </action>
  <verify>
    <automated>git log --name-only -1 HEAD | Select-String -Pattern 'v1\\.6-milestone-audit\\.mjs|v1\\.6-audit-allowlist\\.json|regenerate-supervision-pins\\.mjs|check-phase-66\\.mjs' | Measure-Object | Select-Object -ExpandProperty Count ; if ($? -and (node scripts/validation/v1.6-milestone-audit.mjs ; $LASTEXITCODE) -ne 0) { Write-Error "harness FAIL post-commit" } ; if ((node scripts/validation/v1.6-milestone-audit.mjs --self-test ; $LASTEXITCODE) -ne 0) { Write-Error "self-test FAIL" } ; if ((node scripts/validation/check-phase-62.mjs ; $LASTEXITCODE) -ne 0) { Write-Error "check-62 FAIL" } ; if ((node scripts/validation/check-phase-65.mjs ; $LASTEXITCODE) -ne 0) { Write-Error "check-65 FAIL" }</automated>
    <expected>git log shows >= 4 file names from the regex set (4 mandatory; 5 if Edit 5 applied); harness exit 0; --self-test exit 0; check-phase-62 exit 0 (with or without [EXTENDED Phase 66] marker); check-phase-65 exit 0.</expected>
  </verify>
  <done>ONE atomic commit landed at HEAD containing 4 (or 5) files; post-commit harness + --self-test + check-phase-{62..65} all exit 0; check-phase-66 reports V-66-01..04 + V-66-ABAUDIT-STALENESS all PASS (V-66-05/06/07 still RED until Wave 3 + Wave 5); single git revert restores Phase 65 baseline byte-identically; AUDIT-14 contract satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-commit dry-run ↔ working-tree state | The dry-run runs validators against unstaged working-tree edits; if validators read from disk via fs.readFileSync (they do), the dry-run accurately reflects what the post-commit state will be |
| Atomic commit ↔ chain green-gate | Single git commit must atomically transition the chain from "Phase 65 close-gate baseline" to "Phase 66 AUDIT-14 post-state"; intermediate commits would red the chain |
| C11 windowKeywords +6 tokens ↔ harness exemption decisions | New tokens extend the keyword set that the harness uses to identify "informational/disambiguation" prose patterns; expanding the set ONLY broadens exemption coverage (does not red existing content) |
| c13_rotting_external populated object ↔ V-62-SIDECAR shape assertion | The JSON shape change from [] to populated object may red V-62-SIDECAR if the validator asserts the empty-array shape; dry-run is the discovery mechanism per Pitfall 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-02-V14 | Tampering (configuration drift) | v1.6-milestone-audit.mjs C11 windowKeywords + synthetic regex 7 back-port | mitigate | (a) C11 +6 tokens are LOCKED in ROADMAP.md:239 — exact alternation strings; deviation fails V-66-01 immediately. (b) Regex 7 back-port is verbatim copy from production line 725 — no novel regex authoring; deviation fails V-66-04. (c) --self-test re-runs synthetic C15/C14/C16 fixtures post-edit; preserves harness behavior contract. (d) Pre-commit dry-run is mandatory per D-01 step 4 — green-gate required before staging. |
| T-66-02-A1 | Tampering (atomic violation) | Splitting the commit across multiple SHAs | mitigate | Plan structure enforces single-task atomic commit per D-01 LOCKED + Phase 62-08 / Phase 65 precedent. Post-commit verification `git log --name-only -1 HEAD` confirms all 4-5 files in one SHA; if not, revert + redo. |
| T-66-02-A2 | Tampering (silent shape regression) | c13_rotting_external JSON shape transition from [] to object cascades V-62-SIDECAR | mitigate | Pre-commit dry-run is the discovery mechanism; Pitfall 1 in RESEARCH.md documents the cascade pattern; Edit 5 (V-62-SIDECAR extension with [EXTENDED Phase 66] marker) lands in the same atomic commit IF dry-run cascades. Phase 65 3->4 file growth precedent at 65-VERIFICATION.md:255-261 is the empirical evidence this works. |
| T-66-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO external packages (atomic harness commit is zero-runtime-deps by design per 62-08-PLAN.md:501 invariant). No slopcheck applicable. |
</threat_model>

<verification>
**Per VALIDATION.md 66-02-01 + 66-02-02 verification targets:**

| Task ID | V-66-XX | Command |
|---------|---------|---------|
| 66-02-01 | V-66-01 | `node scripts/validation/check-phase-66.mjs --verbose 2>&1 \| Select-String -Pattern 'V-66-01' \| Select-String -Pattern 'PASS'` |
| 66-02-01 | V-66-02 | `node scripts/validation/check-phase-66.mjs --verbose 2>&1 \| Select-String -Pattern 'V-66-02' \| Select-String -Pattern 'PASS'` |
| 66-02-01 | V-66-03 | `node scripts/validation/check-phase-66.mjs --verbose 2>&1 \| Select-String -Pattern 'V-66-03' \| Select-String -Pattern 'PASS'` |
| 66-02-01 | V-66-04 | `node scripts/validation/check-phase-66.mjs --verbose 2>&1 \| Select-String -Pattern 'V-66-04' \| Select-String -Pattern 'PASS'` |
| 66-02-01 | harness | `node scripts/validation/v1.6-milestone-audit.mjs` exit 0 with C14/C15/C16 PASS |
| 66-02-01 | --self-test | `node scripts/validation/v1.6-milestone-audit.mjs --self-test` exit 0 |
| 66-02-01 | atomic | `git log --name-only -1 HEAD` lists 4-5 files: v1.6-milestone-audit.mjs / v1.6-audit-allowlist.json / regenerate-supervision-pins.mjs / check-phase-66.mjs (+ check-phase-62.mjs IF Edit 5 applied) |
| 66-02-01 | chain | full suite (check-phase-{62..65}) exit 0 modulo CHAIN_SKIP {48,51,58,60,61}; check-phase-66 exit 1 with EXACTLY 3 RED (V-66-05/06/07) |
</verification>

<success_criteria>
1. ONE git commit (single SHA) at HEAD touches exactly 4 (or 5) files per the atomic-commit contract.
2. `git log --name-only -1 HEAD` lists v1.6-milestone-audit.mjs + v1.6-audit-allowlist.json + regenerate-supervision-pins.mjs + check-phase-66.mjs (+ optionally check-phase-62.mjs if dry-run cascade required Edit 5).
3. v1.6-milestone-audit.mjs windowKeywords at line 577 contains all 6 LOCKED tokens (apple-business-side / intune-side / integration-handshake / owned-by-apple-business / owned-by-intune / scope-boundary).
4. v1.6-milestone-audit.mjs synthetic regex 7 at line 854 contains `.{0,160}` window AND all 6 added exclusion terms (renamed, personal, Apple\\s+Business, scopes, ABM, account).
5. v1.6-audit-allowlist.json c13_rotting_external is a populated object (NOT empty array) with ci_1_abm_urls + ci_2_vpp_location_token + ci_3_managed_apple_id + quarterly_audit{cadence:'0 8 1 1,4,7,10 *'}.
6. regenerate-supervision-pins.mjs contains both substrings "BASELINE_10 refreshed" and "Phase 66" in the new freshness comment block; BASELINE_9 array literal at lines 399-409 unchanged.
7. Post-commit: harness exit 0; harness --self-test exit 0; check-phase-{62,63,64,65} exit 0; check-phase-66 exit 1 with V-66-01..04 + V-66-ABAUDIT-STALENESS all PASS and only V-66-05/06/07 RED.
8. Pre-commit dry-run protocol documented in commit message body; rollback semantics (`git revert <SHA>`) verified by spot-check (do not actually revert; just confirm the commit is a single clean operation).
</success_criteria>

<output>
Create `.planning/phases/66-apple-business-validation-tooling-closure-milestone-audit/66-02-SUMMARY.md` with:
- Atomic commit SHA + `git log --name-only -1 HEAD` output (verbatim) listing 4 or 5 files
- Dry-run outcome: whether Edit 5 (check-phase-62.mjs V-62-SIDECAR extension) was required, and why
- Post-commit assertion state: V-66-01..04 PASS + V-66-ABAUDIT-STALENESS PASS + V-66-CHAIN-* PASS/SKIPPED + V-66-AUDIT PASS + V-66-SELF PASS; V-66-05/06/07 remain RED for Wave 3 + Wave 5
- harness self-test verdict + summary line from `v1.6-milestone-audit.mjs --self-test` output
- Wave 3 handoff: CI workflow (audit-harness-v1.6-integrity.yml) + DEFERRED-CLEANUP.md ready for authoring against the now-populated c13_rotting_external state
</output>
</content>
</invoke>