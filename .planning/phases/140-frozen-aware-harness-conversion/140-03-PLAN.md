---
phase: 140-frozen-aware-harness-conversion
plan: 03
type: execute
wave: 3
depends_on: ["140-02"]
files_modified:
  - scripts/validation/v1.4.1-milestone-audit.mjs
  - scripts/validation/v1.5-milestone-audit.mjs
  - scripts/validation/v1.6-milestone-audit.mjs
  - scripts/validation/v1.7-milestone-audit.mjs
  - scripts/validation/v1.8-milestone-audit.mjs
  - scripts/validation/v1.9-milestone-audit.mjs
  - scripts/validation/v1.10-milestone-audit.mjs
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
autonomous: true
requirements: [SWEEP-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node scripts/validation/v1.5-milestone-audit.mjs` prints `Summary: 12 passed, 0 failed, 0 skipped` and exits 0 — both the C5 and C10 freshness assertions pass reading the corpus at V15, with zero corpus edits."
    - "Every converted harness in this plan derives its file scope by enumerating at its own close SHA and reads every corpus file, and its sidecar, from that same SHA — no read in the converted paths reaches live HEAD."
    - "Each converted harness's sidecar path string literal is byte-identical to its pre-conversion value, so its workflow `path-match` job and any chain validator pinning that literal still match."
    - "Each converted harness reports a freshness result driven by the frozen dates, not by live-HEAD dates: the 60-day-vs-90-day mismatch class no longer appears in any converted harness's output."
    - "For each converted harness the before-state and after-state summary lines are both recorded, so a reviewer can see the transition rather than infer it."
  artifacts:
    - scripts/validation/v1.4.1-milestone-audit.mjs
    - scripts/validation/v1.5-milestone-audit.mjs
    - scripts/validation/v1.6-milestone-audit.mjs
    - scripts/validation/v1.7-milestone-audit.mjs
    - scripts/validation/v1.8-milestone-audit.mjs
    - scripts/validation/v1.9-milestone-audit.mjs
    - scripts/validation/v1.10-milestone-audit.mjs
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
  key_links:
    - "`createFrozenCorpusReader` (Plan 02) -> each converted harness's `FROZEN` constant — the single consumption point for all seven files."
    - "Converted v1.5 harness -> `check-phase-60.mjs`'s `V-60-23`, which spawns it and asserts exit 0 — this is the SWEEP-06 evidence surface measured in Plan 05."
    - "Each `SIDECAR_PATH` literal -> its workflow `path-match` job -> the `harness-run` job that depends on it."
  prohibitions:
    - "No corpus document under docs/ is edited."
    - "No glossary's last_verified or review_by value is edited."
    - "No JSON audit-allowlist sidecar file is edited."
    - "No SIDECAR_PATH string literal is renamed, re-cased or re-quoted."
    - "No malformed-JSON degrade branch in any parseAllowlist is converted to a throw."
    - "No freshness comparison operator or threshold constant in any check body is changed."
    - "No check is removed, reordered, renumbered or converted to informational."
    - "scripts/validation/_lib/frozen-at-close.mjs is not edited in this plan."
    - "c17-eee-contract.mjs is not edited or converted."
    - "v1.19-milestone-audit.mjs is not edited."
    - "check-phase-60.mjs is not edited and its 60000 ms subprocess timeout is not raised."
---

<objective>
Expand the proven conversion out to the Family A sibling, the Family B harness, and the first five
Family C harnesses — seven files, one recipe.

Purpose: v1.5 is the harness `check-phase-60.mjs` actually spawns, so it is the file SWEEP-06's
budget criterion measures and the file Phase 141 RED-01 consumes directly. v1.6-v1.10 are the
first half of the nine-file Family C block that carries the same red C5/C10 pair.

Output: seven converted harnesses, each reading corpus and sidecar at its own close SHA, with
before/after summary lines recorded per file and one GOV-02 ledger row.

**Tracer note:** the phase's tracer is Plan 02 Task 2, already landed and measured. Every task in
this plan is horizontal expansion off that proven slice — the architecture is not re-litigated
here, only applied. If any task in this plan reveals an architectural gap in the reader, stop and
escalate rather than working around it per-harness: a per-harness workaround is how sixteen
divergent copies get born.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-frozen-aware-harness-conversion/140-CONTEXT.md
@.planning/phases/140-frozen-aware-harness-conversion/140-RESEARCH.md
@.planning/phases/140-frozen-aware-harness-conversion/140-PATTERNS.md
@.planning/phases/140-frozen-aware-harness-conversion/140-02-SUMMARY.md
@.planning/milestones/v1.20-GOV-02-LEDGER.md
</context>

## Artifacts this phase produces

| Symbol / artifact | Kind | Home | Plan |
|---|---|---|---|
| `readManyAtClose(milestoneTag, relPaths)` | new named export | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `createFrozenCorpusReader(milestoneTag, { extraPaths })` | new named export | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `readAtV14Close` | new convenience export | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `MILESTONE_CLOSE_SHAS.V14` | new registry key (`0b3be9ab`) | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `VUNPINNED` | replacement self-test probe tag (assertion 3) | `scripts/validation/_lib/frozen-at-close.mjs` | 02 |
| `MILESTONE_TAG` | new module constant, one per harness | each `vX.Y-milestone-audit.mjs` | 02, 03, 04 |
| `SIDECAR_PATH` | new module constant, one per harness | each `vX.Y-milestone-audit.mjs` | 02, 03, 04 |
| `FROZEN` | new module constant (the reader instance), one per harness | each `vX.Y-milestone-audit.mjs` | 02, 03, 04 |

No new files, no new CLI flags, no new dependencies. `readFile`, `walkMd` and `parseAllowlist`
keep their existing names and signatures in all 16 harnesses — only their bodies change.

<tasks>

<task type="auto">
  <name>Task 1: Convert v1.4.1 (Family A) and v1.5 (Family B)</name>
  <files>scripts/validation/v1.4.1-milestone-audit.mjs, scripts/validation/v1.5-milestone-audit.mjs</files>
  <read_first>
    - `.planning/phases/140-frozen-aware-harness-conversion/140-RESEARCH.md` — Finding 2's family table (Family A: `readFile` `:29`, `walkMd` `:36`, `parseAllowlist` `:57`, sidecar `:58`, 1 guard; Family B: `readFile` `:43`, `walkMd` `:50`, `parseAllowlist` `:71`, sidecar `:72`, 2 guards), the four-item edit recipe, and Pitfalls 2 and 4
    - `.planning/phases/140-frozen-aware-harness-conversion/140-PATTERNS.md` — the `walkMd` re-absolutization rule and the sidecar fail-loud-vs-tolerant split
    - `.planning/phases/140-frozen-aware-harness-conversion/140-02-SUMMARY.md` — the exact shape landed in v1.4, which is the template for these two
    - `scripts/validation/v1.4-milestone-audit.mjs` (post-conversion) — the reference implementation
    - `scripts/validation/v1.4.1-milestone-audit.mjs` lines 18-90 and `scripts/validation/v1.5-milestone-audit.mjs` lines 30-105 plus its second guard region — the helper sections being replaced
  </read_first>
  <action>
Apply the identical four-item recipe landed in v1.4 to these two files at their own family
coordinates. Do not re-derive the recipe; copy the shape from the converted v1.4 file.

Per file: add the `createFrozenCorpusReader` import next to the existing `node:fs` import; declare
`MILESTONE_TAG`, `SIDECAR_PATH` and `FROZEN`; replace the `readFile` body; replace the `walkMd`
body with a prefix filter over the enumerated paths re-wrapped with `join(process.cwd(), p)`;
replace every bare `existsSync(join(process.cwd(), p))` membership guard with a membership test
against the reader's enumerated set; and make `parseAllowlist` throw on absence while keeping its
inner JSON parse degrade branch untouched.

Tag mapping: v1.4.1 uses `V141`, v1.5 uses `V15`. Both keys already exist in the registry.

Guard counts differ and are the one place a copy-paste error hides: v1.4.1 has exactly one
membership guard, v1.5 has exactly two. Confirm the count per file with a grep before and after,
and confirm that after conversion the file has zero remaining bare membership guards of that
shape. Family B's second guard sits well below the helper section — do not stop at the first hit.

Record each file's before-state and after-state summary line in the SUMMARY. v1.5's expected
after-state is measured and firm. v1.4.1's is not: no prototype conversion was run for it, so its
post-conversion result is discovered here, not predicted. If v1.4.1 does not go fully green, that
is a finding to record — it is not a licence to edit corpus, adjust a threshold, or mark a check
informational. This phase's requirement is that the harness reads frozen, not that it is green;
greening the v1.5-v1.13 freshness leg is Phase 141 RED-01's job.

Commit the two files together as one atom; they are the same recipe applied twice. Suggested
subject: `refactor(140-03): frozen-aware conversion for v1.4.1 and v1.5 harnesses`.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/v1.5-milestone-audit.mjs` (run without a pipe) prints `Summary: 12 passed, 0 failed, 0 skipped` and exits 0 — up from the recorded baseline of `Summary: 10 passed, 2 failed, 0 skipped` / exit 1.
    - `node scripts/validation/v1.5-milestone-audit.mjs 2>&1 | grep -c '(>60)'` returns `0`.
    - `node scripts/validation/v1.4.1-milestone-audit.mjs` runs to completion and its summary line is recorded verbatim in the SUMMARY alongside its recorded baseline of `Summary: 6 passed, 2 failed, 0 skipped`.
    - `node scripts/validation/v1.4.1-milestone-audit.mjs 2>&1 | grep -c '(>60)'` returns `0`.
    - `grep -c 'existsSync(join(process.cwd()' scripts/validation/v1.4.1-milestone-audit.mjs` returns `0`, and the same command against `scripts/validation/v1.5-milestone-audit.mjs` returns `0` — every membership guard converted, none missed.
    - `grep -c 'createFrozenCorpusReader' scripts/validation/v1.4.1-milestone-audit.mjs` and the same against the v1.5 harness each return `2` (the import plus the single factory call).
    - `git diff HEAD~1 -- scripts/validation/v1.4.1-milestone-audit.mjs scripts/validation/v1.5-milestone-audit.mjs | grep -c "audit-allowlist.json"` returns `0` — no sidecar path literal was rewritten.
    - `node scripts/validation/carve-gate.mjs` exits 0 with zero off-list paths.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/v1.5-milestone-audit.mjs; echo "v15_exit=$?"; node scripts/validation/v1.4.1-milestone-audit.mjs; echo "v141_exit=$?"; grep -c 'existsSync(join(process.cwd()' scripts/validation/v1.4.1-milestone-audit.mjs scripts/validation/v1.5-milestone-audit.mjs; node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>
    Both harnesses read corpus and sidecar frozen; v1.5 reports 12 passed / 0 failed / 0 skipped;
    zero bare membership guards remain in either file; no sidecar path literal changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert v1.6 through v1.10 (Family C, first five)</name>
  <files>scripts/validation/v1.6-milestone-audit.mjs, scripts/validation/v1.7-milestone-audit.mjs, scripts/validation/v1.8-milestone-audit.mjs, scripts/validation/v1.9-milestone-audit.mjs, scripts/validation/v1.10-milestone-audit.mjs</files>
  <read_first>
    - `.planning/phases/140-frozen-aware-harness-conversion/140-RESEARCH.md` — Finding 2's Family C row (`readFile` `:50`, `walkMd` `:57`, `parseAllowlist` `:78`, sidecar `:79`, 4 guards) and its milestone-tag mapping table (`V16` `9d8877c`, `V17` `aa6de68`, `V18` `2bd79d8`, `V19` `b29dca5`, `V110` `a3617e9`)
    - `scripts/validation/v1.5-milestone-audit.mjs` (post-conversion, from Task 1) — the immediate reference implementation
    - `scripts/validation/v1.6-milestone-audit.mjs` lines 40-115 and its guard sites at `:100`, `:113`, `:161`, `:212` — the Family C shape, which RESEARCH.md verified is identical across all nine Family C files
    - `.planning/phases/140-frozen-aware-harness-conversion/140-CONTEXT.md` — D-02 (why the census is per-family, not v1.13-shaped) and D-11 (sidecar/content alignment is a maintained property that must be verified per harness, never assumed)
  </read_first>
  <action>
Apply the same four-item recipe to five Family C files. These five share one shape: four
membership guards each, at the coordinates RESEARCH.md verified file by file.

Per file the only values that differ are `MILESTONE_TAG` and `SIDECAR_PATH`. Take each tag from
RESEARCH.md Finding 2's mapping table; every one already exists in the registry, so no pin work
is required here. Take each sidecar path from the literal already present in that file — read it
out of the file rather than constructing it from the version number, so the string stays
byte-identical to what its workflow `path-match` job greps for.

The guard count is four per file and all four convert. A converted read behind a live-HEAD
membership guard is a hybrid that under-audits silently, which is why partial conversion is worse
than none. Verify the post-conversion count is zero per file, not per plan.

Run each harness after converting it and record both its baseline and its post-conversion summary
line. Per D-11, sidecar-pin-to-frozen-content alignment is a maintained property rather than a
structural one, so it is verified per harness here and not assumed from a sibling's result. If a
harness's sidecar-driven check moves in an unexpected direction, record the observation with its
numbers — do not repair a sidecar, and do not edit corpus.

Commit the five files as one atom. Suggested subject:
`refactor(140-03): frozen-aware conversion for v1.6-v1.10 harnesses`.
  </action>
  <acceptance_criteria>
    - For each of the five files, `grep -c 'existsSync(join(process.cwd()' <file>` returns `0`.
    - For each of the five files, `grep -c 'createFrozenCorpusReader' <file>` returns `2`.
    - For each of the five harnesses, `node <harness> 2>&1 | grep -c '(>60)'` returns `0`.
    - Each of the five harnesses runs to completion without an unhandled throw, and both its baseline and post-conversion summary lines are recorded verbatim in the SUMMARY.
    - `git diff HEAD~1 -- scripts/validation/v1.6-milestone-audit.mjs scripts/validation/v1.7-milestone-audit.mjs scripts/validation/v1.8-milestone-audit.mjs scripts/validation/v1.9-milestone-audit.mjs scripts/validation/v1.10-milestone-audit.mjs | grep -c "audit-allowlist.json"` returns `0`.
    - `node scripts/validation/carve-gate.mjs` exits 0 with zero off-list paths.
  </acceptance_criteria>
  <verify>
    <automated>for h in 1.6 1.7 1.8 1.9 1.10; do echo "=== v$h ==="; node scripts/validation/v$h-milestone-audit.mjs; echo "exit=$?"; grep -c 'existsSync(join(process.cwd()' scripts/validation/v$h-milestone-audit.mjs; done; node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>
    All five Family C harnesses read corpus and sidecar frozen with zero remaining bare
    membership guards; every summary line recorded; no sidecar path literal changed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Blast-radius gate and GOV-02 ledger row for the seven conversions</name>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` — the row schema and the Phase 140 rows appended by Plans 01 and 02
    - `.planning/phases/140-frozen-aware-harness-conversion/140-01-SUMMARY.md` — the recorded pre-edit baseline command set and figures
    - `.planning/phases/140-frozen-aware-harness-conversion/140-CONTEXT.md` — D-28's hazard class (harness source text is pinned, pass counts are not)
  </read_first>
  <action>
Re-run the recorded gate command set and append one ledger row covering all seven converted
paths.

Gate: top-level `check-phase-138.mjs`; `CHECK_PHASE_NESTED=1` runs of the six pin-holding
validators from the Plan 01 census; `carve-gate.mjs`. Compare each result against the recorded
pre-edit figures. The two validators that pin harness source text by substring are the ones this
plan can actually perturb — confirm both by validator exit, not by reading the text.

Append one row using the six-column schema, naming all seven modified paths in the File column,
citing the Plan 01 census rather than re-pasting it, and recording per-harness before/after
summary lines in the Result column. Row-per-edit, appended at the end, no existing row touched.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-138.mjs` (top level, never pre-nested) reports `93 PASS, 0 FAIL, 0 SKIPPED`, matching the recorded baseline.
    - `CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-48.mjs`, and the same for `check-phase-58`, `-66`, `-70`, `-73`, `-120`, each exit 0 with the tally recorded in the row.
    - `node scripts/validation/carve-gate.mjs` exits 0 with zero off-list paths.
    - `git diff HEAD~1 --numstat -- .planning/milestones/v1.20-GOV-02-LEDGER.md` shows insertions and zero deletions.
    - The appended row names all seven converted harness paths and carries a before/after summary line for each.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-138.mjs; for n in 48 58 66 70 73 120; do echo "=== $n ==="; CHECK_PHASE_NESTED=1 node scripts/validation/check-phase-$n.mjs; echo "exit=$?"; done; node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>
    One appended ledger row carries the gate results and all seven before/after transitions; the
    apex, all six pin-holders and the CARVE gate are unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git object store -> harness process | Seven harnesses begin reading their assertion inputs from pinned git objects rather than the working tree. |
| harness source text -> CI `path-match` jobs and chain validators | Several validators and all fifteen workflow gate jobs assert on substrings of these files; an edit that moves or rewrites a pinned literal silently disables a gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-140-09 | Tampering | `SIDECAR_PATH` literal in each converted harness | high | mitigate | The literal is read out of the file rather than reconstructed, and a negative diff grep asserts no `audit-allowlist.json` string was rewritten in any converted file. Each workflow's `harness-run` job depends on the `path-match` job that greps it. |
| T-140-10 | Repudiation | partial conversion (a live-HEAD guard left in front of a frozen read) | high | mitigate | Per-file post-conversion guard count asserted to be zero, per file rather than per plan; a hybrid harness reports fewer violations than either pure mode and looks green for the wrong reason. |
| T-140-11 | Tampering | assertion semantics drifting during a mechanical edit | medium | mitigate | Prohibitions bar changing any freshness operator, threshold, check ordering or informational flag; the diff is confined to the helper section, upstream of every check body. |
| T-140-12 | Denial of service | seven additional `git` subprocess pairs per full sweep | low | accept | Enumeration plus one batch per harness process; measured at roughly a sixth of a second per harness against a sixty-second budget. Measured explicitly in Plan 05. |
| T-140-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs in this plan; no new dependency is added. No supply-chain surface exists to gate. |

ASVS level 1; blocking threshold `high`. Both `high` code threats are mitigated in-plan;
T-140-SC is accepted on a measured absence of installs.
</threat_model>

<verification>
- `node scripts/validation/v1.5-milestone-audit.mjs` exits 0 with `Summary: 12 passed, 0 failed, 0 skipped`.
- Every converted harness returns zero hits for the 60-day-vs-90-day mismatch string.
- Every converted harness has zero remaining bare membership guards.
- No converted file's sidecar path literal changed.
- Apex reports 93 PASS / 0 FAIL / 0 SKIPPED; all six nested pin-holders exit 0; `carve-gate.mjs`
  exits 0.
- One GOV-02 ledger row appended, zero lines deleted.
</verification>

<success_criteria>
- Seven harnesses (v1.4.1, v1.5, v1.6, v1.7, v1.8, v1.9, v1.10) read corpus and sidecar at their
  own close SHAs.
- v1.5 is fully green, which is the input `check-phase-60.mjs`'s `V-60-23` spawns and Phase 141
  RED-01 consumes.
- Before/after summary lines recorded for all seven.
- Zero corpus edits, zero sidecar edits, zero threshold edits.
</success_criteria>

<output>
Create `.planning/phases/140-frozen-aware-harness-conversion/140-03-SUMMARY.md` when done
</output>
