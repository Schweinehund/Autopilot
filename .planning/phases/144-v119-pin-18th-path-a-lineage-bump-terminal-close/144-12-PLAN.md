---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 12
type: execute
wave: 10
depends_on: ["144-11"]
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md
autonomous: true
requirements: [HARN-17, HARN-18, HARN-19]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Terminal ordering is fixed and the publish bundle moves BEFORE the close-gate: the roadmap's fourth success criterion fuses the bundle and the flip into ONE criterion with an and, and the bundle exits 1 on any conversion, guard, parity, naming or divergence failure across 225 conversions — under the predecessor's ordering that failure would land after an irreversible 28-requirement flip (D-29)."
    - "The bundle is invoked with an explicit version argument. Its version option defaults to a hardcoded predecessor value and the zip-name derivation accepts it silently, so an unflagged run produces a valid, silently wrong archive at exit 0 as the last act of the close (D-29)."
    - "A SINGLE close-gate commit flips all 28 v1.20 requirements to Validated across the project, roadmap, state and requirements documents (D-24). The FLIP is atomic, not the phase — a later non-gate commit recording the post-gate confirmatory result is permitted and expected."
    - "The requirements traceability cells are corrected in the same gate commit: the frozen-aware-conversion requirement's row spans two phases, and the silent-swallow requirement's row spans two phases (D-04/D-25)."
    - "The frozen-aware-conversion requirement flips on its own amended text; the timing requirement flips only on the measurement recorded in Plan 02, because its authority is a plan summary rather than an inline amendment and the seventeenth harness had never been measured (D-25)."
    - "Both accepted-red dispositions are DELETED from the backlog rather than carried a seventh milestone, which is the milestone bar stated in the requirements document (HARN-19)."
    - "The requirements document is NEVER removed at close — one chain validator live-reads both it and the roadmap, with a negative assertion, and the gate rewrites both (D-30 Class 2)."
    - "Class-2 archival drift can only manifest AFTER the gate rewrites those two documents, so it is checked by the post-gate confirmatory run, not by any earlier scan (D-30)."
    - "The post-gate confirmatory apex run asserts the archived-phase audit check is PASS rather than SKIP — but that proves only that the gate did not break the audit read; it proves NOTHING about the archive-root token, because the resolver checks the live path first (D-29/D-15)."
    - "The apex chain is run SEPARATELY from any verifier or prose-guard pass: a link or anchor change is a prose change and has tripped banned-phrase guards after a passed verifier (D-35)."
    - "The expected post-gate triple is `101 PASS, 0 FAIL, 0 SKIPPED` once the phase verification artifact exists — 1 audit plus 96 span members plus 2 sidecar members plus 1 audit-harness plus 1 self (D-10)."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/PROJECT.md
  key_links:
    - "The close-gate commit -> `check-phase-54.mjs`, the sole LIVE reader of both the requirements and roadmap documents, with a negative assertion; the post-gate run is what proves the rewrite did not break it."
    - "The bundle's explicit version argument -> the produced archive name; the default would name the wrong milestone at exit 0."
    - "The two accepted-red deletions -> the milestone bar in the requirements document."
  prohibitions:
    - statement: "No corpus document, glossary metadata field, assertion threshold or validator is edited to make anything pass at the close. A failing check at this point is a finding that stops the close, never a goalpost to move."
      status: flagged-unverified
      verification: "`git diff <shared SHA>..HEAD -- docs scripts .github` is empty across this plan's commits."
    - statement: "Neither glossary's last-verified or review-by field is touched (D-34): 145 documents sit at exactly 90 days against a strictly-greater-than test and the arithmetic has no wall-clock term, so any edit to either field flips the difference."
      status: flagged-unverified
      verification: "`git diff <shared SHA>..HEAD -- docs` is empty."
    - statement: "The requirements document is not removed, renamed, or emptied at close."
      status: flagged-unverified
      verification: "The file exists after the gate commit and `node scripts/validation/check-phase-54.mjs` exits 0."
    - statement: "The commit message body does not literally quote the carve document's path or any amended file path in prose — the combined message-and-file-list output is what the exclusion check greps (D-33)."
      status: flagged-unverified
      verification: "`git show --name-only --format=%B HEAD` contains each path string exactly once."
---

<objective>
Close the milestone: regenerate the publish bundle at the correct version, land the single close-gate
commit flipping all 28 requirements to Validated, then run the apex once more and record the result.

Purpose: the terminal ordering is deliberate and owner-ratified. The bundle runs BEFORE the gate
because a bundle failure after an irreversible 28-requirement flip is unrecoverable within this phase,
and because the roadmap's own success criterion fuses the two into one condition. The gate is a single
commit because a partial flip leaves the project's four planning documents mutually inconsistent. The
post-gate run exists because the gate rewrites the two documents a chain validator live-reads.

Output: one versioned bundle, one close-gate commit, one post-gate confirmatory record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the publish bundle with an explicit version argument</name>
  <files>.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md</files>
  <read_first>
    - `scripts/pipeline/build-publish-bundle.mjs` — `:23-25` the exit-1 conditions across conversion, guard, parity, naming and divergence; `:40` the version option and its hardcoded default; the zip-name derivation; `:520-523` the approved-row canary
    - `scripts/pipeline/build-filename-map.mjs:282-283` — the SECOND approved-row canary, which sat red for a whole milestone once; both canaries expect the same count
    - `.claude/hooks/publish-bundle-gate.cjs` — the stop-hook the regeneration runs under
    - `144-CONTEXT.md` — D-29 and the specifics entry recording both canaries green at 225 approved rows with both self-tests passing
  </read_first>
  <action>
Run the bundle build with the version argument set explicitly to this milestone. The option's default
is a hardcoded earlier milestone string and the zip-name derivation accepts it without complaint, so
an unflagged run produces a valid archive named for the wrong milestone and exits 0 — a silent
failure at the last act of the close.

Before the run, confirm both approved-row canaries agree with the live registry count. They are two
separate canaries in two separate scripts; one of them sat red for an entire milestone because only
the other was bumped. The measured state is both green at 225, so a disagreement is a finding.

Run the build. It exits 1 on any conversion, guard, parity, naming or divergence failure across the
full conversion set; a non-zero exit stops the close here, before the gate, which is the whole reason
this task precedes it.

Record in `144-EVIDENCE.md`: the exact command including the version argument, the produced archive
name, the conversion count, both canary values, both self-test results, and the exit code. The archive
itself is a build output under an ignored directory and is not committed.
  </action>
  <acceptance_criteria>
    - The build command recorded in the evidence contains an explicit version argument naming this milestone.
    - The produced archive filename names this milestone, verified by listing the output directory.
    - No archive named for an earlier milestone is produced by this run.
    - `node scripts/pipeline/build-publish-bundle.mjs --self-test; echo $?` prints `0` and `node scripts/pipeline/build-filename-map.mjs --self-test; echo $?` prints `0`.
    - Both approved-row canary values equal the live registry's approved-row count.
    - The build exits 0; the exit code is recorded.
    - `git status --porcelain -- docs scripts` prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/pipeline/build-publish-bundle.mjs --self-test</automated>
  </verify>
  <done>A correctly-named bundle exists, both canaries agree with the registry, and the build exited 0 before any requirement was flipped.</done>
</task>

<task type="auto">
  <name>Task 2: The SINGLE close-gate commit — 28 requirements to Validated</name>
  <reversibility rating="one-way">One-way (D-29): the gate flips 28 requirements and rewrites the two documents a chain validator live-reads; the decision and its ordering are owner-ratified in the phase context, and the evidence behind it was human-approved at Plan 10's checkpoint.</reversibility>
  <precondition>Plan 10's evidence checkpoint was approved and the bundle from Task 1 exited 0 with a correctly-named archive.</precondition>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — all 28 requirement rows, the traceability table including the two cells needing correction, the milestone bar statement, and the durable notes at the foot
    - `.planning/ROADMAP.md` — the phase 139 through 144 sections and their checkboxes, and the milestone-level progress section
    - `.planning/STATE.md` — the active-phase pointer, the durable watch items including the two accepted-red dispositions, and the decision log
    - `.planning/PROJECT.md` — the milestone status block
    - `scripts/validation/check-phase-54.mjs:25-40` and `:340-355` — the live reads of the requirements and roadmap documents and the negative assertion at the tail, which the gate must not break
    - `.planning/milestones/v1.19-MILESTONE-AUDIT.md` — the predecessor's own close-gate commit shape, for the flip's wording conventions
    - `144-CONTEXT.md` — D-04, D-24, D-25, D-29, D-30, D-33, D-34
  </read_first>
  <action>
One commit, four documents, no partial state.

Flip all 28 requirements to Validated. The frozen-aware-conversion requirement flips on its own
amended text, which Plan 02 landed. The timing requirement flips ONLY on the measurement Plan 02
recorded, because its authority is a plan summary rather than an inline amendment and the seventeenth
harness had never been measured before this phase — cite the measurement in the update note.

Correct the two traceability cells in the same commit: the frozen-aware-conversion requirement's row
and the silent-swallow requirement's row each span two phases and currently name one.

Delete both accepted-red dispositions from the backlog surfaces rather than carrying them a seventh
milestone — that is the milestone bar's own wording. Their discharge evidence and their
dropped-and-closed entries live in the close artifact from Plan 11; the deletion here is the removal
of the carried watch items from the state document, not the erasure of the record.

Update the roadmap's phase checkboxes and milestone progress, the state document's active-phase
pointer and watch list, and the project document's milestone status.

Do NOT remove, rename or empty the requirements document. One chain validator live-reads it and the
roadmap with a negative assertion; deleting it would break the next apex.

Touch no corpus document, no glossary metadata field, no validator, no workflow. If any check fails
at this point, that is a finding that stops the close — moving a goalpost to green it is the exact
class of remedy this milestone's bar exists to eliminate.

Commit once. Subject form `docs(144-12): …`, naming the milestone close. Per D-33 the message body
must not literally quote the carve document's path or any amended file path in prose.
  </action>
  <acceptance_criteria>
    - `git show --name-only --format= HEAD` lists exactly the four planning documents.
    - `grep -c 'Validated' .planning/REQUIREMENTS.md` accounts for all 28 requirement rows, and no v1.20 requirement row remains Pending or Complete-but-unvalidated.
    - The two traceability cells each name two phases.
    - `node scripts/validation/check-phase-54.mjs; echo $?` prints `0` after the commit.
    - `.planning/REQUIREMENTS.md` exists and is non-empty.
    - `git diff HEAD~1 -- docs scripts .github` is empty.
    - `git show --name-only --format=%B HEAD` contains each amended path string exactly once.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs</automated>
  </verify>
  <done>One commit flips all 28 requirements to Validated across four documents, corrects both traceability cells, removes both accepted-red watch items, and leaves the live-read documents intact.</done>
</task>

<task type="auto">
  <name>Task 3: Post-close-gate confirmatory apex run and Class-2 drift check</name>
  <files>.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md</files>
  <read_first>
    - `scripts/validation/check-phase-144.mjs` — the audit check and its resolver call
    - `scripts/validation/check-phase-54.mjs` — the live reader whose negative assertion the gate could have broken
    - `144-CONTEXT.md` — D-10's expected triples, D-15 on what the audit check does and does not prove, D-29's post-gate requirement, D-35 on running the chain separately
  </read_first>
  <action>
Run the apex once more, after the gate, in its own pass — separately from any verifier or prose-guard
run, because a link or anchor change is a prose change and has tripped banned-phrase guards after a
passed verifier.

Assert the archived-phase audit check is PASS rather than SKIP, now that the phase verification
artifact exists, and that the total is the full triple with no skips. Record honestly what this
proves and what it does not: it proves the gate did not break the audit read; it proves NOTHING about
the archive-root token, because the resolver checks the live path first and would succeed against a
wrong token exactly as a predecessor validator has done, permanently green, for two milestones. The
token itself is covered by the literal string assertion Plan 07 put in the apex source.

Class-2 archival drift is checked here and only here: it can manifest only after the gate rewrote the
two live-read documents. Run the live reader and confirm exit 0.

Record the post-gate triple, both exit codes, and the honest scope statement in `144-EVIDENCE.md`, and
commit that record as a separate, non-gate commit — the flip is atomic, the phase is not.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/check-phase-144.mjs` exits 0 and prints `101 PASS, 0 FAIL, 0 SKIPPED`.
    - The audit check's own line reports PASS, not SKIPPED, and its detail string is quoted in the evidence.
    - `node scripts/validation/check-phase-54.mjs; echo $?` prints `0`.
    - `144-EVIDENCE.md` records the post-gate triple, both exit codes, and an explicit statement that the audit check does not prove archive-root-token correctness.
    - The record lands as its own commit, separate from the gate commit.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-144.mjs</automated>
  </verify>
  <done>The apex is 101/0/0 after the gate, the live reader still exits 0, and the record states honestly what the confirmatory run does and does not prove.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script arguments → produced artifact | An omitted version argument silently produces a correctly-formed archive naming the wrong milestone, at exit 0. |
| close-gate commit → live-reading validator | The gate rewrites the two documents a chain validator reads at HEAD, with a negative assertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-47 | Spoofing | the published archive's name | high | mitigate | Explicit version argument required, the produced filename asserted against this milestone, and the absence of an earlier-named archive asserted |
| T-144-48 | Tampering | the 28-requirement flip | high | mitigate | Single commit over exactly four planning documents, asserted by the changed-file list; the bundle runs first so a bundle failure cannot land after the flip |
| T-144-49 | Denial of Service | the live-reading chain validator | high | mitigate | Post-gate confirmatory run of both the apex and the live reader; the requirements document is never removed |
| T-144-50 | Repudiation | overclaiming what the post-gate run proves | medium | mitigate | The evidence record states explicitly that the audit check cannot detect a wrong archive-root token, with the permanently-green counterexample cited |
| T-144-51 | Tampering | goalpost-moving at the close | high | mitigate | A prohibition plus an empty-diff assertion over the corpus, validators and workflows across this plan's commits |
</threat_model>

<verification>
- The bundle is named for this milestone, both canaries agree with the registry, both self-tests exit 0, and the build exited 0 before the gate.
- One gate commit over exactly four planning documents; 28 requirements Validated; both traceability cells corrected; both accepted-red watch items removed.
- The live-reading chain validator exits 0 after the gate; the requirements document still exists.
- The post-gate apex prints `101 PASS, 0 FAIL, 0 SKIPPED` with the audit check PASS, recorded in a separate non-gate commit.
- No corpus, validator or workflow byte changed in this plan.
</verification>

<success_criteria>
Milestone v1.20 is closed: the bundle is correct and versioned, all 28 requirements are Validated in a
single commit, both accepted-red dispositions are gone from the backlog rather than carried, and the
apex is green after the gate.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files (this plan): `dist/docs-library-v1.20.zip` (build output, gitignored).
New files (phase-wide): `check-phase-139.mjs` … `-144.mjs`, `v1.20-milestone-audit.mjs`,
`v1.20-audit-allowlist.json`, `audit-harness-v1.20-integrity.yml`, `v1.20-MILESTONE-AUDIT.md`,
`144-EVIDENCE.md`.
New exports: `readAtV119Close`, `lsTreeAtV119Close`.
New constants: `MILESTONE_CLOSE_SHAS.V119`, `BASELINE_24`, and the apex's span and sidecar constants.
New CI job keys: `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`,
`check-phase-144`, `check-phase-139` … `check-phase-143`, `rotting-external-quarterly`,
`pin-helper-advisory`, `frozen-read-probe`.
Modified across the phase: `_lib/frozen-at-close.mjs`, `v1.19-milestone-audit.mjs`,
`check-phase-67.mjs`, `regenerate-supervision-pins.mjs`, `audit-harness-v1.7-integrity.yml`,
`audit-harness-v1.8-integrity.yml`, `v1.20-CARVE.md`, `v1.20-GOV-02-LEDGER.md`,
`v1.20-DEFERRED-CLEANUP.md`, `.planning/{PROJECT,ROADMAP,STATE,REQUIREMENTS}.md`.
</artifacts_produced>

<output>
Create `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-12-SUMMARY.md` when done
</output>
