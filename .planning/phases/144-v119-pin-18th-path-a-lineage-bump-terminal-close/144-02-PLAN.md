---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 02
type: execute
wave: 2
depends_on: ["144-01"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
  - scripts/validation/_lib/frozen-at-close.mjs
  - scripts/validation/v1.19-milestone-audit.mjs
  - .planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md
autonomous: true
requirements: [HARN-17, SWEEP-05, SWEEP-06]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`_lib/frozen-at-close.mjs` `MILESTONE_CLOSE_SHAS` gains a `V119` entry storing the ABBREVIATED form `a7bda73e` (full SHA `a7bda73e23efc5e3f9607c3fef37abf8ec4030aa`), inserted after `V118` and before `V14`, with `V14` still last (HARN-17)."
    - "The abbreviated form is load-bearing, not style: `frozenCause()`'s six-pattern stderr taxonomy works only because every entry V15..V118 is a 7-8 character abbreviated SHA; a 40-char pin changes git's stderr wording for the unreachable-sha case."
    - "`readAtV119Close` and `lsTreeAtV119Close` are exported, one line each, matching the predecessor per-tag shape exactly (HARN-17)."
    - "The edit to `_lib/frozen-at-close.mjs` is APPEND-ONLY except for the ONE carved exception at `:10-13` — the stale description of `check-phase-61` as keeping a genuinely inline reader, corrected per `141-03-SUMMARY.md:223-231` (D-31)."
    - "`v1.19-milestone-audit.mjs` reads its corpus through `createFrozenCorpusReader('V119', { extraPaths: [<its own sidecar>] })`, making it the SEVENTEENTH application of the pattern and the last unconverted harness (D-01)."
    - "The v1.19 C17 leg stays on live HEAD — exactly one `existsSync(join(process.cwd()` call survives in the converted harness's check bodies, and it is the C17 contract guard, carrying the v1.18 exception comment verbatim (D-02)."
    - "The C17 extension is recorded as a `[SUCCESS-CRITERION AMENDMENT, D-02]` marker on SWEEP-05 in REQUIREMENTS.md with the superseded text preserved verbatim — never as a sentence in the harness header (D-02)."
    - "`node scripts/validation/v1.19-milestone-audit.mjs` exits 0 both before and after the conversion, and its registered check count is unchanged at 16 (ids 1-7 and 9-17; C8 was never defined)."
    - "The converted v1.19 harness completes inside `check-phase-60.mjs`'s 60-second subprocess budget, measured in milliseconds and recorded — Phase 140 measured sixteen harnesses (slowest 4,177 ms) and never the seventeenth (D-25/SWEEP-06)."
    - "A GOV-02 target-scoped grep census (path string AND symbol, `grep -a` where the target is NUL-classified) is recorded as a ledger row BEFORE either frozen-surface edit."
  artifacts:
    - scripts/validation/_lib/frozen-at-close.mjs
    - scripts/validation/v1.19-milestone-audit.mjs
    - .planning/REQUIREMENTS.md
    - .planning/milestones/v1.20-GOV-02-LEDGER.md
  key_links:
    - "`MILESTONE_CLOSE_SHAS.V119` -> `readAtClose`/`createFrozenCorpusReader` -> `v1.19-milestone-audit.mjs`'s C1-C16 corpus reads (the pin is useless without a consumer; the consumer is impossible without the pin — D-01's mechanical dependency)."
    - "`v1.19-milestone-audit.mjs` -> `check-phase-138.mjs`'s `V-138-AUDIT-HARNESS` spawn -> apex-138's 95/0/0 result (a conversion regression takes the predecessor apex red)."
    - "REQUIREMENTS.md SWEEP-05 amended text -> Phase 144's close-gate flip (D-25: SWEEP-05 flips on its own amended text)."
  prohibitions:
    - statement: "No entry already present in `MILESTONE_CLOSE_SHAS` is edited, reordered or removed, and no existing export line is rewritten — the module is append-only apart from the single carved comment correction."
      status: flagged-unverified
      verification: "`git diff HEAD~1 -- scripts/validation/_lib/frozen-at-close.mjs` shows deletions only inside the `:10-13` comment block."
    - statement: "No check is deleted, renumbered, or weakened while converting the v1.19 harness; conversion changes how bytes are fetched, never what is asserted."
      status: flagged-unverified
      verification: "The harness's registered check count is 16 before and after, and every check id present before is present after."
---

<objective>
The phase tracer. Land the V119 back-anchor pin and, in the same plan, the v1.19 harness's
frozen-aware conversion — the one mechanically-inseparable pair in this phase (D-01: SWEEP-05's own
amended text places the v1.19 conversion here "in the same plan as HARN-17's V119 pin, because
`MILESTONE_CLOSE_SHAS` carries no V119 entry today").

Purpose: this is the thinnest slice that proves the whole frozen-read architecture end-to-end at a
new tag — pin, library export, batched corpus reader, harness, exit code — before any of the five
leaves, the new harness, the apex or the 17th workflow are built on top of it. `[MEASURED]`
`v1.19-milestone-audit.mjs` is the SOLE unconverted harness; 16 of 17 already import
`_lib/frozen-at-close.mjs`. If the pin or the reader is wrong, it surfaces here after one commit,
not after ten.

Output: the V119 entry + two convenience exports + the routed comment correction, the converted
v1.19 harness, the SWEEP-05 success-criterion amendment recording the C17 live-HEAD residue, one
GOV-02 ledger row, and the SWEEP-06 timing measurement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-PATTERNS.md
@.planning/milestones/v1.20-GOV-02-LEDGER.md
</context>

<flagged_assumptions>
**Unresolved edge probe (HARN-17, category `unclassified`).** The deterministic edge probe classified
HARN-17 as `unclassified` and it stays `unresolved` — it is surfaced here as a flagged planner
assumption rather than auto-backstopped into `must_haves`.

The assumption: a milestone-close SHA pin has no meaningful boundary/ordering/emptiness axis to
probe — it is a single content-addressed constant whose only failure mode is being the WRONG commit,
which is why the recovery method is a subject-line pair discriminator with `count = 1` rather than a
range or an ordering rule. The pin is verified by identity (the recorded 40-char SHA resolves, is an
ancestor of `master`, and its subject names the v1.19 close), not by an edge case. If that framing is
wrong, the missing coverage is whatever axis a pin does have — surface it at verification rather than
letting it pass silently.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: SWEEP-05 success-criterion amendment + the GOV-02 census row, before any code edit</name>
  <files>.planning/REQUIREMENTS.md, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — SWEEP-05 (`:20`) and its existing `[SUCCESS-CRITERION AMENDMENT, D-13/D-14]` marker, SWEEP-06 (`:21`), the traceability rows `:131`/`:132`
    - `.planning/ROADMAP.md:147-150` — the live in-repo example of the `**[SUCCESS-CRITERION AMENDMENT, D-NN]**` instrument and of superseded-text preservation
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` — the row schema and the batching precedent (one row may cover several targets edited in the same plan)
    - `.planning/milestones/v1.20-CARVE.md` — the `## GOV-02 grep procedure (D-12)` section: the census must be TARGET-SCOPED (the file path string) as well as symbol-scoped
    - `144-CONTEXT.md` — D-02 (the instrument and the corrected 5-of-5 residue figure), D-31, D-33
  </read_first>
  <action>
Two artifacts, one commit, before any `scripts/` byte changes.

First, the SWEEP-05 amendment. SWEEP-05's named C17 limitation is textually scoped "in v1.15 through
v1.18"; v1.19 is not in that range, so extending the carve-out to the v1.19 harness is a change to
ratified requirement text and needs this repo's own instrument. Append a
`**[SUCCESS-CRITERION AMENDMENT, D-02]**` marker to SWEEP-05 stating that the v1.19 harness's C17
contract-presence guard and the contract subprocess it gates also stay on live HEAD, so the residue
is 5 of the 5 C17-bearing harnesses (v1.15 through v1.19) — not 5 of 17, since only those five carry
a C17 check at all. Annotate-and-supersede: the original sentence stays byte-unchanged and the
amendment is appended after it.

Second, the GOV-02 census. Grep for BOTH forms — the symbol AND the file path string — for each of
the two frozen surfaces this plan edits (`_lib/frozen-at-close.mjs` and the v1.19 harness) across
`scripts/`, `.github/` and `package.json`, and record what pins them. Use `grep -a` for any target
that is NUL-classified (`carve-gate.mjs` is, from its NUL-delimited glob placeholder) or the census
silently reports zero hits. Record the census as one appended ledger row following the existing row
schema, with a concrete Result column naming every pinning call site found and the planned diff
shape. Append rows only; never edit or reorder an existing row.

Commit both together. Per D-33 do not quote either edited document's path in the message body.
  </action>
  <acceptance_criteria>
    - `grep -c 'SUCCESS-CRITERION AMENDMENT, D-02' .planning/REQUIREMENTS.md` resolves to `1`.
    - The pre-amendment SWEEP-05 sentence is still present verbatim (a `git diff` of the SWEEP-05 line shows an appended clause, not a rewritten line).
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` gains exactly one row; `git diff HEAD~1 --numstat` shows a deletions count of `0` for that file.
    - The ledger row names, for each of the two targets, both the symbol-scoped and the path-scoped grep and their hit counts.
    - `node scripts/validation/carve-gate.mjs` exits 0 (this commit touches no in-scope path).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-54.mjs</automated>
  </verify>
  <done>SWEEP-05 carries the D-02 amendment with superseded text preserved, the census row is in the ledger, and no `scripts/` byte has changed yet.</done>
</task>

<task type="tracer">
  <name>Task 2: TRACER — V119 pin + exports + the v1.19 harness conversion, proven end-to-end</name>
  <precondition>The GOV-02 census row from Task 1 is committed and `git log -1 --format=%s` names it — D-03/D-12 require the census to land before any frozen-surface edit.</precondition>
  <files>scripts/validation/_lib/frozen-at-close.mjs, scripts/validation/v1.19-milestone-audit.mjs</files>
  <read_first>
    - `scripts/validation/_lib/frozen-at-close.mjs` — the `:10-13` header comment block (the carved exception), `MILESTONE_CLOSE_SHAS` with `V118` at the tail and `V14` deliberately last, `frozenCause()`'s abbreviated-SHA note, `readAtClose`, `lsTreeAtClose`, `readManyAtClose`, `createFrozenCorpusReader`, and the two convenience-export families
    - `scripts/validation/v1.18-milestone-audit.mjs` — the ALREADY-CONVERTED exemplar and literal template: its `MILESTONE_TAG` / `SIDECAR_PATH` / `FROZEN` declarations, its `readFile`, `walkMd` and `parseAllowlist` bodies, and the C17 exception comment block it carries
    - `scripts/validation/v1.19-milestone-audit.mjs` — the conversion target in full: its current `readFile`/`walkMd`/`parseAllowlist`, every `existsSync(join(process.cwd()` call site, the C17 fold, C5 and C10's 90-day static frontmatter arithmetic
    - `.planning/phases/141-standalone-red-validator-set-chain-members-green/141-03-SUMMARY.md:223-231` — the exact stale claim the `:10-13` comment correction must replace
    - `144-PATTERNS.md` — Pattern 3 (the conversion diff, both sides live-verified) and Pattern 6 (the insertion point and export shape)
  </read_first>
  <action>
One vertical slice through every layer the frozen-read architecture has, wired end-to-end and
committed for keeps. Two files, one commit.

Layer 1 — the pin. Insert a `V119` entry into `MILESTONE_CLOSE_SHAS` immediately after `V118` and
before `V14`, storing the ABBREVIATED form of the v1.19 close SHA. `V14` stays last: it is an
AUDIT-CLOSE pin, not a milestone-close-gate pin, per its own comment. The full SHA and its recovery
provenance (subject-line pair discriminator, count = 1) go in the entry's comment, not in the value —
every entry V15 through V118 stores 7-8 characters and `frozenCause()`'s stderr taxonomy depends on
it. Do NOT re-derive the SHA: `144-CONTEXT.md`'s specifics block carries it as a measured fact.

Layer 2 — the exports. Append `readAtV119Close` and `lsTreeAtV119Close`, one line each, at the same
insertion points the `V118` pair occupies in their respective families. Nothing else in this module
changes except the one carved exception: correct the `:10-13` comment's stale description of
`check-phase-61` (it no longer keeps a genuinely inline reader — Phase 141 Task 1 delegated it) to
match what the code does today.

Layer 3 — the consumer. Convert `v1.19-milestone-audit.mjs` using the v1.18 harness as the literal
template: import `createFrozenCorpusReader`, declare the milestone tag and the harness's own sidecar
path as literal constants (a path literal, so the GOV-02 census in Task 1 already covers it), build
the frozen reader with the sidecar passed as an extra path, then replace the three seam functions —
`readFile` returns the reader's value with `undefined` normalised to `null` so every existing caller
still reads "missing"; `walkMd` filters the reader's enumerated paths by directory prefix instead of
walking the live tree; `parseAllowlist` throws fail-loud when the sidecar is absent at the frozen SHA
instead of degrading to empty arrays. Every former live-existence check inside a check body becomes
the reader's `has`.

The ONE exception: the C17 leg stays on live HEAD. Copy the v1.18 harness's C17 exception comment
block verbatim above the surviving guard and its subprocess spawn. After conversion exactly one
`existsSync(join(process.cwd()` call must remain in the check bodies, and it must be that guard.

Do not touch C5 or C10's date arithmetic, and do not touch any corpus file: both walk the full
Android and Linux document sets, 145 documents sit at exactly 90 days against a `> 90` static test,
and the arithmetic has no wall-clock term — any edit to a `last_verified` or `review_by` field flips
the difference (D-34).

Commit the two files together. Subject form `feat(144-02): …`; per D-33 name the artifacts in words,
not by path.
  </action>
  <acceptance_criteria>
    - `node -e "import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>{const s=m.MILESTONE_CLOSE_SHAS.V119; if(!s||s.length>8) process.exit(1); console.log(s)})"` prints the abbreviated SHA and exits 0.
    - `node -e "import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>{const c=m.readAtV119Close('.planning/REQUIREMENTS.md'); if(!c||c.length===0) process.exit(1); console.log('bytes',c.length)})"` prints a non-zero byte count and exits 0 — a real frozen read at the new tag, end to end.
    - `node -e "import('./scripts/validation/_lib/frozen-at-close.mjs').then(m=>console.log(m.lsTreeAtV119Close('docs').length))"` prints a non-zero entry count.
    - `git rev-parse --verify a7bda73e23efc5e3f9607c3fef37abf8ec4030aa^{commit}` succeeds and `git merge-base --is-ancestor a7bda73e23efc5e3f9607c3fef37abf8ec4030aa HEAD; echo $?` prints `0`.
    - The order of keys is `… V118, V119, V14` — a node one-liner over `Object.keys(MILESTONE_CLOSE_SHAS)` shows `V14` last and `V119` immediately after `V118`.
    - `grep -c 'createFrozenCorpusReader' scripts/validation/v1.19-milestone-audit.mjs` resolves to a value `>= 1`.
    - `grep -c 'existsSync(join(process.cwd()' scripts/validation/v1.19-milestone-audit.mjs` resolves to exactly `1`, and the surviving site is the C17 contract guard.
    - `node scripts/validation/v1.19-milestone-audit.mjs; echo $?` prints `0`, and its summary line reports the same registered check count as before the conversion (16).
    - `node scripts/validation/check-phase-138.mjs` exits 0 with `95 PASS, 0 FAIL, 0 SKIPPED` — the predecessor apex spawns this harness as its AUDIT-HARNESS check, so a conversion regression shows up here.
    - `node scripts/validation/carve-gate.mjs` exits 0 (both edited paths are on-list under Categories 4 and 2).
    - `git diff HEAD~1 -- docs` is empty.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/v1.19-milestone-audit.mjs</automated>
  </verify>
  <done>A real `readAtV119Close` call returns bytes from the v1.19 close SHA, the converted harness exits 0 with an unchanged check inventory, the predecessor apex is still 95/0/0, and both files are committed.</done>
</task>

<task type="auto">
  <name>Task 3: SWEEP-06 measurement — the seventeenth harness against the 60-second budget</name>
  <files>.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md, .planning/milestones/v1.20-GOV-02-LEDGER.md</files>
  <read_first>
    - `scripts/validation/check-phase-60.mjs` — the 60-second per-subprocess timeout this measurement is scored against
    - `.planning/phases/140-*/140-05-PLAN.md` and `140-05-SUMMARY.md` — Phase 140's measurement method and its sixteen-harness result (slowest 4,177 ms), plus its owner-acknowledged note that SWEEP-06's edge coverage is unclassified and stays unresolved
    - `144-CONTEXT.md` — D-25 (SWEEP-06 needs a measurement before it can flip; the seventeenth harness was never measured)
  </read_first>
  <action>
Measure the newly-converted v1.19 harness's wall-clock runtime the same way Phase 140 measured the
other sixteen, three consecutive runs, and record milliseconds — not a "fast enough" judgement.
Score it against `check-phase-60.mjs`'s 60-second per-subprocess budget and against Phase 140's
recorded slowest-of-sixteen figure so the close-gate can flip SWEEP-06 on a measurement rather than
an inference.

Append the result to `144-EVIDENCE.md` (three run times, median, the budget, the margin, the
comparison to the sixteen-harness baseline) and add a short Result note to the Task 1 ledger row's
follow-up, or a new appended row if the schema prefers one row per measurement.

If the median exceeds the budget, STOP and report — do not tune the harness or the budget to fit.
That is a real finding about SWEEP-06, and moving either goalpost to make it pass is exactly the
class of remedy this milestone bars.
  </action>
  <acceptance_criteria>
    - `144-EVIDENCE.md` records three run durations in milliseconds plus the median for the converted harness.
    - The recorded median is compared explicitly against `60000` ms and against Phase 140's slowest-of-sixteen figure.
    - `node scripts/validation/check-phase-60.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-60.mjs</automated>
  </verify>
  <done>The seventeenth harness has a recorded, three-run millisecond measurement against the 60-second budget, and `check-phase-60.mjs` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git object store → `readAtClose` subprocess | Frozen reads shell out to `git show <sha>:<path>`; the SHA and path are the only untrusted-shaped inputs and both are module constants, never caller-supplied at runtime. |
| frozen corpus → harness assertions | The converted harness now asserts against bytes from a past commit; a wrong tag silently changes what "the corpus" means for sixteen checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-04 | Tampering | `MILESTONE_CLOSE_SHAS.V119` | high | mitigate | The pin is proven by identity before use: `git rev-parse --verify <full sha>^{commit}` plus `git merge-base --is-ancestor` against HEAD, and the abbreviated-form constraint is asserted (`length <= 8`) so `frozenCause()`'s stderr taxonomy keeps working |
| T-144-05 | Tampering | `v1.19-milestone-audit.mjs` conversion | high | mitigate | Check inventory asserted unchanged (16) before and after; the predecessor apex's 95/0/0 is re-run as a regression gate; conversion changes byte-fetch only, never an assertion |
| T-144-06 | Spoofing | `git show` subprocess arguments | medium | mitigate | `execFileSync` with an argument ARRAY (never a concatenated shell string) is the existing library contract and is preserved verbatim — the project's one process-level security control |
| T-144-07 | Information Disclosure | frozen reads of `.planning/` at a past SHA | low | accept | The repository is public and every read target is already-published content; no secret is reachable through `readAtClose` |
</threat_model>

<verification>
- A real `readAtV119Close` call returns non-zero bytes; `lsTreeAtV119Close` enumerates a non-zero tree.
- `V119` sits between `V118` and `V14`, abbreviated form, `V14` still last.
- The converted v1.19 harness exits 0 with 16 registered checks and exactly one surviving live-existence call (the C17 guard).
- `check-phase-138.mjs` still reports 95 PASS, 0 FAIL, 0 SKIPPED.
- SWEEP-05 carries the D-02 amendment; the GOV-02 row landed first; the SWEEP-06 measurement is recorded in milliseconds.
</verification>

<success_criteria>
The back-anchor pin exists, is proven reachable, and has a real consumer reading through it — the
frozen-read architecture is demonstrated end-to-end at the new tag before anything else in the phase
is built on top of it.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files: `scripts/validation/check-phase-139.mjs` … `-143.mjs` (five leaves),
`scripts/validation/check-phase-144.mjs` (apex), `scripts/validation/v1.20-milestone-audit.mjs`,
`scripts/validation/v1.20-audit-allowlist.json`,
`.github/workflows/audit-harness-v1.20-integrity.yml`,
`.planning/milestones/v1.20-MILESTONE-AUDIT.md`, `144-EVIDENCE.md`,
`dist/docs-library-v1.20.zip` (gitignored).

New exports (this plan): `readAtV119Close`, `lsTreeAtV119Close`.
New constants (this plan): `MILESTONE_CLOSE_SHAS.V119`; `MILESTONE_TAG`, `SIDECAR_PATH`, `FROZEN`
in `v1.19-milestone-audit.mjs`.

Phase-wide new constants: `BASELINE_24` (`regenerate-supervision-pins.mjs`); `CHAIN_START`,
`CHAIN_END`, `CHAIN_PHASES`, `CHAIN_EXTRA`, `CHAIN_SKIP` (`check-phase-144.mjs`).
Phase-wide new CI job keys: `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`,
`check-phase-144`, `check-phase-139` … `check-phase-143`, `rotting-external-quarterly`,
`pin-helper-advisory`, `frozen-read-probe`.

Modified across the phase: `_lib/frozen-at-close.mjs`, `v1.19-milestone-audit.mjs`,
`check-phase-67.mjs`, `regenerate-supervision-pins.mjs`, `audit-harness-v1.7-integrity.yml`,
`audit-harness-v1.8-integrity.yml`, `v1.20-CARVE.md`, `v1.20-GOV-02-LEDGER.md`,
`v1.20-DEFERRED-CLEANUP.md`, `.planning/{PROJECT,ROADMAP,STATE,REQUIREMENTS}.md`.
</artifacts_produced>

<output>
Create `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-02-SUMMARY.md` when done
</output>
