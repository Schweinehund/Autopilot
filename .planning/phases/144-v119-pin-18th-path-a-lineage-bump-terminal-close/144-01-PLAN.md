---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.20-CARVE.md
autonomous: true
requirements: [HARN-18, HARN-19]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/milestones/v1.20-CARVE.md`'s `carve-allowlist` fenced block gains ten LITERAL paths — `scripts/validation/check-phase-139.mjs` through `-144.mjs` (six), `scripts/validation/v1.20-audit-allowlist.json`, `scripts/pipeline/build-publish-bundle.mjs`, `scripts/pipeline/build-filename-map.mjs`, and `scripts/pipeline/filename-map.md` — never a `check-phase-14*.mjs` glob (D-07: `globToRegExp` maps `*` to `[^/]*` with zero-width allowed and over-matches)."
    - "The amendment commit touches exactly ONE path: `git show --name-only --format= HEAD` prints exactly one line (CARVE `:64-78` rule 1, D-07/D-09)."
    - "The amendment lands BEFORE any edit it authorizes — no `scripts/` or `.github/` path is created or modified anywhere in this plan (CARVE `:64-78` rule 3)."
    - "Each added line carries a one-line `#` rationale comment directly above its group (CARVE `:64-78` rule 2)."
    - "`node scripts/validation/carve-gate.mjs` exits 0 after the amendment commit, with no in-scope path reported off-list."
    - "The added entries are exact literals: every one of the ten new lines contains zero `*`, `?` or `[` characters."
  artifacts:
    - .planning/milestones/v1.20-CARVE.md
  key_links:
    - "v1.20-CARVE.md allowlist -> `carve-gate.mjs` `globToRegExp` -> every `scripts/` file Plans 03-08 create or edit (an off-list in-scope path is a hard exit 1 with no warn-and-continue path)."
    - "v1.20-CARVE.md `## Recorded scope amendments` -> Phase 144's own close-gate, which reads that section as authorized scope (Phase 142 D-33)."
  prohibitions:
    - statement: "The amendment commit message body MUST NOT literally quote the CARVE document's path or any amended file path in prose — `git show --name-only HEAD` prints the message body followed by the file list and the CARVE exclusion check greps the combined output (D-33)."
      status: flagged-unverified
      verification: "`git show --name-only --format=%B HEAD` contains the path string exactly once (the file-list line), never twice."
    - statement: "No allowlist entry already present is edited, reordered or removed; the amendment is append-only within its own new category block."
      status: flagged-unverified
      verification: "`git diff HEAD~1 -- .planning/milestones/v1.20-CARVE.md` shows additions only, zero deletions."
---

<objective>
Land the CARVE allowlist amendment that authorizes every `scripts/` path Phase 144 creates or
edits. This plan runs ALONE and FIRST — it is a hard blocker on every other edit in the phase
(D-07).

Purpose: `[MEASURED]` the v1.20 allowlist contains no entry for `check-phase-139.mjs` …
`check-phase-144.mjs` and no glob covering `scripts/validation/*-audit-allowlist.json`; Category 2's
glob is `.mjs`-only and Category 5's literals stop at `check-phase-138.mjs`. `carve-gate.mjs`
catches brand-new off-list files under `scripts/` via `git status --porcelain --untracked-files=all`,
and a new `check-phase-139.mjs` matches ZERO of the 82 current allowlist patterns. D-09's amendment
procedure forbids the amendment riding in the same commit as the edit it authorizes, so a blocked
edit cannot be cleared later without a full amend-then-redo cycle.

Output: one commit touching one file. No code, no validators, no tracer — this plan is the
governance gate every later plan depends on. The phase tracer is Plan 02 Task 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
@.planning/milestones/v1.20-CARVE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Category-11 allowlist amendment — ten literal paths, alone, first</name>
  <reversibility rating="one-way">One-way within the phase (D-07): a blocked edit cannot be cleared by appending its own category in the same commit; recovery costs a full amend-then-redo cycle. The decision is owner-ratified in CONTEXT.md, so no checkpoint re-asks it.</reversibility>
  <files>.planning/milestones/v1.20-CARVE.md</files>
  <read_first>
    - `.planning/milestones/v1.20-CARVE.md` — the `## Scope exclusion` section, the `## Amendment procedure (D-09)` three numbered rules, the `## Gate failure disposition (D-10)` hard-block statement, and the whole `carve-allowlist` fenced block (Categories 1-10, note Category 5's literal-per-line style and its `check-phase-67.mjs` note)
    - `scripts/validation/carve-gate.mjs` — read with `grep -a` / a plain Read; the file is NUL-classified binary (a literal NUL at offset 2520 from the `'\0DOUBLESTAR\0'` placeholder), so an ordinary grep returns "Binary file … matches" with no line numbers. Confirm `IN_SCOPE_PREFIXES`, the `globToRegExp` translation, and the `--untracked-files=all` status collector before writing any pattern
    - `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md` — D-07 (shape, over-listing rationale, the binary-grep trap), D-09, D-33
  </read_first>
  <action>
Append ONE new category block at the end of the existing `carve-allowlist` fenced block, following
Category 5's literal-per-line style exactly. Number it Category 11 (continuing from Category 10,
added by Phase 143).

Write a one-line `#` rationale comment directly above the block naming the authorizing decision
(D-07) and the phase, then these ten entries, each a bare literal path with no wildcard character:

  the six new validators `scripts/validation/check-phase-139.mjs`, `-140.mjs`, `-141.mjs`,
  `-142.mjs`, `-143.mjs`, `-144.mjs`; the new sidecar `scripts/validation/v1.20-audit-allowlist.json`;
  and three over-listed pipeline paths `scripts/pipeline/build-publish-bundle.mjs`,
  `scripts/pipeline/build-filename-map.mjs`, `scripts/pipeline/filename-map.md`.

Do NOT write a `check-phase-14*.mjs` glob — `globToRegExp` maps `*` to `[^/]*` with zero-width
allowed, so that pattern over-matches `check-phase-14.mjs` and anything else beginning `check-phase-14`.
Over-listing costs nothing while under-listing hard-blocks mid-phase (D-07), which is why the three
pipeline paths are included even though only the two scripts are named in D-07: HARN-19's bundle
regeneration writes `scripts/pipeline/filename-map.md`, an in-scope path under `scripts/` that
matches zero current patterns.

The already-present `.github/workflows/audit-harness-*.yml` glob (Category 1) already covers the
17th workflow, `scripts/validation/v*.*-milestone-audit.mjs` (Category 2) already covers the 18th
harness, and Category 5 already lists `check-phase-67.mjs`, `regenerate-supervision-pins.mjs` and
`carve-gate.mjs` — add none of those again.

Also append one bullet to `## Recorded scope amendments` recording this amendment (authorizing
decision, phase, what it authorizes) so Phase 144's own close-gate reads it as authorized scope
rather than drift.

Commit alone. Per D-33 the message body must not quote the amended document's path or any allowlist
path in prose — name the artifact class in words instead, since `git show --name-only HEAD` prints
the message body followed by the file list and the CARVE exclusion check greps the combined output.
Subject form `docs(144-01): …`.
  </action>
  <acceptance_criteria>
    - `git show --name-only --format= HEAD | wc -l` resolves to `1`.
    - `git diff HEAD~1 --numstat -- .planning/milestones/v1.20-CARVE.md` shows a deletions count of `0`.
    - For each of the ten literal paths, `awk '/```carve-allowlist/,/^```$/' .planning/milestones/v1.20-CARVE.md | grep -v '^#' | grep -Fxc '<path>'` resolves to `1`.
    - `awk '/```carve-allowlist/,/^```$/' .planning/milestones/v1.20-CARVE.md | grep -v '^#' | grep -c 'check-phase-14\*'` resolves to `0`.
    - `node scripts/validation/carve-gate.mjs; echo $?` prints `0`.
    - `git status --porcelain --untracked-files=all -- scripts .github` prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>One commit exists touching only the CARVE document; all ten literal entries are present inside the fenced block; the gate exits 0; no `scripts/` or `.github/` path was created or modified.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the amendment actually authorizes the ten paths</name>
  <files>.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md</files>
  <read_first>
    - `scripts/validation/carve-gate.mjs` (with `grep -a` / plain Read) — the allowlist parser and matcher, to confirm a bare literal line is compared as an exact path match and that comment/blank lines are stripped
    - `.planning/milestones/v1.20-CARVE.md` — the amended fenced block from Task 1
  </read_first>
  <action>
Prove authorization rather than assuming it. Extract the allowlist block from the amended document
with `awk`, strip `#` comments and blank lines (a bare `grep -c` counts comment text and would
self-invalidate), and assert each of the ten candidate paths matches at least one entry using the
same matching semantics `carve-gate.mjs` uses for a wildcard-free line — exact string equality.

Because the six validators and the sidecar do not exist yet, this is a pattern-level proof, not a
gate run: create nothing under `scripts/` to test it (an untracked in-scope file is exactly what the
gate is designed to reject, and creating one here would violate rule 3).

Record the result in a new `144-EVIDENCE.md` under the phase directory: the ten paths, the matching
entry for each, the `carve-gate.mjs` in-scope / on-list / off-list triple and exit code at HEAD, and
the single-file-commit proof from Task 1. This file is the phase's measurement ledger and later plans
append to it.
  </action>
  <acceptance_criteria>
    - A node one-liner reading the stripped allowlist and testing exact equality reports `10/10 matched`.
    - `node scripts/validation/carve-gate.mjs` output line records `off-list=0` and exit code `0`.
    - `144-EVIDENCE.md` exists and contains one row per authorized path plus the recorded gate triple.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>Every one of the ten paths is proven on-list by exact match against the stripped allowlist, and the proof is recorded in `144-EVIDENCE.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| governance document → `carve-gate.mjs` | The allowlist is the sole authorization surface for every frozen-surface edit in this milestone; a wrong pattern here silently widens what any later plan may touch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-01 | Elevation of Privilege | `v1.20-CARVE.md` allowlist block | high | mitigate | Literal paths only, zero wildcard characters, asserted by an acceptance criterion — a `check-phase-14*.mjs` glob would authorize unrelated files via `globToRegExp`'s zero-width `*` |
| T-144-02 | Tampering | the amendment commit | medium | mitigate | Rule-1 single-file commit asserted mechanically (`git show --name-only --format= HEAD` prints one line); an amendment fused with the edit it authorizes is self-authorizing |
| T-144-03 | Repudiation | `## Recorded scope amendments` | low | mitigate | One appended bullet naming decision, phase and authorized class, so the close-gate reads authorized scope rather than drift |
| T-144-SC | Tampering | package-manager installs | low | accept | This phase installs zero packages (RESEARCH.md Package Legitimacy Audit: not applicable); no install task exists to gate |
</threat_model>

<verification>
- `node scripts/validation/carve-gate.mjs` exits 0.
- The amendment commit touches exactly one file.
- All ten literal paths present in the fenced block, zero wildcards among them.
- No `scripts/` or `.github/` path created or modified in this plan.
</verification>

<success_criteria>
Every `scripts/` path Phases 144's later plans create or edit is pre-authorized, the amendment
landed alone and first, and the gate exits 0 — so no later plan can be blocked mid-execution
waiting on an amendment.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files: `scripts/validation/check-phase-139.mjs`, `-140.mjs`, `-141.mjs`, `-142.mjs`, `-143.mjs`,
`scripts/validation/check-phase-144.mjs` (apex), `scripts/validation/v1.20-milestone-audit.mjs`,
`scripts/validation/v1.20-audit-allowlist.json`,
`.github/workflows/audit-harness-v1.20-integrity.yml`,
`.planning/milestones/v1.20-MILESTONE-AUDIT.md`,
`.planning/phases/144-*/144-EVIDENCE.md`, `dist/docs-library-v1.20.zip` (gitignored).

New exports: `readAtV119Close`, `lsTreeAtV119Close` (`scripts/validation/_lib/frozen-at-close.mjs`).

New constants: `MILESTONE_CLOSE_SHAS.V119`; `BASELINE_24` (`regenerate-supervision-pins.mjs`);
`CHAIN_START` / `CHAIN_END` / `CHAIN_PHASES` / `CHAIN_EXTRA` / `CHAIN_SKIP` (`check-phase-144.mjs`);
`MILESTONE_TAG` + `SIDECAR_PATH` + `FROZEN` (`v1.19-milestone-audit.mjs`).

New CI job keys in the 17th workflow: `parse`, `path-match`, `harness-run`,
`linux-chain-ubuntu-latest`, `check-phase-144`, `check-phase-139` … `check-phase-143`,
`rotting-external-quarterly`, `pin-helper-advisory`, `frozen-read-probe`.

Modified: `_lib/frozen-at-close.mjs`, `v1.19-milestone-audit.mjs`, `check-phase-67.mjs`,
`regenerate-supervision-pins.mjs`, `audit-harness-v1.7-integrity.yml`,
`audit-harness-v1.8-integrity.yml`, `.planning/milestones/v1.20-CARVE.md`,
`v1.20-GOV-02-LEDGER.md`, `v1.20-DEFERRED-CLEANUP.md`,
`.planning/{PROJECT,ROADMAP,STATE,REQUIREMENTS}.md`.
</artifacts_produced>

<output>
Create `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-01-SUMMARY.md` when done
</output>
