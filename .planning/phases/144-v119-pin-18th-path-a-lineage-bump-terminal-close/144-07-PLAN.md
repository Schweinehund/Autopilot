---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 07
type: execute
wave: 5
depends_on: ["144-04", "144-05", "144-06"]
files_modified:
  - scripts/validation/check-phase-144.mjs
  - .planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md
autonomous: true
requirements: [HARN-18]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`CHAIN_START = 48`, `CHAIN_END = 143`, and `CHAIN_PHASES` is GENERATED by `Array.from` arithmetic — never transcribed as a literal array (D-08); the length guard asserts 96 and the termini guard asserts 48 and 143."
    - "`CHAIN_SKIP = new Set([])` — empty, per the Phase 68 invariant the SELF check restates."
    - "`CHAIN_EXTRA = [30, 31]` carries forward verbatim and stays OUTSIDE all three module-load guards and outside the SELF dual-invariant, exactly as the predecessor apex keeps it (D-09); collapsing it into the span is forbidden."
    - "Adjacency: the apex adds the guard its predecessor structurally cannot have — every `CHAIN_EXTRA` member is asserted absent from `CHAIN_PHASES` at module load, because `CHAIN_EXTRA` is declared after all three existing guards and is uncoverable by them (D-09). The two sets touch but never merge, and the apex's own phase number is absent from both."
    - "Ordering: children run in `[...CHAIN_PHASES, ...CHAIN_EXTRA]` order — ascending 48 through 143 followed by the two sidecar members — which is deterministic on any machine; and the five new leaves were authored BEFORE this apex, because HAZARD FIX 3 turns an absent chain child into a hard FAIL and apex-first would emit five FAILs indistinguishable from a real regression (D-11)."
    - "Idempotency: the apex writes no file and mutates no state; two consecutive runs at the same commit emit byte-identical summary triples and leave `git status --porcelain` unchanged."
    - "All three HAZARD FIXes are inherited as MECHANISM with every count and span string RE-DERIVED (D-12): the predecessor emits a stale entry-count phrase at runtime, carries a stale span in its header, prints a stale span in its SELF detail, and mis-states its harness's check count — copied literally, those print stale numbers into close-audit evidence."
    - "Retained mechanism, unchanged: the 20 MB subprocess buffer; the narrowed missing-interpreter classifier; absent child means FAIL; the peer classification threshold at phase 67 and above; 600-second peer, 300-second per-subprocess and 300-second audit-harness timeouts."
    - "`V-144-AUDIT-HARNESS` targets `scripts/validation/v1.20-milestone-audit.mjs`: absent means FAIL (a regression, not a skip), and the existence branch precedes the nested guard, so an ordering slip becomes a recorded FAIL rather than a skip (D-10/D-13)."
    - "The archive-root token `'v1.20-phases'` is asserted as a LITERAL STRING in the apex source, and the resolver's behaviour is NOT used as the wrong-token detector: the resolver checks the live path first and returns success regardless of token correctness before archival — the predecessor that carries a wrong token has been permanently green with a false detail string (D-15)."
    - "Expected results are exact: `100 PASS, 0 FAIL, 1 SKIPPED` while `144-VERIFICATION.md` does not yet exist, `101 PASS, 0 FAIL, 0 SKIPPED` once it does, and `99 PASS, 1 FAIL, 1 SKIPPED` if run before the v1.20 harness exists (D-10)."
    - statement: "Concurrency: an interrupted apex run leaves no partial artifact, and the two apex jobs the 17th workflow runs in parallel (chain job and standalone job) cannot interfere, because each runs in its own checkout and the apex only reads."
      verification: backstop
  artifacts:
    - scripts/validation/check-phase-144.mjs
  key_links:
    - "`check-phase-144.mjs` -> 98 child subprocesses (96 span members plus the two sidecar members), each spawned with the nested-invocation variable set on the CHILD, which is why the apex structurally cannot produce a non-nested chain result."
    - "`check-phase-144.mjs` -> `v1.20-milestone-audit.mjs` (AUDIT-HARNESS) and -> the archived-phase-path resolver (AUDIT)."
    - "The five new leaves -> chain members 92 through 96 of this apex's span."
  prohibitions:
    - statement: "The apex must never be described, in source or in evidence, as producing a result across a non-nested chain — it sets the nested variable on every child, so such a result is structurally unproducible (D-32)."
      status: flagged-unverified
      verification: "A grep of the new apex source and of `144-EVIDENCE.md` for that phrasing returns zero hits."
    - statement: "No count, span string or harness check total is transcribed from a predecessor's prose; every number in the new apex is re-derived at authoring time."
      status: flagged-unverified
      verification: "Each numeric literal in the header and in every runtime-emitted string is traced to a command run at authoring time and recorded in `144-EVIDENCE.md`."
---

<objective>
Author `check-phase-144.mjs` — the new apex spanning `[48..143]`, 96 arithmetic entries plus the
two-member sidecar, inheriting the predecessor's three HAZARD FIXes as mechanism with every literal
re-derived, and adding the one module-load guard the predecessor structurally cannot have.

Purpose: this is the chain validator every future milestone inherits and every CI run executes. Its
span is a corrected figure: two ratified sites carry a `[48..138]` drafting error, owner-corrected to
`[48..143]` with 96 entries (D-08/D-25). Its archival-token guardrail is a replacement, not a copy:
the resolver-behaviour approach cannot detect a wrong token, proven by a predecessor that has carried
one green for two milestones (D-15).

Output: one apex validator, its module-load guards proven by negative test, and its exact
PASS/FAIL/SKIP triple recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the apex — arithmetic span, four module-load guards, re-derived literals</name>
  <files>scripts/validation/check-phase-144.mjs</files>
  <read_first>
    - `scripts/validation/check-phase-138.mjs` — in full, as the mechanism template: `:104-125` the span constants and the three module-load guards, `:135` the AUDIT check, `:144` the resolver skip-pass, `:165-172` the sidecar array and the concatenated loop, `:181-186` HAZARD FIX 3, `:187-194` the timeouts and buffer, `:207` HAZARD FIX 2, `:216-236` the audit-harness block with its existence branch preceding the nested guard, `:245-256` the SELF dual-invariant. Note the four stale runtime strings at `:41`, `:70`, `:185` and `:256`
    - `scripts/validation/_lib/archive-path.mjs` — `:7` the caller-owns-fail-semantics contract, `:23-24` the live-path-first resolution order, `:29` the null return without throw
    - `scripts/validation/check-phase-125.mjs:86` — the live counterexample: a wrong archive-root token, permanently green with a false detail string
    - `scripts/validation/v1.20-milestone-audit.mjs` — count its live checks array to derive any check-count string this apex emits
    - `144-CONTEXT.md` — D-08 through D-15 (span, sidecar, expected triples, ordering, mechanism inheritance, audit-harness target, token replacement)
  </read_first>
  <action>
Build the apex from the predecessor's mechanism, not its text.

Span. Set the start and end constants to 48 and 143 and generate the span with `Array.from`
arithmetic — never a transcribed literal array. Carry all three module-load guards across as
mechanism with their embedded numbers re-derived: the duplicate check, the length assertion (96), and
the termini assertion (48 and 143). Keep the skip set empty.

Sidecar. Declare the two-member sidecar array verbatim and keep it outside all three guards and
outside the SELF dual-invariant, exactly as the predecessor does — collapsing it into the span is
forbidden. Then add the fourth guard the predecessor cannot have: assert at module load that no
sidecar member appears in the span. The sidecar is declared after the three existing guards, so it is
structurally uncoverable by them; this closes that gap.

Mechanism to retain unchanged: the 20 MB subprocess buffer, the narrowed missing-interpreter
classifier, absent-child-means-FAIL, the peer classification threshold at phase 67 and above, and the
600 / 300 / 300-second timeout budget.

Checks. Register the archived-phase AUDIT check, the concatenated chain loop over span-then-sidecar,
the AUDIT-HARNESS check pointed at the v1.20 harness with its existence branch BEFORE the nested
guard (absent means FAIL, nested means skip the re-run), and the SELF dual-invariant asserting this
apex's own number is absent from the span and the skip set is empty.

Archive-root token. Assert the token string `'v1.20-phases'` literally in the apex source as a
string-presence check, and separately record that the resolver's own null-versus-success behaviour is
NOT the detector: it checks the live path first, so before archival it succeeds whatever the token
says. Never copy a predecessor's token — three of them carry a wrong one, frozen.

Strings. Every number the apex prints at runtime — the entry count in the hazard-fix message, the
span in the header, the span in the SELF detail, and any harness check count — is derived at
authoring time from the code you have written or from a command you have just run. The predecessor
demonstrates all four failure modes; do not reproduce any of them. Do not write any phrasing that
claims a chain result outside nested invocation: this apex sets the nested variable on every child, so
no such result exists (D-32).

Commit alone. Subject form `feat(144-07): …`.
  </action>
  <acceptance_criteria>
    - `grep -c 'Array.from' scripts/validation/check-phase-144.mjs` resolves to a value `>= 1`, and a grep for a transcribed bracketed span literal returns nothing.
    - A node one-liner importing the module reports `CHAIN_PHASES.length === 96`, `CHAIN_PHASES[0] === 48`, `CHAIN_PHASES[95] === 143`, `CHAIN_SKIP.size === 0`, and `CHAIN_EXTRA` deep-equal to the two-member sidecar.
    - `grep -c "v1.20-phases" scripts/validation/check-phase-144.mjs` resolves to a value `>= 1`.
    - `grep -c 'v1.20-milestone-audit.mjs' scripts/validation/check-phase-144.mjs` resolves to a value `>= 1`.
    - The header's stated span and entry count match the generated constants, verified by a node one-liner rather than by reading prose.
    - `node scripts/validation/carve-gate.mjs` exits 0 with the new file on-list.
  </acceptance_criteria>
  <verify>
    <automated>node -e "import('./scripts/validation/check-phase-144.mjs')"</automated>
  </verify>
  <done>The apex loads without throwing, its four module-load guards are present, its span is generated arithmetically, and every emitted literal is re-derived.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the four module-load guards fail-first, outside the working tree</name>
  <files>.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md</files>
  <read_first>
    - `scripts/validation/check-phase-144.mjs` — the four guards authored in Task 1
    - `scripts/validation/check-phase-138.mjs:104-125` — the three-guard shape being extended
  </read_first>
  <action>
A guard that has never thrown is an assumption. Copy the apex to the session scratchpad — never into
`scripts/`, where an untracked in-scope path is a hard gate failure — and mutate one invariant per
copy: a duplicated span entry, a wrong end constant so the length assertion sees a different number, a
shifted start so the termini assertion fails, and a sidecar member moved inside the span so the new
disjointness guard fires. Load each mutated copy and confirm it throws at module load with a message
naming the violated invariant.

The fourth case is the one that matters most: it is the guard the predecessor lacks, and it is the
only one that can catch a sidecar member being silently absorbed into the span.

Record all four in `144-EVIDENCE.md` with the observed throw messages.
  </action>
  <acceptance_criteria>
    - Four scratchpad copies, four throws at module load, each message naming the violated invariant.
    - The unmutated apex still loads cleanly: `node -e "import('./scripts/validation/check-phase-144.mjs')"` exits 0.
    - `git status --porcelain --untracked-files=all -- scripts` prints nothing.
    - `144-EVIDENCE.md` records all four throw messages verbatim.
  </acceptance_criteria>
  <verify>
    <automated>node -e "import('./scripts/validation/check-phase-144.mjs')"</automated>
  </verify>
  <done>All four module-load guards are proven fail-first against a genuine violation, including the disjointness guard the predecessor cannot have.</done>
</task>

<task type="auto">
  <name>Task 3: Run the apex and record the exact triple, twice</name>
  <files>.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md</files>
  <read_first>
    - `scripts/validation/check-phase-144.mjs` — the authored apex
    - `144-CONTEXT.md` — D-10's three expected triples and the arithmetic behind them (1 audit + 96 span + 2 sidecar + 1 audit-harness + 1 self)
    - `144-CONTEXT.md` D-35 — run the apex chain SEPARATELY from any verifier pass; a link or anchor change is a prose change and has tripped banned-phrase guards after a passed verifier
  </read_first>
  <action>
Run the apex twice, consecutively, at the same commit. Record both summary triples, the wall-clock
duration of each, and confirm they are identical — that is the idempotency evidence, and it is also
how a hidden write or a stateful child surfaces.

At this point in the phase `144-VERIFICATION.md` does not exist, so the expected triple is
`100 PASS, 0 FAIL, 1 SKIPPED`: the archived-phase AUDIT check skips because the verification artifact
is not yet authored. Anything else is a finding — in particular a FAIL count of 1 means the harness
check ran before its target existed, and a FAIL count of 5 means the leaves were not authored first.

Also confirm the tree is unchanged after both runs, and record that the apex performs no write.

Run this separately from any verifier or prose-guard pass, per D-35.
  </action>
  <acceptance_criteria>
    - Two consecutive runs of `node scripts/validation/check-phase-144.mjs` both print `100 PASS, 0 FAIL, 1 SKIPPED` and both exit 0.
    - The two summary lines are byte-identical.
    - `git status --porcelain` output is identical before and after both runs.
    - `144-EVIDENCE.md` records both triples, both durations, the skipped check's id and detail string, and the arithmetic reconciling 101 total checks.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-144.mjs</automated>
  </verify>
  <done>The apex runs green at the expected pre-verification triple, twice identically, with no writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| apex → 98 child subprocesses | Each child is spawned with an argument array and a timeout; a child's stdout/stderr is untrusted-shaped input to the failure-detail formatter. |
| apex assertion → close-audit evidence | Numbers this validator prints become the milestone audit's evidence; a stale transcribed literal launders a wrong figure into a permanent record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-24 | Tampering | span and sidecar constants | high | mitigate | Four module-load guards, all proven fail-first against mutated scratchpad copies; the span is generated arithmetically so it cannot drift by transcription |
| T-144-25 | Repudiation | runtime-emitted counts and spans | high | mitigate | Every literal re-derived at authoring time and traced in the evidence file; the predecessor's four stale strings are named so they cannot be copied |
| T-144-26 | Spoofing | the archive-root token | high | mitigate | The token is asserted as a literal string in source; the resolver's success/null behaviour is explicitly rejected as a detector, with the permanently-green wrong-token counterexample recorded |
| T-144-27 | Denial of Service | 98 child subprocesses | medium | mitigate | Per-subprocess and peer timeouts plus a 20 MB buffer carried forward unchanged; the nested variable set on each child prevents recursive fan-out |
| T-144-28 | Tampering | child stdout/stderr handling | low | mitigate | `execFileSync` argument arrays and the existing failure-detail formatter, both inherited unchanged |
</threat_model>

<verification>
- The apex loads cleanly; all four module-load guards throw against mutated copies.
- `CHAIN_PHASES` has 96 entries generated arithmetically with termini 48 and 143; the skip set is empty; the sidecar is disjoint and outside every guard.
- Two consecutive runs print an identical `100 PASS, 0 FAIL, 1 SKIPPED`, with no writes.
- The archive-root token is asserted literally; the audit-harness check names the v1.20 harness.
- `node scripts/validation/carve-gate.mjs` exits 0.
</verification>

<success_criteria>
The milestone's apex exists, is green at its expected pre-verification triple, and carries a
disjointness guard and a token assertion its predecessor could not.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files (this plan): `scripts/validation/check-phase-144.mjs`.
New constants (this plan): `CHAIN_START`, `CHAIN_END`, `CHAIN_PHASES`, `CHAIN_EXTRA`, `CHAIN_SKIP`.
New files (phase-wide): `check-phase-139.mjs` … `-143.mjs`, `v1.20-milestone-audit.mjs`,
`v1.20-audit-allowlist.json`, `audit-harness-v1.20-integrity.yml`, `v1.20-MILESTONE-AUDIT.md`,
`144-EVIDENCE.md`, `dist/docs-library-v1.20.zip` (gitignored).
New exports: `readAtV119Close`, `lsTreeAtV119Close`.
New constants (phase-wide): `MILESTONE_CLOSE_SHAS.V119`, `BASELINE_24`.
New CI job keys: `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`,
`check-phase-144`, `check-phase-139` … `check-phase-143`, `rotting-external-quarterly`,
`pin-helper-advisory`, `frozen-read-probe`.
Modified across the phase: `_lib/frozen-at-close.mjs`, `v1.19-milestone-audit.mjs`,
`check-phase-67.mjs`, `regenerate-supervision-pins.mjs`, `audit-harness-v1.7-integrity.yml`,
`audit-harness-v1.8-integrity.yml`, `v1.20-CARVE.md`, `v1.20-GOV-02-LEDGER.md`,
`v1.20-DEFERRED-CLEANUP.md`, `.planning/{PROJECT,ROADMAP,STATE,REQUIREMENTS}.md`.
</artifacts_produced>

<output>
Create `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-07-SUMMARY.md` when done
</output>
