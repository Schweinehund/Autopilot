---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 06
type: execute
wave: 4
depends_on: ["144-01", "144-02"]
files_modified:
  - .planning/milestones/v1.20-GOV-02-LEDGER.md
  - scripts/validation/v1.20-audit-allowlist.json
  - scripts/validation/v1.20-milestone-audit.mjs
  - scripts/validation/regenerate-supervision-pins.mjs
  - .planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md
autonomous: true
requirements: [HARN-18]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`scripts/validation/v1.20-milestone-audit.mjs` is the 18th Path-A harness, inheriting C1 through C17 from the v1.19 source, and it reads its own corpus LIVE — never through a frozen reader. There is no V120 pin and there cannot be one at this close (back-anchor circularity, recorded as V120-PIN-DEFERRAL); the correct copy source is therefore the v1.19 harness as it stood BEFORE Plan 02's conversion commit."
    - "`scripts/validation/v1.20-audit-allowlist.json` is a header-fields-only copy of the v1.19 sidecar: only the `generated` and `phase` header fields change; the pin arrays are copied (D-17)."
    - "`c13_rotting_external` in the sidecar is an OBJECT OF NESTED ARRAYS, not a flat list — a naive top-level walk finds 16 `docs/` files instead of the true 33, so any file-set derivation must recurse (D-17)."
    - "The v1.20 harness's sidecar read points at its own sidecar path literally, and that literal is what the CI path-match job greps for (a v1.19 literal copied forward makes that job exit 1)."
    - "`regenerate-supervision-pins.mjs` gains a BASELINE_24 block following BASELINE_23's shape — header-fields-only, no coordinate change; the file already names BASELINE_24 as next in the Path-A chain at `:531-532`."
    - "Pin drift is evidenced by the sidecar-derived pinned-file-set intersected with `git diff --name-only <predecessor close SHA>..HEAD`, adjudicated LINE-granular under `git diff -U0`, never by the pin helper's report mode — that mode hardcodes the v1.7 sidecar (`:290`, `:336`, `:563`) and walks only 26 of 59 line-pins (D-17)."
    - "The measured pin-drift result is ZERO and is not re-derived: 106 changed files, 33 distinct sidecar-named docs files, intersection 5 (3 line-pinned), all 21 hunks 1:1 line-neutral under `git diff -U0` — record these figures, do not recompute them."
    - "A GOV-02 census row lands BEFORE the pin-helper edit; the two new files are creations, not frozen-surface edits, and need no census of their own."
    - "`node scripts/validation/v1.20-milestone-audit.mjs` exits 0, and its registered check count is recorded by COUNTING the live checks array — never copied from any predecessor's prose (`check-phase-138.mjs:41` describes its harness as 17-check when that harness registers 16)."
  artifacts:
    - scripts/validation/v1.20-milestone-audit.mjs
    - scripts/validation/v1.20-audit-allowlist.json
    - scripts/validation/regenerate-supervision-pins.mjs
  key_links:
    - "`v1.20-milestone-audit.mjs` -> `check-phase-144.mjs`'s AUDIT-HARNESS check (Plan 07): absent means FAIL, not skip, and the existence branch precedes the nested guard (D-13)."
    - "`v1.20-milestone-audit.mjs`'s literal sidecar path -> the 17th workflow's path-match job grep (Plan 08)."
    - "`regenerate-supervision-pins.mjs` BASELINE_24 -> the Path-A lineage chain the file's own comments already anticipate."
  prohibitions:
    - statement: "The pin helper's report mode is never cited as pin-drift proof, in the harness header, the evidence file, or the milestone audit."
      status: flagged-unverified
      verification: "A grep for the report-mode flag across `144-EVIDENCE.md` and the new harness returns zero hits in an evidentiary claim."
    - statement: "No check is dropped, renumbered or weakened while copying C1 through C17 forward; the copy is a lineage bump, not a re-scope."
      status: flagged-unverified
      verification: "The new harness's registered check ids match the v1.19 source's ids one for one."
    - statement: "No glossary document's `last_verified` or `review_by` field is touched while wiring the new harness (D-34): 145 documents sit at exactly 90 days against a strictly-greater-than test and the arithmetic has no wall-clock term, so any edit flips the difference."
      status: flagged-unverified
      verification: "`git diff HEAD~3 -- docs` is empty."
---

<objective>
Land the 18th Path-A generation: the v1.20 milestone-audit harness, its allowlist sidecar, and the
BASELINE_24 refresh in the pin-regeneration helper.

Purpose: `check-phase-144.mjs`'s AUDIT-HARNESS check points at this harness and treats absence as a
FAIL rather than a skip (D-13), and the 17th CI workflow's path-match job greps for the sidecar path
literal inside the harness source — so both files must exist, and be internally consistent, before
the apex (Plan 07) and the workflow (Plan 08) can be authored against them.

Output: two new files, one appended baseline block, one census row, and a recorded pin-drift
adjudication using the correct instrument.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-PATTERNS.md
@.planning/milestones/v1.20-CARVE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GOV-02 census row + the sidecar, header fields only</name>
  <files>.planning/milestones/v1.20-GOV-02-LEDGER.md, scripts/validation/v1.20-audit-allowlist.json</files>
  <read_first>
    - `scripts/validation/v1.19-audit-allowlist.json` — in full: the header fields, all five pin arrays (59 line-pins), and the nested-array shape of the rotting-external section
    - `.planning/milestones/v1.20-CARVE.md` — the GOV-02 grep procedure (path string AND symbol) and the amendment already landed in Plan 01 authorizing this file
    - `.planning/milestones/v1.20-GOV-02-LEDGER.md` — the row schema and the most recent rows
    - `scripts/validation/regenerate-supervision-pins.mjs:505-548` — the BASELINE_23 block shape and the comment already naming BASELINE_24 as next
  </read_first>
  <action>
Census first, for the one frozen surface this plan edits: the pin-regeneration helper. Grep both
forms — its path string and the baseline symbols — across `scripts/`, `.github/` and `package.json`,
with `grep -a` on any NUL-classified target, and record one appended ledger row naming every pinning
call site and the planned append-only diff shape. The two new files are creations rather than
frozen-surface edits and need no census of their own; say so explicitly in the row so the omission
reads as a ruling.

Then author the sidecar as a header-fields-only copy: change only the `generated` and `phase` header
fields; copy every pin array through unchanged. When deriving or reporting any file set from this
sidecar, recurse — the rotting-external section is an object of nested arrays, and a top-level walk
silently reports 16 documents where the true count is 33.

Commit the census row first, then the sidecar. Per D-33 do not quote either path in a message body.
  </action>
  <acceptance_criteria>
    - The ledger gains exactly one row; `git diff --numstat` for that file shows a deletions count of `0`.
    - `node -e "JSON.parse(require('fs').readFileSync('scripts/validation/v1.20-audit-allowlist.json','utf8'))"` exits 0.
    - A node one-liner comparing the v1.19 and v1.20 sidecars reports that the only differing top-level keys are the two header fields.
    - A recursive walk of the rotting-external section reports 33 distinct `docs/` paths, not 16.
    - `node scripts/validation/carve-gate.mjs` exits 0 with the new sidecar on-list.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/carve-gate.mjs</automated>
  </verify>
  <done>The census row is committed ahead of the helper edit, and the sidecar exists as a header-fields-only copy that parses.</done>
</task>

<task type="auto">
  <name>Task 2: v1.20-milestone-audit.mjs — Path-A copy from the UNCONVERTED v1.19 source</name>
  <files>scripts/validation/v1.20-milestone-audit.mjs</files>
  <read_first>
    - `scripts/validation/v1.19-milestone-audit.mjs` at the commit BEFORE Plan 02's conversion — retrieve it with `git show <that commit>:scripts/validation/v1.19-milestone-audit.mjs`; this is the copy source
    - `scripts/validation/v1.19-milestone-audit.mjs` at HEAD (post-conversion) — read for contrast only, to confirm which seams differ and to be certain none of the frozen-reader wiring is carried forward
    - `.planning/milestones/v1.20-CARVE.md` — Category 2's glob already covers this path
    - `144-CONTEXT.md` — D-01 (why v1.19 converted here), the Out-of-Scope entry barring a frozen-aware v1.20 harness against its own corpus, and D-34 (the glossary prohibition the C5/C10 legs enforce)
  </read_first>
  <action>
Copy the v1.19 harness forward one generation, from its UNCONVERTED form.

The copy source is deliberate. Plan 02 converted the live v1.19 harness to read at the v1.19 close
SHA; copying that converted form forward would make the v1.20 harness audit v1.19's frozen corpus
instead of its own live one. There is no V120 pin and there cannot be one at this close — a
close-gate cannot reference its own not-yet-existing SHA, which is the back-anchor circularity
recorded as V120-PIN-DEFERRAL. Take the pre-conversion source out of git history and start from
that, so the live-read seams (`readFile`, the directory walk, the sidecar parse) come across intact
rather than being hand-reverted.

Re-derive every hardcoded predecessor string: the sidecar path literal, the milestone label in the
header and in every emitted summary line, the phase span in the header narrative, and any lineage
sentence naming the previous generation. Count the registered checks by reading the live checks array
in the file you have just written and state that number in the header — do not carry a predecessor's
prose count forward. The known example of that failure is the current apex describing the v1.19
harness as 17-check when it registers 16 (ids run 1-7 and 9-17; C8 was never defined).

C1 through C17 are inherited verbatim in substance: same ids, same assertions, same thresholds. C17
keeps its subprocess-spawn form against the shared contract script — with the harness reading live
HEAD throughout, no exception comment is needed.

Do not touch any corpus document. C5 and C10 walk the full Android and Linux document sets with
static frontmatter arithmetic and no wall-clock term; 145 documents sit at exactly 90 days against a
strictly-greater-than test, so any `last_verified` or `review_by` edit flips the difference.

Commit alone. Subject form `feat(144-06): …`.
  </action>
  <acceptance_criteria>
    - `node scripts/validation/v1.20-milestone-audit.mjs; echo $?` prints `0`.
    - `node scripts/validation/v1.20-milestone-audit.mjs --verbose` prints a summary naming the same check ids as the v1.19 source, one for one.
    - `grep -c 'createFrozenCorpusReader' scripts/validation/v1.20-milestone-audit.mjs` resolves to `0` — the new harness reads live HEAD.
    - `grep -c 'scripts/validation/v1.20-audit-allowlist.json' scripts/validation/v1.20-milestone-audit.mjs` resolves to a value `>= 1`, and `grep -c 'v1.19-audit-allowlist.json' scripts/validation/v1.20-milestone-audit.mjs` resolves to `0`.
    - The header's stated check count equals the length of the live checks array, verified by a node one-liner rather than by reading prose.
    - `git diff HEAD~1 -- docs` is empty.
    - `node scripts/validation/carve-gate.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/v1.20-milestone-audit.mjs</automated>
  </verify>
  <done>The 18th harness exists, exits 0, reads live HEAD, points at its own sidecar, and states a counted rather than inherited check total.</done>
</task>

<task type="auto">
  <name>Task 3: BASELINE_24 append + the pin-drift adjudication, recorded with the correct instrument</name>
  <files>scripts/validation/regenerate-supervision-pins.mjs, .planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md</files>
  <precondition>The GOV-02 census row covering the pin-regeneration helper is committed (Task 1).</precondition>
  <read_first>
    - `scripts/validation/regenerate-supervision-pins.mjs:505-548` — BASELINE_23's block shape and the existing comment naming BASELINE_24 as next in the chain
    - `scripts/validation/regenerate-supervision-pins.mjs:280-345` and `:555-570` — the report mode's hardcoded v1.7 sidecar, so the prohibition on citing it is grounded in what the code does
    - `scripts/validation/v1.20-audit-allowlist.json` — the sidecar whose pinned file set drives the drift adjudication
    - `144-CONTEXT.md` specifics — the measured pin-drift figures (106 changed files, 33 sidecar-named docs files, intersection 5, 3 line-pinned, 21 hunks all 1:1 line-neutral) and the adjudication rule that file-granular intersection is only the candidate set while the verdict is line-granular
  </read_first>
  <action>
Append a BASELINE_24 block to the pin-regeneration helper following BASELINE_23's shape exactly:
header fields only, no coordinate change, append-only. The file already names BASELINE_24 as the next
entry in the Path-A chain, so this closes a gap the file itself anticipates.

Then record the pin-drift adjudication in `144-EVIDENCE.md` using the correct instrument: the
sidecar-derived pinned-file set intersected with the changed-file set since the predecessor close SHA,
adjudicated line-granular under a zero-context diff. Do NOT re-derive the numbers — the measurement
was taken and adjudicated; transcribe the recorded figures and state the method, the candidate set,
and the line-granular verdict of zero real drift.

Do not cite the helper's report mode as proof of anything: it hardcodes the v1.7 sidecar and walks
26 of 59 line-pins, and this milestone's predecessor already corrected that citation once.

Then re-run the helper's self-test, which the Plan 04 leaf spawns as a needle, to prove the append
did not disturb it.
  </action>
  <acceptance_criteria>
    - `grep -c 'BASELINE_24' scripts/validation/regenerate-supervision-pins.mjs` resolves to a value `>= 2` (the pre-existing forward reference plus the new block).
    - `git diff HEAD~1 --numstat -- scripts/validation/regenerate-supervision-pins.mjs` shows a deletions count of `0`.
    - `node scripts/validation/regenerate-supervision-pins.mjs --self-test; echo $?` prints `0` and reports an identical diff.
    - `node scripts/validation/check-phase-141.mjs; echo $?` prints `0` — the Plan 04 leaf that spawns the self-test is unaffected.
    - `144-EVIDENCE.md` records the drift method, the candidate-set size, the line-granular verdict, and an explicit note that the helper's report mode was not used as evidence.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/regenerate-supervision-pins.mjs --self-test</automated>
  </verify>
  <done>BASELINE_24 is appended without a coordinate change, the self-test still passes, and the pin-drift verdict is recorded with the sidecar-intersection instrument.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy source → new harness | Copying the wrong generation (converted vs unconverted) silently changes which corpus the new harness audits, with no error and no failing check. |
| sidecar JSON → harness parse | The sidecar is untrusted-shaped input to the harness; a malformed file degrades or throws depending on the parse path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-20 | Tampering | `v1.20-milestone-audit.mjs` copy source | high | mitigate | Copy source is retrieved from git history at the pre-conversion commit; a zero-count grep for the frozen-reader factory asserts no frozen wiring came across |
| T-144-21 | Spoofing | inherited prose counts and strings | medium | mitigate | The check count is derived from the live array length and asserted by a node one-liner, never carried from a predecessor's header |
| T-144-22 | Tampering | pin-drift evidence | medium | mitigate | The report mode is prohibited as evidence; the sidecar-intersection method with a line-granular verdict is recorded instead |
| T-144-23 | Input Validation | sidecar JSON parse | low | mitigate | `JSON.parse` inside the existing try/catch degrade path, unchanged from the copy source; parse asserted by an acceptance criterion |
</threat_model>

<verification>
- The new harness exits 0, reads live HEAD, references only its own sidecar path, and states a counted check total.
- The sidecar parses and differs from its predecessor only in the two header fields.
- BASELINE_24 is appended with zero deletions; the self-test still exits 0 and the Plan 04 leaf that spawns it still exits 0.
- The pin-drift verdict is recorded with the sidecar-intersection method and no report-mode citation.
- `node scripts/validation/carve-gate.mjs` exits 0.
</verification>

<success_criteria>
The 18th Path-A generation exists and runs, so the apex's AUDIT-HARNESS check has a real target and
the 17th workflow's path-match job has a literal that resolves.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files (this plan): `scripts/validation/v1.20-milestone-audit.mjs`,
`scripts/validation/v1.20-audit-allowlist.json`.
New constants (this plan): `BASELINE_24` in `regenerate-supervision-pins.mjs`.
New files (phase-wide): `check-phase-139.mjs` … `-143.mjs`, `check-phase-144.mjs` (apex),
`audit-harness-v1.20-integrity.yml`, `v1.20-MILESTONE-AUDIT.md`, `144-EVIDENCE.md`,
`dist/docs-library-v1.20.zip` (gitignored).
New exports: `readAtV119Close`, `lsTreeAtV119Close`.
New constants (phase-wide): `MILESTONE_CLOSE_SHAS.V119`; `CHAIN_START` / `CHAIN_END` /
`CHAIN_PHASES` / `CHAIN_EXTRA` / `CHAIN_SKIP` in the new apex.
New CI job keys: `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`,
`check-phase-144`, `check-phase-139` … `check-phase-143`, `rotting-external-quarterly`,
`pin-helper-advisory`, `frozen-read-probe`.
Modified across the phase: `_lib/frozen-at-close.mjs`, `v1.19-milestone-audit.mjs`,
`check-phase-67.mjs`, `regenerate-supervision-pins.mjs`, `audit-harness-v1.7-integrity.yml`,
`audit-harness-v1.8-integrity.yml`, `v1.20-CARVE.md`, `v1.20-GOV-02-LEDGER.md`,
`v1.20-DEFERRED-CLEANUP.md`, `.planning/{PROJECT,ROADMAP,STATE,REQUIREMENTS}.md`.
</artifacts_produced>

<output>
Create `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-06-SUMMARY.md` when done
</output>
