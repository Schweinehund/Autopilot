---
phase: 144-v119-pin-18th-path-a-lineage-bump-terminal-close
plan: 09
type: execute
wave: 7
depends_on: ["144-03", "144-08"]
files_modified:
  - .planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md
autonomous: false
requirements: [HARN-19]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The ACCEPTED-SCOPED-RED disposition is discharged by re-running all TEN member validators standalone at the CLOSE SHA, not by inheriting Phase 141 or 142 evidence — 96 commits including Phase 143's 145 corpus repairs have landed since those runs (D-26)."
    - "The ten members are check-phase-30, 31, 48, 60, 61, 62, 63, 64, 65 and 66; all ten exit 0 at HEAD, and the two skips (in the 30 and 31 members) are deterministic environment skips on an absent optional link checker and diagram tool, neither of which is a project dependency — classify them, never read them as gaps."
    - "Archival-drift Class 1 is a PRE-PUSH STATIC CENSUS: enumerate every `scripts/validation/**` file referencing the phases directory, subtract those whose reads go through a frozen reader, and fail on any survivor reading phases 139 through 144. The measured baseline at HEAD is ZERO survivors, out of 51 files that reference the directory at all (D-30)."
    - "Archival-drift Class 2 cannot manifest before the close-gate rewrites the requirements and roadmap documents, so it is NOT part of this pre-push scan — it is covered by the post-gate confirmatory run in Plan 12 (D-30)."
    - "The push is preceded by a freshness check: fetch, then assert zero commits are reachable from the remote head but not from local master. The stored fetch state is stale and the ahead/behind figure was never re-measured this session, so it must be taken fresh (D-18)."
    - "Working-tree hygiene is RULED ON before the push, not ignored: the tree carries roughly 104 untracked entries, 8 worktrees and 7 unmerged agent branches. None sits inside the gate's scope, but Axis-1 and Axis-3 both run against this state, so the ruling is recorded (D-18)."
    - "The 17 workflows are enumerated BY NAME from a live listing of the workflow directory at dispatch time — both YAML extensions — never from a carried count (D-19)."
    - "Dispatch is required explicitly by the roadmap's own success criterion because a push fires nothing: no integrity workflow carries a push trigger, all carry manual-dispatch and schedule triggers, and the weekly cron cannot supply evidence at a chosen SHA (D-19)."
    - "The atom branch from Phase 139 is AUDITED AND REPORTED, never deleted unilaterally: it is fully merged (zero commits reachable from it but not from master), it is a REMOTE ref with no reflog to recover from, and its tip is also the head SHA of the sixteen recorded runs on which two requirements' completion evidence rests (D-23)."
    - "The push and the dispatch are executed BY THE OWNER at a blocking checkpoint; authorization alone does not move the executor across that boundary, and the executor never self-approves (D-18)."
  artifacts:
    - .planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md
  key_links:
    - "The recorded close SHA -> all three audit axes (Plan 10) and the dispatch read-back assertion; one shared SHA is what makes the three axes comparable (D-20)."
    - "The ten standalone re-runs -> the ACCEPTED-SCOPED-RED deletion in Plan 11's close artifact."
    - "The Class-1 census -> the archive move a later milestone-close command performs; a survivor would break the next apex after the phase directories move."
  prohibitions:
    - statement: "The executor MUST NOT run `git push`, `gh workflow run`, or any command that mutates a remote ref — including deleting the Phase 139 atom branch. Those are owner actions at the checkpoint; there is no un-push and a deleted remote branch has no reflog."
      status: flagged-unverified
      verification: "`git rev-parse origin/master` is unchanged across the executor's own work, and the atom branch still resolves, until the owner acts at the checkpoint."
    - statement: "No failing validator is fixed, silenced, or re-scoped in this plan to make the pre-push state look green; a red member is a finding to report, not a defect to paper over."
      status: flagged-unverified
      verification: "`git diff HEAD~1 -- scripts` is empty for this plan's own commits."
---

<objective>
Establish that the local state is genuinely ready to push, then halt for the owner.

Purpose: HARN-19's evidence must be gathered at one shared SHA, and that SHA only exists once the
owner pushes. Before that, three things must be true and recorded: the ten standalone-red validators
are green at the close SHA rather than at a stale earlier one, no validator will break when a later
command moves this milestone's phase directories into the archive, and the working tree that Axis-1
and Axis-3 will run against has been ruled on rather than ignored.

Output: a recorded pre-push readiness census, the live 17-workflow enumeration with the exact
dispatch command block, the atom-branch audit, and a blocking checkpoint the executor must not
self-approve.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-CONTEXT.md
@.planning/milestones/v1.19-DEFERRED-CLEANUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-push readiness census — ten standalone re-runs, Class-1 drift, tree hygiene</name>
  <files>.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md</files>
  <read_first>
    - `.planning/milestones/v1.19-DEFERRED-CLEANUP.md:60-89` — the ACCEPTED-SCOPED-RED disposition: its ten members and four root-cause classes
    - `144-CONTEXT.md` specifics — the measured per-member results at HEAD and the classification of the two deterministic environment skips; the measured working-tree figures; the measured Class-1 baseline of zero survivors out of 51 referencing files
    - `scripts/validation/check-phase-70.mjs:390-420` and `scripts/validation/check-phase-124.mjs:40-100` — the two known frozen-reader call sites that must be SUBTRACTED from the Class-1 population rather than counted as survivors
    - `144-CONTEXT.md` D-26, D-30, D-35
  </read_first>
  <action>
Three censuses, one evidence section, zero code changes.

First, the ten standalone-red members. Run each of check-phase-30, 31, 48, 60, 61, 62, 63, 64, 65 and
66 standalone at the current commit and record exit code and summary triple for each. Do not inherit
the earlier phases' results: 96 commits have landed since, including a large corpus-repair phase.
Classify the two deterministic environment skips explicitly — they arise from an optional link
checker and an optional diagram tool, neither of which is a project dependency, and the invocation
form used means they skip on Linux too. A skip that is classified is evidence; a skip that is merely
observed is a gap.

Second, Class-1 archival drift, as a static census. Enumerate every file under `scripts/validation/`
that references the phases directory, subtract those whose reads go through a frozen reader, and
report any survivor that reads a phase in the 139 to 144 range. The measured baseline is zero
survivors, so a non-zero result is a genuine finding: it means a validator will break when a later
command moves this milestone's phase directories into the archive. Do not confuse the population that
merely references the directory with the population the check acts on.

Third, working-tree hygiene. Record the untracked-entry count, the worktree list and the unmerged
agent branches, and RULE on them: state explicitly that none sits inside the gate's scope, and that
Axis-1 and Axis-3 will run against this state. A ruling, not a silence.

Run the apex separately from any verifier or prose-guard pass (D-35), and record its triple alongside.
Append everything to `144-EVIDENCE.md` under a clearly-named pre-push readiness section.
  </action>
  <acceptance_criteria>
    - All ten members exit 0; each summary triple is recorded individually.
    - Exactly two skips appear across the ten runs, both classified by name and cause in the evidence.
    - The Class-1 census reports zero survivors, with the subtracted frozen-reader call sites named.
    - The untracked-entry count, worktree count and unmerged-branch count are recorded with an explicit in-scope/out-of-scope ruling.
    - `node scripts/validation/check-phase-144.mjs` exits 0 and prints `100 PASS, 0 FAIL, 1 SKIPPED`.
    - `git diff HEAD~1 -- scripts .github` is empty for this plan's commits.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validation/check-phase-144.mjs</automated>
  </verify>
  <done>All ten standalone members are green at the close SHA with both skips classified, Class-1 drift is zero, and the tree state is ruled on and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Live 17-workflow enumeration, freshness pre-flight, atom-branch audit</name>
  <files>.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-EVIDENCE.md</files>
  <read_first>
    - `.github/workflows/` — list it live at this moment, both YAML extensions; the count is derived here, never carried
    - `.planning/phases/139-governance-carve-fetch-depth-retrofit-shallow-job-repair/139-06-SUMMARY.md:110-145` — the branch retention instruction, the recorded run ids, and the display-name-versus-job-key trap
    - `144-CONTEXT.md` D-18, D-19, D-20, D-23
  </read_first>
  <action>
Prepare everything the owner needs, and nothing the executor may run.

Enumerate the workflow directory live, both extensions, and list every workflow file by name. Assert
the count is 17 — sixteen predecessors plus the one this phase authored. If the count differs, that is
a finding, not an adjustment: report it rather than reconciling to the expected number.

Emit the exact dispatch command block, one line per workflow, in the deterministic order the listing
produced, each targeting the default branch. Emit it as text for the owner to run — do not run it.

Freshness pre-flight, as a read-only fetch plus a count: fetch, then count commits reachable from the
remote head but not from local master and assert zero. The stored fetch state is days old and the
ahead/behind figure was never re-measured this session, so a fresh count is mandatory before the push.
Record both the ahead and behind counts and the resulting local head SHA — that SHA becomes the shared
close SHA for all three axes once pushed.

Atom-branch audit, report only. Record: the branch is fully merged (zero commits reachable from it but
not from master), it is a remote ref with no reflog to recover from, and its tip is also the head SHA
of the sixteen recorded runs on which two requirements' completion evidence rests. Present the
keep/delete question to the owner at the checkpoint. Do not delete it, and do not recommend deletion
as a foregone conclusion — the standing instruction from the phase that created it says keep and
re-confirm.

Append all of it to `144-EVIDENCE.md`.
  </action>
  <acceptance_criteria>
    - The live listing of both workflow extensions yields exactly 17 files, each named in the evidence.
    - A ready-to-paste dispatch command block with exactly 17 lines is recorded, targeting the default branch.
    - The fresh behind-count is recorded and is 0; the ahead-count and the local head SHA are recorded.
    - `git rev-list --count master..phase-139-atom-5` is recorded as 0, alongside the branch tip SHA and the run-evidence coupling.
    - `git rev-parse origin/master` is recorded and is unchanged from its value at the start of the plan — the executor pushed nothing.
  </acceptance_criteria>
  <verify>
    <automated>git rev-list --count master..origin/master</automated>
  </verify>
  <done>The 17 workflows are enumerated live, the dispatch block is written out for the owner, the freshness pre-flight is clean, and the atom branch is audited without being touched.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: OWNER checkpoint — push, dispatch, and the atom-branch disposition</name>
  <decision>The owner executes the push and the 17-workflow dispatch, and rules on the Phase 139 atom branch. The executor runs none of these commands and does not self-approve.</decision>
  <context>
Two irreversible doors sit here and both belong to the owner.

The push is one-way: there is no un-push, and it is the precondition for dispatch, because a
manual-dispatch workflow must already exist on the default branch to be dispatchable. The project's
own prior milestone audit bars pushing and firing Actions from inside an unattended execution run;
authorization alone does not move the executor across that boundary. The precedent to copy is the
earlier phase in this same milestone, where the owner pre-authorized the action and the checkpoint was
still honoured as a real halt with no self-approval.

The atom-branch deletion is one-way in a sharper sense: it is a remote ref, so there is no reflog.
The branch is fully merged, but its tip is also the head SHA of the sixteen recorded runs that carry
two requirements' completion evidence, and the phase that created it recorded an explicit instruction
to keep it and re-confirm the disposition here.

Everything the owner needs is in `144-EVIDENCE.md` from Task 2: the freshness pre-flight result, the
local head SHA that becomes the shared close SHA, the 17 named workflows, and the ready-to-paste
dispatch block.

Executor instructions at this checkpoint: HALT. Do not run the push. Do not run any dispatch. Do not
delete or modify any remote ref. Return the evidence and wait.
  </context>
  <options>
    <option id="push-dispatch-keep-branch">
      <name>Owner pushes and dispatches all 17; the atom branch is KEPT</name>
      <pros>Preserves the ref whose tip is the head SHA of the sixteen runs carrying two requirements' completion evidence; matches the standing instruction from the phase that created it; costs nothing to defer.</pros>
      <cons>A merged branch lingers on the remote alongside three older ones of the same class, which a successor phase must eventually sweep.</cons>
    </option>
    <option id="push-dispatch-delete-branch">
      <name>Owner pushes and dispatches all 17; the atom branch is DELETED</name>
      <pros>Closes the branch-hygiene item at this milestone rather than carrying it forward.</pros>
      <cons>One-way with no reflog; the deleted tip is the head SHA cited by two requirements' completion evidence, so the citation would point at a ref that no longer resolves by name.</cons>
    </option>
    <option id="halt">
      <name>Do not push yet</name>
      <pros>Appropriate if the pre-push census surfaced anything unresolved; the phase can absorb one more remediation round before the shared SHA is fixed.</pros>
      <cons>HARN-19 cannot progress: no push means no dispatch, no shared SHA, and no evidence for any of the three axes.</cons>
    </option>
  </options>
  <resume-signal>Reply with the chosen option id, plus the pushed head SHA and confirmation that all 17 dispatches were fired, so Plan 10 can assert the read-back against that exact SHA.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository → public remote | The push publishes 96+ commits to a public remote; it is the phase's first irreversible external action. |
| executor autonomy → owner authority | The push, the dispatch and the remote-ref deletion are owner actions; the boundary is procedural, not technical, and therefore has to be enforced by the plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-34 | Elevation of Privilege | executor performing owner actions | high | mitigate | Blocking checkpoint with explicit halt instructions; a must_have asserts the remote head is unchanged across the executor's own work |
| T-144-35 | Tampering | irreversible remote-ref deletion | high | mitigate | Audit-and-report only; the keep/delete call is an owner option at the checkpoint, with the reflog absence and the evidence coupling stated in the option's own cons |
| T-144-36 | Repudiation | stale ahead/behind figures | medium | mitigate | A fresh fetch and a fresh count immediately before the push, recorded; the stored fetch state is days old |
| T-144-37 | Information Disclosure | pushing local working state | medium | accept | The untracked entries and agent branches are outside the gate's scope and are not pushed; the ruling is recorded so Axis-1 and Axis-3 read against a known state |
</threat_model>

<verification>
- All ten standalone-red members exit 0 at the close SHA with both skips classified.
- Class-1 archival drift: zero survivors, frozen-reader call sites subtracted and named.
- Working-tree state recorded and ruled on.
- 17 workflows enumerated live; a 17-line dispatch block written out for the owner.
- Fresh behind-count is zero; the local head SHA is recorded as the prospective shared close SHA.
- The atom branch is audited and untouched; the remote head is unchanged until the owner acts.
</verification>

<success_criteria>
The owner has everything needed to push and dispatch at one shared SHA, the pre-push state is proven
green rather than assumed, and the executor has crossed no irreversible boundary.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files (this plan): the pre-push readiness, enumeration and atom-branch sections of
`.planning/phases/144-*/144-EVIDENCE.md`.
New files (phase-wide): `check-phase-139.mjs` … `-144.mjs`, `v1.20-milestone-audit.mjs`,
`v1.20-audit-allowlist.json`, `audit-harness-v1.20-integrity.yml`, `v1.20-MILESTONE-AUDIT.md`,
`dist/docs-library-v1.20.zip` (gitignored).
New exports: `readAtV119Close`, `lsTreeAtV119Close`.
New constants: `MILESTONE_CLOSE_SHAS.V119`, `BASELINE_24`, and the apex's span and sidecar constants.
New CI job keys: `parse`, `path-match`, `harness-run`, `linux-chain-ubuntu-latest`,
`check-phase-144`, `check-phase-139` … `check-phase-143`, `rotting-external-quarterly`,
`pin-helper-advisory`, `frozen-read-probe`.
Modified across the phase: `_lib/frozen-at-close.mjs`, `v1.19-milestone-audit.mjs`,
`check-phase-67.mjs`, `regenerate-supervision-pins.mjs`, `audit-harness-v1.7-integrity.yml`,
`audit-harness-v1.8-integrity.yml`, `v1.20-CARVE.md`, `v1.20-GOV-02-LEDGER.md`,
`v1.20-DEFERRED-CLEANUP.md`, `.planning/{PROJECT,ROADMAP,STATE,REQUIREMENTS}.md`.
</artifacts_produced>

<output>
Create `.planning/phases/144-v119-pin-18th-path-a-lineage-bump-terminal-close/144-09-SUMMARY.md` when done
</output>
