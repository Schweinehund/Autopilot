# v1.9 Deferred Cleanup

This file tracks items deliberately omitted from v1.9 content pending verification, to be resolved in v1.10+. Each entry records the omitted item, the reason for omission, where it belongs when resolved, and the requirement linkage.

---

## PSSO-FUT-01: NewUserAuthorizationMode MDM Payload Key Literal

**Item omitted:** The `NewUserAuthorizationMode` MDM plist key literal from the NUAL (New User At Login Window) section of `docs/admin-setup-macos/08-auth-methods-deep-dive.md`.

**Omission reason:** LOW research confidence -- the MDM payload key name was not confirmed from an authoritative Settings Catalog payload schema source. The official Intune Platform SSO scenarios documentation shows the Settings Catalog UI display name as "New User Authorization Mode" with supported values Standard / Admin / Groups, but the underlying MDM plist key name (`NewUserAuthorizationMode`) was not confirmed from the Settings Catalog payload schema or an authoritative Apple MDM specification in this session.

**What IS documented in guide 08:** The NUAL section documents the behavior, the macOS 14+ version gate, Shared Device Keys enablement, `com.apple.PlatformSSO.AccountShortName` account-name mapping, and the display names of the NUAL Settings Catalog settings (Enable Create User At Login, New User Authorization Mode, User Authorization Mode). The key literal is omitted.

**Where it belongs when resolved:** `docs/admin-setup-macos/08-auth-methods-deep-dive.md`, NUAL section -- specifically in the Settings Catalog settings table and any configuration guidance that currently uses the display name only.

**Requirement linkage:**
- PSSO-11 (deferred arm) -- New User At Login Window documentation; the MDM key literal is the deferred component
- PSSO-FUT-01 -- REQUIREMENTS.md lines 70, 88 -- tracks this as a future verification item

**Resolution trigger:** Verify the exact MDM plist key name (`NewUserAuthorizationMode` or other) against:
1. The live Settings Catalog payload schema in Microsoft Learn or the Intune MDM reference
2. `https://learn.microsoft.com/en-us/intune/device-configuration/settings-catalog/configure-platform-sso-scenarios-macos` (current as of research 2026-06-21)
3. Apple MDM protocol specification or Apple Platform Deployment documentation for the `com.apple.extensiblesso` payload

Once confirmed, document the key literal in guide 08's NUAL section Settings Catalog table and update this entry to mark it resolved.

**Tracked by:** Phase 77 execution 2026-06-21 (PSSO-11 / D-03=B decision -- LOW confidence on MDM key name).
