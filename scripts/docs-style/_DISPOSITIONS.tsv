# Dispositions for verify-meaning.py.  Format:  <id><TAB><reason>
#
# A disposition silences ONE finding. The id is a hash of (kind, file, evidence
# text), so it goes stale the moment that line changes again -- a documented keep
# must never silence a future edit to the same line. That is deliberate.
#
# Disposition only what you have actually adjudicated. A residual that is
# correct-by-ruling is normal on this corpus; a residual nobody read is not.
# If a whole class starts needing dispositions, the RULE is wrong -- fix the rule.
#
# Seeded with the one keep the project has already ratified in writing:
1a5a0524bc2a	RATIFIED (RESUME.md, batch-8 rulings): admin-template-android.md carries the same example "What breaks" sentence at line 64 inside the <!-- --> authoring block and at line 86 in the body. Restyling only the visible one would propagate `admin` back into every doc generated from this template, so BOTH were edited. RESUME records this as "the one accepted HTML-comment drift in the whole pass." No validator matches it.
2b9b87aded2d	FALSE POSITIVE, read in context (admin-setup-android/11-aosp-pico.md:83). Baseline: "are NOT on the supported list and will fail Intune AOSP enrollment"; pass dropped "will". The rule's premise -- one modal governing two verbs, only the first converted -- does not hold here: "are" and "fail" are two coordinated finite present-tense verbs, both governed by the same subject, and "fail" is not a bare infinitive under a dropped modal. The sentence is grammatical and states the same fact. Do not "fix" correct prose.
268b3b1cb0df	KEEP (_glossary-android.md). Corpus scare quote, not a vendor citation: "user-controlled hidden personal-side partition that admin policies cannot reach" is this library's own gloss of Android 15 private space, in a "Cross-platform note" the corpus authored. No Google/Microsoft page carries that sentence. `admin` -> `administrator` inside it is terminology consistency, not falsification.
6b1534ddf400	KEEP (admin-setup-ios/06-compliance-policy.md). Corpus scare quote, not a UI label and not a vendor sentence: "enforce via config profile" is the author's shorthand for a capability that does not exist as an Intune control, used to say iOS compliance settings have no such paired equivalent. Verified no Intune blade carries this string. Expanded to "enforce through configuration profile" for consistency with the rest of the corpus.
5122a4be1df3	KEEP (admin-setup-macos/07-platform-sso-setup.md:343). Corpus rhetorical question, not a citation: 'The deciding question is "who creates the macOS account," not "did we use the enrollment profile"'. First person -> second person is the Google second-person rule applied to the corpus's own words. Meaning identical.
fb6e0e5874f4	KEEP (cross-platform/apple-business/_admin-directory.md). Corpus scare quote of a lookup question L1 staff ask -- "which administrator owns this device pool" -- not a vendor sentence or a UI string. `admin` -> `administrator` matches the rest of the pass.
